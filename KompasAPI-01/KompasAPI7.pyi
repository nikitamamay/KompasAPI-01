"""
Интерфейсы Компас API версии 7.
"""

#
# Файл 'KompasAPI7.pyi'
# сгенерирован автоматически 2026-03-09 17:42:16
# с помощью 'https://github.com/nikitamamay/KompasAPI-stubs-generator'
#


import typing

from win32com.client import DispatchBaseClass as IDispatch


class IKompasAPIObject(IDispatch):  # ikompasapiobject.html
    """
    ## Интерфейс IKompasAPIObject

    Базовый объект КОМПАС API (скрытый).

    Данный интерфейс является базовым для всех интерфейсов КОМПАС API, кроме интерфейсов событий и некоторых вспомогательных интерфейсов.
    """

    @property
    def Application(self) -> IApplication:  # ikompasapiobject_application.html
        """
        ## Application - Ссылка на приложение

        Тип данных: указатель на интерфейс IApplication.

        Свойство доступно только для чтения. Возвращает указатель на интерфейс приложения КОМПАС.

        ### Синтаксис Automation

        ```
        Application = iObject.Application       # Получить свойство(* )
        Application = iObject.GetApplication()  # Получить свойство(**)
        ```
        """
        ...

    @property
    def Parent(self) -> IKompasAPIObject:  # ikompasapiobject_parent.html
        """
        ## Parent - Ссылка на владельца

        Тип данных: указатель на интерфейс IKompasAPIObject.

        Свойство доступно только для чтения. Возвращает указатель на интерфейс, у которого был взят данный объект.

        ### Синтаксис Automation

        ```
        Parent = iObject.Parent       # Получить свойство(* )
        Parent = iObject.GetParent()  # Получить свойство(**)
        ```
        """
        ...

    @property
    def Reference(self) -> int:  # ikompasapiobject_reference.html
        """
        ## Reference - Уникальный идентификатор объекта

        Тип данных: long.

        Свойство доступно только для чтения. Является скрытым; недоступно в таких языках как VB и т.п.

        ### Синтаксис Automation

        ```
        Reference = iObject.Reference       # Получить свойство(* )
        Reference = iObject.GetReference()  # Получить свойство(**)
        ```
        """
        ...

    @property
    def Type(self) -> int:  # ikompasapiobject_type.html
        """
        ## Type - Тип объекта

        Тип данных: тип объекта из перечисления KompasAPIObjectTypeEnum.

        Свойство доступно только для чтения. Позволяет определить тип объекта и его интерфейс.

        ### Синтаксис Automation

        ```
        Type = iObject.Type       # Получить свойство(* )
        Type = iObject.GetType()  # Получить свойство(**)
        ```
        """
        ...




class IKompasCollection(IKompasAPIObject):  # ikompascollection.html
    """
    ## Интерфейс IKompasCollection

    Базовая коллекция КОМПАС API (скрытый).

    Данный интерфейс является базовым для всех интерфейсов коллекций КОМПАС API.

    Индекс первого элемента коллекции 0.
    """

    @property
    def Count(self) -> int:  # ikompascollection_count.html
        """
        ## Count - Количество элементов в коллекции

        Тип данных: long.

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        Count = iObject.Count       # Получить свойство(* )
        Count = iObject.GetCount()  # Получить свойство(**)
        ```
        """
        ...




class IApplication(IKompasAPIObject):  # iapplication.html
    """
    ## Интерфейс IApplication

    Интерфейс приложения КОМПАС-3D.

    1. Приложение КОМПАС-3D регистрируется с ProgID «KOMPAS.Application.7».

    2. После запуска приложения из-под внешнего контроллера оно работает в слепом режиме. Чтобы оно стало видимым, нужно установить свойству IApplication::Visible значение TRUE.

    3. Данный интерфейс приложения можно получить у любого объекта с помощью свойства IKompasAPIObject::Application.

    4. Интерфейс приложения может быть создан следующими способами:

    •в С с помощью функций HINSTANCE LoadLibrary(LibFileName), GetProcAddress, CreateKompasApplication(), пример создания интерфейса реализован в функции GetNewKompasAPI() - SDK\\VisualC: Gayka1, Step12, Cube;

    •в Delphi с помощью функции CreateKompasApplication, пример создания интерфейса реализован в функции GetNewKompasAPI() - SDK\\Pascal\\Delphi: Gayka1, Step12, Cube;

    •в VBasic с помощью функции ksGetApplication7, пример создания интерфейса реализован в SDK\\Basic: Gayka1, events.

    5. Посредством вызова метода IUnknown::QueryInterface (const GUID far& iid, void** pif) у данного интерфейса можно получить дополнительные интерфейсы IAttrTypeMng,IPropertyMng и IApplicationLicenseManager.
    """

    @property
    def ActiveDocument(self) -> IKompasDocument:  # iapplication_activedocument.html
        """
        ## ActiveDocument - Получить текущий активный документ

        Тип данных: указатель на интерфейс IKompasDocument.

        Свойство позволяет получить или установить текущий документ, если ни один документ не открыт.

        ### Синтаксис Automation

        ```
        ActiveDocument = iObject.ActiveDocument       # Получить свойство (* )
        iObject.ActiveDocument = ActiveDocument       # Установить свойство (* )
        ActiveDocument = iObject.GetActiveDocument()  # Получить свойство(**)
        iObject.SetActiveDocument (ActiveDocument)    # Установить свойство (**)
        ```
        """
        ...
    @ActiveDocument.setter
    def ActiveDocument(self, value: IKompasDocument): ...

    @property
    def CheckSum(self) -> ICheckSum:  # iapplication_checksum.html
        """
        ## CheckSum - Интерфейс контрольной суммы

        Тип данных: Указатель на интерфейс ICheckSum.

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        ICheckSum * CheckSum = Application.CheckSum       # Получить свойство(* )
        ICheckSum * CheckSum = Application.GetCheckSum()  # Получить свойство(**)
        ```
        """
        ...

    @property
    def CurrentDirectory(self) -> str:  # iapplication_currentdirectory.html
        """
        ## CurrentDirectory - Текущий каталог

        Тип данных: BSTR

        При работе функций КОМПАС может изменяться текущий рабочий каталог приложения.

        Свойство позволяет получить/установить текущий рабочий каталог.

        Версия: Компас v20

        ### Синтаксис Automation

        ```
        CurrentDirectory = Object.CurrentDirectory       # Получить свойство (* )
        Object.CurrentDirectory = CurrentDirectory       # Установить свойство (* )
        CurrentDirectory = Object.GetCurrentDirectory()  # Получить свойство(**)
        Object.SetCurrentDirectory( CurrentDirectory )   # Установить свойство (**)
        ```
        """
        ...
    @CurrentDirectory.setter
    def CurrentDirectory(self, value: str): ...

    @property
    def Documents(self) -> IDocuments:  # iapplication_documents.html
        """
        ## Documents - Коллекция открытых документов в приложении

        Тип данных: указатель на интерфейс коллекции документов IDocuments.

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        Documents = iObject.Documents       # Получить свойство(* )
        Documents = iObject.GetDocuments()  # Получить свойство(**)
        ```
        """
        ...

    @property
    def HideMessage(self) -> int:  # iapplication_hidemessage.html
        """
        ## HideMessage - Скрывать/показывать сообщения

        Тип данных: константа из перечисления ksHideMessageEnum.

        1. После использования флагов ksHideMessageYes или ksHideMessageNo в прикладной библиотеке необходимо предусматривать взведение флага ksShowMessage по завершении работы библиотеки или команды библиотеки. В противном случае сообщения останутся подавленными в визуальном режиме работы с системой КОМПАС.

        2. Константа ksHideMessageYes позволяет выполнять перестроения документа, скрывая все запросы на перестроение.

        3. Константа ksHideMessageNo позволяет не выполнять перестроения документа, скрывая все запросы на перестроение.

        4. Для всех сообщений с одной кнопкой ОК константы ksHideMessageYes и ksHideMessageNo одинаковым образом скрывают сообщения с обработкой нажатия ОК.

        5. Для всех запросов с выбором ДА и НЕТ константа ksHideMessageYes позволяет скрыть запрос с обработкой выбора варианта ДА, константа ksHideMessageNo позволяет скрыть запрос с обработкой выбора варианта НЕТ.

        6. При открытии спецификации, когда отсутствует файл graphic.lyt, константы ksHideMessageYes и ksHideMessageNo позволяют скрыть запрос на выбор нового стиля с отказом от выбора нового стиля.

        7. При открытии спецификации, когда имеется рассогласование с объектами, константа ksHideMessageYes позволяет скрыть запрос Обнаружено рассогласование между спецификацией и сборкой с выбором варианта Взять объект из сборки и скрыть запрос В сборке отсутствует объект с выбором варианта Восстановить в сборке. Константа ksHideMessageNo позволяет скрыть запрос Обнаружено рассогласование между спецификацией и сборкой с выбором варианта Взять объект из спецификации, и скрыть запрос В сборке отсутствует объект с выбором варианта Удалить из спецификации.

        8. При открытии спецификации, когда отсутствует файл чертежа, связанного со спецификацией, константа ksHideMessageYes позволяет скрыть сообщение с удалением чертежа из спецификации, константа ksHideMessageNo позволяет скрыть сообщение без удаления чертежа из спецификации.

        9. При открытии чертежа со вставкой внешних фрагментов, когда фрагменты отсутствуют, константа ksHideMessageYes позволяет скрыть запрос с обработкой выбора варианта удаления из чертежа отсутствующих фрагментов, константа ksHideMessageNo позволяет скрыть запрос с обработкой выбора варианта игнорировать отсутствующие фрагменты.

        10. При открытии ассоциативного чертежа, когда файл модели, связанной с чертежом, отсутствует, константа ksHideMessageYes позволяет скрыть запрос с обработкой выбора варианта удаления из чертежа видов, связанных с отсутствующими моделями, константа ksHideMessageNo позволяет скрыть запрос с обработкой выбора варианта игнорировать отсутствующие модели.

        ### Синтаксис Automation

        ```
        ksHideMessageEnum HideMessage = Application.HideMessage       # Получить свойство (* )
        Application.HideMessage = HideMessage                         # Установить свойство (* )
        ksHideMessageEnum HideMessage = Application.GetHideMessage()  # Получить свойство(**)
        Application.SetHideMessage (HideMessage)                      # Установить свойство (**)
        ```
        """
        ...
    @HideMessage.setter
    def HideMessage(self, value: int): ...

    @property
    def KompasError(self) -> IKompasError:  # iapplication_kompaserror.html
        """
        ## KompasError - Информация об ошибке системы КОМПАС

        Тип данных: указатель на интерфейс IKompasError информации о ошибке системы КОМПАС.

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        KompasError = iObject.KompasError       # Получить свойство(* )
        KompasError = iObject.GetKompasError()  # Получить свойство(**)
        ```
        """
        ...

    @property
    def LibraryManager(self) -> ILibraryManager:  # iapplication_librarymanager.html
        """
        ## LibraryManager - Менеджер библиотек

        Тип данных: указатель на интерфейс ILibraryManager менеджера библиотек.

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        LibraryManager = iObject.LibraryManager       # Получить свойство(* )
        LibraryManager = iObject.GetLibraryManager()  # Получить свойство(**)
        ```
        """
        ...

    @property
    def Math2D(self) -> IMath2D:  # iapplication_math2d.html
        """
        ## Math2D - Интерфейс 2D математики

        Тип данных:указатель на интерфейс IMath2D.

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        Math2D = Object.Math2D       # Получить свойство(* )
        Math2D = Object.GetMath2D()  # Получить свойство(**)
        ```
        """
        ...

    @property
    def PrintJob(self) -> IPrintJob:  # iapplication_printjob.html
        """
        ## PrintJob - Интерфейс задания на печать

        Тип данных:указатель на интерфейс IPrintJob

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        PrintJob = Object.PrintJob       # Получить свойство(* )
        PrintJob = Object.GetPrintJob()  # Получить свойство(**)
        ```
        """
        ...

    @property
    def ProgressBarIndicator(self) -> IProgressBarIndicator:  # iapplication_progressbarindicator.html
        """
        ## ProgressBarIndicator - Интерфейс индикатора прогресса

        Тип данных: указатель на интерфейс IProgressBarIndicator.

        ### Синтаксис Automation

        ```
        IProgressBarIndicator * ProgressBarIndicator = Application.ProgressBarIndicator       # Получить свойство(* )
        IProgressBarIndicator * ProgressBarIndicator = Application.GetProgressBarIndicator()  # Получить свойство(**)
        ```
        """
        ...

    @property
    def SystemSettings(self) -> ISystemSettings:  # iapplication_systemsettings.html
        """
        ## SystemSettings - Интерфейс параметров системы

        Тип данных: указатель на интерфейс ISystemSettings.

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        ISystemSettings * SystemSettings = Application.SystemSettings       # Получить свойство(* )
        ISystemSettings * SystemSettings = Application.GetSystemSettings()  # Получить свойство(**)
        ```
        """
        ...

    @property
    def Visible(self) -> bool:  # iapplication_visible.html
        """
        ## Visible - Видимость приложения

        Тип данных: BOOL

        Позволяет получить и установить свойство видимости приложения КОМПАС-3D.

        ### Синтаксис Automation

        ```
        Visible = iObject.Visible       # Получить свойство (* )
        iObject.Visible = Visible       # Установить свойство (* )
        Visible = iObject.GetVisible()  # Получить свойство(**)
        iObject.SetVisible (Visible)    # Установить свойство (**)
        ```
        """
        ...
    @Visible.setter
    def Visible(self, value: bool): ...

    def ApplicationName(self, FullName) -> bool:  # iapplication_applicationname.html
        """
        ## ApplicationName - Имя приложения

        Тип данных: BOOL.

        ### Синтаксис Automation

        ```
        ApplicationName = Object.ApplicationName( FullName )     # Получить свойство(* )
        ApplicationName = Object.GetApplicationName( FullName )  # Получить свойство(**)
        ```

        ### Входные параметры

        +-----------------------------------------------------------------------+
        | BOOL  | - FullName- полное имя,                                       |
        +-----------------------------------------------------------------------+
        | TRUE  | - полное отображаемое имя приложения в заголовке окна Компас, |
        +-----------------------------------------------------------------------+
        | FALSE | - нелокализованное английское имя.                            |
        +-----------------------------------------------------------------------+
        """
        ...

    def Converter(self, Library) -> IConverter:  # iapplication_converter.html
        """
        ## Converter - Конвертация документов КОМПАС

        Тип данных: указатель на интерфейс IConverter конвертера файлов КОМПАС.

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        Converter = iObject.Converter (Library)     # Получить свойство(* )
        Converter = iObject.GetConverter (Library)  # Получить свойство(**)
        ```

        ### Входные параметры

        +----------------------------------------------------+
        | Library | - полный путь к библиотеке, тип VARIANT. |
        +----------------------------------------------------+
        """
        ...

    def CreateProcessParam(self) -> IProcessParam:  # iapplication_createprocessparam.html
        """
        ## CreateProcessParam - Получить интерфейс параметров процесса

        Данный метод создает новые параметры процесса и возвращает их. Параметры будут удалены после их освобождения библиотекой, поэтому библиотека должна держать ссылку на параметры все время от начала до завершения их использования. При повторном вызове этого метода будут созданы новые параметры.

        ### Синтаксис Automation

        ```
        LPDISPATCH CreateProcessParam();
        ```

        ### Возвращаемое значение

        +-------------------------------------------------------------------------+
        | указатель на интерфейс IProcessParam | - в случае успешного завершения, |
        +-------------------------------------------------------------------------+
        | 0                                    | - в случае неудачи.              |
        +-------------------------------------------------------------------------+
        """
        ...

    def CreatePropertyManager(self, NewManager) -> IPropertyManager:  # iapplication_createpropertymanager.html
        """
        ## CreatePropertyManager - Создать Панель свойств

        1. При получении интерфейса доступа к панели свойств процесса системы КОМПАС-3D будет возможно добавление собственных вкладок с элементами управления. Все остальные вкладки и их элементы управления будут недоступны.

        2. Библиотечная (создаваемая библиотекой) панель свойств и/или создаваемые библиотекой вкладки на панели свойств процесса системы КОМПАС-3D будут удалены после освобождения библиотекой их интерфейса, поэтому библиотека должна держать ссылку на интерфейс панели свойств все время от начала до завершения ее использования.

        3. При повторном вызове этого метода будет будет создана новая библиотечная (создаваемая библиотекой) панель свойств и/или создаваемые библиотекой вкладки на панели свойств процесса системы КОМПАС-3D.

        ### Синтаксис Automation

        ```
        LPDISPATCH CreatePropertyManager (BOOL newManager);
        ```

        ### Входные параметры

        +-----------------------------------------------------------------------------------------------------------------------------------------------------------------+
        | newManager | - тип создаваемой Панели свойств: TRUE - библиотечная (создаваемая библиотекой) панель свойств, FALSE - панель свойств процесса системы КОМПАС-3D. |
        +-----------------------------------------------------------------------------------------------------------------------------------------------------------------+

        ### Возвращаемое значение

        указатель на интерфейс IPropertyManager Панели свойств.
        """
        ...

    def ExecuteKompasCommand(self, CommandID, PostMessage) -> bool:  # iapplication_executekompascommand.html
        """
        ## ExecuteKompasCommand - Выполнить команду системы КОМПАС

        1. Проверить доступность команды можно с помощью функции IApplication::IsKompasCommandEnable.

        2. Проверить, нажата ли в данный момент кнопка команды, можно с помощью IApplication::IsKompasCommandCheck.

        ### Синтаксис Automation

        ```
        BOOL ExecuteKompasCommand (long commandID, BOOL post);
        ```

        ### Входные параметры

        +-------------------------------------------------------------------------------------+
        | commandID | - константа из перечисления ProcessTypeEnum или ksKompasCommandEnum.    |
        +-------------------------------------------------------------------------------------+
        | post      | - true - запуск команды через PostMessage, - false - через SendMessage. |
        +-------------------------------------------------------------------------------------+

        ### Возвращаемое значение

        +----------------------------+
        | TRUE  | - в случае удачи,  |
        +----------------------------+
        | FALSE | - в случае ошибки. |
        +----------------------------+
        """
        ...

    def GetSystemVersion(self) -> bool:  # iapplication_getsystemversion.html
        """
        ## GetSystemVersion - Получить версию системы

        ### Синтаксис Automation

        ```
        BOOL IsKompasCommandEnable (long commandID);
        ```

        ### Входные параметры

        +--------------------------------------------------+
        | iMajor   | - старшее слово версии,               |
        +--------------------------------------------------+
        | iMinor   | - младшее слово версии,               |
        +--------------------------------------------------+
        | iRelease | - номер выпуска внутри одной версии,  |
        +--------------------------------------------------+
        | iBuild   | - номер сборки внутри одного выпуска. |
        +--------------------------------------------------+

        Версия КОМПАС v22

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def IsKompasCommandCheck(self, CommandID):  # iapplication_iskompascommandcheck.html
        """
        ## IsKompasCommandCheck - Проверить, нажата ли кнопка команды

        ### Синтаксис Automation

        ```
        long IsKompasCommandCheck (long commandID);
        ```

        ### Входные параметры

        +----------------------------------------------------------------------------------+
        | commandID | - константа из перечисления ProcessTypeEnum или ksKompasCommandEnum. |
        +----------------------------------------------------------------------------------+

        ### Возвращаемое значение

        +----------------------+
        | 1 | - кнопка нажата, |
        +----------------------+
        | 0 | - кнопка отжата. |
        +----------------------+
        """
        ...

    def IsKompasCommandEnable(self, CommandID) -> bool:  # iapplication_iskompascommandenable.html
        """
        ## IsKompasCommandEnable - Проверить доступность выполнения команды

        ### Синтаксис Automation

        ```
        BOOL IsKompasCommandEnable (long commandID);
        ```

        ### Входные параметры

        +----------------------------------------------------------------------------------+
        | commandID | - константа из перечисления ProcessTypeEnum или ksKompasCommandEnum. |
        +----------------------------------------------------------------------------------+

        ### Возвращаемое значение

        +-------------------------------+
        | TRUE  | - команда доступна,   |
        +-------------------------------+
        | FALSE | - команда недоступна. |
        +-------------------------------+
        """
        ...

    def LibraryStyles(self, Path, StylesType) -> IStyles:  # iapplication_librarystyles.html
        """
        ## LibraryStyles - Стили из библиотеки

        Тип данных: Указатель на интерфейс IStyles

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        LibraryStyles = Object.LibraryStyles( Path, StylesType )     # Получить свойство(* )
        LibraryStyles = Object.GetLibraryStyles( Path, StylesType )  # Получить свойство(**)
        ```
        """
        ...

    def MessageBoxEx(self, Text, Caption, Flags):  # iapplication_messageboxex.html
        """
        ## MessageBoxEx - Выдать всплывающее сообщение

        Параметр Flags и возвращаемое значение соответствует аналогичной функции WinApi MessageBox.

        Дополнительно функция позволяет выдать всплывающее сообщение если в настройках Компас установлен признак Сервис->Параметры->Система->Общее->Всплывающие сообщения->Использовать всплывающие сообщения вместо информационных диалогов.

        Сообщение выдается всплывающим, если используется комбинация флагов MB_OK | MB_ICONINFORMATION или MB_OK | MB_ICONWARNING.

        Если установлены какие-либо другие флаги, то сообщение выдается как обычное сообщение типа MessageBox.

        Если не задан заголовок сообщения, выдается умолчательный заголовок - «Сообщение библиотеки».

        Если не задан текст сообщения, выдается сообщение о текущей ошибке аналогично функции MessageBoxResult.

        ### Синтаксис Automation

        ```
        long MessageBoxEx( BSTR Text, BSTR Caption, long Flags )
        ```

        ### Входные параметры

        +----------------------------------+
        | Text    | - текст сообщения,     |
        +----------------------------------+
        | Caption | - заголовок сообщения, |
        +----------------------------------+
        | Flags   | - флаги.               |
        +----------------------------------+
        """
        ...

    def MessageDlg(self, Parent, Text, Caption, Explanation, Flags, PositiveButton, NegativeButton, CancelButton, HelpId, HelpFileName):  # iapplication_messagedlg.html
        """
        ## MessageDlg - Выдать модельное сообщение

        1. Параметр Flags и возвращаемое значение соответствует аналогичной функции WinApi MessageBox.

        2. Тексты кнопок не обязательные параметры.

        3. Если тексты не заданы, используются названия кнопок по умолчанию.

        4. Наличие кнопок в диалоге зависит от параметра Flags.

        ### Синтаксис Automation

        ```
        long MessageDlg( OLE_HANDLE Parent, BSTR Text, BSTR Caption, BSTR Explanation, long Flags, BSTR PositiveButton, BSTR NegativeButton, BSTR CancelButton, long HelpId, BSTR HelpFileName );
        ```

        ### Входные параметры

        +-------------------------------------------------------------+
        | Parent         | - дескриптор родительского окна,           |
        +-------------------------------------------------------------+
        | Text           | - текст сообщения,                         |
        +-------------------------------------------------------------+
        | Caption        | - заголовок,                               |
        +-------------------------------------------------------------+
        | Explanation    | - дополнительный текст сообщения,          |
        +-------------------------------------------------------------+
        | Flags          | - флаги,                                   |
        +-------------------------------------------------------------+
        | PositiveButton | - текст кнопки для позитивного результата, |
        +-------------------------------------------------------------+
        | NegativeButton | - текст кнопки для негативного результата, |
        +-------------------------------------------------------------+
        | CancelButton   | - текст кнопки для отмены,                 |
        +-------------------------------------------------------------+
        | HelpId         | - идентификатор справки,                   |
        +-------------------------------------------------------------+
        | HelpFileName   | - имя файла справки.                       |
        +-------------------------------------------------------------+

        ### Возвращаемое значение

        - идентификатор нажатой в диалоге кнопки.
        """
        ...

    def Quit(self):  # iapplication_quit.html
        """
        ## Quit - Закрыть приложение

        Используется при работе из-под внешнего контроллера.

        ### Синтаксис Automation

        ```
        void Quit();
        ```
        """
        ...

    def StopCurrentProcess(self, PostMessage, PDoc):  # iapplication_stopcurrentprocess.html
        """
        ## StopCurrentProcess - Остановить текущий процесс в документе

        1. Если процесс требуется остановить немедленно, то параметр postMessage должен иметь значение FALSE, однако такая остановка не всегда возможна, например, остановка из обработчика события. В этом случае результат обработки события должен быть обработан процессом; процессу надо сообщить, чтобы он остановился после обработки события. Для этого параметру postMessage нужно установить значение TRUE.

        2. Если параметру pDoc установлено значение 0, то процесс будет остановлен в текущем документе.

        ### Синтаксис Automation

        ```
        void StopCurrentProcess (BOOL postMessage, LPDISPATCH pDoc);
        ```

        ### Входные параметры

        +-------------------------------------------------------------------------------------------------+
        | postMessage | - способ остановки процесса,                                                      |
        +-------------------------------------------------------------------------------------------------+
        | pDoc        | - указатель на интерфейс IKompasDocument, в котором требуется остановить процесс. |
        +-------------------------------------------------------------------------------------------------+
        """
        ...




class IApplicationLicenseManager(IDispatch):  # iapplicationlicensemanager.html
    """
    ## Интерфейс IApplicationLicenseManager

    Менеджер лицензий

    Интерфейс является дополнительным к интерфейсу приложения IApplication. Интерфейс можно получить посредством вызова метода IUnknown::QueryInterface (const GUID far& iid, void** pif).

    Версия: Компас v19
    """

    @property
    def KompasVariant(self) -> int:  # iapplicationlicensemanager_kompasvariant.html
        """
        ## KompasVariant - Получение типа установленного дистрибутива в виде комбинации флагов из ksKompasVariantEnum

        Тип данных: long

        Свойство доступно только для чтения.

        См. также ksKompasVariantEnum

        ### Синтаксис Automation

        ```
        KompasVariant = Object.KompasVariant       # Получить свойство (* )
        KompasVariant = Object.GetKompasVariant()  # Получить свойство (**)
        ```
        """
        ...

    def EnableKompasInvisible(self, Key, Signature) -> bool:  # iapplicationlicensemanager_enablekompasinvisible.html
        """
        ## EnableKompasInvisible - Установить ключ для КОМПАС- Invisible

        ### Синтаксис Automation

        ```
        BOOL EnableKompasInvisible( BSTR Key, BSTR Signature );
        ```

        ### Входные параметры

        +--------------------------------------------------------------------------------------+
        | Key       | - имя (передается из внешнего приложения),                               |
        +--------------------------------------------------------------------------------------+
        | Signature | - лицензия для проверки подлинности (передается из внешнего приложения). |
        +--------------------------------------------------------------------------------------+

        ### Возвращаемое значение

        +----------------------------------------+
        | TRUE | - в случае удачного завершения. |
        +----------------------------------------+
        """
        ...

    def KompasModuleActive(self, Module) -> bool:  # iapplicationlicensemanager_kompasmoduleactive.html
        """
        ## KompasModuleActive - Доступность компонентов КОМПАС

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        KompasModuleActive = Object.KompasModuleActive( Module )     # Получить свойство (* )
        Object.KompasModuleActive( Module ) = KompasModuleActive     # Установить свойство (* )
        KompasModuleActive = Object.GetKompasModuleActive( Module )  # Получить свойство (**)
        Object.SetKompasModuleActive( Module, KompasModuleActive )   # Установить свойство (* )
        ```

        ### Входные параметры

        +------------------------------------------------+
        | ksKompasModuleEnum | - Module - Модули Компас. |
        +------------------------------------------------+
        """
        ...

    def LibraryActive(self, ProductNumber) -> bool:  # iapplicationlicensemanager_libraryactive.html
        """
        ## LibraryActive - Доступность продукта по номеру

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        LibraryActive = Object.LibraryActive( ProductNumber )     # Получить свойство (* )
        Object.LibraryActive( ProductNumber ) = LibraryActive     # Установить свойство (* )
        LibraryActive = Object.GetLibraryActive( ProductNumber )  # Получить свойство (**)
        Object.SetLibraryActive( ProductNumber, LibraryActive )   # Установить свойство (* )
        ```

        ### Входные параметры

        +----------------------------------------+
        | long ProductNumber | - номер продукта. |
        +----------------------------------------+
        """
        ...

    def LibraryLocalStatus(self, ProductNumber) -> bool:  # iapplicationlicensemanager_librarylocalstatus.html
        """
        ## LibraryLocalStatus - Признак локальный/сетевой продукт

        Тип данных: BOOL

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        LibraryLocalStatus = Object.LibraryLocalStatus( ProductNumber )     # Получить свойство (* )
        LibraryLocalStatus = Object.GetLibraryLocalStatus( ProductNumber )  # Получить свойство (**)
        ```

        ### Входные параметры

        +----------------------------------------+
        | long ProductNumber | - номер продукта. |
        +----------------------------------------+
        """
        ...

    def LibraryProductKeyInfo(self, ProductNumber) -> str:  # iapplicationlicensemanager_libraryproductkeyinfo.html
        """
        ## LibraryProductKeyInfo - Информация о текущей сессии

        Тип данных: BSTR

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        LibraryProductKeyInfo = Object.LibraryProductKeyInfo(ProductNumber)     # Получить свойство (* )
        LibraryProductKeyInfo = Object.GetLibraryProductKeyInfo(ProductNumber)  # Получить свойство (**)
        ```

        ### Входные параметры

        +----------------------------------------+
        | long ProductNumber | - номер продукта. |
        +----------------------------------------+
        """
        ...

    def LibraryProductName(self, ProductNumber) -> str:  # iapplicationlicensemanager_libraryproductname.html
        """
        ## LibraryProductName - Получить название продукта

        Тип данных: BSTR

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        LibraryProductName = Object.LibraryProductName( ProductNumber )     # Получить свойство (* )
        LibraryProductName = Object.GetLibraryProductName( ProductNumber )  # Получить свойство (**)
        ```

        ### Входные параметры

        +----------------------------------------+
        | long ProductNumber | - номер продукта. |
        +----------------------------------------+
        """
        ...

    def LibraryStatus(self, ProductNumber) -> int:  # iapplicationlicensemanager_librarystatus.html
        """
        ## LibraryStatus - Получить текущий статус продукта по номеру

        Тип данных: из перечисления ksProtectProductStatusEnum.

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        LibraryStatus = Object.LibraryStatus( ProductNumber )     # Получить свойство (* )
        LibraryStatus = Object.GetLibraryStatus( ProductNumber )  # Получить свойство (**)
        ```

        ### Входные параметры

        +----------------------------------------+
        | long ProductNumber | - номер продукта. |
        +----------------------------------------+
        """
        ...

    def LibraryTrialStatus(self, ProductNumber) -> bool:  # iapplicationlicensemanager_librarytrialstatus.html
        """
        ## LibraryTrialStatus - Ознакомительный период

        Тип данных: BOOL

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        LibraryTrialStatus = Object.LibraryTrialStatus( ProductNumber )     # Получить свойство (* )
        LibraryTrialStatus = Object.GetLibraryTrialStatus( ProductNumber )  # Получить свойство (**)
        ```
        """
        ...

    def RegisterLibraryNumber(self, ProductNumber):  # iapplicationlicensemanager_registerlibrarynumber.html
        """
        ## RegisterLibraryNumber - Зарегистрировать номер продукта на сервере лицензий Компас

        Уникальный идентификатор продукта используется для разрегистрации продукта функцией IApplicationLicenseManager::UnRegisterLibraryNumber.

        ### Синтаксис Automation

        ```
        long RegisterLibraryNumber( long ProductNumber );
        ```

        ### Входные параметры

        +----------------------------------------+
        | long ProductNumber | - номер продукта. |
        +----------------------------------------+

        ### Возвращаемое значение

        Уникальный идентификатор продукта.
        """
        ...

    def SetKompasModuleActive(self, Module, arg1):
        ...

    def SetLibraryActive(self, ProductNumber, arg1):
        ...

    def UnRegisterLibraryNumber(self, ProductNumbUnicueId) -> bool:  # iapplicationlicensemanager_unregisterlibrarynumber.html
        """
        ## UnRegisterLibraryNumber - Разрегистрировать номер продукта на сервере лицензий Компас

        ### Синтаксис Automation

        ```
        long UnRegisterLibraryNumber( long ProductNumbUnicueId );
        ```

        ### Входные параметры

        +----------------------------------------+
        | long ProductNumber | - номер продукта. |
        +----------------------------------------+

        ### Возвращаемое значение

        +----------------------------------------+
        | TRUE | - в случае удачного завершения. |
        +----------------------------------------+
        """
        ...




class IPLMObject(IDispatch):  # iplmobject.html
    """
    ## Интерфейс IPLMObject

    Интерфейс объекта системы версионирования.
    """

    @property
    def PLMChange(self) -> int:  # iplmobject_plmchange.html
        """
        ## PLMChange - Отличие в системе версионирования

        Тип данных: из перечисления ksPLMChangesEnum.

        ### Синтаксис Automation

        ```
        PLMChange = Object.PLMChange       # Получить свойство (* )
        Object.PLMChange = PLMChange       # Установить свойство (* )
        PLMChange = Object.GetPLMChange()  # Получить свойство (**)
        Object.SetPLMChange( PLMChange )   # Установить свойство (* )
        ```
        """
        ...
    @PLMChange.setter
    def PLMChange(self, value: int): ...

    @property
    def PLMStatus(self) -> int:  # iplmobject_plmstatus.html
        """
        ## PLMStatus - Статус в системе версионирования

        Тип данных: из перечисления ksPLMStatusEnum

        ### Синтаксис Automation

        ```
        PLMStatus = Object.PLMStatus       # Получить свойство (* )
        Object.PLMStatus = PLMStatus       # Установить свойство (* )
        PLMStatus = Object.GetPLMStatus()  # Получить свойство (**)
        Object.SetPLMStatus( PLMStatus )   # Установить свойство (* )
        ```
        """
        ...
    @PLMStatus.setter
    def PLMStatus(self, value: int): ...

    def PLMPropertyValue(self, Property) -> typing.Any:  # iplmobject_plmpropertyvalue.html
        """
        ## PLMPropertyValue - Значение свойств в системе версионирования

        Тип данных: VARIANT

        ### Синтаксис Automation

        ```
        PLMPropertyValue = Object.PLMPropertyValue( Property )     # Получить свойство (* )
        Object.PLMPropertyValue( Property ) = PLMPropertyValue     # Установить свойство (* )
        PLMPropertyValue = Object.GetPLMPropertyValue( Property )  # Получить свойство (**)
        Object.SetPLMPropertyValue( Property, PLMPropertyValue )   # Установить свойство (* )
        ```

        ### Входные параметры

        +--------------------------------------------------------------+
        | Property | - тип свойства из перечисления ksPLMPropertyEnum. |
        +--------------------------------------------------------------+

        Версия Компас v21
        """
        ...

    def SetPLMPropertyValue(self, Property, arg1):
        ...




class IPLMObjectsManager(IDispatch):  # iplmobjectsmanager.html
    """
    ## Интерфейс IPLMObjectsManager

    Менеджер объектов системы версионирования.
    """

    def PLMChange(self, FileName) -> int:  # iplmobjectsmanager_plmchange.html
        """
        ## PLMChange - Отличие в системе версионирования

        Тип данных: из перечисления ksPLMChangesEnum.

        Свойство позволяет получить/установить признак отличия объектов версионирования по имени файла.

        ### Синтаксис Automation

        ```
        PLMChange = Object.PLMChange( FileName )     # Получить свойство (* )
        Object.PLMChange( FileName ) = PLMChange     # Установить свойство (* )
        PLMChange = Object.GetPLMChange( FileName )  # Получить свойство (**)
        Object.SetPLMChange( FileName, PLMChange )   # Установить свойство (* )
        ```

        ### Входные параметры

        +--------------------------------+
        | BSTR | - FileName - имя файла. |
        +--------------------------------+
        """
        ...

    def PLMPropertyValue(self, Property, FileName) -> typing.Any:  # iplmobjectsmanager_plmpropertyvalue.html
        """
        ## PLMPropertyValue - Значение свойств в системе версионирования

        Тип данных: VARIANT

        ### Синтаксис Automation

        ```
        PLMPropertyValue = Object.PLMPropertyValue( Property, FileName )     # Получить свойство (* )
        Object.PLMPropertyValue( Property, FileName ) = PLMPropertyValue     # Установить свойство (* )
        PLMPropertyValue = Object.GetPLMPropertyValue( Property, FileName )  # Получить свойство (**)
        Object.SetPLMPropertyValue( Property, FileName, PLMPropertyValue )   # Установить свойство (* )
        ```

        ### Входные параметры

        +--------------------------------------------------------------+
        | Property | - тип свойства из перечисления ksPLMPropertyEnum, |
        +--------------------------------------------------------------+
        | FileName | - путь к файлу.                                   |
        +--------------------------------------------------------------+

        Версия Компас v21
        """
        ...

    def PLMStatus(self, FileName) -> int:  # iplmobjectsmanager_plmstatus.html
        """
        ## PLMStatus - Статус в системе версионирования

        Тип данных: из перечисления ksPLMStatusEnum.

        Свойство позволяет получить/установить статус объектов версионирования по имени файла.

        ### Синтаксис Automation

        ```
        PLMStatus = Object.PLMStatus( FileName )     # Получить свойство (* )
        Object.PLMStatus( FileName ) = PLMStatus     # Установить свойство (* )
        PLMStatus = Object.GetPLMStatus( FileName )  # Получить свойство (**)
        Object.SetPLMStatus( FileName, PLMStatus )   # Установить свойство (* )
        ```

        ### Входные параметры

        +--------------------------------+
        | BSTR | - FileName - имя файла. |
        +--------------------------------+
        """
        ...

    def SetPLMChange(self, FileName, arg1):
        ...

    def SetPLMChangesAttrAvailability(self, Available, Enabled) -> bool:  # iplmobjectsmanager_setplmchangesattravailability.html
        """
        ## SetPLMChangesAttrAvailability - Установить доступность в настройках Дерева документа пункта меню “ЛОЦМАН Отличия”

        ### Синтаксис Automation

        ```
        BOOL SetPLMChangesAttrAvailability( BOOL Available, BOOL Enabled );
        ```

        ### Входные параметры

        +---------------------------------------------------------------------------------------------------+
        | Available | - доступность атрибута “ЛОЦМАН Отличия” в контекстном меню настроек Дерева документа, |
        +---------------------------------------------------------------------------------------------------+
        | Enabled   | - видимость в атрибутов “ЛОЦМАН Отличия” в Дереве.                                    |
        +---------------------------------------------------------------------------------------------------+

        ### Возвращаемое значение

        +----------------------------------------+
        | TRUE | - в случае удачного завершения. |
        +----------------------------------------+
        """
        ...

    def SetPLMPropertyAvailability(self, Property, Available, Enabled, ValueList) -> bool:  # iplmobjectsmanager_setplmpropertyavailability.html
        """
        ## SetPLMPropertyAvailability - Управление отображением свойств Лоцман в окне “Параметры”

        ### Синтаксис Automation

        ```
        BOOL SetPLMPropertyAvailability( ksPLMPropertyEnum Property, BOOL Available, BOOL Enabled, VARIANT ValueList );
        ```

        ### Выходные параметры

        +---------------------------------------------------------------+
        | Property  | - тип свойства из перечисления ksPLMPropertyEnum, |
        +---------------------------------------------------------------+
        | Available | - включить отображение свойства,                  |
        +---------------------------------------------------------------+
        | Enabled   | - включить доступность изменения значения,        |
        +---------------------------------------------------------------+
        | ValueList | - список значений.                                |
        +---------------------------------------------------------------+

        Версия Компас v21

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def SetPLMPropertyValue(self, Property, FileName, arg2):
        ...

    def SetPLMStatus(self, FileName, arg1):
        ...

    def SetPLMStatusAttrAvailability(self, Available, Enabled) -> bool:  # iplmobjectsmanager_setplmstatusattravailability.html
        """
        ## SetPLMStatusAttrAvailability - Установить доступность в настройках Дерева документа пункта меню “ЛОЦМАН Статус”

        ### Синтаксис Automation

        ```
        BOOL SetPLMStatusAttrAvailability( BOOL Available, BOOL Enabled );
        ```

        ### Входные параметры

        +--------------------------------------------------------------------------------------------------+
        | Available | - доступность атрибута “ЛОЦМАН Статус” в контекстном меню настроек Дерева документа, |
        +--------------------------------------------------------------------------------------------------+
        | Enabled   | - видимость атрибутов “ЛОЦМАН Статус” в Дереве.                                      |
        +--------------------------------------------------------------------------------------------------+

        ### Возвращаемое значение

        +----------------------------------------+
        | TRUE | - в случае удачного завершения. |
        +----------------------------------------+
        """
        ...




class IDocuments(IKompasCollection):  # idocuments.html
    """
    ## Интерфейс IDocuments

    Коллекция документов, открытых в приложении КОМПАС-3D.

    Данный интерфейс может быть получен от интерфейса приложения IApplication с помощью свойства IApplication::Documents.
    """

    @property
    def DocumentSynchronize(self) -> bool:  # idocuments_documentsynchronize.html
        """
        ## DocumentSynchronize - Синхронизироватьс зависимыми документами

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        DocumentSynchronize = Object.DocumentSynchronize       # Получить свойство (* )
        Object.DocumentSynchronize = DocumentSynchronize       # Установить свойство (* )
        DocumentSynchronize = Object.GetDocumentSynchronize()  # Получить свойство(**)
        Object.SetDocumentSynchronize( DocumentSynchronize )   # Установить свойство (**)
        ```
        """
        ...
    @DocumentSynchronize.setter
    def DocumentSynchronize(self, value: bool): ...

    @property
    def LockTemplatesSettings(self) -> bool:  # idocuments_locktemplatessettings.html
        """
        ## LockTemplatesSettings - Игнорировать настройки шаблонов новых документов при создании документов через функции API

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        LockTemplatesSettings = Object.LockTemplatesSettings       # Получить свойство (* )
        Object.LockTemplatesSettings = LockTemplatesSettings       # Установить свойство (* )
        LockTemplatesSettings = Object.GetLockTemplatesSettings()  # Получить свойство(**)
        Object.SetLockTemplatesSettings( LockTemplatesSettings )   # Установить свойство (**)
        ```
        """
        ...
    @LockTemplatesSettings.setter
    def LockTemplatesSettings(self, value: bool): ...

    @property
    def RecoverError(self) -> int:  # idocuments_recovererror.html
        """
        ## RecoverError - Признак ошибки после открытия файла с восстановлением

        Тип данных: ksRecoverErrorEnum

        ### Синтаксис Automation

        ```
        RecoverError = Object.RecoverError       # Получить свойство (* )
        Object.RecoverError = RecoverError       # Установить свойство (* )
        RecoverError = Object.GetRecoverError()  # Получить свойство(**)
        Object.SetRecoverError( RecoverError )   # Установить свойство (**)
        ```
        """
        ...
    @RecoverError.setter
    def RecoverError(self, value: int): ...

    @property
    def RecoverMode(self) -> bool:  # idocuments_recovermode.html
        """
        ## RecoverMode - Признак открытия файлов в режиме восстановления

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        RecoverMode = Object.RecoverMode       # Получить свойство (* )
        Object.RecoverMode = RecoverMode       # Установить свойство (* )
        RecoverMode = Object.GetRecoverMode()  # Получить свойство(**)
        Object.SetRecoverMode( RecoverMode )   # Установить свойство (**)
        ```
        """
        ...
    @RecoverMode.setter
    def RecoverMode(self, value: bool): ...

    @property
    def RecoverModeErrorList(self) -> typing.Any:  # idocuments_recovermodeerrorlist.html
        """
        ## RecoverModeErrorList - Список ошибок, исправленных при открытии файла с восстановлением

        Тип данных: VARIANT

        Свойство доступно только для чтения.

        Список ошибок возвращается в виде массива VT_ARRAY | VT_I4.

        ### Синтаксис Automation

        ```
        RecoverModeErrorList = Object.RecoverModeErrorList( )    # Получить свойство(* )
        RecoverModeErrorList = Object.GetRecoverModeErrorList()  # Получить свойство(**)
        ```
        """
        ...

    def Add(self, Type, Visible) -> IKompasDocument:  # idocuments_add.html
        """
        ## Add - Создать новый документ и добавить егов коллекцию

        ### Синтаксис Automation

        ```
        LPDISPATCH Add (long Type, BOOL Visible);
        ```

        ### Входные параметры

        +--------------------------------------------------------------------------------------------+
        | Type    | - тип документа из перечисления DocumentTypeEnum,                                |
        +--------------------------------------------------------------------------------------------+
        | Visible | - видимость документа: TRUE создавать документ в видимом режиме, FALSE в слепом. |
        +--------------------------------------------------------------------------------------------+

        ### Возвращаемое значение

        указатель на интерфейс IKompasDocument
        """
        ...

    def AddCustomDocument(self, DocumentTypeId) -> IKompasDocument:  # idocuments_addcustomdocument.html
        """
        ## AddCustomDocument - Создать новый документ по идентификатору и добавить его в коллекцию

        ### Синтаксис Automation

        ```
        LPDISPATCH AddCustomDocument( BSTR DocumentTypeId );
        ```

        ### Возвращаемое значение

        указатель на интерфейс IKompasDocument
        """
        ...

    def AddCustomDocumentEx(self, DocumentTypeId, Visible) -> IKompasDocument:  # idocuments_addcustomdocumentex.html
        """
        ## AddCustomDocumentEx - Создать новый документ и добавить его в коллекцию

        ### Синтаксис Automation

        ```
        LPDISPATCH AddCustomDocumentEx( BSTR DocumentTypeId, BOOL Visible );
        ```

        ### Входные параметры

        +-----------------------------------------------------------------------+
        | DocumentTypeId | - идентификатор специализации документа,             |
        +-----------------------------------------------------------------------+
        | Visible        | - TRUE - видимый документ, FALSE - скрытый документ. |
        +-----------------------------------------------------------------------+

        Версия КОМПАС v22

        ### Возвращаемое значение

        указатель на интерфейс IKompasDocument
        """
        ...

    def AddNewDocumentFromTemplate(self, TemplateFileName) -> IKompasDocument:  # idocuments_addnewdocumentfromtemplate.html
        """
        ## AddNewDocumentFromTemplate - Создать новый документ по шаблону и добавить его в коллекцию

        ### Синтаксис Automation

        ```
        LPDISPATCH AddNewDocumentFromTemplate( BSTR TemplateFileName );
        ```

        ### Возвращаемое значение

        указатель на интерфейс IKompasDocument
        """
        ...

    def AddNewDocumentFromTemplateEx(self, TemplateFileName, Visible) -> IKompasDocument:  # idocuments_addnewdocumentfromtemplateex.html
        """
        ## AddNewDocumentFromTemplateEx - Создать новый документ по шаблону и добавить его в коллекцию

        ### Синтаксис Automation

        ```
        LPDISPATCH AddNewDocumentFromTemplateEx( BSTR TemplateFileName, BOOL Visible );
        ```

        ### Входные параметры

        +-------------------------------------------------------------------------+
        | TemplateFileName | - путь к файлу шаблона,                              |
        +-------------------------------------------------------------------------+
        | Visible          | - TRUE - видимый документ, FALSE - скрытый документ. |
        +-------------------------------------------------------------------------+

        Версия КОМПАС v22

        ### Возвращаемое значение

        указатель на интерфейс IKompasDocument
        """
        ...

    def AddWithDefaultSettings(self, Type, Visible) -> IKompasDocument:  # idocuments_addwithdefaultsettings.html
        """
        ## AddWithDefaultSettings - Создать новый документс параметрами из настроек для новых документов

        ### Синтаксис Automation

        ```
        LPDISPATCH AddWithDefaultSettings( DocumentTypeEnum Type, BOOL Visible );
        ```

        ### Входные параметры

        +------------------------------------------------+
        | Type    | - тип документа DocumentTypeEnum,    |
        +------------------------------------------------+
        | Visible | - TRUE видимый, - FALSE - невидимый. |
        +------------------------------------------------+

        ### Возвращаемое значение

        +-----------------------------------------+
        | индекс линии разрыва,                   |
        +-----------------------------------------+
        | указатель на интерфейс IKompasDocument. |
        +-----------------------------------------+
        """
        ...

    def GetEmbodimentsTree(self, FileName, MarkingType, AddSystemDelimer, AddSpaces) -> str:  # idocuments_getembodimentstree.html
        """
        ## GetEmbodimentsTree - Дерево исполнений

        ### Синтаксис Automation

        ```
        VARIANT GetEmbodimentsTree( BSTR FileName, ksVariantMarkingTypeEnum MarkingType, BOOL AddSystemDelimer, BOOL AddSpaces, long * CurrentEmbodiment );
        ```

        ### Входные параметры

        +-----------------------------------------------------------------------------------------------+
        | FileName          | - Имя файла,                                                              |
        +-----------------------------------------------------------------------------------------------+
        | MarkingType       | - Формат возвращаемых обозначений ksVariantMarkingTypeEnum,               |
        +-----------------------------------------------------------------------------------------------+
        | AddSystemDelimer  | - Возвращать системные разделители в обозначении,                         |
        +-----------------------------------------------------------------------------------------------+
        | AddSpaces         | - Добавить пробелы для определения уровня исполнения в дереве исполнений, |
        +-----------------------------------------------------------------------------------------------+
        | CurrentEmbodiment | - Индекс текущего исполнения в списке исполнений.                         |
        +-----------------------------------------------------------------------------------------------+

        ### Возвращаемое значение

        - Дерево исполнений. Массив узлов дерева исполнений. SAFEARRAY BSTR - (VT_ARRAY | VT_BSTR)
        """
        ...

    def GetLoadCombinations(self, PathName) -> str:  # idocuments_getloadcombinations.html
        """
        ## GetLoadCombinations - Получить массив типов загрузки в виде массива SAFEARRAY BSTR - (VT_ARRAY | VT_BSTR)

        Метод позволяет получить массив типов загрузки документа до открытия документа.

        Метод в настоящее время используется только для сборки 3D. У сборки 3D есть три умолчательных способа загрузки, индексы которых соответствуют перечислению ksLoadStateEnum:

        +-------------------------------------------------------+
        | Полная     | 0 | - полная загрузка,                   |
        +-------------------------------------------------------+
        | Пустая     | 1 | - все компоненты не загружены,       |
        +-------------------------------------------------------+
        | Упрощенная | 2 | - все компоненты загружены частично. |
        +-------------------------------------------------------+

        Кроме этого, могут быть еще и пользовательские типы загрузки.

        ### Синтаксис Automation

        ```
        VARIANT GetLoadCombinations (LPCTSTR PathName, long * CurrentIndex);
        ```

        ### Входные параметры

        +---------------------------------------------------------------------------------------+
        | PathName | - полное имя файла документа, состоящее из пути, имени и расширения файла. |
        +---------------------------------------------------------------------------------------+

        ### Выходные параметры

        +-----------------------------------------------------------------------------------------------+
        | CurrentIndex | - текущий индекс типа загрузки, по которому загружается документ по умолчанию. |
        +-----------------------------------------------------------------------------------------------+

        ### Возвращаемое значение

        тип значения VARIANT - массив типов загрузки в виде массива SAFEARRAY BSTR - (VT_ARRAY | VT_BSTR)
        """
        ...

    def GetLoadCombinationsParam(self, PathName) -> ILoadCombinationsParam:  # idocuments_getloadcombinationsparam.html
        """
        ## GetLoadCombinationsParam - Получить интерфейс типов загрузки документа

        ### Синтаксис Automation

        ```
        LPDISPATCH GetLoadCombinationsParam (BSTR PathName);
        ```

        ### Входные параметры

        PathName - имя документа

        ### Возвращаемое значение

        Указатель на интерфейс ILoadCombinationsParam
        """
        ...

    def GetOpenDocumentParam(self) -> IOpenDocumentParam:  # idocuments_getopendocumentparam.html
        """
        ## GetOpenDocumentParam - Получить интерфейс параметров открытия документа

        ### Синтаксис Automation

        ```
        LPDISPATCH GetOpenDocumentParam();
        ```

        ### Возвращаемое значение

        Указатель на интерфейс IOpenDocumentParam
        """
        ...

    def Item(self, Index) -> IKompasDocument:  # idocuments_item.html
        """
        ## Item - Документ, заданный по имени, ссылке или по индексу

        Тип данных: указатель на интерфейс документа IKompasDocument

        1. Свойство доступно только для чтения.

        2. Полное имя файла документа состоит из пути, имени и расширения файла.

        ### Синтаксис Automation

        ```
        document = iObject.Item (Index)     # Получить свойство(* )
        document = iObject.GetItem (Index)  # Получить свойство(**)
        ```

        ### Входные параметры

        +--------------------------------------------------------------------------------------------------+
        | Index | - Тип VARIANT, полное имя PathName документа, ссылка на документ или индекс в коллекции. |
        +--------------------------------------------------------------------------------------------------+
        """
        ...

    def Open(self, PathName, Visible, ReadOnly):  # idocuments_open.html
        """
        ## Open - Открыть документ

        Открывает существующий документ и добавляет его в коллекцию.

        ### Синтаксис Automation

        ```
        LPDISPATCH Open (LPCTSTR PathName, BOOL Visible, BOOL ReadOnly);
        ```

        ### Входные параметры

        +------------------------------------------------------------------------------------------------+
        | PathName | - полное имя файла документа, состоящее из пути, имени и расширения файла,          |
        +------------------------------------------------------------------------------------------------+
        | Visible  | - видимость документа: TRUE создавать документ в видимом режиме, FALSE в невидимом, |
        +------------------------------------------------------------------------------------------------+
        | ReadOnly | - TRUE - открыть документ только для чтения.                                        |
        +------------------------------------------------------------------------------------------------+

        ### Возвращаемое значение

        +---------------------------------------------------------------------------+
        | указатель на интерфейс IKompasDocument | - в случае успешного завершения, |
        +---------------------------------------------------------------------------+
        | NULL                                   | - в случае неудачи.              |
        +---------------------------------------------------------------------------+
        """
        ...

    def OpenDocument(self, FileName, Param) -> IKompasDocument:  # idocuments_opendocument.html
        """
        ## OpenDocument - Открыть документ с заданными параметрами открытия документа

        ### Синтаксис Automation

        ```
        LPDISPATCH OpenDocument( BSTR FileName, IOpenDocumentParam * Param )
        ```

        ### Входные параметры

        +--------------------------------------------------------------------------------------+
        | FileName | - имя файла документа                                                     |
        +--------------------------------------------------------------------------------------+
        | Param    | - указатель на интерфейс параметров открытия документа IOpenDocumentParam |
        +--------------------------------------------------------------------------------------+

        ### Возвращаемое значение

        Указатель на интерфейс документа IKompasDocument
        """
        ...

    def OpenEx(self, PathName, Visible, ReadOnly, LoadCombinationIndex):  # idocuments_openex.html
        """
        ## OpenEx - Открыть документ

        1. Открывает существующий документ и добавляет его в коллекцию.

        2. Для сборки 3D можно задать индекс набора для загрузки. Для других типов документов индекс загружаемого набора игнорируется.

        ### Синтаксис Automation

        ```
        LPDISPATCH OpenEx (LPCTSTR PathName, BOOL Visible, BOOL ReadOnly, VARIANT LoadCombinationIndex);
        ```

        ### Входные параметры

        +------------------------------------------------------------------------------------------------------------+
        | PathName             | - полное имя файла документа, состоящее из пути, имени и расширения файла,          |
        +------------------------------------------------------------------------------------------------------------+
        | Visible              | - видимость документа: TRUE создавать документ в видимом режиме, FALSE в невидимом, |
        +------------------------------------------------------------------------------------------------------------+
        | ReadOnly             | - TRUE - открыть документ только для чтения,                                        |
        +------------------------------------------------------------------------------------------------------------+
        | LoadCombinationIndex | - тип VARIANT - индекс загружаемого набора или имя набора.                          |
        +------------------------------------------------------------------------------------------------------------+

        ### Возвращаемое значение

        +---------------------------------------------------------------------------+
        | указатель на интерфейс IKompasDocument | - в случае успешного завершения, |
        +---------------------------------------------------------------------------+
        | NULL                                   | - в случае неудачи.              |
        +---------------------------------------------------------------------------+
        """
        ...




class IKompasError(IKompasAPIObject):  # ikompaserror.html
    """
    ## Интерфейс IKompasError

    Интерфейс информации об ошибке системы КОМПАС.

    1. Позволяет получить информацию об ошибке, возникшей при работе API системы КОМПАС.

    2. Системные ошибки описаны двумя перечислениями: Ошибки API ErrorType и Ошибки 3D API ErrorType3d

    В случае возникновения фатальных ошибок большинство методов интерфейсов и экспортных функций API КОМПАС выполняться не будут.

    Если в результате работы библиотеки возникает ошибка, то ее можно снять вызовом одного из следующих свойств и методов: Description, Report или Clear. Если указанные свойства и методы не вызывались, сообщение об ошибке будет автоматически выведено после завершения выполнения команды библиотеки.

    Данный интерфейс может быть получен от интерфейса приложения IApplication с помощью свойства IApplication::KompasError.
    """

    @property
    def Code(self) -> int:  # ikompaserror_code.html
        """
        ## Code - Код ошибки

        Тип данных: из перечисления ErrorType и ErrorType3d.

        Свойство доступно только для чтения. Так как коды ошибок из ErrorType и ErrorType3d пересекаются, то для определения из какого перечисления взведена ошибка, нужно воспользоваться свойством Error3D.

        ### Синтаксис Automation

        ```
        Code = iObject.Code       # Получить свойство (* )
        Code = iObject.GetCode()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def Description(self) -> str:  # ikompaserror_description.html
        """
        ## Description - Описание ошибки

        Тип данных:BSTR (строка).

        Свойство доступно только для чтения. Позволяет получить символьное описание возникшей ошибки в системе КОМПАС. После выдачи описания ошибки она сбрасывается, т.е. устанавливается в etSuccess (Успешное завершение).

        ### Синтаксис Automation

        ```
        Description = iObject.Description       # Получить свойство (* )
        Description = iObject.GetDescription()  # Получить свойство(**)
        ```
        """
        ...

    @property
    def Error3D(self) -> bool:  # ikompaserror_error3d.html
        """
        ## Error3D - Ошибка для 3D

        Тип данных:BOOL.

        Значения свойства:

        +----------------------------------------------------------------+
        | TRUE  | - ошибка возникла в 3D API (перечисление ErrorType3d), |
        +----------------------------------------------------------------+
        | FALSE | - ошибка из перечисления ErrorType.                    |
        +----------------------------------------------------------------+

        Свойство доступно только для чтения. Позволяет определить, какому из двух перечислений соответствует код ошибки, полученный через свойство Code

        ### Синтаксис Automation

        ```
        Error3D = iObject.Error3D       # Получить свойство (* )
        Error3D = iObject.GetError3D()  # Получить свойство(**)
        ```
        """
        ...

    def Clear(self):  # ikompaserror_clear.html
        """
        ## Clear- Сбросить ошибку

        В результате выполнения метода код ошибки устанавливается в etSuccess (Успешное завершение).

        ### Синтаксис Automation

        ```
        void Clear();
        ```
        """
        ...

    def Report(self):  # ikompaserror_report.html
        """
        ## Report - Вывести сообщение о ошибке

        В результате выполнения этого метода на экран выводится диалог с описанием возникшей ошибки. После выдачи сообщения о ошибке она сбрасывается, то есть устанавливается в etSuccess (Успешное завершение).

        ### Синтаксис Automation

        ```
        void Report();
        ```
        """
        ...




class IProcessParam(IKompasAPIObject):  # iprocessparam.html
    """
    ## Интерфейс IProcessParam

    Интерфейс параметров процесса.

    1. Интерфейс позволяет управлять параметрами процесса.

    2. Данный интерфейс может быть получен от интерфейса приложения IApplication с помощью свойства IApplication::CreateProcessParam.
    """

    @property
    def AutoReduce(self) -> bool:  # iprocessparam_autoreduce.html
        """
        ## AutoReduce - Завершение процесса автоматически после задания всех параметров

        Тип данных:BOOL

        С помощью данного свойства можно получить и изменить завершение процесса после задания всех параметров.

        ### Синтаксис Automation

        ```
        AutoReduce = iObject.AutoReduce       # Получить свойство (* )
        iObject.AutoReduce = AutoReduce       # Установить свойство (* )
        AutoReduce = iObject.GetAutoReduce()  # Получить свойство(**)
        iObject.SetAutoReduce (AutoReduce)    # Установить свойство (**)
        ```
        """
        ...
    @AutoReduce.setter
    def AutoReduce(self, value: bool): ...

    @property
    def Caption(self) -> str:  # iprocessparam_caption.html
        """
        ## Caption - Заголовок процесса

        Тип данных:BSTR (строка).

        Позволяет получить и установить заголовок процесса.

        ### Синтаксис Automation

        ```
        Caption = iObject.Caption       # Получить свойство (* )
        iObject.Caption = Caption       # Установить свойство (* )
        Caption = iObject.GetCaption()  # Получить свойство(**)
        iObject.SetCaption (Caption)    # Установить свойство (**)
        ```
        """
        ...
    @Caption.setter
    def Caption(self, value: str): ...

    @property
    def DefaultControlFix(self) -> int:  # iprocessparam_defaultcontrolfix.html
        """
        ## DefaultControlFix - Cостояние фиксированности для умолчательных элементов управления Панели свойств

        Тип данных: состояние фиксированности для умолчательных элементов управления панели свойств из перечисления DefaultFixTypeEnum.

        С помощью данного свойства можно получить и изменить состояние фиксированности для умолчательных элементов управления Панели свойств.

        ### Синтаксис Automation

        ```
        DefaultControlFix = iObject.DefaultControlFix       # Получить свойство (* )
        iObject.DefaultControlFix = DefaultControlFix       # Установить свойство (* )
        DefaultControlFix = iObject.GetDefaultControlFix()  # Получить свойство(**)
        iObject.SetDefaultControlFix (DefaultControlFix)    # Установить свойство (**)
        ```
        """
        ...
    @DefaultControlFix.setter
    def DefaultControlFix(self, value: int): ...

    @property
    def EnableUndoRedo(self) -> bool:  # iprocessparam_enableundoredo.html
        """
        ## EnableUndoRedo - Признак обработки процессом Undo/Redo команд

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        EnableUndoRedo = Object.EnableUndoRedo       # Получить свойство (* )
        Object.EnableUndoRedo = EnableUndoRedo       # Установить свойство (* )
        EnableUndoRedo = Object.GetEnableUndoRedo()  # Получить свойство(**)
        Object.SetEnableUndoRedo( EnableUndoRedo )   # Установить свойство (**)
        ```
        """
        ...
    @EnableUndoRedo.setter
    def EnableUndoRedo(self, value: bool): ...

    @property
    def EnterButtonIconType(self) -> int:  # iprocessparam_enterbuttonicontype.html
        """
        ## EnterButtonIconType - Тип иконки для кнопки Cоздать

        Тип данных: из перечисления ksEnterButtonIconTypeEnum

        ### Синтаксис Automation

        ```
        EnterButtonIconType = Object.EnterButtonIconType       # Получить свойство (* )
        Object.EnterButtonIconType = EnterButtonIconType       # Установить свойство (* )
        EnterButtonIconType = Object.GetEnterButtonIconType()  # Получить свойство(**)
        Object.SetEnterButtonIconType( EnterButtonIconType )   # Установить свойство (**)
        ```
        """
        ...
    @EnterButtonIconType.setter
    def EnterButtonIconType(self, value: int): ...

    @property
    def Layout(self) -> int:  # iprocessparam_layout.html
        """
        ## Layout - Положение панели свойств (вверху, внизу, слева, справа, плавает)

        Функция не поддерживается

        Тип данных: положение Панели свойств из перечисления PropertyManagerLayout.

        С помощью данного свойства можно получить положение Панели свойств.

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        Layout = iObject.Layout()     # Получить свойство (* )
        Layout = iObject.GetLayout()  # Получить свойство(**)
        ```
        """
        ...

    @property
    def PropertyTabs(self) -> IPropertyTabs:  # iprocessparam_propertytabs.html
        """
        ## PropertyTabs - Коллекция вкладок Панели свойств

        Тип данных:указатель на интерфейс IPropertyTabs.

        1. Свойство только для чтения.

        2. Позволяет получить доступ к вкладкам Панели свойств.

        ### Синтаксис Automation

        ```
        PropertyTabs = iObject.PropertyTabs       # Получить свойство (* )
        PropertyTabs = iObject.GetPropertyTabs()  # Получить свойство(**)
        ```
        """
        ...

    @property
    def ResModule(self) -> str:  # iprocessparam_resmodule.html
        """
        ## ResModule - Модуль с описанием пользовательской спецпанели

        Тип данных:VARIANT (long или BSTR).

        Значение свойства:

        HINSTANCE (тип значения long)

        - полный путь к файлу (тип значения BSTR) dll-модуля, в котором находятся все необходимые ресурсы.

        Данное свойство позволяет установить или получить dll-модуль, в котором находится описание пользовательской спецпанели, рисунки для пользовательских кнопок (см. IProcessParam::SpecToolbarEx и IProcessParam::BmpBeginID).

        ### Синтаксис Automation

        ```
        ResModule = iObject.ResModule       # Получить свойство (* )
        iObject.ResModule = ResModule       # Установить свойство (* )
        ResModule = iObject.GetResModule()  # Получить свойство(**)
        iObject.SetResModule(ResModule)     # Установить свойство (**)
        ```
        """
        ...
    @ResModule.setter
    def ResModule(self, value: str): ...

    @property
    def ShowCommandWindow(self) -> bool:  # iprocessparam_showcommandwindow.html
        """
        ## ShowCommandWindow - Показывать командное окно

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        ShowCommandWindow = Object.ShowCommandWindow       # Получить свойство (* )
        Object.ShowCommandWindow = ShowCommandWindow       # Установить свойство (* )
        ShowCommandWindow = Object.GetShowCommandWindow()  # Получить свойство(**)
        Object.SetShowCommandWindow( ShowCommandWindow )   # Установить свойство (**)
        ```
        """
        ...
    @ShowCommandWindow.setter
    def ShowCommandWindow(self, value: bool): ...

    @property
    def ShowContextMenuOfGeomCalculator(self) -> bool:  # iprocessparam_showcontextmenuofgeomcalculator.html
        """
        ## ShowContextMenuOfGeomCalculator - Наличие кнопки Геометрический калькулятор в контекстном меню процесса

        Тип данных: BOOL

        Значение свойства:

        +----------------------------------------------------------------------------------------+
        | TRUE  | - команда Геометрический калькулятор присутствует в контекстном меню процесса, |
        +----------------------------------------------------------------------------------------+
        | FALSE | - команда Геометрический калькулятор отсутствует в контекстном меню процесса.  |
        +----------------------------------------------------------------------------------------+

        ### Синтаксис Automation

        ```
        ShowContextMenuOfGeomCalculator = Object.ShowContextMenuOfGeomCalculator       # Получить свойство (* )
        Object.ShowContextMenuOfGeomCalculator = ShowContextMenuOfGeomCalculator       # Установить свойство (* )
        ShowContextMenuOfGeomCalculator = Object.GetShowContextMenuOfGeomCalculator()  # Получить свойство(**)
        Object.SetShowContextMenuOfGeomCalculator( ShowContextMenuOfGeomCalculator )   # Установить свойство (**)
        ```
        """
        ...
    @ShowContextMenuOfGeomCalculator.setter
    def ShowContextMenuOfGeomCalculator(self, value: bool): ...

    @property
    def ShowContextMenuOfSnap(self) -> bool:  # iprocessparam_showcontextmenuofsnap.html
        """
        ## ShowContextMenuOfSnap - Наличие кнопки Привязки в контекстном меню процесса

        Тип данных: BOOL

        Значение свойства:

        +----------------------------------------------------------------------+
        | TRUE  | - команда Привязки присутствует в контекстном меню процесса, |
        +----------------------------------------------------------------------+
        | FALSE | - команда Привязки отсутствует в контекстном меню процесса.  |
        +----------------------------------------------------------------------+

        ### Синтаксис Automation

        ```
        ShowContextMenuOfSnap = Object.ShowContextMenuOfSnap       # Получить свойство (* )
        Object.ShowContextMenuOfSnap = ShowContextMenuOfSnap       # Установить свойство (* )
        ShowContextMenuOfSnap = Object.GetShowContextMenuOfSnap()  # Получить свойство(**)
        Object.SetShowContextMenuOfSnap( ShowContextMenuOfSnap )   # Установить свойство (**)
        ```
        """
        ...
    @ShowContextMenuOfSnap.setter
    def ShowContextMenuOfSnap(self, value: bool): ...

    @property
    def SpecToolbar(self) -> int:  # iprocessparam_spectoolbar.html
        """
        ## SpecToolbar - Специальная панель

        Тип данных: тип предопределенной спецпанели для Панели свойств из перечисления SpecPropertyToolBarEnum.

        С помощью данного свойства можно получить и изменить тип предопределенной спецпанели для Панели свойств.

        ### Синтаксис Automation

        ```
        SpecToolbar = iObject.SpecToolbar       # Получить свойство (* )
        iObject.SpecToolbar = SpecToolbar       # Установить свойство (* )
        SpecToolbar = iObject.GetSpecToolbar()  # Получить свойство(**)
        iObject.SetSpecToolbar (SpecToolbar)    # Установить свойство (**)
        ```
        """
        ...
    @SpecToolbar.setter
    def SpecToolbar(self, value: int): ...

    @property
    def SpecToolbarEx(self) -> int:  # iprocessparam_spectoolbarex.html
        """
        ## SpecToolbarEx - Специальная панель

        Тип данных long.

        Значение свойства:

        long идентификатор спецпанели в Панели свойств.

        Данное свойство является расширением свойства SpecToolbar.

        Свойство позволяет установить или получить идентификатор спецпанели. Может быть установлен идентификатор из перечисления SpecPropertyToolBarEnum - Предопределенные спецпанели для панели свойств либо задан идентификатор ресурса пользовательской спецпанели, уникальный в рамках библиотеки.

        Свойство SpecToolbarEx используется совместно со свойствами ResModule и BmpBeginId.

        Чтобы описать пользовательскую спецпанель, нужно:

        1. Объявить уникальный идентификатор спецпанели в h файле

        #define ITB_SPEC_TOOLBAR 3000

        #define END_OF_RESOURCE_TABLE 0xffff

        2. В ресурсном файле описать RCDATA панели и пользовательские кнопки

        rc2-файл:

        ITB_SPEC_TOOLBAR RCDATA

        {

        1 //pbEnter

        2 //pbEsc

        3 //pbHelpсправка

        20 //пользовательская кнопка

        END_OF_RESOURCE_TABLE

        }

        В примере для пользовательской спецпанели определено 4 кнопки.

        1, 2, 3 - предопределенные кнопки из перечисления SpecPropertyToolBarEnum

        20 - пользовательская кнопка

        //битмап для пользовательской кнопки спецпанели

        20 BITMAP "res\\\\st_reduc.bmp" // CREATE процесса настройки фильтра для выбора объектов отчета

        //tips\\hint для пользовательской кнопки спецпанели

        STRINGTABLE LOADONCALL MOVEABLE DISCARDABLE

        BEGIN

        20 "Для тестирования пользовательской кнопки\\nПользовательская кнопка"

        END

        ### Синтаксис Automation

        ```
        SpecToolbarEx = iObject.SpecToolbarEx       # Получить свойство (* )
        iObject.SpecToolbarEx = SpecToolbarEx       # Установить свойство (* )
        SpecToolbarEx = iObject.GetSpecToolbarEx()  # Получить свойство(**)
        iObject.SetSpecToolbarEx(SpecToolbarEx)     # Установить свойство (**)
        ```
        """
        ...
    @SpecToolbarEx.setter
    def SpecToolbarEx(self, value: int): ...

    def AddSetupMenuCommand(self, Title, Command, Checable) -> bool:  # iprocessparam_addsetupmenucommand.html
        """
        ## AddSetupMenuCommand - Добавить пункт в меню настроек процесса

        ### Синтаксис Automation

        ```
        BOOL AddSetupMenuCommand( BSTR Title, long Command, BOOL Checable );
        ```

        ### Входные параметры

        +------------------------------------------------------------------+
        | Title    | - заголовок,                                          |
        +------------------------------------------------------------------+
        | Command  | - идентификатор команды,                              |
        +------------------------------------------------------------------+
        | Checable | - FALSE - выполняемая команда, TRUE - флаг состояния. |
        +------------------------------------------------------------------+

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def AddSpecToolbarButton(self, BtnID, Bmp, Tips, IconFont) -> bool:  # iprocessparam_addspectoolbarbutton.html
        """
        ## AddSpecToolbarButton - Добавить кнопку в спецпанель

        1. Для использования иконок из шрифта Компас нужно передать параметр IconFont == пустой строке.

        2. Для использования шрифта библиотеки, описанного в манифесте библиотеки, нужно передать строчку полученную методом IProceduresLibrary::IconsFont.

        3. Для использования картинок из ресурсов требуется установить свойство IProcessParam::ResModule. Для использования шрифтовых иконок свойство IProcessParam::ResModule устанавливаться не должно.

        ### Синтаксис Automation

        ```
        BOOL AddSpecToolbarButton( long BtnID, VARIANT Bmp, BSTR Tips, BSTR IconFont );
        ```

        ### Входные параметры

        +----------------------------------------------------------------------------+
        | BtnID    | - идентификатор кнопки,                                         |
        +----------------------------------------------------------------------------+
        | Bmp      | - идентификатор картинки, или символа шрифта, или путь к файлу, |
        +----------------------------------------------------------------------------+
        | ips      | - имя кнопки,                                                   |
        +----------------------------------------------------------------------------+
        | IconFont | - имя шрифта.                                                   |
        +----------------------------------------------------------------------------+

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def BmpBeginId(self, Size) -> int:  # iprocessparam_bmpbeginid.html
        """
        ## BmpBeginId - Начальный диапазон для иконок специальной панели

        Тип данных long.

        ### Синтаксис Automation

        ```
        BmpBeginId(Size) = iObject.BmpBeginId     # Получить свойство (* )
        iObject.BmpBeginId(Size) = BmpBeginId     # Установить свойство (* )
        BmpBeginId = iObject.GetBmpBeginId(Size)  # Получить свойство(**)
        iObject.SetBmpBeginId(Size, BmpBeginId)   # Установить свойство (**)
        ```

        ### Входные параметры

        Size - размер битмапов

        Свойство BmpBeginId используется совместно со свойствами ResModuleI и SpecToolbarEx.
        """
        ...

    def ClearSpecToolbar(self) -> bool:  # iprocessparam_clearspectoolbar.html
        """
        ## ClearSpecToolbar - Очистить спецпанель

        ### Синтаксис Automation

        ```
        BOOL ClearSpecToolbar( );
        ```

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def GetGabaritRect(self):  # iprocessparam_getgabaritrect.html
        """
        ## GetGabaritRect - Получить габаритный прямоугольник Панели свойств

        Функция не поддерживается

        Метод позволяет получить габаритный прямоугольник Панели свойств. Точки задаются в относительных координатах окна КОМПАС.

        ### Синтаксис Automation

        ```
        void GetGabaritRect( long * left,
        long * top,
        long * right,
        long * bottom );
        ```

        ### Выходные параметры

        +---------------------------------------------------+
        | left, top     | - координата верхней левой точки, |
        +---------------------------------------------------+
        | right, bottom | - координата нижней правой точки. |
        +---------------------------------------------------+
        """
        ...

    def PopProcessName(self) -> bool:  # iprocessparam_popprocessname.html
        """
        ## PopProcessName - Убрать имя подпроцесса

        ### Синтаксис Automation

        ```
        BOOL PopProcessName();
        ```

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def PushProcessName(self, Name) -> bool:  # iprocessparam_pushprocessname.html
        """
        ## PushProcessName - Установить имя подпроцесса

        ### Синтаксис Automation

        ```
        BOOL PushProcessName( BSTR Name );
        ```

        ### Входные параметры

        +--------------------------------------+
        | Name | - добавочное имя подпроцесса. |
        +--------------------------------------+

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def SetBmpBeginId(self, Size, arg1):
        ...

    def SetSetupMenuCommandState(self, Command, Visible, Enable, Checked) -> bool:  # iprocessparam_setsetupmenucommandstate.html
        """
        ## SetSetupMenuCommandState - Установить состояние команды меню настроек процесса

        ### Синтаксис Automation

        ```
        BOOL SetSetupMenuCommandState( long Command, BOOL Visible, BOOL Enable, BOOL Checked );
        ```

        ### Входные параметры

        +------------------------------------+
        | Command | - идентификатор команды, |
        +------------------------------------+
        | Visible | - видимость,             |
        +------------------------------------+
        | Enable  | - доступность,           |
        +------------------------------------+
        | Checked | - признак выбора.        |
        +------------------------------------+

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...




class IProcess(IKompasAPIObject):  # iprocess.html
    """
    ## Интерфейс IProcess

    Интерфейс процесса.

    Интерфейс можно получить с помощью методов:

    IKompasDocument2D1::LibProcess

    IKompasDocument3D1::LibProcess
    """

    @property
    def Caption(self) -> str:  # iprocess_caption.html
        """
        ## Caption - Заголовок процесса

        Тип данных: BSTR

        ### Синтаксис Automation

        ```
        Caption= Object.CursorId       # Получить свойство (* )
        Object.Caption= CursorId       # Установить свойство (* )
        Caption= Object.GetCursorId()  # Получить свойство(**)
        Object.SetCursorId( Caption)   # Установить свойство (**)
        ```
        """
        ...
    @Caption.setter
    def Caption(self, value: str): ...

    @property
    def Critical(self) -> bool:  # iprocess_critical.html
        """
        ## Critical - Признак критического процесса

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        Critical = Object.Critical       # Получить свойство (* )
        Object.Critical = Critical       # Установить свойство (* )
        Critical = Object.GetCritical()  # Получить свойство(**)
        Object.SetCritical( Critical )   # Установить свойство (**)
        ```
        """
        ...
    @Critical.setter
    def Critical(self, value: bool): ...

    @property
    def CursorId(self) -> typing.Any:  # iprocess_cursorid.html
        """
        ## CursorId - Строка с именем стандартного курсора или идентификатор

        Тип данных: VARIANT

        ### Синтаксис Automation

        ```
        CursorId = Object.CursorId       # Получить свойство (* )
        Object.CursorId = CursorId       # Установить свойство (* )
        CursorId = Object.GetCursorId()  # Получить свойство(**)
        Object.SetCursorId( CursorId )   # Установить свойство (**)
        ```
        """
        ...
    @CursorId.setter
    def CursorId(self, value: typing.Any): ...

    @property
    def Dynamic(self) -> bool:  # iprocess_dynamic.html
        """
        ## Dynamic - Признак динамического запроса

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        Dynamic = Object.Dynamic       # Получить свойство (* )
        Object.Dynamic = Dynamic       # Установить свойство (* )
        Dynamic = Object.GetDynamic()  # Получить свойство(**)
        Object.SetDynamic( Dynamic )   # Установить свойство (**)
        ```
        """
        ...
    @Dynamic.setter
    def Dynamic(self, value: bool): ...

    @property
    def Menu(self) -> typing.Any:  # iprocess_menu.html
        """
        ## Menu - Меню

        Тип данных: VARIANT

        ### Синтаксис Automation

        ```
        Menu = Object.Menu       # Получить свойство (* )
        Object.Menu = Menu       # Установить свойство (* )
        Menu = Object.GetMenu()  # Получить свойство(**)
        Object.SetMenu( Menu )   # Установить свойство (**)
        ```
        """
        ...
    @Menu.setter
    def Menu(self, value: typing.Any): ...

    @property
    def ProcessParam(self) -> IProcessParam:  # iprocess_processparam.html
        """
        ## ProcessParam - Параметры процесса

        Тип данных: Указатель на интерфейс IProcessParam

        ### Синтаксис Automation

        ```
        ProcessParam = Object.ProcessParam       # Получить свойство (* )
        Object.ProcessParam = ProcessParam       # Установить свойство (* )
        ProcessParam = Object.GetProcessParam()  # Получить свойство(**)
        Object.SetProcessParam( ProcessParam )   # Установить свойство (**)
        ```
        """
        ...
    @ProcessParam.setter
    def ProcessParam(self, value: IProcessParam): ...

    @property
    def Prompt(self) -> str:  # iprocess_prompt.html
        """
        ## Prompt - Строка приглашения

        Тип данных: BSTR

        ### Синтаксис Automation

        ```
        Prompt= Object.Prompt       # Получить свойство (* )
        Object.Prompt= Prompt       # Установить свойство (* )
        Prompt= Object.GetPrompt()  # Получить свойство(**)
        Object.SetPrompt( Prompt)   # Установить свойство (**)
        ```
        """
        ...
    @Prompt.setter
    def Prompt(self, value: str): ...

    @property
    def ResModule(self) -> typing.Any:  # iprocess_resmodule.html
        """
        ## ResModule - Модуль с описанием пользовательской спецпанели

        Тип данных: VARIANT

        ### Синтаксис Automation

        ```
        ResModule = Object.ResModule       # Получить свойство (* )
        Object.ResModule = ResModule       # Установить свойство (* )
        ResModule = Object.GetResModule()  # Получить свойство(**)
        Object.SetResModule( ResModule )   # Установить свойство (**)
        ```
        """
        ...
    @ResModule.setter
    def ResModule(self, value: typing.Any): ...

    def Run(self, Modal, PostMessage) -> bool:  # iprocess_run.html
        """
        ## Run - Запустить процесс

        •При запуске модального процесса Функция ожидает завершение работы процесса.

        •При запуске немодального процесса процесс начнет работу позже.

        •Модальный процесс может быть запущен только один.

        •Немодальные процессы можно запустить в разных документах.

        ### Синтаксис Automation

        ```
        BOOL Run( BOOL Modal, BOOL PostMessage );
        ```

        ### Входные параметры

        +----------------------------------------------------------------------+
        | Modal       | TRUE - модальный процесс, FALSE - немодальный процесс, |
        +----------------------------------------------------------------------+
        | PostMessage | TRUE - отложенный запуск для немодального процесса.    |
        +----------------------------------------------------------------------+

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def SetCursorText(self, PVal) -> bool:  # iprocess_setcursortext.html
        """
        ## SetCursorText - Установить текст курсора

        ### Синтаксис Automation

        ```
        BOOL SetCursorText( BSTR PVal );
        ```

        ### Входные параметры

        PVal - Строка для текста курсора.

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def Stop(self) -> bool:  # iprocess_stop.html
        """
        ## Stop - Остановить процесс

        ### Синтаксис Automation

        ```
        BOOL Stop();
        ```

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def Update(self) -> bool:  # iprocess_update.html
        """
        ## Update - Обновить параметры процесса

        ### Синтаксис Automation

        ```
        BOOL Update();
        ```

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...




class IProcess2D(IProcess):  # iprocess2d.html
    """
    ## Интерфейс IProcess2D

    Интерфейс процесса 2D.

    Интерфейс является дополнительным для IProcess.

    Интерфейс можно получить с помощью метода: IKompasDocument2D1::LibProcess
    """

    @property
    def Angle(self) -> float:  # iprocess2d_angle.html
        """
        ## Angle - Угол

        Тип данных: double

        ### Синтаксис Automation

        ```
        Angle = Object.Angle       # Получить свойство(* )
        Object.Angle = Angle       # Установить свойство (* )
        Angle = Object.GetAngle()  # Получить свойство (**)
        Object.SetAngle( Angle )   # Установить свойство (**)
        ```
        """
        ...
    @Angle.setter
    def Angle(self, value: float): ...

    @property
    def Phantom2D(self) -> IPhantom2D:  # iprocess2d_phantom2d.html
        """
        ## Phantom2D - Параметры фантома

        Тип данных: Указатель на интерфейс IPhantom2D

        +------------------------------------------------------------+
        | Phantom2D = Object.Phantom2D      | Получить свойство(* )  |
        +------------------------------------------------------------+
        | Phantom2D = Object.GetPhantom2D() | Получить свойство (**) |
        +------------------------------------------------------------+

        Свойство доступно только для чтения.
        """
        ...

    @property
    def X(self) -> float:  # iprocess2d_x.html
        """
        ## X - Координата X

        Тип данных: double

        ### Синтаксис Automation

        ```
        X = Object.X        # Получить свойство(* )
        Object.X = X        # Установить свойство (* )
        X = Object.GetX())  # Получить свойство (**)
        Object.SetX( X )    # Установить свойство (**)
        ```
        """
        ...
    @X.setter
    def X(self, value: float): ...

    @property
    def Y(self) -> float:  # iprocess2d_y.html
        """
        ## Y - Координата Y

        Тип данных: double

        ### Синтаксис Automation

        ```
        Y = Object.Y        # Получить свойство(* )
        Object.Y = Y        # Установить свойство (* )
        Y = Object.GetY())  # Получить свойство (**)
        Object.SetY( Y )    # Установить свойство (**)
        ```
        """
        ...
    @Y.setter
    def Y(self, value: float): ...




class IProcess3D(IProcess):  # iprocess3d.html
    """
    ## Интерфейс IProcess3D

    Интерфейс процесса 3D.

    Интерфейс является дополнительным для IProcess.

    Интерфейс можно получить с помощью метода: IKompasDocument3D1::LibProcess
    """

    @property
    def ClearProcessUndo(self) -> bool:  # iprocess3d_clearprocessundo.html
        """
        ## ClearProcessUndo - Очистить стэк команд Undo процесса

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        ClearProcessUndo = Object.ClearProcessUndo       # Получить свойство (* )
        Object.ClearProcessUndo = ClearProcessUndo       # Установить свойство (* )
        ClearProcessUndo = Object.GetClearProcessUndo()  # Получить свойство (**)
        Object.SetClearProcessUndo( ClearProcessUndo )   # Установить свойство (**)
        ```
        """
        ...
    @ClearProcessUndo.setter
    def ClearProcessUndo(self, value: bool): ...

    @property
    def Manipulators(self) -> IManipulators:  # iprocess3d_manipulators.html
        """
        ## Manipulators - Коллекция манипуляторов

        Тип данных: Указатель на интерфейс IManipulators

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        Manipulators = Object.Manipulators       # Получить свойство (* )
        Manipulators = Object.GetManipulators()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def MateConstraints(self) -> IMateConstraints3D:  # iprocess3d_mateconstraints.html
        """
        ## MateConstraints - Коллекция сопряжений

        Тип данных: Указатель на интерфейс IMateConstraints3D

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        MateConstraints = Object.MateConstraints ;     # Получить свойство (* )
        MateConstraints = Object.GetMateConstraints()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def MateConstraintsObjects(self) -> typing.Any:  # iprocess3d_mateconstraintsobjects.html
        """
        ## MateConstraintsObjects - Выбранные объекты для сопряжений

        Тип данных: VARIANT

        ### Синтаксис Automation

        ```
        MateConstraintsObjects = Object.MateConstraintsObjects       # Получить свойство(* )
        Object.MateConstraintsObjects = MateConstraintsObjects       # Установить свойство (* )
        MateConstraintsObjects = Object.GetMateConstraintsObjects()  # Получить свойство (**)
        Object.SetMateConstraintsObjects( MateConstraintsObjects )   # Установить свойство (**)
        ```
        """
        ...
    @MateConstraintsObjects.setter
    def MateConstraintsObjects(self, value: typing.Any): ...

    @property
    def PhantomObject(self) -> IModelObject:  # iprocess3d_phantomobject.html
        """
        ## PhantomObject - Фантом

        Тип данных: Указатель на интерфейс IModelObject

        ### Синтаксис Automation

        ```
        PhantomObject = Object.PhantomObject       # Получить свойство(* )
        Object.PhantomObject = PhantomObject       # Установить свойство (* )
        PhantomObject = Object.GetPhantomObject()  # Получить свойство (**)
        Object.SetPhantomObject( PhantomObject )   # Установить свойство (**)
        ```
        """
        ...
    @PhantomObject.setter
    def PhantomObject(self, value: IModelObject): ...

    @property
    def Placement(self) -> IPlacement3D:  # iprocess3d_placement.html
        """
        ## Placement - Интерфейс локальной системы координат (положение объекта)

        Тип данных: Указатель на интерфейс IPlacement3D

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        Placement = Object.Placement;      # Получить свойство (* )
        Placement = Object.GetPlacement()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def SelectionBandMode(self) -> int:  # iprocess3d_selectionbandmode.html
        """
        ## SelectionBandMode - Режим использования прямоугольной рамки для выделения объектов

        Тип данных: из перечисления ksSelectionBandMode

        Свойство позволяет включить режим группового выделения объектов рамкой в процессе.

        При групповом селектировании рамкой будет посылаться событие ksProcess3DNotify::ProcessingGroupObjects.

        ### Синтаксис Automation

        ```
        SelectionBandMode = Object.SelectionBandMode       # Получить свойство (* )
        Object.SelectionBandMode = SelectionBandMode       # Установить свойство (* )
        SelectionBandMode = Object.GetSelectionBandMode()  # Получить свойство (**)
        Object.SetSelectionBandMode( SelectionBandMode )   # Установить свойство (**)
        ```
        """
        ...
    @SelectionBandMode.setter
    def SelectionBandMode(self, value: int): ...

    @property
    def TakeProcessObject(self) -> IModelObject:  # iprocess3d_takeprocessobject.html
        """
        ## TakeProcessObject - Объект, создаваемый в подпроцессе

        Тип данных: Указатель на интерфейс IModelObject

        ### Синтаксис Automation

        ```
        TakeProcessObject = Object.TakeProcessObject       # Получить свойство(* )
        Object.TakeProcessObject = TakeProcessObject       # Установить свойство (* )
        TakeProcessObject = Object.GetTakeProcessObject()  # Получить свойство (**)
        Object.SetTakeProcessObject( TakeProcessObject )   # Установить свойство (**)
        ```
        """
        ...
    @TakeProcessObject.setter
    def TakeProcessObject(self, value: IModelObject): ...

    def ObjectsFilter3D(self, Type) -> bool:  # iprocess3d_objectsfilter3d.html
        """
        ## ObjectsFilter3D - Способ фильтрации 3D объектов

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        ObjectsFilter3D = Object.ObjectsFilter3D( Type )     # Получить свойство (* )
        Object.ObjectsFilter3D( Type ) = ObjectsFilter3D     # Установить свойство (* )
        ObjectsFilter3D = Object.GetObjectsFilter3D( Type )  # Получить свойство (**)
        Object.SetObjectsFilter3D( Type, ObjectsFilter3D )   # Установить свойство (**)
        ```

        ### Входные параметры

        +---------------------------------------------------------------------------------------------------------+
        | ksProcessObjectsFilter3DEnum | - Type - Режим использования прямоугольной рамки для выделения объектов. |
        +---------------------------------------------------------------------------------------------------------+
        """
        ...

    def RunTakeCreateObjectProcess(self, ProcessType, TakeObject, NeedCreateTakeObj, LostTakeObj) -> bool:  # iprocess3d_runtakecreateobjectprocess.html
        """
        ## RunTakeCreateObjectProcess - Запустить подчиненный режим создания объектов

        ### Синтаксис Automation

        ```
        BOOL RunTakeCreateObjectProcess( ProcessTypeEnum ProcessType, LPDISPATCH TakeObject, BOOL NeedCreateTakeObj, BOOL LostTakeObj );
        ```

        ### Выходные параметры

        +------------------------------------------------------------------------------------+
        | ProcessType       | - тип подпроцесса,                                             |
        +------------------------------------------------------------------------------------+
        | TakeObject        | - указатель на интерфейс редактируемого в подпроцессе объекта, |
        +------------------------------------------------------------------------------------+
        | NeedCreateTakeObj | - необходимость создания объекта в подпроцессе,                |
        +------------------------------------------------------------------------------------+
        | LostTakeObj       | - фиксировать параметры создаваемого объекта.                  |
        +------------------------------------------------------------------------------------+

        Функция позволяет запустить следующие подпроцессы:

        +--------------------------------------------------------------------------------------------+
        | prPoint3D                | Точка 3D                                                        |
        +--------------------------------------------------------------------------------------------+
        | prAxisByDirection        | Ось через вершину по объекту                                    |
        +--------------------------------------------------------------------------------------------+
        | prCAxis2Points           | Ось по двум точкам                                              |
        +--------------------------------------------------------------------------------------------+
        | prCAxis2Planes           | Ось по двум плоскостям                                          |
        +--------------------------------------------------------------------------------------------+
        | prCAxisConeface          | Ось конической грани                                            |
        +--------------------------------------------------------------------------------------------+
        | prCAxisEdge              | Ось проходящая через ребро                                      |
        +--------------------------------------------------------------------------------------------+
        | prContour3D              | Контур 3D                                                       |
        +--------------------------------------------------------------------------------------------+
        | prLocalCoordinateSystem  | Локальная система координат                                     |
        +--------------------------------------------------------------------------------------------+
        | prIsoparamCurve          | Изопараметрическая кривая                                       |
        +--------------------------------------------------------------------------------------------+
        | prEquidistant3D          | Эквидистанта 3D                                                 |
        +--------------------------------------------------------------------------------------------+
        |                          |                                                                 |
        +--------------------------------------------------------------------------------------------+
        | prCPlaneOffset           | Смещённая плоскость                                             |
        +--------------------------------------------------------------------------------------------+
        | prCPlane3Points          | Плоскость по 3-м точкам                                         |
        +--------------------------------------------------------------------------------------------+
        | prCPlaneAngle            | Плоскость под углом                                             |
        +--------------------------------------------------------------------------------------------+
        | prCPlaneEdgePoint        | Плоскость через ребро и вершину                                 |
        +--------------------------------------------------------------------------------------------+
        | prCPlaneParallel         | Плоскость через вершину параллельно другой плоскости            |
        +--------------------------------------------------------------------------------------------+
        | prCPlanePerpendicular    | Плоскость через вершину перпендикулярно ребру                   |
        +--------------------------------------------------------------------------------------------+
        | prCPlaneNormalToSurface  | Нормальная плоскость                                            |
        +--------------------------------------------------------------------------------------------+
        | prCPlaneTangentToSurface | Касательная плоскость                                           |
        +--------------------------------------------------------------------------------------------+
        | prCPlaneLineToEdge       | Плоскость через ребро параллельно/перпендикулярно другому ребру |
        +--------------------------------------------------------------------------------------------+
        | prCPlaneLineToFlat       | Плоскость через ребро параллельно/перпендикулярно грани         |
        +--------------------------------------------------------------------------------------------+
        | prCPlaneMiddle           | Средняя плоскость                                               |
        +--------------------------------------------------------------------------------------------+
        | prCPlaneTangentAtPoint   | Плоскость, касательная к грани в точке                          |
        +--------------------------------------------------------------------------------------------+
        | prCPlaneAtCurve          | Плоскость через плоскую кривую                                  |
        +--------------------------------------------------------------------------------------------+
        |                          |                                                                 |
        +--------------------------------------------------------------------------------------------+
        | prMateParallel           | Сопряжения компонентов - Параллельность                         |
        +--------------------------------------------------------------------------------------------+
        | prMatePerpendicular      | Сопряжения компонентов - Перпендикулярность                     |
        +--------------------------------------------------------------------------------------------+
        | prMateOnDistance         | Сопряжения компонентов - На расстоянии                          |
        +--------------------------------------------------------------------------------------------+
        | prMateOnAngle            | Сопряжения компонентов - Под углом                              |
        +--------------------------------------------------------------------------------------------+
        | prMateTangent            | Сопряжения компонентов - Касание                                |
        +--------------------------------------------------------------------------------------------+
        | prMateConcentric         | Сопряжения компонентов - Соосность                              |
        +--------------------------------------------------------------------------------------------+
        | prMateCoincident         | Сопряжения компонентов - Совпадение                             |
        +--------------------------------------------------------------------------------------------+
        | prMateSymmetry           | Сопряжения компонентов - Симметрия                              |
        +--------------------------------------------------------------------------------------------+
        | prMateDependent          | Сопряжения компонентов - Зависимое положение                    |
        +--------------------------------------------------------------------------------------------+

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def SetObjectsFilter3D(self, Type, arg1):
        ...




class IProgressBarIndicator(IKompasAPIObject):  # iprogressbarindicator.html
    """
    ## Интерфейс IProgressBarIndicator

    Интерфейс индикатора прогресса.

    Позволяет запускать, устанавливать текущее значение, останавливать индикатор прогресса, устанавливать текст в строке состояния.

    1. Данный интерфейс можно получить от интерфейса IApplication с помощью метода IApplication::ProgressBarIndicator.

    2. Для работы с индикатором прогресса необходимо, получив указатель на интерфейс индикатора, запустить его командой IProgressBarIndicator::Start. Текущие значения и текст устанавливать командой IProgressBarIndicator::SetProgress. По завершении отображаемого процесса завершить работу с индикатором командой IProgressBarIndicator::Stop. Для отображения следующего процесса необходимо повторно запустить индикатор (при необходимости с новыми параметрами) от ранее полученного указателя на интерфейс.

    3. Команда IProgressBarIndicator::SetText выполняется независимо от того, запущен индикатор прогресса или нет.

    4. Если получены несколько указателей на интерфейс IProgressBarIndicator, и по одному из указателей индикатор запущен командой IProgressBarIndicator::Start, по любому другому указателю может быть выполнена команда IProgressBarIndicator::SetProgress или IProgressBarIndicator::Stop. Команда IProgressBarIndicator::Start повторно не выполняется до остановки индикатора.
    """

    def SetProgress(self, CurrentVal, TextVal, ResetVal):  # iprogressbarindicator_setprogress.html
        """
        ## SetProgress - Установить текущее значение индикатора

        Аналог данного метода при использовании API экспортных функций - ksSetProgressBar

        Метод выполняется, если индикатор прогресса запущен командой IProgressBarIndicator::Start.

        ### Синтаксис Automation

        ```
        void SetProgress (long currentVal, LPCTSTR newText, BOOL resetText);
        ```

        ### Входные параметры

        +----------------------------------------------------------+
        | currentVal | - текущее значение значение индикатора,     |
        +----------------------------------------------------------+
        | newText    | - текст в строке состояния,                 |
        +----------------------------------------------------------+
        | resetText  | - TRUE - обновить текст в строке состояния. |
        +----------------------------------------------------------+
        """
        ...

    def SetText(self, TextVal):  # iprogressbarindicator_settext.html
        """
        ## SetText - Установить текст в строке состояния индикатора

        Аналог данного метода при использовании API экспортных функций - ksSetProgressText.

        1. Метод выполняется как при запущенном индикаторе прогресса командой IProgressBarIndicator::Start, так и при остановленном.

        2. Если команда выполняется при запущенном индикаторе прогресса, устанавливаемый текст запоминается как последний текст перед запуском индикатора, и после остановки индикатора командой IProgressBarIndicator::Stop в строке состояния будет отображаться текст newText.

        ### Синтаксис Automation

        ```
        void SetText (LPCTSTR newText);
        ```

        ### Входные параметры

        +---------------------------------------+
        | newText | - текст в строке состояния, |
        +---------------------------------------+
        """
        ...

    def Start(self, MinVal, MaxVal, TextVal, ResetVal):  # iprogressbarindicator_start.html
        """
        ## Start - Запустить индикатор прогресса

        Аналог данного метода при использовании API экспортных функций - ksStartProgressBar.

        После завершения работы индикатор прогресса должен быть остановлен командой IProgressBarIndicator::Stop.

        ### Синтаксис Automation

        ```
        void Start (long minVal, long maxVal, LPCTSTR newText, BOOL resetText);
        ```

        ### Входные параметры

        +---------------------------------------------------------+
        | minVal    | - минимальное значение шкалы,               |
        +---------------------------------------------------------+
        | maxVal    | - максимальное значение шкалы,              |
        +---------------------------------------------------------+
        | newText   | - текст в строке состояния,                 |
        +---------------------------------------------------------+
        | resetText | - TRUE - обновить текст в строке состояния. |
        +---------------------------------------------------------+
        """
        ...

    def Stop(self, TextVal, ResetVal):  # iprogressbarindicator_stop.html
        """
        ## Stop - Остановить индикатор прогресса

        Аналог данного метода при использовании API экспортных функций - ksStopProgressBar.

        Метод выполняется, если индикатор прогресса запущен командой IProgressBarIndicator::Start.

        ### Синтаксис Automation

        ```
        void Stop (LPCTSTR newText, BOOL resetTxt);
        ```

        ### Входные параметры

        +---------------------------------------------------------+
        | newText   | - текст в строке состояния,                 |
        +---------------------------------------------------------+
        | resetText | - TRUE - обновить текст в строке состояния. |
        +---------------------------------------------------------+
        """
        ...




class IProcessInfoWindow(IKompasAPIObject):  # iprocessinfowindow.html
    """
    ## Интерфейс IProcessInfoWindow

    Интерфейс информационного окна процесса.

    Интерфейс является дополнительным к интерфейсу IProcessParam и позволяет создавать информационное окно, принадлежащее процессу с возможностью чтения/записи текста и управления видимостью окна, а также дополнительными свойствами.

    Данный интерфейс можно получить у интерфейса IProcessParam процесса, в котором необходимо показать окно, посредством вызова метода IUnknown::QueryInterface (const GUID far& iid, void** pif);
    """

    @property
    def CloseProcess(self) -> bool:  # iprocessinfowindow_closeprocess.html
        """
        ## CloseProcess - Закрытие процесса при закрытии окна

        Тип данных: BOOL.

        Значения свойства:

        +---------------------------------------------------------+
        | TRUE  | - закрывать процесс, которому принадлежит окно, |
        +---------------------------------------------------------+
        | FALSE | - не закрывать.                                 |
        +---------------------------------------------------------+

        ### Синтаксис Automation

        ```
        CloseProcess = iObject.CloseProcess;       # Получить свойство (* )
        iObject.CloseProcess = CloseProcess;       # Установить свойство (* )
        CloseProcess = iObject.GetCloseProcess();  # Получить свойство(**)
        iObject.SetCloseProcess( CloseProcess );   # Установить свойство (**)
        ```
        """
        ...
    @CloseProcess.setter
    def CloseProcess(self, value: bool): ...

    @property
    def CreateWindow(self) -> bool:  # iprocessinfowindow_createwindow.html
        """
        ## CreateWindow - Управление созданием окна в процессе

        Тип данных:BOOL.

        Значения свойства:

        +--------------------------------------+
        | TRUE  | - создавать окно в процессе, |
        +--------------------------------------+
        | FALSE | - не создавать окно.         |
        +--------------------------------------+

        ### Синтаксис Automation

        ```
        CreateWindow = iObject.CreateWindow;       # Получить свойство (* )
        iObject.CreateWindow = CreateWindow;       # Установить свойство (* )
        CreateWindow = iObject.GetCreateWindow();  # Получить свойство(**)
        iObject.SetCreateWindow( CreateWindow );   # Установить свойство (**)
        ```
        """
        ...
    @CreateWindow.setter
    def CreateWindow(self, value: bool): ...

    @property
    def HelpId(self) -> int:  # iprocessinfowindow_helpid.html
        """
        ## HelpId - Идентификатор справки

        Тип данных: long.

        ### Синтаксис Automation

        ```
        HelpId = Object.HelpId       # Получить свойство (* )
        Object.HelpId = HelpId       # Установить свойство (* )
        HelpId = Object.GetHelpId()  # Получить свойство(**)
        Object.SetHelpId( HelpId )   # Установить свойство (**)
        ```
        """
        ...
    @HelpId.setter
    def HelpId(self, value: int): ...

    @property
    def Text(self) -> str:  # iprocessinfowindow_text.html
        """
        ## Text - Текст в окне

        Тип данных: BSTR.

        Позволяет устанавливать и получать текст в окне.

        ### Синтаксис Automation

        ```
        Text = iObject.Text;       # Получить свойство (* )
        iObject.Text = Text;       # Установить свойство (* )
        Text = iObject.GetText();  # Получить свойство(**)
        iObject.SetText( Text );   # Установить свойство (**)
        ```
        """
        ...
    @Text.setter
    def Text(self, value: str): ...

    @property
    def Visible(self) -> bool:  # iprocessinfowindow_visible.html
        """
        ## Visible - Видимость окна

        Тип данных: BOOL.

        Значения свойства:

        +-------------------------+
        | TRUE  | - окно видимое, |
        +-------------------------+
        | FALSE | - окно скрытое. |
        +-------------------------+

        ### Синтаксис Automation

        ```
        Visible = iObject.Visible;       # Получить свойство (* )
        iObject.Visible = Visible;       # Установить свойство (* )
        Visible = iObject.GetVisible();  # Получить свойство(**)
        iObject.SetVisible( Visible );   # Установить свойство (**)
        ```
        """
        ...
    @Visible.setter
    def Visible(self, value: bool): ...

    @property
    def WindowCaption(self) -> str:  # iprocessinfowindow_windowcaption.html
        """
        ## WindowCaption - Заголовок окна

        Тип данных: BSTR.

        Позволяет устанавливать и получать заголовок окна.

        ### Синтаксис Automation

        ```
        WindowCaption = iObject.WindowCaption;       # Получить свойство (* )
        iObject.WindowCaption = WindowCaption;       # Установить свойство (* )
        WindowCaption = iObject.GetWindowCaption();  # Получить свойство(**)
        iObject.SetWindowCaption( WindowCaption );   # Установить свойство (**)
        ```
        """
        ...
    @WindowCaption.setter
    def WindowCaption(self, value: str): ...




class IApplicationDialogs(IDispatch):  # iapplicationdialogs.html
    """
    ## Интерфейс IApplicationDialogs

    Интерфейс доступа к диалогам Компас.
    """

    def GetDialogParam(self, ParamType) -> int:  # iapplicationdialogs_getdialogparam.html
        """
        ## GetDialogParam - Получить интерфейс параметров для вызова диалога

        ### Синтаксис Automation

        ```
        LPDISPATCH GetDialogParam( KompasAPIObjectTypeEnum ParamType );
        ```

        ### Входные параметры

        +------------------------------------------------------------------------------------------------------------------------------------------+
        | ParamType                 | - тип параметров диалога.                                                                                    |
        +------------------------------------------------------------------------------------------------------------------------------------------+
        | ksObjectThreadDialogParam | - параметры диалога Выбор стандарта резьбы IThreadDialogParam для функции IApplicationDialogs::SelectThread. |
        +------------------------------------------------------------------------------------------------------------------------------------------+

        ### Возвращаемое значение

        - интерфейс параметров диалога в зависимости от параметра ParamType.
        """
        ...

    def SelectThread(self, ParentHwnd, DialogParam) -> bool:  # iapplicationdialogs_selectthread.html
        """
        ## SelectThread - Выбор стандарта резьбы

        ### Синтаксис Automation

        ```
        BOOL SelectThread( OLE_HANDLE ParentHwnd, IThreadDialogParam * DialogParam );
        ```

        ### Входные параметры

        +------------------------------------------------+
        | TParentHwnd | - дескриптор родительского окна, |
        +------------------------------------------------+
        | DialogParam | - параметры диалога.             |
        +------------------------------------------------+

        ### Возвращаемое значение

        +------------------------------------------------------------------+
        | TRUE  | - если пользователь закрывает диалог с выбором значения, |
        +------------------------------------------------------------------+
        | FALSE | - при отмене или ошибке.                                 |
        +------------------------------------------------------------------+
        """
        ...

    def ShowContentDialog(self, ParentHwnd, DialogParam):  # iapplicationdialogs_showcontentdialog.html
        """
        ## ShowContentDialog - Диалог с произвольным наполнением

        ### Синтаксис Automation

        ```
        long ShowContentDialog( OLE_HANDLE ParentHwnd, IContentDialogParam * DialogParam );
        ```

        ### Входные параметры

        +--------------------------------------------------------+
        | TParentHwnd | - дескриптор родительского окна,         |
        +--------------------------------------------------------+
        | DialogParam | - параметры диалога IContentDialogParam. |
        +--------------------------------------------------------+

        ### Возвращаемое значение

        - результат работы диалога.
        """
        ...

    def WhatsWrongDlg(self, Objs):  # iapplicationdialogs_whatswrongdlg.html
        """
        ## WhatsWrongDlg - Диалог “Что неверно”

        ### Синтаксис Automation

        ```
        long WhatsWrongDlg( VARIANT Objs );
        ```

        ### Входные параметры

        +-------------------------------------+
        | Objs | - массив объектов документа. |
        +-------------------------------------+

        ### Возвращаемое значение

        - результат работы диалога.
        """
        ...




class IContentDialogParam(IKompasAPIObject):  # icontentdialogparam.html
    """
    ## Интерфейс IContentDialogParam

    Интерфейс параметров диалога с произвольным контентом.
    """

    @property
    def AdditionalButton(self) -> str:  # icontentdialogparam_additionalbutton.html
        """
        ## AdditionalButton - Текст дополнительной кнопки

        Тип данных: BSTR

        Если свойство не задано, кнопка не отображается.

        ### Синтаксис Automation

        ```
        AdditionalButton = Object.AdditionalButton;       # Получить свойство (* )
        Object.AdditionalButton = AdditionalButton;       # Установить свойство (* )
        AdditionalButton = Object.GetAdditionalButton();  # Получить свойство(**)
        Object.SetAdditionalButton( AdditionalButton );   # Установить свойство (**)
        ```
        """
        ...
    @AdditionalButton.setter
    def AdditionalButton(self, value: str): ...

    @property
    def CancelButton(self) -> str:  # icontentdialogparam_cancelbutton.html
        """
        ## CancelButton - Текст кнопки Отмена

        Тип данных: BSTR

        Если свойство не задано, кнопка не отображается.

        ### Синтаксис Automation

        ```
        CancelButton = Object.CancelButton;       # Получить свойство (* )
        Object.CancelButton = CancelButton;       # Установить свойство (* )
        CancelButton = Object.GetCancelButton();  # Получить свойство(**)
        Object.SetCancelButton( CancelButton );   # Установить свойство (**)
        ```
        """
        ...
    @CancelButton.setter
    def CancelButton(self, value: str): ...

    @property
    def CheckBoxChecked(self) -> bool:  # icontentdialogparam_checkboxchecked.html
        """
        ## CheckBoxChecked - Состояние чекбокса

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        CheckBoxChecked = Object.CheckBoxChecked;       # Получить свойство (* )
        Object.CheckBoxChecked = CheckBoxChecked;       # Установить свойство (* )
        CheckBoxChecked = Object.GetCheckBoxChecked();  # Получить свойство(**)
        Object.SetCheckBoxChecked( CheckBoxChecked );   # Установить свойство (**)
        ```
        """
        ...
    @CheckBoxChecked.setter
    def CheckBoxChecked(self, value: bool): ...

    @property
    def CheckBoxTitle(self) -> str:  # icontentdialogparam_checkboxtitle.html
        """
        ## CheckBoxTitle - Текст чекбокса

        Тип данных: BSTR

        Если свойство не задано, чекбокс не отображается.

        ### Синтаксис Automation

        ```
        CheckBoxTitle = Object.CheckBoxTitle;       # Получить свойство (* )
        Object.CheckBoxTitle = CheckBoxTitle;       # Установить свойство (* )
        CheckBoxTitle = Object.GetCheckBoxTitle();  # Получить свойство(**)
        Object.SetCheckBoxTitle( CheckBoxTitle );   # Установить свойство (**)
        ```
        """
        ...
    @CheckBoxTitle.setter
    def CheckBoxTitle(self, value: str): ...

    @property
    def Height(self) -> float:  # icontentdialogparam_height.html
        """
        ## Height - Высота диалога

        Тип данных: double

        ### Синтаксис Automation

        ```
        Height = Object.Height;       # Получить свойство (* )
        Object.Height = Height;       # Установить свойство (* )
        Height = Object.GetHeight();  # Получить свойство(**)
        Object.SetHeight( Height );   # Установить свойство (**)
        ```
        """
        ...
    @Height.setter
    def Height(self, value: float): ...

    @property
    def HelpFileName(self) -> str:  # icontentdialogparam_helpfilename.html
        """
        ## HelpFileName - Имя файла справки

        Тип данных: BSTR

        ### Синтаксис Automation

        ```
        HelpFileName = Object.HelpFileName;       # Получить свойство (* )
        Object.HelpFileName = HelpFileName;       # Установить свойство (* )
        HelpFileName = Object.GetHelpFileName();  # Получить свойство(**)
        Object.SetHelpFileName( HelpFileName );   # Установить свойство (**)
        ```
        """
        ...
    @HelpFileName.setter
    def HelpFileName(self, value: str): ...

    @property
    def HelpId(self) -> int:  # icontentdialogparam_helpid.html
        """
        ## HelpId - Идентификатор справки

        Тип данных: long

        Если идентификатор справки не задан, кнопка справки не отображается.

        ### Синтаксис Automation

        ```
        HelpId = Object.HelpId;       # Получить свойство (* )
        Object.HelpId = HelpId;       # Установить свойство (* )
        HelpId = Object.GetHelpId();  # Получить свойство(**)
        Object.SetHelpId( HelpId );   # Установить свойство (**)
        ```
        """
        ...
    @HelpId.setter
    def HelpId(self, value: int): ...

    @property
    def MaxHeight(self) -> float:  # icontentdialogparam_maxheight.html
        """
        ## MaxHeight - Максимальная высота диалога

        Тип данных: double

        ### Синтаксис Automation

        ```
        MaxHeight = Object.MaxHeight;       # Получить свойство (* )
        Object.MaxHeight = MaxHeight;       # Установить свойство (* )
        MaxHeight = Object.GetMaxHeight();  # Получить свойство(**)
        Object.SetMaxHeight( MaxHeight );   # Установить свойство (**)
        ```
        """
        ...
    @MaxHeight.setter
    def MaxHeight(self, value: float): ...

    @property
    def MaxWidth(self) -> float:  # icontentdialogparam_maxwidth.html
        """
        ## MaxWidth - Максимальная ширина диалога

        Тип данных: double

        ### Синтаксис Automation

        ```
        MaxWidth = Object.MaxWidth;       # Получить свойство (* )
        Object.MaxWidth = MaxWidth;       # Установить свойство (* )
        MaxWidth = Object.GetMaxWidth();  # Получить свойство(**)
        Object.SetMaxWidth( MaxWidth );   # Установить свойство (**)
        ```
        """
        ...
    @MaxWidth.setter
    def MaxWidth(self, value: float): ...

    @property
    def MinHeight(self) -> float:  # icontentdialogparam_minheight.html
        """
        ## MinHeight - Минимальная высота диалога

        Тип данных: double

        ### Синтаксис Automation

        ```
        MinHeight = Object.MinHeight;       # Получить свойство (* )
        Object.MinHeight = MinHeight;       # Установить свойство (* )
        MinHeight = Object.GetMinHeight();  # Получить свойство(**)
        Object.SetMinHeight( MinHeight );   # Установить свойство (**)
        ```
        """
        ...
    @MinHeight.setter
    def MinHeight(self, value: float): ...

    @property
    def MinWidth(self) -> float:  # icontentdialogparam_minwidth.html
        """
        ## MinWidth - Минимальная ширина диалога

        Тип данных: double

        ### Синтаксис Automation

        ```
        MinWidth = Object.MinWidth;       # Получить свойство (* )
        Object.MinWidth = MinWidth;       # Установить свойство (* )
        MinWidth = Object.GetMinWidth();  # Получить свойство(**)
        Object.SetMinWidth( MinWidth );   # Установить свойство (**)
        ```
        """
        ...
    @MinWidth.setter
    def MinWidth(self, value: float): ...

    @property
    def NegativeButton(self) -> str:  # icontentdialogparam_negativebutton.html
        """
        ## NegativeButton - Текст кнопки с отрицательным результатом. Кнопка 'Нет'

        Тип данных: BSTR

        Если свойство не задано, кнопка не отображается.

        ### Синтаксис Automation

        ```
        NegativeButton = Object.NegativeButton;       # Получить свойство (* )
        Object.NegativeButton = NegativeButton;       # Установить свойство (* )
        NegativeButton = Object.GetNegativeButton();  # Получить свойство(**)
        Object.SetNegativeButton( NegativeButton );   # Установить свойство (**)
        ```
        """
        ...
    @NegativeButton.setter
    def NegativeButton(self, value: str): ...

    @property
    def PositiveButton(self) -> str:  # icontentdialogparam_positivebutton.html
        """
        ## PositiveButton - Текст кнопки с положительным результатом. Кнопка 'Да'

        Тип данных: BSTR

        Если свойство не задано, кнопка не отображается.

        ### Синтаксис Automation

        ```
        PositiveButton = Object.PositiveButton;       # Получить свойство (* )
        Object.PositiveButton = PositiveButton;       # Установить свойство (* )
        PositiveButton = Object.GetPositiveButton();  # Получить свойство(**)
        Object.SetPositiveButton( PositiveButton );   # Установить свойство (**)
        ```
        """
        ...
    @PositiveButton.setter
    def PositiveButton(self, value: str): ...

    @property
    def Resizable(self) -> bool:  # icontentdialogparam_resizable.html
        """
        ## Resizable - Доступно ли изменение размера

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        Resizable = Object.Resizable;       # Получить свойство (* )
        Object.Resizable = Resizable;       # Установить свойство (* )
        Resizable = Object.GetResizable();  # Получить свойство(**)
        Object.SetResizable( Resizable );   # Установить свойство (**)
        ```
        """
        ...
    @Resizable.setter
    def Resizable(self, value: bool): ...

    @property
    def Title(self) -> str:  # icontentdialogparam_title.html
        """
        ## Title - Заголовок

        Тип данных: BSTR

        ### Синтаксис Automation

        ```
        Title = Object.Title;       # Получить свойство (* )
        Object.Title = Title;       # Установить свойство (* )
        Title = Object.GetTitle();  # Получить свойство(**)
        Object.SetTitle( Title );   # Установить свойство (**)
        ```
        """
        ...
    @Title.setter
    def Title(self, value: str): ...

    @property
    def UpperCaseTitle(self) -> bool:  # icontentdialogparam_uppercasetitle.html
        """
        ## UpperCaseTitle - Заголовок диалога переводить в верхний регистр

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        UpperCaseTitle = Object.UpperCaseTitle;       # Получить свойство (* )
        Object.UpperCaseTitle = UpperCaseTitle;       # Установить свойство (* )
        UpperCaseTitle = Object.GetUpperCaseTitle();  # Получить свойство(**)
        Object.SetUpperCaseTitle( UpperCaseTitle );   # Установить свойство (**)
        ```
        """
        ...
    @UpperCaseTitle.setter
    def UpperCaseTitle(self, value: bool): ...

    @property
    def Width(self) -> float:  # icontentdialogparam_width.html
        """
        ## Width - Ширина диалога

        Тип данных: double

        ### Синтаксис Automation

        ```
        Width = Object.Width;       # Получить свойство (* )
        Object.Width = Width;       # Установить свойство (* )
        Width = Object.GetWidth();  # Получить свойство(**)
        Object.SetWidth( Width );   # Установить свойство (**)
        ```
        """
        ...
    @Width.setter
    def Width(self, value: float): ...

    def AddLeftSeparator(self) -> bool:  # icontentdialogparam_addleftseparator.html
        """
        ## AddLeftSeparator - Добавить разделитель в левую панель

        ### Синтаксис Automation

        ```
        BOOL AddLeftSeparator();
        ```

        ### Возвращаемое значение

        +-----------------------------------------+
        | TRUE | - в случае успешного завершения. |
        +-----------------------------------------+
        """
        ...

    def AddLeftToolButton(self, Header, HeaderVisible, CommandID, IconId, IconFont, ButtonType) -> bool:  # icontentdialogparam_addlefttoolbutton.html
        """
        ## AddLeftToolButton - Добавить кнопку в левую панель

        ### Синтаксис Automation

        ```
        BOOL AddLeftToolButton( BSTR Header, BOOL HeaderVisible, long CommandID, long IconId, BSTR IconFont, ButtonTypeEnum ButtonType );
        ```

        ### Входные параметры

        +--------------------------------------------------------------+
        | Header        | - имя кнопки,                                |
        +--------------------------------------------------------------+
        | HeaderVisible | - отображать имя кнопки на панели,           |
        +--------------------------------------------------------------+
        | CommandID     | - идентификатор команды,                     |
        +--------------------------------------------------------------+
        | IconId        | - идентификатор иконки из шрифта,            |
        +--------------------------------------------------------------+
        | IconFont      | - имя шрифта,                                |
        +--------------------------------------------------------------+
        | ButtonType    | - тип кнопки из перечисления ButtonTypeEnum. |
        +--------------------------------------------------------------+

        ### Возвращаемое значение

        +-----------------------------------------+
        | TRUE | - в случае успешного завершения. |
        +-----------------------------------------+
        """
        ...

    def AddRightSeparator(self) -> bool:  # icontentdialogparam_addrightseparator.html
        """
        ## AddRightSeparator - Добавить разделитель в правую панель

        ### Синтаксис Automation

        ```
        BOOL AddRightSeparator();
        ```

        ### Возвращаемое значение

        +-----------------------------------------+
        | TRUE | - в случае успешного завершения. |
        +-----------------------------------------+
        """
        ...

    def AddRightToolButton(self, Header, HeaderVisible, CommandID, IconId, IconFont, ButtonType) -> bool:  # icontentdialogparam_addrighttoolbutton.html
        """
        ## AddRightToolButton - Добавить кнопку в правую панель

        ### Синтаксис Automation

        ```
        BOOL AddRightToolButton( BSTR Header, BOOL HeaderVisible, long CommandID, long IconId, BSTR IconFont, ButtonTypeEnum ButtonType );
        ```

        ### Входные параметры

        +--------------------------------------------------------------+
        | Header        | - имя кнопки,                                |
        +--------------------------------------------------------------+
        | HeaderVisible | - отображать имя кнопки на панели,           |
        +--------------------------------------------------------------+
        | CommandID     | - идентификатор команды,                     |
        +--------------------------------------------------------------+
        | IconId        | - идентификатор иконки из шрифта,            |
        +--------------------------------------------------------------+
        | IconFont      | - имя шрифта,                                |
        +--------------------------------------------------------------+
        | ButtonType    | - тип кнопки из перечисления ButtonTypeEnum. |
        +--------------------------------------------------------------+

        ### Возвращаемое значение

        +-----------------------------------------+
        | TRUE | - в случае успешного завершения. |
        +-----------------------------------------+
        """
        ...




class ISerializer(IDispatch):  # iserializer.html
    """
    ## Интерфейс ISerializer

    Интерфейс сериализации.
    """

    @property
    def XML(self) -> str:  # iserializer_xml.html
        """
        ## XML - XML Сериализация/десериализация

        Тип данных: BSTR

        ### Синтаксис Automation

        ```
        XML = Object.XML;       # Получить свойство (* )
        Object.XML = XML;       # Установить свойство (* )
        XML = Object.GetXML();  # Получить свойство(**)
        Object.SetXML( XML );   # Установить свойство (**)
        ```
        """
        ...
    @XML.setter
    def XML(self, value: str): ...




class IManipulators(IKompasCollection):  # imanipulators.html
    """
    ## Интерфейс IManipulators

    Интерфейс коллекции манипуляторов.

    Получить интерфейс коллекции манипуляторов можно, используя свойство процесса 3D IProcess3D::Manipulators.

    KOMPAS v19
    """

    def Add(self, Type) -> IBaseManipulator:  # imanipulators_add.html
        """
        ## Add - Создать манипулятор (добавляет контрол в коллекцию)

        Метод позволяет создать новый базовый интерфейс манипулятора.

        ### Синтаксис Automation

        ```
        LPDISPATCH Add( ksManipulatorTypeEnum Type );
        ```

        ### Входные параметры

        +-----------------------------------------------------------------------+
        | Type | - Способ разбиения зоны из перечисления ksManipulatorTypeEnum. |
        +-----------------------------------------------------------------------+

        ### Возвращаемое значение

        - Указатель на интерфейс IBaseManipulator .
        """
        ...

    def Item(self, Index) -> IBaseManipulator:  # imanipulators_item.html
        """
        ## Item - Возвращает манипулятор, заданный по индексу

        Тип данных: Указатель на интерфейс IBaseManipulator

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        Item = Object.Item( Index )     # Получить свойство(* )
        Item = Object.GetItem( Index )  # Получить свойство (**)
        ```

        ### Входные параметры

        +---------------------------------------+
        | Index | - Идентификатор манипулятора. |
        +---------------------------------------+
        """
        ...

    def Manipulator(self, Id) -> IBaseManipulator:  # imanipulators_manipulator.html
        """
        ## Manipulator - Возвращает манипулятор с заданным идентификатором

        Тип данных: Указатель на интерфейс IBaseManipulator

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        Manipulator = Object.Manipulator( Id )     # Получить свойство(* )
        Manipulator = Object.GetManipulator( Id )  # Получить свойство (**)
        ```

        ### Входные параметры

        +------------------------------------+
        | Id | - Идентификатор манипулятора. |
        +------------------------------------+
        """
        ...




class IBaseManipulator(IDispatch):  # ibasemanipulator.html
    """
    ## Интерфейс IBaseManipulator

    Базовый интерфейс манипулятора.

    Интерфейс можно получить с помощью метода коллекции манипуляторов IManipulators::Add

    KOMPAS v19
    """

    @property
    def Active(self) -> bool:  # ibasemanipulator_active.html
        """
        ## Active - Активный манипулятор

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        Active = Object.Active       # Получить свойство(* )
        Object.Active = Active       # Установить свойство (* )
        Active = Object.GetActive()  # Получить свойство (**)
        Object.SetActive( Active )   # Установить свойство (**)
        ```
        """
        ...
    @Active.setter
    def Active(self, value: bool): ...

    @property
    def Application(self):
        ...

    @property
    def Id(self) -> int:  # ibasemanipulator_id.html
        """
        ## Id - Идентификатор манипулятора

        Тип данных: long

        Для корректной рассылки событий манипуляторов каждому манипулятору требуется задать уникальный ненулевой номер.

        ### Синтаксис Automation

        ```
        Id = Object.Id       # Получить свойство(* )
        Object.Id = Id       # Установить свойство (* )
        Id = Object.GetId()  # Получить свойство (**)
        Object.SetId( Id )   # Установить свойство (**)
        ```
        """
        ...
    @Id.setter
    def Id(self, value: int): ...

    @property
    def ManipulatorType(self) -> int:  # ibasemanipulator_manipulatortype.html
        """
        ## ManipulatorType - Тип манипулятора

        Тип данных: из перечисления ksManipulatorTypeEnum

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        ManipulatorType = Object.ManipulatorType       # Получить свойство(* )
        ManipulatorType = Object.GetManipulatorType()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def Parent(self):
        ...

    @property
    def Placement(self) -> IPlacement3D:  # ibasemanipulator_placement.html
        """
        ## Placement - Получить локальную систему координат

        Тип данных: Указатель на интерфейс IPlacement3D

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        Placement = Object.Placement       # Получить свойство(* )
        Placement = Object.GetPlacement()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def Reference(self):
        ...

    @property
    def Type(self):
        ...

    @property
    def Visible(self) -> bool:  # ibasemanipulator_visible.html
        """
        ## Visible - Свойство видимости манипулятора

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        Visible = Object.Visible       # Получить свойство(* )
        Object.Visible = Visible       # Установить свойство (* )
        Visible = Object.GetVisible()  # Получить свойство (**)
        Object.SetVisible( Visible )   # Установить свойство (**)
        ```
        """
        ...
    @Visible.setter
    def Visible(self, value: bool): ...

    def Create(self) -> bool:  # ibasemanipulator_create.html
        """
        ## Create - Создать манипулятор

        ### Синтаксис Automation

        ```
        BOOL Create();
        ```

        ### Возвращаемое значение

        +-----------------------------------------+
        | TRUE | - в случае успешного завершения. |
        +-----------------------------------------+
        """
        ...

    def Delete(self) -> bool:  # ibasemanipulator_delete.html
        """
        ## Delete - Удалить манипулятор

        ### Синтаксис Automation

        ```
        BOOL Delete();
        ```

        ### Возвращаемое значение

        +-----------------------------------------+
        | TRUE | - в случае успешного завершения. |
        +-----------------------------------------+
        """
        ...

    def UpdatePlacement(self, Redraw) -> bool:  # ibasemanipulator_updateplacement.html
        """
        ## UpdatePlacement - Установить локальную систему координат

        ### Синтаксис Automation

        ```
        BOOL UpdatePlacement( BOOL Redraw );
        ```

        ### Входные параметры

        +---------------------------------+
        | Redraw | - признак перерисовки. |
        +---------------------------------+

        ### Возвращаемое значение

        +-----------------------------------------+
        | TRUE | - в случае успешного завершения. |
        +-----------------------------------------+
        """
        ...




class IEditDoubleManipulator(IBaseManipulator):  # ieditdoublemanipulator.html
    """
    ## Интерфейс IEditDoubleManipulator

    Интерфейс манипулятора - редактора вещественного значения.

    •Манипулятор позволяет вводить вещественное значение в 3D процессе.

    •Интерфейс можно получить, добавив манипулятор через коллекцию манипуляторов IManipulators::Add.

    •Интерфейс является дополнительным для IBaseManipulator.

    Версия: KOMPAS v19
    """

    @property
    def EditValue(self) -> float:  # ieditdoublemanipulator_editvalue.html
        """
        ## EditValue - Текущее значение в редакторе

        Тип данных: double

        ### Синтаксис Automation

        ```
        EditValue = Object.EditValue       # Получить свойство (* )
        Object.EditValue = EditValue       # Установить свойство (* )
        EditValue = Object.GetEditValue()  # Получить свойство (**)
        Object.SetEditValue( EditValue )   # Установить свойство (**)
        ```
        """
        ...
    @EditValue.setter
    def EditValue(self, value: float): ...

    @property
    def IsEditCreated(self) -> bool:  # ieditdoublemanipulator_iseditcreated.html
        """
        ## IsEditCreated - Создан редактор с установленным фокусом

        Тип данных: BOOL

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        IsEditCreated = Object.IsEditCreated       # Получить свойство (* )
        IsEditCreated = Object.GetIsEditCreated()  # Получить свойство (**)
        ```
        """
        ...

    def SetValueRange(self, MinVal, MaxVal) -> bool:  # ieditdoublemanipulator_setvaluerange.html
        """
        ## SetValueRange - Установить новые ограничения на значение

        ### Синтаксис Automation

        ```
        BOOL SetValueRange( double MinVal, double MaxVal );
        ```

        ### Входные параметры

        +-----------------------------------+
        | MinVal | - минимальное значение,  |
        +-----------------------------------+
        | MaxVal | - максимальное значение. |
        +-----------------------------------+

        ### Возвращаемое значение

        +-----------------------------------------+
        | TRUE | - в случае успешного завершения. |
        +-----------------------------------------+
        """
        ...




class IPlacement3DManipulator(IBaseManipulator):  # iplacement3dmanipulator.html
    """
    ## Интерфейс IPlacement3DManipulator

    Интерфейс манипулятора системы координат.

    Манипулятор позволяет указать положение при работе в 3D.

    Интерфейс можно получить, добавив манипулятор через коллекцию манипуляторов IManipulators::Add.

    Интерфейс является дополнительным для IBaseManipulator.

    Версия: KOMPAS v19
    """

    @property
    def EditValue(self) -> float:  # iplacement3dmanipulator_editvalue.html
        """
        ## EditValue - Текущее значение в редакторе

        Тип данных: double

        ### Синтаксис Automation

        ```
        EditValue = Object.EditValue       # Получить свойство (* )
        Object.EditValue = EditValue       # Установить свойство (* )
        EditValue = Object.GetEditValue()  # Получить свойство (**)
        Object.SetEditValue( EditValue )   # Установить свойство (**)
        ```
        """
        ...
    @EditValue.setter
    def EditValue(self, value: float): ...

    @property
    def IsEditCreated(self) -> bool:  # iplacement3dmanipulator_iseditcreated.html
        """
        ## IsEditCreated - Создан редактор с установленным фокусом

        Тип данных: BOOL

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        IsEditCreated = Object.IsEditCreated       # Получить свойство (* )
        IsEditCreated = Object.GetIsEditCreated()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def Mode(self) -> int:  # iplacement3dmanipulator_mode.html
        """
        ## Mode - Режим работы манипулятора

        Тип данных: long

        ### Синтаксис Automation

        ```
        Mode = Object.Mode       # Получить свойство (* )
        Object.Mode = Mode       # Установить свойство (* )
        Mode = Object.GetMode()  # Получить свойство (**)
        Object.SetMode( Mode )   # Установить свойство (**)
        ```
        """
        ...
    @Mode.setter
    def Mode(self, value: int): ...

    @property
    def PrimitiveSelected(self) -> int:  # iplacement3dmanipulator_primitiveselected.html
        """
        ## PrimitiveSelected - Селектированный элемент манипулятора

        Тип данных: из перечисления ksManipulatorPrimitiveEnum

        ### Синтаксис Automation

        ```
        PrimitiveSelected = Object.PrimitiveSelected      # Получить свойство (* )
        Object.PrimitiveSelected = PrimitiveSelected      # Установить свойство (* )
        PrimitiveSelected = Object.GetPrimitiveSelected   # Получить свойство (**)
        Object.SetPrimitiveSelected( PrimitiveSelected )  # Установить свойство (**)
        ```
        """
        ...
    @PrimitiveSelected.setter
    def PrimitiveSelected(self, value: int): ...

    @property
    def ReadOnly(self) -> bool:  # iplacement3dmanipulator_readonly.html
        """
        ## ReadOnly - Запрет изменений

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        ReadOnly = Object.ReadOnly       # Получить свойство (* )
        Object.ReadOnly = ReadOnly       # Установить свойство (* )
        ReadOnly = Object.GetReadOnly()  # Получить свойство (**)
        Object.SetReadOnly( ReadOnly )   # Установить свойство (**)
        ```
        """
        ...
    @ReadOnly.setter
    def ReadOnly(self, value: bool): ...

    @property
    def RotateStep(self) -> float:  # iplacement3dmanipulator_rotatestep.html
        """
        ## RotateStep - Шаг поворота манипулятора вокруг оси

        Тип данных: double

        ### Синтаксис Automation

        ```
        RotateStep = Object.RotateStep       # Получить свойство (* )
        Object.RotateStep = RotateStep       # Установить свойство (* )
        RotateStep = Object.GetRotateStep()  # Получить свойство (**)
        Object.SetRotateStep( RotateStep )   # Установить свойство (**)
        ```
        """
        ...
    @RotateStep.setter
    def RotateStep(self, value: float): ...

    @property
    def ShiftStep(self) -> float:  # iplacement3dmanipulator_shiftstep.html
        """
        ## ShiftStep - Шаг сдвига манипулятора

        Тип данных: double

        ### Синтаксис Automation

        ```
        ShiftStep = Object.ShiftStep       # Получить свойство (* )
        Object.ShiftStep = ShiftStep       # Установить свойство (* )
        ShiftStep = Object.GetShiftStep()  # Получить свойство (**)
        Object.SetShiftStep( ShiftStep )   # Установить свойство (**)
        ```
        """
        ...
    @ShiftStep.setter
    def ShiftStep(self, value: float): ...

    def PrimitiveDisabled(self, Primitive) -> bool:  # iplacement3dmanipulator_primitivedisabled.html
        """
        ## PrimitiveDisabled - Запрет изменения

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        PrimitiveDisabled = Object.PrimitiveDisabled( Primitive )     # Получить свойство (* )
        Object.PrimitiveDisabled( Primitive ) = PrimitiveDisabled     # Установить свойство (* )
        PrimitiveDisabled = Object.GetPrimitiveDisabled( Primitive )  # Получить свойство (**)
        Object.SetPrimitiveDisabled( Primitive, PrimitiveDisabled )   # Установить свойство (**)
        ```

        ### Входные параметры

        +----------------------------------------------------------------------+
        | ksManipulatorPrimitiveEnum Primitive | - тип примитива манипулятора. |
        +----------------------------------------------------------------------+
        """
        ...

    def PrimitiveVisible(self, Primitive) -> bool:  # iplacement3dmanipulator_primitivevisible.html
        """
        ## PrimitiveVisible - Свойство видимости элементов манипулятора

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        PrimitiveVisible = Object.PrimitiveVisible( Primitive )     # Получить свойство (* )
        Object.PrimitiveVisible( Primitive ) = PrimitiveVisible     # Установить свойство (* )
        PrimitiveVisible = Object.GetPrimitiveVisible( Primitive )  # Получить свойство (**)
        Object.SetPrimitiveVisible( Primitive, PrimitiveVisible )   # Установить свойство (**)
        ```

        ### Входные параметры

        +----------------------------------------------------------------------+
        | ksManipulatorPrimitiveEnum Primitive | - тип примитива манипулятора. |
        +----------------------------------------------------------------------+
        """
        ...

    def SetPrimitiveDisabled(self, Primitive, arg1):
        ...

    def SetPrimitiveVisible(self, Primitive, arg1):
        ...

    def SetRotateRange(self, Min, Max) -> bool:  # iplacement3dmanipulator_setrotaterange.html
        """
        ## SetRotateRange - Установить диапазон для поворота манипулятора

        ### Синтаксис Automation

        ```
        BOOL SetRotateRange( double Min, double Max );
        ```

        ### Входные параметры

        +-------------------------------+
        | Min | - минимальное значение. |
        +-------------------------------+

        ### Выходные параметры

        +--------------------------------+
        | Max | - максимальное значение. |
        +--------------------------------+

        ### Возвращаемое значение

        +-----------------------------------------+
        | TRUE | - в случае успешного завершения, |
        +-----------------------------------------+
        """
        ...

    def SetShiftRange(self, Min, Max) -> bool:  # iplacement3dmanipulator_setshiftrange.html
        """
        ## SetShiftRange - Установить диапазон для сдвига манипулятора

        ### Синтаксис Automation

        ```
        BOOL SetShiftRange( double Min, double Max );
        ```

        ### Входные параметры

        +-------------------------------+
        | Min | - минимальное значение. |
        +-------------------------------+

        ### Выходные параметры

        +--------------------------------+
        | Max | - максимальное значение. |
        +--------------------------------+

        ### Возвращаемое значение

        +-----------------------------------------+
        | TRUE | - в случае успешного завершения, |
        +-----------------------------------------+
        """
        ...




class IMouseEnterLeaveParameters7(IKompasAPIObject):  # imouseenterleaveparameters7.html
    """
    ## Интерфейс IMouseEnterLeaveParameters7

    Параметры отображения точки, позволяющей определить место применения контрола.

    Интерфейс параметров используется в событии ksProcess2DNotify::GetMouseEnterLeavePoint.

    Версия: KOMPAS v19
    """

    @property
    def Offset(self) -> float:  # imouseenterleaveparameters7_offset.html
        """
        ## Offset - Смещение символа относительно точки

        Тип данных: double

        ### Синтаксис Automation

        ```
        Offset = Object.Offset       # Получить свойство (* )
        Object.Offset = Offset       # Установить свойство (* )
        Offset = Object.GetOffset()  # Получить свойство (**)
        Object.SetOffset(Offset)     # Установить свойство (**)
        ```
        """
        ...
    @Offset.setter
    def Offset(self, value: float): ...

    @property
    def OffsetAngle(self) -> float:  # imouseenterleaveparameters7_offsetangle.html
        """
        ## OffsetAngle - Направление смещения символа относительно точки

        Тип данных: double

        ### Синтаксис Automation

        ```
        OffsetAngle = Object.OffsetAngle       # Получить свойство (* )
        Object.OffsetAngle = OffsetAngle       # Установить свойство (* )
        OffsetAngle = Object.GetOffsetAngle()  # Получить свойство (**)
        Object.SetOffsetAngle(OffsetAngle)     # Установить свойство (**)
        ```
        """
        ...
    @OffsetAngle.setter
    def OffsetAngle(self, value: float): ...

    @property
    def Symbol(self) -> int:  # imouseenterleaveparameters7_symbol.html
        """
        ## Symbol - Символ, отображаемый в поле документа, при наведении на контрол

        Тип данных: long

        ### Синтаксис Automation

        ```
        Symbol = Object.Symbol       # Получить свойство (* )
        Object.Symbol = Symbol       # Установить свойство (* )
        Symbol = Object.GetSymbol()  # Получить свойство (**)
        Object.SetSymbol(Symbol)     # Установить свойство (**)
        ```
        """
        ...
    @Symbol.setter
    def Symbol(self, value: int): ...

    @property
    def SymbolColor(self) -> int:  # imouseenterleaveparameters7_symbolcolor.html
        """
        ## SymbolColor - Цвет символа, отображаемого в поле документа, при наведении на контрол

        Тип данных: long

        ### Синтаксис Automation

        ```
        SymbolColor = Object.SymbolColor        # Получить свойство (* )
        Object.SymbolColor = SymbolColor        # Установить свойство (* )
        SymbolColor = Object.GetSymbolColor ()  # Получить свойство (**)
        Object.SetSymbolColor (SymbolColor )    # Установить свойство (**)
        ```
        """
        ...
    @SymbolColor.setter
    def SymbolColor(self, value: int): ...

    @property
    def SymbolFont(self) -> str:  # imouseenterleaveparameters7_symbolfont.html
        """
        ## SymbolFont - Шрифт символа, отображаемого в поле документа, при наведении на контрол

        Тип данных: BSTR

        ### Синтаксис Automation

        ```
        SymbolFont = Object.SymbolFont        # Получить свойство (* )
        Object.SymbolFont = SymbolFont        # Установить свойство (* )
        SymbolFont = Object.GetSymbolFont ()  # Получить свойство (**)
        Object.SetSymbolFont (SymbolFont )    # Установить свойство (**)
        ```
        """
        ...
    @SymbolFont.setter
    def SymbolFont(self, value: str): ...

    @property
    def SymbolScale(self) -> float:  # imouseenterleaveparameters7_symbolscale.html
        """
        ## SymbolScale - Увеличение высоты символа, отображаемого в поле документа, при наведении на контрол

        Тип данных: double

        +-------------------------------------------------------------------+
        | SymbolScale = Object.SymbolScale       | Получить свойство (* )   |
        +-------------------------------------------------------------------+
        | Object.SymbolScale = SymbolScale       | Установить свойство (* ) |
        +-------------------------------------------------------------------+
        | SymbolScale = Object.GetSymbolScale () | Получить свойство (**)   |
        +-------------------------------------------------------------------+
        | Object.SetSymbolScale (SymbolScale )   | Установить свойство (**) |
        +-------------------------------------------------------------------+
        """
        ...
    @SymbolScale.setter
    def SymbolScale(self, value: float): ...

    @property
    def X(self) -> float:  # imouseenterleaveparameters7_x.html
        """
        ## X - Координата X

        Тип данных: double

        ### Синтаксис Automation

        ```
        X = Object.X       # Получить свойство (* )
        Object.X = X       # Установить свойство (* )
        X = Object.GetX()  # Получить свойство (**)
        Object.SetX(X)     # Установить свойство (**)
        ```
        """
        ...
    @X.setter
    def X(self, value: float): ...

    @property
    def Y(self) -> float:  # imouseenterleaveparameters7_y.html
        """
        ## Y - Координата Y

        Тип данных: double

        ### Синтаксис Automation

        ```
        Y = Object.Y       # Получить свойство (* )
        Object.Y = Y       # Установить свойство (* )
        Y = Object.GetY()  # Получить свойство (**)
        Object.SetY(Y)     # Установить свойство (**)
        ```
        """
        ...
    @Y.setter
    def Y(self, value: float): ...




class ISaveToPreviusParam7(IDispatch):  # isavetopreviusparam7.html
    """
    ## Интерфейс ISaveToPreviusParam7

    Параметры конвертации при сохранении в предыдущую версию.
    """

    def AddOption(self, UniqueId, OptionName, Options, DefaultValue) -> bool:  # isavetopreviusparam7_addoption.html
        """
        ## AddOption - Добавить настройку конвертации с возможностью выбора варианта конвертации

        ### Синтаксис Automation

        ```
        BOOL AddOption( BSTR UniqueId, BSTR OptionName, VARIANT Options, BSTR DefaultValue );
        ```

        ### Входные параметры

        +-----------------------------------------------------------------------------------------+
        | UniqueId     | - уникальный идентификатор,                                              |
        +-----------------------------------------------------------------------------------------+
        | OptionName   | - отображаемое имя свойства,                                             |
        +-----------------------------------------------------------------------------------------+
        | Options      | - список возможных значений (массив строк SafeArray VT_ARRAY | VT_BSTR), |
        +-----------------------------------------------------------------------------------------+
        | DefaultValue | - базовое значение.                                                      |
        +-----------------------------------------------------------------------------------------+

        ### Возвращаемое значение

        +-----------------------------------------+
        | TRUE | - в случае успешного завершения. |
        +-----------------------------------------+
        """
        ...

    def AddWarning(self, UniqueId, OptionName, Text) -> bool:  # isavetopreviusparam7_addwarning.html
        """
        ## AddWarning - Добавить предупреждение

        ### Синтаксис Automation

        ```
        BOOL AddWarning( BSTR UniqueId, BSTR OptionName, BSTR Text );
        ```

        ### Входные параметры

        +-------------------------------------------+
        | UniqueId   | - уникальный идентификатор,  |
        +-------------------------------------------+
        | OptionName | - отображаемое имя свойства, |
        +-------------------------------------------+
        | Text       | - текст предупреждения.      |
        +-------------------------------------------+

        ### Возвращаемое значение

        +-----------------------------------------+
        | TRUE | - в случае успешного завершения. |
        +-----------------------------------------+
        """
        ...

    def GetCurrentOptionValue(self, UniqueId):  # isavetopreviusparam7_getcurrentoptionvalue.html
        """
        ## GetCurrentOptionValue - Получить текущее значение, выбранное в диалоге параметров конвертации

        ### Синтаксис Automation

        ```
        BSTR GetCurrentOptionValue( BSTR UniqueId );
        ```

        ### Входные параметры

        +----------------------------------------+
        | UniqueId | - уникальный идентификатор. |
        +----------------------------------------+

        ### Возвращаемое значение

        - Текущее значение свойства.
        """
        ...




class IPropertyControl(IKompasAPIObject):  # ipropertycontrol.html
    """
    ## Интерфейс IPropertyControl

    Базовый интерфейс элемента управления Панели свойств.

    При изменении библиотечных элементов управления система передает управление библиотеке посредством вызовов соответствующих событий. Данный интерфейс является базовым для всех элементов управления Панели свойств. Все элементы управления из СontrolTypeEnum являются наследниками от IPropertyControl.

    1. Имя выводится рядом с элементом управления (в зависимости от настроек Панели свойств). Если имя не задано или задана пустая строка, имя выводиться не будет.

    2. Подсказки для элемента управления устанавливаются функциями Hint и Tips.

    3. Идентификатор элемента управления, установленный методом Id, будет приходить в событиях от данного элемента управления, как параметр.

    4. Данный интерфейс может быть получен от интерфейса коллекции элементов управления IPropertyControls с помощью свойства IPropertyControls::Item или метода IPropertyControls::Add.

    5. Посредством вызова метода IUnknown::QueryInterface (const GUID far& iid, void** pif) интерфейс позволяет получить дополнительный интерфейс IPropertyControl1.
    """

    @property
    def ControlType(self):  # ipropertycontrol_controltype.html
        """
        ## СontrolType - Тип элемента управления

        Тип данных: тип элемента управления из перечисления СontrolTypeEnum.

        Свойство только для чтения.

        ### Синтаксис Automation

        ```
        СontrolType = iObject.СontrolType       # Получить свойство (* )
        СontrolType = iObject.GetСontrolType()  # Получить свойство(**)
        ```
        """
        ...

    @property
    def Enable(self) -> bool:  # ipropertycontrol_enable.html
        """
        ## Enable - Состояние (доступность) элемента управления

        Тип данных: BOOL

        Позволяет получить и установить доступность элемента управления.

        ### Синтаксис Automation

        ```
        Enable = iObject.Enable       # Получить свойство (* )
        iObject.Enable = Enable       # Установить свойство (* )
        Enable = iObject.GetEnable()  # Получить свойство(**)
        iObject.SetEnable (Enable)    # Установить свойство (**)
        ```
        """
        ...
    @Enable.setter
    def Enable(self, value: bool): ...

    @property
    def Hint(self) -> str:  # ipropertycontrol_hint.html
        """
        ## Hint - Подсказки элемента управления

        Функция не поддерживается

        Тип данных: строка

        Позволяет получить и установить подсказку элемента управления.

        ### Синтаксис Automation

        ```
        Hint = iObject.Hint       # Получить свойство (* )
        iObject.Hint = Hint       # Установить свойство (* )
        Hint = iObject.GetHint()  # Получить свойство(**)
        iObject.SetHint (Hint)    # Установить свойство (**)
        ```
        """
        ...
    @Hint.setter
    def Hint(self, value: str): ...

    @property
    def Id(self) -> int:  # ipropertycontrol_id.html
        """
        ## Id - Идентификатор элемента управления

        Тип данных: long

        Позволяет получить и установить идентификатор элемента управления.

        ### Синтаксис Automation

        ```
        Id = iObject.Id       # Получить свойство (* )
        iObject.Id = Id       # Установить свойство (* )
        Id = iObject.GetId()  # Получить свойство(**)
        iObject.SetId (Id)    # Установить свойство (**)
        ```
        """
        ...
    @Id.setter
    def Id(self, value: int): ...

    @property
    def Name(self) -> str:  # ipropertycontrol_name.html
        """
        ## Name - Имя элемента управления

        Тип данных: строка

        Позволяет получить и установить имя элемента управления.

        ### Синтаксис Automation

        ```
        Name = iObject.Name       # Получить свойство (* )
        iObject.Name = Name       # Установить свойство (* )
        Name = iObject.GetName()  # Получить свойство(**)
        iObject.SetName (Name)    # Установить свойство (**)
        ```
        """
        ...
    @Name.setter
    def Name(self, value: str): ...

    @property
    def NameVisibility(self) -> int:  # ipropertycontrol_namevisibility.html
        """
        ## NameVisibility - Видимость имени на Панели свойств

        Тип данных: PropertyControlNameVisibility

        Позволяет получить и установить видимость имени элемента управления на Панели свойств.

        ### Синтаксис Automation

        ```
        NameVisibility = iObject.NameVisibility       # Получить свойство (* )
        iObject.NameVisibility = NameVisibility       # Установить свойство (* )
        NameVisibility = iObject.GetNameVisibility()  # Получить свойство(**)
        iObject.SetNameVisibility (NameVisibility)    # Установить свойство (**)
        ```
        """
        ...
    @NameVisibility.setter
    def NameVisibility(self, value: int): ...

    @property
    def Tips(self) -> str:  # ipropertycontrol_tips.html
        """
        ## Tips - Подсказки элемента управления

        Функция не поддерживается

        Тип данных: строка

        Позволяет получить и установить подсказку элемента управления.

        ### Синтаксис Automation

        ```
        Tips = iObject.Tips       # Получить свойство (* )
        iObject.Tips = Tips       # Установить свойство (* )
        Tips = iObject.GetTips()  # Получить свойство(**)
        iObject.SetTips (Tips)    # Установить свойство (**)
        ```
        """
        ...
    @Tips.setter
    def Tips(self, value: str): ...

    @property
    def Value(self) -> typing.Any:  # ipropertycontrol_value.html
        """
        ## Value - Значение элемента управления

        Тип данных: VARIANT

        1. Позволяет получить и установить значение элемента управления.

        2. Тип данных определяется типом элемента управления из перечисления СontrolTypeEnum:

        •для типа ksControlListInt используется тип long,

        •для типа ksControlListReal используется тип double,

        •для типа ksControlEditStr ограничения на значение не накладываются,

        •для типа ksControlEditAngle используется тип double,

        •для типа ksControlEditLength используется тип double,

        •для типа ksControlEditPoint используется тип VT_ARRAY | VT_R8 - на 2 значения координат.

        ### Синтаксис Automation

        ```
        Value = iObject.Value       # Получить свойство (* )
        iObject.Value = Value       # Установить свойство (* )
        Value = iObject.GetValue()  # Получить свойство(**)
        iObject.SetValue (Value)    # Установить свойство (**)
        ```
        """
        ...
    @Value.setter
    def Value(self, value: typing.Any): ...

    @property
    def Visible(self) -> bool:  # ipropertycontrol_visible.html
        """
        ## Visible - Видимость элемента управления

        Тип данных: BOOL

        Позволяет получить и установить свойство видимости элемента управления.

        ### Синтаксис Automation

        ```
        Visible = iObject.Visible       # Получить свойство (* )
        iObject.Visible = Visible       # Установить свойство (* )
        Visible = iObject.GetVisible()  # Получить свойство(**)
        iObject.SetVisible (Visible)    # Установить свойство (**)
        ```
        """
        ...
    @Visible.setter
    def Visible(self, value: bool): ...




class IPropertyBasePoint(IPropertyControl):  # ipropertybasepoint.html
    """
    ## Интерфейс IPropertyBasePoint

    Интерфейс Элемент Панели свойств - Базовая точка
    """

    @property
    def CenterPointVisible(self) -> bool:  # ipropertybasepoint_centerpointvisible.html
        """
        ## CenterPointVisible - Отображать центральную точку

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        CenterPointVisible = Object.CenterPointVisible       # Получить свойство (* )
        Object.CenterPointVisible = CenterPointVisible       # Установить свойство (* )
        CenterPointVisible = Object.GetCenterPointVisible()  # Получить свойство(**)
        Object.SetCenterPointVisible( CenterPointVisible )   # Установить свойство (**)
        ```
        """
        ...
    @CenterPointVisible.setter
    def CenterPointVisible(self, value: bool): ...




class IPropertyMarking(IPropertyControl):  # ipropertymarking.html
    """
    ## Интерфейс IPropertyMarking

    Интерфейс элемента Панели свойств - Обозначение.
    """

    @property
    def ReadOnly(self) -> bool:  # ipropertymarking_readonly.html
        """
        ## ReadOnly - Запрет редактирования значения

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        ReadOnly = Object.ReadOnly       # Получить свойство (* )
        Object.ReadOnly = ReadOnly       # Установить свойство (* )
        ReadOnly = Object.GetReadOnly()  # Получить свойство(**)
        Object.SetReadOnly( ReadOnly )   # Установить свойство (**)
        ```
        """
        ...
    @ReadOnly.setter
    def ReadOnly(self, value: bool): ...

    def GetMarking(self, MarkingType, AddSystemDelimer):  # ipropertymarking_getmarking.html
        """
        ## GetMarking - Обозначение исполнения

        ### Синтаксис Automation

        ```
        BSTR GetMarking( ksVariantMarkingTypeEnum MarkingType, BOOL AddSystemDelimer );
        ```

        ### Входные параметры

        +-----------------------------------------------------------------------------------------+
        | MarkingType      | - индекс части обозначения из перечисления ksVariantMarkingTypeEnum, |
        +-----------------------------------------------------------------------------------------+
        | AddSystemDelimer | - добавлять разделители.                                             |
        +-----------------------------------------------------------------------------------------+
        """
        ...

    def MarkingVisible(self, MarkingType) -> bool:  # ipropertymarking_markingvisible.html
        """
        ## MarkingVisible - Признак отображение части обозначения

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        MarkingVisible = Object.MarkingVisible( MarkingType )     # Получить свойство (* )
        Object.MarkingVisible( MarkingType ) = MarkingVisible     # Установить свойство (* )
        MarkingVisible = Object.GetMarkingVisible( MarkingType )  # Получить свойство(**)
        Object.SetMarkingVisible( MarkingType, MarkingVisible )   # Установить свойство (**)
        ```

        ### Входные параметры

        +-------------------------------------------------------------------------------------+
        | MarkingType | -- индекс части обозначения из перечисления ksVariantMarkingTypeEnum. |
        +-------------------------------------------------------------------------------------+
        """
        ...

    def SetMarking(self, MarkingType, Marking):  # ipropertymarking_setmarking.html
        """
        ## SetMarking - Обозначение исполнения

        При передаче полного обозначения части обозначения нужно разделить разделителями |$

        ### Синтаксис Automation

        ```
        BOOL SetMarking( ksVariantMarkingTypeEnum MarkingType, BSTR Marking );
        ```

        ### Входные параметры

        +-------------------------------------------------------------------------------------+
        | MarkingType | -- индекс части обозначения из перечисления ksVariantMarkingTypeEnum, |
        +-------------------------------------------------------------------------------------+
        | Marking     | - текст обозначения.                                                  |
        +-------------------------------------------------------------------------------------+
        """
        ...

    def SetMarkingVisible(self, MarkingType, arg1):
        ...




class IPropertyBmpList(IPropertyControl):  # ipropertybmplist.html
    """
    ## Интерфейс IPropertyBmpList

    Элемент Панели свойств Раскрывающийся список графических объектов.

    Данный интерфейс может быть получен от интерфейса коллекции элементов управления IPropertyControls с помощью свойства IPropertyControls::Item или метода IPropertyControls::Add с последующим приведением интерфейса IPropertyControl к данному интерфейсу с помощью метода IUnknown::QueryInterface.
    """

    @property
    def Count(self) -> int:  # ipropertybmplist_count.html
        """
        ## Count - Количество элементов списка

        Тип данных: long.

        1. Свойство позволяет получить количество элементов списка.

        2. Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        Count = Object.Count       # Получить свойство (* )
        Count = Object.GetCount()  # Получить свойство(**)
        ```
        """
        ...

    @property
    def CurrentIndex(self) -> int:  # ipropertybmplist_currentindex.html
        """
        ## CurrentIndex - Индекс текущего значения

        Тип данных: long.

        Свойство позволяет устанавливать и получать индекс текущего элемента.

        ### Синтаксис Automation

        ```
        CurrentIndex = Object.CurrentIndex        # Получить свойство (* )
        Object.CurrentIndex = CurrentIndex        # Установить свойство (* )
        CurrentIndex = Object.GetCurrentIndex( )  # Получить свойство(**)2
        Object.SetCurrentIndex( CurrentIndex )    # Установить свойство (**)
        ```
        """
        ...
    @CurrentIndex.setter
    def CurrentIndex(self, value: int): ...

    @property
    def ResModule(self) -> typing.Any:  # ipropertybmplist_resmodule.html
        """
        ## ResModule - Модуль с ресурсом растрового изображения

        Тип данных: VARIANT.

        Свойство позволяет устанавливать и получать модуль с ресурсом битмапа.

        ### Синтаксис Automation

        ```
        ResModule = Object.ResModule        # Получить свойство (* )
        Object.ResModule = ResModule        # Установить свойство (* )
        ResModule = Object.GetResModule( )  # Получить свойство(**)
        Object.SetResModule( ResModule )    # Установить свойство (**)
        ```
        """
        ...
    @ResModule.setter
    def ResModule(self, value: typing.Any): ...

    def Add(self, NewStr, NewBmp):  # ipropertybmplist_add.html
        """
        ## Add- Добавить значение в список

        ### Синтаксис Automation

        ```
        void Add( BSTR NewStr, VARIANT NewBmp );
        ```

        ### Входные параметры

        +--------------------------------------------------------------------------------------------------------------------------+
        | NewStr | - добавляемое значение,                                                                                         |
        +--------------------------------------------------------------------------------------------------------------------------+
        | NewBmp | - идентификатор значка в файле ресурсов (тип данных long) или полный путь к bmp-файлу значка (тип данных BSTR). |
        +--------------------------------------------------------------------------------------------------------------------------+
        """
        ...

    def ClearList(self):  # ipropertybmplist_clearlist.html
        """
        ## ClearList - Очистить список значений

        ### Синтаксис Automation

        ```
        void ClearList();
        ```
        """
        ...

    def Find(self, Val):  # ipropertybmplist_find.html
        """
        ## Find - Найти индекс в списке по значению

        ### Синтаксис Automation

        ```
        long Find( BSTR val );
        ```

        ### Входные параметры

        +-------------------------------------------+
        | Val | - значение в раскрывающемся списке. |
        +-------------------------------------------+

        ### Возвращаемое значение

        +---------------------------------------------------------------+
        | Индекс строки со значением Val в списке | - в случае успеха,  |
        +---------------------------------------------------------------+
        | -1                                      | - в случае неудачи. |
        +---------------------------------------------------------------+
        """
        ...

    def IconFont(self, Index) -> str:  # ipropertybmplist_iconfont.html
        """
        ## IconFont - Шрифт иконок списка

        Тип данных: BSTR

        ### Синтаксис Automation

        ```
        IconFont = Object.IconFont( Index )     # Получить свойство (* )
        Object.IconFont( Index ) = IconFont     # Установить свойство (* )
        IconFont = Object.GetIconFont( Index )  # Получить свойство(**)2
        Object.SetIconFont( Index, IconFont )   # Установить свойство (**)
        ```

        ### Входные параметры

        +-------------------------------------+
        | Index | - индекс значения в списке. |
        +-------------------------------------+
        """
        ...

    def SetIconFont(self, Index, arg1):
        ...




class IPropertyCheckBox(IPropertyControl):  # ipropertycheckbox.html
    """
    ## Интерфейс IPropertyCheckBox

    Интерфейс элемента управления Панели свойств Опция.

    1. Вместо стандартного вида элемента управления можно задать значки для его различных состояний.

    2. Данный интерфейс можно получить через интерфейс коллекции элементов управления панели свойств IPropertyControls.

    3. При активизации элемента управления вызывается событие ksIPropertyManagerNotify::ChangeControlValue.
    """

    @property
    def VisualStyle(self) -> int:  # ipropertycheckbox_visualstyle.html
        """
        ## VisualStyle - Визуальный стиль

        Тип данных: из перечисления ksCheckBoxVisualStyleEnum

        Свойство задает визуальный стиль отображения чекбокса:

        •ksCheckBoxDefault - обычный чекбокс,

        •ksCheckBoxSwitcher - переключатель.

        ### Синтаксис Automation

        ```
        VisualStyle = Object.VisualStyle       # Получить свойство (* )
        Object.VisualStyle = VisualStyle       # Установить свойство (* )
        VisualStyle = Object.GetVisualStyle()  # Получить свойство (**)
        Object.SetVisualStyle( VisualStyle )   # Установить свойство (**)
        ```
        """
        ...
    @VisualStyle.setter
    def VisualStyle(self, value: int): ...

    def SetCustomBitmaps(self, IdUnchecked, IdChecked, IdIndeterminate, HInstance):  # ipropertycheckbox_setcustombitmaps.html
        """
        ## SetCustomBitmaps - Задать значки для различных состояний элемента управления

        Функция не поддерживается

        1. Значки могут быть заданы в виде идентификаторов в ресурсе dll-модуля (тип данных long) или в виде полного пути к bmp-файлу значка (тип данных BSTR). Обязательным является задание значка для ненажатого состояния idUnchecked, остальные могут быть не заданы.

        2. Все значения переменных должны быть одного типа (или long или BSTR).

        3. Если значки задаются в виде идентификатора, то обязательно должен быть задан dll-модуль, в ресурсах которого лежат эти значки. Модуль с ресурсами значков hInstance может быть задан в виде HINSTANCE (тип значения long) или полного пути к файлу (тип значения BSTR) dll-модуля.

        ### Синтаксис Automation

        ```
        void SetCustomBitmaps (const VARIANT& idUnchecked,
        const VARIANT& idChecked,
        const VARIANT& idIndeterminate,
        const VARIANT& hInstance);
        ```

        ### Входные параметры

        +-------------------------------------------------------+
        | idUnchecked     | - значок ненажатого состояния,      |
        +-------------------------------------------------------+
        | idChecked       | - битмап нажатого состояния,        |
        +-------------------------------------------------------+
        | idIndeterminate | - битмап неопределенного состояния, |
        +-------------------------------------------------------+
        | hInstance       | - dll-модуль с ресурсами значков.   |
        +-------------------------------------------------------+
        """
        ...




class IPropertyColor(IPropertyControl):  # ipropertycolor.html
    """
    ## Интерфейс IPropertyColor

    Интерфейс элемента управления Панели свойств - Выбор цвета.

    1. Данный интерфейс можно получить, используя коллекцию элементов управления Панели свойств IPropertyControls.

    2. При изменении значения параметра вызывается событие ksPropertyManagerNotify::ChangeControlValue.
    """

    @property
    def DefaultButtonName(self) -> int:  # ipropertycolor_defaultbuttonname.html
        """
        ## DefaultButtonName - Имя кнопки

        Тип данных: long.

        1. Свойство позволяет получить имя кнопки Цвет по умолчанию или изменить имя на произвольное.

        2. Свойство доступно только если свойство EnableDefaultButton имеет значение TRUE.

        ### Синтаксис Automation

        ```
        DefaultButtonName = iObject.DefaultButtonName     # Получить свойство (* )
        iObject.DefaultButtonName = DefaultButtonName     # Установить свойство (* )
        iObject.GetDefaultButtonName()                    # Получить свойство(**)
        iObject.SetDefaultButtonName (DefaultButtonName)  # Установить свойство (**)
        ```
        """
        ...
    @DefaultButtonName.setter
    def DefaultButtonName(self, value: int): ...

    @property
    def DefaultColor(self) -> int:  # ipropertycolor_defaultcolor.html
        """
        ## DefaultColor - Цвет по умолчанию

        Тип данных: long.

        1. Свойство позволяет получить или изменить значение цвета для кнопки Цвет по умолчанию.

        2. Цвет по умолчанию доступен только если свойство EnableDefaultButton имеет значение TRUE.

        ### Синтаксис Automation

        ```
        DefaultColor = iObject.DefaultColor       # Получить свойство (* )
        iObject.DefaultColor = DefaultColor       # Установить свойство (* )
        DefaultColor = iObject.GetDefaultColor()  # Получить свойство(**)
        iObject.SetDefaultColor (DefaultColor)    # Установить свойство (**)
        ```
        """
        ...
    @DefaultColor.setter
    def DefaultColor(self, value: int): ...

    @property
    def EnableDefaultButton(self) -> bool:  # ipropertycolor_enabledefaultbutton.html
        """
        ## EnableDefaultButton - Отображение кнопки «Цвет по умолчанию»

        Тип данных: BOOL.

        Значения свойства:

        +-----------------------------------------------------------------------------------------------------+
        | TRUE  | - вверху раскрывающегося списка будет отображаться дополнительная кнопка Цвет по умолчанию, |
        +-----------------------------------------------------------------------------------------------------+
        | FALSE | - кнопка Цвет по умолчанию не будет отображаться.                                           |
        +-----------------------------------------------------------------------------------------------------+

        ### Синтаксис Automation

        ```
        EnableDefaultButton = iObject.EnableDefaultButton       # Получить свойство (* )
        iObject.EnableDefaultButton = EnableDefaultButton       # Установить свойство (* )
        EnableDefaultButton = iObject.GetEnableDefaultButton()  # Получить свойство(**)
        iObject.SetEnableDefaultButton (EnableDefaultButton)    # Установить свойство (**)
        ```
        """
        ...
    @EnableDefaultButton.setter
    def EnableDefaultButton(self, value: bool): ...

    @property
    def Width(self) -> int:  # ipropertycolor_width.html
        """
        ## Width - Шаблон для расчета ширины элемента управления в горизонтальном положении (в символах)

        Тип данных:long.

        Значения свойства:

        Количество видимых символов в текстовом поле при горизонтальном положении Панели свойств.

        Для расчета ширины элемента управления в горизонтальном положении используется символ «0».

        ### Синтаксис Automation

        ```
        Width = iObject.Width       # Получить свойство (* )
        iObject.Width = Width       # Установить свойство (* )
        Width = iObject.GetWidth()  # Получить свойство(**)
        iObject.SetWidth (Width)    # Установить свойство (**)
        ```
        """
        ...
    @Width.setter
    def Width(self, value: int): ...




class IPropertyGroupBegin(IPropertyControl):  # ipropertygroupbegin.html
    """
    ## Интерфейс IPropertyGroupBegin

    Элемент Панели свойств - Начало группы контролов.

    1. Данный интерфейс позволяет объединить несколько контролов Панели свойств в группу. Контролы, добавленные после IPropertyGroupBegin, будут объединены в группу.

    2. Для завершения группы нужно добавить контрол IPropertyGroupEnd.

    3. Группа иметь возможность сворачивать и прятать контролы, входящие в нее, если у группы признак IPropertyGroupBegin::Expanding выставлен равным true.

    4. Данный интерфейс может быть получен от интерфейса коллекции элементов управления IPropertyControls с помощью свойства IPropertyControls::Item или метода IPropertyControls::Add с последующим приведением интерфейса IPropertyControl к данному интерфейсу с помощью метода IUnknown::QueryInterface.
    """

    @property
    def Expanded(self) -> bool:  # ipropertygroupbegin_expanded.html
        """
        ## Expanded - Текущее состояние. TRUE - развернута. FALSE - свернута

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        Expanded = Object.Expanded       # Получить свойство (* )
        Object.Expanded = Expanded       # Установить свойство (* )
        Expanded = Object.GetExpanded()  # Получить свойство(**)
        Object.SetExpanded( Expanded )   # Установить свойство (**)
        ```
        """
        ...
    @Expanded.setter
    def Expanded(self, value: bool): ...

    @property
    def Expanding(self) -> bool:  # ipropertygroupbegin_expanding.html
        """
        ## Expanding - Доступность сворачивания

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        Expanding = Object.Expanding       # Получить свойство (* )
        Object.Expanding = Expanding       # Установить свойство (* )
        Expanding = Object.GetExpanding()  # Получить свойство(**)
        Object.SetExpanding( Expanding )   # Установить свойство (**)
        ```
        """
        ...
    @Expanding.setter
    def Expanding(self, value: bool): ...




class IPropertyGroupEnd(IPropertyControl):  # ipropertygroupend.html
    """
    ## Интерфейс IPropertyGroupEnd

    Интерфейс элемента панели свойств - Конец группы контролов.

    1. Данный интерфейс позволяет завершить объединение несколько контролов панели свойств в группу. Контролы, добавленные после IPropertyGroupBegin, будут объединены в группу.

    2. Для завершения группы нужно добавить контрол IPropertyGroupEnd.

    3. Группа иметь возможность сворачивать и прятать контролы, входящие в нее, если у группы признак IPropertyGroupBegin::Expanding выставлен равным true.

    4. Данный интерфейс может быть получен от интерфейса коллекции элементов управления *IPropertyControls с помощью свойства IPropertyControls::Item или метода IPropertyControls::Add с последующим приведением интерфейса IPropertyControl к данному интерфейсу с помощью метода IUnknown::QueryInterface.
    """

    ...


class IPropertyLinkButton(IPropertyControl):  # ipropertylinkbutton.html
    """
    ## Интерфейс IPropertyLinkButton

    Интерфейс элемента Панели свойств - Набор кнопок в виде ссылок.
    """

    def AddButton(self, BtnID, Lebel) -> bool:  # ipropertylinkbutton_addbutton.html
        """
        ## AddButton - Добавить кнопку

        ### Синтаксис Automation

        ```
        BOOL AddButton( long BtnID, BSTR Lebel );
        ```

        ### Входные параметры

        +---------------------------------+
        | BtnID | - идентификатор кнопки, |
        +---------------------------------+
        | Lebel | - заголовок кнопки.     |
        +---------------------------------+

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def ButtonChecked(self, BtnID) -> bool:  # ipropertylinkbutton_buttonchecked.html
        """
        ## ButtonChecked - Нажатие кнопки

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        ButtonChecked = Object.ButtonChecked( BtnID )     # Получить свойство (* )
        Object.ButtonChecked( BtnID ) = ButtonChecked     # Установить свойство (* )
        ButtonChecked = Object.GetButtonChecked( BtnID )  # Получить свойство(**)
        Object.SetButtonChecked( BtnID, ButtonChecked )   # Установить свойство (**)
        ```

        ### Входные параметры

        +-------------------------------------+
        | long BtnID | - идентификатор кнопки |
        +-------------------------------------+
        """
        ...

    def ButtonEnable(self, BtnID) -> bool:  # ipropertylinkbutton_buttonenable.html
        """
        ## ButtonEnable - Доступность кнопки

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        ButtonEnable = Object.ButtonEnable( BtnID )     # Получить свойство (* )
        Object.ButtonEnable( BtnID ) = ButtonEnable     # Установить свойство (* )
        ButtonEnable = Object.GetButtonEnable( BtnID )  # Получить свойство(**)
        Object.SetButtonEnable( BtnID, ButtonEnable )   # Установить свойство (**)
        ```

        ### Входные параметры

        +--------------------------------------+
        | long BtnID | - идентификатор кнопки. |
        +--------------------------------------+
        """
        ...

    def ButtonVisible(self, BtnID) -> bool:  # ipropertylinkbutton_buttonvisible.html
        """
        ## ButtonVisible - Отображение кнопки

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        ButtonVisible = Object.ButtonVisible( BtnID )     # Получить свойство (* )
        Object.ButtonVisible( BtnID ) = ButtonVisible     # Установить свойство (* )
        ButtonVisible = Object.GetButtonVisible( BtnID )  # Получить свойство(**)
        Object.SetButtonVisible( BtnID, ButtonVisible )   # Установить свойство (**)
        ```

        ### Входные параметры

        +--------------------------------------+
        | long BtnID | - идентификатор кнопки. |
        +--------------------------------------+
        """
        ...

    def SetButtonChecked(self, BtnID, arg1):
        ...

    def SetButtonEnable(self, BtnID, arg1):
        ...

    def SetButtonVisible(self, BtnID, arg1):
        ...




class IPropertyTwinSwitcher(IPropertyControl):  # ipropertytwinswitcher.html
    """
    ## Интерфейс IPropertyTwinSwitcher

    Интерфейс элемента Панели свойств - переключателя.

    1. Интерфейс позволяет добавить элемент панели свойств - переключатель.

    Для обозначения состояния контрола имеются две текстовых метки.

    Первая метка задается свойством IPropertyControl::Name.

    Вторая метка задается свойством IPropertyTwinSwitcher::Label2.

    2. Данный интерфейс может быть получен от интерфейса коллекции элементов управления IPropertyControls с помощью свойства IPropertyControls::Item или метода IPropertyControls::Add с последующим приведением интерфейса IPropertyControl к данному интерфейсу с помощью метода IUnknown::QueryInterface.
    """

    @property
    def Label2(self) -> str:  # ipropertytwinswitcher_label2.html
        """
        ## Label2 - Текст для второго состояния

        Тип данных: BSTR

        ### Синтаксис Automation

        ```
        Label2 = Object.Label2       # Получить свойство (* )
        Object.Label2 = Label2       # Установить свойство (* )
        Label2 = Object.GetLabel2()  # Получить свойство(**)
        Object.SetLabel2( Label2 )   # Установить свойство (**)
        ```
        """
        ...
    @Label2.setter
    def Label2(self, value: str): ...




class IPropertyEdit(IPropertyControl):  # ipropertyedit.html
    """
    ## Интерфейс IPropertyEdit

    Интерфейс элемента управления Панели свойств типа Поле ввода.

    1. Данный интерфейс позволяет получить и установить параметры поля ввода.

    2. Данный интерфейс может быть получен от интерфейса коллекции элементов управления IPropertyControls с помощью свойства IPropertyControls::Item или метода IPropertyControls::Add с последующим приведением интерфейса IPropertyControl к данному интерфейсу с помощью метода IUnknown::QueryInterface.
    """

    @property
    def CheckState(self) -> int:  # ipropertyedit_checkstate.html
        """
        ## CheckState - Состояние опции

        Тип данных: состояние опции из перечисления CheckStateEnum.

        Это свойство влияет на состояние переключателя, если свойство WithCheck имеет значение TRUE, и на отображение текста в поле элемента управления, подробнее см. CheckStateEnum.

        ### Синтаксис Automation

        ```
        CheckState = iObject.CheckState       # Получить свойство (* )
        iObject.CheckState = CheckState       # Установить свойство (* )
        CheckState = iObject.GetCheckState()  # Получить свойство(**)
        iObject.SetCheckState (CheckState)    # Установить свойство (**)
        ```
        """
        ...
    @CheckState.setter
    def CheckState(self, value: int): ...

    @property
    def MaxValue(self) -> typing.Any:  # ipropertyedit_maxvalue.html
        """
        ## MaxValue - Максимальное значение

        Тип данных: VARIANT

        1. Свойство доступно только для чтения.

        2. Тип данных определяется типом элемента управления из перечисления ControlTypeEnum:

        •для типа ksControlListInt используется тип long,

        •для типа ksControlListReal используется тип double,

        •для типа ksControlEditStr ограничения на значение не накладываются,

        •для типа ksControlEditAngle используется тип double,

        •для типа ksControlEditLength используется тип double,

        •для типа ksControlEditPoint используется тип VT_ARRAY | VT_R8 - на 2 значения координат.

        ### Синтаксис Automation

        ```
        MaxValue = iObject.MaxValue       # Получить свойство (* )
        MaxValue = iObject.GetMaxValue()  # Получить свойство(**)
        ```
        """
        ...

    @property
    def MinValue(self) -> typing.Any:  # ipropertyedit_minvalue.html
        """
        ## MinValue - Минимальное значение

        Тип данных: VARIANT

        1. Свойство доступно только для чтения.

        2. Тип данных определяется типом элемента управления из перечисления ControlTypeEnum:

        •для типа ksControlListInt используется тип long,

        •для типа ksControlListReal используется тип double,

        •для типа ksControlEditStr ограничения на значение не накладываются,

        •для типа ksControlEditAngle используется тип double,

        •для типа ksControlEditLength используется тип double,

        •для типа ksControlEditPoint используется тип VT_ARRAY | VT_R8 - на 2 значения координат.

        ### Синтаксис Automation

        ```
        MinValue = iObject.MinValue       # Получить свойство (* )
        MinValue = iObject.GetMinValue()  # Получить свойство(**)
        ```
        """
        ...

    @property
    def ReadOnly(self) -> bool:  # ipropertyedit_readonly.html
        """
        ## ReadOnly - Запрет редактирования значения

        Тип данных: BOOL.

        Значение свойства:

        +-------------------------------------------------------------------------------+
        | TRUE  | - ввод нового значения с клавиатуры в поле запрещен,                  |
        +-------------------------------------------------------------------------------+
        | FALSE | - ввод нового значения с клавиатуры в раскрывающийся список разрешен. |
        +-------------------------------------------------------------------------------+

        ### Синтаксис Automation

        ```
        ReadOnly = iObject.ReadOnly       # Получить свойство (* )
        iObject.ReadOnly = ReadOnly       # Установить свойство (* )
        ReadOnly = iObject.GetReadOnly()  # Получить свойство(**)
        iObject.SetReadOnly (ReadOnly)    # Установить свойство (**)
        ```
        """
        ...
    @ReadOnly.setter
    def ReadOnly(self, value: bool): ...

    @property
    def Step(self) -> bool:  # ipropertyedit_step.html
        """
        ## Step - Шаг

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        Step = Object.Step       # Получить свойство (* )
        Object.Step = Step       # Установить свойство (* )
        Step = Object.GetStep()  # Получить свойство(**)
        Object.SetStep( Step )   # Установить свойство (**)
        ```
        """
        ...
    @Step.setter
    def Step(self, value: bool): ...

    @property
    def Width(self) -> int:  # ipropertyedit_width.html
        """
        ## Width - Шаблон для расчета ширины элемента управления в горизонтальном положении (в символах)

        Тип данных: long.

        Значение свойства:

        Количество видимых символов в поле при горизонтальном положении Панели свойств.

        Для расчета ширины элемента управления используется символ «0».

        ### Синтаксис Automation

        ```
        Width = iObject.Width       # Получить свойство (* )
        iObject.Width = Width       # Установить свойство (* )
        Width = iObject.GetWidth()  # Получить свойство(**)
        iObject.SetWidth (Width)    # Установить свойство (**)
        ```
        """
        ...
    @Width.setter
    def Width(self, value: int): ...

    @property
    def WithCheck(self) -> bool:  # ipropertyedit_withcheck.html
        """
        ## WithCheck - Наличие опции рядом с полем ввода

        Тип данных: BOOL

        Значение свойства:

        +------------------------------------------------------------+
        | TRUE  | - рядом с элементом управления выводится опция,    |
        +------------------------------------------------------------+
        | FALSE | - рядом с элементом управления не выводится опция. |
        +------------------------------------------------------------+

        ### Синтаксис Automation

        ```
        WithCheck = iObject.WithCheck       # Получить свойство (* )
        iObject.WithCheck = WithCheck       # Установить свойство (* )
        WithCheck = iObject.GetWithCheck()  # Получить свойство(**)
        iObject.SetWithCheck (WithCheck)    # Установить свойство (**)
        ```
        """
        ...
    @WithCheck.setter
    def WithCheck(self, value: bool): ...

    def SetStringRegularValidator(self, InputMask, ValidateMask, ErrorMessage, EmptyStringErrorMessage) -> bool:  # ipropertyedit_setstringregularvalidator.html
        """
        ## SetStringRegularValidator - Установить валидатор регулярного выражения для строкового редактора

        ### Синтаксис Automation

        ```
        BOOL SetStringRegularValidator( BSTR InputMask, BSTR ValidateMask, BSTR ErrorMessage, BSTR EmptyStringErrorMessage );
        ```

        ### Входные параметры

        +----------------------------------------------------------+
        | InputMask               | - маска ввода,                 |
        +----------------------------------------------------------+
        | ValidateMask            | - маска допустимых символов,   |
        +----------------------------------------------------------+
        | ErrorMessage            | - сообщение об ошибке,         |
        +----------------------------------------------------------+
        | EmptyStringErrorMessage | - сообщение для пустой строки. |
        +----------------------------------------------------------+

        Версия КОМПАС v22

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def SetValueRange(self, MinVal, MaxVal):  # ipropertyedit_setvaluerange.html
        """
        ## SetValueRange - Установить новые ограничения на значение

        Тип данных максимального и минимального допустимого значения определяется типом элемента управления из перечисления ControlTypeEnum:

        •для типа ksControlListInt используется тип long,

        •для типа ksControlListReal используется тип double,

        •для типа ksControlEditStr ограничения на значение не накладываются,

        •для типа ksControlEditAngle используется тип double,

        •для типа ksControlEditLength используется тип double,

        •для типа ksControlEditPoint используется тип VT_ARRAY | VT_R8 - на 2 значения координат.

        ### Синтаксис Automation

        ```
        BOOL SetValueRange (const VARIANT& minVal, const VARIANT& maxVal);
        ```

        ### Входные параметры

        +-----------------------------------+
        | minVal | - минимальное значение,  |
        +-----------------------------------+
        | maxVal | - максимальное значение. |
        +-----------------------------------+
        """
        ...




class IPropertyEditList(IPropertyControl):  # ipropertyeditlist.html
    """
    ## Интерфейс IPropertyEditList

    Интерфейс элемента управления Панели свойств Список.

    Позволяет управлять списком из Панели свойств.

    1. Данный интерфейс может быть получен от интерфейса коллекции элементов управления IPropertyControls с помощью свойства IPropertyControls::Item или метода IPropertyControls::Add с последующим приведением интерфейса IPropertyControl к данному интерфейсу с помощью метода IUnknown::QueryInterface.

    2. Посредством вызова метода IUnknown::QueryInterface (const GUID far& iid, void** pif) у данного интерфейса можно получить дополнительный интерфейс вида IPropertyToolBar.
    """

    @property
    def AllowDelete(self) -> bool:  # ipropertyeditlist_allowdelete.html
        """
        ## AllowDelete - Разрешить удалять элементы из списка

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        AllowDelete = Object.AllowDelete       # Получить свойство (* )
        Object.AllowDelete = AllowDelete       # Установить свойство (* )
        AllowDelete = Object.GetAllowDelete()  # Получить свойство (**)
        Object.SetAllowDelete( AllowDelete )   # Установить свойство (**)
        ```
        """
        ...
    @AllowDelete.setter
    def AllowDelete(self, value: bool): ...

    @property
    def CheckState(self) -> int:  # ipropertyeditlist_checkstate.html
        """
        ## CheckState - Состояние контрола. Текущий, Не текущий

        Тип данных: из перечисления CheckStateEnum

        ### Синтаксис Automation

        ```
        CheckState = Object.CheckState       # Получить свойство (* )
        Object.CheckState = CheckState       # Установить свойство (* )
        CheckState = Object.GetCheckState()  # Получить свойство (**)
        Object.SetCheckState( CheckState )   # Установить свойство (**)
        ```
        """
        ...
    @CheckState.setter
    def CheckState(self, value: int): ...

    @property
    def ComputeUniqueNames(self) -> bool:  # ipropertyeditlist_computeuniquenames.html
        """
        ## ComputeUniqueNames - Генерация уникальных имен элементов

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        ComputeUniqueNames = Object.ComputeUniqueNames       # Получить свойство (* )
        Object.ComputeUniqueNames = ComputeUniqueNames       # Установить свойство (* )
        ComputeUniqueNames = Object.GetComputeUniqueNames()  # Получить свойство (**)
        Object.SetComputeUniqueNames(ComputeUniqueNames)     # Установить свойство (**)
        ```
        """
        ...
    @ComputeUniqueNames.setter
    def ComputeUniqueNames(self, value: bool): ...

    @property
    def DefaultValue(self) -> str:  # ipropertyeditlist_defaultvalue.html
        """
        ## DefaultValue - Значение по умолчанию

        Тип данных: BSTR

        Свойство задает строку- приглашение, отображаемую при незаполненном списке значений.

        ### Синтаксис Automation

        ```
        DefaultValue = Object.DefaultValue       # Получить свойство (* )
        Object.DefaultValue = DefaultValue       # Установить свойство (* )
        DefaultValue = Object.GetDefaultValue()  # Получить свойство (**)
        Object.SetDefaultValue( DefaultValue )   # Установить свойство (**)
        ```
        """
        ...
    @DefaultValue.setter
    def DefaultValue(self, value: str): ...

    @property
    def ItemsCount(self) -> int:  # ipropertyeditlist_itemscount.html
        """
        ## ItemsCount - Количество элементов в списке

        Тип данных: long

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        long ItemsCount = iObject.ItemsCount       # Получить свойство (* )
        long ItemsCount = iObject.GetItemsCount()  # Получить свойство(**)
        ```
        """
        ...

    @property
    def ListType(self) -> int:  # ipropertyeditlist_listtype.html
        """
        ## ListType - Тип списка

        Тип данных: из перечисления ksEditListTypeEnum.

        ### Синтаксис Automation

        ```
        ksEditListTypeEnum ListType = iObject.ListType       # Получить свойство (* )
        iObject.ListType = ListType                          # Установить свойство (* )
        ksEditListTypeEnum ListType = iObject.GetListType()  # Получить свойство(**)
        iObject.SetListType (ListType)                       # Установить свойство (**)
        ```
        """
        ...
    @ListType.setter
    def ListType(self, value: int): ...

    @property
    def MultySelect(self) -> bool:  # ipropertyeditlist_multyselect.html
        """
        ## MultySelect - Возможность множественного выделения

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        BOOL MultySelect = iObject.MultySelect       # Получить свойство (* )
        iObject.MultySelect = MultySelect            # Установить свойство (* )
        BOOL MultySelect = iObject.GetMultySelect()  # Получить свойство(**)
        iObject.SetMultySelect (MultySelect)         # Установить свойство (**)
        ```
        """
        ...
    @MultySelect.setter
    def MultySelect(self, value: bool): ...

    @property
    def Sort(self) -> bool:  # ipropertyeditlist_sort.html
        """
        ## Sort - Состояние сортированности списка

        Тип данных:BOOL.

        ### Синтаксис Automation

        ```
        BOOL Sort = iObject.Sort       # Получить свойство (* )
        iObject.Sort = Sort            # Установить свойство (* )
        BOOL Sort = iObject.GetSort()  # Получить свойство(**)
        iObject.SetSort (Sort)         # Установить свойство (**)
        ```
        """
        ...
    @Sort.setter
    def Sort(self, value: bool): ...

    def Add(self, NewVal):  # ipropertyeditlist_add.html
        """
        ## Add - Добавить значение в список

        ### Синтаксис Automation

        ```
        void Add( LPCTSTR NewVal );
        ```

        ### Входные параметры

        +----------------------------------+
        | NewVal | - добавляемое значение. |
        +----------------------------------+
        """
        ...

    def BeginEditItem(self, Index) -> bool:  # ipropertyeditlist_beginedititem.html
        """
        ## BeginEditItem - Запустить редактирование значения в списке

        ### Синтаксис Automation

        ```
        BOOL BeginEditItem( long Index );
        ```

        ### Входные параметры

        +----------------------------+
        | index | - индекс значения. |
        +----------------------------+

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def BringIntoView(self, Index):  # ipropertyeditlist_bringintoview.html
        """
        ## BringIntoView - Подкрутка к строке

        ### Синтаксис Automation

        ```
        void BringIntoView( long Index );
        ```

        ### Входные параметры

        +--------------------------+
        | index | - индекс строки. |
        +--------------------------+

        Версия: Компас v20
        """
        ...

    def ClearList(self):  # ipropertyeditlist_clearlist.html
        """
        ## ClearList - Очистить список значений

        ### Синтаксис Automation

        ```
        void ClearList();
        ```
        """
        ...

    def Delete(self, Index):  # ipropertyeditlist_delete.html
        """
        ## Delete - Удалить элемент из списка по значению или индексу

        ### Синтаксис Automation

        ```
        void Delete (VARIANT Index);
        ```

        ### Входные параметры

        +----------------------------------------------------------------------------------------+
        | index | - элемент из списка; значение элемента - строка или индекс элемента типа long. |
        +----------------------------------------------------------------------------------------+
        """
        ...

    def Find(self, Val):  # ipropertyeditlist_find.html
        """
        ## Find - Найти индекс в списке по значению

        ### Синтаксис Automation

        ```
        long Find( VARIANT Val );
        ```

        ### Входные параметры

        +---------------------------+
        | Val | - искомое значение. |
        +---------------------------+

        ### Возвращаемое значение

        - индекс искомого значения.
        """
        ...

    def ItemChecked(self, Index) -> bool:  # ipropertyeditlist_itemchecked.html
        """
        ## ItemChecked - Состояние элемента списка

        Тип данных: BOOL.

        ### Синтаксис Automation

        ```
        BOOL ItemChecked = iObject.ItemChecked(index)     # Получить свойство (* )
        iObject.ItemChecked(index) = ItemChecked          # Установить свойство (* )
        BOOL ItemChecked = iObject.GetItemChecked(index)  # Получить свойство(**)
        iObject.SetItemChecked( index, ItemChecked )      # Установить свойство (**)
        ```

        ### Входные параметры

        +-----------------------------------+
        | index | - индекс элемента списка. |
        +-----------------------------------+
        """
        ...

    def ItemMissing(self, Index) -> bool:  # ipropertyeditlist_itemmissing.html
        """
        ## ItemMissing - Потерянный объект

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        ItemMissing = Object.ItemMissing( Index )     # Получить свойство (* )
        Object.ItemMissing( Index ) = ItemMissing     # Установить свойство (* )
        ItemMissing = Object.GetItemMissing( Index )  # Получить свойство (**)
        Object.SetItemMissing( Index, ItemMissing )   # Установить свойство (**)
        ```

        ### Входные параметры

        +----------------------------+
        | Index | - индекс значения. |
        +----------------------------+
        """
        ...

    def ItemSelected(self, Index) -> int:  # ipropertyeditlist_itemselected.html
        """
        ## ItemSelected - Состояние выделенности элемента списка

        Тип данных: long

        ### Синтаксис Automation

        ```
        long ItemSelected = iObject.ItemSelected       # Получить свойство (* )
        iObject.ItemSelected = ItemSelected            # Установить свойство (* )
        long ItemSelected = iObject.GetItemSelected()  # Получить свойство(**)
        iObject.SetItemSelected (ItemSelected)         # Установить свойство (**)
        ```
        """
        ...

    def ItemValue(self, Index):  # ipropertyeditlist_itemvalue.html
        """
        ## ItemValue - Значение элемента списка по индексу

        ### Синтаксис Automation

        ```
        Тип данных: LPSTR.
        ItemValue = iObject.ItemValue( Index )     # Получить свойство (* )
        iObject.ItemValue( Index ) = ItemValue     # Установить свойство (* )
        ItemValue = iObject.GetItemValue( Index )  # Получить свойство(**)
        iObject.SetItemValue( Index )              # Установить свойство (**)
        ```

        ### Входные параметры

        +----------------------------+
        | Index | - индекс элемента. |
        +----------------------------+
        """
        ...

    def SetItemChecked(self, Index, arg1):
        ...

    def SetItemMissing(self, Index, arg1):
        ...

    def SetItemSelected(self, Index, arg1):
        ...

    def SetItemValue(self, Index, arg1):
        ...




class IPropertyFileName(IPropertyControl):  # ipropertyfilename.html
    """
    ## Интерфейс IPropertyFileName

    Интерфейс элемента управления Панели свойств - Выбор файла.

    Элемент управления состоит из текстового поля и кнопки вызова диалога открытия файла.

    1. Данный интерфейс можно получить, используя коллекцию элементов управления Панели свойств IPropertyControls.

    1. При изменении значения параметра вызывается событие ksPropertyManagerNotify::ChangeControlValue.
    """

    @property
    def CreateOpenButton(self) -> bool:  # ipropertyfilename_createopenbutton.html
        """
        ## CreateOpenButton - Отображение кнопки выбора файла

        Тип данных: BOOL.

        Значения свойства:

        +-------------------------------------------------------------------------------------------+
        | TRUE  | - справа от текстового поля будет находится кнопка вызова диалога открытия файла, |
        +-------------------------------------------------------------------------------------------+
        | FALSE | - элемент управления будет состоять только из текстового поля (без кнопки).       |
        +-------------------------------------------------------------------------------------------+

        ### Синтаксис Automation

        ```
        CreateOpenButton = iObject.CreateOpenButton       # Получить свойство (* )
        iObject.CreateOpenButton = CreateOpenButton       # Установить свойство (* )
        CreateOpenButton = iObject.GetCreateOpenButton()  # Получить свойство(**)
        iObject.SetCreateOpenButton (CreateOpenButton)    # Установить свойство (**)
        ```
        """
        ...
    @CreateOpenButton.setter
    def CreateOpenButton(self, value: bool): ...

    @property
    def DefaultValue(self) -> str:  # ipropertyfilename_defaultvalue.html
        """
        ## DefaultValue - Значение по умолчанию

        Тип данных: BSTR

        ### Синтаксис Automation

        ```
        DefaultValue = Object.DefaultValue       # Получить свойство (* )
        Object.DefaultValue = DefaultValue       # Установить свойство (* )
        DefaultValue = Object.GetDefaultValue()  # Получить свойство(**)
        Object.SetDefaultValue( DefaultValue )   # Установить свойство (**)
        ```
        """
        ...
    @DefaultValue.setter
    def DefaultValue(self, value: str): ...

    @property
    def Extension(self) -> str:  # ipropertyfilename_extension.html
        """
        ## Extension - Расширение имени файла

        Тип данных: BSTR (строка).

        1. Свойство позволяет получить/задать расширение имени файла для диалога открытия файла, используемое по умолчанию.

        2. Если пользователь при задании имени файла не укажет расширение, автоматически для открытия будут предлагаться файлы с заданным расширением имени.

        3. Если присвоить свойству значение NULL, то расширение файлов должно задаваться пользователем явно.

        ### Синтаксис Automation

        ```
        Extension = iObject.Extension       # Получить свойство (* )
        iObject.Extension = Extension       # Установить свойство (* )
        Extension = iObject.GetExtension()  # Получить свойство(**)
        iObject.SetExtension (Extension)    # Установить свойство (**)
        ```
        """
        ...
    @Extension.setter
    def Extension(self, value: str): ...

    @property
    def Filter(self) -> str:  # ipropertyfilename_filter.html
        """
        ## Filter - Активный фильтр типов файлов

        Тип данных: BSTR (строка).

        Диалог открытия файла имеет список так называемых фильтров, включающих названия типов файлов и расширения имен файлов данного типа. Выбрав фильтр, пользователь указывает, что он желает работать только с файлами определенного типа, имеющими соответствующее расширение. Файлы с другими расширениями в диалоге не отображаются. Одновременно можно указать несколько фильтров. Каждый фильтр задается двумя строками - строкой, содержащей имя фильтра, и строкой, в которой перечислены соответствующие ему расширения имен файлов. Если одному типу соответствует несколько расширений, они разделяются символом «;». Строка, содержащая имя фильтра, отделяется от строки с расширениями файлов символом «|». Если используется несколько фильтров, то они также отделяются друг от друга символом «|». Например, в качестве строки, задающей фильтры, можно использовать строку вида: КОМПАС-Детали (*.m3d)|*.m3d|Все файлы (*.*)|*.*||.

        ### Синтаксис Automation

        ```
        Filter = iObject.Filter       # Получить свойство (* )
        iObject.Filter = Filter       # Установить свойство (* )
        Filter = iObject.GetFilter()  # Получить свойство(**)
        iObject.SetFilter (Filter)    # Установить свойство (**)
        ```
        """
        ...
    @Filter.setter
    def Filter(self, value: str): ...

    @property
    def Preview(self) -> bool:  # ipropertyfilename_preview.html
        """
        ## Preview - Предварительный просмотр документа в диалоговом окне

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        Preview = Object.Preview       # Получить свойство (* )
        Object.Preview = Preview       # Установить свойство (* )
        Preview = Object.GetPreview()  # Получить свойство(**)
        Object.SetPreview( Preview )   # Установить свойство (**)
        ```
        """
        ...
    @Preview.setter
    def Preview(self, value: bool): ...

    @property
    def ReadOnly(self) -> bool:  # ipropertyfilename_readonly.html
        """
        ## ReadOnly - Запрет редактирования значения

        Тип данных: BOOL

        Значения свойства:

        +-------------------------------------------------------------------------+
        | TRUE  | - запрещает ввод нового значения с клавиатуры в текстовом поле, |
        +-------------------------------------------------------------------------+
        | FALSE | - разрешает ввод нового значения с клавиатуры в текстовом поле. |
        +-------------------------------------------------------------------------+

        ### Синтаксис Automation

        ```
        ReadOnly = iObject.ReadOnly       # Получить свойство (* )
        iObject.ReadOnly = ReadOnly       # Установить свойство (* )
        ReadOnly = iObject.GetReadOnly()  # Получить свойство(**)
        iObject.SetReadOnly (ReadOnly)    # Установить свойство (**)
        ```
        """
        ...
    @ReadOnly.setter
    def ReadOnly(self, value: bool): ...

    @property
    def ResModule(self) -> str:  # ipropertyfilename_resmodule.html
        """
        ## ResModule - Модуль с ресурсом рисунка кнопки

        Функция не поддерживается

        Тип данных: VARIANT (long или BSTR).

        Значения свойства:

        +--------------------------------------------------------------------------------------------------+
        | HINSTANCE (тип значения long),                                                                   |
        +--------------------------------------------------------------------------------------------------+
        | полный путь к файлу (тип значения BSTR) dll модуля, в котором находятся все необходимые ресурсы. |
        +--------------------------------------------------------------------------------------------------+

        Данное свойство позволяет установить или получить dll-модуль, в котором находится рисунок для кнопки (см. Bitmap).

        ### Синтаксис Automation

        ```
        ResModule = iObject.ResModule       # Получить свойство (* )
        iObject.ResModule = ResModule       # Установить свойство (* )
        ResModule = iObject.GetResModule()  # Получить свойство(**)
        iObject.SetResModule (ResModule)    # Установить свойство (**)
        ```
        """
        ...
    @ResModule.setter
    def ResModule(self, value: str): ...

    @property
    def Width(self) -> int:  # ipropertyfilename_width.html
        """
        ## Width - Шаблон для расчета ширины элемента управления в горизонтальном положении (в символах)

        Тип данных: long.

        Значения свойства:

        Количество видимых символов в текстовом поле при горизонтальном положении Панели свойств.

        Для расчета ширины элемента управления в горизонтальном положении используется символ «0».

        ### Синтаксис Automation

        ```
        Width = iObject.Width       # Получить свойство (* )
        iObject.Width = Width       # Установить свойство (* )
        Width = iObject.GetWidth()  # Получить свойство(**)
        iObject.SetWidth (Width)    # Установить свойство (**)
        ```
        """
        ...
    @Width.setter
    def Width(self, value: int): ...

    @property
    def Bitmap(self) -> typing.NoReturn:  # ipropertyfilename_bitmap.html
        """
        ## Bitmap - Рисунок кнопки

        Функция не поддерживается

        Тип данных: VARIANT (long или BSTR).

        1. Свойство доступно только для записи.

        2. Позволяет задать изображение для кнопки вызова диалога открытия файла.

        3. Изображение можно задать в виде идентификаторов в ресурсе dll-модуля (тип данных long) или в виде полного пути к bmp-файлу рисунка (тип данных BSTR).

        4. Если изображение задается в виде идентификатора, то обязательно должен быть задан dll-модуль, в ресурсах которого лежат эти рисунки (см. ResModule).

        ### Синтаксис Automation

        ```
        iObject.Bitmap = Bitmap     # Установить свойство (* )
        iObject.SetBitmap (Bitmap)  # Установить свойство (**)
        ```
        """
        ...
    @Bitmap.setter
    def Bitmap(self, value: str): ...




class IPropertyGrid(IPropertyControl):  # ipropertygrid.html
    """
    ## Интерфейс IPropertyGrid

    Интерфейс элемента управления Панели свойств типа Сетка.

    1. Данный интерфейс можно получить через коллекцию элементов управления Панели свойств IPropertyControls.

    2. После создания элемента управления и вывода его на экран все изменения вносимые с помощью свойств и методов данного интерфейса вступят в силу только после вызова метода UpdateParam.

    3. Для IPropertyGrid можно создавать панели команд. Для создания команд в IPropertyGrid добавлена поддержка интерфейса IPropertyToolBar. Этот дополнительный интерфейс вида можно получить у интерфейса IPropertyGrid посредством вызова метода IUnknown::QueryInterface (const GUID far& iid, void** pif).

    4. Для IPropertyGrid поддерживаются события менеджера панели свойств ChangeControlValue, ControlCommand, SelectItem.
    """

    @property
    def AutoSizeColumns(self) -> bool:  # ipropertygrid_autosizecolumns.html
        """
        ## AutoSizeColumns - Авторазмер колонок

        Тип данных: BOOL

        Значение свойства:

        +-----------------------------------------------------------------+
        | TRUE  | - расчет размера колонок от ширины элемента управления, |
        +-----------------------------------------------------------------+
        | FALSE | - фиксированный размер колонок.                         |
        +-----------------------------------------------------------------+

        Ширина колонки определяется путем деления всей ширины элемента управления на количество колонок.

        ### Синтаксис Automation

        ```
        AutoSizeColumns = iObject.AutoSizeColumns       # Получить свойство (* )
        iObject.AutoSizeColumns = AutoSizeColumns       # Установить свойство (* )
        AutoSizeColumns = iObject.GetAutoSizeColumns()  # Получить свойство(**)
        iObject.SetAutoSizeColumns (AutoSizeColumns)    # Установить свойство (**)
        ```
        """
        ...
    @AutoSizeColumns.setter
    def AutoSizeColumns(self, value: bool): ...

    @property
    def ColumnCount(self) -> int:  # ipropertygrid_columncount.html
        """
        ## ColumnCount - Количество колонок

        Тип данных: long.

        Значение свойства:

        Общее количество колонок в сетке.

        ### Синтаксис Automation

        ```
        ColumnCount = iObject.ColumnCount       # Получить свойство (* )
        iObject.ColumnCount = ColumnCount       # Установить свойство (* )
        ColumnCount = iObject.GetColumnCount()  # Получить свойство(**)
        iObject.SetColumnCount (ColumnCount)    # Установить свойство (**)
        ```
        """
        ...
    @ColumnCount.setter
    def ColumnCount(self, value: int): ...

    @property
    def CurrentColumn(self) -> int:  # ipropertygrid_currentcolumn.html
        """
        ## CurrentColumn - Текущая колонка

        Тип данных: long.

        Позволяет получить и установить текущую колонку для таблицы.

        ### Синтаксис Automation

        ```
        CurrentColumn = iObject.CurrentColumn       # Получить свойство (* )
        iObject.CurrentColumn = CurrentColumn       # Установить свойство (* )
        CurrentColumn = iObject.GetCurrentColumn()  # Получить свойство(**)
        iObject.SetCurrentColumn(CurrentColumn)     # Установить свойство (**)
        ```
        """
        ...
    @CurrentColumn.setter
    def CurrentColumn(self, value: int): ...

    @property
    def CurrentRow(self) -> int:  # ipropertygrid_currentrow.html
        """
        ## CurrentRow - Текущая строка

        Тип данных: long.

        Позволяет получить и установить текущую строку для таблицы.

        ### Синтаксис Automation

        ```
        CurrentRow = iObject.CurrentRow       # Получить свойство (* )
        iObject.CurrentRow = CurrentRow       # Установить свойство (* )
        CurrentRow = iObject.GetCurrentRow()  # Получить свойство(**)
        iObject.SetCurrentRow(CurrentRow)     # Установить свойство (**)
        ```
        """
        ...
    @CurrentRow.setter
    def CurrentRow(self, value: int): ...

    @property
    def EnableDeleteRows(self) -> bool:  # ipropertygrid_enabledeleterows.html
        """
        ## EnableDeleteRows - Разрешить удаление строк

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        EnableDeleteRows = Object.EnableDeleteRows       # Получить свойство (* )
        Object.EnableDeleteRows = EnableDeleteRows       # Установить свойство (* )
        EnableDeleteRows = Object.GetEnableDeleteRows()  # Получить свойство(**)
        Object.SetEnableDeleteRows( EnableDeleteRows )   # Установить свойство (**)
        ```
        """
        ...
    @EnableDeleteRows.setter
    def EnableDeleteRows(self, value: bool): ...

    @property
    def FixedColumnCount(self) -> int:  # ipropertygrid_fixedcolumncount.html
        """
        ## FixedColumnCount - Количество фиксированных колонок

        Тип данных: long.

        Значение свойства:

        Количество фиксированных колонок в сетке.

        ### Синтаксис Automation

        ```
        FixedColumnCount = iObject.FixedColumnCount       # Получить свойство (* )
        iObject.FixedColumnCount = FixedColumnCount       # Установить свойство (* )
        FixedColumnCount = iObject.GetFixedColumnCount()  # Получить свойство(**)
        iObject.SetFixedColumnCount (FixedColumnCount)    # Установить свойство (**)
        ```
        """
        ...
    @FixedColumnCount.setter
    def FixedColumnCount(self, value: int): ...

    @property
    def FixedRowCount(self) -> int:  # ipropertygrid_fixedrowcount.html
        """
        ## FixedRowCount - Количество фиксированных строк

        Тип данных: long.

        Значение свойства:

        Количество фиксированных строк в сетке.

        ### Синтаксис Automation

        ```
        FixedRowCount = iObject.FixedRowCount             # Получить свойство (* )
        iObject.FixedColumnCount = FixedColumnCount       # Установить свойство (* )
        FixedColumnCount = iObject.GetFixedColumnCount()  # Получить свойство(**)
        iObject.SetFixedColumnCount (FixedColumnCount)    # Установить свойство (**)
        ```
        """
        ...
    @FixedRowCount.setter
    def FixedRowCount(self, value: int): ...

    @property
    def Height(self) -> int:  # ipropertygrid_height.html
        """
        ## Height - Высота элемента управления

        Тип данных: long

        Значение свойства:

        Высота видимой части элемента управления (в пикселах).

        ### Синтаксис Automation

        ```
        Height = iObject.Height       # Получить свойство (* )
        iObject.Height = Height       # Установить свойство (* )
        Height = iObject.GetHeight()  # Получить свойство(**)
        iObject.SetHeight (Height)    # Установить свойство (**)
        ```
        """
        ...
    @Height.setter
    def Height(self, value: int): ...

    @property
    def ReadOnly(self) -> bool:  # ipropertygrid_readonly.html
        """
        ## ReadOnly - Запрет редактирования таблицы

        Тип данных: BOOL

        Значение свойства:

        +-------------------------------------------+
        | TRUE  | - редактирование ячеек запрещено, |
        +-------------------------------------------+
        | FALSE | - не реализовано.                 |
        +-------------------------------------------+

        Временно сетка работает только в режиме Только для чтения. Редактирование ее ячеек запрещено.

        ### Синтаксис Automation

        ```
        ReadOnly = iObject.ReadOnly       # Получить свойство (* )
        iObject.ReadOnly = ReadOnly       # Установить свойство (* )
        ReadOnly = iObject.GetReadOnly()  # Получить свойство(**)
        iObject.SetReadOnly (ReadOnly)    # Установить свойство (**)
        ```
        """
        ...
    @ReadOnly.setter
    def ReadOnly(self, value: bool): ...

    @property
    def RowCount(self) -> int:  # ipropertygrid_rowcount.html
        """
        ## RowCount - Количество строк

        Тип данных: long

        Значение свойства:

        Общее количество строк в сетке.

        ### Синтаксис Automation

        ```
        RowCount = iObject.RowCount       # Получить свойство (* )
        iObject.RowCount = RowCount       # Установить свойство (* )
        RowCount = iObject.GetRowCount()  # Получить свойство(**)
        iObject.SetRowCount (RowCount)    # Установить свойство (**)
        ```
        """
        ...
    @RowCount.setter
    def RowCount(self, value: int): ...

    @property
    def Width(self) -> int:  # ipropertygrid_width.html
        """
        ## Width - Ширина элемента управления

        Тип данных: long

        Значение свойства:

        Ширина видимой части элемента управления (в пикселах).

        ### Синтаксис Automation

        ```
        Width = iObject.Width       # Получить свойство (* )
        iObject.Width = Width       # Установить свойство (* )
        Width = iObject.GetWidth()  # Получить свойство(**)
        iObject.SetWidth (Width)    # Установить свойство (**)
        ```
        """
        ...
    @Width.setter
    def Width(self, value: int): ...

    def BringIntoView(self, Index):  # ipropertygrid_bringintoview.html
        """
        ## BringIntoView - Подкрутка к строке

        ### Синтаксис Automation

        ```
        void BringIntoView( long Index );
        ```

        ### Входные параметры

        +--------------------------+
        | index | - индекс строки. |
        +--------------------------+

        Версия: Компас v20
        """
        ...

    def CellFormat(self, NRow, NCol) -> int:  # ipropertygrid_cellformat.html
        """
        ## CellFormat - Формат вывода текста в ячейке

        Тип данных: long

        Значение свойства:

        формат вывода текста в ячейке.

        Формат вывода текста в ячейке задается стандартными значениями, используемыми в методе DrawText ОС Windows.

        ### Синтаксис Automation

        ```
        CellFormat = iObject.CellFormat (nRow, nCol)     # Получить свойство (* )
        iObject.CellFormat (nRow, nCol) = CellFormat     # Установить свойство (* )
        CellFormat = iObject.GetCellFormat (nRow, nCol)  # Получить свойство(**)
        iObject.SetCellFormat (nRow, nCol, CellFormat)   # Установить свойство (**)
        ```

        ### Входные параметры

        +---------------------------------+
        | nRow (long) | - индекс строки,  |
        +---------------------------------+
        | nCol (long) | - индекс колонки. |
        +---------------------------------+
        """
        ...

    def CellText(self, NRow, NCol) -> str:  # ipropertygrid_celltext.html
        """
        ## CellText - Текст в ячейке

        Тип данных: строка

        Значение свойства:

        Текст в ячейке.

        Позволяет получить/установить текст в ячейке.

        ### Синтаксис Automation

        ```
        cllText = iObject.CellText (nRow, nCol)      # Получить свойство (* )
        iObject.CellText (nRow, nCol ) = CellText    # Установить свойство (* )
        CellText = iObject.GetCellText (nRow, nCol)  # Получить свойство(**)
        iObject.SetCellText (nRow, nCol, CellText)   # Установить свойство (**)
        ```

        ### Входные параметры

        +---------------------------------+
        | nRow (long) | - индекс строки,  |
        +---------------------------------+
        | nCol (long) | - индекс колонки. |
        +---------------------------------+
        """
        ...

    def ColumnVisible(self, Index) -> bool:  # ipropertygrid_columnvisible.html
        """
        ## ColumnVisible - Видимость колонки

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        ColumnVisible = Object.ColumnVisible( index )     # Получить свойство (* )
        Object.ColumnVisible( index ) = ColumnVisible     # Установить свойство (* )
        ColumnVisible = Object.GetColumnVisible( index )  # Получить свойство(**)
        Object.SetColumnVisible( index, ColumnVisible )   # Установить свойство (**)
        ```

        ### Входные параметры

        +---------------------------+
        | index | - индекс колонки. |
        +---------------------------+
        """
        ...

    def ColumnWidth(self, Index) -> int:  # ipropertygrid_columnwidth.html
        """
        ## ColumnWidth - Ширина колонки

        Тип данных: long.

        Значение свойства:

        Ширина колонки с индексом index в сетке (в пикселах).

        ### Синтаксис Automation

        ```
        ColumnWidth = iObject.ColumnWidth (index)     # Получить свойство (* )
        iObject.ColumnWidth (index) = ColumnWidth     # Установить свойство (* )
        ColumnWidth = iObject.GetColumnWidth (index)  # Получить свойство(**)
        iObject.SetColumnWidth (index, ColumnWidth)   # Установить свойство (**)
        ```

        ### Входные параметры

        +---------------------------+
        | index | - индекс колонки. |
        +---------------------------+
        """
        ...

    def EnableEdit(self, NRow, NCol) -> bool:  # ipropertygrid_enableedit.html
        """
        ## EnableEdit - Запрет редактирования ячеек

        Тип данных: BOOL.

        ### Синтаксис Automation

        ```
        EnableEdit = Object.EnableEdit( NRow, NCol )     # Получить свойство (* )
        Object.EnableEdit( NRow, NCol ) = EnableEdit     # Установить свойство (* )
        EnableEdit = Object.GetEnableEdit( NRow, NCol )  # Получить свойство(**)
        Object.SetEnableEdit( NRow, NCol, EnableEdit ))  # Установить свойство (**)
        ```

        ### Входные параметры

        +-----------------------------+
        | long NRow | - номер строки  |
        +-----------------------------+
        | long NCol | - номер столбца |
        +-----------------------------+
        """
        ...

    def RowHeight(self, Index) -> int:  # ipropertygrid_rowheight.html
        """
        ## RowHeight - Высота строки

        Тип данных: long

        Значение свойства:

        Высота строки с индексом index в сетке (в пикселах).

        ### Синтаксис Automation

        ```
        RowHeight = iObject.RowHeight (index)     # Получить свойство (* )
        iObject.RowHeight (index) = RowHeight     # Установить свойство (* )
        RowHeight = iObject.GetRowHeight (index)  # Получить свойство(**)
        iObject.SetRowHeight (index, RowHeightr)  # Установить свойство (**)
        ```

        ### Входные параметры

        +--------------------------+
        | index | - индекс строки. |
        +--------------------------+
        """
        ...

    def SetCellFormat(self, NRow, NCol, arg2):
        ...

    def SetCellText(self, NRow, NCol, arg2):
        ...

    def SetColumnVisible(self, Index, arg1):
        ...

    def SetColumnWidth(self, Index, arg1):
        ...

    def SetEnableEdit(self, NRow, NCol, arg2):
        ...

    def SetRowHeight(self, Index, arg1):
        ...

    def UpdateParam(self):  # ipropertygrid_updateparam.html
        """
        ## UpdateParam - Обновить параметры сетки

        Все изменения в сетке вступят в силу только после вызова этого метода.

        ### Синтаксис Automation

        ```
        void UpdateParam();
        ```
        """
        ...




class IPropertyLibExplorer(IPropertyControl):  # ipropertylibexplorer.html
    """
    ## Интерфейс IPropertyLibExplorer

    Элемент Панели свойств Отображение библиотеки документов.

    1. Интерфейс позволяет просматривать библиотеки фрагментов(*.lfr) и библиотеки моделей (*.l3d). Путь к выбранному элементу можно получить через IPropertyControl::Value. Для установки активного элемента также используется IPropertyControl::Value.

    2. Данный интерфейс может быть получен от интерфейса коллекции элементов управления IPropertyControls с помощью свойства IPropertyControls::Item или метода IPropertyControls::Add с последующим приведением интерфейса IPropertyControl к данному интерфейсу с помощью метода IUnknown::QueryInterface.
    """

    @property
    def FileName(self) -> str:  # ipropertylibexplorer_filename.html
        """
        ## FileName - Имя файла библиотеки документов

        Тип данных: BSTR.

        Полное имя к файлу библиотеки документов.

        ### Синтаксис Automation

        ```
        FileName = iObject.FileName        # Получить свойство (* )
        iObject.FileName = FileName        # Установить свойство (* )
        FileName = iObject.GetFileName( )  # Получить свойство(**)
        iObject.SetFileName (FileName)     # Установить свойство (**)
        ```
        """
        ...
    @FileName.setter
    def FileName(self, value: str): ...

    @property
    def FileValue(self) -> bool:  # ipropertylibexplorer_filevalue.html
        """
        ## FileValue - Признак - выбранный узел является файлом

        Тип данных: BOOL.

        Значения свойства:

        +----------------------------------------------+
        | TRUE  | - выбранный узел является файлом,    |
        +----------------------------------------------+
        | FALSE | - выбранный узел не является файлом. |
        +----------------------------------------------+

        1. Свойство доступно только для чтения.

        2. В дереве библиотеки документов могут быть папки и файлы. Путь к выбранному элементу можно получить, используя свойство IPropertyControl::Value.

        ### Синтаксис Automation

        ```
        FileValue = iObject.FileValue        # Получить свойство (* )
        FileValue = iObject.GetFileValue( )  # Получить свойство(**)
        ```
        """
        ...

    @property
    def RootName(self) -> str:  # ipropertylibexplorer_rootname.html
        """
        ## RootName - Имя верхнего узла дерева

        Тип данных: BSTR.

        1. Имя верхнего узла дерева внутренней структуры библиотеки документов. После вызова установки полного пути к библиотеке документов IPropertyLibExplorer::FileName считывается как ее имя.

        2. Задать значение свойства можно только до запуска процесса.

        ### Синтаксис Automation

        ```
        RootName = iObject.RootName        # Получить свойство (* )
        iObject.RootName = RootName        # Установить свойство (* )
        FileName = iObject.GetRootName( )  # Получить свойство(**)
        iObject.SetRootName (RootName)     # Установить свойство (**)
        ```
        """
        ...
    @RootName.setter
    def RootName(self, value: str): ...




class IPropertyList(IPropertyControl):  # ipropertylist.html
    """
    ## Интерфейс IPropertyList

    Интерфейс элемента управления Панели свойств Поле со списком.

    1. Данный интерфейс можно получить через интерфейс коллекции элементов управления панели свойств IPropertyControls.

    2. При нажатии на кнопку элемента управления вызывается событие ksPropertyManagerNotify::ControlCommand.

    3. При изменении значения элемента управления вызывается событие ksIPropertyManagerNotify::ChangeControlValue.
    """

    @property
    def CheckState(self) -> int:  # ipropertylist_checkstate.html
        """
        ## CheckState - Состояние опции

        Тип данных: состояние опции из перечисления CheckStateEnum.

        Это свойство влияет на состояние переключателя, если свойство WithCheck имеет значение TRUE, и на отображение текста в поле элемента управления, подробнее см. CheckStateEnum.

        ### Синтаксис Automation

        ```
        CheckState = iObject.CheckState       # Получить свойство (* )
        iObject.CheckState = CheckState       # Установить свойство (* )
        CheckState = iObject.GetCheckState()  # Получить свойство(**)
        iObject.SetCheckState (CheckState)    # Установить свойство (**)
        ```
        """
        ...
    @CheckState.setter
    def CheckState(self, value: int): ...

    @property
    def Count(self) -> int:  # ipropertylist_count.html
        """
        ## Count - Количество элементов списка

        Тип данных: long.

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        Count = Object.Count       # Получить свойство (* )
        Count = Object.GetCount()  # Получить свойство(**)
        ```
        """
        ...

    @property
    def MaxValue(self) -> int:  # ipropertylist_maxvalue.html
        """
        ## MaxValue - Максимальное значение

        Тип данных: VARIANT (long или double).

        1. Свойство доступно только для чтения.

        2. Тип данных определяется типом элемента управления из перечисления ControlTypeEnum:

        •для типа ksControlListInt используется тип long,

        •для типа ksControlListReal используется тип double,

        •для типа ksControlListStr ограничения на значение не накладываются,

        •для типа ksControlListLength используется тип double,

        •для типа ksControlListAngle используется тип double.

        ### Синтаксис Automation

        ```
        MaxValue = iObject.MinValue       # Получить свойство (* )
        MaxValue = iObject.GetMinValue()  # Получить свойство(**)
        ```
        """
        ...

    @property
    def MinValue(self) -> int:  # ipropertylist_minvalue.html
        """
        ## MinValue - Минимальное значение

        Тип данных: VARIANT (long или double).

        1. Свойство доступно только для чтения.

        2. Тип данных определяется типом элемента управления из перечисления ControlTypeEnum:

        •для типа ksControlListInt используется тип long,

        •для типа ksControlListReal используется тип double,

        •для типа ksControlListStr ограничения на значение не накладываются,

        •для типа ksControlListLength используется тип double,

        •для типа ksControlListAngle используется тип double.

        ### Синтаксис Automation

        ```
        MinValue = iObject.MinValue       # Получить свойство (* )
        MinValue = iObject.GetMinValue()  # Получить свойство(**)
        ```
        """
        ...

    @property
    def Precision(self) -> float:  # ipropertylist_precision.html
        """
        ## Precision - Точность сравнения

        Тип данных: double.

        1. Свойство позволяет установить точность сравнения значений в списке. Сравнение происходит при добавлении значения в список IPropertyList::Add.

        2. Если добавляемое значение в списке есть, то оно не добавляется при установке значения IPropertyControl::Value.

        3. Если устанавливается значение, которое есть в списке, оно селектируется при поиске IPropertyList::Find.

        4. Свойство работает для вещественных типов списка: ksControlListReal, ksControlListLength, ksControlListAngle.

        5. По умолчанию устанавливается точность 0.000001.

        ### Синтаксис Automation

        ```
        Precision = iObject.Precision       # Получить свойство (* )
        iObject.Precision = Precision       # Установить свойство (* )
        Precision = iObject.GetPrecision()  # Получить свойство(**)
        iObject.SetPrecision(Precision)     # Установить свойство (**)
        ```
        """
        ...
    @Precision.setter
    def Precision(self, value: float): ...

    @property
    def ReadOnly(self) -> bool:  # ipropertylist_readonly.html
        """
        ## ReadOnly - Запрет редактирования значения

        Тип данных: BOOL.

        Значение свойства:

        +------------------------------------------------------------------------------------------------------------------------------+
        | TRUE  | - ввод нового значения с клавиатуры в раскрывающийся список запрещен; элемент управления работает в режиме DropList, |
        +------------------------------------------------------------------------------------------------------------------------------+
        | FALSE | - ввод нового значения с клавиатуры в раскрывающийся список разрешен; элемент управления работает в режиме DropDown, |
        +------------------------------------------------------------------------------------------------------------------------------+

        ### Синтаксис Automation

        ```
        ReadOnly = iObject.ReadOnly       # Получить свойство (* )
        iObject.ReadOnly = ReadOnly       # Установить свойство (* )
        ReadOnly = iObject.GetReadOnly()  # Получить свойство(**)
        iObject.SetReadOnly (ReadOnly)    # Установить свойство (**)
        ```
        """
        ...
    @ReadOnly.setter
    def ReadOnly(self, value: bool): ...

    @property
    def Sort(self) -> bool:  # ipropertylist_sort.html
        """
        ## Sort - Сортировать список

        Тип данных BOOL.

        Значение свойства:

        +-----------------------------------------------------------------------------------------+
        | TRUE  | - значения в списке будут сортироваться при добавлении новых,                   |
        +-----------------------------------------------------------------------------------------+
        | FALSE | - значения в списке будут в той последовательности, в какой они были добавлены. |
        +-----------------------------------------------------------------------------------------+

        ### Синтаксис Automation

        ```
        Sort = iObject.Sort       # Получить свойство (* )
        iObject.Sort = Sort       # Установить свойство (* )
        Sort = iObject.GetSort()  # Получить свойство(**)
        iObject.SetSort (Sort)    # Установить свойство (**)
        ```
        """
        ...
    @Sort.setter
    def Sort(self, value: bool): ...

    @property
    def Step(self) -> bool:  # ipropertylist_step.html
        """
        ## Step - Шаг

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        Step = Object.Step       # Получить свойство (* )
        Object.Step = Step       # Установить свойство (* )
        Step = Object.GetStep()  # Получить свойство(**)
        Object.SetStep( Step )   # Установить свойство (**)
        ```
        """
        ...
    @Step.setter
    def Step(self, value: bool): ...

    @property
    def Width(self) -> int:  # ipropertylist_width.html
        """
        ## Width- Шаблон для расчета ширины элемента управления в горизонтальном положении (в символах)

        Тип данных long.

        Значение свойства:

        Количество видимых символов в поле при горизонтальном положении Панели свойств.

        Для расчета ширины элемента управления используется символ «0».

        ### Синтаксис Automation

        ```
        Width = iObject.Width       # Получить свойство (* )
        iObject.Width = Width       # Установить свойство (* )
        Width = iObject.GetWidth()  # Получить свойство(**)
        iObject.SetWidth (Width)    # Установить свойство (**)
        ```
        """
        ...
    @Width.setter
    def Width(self, value: int): ...

    @property
    def WithCheck(self) -> bool:  # ipropertylist_withcheck.html
        """
        ## WithCheck - Наличие опции рядом с полем ввода

        Тип данных: BOOL.

        Значение свойства:

        +------------------------------------------------------------+
        | TRUE  | - рядом с элементом управления выводится опция,    |
        +------------------------------------------------------------+
        | FALSE | - рядом с элементом управления не выводится опция. |
        +------------------------------------------------------------+

        На текущий момент свойство не реализовано, его значение можно установить только равным FALSE.

        ### Синтаксис Automation

        ```
        WithCheck = iObject.WithCheck       # Получить свойство (* )
        iObject.WithCheck = WithCheck       # Установить свойство (* )
        WithCheck = iObject.GetWithCheck()  # Получить свойство(**)
        iObject.SetWithCheck (WithCheck)    # Установить свойство (**)
        ```
        """
        ...
    @WithCheck.setter
    def WithCheck(self, value: bool): ...

    def Add(self, NewVal):  # ipropertylist_add.html
        """
        ## Add - Добавить значение в список

        1. Тип данных нового значения определяется типом элемента управления из перечисления ControlTypeEnum:

        •для типа ksControlListInt используется тип long,

        •для типа ksControlListReal используется тип double,

        •для типа ksControlListStr ограничения на значение не накладываются,

        •для типа ksControlListLength используется тип double,

        •для типа ksControlListAngle используется тип double.

        2. В список можно добавить только уникальные значения. Если в списке уже есть значение, которое пытаются добавить повторно, то оно не будет добавлено.

        3. В список можно сразу передать массив значений. Пример...

        4. См. также: IPropertyControl::Value - Значение элемента управления.

        ### Синтаксис Automation

        ```
        void Add (const VARIANT& newVal);
        ```

        ### Входные параметры

        +----------------------------------------------------+
        | newVal | - новое значение в раскрывающемся списке. |
        +----------------------------------------------------+
        """
        ...

    def ClearList(self):  # ipropertylist_clearlist.html
        """
        ## ClearList - Очистить список значений

        Очищает раскрывающийся список и содержимое его поля ввода.

        ### Синтаксис Automation

        ```
        void ClearList();
        ```
        """
        ...

    def Find(self, Val):  # ipropertylist_find.html
        """
        ## Find - Найти индекс строки в списке по значению

        Метод работает только на несортированном списке, см. IPropertyList::Sort.

        ### Синтаксис Automation

        ```
        long Find (const VARIANT FAR& val);
        ```

        ### Входные параметры

        +-------------------------------------------+
        | val | - значение в раскрывающемся списке. |
        +-------------------------------------------+

        ### Возвращаемое значение

        +---------------------------------------------------------------+
        | Индекс строки со значением val в списке | - в случае успеха,  |
        +---------------------------------------------------------------+
        | -1                                      | - в случае неудачи. |
        +---------------------------------------------------------------+
        """
        ...

    def SetCurrentByIndex(self, Val) -> bool:  # ipropertylist_setcurrentbyindex.html
        """
        ## SetCurrentByIndex - Установить текущую строку по индексу

        Метод работает только на несортированном списке, см. IPropertyList::Sort.

        ### Синтаксис Automation

        ```
        BOOL SetCurrentByIndex( long index );
        ```

        ### Входные параметры

        +-------------------------------------------------+
        | index | - номер строки в раскрывающемся списке. |
        +-------------------------------------------------+

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def SetValueRange(self, MinVal, MaxVal):  # ipropertylist_setvaluerange.html
        """
        ## SetValueRange - Установить новые ограничения на значение

        Тип данных максимального и минимального допустимого значения определяется типом элемента управления из перечисления ControlTypeEnum:

        •для типа ksControlListInt используется тип long,

        •для типа ksControlListReal используется тип double,

        •для типа ksControlListStr ограничения на значение не накладываются,

        •для типа ksControlListLength используется тип double,

        •для типа ksControlListAngle используется тип double.

        ### Синтаксис Automation

        ```
        BOOL SetValueRange (const VARIANT& minVal, const VARIANT& maxVal);
        ```

        ### Входные параметры

        +---------------------------------------------------------------------------+
        | minVal | - минимальное допустимое значение (тип данных long или double),  |
        +---------------------------------------------------------------------------+
        | maxVal | - максимальное допустимое значение (тип данных long или double). |
        +---------------------------------------------------------------------------+
        """
        ...




class IPropertyMultiButton(IPropertyControl):  # ipropertymultibutton.html
    """
    ## Интерфейс IPropertyMultiButton

    Интерфейс элемента управления Панели свойств Набор кнопок.

    1. Элемент управления состоит из набора кнопок определенного типа из перечисления ButtonTypeEnum.

    2. Данный интерфейс можно получить через интерфейс коллекции элементов управления панели свойств IPropertyControls.

    3. При нажатии на одну из кнопок набора вызывается событие ksPropertyManagerNotify::ControlCommand.

    4. Для корректного отображения строки подсказки конкретной кнопки, задаваемой при помощи свойств ButtonHint или ButtonTips, необходимо задать строку подсказки для набора кнопок. Если текст подсказки набора не нужен или не имеет значения, эта строка может состоять, например, из одного пробела.
    """

    @property
    def ButtonsType(self) -> int:  # ipropertymultibutton_buttonstype.html
        """
        ## ButtonsType - Тип кнопок

        Тип данных: тип кнопок из перечисления ButtonTypeEnum.

        Изменить тип кнопок можно только до создания элемента управления, то есть до запуска процесса у процессных параметров или до вызова метода IPropertyManager::ShowTabs.

        ### Синтаксис Automation

        ```
        ButtonsType = iObject.ButtonsType       # Получить свойство (* )
        iObject.ButtonsType = ButtonsType       # Установить свойство (* )
        ButtonsType = iObject.GetButtonsType()  # Получить свойство(**)
        iObject.SetButtonsType (ButtonsType)    # Установить свойство (**)
        ```
        """
        ...
    @ButtonsType.setter
    def ButtonsType(self, value: int): ...

    @property
    def NextCommand(self) -> bool:  # ipropertymultibutton_nextcommand.html
        """
        ## NextCommand - Переключать кнопки по VK_TAB

        Тип данных: BOOL.

        1. Доступно только для кнопок типа и ksRadioButton из перечисления ButtonTypeEnum.

        2. Позволяет получить и установить свойство переключения кнопок комбинацией CTLRTAB.

        ### Синтаксис Automation

        ```
        nextCommand = iKompasObject.NextCommand       # Получить свойство (* )
        iKompasObject.NextCommand = nextCommand       # Установить свойство (* )
        nextCommand = iKompasObject.GetNextCommand()  # Получить свойство(**)
        iKompasObject.SetNextCommand( nextCommand )   # Установить свойство (**)
        ```
        """
        ...
    @NextCommand.setter
    def NextCommand(self, value: bool): ...

    @property
    def ResModule(self) -> str:  # ipropertymultibutton_resmodule.html
        """
        ## ResModule - Модуль с ресурсами значков

        Тип данных: VARIANT (long или BSTR)

        Значение свойства:

        HINSTANCE (тип значения long) или полный путь к файлу (тип значения BSTR) dll модуля, в котором находятся все необходимые ресурсы.

        Данное свойство позволяет установить или получить dll-модуль, из которого будут извлекаться значки для кнопок.

        ### Синтаксис Automation

        ```
        ResModule = iObject.ResModule       # Получить свойство (* )
        iObject.ResModule = ResModule       # Установить свойство (* )
        ResModule = iObject.GetResModule()  # Получить свойство(**)
        iObject.SetResModule (ResModule)    # Установить свойство (**)
        ```
        """
        ...
    @ResModule.setter
    def ResModule(self, value: str): ...

    def AddButton(self, BtnID, Bmp, InsertAt):  # ipropertymultibutton_addbutton.html
        """
        ## AddButton - Добавить кнопку

        Если значок задается в виде идентификатора, то до этого должен быть задан dll-модуль, в ресурсах которого лежит этот значок. Если кнопка с заданным идентификатором уже есть в списке, то ничего добавляться не будет.

        ### Синтаксис Automation

        ```
        void AddButton (long btnID, const VARIANT& bmp, long insertAt);
        ```

        ### Входные параметры

        +----------------------------------------------------------------------------------------------------------------------------+
        | btnID    | - идентификатор добавляемой кнопки,                                                                             |
        +----------------------------------------------------------------------------------------------------------------------------+
        | bmp      | - идентификатор значка в файле ресурсов (тип данных long) или полный путь к bmp-файлу значка (тип данных BSTR), |
        +----------------------------------------------------------------------------------------------------------------------------+
        | insertAt | - индекс кнопки, перед которой нужно вставить добавляемую (-1 - в конец).                                       |
        +----------------------------------------------------------------------------------------------------------------------------+
        """
        ...

    def ButtonChecked(self, BtnID) -> bool:  # ipropertymultibutton_buttonchecked.html
        """
        ## ButtonChecked - Нажатие кнопки

        Тип данных: BOOL.

        Значение свойства:

        +-----------------------------+
        | TRUE  | - кнопка нажата,    |
        +-----------------------------+
        | FALSE | - кнопка не нажата. |
        +-----------------------------+

        Доступно только для кнопок типа ksCheckButton и ksRadioButton из перечисления ButtonTypeEnum.

        ### Синтаксис Automation

        ```
        ButtonChecked = iObject.ButtonChecked (btnID)     # Получить свойство (* )
        iObject.ButtonChecked (btnID) = ButtonChecked     # Установить свойство (* )
        ButtonChecked = iObject.GetButtonChecked (btnID)  # Получить свойство(**)
        iObject.SetButtonChecked (btnID, ButtonChecked)   # Установить свойство (**)
        ```

        ### Входные параметры

        +---------------------------------+
        | btnID | - идентификатор кнопки. |
        +---------------------------------+
        """
        ...

    def ButtonEnable(self, BtnID) -> bool:  # ipropertymultibutton_buttonenable.html
        """
        ## ButtonEnable - Доступность кнопки

        Тип данных: BOOL.

        Значение свойства:

        +------------------------------+
        | TRUE  | - кнопка доступна,   |
        +------------------------------+
        | FALSE | - кнопка недоступна. |
        +------------------------------+

        Данное свойство позволяет установить или получить доступ к кнопке.

        ### Синтаксис Automation

        ```
        ButtonEnable = iObject.ButtonEnable (btnID)     # Получить свойство (* )
        iObject.ButtonEnable (btnID) = ButtonEnable     # Установить свойство (* )
        ButtonEnable = iObject.GetButtonEnable (btnID)  # Получить свойство(**)
        iObject.SetButtonEnable (btnID, ButtonEnable)   # Установить свойство (**)
        ```

        ### Входные параметры

        +---------------------------------+
        | btnID | - идентификатор кнопки. |
        +---------------------------------+
        """
        ...

    def ButtonHint(self, BtnID) -> str:  # ipropertymultibutton_buttonhint.html
        """
        ## ButtonHint - Строка подсказки для кнопки

        Тип данных: BSTR

        1. Данное свойство позволяет установить или получить строку подсказки для кнопки с идентификатором btnID.

        2. Для корректного отображения строки подсказки конкретной кнопки необходимо задать строку подсказки для набора кнопок. Если текст подсказки набора не нужен или не имеет значения, эта строка может состоять, например, из одного пробела.

        ### Синтаксис Automation

        ```
        ButtonHint = iObject.ButtonHint (btnID)     # Получить свойство (* )
        iObject.ButtonHint (btnID) = ButtonHint     # Установить свойство (* )
        ButtonHint = iObject.GetButtonHint (btnID)  # Получить свойство(**)
        iObject.SetButtonHint (btnID, ButtonHint)   # Установить свойство (**)
        ```

        ### Входные параметры

        +---------------------------------+
        | btnID | - идентификатор кнопки. |
        +---------------------------------+
        """
        ...

    def ButtonIconFont(self, BtnID) -> str:  # ipropertymultibutton_buttoniconfont.html
        """
        ## ButtonIconFont - Шрифт для кнопок

        Тип данных: BSTR

        ### Синтаксис Automation

        ```
        ButtonIconFont = Object.ButtonIconFont( BtnID )     # Получить свойство (* )
        Object.ButtonIconFont( BtnID ) = ButtonIconFont     # Установить свойство (* )
        ButtonIconFont = Object.GetButtonIconFont( BtnID )  # Получить свойство(**)
        Object.SetButtonIconFont( BtnID, ButtonIconFont )   # Установить свойство (**)
        ```

        ### Входные параметры

        +---------------------------------+
        | BtnID | - идентификатор кнопки. |
        +---------------------------------+
        """
        ...

    def ButtonTips(self, BtnID) -> str:  # ipropertymultibutton_buttontips.html
        """
        ## ButtonTips - Строка подсказки для кнопки

        Тип данных BSTR

        1. Данное свойство позволяет установить или получить строку подсказки для кнопки с идентификатором btnID.

        2. Для корректного отображения строки подсказки конкретной кнопки необходимо задать строку подсказки для набора кнопок. Если текст подсказки набора не нужен или не имеет значения, эта строка может состоять, например, из одного пробела.

        ### Синтаксис Automation

        ```
        ButtonTips = iObject.ButtonTips (btnID)     # Получить свойство (* )
        iObject.ButtonTips (btnID) = ButtonTips     # Установить свойство (* )
        ButtonTips = iObject.GetButtonTips (btnID)  # Получить свойство(**)
        iObject.SetButtonTips (btnID, ButtonTips)   # Установить свойство (**)
        ```

        ### Входные параметры

        +---------------------------------+
        | btnID | - идентификатор кнопки. |
        +---------------------------------+
        """
        ...

    def ButtonVisible(self, BtnID) -> bool:  # ipropertymultibutton_buttonvisible.html
        """
        ## ButtonVisible - Отображение кнопки

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        ButtonVisible = Object.ButtonVisible( BtnID )     # Получить свойство (* )
        Object.ButtonVisible( BtnID ) = ButtonVisible     # Установить свойство (* )
        ButtonVisible = Object.GetButtonVisible( BtnID )  # Получить свойство(**)
        Object.SetButtonVisible( BtnID, ButtonVisible )   # Установить свойство (**)
        ```

        ### Входные параметры

        +---------------------------------+
        | BtnID | - идентификатор кнопки. |
        +---------------------------------+
        """
        ...

    def SetButtonChecked(self, BtnID, arg1):
        ...

    def SetButtonEnable(self, BtnID, arg1):
        ...

    def SetButtonHint(self, BtnID, arg1):
        ...

    def SetButtonIconFont(self, BtnID, arg1):
        ...

    def SetButtonTips(self, BtnID, arg1):
        ...

    def SetButtonVisible(self, BtnID, arg1):
        ...




class IPropertyOpticalProps(IPropertyControl):  # ipropertyopticalprops.html
    """
    ## Интерфейс IPropertyOpticalProps

    Интерфейс элемента управления Панели свойств - Контрол оптических свойств.
    """

    @property
    def ColorParam(self) -> IColorParam7:  # ipropertyopticalprops_colorparam.html
        """
        ## ColorParam - Получить указатель на параметры

        Тип данных: указатель на интерфейс IColorParam7

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        ColorParam = Object.ColorParam       # Получить свойство (* )
        ColorParam = Object.GetColorParam()  # Получить свойство(**)
        ```
        """
        ...

    @property
    def EnableAmbient(self) -> bool:  # ipropertyopticalprops_enableambient.html
        """
        ## EnableAmbient - Доступность выбора общего цвета

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        EnableAmbient = Object.EnableAmbient       # Получить свойство (* )
        Object.EnableAmbient = EnableAmbient       # Установить свойство (* )
        EnableAmbient = Object.GetEnableAmbient()  # Получить свойство (**)
        Object.SetEnableAmbient( EnableAmbient )   # Установить свойство (**)
        ```
        """
        ...
    @EnableAmbient.setter
    def EnableAmbient(self, value: bool): ...

    @property
    def EnableDiffuse(self) -> bool:  # ipropertyopticalprops_enablediffuse.html
        """
        ## EnableDiffuse - Доступность выбора диффузии

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        EnableDiffuse = Object.EnableDiffuse       # Получить свойство (* )
        Object.EnableDiffuse = EnableDiffuse       # Установить свойство (* )
        EnableDiffuse = Object.GetEnableDiffuse()  # Получить свойство (**)
        Object.SetEnableDiffuse( EnableDiffuse )   # Установить свойство (**)
        ```
        """
        ...
    @EnableDiffuse.setter
    def EnableDiffuse(self, value: bool): ...

    @property
    def EnableEmission(self) -> bool:  # ipropertyopticalprops_enableemission.html
        """
        ## EnableEmission - Доступность выбора излучения

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        EnableEmission = Object.EnableEmission       # Получить свойство (* )
        Object.EnableEmission = EnableEmission       # Установить свойство (* )
        EnableEmission = Object.GetEnableEmission()  # Получить свойство (**)
        Object.SetEnableEmission( EnableEmission )   # Установить свойство (**)
        ```
        """
        ...
    @EnableEmission.setter
    def EnableEmission(self, value: bool): ...

    @property
    def EnableShininess(self) -> bool:  # ipropertyopticalprops_enableshininess.html
        """
        ## EnableShininess - Доступность выбора блеска

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        EnableShininess = Object.EnableShininess       # Получить свойство (* )
        Object.EnableShininess = EnableShininess       # Установить свойство (* )
        EnableShininess = Object.GetEnableShininess()  # Получить свойство (**)
        Object.SetEnableShininess( EnableShininess ))  # Установить свойство (**)
        ```
        """
        ...
    @EnableShininess.setter
    def EnableShininess(self, value: bool): ...

    @property
    def EnableSpecularity(self) -> bool:  # ipropertyopticalprops_enablespecularity.html
        """
        ## EnableSpecularity - Доступность выбора зеркальности

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        EnableSpecularity = Object.EnableSpecularity       # Получить свойство (* )
        Object.EnableSpecularity = EnableSpecularity       # Установить свойство (* )
        EnableSpecularity = Object.GetEnableSpecularity()  # Получить свойство (**)
        Object.SetEnableSpecularity( EnableSpecularity )   # Установить свойство (**)
        ```
        """
        ...
    @EnableSpecularity.setter
    def EnableSpecularity(self, value: bool): ...

    @property
    def EnableTransparency(self) -> bool:  # ipropertyopticalprops_enabletransparency.html
        """
        ## EnableTransparency - Доступность выбора прозрачности

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        EnableTransparency = Object.EnableTransparency       # Получить свойство (* )
        Object.EnableTransparency = EnableTransparency       # Установить свойство (* )
        EnableTransparency = Object.GetEnableTransparency()  # Получить свойство (**)
        Object.SetEnableTransparency( EnableTransparency )   # Установить свойство (**)
        ```
        """
        ...
    @EnableTransparency.setter
    def EnableTransparency(self, value: bool): ...

    def Init(self, PVal) -> bool:  # ipropertyopticalprops_init.html
        """
        ## Init - Инициализировать контрол по параметрам

        ### Синтаксис Automation

        ```
        BOOL Init( IColorParam7 * PVal );
        ```

        ### Входные параметры

        PVal - указатель на интерфейс оптических свойств.

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...




class IPropertySeparator(IPropertyControl):  # ipropertyseparator.html
    """
    ## Интерфейс IPropertySeparator

    Интерфейс элемента управления Панели свойств типа разделитель (сепаратор).

    1. Данный интерфейс позволяет получить и установить параметры разделителя.

    2. Данный интерфейс может быть получен от интерфейса коллекции элементов управления IPropertyControls с помощью свойства IPropertyControls::Item или метода IPropertyControls::Add с последующим приведением интерфейса IPropertyControl к данному интерфейсу с помощью метода IUnknown::QueryInterface.
    """

    @property
    def SeparatorType(self) -> int:  # ipropertyseparator_separatortype.html
        """
        ## SeparatorType - Тип разделителя

        Функция не поддерживается

        Тип данных: тип разделителя из перечисления SeparatorTypeEnum.

        Позволяет получить и установить тип разделителя.

        ### Синтаксис Automation

        ```
        SeparatorType = iObject.SeparatorType       # Получить свойство (* )
        iObject.SeparatorType = SeparatorType       # Установить свойство (* )
        SeparatorType = iObject.GetSeparatorType()  # Получить свойство(**)
        iObject.SetSeparatorType (SeparatorType)    # Установить свойство (**)
        ```
        """
        ...
    @SeparatorType.setter
    def SeparatorType(self, value: int): ...

    def SetImage(self, IdBmp, HInstance):  # ipropertyseparator_setimage.html
        """
        ## SetImage - Установить изображение

        Функция не поддерживается

        Если значок задается в виде идентификатора, то должен быть задан dll-модуль hInstance, в ресурсах которого лежит этот значок.

        ### Синтаксис Automation

        ```
        BOOL SetImage (VARIANT idBmp, VARIANT hInstance);
        ```

        ### Входные параметры

        +-----------------------------------------------------------------------------------------------------------------------------+
        | idBmp     | - идентификатор значка в файле ресурсов (тип данных long) или полный путь к bmp файлу значка (тип данных BSTR), |
        +-----------------------------------------------------------------------------------------------------------------------------+
        | hInstance | - HINSTANCE модуля, в котором находится изображение.                                                            |
        +-----------------------------------------------------------------------------------------------------------------------------+

        ### Возвращаемое значение

        Результат выполнения метода.
        """
        ...




class IPropertySlideBox(IPropertyControl):  # ipropertyslidebox.html
    """
    ## Интерфейс IPropertySlideBox

    Интерфейс элемента управления Панели свойств типа Окно отображения слайда, растрового изображения, файла документа КОМПАС или группы файлов.

    1. Данный интерфейс можно получить через коллекцию элементов управления Панели свойств IPropertyControls.

    2. После создания элемента управления и вывода его на экран все изменения, вносимые с помощью свойств и методов данного интерфейса, вступят в силу только после вызова метода UpdateParam.

    3. Для обновления изображения в окне, например после изменения геометрии в отображаемом документе, можно использовать метод UpdateParam.
    """

    @property
    def CheckBoxVisibility(self) -> bool:  # ipropertyslidebox_checkboxvisibility.html
        """
        ## CheckBoxVisibility - Видимость опции, управляющей отрисовкой изображения

        Тип данных: BOOL.

        Свойство позволяет определить тип отображаемого значения.

        ### Синтаксис Automation

        ```
        CheckBoxVisibility = iObject.CheckBoxVisibility       # Получить свойство (* )
        iObject.CheckBoxVisibility = CheckBoxVisibility       # Установить свойство (* )
        CheckBoxVisibility = iObject.GetCheckBoxVisibility()  # Получить свойство(**)
        iObject.SetCheckBoxVisibility (CheckBoxVisibility)    # Установить свойство (**)
        ```

        ### Возвращаемое значение

        +-------------------------------------------+
        | TRUE  | - опция отображается под слайдом, |
        +-------------------------------------------+
        | FALSE | - опция не отображается.          |
        +-------------------------------------------+
        """
        ...
    @CheckBoxVisibility.setter
    def CheckBoxVisibility(self, value: bool): ...

    @property
    def DrawingSlide(self) -> str:  # ipropertyslidebox_drawingslide.html
        """
        ## DrawingSlide - Отображаемый слайд

        Тип данных: VARIANT (long или BSTR).

        Значение свойства:

        Идентификатор или указатель на группу, документ (тип значения long), или полный путь к файлу (тип значения BSTR) документа или текстового файла с описанием слайда.

        Тип отображаемого объекта определяется свойством SlideType.

        ### Синтаксис Automation

        ```
        DrawingSlide = iObject.DrawingSlide       # Получить свойство (* )
        iObject.DrawingSlide = DrawingSlide       # Установить свойство (* )
        DrawingSlide = iObject.GetDrawingSlide()  # Получить свойство(**)
        iObject.SetDrawingSlide (DrawingSlide)    # Установить свойство (**)
        ```
        """
        ...
    @DrawingSlide.setter
    def DrawingSlide(self, value: str): ...

    @property
    def Height(self) -> int:  # ipropertyslidebox_height.html
        """
        ## Height - Высота элемента управления

        Тип данных: long.

        Значение свойства:

        Высота элемента управления (в пикселах).

        ### Синтаксис Automation

        ```
        Height = iObject.Height       # Получить свойство (* )
        iObject.Height = Height       # Установить свойство (* )
        Height = iObject.GetHeight()  # Получить свойство(**)
        iObject.SetHeight (Height)    # Установить свойство (**)
        ```
        """
        ...
    @Height.setter
    def Height(self, value: int): ...

    @property
    def ResModule(self) -> str:  # ipropertyslidebox_resmodule.html
        """
        ## ResModule - Модуль с ресурсами

        Тип данных: VARIANT (long или BSTR).

        Значение свойства:

        HINSTANCE (тип значения long) или полный путь к файлу (тип значения BSTR) dll модуля, в котором находятся все необходимые ресурсы.

        Данное свойство позволяет установить или получить dll-модуль, с которого будут доставаться значки или слайды для отображения в окне.

        ### Синтаксис Automation

        ```
        ResModule = iObject.ResModule       # Получить свойство (* )
        iObject.ResModule = ResModule       # Установить свойство (* )
        ResModule = iObject.GetResModule()  # Получить свойство(**)
        iObject.SetResModule (ResModule)    # Установить свойство (**)
        ```
        """
        ...
    @ResModule.setter
    def ResModule(self, value: str): ...

    @property
    def SlideType(self) -> int:  # ipropertyslidebox_slidetype.html
        """
        ## SlideType - Тип отображения

        Тип данных: тип отображения из перечисления SlideTypeEnum.

        Свойство позволяет определить тип отображаемого значения.

        ### Синтаксис Automation

        ```
        SlideType = iObject.SlideType       # Получить свойство (* )
        iObject.SlideType = SlideType       # Установить свойство (* )
        SlideType = iObject.GetSlideType()  # Получить свойство(**)
        iObject.SetSlideType (SlideType)    # Установить свойство (**)
        ```
        """
        ...
    @SlideType.setter
    def SlideType(self, value: int): ...

    @property
    def Width(self) -> int:  # ipropertyslidebox_width.html
        """
        ## Width - Ширина элемента управления

        Тип данных: long.

        Значение свойства:

        Ширина элемента управления (в пикселах).

        ### Синтаксис Automation

        ```
        Width = iObject.Width       # Получить свойство (* )
        iObject.Width = Width       # Установить свойство (* )
        Width = iObject.GetWidth()  # Получить свойство(**)
        iObject.SetWidth (Width)    # Установить свойство (**)
        ```
        """
        ...
    @Width.setter
    def Width(self, value: int): ...

    def UpdateParam(self):  # ipropertyslidebox_updateparam.html
        """
        ## UpdateParam - Обновить параметры окна отображения слайда

        1. Все изменения в окне отображения слайда вступят в силу только после вызова этого метода.

        2. Этот метод можно использовать для обновления изображения в окне.

        ### Синтаксис Automation

        ```
        void UpdateParam();
        ```
        """
        ...




class IPropertySpinEdit(IPropertyControl):  # ipropertyspinedit.html
    """
    ## Интерфейс IPropertySpinEdit

    Интерфейс элемента управления Панели свойств типа Поле ввода со счетчиком.

    1. Данный интерфейс можно получить через коллекцию элементов управления Панели свойств IPropertyControls.

    2. При нажатии на кнопку счетчика вызывается событие ksPropertyManagerNotify::ControlCommand.

    3. При изменении значения параметра вызывается событие ksPropertyManagerNotify::ChangeControlValue.
    """

    @property
    def CheckState(self) -> int:  # ipropertyspinedit_checkstate.html
        """
        ## CheckState - Состояние опции

        Тип данных: состояние опции из перечисления CheckStateEnum.

        Это свойство влияет на состояние переключателя, если свойство WithCheck имеет значение TRUE, и на отображение текста в поле элемента управления, подробнее см. CheckStateEnum.

        ### Синтаксис Automation

        ```
        CheckState = iObject.CheckState       # Получить свойство (* )
        iObject.CheckState = CheckState       # Установить свойство (* )
        CheckState = iObject.GetCheckState()  # Получить свойство(**)
        iObject.SetCheckState (CheckState)    # Установить свойство (**)
        ```
        """
        ...
    @CheckState.setter
    def CheckState(self, value: int): ...

    @property
    def MaxValue(self) -> typing.Any:  # ipropertyspinedit_maxvalue.html
        """
        ## MaxValue - Максимальное значение

        Тип данных: VARIANT

        1. Свойство доступно только для чтения.

        2. Тип данных определяется типом элемента управления:

        •для типа ksControlSpinInt из ControlTypeEnum используется тип long,

        •для типа ksControlSpinReal из ControlTypeEnum используется тип double.

        ### Синтаксис Automation

        ```
        MaxValue = iObject.MaxValue       # Получить свойство (* )
        MaxValue = iObject.GetMaxValue()  # Получить свойство(**)
        ```
        """
        ...

    @property
    def MinValue(self) -> typing.Any:  # ipropertyspinedit_minvalue.html
        """
        ## MinValue - Минимальное значение

        Тип данных: VARIANT

        1. Свойство доступно только для чтения.

        2. Тип данных определяется типом элемента управления:

        •для типа ksControlSpinInt из ControlTypeEnum используется тип long,

        •для типа ksControlSpinReal из ControlTypeEnum используется тип double.

        ### Синтаксис Automation

        ```
        MinValue = iObject.MinValue       # Получить свойство (* )
        MinValue = iObject.GetMinValue()  # Получить свойство(**)
        ```
        """
        ...

    @property
    def ReadOnly(self) -> bool:  # ipropertyspinedit_readonly.html
        """
        ## ReadOnly - Запрет редактирования значения

        Тип данных: BOOL.

        Значение свойства:

        +-------------------------------------------------------------------------------+
        | TRUE  | - ввод нового значения с клавиатуры в поле запрещен,                  |
        +-------------------------------------------------------------------------------+
        | FALSE | - ввод нового значения с клавиатуры в раскрывающийся список разрешен. |
        +-------------------------------------------------------------------------------+

        ### Синтаксис Automation

        ```
        ReadOnly = iObject.ReadOnly       # Получить свойство (* )
        iObject.ReadOnly = ReadOnly       # Установить свойство (* )
        ReadOnly = iObject.GetReadOnly()  # Получить свойство(**)
        iObject.SetReadOnly (ReadOnly)    # Установить свойство (**)
        ```
        """
        ...
    @ReadOnly.setter
    def ReadOnly(self, value: bool): ...

    @property
    def Step(self) -> float:  # ipropertyspinedit_step.html
        """
        ## Step - Шаг

        Тип данных: double.

        ### Синтаксис Automation

        ```
        Step = Object.Step       # Получить свойство (* )
        Object.Step = Step       # Установить свойство (* )
        Step = Object.GetStep()  # Получить свойство(**)
        Object.SetStep (Step)    # Установить свойство (**)
        ```
        """
        ...
    @Step.setter
    def Step(self, value: float): ...

    @property
    def Width(self) -> int:  # ipropertyspinedit_width.html
        """
        ## Width - Шаблон для расчета ширины элемента управления в горизонтальном положении (в символах)

        Тип данных: long.

        Значение свойства:

        Количество видимых символов в поле при горизонтальном положении Панели свойств.

        Для расчета ширины элемента управления используется символ «0».

        ### Синтаксис Automation

        ```
        Width = iObject.Width       # Получить свойство (* )
        iObject.Width = Width       # Установить свойство (* )
        Width = iObject.GetWidth()  # Получить свойство(**)
        iObject.SetWidth (Width)    # Установить свойство (**)
        ```
        """
        ...
    @Width.setter
    def Width(self, value: int): ...

    @property
    def WithCheck(self) -> bool:  # ipropertyspinedit_withcheck.html
        """
        ## WithCheck - Наличие опции рядом с полем ввода

        Тип данных: BOOL

        Значение свойства:

        +------------------------------------------------------------+
        | TRUE  | - рядом с элементом управления выводится опция,    |
        +------------------------------------------------------------+
        | FALSE | - рядом с элементом управления не выводится опция. |
        +------------------------------------------------------------+

        ### Синтаксис Automation

        ```
        WithCheck = iObject.WithCheck       # Получить свойство (* )
        iObject.WithCheck = WithCheck       # Установить свойство (* )
        WithCheck = iObject.GetWithCheck()  # Получить свойство(**)
        iObject.SetWithCheck (WithCheck)    # Установить свойство (**)
        ```
        """
        ...
    @WithCheck.setter
    def WithCheck(self, value: bool): ...

    def SetValueRange(self, MinVal, MaxVal):  # ipropertyspinedit_setvaluerange.html
        """
        ## SetValueRange - Установить новые ограничения на значение

        Тип данных максимального и минимального допустимого значения определяется типом элемента управления:

        •для типа ksControlSpinInt из ControlTypeEnum используется тип long,

        •для типа ksControlSpinReal из ControlTypeEnum используется тип double.

        ### Синтаксис Automation

        ```
        BOOL SetValueRange (const VARIANT& minVal, const VARIANT& maxVal);
        ```

        ### Входные параметры

        +-----------------------------------+
        | minVal | - минимальное значение,  |
        +-----------------------------------+
        | maxVal | - максимальное значение. |
        +-----------------------------------+
        """
        ...




class IPropertyStyleList(IPropertyControl):  # ipropertystylelist.html
    """
    ## Интерфейс IPropertyStyleList

    Элемент Панели свойств - Комбобокс со стилем.

    Интерфейс списка, из которого можно выбрать текущий стиль линии.

    Данный интерфейс может быть получен от интерфейса коллекции элементов управления IPropertyControls с помощью свойства IPropertyControls::Item или метода IPropertyControls::Add с последующим приведением интерфейса IPropertyControl к данному интерфейсу с помощью метода IUnknown::QueryInterface.

    Перечень линий, доступных для выбора, а также порядок следования линий в списке определяется настройкой фильтра линий, сделанной для текущего документа.

    Кнопка Другой стиль позволяет выбрать стиль из внешней библиотеки, или из сформированного пользователем набора, или один из стилей, хранящихся в документе. Ее наличием можно управлять с помощью свойства AnotherStyleBtnEnable.

    Можно наполнить комбобокс только нужными стилями.
    """

    @property
    def AnotherStyleBtnEnable(self) -> bool:  # ipropertystylelist_anotherstylebtnenable.html
        """
        ## AnotherStyleBtnEnable - Доступность кнопки Другой стиль

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        AnotherStyleBtnEnable = Object.AnotherStyleBtnEnable       # Получить свойство (* )
        Object.AnotherStyleBtnEnable = AnotherStyleBtnEnable       # Установить свойство (* )
        AnotherStyleBtnEnable = Object.GetAnotherStyleBtnEnable()  # Получить свойство(**)
        Object.SetAnotherStyleBtnEnable( AnotherStyleBtnEnable )   # Установить свойство (**)
        ```
        """
        ...
    @AnotherStyleBtnEnable.setter
    def AnotherStyleBtnEnable(self, value: bool): ...

    @property
    def Count(self) -> int:  # ipropertystylelist_count.html
        """
        ## Count - Количество элементов списка

        Тип данных: long

        ### Синтаксис Automation

        ```
        Count = Object.Count       # Получить свойство (* )
        Object.Count = Count       # Установить свойство (* )
        Count = Object.GetCount()  # Получить свойство(**)
        Object.SetCount( Count )   # Установить свойство (**)
        ```
        """
        ...

    @property
    def CurrentIndex(self) -> int:  # ipropertystylelist_currentindex.html
        """
        ## CurrentIndex - Индекс текущего стиля

        Тип данных: long

        ### Синтаксис Automation

        ```
        CurrentIndex = Object.CurrentIndex       # Получить свойство (* )
        Object.CurrentIndex = CurrentIndex       # Установить свойство (* )
        CurrentIndex = Object.GetCurrentIndex()  # Получить свойство(**)
        Object.SetCurrentIndex( CurrentIndex )   # Установить свойство (**)
        ```
        """
        ...
    @CurrentIndex.setter
    def CurrentIndex(self, value: int): ...

    def Add(self, Styles) -> bool:  # ipropertystylelist_add.html
        """
        ## Add - Добавить массив стилей в список

        Если добавляется один стиль, его можно передать как VT_I4.

        Если требуется добавить несколько стилей, то нужно создать массив VT_ARRAY | VT_I4.

        ### Синтаксис Automation

        ```
        BOOL Add( VARIANT Styles );
        ```

        ### Входные параметры

        +---------------------------+
        | Styles | - список стилей. |
        +---------------------------+

        ### Возвращаемое значение

        +--------------------------+
        | TRUE | - в случае удачи. |
        +--------------------------+
        """
        ...

    def ClearList(self) -> bool:  # ipropertystylelist_clearlist.html
        """
        ## ClearList - Очистить список

        ### Синтаксис Automation

        ```
        BOOL ClearList();
        ```

        ### Возвращаемое значение

        +--------------------------+
        | TRUE | - в случае удачи. |
        +--------------------------+
        """
        ...

    def Find(self, Val):  # ipropertystylelist_find.html
        """
        ## Find - Найти индекс стиля по значению

        ### Синтаксис Automation

        ```
        long Find( long Val );
        ```

        ### Возвращаемое значение

        +--------------------------------------------------------+
        | индекс значения в списке | - в случае удачи,           |
        +--------------------------------------------------------+
        | -1                       | - если значение не найдено. |
        +--------------------------------------------------------+
        """
        ...




class IPropertyTextButton(IPropertyControl):  # ipropertytextbutton.html
    """
    ## Интерфейс IPropertyTextButton

    Интерфейс элемента управления Панели свойств типа Кнопка с текстом.

    1. Данный интерфейс можно получить через коллекцию элементов управления Панели свойств IPropertyControls.

    2. При нажатии на кнопку вызывается событие ksPropertyManagerNotify::ControlCommand.

    3. Элемент управления работает как обычная кнопка со стилем BS_PUSHBUTTON.
    """

    ...


class IPropertyUserControl(IPropertyControl):  # ipropertyusercontrol.html
    """
    ## Интерфейс IPropertyUserControl

    События...

    Интерфейс элемента управления Панели свойств Пользовательский элемент управления.

    Данный интерфейс можно получить через коллекцию элементов управления Панели свойств IPropertyControls.
    """

    @property
    def AutoSize(self) -> bool:  # ipropertyusercontrol_autosize.html
        """
        ## AutoSize - Автоматический расчет высоты контрола. TRUE - Растягивать на всю доступную область

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        AutoSize = Object.AutoSize       # Получить свойство (* )
        Object.AutoSize = AutoSize       # Установить свойство (* )
        AutoSize = Object.GetAutoSize()  # Получить свойство(**)
        Object.SetAutoSize( AutoSize )   # Установить свойство (**)
        ```
        """
        ...
    @AutoSize.setter
    def AutoSize(self, value: bool): ...

    @property
    def FixHeight(self) -> bool:  # ipropertyusercontrol_fixheight.html
        """
        ## FixHeight - Зафиксировать высоту элемента управления

        Тип данных: BOOL

        Значение свойства:

        TRUE - размер элемента управления по высоте зафиксирован и равен значению, установленному в свойстве Height - Высота элемента управления.

        По умолчанию свойство имеет значение FALSE - размер элемента управления по высоте не зафиксирован.

        ### Синтаксис Automation

        ```
        FixHeight = iObject.FixHeight       # Получить свойство (* )
        iObject.FixHeight = FixHeight       # Установить свойство (* )
        FixHeight = iObject.GetFixHeight()  # Получить свойство(**)
        iObject.SetFixHeight(FixHeight)     # Установить свойство (**)
        ```
        """
        ...
    @FixHeight.setter
    def FixHeight(self, value: bool): ...

    @property
    def FixWidth(self) -> bool:  # ipropertyusercontrol_fixwidth.html
        """
        ## FixWidth - Зафиксировать ширину элемента управления

        Тип данных: BOOL

        Значение свойства:

        TRUE - размер элемента управления по ширине зафиксирован и равен значению, установленному в свойстве Width - Ширина элемента управления.

        По умолчанию свойство имеет значение FALSE - размер элемента управления по ширине не зафиксирован. Свойство работает при горизонтальном размещении Панели свойств.

        ### Синтаксис Automation

        ```
        FixWidth = iObject.FixWidth       # Получить свойство (* )
        iObject.FixWidth = FixWidth       # Установить свойство (* )
        FixWidth = iObject.GetFixWidth()  # Получить свойство(**)
        iObject.SetFixWidth(FixWidth)     # Установить свойство (**)
        ```
        """
        ...
    @FixWidth.setter
    def FixWidth(self, value: bool): ...

    @property
    def Height(self) -> int:  # ipropertyusercontrol_height.html
        """
        ## Height - Высота элемента управления

        Тип данных: long.

        Значение свойства:

        Высота элемента управления в пикселах.

        ### Синтаксис Automation

        ```
        Height = iObject.Height       # Получить свойство (* )
        iObject.Height = Height       # Установить свойство (* )
        Height = iObject.GetHeight()  # Получить свойство(**)
        iObject.SetHeight (Height)    # Установить свойство (**)
        ```
        """
        ...
    @Height.setter
    def Height(self, value: int): ...

    @property
    def Width(self) -> int:  # ipropertyusercontrol_width.html
        """
        ## Width - Ширина элемента управления

        Тип данных: long.

        Значение свойства:

        Ширина элемента управления в пикселах.

        Используется только для Панели свойств в горизонтальном положении. В вертикальном - определяется шириной самой Панели свойств.

        ### Синтаксис Automation

        ```
        Width = iObject.Width       # Получить свойство (* )
        iObject.Width = Width       # Установить свойство (* )
        Width = iObject.GetWidth()  # Получить свойство(**)
        iObject.SetWidth (Width)    # Установить свойство (**)
        ```
        """
        ...
    @Width.setter
    def Width(self, value: int): ...

    def SetOCXControl(self, ProgID):  # ipropertyusercontrol_setocxcontrol.html
        """
        ## SetOCXControl - Установить progID элемента управления OCX

        OCX контрол должен быть зарегистрирован в реестре Windows.

        ### Синтаксис Automation

        ```
        void SetOCXControl (LPCTSTR progID);
        ```

        ### Входные параметры

        +---------------------------------------------------+
        | progID | - строка progID элемента управления OCX. |
        +---------------------------------------------------+
        """
        ...




class IPropertyManager(IKompasAPIObject):  # ipropertymanager.html
    """
    ## Интерфейс IPropertyManager

    Интерфейс Панели свойств.

    Создать панель свойств и получить данный интерфейс можно от интерфейса приложения IApplication с помощью свойства IApplication::CreatePropertyManager.
    """

    @property
    def ActivateOnCreate(self) -> bool:  # ipropertymanager_activateoncreate.html
        """
        ## ActivateOnCreate - Активизировать закладку при создании

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        ActivateOnCreate = Object.ActivateOnCreate       # Получить свойство (* )
        Object.ActivateOnCreate = ActivateOnCreate       # Установить свойство (* )
        ActivateOnCreate = Object.GetActivateOnCreate()  # Получить свойство(**)
        Object.SetActivateOnCreate( ActivateOnCreate )   # Установить свойство (**)
        ```
        """
        ...
    @ActivateOnCreate.setter
    def ActivateOnCreate(self, value: bool): ...

    @property
    def Active(self) -> bool:  # ipropertymanager_active.html
        """
        ## Active - Признак активности панели

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        Active = Object.Active       # Получить свойство (* )
        Object.Active = Active       # Установить свойство (* )
        Active = Object.GetActive()  # Получить свойство(**)
        Object.SetActive( Active )   # Установить свойство (**)
        ```
        """
        ...
    @Active.setter
    def Active(self, value: bool): ...

    @property
    def AutoHideMode(self) -> bool:  # ipropertymanager_autohidemode.html
        """
        ## AutoHideMode - Свернутость Панели свойств

        Функция не поддерживается

        Тип данных: BOOL.

        Значения свойства:

        +------------------------------+
        | TRUE  | - панель свернута,   |
        +------------------------------+
        | FALSE | - панель развернута. |
        +------------------------------+

        Позволяет получать и устанавливать свойство, определяющее, свернута панель или нет.

        ### Синтаксис Automation

        ```
        AutoHideMode = iObject.AutoHideMode;       # Получить свойство (* )
        iObject.AutoHideMode = AutoHideMode;       # Установить свойство (* )
        AutoHideMode = iObject.GetAutoHideMode();  # Получить свойство(**)
        iObject.SetAutoHideMode( AutoHideMode );   # Установить свойство (**)
        ```
        """
        ...
    @AutoHideMode.setter
    def AutoHideMode(self, value: bool): ...

    @property
    def Caption(self) -> str:  # ipropertymanager_caption.html
        """
        ## Caption - Заголовок Панели свойств

        Тип данных: BSTR (строка).

        С помощью данного свойства можно получить и изменить заголовок Панели свойств.

        ### Синтаксис Automation

        ```
        Caption = iObject.Caption       # Получить свойство (* )
        iObject.Caption = Caption       # Установить свойство (* )
        Caption = iObject.GetCaption()  # Получить свойство(**)
        iObject.SetCaption (Caption)    # Установить свойство (**)
        ```
        """
        ...
    @Caption.setter
    def Caption(self, value: str): ...

    @property
    def EnterButtonIconType(self) -> int:  # ipropertymanager_enterbuttonicontype.html
        """
        ## EnterButtonIconType - Тип иконки для кнопки Cоздать

        Тип данных: из перечисления ksEnterButtonIconTypeEnum

        ### Синтаксис Automation

        ```
        EnterButtonIconType = Object.EnterButtonIconType       # Получить свойство (* )
        Object.EnterButtonIconType = EnterButtonIconType       # Установить свойство (* )
        EnterButtonIconType = Object.GetEnterButtonIconType()  # Получить свойство(**)
        Object.SetEnterButtonIconType( EnterButtonIconType )   # Установить свойство (**)
        ```
        """
        ...
    @EnterButtonIconType.setter
    def EnterButtonIconType(self, value: int): ...

    @property
    def Label(self) -> str:  # ipropertymanager_label.html
        """
        ## Label - Заголовок 2 панели свойств

        Тип данных: BSTR

        ### Синтаксис Automation

        ```
        Label = Object.Label       # Получить свойство (* )
        Object.Label = Label       # Установить свойство (* )
        Label = Object.GetLabel()  # Получить свойство(**)
        Object.SetLabel( Label )   # Установить свойство (**)
        ```
        """
        ...
    @Label.setter
    def Label(self, value: str): ...

    @property
    def Layout(self) -> int:  # ipropertymanager_layout.html
        """
        ## Layout - Положение Панели свойств (вверху, внизу, слева, справа, плавает)

        Тип данных: положение Панели свойств из перечисления PropertyManagerLayout.

        С помощью данного свойства можно получить и изменить положение Панели свойств.

        ### Синтаксис Automation

        ```
        Layout = iObject.Layout       # Получить свойство (* )
        iObject.Layout = Layout       # Установить свойство (* )
        Layout = iObject.GetLayout()  # Получить свойство(**)
        iObject.SetLayout (Layout)    # Установить свойство (**)
        ```
        """
        ...
    @Layout.setter
    def Layout(self, value: int): ...

    @property
    def PropertyTabs(self) -> IPropertyTabs:  # ipropertymanager_propertytabs.html
        """
        ## PropertyTabs - Коллекция закладок Панели свойств

        Тип данных: указатель на интерфейс IPropertyTabs.

        1. Свойство только для чтения.

        2. Позволяет получить доступ к вкладкам Панели свойств.

        ### Синтаксис Automation

        ```
        PropertyTabs = iObject.PropertyTabs       # Получить свойство (* )
        PropertyTabs = iObject.GetPropertyTabs()  # Получить свойство(**)
        ```
        """
        ...

    @property
    def ResModule(self) -> str:  # ipropertymanager_resmodule.html
        """
        ## ResModule - Модуль с описанием пользовательской спецпанели

        Тип данных: VARIANT ( LONG_PTR или BSTR).

        Значение свойства:

        +----------------------------------------------------------------------------------------------------------------------------------------+
        | HINSTANCE (тип значения LONG_PTR) | - полный путь к файлу (тип значения BSTR) dll-модуля, в котором находятся все необходимые ресурсы. |
        +----------------------------------------------------------------------------------------------------------------------------------------+

        1. HINSTANCE библиотеки нужно передать через VARIANT, как VT_I4 в Win32 и VT_I8 в x64.

        Для правильного формирования _variant_t HINSTANCE нужно передавать через приведение к (LONG_PTR).

        2. Данное свойство позволяет установить или получить dll-модуль, в котором находится описание пользовательской спецпанели, (пример для пользовательских кнопок см. IPropertyManager::SpecToolbarEx).

        3. Свойство не используется при работе со шрифтовыми иконками

        ### Синтаксис Automation

        ```
        ResModule = Object.ResModule       # Получить свойство (* )
        Object.ResModule = ResModule       # Установить свойство (* )
        ResModule = Object.GetResModule()  # Получить свойство(**)
        Object.SetResModule( ResModule )   # Установить свойство (**)
        ```
        """
        ...
    @ResModule.setter
    def ResModule(self, value: str): ...

    @property
    def SpecToolbar(self) -> int:  # ipropertymanager_spectoolbar.html
        """
        ## SpecToolbar - Специальная панель

        Тип данных: тип предопределенной спецпанели для Панели свойств из перечисления SpecPropertyToolBarEnum.

        С помощью данного свойства можно получить и изменить тип предопределенной спецпанели для Панели свойств.

        ### Синтаксис Automation

        ```
        SpecToolbar = iObject.SpecToolbar       # Получить свойство (* )
        iObject.SpecToolbar = SpecToolbar       # Установить свойство (* )
        SpecToolbar = iObject.GetSpecToolbar()  # Получить свойство(**)
        iObject.SetSpecToolbar (SpecToolbar)    # Установить свойство (**)
        ```
        """
        ...
    @SpecToolbar.setter
    def SpecToolbar(self, value: int): ...

    @property
    def SpecToolbarEx(self) -> int:  # ipropertymanager_spectoolbarex.html
        """
        ## SpecToolbarEx - Пользовательская спецпанель

        Тип данных: long.

        1. Данное свойство является расширением свойства IPropertyManager::SpecToolbar.

        2. Свойство позволяет установить или получить идентификатор спецпанели.

        Может быть установлен идентификатор из перечисления SpecPropertyToolBarEnum (предопределенных спецпанелей для панели свойств) либо задан идентификатор ресурса пользовательской спецпанели, уникальный в рамках библиотеки.

        3. Свойство SpecToolbarEx используется совместно со свойством IPropertyManager::ResModule.

        4. Чтобы описать пользовательскую спецпанель, нужно:

        4.1. Объявить уникальный идентификатор спецпанели в h файле

        #define ITB_SPEC_TOOLBAR 3000

        #define END_OF_RESOURCE_TABLE 0xffff

        4.2. В ресурсном файле описать RCDATA панели и пользовательские кнопки

        rc2-файл:

        ITB_SPEC_TOOLBAR RCDATA

        {

        1 //pbEnter

        2 //pbEsc

        3 //pbHelpсправка

        20 //пользовательская кнопка

        END_OF_RESOURCE_TABLE

        }

        В примере для пользовательской спецпанели определено 4 кнопки.

        1, 2, 3 - предопределенные кнопки из перечисления SpecPropertyToolBarEnum

        20 - пользовательская кнопка

        //битмап для пользовательской кнопки спецпанели

        20 ICON DISCARDABLE "res\\\\st_reduc.ico" // CREATE процесса настройки фильтра для выбора объектов отчета

        //tips\\hint для пользовательской кнопки спецпанели

        STRINGTABLE LOADONCALL MOVEABLE DISCARDABLE

        BEGIN

        20 "Для тестирования пользовательской кнопки\\nПользовательская кнопка"

        END

        ### Синтаксис Automation

        ```
        SpecToolbarEx = Object.SpecToolbarEx       # Получить свойство (* )
        Object.SpecToolbarEx = SpecToolbarEx       # Установить свойство (* )
        SpecToolbarEx = Object.GetSpecToolbarEx()  # Получить свойство(**)
        Object.SetSpecToolbarEx( SpecToolbarEx )   # Установить свойство (**)
        ```
        """
        ...
    @SpecToolbarEx.setter
    def SpecToolbarEx(self, value: int): ...

    @property
    def StatusMessage(self) -> str:  # ipropertymanager_statusmessage.html
        """
        ## StatusMessage - Строка подсказки

        Тип данных: BSTR

        ### Синтаксис Automation

        ```
        StatusMessage = Object.StatusMessage       # Получить свойство (* )
        Object.StatusMessage = StatusMessage       # Установить свойство (* )
        StatusMessage = Object.GetStatusMessage()  # Получить свойство(**)
        Object.SetStatusMessage( StatusMessage )   # Установить свойство (**)
        ```
        """
        ...
    @StatusMessage.setter
    def StatusMessage(self, value: str): ...

    @property
    def Visible(self) -> bool:  # ipropertymanager_visible.html
        """
        ## Visible - Видимость Панели свойств

        Тип данных: BOOL.

        С помощью данного свойства можно управлять видимостью Панели свойств.

        ### Синтаксис Automation

        ```
        Visible = iObject.Visible       # Получить свойство (* )
        iObject.Visible = Visible       # Установить свойство (* )
        Visible = iObject.GetVisible()  # Получить свойство(**)
        iObject.SetVisible (Visible)    # Установить свойство (**)
        ```
        """
        ...
    @Visible.setter
    def Visible(self, value: bool): ...

    def AddSetupMenuCommand(self, Title, Command, Checable) -> bool:  # ipropertymanager_addsetupmenucommand.html
        """
        ## AddSetupMenuCommand - Добавить пункт в меню настроек процесса

        ### Синтаксис Automation

        ```
        BOOL AddSetupMenuCommand( BSTR Title, long Command, BOOL Checable );
        ```

        ### Входные параметры

        +-----------------------------------------------------------------+
        | Title    | - заголовок,                                         |
        +-----------------------------------------------------------------+
        | Command  | - идентификатор команды,                             |
        +-----------------------------------------------------------------+
        | Checable | - FALSE -выполняемая команда, TRUE - флаг состояния. |
        +-----------------------------------------------------------------+

        ### Возвращаемое значение

        +--------------------------------+
        | TRUE  | - успешное завершение, |
        +--------------------------------+
        | FALSE | - в случае неудачи.    |
        +--------------------------------+
        """
        ...

    def AddSpecToolbarButton(self, BtnID, Bmp, Tips, IconFont) -> bool:  # ipropertymanager_addspectoolbarbutton.html
        """
        ## AddSpecToolbarButton - Добавить кнопку в спецпанель

        1. Для использования иконок из шрифта Компас нужно передать параметр IconFont == пустой строке.

        2. Для использования шрифта библиотеки, описанного в манифесте библиотеки, нужно передать строчку, полученную методом IProceduresLibrary::IconsFont.

        3. Для использования картинок из ресурсов требуется установить свойство IPropertyManager::ResModule.

        4. Для использования шрифтовых иконок свойство IPropertyManager::ResModule устанавливаться не должно.

        ### Синтаксис Automation

        ```
        BOOL AddSpecToolbarButton( long BtnID, VARIANT Bmp, BSTR Tips, BSTR IconFont );
        ```

        ### Входные параметры

        +--------------------------------------------------------------------------+
        | BtnID    | - идентификатор кнопки,                                       |
        +--------------------------------------------------------------------------+
        | Bmp      | - идентификатор картинки или символа шрифта или путь к файлу, |
        +--------------------------------------------------------------------------+
        | Tips     | - имя кнопки,                                                 |
        +--------------------------------------------------------------------------+
        | IconFont | - имя шрифта.                                                 |
        +--------------------------------------------------------------------------+

        ### Возвращаемое значение

        +--------------------------------+
        | TRUE  | - успешное завершение, |
        +--------------------------------+
        | FALSE | - в случае неудачи.    |
        +--------------------------------+
        """
        ...

    def ClearSpecToolbar(self) -> bool:  # ipropertymanager_clearspectoolbar.html
        """
        ## ClearSpecToolbar - Очистить спецпанель

        ### Синтаксис Automation

        ```
        BOOL ClearSpecToolbar();
        ```

        ### Возвращаемое значение

        +-------------------------------+
        | TRUE | - успешное завершение. |
        +-------------------------------+
        """
        ...

    def GetGabaritRect(self):  # ipropertymanager_getgabaritrect.html
        """
        ## GetGabaritRect - Получить габаритный прямоугольник Панели свойств

        Метод позволяет получить габаритный прямоугольник Панели свойств. Точки задаются в относительных координатах окна КОМПАС.

        ### Синтаксис Automation

        ```
        void GetGabaritRect (long* left,
        long* top,
        long* right,
        long* bottom);
        ```

        ### Выходные параметры

        +---------------------------------------------------+
        | left, top     | - координата верхней левой точки, |
        +---------------------------------------------------+
        | right, bottom | - координата нижней правой точки. |
        +---------------------------------------------------+
        """
        ...

    def HideTabs(self) -> bool:  # ipropertymanager_hidetabs.html
        """
        ## HideTabs - Скрыть вкладки и их содержимое на Панели свойств

        Метод позволяет скрыть вкладки и их содержимое на Панели свойств.

        ### Синтаксис Automation

        ```
        BOOL HideTabs();
        ```

        ### Возвращаемое значение

        +--------------------------------+
        | TRUE  | - успешное завершение, |
        +--------------------------------+
        | FALSE | - в случае неудачи.    |
        +--------------------------------+
        """
        ...

    def RepeatCommand(self) -> bool:  # ipropertymanager_repeatcommand.html
        """
        ## RepeatCommand - Повторить команду (посылает повторно событие панели свойств)

        ### Синтаксис Automation

        ```
        BOOL RepeatCommand();
        ```

        ### Возвращаемое значение

        +--------------------------------+
        | TRUE  | - успешное завершение, |
        +--------------------------------+
        | FALSE | - в случае неудачи.    |
        +--------------------------------+
        """
        ...

    def SetGabaritRect(self, Left, Top, Right, Bottom):  # ipropertymanager_setgabaritrect.html
        """
        ## SetGabaritRect - Установить габаритный прямоугольник Панели свойств

        Функция не поддерживается

        Метод позволяет установить габаритный прямоугольник Панели свойств. Точки задаются в относительных координатах окна КОМПАС.

        ### Синтаксис Automation

        ```
        void SetGabaritRect (long left,
        long top,
        long right,
        long bottom);
        ```

        ### Входные параметры

        +---------------------------------------------------+
        | left, top     | - координата верхней левой точки, |
        +---------------------------------------------------+
        | right, bottom | - координата нижней правой точки. |
        +---------------------------------------------------+
        """
        ...

    def SetIcon(self, Icon, IconFont) -> bool:  # ipropertymanager_seticon.html
        """
        ## SetIcon - Изменить иконку панели

        ### Синтаксис Automation

        ```
        BOOL SetIcon( long Icon, BSTR IconFont );
        ```

        ### Входные параметры

        +----------------------------------------------+
        | Icon     | - идентификатор иконки из шрифта, |
        +----------------------------------------------+
        | IconFont | - шрифт.,                         |
        +----------------------------------------------+

        Версия: Компас v20

        ### Возвращаемое значение

        +--------------------------------+
        | TRUE  | - успешное завершение, |
        +--------------------------------+
        | FALSE | - в случае неудачи.    |
        +--------------------------------+
        """
        ...

    def SetSetupMenuCommandState(self, Command, Visible, Enable, Checked) -> bool:  # ipropertymanager_setsetupmenucommandstate.html
        """
        ## SetSetupMenuCommandState - Установить состояние команды меню настроек процесса

        ### Синтаксис Automation

        ```
        BOOL SetSetupMenuCommandState( long Command, BOOL Visible, BOOL Enable, BOOL Checked );
        ```

        ### Входные параметры

        +------------------------------------+
        | Command | - идентификатор команды, |
        +------------------------------------+
        | Visible | - видимость,             |
        +------------------------------------+
        | Enable  | - доступность,           |
        +------------------------------------+
        | Checked | - признак выбора.        |
        +------------------------------------+

        ### Возвращаемое значение

        +--------------------------------+
        | TRUE  | - успешное завершение, |
        +--------------------------------+
        | FALSE | - в случае неудачи.    |
        +--------------------------------+
        """
        ...

    def ShowTabs(self) -> bool:  # ipropertymanager_showtabs.html
        """
        ## ShowTabs - Отобразить вкладки и их содержимое на Панели свойств

        Метод позволяет отобразить вкладки и их содержимое на Панели свойств.

        ### Синтаксис Automation

        ```
        BOOL ShowTabs();
        ```

        ### Возвращаемое значение

        +--------------------------------+
        | TRUE  | - успешное завершение, |
        +--------------------------------+
        | FALSE | - в случае неудачи.    |
        +--------------------------------+
        """
        ...

    def UpdateTabs(self) -> bool:  # ipropertymanager_updatetabs.html
        """
        ## UpdateTabs - Обновить вкладки и их содержимое на Панели свойств

        Метод позволяет обновить вкладки и их содержимое на Панели свойств.

        ### Синтаксис Automation

        ```
        BOOL UpdateTabs();
        ```

        ### Возвращаемое значение

        +--------------------------------+
        | TRUE  | - успешное завершение, |
        +--------------------------------+
        | FALSE | - в случае неудачи.    |
        +--------------------------------+
        """
        ...




class IPropertyTab(IKompasAPIObject):  # ipropertytab.html
    """
    ## Интерфейс IPropertyTab

    Интерфейс вкладки Панели свойств.

    Данный интерфейс может быть получен от интерфейса коллекции закладок панели свойств IPropertyTabs с помощью свойств IPropertyTabs::Item, IPropertyTabs::Active и метода IPropertyTabs::Add.
    """

    @property
    def Active(self) -> bool:  # ipropertytab_active.html
        """
        ## Active - Активность закладки Панели свойств

        Функция не поддерживается

        Тип данных: BOOL

        Свойство позволяет получить и изменить активность вкладки Панели свойств.

        ### Синтаксис Automation

        ```
        Active = iObject.Active       # Получить свойство (* )
        iObject.Active = Active       # Установить свойство (* )
        Active = iObject.GetActive()  # Получить свойство(**)
        iObject.SetActive (Active)    # Установить свойство (**)
        ```
        """
        ...
    @Active.setter
    def Active(self, value: bool): ...

    @property
    def Caption(self) -> str:  # ipropertytab_caption.html
        """
        ## Caption - Заголовок вкладки Панели свойств

        Тип данных: BSTR (строка)

        Свойство позволяет получить и изменить заголовок вкладки Панели свойств.

        ### Синтаксис Automation

        ```
        Caption = iObject.Caption       # Получить свойство (* )
        iObject.Caption = Caption       # Установить свойство (* )
        Caption = iObject.GetCaption()  # Получить свойство(**)
        iObject.SetCaption (Caption)    # Установить свойство (**)
        ```
        """
        ...
    @Caption.setter
    def Caption(self, value: str): ...

    @property
    def Expanded(self) -> bool:  # ipropertytab_expanded.html
        """
        ## Expanded - Признак развернутой закладки

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        Expanded = Object.Expanded       # Получить свойство (* )
        Object.Expanded = Expanded       # Установить свойство (* )
        Expanded = Object.GetExpanded()  # Получить свойство(**)
        Object.SetExpanded( Expanded )   # Установить свойство (**)
        ```
        """
        ...
    @Expanded.setter
    def Expanded(self, value: bool): ...

    @property
    def Image(self) -> str:  # ipropertytab_image.html
        """
        ## Image - Растровый рисунок на вкладке

        Функция не поддерживается

        Тип данных: VARIANT (long или BSTR).

        Значения свойства:

        +-----------------------------------------------------------------------------------------------------------------------------------+
        | HINSTANCE (тип значения long) | - полный путь к файлу (тип значения BSTR) dll модуля в котором находятся все необходимые ресурсы. |
        +-----------------------------------------------------------------------------------------------------------------------------------+

        Свойство позволяет получить или установить битмап на закладке.

        Битмап можно задать в виде идентификатора в файле ресурсов (тип данных long) или полного пути к bmp-файлу значка (тип данных BSTR).

        ### Синтаксис Automation

        ```
        Image = iObject.Image       # Получить свойство (* )
        iObject.Image = Image       # Установить свойство (* )
        Image = iObject.GetImage()  # Получить свойство(**)
        iObject.SetImage(Image)     # Установить свойство (**)
        ```
        """
        ...
    @Image.setter
    def Image(self, value: str): ...

    @property
    def PropertyControls(self) -> IPropertyControls:  # ipropertytab_propertycontrols.html
        """
        ## PropertyControls - Коллекция элементов управленияна вкладке

        Тип данных: указатель на интерфейс IPropertyControls.

        Свойство доступно только для чтения. Позволяет получить коллекцию элементов управления вкладки Панели свойств.

        ### Синтаксис Automation

        ```
        PropertyControls = iObject.PropertyControls       # Получить свойство (* )
        PropertyControls = iObject.GetPropertyControls()  # Получить свойство(**)
        ```
        """
        ...

    @property
    def ResModule(self) -> str:  # ipropertytab_resmodule.html
        """
        ## ResModule - Растровый рисунок на вкладке

        Функция не поддерживается

        Тип данных: VARIANT (long или BSTR).

        Значения свойства:

        +------------------------------------------------------------------------------------------------------------------------------------+
        | HINSTANCE (тип значения long) | - полный путь к файлу (тип значения BSTR) dll модуля, в котором находятся все необходимые ресурсы. |
        +------------------------------------------------------------------------------------------------------------------------------------+

        Свойство позволяет получить или установить dll-модуль, в котором находится рисунок для кнопки.

        ### Синтаксис Automation

        ```
        ResModule = iObject.ResModule       # Получить свойство (* )
        iObject.ResModule = ResModule       # Установить свойство (* )
        ResModule = iObject.GetResModule()  # Получить свойство(**)
        iObject.SetResModule(ResModule)     # Установить свойство (**)
        ```
        """
        ...
    @ResModule.setter
    def ResModule(self, value: str): ...

    @property
    def Visible(self) -> bool:  # ipropertytab_visible.html
        """
        ## Visible - Видимость вкладки Панели свойств

        Тип данных: BOOL

        Свойство позволяет получить и изменить видимость вкладки Панели свойств.

        ### Синтаксис Automation

        ```
        Visible = iObject.Visible  # Получить свойство (* )
        iObject.Visible = Visible  # Установить свойство (* )
        Visible =iObject.Visible   # Получить свойство(**)
        iObject.SetVisible         # Установить свойство (**)
        ```
        """
        ...
    @Visible.setter
    def Visible(self, value: bool): ...




class IPropertyControl1(IKompasAPIObject):  # ipropertycontrol1.html
    """
    ## Интерфейс IPropertyControl1

    Дополнительный интерфейс элемента управления Панели свойств.

    Данный интерфейс можно получить у элемента управления Панели свойств IPropertyControl посредством вызова метода IUnknown::QueryInterface (const GUID far& IID, void** pif).
    """

    @property
    def HyperLinkNameStyle(self) -> bool:  # ipropertycontrol1_hyperlinknamestyle.html
        """
        ## HyperLinkNameStyle - Отображать имя контрола в виде ссылки

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        HyperLinkNameStyle = Object.HyperLinkNameStyle       # Получить свойство (* )
        Object.HyperLinkNameStyle = HyperLinkNameStyle       # Установить свойство (* )
        HyperLinkNameStyle = Object.GetHyperLinkNameStyle()  # Получить свойство(**)
        Object.SetHyperLinkNameStyle( HyperLinkNameStyle ))  # Установить свойство (**)
        ```
        """
        ...
    @HyperLinkNameStyle.setter
    def HyperLinkNameStyle(self, value: bool): ...

    @property
    def Image(self) -> typing.Any:  # ipropertycontrol1_image.html
        """
        ## Image - Дополнительный битмап контрол

        Функция не поддерживается

        Тип данных: VARIANT

        Значения свойства:

        - HINSTANCE (тип значения LONG_PTR).

        ### Синтаксис Automation

        ```
        Image = iObject.Image       # Получить свойство (* )
        iObject.Image = Image       # Установить свойство (* )
        Image = iObject.GetImage()  # Получить свойство(**)
        iObject.SetImage (Image)    # Установить свойство (**)
        ```
        """
        ...
    @Image.setter
    def Image(self, value: typing.Any): ...

    @property
    def NeedMouseEnterLeaveMessages(self) -> bool:  # ipropertycontrol1_needmouseenterleavemessages.html
        """
        ## NeedMouseEnterLeaveMessages - Необходимость получать сообщения о наведении/уходе курсора мыши на поле редактирования контрола

        Тип данных: BOOL

        Включение свойства позволит обрабатывать событие ksProcess2DNotify::GetMouseEnterLeavePoint.

        Это позволит подсвечивать точку в поле 2D документа, положение которой зависит от контрола под курсором.

        ### Синтаксис Automation

        ```
        NeedMouseEnterLeaveMessages = Object.NeedMouseEnterLeaveMessages       # Получить свойство(* )
        Object.NeedMouseEnterLeaveMessages = NeedMouseEnterLeaveMessages       # Установить свойство (* )
        NeedMouseEnterLeaveMessages = Object.GetNeedMouseEnterLeaveMessages()  # Получить свойство (**)
        Object.SetNeedMouseEnterLeaveMessages( NeedMouseEnterLeaveMessages )   # Установить свойство (**)
        ```
        """
        ...
    @NeedMouseEnterLeaveMessages.setter
    def NeedMouseEnterLeaveMessages(self, value: bool): ...

    @property
    def PredefineNumber(self) -> int:  # ipropertycontrol1_predefinenumber.html
        """
        ## PredefineNumber - Идентификатор предопределённого ввода

        Тип данных: long

        1. Позволяет получить и установить идентификатор предопределённого ввода для элементов управления, содержащих окно ввода, например, поле ввода IPropertyEdit.

        2. Значение по умолчанию = -1.

        3. Если идентификатор задан (положительное число), то он должен быть уникальным в пределах коллекции элементов управления конкретной вкладки.

        4. Работает только на запущенном процессе. При нажатии клавиш клавиатуры из коллекции выбирается элемент управления с наименьшим идентификатором предопределённого ввода (среди отображаемых) элементов управления и ему передается фокус. При подтверждении ввода ищется следующий индекс предопределённого ввода и запоминается для последующих нажатий клавиш. Если пользователь отказывается от ввода, например нажимает клавишу Esc, то индекс элемента управления предопределённого ввода, которому будут передаваться значения, не меняется.

        5. Задать значение свойства можно только до запуска процесса.

        ### Синтаксис Automation

        ```
        predefineNumber = iKompasObject.PredefineNumber       # Получить свойство (* )
        iKompasObject.PredefineNumber = predefineNumber       # Установить свойство (* )
        predefineNumber = iKompasObject.GetPredefineNumber()  # Получить свойство(**)
        iKompasObject.SetPredefineNumber( predefineNumber )   # Установить свойство (**)
        ```
        """
        ...
    @PredefineNumber.setter
    def PredefineNumber(self, value: int): ...

    @property
    def ResModule(self) -> typing.Any:  # ipropertycontrol1_resmodule.html
        """
        ## ResModule - Модуль с ресурсом битмапа

        Функция не поддерживается

        Тип данных: VARIANT

        Значения свойства:

        - HINSTANCE (тип значения LONG_PTR).

        Данное свойство позволяет установить или получить dll-модуль, в котором находится описание пользовательской спецпанели, рисунки для пользовательских кнопок.

        ### Синтаксис Automation

        ```
        ResModule = iObject.ResModule       # Получить свойство (* )
        iObject.ResModule = ResModule       # Установить свойство (* )
        ResModule = iObject.GetResModule()  # Получить свойство(**)
        iObject.SetResModule (ResModule)    # Установить свойство (**)
        ```
        """
        ...
    @ResModule.setter
    def ResModule(self, value: typing.Any): ...

    @property
    def UserMenu(self):  # ipropertycontrol1_usermenu.html
        """
        ## UserMenu - Дескриптор пользовательского меню

        Тип данных: OLE_HANDLE

        Значения свойства:

        Дескриптор пользовательского контекстного меню на элементе управления.

        Если на элементе управления уже было меню, то в случае задания оно заменяется на пользовательское. Дескриптор должен создаваться в библиотеке и быть валидным до конца процесса. Деструктурироваться дескриптор должен на стороне библиотеки по окончанию процесса. После того как меню вывелось и пользователь выбрал какой-то пункт генерируется новое событие - prUserMenuCommand.

        ### Синтаксис Automation

        ```
        UserMenu = iObject.UserMenu       # Получить свойство (* )
        iObject.UserMenu = UserMenu       # Установить свойство (* )
        UserMenu = iObject.GetUserMenu()  # Получить свойство(**)
        iObject.SetUserMenu (UserMenu)    # Установить свойство (**)
        ```
        """
        ...
    @UserMenu.setter
    def UserMenu(self, value): ...

    def AddAdditionButton(self, BtnID, Bmp, Tips, IconFont) -> bool:  # ipropertycontrol1_addadditionbutton.html
        """
        ## AddAdditionButton - Добавить дополнительную кнопку

        ### Синтаксис Automation

        ```
        BOOL AddAdditionButton( long BtnID, VARIANT Bmp, BSTR Tips, BSTR IconFont );
        ```

        ### Входные параметры

        +-------------------------------------------------------------------------------------+
        | BtnID    | - идентификатор кнопки,                                                  |
        +-------------------------------------------------------------------------------------+
        | Bmp      | - идентификатор иконки из шрифта или идентификатор картинки из ресурсов, |
        +-------------------------------------------------------------------------------------+
        | Tips     | - текст подсказки для кнопки,                                            |
        +-------------------------------------------------------------------------------------+
        | IconFont | - имя библиотечного шрифта.                                              |
        +-------------------------------------------------------------------------------------+

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def AddAdditionCheckButton(self, BtnID, BmpChecked, BmpUnChecked, BmpUndefine, Tips, IconFont) -> bool:  # ipropertycontrol1_addadditioncheckbutton.html
        """
        ## AddAdditionCheckButton - Добавить дополнительную кнопку (checkbox)

        ### Синтаксис Automation

        ```
        BOOL AddAdditionCheckButton( long BtnID, VARIANT BmpChecked, VARIANT BmpUnChecked, VARIANT BmpUndefine, BSTR Tips, BSTR IconFont );
        ```

        ### Входные параметры

        +-----------------------------------------------------------------------------------------------------------------------+
        | BtnID        | - идентификатор кнопки,                                                                                |
        +-----------------------------------------------------------------------------------------------------------------------+
        | BmpChecked   | - идентификатор иконки из шрифта или идентификатор картинки из ресурсов для нажатого состояния,        |
        +-----------------------------------------------------------------------------------------------------------------------+
        | BmpUnChecked | - идентификатор иконки из шрифта или идентификатор картинки из ресурсов для отжатого состояния         |
        +-----------------------------------------------------------------------------------------------------------------------+
        | BmpUndefine  | - идентификатор иконки из шрифта или идентификатор картинки из ресурсов для неопределенного состояния, |
        +-----------------------------------------------------------------------------------------------------------------------+
        | Tips         | - подсказка для кнопки,                                                                                |
        +-----------------------------------------------------------------------------------------------------------------------+
        | IconFont     | - имя шрифта.                                                                                          |
        +-----------------------------------------------------------------------------------------------------------------------+

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def AdditionButtonChecked(self, BtnID) -> bool:  # ipropertycontrol1_additionbuttonchecked.html
        """
        ## AdditionButtonChecked - Состояние дополнительной кнопки (нажата/отжата)

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        AdditionButtonChecked = Object.AdditionButtonChecked( BtnID )     # Получить свойство (* )
        Object.AdditionButtonChecked( BtnID ) = AdditionButtonChecked     # Установить свойство (* )
        AdditionButtonChecked = Object.GetAdditionButtonChecked( BtnID )  # Получить свойство(**)
        Object.SetAdditionButtonChecked( BtnID, AdditionButtonChecked )   # Установить свойство (**)
        ```

        ### Входные параметры

        +---------------------------------+
        | BtnID | - идентификатор кнопки. |
        +---------------------------------+
        """
        ...

    def AdditionButtonEnable(self, BtnID) -> bool:  # ipropertycontrol1_additionbuttonenable.html
        """
        ## AdditionButtonEnable - Состояние (доступность) дополнительной кнопки

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        AdditionButtonEnable = Object.AdditionButtonEnable( BtnID )     # Получить свойство (* )
        Object.AdditionButtonEnable( BtnID ) = AdditionButtonEnable     # Установить свойство (* )
        AdditionButtonEnable = Object.GetAdditionButtonEnable( BtnID )  # Получить свойство(**)
        Object.SetAdditionButtonEnable( BtnID, AdditionButtonEnable )   # Установить свойство (**)
        ```

        ### Входные параметры

        +---------------------------------+
        | BtnID | - идентификатор кнопки. |
        +---------------------------------+
        """
        ...

    def AdditionButtonNeedMouseEnterLeaveMessages(self, BtnID) -> bool:  # ipropertycontrol1_additionbuttonneedmouseenterleavemessages.html
        """
        ## AdditionButtonNeedMouseEnterLeaveMessages - Необходимость получать сообщения о наведении/уходе курсора мыши на дополнительных кнопках от данного контрола

        Тип данных: BOOL

        Включение свойства позволит обрабатывать событие ksProcess2DNotify::GetMouseEnterLeavePoint.

        Это позволит подсвечивать точку в поле 2D документа, положение которой зависит от контрола под курсором.

        ### Синтаксис Automation

        ```
        AdditionButtonNeedMouseEnterLeaveMessages = Object.AdditionButtonNeedMouseEnterLeaveMessages       # Получить свойство(* )
        Object.AdditionButtonNeedMouseEnterLeaveMessages = AdditionButtonNeedMouseEnterLeaveMessages       # Установить свойство (* )
        AdditionButtonNeedMouseEnterLeaveMessages = Object.GetAdditionButtonNeedMouseEnterLeaveMessages()  # Получить свойство (**)
        Object.SetAdditionButtonNeedMouseEnterLeaveMessages( AdditionButtonNeedMouseEnterLeaveMessages )   # Установить свойство (**)
        ```

        ### Входные параметры

        +-------------------------------------------------------+
        | long BtnID | - пользовательский идентификатор кнопки. |
        +-------------------------------------------------------+
        """
        ...

    def AdditionButtonVisible(self, BtnID) -> bool:  # ipropertycontrol1_additionbuttonvisible.html
        """
        ## AdditionButtonVisible - Видимость дополнительной кнопки

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        AdditionButtonVisible = Object.AdditionButtonVisible( BtnID )     # Получить свойство (* )
        Object.AdditionButtonVisible( BtnID ) = AdditionButtonVisible     # Установить свойство (* )
        AdditionButtonVisible = Object.GetAdditionButtonVisible( BtnID )  # Получить свойство(**)
        Object.SetAdditionButtonVisible( BtnID, AdditionButtonVisible )   # Установить свойство (**)
        ```

        ### Входные параметры

        +---------------------------------+
        | BtnID | - идентификатор кнопки. |
        +---------------------------------+
        """
        ...

    def GetHWND(self):  # ipropertycontrol1_gethwnd.html
        """
        ## GetHWND - Получить Handle окна

        Функция не поддерживается

        ### Синтаксис Automation

        ```
        OLE_HANDLE GetHWND();
        ```

        ### Возвращаемое значение

        - дескриптор окна.
        """
        ...

    def SetAdditionButtonChecked(self, BtnID, arg1):
        ...

    def SetAdditionButtonEnable(self, BtnID, arg1):
        ...

    def SetAdditionButtonNeedMouseEnterLeaveMessages(self, BtnID, arg1):
        ...

    def SetAdditionButtonVisible(self, BtnID, arg1):
        ...




class IPropertyPoint3D(IPropertyControl):  # ipropertypoint3d.html
    """
    ## Интерфейс IPropertyPoint3D

    Интерфейс элемента Панели свойствТочка 3D.

    1. Интерфейс позволяет добавить элемент Панели свойств - точка 3D.

    2. Данный интерфейс может быть получен от интерфейса коллекции элементов управления IPropertyControls с помощью свойства IPropertyControls::Item или метода IPropertyControls::Add с последующим приведением интерфейса IPropertyControl к данному интерфейсу с помощью метода IUnknown::QueryInterface.
    """

    @property
    def Point3DType(self) -> int:  # ipropertypoint3d_point3dtype.html
        """
        ## Point3DType - Тип системы координат точки

        Тип данных: из перечисления ksPoint3DTypeEnum

        ### Синтаксис Automation

        ```
        Point3DType = Object.Point3DType       # Получить свойство (* )
        Object.Point3DType = Point3DType       # Установить свойство (* )
        Point3DType = Object.GetPoint3DType()  # Получить свойство(**)
        Object.SetPoint3DType( Point3DType )   # Установить свойство (**)
        ```
        """
        ...
    @Point3DType.setter
    def Point3DType(self, value: int): ...

    def Coordinate(self, Index) -> IPropertyEdit:  # ipropertypoint3d_coordinate.html
        """
        ## Coordinate - Параметры координаты по индексу

        Тип данных: Указатель на интерфейс IPropertyEdit

        ### Синтаксис Automation

        ```
        Coordinate = Object.Coordinate( Index )     # Получить свойство (* )
        Coordinate = Object.GetCoordinate( Index )  # Получить свойство (**)
        ```

        ### Входные параметры

        +------------------------------+
        | Index | - индекс координаты. |
        +------------------------------+
        """
        ...

    def CoordinateState(self, Index) -> int:  # ipropertypoint3d_coordinatestate.html
        """
        ## CoordinateState - Состояние координаты по индексу

        Тип данных: из перечисления CheckStateEnum

        ### Синтаксис Automation

        ```
        CoordinateState = Object.CoordinateState( Index )     # Получить свойство (* )
        Object.CoordinateState( Index ) = CoordinateState     # Установить свойство (* )
        CoordinateState = Object.GetCoordinateState( Index )  # Получить свойство(**)
        Object.SetCoordinateState( Index, CoordinateState )   # Установить свойство (**)
        ```

        ### Входные параметры

        +------------------------------+
        | Index | - индекс координаты. |
        +------------------------------+
        """
        ...

    def CoordinateValue(self, Index) -> float:  # ipropertypoint3d_coordinatevalue.html
        """
        ## CoordinateValue - Значение координаты по индексу

        Тип данных: double

        ### Синтаксис Automation

        ```
        CoordinateValue = Object.CoordinateValue( Index )     # Получить свойство (* )
        Object.CoordinateValue( Index ) = CoordinateValue     # Установить свойство (* )
        CoordinateValue = Object.GetCoordinateValue( Index )  # Получить свойство(**)
        Object.SetCoordinateValue( Index, CoordinateValue )   # Установить свойство (**)
        ```

        ### Входные параметры

        +------------------------------+
        | Index | - индекс координаты. |
        +------------------------------+
        """
        ...

    def SetCoordinateState(self, Index, arg1):
        ...

    def SetCoordinateValue(self, Index, arg1):
        ...




class IPropertyEditCheckBox(IKompasAPIObject):  # ipropertyeditcheckbox.html
    """
    ## Интерфейс IPropertyEditCheckBox

    Интерфейс элемента управления Панели свойств типа «Поле c чекбоксом в виде картинки»
    """

    @property
    def CheckButton(self) -> bool:  # ipropertyeditcheckbox_checkbutton.html
        """
        ## CheckButton - Состояние кнопки

        Тип данных: BOOL

        Значения свойства:

        +----------------------+
        | TRUE  | - включено,  |
        +----------------------+
        | FALSE | - отключено. |
        +----------------------+

        ### Синтаксис Automation

        ```
        CheckButton = Object.CheckButton       # Получить свойство (* )
        Object.CheckButton = CheckButton       # Установить свойство (* )
        CheckButton = Object.GetCheckButton()  # Получить свойство(**)
        Object.SetCheckButton( CheckButton )   # Установить свойство (**)
        ```
        """
        ...
    @CheckButton.setter
    def CheckButton(self, value: bool): ...

    @property
    def DefaultValue(self) -> str:  # ipropertyeditcheckbox_defaultvalue.html
        """
        ## DefaultValue - Значение по умолчанию

        Тип данных: BSTR

        Свойство задает строку приглашения отображаемую в пустом регистраторе.

        ### Синтаксис Automation

        ```
        DefaultValue = Object.DefaultValue       # Получить свойство (* )
        Object.DefaultValue = DefaultValue       # Установить свойство (* )
        DefaultValue = Object.GetDefaultValue()  # Получить свойство(**)
        Object.SetDefaultValue( DefaultValue )   # Установить свойство (**)
        ```
        """
        ...
    @DefaultValue.setter
    def DefaultValue(self, value: str): ...

    @property
    def EnableCheckButton(self) -> bool:  # ipropertyeditcheckbox_enablecheckbutton.html
        """
        ## EnableCheckButton - Состояние доступности кнопки

        Тип данных: BOOL

        Значения свойства:

        +-----------------------+
        | TRUE  | - доступна,   |
        +-----------------------+
        | FALSE | - недоступна. |
        +-----------------------+

        ### Синтаксис Automation

        ```
        EnableCheckButton = Object.EnableCheckButton       # Получить свойство (* )
        Object.EnableCheckButton = EnableCheckButton       # Установить свойство (* )
        EnableCheckButton = Object.GetEnableCheckButton()  # Получить свойство(**)
        Object.SetEnableCheckButton( EnableCheckButton )   # Установить свойство (**)
        ```
        """
        ...
    @EnableCheckButton.setter
    def EnableCheckButton(self, value: bool): ...

    @property
    def EnableDeleteValue(self) -> bool:  # ipropertyeditcheckbox_enabledeletevalue.html
        """
        ## EnableDeleteValue - Признак возможности удаления значения

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        EnableDeleteValue = Object.EnableDeleteValue       # Получить свойство (* )
        Object.EnableDeleteValue = EnableDeleteValue       # Установить свойство (* )
        EnableDeleteValue = Object.GetEnableDeleteValue()  # Получить свойство(**)
        Object.SetEnableDeleteValue( EnableDeleteValue )   # Установить свойство (**)
        ```
        """
        ...
    @EnableDeleteValue.setter
    def EnableDeleteValue(self, value: bool): ...

    @property
    def IsLinkValue(self) -> bool:  # ipropertyeditcheckbox_islinkvalue.html
        """
        ## IsLinkValue - Признак ссылочного содержимого

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        IsLinkValue = Object.IsLinkValue       # Получить свойство (* )
        Object.IsLinkValue = IsLinkValue       # Установить свойство (* )
        IsLinkValue = Object.GetIsLinkValue()  # Получить свойство(**)
        Object.SetIsLinkValue( IsLinkValue )   # Установить свойство (**)
        ```
        """
        ...
    @IsLinkValue.setter
    def IsLinkValue(self, value: bool): ...

    @property
    def Missing(self) -> bool:  # ipropertyeditcheckbox_missing.html
        """
        ## Missing - Потерянный объект

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        Missing = Object.Missing       # Получить свойство (* )
        Object.Missing = Missing       # Установить свойство (* )
        Missing = Object.GetMissing()  # Получить свойство(**)
        Object.SetMissing(Missing )    # Установить свойство (**)
        ```
        """
        ...
    @Missing.setter
    def Missing(self, value: bool): ...

    def SetCustomBitmaps(self, IdUnchecked, IdChecked, IdIndeterminate, HInstance) -> bool:  # ipropertyeditcheckbox_setcustombitmaps.html
        """
        ## SetCustomBitmaps - Установить собственное изображение кнопки в разных состояниях

        1. Значки могут быть заданы в виде идентификаторов в ресурсе dll-модуля (тип данных long) или в виде полного пути к bmp-файлу значка (тип данных BSTR). Обязательным является задание значка для ненажатого состояния idUnchecked, остальные могут быть не заданы.

        2. Все значения переменных должны быть одного типа (или long, или BSTR).

        3. Если значки задаются в виде идентификатора, то обязательно должен быть задан dll-модуль, в ресурсах которого лежат эти значки. Модуль с ресурсами значков hInstance может быть задан в виде HINSTANCE (тип значения long) или полного пути к файлу (тип значения BSTR) dll-модуля.

        ### Синтаксис Automation

        ```
        void SetCustomBitmaps( VARIANT IdUnchecked,
        VARIANT IdChecked,
        VARIANT IdIndeterminate,
        VARIANT HInstance );
        ```

        ### Входные параметры

        +-------------------------------------------------------+
        | IdUnchecked     | - значок ненажатого состояния,      |
        +-------------------------------------------------------+
        | IdChecked       | - значок нажатого состояния,        |
        +-------------------------------------------------------+
        | IdIndeterminate | - значок неопределенного состояния, |
        +-------------------------------------------------------+
        | HInstance       | - dll-модуль с ресурсами значков.   |
        +-------------------------------------------------------+

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...




class IPropertyPreviewText(IPropertyControl):  # ipropertypreviewtext.html
    """
    ## Интерфейс IPropertyPreviewText

    Интерфейс элемента Панели свойств Предпросмотр текста.

    1. Интерфейс позволяет добавить элемент Панели свойств - Предпросмотр текста.

    2. Данный интерфейс может быть получен от интерфейса коллекции элементов управления IPropertyControls с помощью свойства IPropertyControls::Item или метода IPropertyControls::Add с последующим приведением интерфейса IPropertyControl к данному интерфейсу с помощью метода IUnknown::QueryInterface.
    """

    @property
    def DoubleSize(self) -> bool:  # ipropertypreviewtext_doublesize.html
        """
        ## DoubleSize - Двойной размер

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        DoubleSize = Object.DoubleSize       # Получить свойство (* )
        Object.DoubleSize = DoubleSize       # Установить свойство (* )
        DoubleSize = Object.GetDoubleSize()  # Получить свойство(**)
        Object.SetDoubleSize( DoubleSize )   # Установить свойство (**)
        ```
        """
        ...
    @DoubleSize.setter
    def DoubleSize(self, value: bool): ...

    @property
    def PreviewText(self) -> IText:  # ipropertypreviewtext_previewtext.html
        """
        ## PreviewText - Текст

        Тип данных: Указатель на интерфейс IText

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        PreviewText = Object.PreviewText       # Получить свойство (* )
        PreviewText = Object.GetPreviewText()  # Получить свойство (**)
        ```
        """
        ...

    def UpdateParam(self) -> bool:  # ipropertypreviewtext_updateparam.html
        """
        ## UpdateParam - Обновить параметры

        ### Синтаксис Automation

        ```
        BOOL UpdateParam();
        ```

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...




class IPropertyReplaceList(IPropertyControl):  # ipropertyreplacelist.html
    """
    ## Интерфейс IPropertyReplaceList

    Интерфейс элемента панели свойств, список замен текстов.

    Позволяет управлять списком замен текстов из Панели свойств.

    Данный интерфейс может быть получен от интерфейса коллекции элементов управления IPropertyControls с помощью свойства IPropertyControls::Item или метода IPropertyControls::Add. с последующим приведением интерфейса IPropertyControl к данному интерфейсу с помощью метода IUnknown::QueryInterface.

    Версия: Компас v20
    """

    def AddItem(self, Document, Text, FindText, StartIndex, ReadOnly) -> bool:  # ipropertyreplacelist_additem.html
        """
        ## AddItem - Добавить найденный фрагмент текста

        ### Синтаксис Automation

        ```
        BOOL AddItem( LPDISPATCH Document, BSTR Text, BSTR FindText, long StartIndex, BOOL ReadOnly );
        ```

        ### Входные параметры

        +----------------------------------------------------------------------+
        | Document   | - указатель на интерфейс окна документа IDocumentFrame, |
        +----------------------------------------------------------------------+
        | Text       | - полный текст,                                         |
        +----------------------------------------------------------------------+
        | FindText   | - найденный текст,                                      |
        +----------------------------------------------------------------------+
        | StartIndex | - позиция найденного текста в полном тексте,            |
        +----------------------------------------------------------------------+
        | ReadOnly   | - признак недоступного для изменения документа.         |
        +----------------------------------------------------------------------+

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def Clear(self):  # ipropertyreplacelist_clear.html
        """
        ## Clear - Очистить список

        ### Синтаксис Automation

        ```
        void Clear();
        ```
        """
        ...

    def GetSelection(self):  # ipropertyreplacelist_getselection.html
        """
        ## GetSelection - Индексы селектированных элементов

        ### Синтаксис Automation

        ```
        VARIANT GetSelection();
        ```

        ### Возвращаемое значение

        - список SafeArray индексов селектированных элементов.
        """
        ...




class IPropertyToolBar(IDispatch):  # ipropertytoolbar.html
    """
    ## Интерфейс IPropertyToolBar

    Интерфейс вспомогательного элемента Панели свойств. Набор команд элемента управления.

    Позволяет управлять командами элемента управления на Панели свойств.

    Данный интерфейс можно получить у интерфейсов IPropertyGrid или IPropertyEditList посредством вызова метода IUnknown::QueryInterface (const GUID far& iid, void** pif).
    """

    @property
    def ResModule(self) -> typing.Any:  # ipropertytoolbar_resmodule.html
        """
        ## ResModule - Модуль с ресурсами битмапов

        Тип данных: VARIANT.

        1. Значение свойства ResModule необходимо устанавливать до добавления кнопок.

        2. Модуль с ресурсами может быть задан строкой (имя файла ресурса) или значением типа long (идентификатор ресурса).

        ### Синтаксис Automation

        ```
        VARIANT ResModule = iObject.ResModule       # Получить свойство (* )
        iObject.ResModule = ResModule               # Установить свойство (* )
        VARIANT ResModule = iObject.GetResModule()  # Получить свойство(**)
        iObject.SetResModule (ResModule)            # Установить свойство (**)
        ```
        """
        ...
    @ResModule.setter
    def ResModule(self, value: typing.Any): ...

    def AddButton(self, BtnID, Bmp, InsertAt):  # ipropertytoolbar_addbutton.html
        """
        ## AddButton - Добавить кнопку

        ### Синтаксис Automation

        ```
        void AddButton( long BtnID, VARIANT Bmp, long InsertAt );
        ```

        ### Входные параметры

        +-----------------------------------------------------------------------+
        | BtnID    | - идентификатор кнопки,                                    |
        +-----------------------------------------------------------------------+
        | Bmp      | - строка (имя bmp-файла) или идентификатор битмапа (long), |
        +-----------------------------------------------------------------------+
        | InsertAt | - индекс кнопки.                                           |
        +-----------------------------------------------------------------------+
        """
        ...

    def ButtonChecked(self, BtnID) -> bool:  # ipropertytoolbar_buttonchecked.html
        """
        ## ButtonChecked - Нажатие кнопки

        Тип данных: BOOL.

        ### Синтаксис Automation

        ```
        BOOL ButtonChecked = iObject.ButtonChecked (BtnID)     # Получить свойство (* )
        BOOL ButtonChecked = iObject.GetButtonChecked (BtnID)  # Установить свойство (* )
        iObject.ButtonChecked (BtnID) = ButtonChecked          # Получить свойство(**)
        iObject.SetButtonChecked (BtnID, ButtonChecked)        # Установить свойство (**)
        ```

        ### Входные параметры

        +--------------------------------------+
        | Long BtnID | - идентификатор кнопки. |
        +--------------------------------------+
        """
        ...

    def ButtonEnable(self, BtnID) -> bool:  # ipropertytoolbar_buttonenable.html
        """
        ## ButtonEnable - Доступность кнопки

        Тип данных: BOOL.

        ### Синтаксис Automation

        ```
        BOOL ButtonEnable = iObject.ButtonEnable( BtnID )     # Получить свойство (* )
        BOOL ButtonEnable = iObject.GetButtonEnable( BtnID )  # Установить свойство (* )
        iObject.ButtonEnable( BtnID ) = ButtonEnable          # Получить свойство(**)
        iObject.SetButtonEnable( BtnID, ButtonEnable )        # Установить свойство (**)
        ```

        ### Входные параметры

        +--------------------------------------+
        | Long BtnID | - идентификатор кнопки. |
        +--------------------------------------+
        """
        ...

    def ButtonHint(self, BtnID) -> str:  # ipropertytoolbar_buttonhint.html
        """
        ## ButtonHint - Строка подсказки для кнопки, отображаемая в Строке сообщений

        Тип данных: BSTR (строка).

        ### Синтаксис Automation

        ```
        ButtonHint = iObject.ButtonHint (BtnID)     # Получить свойство (* )
        ButtonHint = iObject.GetButtonHint (BtnID)  # Установить свойство (* )
        iObject.ButtonHint( BtnID ) = ButtonHint    # Получить свойство(**)
        iObject.SetButtonHint (BtnID, ButtonHint)   # Установить свойство (**)
        ```

        ### Входные параметры

        +--------------------------------------+
        | Long BtnID | - идентификатор кнопки. |
        +--------------------------------------+
        """
        ...

    def ButtonIconFont(self, BtnID) -> str:  # ipropertytoolbar_buttoniconfont.html
        """
        ## ButtonIconFont - Шрифт для кнопок

        Тип данных: BSTR

        ### Синтаксис Automation

        ```
        ButtonIconFont = Object.ButtonIconFont( BtnID )     # Получить свойство (* )
        Object.ButtonIconFont( BtnID ) = ButtonIconFont     # Установить свойство (* )
        ButtonIconFont = Object.GetButtonIconFont( BtnID )  # Получить свойство(**)
        Object.SetButtonIconFont( BtnID, ButtonIconFont )   # Установить свойство (**)
        ```

        ### Входные параметры

        +---------------------------------+
        | BtnID | - идентификатор кнопки. |
        +---------------------------------+
        """
        ...

    def ButtonTips(self, BtnID) -> str:  # ipropertytoolbar_buttontips.html
        """
        ## ButtonTips - Строка контекстной всплывающей подсказки для кнопки

        Тип данных: BSTR (строка).

        ### Синтаксис Automation

        ```
        ButtonTips = iObject.ButtonTips (BtnID)     # Получить свойство (* )
        ButtonTips = iObject.GetButtonTips (BtnID)  # Установить свойство (* )
        iObject.ButtonTips (BtnID) = ButtonTips     # Получить свойство(**)
        iObject.SetButtonTips (BtnID, ButtonTips)   # Установить свойство (**)
        ```

        ### Входные параметры

        +--------------------------------------+
        | Long BtnID | - идентификатор кнопки. |
        +--------------------------------------+
        """
        ...

    def ButtonType(self, BtnID) -> int:  # ipropertytoolbar_buttontype.html
        """
        ## ButtonType - Тип кнопки

        Тип данных: из перечисления ButtonTypeEnum

        ### Синтаксис Automation

        ```
        ButtonType = Object.ButtonType       # Получить свойство (* )
        Object.ButtonType = ButtonType       # Установить свойство (* )
        ButtonType = Object.GetButtonType()  # Получить свойство(**)
        Object.SetButtonType( ButtonType )   # Установить свойство (**)
        ```
        """
        ...

    def ButtonVisible(self, BtnID) -> bool:  # ipropertytoolbar_buttonvisible.html
        """
        ## ButtonVisible - Отображение кнопки

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        ButtonVisible = Object.ButtonVisible( BtnID )     # Получить свойство (* )
        Object.ButtonVisible( BtnID ) = ButtonVisible     # Установить свойство (* )
        ButtonVisible = Object.GetButtonVisible( BtnID )  # Получить свойство(**)
        Object.SetButtonVisible( BtnID, ButtonVisible )   # Установить свойство (**)
        ```

        ### Входные параметры

        +---------------------------------+
        | BtnID | - идентификатор кнопки. |
        +---------------------------------+
        """
        ...

    def SetButtonChecked(self, BtnID, arg1):
        ...

    def SetButtonEnable(self, BtnID, arg1):
        ...

    def SetButtonHint(self, BtnID, arg1):
        ...

    def SetButtonIconFont(self, BtnID, arg1):
        ...

    def SetButtonTips(self, BtnID, arg1):
        ...

    def SetButtonType(self, BtnID, arg1):
        ...

    def SetButtonVisible(self, BtnID, arg1):
        ...




class IPropertyAggregateControl(IPropertyControl):  # ipropertyaggregatecontrol.html
    """
    ## Интерфейс IPropertyAggregateControl

    Интерфейс составного контрола.

    1. Интерфейс позволяет добавить элемент Панели свойств - составной контрол.

    2. Данный интерфейс может быть получен от интерфейса коллекции элементов управления IPropertyControls с помощью свойства IPropertyControls::Item или метода IPropertyControls::Add с последующим приведением интерфейса IPropertyControl к данному интерфейсу с помощью метода IUnknown::QueryInterface.
    """

    def Add(self, Control) -> bool:  # ipropertyaggregatecontrol_add.html
        """
        ## Add - Добавить контрол в группу

        ### Синтаксис Automation

        ```
        BOOL Add( IPropertyControl * Control );
        ```

        ### Входные параметры

        +-----------------------------------------------------------+
        | Control | - указатель на интерфейс добавляемого контрола. |
        +-----------------------------------------------------------+

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def PropertyControl(self, Index) -> IPropertyControl:  # ipropertyaggregatecontrol_propertycontrol.html
        """
        ## PropertyControl - Получить контрол по индексу

        Тип данных: Указатель на интерфейс IPropertyControl

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        PropertyControl = Object.PropertyControl( Index )     # Получить свойство (* )
        PropertyControl = Object.GetPropertyControl( Index )  # Получить свойство(**)
        ```
        """
        ...




class IPropertyControls(IKompasCollection):  # ipropertycontrols.html
    """
    ## Интерфейс IPropertyControls

    Интерфейс коллекции элементов управления.

    1. Интерфейс позволяет управлять коллекцией элементов управления.

    2. Данный интерфейс может быть получен от интерфейса закладки панели свойств IPropertyTab с помощью свойства IPropertyTab::PropertyControls.
    """

    def Add(self, Type) -> IPropertyControl:  # ipropertycontrols_add.html
        """
        ## Add - Создать элемент управления

        Добавляет элемент управления в коллекцию.

        ### Синтаксис Automation

        ```
        LPDISPATCH Add (ControlTypeEnum type);
        ```

        ### Входные параметры

        +------------------------------------------------------+
        | type | - тип элемента управления из ControlTypeEnum. |
        +------------------------------------------------------+

        ### Возвращаемое значение

        Указатель на интерфейс IPropertyControl
        """
        ...

    def Delete(self, Index):  # ipropertycontrols_delete.html
        """
        ## Delete - Удалить элемент управления

        Метод позволяет удалить элемент управления, заданный по индексу.

        ### Синтаксис Automation

        ```
        BOOL Delete (VARIANT Index);
        ```

        ### Входные параметры

        +-------------------------------------------------------------------+
        | index | - индекс элемента управления или имя элемента управления. |
        +-------------------------------------------------------------------+

        ### Возвращаемое значение

        - результат выполнения метода.
        """
        ...

    def Item(self, Index) -> IPropertyControl:  # ipropertycontrols_item.html
        """
        ## Item - Элемент управления, заданный по индексу

        Тип данных: указатель на интерфейс IPropertyControl.

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        Item = iObject.Item (Index)     # Получить свойство (* )
        Item = iObject.GetItem (Index)  # Получить свойство(**)
        ```

        ### Входные параметры

        +---------------------------------------+
        | index | - индекс элемента управления. |
        +---------------------------------------+
        """
        ...




class IPropertyTabs(IKompasCollection):  # ipropertytabs.html
    """
    ## Интерфейс IPropertyTabs

    Интерфейс коллекции вкладок Панели свойств.

    Данный интерфейс может быть получен от интерфейса параметров процесса IProcessParam с помощью свойства IProcessParam::PropertyTabs или от интерфейса панели свойств IPropertyManager с помощью свойства IPropertyManager::PropertyTabs.
    """

    @property
    def Active(self) -> IPropertyTab:  # ipropertytabs_active.html
        """
        ## Active - Интерфейс текущей вкладки Панели свойств

        Функция не поддерживается

        Тип данных: указатель на интерфейс IPropertyTab.

        ### Синтаксис Automation

        ```
        Active = iObject.Active       # Получить свойство (* )
        Active = iObject.GetActive()  # Получить свойство(**)
        ```
        """
        ...

    @property
    def SystemTab(self) -> IPropertyTab:  # ipropertytabs_systemtab.html
        """
        ## SystemTab - Интерфейс системной вкладки

        Интерфейс системной вкладки.

        Тип данных: указатель на интерфейс IPropertyTab.

        1. Свойство доступно только для чтения.

        2. Системную вкладку можно получить для процессных параметров. Для системной вкладки можно установить свойства:

        •Visible - видимость закладки Панели свойств, т.е. сделать закладку невидимой. Caption- изменить заголовок вкладки Панели свойств.

        •Caption- заголовок вкладки Панели свойств.

        •Image - установить битмап на вкладке.

        •ResModule - модуль с ресурсом битмапа, если битмап задан в виде идентификатора ресурса библиотеки.

        ### Синтаксис Automation

        ```
        SystemTab = iObject.SystemTab       # Получить свойство (* )
        SystemTab = iObject.GetSystemTab()  # Получить свойство(**)
        ```
        """
        ...

    def Add(self, Caption) -> IPropertyTab:  # ipropertytabs_add.html
        """
        ## Add - Создать вкладку Панели свойств (добавляет вкладку в коллекцию)

        ### Синтаксис Automation

        ```
        LPDISPATCH Add (BSTR caption);
        ```

        ### Входные параметры

        +---------------------------+
        | caption | - имя закладки. |
        +---------------------------+

        ### Возвращаемое значение

        - указатель на интерфейс IPropertyTab.
        """
        ...

    def Delete(self, Index):  # ipropertytabs_delete.html
        """
        ## Delete - Удалить вкладку Панели свойств, заданную по индексу или по имени

        ### Синтаксис Automation

        ```
        BOOL Delete (VARIANT index);
        ```

        ### Входные параметры

        +------------------------------------+
        | Index | - индекс или имя закладки. |
        +------------------------------------+

        ### Возвращаемое значение

        - результат выполнения метода.
        """
        ...

    def Item(self, Index) -> IPropertyTab:  # ipropertytabs_item.html
        """
        ## Item - Вкладка, заданная по индексу или по имени

        Тип данных: указатель на интерфейс IPropertyTab.

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        Item = iObject.Item       # Получить свойство (* )
        Item = iObject.GetItem()  # Получить свойство(**)
        ```

        ### Входные параметры

        +--------------------------------------------------+
        | index | - индекс или имя закладки (тип VARIANT). |
        +--------------------------------------------------+
        """
        ...




class IContextPanel(IDispatch):  # icontextpanel.html
    """
    ## Интерфейс IContextPanel

    Интерфейс всплывающей контекстной панели.

    Интерфейс используется для создания всплывающей панели для библиотечных элементов.

    Для использования всплывающих панелей в библиотеке требуется реализовать функцию:

    int WINAPI FillContextPanel( LPUNKNOWN contextPanel );

    Версия: Компас v20
    """

    def Fill(self, ContextPanelID) -> bool:  # icontextpanel_fill.html
        """
        ## Fill - Наполнить контекстную панель по описанию в манифесте библиотеки

        ### Синтаксис Automation

        ```
        BOOL Fill( BSTR ContextPanelID );
        ```

        ### Входные параметры

        +---------------------------------------------------------------------------+
        | ContextPanelID | - Строковый идентификатор панели в манифесте библиотеки. |
        +---------------------------------------------------------------------------+

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def FillComboBoxImage(self, ComboBoxID, CommandsGroupID, FilterCommands, CurrentCommand) -> bool:  # icontextpanel_fillcomboboximage.html
        """
        ## FillComboBoxImage - Заполнить комбобокс иконок по идентификатору группы команд в манифесте

        Если в комбобокс требуется добавить только часть команд из группы, можно задать список SafeArray VT_ARRAY | VT_I4 команд, которые требуется добавить в список.

        ### Синтаксис Automation

        ```
        BOOL FillComboBoxImage( BSTR ComboBoxID, BSTR CommandsGroupID, VARIANT FilterCommands, long CurrentCommand );
        ```

        ### Входные параметры

        +----------------------------------------------------------------------------+
        | ComboBoxID      | - строковый идентификатор комбобокса,                    |
        +----------------------------------------------------------------------------+
        | CommandsGroupID | - строковый идентификатор группы в манифесте библиотеки, |
        +----------------------------------------------------------------------------+
        | FilterCommands  | - фильтр команд,                                         |
        +----------------------------------------------------------------------------+
        | CurrentCommand  | - текущая команда.                                       |
        +----------------------------------------------------------------------------+

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def FillComboBoxStyle(self, ComboBoxID, StyleType, Styles, CurrentStyleId) -> bool:  # icontextpanel_fillcomboboxstyle.html
        """
        ## FillComboBoxStyle - Заполнить комбобокс стилей по идентификатору группы команд в манифесте

        ### Синтаксис Automation

        ```
        BOOL FillComboBoxStyle( BSTR ComboBoxID, ControlTypeEnum StyleType, VARIANT Styles, long CurrentStyleId );
        ```

        ### Входные параметры

        +--------------------------------------------------------+
        | ComboBoxID     | - строковый идентификатор комбобокса, |
        +--------------------------------------------------------+
        | StyleType      | - тип стилевого комбобокса,           |
        +--------------------------------------------------------+
        | Styles         | - список стилей,                      |
        +--------------------------------------------------------+
        | CurrentStyleId | - номер текущего стиля.               |
        +--------------------------------------------------------+

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...




class IProcessContextPanel(IDispatch):  # iprocesscontextpanel.html
    """
    ## Интерфейс IProcessContextPanel

    Интерфейс контекстной панели процесса.

    Интерфейс используется в событии ksPropertyManagerNotify::FillContextPanel

    Интерфейс позволяет заменить обычное меню процесса на панель которая может содержать контролы Панели свойств.

    Для использования контекстной панели в процессе нужно в событии ksPropertyManagerNotify::GetContextMenuType вернуть константу ksProcessContextPanel из перечисления ksProcessContextMenuType .
    """

    def AddAdditionalButtonsFromControl(self, PVal, Title) -> bool:  # iprocesscontextpanel_addadditionalbuttonsfromcontrol.html
        """
        ## AddAdditionalButtonsFromControl - Добавить группу дополнительных кнопок

        ### Синтаксис Automation

        ```
        BOOL AddAdditionalButtonsFromControl( IPropertyControl * PVal, BSTR Title );
        ```

        ### Входные параметры

        +---------------------------------------------------------+
        | PVal  | - указатель на интерфейс добавляемого контрола, |
        +---------------------------------------------------------+
        | Title | - заголовок.                                    |
        +---------------------------------------------------------+

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def AddControl(self, PVal) -> bool:  # iprocesscontextpanel_addcontrol.html
        """
        ## AddControl - Добавить контрол

        ### Синтаксис Automation

        ```
        BOOL AddControl( IPropertyControl * PVal );
        ```

        ### Входные параметры

        +--------------------------------------------------------+
        | PVal | - указатель на интерфейс добавляемого контрола. |
        +--------------------------------------------------------+

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def AddGeomCalculatorCommands(self, PVal) -> bool:  # iprocesscontextpanel_addgeomcalculatorcommands.html
        """
        ## AddGeomCalculatorCommands - Добавить команды меню геометрического калькулятора

        ### Синтаксис Automation

        ```
        BOOL AddGeomCalculatorCommands( IPropertyControl * PVal );
        ```

        ### Входные параметры

        +-------------------------------------------+
        | PVal | - указатель на интерфейс контрола. |
        +-------------------------------------------+

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def AddGroupBegin(self, Title) -> bool:  # iprocesscontextpanel_addgroupbegin.html
        """
        ## AddGroupBegin - Добавить начало подгруппы

        ### Синтаксис Automation

        ```
        BOOL AddGroupBegin( BSTR Title );
        ```

        ### Входные параметры

        +----------------------+
        | Title | - заголовок. |
        +----------------------+

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def AddGroupEnd(self) -> bool:  # iprocesscontextpanel_addgroupend.html
        """
        ## AddGroupEnd - Добавить конец подгруппы

        ### Синтаксис Automation

        ```
        BOOL AddGroupEnd();
        ```

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def AddLinkContentButton(self, PVal, Title) -> bool:  # iprocesscontextpanel_addlinkcontentbutton.html
        """
        ## AddLinkContentButton - Добавить кнопку ссылочного содержимого регистратора

        ### Синтаксис Automation

        ```
        BOOL AddLinkContentButton( IPropertyControl * PVal, BSTR Title );
        ```

        ### Входные параметры

        +---------------------------------------------------------+
        | PVal  | - указатель на интерфейс добавляемого контрола, |
        +---------------------------------------------------------+
        | Title | - заголовок.                                    |
        +---------------------------------------------------------+

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def AddNewSearchButton(self) -> bool:  # iprocesscontextpanel_addnewsearchbutton.html
        """
        ## AddNewSearchButton - Добавить кнопку перебор объектов

        ### Синтаксис Automation

        ```
        BOOL AddNewSearchButton();
        ```

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def AddSeparator(self) -> bool:  # iprocesscontextpanel_addseparator.html
        """
        ## AddSeparator - Добавить разделитель

        ### Синтаксис Automation

        ```
        BOOL AddSeparator();
        ```

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def AddSnapCommands(self) -> bool:  # iprocesscontextpanel_addsnapcommands.html
        """
        ## AddSnapCommands - Добавить команды привязок

        ### Синтаксис Automation

        ```
        BOOL AddSnapCommands();
        ```

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...




class IProcessContextIconMenu(IDispatch):  # iprocesscontexticonmenu.html
    """
    ## Интерфейс IProcessContextIconMenu

    Интерфейс контекстного меню процесса с иконками.
    """

    def AddMenuCommand(self, Id, Title, Icon, IconFont) -> bool:  # iprocesscontexticonmenu_addmenucommand.html
        """
        ## AddMenuCommand - Добавить пункт меню

        ### Синтаксис Automation

        ```
        BOOL AddMenuCommand( long Id, BSTR Title, long Icon, BSTR IconFont );
        ```

        ### Входные параметры

        +----------------------------------------------------------------------------+
        | Id       | - идентификатор команды,                                        |
        +----------------------------------------------------------------------------+
        | Title    | - заголовок,                                                    |
        +----------------------------------------------------------------------------+
        | Icon     | - идентификатор иконки из шрифта,                               |
        +----------------------------------------------------------------------------+
        | IconFont | - имя библиотечного шрифта или пустая строка для шрифта Компас. |
        +----------------------------------------------------------------------------+

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...




class IAttrTypeMng(IKompasAPIObject):  # iattrtypemng.html
    """
    ## Интерфейс IAttrTypeMng

    Менеджер типов атрибутов.

    1. Интерфейс позволяет создавать, получать и просматривать типы атрибутов.

    2. Интерфейс является дополнительным к интерфейсу приложения IApplication.
    """

    def ChoiceAttrTypes(self, HWnd, Libname) -> IAttributeType:  # iattrtypemng_choiceattrtypes.html
        """
        ## ChoiceAttrTypes - Открыть диалог для просмотра в библиотеке атрибутов списка типов атрибутов и выбора нужного типа

        Метод позволяет открыть диалог просмотра типов атрибутов в библиотеке.

        ### Синтаксис Automation

        ```
        LPDISPATCH ChoiceAttrTypes( OLE_HANDLE HWnd,
        VARIANT Libname );
        ```

        ### Входные параметры

        +-----------------------------------------------------------------------------------------------------------+
        | HWnd    | - дескриптор главного окна системы КОМПАС, который можно получить с помощью функции GetHWindow, |
        +-----------------------------------------------------------------------------------------------------------+
        | Libname | - имя библиотеки типов атрибутов.                                                               |
        +-----------------------------------------------------------------------------------------------------------+

        ### Возвращаемое значение

        Указатель на интерфейс выбранного в диалоге типа атрибута IAttributeType.
        """
        ...

    def CreateAttrType(self, Libname) -> IAttributeType:  # iattrtypemng_createattrtype.html
        """
        ## CreateAttrType - Создать тип атрибута в заданной библиотеке

        Метод позволяет создать тип атрибута в библиотеке Libname.

        ### Синтаксис Automation

        ```
        LPDISPATCH CreateAttrType( VARIANT Libname );
        ```

        ### Входные параметры

        +---------------------------------------------+
        | Libname | - имя библиотеки типов атрибутов. |
        +---------------------------------------------+

        ### Возвращаемое значение

        - указатель на интерфейс типа атрибута IAttributeType.
        """
        ...

    def GetAttrType(self, AttrID, Libname) -> IAttributeType:  # iattrtypemng_getattrtype.html
        """
        ## GetAttrType - Получить тип атрибута из заданной библиотеки

        Метод позволяет получить тип атрибута из библиотеки Libname.

        ### Синтаксис Automation

        ```
        LPDISPATCH GetAttrType( double AttrID,
        VARIANT Libname );
        ```

        ### Входные параметры

        +---------------------------------------------+
        | AttrID  | - уникальный номер типа атрибута, |
        +---------------------------------------------+
        | Libname | - имя библиотеки типов атрибутов. |
        +---------------------------------------------+

        ### Возвращаемое значение

        - указатель на интерфейс типа атрибута IAttributeType.
        """
        ...

    def GetAttrTypes(self, Libname) -> list[IDispatch]:  # iattrtypemng_getattrtypes.html
        """
        ## GetAttrTypes - Возвращает массив типов атрибутов, находящихся в заданной библиотеке типов

        Метод позволяет получить массив типов атрибутов из заданной библиотеки. Если возвращается один объект, то тип VARIANT-а будет VT_DISPATCH, если возвращается несколько объектов, то тип VARIANT-а будет VT_ARRAY | VT_DISPATCH.

        ### Синтаксис Automation

        ```
        VARIANT GetAttrTypes( VARIANT Libname );
        ```

        ### Входные параметры

        +---------------------------------------------+
        | Libname | - имя библиотеки типов атрибутов. |
        +---------------------------------------------+

        ### Возвращаемое значение

        - массив SAFEARRAY типа VT_ARRAY | VT_DISPATCH указателей на интерфейсы типов атрибутов, находящихся в заданной библиотеке.
        """
        ...




class IAttribute(IKompasAPIObject):  # iattribute.html
    """
    ## Интерфейс IAttribute

    Интерфейс атрибута.

    1. Интерфейс позволяет осуществлять работу с атрибутом.

    2. Интерфейс можно получить с помощью метода IKompasDocument1::CreateAttr.
    """

    @property
    def AttributeType(self) -> IAttributeType:  # iattribute_attributetype.html
        """
        ## AttributeType - Получить тип атрибута

        Тип данных: указатель на интерфейс типа атрибута IAttributeType.

        1. Свойство позволяет получить указатель на интерфейс типа атрибута.

        2. Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        AttributeType = iObject.AttributeType ( Index );    # Получить свойство (* )
        AttributeType = iObject.GetAttributeType( Index );  # Получить свойство (**)
        ```
        """
        ...

    @property
    def ColumnKey(self) -> list[int]:  # iattribute_columnkey.html
        """
        ## ColumnKey - Получить ключи колонок

        Тип данных: VARIANT типа VT_ARRAY | VT_I4

        1. Свойство позволяет получать значения ключей колонок атрибута.

        2. Для задания значений ключей колонок следует использовать метод IAttribute::SetColumnKey.

        3. Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        ColumnKey = Object.ColumnKey       # Получить свойство (* )
        ColumnKey = Object.GetColumnKey()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def ColumnsCount(self) -> int:  # iattribute_columnscount.html
        """
        ## ColumnsCount - Получить количество столбцов в таблице атрибута

        Тип данных: long

        1. Свойство позволяет получить количество столбцов в таблице атрибута.

        2. Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        ColumnsCount = Object.ColumnsCount       # Получить свойство (* )
        ColumnsCount = Object.GetColumnsCount()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def FlagVisible(self) -> list[bool]:  # iattribute_flagvisible.html
        """
        ## FlagVisible - Получить значения видимости колонок в виде массива SAFEARRAY VT_ARRAY | VT_BOOL

        Тип данных: VARIANT типа VT_ARRAY | VT_BOOL

        1. Свойство позволяет получать значения видимости колонок атрибута.

        2. Для задания значений видимости колонок следует использовать метод IAttribute::SetFlagVisible.

        3. Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        FlagVisible = Object.FlagVisible       # Получить свойство (* )
        FlagVisible = Object.GetFlagVisible()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def Objects(self) -> list[IDispatch]:  # iattribute_objects.html
        """
        ## Objects - Объекты, прикрепленные к атрибуту в виде массива SAFEARRAY DISPATCH - VT_ARRAY | VT_DISPATCH

        Тип данных:VARIANT типа VT_ARRAY | VT_DISPATCH

        Свойство позволяет устанавливать и получать массив объектов, прикрепленных к атрибуту.

        ### Синтаксис Automation

        ```
        Objects = Object.Objects       # Получить свойство (* )
        Object.Objects = Objects       # Установить свойство (* )
        Objects = Object.GetObjects()  # Получить свойство (**)
        Object.SetObjects( Objects )   # Установить свойство (**)
        ```
        """
        ...
    @Objects.setter
    def Objects(self, value: list[IDispatch]): ...

    @property
    def RowsCount(self) -> int:  # iattribute_rowscount.html
        """
        ## RowsCount - Получить количество строк в таблице атрибута

        Тип данных: long

        1. Свойство позволяет получить количество строк в таблице атрибута.

        2. Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        RowsCount = Object.RowsCount       # Получить свойство (* )
        RowsCount = Object.GetRowsCount()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def Values(self) -> list[typing.Any]:  # iattribute_values.html
        """
        ## Values - Получить значения всех ячеек из таблицы атрибута

        Тип данных: VARIANT типа VT_ARRAY | VT_VARIANT

        1. Свойство позволяет значения всех ячеек из таблицы атрибута.

        2. Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        Values = Object.Values       # Получить свойство (* )
        Values = Object.GetValues()  # Получить свойство (**)
        ```
        """
        ...

    def AddRow(self, Password, RowNumb) -> bool:  # iattribute_addrow.html
        """
        ## AddRow - Добавить строку к табличному атрибуту неопределенной длины

        Метод позволяет добавить строку в таблицу атрибута.

        ### Синтаксис Automation

        ```
        BOOL AddRow( BSTR Password,
        long RowNumb );
        ```

        ### Входные параметры

        +-----------------------------------------------------------------+
        | Password | - пароль,                                            |
        +-----------------------------------------------------------------+
        | RowNumb  | - номер строки, после которой нужно вставить строку. |
        +-----------------------------------------------------------------+

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def Delete(self, Password, Objects) -> bool:  # iattribute_delete.html
        """
        ## Delete - Удалить атрибут

        Метод позволяет удалить атрибут у объектов.

        ### Синтаксис Automation

        ```
        BOOL Delete( BSTR Password,
        VARIANT Objects );
        ```

        ### Входные параметры

        +-----------------------------------------------------------------------------------------------------+
        | Password | - пароль,                                                                                |
        +-----------------------------------------------------------------------------------------------------+
        | Objects  | - массив SAFEARRAY типа VT_ARRAY | VT_DISPATCH объектов, у которых надо удалить атрибут. |
        +-----------------------------------------------------------------------------------------------------+

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def DeleteRow(self, Password, RowNumb) -> bool:  # iattribute_deleterow.html
        """
        ## DeleteRow - Удалить строку табличного атрибута неопределенной длины

        Метод позволяет удалить строку из таблицы атрибута.

        ### Синтаксис Automation

        ```
        BOOL DeleteRow( BSTR Password,
        long RowNumb );
        ```

        ### Входные параметры

        +--------------------------------------+
        | Password | - пароль,                 |
        +--------------------------------------+
        | RowNumb  | - номер удаляемой строки. |
        +--------------------------------------+

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def GetKeysInfo(self) -> bool:  # iattribute_getkeysinfo.html
        """
        ## GetKeysInfo - Выдать информацию о ключах атрибута

        Метод позволяет получить информацию о ключах атрибута.

        ### Синтаксис Automation

        ```
        BOOL GetKeysInfo( long* Key1,
        long* Key2,
        long* Key3,
        long* Key4,
        double* Numb );
        ```

        ### Выходные параметры

        +-------------------------------------------------------+
        | Key1, Key2, Key3, Key4 | - ключи атрибута,            |
        +-------------------------------------------------------+
        | Numb                   | - уникальный номер атрибута. |
        +-------------------------------------------------------+

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def RecordFlagVisible(self, ColumnNumb) -> list[bool]:  # iattribute_recordflagvisible.html
        """
        ## RecordFlagVisible - Получить значения видимости колонок внутри записи в виде массива SAFEARRAY VT_ARRAY | VT_BOOL

        Тип данных: VARIANT типа VT_ARRAY | VT_BOOL

        1. Свойство позволяет получить значения видимости колонок внутри записи.

        2. Установить значения видимости колонок записи можно с помощью метода IAttribute::SetRecordFlagVisible.

        3. Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        RecordFlagVisible = Object.RecordFlagVisible( ColumnNumb )     # Получить свойство (* )
        RecordFlagVisible = Object.GetRecordFlagVisible( ColumnNumb )  # Получить свойство (**)
        ```

        ### Входные параметры

        +--------------------------------------+
        | ColumnNumb | - номер колонки записи. |
        +--------------------------------------+
        """
        ...

    def SetColumnKey(self, Password, Keys) -> bool:  # iattribute_setcolumnkey.html
        """
        ## SetColumnKey - Установить ключи колонок

        Метод позволяет установить ключи колонок атрибута.

        ### Синтаксис Automation

        ```
        BOOL SetColumnKey( BSTR Password,
        VARIANT Keys );
        ```

        ### Входные параметры

        +---------------------------------------------------------------------------------------+
        | Password | - пароль,                                                                  |
        +---------------------------------------------------------------------------------------+
        | Keys     | - массив SAFEARRAY типа VT_ARRAY | VT_I4 значений ключей колонок атрибута. |
        +---------------------------------------------------------------------------------------+

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def SetFlagVisible(self, Password, Values) -> bool:  # iattribute_setflagvisible.html
        """
        ## SetFlagVisible - Установить значения видимости колонок в виде массива SAFEARRAY VT_ARRAY | VT_BOOL

        Метод позволяет установить значения видимости колонок атрибута.

        ### Синтаксис Automation

        ```
        BOOL SetFlagVisible( BSTR Password,
        VARIANT Values );
        ```

        ### Входные параметры

        +--------------------------------------------------------------------------------------------+
        | Password | - пароль,                                                                       |
        +--------------------------------------------------------------------------------------------+
        | Values   | - массив SAFEARRAY типа VT_ARRAY | VT_BOOL значений видимости колонок атрибута. |
        +--------------------------------------------------------------------------------------------+

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def SetKeysInfo(self, Password, Key1, Key2, Key3, Key4) -> bool:  # iattribute_setkeysinfo.html
        """
        ## SetKeysInfo - Установить информацию о ключах атрибута

        Метод позволяет задать ключи атрибута.

        ### Синтаксис Automation

        ```
        BOOL SetKeysInfo( BSTR Password,
        long Key1,
        long Key2,
        long Key3,
        long Key4 );
        ```

        ### Входные параметры

        +--------------------------------------------+
        | Password               | - пароль,         |
        +--------------------------------------------+
        | Key1, Key2, Key3, Key4 | - ключи атрибута. |
        +--------------------------------------------+

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def SetPassword(self, OldPassword, NewPassword) -> bool:  # iattribute_setpassword.html
        """
        ## SetPassword - Установить новый пароль на изменение данных атрибута

        Метод позволяет установить новый пароль на изменение данных атрибута.

        ### Синтаксис Automation

        ```
        BOOL SetPassword( BSTR OldPassword,
        BSTR NewPassword );
        ```

        ### Входные параметры

        +--------------------------------+
        | OldPassword | - старый пароль, |
        +--------------------------------+
        | NewPassword | - новый пароль.  |
        +--------------------------------+

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def SetRecordFlagVisible(self, Password, ColumnNumb, Values) -> bool:  # iattribute_setrecordflagvisible.html
        """
        ## SetRecordFlagVisible - Установить значения видимости колонок внутри записи в виде массива SAFEARRAY VT_ARRAY | VT_BOOL

        Метод позволяет установить значения видимости колонок записи.

        ### Синтаксис Automation

        ```
        BOOL SetRecordFlagVisible( BSTR Password,
        long ColumnNumb,
        VARIANT Values );
        ```

        ### Входные параметры

        +--------------------------------------------------------------------------------------------+
        | Password   | - пароль,                                                                     |
        +--------------------------------------------------------------------------------------------+
        | ColumnNumb | - номер колонки записи,                                                       |
        +--------------------------------------------------------------------------------------------+
        | Values     | - массив SAFEARRAY типа VT_ARRAY | VT_BOOL значений видимости колонок записи. |
        +--------------------------------------------------------------------------------------------+

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def SetValue(self, Password, RowNumb, ColumnNumb, Value) -> bool:  # iattribute_setvalue.html
        """
        ## SetValue - Установить значение ячейки

        Метод позволяет установить значение ячейки атрибута.

        ### Синтаксис Automation

        ```
        BOOL SetValue( BSTR Password,
        long RowNumb,
        long ColumnNumb,
        VARIANT Value );
        ```

        ### Входные параметры

        +------------------------------------------+
        | Password   | - пароль,                   |
        +------------------------------------------+
        | RowNumb    | - номер строки атрибута,    |
        +------------------------------------------+
        | ColumnNumb | - номер столбца атрибута,   |
        +------------------------------------------+
        | Value      | - значение ячейки атрибута. |
        +------------------------------------------+

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def SetValues(self, Password, Values) -> bool:  # iattribute_setvalues.html
        """
        ## SetValues - Установить значения всех ячеек таблицы атрибута

        Метод позволяет установить значения всех ячеек таблицы атрибута.

        ### Синтаксис Automation

        ```
        BOOL SetValues( BSTR Password,
        VARIANT Values );
        ```

        ### Входные параметры

        +-----------------------------------------------------------------------------------+
        | Password | - пароль,                                                              |
        +-----------------------------------------------------------------------------------+
        | Values   | - массив SAFEARRAY типа VT_ARRAY | VT_VARIANT значений ячеек атрибута. |
        +-----------------------------------------------------------------------------------+

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def Value(self, RowNumb, ColumnNumb) -> typing.Any:  # iattribute_value.html
        """
        ## Value - Значение ячейки из таблицы атрибута

        Тип данных: VARIANT

        1. Свойство позволяет получать значение ячейки атрибута.

        2. Для задания значения ячейки следует использовать метод IAttribute::SetValue.

        3. Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        Value = Object.Value( RowNumb, ColumnNumb )     # Получить свойство (* )
        Value = Object.GetValue( RowNumb, ColumnNumb )  # Получить свойство (**)
        ```

        ### Входные параметры

        +-----------------------------------------------+
        | RowNumb (long)    | - номер строки атрибута,  |
        +-----------------------------------------------+
        | ColumnNumb (long) | - номер столбца атрибута. |
        +-----------------------------------------------+
        """
        ...

    def ViewEdit(self, Parent, Password, ReadOnly) -> bool:  # iattribute_viewedit.html
        """
        ## ViewEdit - Открыть диалог для просмотра и редактирования атрибута

        Метод позволяет открыть диалог для просмотра и редактирования атрибута.

        ### Синтаксис Automation

        ```
        BOOL ViewEdit( OLE_HANDLE Parent,
        BSTR Password,
        BOOL ReadOnly );
        ```

        ### Выходные параметры

        +------------------------------------------------------------------------------------------------------------+
        | Parent   | - дескриптор главного окна системы КОМПАС, который можно получить с помощью функции GetHWindow, |
        +------------------------------------------------------------------------------------------------------------+
        | Password | - пароль,                                                                                       |
        +------------------------------------------------------------------------------------------------------------+
        | ReadOnly | - TRUE - открыть диалог для просмотра - FALSE - открыть диалог для редактирования.              |
        +------------------------------------------------------------------------------------------------------------+

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...




class IAttributeType(IKompasAPIObject):  # iattributetype.html
    """
    ## Интерфейс IAttributeType

    Интерфейс параметров типа атрибута.

    1. Интерфейс позволяет задавать параметры типа атрибута.

    2. Интерфейс можно получить следующими способами:

    •у менеджера типа атрибутов с помощью методов IAttrTypeMng::CreateAttrType, IAttrTypeMng::GetAttrType, IAttrTypeMng::ChoiceAttrTypes,

    •у атрибута с помощью свойства IAttribute::AttributeType.
    """

    @property
    def AttrType(self) -> int:  # iattributetype_attrtype.html
        """
        ## AttrType - Тип данных

        Тип данных: из перечисления ksAttributeTypeEnum

        Свойство позволяет устанавливать и получать тип данных для типа атрибута.

        ### Синтаксис Automation

        ```
        AttrType = Object.AttrType       # Получить свойство (* )
        Object.AttrType = AttrType       # Установить свойство (* )
        AttrType = Object.GetAttrType()  # Получить свойство (**)
        Object.SetAttrType( AttrType )   # Установить свойство (**)
        ```
        """
        ...
    @AttrType.setter
    def AttrType(self, value: int): ...

    @property
    def ColumnsCount(self) -> int:  # iattributetype_columnscount.html
        """
        ## ColumnsCount - Получить количество колонок табличного атрибута

        Тип данных: long

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        ColumnsCount = Object.ColumnsCount       # Получить свойство (* )
        ColumnsCount = Object.GetColumnsCount()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def FileName(self) -> str:  # iattributetype_filename.html
        """
        ## FileName - Имя библиотеки типов атрибутов

        Тип данных: BSTR

        1. Свойство позволяет получить имя библиотеки типов атрибутов.

        2. Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        FileName = Object.FileName       # Получить свойство (* )
        FileName = Object.GetFileName()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def RowsCount(self) -> int:  # iattributetype_rowscount.html
        """
        ## RowsCount - Количество строк в таблице

        Тип данных: long

        Свойство позволяет устанавливать и получать количество строк в таблице.

        ### Синтаксис Automation

        ```
        RowsCount = Object.RowsCount       # Получить свойство (* )
        Object.RowsCount = RowsCount       # Установить свойство (* )
        RowsCount = Object.GetRowsCount()  # Получить свойство (**)
        Object.SetRowsCount( RowsCount )   # Установить свойство (**)
        ```
        """
        ...
    @RowsCount.setter
    def RowsCount(self, value: int): ...

    @property
    def TypeName(self) -> str:  # iattributetype_typename.html
        """
        ## TypeName - Название типа атрибута

        Тип данных: BSTR

        Свойство позволяет устанавливать и получать название типа атрибута.

        ### Синтаксис Automation

        ```
        TypeName = Object.TypeName       # Получить свойство (* )
        Object.TypeName = TypeName       # Установить свойство (* )
        TypeName = Object.GetTypeName()  # Получить свойство (**)
        Object.SetTypeName( TypeName )   # Установить свойство (**)
        ```
        """
        ...
    @TypeName.setter
    def TypeName(self, value: str): ...

    @property
    def UniqueNumb(self) -> float:  # iattributetype_uniquenumb.html
        """
        ## UniqueNumb - Уникальный номер типа

        Тип данных: double.

        1. Свойство позволяет получать уникальный номер типа атрибута.

        2. Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        UniqueNumb = Object.UniqueNumb       # Получить свойство (* )
        UniqueNumb = Object.GetUniqueNumb()  # Получить свойство (**)
        ```
        """
        ...

    def AddColumn(self, Index, Type, Name) -> IColumnInfo:  # iattributetype_addcolumn.html
        """
        ## AddColumn - Добавить колонку

        Метод позволяет добавить колонку к типу атрибута.

        ### Синтаксис Automation

        ```
        LPDISPATCH AddColumn( long Index,
        long Type,
        BSTR Name );
        ```

        ### Входные параметры

        +-----------------------------------------------------------------+
        | Index | - индекс колонки, после которой нужно добавить колонку, |
        +-----------------------------------------------------------------+
        | Type  | - тип значений колонки,                                 |
        +-----------------------------------------------------------------+
        | Name  | - название колонки.                                     |
        +-----------------------------------------------------------------+

        ### Возвращаемое значение

        Указатель на интерфейс IColumnInfo.
        """
        ...

    def ColumnInfo(self, Index) -> IColumnInfo:  # iattributetype_columninfo.html
        """
        ## ColumnInfo - Получить информацию о столбце атрибута

        Тип данных: указатель на интерфейс параметров столбца атрибута IColumnInfo

        1. Свойство позволяет получать интерфейс параметров столбца атрибута.

        2. Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        ColumnInfo = Object.ColumnInfo ( Index )    # Получить свойство (* )
        ColumnInfo = Object.GetColumnInfo( Index )  # Получить свойство (**)
        ```

        ### Входные параметры

        +-------------------------------------+
        | Index (VARIANT) | - индекс столбца. |
        +-------------------------------------+
        """
        ...

    def Delete(self, Password) -> bool:  # iattributetype_delete.html
        """
        ## Delete - Удалить объект

        Метод позволяет удалить тип атрибута.

        ### Синтаксис Automation

        ```
        BOOL Delete (BSTR Password);
        ```

        ### Входные параметры

        +----------------------+
        | Password | - пароль. |
        +----------------------+

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def GetKeysInfo(self) -> bool:  # iattributetype_getkeysinfo.html
        """
        ## GetKeysInfo - Получить информацию о ключах атрибута

        Метод позволяет получить информацию о ключах атрибута.

        ### Синтаксис Automation

        ```
        BOOL GetKeysInfo( long* Key1,
        long* Key2,
        long* Key3,
        long* Key4);
        ```

        ### Выходные параметры

        +--------------------------------------------+
        | Key1, Key2, Key3, Key4 | - ключи атрибута. |
        +--------------------------------------------+

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def SetKeysInfo(self, Key1, Key2, Key3, Key4) -> bool:  # iattributetype_setkeysinfo.html
        """
        ## SetKeysInfo - Установить информацию о ключах атрибута

        Метод позволяет установить информацию о ключах атрибута.

        ### Синтаксис Automation

        ```
        BOOL SetKeysInfo( long Key1,
        long Key2,
        long Key3,
        long Key4 );
        ```

        ### Входные параметры

        +--------------------------------------------+
        | Key1, Key2, Key3, Key4 | - ключи атрибута. |
        +--------------------------------------------+

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def SetPassword(self, OldPassword, NewPassword) -> bool:  # iattributetype_setpassword.html
        """
        ## SetPassword - Установить новый пароль на изменение данных атрибута

        Метод позволяет установить новый пароль на изменение данных атрибута.

        ### Синтаксис Automation

        ```
        BOOL SetPassword( BSTR OldPassword,
        BSTR NewPassword );
        ```

        ### Входные параметры

        +--------------------------------+
        | OldPassword | - старый пароль, |
        +--------------------------------+
        | NewPassword | - новый пароль.  |
        +--------------------------------+

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def Update(self, Password) -> bool:  # iattributetype_update.html
        """
        ## Update - Обновить данные

        Метод позволяет обновить данные в типе атрибута.

        ### Синтаксис Automation

        ```
        BOOL Update( BSTR Password );
        ```

        ### Входные параметры

        +----------------------+
        | Password | - пароль. |
        +----------------------+

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def ViewEdit(self, Parent, Password, ReadOnly) -> bool:  # iattributetype_viewedit.html
        """
        ## ViewEdit - Открыть диалог для просмотра и редактирования типа атрибута

        Метод позволяет открыть диалог для просмотра и редактирования типа атрибута.

        ### Синтаксис Automation

        ```
        BOOL ViewEdit( OLE_HANDLE Parent,
        BSTR Password,
        BOOL ReadOnly );
        ```

        ### Выходные параметры

        +------------------------------------------------------------------------------------------------------------+
        | Parent   | - дескриптор главного окна системы КОМПАС, который можно получить с помощью функции GetHWindow, |
        +------------------------------------------------------------------------------------------------------------+
        | Password | - пароль,                                                                                       |
        +------------------------------------------------------------------------------------------------------------+
        | ReadOnly | - TRUE - открыть диалог для просмотра, - FALSE - открыть диалог для редактирования.             |
        +------------------------------------------------------------------------------------------------------------+

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...




class IColumnInfo(IKompasAPIObject):  # icolumninfo.html
    """
    ## Интерфейс IColumnInfo

    Интерфейс параметров столбца табличного атрибута.

    1. Интерфейс позволяет задавать параметры столбца табличного атрибута.

    2. Интерфейс можно получить следующими способами:

    •у интерфейса типа атрибута с помощью свойства IAttributeType::ColumnInfo и метода IAttributeType::AddColumn.

    •для столбца записи - с помощью свойства IColumnInfo::RecordColumnInfo и метода IColumnInfo::AddRecordColumn.
    """

    @property
    def Caption(self) -> str:  # icolumninfo_caption.html
        """
        ## Caption - Заголовок столбца

        Тип данных: BSTR

        Свойство позволяет устанавливать и получать заголовок столбца атрибута.

        ### Синтаксис Automation

        ```
        Caption = Object.Caption       # Получить свойство (* )
        Object.Caption = Caption       # Установить свойство (* )
        Caption = Object.GetCaption()  # Получить свойство (**)
        Object.SetCaption( Caption )   # Установить свойство (**)
        ```
        """
        ...
    @Caption.setter
    def Caption(self, value: str): ...

    @property
    def ColType(self) -> int:  # icolumninfo_coltype.html
        """
        ## ColType - Тип данных столбца

        Тип данных: из перечисления ksValueTypeEnum

        1. Свойство позволяет устанавливать и получать тип данных столбца атрибута.

        2. При выборе типа ksValueTypeString длина строки задается свойством IColumnInfo::Range (1…4000).

        ### Синтаксис Automation

        ```
        ColType = Object.ColType       # Получить свойство (* )
        Object.ColType = ColType       # Установить свойство (* )
        ColType = Object.GetColType()  # Получить свойство (**)
        Object.SetColType( ColType )   # Установить свойство (**)
        ```
        """
        ...
    @ColType.setter
    def ColType(self, value: int): ...

    @property
    def DefValue(self) -> typing.Any:  # icolumninfo_defvalue.html
        """
        ## DefValue - Значение по умолчанию

        Тип данных: VARIANT

        Свойство позволяет устанавливать и получать умолчательное значение колонки атрибута.

        ### Синтаксис Automation

        ```
        DefValue = Object.DefValue       # Получить свойство (* )
        Object.DefValue = DefValue       # Установить свойство (* )
        DefValue = Object.GetDefValue()  # Получить свойство (**)
        Object.SetDefValue( DefValue )   # Установить свойство (**)
        ```
        """
        ...
    @DefValue.setter
    def DefValue(self, value: typing.Any): ...

    @property
    def Key(self) -> int:  # icolumninfo_key.html
        """
        ## Key - Ключ колонки

        Тип данных:long.

        Свойство позволяет устанавливать и получать ключ столбца атрибута.

        ### Синтаксис Automation

        ```
        Key = Object.Key       # Получить свойство (* )
        Object.Key = Key       # Установить свойство (* )
        Key = Object.GetKey()  # Получить свойство (**)
        Object.SetKey( Key )   # Установить свойство (**)
        ```
        """
        ...
    @Key.setter
    def Key(self, value: int): ...

    @property
    def ListValue(self) -> bool:  # icolumninfo_listvalue.html
        """
        ## ListValue - Использовать список значений

        Тип данных: BOOL

        Свойство позволяет устанавливать и получать признак использования списка значений.

        ### Синтаксис Automation

        ```
        ListValue = Object.ListValue       # Получить свойство (* )
        Object.ListValue = ListValue       # Установить свойство (* )
        ListValue = Object.GetListValue()  # Получить свойство (**)
        Object.SetListValue( ListValue )   # Установить свойство (**)
        ```
        """
        ...
    @ListValue.setter
    def ListValue(self, value: bool): ...

    @property
    def Range(self) -> typing.Any:  # icolumninfo_range.html
        """
        ## Range - Диапазон значений

        Тип данных: VARIANT

        Свойство позволяет устанавливать и получать диапазон значений столбца атрибута.

        ### Синтаксис Automation

        ```
        Range = Object.Range       # Получить свойство (* )
        Object.Range = Range       # Установить свойство (* )
        Range = Object.GetRange()  # Получить свойство (**)
        Object.SetRange( Range )   # Установить свойство (**)
        ```
        """
        ...
    @Range.setter
    def Range(self, value: typing.Any): ...

    @property
    def RecordColumnsCount(self) -> int:  # icolumninfo_recordcolumnscount.html
        """
        ## RecordColumnsCount - Получить количество столбцов записи

        Тип данных: long

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        RecordColumnsCount = Object.RecordColumnsCount       # Получить свойство (* )
        RecordColumnsCount = Object.GetRecordColumnsCount()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def SortListValue(self) -> bool:  # icolumninfo_sortlistvalue.html
        """
        ## SortListValue - Учитывать порядок размещения значений при сортировке

        Тип данных: BOOL

        Свойство позволяет устанавливать и получать признак учета порядка значений при сортировке.

        ### Синтаксис Automation

        ```
        SortListValue = Object.SortListValue       # Получить свойство (* )
        Object.SortListValue = SortListValue       # Установить свойство (* )
        SortListValue = Object.GetSortListValue()  # Получить свойство (**)
        Object.SetSortListValue( SortListValue )   # Установить свойство (**)
        ```
        """
        ...
    @SortListValue.setter
    def SortListValue(self, value: bool): ...

    def AddRecordColumn(self, Index, Type, Name) -> IColumnInfo:  # icolumninfo_addrecordcolumn.html
        """
        ## AddRecordColumn - Добавить колонку

        Метод позволяет добавить колонку к записи.

        ### Синтаксис Automation

        ```
        LPDISPATCH AddRecordColumn( long Index,
        long Type,
        BSTR Name );
        ```

        ### Входные параметры

        +-----------------------------------------------------------------+
        | Index | - индекс колонки, после которой нужно добавить колонку, |
        +-----------------------------------------------------------------+
        | Type  | - тип значений колонки,                                 |
        +-----------------------------------------------------------------+
        | Name  | - название колонки.                                     |
        +-----------------------------------------------------------------+

        ### Возвращаемое значение

        указатель на интерфейс IColumnInfo.
        """
        ...

    def Delete(self) -> bool:  # icolumninfo_delete.html
        """
        ## Delete - Удалить объект

        Метод позволяет добавить колонку к записи.

        ### Синтаксис Automation

        ```
        BOOL Delete();
        ```

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def RecordColumnInfo(self, Index) -> IColumnInfo:  # icolumninfo_recordcolumninfo.html
        """
        ## RecordColumnInfo - Получить информацию о столбце атрибута

        Тип данных: указатель на интерфейс IColumnInfo

        1. Свойство позволяет получать информацию о столбце записи.

        2. Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        RecordColumnInfo = Object.RecordColumnInfo       # Получить свойство (* )
        RecordColumnInfo = Object.GetRecordColumnInfo()  # Получить свойство (**)
        ```
        """
        ...




class IKompasDocument(IKompasAPIObject):  # ikompasdocument.html
    """
    ## Интерфейс IKompasDocument

    Интерфейс событий документа, работа с файлом...

    Базовый класс документов КОМПАС.

    1. Данный интерфейс является базовым для всех интерфейсов документов КОМПАС API.

    2. Данный интерфейс может быть получен следующими способами:

    •от интерфейса приложения IApplication с помощью свойства IApplication::ActiveDocument,

    •от интерфейса коллекции документов IDocuments с помощью свойства IDocuments::Item и методов IDocuments::Add, IDocuments::Open,

    •от интерфейса элемента библиотеки документов IInsert с помощью метода IInsert::Edit.

    3. С помощью метода IUnknown::QueryInterface у интерфейса можно получить дополнительный интерфейс IKompasDocument1и IProductDataManager.
    """

    @property
    def Active(self) -> bool:  # ikompasdocument_active.html
        """
        ## Active - Активность документа

        Тип данных: BOOL

        Позволяет получить активность документа. Установить можно только значение TRUE, т.е. сделать документ активным, если он неактивен. Значение FALSE игнорируется.

        ### Синтаксис Automation

        ```
        Active = iObject.Active       # Получить свойство (* )
        iObject.Active = Active       # Установить свойство (* )
        Active = iObject.GetActive()  # Получить свойство (**)
        iObject.SetActive (Active)    # Установить свойство (**)
        ```
        """
        ...
    @Active.setter
    def Active(self, value: bool): ...

    @property
    def Changed(self) -> bool:  # ikompasdocument_changed.html
        """
        ## Changed - Документ изменен

        Тип данных: BOOL

        Значения свойства:

        +-------------------------------------------------------------+
        | TRUE  | - документ изменен с момента последнего сохранения, |
        +-------------------------------------------------------------+
        | FALSE | - документ не изменялся.                            |
        +-------------------------------------------------------------+

        ### Синтаксис Automation

        ```
        Changed = iObject.Changed       # Получить свойство (* )
        Changed = iObject.GetChanged()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def DocumentFrames(self) -> IDocumentFrames:  # ikompasdocument_documentframes.html
        """
        ## DocumentFrames - Интерфейс коллекции окон документа

        Тип данных: указатель на интерфейс коллекции окон IDocumentFrames

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        DocumentFrames = iObject.DocumentFrames       # Получить свойство(* )
        DocumentFrames = iObject.GetDocumentFrames()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def DocumentSettings(self) -> IDocumentSettings:  # ikompasdocument_documentsettings.html
        """
        ## DocumentSettings - Настройки документа

        Тип данных: - указатель на интерфейс IDocumentSettings текущих настроек документа.

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        DocumentSettings = iObject.DocumentSettings       # Получить свойство (* )
        DocumentSettings = iObject.GetDocumentSettings()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def DocumentType(self) -> int:  # ikompasdocument_documenttype.html
        """
        ## DocumentType - Тип документа

        Тип данных: тип документа из перечисления DocumentTypeEnum

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        DocumentType = iObject.DocumentType       # Получить свойство * )
        DocumentType = iObject.GetDocumentType()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def LayoutSheets(self) -> ILayoutSheets:  # ikompasdocument_layoutsheets.html
        """
        ## LayoutSheets - Листы оформления

        Тип данных: указатель на интерфейс ILayoutSheets коллекции листов оформления

        1. Свойство используется только для документа Чертеж.

        2. Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        LayoutSheets = iObject.LayoutSheets       # Получить свойство
        LayoutSheets = iObject.GetLayoutSheets()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def Name(self) -> str:  # ikompasdocument_name.html
        """
        ## Name - Имя документа

        Тип данных:строка.

        Свойство доступно только для чтения. Возвращает имя файла документа без пути.

        ### Синтаксис Automation

        ```
        Name = iObject.Name       # Получить свойство (* )
        Name = iObject.GetName()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def Path(self) -> str:  # ikompasdocument_path.html
        """
        ## Path - Путь к файлу документа

        Тип данных: строка

        Свойство доступно только для чтения. Возвращает путь к файлу документа без имени и расширения файла.

        ### Синтаксис Automation

        ```
        Path = iObject.Path       # Получить свойство (* )
        Path = iObject.GetPath()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def PathName(self) -> str:  # ikompasdocument_pathname.html
        """
        ## PathName - Полное имя документа

        Тип данных:строка

        Свойство доступно только для чтения. Возвращает полное имя файла документа, состоящее из пути, имени и расширения файла.

        ### Синтаксис Automation

        ```
        PathName = iObject.PathName       # Получить свойство (* )
        PathName = iObject.GetPathName()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def ReadOnly(self) -> bool:  # ikompasdocument_readonly.html
        """
        ## ReadOnly - Только для чтения

        Тип данных:BOOL

        Позволяет получить и изменить свойство документа Только для чтения.

        ### Синтаксис Automation

        ```
        ReadOnly = iObject.ReadOnly       # Получить свойство (* )
        iObject.ReadOnly = ReadOnly       # Установить свойство (* )
        ReadOnly = iObject.GetReadOnly()  # Получить свойство (**)
        iObject.SetReadOnly (ReadOnly)    # Установить свойство (**)
        ```
        """
        ...
    @ReadOnly.setter
    def ReadOnly(self, value: bool): ...

    @property
    def SpecificationDescriptions(self) -> ISpecificationDescriptions:  # ikompasdocument_specificationdescriptions.html
        """
        ## SpecificationDescriptions - Описания спецификации

        Тип данных: указатель на интерфейс ISpecificationDescriptions коллекции описаний спецификации

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        SpecificationDescriptions = iObject.SpecificationDescriptions       # Получить свойство(* )
        SpecificationDescriptions = iObject.GetSpecificationDescriptions()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def UserDataStoragesMng(self) -> IUserDataStoragesMng:  # ikompasdocument_userdatastoragesmng.html
        """
        ## UserDataStoragesMng - Интерфейс Менеджера пользовательских хранилищ

        Тип данных: указатель на интерфейс IUserDataStoragesMng Менеджера пользовательских хранилищ

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        UserDataStoragesMng = iObject.UserDataStoragesMng       # Получить свойство (* )
        UserDataStoragesMng = iObject.GetUserDataStoragesMng()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def Visible(self) -> bool:  # ikompasdocument_visible.html
        """
        ## Visible - Видимость документа

        Тип данных:BOOL

        Значения свойства:

        +-----------------------------------------------+
        | TRUE  | - документ открыт в видимом режиме,   |
        +-----------------------------------------------+
        | FALSE | - документ открыт в невидимом режиме. |
        +-----------------------------------------------+

        Свойство доступно только для чтения. Возвращает свойство видимости документа.

        ### Синтаксис Automation

        ```
        Visible = iObject.Visible       # Получить свойство (* )
        Visible = iObject.GetVisible()  # Получить свойство (**)
        ```
        """
        ...

    def Close(self, closeOptions) -> bool:  # ikompasdocument_close.html
        """
        ## Close - Закрыть документ

        1. Если документ не был изменен или его уже сохранили перед закрытием, то значение входного параметра будет проигнорировано.

        2. Если документ был изменен, то возможны три варианта:

        •закрыть документ без сохранения – kdDoNotSaveChanges (в этом случае все изменения будут потеряны),

        •закрыть документ с сохранением – kdSaveChanges (в этом случае документ будет сначала сохранен, а потом закрыт),

        •предоставить право выбора пользователю – kdPromptToSaveChanges (в этом случае на экран будет выведено сообщение о возможных действиях). Если закрываем в режиме kdPromptToSaveChanges, то пользователь может вообще отказаться от закрытия измененного документа, в этом случае метод вернет FALSE.

        ### Синтаксис Automation

        ```
        BOOL Close (DocumentCloseOptions closeOptions);
        ```

        ### Входные параметры

        +----------------------------------------------------------------------------------------------------+
        | closeOptions | - действия при закрытии измененного документа из перечисления DocumentCloseOptions. |
        +----------------------------------------------------------------------------------------------------+

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def Save(self):  # ikompasdocument_save.html
        """
        ## Save - Сохранить документ на диске

        Сохраняет все изменения в документе и снимает флаг измененности документа.

        ### Синтаксис Automation

        ```
        void Save();
        ```
        """
        ...

    def SaveAs(self, PathName):  # ikompasdocument_saveas.html
        """
        ## SaveAs- Сохранить документ под другим именем

        Позволяет сохранить документ на диск под другим именем.

        ### Синтаксис Automation

        ```
        void SaveAs (LPCTSTR PathName);
        ```

        ### Входные параметры

        +---------------------------------------------------------------------------------------+
        | PathName | - полное имя файла документа, состоящее из пути, имени и расширения файла. |
        +---------------------------------------------------------------------------------------+
        """
        ...




class IKompasDocument2D(IKompasDocument):  # ikompasdocument2d.html
    """
    ## Интерфейс IKompasDocument2D

    Базовый класс графических документов КОМПАС.

    1. Данный интерфейс является базовым для интерфейсов графических документов КОМПАС API - чертежа и фрагмента.

    2. Интерфейс можно получить из коллекции документов IDocuments.

    3. Интерфейс может быть получен с помощью метода IUnknown QueryInterface от

    •интерфейса документа IKompasDocument,

    •интерфейса чертежа IDrawingDocument,

    •интерфейса фрагмента IFragmentDocument.

    4. С помощью метода IUnknown::QueryInterface у интерфейса можно получить дополнительные интерфейсы:

    •IKompasDocument2D1,

    •IPropertyKeeper.
    """

    @property
    def ViewsAndLayersManager(self) -> IViewsAndLayersManager:  # ikompasdocument2d_viewsandlayersmanager.html
        """
        ## ViewsAndLayersManager - Возвращает менеджер видов и слоев документа

        Тип данных: IViewsAndLayersManager

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        ViewsAndLayersManager = iObject.ViewsAndLayersManager       # Получить свойство (* )
        ViewsAndLayersManager = iObject.GetViewsAndLayersManager()  # Получить свойство (**)
        ```
        """
        ...

    def GetDrawingObjectNotifyResult(self):  # ikompasdocument2d_getdrawingobjectnotifyresult.html
        """
        ## GetDrawingObjectNotifyResult - Получить интерфейс результатов редактирования объекта при обработке событий

        ### Синтаксис Automation

        ```
        LPDISPATCH GetDrawingObjectNotifyResult();
        ```

        ### Возвращаемое значение

        указатель на интерфейс ksObject2DNotifyResult
        """
        ...




class IFragmentDocument(IKompasDocument):  # ifragmentdocument.html
    """
    ## Интерфейс IFragmentDocument

    Интерфейс фрагмента.
    """

    @property
    def IsSketch(self) -> bool:  # ifragmentdocument_issketch.html
        """
        ## IsSketch - Документ является эскизом

        Тип данных:BOOL.

        Значения свойства:

        +-----------------------------------------+
        | TRUE  | - документ является эскизом,    |
        +-----------------------------------------+
        | FALSE | - документ не является эскизом. |
        +-----------------------------------------+

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        IsSketch = iObject.IsSketch        # Получить свойство (* )
        IsSketch = iObject.GetIsSketch( )  # Получить свойство (**)
        ```
        """
        ...

    @property
    def ViewsAndLayersManager(self):
        ...

    def GetDrawingObjectNotifyResult(self):
        ...




class IDrawingDocument(IKompasDocument2D):  # idrawingdocument.html
    """
    ## Интерфейс IDrawingDocument

    Интерфейс чертежа.
    """

    @property
    def ChangeListDescriptions(self) -> ISpecificationDescriptions:  # idrawingdocument_changelistdescriptions.html
        """
        ## ChangeListDescriptions - Описания таблицы изменений

        Тип данных: указатель на интерфейс ISpecificationDescriptions

        1. Позволяет получать и создавать объекты спецификации таблицы изменения.

        2. Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        Descriptions = Object.ChangeListDescriptions       # Получить свойство (* )
        Descriptions = Object.GetChangeListDescriptions()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def SpecRough(self) -> ISpecRough:  # idrawingdocument_specrough.html
        """
        ## SpecRough - Неуказанная шероховатость

        Тип данных: указатель на интерфейс ISpecRough

        1. Свойство позволяет получать интерфейс неуказанной шероховатости.

        2. Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        SpecRough = Object.SpecRough       # Получить свойство (* )
        SpecRough = Object.GetSpecRough()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def TechnicalDemand(self) -> ITechnicalDemand:  # idrawingdocument_technicaldemand.html
        """
        ## TechnicalDemand - Технические требования

        Тип данных: указатель на интерфейс ITechnicalDemand

        1. Свойство позволяет получать интерфейс технических требований.

        2. Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        TechnicalDemand = Object.TechnicalDemand       # Получить свойство (* )
        TechnicalDemand = Object.GetTechnicalDemand()  # Получить свойство (**)
        ```
        """
        ...




class IKompasDocument3D(IKompasDocument):  # ikompasdocument3d.html
    """
    ## Интерфейс IKompasDocument3D

    Базовый класс документов-моделей КОМПАС (скрытый).

    1. Данный интерфейс является базовым для интерфейсов документов-моделей КОМПАС API – детали IPartDocument и сборки IAssemblyDocument.

    2. Интерфейс можно получить у интерфейса приложения IApplication с помощью свойства IApplication::ActiveDocument или свойства IApplication::Documents.

    3. У данного интерфейса посредством вызова метода IUnknown::QueryInterface (const GUID far& IID, void** pif) можно получить дополнительные интерфейсы:

    •IEmbodimentsManager

    •IExternalTessellationManager

    •ISelectionManager

    •IChooseManager

    ### Синтаксис Automation

    ```
    IKompasDocument3D document3D (application.GetActiveDocument());
    ```
    """

    @property
    def ChooseManager(self) -> IChooseManager:  # ikompasdocument3d_choosemanager.html
        """
        ## ChooseManager - Менеджер выбора (подсветки) объектов

        Тип данных: указатель на интерфейс менеджера выбора (подсветки) объектов IChooseManager

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        ChooseManager = Object.ChooseManager       # Получить свойство (* )
        ChooseManager = Object.GetChooseManager()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def CreateObjectsInCurrentLocalCS(self) -> bool:  # ikompasdocument3d_createobjectsincurrentlocalcs.html
        """
        ## CreateObjectsInCurrentLocalCS - Создавать новые объекты в текущей ЛСК

        Тип данных: BOOL

        Свойство позволяет включить режим создания объектов модели в текущей системе координат. Для правильной работы библиотек нужно включить режим, создать нужные объекты и выключить режим.

        Если флаг включен, то 3D точка, ломаная, сплайн, эскиз, импортированная поверхность, операция без истории, деталь заготовка через API будут создаваться в текущей локальной системе координат.

        Параметры создания в локальной системе координат зависят от настроек системы.

        См. также: ISystemSettings::ModelLocalCSCreateInAbsoluteCS и ISystemSettings::ModelLocalCSSetActive.

        ### Синтаксис Automation

        ```
        CreateObjectsInCurrentLocalCS = Object.CreateObjectsInCurrentLocalCS       # Получить свойство (* )
        Object.CreateObjectsInCurrentLocalCS = CreateObjectsInCurrentLocalCS       # Установить свойство (* )
        CreateObjectsInCurrentLocalCS = Object.GetCreateObjectsInCurrentLocalCS()  # Получить свойство (**)
        Object.SetCreateObjectsInCurrentLocalCS( CreateObjectsInCurrentLocalCS )   # Установить свойство (**)
        ```
        """
        ...
    @CreateObjectsInCurrentLocalCS.setter
    def CreateObjectsInCurrentLocalCS(self, value: bool): ...

    @property
    def DrawMode(self) -> int:  # ikompasdocument3d_drawmode.html
        """
        ## DrawMode - Режим визуализации

        Тип данных: long

        Значение свойства:

        Из перечисления ViewMode

        ### Синтаксис Automation

        ```
        long DrawMode = iObject.DrawMode       # Получить свойство (* )
        iObject.DrawMode = DrawMode            # Установить свойство (* )
        long DrawMode = iObject.GetDrawMode()  # Получить свойство (**)
        iObject.SetDrawMode( DrawMode )        # Установить свойство (**)
        ```
        """
        ...
    @DrawMode.setter
    def DrawMode(self, value: int): ...

    @property
    def EnableUndo(self) -> bool:  # ikompasdocument3d_enableundo.html
        """
        ## EnableUndo - Включить/отключить добавление команд в Undo

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        EnableUndo = Object.EnableUndo       # Получить свойство (* )
        Object.EnableUndo = EnableUndo       # Установить свойство (* )
        EnableUndo = Object.GetEnableUndo()  # Получить свойство (**)
        Object.SetEnableUndo( EnableUndo )   # Установить свойство (**)
        ```
        """
        ...
    @EnableUndo.setter
    def EnableUndo(self, value: bool): ...

    @property
    def HideAllAuxiliaryGeom(self) -> bool:  # ikompasdocument3d_hideallauxiliarygeom.html
        """
        ## HideAllAuxiliaryGeom - Скрыть / показать все вспомогательные объекты

        Тип данных: BOOL

        В зависимости от флага HideInComponentsMode позволяет скрыть или показать все вспомогательные объекты в документе или, если документ является сборкой, во вставках.

        ### Синтаксис Automation

        ```
        HideAllAuxiliaryGeom = Object.HideAllAuxiliaryGeom       # Получить свойство (* )
        Object.HideAllAuxiliaryGeom = HideAllAuxiliaryGeom       # Установить свойство (* )
        HideAllAuxiliaryGeom = Object.GetHideAllAuxiliaryGeom()  # Получить свойство (**)
        Object.SetHideAllAuxiliaryGeom( HideAllAuxiliaryGeom )   # Установить свойство (**)
        ```
        """
        ...
    @HideAllAuxiliaryGeom.setter
    def HideAllAuxiliaryGeom(self, value: bool): ...

    @property
    def HideAllAxis(self) -> bool:  # ikompasdocument3d_hideallaxis.html
        """
        ## HideAllAxis - Скрыть / показать конструктивные оси

        Тип данных: BOOL

        Значения свойства:

        +----------------------------------------+
        | TRUE  | - конструктивные оси скрыты,   |
        +----------------------------------------+
        | FALSE | - конструктивные оси показаны. |
        +----------------------------------------+

        В зависимости от флага HideInComponentsMode позволяет скрыть или показать все вспомогательные объекты в документе или, если документ является сборкой, во вставках.

        ### Синтаксис Automation

        ```
        BOOL HideAllAxis = iObject.HideAllAxis       # Получить свойство (* )
        iObject.HideAllAxis = HideAllAxis            # Установить свойство (* )
        BOOL HideAllAxis = iObject.GetHideAllAxis()  # Получить свойство (**)
        iObject.SetHideAllAxis (HideAllAxis)         # Установить свойство (**)
        ```
        """
        ...
    @HideAllAxis.setter
    def HideAllAxis(self, value: bool): ...

    @property
    def HideAllControlPoints(self) -> bool:  # ikompasdocument3d_hideallcontrolpoints.html
        """
        ## HideAllControlPoints - Скрыть / показать контрольные точки

        Тип данных: BOOL

        Значения свойства:

        +---------------------------------------+
        | TRUE  | - контрольные точки скрыты,   |
        +---------------------------------------+
        | FALSE | - контрольные точки показаны. |
        +---------------------------------------+

        В зависимости от флага HideInComponentsMode позволяет скрыть или показать все вспомогательные объекты в документе или, если документ является сборкой, во вставках.

        ### Синтаксис Automation

        ```
        BOOL HideAllControlPoints = iObject.HideAllControlPoints       # Получить свойство (* )
        iObject.HideAllControlPoints = HideAllControlPoints            # Установить свойство (* )
        BOOL HideAllControlPoints = iObject.GetHideAllControlPoints()  # Получить свойство (**)
        iObject.SetHideAllControlPoints (HideAllControlPoints)         # Установить свойство (**)
        ```
        """
        ...
    @HideAllControlPoints.setter
    def HideAllControlPoints(self, value: bool): ...

    @property
    def HideAllCurves(self) -> bool:  # ikompasdocument3d_hideallcurves.html
        """
        ## HideAllCurves - Скрыть / показать пространственные кривые

        Тип данных: BOOL

        Значения свойства:

        +---------------------------------------------+
        | TRUE  | - пространственные кривые скрыты,   |
        +---------------------------------------------+
        | FALSE | - пространственные кривые показаны. |
        +---------------------------------------------+

        В зависимости от флага HideInComponentsMode позволяет скрыть или показать все вспомогательные объекты в документе или, если документ является сборкой, во вставках.

        ### Синтаксис Automation

        ```
        BOOL HideAllCurves = iObject.HideAllCurves       # Получить свойство (* )
        iObject.HideAllCurves = HideAllCurves            # Установить свойство (* )
        BOOL HideAllCurves = iObject.GetHideAllCurves()  # Получить свойство (**)
        iObject.SetHideAllCurves (HideAllCurves)         # Установить свойство (**)
        ```
        """
        ...
    @HideAllCurves.setter
    def HideAllCurves(self, value: bool): ...

    @property
    def HideAllDesignations(self) -> bool:  # ikompasdocument3d_hidealldesignations.html
        """
        ## HideAllDesignations - Скрыть / показать условные обозначения

        Тип данных: BOOL

        В зависимости от флага HideInComponentsMode позволяет скрыть или показать все вспомогательные объекты в документе или, если документ является сборкой, во вставках.

        ### Синтаксис Automation

        ```
        HideAllDesignations = Object.HideAllDesignations       # Получить свойство (* )
        Object.HideAllDesignations = HideAllDesignations       # Установить свойство (* )
        HideAllDesignations = Object.GetHideAllDesignations()  # Получить свойство (**)
        Object.SetHideAllDesignations( HideAllDesignations )   # Установить свойство (**)
        ```
        """
        ...
    @HideAllDesignations.setter
    def HideAllDesignations(self, value: bool): ...

    @property
    def HideAllDimensions(self) -> bool:  # ikompasdocument3d_hidealldimensions.html
        """
        ## HideAllDimensions - Скрыть / показать размеры

        Тип данных: BOOL

        В зависимости от флага HideInComponentsMode позволяет скрыть или показать все вспомогательные объекты в документе или, если документ является сборкой, во вставках.

        ### Синтаксис Automation

        ```
        HideAllDimensions = Object.HideAllDimensions       # Получить свойство (* )
        Object.HideAllDimensions = HideAllDimensions       # Установить свойство (* )
        HideAllDimensions = Object.GetHideAllDimensions()  # Получить свойство (**)
        Object.SetHideAllDimensions( HideAllDimensions )   # Установить свойство (**)
        ```
        """
        ...
    @HideAllDimensions.setter
    def HideAllDimensions(self, value: bool): ...

    @property
    def HideAllPlaces(self) -> bool:  # ikompasdocument3d_hideallplaces.html
        """
        ## HideAllPlaces - Скрыть / показать начала координат

        Тип данных: BOOL

        Значения свойства:

        +--------------------------------------+
        | TRUE  | - начала координат скрыты,   |
        +--------------------------------------+
        | FALSE | - начала координат показаны. |
        +--------------------------------------+

        В зависимости от флага HideInComponentsMode позволяет скрыть или показать все вспомогательные объекты в документе или, если документ является сборкой, во вставках.

        ### Синтаксис Automation

        ```
        BOOL HideAllPlaces = iObject.HideAllPlaces       # Получить свойство (* )
        iObject.HideAllPlaces = HideAllPlaces            # Установить свойство (* )
        BOOL HideAllPlaces = iObject.GetHideAllPlaces()  # Получить свойство (**)
        iObject.SetHideAllPlaces (HideAllPlaces)         # Установить свойство (**)
        ```
        """
        ...
    @HideAllPlaces.setter
    def HideAllPlaces(self, value: bool): ...

    @property
    def HideAllPlanes(self) -> bool:  # ikompasdocument3d_hideallplanes.html
        """
        ## HideAllPlanes - Скрыть / показать конструктивные плоскости

        Тип данных: BOOL

        Значения свойства:

        +----------------------------------------------+
        | TRUE  | - конструктивные плоскости скрыты,   |
        +----------------------------------------------+
        | FALSE | - конструктивные плоскости показаны. |
        +----------------------------------------------+

        В зависимости от флага HideInComponentsMode позволяет скрыть или показать все вспомогательные объекты в документе или, если документ является сборкой, во вставках.

        ### Синтаксис Automation

        ```
        BOOL HideAllPlanes = iObject.HideAllPlanes       # Получить свойство (* )
        iObject.HideAllPlanes = HideAllPlanes            # Установить свойство (* )
        BOOL HideAllPlanes = iObject.GetHideAllPlanes()  # Получить свойство (**)
        iObject.SetHideAllPlanes (HideAllPlanes)         # Установить свойство (**)
        ```
        """
        ...
    @HideAllPlanes.setter
    def HideAllPlanes(self, value: bool): ...

    @property
    def HideAllSketches(self) -> bool:  # ikompasdocument3d_hideallsketches.html
        """
        ## HideAllSketches - Скрыть / показать эскизы

        Тип данных: BOOL

        Значения свойства:

        +----------------------------+
        | TRUE  | - эскизы скрыты,   |
        +----------------------------+
        | FALSE | - эскизы показаны. |
        +----------------------------+

        В зависимости от флага HideInComponentsMode позволяет скрыть или показать все вспомогательные объекты в документе или, если документ является сборкой, во вставках.

        ### Синтаксис Automation

        ```
        BOOL HideAllSketches = iObject.HideAllSketches       # Получить свойство (* )
        iObject.HideAllSketches = HideAllSketches            # Установить свойство (* )
        BOOL HideAllSketches = iObject.GetHideAllSketches()  # Получить свойство (**)
        iObject.SetHideAllSketches (HideAllSketches)         # Установить свойство (**)
        ```
        """
        ...
    @HideAllSketches.setter
    def HideAllSketches(self, value: bool): ...

    @property
    def HideAllSurfaces(self) -> bool:  # ikompasdocument3d_hideallsurfaces.html
        """
        ## HideAllSurfaces - Скрыть / показать поверхности

        Тип данных: BOOL

        Значения свойства:

        +---------------------------------+
        | TRUE  | - поверхности скрыты,   |
        +---------------------------------+
        | FALSE | - поверхности показаны. |
        +---------------------------------+

        В зависимости от флага HideInComponentsMode позволяет скрыть или показать все вспомогательные объекты в документе или, если документ является сборкой, во вставках.

        ### Синтаксис Automation

        ```
        BOOL HideAllSurfaces = iObject.HideAllSurfaces       # Получить свойство (* )
        iObject.HideAllSurfaces = HideAllSurfaces            # Установить свойство (* )
        BOOL HideAllSurfaces = iObject.GetHideAllSurfaces()  # Получить свойство (**)
        iObject.SetHideAllSurfaces (HideAllSurfaces)         # Установить свойство (**)
        ```
        """
        ...
    @HideAllSurfaces.setter
    def HideAllSurfaces(self, value: bool): ...

    @property
    def HideAllThreads(self) -> bool:  # ikompasdocument3d_hideallthreads.html
        """
        ## HideAllThreads - Резьбы

        Тип данных: BOOL

        Значения свойства:

        +----------------------------+
        | TRUE  | - резьбы скрыты,   |
        +----------------------------+
        | FALSE | - резьбы показаны. |
        +----------------------------+

        В зависимости от флага HideInComponentsMode позволяет скрыть или показать все вспомогательные объекты в документе или, если документ является сборкой, во вставках.

        ### Синтаксис Automation

        ```
        BOOL HideAllThreads = iObject.HideAllThreads       # Получить свойство (* )
        iObject.HideAllThreads = HideAllThreads            # Установить свойство (* )
        BOOL HideAllThreads = iObject.GetHideAllThreads()  # Получить свойство (**)
        iObject.SetHideAllThreads (HideAllThreads)         # Установить свойство (**)
        ```
        """
        ...
    @HideAllThreads.setter
    def HideAllThreads(self, value: bool): ...

    @property
    def HideInComponentsMode(self) -> bool:  # ikompasdocument3d_hideincomponentsmode.html
        """
        ## HideInComponentsMode - Режим скрытия вспомогательной геометрии в компонентах

        Тип данных: BOOL

        Флаг позволяет для сборки включить режим работы с флагами скрытия вспомогательной геометрии во вставках или в документе.

        Если флаг включен, то свойства: HideAllPlanes, HideAllAxis, HideAllSketches, HideAllPlaces, HideAllSurfaces, HideAllThreads, HideAllCurves, HideAllControlPoints, HideAllDimensions, HideAllDesignations, HideAllAuxiliaryGeom

        управляют признаками скрытия вспомогательной геометрии во вставках.

        ### Синтаксис Automation

        ```
        HideInComponentsMode = Object.HideInComponentsMode       # Получить свойство (* )
        Object.HideInComponentsMode = HideInComponentsMode       # Установить свойство (* )
        HideInComponentsMode = Object.GetHideInComponentsMode()  # Получить свойство (**)
        Object.SetHideInComponentsMode( HideInComponentsMode )   # Установить свойство (**)
        ```
        """
        ...
    @HideInComponentsMode.setter
    def HideInComponentsMode(self, value: bool): ...

    @property
    def Perspective(self) -> bool:  # ikompasdocument3d_perspective.html
        """
        ## Perspective - Скрыть / показать перспективу

        Тип данных: BOOL

        Значения свойства:

        +--------------------------------------+
        | TRUE  | - показывать перспективу,    |
        +--------------------------------------+
        | FALSE | - не показывать перспективу. |
        +--------------------------------------+

        ### Синтаксис Automation

        ```
        BOOL Perspective = iObject.Perspective       # Получить свойство (* )
        iObject.Perspective = Perspective            # Установить свойство (* )
        BOOL Perspective = iObject.GetPerspective()  # Получить свойство (**)
        iObject.SetPerspective (Perspective)         # Установить свойство (**)
        ```
        """
        ...
    @Perspective.setter
    def Perspective(self, value: bool): ...

    @property
    def SelectionManager(self) -> ISelectionManager:  # ikompasdocument3d_selectionmanager.html
        """
        ## SelectionManager - Менеджер селектированных объектов

        Тип данных: указатель на интерфейс менеджера селектированных объектов ISelectionManager

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        SelectionManager = Object.SelectionManager       # Получить свойство (* )
        SelectionManager = Object.GetSelectionManager()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def ShadedWireframe(self) -> bool:  # ikompasdocument3d_shadedwireframe.html
        """
        ## ShadedWireframe - Полутоновое изображение с каркасом

        Тип данных: BOOL

        Значения свойства:

        +---------------------------+
        | TRUE  | - каркас показан, |
        +---------------------------+
        | FALSE | - каркас скрыт.   |
        +---------------------------+

        ### Синтаксис Automation

        ```
        BOOL ShadedWireframe = iObject.ShadedWireframe  # Получить свойство (* )
        iObject.ShadedWireframe = ShadedWireframe       # Установить свойство (* )
        ShadedWireframe = iObject.GetShadedWireframe()  # Получить свойство (**)
        iObject.SetShadedWireframe (ShadedWireframe)    # Установить свойство (**)
        ```
        """
        ...
    @ShadedWireframe.setter
    def ShadedWireframe(self, value: bool): ...

    @property
    def TechnicalDemand3D(self) -> ITechnicalDemand3D:  # ikompasdocument3d_technicaldemand3d.html
        """
        ## TechnicalDemand3D - Технические требования 3D

        Тип данных: указатель на интерфейс ITechnicalDemand3D

        1. Свойство доступно только для чтения.

        2. Свойство позволяет получить указатель на интерфейс технических требований 3D.

        ### Синтаксис Automation

        ```
        TechnicalDemand3D = Object.TechnicalDemand3D       # Получить свойство (* )
        TechnicalDemand3D = Object.GetTechnicalDemand3D()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def ToleranceMode(self):  # ikompasdocument3d_tolerancemode.html
        """
        ## ToleranceMode - Включить/отключить режим пересчета по допуску

        Свойство позволяет включать и отключать режим перестроения по допуску.

        ### Синтаксис Automation

        ```
        ToleranceMode = Object.ToleranceMode       # Получить свойство (* )
        Object.ToleranceMode = ToleranceMode       # Установить свойство (* )
        ToleranceMode = Object.GetToleranceMode()  # Получить свойство (**)
        Object.SetToleranceMode( ToleranceMode )   # Установить свойство (**)
        ```
        """
        ...
    @ToleranceMode.setter
    def ToleranceMode(self, value): ...

    @property
    def TopPart(self) -> IPart7:  # ikompasdocument3d_toppart.html
        """
        ## TopPart - Верхний компонент (сам документ)

        Тип данных: указатель на интерфейс IPart7

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        TopPart = iObject.TopPart       # Получить свойство (* )
        TopPart = iObject.GetTopPart()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def UndoContainer(self) -> bool:  # ikompasdocument3d_undocontainer.html
        """
        ## UndoContainer - Включить/отключить объединение команд в Undo

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        UndoContainer = Object.UndoContainer       # Получить свойство (* )
        Object.UndoContainer = UndoContainer       # Установить свойство (* )
        UndoContainer = Object.GetUndoContainer()  # Получить свойство (**)
        Object.SetUndoContainer( UndoContainer )   # Установить свойство (**)
        ```
        """
        ...
    @UndoContainer.setter
    def UndoContainer(self, value: bool): ...

    def AttributesEx(self, Key1, Key2, Key3, Key4, Numb, Objects, SourcePart) -> typing.Any:  # ikompasdocument3d_attributesex.html
        """
        ## AttributesEx - Массив атрибутов документа в виде массива SAFEARRAY DISPATCH - VT_ARRAY | VT_DISPATCH

        Тип данных: VARIANT

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        AttributesEx = Object.AttributesEx( Key1, Key2, Key3, Key4, Numb, Objects, SourcePart )     # Получить свойство (* )
        AttributesEx = Object.GetAttributesEx( Key1, Key2, Key3, Key4, Numb, Objects, SourcePart )  # Получить свойство (**)
        ```

        ### Входные параметры

        +-----------------------------------------------------------------------------------------------------+
        | key1, key2, key3, key4 | - ключи атрибута,                                                          |
        +-----------------------------------------------------------------------------------------------------+
        | Numb                   | - уникальный номер типа атрибута,                                          |
        +-----------------------------------------------------------------------------------------------------+
        | Objects                | - массив VT_ARRAY | VT_DISPATCH объектов, для которых назначается атрибут, |
        +-----------------------------------------------------------------------------------------------------+
        | SourcePart             | - указатель на интерфейс вставки детали IPart.                             |
        +-----------------------------------------------------------------------------------------------------+
        """
        ...

    def CreateAttrEx(self, AttrID, Libname, Objects, SourcePart) -> IAttribute:  # ikompasdocument3d_createattrex.html
        """
        ## CreateAttrEx - Создать атрибут по номеру типа атрибута из библиотеки libname

        ### Синтаксис Automation

        ```
        LPDISPATCH CreateAttrEx( double AttrID, BSTR Libname, VARIANT Objects, IPart7 * SourcePart );
        ```

        ### Входные параметры

        +--------------------------------------------------------------------------------------------------------------------+
        | AttrID     | - уникальный номер типа атрибута,                                                                     |
        +--------------------------------------------------------------------------------------------------------------------+
        | Libname    | - имя библиотеки типов атрибутов, если libname = NULL, то тип атрибута берется из текущего документа, |
        +--------------------------------------------------------------------------------------------------------------------+
        | Objects    | - массив VT_ARRAY | VT_DISPATCH объектов, для которых назначается атрибут,                            |
        +--------------------------------------------------------------------------------------------------------------------+
        | SourcePart | - указатель на интерфейс вставки детали IPart.                                                        |
        +--------------------------------------------------------------------------------------------------------------------+

        ### Возвращаемое значение

        - указатель на интерфейс IAttribute
        """
        ...

    def DeleteHistory(self) -> bool:  # ikompasdocument3d_deletehistory.html
        """
        ## DeleteHistory - Удалить историю построения

        Метод позволяет удалить историю построения.

        ### Синтаксис Automation

        ```
        BOOL DeleteHistory();
        ```

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def RebuildDocument(self) -> bool:  # ikompasdocument3d_rebuilddocument.html
        """
        ## RebuildDocument - Перестроить документ

        Метод позволяет вызвать перестроение 3D-документа.

        ### Синтаксис Automation

        ```
        BOOL RebuildDocument();
        ```

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...




class IAssemblyDocument(IKompasDocument3D):  # iassemblydocument.html
    """
    ## Интерфейс IAssemblyDocument

    Интерфейс сборки.

    Посредством вызова метода IUnknown::QueryInterface (const GUID far& iid, void** pif) у данного интерфейса можно получить дополнительный интерфейс ILoadCombination.
    """

    @property
    def DismantleMode(self) -> bool:  # iassemblydocument_dismantlemode.html
        """
        ## DismantleMode - Разнесенная сборка

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        DismantleMode = Object.DismantleMode       # Получить свойство (* )
        Object.DismantleMode = DismantleMode       # Установить свойство (* )
        DismantleMode = Object.GetDismantleMode()  # Получить свойство (**)
        Object.SetDismantleMode( DismantleMode )   # Установить свойство (**)
        ```
        """
        ...
    @DismantleMode.setter
    def DismantleMode(self, value: bool): ...




class IPartDocument(IKompasDocument3D):  # ipartdocument.html
    """
    ## Интерфейс IPartDocument

    Интерфейс детали.
    """

    ...


class ITextDocument(IKompasDocument):  # itextdocument.html
    """
    ## Интерфейс ITextDocument

    Интерфейс текстового документа.
    """

    @property
    def BlocksGabarits(self) -> typing.Any:  # itextdocument_blocksgabarits.html
        """
        ## BlocksGabarits - Габариты внутренних областей листов

        Тип данных: VARIANT

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        BlocksGabarits = Object.BlocksGabarits       # Получить свойство (* )
        BlocksGabarits = Object.GetBlocksGabarits()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def BlocksStartLineNumbers(self) -> typing.Any:  # itextdocument_blocksstartlinenumbers.html
        """
        ## BlocksStartLineNumbers - Получить список номеров строк начала страниц

        Тип данных: VARIANT

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        BlocksStartLineNumbers = Object.BlocksStartLineNumbers       # Получить свойство (* )
        BlocksStartLineNumbers = Object.GetBlocksStartLineNumbers()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def SectionsStartLineNumbers(self) -> typing.Any:  # itextdocument_sectionsstartlinenumbers.html
        """
        ## SectionsStartLineNumbers - Получить список номеров строк начала разделов

        Тип данных: VARIANT

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        SectionsStartLineNumbers = Object.SectionsStartLineNumbers       # Получить свойство (* )
        SectionsStartLineNumbers = Object.GetSectionsStartLineNumbers()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def Text(self) -> IText:  # itextdocument_text.html
        """
        ## Text - Текст

        Тип данных: Указатель на интерфейс IText

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        Text = Object.Text       # Получить свойство (* )
        Text = Object.GetText()  # Получить свойство (**)
        ```
        """
        ...

    def Update(self) -> bool:  # itextdocument_update.html
        """
        ## Update - Обновление данных

        ### Синтаксис Automation

        ```
        BOOL Update();
        ```

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...




class ISpecificationDocument(IKompasDocument):  # ispecificationdocument.html
    """
    ## Интерфейс ISpecificationDocument

    Интерфейс документа спецификации.
    """

    @property
    def AttachedDocuments(self) -> IAttachedDocuments:  # ispecificationdocument_attacheddocuments.html
        """
        ## AttachedDocuments - Управление сборкой. Присоединенные документы

        Тип данных: IAttachedDocuments

        Позволяет получать и установить список документов, подключенных в управлении сборкой. Свойство IAttachedDocument_Comment для документов, подключенных через управление сборкой, не используется.

        ### Синтаксис Automation

        ```
        AttachedDocuments = Object.AttachedDocuments       # Получить свойство (* )
        AttachedDocuments = Object.GetAttachedDocuments()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def Crossed(self) -> bool:  # ispecificationdocument_crossed.html
        """
        ## Crossed - Признак необходимости перестроения документа

        Тип данных: BOOL

        Свойство доступно только для чтения.

        Версия Компас v18.1

        ### Синтаксис Automation

        ```
        Crossed = Object.Crossed       # Получить свойство (* )
        Crossed = Object.GetCrossed()  # Получить свойство (**)
        ```
        """
        ...

    def RebuildDocument(self) -> bool:  # ispecificationdocument_rebuilddocument.html
        """
        ## RebuildDocument - Перестроить документ

        ### Синтаксис Automation

        ```
        BOOL RebuildDocument();
        ```

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...




class IKompasDocument1(IKompasAPIObject):  # ikompasdocument1.html
    """
    ## Интерфейс IKompasDocument1

    Дополнительный интерфейс IKompasDocument.

    Интерфейс является дополнительным к интерфейсу документа IKompasDocument. Интерфейс можно получить посредством вызова метода IUnknown::QueryInterface (const GUID far& iid, void** pif).
    """

    @property
    def Author(self) -> bool:  # ikompasdocument1_author.html
        """
        ## Author - Автор

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        Author = Object.Author       # Получить свойство (* )
        Object.Author = Author       # Установить свойство (* )
        Author = Object.GetAuthor()  # Получить свойство (**)
        Object.SetAuthor( Author )   # Установить свойство (**)
        ```
        """
        ...
    @Author.setter
    def Author(self, value: bool): ...

    @property
    def Comment(self) -> bool:  # ikompasdocument1_comment.html
        """
        ## Comment - Комментарий

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        Comment = Object.Comment       # Получить свойство (* )
        Object.Comment = Comment       # Установить свойство (* )
        Comment = Object.GetComment()  # Получить свойство (**)
        Object.SetComment( Author )    # Установить свойство (**)
        ```
        """
        ...
    @Comment.setter
    def Comment(self, value: bool): ...

    @property
    def CreationDate(self) -> float:  # ikompasdocument1_creationdate.html
        """
        ## CreationDate - Получить дату создания документа

        Тип данных: double

        ### Синтаксис Automation

        ```
        CreationDate = Object.CreationDate       # Получить свойство (* )
        Object.CreationDate = CreationDate       # Установить свойство (* )
        CreationDate = Object.GetCreationDate()  # Получить свойство (**)
        Object.SetCreationDate( CreationDate )   # Установить свойство (**)
        ```
        """
        ...

    @property
    def DocumentTypeId(self) -> str:  # ikompasdocument1_documenttypeid.html
        """
        ## DocumentTypeId - Идентификатор типа документа

        Тип данных: BSTR

        ### Синтаксис Automation

        ```
        DocumentTypeId = Object.DocumentTypeId       # Получить свойство (* )
        DocumentTypeId = Object.GetDocumentTypeId()  # Получить свойство (**)
        ```
        """
        ...
    @DocumentTypeId.setter
    def DocumentTypeId(self, value: str): ...

    @property
    def LastChangeDate(self) -> float:  # ikompasdocument1_lastchangedate.html
        """
        ## LastChangeDate - Получить дату последнего изменения документа

        Тип данных: double

        ### Синтаксис Automation

        ```
        LastChangeDate = Object.LastChangeDate       # Получить свойство (* )
        Object.LastChangeDate = LastChangeDate       # Установить свойство (* )
        LastChangeDate = Object.GetLastChangeDate()  # Получить свойство (**)
        Object.SetLastChangeDate( LastChangeDate )   # Установить свойство (**)
        ```
        """
        ...

    @property
    def Metadata(self) -> str:  # ikompasdocument1_metadata.html
        """
        ## Metadata - Метаданные

        Тип данных: BSTR

        ### Синтаксис Automation

        ```
        Metadata = Object.Metadata       # Получить свойство (* )
        Object.Metadata = Metadata       # Установить свойство (* )
        Metadata = Object.GetMetadata()  # Получить свойство (**)
        Object.SetMetadata( Metadata )   # Установить свойство (**)
        ```
        """
        ...
    @Metadata.setter
    def Metadata(self, value: str): ...

    @property
    def OpenVersion(self) -> int:  # ikompasdocument1_openversion.html
        """
        ## OpenVersion - Версия файла, с которой документ был сохранен

        Тип данных: long

        Свойство доступно только для чтения

        Свойство возвращает номер версии Компас, с которой документ был сохранен последний раз в случае открытия документа с диска.

        Если документ сохранен в официальных версиях дистрибутива Компас, свойство вернет значение из перечисления ksSaveDocumentVersionEnum.

        Если документ сохранен в промежуточных версиях, может вернуться числовая версия потока.

        ### Синтаксис Automation

        ```
        OpenVersion = Object.OpenVersion       # Получить свойство (* )
        OpenVersion = Object.GetOpenVersion()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def Organization(self) -> str:  # ikompasdocument1_organization.html
        """
        ## Organization - Организация

        Тип данных: BSTR

        ### Синтаксис Automation

        ```
        Organization = Object.Organization       # Получить свойство (* )
        Object.Organization = Organization       # Установить свойство (* )
        Organization = Object.GetOrganization()  # Получить свойство (**)
        Object.SetOrganization( Organization )   # Установить свойство (**)
        ```
        """
        ...
    @Organization.setter
    def Organization(self, value: str): ...

    def ActivateToolbarSet(self, TolbrSetId) -> bool:  # ikompasdocument1_activatetoolbarset.html
        """
        ## ActivateToolbarSet - Активизировать набор по идентификатору

        ### Синтаксис Automation

        ```
        BOOL ActivateToolbarSet( BSTR TolbrSetId );
        ```

        ### Входные параметры

        +--------------------------------------------------------+
        | TolbrSetId | - cтроковый идентификатор набора панелей. |
        +--------------------------------------------------------+

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def ApplyMetadataFromFile(self, FileName) -> bool:  # ikompasdocument1_applymetadatafromfile.html
        """
        ## ApplyMetadataFromFile - Применить метаданные из файла

        ### Синтаксис Automation

        ```
        BOOL ApplyMetadataFromFile( BSTR FileName );
        ```

        ### Входные параметры

        +-------------------------+
        | FileName | - имя файла. |
        +-------------------------+

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def Attributes(self, Key1, Key2, Key3, Key4, Numb, Objects) -> list[IDispatch]:  # ikompasdocument1_attributes.html
        """
        ## Attributes - Массив атрибутов документа в виде массива SAFEARRAY DISPATCH - VT_ARRAY | VT_DISPATCH

        Тип данных: VARIANT типа VT_ARRAY | VT_DISPATCH

        1. Свойство позволяет получать атрибуты документа. Если возвращается один объект, то тип VARIANT-а будет VT_DISPATCH, если возвращается несколько объектов, то тип VARIANT-а будет VT_ARRAY | VT_DISPATCH.

        2. Свойство доступно только для чтения.

        3. Чтобы получить все объекты с заданным атрибутом, надо в качестве параметра Objects передать VARIANT с типом VT_EMPTY

        4. Чтобы получить атрибуты документа, надо в качестве параметра Objects передать указатель на документ (тип VT_DISPATCH).

        5. Чтобы получить все атрибуты объекта, надо в качестве параметра Numb передать 0.

        6. Если в качестве параметра Objects будет передан массив объектов, то вернутся групповые атрибуты для этих объектов.

        ### Синтаксис Automation

        ```
        Attributes = Object.Attributes( long Key1, long Key2, long Key3, long Key4, double Numb, VARIANT Objects )     # Получить свойство (* )
        Attributes = Object.GetAttributes( long Key1, long Key2, long Key3, long Key4, double Numb, VARIANT Objects )  # Получить свойство (**)
        ```

        ### Входные параметры

        +------------------------------------------------------------------------------------------------------------------------------------------+
        | Key1, Key2, Key3, Key4 | - ключи атрибута,                                                                                               |
        +------------------------------------------------------------------------------------------------------------------------------------------+
        | Numb                   | - уникальный номер типа атрибута,                                                                               |
        +------------------------------------------------------------------------------------------------------------------------------------------+
        | Objects                | - массив VT_ARRAY | VT_DISPATCH объектов, атрибуты которых нужно получить, или указатель на объект VT_DISPATCH. |
        +------------------------------------------------------------------------------------------------------------------------------------------+
        """
        ...

    def CreateAttr(self, AttrID, Libname, Objects) -> IAttribute:  # ikompasdocument1_createattr.html
        """
        ## CreateAttr- Создать атрибут по номеру типа атрибута из библиотеки libname

        Метод позволяет создать атрибут по номеру типа из заданной библиотеки.

        ### Синтаксис Automation

        ```
        LPDISPATCH CreateAttr( double AttrID,
        BSTR Libname,
        VARIANT Objects );
        ```

        ### Входные параметры

        +-----------------------------------------------------------------------------------------------------------------------------------+
        | AttrID  | - уникальный номер типа атрибута,                                                                                       |
        +-----------------------------------------------------------------------------------------------------------------------------------+
        | Libname | - имя библиотеки типов атрибутов,                                                                                       |
        +-----------------------------------------------------------------------------------------------------------------------------------+
        | Objects | - объект или массив объектов, к которым нужно добавить атрибут ( VARIANT типа VT_DISPATCH или VT_ARRAY | VT_DISPATCH ). |
        +-----------------------------------------------------------------------------------------------------------------------------------+

        ### Возвращаемое значение

        указатель на интерфейс атрибута IAttribute.
        """
        ...

    def Delete(self, Objects) -> bool:  # ikompasdocument1_delete.html
        """
        ## Delete - Удалить объект или объекты

        Метод позволяет удалить объект или объекты.

        ### Синтаксис Automation

        ```
        BOOL Delete( VARIANT Objects );
        ```

        ### Входные параметры

        +----------------------------------------------------------------------------------------------------------------------+
        | Objects | - объект или массив объектов, которые нужно удалить (VARIANT типа VT_DISPATCH или VT_ARRAY | VT_DISPATCH). |
        +----------------------------------------------------------------------------------------------------------------------+

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def ExternalFilesNames(self, allFiles) -> list[str]:  # ikompasdocument1_externalfilesnames.html
        """
        ## ExternalFilesNames - Список внешних файлов

        Тип данных: VARIANT типа VT_ARRAY | VT_BSTR

        1. Свойство позволяет получать список внешних файлов.

        2. Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        ExternalFilesNames = Object.ExternalFilesNames       # Получить свойство (* )
        ExternalFilesNames = Object.GetExternalFilesNames()  # Получить свойство (**)
        ```
        """
        ...

    def FindObjectById(self, Id, Parent) -> IKompasAPIObject:  # ikompasdocument1_findobjectbyid.html
        """
        ## FindObjectById - Найти объект по строковому идентификатору

        ### Синтаксис Automation

        ```
        LPDISPATCH FindObjectById( BSTR Id, IKompasAPIObject * Parent );
        ```

        ### Входные параметры

        +--------------------------------------------------------------------------------------------------------------------------------------------------------+
        | Id     | - идентификатор объекта, полученный из функции IKompasDocument1::GetObjectId,                                                                 |
        +--------------------------------------------------------------------------------------------------------------------------------------------------------+
        | Parent | - Владелец объекта, относительно которого нужно сформировать идентификатор. В текущей реализации не используется. Зарезервировано на будущее. |
        +--------------------------------------------------------------------------------------------------------------------------------------------------------+

        Версия Компас v21

        ### Возвращаемое значение

        Указатель на интерфейс объекта IKompasAPIObject.
        """
        ...

    def GetExternalFilesNamesEx(self, allFiles) -> bool:  # ikompasdocument1_getexternalfilesnamesex.html
        """
        ## GetExternalFilesNamesEx - Получить список внешних файлов в виде SAFEARRAY BSTR - (VT_ARRAY | VT_BSTR) и их типов в виде SAFEARRAY long - (VT_ARRAY | VT_I4)

        ### Синтаксис Automation

        ```
        BOOL GetExternalFilesNamesEx( BOOL allFiles, VARIANT * Files, VARIANT * FilesTypes );
        ```

        ### Входные параметры

        +-------------------------------------------------+
        | allFiles | - TRUE - рекурсивно по всем файлам,  |
        +-------------------------------------------------+
        |          | - FALSE - только по внешнему уровню. |
        +-------------------------------------------------+

        ### Выходные параметры

        +-------------------------------------------------------------------------------------------------+
        | Files      | - массив VT_ARRAY | VT_BSTR имен файлов,                                           |
        +-------------------------------------------------------------------------------------------------+
        | FilesTypes | - массив VT_ARRAY | VT_I4 типов файлов (из перечисления ksExternalFilesTypesEnum). |
        +-------------------------------------------------------------------------------------------------+

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def GetInterface(self, Type) -> IKompasAPIObject:  # ikompasdocument1_getinterface.html
        """
        ## GetInterface - Получить вспомогательный интерфейс

        Метод позволяет получить вспомогательный интерфейс.

        В зависимости от значения параметра Type возвращается указатель на интерфейс:

        •в 2D документе:

        •ksObjectCopyObjectParam - параметры копирования ICopyObjectParam

        •ksObjectCurveCopyObjectParam - параметры копирования по кривой ICurveCopyObjectParam

        •ksObjectCircleCopyObjectParam - параметры копирования по окружности ICircleCopyObjectParam

        •ksObjectCircularCopyObjectParam - параметры копирования по концентрической сетке ICircularCopyObjectParam

        •ksObjectMeshCopyObjectParam - параметры копирования по сетке IMeshCopyObjectParam

        •ksObjectFindObjectParameters - интерфейс расширенного поиска объектов IFindObjectParameters

        •в 3D документе:

        •case ksObjectReportProcess - интерфейс для управления процессом Создать отчетIReportProcess

        •ksObjectSaveAsDetailParam - Параметры преобразования в модель ISaveAsDetailParam

        •ksObjectFindObject3DParameters - Параметры поиска объектов 3D IFindObject3DParameters

        •В 2D, 3D и документе - спецификации

        •ksSpecificationObjectCreateParam - Расширенные параметры создания объекта спецификации ISpecificationObjectCreateParam

        ### Синтаксис Automation

        ```
        LPDISPATCH GetInterface( long Type );
        ```

        ### Входные параметры

        +------------------------------------------------------------------+
        | Type | - тип интерфейса из перечисления KompasAPIObjectTypeEnum. |
        +------------------------------------------------------------------+

        ### Возвращаемое значение

        - указатель на интерфейс IKompasAPIObject.
        """
        ...

    def GetObjectId(self, Object, Parent) -> bool:  # ikompasdocument1_getobjectid.html
        """
        ## GetObjectId - Получить строковый идентификатор объекта

        ### Синтаксис Automation

        ```
        BSTR GetObjectId( IKompasAPIObject * Object, IKompasAPIObject * Parent );
        ```

        ### Входные параметры

        +--------------------------------------------------------------------------------------------------------------------------------------------------------+
        | Object | - Указатель на интерфейс объекта IKompasAPIObject,                                                                                            |
        +--------------------------------------------------------------------------------------------------------------------------------------------------------+
        | Parent | - Владелец объекта, относительно которого нужно сформировать идентификатор. В текущей реализации не используется. Зарезервировано на будущее. |
        +--------------------------------------------------------------------------------------------------------------------------------------------------------+

        Версия Компас v21

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def ObjectsByAttr(self, Key1, Key2, Key3, Key4, Numb, ObjectType) -> list[IDispatch]:  # ikompasdocument1_objectsbyattr.html
        """
        ## ObjectsByAttr - Массив объектов документа в виде массива SAFEARRAY DISPATCH - VT_ARRAY | VT_DISPATCH

        Тип данных: VARIANT типа VT_ARRAY | VT_DISPATCH

        1. Свойство позволяет получать объекты с заданным типом атрибута. Если возвращается один объект, то тип VARIANT-а будет VT_DISPATCH, если возвращается несколько объектов, то тип VARIANT-а будет VT_ARRAY | VT_DISPATCH.

        2. Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        Attributes = Object.ObjectsByAttr( long Key1, long Key2, long Key3, long Key4, double Numb, VARIANT ObjectType )  # Получить свойство (* )
        Attributes = Object.ObjectsByAttr( long Key1, long Key2, long Key3, long Key4, double Numb, VARIANT ObjectType )  # Получить свойство (**)
        ```

        ### Входные параметры

        +------------------------------------------------------------+
        | Key1, Key2, Key3, Key4 | - ключи атрибута,                 |
        +------------------------------------------------------------+
        | Numb                   | - уникальный номер типа атрибута, |
        +------------------------------------------------------------+
        | ObjectType             | - тип объектов.                   |
        +------------------------------------------------------------+
        """
        ...

    def RedrawDocument(self, Mode) -> bool:  # ikompasdocument1_redrawdocument.html
        """
        ## RedrawDocument - Перерисовать окна документа

        ### Синтаксис Automation

        ```
        BOOL RedrawDocument( ksRedrawDocumentModeEnum Mode );
        ```

        ### Входные параметры

        +--------------------------------------------+
        | Mode | - режим перерисовки окон документа. |
        +--------------------------------------------+

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def ReplaceExternalFilesNames(self, allFiles, OldFileName, NewFileName) -> bool:  # ikompasdocument1_replaceexternalfilesnames.html
        """
        ## ReplaceExternalFilesNames - Изменить ссылки на файл в документе

        ### Синтаксис Automation

        ```
        BOOL ReplaceExternalFilesNames( BOOL allFiles, BSTR OldFileName, BSTR NewFileName );
        ```

        ### Входные параметры

        +----------------------------------------------------------------------------------------+
        | allFiles    | - TRUE - рекурсивно по всем файлам, - FALSE - только по внешнему уровню, |
        +----------------------------------------------------------------------------------------+
        | OldFileName | - старое имя файла,                                                      |
        +----------------------------------------------------------------------------------------+
        | NewFileName | - тип интерфейса из перечисления KompasAPIObjectTypeEnum.                |
        +----------------------------------------------------------------------------------------+

        Версия: Компас v20

        ### Возвращаемое значение

        +-------------------------------------+
        | TRUE  | - есть локальный результат, |
        +-------------------------------------+
        | FALSE | - в случае неудачи.         |
        +-------------------------------------+
        """
        ...

    def ReportPropertiesMultieditMode(self, On, UpdateProps):  # ikompasdocument1_reportpropertiesmultieditmode.html
        """
        ## ReportPropertiesMultieditMode - Включить/Выключить режим массовой работы со свойствами объектов

        ### Синтаксис Automation

        ```
        BOOL ReportPropertiesMultieditMode( BOOL On, BOOL UpdateProps );
        ```

        ### Входные параметры

        +----------------------------------------------+
        | On          | - включить режим,              |
        +----------------------------------------------+
        | UpdateProps | - применить изменение свойств. |
        +----------------------------------------------+
        """
        ...

    def SaveAsEx(self, PathName, saveMode) -> bool:  # ikompasdocument1_saveasex.html
        """
        ## SaveAsEx - Сохранить документ под другим именем

        ### Синтаксис Automation

        ```
        BOOL SaveAsEx( BSTR PathName, long saveMode );
        ```

        ### Входные параметры

        +-----------------------------------------+
        | PathName | - новое имя файла документа, |
        +-----------------------------------------+
        | saveMode | - версия для сохранения.     |
        +-----------------------------------------+

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def SaveAsToRasterFormat(self, FileName, Param) -> bool:  # ikompasdocument1_saveastorasterformat.html
        """
        ## SaveAsToRasterFormat - Конвертировать окно с входящими дочерними окнами

        Интерфейс IRasterConvertParameters требуется получить с помощью метода IKompasDocument1::GetInterface, используя константу ksObjectRasterConvertParameters.

        Версия Компас v21

        ### Синтаксис Automation

        ```
        BOOL SaveAsToRasterFormat( BSTR FileName, IRasterConvertParameters * Param );
        ```

        ### Входные параметры

        +---------------------------------------------------------------------------------------------+
        | FileName | - имя растрового файла,                                                          |
        +---------------------------------------------------------------------------------------------+
        | Param    | - интерфейс параметров конвертации в растровые форматы IRasterConvertParameters. |
        +---------------------------------------------------------------------------------------------+

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def SetChanged(self, Changed) -> bool:  # ikompasdocument1_setchanged.html
        """
        ## SetChanged - Снять\\Взвести признак изменения документа

        ### Синтаксис Automation

        ```
        BOOL SetChanged( BOOL Changed );
        ```

        ### Входные параметры

        +--------------------------------+
        | Changed | - Признак изменения. |
        +--------------------------------+

        Версия: Компас v20

        ### Возвращаемое значение

        +-------------------------------------+
        | TRUE  | - есть локальный результат, |
        +-------------------------------------+
        | FALSE | - в случае неудачи.         |
        +-------------------------------------+
        """
        ...

    def ViewEditAttr(self, HWnd, Objects) -> bool:  # ikompasdocument1_vieweditattr.html
        """
        ## ViewEditAttr - Открыть диалог для просмотра атрибутов объекта

        Метод позволяет вывести диалог для просмотра атрибутов объекта.

        ### Синтаксис Automation

        ```
        BOOL ViewEditAttr( OLE_HANDLE HWnd,
        VARIANT Objects );
        ```

        ### Входные параметры

        +-------------------------------------------------------------------------------------------------------------------------------------+
        | HWnd    | - дескриптор главного окна КОМПАС (можно получить с помощью функции GetHWindow,                                           |
        +-------------------------------------------------------------------------------------------------------------------------------------+
        | Objects | - объект или массив объектов, атрибуты которых нужно просмотреть ( VARIANT типа VT_DISPATCH или VT_ARRAY | VT_DISPATCH ). |
        +-------------------------------------------------------------------------------------------------------------------------------------+

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def WriteMetadataToFile(self, FileName) -> bool:  # ikompasdocument1_writemetadatatofile.html
        """
        ## WriteMetadataToFile - Записать метаданные в файл

        ### Синтаксис Automation

        ```
        BOOL WriteMetadataToFile( BSTR FileName );
        ```

        ### Входные параметры

        +-------------------------+
        | FileName | - имя файла. |
        +-------------------------+

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...




class IKompasDocument2D1(IKompasAPIObject):  # ikompasdocument2d1.html
    """
    ## Интерфейс IKompasDocument2D1

    Дополнительный интерфейс IKompasDocument2D.

    Интерфейс является дополнительным к интерфейсу документа IKompasDocument2D. Интерфейс можно получить посредством вызова метода IUnknown::QueryInterface (const GUID far& iid, void** pif).
    """

    @property
    def ChooseManager(self) -> IChooseManager:  # ikompasdocument2d1_choosemanager.html
        """
        ## ChooseManager - Менеджер выбора (подсветки) объектов

        Тип данных: указатель на интерфейс менеджера выбора IChooseManager

        1. Свойство позволяет получать указатель на менеджер выбора.

        2. Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        ChooseManager = Object.ChooseManager       # Получить свойство (* )
        ChooseManager = Object.GetChooseManager()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def CurrentGroup(self) -> IDrawingGroup:  # ikompasdocument2d1_currentgroup.html
        """
        ## CurrentGroup - Текущая группа

        Тип данных: указатель на интерфейс группы IDrawingGroup

        1. Свойство позволяет получать указатель на интерфейс текущей группы.

        2. Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        CurrentGroup = Object.CurrentGroup       # Получить свойство (* )
        CurrentGroup = Object.GetCurrentGroup()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def DrawingGroups(self) -> IDrawingGroups:  # ikompasdocument2d1_drawinggroups.html
        """
        ## DrawingGroups - Коллекция групп

        Тип данных: указатель на интерфейс коллекции групп IDrawingGroups

        1. Свойство позволяет получать указатель на коллекцию групп.

        2. Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        DrawingGroups = Object.DrawingGroups       # Получить свойство (* )
        DrawingGroups = Object.GetDrawingGroups()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def EditMacroObject(self) -> IMacroObject:  # ikompasdocument2d1_editmacroobject.html
        """
        ## EditMacroObject - Редактируемый макроэлемент

        Тип данных: указатель на интерфейс макроэлемента IMacroObject

        1. Свойство позволяет получать указатель на редактируемый макроэлемент.

        2. Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        EditMacroObject = Object.EditMacroObject       # Получить свойство (* )
        EditMacroObject = Object.GetEditMacroObject()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def EditMacroVisibleRegime(self) -> bool:  # ikompasdocument2d1_editmacrovisibleregime.html
        """
        ## EditMacroVisibleRegime - Находится ли документ в режиме редактирования макроэлемента

        Тип данных: BOOL

        1. Свойство доступно только для чтения.

        2. Свойство используется при редактировании макроэлементов.

        ### Синтаксис Automation

        ```
        EditMacroVisibleRegime = Object.EditMacroVisibleRegime       # Получить свойство (* )
        EditMacroVisibleRegime = Object.GetEditMacroVisibleRegime()  # Получить свойство (**)
        ```

        ### Возвращаемое значение

        +--------------------------------------------------------+
        | TRUE  | - режим редактирования макроэлемента включен,  |
        +--------------------------------------------------------+
        | FALSE | - режим редактирования макроэлемента отключен. |
        +--------------------------------------------------------+
        """
        ...

    @property
    def EnableUndo(self) -> bool:  # ikompasdocument2d1_enableundo.html
        """
        ## EnableUndo - Включить или отключить добавление команд в Undo

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        EnableUndo = Object.EnableUndo       # Получить свойство (* )
        Object.EnableUndo = EnableUndo       # Установить свойство (* )
        EnableUndo = Object.GetEnableUndo()  # Получить свойство (**)
        Object.SetEnableUndo( EnableUndo )   # Установить свойство (**)
        ```
        """
        ...
    @EnableUndo.setter
    def EnableUndo(self, value: bool): ...

    @property
    def IntervalVariables(self) -> typing.Any:  # ikompasdocument2d1_intervalvariables.html
        """
        ## IntervalVariables - Получить массив интервальных переменных в виде SAFEARRAY VT_DISPATCH

        Тип данных: VARIANT

        Свойство доступно только для чтения.

        Версия: Компас v20

        ### Синтаксис Automation

        ```
        IntervalVariables = Object.IntervalVariables       # Получить свойство (* )
        IntervalVariables = Object.GetIntervalVariables()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def NamedGroups(self) -> IDrawingGroups:  # ikompasdocument2d1_namedgroups.html
        """
        ## NamedGroups - Коллекция именованных групп

        Тип данных: указатель на интерфейс коллекции групп IDrawingGroups

        1. Свойство позволяет получать указатель на коллекцию именованных групп.

        2. Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        NamedGroups = Object.NamedGroups       # Получить свойство (* )
        NamedGroups = Object.GetNamedGroups()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def SelectionManager(self) -> ISelectionManager:  # ikompasdocument2d1_selectionmanager.html
        """
        ## SelectionManager - Менеджер выделенных объектов

        Тип данных: - указатель на интерфейс менеджера выделенных объектов ISelectionManager

        1. Свойство позволяет получать указатель на менеджер выделенных объектов.

        2. Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        SelectionManager = Object.SelectionManager       # Получить свойство (* )
        SelectionManager = Object.GetSelectionManager()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def UndoContainer(self) -> bool:  # ikompasdocument2d1_undocontainer.html
        """
        ## UndoContainer - Включить или отключить объединение команд в Undo

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        UndoContainer = Object.UndoContainer       # Получить свойство (* )
        Object.UndoContainer = UndoContainer       # Установить свойство (* )
        UndoContainer = Object.GetUndoContainer()  # Получить свойство (**)
        Object.SetUndoContainer( UndoContainer )   # Установить свойство (**)
        ```
        """
        ...
    @UndoContainer.setter
    def UndoContainer(self, value: bool): ...

    @property
    def UserFuncVariables(self) -> typing.Any:  # ikompasdocument2d1_userfuncvariables.html
        """
        ## UserFuncVariables - Получить массив интервальных переменных в виде SAFEARRAY VT_DISPATCH

        Тип данных: VARIANT

        Свойство доступно только для чтения.

        Версия: Компас v20

        ### Синтаксис Automation

        ```
        UserFuncVariables = Object.UserFuncVariables       # Получить свойство (* )
        UserFuncVariables = Object.GetUserFuncVariables()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def VariableTable(self) -> IVariableTable:  # ikompasdocument2d1_variabletable.html
        """
        ## VariableTable - Таблица переменных

        Тип данных: указатель на интерфейс таблицы переменных IVariableTable

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        VariableTable = Object.VariableTable       # Получить свойство (* )
        VariableTable = Object.GetVariableTable()  # Получить свойство (**)
        ```
        """
        ...

    def AddIntervalVariable(self, Name, FirstValue, SecondValue) -> IVariable7:  # ikompasdocument2d1_addintervalvariable.html
        """
        ## AddIntervalVariable - Создать интервальную переменную (добавить переменную в массив переменных и документ)

        ### Синтаксис Automation

        ```
        LPDISPATCH AddIntervalVariable( BSTR Name, double FirstValue, double SecondValue );
        ```

        ### Входные параметры

        +--------------------------------------------+
        | Name        | - Имя новой переменной,      |
        +--------------------------------------------+
        | FirstValue  | - Первое значение диапазона, |
        +--------------------------------------------+
        | SecondValue | - Второе значение диапазона. |
        +--------------------------------------------+

        Версия: Компас v20

        ### Возвращаемое значение

        - указатель на интерфейс параметрической переменной IVariable7
        """
        ...

    def AddUserFuncVariable(self, Name, Expression) -> IVariable7:  # ikompasdocument2d1_adduserfuncvariable.html
        """
        ## AddUserFuncVariable - Создать переменную (добавить переменную в массив переменных и документ)

        ### Синтаксис Automation

        ```
        LPDISPATCH AddUserFuncVariable( BSTR Name, BSTR Expression );
        ```

        ### Входные параметры

        +--------------------------------------+
        | Name       | - Имя новой переменной, |
        +--------------------------------------+
        | Expression | - Выражение.            |
        +--------------------------------------+

        Версия: Компас v20

        ### Возвращаемое значение

        - указатель на интерфейс параметрической переменной IVariable7
        """
        ...

    def AddVariable(self, Name, Value, Note) -> IVariable7:  # ikompasdocument2d1_addvariable.html
        """
        ## AddVariable - Создать переменную

        ### Синтаксис Automation

        ```
        LPDISPATCH AddVariable();
        ```

        ### Возвращаемое значение

        - указатель на интерфейс параметрической переменной IVariable7

        Метод позволяет создать переменную (добавить переменную в массив переменных и документ).
        """
        ...

    def ChangeObjectsOrder(self, Objects, OrderObject, OrderType) -> bool:  # ikompasdocument2d1_changeobjectsorder.html
        """
        ## ChangeObjectsOrder - Изменить порядок отрисовки объектов документа

        ### Синтаксис Automation

        ```
        BOOL ChangeObjectsOrder( VARIANT Objects, IDrawingObject * OrderObject, ksChangeOrderType OrderType );
        ```

        ### Входные параметры

        +-----------------------------------------------------------------------------------------------------+
        | Objects     | - список объектов в виде SAFEARRAY'я DISPATCH - VT_ARRAY | VT_DISPATCH,               |
        +-----------------------------------------------------------------------------------------------------+
        | OrderObject | - объект, относительно которого изменяется отрисовка объектов Objects IDrawingObject, |
        +-----------------------------------------------------------------------------------------------------+
        | OrderType   | - тип изменения порядка объектов ksChangeOrderType.                                   |
        +-----------------------------------------------------------------------------------------------------+

        Версия КОМПАС v22

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def ClearUndo(self) -> bool:  # ikompasdocument2d1_clearundo.html
        """
        ## ClearUndo - Очистить очередь Undo

        ### Синтаксис Automation

        ```
        BOOL ClearUndo();
        ```

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+

        Версия Компас v21
        """
        ...

    def CompleteRebuildDocument(self):
        ...

    def CopyObjects(self, Objects, Params) -> list[IDispatch]:  # ikompasdocument2d1_copyobjects.html
        """
        ## CopyObjects - Копировать объекты

        Метод позволяет создать атрибут по номеру типа атрибута.

        Способ копирования зависит от типа интерфейса параметров копирования:

        •ICopyObjectParam - параметры копирования,

        •ICurveCopyObjectParam - параметры копирования по кривой,

        •ICircleCopyObjectParam - параметры копирования по окружности,

        •ICircularCopyObjectParam - параметры копирования по концентрической сетке,

        •IMeshCopyObjectParam - параметры копирования по сетке.

        Нужный интерфейс параметров копирования можно получить с помощью функции IKompasDocument1::GetInterface.

        ### Синтаксис Automation

        ```
        VARIANT CopyObjects( VARIANT Objects,
        LPDISPATCH * params );
        ```

        ### Входные параметры

        +-----------------------------------------------------------------------------------------------------------------+
        | Objects | - объект или массив объектов для копирования ( VARIANT типа VT_DISPATCH или VT_ARRAY | VT_DISPATCH ), |
        +-----------------------------------------------------------------------------------------------------------------+
        | params  | - указатель на интерфейс параметров копирования ICopyObjectParam.                                     |
        +-----------------------------------------------------------------------------------------------------------------+

        ### Возвращаемое значение

        Объект или массив копий объектов ( VARIANT типа VT_DISPATCH или VT_ARRAY | VT_DISPATCH ).
        """
        ...

    def CreateHyperLink(self, Objects, Type, Text, LinkObject, Level) -> bool:  # ikompasdocument2d1_createhyperlink.html
        """
        ## CreateHyperLink - Создать гиперссылку

        ### Синтаксис Automation

        ```
        BOOL CreateHyperLink( VARIANT Objects,
        ksHyperLinkTypeEnum Type,
        BSTR Text,
        IDrawingObject * LinkObject,
        longLevel );
        ```

        ### Входные параметры

        +-------------------------------------------------------------------------------------------+
        | Objects    | - VT_DISPATCH или массив VT_ARRAY | VT_DISPATCH объектов, содержащих ссылку, |
        +-------------------------------------------------------------------------------------------+
        | Type       | - тип ссылки,                                                                |
        +-------------------------------------------------------------------------------------------+
        | Text       | - текст ссылки,                                                              |
        +-------------------------------------------------------------------------------------------+
        | LinkObject | - объект, на который сделана ссылка,                                         |
        +-------------------------------------------------------------------------------------------+
        | Level      | - уровень.                                                                   |
        +-------------------------------------------------------------------------------------------+

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def DeleteHyperLinks(self, Objects) -> bool:  # ikompasdocument2d1_deletehyperlinks.html
        """
        ## DeleteHyperLinks - Удалить гиперссылки

        ### Синтаксис Automation

        ```
        BOOL DeleteHyperLinks( VARIANT Objects );
        ```

        ### Входные параметры

        +----------------------------------------------+
        | Objects | - объекты, содержащие гиперссылку. |
        +----------------------------------------------+

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def DestroyObjects(self, Objects) -> bool:  # ikompasdocument2d1_destroyobjects.html
        """
        ## DestroyObjects - Разрушить объекты

        ### Синтаксис Automation

        ```
        BOOL DestroyObjects( VARIANT Objects );
        ```

        ### Входные параметры

        +-----------------------------------------------------------------------------------+
        | Objects | - cписок объектов в виде SAFEARRAY'я DISPATCH - VT_ARRAY | VT_DISPATCH. |
        +-----------------------------------------------------------------------------------+

        Версия КОМПАС v22

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def FindObject(self, X, Y, Limit, Param) -> IDrawingObject:  # ikompasdocument2d1_findobject.html
        """
        ## FindObject - Найти объект, ближайший к заданной точке, удовлетворяющий параметрам поиска

        Для поиска объектов в точке могут быть заданы расширенные параметры поиска.

        Интерфейс расширенных параметров поиска IFindObjectParameters можно получить с помощью метода IKompasDocument1::GetInterface в 2D - документах при использовании константы ksObjectFindObjectParameters.

        Если расширенные параметры поиска не требуются, необходимо передать NULL в функцию.

        ### Синтаксис Automation

        ```
        LPDISPATCH FindObject( double X, double Y, double Limit, IFindObjectParameters * Param );
        ```

        ### Входные параметры

        +------------------------------------------------------------+
        | X     | - первая координата точки,                         |
        +------------------------------------------------------------+
        | Y     | - вторая координата точки,                         |
        +------------------------------------------------------------+
        | Limit | - предел поиска и найденное расстояние,            |
        +------------------------------------------------------------+
        | Param | - параметры поиска объектов IFindObjectParameters. |
        +------------------------------------------------------------+

        ### Возвращаемое значение

        - указатель на интерфейс объекта IDrawingObject.
        """
        ...

    def FindObjects(self, X, Y, Limit, Param) -> list[IDispatch]:  # ikompasdocument2d1_findobjects.html
        """
        ## FindObjects - Найти объекты в заданной точке, удовлетворяющие параметрам поиска

        Для поиска объектов в точке могут быть заданы расширенные параметры поиска.

        Интерфейс расширенных параметров поиска IFindObjectParameters можно получить с помощью метода IKompasDocument1::GetInterface в 2D - документах при использовании константы ksObjectFindObjectParameters.

        Если расширенные параметры поиска не требуются, необходимо передать NULL в функцию.

        ### Синтаксис Automation

        ```
        VARIANT FindObjects( double X, double Y, double Limit, IFindObjectParameters * Param );
        ```

        ### Входные параметры

        +------------------------------------------------------------+
        | X     | - первая координата точки,                         |
        +------------------------------------------------------------+
        | Y     | - вторая координата точки,                         |
        +------------------------------------------------------------+
        | Limit | - предел поиска и найденное расстояние,            |
        +------------------------------------------------------------+
        | Param | - параметры поиска объектов IFindObjectParameters. |
        +------------------------------------------------------------+

        ### Возвращаемое значение

        +---------------------------------------------------------------------------------------+
        | VARIANT | - объект типа VARIANT на безопасный массив SAFEARRAY: VT_ARRAY|VT_DISPATCH. |
        +---------------------------------------------------------------------------------------+
        """
        ...

    def GetHyperLinkObjects(self, Type, LinkObject, Level, Text) -> list[IDispatch]:  # ikompasdocument2d1_gethyperlinkobjects.html
        """
        ## GetHyperLinkObjects - Получить объекты, имеющие гиперссылку

        ### Синтаксис Automation

        ```
        VARIANT GetHyperLinkObjects( ksHyperLinkTypeEnum Type,
        IDrawingObject * LinkObject,
        long Level,
        BSTR Text );
        ```

        ### Входные параметры

        +---------------------------------------------------+
        | Type       | - тип ссылки,                        |
        +---------------------------------------------------+
        | Text       | - текст ссылки,                      |
        +---------------------------------------------------+
        | LinkObject | - объект, на который сделана ссылка, |
        +---------------------------------------------------+
        | Level      | - уровень.                           |
        +---------------------------------------------------+

        ### Возвращаемое значение

        VT_DISPATCH или массив VT_ARRAY | VT_DISPATCH объектов, содержащих ссылку
        """
        ...

    def GetObjectById(self, Id) -> IDrawingObject:  # ikompasdocument2d1_getobjectbyid.html
        """
        ## GetObjectById - Получить объект по его уникальному идентификатору

        ### Синтаксис Automation

        ```
        LPDISPATCH GetObjectById( int64 Id );
        ```

        ### Входные параметры

        +-------------------------------+
        | Id | - идентификатор объекта. |
        +-------------------------------+

        ### Возвращаемое значение

        - указатель на интерфейс объекта IDrawingObject.
        """
        ...

    def IntervalVariable(self, Index) -> IVariable7:  # ikompasdocument2d1_intervalvariable.html
        """
        ## IntervalVariable - Получить интервальную переменную по имени или индексу

        Тип данных: Указатель на интерфейс IVariable7.

        Свойство доступно только для чтения.

        Версия: Компас v20

        ### Синтаксис Automation

        ```
        IntervalVariable = Object.IntervalVariable( Index )     # Получить свойство (* )
        IntervalVariable = Object.GetIntervalVariable( Index )  # Получить свойство (**)
        ```

        ### Входные параметры

        +------------------------------------------------------------------------------------------------------------------------------------+
        | VARIANT Index | - Индекс объекта в коллекции. Поддерживаются следующие типы: - VT_I4 - индекс объекта, - VT_BSTR - имя переменной. |
        +------------------------------------------------------------------------------------------------------------------------------------+
        """
        ...

    def IsVariableNameValid(self, Name) -> bool:  # ikompasdocument2d1_isvariablenamevalid.html
        """
        ## IsVariableNameValid - Проверить допустимость создания новой переменной с данным именем

        ### Синтаксис Automation

        ```
        BOOL IsVariableNameValid( BSTR Name );
        ```

        ### Входные параметры

        +--------------------------+
        | Name | - имя переменной. |
        +--------------------------+

        ### Возвращаемое значение

        +------------------------------------------------------+
        | TRUE  | - переменную с данным именем создать можно,  |
        +------------------------------------------------------+
        | FALSE | - переменную с данным именем создать нельзя. |
        +------------------------------------------------------+

        Метод позволяет проверить, можно ли создать переменную с данным именем (проверяются синтаксис имени и наличие в документе переменной с данным именем).
        """
        ...

    def LibProcess(self, ProcessType) -> IProcess2D:  # ikompasdocument2d1_libprocess.html
        """
        ## LibProcess - Получить объект процесса

        Тип данных: Указатель на интерфейс IProcess2D

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        LibProcess = Object.LibProcess( ProcessType )     # Получить свойство (* )
        LibProcess = Object.GetLibProcess( ProcessType )  # Получить свойство (**)
        ```
        """
        ...

    def RebuildDocument(self) -> bool:  # ikompasdocument2d1_rebuilddocument.html
        """
        ## RebuildDocument - Перестроить документ

        ### Синтаксис Automation

        ```
        BOOL RebuildDocument();
        ```

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+

        Метод позволяет перестроить документ.
        """
        ...

    def SelectObjects(self, RegionType, XMin, YMin, XMax, YMax) -> list[IDispatch]:  # ikompasdocument2d1_selectobjects.html
        """
        ## SelectObjects - Отобрать объекты рамкой

        ### Синтаксис Automation

        ```
        VARIANT SelectObjects( ksRegionTypeEnum RegionType, double XMin, double YMin, double XMax, double YMax );
        ```

        ### Входные параметры

        +--------------------------------------------------+
        | RegionType | - тип региона,                      |
        +--------------------------------------------------+
        | XMin       | - минимальная граница поиска по x   |
        +--------------------------------------------------+
        | YMin       | - минимальная граница поиска по y,  |
        +--------------------------------------------------+
        | XMax       | - максимальная граница поиска по x, |
        +--------------------------------------------------+
        | YMax       | - максимальная граница поиска по y. |
        +--------------------------------------------------+

        ### Возвращаемое значение

        +---------------------------------------------------------------------------------------+
        | VARIANT | - объект типа VARIANT на безопасный массив SAFEARRAY: VT_ARRAY|VT_DISPATCH. |
        +---------------------------------------------------------------------------------------+
        """
        ...

    def UpdateVariables(self) -> bool:  # ikompasdocument2d1_updatevariables.html
        """
        ## UpdateVariables - Установить новые значения внешних переменных

        ### Синтаксис Automation

        ```
        BOOL UpdateVariables()
        ```

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+

        Метод позволяет применить новые значения внешних переменных.
        """
        ...

    def UserFuncVariable(self, Index) -> IVariable7:  # ikompasdocument2d1_userfuncvariable.html
        """
        ## UserFuncVariable - Получить интервальную переменную по имени

        Тип данных: Указатель на интерфейс IVariable7

        Свойство доступно только для чтения.

        Версия: Компас v20

        ### Синтаксис Automation

        ```
        UserFuncVariable = Object.UserFuncVariable( Index )     # Получить свойство (* )
        UserFuncVariable = Object.GetUserFuncVariable( Index )  # Получить свойство (**)
        ```

        ### Входные параметры

        +------------------------------------------------------------------------------------------------------------------------------------+
        | VARIANT Index | - Индекс объекта в коллекции. Поддерживаются следующие типы: - VT_I4 - индекс объекта, - VT_BSTR - имя переменной. |
        +------------------------------------------------------------------------------------------------------------------------------------+
        """
        ...

    def Variable(self, External, Index) -> IVariable7:  # ikompasdocument2d1_variable.html
        """
        ## Variable - Получить параметрическую переменную по имени, индексу или указателю на размер

        Тип данных: указатель на интерфейс параметрической переменной IVariable7

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        Variable = Object.Variable (VARIANT Index )     # Получить свойство (* )
        Variable = Object.GetVariable (VARIANT Index )  # Получить свойство (**)
        ```

        ### Входные параметры

        +---------------------------------------------------------------------------+
        | Index | - индекс, имя или указатель на размер параметрической переменной. |
        +---------------------------------------------------------------------------+

        Свойство позволяет получать интерфейс параметрической переменной.
        """
        ...

    def Variables(self, ExternalOnly):  # ikompasdocument2d1_variables.html
        ...

    def VariablesCount(self, External) -> int:  # ikompasdocument2d1_variablescount.html
        """
        ## VariablesCount - Получить количество параметрических переменных

        Тип данных: long

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        VariablesCount = Object.VariablesCount       # Получить свойство (* )
        VariablesCount = Object.GetVariablesCount()  # Получить свойство (**)
        ```
        """
        ...




class IKompasDocument3D1(IDispatch):  # ikompasdocument3d1.html
    """
    ## Интерфейс IKompasDocument3D1

    Дополнительный интерфейс для 3D документа.
    """

    @property
    def ComponentPositioner(self) -> IComponentPositioner7:  # ikompasdocument3d1_componentpositioner.html
        """
        ## ComponentPositioner - Интерфейс управления положением компонентов в сборке

        Тип данных: Указатель на интерфейс IComponentPositioner7

        Свойство доступно только для чтения.

        Версия КОМПАС v22

        ### Синтаксис Automation

        ```
        ComponentPositioner = Object.ComponentPositioner       # Получить свойство (* )
        ComponentPositioner = Object.GetComponentPositioner()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def Document3DManager(self) -> IDocument3DManager:  # ikompasdocument3d1_document3dmanager.html
        """
        ## Document3DManager - Менеджер 3D документа

        Тип данных: Указатель на интерфейс IDocument3DManager

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        Document3DManager = Object.Document3DManager       # Получить свойство (* )
        Document3DManager = Object.GetDocument3DManager()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def EditObject(self) -> IFeature7:  # ikompasdocument3d1_editobject.html
        """
        ## EditObject - Редактируемый объект

        Тип данных: Указатель на интерфейс IFeature7

        ### Синтаксис Automation

        ```
        EditObject = Object.EditObject       # Получить свойство (* )
        Object.EditObject = EditObject       # Установить свойство (* )
        EditObject = Object.GetEditObject()  # Получить свойство (**)
        Object.SetEditObject( EditObject )   # Установить свойство (**)
        ```
        """
        ...
    @EditObject.setter
    def EditObject(self, value: IFeature7): ...

    @property
    def HideLayoutGeometry(self) -> bool:  # ikompasdocument3d1_hidelayoutgeometry.html
        """
        ## HideLayoutGeometry - Скрыть / показать компоновочную геометрию

        Тип данных: BOOL

        В зависимости от флага HideInComponentsMode позволяет скрыть или показать все вспомогательные объекты в документе или, если документ является сборкой, во вставках.

        ### Синтаксис Automation

        ```
        HideLayoutGeometry = Object.HideLayoutGeometry       # Получить свойство (* )
        Object.HideLayoutGeometry = HideLayoutGeometry       # Установить свойство (* )
        HideLayoutGeometry = Object.GetHideLayoutGeometry()  # Получить свойство (**)
        Object.SetHideLayoutGeometry( HideLayoutGeometry )   # Установить свойство (**)
        ```
        """
        ...
    @HideLayoutGeometry.setter
    def HideLayoutGeometry(self, value: bool): ...

    @property
    def IntervalVariables(self) -> typing.Any:  # ikompasdocument3d1_intervalvariables.html
        """
        ## IntervalVariables - Получить массив интервальных переменных в виде SAFEARRAY VT_DISPATCH

        Тип данных: VARIANT

        Свойство доступно только для чтения.

        Версия: Компас v20

        ### Синтаксис Automation

        ```
        IntervalVariables = Object.IntervalVariables       # Получить свойство (* )
        IntervalVariables = Object.GetIntervalVariables()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def MateConstraints(self) -> IMateConstraints3D:  # ikompasdocument3d1_mateconstraints.html
        """
        ## MateConstraints - Коллекция сопряжений

        Тип данных: Указатель на интерфейс IMateConstraints3D

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        MateConstraints = Object.MateConstraints       # Получить свойство (* )
        MateConstraints = Object.GetMateConstraints()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def SpecRough(self) -> ISpecRough3D:  # ikompasdocument3d1_specrough.html
        """
        ## SpecRough - Неуказанная шероховатость 3D

        Тип данных: Указатель на интерфейс ISpecRough3D

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        SpecRough = Object.SpecRough       # Получить свойство (* )
        SpecRough = Object.GetSpecRough()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def TreeNeedRebuild(self) -> bool:  # ikompasdocument3d1_treeneedrebuild.html
        """
        ## TreeNeedRebuild - Необходимость перестроения дерева при изменении его состава

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        TreeNeedRebuild = Object.TreeNeedRebuild       # Получить свойство (* )
        Object.TreeNeedRebuild = TreeNeedRebuild       # Установить свойство (* )
        TreeNeedRebuild = Object.GetTreeNeedRebuild()  # Получить свойство (**)
        Object.SetTreeNeedRebuild( TreeNeedRebuild )   # Установить свойство (**)
        ```
        """
        ...
    @TreeNeedRebuild.setter
    def TreeNeedRebuild(self, value: bool): ...

    @property
    def TreeNeedUpdateItems(self) -> bool:  # ikompasdocument3d1_treeneedupdateitems.html
        """
        ## TreeNeedUpdateItems - Необходимость обновление узлов дерева при изменении свойств и состояний объектов документа

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        TreeNeedUpdateItems = Object.TreeNeedUpdateItems       # Получить свойство (* )
        Object.TreeNeedUpdateItems = TreeNeedUpdateItems       # Установить свойство (* )
        TreeNeedUpdateItems = Object.GetTreeNeedUpdateItems()  # Получить свойство (**)
        Object.SetTreeNeedUpdateItems( TreeNeedUpdateItems )   # Установить свойство (**)
        ```
        """
        ...
    @TreeNeedUpdateItems.setter
    def TreeNeedUpdateItems(self, value: bool): ...

    @property
    def UserFuncVariables(self) -> typing.Any:  # ikompasdocument3d1_userfuncvariables.html
        """
        ## UserFuncVariables - Получить массив функциональных переменных в виде SAFEARRAY VT_DISPATCH

        Тип данных: VARIANT

        Свойство доступно только для чтения.

        Версия: Компас v20

        ### Синтаксис Automation

        ```
        UserFuncVariables = Object.UserFuncVariables       # Получить свойство (* )
        UserFuncVariables = Object.GetUserFuncVariables()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def ViewProjectionManager(self) -> IViewProjectionManager:  # ikompasdocument3d1_viewprojectionmanager.html
        """
        ## ViewProjectionManager - Менеджер проекций отображения модели в окне

        Тип данных: Указатель на интерфейс IViewProjectionManager

        Свойство доступно только для чтения.

        Версия КОМПАС v22

        ### Синтаксис Automation

        ```
        ViewProjectionManager = Object.ViewProjectionManager       # Получить свойство (* )
        ViewProjectionManager = Object.GetViewProjectionManager()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def WindowNeedRebuild(self) -> bool:  # ikompasdocument3d1_windowneedrebuild.html
        """
        ## WindowNeedRebuild - Необходимость перерисовки окна документа

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        WindowNeedRebuild = Object.WindowNeedRebuild       # Получить свойство (* )
        Object.WindowNeedRebuild = WindowNeedRebuild       # Установить свойство (* )
        WindowNeedRebuild = Object.GetWindowNeedRebuild()  # Получить свойство (**)
        Object.SetWindowNeedRebuild( WindowNeedRebuild )   # Установить свойство (**)
        ```
        """
        ...
    @WindowNeedRebuild.setter
    def WindowNeedRebuild(self, value: bool): ...

    def AddIntervalVariable(self, Name, FirstValue, SecondValue) -> IVariable7:  # ikompasdocument3d1_addintervalvariable.html
        """
        ## AddIntervalVariable - Создать интервальную переменную (добавить переменную в массив переменных и документ)

        Для новой переменной можно получить интерфейс интервальной переменной IIntervalVariable.

        Версия: Компас v20

        ### Синтаксис Automation

        ```
        LPDISPATCH AddIntervalVariable( BSTR Name, double FirstValue, double SecondValue );
        ```

        ### Входные параметры

        +------------------------------------------------------+
        | Name        | - Имя новой переменной,                |
        +------------------------------------------------------+
        | FirstValue  | - первое граничное значение диапазона, |
        +------------------------------------------------------+
        | SecondValue | - второе граничное значение диапазона. |
        +------------------------------------------------------+

        ### Возвращаемое значение

        - Указатель на интерфейс переменной IVariable7.
        """
        ...

    def AddUserFuncVariable(self, Name, Expression) -> IVariable7:  # ikompasdocument3d1_adduserfuncvariable.html
        """
        ## AddUserFuncVariable - Создать функциональную переменную (добавить переменную в массив переменных и документ)

        ### Синтаксис Automation

        ```
        LPDISPATCH AddUserFuncVariable( BSTR Name, BSTR Expression );
        ```

        ### Входные параметры

        +--------------------------------------+
        | Name       | - Имя новой переменной, |
        +--------------------------------------+
        | Expression | - Выражение.            |
        +--------------------------------------+

        Версия: Компас v20

        ### Возвращаемое значение

        - Указатель на интерфейс переменной IVariable7.
        """
        ...

    def ClearUndo(self) -> bool:  # ikompasdocument3d1_clearundo.html
        """
        ## ClearUndo - Очистить очередь Undo

        ### Синтаксис Automation

        ```
        BOOL ClearUndo();
        ```

        ### Возвращаемое значение

        +-----------------------------------------+
        | TRUE | - в случае успешного завершения. |
        +-----------------------------------------+
        """
        ...

    def CompleteRebuildDocument(self):
        ...

    def ConvertFromAdditionFormat(self, FileName, Param) -> bool:  # ikompasdocument3d1_convertfromadditionformat.html
        """
        ## ConvertFromAdditionFormat - Конвертация документов из дополнительных форматов

        ### Синтаксис Automation

        ```
        BOOL ConvertFromAdditionFormat( BSTR FileName, IAdditionConvertParameters * Param );
        ```

        ### Входные параметры

        +----------------------------------------------------------------+
        | FileName | - имя файла,                                        |
        +----------------------------------------------------------------+
        | Param    | - Параметры конвертации IAdditionConvertParameters. |
        +----------------------------------------------------------------+

        Версия КОМПАС v22

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def ConvertToAdditionFormat(self, FileName, Param) -> bool:  # ikompasdocument3d1_converttoadditionformat.html
        """
        ## ConvertToAdditionFormat - Конвертация документа в дополнительные форматы

        ### Синтаксис Automation

        ```
        BOOL ConvertToAdditionFormat( BSTR FileName, IAdditionConvertParameters * Param );
        ```

        ### Входные параметры

        +----------------------------------------------------------------+
        | FileName | - имя файла,                                        |
        +----------------------------------------------------------------+
        | Param    | - Параметры конвертации IAdditionConvertParameters. |
        +----------------------------------------------------------------+

        Версия КОМПАС v22

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def ExcludeObjects(self, Objects, Excl) -> bool:  # ikompasdocument3d1_excludeobjects.html
        """
        ## ExcludeObjects - Исключить из расчета объект или объекты

        ### Синтаксис Automation

        ```
        BOOL ExcludeObjects( VARIANT Objects, BOOL Excl );
        ```

        ### Входные параметры

        +------------------------------------------------------------------------------+
        | Objects | - объект (VT_DISPATCH) или Список объектов VT_ARRAY | VT_DISPATCH, |
        +------------------------------------------------------------------------------+
        | Excl    | - TRUE - исключить из расчета - FALSE - включить в расчет.         |
        +------------------------------------------------------------------------------+

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def ExecuteProcessOfInsertComponentFromFile(self, FileName, ProcessType) -> bool:  # ikompasdocument3d1_executeprocessofinsertcomponentfromfile.html
        """
        ## ExecuteProcessOfInsertComponentFromFile - Запустить процесс вставки компонента из файла или библиотеки моделей

        ### Синтаксис Automation

        ```
        BOOL ExecuteProcessOfInsertComponentFromFile( BSTR FileName, ProcessTypeEnum ProcessType );
        ```

        ### Входные параметры

        +---------------------------------------------------------------------+
        | FileName                    | - имя файла,                          |
        +---------------------------------------------------------------------+
        | ProcessType                 | - тип процесса,                       |
        +---------------------------------------------------------------------+
        | prAddPartFromFile           | - добавить компонент из файла,        |
        +---------------------------------------------------------------------+
        | prAddLocalPartFromFile      | - добавить локальную деталь из файла, |
        +---------------------------------------------------------------------+
        | prAddLayoutGeometryFromFile | - добавить компоновочную геометрию,   |
        +---------------------------------------------------------------------+
        | prAddBilletPartFromFile     | - добавить деталь-заготовку.          |
        +---------------------------------------------------------------------+

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def FindNearestObject(self, PickRay, FilterParam) -> IModelObject:  # ikompasdocument3d1_findnearestobject.html
        """
        ## FindNearestObject - Найти ближайший объект

        Параметры фильтрации передавать в функцию не обязательно. Если параметры не заданы, то вернется первый ближайший объект.

        Параметры фильтрации позволяют отфильтровывать объекты по типу объекта.

        Также есть возможность подписаться на события ksFindObject3DParametersNotify, что позволяет делать дополнительные проверки подходящих объектов.

        Версия: Компас v20

        ### Синтаксис Automation

        ```
        LPDISPATCH FindNearestObject( VARIANT PickRay, LPDISPATCH FilterParam );
        ```

        ### Входные параметры

        +----------------------------------------------------------------------------------------------+
        | PickRay     | - Параметры вектора взгляда, полученные из функции IDocumentFrame::GetPickRay, |
        +----------------------------------------------------------------------------------------------+
        | FilterParam | - Интерфейс дополнительных параметров фильтрации IFindObject3DParameters.      |
        +----------------------------------------------------------------------------------------------+

        ### Возвращаемое значение

        - Указатель на ближайший найденный объект IModelObject.
        """
        ...

    def FindObjectByAssociationGeometry(self, Geometry) -> IKompasAPIObject:  # ikompasdocument3d1_findobjectbyassociationgeometry.html
        """
        ## FindObjectByAssociationGeometry - Получить 3D объект по геометрии ассоциативного вида

        ### Синтаксис Automation

        ```
        LPDISPATCH FindObjectByAssociationGeometry( LPDISPATCH Geometry );
        ```

        ### Входные параметры

        +------------------------------------------------------------------+
        | Geometry | - Указатель на ассоциативный объект IKompasAPIObject, |
        +------------------------------------------------------------------+

        Версия: Компас v20

        ### Возвращаемое значение

        - Указатель на интерфейс IKompasAPIObject.
        """
        ...

    def IntervalVariable(self, Index) -> IVariable7:  # ikompasdocument3d1_intervalvariable.html
        """
        ## IntervalVariable - Получить интервальную переменную по имени

        Тип данных: Указатель на интерфейс IVariable7.

        Свойство доступно только для чтения.

        Версия: Компас v20

        ### Синтаксис Automation

        ```
        IntervalVariable = Object.IntervalVariable( Index )     # Получить свойство (* )
        IntervalVariable = Object.GetIntervalVariable( Index )  # Получить свойство (**)
        ```

        ### Входные параметры

        +----------------------------------------------------------------------------------------------------------------------------------+
        | VARIANT Index | Индекс объекта в коллекции. Поддерживаются следующие типы: - VT_I4 - индекс объекта, - VT_BSTR - имя переменной. |
        +----------------------------------------------------------------------------------------------------------------------------------+
        """
        ...

    def LibProcess(self, ProcessType) -> IProcess3D:  # ikompasdocument3d1_libprocess.html
        """
        ## LibProcess - Получить объект процесса

        Тип данных: Указатель на интерфейс IProcess3D

        Cвойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        LibProcess = Object.LibProcess( ProcessType )     # Получить свойство (* )
        LibProcess = Object.GetLibProcess( ProcessType )  # Получить свойство (**)
        ```
        """
        ...

    def PickModelObjects(self, PickRay, FilterParam) -> list[IDispatch]:  # ikompasdocument3d1_pickmodelobjects.html
        """
        ## PickModelObjects - Собрать объекты по лучу

        Параметры фильтрации передавать в функцию не обязательно. Если параметры не заданы, то вернутся все найденные объекты, которые можно самостоятельно обработать.

        Параметры фильтрации позволяют отфильтровывать объекты по типу объекта.

        Также есть возможность подписаться на события ksFindObject3DParametersNotify, что позволяет делать дополнительные проверки подходящих объектов.

        Версия: Компас v20

        ### Синтаксис Automation

        ```
        VARIANT PickModelObjects( VARIANT PickRay, LPDISPATCH FilterParam );
        ```

        ### Входные параметры

        +----------------------------------------------------------------------------------------------+
        | PickRay     | - Параметры вектора взгляда, полученные из функции IDocumentFrame::GetPickRay, |
        +----------------------------------------------------------------------------------------------+
        | FilterParam | - Интерфейс дополнительных параметров фильтрации IFindObject3DParameters.      |
        +----------------------------------------------------------------------------------------------+

        ### Возвращаемое значение

        - Массив SafeAray VT_ARRAY | VT_DISPATCH найденных объектов.
        """
        ...

    def SetHiddenObjects(self, Objects, Visible) -> bool:  # ikompasdocument3d1_sethiddenobjects.html
        """
        ## SetHiddenObjects - Скрыть/показать объект или объекты

        ### Синтаксис Automation

        ```
        BOOL SetHiddenObjects( VARIANT Objects, BOOL Visible );
        ```

        ### Входные параметры

        +------------------------------------------------------------------------------+
        | Objects | - объект (VT_DISPATCH) или список объектов VT_ARRAY | VT_DISPATCH, |
        +------------------------------------------------------------------------------+
        | Visible | - TRUE - Отобразить объект, - FALSE - Скрыть объект.               |
        +------------------------------------------------------------------------------+

        Версия: Компас v20

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def UserFuncVariable(self, Index) -> IVariable7:  # ikompasdocument3d1_userfuncvariable.html
        """
        ## UserFuncVariable - Получить функциональную переменную по имени

        Тип данных: Указатель на интерфейс IVariable7.

        Свойство доступно только для чтения.

        Версия: Компас v20

        ### Синтаксис Automation

        ```
        UserFuncVariable = Object.UserFuncVariable( Index )     # Получить свойство (* )
        UserFuncVariable = Object.GetUserFuncVariable( Index )  # Получить свойство (**)
        ```

        ### Входные параметры

        +-------------------------------------------------------------------------------------------------------------------------------------+
        | VARIANT Index- | - Индекс объекта в коллекции. Поддерживаются следующие типы: - VT_I4 - индекс объекта, - VT_BSTR - имя переменной. |
        +-------------------------------------------------------------------------------------------------------------------------------------+
        """
        ...




class IChooseManager(IKompasAPIObject):  # ichoosemanager.html
    """
    ## Интерфейс IChooseManager

    Интерфейс менеджера выбора (подсветки) объектов.

    Интерфейс позволяет осуществлять выбор (подсветку) объектов. Используется при создании отчета по выбранным объектам.

    Интерфейс является дополнительным для интерфейса таблицы отчетов IReport. Интерфейс можно получить посредством вызова метода IUnknown::QueryInterface (const GUID far& iid, void** pif) и у интерфейса документа 3D с помощью свойства IKompasDocument3D::ChooseManager, а также у дополнительного интерфейса документа 2D с помощью свойства IKompasDocument2D1::ChooseManager.
    """

    @property
    def ChoosenObjects(self) -> list[IDispatch]:  # ichoosemanager_choosenobjects.html
        """
        ## ChoosenObjects - Получить массив выделенных объектов в виде SAFEARRAY | VT_DISPATCH.

        Тип данных: VARIANT типа VT_ARRAY | VT_DISPATCH

        1. Свойство позволяет получать массив выделенных объектов. Если возвращается один объект, то тип VARIANT-а будет VT_DISPATCH, если возвращается несколько объектов, то тип VARIANT-а будет VT_ARRAY | VT_DISPATCH.

        2. Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        ChoosenObjects = Object.ChoosenObjects       # Получить свойство (* )
        ChoosenObjects = Object.GetChoosenObjects()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def CurrentManagerType(self) -> int:  # ichoosemanager_currentmanagertype.html
        """
        ## CurrentManagerType - Получить тип текущего менеджера

        Тип данных: из перечисления ksChooseManagerTypeEnum

        ### Синтаксис Automation

        ```
        CurrentManagerType = Object.CurrentManagerType       # Получить свойство (* )
        Object.CurrentManagerType = CurrentManagerType       # Установить свойство (* )
        CurrentManagerType = Object.GetCurrentManagerType()  # Получить свойство (**)
        Object.SetCurrentManagerType( CurrentManagerType )   # Установить свойство (**)
        ```
        """
        ...
    @CurrentManagerType.setter
    def CurrentManagerType(self, value: int): ...

    def Choose(self, Objects) -> bool:  # ichoosemanager_choose.html
        """
        ## Choose - Выделить объект

        Метод позволяет выделить объект.

        ### Синтаксис Automation

        ```
        BOOL Choose(VARIANT Objects);
        ```

        ### Входные параметры

        +-----------------------------------------------------------------------------------------------------------------------+
        | Objects | - объект или массив объектов, которые нужно выделить (VARIANT типа VT_DISPATCH или VT_ARRAY | VT_DISPATCH). |
        +-----------------------------------------------------------------------------------------------------------------------+

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def ChooseColor(self, ManagerType) -> int:  # ichoosemanager_choosecolor.html
        """
        ## ChooseColor - Цвет выбора

        Тип данных: long

        ### Синтаксис Automation

        ```
        ChooseColor = Object.ChooseColor( ManagerType )     # Получить свойство (* )
        Object.ChooseColor( ManagerType ) = ChooseColor     # Установить свойство (* )
        ChooseColor = Object.GetChooseColor( ManagerType )  # Получить свойство (**)
        Object.SetChooseColor( ManagerType, ChooseColor )   # Установить свойство (**)
        ```

        ### Входные параметры

        +-------------------------------------------------------------------------------------+
        | ManagerType | - индекс менеджера подсветки из перечисления ksChooseManagerTypeEnum. |
        +-------------------------------------------------------------------------------------+

        Версия КОМПАС v22
        """
        ...

    def GetManagerIndex(self, Object) -> int:  # ichoosemanager_getmanagerindex.html
        """
        ## GetManagerIndex - Получить индекс менеджера по указателю на выбранный объект

        ### Синтаксис Automation

        ```
        ksChooseManagerTypeEnum GetManagerIndex (LPDISPATCH Object);
        ```

        ### Входные параметры

        +-------------------------------------------+
        | Object | - указатель на выбранный объект. |
        +-------------------------------------------+

        ### Возвращаемое значение

        тип текущего менеджера из перечисления ksChooseManagerTypeEnum
        """
        ...

    def IsChoosen(self, Object) -> bool:  # ichoosemanager_ischoosen.html
        """
        ## IsChoosen - Выделен ли объект

        Метод позволяет определить, выделен ли объект.

        ### Синтаксис Automation

        ```
        BOOL IsChoosen( LPDISPATCH Object );
        ```

        ### Входные параметры

        +---------------------+
        | Objects | - объект. |
        +---------------------+

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def SetChooseColor(self, ManagerType, arg1):
        ...

    def Unchoose(self, Objects) -> bool:  # ichoosemanager_unchoose.html
        """
        ## Unchoose - Снять выделение объекта

        Метод позволяет снять выделение объекта.

        ### Синтаксис Automation

        ```
        BOOL Unchoose( VARIANT Objects );
        ```

        ### Входные параметры

        +----------------------------------------------------------------------------------------------------------------------------------+
        | Objects | - объект или массив объектов, с которых нужно снять выделение ( VARIANT типа VT_DISPATCH или VT_ARRAY | VT_DISPATCH ). |
        +----------------------------------------------------------------------------------------------------------------------------------+

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def UnchooseAll(self) -> bool:  # ichoosemanager_unchooseall.html
        """
        ## UnchooseAll - Снять выделение всех объектов

        Метод позволяет снять выделение со всех объектов.

        ### Синтаксис Automation

        ```
        BOOL UnchooseAll();
        ```

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...




class ISelectionManager(IKompasAPIObject):  # iselectionmanager.html
    """
    ## Интерфейс ISelectionManager

    Интерфейс менеджера выделенных объектов.

    Интерфейс позволяет осуществлять выделение объектов.

    Интерфейс можно получить у интерфейса документа 3D с помощью свойства IKompasDocument3D::SelectionManager и у дополнительного интерфейса документа 2D с помощью свойства IKompasDocument2D1::SelectionManager.
    """

    @property
    def SelectedObjects(self) -> list[IDispatch]:  # iselectionmanager_selectedobjects.html
        """
        ## SelectedObjects - Получить массив выделенных объектов в виде SAFEARRAY | VT_DISPATCH

        Тип данных: VARIANT типа VT_ARRAY | VT_DISPATCH

        1. Свойство позволяет получать массив выделенных объектов. Если возвращается один объект, то тип VARIANT-а будет VT_DISPATCH, если возвращается несколько объектов, то тип VARIANT-а будет VT_ARRAY | VT_DISPATCH.

        2. Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        SelectedObjects = Object.SelectedObjects       # Получить свойство (* )
        SelectedObjects = Object.GetSelectedObjects()  # Получить свойство (**)
        ```
        """
        ...

    def IsSelected(self, Object) -> bool:  # iselectionmanager_isselected.html
        """
        ## IsSelected - Выделен ли объект

        Метод позволяет определить, выделен ли объект.

        ### Синтаксис Automation

        ```
        BOOL IsSelected( LPDISPATCH Object );
        ```

        ### Входные параметры

        +---------------------+
        | Objects | - объект. |
        +---------------------+

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def Select(self, Objects) -> bool:  # iselectionmanager_select.html
        """
        ## Select - Выделить объект

        Метод позволяет выделить объект.

        ### Синтаксис Automation

        ```
        BOOL Select( VARIANT Objects );
        ```

        ### Входные параметры

        +-------------------------------------------------------------------------------------------------------------------------+
        | Objects | - объект или массив объектов, которые нужно выделить ( VARIANT типа VT_DISPATCH или VT_ARRAY | VT_DISPATCH ). |
        +-------------------------------------------------------------------------------------------------------------------------+

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def Unselect(self, Objects) -> bool:  # iselectionmanager_unselect.html
        """
        ## Unselect - Снять выделение объекта

        Метод позволяет снять выделение объекта.

        ### Синтаксис Automation

        ```
        BOOL Unselect( VARIANT Objects );
        ```

        ### Входные параметры

        +----------------------------------------------------------------------------------------------------------------------------------+
        | Objects | - объект или массив объектов, с которых нужно снять выделение ( VARIANT типа VT_DISPATCH или VT_ARRAY | VT_DISPATCH ). |
        +----------------------------------------------------------------------------------------------------------------------------------+

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def UnselectAll(self) -> bool:  # iselectionmanager_unselectall.html
        """
        ## UnselectAll - Снять выделение всех объектов

        Метод позволяет снять выделение со всех объектов.

        ### Синтаксис Automation

        ```
        BOOL UnselectAll();
        ```

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...




class IDocumentFrame(IKompasAPIObject):  # idocumentframe.html
    """
    ## Интерфейс IDocumentFrame

    Интерфейс окна документа.

    1. Данный интерфейс может быть получен от интерфейса окон документа IDocumentFrames с помощью свойства IDocumentFrames::Item.

    2. У данного интерфейса посредством вызова метода IUnknown::QueryInterface (const GUID far& IID, void** pif) можно получить дополнительный интерфейс IFrameTreesManager.
    """

    @property
    def Active(self) -> bool:  # idocumentframe_active.html
        """
        ## Active - Активность окна

        Тип данных: BOOL

        С помощью данного свойства можно получить и изменить активность данного окна.

        ### Синтаксис Automation

        ```
        Active = iObject.Active       # Получить свойство (* )
        iObject.Active = Active       # Установить свойство (* )
        Active = iObject.GetActive()  # Получить свойство (**)
        iObject.SetActive (Active)    # Установить свойство (**)
        ```
        """
        ...
    @Active.setter
    def Active(self, value: bool): ...

    @property
    def Caption(self) -> str:  # idocumentframe_caption.html
        """
        ## Caption - Заголовок окна

        Тип данных: BSTR (строка)

        1. С помощью данного свойства можно получить заголовок данного окна.

        2. Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        Caption = iObject.Caption       # Получить свойство (* )
        Caption = iObject.GetCaption()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def CurrentCursorStep(self) -> bool:  # idocumentframe_currentcursorstep.html
        """
        ## CurrentCursorStep - Текущий шаг дискретного перемещения курсора

        Тип данных: BOOL

        При включенном режиме округления IDocumentFrame::RoundModeOn значения параметров округляются до ближайшего значения, кратного текущему шагу курсора.

        ### Синтаксис Automation

        ```
        CurrentCursorStep = Object.CurrentCursorStep( )    # Получить свойство (* )
        CurrentCursorStep = Object.GetCurrentCursorStep()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def Regime(self) -> int:  # idocumentframe_regime.html
        """
        ## Regime - Pежим отображения окна

        Тип данных: режим отображения окна из перечисления FrameRegimeEnum

        С помощью данного свойства можно получить и изменить режим отображения данного окна.

        ### Синтаксис Automation

        ```
        Regime = iObject.Regime       # Получить свойство (* )
        iObject.Regime = Regime       # Установить свойство (* )
        Regime = iObject.GetRegime()  # Получить свойство (**)
        iObject.SetRegime (Regime)    # Установить свойство (**)
        ```
        """
        ...
    @Regime.setter
    def Regime(self, value: int): ...

    @property
    def RoundModeOn(self) -> bool:  # idocumentframe_roundmodeon.html
        """
        ## RoundModeOn - Состояние режима округления линейных величин до значений, кратных шагу курсора

        Тип данных: BOOL

        При включенном режиме значения параметров округляются до ближайшего значения, кратного текущему шагу курсора IDocumentFrame::CurrentCursorStep.

        ### Синтаксис Automation

        ```
        RoundModeOn = Object.RoundModeOn( )    # Получить свойство (* )
        RoundModeOn = Object.GetRoundModeOn()  # Получить свойство (**)
        ```
        """
        ...

    def ConvertCoordinates(self, Type, LX, LY) -> bool:  # idocumentframe_convertcoordinates.html
        """
        ## ConvertCoordinates - Преобразовать оконные координаты в координаты СК согласно ConvertCoordTypeEnum

        1. Получив координаты мыши в оконных координатах при обработке событий из ksDocumentFrameNotify:

        +------------------------------------------------+
        | frMouseDown     | 3  | Нажатие кнопки мыши.    |
        +------------------------------------------------+
        | frMouseUp       | 4  | Отпускание кнопки мыши. |
        +------------------------------------------------+
        | frMouseDblClick | 5  | Двойной щелчок мыши.    |
        +------------------------------------------------+
        | frMouseMove     | 12 | Перемещение мыши.       |
        +------------------------------------------------+

        их можно преобразовать в системы координат, указанные в ConvertCoordTypeEnum:

        •для графических документов можно получить координаты в СК листа и текущего вида,

        •для СП и текстового документа в СК первого листа,

        •для модели в СК документа и в СК текущей плоской грани. Грань должна быть выделена в дереве.

        2. Если курсор находится в нерабочей области окна активного документа, то конвертация координат не производится, возвращается FALSE. Нерабочей областью окна является, например, панель команд или панель свойств в этом же активном документе.

        См. также ConvertCoordTypeEnum

        ### Синтаксис Automation

        ```
        BOOL ConvertCoordinates (ConvertCoordTypeEnum type,
        long lx,
        long ly,
        double* x,
        double* y,
        double* z);
        ```

        ### Входные параметры

        +---------------------------------------------------------------------+
        | lx, ly | - координаты в экранных пикселах,                          |
        +---------------------------------------------------------------------+
        | type   | - тип СК в которую нужно преобразовать оконные координаты. |
        +---------------------------------------------------------------------+

        ### Выходные параметры

        +-----------------------------------------------------------------+
        | x, y, z | - координаты в мм в СК согласно ConvertCoordTypeEnum. |
        +-----------------------------------------------------------------+

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - преобразование прошло успешно, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def ExecuteKompasCommand(self, CommandID, PostMessage) -> bool:  # idocumentframe_executekompascommand.html
        """
        ## ExecuteKompasCommand - Выполнить команду системы КОМПАС

        1. Проверить доступность команды можно с помощью функции IDocumentFrame::IsKompasCommandEnable.

        2. Проверить, нажата ли в данный момент кнопка команды, можно с помощью IDocumentFrame::IsKompasCommandCheck.

        ### Синтаксис Automation

        ```
        BOOL ExecuteKompasCommand (long commandID, BOOL post);
        ```

        ### Входные параметры

        +-------------------------------------------------------------------------------------+
        | commandID | - константа из перечисления ProcessTypeEnum или ksKompasCommandEnum,    |
        +-------------------------------------------------------------------------------------+
        | post      | - true - запуск команды через PostMessage, - false - через SendMessage. |
        +-------------------------------------------------------------------------------------+

        ### Возвращаемое значение

        +----------------------------+
        | TRUE  | - в случае удачи,  |
        +----------------------------+
        | FALSE | - в случае ошибки. |
        +----------------------------+
        """
        ...

    def GetFPSData(self):
        ...

    def GetHWND(self):  # idocumentframe_gethwnd.html
        """
        ## GetHWND - Получить дескриптор окна

        ### Синтаксис Automation

        ```
        long GetHWND();
        ```

        ### Возвращаемое значение

        Дескриптор окна
        """
        ...

    def GetPickRay(self, wx, wy) -> bool:  # idocumentframe_getpickray.html
        """
        ## GetPickRay - Преобразовать оконные координаты в луч взгляда

        Луч смотрит перпендикулярно экрану. Луч строится с учетом перспективной проекции, если она включена.

        ### Синтаксис Automation

        ```
        BOOL GetPickRay ( long lx, long ly,
        double* x, double* y, double* z,
        double* zx, double* zy, double* zz);
        ```

        ### Входные параметры

        +--------------------------------------------+
        | lx, ly | - координаты в экранных пикселах. |
        +--------------------------------------------+

        ### Выходные параметры

        +-----------------------------------------------------------------------------------------------+
        | x, y, z    | - координаты точки на ближайший к лучу взгляда объект, в координатах компонента, |
        +-----------------------------------------------------------------------------------------------+
        | zx, zy, zz | - направления вектора взгляда.                                                   |
        +-----------------------------------------------------------------------------------------------+

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - преобразование прошло успешно, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def GetWorkAreaRect(self) -> bool:  # idocumentframe_getworkarearect.html
        """
        ## GetWorkAreaRect - Размеры свободной рабочей области (за вычетом быстрой панели)

        ### Синтаксис Automation

        ```
        BOOL GetWorkAreaRect( long * Left, long * Top, long * Right, long * Bottom );
        ```

        ### Выходные параметры

        +------------------------------------------------------------------+
        | Left, Top, Right, Bottom | - Экранные координаты габаритов окна. |
        +------------------------------------------------------------------+

        Версия: Компас v20

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def GetZoomScale(self):  # idocumentframe_getzoomscale.html
        """
        ## GetZoomScale - Получить масштаб и центр окна документа

        1. Метод позволяет получить текущий масштаб отображения документа в окне с центром в точке х, у (точка х, у в центре окна).

        2. Метод работает только для графических документов.

        ### Синтаксис Automation

        ```
        void GetZoomScale (double* x, double* y, double* scale);
        ```

        ### Выходные параметры

        +-------------------------------------------------------------------+
        | x, y  | - координаты (в СК вида) точки центра изменения масштаба, |
        +-------------------------------------------------------------------+
        | scale | - коэффициент изменения масштаба изображения.             |
        +-------------------------------------------------------------------+
        """
        ...

    def IsKompasCommandCheck(self, CommandID):  # idocumentframe_iskompascommandcheck.html
        """
        ## IsKompasCommandCheck - Проверить, нажата ли кнопка команды

        ### Синтаксис Automation

        ```
        long IsKompasCommandCheck (long commandID);
        ```

        ### Входные параметры

        +----------------------------------------------------------------------------------+
        | commandID | - константа из перечисления ProcessTypeEnum или ksKompasCommandEnum. |
        +----------------------------------------------------------------------------------+

        ### Возвращаемое значение

        +----------------------+
        | 1 | - кнопка нажата, |
        +----------------------+
        | 0 | - кнопка отжата. |
        +----------------------+
        """
        ...

    def IsKompasCommandEnable(self, CommandID) -> bool:  # idocumentframe_iskompascommandenable.html
        """
        ## IsKompasCommandEnable - Проверить доступность выполнения команды

        ### Синтаксис Automation

        ```
        BOOL IsKompasCommandEnable (long commandID);
        ```

        ### Входные параметры

        +----------------------------------------------------------------------------------+
        | commandID | - константа из перечисления ProcessTypeEnum или ksKompasCommandEnum. |
        +----------------------------------------------------------------------------------+

        ### Возвращаемое значение

        +-------------------------------+
        | TRUE  | - команда доступна,   |
        +-------------------------------+
        | FALSE | - команда недоступна. |
        +-------------------------------+
        """
        ...

    def RefreshWindow(self):  # idocumentframe_refreshwindow.html
        """
        ## RefreshWindow - Обновить изображение окна

        ### Синтаксис Automation

        ```
        void RefreshWindow();
        ```
        """
        ...

    def SetGabaritModifying(self):  # idocumentframe_setgabaritmodifying.html
        """
        ## SetGabaritModifying - Сообщить окну документа, что библиотека меняет габарит документа

        Использование метода может понадобиться для собственного рисования в окне. Если габарит изменен, то это приведет к генерации события frAddGabarit (определение габаритов документа) из ksDocumentFrameNotify. В обработчике события frAddGabarit библиотека может указать свой габаритный прямоугольник, если документ является чертежом или фрагментом или свой габаритный куб, если документ является моделью. Значение габарита библиотеки сложится с габаритом документа.

        ### Синтаксис Automation

        ```
        void SetGabaritModifying();
        ```
        """
        ...

    def SetZoomScale(self, X, Y, Scale, ViewParam) -> bool:  # idocumentframe_setzoomscale.html
        """
        ## SetZoomScale - Растянуть изображение окна

        ### Синтаксис Automation

        ```
        BOOL SetZoomScale( double X, double Y, double Scale, BOOL ViewParam );
        ```

        ### Входные параметры

        +----------------------------------------------------------------------------------------------------------------------------------+
        | X, Y      | - Координаты точки центра,                                                                                           |
        +----------------------------------------------------------------------------------------------------------------------------------+
        | Scale     | - Масштаб,                                                                                                           |
        +----------------------------------------------------------------------------------------------------------------------------------+
        | ViewParam | - TRUE - координаты заданы в системе координат текущего вида, - FALSE - координаты заданы в системе координат листа. |
        +----------------------------------------------------------------------------------------------------------------------------------+

        Версия: Компас v20

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def UnConvertCoordinates(self, Type, X, Y, Z) -> bool:  # idocumentframe_unconvertcoordinates.html
        """
        ## UnConvertCoordinates - Преобразовать координаты документа в логические координаты экрана

        ### Синтаксис Automation

        ```
        BOOL UnConvertCoordinates( ConvertCoordTypeEnum Type, double X, double Y, double Z, long * LX, long * LY );
        ```

        ### Входные параметры

        +------------------------------------------------------------------+
        | Type    | - тип СК, в которой передаются координаты,             |
        +------------------------------------------------------------------+
        | X, Y, Z | - координаты в мм в СК, согласно ConvertCoordTypeEnum. |
        +------------------------------------------------------------------+

        ### Выходные параметры

        +--------------------------------------------+
        | LX, LY | - координаты в экранных пикселах. |
        +--------------------------------------------+

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def Zoom(self, X1, Y1, X2, Y2):  # idocumentframe_zoom.html
        """
        ## Zoom - Увеличить масштаб окна рамкой

        1. Метод позволяет растянуть часть документа, заданную рамкой, во все окно.

        2. Метод работает только для графических документов.

        ### Синтаксис Automation

        ```
        void Zoom (double x1, double y1, double x2, double y2);
        ```

        ### Входные параметры

        +-----------------------------------------------------------------+
        | x1, y1 | - координаты (в СК вида) первой точки диагонали рамки, |
        +-----------------------------------------------------------------+
        | x2, y2 | - координаты (в СК вида) второй точки диагонали рамки. |
        +-----------------------------------------------------------------+
        """
        ...

    def ZoomPrevNextOrAll(self, Type):  # idocumentframe_zoomprevnextorall.html
        """
        ## ZoomPrevNextOrAll - Отобразить предыдущий/следующий масштаб или показать весь документ

        1. Установка следующего масштаба возможно только после отката к предыдущему масштабу.

        2. Метод работает в полном объеме только для графических документов. Для документов-моделей работает только режим ksZoomAll (Показать весь документ).

        3. В текстовом документе и в спецификации метод не работает.

        ### Синтаксис Automation

        ```
        void ZoomPrevNextOrAll (long Type);
        ```

        ### Входные параметры

        +---------------------------------------------------------------+
        | type | - тип изменения масштаба из перечисления ZoomTypeEnum. |
        +---------------------------------------------------------------+
        """
        ...

    def ZoomScale(self, X, Y, Scale):  # idocumentframe_zoomscale.html
        """
        ## ZoomScale - Изменить масштаб изображения окна

        1. Метод позволяет установить новый масштаб отображения документа в окне с центром в точке х,у.

        2. Метод работает только для графических документов.

        ### Синтаксис Automation

        ```
        void ZoomScale (double x, double y, double scale);
        ```

        ### Входные параметры

        +-------------------------------------------------------------------+
        | x, y  | - координаты (в СК вида) точки центра изменения масштаба, |
        +-------------------------------------------------------------------+
        | scale | - коэффициент изменения масштаба изображения.             |
        +-------------------------------------------------------------------+
        """
        ...




class IDocumentFrames(IKompasCollection):  # idocumentframes.html
    """
    ## Интерфейс IDocumentFrames

    Коллекция окон документа.

    Данный интерфейс может быть получен от интерфейса документа IKompasDocument с помощью свойства IKompasDocument::DocumentFrames.
    """

    def Item(self, Index) -> IDocumentFrame:  # idocumentframes_item.html
        """
        ## Item - Окно, заданное по индексу

        Тип данных: указатель на интерфейс окна IDocumentFrame

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        frame = iObject.Item (Index)     # Получить свойство (* )
        frame = iObject.GetItem (Index)  # Получить свойство (**)
        ```

        ### Входные параметры

        +--------------------------------------------+
        | Index | - индекс в коллекции, тип VARIANT. |
        +--------------------------------------------+
        """
        ...




class IFrameTreesManager(IDispatch):  # iframetreesmanager.html
    """
    ## Интерфейс IFrameTreesManager

    Интерфейс менеджера вкладок окна документа.

    Дополнительный интерфейс для IDocumentFrame.

    Данный интерфейс можно получить у интерфейса IDocumentFrame посредством вызова метода IUnknown::QueryInterface (const GUID far& IID, void** pif).

    1. Позволяет создавать вкладку в окне дерева документа.

    2. Добавить вкладку можно в окно чертежа, 3D модели и сборки.

    3. Для создания закладки используется метод AddTab.
    """

    @property
    def ActiveTab(self) -> IDispatch:  # iframetreesmanager_activetab.html
        """
        ## ActiveTab - Активная закладка

        Тип данных: IDispatch

        1. Позволяет получить и установить активность вкладки окна дерева документа.

        2. Для активизации закладки дерева документа системы КОМПАС нужно передать NULL.

        3. Для активизации закладки ActiveX библиотеки нужно передать указатель на интерфейс созданного функцией AddTab ActiveX.

        ### Синтаксис Automation

        ```
        ActiveTab = Object.ActiveTab       # Получить свойство (* )
        Object.ActiveTab = ActiveTab       # Установить свойство (* )
        ActiveTab = Object.GetActiveTab()  # Получить свойство (**)
        Object.SetActiveTab( ActiveTab )   # Установить свойство (**)
        ```
        """
        ...
    @ActiveTab.setter
    def ActiveTab(self, value: IDispatch): ...

    @property
    def TabsVisible(self) -> bool:  # iframetreesmanager_tabsvisible.html
        """
        ## TabsVisible - Видимость вкладок документа

        Тип данных: BOOL

        Позволяет получить и установить видимость окна дерева документа.

        ### Синтаксис Automation

        ```
        TabsVisible = Object.TabsVisible       # Получить свойство (* )
        Object.TabsVisible = TabsVisible       # Установить свойство (* )
        TabsVisible = Object.GetTabsVisible()  # Получить свойство (**)
        Object.SetTabsVisible( TabsVisible )   # Установить свойство (**)
        ```
        """
        ...
    @TabsVisible.setter
    def TabsVisible(self, value: bool): ...

    def AddTab(self, TabCaption, OcxClassID):  # iframetreesmanager_addtab.html
        """
        ## AddTab - Добавить вкладку

        ### Синтаксис Automation

        ```
        LPDISPATCH AddTab( BSTR tabCaption, BSTR ocxProgID );
        ```

        ### Входные параметры

        +------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------+
        | tabCaption | - заголовок для новой закладки,                                                                                                                                                                                       |
        +------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------+
        | ocxProgID  | - ProgID - ActiveX элемента (ActiveX дерева ); Например, «VCTree.VCTreeCtrl.1» - для создания дерева, из примеров SDK. см SDK\\lib\\VCTree.ocx, проект SDK\\OCX\\VCTree. Пример использования SDK\\C++\\Visualc\\TestVCTree. |
        +------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------+

        ### Возвращаемое значение

        - Указатель на интерфейс созданного ActiveX элемента.
        """
        ...

    def AddTabEx(self, TabCaption, TreeCaption, OcxClassID, Active, Layout):  # iframetreesmanager_addtabex.html
        """
        ## AddTabEx - Добавить закладку

        При повторном создании закладки она будет создана в том же положении, где ее разместил пользователь.

        ### Синтаксис Automation

        ```
        LPDISPATCH AddTabEx( BSTR TabCaption, BSTR TreeCaption, BSTR OcxClassID, BOOL Active, PropertyManagerLayout Layout );
        ```

        ### Входные параметры

        +------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------+
        | tabCaption  | - имя закладки,                                                                                                                                                                                                      |
        +------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------+
        | TreeCaption | - заголовок дерева ,                                                                                                                                                                                                 |
        +------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------+
        | ocxProgID   | - ProgID - ActiveX элемента (ActiveX дерева); Например, «VCTree.VCTreeCtrl.1» - для создания дерева, из примеров SDK. см SDK\\lib\\VCTree.ocx, проект SDK\\OCX\\VCTree. Пример использования SDK\\C++\\Visualc\\TestVCTree, |
        +------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------+
        | Active      | - активизировать закладку при создании,                                                                                                                                                                              |
        +------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------+
        | Layout      | - умолчательное расположение.                                                                                                                                                                                        |
        +------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------+

        ### Возвращаемое значение

        - Указатель на интерфейс созданного ActiveX элемента.
        """
        ...

    def RemoveTab(self, Ocx) -> bool:  # iframetreesmanager_removetab.html
        """
        ## RemoveTab - Удалить вкладку

        ### Синтаксис Automation

        ```
        BOOL RemoveTab( LPDISPATCH ocx );
        ```

        ### Входные параметры

        +----------------------------------------------+
        | ocx | - указатель на интерфейс ActiveX окна. |
        +----------------------------------------------+

        ### Возвращаемое значение

        +-----------------------------+
        | TRUE  | - в случае успеха,  |
        +-----------------------------+
        | FALSE | - в случае неудачи. |
        +-----------------------------+
        """
        ...

    def SetTabVisible(self, Ocx, arg1):
        ...

    def SetTreeCaption(self, Ocx, arg1):
        ...

    def TabVisible(self, Ocx) -> bool:  # iframetreesmanager_tabvisible.html
        """
        ## TabVisible - Видимость закладки

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        TabVisible = Object.TabVisible( ocx )     # Получить свойство (* )
        Object.TabVisible( ocx ) = TabVisible     # Установить свойство (* )
        TabVisible = Object.GetTabVisible( ocx )  # Получить свойство (**)
        Object.SetTabVisible( ocx, TabVisible )   # Установить свойство (**)
        ```

        ### Входные параметры

        +----------------------------------------------------------------------------+
        | ocx | - указатель на интерфейс ActiveX контрола, добавленного на закладку. |
        +----------------------------------------------------------------------------+
        """
        ...

    def TreeCaption(self, Ocx) -> str:  # iframetreesmanager_treecaption.html
        """
        ## TreeCaption - Заголовок окна Дерева документа, устанавливаемый при активизации закладки

        Тип данных: BSTR

        ### Синтаксис Automation

        ```
        TreeCaption = Object.TreeCaption( Ocx )     # Получить свойство (* )
        Object.TreeCaption( Ocx ) = TreeCaption     # Установить свойство (* )
        TreeCaption = Object.GetTreeCaption( Ocx )  # Получить свойство (**)
        Object.SetTreeCaption( Ocx, TreeCaption )   # Установить свойство (**)
        ```

        ### Входные параметры

        Ocx - Указатель на интерфейс ActiveX-окна, возвращаемого методом IFrameTreesManager::AddTab
        """
        ...




class IExternalGDIObject(IKompasAPIObject):  # iexternalgdiobject.html
    """
    ## Интерфейс IExternalGDIObject

    Интерфейс внешнего GDI- объекта.

    1. Интерфейс позволяет реализовать отрисовку внешних GDI- объектов (для V11 только текстов).

    2. Управление интерфейсом (создание и удаление) осуществляется через менеджер IExternalTessellationManager.
    """

    @property
    def AlwaysDrawInScreenPlane(self) -> int:  # iexternalgdiobject_alwaysdrawinscreenplane.html
        """
        ## AlwaysDrawInScreenPlane - Способ преобразования координат внешней триангуляции

        Тип данных: из перечисления ksDrawInScreenPlaneEnum

        ### Синтаксис Automation

        ```
        AlwaysDrawInScreenPlane = Object.AlwaysDrawInScreenPlane       # Получить свойство (* )
        Object.AlwaysDrawInScreenPlane = AlwaysDrawInScreenPlane       # Установить свойство (* )
        AlwaysDrawInScreenPlane = Object.GetAlwaysDrawInScreenPlane()  # Получить свойство (**)
        Object.SetAlwaysDrawInScreenPlane( AlwaysDrawInScreenPlane )   # Установить свойство (**)
        ```
        """
        ...
    @AlwaysDrawInScreenPlane.setter
    def AlwaysDrawInScreenPlane(self, value: int): ...

    @property
    def NonPickable(self):
        ...
    @NonPickable.setter
    def NonPickable(self, value): ...

    @property
    def NonScalableX(self) -> bool:  # iexternalgdiobject_nonscalablex.html
        """
        ## NonScalableX - Не масштабировать по X

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        NonScalableX = Object.NonScalableX       # Получить свойство (* )
        Object.NonScalableX = NonScalableX       # Установить свойство (* )
        NonScalableX = Object.GetNonScalableX()  # Получить свойство (**)
        Object.SetNonScalableX( NonScalableX )   # Установить свойство (**)
        ```
        """
        ...
    @NonScalableX.setter
    def NonScalableX(self, value: bool): ...

    @property
    def NonScalableY(self) -> bool:  # iexternalgdiobject_nonscalabley.html
        """
        ## NonScalableY - Не масштабировать по Y

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        NonScalableY = Object.NonScalableY       # Получить свойство (* )
        Object.NonScalableY = NonScalableY       # Установить свойство (* )
        NonScalableY = Object.GetNonScalableY()  # Получить свойство (**)
        Object.SetNonScalableY( NonScalableY )   # Установить свойство (**)
        ```
        """
        ...
    @NonScalableY.setter
    def NonScalableY(self, value: bool): ...

    @property
    def NonScalableZ(self) -> bool:  # iexternalgdiobject_nonscalablez.html
        """
        ## NonScalableZ - Не масштабировать по Z

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        NonScalableZ = Object.NonScalableZ       # Получить свойство (* )
        Object.NonScalableZ = NonScalableZ       # Установить свойство (* )
        NonScalableZ = Object.GetNonScalableZ()  # Получить свойство (**)
        Object.SetNonScalableZ( NonScalableZ )   # Установить свойство (**)
        ```
        """
        ...
    @NonScalableZ.setter
    def NonScalableZ(self, value: bool): ...

    @property
    def ObjectID(self) -> int:  # iexternalgdiobject_objectid.html
        """
        ## ObjectID - Идентификатор объекта

        Тип данных: long

        1. Позволяет получить идентификатор объекта. По идентификатору объекта осуществляется установка видимости и удаление объектов.

        2. Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        ObjectID = Object.ObjectID       # Получить свойство (* )
        ObjectID = Object.GetObjectID()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def ScalableText(self) -> bool:  # iexternalgdiobject_scalabletext.html
        """
        ## ScalableText - Признак масштабирования текста

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        ScalableText = Object.ScalableText       # Получить свойство (* )
        Object.ScalableText = ScalableText       # Установить свойство (* )
        ScalableText = Object.GetScalableText()  # Получить свойство (**)
        Object.SetScalableText( ScalableText )   # Установить свойство (**)
        ```
        """
        ...
    @ScalableText.setter
    def ScalableText(self, value: bool): ...

    @property
    def Visible(self) -> typing.NoReturn:  # iexternalgdiobject_visible.html
        """
        ## Visible - Показать\\скрыть объект

        Тип данных: BOOL

        1. Позволяет изменить видимость объекта в заданном окне.

        2. Если frame == NULL, то объект будет отображаться во всех окнах документа.

        3. Свойство доступно только для изменения.

        ### Синтаксис Automation

        ```
        Object.Visible( frame ) = Visible    # Установить свойство (* )
        Object.SetVisible( frame, Visible )  # Установить свойство (**)
        ```

        ### Входные параметры

        +-----------------------------------------------------------------+
        | frame | - указатель на интерфейс окна документа IDocumentFrame. |
        +-----------------------------------------------------------------+
        """
        ...
    @Visible.setter
    def Visible(self, value: bool): ...

    def Delete(self) -> bool:  # iexternalgdiobject_delete.html
        """
        ## Delete - Удалить объект

        1. Перед удалением объекта нужно выключить видимость объекта в окнах документа.

        2. Для удаления группы объектов используется функция IExternalTessellationManager::DeleteObjects.

        3. Для удаления всех внешних объектов используется метод IExternalTessellationManager::Clear.

        ### Синтаксис Automation

        ```
        BOOL Delete( );
        ```

        ### Возвращаемое значение

        +-----------------------------+
        | TRUE  | - в случае успеха,  |
        +-----------------------------+
        | FALSE | - в случае неудачи. |
        +-----------------------------+
        """
        ...

    def SetBkColors(self, BkColors) -> bool:  # iexternalgdiobject_setbkcolors.html
        """
        ## SetBkColors - Установить цвета фона текстов с Alpha каналом

        ### Синтаксис Automation

        ```
        BOOL SetBkColors( VARIANT BkColors );
        ```

        ### Входные параметры

        +-------------------------------------------------+
        | BkColors | - массив SafeArray VT_ARRAY | VT_I4. |
        +-------------------------------------------------+

        ### Возвращаемое значение

        +----------------------------+
        | TRUE  | - в случае успеха, |
        +----------------------------+
        | FALSE | - в случае ошибки. |
        +----------------------------+
        """
        ...

    def SetPlace(self, Place) -> bool:  # iexternalgdiobject_setplace.html
        """
        ## SetPlace - Установить матрицу для отрисовки объекта

        ### Синтаксис Automation

        ```
        BOOL SetPlace( VARIANT Place );
        ```

        ### Входные параметры

        +-------------------------------------------------------+
        | Place | - массив SAFEARRAY double (VT_ARRAY | VT_R8). |
        +-------------------------------------------------------+

        ### Возвращаемое значение

        +-----------------------------+
        | TRUE  | - в случае успеха,  |
        +-----------------------------+
        | FALSE | - в случае неудачи. |
        +-----------------------------+
        """
        ...

    def SetTextColors(self, textColors) -> bool:  # iexternalgdiobject_settextcolors.html
        """
        ## SetTextColors - Установить цвета текстов

        ### Синтаксис Automation

        ```
        BOOL SetTextColors( VARIANT textColors );
        ```

        ### Входные параметры

        +------------------------------------------------------------------------+
        | textColors | - массив цветов - SafeArray целых чисел VT_ARRAY | VT_I4. |
        +------------------------------------------------------------------------+

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def SetTextOrientation(self, textOrients) -> bool:  # iexternalgdiobject_settextorientation.html
        """
        ## SetTextOrientation - Установить наклон символов текстов

        ### Синтаксис Automation

        ```
        BOOL SetTextOrientation( VARIANT textOrients );
        ```

        ### Входные параметры

        +----------------------------------+
        | textOrients | - массив наклонов. |
        +----------------------------------+

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def SetTexts(self, Texts, Points, Colors, Fonts, Is3DPoints) -> bool:  # iexternalgdiobject_settexts.html
        """
        ## SetTexts - Установить тексты и их параметры

        ### Синтаксис Automation

        ```
        BOOL SetTexts( VARIANT Texts,
        VARIANT Points,
        VARIANT Colors,
        VARIANT Fonts,
        BOOL Is3DPoints );
        ```

        ### Входные параметры

        +------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------+
        | Points     | - массив SafeArray VT_ARRAY | VT_R8 координат точек отрисовки текстов. Координаты в массиве могут лежать в последовательности : - для Is3DPoints == TRUE x0, y0, z0, x1, y1, z1, ... xi, yi, zi - для Is3DPoints == FALSE x0, y0, x1, y1, ... xi, yi,                                                                                                                                         |
        +------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------+
        | Texts      | - массив SafeArray VT_ARRAY | VT_BSTR текстов,                                                                                                                                                                                                                                                                                                                                                |
        +------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------+
        | Сolors     | - массив цветов текстов. Массив может быть не задан - тип VARIANT-а VT_EMPTY, в этом случае используется черный цвет. Может быть задан один цвет - тип VARIANT-а VT_I4. Может быть задан цвет для каждого текста тип VARIANT-а VT_ARRAY | VT_I4,                                                                                                                                              |
        +------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------+
        | Fonts      | - массив шрифтов текстов. Массив может быть не задан - тип VARIANT-а VT_EMPTY. В этом случае используется шрифт, установленный в модели. Может быть задан один шрифт - тип VARIANT-а VT_DISPATCH. Может быть задан шрифт для каждого текста тип VARIANT-а VT_ARRAY | VT_DISPATCH. Передаваемый интерфейс - интерфейс IFontDispPtr, который можно получить от объекта MFC- класса CFontHolder, |
        +------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------+
        | Is3DPoints | - признак использования 3D-координат отрисовки текстов.                                                                                                                                                                                                                                                                                                                                       |
        +------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------+

        ### Возвращаемое значение

        +-----------------------------+
        | TRUE  | - в случае успеха,  |
        +-----------------------------+
        | FALSE | - в случае неудачи. |
        +-----------------------------+
        """
        ...

    def SetTextsAlign(self, TextsAlign) -> bool:  # iexternalgdiobject_settextsalign.html
        """
        ## SetTextsAlign - Установить выравнивание текста

        ### Синтаксис Automation

        ```
        BOOL SetTextsAlign( VARIANT TextsAlign );
        ```

        ### Входные параметры

        +--------------------------------------------------------------------------------------------+
        | TextsAlign | - массив SafeArray VT_ARRAY | VT_I4 констант из перечисления ksTextAlignEnum. |
        +--------------------------------------------------------------------------------------------+

        ### Возвращаемое значение

        +----------------------------+
        | TRUE  | - в случае успеха, |
        +----------------------------+
        | FALSE | - в случае ошибки. |
        +----------------------------+
        """
        ...

    def SetVisible(self, Frame, arg1):
        ...




class IExternalTessellationManager(IDispatch):  # iexternaltessellationmanager.html
    """
    ## Интерфейс IExternalTessellationManager

    Менеджер объектов с внешней триангуляцией.

    +-------------------------------------------------------+
    | IExternalTessellationManager | - интерфейс Automation |
    +-------------------------------------------------------+

    Дополнительный интерфейс для IKompasDocument3D.

    Данный интерфейс можно получить у интерфейса IKompasDocument3D посредством вызова метода IUnknown::QueryInterface (const GUID far& IID, void** pif).

    Позволяет создавать объекты с внешней триангуляцией, отрисовываемые треугольниками и ребрами.
    """

    @property
    def DisableModelRotation(self) -> bool:  # iexternaltessellationmanager_disablemodelrotation.html
        """
        ## DisableModelRotation - Запретить поворот модели и изменение ориентации вида

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        DisableModelRotation = Object.DisableModelRotation       # Получить свойство (* )
        Object.DisableModelRotation = DisableModelRotation       # Установить свойство (* )
        DisableModelRotation = Object.GetDisableModelRotation()  # Получить свойство (**)
        Object.SetDisableModelRotation( DisableModelRotation )   # Установить свойство (**)
        ```
        """
        ...
    @DisableModelRotation.setter
    def DisableModelRotation(self, value: bool): ...

    def Add(self) -> IExternalTessellationObject:  # iexternaltessellationmanager_add.html
        """
        ## Add - Добавить объект с внешней триангуляцией

        ### Синтаксис Automation

        ```
        LPDISPATCH Add();
        ```

        ### Возвращаемое значение

        - Указатель на интерфейс объекта с внешней триангуляцией IExternalTessellationObject.
        """
        ...

    def AddGDIObject(self) -> IExternalGDIObject:  # iexternaltessellationmanager_addgdiobject.html
        """
        ## AddGDIObject - Добавить внешний GDI- объект

        ### Синтаксис Automation

        ```
        LPDISPATCH AddGDIObject();
        ```

        ### Возвращаемое значение

        - Указатель на интерфейс внешнего GDI- объекта IExternalGDIObject.
        """
        ...

    def Clear(self) -> bool:  # iexternaltessellationmanager_clear.html
        """
        ## Clear - Удалить все объекты

        ### Синтаксис Automation

        ```
        BOOL Clear();
        ```

        ### Возвращаемое значение

        +-----------------------------------------+
        | TRUE | - в случае успешного завершения. |
        +-----------------------------------------+
        """
        ...

    def CreateTextureImage(self, Width, Heigh, RGBA, WrapMode, FiltMode, ImageData) -> bool:  # iexternaltessellationmanager_createtextureimage.html
        """
        ## CreateTextureImage - Создать изображение текстуры

        Созданная текстура используется в методе IExternalTessellationObj::SelectTextureImage.

        ### Синтаксис Automation

        ```
        long CreateTextureImage ( long Width, long Heigh,
        BOOL RGBA,
        BOOL WrapMode,
        BOOL FiltMode,
        VARIANT ImageData );
        ```

        ### Входные параметры

        +------------------------------------------------------------------------+
        | Width, Heigh | - Размер изображения,                                   |
        +------------------------------------------------------------------------+
        | RGBA         | - TRUE - RGBA, FALSE - RGB,                             |
        +------------------------------------------------------------------------+
        | WrapMode     | - Режим повторения TRUE - GL_CLAMP FALSE - GL_REPEAT,   |
        +------------------------------------------------------------------------+
        | FiltMode     | - Режим повторения TRUE - GL_LINEAR FALSE - GL_NEAREST, |
        +------------------------------------------------------------------------+
        | ImageData    | Массив байтов изображения текстуры VT_ARRAY|VT_UI1.     |
        +------------------------------------------------------------------------+

        ### Возвращаемое значение

        - Идентификатор текстуры.

        +----------------------------+
        | TRUE  | - в случае удачи,  |
        +----------------------------+
        | FALSE | - в случае ошибки. |
        +----------------------------+
        """
        ...

    def DeleteObjects(self, Id) -> bool:  # iexternaltessellationmanager_deleteobjects.html
        """
        ## DeleteObjects - Удалить объекты

        ### Синтаксис Automation

        ```
        BOOL DeleteObjects();
        ```

        ### Входные параметры

        +-------------------------------------------------------------------------------------------------------------------------------------------------------------------------+
        | Ids | - Идентификаторы объектов тип VARIANT. Можно передать один идентификатор тип VARIANT-а VT_I4 или массив SafeArray идентификаторов тип VARIANT-а VT_ARRAY | VT_I4. |
        +-------------------------------------------------------------------------------------------------------------------------------------------------------------------------+

        ### Возвращаемое значение

        +-----------------------------+
        | TRUE  | - в случае успеха,  |
        +-----------------------------+
        | FALSE | - в случае неудачи. |
        +-----------------------------+
        """
        ...

    def DeleteTextureImage(self, TexImgId) -> bool:  # iexternaltessellationmanager_deletetextureimage.html
        """
        ## DeleteTextureImage - Удалить изображение текстуры

        ### Синтаксис Automation

        ```
        BOOL DeleteTextureImage( long TexImgId );
        ```

        ### Входные параметры

        +--------------------------------------+
        | TexImgId | - идентификатор текстуры. |
        +--------------------------------------+

        ### Возвращаемое значение

        +-----------------------------+
        | TRUE  | - в случае успеха,  |
        +-----------------------------+
        | FALSE | - в случае неудачи. |
        +-----------------------------+
        """
        ...

    def DisableModelDrawing(self, ForElements) -> bool:  # iexternaltessellationmanager_disablemodeldrawing.html
        """
        ## DisableModelDrawing - Запретить отрисовку модели для указанных элементов

        ### Синтаксис Automation

        ```
        BOOL DisableModelDrawing( long ForElements );
        ```

        ### Входные параметры

        +-----------------------------------------------------------+
        | ForElements | - типы объектов ksModelDrawingElementsEnum. |
        +-----------------------------------------------------------+

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def EnableModelDrawing(self) -> bool:  # iexternaltessellationmanager_enablemodeldrawing.html
        """
        ## EnableModelDrawing - Отменить последний запрет на отрисовку элементов модели

        ### Синтаксис Automation

        ```
        BOOL EnableModelDrawing();
        ```

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def GDIObject(self, Id) -> bool:  # iexternaltessellationmanager_gdiobject.html
        """
        ## GDIObject - Получить внешний GDI объект

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        GDIObject = Object.GDIObject( Id )     # Получить свойство (* )
        GDIObject = Object.GetGDIObject( Id )  # Получить свойство (**)
        ```

        ### Входные параметры

        +-------------------------------+
        | Id | - идентификатор объекта. |
        +-------------------------------+
        """
        ...

    def IsModelDrawingEnabled(self, ForElements) -> bool:  # iexternaltessellationmanager_ismodeldrawingenabled.html
        """
        ## IsModelDrawingEnabled - Проверить, разрешена ли отрисовка для указанных элементов

        ### Синтаксис Automation

        ```
        BOOL IsModelDrawingEnabled( long ForElements );
        ```

        ### Входные параметры

        +-----------------------------------------------------------+
        | ForElements | - типы объектов ksModelDrawingElementsEnum. |
        +-----------------------------------------------------------+

        ### Возвращаемое значение

        +------------------------------------+
        | TRUE | - если отрисовка разрешена. |
        +------------------------------------+
        """
        ...

    def ObjectsVisible(self, Ids, Frame, arg2) -> bool:  # iexternaltessellationmanager_objectsvisible.html
        """
        ## ObjectsVisible - Показать\\скрыть объекты

        Тип данных: BOOL

        1. Позволяет изменить видимость объектов в заданном окне. Если frame == NULL, то объект будет отображаться во всех окнах документа.

        2. Свойство доступно только для изменения.

        ### Синтаксис Automation

        ```
        Object.ObjectsVisible( Ids, frame ) = Visible    # Установить свойство (* )
        Object.SetObjectsVisible( Ids, frame, Visible )  # Установить свойство (**)
        ```

        ### Входные параметры

        +--------------------------------------------------------------------------------------------------------------------------------------------------------------------------+
        | Ids   | - Идентификаторы объектов тип VARIANT. Можно передать один идентификатор тип VARIANT-а VT_I4 или массив SafeArray идентификаторов тип VARIANT-а VT_ARRAY | VT_I4 |
        +--------------------------------------------------------------------------------------------------------------------------------------------------------------------------+
        | frame | - указатель на интерфейс окна документа IDocumentFrame.                                                                                                          |
        +--------------------------------------------------------------------------------------------------------------------------------------------------------------------------+
        """
        ...

    def PickObjects(self, Frame, PickRay, Visible) -> bool:  # iexternaltessellationmanager_pickobjects.html
        """
        ## PickObjects - Собрать объекты по лучу

        ### Синтаксис Automation

        ```
        BOOL PickObjects( LPDISPATCH Frame, VARIANT PickRay, BOOL Visible, VARIANT * PickedObjs, VARIANT * PickedPars );
        ```

        ### Входные параметры

        +----------------------------------------------------------------------------------------------------------------------------------------------+
        | Frame   | - Указатель на интерфейс окна документа IDocumentFrame,                                                                            |
        +----------------------------------------------------------------------------------------------------------------------------------------------+
        | PickRay | - Параметры луча ( VT_ARRAY | VT_R8 ), массив из шести элементов (Точка начала луча и единичный вектор задающий направление луча), |
        +----------------------------------------------------------------------------------------------------------------------------------------------+
        | Visible | - Признак учета видимых объектов: TRUE - пересекать только с видимыми объектами, FALSE - со всеми объектами.                       |
        +----------------------------------------------------------------------------------------------------------------------------------------------+

        ### Выходные параметры

        +--------------------------------------------------------------------------------------------+
        | PickedObjs | - Идентификаторы найденных внешних объектов триангуляции (VT_ARRAY | VT_UI4), |
        +--------------------------------------------------------------------------------------------+
        | PickedPars | - Параметры луча в точке пересечения с объектами (VT_ARRAY | VT_R4).          |
        +--------------------------------------------------------------------------------------------+

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def TessellationObject(self, Id) -> bool:  # iexternaltessellationmanager_tessellationobject.html
        """
        ## TessellationObject - Получить объект по идентификатору

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        TessellationObject = Object.TessellationObject( Id )     # Получить свойство (* )
        TessellationObject = Object.GetTessellationObject( Id )  # Получить свойство (**)
        ```

        ### Входные параметры

        +-------------------------------+
        | Id | - идентификатор объекта. |
        +-------------------------------+
        """
        ...




class IExternalTessellationObject(IKompasAPIObject):  # iexternaltessellationobject.html
    """
    ## Интерфейс IExternalTessellationObject

    Интерфейс объекта с внешней триангуляцией.

    +------------------------------------------------------+
    | IExternalTessellationObject | - интерфейс Automation |
    +------------------------------------------------------+

    Интерфейс позволяет реализовать отрисовку внешних объектов с триангуляцией.
    """

    @property
    def AlwaysDrawInScreenPlane(self) -> int:  # iexternaltessellationobject_alwaysdrawinscreenplane.html
        """
        ## AlwaysDrawInScreenPlane - Способ преобразования координат внешней триангуляции

        Тип данных: из перечисления ksDrawInScreenPlaneEnum

        ### Синтаксис Automation

        ```
        AlwaysDrawInScreenPlane = Object.AlwaysDrawInScreenPlane       # Получить свойство (* )
        Object.AlwaysDrawInScreenPlane = AlwaysDrawInScreenPlane       # Установить свойство (* )
        AlwaysDrawInScreenPlane = Object.GetAlwaysDrawInScreenPlane()  # Получить свойство (**)
        Object.SetAlwaysDrawInScreenPlane( AlwaysDrawInScreenPlane )   # Установить свойство (**)
        ```
        """
        ...
    @AlwaysDrawInScreenPlane.setter
    def AlwaysDrawInScreenPlane(self, value: int): ...

    @property
    def DisableDepthTest(self) -> bool:  # iexternaltessellationobject_disabledepthtest.html
        """
        ## DisableDepthTest - Запрет проверки глубины

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        DisableDepthTest = Object.DisableDepthTest       # Получить свойство (* )
        Object.DisableDepthTest = DisableDepthTest       # Установить свойство (* )
        DisableDepthTest = Object.GetDisableDepthTest()  # Получить свойство (**)
        Object.SetDisableDepthTest( DisableDepthTest )   # Установить свойство (**)
        ```
        """
        ...
    @DisableDepthTest.setter
    def DisableDepthTest(self, value: bool): ...

    @property
    def NonGeometry(self) -> bool:  # iexternaltessellationobject_nongeometry.html
        """
        ## NonGeometry - Не геометрический

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        NonGeometry = Object.NonGeometry       # Получить свойство (* )
        Object.NonGeometry = NonGeometry       # Установить свойство (* )
        NonGeometry = Object.GetNonGeometry()  # Получить свойство (**)
        Object.SetNonGeometry( NonGeometry )   # Установить свойство (**)
        ```
        """
        ...
    @NonGeometry.setter
    def NonGeometry(self, value: bool): ...

    @property
    def NonPickable(self) -> bool:  # iexternaltessellationobject_nonpickable.html
        """
        ## NonPickable - Не выбираемый

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        NonPickable = Object.NonPickable       # Получить свойство (* )
        Object.NonPickable = NonPickable       # Установить свойство (* )
        NonPickable = Object.GetNonPickable()  # Получить свойство (**)
        Object.SetNonPickable( NonPickable )   # Установить свойство (**)
        ```
        """
        ...
    @NonPickable.setter
    def NonPickable(self, value: bool): ...

    @property
    def NonScalableX(self) -> bool:  # iexternaltessellationobject_nonscalablex.html
        """
        ## NonScalableX - Не масштабировать по X

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        NonScalableX = Object.NonScalableX       # Получить свойство (* )
        Object.NonScalableX = NonScalableX       # Установить свойство (* )
        NonScalableX = Object.GetNonScalableX()  # Получить свойство (**)
        Object.SetNonScalableX( NonScalableX )   # Установить свойство (**)
        ```
        """
        ...
    @NonScalableX.setter
    def NonScalableX(self, value: bool): ...

    @property
    def NonScalableY(self) -> bool:  # iexternaltessellationobject_nonscalabley.html
        """
        ## NonScalableY - Не масштабировать по Y

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        NonScalableY = Object.NonScalableY       # Получить свойство (* )
        Object.NonScalableY = NonScalableY       # Установить свойство (* )
        NonScalableY = Object.GetNonScalableY()  # Получить свойство (**)
        Object.SetNonScalableY( NonScalableY )   # Установить свойство (**)
        ```
        """
        ...
    @NonScalableY.setter
    def NonScalableY(self, value: bool): ...

    @property
    def NonScalableZ(self) -> bool:  # iexternaltessellationobject_nonscalablez.html
        """
        ## NonScalableZ - Не масштабировать по Z

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        NonScalableZ = Object.NonScalableZ       # Получить свойство (* )
        Object.NonScalableZ = NonScalableZ       # Установить свойство (* )
        NonScalableZ = Object.GetNonScalableZ()  # Получить свойство (**)
        Object.SetNonScalableZ( NonScalableZ )   # Установить свойство (**)
        ```
        """
        ...
    @NonScalableZ.setter
    def NonScalableZ(self, value: bool): ...

    @property
    def ObjectID(self) -> int:  # iexternaltessellationobject_objectid.html
        """
        ## ObjectID - Идентификатор объекта

        Тип данных:long

        1. Позволяет получить идентификатор объекта. По идентификатору объекта осуществляется установка видимости и удаление объектов.

        2. Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        ObjectID = Object.ObjectID       # Получить свойство (* )
        ObjectID = Object.GetObjectID()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def Visible(self) -> typing.NoReturn:  # iexternaltessellationobject_visible.html
        """
        ## Visible - Идентификатор объекта

        Тип данных: BOOL

        1. Позволяет изменить видимость объекта в заданном окне. Если frame == NULL, то объект будет отображаться во всех окнах документа.

        2. Свойство доступно только для изменения.

        ### Синтаксис Automation

        ```
        Object.Visible( frame ) = Visible    # Установить свойство (* )
        Object.SetVisible( frame, Visible )  # Установить свойство (**)
        ```

        ### Входные параметры

        +-----------------------------------------------------------------+
        | frame | - указатель на интерфейс окна документа IDocumentFrame. |
        +-----------------------------------------------------------------+
        """
        ...
    @Visible.setter
    def Visible(self, value: bool): ...

    def Delete(self) -> bool:  # iexternaltessellationobject_delete.html
        """
        ## Delete - Удалить объект

        1. Перед удалением объекта нужно выключить видимость объекта в окнах документа.

        2. Для удаления группы объектов используется метод IExternalTessellationManager::DeleteObjects. Для удаления всех внешних объектов используется метод IExternalTessellationManager::Clear.

        ### Синтаксис Automation

        ```
        BOOL Delete( );
        ```

        ### Возвращаемое значение

        +-----------------------------+
        | TRUE  | - в случае успеха,  |
        +-----------------------------+
        | FALSE | - в случае неудачи. |
        +-----------------------------+
        """
        ...

    def SelectTextureImage(self, TexImgId) -> bool:  # iexternaltessellationobject_selecttextureimage.html
        """
        ## SelectTextureImage - Задать изображение текстуры по идентификатору

        Метод позволяет установить для объекта текстуру, созданную функцией IExternalTessellationManager::CreateTextureImage.

        ### Синтаксис Automation

        ```
        BOOL SelectTextureImage( long TexImgId );
        ```

        ### Возвращаемое значение

        +----------------------------+
        | TRUE  | - в случае удачи,  |
        +----------------------------+
        | FALSE | - в случае ошибки. |
        +----------------------------+
        """
        ...

    def SetAdvancedColor(self, Color, Ambient, Diffuse, Specularity, Shininess, Transparency, Emission) -> bool:  # iexternaltessellationobject_setadvancedcolor.html
        """
        ## SetAdvancedColor - Установить параметры цвета объекта

        ### Синтаксис Automation

        ```
        BOOL SetAdvancedColor (long color,
        double ambient,
        double diffuse,
        double specularity,
        double shininess,
        double transparency,
        double emission);
        ```

        ### Входные параметры

        +--------------------------------+
        | color        | - цвет,         |
        +--------------------------------+
        | ambient      | - общий свет,   |
        +--------------------------------+
        | diffuse      | - диффузия,     |
        +--------------------------------+
        | specularity  | - зеркальность, |
        +--------------------------------+
        | shininess    | - блеск,        |
        +--------------------------------+
        | transparency | - прозрачность, |
        +--------------------------------+
        | emission     | - излучение.    |
        +--------------------------------+

        ### Возвращаемое значение

        +-----------------------------------------+
        | TRUE | - в случае успешного завершения. |
        +-----------------------------------------+
        """
        ...

    def SetEdgeColors(self, Colors) -> bool:  # iexternaltessellationobject_setedgecolors.html
        """
        ## SetEdgeColors - Установить цвет ребрам сетки

        ### Синтаксис Automation

        ```
        BOOL SetEdgeColors( VARIANT Colors );
        ```

        ### Входные параметры

        +--------------------------------------+
        | Colors | - безопасный массив цветов. |
        +--------------------------------------+

        ### Возвращаемое значение

        +-----------------------------------------+
        | TRUE | - в случае успешного завершения. |
        +-----------------------------------------+
        """
        ...

    def SetEdgeStyles(self, Styles) -> bool:  # iexternaltessellationobject_setedgestyles.html
        """
        ## SetEdgeStyles - Установить стили рёбер сетки

        ### Синтаксис Automation

        ```
        BOOL SetEdgeStyles( VARIANT Styles );
        ```

        ### Входные параметры

        +--------------------------------------------------+
        | Styles | - массив VT_ARRAY | VT_I4 стилей ребер. |
        +--------------------------------------------------+

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def SetEdgeWidths(self, Widths) -> bool:  # iexternaltessellationobject_setedgewidths.html
        """
        ## SetEdgeWidths - Установить толщины ребер сетки

        ### Синтаксис Automation

        ```
        BOOL SetEdgeWidths( VARIANT Widths );
        ```

        ### Входные параметры

        +------------------------------------------------------------+
        | Widths | - массив VT_ARRAY | VT_R8 значений толщины ребер. |
        +------------------------------------------------------------+

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def SetEdges(self, Points, Edges, Colors) -> bool:  # iexternaltessellationobject_setedges.html
        """
        ## SetEdges - Установить ребра сетки

        1. Массив ребер может содержать одно или несколько ребер. Каждое ребро может иметь две или более вершины.

        2. Для описания ребер используется последовательность:

        [ [count0, i00, i01, ...] - первое ребро

        [count1, i10, i11, ...] - второе ребро

        ...

        [counti, ii0, ii1, ...]] - i-тое ребро,

        где count0, count1, counti - количество вершин в ребре, i00, i01, i10, i11, ii0, ii1 - индексы координат точек из массива points.

        ### Синтаксис Automation

        ```
        BOOL SetEdges( VARIANT points,
        VARIANT edges,
        VARIANT colors );
        ```

        ### Входные параметры

        +--------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------+
        | points | - массив SafeArray VT_ARRAY | VT_R8 координат вершин. Если массив координат уже задан функцией SetTessellation, то нужно передать пустой Variant VT_ARRAY | VT_R8,                                                                                                    |
        +--------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------+
        | edges  | - массив SafeArray типа VT_ARRAY | VT_I4 индексов координат вершин,                                                                                                                                                                                                   |
        +--------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------+
        | colors | - массив цветов. Массив может быть не задан - тип VARIANT-а VT_EMPTY. В этом случае используется цвет, заданный функцией SetAdvancedColor. Может быть задан один цвет - тип VARIANT-а VT_I4. Может быть задан цвет для каждой точки - тип VARIANT-а VT_ARRAY | VT_I4. |
        +--------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------+

        ### Возвращаемое значение

        +-----------------------------------------+
        | TRUE | - в случае успешного завершения. |
        +-----------------------------------------+
        """
        ...

    def SetFacetMode(self, SideMode, CullingMode) -> bool:  # iexternaltessellationobject_setfacetmode.html
        """
        ## SetFacetMode - Установить режим отображения граней

        ### Синтаксис Automation

        ```
        BOOL SetFacetMode( BOOL SideMode,
        ksFacetCullingMode CullingMode );
        ```

        ### Входные параметры

        +-----------------------------------------------------------------------------------------------+
        | SideMode    | - TRUE - объект является двусторонним, - FALSE - объект является односторонним, |
        +-----------------------------------------------------------------------------------------------+
        | CullingMode | - режим отображения граней ksFacetCullingMode.                                  |
        +-----------------------------------------------------------------------------------------------+

        ### Возвращаемое значение

        +--------------------------+
        | TRUE | - в случае удачи. |
        +--------------------------+
        """
        ...

    def SetPlaces(self, Places) -> bool:  # iexternaltessellationobject_setplaces.html
        """
        ## SetPlaces - Установить матрицы для отрисовки объекта

        ### Синтаксис Automation

        ```
        BOOL SetPlaces( VARIANT places );
        ```

        ### Входные параметры

        +-----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------+
        | places | - массив SafeArray типа VT_ARRAY | VT_R8. В массиве может быть передана одна или несколько матриц размером 4*4. Если передано несколько матриц, объект будет отрисован столько раз, сколько передано матриц. Используется для многократной отрисовки одинаковых объектов. Матрицы передаются в виде одномерного массива. |
        +-----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------+

        ### Возвращаемое значение

        +-----------------------------------------+
        | TRUE | - в случае успешного завершения. |
        +-----------------------------------------+
        """
        ...

    def SetTessellation(self, Points, Indexes, Normals, Colors):  # iexternaltessellationobject_settessellation.html
        """
        ## SetTessellation - Установить параметры триангуляции

        ### Синтаксис Automation

        ```
        BOOL SetTessellation( VARIANT points,
        VARIANT indexes,
        VARIANT normals,
        VARIANT colors );
        ```

        ### Входные параметры

        +------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------+
        | points  | - массив SafeArray VT_ARRAY | VT_R8 координат точек триангуляционной сетки; координаты в массиве лежат в последовательности x0, y0, z0, x1, y1, z1, ... xi, yi, zi,                                                                                                            |
        +------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------+
        | indexes | - массив SafeArray VT_ARRAY | VT_I4 индексов вершин треугольников; по индексу из массива indexes достаются координаты вершин из массива points,                                                                                                                                |
        +------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------+
        | normals | - массив SafeArray VT_ARRAY | VT_R8 единичных векторов нормалей для вершин. Если направление нормалей во всех вершинах совпадают, можно добавить направление только одной нормали. Иначе количество нормалей должно быть равно количеству точек в массиве points,              |
        +------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------+
        | colors  | - массив цветов в точках. Массив может быть не задан - тип VARIANT-а VT_EMPTY. В этом случае используется цвет, заданный функцией SetAdvancedColor. Может быть задан один цвет - тип VARIANT-а VT_I4. Может быть задан цвет для каждой точки - тип VARIANT-а VT_ARRAY | VT_I4. |
        +------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------+
        """
        ...

    def SetTextureImage(self, Width, Heigh, RGBA, WrapMode, FiltMode, ImageData) -> bool:  # iexternaltessellationobject_settextureimage.html
        """
        ## SetTextureImage - Установить изображение текстуры

        ### Синтаксис Automation

        ```
        BOOL SetTextureImage( long Width,
        long Heigh,
        BOOL RGBA,
        BOOL WrapMode,
        BOOL FiltMode,
        VARIANT ImageData );
        ```

        ### Входные параметры

        +----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------+
        | Width, Heigh | - размер изображения, являющегося элементом текстуры; Рекомендуется использовать изображения, значения размеров которых являются целой положительной степенью числа 2; в противном случае, объекты будут неправильно выведены на печать и конвертированы в растр, |
        +----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------+
        | RGBA         | - TRUE - RGBA, - FALSE - RGB,                                                                                                                                                                                                                                     |
        +----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------+
        | WrapMode     | режим повторения: - TRUE - GL_CLAMP, - FALSE - GL_REPEAT,                                                                                                                                                                                                         |
        +----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------+
        | FiltMode     | режим фильтрации: - TRUE - GL_LINEAR, - FALSE - GL_NEAREST,                                                                                                                                                                                                       |
        +----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------+
        | ImageData    | - массив байтов изображения текстуры VT_ARRAY|VT_UI1.                                                                                                                                                                                                             |
        +----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------+

        ### Возвращаемое значение

        +--------------------------+
        | TRUE | - в случае удачи. |
        +--------------------------+
        """
        ...

    def SetTexturePoints(self, Image2D, ImagePoints) -> bool:  # iexternaltessellationobject_settexturepoints.html
        """
        ## SetTexturePoints - Установить координаты вывода текстуры

        ### Синтаксис Automation

        ```
        BOOL SetTexturePoints( BOOL Image2D, VARIANT ImagePoints );
        ```

        ### Входные параметры

        +-------------------------------------------------------------------------------+
        | Image2D     | - TRUE - двумерные координаты, - FALSE - одномерные координаты, |
        +-------------------------------------------------------------------------------+
        | ImagePoints | - Координаты в диапазоне 0...1.                                 |
        +-------------------------------------------------------------------------------+

        ### Возвращаемое значение

        +--------------------------+
        | TRUE | - в случае удачи. |
        +--------------------------+
        """
        ...

    def SetVisible(self, Frame, arg1):
        ...




class IGabaritObject(IDispatch):  # igabaritobject.html
    """
    ## Интерфейс IGabaritObject

    Вспомогательные данные для изменения габарита документа.

    Данный интерфейс может быть получен в событиях интерфейса событий окна документа ksDocumentFrameNotify, ksDocumentFrameNotify::AddGabarit.
    """

    def AddGabarit(self, X1, Y1, Z1, X2, Y2, Z2):  # igabaritobject_addgabarit.html
        """
        ## AddGabarit - Добавить дополнительный габарит к габариту документа

        ### Синтаксис Automation

        ```
        void AddGabarit (double x1,
        double y1,
        double z1,
        double x2,
        double y2,
        double z2);
        ```

        ### Выходные параметры

        +-----------------------------------------------------------------------------------------------+
        | x1, y1, z1, x2, y2, z2 | - координаты габаритной рамки добавляемого дополнительного габарита. |
        +-----------------------------------------------------------------------------------------------+
        """
        ...

    def GetCurrentGabarit(self, NPage):  # igabaritobject_getcurrentgabarit.html
        """
        ## GetCurrentGabarit - Получить текущий габарит документа

        Метод позволяет получить текущий габарит документа в миллиметрах. Для многолистовых документов можно получить габарит конкретного листа.

        ### Синтаксис Automation

        ```
        void GetCurrentGabarit (long nPage,
        double* p1X,
        double* p1Y,
        double* p1Z,
        double* p2X,
        double* p2Y,
        double* p2Z);
        ```

        ### Входные параметры

        +-------------------------------------------+
        | nPage | - номер листа, начиная с единицы. |
        +-------------------------------------------+

        ### Выходные параметры

        +-------------------------------------------------------------------------+
        | p1X, p1Y, p1Z, p2X, p2Y, p2Z | - координаты габаритной рамки документа. |
        +-------------------------------------------------------------------------+
        """
        ...

    def GetGabaritModifying(self) -> bool:  # igabaritobject_getgabaritmodifying.html
        """
        ## GetGabaritModifying - Изменился ли габарит документа

        В момент отработки события Определение габаритов документа метод позволяет узнать менялся ли габарит документа или нет.

        ### Синтаксис Automation

        ```
        BOOL GetGabaritModifying();
        ```

        ### Возвращаемое значение

        +-----------------------------------------+
        | TRUE  | - габарит документа изменен,    |
        +-----------------------------------------+
        | FALSE | - габарит документа не изменен. |
        +-----------------------------------------+
        """
        ...




class ksGLObject(IDispatch):  # ksglobject.html
    """
    ## Интерфейс ksGLObject

    Вспомогательные данные для создания листа в контексте OpenGL.

    1. Данный интерфейс нужно использовать при отрисовке с помощью OpenGL из внешнего приложения (контроллера). При работе из подключаемой библиотеки (dll) данный интерфейс использовать нельзя. Более подробное описание методов смотрите в описании библиотеки OpenGL.

    2. Данный интерфейс может быть получен в событиях интерфейса событий окна документа ksDocumentFrameNotify:

    •ksDocumentFrameNotify::BeginPaintGL,

    •ksDocumentFrameNotify::ClosePaintGL.
    """

    def glBegin(self, Mode):
        ...

    def glColor3d(self, R, G, B):
        ...

    def glDisable(self, Cap):
        ...

    def glEnable(self, Cap):
        ...

    def glEnd(self):
        ...

    def glLineStipple(self, Factor, Pattern):
        ...

    def glLineWidth(self, W):
        ...

    def glPointSize(self, W):
        ...

    def glPolygonMode(self, Face, Mode):
        ...

    def glVertex2d(self, X, Y):
        ...

    def glVertex2dv(self, PData, CountDouble):
        ...

    def glVertex3d(self, X, Y, Z):
        ...

    def glVertex3dv(self, PData, CountDouble):
        ...

    def glVertex4d(self, X, Y, Z, W):
        ...

    def glVertex4dv(self, PData, CountDouble):
        ...




class IPaintObject(IDispatch):  # ipaintobject.html
    """
    ## Интерфейс IPaintObject

    Интерфейс вспомогательных данные для отрисовки документа.

    Данный интерфейс может быть получен в событиях интерфейса событий окна документа ksDocumentFrameNotify:

    •ksDocumentFrameNotify::BeginPaint,

    •ksDocumentFrameNotify::ClosePaint.
    """

    def GetDIBForOutput(self):  # ipaintobject_getdibforoutput.html
        """
        ## GetDIBForOutput - Создать растровое изображение документа в объект «проекция файла» для дорисовки

        Создает объект «проекция файла» (объект ядра в Windows, создаваемый для организации совместного использования несколькими процессами одних и тех же блоков данных) для возможности дорисовки своей геометрии к изображению документа.

        ### Синтаксис Automation

        ```
        BSTR GetDIBForOutput();
        ```

        ### Возвращаемое значение

        +-----------------------------------------------------------------------------+
        | Имя созданного объекта «проекция файла». | - в случае успешного завершения. |
        +-----------------------------------------------------------------------------+
        """
        ...

    def GetHWND(self):  # ipaintobject_gethwnd.html
        """
        ## GetHWND - Получить дескриптор окна

        ### Синтаксис Automation

        ```
        long GetHWND();
        ```

        ### Возвращаемое значение

        +-----------------------------------------------------------------------------+
        | Дескриптор окна в котором идет отрисовка | - в случае успешного завершения. |
        +-----------------------------------------------------------------------------+
        """
        ...

    def GetTransformMatrix(self):  # ipaintobject_gettransformmatrix.html
        """
        ## GetTransformMatrix - Получить коэффициенты для матрицы преобразования координат

        Матрица для преобразования координат mm -> LP:

        Xlp = Xmm * a11 + Ymm * a12 + Zmm * a13 + a14;

        Ylp = Xmm * a21 + Ymm * a22 + Zmm * a23 + a24;

        ### Синтаксис Automation

        ```
        void GetTransformMatrix (double* a11,
        double* a12,
        double* a13,
        double* a14,
        double* a21,
        double* a22,
        double* a23,
        double* a24);
        ```

        ### Выходные параметры

        +-----------------------------------------------------------------------------------------------+
        | a11, a12, a13, a14, a21, a22, a23, a24 | - коэффициенты для матрицы преобразования координат. |
        +-----------------------------------------------------------------------------------------------+
        """
        ...




class IReportProcess(IKompasAPIObject):  # ireportprocess.html
    """
    ## Интерфейс IReportProcess

    Интерфейс для управления процессом Создать отчет.

    Интерфейс позволяет задавать параметры для процесса Создать отчет.

    Данный интерфейс можно получить с помощью свойства IKompasDocument1::GetInterface c типом ksObjectReportProcess.
    """

    @property
    def ActiveStyleIndex(self) -> typing.Any:  # ireportprocess_activestyleindex.html
        """
        ## ActiveStyleIndex - Активный стиль отчета

        Тип данных: VARIANT

        Тип VT_I4 - номер стиля в списке стилей. При установке стиля можно также использовать VT_R8 - уникальный номер стиля.

        ### Синтаксис Automation

        ```
        ActiveStyleIndex = Object.ActiveStyleIndex       # Получить свойство (* )
        Object.ActiveStyleIndex = ActiveStyleIndex       # Установить свойство (* )
        ActiveStyleIndex = Object.GetActiveStyleIndex()  # Получить свойство (**)
        Object.SetActiveStyleIndex( ActiveStyleIndex )   # Установить свойство (**)
        ```
        """
        ...
    @ActiveStyleIndex.setter
    def ActiveStyleIndex(self, value: typing.Any): ...

    @property
    def ReportFilter(self) -> IReportFilter:  # ireportprocess_reportfilter.html
        """
        ## ReportFilter - Интерфейс управления фильтрами

        Тип данных: указатель на интерфейс свойства IReportFilter

        Свойство только для чтения.

        ### Синтаксис Automation

        ```
        ReportFilter = Object.ReportFilter       # Получить свойство (* )
        ReportFilter = Object.GetReportFilter()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def ReportFilterUse(self) -> bool:  # ireportprocess_reportfilteruse.html
        """
        ## ReportFilterUse - Использовать фильтр

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        ReportFilterUse = Object.ReportFilterUse       # Получить свойство (* )
        Object.ReportFilterUse = ReportFilterUse       # Установить свойство (* )
        ReportFilterUse = Object.GetReportFilterUse()  # Получить свойство (**)
        Object.SetReportFilterUse( ReportFilterUse )   # Установить свойство (**)
        ```
        """
        ...
    @ReportFilterUse.setter
    def ReportFilterUse(self, value: bool): ...

    @property
    def StylesCount(self) -> int:  # ireportprocess_stylescount.html
        """
        ## StylesCount - Количество стилей отчета

        Тип данных: long

        Свойство только для чтения.

        ### Синтаксис Automation

        ```
        StylesCount = Object.StylesCount       # Получить свойство (* )
        StylesCount = Object.GetStylesCount()  # Получить свойство (**)
        ```
        """
        ...




class IReportFilter(IDispatch):  # ireportfilter.html
    """
    ## Интерфейс IReportFilter

    Интерфейс для управления фильтрами в процессе Создать отчет.

    Позволяет управлять фильтром в процессе Создать отчет.

    Данный интерфейс можно получить с помощью свойства IReportProcess::ReportFilter.
    """

    @property
    def ConditionCount(self) -> int:  # ireportfilter_conditioncount.html
        """
        ## ConditionCount - Количество условий

        Тип данных: long

        Свойство только для чтения.

        ### Синтаксис Automation

        ```
        ConditionCount = Object.ConditionCount       # Получить свойство (* )
        ConditionCount = Object.GetConditionCount()  # Получить свойство (**)
        ```
        """
        ...

    def Clear(self) -> bool:  # ireportfilter_clear.html
        """
        ## Clear - Удалить все условия фильтра

        ### Синтаксис Automation

        ```
        BOOL Clear();
        ```

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def GetCondition(self, Index) -> bool:  # ireportfilter_getcondition.html
        """
        ## GetCondition - Получить условия фильтра по индексу

        Свойство только для чтения.

        ### Синтаксис Automation

        ```
        BOOL GetCondition( long index, VARIANT * uniqId, ksReportFiltersTypeEnum * type, VARIANT * val );
        ```

        ### Входные параметры

        +-----------------------------+
        | index | - индекс в массиве. |
        +-----------------------------+

        ### Выходные параметры

        +-----------------------------------------------------------------------------------------------------------+
        | uniqId | - уникальный номер свойства VT_R8,                                                               |
        +-----------------------------------------------------------------------------------------------------------+
        | type   | - условие фильтрации,                                                                            |
        +-----------------------------------------------------------------------------------------------------------+
        | val    | - значение фильтра; тип значения фильтра зависит от типа свойства, для которого задается фильтр. |
        +-----------------------------------------------------------------------------------------------------------+

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def RemoveCondition(self, Index) -> bool:  # ireportfilter_removecondition.html
        """
        ## RemoveCondition - Удалить условия фильтра по индексу

        ### Синтаксис Automation

        ```
        BOOL RemoveCondition( long index )
        ```

        ### Входные параметры

        +-----------------------------+
        | index | - индекс в массиве. |
        +-----------------------------+

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def SetCondition(self, Index, UniqId, Type, Val) -> bool:  # ireportfilter_setcondition.html
        """
        ## SetCondition - Установить условия фильтра по индексу

        ### Синтаксис Automation

        ```
        BOOL SetCondition( long index, VARIANT uniqId, ksReportFiltersTypeEnum type, VARIANT Val );
        ```

        ### Входные параметры

        +-----------------------------------------------------------------------------------------------------------+
        | index  | - индекс в массиве; -1 - добавить в конец,                                                       |
        +-----------------------------------------------------------------------------------------------------------+
        | uniqId | - уникальный номер свойства VT_R8,                                                               |
        +-----------------------------------------------------------------------------------------------------------+
        | type   | - условие фильтрации,                                                                            |
        +-----------------------------------------------------------------------------------------------------------+
        | val    | - значение фильтра; тип значения фильтра зависит от типа свойства, для которого задается фильтр. |
        +-----------------------------------------------------------------------------------------------------------+

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...




class IReportObjectsFilter(IDispatch):  # ireportobjectsfilter.html
    """
    ## Интерфейс IReportObjectsFilter

    Интерфейс фильтра объектов при создании отчета.

    Дополнительный интерфейс для таблицы отчетов IReport.

    Интерфейс можно получить посредством вызова метода IUnknown::QueryInterface (const GUID far& iid, void** pif).
    """

    @property
    def Bodies(self) -> bool:  # ireportobjectsfilter_bodies.html
        """
        ## Bodies - Собирать тела

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        Bodies = Object.Bodies       # Получить свойство(* )
        Object.Bodies = Bodies       # Установить свойство (* )
        Bodies = Object.GetBodies()  # Получить свойство (**)
        Object.SetBodies( Bodies )   # Установить свойство (**)
        ```
        """
        ...
    @Bodies.setter
    def Bodies(self, value: bool): ...

    @property
    def InsertionFragments(self) -> bool:  # ireportobjectsfilter_insertionfragments.html
        """
        ## InsertionFragments - Собирать вставки фрагментов

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        InsertionFragments = Object.InsertionFragments       # Получить свойство(* )
        Object.InsertionFragments = InsertionFragments       # Установить свойство (* )
        InsertionFragments = Object.GetInsertionFragments()  # Получить свойство (**)
        Object.SetInsertionFragments( InsertionFragments )   # Установить свойство (**)
        ```
        """
        ...
    @InsertionFragments.setter
    def InsertionFragments(self, value: bool): ...

    @property
    def InsertionViews(self) -> bool:  # ireportobjectsfilter_insertionviews.html
        """
        ## InsertionViews - Собирать вставки видов

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        InsertionViews = Object.InsertionViews       # Получить свойство(* )
        Object.InsertionViews = InsertionViews       # Установить свойство (* )
        InsertionViews = Object.GetInsertionViews()  # Получить свойство (**)
        Object.SetInsertionViews( InsertionViews )   # Установить свойство (**)
        ```
        """
        ...
    @InsertionViews.setter
    def InsertionViews(self, value: bool): ...

    @property
    def LocalParts(self) -> bool:  # ireportobjectsfilter_localparts.html
        """
        ## LocalParts - Собирать локальные вставки компонентов

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        LocalParts = Object.LocalParts       # Получить свойство(* )
        Object.LocalParts = LocalParts       # Установить свойство (* )
        LocalParts = Object.GetLocalParts()  # Получить свойство (**)
        Object.SetLocalParts( LocalParts )   # Установить свойство (**)
        ```
        """
        ...
    @LocalParts.setter
    def LocalParts(self, value: bool): ...

    @property
    def MacroObjects2D(self) -> bool:  # ireportobjectsfilter_macroobjects2d.html
        """
        ## MacroObjects2D - Собирать макроэлементы

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        MacroObjects2D = Object.MacroObjects2D       # Получить свойство(* )
        Object.MacroObjects2D = MacroObjects2D       # Установить свойство (* )
        MacroObjects2D = Object.GetMacroObjects2D()  # Получить свойство (**)
        Object.SetMacroObjects2D( MacroObjects2D )   # Установить свойство (**)
        ```
        """
        ...
    @MacroObjects2D.setter
    def MacroObjects2D(self, value: bool): ...

    @property
    def ModelObjects(self) -> bool:  # ireportobjectsfilter_modelobjects.html
        """
        ## ModelObjects - Собирать объекты моделей с ассоциативных видов

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        ModelObjects = Object.ModelObjects       # Получить свойство(* )
        Object.ModelObjects = ModelObjects       # Установить свойство (* )
        ModelObjects = Object.GetModelObjects()  # Получить свойство (**)
        Object.SetModelObjects( ModelObjects )   # Установить свойство (**)
        ```
        """
        ...
    @ModelObjects.setter
    def ModelObjects(self, value: bool): ...

    @property
    def Parts(self) -> bool:  # ireportobjectsfilter_parts.html
        """
        ## Parts - Собирать компоненты

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        Parts = Object.Parts       # Получить свойство(* )
        Object.Parts = Parts       # Установить свойство (* )
        Parts = Object.GetParts()  # Получить свойство (**)
        Object.SetParts( Parts )   # Установить свойство (**)
        ```
        """
        ...
    @Parts.setter
    def Parts(self, value: bool): ...

    @property
    def SpecialInfoObjects(self) -> bool:  # ireportobjectsfilter_specialinfoobjects.html
        """
        ## SpecialInfoObjects - Собирать специальные объекты

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        SpecialInfoObjects = Object.SpecialInfoObjects       # Получить свойство(* )
        Object.SpecialInfoObjects = SpecialInfoObjects       # Установить свойство (* )
        SpecialInfoObjects = Object.GetSpecialInfoObjects()  # Получить свойство (**)
        Object.SetSpecialInfoObjects( SpecialInfoObjects )   # Установить свойство (**)
        ```
        """
        ...
    @SpecialInfoObjects.setter
    def SpecialInfoObjects(self, value: bool): ...

    @property
    def Views(self) -> bool:  # ireportobjectsfilter_views.html
        """
        ## Views - Собирать виды (Объекты группируются по видам)

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        Views = Object.Views       # Получить свойство(* )
        Object.Views = Views       # Установить свойство (* )
        Views = Object.GetViews()  # Получить свойство (**)
        Object.SetViews( Views )   # Установить свойство (**)
        ```
        """
        ...
    @Views.setter
    def Views(self, value: bool): ...




class IReport(IKompasAPIObject):  # ireport.html
    """
    ## Интерфейс IReport

    Интерфейс таблицы отчетов.
    """

    @property
    def CurrentReportStyle(self) -> IReportStyle:  # ireport_currentreportstyle.html
        """
        ## CurrentReportStyle - Cтиль отчета

        Тип данных: Указатель на интерфейс IReportStyle

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        CurrentReportStyle = Object.CurrentReportStyle       # Получить свойство (* )
        CurrentReportStyle = Object.GetCurrentReportStyle()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def CurrentStyleIndex(self) -> int:  # ireport_currentstyleindex.html
        """
        ## CurrentStyleIndex - Индекс текущего стиля

        Тип данных: long

        ### Синтаксис Automation

        ```
        CurrentStyleIndex = Object.CurrentStyleIndex       # Получить свойство(* )
        Object.CurrentStyleIndex = CurrentStyleIndex       # Установить свойство (* )
        CurrentStyleIndex = Object.GetCurrentStyleIndex()  # Получить свойство (**)
        Object.SetCurrentStyleIndex( CurrentStyleIndex )   # Установить свойство (**)
        ```
        """
        ...
    @CurrentStyleIndex.setter
    def CurrentStyleIndex(self, value: int): ...

    @property
    def ReportFilter(self) -> IReportFilter:  # ireport_reportfilter.html
        """
        ## ReportFilter - Интерфейс управления фильтрами

        Тип данных: Указатель на интерфейс IReportFilter

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        ReportFilter = Object.ReportFilter       # Получить свойство (* )
        ReportFilter = Object.GetReportFilter()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def ReportType(self) -> int:  # ireport_reporttype.html
        """
        ## ReportType - Тип отчета (по свойствам, по исполнениям, по переменным)

        Тип данных: из перечисления ksReportTypeEnum

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        ReportType = Object.ReportType       # Получить свойство (* )
        ReportType = Object.GetReportType()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def ShowAllObjects(self) -> bool:  # ireport_showallobjects.html
        """
        ## ShowAllObjects - Показывать все объекты

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        ShowAllObjects = Object.ShowAllObjects       # Получить свойство(* )
        Object.ShowAllObjects = ShowAllObjects       # Установить свойство (* )
        ShowAllObjects = Object.GetShowAllObjects()  # Получить свойство (**)
        Object.SetShowAllObjects( ShowAllObjects )   # Установить свойство (**)
        ```
        """
        ...
    @ShowAllObjects.setter
    def ShowAllObjects(self, value: bool): ...

    @property
    def SourceFileName(self) -> str:  # ireport_sourcefilename.html
        """
        ## SourceFileName - Имя файла

        Тип данных: BSTR

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        SourceFileName = Object.SourceFileName       # Получить свойство (* )
        SourceFileName = Object.GetSourceFileName()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def StylesCount(self) -> int:  # ireport_stylescount.html
        """
        ## StylesCount - Количество стилей

        Тип данных: long

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        StylesCount = Object.StylesCount       # Получить свойство (* )
        StylesCount = Object.GetStylesCount()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def UseReportFilter(self) -> bool:  # ireport_usereportfilter.html
        """
        ## UseReportFilter - Использовать фильтр

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        UseReportFilter = Object.UseReportFilter       # Получить свойство(* )
        Object.UseReportFilter = UseReportFilter       # Установить свойство (* )
        UseReportFilter = Object.GetUseReportFilter()  # Получить свойство (**)
        Object.SetUseReportFilter( UseReportFilter )   # Установить свойство (**)
        ```
        """
        ...
    @UseReportFilter.setter
    def UseReportFilter(self, value: bool): ...

    def AddStyle(self, FileName, Id) -> IReportStyle:  # ireport_addstyle.html
        """
        ## AddStyle - Добавить новый стиль

        ### Синтаксис Automation

        ```
        LPDISPATCH AddStyle( BSTR FileName, double Id );
        ```

        ### Входные параметры

        +---------------------------------------------------+
        | FileName | - имя файла библиотеки стилей отчетов, |
        +---------------------------------------------------+
        | Id       | - идентификатор стиля.                 |
        +---------------------------------------------------+

        ### Возвращаемое значение

        - Указатель на интерфейс стиля отчета IReportStyle.
        """
        ...

    def LoadSourceDocument(self, Visible, ReadOnly) -> IKompasDocument:  # ireport_loadsourcedocument.html
        """
        ## LoadSourceDocument - Документ

        ### Синтаксис Automation

        ```
        LPDISPATCH LoadSourceDocument( BOOL Visible, BOOL ReadOnly );
        ```

        ### Входные параметры

        +---------------------------------------+
        | Visible  | - видимый режим,           |
        +---------------------------------------+
        | ReadOnly | - режим только для чтения. |
        +---------------------------------------+

        ### Возвращаемое значение

        Указатель на документ IKompasDocument.
        """
        ...

    def Rebuild(self) -> bool:  # ireport_rebuild.html
        """
        ## Rebuild - Перестроить отчет

        ### Синтаксис Automation

        ```
        BOOL Rebuild();
        ```

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def ReportStyle(self, Index) -> IReportStyle:  # ireport_reportstyle.html
        """
        ## ReportStyle - Cтиль отчета по индексу

        Тип данных: Указатель на интерфейс IReportStyle

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        ReportStyle = Object.ReportStyle( Index )     # Получить свойство (* )
        ReportStyle = Object.GetReportStyle( Index )  # Получить свойство (**)
        ```
        """
        ...

    def SaveAs(self, FileName) -> bool:  # ireport_saveas.html
        """
        ## SaveAs - Отчет сохранить как

        ### Синтаксис Automation

        ```
        BOOL SaveAs( VARIANT FileName );
        ```

        ### Входные параметры

        +-------------------------------------------------------------------------+
        | FileName | - имя сохраняемого файла или указатель на открытый документ. |
        +-------------------------------------------------------------------------+

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...




class IReportStyle(IKompasAPIObject):  # ireportstyle.html
    """
    ## Интерфейс IReportStyle

    Интерфейс стиля отчета.
    """

    @property
    def ColumnNumberingInitVal(self) -> int:  # ireportstyle_columnnumberinginitval.html
        """
        ## ColumnNumberingInitVal - Нумеровать колонки, начиная с

        Тип данных: long

        ### Синтаксис Automation

        ```
        ColumnNumberingInitVal = Object.ColumnNumberingInitVal       # Получить свойство(* )
        Object.ColumnNumberingInitVal = ColumnNumberingInitVal       # Установить свойство (* )
        ColumnNumberingInitVal = Object.GetColumnNumberingInitVal()  # Получить свойство (**)
        Object.SetColumnNumberingInitVal( ColumnNumberingInitVal )   # Установить свойство (**)
        ```
        """
        ...
    @ColumnNumberingInitVal.setter
    def ColumnNumberingInitVal(self, value: int): ...

    @property
    def ColumnNumberingType(self) -> int:  # ireportstyle_columnnumberingtype.html
        """
        ## ColumnNumberingType - Формат нумерации столбцов отчета

        Тип данных: из перечисления ksNumberingTypeEnum

        ### Синтаксис Automation

        ```
        ColumnNumberingType = Object.ColumnNumberingType       # Получить свойство(* )
        Object.ColumnNumberingType = ColumnNumberingType       # Установить свойство (* )
        ColumnNumberingType = Object.GetColumnNumberingType()  # Получить свойство (**)
        Object.SetColumnNumberingType( ColumnNumberingType )   # Установить свойство (**)
        ```
        """
        ...
    @ColumnNumberingType.setter
    def ColumnNumberingType(self, value: int): ...

    @property
    def ColumnsCount(self) -> int:  # ireportstyle_columnscount.html
        """
        ## ColumnsCount - Количество колонок

        Тип данных:long

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        ColumnsCount = Object.ColumnsCount       # Получить свойство (* )
        ColumnsCount = Object.GetColumnsCount()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def DivideIntoPage(self) -> bool:  # ireportstyle_divideintopage.html
        """
        ## DivideIntoPage - Разбивать на страницы

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        DivideIntoPage = Object.DivideIntoPage       # Получить свойство(* )
        Object.DivideIntoPage = DivideIntoPage       # Установить свойство (* )
        DivideIntoPage = Object.GetDivideIntoPage()  # Получить свойство (**)
        Object.SetDivideIntoPage( DivideIntoPage )   # Установить свойство (**)
        ```
        """
        ...
    @DivideIntoPage.setter
    def DivideIntoPage(self, value: bool): ...

    @property
    def DrawBottom(self) -> bool:  # ireportstyle_drawbottom.html
        """
        ## DrawBottom - true - сверху вниз (как обычно), false - отрисовка снизу вверх

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        DrawBottom = Object.DrawBottom       # Получить свойство(* )
        Object.DrawBottom = DrawBottom       # Установить свойство (* )
        DrawBottom = Object.GetDrawBottom()  # Получить свойство (**)
        Object.SetDrawBottom( DrawBottom )   # Установить свойство (**)
        ```
        """
        ...
    @DrawBottom.setter
    def DrawBottom(self, value: bool): ...

    @property
    def Id(self) -> float:  # ireportstyle_id.html
        """
        ## Id - Уникальный номер

        Тип данных: double

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        Id = Object.Id       # Получить свойство (* )
        Id = Object.GetId()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def Name(self) -> str:  # ireportstyle_name.html
        """
        ## Name - Имя

        Тип данных: BSTR

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        Name = Object.Name       # Получить свойство(* )
        Object.Name = Name       # Установить свойство (* )
        Name = Object.GetName()  # Получить свойство (**)
        Object.SetName( Name )   # Установить свойство (**)
        ```
        """
        ...
    @Name.setter
    def Name(self, value: str): ...

    @property
    def RowHeight(self) -> int:  # ireportstyle_rowheight.html
        """
        ## RowHeight - Высота строки ячейки таблицы, мм

        Тип данных: long

        ### Синтаксис Automation

        ```
        RowHeight = Object.RowHeight       # Получить свойство(* )
        Object.RowHeight = RowHeight       # Установить свойство (* )
        RowHeight = Object.GetRowHeight()  # Получить свойство (**)
        Object.SetRowHeight( RowHeight )   # Установить свойство (**)
        ```
        """
        ...
    @RowHeight.setter
    def RowHeight(self, value: int): ...

    @property
    def RowsCount(self) -> int:  # ireportstyle_rowscount.html
        """
        ## RowsCount - Количество строк таблицы

        Тип данных: long

        ### Синтаксис Automation

        ```
        RowsCount = Object.RowsCount       # Получить свойство(* )
        Object.RowsCount = RowsCount       # Установить свойство (* )
        RowsCount = Object.GetRowsCount()  # Получить свойство (**)
        Object.SetRowsCount( RowsCount )   # Установить свойство (**)
        ```
        """
        ...
    @RowsCount.setter
    def RowsCount(self, value: int): ...

    @property
    def RowsNumberingType(self) -> int:  # ireportstyle_rowsnumberingtype.html
        """
        ## RowsNumberingType - Нумеровать строки

        Тип данных: из перечисления ksRowsNumberingTypeEnum

        ### Синтаксис Automation

        ```
        RowsNumberingType = Object.RowsNumberingType       # Получить свойство(* )
        Object.RowsNumberingType = RowsNumberingType       # Установить свойство (* )
        RowsNumberingType = Object.GetRowsNumberingType()  # Получить свойство (**)
        Object.SetRowsNumberingType( RowsNumberingType )   # Установить свойство (**)
        ```
        """
        ...
    @RowsNumberingType.setter
    def RowsNumberingType(self, value: int): ...

    @property
    def ShowEmptyRows(self) -> bool:  # ireportstyle_showemptyrows.html
        """
        ## ShowEmptyRows - Выводить пустые строки

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        ShowEmptyRows = Object.ShowEmptyRows       # Получить свойство(* )
        Object.ShowEmptyRows = ShowEmptyRows       # Установить свойство (* )
        ShowEmptyRows = Object.GetShowEmptyRows()  # Получить свойство (**)
        Object.SetShowEmptyRows( ShowEmptyRows )   # Установить свойство (**)
        ```
        """
        ...
    @ShowEmptyRows.setter
    def ShowEmptyRows(self, value: bool): ...

    @property
    def ShowTitle(self) -> bool:  # ireportstyle_showtitle.html
        """
        ## ShowTitle - Отображать заголовок

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        ShowTitle = Object.ShowTitle       # Получить свойство(* )
        Object.ShowTitle = ShowTitle       # Установить свойство (* )
        ShowTitle = Object.GetShowTitle()  # Получить свойство (**)
        Object.SetShowTitle( ShowTitle )   # Установить свойство (**)
        ```
        """
        ...
    @ShowTitle.setter
    def ShowTitle(self, value: bool): ...

    @property
    def TitleHeight(self) -> int:  # ireportstyle_titleheight.html
        """
        ## TitleHeight - Высота заголовка, мм

        Тип данных: long

        ### Синтаксис Automation

        ```
        TitleHeight = Object.TitleHeight       # Получить свойство(* )
        Object.TitleHeight = TitleHeight       # Установить свойство (* )
        TitleHeight = Object.GetTitleHeight()  # Получить свойство (**)
        Object.SetTitleHeight( TitleHeight )   # Установить свойство (**)
        ```
        """
        ...
    @TitleHeight.setter
    def TitleHeight(self, value: int): ...

    def AddColumn(self, IndexAt, Name, Property):  # ireportstyle_addcolumn.html
        """
        ## AddColumn - Добавить колонку

        ### Синтаксис Automation

        ```
        LPDISPATCH AddColumn( long IndexAt, BSTR Name, IProperty * Property );
        ```

        ### Входные параметры

        +----------------------------------------------------------------------------------------------------------+
        | IndexAt  | - индекс добавляемой колонки,                                                                 |
        +----------------------------------------------------------------------------------------------------------+
        | Name     | - имя колонки,                                                                                |
        +----------------------------------------------------------------------------------------------------------+
        | Property | - указатель на интерфейс свойства, которое будет отображаться в колонке. Можно передать NULL. |
        +----------------------------------------------------------------------------------------------------------+

        ### Возвращаемое значение

        - указатель на интерфейс колонки стиля.
        """
        ...

    def Clear(self) -> bool:  # ireportstyle_clear.html
        """
        ## Clear - Очистить стиль

        ### Синтаксис Automation

        ```
        BOOL Clear();
        ```

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def ClearSortParameters(self) -> bool:  # ireportstyle_clearsortparameters.html
        """
        ## ClearSortParameters - Очистить параметры сортировки

        ### Синтаксис Automation

        ```
        BOOL ClearSortParameters();
        ```

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def Column(self, Index) -> IReportStyleColumn:  # ireportstyle_column.html
        """
        ## Column - Колонка отчета

        Тип данных:Указатель на интерфейс IReportStyleColumn.

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        Column = Object.Column( Index )     # Получить свойство (* )
        Column = Object.GetColumn( Index )  # Получить свойство (**)
        ```
        """
        ...

    def DeleteColumn(self, Index) -> bool:  # ireportstyle_deletecolumn.html
        """
        ## DeleteColumn - Удалить колонку

        ### Синтаксис Automation

        ```
        BOOL DeleteColumn( VARIANT Index );
        ```

        ### Входные параметры

        +-------------------------------------+
        | Index | - индекс удаляемой колонки. |
        +-------------------------------------+

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def GetSortParameters(self, Level) -> bool:  # ireportstyle_getsortparameters.html
        """
        ## GetSortParameters - Параметры сортировки

        ### Синтаксис Automation

        ```
        BOOL GetSortParameters( long Level, long * ColumnNumber, ksSortTypeEnum * SortType );
        ```

        ### Входные параметры

        +-------------------------------+
        | Level | - уровень сортировки. |
        +-------------------------------+

        ### Выходные параметры

        +----------------------------------+
        | ColumnNumber | - номер колонки,  |
        +----------------------------------+
        | SortType     | - тип сортировки. |
        +----------------------------------+

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def Init(self, Type) -> bool:  # ireportstyle_init.html
        """
        ## Init - Инициализация стиля параметрами по умолчанию

        ### Синтаксис Automation

        ```
        BOOL Init( ksReportStyleInitEnum Type );
        ```

        ### Входные параметры

        +-----------------------------------+
        | Type | - тип инициализации стиля. |
        +-----------------------------------+

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def SetSortParameters(self, Level, ColumnNumber, SortType) -> bool:  # ireportstyle_setsortparameters.html
        """
        ## SetSortParameters - Параметры сортировки

        ### Синтаксис Automation

        ```
        BOOL SetSortParameters( long Level, long ColumnNumber, ksSortTypeEnum SortType );
        ```

        ### Входные параметры

        +--------------------------------------+
        | Level        | - уровень сортировки, |
        +--------------------------------------+
        | ColumnNumber | - номер колонки,      |
        +--------------------------------------+
        | SortType     | - тип сортировки.     |
        +--------------------------------------+

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def SwapColumn(self, Index1, Index2) -> bool:  # ireportstyle_swapcolumn.html
        """
        ## SwapColumn - Поменять колонки местами

        ### Синтаксис Automation

        ```
        BOOL SwapColumn( long Index1, long Index2 );
        ```

        ### Входные параметры

        +--------------------------------+
        | Index1 | - индекс 1-й колонки, |
        +--------------------------------+
        | Index2 | - индекс 2-й колонки. |
        +--------------------------------+

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...




class IReportStyleColumn(IKompasAPIObject):  # ireportstylecolumn.html
    """
    ## Интерфейс IReportStyleColumn

    Интерфейс колонки стиля отчета.
    """

    @property
    def CombineCells(self) -> bool:  # ireportstylecolumn_combinecells.html
        """
        ## CombineCells - Объединять ячейки с одинаковыми значениями в колонке

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        CombineCells = Object.CombineCells       # Получить свойство (* )
        Object.CombineCells = CombineCells       # Установить свойство (* )
        CombineCells = Object.GetCombineCells()  # Получить свойство (**)
        Object.SetCombineCells( CombineCells )   # Установить свойство (**)
        ```
        """
        ...
    @CombineCells.setter
    def CombineCells(self, value: bool): ...

    @property
    def GroupeType(self) -> int:  # ireportstylecolumn_groupetype.html
        """
        ## GroupeType - Тип группировки колонки отчета

        Тип данных: из перечисления ksGroupeTypeEnum

        ### Синтаксис Automation

        ```
        GroupeType = Object.GroupeType       # Получить свойство (* )
        Object.GroupeType = GroupeType       # Установить свойство (* )
        GroupeType = Object.GetGroupeType()  # Получить свойство (**)
        Object.SetGroupeType( GroupeType )   # Установить свойство (**)
        ```
        """
        ...
    @GroupeType.setter
    def GroupeType(self, value: int): ...

    @property
    def Header(self) -> str:  # ireportstylecolumn_header.html
        """
        ## Header - Имя колонки

        Тип данных: BSTR

        ### Синтаксис Automation

        ```
        Header = Object.Header       # Получить свойство (* )
        Object.Header = Header       # Установить свойство (* )
        Header = Object.GetHeader()  # Получить свойство (**)
        Object.SetHeader( Header )   # Установить свойство (**)
        ```
        """
        ...
    @Header.setter
    def Header(self, value: str): ...

    @property
    def Id(self) -> float:  # ireportstylecolumn_id.html
        """
        ## Id - Уникальный номер

        Тип данных: double

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        Id = Object.Id       # Получить свойство(* )
        Id = Object.GetId()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def Property(self):
        ...
    @Property.setter
    def Property(self, value): ...

    @property
    def PropertyLevel(self):
        ...
    @PropertyLevel.setter
    def PropertyLevel(self, value): ...

    @property
    def TextAlign(self) -> int:  # ireportstylecolumn_textalign.html
        """
        ## TextAlign - Выравнивание в колонке отчета

        Тип данных: из перечисления ksAlignEnum

        ### Синтаксис Automation

        ```
        TextAlign = Object.TextAlign       # Получить свойство (* )
        Object.TextAlign = TextAlign       # Установить свойство (* )
        TextAlign = Object.GetTextAlign()  # Получить свойство (**)
        Object.SetTextAlign( TextAlign )   # Установить свойство (**)
        ```
        """
        ...
    @TextAlign.setter
    def TextAlign(self, value: int): ...

    @property
    def UserColumn(self):
        ...

    @property
    def ValueFormat(self):  # ireportstylecolumn_valueformat.html
        """
        ## ValueFormat - Формат значения

        Тип данных: из перечисления ksValueFormatEnum

        ### Синтаксис Automation

        ```
        ValueFormat = Object.ValueFormat       # Получить свойство (* )
        Object.ValueFormat = ValueFormat       # Установить свойство (* )
        ValueFormat = Object.GetValueFormat()  # Получить свойство (**)
        Object.SetValueFormat( ValueFormat )   # Установить свойство (**)
        ```
        """
        ...
    @ValueFormat.setter
    def ValueFormat(self, value): ...

    @property
    def Visible(self) -> bool:  # ireportstylecolumn_visible.html
        """
        ## Visible - Отображать в отчете

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        Visible = Object.Visible       # Получить свойство (* )
        Object.Visible = Visible       # Установить свойство (* )
        Visible = Object.GetVisible()  # Получить свойство (**)
        Object.SetVisible( Visible )   # Установить свойство (**)
        ```
        """
        ...
    @Visible.setter
    def Visible(self, value: bool): ...

    @property
    def Width(self) -> float:  # ireportstylecolumn_width.html
        """
        ## Width - Ширина колонки

        Тип данных: double

        ### Синтаксис Automation

        ```
        Width = Object.Width       # Получить свойство (* )
        Object.Width = Width       # Установить свойство (* )
        Width = Object.GetWidth()  # Получить свойство (**)
        Object.SetWidth( Width )   # Установить свойство (**)
        ```
        """
        ...
    @Width.setter
    def Width(self, value: float): ...

    @property
    def WordWrap(self) -> bool:  # ireportstylecolumn_wordwrap.html
        """
        ## WordWrap - Переносить по словам

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        WordWrap = Object.WordWrap       # Получить свойство (* )
        Object.WordWrap = WordWrap       # Установить свойство (* )
        WordWrap = Object.GetWordWrap()  # Получить свойство (**)
        Object.SetWordWrap( WordWrap )   # Установить свойство (**)
        ```
        """
        ...
    @WordWrap.setter
    def WordWrap(self, value: bool): ...




class IReportTable(IDispatch):  # ireporttable.html
    """
    ## Интерфейс IReportTable

    Интерфейс таблицы данных.
    """

    @property
    def ColumnsCount(self) -> int:  # ireporttable_columnscount.html
        """
        ## ColumnsCount - Количество столбцов

        Тип данных: long.

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        ColumnsCount = Object.ColumnsCount       # Получить свойство(* )
        ColumnsCount = Object.GetColumnsCount()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def DisplayMode(self) -> bool:  # ireporttable_displaymode.html
        """
        ## DisplayMode - TRUE - Работает с отображаемой таблицей (скрытые колонки и строки не выдаются)

        Тип данных: BOOL.

        ### Синтаксис Automation

        ```
        DisplayMode = Object.DisplayMode       # Получить свойство (* )
        Object.DisplayMode = DisplayMode       # Установить свойство (* )
        DisplayMode = Object.GetDisplayMode()  # Получить свойство (**)
        Object.SetDisplayMode( DisplayMode )   # Установить свойство (**)
        ```
        """
        ...
    @DisplayMode.setter
    def DisplayMode(self, value: bool): ...

    @property
    def RowsCount(self) -> int:  # ireporttable_rowscount.html
        """
        ## RowsCount - Количество строк

        Тип данных: long

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        RowsCount = Object.RowsCount       # Получить свойство(* )
        RowsCount = Object.GetRowsCount()  # Получить свойство (**)
        ```
        """
        ...

    def CellValue(self, NRow, NCol) -> typing.Any:  # ireporttable_cellvalue.html
        """
        ## CellValue - Значение в ячейке

        Тип данных: VARIANT

        ### Синтаксис Automation

        ```
        CellValue = Object.CellValue( NRow, NCol )     # Получить свойство (* )
        Object.CellValue( NRow, NCol ) = CellValue     # Установить свойство (* )
        CellValue = Object.GetCellValue( NRow, NCol )  # Получить свойство (**)
        Object.SetCellValue( NRow, NCol, CellValue )   # Установить свойство (**)
        ```

        ### Входные параметры

        +------------------------------+
        | long NRow | - номер строки,  |
        +------------------------------+
        | long NCol | - номер колонки. |
        +------------------------------+
        """
        ...

    def FormatCellValue(self, NRow, NCol) -> str:  # ireporttable_formatcellvalue.html
        """
        ## FormatCellValue - Форматированное значение в ячейке

        Тип данных: BSTR.

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        FormatCellValue = Object.FormatCellValue( NRow, NCol )     # Получить свойство(* )
        FormatCellValue = Object.GetFormatCellValue( NRow, NCol )  # Получить свойство (**)
        ```

        ### Входные параметры

        +------------------------------+
        | long NRow | - номер строки,  |
        +------------------------------+
        | long NCol | - номер колонки. |
        +------------------------------+
        """
        ...

    def PropertyKeeper(self, RowIndex) -> IPropertyKeeper:  # ireporttable_propertykeeper.html
        """
        ## PropertyKeeper - Объект отчета

        Тип данных: Указатель на интерфейс IPropertyKeeper

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        PropertyKeeper = Object.PropertyKeeper( RowIndex )     # Получить свойство(* )
        PropertyKeeper = Object.GetPropertyKeeper( RowIndex )  # Получить свойство (**)
        ```
        """
        ...

    def SetCellValue(self, NRow, NCol, arg2):
        ...

    def StyleColumn(self, NCol) -> IReportStyleColumn:  # ireporttable_stylecolumn.html
        """
        ## StyleColumn - Параметры отображаемой в отчете колонки

        Тип данных: Указатель на интерфейс IReportStyleColumn

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        StyleColumn = Object.StyleColumn( NCol )     # Получить свойство(* )
        StyleColumn = Object.GetStyleColumn( NCol )  # Получить свойство (**)
        ```
        """
        ...

    def Text(self, NRow, NCol) -> IText:  # ireporttable_text.html
        """
        ## Text - Текст в ячейке

        Тип данных: Указатель на интерфейс IText.

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        Text = Object.Text( NRow, NCol )     # Получить свойство(* )
        Text = Object.GetText( NRow, NCol )  # Получить свойство (**)
        ```
        """
        ...




class IReportParam(IDispatch):  # ireportparam.html
    """
    ## Интерфейс IReportParam

    Интерфейс параметров отчета.
    """

    @property
    def BuildingType(self) -> int:  # ireportparam_buildingtype.html
        """
        ## BuildingType - Способ выбора объектов

        Тип данных: из перечисления ksReportBuildingTypeEnum

        ### Синтаксис Automation

        ```
        BuildingType = Object.BuildingType       # Получить свойство (* )
        Object.BuildingType = BuildingType       # Установить свойство (* )
        BuildingType = Object.GetBuildingType()  # Получить свойство (**)
        Object.SetBuildingType( BuildingType )   # Установить свойство (**)
        ```
        """
        ...
    @BuildingType.setter
    def BuildingType(self, value: int): ...

    @property
    def LevelsCount(self) -> int:  # ireportparam_levelscount.html
        """
        ## LevelsCount - Число уровней

        Тип данных: long

        ### Синтаксис Automation

        ```
        LevelsCount = Object.LevelsCount       # Получить свойство (* )
        Object.LevelsCount = LevelsCount       # Установить свойство (* )
        LevelsCount = Object.GetLevelsCount()  # Получить свойство (**)
        Object.SetLevelsCount( LevelsCount )   # Установить свойство (**)
        ```
        """
        ...
    @LevelsCount.setter
    def LevelsCount(self, value: int): ...

    @property
    def PageLayoutType(self) -> int:  # ireportparam_pagelayouttype.html
        """
        ## PageLayoutType - Тиn компоновки таблиц отчета

        Тип данных: из перечисления ksPageLayoutTypeEnum

        ### Синтаксис Automation

        ```
        PageLayoutType = Object.PageLayoutType       # Получить свойство (* )
        Object.PageLayoutType = PageLayoutType       # Установить свойство (* )
        PageLayoutType = Object.GetPageLayoutType()  # Получить свойство (**)
        Object.SetPageLayoutType( PageLayoutType )   # Установить свойство (**)
        ```
        """
        ...
    @PageLayoutType.setter
    def PageLayoutType(self, value: int): ...

    @property
    def PagesColumnsCount(self) -> int:  # ireportparam_pagescolumnscount.html
        """
        ## PagesColumnsCount - Число колонок таблиц при установке таблиц в 2D документ

        Тип данных: long

        ### Синтаксис Automation

        ```
        PagesColumnsCount = Object.PagesColumnsCount       # Получить свойство (* )
        Object.PagesColumnsCount = PagesColumnsCount       # Установить свойство (* )
        PagesColumnsCount = Object.GetPagesColumnsCount()  # Получить свойство (**)
        Object.SetPagesColumnsCount( PagesColumnsCount )   # Установить свойство (**)
        ```
        """
        ...
    @PagesColumnsCount.setter
    def PagesColumnsCount(self, value: int): ...

    @property
    def PagesRowsCount(self) -> int:  # ireportparam_pagesrowscount.html
        """
        ## PagesRowsCount - Число рядов таблиц при установке таблиц в 2D документ

        Тип данных: long

        ### Синтаксис Automation

        ```
        PagesRowsCount = Object.PagesRowsCount       # Получить свойство (* )
        Object.PagesRowsCount = PagesRowsCount       # Установить свойство (* )
        PagesRowsCount = Object.GetPagesRowsCount()  # Получить свойство (**)
        Object.SetPagesRowsCount( PagesRowsCount )   # Установить свойство (**)
        ```
        """
        ...
    @PagesRowsCount.setter
    def PagesRowsCount(self, value: int): ...

    @property
    def PagesSpace(self) -> float:  # ireportparam_pagesspace.html
        """
        ## PagesSpace - Зазор между таблицами, мм

        Тип данных: double

        ### Синтаксис Automation

        ```
        PagesSpace = Object.PagesSpace       # Получить свойство (* )
        Object.PagesSpace = PagesSpace       # Установить свойство (* )
        PagesSpace = Object.GetPagesSpace()  # Получить свойство (**)
        Object.SetPagesSpace( PagesSpace )   # Установить свойство (**)
        ```
        """
        ...
    @PagesSpace.setter
    def PagesSpace(self, value: float): ...

    @property
    def UseHyperText(self) -> bool:  # ireportparam_usehypertext.html
        """
        ## UseHyperText - Тип формирования текстов (true - ссылками, false - обычный текст)

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        UseHyperText = Object.UseHyperText       # Получить свойство (* )
        Object.UseHyperText = UseHyperText       # Установить свойство (* )
        UseHyperText = Object.GetUseHyperText()  # Получить свойство (**)
        Object.SetUseHyperText( UseHyperText )   # Установить свойство (**)
        ```
        """
        ...
    @UseHyperText.setter
    def UseHyperText(self, value: bool): ...




class IVariable7(IKompasAPIObject):  # ivariable7.html
    """
    ## Интерфейс IVariable7

    Интерфейс параметрической переменной модели.

    Интерфейс позволяет изменять параметры объектов модели, не прибегая к их прямому редактированию. Выражения дают возможность устанавливать зависимости между параметрами объектов.

    Интерфейс можно получить с помощью свойств:

    •IInsertionFragment::Variable,

    •IInsertionFragment::Variables,

    •IKompasDocument2D1::Variable,

    •IKompasDocument2D1::Variables,

    •IView::Variable,

    •IView::Variables

    и метода:

    •IKompasDocument2D1::AddVariable.
    """

    @property
    def DeviationOn(self) -> bool:  # ivariable7_deviationon.html
        """
        ## DeviationOn - Включить отклонения

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        DeviationOn = Object.DeviationOn       # Получить свойство (* )
        Object.DeviationOn = DeviationOn       # Установить свойство (* )
        DeviationOn = Object.GetDeviationOn()  # Получить свойство (**)
        Object.SetDeviationOn( DeviationOn )   # Установить свойство (**)
        ```
        """
        ...
    @DeviationOn.setter
    def DeviationOn(self, value: bool): ...

    @property
    def DeviationType(self) -> int:  # ivariable7_deviationtype.html
        """
        ## DeviationType - Тип отклонений

        Тип данных: из перечисления ksDimensionDeviationEnum

        ### Синтаксис Automation

        ```
        DeviationType = Object.DeviationType       # Получить свойство (* )
        Object.DeviationType = DeviationType       # Установить свойство (* )
        DeviationType = Object.GetDeviationType()  # Получить свойство (**)
        Object.SetDeviationType( DeviationType )   # Установить свойство (**)
        ```
        """
        ...
    @DeviationType.setter
    def DeviationType(self, value: int): ...

    @property
    def DisplayName(self) -> str:  # ivariable7_displayname.html
        """
        ## DisplayName - Отображаемое имя переменной

        Тип данных: BSTR

        Свойство доступно только для чтения.

        Отображаемое имя переменной может отличаться от имени переменной IVariable7::Name для внешних переменных вставки детали в сборку.

        К имени переменной из источника добавляется префикс, равный имени переменной «исключить из расчета».

        ### Синтаксис Automation

        ```
        DisplayName = Object.DisplayName       # Получить свойство (* )
        DisplayName = Object.GetDisplayName()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def Expression(self) -> str:  # ivariable7_expression.html
        """
        ## Expression - Выражение

        Тип данных: BSTR

        ### Синтаксис Automation

        ```
        Expression = Object.Expression       # Получить свойство (* )
        Object.Expression = Information      # Установить свойство (* )
        Expression = Object.GetExpression()  # Получить свойство (**)
        Object.SetExpression( Expression )   # Установить свойство (**)
        ```
        """
        ...
    @Expression.setter
    def Expression(self, value: str): ...

    @property
    def External(self) -> bool:  # ivariable7_external.html
        """
        ## External - Внешняя переменная

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        External = Object.External       # Получить свойство (* )
        Object.External = External       # Установить свойство (* )
        External = Object.GetExternal()  # Получить свойство (**)
        Object.SetExternal( External )   # Установить свойство (**)
        ```
        """
        ...
    @External.setter
    def External(self, value: bool): ...

    @property
    def HasTolerance(self) -> bool:  # ivariable7_hastolerance.html
        """
        ## HasTolerance - Отключить допуск

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        HasTolerance = Object.HasTolerance       # Получить свойство (* )
        Object.HasTolerance = HasTolerance       # Установить свойство (* )
        HasTolerance = Object.GetHasTolerance()  # Получить свойство (**)
        Object.SetHasTolerance( HasTolerance )   # Установить свойство (**)
        ```
        """
        ...
    @HasTolerance.setter
    def HasTolerance(self, value: bool): ...

    @property
    def HighDeviation(self) -> float:  # ivariable7_highdeviation.html
        """
        ## HighDeviation - Верхнее отклонение

        Тип данных: double

        ### Синтаксис Automation

        ```
        HighDeviation = Object.HighDeviation       # Получить свойство (* )
        Object.HighDeviation = HighDeviation       # Установить свойство (* )
        HighDeviation = Object.GetHighDeviation()  # Получить свойство (**)
        Object.SetHighDeviation( HighDeviation )   # Установить свойство (**)
        ```
        """
        ...
    @HighDeviation.setter
    def HighDeviation(self, value: float): ...

    @property
    def Information(self) -> bool:  # ivariable7_information.html
        """
        ## Information - Информационная переменная

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        Information = Object.Information       # Получить свойство (* )
        Object.Information = Information       # Установить свойство (* )
        Information = Object.GetInformation()  # Получить свойство (**)
        Object.SetInformation( Information )   # Установить свойство (**)
        ```
        """
        ...
    @Information.setter
    def Information(self, value: bool): ...

    @property
    def IsCreatedDeviationVariables(self) -> bool:  # ivariable7_iscreateddeviationvariables.html
        """
        ## IsCreatedDeviationVariables - Созданы ли переменные для отклонений

        Тип данных: BOOL

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        IsCreatedDeviationVariables = Object.IsCreatedDeviationVariables       # Получить свойство (* )
        IsCreatedDeviationVariables = Object.GetIsCreatedDeviationVariables()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def LinkDocumentName(self) -> str:  # ivariable7_linkdocumentname.html
        """
        ## LinkDocumentName - Документ, в котором хранится ссылочная переменная

        Тип данных: BSTR

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        LinkDocumentName = Object.LinkDocumentName       # Получить свойство (* )
        LinkDocumentName = Object.GetLinkDocumentName()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def LinkEmbodimentIndex(self) -> int:  # ivariable7_linkembodimentindex.html
        """
        ## LinkEmbodimentIndex - Индекс текущего исполнения

        Тип данных: long

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        LinkEmbodimentIndex = Object.LinkEmbodimentIndex       # Получить свойство (* )
        LinkEmbodimentIndex = Object.GetLinkEmbodimentIndex()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def LinkVariableName(self) -> str:  # ivariable7_linkvariablename.html
        """
        ## LinkVariableName - Имя переменной, на которую сделана ссылка

        Тип данных: BSTR

        Свойство доступно только для чтения.

        Переменная может ссылаться на переменную из другого документа.

        Ссылка формируется из имени документа IVariable7::LinkDocumentName и имени переменной.

        Ссылку можно создать с помощью функции IVariable7::SetLink.

        ### Синтаксис Automation

        ```
        LinkVariableName = Object.LinkVariableName       # Получить свойство (* )
        LinkVariableName = Object.GetLinkVariableName()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def LowDeviation(self) -> float:  # ivariable7_lowdeviation.html
        """
        ## LowDeviation - Нижнее отклонение

        Тип данных: double

        ### Синтаксис Automation

        ```
        LowDeviation = Object.LowDeviation       # Получить свойство (* )
        Object.LowDeviation = LowDeviation       # Установить свойство (* )
        LowDeviation = Object.GetLowDeviation()  # Получить свойство (**)
        Object.SetLowDeviation( LowDeviation )   # Установить свойство (**)
        ```
        """
        ...
    @LowDeviation.setter
    def LowDeviation(self, value: float): ...

    @property
    def Name(self) -> str:  # ivariable7_name.html
        """
        ## Name - Имя переменной

        Тип данных: BSTR

        Свойство доступно только для чтения

        ### Синтаксис Automation

        ```
        Name = Object.Name       # Получить свойство (* )
        Name = Object.GetName()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def Note(self) -> str:  # ivariable7_note.html
        """
        ## Note - Комментарий к переменной

        Тип данных: BSTR

        ### Синтаксис Automation

        ```
        Note = Object.Note       # Получить свойство (* )
        Object.Note = Note       # Установить свойство (* )
        Note = Object.GetNote()  # Получить свойство (**)
        Object.SetNote( Note )   # Установить свойство (**)
        ```
        """
        ...
    @Note.setter
    def Note(self, value: str): ...

    @property
    def ParameterNote(self) -> str:  # ivariable7_parameternote.html
        """
        ## ParameterNote - Имя параметра переменной

        Тип данных: BSTR

        ### Синтаксис Automation

        ```
        ParameterNote = Object.ParameterNote       # Получить свойство (* )
        Object.ParameterNote = ParameterNote       # Установить свойство (* )
        ParameterNote = Object.GetParameterNote()  # Получить свойство (**)
        Object.SetParameterNote( ParameterNote )   # Установить свойство (**)
        ```
        """
        ...

    @property
    def Property(self):
        ...

    @property
    def ReadOnly(self) -> str:  # ivariable7_readonly.html
        """
        ## ReadOnly - Только чтение

        Тип данных: BSTR

        Свойство задает доступность изменения выражения переменной.

        Свойство используется для переменных документа.

        ### Синтаксис Automation

        ```
        ReadOnly = Object.ReadOnly       # Получить свойство (* )
        Object.ReadOnly = ReadOnly       # Установить свойство (* )
        ReadOnly = Object.GetReadOnly()  # Получить свойство (**)
        Object.SetReadOnly( ReadOnly )   # Установить свойство (**)
        ```
        """
        ...
    @ReadOnly.setter
    def ReadOnly(self, value: str): ...

    @property
    def Rectangle(self) -> bool:  # ivariable7_rectangle.html
        """
        ## Rectangle - Текст в рамочке

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        Rectangle = Object.Rectangle       # Получить свойство (* )
        Object.Rectangle = Rectangle       # Установить свойство (* )
        Rectangle = Object.GetRectangle()  # Получить свойство (**)
        Object.SetRectangle( Rectangle )   # Установить свойство (**)
        ```
        """
        ...
    @Rectangle.setter
    def Rectangle(self, value: bool): ...

    @property
    def Tolerance(self) -> str:  # ivariable7_tolerance.html
        """
        ## Tolerance - Квалитет

        Тип данных: BSTR

        ### Синтаксис Automation

        ```
        Tolerance = Object.Tolerance       # Получить свойство (* )
        Object.Tolerance = Tolerance       # Установить свойство (* )
        Tolerance = Object.GetTolerance()  # Получить свойство (**)
        Object.SetTolerance( Tolerance )   # Установить свойство (**)
        ```
        """
        ...
    @Tolerance.setter
    def Tolerance(self, value: str): ...

    @property
    def ToleranceOn(self) -> bool:  # ivariable7_toleranceon.html
        """
        ## ToleranceOn - Включить квалитет

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        ToleranceOn = Object.ToleranceOn       # Получить свойство (* )
        Object.ToleranceOn = ToleranceOn       # Установить свойство (* )
        ToleranceOn = Object.GetToleranceOn()  # Получить свойство (**)
        Object.SetToleranceOn( ToleranceOn )   # Установить свойство (**)
        ```
        """
        ...
    @ToleranceOn.setter
    def ToleranceOn(self, value: bool): ...

    @property
    def Value(self) -> float:  # ivariable7_value.html
        """
        ## Value - Значение переменной

        Тип данных: double

        ### Синтаксис Automation

        ```
        Value = Object.Value       # Получить свойство (* )
        Object.Value = Value       # Установить свойство (* )
        Value = Object.GetValue()  # Получить свойство (**)
        Object.SetValue( Value )   # Установить свойство (**)
        ```
        """
        ...
    @Value.setter
    def Value(self, value: float): ...

    @property
    def VariableID(self) -> int:  # ivariable7_variableid.html
        """
        ## VariableID - Идентификатор параметра в объектах

        Тип данных: long

        Свойство доступно только для чтения

        Версия Компас v21

        ### Синтаксис Automation

        ```
        VariableID = Object.VariableID       # Получить свойство (* )
        VariableID = Object.GetVariableID()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def VariableType(self) -> int:  # variabletype.html
        """
        ## VariableType - Тип переменной

        Тип данных: из перечисления ksVariableTypeEnum

        Свойство доступно только для чтения.

        Версия КОМПАС v22

        ### Синтаксис Automation

        ```
        VariableType = Object.VariableType       # Получить свойство (* )
        VariableType = Object.GetVariableType()  # Получить свойство (**)
        ```
        """
        ...

    def AddProperty(self) -> IProperty:  # ivariable7_addproperty.html
        """
        ## AddProperty - Создать свойство переменная

        ### Синтаксис Automation

        ```
        LPDISPATCH AddProperty();
        ```

        ### Возвращаемое значение

        - указатель на интерфейс свойства IProperty.
        """
        ...

    def CreateDeviationVariables(self) -> bool:  # ivariable7_createdeviationvariables.html
        """
        ## CreateDeviationVariables - Создать переменные для отклонений

        ### Синтаксис Automation

        ```
        BOOL CreateDeviationVariables();
        ```

        ### Возвращаемое значение

        +-----------------------------+
        | TRUE  | - в случае удачи,   |
        +-----------------------------+
        | FALSE | - в случае неудачи. |
        +-----------------------------+
        """
        ...

    def Delete(self) -> bool:  # ivariable7_delete.html
        """
        ## Delete - Удалить переменную

        ### Синтаксис Automation

        ```
        BOOL Delete();
        ```

        ### Возвращаемое значение

        +--------------------------+
        | TRUE | - в случае удачи. |
        +--------------------------+
        """
        ...

    def DeviationExpression(self, HighDeviation) -> str:  # ivariable7_deviationexpression.html
        """
        ## DeviationExpression - Выражение для переменной отклонения

        Тип данных: BSTR

        ### Синтаксис Automation

        ```
        DeviationExpression = Object.DeviationExpression( HighDeviation )     # Получить свойство (* )
        Object.DeviationExpression( HighDeviation ) = DeviationExpression     # Установить свойство (* )
        DeviationExpression = Object.GetDeviationExpression( HighDeviation )  # Получить свойство (**)
        Object.SetDeviationExpression( HighDeviation, DeviationExpression )   # Установить свойство (**)
        ```

        ### Входные параметры

        +------------------------------------------------------------------------+
        | BOOL | - HighDeviation - верхнее (TRUE) или нижнее (FALSE) отклонение. |
        +------------------------------------------------------------------------+
        """
        ...

    def GetLinkEmbodimentMarking(self, MarkingType, AddSystemDelimer) -> bool:  # ivariable7_getlinkembodimentmarking.html
        """
        ## GetLinkEmbodimentMarking - Обозначение исполнения

        ### Синтаксис Automation

        ```
        BSTR GetLinkEmbodimentMarking( ksVariantMarkingTypeEnum MarkingType, BOOL AddSystemDelimer );
        ```

        ### Входные параметры

        +----------------------------------------------------------------------------------+
        | MarkingType      | - часть обозначения из перечисления ksVariantMarkingTypeEnum, |
        +----------------------------------------------------------------------------------+
        | AddSystemDelimer | - добавлять системные разделители.                            |
        +----------------------------------------------------------------------------------+

        ### Возвращаемое значение

        +-----------------------------+
        | TRUE  | - в случае удачи,   |
        +-----------------------------+
        | FALSE | - в случае неудачи. |
        +-----------------------------+
        """
        ...

    def SetDeviationExpression(self, HighDeviation, arg1):
        ...

    def SetLink(self, DocumentName, VariableName) -> bool:  # ivariable7_setlink.html
        """
        ## SetLink - Установить ссылку на переменную

        ### Синтаксис Automation

        ```
        BOOL SetLink( BSTR DocumentName, BSTR VariableName )
        ```

        ### Входные параметры

        +----------------------------------+
        | DocumentName | - имя документа,  |
        +----------------------------------+
        | VariableName | - имя переменной. |
        +----------------------------------+

        ### Возвращаемое значение

        +--------------------------+
        | TRUE | - в случае удачи. |
        +--------------------------+
        """
        ...

    def SetLinkEmbodiment(self, DocumentName, VariableName, Index):  # ivariable7_setlinkembodiment.html
        """
        ## SetLinkEmbodiment - Установить ссылку на переменную

        ### Синтаксис Automation

        ```
        BOOL SetLinkEmbodiment( BSTR DocumentName, BSTR VariableName, VARIANT Index );
        ```

        ### Входные параметры

        +---------------------------------------------------------+
        | DocumentName | - имя документа,                         |
        +---------------------------------------------------------+
        | VariableName | - имя переменной,                        |
        +---------------------------------------------------------+
        | Index        | - индекс или строковый номер исполнения. |
        +---------------------------------------------------------+
        """
        ...




class IProcessWithVariables(IDispatch):  # iprocesswithvariables.html
    """
    ## Интерфейс IProcessWithVariables

    Интерфейс процесса с переменными.

    Интерфейс является дополнительным к интерфейсу IProcess3D.

    Интерфейс можно получить с помощью метода IUnknown::QueryInterface.

    Версия: Компас v20
    """

    def ClearExpressions(self) -> bool:  # iprocesswithvariables_clearexpressions.html
        """
        ## ClearExpressions - Очистить список выражений и переменных процесса

        ### Синтаксис Automation

        ```
        BOOL ClearExpressions();
        ```

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def GetControlExpression(self, Control):  # iprocesswithvariables_getcontrolexpression.html
        """
        ## GetControlExpression - Получить выражение из контрола

        ### Синтаксис Automation

        ```
        BSTR GetControlExpression( IPropertyControl * Control );
        ```

        ### Входные параметры

        +------------------------------------------------------+
        | Control | - Указатель на интерфейс IPropertyControl, |
        +------------------------------------------------------+

        ### Возвращаемое значение

        - Выражение.
        """
        ...

    def GetControlVariableName(self, Control):  # iprocesswithvariables_getcontrolvariablename.html
        """
        ## GetControlVariableName - Получить имя переменной из контрола

        ### Синтаксис Automation

        ```
        BSTR GetControlVariableName( IPropertyControl * Control );
        ```

        ### Входные параметры

        +------------------------------------------------------+
        | Control | - Указатель на интерфейс IPropertyControl, |
        +------------------------------------------------------+

        ### Возвращаемое значение

        - Имя переменной.
        """
        ...

    def SetControlExpression(self, Control, VariableName, Expression) -> bool:  # iprocesswithvariables_setcontrolexpression.html
        """
        ## SetControlExpression - Установить выражение в контрол

        ### Синтаксис Automation

        ```
        BOOL SetControlExpression( IPropertyControl * Control, BSTR VariableName, BSTR Expression );
        ```

        ### Входные параметры

        +-----------------------------------------------------------+
        | Control      | - Указатель на интерфейс IPropertyControl, |
        +-----------------------------------------------------------+
        | VariableName | - Имя переменной,                          |
        +-----------------------------------------------------------+
        | Expression   | - Выражение.                               |
        +-----------------------------------------------------------+

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def UpdateExpressionsControls(self) -> bool:  # iprocesswithvariables_updateexpressionscontrols.html
        """
        ## UpdateExpressionsControls - Обновить выражения в контролах

        ### Синтаксис Automation

        ```
        BOOL UpdateExpressionsControls();
        ```

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...




class IVariableTable(IKompasAPIObject):  # ivariabletable.html
    """
    ## Интерфейс IVariableTable

    Интерфейс таблицы переменных.

    Позволяет считывать, изменять, добавлять параметры таблицы переменных компонента, применять параметры из таблицы переменных к компоненту.

    1. Данный интерфейс можно получить:

    •от интерфейса IPart7, используя свойство IPart7::VariableTable,

    •от интерфейса IKompasDocument2D1, используя свойство IKompasDocument2D1::VariableTable.

    2. Доступно редактирование таблицы переменных только верхнего компонента.
    """

    @property
    def ColumnsCount(self) -> int:  # ivariabletable_columnscount.html
        """
        ## ColumnsCount - Количество столбцов в таблице переменных

        Тип данных: long

        Значение свойства:

        +-----------------------------------------+
        | Количество столбцов | в случае успеха,  |
        +-----------------------------------------+
        | -1                  | в случае неудачи. |
        +-----------------------------------------+

        Свойство только для чтения.

        ### Синтаксис Automation

        ```
        ColumnsCount = iObject.ColumnsCount       # Получить свойство (* )
        ColumnsCount = iObject.GetColumnsCount()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def RowsCount(self) -> int:  # ivariabletable_rowscount.html
        """
        ## RowsCount - Количество строк в таблице переменных

        Тип данных: long

        Значение свойства:

        +--------------------------------------+
        | Количество строк | в случае успеха,  |
        +--------------------------------------+
        | -1               | в случае неудачи. |
        +--------------------------------------+

        Свойство только для чтения.

        ### Синтаксис Automation

        ```
        RowsCount = iObject.RowsCount       # Получить свойство (* )
        RowsCount = iObject.GetRowsCount()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def VarNames(self) -> typing.Any:  # ivariabletable_varnames.html
        """
        ## VarNames - Массив имен переменных в таблице переменных

        Тип данных: VARIANT

        1. Массив имён переменных передаётся через массив SAFEARRAY значений типа BSTR.

        2. Установка свойства доступна только для верхнего компонента.

        ### Синтаксис Automation

        ```
        VarNames = iObject.VarNames       # Получить свойство (* )
        iObject.VarNames = VarNames       # Установить свойство (* )
        VarNames = iObject.GetVarNames()  # Получить свойство (**)
        iObject.SetVarNames (VarNames)    # Установить свойство (**)
        ```
        """
        ...
    @VarNames.setter
    def VarNames(self, value: typing.Any): ...

    def AddColumn(self, Val):  # ivariabletable_addcolumn.html
        """
        ## AddColumn - Добавить колонку в таблицу переменных

        1. Метод доступен только для верхнего компонента.

        2. Если компонент не имел таблицы переменных, она создается.

        ### Синтаксис Automation

        ```
        long AddColumn (LPCTSTR VarName);
        ```

        ### Входные параметры

        +-----------------------------------------+
        | VarName | - имя добавляемой переменной. |
        +-----------------------------------------+

        ### Возвращаемое значение

        +-------------------------------------------------------------------------------------+
        | ColumnIndex - индекс добавленной колонки в таблице переменных | - в случае успеха,  |
        +-------------------------------------------------------------------------------------+
        | -1                                                            | - в случае неудачи. |
        +-------------------------------------------------------------------------------------+
        """
        ...

    def AddRow(self, Val):  # ivariabletable_addrow.html
        """
        ## AddRow - Добавить строку в таблицу переменных

        1. Метод доступен только для верхнего компонента.

        2. Если компонент не имел таблицы переменных, она создается.

        ### Синтаксис Automation

        ```
        long AddRow (LPCTSTR Comment);
        ```

        ### Входные параметры

        +-----------------------------------------------+
        | Comment | - комментарий к добавляемой строке. |
        +-----------------------------------------------+

        ### Возвращаемое значение

        +------------------------------------------------------------------------------+
        | index - индекс добавленной строки в таблице переменных | - в случае успеха,  |
        +------------------------------------------------------------------------------+
        | -1                                                     | - в случае неудачи. |
        +------------------------------------------------------------------------------+
        """
        ...

    def ApplyVars(self, Index) -> bool:  # ivariabletable_applyvars.html
        """
        ## ApplyVars - Применить параметры строки к компоненту

        Метод доступен только для верхнего компонента.

        ### Синтаксис Automation

        ```
        BOOL ApplyVars (VARIANT index);
        ```

        ### Входные параметры

        +-----------------------------------------------------------------------+
        | index | - индекс строки типа long или комментарий к строке типа BSTR. |
        +-----------------------------------------------------------------------+

        ### Возвращаемое значение

        +----------------------------------------------------------------------------------+
        | TRUE  | - значения параметров из заданной строки успешно применены к компоненту, |
        +----------------------------------------------------------------------------------+
        | FALSE | - значения параметров из заданной строки не применены к компоненту.      |
        +----------------------------------------------------------------------------------+
        """
        ...

    def Cell(self, RowIndexVal, ColIndex) -> float:  # ivariabletable_cell.html
        """
        ## Cell - Значение в ячейке таблицы переменных

        Тип данных: double.

        Установка свойства доступна только для верхнего компонента.

        ### Синтаксис Automation

        ```
        Cell = iObject.Cell (rIndex,cIndex)     # Получить свойство (* )
        iObject.Cell (rIndex,cIndex) = Cell     # Установить свойство (* )
        Cell = iObject.GetCell (rIndex,cIndex)  # Получить свойство (**)
        iObject.SetCell (rIndex,cIndex,Cell)    # Установить свойство (**)
        ```

        ### Входные параметры

        +----------------------------------------------------+
        | long rIndex | - индекс строки таблицы переменных,  |
        +----------------------------------------------------+
        | long cIndex | - индекс колонки таблицы переменных. |
        +----------------------------------------------------+
        """
        ...

    def Clear(self) -> bool:  # ivariabletable_clear.html
        """
        ## Clear - Очистить всю таблицу - удалить все строки и колонки

        Метод доступен только для верхнего компонента.

        ### Синтаксис Automation

        ```
        BOOL Clear();
        ```

        ### Возвращаемое значение

        +-----------------------------+
        | TRUE  | - в случае успеха,  |
        +-----------------------------+
        | FALSE | - в случае неудачи. |
        +-----------------------------+
        """
        ...

    def Comment(self, Val) -> str:  # ivariabletable_comment.html
        """
        ## Comment - Комментарий к строке в таблице переменных

        Тип данных: BSTR

        Установка свойства доступна только для верхнего компонента.

        ### Синтаксис Automation

        ```
        Comment = iObject.Comment       # Получить свойство (* )
        iObject.Comment = Comment       # Установить свойство (* )
        Comment = iObject.GetComment()  # Получить свойство (**)
        iObject.SetComment (Comment)    # Установить свойство (**)
        ```
        """
        ...

    def DeleteColumn(self, ColVal) -> bool:  # ivariabletable_deletecolumn.html
        """
        ## DeleteColumn - Удалить колонку из таблицы переменных

        Метод доступен только для верхнего компонента.

        ### Синтаксис Automation

        ```
        BOOL DeleteColumn (VARIANT Column);
        ```

        ### Входные параметры

        +-------------------------------------------------------------------+
        | Column | - индекс колонки типа long или имя переменной типа BSTR. |
        +-------------------------------------------------------------------+

        ### Возвращаемое значение

        +-----------------------------+
        | TRUE  | - в случае успеха,  |
        +-----------------------------+
        | FALSE | - в случае неудачи. |
        +-----------------------------+
        """
        ...

    def DeleteRow(self, RowVal) -> bool:  # ivariabletable_deleterow.html
        """
        ## DeleteRow - Удалить строку из таблицы переменных

        Метод доступен только для верхнего компонента.

        ### Синтаксис Automation

        ```
        BOOL DeleteRow (VARIANT Row);
        ```

        ### Входные параметры

        +------------------------------------------------------------+
        | Row | - индекс строки типа long или комментарий типа BSTR. |
        +------------------------------------------------------------+

        ### Возвращаемое значение

        +-----------------------------+
        | TRUE  | - в случае успеха,  |
        +-----------------------------+
        | FALSE | - в случае неудачи. |
        +-----------------------------+
        """
        ...

    def FindColumnIndex(self, Val):  # ivariabletable_findcolumnindex.html
        """
        ## FindColumnIndex - Найти индекс колонки по имени переменной

        ### Синтаксис Automation

        ```
        long FindColumnIndex (LPCTSTR val);
        ```

        ### Входные параметры

        +-------------------------+
        | val | - имя переменной. |
        +-------------------------+

        ### Возвращаемое значение

        +--------------------------------------+
        | Индекс колонки | - в случае успеха,  |
        +--------------------------------------+
        | -1             | - в случае неудачи. |
        +--------------------------------------+
        """
        ...

    def SetCell(self, RowIndexVal, ColIndex, arg2):
        ...

    def SetComment(self, Val, arg1):
        ...

    def SetTableRow(self, Index, arg1):
        ...

    def SetVarName(self, Column, arg1):
        ...

    def TableRow(self, Index) -> typing.Any:  # ivariabletable_tablerow.html
        """
        ## TableRow - Строка переменных в таблице переменных

        Тип данных: VARIANT

        Массив значений переменных передаётся через массив SAFEARRAY значений типа double.

        ### Синтаксис Automation

        ```
        Row = iObject.TableRow       # Получить свойство (* )
        iObject.TableRow = Row       # Установить свойство (* )
        Row = iObject.GetRableRow()  # Получить свойство (**)
        iObject.SetTableRow (Row)    # Установить свойство (**)
        ```
        """
        ...

    def VarName(self, Column) -> str:  # ivariabletable_varname.html
        """
        ## VarName - Имя переменной в колонке таблицы переменных

        Тип данных: BSTR (строка)

        Установка свойства доступна только для верхнего компонента.

        ### Синтаксис Automation

        ```
        Name = iObject.VarName (column)     # Получить свойство (* )
        iObject.VarName (column) = Name     # Установить свойство (* )
        Name = iObject.GetVarName (column)  # Получить свойство (**)
        iObject.SetVarName (column, Name)   # Установить свойство (**)
        ```

        ### Входные параметры

        +----------------------------------------------------+
        | long column | - индекс колонки таблицы переменных. |
        +----------------------------------------------------+
        """
        ...

    def VisualTable(self, WinVal, Select) -> int:  # ivariabletable_visualtable.html
        """
        ## VisualTable - Выдать таблицу в визуальном режиме (получить индекс выбранной строки)

        Тип данных: long

        1. Свойство доступно только для чтения.

        2. Если задан признак выбора Select = TRUE, по нажатию ОК в диалоге таблицы переменных возвращается индекс выбранной строки.

        3. Если задан признак выбора Select = FALSE, доступно редактирование и по нажатию ОК в диалоге таблицы переменных возвращается -1.

        4. Если задан признак выбора Select = FALSE, доступно редактирование и по нажатию Присвоить значения параметрам в диалоге таблицы переменных возвращается индекс выбранной строки.

        5. Редактирование таблицы переменных доступно только для верхнего компонента.

        ### Синтаксис Automation

        ```
        rIndex = iObject.VisualTable(WinVal,Select)     # Получить свойство (* )
        rIndex = iObject.GetVisualTable(WinVal,Select)  # Получить свойство (**)
        ```

        ### Входные параметры

        +-------------------------------------------------------------------------------------------+
        | OLE_HANDLE WinVal | - дескриптор окна (может быть задан ноль - признак главного окна),    |
        +-------------------------------------------------------------------------------------------+
        | BOOL Select       | - признак открытия окна только для чтения и выбора строки из таблицы. |
        +-------------------------------------------------------------------------------------------+

        ### Возвращаемое значение

        +-----------------------------------------------+
        | индекс выбранной строки | - в случае успеха,  |
        +-----------------------------------------------+
        | -1                      | - в случае неудачи. |
        +-----------------------------------------------+
        """
        ...




class IIntervalVariable(IDispatch):  # iintervalvariable.html
    """
    ## Интерфейс IIntervalVariable

    Интерфейс интервальной переменной.
    """

    @property
    def FirstValue(self) -> float:  # iintervalvariable_firstvalue.html
        """
        ## FirstValue - Первое значение интервала

        Тип данных: double

        ### Синтаксис Automation

        ```
        FirstValue = Object.FirstValue       # Получить свойство (* )
        Object.FirstValue = FirstValue       # Установить свойство (* )
        FirstValue = Object.GetFirstValue()  # Получить свойство (**)
        Object.SetFirstValue( FirstValue )   # Установить свойство (**)
        ```
        """
        ...
    @FirstValue.setter
    def FirstValue(self, value: float): ...

    @property
    def SecondValue(self) -> float:  # iintervalvariable_secondvalue.html
        """
        ## SecondValue - Второе значение интервала

        Тип данных: double

        ### Синтаксис Automation

        ```
        SecondValue = Object.SecondValue       # Получить свойство (* )
        Object.SecondValue = SecondValue       # Установить свойство (* )
        SecondValue = Object.GetSecondValue()  # Получить свойство (**)
        Object.SetSecondValue( SecondValue )   # Установить свойство (**)
        ```
        """
        ...
    @SecondValue.setter
    def SecondValue(self, value: float): ...




class IProperty(IKompasAPIObject):  # iproperty.html
    """
    ## Интерфейс IProperty

    Интерфейс «свойства».

    Позволяет задавать параметры свойства. Номера системных свойств...

    1. Интерфейс можно получить у менеджера свойств IPropertyMng.

    2. После задания параметров требуется вызвать метод IProperty::Update.
    """

    @property
    def Comment(self) -> str:  # iproperty_comment.html
        """
        ## Comment - Комментарий

        Тип данных: BSTR

        ### Синтаксис Automation

        ```
        Comment = Object.Comment       # Получить свойство (* )
        Object.Comment = Comment       # Установить свойство (* )
        Comment = Object.GetComment()  # Получить свойство (**)
        Object.SetComment( Comment )   # Установить свойство (**)
        ```
        """
        ...
    @Comment.setter
    def Comment(self, value: str): ...

    @property
    def DataType(self):
        ...
    @DataType.setter
    def DataType(self, value): ...

    @property
    def Id(self):
        ...

    @property
    def IsAngular(self) -> bool:  # iproperty_isangular.html
        """
        ## IsAngular - Свойство задано в градусах

        Тип данных: BOOL

        Свойство только для чтения.

        ### Синтаксис Automation

        ```
        IsAngular = Object.IsAngular       # Получить свойство (* )
        IsAngular = Object.GetIsAngular()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def IsVariable(self) -> bool:  # iproperty_isvariable.html
        """
        ## IsVariable - Свойство связано с переменной

        Тип данных: BOOL

        Свойство только для чтения.

        ### Синтаксис Automation

        ```
        IsVariable = Object.IsVariable       # Получить свойство (* )
        IsVariable = Object.GetIsVariable()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def ListVal(self) -> typing.Any:  # iproperty_listval.html
        """
        ## ListVal - Список значений

        Тип данных: VARIANT

        Для свойства с типом данных ksPropertyDataTypeDouble значение должно иметь тип VT_ARRAY|VT_R8.

        Для свойства с типом данных ksPropertyDataTypeLong значение должно иметь тип VT_ARRAY|VT_I4.

        Для свойства с типом данных ksPropertyDataTypeString значение должно иметь тип VT_ARRAY|VT_BSTR.

        ### Синтаксис Automation

        ```
        ListVal = Object.ListVal       # Получить свойство (* )
        Object.ListVal = ListVal       # Установить свойство (* )
        ListVal = Object.GetListVal()  # Получить свойство (**)
        Object.SetListVal( ListVal )   # Установить свойство (**)
        ```
        """
        ...
    @ListVal.setter
    def ListVal(self, value: typing.Any): ...

    @property
    def MaxValue(self) -> typing.Any:  # iproperty_maxvalue.html
        """
        ## MaxValue - Максимальное значение

        Тип данных: VARIANT

        Для свойства с типом данных ksPropertyDataTypeDouble значение будет иметь тип VT_R8.

        Для свойства с типом данных ksPropertyDataTypeLong значение будет иметь тип VT_I4.

        Свойство только для чтения.

        ### Синтаксис Automation

        ```
        MaxValue = Object.MaxValue       # Получить свойство (* )
        MaxValue = Object.GetMaxValue()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def MinValue(self) -> typing.Any:  # iproperty_minvalue.html
        """
        ## MinValue - Минимальное значение

        Тип данных: VARIANT

        Для свойства с типом данных ksPropertyDataTypeDouble значение будет иметь тип VT_R8.

        Для свойства с типом данных ksPropertyDataTypeLong значение будет иметь тип VT_I4.

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        MinValue = Object.MinValue       # Получить свойство (* )
        MinValue = Object.GetMinValue()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def Name(self):
        ...
    @Name.setter
    def Name(self, value): ...

    @property
    def PropertyTypeId(self):
        ...
    @PropertyTypeId.setter
    def PropertyTypeId(self, value): ...

    @property
    def ReadOnly(self) -> bool:  # iproperty_readonly.html
        """
        ## ReadOnly - Доступность редактирования

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        ReadOnly = Object.ReadOnly       # Получить свойство (* )
        Object.ReadOnly = ReadOnly       # Установить свойство (* )
        ReadOnly = Object.GetReadOnly()  # Получить свойство (**)
        Object.SetReadOnly( ReadOnly )   # Установить свойство (**)
        ```
        """
        ...
    @ReadOnly.setter
    def ReadOnly(self, value: bool): ...

    @property
    def SignificantDigitsCount(self) -> int:  # iproperty_significantdigitscount.html
        """
        ## SignificantDigitsCount - Количество значащих цифр после запятой

        Тип данных:long

        Установить/получить свойство можно только для ksPropertyDataTypeDouble.

        ### Синтаксис Automation

        ```
        SignificantDigitsCount = Object.SignificantDigitsCount       # Получить свойство (* )
        Object.SignificantDigitsCount = SignificantDigitsCount       # Установить свойство (* )
        SignificantDigitsCount = Object.GetSignificantDigitsCount()  # Получить свойство (**)
        Object.SetSignificantDigitsCount( SignificantDigitsCount )   # Установить свойство (**)
        ```
        """
        ...
    @SignificantDigitsCount.setter
    def SignificantDigitsCount(self, value: int): ...

    @property
    def SourceName(self):
        ...

    @property
    def UnitId(self):
        ...
    @UnitId.setter
    def UnitId(self, value): ...

    @property
    def UseListVal(self) -> bool:  # iproperty_uselistval.html
        """
        ## UseListVal - Использовать список значений

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        UseListVal = Object.UseListVal       # Получить свойство (* )
        Object.UseListVal = UseListVal       # Установить свойство (* )
        UseListVal = Object.GetUseListVal()  # Получить свойство (**)
        Object.SetUseListVal( UseListVal )   # Установить свойство (**)
        ```
        """
        ...
    @UseListVal.setter
    def UseListVal(self, value: bool): ...

    def Delete(self):
        ...

    def SetValueRange(self, MinVal, MaxVal) -> bool:  # iproperty_setvaluerange.html
        """
        ## SetValueRange - Установить новые ограничения на значение

        Для свойства с типом данных:

        •ksPropertyDataTypeDouble значение должно иметь тип VT_R8,

        •ksPropertyDataTypeLong значение должно иметь тип VT_I4,

        •ksPropertyDataTypeString значение должно иметь тип VT_BSTR.

        ### Синтаксис Automation

        ```
        BOOL SetValueRange( VARIANT MinVal, VARIANT MaxVal );
        ```

        ### Входные параметры

        +-----------------------------------+
        | MinVal | - минимальное значение,  |
        +-----------------------------------+
        | MaxVal | - максимальное значение. |
        +-----------------------------------+

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def Update(self) -> bool:  # iproperty_update.html
        """
        ## Update - Обновить данные

        ### Синтаксис Automation

        ```
        BOOL Update();
        ```

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def ViewEdit(self, Parent, ReadOnly):
        ...




class IPropertyKeeper(IDispatch):  # ipropertykeeper.html
    """
    ## Интерфейс IPropertyKeeper

    Интерфейс получения/редактирования значения свойств

    Позволяет получать и редактировать значения свойств объектов.

    Дополнительный интерфейс. Данный интерфейс можно получить у IPart7, IBody7, IEmbodiment, IMacroObject, IInsertionObject или IKompasDocument2D посредством вызова метода IUnknown::QueryInterface (const GUID far& iid, void** pif).
    """

    @property
    def Properties(self) -> str:  # ipropertykeeper_properties.html
        """
        ## Properties - Метаданные объекта

        Тип данных: BSTR

        ### Синтаксис Automation

        ```
        Properties = Object.Properties       # Получить свойство (* )
        Object.Properties = Properties       # Установить свойство (* )
        Properties = Object.GetProperties()  # Получить свойство (**)
        Object.SetProperties( Properties )   # Установить свойство (**)
        ```
        """
        ...
    @Properties.setter
    def Properties(self, value: str): ...

    @property
    def UniqueMetaObjectKey(self) -> str:  # ipropertykeeper_uniquemetaobjectkey.html
        """
        ## UniqueMetaObjectKey - Уникальный идентификатор объекта

        Тип данных: BSTR

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        UniqueMetaObjectKey = Object.UniqueMetaObjectKey       # Получить свойство(* )
        UniqueMetaObjectKey = Object.GetUniqueMetaObjectKey()  # Получить свойство (**)
        ```
        """
        ...

    def GetComplexPropertyValue(self, Property):
        ...

    def GetPropertyAdditionalStorage(self, Property, Create):  # ipropertykeeper_getpropertyadditionalstorage.html
        """
        ## GetPropertyAdditionalStorage - Получить дополнительные данные, связанные со свойством

        При первом изменении значения свойства дополнительные данные очищаются автоматически.

        ### Синтаксис Automation

        ```
        LPDISPATCH GetPropertyAdditionalStorage( IProperty * Property,
        BOOL Create,
        BOOL * FromSource );
        ```

        ### Входные параметры

        +---------------------------------------------------------+
        | Property | - указатель на интерфейс свойства,           |
        +---------------------------------------------------------+
        | Create   | - создавать хранилище, если оно отсутствует. |
        +---------------------------------------------------------+

        ### Выходные параметры

        +------------------------------------------------------+
        | FromSource | - хранилище было получено из источника. |
        +------------------------------------------------------+

        ### Возвращаемое значение

        указатель на интерфейс пользовательского хранилища с дополнительными данными для свойства.
        """
        ...

    def GetPropertyValue(self, Property, BaseUnit):
        ...

    def GetUserDataStoragesManager(self, FromSource) -> IUserDataStoragesMng:  # ipropertykeeper_getuserdatastoragesmanager.html
        """
        ## GetUserDataStoragesManager - Менеджер для работы с пользовательским хранилищем данных

        ### Синтаксис Automation

        ```
        LPDISPATCH GetUserDataStoragesManager( BOOL FromSource );
        ```

        ### Входные параметры

        +-------------------------------------------------------------------------------------------------------------------------+
        | FromSource | - TRUE вернуть менеджер из файла источника объекта, - FALSE - вернуть менеджер из файла владельца объекта. |
        +-------------------------------------------------------------------------------------------------------------------------+

        ### Возвращаемое значение

        - Указатель на интерфейс менеджера пользовательских данных IUserDataStoragesMng,
        """
        ...

    def InsertHypertextReference(self, Property, Object, Type, Brackets, TextLineIndex, Precision, PropertyId):
        ...

    def IsComplexPropertyValue(self, Property):
        ...

    def SetComplexPropertyValue(self, Property, Value):
        ...

    def SetPropertyValue(self, Property, Value, BaseUnit):
        ...




class IPropertyMng(IDispatch):  # ipropertymng.html
    """
    ## Интерфейс IPropertyMng

    Интерфейс Менеджера свойств.

    Позволяет управлять свойствами.

    Дополнительный интерфейс. Данный интерфейс можно получить у IApplication посредством вызова метода IUnknown::QueryInterface (const GUID far& iid, void** pif).
    """

    def AddProperty(self, Libname, Val):
        ...

    def GetProperties(self, Libname) -> list[IDispatch]:  # ipropertymng_getproperties.html
        """
        ## GetProperties - Получить массив свойств

        Если возвращается один объект, то тип VARIANT-а будет VT_DISPATCH, если возвращается несколько объектов, то тип VARIANT-а будет VT_ARRAY | VT_DISPATCH.

        ### Синтаксис Automation

        ```
        VARIANT GetProperties ( VARIANT libname );
        ```

        ### Входные параметры

        +----------------------------------------------------------------------------------------------------------------------------------------+
        | Libname ( VARIANT) | - полный путь к библиотеке на диске VT_BSTR, либо указатель на документ VT_DISPATCH. Текущий документ - VT_EMPTY. |
        +----------------------------------------------------------------------------------------------------------------------------------------+

        ### Возвращаемое значение

        - Массив библиотек VARIANT типа VT_ARRAY | VT_DISPATCH.
        """
        ...

    def GetProperty(self, Libname, Index):
        ...

    def GetReport(self, Document, Type) -> IReport:  # ipropertymng_getreport.html
        """
        ## GetReport - Получить интерфейс таблицы отчета по имени файла или указателю на документ

        ### Синтаксис Automation

        ```
        LPDISPATCH GetReport( VARIANT Document, ksReportTypeEnum Type );
        ```

        ### Входные параметры

        +---------------------------------------------------+
        | Document | - имя файла или указатель на документ. |
        +---------------------------------------------------+

        ### Выходные параметры

        +----------------------+
        | Type | - тип отчета. |
        +----------------------+

        ### Возвращаемое значение

        - Указатель на интерфейс отчета IReport.
        """
        ...

    def PropertyCount(self, Libname) -> int:  # ipropertymng_propertycount.html
        """
        ## PropertyCount - Количество свойств

        Тип данных: long

        +---------------------------------------------------------------------------+
        | PropertyCount = Object.PropertyCount(Libname)    | Получить свойство (* ) |
        +---------------------------------------------------------------------------+
        | PropertyCount = Object.GetPropertyCount(Libname) | Получить свойство (**) |
        +---------------------------------------------------------------------------+

        Свойство только для чтения.

        ### Входные параметры

        +---------------------------------------------------------------------------------------------------------------------------------------+
        | Libname ( VARIANT) | - полный путь к библиотеке на диске VT_BSTR либо указатель на документ VT_DISPATCH. Текущий документ - VT_EMPTY. |
        +---------------------------------------------------------------------------------------------------------------------------------------+
        """
        ...

    def RemoveProperty(self, Libname, Val):
        ...




class IFormatLabelRules(IKompasAPIObject):  # iformatlabelrules.html
    """
    ## Интерфейс IFormatLabelRules

    Интерфейс Правила формирования отображаемых имен объектов в дереве документа.

    1. Интерфейс позволяет сформировать правила формирования отображаемых имен объектов в дереве документа.

    2. Интерфейс можно получить с помощью метода IDocumentSettings1::FormatLabelRules.

    Версия: Компас v20
    """

    @property
    def PropertiesCount(self) -> int:  # iformatlabelrules_propertiescount.html
        """
        ## PropertiesCount - Количество свойств в настройках

        Тип данных: long

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        PropertiesCount = Object.PropertiesCount       # Получить свойство(* )
        PropertiesCount = Object.GetPropertiesCount()  # Получить свойство(**)
        ```
        """
        ...

    def AddProperty(self, IndexAt, Property, Use, Prefix) -> bool:  # iformatlabelrules_addproperty.html
        """
        ## AddProperty - Добавить свойство в список правил

        ### Синтаксис Automation

        ```
        BOOL AddProperty( long IndexAt, IProperty * Property, BOOL Use, BSTR Prefix );
        ```

        ### Входные параметры

        +--------------------------------------------------------------------+
        | IndexAt    | - индекс добавляемой настройки,                       |
        +--------------------------------------------------------------------+
        | PropertyId | - указатель на интерфейс описания свойства IProperty, |
        +--------------------------------------------------------------------+
        | Use        | - признак использования,                              |
        +--------------------------------------------------------------------+
        | Prefix     | - префикс.                                            |
        +--------------------------------------------------------------------+

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def AddPropertyById(self, IndexAt, PropertyId, Use, Prefix) -> bool:  # iformatlabelrules_addpropertybyid.html
        """
        ## AddPropertyById - Добавить свойство по идентификатору в список правил

        ### Синтаксис Automation

        ```
        BOOL AddPropertyById( long IndexAt, double PropertyId, BOOL Use, BSTR Prefix );
        ```

        ### Входные параметры

        +--------------------------------------------------------------------+
        | IndexAt    | - индекс добавляемой настройки,                       |
        +--------------------------------------------------------------------+
        | PropertyId | - указатель на интерфейс описания свойства IProperty, |
        +--------------------------------------------------------------------+
        | Use        | - признак использования,                              |
        +--------------------------------------------------------------------+
        | Prefix     | - префикс.                                            |
        +--------------------------------------------------------------------+

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def AddPropertyFromLibrary(self, IndexAt, FileName, PropertyId, Use, Prefix) -> bool:  # iformatlabelrules_addpropertyfromlibrary.html
        """
        ## AddPropertyFromLibrary - Добавить свойство из библиотеки в список правил

        ### Синтаксис Automation

        ```
        BOOL AddPropertyFromLibrary( long IndexAt, BSTR FileName, double PropertyId, BOOL Use, BSTR Prefix );
        ```

        ### Входные параметры

        +--------------------------------------------------------------------+
        | IndexAt    | - индекс добавляемой настройки,                       |
        +--------------------------------------------------------------------+
        | FileName   | - имя файла библиотеки,                               |
        +--------------------------------------------------------------------+
        | PropertyId | - указатель на интерфейс описания свойства IProperty, |
        +--------------------------------------------------------------------+
        | Use        | - признак использования,                              |
        +--------------------------------------------------------------------+
        | Prefix     | - префикс.                                            |
        +--------------------------------------------------------------------+

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def Clear(self) -> bool:  # iformatlabelrules_clear.html
        """
        ## Clear - Очистить список

        ### Синтаксис Automation

        ```
        BOOL Clear();
        ```

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def DeleteProperty(self, Index) -> bool:  # iformatlabelrules_deleteproperty.html
        """
        ## DeleteProperty - Удалить настройку для свойства по индексу

        ### Синтаксис Automation

        ```
        BOOL DeleteProperty( long Index );
        ```

        ### Входные параметры

        +-----------------------------+
        | Index | - индекс настройки. |
        +-----------------------------+

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def Property(self, Index):
        ...

    def PropertyId(self, Index):
        ...

    def PropertyPrefix(self, Index):
        ...

    def PropertyUse(self, Index):
        ...

    def SetProperty(self, Index, arg1):
        ...

    def SetPropertyId(self, Index, arg1):
        ...

    def SetPropertyPrefix(self, Index, arg1):
        ...

    def SetPropertyUse(self, Index, arg1):
        ...

    def SwapProperties(self, Index1, Index2) -> bool:  # iformatlabelrules_swapproperties.html
        """
        ## SwapProperties - Поменять свойства местами

        ### Синтаксис Automation

        ```
        BOOL SwapProperties( long Index1, long Index2 );
        ```

        ### Входные параметры

        +-------------------------------------+
        | Index1 | - индекс первого свойства, |
        +-------------------------------------+
        | Index2 | - индекс второго свойства. |
        +-------------------------------------+

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...




class IUserDataStorage(IKompasAPIObject):  # iuserdatastorage.html
    """
    ## Интерфейс IUserDataStorage

    Интерфейс пользовательского хранилища.

    Хранилище представляет собой именованную «закладку» в коллекции. Для хранилища могут задаваться версии. Хранилище может быть защищено от изменений паролем. Парольная защита не препятствует просмотру содержания хранилища. Данные в хранилище представлены в виде таблицы, состоящей из двух столбцов – данных различных типов и комментария.

    Интерфейс можно получить у коллекции хранилищ, используя свойство IUserDataStorages::Item.
    """

    def AddObject(self, Pass, Object, Comment):  # iuserdatastorage_addobject.html
        """
        ## AddObject - Добавить объект и комментарий в пользовательское хранилище

        1. Объект может быть variant'ом с типом VT_INT, VT_I2, VT_I4, VT_R4, VT_R8, VT_DATE, VT_UI1, VT_UINT, VT_BOOL, VT_BSTR, VT_DISPATCH, а также VT_ARRAY с этими типами данных. Индекс массива начинается с нуля.

        2. Если задан интерфейс VT_DISPATCH и массив объектов VT_ARRAY | VT_DISPATCH, то под объектом подразумевается указатель на IDispatch от объектов API. Они также будут сохранены вместе с документом и при следующем запросе найдены.

        3. Индексация массивов должна начинаться с нуля.

        4. Если хранилище защищено паролем от изменений, то метод будет выполнен только после указания правильного пароля.

        5. Под объектом API следует понимать объекты:

        для 3D хранилища - наследник от IModelObject,

        для 2D хранилища - наследник от IDrawingObject (кроме ILayer).

        ### Синтаксис Automation

        ```
        long AddObject( LPCTSTR pass,
        const VARIANT & object,
        LPCTSTR comment );
        ```

        ### Входные параметры

        +----------------------------------+
        | Pass    | - пароль,              |
        +----------------------------------+
        | Object  | - указатель на объект, |
        +----------------------------------+
        | comment | - комментарий.         |
        +----------------------------------+

        ### Возвращаемое значение

        - индекс объекта,
        """
        ...

    def AddObjects(self, Pass, Objects, Comments) -> bool:  # iuserdatastorage_addobjects.html
        """
        ## AddObjects - Добавить объекты пользовательского хранилища и комментарии к ним

        ### Синтаксис Automation

        ```
        BOOL AddObjects( BSTR Pass, VARIANT Objects, VARIANT Comments );
        ```

        ### Входные параметры

        +---------------------------------------------------------------------+
        | Pass     | - пароль,                                                |
        +---------------------------------------------------------------------+
        | Objects  | - список данных в виде массива VT_ARRAY | VT_VARIANT,    |
        +---------------------------------------------------------------------+
        | comments | - список комментариев в виде массива VT_ARRAY | VT_BSTR. |
        +---------------------------------------------------------------------+

        Версия Компас v21

        ### Возвращаемое значение

        +-----------------------------+
        | TRUE  | - в случае успеха,  |
        +-----------------------------+
        | FALSE | - в случае неудачи. |
        +-----------------------------+
        """
        ...

    def Clear(self, Pass) -> bool:  # iuserdatastorage_clear.html
        """
        ## Clear - Очистить хранилище, удалив все объекты

        Если хранилище защищено паролем от изменений, то метод будет выполнен только после указания правильного пароля.

        ### Синтаксис Automation

        ```
        BOOL Clear ( LPCTSTR Pass );
        ```

        ### Входные параметры

        +------------------+
        | Pass | - пароль. |
        +------------------+

        ### Возвращаемое значение

        +-----------------------------+
        | TRUE  | - в случае успеха,  |
        +-----------------------------+
        | FALSE | - в случае неудачи. |
        +-----------------------------+
        """
        ...

    def Delete(self, Pass, Index) -> bool:  # iuserdatastorage_delete.html
        """
        ## Delete - Удалить объект, заданный по индексу или по комментарию

        1. В качестве индекса можно задать число или комментарий к объекту.

        2. Если хранилище защищено паролем от изменений, то метод будет выполнен только после указания правильного пароля.

        ### Синтаксис Automation

        ```
        BOOL Delete( LPCTSTR pass,
        const VARIANT & index );
        ```

        ### Входные параметры

        +-------------------------------------------+
        | Pass  | - пароль,                         |
        +-------------------------------------------+
        | Index | - индекс объекта или комментарий. |
        +-------------------------------------------+

        ### Возвращаемое значение

        +-----------------------------+
        | TRUE  | - в случае успеха,  |
        +-----------------------------+
        | FALSE | - в случае неудачи. |
        +-----------------------------+
        """
        ...

    def GetObject(self, Index) -> str:  # iuserdatastorage_getobject.html
        """
        ## GetObject - Получить объект пользовательского хранилища и комментарий к нему по комментарию или индексу

        1. В качестве индекса можно задать число или комментарий к объекту.

        2. Объект может быть variant'ом с типом VT_INT, VT_I2, VT_I4, VT_R4, VT_R8, VT_DATE, VT_UI1, VT_UINT, VT_BOOL, VT_BSTR, VT_DISPATCH, а также VT_ARRAY с этими типами данных.

        3. Если задан интерфейс VT_DISPATCH и массив объектов VT_ARRAY | VT_DISPATCH, то под объектом подразумевается указатель на IDispatch от объектов API. Они также будут сохранены вместе с документом и при следующем запросе найдены.

        ### Синтаксис Automation

        ```
        BSTR GetObject( const VARIANT & index, VARIANT * object, long * numb );
        ```

        ### Входные параметры

        +-------------------------------------------+
        | Index | - индекс объекта или комментарий. |
        +-------------------------------------------+

        ### Выходные параметры

        +-------------------------------------------+
        | Object | - указатель на объект хранилища, |
        +-------------------------------------------+
        | numb   | - указатель на номер объекта.    |
        +-------------------------------------------+

        ### Возвращаемое значение

        +---------------------------------+
        | Comment (BSTR) | - комментарий. |
        +---------------------------------+
        """
        ...

    def Name(self, Pass):  # iuserdatastorage_name.html
        """
        ## Name - Имя хранилища

        Если хранилище защищено паролем, то изменить свойство можно, только указав правильный пароль.

        ### Синтаксис Automation

        ```
        Тип данных: LPSTR
        Name = StorageObj.Name(Pass)     # Получить свойство (* )
        StorageObj.Name(Pass) = Name     # Установить свойство (* )
        Name = StorageObj.GetName(Pass)  # Получить свойство (**)
        StorageObj.SetName (Name, Pass)  # Установить свойство (**)
        ```

        ### Входные параметры

        +------------------------------------+
        | Pass (LPSTR) | - пароль хранилища. |
        +------------------------------------+

        +-----------------------------------+
        | Pass (BSTR) | - пароль хранилища. |
        +-----------------------------------+
        """
        ...

    def SetName(self, Pass, arg1):
        ...

    def SetObject(self, Pass, Index, Object, Comment) -> bool:  # iuserdatastorage_setobject.html
        """
        ## SetObject - Установить объект пользовательского хранилища и комментарий к нему по комментарию или индексу

        1. В качестве индекса можно задать число или комментарий к объекту.

        2. Объект может быть variant'ом с типом VT_INT, VT_I2, VT_I4, VT_R4, VT_R8, VT_DATE, VT_UI1, VT_UINT, VT_BOOL, VT_BSTR, VT_DISPATCH, а также VT_ARRAY с этими типами данных.

        3. Если задан интерфейс VT_DISPATCH и массив объектов VT_ARRAY | VT_DISPATCH, то под объектом подразумевается указатель на IDispatch от объектов API. Они также будут сохранены вместе с документом и при следующем запросе найдены.

        4. Если хранилище защищено паролем от изменений, то метод будет выполнен только после указания правильного пароля.

        ### Синтаксис Automation

        ```
        long SetObject( LPCTSTR pass,
        const VARIANT index,
        const VARIANT & object,
        LPCTSTR comment );
        ```

        ### Входные параметры

        +---------------------------------------------+
        | Pass    | - пароль,                         |
        +---------------------------------------------+
        | Index   | - индекс объекта или комментарий, |
        +---------------------------------------------+
        | Object  | - указатель на объект,            |
        +---------------------------------------------+
        | comment | - комментарий.                    |
        +---------------------------------------------+

        ### Возвращаемое значение

        +-----------------------------+
        | TRUE  | - в случае успеха,  |
        +-----------------------------+
        | FALSE | - в случае неудачи. |
        +-----------------------------+
        """
        ...

    def SetPassword(self, OldPass, NewPass):  # iuserdatastorage_setpassword.html
        """
        ## SetPassword - Установить пароль на изменение данных в хранилище

        1. Если хранилище защищено паролем от изменений, то изменить пароль можно, только указав текущий пароль.

        2. По умолчанию пароль представляет собой пустую строку или нулевой указатель.

        ### Синтаксис Automation

        ```
        BOOL SetPassword( LPCTSTR oldPass, LPCTSTR newPass );
        ```

        ### Входные параметры

        +----------------------------+
        | OldPass | - старый пароль, |
        +----------------------------+
        | NewPass | - новый пароль.  |
        +----------------------------+
        """
        ...

    def SetVersion(self, Pass, arg1):
        ...

    def Version(self, Pass):  # iuserdatastorage_version.html
        """
        ## Version - Версия хранилища

        Если хранилище защищено паролем, то изменить свойство можно, только указав пароль.

        ### Синтаксис Automation

        ```
        Тип данных: long
        Version = StorageObj.Version(Pass)     # Получить свойство (* )
        StorageObj.Version(Pass) = Version     # Установить свойство (* )
        Version = StorageObj.GetVersion(Pass)  # Получить свойство (**)
        StorageObj.SetVersion (Version, Pass)  # Установить свойство (**)
        ```

        ### Входные параметры

        +------------------------------------+
        | Pass (LPSTR) | - пароль хранилища. |
        +------------------------------------+

        +-----------------------------------+
        | Pass (BSTR) | - пароль хранилища. |
        +-----------------------------------+
        """
        ...




class IUserDataStorages(IKompasCollection):  # iuserdatastorages.html
    """
    ## Интерфейс IUserDataStorages

    Интерфейс коллекции пользовательских хранилищ.

    Коллекция привязана к одному объекту в документе или непосредственно к документу (объект NULL).

    Элементы коллекции – хранилища представляют собой «именованные закладки», каждая из которых содержит данные различных типов в табличной форме.

    Интерфейс можно получить у Менеджера хранилищ, используя метод IUserDataStoragesMng::Item.
    """

    def Add(self, StorageName):  # iuserdatastorages_add.html
        """
        ## Add - Создать новое хранилище и добавить его в коллекцию

        ### Синтаксис Automation

        ```
        LPDISPATCH Add( LPCTSTR storageName );
        ```

        ### Входные параметры

        +---------------------------------------------+
        | storageName | - имя создаваемого хранилища. |
        +---------------------------------------------+
        """
        ...

    def Delete(self, Password, Index) -> bool:  # iuserdatastorages_delete.html
        """
        ## Delete - Удалить хранилище из коллекции по имени или по индексу

        1. В качестве индекса можно задать число или имя хранилища (VT_BSTR).

        2. Если хранилище защищено паролем, то метод работает только в случае указания правильного пароля.

        ### Синтаксис Automation

        ```
        BOOL Delete ( LPCTSTR password, const VARIANT index );
        ```

        ### Входные параметры

        +----------------------------------------+
        | Index    | - индекс или имя хранилища, |
        +----------------------------------------+
        | password | - пароль на хранилище.      |
        +----------------------------------------+

        ### Возвращаемое значение

        +-----------------------------+
        | TRUE  | - в случае успеха,  |
        +-----------------------------+
        | FALSE | - в случае неудачи. |
        +-----------------------------+
        """
        ...

    def Item(self, Index) -> IUserDataStorage:  # iuserdatastorages_item.html
        """
        ## Item - Хранилище, заданное по имени или по индексу

        Тип данных: указатель на интерфейс IUserDataStorage хранилища

        1. Свойство доступно только для чтения.

        2. В качестве индекса можно задать число или имя хранилища (VT_BSTR).

        ### Синтаксис Automation

        ```
        StorageObj = iObject.Item (Index)     # Получить свойство (* )
        StorageObj = iObject.GetItem (Index)  # Получить свойство (**)
        ```

        ### Входные параметры

        +-----------------------------+
        | Index | - индекс хранилища. |
        +-----------------------------+
        """
        ...




class IUserDataStoragesMng(IKompasCollection):  # iuserdatastoragesmng.html
    """
    ## Интерфейс IUserDataStoragesMng

    Интерфейс Менеджера пользовательских хранилищ.

    Объект документа или непосредственно документ (объект NULL) может быть связан с коллекцией хранилищ. Каждое хранилище представляет собой структурированный набор данных простых типов (Variant) или также указателей на объекты документа. Менеджер пользовательских хранилищ позволяет управлять пользовательскими хранилищами объектов и их коллекциями. Хранилище может быть использовано для графических документов и документов–моделей.

    Интерфейс можно получить у документа, используя метод IKompasDocument::UserDataStoragesMng.
    """

    def Add(self, Object):  # iuserdatastoragesmng_add.html
        """
        ## Add - Создать новое хранилища по объекту и добавить его в коллекцию

        1. Метод позволяет добавить массив хранилищ по объекту в коллекцию. Если объект NULL, то хранилище добавляется к документу.

        2. Если в коллекции было создано хранилище по выбранному объекту, то метод сработает как GetItem.

        ### Синтаксис Automation

        ```
        LPDISPATCH Add( LPDISPATCH Object );
        ```

        ### Входные параметры

        +-------------------------------------------------------------+
        | Object | - Указатель на интерфейс объекта IKompasAPIObject. |
        +-------------------------------------------------------------+

        ### Выходные параметры

        - Указатель на интерфейс коллекции объектов хранилища IUserDataStorages.
        """
        ...

    def Clear(self) -> bool:  # iuserdatastoragesmng_clear.html
        """
        ## Clear- Очистить менеджер от всех коллекций

        ### Синтаксис Automation

        ```
        BOOL Clear();
        ```

        ### Возвращаемое значение

        +-----------------------------+
        | TRUE  | - в случае успеха,  |
        +-----------------------------+
        | FALSE | - в случае неудачи. |
        +-----------------------------+
        """
        ...

    def Delete(self, Index) -> bool:  # iuserdatastoragesmng_delete.html
        """
        ## Delete - Удалить коллекцию по ссылке на объект или по индексу

        В качестве индекса можно задать число или объект, к которому привязано хранилище (тип VT_DISPATCH)

        ### Синтаксис Automation

        ```
        BOOL Delete (const VARIANT & index );
        ```

        ### Входные параметры

        +---------------------------------------------------------------------------------------------+
        | Index | - Индекс или указатель на интерфейс коллекции объектов хранилища IUserDataStorages. |
        +---------------------------------------------------------------------------------------------+

        ### Возвращаемое значение

        +-----------------------------+
        | TRUE  | - в случае успеха,  |
        +-----------------------------+
        | FALSE | - в случае неудачи. |
        +-----------------------------+
        """
        ...

    def Item(self, Index) -> IUserDataStorages:  # iuserdatastoragesmng_item.html
        """
        ## Item - Объект, заданный по ссылке на объект или по индексу

        Тип данных: указатель на интерфейс IUserDataStorages коллекции объектов хранилища.

        1. Свойство доступно только для чтения.

        2. В качестве индекса можно задать число или объект, к которому привязано хранилище (тип VT_DISPATCH).

        ### Синтаксис Automation

        ```
        StorageObj = iObject.Item (Index)     # Получить свойство (* )
        StorageObj = iObject.GetItem (Index)  # Получить свойство (**)
        ```
        """
        ...




class IUserMetadataManager(IDispatch):  # iusermetadatamanager.html
    """
    ## Интерфейс IUserMetadataManager

    Менеджер пользовательских метаданных.
    """

    def AddFile(self, ApplicationIID, SrcFileName, DestFileName, AllowReplacement, Compress) -> bool:  # iusermetadatamanager_addfile.html
        """
        ## AddFile - Добавить файл в хранилище

        ### Синтаксис Automation

        ```
        BOOL AddFile( BSTR ApplicationIID, BSTR SrcFileName, BSTR DestFileName, BOOL AllowReplacement, BOOL Compress );
        ```

        ### Входные параметры

        +---------------------------------------------+
        | ApplicationIID   | - GUID приложения,       |
        +---------------------------------------------+
        | SrcFileName      | - имя файла в хранилище, |
        +---------------------------------------------+
        | DestFileName     | - имя файла на диске,    |
        +---------------------------------------------+
        | AllowReplacement | - перезаписывать,        |
        +---------------------------------------------+
        | Compress         | - добавить со сжатием.   |
        +---------------------------------------------+

        ### Возвращаемое значение

        +-----------------------------+
        | TRUE  | - в случае успеха,  |
        +-----------------------------+
        | FALSE | - в случае неудачи. |
        +-----------------------------+
        """
        ...

    def CreateStorage(self, ApplicationIID, ApplicationDescription, Version) -> bool:  # iusermetadatamanager_createstorage.html
        """
        ## CreateStorage - Создать хранилище файлов приложения

        ### Синтаксис Automation

        ```
        BOOL CreateStorage( BSTR ApplicationIID, BSTR ApplicationDescription, BSTR Version );
        ```

        ### Входные параметры

        +----------------------------------------------+
        | ApplicationIID         | - GUID приложения,  |
        +----------------------------------------------+
        | ApplicationDescription | - имя приложения,   |
        +----------------------------------------------+
        | DestFileName           | - версия хранилища. |
        +----------------------------------------------+

        ### Возвращаемое значение

        +-----------------------------+
        | TRUE  | - в случае успеха,  |
        +-----------------------------+
        | FALSE | - в случае неудачи. |
        +-----------------------------+
        """
        ...

    def DeleteFile(self, ApplicationIID, StorageFileName) -> bool:  # iusermetadatamanager_deletefile.html
        """
        ## DeleteFile - Удалить файл из хранилища

        ### Синтаксис Automation

        ```
        BOOL DeleteFile( BSTR ApplicationIID, BSTR StorageFileName );
        ```

        ### Входные параметры

        +-------------------------------------------+
        | ApplicationIID  | - GUID приложения,      |
        +-------------------------------------------+
        | StorageFileName | - имя удаляемого файла. |
        +-------------------------------------------+

        ### Возвращаемое значение

        +-----------------------------+
        | TRUE  | - в случае успеха,  |
        +-----------------------------+
        | FALSE | - в случае неудачи. |
        +-----------------------------+
        """
        ...

    def DeleteStorage(self, ApplicationIID) -> bool:  # iusermetadatamanager_deletestorage.html
        """
        ## DeleteStorage - Удалить хранилище файлов приложения

        ### Синтаксис Automation

        ```
        BOOL DeleteStorage( BSTR ApplicationIID );
        ```

        ### Входные параметры

        +-------------------------------------+
        | ApplicationIID | - GUID приложения. |
        +-------------------------------------+

        ### Возвращаемое значение

        +-----------------------------+
        | TRUE  | - в случае успеха,  |
        +-----------------------------+
        | FALSE | - в случае неудачи. |
        +-----------------------------+
        """
        ...

    def ExistStorage(self, ApplicationIID) -> bool:  # iusermetadatamanager_existstorage.html
        """
        ## ExistStorage - Проверить наличие хранилища данных приложения

        ### Синтаксис Automation

        ```
        BOOL ExistStorage( BSTR ApplicationIID );
        ```

        ### Входные параметры

        +-------------------------------------+
        | ApplicationIID | - GUID приложения. |
        +-------------------------------------+

        ### Возвращаемое значение

        +------------------------------------+
        | TRUE  | -хранилище существует,     |
        +------------------------------------+
        | FALSE | - хранилище не существует. |
        +------------------------------------+
        """
        ...

    def ExtractFile(self, ApplicationIID, SrcFileName, DestFileName, AllowReplacement) -> bool:  # iusermetadatamanager_extractfile.html
        """
        ## ExtractFile - Извлечь файл из хранилища

        ### Синтаксис Automation

        ```
        BOOL ExtractFile( BSTR ApplicationIID, BSTR SrcFileName, BSTR DestFileName, BOOL AllowReplacement );
        ```

        ### Входные параметры

        +---------------------------------------------+
        | ApplicationIID   | - GUID приложения,       |
        +---------------------------------------------+
        | SrcFileName      | - имя файла в хранилище, |
        +---------------------------------------------+
        | DestFileName     | - имя файла на диске,    |
        +---------------------------------------------+
        | AllowReplacement | - перезаписывать.        |
        +---------------------------------------------+

        ### Возвращаемое значение

        +-----------------------------+
        | TRUE  | - в случае успеха,  |
        +-----------------------------+
        | FALSE | - в случае неудачи. |
        +-----------------------------+
        """
        ...

    def GetAllFilenames(self, ApplicationIID) -> list[str]:  # iusermetadatamanager_getallfilenames.html
        """
        ## GetAllFilenames - Получить список файлов приложения

        ### Синтаксис Automation

        ```
        VARIANT GetAllFilenames( BSTR ApplicationIID );
        ```

        ### Входные параметры

        +-------------------------------------+
        | ApplicationIID | - GUID приложения. |
        +-------------------------------------+

        ### Возвращаемое значение

        -массив VT_ARRAY | VT_BSTR имен файлов в хранилище.
        """
        ...

    def IsExistFile(self, ApplicationIID, StorageFileName) -> bool:  # iusermetadatamanager_isexistfile.html
        """
        ## IsExistFile - Проверить наличие файла в хранилище

        ### Синтаксис Automation

        ```
        BOOL IsExistFile( BSTR ApplicationIID, BSTR StorageFileName );
        ```

        ### Входные параметры

        +-------------------------------------------+
        | ApplicationIID  | - GUID приложения,      |
        +-------------------------------------------+
        | StorageFileName | - имя удаляемого файла. |
        +-------------------------------------------+

        ### Возвращаемое значение

        +-------------------------------+
        | TRUE  | - файл существует,    |
        +-------------------------------+
        | FALSE | - файл не существует. |
        +-------------------------------+
        """
        ...

    def SetStorageInfo(self, ApplicationIID, ParameterName, arg2):
        ...

    def StorageInfo(self, ApplicationIID, ParameterName) -> str:  # iusermetadatamanager_storageinfo.html
        """
        ## StorageInfo - Получить данные из StorageInfo

        Тип данных: BSTR

        Свойство позволяет устанавливать и получать имя или версию хранилища:

        •для получения имени ParameterName - "storageInfo.appName",

        •для получения версии - "storageInfo.appVersion".

        ### Синтаксис Automation

        ```
        StorageInfo = Object.StorageInfo(ApplicationIID, ParameterName )     # Получить свойство (* )
        Object.StorageInfo( ApplicationIID, ParameterName ) = Visible        # Установить свойство (* )
        StorageInfo= Object.GetStorageInfo( ApplicationIID, ParameterName )  # Получить свойство (**)
        Object.SetStorageInfo( ApplicationIID, ParameterName, StorageInfo )  # Установить свойство (**)
        ```

        ### Входные параметры

        +------------------------------------------+
        | BSTR ApplicationIID | - GUID приложения, |
        +------------------------------------------+
        | BSTR ParameterName  | - имя параметр.    |
        +------------------------------------------+
        """
        ...




class IViewsAndLayersManager(IKompasAPIObject):  # iviewsandlayersmanager.html
    """
    ## Интерфейс IViewsAndLayersManager

    События

    Менеджер слоев и видов графического документа.

    1. Данный интерфейс предназначен для работы с видами, слоями, группами слоев.

    2. Интерфейс можно получить при помощи свойства IKompasDocument2D::ViewsAndLayersManager от интерфейса 2D-документа IKompasDocument2D.
    """

    @property
    def LayerGroups(self) -> ILayerGroups:  # iviewsandlayersmanager_layergroups.html
        """
        ## LayerGroups - Возвращает коллекцию групп слоев

        Тип данных: ILayerGroups

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        LayerGroups = iObject.LayerGroups       # Получить свойство (* )
        LayerGroups = iObject.GetLayerGroups()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def Views(self) -> IViews:  # iviewsandlayersmanager_views.html
        """
        ## Views - Возвращает коллекцию видов

        Тип данных: IViews

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        Views = iObject.Views       # Получить свойство (* )
        Views = iObject.GetViews()  # Получить свойство (**)
        ```
        """
        ...




class ILayerGroup(IKompasAPIObject):  # ilayergroup.html
    """
    ## Интерфейс ILayerGroup

    Интерфейс группы слоев.

    Данный интерфейс можно получить у коллекции групп слоев ILayerGroups с помощью свойства ILayerGroups::Item, метода ILayerGroups::Add либо от интерфейса другой группы слоев с помощью свойства ILayerGroup::OwnerGroup.

    Группы являются именованными объектами.

    По умолчанию при создании группы ей присваивается имя, состоящее из слова "Группа" (для статической группы и группы свойств) или "Фильтр" (для динамической группы) и порядкового номера. Это имя может быть изменено.

    Группы слоев сохраняются в документе.

    Группа слоев может принадлежать:

    1. Виду - в группе объединяются слои, принадлежащие виду.

    2. Документу - в группе объединяются слои с разных видов.

    Группа может быть:

    •статической - Группа слоев или Группа свойств слоев;

    •динамической - Фильтр.

    Группа слоев группа объединяет слои. Для создания статической группы используется метод ILayerGroups::Add, в который передается параметр dynamic равный FALSE. Работа с группой слоев доступна, если в документе установлен режим Группировать слои, т.е. свойство IDocument2DSettings::LayersGroupWay имеет значение wgLayers.

    Фильтр включает в себя слои, отфильтрованные согласно заданным условиям. См. ILayerFilterConditions и ILayerFilterCondition.

    Признаком того, что группа является динамической, является возможность получения у нее коллекции условий ILayerFilterConditions.

    Для создания динамической группы используется метод ILayerGroups::Add, в который передается параметр dynamic равный TRUE. Работа с группой слоев доступна, если в документе установлен режим Группировать слои, т. е. свойство IDocument2DSettings::LayersGroupWay имеет значение wgLayers.

    Группа свойств слоев, кроме списка слоев, содержит массив свойств, устанавливаемых данным слоям. Для создания группы используется метод ILayerGroups::Add, в котором параметр dynamic не учитывается. Для передачи параметров слоям нужно установить свойство ILayerGroup::Current равным TRUE

    Работа с Группой свойств слоев доступна, если в документе установлен режим Группировать свойства слоев, т. е. свойство IDocument2DSettings::LayersGroupWay имеет значение wgLayersCharacteristics.

    Добавление слоя в документ не приводит к автоматическому добавлению этого слоя в статическую группу или в группу свойств.

    При удалении слоя из группы он не удаляется из документа.

    При удалении слоя из документа он автоматически удаляется из группы.

    Изменение способа группировки в документе приводит к необратимым изменениям созданных групп.

    Если устанавливается режим группировки слоев, то Группы свойств слоев превращаются в Группы слоев.

    Если устанавливается режим группировки свойств слоев, то Группы слоев и Фильтры превращаются в Группы свойств.
    """

    @property
    def Current(self) -> bool:  # ilayergroup_current.html
        """
        ## Current - Группа является текущей (состояния слоев переданы в модель)

        Тип данных: BOOL

        Свойство используется, если в документе установлен режим Группировать свойства слоев, т. е. свойство IDocument2DSettings::LayersGroupWay имеет значение wgLayersCharacteristics.

        ### Синтаксис Automation

        ```
        Current = iObject.Current       # Получить свойство (* )
        iObject.Current = Current       # Установить свойство (* )
        Current = iObject.GetCurrent()  # Получить свойство (**)
        iObject.SetCurrent (Current)    # Установить свойство (**)
        ```
        """
        ...
    @Current.setter
    def Current(self, value: bool): ...

    @property
    def LayerFilterConditions(self) -> ILayerFilterConditions:  # ilayergroup_layerfilterconditions.html
        """
        ## LayerFilterConditions - Коллекция условий фильтрации слоев

        Тип данных: указатель на интерфейс коллекции условий фильтрации ILayerFilterConditions

        Коллекцию условий можно получить только для динамической группы слоев.

        ### Синтаксис Automation

        ```
        LayerFilterConditions = iObject.LayerFilterConditions       # Получить свойство (* )
        LayerFilterConditions = iObject.GetLayerFilterConditions()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def LayerGroups(self) -> ILayerGroups:  # ilayergroup_layergroups.html
        """
        ## LayerGroups - Коллекция групп слоев

        Тип данных: указатель на интерфейс ILayerGroups

        ### Синтаксис Automation

        ```
        LayerGroups = iObject.LayerGroups       # Получить свойство (* )
        LayerGroups = iObject.GetLayerGroups()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def Layers(self) -> ILayers:  # ilayergroup_layers.html
        """
        ## Layers - Коллекция слоев группы

        Тип данных: указатель на интерфейс ILayers

        ### Синтаксис Automation

        ```
        Layers = iObject.Layers       # Получить свойство (* )
        Layers = iObject.GetLayers()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def Name(self) -> str:  # ilayergroup_name.html
        """
        ## Name - Имя группы

        Тип данных: BSTR

        ### Синтаксис Automation

        ```
        Name = iObject.Name       # Получить свойство (* )
        iObject.Name = Name       # Установить свойство (* )
        Name = iObject.GetName()  # Получить свойство (**)
        iObject.SetName (Name)    # Установить свойство (**)
        ```
        """
        ...
    @Name.setter
    def Name(self, value: str): ...

    @property
    def OwnerGroup(self) -> ILayerGroup:  # ilayergroup_ownergroup.html
        """
        ## OwnerGroup - Владелец группы - другая группа

        Тип данных: указатель на интерфейс группы ILayerGroup

        1. Позволяет получить владельца группы.

        2. Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        OwnerGroup = iObject.OwnerGroup       # Получить свойство (* )
        OwnerGroup = iObject.GetOwnerGroup()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def OwnerView(self) -> IView:  # ilayergroup_ownerview.html
        """
        ## OwnerView - Владелец группы - вид

        Тип данных: указатель на интерфейс вида IView

        1. Свойство позволяет отсоединить группу от вида.

        2. Чтобы установить свойство отсоединения группы от вида, необходимо передать параметр с нулевым значением.

        ### Синтаксис Automation

        ```
        OwnerView = iObject.OwnerView       # Получить свойство (* )
        iObject.OwnerView = OwnerView       # Установить свойство (* )
        OwnerView = iObject.GetOwnerView()  # Получить свойство (**)
        iObject.SetOwnerView (OwnerView)    # Установить свойство (**)
        ```
        """
        ...
    @OwnerView.setter
    def OwnerView(self, value: IView): ...

    @property
    def UniqueId(self) -> float:  # ilayergroup_uniqueid.html
        """
        ## UniqueId - Уникальный идентификатор группы слоев

        Тип данных: double

        1. Свойство доступно только для чтения.

        2. После создания группы свойство не изменяется и хранится в чертеже.

        ### Синтаксис Automation

        ```
        UniqueId = iObject.UniqueId       # Получить свойство (* )
        UniqueId = iObject.GetUniqueId()  # Получить свойство (**)
        ```
        """
        ...

    def Delete(self) -> bool:  # ilayergroup_delete.html
        """
        ## Delete - Удалить группу слоев

        При удалении группы слоев слои, входящие в нее, из документа не удаляются. Для удаления слоя используется метод IDrawingObject::Delete.

        ### Синтаксис Automation

        ```
        BOOL Delete();
        ```

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def GetLayerStates(self, LayerIndex) -> bool:  # ilayergroup_getlayerstates.html
        """
        ## GetLayerStates - Получить состояния слоя группы по индексу, имени или ссылке

        1. Если в документе установлен режим Группировать свойства слоев, т. е. свойство IDocument2DSettings::LayersGroupWay имеет значение wgLayersCharacteristics, то метод позволяет получить свойства слоя в группе. При этом значения свойств могут отличаться от установленных в текущий момент у слоя в документе. Для передачи свойств слою из группы нужно сделать группу текущей, то есть установить значение ILayerGroup::Current равным TRUE.

        2. Если в документе установлен режим Группировать слои, т. е. свойство IDocument2DSettings::LayersGroupWay имеет значение wgLayers, то метод используется для получения свойств конкретного слоя (LayerIndex - индекс слоя).

        ### Синтаксис Automation

        ```
        BOOL GetLayerStates ([in] VARIANT LayerIndex,
        [out] BOOL* visible,
        [out] BOOL* background,
        [out] BOOL* curent,
        [out] long* color);
        ```

        ### Входные параметры

        +-----------------------------+
        | LayerIndex | - индекс слоя. |
        +-----------------------------+

        ### Выходные параметры

        +-----------------------------------------+
        | visible    | - свойство - видимый слой, |
        +-----------------------------------------+
        | background | - свойство - фоновый слой, |
        +-----------------------------------------+
        | current    | - свойство - текущий слой, |
        +-----------------------------------------+
        | color      | - цвет слоя.               |
        +-----------------------------------------+

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def SetLayerStates(self, LayerIndex, Visible, Background, Curent, Color) -> bool:  # ilayergroup_setlayerstates.html
        """
        ## SetLayerStates - Изменить состояния слоя группы по индексу или имени

        1. Если в документе установлен режим Группировать свойства слоев, т. е. свойство IDocument2DSettings::LayersGroupWay имеет значение wgLayersCharacteristics, то метод позволяет установить свойства слоя в группе. При этом параметры слоя в документе не меняются. При этом значения свойств могут отличаться от установленных в текущий момент у слоя в документе. Для передачи свойств слою из группы нужно сделать группу текущей, то есть установить значение ILayerGroup::Current равным TRUE.

        2. Если в документе установлен режим Группировать слои, т. е. свойство IDocument2DSettings::LayersGroupWay имеет значение wgLayers, то метод используется для установки свойств конкретного слоя (LayerIndex - индекс слоя). Для установки одинаковых состояний и цвета всем слоям, входящим в группу, нужно передать LayerIndex = -1.

        ### Синтаксис Automation

        ```
        BOOL SetLayerStates ([in] VARIANT LayerIndex,
        [in] BOOL visible,
        [in]BOOL background,
        [in] BOOL* curent,
        [in] long color);
        ```

        ### Входные параметры

        +-----------------------------------------+
        | LayerIndex | - индекс слоя,             |
        +-----------------------------------------+
        | visible    | - свойство - видимый слой, |
        +-----------------------------------------+
        | background | - свойство - фоновый слой, |
        +-----------------------------------------+
        | current    | - свойство - текущий слой, |
        +-----------------------------------------+
        | color      | - цвет слоя.               |
        +-----------------------------------------+

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...




class ILayerFilterCondition(IKompasAPIObject):  # ilayerfiltercondition.html
    """
    ## Интерфейс ILayerFilterCondition

    Интерфейс условия фильтрации слоев.

    Условия фильтрации используются для формирования состава динамической группы слоев.

    Данный интерфейс можно получить через коллекцию условий фильтрации слоев ILayerFilterConditions с помощью свойства ILayerFilterConditions::Item.
    """

    @property
    def Background(self) -> int:  # ilayerfiltercondition_background.html
        """
        ## Background - Состояние слоя для фильтрации - фоновый

        Тип данных: из перечисления FilterConditionStateEnum.

        После установки нового значения данного свойства состав группы будет автоматически изменен.

        ### Синтаксис Automation

        ```
        Background = iObject.Background       # Получить свойство (* )
        iObject.Background = Background       # Установить свойство (* )
        Background = iObject.GetBackground()  # Получить свойство (**)
        iObject.SetBackground (Background)    # Установить свойство (**)
        ```
        """
        ...
    @Background.setter
    def Background(self, value: int): ...

    @property
    def Color(self) -> int:  # ilayerfiltercondition_color.html
        """
        ## Color - Цвет слоя для фильтрации

        Тип данных: long

        После установки нового значения данного свойства состав группы будет автоматически изменен.

        ### Синтаксис Automation

        ```
        Color = iObject.Color       # Получить свойство (* )
        iObject.Color = Color       # Установить свойство (* )
        Color = iObject.GetColor()  # Получить свойство (**)
        iObject.SetColor (Color)    # Установить свойство (**)
        ```
        """
        ...
    @Color.setter
    def Color(self, value: int): ...

    @property
    def Comment(self) -> str:  # ilayerfiltercondition_comment.html
        """
        ## Comment - Содержание комментария для фильтрации

        Тип данных: BSTR

        1. При задании условия фильтрации по номеру можно использовать маски. В масках знак «*» заменяет любое количество любых символов. Знак «?» заменяет один любой символ.

        2. После установки нового значения данного свойства состав группы будет автоматически изменен.

        ### Синтаксис Automation

        ```
        Comment = iObject.Comment       # Получить свойство (* )
        iObject.Comment = Comment       # Установить свойство (* )
        Comment = iObject.GetComment()  # Получить свойство (**)
        iObject.SetComment (Comment)    # Установить свойство (**)
        ```
        """
        ...
    @Comment.setter
    def Comment(self, value: str): ...

    @property
    def HaveObjects(self) -> int:  # ilayerfiltercondition_haveobjects.html
        """
        ## HaveObjects - Состояние слоя для фильтрации - с объектами или пустой

        Тип данных: из перечисления FilterConditionStateEnum

        После установки нового значения данного свойства состав группы будет автоматически изменен.

        ### Синтаксис Automation

        ```
        HaveObjects = iObject.HaveObjects       # Получить свойство (* )
        iObject.HaveObjects = HaveObjects       # Установить свойство (* )
        HaveObjects = iObject.GetHaveObjects()  # Получить свойство (**)
        iObject.SetHaveObjects (HaveObjects)    # Установить свойство (**)
        ```
        """
        ...
    @HaveObjects.setter
    def HaveObjects(self, value: int): ...

    @property
    def Name(self) -> str:  # ilayerfiltercondition_name.html
        """
        ## Name - Имя слоя для фильтрации

        Тип данных: BSTR (строка)

        1. При задании условия фильтрации по номеру можно использовать маски. В масках знак «*» заменяет любое количество любых символов. Знак «?» заменяет один любой символ.

        2. После установки нового значения данного свойства состав группы будет автоматически изменен.

        ### Синтаксис Automation

        ```
        Name = iObject.Name       # Получить свойство (* )
        iObject.Name = Name       # Установить свойство (* )
        Name = iObject.GetName()  # Получить свойство (**)
        iObject.SetName (Name)    # Установить свойство (**)
        ```
        """
        ...
    @Name.setter
    def Name(self, value: str): ...

    @property
    def Number(self) -> str:  # ilayerfiltercondition_number.html
        """
        ## Number - Номер слоя для фильтрации

        Тип данных: BSTR (строка)

        1. При задании условия фильтрации по номеру можно использовать маски. В масках знак «*» заменяет любое количество любых символов. Знак «?» заменяет один любой символ.

        2. После установки нового значения данного свойства состав группы будет автоматически изменен.

        ### Синтаксис Automation

        ```
        Number = iObject.Number       # Получить свойство (* )
        iObject.Number = Number       # Установить свойство (* )
        Number = iObject.GetNumber()  # Получить свойство (**)
        iObject.SetNumber (Number)    # Установить свойство (**)
        ```
        """
        ...
    @Number.setter
    def Number(self, value: str): ...

    @property
    def Projected(self) -> int:  # ilayerfiltercondition_projected.html
        """
        ## Projected - Признак проецирования для слоя 3D

        Тип данных: из перечисления FilterConditionStateEnum

        ### Синтаксис Automation

        ```
        Projected = Object.Projected       # Получить свойство (* )
        Object.Projected = Projected       # Установить свойство (* )
        Projected = Object.GetProjected()  # Получить свойство (**)
        Object.SetProjected( Projected )   # Установить свойство (**)
        ```
        """
        ...
    @Projected.setter
    def Projected(self, value: int): ...

    @property
    def Visible(self) -> int:  # ilayerfiltercondition_visible.html
        """
        ## Visible - Состояние слоя для фильтрации - видимый или погашенный

        Тип данных: из перечисления FilterConditionStateEnum

        После установки нового значения данного свойства состав группы будет автоматически изменен.

        ### Синтаксис Automation

        ```
        Visible = iObject.Visible       # Получить свойство (* )
        iObject.Visible = Visible       # Установить свойство (* )
        Visible = iObject.GetVisible()  # Получить свойство (**)
        iObject.SetVisible (Visible)    # Установить свойство (**)
        ```
        """
        ...
    @Visible.setter
    def Visible(self, value: int): ...




class ILayerGroups(IKompasCollection):  # ilayergroups.html
    """
    ## Интерфейс ILayerGroups

    Интерфейс коллекции групп слоев.

    Коллекция объединяет группы слоев ILayerGroup.

    Данный интерфейс может быть получен следующими способами:

    •от интерфейса менеджера видов и слоёв IViewsAndLayersManager с помощью свойства IViewsAndLayersManager::LayerGroups,

    •от интерфейса группы слоев ILayerGroup с помощью свойства ILayerGroup::LayerGroups.
    """

    def Add(self, Owner, Dinamic) -> ILayerGroup:  # ilayergroups_add.html
        """
        ## Add - Создать группу

        Группа добавляется в коллекцию.

        ### Синтаксис Automation

        ```
        LPDISPATCH Add( [in] LPDISPATCH owner, [in] VARIANT_BOOL dinamic );
        HRESULT Add( [in] IView* owner, [in] VARIANT_BOOL dinamic, [out, retval] ILayerGroup** Result );
        ```

        ### Входные параметры

        +------------------------------------------+
        | owner   | - вид - владелец группы,       |
        +------------------------------------------+
        | dinamic | - признак динамической группы. |
        +------------------------------------------+

        ### Возвращаемое значение

        Указатель на интерфейс группы слоев ILayerGroup
        """
        ...

    def Attach(self, PGroup) -> bool:  # ilayergroups_attach.html
        """
        ## Attach - Добавить существующую группу

        Интерфейс группы добавляется в коллекцию.

        ### Синтаксис Automation

        ```
        BOOL Attach (LPDISPATCH pVal);
        HRESULT Attach ([in] ILayerGroup * pVal, [out, retval] VARIANT_BOOL * Result);
        ```

        ### Входные параметры

        +-----------------------------------------------------------+
        | pVal | - Указатель на интерфейс группы слоев ILayerGroup. |
        +-----------------------------------------------------------+

        ### Возвращаемое значение

        +-----------------------------+
        | TRUE  | - в случае успеха,  |
        +-----------------------------+
        | FALSE | - в случае неудачи. |
        +-----------------------------+
        """
        ...

    def Detach(self, PGroup) -> bool:  # ilayergroups_detach.html
        """
        ## Detach - Отсоединить группу слоев

        Интерфейс группы слоев отсоединяется от коллекции, группа при этом из документа не удаляется.

        ### Синтаксис Automation

        ```
        BOOL Detach (LPDISPATCH pVal);
        ```

        ### Входные параметры

        +-----------------------------------------------------------+
        | pVal | - Указатель на интерфейс группы слоев ILayerGroup. |
        +-----------------------------------------------------------+

        ### Возвращаемое значение

        +-----------------------------+
        | TRUE  | - в случае успеха,  |
        +-----------------------------+
        | FALSE | - в случае неудачи. |
        +-----------------------------+
        """
        ...

    def Item(self, Index) -> ILayerGroup:  # ilayergroups_item.html
        """
        ## Item - Группа, заданная по индексу, имени или уникальному идентификатору

        Тип данных: указатель на интерфейс ILayerGroup

        Свойство доступно только для чтения.

        В качестве индекса может использоваться индекс элемента в массиве, имя или уникальный идентификатор ILayerGroup::UniqueId. Для получения группы по идентификатору нужно передать вещественное число, установив тип значения VT_R8.

        ### Синтаксис Automation

        ```
        Item = iObject.Item       # Получить свойство (* )
        Item = iObject.GetItem()  # Получить свойство (**)
        ```

        ### Входные параметры

        +-----------------------------------------------------+
        | index | - индекс объекта в коллекции (тип VARIANT). |
        +-----------------------------------------------------+
        """
        ...




class ILayerFilterConditions(IKompasCollection):  # ilayerfilterconditions.html
    """
    ## Интерфейс ILayerFilterConditions

    Интерфейс коллекции условий фильтрации слоев.

    1. Условия фильтрации используются для формирования состава динамической группы слоев.

    2. Слой автоматически будет включен в состав группы если его свойства удовлетворяют хотя бы одному из условий.

    Данный интерфейс можно получить через группу слоев ILayerGroup с помощью свойства ILayerGroup::LayerFilterConditions.
    """

    def Add(self) -> ILayerFilterCondition:  # ilayerfilterconditions_add.html
        """
        ## Add - Создать условие фильтрации слоев

        После вызова этого метода условие фильтрации слоев будет добавлено в коллекцию.

        Состав группы изменится в соответствии с новыми условиями.

        ### Синтаксис Automation

        ```
        LPDISPATCH Add();
        ```

        ### Возвращаемое значение

        Указатель на интерфейс ILayerFilterCondition
        """
        ...

    def Item(self, Index) -> ILayerFilterCondition:  # ilayerfilterconditions_item.html
        """
        ## Item - Условие фильтрации слоев, заданное по индексу

        Тип данных: указатель на интерфейс ILayerFilterCondition

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        LayerFilterCondition = iObject.Item (Index)     # Получить свойство (* )
        LayerFilterCondition = iObject.GetItem (Index)  # Получить свойство (**)
        ```

        ### Входные параметры

        +--------------------------------------------+
        | index | - индекс в коллекции; тип VARIANT. |
        +--------------------------------------------+
        """
        ...




class IDrawingObjects(IKompasCollection):  # idrawingobjects.html
    """
    ## IDrawingObjects

    Базовый интерфейс для коллекций графических объектов.

    1. Данный интерфейс является базовым для коллекций графических объектов.

    2. Интерфейс может быть получен с помощью метода IUnknown:: QueryInterface от интерфейса коллекции конкретных объектов, например, от интерфейса видов IViews, слоев ILayers.
    """

    def Item(self, Index) -> IDrawingObject:  # idrawingobjects_item.html
        """
        ## Item - Объект, заданный по ссылке или по индексу

        Тип данных: указатель на интерфейс IDrawingObject

        1. Свойство доступно только для чтения.

        2. В качестве индекса может использоваться индекс элемента в массиве, reference объекта.

        ### Синтаксис Automation

        ```
        Item = iObject.Item       # Получить свойство (* )
        Item = iObject.GetItem()  # Получить свойство (**)
        ```

        ### Входные параметры

        +----------------------------------------------------+
        | Index | - индекс объекта в коллекции, тип VARIANT. |
        +----------------------------------------------------+
        """
        ...




class ILayers(IDrawingObjects):  # ilayers.html
    """
    ## Интерфейс ILayers

    Интерфейс коллекции слоев графического документа.

    Интерфейс коллекции слоев можно получить от интерфейса вида с помощью свойства IView::Layers или у группы слоев с помощью свойства ILayerGroup::Layers.
    """

    def Add(self) -> ILayer:  # ilayers_add.html
        """
        ## Add - Создать слой (добавляет слой в коллекцию)

        1. Интерфейс слоя добавляется в коллекцию.

        2. Слой в документе будет создан после вызова метода IDrawingObject::Update.

        ### Синтаксис Automation

        ```
        LPDISPATCH Add();
        ```

        ### Возвращаемое значение

        - Указатель на интерфейс слоя ILayer.
        """
        ...

    def Attach(self, PVal) -> bool:  # ilayers_attach.html
        """
        ## Attach - Добавить существующий слой

        1. Интерфейс слоя добавляется в коллекцию.

        2. Метод работает только для коллекции слоев группы ILayerGroup, если она не является динамической.

        ### Синтаксис Automation

        ```
        BOOL Attach (LPDISPATCH pVal);
        ```

        ### Входные параметры

        +----------------------------------------------+
        | pVal | - Указатель на интерфейс слоя ILayer. |
        +----------------------------------------------+

        ### Возвращаемое значение

        +-----------------------------+
        | TRUE  | - в случае успеха,  |
        +-----------------------------+
        | FALSE | - в случае неудачи. |
        +-----------------------------+
        """
        ...

    def Detach(self, PVal) -> ILayer:  # ilayers_detach.html
        """
        ## Detach - Отсоединить слой

        Тип данных : указатель на интерфейс слоя ILayer.

        1. Интерфейс слоя отсоединяется от коллекции, слой при этом из документа не удаляется.

        2. Метод работает только для коллекции слоев группы ILayerGroup, если она не является динамической.

        ### Синтаксис Automation

        ```
        BOOL Detach (LPDISPATCH pVal);
        ```

        ### Входные параметры

        +----------------------------------------------+
        | pVal | - Указатель на интерфейс слоя ILayer. |
        +----------------------------------------------+

        ### Возвращаемое значение

        +-----------------------------+
        | TRUE  | - в случае успеха,  |
        +-----------------------------+
        | FALSE | - в случае неудачи. |
        +-----------------------------+
        """
        ...

    def Layer(self, Index) -> ILayer:  # ilayers_layer.html
        """
        ## Layer - Слой, заданный по индексу

        Тип данных: указатель на интерфейс слоя ILayer

        1. Свойство доступно только для чтения.

        2. В качестве индекса может использоваться индекс элемента в массиве, ссылка на слой, имя слоя.

        ### Синтаксис Automation

        ```
        Layer = iObject.Layer (Index)     # Получить свойство (* )
        Layer = iObject.GetLayer (Index)  # Получить свойство (**)
        ```

        ### Входные параметры

        +-------------------------------------------------+
        | index | - индекс слоя в коллекции; тип VARIANT. |
        +-------------------------------------------------+
        """
        ...

    def LayerByNumber(self, Number) -> ILayer:  # ilayers_layerbynumber.html
        """
        ## LayerByNumber - Слой, заданный по номеру

        Тип данных: указатель на интерфейс слоя ILayer

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        Layer = iObject.LayerByNumber (Number)     # Получить свойство (* )
        Layer = iObject.GetLayerByNumber (Number)  # Получить свойство (**)
        ```

        ### Входные параметры

        +----------------------------------+
        | number | - номер слоя; тип long. |
        +----------------------------------+
        """
        ...




class IViews(IDrawingObjects):  # iviews.html
    """
    ## Интерфейс IViews

    Интерфейс коллекции видов графического документа.

    Типы видов описаны в перечислении LtViewType.

    В документе типа Фрагмент есть только один системный вид.

    В документе типа Чертеж обязательно есть системный вид (только один) и могут быть до 254 видов любого типа из перечисления LtViewType, кроме системного.

    Данный интерфейс можно получить от интерфейса менеджера видов и слоев IViewsAndLayersManager с помощью свойства IViewsAndLayersManager::Views.
    """

    @property
    def ActiveView(self) -> IView:  # iviews_activeview.html
        """
        ## ActiveView - Активный вид

        Тип данных: указатель на интерфейс вида IView

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        ActiveView = iObject.ActiveView     # Получить свойство (* )
        ActiveView = iObject.GetActiveView  # Получить свойство (**)
        ```
        """
        ...

    def Add(self, ViewType) -> IView:  # iviews_add.html
        """
        ## Add - Создать вид (добавляет вид в коллекцию)

        1. Системный вид создается автоматически при создании документа. Создавать второй системный вид (тип vt_System) нельзя.

        2. После вызова этого метода вид будет добавлен в коллекцию, но в документе не появится.

        3. Для отображения вида в документе нужно задать его параметры и вызвать метод IDrawingObject::Update.

        ### Синтаксис Automation

        ```
        LPDISPATCH Add (long ViewType);
        ```

        ### Входные параметры

        +----------------------------------------------------------------+
        | ViewType | - тип создаваемого вида из перечисления LtViewType. |
        +----------------------------------------------------------------+

        ### Возвращаемое значение

        - Указатель на интерфейс вида IView.
        """
        ...

    def AddStandartViews(self, FileName, ProjectionName, ProjectionsTypes, X, Y, Scale, DX, DY) -> bool:  # iviews_addstandartviews.html
        """
        ## AddStandartViews - Создать группу стандартных ассоциативных видов (добавить виды в коллекцию)

        ### Синтаксис Automation

        ```
        BOOL AddStandartViews (BSTR FileName,
        BSTR ProjectionName,
        VARIANT ProjectionsTypes,
        double X,
        double Y,
        double Scale,
        double DX,
        double DY );
        ```

        ### Входные параметры

        +------------------------------------------------------------------+
        | FileName         | - имя файла модели-источника для проекции,    |
        +------------------------------------------------------------------+
        | ProjectionName   | - ориентация модели,                          |
        +------------------------------------------------------------------+
        | ProjectionsTypes | - массив SAFEARRAY VT | VT_I4 типов проекций, |
        +------------------------------------------------------------------+
        | X, Y             | - точка привязки группы видов,                |
        +------------------------------------------------------------------+
        | Scale            | - масштаб,                                    |
        +------------------------------------------------------------------+
        | DX, DY           | - расстояние между видами.                    |
        +------------------------------------------------------------------+

        ### Возвращаемое значение

        +--------------------------+
        | TRUE | - в случае удачи. |
        +--------------------------+
        """
        ...

    def View(self, Index) -> IView:  # iviews_view.html
        """
        ## View - Вид, заданный по имени, ссылке или по индексу

        Тип данных: указатель на интерфейс вида IView

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        View = iObject.View (Index)     # Получить свойство (* )
        View = iObject.GetView (Index)  # Получить свойство (**)
        ```

        ### Входные параметры

        +-----------------------------------------------------------------------------+
        | index | - индекс вида в коллекции, ссылка на вид или имя вида; тип VARIANT. |
        +-----------------------------------------------------------------------------+
        """
        ...

    def ViewByNumber(self, Numb) -> IView:  # iviews_viewbynumber.html
        """
        ## ViewByNumber - Вид, заданный по номеру

        Тип данных: указатель на интерфейс вида IView

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        View = iObject.ViewByNumber (number)     # Получить свойство (* )
        View = iObject.GetViewByNumber (number)  # Получить свойство (**)
        ```

        ### Входные параметры

        +----------------------------------+
        | number | - номер вида; тип long. |
        +----------------------------------+
        """
        ...




class IDrawingObject(IKompasAPIObject):  # idrawingobject.html
    """
    ## Интерфейс IDrawingObject

    Базовый интерфейс для всех графических объектов.

    1. Данный интерфейс является базовым для графических объектов.

    2. Интерфейс можно получить из коллекции графических объектов IDrawingObjects с помощью свойства IDrawingObjects::Item.

    3. Имея интерфейс IDrawingObject, можно подписаться на события ksDrawingObjectNotify.

    4. Данный интерфейс может быть получен с помощью метода IUnknown QueryInterface от интерфейса конкретного объекта, например, от интерфейса вида IView, слоя ILayer.

    5. Посредством вызова метода IUnknown::QueryInterface (const GUID far& IID, void** pif) у данного интерфейса можно получить дополнительный интерфейс для графических объектов IDrawingObject1.
    """

    @property
    def DrawingObjectParamType(self) -> int:  # idrawingobject_drawingobjectparamtype.html
        """
        ## DrawingObjectParamType - Тип для получения и изменения параметров объекта

        Тип данных: ksDrawingObjectParamTypeEnum.

        Свойство позволяет установить и получить тип используемой матрицы для пересчета параметров объектов. От матрицы зависят возвращаемые значения координат, углов и скалярных величин размеров, например, радиус, высота, длина.

        ### Синтаксис Automation

        ```
        type = iObject.DrawingObjectParamType       # Получить свойство(* )
        iObject.DrawingObjectParamType = type       # Установить свойство (* )
        type = iObject.GetDrawingObjectParamType()  # Получить свойство (**)
        iObject.SetDrawingObjectParamType (type)    # Установить свойство (**)
        ```
        """
        ...
    @DrawingObjectParamType.setter
    def DrawingObjectParamType(self, value: int): ...

    @property
    def DrawingObjectType(self) -> int:  # idrawingobject_drawingobjecttype.html
        """
        ## DrawingObjectType - Тип графического объекта

        Тип данных: DrawingObjectTypeEnum.

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        DrawingObjectType = iObject.DrawingObjectType       # Получить свойство(* )
        DrawingObjectType = iObject.GetDrawingObjectType()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def LayerNumber(self) -> int:  # idrawingobject_layernumber.html
        """
        ## LayerNumber - Номер слоя, на котором расположен объект. Для вида - номер активного слоя

        Тип данных: long

        Позволяет получить и установить номер слоя.

        ### Синтаксис Automation

        ```
        LayerNumber = iObject.LayerNumber       # Получить свойство(* )
        iObject.LayerNumber = LayerNumber       # Установить свойство (* )
        LayerNumber = iObject.GetLayerNumber()  # Получить свойство (**)
        iObject.SetLayerNumber (LayerNumber)    # Установить свойство (**)
        ```
        """
        ...
    @LayerNumber.setter
    def LayerNumber(self, value: int): ...

    @property
    def Temp(self) -> bool:  # idrawingobject_temp.html
        """
        ## Temp - Признак временности объекта

        Тип данных: BOOL

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        Temp = iObject.Temp       # Получить свойство(* )
        Temp = iObject.GetTemp()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def Valid(self) -> bool:  # idrawingobject_valid.html
        """
        ## Valid - Признак невырожденности объекта

        Тип данных: BOOL

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        Valid = iObject.Valid       # Получить свойство(* )
        Valid = iObject.GetValid()  # Получить свойство (**)
        ```
        """
        ...

    def Delete(self) -> bool:  # idrawingobject_delete.html
        """
        ## Delete - Удалить объект

        После успешного выполнения метода объект будет удален из модели. Свойство Valid для объекта будет возвращать FALSE. Все методы интерфейса возвращают ошибку (FALSE).

        ### Синтаксис Automation

        ```
        BOOL Delete();
        ```

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def Update(self) -> bool:  # idrawingobject_update.html
        """
        ## Update - Обновить данные объекта

        1. Метод Update необходимо вызвать для объекта, свойства которого были изменены (после вызова Set- cвойств или методов интерфейса), чтобы эти изменения вступили в силу.

        2. Метод Update вернёт TRUE в случае успеха и FALSE в случае неудачи.

        3. Вызов Update сработает как при изменении одного свойства, так и при изменении группы свойств объекта.

        ### Синтаксис Automation

        ```
        BOOL Update();
        ```

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...




class ILayer(IDrawingObject):  # ilayer.html
    """
    ## Интерфейс ILayer

    Интерфейс слоя графического документа.

    В графическом документе в каждом виде всегда присутствует хотя бы один слой, который создается автоматически при создании документа и нового вида.

    Данный интерфейс можно получить через коллекцию слоев ILayers с помощью свойства ILayers::Layer, ILayers::LayerByNumber, метода ILayers::Add, либо с помощью свойства IDrawingObjects::Item с последующим приведением к данному интерфейсу с помощью метода IUnknown QueryInterface.
    """

    @property
    def Background(self) -> bool:  # ilayer_background.html
        """
        ## Background - Состояние слоя - фоновый

        Тип данных: BOOL

        1. Позволяет получить и установить свойство слоя - фоновый.

        2. Свойство вступает в силу после вызова метода IDrawingObject::Update.

        ### Синтаксис Automation

        ```
        Background = iObject.Background       # Получить свойство (* )
        iObject.Background = Background       # Установить свойство (* )
        Background = iObject.GetBackground()  # Получить свойство (**)
        iObject.SetBackground (Background)    # Установить свойство (**)
        ```
        """
        ...
    @Background.setter
    def Background(self, value: bool): ...

    @property
    def Color(self) -> int:  # ilayer_color.html
        """
        ## Color - Цвет слоя

        Тип данных: long

        1. Позволяет получить и установить цвет слоя.

        2. Свойство вступает в силу после вызова метода IDrawingObject::Update.

        ### Синтаксис Automation

        ```
        Color = iObject.Color       # Получить свойство (* )
        iObject.Color = Color       # Установить свойство (* )
        Color = iObject.GetColor()  # Получить свойство (**)
        iObject.SetColor (Color)    # Установить свойство (**)
        ```
        """
        ...
    @Color.setter
    def Color(self, value: int): ...

    @property
    def Comment(self) -> str:  # ilayer_comment.html
        """
        ## Comment - Примечание

        Тип данных: BSTR

        1. Позволяет получить и установить примечание для слоя.

        2. Свойство вступает в силу после вызова метода IDrawingObject::Update.

        ### Синтаксис Automation

        ```
        Comment = iObject.Comment       # Получить свойство (* )
        iObject.Comment = Comment       # Установить свойство (* )
        Comment = iObject.GetComment()  # Получить свойство (**)
        iObject.SetComment (Comment)    # Установить свойство (**)
        ```
        """
        ...
    @Comment.setter
    def Comment(self, value: str): ...

    @property
    def Current(self) -> bool:  # ilayer_current.html
        """
        ## Current - Состояние слоя - текущий

        Тип данных: BOOL

        1. Позволяет получить и установить состояние слоя - текущий.

        2. Установить свойство текущий можно только равным TRUE. Новый слой всегда создается текущим.

        3. Свойство вступает в силу после вызова метода IDrawingObject::Update.

        ### Синтаксис Automation

        ```
        Current = iObject.Current       # Получить свойство (* )
        iObject.Current = Current       # Установить свойство (* )
        Current = iObject.GetCurrent()  # Получить свойство (**)
        iObject.SetCurrent (Current)    # Установить свойство (**)
        ```
        """
        ...
    @Current.setter
    def Current(self, value: bool): ...

    @property
    def Name(self) -> str:  # ilayer_name.html
        """
        ## Name - Имя слоя

        Тип данных: BSTR (строка)

        1. Позволяет получить и установить имя слоя.

        2. Свойство вступает в силу после вызова метода IDrawingObject::Update.

        ### Синтаксис Automation

        ```
        Name = iObject.Name       # Получить свойство (* )
        iObject.Name = Name       # Установить свойство (* )
        Name = iObject.GetName()  # Получить свойство (**)
        iObject.SetName (Name)    # Установить свойство (**)
        ```
        """
        ...
    @Name.setter
    def Name(self, value: str): ...

    @property
    def ObjectCount(self) -> int:  # ilayer_objectcount.html
        """
        ## ObjectCount - Количество объектов в слое

        Тип данных: long

        1. Позволяет получить количество объектов в слое.

        2. Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        ObjectCount = iObject.ObjectCount       # Получить свойство (* )
        ObjectCount = iObject.GetObjectCount()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def Printable(self) -> bool:  # ilayer_printable.html
        """
        ## Printable - Признак разрешения/запрещения печати слоя

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        Printable = Object.Printable       # Получить свойство (* )
        Object.Printable = Printable       # Установить свойство (* )
        Printable = Object.GetPrintable()  # Получить свойство (**)
        Object.SetPrintable (Printable)    # Установить свойство (**)
        ```
        """
        ...
    @Printable.setter
    def Printable(self, value: bool): ...

    @property
    def Visible(self) -> bool:  # ilayer_visible.html
        """
        ## Visible - Состояние слоя - видимый или погашенный

        Тип данных: BOOL

        1. Позволяет получить и установить свойство слоя - видимый или погашенный.

        2. Свойство вступает в силу после вызова метода IDrawingObject::Update.

        ### Синтаксис Automation

        ```
        Visible = iObject.Visible       # Получить свойство (* )
        iObject.Visible = Visible       # Установить свойство (* )
        Visible = iObject.GetVisible()  # Получить свойство (**)
        iObject.SetVisible (Visible)    # Установить свойство (**)
        ```
        """
        ...
    @Visible.setter
    def Visible(self, value: bool): ...




class IView(IDrawingObject):  # iview.html
    """
    ## Интерфейс IView

    Интерфейс вида графического документа.

    Типы видов описаны в перечислении LtViewType.

    В графическом документе всегда присутствует системный вид, который формируется автоматически при создании документа, и может быть только один. В документе типа Фрагмент может быть только один вид - системный.

    Посредством вызова метода IUnknown::QueryInterface (const GUID far& iid, void** pif) у данного интерфейса можно получить дополнительные интерфейсы вида:

    •Интерфейс обозначения вида IViewDesignation.

    •Интерфейс контейнера объектов вида графического документа IDrawingContainer.

    •Интерфейс контейнера объектов СПДС IBuildingContainer.

    •Интерфейс контейнера условных обозначений ISymbols2DContainer.

    •Интерфейс параметров разрыва вида IBreakViewParam. Этот интерфейс может быть получен для видов всех типов, кроме системного.

    •Интерфейс параметров разреза вида ICutViewParam. Этот интерфейс может быть получен для видов всех типов, кроме системного.

    •Интерфейс для получения коллекции ЛСК и признака редактирования макроэлемента IView1.

    Данный интерфейс можно получить следующими способами:

    •через коллекцию видов IViews,

    •с помощью свойства IViews::View, IViews::ViewByNumber или метода IViews::Add, либо с помощью свойства IDrawingObjects::Item с последующим приведением к данному интерфейсу с помощью метода IUnknown QueryInterface,

    •от интерфейса группы слоев ILayerGroup с помощью свойства ILayerGroup::OwnerView,
    """

    @property
    def Angle(self) -> float:  # iview_angle.html
        """
        ## Angle - Угол поворота вида

        Тип данных: double

        1. Позволяет получить и установить угол поворота вида.

        2. Свойство вступает в силу после вызова метода IDrawingObject::Update.

        ### Синтаксис Automation

        ```
        Angle = iObject.Angle     # Получить свойство (* )
        iObject.Angle = Angle     # Установить свойство (* )
        iObject.Angle = Angle     # Получить свойство (**)
        iObject.SetAngle (Angle)  # Установить свойство (**)
        ```
        """
        ...
    @Angle.setter
    def Angle(self, value: float): ...

    @property
    def Background(self) -> bool:  # iview_background.html
        """
        ## Background - Состояние вида - фоновый

        Тип данных: BOOL

        1. Позволяет получить и установить свойство слоя - фоновый.

        2. Свойство вступает в силу после вызова метода IDrawingObject::Update.

        ### Синтаксис Automation

        ```
        Background = iObject.Background       # Получить свойство (* )
        iObject.Background = Background       # Установить свойство (* )
        Background = iObject.GetBackground()  # Получить свойство (**)
        iObject.SetBackground (Background)    # Установить свойство (**)
        ```
        """
        ...
    @Background.setter
    def Background(self, value: bool): ...

    @property
    def Color(self) -> int:  # iview_color.html
        """
        ## Color - Цвет вида

        Тип данных: long

        1. Позволяет получить и установить цвет вида.

        2. Свойство вступает в силу после вызова метода IDrawingObject::Update.

        ### Синтаксис Automation

        ```
        Color = iObject.Color       # Получить свойство (* )
        iObject.Color = Color       # Установить свойство (* )
        Color = iObject.GetColor()  # Получить свойство (**)
        iObject.SetColor( Color )   # Установить свойство (**)
        ```
        """
        ...
    @Color.setter
    def Color(self, value: int): ...

    @property
    def Comment(self) -> str:  # iview_comment.html
        """
        ## Comment - Примечание

        Тип данных: BSTR

        1. Позволяет получить и установить примечание.

        2. Свойство вступает в силу после вызова метода IDrawingObject::Update.

        ### Синтаксис Automation

        ```
        Comment = iObject.Comment       # Получить свойство (* )
        iObject.Comment = Comment       # Установить свойство (* )
        Comment = iObject.GetComment()  # Получить свойство (**)
        iObject.SetComment (Comment)    # Установить свойство (**)
        ```
        """
        ...
    @Comment.setter
    def Comment(self, value: str): ...

    @property
    def Current(self) -> bool:  # iview_current.html
        """
        ## Current - Состояние вида - текущий

        Тип данных: BOOL

        1. Позволяет узнать, является ли вид текущим или сделать его текущим.

        2. Установить свойство "текущий" можно только равным TRUE.

        3. Новый вид всегда создается текущим.

        4. Свойство вступает в силу после вызова метода IDrawingObject::Update.

        ### Синтаксис Automation

        ```
        Current = iObject.Current       # Получить свойство (* )
        iObject.Current = Current       # Установить свойство (* )
        Current = iObject.GetCurrent()  # Получить свойство (**)
        iObject.SetCurrent (Current)    # Установить свойство (**)
        ```
        """
        ...
    @Current.setter
    def Current(self, value: bool): ...

    @property
    def Layers(self) -> ILayers:  # iview_layers.html
        """
        ## Layers - Коллекция слоев

        Тип данных: указатель на интерфейс ILayers коллекции слоев.

        1. Позволяет получить коллекцию слоев вида.

        2. Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        Layers = iObject.Layers       # Получить свойство (* )
        Layers = iObject.GetLayers()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def Name(self) -> str:  # iview_name.html
        """
        ## Name - Имя вида

        Тип данных: BSTR (строка)

        1. Для нового вида, полученного методом IViews::Add, имя должно быть обязательно установлено до вызова метода IDrawingObject::Update.

        2. Свойство вступает в силу после вызова метода IDrawingObject::Update.

        ### Синтаксис Automation

        ```
        Name = iObject.Name       # Получить свойство (* )
        iObject.Name = Name       # Установить свойство (* )
        Name = iObject.GetName()  # Получить свойство (**)
        iObject.SetName (Name)    # Установить свойство (**)
        ```
        """
        ...
    @Name.setter
    def Name(self, value: str): ...

    @property
    def Number(self) -> int:  # iview_number.html
        """
        ## Number - Номер вида

        Тип данных: long

        Номер вида "0" зарезервирован для системного вида. Номер должен быть уникальным в документе. Для нового вида, полученного методом IViews::Add, можно задать номер "-1" или не задавать номер. В этом случае при вызове метода IDrawingObject::Update для данного вида номер будет задан автоматически.

        ### Синтаксис Automation

        ```
        Number = iObject.Number       # Получить свойство (* )
        iObject.Number = Number       # Установить свойство (* )
        Number = iObject.GetNumber()  # Получить свойство (**)
        iObject.SetNumber (Number)    # Установить свойство (**)
        ```
        """
        ...
    @Number.setter
    def Number(self, value: int): ...

    @property
    def ObjectCount(self) -> int:  # iview_objectcount.html
        """
        ## ObjectCount - Количество объектов в виде

        Тип данных: long

        1. Позволяет получить количество объектов в виде.

        2. Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        ObjectCount = iObject.ObjectCount       # Получить свойство (* )
        ObjectCount = iObject.GetObjectCount()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def Scale(self) -> float:  # iview_scale.html
        """
        ## Scale - Масштаб вида

        Тип данных: double

        1. Позволяет получить и установить масштаб вида.

        2. Свойство вступает в силу после вызова метода IDrawingObject::Update.

        ### Синтаксис Automation

        ```
        Scale = iObject.Scale       # Получить свойство (* )
        iObject.Scale = Scale       # Установить свойство (* )
        Scale = iObject.GetScale()  # Получить свойство (**)
        iObject.SetScale (Scale)    # Установить свойство (**)
        ```
        """
        ...
    @Scale.setter
    def Scale(self, value: float): ...

    @property
    def Variables(self):  # iview_variables.html
        ...

    @property
    def VariablesCount(self) -> int:  # iview_variablescount.html
        """
        ## VariablesCount - Получить количество параметрических переменных

        Тип данных: long

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        VariablesCount = Object.VariablesCount       # Получить свойство (* )
        VariablesCount = Object.GetVariablesCount()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def ViewType(self) -> int:  # iview_viewtype.html
        """
        ## ViewType - Тип вида

        Тип данных: Тип вида из перечисления LtViewType

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        ViewType = iObject.ViewType       # Получить свойство (* )
        ViewType = iObject.GetViewType()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def Visible(self) -> bool:  # iview_visible.html
        """
        ## Visible - Состояние вида - видимый или погашенный

        Тип данных: BOOL

        1. Позволяет получить и установить свойство вида - видимый или погашенный.

        2. Свойство вступает в силу после вызова метода IDrawingObject::Update.

        ### Синтаксис Automation

        ```
        Visible = iObject.Visible       # Получить свойство (* )
        iObject.Visible = Visible       # Установить свойство (* )
        Visible = iObject.GetVisible()  # Получить свойство (**)
        iObject.SetVisible (Visible)    # Установить свойство (**)
        ```
        """
        ...
    @Visible.setter
    def Visible(self, value: bool): ...

    @property
    def X(self) -> float:  # iview_x.html
        """
        ## X - Координата привязки по X

        Тип данных: double

        1. Позволяет получить и установить координату привязки вида по оси X.

        2. Свойство вступает в силу после вызова метода IDrawingObject::Update.

        ### Синтаксис Automation

        ```
        X = iObject.X       # Получить свойство (* )
        iObject.X = X       # Установить свойство (* )
        X = iObject.GetX()  # Получить свойство (**)
        iObject.SetX (X)    # Установить свойство (**)
        ```
        """
        ...
    @X.setter
    def X(self, value: float): ...

    @property
    def Y(self) -> float:  # iview_y.html
        """
        ## Y - Координата привязки по Y

        Тип данных: double

        1. Позволяет получить и установить координату привязки вида по оси Y.

        2. Свойство вступает в силу после вызова метода IDrawingObject::Update.

        ### Синтаксис Automation

        ```
        Y = iObject.Y       # Получить свойство (* )
        iObject.Y = Y       # Установить свойство (* )
        Y = iObject.GetY()  # Получить свойство (**)
        iObject.SetY (Y)    # Установить свойство (**)
        ```
        """
        ...
    @Y.setter
    def Y(self, value: float): ...

    def Variable(self, Index) -> IVariable7:  # iview_variable.html
        """
        ## Variable - Получить параметрическую переменную по имени, индексу или указателю на размер

        Тип данных: указатель на интерфейс параметрической переменной IVariable7

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        Variable = Object.Variable (VARIANT Index)     # Получить свойство (* )
        Variable = Object.GetVariable (VARIANT Index)  # Получить свойство (**)
        ```

        ### Входные параметры

        +---------------------------------------------------------------------------+
        | Index | - индекс, имя или указатель на размер параметрической переменной. |
        +---------------------------------------------------------------------------+

        Свойство позволяет получать интерфейс параметрической переменной.
        """
        ...




class IAssociationView(IView):  # iassociationview.html
    """
    ## Интерфейс IAssociationView

    Интерфейс ассоциативного вида графического документа.

    Типы видов описаны в перечислении LtViewType.

    1. Данный интерфейс можно получить следующими способами:

    •через коллекцию видов IViews,

    •с помощью метода IUnknown QueryInterface от интерфейса IView.

    2. Посредством вызова метода IUnknown::QueryInterface (const GUID far& iid, void** pif) интерфейс позволяет получить дополнительные интерфейсы:

    •IHatchParam

    •IAssociationViewElements.
    """

    @property
    def BaseObject(self) -> IDrawingObject:  # iassociationview_baseobject.html
        """
        ## BaseObject - Опорный объект

        Тип данных: указатель на интерфейс IDrawingObject

        В зависимости от типа ассоциативного вида нужно указать вспомогательный опорный объект.

        Опорный объект требуется при создании следующих видов:

        •Вид по стрелке - требуется объект Стрелка взгляда,

        •Выносной вид - требуется объект Обозначение выносного элемента,

        •Вид разрез/сечение - требуется объект Линия разреза.

        ### Синтаксис Automation

        ```
        BaseObject = Object.BaseObject       # Получить свойство (* )
        Object.BaseObject = BaseObject       # Установить свойство (* )
        BaseObject = Object.GetBaseObject()  # Получить свойство (**)
        Object.SetBaseObject( BaseObject )   # Установить свойство (**)
        ```
        """
        ...
    @BaseObject.setter
    def BaseObject(self, value: IDrawingObject): ...

    @property
    def BaseView(self) -> IView:  # iassociationview_baseview.html
        """
        ## BaseView - Опорный вид

        Тип данных: указатель на интерфейс вида IView

        Свойство используется при создании проекционного вида.

        Позволяет установить вид, относительно которого выполняется производная проекция.

        Базовый вид должен быть ассоциативным.

        ### Синтаксис Automation

        ```
        BaseView = Object.BaseView       # Получить свойство (* )
        Object.BaseView = BaseView       # Установить свойство (* )
        BaseView = Object.GetBaseView()  # Получить свойство (**)
        Object.SetBaseView( BaseView )   # Установить свойство (**)
        ```
        """
        ...
    @BaseView.setter
    def BaseView(self, value: IView): ...

    @property
    def BendLinesStyle(self) -> bool:  # iassociationview_bendlinesstyle.html
        """
        ## BendLinesStyle - Cтиль линий сгиба

        Тип данных: BOOL

        Позволяет установить системный (ksCurveStyleEnum) или пользовательский стиль линий для отображения линий сгиба.

        ### Синтаксис Automation

        ```
        BendLinesStyle = Object.BendLinesStyle       # Получить свойство (* )
        Object.BendLinesStyle = BendLinesStyle       # Установить свойство (* )
        BendLinesStyle = Object.GetBendLinesStyle()  # Получить свойство (**)
        Object.SetBendLinesStyle( BendLinesStyle )   # Установить свойство (**)
        ```
        """
        ...
    @BendLinesStyle.setter
    def BendLinesStyle(self, value: bool): ...

    @property
    def BendLinesVisible(self) -> bool:  # iassociationview_bendlinesvisible.html
        """
        ## BendLinesVisible - Показывать линии сгиба

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        BendLinesVisible = Object.BendLinesVisible       # Получить свойство (* )
        Object.BendLinesVisible = BendLinesVisible       # Установить свойство (* )
        BendLinesVisible = Object.GetBendLinesVisible()  # Получить свойство (**)
        Object.SetBendLinesVisible( BendLinesVisible )   # Установить свойство (**)
        ```
        """
        ...
    @BendLinesVisible.setter
    def BendLinesVisible(self, value: bool): ...

    @property
    def BreakLinesStyle(self) -> int:  # iassociationview_breaklinesstyle.html
        """
        ## BreakLinesStyle - Cтиль линий перехода

        Тип данных: long

        Позволяет установить системный (ksCurveStyleEnum) или пользовательский стиль линий для отображения линий перехода.

        ### Синтаксис Automation

        ```
        BreakLinesStyle = Object.BreakLinesStyle       # Получить свойство (* )
        Object.BreakLinesStyle = BreakLinesStyle       # Установить свойство (* )
        BreakLinesStyle = Object.GetBreakLinesStyle()  # Получить свойство (**)
        Object.SetBreakLinesStyle( BreakLinesStyle )   # Установить свойство (**)
        ```
        """
        ...
    @BreakLinesStyle.setter
    def BreakLinesStyle(self, value: int): ...

    @property
    def BreakLinesVisible(self) -> int:  # iassociationview_breaklinesvisible.html
        """
        ## BreakLinesVisible - Показывать линии перехода

        Тип данных: long

        ### Синтаксис Automation

        ```
        BreakLinesVisible = Object.BreakLinesVisible        # Получить свойство (* )
        Object.BreakLinesVisible = BreakLinesVisible        # Установить свойство (* )
        BreakLinesVisible = Object.GetHiddenLinesVisible()  # Получить свойство (**)
        Object.SetBreakLinesVisible( BreakLinesVisible )    # Установить свойство (**)
        ```
        """
        ...
    @BreakLinesVisible.setter
    def BreakLinesVisible(self, value: int): ...

    @property
    def CenterLinesVisible(self) -> bool:  # iassociationview_centerlinesvisible.html
        """
        ## CenterLinesVisible - Показывать осевые линии

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        CenterLinesVisible = Object.CenterLinesVisible       # Получить свойство (* )
        Object.CenterLinesVisible = CenterLinesVisible       # Установить свойство (* )
        CenterLinesVisible = Object.GetCenterLinesVisible()  # Получить свойство (**)
        Object.SetCenterLinesVisible( CenterLinesVisible )   # Установить свойство (**)
        ```
        """
        ...
    @CenterLinesVisible.setter
    def CenterLinesVisible(self, value: bool): ...

    @property
    def ExplodedView(self) -> bool:  # iassociationview_explodedview.html
        """
        ## ExplodedView - Сборка в разнесенном виде

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        ExplodedView = Object.ExplodedView       # Получить свойство (* )
        Object.ExplodedView = ExplodedView       # Установить свойство (* )
        ExplodedView = Object.GetExplodedView()  # Получить свойство (**)
        Object.SetExplodedView( ExplodedView )   # Установить свойство (**)
        ```
        """
        ...
    @ExplodedView.setter
    def ExplodedView(self, value: bool): ...

    @property
    def HiddenLines(self) -> int:  # iassociationview_hiddenlines.html
        """
        ## HiddenLines - Передавать невидимые линии в вид

        Тип данных: long

        Невидимые линии хранятся в виде вне зависимости от того, включена их отрисовка или нет (IAssociationView::HiddenLinesVisible).

        Если отрисовка этих линий не требуется, отключите опцию Невидимые линии — это уменьшит размер файла чертежа.

        Рекомендуется отключать опцию Невидимые линии при построении ассоциативных видов сборок, содержащих более 1000 компонентов.

        ### Синтаксис Automation

        ```
        HiddenLines = Object.HiddenLines       # Получить свойство (* )
        Object.HiddenLines = HiddenLines       # Установить свойство (* )
        HiddenLines = Object.GetHiddenLines()  # Получить свойство (**)
        Object.SetHiddenLines( HiddenLines )   # Установить свойство (**)
        ```
        """
        ...
    @HiddenLines.setter
    def HiddenLines(self, value: int): ...

    @property
    def HiddenLinesStyle(self) -> int:  # iassociationview_hiddenlinesstyle.html
        """
        ## HiddenLinesStyle - Cтиль невидимых линий

        Тип данных: long

        Позволяет установить системный (ksCurveStyleEnum) или пользовательский стиль линий для отображения невидимых линий

        Для использования данного свойства требуется включение передачи невидимых линий (IAssociationView::HiddenLines) и включение отображения невидимых линий (IAssociationView::HiddenLinesVisible).

        ### Синтаксис Automation

        ```
        HiddenLinesStyle = Object.HiddenLinesStyle       # Получить свойство (* )
        Object.HiddenLinesStyle = HiddenLinesStyle       # Установить свойство (* )
        HiddenLinesStyle = Object.GetHiddenLinesStyle()  # Получить свойство (**)
        Object.SetHiddenLinesStyle( HiddenLinesStyle )   # Установить свойство (**)
        ```
        """
        ...
    @HiddenLinesStyle.setter
    def HiddenLinesStyle(self, value: int): ...

    @property
    def HiddenLinesVisible(self) -> int:  # iassociationview_hiddenlinesvisible.html
        """
        ## HiddenLinesVisible - Показывать невидимые линии

        Тип данных: long

        Для использования данного свойства требуется включение передачи невидимых линий (IAssociationView::HiddenLines).

        ### Синтаксис Automation

        ```
        HiddenLinesVisible = Object.HiddenLinesVisible       # Получить свойство (* )
        Object.HiddenLinesVisible = HiddenLinesVisible       # Установить свойство (* )
        HiddenLinesVisible = Object.GetHiddenLinesVisible()  # Получить свойство (**)
        Object.SetHiddenLinesVisible( HiddenLinesVisible )   # Установить свойство (**)
        ```
        """
        ...
    @HiddenLinesVisible.setter
    def HiddenLinesVisible(self, value: int): ...

    @property
    def Local(self) -> bool:  # iassociationview_local.html
        """
        ## Local - Местный вид

        Тип данных: BOOL

        Включает полное изображение вида или усеченное (местный вид).

        Требует предварительного создания местного вида с помощью команды IAssociationView::CreateLocalView

        ### Синтаксис Automation

        ```
        Local = Object.Local       # Получить свойство (* )
        Object.Local = Local       # Установить свойство (* )
        Local = Object.GetLocal()  # Получить свойство (**)
        Object.SetLocal( Local )   # Установить свойство (**)
        ```
        """
        ...
    @Local.setter
    def Local(self, value: bool): ...

    @property
    def ProjectionLink(self) -> bool:  # iassociationview_projectionlink.html
        """
        ## ProjectionLink - Проекционная связь

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        ProjectionLink = Object.ProjectionLink       # Получить свойство (* )
        Object.ProjectionLink = ProjectionLink       # Установить свойство (* )
        ProjectionLink = Object.GetProjectionLink()  # Получить свойство (**)
        Object.SetProjectionLink( ProjectionLink )   # Установить свойство (**)
        ```
        """
        ...
    @ProjectionLink.setter
    def ProjectionLink(self, value: bool): ...

    @property
    def ProjectionMatrix(self) -> typing.Any:  # iassociationview_projectionmatrix.html
        """
        ## ProjectionMatrix - Матрица ассоциативного вида

        Тип данных: VARIANT

        Матрица представляет собой массив SAFEARRAY double (VT_ARRAY | VT_R8)

        В массиве будут лежать 16 элементов, которые представляют собой матрицу размера 4х4.

        ### Синтаксис Automation

        ```
        ProjectionMatrix = Object.ProjectionMatrix       # Получить свойство (* )
        Object.ProjectionMatrix = ProjectionMatrix       # Установить свойство (* )
        ProjectionMatrix = Object.GetProjectionMatrix()  # Получить свойство (**)
        Object.SetProjectionMatrix( ProjectionMatrix )   # Установить свойство (**)
        ```
        """
        ...
    @ProjectionMatrix.setter
    def ProjectionMatrix(self, value: typing.Any): ...

    @property
    def ProjectionName(self) -> str:  # iassociationview_projectionname.html
        """
        ## ProjectionName - Имя проекции

        Тип данных: BSTR

        ### Синтаксис Automation

        ```
        ProjectionName = Object.ProjectionName       # Получить свойство (* )
        Object.ProjectionName = ProjectionName       # Установить свойство (* )
        ProjectionName = Object.GetProjectionName()  # Получить свойство (**)
        Object.SetProjectionName (ProjectionName)    # Установить свойство (**)
        ```
        """
        ...
    @ProjectionName.setter
    def ProjectionName(self, value: str): ...

    @property
    def SameHatch(self) -> bool:  # iassociationview_samehatch.html
        """
        ## SameHatch - Признак одинаковой штриховки всех деталей сборки

        Тип данных: BOOL

        Свойство используется только в виде разрез/сечение.

        ### Синтаксис Automation

        ```
        SameHatch = Object.SameHatch       # Получить свойство (* )
        Object.SameHatch = SameHatch       # Установить свойство (* )
        SameHatch = Object.GetSameHatch()  # Получить свойство (**)
        Object.SetSameHatch( SameHatch )   # Установить свойство (**)
        ```
        """
        ...
    @SameHatch.setter
    def SameHatch(self, value: bool): ...

    @property
    def Section(self) -> bool:  # iassociationview_section.html
        """
        ## Section - Признак «разрез/сечение»

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        Section = Object.Section       # Получить свойство (* )
        Object.Section = Section       # Установить свойство (* )
        Section = Object.GetSection()  # Получить свойство (**)
        Object.SetSection( Section )   # Установить свойство (**)
        ```
        """
        ...
    @Section.setter
    def Section(self, value: bool): ...

    @property
    def SourceFileName(self) -> str:  # iassociationview_sourcefilename.html
        """
        ## SourceFileName - Полное имя файла-источника

        Тип данных: BSTR

        ### Синтаксис Automation

        ```
        SourceFileName = Object.SourceFileName       # Получить свойство (* )
        Object.SourceFileName = SourceFileName       # Установить свойство (* )
        SourceFileName = Object.GetSourceFileName()  # Получить свойство (**)
        Object.SetSourceFileName (SourceFileName)    # Установить свойство (**)
        ```
        """
        ...
    @SourceFileName.setter
    def SourceFileName(self, value: str): ...

    @property
    def Unfold(self) -> bool:  # iassociationview_unfold.html
        """
        ## Unfold - Листовая деталь в разогнутом виде

        Тип данных:BOOL

        ### Синтаксис Automation

        ```
        Unfold = Object.Unfold       # Получить свойство (* )
        Object.Unfold = Unfold       # Установить свойство (* )
        Unfold = Object.GetUnfold()  # Получить свойство (**)
        Object.SetUnfold( Unfold )   # Установить свойство (**)
        ```
        """
        ...
    @Unfold.setter
    def Unfold(self, value: bool): ...

    @property
    def UseOcclusion(self) -> bool:  # iassociationview_useocclusion.html
        """
        ## UseOcclusion - Черновое проецирование

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        UseOcclusion = Object.UseOcclusion       # Получить свойство (* )
        Object.UseOcclusion = UseOcclusion       # Установить свойство (* )
        UseOcclusion = Object.GetUseOcclusion()  # Получить свойство (**)
        Object.SetUseOcclusion( UseOcclusion )   # Установить свойство (**)
        ```
        """
        ...
    @UseOcclusion.setter
    def UseOcclusion(self, value: bool): ...

    @property
    def VisibleLinesStyle(self) -> int:  # iassociationview_visiblelinesstyle.html
        """
        ## VisibleLinesStyle - Cтиль видимых линий

        Тип данных: long

        ### Синтаксис Automation

        ```
        VisibleLinesStyle = Object.VisibleLinesStyle       # Получить свойство (* )
        Object.VisibleLinesStyle = VisibleLinesStyle       # Установить свойство (* )
        VisibleLinesStyle = Object.GetVisibleLinesStyle()  # Получить свойство (**)
        Object.SetVisibleLinesStyle( VisibleLinesStyle )   # Установить свойство (**)
        ```
        """
        ...
    @VisibleLinesStyle.setter
    def VisibleLinesStyle(self, value: int): ...

    @property
    def DimensionLayoutScaling(self) -> typing.NoReturn:  # iassociationview_dimensionlayoutscaling.html
        """
        ## DimensionLayoutScaling - Масштабировать аннотационные объекты вида

        Тип данных: BOOL

        1. Свойство доступно только для изменения.

        2. Значение свойства не сохраняется и используется при создании и изменении параметров вида.

        ### Синтаксис Automation

        ```
        Object.DimensionLayoutScaling = DimensionLayoutScaling      # Установить свойство (* )
        Object.SetDimensionLayoutScaling( DimensionLayoutScaling )  # Установить свойство (**)
        ```
        """
        ...
    @DimensionLayoutScaling.setter
    def DimensionLayoutScaling(self, value: bool): ...

    def AssociationObjects(self, ModelObject) -> typing.Any:  # iassociationview_associationobjects.html
        """
        ## AssociationObjects - Получить массив SAFEARRAY ассоциативных объектов по 3D объекту

        Тип данных: VARIANT

        Свойство доступно только для чтения.

        Версия: Компас v20

        ### Синтаксис Automation

        ```
        AssociationObjects = Object.AssociationObjects( ModelObject )     # Получить свойство (* )
        AssociationObjects = Object.GetAssociationObjects( ModelObject )  # Получить свойство (**)
        ```
        """
        ...

    def CreateLocalView(self, Contour) -> bool:  # iassociationview_createlocalview.html
        """
        ## CreateLocalView - Местный вид

        Границей местного вида может являться любой замкнутый контур (окружность, эллипс, замкнутая кривая Безье и т.п.).

        Если в опорном виде нет замкнутого контура, создайте его

        Необходимым условием корректного построения местного вида является расположение ограничивающего его контура именно в опорном виде.

        Переключение между полным и усеченным отображением вида осуществляется с помощью свойства IAssociationView::Local.

        ### Синтаксис Automation

        ```
        BOOL CreateLocalView( LPDISPATCH Contour );
        ```

        ### Входные параметры

        +--------------------------------------------+
        | Contour | - указатель на замкнутую кривую. |
        +--------------------------------------------+

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def FindFace(self, X, Y, Document3D) -> IModelObject:  # iassociationview_findface.html
        """
        ## FindFace - Найти грань по точке в чертеже

        ### Синтаксис Automation

        ```
        LPDISPATCH FindFace( double X, double Y, IKompasDocument3D * Document3D );
        ```

        ### Входные параметры

        +--------------------------------------------------------------------+
        | X, Y       | - координаты точки,                                   |
        +--------------------------------------------------------------------+
        | Document3D | - указатель на интерфейс документа IKompasDocument3D. |
        +--------------------------------------------------------------------+

        Версия: Компас v20

        ### Возвращаемое значение

        - интерфейс объекта модели IModelObject.
        """
        ...

    def FindFaceEdges(self, X, Y) -> list[IDispatch]:  # iassociationview_findfaceedges.html
        """
        ## FindFaceEdges - Получить массив SAFEARRAY ассоциативных объектов ребер грани по точке в чертеже

        ### Синтаксис Automation

        ```
        VARIANT FindFaceEdges( double X, double Y );
        ```

        ### Входные параметры

        +----------------------------+
        | X, Y | - координаты точки. |
        +----------------------------+

        Версия: Компас v20

        ### Возвращаемое значение

        - объект (VT_DISPATCH) или Список объектов VT_ARRAY | VT_DISPATCH, VT_EMPTY - если объекты не найдены.
        """
        ...

    def GetProjectionObjects(self, Document3D) -> list[IDispatch]:  # iassociationview_getprojectionobjects.html
        """
        ## GetProjectionObjects - Получить массив SAFEARRAY проецируемых объектов. Вставка или тела

        ### Синтаксис Automation

        ```
        VARIANT GetProjectionObjects( IKompasDocument3D * Document3D );
        ```

        ### Входные параметры

        +--------------------------------------------+
        | Document3D | - документ IKompasDocument3D. |
        +--------------------------------------------+

        ### Возвращаемое значение

        - Список объектов в виде SAFEARRAY'я DISPATCH - VT_ARRAY | VT_DISPATCH.

        Версия КОМПАС v22
        """
        ...

    def GetProjectionPoint(self, X, Y, Z) -> bool:  # iassociationview_getprojectionpoint.html
        """
        ## GetProjectionPoint - Получить координаты точки на виде по 3D координатам модели

        ### Синтаксис Automation

        ```
        BOOL GetProjectionPoint( double X, double Y, double Z, double * Xv, double * Yv );
        ```

        ### Входные параметры

        +----------------------------------------------+
        | X, Y, Z | - координаты точки в 3D документе. |
        +----------------------------------------------+

        ### Выходные параметры

        +---------------------------------------+
        | Xv, Yv | - координаты проекции точки. |
        +---------------------------------------+

        Версия КОМПАС v22

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def IsAssociationObjectsVisible(self, ModelObject) -> bool:  # iassociationview_isassociationobjectsvisible.html
        """
        ## IsAssociationObjectsVisible - Получить признак видимости ассоциативных объектов по 3D объекту

        Тип данных: BOOL

        Свойство доступно только для чтения.

        Версия: Компас v20

        ### Синтаксис Automation

        ```
        IsAssociationObjectsVisible = Object.IsAssociationObjectsVisible( ModelObject )     # Получить свойство (* )
        IsAssociationObjectsVisible = Object.GetIsAssociationObjectsVisible( ModelObject )  # Получить свойство (**)
        ```
        """
        ...

    def IsViewProjectionPointInBreakView(self, X, Y, Z) -> bool:  # iassociationview_isviewprojectionpointinbreakview.html
        """
        ## IsViewProjectionPointInBreakView - Видна ли проекция точки на виде с разрывами

        ### Синтаксис Automation

        ```
        BOOL IsViewProjectionPointInBreakView( double X, double Y, double Z );
        ```

        ### Входные параметры

        +--------------------------------------------+
        | Contour | - указатель на замкнутую кривую. |
        +--------------------------------------------+

        +----------------------------------------------+
        | X, Y, Z | - координаты точки в 3D документе. |
        +----------------------------------------------+

        Версия КОМПАС v22

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def RestoreStyles(self) -> bool:  # iassociationview_restorestyles.html
        """
        ## RestoreStyles - Восстановить стили объектов ассоциативного вида

        ### Синтаксис Automation

        ```
        BOOL RestoreStyles();
        ```

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+

        Версия КОМПАС v22
        """
        ...

    def SetProjectionObjects(self, Objects) -> bool:  # iassociationview_setprojectionobjects.html
        """
        ## SetProjectionObjects - Установить массив SAFEARRAY проецируемых объектов. Вставка или тела

        ### Синтаксис Automation

        ```
        BOOL SetProjectionObjects( VARIANT Objects );
        ```

        ### Входные параметры

        +-----------------------------------------------------+
        | Objects | - cписок объектов VT_ARRAY | VT_DISPATCH. |
        +-----------------------------------------------------+

        Версия КОМПАС v22

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...




class IAssociationViewElements(IDispatch):  # iassociationviewelements.html
    """
    ## Интерфейс IAssociationViewElements

    Интерфейс - Объекты и элементы для передачи в ассоциативный вид.

    Интерфейс позволяет задать, какие объекты и элементы надо передавать в ассоциативный вид.

    Интерфейс является дополнительным к интерфейсу ассоциативного вида IAssociationView.
    """

    @property
    def CreateAxis(self) -> bool:  # iassociationviewelements_createaxis.html
        """
        ## CreateAxis - Создавать оси

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        CreateAxis = Object.CreateAxis       # Получить свойство (* )
        Object.CreateAxis = CreateAxis       # Установить свойство (* )
        CreateAxis = Object.GetCreateAxis()  # Получить свойство (**)
        Object.SetCreateAxis( CreateAxis )   # Установить свойство (**)
        ```
        """
        ...
    @CreateAxis.setter
    def CreateAxis(self, value: bool): ...

    @property
    def CreateCentresMarkers(self) -> bool:  # iassociationviewelements_createcentresmarkers.html
        """
        ## CreateCentresMarkers - Создавать обозначения центров

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        CreateCentresMarkers = Object.CreateCentresMarkers       # Получить свойство (* )
        Object.CreateCentresMarkers = CreateCentresMarkers       # Установить свойство (* )
        CreateCentresMarkers = Object.GetCreateCentresMarkers()  # Получить свойство (**)
        Object.SetCreateCentresMarkers( CreateCentresMarkers )   # Установить свойство (**)
        ```
        """
        ...
    @CreateCentresMarkers.setter
    def CreateCentresMarkers(self, value: bool): ...

    @property
    def CreateCircularCentres(self) -> bool:  # iassociationviewelements_createcircularcentres.html
        """
        ## CreateCircularCentres - Создавать круговые сетки центров

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        CreateCircularCentres = Object.CreateCircularCentres       # Получить свойство (* )
        Object.CreateCircularCentres = CreateCircularCentres       # Установить свойство (* )
        CreateCircularCentres = Object.GetCreateCircularCentres()  # Получить свойство (**)
        Object.SetCreateCircularCentres( CreateCircularCentres )   # Установить свойство (**)
        ```
        """
        ...
    @CreateCircularCentres.setter
    def CreateCircularCentres(self, value: bool): ...

    @property
    def CreateLinearCentres(self) -> bool:  # iassociationviewelements_createlinearcentres.html
        """
        ## CreateLinearCentres - Создавать линейные сетки центров

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        CreateLinearCentres = Object.CreateLinearCentres       # Получить свойство (* )
        Object.CreateLinearCentres = CreateLinearCentres       # Установить свойство (* )
        CreateLinearCentres = Object.GetCreateLinearCentres()  # Получить свойство (**)
        Object.SetCreateLinearCentres( CreateLinearCentres )   # Установить свойство (**)
        ```
        """
        ...
    @CreateLinearCentres.setter
    def CreateLinearCentres(self, value: bool): ...

    @property
    def HiddenObjectsVisible(self) -> bool:  # iassociationviewelements_hiddenobjectsvisible.html
        """
        ## HiddenObjectsVisible - Отображать скрытые объекты

        Тип данных: BOOL

        Свойство позволяет устанавливать и получать признак отображения скрытых объектов.

        ### Синтаксис Automation

        ```
        HiddenObjectsVisible = Object.HiddenObjectsVisible       # Получить свойство (* )
        Object.HiddenObjectsVisible = HiddenObjectsVisible       # Установить свойство (* )
        HiddenObjectsVisible = Object.GetHiddenObjectsVisible()  # Получить свойство (**)
        Object.SetHiddenObjectsVisible( HiddenObjectsVisible )   # Установить свойство (**)
        ```
        """
        ...
    @HiddenObjectsVisible.setter
    def HiddenObjectsVisible(self, value: bool): ...

    @property
    def ProjectAllDesignations(self) -> bool:  # iassociationviewelements_projectalldesignations.html
        """
        ## ProjectAllDesignations - Передавать в вид все обозначения

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        ProjectAllDesignations = Object.ProjectAllDesignations       # Получить свойство (* )
        Object.ProjectAllDesignations = ProjectAllDesignations       # Установить свойство (* )
        ProjectAllDesignations = Object.GetProjectAllDesignations()  # Получить свойство (**)
        Object.SetProjectAllDesignations( ProjectAllDesignations )   # Установить свойство (**)
        ```
        """
        ...
    @ProjectAllDesignations.setter
    def ProjectAllDesignations(self, value: bool): ...

    @property
    def ProjectAllObjects(self) -> bool:  # iassociationviewelements_projectallobjects.html
        """
        ## ProjectAllObjects - Передавать в вид все объекты

        Тип данных: BOOL

        Свойство позволяет одновременно включить проецирование тел ProjectBodies, поверхностей ProjectSurfaces, кривых ProjectCurves и точек ProjectPoints.

        См. также IAssociationViewElements::ProjectAllDesignations

        ### Синтаксис Automation

        ```
        ProjectAllObjects = Object.ProjectAllObjects       # Получить свойство (* )
        Object.ProjectAllObjects = ProjectAllObjects       # Установить свойство (* )
        ProjectAllObjects = Object.GetProjectAllObjects()  # Получить свойство (**)
        Object.SetProjectAllObjects( ProjectAllObjects )   # Установить свойство (**)
        ```
        """
        ...
    @ProjectAllObjects.setter
    def ProjectAllObjects(self, value: bool): ...

    @property
    def ProjectAxis(self) -> bool:  # iassociationviewelements_projectaxis.html
        """
        ## ProjectAxis - Передавать в вид оси

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        ProjectAxis = Object.ProjectAxis       # Получить свойство (* )
        Object.ProjectAxis = ProjectAxis       # Установить свойство (* )
        ProjectAxis = Object.GetProjectAxis()  # Получить свойство (**)
        Object.SetProjectAxis( ProjectAxis )   # Установить свойство (**)
        ```
        """
        ...
    @ProjectAxis.setter
    def ProjectAxis(self, value: bool): ...

    @property
    def ProjectBases(self) -> bool:  # iassociationviewelements_projectbases.html
        """
        ## ProjectBases - Передавать в вид обозначения баз

        Тип данных: BOOL

        Свойство позволяет устанавливать и получать признак передачи в вид обозначений баз.

        ### Синтаксис Automation

        ```
        ProjectBases = Object.ProjectBases       # Получить свойство (* )
        Object.ProjectBases = ProjectBases       # Установить свойство (* )
        ProjectBases = Object.GetProjectBases()  # Получить свойство (**)
        Object.SetProjectBases( ProjectBases )   # Установить свойство (**)
        ```
        """
        ...
    @ProjectBases.setter
    def ProjectBases(self, value: bool): ...

    @property
    def ProjectBodies(self) -> bool:  # iassociationviewelements_projectbodies.html
        """
        ## ProjectBodies - Передавать в вид тела

        Тип данных: BOOL

        Свойство позволяет устанавливать и получать признак передачи в вид тел.

        ### Синтаксис Automation

        ```
        ProjectBodies = Object.ProjectBodies       # Получить свойство (* )
        Object.ProjectBodies = ProjectBodies       # Установить свойство (* )
        ProjectBodies = Object.GetProjectBodies()  # Получить свойство (**)
        Object.SetProjectBodies( ProjectBodies )   # Установить свойство (**)
        ```
        """
        ...
    @ProjectBodies.setter
    def ProjectBodies(self, value: bool): ...

    @property
    def ProjectBrandLeaders(self) -> bool:  # iassociationviewelements_projectbrandleaders.html
        """
        ## ProjectBrandLeaders - Передавать в вид обозначения клеймений

        Тип данных: BOOL

        Свойство позволяет устанавливать и получать признак передачи в вид обозначений клеймения.

        ### Синтаксис Automation

        ```
        ProjectBrandLeaders = Object.ProjectBrandLeaders       # Получить свойство (* )
        Object.ProjectBrandLeaders = ProjectBrandLeaders       # Установить свойство (* )
        ProjectBrandLeaders = Object.GetProjectBrandLeaders()  # Получить свойство (**)
        Object.SetProjectBrandLeaders( ProjectBrandLeaders )   # Установить свойство (**)
        ```
        """
        ...
    @ProjectBrandLeaders.setter
    def ProjectBrandLeaders(self, value: bool): ...

    @property
    def ProjectCurves(self) -> bool:  # iassociationviewelements_projectcurves.html
        """
        ## ProjectCurves - Передавать в вид кривые

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        ProjectCurves = Object.ProjectCurves       # Получить свойство (* )
        Object.ProjectCurves = ProjectCurves       # Установить свойство (* )
        ProjectCurves = Object.GetProjectCurves()  # Получить свойство (**)
        Object.SetProjectCurves( ProjectCurves )   # Установить свойство (**)
        ```
        """
        ...
    @ProjectCurves.setter
    def ProjectCurves(self, value: bool): ...

    @property
    def ProjectDesTables(self) -> bool:  # iassociationviewelements_projectdestables.html
        """
        ## ProjectDesTables - Передавать в вид таблицы

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        ProjectDesTables = Object.ProjectDesTables       # Получить свойство (* )
        Object.ProjectDesTables = ProjectDesTables       # Установить свойство (* )
        ProjectDesTables = Object.GetProjectDesTables()  # Получить свойство (**)
        Object.SetProjectDesTables( ProjectDesTables )   # Установить свойство (**)
        ```
        """
        ...
    @ProjectDesTables.setter
    def ProjectDesTables(self, value: bool): ...

    @property
    def ProjectDesTexts(self) -> bool:  # iassociationviewelements_projectdestexts.html
        """
        ## ProjectDesTexts - Передавать в вид надписи

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        ProjectDesTexts = Object.ProjectDesTexts       # Получить свойство (* )
        Object.ProjectDesTexts = ProjectDesTexts       # Установить свойство (* )
        ProjectDesTexts = Object.GetProjectDesTexts()  # Получить свойство (**)
        Object.SetProjectDesTexts( ProjectDesTexts )   # Установить свойство (**)
        ```
        """
        ...
    @ProjectDesTexts.setter
    def ProjectDesTexts(self, value: bool): ...

    @property
    def ProjectDimensions(self) -> bool:  # iassociationviewelements_projectdimensions.html
        """
        ## ProjectDimensions - Передавать в вид обозначения размеров

        Тип данных: BOOL

        Свойство позволяет устанавливать и получать признак передачи в вид обозначений размеров.

        ### Синтаксис Automation

        ```
        ProjectDimensions = Object.ProjectDimensions       # Получить свойство (* )
        Object.ProjectDimensions = ProjectDimensions       # Установить свойство (* )
        ProjectDimensions = Object.GetProjectDimensions()  # Получить свойство (**)
        Object.SetProjectDimensions( ProjectDimensions )   # Установить свойство (**)
        ```
        """
        ...
    @ProjectDimensions.setter
    def ProjectDimensions(self, value: bool): ...

    @property
    def ProjectHiddenComponents(self) -> bool:  # iassociationviewelements_projecthiddencomponents.html
        """
        ## ProjectHiddenComponents - Передавать в вид скрытые компоненты

        Тип данных: BOOL

        Свойство позволяет устанавливать и получать признак передачи в вид скрытых компонентов.

        ### Синтаксис Automation

        ```
        ProjectHiddenComponents = Object.ProjectHiddenComponents       # Получить свойство (* )
        Object.ProjectHiddenComponents = ProjectHiddenComponents       # Установить свойство (* )
        ProjectHiddenComponents = Object.GetProjectHiddenComponents()  # Получить свойство (**)
        Object.SetProjectHiddenComponents( ProjectHiddenComponents )   # Установить свойство (**)
        ```
        """
        ...
    @ProjectHiddenComponents.setter
    def ProjectHiddenComponents(self, value: bool): ...

    @property
    def ProjectLayers(self) -> bool:  # iassociationviewelements_projectlayers.html
        """
        ## ProjectLayers - Учитывать при проецировании слои

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        ProjectLayers = Object.ProjectLayers       # Получить свойство (* )
        Object.ProjectLayers = ProjectLayers       # Установить свойство (* )
        ProjectLayers = Object.GetProjectLayers()  # Получить свойство (**)
        Object.SetProjectLayers( ProjectLayers )   # Установить свойство (**)
        ```
        """
        ...
    @ProjectLayers.setter
    def ProjectLayers(self, value: bool): ...

    @property
    def ProjectLeaders(self) -> bool:  # iassociationviewelements_projectleaders.html
        """
        ## ProjectLeaders - Передавать в вид обозначения линий-выносок

        Тип данных: BOOL

        Свойство позволяет устанавливать и получать признак передачи в вид обозначений линий-выносок.

        ### Синтаксис Automation

        ```
        ProjectLeaders = Object.ProjectLeaders       # Получить свойство (* )
        Object.ProjectLeaders = ProjectLeaders       # Установить свойство (* )
        ProjectLeaders = Object.GetProjectLeaders()  # Получить свойство (**)
        Object.SetProjectLeaders( ProjectLeaders )   # Установить свойство (**)
        ```
        """
        ...
    @ProjectLeaders.setter
    def ProjectLeaders(self, value: bool): ...

    @property
    def ProjectMarkLeaders(self) -> bool:  # iassociationviewelements_projectmarkleaders.html
        """
        ## ProjectMarkLeaders - Передавать в вид обозначения маркировок

        Тип данных: BOOL

        Свойство позволяет устанавливать и получать признак передачи в вид обозначений маркировок.

        ### Синтаксис Automation

        ```
        ProjectMarkLeaders = Object.ProjectMarkLeaders       # Получить свойство (* )
        Object.ProjectMarkLeaders = ProjectMarkLeaders       # Установить свойство (* )
        ProjectMarkLeaders = Object.GetProjectMarkLeaders()  # Получить свойство (**)
        Object.SetProjectMarkLeaders( ProjectMarkLeaders )   # Установить свойство (**)
        ```
        """
        ...
    @ProjectMarkLeaders.setter
    def ProjectMarkLeaders(self, value: bool): ...

    @property
    def ProjectPoints(self) -> bool:  # iassociationviewelements_projectpoints.html
        """
        ## ProjectPoints - Передавать в вид точки

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        ProjectPoints = Object.ProjectPoints       # Получить свойство (* )
        Object.ProjectPoints = ProjectPoints       # Установить свойство (* )
        ProjectPoints = Object.GetProjectPoints()  # Получить свойство (**)
        Object.SetProjectPoints( ProjectPoints )   # Установить свойство (**)
        ```
        """
        ...
    @ProjectPoints.setter
    def ProjectPoints(self, value: bool): ...

    @property
    def ProjectPositions(self) -> bool:  # iassociationviewelements_projectpositions.html
        """
        ## ProjectPositions - Передавать в вид обозначения позиций

        Тип данных: BOOL

        Свойство позволяет устанавливать и получать признак передачи в вид обозначений позиций.

        ### Синтаксис Automation

        ```
        ProjectPositions = Object.ProjectPositions       # Получить свойство (* )
        Object.ProjectPositions = ProjectPositions       # Установить свойство (* )
        ProjectPositions = Object.GetProjectPositions()  # Получить свойство (**)
        Object.SetProjectPositions( ProjectPositions )   # Установить свойство (**)
        ```
        """
        ...
    @ProjectPositions.setter
    def ProjectPositions(self, value: bool): ...

    @property
    def ProjectRoughs(self) -> bool:  # iassociationviewelements_projectroughs.html
        """
        ## ProjectRoughs - Передавать в вид обозначения шероховатостей

        Тип данных: BOOL

        Свойство позволяет устанавливать и получать признак передачи в вид обозначений шероховатостей.

        ### Синтаксис Automation

        ```
        ProjectRoughs = Object.ProjectRoughs       # Получить свойство (* )
        Object.ProjectRoughs = ProjectRoughs       # Установить свойство (* )
        ProjectRoughs = Object.GetProjectRoughs()  # Получить свойство (**)
        Object.SetProjectRoughs( ProjectRoughs )   # Установить свойство (**)
        ```
        """
        ...
    @ProjectRoughs.setter
    def ProjectRoughs(self, value: bool): ...

    @property
    def ProjectSketches(self) -> bool:  # iassociationviewelements_projectsketches.html
        """
        ## ProjectSketches - Передавать в вид эскизы

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        ProjectSketches = Object.ProjectSketches       # Получить свойство (* )
        Object.ProjectSketches = ProjectSketches       # Установить свойство (* )
        ProjectSketches = Object.GetProjectSketches()  # Получить свойство (**)
        Object.SetProjectSketches( ProjectSketches )   # Установить свойство (**)
        ```
        """
        ...
    @ProjectSketches.setter
    def ProjectSketches(self, value: bool): ...

    @property
    def ProjectSpecRough(self) -> bool:  # iassociationviewelements_projectspecrough.html
        """
        ## ProjectSpecRough - Передавать в вид обозначения неуказанной шероховатости

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        ProjectSpecRough = Object.ProjectSpecRough       # Получить свойство (* )
        Object.ProjectSpecRough = ProjectSpecRough       # Установить свойство (* )
        ProjectSpecRough = Object.GetProjectSpecRough()  # Получить свойство (**)
        Object.SetProjectSpecRough( ProjectSpecRough )   # Установить свойство (**)
        ```
        """
        ...
    @ProjectSpecRough.setter
    def ProjectSpecRough(self, value: bool): ...

    @property
    def ProjectStandartElements(self) -> bool:  # iassociationviewelements_projectstandartelements.html
        """
        ## ProjectStandartElements - Передавать в вид библиотечные элементы

        Тип данных: BOOL

        Свойство позволяет устанавливать и получать признак передачи в вид библиотечных элементов.

        ### Синтаксис Automation

        ```
        ProjectStandartElements = Object.ProjectStandartElements       # Получить свойство (* )
        Object.ProjectStandartElements = ProjectStandartElements       # Установить свойство (* )
        ProjectStandartElements = Object.GetProjectStandartElements()  # Получить свойство (**)
        Object.SetProjectStandartElements( ProjectStandartElements )   # Установить свойство (**)
        ```
        """
        ...
    @ProjectStandartElements.setter
    def ProjectStandartElements(self, value: bool): ...

    @property
    def ProjectSurfaces(self) -> bool:  # iassociationviewelements_projectsurfaces.html
        """
        ## ProjectSurfaces - Передавать в вид поверхности

        Тип данных: BOOL

        Свойство позволяет устанавливать и получать признак передачи в вид поверхностей.

        ### Синтаксис Automation

        ```
        ProjectSurfaces = Object.ProjectSurfaces       # Получить свойство (* )
        Object.ProjectSurfaces = ProjectSurfaces       # Установить свойство (* )
        ProjectSurfaces = Object.GetProjectSurfaces()  # Получить свойство (**)
        Object.SetProjectSurfaces( ProjectSurfaces )   # Установить свойство (**)
        ```
        """
        ...
    @ProjectSurfaces.setter
    def ProjectSurfaces(self, value: bool): ...

    @property
    def ProjectThreads(self) -> bool:  # iassociationviewelements_projectthreads.html
        """
        ## ProjectThreads - Передавать в вид обозначения резьб

        Тип данных: BOOL

        Свойство позволяет устанавливать и получать признак передачи в вид обозначений резьб.

        ### Синтаксис Automation

        ```
        ProjectThreads = Object.ProjectThreads       # Получить свойство (* )
        Object.ProjectThreads = ProjectThreads       # Установить свойство (* )
        ProjectThreads = Object.GetProjectThreads()  # Получить свойство (**)
        Object.SetProjectThreads( ProjectThreads )   # Установить свойство (**)
        ```
        """
        ...
    @ProjectThreads.setter
    def ProjectThreads(self, value: bool): ...

    @property
    def ProjectTolerances(self) -> bool:  # iassociationviewelements_projecttolerances.html
        """
        ## ProjectTolerances - Передавать в вид обозначения допусков форм

        Тип данных: BOOL

        Свойство позволяет устанавливать и получать признак передачи в вид обозначений допусков форм.

        ### Синтаксис Automation

        ```
        ProjectTolerances = Object.ProjectTolerances       # Получить свойство (* )
        Object.ProjectTolerances = ProjectTolerances       # Установить свойство (* )
        ProjectTolerances = Object.GetProjectTolerances()  # Получить свойство (**)
        Object.SetProjectTolerances( ProjectTolerances )   # Установить свойство (**)
        ```
        """
        ...
    @ProjectTolerances.setter
    def ProjectTolerances(self, value: bool): ...




class IView1(IDispatch):  # iview1.html
    """
    ## Интерфейс IView1

    Дополнительный интерфейс для вида.

    Дополнительный интерфейс для графических объектов. Данный интерфейс можно получить у интерфейса вида IView посредством вызова метода IUnknown::QueryInterface (const GUID far& IID, void** pif).
    """

    @property
    def BaseObject(self) -> IDrawingObject:  # iview1_baseobject.html
        """
        ## BaseObject - Опорный объект

        Тип данных: Указатель на интерфейс IDrawingObject

        В зависимости от типа ассоциативного вида нужно указать вспомогательный опорный объект.

        Опорный объект требуется при создании следующих видов:

        •Вид по стрелке - требуется объект стрелка взгляда.

        •Выносной вид - требуется объект обозначение выносного элемента.

        •Вид разреза/сечения - требуется объект линия разреза.

        ### Синтаксис Automation

        ```
        BaseObject = Object.BaseObject       # Получить свойство (* )
        Object.BaseObject = BaseObject       # Установить свойство (* )
        BaseObject = Object.GetBaseObject()  # Получить свойство (**)
        Object.SetBaseObject( BaseObject )   # Установить свойство (**)
        ```
        """
        ...
    @BaseObject.setter
    def BaseObject(self, value: IDrawingObject): ...

    @property
    def Crossed(self) -> bool:  # iview1_crossed.html
        """
        ## Crossed - Признак необходимости перестроения вида

        Тип данных: BOOL

        Свойство доступно только для чтения.

        Версия Компас v18.1

        ### Синтаксис Automation

        ```
        Crossed = Object.Crossed       # Установить свойство (* )
        Crossed = Object.GetCrossed()  # Установить свойство (**)
        ```
        """
        ...

    @property
    def CrossedTitle(self) -> bool:  # iview1_crossedtitle.html
        """
        ## CrossedTitle - Признак необходимости перестроения заголовка вида

        Тип данных: BOOL

        Свойство доступно только для чтения.

        Версия Компас v18.1

        ### Синтаксис Automation

        ```
        CrossedTitle = Object.CrossedTitle       # Установить свойство (* )
        CrossedTitle = Object.GetCrossedTitle()  # Установить свойство (**)
        ```
        """
        ...

    @property
    def EditMacroVisibleRegime(self) -> bool:  # iview1_editmacrovisibleregime.html
        """
        ## EditMacroVisibleRegime - Находится ли вид в режиме редактирования макроэлемента

        Тип данных: BOOL

        Свойство используется при редактировании макроэлементов.

        ### Синтаксис Automation

        ```
        EditMacroVisibleRegime = Object.EditMacroVisibleRegime       # Получить свойство (* )
        EditMacroVisibleRegime = Object.GetEditMacroVisibleRegime()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def LocalCoordinateSystems2D(self) -> ILocalCoordinateSystems2D:  # iview1_localcoordinatesystems2d.html
        """
        ## LocalCoordinateSystems2D - Получить коллекцию ЛСК вида

        Тип данных: указатель на интерфейс ILocalCoordinateSystems2D

        ### Синтаксис Automation

        ```
        LocalCoordinateSystems2D = Object.LocalCoordinateSystems2D       # Получить свойство (* )
        LocalCoordinateSystems2D = Object.GetLocalCoordinateSystems2D()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def Numerator(self) -> float:  # iview1_numerator.html
        """
        ## Numerator - Числитель масштаба

        Тип данных: double

        ### Синтаксис Automation

        ```
        Numerator = Object.Numerator       # Получить свойство (* )
        Object.Numerator = Numerator       # Установить свойство (* )
        Numerator = Object.GetNumerator()  # Получить свойство (**)
        Object.SetNumerator( Numerator )   # Установить свойство (**)
        ```
        """
        ...
    @Numerator.setter
    def Numerator(self, value: float): ...

    @property
    def Printable(self) -> bool:  # iview1_printable.html
        """
        ## Printable - Признак разрешения/запрещения печати вида

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        Printable = Object.Printable       # Получить свойство (* )
        Object.Printable = Printable       # Установить свойство (* )
        Printable = Object.GetPrintable()  # Получить свойство (**)
        Object.SetPrintable( Printable )   # Установить свойство (**)
        ```
        """
        ...
    @Printable.setter
    def Printable(self, value: bool): ...

    def FindObject(self, X, Y, Limit, Param) -> IDrawingObject:  # iview1_findobject.html
        """
        ## FindObject - Найти объект, ближайший к заданной точке, удовлетворяющий параметрам поиска

        Для поиска объектов в точке могут быть заданы расширенные параметры поиска.

        Интерфейс расширенных параметров поиска IFindObjectParameters можно получить с помощью метода IKompasDocument1::GetInterface; в 2D документах при использовании константы ksObjectFindObjectParameters.

        Если расширенные параметры поиска не требуются, необходимо передать NULL в функцию.

        ### Синтаксис Automation

        ```
        LPDISPATCH FindObject( double X, double Y, double Limit, IFindObjectParameters * Param );
        ```

        ### Входные параметры

        +------------------------------------------------------------+
        | X     | - первая координата точки,                         |
        +------------------------------------------------------------+
        | Y     | - вторая координата точки,                         |
        +------------------------------------------------------------+
        | Limit | - предел поиска и найденное расстояние,            |
        +------------------------------------------------------------+
        | Param | - параметры поиска объектов IFindObjectParameters. |
        +------------------------------------------------------------+

        ### Возвращаемое значение

        указатель на интерфейс IDrawingObject.
        """
        ...

    def FindObjects(self, X, Y, Limit, Param) -> list[IDispatch]:  # iview1_findobjects.html
        """
        ## FindObjects - Найти объекты в заданной точке, удовлетворяющие параметрам поиска

        Для поиска объектов в точке могут быть заданы расширенные параметры поиска.

        Интерфейс расширенных параметров поиска IFindObjectParameters можно получить с помощью метода IKompasDocument1::GetInterface; в 2D документах при использовании константы ksObjectFindObjectParameters.

        Если расширенные параметры поиска не требуются, необходимо передать NULL в функцию.

        ### Синтаксис Automation

        ```
        VARIANT FindObjects( double X, double Y, double Limit, IFindObjectParameters * Param );
        ```

        ### Входные параметры

        +------------------------------------------------------------+
        | X     | - первая координата точки,                         |
        +------------------------------------------------------------+
        | Y     | - вторая координата точки,                         |
        +------------------------------------------------------------+
        | Limit | - предел поиска и найденное расстояние,            |
        +------------------------------------------------------------+
        | Param | - параметры поиска объектов IFindObjectParameters. |
        +------------------------------------------------------------+

        ### Возвращаемое значение

        +---------------------------------------------------------------------------------------+
        | VARIANT | - объект типа VARIANT на безопасный массив SAFEARRAY: VT_ARRAY|VT_DISPATCH. |
        +---------------------------------------------------------------------------------------+
        """
        ...

    def IsViewPointInBreakView(self, X, Y) -> bool:  # iview1_isviewpointinbreakview.html
        """
        ## IsViewPointInBreakView - Видна ли точка на виде с разрывами

        ### Синтаксис Automation

        ```
        BOOL IsViewPointInBreakView( double X, double Y );
        ```

        ### Входные параметры

        +----------------------------+
        | X, Y | - координаты точки. |
        +----------------------------+

        Версия КОМПАС v22

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def SelectObjects(self, RegionType, XMin, YMin, XMax, YMax) -> list[IDispatch]:  # iview1_selectobjects.html
        """
        ## SelectObjects - Отобрать объекты рамкой

        ### Синтаксис Automation

        ```
        VARIANT SelectObjects( ksRegionTypeEnum RegionType, double XMin, double YMin, double XMax, double YMax );
        ```

        ### Возвращаемое значение

        +---------------------------------------------------------------------------------------+
        | VARIANT | - объект типа VARIANT на безопасный массив SAFEARRAY: VT_ARRAY|VT_DISPATCH. |
        +---------------------------------------------------------------------------------------+

        +--------------------------------------------------+
        | RegionType | - тип региона,                      |
        +--------------------------------------------------+
        | XMin       | - минимальная граница поиска по x,  |
        +--------------------------------------------------+
        | YMin       | - минимальная граница поиска по y,  |
        +--------------------------------------------------+
        | XMax       | - максимальная граница поиска по x, |
        +--------------------------------------------------+
        | YMax       | - максимальная граница поиска по y. |
        +--------------------------------------------------+
        """
        ...

    def TransformAngleFromSheet(self, Angle) -> bool:  # iview1_transformanglefromsheet.html
        """
        ## TransformAngleFromSheet - Перевод угла из CK листа в CK вида

        ### Синтаксис Automation

        ```
        BOOL TransformAngleFromSheet( double * Angle );
        ```

        ### Входные параметры

        +-------------------------------------------+
        | Angle | - угол в системе координат листа. |
        +-------------------------------------------+

        ### Выходные параметры

        +------------------------------------------+
        | Angle | - угол в системе координат вида. |
        +------------------------------------------+

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def TransformAngleToSheet(self, Angle) -> bool:  # iview1_transformangletosheet.html
        """
        ## TransformAngleToSheet - Перевод угла из CK вида в CK листа

        ### Синтаксис Automation

        ```
        BOOL TransformAngleToSheet( double * Angle );
        ```

        ### Входные параметры

        +------------------------------------------+
        | Angle | - угол в системе координат вида. |
        +------------------------------------------+

        ### Выходные параметры

        +-------------------------------------------+
        | Angle | - угол в системе координат листа. |
        +-------------------------------------------+

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def TransformBreakViewPointToView(self, X, Y) -> bool:  # iview1_transformbreakviewpointtoview.html
        """
        ## TransformBreakViewPointToView - Перевод координат точки вида c разрывами в CK вида без разрывов

        ### Синтаксис Automation

        ```
        BOOL TransformBreakViewPointToView( double * X, double * Y );
        ```

        ### Входные параметры

        +----------------------------+
        | X, Y | - координаты точки. |
        +----------------------------+

        Версия КОМПАС v22

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def TransformPointFromSheet(self, X, Y) -> bool:  # iview1_transformpointfromsheet.html
        """
        ## TransformPointFromSheet - Перевод координат точки из CK листа в CK вида

        ### Синтаксис Automation

        ```
        BOOL TransformPointFromSheet( double * X, double * Y );
        ```

        ### Входные параметры

        +------------------------------------------------------+
        | X, Y | - координаты точек в системе координат листа. |
        +------------------------------------------------------+

        ### Выходные параметры

        +-----------------------------------------------------+
        | X, Y | - координаты точек в системе координат вида. |
        +-----------------------------------------------------+

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def TransformPointToSheet(self, X, Y) -> bool:  # iview1_transformpointtosheet.html
        """
        ## TransformPointToSheet - Перевод координат точки из CK вида в CK листа

        ### Синтаксис Automation

        ```
        BOOL TransformPointToSheet( double * X, double * Y );
        ```

        ### Входные параметры

        +-----------------------------------------------------+
        | X, Y | - координаты точек в системе координат вида. |
        +-----------------------------------------------------+

        ### Выходные параметры

        +------------------------------------------------------+
        | X, Y | - координаты точек в системе координат листа. |
        +------------------------------------------------------+

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def TransformViewPointToBreakView(self, X, Y) -> bool:  # iview1_transformviewpointtobreakview.html
        """
        ## TransformViewPointToBreakView - Перевод координат точки вида без разрывов в CK вида с разрывами

        ### Синтаксис Automation

        ```
        BOOL TransformViewPointToBreakView( double * X, double * Y );
        ```

        ### Входные параметры

        +----------------------------+
        | X, Y | - координаты точки. |
        +----------------------------+

        Версия КОМПАС v22

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...




class IViewDesignation(IDispatch):  # iviewdesignation.html
    """
    ## Интерфейс IViewDesignation

    Интерфейс обозначения вида.

    Позволяет получить и изменить обозначение вида.

    Дополнительный интерфейс вида. Данный интерфейс можно получить у вида IView посредством вызова метода IUnknown::QueryInterface (const GUID far& iid, void** pif); в визуальном режиме этому интерфейсу соответствует закладка в параметрах вида "Обозначение вида".

    Если простой вид, интерфейс позволяет получить или установить ссылку на объект "Стрелка взгляда", "Линия разреза", "Выносной элемент".

    С помощью флагов можно регулировать надпись обозначения вида, включать или выключать соответствующие компоненты обозначения.

    Для ассоциативных видов изменить ссылку на объект нельзя. Можно только регулировать надпись обозначения вида при помощи флагов.

    Чтобы изменения, установленные в интерфейсе, передались в модель, нужно вызвать у вида метод Update();
    """

    @property
    def Designation(self) -> str:  # iviewdesignation_designation.html
        """
        ## Designation - Обозначение вида

        Тип данных: BSTR

        1. Свойство позволяет получить обозначение вида в виде строки текста.

        2. Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        Designation = iObject.Designation  # Получить свойство (* )
        iObject.GetDesignation()           # Получить свойство (**)
        ```
        """
        ...

    @property
    def DrawingText(self) -> IDrawingObject:  # iviewdesignation_drawingtext.html
        """
        ## DrawingText - Надпись вида

        Тип данных: Указатель на интерфейс IDrawingObject

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        DrawingText = Object.DrawingText       # Получить свойство (* )
        DrawingText = Object.GetDrawingText()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def RefObject(self) -> typing.Any:  # iviewdesignation_refobject.html
        """
        ## RefObject - Ссылка на объект "Стрелка вида", "Линия разреза" или "Выносной элемент"

        Тип данных: VARIANT

        Свойство позволяет получить или установить ссылку на объект "Стрелка вида", "Линия разреза", "Выносной элемент".

        Наименование из этого объекта используется в формировании обозначения вида. Если вид ассоциативный, свойство работает как readOnly (только для чтения). В настоящее время свойство реализовано через reference на объект.

        VARIANT pVal;

        pVal.lVal = p; // reference объекта

        pVal.vt = VT_I4; // тип long

        ### Синтаксис Automation

        ```
        RefObject = iObject.RefObject       # Получить свойство (* )
        iObject.RefObject = RefObject       # Установить свойство (* )
        RefObject = iObject.GetRefObject()  # Получить свойство (**)
        iObject.SetRefObject (RefObject)    # Установить свойство (**)
        ```
        """
        ...
    @RefObject.setter
    def RefObject(self, value: typing.Any): ...

    @property
    def ShowAngle(self) -> bool:  # iviewdesignation_showangle.html
        """
        ## ShowAngle - Показывать "Повернуто на угол"

        Тип данных: BOOL

        Если свойство возвращает TRUE, в обозначении вида включается угол поворота вида.

        Если FALSE, угол поворота вида из обозначения выключается.

        ### Синтаксис Automation

        ```
        ShowAngle = iObject.ShowAngle       # Получить свойство (* )
        iObject.ShowAngle = ShowAngle       # Установить свойство (* )
        ShowAngle = iObject.GetShowAngle()  # Получить свойство (**)
        iObject.SetShowAngle (ShowAngle)    # Установить свойство (**)
        ```
        """
        ...
    @ShowAngle.setter
    def ShowAngle(self, value: bool): ...

    @property
    def ShowName(self) -> bool:  # iviewdesignation_showname.html
        """
        ## ShowName - Показывать наименование

        Тип данных: BOOL

        Если свойство возвращает TRUE, в обозначении вида включается наименование из ссылки на объект например (A-A).

        Если FALSE, наименование из обозначения выключается.

        ### Синтаксис Automation

        ```
        ShowName = iObject.ShowName       # Получить свойство (* )
        iObject.ShowName = ShowName       # Установить свойство (* )
        ShowName = iObject.GetShowName()  # Получить свойство (**)
        iObject.SetShowName (ShowName)    # Установить свойство (**)
        ```
        """
        ...
    @ShowName.setter
    def ShowName(self, value: bool): ...

    @property
    def ShowPage(self) -> bool:  # iviewdesignation_showpage.html
        """
        ## ShowPage - Показывать лист

        Тип данных: BOOL

        Если свойство возвращает TRUE, в обозначении вида включается номер листа.

        Если FALSE, номер листа из обозначения выключается.

        ### Синтаксис Automation

        ```
        ShowPage = iObject.ShowPage       # Получить свойство (* )
        iObject.ShowPage = ShowPage       # Установить свойство (* )
        ShowPage = iObject.GetShowPage()  # Получить свойство (**)
        iObject.SetShowPage (ShowPage)    # Установить свойство (**)
        ```
        """
        ...
    @ShowPage.setter
    def ShowPage(self, value: bool): ...

    @property
    def ShowScale(self) -> bool:  # iviewdesignation_showscale.html
        """
        ## ShowScale - Показывать масштаб

        Тип данных: BOOL

        Если свойство возвращает TRUE, в обозначении вида включается масштаб.

        Если FALSE, масштаб из обозначения выключается.

        ### Синтаксис Automation

        ```
        ShowScale = iObject.ShowScale       # Получить свойство (* )
        iObject.ShowScale = ShowScale       # Установить свойство (* )
        ShowScale = iObject.GetShowScale()  # Получить свойство (**)
        iObject.SetShowScale (ShowScale)    # Установить свойство (**)
        ```
        """
        ...
    @ShowScale.setter
    def ShowScale(self, value: bool): ...

    @property
    def ShowTurn(self) -> bool:  # iviewdesignation_showturn.html
        """
        ## ShowTurn - Показывать "Повернуто"

        Тип данных: BOOL

        Если свойство возвращает TRUE, в Обозначении вида включается спецзнак "Повернуто".

        Если FALSE, спецзнак "Повернуто" из обозначения выключается.

        ### Синтаксис Automation

        ```
        ShowTurn = iObject.ShowTurn       # Получить свойство (* )
        iObject.ShowTurn = ShowTurn       # Установить свойство (* )
        ShowTurn = iObject.GetShowTurn()  # Получить свойство (**)
        iObject.SetShowTurn (ShowTurn)    # Установить свойство (**)
        ```
        """
        ...
    @ShowTurn.setter
    def ShowTurn(self, value: bool): ...

    @property
    def ShowUnfold(self) -> bool:  # iviewdesignation_showunfold.html
        """
        ## ShowUnfold - Показывать "Развернуто"

        Тип данных: BOOL

        Если свойство возвращает TRUE, в Обозначении вида включается спецзнак "Развернуто".

        Если FALSE, спецзнак "Развернуто" из обозначения выключается.

        ### Синтаксис Automation

        ```
        ShowUnfold = iObject.ShowUnfold       # Получить свойство (* )
        iObject.ShowUnfold = ShowUnfold       # Установить свойство (* )
        ShowUnfold = iObject.GetShowUnfold()  # Получить свойство (**)
        iObject.SetShowUnfold (ShowUnfold)    # Установить свойство (**)
        ```
        """
        ...
    @ShowUnfold.setter
    def ShowUnfold(self, value: bool): ...

    @property
    def ShowZone(self) -> bool:  # iviewdesignation_showzone.html
        """
        ## ShowZone - Показывать зону

        Тип данных: BOOL

        Если свойство возвращает TRUE, в обозначении вида включается зона.

        Если FALSE, зона из обозначения выключается.

        ### Синтаксис Automation

        ```
        ShowZone = iObject.ShowZone       # Получить свойство (* )
        iObject.ShowZone = ShowZone       # Установить свойство (* )
        ShowZone = iObject.GetShowZone()  # Получить свойство (**)
        iObject.SetShowZone (ShowZone)    # Установить свойство (**)
        ```
        """
        ...
    @ShowZone.setter
    def ShowZone(self, value: bool): ...




class ILocalCoordinateSystems2D(IKompasCollection):  # ilocalcoordinatesystems2d.html
    """
    ## Интерфейс ILocalCoordinateSystems2D

    Коллекция локальных систем координат.

    Данный интерфейс можно получить с помощью свойства LocalCoordinateSystems2D интерфейса IView1.
    """

    @property
    def Current(self) -> ILocalCoordinateSystem2D:  # ilocalcoordinatesystems2d_current.html
        """
        ## Current - Получить/установить текущую ЛСК

        Тип данных: указатель на интерфейс ILocalCoordinateSystem2D

        ### Синтаксис Automation

        ```
        Current = Object.Current       # Получить свойство (* )
        Object.Current = Current       # Установить свойство (* )
        Current = Object.GetCurrent()  # Получить свойство (**)
        Object.SetCurrent( Current )   # Установить свойство (**)
        ```
        """
        ...
    @Current.setter
    def Current(self, value: ILocalCoordinateSystem2D): ...

    def Add(self):  # ilocalcoordinatesystems2d_add.html
        """
        ## Add - Создает новую ЛСК и добавляет её в коллекцию

        ### Синтаксис Automation

        ```
        LPDISPATCH Add();
        ```

        ### Возвращаемое значение

        Указатель на интерфейс добавленной ЛСК.
        """
        ...

    def Item(self, Index) -> ILocalCoordinateSystem2D:  # ilocalcoordinatesystems2d_item.html
        """
        ## Item - Возвращает ЛСК, заданную по индексу или имени

        Тип данных: указатель на интерфейс ILocalCoordinateSystem2D

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        Item = Object.Item( Index )     # Получить свойство (* )
        Item = Object.GetItem( Index )  # Получить свойство (**)
        ```
        """
        ...




class ILocalCoordinateSystem2D(IKompasAPIObject):  # ilocalcoordinatesystem2d.html
    """
    ## Интерфейс ILocalCoordinateSystem2D

    Интерфейс локальной системы координат.

    Данный интерфейс можно получить с помощью свойств ItemILocal и CurrentILocal, а также метода AddILocal интерфейса ILocalCoordinateSystems2D.
    """

    @property
    def Angle(self) -> float:  # ilocalcoordinatesystem2d_angle.html
        """
        ## Angle - Угол наклона оси Х ЛСК

        Тип данных: double

        ### Синтаксис Automation

        ```
        Angle = Object.Angle       # Получить свойство (* )
        Object.Angle = Angle       # Установить свойство (* )
        Angle = Object.GetAngle()  # Получить свойство (**)
        Object.SetAngle( Angle )   # Установить свойство (**)
        ```
        """
        ...
    @Angle.setter
    def Angle(self, value: float): ...

    @property
    def AxisXLabel(self) -> str:  # ilocalcoordinatesystem2d_axisxlabel.html
        """
        ## AxisXLabel - Обозначение оси X ЛСК

        Тип данных: BSTR

        ### Синтаксис Automation

        ```
        AxisXLabel = Object.AxisXLabel       # Получить свойство (* )
        Object.AxisXLabel = AxisXLabel       # Установить свойство (* )
        AxisXLabel = Object.GetAxisXLabel()  # Получить свойство (**)
        Object.SetAxisXLabel( AxisXLabel )   # Установить свойство (**)
        ```
        """
        ...
    @AxisXLabel.setter
    def AxisXLabel(self, value: str): ...

    @property
    def AxisYLabel(self) -> str:  # ilocalcoordinatesystem2d_axisylabel.html
        """
        ## AxisYLabel - Обозначение оси Y ЛСК

        Тип данных: BSTR

        ### Синтаксис Automation

        ```
        AxisYLabel = Object.AxisYLabel       # Получить свойство (* )
        Object.AxisYLabel = AxisYLabel       # Установить свойство (* )
        AxisYLabel = Object.GetAxisYLabel()  # Получить свойство (**)
        Object.SetAxisYLabel( AxisYLabel )   # Установить свойство (**)
        ```
        """
        ...
    @AxisYLabel.setter
    def AxisYLabel(self, value: str): ...

    @property
    def Name(self) -> str:  # ilocalcoordinatesystem2d_name.html
        """
        ## Name - Название ЛСК

        Тип данных: BSTR

        ### Синтаксис Automation

        ```
        Name = Object.Name       # Получить свойство (* )
        Object.Name = Name       # Установить свойство (* )
        Name = Object.GetName()  # Получить свойство (**)
        Object.SetName( Name )   # Установить свойство (**)
        ```
        """
        ...
    @Name.setter
    def Name(self, value: str): ...

    @property
    def X(self) -> float:  # ilocalcoordinatesystem2d_x.html
        """
        ## X - X-координата начала ЛСК

        Тип данных: double

        ### Синтаксис Automation

        ```
        X = Object.X       # Получить свойство (* )
        Object.X = X       # Установить свойство (* )
        X = Object.GetX()  # Получить свойство (**)
        Object.SetX( X )   # Установить свойство (**)
        ```
        """
        ...
    @X.setter
    def X(self, value: float): ...

    @property
    def Y(self) -> float:  # ilocalcoordinatesystem2d_y.html
        """
        ## Y - Y-координата начала ЛСК

        Тип данных: double

        ### Синтаксис Automation

        ```
        Y = Object.Y       # Получить свойство (* )
        Object.Y = Y       # Установить свойство (* )
        Y = Object.GetY()  # Получить свойство (**)
        Object.SetY( Y )   # Установить свойство (**)
        ```
        """
        ...
    @Y.setter
    def Y(self, value: float): ...

    def Delete(self) -> bool:  # ilocalcoordinatesystem2d_delete.html
        """
        ## Delete - Удаление ЛСК

        ### Синтаксис Automation

        ```
        BOOL Delete();
        ```

        ### Возвращаемое значение

        Возвращает TRUE в случае успешного удаления, FALSE - в случае неудачи.
        """
        ...




class IInsertionsManager(IDispatch):  # iinsertionsmanager.html
    """
    ## Интерфейс IInsertionsManager

    Интерфейс менеджера вставок фрагментов и видов.

    Интерфейс позволяет создавать и получать вставки фрагментов и видов.

    Интерфейс является дополнительным к интерфейсу документа IKompasDocument. Интерфейс можно получить посредством вызова метода IUnknown::QueryInterface (const GUID far& iid, void** pif).
    """

    def AddDefinition(self, Type, Name, FileName) -> IInsertionDefinition:  # iinsertionsmanager_adddefinition.html
        """
        ## AddDefinition - Создать новое описание для фрагмента или вида

        Метод позволяет создать новое описание вставки фрагмента или вида.

        ### Синтаксис Automation

        ```
        LPDISPATCH AddDefinition( long type,
        BSTR Name,
        BSTR FileName );
        ```

        ### Входные параметры

        +-----------------------------------------------------------+
        | type     | - тип вставки фрагмента или вида,              |
        +-----------------------------------------------------------+
        | Name     | - имя вставки,                                 |
        +-----------------------------------------------------------+
        | FileName | - имя файла, из которого производится вставка. |
        +-----------------------------------------------------------+

        ### Возвращаемое значение

        - Указатель на интерфейс вставки фрагмента или вида IInsertionDefinition.
        """
        ...

    def DefinitionsCount(self, Type) -> int:  # iinsertionsmanager_definitionscount.html
        """
        ## DefinitionsCount - Количество описаний

        Тип данных: long

        1. Свойство позволяет получать количество описаний вставок фрагмента или вида заданного типа.

        2. Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        DefinitionsCount = Object.DefinitionsCount( long type )     # Получить свойство (* )
        DefinitionsCount = Object.GetDefinitionsCount( long type )  # Получить свойство (**)
        ```

        ### Входные параметры

        +------------------------------------------+
        | type | - тип вставки фрагмента или вида. |
        +------------------------------------------+
        """
        ...

    def InsertionDefinition(self, Type, Index) -> IInsertionDefinition:  # iinsertionsmanager_insertiondefinition.html
        """
        ## InsertionDefinition - Получить описание по имени или индексу

        Тип данных: указатель на интерфейс IInsertionDefinition

        1. Свойство позволяет получать описание вставки фрагмента или вида по имени или индексу.

        2. Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        InsertionDefinition = Object.InsertionDefinition( long type, VARIANT Index )     # Получить свойство (* )
        InsertionDefinition = Object.GetInsertionDefinition( long type, VARIANT Index )  # Получить свойство (**)
        ```

        ### Входные параметры

        +------------------------------------------------------+
        | type  | - тип вставки фрагмента или вида,            |
        +------------------------------------------------------+
        | Index | - индекс или имя вставки фрагмента или вида. |
        +------------------------------------------------------+
        """
        ...

    def InsertionDefinitions(self, Type) -> list[IDispatch]:  # iinsertionsmanager_insertiondefinitions.html
        """
        ## InsertionDefinitions - Описания фрагментов и видов

        Тип данных: VARIANT типа VT_ARRAY | VT_DISPATCH

        1. Свойство позволяет получать массив описаний фрагментов и видов. Если возвращается один объект, то тип VARIANT-а будет VT_DISPATCH, если возвращается несколько объектов, то тип VARIANT-а будет VT_ARRAY | VT_DISPATCH.

        2. Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        InsertionDefinitions = Object.InsertionDefinitions       # Получить свойство (* )
        InsertionDefinitions = Object.GetInsertionDefinitions()  # Получить свойство (**)
        ```
        """
        ...




class IInsertionObjects(IDrawingObjects):  # iinsertionobjects.html
    """
    ## Интерфейс IInsertionObjects

    Коллекция вставок фрагментов и видов других чертежей.

    Позволяет создавать и получать вставки фрагментов и видов.

    Данный интерфейс можно получить у контейнера графических объектов IDrawingContainer::InsertionObjects.
    """

    def Add(self, Definition):  # iinsertionobjects_add.html
        """
        ## Add - Создать новый элемент и добавить его в коллекцию

        ### Синтаксис Automation

        ```
        LPDISPATCH Add( LPDISPATCH Definition );
        ```

        ### Входные параметры

        +------------------------------------------------------------------------------------------------+
        | Definition | - Указатель на интерфейс описания вставки фрагмента или вида IInsertionDefinition |
        +------------------------------------------------------------------------------------------------+
        """
        ...

    def InsertionObject(self, Index) -> IInsertionObject:  # iinsertionobjects_insertionobject.html
        """
        ## InsertionObject - Вставка фрагмента, заданная по индексу

        Тип данных: указатель на интерфейс вставки фрагмента IInsertionObject

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        BrokenLine = iObject.BrokenLine ( Index );    # Получить свойство (* )
        BrokenLine = iObject.GetBrokenLine( Index );  # Получить свойство (**)
        ```
        """
        ...




class IOleDrawingObjects(IDrawingObjects):  # ioledrawingobjects.html
    """
    ## Интерфейс IOleDrawingObjects

    Интерфейс коллекции OLE-объектов.

    Позволяет создавать и получать OLE-объекты.

    Получить интерфейс можно, используя метод контейнера графических объектов IDrawingContainer::OleDrawingObjects.
    """

    def Add(self) -> IOleDrawingObject:  # ioledrawingobjects_add.html
        """
        ## Add - Создать новый элемент и добавить его в коллекцию

        ### Синтаксис Automation

        ```
        LPDISPATCH Add();
        ```

        ### Возвращаемое значение

        - Указатель на интерфейс OLE-объекта IOleDrawingObject.
        """
        ...

    def OleDrawingObject(self, Index) -> IOleDrawingObject:  # ioledrawingobjects_oledrawingobject.html
        """
        ## OleDrawingObject - OLE-объект, заданный по индексу

        Тип данных: указатель на интерфейс OLE-объекта IOleDrawingObject

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        OleDrawingObject = iObject.OleDrawingObject ( Index );    # Получить свойство (* )
        OleDrawingObject = iObject.GetOleDrawingObject( Index );  # Получить свойство (**)
        ```
        """
        ...




class IInsertionObject(IDrawingObject):  # iinsertionobject.html
    """
    ## Интерфейс IInsertionObject

    Базовый интерфейс для вставки фрагментов и видов другого чертежа.

    Интерфейс позволяет задавать и получать параметры вставки фрагментов и видов другого чертежа.

    1. Интерфейс можно получить у коллекции вставок фрагментов и видов, используя свойство IInsertionObjects::InsertionObject или метод IInsertionObjects::Add.

    2. После задания параметров объекта требуется вызвать метод IDrawingObject::Update.

    3. Посредством вызова метода IUnknown::QueryInterface (const GUID far& iid, void** pif) у данного интерфейса можно получить дополнительные интерфейсы:

    •IInsertionFragment,

    •IInsertionView,

    •IPropertyKeeper.
    """

    @property
    def DimensionLineScale(self) -> bool:  # iinsertionobject_dimensionlinescale.html
        """
        ## DimensionLineScale - Масштабирование выносных линий

        Тип данных: BOOL

        Значения свойства:

        +------------------------------------------+
        | TRUE  | - выносные линии масштабировать, |
        +------------------------------------------+
        | FALSE | - не масштабировать.             |
        +------------------------------------------+

        Свойство позволяет устанавливать и получать признак масштабирования выносных линий.

        ### Синтаксис Automation

        ```
        DimensionLineScale = Object.DimensionLineScale       # Получить свойство (* )
        Object.DimensionLineScale = DimensionLineScale       # Установить свойство (* )
        DimensionLineScale = Object.GetDimensionLineScale()  # Получить свойство (**)
        Object.SetDimensionLineScale( DimensionLineScale )   # Установить свойство (**)
        ```
        """
        ...
    @DimensionLineScale.setter
    def DimensionLineScale(self, value: bool): ...

    @property
    def FileName(self) -> str:  # iinsertionobject_filename.html
        """
        ## FileName - Имя файла источника

        Тип данных: BSTR

        1. Свойство позволяет получать имя файла источника.

        2. Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        FileName = Object.FileName       # Получить свойство (* )
        FileName = Object.GetFileName()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def InsertionDefinition(self) -> IInsertionDefinition:  # iinsertionobject_insertiondefinition.html
        """
        ## InsertionDefinition - Описание вставки фрагмента или вида

        Тип данных: указатель на интерфейс описания вставки IInsertionDefinition

        Свойство позволяет устанавливать и получать интерфейс описания вставки фрагмента или вида.

        ### Синтаксис Automation

        ```
        InsertionDefinition = Object.InsertionDefinition       # Получить свойство (* )
        Object.InsertionDefinition = InsertionDefinition       # Установить свойство (* )
        InsertionDefinition = Object.GetInsertionDefinition()  # Получить свойство (**)
        Object.SetInsertionDefinition( InsertionDefinition )   # Установить свойство (**)
        ```
        """
        ...
    @InsertionDefinition.setter
    def InsertionDefinition(self, value: IInsertionDefinition): ...

    @property
    def InsertionType(self) -> int:  # iinsertionobject_insertiontype.html
        """
        ## InsertionType - Способ вставки

        Тип данных: из перечисления ksInsertionTypeEnum

        1. Свойство позволяет получать способ вставки.

        2. Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        InsertionType = Object.InsertionType       # Получить свойство (* )
        InsertionType = Object.GetInsertionType()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def Name(self) -> str:  # iinsertionobject_name.html
        """
        ## Name - Имя вставки

        Тип данных: BSTR

        Свойство позволяет устанавливать и получать имя вставки фрагмента.

        ### Синтаксис Automation

        ```
        Name = Object.Name       # Получить свойство (* )
        Object.Name = Name       # Установить свойство (* )
        Name = Object.GetName()  # Получить свойство (**)
        Object.SetName( Name )   # Установить свойство (**)
        ```
        """
        ...
    @Name.setter
    def Name(self, value: str): ...

    def GetPlacement(self) -> bool:  # iinsertionobject_getplacement.html
        """
        ## GetPlacement - Получить местоположение объекта

        Метод позволяет получить местоположение объекта.

        ### Синтаксис Automation

        ```
        BOOL GetPlacement( double * X,
        double * Y,
        double * Angle,
        BOOL * MirrorSymmetry )
        ```

        ### Выходные параметры

        +----------------------------------------------------------+
        | X, Y           | - координаты объекта,                   |
        +----------------------------------------------------------+
        | Angle          | - угол поворота объекта,                |
        +----------------------------------------------------------+
        | MirrorSymmetry | - признак зеркальной симметрии объекта. |
        +----------------------------------------------------------+

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def SetPlacement(self, X, Y, Angle, MirrorSymmetry) -> bool:  # iinsertionobject_setplacement.html
        """
        ## SetPlacement - Установить местоположение объекта

        Метод позволяет установить местоположение объекта.

        ### Синтаксис Automation

        ```
        BOOL SetPlacement( double X,
        double Y,
        double Angle,
        BOOL MirrorSymmetry )
        ```

        ### Входные параметры

        +----------------------------------------------------------+
        | X, Y           | - координаты объекта,                   |
        +----------------------------------------------------------+
        | Angle          | - угол поворота объекта,                |
        +----------------------------------------------------------+
        | MirrorSymmetry | - признак зеркальной симметрии объекта. |
        +----------------------------------------------------------+

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...




class IInsertionFragment(IInsertionObject):  # iinsertionfragment.html
    """
    ## Интерфейс IInsertionFragment

    Интерфейс вставки фрагментов (локальных и внешних).

    Интерфейс позволяет задавать и получать параметры вставки локальных и внешних фрагментов.

    Дополнительный интерфейс базового интерфейса вставки фрагмента IInsertionObject. Может быть получен с помощью метода IUnknown::QueryInterface.
    """

    @property
    def BreakObjectsEnabled(self) -> bool:  # iinsertionfragment_breakobjectsenabled.html
        """
        ## BreakObjectsEnabled - Учитывать разрыв вида

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        BreakObjectsEnabled = Object.BreakObjectsEnabled       # Получить свойство (* )
        Object.BreakObjectsEnabled = BreakObjectsEnabled       # Установить свойство (* )
        BreakObjectsEnabled = Object.GetBreakObjectsEnabled()  # Получить свойство (**)
        Object.SetBreakObjectsEnabled( BreakObjectsEnabled )   # Установить свойство (**)
        ```
        """
        ...
    @BreakObjectsEnabled.setter
    def BreakObjectsEnabled(self, value: bool): ...

    @property
    def CreateSpcObjects(self) -> bool:  # iinsertionfragment_createspcobjects.html
        """
        ## CreateSpcObjects - Создавать объекты спецификации

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        CreateSpcObjects = Object.CreateSpcObjects       # Получить свойство (* )
        Object.CreateSpcObjects = CreateSpcObjects       # Установить свойство (* )
        CreateSpcObjects = Object.GetCreateSpcObjects()  # Получить свойство (**)
        Object.SetCreateSpcObjects( CreateSpcObjects )   # Установить свойство (**)
        ```
        """
        ...
    @CreateSpcObjects.setter
    def CreateSpcObjects(self, value: bool): ...

    @property
    def Label(self) -> str:  # iinsertionfragment_label.html
        """
        ## Label - Отображаемое имя объекта в дереве

        Тип данных: BSTR

        Свойство доступно только для чтения.

        Версия: Компас v20

        ### Синтаксис Automation

        ```
        Label = Object.Label       # Получить свойство (* )
        Label = Object.GetLabel()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def MultiLayer(self) -> bool:  # iinsertionfragment_multilayer.html
        """
        ## MultiLayer - Многослойность

        Тип данных: BOOL.

        Значения свойства:

        +--------------------------------+
        | TRUE  | - многослойный объект, |
        +--------------------------------+
        | FALSE | - однослойный объект.  |
        +--------------------------------+

        Свойство позволяет устанавливать и получать признак многослойности объекта.

        ### Синтаксис Automation

        ```
        MultiLayer = Object.MultiLayer       # Получить свойство (* )
        Object.MultiLayer = MultiLayer       # Установить свойство (* )
        MultiLayer = Object.GetMultiLayer()  # Получить свойство (**)
        Object.SetMultiLayer( MultiLayer )   # Установить свойство (**)
        ```
        """
        ...
    @MultiLayer.setter
    def MultiLayer(self, value: bool): ...

    @property
    def Scale(self) -> float:  # iinsertionfragment_scale.html
        """
        ## Scale - Масштаб

        Тип данных: double

        Свойство позволяет устанавливать и получать масштаб вставляемого фрагмента.

        ### Синтаксис Automation

        ```
        Scale = Object.Scale       # Получить свойство (* )
        Object.Scale = Scale       # Установить свойство (* )
        Scale = Object.GetScale()  # Получить свойство (**)
        Object.SetScale( Scale )   # Установить свойство (**)
        ```
        """
        ...
    @Scale.setter
    def Scale(self, value: float): ...

    @property
    def VariableTable(self) -> IVariableTable:  # iinsertionfragment_variabletable.html
        """
        ## VariableTable - Таблица переменных

        Тип данных: указатель на интерфейс таблицы переменных IVariableTable

        1. Свойство позволяет получать интерфейс таблицы переменных.

        2. Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        VariableTable = Object.VariableTable()     # Получить свойство (* )
        VariableTable = Object.GetVariableTable()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def Variables(self):  # iinsertionfragment_variables.html
        ...

    @property
    def VariablesCount(self) -> int:  # iinsertionfragment_variablescount.html
        """
        ## VariablesCount - Получить количество параметрических переменных

        Тип данных: long

        1. Свойство позволяет получать количество параметрических переменных.

        2. Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        VariablesCount = Object.VariablesCount()     # Получить свойство (* )
        VariablesCount = Object.GetVariablesCount()  # Получить свойство (**)
        ```
        """
        ...

    def Variable(self, Index) -> IVariable7:  # iinsertionfragment_variable.html
        """
        ## Variable - Получить параметрическую переменную по имени, индексу или указателю на размер

        Тип данных: указатель на интерфейс параметрической переменной IVariable7

        1. Свойство позволяет получать интерфейс параметрической переменной.

        2. Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        Variable = Object.Variable ( VARIANT Index )    # Получить свойство (* )
        Variable = Object.GetVariable( VARIANT Index )  # Получить свойство (**)
        ```

        ### Входные параметры

        +---------------------------------------------------------------------------+
        | Index | - индекс, имя или указатель на размер параметрической переменной. |
        +---------------------------------------------------------------------------+
        """
        ...




class IInsertionView(IInsertionObject):  # iinsertionview.html
    """
    ## Интерфейс IInsertionView

    Интерфейс вставки вида из другого чертежа.

    Интерфейс позволяет задавать и получать параметры вставки вида из другого чертежа.

    Дополнительный интерфейс базового интерфейса вставки фрагмента IInsertionObject. Может быть получен с помощью метода IUnknown::QueryInterface.
    """

    @property
    def CreateSpcObjects(self) -> bool:  # iinsertionview_createspcobjects.html
        """
        ## CreateSpcObjects - Создавать объекты спецификации

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        CreateSpcObjects = Object.CreateSpcObjects       # Получить свойство (* )
        Object.CreateSpcObjects = CreateSpcObjects       # Установить свойство (* )
        CreateSpcObjects = Object.GetCreateSpcObjects()  # Получить свойство (**)
        Object.SetCreateSpcObjects( CreateSpcObjects )   # Установить свойство (**)
        ```
        """
        ...
    @CreateSpcObjects.setter
    def CreateSpcObjects(self, value: bool): ...

    @property
    def Label(self):
        ...

    @property
    def OwnerDocumentParams(self) -> bool:  # iinsertionview_ownerdocumentparams.html
        """
        ## OwnerDocumentParams - Параметры документа

        Тип данных: BOOL

        Значения свойства:

        +---------------------------------------------------------+
        | TRUE  | - отображать с параметрами документа владельца, |
        +---------------------------------------------------------+
        | FALSE | - отображать с параметрами документа источника. |
        +---------------------------------------------------------+

        Свойство позволяет устанавливать и получать способ отображения параметров.

        ### Синтаксис Automation

        ```
        OwnerDocumentParams = Object.OwnerDocumentParams       # Получить свойство (* )
        Object.OwnerDocumentParams = OwnerDocumentParams       # Установить свойство (* )
        OwnerDocumentParams = Object.GetOwnerDocumentParams()  # Получить свойство (**)
        Object.SetOwnerDocumentParams( OwnerDocumentParams )   # Установить свойство (**)
        ```
        """
        ...
    @OwnerDocumentParams.setter
    def OwnerDocumentParams(self, value: bool): ...

    @property
    def ThinLines(self) -> bool:  # iinsertionview_thinlines.html
        """
        ## ThinLines - В тонких линиях

        Тип данных: BOOL

        Свойство позволяет устанавливать и получать признак отрисовки в тонких линиях.

        ### Синтаксис Automation

        ```
        ThinLines = Object.ThinLines       # Получить свойство (* )
        Object.ThinLines = ThinLines       # Установить свойство (* )
        ThinLines = Object.GetThinLines()  # Получить свойство (**)
        Object.SetThinLines( ThinLines )   # Установить свойство (**)
        ```
        """
        ...
    @ThinLines.setter
    def ThinLines(self, value: bool): ...

    @property
    def ViewName(self) -> str:  # iinsertionview_viewname.html
        """
        ## ViewName - Имя вида

        Тип данных: BSTR

        1. Свойство позволяет получать имя вида.

        2. Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        ViewName = Object.ViewName       # Получить свойство (* )
        ViewName = Object.GetViewName()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def ViewNumber(self) -> int:  # iinsertionview_viewnumber.html
        """
        ## ViewNumber - Номер вида

        Тип данных: long

        Свойство позволяет устанавливать и получать номер вида.

        ### Синтаксис Automation

        ```
        ViewNumber = Object.ViewNumber       # Получить свойство (* )
        Object.ViewNumber = ViewNumber       # Установить свойство (* )
        ViewNumber = Object.GetViewNumber()  # Получить свойство (**)
        Object.SetViewNumber( ViewNumber )   # Установить свойство (**)
        ```
        """
        ...
    @ViewNumber.setter
    def ViewNumber(self, value: int): ...




class IOleDrawingObject(IDrawingObject):  # ioledrawingobject.html
    """
    ## Интерфейс IOleDrawingObject

    Интерфейс OLE-объекта.

    Интерфейс позволяет задавать параметры OLE-объекта.

    1. Интерфейс можно получить у коллекции OLE-объектов, используя свойство IOleDrawingObjects::OleDrawingObject или метод IOleDrawingObjects::Add.

    2. После задания параметров OLE-объекта требуется вызвать метод IDrawingObject::Update.

    3. OLE-объект можно создать двумя способами:

    •внедрить телом в документ (для этого требуется задать свойство IOleDrawingObject::ClassId),

    •вставить по ссылке из файла (для этого требуется задать свойство IOleDrawingObject::FileName).
    """

    @property
    def ClassId(self) -> str:  # ioledrawingobject_classid.html
        """
        ## ClassId - Идентификатор объекта

        Тип данных: BSTR

        1. Свойство позволяет устанавливать и получать идентификатор OLE-объекта.

        2. Значение свойства нельзя изменить после создания OLE-объекта.

        3. Если задано ClassId, объект вставляется телом, тип объекта определяется по ClassId.

        ### Синтаксис Automation

        ```
        ClassId = Object.ClassId       # Получить свойство (* )
        Object.ClassId = ClassId       # Установить свойство (* )
        ClassId = Object.GetClassId()  # Получить свойство (**)
        Object.SetClassId( ClassId )   # Установить свойство (**)
        ```
        """
        ...
    @ClassId.setter
    def ClassId(self, value: str): ...

    @property
    def FileName(self) -> str:  # ioledrawingobject_filename.html
        """
        ## FileName - Имя файла объекта

        Тип данных: BSTR

        Свойство позволяет устанавливать и получать имя файла объекта.

        ### Синтаксис Automation

        ```
        FileName = Object.FileName       # Получить свойство (* )
        Object.FileName = FileName       # Установить свойство (* )
        FileName = Object.GetFileName()  # Получить свойство (**)
        Object.SetFileName( FileName )   # Установить свойство (**)
        ```
        """
        ...
    @FileName.setter
    def FileName(self, value: str): ...

    @property
    def InsertionType(self) -> bool:  # ioledrawingobject_insertiontype.html
        """
        ## InsertionType - Способ вставки

        Тип данных: BOOL

        Значения свойства:

        +-------------------------------+
        | TRUE  | - в виде значка,      |
        +-------------------------------+
        | FALSE | - в виде содержимого. |
        +-------------------------------+

        Свойство позволяет устанавливать и получать способ вставки OLE-объекта.

        ### Синтаксис Automation

        ```
        InsertionType = Object.InsertionType       # Получить свойство (* )
        Object.InsertionType = InsertionType       # Установить свойство (* )
        InsertionType = Object.GetInsertionType()  # Получить свойство (**)
        Object.SetInsertionType( InsertionType )   # Установить свойство (**)
        ```
        """
        ...
    @InsertionType.setter
    def InsertionType(self, value: bool): ...

    @property
    def Link(self) -> bool:  # ioledrawingobject_link.html
        """
        ## Link - Связать файл и объект

        Тип данных: BOOL

        Свойство позволяет устанавливать и получать признак связи файла и OLE-объекта.

        ### Синтаксис Automation

        ```
        Link = Object.Link       # Получить свойство (* )
        Object.Link = Link       # Установить свойство (* )
        Link = Object.GetLink()  # Получить свойство (**)
        Object.SetLink( Link )   # Установить свойство (**)
        ```
        """
        ...
    @Link.setter
    def Link(self, value: bool): ...

    @property
    def Scale(self) -> float:  # ioledrawingobject_scale.html
        """
        ## Scale - Масштаб

        Тип данных: double

        Свойство позволяет устанавливать и получать масштаб OLE-объекта.

        ### Синтаксис Automation

        ```
        Scale = Object.Scale       # Получить свойство (* )
        Object.Scale = Scale       # Установить свойство (* )
        Scale = Object.GetScale()  # Получить свойство (**)
        Object.SetScale( Scale )   # Установить свойство (**)
        ```
        """
        ...
    @Scale.setter
    def Scale(self, value: float): ...

    def Close(self, Save) -> bool:  # ioledrawingobject_close.html
        """
        ## Close - Завершить редактирование

        ### Синтаксис Automation

        ```
        BOOL Close( BOOL Save );
        ```

        ### Входные параметры

        +-------------------------------+
        | Save | - сохранить изменения. |
        +-------------------------------+

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def DoVerb(self, iVerb):  # ioledrawingobject_doverb.html
        """
        ## DoVerb - Запустить редактирование вставки OLE-объекта

        ### Синтаксис Automation

        ```
        LPDISPATCH DoVerb( long iVerb );
        ```

        ### Входные параметры

        +---------------------------------------------------------------------+
        | iVerb | - действие, которое должно быть выполнено над OLE-объектом. |
        +---------------------------------------------------------------------+

        ### Возвращаемое значение

        - Указатель на интерфейс OLE-объекта.
        """
        ...

    def GetPlacement(self) -> bool:  # ioledrawingobject_getplacement.html
        """
        ## GetPlacement - Получить местоположение объекта

        Метод позволяет получить точку привязки и угол поворота растрового объекта.

        ### Синтаксис Automation

        ```
        BOOL GetPlacement( double * X,
        double * Y,
        double * Angle,
        BOOL * MirrorSymmetry );
        ```

        ### Входные параметры

        +----------------------------------------------------------+
        | X, Y           | - координаты точки привязки,            |
        +----------------------------------------------------------+
        | Angle          | - угол поворота объекта,                |
        +----------------------------------------------------------+
        | MirrorSymmetry | - признак зеркальной симметрии объекта. |
        +----------------------------------------------------------+

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def SetPlacement(self, X, Y, Angle, MirrorSymmetry) -> bool:  # ioledrawingobject_setplacement.html
        """
        ## SetPlacement - Установить местоположение объекта

        Метод позволяет установить точку привязки и угол поворота растрового объекта.

        ### Синтаксис Automation

        ```
        BOOL SetPlacement( double X,
        double Y,
        double Angle,
        BOOL * MirrorSymmetry );
        ```

        ### Входные параметры

        +----------------------------------------------------------+
        | X, Y           | - координаты точки привязки,            |
        +----------------------------------------------------------+
        | Angle          | - угол поворота объекта,                |
        +----------------------------------------------------------+
        | MirrorSymmetry | - признак зеркальной симметрии объекта. |
        +----------------------------------------------------------+

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...




class IInsertionDefinition(IKompasAPIObject):  # iinsertiondefinition.html
    """
    ## Интерфейс IInsertionDefinition

    Описание вставки фрагмента или вида.

    Интерфейс позволяет задавать и получать параметры описания вставки фрагмента или вида.

    Данный интерфейс можно получить следующими способами:

    •с помощью метода IInsertionObject::InsertionDefinition.

    •с помощью метода IInsertionsManager::InsertionDefinition.
    """

    @property
    def FileName(self) -> str:  # iinsertiondefinition_filename.html
        """
        ## FileName - Имя файла источника

        Тип данных: BSTR

        Свойство позволяет устанавливать и получать имя файла источника.

        ### Синтаксис Automation

        ```
        FileName = Object.FileName       # Получить свойство (* )
        Object.FileName = FileName       # Установить свойство (* )
        FileName = Object.GetFileName()  # Получить свойство (**)
        Object.SetFileName( FileName )   # Установить свойство (**)
        ```
        """
        ...
    @FileName.setter
    def FileName(self, value: str): ...

    @property
    def InsertionObjectsCount(self) -> int:  # iinsertiondefinition_insertionobjectscount.html
        """
        ## InsertionObjectsCount - Количество вставок

        Тип данных: long

        1. Свойство позволяет получать количество вставок.

        2. Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        InsertionObjectsCount = Object.InsertionObjectsCount()     # Получить свойство (* )
        InsertionObjectsCount = Object.GetInsertionObjectsCount()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def InsertionType(self) -> int:  # iinsertiondefinition_insertiontype.html
        """
        ## InsertionType - Способ вставки

        Тип данных: из перечисления ksInsertionTypeEnum

        1. Свойство позволяет получать способ вставки фрагмента.

        2. Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        InsertionType = Object.InsertionType()     # Получить свойство (* )
        InsertionType = Object.GetInsertionType()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def Name(self) -> str:  # iinsertiondefinition_name.html
        """
        ## Name - Имя описания

        Тип данных: BSTR

        Свойство позволяет устанавливать и получать имя описания вставки.

        ### Синтаксис Automation

        ```
        Name = Object.Name       # Получить свойство (* )
        Object.Name = Name       # Установить свойство (* )
        Name = Object.GetName()  # Получить свойство (**)
        Object.SetName( Name )   # Установить свойство (**)
        ```
        """
        ...
    @Name.setter
    def Name(self, value: str): ...

    def Open(self, Visible, ReadOnly) -> IKompasDocument2D:  # iinsertiondefinition_open.html
        """
        ## Open - Открыть документ-источник на редактирование

        Метод позволяет открыть документ-источник на редактирование.

        ### Синтаксис Automation

        ```
        LPDISPATCH Open( BOOL Visible,
        BOOL ReadOnly );
        ```

        ### Выходные параметры

        +----------------------------------------------------------------+
        | Visible  | - открыть в видимом режиме (по умолчанию - TRUE),   |
        +----------------------------------------------------------------+
        | ReadOnly | - открыть только для чтения (по умолчанию - FALSE). |
        +----------------------------------------------------------------+

        ### Возвращаемое значение

        - указатель на интерфейс документа IKompasDocument2D.
        """
        ...




class IInsertionParameters(IKompasAPIObject):  # iinsertionparameters.html
    """
    ## Интерфейс IInsertionParameters

    Параметры вставки фрагмента и растрового объекта в ячейку таблицы.

    Интерфейс позволяет получить и изменить параметры вставки фрагмента и растрового объекта в текст ячейки таблицы.

    Интерфейс можно получить с помощью свойства ITextLine::TextLineData.

    Чтобы вставить фрагмент, свойство ITextLine::TextLineType должно иметь значение ksTLFragment, чтобы вставить растровый объект - ksTLRaster.
    """

    @property
    def Angle(self) -> int:  # iinsertionparameters_angle.html
        """
        ## Angle - Угол

        Тип данных: ksAngleEnum

        ### Синтаксис Automation

        ```
        Angle = Object.Angle       # Получить свойство (* )
        Object.Angle = Angle       # Установить свойство (* )
        Angle = Object.GetAngle()  # Получить свойство (**)
        Object.SetAngle( Angle )   # Установить свойство (**)
        ```
        """
        ...
    @Angle.setter
    def Angle(self, value: int): ...

    @property
    def FileName(self) -> str:  # iinsertionparameters_filename.html
        """
        ## FileName - Имя файла-источника

        Тип данных: BSTR

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        FileName = Object.FileName       # Получить свойство (* )
        FileName = Object.GetFileName()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def Height(self) -> float:  # iinsertionparameters_height.html
        """
        ## Height - Высота вставки

        Тип данных: double

        ### Синтаксис Automation

        ```
        Height = Object.Height       # Получить свойство (* )
        Object.Height = Height       # Установить свойство (* )
        Height = Object.GetHeight()  # Получить свойство (**)
        Object.SetHeight( Height )   # Установить свойство (**)
        ```
        """
        ...
    @Height.setter
    def Height(self, value: float): ...

    @property
    def ImageResolution(self) -> float:  # iinsertionparameters_imageresolution.html
        """
        ## ImageResolution - Разрешение изображения для вставки растра

        Тип данных: double

        ### Синтаксис Automation

        ```
        ImageResolution = Object.ImageResolution       # Получить свойство (* )
        Object.ImageResolution = ImageResolution       # Установить свойство (* )
        ImageResolution = Object.GetImageResolution()  # Получить свойство (**)
        Object.SetImageResolution( ImageResolution )   # Установить свойство (**)
        ```
        """
        ...
    @ImageResolution.setter
    def ImageResolution(self, value: float): ...

    @property
    def InsertionDefinition(self) -> IInsertionDefinition:  # iinsertionparameters_insertiondefinition.html
        """
        ## InsertionDefinition - описание вставки фрагмента

        Тип данных: указатель на интерфейс IInsertionDefinition

        ### Синтаксис Automation

        ```
        InsertionDefinition = Object.InsertionDefinition       # Получить свойство (* )
        Object.InsertionDefinition = InsertionDefinition       # Установить свойство (* )
        InsertionDefinition = Object.GetInsertionDefinition()  # Получить свойство (**)
        Object.SetInsertionDefinition( InsertionDefinition )   # Установить свойство (**)
        ```
        """
        ...
    @InsertionDefinition.setter
    def InsertionDefinition(self, value: IInsertionDefinition): ...

    @property
    def Palette(self) -> float:  # iinsertionparameters_palette.html
        """
        ## Palette - Цветовая палитра (количество разрядов)

        Тип данных: double

        ### Синтаксис Automation

        ```
        Palette = Object.Palette       # Получить свойство (* )
        Object.Palette = Palette       # Установить свойство (* )
        Palette = Object.GetPalette()  # Получить свойство (**)
        Object.SetPalette( Palette )   # Установить свойство (**)
        ```
        """
        ...

    @property
    def Scale(self) -> float:  # iinsertionparameters_scale.html
        """
        ## Scale - Масштаб

        Тип данных: double

        ### Синтаксис Automation

        ```
        Scale = Object.Scale       # Получить свойство (* )
        Object.Scale = Scale       # Установить свойство (* )
        Scale = Object.GetScale()  # Получить свойство (**)
        Object.SetScale( Scale )   # Установить свойство (**)
        ```
        """
        ...
    @Scale.setter
    def Scale(self, value: float): ...

    @property
    def SourceHeight(self) -> float:  # iinsertionparameters_sourceheight.html
        """
        ## SourceHeight - Высота источника

        Тип данных: double

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        SourceHeight = Object.SourceHeight       # Получить свойство (* )
        SourceHeight = Object.GetSourceHeight()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def SourceWidth(self) -> float:  # iinsertionparameters_sourcewidth.html
        """
        ## SourceWidth - Ширина источника

        Тип данных: double

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        SourceWidth = Object.SourceWidth       # Получить свойство (* )
        SourceWidth = Object.GetSourceWidth()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def Width(self) -> float:  # iinsertionparameters_width.html
        """
        ## Width - Ширина вставки

        Тип данных: double

        ### Синтаксис Automation

        ```
        Width = Object.Width       # Получить свойство (* )
        Object.Width = Width       # Установить свойство (* )
        Width = Object.GetWidth()  # Получить свойство (**)
        Object.SetWidth( Width )   # Установить свойство (**)
        ```
        """
        ...
    @Width.setter
    def Width(self, value: float): ...

    def AutoScale(self, ByHeight) -> bool:  # iinsertionparameters_autoscale.html
        """
        ## AutoScale - Подобрать масштаб

        ### Синтаксис Automation

        ```
        BOOL AutoScale( BOOL ByHeight );
        ```

        ### Входные параметры

        +---------------------------------------------------+
        | ByHeight | - TRUE - по высоте, - FALSE по ширине. |
        +---------------------------------------------------+

        ### Возвращаемое значение

        +--------------------------+
        | TRUE | - в случае удачи. |
        +--------------------------+
        """
        ...




class IDrawingContainer(IDispatch):  # idrawingcontainer.html
    """
    ## Интерфейс IDrawingContainer

    Интерфейс контейнера объектов вида графического документа.

    Позволяет получить коллекции объектов, которые входят в состав вида графического документа или его объекта.

    Дополнительный интерфейс вида. Данный интерфейс можно получить у вида IView посредством вызова метода IUnknown::QueryInterface (const GUID far& IID, void** pif).
    """

    @property
    def Arcs(self) -> IArcs:  # idrawingcontainer_arcs.html
        """
        ## Arcs - Коллекция дуг

        Тип данных: указатель на интерфейс коллекции дуг IArcs

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        IArcs = iObject.Arcs;       # Получить свойство(* )
        IArcs = iObject.GetArcs();  # Получить свойство (**)
        ```
        """
        ...

    @property
    def Beziers(self) -> IBeziers:  # idrawingcontainer_beziers.html
        """
        ## Beziers - Коллекция Bezier-сплайнов

        Тип данных: указатель на интерфейс IBeziers

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        Beziers = Object.Beziers       # Получить свойство(* )
        Beziers = Object.GetBeziers()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def Circles(self) -> ICircles:  # idrawingcontainer_circles.html
        """
        ## Circles - Коллекция окружностей

        Тип данных: указатель на интерфейс ICircles

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        Circles = Object.Circles       # Получить свойство(* )
        Circles = Object.GetCircles()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def Colourings(self) -> IColourings:  # idrawingcontainer_colourings.html
        """
        ## Colourings - Коллекция заливок

        Тип данных: указатель на интерфейс IColourings

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        Colourings = Object.Colourings       # Получить свойство(* )
        Colourings = Object.GetColourings()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def ConicCurves(self) -> IConicCurves:  # idrawingcontainer_coniccurves.html
        """
        ## ConicCurves - Коллекция конических кривых

        Тип данных: Указатель на интерфейс IConicCurves.

        Свойство доступно только для чтения.

        Версия Компас v18.1

        ### Синтаксис Automation

        ```
        ConicCurves = Object.ConicCurves       # Получить свойство(* )
        ConicCurves = Object.GetConicCurves()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def DrawingContours(self) -> IDrawingContours:  # idrawingcontainer_drawingcontours.html
        """
        ## DrawingContours - Коллекция контуров

        Тип данных: указатель на интерфейс коллекции контуров IDrawingContours

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        IDrawingContours = iObject.DrawingContours;       # Получить свойство(* )
        IDrawingContours = iObject.GetDrawingContours();  # Получить свойство (**)
        ```
        """
        ...

    @property
    def DrawingTexts(self) -> IDrawingTexts:  # idrawingcontainer_drawingtexts.html
        """
        ## DrawingTexts - Коллекция текстов на чертеже

        Тип данных: указатель на интерфейс коллекции текстов на чертеже IDrawingTexts.

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        DrawingTexts = iObject.DrawingTexts       # Получить свойство(* )
        DrawingTexts = iObject.GetDrawingTexts()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def EllipseArcs(self) -> IEllipseArcs:  # idrawingcontainer_ellipsearcs.html
        """
        ## EllipseArcs - Коллекция дуг эллипсов

        Тип данных: указатель на интерфейс IEllipseArcs

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        EllipseArcs = Object.EllipseArcs       # Получить свойство(* )
        EllipseArcs = Object.GetEllipseArcs()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def Ellipses(self) -> IEllipses:  # idrawingcontainer_ellipses.html
        """
        ## Ellipses - Коллекция эллипсов

        Тип данных: указатель на интерфейс IEllipses

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        Ellipses = Object.Ellipses       # Получить свойство(* )
        Ellipses = Object.GetEllipses()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def Equidistants(self) -> IEquidistants:  # idrawingcontainer_equidistants.html
        """
        ## Equidistants - Коллекция эквидистант

        Тип данных: указатель на интерфейс IEquidistants

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        Equidistants = Object.Equidistants       # Получить свойство(* )
        Equidistants = Object.GetEquidistants()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def Hatches(self) -> IHatches:  # idrawingcontainer_hatches.html
        """
        ## Hatches - Коллекция штриховок

        Тип данных: указатель на интерфейс IHatches

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        Hatches = Object.Hatches       # Получить свойство(* )
        Hatches = Object.GetHatches()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def InsertionObjects(self) -> IInsertionObjects:  # idrawingcontainer_insertionobjects.html
        """
        ## InsertionObjects - Коллекция вставок фрагментови видов другого чертежа

        Тип данных: указатель на интерфейс IInsertionObjects

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        InsertionObjects = Object.InsertionObjects       # Получить свойство(* )
        InsertionObjects = Object.GetInsertionObjects()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def LineSegments(self) -> ILineSegments:  # idrawingcontainer_linesegments.html
        """
        ## LineSegments - Коллекция отрезков

        Тип данных: указатель на интерфейс коллекции отрезков в документе ILineSegments

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        LineSegments = iObject.LineSegments       # Получить свойство(* )
        LineSegments = iObject.GetLineSegments()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def Lines(self) -> ILines:  # idrawingcontainer_lines.html
        """
        ## Lines - Коллекция линий

        Тип данных: указатель на интерфейс ILines

        1. Свойство позволяет получать интерфейс коллекции линий.

        2. Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        Lines = Object.Lines       # Получить свойство(* )
        Lines = Object.GetLines()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def MacroObjects(self) -> IMacroObjects:  # idrawingcontainer_macroobjects.html
        """
        ## MacroObjects - Коллекция макроэлементов

        Тип данных: указатель на интерфейс IMacroObjects

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        MacroObjects = Object.MacroObjects       # Получить свойство(* )
        MacroObjects = Object.GetMacroObjects()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def Multilines(self) -> IMultilines:  # idrawingcontainer_multilines.html
        """
        ## Multilines - Коллекция мультилиний

        Тип данных: указатель на интерфейс коллекции дуг IMultilines

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        IMultilines = iObject.Multilines;       # Получить свойство(* )
        IMultilines = iObject.GetMultilines();  # Получить свойство (**)
        ```
        """
        ...

    @property
    def Nurbses(self) -> INurbses:  # idrawingcontainer_nurbses.html
        """
        ## Nurbses - Коллекция Nurbs-сплайнов

        Тип данных: указатель на интерфейс INurbses

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        Nurbses = Object.Nurbses     # Получить свойство(* )
        Nurbses = Object.GetNurbses  # Получить свойство (**)
        ```
        """
        ...

    @property
    def NurbsesByPoints(self) -> INurbses:  # idrawingcontainer_nurbsesbypoints.html
        """
        ## NurbsesByPoints - Получить коллекцию NURBS-кривых по точкам

        Тип данных: указатель на интерфейс INurbses

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        NurbsesByPoints = Object.NurbsesByPoints     # Получить свойство(* )
        NurbsesByPoints = Object.GetNurbsesByPoints  # Получить свойство (**)
        ```
        """
        ...

    @property
    def OleDrawingObjects(self) -> IOleDrawingObjects:  # idrawingcontainer_oledrawingobjects.html
        """
        ## OleDrawingObjects - Коллекция OLE-объектов

        Тип данных: указатель на интерфейс IOleDrawingObjects

        1. Свойство позволяет получать интерфейс коллекции OLE-объектов.

        2. Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        OleDrawingObjects = Object.OleDrawingObjects       # Получить свойство(* )
        OleDrawingObjects = Object.GetOleDrawingObjects()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def Points(self) -> IPoints:  # idrawingcontainer_points.html
        """
        ## Points - Коллекция точек

        Тип данных: указатель на интерфейс IPoints

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        Points = Object.Points       # Получить свойство(* )
        Points = Object.GetPoints()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def PolyLines2D(self) -> IPolyLines2D:  # idrawingcontainer_polylines2d.html
        """
        ## PolyLines2D - Коллекция 2D-ломаных

        Тип данных: указатель на интерфейс IPolyLines2D

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        PolyLines2D = Object.PolyLines2D       # Получить свойство(* )
        PolyLines2D = Object.GetPolyLines2D()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def Rasters(self) -> IRasters:  # idrawingcontainer_rasters.html
        """
        ## Rasters - Коллекция растровых объектов

        Тип данных: указатель на интерфейс IRasters

        1. Свойство позволяет получать интерфейс коллекции растровых объектов.

        2. Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        Rasters = Object.Rasters       # Получить свойство(* )
        Rasters = Object.GetRasters()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def Rectangles(self) -> IRectangles:  # idrawingcontainer_rectangles.html
        """
        ## Rectangles - Коллекция прямоугольников

        Тип данных: указатель на интерфейс IRectangles

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        Rectangles = Object.Rectangles       # Получить свойство(* )
        Rectangles = Object.GetRectangles()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def RegularPolygons(self) -> IRegularPolygons:  # idrawingcontainer_regularpolygons.html
        """
        ## RegularPolygons - Коллекция многоугольников

        Тип данных: указатель на интерфейс IRegularPolygons

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        RegularPolygons = Object.RegularPolygons       # Получить свойство(* )
        RegularPolygons = Object.GetRegularPolygons()  # Получить свойство (**)
        ```
        """
        ...

    def Objects(self, ObjType) -> typing.Any:  # idrawingcontainer_objects.html
        """
        ## Objects - Массив SAFEARRAY объектов, входящих в состав данного объекта вида

        Массив SAFEARRAY объектов, входящих в состав данного объекта вида.

        Тип данных: VARIANT

        Типы объектов могут быть из перечисления DrawingObjectTypeEnum. Если Null – все объекты.

        ### Синтаксис Automation

        ```
        Objects = iObject.Objects( ObjType )     # Получить свойство(* )
        Objects = iObject.GetObjects( ObjType )  # Получить свойство (**)
        ```

        ### Входные параметры

        +--------------------------------+
        | ObjType | Тип объекта VARIANT. |
        +--------------------------------+
        """
        ...




class IBoundariesObject(IDispatch):  # iboundariesobject.html
    """
    ## Интерфейс IBoundariesObject

    Интерфейс объекта с границами.

    Позволяет редактировать границы объекта. Дополнительный интерфейс штриховки IHatch, заливки IColouring и растрового объекта IRaster.

    Данный интерфейс можно получить с помощью метода IUnknown::QueryInterface.
    """

    @property
    def Boundaries(self):  # iboundariesobject_boundaries.html
        ...

    def AddBoundaries(self, Objects, DeleteSource) -> bool:  # iboundariesobject_addboundaries.html
        """
        ## AddBoundaries - Добавить объекты в границу

        Метод позволяет добавить объекты в границу.

        ### Синтаксис Automation

        ```
        BOOL AddBoundaries( VARIANT Objects,
        BOOL DeleteSource );
        ```

        ### Входные параметры

        +------------------------------------------------------------------------------------------+
        | Objects      | - массив SAFEARRAY | VT_DISPATCH объектов, добавляемых в границу,         |
        +------------------------------------------------------------------------------------------+
        | DeleteSource | - TRUE - удалить исходные объекты, - FALSE - копировать исходные объекты. |
        +------------------------------------------------------------------------------------------+

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def Clear(self) -> bool:  # iboundariesobject_clear.html
        """
        ## Clear - Удалить все границы

        Метод позволяет удалить границы.

        ### Синтаксис Automation

        ```
        BOOL Clear();
        ```

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...




class IArcs(IDrawingObjects):  # iarcs.html
    """
    ## Интерфейс IArcs

    Интерфейс коллекции дуг.

    Интерфейс позволяет получить коллекции дуг.

    Получить интерфейс коллекции дуг можно, используя метод контейнера объектов IDrawingContainer::Arcs.
    """

    def Add(self) -> IArc:  # iarcs_add.html
        """
        ## Add - Создать дугу (добавить дугу в коллекцию)

        Метод позволяет создать новый интерфейс дуги. После получения нового интерфейса нужно задать параметры дуги и вызвать метод IDrawingObject::Update.

        ### Синтаксис Automation

        ```
        LPDISPATCH Add();
        ```

        ### Возвращаемое значение

        +---------------------------------------------------------+
        | Result | - Указатель на базовый интерфейс для дуг IArc. |
        +---------------------------------------------------------+
        """
        ...

    def Arc(self, Index) -> IArc:  # iarcs_arc.html
        """
        ## Arc - Дуга, заданная по индексу

        Тип данных: указатель на интерфейс дуги IArc.

        1. Свойство доступно только для чтения.

        2. В качестве индекса могут использоваться следующие типы:

        •Индекс объекта в коллекции,

        •Ссылка на объект (reference),

        ### Синтаксис Automation

        ```
        Arc = iObject.Arc( Index );     # Получить свойство(* )
        Arc = iObject.GetArc( Index );  # Получить свойство (**)
        ```

        ### Входные параметры

        +-------------------------------------------------------------------------------------------------------------------------------+
        | Index (Variant) | - Индекс отрезка в коллекции. Поддерживаются следующие типы: – VT_I4 - индекс объекта, – reference объекта. |
        +-------------------------------------------------------------------------------------------------------------------------------+
        """
        ...




class IBeziers(IDrawingObjects):  # ibeziers.html
    """
    ## Интерфейс IBeziers

    Коллекция кривых Безье.

    Позволяет создавать и получать кривые Безье на чертеже.

    Получить интерфейс можно, используя метод контейнера графических объектов IDrawingContainer::Beziers.
    """

    def Add(self) -> IBezier:  # ibeziers_add.html
        """
        ## Add - Создать новый элемент и добавить его в коллекцию

        ### Синтаксис Automation

        ```
        LPDISPATCH Add();
        ```

        ### Возвращаемое значение

        - Указатель на интерфейс кривой Безье IBezier.
        """
        ...

    def Bezier(self, Index) -> IBezier:  # ibeziers_bezier.html
        """
        ## Bezier - Кривая Безье, заданная по индексу

        Тип данных: указатель на интерфейс кривой Безье на чертеже Bezier

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        Bezier = iObject.Bezier( Index );     # Получить свойство(* )
        Bezier = iObject.GetBezier( Index );  # Получить свойство (**)
        ```
        """
        ...




class ICircles(IDrawingObjects):  # icircles.html
    """
    ## Интерфейс ICircles

    Коллекция окружностей.

    Интерфейс позволяет создавать и получать окружности на чертеже.

    Получить интерфейс можно используя метод контейнера графических объектов IDrawingContainer::Circles.
    """

    def Add(self) -> ICircle:  # icircles_add.html
        """
        ## Add - Создать новый элемент и добавить его в коллекцию

        ### Синтаксис Automation

        ```
        LPDISPATCH Add();
        ```

        ### Возвращаемое значение

        - Указатель на интерфейс окружности на чертеже ICircle.
        """
        ...

    def Circle(self, Index) -> ICircle:  # icircles_circle.html
        """
        ## Circle - Окружность, заданная по индексу

        Тип данных: указатель на интерфейс окружности на чертеже ICircle

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        Circle = iObject.Circle( Index );     # Получить свойство(* )
        Circle = iObject.GetCircle( Index );  # Получить свойство (**)
        ```
        """
        ...




class IColourings(IDrawingObjects):  # icolourings.html
    """
    ## Интерфейс IColourings

    Интерфейс коллекции заливок.

    Позволяет создавать и получать заливки.

    Получить интерфейс можно, используя метод контейнера графических объектов IDrawingContainer::Colourings.
    """

    def Add(self) -> IColouring:  # icolourings_add.html
        """
        ## Add - Создать новый элемент и добавить его в коллекцию

        ### Синтаксис Automation

        ```
        LPDISPATCH Add();
        ```

        ### Возвращаемое значение

        - Указатель на интерфейс заливки IColouring.
        """
        ...

    def Colouring(self, Index) -> IColouring:  # icolourings_colouring.html
        """
        ## Colouring - Заливка, заданная по индексу

        Тип данных: указатель на интерфейс заливки IColouring

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        Colouring = iObject.Colouring ( Index );    # Получить свойство(* )
        Colouring = iObject.GetColouring( Index );  # Получить свойство (**)
        ```
        """
        ...




class IConicCurves(IDrawingObjects):  # iconiccurves.html
    """
    ## Интерфейс IConicCurves

    Интерфейс коллекции конических кривых.
    """

    def Add(self) -> IConicCurve:  # iconiccurves_add.html
        """
        ## Add - Создает новый элемент и добавляет его в коллекцию

        1. Метод позволяет создать новый интерфейс конической кривой.

        2. После получения нового интерфейса нужно задать параметры операции и вызвать метод IModelObject::Update.

        ### Синтаксис Automation

        ```
        LPDISPATCH Add();
        ```

        ### Возвращаемое значение

        - Указатель на интерфейс IConicCurve.
        """
        ...

    def ConicCurve(self, Index) -> IConicCurve:  # iconiccurves_coniccurve.html
        """
        ## ConicCurve - Возвращает элемент, заданный по индексу или по имени

        Тип данных: Указатель на интерфейс IConicCurve.

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        ConicCurve = Object.ConicCurve( Index )     # Получить свойство (* )
        ConicCurve = Object.GetConicCurve( Index )  # Получить свойство (**)
        ```

        ### Входные параметры

        +--------------------------------------------------+
        | VARIANT Index | - индекс или reference элемента. |
        +--------------------------------------------------+
        """
        ...




class IDrawingContours(IDrawingObjects):  # idrawingcontours.html
    """
    ## Интерфейс IDrawingContours

    Интерфейс коллекции контуров

    Позволяет получать и создавать контуры.

    Получить интерфейс можно, используя свойство контейнера графических объектов IDrawingContainer::DrawingContours.
    """

    def Add(self) -> IDrawingContour:  # idrawingcontours_add.html
        """
        ## Add - Создать контур (добавить в коллекцию)

        ### Синтаксис Automation

        ```
        LPDISPATCH Add();
        ```

        ### Возвращаемое значение

        - Указатель на интерфейс контура IDrawingContour.
        """
        ...

    def AddBooleanResultContours(self, Contour1, Contour2, BooleanType) -> list[IDispatch]:  # idrawingcontours_addbooleanresultcontours.html
        """
        ## AddBooleanResultContours - Булева операция над замкнутыми и не самопересекающимися кривыми

        1. Кривые должны быть замкнутыми и не иметь самопересечений.

        2. Если кривые не подходят или не пересекаются, вернется пустой массив.

        3. Если возвращается один объект, то тип VARIANT будет VT_DISPATCH.

        4. Если возвращается несколько объектов, то тип VARIANT будет VT_ARRAY | VT_DISPATCH.

        ### Синтаксис Automation

        ```
        VARIANT AddBooleanResultContours( IDrawingObject * Contour1,IDrawingObject * Contour2, ksBooleanType BooleanType );
        ```

        ### Входные параметры

        +---------------------------------------------------------------------+
        | Contour1    | - 1-й контур или кривая,                              |
        +---------------------------------------------------------------------+
        | Contour2    | - 2-й контур или кривая,                              |
        +---------------------------------------------------------------------+
        | BooleanType | - тип булевой операции из перечисления ksBooleanType. |
        +---------------------------------------------------------------------+

        Метод позволяет выполнять над контурами операции пересечения, вычитания и объединения.

        ### Возвращаемое значение

        - Массив контуров IDrawingContour в виде SAFEARRAY DISPATCH - VT_ARRAY | VT_DISPATCH.
        """
        ...

    def DrawingContour(self, Index) -> IDrawingContour:  # idrawingcontours_drawingcontour.html
        """
        ## DrawingContour - Контур, заданный по индексу

        Тип данных: указатель на интерфейс контура IDrawingContour

        1. Свойство доступно только для чтения.

        2. В качестве индекса может использоваться индекс в коллекции и reference объекта.

        ### Синтаксис Automation

        ```
        DrawingContour = iObject.DrawingContour( Index );     # Получить свойство(* )
        DrawingContour = iObject.GetDrawingContour( Index );  # Получить свойство (**)
        ```

        ### Входные параметры

        +---------------------------------------------------------------------------------------------------------------------+
        | Index (Variant) | - Индекс узла в коллекции. Поддерживаются следующие типы: - VT_I4 - индекс линии разреза/сечения. |
        +---------------------------------------------------------------------------------------------------------------------+
        """
        ...

    def MakeEncloseContours(self, Objects, X, Y, ForHatch) -> IDrawingGroup:  # idrawingcontours_makeenclosecontours.html
        """
        ## MakeEncloseContours - Вернуть временную группу контуров, охватывающих точку

        Если список объектов не задан для построения контура, используются все объекты вида:

        •При cоздании контуров для штриховки используются объекты со стилем основная и утолщенная, объекты других стилей не учитываются.

        •При cоздании простого контура с использованием любых стилей линий.

        Версия КОМПАС v22

        ### Синтаксис Automation

        ```
        LPDISPATCH MakeEncloseContours( VARIANT Objects, double X, double Y, BOOL ForHatch );
        ```

        ### Входные параметры

        +------------------------------------------------------------------------------------+
        | Objects  | - cписок объектов в виде SAFEARRAY'я DISPATCH - VT_ARRAY | VT_DISPATCH, |
        +------------------------------------------------------------------------------------+
        | X, Y     | - точка внутри контура,                                                 |
        +------------------------------------------------------------------------------------+
        | ForHatch | - признак создания контура для штриховки.                               |
        +------------------------------------------------------------------------------------+

        ### Возвращаемое значение

        - Указатель на интерфейс IDrawingGroup.
        """
        ...

    def MakeOutersContours(self, Objects, ForHatch) -> IDrawingGroup:  # idrawingcontours_makeouterscontours.html
        """
        ## MakeOutersContours - Вернуть временную группу внешних контуров

        Если список объектов не задан для построения контура, используются все объекты вида:

        •При cоздании контуров для штриховки используются объекты со стилем основная и утолщенная, объекты других стилей не учитываются.

        •При cоздании простого контура с использованием любых стилей линий.

        Версия КОМПАС v22

        ### Синтаксис Automation

        ```
        LPDISPATCH MakeOutersContours( VARIANT Objects, BOOL ForHatch );
        ```

        ### Входные параметры

        +------------------------------------------------------------------------------------+
        | Objects  | - cписок объектов в виде SAFEARRAY'я DISPATCH - VT_ARRAY | VT_DISPATCH, |
        +------------------------------------------------------------------------------------+
        | ForHatch | - признак создания контура для штриховки.                               |
        +------------------------------------------------------------------------------------+

        ### Возвращаемое значение

        - Указатель на интерфейс IDrawingGroup.
        """
        ...




class IEllipseArcs(IDrawingObjects):  # iellipsearcs.html
    """
    ## Интерфейс IEllipseArcs

    Интерфейс коллекции дуг эллипсов.

    Позволяет создавать и получать дуги эллипсов на чертеже.

    Получить интерфейс можно, используя метод контейнера графических объектов IDrawingContainer::EllipseArcs.
    """

    def Add(self) -> IEllipseArc:  # iellipsearcs_add.html
        """
        ## Add - Создать новый элемент и добавить его в коллекцию

        ### Синтаксис Automation

        ```
        LPDISPATCH Add();
        ```

        ### Возвращаемое значение

        - Указатель на интерфейс дуги эллипса IEllipseArc.
        """
        ...

    def EllipseArc(self, Index):
        ...




class IEllipses(IDrawingObjects):  # iellipses.html
    """
    ## Интерфейс IEllipses

    Интерфейс коллекции эллипсов.

    Позволяет создавать и получать эллипсы на чертеже.

    Получить интерфейс можно, используя метод контейнера графических объектов IDrawingContainer::Ellipses.
    """

    def Add(self) -> IEllipse:  # iellipses_add.html
        """
        ## Add - Создать новый элемент и добавить его в коллекцию

        ### Синтаксис Automation

        ```
        LPDISPATCH Add();
        ```

        ### Возвращаемое значение

        - Указатель на интерфейс эллипса IEllipse.
        """
        ...

    def Ellipse(self, Index) -> IEllipse:  # iellipses_ellipse.html
        """
        ## Ellipse - Эллипс, заданный по индексу

        Тип данных: указатель на интерфейс эллипса на чертеже IEllipse

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        Ellipse = iObject.Ellipse ( Index );    # Получить свойство(* )
        Ellipse = iObject.GetEllipse( Index );  # Получить свойство (**)
        ```
        """
        ...




class IEquidistants(IDrawingObjects):  # iequidistants.html
    """
    ## Интерфейс IEquidistants

    Интерфейс коллекции эквидистант.

    Позволяет создавать и получать эквидистанты кривых.

    Получить интерфейс можно, используя метод контейнера графических объектов IDrawingContainer::Equidistants.
    """

    def Add(self) -> IEquidistant:  # iequidistants_add.html
        """
        ## Add - Создать новый элемент и добавить его в коллекцию

        ### Синтаксис Automation

        ```
        LPDISPATCH Add();
        ```

        ### Возвращаемое значение

        - Указатель на интерфейс эквидистанты IEquidistant.
        """
        ...

    def Equidistant(self, Index) -> IEquidistant:  # iequidistants_equidistant.html
        """
        ## Equidistant - Эквидистанта, заданная по индексу

        Тип данных: указатель на интерфейс эквидистанты IEquidistant

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        Equidistant = iObject.Equidistant ( Index );    # Получить свойство(* )
        Equidistant = iObject.GetEquidistant( Index );  # Получить свойство (**)
        ```
        """
        ...




class IHatches(IDrawingObjects):  # ihatches.html
    """
    ## Интерфейс IHatches

    Интерфейс коллекции эквидистант.

    Позволяет создавать и получать штриховки.

    Получить интерфейс можно, используя метод контейнера графических объектов IDrawingContainer::Hatches.
    """

    def Add(self) -> IHatch:  # ihatches_add.html
        """
        ## Add - Создать новый элемент и добавить его в коллекцию

        ### Синтаксис Automation

        ```
        LPDISPATCH Add();
        ```

        ### Возвращаемое значение

        - Указатель на интерфейс штриховки IHatch.
        """
        ...

    def Hatch(self, Index) -> IHatch:  # ihatches_hatch.html
        """
        ## Hatch - Штриховка, заданная по индексу

        Тип данных: указатель на интерфейс штриховки IHatch

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        Hatch = iObject.Hatch ( Index );    # Получить свойство(* )
        Hatch = iObject.GetHatch( Index );  # Получить свойство (**)
        ```
        """
        ...




class ILines(IDrawingObjects):  # ilines.html
    """
    ## Интерфейс ILines

    Интерфейс коллекции линий.

    Позволяет создавать и получать прямые линии.

    Получить интерфейс можно, используя метод контейнера графических объектов IDrawingContainer::Lines.
    """

    def Add(self) -> ILine:  # ilines_add.html
        """
        ## Add - Создать новый элемент и добавить его в коллекцию

        ### Синтаксис Automation

        ```
        LPDISPATCH Add();
        ```

        ### Возвращаемое значение

        - Указатель на интерфейс линии ILine.
        """
        ...

    def Line(self, Index) -> ILine:  # ilines_line.html
        """
        ## Line - Линия, заданная по индексу

        Тип данных: указатель на интерфейс линии на чертеже ILine

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        Line = iObject.Line ( Index );    # Получить свойство(* )
        Line = iObject.GetLine( Index );  # Получить свойство (**)
        ```
        """
        ...




class ILineSegments(IDrawingObjects):  # ilinesegments.html
    """
    ## Интерфейс ILineSegments

    Интерфейс коллекции отрезков.

    Интерфейс позволяет получить коллекции отрезков.

    Данный интерфейс можно получить у интерфейса IDrawingContainer, используя свойство IDrawingContainer::LineSegments.
    """

    def Add(self) -> ILineSegment:  # ilinesegments_add.html
        """
        ## Add - Создать отрезок и добавить его в коллекцию

        Метод позволяет добавить отрезок в коллекцию. После получения нового интерфейса нужно задать параметры отрезка и вызвать метод IDrawingObject::Update.

        ### Синтаксис Automation

        ```
        LPDISPATCH Add;
        ```

        ### Возвращаемое значение

        - Указатель на интерфейс ILineSegment отрезка.
        """
        ...

    def LineSegment(self, Index) -> ILineSegment:  # ilinesegments_linesegment.html
        """
        ## LineSegment - Отрезок, заданный по индексу

        Тип данных: указатель на интерфейс отрезка ILineSegment

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        LineSegment = iObject.LineSegment( Index );     # Получить свойство(* )
        LineSegment = iObject.GetLineSegment( Index );  # Получить свойство (**)
        ```

        ### Входные параметры

        +-------------------------------------------------------------------------------------------------------------------------------+
        | Index (Variant) | - Индекс отрезка в коллекции. Поддерживаются следующие типы: - VT_I4 - индекс объекта, - reference объекта. |
        +-------------------------------------------------------------------------------------------------------------------------------+
        """
        ...




class IMultilines(IDrawingObjects):  # imultilines.html
    """
    ## Интерфейс IMultilines

    Интерфейс коллекции мультилиний.

    Позволяет получать и создавать объект мультилинии.

    Получить интерфейс можно, используя свойство контейнера графических объектов IDrawingContainer::Multilines.
    """

    def Add(self) -> IMultiline:  # imultilines_add.html
        """
        ## Add - Создать мультилинию (добавить в коллекцию)

        ### Синтаксис Automation

        ```
        LPDISPATCH Add();
        ```

        ### Возвращаемое значение

        - Указатель на интерфейс мультилинии IMultiLine.
        """
        ...

    def Multiline(self, Index) -> IMultiline:  # imultilines_multiline.html
        """
        ## Multiline - Мультилиния, заданная по индексу

        Тип данных: указатель на интерфейс мультилинии IMultiline

        1. Свойство доступно только для чтения.

        2. В качестве индекса может использоваться индекс в коллекции и reference объекта.

        ### Синтаксис Automation

        ```
        Multiline = iObject.Multiline( Index );     # Получить свойство(* )
        Multiline = iObject.GetMultiline( Index );  # Получить свойство (**)
        ```

        ### Входные параметры

        +---------------------------------------------------------------------------------------------------------------------+
        | Index (Variant) | - Индекс узла в коллекции. Поддерживаются следующие типы: - VT_I4 - индекс линии разреза/сечения. |
        +---------------------------------------------------------------------------------------------------------------------+
        """
        ...




class INurbses(IDrawingObjects):  # inurbses.html
    """
    ## Интерфейс INurbses

    Интерфейс коллекции NURBS-кривых.

    Позволяет создавать и получать NURBS-сплайны.

    Получить интерфейс можно, используя метод контейнера графических объектов IDrawingContainer::Nurbses.
    """

    def Add(self) -> INurbs:  # inurbses_add.html
        """
        ## Add - Создать новый элемент и добавить его в коллекцию

        ### Синтаксис Automation

        ```
        LPDISPATCH Add();
        ```

        ### Возвращаемое значение

        - Указатель на интерфейс NURBS-кривой INurbs.
        """
        ...

    def Convert(self, Curve) -> INurbs:  # inurbses_convert.html
        """
        ## Convert - Преобразовать кривую в NURBS-кривую и добавить в коллекцию

        ### Синтаксис Automation

        ```
        LPDISPATCH Convert( LPDISPATCH * Curve );
        ```

        ### Входные параметры

        +------------------------------------------------------------------------------+
        | Curve | - указатель на интерфейс кривой, которую надо преобразовать в NURBS. |
        +------------------------------------------------------------------------------+

        ### Возвращаемое значение

        - Указатель на интерфейс NURBS-кривой INurbs.
        """
        ...

    def ConvertEx(self, Curve, AproximationType, Step):
        ...

    def Nurbs(self, Index) -> INurbs:  # inurbses_nurbs.html
        """
        ## Nurbs - NURBS-кривая, заданная по индексу

        Тип данных: указатель на интерфейс NURBS-кривой INurbs

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        Nurbs = iObject.Nurbs ( Index );    # Получить свойство(* )
        Nurbs = iObject.GetNurbs( Index );  # Получить свойство (**)
        ```
        """
        ...




class IPoints(IDrawingObjects):  # ipoints.html
    """
    ## Интерфейс IPoints

    Коллекция точек.

    Интерфейс позволяет создавать и получать окружности на чертеже.

    Получить интерфейс можно, используя метод контейнера графических объектов IDrawingContainer::Points.
    """

    def Add(self) -> IPoint:  # ipoints_add.html
        """
        ## Add - Создать новый элемент и добавить его в коллекцию

        ### Синтаксис Automation

        ```
        LPDISPATCH Add();
        ```

        ### Возвращаемое значение

        - Указатель на интерфейс точки IPoint.
        """
        ...

    def Point(self, Index) -> IPoint:  # ipoints_point.html
        """
        ## Point - Точка, заданная по индексу

        Тип данных: указатель на интерфейс точки на чертеже IPoint

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        Point = iObject.Point( Index );     # Получить свойство(* )
        Point = iObject.GetPoint( Index );  # Получить свойство (**)
        ```
        """
        ...




class IPolyLines2D(IDrawingObjects):  # ipolylines2d.html
    """
    ## Интерфейс IPolyLines2D

    Интерфейс коллекции полилиний.

    Позволяет создавать и получать полилинии.

    Получить интерфейс можно, используя метод контейнера графических объектов IDrawingContainer::PolyLines2D.
    """

    def Add(self) -> IPolyLine2D:  # ipolylines2d_add.html
        """
        ## Add - Создать новый элемент и добавить его в коллекцию

        ### Синтаксис Automation

        ```
        LPDISPATCH Add();
        ```

        ### Возвращаемое значение

        - Указатель на интерфейс полилинии IPolyLine2D.
        """
        ...

    def PolyLine2D(self, Index) -> IPolyLine2D:  # ipolylines2d_polyline2d.html
        """
        ## PolyLine2D - Полилиния, заданная по индексу

        Тип данных: указатель на интерфейс полилинии IPolyLine2D.

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        PolyLine2D = iObject.PolyLine2D ( Index );    # Получить свойство(* )
        PolyLine2D = iObject.GetPolyLine2D( Index );  # Получить свойство (**)
        ```
        """
        ...




class IRasters(IDrawingObjects):  # irasters.html
    """
    ## Интерфейс IRasters

    Интерфейс коллекции растровых объектов.

    Позволяет создавать и получать растровые объекты.

    Получить интерфейс можно, используя метод контейнера графических объектов IDrawingContainer::Rasters.
    """

    def Add(self) -> IRaster:  # irasters_add.html
        """
        ## Add - Создать новый элемент и добавить его в коллекцию

        ### Синтаксис Automation

        ```
        LPDISPATCH Add();
        ```

        ### Возвращаемое значение

        - Указатель на интерфейс растрового объекта IRaster.
        """
        ...

    def Raster(self, Index) -> IRaster:  # irasters_raster.html
        """
        ## Raster - Растровый объект, заданный по индексу

        Тип данных: указатель на интерфейс растрового объекта IRaster

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        Raster = iObject.Raster ( Index );    # Получить свойство(* )
        Raster = iObject.GetRaster( Index );  # Получить свойство (**)
        ```
        """
        ...




class IRectangles(IDrawingObjects):  # irectangles.html
    """
    ## Интерфейс IRectangles

    Интерфейс коллекции прямоугольников.

    Позволяет создавать и получать прямоугольники.

    Получить интерфейс можно, используя метод контейнера графических объектов IDrawingContainer::Rectangles.
    """

    def Add(self) -> IRectangle:  # irectangles_add.html
        """
        ## Add - Создать новый элемент и добавить его в коллекцию

        ### Синтаксис Automation

        ```
        LPDISPATCH Add();
        ```

        ### Возвращаемое значение

        - Указатель на интерфейс прямоугольника IRectangle.
        """
        ...

    def Rectangle(self, Index) -> IRectangle:  # irectangles_rectangle.html
        """
        ## Rectangle - Прямоугольник, заданный по индексу

        Тип данных: указатель на интерфейс прямоугольника IRectangle

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        Rectangle = iObject.Rectangle ( Index );    # Получить свойство(* )
        Rectangle = iObject.GetRectangle( Index );  # Получить свойство (**)
        ```
        """
        ...




class IRegularPolygons(IDrawingObjects):  # iregularpolygons.html
    """
    ## Интерфейс IRegularPolygons

    Интерфейс коллекции многоугольников.

    Позволяет создавать и получать многоугольники.

    Получить интерфейс можно, используя метод контейнера графических объектов IDrawingContainer::RegularPolygons.
    """

    def Add(self) -> IRegularPolygon:  # iregularpolygons_add.html
        """
        ## Add - Создать новый элемент и добавить его в коллекцию

        ### Синтаксис Automation

        ```
        LPDISPATCH Add();
        ```

        ### Возвращаемое значение

        - Указатель на интерфейс многоугольника IRegularPolygon.
        """
        ...

    def RegularPolygon(self, Index) -> IRegularPolygon:  # iregularpolygons_regularpolygon.html
        """
        ## RegularPolygon - Многоугольник, заданный по индексу

        Тип данных: указатель на интерфейс многоугольника IRegularPolygon

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        RegularPolygon = iObject.RegularPolygon ( Index );    # Получить свойство(* )
        RegularPolygon = iObject.GetRegularPolygon( Index );  # Получить свойство (**)
        ```
        """
        ...




class IArc(IDrawingObject):  # iarc.html
    """
    ## Интерфейс IArc

    Интерфейс дуги.

    Интерфейс позволяет получить дугу.

    1. Интерфейс можно получить у коллекции дуг, используя свойство IArcs::Arc или метод IArcs::Add.

    2. При создании дуговой оси можно использовать несколько способов построения.

    •По координатам,

    •По трем точкам,

    •По углам,

    •По точке и углу,

    •По углу, точке и радиусу,

    •По конечным точкам и углу,

    •Плавающий центр, вариант 1,

    •Плавающий центр, вариант 2.

    3. После задания параметров оси требуется вызвать метод IDrawingObject::Update.

    4. При изменении параметров зависимые параметры пересчитываются после вызова метода IDrawingObject::Update.
    """

    @property
    def Angle1(self) -> float:  # iarc_angle1.html
        """
        ## Angle1 - Угол между осью 0X и отрезком, соединяющим первую точку дуги с началом координат

        Тип данных: double

        Свойство позволяет получить и установить угол между осью 0X и отрезком, соединяющим первую точку дуги с началом координат.

        ### Синтаксис Automation

        ```
        Angle1 = iObject.Angle1;       # Получить свойство(* )
        iObject.Angle1 = Angle1;       # Установить свойство (* )
        Angle1 = iObject.GetAngle1();  # Получить свойство (**)
        iObject.SetAngle1( Angle1 );   # Установить свойство (**)
        ```
        """
        ...
    @Angle1.setter
    def Angle1(self, value: float): ...

    @property
    def Angle2(self) -> float:  # iarc_angle2.html
        """
        ## Angle2 - Угол между осью 0X и отрезком, соединяющим вторую точку дуги с началом координат

        Тип данных: double

        Свойство позволяет получить и установить угол между осью 0X и отрезком, соединяющим вторую точку дуги с началом координат.

        ### Синтаксис Automation

        ```
        Angle2 = iObject.Angle2;       # Получить свойство(* )
        iObject.Angle2 = Angle2;       # Установить свойство (* )
        Angle2 = iObject.GetAngle2();  # Получить свойство (**)
        iObject.SetAngle2( Angle2 );   # Установить свойство (**)
        ```
        """
        ...
    @Angle2.setter
    def Angle2(self, value: float): ...

    @property
    def Direction(self) -> bool:  # iarc_direction.html
        """
        ## Direction - Направление построения дуги

        Тип данных: BOOL

        Значения свойства:

        +-----------------------------------+
        | TRUE  | - по часовой стрелке,     |
        +-----------------------------------+
        | FALSE | - против часовой стрелки. |
        +-----------------------------------+

        Свойство позволяет получить и установить направление построения дуги.

        ### Синтаксис Automation

        ```
        Direction = iObject.Direction;       # Получить свойство(* )
        iObject.Direction = Direction;       # Установить свойство (* )
        Direction = iObject.GetDirection();  # Получить свойство (**)
        iObject.SetDirection( Direction );   # Установить свойство (**)
        ```
        """
        ...
    @Direction.setter
    def Direction(self, value: bool): ...

    @property
    def Radius(self) -> float:  # iarc_radius.html
        """
        ## Radius - Радиус дуги

        Тип данных: double

        Свойство позволяет получить и установить радиус дуги.

        ### Синтаксис Automation

        ```
        Radius = iObject.Radius;       # Получить свойство(* )
        iObject.Radius = Radius;       # Установить свойство (* )
        Radius = iObject.GetRadius();  # Получить свойство (**)
        iObject.SetRadius( Radius );   # Установить свойство (**)
        ```
        """
        ...
    @Radius.setter
    def Radius(self, value: float): ...

    @property
    def Style(self) -> int:  # iarc_style.html
        """
        ## Style - Стиль линии

        Тип данных: long

        Свойство позволяет считывать и устанавливать стиль линии.

        ### Синтаксис Automation

        ```
        style = iObject.style;       # Получить свойство(* )
        iObject.style = style;       # Установить свойство (* )
        style = iObject.GetStyle();  # Получить свойство (**)
        iObject.SetStyle( style );   # Установить свойство (**)
        ```
        """
        ...
    @Style.setter
    def Style(self, value: int): ...

    @property
    def X1(self) -> float:  # iarc_x1.html
        """
        ## X1 - Координата первой точки по X

        Тип данных: double

        Свойство позволяет получить и установить координату первой точки дуги X1.

        ### Синтаксис Automation

        ```
        X1 = iObject.X1;       # Получить свойство(* )
        iObject.X1 = X1;       # Установить свойство (* )
        X1 = iObject.GetX1();  # Получить свойство (**)
        iObject.SetX1( X1 );   # Установить свойство (**)
        ```
        """
        ...
    @X1.setter
    def X1(self, value: float): ...

    @property
    def X2(self) -> float:  # iarc_x2.html
        """
        ## X2 - Координата второй точки по X

        Тип данных: double

        Свойство позволяет получить и установить координату первой точки дуги X2.

        ### Синтаксис Automation

        ```
        X2 = iObject.X2;       # Получить свойство(* )
        iObject.X2 = X2;       # Установить свойство (* )
        X2 = iObject.GetX2();  # Получить свойство (**)
        iObject.SetX2( X2 );   # Установить свойство (**)
        ```
        """
        ...
    @X2.setter
    def X2(self, value: float): ...

    @property
    def X3(self) -> float:  # iarc_x3.html
        """
        ## X3 - Координата средней точки, лежащей на дуге по X

        Тип данных: double

        Свойство позволяет получить и установить координату средней точки, лежащей на дуге по X.

        ### Синтаксис Automation

        ```
        X3 = iObject.X3;       # Получить свойство(* )
        iObject.X3 = X3;       # Установить свойство (* )
        X3 = iObject.GetX3();  # Получить свойство (**)
        iObject.SetX3( X3 );   # Установить свойство (**)
        ```
        """
        ...
    @X3.setter
    def X3(self, value: float): ...

    @property
    def Xc(self) -> float:  # iarc_xc.html
        """
        ## Xc - Координата X центра дуги

        Тип данных: double

        Свойство позволяет получить и установить координату центра дуги Xc.

        ### Синтаксис Automation

        ```
        Xc = iObject.Xc;       # Получить свойство(* )
        iObject.Xc = Xc;       # Установить свойство (* )
        Xc = iObject.GetXс();  # Получить свойство (**)
        iObject.SetXс( Xc );   # Установить свойство (**)
        ```
        """
        ...
    @Xc.setter
    def Xc(self, value: float): ...

    @property
    def Y1(self) -> float:  # iarc_y1.html
        """
        ## Y1 - Координата первой точки по Y

        Тип данных: double

        Свойство позволяет получить и установить координату первой точки дуги Y1.

        ### Синтаксис Automation

        ```
        Y1 = iObject.Y1;       # Получить свойство(* )
        iObject.Y1 = Y1;       # Установить свойство (* )
        Y1 = iObject.GetY1();  # Получить свойство (**)
        iObject.SetY1( Y1 );   # Установить свойство (**)
        ```
        """
        ...
    @Y1.setter
    def Y1(self, value: float): ...

    @property
    def Y2(self) -> float:  # iarc_y2.html
        """
        ## Y2 - Координата второй точки по Y

        Тип данных: double

        Свойство позволяет получить и установить координату второй точки дуги Y2.

        ### Синтаксис Automation

        ```
        Y2 = iObject.Y2;       # Получить свойство(* )
        iObject.Y2 = Y2;       # Установить свойство (* )
        Y2 = iObject.GetY2();  # Получить свойство (**)
        iObject.SetY2( Y2 );   # Установить свойство (**)
        ```
        """
        ...
    @Y2.setter
    def Y2(self, value: float): ...

    @property
    def Y3(self) -> float:  # iarc_y3.html
        """
        ## Y3 - Координата средней точки, лежащей на дуге по Y

        Тип данных: double

        Свойство позволяет получить и установить координату средней точки, лежащей на дуге по Y.

        ### Синтаксис Automation

        ```
        Y3 = iObject.Y3;       # Получить свойство(* )
        iObject.Y3 = Y3;       # Установить свойство (* )
        Y3 = iObject.GetY3();  # Получить свойство (**)
        iObject.SetY3( Y3 );   # Установить свойство (**)
        ```
        """
        ...
    @Y3.setter
    def Y3(self, value: float): ...

    @property
    def Yc(self) -> float:  # iarc_yc.html
        """
        ## Yc - Координата Y центра дуги

        Тип данных: double

        Свойство позволяет получить и установить координату центра дуги Yc.

        ### Синтаксис Automation

        ```
        Yc = iObject.Yc;       # Получить свойство(* )
        iObject.Yc = Yc;       # Установить свойство (* )
        Yc = iObject.GetYc();  # Получить свойство (**)
        iObject.SetYc( Yc );   # Установить свойство (**)
        ```
        """
        ...
    @Yc.setter
    def Yc(self, value: float): ...




class IBezier(IDrawingObject):  # ibezier.html
    """
    ## Интерфейс IBezier

    Интерфейс кривой Безье.

    1. Интерфейс можно получить, используя свойство коллекции кривых Безье IBeziers::Bezier или метод IBeziers::Add.

    2. После задания параметров окружности требуется вызвать метод IDrawingObject::Update.
    """

    @property
    def Closed(self) -> bool:  # ibezier_closed.html
        """
        ## Closed - Замкнутость

        Тип данных: BOOL

        Свойство позволяет устанавливать и получать замкнутость кривой.

        ### Синтаксис Automation

        ```
        Closed = Object.Closed       # Получить свойство(* )
        Object.Closed = Closed       # Установить свойство (* )
        Closed = Object.GetClosed()  # Получить свойство (**)
        Object.SetClosed( Closed )   # Установить свойство (**)
        ```
        """
        ...
    @Closed.setter
    def Closed(self, value: bool): ...

    @property
    def PointsCount(self) -> int:  # ibezier_pointscount.html
        """
        ## PointsCount - Количество опорных точек

        Тип данных: long

        1. Свойство позволяет получать количество опорных точек.

        2. Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        PointsCount = Object.PointsCount       # Получить свойство(* )
        PointsCount = Object.GetPointsCount()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def Style(self) -> int:  # ibezier_style.html
        """
        ## Style - Cтиль линии

        Тип данных: long

        1. Свойство позволяет устанавливать и получать стиль линии кривой.

        2. Системные стили линий берутся из перечисления ksCurveStyleEnum.

        ### Синтаксис Automation

        ```
        Style = Object.Style       # Получить свойство(* )
        Object.Style = Style       # Установить свойство (* )
        Style = Object.GetStyle()  # Получить свойство (**)
        Object.SetStyle( Style )   # Установить свойство (**)
        ```
        """
        ...
    @Style.setter
    def Style(self, value: int): ...

    def AddPoint(self, Index, XBase, YBase, XLeft, YLeft, XRight, YRight) -> bool:  # ibezier_addpoint.html
        """
        ## AddPoint - Добавить узел

        Метод позволяет добавить узел в кривую Безье.

        ### Синтаксис Automation

        ```
        BOOL AddPoint( long Index,
        double XBase,
        double YBase,
        double * XLeft,
        double * YLeft,
        double * XRight,
        double * YRight )
        ```

        ### Входные параметры

        +---------------------------------------------------+
        | Index          | - индекс добавляемого узла,      |
        +---------------------------------------------------+
        | XBase, YBase   | - координаты опорной точки узла, |
        +---------------------------------------------------+
        | XLeft, YLeft   | - координаты левой точки узла,   |
        +---------------------------------------------------+
        | XRight, YRight | - координаты правой точки узла.  |
        +---------------------------------------------------+

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def Clear(self) -> bool:  # ibezier_clear.html
        """
        ## Clear - Удалить все узлы

        Метод позволяет удалить все узлы кривой.

        ### Синтаксис Automation

        ```
        BOOL Clear()
        ```

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def DeletePoint(self, Index) -> bool:  # ibezier_deletepoint.html
        """
        ## DeletePoint - Удалить узел

        Метод позволяет удалить заданный узел кривой.

        ### Синтаксис Automation

        ```
        BOOL DeletePoint( long Index )
        ```

        ### Входные параметры

        +-----------------------------------+
        | Index | - индекс удаляемого узла. |
        +-----------------------------------+

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def GetPoint(self, Index) -> bool:  # ibezier_getpoint.html
        """
        ## GetPoint - Получить параметры узла

        Метод позволяет получить параметры узла кривой Безье.

        ### Синтаксис Automation

        ```
        BOOL GetPoint( long Index,
        double XBase,
        double YBase,
        double * XLeft,
        double * YLeft,
        double * XRight,
        double * YRight )
        ```

        ### Входные параметры

        +------------------------+
        | Index | - индекс узла, |
        +------------------------+

        ### Выходные параметры

        +---------------------------------------------------+
        | XBase, YBase   | - координаты опорной точки узла, |
        +---------------------------------------------------+
        | XLeft, YLeft   | - координаты левой точки узла,   |
        +---------------------------------------------------+
        | XRight, YRight | - координаты правой точки узла.  |
        +---------------------------------------------------+

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def Points(self, AllPoints) -> list[float]:  # ibezier_points.html
        """
        ## Points - Параметры узлов в виде SAFEARRAY | VT_R8

        Тип данных: VARIANT типа VT_ARRAY | VT_R8

        Свойство позволяет устанавливать и получать параметры узлов кривой.

        ### Синтаксис Automation

        ```
        Points = Object.Points( AllPoints )     # Получить свойство(* )
        Object.Points( AllPoints ) = Points     # Установить свойство (* )
        Points = Object.GetPoints( AllPoints )  # Получить свойство (**)
        Object.SetPoints( AllPoints, Points )   # Установить свойство (**)
        ```

        ### Входные параметры

        +-----------------------------------------------------------------------------------------------------------------------------------------------------------------+
        | BOOL AllPoints | - TRUE - узел в массиве Points представлен опорной точкой и двумя крайними, - FALSE - узел в массиве Points представлен только опорной точкой. |
        +-----------------------------------------------------------------------------------------------------------------------------------------------------------------+
        """
        ...

    def ReadFromFile(self, FileName) -> bool:  # ibezier_readfromfile.html
        """
        ## ReadFromFile - Прочитать файл с данными

        ### Синтаксис Automation

        ```
        BOOL ReadToFile( LPCTSTR FileName );
        ```

        ### Входные параметры

        +------------------------------------------+
        | FileName | - полное имя файла с данными. |
        +------------------------------------------+

        ### Возвращаемое значение

        +--------------------------+
        | TRUE | - в случае удачи. |
        +--------------------------+
        """
        ...

    def SetPoints(self, AllPoints, arg1):
        ...

    def WriteToFile(self, FileName) -> bool:  # ibezier_writetofile.html
        """
        ## WriteToFile - Записать файл с данными

        ### Синтаксис Automation

        ```
        BOOL WriteToFile( LPCTSTR FileName );
        ```

        ### Входные параметры

        +------------------------------------------+
        | FileName | - полное имя файла с данными. |
        +------------------------------------------+

        ### Возвращаемое значение

        +--------------------------+
        | TRUE | - в случае удачи. |
        +--------------------------+
        """
        ...




class ICircle(IDrawingObject):  # icircle.html
    """
    ## Интерфейс ICircle

    Интерфейс окружности.

    1. Интерфейс можно получить, используя свойство коллекции окружностей ICircles::Circle или метод ICircles::Add.

    2. После задания параметров окружности требуется вызвать метод IDrawingObject::Update.
    """

    @property
    def Radius(self) -> float:  # icircle_radius.html
        """
        ## Radius - Радиус окружности

        Тип данных: double

        Свойство позволяет устанавливать и получать радиус окружности.

        ### Синтаксис Automation

        ```
        Radius = Object.Radius       # Получить свойство(* )
        Object.Radius = Radius       # Установить свойство (* )
        Radius = Object.GetRadius()  # Получить свойство (**)
        Object.SetRadius( Radius )   # Установить свойство (**)
        ```
        """
        ...
    @Radius.setter
    def Radius(self, value: float): ...

    @property
    def Style(self) -> int:  # icircle_style.html
        """
        ## Style - Cтиль линии

        Тип данных: long

        1. Свойство позволяет устанавливать и получать стиль линии окружности.

        2. Системные стили линий берутся из перечисления ksCurveStyleEnum.

        ### Синтаксис Automation

        ```
        Style = Object.Style       # Получить свойство(* )
        Object.Style = Style       # Установить свойство (* )
        Style = Object.GetStyle()  # Получить свойство (**)
        Object.SetStyle( Style )   # Установить свойство (**)
        ```
        """
        ...
    @Style.setter
    def Style(self, value: int): ...

    @property
    def X(self) -> float:  # icircle_x.html
        """
        ## X - Координата точки на окружности по оси X

        Тип данных: double

        Свойство позволяет устанавливать и получать координату точки на окружности по оси X.

        ### Синтаксис Automation

        ```
        X = Object.X       # Получить свойство(* )
        Object.X = X       # Установить свойство (* )
        X = Object.GetX()  # Получить свойство (**)
        Object.SetXc( X )  # Установить свойство (**)
        ```
        """
        ...
    @X.setter
    def X(self, value: float): ...

    @property
    def Xc(self) -> float:  # icircle_xc.html
        """
        ## Xc - Координата центра окружности по оси Х

        Тип данных: double

        Свойство позволяет устанавливать и получать координату центра окружности по оси Х.

        ### Синтаксис Automation

        ```
        Xc = Object.Xc       # Получить свойство(* )
        Object.Xc = Xc       # Установить свойство (* )
        Xc = Object.GetXc()  # Получить свойство (**)
        Object.SetXc( Xc )   # Установить свойство (**)
        ```
        """
        ...
    @Xc.setter
    def Xc(self, value: float): ...

    @property
    def Y(self) -> float:  # icircle_y.html
        """
        ## Y - Координата точки на окружности по оси Y

        Тип данных: double

        Свойство позволяет устанавливать и получать координату точки на окружности по оси Y.

        ### Синтаксис Automation

        ```
        Y = Object.Y       # Получить свойство(* )
        Object.Y = Y       # Установить свойство (* )
        Y = Object.GetY()  # Получить свойство (**)
        Object.SetY( Y )   # Установить свойство (**)
        ```
        """
        ...
    @Y.setter
    def Y(self, value: float): ...

    @property
    def Yc(self) -> float:  # icircle_yc.html
        """
        ## Yc - Координата центра окружности по оси Y

        Тип данных: double

        Свойство позволяет устанавливать и получать координату центра окружности по оси Y.

        ### Синтаксис Automation

        ```
        Yc = Object.Yc       # Получить свойство(* )
        Object.Yc = Yc       # Установить свойство (* )
        Yc = Object.GetYc()  # Получить свойство (**)
        Object.SetYc( Yc )   # Установить свойство (**)
        ```
        """
        ...
    @Yc.setter
    def Yc(self, value: float): ...




class IColouring(IDrawingObject):  # icolouring.html
    """
    ## Интерфейс IColouring

    Интерфейс заливки.

    Интерфейс позволяет задавать параметры заливки.

    1. Интерфейс можно получить у коллекции заливок, используя свойство IColourings::Colouring или метод IColourings::Add.

    2. После задания параметров штриховки требуется вызвать метод IDrawingObject::Update.

    3. Посредством вызова метода IUnknown::QueryInterface (const GUID far& iid, void** pif) у данного интерфейса можно получить дополнительный интерфейс IBoundariesObject.
    """

    @property
    def Color1(self) -> int:  # icolouring_color1.html
        """
        ## Color1 - Цвет или начальный цвет

        Тип данных: long

        Свойство позволяет устанавливать и получать начальный цвет заливки.

        ### Синтаксис Automation

        ```
        Color1 = Object.Color1       # Получить свойство(* )
        Object.Color1 = Color1       # Установить свойство (* )
        Color1 = Object.GetColor1()  # Получить свойство (**)
        Object.SetColor1( Color1 )   # Установить свойство (**)
        ```
        """
        ...
    @Color1.setter
    def Color1(self, value: int): ...

    @property
    def Color2(self) -> int:  # icolouring_color2.html
        """
        ## Color2 - Цвет или конечный цвет

        Тип данных: long

        Свойство позволяет устанавливать и получать конечный цвет заливки.

        ### Синтаксис Automation

        ```
        Color2 = Object.Color2       # Получить свойство(* )
        Object.Color2 = Color2       # Установить свойство (* )
        Color2 = Object.GetColor2()  # Получить свойство (**)
        Object.SetColor2( Color2 )   # Установить свойство (**)
        ```
        """
        ...
    @Color2.setter
    def Color2(self, value: int): ...

    @property
    def ColouringType(self) -> int:  # icolouring_colouringtype.html
        """
        ## ColouringType - Тип заливки

        Тип данных: из перечисления ksColouringTypeEnum

        Свойство позволяет устанавливать и получать тип заливки.

        ### Синтаксис Automation

        ```
        ColouringType = Object.ColouringType       # Получить свойство(* )
        Object.ColouringType = ColouringType       # Установить свойство (* )
        ColouringType = Object.GetColouringType()  # Получить свойство (**)
        Object.SetColouringType( ColouringType )   # Установить свойство (**)
        ```
        """
        ...
    @ColouringType.setter
    def ColouringType(self, value: int): ...

    @property
    def GradationsCount(self) -> int:  # icolouring_gradationscount.html
        """
        ## GradationsCount - Количество переходов цвета

        Тип данных: long

        1. Свойство позволяет получать количество переходов цвета.

        2. Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        GradationsCount = Object.GradationsCount       # Получить свойство(* )
        GradationsCount = Object.GetGradationsCount()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def GradientAngle(self) -> float:  # icolouring_gradientangle.html
        """
        ## GradientAngle - Угол поворота линий градиента

        Тип данных: double

        Свойство позволяет устанавливать и получать угол поворота линий градиента.

        ### Синтаксис Automation

        ```
        GradientAngle = Object.GradientAngle       # Получить свойство(* )
        Object.GradientAngle = GradientAngle       # Установить свойство (* )
        GradientAngle = Object.GetGradientAngle()  # Получить свойство (**)
        Object.SetGradientAngle( GradientAngle )   # Установить свойство (**)
        ```
        """
        ...
    @GradientAngle.setter
    def GradientAngle(self, value: float): ...

    @property
    def GradientCount(self) -> bool:  # icolouring_gradientcount.html
        """
        ## GradientCount - Количество шагов

        Тип данных: BOOL

        Значения свойства:

        +--------------------------------------+
        | TRUE  | - плавное изменение цвета,   |
        +--------------------------------------+
        | FALSE | - заданное количество шагов. |
        +--------------------------------------+

        Свойство позволяет устанавливать и получать количество шагов градиентной заливки.

        ### Синтаксис Automation

        ```
        GradientCount = Object.GradientCount       # Получить свойство(* )
        Object.GradientCount = GradientCount       # Установить свойство (* )
        GradientCount = Object.GetGradientCount()  # Получить свойство (**)
        Object.SetGradientCount( GradientCount )   # Установить свойство (**)
        ```
        """
        ...
    @GradientCount.setter
    def GradientCount(self, value: bool): ...

    @property
    def GradientType(self) -> bool:  # icolouring_gradienttype.html
        """
        ## GradientType - Тип градиента

        Тип данных: BOOL

        Значения свойства:

        +--------------------------------------+
        | TRUE  | - плавное изменение цвета,   |
        +--------------------------------------+
        | FALSE | - заданное количество шагов. |
        +--------------------------------------+

        Свойство позволяет устанавливать и получать тип градиентной заливки.

        ### Синтаксис Automation

        ```
        GradientType = Object.GradientType       # Получить свойство(* )
        Object.GradientType = GradientType       # Установить свойство (* )
        GradientType = Object.GetGradientType()  # Получить свойство (**)
        Object.SetGradientType( GradientType )   # Установить свойство (**)
        ```
        """
        ...
    @GradientType.setter
    def GradientType(self, value: bool): ...

    @property
    def Transparency1(self) -> int:  # icolouring_transparency1.html
        """
        ## Transparency1 - Начальная прозрачность

        Тип данных: long

        Свойство позволяет устанавливать и получать начальную прозрачность в процентах.

        ### Синтаксис Automation

        ```
        Transparency1 = Object.Transparency1       # Получить свойство(* )
        Object.Transparency1 = Transparency1       # Установить свойство (* )
        Transparency1 = Object.GetTransparency1()  # Получить свойство (**)
        Object.SetTransparency1( Transparency1 )   # Установить свойство (**)
        ```
        """
        ...
    @Transparency1.setter
    def Transparency1(self, value: int): ...

    @property
    def Transparency2(self) -> int:  # icolouring_transparency2.html
        """
        ## Transparency2 - Конечная прозрачность

        Тип данных: long

        Свойство позволяет устанавливать и получать конечную прозрачность в процентах.

        ### Синтаксис Automation

        ```
        Transparency2 = Object.Transparency2       # Получить свойство(* )
        Object.Transparency2 = Transparency2       # Установить свойство (* )
        Transparency2 = Object.GetTransparency2()  # Получить свойство (**)
        Object.SetTransparency2( Transparency2 )   # Установить свойство (**)
        ```
        """
        ...
    @Transparency2.setter
    def Transparency2(self, value: int): ...

    @property
    def Xc(self) -> float:  # icolouring_xc.html
        """
        ## Xc - Координата положения центра заливки по оси X

        Тип данных: double

        Свойство позволяет устанавливать и получать координату положения центра заливки по оси Х в процентах относительно габарита области.

        ### Синтаксис Automation

        ```
        Xc = Object.Xc       # Получить свойство(* )
        Object.Xc = Xc       # Установить свойство (* )
        Xc = Object.GetXc()  # Получить свойство (**)
        Object.SetXc( Xc )   # Установить свойство (**)
        ```
        """
        ...
    @Xc.setter
    def Xc(self, value: float): ...

    @property
    def Yc(self) -> float:  # icolouring_yc.html
        """
        ## Yc - Координата положения центра заливки по оси Y

        Тип данных: double

        Свойство позволяет устанавливать и получать координату положения центра заливки по оси Y в процентах относительно габарита области.

        ### Синтаксис Automation

        ```
        Yc = Object.Yc       # Получить свойство(* )
        Object.Yc = Yc       # Установить свойство (* )
        Yc = Object.GetYc()  # Получить свойство (**)
        Object.SetYc( Yc )   # Установить свойство (**)
        ```
        """
        ...
    @Yc.setter
    def Yc(self, value: float): ...

    def AddGradation(self, Position, Color, Transparency, Interpolation):  # icolouring_addgradation.html
        """
        ## AddGradation - Добавить переход цвета

        Метод позволяет установить параметры переход цвета.

        ### Синтаксис Automation

        ```
        long AddGradation( long Position,
        long Color,
        long Transparency,
        BOOL Interpolation );
        ```

        ### Входные параметры

        +----------------------------------------------------------------------------------------+
        | Position      | - положение перехода относительно начальной точки заливки в процентах, |
        +----------------------------------------------------------------------------------------+
        | Color         | - цвет перехода,                                                       |
        +----------------------------------------------------------------------------------------+
        | Transparency  | - прозрачность перехода,                                               |
        +----------------------------------------------------------------------------------------+
        | Interpolation | - значений опции Интерполирование для перехода.                        |
        +----------------------------------------------------------------------------------------+

        ### Возвращаемое значение

        - индекс перехода.
        """
        ...

    def ClearGradations(self) -> bool:  # icolouring_cleargradations.html
        """
        ## ClearGradations - Удалить промежуточные переходы цвета

        Метод позволяет удалить промежуточные переходы цвета.

        ### Синтаксис Automation

        ```
        BOOL ClearGradations();
        ```

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def DeleteGradation(self, Index) -> bool:  # icolouring_deletegradation.html
        """
        ## DeleteGradation - Удалить переход цвета по индексу

        Метод позволяет удалить переход цвета по индексу.

        ### Синтаксис Automation

        ```
        BOOL DeleteGradation( long Index );
        ```

        ### Входные параметры

        +----------------------------+
        | Index | - индекс перехода. |
        +----------------------------+

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def FindPosition(self, Position):  # icolouring_findposition.html
        """
        ## FindPosition - Получить индекс позиции

        Метод позволяет получить индекс позиции перехода цвета.

        ### Синтаксис Automation

        ```
        long FindPosition( long Position );
        ```

        ### Входные параметры

        +-----------------------------------------------------------------------------------+
        | Position | - положение перехода относительно начальной точки заливки в процентах. |
        +-----------------------------------------------------------------------------------+

        ### Возвращаемое значение

        - индекс перехода.
        """
        ...

    def GetGradation(self, Index) -> bool:  # icolouring_getgradation.html
        """
        ## GetGradation - Получить параметры перехода цвета по индексу

        Метод позволяет получить параметры перехода цвета по индексу.

        ### Синтаксис Automation

        ```
        BOOL GetGradation( long Index,
        long * Position,
        long * Color,
        long * Transparency,
        BOOL * Interpolation );
        ```

        ### Входные параметры

        +----------------------------+
        | Index | - индекс перехода. |
        +----------------------------+

        ### Выходные параметры

        +----------------------------------------------------------------------------------------+
        | Position      | - положение перехода относительно начальной точки заливки в процентах, |
        +----------------------------------------------------------------------------------------+
        | Color         | - цвет перехода,                                                       |
        +----------------------------------------------------------------------------------------+
        | Transparency  | - прозрачность перехода,                                               |
        +----------------------------------------------------------------------------------------+
        | Interpolation | - значений опции Интерполирование для перехода.                        |
        +----------------------------------------------------------------------------------------+

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def GetGradations(self) -> bool:  # icolouring_getgradations.html
        """
        ## GetGradations - Получить параметры переходов цвета

        Метод позволяет получить параметры переходов цветов.

        ### Синтаксис Automation

        ```
        BOOL GetGradations( VARIANT * Positions,
        VARIANT * Colors,
        VARIANT * Transparences,
        VARIANT * Interpolations );
        ```

        ### Выходные параметры

        +-------------------------------------------------------------------------------------------------------------------+
        | Positions      | - массив SAFEARRAY | VT_I4 положений переходов относительно начальной точки заливки в процентах, |
        +-------------------------------------------------------------------------------------------------------------------+
        | Colors         | - массив SAFEARRAY | VT_I4 цветов переходов,                                                     |
        +-------------------------------------------------------------------------------------------------------------------+
        | Transparences  | - массив SAFEARRAY | VT_I4 прозрачностей переходов,                                              |
        +-------------------------------------------------------------------------------------------------------------------+
        | Interpolations | - массив SAFEARRAY | VT_BOOL значений опции Интерполирование для переходов.                      |
        +-------------------------------------------------------------------------------------------------------------------+

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def SetGradation(self, Index, Position, Color, Transparency, Interpolation) -> bool:  # icolouring_setgradation.html
        """
        ## SetGradation - Установить параметры перехода цвета по индексу

        Метод позволяет установить параметры перехода цвета по индексу.

        ### Синтаксис Automation

        ```
        BOOL SetGradation( long Index,
        long * Position,
        long * Color,
        long * Transparency,
        BOOL * Interpolation );
        ```

        ### Входные параметры

        +----------------------------------------------------------------------------------------+
        | Index         | - индекс перехода,                                                     |
        +----------------------------------------------------------------------------------------+
        | Position      | - положение перехода относительно начальной точки заливки в процентах, |
        +----------------------------------------------------------------------------------------+
        | Color         | - цвет перехода,                                                       |
        +----------------------------------------------------------------------------------------+
        | Transparency  | - прозрачность перехода,                                               |
        +----------------------------------------------------------------------------------------+
        | Interpolation | - значений опции Интерполирование для перехода.                        |
        +----------------------------------------------------------------------------------------+

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def SetGradations(self, Positions, Colors, Transparences, Interpolations) -> bool:  # icolouring_setgradations.html
        """
        ## SetGradations - Установить параметры переходов цвета

        Метод позволяет установить параметры переходов цветов.

        ### Синтаксис Automation

        ```
        BOOL SetGradations( VARIANT * Positions,
        VARIANT * Colors,
        VARIANT * Transparences,
        VARIANT * Interpolations );
        ```

        ### Входные параметры

        +-------------------------------------------------------------------------------------------------------------------+
        | Positions      | - массив SAFEARRAY | VT_I4 положений переходов относительно начальной точки заливки в процентах, |
        +-------------------------------------------------------------------------------------------------------------------+
        | Colors         | - массив SAFEARRAY | VT_I4 цветов переходов,                                                     |
        +-------------------------------------------------------------------------------------------------------------------+
        | Transparences  | - массив SAFEARRAY | VT_I4 прозрачностей переходов,                                              |
        +-------------------------------------------------------------------------------------------------------------------+
        | Interpolations | - массив SAFEARRAY | VT_BOOL значений опции Интерполирование для переходов.                      |
        +-------------------------------------------------------------------------------------------------------------------+

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...




class IConicCurve(IDrawingObject):  # iconiccurve.html
    """
    ## Интерфейс IConicCurve

    Интерфейс параметров конической кривой.
    """

    @property
    def Angle1(self) -> float:  # iconiccurve_angle1.html
        """
        ## Angle1 - Угол наклона первой кривой

        Тип данных: double

        ### Синтаксис Automation

        ```
        Angle1 = Object.Angle1       # Получить свойство(* )
        Object.Angle1 = Angle1       # Установить свойство (* )
        Angle1 = Object.GetAngle1()  # Получить свойство (**)
        Object.SetAngle1( Angle1 )   # Установить свойство (**)
        ```
        """
        ...
    @Angle1.setter
    def Angle1(self, value: float): ...

    @property
    def Angle2(self) -> float:  # iconiccurve_angle2.html
        """
        ## Angle2 - Угол наклона второй кривой

        Тип данных: double

        ### Синтаксис Automation

        ```
        Angle2 = Object.Angle2       # Получить свойство(* )
        Object.Angle2 = Angle2       # Установить свойство (* )
        Angle2 = Object.GetAngle2()  # Получить свойство (**)
        Object.SetAngle2( Angle2 )   # Установить свойство (**)
        ```
        """
        ...
    @Angle2.setter
    def Angle2(self, value: float): ...

    @property
    def Coefficient(self) -> float:  # iconiccurve_coefficient.html
        """
        ## Coefficient - Коэффициент

        Тип данных: double

        ### Синтаксис Automation

        ```
        Coefficient = Object.Coefficient       # Получить свойство(* )
        Object.Coefficient = Coefficient       # Установить свойство (* )
        Coefficient = Object.GetCoefficient()  # Получить свойство (**)
        Object.SetCoefficient( Coefficient )   # Установить свойство (**)
        ```
        """
        ...
    @Coefficient.setter
    def Coefficient(self, value: float): ...

    @property
    def Height(self) -> float:  # iconiccurve_height.html
        """
        ## Height - Высота

        Тип данных: double

        ### Синтаксис Automation

        ```
        Height = Object.Height       # Получить свойство(* )
        Object.Height = Height       # Установить свойство (* )
        Height = Object.GetHeight()  # Получить свойство (**)
        Object.SetHeight( Height )   # Установить свойство (**)
        ```
        """
        ...
    @Height.setter
    def Height(self, value: float): ...

    @property
    def Style(self) -> int:  # iconiccurve_style.html
        """
        ## Style - Cтиль линии

        Тип данных: long

        ### Синтаксис Automation

        ```
        Style = Object.Style       # Получить свойство(* )
        Object.Style = Style       # Установить свойство (* )
        Style = Object.GetStyle()  # Получить свойство (**)
        Object.SetStyle( Style )   # Установить свойство (**)
        ```
        """
        ...
    @Style.setter
    def Style(self, value: int): ...

    def GetPoint(self, PointIndex) -> bool:  # iconiccurve_getpoint.html
        """
        ## GetPoint - Получить точку пересечения

        ### Синтаксис Automation

        ```
        BOOL GetPoint( ksConicCurvePontIndexEnum PointIndex, double * X, double * Y );
        ```

        ### Входные параметры

        +------------------------------------------------------------------------+
        | PointIndex | - индекс точки из перечисления ksConicCurvePontIndexEnum. |
        +------------------------------------------------------------------------+

        ### Выходные параметры

        +----------------------------+
        | X, Y | - координаты точки. |
        +----------------------------+

        ### Возвращаемое значение

        +-----------------------------------------+
        | TRUE | - в случае успешного завершения. |
        +-----------------------------------------+
        """
        ...

    def SetPoint(self, PointIndex, X, Y) -> bool:  # iconiccurve_setpoint.html
        """
        ## SetPoint - Установить точку пересечения

        ### Синтаксис Automation

        ```
        BOOL SetPoint( ksConicCurvePontIndexEnum PointIndex, double X, double Y );
        ```

        ### Входные параметры

        +------------------------------------------------------------------------+
        | PointIndex | - индекс точки из перечисления ksConicCurvePontIndexEnum, |
        +------------------------------------------------------------------------+
        | X, Y       | - координаты точки.                                       |
        +------------------------------------------------------------------------+

        ### Возвращаемое значение

        +-----------------------------------------+
        | TRUE | - в случае успешного завершения. |
        +-----------------------------------------+
        """
        ...




class IDrawingContour(IDrawingObject):  # idrawingcontour.html
    """
    ## Интерфейс IDrawingContour

    Интерфейс контура.

    1. Интерфейс позволяет получить и задать свойства объекта "контур".

    2. Получить интерфейс можно, используя свойство IDrawingContours::DrawingContour или метод IDrawingContours::Add интерфейса коллекции контуров.

    3. После задания параметров объекта требуется вызвать метод IDrawingObject::Update.

    4. Интерфейс IContour являются дополнительным. Его можно получить с помощью метода IUnknown::QueryInterface.
    """

    @property
    def Style(self) -> int:  # idrawingcontour_style.html
        """
        ## Style - Стиль линии

        Тип данных: long (соответствует перечислению ksCurveStyleEnum)

        Свойство позволяет устанавливать и получать стиль линии.

        ### Синтаксис Automation

        ```
        Style = iObject.Style;        # Получить свойство(* )
        iObject.Style = Style;        # Установить свойство (* )
        Style = iObject.GetStyle( );  # Получить свойство (**)
        iObject.SetStyle( Style );    # Установить свойство (**)
        ```
        """
        ...
    @Style.setter
    def Style(self, value: int): ...




class IEllipse(IDrawingObject):  # iellipse.html
    """
    ## Интерфейс IEllipse

    Интерфейс эллипса.

    Интерфейс позволяет получить и задать свойства эллипса.

    1. Интерфейс можно получить, используя свойство коллекции эллипсов IEllipses::Ellipse или метод IEllipses::Add.

    2. После задания параметров эллипса требуется вызвать метод IDrawingObject::Update.
    """

    @property
    def Angle(self) -> float:  # iellipse_angle.html
        """
        ## Angle - Угол наклона первой полуоси

        Тип данных: double

        Свойство позволяет устанавливать и получать угол наклона первой полуоси эллипса.

        ### Синтаксис Automation

        ```
        Angle = Object.Angle       # Получить свойство(* )
        Object.Angle = Angle       # Установить свойство (* )
        Angle = Object.GetAngle()  # Получить свойство (**)
        Object.SetAngle( Angle )   # Установить свойство (**)
        ```
        """
        ...
    @Angle.setter
    def Angle(self, value: float): ...

    @property
    def SemiAxisA(self) -> float:  # iellipse_semiaxisa.html
        """
        ## SemiAxisA - Длина первой полуоси

        Тип данных: double

        Свойство позволяет устанавливать и получать длину первой полуоси эллипса.

        ### Синтаксис Automation

        ```
        SemiAxisA = Object.SemiAxisA       # Получить свойство(* )
        Object.SemiAxisA = SemiAxisA       # Установить свойство (* )
        SemiAxisA = Object.GetSemiAxisA()  # Получить свойство (**)
        Object.SetSemiAxisA( SemiAxisA )   # Установить свойство (**)
        ```
        """
        ...
    @SemiAxisA.setter
    def SemiAxisA(self, value: float): ...

    @property
    def SemiAxisB(self) -> float:  # iellipse_semiaxisb.html
        """
        ## SemiAxisB - Длина второй полуоси

        Тип данных: double

        Свойство позволяет устанавливать и получать длину второй полуоси эллипса.

        ### Синтаксис Automation

        ```
        SemiAxisB = Object.SemiAxisB       # Получить свойство(* )
        Object.SemiAxisB = SemiAxisB       # Установить свойство (* )
        SemiAxisB = Object.GetSemiAxisB()  # Получить свойство (**)
        Object.SetSemiAxisB( SemiAxisB )   # Установить свойство (**)
        ```
        """
        ...
    @SemiAxisB.setter
    def SemiAxisB(self, value: float): ...

    @property
    def Style(self) -> int:  # iellipse_style.html
        """
        ## Style - Cтиль линии

        Тип данных: long

        1. Свойство позволяет устанавливать и получать стиль линии эллипса.

        2. Системные стили линий берутся из перечисления ksCurveStyleEnum.

        ### Синтаксис Automation

        ```
        Style = Object.Style       # Получить свойство(* )
        Object.Style = Style       # Установить свойство (* )
        Style = Object.GetStyle()  # Получить свойство (**)
        Object.SetStyle( Style )   # Установить свойство (**)
        ```
        """
        ...
    @Style.setter
    def Style(self, value: int): ...

    @property
    def X1(self) -> float:  # iellipse_x1.html
        """
        ## X1 - Координата конечной точки первой полуоси по оси X

        Тип данных: double

        Свойство позволяет устанавливать и получать координату конечной точки первой полуоси по оси X.

        ### Синтаксис Automation

        ```
        X1 = Object.X1       # Получить свойство(* )
        Object.X1 = X1       # Установить свойство (* )
        X1 = Object.GetX1()  # Получить свойство (**)
        Object.SetX1( X1 )   # Установить свойство (**)
        ```
        """
        ...
    @X1.setter
    def X1(self, value: float): ...

    @property
    def X2(self) -> float:  # iellipse_x2.html
        """
        ## X2 - Координата конечной точки второй полуоси по оси X

        Тип данных: double

        Свойство позволяет устанавливать и получать координату конечной точки второй полуоси по оси X.

        ### Синтаксис Automation

        ```
        X2 = Object.X2       # Получить свойство(* )
        Object.X2 = X2       # Установить свойство (* )
        X2 = Object.GetX2()  # Получить свойство (**)
        Object.SetX2( X2 )   # Установить свойство (**)
        ```
        """
        ...
    @X2.setter
    def X2(self, value: float): ...

    @property
    def Xc(self) -> float:  # iellipse_xc.html
        """
        ## Xc - Координата центра эллипса по оси X

        Тип данных:double.

        Свойство позволяет устанавливать и получать координату центра эллипса по оси X.

        ### Синтаксис Automation

        ```
        Xc = Object.Xc       # Получить свойство(* )
        Object.Xc = Xc       # Установить свойство (* )
        Xc = Object.GetXc()  # Получить свойство (**)
        Object.SetXc( Xc )   # Установить свойство (**)
        ```
        """
        ...
    @Xc.setter
    def Xc(self, value: float): ...

    @property
    def Y1(self) -> float:  # iellipse_y1.html
        """
        ## Y1 - Координата конечной точки первой полуоси по оси Y

        Тип данных: double

        Свойство позволяет устанавливать и получать координату конечной точки первой полуоси по оси Y.

        ### Синтаксис Automation

        ```
        Y1 = Object.Y1       # Получить свойство(* )
        Object.Y1 = Y1       # Установить свойство (* )
        Y1 = Object.GetY1()  # Получить свойство (**)
        Object.SetY1( Y1 )   # Установить свойство (**)
        ```
        """
        ...
    @Y1.setter
    def Y1(self, value: float): ...

    @property
    def Y2(self) -> float:  # iellipse_y2.html
        """
        ## Y2 - Координата конечной точки второй полуоси по оси Y

        Тип данных:double.

        Свойство позволяет устанавливать и получать координату конечной точки второй полуоси по оси Y.

        ### Синтаксис Automation

        ```
        Y2 = Object.Y2       # Получить свойство(* )
        Object.Y2 = Y2       # Установить свойство (* )
        Y2 = Object.GetY2()  # Получить свойство (**)
        Object.SetY2( Y2 )   # Установить свойство (**)
        ```
        """
        ...
    @Y2.setter
    def Y2(self, value: float): ...

    @property
    def Yc(self) -> float:  # iellipse_yc.html
        """
        ## Yc - Координата центра эллипса по оси Y

        Тип данных: double

        Свойство позволяет устанавливать и получать координату центра эллипса по оси Y.

        ### Синтаксис Automation

        ```
        Yc = Object.Yc       # Получить свойство(* )
        Object.Yc = Yc       # Установить свойство (* )
        Yc = Object.GetYc()  # Получить свойство (**)
        Object.SetYc( Yc )   # Установить свойство (**)
        ```
        """
        ...
    @Yc.setter
    def Yc(self, value: float): ...




class IEllipseArc(IDrawingObject):  # iellipsearc.html
    """
    ## Интерфейс IEllipseArc

    Интерфейс дуги эллипса.

    Интерфейс позволяет получить и задать свойства дуги эллипса.

    1. Интерфейс можно получить, используя свойство коллекции дуг эллипсов IEllipseArcs::EllipseArc или метод IEllipseArcs::Add.

    2. После задания параметров дуги эллипса требуется вызвать метод IDrawingObject::Update.
    """

    @property
    def Angle(self) -> float:  # iellipsearc_angle.html
        """
        ## Angle - Угол наклона первой полуоси

        Тип данных: double

        Свойство позволяет устанавливать и получать угол наклона первой полуоси дуги эллипса.

        ### Синтаксис Automation

        ```
        Angle = Object.Angle       # Получить свойство(* )
        Object.Angle = Angle       # Установить свойство (* )
        Angle = Object.GetAngle()  # Получить свойство (**)
        Object.SetAngle( Angle )   # Установить свойство (**)
        ```
        """
        ...
    @Angle.setter
    def Angle(self, value: float): ...

    @property
    def Angle1(self) -> float:  # iellipsearc_angle1.html
        """
        ## Angle1 - Начальный угол дуги к первой полуоси

        Тип данных: double

        Свойство позволяет устанавливать и получать начальный угол дуги к первой полуоси эллипса.

        ### Синтаксис Automation

        ```
        Angle1 = Object.Angle1       # Получить свойство(* )
        Object.Angle1 = Angle1       # Установить свойство (* )
        Angle1 = Object.GetAngle1()  # Получить свойство (**)
        Object.SetAngle1( Angle1 )   # Установить свойство (**)
        ```
        """
        ...
    @Angle1.setter
    def Angle1(self, value: float): ...

    @property
    def Angle2(self) -> float:  # iellipsearc_angle2.html
        """
        ## Angle2 - Конечный угол дуги к первой полуоси

        Тип данных: double

        Свойство позволяет устанавливать и получать конечный угол дуги к первой полуоси эллипса.

        ### Синтаксис Automation

        ```
        Angle2 = Object.Angle2       # Получить свойство(* )
        Object.Angle2 = Angle2       # Установить свойство (* )
        Angle2 = Object.GetAngle2()  # Получить свойство (**)
        Object.SetAngle2( Angle2 )   # Установить свойство (**)
        ```
        """
        ...
    @Angle2.setter
    def Angle2(self, value: float): ...

    @property
    def Direction(self) -> bool:  # iellipsearc_direction.html
        """
        ## Direction - Направление построения

        Тип данных: BOOL

        Значения свойства:

        +-----------------------------------+
        | TRUE  | - по часовой стрелке,     |
        +-----------------------------------+
        | FALSE | - против часовой стрелки. |
        +-----------------------------------+

        Свойство позволяет устанавливать и получать направление построения дуги эллипса.

        ### Синтаксис Automation

        ```
        Direction = Object.Direction                              # Получить свойство(* )
        Object.Direction = Direction                              # Установить свойство (* )
        Direction = Object.GetDirection()                         # Получить свойство (**)
        Object.SetAngle( Angle )Object.SetDirection( Direction )  # Установить свойство (**)
        ```
        """
        ...
    @Direction.setter
    def Direction(self, value: bool): ...

    @property
    def SemiAxisA(self) -> float:  # iellipsearc_semiaxisa.html
        """
        ## SemiAxisA - Длина первой полуоси

        Тип данных: double

        Свойство позволяет устанавливать и получать длину первой полуоси дуги эллипса.

        ### Синтаксис Automation

        ```
        SemiAxisA = Object.SemiAxisA       # Получить свойство(* )
        Object.SemiAxisA = SemiAxisA       # Установить свойство (* )
        SemiAxisA = Object.GetSemiAxisA()  # Получить свойство (**)
        Object.SetSemiAxisA( SemiAxisA )   # Установить свойство (**)
        ```
        """
        ...
    @SemiAxisA.setter
    def SemiAxisA(self, value: float): ...

    @property
    def SemiAxisB(self) -> float:  # iellipsearc_semiaxisb.html
        """
        ## SemiAxisB - Длина второй полуоси

        Тип данных: double

        Свойство позволяет устанавливать и получать длину второй полуоси дуги эллипса.

        ### Синтаксис Automation

        ```
        SemiAxisB = Object.SemiAxisB       # Получить свойство(* )
        Object.SemiAxisB = SemiAxisB       # Установить свойство (* )
        SemiAxisB = Object.GetSemiAxisB()  # Получить свойство (**)
        Object.SetSemiAxisB( SemiAxisB )   # Установить свойство (**)
        ```
        """
        ...
    @SemiAxisB.setter
    def SemiAxisB(self, value: float): ...

    @property
    def Style(self) -> int:  # iellipsearc_style.html
        """
        ## Style - Cтиль линии

        Тип данных: long

        1. Свойство позволяет устанавливать и получать стиль линии дуги эллипса.

        2. Системные стили линий берутся из перечисления ksCurveStyleEnum.

        ### Синтаксис Automation

        ```
        Style = Object.Style       # Получить свойство(* )
        Object.Style = Style       # Установить свойство (* )
        Style = Object.GetStyle()  # Получить свойство (**)
        Object.SetStyle( Style )   # Установить свойство (**)
        ```
        """
        ...
    @Style.setter
    def Style(self, value: int): ...

    @property
    def T1(self) -> float:  # iellipsearc_t1.html
        """
        ## T1 - Начальное значение параметра

        Тип данных: double

        Свойство позволяет устанавливать и получать начальное значение параметра.

        ### Синтаксис Automation

        ```
        T1 = Object.T1       # Получить свойство(* )
        Object.T1 = T1       # Установить свойство (* )
        T1 = Object.GetT1()  # Получить свойство (**)
        Object.SetT1( T1 )   # Установить свойство (**)
        ```
        """
        ...
    @T1.setter
    def T1(self, value: float): ...

    @property
    def T2(self) -> float:  # iellipsearc_t2.html
        """
        ## T2 - Конечное значение параметра

        Тип данных: double

        Свойство позволяет устанавливать и получать конечное значение параметра.

        ### Синтаксис Automation

        ```
        T2 = Object.T2       # Получить свойство(* )
        Object.T2 = T2       # Установить свойство (* )
        T2 = Object.GetT2()  # Получить свойство (**)
        Object.SetT2( T2 )   # Установить свойство (**)
        ```
        """
        ...
    @T2.setter
    def T2(self, value: float): ...

    @property
    def Xc(self) -> float:  # iellipsearc_xc.html
        """
        ## Xc - Координата центра дуги эллипса по оси X

        Тип данных: double

        Свойство позволяет устанавливать и получать координату центра дуги эллипса по оси X.

        ### Синтаксис Automation

        ```
        Xc = Object.Xc       # Получить свойство(* )
        Object.Xc = Xc       # Установить свойство (* )
        Xc = Object.GetXc()  # Получить свойство (**)
        Object.SetXc( Xc )   # Установить свойство (**)
        ```
        """
        ...
    @Xc.setter
    def Xc(self, value: float): ...

    @property
    def Yc(self) -> float:  # iellipsearc_yc.html
        """
        ## Yc - Координата центра дуги эллипса по оси Y

        Тип данных: double

        Свойство позволяет устанавливать и получать координату центра дуги эллипса по оси Y.

        ### Синтаксис Automation

        ```
        Yc = Object.Yc       # Получить свойство(* )
        Object.Yc = Yc       # Установить свойство (* )
        Yc = Object.GetYc()  # Получить свойство (**)
        Object.SetYc( Yc )   # Установить свойство (**)
        ```
        """
        ...
    @Yc.setter
    def Yc(self, value: float): ...




class IEquidistant(IDrawingObject):  # iequidistant.html
    """
    ## Интерфейс IEquidistant

    Интерфейс эквидистанты.

    Интерфейс позволяет задавать параметры эквидистанты.

    1. Интерфейс можно получить у коллекции эквидистант, используя свойство IEquidistants::Equidistant или метод IEquidistants::Add.

    2. После задания параметров эквидистанты требуется вызвать метод IDrawingObject::Update.
    """

    @property
    def BaseObject(self) -> IDrawingObject:  # iequidistant_baseobject.html
        """
        ## BaseObject - Базовая кривая

        Тип данных: указатель на интерфейс графического объекта IDrawingObject

        Свойство позволяет устанавливать и получать базовую кривую эквидистанты.

        ### Синтаксис Automation

        ```
        BaseObject = Object.BaseObject       # Получить свойство(* )
        Object.BaseObject = BaseObject       # Установить свойство (* )
        BaseObject = Object.GetBaseObject()  # Получить свойство (**)
        Object.SetBaseObject( BaseObject )   # Установить свойство (**)
        ```
        """
        ...
    @BaseObject.setter
    def BaseObject(self, value: IDrawingObject): ...

    @property
    def CutMode(self) -> bool:  # iequidistant_cutmode.html
        """
        ## CutMode - Обход угла

        Тип данных – BOOL.

        Значения свойства:

        +-------------------------+
        | TRUE  | - обход дугой,  |
        +-------------------------+
        | FALSE | - обход срезом. |
        +-------------------------+

        Свойство позволяет устанавливать и получать способ обхода угла кривой.

        ### Синтаксис Automation

        ```
        CutMode = Object.CutMode       # Получить свойство(* )
        Object.CutMode = CutMode       # Установить свойство (* )
        CutMode = Object.GetCutMode()  # Получить свойство (**)
        Object.SetCutMode( CutMode )   # Установить свойство (**)
        ```
        """
        ...
    @CutMode.setter
    def CutMode(self, value: bool): ...

    @property
    def DegenerateSegment(self) -> bool:  # iequidistant_degeneratesegment.html
        """
        ## DegenerateSegment - Режим отрисовки вырожденных участков

        Тип данных: BOOL

        Значения свойства:

        +-------------------------------------------+
        | TRUE  | - вырожденные сегменты разрешены, |
        +-------------------------------------------+
        | FALSE | - вырожденные сегменты запрещены. |
        +-------------------------------------------+

        Свойство позволяет устанавливать и получать режим отрисовки вырожденных участков.

        ### Синтаксис Automation

        ```
        DegenerateSegment = Object.DegenerateSegment       # Получить свойство(* )
        Object.DegenerateSegment = DegenerateSegment       # Установить свойство (* )
        DegenerateSegment = Object.GetDegenerateSegment()  # Получить свойство (**)
        Object.SetDegenerateSegment( DegenerateSegment )   # Установить свойство (**)
        ```
        """
        ...
    @DegenerateSegment.setter
    def DegenerateSegment(self, value: bool): ...

    @property
    def LeftRadius(self) -> float:  # iequidistant_leftradius.html
        """
        ## LeftRadius - Радиус эквидистанты слева

        Тип данных: double

        Свойство позволяет устанавливать и получать радиус эквидистанты слева.

        ### Синтаксис Automation

        ```
        LeftRadius = Object.LeftRadius       # Получить свойство(* )
        Object.LeftRadius = LeftRadius       # Установить свойство (* )
        LeftRadius = Object.GetLeftRadius()  # Получить свойство (**)
        Object.SetLeftRadius( LeftRadius )   # Установить свойство (**)
        ```
        """
        ...
    @LeftRadius.setter
    def LeftRadius(self, value: float): ...

    @property
    def RightRadius(self) -> float:  # iequidistant_rightradius.html
        """
        ## RightRadius - Радиус эквидистанты справа

        Тип данных: double

        Свойство позволяет устанавливать и получать радиус эквидистанты справа.

        ### Синтаксис Automation

        ```
        RightRadius = Object.RightRadius       # Получить свойство(* )
        Object.RightRadius = RightRadius       # Установить свойство (* )
        RightRadius = Object.GetRightRadius()  # Получить свойство (**)
        Object.SetRightRadius( RightRadius )   # Установить свойство (**)
        ```
        """
        ...
    @RightRadius.setter
    def RightRadius(self, value: float): ...

    @property
    def Side(self) -> int:  # iequidistant_side.html
        """
        ## Side - Тип построения

        Тип данных – из перечисления ksEquidistantTypeEnum.

        Свойство позволяет устанавливать и получать тип построения эквидистанты.

        ### Синтаксис Automation

        ```
        Side = Object.Side       # Получить свойство(* )
        Object.Side = Side       # Установить свойство (* )
        Side = Object.GetSide()  # Получить свойство (**)
        Object.SetSide( Side )   # Установить свойство (**)
        ```
        """
        ...
    @Side.setter
    def Side(self, value: int): ...

    @property
    def Style(self) -> int:  # iequidistant_style.html
        """
        ## Style - Cтиль линии

        Тип данных: long

        1. Свойство позволяет устанавливать и получать стиль линии окружности.

        2. Системные стили линий берутся из перечисления ksCurveStyleEnum.

        ### Синтаксис Automation

        ```
        Style = Object.Style       # Получить свойство(* )
        Object.Style = Style       # Установить свойство (* )
        Style = Object.GetStyle()  # Получить свойство (**)
        Object.SetStyle( Style )   # Установить свойство (**)
        ```
        """
        ...
    @Style.setter
    def Style(self, value: int): ...




class IHatch(IDrawingObject):  # ihatch.html
    """
    ## Интерфейс IHatch

    Интерфейс штриховки.

    Интерфейс позволяет задавать параметры штриховки.

    1. Интерфейс можно получить у коллекции штриховок, используя свойство IHatches::Hatch или метод IHatches::Add.

    2. После задания параметров штриховки требуется вызвать метод IDrawingObject::Update.

    3. Посредством вызова метода IUnknown::QueryInterface (const GUID far& iid, void** pif) у данного интерфейса можно получить дополнительные интерфейсы:

    •IHatchParam,

    •IBoundariesObject.
    """

    @property
    def Side(self) -> bool:  # ihatch_side.html
        """
        ## Side - Расположение штриховки

        Тип данных: BOOL

        Значения свойства:

        +---------------------+
        | TRUE | - сторона 1. |
        +---------------------+

        Свойство позволяет устанавливать и получать расположение штриховки.

        ### Синтаксис Automation

        ```
        Side = Object.Side       # Получить свойство(* )
        Object.Side = Side       # Установить свойство (* )
        Side = Object.GetSide()  # Получить свойство (**)
        Object.SetSide( Side )   # Установить свойство (**)
        ```
        """
        ...
    @Side.setter
    def Side(self, value: bool): ...

    @property
    def X(self) -> float:  # ihatch_x.html
        """
        ## X - Координата базовой точки штриховки по оси X

        Тип данных: double

        Свойство позволяет устанавливать и получать координату базовой точки штриховки по оси Х.

        ### Синтаксис Automation

        ```
        X = Object.X       # Получить свойство(* )
        Object.X = X       # Установить свойство (* )
        X = Object.GetX()  # Получить свойство (**)
        Object.SetX( X )   # Установить свойство (**)
        ```
        """
        ...
    @X.setter
    def X(self, value: float): ...

    @property
    def Y(self) -> float:  # ihatch_y.html
        """
        ## Y - Координата базовой точки штриховки по оси Y

        Тип данных: double

        Свойство позволяет устанавливать и получать координату базовой точки штриховки по оси Y.

        ### Синтаксис Automation

        ```
        Y = Object.Y       # Получить свойство(* )
        Object.Y = Y       # Установить свойство (* )
        Y = Object.GetY()  # Получить свойство (**)
        Object.SetY( Y )   # Установить свойство (**)
        ```
        """
        ...
    @Y.setter
    def Y(self, value: float): ...




class ILine(IDrawingObject):  # iline.html
    """
    ## Интерфейс ILine

    Интерфейс линии.

    Интерфейс позволяет получить и задать свойства прямой линии.

    1. Интерфейс можно получить, используя свойство коллекции линий ILines::Line или метод ILines::Add.

    2. После задания параметров линии требуется вызвать метод IDrawingObject::Update.
    """

    @property
    def Angle(self) -> float:  # iline_angle.html
        """
        ## Angle - Угол между линией и осью OX в текущей системе координат

        Тип данных: double

        Свойство позволяет устанавливать и получать угол между линией и осью OX в текущей системе координат.

        ### Синтаксис Automation

        ```
        Angle = Object.Angle       # Получить свойство(* )
        Object.Angle = Angle       # Установить свойство (* )
        Angle = Object.GetAngle()  # Получить свойство (**)
        Object.SetAngle( Angle )   # Установить свойство (**)
        ```
        """
        ...
    @Angle.setter
    def Angle(self, value: float): ...

    @property
    def X1(self) -> float:  # iline_x1.html
        """
        ## X1 - Координата первой точки по оси X

        Тип данных: double

        Свойство позволяет устанавливать и получать координату первой точки, через которую проходит линия, по оси Х.

        ### Синтаксис Automation

        ```
        X1 = Object.X1       # Получить свойство(* )
        Object.X1 = X1       # Установить свойство (* )
        X1 = Object.GetX1()  # Получить свойство (**)
        Object.SetX1( X1 )   # Установить свойство (**)
        ```
        """
        ...
    @X1.setter
    def X1(self, value: float): ...

    @property
    def X2(self) -> float:  # iline_x2.html
        """
        ## X2 - Координата второй точки по оси X

        Тип данных: double

        Свойство позволяет устанавливать и получать координату первой точки, через которую проходит линия, по оси Y.

        ### Синтаксис Automation

        ```
        X2 = Object.X2       # Получить свойство(* )
        Object.X2 = X2       # Установить свойство (* )
        X2 = Object.GetX2()  # Получить свойство (**)
        Object.SetX2( X2 )   # Установить свойство (**)
        ```
        """
        ...
    @X2.setter
    def X2(self, value: float): ...

    @property
    def Y1(self) -> float:  # iline_y1.html
        """
        ## Y1 - Координата первой точки по оси Y

        Тип данных: double

        Свойство позволяет устанавливать и получать координату первой точки, через которую проходит линия, по оси Y.

        ### Синтаксис Automation

        ```
        Y1 = Object.Y1       # Получить свойство(* )
        Object.Y1 = Y1       # Установить свойство (* )
        Y1 = Object.GetY1()  # Получить свойство (**)
        Object.SetY1( Y1 )   # Установить свойство (**)
        ```
        """
        ...
    @Y1.setter
    def Y1(self, value: float): ...

    @property
    def Y2(self) -> float:  # iline_y2.html
        """
        ## Y2 - Координата второй точки по оси Y

        Тип данных: double

        Свойство позволяет устанавливать и получать координату второй точки, через которую проходит линия, по оси Y.

        ### Синтаксис Automation

        ```
        Y2 = Object.Y2       # Получить свойство(* )
        Object.Y2 = Y2       # Установить свойство (* )
        Y2 = Object.GetY2()  # Получить свойство (**)
        Object.SetY2( Y2 )   # Установить свойство (**)
        ```
        """
        ...
    @Y2.setter
    def Y2(self, value: float): ...




class ILineSegment(IDrawingObject):  # ilinesegment.html
    """
    ## Интерфейс ILineSegment

    Интерфейс отрезка.

    1. Интерфейс можно получить у коллекции отрезков, используя свойство ILineSegments::LineSegment или метод ILineSegments::Add.

    2. При создании отрезка можно использовать несколько типов построения:

    2.1. по координатам (требуется задать координаты точек (X1; Y1), (X2; Y2).

    2.2. По точке 1 (X1; Y1), длине Length и углу Angle,-

    По точке 2 (X2; Y2), длине Length и углу Angle относительно точки 1.

    3. После задания параметров отрезка требуется вызвать метод IDrawingObject::Update.

    4. При изменении параметров зависимые параметры пересчитываются после вызова метода IDrawingObject::Update.
    """

    @property
    def Angle(self) -> float:  # ilinesegment_angle.html
        """
        ## Angle - Угол наклона

        Тип данных: double

        Свойство позволяет получить и установить угол наклона отрезка.

        ### Синтаксис Automation

        ```
        Angle = iObject.Angle;       # Получить свойство(* )
        iObject.Angle = Angle;       # Установить свойство (* )
        Angle = iObject.GetAngle();  # Получить свойство (**)
        iObject.SetAngle( Angle );   # Установить свойство (**)
        ```
        """
        ...
    @Angle.setter
    def Angle(self, value: float): ...

    @property
    def Length(self) -> float:  # ilinesegment_length.html
        """
        ## Length - Длина отрезка

        Тип данных: double

        Свойство позволяет получить и установить длину отрезка.

        ### Синтаксис Automation

        ```
        Length = iObject.Length;       # Получить свойство(* )
        iObject.Length = Length;       # Установить свойство (* )
        Length = iObject.GetLength();  # Получить свойство (**)
        iObject.SetLength( Length );   # Установить свойство (**)
        ```
        """
        ...
    @Length.setter
    def Length(self, value: float): ...

    @property
    def Style(self) -> int:  # ilinesegment_style.html
        """
        ## Style - Стиль линии

        Тип данных: long

        Свойство позволяет получить и установить стиль линии отрезка.

        ### Синтаксис Automation

        ```
        style = iObject.style;       # Получить свойство(* )
        iObject.style = style;       # Установить свойство (* )
        style = iObject.GetStyle();  # Получить свойство (**)
        iObject.SetStyle( style );   # Установить свойство (**)
        ```
        """
        ...
    @Style.setter
    def Style(self, value: int): ...

    @property
    def X1(self) -> float:  # ilinesegment_x1.html
        """
        ## X1 - Координата первой точки по оси X

        Тип данных: double

        Свойство позволяет получить и установить координату первой точки отрезка по оси X.

        ### Синтаксис Automation

        ```
        X1 = iObject.X1;       # Получить свойство(* )
        iObject.X1 = X1;       # Установить свойство (* )
        X1 = iObject.GetX1();  # Получить свойство (**)
        iObject.SetX1( X1 );   # Установить свойство (**)
        ```
        """
        ...
    @X1.setter
    def X1(self, value: float): ...

    @property
    def X2(self) -> float:  # ilinesegment_x2.html
        """
        ## X2 - Координата второй точки по оси X

        Тип данных: double

        Свойство позволяет получить и установить координату второй точки отрезка по оси X.

        ### Синтаксис Automation

        ```
        X2 = iObject.X2;       # Получить свойство(* )
        iObject.X2 = X2;       # Установить свойство (* )
        X2 = iObject.GetX2();  # Получить свойство (**)
        iObject.SetX2( X2 );   # Установить свойство (**)
        ```
        """
        ...
    @X2.setter
    def X2(self, value: float): ...

    @property
    def Y1(self) -> float:  # ilinesegment_y1.html
        """
        ## Y1 - Координата первой точки по оси Y

        Тип данных: double

        Свойство позволяет получить и установить координату первой точки отрезка по оси Y.

        ### Синтаксис Automation

        ```
        Y1 = iObject.Y1;       # Получить свойство(* )
        iObject.Y1 = Y1;       # Установить свойство (* )
        Y1 = iObject.GetY1();  # Получить свойство (**)
        iObject.SetY1( Y1 );   # Установить свойство (**)
        ```
        """
        ...
    @Y1.setter
    def Y1(self, value: float): ...

    @property
    def Y2(self) -> float:  # ilinesegment_y2.html
        """
        ## Y2 - Координата второй точки по оси Y

        Тип данных: double

        Свойство позволяет получить и установить координату второй точки отрезка по оси Y.

        ### Синтаксис Automation

        ```
        Y2 = iObject.Y2;       # Получить свойство(* )
        iObject.Y2 = Y2;       # Установить свойство (* )
        Y2 = iObject.GetY2();  # Получить свойство (**)
        iObject.SetY2( Y2 );   # Установить свойство (**)
        ```
        """
        ...
    @Y2.setter
    def Y2(self, value: float): ...




class IMultiline(IDrawingObject):  # imultiline.html
    """
    ## Интерфейс IMultiline

    Интерфейс мультилинии.

    1. Интерфейс позволяет получить и задать свойства объекта "мультилиния". Для создания объекта должен быть задан базовый контур и хотя бы одна линия.

    2. Получить интерфейс можно, используя свойство IMultilines::MultiLine или метод IMultilines::Add интерфейса коллекции мультилиний.

    3. После задания параметров объекта требуется вызвать метод IDrawingObject::Update.
    """

    @property
    def BaseContour(self) -> IContour:  # imultiline_basecontour.html
        """
        ## BaseContour - Базовый контур

        Тип данных: указатель на интерфейс контура IContour.

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        BaseContour = iObject.BaseContour();     # Получить свойство(* )
        BaseContour = iObject.GetBaseContour();  # Получить свойство (**)
        ```
        """
        ...

    @property
    def Closed(self) -> bool:  # imultiline_closed.html
        """
        ## Closed - Замкнутость

        Тип данных: BOOL

        Значения свойства:

        +-----------------------------------+
        | TRUE  | - мультилиния замкнута,   |
        +-----------------------------------+
        | FALSE | - мультилиния разомкнута. |
        +-----------------------------------+

        1. Свойство позволяет устанавливать и получать замкнутость.

        2. Если при создании замкнутой линии не совпадают точки начала и конца, будет создан дополнительный сегмент.

        3. Ограничители у замкнутой мультилинии не учитываются.

        ### Синтаксис Automation

        ```
        Closed = iObject.Closed;       # Получить свойство(* )
        iObject.Closed = Closed;       # Установить свойство (* )
        Closed = iObject.GetClosed();  # Получить свойство (**)
        iObject.SetClosed(Closed);     # Установить свойство (**)
        ```
        """
        ...
    @Closed.setter
    def Closed(self, value: bool): ...

    @property
    def LineCount(self) -> int:  # imultiline_linecount.html
        """
        ## LineCount - Количество линий

        Тип данных: long

        1. Свойство позволяет получить количество линий в мультилинии.

        2. Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        LineCount = iObject.LineCount();     # Получить свойство(* )
        LineCount = iObject.GetLineCount();  # Получить свойство (**)
        ```
        """
        ...

    @property
    def LineOffsets(self) -> typing.Any:  # imultiline_lineoffsets.html
        """
        ## LineOffsets - Массив смещений в виде массива SAFEARRAY double - VT_ARRAY | VT_R8

        Тип данных: VARIANT

        Значения свойства:

        Массив SafeArray типа VT_ARRAY | VT_R8.

        1. Свойство позволяет получить массив SAFEARRAY смещения всех линий.

        2. Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        LineOffsets = iObject.LineOffsets;       # Получить свойство(* )
        LineOffsets = iObject.GetLineOffsets();  # Получить свойство (**)
        ```
        """
        ...

    @property
    def VertexCount(self) -> IContour:  # imultiline_vertexcount.html
        """
        ## VertexCount - Количество вершин без учета концов

        Тип данных: указатель на интерфейс контура IContour

        1. Свойство позволяет получить количество вершин мультилинии.

        2. Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        VertexCount = iObject.VertexCount();     # Получить свойство(* )
        VertexCount = iObject.GetVertexCount();  # Получить свойство (**)
        ```
        """
        ...

    def AddLine(self, Offset, Style) -> bool:  # imultiline_addline.html
        """
        ## AddLine - Добавить линию

        ### Синтаксис Automation

        ```
        BOOL AddLine( double Offset, long Style );
        ```

        ### Входные параметры

        +----------------------------------------------------------+
        | Offset | - смещение линии относительно базового контура, |
        +----------------------------------------------------------+
        | Style  | - стиль линии из перечисления ksCurveStyleEnum. |
        +----------------------------------------------------------+

        ### Возвращаемое значение

        +-----------------------------------------+
        | TRUE  | - в случае удачного завершения, |
        +-----------------------------------------+
        | FALSE | - в случае неудачи.             |
        +-----------------------------------------+
        """
        ...

    def DeleteLine(self, LineIndex) -> bool:  # imultiline_deleteline.html
        """
        ## DeleteLine - Удалить линию

        ### Синтаксис Automation

        ```
        BOOL DeleteLine( long LineIndex );
        ```

        ### Входные параметры

        +-----------------------------+
        | LineIndex | - индекс линии. |
        +-----------------------------+

        ### Возвращаемое значение

        +-----------------------------------------+
        | TRUE  | - в случае удачного завершения, |
        +-----------------------------------------+
        | FALSE | - в случае неудачи.             |
        +-----------------------------------------+
        """
        ...

    def EndLimiter(self, First) -> int:  # imultiline_endlimiter.html
        """
        ## EndLimiter - Типы ограничений на концах мультилинии

        Тип данных: из перечисления ksMlEndLimiterEnum

        Свойство позволяет устанавливать и получать типы ограничений на концах мультилинии.

        ### Синтаксис Automation

        ```
        EndLimiter = iObject.EndLimiter( First );     # Получить свойство(* )
        iObject.EndLimiter( First ) = EndLimiter;     # Установить свойство (* )
        EndLimiter = iObject.GetEndLimiter( First );  # Получить свойство (**)
        iObject.SetEndLimiter( First, EndLimiter );   # Установить свойство (**)
        ```

        ### Входные параметры

        +---------------------------------------------------------------+
        | First | - номер ограничителя (TRUE - первая, FALSE - вторая). |
        +---------------------------------------------------------------+
        """
        ...

    def EndParameter(self, First) -> float:  # imultiline_endparameter.html
        """
        ## EndParameter - Параметр ограничителя в %(0-100)

        Тип данных: double

        Свойство позволяет устанавливать и получать параметр ограничителя в процентах от ширины мультилинии.

        ### Синтаксис Automation

        ```
        EndParameter = iObject.EndParameter( First );  # Получить свойство(* )
        iObject.EndParameter( First ) = EndParameter;  # Установить свойство (* )
        EndParameter = iObject.EndParameter( First );  # Получить свойство (**)
        iObject.EndParameter( First, EndParameter );   # Установить свойство (**)
        ```

        ### Входные параметры

        +---------------------------------------------------------------+
        | First | - номер ограничителя (TRUE - первая, FALSE - вторая). |
        +---------------------------------------------------------------+
        """
        ...

    def EndStyle(self, First) -> int:  # imultiline_endstyle.html
        """
        ## EndStyle - Стиль линии кривой в ограничителе

        Тип данных: long (соответствует перечислению ksCurveStyleEnum)

        Свойство позволяет устанавливать и получать стиль линии в ограничителе.

        ### Синтаксис Automation

        ```
        EndStyle = iObject.EndStyle( First );     # Получить свойство(* )
        iObject.EndStyle( First ) = EndStyle;     # Установить свойство (* )
        EndStyle = iObject.GetEndStyle( First );  # Получить свойство (**)
        iObject.SetEndStyle( First, EndStyle );   # Установить свойство (**)
        ```

        ### Входные параметры

        +---------------------------------------------------------------+
        | First | - номер ограничителя (TRUE - первая, FALSE - вторая). |
        +---------------------------------------------------------------+
        """
        ...

    def FindLine(self, Offset):  # imultiline_findline.html
        """
        ## FindLine - Найти индекс линии по смещению

        ### Синтаксис Automation

        ```
        long FindLine( double Offset );
        ```

        ### Входные параметры

        +----------------------------------------------------------+
        | Offset | - смещение линии относительно базового контура. |
        +----------------------------------------------------------+

        ### Возвращаемое значение

        - индекс линии, имеющей указанное смещение.
        """
        ...

    def LineOffset(self, LineIndex) -> float:  # imultiline_lineoffset.html
        """
        ## LineOffset - Смещение линии

        Тип данных: double

        Свойство позволяет устанавливать смещение линии относительно базового контура.

        ### Синтаксис Automation

        ```
        LineOffset = iObject.LineOffset( LineIndex );      # Получить свойство(* )
        iObject.LineOffset( LineIndex ) = LineOffset;      # Установить свойство (* )
        LineOffset = iObject.GetLineOffset( LineIndex );   # Получить свойство (**)
        iObject.SetLineOffset( LineIndex, LineOffset ););  # Установить свойство (**)
        ```

        ### Входные параметры

        +----------------------------------+
        | long LineIndex | - индекс линии. |
        +----------------------------------+
        """
        ...

    def LineStyle(self, LineIndex) -> int:  # imultiline_linestyle.html
        """
        ## LineStyle - Стиль линии

        Тип данных: long (соответствует перечислению ksCurveStyleEnum)

        Свойство позволяет устанавливать и получать стиль линии.

        ### Синтаксис Automation

        ```
        LineStyle = iObject.LineStyle( LineIndex );     # Получить свойство(* )
        iObject.LineStyle( LineIndex ) = LineStyle;     # Установить свойство (* )
        LineStyle = iObject.GetLineStyle( LineIndex );  # Получить свойство (**)
        iObject.SetLineStyle( LineIndex, LineStyle );   # Установить свойство (**)
        ```

        ### Входные параметры

        +----------------------------------+
        | long LineIndex | - индекс линии. |
        +----------------------------------+
        """
        ...

    def SetEndLimiter(self, First, arg1):
        ...

    def SetEndParameter(self, First, arg1):
        ...

    def SetEndStyle(self, First, arg1):
        ...

    def SetLineOffset(self, LineIndex, arg1):
        ...

    def SetLineStyle(self, LineIndex, arg1):
        ...

    def SetTrackingType(self, VertexIndex, arg1):
        ...

    def SetVertexDirection(self, VertexIndex, arg1):
        ...

    def SetVertexLimiter(self, VertexIndex, arg1):
        ...

    def SetVertexRadius(self, VertexIndex, arg1):
        ...

    def SetVertexStyle(self, VertexIndex, arg1):
        ...

    def TrackingType(self, VertexIndex) -> int:  # imultiline_trackingtype.html
        """
        ## TrackingType - Тип обхода вершины

        Тип данных: из перечисления ksMlVertexTrackingEnum

        Свойство позволяет устанавливать и получать тип обхода вершины мультилинии.

        ### Синтаксис Automation

        ```
        TrackingType = iObject.TrackingType( LineIndex );     # Получить свойство(* )
        iObject.TrackingType( LineIndex ) = TrackingType;     # Установить свойство (* )
        TrackingType = iObject.GetTrackingType( LineIndex );  # Получить свойство (**)
        iObject.SetTrackingType( LineIndex, TrackingType );   # Установить свойство (**)
        ```

        ### Входные параметры

        +----------------------------------+
        | long LineIndex | - индекс линии. |
        +----------------------------------+
        """
        ...

    def VertexDirection(self, VertexIndex) -> bool:  # imultiline_vertexdirection.html
        """
        ## VertexDirection - Направление дуги в вершине

        Тип данных: BOOL

        Значения свойства:

        +-----------------------------------+
        | TRUE  | - по часовой стрелке,     |
        +-----------------------------------+
        | FALSE | - против часовой стрелки. |
        +-----------------------------------+

        1. Свойство позволяет устанавливать и получать направление дуги в вершине мультилинии.

        2. Свойство доступно только при типе обхода вершины ksMTrShear (ksMlEndLimiterEnum).

        ### Синтаксис Automation

        ```
        VertexDirection = iObject.VertexDirection( VertexIndex );     # Получить свойство(* )
        iObject.VertexDirection( VertexIndex ) = VertexDirection;     # Установить свойство (* )
        VertexDirection = iObject.GetVertexDirection( VertexIndex );  # Получить свойство (**)
        iObject.SetVertexDirection( VertexIndex, VertexDirection );   # Установить свойство (**)
        ```

        ### Входные параметры

        +--------------------------------------+
        | long VertexIndex | - индекс вершины. |
        +--------------------------------------+
        """
        ...

    def VertexLimiter(self, VertexIndex) -> int:  # imultiline_vertexlimiter.html
        """
        ## VertexLimiter - Типы ограничений в вершинах мультилинии

        Тип данных: из перечисления ksMlVertexLimiterEnum

        1. Свойство позволяет устанавливать и получать тип ограничения в вершине мультилинии.

        2. Свойство доступно только при типе обхода вершины ksMTrShear (ksMlEndLimiterEnum).

        ### Синтаксис Automation

        ```
        VertexLimiter = iObject.VertexLimiter( VertexIndex );     # Получить свойство(* )
        iObject.VertexLimiter( VertexIndex ) = VertexLimiter;     # Установить свойство (* )
        VertexLimiter = iObject.GetVertexLimiter( VertexIndex );  # Получить свойство (**)
        iObject.SetVertexLimiter( VertexIndex, VertexLimiter );   # Установить свойство (**)
        ```

        ### Входные параметры

        +--------------------------------------+
        | long VertexIndex | - индекс вершины. |
        +--------------------------------------+
        """
        ...

    def VertexRadius(self, VertexIndex) -> float:  # imultiline_vertexradius.html
        """
        ## VertexRadius - Радиус дуги в вершине

        Тип данных: double

        1. Свойство позволяет устанавливать и получать радиус дуги в вершине мультилинии.

        2. Свойство доступно только при типе обхода вершины ksMTrShear (ksMlEndLimiterEnum).

        ### Синтаксис Automation

        ```
        VertexRadius = iObject.VertexRadius( VertexIndex );     # Получить свойство(* )
        iObject.VertexRadius( VertexIndex ) = VertexRadius;     # Установить свойство (* )
        VertexRadius = iObject.GetVertexRadius( VertexIndex );  # Получить свойство (**)
        iObject.SetVertexRadius( VertexIndex, VertexRadius );   # Установить свойство (**)
        ```

        ### Входные параметры

        +--------------------------------------+
        | long VertexIndex | - индекс вершины. |
        +--------------------------------------+
        """
        ...

    def VertexStyle(self, VertexIndex) -> int:  # imultiline_vertexstyle.html
        """
        ## VertexStyle - Стиль кривой в вершине

        Тип данных: long (соответствует перечислению ksCurveStyleEnum)

        1. Свойство позволяет устанавливать и получать тип ограничения в вершине мультилинии.

        2. Свойство доступно только при типе обхода вершины ksMTrShear (ksMlEndLimiterEnum).

        ### Синтаксис Automation

        ```
        VertexStyle = iObject.VertexStyle( VertexIndex );     # Получить свойство(* )
        iObject.VertexStyle( VertexIndex ) = VertexStyle;     # Установить свойство (* )
        VertexStyle = iObject.GetVertexStyle( VertexIndex );  # Получить свойство (**)
        iObject.SetVertexStyle( VertexIndex, VertexStyle );   # Установить свойство (**)
        ```

        ### Входные параметры

        +--------------------------------------+
        | long VertexIndex | - индекс вершины. |
        +--------------------------------------+
        """
        ...




class INurbs(IDrawingObject):  # inurbs.html
    """
    ## Интерфейс INurbs

    Интерфейс NURBS-кривой.

    Интерфейс позволяет задавать параметры NURBS-кривой.

    1. Интерфейс можно получить у коллекции NURBS-кривых, используя свойство INurbses::Nurbs или метод INurbses::Add.

    2. После задания параметров NURBS-кривой требуется вызвать метод IDrawingObject::Update.
    """

    @property
    def Closed(self) -> bool:  # inurbs_closed.html
        """
        ## Closed - Замкнутость

        Тип данных: BOOL

        Свойство позволяет устанавливать и получать признак замкнутости NURBS-кривой.

        ### Синтаксис Automation

        ```
        Closed = Object.Closed       # Получить свойство(* )
        Object.Closed = Closed       # Установить свойство (* )
        Closed = Object.GetClosed()  # Получить свойство (**)
        Object.SetClosed( Closed )   # Установить свойство (**)
        ```
        """
        ...
    @Closed.setter
    def Closed(self, value: bool): ...

    @property
    def Degree(self) -> int:  # inurbs_degree.html
        """
        ## Degree - Порядок кривой

        Тип данных: long

        Свойство позволяет устанавливать и получать порядок NURBS-кривой.

        Для изменения пордка NURBS-кривой нужно, чтобы количество точек было равно или больше устанавливаемого значения, см. Пример...

        ### Синтаксис Automation

        ```
        Degree = Object.Degree       # Получить свойство(* )
        Object.Degree = Degree       # Установить свойство (* )
        Degree = Object.GetDegree()  # Получить свойство (**)
        Object.SetDegree( Degree )   # Установить свойство (**)
        ```
        """
        ...
    @Degree.setter
    def Degree(self, value: int): ...

    @property
    def Periodic(self) -> bool:  # inurbs_periodic.html
        """
        ## Periodic - Порядок кривой

        Тип данных: BOOL

        1. Свойство позволяет получить признак периодичности NURBS-кривой.

        2. Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        Periodic = Object.Periodic  # Получить свойство(* )
        Periodic = Object.Periodic  # Получить свойство (**)
        ```
        """
        ...

    @property
    def PointsCount(self) -> int:  # inurbs_pointscount.html
        """
        ## PointsCount - Количество точек

        Тип данных: long

        1. Свойство позволяет получить количество точек NURBS-кривой.

        2. Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        PointsCount = Object.PointsCount       # Получить свойство(* )
        PointsCount = Object.GetPointsCount()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def Style(self) -> int:  # inurbs_style.html
        """
        ## Style - Cтиль линии

        Тип данных: long

        Свойство позволяет устанавливать и получать стиль линии NURBS-кривой.

        ### Синтаксис Automation

        ```
        Style = Object.Style       # Получить свойство(* )
        Object.Style = Style       # Установить свойство (* )
        Style = Object.GetStyle()  # Получить свойство (**)
        Object.SetStyle( Style )   # Установить свойство (**)
        ```
        """
        ...
    @Style.setter
    def Style(self, value: int): ...

    def AddPoint(self, Index, X, Y, Weight) -> bool:  # inurbs_addpoint.html
        """
        ## AddPoint - Добавить точку

        Метод позволяет добавить точку в NURBS-кривую.

        ### Синтаксис Automation

        ```
        BOOL AddPoint( long Index,
        double X,
        double Y
        double Weight );
        ```

        ### Входные параметры

        +------------------------------------------+
        | Index  | - индекс добавляемой точки,     |
        +------------------------------------------+
        | X, Y   | - координаты добавляемой точки, |
        +------------------------------------------+
        | Weight | - вес точки.                    |
        +------------------------------------------+

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def Clear(self) -> bool:  # inurbs_clear.html
        """
        ## Clear - Удалить все точки

        Метод позволяет удалить все точки NURBS-кривой.

        ### Синтаксис Automation

        ```
        BOOL Clear();
        ```

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def DeletePoint(self, Index) -> bool:  # inurbs_deletepoint.html
        """
        ## DeletePoint - Удалить точку

        Метод позволяет удалить заданную точку NURBS-кривой.

        ### Синтаксис Automation

        ```
        BOOL DeletePoint( long Index );
        ```

        ### Входные параметры

        +-----------------------------------+
        | Index | - индекс удаляемой точки. |
        +-----------------------------------+

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def GetNurbsParams(self) -> bool:  # inurbs_getnurbsparams.html
        """
        ## GetNurbsParams - Получить параметры NURBS-кривой

        Метод позволяет получить параметры NURBS-кривой в виде массивов SAFEARRAY | VT_R8.

        ### Синтаксис Automation

        ```
        BOOL GetNurbsParams( VARIANT * Points,
        VARIANT * Weight,
        VARIANT * Knots );
        ```

        ### Выходные параметры

        +-------------------------------------------------------------+
        | Points | - массив SAFEARRAY | VT_R8 координат точек кривой, |
        +-------------------------------------------------------------+
        | Weight | - массив SAFEARRAY | VT_R8 весов точек,            |
        +-------------------------------------------------------------+
        | Knots  | - массив SAFEARRAY | VT_R8 узлов сплайна.          |
        +-------------------------------------------------------------+

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def GetPoint(self, Index) -> bool:  # inurbs_getpoint.html
        """
        ## GetPoint - Получить параметры точки

        Метод позволяет получить параметры точки NURBS-кривой в виде массивов SAFEARRAY | VT_R8.

        ### Синтаксис Automation

        ```
        BOOL GetPoint( long Index,
        double * X,
        double * Y,
        double * Weight );
        ```

        ### Входные параметры

        +-------------------------+
        | Index | - индекс точки. |
        +-------------------------+

        ### Выходные параметры

        +--------------------------------------------------+
        | x, y   | - координаты точки,                     |
        +--------------------------------------------------+
        | Weight | - массив SAFEARRAY | VT_R8 весов точек. |
        +--------------------------------------------------+

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def ReadFromFile(self, FileName) -> bool:  # inurbs_readfromfile.html
        """
        ## ReadFromFile - Прочитать файл с данными

        ### Синтаксис Automation

        ```
        BOOL ReadFromFile( LPCTSTR FileName );
        ```

        ### Входные параметры

        +------------------------------------------+
        | FileName | - полное имя файла с данными. |
        +------------------------------------------+

        ### Возвращаемое значение

        +--------------------------+
        | TRUE | - в случае удачи. |
        +--------------------------+
        """
        ...

    def SetNurbsParams(self, Points, Weight, Knots, Degree, Closed) -> bool:  # inurbs_setnurbsparams.html
        """
        ## SetNurbsParams - Установить параметры NURBS-кривой

        Метод позволяет установить параметры NURBS-кривой в виде массивов SAFEARRAY | VT_R8.

        ### Синтаксис Automation

        ```
        BOOL SetNurbsParams( VARIANT Points,
        VARIANT Weight,
        VARIANT Knots,
        long Degree,
        BOOL Closed );
        ```

        ### Входные параметры

        +----------------------------------------------------------------------+
        | Points | - массив SAFEARRAY | VT_R8 координат точек кривой,          |
        +----------------------------------------------------------------------+
        | Weight | - массив SAFEARRAY | VT_R8 весов точек,                     |
        +----------------------------------------------------------------------+
        | Knots  | - массив SAFEARRAY | VT_R8 узлов сплайна,                   |
        +----------------------------------------------------------------------+
        | Degree | - порядок кривой,                                           |
        +----------------------------------------------------------------------+
        | Closed | - признак замкнутости (нужно задавать после задания точек). |
        +----------------------------------------------------------------------+

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def WriteToFile(self, FileName) -> bool:  # inurbs_writetofile.html
        """
        ## WriteToFile - Записать файл с данными

        ### Синтаксис Automation

        ```
        BOOL WriteToFile( LPCTSTR FileName );
        ```

        ### Входные параметры

        +------------------------------------------+
        | FileName | - полное имя файла с данными. |
        +------------------------------------------+

        ### Возвращаемое значение

        +-----------------------------------------+
        | TRUE | - в случае успешного завершения. |
        +-----------------------------------------+
        """
        ...




class INurbsByPoints(INurbs):  # inurbsbypoints.html
    """
    ## Интерфейс INurbsByPoints

    Интерфейс NURBS-кривой по точкам.
    """

    @property
    def PointsBuildingType(self) -> int:  # inurbsbypoints_pointsbuildingtype.html
        """
        ## PointsBuildingType - Способ формирования точек сплайна

        Тип данных: из перечисления ksNurbsByPointsBuildingTypeEnum

        ### Синтаксис Automation

        ```
        PointsBuildingType = Object.PointsBuildingType       # Получить свойство(* )
        Object.PointsBuildingType = PointsBuildingType       # Установить свойство (* )
        PointsBuildingType = Object.GetPointsBuildingType()  # Получить свойство (**)
        Object.SetPointsBuildingType( PointsBuildingType )   # Установить свойство (**)
        ```
        """
        ...
    @PointsBuildingType.setter
    def PointsBuildingType(self, value: int): ...

    def AddPointByParam(self, Index, X, Y, ConstraintsType, DerivativeLenght, DerivativeAngle, Curvature) -> bool:  # inurbsbypoints_addpointbyparam.html
        """
        ## AddPointByParam - Добавить новую точку с параметрами

        ### Синтаксис Automation

        ```
        BOOL AddPointByParam( long Index, double X, double Y,
        ksNurbsByPointsPointConstraintsEnum ConstraintsType,
        double * DerivativeLenght, double * DerivativeAngle, double * Curvature );
        ```

        ### Входные параметры

        +---------------------------------------------------------+
        | Index            | - индекс добавляемой точки,          |
        +---------------------------------------------------------+
        | X                | - координата точки по X,             |
        +---------------------------------------------------------+
        | Y                | - координата точки по Y,             |
        +---------------------------------------------------------+
        | ConstraintsType  | - вариант управления точкой сплайна, |
        +---------------------------------------------------------+
        | DerivativeLenght | - длина вектора производной,         |
        +---------------------------------------------------------+
        | DerivativeAngle  | - угол вектора производной,          |
        +---------------------------------------------------------+
        | Curvature        | - величина кривизны.                 |
        +---------------------------------------------------------+

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def CurvatureInPoint(self, PointIndex) -> float:  # inurbsbypoints_curvatureinpoint.html
        """
        ## CurvatureInPoint - Величина кривизны в указанной точке

        Тип данных: double

        ### Синтаксис Automation

        ```
        CurvatureInPoint = Object.CurvatureInPoint( PointIndex )     # Получить свойство(* )
        Object.CurvatureInPoint( PointIndex ) = CurvatureInPoint     # Установить свойство (* )
        CurvatureInPoint = Object.GetCurvatureInPoint( PointIndex )  # Получить свойство (**)
        Object.SetCurvatureInPoint( PointIndex, CurvatureInPoint )   # Установить свойство (**)
        ```

        ### Входные параметры

        +-------------------------------------+
        | long | - PointIndex - индекс точки. |
        +-------------------------------------+
        """
        ...

    def DerivativeAngleInPoint(self, PointIndex) -> float:  # inurbsbypoints_derivativeangleinpoint.html
        """
        ## DerivativeAngleInPoint - Угол вектора производнойв указанной точке

        Тип данных: double

        ### Синтаксис Automation

        ```
        DerivativeAngleInPoint = Object.DerivativeAngleInPoint( PointIndex )     # Получить свойство(* )
        Object.DerivativeAngleInPoint( PointIndex ) = DerivativeAngleInPoint     # Установить свойство (* )
        DerivativeAngleInPoint = Object.GetDerivativeAngleInPoint( PointIndex )  # Получить свойство (**)
        Object.SetDerivativeAngleInPoint( PointIndex, DerivativeAngleInPoint )   # Установить свойство (**)
        ```

        ### Входные параметры

        +-------------------------------------+
        | long | - PointIndex - индекс точки. |
        +-------------------------------------+
        """
        ...

    def DerivativeLenghtInPoint(self, PointIndex) -> float:  # inurbsbypoints_derivativelenghtinpoint.html
        """
        ## DerivativeLenghtInPoint - Длина вектора производной в указанной точке

        Тип данных: double

        ### Синтаксис Automation

        ```
        DerivativeLenghtInPoint = Object.DerivativeLenghtInPoint( PointIndex )     # Получить свойство(* )
        Object.DerivativeLenghtInPoint( PointIndex ) = DerivativeLenghtInPoint     # Установить свойство (* )
        DerivativeLenghtInPoint = Object.GetDerivativeLenghtInPoint( PointIndex )  # Получить свойство (**)
        Object.SetDerivativeLenghtInPoint( PointIndex, DerivativeLenghtInPoint )   # Установить свойство (**)
        ```

        ### Входные параметры

        +-------------------------------------+
        | long | - PointIndex - индекс точки. |
        +-------------------------------------+
        """
        ...

    def GetPointParam(self, Index) -> bool:  # inurbsbypoints_getpointparam.html
        """
        ## GetPointParam - Получить параметры точек сплайна

        ### Синтаксис Automation

        ```
        BOOL GetPointParam( long Index, double * X, double * Y,
        ksNurbsByPointsPointConstraintsEnum * ConstraintsType, double * DerivativeLenght,
        double * DerivativeAngle, double * Curvature );
        ```

        ### Входные параметры

        +---------------------------------------------------------+
        | X                | - координата точки по X,             |
        +---------------------------------------------------------+
        | Y                | - координата точки по Y,             |
        +---------------------------------------------------------+
        | ConstraintsType  | - вариант управления точкой сплайна, |
        +---------------------------------------------------------+
        | DerivativeLenght | - длина вектора производной,         |
        +---------------------------------------------------------+
        | DerivativeAngle  | - угол вектора производной,          |
        +---------------------------------------------------------+
        | Curvature        | - величина кривизны.                 |
        +---------------------------------------------------------+

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def PointConstraints(self, PointIndex) -> int:  # inurbsbypoints_pointconstraints.html
        """
        ## PointConstraints - Вариант управления точкой сплайна

        Тип данных: из перечисления ksNurbsByPointsPointConstraintsEnum

        ### Синтаксис Automation

        ```
        PointConstraints = Object.PointConstraints( PointIndex )     # Получить свойство(* )
        Object.PointConstraints( PointIndex ) = PointConstraints     # Установить свойство (* )
        PointConstraints = Object.GetPointConstraints( PointIndex )  # Получить свойство (**)
        Object.SetPointConstraints( PointIndex, PointConstraints )   # Установить свойство (**)
        ```

        ### Входные параметры

        +-------------------------------------+
        | long | - PointIndex - индекс точки. |
        +-------------------------------------+
        """
        ...

    def SetCurvatureInPoint(self, PointIndex, arg1):
        ...

    def SetDerivativeAngleInPoint(self, PointIndex, arg1):
        ...

    def SetDerivativeLenghtInPoint(self, PointIndex, arg1):
        ...

    def SetPointConstraints(self, PointIndex, arg1):
        ...




class IPoint(IDrawingObject):  # ipoint.html
    """
    ## Интерфейс IPoint

    Интерфейс точки.

    1. Интерфейс можно получить, используя свойство коллекции точек IPoints::Point или метод IPoints::Add.

    2. После задания параметров окружности требуется вызвать метод IDrawingObject::Update.
    """

    @property
    def Angle(self) -> float:  # ipoint_angle.html
        """
        ## Angle - Угол для точки со стрелкой

        Тип данных: double

        Свойство позволяет устанавливать и получать угол для точки со стрелкой из перечисления ksAnnotativeTerminatorSignEnum.

        ### Синтаксис Automation

        ```
        Angle = Object.Angle       # Получить свойство(* )
        Object.Angle = Angle       # Установить свойство (* )
        Angle = Object.GetAngle()  # Получить свойство (**)
        Object.SetAngle( Angle )   # Установить свойство (**)
        ```
        """
        ...
    @Angle.setter
    def Angle(self, value: float): ...

    @property
    def Style(self) -> int:  # ipoint_style.html
        """
        ## Style - Cтиль точки

        Тип данных: long

        1. Свойство позволяет устанавливать и получать стиль точки.

        2. Стили точек берутся из перечисления ksAnnotationSymbolEnum.

        ### Синтаксис Automation

        ```
        Style = Object.Style       # Получить свойство(* )
        Object.Style = Style       # Установить свойство (* )
        Style = Object.GetStyle()  # Получить свойство (**)
        Object.SetStyle( Style )   # Установить свойство (**)
        ```
        """
        ...
    @Style.setter
    def Style(self, value: int): ...

    @property
    def X(self) -> float:  # ipoint_x.html
        """
        ## X - Координата точки по оси X

        Тип данных: double

        Свойство позволяет устанавливать и получать координату точки по оси X.

        ### Синтаксис Automation

        ```
        X = Object.X       # Получить свойство(* )
        Object.X = X       # Установить свойство (* )
        X = Object.GetX()  # Получить свойство (**)
        Object.SetXc( X )  # Установить свойство (**)
        ```
        """
        ...
    @X.setter
    def X(self, value: float): ...

    @property
    def Y(self) -> float:  # ipoint_y.html
        """
        ## Y - Координата точки по оси Y

        Тип данных: double

        Свойство позволяет устанавливать и получать координату точки по оси Y.

        ### Синтаксис Automation

        ```
        Y = Object.Y       # Получить свойство(* )
        Object.Y = Y       # Установить свойство (* )
        Y = Object.GetY()  # Получить свойство (**)
        Object.SetY( Y )   # Установить свойство (**)
        ```
        """
        ...
    @Y.setter
    def Y(self, value: float): ...




class IPolyLine2D(IDrawingObject):  # ipolyline2d.html
    """
    ## Интерфейс IPolyLine2D

    Интерфейс полилинии (ломаной).

    Интерфейс позволяет задавать параметры ломаной.

    1. Интерфейс можно получить у коллекции полилиний, используя свойство IPolyLines2D::PolyLine2D или метод IPolyLines2D::Add.

    2. После задания параметров полилинии требуется вызвать метод IDrawingObject::Update.
    """

    @property
    def Closed(self) -> bool:  # ipolyline2d_closed.html
        """
        ## Closed - Замкнутость

        Тип данных: BOOL

        Свойство позволяет устанавливать и получать признак замкнутости полилинии.

        ### Синтаксис Automation

        ```
        Closed = Object.Closed       # Получить свойство(* )
        Object.Closed = Closed       # Установить свойство (* )
        Closed = Object.GetClosed()  # Получить свойство (**)
        Object.SetClosed( Closed )   # Установить свойство (**)
        ```
        """
        ...
    @Closed.setter
    def Closed(self, value: bool): ...

    @property
    def Points(self) -> list[float]:  # ipolyline2d_points.html
        """
        ## Points - Массив SAFEARRAY точек полилинии

        Тип данных: VARIANT типа VT_ARRAY | VT_R8

        Свойство позволяет устанавливать и получать массив координат точек полилинии.

        ### Синтаксис Automation

        ```
        Points = Object.Points       # Получить свойство(* )
        Object.Points = Points       # Установить свойство (* )
        Points = Object.GetPoints()  # Получить свойство (**)
        Object.SetPoints( Points )   # Установить свойство (**)
        ```
        """
        ...
    @Points.setter
    def Points(self, value: list[float]): ...

    @property
    def PointsCount(self) -> int:  # ipolyline2d_pointscount.html
        """
        ## PointsCount - Количество точек

        Тип данных: long

        1. Свойство позволяет получить количество точек полилинии.

        2. Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        PointsCount = Object.PointsCount       # Получить свойство(* )
        PointsCount = Object.GetPointsCount()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def Style(self) -> int:  # ipolyline2d_style.html
        """
        ## Style - Cтиль линии

        Тип данных: long

        Свойство позволяет устанавливать и получать стиль линии.

        ### Синтаксис Automation

        ```
        Style = Object.Style       # Получить свойство(* )
        Object.Style = Style       # Установить свойство (* )
        Style = Object.GetStyle()  # Получить свойство (**)
        Object.SetStyle( Style )   # Установить свойство (**)
        ```
        """
        ...
    @Style.setter
    def Style(self, value: int): ...

    def AddPoint(self, Index, X, Y) -> bool:  # ipolyline2d_addpoint.html
        """
        ## AddPoint - Добавить точку

        Метод позволяет добавить точку в полилинию.

        ### Синтаксис Automation

        ```
        BOOL AddPoint( long Index,
        double X,
        double Y );
        ```

        ### Входные параметры

        +-----------------------------------------+
        | Index | - индекс добавляемой точки,     |
        +-----------------------------------------+
        | X, Y  | - координаты добавляемой точки. |
        +-----------------------------------------+

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def Clear(self) -> bool:  # ipolyline2d_clear.html
        """
        ## Clear - Удалить все точки

        Метод позволяет удалить все точки полилинии.

        ### Синтаксис Automation

        ```
        BOOL Clear();
        ```

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def DeletePoint(self, Index) -> bool:  # ipolyline2d_deletepoint.html
        """
        ## DeletePoint - Удалить точку

        Метод позволяет удалить заданную точку полилинии.

        ### Синтаксис Automation

        ```
        BOOL DeletePoint( long Index );
        ```

        ### Входные параметры

        +-----------------------------------+
        | Index | - индекс удаляемой точки. |
        +-----------------------------------+

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def GetPoint(self, Index) -> bool:  # ipolyline2d_getpoint.html
        """
        ## GetPoint - Получить параметры точки

        Метод позволяет получить параметры точки полилинии, заданной по индексу.

        ### Синтаксис Automation

        ```
        BOOL GetPoint( long Index,
        double * X,
        double * Y );
        ```

        ### Входные параметры

        +-------------------------+
        | Index | - индекс точки. |
        +-------------------------+

        ### Выходные параметры

        +----------------------------+
        | X, Y | - координаты точки. |
        +----------------------------+

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def ReadFromFile(self, FileName) -> bool:  # ipolyline2d_readfromfile.html
        """
        ## ReadFromFile - Прочитать файл с данными

        ### Синтаксис Automation

        ```
        BOOL ReadFromFile( LPCTSTR FileName );
        ```

        ### Входные параметры

        +------------------------------------------+
        | FileName | - полное имя файла с данными. |
        +------------------------------------------+

        ### Возвращаемое значение

        +--------------------------+
        | TRUE | - в случае удачи. |
        +--------------------------+
        """
        ...

    def WriteToFile(self, FileName) -> bool:  # ipolyline2d_writetofile.html
        """
        ## WriteToFile - Записать файл с данными

        ### Синтаксис Automation

        ```
        BOOL WriteToFile( LPCTSTR FileName );
        ```

        ### Входные параметры

        +------------------------------------------+
        | FileName | - полное имя файла с данными. |
        +------------------------------------------+

        ### Возвращаемое значение

        +--------------------------+
        | TRUE | - в случае удачи. |
        +--------------------------+
        """
        ...




class IRectangle(IDrawingObject):  # irectangle.html
    """
    ## Интерфейс IRectangle

    Интерфейс прямоугольника.

    Интерфейс позволяет задавать параметры прямоугольника.

    1. Интерфейс можно получить у коллекции прямоугольников, используя свойство IRectangles::Rectangle или метод IRectangles::Add.

    2. После задания параметров прямоугольника требуется вызвать метод IDrawingObject::Update.
    """

    @property
    def Angle(self) -> float:  # irectangle_angle.html
        """
        ## Angle - Угол наклона стороны прямоугольника, выходящей из базовой точки

        Тип данных: double

        Свойство позволяет устанавливать и получать угол наклона прямоугольника.

        ### Синтаксис Automation

        ```
        Angle = Object.Angle       # Получить свойство(* )
        Object.Angle = Angle       # Установить свойство (* )
        Angle = Object.GetAngle()  # Получить свойство (**)
        Object.SetAngle( Angle )   # Установить свойство (**)
        ```
        """
        ...
    @Angle.setter
    def Angle(self, value: float): ...

    @property
    def ContourSegmentsPoints(self) -> typing.Any:  # irectangle_contoursegmentspoints.html
        """
        ## ContourSegmentsPoints - Координаты точек сегментов контура многоугольника

        Тип данных: VARIANT

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        ContourSegmentsPoints = Object.ContourSegmentsPoints       # Получить свойство(* )
        ContourSegmentsPoints = Object.GetContourSegmentsPoints()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def Height(self) -> float:  # irectangle_height.html
        """
        ## Height - Высота прямоугольника

        Тип данных: double

        Свойство позволяет устанавливать и получать высоту прямоугольника.

        ### Синтаксис Automation

        ```
        Height = Object.Height       # Получить свойство(* )
        Object.Height = Height       # Установить свойство (* )
        Height = Object.GetHeight()  # Получить свойство (**)
        Object.SetHeight( Height )   # Установить свойство (**)
        ```
        """
        ...
    @Height.setter
    def Height(self, value: float): ...

    @property
    def Style(self) -> int:  # irectangle_style.html
        """
        ## Style - Cтиль линии

        Тип данных: long

        Свойство позволяет устанавливать и получать стиль линии прямоугольника.

        ### Синтаксис Automation

        ```
        Style = Object.Style       # Получить свойство(* )
        Object.Style = Style       # Установить свойство (* )
        Style = Object.GetStyle()  # Получить свойство (**)
        Object.SetStyle( Style )   # Установить свойство (**)
        ```
        """
        ...
    @Style.setter
    def Style(self, value: int): ...

    @property
    def Width(self) -> float:  # irectangle_width.html
        """
        ## Width - Ширина прямоугольника

        Тип данных: double

        Свойство позволяет устанавливать и получать ширину прямоугольника.

        ### Синтаксис Automation

        ```
        Width = Object.Width       # Получить свойство(* )
        Object.Width = Width       # Установить свойство (* )
        Width = Object.GetWidth()  # Получить свойство (**)
        Object.SetWidth( Width )   # Установить свойство (**)
        ```
        """
        ...
    @Width.setter
    def Width(self, value: float): ...

    @property
    def X(self) -> float:  # irectangle_x.html
        """
        ## X - Координата базовой точки по оси X

        Тип данных: double

        Свойство позволяет устанавливать и получать координату базовой точки прямоугольника по оси X.

        ### Синтаксис Automation

        ```
        X = Object.X       # Получить свойство(* )
        Object.X = X       # Установить свойство (* )
        X = Object.GetX()  # Получить свойство (**)
        Object.SetX( X )   # Установить свойство (**)
        ```
        """
        ...
    @X.setter
    def X(self, value: float): ...

    @property
    def Y(self) -> float:  # irectangle_y.html
        """
        ## Y - Координата базовой точки по оси Y

        Тип данных: double

        Свойство позволяет устанавливать и получать координату базовой точки прямоугольника по оси Y.

        ### Синтаксис Automation

        ```
        Y = Object.Y       # Получить свойство(* )
        Object.Y = Y       # Установить свойство (* )
        Y = Object.GetY()  # Получить свойство (**)
        Object.SetY( Y )   # Установить свойство (**)
        ```
        """
        ...
    @Y.setter
    def Y(self, value: float): ...

    def GetCorner(self, Index) -> bool:  # irectangle_getcorner.html
        """
        ## GetCorner - Получить параметры скругления/усечения угла

        Метод позволяет получить параметры скругления/усечения угла прямоугольника.

        ### Синтаксис Automation

        ```
        BOOL GetCorner( long Index,
        long * Type,
        double * L1,
        double * L2 );
        ```

        ### Входные параметры

        +------------------------+
        | Index | - индекс угла. |
        +------------------------+

        ### Выходные параметры

        +--------------------------------------------------------------+
        | Type | - тип угла из перечисления ksCornerTypeEnum,          |
        +--------------------------------------------------------------+
        | L1   | - длина фаски первого сегмента или радиус скругления, |
        +--------------------------------------------------------------+
        | L2   | - длина фаски второго сегмента.                       |
        +--------------------------------------------------------------+

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def SetCorner(self, Index, Type, L1, L2) -> bool:  # irectangle_setcorner.html
        """
        ## SetCorner - Установить параметры скругления/усечения угла

        Метод позволяет установить параметры скругления/усечения угла прямоугольника.

        ### Синтаксис Automation

        ```
        BOOL SetCorner( long Index,
        long Type,
        double L1,
        double L2 );
        ```

        ### Входные параметры

        +---------------------------------------------------------------+
        | Index | - индекс угла,                                        |
        +---------------------------------------------------------------+
        | Type  | - тип угла из перечисления ksCornerTypeEnum,          |
        +---------------------------------------------------------------+
        | L1    | - длина фаски первого сегмента или радиус скругления, |
        +---------------------------------------------------------------+
        | L2    | - длина фаски второго сегмента.                       |
        +---------------------------------------------------------------+

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...




class IRegularPolygon(IDrawingObject):  # iregularpolygon.html
    """
    ## Интерфейс IRegularPolygon

    Интерфейс многоугольника.

    Интерфейс позволяет задавать параметры многоугольника.

    1. Интерфейс можно получить у коллекции многоугольников, используя свойство IRegularPolygons::RegularPolygon или метод IRegularPolygons::Add.

    2. После задания параметров многоугольника требуется вызвать метод IDrawingObject::Update.
    """

    @property
    def Angle(self) -> float:  # iregularpolygon_angle.html
        """
        ## Angle - Угол радиус-вектора, направленного от центра к первой вершине

        Тип данных: double

        Свойство позволяет устанавливать и получать угол радиус-вектора, направленного от центра к первой вершине.

        ### Синтаксис Automation

        ```
        Angle = Object.Angle       # Получить свойство(* )
        Object.Angle = Angle       # Установить свойство (* )
        Angle = Object.GetAngle()  # Получить свойство (**)
        Object.SetAngle( Angle )   # Установить свойство (**)
        ```
        """
        ...
    @Angle.setter
    def Angle(self, value: float): ...

    @property
    def ContourSegmentsPoints(self):
        ...

    @property
    def Count(self) -> int:  # iregularpolygon_count.html
        """
        ## Count - Количество вершин многоугольника

        Тип данных: long

        1. Свойство позволяет получить количество вершин многоугольника.

        2. Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        Count = Object.Count       # Получить свойство(* )
        Count = Object.GetCount()  # Получить свойство (**)
        ```
        """
        ...
    @Count.setter
    def Count(self, value: int): ...

    @property
    def Describe(self) -> bool:  # iregularpolygon_describe.html
        """
        ## Describe - Признак построения по описанной или вписанной окружности

        Тип данных: BOOL

        Свойство позволяет устанавливать и получать признак построения по описанной или вписанной окружности.

        ### Синтаксис Automation

        ```
        Describe = Object.Describe       # Получить свойство(* )
        Object.Describe = Describe       # Установить свойство (* )
        Describe = Object.GetDescribe()  # Получить свойство (**)
        Object.SetDescribe( Describe )   # Установить свойство (**)
        ```
        """
        ...
    @Describe.setter
    def Describe(self, value: bool): ...

    @property
    def Radius(self) -> float:  # iregularpolygon_radius.html
        """
        ## Radius - Радиус вписанной или описанной окружности

        Тип данных: double

        Свойство позволяет устанавливать и получать радиус вписанной или описанной окружности.

        ### Синтаксис Automation

        ```
        Radius = Object.Radius       # Получить свойство(* )
        Object.Radius = Radius       # Установить свойство (* )
        Radius = Object.GetRadius()  # Получить свойство (**)
        Object.SetRadius( Radius )   # Установить свойство (**)
        ```
        """
        ...
    @Radius.setter
    def Radius(self, value: float): ...

    @property
    def Style(self) -> int:  # iregularpolygon_style.html
        """
        ## Style - Cтиль линии

        Тип данных: long

        Свойство позволяет устанавливать и получать стиль линии многоугольника.

        ### Синтаксис Automation

        ```
        Style = Object.Style       # Получить свойство(* )
        Object.Style = Style       # Установить свойство (* )
        Style = Object.GetStyle()  # Получить свойство (**)
        Object.SetStyle( Style )   # Установить свойство (**)
        ```
        """
        ...
    @Style.setter
    def Style(self, value: int): ...

    @property
    def Xc(self) -> float:  # iregularpolygon_xc.html
        """
        ## Xc - Координата точки центра вписанной или описанной окружности по оси X

        Тип данных: double

        Свойство позволяет устанавливать и получать координату точки центра вписанной или описанной окружности по оси X.

        ### Синтаксис Automation

        ```
        Xc = Object.Xc       # Получить свойство(* )
        Object.Xc = Xc       # Установить свойство (* )
        Xc = Object.GetXc()  # Получить свойство (**)
        Object.SetXc( Xc )   # Установить свойство (**)
        ```
        """
        ...
    @Xc.setter
    def Xc(self, value: float): ...

    @property
    def Yc(self) -> float:  # iregularpolygon_yc.html
        """
        ## Yc - Координата точки центра вписанной или описанной окружности по оси Y

        Тип данных: double

        Свойство позволяет устанавливать и получать координату точки центра вписанной или описанной окружности по оси Y.

        ### Синтаксис Automation

        ```
        Yc = Object.Yc       # Получить свойство(* )
        Object.Yc = Yc       # Установить свойство (* )
        Yc = Object.GetYc()  # Получить свойство (**)
        Object.SetYc( Yc )   # Установить свойство (**)
        ```
        """
        ...
    @Yc.setter
    def Yc(self, value: float): ...

    def GetCorner(self, Index) -> bool:  # iregularpolygon_getcorner.html
        """
        ## GetCorner - Получить параметры скругления/усечения угла

        Метод позволяет получить параметры скругления/усечения угла многоугольника.

        ### Синтаксис Automation

        ```
        BOOL GetCorner( long Index,
        long * Type,
        double * L1,
        double * L2 );
        ```

        ### Входные параметры

        +------------------------+
        | Index | - индекс угла. |
        +------------------------+

        ### Выходные параметры

        +--------------------------------------------------------------+
        | Type | - тип угла из перечисления ksCornerTypeEnum,          |
        +--------------------------------------------------------------+
        | L1   | - длина фаски первого сегмента или радиус скругления, |
        +--------------------------------------------------------------+
        | L2   | - длина фаски второго сегмента.                       |
        +--------------------------------------------------------------+

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def SetCorner(self, Index, Type, L1, L2) -> bool:  # iregularpolygon_setcorner.html
        """
        ## SetCorner - Установить параметры скругления/усечения угла

        Метод позволяет установить параметры скругления/усечения угла многоугольника.

        ### Синтаксис Automation

        ```
        BOOL SetCorner( long Index,
        long * Type,
        double * L1,
        double * L2 );
        ```

        ### Входные параметры

        +---------------------------------------------------------------+
        | Index | - индекс угла,                                        |
        +---------------------------------------------------------------+
        | Type  | - тип угла из перечисления ksCornerTypeEnum,          |
        +---------------------------------------------------------------+
        | L1    | - длина фаски первого сегмента или радиус скругления, |
        +---------------------------------------------------------------+
        | L2    | - длина фаски второго сегмента.                       |
        +---------------------------------------------------------------+

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...




class IRaster(IDrawingObject):  # iraster.html
    """
    ## Интерфейс IRaster

    Интерфейс растрового объекта.

    Интерфейс позволяет задавать параметры растрового объекта.

    1. Интерфейс можно получить у коллекции растровых объектов, используя свойство IRasters::Raster или метод IRasters::Add.

    2. После задания параметров растрового объекта требуется вызвать метод IDrawingObject::Update.

    3. Посредством вызова метода IUnknown::QueryInterface (const GUID far& iid, void** pif) у данного интерфейса можно получить дополнительный интерфейс IBoundariesObject.

    4. Для задания границы обрезки растра нужно использовать IBoundariesObject::AddBoundaries. В качестве границы обрезки может быть задан только один замкнутый контур.
    """

    @property
    def DisplayModePartial(self) -> bool:  # iraster_displaymodepartial.html
        """
        ## DisplayModePartial - Режим отрисовки

        Тип данных: BOOL

        Значения свойства:

        +---------------------------------+
        | TRUE  | - показывать полностью, |
        +---------------------------------+
        | FALSE | - показывать часть.     |
        +---------------------------------+

        Свойство позволяет устанавливать и получать режим отрисовки растрового объекта.

        ### Синтаксис Automation

        ```
        DisplayModePartial = Object.DisplayModePartial       # Получить свойство(* )
        Object.DisplayModePartial = DisplayModePartial       # Установить свойство (* )
        DisplayModePartial = Object.GetDisplayModePartial()  # Получить свойство (**)
        Object.SetDisplayModePartial( DisplayModePartial )   # Установить свойство (**)
        ```
        """
        ...
    @DisplayModePartial.setter
    def DisplayModePartial(self, value: bool): ...

    @property
    def FileName(self) -> str:  # iraster_filename.html
        """
        ## FileName - Имя файла растрового объекта

        Тип данных: BSTR

        Свойство позволяет устанавливать и получать имя файла растрового объекта.

        ### Синтаксис Automation

        ```
        FileName = Object.FileName       # Получить свойство(* )
        Object.FileName = FileName       # Установить свойство (* )
        FileName = Object.GetFileName()  # Получить свойство (**)
        Object.SetFileName( FileName )   # Установить свойство (**)
        ```
        """
        ...
    @FileName.setter
    def FileName(self, value: str): ...

    @property
    def Height(self) -> float:  # iraster_height.html
        """
        ## Height - Высота вставки

        Тип данных: double

        Свойство позволяет устанавливать и получать высоту вставки растрового объекта.

        ### Синтаксис Automation

        ```
        Height = Object.Height       # Получить свойство(* )
        Object.Height = Height       # Установить свойство (* )
        Height = Object.GetHeight()  # Получить свойство (**)
        Object.SetHeight( Height )   # Установить свойство (**)
        ```
        """
        ...
    @Height.setter
    def Height(self, value: float): ...

    @property
    def InsertionType(self) -> bool:  # iraster_insertiontype.html
        """
        ## InsertionType - Способ вставки растрового объекта

        Тип данных: BOOL

        Значения свойства:

        +------------------------------------+
        | TRUE  | - внедрить в документ,(* ) |
        +------------------------------------+
        | FALSE | - вставлять ссылкой.       |
        +------------------------------------+

        Свойство позволяет устанавливать и получать способ вставки растрового объекта.

        ### Синтаксис Automation

        ```
        InsertionType = Object.InsertionType       # Получить свойство(* )
        Object.InsertionType = InsertionType       # Установить свойство (* )
        InsertionType = Object.GetInsertionType()  # Получить свойство (**)
        Object.SetInsertionType( InsertionType )   # Установить свойство (**)
        ```
        """
        ...
    @InsertionType.setter
    def InsertionType(self, value: bool): ...

    @property
    def IsCutBoundarySet(self) -> bool:  # iraster_iscutboundaryset.html
        """
        ## IsCutBoundarySet - Признак задания границы

        Тип данных: BOOL

        1. Свойство позволяет получать признак задания границы растрового объекта.

        2. Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        IsCutBoundarySet = Object.IsCutBoundarySet       # Получить свойство(* )
        IsCutBoundarySet = Object.GetIsCutBoundarySet()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def IsEnableChangeResolution(self) -> bool:  # iraster_isenablechangeresolution.html
        """
        ## IsEnableChangeResolution - Доступность изменения разрешения

        Тип данных: BOOL

        1. Свойство позволяет получать признак доступности изменения разрешения растрового объекта.

        2. Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        IsEnableChangeResolution = Object.IsEnableChangeResolution       # Получить свойство(* )
        IsEnableChangeResolution = Object.GetIsEnableChangeResolution()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def Palette(self) -> int:  # iraster_palette.html
        """
        ## Palette - Цветовая палитра (Количество разрядов)

        Тип данных: long

        1. Свойство позволяет получать количество разрядов цветовой палитры растрового объекта.

        2. Свойство доступно только для чтения

        ### Синтаксис Automation

        ```
        Palette = Object.Palette       # Получить свойство(* )
        Palette = Object.GetPalette()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def Resolution(self) -> float:  # iraster_resolution.html
        """
        ## Resolution - Разрешение растра

        Тип данных: double

        Свойство позволяет устанавливать и получать разрешение растра.

        ### Синтаксис Automation

        ```
        Resolution = Object.Resolution       # Получить свойство(* )
        Object.Resolution = Resolution       # Установить свойство (* )
        Resolution = Object.GetResolution()  # Получить свойство (**)
        Object.SetResolution( Resolution )   # Установить свойство (**)
        ```
        """
        ...
    @Resolution.setter
    def Resolution(self, value: float): ...

    @property
    def Scale(self) -> float:  # iraster_scale.html
        """
        ## Scale - Масштаб

        Тип данных: double

        Свойство позволяет устанавливать и получать масштаб растрового объекта.

        ### Синтаксис Automation

        ```
        Scale = Object.Scale       # Получить свойство(* )
        Object.Scale = Scale       # Установить свойство (* )
        Scale = Object.GetScale()  # Получить свойство (**)
        Object.SetScale( Scale )   # Установить свойство (**)
        ```
        """
        ...
    @Scale.setter
    def Scale(self, value: float): ...

    @property
    def SourceHeight(self) -> float:  # iraster_sourceheight.html
        """
        ## SourceHeight - Высота источника

        Тип данных: double

        1. Свойство позволяет получать значение высоты источника.

        2. Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        SourceHeight = Object.SourceHeight       # Получить свойство(* )
        SourceHeight = Object.GetSourceHeight()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def SourceWidth(self) -> float:  # iraster_sourcewidth.html
        """
        ## SourceWidth - Ширина источника

        Тип данных: double

        1. Свойство позволяет получать значение ширины источника.

        2. Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        SourceWidth = Object.SourceWidth       # Получить свойство(* )
        SourceWidth = Object.GetSourceWidth()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def Width(self) -> float:  # iraster_width.html
        """
        ## Width - Ширина вставки

        Тип данных: double

        Свойство позволяет устанавливать и получать ширину вставки растрового объекта.

        ### Синтаксис Automation

        ```
        Width = Object.Width       # Получить свойство(* )
        Object.Width = Width       # Установить свойство (* )
        Width = Object.GetWidth()  # Получить свойство (**)
        Object.SetWidth( Width )   # Установить свойство (**)
        ```
        """
        ...
    @Width.setter
    def Width(self, value: float): ...

    def GetPlacement(self) -> bool:  # iraster_getplacement.html
        """
        ## GetPlacement - Получить местоположение растрового объекта

        1. Метод позволяет получить точку привязки и угол поворота растрового объекта.

        2. После применения к растровому объекту операции Симметрия его признак зеркальной симметрии автоматически инвертируется.

        ### Синтаксис Automation

        ```
        BOOL GetPlacement( double * X,
        double * Y,
        double * Angle,
        BOOL * MirrorSymmetry );
        ```

        ### Входные параметры

        +----------------------------------------------------------+
        | X, Y           | - координаты точки привязки,            |
        +----------------------------------------------------------+
        | Angle          | - угол поворота объекта,                |
        +----------------------------------------------------------+
        | MirrorSymmetry | - признак зеркальной симметрии объекта. |
        +----------------------------------------------------------+

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def SetPlacement(self, X, Y, Angle, MirrorSymmetry) -> bool:  # iraster_setplacement.html
        """
        ## SetPlacement - Установить местоположение растрового объекта

        1. Метод позволяет установить точку привязки и угол поворота растрового объекта.

        2. После применения к растровому объекту операции Симметрия его признак зеркальной симметрии автоматически инвертируется.

        ### Синтаксис Automation

        ```
        BOOL SetPlacement( double X,
        double Y,
        double Angle,
        BOOL * MirrorSymmetry );
        ```

        ### Входные параметры

        +----------------------------------------------------------+
        | X, Y           | - координаты точки привязки,            |
        +----------------------------------------------------------+
        | Angle          | - угол поворота объекта,                |
        +----------------------------------------------------------+
        | MirrorSymmetry | - признак зеркальной симметрии объекта. |
        +----------------------------------------------------------+

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...




class IContour(IKompasAPIObject):  # icontour.html
    """
    ## Интерфейс IContour

    Интерфейс контура.

    Позволяет создавать и редактировать контур.

    1. Интерфейс можно получить у интерфейса мультилинии с помощью свойства IMultiline::BaseContour.

    2. Интерфейс IContour является дополнительным для интерфейса IDrawingContour. Его можно получить с помощью метода IUnknown::QueryInterface.

    3. После задания параметров объекта требуется вызвать метод IDrawingObject::Update у мультилинии.
    """

    @property
    def Closed(self) -> bool:  # icontour_closed.html
        """
        ## Closed - Замкнутость

        Тип данных: BOOL

        Значения свойства:

        +-----------------------------+
        | TRUE  | - контур замкнут,   |
        +-----------------------------+
        | FALSE | - контур разомкнут. |
        +-----------------------------+

        1. Свойство позволяет устанавливать и получать замкнутость мультилинии.

        2. Если мультилиния замкнута, то контур тоже замкнут.

        ### Синтаксис Automation

        ```
        Closed = iObject.Closed;       # Получить свойство(* )
        iObject.Closed = Closed;       # Установить свойство (* )
        Closed = iObject.GetClosed();  # Получить свойство (**)
        iObject.SetClosed(Closed);     # Установить свойство (**)
        ```
        """
        ...
    @Closed.setter
    def Closed(self, value: bool): ...

    @property
    def Count(self) -> int:  # icontour_count.html
        """
        ## Count - Количество сегментов

        Тип данных: long

        1. Свойство позволяет получить количество сегментов контура.

        2. Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        Count = iObject.Count();     # Получить свойство(* )
        Count = iObject.GetCount();  # Получить свойство (**)
        ```
        """
        ...

    @property
    def TmpObjects(self) -> typing.Any:  # icontour_tmpobjects.html
        """
        ## TmpObjects - Временные объекты из сегментов в виде SAFEARRAY'я DISPATCH - VT_ARRAY | VT_DISPATCH

        Тип данных: VARIANT

        Значения свойства:

        Массив SafeArray типа VT_ARRAY | VT_DISPATCH.

        1. Свойство позволяет получить массив SAFEARRAY временных объектов, входящих в контур.

        2. Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        TmpObjects = iObject.TmpObjects;       # Получить свойство(* )
        TmpObjects = iObject.GetTmpObjects();  # Получить свойство (**)
        ```
        """
        ...

    def AddSegment(self, Type) -> IContourSegment:  # icontour_addsegment.html
        """
        ## AddSegment - Добавить сегмент

        Метод позволяет добавить сегмент в контур.

        ### Синтаксис Automation

        ```
        LPDISPATCH Add( long type );
        ```

        ### Входные параметры

        +------------------------+
        | type | - тип сегмента. |
        +------------------------+

        ### Возвращаемое значение

        - указатель на интерфейс сегмента контура IContourSegment.
        """
        ...

    def Clear(self) -> bool:  # icontour_clear.html
        """
        ## Clear - Очистить контур

        Метод позволяет удалить все сегменты контура.

        ### Синтаксис Automation

        ```
        BOOL Clear();
        ```

        ### Возвращаемое значение

        +-----------------------------------------+
        | TRUE  | - в случае удачного завершения, |
        +-----------------------------------------+
        | FALSE | - в случае неудачи.             |
        +-----------------------------------------+
        """
        ...

    def CopyCurve(self, Curve, DeleteSource) -> bool:  # icontour_copycurve.html
        """
        ## CopyCurve - Добавить сегмент

        После добавления сегментов в контур требуется вызов метода IDrawingObject::Update, в котором эти сегменты будут добавлены в 2D кривую. (При условии совпадения точек сегментов с точностью до 1e-3).

        ### Синтаксис Automation

        ```
        BOOL CopyCurve( LPDISPATCH Curve,
        BOOL DeleteSource );
        ```

        ### Входные параметры

        +--------------------------------------------------+
        | Curve        | - объект для добавления в контур, |
        +--------------------------------------------------+
        | DeleteSource | - удалить исходный объект.        |
        +--------------------------------------------------+

        ### Возвращаемое значение

        +-----------------------------------------+
        | TRUE  | - в случае удачного завершения, |
        +-----------------------------------------+
        | FALSE | - в случае неудачи.             |
        +-----------------------------------------+
        """
        ...

    def CopySegments(self, Val, DeleteSource) -> bool:  # icontour_copysegments.html
        """
        ## CopySegments - Объекты вида для добавления в контур в виде SAFEARRAY'я DISPATCH - VT_ARRAY | VT_DISPATCH

        ### Синтаксис Automation

        ```
        BOOL CopySegments( VARIANT Val,
        BOOL DeleteSource );
        ```

        ### Входные параметры

        +-----------------------------------------------------------------------------------------------------------------------------------------+
        | Val          | - массив SAFEARRAY типа VT_ARRAY | VT_DISPATCH, содержащий объекты вида (отрезки, дуги и т. п.) для добавления в контур, |
        +-----------------------------------------------------------------------------------------------------------------------------------------+
        | DeleteSource | - удалить исходные объекты.                                                                                              |
        +-----------------------------------------------------------------------------------------------------------------------------------------+

        ### Возвращаемое значение

        +-----------------------------------------+
        | TRUE  | - в случае удачного завершения, |
        +-----------------------------------------+
        | FALSE | - в случае неудачи.             |
        +-----------------------------------------+
        """
        ...

    def RemoveContourGaps(self, Accuracy, CanInsert, CanReplace) -> bool:  # icontour_removecontourgaps.html
        """
        ## RemoveContourGaps - Устранить разрывы в контуре

        ### Синтаксис Automation

        ```
        BOOL RemoveContourGaps( double Accuracy, BOOL CanInsert, BOOL CanReplace );
        ```

        ### Входные параметры

        +-------------------------------------------------+
        | Accuracy   | - размер разрывов,                 |
        +-------------------------------------------------+
        | CanInsert  | - разрешение на вставку сегментов, |
        +-------------------------------------------------+
        | CanReplace | - разрешение на подмену сегментов. |
        +-------------------------------------------------+

        ### Возвращаемое значение

        +----------------------------------------+
        | TRUE | - в случае удачного завершения. |
        +----------------------------------------+
        """
        ...

    def Segment(self, Index) -> IKompasAPIObject:  # icontour_segment.html
        """
        ## Segment - Получить сегмент

        Тип данных: указатель на интерфейс IKompasAPIObject

        1. Свойство позволяет получить интерфейс сегмента контура IContourSegment.

        2. Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        Segment = iObject.Segment(Index);     # Получить свойство(* )
        Segment = iObject.GetSegment(Index);  # Получить свойство (**)
        ```
        """
        ...




class IContourSegment(IKompasAPIObject):  # icontoursegment.html
    """
    ## Интерфейс IContourSegment

    Интерфейс сегмента контура.

    Интерфейс позволяет редактировать сегмент контура.

    1. Интерфейс можно получить у интерфейса контура с помощью свойства IContour::Segment или метода IContour::AddSegment.

    2. Имеет следующие дополнительные интерфейсы, которые можно получить с помощью метода IUnknown::QueryInterface:

    •IContourLineSegment,

    •IContourArc.
    """

    @property
    def Curve2D(self) -> ICurve2D:  # icontoursegment_curve2d.html
        """
        ## Curve2D - Математическая кривая

        Тип данных: указатель на интерфейс математической кривой ICurve2D

        1. Свойство позволяет получить математическую кривую.

        2. Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        Curve2D = iObject.Curve2D();     # Получить свойство(* )
        Curve2D = iObject.GetCurve2D();  # Получить свойство (**)
        ```
        """
        ...

    @property
    def SegmentType(self) -> int:  # icontoursegment_segmenttype.html
        """
        ## SegmentType - Тип сегмента

        Тип данных: из перечисления ksContourSegmentEnum

        1. Свойство позволяет получить тип сегмента контура.

        2. Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        SegmentType = iObject.SegmentType;     # Получить свойство(* )
        SegmentType = iObject.GetSegmentType;  # Получить свойство (**)
        ```
        """
        ...




class IContourArc(IContourSegment):  # icontourarc.html
    """
    ## Интерфейс IContourArc

    Интерфейс сегмента контура - дуги.

    Интерфейс позволяет создавать и редактировать сегмент контура – дугу.

    Интерфейс можно получить следующими способами:

    •у интерфейса контура с помощью свойства IContour::Segment или метода IContour::AddSegment.

    •у интерфейса IContourSegment с помощью метода IUnknown::QueryInterface.
    """

    @property
    def Angle1(self) -> float:  # icontourarc_angle1.html
        """
        ## Angle1 - Угол первой точки

        Тип данных: double

        Свойство позволяет устанавливать и получать угол первой точки дуги.

        ### Синтаксис Automation

        ```
        Angle1 = iObject.Angle1();     # Получить свойство(* )
        iObject.Angle1() = Angle1;     # Установить свойство (* )
        Angle1 = iObject.GetAngle1();  # Получить свойство (**)
        iObject.SetAngle1( Angle1 );   # Установить свойство (**)
        ```
        """
        ...
    @Angle1.setter
    def Angle1(self, value: float): ...

    @property
    def Angle2(self) -> float:  # icontourarc_angle2.html
        """
        ## Angle2 - Угол второй точки

        Тип данных: double

        Свойство позволяет устанавливать и получать угол второй точки дуги.

        ### Синтаксис Automation

        ```
        Angle2 = iObject.Angle2();     # Получить свойство(* )
        iObject.Angle2() = Angle2;     # Установить свойство (* )
        Angle2 = iObject.GetAngle2();  # Получить свойство (**)
        iObject.SetAngle2( Angle2 );   # Установить свойство (**)
        ```
        """
        ...
    @Angle2.setter
    def Angle2(self, value: float): ...

    @property
    def Direction(self) -> bool:  # icontourarc_direction.html
        """
        ## Direction - Направление

        Тип данных: BOOL

        Значения свойства:

        +-----------------------------------+
        | TRUE  | - по часовой стрелке,     |
        +-----------------------------------+
        | FALSE | - против часовой стрелки. |
        +-----------------------------------+

        Свойство позволяет устанавливать и получать направление дуги.

        ### Синтаксис Automation

        ```
        Direction = iObject.Direction();     # Получить свойство(* )
        iObject.Direction() = Direction;     # Установить свойство (* )
        Direction = iObject.GetDirection();  # Получить свойство (**)
        iObject.SetDirection( Direction );   # Установить свойство (**)
        ```
        """
        ...
    @Direction.setter
    def Direction(self, value: bool): ...

    @property
    def Radius(self) -> float:  # icontourarc_radius.html
        """
        ## Radius - Радиус дуги

        Тип данных: double

        Свойство позволяет устанавливать и получать радиус дуги.

        ### Синтаксис Automation

        ```
        Radius = iObject.Radius();     # Получить свойство(* )
        iObject.Radius() = Radius;     # Установить свойство (* )
        Radius = iObject.GetRadius();  # Получить свойство (**)
        iObject.SetRadius( Radius );   # Установить свойство (**)
        ```
        """
        ...
    @Radius.setter
    def Radius(self, value: float): ...

    @property
    def X1(self) -> float:  # icontourarc_x1.html
        """
        ## X1 - Координата первой точки по оси X

        Тип данных: double

        Свойство позволяет устанавливать и получать координату первой точки дуги по оси Х.

        ### Синтаксис Automation

        ```
        X1 = iObject.X1();     # Получить свойство(* )
        iObject.X1() = X1;     # Установить свойство (* )
        X1 = iObject.GetX1();  # Получить свойство (**)
        iObject.SetX1( X1 );   # Установить свойство (**)
        ```
        """
        ...
    @X1.setter
    def X1(self, value: float): ...

    @property
    def X2(self) -> float:  # icontourarc_x2.html
        """
        ## X2 - Координата второй точки по оси X

        Тип данных: double

        Свойство позволяет устанавливать и получать координату второй точки дуги по оси Х.

        ### Синтаксис Automation

        ```
        X2 = iObject.X2();     # Получить свойство(* )
        iObject.X2() = X2;     # Установить свойство (* )
        X2 = iObject.GetX2();  # Получить свойство (**)
        iObject.SetX2( X2 );   # Установить свойство (**)
        ```
        """
        ...
    @X2.setter
    def X2(self, value: float): ...

    @property
    def X3(self) -> float:  # icontourarc_x3.html
        """
        ## X3 - Координата третьей точки по оси X

        Тип данных: double

        Свойство позволяет устанавливать и получать координату третьей точки дуги по оси Х.

        ### Синтаксис Automation

        ```
        X3 = iObject.X3();     # Получить свойство(* )
        iObject.X3() = X3;     # Установить свойство (* )
        X3 = iObject.GetX3();  # Получить свойство (**)
        iObject.SetX3( X3 );   # Установить свойство (**)
        ```
        """
        ...
    @X3.setter
    def X3(self, value: float): ...

    @property
    def Xc(self) -> float:  # icontourarc_xc.html
        """
        ## Xc - Координата центра по оси X

        Тип данных: double

        Свойство позволяет устанавливать и получать координату центра дуги по оси Х.

        ### Синтаксис Automation

        ```
        Xc = iObject.Xc();     # Получить свойство(* )
        iObject.Xc() = Xc;     # Установить свойство (* )
        Xc = iObject.GetXc();  # Получить свойство (**)
        iObject.SetXc( Xc );   # Установить свойство (**)
        ```
        """
        ...
    @Xc.setter
    def Xc(self, value: float): ...

    @property
    def Y1(self) -> float:  # icontourarc_y1.html
        """
        ## Y1 - Координата первой точки по оси Y

        Тип данных: double

        Свойство позволяет устанавливать и получать координату первой точки дуги по оси Х.

        ### Синтаксис Automation

        ```
        Y1 = iObject.Y1();     # Получить свойство(* )
        iObject.Y1() = Y1;     # Установить свойство (* )
        Y1 = iObject.GetY1();  # Получить свойство (**)
        iObject.SetY1( Y1 );   # Установить свойство (**)
        ```
        """
        ...
    @Y1.setter
    def Y1(self, value: float): ...

    @property
    def Y2(self) -> float:  # icontourarc_y2.html
        """
        ## Y2 - Координата второй точки по оси Y

        Тип данных: double

        Свойство позволяет устанавливать и получать координату второй точки дуги по оси Х.

        ### Синтаксис Automation

        ```
        Y2 = iObject.Y2();     # Получить свойство(* )
        iObject.Y2() = Y2;     # Установить свойство (* )
        Y2 = iObject.GetY2();  # Получить свойство (**)
        iObject.SetY2( Y2 );   # Установить свойство (**)
        ```
        """
        ...
    @Y2.setter
    def Y2(self, value: float): ...

    @property
    def Y3(self) -> float:  # icontourarc_y3.html
        """
        ## Y3 - Координата третьей точки по оси Y

        Тип данных: double

        Свойство позволяет устанавливать и получать координату третьей точки дуги по оси Х.

        ### Синтаксис Automation

        ```
        Y3 = iObject.Y3();     # Получить свойство(* )
        iObject.Y3() = Y3;     # Установить свойство (* )
        Y3 = iObject.GetY3();  # Получить свойство (**)
        iObject.SetY3( Y3 );   # Установить свойство (**)
        ```
        """
        ...
    @Y3.setter
    def Y3(self, value: float): ...

    @property
    def Yc(self) -> float:  # icontourarc_yc.html
        """
        ## Yc - Координата центра по оси Y

        Тип данных: double

        Свойство позволяет устанавливать и получать координату центра дуги по оси Х.

        ### Синтаксис Automation

        ```
        Yc = iObject.Yc();     # Получить свойство(* )
        iObject.Yc() = Yc;     # Установить свойство (* )
        Yc = iObject.GetYc();  # Получить свойство (**)
        iObject.SetYc( Yc );   # Установить свойство (**)
        ```
        """
        ...
    @Yc.setter
    def Yc(self, value: float): ...




class IContourLineSegment(IContourSegment):  # icontourlinesegment.html
    """
    ## Интерфейс IContourLineSegment

    Интерфейс сегмента контура – отрезка.

    Интерфейс позволяет создавать и редактировать сегмент контура – отрезок.

    Интерфейс можно получить следующими способами:

    •у интерфейса контура с помощью свойства IContour::Segment или метода IContour::AddSegment.

    •у интерфейса IContourSegment с помощью метода IUnknown::QueryInterface.
    """

    @property
    def Angle(self) -> float:  # icontourlinesegment_angle.html
        """
        ## Angle - Угол

        Тип данных: double

        Свойство позволяет устанавливать и получать угол наклона отрезка.

        ### Синтаксис Automation

        ```
        Angle = iObject.Angle();     # Получить свойство(* )
        iObject.Angle() = Angle;     # Установить свойство (* )
        Angle = iObject.GetAngle();  # Получить свойство (**)
        iObject.SetAngle( Angle );   # Установить свойство (**)
        ```
        """
        ...
    @Angle.setter
    def Angle(self, value: float): ...

    @property
    def Length(self) -> float:  # icontourlinesegment_length.html
        """
        ## Length - Длина

        Тип данных: double

        Свойство позволяет устанавливать и получать длину отрезка.

        ### Синтаксис Automation

        ```
        Length = iObject.Length;       # Получить свойство(* )
        iObject.Length = Length;       # Установить свойство (* )
        Length = iObject.GetLength();  # Получить свойство (**)
        iObject.SetLength( Length );   # Установить свойство (**)
        ```
        """
        ...
    @Length.setter
    def Length(self, value: float): ...

    @property
    def X1(self) -> float:  # icontourlinesegment_x1.html
        """
        ## X1 - Координата первой точки по оси X

        Тип данных: double

        Свойство позволяет устанавливать и получать координату Х первой точки отрезка.

        ### Синтаксис Automation

        ```
        X1 = iObject.X1();     # Получить свойство(* )
        iObject.X1() = X1;     # Установить свойство (* )
        X1 = iObject.GetX1();  # Получить свойство (**)
        iObject.SetX1( X1 );   # Установить свойство (**)
        ```
        """
        ...
    @X1.setter
    def X1(self, value: float): ...

    @property
    def X2(self) -> float:  # icontourlinesegment_x2.html
        """
        ## X2 - Координата второй точки по оси X

        Тип данных: double

        Свойство позволяет устанавливать и получать координату Х второй точки отрезка.

        ### Синтаксис Automation

        ```
        X2 = iObject.X2();     # Получить свойство(* )
        iObject.X2() = X2;     # Установить свойство (* )
        X2 = iObject.GetX2();  # Получить свойство (**)
        iObject.SetX21( X2 );  # Установить свойство (**)
        ```
        """
        ...
    @X2.setter
    def X2(self, value: float): ...

    @property
    def Y1(self) -> float:  # icontourlinesegment_y1.html
        """
        ## Y1 - Координата первой точки по оси Y

        Тип данных: double

        Свойство позволяет устанавливать и получать координату Y первой точки отрезка.

        ### Синтаксис Automation

        ```
        Y1 = iObject.Y1();     # Получить свойство(* )
        iObject.Y1() = Y1;     # Установить свойство (* )
        Y1 = iObject.GetY1();  # Получить свойство (**)
        iObject.SetY1( Y1 );   # Установить свойство (**)
        ```
        """
        ...
    @Y1.setter
    def Y1(self, value: float): ...

    @property
    def Y2(self) -> float:  # icontourlinesegment_y2.html
        """
        ## Y2 - Координата второй точки по оси Y

        Тип данных: double

        Свойство позволяет устанавливать и получать координату Y второй точки отрезка.

        ### Синтаксис Automation

        ```
        Y2 = iObject.Y2();     # Получить свойство(* )
        iObject.Y2() = Y2;     # Установить свойство (* )
        Y2 = iObject.GetY2();  # Получить свойство (**)
        iObject.SetY2( Y2 );   # Установить свойство (**)
        ```
        """
        ...
    @Y2.setter
    def Y2(self, value: float): ...




class ICurve2D(IKompasAPIObject):  # icurve2d.html
    """
    ## Интерфейс ICurve2D

    Интерфейс математической кривой 2D.

    Позволяет создавать и редактировать математическую кривую 2D.

    Интерфейс можно получить у интерфейса сегмента контура с помощью свойства IContourSegment::Curve2D.
    """

    @property
    def BaseCurveType(self) -> int:  # icurve2d_basecurvetype.html
        """
        ## BaseCurveType - Тип базовой 2D кривой

        Тип данных: из перечисления ksMathCurve2DTypeEnum

        Свойство доступно только для чтения.

        Версия КОМПАС v22

        ### Синтаксис Automation

        ```
        Length = BaseCurveType()            # Получить свойство(* )
        Length = Object.GetBaseCurveType()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def CurveType(self) -> int:  # icurve2d_curvetype.html
        """
        ## CurveType - Тип 2D кривой

        Тип данных: из перечисления ksMathCurve2DTypeEnum

        Свойство доступно только для чтения.

        Версия КОМПАС v22

        ### Синтаксис Automation

        ```
        Length = CurveType()            # Получить свойство(* )
        Length = Object.GetCurveType()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def IsClosed(self) -> bool:  # icurve2d_isclosed.html
        """
        ## IsClosed - Замкнутость кривой

        Тип данных: BOOL

        1. Свойство позволяет получать признак замкнутости кривой.

        2. Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        IsClosed = Object.IsClosed()     # Получить свойство(* )
        IsClosed = Object.GetIsClosed()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def IsSelfIntersect(self) -> bool:  # icurve2d_isselfintersect.html
        """
        ## IsSelfIntersect - Самопересечение кривой

        Тип данных: BOOL

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        IsSelfIntersect = Object.IsSelfIntersect()     # Получить свойство(* )
        IsSelfIntersect = Object.GetIsSelfIntersect()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def Length(self) -> float:  # icurve2d_length.html
        """
        ## Length - Длина кривой

        Тип данных: double

        1. Свойство позволяет получать длину кривой.

        2. Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        Length = Object.Length()     # Получить свойство(* )
        Length = Object.GetLength()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def ParamMax(self) -> float:  # icurve2d_parammax.html
        """
        ## ParamMax - Максимальный параметр

        Тип данных: double

        1. Свойство позволяет получать максимальный параметр кривой.

        2. Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        ParamMax = Object.ParamMaxLength()  # Получить свойство(* )
        ParamMax = Object.GetParamMax()     # Получить свойство (**)
        ```
        """
        ...

    @property
    def ParamMin(self) -> float:  # icurve2d_parammin.html
        """
        ## ParamMin - Минимальный параметр

        Тип данных: double

        1. Свойство позволяет получать минимальный параметр кривой.

        2. Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        ParamMin = Object.ParamMin()     # Получить свойство(* )
        ParamMin = Object.GetParamMin()  # Получить свойство (**)
        ```
        """
        ...

    def CalculatePolygonByStep(self, Step) -> list[float]:  # icurve2d_calculatepolygonbystep.html
        """
        ## CalculatePolygonByStep - Выдать массив отрисовочных точек с заданной стрелкой прогиба

        Метод позволяет получить массив отрисовочных точек с заданной стрелкой прогиба.

        ### Синтаксис Automation

        ```
        VARIANT CalculatePolygonByStep( double sag );
        ```

        ### Входные параметры

        +----------------------------------------+
        | Sag | - максимальная величина прогиба. |
        +----------------------------------------+

        ### Возвращаемое значение

        Массив SAFEARRAY точек VT_ARRAY | VT_R8.
        """
        ...

    def CouplingCurvCurv(self, Curve2, Radius) -> float:  # icurve2d_couplingcurvcurv.html
        """
        ## CouplingCurvCurv - Расчет скруглений для двух кривых

        Метод позволяет рассчитать скругления для двух кривых.

        ### Синтаксис Automation

        ```
        VARIANT CouplingCurvCurv( LPDISPATCH * Curve2,
        double Radius );
        ```

        ### Входные параметры

        +--------------------------------------------------+
        | Curve2 | - указатель на интерфейс второй кривой, |
        +--------------------------------------------------+
        | Radius | - радиус скругления.                    |
        +--------------------------------------------------+

        ### Возвращаемое значение

        - Массив SAFEARRAY структуры сопряжения VT_ARRAY | VT_R8. Данные возвращаются в следующей последовательности: • double xc, yc - координаты центра сопрягающей окружности, • double x1, y1 - координаты первой точки сопряжения, • double x2, y2 - координаты второй точки сопряжения.
        """
        ...

    def GetDistancePointPoint(self, X1, Y1, X2, Y2):  # icurve2d_getdistancepointpoint.html
        """
        ## GetDistancePointPoint - Получить расстояние между двумя точками на кривой

        Метод позволяет получить расстояние между двумя точками на кривой.

        ### Синтаксис Automation

        ```
        double GetDistancePointPoint( double X1,
        double Y1,
        double X2,
        double Y2 );
        ```

        ### Входные параметры

        +-------------------------------------+
        | X1, Y1 | - координаты первой точки, |
        +-------------------------------------+
        | X2, Y2 | - координаты второй точки. |
        +-------------------------------------+

        ### Возвращаемое значение

        - Расстояние между двумя точками.
        """
        ...

    def GetDistanceToPoint(self, X, Y):  # icurve2d_getdistancetopoint.html
        """
        ## GetDistanceToPoint - Получить расстояние между точкой и проекцией точки на кривую

        Метод позволяет получить расстояние между точкой и проекцией точки на кривую.

        ### Синтаксис Automation

        ```
        double GetDistanceToPoint( double X,
        double Y );
        ```

        ### Входные параметры

        +----------------------------+
        | X, Y | - координаты точки. |
        +----------------------------+

        ### Возвращаемое значение

        - Расстояние между точкой и ее проекцией на кривую.
        """
        ...

    def GetMetricLength(self, T1, T2):  # icurve2d_getmetriclength.html
        """
        ## GetMetricLength - Метрическая длина кривой

        Метод позволяет получить метрическую длину кривой.

        ### Синтаксис Automation

        ```
        double GetMetricLength( double T1,
        double T2 );
        ```

        ### Входные параметры

        +------------------------------+
        | Т1, Т2 | - параметры кривой. |
        +------------------------------+

        ### Возвращаемое значение

        - Метрическая длина кривой.
        """
        ...

    def GetNurbsParams(self, Closed) -> bool:  # icurve2d_getnurbsparams.html
        """
        ## GetNurbsParams - Получить параметры в NURBS-представлении

        Метод позволяет получить параметры кривой в NURBS-представлении.

        ### Синтаксис Automation

        ```
        BOOL GetNurbsParams( BOOL UnClamped,
        VARIANT * Points,
        VARIANT * Weights,
        VARIANT * Knots,
        double * TMin,
        double * TMax );
        ```

        ### Входные параметры

        +----------------------------------------------------------------------------------------------------------------------------------------------+
        | UnClamped | - TRUE - вернуть параметры точек для замкнутого представления, - FALSE - вернуть параметры точек для разомкнутого представления. |
        +----------------------------------------------------------------------------------------------------------------------------------------------+

        ### Выходные параметры

        +---------------------------------------------------------+
        | Points  | - SAFEARRAY координат точек VT_ARRAY | VT_R8, |
        +---------------------------------------------------------+
        | Weights | - SAFEARRAY весов точек VT_ARRAY | VT_R8,     |
        +---------------------------------------------------------+
        | Knots   | - SAFEARRAY узлов точек VT_ARRAY | VT_R8,     |
        +---------------------------------------------------------+
        | TMin    | - минимальный параметр кривой,                |
        +---------------------------------------------------------+
        | TMax    | - максимальный параметр кривой.               |
        +---------------------------------------------------------+

        ### Возвращаемое значение

        +-----------------------------+
        | TRUE  | - в случае успеха,  |
        +-----------------------------+
        | FALSE | - в случае неудачи. |
        +-----------------------------+
        """
        ...

    def GetPointLocation(self, X, Y) -> int:  # icurve2d_getpointlocation.html
        """
        ## GetPointLocation - Положение точки относительно кривой

        ### Синтаксис Automation

        ```
        ksPointLocationTypeEnum GetPointLocation( double X, double Y );
        ```

        ### Входные параметры

        +--------------------------------+
        | X | - первая координата точки, |
        +--------------------------------+
        | Y | - вторая координата точки. |
        +--------------------------------+

        ### Возвращаемое значение

        +---------------------------------------------------------------------------------------------+
        | Result | - положение двумерной точки относительно двумерной кривой ksPointLocationTypeEnum. |
        +---------------------------------------------------------------------------------------------+
        """
        ...

    def GetSelfIntersectPoints(self) -> list[float]:  # icurve2d_getselfintersectpoints.html
        """
        ## GetSelfIntersectPoints - Получить точки самопересечения

        ### Синтаксис Automation

        ```
        VARIANT GetSelfIntersectPoints();
        ```

        ### Возвращаемое значение

        - массив SAFEARRAY точек VT_ARRAY | VT_R8,

        Версия КОМПАС v22
        """
        ...

    def GetSelfIntersectPointsEx(self, Epsilon) -> bool:  # icurve2d_getselfintersectpointsex.html
        """
        ## GetSelfIntersectPointsEx - Получить точки самопересечения

        ### Синтаксис Automation

        ```
        BOOL GetSelfIntersectPointsEx( double Epsilon, VARIANT * Points, VARIANT * T, VARIANT * IntectForm );
        ```

        ### Входные параметры

        +-------------------------------------+
        | Epsilon | - погрешность вычисления. |
        +-------------------------------------+

        ### Выходные параметры

        +--------------------------------------------------------------------------------------------------------------------+
        | Points     | - массив SAFEARRAY точек VT_ARRAY | VT_R8,                                                            |
        +--------------------------------------------------------------------------------------------------------------------+
        | T          | - массив SAFEARRAY параметров кривой VT_ARRAY | VT_R8                                                 |
        +--------------------------------------------------------------------------------------------------------------------+
        | IntectForm | - массив SAFEARRAY типов самопересечения из перечисления ksCurvesIntersectFormEnum VT_ARRAY | VT_I4 . |
        +--------------------------------------------------------------------------------------------------------------------+

        Версия КОМПАС v22

        ### Возвращаемое значение

        +-----------------------------+
        | TRUE  | - в случае успеха,  |
        +-----------------------------+
        | FALSE | - в случае неудачи. |
        +-----------------------------+
        """
        ...

    def Intersect(self, Curve2) -> list[float]:  # icurve2d_intersect.html
        """
        ## Intersect - Пересечение двух кривых

        Метод позволяет получить координаты точек пересечения двух кривых.

        ### Синтаксис Automation

        ```
        VARIANT Intersect( LPDISPATCH * Curve2 );
        ```

        ### Входные параметры

        +-----------------------------------------------------------------------------+
        | Curve2 | - указатель на интерфейс кривой, которая пересекает данную кривую. |
        +-----------------------------------------------------------------------------+

        ### Возвращаемое значение

        - Массив SAFEARRAY точек пересечений VT_ARRAY | VT_R8
        """
        ...

    def IntersectEx(self, Curve2, Epsilon, TouchInclude) -> bool:  # icurve2d_intersectex.html
        """
        ## IntersectEx - Пересечение двух кривых

        ### Синтаксис Automation

        ```
        BOOL IntersectEx( ICurve2D * Curve2, double Epsilon, BOOL TouchInclude, VARIANT * Points, VARIANT * T1, VARIANT * T2, VARIANT * IntectForm );
        ```

        ### Входные параметры

        +--------------------------------------------+
        | Curve2       | - вторая кривая ICurve2D,   |
        +--------------------------------------------+
        | Epsilon      | - погрешность вычисления,   |
        +--------------------------------------------+
        | TouchInclude | - возвращать точки касания. |
        +--------------------------------------------+

        ### Выходные параметры

        +--------------------------------------------------------------------------------------------------------------------+
        | Points     | - массив SAFEARRAY точек VT_ARRAY | VT_R8,                                                            |
        +--------------------------------------------------------------------------------------------------------------------+
        | T1         | - массив SAFEARRAY параметров для первой кривой VT_ARRAY | VT_R8,                                     |
        +--------------------------------------------------------------------------------------------------------------------+
        | T2         | - массив SAFEARRAY параметров для первой кривой VT_ARRAY | VT_R8,                                     |
        +--------------------------------------------------------------------------------------------------------------------+
        | IntectForm | - массив SAFEARRAY типов самопересечения из перечисления ksCurvesIntersectFormEnum VT_ARRAY | VT_I4 . |
        +--------------------------------------------------------------------------------------------------------------------+

        Версия КОМПАС v22

        ### Возвращаемое значение

        +-----------------------------+
        | TRUE  | - в случае успеха,  |
        +-----------------------------+
        | FALSE | - в случае неудачи. |
        +-----------------------------+
        """
        ...

    def MovePoint(self, X, Y, Lenght, Direction) -> bool:  # icurve2d_movepoint.html
        """
        ## MovePoint - Сдвинуть точку на расстояние Lenght по кривой

        ### Синтаксис Automation

        ```
        BOOL MovePoint( double * X,
        double * Y,
        double Lenght,
        BOOL Direction );
        ```

        ### Входные параметры

        +------------------------------------------------------------+
        | X, Y      | - координаты точки,                            |
        +------------------------------------------------------------+
        | Lenght    | - расстояние, на которое нужно сместить точку, |
        +------------------------------------------------------------+
        | Direction | - направление смещения.                        |
        +------------------------------------------------------------+

        ### Возвращаемое значение

        +-----------------------------+
        | TRUE  | - в случае успеха,  |
        +-----------------------------+
        | FALSE | - в случае неудачи. |
        +-----------------------------+
        """
        ...

    def PointOn(self, T) -> bool:  # icurve2d_pointon.html
        """
        ## PointOn - Получить координаты точки по параметру t

        Метод позволяет получить координаты точки по параметру кривой.

        ### Синтаксис Automation

        ```
        BOOL PointOn( double T,
        double * X,
        double * Y );
        ```

        ### Входные параметры

        +------------------------+
        | Т | - параметр кривой. |
        +------------------------+

        ### Выходные параметры

        +----------------------------+
        | X, Y | - координаты точки. |
        +----------------------------+

        ### Возвращаемое значение

        +-----------------------------+
        | TRUE  | - в случае успеха,  |
        +-----------------------------+
        | FALSE | - в случае неудачи. |
        +-----------------------------+
        """
        ...

    def PointProjection(self, X, Y) -> bool:  # icurve2d_pointprojection.html
        """
        ## PointProjection - Проекция точки на кривую

        Метод позволяет построить проекцию точки на кривую.

        ### Синтаксис Automation

        ```
        BOOL PointProjection( double X,
        double Y,
        double * Kx,
        double * Ky,
        double * T,
        double * Angle );
        ```

        ### Входные параметры

        +----------------------------+
        | X, Y | - координаты точки. |
        +----------------------------+

        ### Выходные параметры

        +---------------------------------------+
        | Kx, Ky | - координаты проекции точки, |
        +---------------------------------------+
        | Т      | - параметр кривой,           |
        +---------------------------------------+
        | Angle  | - угол нормали.              |
        +---------------------------------------+

        ### Возвращаемое значение

        +-----------------------------+
        | TRUE  | - в случае успеха,  |
        +-----------------------------+
        | FALSE | - в случае неудачи. |
        +-----------------------------+
        """
        ...

    def Tangent(self, Curve2) -> bool:  # icurve2d_tangent.html
        """
        ## Tangent - Касательные для двух кривых

        Метод позволяет получить касательные для двух кривых.

        ### Синтаксис Automation

        ```
        BOOL Tangent( LPDISPATCH * Curve2,
        VARIANT * Curve1Points,
        VARIANT * Curve2Points );
        ```

        ### Входные параметры

        +--------------------------------------------------+
        | Curve2 | - указатель на интерфейс второй кривой. |
        +--------------------------------------------------+

        ### Выходные параметры

        +----------------------------------------------------------------------------+
        | Curve1Points | - массив SAFEARRAY точек на первой кривой VT_ARRAY | VT_R8, |
        +----------------------------------------------------------------------------+
        | Curve2Points | - массив SAFEARRAY точек на второй кривой VT_ARRAY | VT_R8. |
        +----------------------------------------------------------------------------+

        ### Возвращаемое значение

        +-----------------------------+
        | TRUE  | - в случае успеха,  |
        +-----------------------------+
        | FALSE | - в случае неудачи. |
        +-----------------------------+
        """
        ...

    def TangentLinePoint(self, X, Y) -> list[float]:  # icurve2d_tangentlinepoint.html
        """
        ## TangentLinePoint - Точки касания кривой и прямой из заданной точки

        Метод позволяет получить координаты точек касания кривой и прямой из заданной точки.

        ### Синтаксис Automation

        ```
        VARIANT TangentLinePoint( double X,
        double Y );
        ```

        ### Входные параметры

        +----------------------------+
        | X, Y | - координаты точки. |
        +----------------------------+

        ### Возвращаемое значение

        - Массив SAFEARRAY точек на кривой VT_ARRAY | VT_R8.
        """
        ...




class IDrawingGroup(IKompasAPIObject):  # idrawinggroup.html
    """
    ## Интерфейс IDrawingGroup

    Интерфейс группы (постоянной, временной, именованной).

    Интерфейс позволяет задавать параметры группы (постоянной, временной или именованной).

    Интерфейс можно получить у коллекции групп, используя свойство IDrawingGroups::Item или метод IDrawingGroups::Add или у дополнительного интерфейса документа 2D с помощью свойства IKompasDocument2D1::CurrentGroup.
    """

    @property
    def Current(self) -> bool:  # idrawinggroup_current.html
        """
        ## Current - Текущая группа

        Тип данных: BOOL

        1. Свойство позволяет получать признак того, что группа является текущей.

        2. Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        Current = Object.Current       # Получить свойство(* )
        Current = Object.GetCurrent()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def Name(self) -> str:  # idrawinggroup_name.html
        """
        ## Name - Имя именованной группы

        Тип данных: BSTR

        Свойство позволяет устанавливать и получать имя группы и добавляет группу в именованные.

        ### Синтаксис Automation

        ```
        Name = Object.Name       # Получить свойство(* )
        Object.Name = Name       # Установить свойство (* )
        Name = Object.GetName()  # Получить свойство (**)
        Object.SetName( Name )   # Установить свойство (**)
        ```
        """
        ...
    @Name.setter
    def Name(self, value: str): ...

    @property
    def ObjectsCount(self) -> int:  # idrawinggroup_objectscount.html
        """
        ## ObjectsCount - Количество объектов в группе

        Тип данных: long

        1. Свойство позволяет получать количество объектов в группе.

        2. Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        ObjectsCount = Object.ObjectsCount       # Получить свойство(* )
        ObjectsCount = Object.GetObjectsCount()  # Получить свойство (**)
        ```
        """
        ...

    def AddObjects(self, Objects) -> bool:  # idrawinggroup_addobjects.html
        """
        ## AddObjects - Добавить объекты в группу

        Метод позволяет добавить объекты в группу.

        ### Синтаксис Automation

        ```
        BOOL AddObjects( VARIANT Objects );
        ```

        ### Входные параметры

        +------------------------------------------------------------------------------------------------------------------------+
        | Objects | - объект или массив объектов для добавления в группу ( VARIANT типа VT_DISPATCH или VT_ARRAY | VT_DISPATCH). |
        +------------------------------------------------------------------------------------------------------------------------+

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def AddRegion(self, RegionType, XMin, YMin, XMax, YMax) -> bool:  # idrawinggroup_addregion.html
        """
        ## AddRegion - Добавить объекты в группу рамкой

        Метод позволяет добавить объекты в группу рамкой.

        ### Синтаксис Automation

        ```
        BOOL AddRegion( long RegionType,
        double XMin,
        double YMin,
        double XMax,
        double YMax );
        ```

        ### Входные параметры

        +------------------------------------------------------------+
        | RegionType | - тип рамки из перечисления ksRegionTypeEnum, |
        +------------------------------------------------------------+
        | XMin, YMin | - координаты нижнего левого угла рамки,       |
        +------------------------------------------------------------+
        | XMax, YMax | - координаты верхнего правого угла рамки.     |
        +------------------------------------------------------------+

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def Clear(self, DeleteTmp) -> bool:  # idrawinggroup_clear.html
        """
        ## Clear - Очистить группу

        Метод позволяет удалить все объекты из группы.

        ### Синтаксис Automation

        ```
        BOOL Clear( BOOL DeleteTmp );
        ```

        ### Входные параметры

        +---------------------------------------------+
        | DeleteTmp | - удалять ли временные объекты. |
        +---------------------------------------------+

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def Close(self) -> bool:  # idrawinggroup_close.html
        """
        ## Close - Закрыть группу

        Метод позволяет закрыть отредактированную группу.

        ### Синтаксис Automation

        ```
        BOOL Close();
        ```

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def Delete(self) -> bool:  # idrawinggroup_delete.html
        """
        ## Delete - Удалить группу

        Метод позволяет удалить группу.

        ### Синтаксис Automation

        ```
        BOOL Delete();
        ```

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def DetachObjects(self, Objects, DeleteTmp) -> bool:  # idrawinggroup_detachobjects.html
        """
        ## DetachObjects - Исключить объекты из группы

        Метод позволяет исключить объекты из группы.

        ### Синтаксис Automation

        ```
        BOOL DetachObjects( VARIANT Objects,
        BOOL DeleteTmp );
        ```

        ### Входные параметры

        +-------------------------------------------------------------------------------------------------------------------------+
        | Objects   | - объект или массив объектов для удаления из группы( VARIANT типа VT_DISPATCH или VT_ARRAY | VT_DISPATCH ), |
        +-------------------------------------------------------------------------------------------------------------------------+
        | DeleteTmp | - удалять ли временные объекты.                                                                             |
        +-------------------------------------------------------------------------------------------------------------------------+

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def Draw(self, HWnd) -> bool:  # idrawinggroup_draw.html
        """
        ## Draw - Отрисовать в окне

        Метод позволяет отрисовать группу в окне.

        ### Синтаксис Automation

        ```
        BOOL Draw( OLE_HANDLE HWnd );
        ```

        ### Входные параметры

        +---------------------------+
        | HWnd | - дескриптор окна. |
        +---------------------------+

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def ExistObject(self, Object) -> bool:  # idrawinggroup_existobject.html
        """
        ## ExistObject - Проверить, есть ли объект в группе

        Метод позволяет определить, есть ли в группе заданный объект.

        ### Синтаксис Automation

        ```
        BOOL ExistObject( LPDISPATCH Object );
        ```

        ### Входные параметры

        +--------------------------------------------------------------+
        | Object | - объект, наличие которого в группе надо проверить. |
        +--------------------------------------------------------------+

        ### Возвращаемое значение

        +---------------------------------+
        | TRUE  | - объект есть в группе, |
        +---------------------------------+
        | FALSE | - объекта нет в группе. |
        +---------------------------------+
        """
        ...

    def Objects(self, Type) -> list[IDispatch]:  # idrawinggroup_objects.html
        """
        ## Objects - Получить массив объектов SAFEARRAY|VT_DISPATCH

        Тип данных: VARIANT типа VT_ARRAY | VT_DISPATCH

        1. Свойство позволяет получать массив объектов, входящих в группу. Если возвращается один объект, то тип VARIANT-а будет VT_DISPATCH, если возвращается несколько объектов, то тип VARIANT-а будет VT_ARRAY | VT_DISPATCH.

        2. Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        Objects = Object.Objects       # Получить свойство(* )
        Objects = Object.GetObjects()  # Получить свойство (**)
        ```
        """
        ...

    def Open(self) -> bool:  # idrawinggroup_open.html
        """
        ## Open - Открыть группу

        Метод позволяет открыть группу для редактирования.

        ### Синтаксис Automation

        ```
        BOOL Open();
        ```

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def ReadFragment(self, FileName, CurentLayer, XBase, YBase, Scale, Angle, ScaleProjLinesSize) -> bool:  # idrawinggroup_readfragment.html
        """
        ## ReadFragment - Добавить объекты в группу из фрагмента

        Метод позволяет добавить объекты в группу из фрагмента.

        ### Синтаксис Automation

        ```
        BOOL ReadFragment( BSTR FileName,
        BOOL CurentLayer,
        double XBase,
        double YBase,
        double Scale,
        double Angle,
        BOOL ScaleProjLinesSize );
        ```

        ### Входные параметры

        +---------------------------------------------------------------------------------------------+
        | FileName           | - имя файла фрагмента,                                                 |
        +---------------------------------------------------------------------------------------------+
        | CurentLayer        | - тип размещения по слоям FALSE - на свои слои, TRUE - в текущий слой, |
        +---------------------------------------------------------------------------------------------+
        | XBase, YBase       | - координаты базовой точки в СК вида,                                  |
        +---------------------------------------------------------------------------------------------+
        | Scale              | - масштаб,                                                             |
        +---------------------------------------------------------------------------------------------+
        | Angle              | - угол поворота в СК вида,                                             |
        +---------------------------------------------------------------------------------------------+
        | ScaleProjLinesSize | - масштабировать ли проекционные линии.                                |
        +---------------------------------------------------------------------------------------------+

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def ReadFromClip(self, AttrCopy, SpcObjCopy) -> bool:  # idrawinggroup_readfromclip.html
        """
        ## ReadFromClip - Считать объекты из клипа

        Метод позволяет считать объекты из клипа в группу.

        ### Синтаксис Automation

        ```
        BOOL ReadFromClip( BOOL AttrCopy,
        BOOL SpcObjCopy );
        ```

        ### Входные параметры

        +----------------------------------------------------+
        | AttrCopy   | - копировать ли атрибуты,             |
        +----------------------------------------------------+
        | SpcObjCopy | - копировать ли объекты спецификации. |
        +----------------------------------------------------+

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def Store(self) -> bool:  # idrawinggroup_store.html
        """
        ## Store - Вставить временную группу в документ

        Метод позволяет вставить временную группу в документ.

        ### Синтаксис Automation

        ```
        BOOL Store();
        ```

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def WriteFragment(self, FileName, Comment, XBase, YBase) -> bool:  # idrawinggroup_writefragment.html
        """
        ## WriteFragment - Записать в фрагмент

        Метод позволяет записать группу в фрагмент.

        ### Синтаксис Automation

        ```
        BOOL WriteFragment( BSTR FileName,
        BSTR Comment,
        double XBase,
        double YBase );
        ```

        ### Входные параметры

        +------------------------------------------------------+
        | FileName     | - имя файла фрагмента,                |
        +------------------------------------------------------+
        | Comment      | - комментарий,                        |
        +------------------------------------------------------+
        | XBase, YBase | - координаты базовой точки в СК вида, |
        +------------------------------------------------------+

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def WriteToClip(self, AttrCopy, SpcObjCopy) -> bool:  # idrawinggroup_writetoclip.html
        """
        ## WriteToClip - Записать объекты в клип

        Метод позволяет скопировать объекты из группы в клип.

        ### Синтаксис Automation

        ```
        BOOL WriteToClip( BOOL AttrCopy,
        BOOL SpcObjCopy );
        ```

        ### Входные параметры

        +----------------------------------------------------+
        | AttrCopy   | - копировать ли атрибуты,             |
        +----------------------------------------------------+
        | SpcObjCopy | - копировать ли объекты спецификации. |
        +----------------------------------------------------+

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...




class IDrawingGroups(IKompasCollection):  # idrawinggroups.html
    """
    ## Интерфейс IDrawingGroups

    Интерфейс коллекции групп (именованных, рабочих временных и постоянных).

    Позволяет создавать и получать группы (именованные, рабочие временные и постоянные).

    Данный интерфейс можно получить у дополнительного интерфейса документа 2D с помощью свойства IKompasDocument2D1::DrawingGroups.
    """

    def Add(self, Temp, Name) -> IDrawingGroup:  # idrawinggroups_add.html
        """
        ## Add - Создать новый элемент и добавить его в коллекцию

        ### Синтаксис Automation

        ```
        LPDISPATCH Add( BOOL temp,
        BSTR Name );
        ```

        ### Входные параметры

        +----------------------------------------+
        | temp | - является ли группа временной, |
        +----------------------------------------+
        | Name | - имя группы.                   |
        +----------------------------------------+

        ### Возвращаемое значение

        - Указатель на интерфейс группы IDrawingGroup.
        """
        ...

    def Item(self, Index) -> IDrawingGroup:  # idrawinggroups_item.html
        """
        ## Item - Элемент, заданный по индексу

        Тип данных: указатель на интерфейс группы IDrawingGroup

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        Item = iObject.Item ( Index );    # Получить свойство(* )
        Item = iObject.GetItem( Index );  # Получить свойство (**)
        ```
        """
        ...




class ILayoutSheets(IKompasCollection):  # ilayoutsheets.html
    """
    ## Интерфейс ILayoutSheets

    Интерфейс коллекции листов оформления.

    Управляет массивом интерфейсов листов оформления ILayoutSheet.

    1. После вызова метода Add лист оформления будет добавлен с умолчательными параметрами (заданными в настройках). Для изменения параметров листа нужно изменить их у полученного объекта и вызывать ILayoutSheet::Update.

    2. Данный интерфейс может быть получен от интерфейса документа IKompasDocument с помощью свойства IKompasDocument::LayoutSheets.
    """

    def Add(self) -> ILayoutSheet:  # ilayoutsheets_add.html
        """
        ## Add - Создать лист оформления (добавляет лист оформления в коллекцию)

        Лист оформления будет добавлен с умолчательными параметрами (заданными в настройках). Для изменения параметров листа в документе нужно задать его параметры и вызвать метод ILayoutSheet::Update.

        ### Синтаксис Automation

        ```
        LPDISPATCH Add();
        ```

        ### Возвращаемое значение

        - Указатель на интерфейс листа оформления ILayoutSheet.
        """
        ...

    def Item(self, Index) -> ILayoutSheet:  # ilayoutsheets_item.html
        """
        ## Item - Лист оформления, заданный по индексу

        Тип данных: указатель на интерфейс ILayoutSheet листа оформления

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        Sheet = iObject.Item (Index)     # Получить свойство(* )
        Sheet = iObject.GetItem (Index)  # Получить свойство (**)
        ```
        """
        ...

    def ItemByNumber(self, Number) -> ILayoutSheet:  # ilayoutsheets_itembynumber.html
        """
        ## ItemByNumber - Лист оформления, заданный по номеру

        Тип данных: указатель на интерфейс ILayoutSheet листа оформления

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        Sheet = iObject.ItemByNumber (Number)     # Получить свойство(* )
        Sheet = iObject.GetItemByNumber (Number)  # Получить свойство (**)
        ```

        ### Входные параметры

        +----------------------------------------------------------+
        | Number | - номер листа, отображается в основной надписи. |
        +----------------------------------------------------------+
        """
        ...




class ILayoutSheet(IKompasAPIObject):  # ilayoutsheet.html
    """
    ## Интерфейс ILayoutSheet

    Интерфейс параметров листа оформления.

    В чертежно-графическом редакторе КОМПАС-График при работе с документами используется понятие оформления. Оформление включает основную надпись, а также внешнюю и внутреннюю рамки. Данный интерфейс позволяет выбрать оформление, которое будет использоваться для листов документа. Оформления документов хранятся в специальных системных библиотеках - файлах с расширением lyt.

    Данный интерфейс можно получить у коллекции листов оформления ILayoutSheets с помощью свойства ILayoutSheets::Item, ILayoutSheets::ItemByNumber, метода ILayoutSheets::Add.
    """

    @property
    def Format(self) -> ISheetFormat:  # ilayoutsheet_format.html
        """
        ## Format - Формат листа

        Тип данных: указатель на интерфейс ISheetFormat формата листа оформления

        1. Данное свойство определяет формат листа оформления. После изменения значений свойств формата листа оформления, для того чтобы изменения вступили в силу, надо вызвать метод Update.

        2. Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        Format = iObject.Format       # Получить свойство(* )
        Format = iObject.GetFormat()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def LayoutLibraryFileName(self) -> str:  # ilayoutsheet_layoutlibraryfilename.html
        """
        ## LayoutLibraryFileName - Имя файла библиотеки стилей оформления

        Тип данных: BSTR (строка)

        Данное свойство позволяет получить/изменить полное имя файла библиотеки стилей оформлений, из которой взят данный стиль. После изменения значений свойств формата листа оформления, для того чтобы изменения вступили в силу, надо вызвать метод Update.

        ### Синтаксис Automation

        ```
        LayoutLibraryFileName = iObject.LayoutLibraryFileName       # Получить свойство(* )
        iObject.LayoutLibraryFileName = LayoutLibraryFileName       # Установить свойство (* )
        LayoutLibraryFileName = iObject.GetLayoutLibraryFileName()  # Получить свойство (**)
        iObject.SetLayoutLibraryFileName (LayoutLibraryFileName)    # Установить свойство (**)
        ```
        """
        ...
    @LayoutLibraryFileName.setter
    def LayoutLibraryFileName(self, value: str): ...

    @property
    def LayoutStyleNumber(self) -> float:  # ilayoutsheet_layoutstylenumber.html
        """
        ## LayoutStyleNumber - Номер стиля оформления

        Тип данных: double

        Данное свойство позволяет получить\\изменить номер используемого стиля в библиотеке стилей оформлений (см. LayoutLibraryFileName). После изменения значений свойств формата листа оформления, для того чтобы изменения вступили в силу, надо вызвать метод Update.

        ### Синтаксис Automation

        ```
        LayoutStyleNumber = iObject.LayoutStyleNumber       # Получить свойство(* )
        iObject.LayoutStyleNumber = LayoutStyleNumber       # Установить свойство (* )
        LayoutStyleNumber = iObject.GetLayoutStyleNumber()  # Получить свойство (**)
        iObject.SetLayoutStyleNumber (LayoutStyleNumber)    # Установить свойство (**)
        ```
        """
        ...
    @LayoutStyleNumber.setter
    def LayoutStyleNumber(self, value: float): ...

    @property
    def SheetType(self) -> int:  # ilayoutsheet_sheettype.html
        """
        ## SheetType - Тип листа

        Тип данных: из перечисления ksSheetTypeEnum

        ### Синтаксис Automation

        ```
        SheetType = iObject.SheetType       # Получить свойство(* )
        iObject.SheetType = SheetType       # Установить свойство (* )
        SheetType = iObject.GetSheetType()  # Получить свойство (**)
        iObject.SetSheetType (SheetType)    # Установить свойство (**)
        ```
        """
        ...
    @SheetType.setter
    def SheetType(self, value: int): ...

    @property
    def Stamp(self) -> IStamp:  # ilayoutsheet_stamp.html
        """
        ## Stamp - Основная надпись листа

        Тип данных: указатель на интерфейс IStamp

        1. Свойство позволяет получать интерфейс основной надписи.

        2. Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        Stamp = Object.Stamp       # Получить свойство(* )
        Stamp = Object.GetStamp()  # Получить свойство (**)
        ```
        """
        ...

    def Delete(self) -> bool:  # ilayoutsheet_delete.html
        """
        ## Delete - Удалить лист

        Удаляет лист оформления из документа.

        ### Синтаксис Automation

        ```
        BOOL Delete();
        ```

        ### Возвращаемое значение

        +-----------------------------+
        | TRUE  | - в случае успеха,  |
        +-----------------------------+
        | FALSE | - в случае неудачи. |
        +-----------------------------+
        """
        ...

    def GetPlaceInsideFrames(self) -> bool:  # ilayoutsheet_getplaceinsideframes.html
        """
        ## GetPlaceInsideFrames - Выдать свободное место внутри рамок

        ### Синтаксис Automation

        ```
        BOOL GetPlaceInsideFrames( double* Left, double* Top, double* Right, double* Bottom);
        ```

        ### Выходные параметры

        +---------------------------------------------------+
        | Left, Top     | - координата верхней левой точки, |
        +---------------------------------------------------+
        | Right, Bottom | - координата нижней правой точки. |
        +---------------------------------------------------+

        ### Возвращаемое значение

        +-----------------------------+
        | TRUE  | - в случае успеха,  |
        +-----------------------------+
        | FALSE | - в случае неудачи. |
        +-----------------------------+
        """
        ...

    def Update(self) -> bool:  # ilayoutsheet_update.html
        """
        ## Update - Обновить данные

        Все изменения, сделанные в данном интерфейсе и интерфейсе формата листа, вступят в силу только после вызова этого метода.

        ### Синтаксис Automation

        ```
        BOOL Update();
        ```

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - обновление прошло успешно,     |
        +------------------------------------------+
        | FALSE | - обновить настройки не удалось. |
        +------------------------------------------+
        """
        ...




class ISheetFormat(IKompasAPIObject):  # isheetformat.html
    """
    ## Интерфейс ISheetFormat

    Интерфейс формата листа.

    1. Данный интерфейс позволяет получить и изменить текущий формат листа для документа.

    2. Данный интерфейс может быть получен следующими способами:

    •от интерфейса стиля спецификации ISpecificationStyle с помощью свойства ISpecificationStyle::Format,

    •от интерфейса параметров листа оформления ILayoutSheet с помощью свойства ILayoutSheet::Format.
    """

    @property
    def Format(self) -> int:  # isheetformat_format.html
        """
        ## Format - Формат листа

        Тип данных: из перечисления ksDocumentFormatEnum

        ### Синтаксис Automation

        ```
        Format = iObject.Format       # Получить свойство(* )
        iObject.Format = Format       # Установить свойство (* )
        Format = iObject.GetFormat()  # Получить свойство (**)
        iObject.SetFormat (Format)    # Установить свойство (**)
        ```
        """
        ...
    @Format.setter
    def Format(self, value: int): ...

    @property
    def FormatHeight(self) -> int:  # isheetformat_formatheight.html
        """
        ## FormatHeight - Высота пользовательского формата

        Тип данных: long

        Свойство используется только для пользовательских форматов листа (см. ISheetFormat::Format).

        ### Синтаксис Automation

        ```
        FormatHeight = iObject.FormatHeight       # Получить свойство(* )
        iObject.FormatHeight = FormatHeight       # Установить свойство (* )
        FormatHeight = iObject.GetFormatHeight()  # Получить свойство (**)
        iObject.SetFormatHeight (FormatHeight)    # Установить свойство (**)
        ```
        """
        ...
    @FormatHeight.setter
    def FormatHeight(self, value: int): ...

    @property
    def FormatMultiplicity(self) -> int:  # isheetformat_formatmultiplicity.html
        """
        ## FormatMultiplicity - Кратность формата листа

        Тип данных: long

        1. Свойство используется только для стандартных форматов листа (см. ISheetFormat::Format).

        2. Свойство не используется для текстовых документов.

        ### Синтаксис Automation

        ```
        FormatMultiplicity = iObject.FormatMultiplicity       # Получить свойство(* )
        iObject.FormatMultiplicity = FormatMultiplicity       # Установить свойство (* )
        FormatMultiplicity = iObject.GetFormatMultiplicity()  # Получить свойство (**)
        iObject.SetFormatMultiplicity (FormatMultiplicity)    # Установить свойство (**)
        ```
        """
        ...
    @FormatMultiplicity.setter
    def FormatMultiplicity(self, value: int): ...

    @property
    def FormatWidth(self) -> int:  # isheetformat_formatwidth.html
        """
        ## FormatWidth - Ширина пользовательского формата

        Тип данных: long

        Свойство используется только для пользовательских форматов листа (см. ISheetFormat::Format).

        ### Синтаксис Automation

        ```
        FormatWidth = iObject.FormatWidth       # Получить свойство(* )
        iObject.FormatWidth = FormatWidth       # Установить свойство (* )
        FormatWidth = iObject.GetFormatWidth()  # Получить свойство (**)
        iObject.SetFormatWidth (FormatWidth)    # Установить свойство (**)
        ```
        """
        ...
    @FormatWidth.setter
    def FormatWidth(self, value: int): ...

    @property
    def VerticalOrientation(self) -> int:  # isheetformat_verticalorientation.html
        """
        ## VerticalOrientation - Вертикальная ориентация формата листа

        Тип данных: long

        Свойство используется только для стандартных форматов листа (см. ISheetFormat::Format).

        ### Синтаксис Automation

        ```
        VerticalOrientation = iObject.VerticalOrientation       # Получить свойство(* )
        iObject.VerticalOrientation = VerticalOrientation       # Установить свойство (* )
        VerticalOrientation = iObject.GetVerticalOrientation()  # Получить свойство (**)
        iObject.SetVerticalOrientation (VerticalOrientation)    # Установить свойство (**)
        ```
        """
        ...
    @VerticalOrientation.setter
    def VerticalOrientation(self, value: int): ...




class ISpecRough(IKompasAPIObject):  # ispecrough.html
    """
    ## Интерфейс ISpecRough

    Неуказанная шероховатость.

    Интерфейс позволяет задавать и получать параметры неуказанной шероховатости.

    Интерфейс можно получить у интерфейса чертежа с помощью свойства IDrawingDocument::SpecRough.
    """

    @property
    def AddSign(self) -> bool:  # ispecrough_addsign.html
        """
        ## AddSign - Добавить знак в скобках

        Тип данных: BOOL

        Свойство позволяет устанавливать и получать признак знака в скобках.

        ### Синтаксис Automation

        ```
        AddSign = Object.AddSign       # Получить свойство(* )
        Object.AddSign = AddSign       # Установить свойство (* )
        AddSign = Object.GetAddSign()  # Получить свойство (**)
        Object.SetAddSign( AddSign )   # Установить свойство (**)
        ```
        """
        ...
    @AddSign.setter
    def AddSign(self, value: bool): ...

    @property
    def AutoPlacement(self) -> bool:  # ispecrough_autoplacement.html
        """
        ## AutoPlacement - Авторазмещение

        Тип данных: BOOL

        Свойство позволяет устанавливать и получать признак авторазмещения неуказанной шероховатости.

        ### Синтаксис Automation

        ```
        AutoPlacement = Object.AutoPlacement       # Получить свойство(* )
        Object.AutoPlacement = AutoPlacement       # Установить свойство (* )
        AutoPlacement = Object.GetAutoPlacement()  # Получить свойство (**)
        Object.SetAutoPlacement( AutoPlacement )   # Установить свойство (**)
        ```
        """
        ...
    @AutoPlacement.setter
    def AutoPlacement(self, value: bool): ...

    @property
    def Crossed(self) -> bool:  # ispecrough_crossed.html
        """
        ## Crossed - Признак необходимости перестроения неуказанной шероховатости

        Тип данных: BOOL

        Свойство доступно только для чтения.

        Версия Компас v18.1

        ### Синтаксис Automation

        ```
        Crossed = Object.Crossed       # Получить свойство(* )
        Crossed = Object.GetCrossed()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def Distance(self) -> float:  # ispecrough_distance.html
        """
        ## Distance - Расстояние до знака

        Тип данных: double

        Свойство позволяет устанавливать и получать расстояние до знака неуказанной шероховатости.

        ### Синтаксис Automation

        ```
        Distance = Object.Distance       # Получить свойство(* )
        Object.Distance = Distance       # Установить свойство (* )
        Distance = Object.GetDistance()  # Получить свойство (**)
        Object.SetDistance( Distance )   # Установить свойство (**)
        ```
        """
        ...
    @Distance.setter
    def Distance(self, value: float): ...

    @property
    def IsCreated(self) -> bool:  # ispecrough_iscreated.html
        """
        ## IsCreated - Признак отображения в документе

        Тип данных: BOOL

        1. Свойство позволяет получать признак отображения неуказанной шероховатости в документе.

        2. Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        IsCreated = Object.IsCreated       # Получить свойство(* )
        IsCreated = Object.GetIsCreated()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def SignType(self) -> int:  # ispecrough_signtype.html
        """
        ## SignType - Тип значка

        Тип данных: из перечисления ksRoughSignEnum

        Свойство позволяет устанавливать тип значка неуказанной шероховатости.

        ### Синтаксис Automation

        ```
        SignType = Object.SignType       # Получить свойство(* )
        Object.SignType = SignType       # Установить свойство (* )
        SignType = Object.GetSignType()  # Получить свойство (**)
        Object.SetSignType( SignType )   # Установить свойство (**)
        ```
        """
        ...
    @SignType.setter
    def SignType(self, value: int): ...

    @property
    def Text(self) -> str:  # ispecrough_text.html
        """
        ## Text - Текст

        Тип данных: BSTR

        Свойство позволяет устанавливать и получать текст неуказанной шероховатости.

        ### Синтаксис Automation

        ```
        Text = Object.Text       # Получить свойство(* )
        Object.Text = Text       # Установить свойство (* )
        Text = Object.GetText()  # Получить свойство (**)
        Object.SetText( Text )   # Установить свойство (**)
        ```
        """
        ...
    @Text.setter
    def Text(self, value: str): ...

    @property
    def X(self) -> float:  # ispecrough_x.html
        """
        ## X - Координата начала выносной линии или положение знака по оси Х

        Тип данных: double

        Свойство позволяет устанавливать и получать координату Х положения знака неуказанной шероховатости.

        ### Синтаксис Automation

        ```
        X = Object.X       # Получить свойство(* )
        Object.X = X       # Установить свойство (* )
        X = Object.GetX()  # Получить свойство (**)
        Object.SetX( X )   # Установить свойство (**)
        ```
        """
        ...
    @X.setter
    def X(self, value: float): ...

    @property
    def Y(self) -> float:  # ispecrough_y.html
        """
        ## Y - Координата начала выносной линии или положение знака по оси Y

        Тип данных: double

        Свойство позволяет устанавливать и получать координату Y положения знака неуказанной шероховатости.

        ### Синтаксис Automation

        ```
        Y = Object.Y       # Получить свойство(* )
        Object.Y = Y       # Установить свойство (* )
        Y = Object.GetY()  # Получить свойство (**)
        Object.SetY( Y )   # Установить свойство (**)
        ```
        """
        ...
    @Y.setter
    def Y(self, value: float): ...

    def Delete(self) -> bool:  # ispecrough_delete.html
        """
        ## Delete - Удалить объект

        Метод позволяет удалить неуказанную шероховатость.

        ### Синтаксис Automation

        ```
        BOOL Delete();
        ```

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def Update(self) -> bool:  # ispecrough_update.html
        """
        ## Update - Обновить данные

        Метод позволяет применить заданные параметры неуказанной шероховатости.

        ### Синтаксис Automation

        ```
        BOOL Update();
        ```

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...




class IStamp(IKompasAPIObject):  # istamp.html
    """
    ## Интерфейс IStamp

    Интерфейс основной надписи (штампа).

    Интерфейс позволяет задавать и получать параметры штампа.

    Интерфейс можно получить у интерфейса параметров листа оформления с помощью свойства ILayoutSheet::Stamp.
    """

    @property
    def Crossed(self) -> bool:  # istamp_crossed.html
        """
        ## Crossed - Признак необходимости перестроения штампа

        Тип данных: BOOL

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        Crossed = Object.Crossed       # Получить свойство(* )
        Crossed = Object.GetCrossed()  # Получить свойство (**)
        ```
        """
        ...

    def Clear(self, Id) -> bool:  # istamp_clear.html
        """
        ## Clear - Очистить штамп

        Метод позволяет очистить штамп.

        ### Синтаксис Automation

        ```
        BOOL Clear();
        ```

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def GetNextColumnId(self, Id):  # istamp_getnextcolumnid.html
        """
        ## GetNextColumnId - Получить идентификатор следующей непустой ячейки штампа

        Метод позволяет получить идентификатор следующей непустой ячейки штампа.

        ### Синтаксис Automation

        ```
        long GetNextColumnId( long Id );
        ```

        ### Входные параметры

        +-------------------------------------+
        | Id | - идентификатор ячейки штампа. |
        +-------------------------------------+

        ### Возвращаемое значение

        - Идентификатор следующей не пустой ячейки штампа.
        """
        ...

    def Text(self, Id) -> IText:  # istamp_text.html
        """
        ## Text - Текст

        Тип данных: указатель на интерфейс текста IText

        1. Свойство позволяет получать интерфейс текста ячейки штампа.

        2. Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        Text = Object.Text( Id )     # Получить свойство(* )
        Text = Object.GetText( Id )  # Получить свойство (**)
        ```

        ### Входные параметры

        +-------------------------------------+
        | Id (long) | - идентификатор ячейки. |
        +-------------------------------------+
        """
        ...

    def Update(self) -> bool:  # istamp_update.html
        """
        ## Update - Обновить данные

        Метод позволяет применить заданные параметры штампа.

        ### Синтаксис Automation

        ```
        BOOL Update();
        ```

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...




class ITechnicalDemand(IKompasAPIObject):  # itechnicaldemand.html
    """
    ## Интерфейс ITechnicalDemand

    Интерфейс технических требований.

    Интерфейс позволяет задавать и получать параметры технических требований.

    Интерфейс можно получить у интерфейса чертежа с помощью свойства IDrawingDocument::TechnicalDemand.
    """

    @property
    def AutoPlacement(self) -> bool:  # itechnicaldemand_autoplacement.html
        """
        ## AutoPlacement - Авторазмещение

        Тип данных: BOOL

        Свойство позволяет устанавливать и получать признак авторазмещения технических требований.

        ### Синтаксис Automation

        ```
        AutoPlacement = Object.AutoPlacement       # Получить свойство(* )
        Object.AutoPlacement = AutoPlacement       # Установить свойство (* )
        AutoPlacement = Object.GetAutoPlacement()  # Получить свойство (**)
        Object.SetAutoPlacement( AutoPlacement )   # Установить свойство (**)
        ```
        """
        ...
    @AutoPlacement.setter
    def AutoPlacement(self, value: bool): ...

    @property
    def BlocksGabarits(self) -> list[float]:  # itechnicaldemand_blocksgabarits.html
        """
        ## BlocksGabarits - Габариты

        Тип данных: VARIANT типа VT_ARRAY | VT_R8

        Свойство позволяет устанавливать и получать габариты технических требований.

        ### Синтаксис Automation

        ```
        BlocksGabarits = Object.BlocksGabarits       # Получить свойство(* )
        Object.BlocksGabarits = BlocksGabarits       # Установить свойство (* )
        BlocksGabarits = Object.GetBlocksGabarits()  # Получить свойство (**)
        Object.SetBlocksGabarits( BlocksGabarits )   # Установить свойство (**)
        ```
        """
        ...
    @BlocksGabarits.setter
    def BlocksGabarits(self, value: list[float]): ...

    @property
    def BlocksStartLineNumbers(self) -> list[int]:  # itechnicaldemand_blocksstartlinenumbers.html
        """
        ## BlocksStartLineNumbers - Получить список номеров строк в начале блоков

        Тип данных: VARIANT типа VT_ARRAY | VT_I4

        1. Свойство позволяет получать список номеров строк в начале блоков технических требований.

        2. Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        BlocksStartLineNumbers = Object.BlocksStartLineNumbers       # Получить свойство(* )
        BlocksStartLineNumbers = Object.GetBlocksStartLineNumbers()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def IsCreated(self) -> bool:  # itechnicaldemand_iscreated.html
        """
        ## IsCreated - Признак отображения в документе

        Тип данных: BOOL

        1. Свойство позволяет получать признак отображения технических требований в документе.

        2. Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        IsCreated = Object.IsCreated       # Получить свойство(* )
        IsCreated = Object.GetIsCreated()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def Text(self) -> IText:  # itechnicaldemand_text.html
        """
        ## Text - Текст

        Тип данных: указатель на интерфейс текста IText

        1. Свойство позволяет получать интерфейс текста технических требований.

        2. Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        Text = Object.Text       # Получить свойство(* )
        Text = Object.GetText()  # Получить свойство (**)
        ```
        """
        ...

    def Delete(self) -> bool:  # itechnicaldemand_delete.html
        """
        ## Delete - Удалить объект

        Метод позволяет удалить технические требования.

        ### Синтаксис Automation

        ```
        BOOL Delete();
        ```

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def Synchronize(self) -> bool:  # itechnicaldemand_synchronize.html
        """
        ## Synchronize - Синхронизация

        ### Синтаксис Automation

        ```
        BOOL Synchronize();
        ```

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+

        Метод позволяет при создании ассоциативного вида синхронизировать технические требования чертежа с техническими требованиями документа 3D, с которого создается вид.
        """
        ...

    def Update(self) -> bool:  # itechnicaldemand_update.html
        """
        ## Update - Обновить данные

        Метод позволяет применить заданные параметры технических требований.

        ### Синтаксис Automation

        ```
        BOOL Update();
        ```

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...




class IMacroObjects(IDrawingObjects):  # imacroobjects.html
    """
    ## Интерфейс IMacroObjects

    Интерфейс коллекции макроэлементов.

    Позволяет создавать и получать макроэлементы.

    1. Получить интерфейс можно, используя метод контейнера графических объектов IDrawingContainer::MacroObjects.

    2. Посредством вызова метода IUnknown::QueryInterface (const GUID far& iid, void** pif) интерфейс позволяет получить дополнительный интерфейс IAnnotativeContainer.
    """

    def Add(self, MultiLayer) -> IMacroObject:  # imacroobjects_add.html
        """
        ## Add - Создать новый элемент и добавить его в коллекцию

        ### Синтаксис Automation

        ```
        LPDISPATCH Add();
        ```

        ### Возвращаемое значение

        - Указатель на интерфейс макроэлемента IMacroObject.
        """
        ...

    def MacroObject(self, Index) -> IMacroObject:  # imacroobjects_macroobject.html
        """
        ## MacroObject - Макроэлемент, заданный по индексу

        Тип данных: указатель на интерфейс макроэлемента IMacroObject

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        MacroObject = iObject.MacroObject ( Index );    # Получить свойство(* )
        MacroObject = iObject.GetMacroObject( Index );  # Получить свойство (**)
        ```
        """
        ...




class IMacroObject(IDrawingObject):  # imacroobject.html
    """
    ## Интерфейс IMacroObject

    Интерфейс нетипизированного макроэлемента.

    Интерфейс позволяет задавать параметры макроэлемента.

    1. Интерфейс можно получить, используя свойство коллекции макроэлементов IMacroObjects::MacroObject или метод IMacroObjects::Add.

    2. После задания параметров макроэлемента требуется вызвать метод IDrawingObject::Update.

    3. Посредством вызова метода IUnknown::QueryInterface (const GUID far& IID, void** pif) у данного интерфейса можно получить дополнительные интерфейсы:

    •интерфейс контейнера аннотационных объектов IAnnotativeContainer,

    •интерфейс контейнера объектов вида графического документа IDrawingContainer,

    •интерфейс контейнера объектов СПДС IBuildingContainer,

    •интерфейс контейнера условных обозначений ISymbols2DContainer,

    •интерфейс получения/редактирования значения свойств IPropertyKeeper.
    """

    @property
    def AttachedLeaders(self) -> ILeaders:  # imacroobject_attachedleaders.html
        """
        ## AttachedLeaders - Присоединенные линии выноски

        Тип данных: Указатель на интерфейс ILeaders

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        AttachedLeaders = Object.AttachedLeaders       # Получить свойство(* )
        AttachedLeaders = Object.GetAttachedLeaders()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def BreakObjectsEnabled(self) -> bool:  # imacroobject_breakobjectsenabled.html
        """
        ## BreakObjectsEnabled - Учитывать разрыв вида

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        BreakObjectsEnabled = Object.BreakObjectsEnabled       # Получить свойство(* )
        Object.BreakObjectsEnabled = BreakObjectsEnabled       # Установить свойство (* )
        BreakObjectsEnabled = Object.GetBreakObjectsEnabled()  # Получить свойство (**)
        Object.SetBreakObjectsEnabled( BreakObjectsEnabled )   # Установить свойство (**)
        ```
        """
        ...
    @BreakObjectsEnabled.setter
    def BreakObjectsEnabled(self, value: bool): ...

    @property
    def Command(self) -> int:  # imacroobject_command.html
        """
        ## Command - Номер команды редактирования

        Тип данных: long

        Свойство позволяет устанавливать и получать номер команды редактирования в меню библиотеки.

        ### Синтаксис Automation

        ```
        Command = Object.Command       # Получить свойство(* )
        Object.Command = Command       # Установить свойство (* )
        Command = Object.GetCommand()  # Получить свойство (**)
        Object.SetCommand( Command )   # Установить свойство (**)
        ```
        """
        ...
    @Command.setter
    def Command(self, value: int): ...

    @property
    def CreateSpcObjects(self) -> bool:  # imacroobject_createspcobjects.html
        """
        ## CreateSpcObjects - Создавать объекты спецификации

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        CreateSpcObjects = Object.CreateSpcObjects       # Получить свойство(* )
        Object.CreateSpcObjects = CreateSpcObjects       # Установить свойство (* )
        CreateSpcObjects = Object.GetCreateSpcObjects()  # Получить свойство (**)
        Object.SetCreateSpcObjects( CreateSpcObjects )   # Установить свойство (**)
        ```
        """
        ...
    @CreateSpcObjects.setter
    def CreateSpcObjects(self, value: bool): ...

    @property
    def DoubleClickEditable(self) -> bool:  # imacroobject_doubleclickeditable.html
        """
        ## DoubleClickEditable - Редактирование по двойному клику поддерживается

        Тип данных: BOOL

        Свойство позволяет устанавливать и получать признак редактирования по двойному щелчку мыши.

        ### Синтаксис Automation

        ```
        DoubleClickEditable = Object.DoubleClickEditable       # Получить свойство(* )
        Object.DoubleClickEditable = DoubleClickEditable       # Установить свойство (* )
        DoubleClickEditable = Object.GetDoubleClickEditable()  # Получить свойство (**)
        Object.SetDoubleClickEditable( DoubleClickEditable )   # Установить свойство (**)
        ```
        """
        ...
    @DoubleClickEditable.setter
    def DoubleClickEditable(self, value: bool): ...

    @property
    def ExternalEditable(self) -> bool:  # imacroobject_externaleditable.html
        """
        ## ExternalEditable - Поддерживается интерфейс внешнего управления

        Тип данных: BOOL

        Свойство позволяет устанавливать и получать признак поддержки интерфейса внешнего управления.

        ### Синтаксис Automation

        ```
        ExternalEditable = Object.ExternalEditable       # Получить свойство(* )
        Object.ExternalEditable = ExternalEditable       # Установить свойство (* )
        ExternalEditable = Object.GetExternalEditable()  # Получить свойство (**)
        Object.SetExternalEditable( ExternalEditable )   # Установить свойство (**)
        ```
        """
        ...
    @ExternalEditable.setter
    def ExternalEditable(self, value: bool): ...

    @property
    def HotPointsEditable(self) -> bool:  # imacroobject_hotpointseditable.html
        """
        ## HotPointsEditable - Редактирование при помощи характерных точек поддерживается

        Тип данных: BOOL

        Свойство позволяет устанавливать и получать признак редактирования при помощи характерных точек.

        ### Синтаксис Automation

        ```
        HotPointsEditable = Object.HotPointsEditable       # Получить свойство(* )
        Object.HotPointsEditable = HotPointsEditable       # Установить свойство (* )
        HotPointsEditable = Object.GetHotPointsEditable()  # Получить свойство (**)
        Object.SetHotPointsEditable( HotPointsEditable )   # Установить свойство (**)
        ```
        """
        ...
    @HotPointsEditable.setter
    def HotPointsEditable(self, value: bool): ...

    @property
    def Label(self) -> str:  # imacroobject_label.html
        """
        ## Label - Отображаемое имя объекта в дереве

        Тип данных: BSTR

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        Label = Object.Label       # Получить свойство (* )
        Label = Object.GetLabel()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def LibraryFileName(self) -> str:  # imacroobject_libraryfilename.html
        """
        ## LibraryFileName - Имя файла библиотеки, в которой находится функция редактирования

        Тип данных: BSTR

        Свойство позволяет устанавливать и получать имя файла библиотеки, в которой находится функция редактирования макроэлемента.

        ### Синтаксис Automation

        ```
        LibraryFileName = Object.LibraryFileName       # Получить свойство(* )
        Object.LibraryFileName = LibraryFileName       # Установить свойство (* )
        LibraryFileName = Object.GetLibraryFileName()  # Получить свойство (**)
        Object.SetLibraryFileName( LibraryFileName )   # Установить свойство (**)
        ```
        """
        ...
    @LibraryFileName.setter
    def LibraryFileName(self, value: str): ...

    @property
    def LibraryName(self) -> str:  # imacroobject_libraryname.html
        """
        ## LibraryName - Имя библиотеки, в которой находится функция редактирования

        Тип данных: BSTR

        Свойство позволяет устанавливать и получать имя библиотеки, в которой находится функция редактирования макроэлемента (отображаемое имя библиотеки).

        ### Синтаксис Automation

        ```
        LibraryName = Object.LibraryName       # Получить свойство(* )
        Object.LibraryName = LibraryName       # Установить свойство (* )
        LibraryName = Object.GetLibraryName()  # Получить свойство (**)
        Object.SetLibraryName( LibraryName )   # Установить свойство (**)
        ```
        """
        ...
    @LibraryName.setter
    def LibraryName(self, value: str): ...

    @property
    def Name(self):
        ...
    @Name.setter
    def Name(self, value): ...

    @property
    def PropertyObjectEditable(self) -> bool:  # imacroobject_propertyobjecteditable.html
        """
        ## PropertyObjectEditable - Поддерживается интерфейс внешних свойств объекта

        Тип данных: BOOL

        Свойство позволяет устанавливать и получать признак поддержки интерфейса внешних свойств объекта ILibPropertyObject.

        ### Синтаксис Automation

        ```
        PropertyObjectEditable = Object.PropertyObjectEditable       # Получить свойство(* )
        Object.PropertyObjectEditable = PropertyObjectEditable       # Установить свойство (* )
        PropertyObjectEditable = Object.GetPropertyObjectEditable()  # Получить свойство (**)
        Object.SetPropertyObjectEditable( PropertyObjectEditable )   # Установить свойство (**)
        ```
        """
        ...
    @PropertyObjectEditable.setter
    def PropertyObjectEditable(self, value: bool): ...

    @property
    def UserParams(self) -> typing.Any:  # imacroobject_userparams.html
        """
        ## UserParams - Массив SAFEARRAY байтов пользовательских параметров объекта

        Тип данных: VARIANT

        Свойство позволяет устанавливать и получать массив SAFEARRAY со структурой пользовательских параметров объекта.

        ### Синтаксис Automation

        ```
        HotPointsEditable = Object.HotPointsEditable       # Получить свойство(* )
        Object.HotPointsEditable = HotPointsEditable       # Установить свойство (* )
        HotPointsEditable = Object.GetHotPointsEditable()  # Получить свойство (**)
        Object.SetHotPointsEditable( HotPointsEditable )   # Установить свойство (**)
        ```
        """
        ...
    @UserParams.setter
    def UserParams(self, value: typing.Any): ...

    def AddDefaultHotPoint(self, X, Y) -> bool:  # imacroobject_adddefaulthotpoint.html
        """
        ## AddDefaultHotPoint - Задать характерную точку макроэлемента

        ### Синтаксис Automation

        ```
        BOOL AddDefaultHotPoint( double X, double Y );
        ```

        ### Входные параметры

        +----------------------------------------+
        | X, Y | - координаты характерной точки. |
        +----------------------------------------+

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def AddObjects(self, Objects) -> bool:  # imacroobject_addobjects.html
        """
        ## AddObjects - Добавить объект, слой, вид, группу или SAFEARRAY объектов в макрообъект

        Метод позволяет добавлять объект, слой, вид, группу или SAFEARRAY объектов в макроэлемент

        ### Синтаксис Automation

        ```
        BOOL AddObjects( VARIANT Objects );
        ```

        ### Входные параметры

        +---------------------------------------------------------------------+
        | Objects | - объект или массив объектов, добавляемых в макроэлемент. |
        +---------------------------------------------------------------------+

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def DeleteAttachedLeaders(self) -> bool:  # imacroobject_deleteattachedleaders.html
        """
        ## DeleteAttachedLeaders - Удалить присоединенные линии выноски

        ### Синтаксис Automation

        ```
        BOOL DeleteAttachedLeaders();
        ```

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def DeleteDefaultHotPoint(self) -> bool:  # imacroobject_deletedefaulthotpoint.html
        """
        ## DeleteDefaultHotPoint - Удалить характерную точку макроэлемента

        ### Синтаксис Automation

        ```
        BOOL DeleteDefaultHotPoint();
        ```

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def FindObject(self, X, Y, Limit, Param) -> IDrawingObject:  # imacroobject_findobject.html
        """
        ## FindObject - Найти объект, ближайший к заданной точке, удовлетворяющий параметрам поиска

        Для поиска объектов в точке могут быть заданы расширенные параметры поиска.

        Интерфейс расширенных параметров поиска IFindObjectParameters можно получить с помощью метода IKompasDocument1::GetInterface в 2D документах при использовании константы ksObjectFindObjectParameters.

        Если расширенные параметры поиска не требуются, необходимо передать NULL в функцию.

        ### Синтаксис Automation

        ```
        LPDISPATCH FindObject( double X, double Y, double Limit, IFindObjectParameters * Param );
        ```

        ### Входные параметры

        +-----------------------------------------------------------+
        | X     | - первая координата точки,                        |
        +-----------------------------------------------------------+
        | Y     | - вторая координата точки,                        |
        +-----------------------------------------------------------+
        | Limit | - предел поиска и найденное расстояние,           |
        +-----------------------------------------------------------+
        | Param | - параметры поиска объектов IFindObjectParameters |
        +-----------------------------------------------------------+

        ### Возвращаемое значение

        - Указатель на интерфейс IDrawingObject.
        """
        ...

    def GetPlacement(self) -> bool:  # imacroobject_getplacement.html
        """
        ## GetPlacement - Получить точку привязки и угол поворота системы координат макроэлемента

        Метод позволяет получить точку привязки и угол поворота макроэлемента.

        ### Синтаксис Automation

        ```
        BOOL GetPlacement( double * X,
        double * Y,
        double * Angle,
        BOOL * MirrorSymmetry );
        ```

        ### Входные параметры

        +----------------------------------------------------------+
        | X, Y           | - координаты точки привязки,            |
        +----------------------------------------------------------+
        | Angle          | - угол поворота макроэлемента,          |
        +----------------------------------------------------------+
        | MirrorSymmetry | - признак зеркальной симметрии объекта. |
        +----------------------------------------------------------+

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def SetPlacement(self, X, Y, Angle, MirrorSymmetry, Relative) -> bool:  # imacroobject_setplacement.html
        """
        ## SetPlacement - Установить точку привязки и угол поворота системы координат макроэлемента

        Метод позволяет установить точку привязки и угол поворота макроэлемента.

        Смотрите также:

        Рекомендации по обеспечению корректного редактирования библиотекой макроэлементов, геометрия которых зеркально отражена относительно исходного ее построения

        ### Синтаксис Automation

        ```
        BOOL SetPlacement( double X,
        double Y,
        double Angle,
        BOOL * MirrorSymmetry );
        ```

        ### Входные параметры

        +---------------------------------------------------------------------------------------+
        | X, Y           | - координаты точки привязки,                                         |
        +---------------------------------------------------------------------------------------+
        | Angle          | - угол поворота макроэлемента,                                       |
        +---------------------------------------------------------------------------------------+
        | MirrorSymmetry | - признак зеркальной симметрии объекта (NULL – не изменять признак). |
        +---------------------------------------------------------------------------------------+

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def TransformAngleFromView(self, Angle) -> bool:  # imacroobject_transformanglefromview.html
        """
        ## TransformAngleFromView - Перевод угла из CK листа в CK вида

        ### Синтаксис Automation

        ```
        BOOL TransformAngleFromView( double * Angle );
        ```

        ### Входные параметры

        +------------------------------------------+
        | Angle | - угол в системе координат вида. |
        +------------------------------------------+

        ### Выходные параметры

        +--------------------------------------------------+
        | Angle | - угол в системе координат макрообъекта. |
        +--------------------------------------------------+

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def TransformAngleToView(self, Angle) -> bool:  # imacroobject_transformangletoview.html
        """
        ## TransformAngleToView - Перевод угла из CK вида в CK листа

        ### Синтаксис Automation

        ```
        BOOL TransformAngleToView( double * Angle );
        ```

        ### Входные параметры

        +-------------------------------------------------+
        | Angle | - угол в системе координат марообъекта. |
        +-------------------------------------------------+

        ### Выходные параметры

        +------------------------------------------+
        | Angle | - угол в системе координат вида. |
        +------------------------------------------+

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def TransformPointFromView(self, X, Y) -> bool:  # imacroobject_transformpointfromview.html
        """
        ## TransformPointFromView - Перевод координат точки из CK вида в CK макроэлемента

        ### Синтаксис Automation

        ```
        BOOL TransformPointFromView( double * X, double * Y );
        ```

        ### Входные параметры

        +-----------------------------------------------------+
        | X, Y | - координаты точек в системе координат вида. |
        +-----------------------------------------------------+

        ### Выходные параметры

        +-------------------------------------------------------------+
        | X, Y | - координаты точек в системе координат макрообъекта. |
        +-------------------------------------------------------------+

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def TransformPointToView(self, X, Y) -> bool:  # imacroobject_transformpointtoview.html
        """
        ## TransformPointToView - Перевод координат точки из CK макроэлемента в CK вида

        ### Синтаксис Automation

        ```
        BOOL TransformPointToView( double * X, double * Y );
        ```

        ### Входные параметры

        +------------------------------------------------------------+
        | X, Y | - координаты точек в системе координат марообъекта. |
        +------------------------------------------------------------+

        ### Выходные параметры

        +-----------------------------------------------------+
        | X, Y | - координаты точек в системе координат вида. |
        +-----------------------------------------------------+

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...




class IAnnotativeContainer(IDispatch):  # iannotativecontainer.html
    """
    ## Интерфейс IAnnotativeContainer

    Интерфейс контейнера аннотационных объектов.

    Интерфейс позволяет создавать и получать аннотационные объекты.

    Интерфейс является дополнительным к интерфейсу макрообъекта IMacroObject. Данный интерфейс можно получить посредством вызова метода IUnknown::QueryInterface (const GUID far& IID, void** pif).
    """

    @property
    def Count(self) -> int:  # iannotativecontainer_count.html
        """
        ## Count - Количество объектов

        Тип данных: long

        1. Свойство позволяет получать количество объектов в контейнере.

        2. Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        Count = Object.Count       # Получить свойство(* )
        Count = Object.GetCount()  # Получить свойство (**)
        ```
        """
        ...

    def Add(self, Type) -> IAnnotativeObject:  # iannotativecontainer_add.html
        """
        ## Add - Добавить объект

        Метод позволяет добавить объект в контейнер.

        ### Синтаксис Automation

        ```
        LPDISPATCH Add( long type );
        ```

        ### Входные параметры

        +--------------------------------------------------------------------------+
        | type | - тип добавляемого объекта из перечисления DrawingObjectTypeEnum. |
        +--------------------------------------------------------------------------+

        ### Возвращаемое значение

        - указатель на интерфейс аннотационного объекта IAnnotativeObject.
        """
        ...

    def CreateByGeomObject(self, Obj) -> IAnnotativeObject:  # iannotativecontainer_createbygeomobject.html
        """
        ## CreateByGeomObject - Преобразовать геометрический объект в аннотационный

        Метод позволяет преобразовать геометрический объект в аннотационный и добавить его в контейнер.

        ### Синтаксис Automation

        ```
        LPDISPATCH CreateByGeomObject( LPDISPATCH obj );
        ```

        ### Входные параметры

        +------------------------------------------------------------------------+
        | obj | - указатель на интерфейс геометрического объекта IDrawingObject. |
        +------------------------------------------------------------------------+

        ### Возвращаемое значение

        - указатель на интерфейс аннотационного объекта IAnnotativeObject.
        """
        ...

    def Item(self, Index) -> IAnnotativeObject:  # iannotativecontainer_item.html
        """
        ## Item - Получить элемент по индексу

        Тип данных: указатель на интерфейс IAnnotativeObject

        1. Свойство позволяет получать аннотационные объекты.

        2. Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        Item = Object.Item( Index )     # Получить свойство(* )
        Item = Object.GetItem( Index )  # Получить свойство (**)
        ```

        ### Входные параметры

        +----------------------------+
        | Index | - Индекс элемента. |
        +----------------------------+
        """
        ...




class IAnnotativeObject(IDispatch):  # iannotativeobject.html
    """
    ## Интерфейс IAnnotativeObject

    Интерфейс аннотационных объектов.

    Интерфейс позволяет задать параметры аннотационного объекта.

    Интерфейс можно получить у контейнера аннотационных объектов с помощью свойства IAnnotativeContainer::Item и методов IAnnotativeContainer::Add и IAnnotativeContainer::CreateByGeomObject.
    """

    @property
    def Sign1(self) -> int:  # iannotativeobject_sign1.html
        """
        ## Sign1 - Тип значка

        Тип данных: из перечисления ksAnnotativeTerminatorSignEnum

        Свойство позволяет устанавливать и получать тип значка 1.

        ### Синтаксис Automation

        ```
        Sign1 = Object.Sign1       # Получить свойство(* )
        Object.Sign1 = Sign1       # Установить свойство (* )
        Sign1 = Object.GetSign1()  # Получить свойство (**)
        Object.SetSign1( Sign1 )   # Установить свойство (**)
        ```
        """
        ...
    @Sign1.setter
    def Sign1(self, value: int): ...

    @property
    def Sign2(self) -> int:  # iannotativeobject_sign2.html
        """
        ## Sign2 - Тип значка

        Тип данных: из перечисления ksAnnotativeTerminatorSignEnum.

        Свойство позволяет устанавливать и получать тип значка 2.

        ### Синтаксис Automation

        ```
        Sign2 = Object.Sign2       # Получить свойство(* )
        Object.Sign2 = Sign2       # Установить свойство (* )
        Sign2 = Object.GetSign2()  # Получить свойство (**)
        Object.SetSign2( Sign2 )   # Установить свойство (**)
        ```
        """
        ...
    @Sign2.setter
    def Sign2(self, value: int): ...




class IDimension2D(IDispatch):  # idimension2d.html
    """
    ## Интерфейс IDimension2D

    Интерфейс размера 2D.

    Интерфейс является дополнительным для размеров 2D.

    Версия: Компас v20
    """

    @property
    def AutoSorted(self) -> bool:  # idimension2d_autosorted.html
        """
        ## AutoSorted - Автосортировка

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        AutoSorted = Object.AutoSorted       # Получить свойство(* )
        Object.AutoSorted = AutoSorted       # Установить свойство (* )
        AutoSorted = Object.GetAutoSorted()  # Получить свойство (**)
        Object.SetAutoSorted( AutoSorted )   # Установить свойство (**)
        ```
        """
        ...
    @AutoSorted.setter
    def AutoSorted(self, value: bool): ...

    def GetDimensionPoint(self, DimensionPoint, PointIndex) -> bool:  # idimension2d_getdimensionpoint.html
        """
        ## GetDimensionPoint - Получить координаты точек размера

        ### Синтаксис Automation

        ```
        BOOL GetDimensionPoint( ksDimensionPointEnum DimensionPoint, long PointIndex, double * X, double * Y );
        ```

        ### Входные параметры

        +--------------------------------------------------------------------+
        | DimensionPoint | - тип точки из перечисления ksDimensionPointEnum, |
        +--------------------------------------------------------------------+
        | PointIndex     | - координаты точки.                               |
        +--------------------------------------------------------------------+

        ### Выходные параметры

        +---------------------------+
        | X,Y | - координаты точки. |
        +---------------------------+

        Версия Компас v21

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def GetTextPosition(self) -> bool:  # idimension2d_gettextposition.html
        """
        ## GetTextPosition - Получить положение размерной линии

        ### Синтаксис Automation

        ```
        BOOL GetTextPosition( double * X, double * Y );
        ```

        ### Выходные параметры

        +----------------------------+
        | X, Y | - координаты точки. |
        +----------------------------+

        ### Возвращаемое значение

        +-----------------------------+
        | TRUE  | - в случае удачи,   |
        +-----------------------------+
        | FALSE | - в случае неудачи. |
        +-----------------------------+
        """
        ...

    def SetTextPosition(self, X, Y) -> bool:  # idimension2d_settextposition.html
        """
        ## SetTextPosition - Установить положение размерной линии

        ### Синтаксис Automation

        ```
        BOOL SetTextPosition( double X, double Y );
        ```

        ### Входные параметры

        +----------------------------+
        | X, Y | - координаты точки. |
        +----------------------------+

        ### Возвращаемое значение

        +-----------------------------+
        | TRUE  | - в случае удачи,   |
        +-----------------------------+
        | FALSE | - в случае неудачи. |
        +-----------------------------+
        """
        ...




class ISymbols2DContainer(IDispatch):  # isymbols2dcontainer.html
    """
    ## Интерфейс ISymbols2DContainer

    Интерфейс контейнера условных обозначений.

    Позволяет получить коллекции 2D размеров и обозначений.

    Дополнительный интерфейс вида. Данный интерфейс можно получить у интерфейса вида IView посредством вызова метода IUnknown::QueryInterface (const GUID far& iid, void** pif).
    """

    @property
    def AngleDimensions(self) -> IAngleDimensions:  # isymbols2dcontainer_angledimensions.html
        """
        ## AngleDimensions - Угловые размеры

        Тип данных: IAngleDimensions.

        1. Свойство позволяет получать и создавать угловые и угловые с обрывом размеры.

        2. Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        AngleDimensions = Object.AngleDimensions       # Получить свойство(* )
        AngleDimensions = Object.GetAngleDimensions()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def ArcDimensions(self) -> IArcDimensions:  # isymbols2dcontainer_arcdimensions.html
        """
        ## ArcDimensions- Размеры дуг окружностей

        Тип данных: IArcDimensions

        1. Свойство позволяет получать и создавать размеры дуг окружностей.

        2. Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        ArcDimensions = Object.ArcDimensions       # Получить свойство(* )
        ArcDimensions = Object.GetArcDimensions()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def AssociationTables(self) -> IAssociationTables:  # isymbols2dcontainer_associationtables.html
        """
        ## AssociationTables - Ассоциативные таблицы отчетов

        Тип данных: IAssociationTables

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        AssociationTables = Object.AssociationTables       # Получить свойство(* )
        AssociationTables = Object.GetAssociationTables()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def AxisLines(self) -> IAxisLines:  # isymbols2dcontainer_axislines.html
        """
        ## AxisLines - Коллекция осевых линий

        Тип данных: указатель на интерфейс IAxisLines

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        AxisLines = Object.AxisLines       # Получить свойство(* )
        AxisLines = Object.GetAxisLines()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def Bases(self) -> IBases:  # isymbols2dcontainer_bases.html
        """
        ## Bases - Обозначения базы

        Тип данных: IBases

        1. Свойство позволяет получать и создавать обозначения базы.

        2. Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        Bases = Object.Bases       # Получить свойство(* )
        Bases = Object.GetBases()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def BreakLineDimensions(self) -> IBreakLineDimensions:  # isymbols2dcontainer_breaklinedimensions.html
        """
        ## BreakLineDimensions - Линейные размеры с обрывом

        Тип данных: IBreakLineDimensions

        1. Свойство позволяет получать и создавать линейные размеры с обрывом.

        2. Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        BreakLineDimensions = Object.BreakLineDimensions       # Получить свойство(* )
        BreakLineDimensions = Object.GetBreakLineDimensions()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def BreakRadialDimensions(self) -> IBreakRadialDimensions:  # isymbols2dcontainer_breakradialdimensions.html
        """
        ## BreakRadialDimensions - Радиальные размеры с изломом

        Тип данных: IBreakRadialDimensions

        1. Свойство позволяет получать и создавать радиальные размеры с изломом.

        2. Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        BreakRadialDimensions = Object.BreakRadialDimensions       # Получить свойство(* )
        BreakRadialDimensions = Object.GetBreakRadialDimensions()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def BrokenLines(self) -> IBrokenLines:  # isymbols2dcontainer_brokenlines.html
        """
        ## BrokenLines - Коллекция линий обрыва с изломами

        Тип данных: указатель на интерфейс IBrokenLines

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        BrokenLines = Object.BrokenLiness      # Получить свойство(* )
        BrokenLines = Object.GetBrokenLines()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def CentreMarkers(self) -> ICentreMarkers:  # isymbols2dcontainer_centremarkers.html
        """
        ## CentreMarkers - Коллекция обозначений центра

        Тип данных: указатель на интерфейс ICentreMarkers

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        CentreMarkers = Object.CentreMarkers       # Получить свойство(* )
        CentreMarkers = Object.GetCentreMarkers()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def CircularsCentries(self) -> ICircularsCentries:  # isymbols2dcontainer_circularscentries.html
        """
        ## CircularsCentries - Коллекция круговых сеток центров

        Тип данных: Указатель на интерфейс ICircularsCentries.

        Свойство доступно только для чтения.

        Версия Компас v18.1

        ### Синтаксис Automation

        ```
        CircularsCentries = Object.CircularsCentries       # Получить свойство(* )
        CircularsCentries = Object.GetCircularsCentries()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def ConditionIntersects(self) -> IConditionIntersects:  # isymbols2dcontainer_conditionintersects.html
        """
        ## ConditionIntersects - Коллекция условных пересечений

        Тип данных: Указатель на интерфейс IConditionIntersects

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        ConditionIntersects = Object.ConditionIntersects       # Получить свойство(* )
        ConditionIntersects = Object.GetConditionIntersects()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def CutLines(self) -> ICutLines:  # isymbols2dcontainer_cutlines.html
        """
        ## CutLines - Линии разреза/сечения

        Тип данных: ICutLines

        1. Свойство позволяет получать и создавать линии разреза/сечения.

        2. Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        CutLines = Object.CutLines       # Получить свойство(* )
        CutLines = Object.GetCutLines()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def DiametralDimensions(self) -> IDiametralDimensions:  # isymbols2dcontainer_diametraldimensions.html
        """
        ## DiametralDimensions - Диаметральные размеры

        Тип данных: IDiametralDimensions

        1. Свойство позволяет получать и создавать диаметральные размеры.

        2. Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        DiametralDimensions = Object.DiametralDimensions       # Получить свойство(* )
        DiametralDimensions = Object.GetDiametralDimensions()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def DrawingTables(self) -> IDrawingTables:  # isymbols2dcontainer_drawingtables.html
        """
        ## DrawingTables - Таблицы

        Тип данных: IDrawingTables

        1. Свойство позволяет получать и создавать таблицы.

        2. Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        DrawingTables = Object.DrawingTables       # Получить свойство(* )
        DrawingTables = Object.GetDrawingTables()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def HeightDimensions(self) -> IHeightDimensions:  # isymbols2dcontainer_heightdimensions.html
        """
        ## HeightDimensions - Размеры высоты

        Тип данных: IHeightDimensions

        1. Свойство позволяет получать и создавать размеры высоты.

        2. Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        HeightDimensions = Object.HeightDimensions       # Получить свойство(* )
        HeightDimensions = Object.GetHeightDimensions()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def Leaders(self) -> ILeaders:  # isymbols2dcontainer_leaders.html
        """
        ## Leaders - Линия-выноска

        Тип данных: ILeaders

        1. Свойство позволяет получать и создавать линии-выноски.

        2. Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        Leaders = Object.Leaders        # Получить свойство(* )
        Object.get_Leaders( &Leaders )  # Получить свойство (**)
        ```
        """
        ...

    @property
    def LineDimensions(self) -> ILineDimensions:  # isymbols2dcontainer_linedimensions.html
        """
        ## LineDimensions - Линейные размеры

        Тип данных: ILineDimensions

        1. Свойство позволяет получать и создавать линейные размеры.

        2. Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        LineDimensions = Object.LineDimensions       # Получить свойство(* )
        LineDimensions = Object.GetLineDimensions()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def LinearsCentries(self) -> ILinearsCentries:  # isymbols2dcontainer_linearscentries.html
        """
        ## LinearsCentries - Коллекция линейных сеток центров

        Тип данных: Указатель на интерфейс ILinearsCentries.

        Свойство доступно только для чтения.

        Версия Компас v18.1

        ### Синтаксис Automation

        ```
        LinearsCentries = Object.LinearsCentries       # Получить свойство(* )
        LinearsCentries = Object.GetLinearsCentries()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def RadialDimensions(self) -> IRadialDimensions:  # isymbols2dcontainer_radialdimensions.html
        """
        ## RadialDimensions - Радиальные размеры

        Тип данных: IRadialDimensions

        1. Свойство позволяет получать и создавать радиальные размеры.

        2. Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        RadialDimensions = Object.RadialDimensions       # Получить свойство(* )
        RadialDimensions = Object.GetRadialDimensions()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def RemoteElements(self) -> IRemoteElements:  # isymbols2dcontainer_remoteelements.html
        """
        ## RemoteElements - Коллекция выносных элементов

        Тип данных: указатель на интерфейс IRemoteElements

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        RemoteElements = Object.RemoteElements       # Получить свойство(* )
        RemoteElements = Object.GetRemoteElements()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def Roughs(self) -> IRoughs:  # isymbols2dcontainer_roughs.html
        """
        ## Roughs - Обозначения шероховатости

        Тип данных: IRoughs

        1. Свойство позволяет получать и создавать обозначения шероховатости.

        2. Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        Roughs = Object.Roughs       # Получить свойство(* )
        Roughs = Object.GetRoughs()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def Tolerances(self) -> ITolerances:  # isymbols2dcontainer_tolerances.html
        """
        ## Tolerances - Допуск формы

        Тип данных: ITolerances

        1. Свойство позволяет получать и создавать допуски формы.

        2. Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        Tolerances = Object.Tolerances       # Получить свойство(* )
        Tolerances = Object.GetTolerances()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def ViewPointers(self) -> IViewPointers:  # isymbols2dcontainer_viewpointers.html
        """
        ## ViewPointers - Стрелки взгляда

        Тип данных: IViewPointers

        1. Свойство позволяет получать и создавать линии стрелки взгляда.

        2. Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        ViewPointers = Object.ViewPointers       # Получить свойство(* )
        ViewPointers = Object.GetViewPointers()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def WaveLines(self) -> IWaveLines:  # isymbols2dcontainer_wavelines.html
        """
        ## WaveLines - Коллекция волнистых линий

        Тип данных: указатель на интерфейс IWaveLines

        1. Свойство позволяет получать интерфейс коллекции волнистых линий.

        2. Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        WaveLines = Object.WaveLines       # Получить свойство(* )
        WaveLines = Object.GetWaveLines()  # Получить свойство (**)
        ```
        """
        ...




class IBranchs(IDispatch):  # ibranchs.html
    """
    ## Интерфейс IBranchs

    Интерфейс для работы с ответвлениями.

    Интерфейс можно получить с помощью метода IUnknown::QueryInterface у интерфейсов IBaseLeader, ITolerance, IMarkLeader, IPositionLeader, IChangeLeader, IBrandLeader, IRough.
    """

    @property
    def BranchCount(self) -> int:  # ibranchs_branchcount.html
        """
        ## BranchCount - Число ответвлений

        Тип данных: long

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        BranchCount = Object.BranchCount       # Получить свойство(* )
        BranchCount = Object.GetBranchCount()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def X0(self) -> float:  # ibranchs_x0.html
        """
        ## X0 - Координата начала полки или точка привязки по X

        Тип данных: double

        Значения координат зависят от текущей системы координат для объекта, задаваемой параметром IDrawingObject::DrawingObjectParamType.

        ### Синтаксис Automation

        ```
        X0 = Object.X0       # Получить свойство(* )
        Object.X0 = X0       # Установить свойство (* )
        X0 = Object.GetX0()  # Получить свойство (**)
        Object.SetX0( X0 )   # Установить свойство (**)
        ```
        """
        ...
    @X0.setter
    def X0(self, value: float): ...

    @property
    def Y0(self) -> float:  # ibranchs_y0.html
        """
        ## Y0 - Координата начала полки или точка привязки по Y

        Тип данных: double

        Значения координат зависят от текущей системы координат для объекта, задаваемой параметром IDrawingObject::DrawingObjectParamType.

        ### Синтаксис Automation

        ```
        Y0 = Object.Y0       # Получить свойство(* )
        Object.Y0 = Y0       # Установить свойство (* )
        Y0 = Object.GetY0()  # Получить свойство (**)
        Object.SetY0( Y0 )   # Установить свойство (**)
        ```
        """
        ...
    @Y0.setter
    def Y0(self, value: float): ...

    def AddBranch(self, Index, Points) -> bool:  # ibranchs_addbranch.html
        """
        ## AddBranch - Добавить ответвление

        1. Координаты точек в массиве лежат в следующей последовательности: x0, y0, x1, y1, ...xi, yi.

        2. Значения координат зависят от текущей системы координат для объекта, задаваемой параметром IDrawingObject::DrawingObjectParamType.

        ### Синтаксис Automation

        ```
        BOOL AddBranch( long Index,
        VARIANT Points );
        ```

        ### Входные параметры

        +-------------------------------------------------------+
        | Index  | - индекс, с которым добавляется ответвление, |
        +-------------------------------------------------------+
        | Points | - массив SafeArray координат точек.          |
        +-------------------------------------------------------+

        ### Возвращаемое значение

        +---------------------------------+
        | TRUE | - ответвление добавлено. |
        +---------------------------------+
        """
        ...

    def AddBranchByPoint(self, Index, X, Y) -> bool:  # ibranchs_addbranchbypoint.html
        """
        ## AddBranchByPoint - Добавить прямолинейное ответвление

        Значения координат зависят от текущей системы координат для объекта, задаваемой параметром IDrawingObject::DrawingObjectParamType.

        ### Синтаксис Automation

        ```
        BOOL AddBranchByPoint( long Index,
        double X,
        double Y );
        ```

        ### Входные параметры

        +------------------------------------------------------+
        | Index | - индекс, с которым добавляется ответвление, |
        +------------------------------------------------------+
        | x, y  | - координаты точки целеуказания.             |
        +------------------------------------------------------+

        ### Возвращаемое значение

        +---------------------------------+
        | TRUE | - ответвление добавлено. |
        +---------------------------------+
        """
        ...

    def BranchPoints(self, Index) -> list[float]:  # ibranchs_branchpoints.html
        """
        ## BranchPoints - Массив SAFEARRAY координат точек ответвления

        Тип данных: VARIANT типа VT_ARRAY | VT_R8

        1. Координаты точек в массиве лежат в следующей последовательности: x0, y0, x1, y1, ...xi, yi.

        2. Значения координат зависят от текущей системы координат для объекта, задаваемой параметром IDrawingObject::DrawingObjectParamType.

        ### Синтаксис Automation

        ```
        BranchPoints = Object.BranchPoints( Index )     # Получить свойство(* )
        Object.BranchPoints( Index ) = BranchPoints     # Установить свойство (* )
        BranchPoints = Object.GetBranchPoints( Index )  # Получить свойство (**)
        Object.SetBranchPoints( Index, BranchPoints )   # Установить свойство (**)
        ```
        """
        ...

    def BranchPointsCount(self, Index) -> int:  # ibranchs_branchpointscount.html
        """
        ## BranchPointsCount - Число ответвлений

        Тип данных: long

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        BranchPointsCount = Object.BranchPointsCount()     # Получить свойство(* )
        BranchPointsCount = Object.GetBranchPointsCount()  # Получить свойство (**)
        ```
        """
        ...

    def BranchX(self, Index):
        ...

    def BranchY(self, Index):
        ...

    def DeleteBranch(self, Index) -> bool:  # ibranchs_deletebranch.html
        """
        ## DeleteBranch - Удалить ответвление

        ### Синтаксис Automation

        ```
        BOOL DeleteBranch( long Index );
        ```

        ### Входные параметры

        +------------------------------------------------------+
        | Index | - индекс, с которым добавляется ответвление. |
        +------------------------------------------------------+

        ### Возвращаемое значение

        +-------------------------------+
        | TRUE | - ответвление удалено. |
        +-------------------------------+
        """
        ...

    def GetBaseObject(self, Index) -> IDrawingObject:  # ibranchs_getbaseobject.html
        """
        ## GetBaseObject - Получить базовый объект выноски

        ### Синтаксис Automation

        ```
        BOOL DeleteBranch( long Index );
        ```

        ### Входные параметры

        +-------------------------------+
        | Index | - индекс ответвления. |
        +-------------------------------+

        Версия КОМПАС v22

        ### Возвращаемое значение

        - Указатель на интерфейс IDrawingObject.
        """
        ...

    def SetBaseObject(self, Index, NewObject, SegmentIndex) -> bool:  # ibranchs_setbaseobject.html
        """
        ## SetBaseObject - Установить базовый объект выноски

        ### Синтаксис Automation

        ```
        BOOL SetBaseObject( long Index, IDrawingObject * NewObject, long SegmentIndex );
        ```

        ### Входные параметры

        +-------------------------------------------------------------+
        | Index        | - индекс, с которым добавляется ответвление. |
        +-------------------------------------------------------------+
        | NewObject    | - указатель на интерфейс объекта,            |
        +-------------------------------------------------------------+
        | SegmentIndex | - индекс сегмента.                           |
        +-------------------------------------------------------------+

        Версия КОМПАС v22

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def SetBranchPoints(self, Index, arg1):
        ...

    def SetBranchX(self, Index, arg1):
        ...

    def SetBranchY(self, Index, arg1):
        ...




class IBrandLeader(IDispatch):  # ibrandleader.html
    """
    ## Интерфейс IBrandLeader

    Интерфейс параметров обозначения клеймения.

    1. Интерфейс можно получить с помощью метода IUnknown::QueryInterface у интерфейсов IBaseLeader или IBaseLeader3D.

    2. С помощью метода IUnknown::QueryInterface у интерфейса можно получить интерфейс ответвлений IBranchs или IBranchs3D.
    """

    @property
    def Designation(self) -> IText:  # ibrandleader_designation.html
        """
        ## Designation - Обозначение

        Тип данных: указатель на интерфейс IText

        1. Свойство позволяет получить интерфейс текста обозначения.

        2. Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        Designation = Object.Designation       # Получить свойство(* )
        Designation = Object.GetDesignation()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def Direction(self) -> bool:  # ibrandleader_direction.html
        """
        ## Direction - Направление

        Тип данных: BOOL

        Значения свойства:

        +------------------------+
        | FALSE | - знак влево,  |
        +------------------------+
        | TRUE  | - знак вправо. |
        +------------------------+

        Свойство позволяет устанавливать и получать направление знака.

        ### Синтаксис Automation

        ```
        Direction = Object.Direction        # Получить свойство(* )
        Object.Direction = Direction        # Установить свойство (* )
        Direction = Object.GetDirection( )  # Получить свойство (**)
        Object.SetDirection( Direction )    # Установить свойство (**)
        ```
        """
        ...
    @Direction.setter
    def Direction(self, value: bool): ...

    @property
    def TextOnBranch(self) -> IText:  # ibrandleader_textonbranch.html
        """
        ## TextOnBranch - Текст над ножкой

        Тип данных: указатель на интерфейс IText

        1. Свойство позволяет получить интерфейс текста над ножкой.

        2. Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        TextOnBranch = Object.TextOnBranch       # Получить свойство(* )
        TextOnBranch = Object.GetTextOnBranch()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def TextUnderBranch(self) -> IText:  # ibrandleader_textunderbranch.html
        """
        ## TextUnderBranch - Текст под ножкой

        Тип данных: указатель на интерфейс IText

        1. Свойство позволяет получить интерфейс текста под ножкой.

        2. Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        TextUnderBranch = Object.TextUnderBranch       # Получить свойство(* )
        TextUnderBranch = Object.GetTextUnderBranch()  # Получить свойство (**)
        ```
        """
        ...




class IChangeLeader(IDispatch):  # ichangeleader.html
    """
    ## Интерфейс IChangeLeader

    Интерфейс параметров знака изменения.

    1. Дополнительный интерфейс для IBaseLeader.

    2. Интерфейс можно получить посредством вызова метода IUnknown::QueryInterface (const GUID far& iid, void** pif).

    3. С помощью метода IUnknown::QueryInterface у интерфейса можно получить интерфейс ответвлений IBranchs или IBranchs3D.
    """

    @property
    def Designation(self) -> IText:  # ichangeleader_designation.html
        """
        ## Designation - Обозначение

        Тип данных: указатель на интерфейс IText

        1. Свойство позволяет получить интерфейс текста обозначения.

        2. Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        Designation = Object.Designation       # Получить свойство(* )
        Designation = Object.GetDesignation()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def FullLeaderLength(self) -> bool:  # ichangeleader_fullleaderlength.html
        """
        ## FullLeaderLength - На всю длину

        Тип данных: BOOL

        Значения свойства:

        +---------------------------------+
        | TRUE  | - выноска полной длины, |
        +---------------------------------+
        | FALSE | - ограниченный отрезок. |
        +---------------------------------+

        Свойство позволяет устанавливать и получать тип выноски: выноска полной длины или ограниченный отрезок.

        ### Синтаксис Automation

        ```
        FullLeaderLength = Object.FullLeaderLength        # Получить свойство(* )
        Object.FullLeaderLength = FullLeaderLength        # Установить свойство (* )
        FullLeaderLength = Object.GetFullLeaderLength( )  # Получить свойство (**)
        Object.SetFullLeaderLength( FullLeaderLength )    # Установить свойство (**)
        ```
        """
        ...
    @FullLeaderLength.setter
    def FullLeaderLength(self, value: bool): ...

    @property
    def LeaderLength(self) -> float:  # ichangeleader_leaderlength.html
        """
        ## LeaderLength - Длина выноски

        Тип данных: double

        Свойство позволяет устанавливать и получать длину выноски.

        ### Синтаксис Automation

        ```
        LeaderLength = Object.LeaderLength        # Получить свойство(* )
        Object.LeaderLength = LeaderLength        # Установить свойство (* )
        LeaderLength = Object.GetLeaderLength( )  # Получить свойство (**)
        Object.SetLeaderLength( LeaderLength )    # Установить свойство (**)
        ```
        """
        ...
    @LeaderLength.setter
    def LeaderLength(self, value: float): ...

    @property
    def NumericGroup(self) -> INumericGroup:  # ichangeleader_numericgroup.html
        """
        ## NumericGroup - Группа нумерации

        Тип данных: Указатель на интерфейс INumericGroup

        ### Синтаксис Automation

        ```
        NumericGroup = Object.NumericGroup       # Получить свойство(* )
        Object.NumericGroup = NumericGroup       # Установить свойство (* )
        NumericGroup = Object.GetNumericGroup()  # Получить свойство (**)
        Object.SetNumericGroup( NumericGroup )   # Установить свойство (**)
        ```
        """
        ...
    @NumericGroup.setter
    def NumericGroup(self, value: INumericGroup): ...

    @property
    def SignHeight(self) -> float:  # ichangeleader_signheight.html
        """
        ## SignHeight - Высота знака

        Тип данных: double

        Свойство позволяет устанавливать и получать высоту знака.

        ### Синтаксис Automation

        ```
        SignHeight = Object.SignHeight        # Получить свойство(* )
        Object.SignHeight = SignHeight        # Установить свойство (* )
        SignHeight = Object.GetSignHeight( )  # Получить свойство (**)
        Object.SetSignHeight( SignHeight )    # Установить свойство (**)
        ```
        """
        ...
    @SignHeight.setter
    def SignHeight(self, value: float): ...

    @property
    def SignType(self) -> int:  # ichangeleader_signtype.html
        """
        ## SignType - Тип значка

        Тип данных: из перечисления ksChangeLeaderSignEnum

        Свойство позволяет устанавливать и получать тип значка.

        ### Синтаксис Automation

        ```
        SignType = Object.SignType        # Получить свойство(* )
        Object.SignType = SignType        # Установить свойство (* )
        SignType = Object.GetSignType( )  # Получить свойство (**)
        Object.SetSignType( SignType )    # Установить свойство (**)
        ```
        """
        ...
    @SignType.setter
    def SignType(self, value: int): ...




class ILeader(IDispatch):  # ileader.html
    """
    ## Интерфейс ILeader

    Интерфейс параметров линии-выноски.

    1. Дополнительный интерфейс для IBaseLeader и IBaseLeader3D.

    2. Интерфейс можно получить посредством вызова метода IUnknown::QueryInterface (const GUID far& iid, void** pif).
    """

    @property
    def Arround(self) -> bool:  # ileader_arround.html
        """
        ## Arround - Признак обработки по контуру

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        Arround = Object.Arround       # Получить свойство(* )
        Object.Arround = Arround       # Установить свойство (* )
        Arround = Object.GetArround()  # Получить свойство (**)
        Object.SetArround( Arround )   # Установить свойство (**)
        ```
        """
        ...
    @Arround.setter
    def Arround(self, value: bool): ...

    @property
    def AutoSorted(self) -> bool:  # ileader_autosorted.html
        """
        ## AutoSorted - Автосортировка

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        AutoSorted = Object.AutoSorted       # Получить свойство(* )
        Object.AutoSorted = AutoSorted       # Установить свойство (* )
        AutoSorted = Object.GetAutoSorted()  # Получить свойство (**)
        Object.SetAutoSorted( AutoSorted )   # Установить свойство (**)
        ```
        """
        ...
    @AutoSorted.setter
    def AutoSorted(self, value: bool): ...

    @property
    def ParallelBranch(self) -> bool:  # ileader_parallelbranch.html
        """
        ## ParallelBranch - Параллельные ответвления

        Тип данных: BOOL

        Значение свойства:

        +----------------------------+
        | TRUE  | - от начала полки, |
        +----------------------------+
        | FALSE | - от конца полки.  |
        +----------------------------+

        ### Синтаксис Automation

        ```
        ParallelBranch = Object.ParallelBranch       # Получить свойство(* )
        Object.ParallelBranch = ParallelBranch       # Установить свойство (* )
        ParallelBranch = Object.GetParallelBranch()  # Получить свойство (**)
        Object.SetParallelBranch( ParallelBranch )   # Установить свойство (**)
        ```

        ### Входные параметры

        +-------------------------------+
        | Index | - индекс ответвления. |
        +-------------------------------+
        """
        ...
    @ParallelBranch.setter
    def ParallelBranch(self, value: bool): ...

    @property
    def ShelfDirection(self) -> int:  # ileader_shelfdirection.html
        """
        ## ShelfDirection - Направление полки

        Тип данных: из перечисления ksShelfDirectionEnum

        ### Синтаксис Automation

        ```
        ShelfDirection = Object.ShelfDirection       # Получить свойство(* )
        Object.ShelfDirection = ShelfDirection       # Установить свойство (* )
        ShelfDirection = Object.GetShelfDirection()  # Получить свойство (**)
        Object.SetShelfDirection( ShelfDirection )   # Установить свойство (**)
        ```
        """
        ...
    @ShelfDirection.setter
    def ShelfDirection(self, value: int): ...

    @property
    def SignType(self) -> int:  # ileader_signtype.html
        """
        ## SignType - Тип значка

        Тип данных: из перечисления ksLeaderSignEnum

        Свойство позволяет выбрать значок для обозначения соединения.

        ### Синтаксис Automation

        ```
        SignType = Object.SignType       # Получить свойство(* )
        Object.SignType = SignType       # Установить свойство (* )
        SignType = Object.GetSignType()  # Получить свойство (**)
        Object.SetSignType( SignType )   # Установить свойство (**)
        ```
        """
        ...
    @SignType.setter
    def SignType(self, value: int): ...

    @property
    def TextAfterShelf(self) -> IText:  # ileader_textaftershelf.html
        """
        ## TextAfterShelf - Текст за полкой

        Тип данных: указатель на интерфейс IText

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        TextAfterShelf = Object.TextAfterShelf       # Получить свойство(* )
        TextAfterShelf = Object.GetTextAfterShelf()  # Получить свойство (**)
        ```

        ### Входные параметры

        +-------------------------------+
        | Index | - индекс ответвления. |
        +-------------------------------+
        """
        ...

    @property
    def TextOnBranch(self) -> IText:  # ileader_textonbranch.html
        """
        ## TextOnBranch - Текст над ножкой

        Тип данных: указатель на интерфейс IText

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        TextOnBranch = Object.TextOnBranch       # Получить свойство(* )
        TextOnBranch = Object.GetTextOnBranch()  # Получить свойство (**)
        ```

        ### Входные параметры

        +-------------------------------+
        | Index | - индекс ответвления. |
        +-------------------------------+
        """
        ...

    @property
    def TextOnShelf(self) -> IText:  # ileader_textonshelf.html
        """
        ## TextOnShelf - Текст над полкой

        Тип данных: указатель на интерфейс IText

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        TextOnShelf = Object.TextOnShelf       # Получить свойство(* )
        TextOnShelf = Object.GetTextOnShelf()  # Получить свойство (**)
        ```

        ### Входные параметры

        +-------------------------------+
        | Index | - индекс ответвления. |
        +-------------------------------+
        """
        ...

    @property
    def TextUnderBranch(self) -> IText:  # ileader_textunderbranch.html
        """
        ## TextUnderBranch - Текст под ножкой

        Тип данных: указатель на интерфейс IText

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        TextUnderBranch = Object.TextUnderBranch       # Получить свойство(* )
        TextUnderBranch = Object.GetTextUnderBranch()  # Получить свойство (**)
        ```

        ### Входные параметры

        +-------------------------------+
        | Index | - индекс ответвления. |
        +-------------------------------+
        """
        ...

    @property
    def TextUnderShelf(self) -> IText:  # ileader_textundershelf.html
        """
        ## TextUnderShelf - Текст под полкой

        Тип данных: указатель на интерфейс IText

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        TextUnderShelf = Object.TextUnderShelf       # Получить свойство(* )
        TextUnderShelf = Object.GetTextUnderShelf()  # Получить свойство (**)
        ```

        ### Входные параметры

        +-------------------------------+
        | Index | - индекс ответвления. |
        +-------------------------------+
        """
        ...

    def BranchBegin(self, Index) -> bool:  # ileader_branchbegin.html
        """
        ## BranchBegin - Начало ответвления

        Тип данных: BOOL

        Значение свойства:

        +----------------------------+
        | TRUE  | - от начала полки, |
        +----------------------------+
        | FALSE | - от конца полки.  |
        +----------------------------+

        ### Синтаксис Automation

        ```
        BranchBegin = Object.BranchBegin( Index )     # Получить свойство(* )
        Object.BranchBegin( Index ) = BranchBegin     # Установить свойство (* )
        BranchBegin = Object.GetBranchBegin( Index )  # Получить свойство (**)
        Object.SetBranchBegin( Index, BranchBegin )   # Установить свойство (**)
        ```

        ### Входные параметры

        +-------------------------------+
        | Index | - индекс ответвления. |
        +-------------------------------+
        """
        ...

    def GetSignPosition(self, BranchIndex) -> bool:  # ileader_getsignposition.html
        """
        ## GetSignPosition - Получить координаты точки привязки знака на линии выноски

        ### Синтаксис Automation

        ```
        BOOL GetSignPosition( long BranchIndex, double * X, double * Y );
        ```

        ### Входные параметры

        +-------------------------------------+
        | BranchIndex | - индекс ответвления. |
        +-------------------------------------+

        ### Выходные параметры

        +-------------------------------------+
        | X, Y | - координаты точки привязки. |
        +-------------------------------------+

        Версия КОМПАС v22

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def SetBranchBegin(self, Index, arg1):
        ...

    def SetSignPosition(self, BranchIndex, X, Y) -> bool:  # ileader_setsignposition.html
        """
        ## SetSignPosition - Установить координаты точки привязки знака на линии выноски

        ### Синтаксис Automation

        ```
        BOOL SetSignPosition( long BranchIndex, double X, double Y );
        ```

        ### Входные параметры

        +------------------------------------------------------------+
        | BranchIndex | - индекс, с которым добавляется ответвление, |
        +------------------------------------------------------------+
        | X, Y        | - координаты точки привязки.                 |
        +------------------------------------------------------------+

        Версия КОМПАС v22

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...




class IPositionLeader(IDispatch):  # ipositionleader.html
    """
    ## Интерфейс IPositionLeader

    Интерфейс параметров линии-выноски для обозначения позиции.

    1. Дополнительный интерфейс для IBaseLeader и IBaseLeader3D.

    2. Интерфейс можно получить посредством вызова метода IUnknown::QueryInterface (const GUID far& iid, void** pif).

    3. С помощью метода IUnknown::QueryInterface у интерфейса можно получить интерфейс ответвлений IBranchs или IBranchs3D.
    """

    @property
    def Form(self) -> int:  # ipositionleader_form.html
        """
        ## Form - Тип формы

        Тип данных: из перечисления ksPositionLederFormEnum

        Свойство позволяет устанавливать и получать тип формы.

        ### Синтаксис Automation

        ```
        Form = Object.Form        # Получить свойство(* )
        Object.Form = Form        # Установить свойство (* )
        Form = Object.GetForm( )  # Получить свойство (**)
        Object.SetForm( Form )    # Установить свойство (**)
        ```
        """
        ...
    @Form.setter
    def Form(self, value: int): ...

    @property
    def Horizontally(self) -> bool:  # ipositionleader_horizontally.html
        """
        ## Horizontally - Горизонтальное расположение позиций

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        Horizontally = Object.Horizontally        # Получить свойство(* )
        Object.Horizontally = Horizontally        # Установить свойство (* )
        Horizontally = Object.GetHorizontally( )  # Получить свойство (**)
        Object.SetHorizontally( Horizontally )    # Установить свойство (**)
        ```
        """
        ...
    @Horizontally.setter
    def Horizontally(self, value: bool): ...

    @property
    def Positions(self) -> IText:  # ipositionleader_positions.html
        """
        ## Positions - Позиции

        Тип данных: указатель на интерфейс IText.

        1. Свойство позволяет получить интерфейс текста позиций.

        2. Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        Positions = Object.Positions       # Получить свойство(* )
        Positions = Object.GetPositions()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def ShelfDirection(self) -> int:  # ipositionleader_shelfdirection.html
        """
        ## ShelfDirection - Направление полки

        Тип данных: из перечисления ksShelfDirectionEnum

        Свойство позволяет устанавливать и получать направление полки.

        ### Синтаксис Automation

        ```
        ShelfDirection = Object.ShelfDirection       # Получить свойство(* )
        Object.ShelfDirection = ShelfDirection       # Установить свойство (* )
        ShelfDirection = Object.GetShelfDirection()  # Получить свойство (**)
        Object.SetShelfDirection( ShelfDirection )   # Установить свойство (**)
        ```
        """
        ...
    @ShelfDirection.setter
    def ShelfDirection(self, value: int): ...

    @property
    def ShelfVisible(self) -> bool:  # ipositionleader_shelfvisible.html
        """
        ## ShelfVisible - Отрисовка полки

        Тип данных: BOOL

        Свойство позволяет устанавливать и получать признак отрисовки полки.

        ### Синтаксис Automation

        ```
        ShelfVisible = Object.ShelfVisible        # Получить свойство(* )
        Object.ShelfVisible = ShelfVisible        # Установить свойство (* )
        ShelfVisible = Object.GetShelfVisible( )  # Получить свойство (**)
        Object.SetShelfVisible( ShelfVisible )    # Установить свойство (**)
        ```
        """
        ...
    @ShelfVisible.setter
    def ShelfVisible(self, value: bool): ...

    @property
    def TextDirection(self) -> bool:  # ipositionleader_textdirection.html
        """
        ## TextDirection - Направление текста

        Тип данных: BOOL

        Значения свойства:

        +------------------+
        | TRUE  | - вверх, |
        +------------------+
        | FALSE | - вниз.  |
        +------------------+

        Свойство позволяет устанавливать и получать направление текста.

        ### Синтаксис Automation

        ```
        TextDirection = Object.TextDirection        # Получить свойство(* )
        Object.TextDirection = TextDirection        # Установить свойство (* )
        TextDirection = Object.GetTextDirection( )  # Получить свойство (**)
        Object.SetTextDirection( TextDirection )    # Установить свойство (**)
        ```
        """
        ...
    @TextDirection.setter
    def TextDirection(self, value: bool): ...

    @property
    def UnderPositionText(self) -> IText:  # ipositionleader_underpositiontext.html
        """
        ## UnderPositionText - Текст под позицией

        Тип данных: указатель на интерфейс IText

        1. Свойство позволяет получить интерфейс текста под позицией.

        2. Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        UnderPositionText = Object.UnderPositionText       # Получить свойство(* )
        UnderPositionText = Object.GetUnderPositionText()  # Получить свойство (**)
        ```
        """
        ...




class IDimensionText(IDispatch):  # idimensiontext.html
    """
    ## Интерфейс IDimensionText

    Интерфейс параметров текста размерной надписи.

    Является дополнительным интерфейсом для 2D и 3D размеров (интерфейсы IAngleDimension3D, IAngleDimension, IArcDimension, IBaseLineDimension3D, IBreakAngleDimension, IBreakLineDimension, IBreakRadialDimension, IDiametralDimension3D, IDiametralDimension, IHeightDimension, ILineDimension3D, ILineDimension, IRadialDimension3D, IRadialDimension). Позволяет сформировать нужный текст размерной надписи.
    """

    @property
    def Accuracy(self) -> int:  # idimensiontext_accuracy.html
        """
        ## Accuracy - Количество знаков после запятой

        Тип данных: из перечисления ksAccuracyEnum

        ### Синтаксис Automation

        ```
        Accuracy = Object.Accuracy       # Получить свойство(* )
        Object.Accuracy = Accuracy       # Установить свойство (* )
        Accuracy = Object.GetAccuracy()  # Получить свойство (**)
        Object.SetAccuracy( Accuracy )   # Установить свойство (**)
        ```
        """
        ...
    @Accuracy.setter
    def Accuracy(self, value: int): ...

    @property
    def AccuracyDecimalsCount(self) -> int:  # idimensiontext_accuracydecimalscount.html
        """
        ## AccuracyDecimalsCount - Количество знаков после запятой, используемое при округлении размера

        Тип данных: long

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        AccuracyDecimalsCount = Object. AccuracyDecimalsCount       # Получить свойство(* )
        AccuracyDecimalsCount = Object.Get AccuracyDecimalsCount()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def AutoNominalValue(self) -> bool:  # idimensiontext_autonominalvalue.html
        """
        ## AutoNominalValue - Автоматическое определение номинального значения размера

        Тип данных: BOOL

        По умолчанию признак включен и задает автоматическое вычисление значения размера. Если признак снят, то значение вводили вручную. При необходимости можно вернуть автоматическое вычисление значения размера. Для этого нужно установить флаг в TRUE. При этом отображаемое значение изменится. Точность отображения автоматически вычисленного значения можно задать в диалоге настройки точности размерных надписей.

        Если значение размера введено вручную, то настройка точности на него не влияет.

        ### Синтаксис Automation

        ```
        AutoNominalValue = Object.AutoNominalValue       # Получить свойство(* )
        Object.AutoNominalValue = AutoNominalValue       # Установить свойство (* )
        AutoNominalValue = Object.GetAutoNominalValue()  # Получить свойство (**)
        Object.SetAutoNominalValue( AutoNominalValue )   # Установить свойство (**)
        ```
        """
        ...
    @AutoNominalValue.setter
    def AutoNominalValue(self, value: bool): ...

    @property
    def Brackets(self) -> int:  # idimensiontext_brackets.html
        """
        ## Brackets - Размер в скобках

        Тип данных: из перечисления ksDimensionTextBracketsEnum

        Позволяет управлять отображением значений полей Символ, Значение и Отклонение в размерной надписи, обрамленных квадратными или круглыми скобками.

        Если для элементов размерной надписи включено отображение рамки и/или подчеркивания, то они также заключаются в скобки.

        ### Синтаксис Automation

        ```
        Brackets = Object.Brackets       # Получить свойство(* )
        Object.Brackets = Brackets       # Установить свойство (* )
        Brackets = Object.GetBrackets()  # Получить свойство (**)
        Object.SetBrackets( Brackets )   # Установить свойство (**)
        ```
        """
        ...
    @Brackets.setter
    def Brackets(self, value: int): ...

    @property
    def DeviationOn(self) -> bool:  # idimensiontext_deviationon.html
        """
        ## DeviationOn - Включить отклонения

        Тип данных: BOOL

        Установите свойство в TRUE, чтобы отклонения или пределы были отрисованы в размерной надписи. Если пределы включены в размерную надпись, а квалитет нет, то номинальное значение в размерной надписи не отображается.

        Если отображение предельных значений размера включено, а квалитет не задан, то изменение геометрии размера (например, при перестроении ассоциативного размера) не приводит к пересчету предельных значений.

        ### Синтаксис Automation

        ```
        DeviationOn = Object.DeviationOn       # Получить свойство(* )
        Object.DeviationOn = DeviationOn       # Установить свойство (* )
        DeviationOn = Object.GetDeviationOn()  # Получить свойство (**)
        Object.SetDeviationOn( DeviationOn )   # Установить свойство (**)
        ```
        """
        ...
    @DeviationOn.setter
    def DeviationOn(self, value: bool): ...

    @property
    def DeviationType(self) -> int:  # idimensiontext_deviationtype.html
        """
        ## DeviationType - Тип отклонений

        Тип данных: Из перечисления ksDimensionDeviationEnum

        Установите значение свойства равным:

        •ksDimDeviation, если требуется внести в размерную надпись предельные отклонения размера,

        •ksDimLimits, если требуется внести предельные значения размера (предельные значения записываются одно над другим) или

        •ksDimLineLimits, если требуется внести предельные значения размера (предельные значения записываются друг за другом через дефис).

        Отклонения или предельные значения размера вычисляются автоматически в соответствии с выбранным квалитетом и номинальным значением размера. При необходимости вы можете ввести значения отклонений или пределов вручную.

        Если значения отклонений равны по модулю, но отличаются по знаку, то перед значением отклонения отображается знак ±.

        После ввода отклонений или пределов вручную поле Квалитет очищается.

        ### Синтаксис Automation

        ```
        DeviationType = Object.DeviationType       # Получить свойство(* )
        Object.DeviationType = DeviationType       # Установить свойство (* )
        DeviationType = Object.GetDeviationType()  # Получить свойство (**)
        Object.SetDeviationType( DeviationType )   # Установить свойство (**)
        ```
        """
        ...
    @DeviationType.setter
    def DeviationType(self, value: int): ...

    @property
    def HasTolerance(self) -> bool:  # idimensiontext_hastolerance.html
        """
        ## HasTolerance - Отключить допуск

        Тип данных: BOOL

        Свойство позволяет устанавливать и получать признак отключения допуска.

        ### Синтаксис Automation

        ```
        HasTolerance = Object.HasTolerance       # Получить свойство(* )
        Object.HasTolerance = HasTolerance       # Установить свойство (* )
        HasTolerance = Object.GetHasTolerance()  # Получить свойство (**)
        Object.SetHasTolerance( HasTolerance )   # Установить свойство (**)
        ```
        """
        ...
    @HasTolerance.setter
    def HasTolerance(self, value: bool): ...

    @property
    def HighDeviation(self) -> ITextLine:  # idimensiontext_highdeviation.html
        """
        ## HighDeviation - Верхнее отклонение

        Тип данных: интерфейс строки текста ITextLine

        1. Свойство позволяет получить и задать верхнее отклонение или предельное значение для размера.

        2. Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        HighDeviation = Object.HighDeviation       # Получить свойство(* )
        HighDeviation = Object.GetHighDeviation()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def HighDeviationValue(self) -> float:  # idimensiontext_highdeviationvalue.html
        """
        ## HighDeviationValue - Верхнее отклонение

        Тип данных: double

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        HighDeviationValue = Object.HighDeviationValue       # Получить свойство(* )
        HighDeviationValue = Object.GetHighDeviationValue()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def LowDeviation(self) -> ITextLine:  # idimensiontext_lowdeviation.html
        """
        ## LowDeviation - Нижнее отклонение

        Тип данных: интерфейс строки текста ITextLine

        1. Свойство позволяет получить и задать нижнее отклонение или предельное значение для размера.

        2. Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        LowDeviation = Object.LowDeviation       # Получить свойство(* )
        LowDeviation = Object.GetLowDeviation()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def LowDeviationValue(self) -> float:  # idimensiontext_lowdeviationvalue.html
        """
        ## LowDeviationValue - Нижнее отклонение

        Тип данных: double

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        LowDeviationValue = Object.LowDeviationValue       # Получить свойство(* )
        LowDeviationValue = Object.GetLowDeviationValue()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def NominalText(self) -> ITextLine:  # idimensiontext_nominaltext.html
        """
        ## NominalText - Текст номинального значения

        Тип данных: интерфейс строки текста ITextLine

        1. Свойство позволяет получить и задать текст значения размера, заданного пользователем.

        2. Для задания текста нужно снять признак IDimensionText::AutoNominalValue.

        3. Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        NominalText = Object.NominalText       # Получить свойство(* )
        NominalText = Object.GetNominalText()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def NominalValue(self) -> float:  # idimensiontext_nominalvalue.html
        """
        ## NominalValue - Значение размера

        Тип данных: double

        1. Позволяет получить и установить значение размера.

        2. При изменении свойства выполняются следующие действия:

        •формируется текстовое значение номинала IDimensionText::NominalText;

        •у размера снимается признак автоматического определения номинального значения IDimensionText::AutoNominalValue;

        Значение размера при этом не изменяется. Значение размера изменяется при изменении параметров размера, например, при изменении расстояния между выносными линиями линейного размера.

        ### Синтаксис Automation

        ```
        NominalValue = Object.NominalValue       # Получить свойство(* )
        Object.NominalValue = NominalValue       # Установить свойство (* )
        NominalValue = Object.GetNominalValue()  # Получить свойство (**)
        Object.SetNominalValue( NominalValue )   # Установить свойство (**)
        ```
        """
        ...
    @NominalValue.setter
    def NominalValue(self, value: float): ...

    @property
    def Prefix(self) -> ITextLine:  # idimensiontext_prefix.html
        """
        ## Prefix - Текст до (префикс)

        Тип данных: интерфейс строки текста ITextLine

        1. Свойство позволяет получить и задать текст, предшествующий основному содержанию размерной надписи.

        2. Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        Prefix = Object.Prefix       # Получить свойство(* )
        Prefix = Object.GetPrefix()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def Rectangle(self) -> bool:  # idimensiontext_rectangle.html
        """
        ## Rectangle - Текст в рамке

        Тип данных: BOOL

        Позволяет управлять отрисовкой рамки вокруг полей Символ, Значение и Отклонение в размерной надписи.

        ### Синтаксис Automation

        ```
        Rectangle = Object.Rectangle       # Получить свойство(* )
        Object.Rectangle = Rectangle       # Установить свойство (* )
        Rectangle = Object.GetRectangle()  # Получить свойство (**)
        Object.SetRectangle( Rectangle )   # Установить свойство (**)
        ```
        """
        ...
    @Rectangle.setter
    def Rectangle(self, value: bool): ...

    @property
    def Sign(self) -> int:  # idimensiontext_sign.html
        """
        ## Sign - Номер условного значка перед номиналом

        Тип данных: long

        Значения свойства:

        +---------------------------------------------------------------------------------------------------------------------+
        | 0  | - нет значка,                                                                                                  |
        +---------------------------------------------------------------------------------------------------------------------+
        | 1  | - диаметр,                                                                                                     |
        +---------------------------------------------------------------------------------------------------------------------+
        | 2  | - квадрат,                                                                                                     |
        +---------------------------------------------------------------------------------------------------------------------+
        | 3  | - К - радиус,                                                                                                  |
        +---------------------------------------------------------------------------------------------------------------------+
        | 4  | - М - метрическая резьба,                                                                                      |
        +---------------------------------------------------------------------------------------------------------------------+
        | >4 | - символ из любого шрифта, задаваемого через свойство IDimensionText::SignFont (по умолчанию 'Symbol type A'). |
        +---------------------------------------------------------------------------------------------------------------------+

        Позволяет получить установить номер условного значка перед номиналом.

        ### Синтаксис Automation

        ```
        Sign = Object.Sign       # Получить свойство(* )
        Object.Sign = Sign       # Установить свойство (* )
        Sign = Object.GetSign()  # Получить свойство (**)
        Object.SetSign( Sign )   # Установить свойство (**)
        ```
        """
        ...
    @Sign.setter
    def Sign(self, value: int): ...

    @property
    def SignFont(self) -> str:  # idimensiontext_signfont.html
        """
        ## SignFont - Шрифт для условного значка перед номиналом

        Тип данных: BSTR

        1. Позволяет получить установить шрифт для условного значка перед номиналом.

        2. Используется для символов с номером IDimensionText::Sign> 4.

        3. По умолчанию используется шрифт Symbol type A.

        ### Синтаксис Automation

        ```
        SignFont = Object.SignFont       # Получить свойство(* )
        Object.SignFont = SignFont       # Установить свойство (* )
        SignFont = Object.GetSignFont()  # Получить свойство (**)
        Object.SetSignFont( SignFont )   # Установить свойство (**)
        ```
        """
        ...
    @SignFont.setter
    def SignFont(self, value: str): ...

    @property
    def Style(self) -> int:  # idimensiontext_style.html
        """
        ## Style - Стиль текста

        Тип данных: long

        Позволяет получить и установить стиль для текстов размера. 0 - умолчательный стиль для текстов размера.

        ### Синтаксис Automation

        ```
        Style = Object.Style       # Получить свойство(* )
        Object.Style = Style       # Установить свойство (* )
        Style = Object.GetStyle()  # Получить свойство (**)
        Object.SetStyle( Style )   # Установить свойство (**)
        ```
        """
        ...
    @Style.setter
    def Style(self, value: int): ...

    @property
    def Suffix(self) -> ITextLine:  # idimensiontext_suffix.html
        """
        ## Suffix - Текст после (суффикс)

        Тип данных: интерфейс строки текста ITextLine

        1. Свойство позволяет получить и задать текст, следующий за основным содержанием размерной надписи.

        2. Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        Suffix = Object.Suffix       # Получить свойство(* )
        Suffix = Object.GetSuffix()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def TextAlign(self) -> int:  # idimensiontext_textalign.html
        """
        ## TextAlign - Выравнивание текста

        Тип данных: из перечисления ksDimensionTextAlignEnum

        Свойство позволяет выбрать способ расположения отклонений или предельных значений относительно номинального значения.

        ### Синтаксис Automation

        ```
        TextAlign = Object.TextAlign       # Получить свойство(* )
        Object.TextAlign = TextAlign       # Установить свойство (* )
        TextAlign = Object.GetTextAlign()  # Получить свойство (**)
        Object.SetTextAlign( TextAlign )   # Установить свойство (**)
        ```
        """
        ...
    @TextAlign.setter
    def TextAlign(self, value: int): ...

    @property
    def TextFormat(self) -> int:  # idimensiontext_textformat.html
        """
        ## TextFormat - Формат текста

        Тип данных: из перечисления ksDimTextFormatEnum

        1. Свойство позволяет получить и задать формат отображаемого значения размера.

        2. Свойство используется для углового размера и углового размера с обрывом.

        ### Синтаксис Automation

        ```
        TextFormat = Object.TextFormat       # Получить свойство(* )
        Object.TextFormat = TextFormat       # Установить свойство (* )
        TextFormat = Object.GetTextFormat()  # Получить свойство (**)
        Object.SetTextFormat( TextFormat )   # Установить свойство (**)
        ```
        """
        ...
    @TextFormat.setter
    def TextFormat(self, value: int): ...

    @property
    def TextUnder(self) -> IText:  # idimensiontext_textunder.html
        """
        ## TextUnder - Текст под размерной надписью

        Тип данных: интерфейс текста для работы с аннотационными объектами IText

        1. Свойство позволяет получить и задать текст, который располагается под размерной надписью.

        2. Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        TextUnder = Object.TextUnder       # Получить свойство(* )
        TextUnder = Object.GetTextUnder()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def Tolerance(self) -> str:  # idimensiontext_tolerance.html
        """
        ## Tolerance - Текст номинального значения

        Тип данных: BSTR

        Свойство позволяет получить и задать квалитет для размера.

        ### Синтаксис Automation

        ```
        Tolerance = Object.Tolerance       # Получить свойство(* )
        Object.Tolerance = Tolerance       # Установить свойство (* )
        Tolerance = Object.GetTolerance()  # Получить свойство (**)
        Object.SetTolerance( Tolerance )   # Установить свойство (**)
        ```
        """
        ...
    @Tolerance.setter
    def Tolerance(self, value: str): ...

    @property
    def ToleranceOn(self) -> bool:  # idimensiontext_toleranceon.html
        """
        ## ToleranceOn - Включить квалитет

        Тип данных: BOOL

        Установите свойство в TRUE, чтобы обозначение квалитета было отрисовано в размерной надписи. Значение квалитета может не отображаться, если выбран номер квалитета, для которого отключена вставка в размерную надпись.

        Номер, начиная с которого квалитет не вносится в надпись, задается в диалоге настройки точности размерных надписей.

        ### Синтаксис Automation

        ```
        ToleranceOn = Object.ToleranceOn       # Получить свойство(* )
        Object.ToleranceOn = ToleranceOn       # Установить свойство (* )
        ToleranceOn = Object.GetToleranceOn()  # Получить свойство (**)
        Object.SetToleranceOn( ToleranceOn )   # Установить свойство (**)
        ```
        """
        ...
    @ToleranceOn.setter
    def ToleranceOn(self, value: bool): ...

    @property
    def Underline(self) -> bool:  # idimensiontext_underline.html
        """
        ## UnderLine - Подчеркнутый текст

        Тип данных: BOOL

        Позволяет управлять подчеркиванием значений полей Символ, Значение и Отклонение в размерной надписи.

        ### Синтаксис Automation

        ```
        UnderLine = Object.UnderLine       # Получить свойство(* )
        Object.UnderLine = UnderLine       # Установить свойство (* )
        UnderLine = Object.GetUnderLine()  # Получить свойство (**)
        Object.SetUnderLine( UnderLine )   # Установить свойство (**)
        ```
        """
        ...
    @Underline.setter
    def Underline(self, value: bool): ...

    @property
    def Unit(self) -> ITextLine:  # idimensiontext_unit.html
        """
        ## Unit - Единица измерения

        Тип данных: интерфейс строки текста ITextLine

        1. Свойство позволяет получить и задать текст для обозначения единиц измерения проставляемого размера.

        2. Текст будет отрисован в размерной надписи сразу после предельных отклонений.

        3. Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        Unit = Object.Unit       # Получить свойство(* )
        Unit = Object.GetUnit()  # Получить свойство (**)
        ```
        """
        ...

    def InitDeviations(self, HighDeviationValue, LowDeviationValue) -> bool:  # idimensiontext_initdeviations.html
        """
        ## InitDeviations - Установить отклонения

        ### Синтаксис Automation

        ```
        BOOL InitDeviations( double HighDeviationValue, double LowDeviationValue );
        ```

        ### Входные параметры

        +------------------------------------------------------+
        | HighDeviationValue | - значение верхнего отклонения, |
        +------------------------------------------------------+
        | LowDeviationValue  | - значение нижнего отклонения.  |
        +------------------------------------------------------+

        ### Возвращаемое значение

        +-----------------------------------------+
        | TRUE | - в случае успешного завершения. |
        +-----------------------------------------+
        """
        ...




class IMarkLeader(IDispatch):  # imarkleader.html
    """
    ## Интерфейс IMarkLeader

    Интерфейс параметров обозначения маркировки.

    1. Интерфейс можно получить с помощью метода IUnknown::QueryInterface у интерфейсов IBaseLeader или IBaseLeader3D.

    2. С помощью метода IUnknown::QueryInterface у интерфейса можно получить интерфейс ответвлений IBranchs или IBranchs3D.
    """

    @property
    def Designation(self) -> IText:  # imarkleader_designation.html
        """
        ## Designation - Обозначение

        Тип данных: указатель на интерфейс IText

        1. Свойство позволяет получить интерфейс текста обозначения.

        2. Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        Designation = Object.Designation       # Получить свойство(* )
        Designation = Object.GetDesignation()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def TextOnBranch(self) -> IText:  # imarkleader_textonbranch.html
        """
        ## TextOnBranch - Текст над ножкой

        Тип данных: указатель на интерфейс IText

        1. Свойство позволяет получить интерфейс текста над ножкой.

        2. Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        TextOnBranch = Object.TextOnBranch       # Получить свойство(* )
        TextOnBranch = Object.GetTextOnBranch()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def TextUnderBranch(self) -> IText:  # imarkleader_textunderbranch.html
        """
        ## TextUnderBranch - Текст под ножкой

        Тип данных: указатель на интерфейс IText

        1. Свойство позволяет получить интерфейс текста под ножкой.

        2. Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        TextUnderBranch = Object.TextUnderBranch       # Получить свойство(* )
        TextUnderBranch = Object.GetTextUnderBranch()  # Получить свойство (**)
        ```
        """
        ...




class IAssociationTables(IDrawingObjects):  # iassociationtables.html
    """
    ## Интерфейс IAssociationTables

    Интерфейс коллекции ассоциативных таблиц.
    """

    def Add(self, Document, Type) -> IAssociationTable:  # iassociationtables_add.html
        """
        ## Add - Добавить таблицу в коллекцию

        ### Синтаксис Automation

        ```
        LPDISPATCH Add( VARIANT Document, ksReportTypeEnum Type );
        ```

        ### Входные параметры

        +-------------------------------------------------------+
        | Document | - имя документа или указатель на документ, |
        +-------------------------------------------------------+
        | Type     | - тип отчета.                              |
        +-------------------------------------------------------+

        ### Возвращаемое значение

        - указатель на ассоциативную таблицу IAssociationTable.
        """
        ...

    def AssociationTable(self, Index) -> IAssociationTable:  # iassociationtables_associationtable.html
        """
        ## AssociationTable - Получить таблицу по индексу

        Тип данных: указатель на интерфейс IAssociationTable

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        AssociationTable = Object.AssociationTable( Index )     # Получить свойство(* )
        AssociationTable = Object.GetAssociationTable( Index )  # Получить свойство (**)
        ```
        """
        ...




class IAxisLines(IDrawingObjects):  # iaxislines.html
    """
    ## Интерфейс IAxisLines

    Интерфейс коллекции осевых линий.

    Позволяет создавать и получать осевые линии.

    Данный интерфейс можно получить у контейнера условных обозначений 2D

    ISymbols2DContainer::AxisLines.
    """

    def Add(self) -> IAxisLine:  # iaxislines_add.html
        """
        ## Add - Создать новый элемент и добавить его в коллекцию

        ### Синтаксис Automation

        ```
        LPDISPATCH Add();
        ```

        ### Возвращаемое значение

        - Указатель на интерфейс осевой линии IAxisLine.
        """
        ...

    def AxisLine(self, Index) -> IAxisLine:  # iaxislines_axisline.html
        """
        ## AxisLine - Осевая линия, заданная по индексу

        Тип данных: указатель на интерфейс осевой линии IAxisLine

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        AxisLine = iObject.AxisLine ( Index );    # Получить свойство(* )
        AxisLine = iObject.GetAxisLine( Index );  # Получить свойство (**)
        ```
        """
        ...




class IBases(IDrawingObjects):  # ibases.html
    """
    ## Интерфейс IBases

    Интерфейс коллекции обозначений баз.

    Интерфейс позволяет получать и создавать обозначение базы.

    Получить интерфейс можно, используя свойство контейнера условных обозначений 2D ISymbols2DContainer::Bases.
    """

    def Add(self) -> IBase:  # ibases_add.html
        """
        ## Add - Создать обозначение базы (добавить в коллекцию)

        ### Синтаксис Automation

        ```
        LPDISPATCH Add();
        ```

        ### Возвращаемое значение

        - Указатель на интерфейс обозначения базы IBase.
        """
        ...

    def Base(self, Index) -> IBase:  # ibases_base.html
        """
        ## Base - Обозначение базы, заданное по индексу

        Тип данных: указатель на интерфейс обозначения базы IBase

        1. Свойство доступно только для чтения.

        2. В качестве индекса может использоваться индекс в коллекции и reference объекта.

        ### Синтаксис Automation

        ```
        Base = iObject.Base( Index );     # Получить свойство(* )
        Base = iObject.GetBase( Index );  # Получить свойство (**)
        ```

        ### Входные параметры

        +----------------------------------------------------------------------------------------------------------------+
        | Index (Variant) | - Индекс узла в коллекции. Поддерживаются следующие типы: - VT_I4 - индекс обозначения базы. |
        +----------------------------------------------------------------------------------------------------------------+
        """
        ...




class IBrokenLines(IDrawingObjects):  # ibrokenlines.html
    """
    ## Интерфейс IBrokenLines

    Интерфейс коллекции линий обрыва с изломами.

    Позволяет создавать и получать линии обрыва с изломами.

    Данный интерфейс можно получить у контейнера условных обозначений ISymbols2DContainer::BrokenLines.
    """

    def Add(self) -> IBrokenLine:  # ibrokenlines_add.html
        """
        ## Add - Создать новый элемент и добавить егов коллекцию

        Метод позволяет добавить волнистую линию.

        ### Синтаксис Automation

        ```
        LPDISPATCH Add();
        ```

        ### Возвращаемое значение

        - Указатель на интерфейс линии обрыва с изломами IBrokenLine.
        """
        ...

    def BrokenLine(self, Index) -> IBrokenLine:  # ibrokenlines_brokenline.html
        """
        ## BrokenLine - Линия обрыва с изломами, заданная по индексу

        Тип данных: указатель на интерфейс линии обрыва с изломами IBrokenLine

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        BrokenLine = iObject.BrokenLine ( Index );    # Получить свойство(* )
        BrokenLine = iObject.GetBrokenLine( Index );  # Получить свойство (**)
        ```
        """
        ...




class ICentreMarkers(IDrawingObjects):  # icentremarkers.html
    """
    ## Интерфейс ICentreMarkers

    Интерфейс коллекции обозначений центра.

    Позволяет создавать и получать обозначения центра.

    Данный интерфейс можно получить у контейнера условных обозначений 2D ISymbols2DContainer::CentreMarkers.
    """

    def Add(self) -> ICentreMarker:  # icentremarkers_add.html
        """
        ## Add - Создать новый элемент и добавить его в коллекцию

        ### Синтаксис Automation

        ```
        LPDISPATCH Add();
        ```

        ### Возвращаемое значение

        - Указатель на интерфейс обозначения центра ICentreMarker.
        """
        ...

    def CentreMarker(self, Index) -> ICentreMarker:  # icentremarkers_centremarker.html
        """
        ## CentreMarker - Обозначение центра, заданное по индексу

        Тип данных: указатель на интерфейс обозначения центра ICentreMarker

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        CentreMarker = iObject.CentreMarker( Index );     # Получить свойство(* )
        CentreMarker = iObject.GetCentreMarker( Index );  # Получить свойство (**)
        ```
        """
        ...




class ICircularsCentries(IDrawingObjects):  # icircularscentries.html
    """
    ## Интерфейс ICircularsCentries

    Интерфейс коллекции круговых сеток центров.
    """

    def Add(self, BaseObjects, AutoFindOtherObjects, Closed, WithCenter) -> ICircularCentres:  # icircularscentries_add.html
        """
        ## Add - Создает новый элемент и добавляет его в коллекцию

        Координаты центра осевой линии определяются по координатам центров первых трех окружностей.

        Для остальных окружностей выполняется проверка на одинаковое расстояние от точки центра осевой линии до центра окружности

        ### Синтаксис Automation

        ```
        LPDISPATCH Add( VARIANT BaseObjects, BOOL AutoFindOtherObjects, BOOL Closed, BOOL WithCenter );
        ```

        ### Входные параметры

        +-----------------------------------------------------------------------------------------------------------------------------+
        | BaseObjects          | - Базовые окружности.                                                                                |
        +-----------------------------------------------------------------------------------------------------------------------------+
        | AutoFindOtherObjects | - Автоматический поиск других подходящих окружностей, центры которых лежат на круговой осевой линии. |
        +-----------------------------------------------------------------------------------------------------------------------------+
        | Closed               | - Замкнуть осевую.                                                                                   |
        +-----------------------------------------------------------------------------------------------------------------------------+
        | WithCenter           | - С обозначением центра.                                                                             |
        +-----------------------------------------------------------------------------------------------------------------------------+

        ### Возвращаемое значение

        - Указатель на интерфейс ICircularCentres..
        """
        ...

    def AddByPoint(self, X0, Y0, BaseObjects, AutoFindOtherObjects, Closed, WithCenter) -> ICircularCentres:  # icircularscentries_addbypoint.html
        """
        ## AddByPoint - Создает новый элемент и добавляет егов коллекцию

        Радиус обозначения центра определяется по удалению точки центра первой окружности.

        Для остальных окружностей выполняется проверка на одинаковое расстояние от точки центра осевой линии до центра окружности.

        ### Синтаксис Automation

        ```
        LPDISPATCH AddByPoint( double X0, double Y0, VARIANT BaseObjects, BOOL AutoFindOtherObjects, BOOL Closed, BOOL WithCenter );
        ```

        ### Входные параметры

        +-----------------------------------------------------------------------------------------------------------------------------+
        | X0, Y0               | - Координаты цента осевой линии.                                                                     |
        +-----------------------------------------------------------------------------------------------------------------------------+
        | BaseObjects          | - Базовые окружности.                                                                                |
        +-----------------------------------------------------------------------------------------------------------------------------+
        | AutoFindOtherObjects | - Автоматический поиск других подходящих окружностей, центры которых лежат на круговой осевой линии. |
        +-----------------------------------------------------------------------------------------------------------------------------+
        | Closed               | - Замкнуть осевую.                                                                                   |
        +-----------------------------------------------------------------------------------------------------------------------------+
        | WithCenter           | - С обозначением центра.                                                                             |
        +-----------------------------------------------------------------------------------------------------------------------------+

        ### Возвращаемое значение

        - Указатель на интерфейс ICircularCentres.
        """
        ...

    def CircularCentres(self, Index) -> ICircularCentres:  # icircularscentries_circularcentres.html
        """
        ## CircularCentres - Возвращает элемент, заданныйпо индексу или по имени

        Тип данных: Указатель на интерфейс ICircularCentres.

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        CircularCentres = Object.CircularCentres( Index );     # Получить свойство(* )
        CircularCentres = Object.GetCircularCentres( Index );  # Получить свойство (**)
        ```

        ### Входные параметры

        +--------------------------------------------+
        | VARIANT Index | - индекс или имя элемента. |
        +--------------------------------------------+
        """
        ...




class IConditionIntersects(IDrawingObjects):  # iconditionintersects.html
    """
    ## Интерфейс IConditionIntersects

    Интерфейс коллекции условных пересечений.

    Получить интерфейс коллекции условных операций можно, используя свойство контейнера условных обозначений 2D ISymbols2DContainer::ConditionIntersects.

    Версия: KOMPAS v19
    """

    def Add(self) -> IConditionIntersect:  # iconditionintersects_add.html
        """
        ## Add - Создает новый элемент и добавляет егов коллекцию

        1. Метод позволяет создать новый интерфейс условного пересечения.

        2. После получения нового интерфейса нужно задать параметры и вызвать метод IDrawingObject::Update.

        ### Синтаксис Automation

        ```
        LPDISPATCH Add();
        ```

        ### Возвращаемое значение

        - Указатель на интерфейс IConditionIntersect.
        """
        ...

    def ConditionIntersect(self, Index) -> IConditionIntersect:  # iconditionintersects_conditionintersect.html
        """
        ## ConditionIntersect - Возвращает элемент, заданныйпо индексу или по имени

        Тип данных: Указатель на интерфейс IConditionIntersect.

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        ConditionIntersect = Object.ConditionIntersect( Index )     # Получить свойство (* )
        ConditionIntersect = Object.GetConditionIntersect( Index )  # Получить свойство (**)
        ```

        ### Входные параметры

        +--------------------------------------------+
        | VARIANT Index | - индекс или имя элемента. |
        +--------------------------------------------+
        """
        ...




class ILinearsCentries(IDrawingObjects):  # ilinearscentries.html
    """
    ## Интерфейс ILinearsCentries

    Интерфейс коллекции линейных сеток центров.
    """

    def Add(self, BaseObjects, AutoFindOtherObjects, HasBreaks, AxisAngle, TurnAngle) -> ILinearCentres:  # ilinearscentries_add.html
        """
        ## Add - Создает новый элемент и добавляет его в коллекцию

        ### Синтаксис Automation

        ```
        LPDISPATCH Add( VARIANT BaseObjects, BOOL AutoFindOtherObjects, BOOL HasBreaks, double AxisAngle, double TurnAngle );
        ```

        ### Входные параметры

        +-----------------------------------------------------------------------------------------------------------------------------+
        | BaseObjects          | - Базовые окружности.                                                                                |
        +-----------------------------------------------------------------------------------------------------------------------------+
        | AutoFindOtherObjects | - Автоматический поиск других подходящих окружностей, центры которых лежат на круговой осевой линии. |
        +-----------------------------------------------------------------------------------------------------------------------------+
        | HasBreaks            | - С разрывами.                                                                                       |
        +-----------------------------------------------------------------------------------------------------------------------------+
        | AxisAngle            | - Наклон первой оси.                                                                                 |
        +-----------------------------------------------------------------------------------------------------------------------------+
        | TurnAngle            | - Угол раствора.                                                                                     |
        +-----------------------------------------------------------------------------------------------------------------------------+

        ### Возвращаемое значение

        - Указатель на интерфейс ILinearCentres.
        """
        ...

    def LinearCentres(self, Index) -> ILinearCentres:  # ilinearscentries_linearcentres.html
        """
        ## LinearCentres - Возвращает элемент, заданныйпо индексу или по имени

        Тип данных: Указатель на интерфейс ILinearCentres.

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        LinearCentres = Object.LinearCentres( Index );     # Получить свойство(* )
        LinearCentres = Object.GetLinearCentres( Index );  # Получить свойство (**)
        ```

        ### Входные параметры

        +--------------------------------------------+
        | VARIANT Index | - индекс или имя элемента. |
        +--------------------------------------------+
        """
        ...




class ICutLines(IDrawingObjects):  # icutlines.html
    """
    ## Интерфейс ICutLines

    Интерфейс коллекции линий разреза/сечения.

    Интерфейс позволяет получать и создавать линию разреза/сечения.

    1. Интерфейс можно получить, используя свойство контейнера условных обозначений 2D ISymbols2DContainer::CutLines.

    2. Кроме того, интерфейс можно получить, используя свойство коллекции линий разреза для СПДС IBuildingContainer::BuildingCutLines.
    """

    def Add(self) -> ICutLine:  # icutlines_add.html
        """
        ## Add - Создать линию разреза/сечения (добавить в коллекцию)

        ### Синтаксис Automation

        ```
        LPDISPATCH Add();
        ```

        ### Возвращаемое значение

        - Указатель на интерфейс линии разреза/сечения ICutLine.
        """
        ...

    def CutLine(self, Index) -> ICutLine:  # icutlines_cutline.html
        """
        ## CutLine - Линия разреза/сечения, заданная по индексу

        Тип данных: указатель на интерфейс линии разреза/сечения ICutLine

        1. Свойство доступно только для чтения.

        2. В качестве индекса может использоваться индекс в коллекции и reference объекта.

        ### Синтаксис Automation

        ```
        CutLine = iObject.CutLine( Index );     # Получить свойство(* )
        CutLine = iObject.GetCutLine( Index );  # Получить свойство (**)
        ```

        ### Входные параметры

        +---------------------------------------------------------------------------------------------------------------------+
        | Index (Variant) | - Индекс узла в коллекции. Поддерживаются следующие типы: - VT_I4 - индекс линии разреза/сечения. |
        +---------------------------------------------------------------------------------------------------------------------+
        """
        ...




class IDrawingTables(IDrawingObjects):  # idrawingtables.html
    """
    ## Интерфейс IDrawingTables

    Интерфейс коллекции таблиц на чертеже.

    Позволяет получать и создавать таблицы на чертеже.

    Получить интерфейс коллекции можно, используя свойство контейнера условных обозначений ISymbols2DContainer::DrawingTables.
    """

    def Add(self, RowsCount, ColumnsCount, RowHeigh, ColumnsWidth, TitlePos) -> IDrawingTable:  # idrawingtables_add.html
        """
        ## Add - Создать новый элемент и добавить его в коллекцию

        ### Синтаксис Automation

        ```
        LPDISPATCH Add( long RowsCount,
        long ColumnsCount,
        double RowHeigh,
        double ColumnsWidth,
        long titlePos);
        ```

        ### Входные параметры

        +--------------------------------------------------+
        | RowsCount    | - количество строк таблицы,       |
        +--------------------------------------------------+
        | ColumnsCount | - количество столбцов таблицы,    |
        +--------------------------------------------------+
        | RowHeigh     | - высота строк,                   |
        +--------------------------------------------------+
        | ColumnsWidth | - ширина столбцов,                |
        +--------------------------------------------------+
        | titlePos     | - расположение заголовка таблицы. |
        +--------------------------------------------------+

        ### Возвращаемое значение

        - Указатель на интерфейс таблицы на чертеже IDrawingTable.
        """
        ...

    def DrawingTable(self, Index) -> IDrawingTable:  # idrawingtables_drawingtable.html
        """
        ## DrawingTable - Указатель на таблицу, заданную по индексу

        Тип данных: указатель на интерфейс IDrawingTable

        1. В качестве индекса может использоваться индекс в коллекции и reference объекта.

        2. Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        DrawingTable = iObject.DrawingTable( Index );     # Получить свойство(* )
        DrawingTable = iObject.GetDrawingTable( Index );  # Получить свойство (**)
        ```

        ### Входные параметры

        +-----------------------------------+
        | Index | - VT_I4 - индекс объекта. |
        +-----------------------------------+
        """
        ...

    def Load(self, FileName) -> IDrawingTable:  # idrawingtables_load.html
        """
        ## Load - Загрузить таблицу из файла

        ### Синтаксис Automation

        ```
        LPDISPATCH Load( BSTR fileName );
        ```

        ### Входные параметры

        +---------------------------------+
        | fileName | - имя файла таблицы. |
        +---------------------------------+

        ### Возвращаемое значение

        - указатель на интерфейс таблицы на чертеже IDrawingTable.
        """
        ...




class IDrawingTexts(IDrawingObjects):  # idrawingtexts.html
    """
    ## Интерфейс IDrawingTexts

    Интерфейс коллекции текстов на чертеже.

    Интерфейс позволяет получить доступ к текстам на чертеже для указанного вида, а также создавать новые тексты.

    Данный интерфейс можно получить у интерфейса IDrawingContainer, используя свойство IDrawingContainer::DrawingTexts.
    """

    def Add(self) -> IDrawingText:  # idrawingtexts_add.html
        """
        ## Add - Создать текст на чертеже и добавить его в коллекцию

        Метод позволяет добавить текст в коллекцию. После получения нового интерфейса нужно задать параметры текста и вызвать метод IDrawingObject::Update.

        ### Синтаксис Automation

        ```
        LPDISPATCH Add;
        ```

        ### Возвращаемое значение

        - Указатель на интерфейс текста на чертеже IDrawingText.
        """
        ...

    def AddTextMark(self, Object) -> IDrawingText:  # idrawingtexts_addtextmark.html
        """
        ## AddTextMark - Добавить текстовую метку для объекта

        Создание текстовой метки в текущий момент доступно для объектов:

        •База - IBase,

        •Шероховатость - IRough,

        •Допуск формы - ITolerance.

        Версия: Компас v20

        ### Синтаксис Automation

        ```
        LPDISPATCH AddTextMark( IDrawingObject * Object );
        ```

        ### Входные параметры

        +---------------------------------------------------------------------+
        | Object | - Объект, для которого требуется добавить текстовую метку. |
        +---------------------------------------------------------------------+

        ### Возвращаемое значение

        +-------------------------------------------------------------------+
        | Текстовый объект | - марка - указатель на интерфейс IDrawingText, |
        +-------------------------------------------------------------------+
        """
        ...

    def DeleteTextMark(self, Object) -> bool:  # idrawingtexts_deletetextmark.html
        """
        ## DeleteTextMark - Удалить текстовую метку

        Если у объекта есть созданная текстовая метка, она будет удалена.

        Версия: Компас v20

        ### Синтаксис Automation

        ```
        BOOL DeleteTextMark( IDrawingObject * Object );
        ```

        ### Входные параметры

        +-------------------------------------------------------------------------+
        | Object | - Объект, которому принадлежит текстовая метка IDrawingObject. |
        +-------------------------------------------------------------------------+

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def DrawingText(self, Index) -> IDrawingText:  # idrawingtexts_drawingtext.html
        """
        ## DrawingText - Текст на чертеже, заданный по индексу

        Тип данных: указатель на интерфейс текста на чертеже IDrawingText

        1. Свойство доступно только для чтения.

        2. В случае неудачи получения свойства, возвращаемое значение NULL.

        ### Синтаксис Automation

        ```
        DrawingText = iObject.DrawingText(Index);     # Получить свойство(* )
        DrawingText = iObject.GetDrawingText(Index);  # Получить свойство (**)
        ```

        ### Входные параметры

        +------------------------------------------------------------------------------------------------------------------------------+
        | Index (Variant) | - Индекс текста в коллекции. Поддерживаются следующие типы: - VT_I4 - индекс объекта, - reference объекта. |
        +------------------------------------------------------------------------------------------------------------------------------+
        """
        ...

    def FindTextMark(self, Object) -> IDrawingText:  # idrawingtexts_findtextmark.html
        """
        ## FindTextMark - Найти текстовую метку для объекта

        Создание текстовой метки в текущий момент доступно для объектов:

        •База - IBase,

        •Шероховатость - IRough,

        •Допуск формы - ITolerance.

        Версия: Компас v20

        ### Синтаксис Automation

        ```
        LPDISPATCH FindTextMark( IDrawingObject * Object );
        ```

        ### Входные параметры

        +------------------------------------------------------------------+
        | Object | - Объект, для которого требуется найти текстовую метку. |
        +------------------------------------------------------------------+

        ### Возвращаемое значение

        +-------------------------------------------------------------------+
        | Текстовый объект | - марка - указатель на интерфейс IDrawingText, |
        +-------------------------------------------------------------------+
        """
        ...

    def FindTextMarkObject(self, Mark) -> IDrawingObject:  # idrawingtexts_findtextmarkobject.html
        """
        ## FindTextMarkObject - Найти объект по текстовой метке

        ### Синтаксис Automation

        ```
        LPDISPATCH FindTextMarkObject( IDrawingText * Mark );
        ```

        ### Входные параметры

        +-----------------------------------------------------+
        | Mark | - Указатель на текстовую метку IDrawingText. |
        +-----------------------------------------------------+

        Версия: Компас v20

        ### Возвращаемое значение

        +-------------------------------------------------------------------------+
        | Object | - Объект, которому принадлежит текстовая метка IDrawingObject, |
        +-------------------------------------------------------------------------+
        """
        ...




class ILeaders(IDrawingObjects):  # ileaders.html
    """
    ## Интерфейс ILeaders

    Интерфейс коллекции линий-выносок.

    Позволяет получать и создавать линии-выноски.

    Получить интерфейс коллекции можно, используя свойство контейнера условных обозначений ISymbols2DContainer::Leaders.
    """

    def Add(self, DimType) -> IBaseLeader:  # ileaders_add.html
        """
        ## Add - Создать новый элемент и добавить его в коллекцию

        ### Синтаксис Automation

        ```
        LPDISPATCH Add( DrawingObjectTypeEnum DimType );
        ```

        ### Входные параметры

        +--------------------------------+
        | DimType | - тип линии-выноски. |
        +--------------------------------+

        Типы линий-выносок:

        +---------------------------------------------------------------------+
        | ksDrLeader       | 20 | Простая линия-выноска,                      |
        +---------------------------------------------------------------------+
        | ksDrPosLeader    | 21 | Линия-выноска для обозначения позиции,      |
        +---------------------------------------------------------------------+
        | ksDrBrandLeader  | 22 | Линия-выноска для обозначения клеймения,    |
        +---------------------------------------------------------------------+
        | ksDrMarkerLeader | 23 | Линия-выноска для обозначения маркирования. |
        +---------------------------------------------------------------------+

        ### Возвращаемое значение

        - Указатель на интерфейс линии выноски IBaseLeader.
        """
        ...

    def Leader(self, Index) -> IBaseLeader:  # ileaders_leader.html
        """
        ## Leader - Указатель на линию-выноску, заданную по индексу

        Тип данных: указатель на интерфейс IBaseLeader

        1. В качестве индекса может использоваться индекс в коллекции, reference объекта и имя объекта.

        2. Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        Leader = Object.Leader( Index )     # Получить свойство(* )
        Leader = Object.GetLeader( Index )  # Получить свойство (**)
        ```

        ### Входные параметры

        +---------------------------------------------------------------------------------------------------------------------+
        | Index | - Индекс узла в коллекции. Поддерживаются следующие типы: - VT_I4 - индекс объекта - VT_BSTR - имя объекта. |
        +---------------------------------------------------------------------------------------------------------------------+
        """
        ...




class IRemoteElements(IDrawingObjects):  # iremoteelements.html
    """
    ## Интерфейс IRemoteElements

    Интерфейс коллекции выносных элементов.

    Позволяет создавать и получать выносные элементы.

    Данный интерфейс можно получить у контейнера условных обозначений 2D ISymbols2DContainer::RemoteElements.
    """

    def Add(self) -> IRemoteElement:  # iremoteelements_add.html
        """
        ## Add - Создать новый элемент и добавить его в коллекцию

        ### Синтаксис Automation

        ```
        LPDISPATCH Add();
        ```

        ### Возвращаемое значение

        - Указатель на интерфейс выносного элемента IRemoteElement.
        """
        ...

    def RemoteElement(self, Index) -> IRemoteElement:  # iremoteelements_remoteelement.html
        """
        ## RemoteElement - Выносной элемент, заданный по индексу

        Тип данных: указатель на интерфейс выносного элемента IRemoteElement

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        RemoteElement = iObject.RemoteElement ( Index );    # Получить свойство(* )
        RemoteElement = iObject.GetRemoteElement( Index );  # Получить свойство (**)
        ```
        """
        ...




class IRoughs(IDrawingObjects):  # iroughs.html
    """
    ## Интерфейс IRoughs

    Интерфейс коллекции обозначений шероховатости.

    Позволяет получать и создавать обозначения шероховатости. Данный интерфейс можно получить у контейнера условных обозначений 2D, используя свойство ISymbols2DContainer::Roughs.
    """

    def Add(self) -> IRough:  # iroughs_add.html
        """
        ## Add - Добавить обозначение шероховатости в коллекцию

        ### Синтаксис Automation

        ```
        LPDISPATCH Add;
        ```

        ### Возвращаемое значение

        - Указатель на интерфейс обозначения шероховатости IRough.
        """
        ...

    def Rough(self, Index) -> IRough:  # iroughs_rough.html
        """
        ## Rough - Обозначение шероховатости, заданноепо индексу, ссылке или тексту

        Тип данных: указатель на интерфейс обозначения шероховатости IRough

        1. Свойство доступно только для чтения.

        2. В качестве индекса может использоваться индекс в коллекции и reference объекта.

        ### Синтаксис Automation

        ```
        Rough = iObject.Rough( Index );     # Получить свойство(* )
        Rough = iObject.GetRough( Index );  # Получить свойство (**)
        ```

        ### Входные параметры

        +----------------------------------------------------------------------------------------------------------------------------------------+
        | Index (Variant) | - Индекс обозначения узла в коллекции. Поддерживаются следующие типы: - VT_I4 - индекс объекта, - reference объекта. |
        +----------------------------------------------------------------------------------------------------------------------------------------+
        """
        ...




class ITolerances(IDrawingObjects):  # itolerances.html
    """
    ## Интерфейс ITolerances

    Интерфейс коллекции допусков формы.

    Позволяет получать и создавать обозначения допуска формы.

    Получить интерфейс коллекции можно используя свойство контейнера условных обозначений ISymbols2DContainer::Tolerances.
    """

    def Add(self) -> ITolerance:  # itolerances_add.html
        """
        ## Add - Создать новый элемент и добавить егов коллекцию

        ### Синтаксис Automation

        ```
        LPDISPATCH Add();
        ```

        ### Возвращаемое значение

        - Указатель на интерфейс допуска формы ITolerance.
        """
        ...

    def Tolerance(self, Index) -> ITolerance:  # itolerances_tolerance.html
        """
        ## Tolerance - Указатель на допуск формы, заданныйпо индексу

        Тип данных: указатель на интерфейс ITolerance

        1. В качестве индекса может использоваться индекс в коллекции и reference объекта.

        2. Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        Tolerance = iObject.Tolerance( Index );     # Получить свойство(* )
        Tolerance = iObject.GetTolerance( Index );  # Получить свойство (**)
        ```

        ### Входные параметры

        +---------------------------------+
        | Index | VT_I4 - индекс объекта. |
        +---------------------------------+
        """
        ...




class IViewPointers(IDrawingObjects):  # iviewpointers.html
    """
    ## Интерфейс IViewPointers

    Интерфейс коллекции стрелок взгляда.

    Позволяет получать и создавать стрелки взгляда.

    Получить интерфейс коллекции можно, используя свойство контейнера условных обозначений ISymbols2DContainer::ViewPointers.
    """

    def Add(self) -> IViewPointer:  # iviewpointers_add.html
        """
        ## Add - Создать новый элемент и добавить его в коллекцию

        ### Синтаксис Automation

        ```
        LPDISPATCH Add();
        ```

        ### Возвращаемое значение

        - Указатель на интерфейс стрелки взгляда ViewPointer.
        """
        ...

    def ViewPointer(self, Index) -> IViewPointer:  # iviewpointers_viewpointer.html
        """
        ## ViewPointer - Указатель на стрелку взгляда, заданную по индексу

        Тип данных: указатель на интерфейс IViewPointer

        1. В качестве индекса может использоваться индекс в коллекции, reference объекта и имя объекта.

        2. Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        ViewPointer = iObject.ViewPointer( Index );     # Получить свойство(* )
        ViewPointer = iObject.GetViewPointer( Index );  # Получить свойство (**)
        ```

        ### Входные параметры

        +---------------------------------------------------------------------------------------------------------------------+
        | Index | - Индекс узла в коллекции. Поддерживаются следующие типы: - VT_I4 - индекс объекта - VT_BSTR - имя объекта. |
        +---------------------------------------------------------------------------------------------------------------------+
        """
        ...




class IWaveLines(IDrawingObjects):  # iwavelines.html
    """
    ## Интерфейс IWaveLines

    Интерфейс коллекции волнистых линий.

    Интерфейс позволяет создавать и получать волнистые линии линии.

    Данный интерфейс можно получить у контейнера условных обозначений ISymbols2DContainer::WaveLines.
    """

    def Add(self) -> IWaveLine:  # iwavelines_add.html
        """
        ## Add - Создать новый элемент и добавить егов коллекцию

        Метод позволяет добавить волнистую линию.

        ### Синтаксис Automation

        ```
        LPDISPATCH Add();
        ```

        ### Возвращаемое значение

        - Указатель на интерфейс волнистой линии IWaveLine.
        """
        ...

    def WaveLine(self, Index) -> IWaveLine:  # iwavelines_waveline.html
        """
        ## WaveLine - Волнистая линия, заданная по индексу

        Тип данных: указатель на интерфейс волнистой линии IWaveLine

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        WaveLine = iObject.WaveLine ( Index );    # Получить свойство(* )
        WaveLine = iObject.GetWaveLine( Index );  # Получить свойство (**)
        ```
        """
        ...




class IDesignation2D(IDispatch):  # idesignation2d.html
    """
    ## Интерфейс IDesignation2D

    Интерфейс обозначения 2D.

    Интерфейс является дополнительным для 2D обозначений.

    Версия Компас v21
    """

    def GetDesignationPoint(self, DimensionPoint, PointIndex) -> bool:  # idesignation2d_getdesignationpoint.html
        """
        ## GetDesignationPoint - Получить координаты точек размера

        ### Синтаксис Automation

        ```
        BOOL GetDesignationPoint( ksDesignationPointEnum DimensionPoint, long PointIndex, double * X, double * Y );
        ```

        ### Входные параметры

        +----------------------------------------------------------------------+
        | DimensionPoint | - тип точки из перечисления ksDesignationPointEnum, |
        +----------------------------------------------------------------------+
        | PointIndex     | - индекс точки.                                     |
        +----------------------------------------------------------------------+

        ### Выходные параметры

        +-------------------------------+
        | X, Y, Z | - координаты точки. |
        +-------------------------------+

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...




class IAssociationTable(IDrawingObject):  # iassociationtable.html
    """
    ## Интерфейс IAssociationTable

    Интерфейс ассоциативной таблицы.
    """

    @property
    def Actual(self) -> bool:  # iassociationtable_actual.html
        """
        ## Actual - Актуальность таблицы

        Тип данных: BOOL

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        Actual = Object.Actual       # Получить свойство(* )
        Actual = Object.GetActual()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def NumericGroup(self) -> INumericGroup:  # iassociationtable_numericgroup.html
        """
        ## NumericGroup - Группа нумерации

        Тип данных: Указатель на интерфейс INumericGroup

        ### Синтаксис Automation

        ```
        NumericGroup = Object.NumericGroup       # Получить свойство(* )
        Object.NumericGroup = NumericGroup       # Установить свойство (* )
        NumericGroup = Object.GetNumericGroup()  # Получить свойство (**)
        Object.SetNumericGroup( NumericGroup )   # Установить свойство (**)
        ```
        """
        ...
    @NumericGroup.setter
    def NumericGroup(self, value: INumericGroup): ...

    @property
    def Report(self) -> IReport:  # iassociationtable_report.html
        """
        ## Report - Получить интерфейс отчета

        Тип данных: Указатель на интерфейс IReport

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        Report = Object.Report       # Получить свойство(* )
        Report = Object.GetReport()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def TablePlaceType(self) -> int:  # iassociationtable_tableplacetype.html
        """
        ## TablePlaceType - Тип привязки таблицы

        Тип данных: из перечисления ksTablePointEnum

        ### Синтаксис Automation

        ```
        TablePlaceType = Object.TablePlaceType       # Получить свойство(* )
        Object.TablePlaceType = TablePlaceType       # Установить свойство (* )
        TablePlaceType = Object.GetTablePlaceType()  # Получить свойство (**)
        Object.SetTablePlaceType( TablePlaceType )   # Установить свойство (**)
        ```
        """
        ...
    @TablePlaceType.setter
    def TablePlaceType(self, value: int): ...

    @property
    def TablesCount(self) -> int:  # iassociationtable_tablescount.html
        """
        ## TablesCount - Количество таблиц в созданном отчете

        Тип данных: long

        Свойство доступно только для чтения.

        Версия Компас v21

        ### Синтаксис Automation

        ```
        TablesCount = Object.TablesCount       # Получить свойство(* )
        TablesCount = Object.GetTablesCount()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def X(self) -> float:  # iassociationtable_x.html
        """
        ## X - Координата точки привязки по Х

        Тип данных: double

        ### Синтаксис Automation

        ```
        X = Object.X       # Получить свойство(* )
        Object.X = X       # Установить свойство (* )
        X = Object.GetX()  # Получить свойство (**)
        Object.SetX( X )   # Установить свойство (**)
        ```
        """
        ...
    @X.setter
    def X(self, value: float): ...

    @property
    def Y(self) -> float:  # iassociationtable_y.html
        """
        ## Y - Координата точки привязки по Y

        Тип данных: double

        ### Синтаксис Automation

        ```
        Y = Object.Y       # Получить свойство(* )
        Object.Y = Y       # Установить свойство (* )
        Y = Object.GetY()  # Получить свойство (**)
        Object.SetY( Y )   # Установить свойство (**)
        ```
        """
        ...
    @Y.setter
    def Y(self, value: float): ...

    def Rebuild(self) -> bool:  # iassociationtable_rebuild.html
        """
        ## Rebuild - Перестроить отчет

        ### Синтаксис Automation

        ```
        BOOL Rebuild();
        ```

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def Title(self, TableIndex) -> IText:  # iassociationtable_title.html
        """
        ## Title - Заголовок

        Тип данных: Указатель на интерфейс IText

        Свойство доступно только для чтения.

        Версия Компас v21

        ### Синтаксис Automation

        ```
        Title = Object.Title( TableIndex )     # Получить свойство(* )
        Title = Object.GetTitle( TableIndex )  # Получить свойство (**)
        ```
        """
        ...




class IAxisLine(IDrawingObject):  # iaxisline.html
    """
    ## Интерфейс IAxisLine

    Интерфейс осевой линии.

    Интерфейс позволяет задавать параметры осевой линии.

    1. Интерфейс можно получить у коллекции осевых линий, используя свойство IAxisLines::AxisLine или метод IAxisLines::Add.

    2. После задания параметров осевой линии требуется вызвать метод IDrawingObject::Update.

    3. Посредством вызова метода IUnknown::QueryInterface (const GUID far& iid, void** pif) у данного интерфейса можно получить дополнительный интерфейс IAxisLineParam.
    """

    @property
    def Angle(self) -> float:  # iaxisline_angle.html
        """
        ## Angle - Угол между осевой и осью OX текущей СК

        Тип данных: double

        Свойство позволяет устанавливать и получать угол между осевой и осью OX текущей СК.

        ### Синтаксис Automation

        ```
        Angle = Object.Angle       # Получить свойство(* )
        Object.Angle = Angle       # Установить свойство (* )
        Angle = Object.GetAngle()  # Получить свойство (**)
        Object.SetAngle( Angle )   # Установить свойство (**)
        ```
        """
        ...
    @Angle.setter
    def Angle(self, value: float): ...

    @property
    def Length(self) -> float:  # iaxisline_length.html
        """
        ## Length - Длина

        Тип данных: double

        Свойство позволяет устанавливать и получать длину осевой линии.

        ### Синтаксис Automation

        ```
        Length = Object.Length       # Получить свойство(* )
        Object.Length = Length       # Установить свойство (* )
        Length = Object.GetLength()  # Получить свойство (**)
        Object.SetLength( Length )   # Установить свойство (**)
        ```
        """
        ...
    @Length.setter
    def Length(self, value: float): ...

    @property
    def X1(self) -> float:  # iaxisline_x1.html
        """
        ## X1 - Координата первой точки по оси X

        Тип данных: double

        Свойство позволяет устанавливать и получать координату первой точки осевой линии по оси Х.

        ### Синтаксис Automation

        ```
        X1 = Object.X1       # Получить свойство(* )
        Object.X1 = X1       # Установить свойство (* )
        X1 = Object.GetX1()  # Получить свойство (**)
        Object.SetX1( X1 )   # Установить свойство (**)
        ```
        """
        ...
    @X1.setter
    def X1(self, value: float): ...

    @property
    def X2(self) -> float:  # iaxisline_x2.html
        """
        ## X2 - Координата второй точки по оси X

        Тип данных: double

        Свойство позволяет устанавливать и получать координату второй точки осевой линии по оси Х.

        ### Синтаксис Automation

        ```
        X2 = Object.X2       # Получить свойство(* )
        Object.X2 = X2       # Установить свойство (* )
        X2 = Object.GetX2()  # Получить свойство (**)
        Object.SetX2( X2 )   # Установить свойство (**)
        ```
        """
        ...
    @X2.setter
    def X2(self, value: float): ...

    @property
    def Y1(self) -> float:  # iaxisline_y1.html
        """
        ## Y1 - Координата первой точки по оси Y

        Тип данных: double

        Свойство позволяет устанавливать и получать координату первой точки осевой линии по оси Y.

        ### Синтаксис Automation

        ```
        Y1 = Object.Y1       # Получить свойство(* )
        Object.Y1 = Y1       # Установить свойство (* )
        Y1 = Object.GetY1()  # Получить свойство (**)
        Object.SetY1( Y1 )   # Установить свойство (**)
        ```
        """
        ...
    @Y1.setter
    def Y1(self, value: float): ...

    @property
    def Y2(self) -> float:  # iaxisline_y2.html
        """
        ## Y2 - Координата второй точки по оси Y

        Тип данных: double

        Свойство позволяет устанавливать и получать координату второй точки осевой линии по оси Y.

        ### Синтаксис Automation

        ```
        Y2 = Object.Y2       # Получить свойство(* )
        Object.Y2 = Y2       # Установить свойство (* )
        Y2 = Object.GetY2()  # Получить свойство (**)
        Object.SetY2( Y2 )   # Установить свойство (**)
        ```
        """
        ...
    @Y2.setter
    def Y2(self, value: float): ...




class IBase(IDrawingObject):  # ibase.html
    """
    ## Интерфейс IBase

    Интерфейс обозначения базы.

    1. Интерфейс позволяет получить и задать свойства объекта "база".

    2. Получить интерфейс можно, используя свойство IBases::Base или метод IBases::Add интерфейса коллекции обозначений базы.

    3. После задания параметров объекта требуется вызвать метод IDrawingObject::Update.
    """

    @property
    def AutoSorted(self) -> bool:  # ibase_autosorted.html
        """
        ## AutoSorted - Автосортировка

        Тип данных: BOOL

        Значения свойства:

        +----------------------------+
        | TRUE  | - включить опцию,  |
        +----------------------------+
        | FALSE | - отключить опцию. |
        +----------------------------+

        При отключенной опции доступно редактирование текста базы с помощью свойства IBase::Text.

        ### Синтаксис Automation

        ```
        AutoSorted = Object.AutoSorted       # Получить свойство(* )
        Object.AutoSorted = AutoSorted       # Установить свойство (* )
        AutoSorted = Object.GetAutoSorted()  # Получить свойство (**)
        Object.SetAutoSorted( AutoSorted )   # Установить свойство (**)
        ```
        """
        ...
    @AutoSorted.setter
    def AutoSorted(self, value: bool): ...

    @property
    def BaseObject(self) -> IDrawingObject:  # ibase_baseobject.html
        """
        ## BaseObject - Опорный объект

        Тип данных: указатель на интерфейс IDrawingObject

        После указания базового объекта все устанавливаемые объекту координаты положения знака на поверхности будут проецироваться на базовый объект или его продолжение.

        ### Синтаксис Automation

        ```
        BaseObject = Object.BaseObject       # Получить свойство(* )
        Object.BaseObject = BaseObject       # Установить свойство (* )
        BaseObject = Object.GetBaseObject()  # Получить свойство (**)
        Object.SetBaseObject( BaseObject )   # Установить свойство (**)
        ```
        """
        ...
    @BaseObject.setter
    def BaseObject(self, value: IDrawingObject): ...

    @property
    def BranchX(self) -> float:  # ibase_branchx.html
        """
        ## BranchX - Координата X конечной точки выноски

        Тип данных: double

        ### Синтаксис Automation

        ```
        BranchX = Object.BranchX       # Получить свойство(* )
        Object.BranchX = BranchX       # Установить свойство (* )
        BranchX = Object.GetBranchX()  # Получить свойство (**)
        Object.SetBranchX( BranchX )   # Установить свойство (**)
        ```
        """
        ...
    @BranchX.setter
    def BranchX(self, value: float): ...

    @property
    def BranchY(self) -> float:  # ibase_branchy.html
        """
        ## BranchY - Координата Y конечной точки выноски

        Тип данных: double

        ### Синтаксис Automation

        ```
        BranchY = Object.BranchY       # Получить свойство(* )
        Object.BranchY = BranchY       # Установить свойство (* )
        BranchY = Object.GetBranchY()  # Получить свойство (**)
        Object.SetBranchY( BranchY )   # Установить свойство (**)
        ```
        """
        ...
    @BranchY.setter
    def BranchY(self, value: float): ...

    @property
    def DrawType(self) -> bool:  # ibase_drawtype.html
        """
        ## DrawType - Способ отрисовки обозначения базы

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        DrawType = Object.DrawType       # Получить свойство(* )
        Object.DrawType = DrawType       # Установить свойство (* )
        DrawType = Object.GetDrawType()  # Получить свойство (**)
        Object.SetDrawType( DrawType )   # Установить свойство (**)
        ```
        """
        ...
    @DrawType.setter
    def DrawType(self, value: bool): ...

    @property
    def Text(self) -> IText:  # ibase_text.html
        """
        ## Text - Текст базы

        Тип данных: указатель на интерфейс IText

        1. Свойство доступно только для чтения.

        2. Свойство позволяет получить указатель на интерфейс текста IText для редактирования текста базы.

        3. Указатель на интерфейс IText вернётся только в случае отключенной опции Автосортировка. Если автосортировка текста включена, свойство вернёт NULL.

        ### Синтаксис Automation

        ```
        Text = Object.Text       # Получить свойство(* )
        Text = Object.GetText()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def X0(self) -> float:  # ibase_x0.html
        """
        ## X0 - Координата X положения знака на поверхности

        Тип данных: double

        ### Синтаксис Automation

        ```
        X0 = Object.X0       # Получить свойство(* )
        Object.X0 = X0       # Установить свойство (* )
        X0 = Object.GetX0()  # Получить свойство (**)
        Object.SetX0( X0 )   # Установить свойство (**)
        ```
        """
        ...
    @X0.setter
    def X0(self, value: float): ...

    @property
    def Y0(self) -> float:  # ibase_y0.html
        """
        ## Y0 - Координата Y положения знака на поверхности

        Тип данных: double

        ### Синтаксис Automation

        ```
        Y0 = Object.Y0       # Получить свойство(* )
        Object.Y0 = Y0       # Установить свойство (* )
        Y0 = Object.GetY0()  # Получить свойство (**)
        Object.SetY0( Y0 )   # Установить свойство (**)
        ```
        """
        ...
    @Y0.setter
    def Y0(self, value: float): ...




class IBaseLeader(IDrawingObject):  # ibaseleader.html
    """
    ## Интерфейс IBaseLeader

    Интерфейс обозначения линии-выноски 2D.

    1. Получить интерфейс можно, используя свойство интерфейса коллекции линий-выносок ILeaders::Leader или метод ILeaders::Add.

    2. Посредством вызова метода IUnknown::QueryInterface (const GUID far& iid, void** pif) интерфейс позволяет получить следующие дополнительные интерфейсы:

    +---------------------------------------------------------------------------------------+
    | Объект                                      | DrawingObjectTypeEnum | Интерфейс       |
    +---------------------------------------------------------------------------------------+
    | Простая линия-выноска                       | ksDrLeader            | ILeader         |
    +---------------------------------------------------------------------------------------+
    | Линия-выноска для обозначения позиции       | ksDrPosLeader         | IPositionLeader |
    +---------------------------------------------------------------------------------------+
    | Линия-выноска для для обозначения клеймения | ksDrBrandLeader       | IBrandLeader    |
    +---------------------------------------------------------------------------------------+
    | Линия-выноска для обозначения маркирования  | ksDrMarkLeader        | IMarkLeader     |
    +---------------------------------------------------------------------------------------+
    | Знак изменения                              | ksDrChangeLeader      | IChangeLeader   |
    +---------------------------------------------------------------------------------------+
    | Ответвления                                 |                       | IBranchs        |
    +---------------------------------------------------------------------------------------+

    3. После задания параметров объекта требуется вызвать метод IDrawingObject::Update.
    """

    @property
    def ArrowType(self) -> int:  # ibaseleader_arrowtype.html
        """
        ## ArrowType - Тип стрелки линии-выноски

        Тип данных: из перечисления ksArrowEnum

        Свойство позволяет устанавливать и получать тип стрелки линии-выноски.

        ### Синтаксис Automation

        ```
        ArrowType = Object.ArrowType       # Получить свойство(* )
        Object.ArrowType = ArrowType       # Установить свойство (* )
        ArrowType = Object.GetArrowType()  # Получить свойство (**)
        Object.SetArrowType( ArrowType )   # Установить свойство (**)
        ```
        """
        ...
    @ArrowType.setter
    def ArrowType(self, value: int): ...

    def GetBranchTextPosition(self) -> bool:  # ibaseleader_getbranchtextposition.html
        """
        ## GetBranchTextPosition - Получить координаты точки привязки текста на линии выноски

        ### Синтаксис Automation

        ```
        BOOL GetBranchTextPosition( double * X, double * Y );
        ```

        ### Выходные параметры

        +-------------------------------------+
        | X, Y | - координаты точки привязки. |
        +-------------------------------------+

        Версия КОМПАС v22

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def SetBranchTextPosition(self, X, Y) -> bool:  # ibaseleader_setbranchtextposition.html
        """
        ## SetBranchTextPosition - Установить координаты точки привязки текста на линии выноски

        ### Синтаксис Automation

        ```
        BOOL SetBranchTextPosition( double X, double Y );
        ```

        ### Входные параметры

        +-------------------------------------+
        | X, Y | - координаты точки привязки. |
        +-------------------------------------+

        Версия КОМПАС v22

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...




class IBrokenLine(IDrawingObject):  # ibrokenline.html
    """
    ## Интерфейс IBrokenLine

    Интерфейс линии обрыва с изломами.

    Интерфейс позволяет задавать и получать параметры линии обрыва с изломами.

    1. Интерфейс можно получить у коллекции линий обрыва с изломами используя свойство IBrokenLines::BrokenLine или метод IBrokenLines::Add.

    2. После задания параметров объекта требуется вызвать метод IDrawingObject::Update.
    """

    @property
    def Angle(self) -> float:  # ibrokenline_angle.html
        """
        ## Angle - Угол между линией и осью OX текущей СК

        Тип данных: double

        Свойство позволяет устанавливать и получать угол между линией обрыва с изломами и осью ОХ текущей СК.

        ### Синтаксис Automation

        ```
        Angle = Object.Angle       # Получить свойство(* )
        Object.Angle = Angle       # Установить свойство (* )
        Angle = Object.GetAngle()  # Получить свойство (**)
        Object.SetAngle( Angle )   # Установить свойство (**)
        ```
        """
        ...
    @Angle.setter
    def Angle(self, value: float): ...

    @property
    def AutoBreakAmplitude(self) -> float:  # ibrokenline_autobreakamplitude.html
        """
        ## AutoBreakAmplitude - Использовать значение амплитуды излома из настроек документа

        Тип данных: double

        Свойство позволяет устанавливать и получать признак использования амплитуды излома из настроек документа.

        ### Синтаксис Automation

        ```
        AutoBreakAmplitude = Object.AutoBreakAmplitude       # Получить свойство(* )
        Object.AutoBreakAmplitude = AutoBreakAmplitude       # Установить свойство (* )
        AutoBreakAmplitude = Object.GetAutoBreakAmplitude()  # Получить свойство (**)
        Object.SetAutoBreakAmplitude( AutoBreakAmplitude )   # Установить свойство (**)
        ```
        """
        ...
    @AutoBreakAmplitude.setter
    def AutoBreakAmplitude(self, value: float): ...

    @property
    def AutoJutValue(self) -> bool:  # ibrokenline_autojutvalue.html
        """
        ## AutoJutValue - Использовать значение выступа линии из настроек документа

        Тип данных: BOOL

        Свойство позволяет устанавливать и получать признак использования значения выступа линии из настроек документа.

        ### Синтаксис Automation

        ```
        AutoJutValue = Object.AutoJutValue       # Получить свойство(* )
        Object.AutoJutValue = AutoJutValue       # Установить свойство (* )
        AutoJutValue = Object.GetAutoJutValue()  # Получить свойство (**)
        Object.SetAutoJutValue( AutoJutValue )   # Установить свойство (**)
        ```
        """
        ...
    @AutoJutValue.setter
    def AutoJutValue(self, value: bool): ...

    @property
    def BreakAmplitude(self) -> float:  # ibrokenline_breakamplitude.html
        """
        ## BreakAmplitude - Амплитуда излома

        Тип данных: double

        Свойство позволяет устанавливать и получать амплитуду излома.

        ### Синтаксис Automation

        ```
        BreakAmplitude = Object.BreakAmplitude       # Получить свойство(* )
        Object.BreakAmplitude = BreakAmplitude       # Установить свойство (* )
        BreakAmplitude = Object.GetBreakAmplitude()  # Получить свойство (**)
        Object.SetBreakAmplitude( BreakAmplitude )   # Установить свойство (**)
        ```
        """
        ...
    @BreakAmplitude.setter
    def BreakAmplitude(self, value: float): ...

    @property
    def BreakDisplacement(self) -> float:  # ibrokenline_breakdisplacement.html
        """
        ## BreakDisplacement - Смещение излома

        Тип данных: double

        Свойство позволяет устанавливать и получать смещение излома.

        ### Синтаксис Automation

        ```
        BreakDisplacement = Object.BreakDisplacement       # Получить свойство(* )
        Object.BreakDisplacement = BreakDisplacement       # Установить свойство (* )
        BreakDisplacement = Object.GetBreakDisplacement()  # Получить свойство (**)
        Object.SetBreakDisplacement( BreakDisplacement )   # Установить свойство (**)
        ```
        """
        ...
    @BreakDisplacement.setter
    def BreakDisplacement(self, value: float): ...

    @property
    def BreaksCount(self) -> int:  # ibrokenline_breakscount.html
        """
        ## BreaksCount - Количество изломов

        Тип данных: long

        Свойство позволяет устанавливать и получать количество изломов.

        ### Синтаксис Automation

        ```
        BreaksCount = Object.BreaksCount       # Получить свойство(* )
        Object.BreaksCount = BreaksCount       # Установить свойство (* )
        BreaksCount = Object.GetBreaksCount()  # Получить свойство (**)
        Object.SetBreaksCount( BreaksCount )   # Установить свойство (**)
        ```
        """
        ...
    @BreaksCount.setter
    def BreaksCount(self, value: int): ...

    @property
    def JutValue(self) -> float:  # ibrokenline_jutvalue.html
        """
        ## JutValue - Значение выступа линии

        Тип данных: double

        Свойство позволяет устанавливать и получать значение выступа линии.

        ### Синтаксис Automation

        ```
        JutValue = Object.JutValue       # Получить свойство(* )
        Object.JutValue = JutValue       # Установить свойство (* )
        JutValue = Object.GetJutValue()  # Получить свойство (**)
        Object.SetJutValue( JutValue )   # Установить свойство (**)
        ```
        """
        ...
    @JutValue.setter
    def JutValue(self, value: float): ...

    @property
    def Length(self) -> float:  # ibrokenline_length.html
        """
        ## Length - Длина

        Тип данных: double

        Свойство позволяет устанавливать и получать длину линии обрыва с изломами.

        ### Синтаксис Automation

        ```
        Length = Object.Length       # Получить свойство(* )
        Object.Length = Length       # Установить свойство (* )
        Length = Object.GetLength()  # Получить свойство (**)
        Object.SetLength( Length )   # Установить свойство (**)
        ```
        """
        ...
    @Length.setter
    def Length(self, value: float): ...

    @property
    def Style(self) -> int:  # ibrokenline_style.html
        """
        ## Style - Cтиль линии

        Тип данных: long

        1. Свойство позволяет устанавливать и получать стиль линии.

        2. Системные стили линий берутся из перечисления ksCurveStyleEnum.

        ### Синтаксис Automation

        ```
        Style = Object.Style       # Получить свойство(* )
        Object.Style = Style       # Установить свойство (* )
        Style = Object.GetStyle()  # Получить свойство (**)
        Object.SetStyle( Style )   # Установить свойство (**)
        ```
        """
        ...
    @Style.setter
    def Style(self, value: int): ...

    @property
    def Type1(self) -> bool:  # ibrokenline_type1.html
        """
        ## Type1 - Тип

        Тип данных: BOOL

        Значения свойства:

        +------------------+
        | TRUE  | - Тип 1, |
        +------------------+
        | FALSE | - Тип 2. |
        +------------------+

        Свойство позволяет устанавливать и получать тип линии обрыва с изломами.

        ### Синтаксис Automation

        ```
        Type1 = Object.Type1       # Получить свойство(* )
        Object.Type1 = Type1       # Установить свойство (* )
        Type1 = Object.GetType1()  # Получить свойство (**)
        Object.SetType1( Type1 )   # Установить свойство (**)
        ```
        """
        ...
    @Type1.setter
    def Type1(self, value: bool): ...

    @property
    def X1(self) -> float:  # ibrokenline_x1.html
        """
        ## X1 - Координата первой точки по оси X

        Тип данных: double

        Свойство позволяет устанавливать и получать координату первой точки по оси Х.

        ### Синтаксис Automation

        ```
        X1 = Object.X1       # Получить свойство(* )
        Object.X1 = X1       # Установить свойство (* )
        X1 = Object.GetX1()  # Получить свойство (**)
        Object.SetX1( X1 )   # Установить свойство (**)
        ```
        """
        ...
    @X1.setter
    def X1(self, value: float): ...

    @property
    def X2(self) -> float:  # ibrokenline_x2.html
        """
        ## X2 - Координата второй точки по оси X

        Тип данных: double

        Свойство позволяет устанавливать и получать координату второй точки по оси Х.

        ### Синтаксис Automation

        ```
        X2 = Object.X2       # Получить свойство(* )
        Object.X2 = X2       # Установить свойство (* )
        X2 = Object.GetX2()  # Получить свойство (**)
        Object.SetX2( X2 )   # Установить свойство (**)
        ```
        """
        ...
    @X2.setter
    def X2(self, value: float): ...

    @property
    def Y1(self) -> float:  # ibrokenline_y1.html
        """
        ## Y1 - Координата первой точки по оси Y

        Тип данных: double

        Свойство позволяет устанавливать и получать координату первой точки по оси Y.

        ### Синтаксис Automation

        ```
        Y1 = Object.Y1       # Получить свойство(* )
        Object.Y1 = Y1       # Установить свойство (* )
        Y1 = Object.GetY1()  # Получить свойство (**)
        Object.SetY1( Y1 )   # Установить свойство (**)
        ```
        """
        ...
    @Y1.setter
    def Y1(self, value: float): ...

    @property
    def Y2(self) -> float:  # ibrokenline_y2.html
        """
        ## Y2 - Координата второй точки по оси Y

        Тип данных: double

        Свойство позволяет устанавливать и получать координату второй точки по оси Y.

        ### Синтаксис Automation

        ```
        Y2 = Object.Y2       # Получить свойство(* )
        Object.Y2 = Y2       # Установить свойство (* )
        Y2 = Object.GetY2()  # Получить свойство (**)
        Object.SetY2( Y2 )   # Установить свойство (**)
        ```
        """
        ...
    @Y2.setter
    def Y2(self, value: float): ...




class ICentreMarker(IDrawingObject):  # icentremarker.html
    """
    ## Интерфейс ICentreMarker

    Интерфейс обозначения центра.

    Интерфейс позволяет задавать параметры обозначения центра.

    1. Интерфейс можно получить у коллекции обозначений центра, используя свойство ICentreMarkers::CentreMarker или метод ICentreMarkers::Add.

    2. После задания параметров обозначения центра требуется вызвать метод IDrawingObject::Update.

    3. Посредством вызова метода IUnknown::QueryInterface (const GUID far& iid, void** pif) у данного интерфейса можно получить дополнительный интерфейс IAxisLineParam.
    """

    @property
    def Angle(self) -> float:  # icentremarker_angle.html
        """
        ## Angle - Угол наклона обозначения центра

        Тип данных: double

        Свойство позволяет устанавливать и получать угол наклона обозначения центра.

        ### Синтаксис Automation

        ```
        Angle = Object.Angle       # Получить свойство(* )
        Object.Angle = Angle       # Установить свойство (* )
        Angle = Object.GetAngle()  # Получить свойство (**)
        Object.SetAngle( Angle )   # Установить свойство (**)
        ```
        """
        ...
    @Angle.setter
    def Angle(self, value: float): ...

    @property
    def BaseObject(self) -> IDrawingObject:  # icentremarker_baseobject.html
        """
        ## BaseObject - Базовая кривая

        Тип данных: указатель на интерфейс графического объекта IDrawingObject

        Свойство позволяет устанавливать и получать базовую кривую обозначения центра.

        ### Синтаксис Automation

        ```
        BaseObject = Object.BaseObject       # Получить свойство(* )
        Object.BaseObject = BaseObject       # Установить свойство (* )
        BaseObject = Object.GetBaseObject()  # Получить свойство (**)
        Object.SetBaseObject( BaseObject )   # Установить свойство (**)
        ```
        """
        ...
    @BaseObject.setter
    def BaseObject(self, value: IDrawingObject): ...

    @property
    def CrosshairSize(self) -> float:  # icentremarker_crosshairsize.html
        """
        ## CrosshairSize - Размер обозначения крестика

        Тип данных: double

        Свойство позволяет устанавливать и получать размер обозначения крестика.

        ### Синтаксис Automation

        ```
        CrosshairSize = Object.CrosshairSize       # Получить свойство(* )
        Object.CrosshairSize = CrosshairSize       # Установить свойство (* )
        CrosshairSize = Object.GetCrosshairSize()  # Получить свойство (**)
        Object.SetCrosshairSize( CrosshairSize )   # Установить свойство (**)
        ```
        """
        ...
    @CrosshairSize.setter
    def CrosshairSize(self, value: float): ...

    @property
    def CrosshairSizeModify(self) -> bool:  # icentremarker_crosshairsizemodify.html
        """
        ## CrosshairSizeModify - Использовать размер обозначения крестика из настроек документа

        Тип данных: BOOL

        Свойство позволяет устанавливать и получать признак использования крестика из настроек документа.

        ### Синтаксис Automation

        ```
        CrosshairSizeModify = Object.CrosshairSizeModify       # Получить свойство(* )
        Object.CrosshairSizeModify = CrosshairSizeModify       # Установить свойство (* )
        CrosshairSizeModify = Object.GetCrosshairSizeModify()  # Получить свойство (**)
        Object.SetCrosshairSizeModify( CrosshairSizeModify )   # Установить свойство (**)
        ```
        """
        ...
    @CrosshairSizeModify.setter
    def CrosshairSizeModify(self, value: bool): ...

    @property
    def SignType(self) -> int:  # icentremarker_signtype.html
        """
        ## SignType - Тип обозначения центра

        Тип данных: из перечисления ksCentreMarkerEnum

        Свойство позволяет устанавливать и получать тип обозначения центра.

        ### Синтаксис Automation

        ```
        SignType = Object.SignType       # Получить свойство(* )
        Object.SignType = SignType       # Установить свойство (* )
        SignType = Object.GetSignType()  # Получить свойство (**)
        Object.SetSignType( SignType )   # Установить свойство (**)
        ```
        """
        ...
    @SignType.setter
    def SignType(self, value: int): ...

    @property
    def X(self) -> float:  # icentremarker_x.html
        """
        ## X - Координата точки вставки по оси X

        Тип данных: double

        Свойство позволяет устанавливать и получать координату точки вставки по оси Х.

        ### Синтаксис Automation

        ```
        X = Object.X       # Получить свойство(* )
        Object.X = X       # Установить свойство (* )
        X = Object.GetX()  # Получить свойство (**)
        Object.SetX( X )   # Установить свойство (**)
        ```
        """
        ...
    @X.setter
    def X(self, value: float): ...

    @property
    def Y(self) -> float:  # icentremarker_y.html
        """
        ## Y - Координата точки вставки по оси Y

        Тип данных: double

        Свойство позволяет устанавливать и получать координату точки вставки по оси Y.

        ### Синтаксис Automation

        ```
        Y = Object.Y       # Получить свойство(* )
        Object.Y = Y       # Установить свойство (* )
        Y = Object.GetY()  # Получить свойство (**)
        Object.SetY( Y )   # Установить свойство (**)
        ```
        """
        ...
    @Y.setter
    def Y(self, value: float): ...

    def SemiAxisAutoLength(self, AxisType) -> bool:  # icentremarker_semiaxisautolength.html
        """
        ## SemiAxisAutoLength - Ассоциативное задание длины полуоси

        Тип данных: BOOL

        Свойство позволяет устанавливать и получать признак ассоциативного задания длины полуоси.

        ### Синтаксис Automation

        ```
        SemiAxisAutoLength = Object.SemiAxisAutoLength       # Получить свойство(* )
        Object.SemiAxisAutoLength = SemiAxisAutoLength       # Установить свойство (* )
        SemiAxisAutoLength = Object.GetSemiAxisAutoLength()  # Получить свойство (**)
        Object.SetSemiAxisAutoLength( SemiAxisAutoLength )   # Установить свойство (**)
        ```
        """
        ...

    def SemiAxisLength(self, AxisType) -> float:  # icentremarker_semiaxislength.html
        """
        ## SemiAxisLength - Длина полуоси

        Тип данных: double

        Свойство позволяет устанавливать и получать длину полуоси обозначения центра.

        ### Синтаксис Automation

        ```
        SemiAxisLength = Object.SemiAxisLength       # Получить свойство(* )
        Object.SemiAxisLength = SemiAxisLength       # Установить свойство (* )
        SemiAxisLength = Object.GetSemiAxisLength()  # Получить свойство (**)
        Object.SetSemiAxisLength( SemiAxisLength )   # Установить свойство (**)
        ```
        """
        ...

    def SetSemiAxisAutoLength(self, AxisType, arg1):
        ...

    def SetSemiAxisLength(self, AxisType, arg1):
        ...




class ICircularCentres(IDrawingObject):  # icircularcentres.html
    """
    ## Интерфейс ICircularCentres

    Интерфейс параметров круговой сетки центров
    """

    @property
    def BaseObjects(self) -> typing.Any:  # icircularcentres_baseobjects.html
        """
        ## BaseObjects - Объекты

        Тип данных: VARIANT

        Позволяет получать и устанавливать опорный объект или массив опорных объектов.

        Массив объектов возвращается в виде массива SAFEARRAY объектов LPDISPATCH (VT_ARRAY | VT_DISPATCH).

        ### Синтаксис Automation

        ```
        BaseObjects = Object.BaseObjects       # Получить свойство(* )
        Object.BaseObjects = BaseObjects       # Установить свойство (* )
        BaseObjects = Object.GetBaseObjects()  # Получить свойство (**)
        Object.SetBaseObjects( BaseObjects )   # Установить свойство (**)
        ```
        """
        ...
    @BaseObjects.setter
    def BaseObjects(self, value: typing.Any): ...

    @property
    def Centres(self) -> typing.Any:  # icircularcentres_centres.html
        """
        ## Centres - Координаты центров осевых линий

        Тип данных: VARIANT

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        Centres = Object.Centres       # Получить свойство(* )
        Centres = Object.GetCentres()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def Closed(self) -> bool:  # icircularcentres_closed.html
        """
        ## Closed - Замкнуть осевую

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        Closed = Object.Closed       # Получить свойство(* )
        Object.Closed = Closed       # Установить свойство (* )
        Closed = Object.GetClosed()  # Получить свойство (**)
        Object.SetClosed( Closed )   # Установить свойство (**)
        ```
        """
        ...
    @Closed.setter
    def Closed(self, value: bool): ...

    @property
    def Radiuses(self) -> typing.Any:  # icircularcentres_radiuses.html
        """
        ## Radiuses - Радиусы дуг и окружностей

        Тип данных: VARIANT

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        Radiuses = Object.Radiuses       # Получить свойство(* )
        Radiuses = Object.GetRadiuses()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def WithCenter(self) -> bool:  # icircularcentres_withcenter.html
        """
        ## WithCenter - С обозначением центра

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        WithCenter = Object.WithCenter       # Получить свойство(* )
        Object.WithCenter = WithCenter       # Установить свойство (* )
        WithCenter = Object.GetWithCenter()  # Получить свойство (**)
        Object.SetWithCenter( WithCenter )   # Установить свойство (**)
        ```
        """
        ...
    @WithCenter.setter
    def WithCenter(self, value: bool): ...

    @property
    def X0(self) -> float:  # icircularcentres_x0.html
        """
        ## X0 - Координата центра по X

        Тип данных: double

        ### Синтаксис Automation

        ```
        X0 = Object.X0       # Получить свойство(* )
        Object.X0 = X0       # Установить свойство (* )
        X0 = Object.GetX0()  # Получить свойство (**)
        Object.SetX0( X0 )   # Установить свойство (**)
        ```
        """
        ...
    @X0.setter
    def X0(self, value: float): ...

    @property
    def Y0(self) -> float:  # icircularcentres_y0.html
        """
        ## Y0 - Координата центра по Y

        Тип данных: double

        ### Синтаксис Automation

        ```
        Y0 = Object.Y0       # Получить свойство(* )
        Object.Y0 = Y0       # Установить свойство (* )
        Y0 = Object.GetY0()  # Получить свойство (**)
        Object.SetY0( Y0 )   # Установить свойство (**)
        ```
        """
        ...
    @Y0.setter
    def Y0(self, value: float): ...

    def AddCentre(self, X, Y, Radius) -> bool:  # icircularcentres_addcentre.html
        """
        ## AddCentre - Добавить обозначение центра в сетку

        ### Синтаксис Automation

        ```
        BOOL AddCentre( double X, double Y, double Radius );
        ```

        ### Входные параметры

        +------------------------------------------+
        | X, Y   | - координаты центра окружности. |
        +------------------------------------------+
        | Radius | - радиус.                       |
        +------------------------------------------+

        ### Возвращаемое значение

        +-----------------------------------------+
        | TRUE | - в случае успешного завершения. |
        +-----------------------------------------+
        """
        ...

    def AddCentreByObject(self, Object) -> bool:  # icircularcentres_addcentrebyobject.html
        """
        ## AddCentreByObject - Добавить обозначение центра в сетку

        ### Синтаксис Automation

        ```
        BOOL AddCentreByObject( IKompasAPIObject * Object );
        ```

        ### Входные параметры

        +-------------------------------------+
        | Object | - указатель на окружность. |
        +-------------------------------------+

        ### Возвращаемое значение

        +-----------------------------------------+
        | TRUE | - в случае успешного завершения. |
        +-----------------------------------------+
        """
        ...

    def AxisesCount(self, WithCenter) -> int:  # icircularcentres_axisescount.html
        """
        ## AxisesCount - Количество осевых линий в обозначении

        Тип данных: long

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        AxisesCount = Object.AxisesCount( WithCenter )     # Получить свойство(* )
        AxisesCount = Object.GetAxisesCount( WithCenter )  # Получить свойство (**)
        ```
        """
        ...

    def Clear(self) -> bool:  # icircularcentres_clear.html
        """
        ## Clear - Очистить список центров

        ### Синтаксис Automation

        ```
        BOOL Clear();
        ```

        ### Возвращаемое значение

        +-----------------------------------------+
        | TRUE | - в случае успешного завершения. |
        +-----------------------------------------+
        """
        ...

    def DeleteCentre(self, Index) -> bool:  # icircularcentres_deletecentre.html
        """
        ## DeleteCentre - Удалить обозначение центра из сетки

        ### Синтаксис Automation

        ```
        BOOL DeleteCentre( long Index );
        ```

        ### Входные параметры

        +--------------------------------------+
        | Index | - индекс обозначения центра. |
        +--------------------------------------+

        ### Возвращаемое значение

        +-----------------------------------------+
        | TRUE | - в случае успешного завершения. |
        +-----------------------------------------+
        """
        ...

    def DeleteCentreByPoint(self, X, Y) -> bool:  # icircularcentres_deletecentrebypoint.html
        """
        ## DeleteCentreByPoint - Удалить обозначение центра из сетки по координатам

        ### Синтаксис Automation

        ```
        BOOL DeleteCentreByPoint( double X, double Y );
        ```

        ### Входные параметры

        +-----------------------------------------+
        | X, Y | - координаты обозначения центра. |
        +-----------------------------------------+

        ### Возвращаемое значение

        +-----------------------------------------+
        | TRUE | - в случае успешного завершения. |
        +-----------------------------------------+
        """
        ...

    def SemiAxisAutoLength(self, WithCenter, Index, AxisType) -> bool:  # icircularcentres_semiaxisautolength.html
        """
        ## SemiAxisAutoLength - TRUE - ассоциативное задание длины полуоси

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        SemiAxisAutoLength = Object.SemiAxisAutoLength( WithCenter, Index, AxisType )     # Получить свойство(* )
        Object.SemiAxisAutoLength( WithCenter, Index, AxisType ) = SemiAxisAutoLength     # Установить свойство (* )
        SemiAxisAutoLength = Object.GetSemiAxisAutoLength( WithCenter, Index, AxisType )  # Получить свойство (**)
        Object.SetSemiAxisAutoLength( WithCenter, Index, AxisType, SemiAxisAutoLength )   # Установить свойство (**)
        ```

        ### Входные параметры

        +-------------------------------------------------------------------------------------------+
        | BOOL               | WithCenter - TRUE, устанавливать параметры центрального обозначения. |
        +-------------------------------------------------------------------------------------------+
        | long               | Index - индекс оси.                                                  |
        +-------------------------------------------------------------------------------------------+
        | ksSemiAxisTypeEnum | AxisType - индекс полуоси.                                           |
        +-------------------------------------------------------------------------------------------+
        """
        ...

    def SemiAxisLength(self, WithCenter, Index, AxisType) -> float:  # icircularcentres_semiaxislength.html
        """
        ## SemiAxisLength - Длина полуоси

        Тип данных: double

        ### Синтаксис Automation

        ```
        SemiAxisLength = Object.SemiAxisLength( WithCenter, Index, AxisType )     # Получить свойство(* )
        Object.SemiAxisLength( WithCenter, Index, AxisType ) = SemiAxisLength     # Установить свойство (* )
        SemiAxisLength = Object.GetSemiAxisLength( WithCenter, Index, AxisType )  # Получить свойство (**)
        Object.SetSemiAxisLength( WithCenter, Index, AxisType, SemiAxisLength )   # Установить свойство (**)
        ```

        ### Входные параметры

        +-------------------------------------------------------------------------------------------+
        | BOOL               | WithCenter - TRUE, устанавливать параметры центрального обозначения. |
        +-------------------------------------------------------------------------------------------+
        | long               | Index - индекс оси.                                                  |
        +-------------------------------------------------------------------------------------------+
        | ksSemiAxisTypeEnum | AxisType - индекс полуоси.                                           |
        +-------------------------------------------------------------------------------------------+
        """
        ...

    def SetSemiAxisAutoLength(self, WithCenter, Index, AxisType, arg3):
        ...

    def SetSemiAxisLength(self, WithCenter, Index, AxisType, arg3):
        ...




class IConditionIntersect(IDrawingObject):  # iconditionintersect.html
    """
    ## Интерфейс IConditionIntersect

    Интерфейс условного пересечения.

    Интерфейс можно получить с помощью метода коллекции условных пересечений IConditionIntersects::Add.

    Версия Компас v19
    """

    @property
    def Gap(self) -> bool:  # iconditionintersect_gap.html
        """
        ## Gap - Зазор или длина выносной линии

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        Gap = Object.Gap       # Получить свойство (* )
        Object.Gap = Gap       # Установить свойство (* )
        Gap = Object.GetGap()  # Получить свойство (**)
        Object.SetGap( Gap )   # Установить свойство (**)
        ```
        """
        ...
    @Gap.setter
    def Gap(self, value: bool): ...

    @property
    def GapValue(self) -> float:  # iconditionintersect_gapvalue.html
        """
        ## GapValue - Значение зазора или длины

        Тип данных: double

        ### Синтаксис Automation

        ```
        GapValue = Object.GapValue       # Получить свойство (* )
        Object.GapValue = GapValue       # Установить свойство (* )
        GapValue = Object.GetGapValue()  # Получить свойство (**)
        Object.SetGapValue( GapValue )   # Установить свойство (**)
        ```
        """
        ...
    @GapValue.setter
    def GapValue(self, value: float): ...

    @property
    def PointStyle(self) -> int:  # iconditionintersect_pointstyle.html
        """
        ## PointStyle - Стиль точки

        Тип данных: из перечисления ksAnnotationSymbolEnum.

        ### Синтаксис Automation

        ```
        PointStyle = Object.PointStyle       # Получить свойство (* )
        Object.PointStyle = PointStyle       # Установить свойство (* )
        PointStyle = Object.GetPointStyle()  # Получить свойство (**)
        Object.SetPointStyle( PointStyle )   # Установить свойство (**)
        ```
        """
        ...
    @PointStyle.setter
    def PointStyle(self, value: int): ...

    @property
    def PointVisible(self) -> bool:  # iconditionintersect_pointvisible.html
        """
        ## PointVisible - Отрисовывать точку пересечения

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        PointVisible = Object.PointVisible       # Получить свойство (* )
        Object.PointVisible = PointVisible       # Установить свойство (* )
        PointVisible = Object.GetPointVisible()  # Получить свойство (**)
        Object.SetPointVisible( PointVisible )   # Установить свойство (**)
        ```
        """
        ...
    @PointVisible.setter
    def PointVisible(self, value: bool): ...

    @property
    def RemoteLine1Visible(self) -> bool:  # iconditionintersect_remoteline1visible.html
        """
        ## RemoteLine1Visible - Признак отрисовки первой выносной линии

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        RemoteLine1Visible = Object.RemoteLine1Visible       # Получить свойство (* )
        Object.RemoteLine1Visible = RemoteLine1Visible       # Установить свойство (* )
        RemoteLine1Visible = Object.GetRemoteLine1Visible()  # Получить свойство (**)
        Object.SetRemoteLine1Visible( RemoteLine1Visible )   # Установить свойство (**)
        ```
        """
        ...
    @RemoteLine1Visible.setter
    def RemoteLine1Visible(self, value: bool): ...

    @property
    def RemoteLine2Visible(self) -> bool:  # iconditionintersect_remoteline2visible.html
        """
        ## RemoteLine2Visible - Признак отрисовки второй выносной линии

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        RemoteLine2Visible = Object.RemoteLine2Visible       # Получить свойство (* )
        Object.RemoteLine2Visible = RemoteLine2Visible       # Установить свойство (* )
        RemoteLine2Visible = Object.GetRemoteLine2Visible()  # Получить свойство (**)
        Object.SetRemoteLine2Visible( RemoteLine2Visible )   # Установить свойство (**)
        ```
        """
        ...
    @RemoteLine2Visible.setter
    def RemoteLine2Visible(self, value: bool): ...

    def AssociationObject(self, First) -> IDrawingObject:  # iconditionintersect_associationobject.html
        """
        ## AssociationObject - Получить кривую

        Тип данных: Указатель на интерфейс IDrawingObject.

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        AssociationObject = Object.AssociationObject( First )     # Получить свойство (* )
        AssociationObject = Object.GetAssociationObject( First )  # Получить свойство (**)
        ```
        """
        ...

    def GetCurvePoint(self, FirstCurve) -> bool:  # iconditionintersect_getcurvepoint.html
        """
        ## GetCurvePoint - Получить точку на кривой

        ### Синтаксис Automation

        ```
        BOOL GetCurvePoint( BOOL FirstCurve, double * X, double * Y );
        ```

        ### Входные параметры

        +----------------------------------------------+
        | FirstCurve | - получить точку первой кривой. |
        +----------------------------------------------+

        ### Выходные параметры

        +--------------------------------+
        | X | - первая координата точки, |
        +--------------------------------+
        | Y | - вторая координата точки. |
        +--------------------------------+

        ### Возвращаемое значение

        +-----------------------------------------+
        | TRUE | - в случае успешного завершения, |
        +-----------------------------------------+
        """
        ...

    def GetIntersectPoint(self) -> bool:  # iconditionintersect_getintersectpoint.html
        """
        ## GetIntersectPoint - Получить точку пересечения

        ### Синтаксис Automation

        ```
        BOOL GetIntersectPoint( double * X, double * Y );
        ```

        ### Выходные параметры

        +--------------------------------+
        | X | - первая координата точки, |
        +--------------------------------+
        | Y | - вторая координата точки. |
        +--------------------------------+

        ### Возвращаемое значение

        +-----------------------------------------+
        | TRUE | - в случае успешного завершения, |
        +-----------------------------------------+
        """
        ...

    def InitByObjects(self, Curve1, Curve2, X1, Y1, X2, Y2, RemoteLine1Visible, RemoteLine2Visible) -> bool:  # iconditionintersect_initbyobjects.html
        """
        ## InitByObjects - Инициализация по объектам

        ### Синтаксис Automation

        ```
        BOOL InitByObjects( LPDISPATCH Curve1, LPDISPATCH Curve2, double * X1, double * Y1, double * X2, double * Y2, BOOL RemoteLine1Visible, BOOL RemoteLine2Visible );
        ```

        ### Входные параметры

        +-----------------------------------------------------------+
        | Curve1             | - первая кривая,                     |
        +-----------------------------------------------------------+
        | Curve2             | - вторая кривая,                     |
        +-----------------------------------------------------------+
        | X1, Y1             | - координаты точки на первой кривой, |
        +-----------------------------------------------------------+
        | X2, Y2             | - координаты точки на второй кривой, |
        +-----------------------------------------------------------+
        | RemoteLine1Visible | - признак видимости первой кривой.   |
        +-----------------------------------------------------------+

        ### Возвращаемое значение

        +-----------------------------------------+
        | TRUE | - в случае успешного завершения, |
        +-----------------------------------------+
        """
        ...




class ILinearCentres(IDrawingObject):  # ilinearcentres.html
    """
    ## Интерфейс ILinearCentres

    Интерфейс параметров линейной сетки центров.
    """

    @property
    def AxisAngle(self) -> float:  # ilinearcentres_axisangle.html
        """
        ## AxisAngle - Наклон первой оси

        Тип данных: double

        ### Синтаксис Automation

        ```
        AxisAngle = Object.AxisAngle       # Получить свойство(* )
        Object.AxisAngle = AxisAngle       # Установить свойство (* )
        AxisAngle = Object.GetAxisAngle()  # Получить свойство (**)
        Object.SetAxisAngle( AxisAngle )   # Установить свойство (**)
        ```
        """
        ...
    @AxisAngle.setter
    def AxisAngle(self, value: float): ...

    @property
    def BaseObjects(self) -> typing.Any:  # ilinearcentres_baseobjects.html
        """
        ## BaseObjects - Объекты

        Тип данных: VARIANT

        Позволяет получать и устанавливать опорный объект или массив опорных объектов.

        Массив объектов возвращается в виде массива SAFEARRAY объектов LPDISPATCH (VT_ARRAY | VT_DISPATCH).

        ### Синтаксис Automation

        ```
        BaseObjects = Object.BaseObjects       # Получить свойство(* )
        Object.BaseObjects = BaseObjects       # Установить свойство (* )
        BaseObjects = Object.GetBaseObjects()  # Получить свойство (**)
        Object.SetBaseObjects( BaseObjects )   # Установить свойство (**)
        ```
        """
        ...
    @BaseObjects.setter
    def BaseObjects(self, value: typing.Any): ...

    @property
    def Centres(self) -> typing.Any:  # ilinearcentres_centres.html
        """
        ## Centres - Координаты центров осевых линий

        Тип данных: VARIANT

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        Centres = Object.Centres       # Получить свойство(* )
        Centres = Object.GetCentres()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def HasBreaks(self) -> bool:  # ilinearcentres_hasbreaks.html
        """
        ## HasBreaks - С разрывами

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        HasBreaks = Object.HasBreaks       # Получить свойство(* )
        Object.HasBreaks = HasBreaks       # Установить свойство (* )
        HasBreaks = Object.GetHasBreaks()  # Получить свойство (**)
        Object.SetHasBreaks( HasBreaks )   # Установить свойство (**)
        ```
        """
        ...
    @HasBreaks.setter
    def HasBreaks(self, value: bool): ...

    @property
    def Radiuses(self) -> typing.Any:  # ilinearcentres_radiuses.html
        """
        ## Radiuses - Радиусы дуг и окружностей

        Тип данных: VARIANT

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        Radiuses = Object.Radiuses       # Получить свойство(* )
        Radiuses = Object.GetRadiuses()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def TurnAngle(self) -> float:  # ilinearcentres_turnangle.html
        """
        ## TurnAngle - Угол раствора

        Тип данных: double

        ### Синтаксис Automation

        ```
        TurnAngle = Object.TurnAngle       # Получить свойство(* )
        Object.TurnAngle = TurnAngle       # Установить свойство (* )
        TurnAngle = Object.GetTurnAngle()  # Получить свойство (**)
        Object.SetTurnAngle( TurnAngle )   # Установить свойство (**)
        ```
        """
        ...
    @TurnAngle.setter
    def TurnAngle(self, value: float): ...

    def AddCentre(self, X, Y, Radius) -> bool:  # ilinearcentres_addcentre.html
        """
        ## AddCentre - Добавить обозначение центра в сетку

        ### Синтаксис Automation

        ```
        BOOL AddCentre( double X, double Y, double Radius );
        ```

        ### Входные параметры

        +-------------------------------------------+
        | X, Y   | - координаты обозначения центра. |
        +-------------------------------------------+
        | Radius | - радиус.                        |
        +-------------------------------------------+

        ### Возвращаемое значение

        +-----------------------------------------+
        | TRUE | - в случае успешного завершения. |
        +-----------------------------------------+
        """
        ...

    def AddCentreByObject(self, Object) -> bool:  # ilinearcentres_addcentrebyobject.html
        """
        ## AddCentreByObject - Добавить обозначение центрав сетку

        ### Синтаксис Automation

        ```
        BOOL AddCentreByObject( IKompasAPIObject * Object );
        ```

        ### Входные параметры

        +-------------------------------------+
        | Object | - Указатель на окружность. |
        +-------------------------------------+

        ### Возвращаемое значение

        +-----------------------------------------+
        | TRUE | - в случае успешного завершения. |
        +-----------------------------------------+
        """
        ...

    def Clear(self) -> bool:  # ilinearcentres_clear.html
        """
        ## Clear - Очистить список центров

        ### Синтаксис Automation

        ```
        BOOL Clear();
        ```

        ### Возвращаемое значение

        +-----------------------------------------+
        | TRUE | - в случае успешного завершения. |
        +-----------------------------------------+
        """
        ...

    def DeleteCentre(self, Index) -> bool:  # ilinearcentres_deletecentre.html
        """
        ## DeleteCentre - Удалить обозначение центра в сетку

        ### Синтаксис Automation

        ```
        BOOL DeleteCentre( long Index );
        ```

        ### Входные параметры

        +--------------------------------------+
        | Index | - индекс обозначения центра. |
        +--------------------------------------+

        ### Возвращаемое значение

        +-----------------------------------------+
        | TRUE | - в случае успешного завершения. |
        +-----------------------------------------+
        """
        ...

    def DeleteCentreByPoint(self, X, Y) -> bool:  # ilinearcentres_deletecentrebypoint.html
        """
        ## DeleteCentreByPoint - Удалить обозначение центраиз сетки по координатам

        ### Синтаксис Automation

        ```
        BOOL DeleteCentreByPoint( double X, double Y );
        ```

        ### Входные параметры

        +----------------------------------------+
        | X,Y | - координаты обозначения центра. |
        +----------------------------------------+

        ### Возвращаемое значение

        +-----------------------------------------+
        | TRUE | - в случае успешного завершения. |
        +-----------------------------------------+
        """
        ...




class ICutLine(IDrawingObject):  # icutline.html
    """
    ## Интерфейс ICutLine

    Интерфейс линии разреза/сечения.

    1. Интерфейс позволяет получить и задать свойства объекта "линия разреза/сечения"

    2. Получить интерфейс можно, используя свойство ICutLines::CutLine или метод ICutLines::Add интерфейса коллекции линий разреза/сечения.

    3. После задания параметров объекта требуется вызвать метод IDrawingObject::Update.
    """

    @property
    def AdditionalText(self) -> IText:  # icutline_additionaltext.html
        """
        ## AdditionalText - Дополнительный текст линии разреза/сечения

        Тип данных: указатель на интерфейс IText

        1. Свойство доступно только для чтения.

        2. Свойство позволяет получить указатель на интерфейс текста IText для редактирования дополнительного текста линии разреза/сечения.

        ### Синтаксис Automation

        ```
        AdditionText = Object.AdditionText       # Получить свойство(* )
        AdditionText = Object.GetAdditionText()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def AdditionalTextPos(self) -> bool:  # icutline_additionaltextpos.html
        """
        ## AdditionalTextPos - Размещение дополнительного текста

        Тип данных: BOOL

        Значения свойства:

        +-----------------------------+
        | TRUE  | - у первой стрелки, |
        +-----------------------------+
        | FALSE | - у второй.         |
        +-----------------------------+

        По умолчанию значение свойства равно TRUE.

        ### Синтаксис Automation

        ```
        AdditionTextPos = Object.AdditionTextPos       # Получить свойство(* )
        Object.AdditionTextPos = AdditionTextPos       # Установить свойство (* )
        AdditionTextPos = Object.GetAdditionTextPos()  # Получить свойство (**)
        Object.SetAdditionTextPos( AdditionTextPos )   # Установить свойство (**)
        ```
        """
        ...
    @AdditionalTextPos.setter
    def AdditionalTextPos(self, value: bool): ...

    @property
    def ArrowPos(self) -> bool:  # icutline_arrowpos.html
        """
        ## ArrowPos - Расположение стрелок

        Тип данных: BOOL

        Значения свойства:

        +------------------------------------------+
        | TRUE  | - слева по направлению ломаной,  |
        +------------------------------------------+
        | FALSE | - справа по направлению ломаной. |
        +------------------------------------------+

        ### Синтаксис Automation

        ```
        ArrowPos = Object.ArrowPos       # Получить свойство(* )
        Object.ArrowPos = ArrowPos       # Установить свойство (* )
        ArrowPos = Object.GetArrowPos()  # Получить свойство (**)
        Object.SetArrowPos( ArrowPos )   # Установить свойство (**)
        ```
        """
        ...
    @ArrowPos.setter
    def ArrowPos(self, value: bool): ...

    @property
    def ArrowType(self) -> int:  # icutline_arrowtype.html
        """
        ## ArrowType - Тип стрелки

        Тип данных: из перечисления ksArrowEnum

        ### Синтаксис Automation

        ```
        ArrowType = Object.ArrowType       # Получить свойство(* )
        Object.ArrowType = ArrowType       # Установить свойство (* )
        ArrowType = Object.GetArrowType()  # Получить свойство (**)
        Object.SetArrowType( ArrowType )   # Установить свойство (**)
        ```
        """
        ...
    @ArrowType.setter
    def ArrowType(self, value: int): ...

    @property
    def AutoSheet(self) -> bool:  # icutline_autosheet.html
        """
        ## AutoSheet - Лист

        Тип данных: BOOL

        Значения свойства:

        +----------------------------+
        | TRUE  | - включить опцию,  |
        +----------------------------+
        | FALSE | - отключить опцию. |
        +----------------------------+

        По умолчанию значение свойства равно FALSE.

        ### Синтаксис Automation

        ```
        AutoSorted = Object.AutoSorted       # Получить свойство(* )
        Object.AutoSorted = AutoSorted       # Установить свойство (* )
        AutoSorted = Object.GetAutoSorted()  # Получить свойство (**)
        Object.SetAutoSorted( AutoSorted )   # Установить свойство (**)
        ```
        """
        ...
    @AutoSheet.setter
    def AutoSheet(self, value: bool): ...

    @property
    def AutoSorted(self) -> bool:  # icutline_autosorted.html
        """
        ## AutoSorted - Автосортировка

        Тип данных: BOOL

        Значения свойства:

        +-------------------------------------+
        | TRUE  | - автосортировка включена,  |
        +-------------------------------------+
        | FALSE | - автосортировка выключена. |
        +-------------------------------------+

        1. По умолчанию значение свойства равно TRUE.

        2. При отключенной опции доступно редактирование основного текста линии разреза/сечения с помощью свойства ICutLine::Text.

        ### Синтаксис Automation

        ```
        AutoSorted = Object.AutoSorted       # Получить свойство(* )
        Object.AutoSorted = AutoSorted       # Установить свойство (* )
        AutoSorted = Object.GetAutoSorted()  # Получить свойство (**)
        Object.SetAutoSorted( AutoSorted )   # Установить свойство (**)
        ```
        """
        ...
    @AutoSorted.setter
    def AutoSorted(self, value: bool): ...

    @property
    def AutoZone(self) -> bool:  # icutline_autozone.html
        """
        ## AutoZone - Зона

        Тип данных: BOOL

        Значения свойства:

        +----------------------------+
        | TRUE  | - включить опцию,  |
        +----------------------------+
        | FALSE | - отключить опцию. |
        +----------------------------+

        По умолчанию значение свойства равно FALSE.

        ### Синтаксис Automation

        ```
        AutoZone = Object.AutoZone       # Получить свойство(* )
        Object.AutoZone = AutoZone       # Установить свойство (* )
        AutoZone = Object.GetAutoZone()  # Получить свойство (**)
        Object.SetAutoZone( AutoZone )   # Установить свойство (**)
        ```
        """
        ...
    @AutoZone.setter
    def AutoZone(self, value: bool): ...

    @property
    def Points(self) -> typing.Any:  # icutline_points.html
        """
        ## Points - Массив координат точек линии разреза/сечения в виде SAFEARRAY double - VT_ARRAY | VT_R8

        Тип данных: VARIANT

        Координаты точек располагаются в массиве в следующем порядке:

        x0, y0 (начальная точка), x1, y1, ... x2, y2, ... (точки перегибов), xn, yn (конечная точка). Количество элементов массива должно быть четным.

        ### Синтаксис Automation

        ```
        Points = Object.Points       # Получить свойство(* )
        Object.Points = Points       # Установить свойство (* )
        Points = Object.GetPoints()  # Получить свойство (**)
        Object.SetPoints( Points )   # Установить свойство (**)
        ```
        """
        ...
    @Points.setter
    def Points(self, value: typing.Any): ...

    @property
    def Text(self) -> IText:  # icutline_text.html
        """
        ## Text - Текст обозначения линии разреза/сечения

        Тип данных: указатель на интерфейс IText

        1. Свойство доступно только для чтения.

        2. Свойство позволяет получить указатель на интерфейс текста IText для редактирования основного текста линии разреза/сечения. Указатель на интерфейс IText вернется только в случае отключенной опции Автосортировка. Если автосортировка текста включена, свойство вернёт NULL.

        ### Синтаксис Automation

        ```
        Text = Object.Text       # Получить свойство(* )
        Text = Object.GetText()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def X1(self) -> float:  # x1.html
        """
        ## X1 - Координата начального текста по X

        Тип данных: double

        ### Синтаксис Automation

        ```
        X1 = Object.X1       # Получить свойство(* )
        Object.X1 = X1       # Установить свойство (* )
        X1 = Object.GetX1()  # Получить свойство (**)
        Object.SetX1( X1 )   # Установить свойство (**)
        ```
        """
        ...
    @X1.setter
    def X1(self, value: float): ...

    @property
    def X2(self) -> float:  # icutline_x2.html
        """
        ## X2 - Координата конечного текста по X

        Тип данных: double

        ### Синтаксис Automation

        ```
        X2 = Object.X2       # Получить свойство(* )
        Object.X2 = X2       # Установить свойство (* )
        X2 = Object.GetX2()  # Получить свойство (**)
        Object.SetX2( X2 )   # Установить свойство (**)
        ```
        """
        ...
    @X2.setter
    def X2(self, value: float): ...

    @property
    def Y1(self) -> float:  # icutline_y1.html
        """
        ## Y1 - Координата начального текста по Y

        Тип данных: double

        ### Синтаксис Automation

        ```
        Y1 = Object.Y1       # Получить свойство(* )
        Object.Y1 = Y1       # Установить свойство (* )
        Y1 = Object.GetY1()  # Получить свойство (**)
        Object.SetY1( Y1 )   # Установить свойство (**)
        ```
        """
        ...
    @Y1.setter
    def Y1(self, value: float): ...

    @property
    def Y2(self) -> float:  # icutline_y2.html
        """
        ## Y2 - Координата конечного текста по Y

        Тип данных: double

        ### Синтаксис Automation

        ```
        Y2 = Object.Y2       # Получить свойство(* )
        Object.Y2 = Y2       # Установить свойство (* )
        Y2 = Object.GetY2()  # Получить свойство (**)
        Object.SetY2( Y2 )   # Установить свойство (**)
        ```
        """
        ...
    @Y2.setter
    def Y2(self, value: float): ...




class IDrawingTable(IDrawingObject):  # idrawingtable.html
    """
    ## Интерфейс IDrawingTable

    Интерфейс таблицы на чертеже.

    1. Получить интерфейс можно, используя свойство интерфейса коллекции таблиц IDrawingTables::DrawingTable.

    2. Посредством вызова метода IUnknown::QueryInterface интерфейс позволяет получить интерфейс сетки таблицы ITable.

    3. Для того, чтобы сузить текст в ячейке таблицы, необходимо:

    •снять признак запрета на редактирование ячейки с помощью свойства ICellFormat::ReadOnly и изменить текст,

    •вызвать метод IDrawingObject::Update,

    •установить признак запрета на редактирование ячейки,

    •вызвать метод IDrawingObject::Update.
    """

    @property
    def Angle(self) -> float:  # idrawingtable_angle.html
        """
        ## Angle - Угол наклона таблицы

        Тип данных: double

        Свойство позволяет устанавливать и получать угол наклона таблицы.

        ### Синтаксис Automation

        ```
        Angle = Object.Angle        # Получить свойство(* )
        Object.Angle = Angle        # Установить свойство (* )
        Angle = Object.GetAngle( )  # Получить свойство (**)
        Object.SetAngle( Angle )    # Установить свойство (**)
        ```
        """
        ...
    @Angle.setter
    def Angle(self, value: float): ...

    @property
    def FixedCellsSize(self) -> bool:  # idrawingtable_fixedcellssize.html
        """
        ## FixedCellsSize - Зафиксировать габариты ячеек

        Тип данных: BOOL

        Свойство позволяет устанавливать и получать признак фиксирования габаритов ячеек.

        ### Синтаксис Automation

        ```
        FixedCellsSize = Object.FixedCellsSize        # Получить свойство(* )
        Object.FixedCellsSize = FixedCellsSize        # Установить свойство (* )
        FixedCellsSize = Object.GetFixedCellsSize( )  # Получить свойство (**)
        Object.SetFixedCellsSize( FixedCellsSize )    # Установить свойство (**)
        ```
        """
        ...
    @FixedCellsSize.setter
    def FixedCellsSize(self, value: bool): ...

    @property
    def FixedColumnCount(self) -> bool:  # idrawingtable_fixedcolumncount.html
        """
        ## FixedColumnCount - Запретить изменять число столбцов

        Тип данных: BOOL

        Свойство позволяет устанавливать и получать признак запрета на изменение числа столбцов.

        ### Синтаксис Automation

        ```
        FixedColumnCount = Object.FixedColumnCount        # Получить свойство(* )
        Object.FixedColumnCount = FixedColumnCount        # Установить свойство (* )
        FixedColumnCount = Object.GetFixedColumnCount( )  # Получить свойство (**)
        Object.SetFixedColumnCount( FixedColumnCount )    # Установить свойство (**)
        ```
        """
        ...
    @FixedColumnCount.setter
    def FixedColumnCount(self, value: bool): ...

    @property
    def FixedRowCount(self) -> bool:  # idrawingtable_fixedrowcount.html
        """
        ## FixedRowCount - Запретить изменять число строк

        Тип данных: BOOL

        Свойство позволяет устанавливать и получать признак запрета на изменение числа строк.

        ### Синтаксис Automation

        ```
        FixedRowCount = Object.FixedRowCount        # Получить свойство(* )
        Object.FixedRowCount = FixedRowCount        # Установить свойство (* )
        FixedRowCount = Object.GetFixedRowCount( )  # Получить свойство (**)
        Object.SetFixedRowCount( FixedRowCount )    # Установить свойство (**)
        ```
        """
        ...
    @FixedRowCount.setter
    def FixedRowCount(self, value: bool): ...

    @property
    def NumericGroup(self) -> INumericGroup:  # idrawingtable_numericgroup.html
        """
        ## NumericGroup - Группа нумерации

        Тип данных: Указатель на интерфейс INumericGroup

        ### Синтаксис Automation

        ```
        NumericGroup = Object.NumericGroup       # Получить свойство(* )
        Object.NumericGroup = NumericGroup       # Установить свойство (* )
        NumericGroup = Object.GetNumericGroup()  # Получить свойство (**)
        Object.SetNumericGroup( NumericGroup )   # Установить свойство (**)
        ```
        """
        ...
    @NumericGroup.setter
    def NumericGroup(self, value: INumericGroup): ...

    @property
    def Title(self) -> IText:  # idrawingtable_title.html
        """
        ## Title - Заголовок

        Тип данных: Указатель на интерфейс IText

        Свойство доступно только для чтения.

        Версия Компас v21

        ### Синтаксис Automation

        ```
        Title = Object.Title       # Получить свойство (* )
        Title = Object.GetTitle()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def X(self) -> float:  # idrawingtable_x.html
        """
        ## X - Координата точки привязки по X

        Тип данных: double

        Свойство позволяет устанавливать и получать координату точки привязки по X.

        ### Синтаксис Automation

        ```
        X = Object.X        # Получить свойство(* )
        Object.Х = X        # Установить свойство (* )
        X = Object.GetX( )  # Получить свойство (**)
        Object.SetХ( X )    # Установить свойство (**)
        ```
        """
        ...
    @X.setter
    def X(self, value: float): ...

    @property
    def Y(self) -> float:  # idrawingtable_y.html
        """
        ## Y - Координата точки привязки по Y

        Тип данных: double

        Свойство позволяет устанавливать и получать координату точки привязки по Y.

        ### Синтаксис Automation

        ```
        Y = Object.Y        # Получить свойство(* )
        Object.Y = Y        # Установить свойство (* )
        Y = Object.GetY( )  # Получить свойство (**)
        Object.SetY( Y )    # Установить свойство (**)
        ```
        """
        ...
    @Y.setter
    def Y(self, value: float): ...

    def Save(self, FileName) -> bool:  # idrawingtable_save.html
        """
        ## Save - Сохранить в файл

        Метод позволяет сохранить таблицу в файл.

        ### Синтаксис Automation

        ```
        BOOL Save( BSTR fileName );
        ```

        ### Входные параметры

        +---------------------------------+
        | fileName | - имя файла таблицы. |
        +---------------------------------+

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...




class IDrawingText(IDrawingObject):  # idrawingtext.html
    """
    ## Интерфейс IDrawingText

    Интерфейс текста на чертеже.

    1. Интерфейс позволяет создать текст на чертеже. В общем случае это может быть многострочный, отформатированный по высоте и горизонтали текст.

    2. У данного интерфейса можно получить дополнительный интерфейс IText посредством вызова метода IUnknown::QueryInterface (const GUID far& iid, void** pif).

    3. Интерфейс можно получить у интерфейса коллекции текстов на чертеже, используя свойство IDrawingTexts::DrawingText.
    """

    @property
    def Allocation(self) -> int:  # idrawingtext_allocation.html
        """
        ## Allocation - Размещение текста относительно точки привязки

        Тип данных: из перечисления ksAllocationEnum

        Свойство позволяет установить и получить тип размещения точки привязки текста.

        ### Синтаксис Automation

        ```
        Allocation = iObject.Allocation;       # Получить свойство(* )
        iObject.Allocation = Allocation;       # Установить свойство (* )
        Allocation = iObject.GetAllocation();  # Получить свойство (**)
        iObject.SetAllocation(Allocation);     # Установить свойство (**)
        ```
        """
        ...
    @Allocation.setter
    def Allocation(self, value: int): ...

    @property
    def Angle(self) -> float:  # idrawingtext_angle.html
        """
        ## Angle - Угол наклона текста

        Тип данных: double, размерность - градусы

        Свойство позволяет получить и установить угол наклона текста.

        ### Синтаксис Automation

        ```
        Angle = iObject.Angle;       # Получить свойство(* )
        iObject.Angle = Angle;       # Установить свойство (* )
        Angle = iObject.GetAngle();  # Получить свойство (**)
        iObject.SetAngle( Angle );   # Установить свойство (**)
        ```
        """
        ...
    @Angle.setter
    def Angle(self, value: float): ...

    @property
    def HFormat(self) -> int:  # idrawingtext_hformat.html
        """
        ## HFormat - Признак горизонтального форматирования

        Тип данных: из перечисления ksTextHorizontalFormatEnum

        Свойство позволяет установить и получить признак горизонтального форматирования.

        ### Синтаксис Automation

        ```
        HFormat = iObject.HFormat;       # Получить свойство(* )
        iObject.HFormat = HFormat;       # Установить свойство (* )
        HFormat = iObject.GetHFormat();  # Получить свойство (**)
        iObject.SetHFormat(HFormat);     # Установить свойство (**)
        ```
        """
        ...
    @HFormat.setter
    def HFormat(self, value: int): ...

    @property
    def Height(self) -> float:  # idrawingtext_height.html
        """
        ## Height - Высота блока форматирования

        Тип данных: double

        Свойство позволяет установить и получить высоту блока форматирования.

        ### Синтаксис Automation

        ```
        Height = iObject.Height;       # Получить свойство(* )
        iObject.Height = Height;       # Установить свойство (* )
        Height = iObject.GetHeight();  # Получить свойство (**)
        iObject.SetHeight( Height );   # Установить свойство (**)
        ```
        """
        ...
    @Height.setter
    def Height(self, value: float): ...

    @property
    def IsTextMark(self) -> bool:  # idrawingtext_istextmark.html
        """
        ## IsTextMark - True - текст является текстовой меткой

        Тип данных: BOOL

        Свойство доступно только для чтения.

        Версия: Компас v20

        ### Синтаксис Automation

        ```
        IsTextMark = Object.IsTextMark       # Получить свойство(* )
        IsTextMark = Object.GetIsTextMark()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def MirrorSymmetry(self) -> bool:  # idrawingtext_mirrorsymmetry.html
        """
        ## MirrorSymmetry - Отображать зеркально

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        MirrorSymmetry = Object.MirrorSymmetry       # Получить свойство(* )
        Object.MirrorSymmetry = MirrorSymmetry       # Установить свойство (* )
        MirrorSymmetry = Object.GetMirrorSymmetry()  # Получить свойство (**)
        Object.SetMirrorSymmetry( MirrorSymmetry )   # Установить свойство (**)
        ```
        """
        ...
    @MirrorSymmetry.setter
    def MirrorSymmetry(self, value: bool): ...

    @property
    def VFormat(self) -> bool:  # idrawingtext_vformat.html
        """
        ## VFormat - Признак вертикального форматирования

        Тип данных: BOOL

        Значения свойства:

        +---------------------------------+
        | TRUE  | - изменение шага строк, |
        +---------------------------------+
        | FALSE | - нет форматирования.   |
        +---------------------------------+

        Свойство позволяет установить и получить признак вертикального форматирования.

        ### Синтаксис Automation

        ```
        VFormat = iObject.VFormat;       # Получить свойство(* )
        iObject.VFormat = VFormat;       # Установить свойство (* )
        VFormat = iObject.GetVFormat();  # Получить свойство (**)
        iObject.SetVFormat(VFormat);     # Установить свойство (**)
        ```
        """
        ...
    @VFormat.setter
    def VFormat(self, value: bool): ...

    @property
    def Width(self) -> float:  # idrawingtext_width.html
        """
        ## Width - Ширина блока форматирования

        Тип данных: double

        Свойство позволяет установить и получить ширину блока форматирования.

        ### Синтаксис Automation

        ```
        Width = iObject.Width;       # Получить свойство(* )
        iObject.Width = Width;       # Установить свойство (* )
        Width = iObject.GetWidth();  # Получить свойство (**)
        iObject.SetWidth( Width );   # Установить свойство (**)
        ```
        """
        ...
    @Width.setter
    def Width(self, value: float): ...

    @property
    def X(self) -> float:  # idrawingtext_x.html
        """
        ## X - Координата точки привязки по Х

        Тип данных: double

        Свойство позволяет получить и установить координату точки привязки по Х.

        ### Синтаксис Automation

        ```
        X = iObject.X;       # Получить свойство(* )
        iObject.X = X;       # Установить свойство (* )
        X = iObject.GetX();  # Получить свойство (**)
        iObject.SetX( X );   # Установить свойство (**)
        ```
        """
        ...
    @X.setter
    def X(self, value: float): ...

    @property
    def Y(self) -> float:  # idrawingtext_y.html
        """
        ## Y - Координата точки привязки по Y

        Тип данных: double

        Свойство позволяет получить и установить координату точки привязки по Y.

        ### Синтаксис Automation

        ```
        Y = iObject.Y;       # Получить свойство(* )
        iObject.Y = Y;       # Установить свойство (* )
        Y = iObject.GetY();  # Получить свойство (**)
        iObject.SetY(Y);     # Установить свойство (**)
        ```
        """
        ...
    @Y.setter
    def Y(self, value: float): ...




class IRough(IDrawingObject):  # irough.html
    """
    ## Интерфейс IRough

    Интерфейс обозначения шероховатости.

    Интерфейс позволяет получить и задать свойства обозначения шероховатости.

    Интерфейс можно получить у коллекции обозначений шероховатости, используя свойство IRoughs::Rough или метод IRoughs::Add.

    После задания параметров размера требуется вызвать метод IDrawingObject::Update.

    Интерфейс IRoughParams является дополнительным. Его можно получить с помощью метода IUnknown::QueryInterface.
    """

    @property
    def BaseObject(self) -> IDrawingObject:  # irough_baseobject.html
        """
        ## BaseObject - Опорный объект

        Тип данных: указатель на интерфейс IDrawingObject

        ### Синтаксис Automation

        ```
        BaseObject = Object.BaseObject       # Получить свойство(* )
        Object.BaseObject = BaseObject       # Установить свойство (* )
        BaseObject = Object.GetBaseObject()  # Получить свойство (**)
        Object.SetBaseObject( BaseObject )   # Установить свойство (**)
        ```
        """
        ...
    @BaseObject.setter
    def BaseObject(self, value: IDrawingObject): ...

    @property
    def BranchX0(self) -> float:  # irough_branchx0.html
        """
        ## BranchX0 - Координата начала выносной линии или положение знака по Х

        Тип данных: double

        ### Синтаксис Automation

        ```
        BranchX0 = Object.BranchX0       # Получить свойство(* )
        Object.BranchX0 = BranchX0       # Установить свойство (* )
        BranchX0 = Object.GetBranchX0()  # Получить свойство (**)
        Object.SetBranchX0( BranchX0 )   # Установить свойство (**)
        ```
        """
        ...
    @BranchX0.setter
    def BranchX0(self, value: float): ...

    @property
    def BranchY0(self) -> float:  # irough_branchy0.html
        """
        ## BranchY0 - Координата начала выносной линии или положение знака по Y

        Тип данных: double

        ### Синтаксис Automation

        ```
        BranchY0 = Object.BranchY0       # Получить свойство(* )
        Object.BranchY0 = BranchY0       # Установить свойство (* )
        BranchY0 = Object.GetBranchY0()  # Получить свойство (**)
        Object.SetBranchY0( BranchY0 )   # Установить свойство (**)
        ```
        """
        ...
    @BranchY0.setter
    def BranchY0(self, value: float): ...

    @property
    def ShelfX(self) -> float:  # irough_shelfx.html
        """
        ## ShelfX - Координата начала полки по Х

        Тип данных: double

        ### Синтаксис Automation

        ```
        ShelfX = Object.ShelfX       # Получить свойство(* )
        Object.ShelfX = ShelfX       # Установить свойство (* )
        ShelfX = Object.GetShelfX()  # Получить свойство (**)
        Object.SetShelfX( ShelfX )   # Установить свойство (**)
        ```
        """
        ...
    @ShelfX.setter
    def ShelfX(self, value: float): ...

    @property
    def ShelfY(self) -> float:  # irough_shelfy.html
        """
        ## ShelfY - Координата начала полки по Y

        Тип данных: double

        ### Синтаксис Automation

        ```
        ShelfY = Object.ShelfY       # Получить свойство(* )
        Object.ShelfY = ShelfY       # Установить свойство (* )
        ShelfY = Object.GetShelfY()  # Получить свойство (**)
        Object.SetShelfY( ShelfY )   # Установить свойство (**)
        ```
        """
        ...
    @ShelfY.setter
    def ShelfY(self, value: float): ...

    def BranchArrowInside(self, Index) -> bool:  # irough_brancharrowinside.html
        """
        ## BranchArrowInside - Расположение стрелки линии-выноски (TRUE- внутри, FALSE- снаружи)

        Тип данных: BOOL

        +-------------------------------------------------------------------------------------+
        | BranchArrowInside = Object.BranchArrowInside( Index )    | Получить свойство(* )    |
        +-------------------------------------------------------------------------------------+
        | Object.BranchArrowInside( Index ) = BranchArrowInside)   | Установить свойство (* ) |
        +-------------------------------------------------------------------------------------+
        | BranchArrowInside = Object.GetBranchArrowInside( Index ) | Получить свойство (**)   |
        +-------------------------------------------------------------------------------------+
        | Object.SetBranchArrowInside( Index, BranchArrowInside )  | Установить свойство (**) |
        +-------------------------------------------------------------------------------------+

        ### Входные параметры

        +------------------------------+
        | Index | - Индекс ответвления |
        +------------------------------+
        """
        ...

    def BranchArrowType(self, Index) -> int:  # irough_brancharrowtype.html
        """
        ## BranchArrowType - Тип значка на ответвлении

        Тип данных: из перечисления ksArrowEnum

        ### Синтаксис Automation

        ```
        BranchArrowType = Object.BranchArrowType( Index )     # Получить свойство(* )
        Object.BranchArrowType( Index ) = BranchArrowType     # Установить свойство (* )
        BranchArrowType = Object.GetBranchArrowType( Index )  # Получить свойство (**)
        Object.SetBranchArrowType( Index, BranchArrowType )   # Установить свойство (**)
        ```

        ### Входные параметры

        +------------------------------+
        | Index | - Индекс ответвления |
        +------------------------------+
        """
        ...

    def SetBranchArrowInside(self, Index, arg1):
        ...

    def SetBranchArrowType(self, Index, arg1):
        ...




class ITolerance(IDrawingObject):  # itolerance.html
    """
    ## Интерфейс ITolerance

    Интерфейс обозначения допуска формы 2D.

    1. Получить интерфейс можно, используя свойство интерфейса коллекции ITolerances::Tolerance или метод ITolerances::Add.

    2. Посредством вызова метода IUnknown::QueryInterface (const GUID far& iid, void** pif) интерфейс позволяет получить следующие дополнительные интерфейсы:

    •IToleranceParam - интерфейс параметров допуска формы,

    •IBranchs - интерфейс для работы с ответвлениями,

    •ITable - интерфейс таблицы.

    3. После задания параметров объекта требуется вызвать метод IDrawingObject::Update.
    """

    def ArrowType(self, Index) -> bool:  # itolerance_arrowtype.html
        """
        ## ArrowType - Тип стрелки ответвления

        Тип данных: BOOL

        Значения свойства:

        +------------------------+
        | TRUE  | - стрелка,     |
        +------------------------+
        | FALSE | - треугольник. |
        +------------------------+

        Свойство позволяет устанавливать и получать тип стрелки ответвления.

        ### Синтаксис Automation

        ```
        ArrowType = Object.ArrowType( Index )     # Получить свойство(* )
        Object.ArrowType( Index ) = ArrowType     # Установить свойство (* )
        ArrowType = Object.GetArrowType( Index )  # Получить свойство (**)
        Object.SetArrowType( Index, ArrowType )   # Установить свойство (**)
        ```

        ### Входные параметры

        +----------------------------------------+
        | Index | - индекс ответвления ( long ). |
        +----------------------------------------+
        """
        ...

    def BranchPos(self, Index) -> int:  # itolerance_branchpos.html
        """
        ## BranchPos - Положение ножки

        Тип данных: из перечисления ksTablePointEnum

        Свойство позволяет устанавливать и получать тип положения ножки ответвления.

        ### Синтаксис Automation

        ```
        BranchPos = Object.BranchPos( Index )     # Получить свойство(* )
        Object.BranchPos( Index ) = BranchPos     # Установить свойство (* )
        BranchPos = Object.GetBranchPos( Index )  # Получить свойство (**)
        Object.SetBranchPos( Index, BranchPos )   # Установить свойство (**)
        ```

        ### Входные параметры

        +----------------------------------------+
        | Index | - индекс ответвления ( long ). |
        +----------------------------------------+
        """
        ...

    def FormatTolerance(self) -> bool:  # itolerance_formattolerance.html
        """
        ## FormatTolerance - Форматировать таблицу допуска формы

        Функция выполняет обновление таблицы допуска формы.

        Пустые столбцы и строки в начале и в конце таблицы будут удалены.

        Границы пустых граничных ячеек в непустых столбцах будут удалены.

        Версия: Компас v20

        ### Синтаксис Automation

        ```
        BOOL FormatTolerance();
        ```

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def SetArrowType(self, Index, arg1):
        ...

    def SetBranchPos(self, Index, arg1):
        ...

    def SetToleranceArrowType(self, Index, arg1):
        ...

    def ToleranceArrowType(self, Index) -> int:  # itolerance_tolerancearrowtype.html
        """
        ## ToleranceArrowType - Тип стрелки ответвления

        Тип данных: из перечисления ksToleranceArrowType

        ### Синтаксис Automation

        ```
        ToleranceArrowType = Object.ToleranceArrowType( Index )     # Получить свойство(* )
        Object.ToleranceArrowType( Index ) = ToleranceArrowType     # Установить свойство (* )
        ToleranceArrowType = Object.GetToleranceArrowType( Index )  # Получить свойство (**)
        Object.SetToleranceArrowType( Index, ToleranceArrowType )   # Установить свойство (**)
        ```

        ### Входные параметры

        +-------------------------------+
        | Index | - индекс ответвления. |
        +-------------------------------+
        """
        ...




class IViewPointer(IDrawingObject):  # iviewpointer.html
    """
    ## Интерфейс IViewPointer

    Интерфейс стрелки взгляда.

    Интерфейс можно получить, используя свойство коллекции стрелок взгляда IViewPointers::ViewPointer или IModelObjects::Item или метод IViewPointers::Add.
    """

    @property
    def AdditionalText(self) -> IText:  # iviewpointer_additionaltext.html
        """
        ## AdditionalText - Дополнительный текст

        Тип данных: указатель на интерфейс IText

        1. Свойство позволяет получить интерфейс дополнительного текста обозначения направления взгляда.

        2. Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        AdditionalText = Object.AdditionalText       # Получить свойство(* )
        AdditionalText = Object.GetAdditionalText()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def ArrowType(self) -> int:  # iviewpointer_arrowtype.html
        """
        ## ArrowType - Тип стрелки

        Тип данных: из перечисления ksArrowEnum

        ### Синтаксис Automation

        ```
        ArrowType = Object.ArrowType        # Получить свойство(* )
        Object.ArrowType = ArrowType        # Установить свойство (* )
        ArrowType = Object.GetArrowType( )  # Получить свойство (**)
        Object.SetArrowType( ArrowType )    # Установить свойство (**)
        ```
        """
        ...
    @ArrowType.setter
    def ArrowType(self, value: int): ...

    @property
    def AutoSheet(self) -> bool:  # iviewpointer_autosheet.html
        """
        ## AutoSheet - Автоматическое формирование обозначения листа

        Тип данных: BOOL

        Свойство позволяет устанавливать и получать признак автоматического формирования обозначения листа.

        ### Синтаксис Automation

        ```
        AutoSheet = Object.AutoSheet        # Получить свойство(* )
        Object.AutoSheet = AutoSheet        # Установить свойство (* )
        AutoSheet = Object.GetAutoSheet( )  # Получить свойство (**)
        Object.SetAutoSheet( AutoSheet )    # Установить свойство (**)
        ```
        """
        ...
    @AutoSheet.setter
    def AutoSheet(self, value: bool): ...

    @property
    def AutoSorted(self) -> bool:  # iviewpointer_autosorted.html
        """
        ## AutoSorted - Автосортировка

        Тип данных: BOOL

        Свойство позволяет устанавливать и получать признак автосортировки.

        ### Синтаксис Automation

        ```
        AutoSorted = Object.AutoSorted        # Получить свойство(* )
        Object.AutoSorted = AutoSorted        # Установить свойство (* )
        AutoSorted = Object.GetAutoSorted( )  # Получить свойство (**)
        Object.SetAutoSorted( AutoSorted )    # Установить свойство (**)
        ```
        """
        ...
    @AutoSorted.setter
    def AutoSorted(self, value: bool): ...

    @property
    def AutoZone(self) -> bool:  # iviewpointer_autozone.html
        """
        ## AutoZone - Автоматическое формирование обозначения зоны

        Тип данных: BOOL

        Свойство позволяет устанавливать и получать признак автоматического формирования обозначения зоны.

        ### Синтаксис Automation

        ```
        AutoZone = Object.AutoZone        # Получить свойство(* )
        Object.AutoZone = AutoZone        # Установить свойство (* )
        AutoZone = Object.GetAutoZone( )  # Получить свойство (**)
        Object.SetAutoZone( AutoZone )    # Установить свойство (**)
        ```
        """
        ...
    @AutoZone.setter
    def AutoZone(self, value: bool): ...

    @property
    def Text(self) -> IText:  # iviewpointer_text.html
        """
        ## Text - Обозначение направления взгляда

        Тип данных: указатель на интерфейс IText

        1. Свойство позволяет получить интерфейс текста обозначения направления взгляда.

        2. Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        Text = Object.Text       # Получить свойство(* )
        Text = Object.GetText()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def TextX(self) -> float:  # iviewpointer_textx.html
        """
        ## TextX - Координата точки привязки текста по оси X

        Тип данных: double

        Свойство позволяет устанавливать и получать координату точки привязки текста по оси Х.

        ### Синтаксис Automation

        ```
        TextX = Object.TextX        # Получить свойство(* )
        Object.TextX = TextX        # Установить свойство (* )
        TextX = Object.GetTextX( )  # Получить свойство (**)
        Object.SetTextX( TextX )    # Установить свойство (**)
        ```
        """
        ...
    @TextX.setter
    def TextX(self, value: float): ...

    @property
    def TextY(self) -> float:  # iviewpointer_texty.html
        """
        ## TextY - Координата точки привязки текста по оси Y

        Тип данных: double

        Свойство позволяет устанавливать и получать координату точки привязки текста по оси Y.

        ### Синтаксис Automation

        ```
        TextY = Object.TextY        # Получить свойство(* )
        Object.TextY = TextY        # Установить свойство (* )
        TextY = Object.GetTextY( )  # Получить свойство (**)
        Object.SetTextY( TextY )    # Установить свойство (**)
        ```
        """
        ...
    @TextY.setter
    def TextY(self, value: float): ...

    @property
    def X1(self) -> float:  # iviewpointer_x1.html
        """
        ## X1 - Координата начальной точки стрелки по оси Х

        Тип данных: double

        Свойство позволяет устанавливать и получать координату начальной точки стрелки по оси Х.

        ### Синтаксис Automation

        ```
        X1 = Object.X1        # Получить свойство(* )
        Object.X1 = X1        # Установить свойство (* )
        X1 = Object.GetX1( )  # Получить свойство (**)
        Object.SetX1( X1 )    # Установить свойство (**)
        ```
        """
        ...
    @X1.setter
    def X1(self, value: float): ...

    @property
    def X2(self) -> float:  # iviewpointer_x2.html
        """
        ## X2 - Координата конечной точки стрелки по оси Х

        Тип данных: double

        Свойство позволяет устанавливать и получать координату конечной точки стрелки по оси Х.

        ### Синтаксис Automation

        ```
        X2 = Object.X2        # Получить свойство(* )
        Object.X2 = X2        # Установить свойство (* )
        X2 = Object.GetX2( )  # Получить свойство (**)
        Object.SetX2( X2 )    # Установить свойство (**)
        ```
        """
        ...
    @X2.setter
    def X2(self, value: float): ...

    @property
    def Y1(self) -> float:  # iviewpointer_y1.html
        """
        ## Y1 - Координата начальной точки стрелки по оси Y

        Тип данных: double

        Свойство позволяет устанавливать и получать координату начальной точки стрелки по оси Y.

        ### Синтаксис Automation

        ```
        Y1 = Object.Y1        # Получить свойство(* )
        Object.Y1 = Y1        # Установить свойство (* )
        Y1 = Object.GetY1( )  # Получить свойство (**)
        Object.SetY1( Y1 )    # Установить свойство (**)
        ```
        """
        ...
    @Y1.setter
    def Y1(self, value: float): ...

    @property
    def Y2(self) -> float:  # iviewpointer_y2.html
        """
        ## Y2 - Координата конечной точки стрелки по оси Y

        Тип данных: double

        Свойство позволяет устанавливать и получать координату конечной точки стрелки по оси Y.

        ### Синтаксис Automation

        ```
        Y2 = Object.Y2        # Получить свойство(* )
        Object.Y2 = Y2        # Установить свойство (* )
        Y2 = Object.GetY2( )  # Получить свойство (**)
        Object.SetY2( Y2 )    # Установить свойство (**)
        ```
        """
        ...
    @Y2.setter
    def Y2(self, value: float): ...




class IWaveLine(IDrawingObject):  # iwaveline.html
    """
    ## Интерфейс IWaveLine

    Интерфейс волнистой линии.

    Интерфейс позволяет задавать и получать параметры волнистой линии.

    1. Интерфейс можно получить у коллекции волнистых линий, используя свойство IWaveLines::WaveLine или метод IWaveLines::Add.

    2. После задания параметров объекта требуется вызвать метод IDrawingObject::Update.
    """

    @property
    def Angle(self) -> float:  # iwaveline_angle.html
        """
        ## Angle - Угол между линией и осью OX текущей СК

        Тип данных: double

        Свойство позволяет устанавливать и получать угол между волнистой линией и осью ОХ текущей СК.

        ### Синтаксис Automation

        ```
        Angle = Object.Angle       # Получить свойство(* )
        Object.Angle = Angle       # Установить свойство (* )
        Angle = Object.GetAngle()  # Получить свойство (**)
        Object.SetAngle( Angle )   # Установить свойство (**)
        ```
        """
        ...
    @Angle.setter
    def Angle(self, value: float): ...

    @property
    def AutoWavesAmplitude(self) -> bool:  # iwaveline_autowavesamplitude.html
        """
        ## AutoWavesAmplitude - Использовать задание амплитуды волны из настроек документа

        Тип данных: BOOL

        Свойство позволяет устанавливать и получать признак задания амплитуды волны из настроек документа.

        ### Синтаксис Automation

        ```
        AutoWavesAmplitude = Object.AutoWavesAmplitude       # Получить свойство(* )
        Object.AutoWavesAmplitude = AutoWavesAmplitude       # Установить свойство (* )
        AutoWavesAmplitude = Object.GetAutoWavesAmplitude()  # Получить свойство (**)
        Object.SetAutoWavesAmplitude( AutoWavesAmplitude )   # Установить свойство (**)
        ```
        """
        ...
    @AutoWavesAmplitude.setter
    def AutoWavesAmplitude(self, value: bool): ...

    @property
    def Direction(self) -> bool:  # iwaveline_direction.html
        """
        ## Direction - Направление

        Тип данных: BOOL

        Значение свойства:

        +--------------------------+
        | TRUE  | - направление 1, |
        +--------------------------+
        | FALSE | - направление 2. |
        +--------------------------+

        Свойство позволяет устанавливать и получать направление.

        ### Синтаксис Automation

        ```
        Direction = Object.Direction       # Получить свойство(* )
        Object.Direction = Direction       # Установить свойство (* )
        Direction = Object.GetDirection()  # Получить свойство (**)
        Object.SetDirection( Direction )   # Установить свойство (**)
        ```
        """
        ...
    @Direction.setter
    def Direction(self, value: bool): ...

    @property
    def HalfWavesCount(self) -> int:  # iwaveline_halfwavescount.html
        """
        ## HalfWavesCount - Количество полуволн

        Тип данных: long

        Свойство позволяет устанавливать и получать количество полуволн.

        ### Синтаксис Automation

        ```
        HalfWavesCount = Object.HalfWavesCount       # Получить свойство(* )
        Object.HalfWavesCount = HalfWavesCount       # Установить свойство (* )
        HalfWavesCount = Object.GetHalfWavesCount()  # Получить свойство (**)
        Object.SetHalfWavesCount( HalfWavesCount )   # Установить свойство (**)
        ```
        """
        ...
    @HalfWavesCount.setter
    def HalfWavesCount(self, value: int): ...

    @property
    def Length(self) -> float:  # iwaveline_length.html
        """
        ## Length - Длина

        Тип данных: double

        Свойство позволяет устанавливать и получать длину волнистой линии.

        ### Синтаксис Automation

        ```
        Length = Object.Length       # Получить свойство(* )
        Object.Length = Length       # Установить свойство (* )
        Length = Object.GetLength()  # Получить свойство (**)
        Object.SetLength( Length )   # Установить свойство (**)
        ```
        """
        ...
    @Length.setter
    def Length(self, value: float): ...

    @property
    def Style(self) -> int:  # iwaveline_style.html
        """
        ## Style - Cтиль линии

        Тип данных: long.

        1. Свойство позволяет устанавливать и получать стиль линии.

        2. Системные стили линий берутся из перечисления ksCurveStyleEnum.

        ### Синтаксис Automation

        ```
        Style = Object.Style       # Получить свойство(* )
        Object.Style = Style       # Установить свойство (* )
        Style = Object.GetStyle()  # Получить свойство (**)
        Object.SetStyle( Style )   # Установить свойство (**)
        ```
        """
        ...
    @Style.setter
    def Style(self, value: int): ...

    @property
    def WaveLength(self) -> float:  # iwaveline_wavelength.html
        """
        ## WaveLength - Длина волны

        Тип данных: double

        Свойство позволяет устанавливать и получать длину волны.

        ### Синтаксис Automation

        ```
        WaveLength = Object.WaveLength       # Получить свойство(* )
        Object.WaveLength = WaveLength       # Установить свойство (* )
        WaveLength = Object.GetWaveLength()  # Получить свойство (**)
        Object.SetWaveLength( WaveLength )   # Установить свойство (**)
        ```
        """
        ...
    @WaveLength.setter
    def WaveLength(self, value: float): ...

    @property
    def WavesAmplitude(self) -> float:  # iwaveline_wavesamplitude.html
        """
        ## WavesAmplitude - Амплитуда волн

        Тип данных: double

        1. Свойство позволяет получать значение амплитуды волн.

        2. Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        WavesAmplitude = Object.WavesAmplitude        # Получить свойство(* )
        WavesAmplitude = Object.GetWavesAmplitude ()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def WavesAmplitudeRepresentation(self) -> bool:  # iwaveline_wavesamplituderepresentation.html
        """
        ## WavesAmplitudeRepresentation - Получить представление амплитуды волн

        Тип данных: BOOL

        Значение свойства:

        +----------------------------------------------------+
        | TRUE  | - амплитуда задана в абсолютных значениях, |
        +----------------------------------------------------+
        | FALSE | - в процентах.                             |
        +----------------------------------------------------+

        1. Свойство позволяет получать представление амплитуды волн.

        2. Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        WavesAmplitudeRepresentation = Object.WavesAmplitudeRepresentation       # Получить свойство(* )
        WavesAmplitudeRepresentation = Object.GetWavesAmplitudeRepresentation()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def X1(self) -> float:  # iwaveline_x1.html
        """
        ## X1 - Координата первой точки по оси X

        Тип данных: double

        Свойство позволяет устанавливать и получать координату первой точки по оси Х.

        ### Синтаксис Automation

        ```
        X1 = Object.X1       # Получить свойство(* )
        Object.X1 = X1       # Установить свойство (* )
        X1 = Object.GetX1()  # Получить свойство (**)
        Object.SetX1( X1 )   # Установить свойство (**)
        ```
        """
        ...
    @X1.setter
    def X1(self, value: float): ...

    @property
    def X2(self) -> float:  # iwaveline_x2.html
        """
        ## X2 - Координата второй точки по оси X

        Тип данных: double

        Свойство позволяет устанавливать и получать координату второй точки по оси Х.

        ### Синтаксис Automation

        ```
        X2 = Object.X2       # Получить свойство(* )
        Object.X2 = X2       # Установить свойство (* )
        X2 = Object.GetX2()  # Получить свойство (**)
        Object.SetX2( X2 )   # Установить свойство (**)
        ```
        """
        ...
    @X2.setter
    def X2(self, value: float): ...

    @property
    def Y1(self) -> float:  # iwaveline_y1.html
        """
        ## Y1 - Координата первой точки по оси Y

        Тип данных: double

        Свойство позволяет устанавливать и получать координату первой точки по оси Y.

        ### Синтаксис Automation

        ```
        Y1 = Object.Y1       # Получить свойство(* )
        Object.Y1 = Y1       # Установить свойство (* )
        Y1 = Object.GetY1()  # Получить свойство (**)
        Object.SetY1( Y1 )   # Установить свойство (**)
        ```
        """
        ...
    @Y1.setter
    def Y1(self, value: float): ...

    @property
    def Y2(self) -> float:  # iwaveline_y2.html
        """
        ## Y2 - Координата второй точки по оси Y

        Тип данных: double

        Свойство позволяет устанавливать и получать координату второй точки по оси Y.

        ### Синтаксис Automation

        ```
        Y2 = Object.Y2       # Получить свойство(* )
        Object.Y2 = Y2       # Установить свойство (* )
        Y2 = Object.GetY2()  # Получить свойство (**)
        Object.SetY2( Y2 )   # Установить свойство (**)
        ```
        """
        ...
    @Y2.setter
    def Y2(self, value: float): ...

    def SetWavesAmplitude(self, Representation, NewVal) -> bool:  # iwaveline_setwavesamplitude.html
        """
        ## SetWavesAmplitude - Задать представление амплитуды волн

        Метод позволяет задать представление амплитуды волн.

        ### Синтаксис Automation

        ```
        BOOL SetWavesAmplitude( BOOL Representation,
        double NewVal )
        ```

        ### Входные параметры

        +-----------------------------------------------------------------------------------------+
        | Representation | - TRUE - амплитуда задана в абсолютных значениях, FALSE - в процентах, |
        +-----------------------------------------------------------------------------------------+
        | NewVal         | - значение амплитуды.                                                  |
        +-----------------------------------------------------------------------------------------+

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...




class IAngleDimensions(IDrawingObjects):  # iangledimensions.html
    """
    ## Интерфейс AngleDimensions

    Интерфейс коллекции угловых размеров.

    Интерфейс позволяет получать и создавать угловые размеры.

    1. Коллекция содержит угловые и угловые с обрывом размеры.

    2. Данный интерфейс можно получить у контейнера условных обозначений, используя свойство ISymbols2DContainer::AngleDimensions.
    """

    def Add(self, DimType) -> IAngleDimension:  # iangledimensions_add.html
        """
        ## Add - Добавить угловой размер в коллекцию

        Для добавления углового размера в качестве параметра DimType нужно передать константу ksDrADimension, для углового размера с обрывом нужно передать константу ksDrABreakDimension.

        ### Синтаксис Automation

        ```
        LPDISPATCH Add( DrawingObjectTypeEnum DimType );
        ```

        ### Входные параметры

        +--------------------------+
        | DimType | - тип объекта. |
        +--------------------------+

        ### Возвращаемое значение

        - указатель на интерфейс углового размера IAngleDimension.
        """
        ...

    def AngleDimension(self, Index) -> IAngleDimension:  # iangledimensions_angledimension.html
        """
        ## AngleDimension - Угловой размер, заданный по индексу

        Тип данных: указатель на интерфейс углового размера IAngleDimension

        1. В качестве индекса может использоваться индекс в коллекции и reference объекта.

        2. Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        AngleDimension = iObject.AngleDimension( Index );     # Получить свойство(* )
        AngleDimension = iObject.GetAngleDimension( Index );  # Получить свойство (**)
        ```

        ### Входные параметры

        +----------------------------------------------------------------------------------------------------------+
        | Index (Variant) | - Индекс размера в коллекции. Поддерживаются следующие типы: - VT_I4 - индекс размера. |
        +----------------------------------------------------------------------------------------------------------+
        """
        ...




class IArcDimensions(IDrawingObjects):  # iarcdimensions.html
    """
    ## Интерфейс IArcDimensions

    Интерфейс коллекции размеров дуг окружностей.

    Интерфейс позволяет получать и создавать размеры дуг окружностей.

    Данный интерфейс можно получить у контейнера условных обозначений, используя свойство ISymbols2DContainer::ArcDimensions.
    """

    def Add(self) -> IArcDimension:  # iarcdimensions_add.html
        """
        ## Add - Добавить размер дуги окружности

        ### Синтаксис Automation

        ```
        LPDISPATCH Add( );
        ```

        ### Возвращаемое значение

        - указатель на интерфейс размера дуги окружности IArcDimension.
        """
        ...

    def ArcDimension(self, Index) -> IArcDimension:  # iarcdimensions_arcdimension.html
        """
        ## ArcDimension - Размер дуги окружности, заданныйпо индексу

        Тип данных: указатель на интерфейс размера дуги окружности IArcDimension

        1. В качестве индекса может использоваться индекс в коллекции и reference объекта.

        2. Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        ArcDimension = iObject.ArcDimension( Index );     # Получить свойство(* )
        ArcDimension = iObject.GetArcDimension( Index );  # Получить свойство (**)
        ```

        ### Входные параметры

        +----------------------------------------------------------------------------------------------------------+
        | Index (Variant) | - Индекс размера в коллекции. Поддерживаются следующие типы: - VT_I4 - индекс размера. |
        +----------------------------------------------------------------------------------------------------------+
        """
        ...




class IBreakLineDimensions(IDrawingObjects):  # ibreaklinedimensions.html
    """
    ## Интерфейс IBreakLineDimensions

    Интерфейс коллекции линейных размеров с обрывом.

    Интерфейс позволяет получать и создавать линейные размеры с обрывом.

    Данный интерфейс можно получить у контейнера условных обозначений, используя свойство ISymbols2DContainer::BreakLineDimensions.
    """

    def Add(self) -> IBreakLineDimension:  # ibreaklinedimensions_add.html
        """
        ## Add - Добавить линейный размер с обрывом в коллекцию

        ### Синтаксис Automation

        ```
        LPDISPATCH Add( );
        ```

        ### Возвращаемое значение

        - указатель на интерфейс линейного размера с обрывом IBreakLineDimension.
        """
        ...

    def BreakLineDimension(self, Index) -> IBreakLineDimension:  # ibreaklinedimensions_breaklinedimension.html
        """
        ## BreakLineDimension - Линейный размер с обрывом, заданный по индексу

        Тип данных: указатель на интерфейс линейного размера с обрывом IBreakLineDimension

        1. В качестве индекса может использоваться индекс в коллекции и reference объекта.

        2. Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        LineDimension = iObject.BreakLineDimension( Index );     # Получить свойство(* )
        LineDimension = iObject.GetBreakLineDimension( Index );  # Получить свойство (**)
        ```

        ### Входные параметры

        +----------------------------------------------------------------------------------------------------------+
        | Index (Variant) | - Индекс размера в коллекции. Поддерживаются следующие типы: - VT_I4 - индекс размера. |
        +----------------------------------------------------------------------------------------------------------+
        """
        ...




class IBreakRadialDimensions(IDrawingObjects):  # ibreakradialdimensions.html
    """
    ## Интерфейс IBreakRadialDimensions

    Интерфейс коллекции радиальных размеров c изломом.

    Интерфейс позволяет получать и создавать радиальные размеры с изломом.

    Данный интерфейс можно получить у контейнера условных обозначений, используя свойство ISymbols2DContainer::BreakRadialDimensions.
    """

    def Add(self) -> IBreakRadialDimension:  # ibreakradialdimensions_add.html
        """
        ## Add - Добавить радиальный размер с изломом в коллекцию

        ### Синтаксис Automation

        ```
        LPDISPATH Add( );
        ```

        ### Возвращаемое значение

        - указатель на интерфейс радиального размера с изломом IBreakRadialDimension.
        """
        ...

    def BreakRadialDimension(self, Index):
        ...




class IDiametralDimensions(IDrawingObjects):  # idiametraldimensions.html
    """
    ## Интерфейс IDiametralDimensions

    Интерфейс коллекции диаметральных размеров.

    Интерфейс позволяет получать и создавать диаметральные размеры.

    Данный интерфейс можно получить у контейнера условных обозначений, используя свойство ISymbols2DContainer::DiametralDimensions.
    """

    def Add(self) -> IDiametralDimension:  # idiametraldimensions_add.html
        """
        ## Add - Добавить диаметральный размер в коллекцию

        ### Синтаксис Automation

        ```
        LPDISPATH Add( );
        ```

        ### Возвращаемое значение

        - указатель на интерфейс диаметрального размера IDiametralDimension.
        """
        ...

    def DiametralDimension(self, Index) -> IDiametralDimension:  # idiametraldimensions_diametraldimension.html
        """
        ## DiametralDimension - Диаметральный размер, заданный по индексу

        Тип данных: указатель на интерфейс диаметрального размера IDiametralDimension

        1. В качестве индекса может использоваться индекс в коллекции и reference объекта.

        2. Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        DiametralDimension = iObject.DiametralDimension( Index );     # Получить свойство(* )
        DiametralDimension = iObject.GetDiametralDimension( Index );  # Получить свойство (**)
        ```

        ### Входные параметры

        +-------------------------------------------------------------------------------------------------------+
        | Index (Variant) | - Индекс размера в коллекции. Поддерживаются следующие типы: - VT_I4 - индекс узла. |
        +-------------------------------------------------------------------------------------------------------+
        """
        ...




class IHeightDimensions(IDrawingObjects):  # iheightdimensions.html
    """
    ## Интерфейс IHeightDimensions

    Интерфейс коллекции размеров высоты.

    Интерфейс позволяет получать и создавать размеры высоты.

    Данный интерфейс можно получить у контейнера условных обозначений, используя свойство ISymbols2DContainer::HeightDimensions.
    """

    def Add(self) -> IHeightDimension:  # iheightdimensions_add.html
        """
        ## Add - Добавить размер высоты в коллекцию

        ### Синтаксис Automation

        ```
        LPDISPATCH Add( );
        ```

        ### Возвращаемое значение

        - указатель на интерфейс размера высоты IHeightDimension.
        """
        ...

    def HeightDimension(self, Index) -> IHeightDimension:  # iheightdimensions_heightdimension.html
        """
        ## HeightDimension - Размер высоты, заданный по индексу

        Тип данных: указатель на интерфейс размера высоты IHeightDimension

        1. В качестве индекса может использоваться индекс в коллекции и reference объекта.

        2. Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        HeightDimension = iObject.HeightDimension( Index );     # Получить свойство(* )
        HeightDimension = iObject.GetHeightDimension( Index );  # Получить свойство (**)
        ```

        ### Входные параметры

        +----------------------------------------------------------------------------------------------------------+
        | Index (Variant) | - Индекс размера в коллекции. Поддерживаются следующие типы: - VT_I4 - индекс размера. |
        +----------------------------------------------------------------------------------------------------------+
        """
        ...




class ILineDimensions(IDrawingObjects):  # ilinedimensions.html
    """
    ## Интерфейс ILineDimensions

    Интерфейс коллекции линейных размеров.

    Интерфейс позволяет получать и создавать линейные размеры.

    Данный интерфейс можно получить у контейнера условных обозначений, используя свойство ISymbols2DContainer::LineDimensions.
    """

    def Add(self) -> ILineDimension:  # ilinedimensions_add.html
        """
        ## Add - Добавить линейный размер в коллекцию

        ### Синтаксис Automation

        ```
        LPDISPATCH Add( );
        ```

        ### Возвращаемое значение

        - указатель на интерфейс линейного размера ILineDimension.
        """
        ...

    def AddLineSegmentAndPointDimension(self) -> ILineDimension|ILineSegmentAndPointDimension:  # ilinedimensions_addlinesegmentandpointdimension.html
        """
        ## AddLineSegmentAndPointDimension - Добавить линейный размер от отрезка до точки

        ### Синтаксис Automation

        ```
        LPDISPATCH AddLineSegmentAndPointDimension();
        ```

        ### Возвращаемое значение

        +-------------------------------------------------------------------------------------------------------------------------+
        | - указатель на интерфейс линейного размера ILineDimension.                                                              |
        +-------------------------------------------------------------------------------------------------------------------------+
        | - новый объект имеет также дополнительный интерфейс ILineSegmentAndPointDimension - линейный размер от отрезка до точки |
        +-------------------------------------------------------------------------------------------------------------------------+
        """
        ...

    def LineDimension(self, Index) -> ILineDimension:  # ilinedimensions_linedimension.html
        """
        ## LineDimension - Линейный размер, заданный по индексу

        Тип данных: указатель на интерфейс линейного размера ILineDimension

        1. В качестве индекса может использоваться индекс в коллекции и reference объекта.

        2. Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        LineDimension = iObject.LineDimension( Index );     # Получить свойство(* )
        LineDimension = iObject.GetLineDimension( Index );  # Получить свойство (**)
        ```

        ### Входные параметры

        +----------------------------------------------------------------------------------------------------------+
        | Index (Variant) | - Индекс размера в коллекции. Поддерживаются следующие типы: - VT_I4 - индекс размера. |
        +----------------------------------------------------------------------------------------------------------+
        """
        ...




class IRadialDimensions(IDrawingObjects):  # iradialdimensions.html
    """
    ## Интерфейс IRadialDimensions

    Интерфейс коллекции радиальных размеров.

    Интерфейс позволяет получать и создавать радиальные размеры.

    Данный интерфейс можно получить у контейнера условных обозначений, используя свойство ISymbols2DContainer::RadialDimensions.
    """

    def Add(self) -> IRadialDimension:  # iradialdimensions_add.html
        """
        ## Add - Добавить радиальный размер в коллекцию

        ### Синтаксис Automation

        ```
        LPDISPATH Add( );
        ```

        ### Возвращаемое значение

        - указатель на интерфейс радиального размера IRadialDimension.
        """
        ...

    def RadialDimension(self, Index) -> IRadialDimension:  # iradialdimensions_radialdimension.html
        """
        ## RadialDimension - Радиальный размер, заданный по индексу

        Тип данных: указатель на интерфейс радиального размера IRadialDimension

        1. В качестве индекса может использоваться индекс в коллекции и reference объекта.

        2. Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        RadialDimension = iObject.RadialDimension( Index );     # Получить свойство(* )
        RadialDimension = iObject.GetRadialDimension( Index );  # Получить свойство (**)
        ```

        ### Входные параметры

        +----------------------------------------------------------------------------------------------------------+
        | Index (Variant) | - Индекс размера в коллекции. Поддерживаются следующие типы: - VT_I4 - индекс размера. |
        +----------------------------------------------------------------------------------------------------------+
        """
        ...




class IAngleDimension(IDrawingObject):  # iangledimension.html
    """
    ## Интерфейс IAngleDimension

    Интерфейс параметров углового размера.

    Интерфейс позволяет получить и задать свойства углового размера.

    Интерфейс можно получить у коллекции угловых размеров, используя свойство IAngleDimensions::AngleDimension или метод IAngleDimensions::Add.

    После задания параметров размера требуется вызвать метод IDrawingObject::Update.

    Интерфейсы IDimensionText и IDimensionParams являются дополнительными. Их можно получить с помощью метода IUnknown::QueryInterface.

    Если при создании размера задать опорные объекты IAngleDimension::BaseObject1, IAngleDimension::BaseObject2, то координаты центра IAngleDimension::Xc, IAngleDimension::Yc, радиус IAngleDimension::Radius, IAngleDimension::X1, IAngleDimension::Y1, IAngleDimension::X2, IAngleDimension::Y2, задавать не нужно. Данные свойства будут получены с опорного объекта.

    В качестве базовых объектов могут использоваться отрезки. Для углового размера второй опорный объект задает направление второй выносной линии. Для углового размера с обрывом второй опорный объект задает ось симметрии (биссектрису угла).

    Свойство IDimensionText::NominalValue возвращает угол.
    """

    @property
    def Angle1(self) -> float:  # iangledimension_angle1.html
        """
        ## Angle1 - Угол наклона первой размерной линии

        Тип данных: double

        ### Синтаксис Automation

        ```
        Angle1 = Object.Angle1       # Получить свойство(* )
        Object.Angle1 = Angle1       # Установить свойство (* )
        Angle1 = Object.GetAngle1()  # Получить свойство (**)
        Object.SetAngle1( Angle1 )   # Установить свойство (**)
        ```
        """
        ...
    @Angle1.setter
    def Angle1(self, value: float): ...

    @property
    def Angle2(self) -> float:  # iangledimension_angle2.html
        """
        ## Angle2 - Угол наклона второй размерной линии

        Тип данных: double

        ### Синтаксис Automation

        ```
        Angle2 = Object.Angle2       # Получить свойство(* )
        Object.Angle2 = Angle2       # Установить свойство (* )
        Angle2 = Object.GetAngle2()  # Получить свойство (**)
        Object.SetAngle2( Angle2 )   # Установить свойство (**)
        ```
        """
        ...
    @Angle2.setter
    def Angle2(self, value: float): ...

    @property
    def BaseObject1(self) -> IDrawingObject:  # iangledimension_baseobject1.html
        """
        ## BaseObject1 - Первый опорный объект

        Тип данных: интерфейс IDrawingObject

        Если при создании размера задать опорные объекты IAngleDimension::BaseObject1, IAngleDimension::BaseObject2, то координаты центра IAngleDimension::Xc, IAngleDimension::Yc, радиус IAngleDimension::Radius, IAngleDimension::X1, IAngleDimension::Y1, IAngleDimension::X2, IAngleDimension::Y2, задавать не нужно. Данные свойства будут получены с опорного объекта.

        В качестве базовых объектов могут использоваться отрезки.

        Чтобы разорвать связь с объектом, нужно установить значение свойства равным NULL.

        ### Синтаксис Automation

        ```
        BaseObject1 = Object.BaseObject1       # Получить свойство(* )
        Object.BaseObject1 = BaseObject1       # Установить свойство (* )
        BaseObject1 = Object.GetBaseObject1()  # Получить свойство (**)
        Object.SetBaseObject1( BaseObject1 )   # Установить свойство (**)
        ```
        """
        ...
    @BaseObject1.setter
    def BaseObject1(self, value: IDrawingObject): ...

    @property
    def BaseObject2(self) -> IDrawingObject:  # iangledimension_baseobject2.html
        """
        ## BaseObject2 - Второй опорный объект

        Тип данных: интерфейс IDrawingObject

        Если при создании размера задать опорные объекты IAngleDimension::BaseObject1, IAngleDimension::BaseObject2, то координаты центра IAngleDimension::Xc, IAngleDimension::Yc, радиус IAngleDimension::Radius, IAngleDimension::X1, IAngleDimension::Y1, IAngleDimension::X2, IAngleDimension::Y2, задавать не нужно. Данные свойства будут получены с опорного объекта.

        В качестве базовых объектов могут использоваться отрезки.

        Чтобы разорвать связь с объектом, нужно установить значение свойства равным NULL.

        ### Синтаксис Automation

        ```
        BaseObject2 = Object.BaseObject2       # Получить свойство(* )
        Object.BaseObject2 = BaseObject2       # Установить свойство (* )
        BaseObject2 = Object.GetBaseObject2()  # Получить свойство (**)
        Object.SetBaseObject2( BaseObject2 )   # Установить свойство (**)
        ```
        """
        ...
    @BaseObject2.setter
    def BaseObject2(self, value: IDrawingObject): ...

    @property
    def DimensionType(self) -> int:  # iangledimension_dimensiontype.html
        """
        ## DimensionType - Тип углового размера

        Тип данных: из перечисления ksAngleDimTypeEnum

        ### Синтаксис Automation

        ```
        DimensionType = Object.DimensionType       # Получить свойство(* )
        Object.DimensionType = DimensionType       # Установить свойство (* )
        DimensionType = Object.GetDimensionType()  # Получить свойство (**)
        Object.SetDimensionType( DimensionType )   # Установить свойство (**)
        ```
        """
        ...
    @DimensionType.setter
    def DimensionType(self, value: int): ...

    @property
    def Direction(self) -> bool:  # iangledimension_direction.html
        """
        ## Direction - Направление размерной дуги

        Тип данных: BOOL

        Значения свойства:

        +-----------------------------------+
        | true  | - по часовой стрелке;     |
        +-----------------------------------+
        | false | - против часовой стрелки. |
        +-----------------------------------+

        ### Синтаксис Automation

        ```
        Direction = Object.Direction       # Получить свойство(* )
        Object.Direction = Direction       # Установить свойство (* )
        Direction = Object.GetDirection()  # Получить свойство (**)
        Object.SetDirection( Direction )   # Установить свойство (**)
        ```
        """
        ...
    @Direction.setter
    def Direction(self, value: bool): ...

    @property
    def Radius(self) -> float:  # iangledimension_radius.html
        """
        ## Radius - Радиус

        Тип данных: double

        ### Синтаксис Automation

        ```
        Radius = Object.Radius       # Получить свойство(* )
        Object.Radius = Radius       # Установить свойство (* )
        Radius = Object.GetRadius()  # Получить свойство (**)
        Object.SetRadius( Radius )   # Установить свойство (**)
        ```
        """
        ...
    @Radius.setter
    def Radius(self, value: float): ...

    @property
    def ShelfX(self) -> float:  # iangledimension_shelfx.html
        """
        ## ShelfX - Точка начала полки

        Тип данных: double

        Перед заданием свойства нужно установить признак размещения на полке TextType = ksDTPOnShelf.

        ### Синтаксис Automation

        ```
        ShelfX = Object.ShelfX       # Получить свойство(* )
        Object.ShelfX = ShelfX       # Установить свойство (* )
        ShelfX = Object.GetShelfX()  # Получить свойство (**)
        Object.SetShelfX( ShelfX )   # Установить свойство (**)
        ```
        """
        ...
    @ShelfX.setter
    def ShelfX(self, value: float): ...

    @property
    def ShelfY(self) -> float:  # iangledimension_shelfy.html
        """
        ## ShelfY - Точка начала полки

        Тип данных: double

        Перед заданием свойства нужно установить признак размещения на полке TextType = ksDTPOnShelf.

        ### Синтаксис Automation

        ```
        ShelfY = Object.ShelfY       # Получить свойство(* )
        Object.ShelfY = ShelfY       # Установить свойство (* )
        ShelfY = Object.GetShelfY()  # Получить свойство (**)
        Object.SetShelfY( ShelfY )   # Установить свойство (**)
        ```
        """
        ...
    @ShelfY.setter
    def ShelfY(self, value: float): ...

    @property
    def X1(self) -> float:  # iangledimension_x1.html
        """
        ## X1 - Координата точки выхода первой выносной линии по X

        Тип данных: double

        ### Синтаксис Automation

        ```
        X1 = Object.X1       # Получить свойство(* )
        Object.X1 = X1       # Установить свойство (* )
        X1 = Object.GetX1()  # Получить свойство (**)
        Object.SetX1( X1 )   # Установить свойство (**)
        ```
        """
        ...
    @X1.setter
    def X1(self, value: float): ...

    @property
    def X2(self) -> float:  # iangledimension_x2.html
        """
        ## X2 - Координата точки выхода первой выносной линии по X

        Тип данных: double

        ### Синтаксис Automation

        ```
        X1 = Object.X2       # Получить свойство(* )
        Object.X2 = X2       # Установить свойство (* )
        X2 = Object.GetX2()  # Получить свойство (**)
        Object.SetX2( X2 )   # Установить свойство (**)
        ```
        """
        ...
    @X2.setter
    def X2(self, value: float): ...

    @property
    def X3(self) -> float:  # iangledimension_x3.html
        """
        ## X3 - Начало ножки или точка на дуге задающая положение текста по X

        Тип данных: double

        ### Синтаксис Automation

        ```
        X3 = Object.X3       # Получить свойство(* )
        Object.X3 = X3       # Установить свойство (* )
        X3 = Object.GetX3()  # Получить свойство (**)
        Object.SetX3( X3 )   # Установить свойство (**)
        ```
        """
        ...
    @X3.setter
    def X3(self, value: float): ...

    @property
    def Xc(self) -> float:  # iangledimension_xc.html
        """
        ## Xc - Координата центра по X

        Тип данных: double

        ### Синтаксис Automation

        ```
        Xc = Object.Xc       # Получить свойство(* )
        Object.Xc = Xc       # Установить свойство (* )
        Xc = Object.GetXc()  # Получить свойство (**)
        Object.SetXc( Xc )   # Установить свойство (**)
        ```
        """
        ...
    @Xc.setter
    def Xc(self, value: float): ...

    @property
    def Y1(self) -> float:  # iangledimension_y1.html
        """
        ## Y1 - Координата точки выхода первой выносной линии по Y

        Тип данных: double

        ### Синтаксис Automation

        ```
        Y1 = Object.Y1       # Получить свойство(* )
        Object.Y1 = Y1       # Установить свойство (* )
        Y1 = Object.GetY1()  # Получить свойство (**)
        Object.SetY1( Y1 )   # Установить свойство (**)
        ```
        """
        ...
    @Y1.setter
    def Y1(self, value: float): ...

    @property
    def Y2(self) -> float:  # iangledimension_y2.html
        """
        ## Y2 - Координата точки выхода первой выносной линии по Y

        Тип данных: double

        ### Синтаксис Automation

        ```
        Y2 = Object.Y2       # Получить свойство(* )
        Object.Y2 = Y2       # Установить свойство (* )
        Y2 = Object.GetY2()  # Получить свойство (**)
        Object.SetY2( Y2 )   # Установить свойство (**)
        ```
        """
        ...
    @Y2.setter
    def Y2(self, value: float): ...

    @property
    def Y3(self) -> float:  # iangledimension_y3.html
        """
        ## Y3 - Начало ножки или точка на дуге, задающая положение текста по Y

        Тип данных: double

        ### Синтаксис Automation

        ```
        Y3 = Object.Y3      # Получить свойство(* )
        Object.Y3 = Y2      # Установить свойство (* )
        Y3 = Object.Get3()  # Получить свойство (**)
        Object.SetY3( Y3 )  # Установить свойство (**)
        ```
        """
        ...
    @Y3.setter
    def Y3(self, value: float): ...

    @property
    def Yc(self) -> float:  # iangledimension_yc.html
        """
        ## Yc - Координата центра по Y

        Тип данных: double

        ### Синтаксис Automation

        ```
        Yc = Object.Yc       # Получить свойство(* )
        Object.Yc = Yc       # Установить свойство (* )
        Yc = Object.GetYc()  # Получить свойство (**)
        Object.SetYc( Yc )   # Установить свойство (**)
        ```
        """
        ...
    @Yc.setter
    def Yc(self, value: float): ...




class IBreakAngleDimension(IAngleDimension):  # ibreakangledimension.html
    """
    ## Интерфейс IBreakAngleDimension

    Интерфейс параметров углового размера c обрывом.

    Интерфейс позволяет получить и задать свойства углового размера c обрывом.

    Интерфейс можно получить у коллекции угловых размеров, используя свойство IAngleDimensions::AngleDimension или метод IAngleDimensions::Add.

    После задания параметров размера требуется вызвать метод IDrawingObject::Update.

    Интерфейсы IDimensionText и IDimensionParams являются дополнительными. Их можно получить с помощью метода IUnknown::QueryInterface.
    """

    ...


class IArcDimension(IDrawingObject):  # iarcdimension.html
    """
    ## Интерфейс IArcDimension

    Интерфейс параметров размера дуги окружности.

    Интерфейс позволяет получить и задать свойства размера дуги окружности.

    Интерфейс можно получить у коллекции размеров дуг окружностей, используя свойство IArcDimensions::ArcDimension или метод IArcDimensions::Add.

    После задания параметров размера требуется вызвать метод IDrawingObject::Update.

    Интерфейсы IDimensionText и IDimensionParams являются дополнительными. Их можно получить с помощью метода IUnknown::QueryInterface.

    1. Свойство IDimensionText::NominalValue возвращает длину дуги.

    2. Если при создании размера задать опорный объект IArcDimension::BaseObject, то значения свойств IArcDimension::Xc, IArcDimension::Yc, IArcDimension::X1, IArcDimension::Y1, IArcDimension::X2, IArcDimension::Y2, IArcDimension::Direction задавать не нужно. Данные свойства будут получены с исходных объектов. В качестве базового объекта может использоваться дуга окружности.
    """

    @property
    def BaseObject(self) -> IDrawingObject:  # iarcdimension_baseobject.html
        """
        ## BaseObject - Опорный объект

        Тип данных: указатель на интерфейс IDrawingObject

        Если при создании размера задать опорный объект IArcDimension::BaseObject, то значения свойств IArcDimension::Xc, IArcDimension::Yc, IArcDimension::X1, IArcDimension::Y1, IArcDimension::X2, IArcDimension::Y2, IArcDimension::Direction задавать не нужно. Данные свойства будут получены с исходных объектов. В качестве базового объекта может использоваться дуга окружности.

        Чтобы разорвать связь с объектом, нужно установить значение свойства, равное NULL.

        ### Синтаксис Automation

        ```
        BaseObject = Object.BaseObject       # Получить свойство(* )
        Object.BaseObject = BaseObject       # Установить свойство (* )
        BaseObject = Object.GetBaseObject()  # Получить свойство (**)
        Object.SetBaseObject( BaseObject )   # Установить свойство (**)
        ```
        """
        ...
    @BaseObject.setter
    def BaseObject(self, value: IDrawingObject): ...

    @property
    def DimensionType(self) -> bool:  # iarcdimension_dimensiontype.html
        """
        ## DimensionType - Тип размера (Выносные линии от центра\\Параллельные выносные линии)

        Тип данных: BOOL

        Значения свойства:

        +----------------------------------------+
        | true  | - выносные линии от центра;    |
        +----------------------------------------+
        | false | - параллельные выносные линии. |
        +----------------------------------------+

        Свойство позволяет задать направление выносных линий – от центра или параллельные выносные линии, проведенному в середину дуги. Если угол раствора дуги больше 180 градусов, возможно создание размера только с выносными линиями от центра.

        ### Синтаксис Automation

        ```
        DimensionType = Object.DimensionType       # Получить свойство(* )
        Object.DimensionType = DimensionType       # Установить свойство (* )
        DimensionType = Object.GetDimensionType()  # Получить свойство (**)
        Object.SetDimensionType( DimensionType )   # Установить свойство (**)
        ```
        """
        ...
    @DimensionType.setter
    def DimensionType(self, value: bool): ...

    @property
    def Direction(self) -> bool:  # iarcdimension_direction.html
        """
        ## Direction - Направление размерной дуги

        Тип данных: BOOL

        Значения свойства:

        +-----------------------------------+
        | true  | - по часовой стрелке;     |
        +-----------------------------------+
        | false | - против часовой стрелки. |
        +-----------------------------------+

        ### Синтаксис Automation

        ```
        Direction = Object.Direction       # Получить свойство(* )
        Object.Direction = Direction       # Установить свойство (* )
        Direction = Object.GetDirection()  # Получить свойство (**)
        Object.SetDirection( Direction )   # Установить свойство (**)
        ```
        """
        ...
    @Direction.setter
    def Direction(self, value: bool): ...

    @property
    def ShelfX(self) -> float:  # iarcdimension_shelfx.html
        """
        ## ShelfX - Значение координаты положения точки начала полки по оси X

        Тип данных: double

        Перед заданием свойства нужно установить признак размещения на полке TextType = ksDTPOnShelf.

        ### Синтаксис Automation

        ```
        ShelfX = Object.ShelfX       # Получить свойство(* )
        Object.ShelfX = ShelfX       # Установить свойство (* )
        ShelfX = Object.GetShelfX()  # Получить свойство (**)
        Object.SetShelfX( ShelfX )   # Установить свойство (**)
        ```
        """
        ...
    @ShelfX.setter
    def ShelfX(self, value: float): ...

    @property
    def ShelfY(self) -> float:  # iarcdimension_shelfy.html
        """
        ## ShelfY - Значение координаты положения точки начала полки по оси Y

        Тип данных: double

        Перед заданием свойства нужно установить признак размещения на полке TextType = ksDTPOnShelf.

        ### Синтаксис Automation

        ```
        ShelfY = Object.ShelfY       # Получить свойство(* )
        Object.ShelfY = ShelfY       # Установить свойство (* )
        ShelfY = Object.GetShelfY()  # Получить свойство (**)
        Object.SetShelfY( ShelfY )   # Установить свойство (**)
        ```
        """
        ...
    @ShelfY.setter
    def ShelfY(self, value: float): ...

    @property
    def TextPointer(self) -> bool:  # iarcdimension_textpointer.html
        """
        ## TextPointer - Указатель от текста к дуге

        Тип данных: BOOL

        Значения свойства:

        +--------------------------------------+
        | true  | - отрисовать указатель;      |
        +--------------------------------------+
        | false | - не отрисовывать указатель. |
        +--------------------------------------+

        Свойство позволяет включить соединить указателем дугу и текст, относящегося к ней размера. Это может потребоваться, например, при простановке размеров концентрических дуг с одинаковым раствором и начальным углом.

        ### Синтаксис Automation

        ```
        TextPointer = Object.TextPointer       # Получить свойство(* )
        Object.TextPointer = TextPointer       # Установить свойство (* )
        TextPointer = Object.GetTextPointer()  # Получить свойство (**)
        Object.SetTextPointer( TextPointer )   # Установить свойство (**)
        ```
        """
        ...
    @TextPointer.setter
    def TextPointer(self, value: bool): ...

    @property
    def X1(self) -> float:  # iarcdimension_x1.html
        """
        ## X1 - Значение координаты первой точки дуги по оси X

        Тип данных: double

        ### Синтаксис Automation

        ```
        X1 = Object.X1       # Получить свойство(* )
        Object.X1 = X1       # Установить свойство (* )
        X1 = Object.GetX1()  # Получить свойство (**)
        Object.SetX1( X1 )   # Установить свойство (**)
        ```
        """
        ...
    @X1.setter
    def X1(self, value: float): ...

    @property
    def X2(self) -> float:  # iarcdimension_x2.html
        """
        ## X2 - Значение координаты второй точки дуги по оси X

        Тип данных: double

        ### Синтаксис Automation

        ```
        X2 = Object.X2       # Получить свойство(* )
        Object.X2 = X2       # Установить свойство (* )
        X2 = Object.GetX2()  # Получить свойство (**)
        Object.SetX2( X2 )   # Установить свойство (**)
        ```
        """
        ...
    @X2.setter
    def X2(self, value: float): ...

    @property
    def X3(self) -> float:  # iarcdimension_x3.html
        """
        ## X3 - Значение координаты положения размерной линии и надписи по оси X

        Тип данных: double

        ### Синтаксис Automation

        ```
        X3 = Object.X3       # Получить свойство(* )
        Object.X3 = X3       # Установить свойство (* )
        X2 = Object.GetX2()  # Получить свойство (**)
        Object.SetX3( X3 )   # Установить свойство (**)
        ```
        """
        ...
    @X3.setter
    def X3(self, value: float): ...

    @property
    def Xc(self) -> float:  # iarcdimension_xc.html
        """
        ## Xc - Значение координаты центра по оси X

        Тип данных: double

        ### Синтаксис Automation

        ```
        Xc = Object.Xc       # Получить свойство(* )
        Object.Xc = Xc       # Установить свойство (* )
        Xc = Object.GetXc()  # Получить свойство (**)
        Object.SetXc( Xc )   # Установить свойство (**)
        ```
        """
        ...
    @Xc.setter
    def Xc(self, value: float): ...

    @property
    def Y1(self) -> float:  # iarcdimension_y1.html
        """
        ## Y1 - Значение координаты первой точки дуги по оси Y

        Тип данных: double

        ### Синтаксис Automation

        ```
        Y1 = Object.Y1       # Получить свойство(* )
        Object.Y1 = Y1       # Установить свойство (* )
        Y1 = Object.GetY1()  # Получить свойство (**)
        Object.SetY1( Y1 )   # Установить свойство (**)
        ```
        """
        ...
    @Y1.setter
    def Y1(self, value: float): ...

    @property
    def Y2(self) -> float:  # iarcdimension_y2.html
        """
        ## Y2 - Значение координаты второй точки дуги по оси Y

        Тип данных: double

        ### Синтаксис Automation

        ```
        Y2 = Object.Y2       # Получить свойство(* )
        Object.Y2 = Y2       # Установить свойство (* )
        Y2 = Object.GetY2()  # Получить свойство (**)
        Object.SetY2( Y2 )   # Установить свойство (**)
        ```
        """
        ...
    @Y2.setter
    def Y2(self, value: float): ...

    @property
    def Y3(self) -> float:  # iarcdimension_y3.html
        """
        ## Y3 - Значение координаты положения размерной линии и надписи по оси Y

        Тип данных: double

        ### Синтаксис Automation

        ```
        Y3 = Object.Y3       # Получить свойство(* )
        Object.Y3 = Y3       # Установить свойство (* )
        Y3 = Object.GetY3()  # Получить свойство (**)
        Object.SetY3( Y3 )   # Установить свойство (**)
        ```
        """
        ...
    @Y3.setter
    def Y3(self, value: float): ...

    @property
    def Yc(self) -> float:  # iarcdimension_yc.html
        """
        ## Yc - Значение координаты центра по оси Y

        Тип данных: double

        ### Синтаксис Automation

        ```
        Yc = Object.Yc       # Получить свойство(* )
        Object.Yc = Yc       # Установить свойство (* )
        Yc = Object.GetYc()  # Получить свойство (**)
        Object.SetYc( Yc )   # Установить свойство (**)
        ```
        """
        ...
    @Yc.setter
    def Yc(self, value: float): ...




class IBreakLineDimension(IDrawingObject):  # ibreaklinedimension.html
    """
    ## Интерфейс IBreakLineDimension

    Интерфейс параметров линейного размера с обрывом.

    Интерфейс позволяет получить и задать свойства линейного размера с обрывом.

    Интерфейс можно получить у коллекции линейных размеров с обрывом, используя свойство IBreakLineDimensions::BreakLineDimension или метод IBreakLineDimensions::Add.

    После задания параметров размера требуется вызвать метод IDrawingObject::Update.

    Интерфейсы IDimensionText и IDimensionParams являются дополнительными. Их можно получить с помощью метода IUnknown::QueryInterface.
    """

    @property
    def BaseObject(self) -> IDrawingObject:  # ibreaklinedimension_baseobject.html
        """
        ## BaseObject - Опорный объект

        Тип данных: интерфейс IDrawingObject

        1. Если при создании размера задать опорный объект, то координаты центра IBreakLineDimension::X1, IBreakLineDimension::Y1, IBreakLineDimension::X2, IBreakLineDimension::Y2 задавать не нужно. Данные свойства будут получены с исходного объекта.

        В качестве опорного объекта может использоваться отрезок.

        2. Чтобы разорвать связь с объектом, нужно установить значение свойства BaseObject, равное NULL.

        ### Синтаксис Automation

        ```
        BaseObject = Object.BaseObject       # Получить свойство(* )
        Object.BaseObject = BaseObject       # Установить свойство (* )
        BaseObject = Object.GetBaseObject()  # Получить свойство (**)
        Object.SetBaseObject( BaseObject )   # Установить свойство (**)
        ```
        """
        ...
    @BaseObject.setter
    def BaseObject(self, value: IDrawingObject): ...

    @property
    def ShelfX(self) -> float:  # ibreaklinedimension_shelfx.html
        """
        ## ShelfX - Координата X точки начала полки

        Тип данных: double

        Перед заданием свойства нужно установить признак размещения на полке TextType = ksDTPOnShelf.

        ### Синтаксис Automation

        ```
        ShelfX = Object.ShelfX       # Получить свойство(* )
        Object.ShelfX = ShelfX       # Установить свойство (* )
        ShelfX = Object.GetShelfX()  # Получить свойство (**)
        Object.SetShelfX( ShelfX )   # Установить свойство (**)
        ```
        """
        ...
    @ShelfX.setter
    def ShelfX(self, value: float): ...

    @property
    def ShelfY(self) -> float:  # ibreaklinedimension_shelfy.html
        """
        ## ShelfY - Координата Y точки начала полки

        Тип данных: double

        Перед заданием свойства нужно установить признак размещения на полке TextType = ksDTPOnShelf.

        ### Синтаксис Automation

        ```
        ShelfY = Object.ShelfY       # Получить свойство(* )
        Object.ShelfY = ShelfY       # Установить свойство (* )
        ShelfY = Object.GetShelfY()  # Получить свойство (**)
        Object.SetShelfY( ShelfY )   # Установить свойство (**)
        ```
        """
        ...
    @ShelfY.setter
    def ShelfY(self, value: float): ...

    @property
    def X1(self) -> float:  # ibreaklinedimension_o1.html
        """
        ## X1 - Координата X первой точки привязки размера

        Тип данных: double

        ### Синтаксис Automation

        ```
        X1 = Object.X1       # Получить свойство(* )
        Object.X1 = X1       # Установить свойство (* )
        X1 = Object.GetX1()  # Получить свойство (**)
        Object.SetX1( X1 )   # Установить свойство (**)
        ```
        """
        ...
    @X1.setter
    def X1(self, value: float): ...

    @property
    def X2(self) -> float:  # ibreaklinedimension_x2.html
        """
        ## X2 - Координата X второй точки привязки размера

        Тип данных: double

        ### Синтаксис Automation

        ```
        X2 = Object.X2       # Получить свойство(* )
        Object.X2 = X2       # Установить свойство (* )
        X2 = Object.GetX2()  # Получить свойство (**)
        Object.SetX2( X2 )   # Установить свойство (**)
        ```
        """
        ...
    @X2.setter
    def X2(self, value: float): ...

    @property
    def X3(self) -> float:  # ibreaklinedimension_x3.html
        """
        ## X3 - Координата X положения размерной линии

        Тип данных: double

        ### Синтаксис Automation

        ```
        X3 = Object.X3       # Получить свойство(* )
        Object.X3 = X3       # Установить свойство (* )
        X3 = Object.GetX3()  # Получить свойство (**)
        Object.SetX3( X3 )   # Установить свойство (**)
        ```
        """
        ...
    @X3.setter
    def X3(self, value: float): ...

    @property
    def Y1(self) -> float:  # ibreaklinedimension_y1.html
        """
        ## Y1 - Координата Y первой точки привязки размера

        Тип данных: double

        ### Синтаксис Automation

        ```
        Y1 = Object.Y1       # Получить свойство(* )
        Object.Y1 = Y1       # Установить свойство (* )
        Y1 = Object.GetY1()  # Получить свойство (**)
        Object.SetY1( Y1 )   # Установить свойство (**)
        ```
        """
        ...
    @Y1.setter
    def Y1(self, value: float): ...

    @property
    def Y2(self) -> float:  # ibreaklinedimension_y2.html
        """
        ## Y2 - Координата Y второй точки привязки размера

        Тип данных: double

        ### Синтаксис Automation

        ```
        Y2 = Object.Y2       # Получить свойство(* )
        Object.Y2 = Y2       # Установить свойство (* )
        Y2 = Object.GetY2()  # Получить свойство (**)
        Object.SetY2( Y2 )   # Установить свойство (**)
        ```
        """
        ...
    @Y2.setter
    def Y2(self, value: float): ...

    @property
    def Y3(self) -> float:  # ibreaklinedimension_y3.html
        """
        ## Y3 - Координата Y положения размерной линии

        Тип данных: double

        ### Синтаксис Automation

        ```
        Y3 = Object.Y3       # Получить свойство(* )
        Object.Y3 = Y3       # Установить свойство (* )
        Y3 = Object.GetY3()  # Получить свойство (**)
        Object.SetY3( Y3 )   # Установить свойство (**)
        ```
        """
        ...
    @Y3.setter
    def Y3(self, value: float): ...




class IDiametralDimension(IDrawingObject):  # idiametraldimension.html
    """
    ## Интерфейс IDiametralDimension

    Интерфейс параметров диаметрального размера.

    Интерфейс позволяет получить и задать свойства диаметрального размера.

    Интерфейс можно получить у коллекции диаметральных размеров, используя свойство IDiametralDimensions::DiametralDimension или метод IDiametralDimensions::Add.

    После задания параметров размера требуется вызвать метод IDrawingObject::Update.

    Интерфейсы IDimensionText и IDimensionParams являются дополнительными. Их можно получить с помощью метода IUnknown::QueryInterface.

    Если при создании размера задать опорный объект IDiametralDimension::BaseObject, то координаты центра IDiametralDimension::Xc, IDiametralDimension::Yc и радиус IDiametralDimension::Radius задавать не нужно. Данные свойства будут получены с опорного объекта. Свойство IDimensionText::NominalValue возвращает диаметр.

    В качестве опорного объекта может быть использована окружность или дуга окружности.
    """

    @property
    def Angle(self) -> float:  # idiametraldimension_angle.html
        """
        ## Angle - Угол наклона размерной линии

        Тип данных: double

        ### Синтаксис Automation

        ```
        Angle = Object.Angle       # Получить свойство(* )
        Object.Angle = Angle       # Установить свойство (* )
        Angle = Object.GetAngle()  # Получить свойство (**)
        Object.SetAngle( Angle )   # Установить свойство (**)
        ```
        """
        ...
    @Angle.setter
    def Angle(self, value: float): ...

    @property
    def BaseObject(self) -> IDrawingObject:  # idiametraldimension_baseobject.html
        """
        ## BaseObject - Опорный объект

        Тип данных: интерфейс IDrawingObject

        Если при создании размера задать опорный объект IDiametralDimension::BaseObject, то координаты центра IDiametralDimension::Xc,IDiametralDimension::Yc и радиус IDiametralDimension::Radius задавать не нужно. Данные свойства будут получены с опорного объекта.

        В качестве опорного объекта может быть использована окружность или дуга окружности.

        Чтобы разорвать связь с объектом, нужно установить значение свойства равным NULL.

        ### Синтаксис Automation

        ```
        BaseObject = Object.BaseObject       # Получить свойство(* )
        Object.BaseObject = BaseObject       # Установить свойство (* )
        BaseObject = Object.GetBaseObject()  # Получить свойство (**)
        Object.SetBaseObject( BaseObject )   # Установить свойство (**)
        ```
        """
        ...
    @BaseObject.setter
    def BaseObject(self, value: IDrawingObject): ...

    @property
    def DimensionType(self) -> bool:  # idiametraldimension_dimensiontype.html
        """
        ## DimensionType - Тип диаметрального размера (полная размерная линия\\размерная линия с обрывом)

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        DimensionType = Object.DimensionType       # Получить свойство(* )
        Object.DimensionType = DimensionType       # Установить свойство (* )
        DimensionType = Object.GetDimensionType()  # Получить свойство (**)
        Object.SetDimensionType( DimensionType )   # Установить свойство (**)
        ```
        """
        ...
    @DimensionType.setter
    def DimensionType(self, value: bool): ...

    @property
    def Radius(self) -> float:  # idiametraldimension_radius.html
        """
        ## Radius - Радиус

        Тип данных: double

        ### Синтаксис Automation

        ```
        Radius = Object.Radius       # Получить свойство(* )
        Object.Radius = Radius       # Установить свойство (* )
        Radius = Object.GetRadius()  # Получить свойство (**)
        Object.SetRadius( Radius )   # Установить свойство (**)
        ```
        """
        ...
    @Radius.setter
    def Radius(self, value: float): ...

    @property
    def Xc(self) -> float:  # idiametraldimension_xc.html
        """
        ## Xc - Координата центра по X

        Тип данных: double

        ### Синтаксис Automation

        ```
        Xc = Object.Xc       # Получить свойство(* )
        Object.Xc = Xc       # Установить свойство (* )
        Xc = Object.GetXc()  # Получить свойство (**)
        Object.SetXc( Xc )   # Установить свойство (**)
        ```
        """
        ...
    @Xc.setter
    def Xc(self, value: float): ...

    @property
    def Yc(self) -> float:  # idiametraldimension_yc.html
        """
        ## Yc - Координата центра по Y

        Тип данных: double

        ### Синтаксис Automation

        ```
        Yc = Object.Yc       # Получить свойство(* )
        Object.Yc = Yc       # Установить свойство (* )
        Yc = Object.GetYc()  # Получить свойство (**)
        Object.SetYc( Yc )   # Установить свойство (**)
        ```
        """
        ...
    @Yc.setter
    def Yc(self, value: float): ...




class IHeightDimension(IDrawingObject):  # iheightdimension.html
    """
    ## Интерфейс IHeightDimension

    Интерфейс параметров размера высоты.

    Интерфейс позволяет получить и задать свойства размера высоты.

    Интерфейс можно получить у коллекции размеров высоты, используя свойство IHeightDimensions::HeightDimension или метод IHeightDimensions::Add.

    После задания параметров размера требуется вызвать метод IDrawingObject::Update.

    Интерфейсы IDimensionText и IDimensionParams являются дополнительными. Их можно получить с помощью метода IUnknown::QueryInterface.

    Свойство IDimensionText::NominalValue возвращает значение высоты (Y1 - Y).
    """

    @property
    def DimensionType(self) -> int:  # iheightdimension_dimensiontype.html
        """
        ## DimensionType - Тип размера

        Тип данных: из перечисления ksHeightDimTypeEnum

        ### Синтаксис Automation

        ```
        DimensionType = Object.DimensionType       # Получить свойство(* )
        Object.DimensionType = DimensionType       # Установить свойство (* )
        DimensionType = Object.GetDimensionType()  # Получить свойство (**)
        Object.SetDimensionType( DimensionType )   # Установить свойство (**)
        ```
        """
        ...
    @DimensionType.setter
    def DimensionType(self, value: int): ...

    @property
    def X(self) -> float:  # iheightdimension_x.html
        """
        ## X - Значение координаты точки нулевого уровня по оси X

        Тип данных: double

        ### Синтаксис Automation

        ```
        X = Object.X       # Получить свойство(* )
        Object.X = X       # Установить свойство (* )
        X = Object.GetX()  # Получить свойство (**)
        Object.SetX( X )   # Установить свойство (**)
        ```
        """
        ...
    @X.setter
    def X(self, value: float): ...

    @property
    def X1(self) -> float:  # iheightdimension_x1.html
        """
        ## X1 - Значение координаты точки измеряемого уровняпо оси X

        Тип данных: double

        ### Синтаксис Automation

        ```
        X1 = Object.X1       # Получить свойство(* )
        Object.X1 = X1       # Установить свойство (* )
        X1 = Object.GetX1()  # Получить свойство (**)
        Object.SetX1( X1 )   # Установить свойство (**)
        ```
        """
        ...
    @X1.setter
    def X1(self, value: float): ...

    @property
    def X2(self) -> float:  # iheightdimension_x2.html
        """
        ## X2 - Значение координаты положения размерной надписи по оси X

        Тип данных: double

        ### Синтаксис Automation

        ```
        X2 = Object.X2       # Получить свойство(* )
        Object.X2 = X2       # Установить свойство (* )
        X2 = Object.GetX2()  # Получить свойство (**)
        Object.SetX2( X2 )   # Установить свойство (**)
        ```
        """
        ...
    @X2.setter
    def X2(self, value: float): ...

    @property
    def Y(self) -> float:  # iheightdimension_y.html
        """
        ## Y - Значение координаты точки нулевого уровня по оси Y

        Тип данных: double

        ### Синтаксис Automation

        ```
        Y = Object.Y       # Получить свойство(* )
        Object.Y = Y       # Установить свойство (* )
        Y = Object.GetY()  # Получить свойство (**)
        Object.SetY( Y )   # Установить свойство (**)
        ```
        """
        ...
    @Y.setter
    def Y(self, value: float): ...

    @property
    def Y1(self) -> float:  # iheightdimension_y1.html
        """
        ## Y1 - Значение координаты точки измеряемого уровня по оси Y

        Тип данных: double

        ### Синтаксис Automation

        ```
        Y1 = Object.Y1       # Получить свойство(* )
        Object.Y1 = Y1       # Установить свойство (* )
        Y1 = Object.GetY1()  # Получить свойство (**)
        Object.SetY1( Y1 )   # Установить свойство (**)
        ```
        """
        ...
    @Y1.setter
    def Y1(self, value: float): ...

    @property
    def Y2(self) -> float:  # iheightdimension_y2.html
        """
        ## Y2 - Значение координаты положения размерной надписи по оси Y

        Тип данных: double

        ### Синтаксис Automation

        ```
        Y2 = Object.Y2       # Получить свойство(* )
        Object.Y2 = Y2       # Установить свойство (* )
        Y2 = Object.GetY2()  # Получить свойство (**)
        Object.SetY2( Y2 )   # Установить свойство (**)
        ```
        """
        ...
    @Y2.setter
    def Y2(self, value: float): ...




class ILineDimension(IDrawingObject):  # ilinedimension.html
    """
    ## Интерфейс ILineDimension

    Интерфейс параметров линейного размера.

    Интерфейс позволяет получить и задать свойства линейного размера.

    Интерфейс можно получить у коллекции линейных размеров, используя свойство ILineDimensions::LineDimension или метод ILineDimensions::Add.

    После задания параметров размера требуется вызвать метод IModelObject::Update.

    Интерфейсы IDimensionText и IDimensionParams являются дополнительными. Их можно получить с помощью метода IUnknown::QueryInterface.

    Свойство IDimensionText::NominalValue возвращает расстояние между точками (X1, Y1) и (X2, Y2).
    """

    @property
    def Angle(self) -> float:  # ilinedimension_angle.html
        """
        ## Angle - Угол наклона размера

        Тип данных: double

        1. Свойство позволяет задавать угол наклона выносных линий размера.

        2. Свойство используется при создании размера, параллельного объекту. Свойство ILineDimension::Orientation должно быть равно ksLinDParallel.

        ### Синтаксис Automation

        ```
        Angle = Object.Angle       # Получить свойство(* )
        Object.Angle = Angle       # Установить свойство (* )
        Angle = Object.GetAngle()  # Получить свойство (**)
        Object.SetAngle( Angle )   # Установить свойство (**)
        ```
        """
        ...
    @Angle.setter
    def Angle(self, value: float): ...

    @property
    def Orientation(self) -> int:  # ilinedimension_orientation.html
        """
        ## Orientation - Тип ориентации линейного размера

        Тип данных: из перечисления ksLineDimensionOrientationEnum

        ### Синтаксис Automation

        ```
        Orientation = Object.Orientation       # Получить свойство(* )
        Object.Orientation = Orientation       # Установить свойство (* )
        Orientation = Object.GetOrientation()  # Получить свойство (**)
        Object.SetOrientation( Orientation )   # Установить свойство (**)
        ```
        """
        ...
    @Orientation.setter
    def Orientation(self, value: int): ...

    @property
    def ShelfX(self) -> float:  # ilinedimension_shelfx.html
        """
        ## ShelfX - Координата X точки начала полки

        Тип данных: double

        Перед заданием свойства нужно установить признак размещения на полке TextType = ksDTPOnShelf.

        ### Синтаксис Automation

        ```
        ShelfX = Object.ShelfX       # Получить свойство(* )
        Object.ShelfX = ShelfX       # Установить свойство (* )
        ShelfX = Object.GetShelfX()  # Получить свойство (**)
        Object.SetShelfX( ShelfX )   # Установить свойство (**)
        ```
        """
        ...
    @ShelfX.setter
    def ShelfX(self, value: float): ...

    @property
    def ShelfY(self) -> float:  # ilinedimension_shelfy.html
        """
        ## ShelfY - Координата Y точки начала полки

        Тип данных: double

        Перед заданием свойства нужно установить признак размещения на полке TextType = ksDTPOnShelf.

        ### Синтаксис Automation

        ```
        ShelfY = Object.ShelfY       # Получить свойство(* )
        Object.ShelfY = ShelfY       # Установить свойство (* )
        ShelfY = Object.GetShelfY()  # Получить свойство (**)
        Object.SetShelfY( ShelfY )   # Установить свойство (**)
        ```
        """
        ...
    @ShelfY.setter
    def ShelfY(self, value: float): ...

    @property
    def X1(self) -> float:  # ilinedimension_x1.html
        """
        ## X1 - Координата X первой точки привязки размера

        Тип данных: double

        ### Синтаксис Automation

        ```
        X1 = Object.X1       # Получить свойство(* )
        Object.X1 = X1       # Установить свойство (* )
        X1 = Object.GetX1()  # Получить свойство (**)
        Object.SetX1( X1 )   # Установить свойство (**)
        ```
        """
        ...
    @X1.setter
    def X1(self, value: float): ...

    @property
    def X2(self) -> float:  # ilinedimension_x2.html
        """
        ## X2 - Координата X второй точки привязки размера

        Тип данных: double

        ### Синтаксис Automation

        ```
        X2 = Object.X2       # Получить свойство(* )
        Object.X2 = X2       # Установить свойство (* )
        X2 = Object.GetX2()  # Получить свойство (**)
        Object.SetX2( X2 )   # Установить свойство (**)
        ```
        """
        ...
    @X2.setter
    def X2(self, value: float): ...

    @property
    def X3(self) -> float:  # ilinedimension_x3.html
        """
        ## X3 - Координата X положения размерной линии

        Тип данных: double

        ### Синтаксис Automation

        ```
        X3 = Object.X3       # Получить свойство(* )
        Object.X3 = X3       # Установить свойство (* )
        X3 = Object.GetX3()  # Получить свойство (**)
        Object.SetX3( X3 )   # Установить свойство (**)
        ```
        """
        ...
    @X3.setter
    def X3(self, value: float): ...

    @property
    def Y1(self) -> float:  # ilinedimension_y1.html
        """
        ## Y1 - Координата Y первой точки привязки размера

        Тип данных: double

        ### Синтаксис Automation

        ```
        Y1 = Object.Y1       # Получить свойство(* )
        Object.Y1 = Y1       # Установить свойство (* )
        Y1 = Object.GetY1()  # Получить свойство (**)
        Object.SetY1( Y1 )   # Установить свойство (**)
        ```
        """
        ...
    @Y1.setter
    def Y1(self, value: float): ...

    @property
    def Y2(self) -> float:  # ilinedimension_y2.html
        """
        ## Y2 - Координата Y второй точки привязки размера

        Тип данных: double

        ### Синтаксис Automation

        ```
        Y2 = Object.Y2       # Получить свойство(* )
        Object.Y2 = Y2       # Установить свойство (* )
        Y2 = Object.GetY2()  # Получить свойство (**)
        Object.SetY2( Y2 )   # Установить свойство (**)
        ```
        """
        ...
    @Y2.setter
    def Y2(self, value: float): ...

    @property
    def Y3(self) -> float:  # ilinedimension_y3.html
        """
        ## Y3 - Координата Y положения размерной линии

        Тип данных: double

        ### Синтаксис Automation

        ```
        Y3 = Object.Y3       # Получить свойство(* )
        Object.Y3 = Y3       # Установить свойство (* )
        Y3 = Object.GetY3()  # Получить свойство (**)
        Object.SetY3( Y3 )   # Установить свойство (**)
        ```
        """
        ...
    @Y3.setter
    def Y3(self, value: float): ...

    def GetOrientationEx(self, CoordinateSystem) -> int:  # ilinedimension_getorientationex.html
        """
        ## GetOrientationEx - Получить тип ориентации линейного размера с учетом системы координат

        ### Синтаксис Automation

        ```
        ksLineDimensionOrientationEnum GetOrientationEx( ILocalCoordinateSystem2D * CoordinateSystem );
        ```

        ### Входные параметры

        +------------------------------------------------------------------+
        | CoordinateSystem | - система координат ILocalCoordinateSystem2D. |
        +------------------------------------------------------------------+

        Версия КОМПАС v22

        ### Возвращаемое значение

        - Тип ориентации линейного размера из перечисления ksLineDimensionOrientationEnum.
        """
        ...

    def SetOrientationEx(self, CoordinateSystem, Value) -> bool:  # ilinedimension_setorientationex.html
        """
        ## SetOrientationEx - Установить тип ориентации линейного размера с учетом системы координат

        ### Синтаксис Automation

        ```
        BOOL SetOrientationEx( ILocalCoordinateSystem2D * CoordinateSystem, ksLineDimensionOrientationEnum Value );
        ```

        ### Входные параметры

        +-------------------------------------------------------------------------------------------------------+
        | CoordinateSystem | - система координат ILocalCoordinateSystem2D.                                      |
        +-------------------------------------------------------------------------------------------------------+
        | Value            | - тип ориентации линейного размера из перечисления ksLineDimensionOrientationEnum. |
        +-------------------------------------------------------------------------------------------------------+

        Версия КОМПАС v22

        ### Возвращаемое значение

        +-----------------------------------------+
        | TRUE  | - в случае удачного завершения, |
        +-----------------------------------------+
        | FALSE | - в случае неудачи.             |
        +-----------------------------------------+
        """
        ...




class ILineSegmentAndPointDimension(IDispatch):  # ilinesegmentandpointdimension.html
    """
    ## Интерфейс ILineSegmentAndPointDimension

    Интерфейс размера от отрезка до точки.

    Интерфейс является дополнительным для линейного размера ILineDimension, созданным командой Линейный от отрезка до точки

    Для создания размера от отрезка до точки требуется воспользоваться методом ILineDimensions::AddLineSegmentAndPointDimension.
    """

    def GetBaseObject(self, Index) -> IDrawingObject:  # ilinesegmentandpointdimension_getbaseobject.html
        """
        ## GetBaseObject - Получить базовый объект

        ### Синтаксис Automation

        ```
        LPDISPATCH GetBaseObject( long Index );
        ```

        ### Входные параметры

        +------------------------------------+
        | Index | - индекс базового объекта. |
        +------------------------------------+

        ### Возвращаемое значение

        Указатель на интерфейс базового объекта IDrawingObject
        """
        ...

    def GetBaseObjectPoint(self, PointIndex) -> bool:  # ilinesegmentandpointdimension_getbaseobjectpoint.html
        """
        ## GetBaseObjectPoint - Получить координаты точек размера

        ### Синтаксис Automation

        ```
        BOOL GetBaseObjectPoint( long PointIndex, double * X, double * Y );
        ```

        ### Входные параметры

        +-----------------------------------------------+
        | PointIndex | - индекс точки базового объекта. |
        +-----------------------------------------------+

        ### Выходные параметры

        +----------------------------+
        | X, Y | - координаты точки. |
        +----------------------------+

        ### Возвращаемое значение

        +-----------------------------------------+
        | TRUE  | - в случае удачного завершения, |
        +-----------------------------------------+
        | FALSE | - в случае неудачи.             |
        +-----------------------------------------+
        """
        ...

    def SetBaseObject(self, Index, NewObject, SegmentIndex):  # ilinesegmentandpointdimension_setbaseobject.html
        """
        ## SetBaseObject - Установить базовый объект

        ### Синтаксис Automation

        ```
        BOOL SetBaseObject( long Index, IDrawingObject * NewObject, long SegmentIndex );
        ```

        ### Входные параметры

        +-----------------------------------------------------------------------+
        | Index        | - индекс базового объекта,                             |
        +-----------------------------------------------------------------------+
        | NewObject    | - базовыйй объект,                                     |
        +-----------------------------------------------------------------------+
        | SegmentIndex | - индекс-идентификатор примитива в составных объектах. |
        +-----------------------------------------------------------------------+
        """
        ...

    def SetBaseObjectPoint(self, PointIndex, X, Y) -> bool:  # ilinesegmentandpointdimension_setbaseobjectpoint.html
        """
        ## SetBaseObjectPoint - Установить координаты точек размера

        ### Синтаксис Automation

        ```
        BOOL SetBaseObjectPoint( long PointIndex, double X, double Y );
        ```

        ### Входные параметры

        +-----------------------------------------------+
        | PointIndex | - индекс точки базового объекта, |
        +-----------------------------------------------+
        | X, Y       | - координаты точки.              |
        +-----------------------------------------------+

        ### Возвращаемое значение

        +-----------------------------------------+
        | TRUE  | - в случае удачного завершения, |
        +-----------------------------------------+
        | FALSE | - в случае неудачи.             |
        +-----------------------------------------+
        """
        ...




class IRadialDimension(IDrawingObject):  # iradialdimension.html
    """
    ## Интерфейс IRadialDimension

    Интерфейс параметров радиального размера.

    Интерфейс позволяет получить и задать свойства радиального размера.

    Интерфейс можно получить у коллекции радиальных размеров, используя свойство IRadialDimensions::RadialDimension или метод IRadialDimensions::Add.

    После задания параметров размера требуется вызвать метод IDrawingObject::Update.

    Интерфейсы IDimensionText и IDimensionParams являются дополнительными. Их можно получить с помощью метода IUnknown::QueryInterface

    Если при создании размера задать опорный объект IRadialDimension::BaseObject, то координаты центра IRadialDimension::XcIRadialDimension::Yc и радиус IRadialDimension::Radius задавать не нужно. Данные свойства будут получены с опорного объекта. Свойство IDimensionText::NominalValue возвращает диаметр.

    В качестве опорного объекта может быть использована окружность или дуга окружности.
    """

    @property
    def Angle(self) -> float:  # iradialdimension_angle.html
        """
        ## Angle - Угол наклона размерной линии

        Тип данных: double

        ### Синтаксис Automation

        ```
        Angle = Object.Angle       # Получить свойство(* )
        Object.Angle = Angle       # Установить свойство (* )
        Angle = Object.GetAngle()  # Получить свойство (**)
        Object.SetAngle( Angle )   # Установить свойство (**)
        ```
        """
        ...
    @Angle.setter
    def Angle(self, value: float): ...

    @property
    def BaseObject(self) -> IDrawingObject:  # iradialdimension_baseobject.html
        """
        ## BaseObject - Опорный объект

        Тип данных: интерфейс IDrawingObject

        Если при создании размера задать опорный объект IRadialDimension::BaseObject, то координаты центра IRadialDimension::XcIRadialDimension::Yc и радиус IRadialDimension::Radius задавать не нужно. Данные свойства будут получены с опорного объекта.

        В качестве опорного объекта может быть использована окружность или дуга окружности.

        Чтобы разорвать связь с объектом, нужно установить значение свойства равным NULL.

        ### Синтаксис Automation

        ```
        BaseObject = Object.BaseObject       # Получить свойство(* )
        Object.BaseObject = BaseObject       # Установить свойство (* )
        BaseObject = Object.GetBaseObject()  # Получить свойство (**)
        Object.SetBaseObject( BaseObject )   # Установить свойство (**)
        ```
        """
        ...
    @BaseObject.setter
    def BaseObject(self, value: IDrawingObject): ...

    @property
    def BranchsCount(self) -> int:  # iradialdimension_branchscount.html
        """
        ## BranchsCount - Количество ответвлений

        Тип данных: long

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        BranchsCount = Object.BranchsCount       # Получить свойство(* )
        BranchsCount = Object.GetBranchsCount()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def DimensionType(self) -> bool:  # iradialdimension_dimensiontype.html
        """
        ## DimensionType - Тип радиального размера (от центра\\не от центра)

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        DimensionType = Object.DimensionType       # Получить свойство(* )
        Object.DimensionType = DimensionType       # Установить свойство (* )
        DimensionType = Object.GetDimensionType()  # Получить свойство (**)
        Object.SetDimensionType( DimensionType )   # Установить свойство (**)
        ```
        """
        ...
    @DimensionType.setter
    def DimensionType(self, value: bool): ...

    @property
    def Radius(self) -> float:  # iradialdimension_radius.html
        """
        ## Radius - Радиус

        Тип данных: double

        ### Синтаксис Automation

        ```
        Radius = Object.Radius       # Получить свойство(* )
        Object.Radius = Radius       # Установить свойство (* )
        Radius = Object.GetRadius()  # Получить свойство (**)
        Object.SetRadius( Radius )   # Установить свойство (**)
        ```
        """
        ...
    @Radius.setter
    def Radius(self, value: float): ...

    @property
    def ShelfX(self) -> float:  # iradialdimension_shelfx.html
        """
        ## ShelfX - Точка начала полки - координата X

        Тип данных: double

        Перед заданием свойства нужно установить признак размещения на полке TextType = ksDTPOnShelf.

        ### Синтаксис Automation

        ```
        ShelfX = Object.ShelfX       # Получить свойство(* )
        Object.ShelfX = ShelfX       # Установить свойство (* )
        ShelfX = Object.GetShelfX()  # Получить свойство (**)
        Object.SetShelfX( ShelfX )   # Установить свойство (**)
        ```
        """
        ...
    @ShelfX.setter
    def ShelfX(self, value: float): ...

    @property
    def ShelfY(self) -> float:  # iradialdimension_shelfy.html
        """
        ## ShelfY - Точка начала полки - координата Y

        Тип данных: double

        Перед заданием свойства нужно установить признак размещения на полке TextType = ksDTPOnShelf.

        ### Синтаксис Automation

        ```
        ShelfY = Object.ShelfY       # Получить свойство(* )
        Object.ShelfY = ShelfY       # Установить свойство (* )
        ShelfY = Object.GetShelfY()  # Получить свойство (**)
        Object.SetShelfY( ShelfY )   # Установить свойство (**)
        ```
        """
        ...
    @ShelfY.setter
    def ShelfY(self, value: float): ...

    @property
    def Xc(self) -> float:  # iradialdimension_xc.html
        """
        ## Xc - Координата центра по X

        Тип данных: double

        ### Синтаксис Automation

        ```
        Xc = Object.Xc       # Получить свойство(* )
        Object.Xc = Xc       # Установить свойство (* )
        Xc = Object.GetXc()  # Получить свойство (**)
        Object.SetXc( Xc )   # Установить свойство (**)
        ```
        """
        ...
    @Xc.setter
    def Xc(self, value: float): ...

    @property
    def Yc(self) -> float:  # iradialdimension_yc.html
        """
        ## Yc - Координата центра по Y

        Тип данных: double

        ### Синтаксис Automation

        ```
        Yc = Object.Yc       # Получить свойство(* )
        Object.Yc = Yc       # Установить свойство (* )
        Yc = Object.GetYc()  # Получить свойство (**)
        Object.SetYc( Yc )   # Установить свойство (**)
        ```
        """
        ...
    @Yc.setter
    def Yc(self, value: float): ...

    def AddBranch(self, BranchBegin, BranchObject) -> bool:  # iradialdimension_addbranch.html
        """
        ## AddBranch - Добавить ответвление для объекта

        ### Синтаксис Automation

        ```
        BOOL AddBranch( BOOL BranchBegin,
        IDrawingObject * BranchObject );
        ```

        ### Входные параметры

        +-------------------------------------------+
        | BranchBegin  | - строить от начала полки, |
        +-------------------------------------------+
        | BranchObject | - опорный объект.          |
        +-------------------------------------------+

        Метод позволяет добавить ответвление, используя опорный объект.

        ### Возвращаемое значение

        +-----------------------------------------+
        | TRUE  | - в случае удачного завершения, |
        +-----------------------------------------+
        | FALSE | - в случае неудачи.             |
        +-----------------------------------------+
        """
        ...

    def AddBranchByArcParam(self, BranchBegin, Xc, Yc, Radius, Angle1, Angle2, Direction) -> bool:  # iradialdimension_addbranchbyarcparam.html
        """
        ## AddBranchByArcParam - Добавить ответвление по параметрам дуги

        ### Синтаксис Automation

        ```
        BOOL AddBranchByArcParam( BOOL BranchBegin,
        double Xc,
        double Yc,
        double Radius,
        double Angle1,
        double Angle2,
        BOOL Direction );
        ```

        ### Входные параметры

        +------------------------------------------+
        | BranchBegin | - строить от начала полки, |
        +------------------------------------------+
        | Xc,Yc       | - координаты центра дуги,  |
        +------------------------------------------+
        | Radius      | - радиус дуги,             |
        +------------------------------------------+
        | Angle1      | - 1-й угол дуги,           |
        +------------------------------------------+
        | Angle2      | - 2-й угол дуги,           |
        +------------------------------------------+
        | Direction   | - направление дуги.        |
        +------------------------------------------+

        Метод позволяет построить ответвление, используя параметры дуги.

        ### Возвращаемое значение

        +-----------------------------------------+
        | TRUE  | - в случае удачного завершения, |
        +-----------------------------------------+
        | FALSE | - в случае неудачи.             |
        +-----------------------------------------+
        """
        ...

    def BranchBegin(self, Index) -> bool:  # iradialdimension_branchbegin.html
        """
        ## BranchBegin - Начало ответвления

        Тип данных: BOOL

        Значения свойства:

        +----------------------------+
        | TRUE  | - от начала полки, |
        +----------------------------+
        | FALSE | - от конца полки.  |
        +----------------------------+

        ### Синтаксис Automation

        ```
        BranchBegin = Object.BranchBegin( Index )     # Получить свойство(* )
        Object.BranchBegin( Index ) = BranchBegin     # Установить свойство (* )
        BranchBegin = Object.GetBranchBegin( Index )  # Получить свойство (**)
        BranchBegin = Object.GetBranchBegin( Index )  # Установить свойство (**)
        ```

        ### Входные параметры

        long Index - номер ответвления
        """
        ...

    def BranchObject(self, Index) -> IDrawingObject:  # iradialdimension_branchobject.html
        """
        ## BranchObject - Опорный объект для дополнительного ответвления

        Тип данных: интерфейс IDrawingObject

        ### Синтаксис Automation

        ```
        BranchObject = Object.BranchObject( Index )     # Получить свойство(* )
        Object.BranchObject( Index ) = BranchObject     # Установить свойство (* )
        BranchObject = Object.GetBranchObject( Index )  # Получить свойство (**)
        Object.SetBranchObject( Index, BranchObject )   # Установить свойство (**)
        ```

        ### Входные параметры

        long Index - номер ответвления.

        Свойство позволяет устанавливать и получать объект, на который указывает ответвление.
        """
        ...

    def DeleteBranch(self, Index) -> bool:  # iradialdimension_deletebranch.html
        """
        ## DeleteBranch - Удалить ответвление

        ### Синтаксис Automation

        ```
        BOOL DeleteBranch( long Index );
        ```

        ### Входные параметры

        Index - номер ответвления.

        ### Возвращаемое значение

        +-----------------------------------------+
        | TRUE  | - в случае удачного завершения, |
        +-----------------------------------------+
        | FALSE | - в случае неудачи.             |
        +-----------------------------------------+
        """
        ...

    def GetBranchParam(self, Index) -> bool:  # iradialdimension_getbranchparam.html
        """
        ## GetBranchParam - Получить параметры ответвления для объекта

        ### Синтаксис Automation

        ```
        BOOL GetBranchParam( long Index,
        double * Xc,
        double * Yc,
        double * Angle1,
        double * Angle2,
        BOOL * Direction );
        ```

        ### Входные параметры

        Index - номер ответвления.

        ### Выходные параметры

        +---------------------------------------+
        | Xc,Yc     | - координаты центра дуги, |
        +---------------------------------------+
        | Angle1    | - 1-й угол дуги,          |
        +---------------------------------------+
        | Angle2    | - 2-й угол дуги,          |
        +---------------------------------------+
        | Direction | - направление дуги.       |
        +---------------------------------------+

        ### Возвращаемое значение

        +-----------------------------------------+
        | TRUE  | - в случае удачного завершения, |
        +-----------------------------------------+
        | FALSE | - в случае неудачи.             |
        +-----------------------------------------+
        """
        ...

    def SetBranchBegin(self, Index, arg1):
        ...

    def SetBranchObject(self, Index, arg1):
        ...

    def SetBranchParam(self, Index, Xc, Yc, Angle1, Angle2, Direction) -> bool:  # iradialdimension_setbranchparam.html
        """
        ## SetBranchParam - Установить параметры ответвления для объекта

        ### Синтаксис Automation

        ```
        BOOL SetBranchParam( long Index, double Xc, double Yc, double Angle1, double Angle2, BOOL Direction );
        ```

        ### Входные параметры

        +---------------------------------------+
        | Index     | - номер ответвления,      |
        +---------------------------------------+
        | Xc,Yc     | - координаты центра дуги, |
        +---------------------------------------+
        | Radius    | - радиус дуги,            |
        +---------------------------------------+
        | Angle1    | - 1-й угол дуги,          |
        +---------------------------------------+
        | Angle2    | - 2-й угол дуги,          |
        +---------------------------------------+
        | Direction | - направление дуги.       |
        +---------------------------------------+

        ### Возвращаемое значение

        +-----------------------------------------+
        | TRUE  | - в случае удачного завершения, |
        +-----------------------------------------+
        | FALSE | - в случае неудачи.             |
        +-----------------------------------------+
        """
        ...




class IBreakRadialDimension(IDrawingObject):  # ibreakradialdimension.html
    """
    ## Интерфейс IBreakRadialDimension

    Интерфейс параметров радиального размера с изломом.

    Интерфейс позволяет получить и задать свойства радиального размера с изломом.

    Интерфейс можно получить у коллекции радиальных размеров с изломом, используя свойство IBreakRadialDimensions::BreakRadialDimension или метод IBreakRadialDimensions::Add.

    После задания параметров размера требуется вызвать метод IDrawingObject::Update.

    Интерфейс IDimensionText является дополнительным. Его можно получить с помощью метода IUnknown::QueryInterface. Интерфейс IDimensionParams для радиального размер с изломом не поддерживается.

    Если при создании размера задать опорный объект IBreakRadialDimension::BaseObject, то координаты центра IBreakRadialDimension::Xc, IBreakRadialDimension::Yc и радиус IBreakRadialDimension::Radius задавать не нужно. Данные свойства будут получены с опорного объекта. Свойство IDimensionText::NominalValue возвращает радиус.

    В качестве опорного объекта может быть использована окружность или дуга окружности.
    """

    @property
    def Angle(self) -> float:  # ibreakradialdimension_angle.html
        """
        ## Angle - Угол наклона размерной линии

        Тип данных: double

        ### Синтаксис Automation

        ```
        Angle = Object.Angle       # Получить свойство(* )
        Object.Angle = Angle       # Установить свойство (* )
        Angle = Object.GetAngle()  # Получить свойство (**)
        Object.SetAngle( Angle )   # Установить свойство (**)
        ```
        """
        ...
    @Angle.setter
    def Angle(self, value: float): ...

    @property
    def BaseObject(self) -> IDrawingObject:  # ibreakradialdimension_baseobject.html
        """
        ## BaseObject - Опорный объект

        Тип данных: интерфейс IDrawingObject

        Если при создании размера задать опорный объект IBreakRadialDimension::BaseObject, то координаты центра IBreakRadialDimension::XcIBreakRadialDimension::Yc и радиус IBreakRadialDimension::Radius задавать не нужно. Данные свойства будут получены с опорного объекта.

        В качестве опорного объекта может быть использована окружность или дуга окружности.

        Чтобы разорвать связь с объектом, нужно установить значение свойства равным NULL.

        ### Синтаксис Automation

        ```
        BaseObject = Object.BaseObject       # Получить свойство(* )
        Object.BaseObject = BaseObject       # Установить свойство (* )
        BaseObject = Object.GetBaseObject()  # Получить свойство (**)
        Object.SetBaseObject( BaseObject )   # Установить свойство (**)
        ```
        """
        ...
    @BaseObject.setter
    def BaseObject(self, value: IDrawingObject): ...

    @property
    def BreakAngle(self) -> float:  # ibreakradialdimension_breakangle.html
        """
        ## BreakAngle - Угол излома

        Тип данных: double

        ### Синтаксис Automation

        ```
        BreakAngle = Object.BreakAngle       # Получить свойство(* )
        Object.BreakAngle = BreakAngle       # Установить свойство (* )
        BreakAngle = Object.GetBreakAngle()  # Получить свойство (**)
        Object.SetBreakAngle( BreakAngle )   # Установить свойство (**)
        ```
        """
        ...
    @BreakAngle.setter
    def BreakAngle(self, value: float): ...

    @property
    def BreakLength(self) -> float:  # ibreakradialdimension_breaklength.html
        """
        ## BreakLength - Длина излома

        Тип данных: double

        ### Синтаксис Automation

        ```
        BreakLength = Object.BreakLength       # Получить свойство(* )
        Object.BreakLength = BreakLength       # Установить свойство (* )
        BreakLength = Object.GetBreakLength()  # Получить свойство (**)
        Object.SetBreakLength( BreakLength )   # Установить свойство (**)
        ```
        """
        ...
    @BreakLength.setter
    def BreakLength(self, value: float): ...

    @property
    def BreakX1(self) -> float:  # ibreakradialdimension_breakx1.html
        """
        ## BreakX1 - Координата X начала излома

        Тип данных: double

        ### Синтаксис Automation

        ```
        BreakX1 = Object.BreakX1       # Получить свойство(* )
        Object.BreakX1 = BreakX1       # Установить свойство (* )
        BreakX1 = Object.GetBreakX1()  # Получить свойство (**)
        Object.SetBreakX1( BreakX1 )   # Установить свойство (**)
        ```
        """
        ...
    @BreakX1.setter
    def BreakX1(self, value: float): ...

    @property
    def BreakX2(self) -> float:  # ibreakradialdimension_breakx2.html
        """
        ## BreakX2 - Координата X конца излома

        Тип данных: double

        ### Синтаксис Automation

        ```
        BreakX2 = Object.BreakX2       # Получить свойство(* )
        Object.BreakX2 = BreakX2       # Установить свойство (* )
        BreakX2 = Object.GetBreakX2()  # Получить свойство (**)
        Object.SetBreakX2( BreakX2 )   # Установить свойство (**)
        ```
        """
        ...
    @BreakX2.setter
    def BreakX2(self, value: float): ...

    @property
    def BreakY1(self) -> float:  # ibreakradialdimension_breaky1.html
        """
        ## BreakY1 - Координата Y начала излома

        Тип данных: double

        ### Синтаксис Automation

        ```
        BreakY1 = Object.BreakY1       # Получить свойство(* )
        Object.BreakY1 = BreakY1       # Установить свойство (* )
        BreakY1 = Object.GetBreakY1()  # Получить свойство (**)
        Object.SetBreakY1( BreakY1 )   # Установить свойство (**)
        ```
        """
        ...
    @BreakY1.setter
    def BreakY1(self, value: float): ...

    @property
    def BreakY2(self) -> float:  # ibreakradialdimension_breaky2.html
        """
        ## BreakY2 - Координата Y конца излома

        Тип данных: double

        ### Синтаксис Automation

        ```
        BreakY2 = Object.BreakY2       # Получить свойство(* )
        Object.BreakY2 = BreakY2       # Установить свойство (* )
        BreakY2 = Object.GetBreakY2()  # Получить свойство (**)
        Object.SetBreakY2( BreakY2 )   # Установить свойство (**)
        ```
        """
        ...
    @BreakY2.setter
    def BreakY2(self, value: float): ...

    @property
    def Radius(self) -> float:  # ibreakradialdimension_radius.html
        """
        ## Radius - Радиус

        Тип данных: double

        ### Синтаксис Automation

        ```
        Radius = Object.Radius       # Получить свойство(* )
        Object.Radius = Radius       # Установить свойство (* )
        Radius = Object.GetRadius()  # Получить свойство (**)
        Object.SetRadius( Radius )   # Установить свойство (**)
        ```
        """
        ...
    @Radius.setter
    def Radius(self, value: float): ...

    @property
    def TextOnLine(self) -> int:  # ibreakradialdimension_textonline.html
        """
        ## TextOnLine - Положение размерной надписи отноcительно размерной линии

        Тип данных: из перечисления ksDimensionTextPosEnum

        Позволяет задать положение размерной надписи отноcительно размерной линии. Умолчательное положение определяется в диалоге настройки положения надписи, сделанной для текущего документа.

        ### Синтаксис Automation

        ```
        TextOnLine = Object.TextOnLine       # Получить свойство(* )
        Object.TextOnLine = TextOnLine       # Установить свойство (* )
        TextOnLine = Object.GetTextOnLine()  # Получить свойство (**)
        Object.SetTextOnLine( TextOnLine )   # Установить свойство (**)
        ```
        """
        ...
    @TextOnLine.setter
    def TextOnLine(self, value: int): ...

    @property
    def Xc(self) -> float:  # ibreakradialdimension_xc.html
        """
        ## Xc - Координата центра по X

        Тип данных: double

        ### Синтаксис Automation

        ```
        Xc = Object.Xc       # Получить свойство(* )
        Object.Xc = Xc       # Установить свойство (* )
        Xc = Object.GetXc()  # Получить свойство (**)
        Object.SetXc( Xc )   # Установить свойство (**)
        ```
        """
        ...
    @Xc.setter
    def Xc(self, value: float): ...

    @property
    def Yc(self) -> float:  # ibreakradialdimension_yc.html
        """
        ## Yc - Координата центра по Y

        Тип данных: double

        ### Синтаксис Automation

        ```
        Yc = Object.Yc       # Получить свойство(* )
        Object.Yc = Yc       # Установить свойство (* )
        Yc = Object.GetYc()  # Получить свойство (**)
        Object.SetYc( Yc )   # Установить свойство (**)
        ```
        """
        ...
    @Yc.setter
    def Yc(self, value: float): ...




class IBuildingContainer(IDispatch):  # ibuildingcontainer.html
    """
    ## Интерфейс IBuildingContainer

    Контейнер объектов СПДС.

    Позволяет получить доступ к коллекциям объектов СПДС.

    Дополнительный интерфейс вида. Данный интерфейс можно получить у вида IView посредством вызова метода IUnknown::QueryInterface (const GUID far& IID, void** pif).
    """

    @property
    def Braces(self) -> IBraces:  # ibuildingcontainer_braces.html
        """
        ## Braces - Интерфейс коллекции фигурных скобок

        Тип данных: указатель на интерфейс коллекции фигурных скобок IBraces

        1. Свойство доступно только для чтения.

        2. Коллекция позволяет получать и создавать фигурные скобки.

        ### Синтаксис Automation

        ```
        IBraces = iObject.Braces;       # Получить свойство(* )
        IBraces = iObject.GetBraces();  # Получить свойство (**)
        ```
        """
        ...

    @property
    def BuildingAxes(self) -> IBuildingAxes:  # ibuildingcontainer_buildingaxes.html
        """
        ## BuildingAxes - Интерфейс коллекции строительных осей

        Тип данных: указатель на интерфейс IBuildingAxes коллекции строительных осей

        1. Свойство доступно только для чтения.

        2. Коллекция позволяет получать и создавать строительные оси.

        ### Синтаксис Automation

        ```
        BuildingAxes = iObject.BuildingAxes       # Получить свойство(* )
        BuildingAxes = iObject.GetBuildingAxes()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def BuildingCutLines(self) -> ICutLines:  # ibuildingcontainer_buildingcutlines.html
        """
        ## BuildingCutLines - Коллекция линий разреза для СПДС

        Тип данных: указатель на интерфейс ICutLines

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        BuildingCutLines = Object.BuildingCutLines       # Получить свойство(* )
        BuildingCutLines = Object.GetBuildingCutLines()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def CutUnitMarkings(self) -> ICutUnitMarkings:  # ibuildingcontainer_cutunitmarkings.html
        """
        ## CutUnitMarkings - Интерфейс коллекции обозначений узла в сечении

        Тип данных: указатель на интерфейс коллекции обозначений узла в сечении ICutUnitMarkings

        1. Свойство доступно только для чтения.

        2. Коллекция позволяет получать и создавать обозначения узла в сечении.

        ### Синтаксис Automation

        ```
        CutUnitMarkings = iObject.CutUnitMarkings       # Получить свойство(* )
        CutUnitMarkings = iObject.GetCutUnitMarkings()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def Marks(self) -> IMarks:  # ibuildingcontainer_marks.html
        """
        ## Marks - Интерфейс коллекции марок

        Тип данных: указатель на интерфейс коллекции марок IMarks

        1. Свойство доступно только для чтения.

        2. Коллекция позволяет получать и создавать марки.

        ### Синтаксис Automation

        ```
        Marks = iObject.Marks       # Получить свойство(* )
        Marks = iObject.GetMarks()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def MultiTextLeaders(self) -> IMultiTextLeaders:  # ibuildingcontainer_multitextleaders.html
        """
        ## MultiTextLeaders - Интерфейс коллекции выносных надписей к многослойным конструкциям

        Тип данных: указатель на интерфейс коллекции надписей к многослойным конструкциям IMultiTextLeaders

        1. Свойство доступно только для чтения.

        2. Коллекция позволяет получать и создавать надписи к многослойным конструкциям.

        ### Синтаксис Automation

        ```
        MultiTextLeaders = iObject.MultiTextLeaders       # Получить свойство(* )
        MultiTextLeaders = iObject.GetMultiTextLeaders()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def UnitMarkings(self) -> IUnitMarkings:  # ibuildingcontainer_unitmarkings.html
        """
        ## UnitMarkings - Интерфейс коллекции обозначений узлов

        Тип данных: указатель на интерфейс коллекции обозначений узлов IUnitMarkings

        1. Свойство доступно только для чтения.

        2. Коллекция позволяет получать и создавать обозначения узлов.

        ### Синтаксис Automation

        ```
        UnitMarkings = iObject.UnitMarkings       # Получить свойство(* )
        UnitMarkings = iObject.GetUnitMarkings()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def UnitNumbers(self) -> IUnitNumbers:  # ibuildingcontainer_unitnumbers.html
        """
        ## UnitNumbers - Интерфейс коллекции номеров узла

        Тип данных: указатель на интерфейс коллекции номеров узла IUnitNumbers

        1. Свойство доступно только для чтения.

        2. Коллекция позволяет получать и создавать номера узла.

        ### Синтаксис Automation

        ```
        IUnitNumbers = iObject.UnitNumbers;       # Получить свойство(* )
        IUnitNumbers = iObject.GetUnitNumbers();  # Получить свойство (**)
        ```
        """
        ...




class IBraces(IDrawingObjects):  # ibraces.html
    """
    ## Интерфейс IBraces

    Интерфейс коллекции фигурных скобок.

    Интерфейс позволяет получить доступ к фигурным скобкам на чертеже.

    Получить интерфейс коллекции фигурных скобок можно, используя метод контейнера объектов СПДС IBuildingContainer::Braces.
    """

    def Add(self) -> IBrace:  # ibraces_add.html
        """
        ## Add - Создать фигурную скобку

        Метод позволяет создать новый интерфейс фигурной скобки. После получения нового интерфейса нужно задать параметры фигурной скобки и вызвать метод IDrawingObject::Update.

        ### Синтаксис Automation

        ```
        LPDISPATH Add( );
        ```

        ### Возвращаемое значение

        +-----------------------------------------------------------+
        | Result | - Указатель на интерфейс фигурной скобки IBrace. |
        +-----------------------------------------------------------+
        """
        ...

    def Brace(self, Index) -> IBrace:  # ibraces_brace.html
        """
        ## Brace - Фигурная скобка, заданная по индексу, ссылке или имени и номеру

        Тип данных: указатель на интерфейс фигурной скобки IBrace

        Пример:

        Brace = iBraces.Brace("Text");

        где "Brace" - текст на фигурной скобке.

        1. Свойство доступно только для чтения.

        2. В качестве индекса могут использоваться следующие типы:

        •индекс объекта в коллекции,

        •ссылка на объект (reference),

        •строковое значение текста IBrace::Text.

        ### Синтаксис Automation

        ```
        Brace = iObject.Brace( index )     # Получить свойство(* )
        Brace = iObject.GetBrace( index )  # Получить свойство (**)
        ```

        ### Входные параметры

        +---------------------------+
        | Index (Variant) | Индекс. |
        +---------------------------+
        """
        ...




class IBuildingAxes(IDrawingObjects):  # ibuildingaxes.html
    """
    ## Интерфейс IBuildingAxes

    Интерфейс коллекции строительных (координационных) осей.

    Интерфейс позволяет получать и создавать объекты СПДС "строительные оси".

    Получить интерфейс коллекции строительных осей можно, используя метод контейнера объектов СПДС IBuildingContainer::BuildingAxes.
    """

    def Add(self, AxisType) -> IBuildingAxis:  # ibuildingaxes_add.html
        """
        ## Add - Создать ось

        Метод позволяет создать новый интерфейс строительной оси. После получения нового интерфейса нужно задать параметры марки и вызвать метод IDrawingObject::Update.

        ### Синтаксис Automation

        ```
        LPDISPATH Add( DrawingObjectTypeEnum type );
        ```

        ### Входные параметры

        type - тип объекта из перечисления DrawingObjectTypeEnum.

        Возможные значения type:

        +---------------------------------------------------+
        | ksDrStraightAxis | 56 | IStraightAxis Прямая ось, |
        +---------------------------------------------------+
        | ksDrCircleAxis   | 58 | ICircleAxis Круговая ось, |
        +---------------------------------------------------+
        | ksDrArcAxis      | 59 | IArcAxis Дуговая ось.     |
        +---------------------------------------------------+

        ### Возвращаемое значение

        +--------------------------------------------------------------------------------+
        | Result | - Указатель на базовый интерфейс для строительных осей IBuildingAxis. |
        +--------------------------------------------------------------------------------+
        """
        ...

    def BuildingAxis(self, Index) -> IBuildingAxis:  # ibuildingaxes_buildingaxis.html
        """
        ## BuildingAxis - Ось, заданная по индексу, ссылке или имени

        Тип данных: указатель на интерфейс оси IBuildingAxis

        Пример:

        axis = iBuildingAxes.BuildingAxis("A1")

        где "А1" - текст оси

        1. Свойство доступно только для чтения.

        2. В качестве индекса могут использоваться следующие типы:

        •Индекс объекта в коллекции,

        •Ссылка на объект (reference),

        •Строковое значение текста строительной оси IBuildingAxis::Text.

        ### Синтаксис Automation

        ```
        Axis = iObject.BuildingAxis(index)     # Получить свойство(* )
        Axis = iObject.GetBuildingAxis(index)  # Получить свойство (**)
        ```

        ### Входные параметры

        +---------------------------+
        | Index (Variant) | Индекс. |
        +---------------------------+
        """
        ...




class IMarks(IDrawingObjects):  # imarks.html
    """
    ## Интерфейс IMarks

    Интерфейс коллекции марок (содержит марки всех типов).

    Интерфейс позволяет получить доступ к маркам на чертеже.

    Получить интерфейс коллекции марок можно, используя метод контейнера объектов СПДС IBuildingContainer::Marks.
    """

    def Add(self, MarkType) -> IMark:  # imarks_add.html
        """
        ## Add - Создать марку

        Метод позволяет создать новый интерфейс марки. После получения нового интерфейса нужно задать параметры марки и вызвать метод IDrawingObject::Update.

        ### Синтаксис Automation

        ```
        LPDISPATH Add( DrawingObjectTypeEnum MarkType );
        ```

        ### Входные параметры

        +-----------------------------------------------------------------+
        | MarkType | - тип объекта из перечисления DrawingObjectTypeEnum. |
        +-----------------------------------------------------------------+

        Возможные значения MarkType

        +----------------------------------------------------------------------------------------------+
        | ksDrMarkOnLeader   | 52 | IMarkOnLeader - Марка/позиционное обозначение с линией-выноской.   |
        +----------------------------------------------------------------------------------------------+
        | ksDrMarkOnLine     | 53 | IMarkOnLine - Марка/позиционное обозначение на линии.              |
        +----------------------------------------------------------------------------------------------+
        | ksDrMarkInsideForm | 54 | IMarkInsideForm - Марка/позиционное обозначение без линии-выноски. |
        +----------------------------------------------------------------------------------------------+

        ### Возвращаемое значение

        +------------------------------------------------------------+
        | Result | - указатель на базовый интерфейс для марок IMark. |
        +------------------------------------------------------------+
        """
        ...

    def Mark(self, Index) -> IMark:  # imarks_mark.html
        """
        ## Mark - Марка, заданная по индексу, ссылке или имени и номеру

        Тип данных: указатель на интерфейс марки IMark

        Пример:

        mark = iMarks.Mark("A1");

        где "А" - имя марки

        "1" - номер марки

        1. Свойство доступно только для чтения.

        2. В качестве индекса могут использоваться следующие типы:

        •Индекс объекта в коллекции,

        •Сcылка на объект (reference),

        •Cуммарная строка – имя (IMark::Name) + номер марки (IMark::Number) без дополнительных разделителей.

        ### Синтаксис Automation

        ```
        Mark = iObject.Mark(index)     # Получить свойство(* )
        Mark = iObject.GetMark(index)  # Получить свойство (**)
        ```

        ### Входные параметры

        +-----------------------------+
        | Index (Variant) | - Индекс. |
        +-----------------------------+
        """
        ...




class ICutUnitMarkings(IDrawingObjects):  # icutunitmarkings.html
    """
    ## Интерфейс ICutUnitMarkings

    Интерфейс коллекции обозначений узла в сечении.

    Интерфейс позволяет получить коллекции обозначений узла в сечении.

    Данный интерфейс можно получить у интерфейса контейнера объектов СПДС, используя метод IBuildingContainer::CutUnitMarkings.
    """

    def Add(self) -> ICutUnitMarking:  # icutunitmarkings_add.html
        """
        ## Add - Создать обозначение узла в сечении и добавить его в коллекцию

        Метод позволяет создать новый интерфейс обозначения узла в сечении. После получения нового интерфейса нужно задать параметры обозначения узла и вызвать метод IDrawingObject::Update.

        ### Синтаксис Automation

        ```
        LPDISPATCH Add;
        ```

        ### Возвращаемое значение

        - Указатель на интерфейс обозначения узла в сечении ICutUnitMarking.
        """
        ...

    def CutUnitMarking(self, Index) -> ICutUnitMarking:  # icutunitmarkings_cutunitmarking.html
        """
        ## CutUnitMarking - Обозначение узла в сечении, заданное по индексу, ссылке или тексту

        Тип данных: указатель на интерфейс ICutUnitMarking

        Пример:

        mark = iMarks.Mark("A1");

        где "А1" - текст сверху

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        CutUnitMarking = iObject.CutUnitMarking(index)     # Получить свойство(* )
        CutUnitMarking = iObject.GetCutUnitMarking(index)  # Получить свойство (**)
        ```

        ### Входные параметры

        +---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------+
        | Index (Variant) | - Индекс обозначения узла в коллекции. Поддерживаются следующие типы: - VT_I4 - индекс объекта, - reference объекта - строковое значение текста сверху ICutUnitMarking::TextUp. |
        +---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------+
        """
        ...




class IMultiTextLeaders(IDrawingObjects):  # imultitextleaders.html
    """
    ## Интерфейс IMultiTextLeaders

    Интерфейс коллекции выносных надписей к многослойным конструкциям.

    Интерфейс позволяет получить доступ к выносным надписям к многослойным конструкциям для данного вида, а также позволяет создавать новые выносные надписи к многослойным конструкциям.

    Получить интерфейс можно, используя свойство IBuildingContainer::MultiTextLeaders.
    """

    def Add(self) -> IMultiTextLeader:  # imultitextleaders_add.html
        """
        ## Add - Создать выносную надпись к многослойным конструкциям (Добавить объект в коллекцию)

        После получения нового интерфейса нужно задать параметры объекта и вызвать метод IDrawingObject::Update. Объект в модели появится после вызова этого метода.

        ### Синтаксис Automation

        ```
        LPDISPATCH Add();
        ```

        ### Возвращаемое значение

        - указатель на интерфейс IMultiTextLeader
        """
        ...

    def MultiTextLeader(self, Index) -> IMultiTextLeader:  # imultitextleaders_multitextleader.html
        """
        ## MultiTextLeader - Выносная надпись к многослойным конструкциям, заданная по индексу

        Тип данных: указатель на интерфейс IMultiTextLeader

        Значения свойства:

        +---------------------------------------------------------------+
        | указатель на интерфейс IMultiTextLeader | - в случае успеха,  |
        +---------------------------------------------------------------+
        | NULL                                    | - в случае неудачи. |
        +---------------------------------------------------------------+

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        MultiTextLeader = iObject.MultiTextLeader(Index)     # Получить свойство(* )
        MultiTextLeader = iObject.GetMultiTextLeader(Index)  # Получить свойство (**)
        ```

        ### Входные параметры

        +--------------------------------------------------------------------------------------------------------------------------+
        | Index (Variant) | - Индекс узла в коллекции. Поддерживаются следующие типы: - VT_I4 - индекс объекта, reference объекта. |
        +--------------------------------------------------------------------------------------------------------------------------+
        """
        ...




class IUnitMarkings(IDrawingObjects):  # iunitmarkings.html
    """
    ## Интерфейс IUnitMarkings

    Интерфейс коллекции обозначений узла.

    Интерфейс позволяет получить коллекции обозначений узла.

    Данный интерфейс можно получить у интерфейса контейнера объектов СПДС, используя метод IBuildingContainer::UnitMarkings.
    """

    def Add(self) -> IUnitMarking:  # iunitmarkings_add.html
        """
        ## Add - Создать обозначение узла и добавить его в коллекцию

        Метод позволяет создать новый интерфейс обозначения узла. После получения нового интерфейса нужно задать параметры обозначения узла и вызвать метод IDrawingObject::Update.

        ### Синтаксис Automation

        ```
        LPDISPATCH Add;
        ```

        ### Возвращаемое значение

        - Указатель на интерфейс обозначения узла IUnitMarking.
        """
        ...

    def UnitMarking(self, Index) -> IUnitMarking:  # iunitmarkings_unitmarking.html
        """
        ## UnitMarking - Обозначение узла, заданное по индексу, ссылке или тексту

        Тип данных: указатель на интерфейс IUnitMarking

        Пример:

        UnitMarking = iUnitMarkings.UnitMarking("A1");

        где "А1" - текст сверху

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        UnitMarking = iObject.UnitMarking(index)     # Получить свойство(* )
        UnitMarking = iObject.GetUnitMarking(index)  # Получить свойство (**)
        ```

        ### Входные параметры

        +------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------+
        | Index (Variant) | - Индекс обозначения узла в коллекции. Поддерживаются следующие типы: - VT_I4 - индекс объекта, - reference объекта - строковое значение текста сверху IUnitMarking::TextUp. |
        +------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------+
        """
        ...




class IUnitNumbers(IDrawingObjects):  # iunitnumbers.html
    """
    ## Интерфейс IUnitNumbers

    Интерфейс коллекции номеров узла.

    Интерфейс позволяет получить доступ к номерам узла на чертеже.

    Получить интерфейс коллекции номеров узла можно, используя метод контейнера объектов СПДС IBuildingContainer::UnitNumbers.
    """

    def Add(self) -> IUnitNumber:  # iunitnumbers_add.html
        """
        ## Add - Создать номер узла

        Метод позволяет создать новый интерфейс номера узла. После получения нового интерфейса нужно задать параметры номера узла и вызвать метод IDrawingObject::Update.

        ### Синтаксис Automation

        ```
        LPDISPATH Add( );
        ```

        ### Возвращаемое значение

        +------------------------------------------------------------+
        | Result | - Указатель на интерфейс номера узла IUnitNumber. |
        +------------------------------------------------------------+
        """
        ...

    def UnitNumber(self, Index) -> IUnitNumber:  # iunitnumbers_unitnumber.html
        """
        ## UnitNumber - Номер узла, заданный по индексу, или ссылке

        Тип данных: указатель на интерфейс марки IUnitNumber

        Пример:

        UnitNumber = iUnitNumbers.UnitNumber("TextUp");

        Где "TextUp" - верхний текст номера узла.

        1. Свойство доступно только для чтения.

        2. В качестве индекса могут использоваться следующие типы:

        •индекс объекта в коллекции,

        •ссылка на объект (reference),

        •строковое значение текста сверху IUnitNumber::TextUp.

        ### Синтаксис Automation

        ```
        UnitNumber = iObject.UnitNumber( index )     # Получить свойство(* )
        UnitNumber = iObject.GetUnitNumber( index )  # Получить свойство (**)
        ```

        ### Входные параметры

        +-----------------------------+
        | Index (Variant) | - Индекс. |
        +-----------------------------+
        """
        ...




class IMarkNodes(IKompasCollection):  # imarknodes.html
    """
    ## Интерфейс IMarkNodes

    Интерфейс коллекции узлов для вставки дополнительных марок.

    Интерфейс позволяет получать и создавать дополнительные узлы для вставки марок.

    Получить интерфейс коллекции марок можно, используя методы IStraightAxis::MarkNodes, IArcAxis::MarkNodes, IMarkNode::MarkNodes.
    """

    def Add(self, Type, Index) -> IMarkNode:  # imarknodes_add.html
        """
        ## Add - Создать узел (добавить узел в коллекцию)

        Метод позволяет создать дополнительные узлы для марок.

        ### Синтаксис Automation

        ```
        LPDISPATH Add( ksMarkNodeEnum Type,
        VARIANT Index );
        ```

        ### Входные параметры

        +--------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------+
        | Type  | - тип марки из перечисления ksMarkNodeEnum,                                                                                                                                                                                                    |
        +--------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------+
        | Index | - индекс узла для вставки марки. Поддерживаются следующие типы: VT_I4 - добавление узла по индексу, -1 - добавление в конец списка, VT_BSTR - добавление узла перед элементом с заданным именем, VT_DISPATCH - добавление узла перед заданным. |
        +--------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------+

        ### Возвращаемое значение

        - Указатель на интерфейс узла IMarkNode.
        """
        ...

    def Clear(self) -> bool:  # imarknodes_clear.html
        """
        ## Clear - Очистить коллекцию

        Метод позволяет удалить дополнительные узлы для марок, при этом узлы удаляются из коллекции. Из объекта узлы будут удалены после вызова метода IDrawingObject::Update.

        ### Синтаксис Automation

        ```
        BOOL Clear();
        ```

        ### Возвращаемое значение

        +-----------------------------+
        | TRUE  | - в случае успеха,  |
        +-----------------------------+
        | FALSE | - в случае неудачи. |
        +-----------------------------+
        """
        ...

    def Item(self, Index) -> IMarkNode:  # imarknodes_item.html
        """
        ## Item - Узел, заданный по индексу или тексту марки

        Тип данных: указатель на интерфейс узла IMarkNode

        Индекс равный 0 позволяет получить указатель на узел, являющийся маркой оси.

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        Item = iObject.Item(Index)     # Получить свойство(* )
        Item = iObject.GetItem(Index)  # Получить свойство (**)
        ```

        ### Входные параметры

        +--------------------------------------------------------------------------------------------------------------------------+
        | Index (Variant) | - Индекс узла в коллекции. Поддерживаются следующие типы: - VT_I4 - индекс узла, - VT_BSTR - имя узла. |
        +--------------------------------------------------------------------------------------------------------------------------+
        """
        ...




class IBuildingAxis(IDrawingObject):  # ibuildingaxis.html
    """
    ## Интерфейс IBuildingAxis

    Cтроительная ось (базовый интерфейс для строительных осей).

    Интерфейс позволяет получать и изменять общие свойства для строительных осей.

    Получить интерфейс строительной оси можно, используя методы коллекции строительных осей IBuildingAxes::BuildingAxis, IBuildingAxes::Add.
    """

    @property
    def AutoStroke(self) -> bool:  # ibuildingaxis_autostroke.html
        """
        ## AutoStroke - Автоопределение длины штриха

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        AutoStroke = iObject.AutoStroke       # Получить свойство(* )
        iObject.AutoStroke = AutoStroke       # Установить свойство (* )
        AutoStroke = iObject.GetAutoStroke()  # Получить свойство (**)
        iObject.SetAutoStroke(AutoStroke)     # Установить свойство (**)
        ```
        """
        ...
    @AutoStroke.setter
    def AutoStroke(self, value: bool): ...

    @property
    def DottedLength(self) -> float:  # ibuildingaxis_dottedlength.html
        """
        ## DottedLength - Длина пунктира

        Тип данных: double

        ### Синтаксис Automation

        ```
        DottedLength = iObject.DottedLength    # Получить свойство(* )
        iObject.DottedLength = DottedLength    # Установить свойство (* )
        iObject.GetDottedLength()              # Получить свойство (**)
        iObject.SetDottedLength(DottedLength)  # Установить свойство (**)
        ```
        """
        ...
    @DottedLength.setter
    def DottedLength(self, value: float): ...

    @property
    def DoubleMark(self) -> bool:  # ibuildingaxis_doublemark.html
        """
        ## DoubleMark - Тип марки

        Тип данных: BOOL

        Значения свойства:

        +----------------------+
        | TRUE  | - двойная,   |
        +----------------------+
        | FALSE | - одинарная. |
        +----------------------+

        Свойство позволяет получить и установить тип марки.

        ### Синтаксис Automation

        ```
        DoubleMark = iObject.DoubleMark       # Получить свойство(* )
        iObject.DoubleMark = DoubleMark       # Установить свойство (* )
        DoubleMark = iObject.GetDoubleMark()  # Получить свойство (**)
        iObject.SetDoubleMark(DoubleMark)     # Установить свойство (**)
        ```
        """
        ...
    @DoubleMark.setter
    def DoubleMark(self, value: bool): ...

    @property
    def Interval(self) -> float:  # ibuildingaxis_interval.html
        """
        ## Interval - Длина промежутка

        Тип данных: double

        ### Синтаксис Automation

        ```
        Interval = iObject.Interval       # Получить свойство(* )
        iObject.Interval = Interval       # Установить свойство (* )
        Interval = iObject.GetInterval()  # Получить свойство (**)
        iObject.SetInterval(Interval)     # Установить свойство (**)
        ```
        """
        ...
    @Interval.setter
    def Interval(self, value: float): ...

    @property
    def MarkSize(self) -> float:  # ibuildingaxis_marksize.html
        """
        ## MarkSize - Размер марки

        Тип данных: double

        Значения параметра пересчитываются в зависимости от текущей системы координат для объекта, задаваемой параметром IDrawingObject::DrawingObjectParamType.

        ### Синтаксис Automation

        ```
        MarkSize = iObject.MarkSize       # Получить свойство(* )
        iObject.MarkSize = MarkSize       # Установить свойство (* )
        MarkSize = iObject.GetMarkSize()  # Получить свойство (**)
        iObject.SetMarkSize(MarkSize)     # Установить свойство (**)
        ```
        """
        ...
    @MarkSize.setter
    def MarkSize(self, value: float): ...

    @property
    def Stroke(self) -> float:  # ibuildingaxis_stroke.html
        """
        ## Stroke - Длина штриха

        Тип данных: double

        ### Синтаксис Automation

        ```
        Stroke = iObject.Stroke       # Получить свойство(* )
        iObject.Stroke = Stroke       # Установить свойство (* )
        Stroke = iObject.GetStroke()  # Получить свойство (**)
        iObject.SetStroke(Stroke)     # Установить свойство (**)
        ```
        """
        ...
    @Stroke.setter
    def Stroke(self, value: float): ...

    @property
    def Text(self) -> IText:  # ibuildingaxis_text.html
        """
        ## Text - Текст

        Тип данных: указатель на интерфейс IText

        Пример:

        axis = iBuildingAxes.Axis("A1");

        1. Свойство доступно только для чтения.

        2. Значение свойства можно использовать для поиска марки в коллекции строительных осей IBuildingAxes, а также для поиска в функции IDrawingObjects::Item или IBuildingAxes::BuildingAxis строкового значения текста.

        ### Синтаксис Automation

        ```
        Text = iObject.Text       # Получить свойство(* )
        Text = iObject.GetText()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def TextAfter(self) -> IText:  # ibuildingaxis_textafter.html
        """
        ## ТextAfter - Текст после

        Тип данных: указатель на интерфейс IText

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        Text = iObject.TextAfter       # Получить свойство(* )
        Text = iObject.GetTextAfter()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def TextBefore(self) -> IText:  # ibuildingaxis_textbefore.html
        """
        ## TextBefore - Текст до

        Тип данных: указатель на интерфейс IText

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        Text = iObject.TextBefore       # Получить свойство(* )
        Text = iObject.GetTextBefore()  # Получить свойство (**)
        ```
        """
        ...

    def AddNodeByPoint(self, Type, X, Y):  # ibuildingaxis_addnodebypoint.html
        """
        ## AddNodeByPoint - Добавить узел марки по координатам

        Значения координат и направлений пересчитываются в зависимости от текущей системы координат для объекта, задаваемой параметром IDrawingObject::DrawingObjectParamType.

        ### Синтаксис Automation

        ```
        LPDISPATCH AddNodeByPoint( ksMarkNodeEnum Type,
        double X,
        double Y );
        ```

        ### Входные параметры

        +------------------------------------------------+
        | Type | - тип узла для марки из ksMarkNodeEnum, |
        +------------------------------------------------+
        | X, Y | - координаты для подключения узла.      |
        +------------------------------------------------+

        ### Возвращаемое значение

        +-----------------------------------------------------------+
        | Указатель на интерфейс IMarkNode | - если узел подключен, |
        +-----------------------------------------------------------+
        | NULL                             | - в случае неудачи.    |
        +-----------------------------------------------------------+
        """
        ...

    def GetInsertionPoints(self, First) -> bool:  # ibuildingaxis_getinsertionpoints.html
        """
        ## GetInsertionPoints - Получить координаты точек для вставки дополнительных элементов

        1. Параметры Points, Directions и Nodes не являются обязательными. В функцию достаточно передать один из указателей на VARIANT.

        2. Массивы являются согласованными.

        Координаты точек в массиве Points лежат в следующей последовательности: x0, y0, x1, y1, ...xi, yi

        Направления в масиве Directions лежат в последовательности: angle0, angle1, ...anglei

        В массиве Nodes лежат указатели на интерфейсы узлов для вставок марок IMarkNode.

        3. У узла может быть от 1 до 3 точек для подключения дополнительных узлов в зависимости от типа узла и его расположения. Так как массивы координат и узлов должны быть согласованы, то один и тот же указатель на интерфейс может встречаться в массиве более одного раза.

        4. Значения координат и направлений пересчитываются в зависимости от текущей системы координат для объекта, задаваемой параметром IDrawingObject::DrawingObjectParamType.

        5. Если возвращается один объект, то тип VARIANT-а будет VT_DISPATCH, если возвращается несколько объектов, то тип VARIANT-а будет VT_ARRAY | VT_DISPATCH.

        ### Синтаксис Automation

        ```
        BOOL GetInsertionPoints( BOOL First,
        VARIANT * Points,
        VARIANT * Directions,
        VARIANT * Nodes,
        BOOL * RetVal );
        ```

        ### Входные параметры

        +-----------------------------------------------------------------------------------------------------+
        | First | - вернуть координаты точек TRUE - для первого ответвления, FALSE - для второго ответвления. |
        +-----------------------------------------------------------------------------------------------------+

        ### Выходные параметры

        +-------------------------------------------------------------------------------------------------------------------+
        | Points     | - массив SafeArray типа VT_ARRAY | VT_R8 координат точек для подключения дополнительных узлов марок, |
        +-------------------------------------------------------------------------------------------------------------------+
        | Directions | - массив SafeArray типа VT_ARRAY | VT_R8 направлений (углы относительно центра родительского узла),  |
        +-------------------------------------------------------------------------------------------------------------------+
        | Nodes      | - массив SafeArray типа VT_ARRAY | VT_DISPATCH родительские узлы для точек подключения.              |
        +-------------------------------------------------------------------------------------------------------------------+

        ### Возвращаемое значение

        +--------------------------------+
        | TRUE  | - координаты получены, |
        +--------------------------------+
        | FALSE | - в случае неудачи.    |
        +--------------------------------+
        """
        ...

    def GetNodeByPoint(self, X, Y, Limit):  # ibuildingaxis_getnodebypoint.html
        """
        ## GetNodeByPoint - Получить узел марки по координатам

        1. Значения координат и направлений пересчитываются в зависимости от текущей системы координат для объекта, задаваемой параметром IDrawingObject::DrawingObjectParamType

        2. Подключить узел можно только к точкам подключения, координаты которых возвращаются функциями IBuildingAxis::GetInsertionPoints и IMarkNode::GetInsertionPoints.

        3. После добавления узла координаты и список доступных точек для подключения новых узлов может измениться.

        ### Синтаксис Automation

        ```
        LPDISPATCH GetNodeByPoint( double X, double Y, double Limit );
        ```

        ### Входные параметры

        +----------------------------------------------------------------+
        | X, Y  | - координаты точки,                                    |
        +----------------------------------------------------------------+
        | Limit | - максимальное расстояние от точки до узла при поиске. |
        +----------------------------------------------------------------+

        ### Возвращаемое значение

        +--------------------------------------------------------+
        | Указатель на интерфейс IMarkNode | - если узел найден, |
        +--------------------------------------------------------+
        | NULL                             | - в случае неудачи. |
        +--------------------------------------------------------+
        """
        ...




class IArcAxis(IBuildingAxis):  # iarcaxis.html
    """
    ## Интерфейс IArcAxis

    Интерфейс дуговой координационной оси.

    Интерфейс позволяет получать и изменять свойства дуговой строительной оси.

    1. Получить интерфейс строительной оси можно, используя методы коллекции строительных осей IBuildingAxes::BuildingAxis, IBuildingAxes::Add.

    2. При создании дуговой оси можно использовать несколько способов построения.

    •По координатам,

    •По трем точкам,

    •По углам,

    •По точке и углу,

    •По углу, точке и радиусу,

    •По конечным точкам и углу,

    •Плавающий центр, вариант 1,

    •Плавающий центр, вариант 2.

    3. После задания параметров оси требуется вызвать метод IDrawingObject::Update.

    4. При изменении параметров зависимые параметры пересчитываются после вызова метода IDrawingObject::Update.
    """

    @property
    def Angle1(self) -> float:  # iarcaxis_angle1.html
        """
        ## Angle1 - Угол между осью 0X и отрезком, соединяющим первую точку дуги с началом координат

        Тип данных: double

        Свойство позволяет получить и установить угол между осью 0X и отрезком, соединяющим первую точку дуги с началом координат.

        ### Синтаксис Automation

        ```
        Angle1 = iObject.Angle1;       # Получить свойство(* )
        iObject.Angle1 = Angle1;       # Установить свойство (* )
        Angle1 = iObject.GetAngle1();  # Получить свойство (**)
        iObject.SetAngle1( Angle1 );   # Установить свойство (**)
        ```
        """
        ...
    @Angle1.setter
    def Angle1(self, value: float): ...

    @property
    def Angle2(self) -> float:  # iarcaxis_angle2.html
        """
        ## Angle2 - Угол между осью 0X и отрезком, соединяющим вторую точку дуги с началом координат

        Тип данных: double

        Свойство позволяет получить и установить угол между осью 0X и отрезком, соединяющим вторую точку дуги с началом координат.

        ### Синтаксис Automation

        ```
        Angle2 = iObject.Angle2;       # Получить свойство(* )
        iObject.Angle2 = Angle2;       # Установить свойство (* )
        Angle2 = iObject.GetAngle2();  # Получить свойство (**)
        iObject.SetAngle2( Angle2 );   # Установить свойство (**)
        ```
        """
        ...
    @Angle2.setter
    def Angle2(self, value: float): ...

    @property
    def Direction(self) -> bool:  # iarcaxis_direction.html
        """
        ## Direction - Направление построения дуги

        Тип данных: BOOL

        Значения свойства:

        +-----------------------------------+
        | TRUE  | - по часовой стрелке,     |
        +-----------------------------------+
        | FALSE | - против часовой стрелки. |
        +-----------------------------------+

        Свойство позволяет получить и установить направление построения дуги.

        ### Синтаксис Automation

        ```
        Direction = iObject.Direction;       # Получить свойство(* )
        iObject.Direction = Direction;       # Установить свойство (* )
        Direction = iObject.GetDirection();  # Получить свойство (**)
        iObject.SetDirection( Direction );   # Установить свойство (**)
        ```
        """
        ...
    @Direction.setter
    def Direction(self, value: bool): ...

    @property
    def MarkNodes(self) -> IMarkNodes:  # iarcaxis_marknodes.html
        """
        ## MarkNodes - Коллекция узлов марок

        Тип данных: указатель на интерфейс коллекции IMarkNodes

        1. Свойство доступно только для чтения.

        2. Узлы правого и левого выступа имеют одинаковые параметры, поэтому редактирование осуществляется через одну коллекцию.

        ### Синтаксис Automation

        ```
        MarkNodes = iObject.MarkNodes;       # Получить свойство(* )
        MarkNodes = iObject.GetMarkNodes();  # Получить свойство (**)
        ```
        """
        ...

    @property
    def Radius(self) -> float:  # iarcaxis_radius.html
        """
        ## Radius - Радиус дуги

        Тип данных: double

        Свойство позволяет получить и установить радиус дуги.

        ### Синтаксис Automation

        ```
        Radius = iObject.Radius;       # Получить свойство(* )
        iObject.Radius = Radius;       # Установить свойство (* )
        Radius = iObject.GetRadius();  # Получить свойство (**)
        iObject.SetRadius( Radius );   # Установить свойство (**)
        ```
        """
        ...
    @Radius.setter
    def Radius(self, value: float): ...

    @property
    def X1(self) -> float:  # iarcaxis_x1.html
        """
        ## X1 - Координата первой точки по X

        Тип данных: double

        Свойство позволяет получить и установить координату первой точки дуги X1.

        ### Синтаксис Automation

        ```
        X1 = iObject.X1;       # Получить свойство(* )
        iObject.X1 = X1;       # Установить свойство (* )
        X1 = iObject.GetX1();  # Получить свойство (**)
        iObject.SetX1( X1 );   # Установить свойство (**)
        ```
        """
        ...
    @X1.setter
    def X1(self, value: float): ...

    @property
    def X2(self) -> float:  # iarcaxis_x2.html
        """
        ## X2 - Координата второй точки по X

        Тип данных: double

        Свойство позволяет получить и установить координату первой точки дуги X2.

        ### Синтаксис Automation

        ```
        X2 = iObject.X2;       # Получить свойство(* )
        iObject.X2 = X2;       # Установить свойство (* )
        X2 = iObject.GetX2();  # Получить свойство (**)
        iObject.SetX2( X2 );   # Установить свойство (**)
        ```
        """
        ...
    @X2.setter
    def X2(self, value: float): ...

    @property
    def X3(self) -> float:  # iarcaxis_x3.html
        """
        ## X3 - Координата средней точки, лежащей на дуге по X

        Тип данных: double

        Свойство позволяет получить и установить координату средней точки, лежащей на дуге по X.

        ### Синтаксис Automation

        ```
        X3 = iObject.X3;       # Получить свойство(* )
        iObject.X3 = X3;       # Установить свойство (* )
        X3 = iObject.GetX3();  # Получить свойство (**)
        iObject.SetX3( X3 );   # Установить свойство (**)
        ```
        """
        ...
    @X3.setter
    def X3(self, value: float): ...

    @property
    def Xc(self) -> float:  # iarcaxis_xc.html
        """
        ## Xc - Координата X центра дуги

        Тип данных: double

        Свойство позволяет получить и установить координату центра дуги Xc.

        ### Синтаксис Automation

        ```
        Xc = iObject.Xc;       # Получить свойство(* )
        iObject.Xc = Xc;       # Установить свойство (* )
        Xc = iObject.GetXс();  # Получить свойство (**)
        iObject.SetXс( Xc );   # Установить свойство (**)
        ```
        """
        ...
    @Xc.setter
    def Xc(self, value: float): ...

    @property
    def Y1(self) -> float:  # iarcaxis_y1.html
        """
        ## Y1 - Координата первой точки по Y

        Тип данных: double

        Свойство позволяет получить и установить координату первой точки дуги Y1.

        ### Синтаксис Automation

        ```
        Y1 = iObject.Y1;       # Получить свойство(* )
        iObject.Y1 = Y1;       # Установить свойство (* )
        Y1 = iObject.GetY1();  # Получить свойство (**)
        iObject.SetY1( Y1 );   # Установить свойство (**)
        ```
        """
        ...
    @Y1.setter
    def Y1(self, value: float): ...

    @property
    def Y2(self) -> float:  # iarcaxis_y2.html
        """
        ## Y2 - Координата второй точки по Y

        Тип данных: double

        Свойство позволяет получить и установить координату второй точки дуги Y2.

        ### Синтаксис Automation

        ```
        Y2 = iObject.Y2;       # Получить свойство(* )
        iObject.Y2 = Y2;       # Установить свойство (* )
        Y2 = iObject.GetY2();  # Получить свойство (**)
        iObject.SetY2( Y2 );   # Установить свойство (**)
        ```
        """
        ...
    @Y2.setter
    def Y2(self, value: float): ...

    @property
    def Y3(self) -> float:  # iarcaxis_y3.html
        """
        ## Y3 - Координата средней точки, лежащей на дуге по Y

        Тип данных: double

        Свойство позволяет получить и установить координату средней точки, лежащей на дуге по Y.

        ### Синтаксис Automation

        ```
        Y3 = iObject.Y3;       # Получить свойство(* )
        iObject.Y3 = Y3;       # Установить свойство (* )
        Y3 = iObject.GetY3();  # Получить свойство (**)
        iObject.SetY3( Y3 );   # Установить свойство (**)
        ```
        """
        ...
    @Y3.setter
    def Y3(self, value: float): ...

    @property
    def Yc(self) -> float:  # iarcaxis_yc.html
        """
        ## Yc - Координата Y центра дуги

        Тип данных: double

        Свойство позволяет получить и установить координату центра дуги Yc.

        ### Синтаксис Automation

        ```
        Yc = iObject.Yc;       # Получить свойство(* )
        iObject.Yc = Yc;       # Установить свойство (* )
        Yc = iObject.GetYc();  # Получить свойство (**)
        iObject.SetYc( Yc );   # Установить свойство (**)
        ```
        """
        ...
    @Yc.setter
    def Yc(self, value: float): ...

    def Jut(self, First) -> IAxisJut:  # iarcaxis_jut.html
        """
        ## Jut - Получить выступ

        Тип данных: указатель на интерфейс выступа IAxisJut

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        Jut = iObject.Jut( First );     # Получить свойство(* )
        Jut = iObject.GetJut( First );  # Получить свойство (**)
        ```

        ### Входные параметры

        +--------------------------------------------------------------------------------------+
        | First | - признак порядка выступа на оси, BOOL, TRUE- первый выступ, FALSE - второй. |
        +--------------------------------------------------------------------------------------+
        """
        ...




class ICircleAxis(IBuildingAxis):  # icircleaxis.html
    """
    ## Интерфейс ICircleAxis

    Интерфейс круговой координационной оси.

    Интерфейс позволяет получать и изменять свойства круговой строительной оси.

    1. Получить интерфейс строительной оси можно, используя методы коллекции строительных осей IBuildingAxes::BuildingAxis, IBuildingAxes::Add.

    2. При создании дуговой оси необходимо задать следующие параметры:

    •координаты центра XC; YC,

    •радиус Radius.

    3. После задания параметров оси требуется вызвать метод IDrawingObject::Update.

    4. При изменении параметров зависимые параметры пересчитываются после вызова метода IDrawingObject::Update.
    """

    @property
    def BaseMarkNode(self) -> IMarkNode:  # icircleaxis_basemarknode.html
        """
        ## BaseMarkNode - Базовый узел для марки

        Тип данных: указатель на интерфейс IMarkNode

        1. Свойство доступно только для чтения.

        2. Интерфейс IMarkNode позволяет задать параметры основного узла марки оси. Позволяет добавлять и удалять дополнительные узлы.

        ### Синтаксис Automation

        ```
        MarkNode = iObject.BaseMarkNode;       # Получить свойство(* )
        MarkNode = iObject.GetBaseMarkNode();  # Получить свойство (**)
        ```
        """
        ...

    @property
    def MarkAngle(self) -> float:  # icircleaxis_markangle.html
        """
        ## MarkAngle - Угол привязки марки

        Тип данных: double

        Свойство позволяет получить и установить угол между осью 0X и отрезком, соединяющим центр оси с маркой.

        ### Синтаксис Automation

        ```
        MarkAngle = iObject.MarkAngle       # Получить свойство(* )
        iObject.MarkAngle = MarkAngle       # Установить свойство (* )
        MarkAngle = iObject.GetMarkAngle()  # Получить свойство (**)
        iObject.SetMarkAngle( MarkAngle )   # Установить свойство (**)
        ```
        """
        ...
    @MarkAngle.setter
    def MarkAngle(self, value: float): ...

    @property
    def MarkOn(self) -> bool:  # icircleaxis_markon.html
        """
        ## MarkOn - Марка вкючена\\выключена

        Тип данных: BOOL

        Свойство позволяет управлять отображением марки на координационной оси.

        ### Синтаксис Automation

        ```
        MarkOn = iObject.MarkOn       # Получить свойство(* )
        iObject.MarkOn = MarkOn       # Установить свойство (* )
        MarkOn = iObject.GetMarkOn()  # Получить свойство (**)
        iObject.SetMarkOn( MarkOn )   # Установить свойство (**)
        ```
        """
        ...
    @MarkOn.setter
    def MarkOn(self, value: bool): ...

    @property
    def Radius(self) -> float:  # icircleaxis_radius.html
        """
        ## Radius - Радиус

        Тип данных: double

        Свойство позволяет получить и установить радиус дуги.

        ### Синтаксис Automation

        ```
        Radius = iObject.Radius;       # Получить свойство(* )
        iObject.Radius = Radius;       # Установить свойство (* )
        Radius = iObject.GetRadius();  # Получить свойство (**)
        iObject.SetRadius( Radius );   # Установить свойство (**)
        ```
        """
        ...
    @Radius.setter
    def Radius(self, value: float): ...

    @property
    def Xc(self) -> float:  # icircleaxis_xc.html
        """
        ## Xc - Координата центра по X

        Тип данных: double

        Свойство позволяет получить и установить координату центра дуги Xc.

        ### Синтаксис Automation

        ```
        Xc = iObject.Xc;       # Получить свойство(* )
        iObject.Xc = Xc;       # Установить свойство (* )
        Xc = iObject.GetXс();  # Получить свойство (**)
        iObject.SetXс( Xc );   # Установить свойство (**)
        ```
        """
        ...
    @Xc.setter
    def Xc(self, value: float): ...

    @property
    def Yc(self) -> float:  # icircleaxis_yc.html
        """
        ## Yc - Координата центра по Y

        Тип данных: double

        Свойство позволяет получить и установить координату центра дуги Yc.

        ### Синтаксис Automation

        ```
        Yc = iObject.Yc;       # Получить свойство(* )
        iObject.Yc = Yc;       # Установить свойство (* )
        Yc = iObject.GetYc();  # Получить свойство (**)
        iObject.SetYc( Yc );   # Установить свойство (**)
        ```
        """
        ...
    @Yc.setter
    def Yc(self, value: float): ...




class IStraightAxis(IBuildingAxis):  # istraightaxis.html
    """
    ## Интерфейс IStraightAxis

    Интерфейс прямой координационной оси.

    Интерфейс позволяет получать и изменять свойства прямой строительной оси.

    1. Получить интерфейс строительной оси можно, используя методы коллекции строительных осей IBuildingAxes::BuildingAxis, IBuildingAxes::Add.

    2. При создании прямой оси можно использовать несколько типов построения:

    2.1. по координатам (требуется задать координаты точек X1; Y1, X2; Y2).

    2.2. По точке 1 (X1; Y1), длине Length и углу Angle,

    2.3. По точке 2 (X2; Y2), длине Length и углу Angle относительно точки 1.

    3. После задания параметров оси требуется вызвать метод IDrawingObject::Update.

    4. При изменении параметров зависимые параметры пересчитываются после вызова метода IDrawingObject::Update.
    """

    @property
    def Angle(self) -> float:  # istraightaxis_angle.html
        """
        ## Angle - Угол наклона

        Тип данных: double

        Свойство позволяет получить и установить угол наклона отрезка.

        ### Синтаксис Automation

        ```
        Angle = iObject.Angle;       # Получить свойство(* )
        iObject.Angle = Angle;       # Установить свойство (* )
        Angle = iObject.GetAngle();  # Получить свойство (**)
        iObject.SetAngle( Angle );   # Установить свойство (**)
        ```
        """
        ...
    @Angle.setter
    def Angle(self, value: float): ...

    @property
    def Length(self) -> float:  # istraightaxis_length.html
        """
        ## Length - Длина отрезка

        Тип данных: double

        Свойство позволяет получить и установить длину отрезка.

        ### Синтаксис Automation

        ```
        Length = iObject.Length;       # Получить свойство(* )
        iObject.Length = Length;       # Установить свойство (* )
        Length = iObject.GetLength();  # Получить свойство (**)
        iObject.SetLength( Length );   # Установить свойство (**)
        ```
        """
        ...
    @Length.setter
    def Length(self, value: float): ...

    @property
    def MarkNodes(self) -> IMarkNodes:  # istraightaxis_marknodes.html
        """
        ## MarkNodes - Коллекция узлов марок

        Тип данных: указатель на интерфейс IMarkNodes

        1. Свойство доступно только для чтения.

        2. Узлы правого и левого выступа имеют одинаковые параметры, поэтому редактирование осуществляется через одну коллекцию.

        ### Синтаксис Automation

        ```
        MarkNodes = iObject.MarkNodes;       # Получить свойство(* )
        MarkNodes = iObject.GetMarkNodes();  # Получить свойство (**)
        ```
        """
        ...

    @property
    def X1(self) -> float:  # istraightaxis_x1.html
        """
        ## X1 - Координата первой точки по оси X

        Тип данных: double

        Свойство позволяет получить и установить координату первой точки отрезка по оси X.

        ### Синтаксис Automation

        ```
        X1 = iObject.X1;       # Получить свойство(* )
        iObject.X1 = X1;       # Установить свойство (* )
        X1 = iObject.GetX1();  # Получить свойство (**)
        iObject.SetX1( X1 );   # Установить свойство (**)
        ```
        """
        ...
    @X1.setter
    def X1(self, value: float): ...

    @property
    def X2(self) -> float:  # istraightaxis_x2.html
        """
        ## X2 - Координата второй точки по оси X

        Тип данных: double

        Свойство позволяет получить и установить координату второй точки отрезка по оси X.

        ### Синтаксис Automation

        ```
        X2 = iObject.X2;       # Получить свойство(* )
        iObject.X2 = X2;       # Установить свойство (* )
        X2 = iObject.GetX2();  # Получить свойство (**)
        iObject.SetX2( X2 );   # Установить свойство (**)
        ```
        """
        ...
    @X2.setter
    def X2(self, value: float): ...

    @property
    def Y1(self) -> float:  # istraightaxis_y1.html
        """
        ## Y1 - Координата первой точки по оси Y

        Тип данных: double

        Свойство позволяет получить и установить координату первой точки отрезка по оси Y.

        ### Синтаксис Automation

        ```
        Y1 = iObject.Y1;       # Получить свойство(* )
        iObject.Y1 = Y1;       # Установить свойство (* )
        Y1 = iObject.GetY1();  # Получить свойство (**)
        iObject.SetY1( Y1 );   # Установить свойство (**)
        ```
        """
        ...
    @Y1.setter
    def Y1(self, value: float): ...

    @property
    def Y2(self) -> float:  # istraightaxis_y2.html
        """
        ## Y2 - Координата второй точки по оси Y

        Тип данных: double

        Свойство позволяет получить и установить координату второй точки отрезка по оси Y.

        ### Синтаксис Automation

        ```
        Y2 = iObject.Y2;       # Получить свойство(* )
        iObject.Y2 = Y2;       # Установить свойство (* )
        Y2 = iObject.GetY2();  # Получить свойство (**)
        iObject.SetY2( Y2 );   # Установить свойство (**)
        ```
        """
        ...
    @Y2.setter
    def Y2(self, value: float): ...

    def Jut(self, First) -> IAxisJut:  # istraightaxis_jut.html
        """
        ## Jut - Выступ

        Тип данных: указатель на интерфейс IAxisJut

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        Jut = iObject.Jut( First );     # Получить свойство(* )
        Jut = iObject.GetJut( First );  # Получить свойство (**)
        ```

        ### Входные параметры

        +------------------------------------------------------------------+
        | First | -тип данных BOOL (TRUE - первый выступ, FALSE - второй). |
        +------------------------------------------------------------------+
        """
        ...




class IBrace(IDrawingObject):  # ibrace.html
    """
    ## Интерфейс IBrace

    Интерфейс фигурной скобки.

    Интерфейс позволяет получать и изменять свойства фигурных скобок.

    Получить интерфейс марки можно, используя свойство интерфейса коллекции фигурных скобок IBraces::Brace или метод IBraces::Add.
    """

    @property
    def Alignment(self) -> int:  # ibrace_alignment.html
        """
        ## Alignment - Ориентация фигурной скобки

        Тип данных: тип ориентации из перечисления ksAlignmentTypeEnum

        ### Синтаксис Automation

        ```
        Alignment = iObject.Alignment       # Получить свойство(* )
        iObject.Alignment = Alignment       # Установить свойство (* )
        Alignment = iObject.GetAlignment()  # Получить свойство (**)
        iObject.SetAlignment( Alignment )   # Установить свойство (**)
        ```
        """
        ...
    @Alignment.setter
    def Alignment(self, value: int): ...

    @property
    def Angle(self) -> float:  # ibrace_angle.html
        """
        ## Angle - Угол наклона фигурной скобки

        Тип данных: double

        ### Синтаксис Automation

        ```
        Angle = iObject.Angle       # Получить свойство(* )
        iObject.Angle = Angle       # Установить свойство (* )
        Angle = iObject.GetAngle()  # Получить свойство (**)
        iObject.SetAngle( Angle )   # Установить свойство (**)
        ```
        """
        ...
    @Angle.setter
    def Angle(self, value: float): ...

    @property
    def Direction(self) -> bool:  # ibrace_direction.html
        """
        ## Direction - Ориентация горлышка скобки

        Тип данных: BOOL

        Значения свойства:

        +----------------------------------+
        | TRUE  | - нормальная ориентация, |
        +----------------------------------+
        | FALSE | - перевернутая.          |
        +----------------------------------+

        ### Синтаксис Automation

        ```
        Direction = iObject.Direction       # Получить свойство(* )
        iObject.Direction = Direction       # Установить свойство (* )
        Direction = iObject.GetDirection()  # Получить свойство (**)
        iObject.SetDirection( Direction )   # Установить свойство (**)
        ```
        """
        ...
    @Direction.setter
    def Direction(self, value: bool): ...

    @property
    def Length(self) -> float:  # ibrace_length.html
        """
        ## Length - Длина фигурной скобки

        Тип данных: double

        ### Синтаксис Automation

        ```
        Length = iObject.Length       # Получить свойство(* )
        iObject.Length = Length       # Установить свойство (* )
        Length = iObject.GetLength()  # Получить свойство (**)
        iObject.SetLength( Length )   # Установить свойство (**)
        ```
        """
        ...
    @Length.setter
    def Length(self, value: float): ...

    @property
    def Radius(self) -> float:  # ibrace_radius.html
        """
        ## Radius - Радиус скругления скобки

        Тип данных: double

        ### Синтаксис Automation

        ```
        Radius = iObject.Radius       # Получить свойство(* )
        iObject.Radius = Radius       # Установить свойство (* )
        Radius = iObject.GetRadius()  # Получить свойство (**)
        iObject.SetRadius( Radius )   # Установить свойство (**)
        ```
        """
        ...
    @Radius.setter
    def Radius(self, value: float): ...

    @property
    def ShelfDirection(self) -> int:  # ibrace_shelfdirection.html
        """
        ## ShelfDirection - Положение полки

        Тип данных: Тип положения полки из перечисления ksShelfDirectionEnum

        ### Синтаксис Automation

        ```
        ShelfDirection = iObject.ShelfDirection       # Получить свойство(* )
        iObject.ShelfDirection = ShelfDirection       # Установить свойство (* )
        ShelfDirection = iObject.GetShelfDirection()  # Получить свойство (**)
        iObject.SetShelfDirection( ShelfDirection )   # Установить свойство (**)
        ```
        """
        ...
    @ShelfDirection.setter
    def ShelfDirection(self, value: int): ...

    @property
    def ShelfPoints(self) -> list[float]:  # ibrace_shelfpoints.html
        """
        ## ShelfPoints - Массив SAFEARRAY координат точек полки

        Тип данных: VARIANT типа VT_ARRAY | VT_R8

        ### Синтаксис Automation

        ```
        ShelfPoints = Object.ShelfPoints       # Получить свойство(* )
        Object.ShelfPoints = ShelfPoints       # Установить свойство (* )
        ShelfPoints = Object.GetShelfPoints()  # Получить свойство (**)
        Object.SetShelfPoints( ShelfPoints )   # Установить свойство (**)
        ```
        """
        ...
    @ShelfPoints.setter
    def ShelfPoints(self, value: list[float]): ...

    @property
    def ShelfX(self) -> float:  # ibrace_shelfx.html
        """
        ## ShelfX - Координата X начала полки

        Тип данных: double

        При попытке получить координату полки, которой нет, свойство вернёт 0.0.

        ### Синтаксис Automation

        ```
        ShelfX = iObject.ShelfX       # Получить свойство(* )
        iObject.ShelfX = ShelfX       # Установить свойство (* )
        ShelfX = iObject.GetShelfX()  # Получить свойство (**)
        iObject.SetShelfX( ShelfX )   # Установить свойство (**)
        ```
        """
        ...
    @ShelfX.setter
    def ShelfX(self, value: float): ...

    @property
    def ShelfY(self) -> float:  # ibrace_shelfy.html
        """
        ## ShelfY - Координата Y начала полки

        Тип данных: double

        При попытке получить координату полки, которой нет, свойство вернёт 0.0.

        ### Синтаксис Automation

        ```
        ShelfY = iObject.ShelfY       # Получить свойство(* )
        iObject.ShelfY = ShelfY       # Установить свойство (* )
        ShelfY = iObject.GetShelfY()  # Получить свойство (**)
        iObject.SetShelfY( ShelfY )   # Установить свойство (**)
        ```
        """
        ...
    @ShelfY.setter
    def ShelfY(self, value: float): ...

    @property
    def Style(self) -> int:  # ibrace_style.html
        """
        ## Style - Стиль линии фигурной скобки

        Тип данных: long

        ### Синтаксис Automation

        ```
        Style = iObject.Style       # Получить свойство(* )
        iObject.Style = Style       # Установить свойство (* )
        Style = iObject.GetStyle()  # Получить свойство (**)
        iObject.SetStyle( Style )   # Установить свойство (**)
        ```
        """
        ...
    @Style.setter
    def Style(self, value: int): ...

    @property
    def Text(self) -> IText:  # ibrace_text.html
        """
        ## Text - Текст фигурной скобки

        Тип данных: указатель на интерфейс текста IText

        Пример:

        Brace = iBraces.Brace("Text");

        где "Text" - текст на фигурной скобке;

        1. Свойство доступно только для чтения.

        2. Текст можно использовать для поиска фигурной скобки в коллекции IBraces. Для поиска в функции IDrawingObjects::Item или IBraces::Brace нужно передать строковое значение текста.

        ### Синтаксис Automation

        ```
        Text = iObject.Text       # Получить свойство(* )
        Text = iObject.GetText()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def X1(self) -> float:  # ibrace_x1.html
        """
        ## X1 - Координата первой точки по Х

        Тип данных: double

        Значение параметра зависит от текущей системы координат для объекта, задаваемой параметром IDrawingObject::DrawingObjectParamType.

        ### Синтаксис Automation

        ```
        X1 = iObject.X1;       # Получить свойство(* )
        iObject.X1 = X1;       # Установить свойство (* )
        X1 = iObject.GetX1();  # Получить свойство (**)
        iObject.SetX1( X1 );   # Установить свойство (**)
        ```
        """
        ...
    @X1.setter
    def X1(self, value: float): ...

    @property
    def X2(self) -> float:  # ibrace_x2.html
        """
        ## X2 - Координата второй точки по Х

        Тип данных: double

        Значение параметра зависит от текущей системы координат для объекта, задаваемой параметром IDrawingObject::DrawingObjectParamType.

        ### Синтаксис Automation

        ```
        X2 = iObject.X2;       # Получить свойство(* )
        iObject.X2 = X2;       # Установить свойство (* )
        X2 = iObject.GetX2();  # Получить свойство (**)
        iObject.SetX2( X2 );   # Установить свойство (**)
        ```
        """
        ...
    @X2.setter
    def X2(self, value: float): ...

    @property
    def Y1(self) -> float:  # ibrace_y1.html
        """
        ## Y1 - Координата первой точки по Y

        Тип данных: double

        Значение параметра зависит от текущей системы координат для объекта, задаваемой параметром IDrawingObject::DrawingObjectParamType.

        ### Синтаксис Automation

        ```
        Y1 = iObject.Y1;       # Получить свойство(* )
        iObject.Y1 = Y1;       # Установить свойство (* )
        Y1 = iObject.GetY1();  # Получить свойство (**)
        iObject.SetY1(Y1);     # Установить свойство (**)
        ```
        """
        ...
    @Y1.setter
    def Y1(self, value: float): ...

    @property
    def Y2(self) -> float:  # ibrace_y2.html
        """
        ## Y2 - Координата второй точки по Y

        Тип данных: double

        Значение параметра зависит от текущей системы координат для объекта, задаваемой параметром IDrawingObject::DrawingObjectParamType.

        ### Синтаксис Automation

        ```
        Y2 = iObject.Y2;       # Получить свойство(* )
        iObject.Y2 = Y2;       # Установить свойство (* )
        Y2 = iObject.GetY2();  # Получить свойство (**)
        iObject.SetY2(Y2);     # Установить свойство (**)
        ```
        """
        ...
    @Y2.setter
    def Y2(self, value: float): ...




class ICutUnitMarking(IDrawingObject):  # icutunitmarking.html
    """
    ## Интерфейс ICutUnitMarking

    Интерфейс обозначения узла в сечении.

    Получить интерфейс можно, используя свойство интерфейса коллекции ICutUnitMarkings::CutUnitMarking или метод ICutUnitMarkings::Add.
    """

    @property
    def Angle(self) -> float:  # icutunitmarking_angle.html
        """
        ## Angle - Угол наклона

        Тип данных: double

        ### Синтаксис Automation

        ```
        Angle = iObject.Angle       # Получить свойство(* )
        iObject.Angle = Angle       # Установить свойство (* )
        Angle = iObject.GetAngle()  # Получить свойство (**)
        iObject.SetAngle( Angle )   # Установить свойство (**)
        ```
        """
        ...
    @Angle.setter
    def Angle(self, value: float): ...

    @property
    def ShelfDirection(self) -> int:  # icutunitmarking_shelfdirection.html
        """
        ## ShelfDirection - Направление полки

        Тип данных: из перечисления ksShelfDirectionEnum

        ### Синтаксис Automation

        ```
        ShelfDirection = iObject.ShelfDirection       # Получить свойство(* )
        iObject.ShelfDirection = ShelfDirection       # Установить свойство (* )
        ShelfDirection = iObject.GetShelfDirection()  # Получить свойство (**)
        iObject.SetShelfDirection( ShelfDirection )   # Установить свойство (**)
        ```
        """
        ...
    @ShelfDirection.setter
    def ShelfDirection(self, value: int): ...

    @property
    def ShelfX(self) -> float:  # icutunitmarking_shelfx.html
        """
        ## ShelfX - Координата начала полки по оси X

        Тип данных: double

        ### Синтаксис Automation

        ```
        ShelfX = iObject.ShelfX       # Получить свойство(* )
        iObject.ShelfX = ShelfX       # Установить свойство (* )
        ShelfX = iObject.GetShelfX()  # Получить свойство (**)
        iObject.SetShelfX( ShelfX )   # Установить свойство (**)
        ```
        """
        ...
    @ShelfX.setter
    def ShelfX(self, value: float): ...

    @property
    def ShelfY(self) -> float:  # icutunitmarking_shelfy.html
        """
        ## ShelfY - Координата начала полки по оси Y

        Тип данных: double

        ### Синтаксис Automation

        ```
        ShelfY = iObject.StrokeY      # Получить свойство(* )
        iObject.ShelfY = StrokeY      # Установить свойство (* )
        ShelfY = iObject.GetShelfY()  # Получить свойство (**)
        iObject.SetShelfY( ShelfY )   # Установить свойство (**)
        ```
        """
        ...
    @ShelfY.setter
    def ShelfY(self, value: float): ...

    @property
    def StrokeCount(self) -> int:  # icutunitmarking_strokecount.html
        """
        ## StrokeCount - Число штрихов

        Тип данных: long

        ### Синтаксис Automation

        ```
        Count = iObject.StrokeCount       # Получить свойство(* )
        iObject.StrokeCount = Count       # Установить свойство (* )
        Count = iObject.GetStrokeCount()  # Получить свойство (**)
        iObject.SetStrokeCount( Count )   # Установить свойство (**)
        ```
        """
        ...

    @property
    def TextDown(self) -> IText:  # icutunitmarking_textdown.html
        """
        ## TextDown - Текст внизу

        Тип данных: указатель на интерфейс IText

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        TextDown = iObject.TextDown       # Получить свойство(* )
        TextDown = iObject.GetTextDown()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def TextUp(self) -> IText:  # icutunitmarking_textup.html
        """
        ## TextUp - Текст вверху

        Тип данных: указатель на интерфейс IText

        Свойство Текст вверху можно использовать для поиска обозначения узла в сечении в коллекции ICutUnitMarkings. Для поиска в функции IDrawingObjects::Item или ICutUnitMarkings::CutUnitMarking нужно передать строковое значение текста.

        Пример:

        сutUnitMarking = iCutUnitMarkings.CutUnitMarking("A1");

        где "A1" - Текст вверху

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        TextUp = iObject.TextUp       # Получить свойство(* )
        TextUp = iObject.GetTextUp()  # Получить свойство (**)
        ```
        """
        ...

    def AddStroke(self, X, Y, Length) -> bool:  # icutunitmarking_addstroke.html
        """
        ## AddStroke - Добавить штрих

        Значения координат зависят от текущей системы координат для объекта, задаваемой параметром IDrawingObject::DrawingObjectParamType

        ### Синтаксис Automation

        ```
        BOOL AddStoke( double X,
        double Y,
        double Length );
        ```

        ### Входные параметры

        +----------------------------------------------+
        | X, Y   | - координаты точки привязки штриха, |
        +----------------------------------------------+
        | Length | - длина.                            |
        +----------------------------------------------+

        ### Возвращаемое значение

        +-----------------------------+
        | TRUE  | - штрих добавлен,   |
        +-----------------------------+
        | FALSE | - в случае неудачи. |
        +-----------------------------+
        """
        ...

    def DeleteStroke(self, Index) -> bool:  # icutunitmarking_deletestroke.html
        """
        ## DeleteStroke - Удалить штрих

        ### Синтаксис Automation

        ```
        BOOL DeleteStroke( long Index );
        ```

        ### Входные параметры

        +------------------------------------+
        | index | - номер удаляемого штриха. |
        +------------------------------------+

        ### Возвращаемое значение

        +-----------------------------+
        | TRUE  | - штрих добавлено,  |
        +-----------------------------+
        | FALSE | - в случае неудачи. |
        +-----------------------------+
        """
        ...

    def SetStroke(self, Index, arg1):
        ...

    def SetStrokeX(self, Index, arg1):
        ...

    def SetStrokeY(self, Index, arg1):
        ...

    def Stroke(self, Index) -> float:  # icutunitmarking_stroke.html
        """
        ## Stroke - Длина штриха

        Тип данных: double

        ### Синтаксис Automation

        ```
        Stroke = iObject.Stroke( Index )     # Получить свойство(* )
        iObject.Stroke( Index ) = Stroke     # Установить свойство (* )
        Stroke = iObject.GetStroke( Index )  # Получить свойство (**)
        iObject.SetStroke( Index, Stroke )   # Установить свойство (**)
        ```

        ### Входные параметры

        +---------------------------------+
        | Index (long) | - индекс штриха. |
        +---------------------------------+
        """
        ...

    def StrokeX(self, Index) -> float:  # icutunitmarking_strokex.html
        """
        ## StrokeX - Координата точки привязки штриха по оси X

        Тип данных: double

        ### Синтаксис Automation

        ```
        StrokeX = iObject.StrokeX( Index )     # Получить свойство(* )
        iObject.StrokeX( Index ) = StrokeX     # Установить свойство (* )
        StrokeX = iObject.GetStrokeX( Index )  # Получить свойство (**)
        iObject.SetStrokeX( Index, StrokeX )   # Установить свойство (**)
        ```

        ### Входные параметры

        +---------------------------------+
        | Index (long) | - индекс штриха. |
        +---------------------------------+
        """
        ...

    def StrokeY(self, Index) -> float:  # icutunitmarking_strokey.html
        """
        ## StrokeY - Координата точки привязки штриха по оси Y

        Тип данных: double

        ### Синтаксис Automation

        ```
        StrokeY = iObject.StrokeY( Index )     # Получить свойство(* )
        iObject.StrokeY( Index ) = StrokeY     # Установить свойство (* )
        StrokeY = iObject.GetStrokeY( Index )  # Получить свойство (**)
        iObject.SetStrokeY( Index, StrokeY )   # Установить свойство (**)
        ```

        ### Входные параметры

        +---------------------------------+
        | Index (long) | - индекс штриха. |
        +---------------------------------+
        """
        ...




class IMark(IDrawingObject):  # imark.html
    """
    ## Интерфейс IMark

    Марка - базовый интерфейс для марок.

    Интерфейс позволяет получать и изменять общие свойства для марок.

    Получить интерфейс марки можно, используя методы интерфейса коллекции марок IMarks::Mark и IMarks::Add.
    """

    @property
    def AutoNumber(self) -> bool:  # imark_autonumber.html
        """
        ## AutoNumber - Автонумерация марок и позиционных обозначений

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        AutoNumber = iObject.AutoNumber       # Получить свойство(* )
        iObject.AutoNumber = AutoNumber       # Установить свойство (* )
        AutoNumber = iObject.GetAutoNumber()  # Получить свойство (**)
        iObject.SetAutoNumber(AutoNumber)     # Установить свойство (**)
        ```
        """
        ...
    @AutoNumber.setter
    def AutoNumber(self, value: bool): ...

    @property
    def Comment(self) -> str:  # imark_comment.html
        """
        ## Comment - Текст комментария

        Тип данных: BSTR

        Свойство позволяет получить и установить текст комментария.

        ### Синтаксис Automation

        ```
        Comment = iObject.Comment       # Получить свойство(* )
        iObject.Comment = Comment       # Установить свойство (* )
        Comment = iObject.GetComment()  # Получить свойство (**)
        iObject.SetComment(Comment)     # Установить свойство (**)
        ```
        """
        ...
    @Comment.setter
    def Comment(self, value: str): ...

    @property
    def Name(self) -> IText:  # imark_name.html
        """
        ## Name - Имя марки

        Тип данных: указатель на интерфейс IText

        Пример:

        mark = iMarks.Mark("A1");

        где "А" - имя марки

        "1" - номер марки

        1. Свойство доступно только для чтения.

        2. Имя марки и номер марки IMark::Number можно использовать для поиска марки в коллекции марок IMarks.

        3. Для поиска в функции IDrawingObjects::Item или IMarks::Mark нужно передать суммарную строку, включающую в себя имя марки + номер марки без дополнительных разделителей.

        ### Синтаксис Automation

        ```
        Name = iObject.Name       # Получить свойство(* )
        Name = iObject.GetName()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def Number(self) -> IText:  # imark_number.html
        """
        ## Number - Номер марки

        Тип данных: указатель на интерфейс IText

        Пример:

        mark = iMarks.Mark("A1");

        где "А" - имя марки

        "1" - номер марки

        1. Свойство доступно только для чтения.

        2. Имя марки IMark::Name и номер марки можно использовать для поиска марки в коллекции марок IMarks.

        3. Для поиска в функции IDrawingObjects::Item или IMarks::Mark нужно передать суммарную строку, включающую в себя имя марки + номер марки без дополнительных разделителей.

        ### Синтаксис Automation

        ```
        Number = iObject.Number       # Получить свойство(* )
        Number = iObject.GetNumber()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def TextAfter(self) -> IText:  # imark_textafter.html
        """
        ## TextAfter - Текст после

        Тип данных: указатель на интерфейс IText

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        TextAfter = iObject.TextAfter       # Получить свойство(* )
        TextAfter = iObject.GetTextAfter()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def TextBefore(self) -> IText:  # imark_textbefore.html
        """
        ## TextBefore - Текст до

        Тип данных: указатель на интерфейс IText

        Своство доступно только для чтения.

        ### Синтаксис Automation

        ```
        TextBefore = iObject.TextBefore       # Получить свойство(* )
        TextBefore = iObject.GetTextBefore()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def TextUnder(self) -> IText:  # imark_textunder.html
        """
        ## TextUnder - Текст под

        Тип данных: указатель на интерфейс IText

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        TextUnder = iObject.TextUnder       # Получить свойство(* )
        TextUnder = iObject.GetTextUnder()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def X(self) -> float:  # imark_x.html
        """
        ## X - Координата точки привязки по Х

        Тип данных: double

        Значение параметра зависит от текущей системы координат для объекта, задаваемой параметром IDrawingObject::DrawingObjectParamType.

        ### Синтаксис Automation

        ```
        X = iObject.X;       # Получить свойство(* )
        iObject.X = X;       # Установить свойство (* )
        X = iObject.GetX();  # Получить свойство (**)
        iObject.SetX( X );   # Установить свойство (**)
        ```
        """
        ...
    @X.setter
    def X(self, value: float): ...

    @property
    def Y(self) -> float:  # imark_y.html
        """
        ## Y - Координата точки привязки марки по Y

        Тип данных: double

        Значение параметра зависит от текущей системы координат для объекта, задаваемой параметром IDrawingObject::DrawingObjectParamType.

        ### Синтаксис Automation

        ```
        Y = iObject.Y;       # Получить свойство(* )
        iObject.Y = Y;       # Установить свойство (* )
        Y = iObject.GetY();  # Получить свойство (**)
        iObject.SetY(Y);     # Установить свойство (**)
        ```
        """
        ...
    @Y.setter
    def Y(self, value: float): ...




class IMarkOnLeader(IMark):  # imarkonleader.html
    """
    ## Интерфейс IMarkOnLeader

    Интерфейс марки и позиционного обозначения (с линией-выноской).

    Получить интерфейс марки можно, используя методы интерфейса коллекции марок IMarks::Mark и IMarks::Add.
    """

    @property
    def ArrowType(self) -> int:  # imarkonleader_arrowtype.html
        """
        ## ArrowType - Тип стрелки линии-выноски

        Тип данных: из перечисления ksArrowEnum

        ### Синтаксис Automation

        ```
        ArrowType = iObject.ArrowType       # Получить свойство(* )
        iObject.ArrowType = ArrowType       # Установить свойство (* )
        ArrowType = iObject.GetArrowType()  # Получить свойство (**)
        iObject.SetArrowType(ArrowType)     # Установить свойство (**)
        ```
        """
        ...
    @ArrowType.setter
    def ArrowType(self, value: int): ...

    @property
    def BranchCount(self) -> int:  # imarkonleader_branchcount.html
        """
        ## BranchCount - Число ответвлений

        Тип данных: long

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        BranchCount = iObject.BranchCount       # Получить свойство(* )
        BranchCount = iObject.GetBranchCount()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def ParallelBranch(self) -> bool:  # imarkonleader_parallelbranch.html
        """
        ## ParallelBranch - Параллельное ответвление

        Тип данных: BOOL

        Значения свойства:

        +------------------------------------------------+
        | TRUE  | - строятся параллельные ответвления,   |
        +------------------------------------------------+
        | FALSE | - строятся непараллельные ответвления. |
        +------------------------------------------------+

        ### Синтаксис Automation

        ```
        ParallelBranch = iObject.ParallelBranch       # Получить свойство(* )
        iObject.ParallelBranch = ParallelBranch       # Установить свойство (* )
        ParallelBranch = iObject.GetParallelBranch()  # Получить свойство (**)
        iObject.SetParallelBranch(ParallelBranch)     # Установить свойство (**)
        ```
        """
        ...
    @ParallelBranch.setter
    def ParallelBranch(self, value: bool): ...

    @property
    def ShelfDirection(self) -> int:  # imarkonleader_shelfdirection.html
        """
        ## ShelfDirection - Направление полки

        Тип данных: из перечисления ksShelfDirectionEnum

        ### Синтаксис Automation

        ```
        ShelfDirection = iObject.ShelfDirection       # Получить свойство(* )
        iObject.ShelfDirection = ShelfDirection       # Установить свойство (* )
        ShelfDirection = iObject.GetShelfDirection()  # Получить свойство (**)
        iObject.SetShelfDirection(ShelfDirection)     # Установить свойство (**)
        ```
        """
        ...
    @ShelfDirection.setter
    def ShelfDirection(self, value: int): ...

    def AddBranch(self, Index, Begin, Points) -> bool:  # imarkonleader_addbranch.html
        """
        ## AddBranch - Добавить ответвление

        1. Координаты точек в массиве лежат в следующей последовательности: x0, y0, x1, y1, ...xi, yi.

        2. Значения координат зависят от текущей системы координат для объекта, задаваемой параметром IDrawingObject::DrawingObjectParamType

        ### Синтаксис Automation

        ```
        BOOL AddBranch( long Index,
        BOOL Begin,
        VARIANT Points );
        ```

        ### Входные параметры

        +---------------------------------------------------------------------------------------+
        | index  | - индекс, с которым добавляется ответвление,                                 |
        +---------------------------------------------------------------------------------------+
        | Begin  | - признак начала ответвления TRUE - от начала полки, FALSE - от конца полки, |
        +---------------------------------------------------------------------------------------+
        | Points | - массив SafeArray координат точек.                                          |
        +---------------------------------------------------------------------------------------+

        ### Возвращаемое значение

        +----------------------------------+
        | TRUE  | - ответвление добавлено, |
        +----------------------------------+
        | FALSE | - в случае неудачи.      |
        +----------------------------------+
        """
        ...

    def AddBranchByPoint(self, Index, Begin, X, Y) -> bool:  # imarkonleader_addbranchbypoint.html
        """
        ## AddBranchByPoint - Добавить прямолинейное ответвление

        Значения координат пересчитываются в зависимости от текущей системы координат для объекта, задаваемой параметром IDrawingObject::DrawingObjectParamType

        ### Синтаксис Automation

        ```
        BOOL AddBranchByPoint( long Index,
        BOOL Begin,
        double x, double y );
        ```

        ### Входные параметры

        +--------------------------------------------------------------------------------------+
        | index | - индекс, с которым добавляется ответвление,                                 |
        +--------------------------------------------------------------------------------------+
        | Begin | - признак начала ответвления TRUE - от начала полки, FALSE - от конца полки, |
        +--------------------------------------------------------------------------------------+
        | x, y  | - координаты конца ответвления.                                              |
        +--------------------------------------------------------------------------------------+

        ### Возвращаемое значение

        +----------------------------------+
        | TRUE  | - ответвление добавлено, |
        +----------------------------------+
        | FALSE | - в случае неудачи.      |
        +----------------------------------+
        """
        ...

    def BranchBegin(self, Index) -> bool:  # imarkonleader_branchbegin.html
        """
        ## BranchBegin - Начало ответвления

        Тип данных: BOOL

        Значения свойства:

        +----------------------------+
        | TRUE  | - от начала полки, |
        +----------------------------+
        | FALSE | - от конца полки.  |
        +----------------------------+

        Свойство задает признак начала ответвления.

        ### Синтаксис Automation

        ```
        BranchBegin = iObject.BranchBegin       # Получить свойство(* )
        iObject.BranchBegin = BranchBegin       # Установить свойство (* )
        BranchBegin = iObject.GetBranchBegin()  # Получить свойство (**)
        iObject.SetBranchBegin(BranchBegin)     # Установить свойство (**)
        ```
        """
        ...

    def BranchPoints(self, Index) -> list[float]:  # imarkonleader_branchpoints.html
        """
        ## BranchPoints - Массив SAFEARRAY координат точек ответвления

        Тип данных: VARIANT типа VT_ARRAY | VT_R8

        1. Координаты точек в массиве лежат в следующей последовательности: x0, y0, x1, y1, ...xi, yi.

        2. Значения координат зависят от текущей системы координат для объекта, задаваемой параметром IDrawingObject::DrawingObjectParamType.

        ### Синтаксис Automation

        ```
        BranchPoints = iObject.BranchPoints(index)     # Получить свойство(* )
        iObject.BranchPoints(index) = BranchPoints     # Установить свойство (* )
        BranchPoints = iObject.GetBranchPoints(index)  # Получить свойство (**)
        iObject.SetBranchPoints(index, BranchPoints)   # Установить свойство (**)
        ```
        """
        ...

    def BranchPointsCount(self, Index) -> float:  # imarkonleader_branchpointscount.html
        """
        ## BranchPointsCount - Число точек в ответвлении

        Тип данных: double

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        Count = iObject.BranchPointsCount       # Получить свойство(* )
        Count = iObject.GetBranchPointsCount()  # Получить свойство (**)
        ```
        """
        ...

    def BranchX(self, Index) -> float:  # imarkonleader_branchx.html
        """
        ## BranchX - Координата конечной точки ответвления по оси X

        Тип данных: double

        Значения координат зависят от текущей системы координат для объекта, задаваемой параметром IDrawingObject::DrawingObjectParamType.

        ### Синтаксис Automation

        ```
        BranchX = iObject.BranchX       # Получить свойство(* )
        iObject.BranchX = BranchX       # Установить свойство (* )
        BranchX = iObject.GetBranchX()  # Получить свойство (**)
        iObject.SetBranchX(BranchX)     # Установить свойство (**)
        ```
        """
        ...

    def BranchY(self, Index) -> float:  # imarkonleader_branchy.html
        """
        ## BranchY - Координата конечной точки ответвления по оси Y

        Тип данных: double

        Значения координат зависят от текущей системы координат для объекта, задаваемой параметром IDrawingObject::DrawingObjectParamType.

        ### Синтаксис Automation

        ```
        BranchY = iObject.BranchY       # Получить свойство(* )
        iObject.BranchY = BranchY       # Установить свойство (* )
        BranchY = iObject.GetBranchY()  # Получить свойство (**)
        iObject.SetBranchY(BranchY)     # Установить свойство (**)
        ```
        """
        ...

    def DeleteBranch(self, Index) -> bool:  # imarkonleader_deletebranch.html
        """
        ## DeleteBranch - Удалить ответвление

        ### Синтаксис Automation

        ```
        BOOL DeleteBranch( long Index )
        ```

        ### Входные параметры

        +------------------------------------------+
        | index | - индекс удаляемого ответвление. |
        +------------------------------------------+

        ### Возвращаемое значение

        +--------------------------------+
        | TRUE  | - ответвление удалено, |
        +--------------------------------+
        | FALSE | - в случае неудачи.    |
        +--------------------------------+
        """
        ...

    def SetBranchBegin(self, Index, arg1):
        ...

    def SetBranchPoints(self, Index, arg1):
        ...

    def SetBranchX(self, Index, arg1):
        ...

    def SetBranchY(self, Index, arg1):
        ...




class IMarkInsideForm(IMark):  # imarkinsideform.html
    """
    ## Интерфейс IMarkInsideForm

    Интерфейс марки и позиционного обозначения (без линии-выноски).

    Получить интерфейс марки можно, используя методы интерфейса коллекции марок IMarks::Mark и IMarks::Add.
    """

    @property
    def Angle(self) -> float:  # imarkinsideform_angle.html
        """
        ## Angle - Угол

        Тип данных: double

        Значение параметра зависит от текущей системы координат для объекта, задаваемой параметром IDrawingObject::DrawingObjectParamType.

        ### Синтаксис Automation

        ```
        Angle = iObject.Angle       # Получить свойство(* )
        iObject.Angle = Angle       # Установить свойство (* )
        Angle = iObject.GetAngle()  # Получить свойство (**)
        iObject.SetAngle(Angle)     # Установить свойство (**)
        ```
        """
        ...
    @Angle.setter
    def Angle(self, value: float): ...

    @property
    def Form(self) -> int:  # imarkinsideform_form.html
        """
        ## Form - Форма

        Тип данных: из перечисления ksMarkInsideFormEnum

        ### Синтаксис Automation

        ```
        Form = iObject.Form       # Получить свойство(* )
        iObject.Form = Form       # Установить свойство (* )
        Form = iObject.GetForm()  # Получить свойство (**)
        iObject.SetForm(Form)     # Установить свойство (**)
        ```
        """
        ...
    @Form.setter
    def Form(self, value: int): ...

    @property
    def FormGabarit(self) -> float:  # imarkinsideform_formgabarit.html
        """
        ## FormGabarit - Габарит формы (радиус, ширина)

        Тип данных: double

        Значение параметра зависит от текущей системы координат для объекта, задаваемой параметром IDrawingObject::DrawingObjectParamType.

        ### Синтаксис Automation

        ```
        FormGabarit = iObject.FormGabarit       # Получить свойство(* )
        iObject.FormGabarit = FormGabarit       # Установить свойство (* )
        FormGabarit = iObject.GetFormGabarit()  # Получить свойство (**)
        iObject.SetFormGabarit(FormGabarit)     # Установить свойство (**)
        ```
        """
        ...
    @FormGabarit.setter
    def FormGabarit(self, value: float): ...

    @property
    def FormHeight(self) -> float:  # imarkinsideform_formheight.html
        """
        ## FormHeight - Высота формы

        Тип данных: double

        ### Синтаксис Automation

        ```
        FormHeight = iObject.FormHeight       # Получить свойство(* )
        iObject.FormHeight = FormHeight       # Установить свойство (* )
        FormHeight = iObject.GetFormHeight()  # Получить свойство (**)
        iObject.SetFormHeight(FormHeight)     # Установить свойство (**)
        ```
        """
        ...
    @FormHeight.setter
    def FormHeight(self, value: float): ...

    @property
    def FormStyle(self) -> int:  # imarkinsideform_formstyle.html
        """
        ## FormStyle - Стиль линий формы

        Тип данных: long

        Стиль формы позволяет задать стиль линий, используемых для отрисовки формы:

        Стандартные стили ksCurveStyleEnum, используемые для отрисовки формы марки:

        +----------------------------+
        | ksCSNormal | 1 | Основная, |
        +----------------------------+
        | ksCSThin   | 2 | Тонкая,   |
        +----------------------------+
        | ksCSAxial  | 3 | Осевая.   |
        +----------------------------+

        Допустимо задавать и другие стили, в том числе и пользовательские.

        ### Синтаксис Automation

        ```
        FormStyle = iObject.FormStyle       # Получить свойство(* )
        iObject.FormStyle = FormStyle       # Установить свойство (* )
        FormStyle = iObject.GetFormStyle()  # Получить свойство (**)
        iObject.SetFormStyle(FormStyle)     # Установить свойство (**)
        ```
        """
        ...
    @FormStyle.setter
    def FormStyle(self, value: int): ...




class IMarkOnLine(IMark):  # imarkonline.html
    """
    ## Интерфейс IMarkOnLine

    Интерфейс марки и позиционного обозначения (на линии).

    Получить интерфейс марки можно, используя методы интерфейса коллекции марок IMarks::Mark и IMarks::Add.
    """

    @property
    def Line(self) -> IDrawingObject:  # imarkonline_line.html
        """
        ## Line - Линия, с которой связывается марка

        Тип данных: указатель на интерфейс IDrawingObject

        ### Синтаксис Automation

        ```
        Line = iObject.Line       # Получить свойство(* )
        iObject.Line = Line       # Установить свойство (* )
        Line = iObject.GetLine()  # Получить свойство (**)
        iObject.SetLine(Line)     # Установить свойство (**)
        ```
        """
        ...
    @Line.setter
    def Line(self, value: IDrawingObject): ...

    @property
    def Position(self) -> int:  # imarkonline_position.html
        """
        ## Position - Положение марки относительно линии

        Тип данных: из перечисления ksMarkOnLinePosTypeEnum

        ### Синтаксис Automation

        ```
        Position = iObject.Position       # Получить свойство(* )
        iObject.Position = Position       # Установить свойство (* )
        Position = iObject.GetPosition()  # Получить свойство (**)
        iObject.SetPosition(Position)     # Установить свойство (**)
        ```
        """
        ...
    @Position.setter
    def Position(self, value: int): ...




class IMultiTextLeader(IDrawingObject):  # imultitextleader.html
    """
    ## Интерфейс IMultiTextLeader

    Интерфейс выносной надписи к многослойным конструкциям.

    Интерфейс позволяет получить доступ к выносной надписи к многослойным конструкциям. С помощью интерфейса можно получить и отредактировать параметры объекта.

    1. Получить интерфейс можно, используя свойство интерфейса коллекции IMultiTextLeaders::MultiTextLeader или метод IMultiTextLeaders::Add.

    2. Новые параметры вступят в силу после вызова метода IDrawingObject::Update.
    """

    @property
    def Align(self) -> bool:  # imultitextleader_align.html
        """
        ## Align - Выравнивание

        Тип данных: BOOL

        Значения свойства:

        +-----------------------------------+
        | TRUE  | - полки одинаковой длины, |
        +-----------------------------------+
        | FALSE | - полки по длине текста.  |
        +-----------------------------------+

        Свойство позволяет установить и получить выравнивание полок.

        ### Синтаксис Automation

        ```
        VFormat = iObject.VFormat;        # Получить свойство (* )
        iObject.VFormat = VFormat;        # Установить свойство (* )
        VFormat = iObject.GetVFormat( );  # Получить свойство (**)
        iObject.SetVFormat( VFormat );    # Установить свойство (**)
        ```
        """
        ...
    @Align.setter
    def Align(self, value: bool): ...

    @property
    def ArrowType(self) -> int:  # imultitextleader_arrowtype.html
        """
        ## ArrowType - Тип стрелки линии-выноски

        Тип данных: из перечисления ksArrowEnum

        Свойство позволяет установить и получить тип стрелки линии-выноски.

        ### Синтаксис Automation

        ```
        ArrowType = iObject.ArrowType        # Получить свойство(* )
        iObject.ArrowType = ArrowType        # Установить свойство (* )
        ArrowType = iObject.GetArrowType( )  # Получить свойство (**)
        iObject.SetArrowType( ArrowType )    # Установить свойство (**)
        ```
        """
        ...
    @ArrowType.setter
    def ArrowType(self, value: int): ...

    @property
    def BranchCount(self) -> int:  # imultitextleader_branchcount.html
        """
        ## BranchCount - Количество ответвлений

        Тип данных: long

        1. Свойство доступно только для чтения.

        2. Свойство позволяет получить количество ответвлений.

        ### Синтаксис Automation

        ```
        BranchCount = iObject.BranchCount        # Получить свойство (* )
        BranchCount = iObject.GetBranchCount( )  # Получить свойство (**)
        ```
        """
        ...

    @property
    def Form(self) -> bool:  # imultitextleader_form.html
        """
        ## Form - Тип формы

        Тип данных: BOOL

        Значения свойства:

        +------------------------+
        | TRUE  | - с выступом,  |
        +------------------------+
        | FALSE | - без выступа. |
        +------------------------+

        Свойство позволяет установить и получить тип формы.

        ### Синтаксис Automation

        ```
        Form = iObject.Form        # Получить свойство (* )
        iObject.Form = Form        # Установить свойство (* )
        Form = iObject.GetForm( )  # Получить свойство (**)
        iObject.SetForm( Form )    # Установить свойство (**)
        ```
        """
        ...
    @Form.setter
    def Form(self, value: bool): ...

    @property
    def ShelfDirection(self) -> int:  # imultitextleader_shelfdirection.html
        """
        ## ShelfDirection - Направление полки

        Тип данных: из перечисления ksShelfDirectionEnum

        Значения свойства:

        +----------------------------+
        | ksLSLeft  | -1 | - влево,  |
        +----------------------------+
        | ksLSRight | 1  | - вправо. |
        +----------------------------+

        Свойство позволяет установить и получить направление полки.

        ### Синтаксис Automation

        ```
        ShelfDirection = iObject.ShelfDirection        # Получить свойство (* )
        iObject.ShelfDirection = ShelfDirection        # Установить свойство (* )
        ShelfDirection = iObject.GetShelfDirection( )  # Получить свойство (**)
        iObject.SetShelfDirection( ShelfDirection )    # Установить свойство (**)
        ```
        """
        ...
    @ShelfDirection.setter
    def ShelfDirection(self, value: int): ...

    @property
    def ShelfX(self) -> float:  # imultitextleader_shelfx.html
        """
        ## ShelfX - Координата начала полки по оси X

        Тип данных: double

        1. Свойство позволяет установить и получить координату начала полки по Х.

        2. Значения координат пересчитываются в зависимости от текущей системы координат для объекта, задаваемой параметром IDrawingObject::DrawingObjectParamType.

        ### Синтаксис Automation

        ```
        ShelfX = iObject.ShelfX        # Получить свойство (* )
        iObject.ShelfX = ShelfX        # Установить свойство (* )
        ShelfX = iObject.GetShelfX( )  # Получить свойство (**)
        iObject.SetShelfX( ShelfX )    # Установить свойство (**)
        ```
        """
        ...
    @ShelfX.setter
    def ShelfX(self, value: float): ...

    @property
    def ShelfY(self) -> float:  # imultitextleader_shelfy.html
        """
        ## ShelfY - Координата начала полки по оси Y

        Тип данных: double

        1. Свойство позволяет установить и получить координату начала полки по Y.

        2. Значения координат пересчитываются в зависимости от текущей системы координат для объекта, задаваемой параметром IDrawingObject::DrawingObjectParamType.

        ### Синтаксис Automation

        ```
        ShelfY = iObject.ShelfY        # Получить свойство (* )
        iObject.ShelfY = ShelfY        # Установить свойство (* )
        ShelfY = iObject.GetShelfY( )  # Получить свойство (**)
        iObject.SetShelfY( ShelfY )    # Установить свойство (**)
        ```
        """
        ...
    @ShelfY.setter
    def ShelfY(self, value: float): ...

    @property
    def Text(self) -> IText:  # imultitextleader_text.html
        """
        ## Text - Текст

        Тип данных: указатель на интерфейс IText

        1. Свойство доступно только для чтения.

        2. Свойство позволяет получить текст объекта.

        ### Синтаксис Automation

        ```
        Text = iObject.Text        # Получить свойство (* )
        Text = iObject.GetText( )  # Получить свойство (**)
        ```
        """
        ...

    @property
    def TextDirection(self) -> bool:  # imultitextleader_textdirection.html
        """
        ## TextDirection - Направление текста

        Тип данных: BOOL

        Значения свойства:

        +------------------+
        | TRUE  | - вверх, |
        +------------------+
        | FALSE | - вниз.  |
        +------------------+

        Свойство позволяет установить и получить направление текста.

        ### Синтаксис Automation

        ```
        TextDirection = iObject.TextDirection        # Получить свойство (* )
        iObject.TextDirection = TextDirection        # Установить свойство (* )
        TextDirection = iObject.GetTextDirection( )  # Получить свойство (**)
        iObject.SetTextDirection( TextDirection )    # Установить свойство (**)
        ```
        """
        ...
    @TextDirection.setter
    def TextDirection(self, value: bool): ...

    def AddBranch(self, Index, Points) -> bool:  # imultitextleader_addbranch.html
        """
        ## AddBranch - Добавить ответвление

        1. Координаты точек в массиве лежат в следующей последовательности: x0, y0, x1, y1, ...xi, yi.

        2. Значения координат пересчитываются в зависимости от текущей системы координат для объекта, задаваемой параметром IDrawingObject::DrawingObjectParamType.

        3. Если index = -1, ответвление добавляется в конец массива ответвлений.

        ### Синтаксис Automation

        ```
        BOOL AddBranch( long index,
        VARIANT Points );
        ```

        ### Входные параметры

        +-------------------------------------------------------+
        | index  | - индекс, с которым добавляется ответвление, |
        +-------------------------------------------------------+
        | Points | - массив SafeArray координат точек.          |
        +-------------------------------------------------------+

        ### Возвращаемое значение

        +----------------------------------+
        | TRUE  | - ответвление добавлено, |
        +----------------------------------+
        | FALSE | - в случае неудачи.      |
        +----------------------------------+
        """
        ...

    def AddBranchByPoint(self, Index, X, Y) -> bool:  # imultitextleader_addbranchbypoint.html
        """
        ## AddBranchByPoint - Добавить прямолинейное ответвление

        1. Значения координат пересчитываются в зависимости от текущей системы координат для объекта, задаваемой параметром IDrawingObject::DrawingObjectParamType.

        2. Если index = -1, ответвление добавляется в конец массива ответвлений.

        ### Синтаксис Automation

        ```
        BOOL AddBranchByPoint( long index,
        double x, double y );
        ```

        ### Входные параметры

        +------------------------------------------------------+
        | index | - индекс, с которым добавляется ответвление, |
        +------------------------------------------------------+
        | x, y  | - координаты конца ответвления.              |
        +------------------------------------------------------+

        ### Возвращаемое значение

        +----------------------------------+
        | TRUE  | - ответвление добавлено, |
        +----------------------------------+
        | FALSE | - в случае неудачи.      |
        +----------------------------------+
        """
        ...

    def BranchPoints(self, Index) -> list[float]:  # imultitextleader_branchpoints.html
        """
        ## BranchPoints - Массив SAFEARRAY координат точек ответвления

        Тип данных: VARIANT ( VT_ARRAY | VT_R8 )

        1. Свойство доступно только для чтения.

        2. Свойство позволяет получить SAFEARRAY координат точек ответвления. В массиве последовательно собраны координаты X и Y для каждой точки ответвления: x0, y0, x1, y1, ...xi, yi.

        ### Синтаксис Automation

        ```
        BranchPoints = iObject.BranchPoints        # Получить свойство (* )
        BranchPoints = iObject.GetBranchPoints( )  # Получить свойство (**)
        ```
        """
        ...

    def BranchPointsCount(self, Index) -> int:  # imultitextleader_branchpointscount.html
        """
        ## BranchPointsCount - Количество точек в ответвлении

        Тип данных: long

        1. Свойство доступно только для чтения.

        2. Свойство позволяет получить количество точек в ответвлении.

        ### Синтаксис Automation

        ```
        BranchPointsCount = iObject.BranchPointsCount  # Получить свойство (* )
        iObject.GetBranchPointsCount( )                # Получить свойство (**)
        ```
        """
        ...

    def BranchX(self, Index) -> float:  # imultitextleader_branchx.html
        """
        ## BranchX - Координата конечной точки ответвления по оси X

        Тип данных: double

        1. Свойство позволяет установить и получить координату конечной точки ответвления по оси Х.

        2. Значения координат пересчитываются в зависимости от текущей системы координат для объекта, задаваемой параметром IDrawingObject::DrawingObjectParamType.

        ### Синтаксис Automation

        ```
        BranchX = iObject.BranchX( Index )     # Получить свойство (* )
        iObject.BranchX = BranchX( Index )     # Установить свойство (* )
        BranchX = iObject.GetBranchX( Index )  # Получить свойство (**)
        iObject.SetBranchX( Index, BranchX )   # Установить свойство (**)
        ```

        ### Входные параметры

        +------------------------------------+
        | Index | - индекс объекта ( long ). |
        +------------------------------------+
        """
        ...

    def BranchY(self, Index) -> float:  # imultitextleader_branchy.html
        """
        ## BranchY - Координата конечной точки ответвления по оси Y

        Тип данных: double

        1. Свойство позволяет установить и получить координату конечной точки ответвления по оси Y.

        2. Значения координат пересчитываются в зависимости от текущей системы координат для объекта, задаваемой параметром IDrawingObject::DrawingObjectParamType.

        ### Синтаксис Automation

        ```
        BranchY = iObject.BranchY( Index )     # Получить свойство (* )
        iObject.BranchY = BranchY( Index )     # Установить свойство (* )
        BranchY = iObject.GetBranchY( Index )  # Получить свойство (**)
        iObject.SetBranchY( Index, BranchY )   # Установить свойство (**)
        ```

        ### Входные параметры

        +-----------------------------------+
        | Index | - индекс объекта (long ). |
        +-----------------------------------+
        """
        ...

    def DeleteBranch(self, Index) -> bool:  # imultitextleader_deletebranch.html
        """
        ## DeleteBranch - Удалить ответвление

        1. Значения координат пересчитываются в зависимости от текущей системы координат для объекта, задаваемой параметром IDrawingObject::DrawingObjectParamType.

        2. Если index = -1, ответвление добавляется в конец массива ответвлений.

        ### Синтаксис Automation

        ```
        BOOL DeleteBranch( long index );
        ```

        ### Входные параметры

        +------------------------------------------+
        | index | - индекс удаляемого ответвления. |
        +------------------------------------------+

        ### Возвращаемое значение

        +--------------------------------+
        | TRUE  | - ответвление удалено, |
        +--------------------------------+
        | FALSE | - в случае неудачи.    |
        +--------------------------------+
        """
        ...

    def SetBranchPoints(self, Index, arg1):
        ...

    def SetBranchX(self, Index, arg1):
        ...

    def SetBranchY(self, Index, arg1):
        ...




class IUnitMarking(IDrawingObject):  # iunitmarking.html
    """
    ## Интерфейс IUnitMarking

    Интерфейс обозначения узла.

    Получить интерфейс можно, используя свойство IUnitMarkings::UnitMarking или метод IUnitMarkings::Add интерфейса коллекции обозначений узла.
    """

    @property
    def FilletRadius(self) -> float:  # iunitmarking_filletradius.html
        """
        ## FilletRadius - Радиус скругления прямоугольного контура

        Тип данных: double

        ### Синтаксис Automation

        ```
        FilletRadius = iObject.FilletRadius       # Получить свойство(* )
        iObject.FilletRadius = FilletRadius       # Установить свойство (* )
        FilletRadius = iObject.GetFilletRadius()  # Получить свойство (**)
        iObject.SetFilletRadius( FilletRadius )   # Установить свойство (**)
        ```
        """
        ...
    @FilletRadius.setter
    def FilletRadius(self, value: float): ...

    @property
    def Form(self) -> int:  # iunitmarking_form.html
        """
        ## Form - Форма контура

        Тип данных: тип формы из перечисления ksContourFormEnum

        ### Синтаксис Automation

        ```
        Form = iObject.Form       # Получить свойство(* )
        iObject.Form = Form       # Установить свойство (* )
        Form = iObject.GetForm()  # Получить свойство (**)
        iObject.SetForm( Form )   # Установить свойство (**)
        ```
        """
        ...
    @Form.setter
    def Form(self, value: int): ...

    @property
    def Height(self) -> float:  # iunitmarking_height.html
        """
        ## Height - Высота контура

        Тип данных: double

        ### Синтаксис Automation

        ```
        Height = iObject.Height       # Получить свойство(* )
        iObject.Height = Height       # Установить свойство (* )
        Height = iObject.GetHeight()  # Получить свойство (**)
        iObject.SetHeight( Height )   # Установить свойство (**)
        ```
        """
        ...
    @Height.setter
    def Height(self, value: float): ...

    @property
    def Radius(self) -> float:  # iunitmarking_radius.html
        """
        ## Radius - Радиус контура обозначения узла

        Тип данных: double

        ### Синтаксис Automation

        ```
        Radius = iObject.Radius       # Получить свойство(* )
        iObject.Radius = Radius       # Установить свойство (* )
        Radius = iObject.GetRadius()  # Получить свойство (**)
        iObject.SetRadius( Radius )   # Установить свойство (**)
        ```
        """
        ...
    @Radius.setter
    def Radius(self, value: float): ...

    @property
    def ShelfDirection(self) -> int:  # iunitmarking_shelfdirection.html
        """
        ## ShelfDirection - Направление полки

        Тип данных: тип направления полки из перечисления ksShelfDirectionEnum

        ### Синтаксис Automation

        ```
        ShelfDirection = iObject.ShelfDirection       # Получить свойство(* )
        iObject.ShelfDirection = ShelfDirection       # Установить свойство (* )
        ShelfDirection = iObject.GetShelfDirection()  # Получить свойство (**)
        iObject.SetShelfDirection( ShelfDirection )   # Установить свойство (**)
        ```
        """
        ...
    @ShelfDirection.setter
    def ShelfDirection(self, value: int): ...

    @property
    def ShelfX(self) -> float:  # iunitmarking_shelfx.html
        """
        ## ShelfX - Координата начала полки по оси X

        Тип данных: double

        ### Синтаксис Automation

        ```
        ShelfX = iObject.ShelfX       # Получить свойство(* )
        iObject.ShelfX = ShelfX       # Установить свойство (* )
        ShelfX = iObject.GetShelfX()  # Получить свойство (**)
        iObject.SetShelfX( ShelfX )   # Установить свойство (**)
        ```
        """
        ...
    @ShelfX.setter
    def ShelfX(self, value: float): ...

    @property
    def ShelfY(self) -> float:  # iunitmarking_shelfy.html
        """
        ## ShelfY - Координата начала полки по оси Y

        Тип данных: double

        ### Синтаксис Automation

        ```
        ShelfY = iObject.ShelfY       # Получить свойство(* )
        iObject.ShelfY = ShelfY       # Установить свойство (* )
        ShelfY = iObject.GetShelfY()  # Получить свойство (**)
        iObject.SetShelfY( ShelfY )   # Установить свойство (**)
        ```
        """
        ...
    @ShelfY.setter
    def ShelfY(self, value: float): ...

    @property
    def TextDown(self) -> IText:  # iunitmarking_textdown.html
        """
        ## TextDown - Текст внизу

        Тип данных: указатель на интерфейс IText

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        TextDown = iObject.TextDown       # Получить свойство(* )
        TextDown = iObject.GetTextDown()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def TextUp(self) -> IText:  # iunitmarking_textup.html
        """
        ## TextUp - Текст вверху

        Тип данных: указатель на интерфейс IText

        Свойство Текст вверху можно использовать для поиска обозначения узла в коллекции IUnitMarkings. Для поиска в функции IDrawingObjects::Item или IUnitMarkings::UnitMarking нужно передать строковое значение текста.

        Пример:

        UnitMarking = IUnitMarkings.UnitMarking("A1");

        где "A1" - Текст вверху

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        TextUp = iObject.TextUp       # Получить свойство(* )
        TextUp = iObject.GetTextUp()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def Width(self) -> float:  # iunitmarking_width.html
        """
        ## Width - Ширина контура

        Тип данных: double

        ### Синтаксис Automation

        ```
        Width = iObject.Width       # Получить свойство(* )
        iObject.Width = Width       # Установить свойство (* )
        Width = iObject.GetWidth()  # Получить свойство (**)
        iObject.SetWidth( Width )   # Установить свойство (**)
        ```
        """
        ...
    @Width.setter
    def Width(self, value: float): ...

    @property
    def Xc(self) -> float:  # iunitmarking_xc.html
        """
        ## Xc - Координата X центра контура

        Тип данных: double

        ### Синтаксис Automation

        ```
        Xc = iObject.Xc       # Получить свойство(* )
        iObject.Xc = Xc       # Установить свойство (* )
        Xc = iObject.GetXc()  # Получить свойство (**)
        iObject.SetXc( Xc )   # Установить свойство (**)
        ```
        """
        ...
    @Xc.setter
    def Xc(self, value: float): ...

    @property
    def Yc(self) -> float:  # iunitmarking_yc.html
        """
        ## Yc - Координата Y центра контура

        Тип данных: double

        ### Синтаксис Automation

        ```
        Yс = iObject.Yс       # Получить свойство(* )
        iObject.Yс = Yс       # Установить свойство (* )
        Yс = iObject.GetYс()  # Получить свойство (**)
        iObject.SetYс( Yс )   # Установить свойство (**)
        ```
        """
        ...
    @Yc.setter
    def Yc(self, value: float): ...




class IRemoteElement(IUnitMarking):  # iremoteelement.html
    """
    ## Интерфейс IRemoteElement

    Интерфейс выносного элемента.

    Интерфейс позволяет задавать параметры выносного элемента.

    1. Интерфейс можно получить у коллекции выносных элементов, используя свойство IRemoteElements::RemoteElement или метод IRemoteElements::Add.

    2. После задания параметров выносного элемента требуется вызвать метод IDrawingObject::Update.
    """

    @property
    def AdditionalText(self) -> IText:  # iremoteelement_additionaltext.html
        """
        ## AdditionalText - Дополнительный текст

        Тип данных: указатель на интерфейс текста IText

        1. Свойство позволяет получить дополнительный текст выносного элемента.

        2. Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        AdditionalText = iObject.AdditionalText ( Index );    # Получить свойство(* )
        AdditionalText = iObject.GetAdditionalText( Index );  # Получить свойство (**)
        ```
        """
        ...

    @property
    def AutoSheet(self) -> bool:  # iremoteelement_autosheet.html
        """
        ## AutoSheet - Лист

        Тип данных: BOOL

        Свойство позволяет устанавливать и получать признак включения номера листа в обозначение выносного элемента.

        ### Синтаксис Automation

        ```
        AutoSheet = Object.AutoSheet       # Получить свойство(* )
        Object.AutoSheet = AutoSheet       # Установить свойство (* )
        AutoSheet = Object.GetAutoSheet()  # Получить свойство (**)
        Object.SetAutoSheet( AutoSheet )   # Установить свойство (**)
        ```
        """
        ...
    @AutoSheet.setter
    def AutoSheet(self, value: bool): ...

    @property
    def AutoSorted(self) -> bool:  # iremoteelement_autosorted.html
        """
        ## AutoSorted - Автосортировка

        Тип данных: BOOL

        Свойство позволяет устанавливать и получать признак автосортировки.

        ### Синтаксис Automation

        ```
        AutoSorted = Object.AutoSorted       # Получить свойство(* )
        Object.AutoSorted = AutoSorted       # Установить свойство (* )
        AutoSorted = Object.GetAutoSorted()  # Получить свойство (**)
        Object.SetAutoSorted( AutoSorted )   # Установить свойство (**)
        ```
        """
        ...
    @AutoSorted.setter
    def AutoSorted(self, value: bool): ...

    @property
    def AutoZone(self) -> bool:  # iremoteelement_autozone.html
        """
        ## AutoZone - Зона

        Тип данных: BOOL

        Свойство позволяет устанавливать и получать признак включения обозначения зоны в обозначение выносного элемента.

        ### Синтаксис Automation

        ```
        AutoZone = Object.AutoZone       # Получить свойство(* )
        Object.AutoZone = AutoZone       # Установить свойство (* )
        AutoZone = Object.GetAutoZone()  # Получить свойство (**)
        Object.SetAutoZone( AutoZone )   # Установить свойство (**)
        ```
        """
        ...
    @AutoZone.setter
    def AutoZone(self, value: bool): ...




class IUnitNumber(IDrawingObject):  # iunitnumber.html
    """
    ## Интерфейс IUnitNumber

    Интерфейс номера узла.

    Интерфейс позволяет получать и изменять свойства номеров узлов на чертеже.

    Интерфейс можно получить с помощью метода коллекции номеров узла IUnitNumbers::Add или свойства IUnitNumbers::UnitNumber.
    """

    @property
    def TextDown(self) -> IText:  # iunitnumber_textdown.html
        """
        ## TextDown - Текст снизу

        Тип данных: указатель на интерфейс текста IText

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        TextDown = iObject.TextDown    # Получить свойство(* )
        TextDown = iObject.TextDown()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def TextUp(self) -> IText:  # iunitnumber_textup.html
        """
        ## TextUp - Текст сверху

        Тип данных: указатель на интерфейс текста IText

        Пример:

        UnitNumber = iUnitNumbers.UnitNumber("TextUp"),

        где "TextUp" - текст сверху номера узла.

        1. Свойство доступно только для чтения.

        2. Текст можно использовать для поиска номера узла в коллекции IUnitNumbers. Для поиска в функции IDrawingObjects::Item или IUnitNumbers::UnitNumber нужно передать строковое значение текста.

        ### Синтаксис Automation

        ```
        TextUp = iObject.TextUp    # Получить свойство(* )
        TextUp = iObject.TextUp()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def X(self) -> float:  # iunitnumber_x.html
        """
        ## X - Координата номера узла по Х

        Тип данных: double

        1. Значение параметра зависит от текущей системы координат для объекта, задаваемой параметром IDrawingObject::DrawingObjectParamType.

        2. Свойство позволяет считывать и устанавливать координату номера узла по X.

        ### Синтаксис Automation

        ```
        X = iObject.X;       # Получить свойство(* )
        iObject.X = X;       # Установить свойство (* )
        X = iObject.GetX();  # Получить свойство (**)
        iObject.SetX( X );   # Установить свойство (**)
        ```
        """
        ...
    @X.setter
    def X(self, value: float): ...

    @property
    def Y(self) -> float:  # iunitnumber_y.html
        """
        ## Y - Координата номера узла по Y

        Тип данных: double

        1. Значение параметра зависит от текущей системы координат для объекта, задаваемой параметром IDrawingObject::DrawingObjectParamType.

        2. Свойство позволяет считывать и устанавливать координату номера узла по Y.

        ### Синтаксис Automation

        ```
        Y = iObject.Y;       # Получить свойство(* )
        iObject.Y = Y;       # Установить свойство (* )
        Y = iObject.GetY();  # Получить свойство (**)
        iObject.SetY(Y);     # Установить свойство (**)
        ```
        """
        ...
    @Y.setter
    def Y(self, value: float): ...




class IAxisJut(IKompasAPIObject):  # iaxisjut.html
    """
    ## Интерфейс IAxisJut

    Интерфейс выступа строительной оси.

    Интерфейс позволяет задавать параметры выступов для прямой и дуговой строительных осей.

    Интерфейс можно получить с помощью свойств IStraightAxis::Jut и IArcAxis::Jut.
    """

    @property
    def BreakDirection(self) -> bool:  # iaxisjut_breakdirection.html
        """
        ## BreakDirection - Направление смещения

        Тип данных: BOOL

        Значения свойства:

        +----------------------------+
        | TRUE  | - смещение вправо, |
        +----------------------------+
        | FALSE | - смещение влево.  |
        +----------------------------+

        ### Синтаксис Automation

        ```
        BreakDirection = iObject.BreakDirection       # Получить свойство(* )
        iObject.BreakDirection = BreakDirection       # Установить свойство (* )
        BreakDirection = iObject.GetBreakDirection()  # Получить свойство (**)
        iObject.SetBreakDirection( BreakDirection )   # Установить свойство (**)
        ```
        """
        ...
    @BreakDirection.setter
    def BreakDirection(self, value: bool): ...

    @property
    def BreakOffset(self) -> float:  # iaxisjut_breakoffset.html
        """
        ## BreakOffset - Cмещение излома

        Тип данных: double

        Значение свойства рассчитывается в процентах от длины выступа.

        ### Синтаксис Automation

        ```
        BreakOffset = iObject.BreakOffset       # Получить свойство(* )
        iObject.BreakOffset = BreakOffset       # Установить свойство (* )
        BreakOffset = iObject.GetBreakOffset()  # Получить свойство (**)
        iObject.SetBreakOffset( BreakOffset )   # Установить свойство (**)
        ```
        """
        ...
    @BreakOffset.setter
    def BreakOffset(self, value: float): ...

    @property
    def Length(self) -> float:  # iaxisjut_length.html
        """
        ## Length - Длина выступа

        Тип данных: double

        Свойство позволяет получить и установить координату центра дуги Xc.

        ### Синтаксис Automation

        ```
        Length = iObject.Length       # Получить свойство(* )
        iObject.Length = Length       # Установить свойство (* )
        Length = iObject.GetLength()  # Получить свойство (**)
        iObject.SetLength( Length )   # Установить свойство (**)
        ```
        """
        ...
    @Length.setter
    def Length(self, value: float): ...

    @property
    def MarkOffset(self) -> float:  # iaxisjut_markoffset.html
        """
        ## MarkOffset - Cмещение марки

        Тип данных: double

        Свойство позволяет получить и установить расстояние от марки до оси.

        ### Синтаксис Automation

        ```
        MarkOffset = iObject.MarkOffset       # Получить свойство(* )
        iObject.MarkOffset = MarkOffset       # Установить свойство (* )
        MarkOffset = iObject.GetMarkOffset()  # Получить свойство (**)
        iObject.SetMarkOffset( MarkOffset )   # Установить свойство (**)
        ```
        """
        ...
    @MarkOffset.setter
    def MarkOffset(self, value: float): ...

    @property
    def MarkOn(self) -> bool:  # iaxisjut_markon.html
        """
        ## MarkOn - Марка включена\\выключена

        Тип данных: BOOL

        Свойство позволяет управлять отображением марки на координационной оси.

        ### Синтаксис Automation

        ```
        MarkOn = iObject.MarkOn       # Получить свойство(* )
        iObject.MarkOn = MarkOn       # Установить свойство (* )
        MarkOn = iObject.GetMarkOn()  # Получить свойство (**)
        iObject.SetMarkOn( MarkOn )   # Установить свойство (**)
        ```
        """
        ...
    @MarkOn.setter
    def MarkOn(self, value: bool): ...




class IMarkNode(IKompasAPIObject):  # imarknode.html
    """
    ## Интерфейс IMarkNode

    Интерфейс узла для вставки дополнительных марок.

    Интерфейс можно получить с помощью методов IMarkNodes::Item, IMarkNodes::Add, ICircleAxis::BaseMarkNode.
    """

    @property
    def DoubleMark(self) -> bool:  # imarknode_doublemark.html
        """
        ## DoubleMark - Признак марки

        Тип данных: BOOL

        Значения свойства:

        +-------------------------------+
        | TRUE  | - двойная окружность, |
        +-------------------------------+
        | FALSE | - одинарная.          |
        +-------------------------------+

        Свойство используется для узлов типов ksMarkCirle и ksMarkRefCirle из перечисления ksMarkNodeEnum.

        ### Синтаксис Automation

        ```
        DoubleMark = iObject.DoubleMark       # Получить свойство(* )
        iObject.DoubleMark = DoubleMark       # Установить свойство (* )
        DoubleMark = iObject.GetDoubleMark()  # Получить свойство (**)
        iObject.SetDoubleMark( DoubleMark )   # Установить свойство (**)
        ```
        """
        ...
    @DoubleMark.setter
    def DoubleMark(self, value: bool): ...

    @property
    def MarkType(self) -> int:  # imarknode_marktype.html
        """
        ## MarkType - Тип марки

        Тип данных: тип марки из перечисления ksMarkNodeEnum

        Свойство позволяет получить и установить тип марки.

        ### Синтаксис Automation

        ```
        MarkType = iObject.MarkType       # Получить свойство(* )
        iObject.MarkType = MarkType       # Установить свойство (* )
        MarkType = iObject.GetMarkType()  # Получить свойство (**)
        iObject.SetMarkType( MarkType )   # Установить свойство (**)
        ```
        """
        ...
    @MarkType.setter
    def MarkType(self, value: int): ...

    @property
    def RefLength(self) -> float:  # imarknode_reflength.html
        """
        ## RefLength - Длина указателя

        Тип данных: double

        Свойство используется для узлов типа ksMarkRefCirle из перечисления ksMarkNodeEnum.

        ### Синтаксис Automation

        ```
        RefLength = iObject.RefLength       # Получить свойство(* )
        iObject.RefLength = RefLength       # Установить свойство (* )
        RefLength = iObject.GetRefLength()  # Получить свойство (**)
        iObject.SetRefLength( RefLength )   # Установить свойство (**)
        ```
        """
        ...
    @RefLength.setter
    def RefLength(self, value: float): ...

    @property
    def Text(self) -> IText:  # imarknode_text.html
        """
        ## Text - Текст

        Тип данных: указатель на интерфейс IText

        1. Свойство доступно только для чтения.

        2. Текст можно использовать для поиска узла марки в коллекции IMarkNodes.

        3. Для поиска в функцию IMarkNodes::Item нужно передать строковое значение текста.

        Пример:

        node = iMarkNodes.Item("A1");

        ### Синтаксис Automation

        ```
        Text = iObject.Text       # Получить свойство(* )
        Text = iObject.GetText()  # Получить свойство (**)
        ```
        """
        ...

    def Delete(self) -> bool:  # imarknode_delete.html
        """
        ## Delete - Удалить узел

        1. Метод позволяет удалить дополнительные узлы для марок.

        2. Из объекта узлы удалятся после вызова метода IDrawingObject::Update.

        ### Синтаксис Automation

        ```
        BOOL Delete();
        ```

        ### Возвращаемое значение

        +-----------------------------+
        | TRUE  | - в случае успеха,  |
        +-----------------------------+
        | FALSE | - в случае неудачи. |
        +-----------------------------+
        """
        ...

    def GetInsertionPoints(self, LeftJut) -> bool:  # imarknode_getinsertionpoints.html
        """
        ## GetInsertionPoints - Получить координаты точек для вставки дополнительных элементов

        1. Параметры Points, Directions не являются обязательными.

        2. В функцию достаточно передать один из указателей на VARIANT.

        3. Массивы являются согласованными.

        4. Координаты точек в массиве Points лежат в следующей последовательности: x0, y0, x1, y1, ...xi, yi.

        5. Направления в массиве Directions лежат в последовательности: angle0, angle1, ...anglei.

        6. Значения координат и направлений пересчитываются в зависимости от текущей системы координат для объекта, задаваемой параметром IDrawingObject::DrawingObjectParamType

        ### Синтаксис Automation

        ```
        BOOL GetInsertionPoints( BOOL LeftJut,
        VARIANT * Points,
        VARIANT * Directions );
        ```

        ### Входные параметры

        +-----------------------------------------------------------------------------------------------------------------------------------+
        | LeftJut | - TRUE координаты точек для левого ответвления, - FALSE - для правого. Для круглой оси нужно передавать LeftJut = TRUE. |
        +-----------------------------------------------------------------------------------------------------------------------------------+

        ### Выходные параметры

        +-------------------------------------------------------------------------------------------------------------------+
        | Points     | - массив SafeArray типа VT_ARRAY | VT_R8 координат точек для подключения дополнительных узлов марок, |
        +-------------------------------------------------------------------------------------------------------------------+
        | Directions | - массив SafeArray типа VT_ARRAY | VT_R8 направлений (углы относительно центра родительского узла).  |
        +-------------------------------------------------------------------------------------------------------------------+

        ### Возвращаемое значение

        +--------------------------------+
        | TRUE  | - координаты получены, |
        +--------------------------------+
        | FALSE | - в случае неудачи.    |
        +--------------------------------+
        """
        ...

    def MarkNodes(self, Left) -> IMarkNodes:  # imarknode_marknodes.html
        """
        ## MarkNodes - Коллекция узлов марок

        Тип данных: указатель на интерфейс IMarkNodes

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        MarkNodes = iObject.MarkNodes( Left )     # Получить свойство(* )
        MarkNodes = iObject.GetMarkNodes( Left )  # Получить свойство (**)
        ```

        ### Входные параметры

        +---------------------------------------------------------------------------------------------+
        | Left (BOOL) | TRUE - возвращается коллекция узлов слева по направлению оси, FALSE - справа. |
        +---------------------------------------------------------------------------------------------+
        """
        ...




class IParametriticConstraint(IKompasAPIObject):  # iparametriticconstraint.html
    """
    ## Интерфейс IParametriticConstraint

    Интерфейс параметрического ограничения.

    Интерфейс позволяет получить доступ к параметрическому ограничению в 2D документе.

    Изменение параметров ограничения возможно на этапе создания ограничения. Создать ограничение можно при помощи метода IDrawingObject1::NewConstraint.

    Если необходимо отредактировать параметрическое ограничение, то его нужно удалить и создать новое с нужными параметрами.

    1. Аналоги интерфейса в API5:

    API Экспортных функций:

    •Структура параметров: ConstraintParam.

    Функции:

    •ksSetObjConstraint - Установить параметрическое ограничение.

    •ksDestroyObjConstraint - Разрушить параметрическое ограничение.

    Automation:

    •ksConstraintParam - Интерфейс параметров для параметрических ограничений.

    Методы:

    •ksDocument2D::ksSetObjConstraint - Установить параметрическое ограничение.

    •ksDocument2D::ksDestroyObjConstraint - Разрушить параметрическое ограничение.

    2. В API5 нельзя создать или получить ограничения следующих типов:

    •Ассоциативная связь.

    •Размер с переменной.

    •Фиксированный размер.
    """

    @property
    def Axis(self) -> IDrawingObject:  # iparametriticconstraint_axis.html
        """
        ## Axis - Ось симметрии для ограничения "симметрия точек"

        Тип данных: указатель на интерфейс IDrawingObject

        Свойство позволяет задать ось симметрии для ограничения типа "симметрия точек". В качестве оси симметрии может использоваться отрезок или объект "прямая ось".

        ### Синтаксис Automation

        ```
        Axis = Object.Axis       # Получить свойство(* )
        Object.Axis = Axis       # Установить свойство (* )
        Axis = Object.GetAxis()  # Получить свойство (**)
        Object.SetAxis( Axis )   # Установить свойство (**)
        ```
        """
        ...
    @Axis.setter
    def Axis(self, value: IDrawingObject): ...

    @property
    def AxisSegmentIndex(self) -> int:  # iparametriticconstraint_axissegmentindex.html
        """
        ## AxisSegmentIndex - Индекс сегмента для оси

        Тип данных: long

        ### Синтаксис Automation

        ```
        AxisSegmentIndex = Object. AxisSegmentIndex       # Получить свойство(* )
        Object. AxisSegmentIndex = AxisSegmentIndex       # Установить свойство (* )
        AxisSegmentIndex = Object.Get AxisSegmentIndex()  # Получить свойство (**)
        Object.Set AxisSegmentIndex( AxisSegmentIndex )   # Установить свойство (**)
        ```
        """
        ...
    @AxisSegmentIndex.setter
    def AxisSegmentIndex(self, value: int): ...

    @property
    def BisectorVariant(self) -> int:  # iparametriticconstraint_bisectorvariant.html
        """
        ## BisectorVariant - Вариант решения биссектрисы

        Тип данных: из перечисления ksBisectorVariant

        Свойство имеет смысл только для ограничения с типом ksCBisector.

        ### Синтаксис Automation

        ```
        BisectorVariant = Object.BisectorVariant       # Получить свойство(* )
        Object.BisectorVariant = BisectorVariant       # Установить свойство (* )
        BisectorVariant = Object.GetBisectorVariant()  # Получить свойство (**)
        Object.SetBisectorVariant( BisectorVariant )   # Установить свойство (**)
        ```
        """
        ...
    @BisectorVariant.setter
    def BisectorVariant(self, value: int): ...

    @property
    def Comment(self) -> str:  # iparametriticconstraint_comment.html
        """
        ## Comment - Комментарий

        Тип данных: BSTR (строка)

        1. Свойство позволяет установить и получить комментарий. Используется только для ограничения "Размер с переменной" (ksCDimWithVariable). Комментарий можно установить для размеров при редактировании эскизов.

        2. Изменение параметра возможно на этапе создания ограничения.

        ### Синтаксис Automation

        ```
        Comment = iObject.Comment       # Получить свойство(* )
        iObject.Comment = Comment       # Установить свойство (* )
        Comment = iObject.GetComment()  # Получить свойство (**)
        iObject.SetComment(Comment)     # Установить свойство (**)
        ```
        """
        ...
    @Comment.setter
    def Comment(self, value: str): ...

    @property
    def ConstraintType(self) -> int:  # iparametriticconstraint_constrainttype.html
        """
        ## ConstraintType - Тип ограничения

        Тип данных: из перечисления ksConstraintTypeEnum

        1. Свойство позволяет установить и получить тип ограничения.

        2. Изменение параметра возможно на этапе создания ограничения.

        ### Синтаксис Automation

        ```
        ConstraintType = iObject.ConstraintType       # Получить свойство(* )
        iObject.ConstraintType = ConstraintType       # Установить свойство (* )
        ConstraintType = iObject.GetConstraintType()  # Получить свойство (**)
        iObject.SetConstraintType(ConstraintType)     # Установить свойство (**)
        ```
        """
        ...
    @ConstraintType.setter
    def ConstraintType(self, value: int): ...

    @property
    def Degrees(self) -> int:  # iparametriticconstraint_degrees.html
        """
        ## Degrees - Градусы

        Тип данных: long

        1. Свойство позволяет установить и получить составляющую "Градусы" для значения углового размера. Используется только для ограничения "Размер с переменной" (ksCDimWithVariable).

        2. Изменение параметра возможно на этапе создания ограничения.

        ### Синтаксис Automation

        ```
        Degrees = iObject.Degrees       # Получить свойство(* )
        iObject.Degrees = Degrees       # Установить свойство (* )
        Degrees = iObject.GetDegrees()  # Получить свойство (**)
        iObject.SetDegrees(Degrees)     # Установить свойство (**)
        ```
        """
        ...
    @Degrees.setter
    def Degrees(self, value: int): ...

    @property
    def Expression(self) -> str:  # iparametriticconstraint_expression.html
        """
        ## Expression - Выражение

        Тип данных: BSTR (строка)

        1. Свойство позволяет установить и получить выражение. Используется только для ограничения "Размер с переменной" (ksCDimWithVariable).

        2. Изменение параметра возможно на этапе создания ограничения.

        ### Синтаксис Automation

        ```
        Expression = iObject.Expression       # Получить свойство(* )
        iObject.Expression = Expression       # Установить свойство (* )
        Expression = iObject.GetExpression()  # Получить свойство (**)
        iObject.SetExpression(Expression)     # Установить свойство (**)
        ```
        """
        ...
    @Expression.setter
    def Expression(self, value: str): ...

    @property
    def Index(self) -> int:  # iparametriticconstraint_index.html
        """
        ## Index - Индекс точки на объекте (начинается с 0, у дуги и окружности 0-центр)

        Тип данных: long

        1. Свойство позволяет установить и получить индекс точки на данном графическом объекте. Применяется, например, для ограничения "Совпадение двух точек"(ksCMergePoints).

        2. Изменение параметра возможно на этапе создания ограничения.

        ### Синтаксис Automation

        ```
        Index = iObject.Index       # Получить свойство(* )
        iObject.Index = Index       # Установить свойство (* )
        Index = iObject.GetIndex()  # Получить свойство (**)
        iObject.SetIndex(Index)     # Установить свойство (**)
        ```
        """
        ...
    @Index.setter
    def Index(self, value: int): ...

    @property
    def Minutes(self) -> int:  # iparametriticconstraint_minutes.html
        """
        ## Minutes - Минуты

        Тип данных: long

        1. Свойство позволяет установить и получить составляющую "Минуты" для значения углового размера. Используется только для ограничения "Размер с переменной"(ksCDimWithVariable).

        2. Изменение параметра возможно на этапе создания ограничения.

        ### Синтаксис Automation

        ```
        Minutes = iObject.Minutes       # Получить свойство(* )
        iObject.Minutes = Minutes       # Установить свойство (* )
        Minutes = iObject.GetMinutes()  # Получить свойство (**)
        iObject.SetMinutes(Minutes)     # Установить свойство (**)
        ```
        """
        ...
    @Minutes.setter
    def Minutes(self, value: int): ...

    @property
    def Partner(self) -> list[IDispatch]:  # iparametriticconstraint_partner.html
        """
        ## Partner - Второй объект или массив SAFEARRAY объектов для установки ограничения

        Тип данных: VARIANT (VT_ARRAY | VT_DISPATCH) или VT_DISPATCH

        1. Свойство позволяет установить и получить второй объект. SAFEARRAY используется только для ограничения "Ассоциативная связь"(ksCAssociation). Например, для углового размера ассоциативная связь может быть установлена на два отрезка, между которыми установлен размер.

        2. Изменение параметра возможно на этапе создания ограничения.

        ### Синтаксис Automation

        ```
        Partner = iObject.Partner       # Получить свойство(* )
        iObject.Partner = Partner       # Установить свойство (* )
        Partner = iObject.GetPartner()  # Получить свойство (**)
        iObject.SetPartner(Partner)     # Установить свойство (**)
        ```
        """
        ...
    @Partner.setter
    def Partner(self, value: list[IDispatch]): ...

    @property
    def PartnerIndex(self) -> int:  # iparametriticconstraint_partnerindex.html
        """
        ## PartnerIndex - Индекс точки на втором объекте (начинается с 0, у дуги и окружности 0-центр)

        Тип данных: long

        Значения свойства:

        +-------------------------------+
        | TRUE  | - описание активно,   |
        +-------------------------------+
        | FALSE | - описание неактивно. |
        +-------------------------------+

        Свойство позволяет установить и получить индекс точки на втором графическом объекте, который используется для создания ограничения.

        Применяется, например, для ограничения "Совпадение двух точек"(ksCMergePoints).

        Изменение параметра возможно на этапе создания ограничения.

        ### Синтаксис Automation

        ```
        PartnerIndex = iObject.PartnerIndex       # Получить свойство(* )
        iObject.PartnerIndex = PartnerIndex       # Установить свойство (* )
        PartnerIndex = iObject.GetPartnerIndex()  # Получить свойство (**)
        iObject.SetPartnerIndex(PartnerIndex)     # Установить свойство (**)
        ```
        """
        ...
    @PartnerIndex.setter
    def PartnerIndex(self, value: int): ...

    @property
    def PartnerSegmentIndex(self) -> int:  # iparametriticconstraint_partnersegmentindex.html
        """
        ## PartnerSegmentIndex - Индекс сегмента партнера

        Тип данных: long

        ### Синтаксис Automation

        ```
        PartnerSegmentIndex = Object.PartnerSegmentIndex       # Получить свойство(* )
        Object.PartnerSegmentIndex = PartnerSegmentIndex       # Установить свойство (* )
        PartnerSegmentIndex = Object.GetPartnerSegmentIndex()  # Получить свойство (**)
        Object.SetPartnerSegmentIndex( PartnerSegmentIndex )   # Установить свойство (**)
        ```
        """
        ...
    @PartnerSegmentIndex.setter
    def PartnerSegmentIndex(self, value: int): ...

    @property
    def Seconds(self) -> float:  # iparametriticconstraint_seconds.html
        """
        ## Seconds - Секунды

        Тип данных: double

        1. Свойство позволяет установить и получить составляющую "Секунды" для значения угового размера. Используется только для ограничения "Размер с переменной"(ksCDimWithVariable).

        2. Изменение параметра возможно на этапе создания ограничения.

        ### Синтаксис Automation

        ```
        Seconds = iObject.Seconds       # Получить свойство(* )
        iObject.Seconds = Seconds       # Установить свойство (* )
        Seconds = iObject.GetSeconds()  # Получить свойство (**)
        iObject.SetSeconds(Seconds)     # Установить свойство (**)
        ```
        """
        ...
    @Seconds.setter
    def Seconds(self, value: float): ...

    @property
    def SegmentIndex(self) -> int:  # iparametriticconstraint_segmentindex.html
        """
        ## SegmentIndex - Индекс сегмента объекта

        Тип данных: long

        ### Синтаксис Automation

        ```
        SegmentIndex = Object.SegmentIndex       # Получить свойство(* )
        Object.SegmentIndex = SegmentIndex       # Установить свойство (* )
        SegmentIndex = Object.GetSegmentIndex()  # Получить свойство (**)
        Object.SetSegmentIndex( SegmentIndex )   # Установить свойство (**)
        ```
        """
        ...
    @SegmentIndex.setter
    def SegmentIndex(self, value: int): ...

    @property
    def Valid(self) -> bool:  # iparametriticconstraint_valid.html
        """
        ## Valid - Действие ограничения

        Тип данных: BOOL

        Значения свойства:

        +--------------------------------------------------------+
        | TRUE  | - параметрическое ограничение действительно,   |
        +--------------------------------------------------------+
        | FALSE | - параметрическое ограничение недействительно. |
        +--------------------------------------------------------+

        Свойство позволяет определить действительно ограничение или нет.

        ### Синтаксис Automation

        ```
        Valid = iObject.Valid       # Получить свойство(* )
        Valid = iObject.GetValid()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def Value(self) -> float:  # iparametriticconstraint_value.html
        """
        ## Value - Значение

        Тип данных: double

        1. Свойство позволяет установить и получить значение. Используется только для ограничения "Размер с переменной" (ksCDimWithVariable).

        Установить значение можно, если на объекте есть ограничение "Фиксированный размер"(ksCFixedDim).

        2. Размер не является информационным.

        3. Изменение параметра возможно на этапе создания ограничения.

        ### Синтаксис Automation

        ```
        Value = iObject.Value       # Получить свойство(* )
        iObject.Value = Value       # Установить свойство (* )
        Value = iObject.GetValue()  # Получить свойство (**)
        iObject.SetValue(Value)     # Установить свойство (**)
        ```
        """
        ...
    @Value.setter
    def Value(self, value: float): ...

    @property
    def Variable(self) -> str:  # iparametriticconstraint_variable.html
        """
        ## Variable - Имя переменной

        Тип данных: BSTR (строка)

        1. Свойство позволяет установить и получить имя переменной. Используется только для ограничения "Размер с переменной" (ksCDimWithVariable).

        2. При создании ограничения ksCDimWithVariable автоматически присваивается уникальное имя переменной. При установке своего имени нужно следить за уникальностью имени самостоятельно. Иначе ограничение не создастся и взведется ошибка "Ограничение создать нельзя. Недопустимое имя переменной."

        3. Изменение параметра возможно на этапе создания ограничения.

        ### Синтаксис Automation

        ```
        Variable = iObject.Variable       # Получить свойство(* )
        iObject.Variable = Variable       # Установить свойство (* )
        Variable = iObject.GetVariable()  # Получить свойство (**)
        iObject.SetVariable(Variable)     # Установить свойство (**)
        ```
        """
        ...
    @Variable.setter
    def Variable(self, value: str): ...

    def Create(self) -> bool:  # iparametriticconstraint_create.html
        """
        ## Create - Создать ограничение в модели

        1. Если метод выполнился успешно, ограничение появится в модели и вступит в силу. Значение свойства IParametriticConstraint::Valid становится равным TRUE (ограничение недействительно). Если ограничение в модели создать нельзя, метод Create вернет FALSE.

        2. В процессе создания ограничения могут быть взведены ошибки из перечисления ErrorType:

        •Значение выходит за границы диапазона.

        •Значение размера выходит за границы диапазона 30'' - 359°59'30''.

        •Значение размера выходит за границы диапазона 0.5'' - 359°59'59.5''.

        •Значение размера выходит за границы диапазона 30' - 359°30'.

        •Ограничение создать нельзя. Данный тип размера не параметризуется.

        •Ограничение создать нельзя. Нет информации о привязке.

        •Ограничение создать нельзя.

        •Ограничение такого типа уже существует.

        •Ограничение создать нельзя. Недопустимое имя переменной.

        •Ограничение создать нельзя. Недопустимое значение размера.

        •Ограничение создать нельзя. Размер может быть только информационным.

        ### Синтаксис Automation

        ```
        BOOL Create();
        ```

        ### Возвращаемое значение

        +------------------------------------------------+
        | TRUE  | - параметрическое ограничение создано, |
        +------------------------------------------------+
        | FALSE | - в случае неудачи.                    |
        +------------------------------------------------+
        """
        ...

    def Delete(self) -> bool:  # iparametriticconstraint_delete.html
        """
        ## Delete - Удалить ограничение

        Метод удаляет описание ограничение. Значение свойства IParametriticConstraint::Valid становится равным FALSE (ограничение недействительно).

        ### Синтаксис Automation

        ```
        BOOL Delete();
        ```

        ### Возвращаемое значение

        +------------------------------------------------+
        | TRUE  | - параметрическое ограничение удалено, |
        +------------------------------------------------+
        | FALSE | - в случае неудачи.                    |
        +------------------------------------------------+
        """
        ...




class IDrawingObject1(IDispatch):  # idrawingobject1.html
    """
    ## Интерфейс IDrawingObject1

    Дополнительный интерфейс для графических объектов.

    1. Дополнительный интерфейс для графических объектов. Данный интерфейс можно получить у интерфейса графического объекта IDrawingObject посредством вызова метода IUnknown::QueryInterface (const GUID far& IID, void** pif).

    2. Интерфейс позволяет выполнить следующие действия:

    •получить массив ограничений для данного графического объекта,

    •добавить новое ограничение,

    •ассоциировать данный графический объект с другими объектами,

    •удалить все ограничения.
    """

    @property
    def AutoTransparentBackground(self) -> bool:  # idrawingobject1_autotransparentbackground.html
        """
        ## AutoTransparentBackground - Включение/отключение управления очисткой фона под объектом через настройку документа

        Тип данных: BOOL

        Значение свойства:

        +-------------------------------------------------------------------------+
        | TRUE  | - включено управление очисткой фона через настройку документа,  |
        +-------------------------------------------------------------------------+
        | FALSE | - отключено управление очисткой фона через настройку документа. |
        +-------------------------------------------------------------------------+

        Позволяет включить/отключить признак подчинения очистки фона под аннотационными объектами DrawingObject1::TransparentBackground настройкам документа.

        ### Синтаксис Automation

        ```
        AutoTransparentBackground = Object.AutoTransparentBackground       # Получить свойство(* )
        Object.AutoTransparentBackground = AutoTransparentBackground       # Установить свойство (* )
        AutoTransparentBackground = Object.GetAutoTransparentBackground()  # Получить свойство (**)
        Object.SetAutoTransparentBackground( AutoTransparentBackground )   # Установить свойство (**)
        ```
        """
        ...
    @AutoTransparentBackground.setter
    def AutoTransparentBackground(self, value: bool): ...

    @property
    def Constraints(self) -> list[IDispatch]:  # idrawingobject1_constraints.html
        """
        ## Constraints - Массив SAFEARRAY ограничений для данного объекта

        Аналоги метода в API5: экспортная функция ksGetObjConstraints и метод ksDocument2D::ksGetObjConstraints. Тип данных:VARIANT ( VT_ARRAY|VT_DISPATCH ).

        Свойство позволяет получить массив SAFEARRAY ограничений для данного объекта. Это массив LPDISPATCH, который можно преобразовать в интерфейсы IParametriticConstraint. Если возвращается один объект, то тип VARIANT-а будет VT_DISPATCH, если возвращается несколько объектов, то тип VARIANT-а будет VT_ARRAY | VT_DISPATCH.

        ### Синтаксис Automation

        ```
        Constraints = iObject.Constraints       # Получить свойство(* )
        Constraints = iObject.GetConstraints()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def ConstraintsState(self) -> int:  # constraintsstate.html
        """
        ## ConstraintsState - Параметрическое состояние эскиза

        Тип данных: из перечисления ksObjectConstraintsStateEnum

        Свойство доступно только для чтения.

        Версия КОМПАС v22

        ### Синтаксис Automation

        ```
        ConstraintsState = Object.ConstraintsState       # Получить свойство(* )
        ConstraintsState = Object.GetConstraintsState()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def Id(self):  # idrawingobject1_id.html
        """
        ## Id - Идентификатор объекта

        Тип данных: int64

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        Id = Object.Id       # Получить свойство(* )
        Id = Object.GetId()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def IsAnnotativeObject(self) -> bool:  # idrawingobject1_isannotativeobject.html
        """
        ## IsAnnotativeObject - Признак аннотационного объекта

        Тип данных: BOOL

        Позволяет получить признак аннотационного объекта.

        Различаются геометрические, аннотационные и составные объекты.

        См. также IDrawingObject1::IsGeometryObject.

        ### Синтаксис Automation

        ```
        IsAnnotativeObject = Object.IsAnnotativeObject       # Получить свойство(* )
        IsAnnotativeObject = Object.GetIsAnnotativeObject()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def IsCurve(self) -> bool:  # idrawingobject1_iscurve.html
        """
        ## IsCurve - Объект является кривой

        Тип данных: BOOL

        Значение свойства:

        +----------------------------------+
        | TRUE | - объект является кривой. |
        +----------------------------------+

        Свойство позволяет получить признак того, что объект является кривой.

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        IsCurve = Object.IsCurve       # Получить свойство(* )
        IsCurve = Object.GetIsCurve()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def IsGeometryObject(self) -> bool:  # idrawingobject1_isgeometryobject.html
        """
        ## IsGeometryObject - Признак геометрического объекта

        Тип данных: BOOL

        Позволяет получить признак геометрического объекта.

        Различаются геометрические, аннотационные и составные объекты.

        См. также IDrawingObject1::IsAnnotativeObject.

        ### Синтаксис Automation

        ```
        IsGeometryObject = Object.IsGeometryObject       # Получить свойство(* )
        IsGeometryObject = Object.GetIsGeometryObject()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def IsInAssociationView(self) -> bool:  # idrawingobject1_isinassociationview.html
        """
        ## IsInAssociationView - Объект является ассоциативным

        Тип данных: BOOL

        Свойство доступно только для чтения.

        Версия: Компас v20

        ### Синтаксис Automation

        ```
        IsInAssociationView = Object.IsInAssociationView       # Получить свойство(* )
        IsInAssociationView = Object.GetIsInAssociationView()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def IsVisibleInAssociationView(self) -> bool:  # idrawingobject1_isvisibleinassociationview.html
        """
        ## IsVisibleInAssociationView - Получить признак видимости ассоциативного объекта на ассоциативном виде

        Тип данных: BOOL

        Свойство доступно только для чтения.

        Версия: Компас v20

        ### Синтаксис Automation

        ```
        IsVisibleInAssociationView = Object.IsVisibleInAssociationView       # Получить свойство(* )
        IsVisibleInAssociationView = Object.GetIsVisibleInAssociationView()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def TransparentBackground(self) -> bool:  # idrawingobject1_transparentbackground.html
        """
        ## TransparentBackground - Включение/отключение очистки фона под аннотационным объектом

        Тип данных: BOOL

        Значение свойства:

        +------------------------------------+
        | TRUE  | - включение очистки фона,  |
        +------------------------------------+
        | FALSE | - отключение очистки фона. |
        +------------------------------------+

        Позволяет включить или отключить очистку фона под аннотационными объектами.

        ### Синтаксис Automation

        ```
        ShelfPoints = Object.ShelfPoints       # Получить свойство(* )
        Object.ShelfPoints = ShelfPoints       # Установить свойство (* )
        ShelfPoints = Object.GetShelfPoints()  # Получить свойство (**)
        Object.SetShelfPoints( ShelfPoints )   # Установить свойство (**)
        ```
        """
        ...
    @TransparentBackground.setter
    def TransparentBackground(self, value: bool): ...

    def Associate(self) -> bool:  # idrawingobject1_associate.html
        """
        ## Associate - Ассоциировать данный объект с другими объектами

        1. Данный метод позволяет автоматизировать процесс наложения ассоциативных связей, если они возможны.

        2. Существует также альтернативный вариант наложения ассоциативных связей - ручной, с использованием метода IDrawingObject1::NewConstraint. В этом случае пользователь сам несет ответственность за правильность заполнения параметров ограничения и за полноту ассоциативных связей.

        3. В некоторых случаях, как например, на рисунке ниже, в результате использования данного метода при создании ассоциативного размера, с т.2 размера может быть ассоциирована как конечная точка вертикального отрезка, так и средняя точка горизонтального отрезка. В подобных ситуациях для обеспечения ассоциации точек размера с конкретными точками нужных объектов, требуется явно создавать новое ограничение через метод IDrawingObject1::NewConstraint.

        Пример создания ограничения через метод IDrawingObject1::NewConstraint...

        ### Синтаксис Automation

        ```
        BOOL Associate();
        ```

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def DeleteConstraints(self) -> bool:  # idrawingobject1_deleteconstraints.html
        """
        ## DeleteConstraints - Удалить все ограничения

        Метод позволяет удалить все ограничения для данного графического объекта.

        ### Синтаксис Automation

        ```
        BOOL DeleteConstraints();
        ```

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def DeleteHyperLink(self) -> bool:  # idrawingobject1_deletehyperlink.html
        """
        ## DeleteHyperLink - Удалить гиперссылку

        ### Синтаксис Automation

        ```
        BOOL DeleteHyperLink();
        ```

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def GetCurve2D(self) -> ICurve2D:  # idrawingobject1_getcurve2d.html
        """
        ## GetCurve2D - Получить математическую кривую

        ### Синтаксис Automation

        ```
        LPDISPATCH GetCurve2D()
        ```

        ### Возвращаемое значение

        - указатель на интерфейс ICurve2D
        """
        ...

    def GetHyperLinkParam(self):  # idrawingobject1_gethyperlinkparam.html
        """
        ## GetHyperLinkParam - Получить параметры гиперссылки

        ### Синтаксис Automation

        ```
        BSTR GetHyperLinkParam( ksHyperLinkTypeEnum * Type,
        IDrawingObject ** LinkObject,
        long * Level );
        ```

        ### Возвращаемое значение

        текст гиперссылки.

        +---------------------------------------------------+
        | Type       | - тип ссылки,                        |
        +---------------------------------------------------+
        | LinkObject | - объект, на который сделана ссылка, |
        +---------------------------------------------------+
        | Level      | - уровень.                           |
        +---------------------------------------------------+
        """
        ...

    def NewConstraint(self) -> IParametriticConstraint:  # idrawingobject1_newconstraint.html
        """
        ## NewConstraint - Создать новое ограничение

        Метод создает новое параметрическое ограничение. Для изменения параметров ограничения, необходимо задать его свойства. Ограничение появится в модели и вступит в силу после вызова метода IParametriticConstraint::Create.

        ### Синтаксис Automation

        ```
        LPDISPATCH NewConstraint();
        ```

        ### Возвращаемое значение

        указатель на интерфейс IParametriticConstraint параметрического ограничения
        """
        ...




class IAxisLineParam(IKompasAPIObject):  # iaxislineparam.html
    """
    ## Интерфейс IAxisLineParam

    Параметры осевой линии или обозначения центра.

    Интерфейс позволяет задавать и получать параметры осевой линии.

    Интерфейс является дополнительным к интерфейсу осевой линии IAxisLine и интерфейсу обозначения центра ICentreMarker.
    """

    @property
    def AutoDetectedDash(self) -> bool:  # iaxislineparam_autodetecteddash.html
        """
        ## AutoDetectedDash - Ручное/автоматическое задание длины штриха

        Тип данных: BOOL

        Свойство позволяет устанавливать и получать признак автоматического задания длины штриха осевой линии.

        ### Синтаксис Automation

        ```
        AutoDetectedDash = Object.AutoDetectedDash       # Получить свойство(* )
        Object.AutoDetectedDash = AutoDetectedDash       # Установить свойство (* )
        AutoDetectedDash = Object.GetAutoDetectedDash()  # Получить свойство (**)
        Object.SetAutoDetectedDash( AutoDetectedDash )   # Установить свойство (**)
        ```
        """
        ...
    @AutoDetectedDash.setter
    def AutoDetectedDash(self, value: bool): ...

    @property
    def AutoDetectedDashModify(self) -> bool:  # iaxislineparam_autodetecteddashmodify.html
        """
        ## AutoDetectedDashModify - Использовать параметр Задание длины штриха из настроек документа

        Тип данных: BOOL

        Свойство позволяет устанавливать и получать признак использования параметра Задание длины штриха из настроек документа.

        ### Синтаксис Automation

        ```
        AutoDetectedDashModify = Object.AutoDetectedDashModify       # Получить свойство(* )
        Object.AutoDetectedDashModify = AutoDetectedDashModify       # Установить свойство (* )
        AutoDetectedDashModify = Object.GetAutoDetectedDashModify()  # Получить свойство (**)
        Object.SetAutoDetectedDashModify( AutoDetectedDashModify )   # Установить свойство (**)
        ```
        """
        ...
    @AutoDetectedDashModify.setter
    def AutoDetectedDashModify(self, value: bool): ...

    @property
    def DashMaxLength(self) -> float:  # iaxislineparam_dashmaxlength.html
        """
        ## DashMaxLength - Максимальная длина штриха

        Тип данных: double

        Свойство позволяет устанавливать и получать максимальную длину штриха осевой линии.

        ### Синтаксис Automation

        ```
        DashMaxLength = Object.DashMaxLength       # Получить свойство(* )
        Object.DashMaxLength = DashMaxLength       # Установить свойство (* )
        DashMaxLength = Object.GetDashMaxLength()  # Получить свойство (**)
        Object.SetDashMaxLength( DashMaxLength )   # Установить свойство (**)
        ```
        """
        ...
    @DashMaxLength.setter
    def DashMaxLength(self, value: float): ...

    @property
    def DashMaxLengthModify(self) -> bool:  # iaxislineparam_dashmaxlengthmodify.html
        """
        ## DashMaxLengthModify - Использовать параметр Максимальная длина штриха из настроек документа

        Тип данных: BOOL

        Свойство позволяет устанавливать и получать признак использования параметра Максимальная длина штриха из настроек документа.

        ### Синтаксис Automation

        ```
        DashMaxLengthModify = Object.DashMaxLengthModify       # Получить свойство(* )
        Object.DashMaxLengthModify = DashMaxLengthModify       # Установить свойство (* )
        DashMaxLengthModify = Object.GetDashMaxLengthModify()  # Получить свойство (**)
        Object.SetDashMaxLengthModify( DashMaxLengthModify )   # Установить свойство (**)
        ```
        """
        ...
    @DashMaxLengthModify.setter
    def DashMaxLengthModify(self, value: bool): ...

    @property
    def DottedLength(self) -> float:  # iaxislineparam_dottedlength.html
        """
        ## DottedLength - Длина пунктира

        Тип данных: double

        Свойство позволяет устанавливать и получать длину пунктира осевой линии.

        ### Синтаксис Automation

        ```
        DottedLength = Object.DottedLength       # Получить свойство(* )
        Object.DottedLength = DottedLength       # Установить свойство (* )
        DottedLength = Object.GetDottedLength()  # Получить свойство (**)
        Object.SetDottedLength( DottedLength )   # Установить свойство (**)
        ```
        """
        ...
    @DottedLength.setter
    def DottedLength(self, value: float): ...

    @property
    def DottedLengthModify(self) -> bool:  # iaxislineparam_dottedlengthmodify.html
        """
        ## DottedLengthModify - Использовать параметр Длина пунктира из настроек документа

        Тип данных: BOOL

        Свойство позволяет устанавливать и получать признак использования параметра Длина пунктира из настроек документа.

        ### Синтаксис Automation

        ```
        DottedLengthModify = Object.DottedLengthModify       # Получить свойство(* )
        Object.DottedLengthModify = DottedLengthModify       # Установить свойство (* )
        DottedLengthModify = Object.GetDottedLengthModify()  # Получить свойство (**)
        Object.SetDottedLengthModify( DottedLengthModify )   # Установить свойство (**)
        ```
        """
        ...
    @DottedLengthModify.setter
    def DottedLengthModify(self, value: bool): ...

    @property
    def Interval(self) -> float:  # iaxislineparam_interval.html
        """
        ## Interval - Промежуток

        Тип данных: double

        Свойство позволяет устанавливать и получать длину промежутка между штрихами пунктира осевой линии.

        ### Синтаксис Automation

        ```
        Interval = Object.Interval       # Получить свойство(* )
        Object.Interval = Interval       # Установить свойство (* )
        Interval = Object.GetInterval()  # Получить свойство (**)
        Object.SetInterval( Interval )   # Установить свойство (**)
        ```
        """
        ...
    @Interval.setter
    def Interval(self, value: float): ...

    @property
    def IntervalModify(self) -> bool:  # iaxislineparam_intervalmodify.html
        """
        ## IntervalModify - Использовать параметр Промежуток из настроек документа

        Тип данных: BOOL

        Свойство позволяет устанавливать и получать признак использования параметра Промежуток из настроек документа.

        ### Синтаксис Automation

        ```
        IntervalModify = Object.IntervalModify       # Получить свойство(* )
        Object.IntervalModify = IntervalModify       # Установить свойство (* )
        IntervalModify = Object.GetIntervalModify()  # Получить свойство (**)
        Object.SetIntervalModify( IntervalModify )   # Установить свойство (**)
        ```
        """
        ...
    @IntervalModify.setter
    def IntervalModify(self, value: bool): ...

    @property
    def JutLength(self) -> float:  # iaxislineparam_jutlength.html
        """
        ## JutLength - Выступание осевой линии

        Тип данных: double

        Свойство позволяет устанавливать и получать выступание осевой линии.

        ### Синтаксис Automation

        ```
        JutLength = Object.JutLength       # Получить свойство(* )
        Object.JutLength = JutLength       # Установить свойство (* )
        JutLength = Object.GetJutLength()  # Получить свойство (**)
        Object.SetJutLength( JutLength )   # Установить свойство (**)
        ```
        """
        ...
    @JutLength.setter
    def JutLength(self, value: float): ...

    @property
    def JutLength1(self) -> float:  # iaxislineparam_jutlength1.html
        """
        ## JutLength1 - Выступ осевой 1

        Тип данных: double

        ### Синтаксис Automation

        ```
        JutLength1 = Object.JutLength1       # Получить свойство(* )
        Object.JutLength1 = JutLength1       # Установить свойство (* )
        JutLength1 = Object.GetJutLength1()  # Получить свойство (**)
        Object.SetJutLength1( JutLength1 )   # Установить свойство (**)
        ```
        """
        ...
    @JutLength1.setter
    def JutLength1(self, value: float): ...

    @property
    def JutLength2(self) -> float:  # iaxislineparam_jutlength2.html
        """
        ## JutLength2 - Выступ осевой 2

        Тип данных: double

        ### Синтаксис Automation

        ```
        JutLength2 = Object.JutLength2       # Получить свойство(* )
        Object.JutLength2 = JutLength2       # Установить свойство (* )
        JutLength2 = Object.GetJutLength2()  # Получить свойство (**)
        Object.SetJutLength2( JutLength2 )   # Установить свойство (**)
        ```
        """
        ...
    @JutLength2.setter
    def JutLength2(self, value: float): ...

    @property
    def JutLengthModify(self) -> bool:  # iaxislineparam_jutlengthmodify.html
        """
        ## JutLengthModify - Использовать параметр Выступание осевой из настроек документа

        Тип данных: BOOL

        Свойство позволяет устанавливать и получать признак использования параметра Выступание осевой из настроек документа.

        ### Синтаксис Automation

        ```
        JutLengthModify = Object.JutLengthModify       # Получить свойство(* )
        Object.JutLengthModify = JutLengthModify       # Установить свойство (* )
        JutLengthModify = Object.GetJutLengthModify()  # Получить свойство (**)
        Object.SetJutLengthModify( JutLengthModify )   # Установить свойство (**)
        ```
        """
        ...
    @JutLengthModify.setter
    def JutLengthModify(self, value: bool): ...




class IBreakViewParam(IDispatch):  # ibreakviewparam.html
    """
    ## Интерфейс IBreakViewParam

    Интерфейс параметров разрыва вида.

    Интерфейс позволяет задавать и получать параметры разрыва вида.

    Интерфейс является дополнительным к интерфейсу вида IView для видов всех типов, кроме системного.
    """

    @property
    def BreaksCount(self) -> int:  # ibreakviewparam_breakscount.html
        """
        ## BreaksCount - Количество линий разрыва

        Тип данных: long

        1. Свойство позволяет получать количество линий разрыва.

        2. Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        BreaksCount = Object.BreaksCount       # Получить свойство(* )
        BreaksCount = Object.GetBreaksCount()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def BreaksVisible(self) -> bool:  # ibreakviewparam_breaksvisible.html
        """
        ## Breaksvisible - Отображение линий разрыва

        Тип данных: BOOL

        Свойство позволяет устанавливать и получать признак отображения разрывов.

        ### Синтаксис Automation

        ```
        BreaksVisible = Object.BreaksVisible       # Получить свойство(* )
        Object.BreaksVisible = BreaksVisible       # Установить свойство (* )
        BreaksVisible = Object.GetBreaksVisible()  # Получить свойство (**)
        Object.SetBreaksVisible( BreaksVisible )   # Установить свойство (**)
        ```
        """
        ...
    @BreaksVisible.setter
    def BreaksVisible(self, value: bool): ...

    def AddBreakLine(self, X1, Y1, X2, Y2, Angle):  # ibreakviewparam_addbreakline.html
        """
        ## AddBreakLine - Добавить линию разрыва

        Метод позволяет добавить линию разрыва вида.

        Угол первого разрыва может быть произвольным. Последующие разрывы должны быть параллельны или перпендикулярны первому.

        Например, в виде создан разрыв, направление сдвига которого расположено под углом 30 градусов к оси Х. Впоследствии в этом виде можно будет создать разрывы, направления сдвига которых располагаются либо под таким же углом, либо под углом 120 градусов к оси Х.

        ### Синтаксис Automation

        ```
        long AddBreakLine( double X1,
        double Y1,
        double X2,
        double Y2,
        double angle )
        ```

        ### Входные параметры

        +------------------------------------------------+
        | X1, Y1, X2, Y2 | - координаты границы разрыва, |
        +------------------------------------------------+
        | angle          | - угол.                       |
        +------------------------------------------------+

        ### Возвращаемое значение

        - индекс линии разрыва.
        """
        ...

    def DeleteAllBreakLines(self) -> bool:  # ibreakviewparam_deleteallbreaklines.html
        """
        ## DeleteAllBreakLines - Удалить все линии разрыва

        Метод позволяет удалить все линии разрыва вида.

        ### Синтаксис Automation

        ```
        BOOL DeleteAllBreakLines()
        ```

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def DeleteBreakLine(self, Index) -> bool:  # ibreakviewparam_deletebreakline.html
        """
        ## DeleteBreakLine - Удалить линию разрыва

        Метод позволяет удалить линию разрыва вида с заданным индексом.

        ### Синтаксис Automation

        ```
        BOOL DeleteBreakLine( long Index )
        ```

        ### Входные параметры

        +-------------------------------------------+
        | Index | - индекс удаляемой линии разрыва. |
        +-------------------------------------------+

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def GetBreakLineParams(self, Index) -> bool:  # ibreakviewparam_getbreaklineparams.html
        """
        ## GetBreakLineParams - Получить параметры линии разрыва

        Метод позволяет получать параметры границ разрыва.

        ### Синтаксис Automation

        ```
        BOOL SetBreakLineParams( long Index,
        double Angle,
        double Clearance,
        long BreakLineType,
        double Amplitude,
        double MaxAmplitude )
        ```

        ### Входные параметры

        +---------------------------------+
        | Index | - индекс линии разрыва, |
        +---------------------------------+

        ### Выходные параметры

        +----------------------------------------------+
        | Angle         | - угол, кратный 90 градусам, |
        +----------------------------------------------+
        | Clearance     | - зазор,                     |
        +----------------------------------------------+
        | BreakLineType | - тип линии разрыва,         |
        +----------------------------------------------+
        | Amplitude     | - амплитуда,                 |
        +----------------------------------------------+
        | MaxAmplitude  | - максимальная амплитуда.    |
        +----------------------------------------------+

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def GetBreakLinePosition(self, Index) -> bool:  # ibreakviewparam_getbreaklineposition.html
        """
        ## GetBreakLinePosition - Получить координаты границ разрыва

        Метод позволяет получать координаты границ разрыва.

        ### Синтаксис Automation

        ```
        BOOL GetBreakLinePosition( long Index,
        double * X1,
        double * Y1,
        double * X2,
        double * Y2 )
        ```

        ### Входные параметры

        +---------------------------------+
        | Index | - индекс линии разрыва. |
        +---------------------------------+

        ### Выходные параметры

        +------------------------------------------------+
        | X1, Y1, X2, Y2 | - координаты границы разрыва. |
        +------------------------------------------------+

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def SetBreakLineParams(self, Index, Angle, Clearance, BreakLineType, Amplitude, MaxAmplitude) -> bool:  # ibreakviewparam_setbreaklineparams.html
        """
        ## SetBreakLineParams - Установить параметры линии разрыва

        Метод позволяет установить параметры границ разрыва.

        ### Синтаксис Automation

        ```
        BOOL SetBreakLineParams( long Index,
        double Angle,
        double Clearance,
        long BreakLineType,
        double Amplitude,
        double MaxAmplitude )
        ```

        ### Входные параметры

        +----------------------------------------------+
        | Index         | - индекс линии разрыва,      |
        +----------------------------------------------+
        | Angle         | - угол, кратный 90 градусам, |
        +----------------------------------------------+
        | Clearance     | - зазор,                     |
        +----------------------------------------------+
        | BreakLineType | - тип линии разрыва,         |
        +----------------------------------------------+
        | Amplitude     | - амплитуда,                 |
        +----------------------------------------------+
        | MaxAmplitude  | - максимальная амплитуда.    |
        +----------------------------------------------+

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def SetBreakLinePosition(self, Index, X1, Y1, X2, Y2) -> bool:  # ibreakviewparam_setbreaklineposition.html
        """
        ## SetBreakLinePosition - Установить координаты границ разрыва

        Метод позволяет установить координаты границ разрыва.

        ### Синтаксис Automation

        ```
        BOOL SetBreakLinePosition( long Index,
        double X1,
        double Y1,
        double X2,
        double Y2 )
        ```

        ### Входные параметры

        +------------------------------------------------+
        | Index          | - индекс линии разрыва,       |
        +------------------------------------------------+
        | X1, Y1, X2, Y2 | - координаты границы разрыва. |
        +------------------------------------------------+

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...




class ICopyObjectParam1(IDispatch):  # icopyobjectparam1.html
    """
    ## Интерфейс ICopyObjectParam1

    Интерфейс дополнительных параметров копирования объектов

    Интерфейс является дополнительным для ICopyObjectParam.
    """

    @property
    def HyperLinksCopy(self) -> bool:  # icopyobjectparam1_hyperlinkscopy.html
        """
        ## HyperLinksCopy - Копировать ссылки

        Тип данных: BOOL

        Значения свойства:

        +---------------------------------+
        | TRUE  | - копировать ссылки,    |
        +---------------------------------+
        | FALSE | - не копировать ссылки. |
        +---------------------------------+

        ### Синтаксис Automation

        ```
        HyperLinksCopy = Object.HyperLinksCopy       # Получить свойство(* )
        Object.HyperLinksCopy = HyperLinksCopy       # Установить свойство (* )
        HyperLinksCopy = Object.GetHyperLinksCopy()  # Получить свойство (**)
        Object.SetHyperLinksCopy( HyperLinksCopy )   # Установить свойство (**)
        ```
        """
        ...
    @HyperLinksCopy.setter
    def HyperLinksCopy(self, value: bool): ...

    @property
    def StoragesCopy(self):
        ...
    @StoragesCopy.setter
    def StoragesCopy(self, value): ...




class ICutViewParam(IDispatch):  # icutviewparam.html
    """
    ## Интерфейс ICutViewParam

    Интерфейс параметров разреза вида.

    Интерфейс позволяет задавать и получать параметры разреза вида.

    Интерфейс является дополнительным к интерфейсу вида IView для видов всех типов, кроме системного.
    """

    @property
    def CutsCount(self) -> int:  # icutviewparam_cutscount.html
        """
        ## CutsCount - Количество разрезов

        Тип данных: long

        1. Свойство позволяет получать количество разрезов.

        2. Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        CutsCount = Object.CutsCount       # Получить свойство(* )
        CutsCount = Object.GetCutsCount()  # Получить свойство (**)
        ```
        """
        ...

    def AddCut(self, Name, Number, X, Y, ModelCut, Contour, View):  # icutviewparam_addcut.html
        """
        ## AddCut - Добавить разрез

        Метод позволяет добавить разрез.

        ### Синтаксис Automation

        ```
        long AddCut( BSTR Name,
        long Number,
        double X,
        double Y,
        BOOL ModelCut,
        LPDISPATCH * Contour,
        LPDISPATCH * View )
        ```

        ### Входные параметры

        +-------------------------------------------------------------------------------------------------------+
        | Name     | - имя разреза,                                                                             |
        +-------------------------------------------------------------------------------------------------------+
        | Number   | - номер разреза,                                                                           |
        +-------------------------------------------------------------------------------------------------------+
        | X, Y     | - координаты точки, через которую пройдет секущая плоскость,                               |
        +-------------------------------------------------------------------------------------------------------+
        | ModelCut | - признак разреза, TRUE - разрез, FALSE - сечение,                                         |
        +-------------------------------------------------------------------------------------------------------+
        | Contour  | - контур, ограничивающий разрез,                                                           |
        +-------------------------------------------------------------------------------------------------------+
        | View     | - опорный вид для разреза (проекционный вид для базового вида, в котором создается вырез). |
        +-------------------------------------------------------------------------------------------------------+

        ### Возвращаемое значение

        - Индекс разреза.
        """
        ...

    def DeleteAllCuts(self) -> bool:  # icutviewparam_deleteallcuts.html
        """
        ## DeleteAllCuts - Удалить все разрезы

        Метод позволяет удалить все разрезы.

        ### Синтаксис Automation

        ```
        BOOL DeleteAllCuts()
        ```

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def DeleteCut(self, Index) -> bool:  # icutviewparam_deletecut.html
        """
        ## DeleteCut - Удалить разрез

        Метод позволяет удалить разрез с заданным индексом.

        ### Синтаксис Automation

        ```
        BOOL DeleteCut( long Index )
        ```

        ### Входные параметры

        +--------------------------------------+
        | Index | - индекс удаляемого разреза. |
        +--------------------------------------+

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def GetCutParams(self, Index):  # icutviewparam_getcutparams.html
        """
        ## GetCutParams - Получить параметры разреза

        Метод позволяет установить параметры разреза.

        ### Синтаксис Automation

        ```
        LPDISPATCH SetCutParams( long Index,
        BSTR * Name,
        long * Number,
        BOOL * ModelCut )
        ```

        ### Входные параметры

        +---------------------------+
        | Index | - индекс разреза. |
        +---------------------------+

        ### Выходные параметры

        +---------------------------------------------------------------+
        | Name     | - имя разреза,                                     |
        +---------------------------------------------------------------+
        | Number   | - номер разреза,                                   |
        +---------------------------------------------------------------+
        | ModelCut | - признак разреза, TRUE - разрез, FALSE - сечение. |
        +---------------------------------------------------------------+

        ### Возвращаемое значение

        - Указатель на контур, ограничивающий разрез.
        """
        ...

    def GetCutPlanePosition(self, Index) -> bool:  # icutviewparam_getcutplaneposition.html
        """
        ## GetCutPlanePosition - Получить координаты точки, через которую пройдет секущая плоскость

        Метод позволяет получить координаты точки, через которую пройдет секущая плоскость.

        ### Синтаксис Automation

        ```
        BOOL GetCutPlanePosition( long Index,
        double * X,
        double * Y )
        ```

        ### Входные параметры

        +---------------------------+
        | Index | - индекс разреза. |
        +---------------------------+

        ### Выходные параметры

        +---------------------------------------------------------------------+
        | X, Y | - координаты точки, через которую пройдет секущая плоскость. |
        +---------------------------------------------------------------------+

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def HatchParam(self, Index) -> IHatchParam:  # icutviewparam_hatchparam.html
        """
        ## HatchParam - Параметры штриховки

        Тип данных: указатель на интерфейс параметров штриховки IHatchParam

        1. Свойство позволяет получать интерфейс параметров штриховки разреза.

        2. Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        HatchParam = Object.HatchParam       # Получить свойство(* )
        HatchParam = Object.GetHatchParam()  # Получить свойство (**)
        ```
        """
        ...

    def LocalCut(self, Index) -> bool:  # icutviewparam_localcut.html
        """
        ## LocalCut - Местный разрез

        Тип данных: BOOL

        Свойство позволяет включать и выключать местный разрез.

        ### Синтаксис Automation

        ```
        LocalCut = Object.LocalCut                # Получить свойство(* )
        Object.LocalCut = LocalCut                # Установить свойство (* )
        LocalCut = Object.GetLocalCut()           # Получить свойство (**)
        Object.SetBreaksVisible( BreaksVisible )  # Установить свойство (**)
        ```
        """
        ...

    def SetCutParams(self, Index, Name, Number, ModelCut) -> bool:  # icutviewparam_setcutparams.html
        """
        ## SetCutParams - Установить параметры разреза

        Метод позволяет установить параметры разреза.

        ### Синтаксис Automation

        ```
        BOOL SetCutParams( long Index,
        BSTR Name,
        long Number,
        BOOL ModelCut )
        ```

        ### Входные параметры

        +---------------------------------------------------------------+
        | Index    | - индекс разреза,                                  |
        +---------------------------------------------------------------+
        | Name     | - имя разреза,                                     |
        +---------------------------------------------------------------+
        | Number   | - номер разреза,                                   |
        +---------------------------------------------------------------+
        | ModelCut | - признак разреза, TRUE - разрез, FALSE - сечение. |
        +---------------------------------------------------------------+

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def SetCutPlanePosition(self, Index, X, Y) -> bool:  # icutviewparam_setcutplaneposition.html
        """
        ## SetCutPlanePosition - Установить координаты точки, через которую пройдет секущая плоскость

        Метод позволяет установить координаты точки, через которую пройдет секущая плоскость.

        ### Синтаксис Automation

        ```
        BOOL SetCutPlanePosition( long Index,
        double X,
        double Y )
        ```

        ### Входные параметры

        +----------------------------------------------------------------------+
        | Index | - индекс разреза,                                            |
        +----------------------------------------------------------------------+
        | X, Y  | - координаты точки, через которую пройдет секущая плоскость. |
        +----------------------------------------------------------------------+

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def SetLocalCut(self, Index, arg1):
        ...




class IDimensionParams(IDispatch):  # idimensionparams.html
    """
    ## Интерфейс IDimensionParams

    Интерфейс параметров отрисовки размера.

    Является дополнительным интерфейсом для 2D и 3D размеров (интерфейсы IAngleDimension3D, IAngleDimension, IArcDimension, IBaseLineDimension3D, IBreakAngleDimension, IBreakLineDimension, IDiametralDimension3D, IDiametralDimension, IHeightDimension, ILineDimension3D, ILineDimension, IRadialDimension3D, IRadialDimension). Позволяет сформировать нужный текст размерной надписи.
    """

    @property
    def ArrowPos(self) -> int:  # idimensionparams_arrowpos.html
        """
        ## ArrowPos - Размещение стрелок

        Тип данных: Из перечисления ksDimensionArrowPosEnum

        Свойство позволяет выбрать способ размещения стрелок относительно выносных линий: внутри, снаружи или автоматическое определение положения. Вариант автоматического определения положения означает, что система будет для каждого размера автоматически определять, ставить ли стрелки изнутри или снаружи. Умолчательное размещение определяется настройкой стрелок, сделанной для текущего документа.

        ### Синтаксис Automation

        ```
        ArrowPos = Object.ArrowPos       # Получить свойство(* )
        Object.ArrowPos = ArrowPos       # Установить свойство (* )
        ArrowPos = Object.GetArrowPos()  # Получить свойство (**)
        Object.SetArrowPos( ArrowPos )   # Установить свойство (**)
        ```
        """
        ...
    @ArrowPos.setter
    def ArrowPos(self, value: int): ...

    @property
    def ArrowType1(self) -> int:  # idimensionparams_arrowtype1.html
        """
        ## ArrowType1 - Тип стрелки у первой выносной линии

        Тип данных: из перечисления ksArrowEnum

        Позволяют задать вид стрелки у первой выносной линии.

        ### Синтаксис Automation

        ```
        ArrowType1 = Object.ArrowType1       # Получить свойство(* )
        Object.ArrowType1 = ArrowType1       # Установить свойство (* )
        ArrowType1 = Object.GetArrowType1()  # Получить свойство (**)
        Object.SetArrowType1( ArrowType1 )   # Установить свойство (**)
        ```
        """
        ...
    @ArrowType1.setter
    def ArrowType1(self, value: int): ...

    @property
    def ArrowType2(self) -> int:  # idimensionparams_arrowtype2.html
        """
        ## ArrowType2 - Тип стрелки у второй выносной линии

        Тип данных: из перечисления ksArrowEnum

        Позволяют задать вид стрелки у второй выносной линии.

        ### Синтаксис Automation

        ```
        ArrowType2 = Object.ArrowType2       # Получить свойство(* )
        Object.ArrowType2 = ArrowType2       # Установить свойство (* )
        ArrowType2 = Object.GetArrowType2()  # Получить свойство (**)
        Object.SetArrowType2( ArrowType2 )   # Установить свойство (**)
        ```
        """
        ...
    @ArrowType2.setter
    def ArrowType2(self, value: int): ...

    @property
    def Gap(self) -> bool:  # idimensionparams_gap.html
        """
        ## Gap - Зазор или длина выносной линии

        Тип данных: BOOL

        1. Позволяет выбрать способы формирования зазора выносных линий. Если требуется, чтобы заданное число IDimensionParams::GapValue определяло зазор между началом выносной линии и точкой привязки размера, установите свойство в TRUE. В этом случае при изменении положения размерной линии будет изменяться длина выносной линии. Если значение должно определять длину выносной линии, установите значение свойства, равное FALSE. В этом случае при изменении положения размерной линии зазор будет изменяться, а длина выносной линии оставаться постоянной и равной IDimensionParams::GapValue.

        Если значение IDimensionParams::GapValue равно нулю, то выносные линии начинаются в точках привязки размера и могут иметь любую длину.

        2. Свойство используется только в линейных размерах.

        ### Синтаксис Automation

        ```
        Gap = Object.Gap       # Получить свойство(* )
        Object.Gap = Gap       # Установить свойство (* )
        Gap = Object.GetGap()  # Получить свойство (**)
        Object.SetGap( Gap )   # Установить свойство (**)
        ```
        """
        ...
    @Gap.setter
    def Gap(self, value: bool): ...

    @property
    def GapValue(self) -> float:  # idimensionparams_gapvalue.html
        """
        ## GapValue - Значение зазора или длины

        Тип данных: double

        1. Позволяет задать значение зазора или длины выносных линий. Если требуется, чтобы заданное число определяло зазор между началом выносной линии и точкой привязки размера, установите свойство IDimensionParams::Gap в TRUE. В этом случае при изменении положения размерной линии будет изменяться длина выносной линии, а зазор - оставаться постоянным и равным заданному значению.

        Если значение должно определять длину выносной линии, установите свойство IDimensionParams::Gap в FALSE. В этом случае при изменении положения размерной линии зазор будет изменяться, а длина выносной линии оставаться постоянной и равной заданному значению.

        Если значение равно нулю, то выносные линии начинаются в точках привязки размера и могут иметь любую длину.

        2. Свойство используется только в линейных размерах.

        ### Синтаксис Automation

        ```
        GapValue = Object.GapValue       # Получить свойство(* )
        Object.GapValue = GapValue       # Установить свойство (* )
        GapValue = Object.GetGapValue()  # Получить свойство (**)
        Object.SetGapValue( GapValue )   # Установить свойство (**)
        ```
        """
        ...
    @GapValue.setter
    def GapValue(self, value: float): ...

    @property
    def RemoteLine1(self) -> bool:  # idimensionparams_remoteline1.html
        """
        ## RemoteLine1 - Признак отрисовки первой выносной линии

        Тип данных: BOOL

        Позволяет управлять отрисовкой первой выносной линии.

        ### Синтаксис Automation

        ```
        RemoteLine1 = Object.RemoteLine1       # Получить свойство(* )
        Object.RemoteLine1 = RemoteLine1       # Установить свойство (* )
        RemoteLine1 = Object.GetRemoteLine1()  # Получить свойство (**)
        Object.SetRemoteLine1( RemoteLine1 )   # Установить свойство (**)
        ```
        """
        ...
    @RemoteLine1.setter
    def RemoteLine1(self, value: bool): ...

    @property
    def RemoteLine2(self) -> bool:  # idimensionparams_remoteline2.html
        """
        ## RemoteLine2 - Признак отрисовки второй выносной линии

        Тип данных: BOOL

        Позволяет управлять отрисовкой второй выносной линии.

        ### Синтаксис Automation

        ```
        RemoteLine2 = Object.RemoteLine2       # Получить свойство(* )
        Object.RemoteLine2 = RemoteLine2       # Установить свойство (* )
        RemoteLine2 = Object.GetRemoteLine2()  # Получить свойство (**)
        Object.SetRemoteLine2( RemoteLine2 )   # Установить свойство (**)
        ```
        """
        ...
    @RemoteLine2.setter
    def RemoteLine2(self, value: bool): ...

    @property
    def ShelfAngle(self) -> float:  # idimensionparams_shelfangle.html
        """
        ## ShelfAngle - Угол наклона выносной полки

        Тип данных: double

        Позволяет задать угол наклона выносной полки к оси абсцисс текущей системы координат.

        При использовании свойств ShelfX и ShelfY задание данного свойства не требуется.

        ### Синтаксис Automation

        ```
        ShelfAngle = Object.ShelfAngle       # Получить свойство(* )
        Object.ShelfAngle = ShelfAngle       # Установить свойство (* )
        ShelfAngle = Object.GetShelfAngle()  # Получить свойство (**)
        Object.SetShelfAngle( ShelfAngle )   # Установить свойство (**)
        ```
        """
        ...
    @ShelfAngle.setter
    def ShelfAngle(self, value: float): ...

    @property
    def ShelfDirection(self) -> int:  # idimensionparams_shelfdirection.html
        """
        ## ShelfDirection - Направление выносной полки

        Тип данных: из перечисления ksShelfDirectionEnum

        1. Позволяет задать направление выносной полки.

        2. Свойство используется совместно со свойством IDimensionParams::TextType.

        ### Синтаксис Automation

        ```
        ShelfDirection = Object.ShelfDirection       # Получить свойство(* )
        Object.ShelfDirection = ShelfDirection       # Установить свойство (* )
        ShelfDirection = Object.GetShelfDirection()  # Получить свойство (**)
        Object.SetShelfDirection( ShelfDirection )   # Установить свойство (**)
        ```
        """
        ...
    @ShelfDirection.setter
    def ShelfDirection(self, value: int): ...

    @property
    def ShelfLength(self) -> float:  # idimensionparams_shelflength.html
        """
        ## ShelfLength - Длина ножки выносной полки

        Тип данных: double

        Позволяет задать длину ножки выносной полки.

        При использовании свойств ShelfX и ShelfY задание данного свойства не требуется.

        ### Синтаксис Automation

        ```
        ShelfLength = Object.ShelfLength       # Получить свойство(* )
        Object.ShelfLength = ShelfLength       # Установить свойство (* )
        ShelfLength = Object.GetShelfLength()  # Получить свойство (**)
        Object.SetShelfLength( ShelfLength )   # Установить свойство (**)
        ```
        """
        ...
    @ShelfLength.setter
    def ShelfLength(self, value: float): ...

    @property
    def TextBase(self) -> int:  # idimensionparams_textbase.html
        """
        ## TextBase - Параметр отрисовки текста

        Тип данных: из перечисления ksDimensionBaseEnum

        Позволяет задать положение размерной надписи.

        ### Синтаксис Automation

        ```
        TextBase = Object.TextBase       # Получить свойство(* )
        Object.TextBase = TextBase       # Установить свойство (* )
        TextBase = Object.GetTextBase()  # Получить свойство (**)
        Object.SetTextBase( TextBase )   # Установить свойство (**)
        ```
        """
        ...
    @TextBase.setter
    def TextBase(self, value: int): ...

    @property
    def TextOnLine(self) -> int:  # idimensionparams_textonline.html
        """
        ## TextOnLine - Положение размерной надписи отноcительно размерной линии

        Тип данных: из перечисления ksDimensionTextPosEnum

        Позволяет задать положение размерной надписи отноcительно размерной линии. Умолчательное положение определяется Настройкой положения надписи, сделанной для текущего документа.

        ### Синтаксис Automation

        ```
        TextOnLine = Object.TextOnLine       # Получить свойство(* )
        Object.TextOnLine = TextOnLine       # Установить свойство (* )
        TextOnLine = Object.GetTextOnLine()  # Получить свойство (**)
        Object.SetTextOnLine( TextOnLine )   # Установить свойство (**)
        ```
        """
        ...
    @TextOnLine.setter
    def TextOnLine(self, value: int): ...

    @property
    def TextPos(self) -> int:  # idimensionparams_textpos.html
        """
        ## TextPos - Условное расстояние текста от выносной линии

        Тип данных: long

        Значения свойства:

        +--------------------------------------------------------------------------------------------+
        | 0   | - автоматическое размещение текста,                                                  |
        +--------------------------------------------------------------------------------------------+
        | > 0 | - размещение текста на указанном расстоянии в направлении от первой точки ко второй, |
        +--------------------------------------------------------------------------------------------+
        | < 0 | - размещение текста на указанном расстоянии в направлении от второй точки к первой.  |
        +--------------------------------------------------------------------------------------------+

        1. Значение свойства задается в миллиметрах для линейных размеров и в градусах для угловых.

        2. Параметр является аннотационным (не зависит от масштаба, измеряется "по бумаге").

        3. Для радиального и диаметрального размера задает длину "ножки":

        +--------------------------------------------------------------------------------------------------+
        | 0  | - автоматическое размещение в центре,                                                       |
        +--------------------------------------------------------------------------------------------------+
        | 1  | - автоматическое размещение за дугой по направлению угла,                                   |
        +--------------------------------------------------------------------------------------------------+
        | -1 | - автоматическое размещение за дугой против направления угла, откладывается от центра дуги. |
        +--------------------------------------------------------------------------------------------------+

        ### Синтаксис Automation

        ```
        TextPos = Object.TextPos       # Получить свойство(* )
        Object.TextPos = TextPos       # Установить свойство (* )
        TextPos = Object.GetTextPos()  # Получить свойство (**)
        Object.SetTextPos( TextPos )   # Установить свойство (**)
        ```
        """
        ...
    @TextPos.setter
    def TextPos(self, value: int): ...

    @property
    def TextType(self) -> int:  # idimensionparams_texttype.html
        """
        ## TextType - Тип размещения размерной надписи

        Тип данных: из перечисления ksDimensionTextTypeEnum

        Позволяет задать тип размещения размерной надписи: Автоматический, ручной или на полке. При размещении текста на полке можно задать также направление полки см. ShelfDirection.

        ### Синтаксис Automation

        ```
        TextType = Object.TextType       # Получить свойство(* )
        Object.TextType = TextType       # Установить свойство (* )
        TextType = Object.GetTextType()  # Получить свойство (**)
        Object.SetTextType( TextType )   # Установить свойство (**)
        ```
        """
        ...
    @TextType.setter
    def TextType(self, value: int): ...

    def InitDefaultValues(self) -> bool:  # idimensionparams_initdefaultvalues.html
        """
        ## InitDefaultValues - Инициализация параметров умолчательными значениями

        ### Синтаксис Automation

        ```
        BOOL InitDefaultValues()
        ```

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | в случае неудачи.                |
        +------------------------------------------+
        """
        ...




class IHatchParam(IDispatch):  # ihatchparam.html
    """
    ## Интерфейс IHatchParam

    Интерфейс параметров штриховки.

    Интерфейс позволяет задавать параметры штриховки.

    1. Интерфейс является дополнительным для IHatch и IAssociationView.

    2. Интерфейс можно получить с помощью метода ICutViewParam::HatchParam.
    """

    @property
    def HatchAngle(self) -> float:  # ihatchparam_hatchangle.html
        """
        ## HatchAngle - Угол наклона штриховки

        Тип данных: double

        Свойство позволяет устанавливать и получать стиль штриховки.

        ### Синтаксис Automation

        ```
        Angle = Object.Angle       # Получить свойство(* )
        Object.Style = Style       # Установить свойство (* )
        Style = Object.GetStyle()  # Получить свойство (**)
        Object.SetStyle( Style )   # Установить свойство (**)
        ```
        """
        ...
    @HatchAngle.setter
    def HatchAngle(self, value: float): ...

    @property
    def HatchColor(self) -> int:  # ihatchparam_hatchcolor.html
        """
        ## HatchColor - Цвет

        Тип данных: long

        Свойство позволяет устанавливать и получать цвет штриховки.

        ### Синтаксис Automation

        ```
        Color = Object.Color       # Получить свойство(* )
        Object.Color = Color       # Установить свойство (* )
        Color = Object.GetColor()  # Получить свойство (**)
        Object.SetColor( Color )   # Установить свойство (**)
        ```
        """
        ...
    @HatchColor.setter
    def HatchColor(self, value: int): ...

    @property
    def HatchFileName(self) -> str:  # ihatchparam_hatchfilename.html
        """
        ## HatchFileName - Имя файла для стиля штриховки

        Тип данных: BSTR

        1. Свойство позволяет получить имя файла библиотеки стилей штриховки (*.lhs).

        2. Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        HatchFileName = Object.HatchFileName       # Получить свойство(* )
        HatchFileName = Object.GetHatchFileName()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def HatchType(self) -> bool:  # ihatchparam_hatchtype.html
        """
        ## HatchType - Тип штриховки

        Тип данных: BOOL

        Значения свойства:

        +--------------------+
        | TRUE  | - область, |
        +--------------------+
        | FALSE | - полоса.  |
        +--------------------+

        Свойство позволяет устанавливать и получать тип штриховки.

        ### Синтаксис Automation

        ```
        HatchType = Object.HatchType       # Получить свойство(* )
        Object.HatchType = HatchType       # Установить свойство (* )
        HatchType = Object.GetHatchType()  # Получить свойство (**)
        Object.SetHatchType( HatchType )   # Установить свойство (**)
        ```
        """
        ...
    @HatchType.setter
    def HatchType(self, value: bool): ...

    @property
    def IsSheetAngle(self) -> bool:  # ihatchparam_issheetangle.html
        """
        ## IsSheetAngle - Угол наклона штриховки относительно листа или границ

        Тип данных: BOOL

        Значения свойства:

        +------------------------------------------------------------------------------------------------------------------------------------+
        | TRUE  | - обычная штриховка (угол штриховки - постоянный),                                                                         |
        +------------------------------------------------------------------------------------------------------------------------------------+
        | FALSE | - угол штриховки относительно ее границ сохраняется при повороте границ (используется при изображении накатки на деталях). |
        +------------------------------------------------------------------------------------------------------------------------------------+

        Свойство позволяет устанавливать и получать признак, является ли угол наклона штриховки листовым.

        ### Синтаксис Automation

        ```
        IsSheetAngle = Object.IsSheetAngle       # Получить свойство(* )
        Object.IsSheetAngle = IsSheetAngle       # Установить свойство (* )
        IsSheetAngle = Object.GetIsSheetAngle()  # Получить свойство (**)
        Object.SetIsSheetAngle( IsSheetAngle )   # Установить свойство (**)
        ```
        """
        ...
    @IsSheetAngle.setter
    def IsSheetAngle(self, value: bool): ...

    @property
    def LibraryStyleNumber(self) -> float:  # ihatchparam_librarystylenumber.html
        """
        ## LibraryStyleNumber - Идентификатор стиля в библиотеке

        Тип данных: double

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        LibraryStyleNumber = Object. LibraryStyleNumber      # Получить свойство(* )
        LibraryStyleNumber = Object.GetLibraryStyleNumber()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def Step(self) -> float:  # ihatchparam_step.html
        """
        ## Step - Шаг или масштаб

        Тип данных: double

        Свойство позволяет устанавливать и получать шаг или масштаб штриховки.

        ### Синтаксис Automation

        ```
        Step = Object.Step       # Получить свойство(* )
        Object.Step = Step       # Установить свойство (* )
        Step = Object.GetStep()  # Получить свойство (**)
        Object.SetStep( Step )   # Установить свойство (**)
        ```
        """
        ...
    @Step.setter
    def Step(self, value: float): ...

    @property
    def Style(self) -> int:  # ihatchparam_style.html
        """
        ## Style - Cтиль штриховки

        Тип данных: long

        1. Свойство позволяет устанавливать и получать стиль штриховки.

        2. Системные стили штриховки берутся из перечисления ksHatchStyleEnum.

        ### Синтаксис Automation

        ```
        Style = Object.Style       # Получить свойство(* )
        Object.Style = Style       # Установить свойство (* )
        Style = Object.GetStyle()  # Получить свойство (**)
        Object.SetStyle( Style )   # Установить свойство (**)
        ```
        """
        ...
    @Style.setter
    def Style(self, value: int): ...

    @property
    def Width(self) -> float:  # ihatchparam_width.html
        """
        ## Width - Ширина полосы

        Тип данных: double

        Значения свойства:

        +--------------------+
        | TRUE  | - область, |
        +--------------------+
        | FALSE | - полоса.  |
        +--------------------+

        Свойство позволяет устанавливать и получать ширину полосы штриховки.

        ### Синтаксис Automation

        ```
        Width = Object.Width       # Получить свойство(* )
        Object.Width = Width       # Установить свойство (* )
        Width = Object.GetWidth()  # Получить свойство (**)
        Object.SetWidth( Width )   # Установить свойство (**)
        ```
        """
        ...
    @Width.setter
    def Width(self, value: float): ...

    def AddStyleFromLibrary(self, FileName, StyleNumber) -> bool:  # ihatchparam_addstylefromlibrary.html
        """
        ## AddStyleFromLibrary - Инициализация стиля штриховки из библиотеки стилей

        ### Синтаксис Automation

        ```
        BOOL AddStyleFromLibrary( BSTR FileName, double StyleNumber);
        ```

        ### Входные параметры

        +---------------------------------------------------+
        | FileName    | - имя файла для стиля штриховки,    |
        +---------------------------------------------------+
        | StyleNumber | - идентификатор стиля в библиотеке. |
        +---------------------------------------------------+

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...




class IPhantom2D(IKompasAPIObject):  # iphantom2d.html
    """
    ## Интерфейс IPhantom2D

    Параметры фантома 2D.
    """

    @property
    def Angle(self) -> float:  # iphantom2d_angle.html
        """
        ## Angle - Угол

        Тип данных: double

        ### Синтаксис Automation

        ```
        Angle = Object.Angle       # Получить свойство(* )
        Object.Angle = Angle       # Установить свойство (* )
        Angle = Object.GetAngle()  # Получить свойство (**)
        Object.SetAngle( Angle )   # Установить свойство (**)
        ```
        """
        ...
    @Angle.setter
    def Angle(self, value: float): ...

    @property
    def Horizontal(self) -> bool:  # iphantom2d_horizontal.html
        """
        ## Horizontal - True - подходим к курсору по горизонтали, False - по вертикали

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        Horizontal = Object. Horizontal       # Получить свойство(* )
        Object. Horizontal = Horizontal       # Установить свойство (* )
        Horizontal = Object.Get Horizontal()  # Получить свойство (**)
        Object.Set Horizontal( Horizontal )   # Установить свойство (**)
        ```
        """
        ...
    @Horizontal.setter
    def Horizontal(self, value: bool): ...

    @property
    def PhantomGroup(self) -> IDrawingGroup:  # iphantom2d_phantomgroup.html
        """
        ## PhantomGroup - Фантомная группа

        Тип данных: Указатель на интерфейс IDrawingGroup

        ### Синтаксис Automation

        ```
        PhantomGroup = Object.PhantomGroup       # Получить свойство(* )
        Object.PhantomGroup = PhantomGroup       # Установить свойство (* )
        PhantomGroup = Object.GetPhantomGroup()  # Получить свойство (**)
        Object.SetPhantomGroup( PhantomGroup )   # Установить свойство (**)
        ```
        """
        ...
    @PhantomGroup.setter
    def PhantomGroup(self, value: IDrawingGroup): ...

    @property
    def PhantomType(self) -> int:  # iphantom2d_phantomtype.html
        """
        ## PhantomType - Тип резинки

        Тип данных: из перечисления ksPhantomTypeEnum

        ### Синтаксис Automation

        ```
        PhantomType = Object.PhantomType       # Получить свойство(* )
        Object.PhantomType = PhantomType       # Установить свойство (* )
        PhantomType = Object.GetPhantomType()  # Получить свойство (**)
        Object.SetPhantomType( PhantomType )   # Установить свойство (**)
        ```
        """
        ...
    @PhantomType.setter
    def PhantomType(self, value: int): ...

    @property
    def Scale(self) -> float:  # iphantom2d_scale.html
        """
        ## Scale - Масштаб

        Тип данных: double

        ### Синтаксис Automation

        ```
        Scale = Object.Scale       # Получить свойство(* )
        Object.Scale = Scale       # Установить свойство (* )
        Scale = Object.GetScale()  # Получить свойство (**)
        Object.SetScale( Scale )   # Установить свойство (**)
        ```
        """
        ...
    @Scale.setter
    def Scale(self, value: float): ...

    @property
    def X(self) -> float:  # iphantom2d_x.html
        """
        ## X - Координата X

        Тип данных: double

        ### Синтаксис Automation

        ```
        X = Object.X       # Получить свойство(* )
        Object.X = X       # Установить свойство (* )
        X = Object.GetX()  # Получить свойство (**)
        Object.SetX( X )   # Установить свойство (**)
        ```
        """
        ...
    @X.setter
    def X(self, value: float): ...

    @property
    def Y(self) -> float:  # iphantom2d_y.html
        """
        ## Y - Координата Y

        Тип данных: double

        ### Синтаксис Automation

        ```
        Y = Object.Y       # Получить свойство(* )
        Object.Y = Y       # Установить свойство (* )
        Y = Object.GetY()  # Получить свойство (**)
        Object.SetY( Y )   # Установить свойство (**)
        ```
        """
        ...
    @Y.setter
    def Y(self, value: float): ...

    def Hide(self) -> bool:  # iphantom2d_hide.html
        """
        ## Hide - Погасить фантом

        ### Синтаксис Automation

        ```
        BOOL Hide();
        ```

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def Show(self) -> bool:  # iphantom2d_show.html
        """
        ## Show - Показать фантом

        ### Синтаксис Automation

        ```
        BOOL Show();
        ```

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def Update(self) -> bool:  # iphantom2d_update.html
        """
        ## Update - Обновить фантом

        ### Синтаксис Automation

        ```
        BOOL Update();
        ```

        ### Возвращаемое значение

        +---------------------------------+
        | TRUE | - если фантом обновился. |
        +---------------------------------+
        """
        ...




class IRoughParams(IDispatch):  # iroughparams.html
    """
    ## Интерфейс IRoughParams

    Интерфейс параметров обозначения шероховатости.

    1. Является дополнительным интерфейсом для 2D и 3D обозначений шероховатости.

    2. Позволяет задать и получить параметры обозначения шероховатости.

    3. Интерфейс можно получить с помощью метода IUnknown::QueryInterface у интерфейсов IRough3D или IRough.
    """

    @property
    def ArrowInside(self) -> bool:  # iroughparams_arrowinside.html
        """
        ## ArrowInside - Расположение стрелки линии-выноски

        Тип данных: BOOL

        Значения свойства:

        +--------------------+
        | TRUE  | - внутри,  |
        +--------------------+
        | FALSE | - снаружи. |
        +--------------------+

        ### Синтаксис Automation

        ```
        ArrowInside = Object.ArrowInside       # Получить свойство(* )
        Object.ArrowInside = ArrowInside       # Установить свойство (* )
        ArrowInside = Object.GetArrowInside()  # Получить свойство (**)
        Object.SetArrowInside( ArrowInside )   # Установить свойство (**)
        ```
        """
        ...
    @ArrowInside.setter
    def ArrowInside(self, value: bool): ...

    @property
    def ArrowType(self) -> int:  # iroughparams_arrowtype.html
        """
        ## ArrowType - Тип стрелки линии-выноски

        Тип данных: из перечисления ksArrowEnum

        Для обозначения шероховатости используются следующие типы стрелок:

        •стрелка (внутри/снаружи);

        •засечка;

        •засечка c наклоном влево;

        •вспомогательная точка;

        •без стрелки.

        ### Синтаксис Automation

        ```
        ArrowType = Object.ArrowType       # Получить свойство(* )
        Object.ArrowType = ArrowType       # Установить свойство (* )
        ArrowType = Object.GetArrowType()  # Получить свойство (**)
        Object.SetArrowType( ArrowType )   # Установить свойство (**)
        ```
        """
        ...
    @ArrowType.setter
    def ArrowType(self, value: int): ...

    @property
    def BaseLengthText(self) -> IText:  # iroughparams_baselengthtext.html
        """
        ## BaseLengthText - Текст "Базовая длина по ГОСТ 2789-73"

        Тип данных: указатель на интерфейс IText

        1. Свойство позволяет получить указатель на интерфейс текста IText для редактирования текста "Базовая длина по ГОСТ 2789-73".

        2. Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        BaseLengthText = Object.BaseLengthText       # Получить свойство(* )
        BaseLengthText = Object.GetBaseLengthText()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def LeaderAngle(self) -> float:  # iroughparams_leaderangle.html
        """
        ## LeaderAngle - Угол наклона линии-выноски

        Тип данных: double

        ### Синтаксис Automation

        ```
        LeaderAngle = Object.LeaderAngle       # Получить свойство(* )
        Object.LeaderAngle = LeaderAngle       # Установить свойство (* )
        LeaderAngle = Object.GetLeaderAngle()  # Получить свойство (**)
        Object.SetLeaderAngle( LeaderAngle )   # Установить свойство (**)
        ```
        """
        ...
    @LeaderAngle.setter
    def LeaderAngle(self, value: float): ...

    @property
    def LeaderLength(self) -> float:  # iroughparams_leaderlength.html
        """
        ## LeaderLength - Длина линии-выноски

        Тип данных: double

        ### Синтаксис Automation

        ```
        LeaderLength = Object.LeaderLength       # Получить свойство(* )
        Object.LeaderLength = LeaderLength       # Установить свойство (* )
        LeaderLength = Object.GetLeaderLength()  # Получить свойство (**)
        Object.SetLeaderLength( LeaderLength )   # Установить свойство (**)
        ```
        """
        ...
    @LeaderLength.setter
    def LeaderLength(self, value: float): ...

    @property
    def ProcessText(self) -> IText:  # iroughparams_processtext.html
        """
        ## ProcessText - Текст "Способ обработки поверхностии (или) другие дополнительные указания"

        Тип данных: указатель на интерфейс IText

        1. Свойство позволяет получить указатель на интерфейс текста IText для редактирования текста "Способ обработки поверхности и (или) другие дополнительные указания".

        2. Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        ProcessText = Object.ProcessText       # Получить свойство(* )
        ProcessText = Object.GetProcessText()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def ProcessingByContour(self) -> bool:  # iroughparams_processingbycontour.html
        """
        ## ProcessingByContour- Обработка по контуру

        Тип данных: BOOL

        Значения свойства:

        +---------------------------------+
        | TRUE  | - обработка по контуру, |
        +---------------------------------+
        | FALSE | - не по контуру.        |
        +---------------------------------+

        ### Синтаксис Automation

        ```
        ProcessingByContour = Object.ProcessingByContour       # Получить свойство(* )
        Object.ProcessingByContour = ProcessingByContour       # Установить свойство (* )
        ProcessingByContour = Object.GetProcessingByContour()  # Получить свойство (**)
        Object.SetProcessingByContour( ProcessingByContour )   # Установить свойство (**)
        ```
        """
        ...
    @ProcessingByContour.setter
    def ProcessingByContour(self, value: bool): ...

    @property
    def RoughParamText(self) -> IText:  # iroughparams_roughparamtext.html
        """
        ## RoughParamText - Текст "Параметр (параметры) шероховатости по ГОСТ 2789-73"

        Тип данных: указатель на интерфейс IText

        1. Свойство позволяет получить указатель на интерфейс текста IText для редактирования текста "Параметр (параметры) шероховатости по ГОСТ 2789-73".

        2. Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        RoughParamText = Object.RoughParamText       # Получить свойство(* )
        RoughParamText = Object.GetRoughParamText()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def ShelfDirection(self) -> int:  # iroughparams_shelfdirection.html
        """
        ## ShelfDirection - Направление полки

        Тип данных: из перечисления ksShelfDirectionEnum

        ### Синтаксис Automation

        ```
        ShelfDirection = Object.ShelfDirection       # Получить свойство(* )
        Object.ShelfDirection = ShelfDirection       # Установить свойство (* )
        ShelfDirection = Object.GetShelfDirection()  # Получить свойство (**)
        Object.SetShelfDirection( ShelfDirection )   # Установить свойство (**)
        ```
        """
        ...
    @ShelfDirection.setter
    def ShelfDirection(self, value: int): ...

    @property
    def SignType(self) -> int:  # iroughparams_signtype.html
        """
        ## SignType - Тип значка

        Тип данных: из перечисления ksRoughSignEnum.

        ### Синтаксис Automation

        ```
        SignType = Object.SignType       # Получить свойство(* )
        Object.SignType = SignType       # Установить свойство (* )
        SignType = Object.GetSignType()  # Получить свойство (**)
        Object.SetSignType( SignType )   # Установить свойство (**)
        ```
        """
        ...
    @SignType.setter
    def SignType(self, value: int): ...

    @property
    def TrendText(self) -> IText:  # iroughparams_trendtext.html
        """
        ## TrendText - Текст "Условное обозначение направления неровностей"

        Тип данных: указатель на интерфейс IText

        1. Свойство позволяет получить указатель на интерфейс текста IText для редактирования текста "Условное обозначение направления неровностей".

        2. Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        TrendText = Object.TrendText       # Получить свойство(* )
        TrendText = Object.GetTrendText()  # Получить свойство (**)
        ```
        """
        ...




class IToleranceParam(IDispatch):  # itoleranceparam.html
    """
    ## Интерфейс IToleranceParam

    Интерфейс параметров обозначения допуска формы.

    1. Является дополнительным интерфейсом для 2D и 3D обозначений допуска формы.

    2. Позволяет задать и получить параметры обозначения допуска формы.

    3. Интерфейс можно получить с помощью метода IUnknown::QueryInterface у интерфейсов ITolerance и ITolerance3D.
    """

    @property
    def BasePointPos(self) -> int:  # itoleranceparam_basepointpos.html
        """
        ## BasePointPos - Базовая точка

        Тип данных: из перечисления ksTablePointEnum

        Свойство позволяет устанавливать и получать тип расположения базовой точки.

        ### Синтаксис Automation

        ```
        BasePointPos = Object.BasePointPos        # Получить свойство(* )
        Object.BasePointPos = BasePointPos        # Установить свойство (* )
        BasePointPos = Object.GetBasePointPos( )  # Получить свойство (**)
        Object.SetBasePointPos( BasePointPos )    # Установить свойство (**)
        ```
        """
        ...
    @BasePointPos.setter
    def BasePointPos(self, value: int): ...

    @property
    def BaseSign1(self) -> int:  # itoleranceparam_basesign1.html
        """
        ## BaseSign1 - Знак после базы 1

        Тип данных: из перечисления ksToleranceSuffixSignEnum

        Свойство позволяет устанавливать и получать знак после базы 1.

        ### Синтаксис Automation

        ```
        BaseSign1 = Object.BaseSign1        # Получить свойство(* )
        Object.BaseSign1 = BaseSign1        # Установить свойство (* )
        BaseSign1 = Object.GetBaseSign1( )  # Получить свойство (**)
        Object.SetBaseSign1( BaseSign1 )    # Установить свойство (**)
        ```
        """
        ...
    @BaseSign1.setter
    def BaseSign1(self, value: int): ...

    @property
    def BaseSign2(self) -> int:  # itoleranceparam_basesign2.html
        """
        ## BaseSign2 - Знак после базы 2

        Тип данных: из перечисления ksToleranceSuffixSignEnum

        Свойство позволяет устанавливать и получать знак после базы 2.

        ### Синтаксис Automation

        ```
        BaseSign2 = Object.BaseSign2        # Получить свойство(* )
        Object.BaseSign2 = BaseSign2        # Установить свойство (* )
        BaseSign2 = Object.GetBaseSign2( )  # Получить свойство (**)
        Object.SetBaseSign2( BaseSign2 )    # Установить свойство (**)
        ```
        """
        ...
    @BaseSign2.setter
    def BaseSign2(self, value: int): ...

    @property
    def BaseValue1(self) -> ITextLine:  # itoleranceparam_basevalue1.html
        """
        ## BaseValue1 - Значение базы 1

        Тип данных: указатель на интерфейс ITextLine

        1. Свойство позволяет получить интерфейс текстовой строки со значением базы 1.

        2. Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        BaseValue1 = Object.BaseValue1       # Получить свойство(* )
        BaseValue1 = Object.GetBaseValue1()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def BaseValue2(self) -> ITextLine:  # itoleranceparam_basevalue2.html
        """
        ## BaseValue2 - Значение базы 2

        Тип данных: указатель на интерфейс ITextLine

        1. Свойство позволяет получить интерфейс текстовой строки со значением базы 2.

        2. Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        BaseValue2 = Object.BaseValue1       # Получить свойство(* )
        BaseValue2 = Object.GetBaseValue2()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def PrefixSign(self) -> int:  # itoleranceparam_prefixsign.html
        """
        ## PrefixSign - Знак перед числовым значением

        Тип данных: из перечисления ksTolerancePrefixSignEnum

        Свойство позволяет устанавливать и получать знак перед числовым значением допуска формы.

        ### Синтаксис Automation

        ```
        PrefixSign = Object.PrefixSign        # Получить свойство(* )
        Object.PrefixSign = PrefixSign        # Установить свойство (* )
        PrefixSign = Object.GetPrefixSign( )  # Получить свойство (**)
        Object.SetPrefixSign( PrefixSign )    # Установить свойство (**)
        ```
        """
        ...
    @PrefixSign.setter
    def PrefixSign(self, value: int): ...

    @property
    def Sign(self) -> int:  # itoleranceparam_sign.html
        """
        ## Sign - Номер спецзнака

        Тип данных: long

        Свойство позволяет устанавливать и получать номер спецзнака. Пример спецзнаков: см. файл SDK\\NumbSymb.frw. Коды спецзнаков хранятся в файле Sys\\GRAPHIC.SSS.

        ### Синтаксис Automation

        ```
        Sign = Object.Sign        # Получить свойство(* )
        Object.Sign = Sign        # Установить свойство (* )
        Sign = Object.GetSign( )  # Получить свойство (**)
        Object.SetSign( Sign )    # Установить свойство (**)
        ```
        """
        ...
    @Sign.setter
    def Sign(self, value: int): ...

    @property
    def SuffixSign(self) -> int:  # itoleranceparam_suffixsign.html
        """
        ## SuffixSign - Знак после числового значения

        Тип данных: из перечисления ksToleranceSuffixSignEnum

        Свойство позволяет устанавливать и получать знак после числового значения допуска формы.

        ### Синтаксис Automation

        ```
        SuffixSign = Object.SuffixSign        # Получить свойство(* )
        Object.SuffixSign = SuffixSign        # Установить свойство (* )
        SuffixSign = Object.GetSuffixSign( )  # Получить свойство (**)
        Object.SetSuffixSign( SuffixSign )    # Установить свойство (**)
        ```
        """
        ...
    @SuffixSign.setter
    def SuffixSign(self, value: int): ...

    @property
    def Table(self) -> ITable:  # itoleranceparam_table.html
        """
        ## Table - Текст надписи

        Тип данных: указатель на интерфейс ITable

        1. Свойство позволяет получить интерфейс таблицы, содержащей текст допуска формы.

        2. Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        Table = Object.Table       # Получить свойство(* )
        Table = Object.GetTable()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def Value(self) -> str:  # itoleranceparam_value.html
        """
        ## Value - Строка с числовым значением

        Тип данных: BSTR

        Свойство позволяет устанавливать и получать строку с числовым значением допуска формы.

        ### Синтаксис Automation

        ```
        Value = Object.Value        # Получить свойство(* )
        Object.Value = Value        # Установить свойство (* )
        Value = Object.GetValue( )  # Получить свойство (**)
        Object.SetValue( Value )    # Установить свойство (**)
        ```
        """
        ...
    @Value.setter
    def Value(self, value: str): ...

    @property
    def Vertical(self) -> bool:  # itoleranceparam_vertical.html
        """
        ## Vertical - Вертикальное расположение объекта

        Тип данных: BOOL

        Свойство позволяет устанавливать и получать признак вертикального расположения объекта.

        ### Синтаксис Automation

        ```
        Vertical = Object.Vertical        # Получить свойство(* )
        Object.Vertical = Vertical        # Установить свойство (* )
        Vertical = Object.GetVertical( )  # Получить свойство (**)
        Object.SetVertical( Vertical )    # Установить свойство (**)
        ```
        """
        ...
    @Vertical.setter
    def Vertical(self, value: bool): ...




class ICopyObjectParam(IKompasAPIObject):  # icopyobjectparam.html
    """
    ## Интерфейс ICopyObjectParam

    Интерфейс параметров копирования объектов.

    Интерфейс позволяет задавать и получать параметры копирования объектов.

    1. Данный интерфейс можно получить с помощью метода IKompasDocument1::GetInterface.

    2. Посредством вызова метода IUnknown::QueryInterface (const GUID far& iid, void** pif) у данного интерфейса можно получить дополнительные интерфейсы ICurveCopyObjectParam и ICopyObjectParam1.
    """

    @property
    def Angle(self) -> float:  # icopyobjectparam_angle.html
        """
        ## Angle - Угол поворота

        Тип данных: double

        Свойство позволяет устанавливать и получать угол поворота в градусах.

        ### Синтаксис Automation

        ```
        Angle = Object.Angle       # Получить свойство(* )
        Object.Angle = Angle       # Установить свойство (* )
        Angle = Object.GetAngle()  # Получить свойство (**)
        Object.SetAngle( Angle )   # Установить свойство (**)
        ```
        """
        ...
    @Angle.setter
    def Angle(self, value: float): ...

    @property
    def AttributeCopy(self) -> bool:  # icopyobjectparam_attributecopy.html
        """
        ## AttributeCopy - Признак копирования атрибутов

        Тип данных: BOOL

        Свойство позволяет устанавливать и получать признак копирования атрибутов.

        ### Синтаксис Automation

        ```
        AttributeCopy = Object.AttributeCopy       # Получить свойство(* )
        Object.AttributeCopy = AttributeCopy       # Установить свойство (* )
        AttributeCopy = Object.GetAttributeCopy()  # Получить свойство (**)
        Object.SetAttributeCopy( AttributeCopy )   # Установить свойство (**)
        ```
        """
        ...
    @AttributeCopy.setter
    def AttributeCopy(self, value: bool): ...

    @property
    def DimensionLineScale(self) -> bool:  # icopyobjectparam_dimensionlinescale.html
        """
        ## DimensionLineScale - Масштабировать выносные линии

        Тип данных: BOOL

        Свойство позволяет устанавливать и получать признак масштабирования выносных линий.

        ### Синтаксис Automation

        ```
        DimensionLineScale = Object.DimensionLineScale       # Получить свойство(* )
        Object.DimensionLineScale = DimensionLineScale       # Установить свойство (* )
        DimensionLineScale = Object.GetDimensionLineScale()  # Получить свойство (**)
        Object.SetDimensionLineScale( DimensionLineScale )   # Установить свойство (**)
        ```
        """
        ...
    @DimensionLineScale.setter
    def DimensionLineScale(self, value: bool): ...

    @property
    def Scale(self) -> float:  # icopyobjectparam_scale.html
        """
        ## Scale - Масштаб

        Тип данных: double

        Свойство позволяет устанавливать и получать масштаб копии.

        ### Синтаксис Automation

        ```
        Scale = Object.Scale       # Получить свойство(* )
        Object.Scale = Scale       # Установить свойство (* )
        Scale = Object.GetScale()  # Получить свойство (**)
        Object.SetScale( Scale )   # Установить свойство (**)
        ```
        """
        ...
    @Scale.setter
    def Scale(self, value: float): ...

    @property
    def SpecificationObjectCopy(self) -> bool:  # icopyobjectparam_specificationobjectcopy.html
        """
        ## SpecificationObjectCopy - Копировать объекты спецификации

        Тип данных: BOOL

        Свойство позволяет устанавливать и получать признак копирования объектов спецификации.

        ### Синтаксис Automation

        ```
        SpecificationObjectCopy = Object.SpecificationObjectCopy       # Получить свойство(* )
        Object.SpecificationObjectCopy = SpecificationObjectCopy       # Установить свойство (* )
        SpecificationObjectCopy = Object.GetSpecificationObjectCopy()  # Получить свойство (**)
        Object.SetSpecificationObjectCopy( SpecificationObjectCopy )   # Установить свойство (**)
        ```
        """
        ...
    @SpecificationObjectCopy.setter
    def SpecificationObjectCopy(self, value: bool): ...

    @property
    def XNew(self) -> float:  # icopyobjectparam_xnew.html
        """
        ## XNew - Координата новой точки привязки объекта по оси X

        Тип данных: double

        Свойство позволяет устанавливать и получать координату новой точки привязки объекта по оси X.

        ### Синтаксис Automation

        ```
        XNew = Object.XNew       # Получить свойство(* )
        Object.XNew = XNew       # Установить свойство (* )
        XNew = Object.GetXNew()  # Получить свойство (**)
        Object.SetXNew( XNew )   # Установить свойство (**)
        ```
        """
        ...
    @XNew.setter
    def XNew(self, value: float): ...

    @property
    def XOld(self) -> float:  # icopyobjectparam_xold.html
        """
        ## XOld - Координата базовой точки объекта по оси X

        Тип данных: double

        Свойство позволяет устанавливать и получать координату базовой точки объекта по оси X.

        ### Синтаксис Automation

        ```
        XOld = Object.XOld       # Получить свойство(* )
        Object.XOld = XOld       # Установить свойство (* )
        XOld = Object.GetXOld()  # Получить свойство (**)
        Object.SetXOld( XOld )   # Установить свойство (**)
        ```
        """
        ...
    @XOld.setter
    def XOld(self, value: float): ...

    @property
    def YNew(self) -> float:  # icopyobjectparam_ynew.html
        """
        ## YNew - Координата новой точки привязки объекта по оси Y

        Тип данных: double

        Свойство позволяет устанавливать и получать координату новой точки привязки объекта по оси Y.

        ### Синтаксис Automation

        ```
        YNew = Object.YNew       # Получить свойство(* )
        Object.YNew = YNew       # Установить свойство (* )
        YNew = Object.GetYNew()  # Получить свойство (**)
        Object.SetYNew( YNew )   # Установить свойство (**)
        ```
        """
        ...
    @YNew.setter
    def YNew(self, value: float): ...

    @property
    def YOld(self) -> float:  # icopyobjectparam_yold.html
        """
        ## YOld - Координата базовой точки объекта по оси Y

        Тип данных: double

        Свойство позволяет устанавливать и получать координату базовой точки объекта по оси Y.

        ### Синтаксис Automation

        ```
        YOld = Object.YOld       # Получить свойство(* )
        Object.YOld = YOld       # Установить свойство (* )
        YOld = Object.GetYOld()  # Получить свойство (**)
        Object.SetYOld( YOld )   # Установить свойство (**)
        ```
        """
        ...
    @YOld.setter
    def YOld(self, value: float): ...




class ICircleCopyObjectParam(ICopyObjectParam):  # icirclecopyobjectparam.html
    """
    ## Интерфейс ICircleCopyObjectParam

    Интерфейс параметров копирования объектов по окружности.

    Интерфейс можно получить с помощью функции IKompasDocument1::GetInterface.
    """

    @property
    def ByStep(self) -> bool:  # icirclecopyobjectparam_bystep.html
        """
        ## ByStep - Способ определения шага: заданный или расчетный

        Тип данных: BOOL

        Значения свойства:

        +---------------------------------------------------+
        | TRUE  | - с заданным шагом,                       |
        +---------------------------------------------------+
        | FALSE | - шаг рассчитывается по количеству копий. |
        +---------------------------------------------------+

        ### Синтаксис Automation

        ```
        ByStep = Object.ByStep       # Получить свойство (* )
        Object.ByStep = ByStep       # Установить свойство (**)
        ByStep = Object.GetByStep()  # Получить свойство (**)
        Object.SetByStep( ByStep )   # Установить свойство (**)
        ```
        """
        ...
    @ByStep.setter
    def ByStep(self, value: bool): ...

    @property
    def Count(self) -> int:  # icirclecopyobjectparam_count.html
        """
        ## Count - Количество копий

        Тип данных: long

        ### Синтаксис Automation

        ```
        Count = Object.Count       # Получить свойство (* )
        Object.Count = Count       # Установить свойство (**)
        Count = Object.GetCount()  # Получить свойство (**)
        Object.SetCount( Count )   # Установить свойство (**)
        ```
        """
        ...
    @Count.setter
    def Count(self, value: int): ...

    @property
    def PositiveDirection(self) -> bool:  # icirclecopyobjectparam_positivedirection.html
        """
        ## PositiveDirection - Положительное направление

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        PositiveDirection = Object.PositiveDirection       # Получить свойство (* )
        Object.PositiveDirection = PositiveDirection       # Установить свойство (**)
        PositiveDirection = Object.GetPositiveDirection()  # Получить свойство (**)
        Object.SetPositiveDirection( PositiveDirection )   # Установить свойство (**)
        ```
        """
        ...
    @PositiveDirection.setter
    def PositiveDirection(self, value: bool): ...

    @property
    def Step(self) -> float:  # icirclecopyobjectparam_step.html
        """
        ## Step - Шаг копирования

        Тип данных: double

        ### Синтаксис Automation

        ```
        Step = Object.Step       # Получить свойство (* )
        Object.Step = Step       # Установить свойство (**)
        Step = Object.GetStep()  # Получить свойство (**)
        Object.SetStep( Step )   # Установить свойство (**)
        ```
        """
        ...
    @Step.setter
    def Step(self, value: float): ...

    @property
    def Xc(self) -> float:  # icirclecopyobjectparam_xc.html
        """
        ## Xc - Координата центра по X

        Тип данных: double

        ### Синтаксис Automation

        ```
        Xc = Object.Xc       # Получить свойство (* )
        Object.Xc = Xc       # Установить свойство (**)
        Xc = Object.GetXc()  # Получить свойство (**)
        Object.SetXc( Xc )   # Установить свойство (**)
        ```
        """
        ...
    @Xc.setter
    def Xc(self, value: float): ...

    @property
    def Yc(self) -> float:  # icirclecopyobjectparam_yc.html
        """
        ## Yc - Координата центра по Y

        Тип данных: double

        ### Синтаксис Automation

        ```
        Yc = Object.Yc       # Получить свойство (* )
        Object.Yc = Yc       # Установить свойство (**)
        Yc = Object.GetYc()  # Получить свойство (**)
        Object.SetYc( Yc )   # Установить свойство (**)
        ```
        """
        ...
    @Yc.setter
    def Yc(self, value: float): ...




class ICircularCopyObjectParam(ICopyObjectParam):  # icircularcopyobjectparam.html
    """
    ## Интерфейс ICircularCopyObjectParam

    Параметры копирования объектов по концентрической сетке

    Интерфейс можно получить с помощью функции IKompasDocument1::GetInterface.
    """

    @property
    def Angle2(self) -> float:  # icircularcopyobjectparam_angle2.html
        """
        ## Angle2 - Начальный угол

        Тип данных: double

        ### Синтаксис Automation

        ```
        Angle2 = Object.Angle2       # Получить свойство (* )
        Object.Angle2 = Angle2       # Установить свойство (**)
        Angle2 = Object.GetAngle2()  # Получить свойство (**)
        Object.SetAngle2( Angle2 )   # Установить свойство (**)
        ```
        """
        ...
    @Angle2.setter
    def Angle2(self, value: float): ...

    @property
    def Count1(self) -> int:  # icircularcopyobjectparam_count1.html
        """
        ## Count1 - Количество копий в радиальном направлении

        Тип данных: long

        ### Синтаксис Automation

        ```
        Count1 = Object.Count1       # Получить свойство (* )
        Object.Count1 = Count1       # Установить свойство (**)
        Count1 = Object.GetCount1()  # Получить свойство (**)
        Object.SetCount1( Count1 )   # Установить свойство (**)
        ```
        """
        ...
    @Count1.setter
    def Count1(self, value: int): ...

    @property
    def Count2(self) -> int:  # icircularcopyobjectparam_count2.html
        """
        ## Count2 - Количество копий в кольцевом направлении

        Тип данных: long

        ### Синтаксис Automation

        ```
        Count2 = Object.Count2       # Получить свойство (* )
        Object.Count2 = Count2       # Установить свойство (**)
        Count2 = Object.GetCount2()  # Получить свойство (**)
        Object.SetCount2( Count2 )   # Установить свойство (**)
        ```
        """
        ...
    @Count2.setter
    def Count2(self, value: int): ...

    @property
    def Radius(self) -> float:  # icircularcopyobjectparam_radius.html
        """
        ## Radius - Начальный радиус

        Тип данных: double

        ### Синтаксис Automation

        ```
        Radius = Object.Radius       # Получить свойство (* )
        Object.Radius = Radius       # Установить свойство (**)
        Radius = Object.GetRadius()  # Получить свойство (**)
        Object.SetRadius( Radius )   # Установить свойство (**)
        ```
        """
        ...
    @Radius.setter
    def Radius(self, value: float): ...

    @property
    def SaveCentreCopy(self) -> bool:  # icircularcopyobjectparam_savecentrecopy.html
        """
        ## SaveCentreCopy - Признак наличия копии в центре сетки

        Тип данных: BOOL

        Значения свойства:

        +----------------------------------------------+
        | TRUE  | - оставлять копию в центре сетки,    |
        +----------------------------------------------+
        | FALSE | - не оставлять копию в центре сетки. |
        +----------------------------------------------+

        ### Синтаксис Automation

        ```
        SaveCentreCopy = Object.SaveCentreCopy       # Получить свойство (* )
        Object.SaveCentreCopy = SaveCentreCopy       # Установить свойство (**)
        SaveCentreCopy = Object.GetSaveCentreCopy()  # Получить свойство (**)
        Object.SetSaveCentreCopy( SaveCentreCopy )   # Установить свойство (**)
        ```
        """
        ...
    @SaveCentreCopy.setter
    def SaveCentreCopy(self, value: bool): ...

    @property
    def Step1(self) -> float:  # icircularcopyobjectparam_step1.html
        """
        ## Step1 - Шаг копирования

        Тип данных: double

        ### Синтаксис Automation

        ```
        Step1 = Object.Step1       # Получить свойство (* )
        Object.Step1 = Step1       # Установить свойство (**)
        Step1 = Object.GetStep1()  # Получить свойство (**)
        Object.SetStep1( Step1 )   # Установить свойство (**)
        ```
        """
        ...
    @Step1.setter
    def Step1(self, value: float): ...

    @property
    def Step2(self) -> float:  # icircularcopyobjectparam_step2.html
        """
        ## Step2 - Угловой шаг (градусы)

        Тип данных: double

        ### Синтаксис Automation

        ```
        Step2 = Object.Step2       # Получить свойство (* )
        Object.Step2 = Step2       # Установить свойство (**)
        Step2 = Object.GetStep2()  # Получить свойство (**)
        Object.SetStep2( Step2 )   # Установить свойство (**)
        ```
        """
        ...
    @Step2.setter
    def Step2(self, value: float): ...

    @property
    def StepFactor1(self) -> bool:  # icircularcopyobjectparam_stepfactor1.html
        """
        ## StepFactor1 - Интерпретация шага в радиальном направлении: расстояние между соседними копиями или между крайними

        Тип данных: BOOL

        Значения свойства:

        +-----------------------------------------------+
        | TRUE  | - расстояние между соседними копиями, |
        +-----------------------------------------------+
        | FALSE | - расстояние между крайними копиями.  |
        +-----------------------------------------------+

        ### Синтаксис Automation

        ```
        StepFactor1 = Object.StepFactor1       # Получить свойство (* )
        Object.StepFactor1 = StepFactor1       # Установить свойство (**)
        StepFactor1 = Object.GetStepFactor1()  # Получить свойство (**)
        Object.SetStepFactor1( StepFactor1 )   # Установить свойство (**)
        ```
        """
        ...
    @StepFactor1.setter
    def StepFactor1(self, value: bool): ...

    @property
    def StepFactor2(self) -> bool:  # icircularcopyobjectparam_stepfactor2.html
        """
        ## StepFactor2 - Интерпретация шага в кольцевом направлении: угол между соседними копиями или между крайними

        Тип данных: BOOL

        Значения свойства:

        +-----------------------------------------+
        | TRUE  | - угол между соседними копиями, |
        +-----------------------------------------+
        | FALSE | - угол между крайними копиями.  |
        +-----------------------------------------+

        ### Синтаксис Automation

        ```
        StepFactor2 = Object.StepFactor2       # Получить свойство (* )
        Object.StepFactor2 = StepFactor2       # Установить свойство (**)
        StepFactor2 = Object.GetStepFactor2()  # Получить свойство (**)
        Object.SetStepFactor2( StepFactor2 )   # Установить свойство (**)
        ```
        """
        ...
    @StepFactor2.setter
    def StepFactor2(self, value: bool): ...

    @property
    def TurnObject(self) -> bool:  # icircularcopyobjectparam_turnobject.html
        """
        ## TurnObject - Признак доворота копий до радиального направления

        Тип данных: BOOL

        Значения свойства:

        +-------------------------------------------------------------+
        | TRUE  | - доворачивать копии до радиального направления,    |
        +-------------------------------------------------------------+
        | FALSE | - не доворачивать копии до радиального направления. |
        +-------------------------------------------------------------+

        ### Синтаксис Automation

        ```
        TurnObject = Object.TurnObject       # Получить свойство (* )
        Object.TurnObject = TurnObject       # Установить свойство (**)
        TurnObject = Object.GetTurnObject()  # Получить свойство (**)
        Object.SetTurnObject( TurnObject )   # Установить свойство (**)
        ```
        """
        ...
    @TurnObject.setter
    def TurnObject(self, value: bool): ...




class ICurveCopyObjectParam(IKompasAPIObject):  # icurvecopyobjectparam.html
    """
    ## Интерфейс ICurveCopyObjectParam

    Интерфейс параметров копирования объектов вдоль кривой.

    Интерфейс позволяет задавать и получать параметры копирования объектов вдоль кривой.

    Интерфейс является дополнительным для интерфейса ICopyObjectParam. Он может быть получен с помощью метода IUnknown::QueryInterface.
    """

    @property
    def Angle(self):
        ...
    @Angle.setter
    def Angle(self, value): ...

    @property
    def AttributeCopy(self):
        ...
    @AttributeCopy.setter
    def AttributeCopy(self, value): ...

    @property
    def BaseCurve(self) -> IDrawingObject:  # icurvecopyobjectparam_basecurve.html
        """
        ## BaseCurve - Базовая кривая

        Тип данных: указатель на интерфейс IDrawingObject

        Свойство позволяет устанавливать и получать базовую кривую.

        ### Синтаксис Automation

        ```
        BaseCurve = Object.BaseCurve       # Получить свойство (* )
        Object.BaseCurve = BaseCurve       # Установить свойство (**)
        BaseCurve = Object.GetBaseCurve()  # Получить свойство (**)
        Object.SetBaseCurve( BaseCurve )   # Установить свойство (**)
        ```
        """
        ...
    @BaseCurve.setter
    def BaseCurve(self, value: IDrawingObject): ...

    @property
    def Count(self) -> int:  # icurvecopyobjectparam_count.html
        """
        ## Count - Количество копий

        Тип данных: long

        Свойство позволяет устанавливать и получать количество копий.

        ### Синтаксис Automation

        ```
        Count = Object.Count       # Получить свойство (* )
        Object.Count = Count       # Установить свойство (**)
        Count = Object.GetCount()  # Получить свойство (**)
        Object.SetCount( Count )   # Установить свойство (**)
        ```
        """
        ...
    @Count.setter
    def Count(self, value: int): ...

    @property
    def DimensionLineScale(self):
        ...
    @DimensionLineScale.setter
    def DimensionLineScale(self, value): ...

    @property
    def PositiveDirection(self) -> bool:  # icurvecopyobjectparam_positivedirection.html
        """
        ## PositiveDirection - Положительное направление

        Тип данных: BOOL

        Свойство позволяет устанавливать и получать признак положительного направления.

        ### Синтаксис Automation

        ```
        PositiveDirection = Object.PositiveDirection       # Получить свойство (* )
        Object.PositiveDirection = PositiveDirection       # Установить свойство (**)
        PositiveDirection = Object.GetPositiveDirection()  # Получить свойство (**)
        Object.SetPositiveDirection( PositiveDirection )   # Установить свойство (**)
        ```
        """
        ...
    @PositiveDirection.setter
    def PositiveDirection(self, value: bool): ...

    @property
    def Scale(self):
        ...
    @Scale.setter
    def Scale(self, value): ...

    @property
    def SpecificationObjectCopy(self):
        ...
    @SpecificationObjectCopy.setter
    def SpecificationObjectCopy(self, value): ...

    @property
    def Step(self) -> float:  # icurvecopyobjectparam_step.html
        """
        ## Step - Шаг

        Тип данных: double

        Свойство позволяет устанавливать и получать шаг копирования.

        ### Синтаксис Automation

        ```
        Step = Object.Step       # Получить свойство (* )
        Object.Step = Step       # Установить свойство (**)
        Step = Object.GetStep()  # Получить свойство (**)
        Object.SetStep( Step )   # Установить свойство (**)
        ```
        """
        ...
    @Step.setter
    def Step(self, value: float): ...

    @property
    def StepFactor(self) -> bool:  # icurvecopyobjectparam_stepfactor.html
        """
        ## StepFactor - Способ задания шага

        Тип данных: BOOL

        Значения свойства:

        +----------------------------------------------+
        | TRUE  | - шаг задан между крайними копиями,  |
        +----------------------------------------------+
        | FALSE | - шаг задан между соседними копиями. |
        +----------------------------------------------+

        Свойство позволяет устанавливать и получать способ задания шага.

        ### Синтаксис Automation

        ```
        StepFactor = Object.StepFactor       # Получить свойство (* )
        Object.StepFactor = StepFactor       # Установить свойство (**)
        StepFactor = Object.GetStepFactor()  # Получить свойство (**)
        Object.SetStepFactor( StepFactor )   # Установить свойство (**)
        ```
        """
        ...
    @StepFactor.setter
    def StepFactor(self, value: bool): ...

    @property
    def TurnToNormal(self) -> bool:  # icurvecopyobjectparam_turntonormal.html
        """
        ## TurnToNormal - Доворачивать до нормали

        Тип данных: BOOL

        Свойство позволяет устанавливать и получать признак доворачивания до нормали.

        ### Синтаксис Automation

        ```
        TurnToNormal = Object.TurnToNormal       # Получить свойство (* )
        Object.TurnToNormal = TurnToNormal       # Установить свойство (**)
        TurnToNormal = Object.GetTurnToNormal()  # Получить свойство (**)
        Object.SetTurnToNormal( TurnToNormal )   # Установить свойство (**)
        ```
        """
        ...
    @TurnToNormal.setter
    def TurnToNormal(self, value: bool): ...

    @property
    def XNew(self):
        ...
    @XNew.setter
    def XNew(self, value): ...

    @property
    def XOld(self):
        ...
    @XOld.setter
    def XOld(self, value): ...

    @property
    def YNew(self):
        ...
    @YNew.setter
    def YNew(self, value): ...

    @property
    def YOld(self):
        ...
    @YOld.setter
    def YOld(self, value): ...




class IMeshCopyObjectParam(ICopyObjectParam):  # imeshcopyobjectparam.html
    """
    ## Интерфейс IMeshCopyObjectParam

    Параметры копирования объектов по параллелограммной сетке

    Интерфейс можно получить с помощью функции IKompasDocument1::GetInterface.
    """

    @property
    def Angle1(self) -> float:  # imeshcopyobjectparam_angle1.html
        """
        ## Angle1 - Угол наклона первой оси сетки

        Тип данных: double

        ### Синтаксис Automation

        ```
        Angle1 = Object.Angle1       # Получить свойство (* )
        Object.Angle1 = Angle1       # Установить свойство (**)
        Angle1 = Object.GetAngle1()  # Получить свойство (**)
        Object.SetAngle1( Angle1 )   # Установить свойство (**)
        ```
        """
        ...
    @Angle1.setter
    def Angle1(self, value: float): ...

    @property
    def Angle2(self) -> float:  # imeshcopyobjectparam_angle2.html
        """
        ## Angle2 - Угол между первой и второй осями сетки

        Тип данных: double

        ### Синтаксис Automation

        ```
        Angle2 = Object.Angle2       # Получить свойство (* )
        Object.Angle2 = Angle2       # Установить свойство (**)
        Angle2 = Object.GetAngle2()  # Получить свойство (**)
        Object.SetAngle2( Angle2 )   # Установить свойство (**)
        ```
        """
        ...
    @Angle2.setter
    def Angle2(self, value: float): ...

    @property
    def Count1(self) -> int:  # imeshcopyobjectparam_count1.html
        """
        ## Count1 - Количество копий по оси 1

        Тип данных: long

        ### Синтаксис Automation

        ```
        Count1 = Object.Count1       # Получить свойство (* )
        Object.Count1 = Count1       # Установить свойство (**)
        Count1 = Object.GetCount1()  # Получить свойство (**)
        Object.SetCount1( Count1 )   # Установить свойство (**)
        ```
        """
        ...
    @Count1.setter
    def Count1(self, value: int): ...

    @property
    def Count2(self) -> int:  # imeshcopyobjectparam_count2.html
        """
        ## Count2 - Количество копий по оси 2

        Тип данных: long

        ### Синтаксис Automation

        ```
        Count2 = Object.Count2       # Получить свойство (* )
        Object.Count2 = Count2       # Установить свойство (**)
        Count2 = Object.GetCount2()  # Получить свойство (**)
        Object.SetCount2( Count2 )   # Установить свойство (**)
        ```
        """
        ...
    @Count2.setter
    def Count2(self, value: int): ...

    @property
    def SaveCentreCopy(self) -> bool:  # imeshcopyobjectparam_savecentrecopy.html
        """
        ## SaveCentreCopy - Признак наличия копий внутри сетки

        Тип данных: BOOL

        Значения свойства:

        +----------------------------------------------+
        | TRUE  | - оставлять копию в центре сетки,    |
        +----------------------------------------------+
        | FALSE | - не оставлять копию в центре сетки. |
        +----------------------------------------------+

        ### Синтаксис Automation

        ```
        SaveCentreCopy = Object.SaveCentreCopy       # Получить свойство (* )
        Object.SaveCentreCopy = SaveCentreCopy       # Установить свойство (**)
        SaveCentreCopy = Object.GetSaveCentreCopy()  # Получить свойство (**)
        Object.SetSaveCentreCopy( SaveCentreCopy )   # Установить свойство (**)
        ```
        """
        ...
    @SaveCentreCopy.setter
    def SaveCentreCopy(self, value: bool): ...

    @property
    def SaveCornersCopy(self) -> bool:  # imeshcopyobjectparam_savecornerscopy.html
        """
        ## SaveCornersCopy - Признак наличия копий в углах сетки

        Тип данных: BOOL

        Значения свойства:

        +---------------------------------------------+
        | TRUE  | - оставлять копии в углах сетки,    |
        +---------------------------------------------+
        | FALSE | - не оставлять копии в углах сетки. |
        +---------------------------------------------+

        ### Синтаксис Automation

        ```
        SaveCornersCopy = Object.SaveCornersCopy       # Получить свойство (* )
        Object.SaveCornersCopy = SaveCornersCopy       # Установить свойство (**)
        SaveCornersCopy = Object.GetSaveCornersCopy()  # Получить свойство (**)
        Object.SetSaveCornersCopy( SaveCornersCopy )   # Установить свойство (**)
        ```
        """
        ...
    @SaveCornersCopy.setter
    def SaveCornersCopy(self, value: bool): ...

    @property
    def Step1(self) -> float:  # imeshcopyobjectparam_step1.html
        """
        ## Step1 - Шаг копирования по оси 1

        Тип данных: double

        ### Синтаксис Automation

        ```
        Step1 = Object.Step1       # Получить свойство (* )
        Object.Step1 = Step1       # Установить свойство (**)
        Step1 = Object.GetStep1()  # Получить свойство (**)
        Object.SetStep1( Step1 )   # Установить свойство (**)
        ```
        """
        ...
    @Step1.setter
    def Step1(self, value: float): ...

    @property
    def Step2(self) -> float:  # imeshcopyobjectparam_step2.html
        """
        ## Step2 - Шаг копирования по оси 2

        Тип данных: double

        ### Синтаксис Automation

        ```
        Step2 = Object.Step2       # Получить свойство (* )
        Object.Step2 = Step2       # Установить свойство (**)
        Step2 = Object.GetStep2()  # Получить свойство (**)
        Object.SetStep2( Step2 )   # Установить свойство (**)
        ```
        """
        ...
    @Step2.setter
    def Step2(self, value: float): ...

    @property
    def StepFactor1(self) -> bool:  # imeshcopyobjectparam_stepfactor1.html
        """
        ## StepFactor1 - Интерпретация шага по первой оси: расстояние между соседними копиями или между крайними копиями

        Тип данных: BOOL

        Значения свойства:

        +-----------------------------------------------+
        | TRUE  | - расстояние между соседними копиями, |
        +-----------------------------------------------+
        | FALSE | - расстояние между крайними копиями.  |
        +-----------------------------------------------+

        ### Синтаксис Automation

        ```
        StepFactor1 = Object.StepFactor1       # Получить свойство (* )
        Object.StepFactor1 = StepFactor1       # Установить свойство (**)
        StepFactor1 = Object.GetStepFactor1()  # Получить свойство (**)
        Object.SetStepFactor1( StepFactor1 )   # Установить свойство (**)
        ```
        """
        ...
    @StepFactor1.setter
    def StepFactor1(self, value: bool): ...

    @property
    def StepFactor2(self) -> bool:  # imeshcopyobjectparam_stepfactor2.html
        """
        ## StepFactor2 - Интерпретация шага по второй оси: расстояние между соседними копиями или между крайними копиями

        Тип данных: BOOL

        Значения свойства:

        +-----------------------------------------------+
        | TRUE  | - расстояние между соседними копиями, |
        +-----------------------------------------------+
        | FALSE | - расстояние между крайними копиями.  |
        +-----------------------------------------------+

        ### Синтаксис Automation

        ```
        StepFactor2 = Object.StepFactor2       # Получить свойство (* )
        Object.StepFactor2 = StepFactor2       # Установить свойство (**)
        StepFactor2 = Object.GetStepFactor2()  # Получить свойство (**)
        Object.SetStepFactor2( StepFactor2 )   # Установить свойство (**)
        ```
        """
        ...
    @StepFactor2.setter
    def StepFactor2(self, value: bool): ...




class IFindObjectParameters(IKompasAPIObject):  # ifindobjectparameters.html
    """
    ## Интерфейс IFindObjectParameters

    Параметры поиска объектов.
    """

    @property
    def DisabledObjects(self) -> typing.Any:  # ifindobjectparameters_disabledobjects.html
        """
        ## DisabledObjects - Игнорируемые объекты

        Тип данных: VARIANT

        1. Позволяет получать и устанавливать объект или список объектов, игнорируемые при поиске.

        2. Массив возвращается в виде массива SAFEARRAY объектов LPDISPATCH (VT_ARRAY | VT_DISPATCH).

        ### Синтаксис Automation

        ```
        DisabledObjects = Object.DisabledObjects       # Получить свойство (* )
        Object.DisabledObjects = DisabledObjects       # Установить свойство (**)
        DisabledObjects = Object.GetDisabledObjects()  # Получить свойство (**)
        Object.SetDisabledObjects( DisabledObjects )   # Установить свойство (**)
        ```
        """
        ...
    @DisabledObjects.setter
    def DisabledObjects(self, value: typing.Any): ...

    @property
    def DisabledViews(self) -> typing.Any:  # ifindobjectparameters_disabledviews.html
        """
        ## DisabledViews - Игнорируемые виды

        Тип данных: VARIANT

        1. Позволяет получать и устанавливать вид или список видов, игнорируемые при поиске.

        2. Массив возвращается в виде массива SAFEARRAY видов LPDISPATCH (VT_ARRAY | VT_DISPATCH).

        ### Синтаксис Automation

        ```
        DisabledViews = Object.DisabledViews       # Получить свойство (* )
        Object.DisabledViews = DisabledViews       # Установить свойство (**)
        DisabledViews = Object.GetDisabledViews()  # Получить свойство (**)
        Object.SetDisabledViews( DisabledViews )   # Установить свойство (**)
        ```
        """
        ...
    @DisabledViews.setter
    def DisabledViews(self, value: typing.Any): ...

    @property
    def DrawingObjectType(self) -> int:  # ifindobjectparameters_drawingobjecttype.html
        """
        ## DrawingObjectType - Фильтрация по типу графического объекта

        Тип данных: из перечисления DrawingObjectTypeEnum

        ### Синтаксис Automation

        ```
        Object.DrawingObjectType = DrawingObjectType       # Получить свойство (* )
        Object.FindObjectsType = FindObjectsType           # Установить свойство (**)
        DrawingObjectType = Object.GetDrawingObjectType()  # Получить свойство (**)
        Object.SetDrawingObjectType( DrawingObjectType )   # Установить свойство (**)
        ```
        """
        ...
    @DrawingObjectType.setter
    def DrawingObjectType(self, value: int): ...

    @property
    def FindInBackgroundViewsAndLayers(self) -> bool:  # ifindobjectparameters_findinbackgroundviewsandlayers.html
        """
        ## FindInBackgroundViewsAndLayers - Искать в фоновых видах и слоях

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        FindInBackgroundViewsAndLayers = Object.FindInBackgroundViewsAndLayers       # Получить свойство (* )
        Object.FindInBackgroundViewsAndLayers = FindInBackgroundViewsAndLayers       # Установить свойство (**)
        FindInBackgroundViewsAndLayers = Object.GetFindInBackgroundViewsAndLayers()  # Получить свойство (**)
        Object.SetFindInBackgroundViewsAndLayers( FindInBackgroundViewsAndLayers )   # Установить свойство (**)
        ```
        """
        ...
    @FindInBackgroundViewsAndLayers.setter
    def FindInBackgroundViewsAndLayers(self, value: bool): ...

    @property
    def FindObjectsType(self) -> int:  # ifindobjectparameters_findobjectstype.html
        """
        ## FindObjectsType - Тип поиска объектов

        Тип данных: из перечисления ksFindObjectsTypeEnum

        ### Синтаксис Automation

        ```
        FindObjectsType = Object.FindObjectsType       # Получить свойство (* )
        Object.FindObjectsType = FindObjectsType       # Установить свойство (**)
        FindObjectsType = Object.GetFindObjectsType()  # Получить свойство (**)
        Object.SetFindObjectsType( FindObjectsType )   # Установить свойство (**)
        ```
        """
        ...
    @FindObjectsType.setter
    def FindObjectsType(self, value: int): ...

    @property
    def GeometryOnly(self) -> bool:  # ifindobjectparameters_geometryonly.html
        """
        ## GeometryOnly - Искать только среди геометрических объектов

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        GeometryOnly = Object.GeometryOnly       # Получить свойство (* )
        Object.GeometryOnly = GeometryOnly       # Установить свойство (**)
        GeometryOnly = Object.GetGeometryOnly()  # Получить свойство (**)
        Object.SetGeometryOnly( GeometryOnly )   # Установить свойство (**)
        ```
        """
        ...
    @GeometryOnly.setter
    def GeometryOnly(self, value: bool): ...

    def Clear(self) -> bool:  # ifindobjectparameters_clear.html
        """
        ## Clear - Очистить параметры поиска

        ### Синтаксис Automation

        ```
        BOOL Clear();
        ```

        ### Возвращаемое значение

        +-----------------------------------------+
        | TRUE | - в случае успешного завершения. |
        +-----------------------------------------+
        """
        ...




class IAutoNumerationManager(IDispatch):  # iautonumerationmanager.html
    """
    ## Интерфейс IAutoNumerationManager

    Интерфейс Менеджер автонумерации.

    1. Интерфейс является дополнительным к интерфейсу 2D документа IKompasDocument2D.

    2. В 2D документе создается три системные группы нумерации:

    •#Автосортировка.

    •#Нумерация обозначений изменений.

    •#Нумерация таблиц.

    Дополнительно могут быть созданы пользовательские группы нумерации.

    Версия: Компас v20
    """

    @property
    def CurrentNumericGroup(self) -> INumericGroup:  # iautonumerationmanager_currentnumericgroup.html
        """
        ## CurrentNumericGroup - Текущая группа нумерации

        Тип данных: Указатель на интерфейс INumericGroup.

        ### Синтаксис Automation

        ```
        CurrentNumericGroup = Object.CurrentNumericGroup       # Получить свойство (* )
        Object.CurrentNumericGroup = CurrentNumericGroup       # Установить свойство (**)
        CurrentNumericGroup = Object.GetCurrentNumericGroup()  # Получить свойство (**)
        Object.SetCurrentNumericGroup( CurrentNumericGroup )   # Установить свойство (**)
        ```
        """
        ...
    @CurrentNumericGroup.setter
    def CurrentNumericGroup(self, value: INumericGroup): ...

    @property
    def NumericGroupsCount(self) -> int:  # iautonumerationmanager_numericgroupscount.html
        """
        ## NumericGroupsCount - Количество групп нумерации

        Тип данных: long

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        NumericGroupsCount = Object.NumericGroupsCount       # Получить свойство(* )
        NumericGroupsCount = Object.GetNumericGroupsCount()  # Получить свойство (**)
        ```
        """
        ...

    def AddNumericGroup(self, Name) -> INumericGroup:  # iautonumerationmanager_addnumericgroup.html
        """
        ## AddNumericGroup - Добавить группу нумерации

        ### Синтаксис Automation

        ```
        LPDISPATCH AddNumericGroup( BSTR Name );
        ```

        ### Входные параметры

        +----------------------+
        | Name | - Имя группы. |
        +----------------------+

        ### Возвращаемое значение

        - Указатель на интерфейс INumericGroup.
        """
        ...

    def NumericGroup(self, Index) -> INumericGroup:  # iautonumerationmanager_numericgroup.html
        """
        ## NumericGroup - Получить группу нумерации по индексу, имени или объекту, входящему в группу

        Тип данных: Указатель на интерфейс INumericGroup.

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        NumericGroup= Object.NumericGroup( Index )     # Получить свойство(* )
        NumericGroup= Object.GetNumericGroup( Index )  # Получить свойство (**)
        ```

        ### Входные параметры

        +-------------------------------------------------------------------------------------------------------------------------------+
        | VARIANT Index | - Индекс группы в коллекции. Поддерживаются следующие типы: - VT_I4 - индекс объекта, - VT_BSTR - имя группы. |
        +-------------------------------------------------------------------------------------------------------------------------------+
        """
        ...




class INumericGroup(IKompasAPIObject):  # inumericgroup.html
    """
    ## Интерфейс INumericGroup

    Интерфейс Группа автонумерации.

    Интерфейс можно получить с помощью методов интерфейса IAutoNumerationManager.

    Версия: Компас v20
    """

    @property
    def AutoNumerationEnable(self) -> bool:  # inumericgroup_autonumerationenable.html
        """
        ## AutoNumerationEnable - Автоперестроение нумерации

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        AutoNumerationEnable = Object.AutoNumerationEnable        # Получить свойство (* )
        Object.AutoNumerationEnable =AutoNumerationEnable         # Установить свойство (**)
        AutoNumerationEnable = Object.GetAutoNumerationEnable ()  # Получить свойство (**)
        Object.SetAutoNumerationEnable (AutoNumerationEnable )    # Установить свойство (**)
        ```
        """
        ...
    @AutoNumerationEnable.setter
    def AutoNumerationEnable(self, value: bool): ...

    @property
    def AutoNumerationSymbols(self) -> str:  # inumericgroup_autonumerationsymbols.html
        """
        ## AutoNumerationSymbols - Набор символов

        Тип данных: BSTR

        Свойство доступно для групп с буквенной нумерацией

        INumericGroup::NumericGroupType = ksNumericGroupSymbols.

        ### Синтаксис Automation

        ```
        AutoNumerationSymbols = Object.AutoNumerationSymbols        # Получить свойство (* )
        Object.AutoNumerationSymbols = AutoNumerationSymbols        # Установить свойство (**)
        AutoNumerationSymbols = Object.GetAutoNumerationSymbols ()  # Получить свойство (**)
        Object.SetAutoNumerationSymbols ( AutoNumerationSymbols )   # Установить свойство (**)
        ```
        """
        ...
    @AutoNumerationSymbols.setter
    def AutoNumerationSymbols(self, value: str): ...

    @property
    def Count(self) -> int:  # inumericgroup_count.html
        """
        ## Count - Количество объектов в группе

        Тип данных: long

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        Count = Object.Count       # Получить свойство(* )
        Count = Object.GetCount()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def IsSystem(self) -> bool:  # inumericgroup_issystem.html
        """
        ## IsSystem - Признак системной группы

        Тип данных: BOOL

        Свойство доступно только для чтения.

        В 2D документе создается три системные группы нумерации:

        •#Автосортировка,

        •#Нумерация обозначений изменений,

        •#Нумерация таблиц.

        Дополнительно могут быть созданы пользовательские группы нумерации.

        ### Синтаксис Automation

        ```
        IsSystem = Object.IsSystem       # Получить свойство(* )
        IsSystem = Object.GetIsSystem()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def MinCharsCount(self) -> int:  # inumericgroup_mincharscount.html
        """
        ## MinCharsCount - Минимальное число символов в номере, если меньше этого значения, то дополняется нулями слева

        Тип данных: long

        Свойство доступно для групп с числовой нумерацией

        INumericGroup::NumericGroupType = ksNumericGroupNumbers.

        ### Синтаксис Automation

        ```
        MinCharsCount = Object.MinCharsCount        # Получить свойство (* )
        Object.MinCharsCount = MinCharsCount        # Установить свойство (**)
        MinCharsCount = Object.GetMinCharsCount ()  # Получить свойство (**)
        Object.SetMinCharsCount ( MinCharsCount )   # Установить свойство (**)
        ```
        """
        ...
    @MinCharsCount.setter
    def MinCharsCount(self, value: int): ...

    @property
    def Name(self) -> str:  # inumericgroup_name.html
        """
        ## Name - Имя группы

        Тип данных: BSTR

        Изменение имени доступно только для пользовательских групп нумерации.

        ### Синтаксис Automation

        ```
        Name = Object. Name        # Получить свойство (* )
        Object. Name = Name        # Установить свойство (**)
        Name = Object.Get Name ()  # Получить свойство (**)
        Object.Set Name ( Name )   # Установить свойство (**)
        ```
        """
        ...
    @Name.setter
    def Name(self, value: str): ...

    @property
    def NumericGroupType(self) -> int:  # inumericgroup_numericgrouptype.html
        """
        ## NumericGroupType - Тип группы нумерации. Тип номера

        Тип данных: из перечисления ksObj3dTypeEnum

        ### Синтаксис Automation

        ```
        NumericGroupType = Object.NumericGroupType        # Получить свойство (* )
        Object.NumericGroupType =NumericGroupType         # Установить свойство (**)
        NumericGroupType = Object.GetNumericGroupType ()  # Получить свойство (**)
        Object.SetNumericGroupType (NumericGroupType )    # Установить свойство (**)
        ```
        """
        ...
    @NumericGroupType.setter
    def NumericGroupType(self, value: int): ...

    @property
    def Objects(self) -> typing.Any:  # inumericgroup_objects.html
        """
        ## Objects - Список объектов в группе

        Тип данных: VARIANT

        ### Синтаксис Automation

        ```
        Objects = Object.Objects        # Получить свойство (* )
        Object.Objects = Objects        # Установить свойство (**)
        Objects = Object.GetObjects ()  # Получить свойство (**)
        Object.SetObjects ( Objects )   # Установить свойство (**)
        ```
        """
        ...
    @Objects.setter
    def Objects(self, value: typing.Any): ...

    @property
    def StartIndex(self) -> int:  # inumericgroup_startindex.html
        """
        ## StartIndex - Стартовый индекс. Начинать с

        Тип данных: long

        ### Синтаксис Automation

        ```
        StartIndex = Object.StartIndex        # Получить свойство (* )
        Object.StartIndex =StartIndex         # Установить свойство (**)
        StartIndex = Object.GetStartIndex ()  # Получить свойство (**)
        Object.SetStartIndex (StartIndex )    # Установить свойство (**)
        ```
        """
        ...
    @StartIndex.setter
    def StartIndex(self, value: int): ...

    @property
    def TextAfter(self) -> str:  # inumericgroup_textafter.html
        """
        ## TextAfter - Текст после номера

        Тип данных: BSTR

        ### Синтаксис Automation

        ```
        TextAfter = Object.TextAfter        # Получить свойство (* )
        Object.TextAfter =TextAfter         # Установить свойство (**)
        TextAfter = Object.GetTextAfter ()  # Получить свойство (**)
        Object.SetTextAfter (TextAfter )    # Установить свойство (**)
        ```
        """
        ...
    @TextAfter.setter
    def TextAfter(self, value: str): ...

    @property
    def TextBefore(self) -> str:  # inumericgroup_textbefore.html
        """
        ## TextBefore - Текст перед номером

        Тип данных: BSTR

        ### Синтаксис Automation

        ```
        TextBefore = Object.TextBefore        # Получить свойство (* )
        Object.TextBefore = TextBefore        # Установить свойство (**)
        TextBefore = Object.GetTextBefore ()  # Получить свойство (**)
        Object.SetTextBefore ( TextBefore )   # Установить свойство (**)
        ```
        """
        ...
    @TextBefore.setter
    def TextBefore(self, value: str): ...

    def Attach(self, Object) -> bool:  # inumericgroup_attach.html
        """
        ## Attach - Добавить объект в группу

        ### Синтаксис Automation

        ```
        BOOL Attach( LPDISPATCH Object );
        ```

        ### Входные параметры

        +-------------------------------------------------------------+
        | Object | - указатель на интерфейс объекта IKompasAPIObject, |
        +-------------------------------------------------------------+

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def Delete(self) -> bool:  # inumericgroup_delete.html
        """
        ## Delete - Удалить пользовательскую группу

        ### Синтаксис Automation

        ```
        BOOL Delete();
        ```

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def Dettach(self, Object) -> bool:  # inumericgroup_dettach.html
        """
        ## Dettach - Исключить объект из группы

        ### Синтаксис Automation

        ```
        BOOL Dettach( LPDISPATCH Object );
        ```

        ### Входные параметры

        +-------------------------------------------------------------+
        | Object | - указатель на интерфейс объекта IKompasAPIObject, |
        +-------------------------------------------------------------+

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def GetNextName(self, Object):  # inumericgroup_getnextname.html
        """
        ## GetNextName - Получить имя следующего объекта в группе

        Метод используется для предварительного получения имени объекта перед добавлением в группу нумерации.

        ### Синтаксис Automation

        ```
        BSTR GetNextName( LPDISPATCH Object );
        ```

        ### Входные параметры

        +-------------------------------------------------------------+
        | Object | - указатель на интерфейс объекта IKompasAPIObject, |
        +-------------------------------------------------------------+

        ### Возвращаемое значение

        - Имя следующего объекта в группе.
        """
        ...

    def GetObjectNumber(self, Object, FullName):  # inumericgroup_getobjectnumber.html
        """
        ## GetObjectNumber - Строковый номер объекта в группе

        ### Синтаксис Automation

        ```
        BSTR GetObjectNumber( LPDISPATCH Object, BOOL FullName );
        ```

        ### Входные параметры

        +---------------------------------------------------------------+
        | Object   | - указатель на интерфейс объекта IKompasAPIObject, |
        +---------------------------------------------------------------+
        | FullName | - TRUE - вернуть полное имя, FALSE - только номер. |
        +---------------------------------------------------------------+

        ### Возвращаемое значение

        - Строковый номер объекта в группе.
        """
        ...

    def IsEnableAdd(self, Object) -> bool:  # inumericgroup_isenableadd.html
        """
        ## IsEnableAdd - Можно ли добавить объект в группу

        1. Возможно объект уже есть в группе.

        2. Не для всех типов объектов есть поддержка нумерации.

        •В системных группах есть фильтр по типу объектов.

        •Для группы #Автосортировка разрешено добавление объектов:

        •Стрелка взгляда.

        •Выносной элемент.

        •Линия разреза\\сечения.

        •Обозначение базы.

        •Линия-выноска.

        •Размеры.

        •В настройках документа есть управление автоматическим добавлением данных объектов в группу и задана последовательность сортировки.

        •В группу #Нумерация обозначений изменений разрешено добавление:

        •Обозначение изменения.

        •В группу #Нумерация таблиц разрешено добавление:

        •Таблиц и ассоциативных таблиц отчетов.

        •Для пользовательских групп доступно добавление любых из выше перечисленных объектов. Другие объекты в группу добавить нельзя.

        ### Синтаксис Automation

        ```
        BOOL IsEnableAdd( LPDISPATCH Object );
        ```

        ### Входные параметры

        +-------------------------------------------------------------+
        | Object | - указатель на интерфейс объекта IKompasAPIObject, |
        +-------------------------------------------------------------+

        ### Возвращаемое значение

        +--------------------------------------------+
        | TRUE  | - объект можно добавить в группу,  |
        +--------------------------------------------+
        | FALSE | - объект в группу добавить нельзя. |
        +--------------------------------------------+
        """
        ...

    def IsExist(self, Object) -> bool:  # inumericgroup_isexist.html
        """
        ## IsExist - Входит ли объект в группу

        ### Синтаксис Automation

        ```
        BOOL IsExist( LPDISPATCH Object );
        ```

        ### Входные параметры

        +-------------------------------------------------------------+
        | Object | - указатель на интерфейс объекта IKompasAPIObject, |
        +-------------------------------------------------------------+

        ### Возвращаемое значение

        +-------------------------------------+
        | TRUE  | - объект уже есть в группе, |
        +-------------------------------------+
        | FALSE | - объекта нет в группе.     |
        +-------------------------------------+
        """
        ...

    def Item(self, Index) -> IKompasAPIObject:  # inumericgroup_item.html
        """
        ## Item - Получить объект по индексу или строковому обозначению в группе (зависит от правил нумерации)

        Тип данных: Указатель на интерфейс IKompasAPIObject

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        Item = Object.Item( Index )    # Получить свойство(* )
        Item = Object.GetItem(Index )  # Получить свойство (**)
        ```

        ### Входные параметры

        +---------------------------------------------------------------------------------------------------------------------------------------------------------+
        | VARIANT Index | - Индекс объекта в группе. Поддерживаются следующие типы: - VT_I4 - индекс объекта, - VT_BSTR - строковое обозначение объекта в группе. |
        +---------------------------------------------------------------------------------------------------------------------------------------------------------+
        """
        ...




class IAutoSortObjectsParameters(IDispatch):  # iautosortobjectsparameters.html
    """
    ## Интерфейс IAutoSortObjectsParameters

    Интерфейс параметров автосортировки буквенных обозначений.

    Интерфейс можно получить с помощью метода IDocument2DSettings::AutoSortObjectsParameters.

    Версия: Компас v20
    """

    @property
    def AutoNumerationObjects(self) -> typing.Any:  # iautosortobjectsparameters_autonumerationobjects.html
        """
        ## AutoNumerationObjects - Порядок сортировки

        Тип данных: VARIANT

        ### Синтаксис Automation

        ```
        AutoNumerationObjects = Object.AutoNumerationObjects       # Получить свойство (* )
        Object.AutoNumerationObjects = AutoNumerationObjects       # Установить свойство (* )
        AutoNumerationObjects = Object.GetAutoNumerationObjects()  # Получить свойство(**)
        Object.SetAutoNumerationObjects( AutoNumerationObjects )   # Установить свойство (**)
        ```

        ### Возвращаемое значение

        - Возвращается массив SafeArray VT_ARRAY | VT_I4 c идентификаторами типов объектов из перечисления DrawingObjectTypeEnum.
        """
        ...
    @AutoNumerationObjects.setter
    def AutoNumerationObjects(self, value: typing.Any): ...

    @property
    def AutoNumerationSymbols(self) -> str:  # iautosortobjectsparameters_autonumerationsymbols.html
        """
        ## AutoNumerationSymbols - Набор символов

        Тип данных: BSTR

        ### Синтаксис Automation

        ```
        AutoNumerationSymbols = Object.AutoNumerationSymbols       # Получить свойство (* )
        Object.AutoNumerationSymbols = AutoNumerationSymbols       # Установить свойство (* )
        AutoNumerationSymbols = Object.GetAutoNumerationSymbols()  # Получить свойство(**)
        Object.SetAutoNumerationSymbols( AutoNumerationSymbols )   # Установить свойство (**)
        ```
        """
        ...
    @AutoNumerationSymbols.setter
    def AutoNumerationSymbols(self, value: str): ...

    def AutoNumerationEnable(self, ObjectType) -> bool:  # iautosortobjectsparameters_autonumerationenable.html
        """
        ## AutoNumerationEnable - Включить автосортировку для объектов по типу

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        AutoNumerationEnable = Object.AutoNumerationEnable( ObjectType )     # Получить свойство (* )
        Object.AutoNumerationEnable( ObjectType ) = AutoNumerationEnable     # Установить свойство (* )
        AutoNumerationEnable = Object.GetAutoNumerationEnable( ObjectType )  # Получить свойство(**)
        Object.SetAutoNumerationEnable( ObjectType, AutoNumerationEnable )   # Установить свойство (**)
        ```

        ### Входные параметры

        +------------------------------------------------------------------------+
        | long ObjectType | - тип объекта из перечисления DrawingObjectTypeEnum. |
        +------------------------------------------------------------------------+
        """
        ...

    def SetAutoNumerationEnable(self, ObjectType, arg1):
        ...




class ITable(IDispatch):  # itable.html
    """
    ## Интерфейс ITable

    Интерфейс сетки таблицы.

    Интерфейс можно получить с помощью следующих методов:

    •IToleranceParam::Table у интерфейса параметров обозначения допуска формы,

    •метода IUnknown::QueryInterface у следующих интерфейсов:

    •интерфейса таблицы на чертеже IDrawingTable,

    •допуска формы ITolerance и ITolerance3D.
    """

    @property
    def ColumnsCount(self) -> int:  # itable_columnscount.html
        """
        ## ColumnsCount - Количество столбцов

        Тип данных: long

        1. Свойство позволяет получить количество столбцов таблицы.

        2. Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        ColumnsCount = Object.ColumnsCount       # Получить свойство(* )
        ColumnsCount = Object.GetColumnsCount()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def RowsCount(self) -> int:  # itable_rowscount.html
        """
        ## RowsCount - Количество строк

        Тип данных: long

        1. Свойство позволяет получить количество строк таблицы.

        2. Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        RowsCount = Object.RowsCount       # Получить свойство(* )
        RowsCount = Object.GetRowsCount()  # Получить свойство (**)
        ```
        """
        ...

    def AddColumn(self, ColN, Right) -> bool:  # itable_addcolumn.html
        """
        ## AddColumn - Добавить колонку

        Позволяет добавить в таблицу столбец справа или слева от указанного.

        ### Синтаксис Automation

        ```
        BOOL AddColumn( long ColN,
        BOOL right );
        ```

        ### Входные параметры

        +------------------------------------------------------------------------------+
        | ColN  | - номер столбца, относительно которого нужно вставить новый столбец, |
        +------------------------------------------------------------------------------+
        | right | - справа (TRUE) или слева (FALSE) от указанного.                     |
        +------------------------------------------------------------------------------+

        ### Возвращаемое значение

        +-----------------------------+
        | TRUE  | - в случае успеха,  |
        +-----------------------------+
        | FALSE | - в случае неудачи. |
        +-----------------------------+
        """
        ...

    def AddRow(self, RowN, Down) -> bool:  # itable_addrow.html
        """
        ## AddRow - Добавить строку

        ### Синтаксис Automation

        ```
        BOOL AddRow( long RowN, BOOL down );
        ```

        ### Входные параметры

        +--------------------------------------------------------------------------+
        | RowN | - номер строки, относительно которой нужно вставить новую строку, |
        +--------------------------------------------------------------------------+
        | down | - снизу (TRUE) или сверху (FALSE) от указанной.                   |
        +--------------------------------------------------------------------------+

        ### Возвращаемое значение

        +-----------------------------+
        | TRUE  | - в случае успеха,  |
        +-----------------------------+
        | FALSE | - в случае неудачи. |
        +-----------------------------+
        """
        ...

    def Cell(self, RowN, ColN) -> ITableCell:  # itable_cell.html
        """
        ## Cell - Значение в ячейке таблицы

        Тип данных: указатель на интерфейс ITableCell

        1. Свойство позволяет получить интерфейс ячейки таблицы.

        2. Свойство доступно только для чтения.

        3. Для допуска формы возвращается интерфейс ITextLine.

        ### Синтаксис Automation

        ```
        Cell = Object.Cell (RowN, ColN)     # Получить свойство(* )
        Cell = Object.GetCell (RowN, ColN)  # Получить свойство (**)
        ```

        ### Входные параметры

        +-------------------------+
        | RowN | - номер строки,  |
        +-------------------------+
        | ColN | - номер колонки. |
        +-------------------------+
        """
        ...

    def CellById(self, CellID) -> ITableCell:  # itable_cellbyid.html
        """
        ## CellById - Значение в ячейке таблицы, заданное по идентификатору

        Тип данных: указатель на интерфейс ITableCell

        1. Свойство позволяет получить интерфейс ячейки таблицы.

        2. Свойство доступно только для чтения.

        3. Идентификаторы ячеек задаются, начиная с 1.

        ### Синтаксис Automation

        ```
        CellById = Object.CellById( CellID )     # Получить свойство(* )
        CellById = Object.GetCellById( CellID )  # Получить свойство (**)
        ```

        ### Входные параметры

        +----------------------------------+
        | CellID | - Идентификатор ячейки. |
        +----------------------------------+
        """
        ...

    def Clear(self) -> bool:  # itable_clear.html
        """
        ## Clear - Очистить всю таблицу

        Позволяет очистить таблицу.

        ### Синтаксис Automation

        ```
        BOOL Clear();
        ```

        ### Входные параметры

        +------------------------------------+
        | ColN | - номер удаляемого столбца. |
        +------------------------------------+

        ### Возвращаемое значение

        +-----------------------------+
        | TRUE  | - в случае успеха,  |
        +-----------------------------+
        | FALSE | - в случае неудачи. |
        +-----------------------------+
        """
        ...

    def DeleteColumn(self, ColN) -> bool:  # itable_deletecolumn.html
        """
        ## DeleteColumn - Удалить колонку из таблицы

        Позволяет удалить столбец из таблицы.

        ### Синтаксис Automation

        ```
        BOOL DeleteColumn( long ColN );
        ```

        ### Входные параметры

        +------------------------------------+
        | ColN | - номер удаляемого столбца. |
        +------------------------------------+

        ### Возвращаемое значение

        +-----------------------------+
        | TRUE  | - в случае успеха,  |
        +-----------------------------+
        | FALSE | - в случае неудачи. |
        +-----------------------------+
        """
        ...

    def DeleteRow(self, RowN) -> bool:  # itable_deleterow.html
        """
        ## DeleteRow - Удалить строку из таблицы

        Позволяет удалить строку из таблицы.

        ### Синтаксис Automation

        ```
        BOOL DeleteRow( long RowN );
        ```

        ### Входные параметры

        +------------------------------------+
        | ColN | - номер удаляемого столбца. |
        +------------------------------------+

        ### Возвращаемое значение

        +-----------------------------+
        | TRUE  | - в случае успеха,  |
        +-----------------------------+
        | FALSE | - в случае неудачи. |
        +-----------------------------+
        """
        ...

    def Range(self, BeginRow, BeginColumn, EndRow, EndColumn) -> ITableRange:  # itable_range.html
        """
        ## Range - Интерфейс для групповых операций над колонками

        Тип данных: указатель на интерфейс ITableRange

        1. Свойство позволяет получить интерфейс для групповых операций над ячейками таблицы.

        2. Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        Cell = Object.Range( BeginRow, BeginColumn EndRow, EndColumn )  # Получить свойство(* )
        Cell = Object.Range( BeginRow, BeginColumn EndRow, EndColumn )  # Получить свойство (**)
        ```
        """
        ...




class ITableRange(IDispatch):  # itablerange.html
    """
    ## Интерфейс ITableRange

    Интерфейс для групповых операций над ячейками таблицы.

    Интерфейс можно получить с помощью метода интерфейса таблицы ITable::Range
    """

    @property
    def Cells(self) -> typing.Any:  # itablerange_cells.html
        """
        ## Cells - Массив ячеек в виде SAFEARRAY DISPATCH - VT_ARRAY | VT_DISPATCH

        Тип данных: VARIANT

        Значения свойства:

        - Массив SafeArray типа VT_ARRAY | VT_DISPATCH.

        1. Свойство позволяет получить массив ячеек SAFEARRAY. Это массив VT_DISPATCH, которые можно преобразовать в интерфейсы ITableCell. Если возвращается один объект, то тип VARIANT-а будет VT_DISPATCH, если возвращается несколько объектов, то тип VARIANT-а будет VT_ARRAY | VT_DISPATCH.

        2. Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        Cells = Object.Cells     # Получить свойство(* )
        Cells = Object.GetCells  # Получить свойство (**)
        ```
        """
        ...

    @property
    def CellsBoundaries(self) -> ICellBoundaries:  # itablerange_cellsboundaries.html
        """
        ## CellsBoundaries - Границы ячеек

        Тип данных: указатель на интерфейс ICellBoundaries

        1. Свойство позволяет получить интерфейс границы ячеек ICellBoundaries.

        2. Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        CellsBoundaries = Object.CellsBoundaries()     # Получить свойство(* )
        CellsBoundaries = Object.GetCellsBoundaries()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def CellsFormat(self) -> ICellFormat:  # itablerange_cellsformat.html
        """
        ## CellsFormat - Формат ячеек

        Тип данных: указатель на интерфейс ICellFormat

        1. Свойство позволяет получить интерфейс формата ячеек ICellFormat.

        2. Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        CellsFormat = Object.CellsFormat()     # Получить свойство(* )
        CellsFormat = Object.GetCellsFormat()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def Texts(self) -> typing.Any:  # itablerange_texts.html
        """
        ## Texts - Получить тексты из ячеек в виде массива VT_ARRAY | VT_BSTR

        Тип данных: VARIANT

        ### Синтаксис Automation

        ```
        Texts = Object.Texts        # Получить свойство(* )
        Object.Texts = Texts        # Установить свойство (* )
        Texts = Object.GetTexts( )  # Получить свойство (**)
        Object.SetTexts( Texts )    # Установить свойство (**)
        ```
        """
        ...
    @Texts.setter
    def Texts(self, value: typing.Any): ...

    def ClearCells(self) -> bool:  # itablerange_clearcells.html
        """
        ## ClearCells - Очистить ячейки таблицы

        Позволяет очистить ячейки таблицы.

        ### Синтаксис Automation

        ```
        BOOL ClearCells();
        ```

        ### Возвращаемое значение

        +-----------------------------+
        | TRUE  | - в случае успеха,  |
        +-----------------------------+
        | FALSE | - в случае неудачи. |
        +-----------------------------+
        """
        ...

    def CombineCells(self) -> bool:  # itablerange_combinecells.html
        """
        ## CombineCells - Объединить ячейки

        Позволяет объединить ячейки таблицы.

        ### Синтаксис Automation

        ```
        BOOL CombineCells();
        ```

        ### Возвращаемое значение

        +-----------------------------+
        | TRUE  | - в случае успеха,  |
        +-----------------------------+
        | FALSE | - в случае неудачи. |
        +-----------------------------+
        """
        ...

    def DivideCells(self, ColumnCount, RowCount, PrepareCombine) -> bool:  # itablerange_dividecells.html
        """
        ## DivideCells - Разбить ячейки

        Позволяет разбить ячейки таблицы.

        ### Синтаксис Automation

        ```
        BOOL DivideCells( long ColumnCount,
        long RowCount,
        BOOL PrepareCombine );
        ```

        ### Входные параметры

        +--------------------------------------------------------+
        | ColumnCount    | - число столбцов,                     |
        +--------------------------------------------------------+
        | RowCount       | - число строк,                        |
        +--------------------------------------------------------+
        | PrepareCombine | - объединить ячейки перед разбиением. |
        +--------------------------------------------------------+

        ### Возвращаемое значение

        +-----------------------------+
        | TRUE  | - в случае успеха,  |
        +-----------------------------+
        | FALSE | - в случае неудачи. |
        +-----------------------------+
        """
        ...




class ICellFormat(IDispatch):  # icellformat.html
    """
    ## Интерфейс ICellFormat

    Интерфейс формата ячейки.

    Интерфейс можно получить с помощью метода IUnknown::QueryInterface у интерфейса ячейки таблицы ITableCell и с помощью свойства интерфейса операций над ячейками ITableRange::CellsFormat.
    """

    @property
    def HFormat(self) -> int:  # icellformat_hformat.html
        """
        ## HFormat - Признак горизонтального форматирования

        Тип данных: ksTextHorizontalFormatEnum

        Свойство позволяет устанавливать и получать признак горизонтального форматирования.

        ### Синтаксис Automation

        ```
        HFormat = Object.HFormat        # Получить свойство(* )
        Object.HFormat = HFormat        # Установить свойство (* )
        HFormat = Object.GetHFormat( )  # Получить свойство (**)
        Object.SetHFormat( HFormat )    # Установить свойство (**)
        ```
        """
        ...
    @HFormat.setter
    def HFormat(self, value: int): ...

    @property
    def Height(self) -> float:  # icellformat_height.html
        """
        ## Height - Высота строки

        Тип данных: double

        Свойство позволяет устанавливать и получать высоту строки.

        ### Синтаксис Automation

        ```
        Height = Object.Height        # Получить свойство(* )
        Object.Height = Height        # Установить свойство (* )
        Height = Object.GetHeight( )  # Получить свойство (**)
        Object.SetHeight( Height )    # Установить свойство (**)
        ```
        """
        ...
    @Height.setter
    def Height(self, value: float): ...

    @property
    def LeftEdge(self) -> float:  # icellformat_leftedge.html
        """
        ## LeftEdge - Отступ слева

        Тип данных: double

        Свойство позволяет устанавливать и получать величину отступа слева.

        ### Синтаксис Automation

        ```
        LeftEdge = Object.LeftEdge        # Получить свойство(* )
        Object.LeftEdge = LeftEdge        # Установить свойство (* )
        LeftEdge = Object.GetLeftEdge( )  # Получить свойство (**)
        Object.SetLeftEdge( LeftEdge )    # Установить свойство (**)
        ```
        """
        ...
    @LeftEdge.setter
    def LeftEdge(self, value: float): ...

    @property
    def OneLine(self) -> bool:  # icellformat_oneline.html
        """
        ## OneLine - Однострочный текст

        Тип данных: BOOL

        Свойство позволяет устанавливать и получать признак однострочного текста.

        ### Синтаксис Automation

        ```
        OneLine = Object.OneLine        # Получить свойство(* )
        Object.OneLine = OneLine        # Установить свойство (* )
        OneLine = Object.GetOneLine( )  # Получить свойство (**)
        Object.SetOneLine( OneLine )    # Установить свойство (**)
        ```
        """
        ...
    @OneLine.setter
    def OneLine(self, value: bool): ...

    @property
    def ReadOnly(self) -> bool:  # icellformat_readonly.html
        """
        ## ReadOnly - Запретить изменение текста в ячейке

        Тип данных: BOOL

        1. Свойство позволяет устанавливать и получать признак запрета на изменение текста в ячейке.

        2. Для того, чтобы сузить текст в ячейке таблицы необходимо:

        •снять признак запрета на редактирование ячейки и изменить текст,

        •вызвать метод IDrawingObject::Update,

        •установить признак запрета на редактирование ячейки,

        •вызвать метод IDrawingObject::Update.

        ### Синтаксис Automation

        ```
        ReadOnly = Object.ReadOnly        # Получить свойство(* )
        Object.ReadOnly = ReadOnly        # Установить свойство (* )
        ReadOnly = Object.GetReadOnly( )  # Получить свойство (**)
        Object.SetReadOnly( ReadOnly )    # Установить свойство (**)
        ```
        """
        ...
    @ReadOnly.setter
    def ReadOnly(self, value: bool): ...

    @property
    def RightEdge(self) -> float:  # icellformat_rightedge.html
        """
        ## RightEdge - Отступ справа

        Тип данных: double

        Свойство позволяет устанавливать и получать величину отступа справа.

        ### Синтаксис Automation

        ```
        RightEdge = Object.RightEdge        # Получить свойство(* )
        Object.RightEdge = RightEdge        # Установить свойство (* )
        RightEdge = Object.GetRightEdge( )  # Получить свойство (**)
        Object.SetRightEdge( RightEdge )    # Установить свойство (**)
        ```
        """
        ...
    @RightEdge.setter
    def RightEdge(self, value: float): ...

    @property
    def SpaceAfter(self) -> float:  # icellformat_spaceafter.html
        """
        ## SpaceAfter - Отступ снизу

        Тип данных: double

        Свойство позволяет устанавливать и получать величину отступа снизу.

        ### Синтаксис Automation

        ```
        SpaceAfter = Object.SpaceAfter        # Получить свойство(* )
        Object.SpaceAfter = SpaceAfter        # Установить свойство (* )
        SpaceAfter = Object.GetSpaceAfter( )  # Получить свойство (**)
        Object.SetSpaceAfter( SpaceAfter )    # Установить свойство (**)
        ```
        """
        ...
    @SpaceAfter.setter
    def SpaceAfter(self, value: float): ...

    @property
    def SpaceBefore(self) -> float:  # icellformat_spacebefore.html
        """
        ## SpaceBefore - Отступ сверху

        Тип данных: double

        Свойство позволяет устанавливать и получать величину отступа сверху.

        ### Синтаксис Automation

        ```
        SpaceBefore = Object.SpaceBefore        # Получить свойство(* )
        Object.SpaceBefore = SpaceBefore        # Установить свойство (* )
        SpaceBefore = Object.GetSpaceBefore( )  # Получить свойство (**)
        Object.SetSpaceBefore( SpaceBefore )    # Установить свойство (**)
        ```
        """
        ...
    @SpaceBefore.setter
    def SpaceBefore(self, value: float): ...

    @property
    def TextStyle(self) -> int:  # icellformat_textstyle.html
        """
        ## TextStyle - Стиль текста

        Тип данных: long

        Свойство позволяет устанавливать и получать стиль текста в ячейке.

        ### Синтаксис Automation

        ```
        TextStyle = Object.TextStyle        # Получить свойство(* )
        Object.TextStyle = TextStyle        # Установить свойство (* )
        TextStyle = Object.GetTextStyle( )  # Получить свойство (**)
        Object.SetTextStyle( TextStyle )    # Установить свойство (**)
        ```
        """
        ...
    @TextStyle.setter
    def TextStyle(self, value: int): ...

    @property
    def VFormat(self) -> bool:  # icellformat_vformat.html
        """
        ## VFormat - Признак вертикального форматирования

        Тип данных: BOOL

        Значения свойства:

        +---------------------------------+
        | TRUE  | - изменение шага строк, |
        +---------------------------------+
        | FALSE | - нет форматирования.   |
        +---------------------------------+

        Свойство позволяет устанавливать и получать признак вертикального форматирования.

        ### Синтаксис Automation

        ```
        VFormat = Object.VFormat        # Получить свойство(* )
        Object.VFormat = VFormat        # Установить свойство (* )
        VFormat = Object.GetVFormat( )  # Получить свойство (**)
        Object.SetVFormat( VFormat )    # Установить свойство (**)
        ```
        """
        ...
    @VFormat.setter
    def VFormat(self, value: bool): ...

    @property
    def Width(self) -> float:  # icellformat_width.html
        """
        ## Width - Ширина столбца

        Тип данных: double

        Свойство позволяет устанавливать и получать ширину столбца.

        ### Синтаксис Automation

        ```
        Width = Object.Width        # Получить свойство(* )
        Object.Width = Width        # Установить свойство (* )
        Width = Object.GetWidth( )  # Получить свойство (**)
        Object.SetWidth( Width )    # Установить свойство (**)
        ```
        """
        ...
    @Width.setter
    def Width(self, value: float): ...




class ICellBoundaries(IDispatch):  # icellboundaries.html
    """
    ## Интерфейс ICellBoundaries

    Интерфейс границ ячейки.

    Интерфейс можно получить с помощью метода IUnknown::QueryInterface у интерфейса ячейки таблицы ITableCell и с помощью свойства интерфейса операций над ячейками ITableRange::CellsBoundaries.
    """

    def LineStyle(self, Index) -> int:  # icellboundaries_linestyle.html
        """
        ## LineStyle - Стиль линии границы

        Тип данных: из перечисления ksCurveStyleEnum

        Свойство позволяет устанавливать и получать стиль линии границы ячейки.

        ### Синтаксис Automation

        ```
        LineStyle = Object.LineStyle( index )     # Получить свойство(* )
        Object.LineStyle( index ) = LineStyle     # Установить свойство (* )
        LineStyle = Object.GetLineStyle( index )  # Получить свойство (**)
        Object.SetLineStyle( index, LineStyle )   # Установить свойство (**)
        ```

        ### Входные параметры

        +---------------------------------------------------------------------------------------------------+
        | index | - граница, для которой можно установить видимость (из перечисления ksCellBoundariesEnum). |
        +---------------------------------------------------------------------------------------------------+
        """
        ...

    def LineVisible(self, Index) -> bool:  # icellboundaries_linevisible.html
        """
        ## LineVisible - Видимость границы

        Тип данных: BOOL

        Свойство позволяет устанавливать и получать видимость границы ячейки.

        ### Синтаксис Automation

        ```
        LineVisible = Object.LineVisible( index )     # Получить свойство(* )
        Object.LineVisible( index ) = LineVisible     # Установить свойство (* )
        LineVisible = Object.GetLineVisible( index )  # Получить свойство (**)
        Object.SetLineVisible( index, LineVisible )   # Установить свойство (**)
        ```

        ### Входные параметры

        +---------------------------------------------------------------------------------------------------+
        | index | - граница, для которой можно установить видимость (из перечисления ksCellBoundariesEnum). |
        +---------------------------------------------------------------------------------------------------+
        """
        ...

    def SetLineStyle(self, Index, arg1):
        ...

    def SetLineVisible(self, Index, arg1):
        ...




class ITableCell(IKompasAPIObject):  # itablecell.html
    """
    ## Интерфейс ITableCell

    Интерфейс для работы c таблицами.

    1. Интерфейс можно получить с помощью метода интерфейса сетки таблицы ITable::Cell или свойства ITable::CellById.

    2. Для ячейки таблицы 2D документа посредством вызова метода IUnknown::QueryInterface (const GUID far& iid, void** pif) интерфейс позволяет получить следующие дополнительные интерфейсы:

    •ICellFormat - формат ячейки,

    •ICellBoundaries - границы ячейки.

    3. Для ячейки таблицы допуска формы интерфейсы ICellFormat и ICellBoundaries не поддерживаются.
    """

    @property
    def CellID(self) -> int:  # itablecell_cellid.html
        """
        ## CellID - Идентификатор ячейки

        Тип данных: long

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        CellID = Object.CellID       # Получить свойство(* )
        CellID = Object.GetCellID()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def Column(self) -> int:  # itablecell_column.html
        """
        ## Column - Номер колонки

        Тип данных: long

        1. Свойство доступно только для чтения.

        2. Свойство возвращает номер колонки в виртуальной сетке таблицы.

        ### Синтаксис Automation

        ```
        Row = Object.Row       # Получить свойство(* )
        Row = Object.GetRow()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def Row(self) -> int:  # itablecell_row.html
        """
        ## Row - Номер строки

        Тип данных: long

        1. Свойство доступно только для чтения.

        2. Свойство возвращает номер строки в виртуальной сетке таблицы.

        ### Синтаксис Automation

        ```
        Row = Object.Row       # Получить свойство(* )
        Row = Object.GetRow()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def Text(self) -> IKompasAPIObject:  # itablecell_text.html
        """
        ## Text - Текст

        Тип данных: указатель на интерфейс IKompasAPIObject

        1. Свойство доступно только для чтения.

        2. Для ячейки таблицы 2D документа возвращается интерфейс IText.

        3. Для допуска формы возвращается интерфейс ITextLine.

        ### Синтаксис Automation

        ```
        Text = Object.Text       # Получить свойство(* )
        Text = Object.GetText()  # Получить свойство (**)
        ```
        """
        ...




class ITabulator(IKompasAPIObject):  # itabulator.html
    """
    ## Интерфейс ITabulator

    Интерфейс позиции табулятора.

    Позволяет получить доступ к параметрам позиции табулятора и редактировать их.

    Данный интерфейс может быть получен от интерфейса коллекции позиций табулятора ITabulators.
    """

    @property
    def Align(self) -> int:  # itabulator_align.html
        """
        ## Align - Выравнивание

        Тип данных: из перечисления ksAlignEnum

        ### Синтаксис Automation

        ```
        Align = Object.Align;       # Получить свойство(* )
        Object.Align = Align;       # Установить свойство (* )
        Align = Object.GetAlign();  # Получить свойство (**)
        Object.SetAlign(Align);     # Установить свойство (**)
        ```
        """
        ...
    @Align.setter
    def Align(self, value: int): ...

    @property
    def Filling(self) -> int:  # itabulator_filling.html
        """
        ## Filling - Заполнение

        Тип данных: из перечисления ksTabulatorFillingEnum

        ### Синтаксис Automation

        ```
        Filling = Object.Filling;       # Получить свойство(* )
        Object.Filling = Filling;       # Установить свойство (* )
        Filling = Object.GetFilling();  # Получить свойство (**)
        Object.SetFilling(Filling);     # Установить свойство (**)
        ```
        """
        ...
    @Filling.setter
    def Filling(self, value: int): ...

    @property
    def Position(self) -> int:  # itabulator_position.html
        """
        ## Position - Позиция табуляции в мм * 100

        Тип данных: long

        ### Синтаксис Automation

        ```
        Position = Object.Position;       # Получить свойство(* )
        Object.Position = Position;       # Установить свойство (* )
        Position = Object.GetPosition();  # Получить свойство (**)
        Object.SetPosition(Position);     # Установить свойство (**)
        ```
        """
        ...
    @Position.setter
    def Position(self, value: int): ...




class IText(IKompasAPIObject):  # itext.html
    """
    ## Интерфейс IText

    Интерфейс текста для работы с аннотационными объектами.

    Интерфейс позволяет получить и отредактировать текст.

    Текст имеет стиль. Стиль может быть системным из перечисления ksTextStyleEnum или пользовательским.

    Состоит из строк, которые представлены интерфейсом ITextLine.

    Строки текста состоят из компонент, которые представлены интерфейсом ITextItem.

    Компонент текста - это часть строки, отличающаяся типом ksTextItemEnum и параметрами шрифта.

    Данный интерфейс можно получить как дополнительный через интерфейс IDrawingText посредством вызова метода IUnknown::QueryInterface (const GUID far& iid, void** pif) или как самостоятельный объект у аннотационных объектов, имеющих тексты (линия-выноска, размер и т.п.).
    """

    @property
    def Count(self) -> int:  # itext_count.html
        """
        ## Count - Количество строк текста

        Тип данных: long

        Свойство позволяет получить количество строк текста.

        ### Синтаксис Automation

        ```
        Count = iObject.Count;       # Получить свойство(* )
        Count = iObject.GetCount();  # Получить свойство (**)
        ```
        """
        ...

    @property
    def Str(self) -> str:  # itext_str.html
        """
        ## Str - Текст в виде строки

        Тип данных: BSTR(строка)

        Свойство позволяет установить и получить текст в виде массива символов.

        Дроби представляются в формате $d числитель $ знаменатель $.

        Выражение типа суммы в формате $ верхний индекс $ нижний индекс $.

        Спецзнак в формате &N или @N.

        При замене текста все строки очищаются и создаются новые с учетом символа перевода строки.

        ### Синтаксис Automation

        ```
        Str = iObject.Str;       # Получить свойство(* )
        iObject.Str = Str;       # Установить свойство (* )
        Str = iObject.GetStr();  # Получить свойство (**)
        iObject.SetStr(Str);     # Установить свойство (**)
        ```
        """
        ...
    @Str.setter
    def Str(self, value: str): ...

    @property
    def Style(self) -> int:  # itext_style.html
        """
        ## Style - Cтиль текста

        Тип данных: long

        1. Свойство позволяет получить и установить стиль текста.

        2. Стиль может быть системным из перечисления ksTextStyleEnum или пользовательским.

        ### Синтаксис Automation

        ```
        Style = iObject.Style;       # Получить свойство(* )
        iObject.Style = Style;       # Установить свойство (* )
        Style = iObject.GetStyle();  # Получить свойство (**)
        iObject.SetStyle(Style);     # Установить свойство (**)
        ```
        """
        ...
    @Style.setter
    def Style(self, value: int): ...

    @property
    def TextLines(self) -> list[IDispatch]:  # itext_textlines.html
        """
        ## TextLines - Массив SAFEARRAY строк текста

        Тип данных: VARIANT ( VT_ARRAY|VT_DISPATCH )

        Свойство позволяет получить массив SAFEARRAY строк текста. Это массив интерфейсов ITextLine. Если возвращается один объект, то тип VARIANT-а будет VT_DISPATCH, если возвращается несколько объектов, то тип VARIANT-а будет VT_ARRAY | VT_DISPATCH.

        ### Синтаксис Automation

        ```
        TextLines = iObject.TextLines;       # Получить свойство(* )
        TextLines = iObject.GetTextLines();  # Получить свойство (**)
        ```
        """
        ...

    def Add(self) -> str:  # itext_add.html
        """
        ## Add - Добавить строку в конец текста

        Позволяет добавить строку в конец текста.

        ### Синтаксис Automation

        ```
        LPDISPATCH Add();
        ```

        ### Возвращаемое значение

        - указатель на интерфейс ITextLine строка текста.
        """
        ...

    def AddBefore(self, Index) -> str:  # itext_addbefore.html
        """
        ## AddBefore - Добавить строку перед строкой с заданным индексом

        Позволяет добавить строку перед строкой с заданным индексом.

        ### Синтаксис Automation

        ```
        LPDISPATCH AddBefore( long Index );
        ```

        ### Входные параметры

        +----------------------------------------------------------------------------+
        | Index (long) | - индекс строки, перед которой нужно вставить новую строку. |
        +----------------------------------------------------------------------------+

        ### Возвращаемое значение

        - указатель на интерфейс ITextLine строка текста.
        """
        ...

    def AddTable(self, IndexAt, RowsCount, ColumnsCount, RowHeigh, ColumnsWidth, TitlePos) -> ITextTable:  # itext_addtable.html
        """
        ## AddTable - Добавить таблицу в текст (для текстового документа)

        ### Синтаксис Automation

        ```
        LPDISPATCH AddTable( long IndexAt,
        long RowsCount,
        long ColumnsCount,
        double RowHeigh,
        double ColumnsWidth,
        ksTableTileLayoutEnum TitlePos );
        ```

        ### Входные параметры

        +------------------------------------------------------------------------------------------+
        | RowsCount    | - количество строк,                                                       |
        +------------------------------------------------------------------------------------------+
        | ColumnsCount | - количество столбцов,                                                    |
        +------------------------------------------------------------------------------------------+
        | RowHeigh     | - высота строк,                                                           |
        +------------------------------------------------------------------------------------------+
        | ColumnsWidth | - ширина столбцов,                                                        |
        +------------------------------------------------------------------------------------------+
        | TitlePos     | - расположение заголовка таблицы (из перечисления ksTableTileLayoutEnum). |
        +------------------------------------------------------------------------------------------+

        ### Выходные параметры

        +------------------------------------------------------------------+
        | IndexAt | - индекс строки текста, с которой вставляется таблица. |
        +------------------------------------------------------------------+

        Метод позволяет вставить таблицу в текстовый документ

        ### Возвращаемое значение

        Указатель на интерфейс таблицы ITextTable
        """
        ...

    def AddTextLine(self, Type, FileName) -> ITextLine:  # itext_addtextline.html
        """
        ## AddTextLine - Добавить строку c внешними данными

        1. В 2D объекте таблица IDrawingTable доступна возможность вставки вертикального текста, растрового объекта и фрагмента.

        2. В текстовом документе есть возможность вставки таблицы в текст.

        3. С помощью функции AddTextLine и AddTextLineBefore можно выполнить вставку следующих объектов через API системы КОМПАС:

        - для типов:

        •ksTLText - простой текст,

        •ksTLVerticalText - вертикальный текст.

        Параметр filename используется для передачи пути к файлу с расширениями txt, rtf и kdw. Из файла будет вставлена первая строчка текста. Если расширение не задано, то параметр filename воспринимается как обычная строка и она добавляется в текст.

        •ksTLFragment - вставка фрагмента.

        Параметр filename используется для передачи пути к файлу фрагмента системы КОМПАС с расширением frw.

        •ksTLRaster - вставка рисунка.

        Параметр filename используется для передачи пути к растровым файлам.

        •ksTLTable - вставка таблицы.

        Параметр filename используется для передачи пути к файлу таблицы с расширением kdw. Не реализовано, вставка таблицы будет доступна в текстовом документе.

        4. Получить и изменить параметры вставленного объекта можно с помощью свойства ITextLine::TextLineData.

        ### Синтаксис Automation

        ```
        LPDISPATCH AddTextLine( ksTextLineType type,
        BSTR filename );
        ```

        ### Входные параметры

        +--------------------------------------------+
        | type     | - тип строки ksTextLineType,    |
        +--------------------------------------------+
        | filename | - имя файла - источник вставки. |
        +--------------------------------------------+

        ### Возвращаемое значение

        Указатель на интерфейс ITextLine строки текста.
        """
        ...

    def AddTextLineBefore(self, Type, Index, FileName) -> ITextLine:  # itext_addtextlinebefore.html
        """
        ## AddTextLineBefore - Добавить строку c внешними данными перед строкой с заданным индексом

        1. В 2D объекте таблица IDrawingTable доступна возможность вставки вертикального текста, растрового объекта и фрагмента.

        2. В текстовом документе есть возможность вставки таблицы в текст.

        3. С помощью функции AddTextLine и AddTextLineBefore можно выполнить вставку следующих объектов через API системы КОМПАС:

        - для типов:

        •ksTLText - простой текст,

        •ksTLVerticalText - вертикальный текст.

        Параметр filename используется для передачи пути к файлу с расширениями txt, rtf и kdw. Из файла будет вставлена первая строчка текста. Если расширение не задано, то параметр filename воспринимается как обычная строка и она добавляется в текст.

        •ksTLFragment - вставка фрагмента.

        Параметр filename используется для передачи пути к файлу фрагмента системы КОМПАС с расширением frw.

        •ksTLRaster - вставка рисунка.

        Параметр filename используется для передачи пути к растровым файлам.

        •ksTLTable - вставка таблицы.

        Параметр filename используется для передачи пути к файлу таблицы с расширением kdw. Не реализовано, вставка таблицы будет доступна в текстовом документе.

        4. Получить и изменить параметры вставленного объекта можно с помощью свойства ITextLine::TextLineData.

        ### Синтаксис Automation

        ```
        LPDISPATCH AddTextLineBefore( ksTextLineType type,
        long Index,
        BSTR filename );
        ```

        ### Входные параметры

        +--------------------------------------------+
        | type     | - тип строки ksTextLineType,    |
        +--------------------------------------------+
        | Index    | - индекс строки,                |
        +--------------------------------------------+
        | filename | - имя файла - источник вставки. |
        +--------------------------------------------+

        ### Возвращаемое значение

        Указатель на интерфейс ITextLine строки текста.
        """
        ...

    def Assign(self, Other) -> bool:  # itext_assign.html
        """
        ## Assign - Скопировать текст

        ### Синтаксис Automation

        ```
        BOOL Assign( LPDISPATCH Other );
        ```

        ### Входные параметры

        +------------------------------------------------------------+
        | Other | - копируемый текст - указатель на интерфейс IText. |
        +------------------------------------------------------------+

        ### Возвращаемое значение

        +----------------------------------------+
        | TRUE | - в случае удачного завершения. |
        +----------------------------------------+
        """
        ...

    def Clear(self) -> bool:  # itext_clear.html
        """
        ## Clear - Очистить текст

        Позволяет очистить текст. После этого можно вводить новый.

        ### Синтаксис Automation

        ```
        BOOL Clear();
        ```

        ### Возвращаемое значение

        +-------------------------+
        | TRUE  | - текст очищен, |
        +-------------------------+
        | FALSE | - ошибка.       |
        +-------------------------+
        """
        ...

    def Edit(self, HWnd) -> bool:  # itext_edit.html
        """
        ## Edit - Редактировать в окне

        ### Синтаксис Automation

        ```
        BOOL Edit( OLE_HANDLE HWnd );
        ```

        ### Возвращаемое значение

        +--------------------------------------+
        | TRUE  | - в случае изменения текста, |
        +--------------------------------------+
        | FALSE | - в случае отмены.           |
        +--------------------------------------+
        """
        ...

    def Find(self, StartPosition, SrcText, Case, WordOnly):  # itext_find.html
        """
        ## Find - Найти текст

        ### Синтаксис Automation

        ```
        int64 Find( int64 StartPosition, BSTR SrcText, BOOL Case, BOOL WordOnly );
        ```

        ### Входные параметры

        +----------------------------------------------------+
        | StartPosition | - начальная позиция поиска текста, |
        +----------------------------------------------------+
        | SrcText       | - строка поиска,                   |
        +----------------------------------------------------+
        | Case          | - различать регистр,               |
        +----------------------------------------------------+
        | WordOnly      | - только слово целиком.            |
        +----------------------------------------------------+

        Версия: Компас v20

        ### Возвращаемое значение

        - позиция найденного текста.
        """
        ...

    def Replace(self, SrcText, NewText, Case, WordOnly, ReplaceAll) -> bool:  # itext_replace.html
        """
        ## Replace - Заменить подстроку текста

        ### Синтаксис Automation

        ```
        BOOL Replace( BSTR SrcText, BSTR NewText,
        BOOL Case,
        BOOL WordOnly,
        BOOL ReplaceAll );
        ```

        ### Входные параметры

        +-------------------------------------------------------------------------------------------+
        | SrcText    | - искомая подстрока,                                                         |
        +-------------------------------------------------------------------------------------------+
        | NewText    | - новая строка,                                                              |
        +-------------------------------------------------------------------------------------------+
        | Case       | - TRUE - различать регистр, - FALSE - поиск без учета регистра,              |
        +-------------------------------------------------------------------------------------------+
        | WordOnly   | - TRUE - только слово целиком,                                               |
        +-------------------------------------------------------------------------------------------+
        | ReplaceAll | - TRUE - заменить все вхождения, - FALSE - заменить только первое вхождение. |
        +-------------------------------------------------------------------------------------------+

        ### Возвращаемое значение

        +----------------------------------------+
        | TRUE | - в случае удачного завершения. |
        +----------------------------------------+
        """
        ...

    def ReplaceStr(self, StartPosition, SrcText, NewText, Case, WordOnly, ReplaceAll):  # itext_replacestr.html
        """
        ## ReplaceStr - Заменить текст, начиная с заданной позиции

        ### Синтаксис Automation

        ```
        BOOL ReplaceStr( int64 StartPosition, BSTR SrcText, BSTR NewText, BOOL Case, BOOL WordOnly, BOOL ReplaceAll );
        ```

        ### Входные параметры

        +----------------------------------------------------+
        | StartPosition | - начальная позиция поиска текста, |
        +----------------------------------------------------+
        | SrcText       | - строка поиска,                   |
        +----------------------------------------------------+
        | NewText       | - новое значение,                  |
        +----------------------------------------------------+
        | Case          | - различать регистр,               |
        +----------------------------------------------------+
        | WordOnly      | - только слово целиком.            |
        +----------------------------------------------------+
        | ReplaceAll    | - заменить все.                    |
        +----------------------------------------------------+

        Версия: Компас v20
        """
        ...

    def TextLine(self, Index) -> ITextLine:  # itext_textline.html
        """
        ## TextLine - Строка текста с заданным индексом

        Тип данных: указатель на интерфейс строки текста ITextLine

        Свойство позволяет получить интерфейс строки текста ITextLine.

        ### Синтаксис Automation

        ```
        TextLine = iObject.TextLine(Index);     # Получить свойство(* )
        TextLine = iObject.GetTextLine(Index);  # Получить свойство (**)
        ```

        ### Входные параметры

        +-----------------------------------+
        | Index ( long ) | - индекс строки. |
        +-----------------------------------+
        """
        ...




class ITextFont(IKompasAPIObject):  # itextfont.html
    """
    ## Интерфейс ITextFont

    Интерфейс параметров шрифта.

    Данный интерфейс позволяет назначить параметры шрифта, которые будут использоваться при вводе надписей в документ.

    1. Данный интерфейс можно получить от интерфейса параметров стиля текста ITextStyle, используя свойство ITextStyle::Font.

    2. Интерфейс ITextFont является дополнительным для интерфейса ITextItem.
    """

    @property
    def Bold(self) -> bool:  # itextfont_bold.html
        """
        ## Bold - Жирный шрифт

        Тип данных: BOOL

        Значение свойства:

        +-------------------------------------------+
        | TRUE  | - утолщенное начертание символов, |
        +-------------------------------------------+
        | FALSE | - обычное начертание символов.    |
        +-------------------------------------------+

        Свойство позволяет получить\\изменить утолщенное начертание символов.

        ### Синтаксис Automation

        ```
        Bold = iObject.Bold       # Получить свойство(* )
        iObject.Bold = Bold       # Установить свойство (* )
        Bold = iObject.GetBold()  # Получить свойство (**)
        iObject.SetBold (Bold)    # Установить свойство (**)
        ```
        """
        ...
    @Bold.setter
    def Bold(self, value: bool): ...

    @property
    def Color(self) -> int:  # itextfont_color.html
        """
        ## Color - Цвет

        Тип данных: long

        Свойство позволяет получить\\изменить цвет символов.

        ### Синтаксис Automation

        ```
        Color = iObject.Color       # Получить свойство(* )
        iObject.Color = Color       # Установить свойство (* )
        Color = iObject.GetColor()  # Получить свойство (**)
        iObject.SetColor (Color)    # Установить свойство (**)
        ```
        """
        ...
    @Color.setter
    def Color(self, value: int): ...

    @property
    def FontName(self) -> str:  # itextfont_fontname.html
        """
        ## FontName - Имя шрифта

        Тип данных: BSTR (строка)

        Свойство позволяет получить/изменить название шрифта из доступных шрифтов.

        ### Синтаксис Automation

        ```
        FontName = iObject.FontName       # Получить свойство(* )
        iObject.FontName = FontName       # Установить свойство (* )
        FontName = iObject.GetFontName()  # Получить свойство (**)
        iObject.SetFontName (FontName)    # Установить свойство (**)
        ```
        """
        ...
    @FontName.setter
    def FontName(self, value: str): ...

    @property
    def Height(self) -> float:  # itextfont_height.html
        """
        ## Height - Высота текста

        Тип данных: double

        1. Свойство позволяет получить/изменить высоту символов.

        2. Значение высоты символов задается в миллиметрах.

        ### Синтаксис Automation

        ```
        Height = iObject.Height       # Получить свойство(* )
        iObject.Height = Height       # Установить свойство (* )
        Height = iObject.GetHeight()  # Получить свойство (**)
        iObject.SetHeight (Height)    # Установить свойство (**)
        ```
        """
        ...
    @Height.setter
    def Height(self, value: float): ...

    @property
    def Italic(self) -> bool:  # itextfont_italic.html
        """
        ## Italic - Курсив

        Тип данных: BOOL

        Значение свойства:

        +------------------------------------------+
        | TRUE  | - курсивное начертание символов, |
        +------------------------------------------+
        | FALSE | - обычное начертание символов.   |
        +------------------------------------------+

        Свойство позволяет получить\\изменить курсивное (наклонное) начертание символов.

        ### Синтаксис Automation

        ```
        Italic = iObject.Italic       # Получить свойство(* )
        iObject.Italic = Italic       # Установить свойство (* )
        Italic = iObject.GetItalic()  # Получить свойство (**)
        iObject.SetItalic (Italic)    # Установить свойство (**)
        ```
        """
        ...
    @Italic.setter
    def Italic(self, value: bool): ...

    @property
    def TextLineStep(self) -> bool:  # itextfont_textlinestep.html
        """
        ## TextLineStep - Шаг строк для настроек

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        TextLineStep = Object.TextLineStep       # Получить свойство(* )
        Object.TextLineStep = TextLineStep       # Установить свойство (* )
        TextLineStep = Object.GetTextLineStep()  # Получить свойство (**)
        Object.SetTextLineStep (TextLineStep)    # Установить свойство (**)
        ```
        """
        ...
    @TextLineStep.setter
    def TextLineStep(self, value: bool): ...

    @property
    def Underline(self) -> bool:  # itextfont_underline.html
        """
        ## Underline - Подчеркивание

        Тип данных: BOOL

        Значение свойства:

        +----------------------------------------+
        | TRUE  | - подчеркивание символов,      |
        +----------------------------------------+
        | FALSE | - обычное начертание символов. |
        +----------------------------------------+

        Свойство позволяет получить\\изменить курсивное (наклонное) подчеркивание текста.

        ### Синтаксис Automation

        ```
        Underline = iObject.Underline       # Получить свойство(* )
        iObject.Underline = Underline       # Установить свойство (* )
        Underline = iObject.GetUnderline()  # Получить свойство (**)
        iObject.SetUnderline (Underline)    # Установить свойство (**)
        ```
        """
        ...
    @Underline.setter
    def Underline(self, value: bool): ...

    @property
    def WidthFactor(self) -> float:  # itextfont_widthfactor.html
        """
        ## WidthFactor - Коэффициент сужения

        Тип данных: double

        Свойство позволяет получить\\изменить величину коэффициента сужения для символов.

        ### Синтаксис Automation

        ```
        WidthFactor = iObject.WidthFactor       # Получить свойство(* )
        iObject.WidthFactor = WidthFactor       # Установить свойство (* )
        WidthFactor = iObject.GetWidthFactor()  # Получить свойство (**)
        iObject.SetWidthFactor (WidthFactor)    # Установить свойство (**)
        ```
        """
        ...
    @WidthFactor.setter
    def WidthFactor(self, value: float): ...




class ITextItem(IKompasAPIObject):  # itextitem.html
    """
    ## Интерфейс ITextItem

    Интерфейс компоненты строки текста.

    Интерфейс позволяет получить и отредактировать компонент строки текста. Компонент строки - это часть строки, отличающаяся типом ksTextItemEnum и параметрами шрифта.

    Интерфейс имеет дополнительные интерфейсы ITextFont и IHypertextReferenceParam.

    Данный интерфейс можно получить через интерфейс ITextLine, используя свойство ITextLine::TextItem.
    """

    @property
    def ItemType(self) -> int:  # itextitem_itemtype.html
        """
        ## ItemType - Cтиль текста

        Тип данных: ksTextItemEnum

        Свойство позволяет установить и получить тип компоненты текста. Используя соответствующий тип компоненты, можно формировать дроби, выражения типа суммы, вставлять спецзнаки, символы из произвольных шрифтов, вводить текст с разными параметрами шрифта (цвет, высота, сужение.и т.п.).

        ### Синтаксис Automation

        ```
        ItemType = iObject.ItemType       # Получить свойство(* )
        iObject.ItemType = ItemType       # Установить свойство (* )
        ItemType = iObject.GetItemType()  # Получить свойство (**)
        iObject.SetItemType(ItemType)     # Установить свойство (**)
        ```
        """
        ...
    @ItemType.setter
    def ItemType(self, value: int): ...

    @property
    def NewLine(self) -> bool:  # itextitem_newline.html
        """
        ## NewLine - Признак начала строки

        Тип данных: BOOL

        Свойство позволяет установить и получить признак начала строки.

        ### Синтаксис Automation

        ```
        NewLine = iObject.NewLine       # Получить свойство(* )
        iObject.NewLine = NewLine       # Установить свойство (* )
        NewLine = iObject.GetNewLine()  # Получить свойство (**)
        iObject.SetNewLine(NewLine)     # Установить свойство (**)
        ```
        """
        ...
    @NewLine.setter
    def NewLine(self, value: bool): ...

    @property
    def Number(self) -> int:  # itextitem_number.html
        """
        ## Number - Номер спецсимвола, символа из произвольного шрифта

        Тип данных: long

        1. Свойство позволяет установить и получить номер спецсимвола, символа из произвольного шрифта.

        2. Это свойство используется для компонент типа ksTItSpecialSymbol и ksTItFontSymbol из перечисления ksTextItemEnum.

        ### Синтаксис Automation

        ```
        Number = iObject.Number       # Получить свойство(* )
        iObject.Number = Number       # Установить свойство (* )
        Number = iObject.GetNumber()  # Получить свойство (**)
        iObject.SetNumber(Number)     # Установить свойство (**)
        ```
        """
        ...
    @Number.setter
    def Number(self, value: int): ...

    @property
    def SizeFactor(self) -> int:  # itextitem_sizefactor.html
        """
        ## SizeFactor - Размерный коэффициент текста для дроби, отклонений, выражения типа суммы

        Тип данных: из перечисления ksTextSizeEnum

        1. Свойство позволяет установить и получить размерный коэффициент текста для дроби, отклонений, выражения типа суммы.

        2. Это свойство используется для компонент типа ksTItNumerator, ksTItDenominator, ksTItSBase из перечисления ksTextItemEnum.

        3. Размерный коэффициент текста используется для задания размера текста дроби, отклонений, для выражения типа суммы

        Для дроби и отклонений используются:

        +-------------------------------------+
        | ksTextNormal | - полной высоты,     |
        +-------------------------------------+
        | ksTextMiddle | - в 1.5 раза меньше, |
        +-------------------------------------+
        | ksTextSmall  | - в 2 раза меньше.   |
        +-------------------------------------+

        Для выражения типа суммы используются:

        +-------------------------------------+
        | ksTextNormal | - нормальной высоты, |
        +-------------------------------------+
        | ksTextBig    | - в 1.5 раза больше. |
        +-------------------------------------+

        ### Синтаксис Automation

        ```
        SizeFactor = iObject.SizeFactor       # Получить свойство(* )
        iObject.SizeFactor = SizeFactor       # Установить свойство (* )
        SizeFactor = iObject.GetSizeFactor()  # Получить свойство (**)
        iObject.SetSizeFactor(SizeFactor)     # Установить свойство (**)
        ```
        """
        ...
    @SizeFactor.setter
    def SizeFactor(self, value: int): ...

    @property
    def Str(self) -> str:  # itextitem_str.html
        """
        ## Str - Текстовое значение компоненты текста

        Тип данных: BSTR(строка)

        1. Свойство позволяет установить и получить текст в виде массива символов.

        2. Это свойство не используется для компонент типа ksTItSpecialSymbol и ksTItFontSymbol из перечисления ksTextItemEnum.

        ### Синтаксис Automation

        ```
        Str = iObject.Str;       # Получить свойство(* )
        iObject.Str = Str;       # Установить свойство (* )
        Str = iObject.GetStr();  # Получить свойство (**)
        iObject.SetStr(Str);     # Установить свойство (**)
        ```
        """
        ...
    @Str.setter
    def Str(self, value: str): ...

    @property
    def SymbolFontName(self) -> str:  # itextitem_symbolfontname.html
        """
        ## SymbolFontName - Имя шрифта для символа

        Тип данных: BSTR (строка)

        1. Свойство позволяет установить и получить имя шрифта для символа.

        2. Это свойство используется для компонент типа ksTItFontSymbol из перечисления ksTextItemEnum.

        ### Синтаксис Automation

        ```
        SymbolFontName = iObject.SymbolFontName    # Получить свойство(* )
        iObject.SymbolFontName = SymbolFontName    # Установить свойство (* )
        iObject.GetSymbolFontName()                # Получить свойство (**)
        iObject.SetSymbolFontName(SymbolFontName)  # Установить свойство (**)
        ```
        """
        ...
    @SymbolFontName.setter
    def SymbolFontName(self, value: str): ...

    def Delete(self) -> bool:  # itextitem_delete.html
        """
        ## Delete - Удалить компоненту строки

        Позволяет удалить компоненту строки

        ### Синтаксис Automation

        ```
        BOOL Delete();
        ```

        ### Возвращаемое значение

        +---------------------------------------------+
        | TRUE  | - компонента строки текста удалена, |
        +---------------------------------------------+
        | FALSE | - ошибка.                           |
        +---------------------------------------------+
        """
        ...

    def Update(self) -> bool:  # itextitem_update.html
        """
        ## Update - Обновить данные компонента

        Обновление данных компонента необходимо, так как многие параметры компонента взаимосвязаны и зависят от типа ksTextItemEnum. Поэтому менять компоненту в строке нужно целиком.

        ### Синтаксис Automation

        ```
        BOOL Update();
        ```

        ### Возвращаемое значение

        +--------------------------------------------------------+
        | TRUE  | - обновление данных компонента прошло успешно, |
        +--------------------------------------------------------+
        | FALSE | - ошибка.                                      |
        +--------------------------------------------------------+
        """
        ...




class ITextLine(IKompasAPIObject):  # itextline.html
    """
    ## Интерфейс ITextLine

    Интерфейс строки текста.

    Интерфейс позволяет получить и отредактировать строку текста.

    Строка может иметь стиль, отличающийся от стиля текста. Стиль может быть системным из перечисления ksTextStyleEnum или пользовательским.

    Строки текста состоят из компонентов, которые представлены интерфейсом ITextItem.

    Компонент - это часть строки, отличающаяся типом ksTextItemEnum и параметрами шрифта.

    Данный интерфейс можно получить через интерфейс IText, используя свойство IText::TextLine.
    """

    @property
    def Align(self) -> int:  # itextline_align.html
        """
        ## Align - Выравнивание

        Тип данных: типы выравнивания ksAlignEnum

        Свойство позволяет установить и получить выравнивание строк.

        Строка представляет собой абзац текста и может размещаться на нескольких строках, если включено выравнивание текста по горизонтали и вертикали.

        ### Синтаксис Automation

        ```
        Align = iObject.Align;        # Получить свойство(* )
        iObject.Align = Align;        # Установить свойство (* )
        Align = iObject.GetAlign ();  # Получить свойство (**)
        iObject.SetAlign (Align);     # Установить свойство (**)
        ```
        """
        ...
    @Align.setter
    def Align(self, value: int): ...

    @property
    def BlockIndex(self) -> int:  # itextline_blockindex.html
        """
        ## BlockIndex - Индекс листа в документе

        Тип данных: long

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        BlockIndex = Object.BlockIndex       # Получить свойство(* )
        BlockIndex = Object.GetBlockIndex()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def Count(self) -> int:  # itextline_count.html
        """
        ## Count - Количество компонентов строки текста

        Тип данных: long

        Свойство позволяет получить количество компонентов строки текста.

        ### Синтаксис Automation

        ```
        Count = iObject.Count;        # Получить свойство(* )
        Count = iObject.GetCount ();  # Получить свойство (**)
        ```
        """
        ...

    @property
    def IndentedLine(self) -> float:  # itextline_indentedline.html
        """
        ## IndentedLine - Смещение красной строки

        Тип данных: double

        Свойство позволяет установить и получить смещение красной строки.

        ### Синтаксис Automation

        ```
        IndentedLine = iObject.IndentedLine;        # Получить свойство(* )
        iObject.IndentedLine = IndentedLine;        # Установить свойство (* )
        IndentedLine = iObject.GetIndentedLine ();  # Получить свойство (**)
        iObject.SetIndentedLine (IndentedLine);     # Установить свойство (**)
        ```
        """
        ...
    @IndentedLine.setter
    def IndentedLine(self, value: float): ...

    @property
    def LeftEdge(self) -> float:  # itextline_leftedge.html
        """
        ## LeftEdge - Отступ текста слева

        Тип данных: double

        Свойство позволяет установить и получить отступ текста слева.

        ### Синтаксис Automation

        ```
        LeftEdge = iObject.LeftEdge;        # Получить свойство(* )
        iObject.LeftEdge = LeftEdge;        # Установить свойство (* )
        LeftEdge = iObject.GetLeftEdge ();  # Получить свойство (**)
        iObject.SetLeftEdge (LeftEdge);     # Установить свойство (**)
        ```
        """
        ...
    @LeftEdge.setter
    def LeftEdge(self, value: float): ...

    @property
    def Level(self) -> int:  # itextline_level.html
        """
        ## Level - Уровень вложенности нумерации

        Тип данных: long

        Свойство позволяет установить и получить уровень вложенности нумерации.

        ### Синтаксис Automation

        ```
        Level = iObject.Level;        # Получить свойство(* )
        iObject.Level = Level;        # Установить свойство (* )
        Level = iObject.GetLevel ();  # Получить свойство (**)
        iObject.SetLevel (Level);     # Установить свойство (**)
        ```
        """
        ...
    @Level.setter
    def Level(self, value: int): ...

    @property
    def NewPage(self) -> bool:  # itextline_newpage.html
        """
        ## NewPage - Признак начала абзаца с новой страницы (Ctrl+Enter)

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        NewPage = Object.NewPage       # Получить свойство(* )
        Object.NewPage = NewPage       # Установить свойство (* )
        NewPage = Object.GetNewPage()  # Получить свойство (**)
        Object.SetNewPage( NewPage )   # Установить свойство (**)
        ```
        """
        ...
    @NewPage.setter
    def NewPage(self, value: bool): ...

    @property
    def NewSection(self) -> bool:  # itextline_newsection.html
        """
        ## NewSection - Признак начала раздела

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        NewSection = Object.NewSection       # Получить свойство(* )
        Object.NewSection = NewSection       # Установить свойство (* )
        NewSection = Object.GetNewSection()  # Получить свойство (**)
        Object.SetNewSection( NewSection )   # Установить свойство (**)
        ```
        """
        ...
    @NewSection.setter
    def NewSection(self, value: bool): ...

    @property
    def Numbering(self) -> int:  # itextline_numbering.html
        """
        ## Numbering - Тип нумерации абзаца

        Тип данных: из перечисления ksTextNumberingEnum

        Свойство позволяет установить и получить тип нумерации абзаца.

        ### Синтаксис Automation

        ```
        Numbering = iObject.Numbering;        # Получить свойство(* )
        iObject.Numbering = Numbering;        # Установить свойство (* )
        Numbering = iObject.GetNumbering ();  # Получить свойство (**)
        iObject.SetNumbering (Numbering);     # Установить свойство (**)
        ```
        """
        ...
    @Numbering.setter
    def Numbering(self, value: int): ...

    @property
    def RightEdge(self) -> float:  # itextline_rightedge.html
        """
        ## RightEdge - Отступ текста справа

        Тип данных: double

        Свойство позволяет установить и получить отступ текста справа.

        ### Синтаксис Automation

        ```
        RightEdge = iObject.RightEdge;        # Получить свойство(* )
        iObject.RightEdge = RightEdge;        # Установить свойство (* )
        RightEdge = iObject.GetRightEdge ();  # Получить свойство (**)
        iObject.SetRightEdge (RightEdge);     # Установить свойство (**)
        ```
        """
        ...
    @RightEdge.setter
    def RightEdge(self, value: float): ...

    @property
    def SectionBlockIndex(self) -> int:  # itextline_sectionblockindex.html
        """
        ## SectionBlockIndex - Индекс листа в разделе

        Тип данных: long

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        SectionBlockIndex = Object.SectionBlockIndex       # Получить свойство(* )
        SectionBlockIndex = Object.GetSectionBlockIndex()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def Step(self) -> float:  # itextline_step.html
        """
        ## Step - Шаг строк

        Тип данных: double

        Свойство позволяет установить и получить шаг строк.

        Строка представляет собой абзац текста и может размещаться на нескольких строках, если включено выравнивание текста по горизонтали и вертикали.

        ### Синтаксис Automation

        ```
        Step = iObject.Step;        # Получить свойство(* )
        iObject.Step = Step;        # Установить свойство (* )
        Step = iObject.GetStep ();  # Получить свойство (**)
        iObject.SetStep (Step);     # Установить свойство (**)
        ```
        """
        ...
    @Step.setter
    def Step(self, value: float): ...

    @property
    def StepAfterParagraph(self) -> float:  # itextline_stepafterparagraph.html
        """
        ## StepAfterParagraph - Дополнительный шаг после абзаца

        Тип данных: double

        Свойство позволяет установить и получить дополнительный шаг после абзаца.

        ### Синтаксис Automation

        ```
        StepAfterParagraph = iObject.StepAfterParagraph;        # Получить свойство(* )
        iObject.StepAfterParagraph = StepAfterParagraph;        # Установить свойство (* )
        StepAfterParagraph = iObject.GetStepAfterParagraph ();  # Получить свойство (**)
        iObject.SetStepAfterParagraph (StepAfterParagraph);     # Установить свойство (**)
        ```
        """
        ...
    @StepAfterParagraph.setter
    def StepAfterParagraph(self, value: float): ...

    @property
    def StepBeforeParagraph(self) -> float:  # itextline_stepbeforeparagraph.html
        """
        ## StepBeforeParagraph - Дополнительный шаг перед абзацем

        Тип данных: double

        Свойство позволяет установить и получить дополнительный шаг перед абзацем.

        ### Синтаксис Automation

        ```
        StepBeforeParagraph = iObject.StepBeforeParagraph;     # Получить свойство(* )
        iObject.StepBeforeParagraph = StepBeforeParagraph;     # Установить свойство (* )
        iObject.GetStepBeforeParagraph ();                     # Получить свойство (**)
        iObject.SetStepBeforeParagraph (StepBeforeParagraph);  # Установить свойство (**)
        ```
        """
        ...
    @StepBeforeParagraph.setter
    def StepBeforeParagraph(self, value: float): ...

    @property
    def Str(self) -> str:  # itextline_str.html
        """
        ## Str - Текст в виде строки

        Тип данных: BSTR (строка)

        Свойство позволяет установить и получить текст в виде массива символов.

        Дроби представляются в формате $d числитель $ знаменатель $.

        Выражение типа суммы в формате $ верхний индекс $ нижний индекс $.

        Спецзнак в формате &N или @N.

        При замене строки очищаются и создаются новые компоненты.

        ### Синтаксис Automation

        ```
        Str = iObject.Str;        # Получить свойство(* )
        iObject.Str = Str;        # Установить свойство (* )
        Str = iObject.GetStr ();  # Получить свойство (**)
        iObject.SetStr (Str);     # Установить свойство (**)
        ```
        """
        ...
    @Str.setter
    def Str(self, value: str): ...

    @property
    def Style(self) -> int:  # itextline_style.html
        """
        ## Style - Cтиль текста

        Тип данных: long

        1. Свойство позволяет получить и установить стиль текста.

        2. Стиль может быть системным из перечисления ksTextStyleEnum или пользовательским.

        ### Синтаксис Automation

        ```
        Style = iObject.Style;        # Получить свойство(* )
        iObject.Style = Style;        # Установить свойство (* )
        Style = iObject.GetStyle ();  # Получить свойство (**)
        iObject.SetStyle (Style);     # Установить свойство (**)
        ```
        """
        ...
    @Style.setter
    def Style(self, value: int): ...

    @property
    def Tabulators(self) -> ITabulators:  # itextline_tabulators.html
        """
        ## Tabulators - Коллекция позиции табуляторов

        Тип данных: Указатель на интерфейс ITabulators

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        Tabulators = Object.Tabulators;       # Получить свойство(* )
        Tabulators = Object.GetTabulators();  # Получить свойство (**)
        ```
        """
        ...

    @property
    def TextItems(self) -> list[IDispatch]:  # itextline_textitems.html
        """
        ## TextItems - Массив SAFEARRAY компонентов строки текста

        Тип данных: VARIANT ( VT_ARRAY|VT_DISPATCH )

        Свойство позволяет получить массив SAFEARRAY компонентов строки текста. Это массив интерфейсов ITextItem. Если возвращается один объект, то тип VARIANT-а будет VT_DISPATCH, если возвращается несколько объектов, то тип VARIANT-а будет VT_ARRAY | VT_DISPATCH.

        ### Синтаксис Automation

        ```
        TextItems = iObject.TextItems;        # Получить свойство(* )
        TextItems = iObject.GetTextItems ();  # Получить свойство (**)
        ```
        """
        ...

    @property
    def TextLineData(self) -> IKompasAPIObject:  # itextline_textlinedata.html
        """
        ## TextLineData - Интерфейс дополнительных данных строки

        Тип данных: IKompasAPIObject

        1. Свойство доступно только для чтения.

        2. В зависимости от типа строки TextLineType возвращаются следующие значения свойства:

        •ksTLText - NULL,

        •ksTLVerticalText - указатель на интерфейс IText,

        •ksTLFragment - указатель на интерфейс IInsertionParameters,

        •ksTLRaster - указатель на интерфейс IInsertionParameters,

        •ksTLTable - указатель на интерфейс ITextTable.

        ### Синтаксис Automation

        ```
        TextLineData = Object.TextLineData        # Получить свойство(* )
        TextLineData = Object.GetTextLineData ()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def TextLineType(self):  # itextline_textlinetype.html
        """
        ## TextLineType - Тип строки

        Тип данных: ksTextLineType

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        TextLineType = Object.TextLineType        # Получить свойство(* )
        TextLineType = Object.GetTextLineType ()  # Получить свойство (**)
        ```
        """
        ...

    def Add(self) -> bool:  # itextline_add.html
        """
        ## Add - Добавить компонент строки в конец строки

        Позволяет добавить компонент строки в конец строки.

        ### Синтаксис Automation

        ```
        LPDISPATCH Add ();
        ```

        ### Возвращаемое значение

        +---------------------------------------------------------------------+
        | TRUE | - указатель на интерфейс ITextItem компонента строки текста. |
        +---------------------------------------------------------------------+
        """
        ...

    def AddBefore(self, Index) -> str:  # itextline_addbefore.html
        """
        ## AddBefore - Добавить компонент строки перед компонентом с заданным индексом

        Позволяет добавить компонент строки перед компонентом с заданным индексом.

        ### Синтаксис Automation

        ```
        LPDISPATCH AddBefore ( long Index );
        ```

        ### Выходные параметры

        +------------------------------------------------------------------------------------------+
        | Index (long) | - индекс компонента строки, перед которым нужно вставить новый компонент. |
        +------------------------------------------------------------------------------------------+

        ### Возвращаемое значение

        - указатель на интерфейс ITextItem строка текста.
        """
        ...

    def Assign(self, Other) -> bool:  # itextline_assign.html
        """
        ## Assign - Скопировать текст

        ### Синтаксис Automation

        ```
        BOOL Assign( LPDISPATCH Other );
        ```

        ### Входные параметры

        +----------------------------------------------------------------+
        | Other | - копируемый текст - указатель на интерфейс ITextLine. |
        +----------------------------------------------------------------+

        ### Возвращаемое значение

        +----------------------------------------+
        | TRUE | - в случае удачного завершения. |
        +----------------------------------------+
        """
        ...

    def Clear(self) -> bool:  # itextline_clear.html
        """
        ## Clear - Очистить строку текста

        Позволяет очистить строку текста. После этого можно вводить новую строку.

        ### Синтаксис Automation

        ```
        BOOL Clear ();
        ```

        ### Возвращаемое значение

        +---------------------------+
        | TRUE  | - строка очищена, |
        +---------------------------+
        | FALSE | - ошибка.         |
        +---------------------------+
        """
        ...

    def Delete(self) -> bool:  # itextline_delete.html
        """
        ## Delete - Удалить строку текста

        Позволяет удалить строку текста.

        ### Синтаксис Automation

        ```
        BOOL Delete ();
        ```

        ### Возвращаемое значение

        +----------------------------------+
        | TRUE  | - строка текста удалена, |
        +----------------------------------+
        | FALSE | - ошибка.                |
        +----------------------------------+
        """
        ...

    def Edit(self, HWnd) -> bool:  # itextline_edit.html
        """
        ## Edit - Редактировать в окне

        ### Синтаксис Automation

        ```
        BOOL Edit( OLE_HANDLE HWnd );
        ```

        ### Возвращаемое значение

        +--------------------------------------+
        | TRUE  | - в случае изменения текста, |
        +--------------------------------------+
        | FALSE | - в случае отмены.           |
        +--------------------------------------+
        """
        ...

    def Find(self, StartPosition, SrcText, Case, WordOnly):  # itextline_find.html
        """
        ## Find - Найти текст, начиная с заданной позиции

        ### Синтаксис Automation

        ```
        int64 Find( int64 StartPosition, BSTR SrcText, BOOL Case, BOOL WordOnly );
        ```

        ### Входные параметры

        +---------------------------------------------------------------------------------+
        | StartPosition | - начальная позиция для поиска,                                 |
        +---------------------------------------------------------------------------------+
        | SrcText       | - искомая подстрока,                                            |
        +---------------------------------------------------------------------------------+
        | NewText       | - новая строка,                                                 |
        +---------------------------------------------------------------------------------+
        | Case          | - TRUE - различать регистр, - FALSE - поиск без учета регистра, |
        +---------------------------------------------------------------------------------+
        | WordOnly      | - TRUE - только слово целиком,                                  |
        +---------------------------------------------------------------------------------+

        Версия Компас v21

        ### Возвращаемое значение

        Позиция найденного текста или -1, если текст не найден.
        """
        ...

    def InsertHypertextReference(self, TextItemIndex, Object, Type, Brackets, TextLineIndex, Precision, PropertyId):  # itextline_inserthypertextreference.html
        """
        ## InsertHypertextReference - Добавить ссылку в текст

        ### Синтаксис Automation

        ```
        LPDISPATCH InsertHypertextReference( long TextItemIndex,
        IKompasAPIObject * Object,
        ksHypertextTypeEnum Type,
        BOOL Brackets,
        long TextLineIndex,
        long Precision,
        double PropertyId);
        ```

        ### Входные параметры

        +------------------------------------------------------+
        | TextItemIndex | - индекс компонента текста,          |
        +------------------------------------------------------+
        | Object        | - объект, на который сделана ссылка, |
        +------------------------------------------------------+
        | Type          | - тип ссылки,                        |
        +------------------------------------------------------+
        | Brackets      | - признак скобок,                    |
        +------------------------------------------------------+
        | TextLineIndex | - индекс строки в тексте,            |
        +------------------------------------------------------+
        | Precision     | - точность вывода числовых значений, |
        +------------------------------------------------------+
        | PropertyId    | - идентификатор свойства.            |
        +------------------------------------------------------+

        ### Возвращаемое значение

        - указатель на интерфейс компонента текста, содержащего ссылку.
        """
        ...

    def ReplaceStr(self, StartPosition, SrcText, NewText, Case, WordOnly, ReplaceAll) -> bool:  # itextline_replacestr.html
        """
        ## ReplaceStr - Заменить текст, начиная с заданной позиции

        ### Синтаксис Automation

        ```
        BOOL ReplaceStr( int64 StartPosition, BSTR SrcText, BSTR NewText, BOOL Case, BOOL WordOnly, BOOL ReplaceAll );
        ```

        ### Входные параметры

        +----------------------------------------------------------------------------------------------+
        | StartPosition | - начальная позиция для поиска,                                              |
        +----------------------------------------------------------------------------------------------+
        | SrcText       | - искомая подстрока,                                                         |
        +----------------------------------------------------------------------------------------------+
        | NewText       | - новая строка,                                                              |
        +----------------------------------------------------------------------------------------------+
        | Case          | - TRUE - различать регистр, - FALSE - поиск без учета регистра,              |
        +----------------------------------------------------------------------------------------------+
        | WordOnly      | - TRUE - только слово целиком,                                               |
        +----------------------------------------------------------------------------------------------+
        | ReplaceAll    | - TRUE - заменить все вхождения, - FALSE - заменить только первое вхождение. |
        +----------------------------------------------------------------------------------------------+

        Версия Компас v21

        ### Возвращаемое значение

        +--------------------------------------+
        | TRUE  | - в случае изменения текста, |
        +--------------------------------------+
        | FALSE | - в случае отмены.           |
        +--------------------------------------+
        """
        ...

    def TextItem(self, Index) -> ITextItem:  # itextline_textitem.html
        """
        ## TextItem - Компонент строки текста с заданным индексом

        Тип данных: указатель на интерфейс компонента строки текста ITextItem

        Свойство позволяет получить интерфейс компонента строки текста ITextItem.

        ### Синтаксис Automation

        ```
        TextItem = iObject.TextItem (Index);     # Получить свойство(* )
        TextItem = iObject.GetTextItem (Index);  # Получить свойство (**)
        ```

        ### Входные параметры

        +----------------------------------------------+
        | Index ( long ) | - индекс компонента строки. |
        +----------------------------------------------+
        """
        ...




class ITextStyle(IKompasAPIObject):  # itextstyle.html
    """
    ## Интерфейс ITextStyle

    Интерфейс параметров стиля текста.

    Данный интерфейс можно получить от интерфейса настроек спецификации ISpecificationTuning, используя:

    •ISpecificationTuning::SectionTextStyleFirst;

    •ISpecificationTuning::SectionTextStyleNext;

    •ISpecificationTuning::ObjectTextStyle;

    •ISpecificationTuning::PerformanceBlockTextStyleFirst;

    •ISpecificationTuning::PerformanceBlockTextStyleNext;

    •ISpecificationTuning::AdditionalBlockTextStyleFirst;

    •ISpecificationTuning::AdditionalBlockTextStyleNext;

    •ISpecificationTuning::NestingBlockTextStyleFirst;

    •ISpecificationTuning::NestingBlockTextStyleNext.
    """

    @property
    def Align(self) -> int:  # itextstyle_align.html
        """
        ## Align - Выравнивание

        Тип данных: из перечисления ksAlignEnum

        1. Свойство позволяет получить/изменить выравнивание абзацев (по левой границе, по правой границе, центрирование, выравнивание по двум границам).

        2. Свойство используется для стиля, полученного через методы интерфейса ISpecificationTuning.

        ### Синтаксис Automation

        ```
        Align = iObject.Align       # Получить свойство(* )
        iObject.Align = Align       # Установить свойство (* )
        Align = iObject.GetAlign()  # Получить свойство (**)
        iObject.SetAlign (Align)    # Установить свойство (**)
        ```
        """
        ...
    @Align.setter
    def Align(self, value: int): ...

    @property
    def EnableLine1(self) -> bool:  # itextstyle_enableline1.html
        """
        ## EnableLine1 - Разрешена ли первая комбинация для использования

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        EnableLine1 = Object.EnableLine1       # Получить свойство(* )
        Object.EnableLine1 = EnableLine1       # Установить свойство (* )
        EnableLine1 = Object.GetEnableLine1()  # Получить свойство (**)
        Object.SetEnableLine1 (EnableLine1)    # Установить свойство (**)
        ```
        """
        ...
    @EnableLine1.setter
    def EnableLine1(self, value: bool): ...

    @property
    def EnableLine2(self) -> bool:  # itextstyle_enableline2.html
        """
        ## EnableLine2 - Разрешена ли вторая комбинация для использования

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        EnableLine2 = Object.EnableLine2       # Получить свойство(* )
        Object.EnableLine2 = EnableLine2       # Установить свойство (* )
        EnableLine2 = Object.GetEnableLine2()  # Получить свойство (**)
        Object.SetEnableLine2 (EnableLine2)    # Установить свойство (**)
        ```
        """
        ...
    @EnableLine2.setter
    def EnableLine2(self, value: bool): ...

    @property
    def EnableLine3(self) -> bool:  # itextstyle_enableline3.html
        """
        ## EnableLine3 - Разрешена ли третья комбинация для использования

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        EnableLine3 = Object.EnableLine3       # Получить свойство(* )
        Object.EnableLine3 = EnableLine3       # Установить свойство (* )
        EnableLine3 = Object.GetEnableLine3()  # Получить свойство (**)
        Object.SetEnableLine3 (EnableLine3)    # Установить свойство (**)
        ```
        """
        ...
    @EnableLine3.setter
    def EnableLine3(self, value: bool): ...

    @property
    def Extended(self) -> bool:  # itextstyle_extended.html
        """
        ## Extended - Расширенный стиль текста

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        Extended = Object.Extended       # Получить свойство(* )
        Object.Extended = Extended       # Установить свойство (* )
        Extended = Object.GetExtended()  # Получить свойство (**)
        Object.SetExtended (Extended)    # Установить свойство (**)
        ```
        """
        ...
    @Extended.setter
    def Extended(self, value: bool): ...

    @property
    def Font(self) -> ITextFont:  # itextstyle_font.html
        """
        ## Font - Параметры шрифта

        Тип данных: указатель на интерфейс ITextFont параметров шрифта

        1. Свойство позволяет параметры шрифта.

        2. Свойство доступно только для чтения.

        3. Свойство используется для стиля, полученного через методы интерфейса ISpecificationTuning.

        ### Синтаксис Automation

        ```
        Font = iObject.Font       # Получить свойство(* )
        Font = iObject.GetFont()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def Height1(self) -> float:  # itextstyle_height1.html
        """
        ## Height1 - Высота первой строки текста

        Тип данных: double

        ### Синтаксис Automation

        ```
        Height1 = Object.Height1       # Получить свойство(* )
        Object.Height1 = Height1       # Установить свойство (* )
        Height1 = Object.GetHeight1()  # Получить свойство (**)
        Object.SetHeight1 (Height1)    # Установить свойство (**)
        ```
        """
        ...
    @Height1.setter
    def Height1(self, value: float): ...

    @property
    def Height2(self) -> float:  # itextstyle_height2.html
        """
        ## Height2 - Высота второй строки текста

        Тип данных: double

        ### Синтаксис Automation

        ```
        Height2 = Object.Height2       # Получить свойство(* )
        Object.Height2 = Height2       # Установить свойство (* )
        Height2 = Object.GetHeight2()  # Получить свойство (**)
        Object.SetHeight2 (Height2)    # Установить свойство (**)
        ```
        """
        ...
    @Height2.setter
    def Height2(self, value: float): ...

    @property
    def Height3(self) -> float:  # itextstyle_height3.html
        """
        ## Height3 - Высота третьей строки текста

        Тип данных: double

        ### Синтаксис Automation

        ```
        Height3 = Object.Height3       # Получить свойство(* )
        Object.Height3 = Height3       # Установить свойство (* )
        Height3 = Object.GetHeight3()  # Получить свойство (**)
        Object.SetHeight3 (Height3)    # Установить свойство (**)
        ```
        """
        ...
    @Height3.setter
    def Height3(self, value: float): ...

    @property
    def IndentedLine(self) -> float:  # itextstyle_indentedline.html
        """
        ## IndentedLine - Смещение красной строки

        Тип данных: double

        ### Синтаксис Automation

        ```
        IndentedLine = Object.IndentedLine       # Получить свойство(* )
        Object.IndentedLine = IndentedLine       # Установить свойство (* )
        IndentedLine = Object.GetIndentedLine()  # Получить свойство (**)
        Object.SetIndentedLine (IndentedLine)    # Установить свойство (**)
        ```
        """
        ...
    @IndentedLine.setter
    def IndentedLine(self, value: float): ...

    @property
    def LeftEdge(self) -> float:  # itextstyle_leftedge.html
        """
        ## LeftEdge - Отступ текста слева

        Тип данных: double

        ### Синтаксис Automation

        ```
        LeftEdge = Object.LeftEdge       # Получить свойство(* )
        Object.LeftEdge = LeftEdge       # Установить свойство (* )
        LeftEdge = Object.GetLeftEdge()  # Получить свойство (**)
        Object.SetLeftEdge (LeftEdge)    # Установить свойство (**)
        ```
        """
        ...
    @LeftEdge.setter
    def LeftEdge(self, value: float): ...

    @property
    def LinesCount(self) -> int:  # itextstyle_linescount.html
        """
        ## LinesCount - Количество строк

        Тип данных: long

        ### Синтаксис Automation

        ```
        LinesCount = Object.LinesCount       # Получить свойство(* )
        Object.LinesCount = LinesCount       # Установить свойство (* )
        LinesCount = Object.GetLinesCount()  # Получить свойство (**)
        Object.SetLinesCount (LinesCount)    # Установить свойство (**)
        ```
        """
        ...
    @LinesCount.setter
    def LinesCount(self, value: int): ...

    @property
    def Name(self) -> str:  # itextstyle_name.html
        """
        ## Name - Имя стиля

        Тип данных: BSTR

        ### Синтаксис Automation

        ```
        Name = Object.Name       # Получить свойство(* )
        Object.Name = Name       # Установить свойство (* )
        Name = Object.GetName()  # Получить свойство (**)
        Object.SetName (Name)    # Установить свойство (**)
        ```
        """
        ...
    @Name.setter
    def Name(self, value: str): ...

    @property
    def Number(self) -> int:  # itextstyle_number.html
        """
        ## Number - Номер стиля

        Тип данных: long

        ### Синтаксис Automation

        ```
        Number = Object.Number       # Получить свойство(* )
        Object.Number = Number       # Установить свойство (* )
        Number = Object.GetNumber()  # Получить свойство (**)
        Object.SetNumber (Number)    # Установить свойство (**)
        ```
        """
        ...
    @Number.setter
    def Number(self, value: int): ...

    @property
    def RightEdge(self) -> float:  # itextstyle_rightedge.html
        """
        ## RightEdge - Отступ текста справа

        Тип данных: double

        ### Синтаксис Automation

        ```
        RightEdge = Object.RightEdge       # Получить свойство(* )
        Object.RightEdge = RightEdge       # Установить свойство (* )
        RightEdge = Object.GetRightEdge()  # Получить свойство (**)
        Object.SetRightEdge (RightEdge)    # Установить свойство (**)
        ```
        """
        ...
    @RightEdge.setter
    def RightEdge(self, value: float): ...

    @property
    def Step(self) -> float:  # itextstyle_step.html
        """
        ## Step - Шаг строк

        Тип данных: double

        1. Свойство позволяет получить/изменить шаг строк (расстояние между строками текста).

        2. Значение шага задается в миллиметрах.

        3. Свойство используется для стиля, полученного через методы интерфейса ISpecificationTuning.

        ### Синтаксис Automation

        ```
        Step = iObject.Step       # Получить свойство(* )
        iObject.Step = Step       # Установить свойство (* )
        Step = iObject.GetStep()  # Получить свойство (**)
        iObject.SetStep (Step)    # Установить свойство (**)
        ```
        """
        ...
    @Step.setter
    def Step(self, value: float): ...

    @property
    def Step1(self) -> float:  # itextstyle_step1.html
        """
        ## Step1 - Шаг первой строки текста

        Тип данных: double

        ### Синтаксис Automation

        ```
        Step1 = Object.Step1       # Получить свойство(* )
        Object.Step1 = Step1       # Установить свойство (* )
        Step1 = Object.GetStep1()  # Получить свойство (**)
        Object.SetStep1 (Step1)    # Установить свойство (**)
        ```
        """
        ...
    @Step1.setter
    def Step1(self, value: float): ...

    @property
    def Step2(self) -> float:  # itextstyle_step2.html
        """
        ## Step2 - Шаг второй строки текста

        Тип данных: double

        ### Синтаксис Automation

        ```
        Step2 = Object.Step2       # Получить свойство(* )
        Object.Step2 = Step2       # Установить свойство (* )
        Step2 = Object.GetStep2()  # Получить свойство (**)
        Object.SetStep2 (Step2)    # Установить свойство (**)
        ```
        """
        ...
    @Step2.setter
    def Step2(self, value: float): ...

    @property
    def Step3(self) -> float:  # itextstyle_step3.html
        """
        ## Step3 - Шаг третьей строки текста

        Тип данных: double

        ### Синтаксис Automation

        ```
        Step3 = Object.Step3       # Получить свойство(* )
        Object.Step3 = Step3       # Установить свойство (* )
        Step3 = Object.GetStep3()  # Получить свойство (**)
        Object.SetStep3 (Step3)    # Установить свойство (**)
        ```
        """
        ...
    @Step3.setter
    def Step3(self, value: float): ...

    @property
    def StepAfterParagraph(self) -> float:  # itextstyle_stepafterparagraph.html
        """
        ## StepAfterParagraph - Дополнительный шаг после абзаца

        Тип данных: double

        ### Синтаксис Automation

        ```
        StepAfterParagraph = Object.StepAfterParagraph       # Получить свойство(* )
        Object.StepAfterParagraph = StepAfterParagraph       # Установить свойство (* )
        StepAfterParagraph = Object.GetStepAfterParagraph()  # Получить свойство (**)
        Object.SetStepAfterParagraph (StepAfterParagraph)    # Установить свойство (**)
        ```
        """
        ...
    @StepAfterParagraph.setter
    def StepAfterParagraph(self, value: float): ...

    @property
    def StepBeforeParagraph(self) -> float:  # itextstyle_stepbeforeparagraph.html
        """
        ## StepBeforeParagraph - Дополнительный шаг перед абзацем

        Тип данных: double

        ### Синтаксис Automation

        ```
        StepBeforeParagraph = Object.StepBeforeParagraph       # Получить свойство(* )
        Object.StepBeforeParagraph = StepBeforeParagraph       # Установить свойство (* )
        StepBeforeParagraph = Object.GetStepBeforeParagraph()  # Получить свойство (**)
        Object.SetStepBeforeParagraph (StepBeforeParagraph)    # Установить свойство (**)
        ```
        """
        ...
    @StepBeforeParagraph.setter
    def StepBeforeParagraph(self, value: float): ...

    @property
    def Tabulators(self) -> ITabulators:  # itextstyle_tabulators.html
        """
        ## Tabulators - Коллекция позиции табуляторов

        Тип данных: указатель на интерфейс ITabulators

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        Tabulators = Object.Tabulators       # Получить свойство(* )
        Tabulators = Object.GetTabulators()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def WidthFactor1(self) -> float:  # itextstyle_widthfactor1.html
        """
        ## WidthFactor1 - Коэффициент сужения первой строки текста

        Тип данных: double

        ### Синтаксис Automation

        ```
        WidthFactor1 = Object.WidthFactor1       # Получить свойство(* )
        Object.WidthFactor1 = WidthFactor1       # Установить свойство (* )
        WidthFactor1 = Object.GetWidthFactor1()  # Получить свойство (**)
        Object.SetWidthFactor1 (WidthFactor1)    # Установить свойство (**)
        ```
        """
        ...
    @WidthFactor1.setter
    def WidthFactor1(self, value: float): ...

    @property
    def WidthFactor2(self) -> float:  # itextstyle_widthfactor2.html
        """
        ## WidthFactor2 - Коэффициент сужения второй строки текста

        Тип данных: double

        ### Синтаксис Automation

        ```
        WidthFactor2 = Object.WidthFactor2       # Получить свойство(* )
        Object.WidthFactor2 = WidthFactor2       # Установить свойство (* )
        WidthFactor2 = Object.GetWidthFactor2()  # Получить свойство (**)
        Object.SetWidthFactor2 (WidthFactor2)    # Установить свойство (**)
        ```
        """
        ...
    @WidthFactor2.setter
    def WidthFactor2(self, value: float): ...

    @property
    def WidthFactor3(self) -> float:  # itextstyle_widthfactor3.html
        """
        ## WidthFactor3 - Коэффициент сужения третьей строки текста

        Тип данных: double

        ### Синтаксис Automation

        ```
        WidthFactor3 = Object.WidthFactor3       # Получить свойство(* )
        Object.WidthFactor3 = WidthFactor3       # Установить свойство (* )
        WidthFactor3 = Object.GetWidthFactor3()  # Получить свойство (**)
        Object.SetWidthFactor3 (WidthFactor3)    # Установить свойство (**)
        ```
        """
        ...
    @WidthFactor3.setter
    def WidthFactor3(self, value: float): ...




class ITextTable(IKompasAPIObject):  # itexttable.html
    """
    ## Интерфейс ITextTable

    Таблица в тексте текстового документа.

    Интерфейс можно получить с помощью метода IText::AddTable и свойства ITextLine::TextLineData.
    """

    @property
    def FixedCellsSize(self) -> bool:  # itexttable_fixedcellssize.html
        """
        ## FixedCellsSize - Зафиксировать габариты ячеек

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        FixedCellsSize = Object.FixedCellsSize       # Получить свойство(* )
        Object.FixedCellsSize = FixedCellsSize       # Установить свойство (* )
        FixedCellsSize = Object.GetFixedCellsSize()  # Получить свойство (**)
        Object.SetFixedCellsSize( FixedCellsSize )   # Установить свойство (**)
        ```
        """
        ...
    @FixedCellsSize.setter
    def FixedCellsSize(self, value: bool): ...

    @property
    def FixedColumnCount(self) -> bool:  # itexttable_fixedcolumncount.html
        """
        ## FixedColumnCount - Запретить изменять число столбцов

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        FixedColumnCount = Object.FixedColumnCount       # Получить свойство(* )
        Object.FixedColumnCount = FixedColumnCount       # Установить свойство (* )
        FixedColumnCount = Object.GetFixedColumnCount()  # Получить свойство (**)
        Object.SetFixedColumnCount( FixedColumnCount )   # Установить свойство (**)
        ```
        """
        ...
    @FixedColumnCount.setter
    def FixedColumnCount(self, value: bool): ...

    @property
    def FixedRowCount(self) -> bool:  # itexttable_fixedrowcount.html
        """
        ## FixedRowCount - Запретить изменять число строк

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        FixedRowCount = Object.FixedRowCount       # Получить свойство(* )
        Object.FixedRowCount = FixedRowCount       # Установить свойство (* )
        FixedRowCount = Object.GetFixedRowCount()  # Получить свойство (**)
        Object.SetFixedRowCount( FixedRowCount )   # Установить свойство (**)
        ```
        """
        ...
    @FixedRowCount.setter
    def FixedRowCount(self, value: bool): ...

    def Save(self, FileName) -> bool:  # itexttable_save.html
        """
        ## Save - Сохранить таблицу в файл

        ### Синтаксис Automation

        ```
        BOOL Save( BSTR FileName );
        ```

        ### Входные параметры

        +--------------------------------------+
        | FileName | - имя сохраняемого файла. |
        +--------------------------------------+

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...




class ITabulators(IKompasCollection):  # itabulators.html
    """
    ## Интерфейс ITabulators

    Интерфейс коллекции позиций табулятора.

    Позволяет получить доступ к коллекции позиций табулятора, считывать параметры позиций, добавлять и удалять позиции из коллекции.
    """

    def Add(self, Position) -> ITabulator:  # itabulators_add.html
        """
        ## Add - Создает новую позицию табулятора и добавляет ее в коллекцию

        ### Синтаксис Automation

        ```
        LPDISPATCH Add( long Position );
        ```

        ### Входные параметры

        +-----------------------+
        | Position | - позиция. |
        +-----------------------+

        ### Возвращаемое значение

        - указатель на интерфейс ITabulator.
        """
        ...

    def Clear(self) -> bool:  # itabulators_clear.html
        """
        ## Clear - Очищает коллекцию, удаляет из коллекции все позиции табулятора

        ### Синтаксис Automation

        ```
        BOOL Clear();
        ```

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def Item(self, Index) -> ITabulators:  # itabulators_item.html
        """
        ## Item - Возвращает позицию табулятора, заданную по индексу

        Тип данных: указатель на интерфейс ITabulators.

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        Item = Object.Item( Index )     # Получить свойство(* )
        Item = Object.GetItem( Index )  # Получить свойство (**)
        ```
        """
        ...




class IHypertextReferenceParam(IDispatch):  # ihypertextreferenceparam.html
    """
    ## Интерфейс IHypertextReferenceParam

    Интерфейс параметров вставки ссылки в текст
    """

    @property
    def Brackets(self) -> bool:  # ihypertextreferenceparam_brackets.html
        """
        ## Brackets - Признак скобок

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        Brackets = Object.Brackets       # Получить свойство(* )
        Object.Brackets = Brackets       # Установить свойство (* )
        Brackets = Object.GetBrackets()  # Получить свойство (**)
        Object.SetBrackets( Brackets )   # Установить свойство (**)
        ```
        """
        ...
    @Brackets.setter
    def Brackets(self, value: bool): ...

    @property
    def HypertextType(self):  # ihypertextreferenceparam_hypertexttype.html
        """
        ## HypertextType - Тип ссылки

        Тип данных: из перечисления ksHypertextTypeEnum

        ### Синтаксис Automation

        ```
        HypertextType = Object.HypertextType       # Получить свойство(* )
        Object.HypertextType = HypertextType       # Установить свойство (* )
        HypertextType = Object.GetHypertextType()  # Получить свойство (**)
        Object.SetHypertextType( HypertextType )   # Установить свойство (**)
        ```
        """
        ...
    @HypertextType.setter
    def HypertextType(self, value): ...

    @property
    def LinkObject(self) -> IKompasAPIObject:  # ihypertextreferenceparam_linkobject.html
        """
        ## LinkObject - Объект, на который сделана ссылка

        Тип данных: указатель на интерфейс IKompasAPIObject

        ### Синтаксис Automation

        ```
        LinkObject = Object.LinkObject       # Получить свойство(* )
        Object.LinkObject = LinkObject       # Установить свойство (* )
        LinkObject = Object.GetLinkObject()  # Получить свойство (**)
        Object.SetLinkObject( LinkObject )   # Установить свойство (**)
        ```
        """
        ...
    @LinkObject.setter
    def LinkObject(self, value: IKompasAPIObject): ...

    @property
    def Precission(self) -> int:  # ihypertextreferenceparam_precission.html
        """
        ## Precission - Точность вывода числовых значений

        Тип данных: long

        ### Синтаксис Automation

        ```
        Precission = Object.Precission       # Получить свойство(* )
        Object.Precission = Precission       # Установить свойство (* )
        Precission = Object.GetPrecission()  # Получить свойство (**)
        Object.SetPrecission( Precission )   # Установить свойство (**)
        ```
        """
        ...
    @Precission.setter
    def Precission(self, value: int): ...

    @property
    def PropertyId(self):
        ...
    @PropertyId.setter
    def PropertyId(self, value): ...

    @property
    def TextLineIndex(self) -> int:  # ihypertextreferenceparam_textlineindex.html
        """
        ## TextLineIndex - Индекс строки в тексте

        Тип данных: long

        ### Синтаксис Automation

        ```
        TextLineIndex = Object.TextLineIndex       # Получить свойство(* )
        Object.TextLineIndex = TextLineIndex       # Установить свойство (* )
        TextLineIndex = Object.GetTextLineIndex()  # Получить свойство (**)
        Object.SetTextLineIndex( TextLineIndex )   # Установить свойство (**)
        ```
        """
        ...
    @TextLineIndex.setter
    def TextLineIndex(self, value: int): ...

    def Destroy(self) -> bool:  # ihypertextreferenceparam_destroy.html
        """
        ## Destroy - Разрушить ссылку

        ### Синтаксис Automation

        ```
        BOOL Destroy();
        ```

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+

        Метод позволяет удалить связь между ссылкой и ее источником. В результате разрушения содержимое ссылки становится обычным текстом. Обновление разрушенной ссылки становится невозможным.
        """
        ...




class IAuxiliaryGeomContainer(IDispatch):  # iauxiliarygeomcontainer.html
    """
    ## Интерфейс IAuxiliaryGeomContainer

    Интерфейс контейнера объектов вспомогательной геометрии.

    Позволяет получить коллекции объектов вспомогательной геометрии (ЛСК, сплайн и т.д).

    Дополнительный интерфейс компонента. Данный интерфейс можно получить у компонента IPart7 посредством вызова метода IUnknown::QueryInterface (const GUID far& iid, void** pif).
    """

    @property
    def Arcs3D(self) -> IArcs3D:  # iauxiliarygeomcontainer_arcs3d.html
        """
        ## Arcs3D - Интерфейс коллекции дуг 3D

        Тип данных: указатель на интерфейс IArcs3D

        1. Свойство позволяет получить коллекцию всех дуг, входящих в состав данного объекта.

        2. Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        Arcs3D = Object.Arcs3D       # Получить свойство (* )
        Arcs3D = Object.GetArcs3D()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def Axes3D(self) -> IAxes3D:  # iauxiliarygeomcontainer_axes3d.html
        """
        ## Axes3D - Интерфейс коллекции вспомогательных осей 3D

        Тип данных: указатель на интерфейс IAxes3D

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        Axes3D = Object.Axes3D;       # Получить свойство (* )
        Axes3D = Object.GetAxes3D();  # Получить свойство (**)
        ```
        """
        ...

    @property
    def ConicCurves3D(self) -> IConicCurves3D:  # iauxiliarygeomcontainer_coniccurves3d.html
        """
        ## ConicCurves3D - Коллекция конических кривых 3D

        Тип данных: указатель на интерфейс IConicCurves3D

        Свойство доступно только для чтения.

        Версия КОМПАС v22

        ### Синтаксис Automation

        ```
        ConicCurves3D = Object.ConicCurves3D       # Получить свойство (* )
        ConicCurves3D = Object.GetConicCurves3D()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def ConjunctivePoints(self) -> IConjunctivePoints:  # iauxiliarygeomcontainer_conjunctivepoints.html
        """
        ## ConjunctivePoints - Интерфейс коллекции присоединительных точек

        Тип данных: указатель на интерфейс IConjunctivePoints

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        ConjunctivePoints = Object.ConjunctivePoints;       # Получить свойство (* )
        ConjunctivePoints = Object.GetConjunctivePoints();  # Получить свойство (**)
        ```
        """
        ...

    @property
    def ConnectCurves(self) -> IConnectCurves:  # iauxiliarygeomcontainer_connectcurves.html
        """
        ## ConnectCurves - Интерфейс коллекции операций соединения кривых

        Тип данных: указатель на интерфейс IConnectCurves

        1. Свойство позволяет получить коллекцию всех операций соединения кривых, входящих в состав данного объекта.

        2. Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        ConnectCurves = Object.ConnectCurves       # Получить свойство (* )
        ConnectCurves = Object.GetConnectCurves()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def Contours3D(self) -> IContours3D:  # iauxiliarygeomcontainer_contours3d.html
        """
        ## Contours3D - Интерфейс коллекции контуров 3D

        Тип данных: указатель на интерфейс IContours3D

        1. Свойство позволяет получить коллекцию всех контуров 3D, входящих в состав данного объекта.

        2. Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        Contours3D = Object.Contours3D       # Получить свойство (* )
        Contours3D = Object.GetContours3D()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def ControlPoints(self) -> IControlPoints:  # iauxiliarygeomcontainer_controlpoints.html
        """
        ## ControlPoints - Интерфейс коллекции контрольных точек

        Тип данных: указатель на интерфейс IControlPoints

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        ControlPoints = Object.ControlPoints;       # Получить свойство (* )
        ControlPoints = Object.GetControlPoints();  # Получить свойство (**)
        ```
        """
        ...

    @property
    def CurveByLaws(self) -> ICurveByLaws:  # iauxiliarygeomcontainer_curvebylaws.html
        """
        ## CurveByLaws - Интерфейс коллекции кривых по закону

        Тип данных: указатель на интерфейс ICurveByLaws

        1. Свойство позволяет получить коллекцию всех кривых по закону, входящих в состав данного объекта.

        2. Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        CurveByLaws = Object.CurveByLaws       # Получить свойство (* )
        CurveByLaws = Object.GetCurveByLaws()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def CurveOutLines(self) -> ICurveOutLines:  # iauxiliarygeomcontainer_curveoutlines.html
        """
        ## CurveOutLines - Интерфейс коллекции линий очерка

        Тип данных: указатель на интерфейс ICurveOutLines

        1. Свойство позволяет получить коллекцию всех линий очерка, входящих в состав данного объекта.

        2. Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        CurveOutLines = Object.CurveOutLines       # Получить свойство (* )
        CurveOutLines = Object.GetCurveOutLines()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def CurvesBy2Projectionses(self) -> ICurvesBy2Projectionses:  # iauxiliarygeomcontainer_curvesby2projectionses.html
        """
        ## CurvesBy2Projectionses - Интерфейс коллекции кривых по двум проекциям

        Тип данных: указатель на интерфейс ICurvesBy2Projectionses

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        CurvesBy2Projectionses = Object.CurvesBy2Projectionses;       # Получить свойство (* )
        CurvesBy2Projectionses = Object.GetCurvesBy2Projectionses();  # Получить свойство (**)
        ```
        """
        ...

    @property
    def Equidistants3D(self) -> IEquidistants3D:  # iauxiliarygeomcontainer_equidistants3d.html
        """
        ## Equidistants3D - Интерфейс коллекции 3D-эквидистант

        Тип данных: указатель на интерфейс IEquidistants3D

        1. Свойство позволяет получить коллекцию 3D-эквидистант.

        2. Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        Equidistants3D = Object.Equidistants3D       # Получить свойство (* )
        Equidistants3D = Object.GetEquidistants3D()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def ExtensionCurves(self) -> IExtensionCurves:  # iauxiliarygeomcontainer_extensioncurves.html
        """
        ## ExtensionCurves - Коллекция продлений кривых

        Тип данных: указатель на интерфейс IExtensionCurves

        Свойство доступно только для чтения.

        Версия КОМПАС v22

        ### Синтаксис Automation

        ```
        ExtensionCurves = Object.ExtensionCurves       # Получить свойство (* )
        ExtensionCurves = Object.GetExtensionCurves()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def FilletCurves(self) -> IFilletCurves:  # iauxiliarygeomcontainer_filletcurves.html
        """
        ## FilletCurves - Интерфейс коллекции операций скругления кривых

        Тип данных: указатель на интерфейс IFilletCurves

        1. Свойство позволяет получить коллекцию всех операций скругления кривых, входящих в состав данного объекта.

        2. Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        FilletCurves = Object.FilletCurves       # Получить свойство (* )
        FilletCurves = Object.GetFilletCurves()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def IsoparametricCurves(self) -> IIsoparametricCurves:  # iauxiliarygeomcontainer_isoparametriccurves.html
        """
        ## IsoparametricCurves - Интерфейс коллекции изопараметрических кривых

        Тип данных: указатель на интерфейс IIsoparametricCurves

        1. Свойство позволяет получить коллекцию всех изопараметрических кривых, входящих в состав данного объекта.

        2. Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        IsoparametricCurves = Object.IsoparametricCurves       # Получить свойство (* )
        IsoparametricCurves = Object.GetIsoparametricCurves()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def IsoparametricCurvesSets(self) -> IIsoparametricCurvesSets:  # iauxiliarygeomcontainer_isoparametriccurvessets.html
        """
        ## IsoparametricCurvesSets - Интерфейс коллекции групп изопараметрических кривых

        Тип данных: указатель на интерфейс IIsoparametricCurvesSets

        1. Свойство позволяет получить коллекцию всех групп изопараметрических кривых, входящих в состав данного объекта.

        2. Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        IsoparametricCurvesSets = Object.IsoparametricCurvesSets       # Получить свойство (* )
        IsoparametricCurvesSets = Object.GetIsoparametricCurvesSets()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def LineSegments3D(self) -> ILineSegments3D:  # iauxiliarygeomcontainer_linesegments3d.html
        """
        ## LineSegments3D - Интерфейс коллекции отрезков 3D

        Тип данных: указатель на интерфейс ILineSegments3D

        1. Свойство позволяет получить коллекцию всех отрезков 3D, входящих в состав данного объекта.

        2. Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        LineSegments3D = Object.LineSegments3D       # Получить свойство (* )
        LineSegments3D = Object.GetLineSegments3D()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def LocalCoordinateSystems(self) -> ILocalCoordinateSystems:  # iauxiliarygeomcontainer_localcoordinatesystems.html
        """
        ## LocalCoordinateSystems - Интерфейс коллекции локальных систем координат

        Тип данных: указатель на интерфейс ЛСК ILocalCoordinateSystems

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        LocalCoordinateSystems = Object.LocalCoordinateSystems;  # Получить свойство (* )
        Object.LocalCoordinateSystems = LocalCoordinateSystems;  # Получить свойство (**)
        ```
        """
        ...

    @property
    def Planes3D(self) -> IPlanes3D:  # iauxiliarygeomcontainer_planes3d.html
        """
        ## Planes3D - Интерфейс коллекции плоскостей

        Тип данных: указатель на интерфейс IPlanes3D

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        Planes3D = Object.Planes3D       # Получить свойство (* )
        Planes3D = Object.GetPlanes3D()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def PointsArrsFromFiles(self) -> IPointsArrsFromFiles:  # iauxiliarygeomcontainer_pointsarrsfromfiles.html
        """
        ## PointsArrsFromFiles - Интерфейс коллекции групп точек из файлов

        Тип данных: указатель на интерфейс IPointsArrsFromFiles

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        PointsArrsFromFiles = Object.PointsArrsFromFiles       # Получить свойство (* )
        PointsArrsFromFiles = Object.GetPointsArrsFromFiles()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def PointsArrsOnCurves(self) -> IPointsArrsOnCurves:  # iauxiliarygeomcontainer_pointsarrsoncurves.html
        """
        ## PointsArrsOnCurves - Интерфейс коллекции групп точек по кривым

        Тип данных: указатель на интерфейс IPointsArrsOnCurves

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        PointsArrsOnCurves = Object.PointsArrsOnCurves       # Получить свойство (* )
        PointsArrsOnCurves = Object.GetPointsArrsOnCurves()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def PointsArrsOnSurfaces(self) -> IPointsArrsOnSurfaces:  # iauxiliarygeomcontainer_pointsarrsonsurfaces.html
        """
        ## PointsArrsOnSurfaces - Интерфейс коллекции групп точек по поверхности

        Тип данных: указатель на интерфейс IPointsArrsOnSurfaces

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        PointsArrsOnSurfaces = Object.PointsArrsOnSurfaces       # Получить свойство (* )
        PointsArrsOnSurfaces = Object.GetPointsArrsOnSurfaces()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def PolyLines(self) -> IPolyLines:  # iauxiliarygeomcontainer_polylines.html
        """
        ## PolyLines - Интерфейс коллекции пространственных ломаных

        Тип данных: указатель на интерфейс IPolyLines

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        PolyLines = Object.PolyLines;  # Получить свойство (* )
        Object.PolyLines = PolyLines;  # Получить свойство (**)
        ```
        """
        ...

    @property
    def ProjectionCurves(self) -> IProjectionCurves:  # iauxiliarygeomcontainer_projectioncurves.html
        """
        ## ProjectionCurves - Интерфейс коллекции проекционных кривых

        Тип данных: указатель на интерфейс IProjectionCurves

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        ProjectionCurves = Object.ProjectionCurves;       # Получить свойство (* )
        ProjectionCurves = Object.GetProjectionCurves();  # Получить свойство (**)
        ```
        """
        ...

    @property
    def Spirals3D(self) -> ISpirals3D:  # iauxiliarygeomcontainer_spirals3d.html
        """
        ## Spirals3D - Интерфейс коллекции спиралей 3D

        Тип данных: указатель на интерфейс ISpirals3D

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        Spirals3D = Object.Spirals3D       # Получить свойство (* )
        Spirals3D = Object.GetSpirals3D()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def Splines3D(self) -> ISplines3D:  # iauxiliarygeomcontainer_splines3d.html
        """
        ## Splines3D - Интерфейс коллекции пространственных сплайнов

        Тип данных: указатель на интерфейс ISplines3D

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        Splines3D = Object.Splines3D;  # Получить свойство (* )
        Object.Splines3D = Splines3D;  # Получить свойство (**)
        ```
        """
        ...

    @property
    def SplinesOnSurfaces(self) -> ISplinesOnSurfaces:  # iauxiliarygeomcontainer_splinesonsurfaces.html
        """
        ## SplinesOnSurfaces - Интерфейс коллекции сплайнов на поверхностях

        Тип данных: указатель на интерфейс ISplinesOnSurfaces

        1. Свойство позволяет получить коллекцию всех сплайнов на поверхностях, входящих в состав данного объекта.

        2. Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        SplinesOnSurfaces = Object.SplinesOnSurfaces       # Получить свойство (* )
        SplinesOnSurfaces = Object.GetSplinesOnSurfaces()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def SplitLines(self) -> ISplitLines:  # iauxiliarygeomcontainer_splitlines.html
        """
        ## SplitLines - Интерфейс коллекции линий разъема

        Тип данных: указатель на интерфейс ISplitLines

        1. Свойство позволяет получить коллекцию линий разъема.

        2. Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        SplitLines = Object.SplitLines();     # Получить свойство (* )
        SplitLines = Object.GetSplitLines();  # Получить свойство (**)
        ```
        """
        ...

    @property
    def SurfacesIntersectionCurves(self) -> ISurfacesIntersectionCurves:  # iauxiliarygeomcontainer_surfacesintersectioncurves.html
        """
        ## SurfacesIntersectionCurves - Интерфейс коллекции кривых пересечений поверхностей

        Тип данных: указатель на интерфейс ISurfacesIntersectionCurves

        1. Свойство позволяет получить коллекцию кривых пересечений поверхностей.

        2. Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        SurfacesIntersectionCurves = Object.SurfacesIntersectionCurves       # Получить свойство (* )
        SurfacesIntersectionCurves = Object.GetSurfacesIntersectionCurves()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def TrimmedCurves(self) -> ITrimmedCurves:  # iauxiliarygeomcontainer_trimmedcurves.html
        """
        ## TrimmedCurves - Интерфейс коллекции операций усечения кривой

        Тип данных: указатель на интерфейс ITrimmedCurves

        1. Свойство позволяет получить коллекцию всех операций усечения кривой, входящих в состав данного объекта.

        2. Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        TrimmedCurves = Object.TrimmedCurves       # Получить свойство (* )
        TrimmedCurves = Object.GetTrimmedCurves()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def UnhistoredCurves3D(self) -> IUnhistoredCurves3D:  # iauxiliarygeomcontainer_unhistoredcurves3d.html
        """
        ## UnhistoredCurves3D - Интерфейс коллекции кривых без истории

        Тип данных: указатель на интерфейс IUnhistoredCurves3D

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        UnhistoredCurves3D = Object.UnhistoredCurves3D       # Получить свойство (* )
        UnhistoredCurves3D = Object.GetUnhistoredCurves3D()  # Получить свойство (**)
        ```
        """
        ...




class IAxis3D1(IDispatch):  # iaxis3d1.html
    """
    ## Интерфейс IAxis3D1

    Интерфейс вспомогательной оси 3D.

    Интерфейс является дополнительным к интерфейсу IAxis3D.

    Интерфейс можно получить с помощью метода IUnknown::QueryInterface.

    Версия: Компас v20
    """

    @property
    def ShowName(self) -> bool:  # iaxis3d1_showname.html
        """
        ## ShowName - Показывать надпись

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        ShowName = Object.ShowName       # Получить свойство (* )
        Object.ShowName = ShowName       # Установить свойство (* )
        ShowName = Object.GetShowName()  # Получить свойство (**)
        Object.SetShowName( ShowName )   # Установить свойство (**)
        ```
        """
        ...
    @ShowName.setter
    def ShowName(self, value: bool): ...




class IPlane3D1(IDispatch):  # iplane3d1.html
    """
    ## Интерфейс IPlane3D1

    Интерфейс плоскости 3D.

    Интерфейс является дополнительным к интерфейсу плоскости IPlane3D.

    Интерфейс можно получить с помощью метода IUnknown::QueryInterface.

    Версия: Компас v20
    """

    @property
    def ParamUMax(self) -> float:  # iplane3d1_paramumax.html
        """
        ## ParamUMax - Значение параметра U конечное

        Тип данных: double

        Свойство доступно только для чтения.

        Версия Компас v21

        ### Синтаксис Automation

        ```
        ParamUMax = Object.ParamUMax       # Получить свойство (* )
        ParamUMax = Object.GetParamUMax()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def ParamUMin(self) -> float:  # iplane3d1_paramumin.html
        """
        ## ParamUMin - Значение параметра U начальное

        Тип данных: double

        Свойство доступно только для чтения.

        Версия Компас v21

        ### Синтаксис Automation

        ```
        ParamUMin = Object.ParamUMin       # Получить свойство (* )
        ParamUMin = Object.GetParamUMin()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def ParamVMax(self) -> float:  # iplane3d1_paramvmax.html
        """
        ## ParamVMax - Значение параметра V конечное

        Тип данных: double

        Свойство доступно только для чтения.

        Версия Компас v21

        ### Синтаксис Automation

        ```
        ParamVMax = Object.ParamVMax       # Получить свойство (* )
        ParamVMax = Object.GetParamVMax()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def ParamVMin(self) -> float:  # iplane3d1_paramvmin.html
        """
        ## ParamVMin - Значение параметра V начальное

        Тип данных: double

        Свойство доступно только для чтения.

        Версия Компас v21

        ### Синтаксис Automation

        ```
        ParamVMin = Object.ParamVMin       # Получить свойство (* )
        ParamVMin = Object.GetParamVMin()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def ScaleGabarit(self) -> bool:  # iplane3d1_scalegabarit.html
        """
        ## ScaleGabarit - Подгонять габарит

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        ScaleGabarit = Object.ScaleGabarit       # Получить свойство (* )
        Object.ScaleGabarit = ScaleGabarit       # Установить свойство (* )
        ScaleGabarit = Object.GetScaleGabarit()  # Получить свойство (**)
        Object.SetScaleGabarit( ScaleGabarit )   # Установить свойство (**)
        ```
        """
        ...
    @ScaleGabarit.setter
    def ScaleGabarit(self, value: bool): ...

    @property
    def ShowName(self) -> bool:  # iplane3d1_showname.html
        """
        ## ShowName - Показывать надпись

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        ShowName = Object.ShowName       # Получить свойство (* )
        Object.ShowName = ShowName       # Установить свойство (* )
        ShowName = Object.GetShowName()  # Получить свойство (**)
        Object.SetShowName( ShowName )   # Установить свойство (**)
        ```
        """
        ...
    @ShowName.setter
    def ShowName(self, value: bool): ...

    def SetLimit(self, Umin, Vmin, Umax, Vmax) -> bool:  # iplane3d1_setlimit.html
        """
        ## SetLimit - Установить пределы поверхности

        ### Синтаксис Automation

        ```
        BOOL SetLimit( double Umin, double Vmin, double Umax, double Vmax );
        ```

        ### Входные параметры

        +-------------------------------------------------------------------+
        | Umin, Vmin, Umax, Vmax | - новые граничные параметры поверхности. |
        +-------------------------------------------------------------------+

        Версия Компас v21

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...




class IConjunctivePoints(IModelObjects):  # iconjunctivepoints.html
    """
    ## Интерфейс IConjunctivePoints

    Интерфейс коллекции присоединительных точек.

    Получить интерфейс коллекции можно, используя свойство контейнера объектов вспомогательной геометрии IAuxiliaryGeomContainer::ConjunctivePoints.
    """

    def Add(self) -> IConjunctivePoint:  # iconjunctivepoints_add.html
        """
        ## Add - Создать новый элемент и добавить его в коллекцию

        1. Метод позволяет создать новый интерфейс присоединительной точки.

        2. После получения нового интерфейса нужно задать параметры и вызвать метод IModelObject::Update.

        ### Синтаксис Automation

        ```
        LPDISPATCH Add();
        ```

        ### Возвращаемое значение

        - Указатель на интерфейс присоединительной точки IConjunctivePoint.
        """
        ...

    def ConjunctivePoint(self, Index) -> IConjunctivePoint:  # iconjunctivepoints_conjunctivepoint.html
        """
        ## ConjunctivePoint - Указатель на элемент, заданный по индексу

        Тип данных: указатель на интерфейс IConjunctivePoint

        1. Свойство доступно только для чтения.

        2. В качестве индекса может использоваться индекс в коллекции и reference объекта.

        ### Синтаксис Automation

        ```
        ConjunctivePoint = Object.ConjunctivePoint( Index )     # Получить свойство (* )
        ConjunctivePoint = Object.GetConjunctivePoint( Index )  # Получить свойство (**)
        ```

        ### Входные параметры

        +---------------------------+
        | Index | - индекс объекта. |
        +---------------------------+
        """
        ...




class IControlPoints(IModelObjects):  # icontrolpoints.html
    """
    ## Интерфейс IControlPoints

    Интерфейс коллекции контрольных точек.

    Получить интерфейс коллекции можно, используя свойство контейнера объектов вспомогательной геометрии IAuxiliaryGeomContainer::ControlPoints.
    """

    def Add(self) -> IControlPoint:  # icontrolpoints_add.html
        """
        ## Add - Создать новый элемент и добавить его в коллекцию

        1. Метод позволяет создать новый интерфейс локальной системы координат.

        2. После получения нового интерфейса нужно задать параметры и вызвать метод IModelObject::Update.

        ### Синтаксис Automation

        ```
        LPDISPATCH Add();
        ```

        ### Возвращаемое значение

        - Указатель на интерфейс контрольной точки IControlPoint.
        """
        ...

    def ControlPoint(self, Index) -> IControlPoint:  # icontrolpoints_controlpoint.html
        """
        ## ControlPoint - Указатель на элемент, заданный по индексу

        Тип данных: указатель на интерфейс IControlPoint

        1. Свойство доступно только для чтения.

        2. В качестве индекса может использоваться индекс в коллекции и reference объекта.

        ### Синтаксис Automation

        ```
        ControlPoint = Object.ControlPoint( Index )     # Получить свойство (* )
        ControlPoint = Object.GetControlPoint( Index )  # Получить свойство (**)
        ```

        ### Входные параметры

        +---------------------------------+
        | Index | VT_I4 - индекс объекта. |
        +---------------------------------+
        """
        ...




class ILocalCoordinateSystems(IModelObjects):  # ilocalcoordinatesystems.html
    """
    ## Интерфейс ILocalCoordinateSystems

    Интерфейс коллекции локальных систем координат.

    Получить интерфейс коллекции можно, используя свойство контейнера объектов вспомогательной геометрии IAuxiliaryGeomContainer::LocalCoordinateSystems.
    """

    @property
    def Current(self) -> IModelObject:  # ilocalcoordinatesystems_current.html
        """
        ## Current - Указатель на текущую СК

        Тип данных: указатель на интерфейс IModelObject

        Свойство доступно только для чтения.

        Если на момент получения свойства текущей является ЛСК, то можно привести интерфейс IModelObject, возвращаемый свойством к ILocalCoordinateSystem, с помощью IUnknown::QueryInterface (const GUID far& iid, void** pif). Если же текущей является мировая СК, то привести возвращаемый интерфейс к ILocalCoordinateSystem не удастся.

        ### Синтаксис Automation

        ```
        LocalCoordinateSystem = iObject.Current()      # Получить свойство (* )
        LocalCoordinateSystems = iObject.GetCurrent()  # Получить свойство (**)
        ```
        """
        ...

    def Add(self) -> ILocalCoordinateSystem:  # ilocalcoordinatesystems_add.html
        """
        ## Add - Создать новый элемент и добавить его в коллекцию

        1. Метод позволяет создать новый интерфейс локальной системы координат.

        2. После получения нового интерфейса нужно задать параметры и вызвать метод IModelObject::Update.

        ### Синтаксис Automation

        ```
        LPDISPATCH Add();
        ```

        ### Возвращаемое значение

        - Указатель на интерфейс ЛСК ILocalCoordinateSystem.
        """
        ...

    def LocalCoordinateSystem(self, Index) -> ILocalCoordinateSystem:  # ilocalcoordinatesystems_localcoordinatesystem.html
        """
        ## LocalCoordinateSystem - Указатель на элемент, заданный по индексу

        Тип данных: указатель на интерфейс ILocalCoordinateSystem

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        LocalCoordinateSystem = iObject.LocalCoordinateSystem( index )     # Получить свойство (* )
        LocalCoordinateSystem = iObject.GetLocalCoordinateSystem( index )  # Получить свойство (**)
        ```

        ### Входные параметры

        +-----------------------------------+
        | Index | - VT_I4 - индекс объекта. |
        +-----------------------------------+
        """
        ...

    def SetCurrent(self, Object) -> bool:  # ilocalcoordinatesystems_setcurrent.html
        """
        ## SetCurrent - Установить ЛСК в качестве текущей СК

        1. Метод позволяет установить ЛСК, передаваемую входным параметром, в качестве текущей СК.

        2. Если входным параметром передать NULL, текущей станет мировая СК. См. также ILocalCoordinateSystem::Current.

        ### Синтаксис Automation

        ```
        BOOL SetCurrent( ILocalCoordinateSystem * object );
        ```

        ### Входные параметры

        - Указатель на интерфейс ЛСК ILocalCoordinateSystem.

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...




class ILocalCSAxesDirectionParam(IKompasAPIObject):  # ilocalcsaxesdirectionparam.html
    """
    ## Интерфейс ILocalCSAxesDirectionParam

    Интерфейс параметров ЛСК для типа ориентации "Направление осей".

    Указатель на интерфейс можно получить, используя свойство ЛСК ILocalCoordinateSystem::LocalCSParameters.
    """

    @property
    def LeadAxis(self) -> int:  # ilocalcsaxesdirectionparam_leadaxis.html
        """
        ## LeadAxis - Ведущая ось

        Тип данных: из перечисления ksObj3dTypeEnum

        ### Синтаксис Automation

        ```
        LeadAxis = Object.LeadAxis       # Получить свойство (* )
        Object.LeadAxis = LeadAxis       # Установить свойство (* )
        LeadAxis = Object.GetLeadAxis()  # Получить свойство (**)
        Object.SetLeadAxis( LeadAxis )   # Установить свойство (* )
        ```
        """
        ...
    @LeadAxis.setter
    def LeadAxis(self, value: int): ...

    def AngleByOwnAxis(self, Axis, arg1):  # ilocalcsaxesdirectionparam_anglebyownaxis.html
        """
        ## AngleByOwnAxis - Угол поворота вокруг собственной оси

        Свойство доступно только для записи.

        ### Синтаксис Automation

        ```
        iObject.AngleByOwnAxis = AngleByOwnAxis( axis );  # Установить свойство (* )
        iObject.SetAngleByOwnAxis( axis, angle );         # Установить свойство (**)
        ```

        ### Входные параметры

        +-----------------------------------------------------------------------------------------------------------------+
        | axis  | - тип оси из перечисления ksObj3dTypeEnum, допустимые значения: - o3d_axisOX - o3d_axisOY - o3d_axisOZ, |
        +-----------------------------------------------------------------------------------------------------------------+
        | angle | - угол поворота.                                                                                        |
        +-----------------------------------------------------------------------------------------------------------------+
        """
        ...

    def DirectingObject(self, Axis) -> IModelObject:  # ilocalcsaxesdirectionparam_directingobject.html
        """
        ## DirectingObject - Направляющий объект для оси

        Тип данных: указатель на интерфейс модельного объекта IModelObject

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        DirectingObject = iObject.DirectingObject( axis );     # Получить свойство (* )
        DirectingObject = iObject.GetDirectingObject( axis );  # Получить свойство (**)
        ```

        ### Входные параметры

        +----------------------------------------------------------------------------------------------------------------+
        | axis | - тип оси из перечисления ksObj3dTypeEnum, допустимые значения: - o3d_axisOX - o3d_axisOY - o3d_axisOZ. |
        +----------------------------------------------------------------------------------------------------------------+
        """
        ...

    def RotateAxis(self, Axis) -> bool:  # ilocalcsaxesdirectionparam_rotateaxis.html
        """
        ## RotateAxis - Сменить направление осина противоположное

        1. Метод позволяет менять направления осей на противоположные.

        2. Для осей можно выбирать направляющие объекты (см. метод ILocalCSAxesDirectionParam::SetDirectingObject).

        3. Можно выбрать направление для двух осей, третья ось ориентируется, исходя из положения двух выбранных. Менять направление на противоположное можно только у выбранных осей.

        4. У оси с автоориентацией смена направления на противоположное недоступна.

        ### Синтаксис Automation

        ```
        BOOL RotateAxis( ksObj3dTypeEnum axis );
        HRESULT RotateAxis( ksObj3dTypeEnum axis,
        BOOL * Result);
        ```

        ### Входные параметры

        +----------------------------------------------------------------------------------------------------------------+
        | axis | - тип оси из перечисления ksObj3dTypeEnum, допустимые значения: - o3d_axisOX - o3d_axisOY - o3d_axisOZ. |
        +----------------------------------------------------------------------------------------------------------------+

        ### Возвращаемое значение

        +-----------------------------------------+
        | TRUE  | - в случае удачного завершения. |
        +-----------------------------------------+
        | FALSE | - в случае неудачи.             |
        +-----------------------------------------+
        """
        ...

    def SetDirectingObject(self, Axis, Object) -> bool:  # ilocalcsaxesdirectionparam_setdirectingobject.html
        """
        ## SetDirectingObject - Установить направляющий объект для оси

        1. Объекты, которые могут определять направление оси: прямолинейные объекты, плоские объекты, цилиндрические и конические поверхности. Также могут быть выбраны произвольные кривые или ребра при условии, что позиция начала ЛСК лежит на этой кривой (ребре).

        2. Можно выбрать направление для двух осей. Третья ось ориентируется, исходя из положения двух выбранных. Менять направление на противоположное можно только у выбранных осей. У оси с автоориентацией смена направления на противоположное недоступна.

        ### Синтаксис Automation

        ```
        BOOL SetDirectingObject( ksObj3dTypeEnum axis,
        LPDISPATCH obj );
        HRESULT SetDirectingObject( ksObj3dTypeEnum axis,
        IModelObject * obj,
        VARIANT_BOOL * Result );
        ```

        ### Входные параметры

        +----------------------------------------------------------------------------------------------------------------+
        | obj  | - направляющий объект,                                                                                  |
        +----------------------------------------------------------------------------------------------------------------+
        | axis | - тип оси из перечисления ksObj3dTypeEnum, допустимые значения: - o3d_axisOX - o3d_axisOY - o3d_axisOZ. |
        +----------------------------------------------------------------------------------------------------------------+

        ### Возвращаемое значение

        +-----------------------------------------+
        | TRUE  | - в случае удачного завершения. |
        +-----------------------------------------+
        | FALSE | - в случае неудачи.             |
        +-----------------------------------------+
        """
        ...




class ILocalCSEulerParam(IKompasAPIObject):  # ilocalcseulerparam.html
    """
    ## Интерфейс ILocalCSEulerParam

    Интерфейс параметров ЛСК для типа ориентации "Система углов Эйлера".

    Указатель на интерфейс можно получить, используя свойство ЛСК ILocalCoordinateSystem::LocalCSParameters.
    """

    @property
    def NutationAngle(self) -> float:  # ilocalcseulerparam_nutationangle.html
        """
        ## NutationAngle - Угол нутации

        Тип данных: double

        Свойство позволяет устанавливать и получать угол нутации.

        ### Синтаксис Automation

        ```
        NutationAngle = iObject.NutationAngle;       # Получить свойство (* )
        iObject.NutationAngle = NutationAngle;       # Установить свойство (* )
        NutationAngle = iObject.GetNutationAngle();  # Получить свойство (**)
        iObject.SetNutationAngle( NutationAngle );   # Установить свойство (* )
        ```
        """
        ...
    @NutationAngle.setter
    def NutationAngle(self, value: float): ...

    @property
    def PrecessionAngle(self) -> float:  # ilocalcseulerparam_precessionangle.html
        """
        ## PrecessionAngle - Угол прецессии

        Тип данных: double

        Свойство позволяет устанавливать и получать угол прецессии.

        ### Синтаксис Automation

        ```
        PrecessionAngle = iObject.PrecessionAngle;       # Получить свойство (* )
        iObject.PrecessionAngle = PrecessionAngle;       # Установить свойство (* )
        PrecessionAngle = iObject.GetPrecessionAngle();  # Получить свойство (**)
        iObject.SetPrecessionAngle( PrecessionAngle );   # Установить свойство (* )
        ```
        """
        ...
    @PrecessionAngle.setter
    def PrecessionAngle(self, value: float): ...

    @property
    def RotationAngle(self) -> float:  # ilocalcseulerparam_rotationangle.html
        """
        ## RotationAngle - Угол вращения

        Тип данных: double

        Свойство позволяет устанавливать и получать угол вращения.

        Cм.Рисунок...

        ### Синтаксис Automation

        ```
        RotationAngle = iObject.RotationAngle;       # Получить свойство (* )
        iObject.RotationAngle = RotationAngle;       # Установить свойство (* )
        RotationAngle = iObject.GetRotationAngle();  # Получить свойство (**)
        iObject.SetRotationAngle( RotationAngle );   # Установить свойство (* )
        ```
        """
        ...
    @RotationAngle.setter
    def RotationAngle(self, value: float): ...




class ILocalCSOrientByObjectParam(IKompasAPIObject):  # ilocalcsorientbyobjectparam.html
    """
    ## Интерфейс ILocalCSOrientByObjectParam

    Интерфейс параметров ЛСК для типа ориентации "По объекту".

    Указатель на интерфейс можно получить, используя свойство ЛСК ILocalCoordinateSystem::LocalCSParameters.
    """

    @property
    def OrientationObject(self):
        ...

    def SetOrientationObject(self, Object):
        ...




class ILocalCSObject(IDispatch):  # ilocalcsobject.html
    """
    ## Интерфейс ILocalCSObject

    Интерфейс подчиненного объекта ЛСК.

    Интерфейс является дополнительным для следующих объектов:

    •IPoint3D - Точка 3D,

    •IControlPoint - Контрольная точка,

    •IConjunctivePoint - Присоединительная точка,

    •ILocalCoordinateSystem - ЛСК,

    •ISketch- Эскиз,

    •ITablePattern - Массив по таблице,

    •IMeshPointsSurface - Поверхность по сети точек,

    •ICloudPointsSurface - Поверхность по пласту (облаку) точек,

    •IImportedSurface - Импортированная поверхность,

    •IArc3D - 3D дуга,

    •IPolyLine - Пространственная ломаная,

    •ISpline3D - Сплайн,

    •IHole3D

    Данный интерфейс можно получить от интерфейсов перечисленных объектов посредством вызова метода IUnknown::QueryInterface.

    Интерфейс позволяет получать и изменять параметры объектов в удобной для этого системе координат. По умолчанию используется СК компонента.
    """

    @property
    def CoordinateSystem(self) -> IModelObject:  # ilocalcsobject_coordinatesystem.html
        """
        ## CoordinateSystem - СК объекта

        Тип данных: Указатель на интерфейс IModelObject

        ### Синтаксис Automation

        ```
        CoordinateSystem = Object.CoordinateSystem;       # Получить свойство (* )
        Object.CoordinateSystem = CoordinateSystem;       # Установить свойство (* )
        CoordinateSystem = Object.GetCoordinateSystem();  # Получить свойство (**)
        Object.SetCoordinateSystem( CoordinateSystem )    # Установить свойство (* )
        ```
        """
        ...
    @CoordinateSystem.setter
    def CoordinateSystem(self, value: IModelObject): ...

    @property
    def LocalCoordinateSystem(self) -> int:  # ilocalcsobject_localcoordinatesystem.html
        """
        ## LocalCoordinateSystem - Тип параметров объекта

        Тип данных: из перечисления ksModelObjectParamTypeEnum

        Свойство позволяет получить локальную систему координат объекта.

        Если свойство возвращает NULL, объект создан в базовой СК объекта. Для переноса объекта в другую СК используется метод IPart7::TransferObjects.

        ### Синтаксис Automation

        ```
        LocalCoordinateSystem = Object.LocalCoordinateSystem       # Получить свойство (* )
        LocalCoordinateSystem = Object.GetLocalCoordinateSystem()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def ModelObjectParamType(self) -> int:  # ilocalcsobject_modelobjectparamtype.html
        """
        ## ModelObjectParamType - Тип параметров объекта

        Тип данных: из перечисления ksModelObjectParamTypeEnum

        Свойство позволяет установить тип параметров объекта.

        После установки данного свойства параметры объекта будут выдаваться пересчитанными в установленную систему координат. При установке параметров также будет производиться пересчет параметров. Для правильной работы библиотек требуется установить нужный тип параметров, получить или изменить параметры и вернуть тип параметров ksMOPartAllParam-Параметры в системе координат детали (см. ksModelObjectParamTypeEnum-Тип параметров объекта).

        ### Синтаксис Automation

        ```
        ModelObjectParamType = Object.ModelObjectParamType       # Получить свойство (* )
        Object.ModelObjectParamType = ModelObjectParamType       # Установить свойство (* )
        ModelObjectParamType = Object.GetModelObjectParamType()  # Получить свойство (**)
        Object.SetModelObjectParamType( ModelObjectParamType )   # Установить свойство (**)
        ```
        """
        ...
    @ModelObjectParamType.setter
    def ModelObjectParamType(self, value: int): ...




class IPlacement3D(IKompasAPIObject):  # iplacement3d.html
    """
    ## Интерфейс IPlacement3D

    Интерфейс локальной системы координат (положение объекта).
    """

    def GetMatrix3D(self) -> float:  # iplacement3d_getmatrix3d.html
        """
        ## GetMatrix3D - Получить матрицу системы координат в виде массива. SAFEARRAY double (VT_ARRAY | VT_R8)

        ### Синтаксис Automation

        ```
        VARIANT GetMatrix3D();
        ```

        ### Возвращаемое значение

        - массив координат SAFEARRAY double (VT_ARRAY | VT_R8).
        """
        ...

    def GetOrigin(self) -> bool:  # iplacement3d_getorigin.html
        """
        ## GetOrigin - Получить координаты начала локальной системы координат

        ### Синтаксис Automation

        ```
        BOOL GetOrigin( double * X, double * Y, double * Z );
        ```

        ### Выходные параметры

        +------------------------------+
        | X | - координата точки по X, |
        +------------------------------+
        | Y | - координата точки по Y, |
        +------------------------------+
        | Z | - координата точки по Z. |
        +------------------------------+

        ### Возвращаемое значение

        +-----------------------------------------+
        | TRUE  | - в случае удачного завершения, |
        +-----------------------------------------+
        | FALSE | - в случае неудачи.             |
        +-----------------------------------------+
        """
        ...

    def GetPoint3D(self, XIn, YIn) -> bool:  # iplacement3d_getpoint3d.html
        """
        ## GetPoint3D - Получить пространственную точку по точке на плоскости xy

        ### Синтаксис Automation

        ```
        BOOL GetPoint3D( double XIn, double YIn, double * XOut, double * YOut, double * ZOut );
        ```

        ### Входные параметры

        +------------------------------------------+
        | XIn | - координата по X на плоскости XY, |
        +------------------------------------------+
        | YIn | - координата по Y на плоскости XY. |
        +------------------------------------------+

        ### Выходные параметры

        +------------------------------------------+
        | YIn  | - координата по X в пространстве, |
        +------------------------------------------+
        | YOut | - координата по Y в пространстве, |
        +------------------------------------------+
        | ZOut | - координата по Z в пространстве. |
        +------------------------------------------+

        ### Возвращаемое значение

        +-----------------------------------------+
        | TRUE  | - в случае удачного завершения, |
        +-----------------------------------------+
        | FALSE | - в случае неудачи.             |
        +-----------------------------------------+
        """
        ...

    def GetPointProjectionToXY(self, XIn, YIn, ZIn) -> bool:  # iplacement3d_getpointprojectiontoxy.html
        """
        ## GetPointProjectionToXY - Проекция точки на плоскость xy

        ### Синтаксис Automation

        ```
        BOOL GetPointProjectionToXY( double XIn, double YIn, double ZIn, double * XOut, double * YOut );
        ```

        ### Входные параметры

        +------------------------------+
        | X | - координата точки по X, |
        +------------------------------+
        | Y | - координата точки по Y, |
        +------------------------------+
        | Z | - координата точки по Z. |
        +------------------------------+

        ### Выходные параметры

        +-------------------------------------------+
        | XOut | - координата по X на плоскости XY, |
        +-------------------------------------------+
        | YOut | - координата по Y на плоскости XY. |
        +-------------------------------------------+

        ### Возвращаемое значение

        +-----------------------------------------+
        | TRUE  | - в случае удачного завершения, |
        +-----------------------------------------+
        | FALSE | - в случае неудачи.             |
        +-----------------------------------------+
        """
        ...

    def GetVector(self, Axis) -> bool:  # iplacement3d_getvector.html
        """
        ## GetVector - Получить вектор для указанной оси

        ### Синтаксис Automation

        ```
        BOOL GetVector( ksObj3dTypeEnum Axis, double * X, double * Y, double * Z );
        ```

        ### Входные параметры

        +---------------+
        | Axis | - ось. |
        +---------------+

        ### Выходные параметры

        +------------------------------+
        | X | - координата точки по X, |
        +------------------------------+
        | Y | - координата точки по Y, |
        +------------------------------+
        | Z | - координата точки по Z. |
        +------------------------------+

        ### Возвращаемое значение

        +-----------------------------------------+
        | TRUE  | - в случае удачного завершения, |
        +-----------------------------------------+
        | FALSE | - в случае неудачи.             |
        +-----------------------------------------+
        """
        ...

    def InitByMatrix3D(self, mtr) -> bool:  # iplacement3d_initbymatrix3d.html
        """
        ## InitByMatrix3D - Установить систему координат по матрице. SAFEARRAY double (VT_ARRAY | VT_R8)

        ### Синтаксис Automation

        ```
        BOOL InitByMatrix3D( VARIANT mtr );
        ```

        ### Входные параметры

        +---------------------------------------------------------------+
        | mtr | - массив координат SAFEARRAY double (VT_ARRAY | VT_R8). |
        +---------------------------------------------------------------+

        ### Возвращаемое значение

        +-----------------------------------------+
        | TRUE  | - в случае удачного завершения, |
        +-----------------------------------------+
        | FALSE | - в случае неудачи.             |
        +-----------------------------------------+
        """
        ...

    def Rotate(self, X0, Y0, Z0, AxisZX, AxisZXY, AxisZZ, Angle) -> bool:  # iplacement3d_rotate.html
        """
        ## Rotate - Повернуть систему координат на угол вокруг оси

        ### Синтаксис Automation

        ```
        BOOL Rotate( double X0, double Y0, double Z0, double AxisZX, double AxisZXY, double AxisZZ, double Angle );
        ```

        ### Входные параметры

        +---------------------------------------------+
        | X0      | - первая координата точки,        |
        +---------------------------------------------+
        | Y0      | - вторая координата точки,        |
        +---------------------------------------------+
        | Z0      | - третья координата точки,        |
        +---------------------------------------------+
        | AxisZX  | - вектор "X" стандартного базиса, |
        +---------------------------------------------+
        | AxisZXY | - вектор "Y" стандартного базиса, |
        +---------------------------------------------+
        | AxisZZ  | - вектор "Z" стандартного базиса, |
        +---------------------------------------------+
        | Angle   | - угол поворота.                  |
        +---------------------------------------------+

        ### Возвращаемое значение

        +----------------------------------------+
        | TRUE | - в случае удачного завершения. |
        +----------------------------------------+
        """
        ...

    def SetOrigin(self, X, Y, Z) -> bool:  # iplacement3d_setorigin.html
        """
        ## SetOrigin - Получить координаты начала локальной системы координат

        ### Синтаксис Automation

        ```
        BOOL SetOrigin( double X, double Y, double Z );
        ```

        ### Входные параметры

        +------------------------------+
        | X | - координата точки по X, |
        +------------------------------+
        | Y | - координата точки по Y, |
        +------------------------------+
        | Z | - координата точки по Z. |
        +------------------------------+

        ### Возвращаемое значение

        +-----------------------------------------+
        | TRUE  | - в случае удачного завершения, |
        +-----------------------------------------+
        | FALSE | - в случае неудачи.             |
        +-----------------------------------------+
        """
        ...

    def SetVector(self, Axis, X, Y, Z) -> bool:  # iplacement3d_setvector.html
        """
        ## SetVector - Задать вектор для указанной оси

        ### Синтаксис Automation

        ```
        BOOL SetVector( ksObj3dTypeEnum Axis, double X, double Y, double Z );
        ```

        ### Входные параметры

        +---------------------------------+
        | Axis | - ось,                   |
        +---------------------------------+
        | X    | - координата точки по X, |
        +---------------------------------+
        | Y    | - координата точки по Y, |
        +---------------------------------+
        | Z    | - координата точки по Z. |
        +---------------------------------+

        ### Возвращаемое значение

        +-----------------------------------------+
        | TRUE  | - в случае удачного завершения, |
        +-----------------------------------------+
        | FALSE | - в случае неудачи.             |
        +-----------------------------------------+
        """
        ...




class IModelObjects(IKompasCollection):  # imodelobjects.html
    """
    ## Интерфейс IModelObjects

    Базовый интерфейс для всех коллекций модельных объектов.

    1. Данный интерфейс является базовым для коллекций модельных объектов.

    2. Интерфейс может быть получен с помощью метода IUnknown QueryInterface от коллекций модельных объектов, например, IParts7.
    """

    def Item(self, Index) -> IModelObject:  # imodelobjects_item.html
        """
        ## Item - Объект, заданный по индексу или по имени

        Тип данных: указатель на интерфейс IModelObject

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        Item = iObject.Item ( Index)     # Получить свойство (* )
        Item = iObject.GetItem ( Index)  # Получить свойство (**)
        ```

        ### Входные параметры

        +---------------------------------------------------------------------+
        | Index ( Variant) | - VT_BSTR - имя объекта, VT_I4 - индекс объекта. |
        +---------------------------------------------------------------------+
        """
        ...




class IArcs3D(IModelObjects):  # iarcs3d.html
    """
    ## Интерфейс IArcs3D

    Коллекция дуг 3D.
    """

    def Add(self) -> IArc3D:  # iarcs3d_add.html
        """
        ## Add - Создает новый элемент и добавляет его в коллекцию

        ### Синтаксис Automation

        ```
        LPDISPATCH Add();
        ```

        ### Возвращаемое значение

        Указатель на интерфейс дуги 3D IArc3D
        """
        ...

    def Arc3D(self, Index) -> IArc3D:  # iarcs3d_arc3d.html
        """
        ## Arc3D - Возвращает элемент, заданный по индексу

        Тип данных: указатель на интерфейс IArc3D

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        Arc3D = Object.Arc3D( Index )     # Получить свойство (* )
        Arc3D = Object.GetArc3D( Index )  # Получить свойство (**)
        ```
        """
        ...




class IAxes3D(IModelObjects):  # iaxes3d.html
    """
    ## Интерфейс IAxes3D

    Коллекция вспомогательных осей 3D.
    """

    def Add(self, Type):  # iaxes3d_add.html
        """
        ## Add - Создает новый элемент и добавляет его в коллекцию

        ### Синтаксис Automation

        ```
        LPDISPATCH Add( ksObj3dTypeEnum Type );
        ```

        ### Входные параметры

        +-------------------+
        | Type | - тип оси. |
        +-------------------+
        """
        ...

    def Axis3D(self, Index) -> IAxis3D:  # iaxes3d_axis3d.html
        """
        ## Axis3D - Возвращает элемент, заданный по ссылке или индексу

        Тип данных: Указатель на интерфейс IAxis3D

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        Axis3D = Object.Axis3D( Index )     # Получить свойство(* )
        Axis3D = Object.GetAxis3D( Index )  # Получить свойство (**)
        ```
        """
        ...




class IBilletsObsoletes(IModelObjects):  # ibilletsobsoletes.html
    """
    ## Интерфейс IBilletsObsoletes

    Коллекция деталей заготовок и зеркальных деталей.
    """

    def Add(self, FileName, Mirror) -> IBilletObsolete:  # ibilletsobsoletes_add.html
        """
        ## Add - Создает новый элемент и добавляет его в коллекцию

        ### Синтаксис Automation

        ```
        LPDISPATCH Add( BSTR FileName, BOOL Mirror );
        ```

        ### Входные параметры

        +--------------------------------------------+
        | FileName | - имя файла детали,             |
        +--------------------------------------------+
        | Mirror   | - признак зеркальной симметрии. |
        +--------------------------------------------+

        ### Возвращаемое значение

        Указатель на интерфейс IBilletObsolete
        """
        ...

    def BilletObsolete(self, Index) -> IBilletObsolete:  # ibilletsobsoletes_billetobsolete.html
        """
        ## BilletObsolete - Возвращает элемент, заданный по ссылке или индексу

        Тип данных: указатель указатель на интерфейс IBilletObsolete

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        BilletObsolete = Object.BilletObsolete( Index )     # Получить свойство (* )
        BilletObsolete = Object.GetBilletObsolete( Index )  # Получить свойство (**)
        ```
        """
        ...




class IBooleans(IModelObjects):  # ibooleans.html
    """
    ## Интерфейс IBooleans

    Интерфейс коллекции булевых операций.

    Данный интерфейс можно получить, используя свойство контейнера трехмерных объектов IModelContainer::Booleans.

    КОМПАС версия v18
    """

    def Add(self) -> IBoolean:  # ibooleans_add.html
        """
        ## Add - Создает новый элемент и добавляет его в коллекцию

        Метод позволяет создать новый интерфейс булевой операции.

        После получения нового интерфейса нужно задать параметры операции и вызвать метод IModelObject::Update.

        ### Синтаксис Automation

        ```
        LPDISPATCH Add();
        ```

        ### Возвращаемое значение

        - указатель на интерфейс IBoolean.
        """
        ...

    def Boolean(self, Index) -> IBoolean:  # ibooleans_boolean.html
        """
        ## Boolean - Возвращает элемент, заданный по индексу или по имени

        Тип данных: Указатель на интерфейс IBoolean

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        Boolean = Object.Boolean( Index )     # Получить свойство(* )
        Boolean = Object.GetBoolean( Index )  # Получить свойство (**)
        ```

        ### Входные параметры

        +--------------------------------------------+
        | VARIANT Index | - индекс или имя элемента. |
        +--------------------------------------------+
        """
        ...




class IChamfers(IModelObjects):  # ichamfers.html
    """
    ## Интерфейс IChamfers

    Интерфейс коллекции операций Фаска.
    """

    def Add(self) -> IChamfer:  # ichamfers_add.html
        """
        ## Add - Создает новый элемент и добавляет его в коллекцию

        1. Метод позволяет создать новый интерфейс операции фаска.

        2. После получения нового интерфейса нужно задать параметры операции и вызвать метод IModelObject::Update.

        ### Синтаксис Automation

        ```
        LPDISPATCH Add();
        ```

        ### Возвращаемое значение

        - указатель на интерфейс IChamfer.
        """
        ...

    def Chamfer(self, Index) -> IChamfer:  # ichamfers_chamfer.html
        """
        ## Chamfer - Возвращает элемент, заданный по индексу или по имени

        Тип данных: Указатель на интерфейс IChamfer

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        Chamfer = Object.Chamfer( Index )     # Получить свойство (* )
        Chamfer = Object.GetChamfer( Index )  # Получить свойство (**)
        ```

        ### Входные параметры

        +--------------------------------------------+
        | VARIANT Index | - индекс или имя элемента. |
        +--------------------------------------------+
        """
        ...




class IFullFillets(IModelObjects):  # ifullfillets.html
    """
    ## Интерфейс IFullFillets

    Интерфейс коллекции операций полного скругления.
    """

    def Add(self) -> IFullFillet:  # ifullfillets_add.html
        """
        ## Add - Создает новый элемент и добавляет его в коллекцию

        1. Метод позволяет создать новый интерфейс операции полного скругления.

        2. После получения нового интерфейса нужно задать параметры операции и вызвать метод IModelObject::Update.

        ### Синтаксис Automation

        ```
        LPDISPATCH Add();
        ```

        ### Возвращаемое значение

        - Указатель на интерфейс IFullFillet.
        """
        ...

    def FullFillet(self, Index) -> IFullFillet:  # ifullfillets_fullfillet.html
        """
        ## FullFillet - Возвращает элемент, заданный по индексу или по имени

        Тип данных: Указатель на интерфейс IFullFillet

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        FullFillet = Object.FullFillet( Index )     # Получить свойство (* )
        FullFillet = Object.GetFullFillet( Index )  # Получить свойство (**)
        ```

        ### Входные параметры

        +--------------------------------------------+
        | VARIANT Index | - индекс или имя элемента. |
        +--------------------------------------------+
        """
        ...




class ICollectionsGeometry(IModelObjects):  # icollectionsgeometry.html
    """
    ## Интерфейс ICollectionsGeometry

    Коллекции геометрии.
    """

    def Add(self) -> ICollectionGeometry:  # icollectionsgeometry_add.html
        """
        ## Add - Создает новый элемент и добавляет его в коллекцию

        ### Синтаксис Automation

        ```
        LPDISPATCH Add();
        ```

        ### Возвращаемое значение

        Указатель на интерфейс ICollectionGeometry
        """
        ...

    def CollectionGeometry(self, Index) -> ICollectionGeometry:  # icollectionsgeometry_collectiongeometry.html
        """
        ## CollectionGeometry - Возвращает элемент, заданный по индексу или по имени

        Тип данных: указатель указатель на интерфейс ICollectionGeometry

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        CollectionGeometry = Object.CollectionGeometry( Index )     # Получить свойство (* )
        CollectionGeometry = Object.GetCollectionGeometry( Index )  # Получить свойство (**)
        ```
        """
        ...




class IConnectCurves(IModelObjects):  # iconnectcurves.html
    """
    ## Интерфейс IConnectCurves

    Коллекция операций соединения кривых
    """

    def Add(self) -> IConnectCurve:  # iconnectcurves_add.html
        """
        ## Add - Создает новый элемент и добавляет его в коллекцию

        ### Синтаксис Automation

        ```
        LPDISPATCH Add();
        ```

        ### Возвращаемое значение

        Указатель на интерфейс операции соединения кривых IConnectCurve
        """
        ...

    def ConnectCurve(self, Index) -> IConnectCurve:  # iconnectcurves_connectcurve.html
        """
        ## ConnectCurve - Возвращает элемент, заданный по индексу

        Тип данных: указатель указатель на интерфейс IConnectCurve

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        ConnectCurve = Object.ConnectCurve( Index )     # Получить свойство (* )
        ConnectCurve = Object.GetConnectCurve( Index )  # Получить свойство (**)
        ```
        """
        ...




class IContours3D(IModelObjects):  # icontours3d.html
    """
    ## Интерфейс IContours3D

    Коллекция контуров 3D.
    """

    def Add(self) -> IContour3D:  # icontours3d_add.html
        """
        ## Add - Создает новый элемент и добавляет его в коллекцию

        ### Синтаксис Automation

        ```
        LPDISPATCH Add();
        ```

        ### Возвращаемое значение

        Указатель на интерфейс контура IContour3D
        """
        ...

    def Contour3D(self, Index) -> IContour3D:  # icontours3d_contour3d.html
        """
        ## Contour3D - Возвращает элемент, заданный по ссылке или индексу

        Тип данных: указатель указатель на интерфейс IContour3D

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        Contour3D = Object.Contour3D( Index )     # Получить свойство (* )
        Contour3D = Object.GetContour3D( Index )  # Получить свойство (**)
        ```
        """
        ...




class ICopiesGeometry(IModelObjects):  # icopiesgeometry.html
    """
    ## Интерфейс ICopiesGeometry

    Коллекция копий геометрии
    """

    def Add(self) -> ICopyGeometry:  # icopiesgeometry_add.html
        """
        ## Add - Создает новый элемент и добавляет его в коллекцию

        ### Синтаксис Automation

        ```
        LPDISPATCH Add();
        ```

        ### Возвращаемое значение

        Указатель на интерфейс ICopyGeometry.
        """
        ...

    def CopyGeometry(self, Index) -> ICopyGeometry:  # icopiesgeometry_copygeometry.html
        """
        ## CopyGeometry - Возвращает элемент, заданный по индексу или по имени

        Тип данных: указатель на интерфейс ICopyGeometry

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        CopyGeometry = Object.CopyGeometry( Index )   # Получить свойство (* )
        CopyGeometry = Object.GetCopyGeometry( Index  # Получить свойство (**)
        ```
        """
        ...




class IConicCurves3D(IModelObjects):  # iconiccurves3d.html
    """
    ## Интерфейс IConicCurves3D

    Интерфейс коллекции конических кривых 3D.

    Интерфейс можно получить, используя свойство IAuxiliaryGeomContainer::ConicCurves3D.

    Версия КОМПАС v22
    """

    def Add(self) -> IConicCurve3D:  # iconiccurves3d_add.html
        """
        ## Add - Создает новый элемент и добавляет его в коллекцию

        ### Синтаксис Automation

        ```
        LPDISPATCH Add();
        ```

        ### Возвращаемое значение

        Указатель на интерфейс IConicCurve3D
        """
        ...

    def ConicCurve3D(self, Index) -> IConicCurve3D:  # iconiccurves_3dconiccurve3d.html
        """
        ## ConicCurve3D - Возвращает элемент, заданный по индексу

        Тип данных: указатель указатель на интерфейс IConicCurve3D

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        ConicCurve3D = Object.ConicCurve3D( Index )     # Получить свойство (* )
        ConicCurve3D = Object.GetConicCurve3D( Index )  # Получить свойство (**)
        ```
        """
        ...




class ICurveByLaws(IModelObjects):  # icurvebylaws.html
    """
    ## Интерфейс ICurveByLaws

    Коллекция кривых по закону.

    Интерфейс можно получить у контейнера объектов вспомогательной геометрии с помощью свойства IAuxiliaryGeomContainer::CurveByLaws.
    """

    def Add(self) -> ICurveByLaw:  # icurvebylaws_add.html
        """
        ## Add - Создает новый элемент и добавляет его в коллекцию

        ### Синтаксис Automation

        ```
        LPDISPATCH Add();
        ```

        ### Возвращаемое значение

        Указатель на интерфейс кривой по закону ICurveByLaw
        """
        ...

    def CurveByLaw(self, Index) -> ICurveByLaw:  # icurvebylaws_curvebylaw.html
        """
        ## CurveByLaw - Возвращает элемент, заданный по ссылке или индексу

        Тип данных: Указатель на интерфейс ICurveByLaw

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        CurveByLaw = Object.CurveByLaw( Index )     # Получить свойство (* )
        CurveByLaw = Object.GetCurveByLaw( Index )  # Получить свойство (**)
        ```
        """
        ...




class ICurvesBy2Projectionses(IModelObjects):  # icurvesby2projectionses.html
    """
    ## Интерфейс ICurvesBy2Projectionses

    Коллекция кривых по двум проекциям

    Интерфейс можно получить с помощью свойства IAuxiliaryGeomContainer::CurvesBy2Projectionses.
    """

    def Add(self) -> ICurveBy2Projections:  # icurvesby2projectionses_add.html
        """
        ## Add - Создает новый элемент и добавляет его в коллекцию.

        ### Синтаксис Automation

        ```
        LPDISPATCH Add();
        ```

        ### Возвращаемое значение

        Указатель на интерфейс ICurveBy2Projections
        """
        ...

    def CurveBy2Projections(self, Index) -> ICurveBy2Projections:  # icurvesby2projectionses_curveby2projections.html
        """
        ## CurveBy2Projections - Возвращает элемент, заданный по ссылке или индексу

        Тип данных: Указатель на интерфейс ICurveBy2Projections

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        CurveBy2Projections = Object.CurveBy2Projections( Index )     # Получить свойство (* )
        CurveBy2Projections = Object.GetCurveBy2Projections( Index )  # Получить свойство (**)
        ```
        """
        ...




class ICurveOutLines(IModelObjects):  # icurveoutlines.html
    """
    ## Интерфейс ICurveOutLines

    Коллекция линий очерка.

    Интерфейс можно получить у контейнера объектов вспомогательной геометрии с помощью свойства IAuxiliaryGeomContainer::CurveOutLines.
    """

    def Add(self) -> ICurveOutLine:  # icurveoutlines_add.html
        """
        ## Add - Создает новый элемент и добавляет его в коллекцию

        ### Синтаксис Automation

        ```
        LPDISPATCH Add();
        ```

        ### Возвращаемое значение

        Указатель на интерфейс линии очерка ICurveOutLine
        """
        ...

    def CurveOutLine(self, Index) -> ICurveOutLine:  # icurveoutlines_curveoutline.html
        """
        ## CurveOutLine - Возвращает элемент, заданный по ссылке или индексу

        Тип данных: Указатель на интерфейс ICurveOutLine

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        CurveOutLine = Object.CurveOutLine( Index )     # Получить свойство (* )
        CurveOutLine = Object.GetCurveOutLine( Index )  # Получить свойство (**)
        ```
        """
        ...




class ICuts(IModelObjects):  # icuts.html
    """
    ## Интерфейс ICuts

    Интерфейс коллекции операций Сечение.
    """

    def Add(self) -> ICut:  # icuts_add.html
        """
        ## Add - Создает новый элемент и добавляет его в коллекцию

        Метод позволяет создать новый интерфейс операции сечения.

        После получения нового интерфейса нужно задать параметры операции и вызвать метод IModelObject::Update.

        ### Синтаксис Automation

        ```
        LPDISPATCH Add();
        ```

        ### Возвращаемое значение

        - указатель на интерфейс ICut.
        """
        ...

    def Cut(self, Index) -> ICut:  # icuts_cut.html
        """
        ## Cut - Возвращает элемент, заданный по индексу или по имени

        Тип данных: Указатель на интерфейс ICut.

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        Cut = Object.Cut( Index )     # Получить свойство (* )
        Cut = Object.GetCut( Index )  # Получить свойство (**)
        ```

        ### Входные параметры

        +-------------------------------------------------------+
        | VARIANT Index | - индекс в коллекции или имя объекта. |
        +-------------------------------------------------------+
        """
        ...




class IDraftsFromEdges(IModelObjects):  # idraftsfromedges.html
    """
    ## Интерфейс IDraftsFromEdges

    Интерфейс коллекции операций "Уклон от базовой линии"

    Интерфейс можно получить используя свойство IModelContainer::DraftsFromEdges.

    Версия КОМПАС v22
    """

    def Add(self) -> IDraftFromEdges:  # idraftsfromedges_add.html
        """
        ## Add - Создает новый элемент и добавляет его в коллекцию

        ### Синтаксис Automation

        ```
        LPDISPATCH Add();
        ```

        ### Возвращаемое значение

        - Указатель на интерфейс операции Уклон от базовой линии IDraftFromEdges
        """
        ...

    def DraftFromEdges(self, Index) -> IDraftFromEdges:  # idraftsfromedges_draftfromedges.html
        """
        ## DraftFromEdges - Возвращает элемент, заданный по индексу или имени

        Тип данных: Указатель на интерфейс IDraftFromEdges

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        DraftFromEdges = Object.DraftFromEdges( Index )     # Получить свойство (* )
        DraftFromEdges = Object.GetDraftFromEdges( Index )  # Получить свойство (**)
        ```
        """
        ...




class IEquidistants3D(IModelObjects):  # iequidistants3d.html
    """
    ## Интерфейс IEquidistants3D

    Коллекция 3D-эквидистант.

    Позволяет создавать и получать эквидистанты в 3D документе.

    Данный интерфейс можно получить у контейнера вспомогательной геометрии IAuxiliaryGeomContainer::Equidistants3D.
    """

    def Add(self) -> IEquidistant3D:  # iequidistants3d_add.html
        """
        ## Add - Создать новый элемент и добавить в коллекцию

        ### Синтаксис Automation

        ```
        LPDISPATCH Add();
        ```

        ### Возвращаемое значение

        - указатель на интерфейс 3D-эквидистанты IEquidistant3D.
        """
        ...

    def Equidistant3D(self, Index) -> IEquidistant3D:  # iequidistants3d_equidistant3d.html
        """
        ## Equidistant3D - Возвращает 3D-эквидистанту, заданную по индексу или имени

        Тип данных: указатель на интерфейс 3D-эквидистанты IEquidistant3D

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        Equidistant3D = iObject.Equidistant3D( Index )     # Получить свойство (* )
        Equidistant3D = iObject.GetEquidistant3D( Index )  # Получить свойство (**)
        ```

        ### Входные параметры

        +-----------------------------------------------+
        | Index | - индекс в коллекции или имя объекта. |
        +-----------------------------------------------+
        """
        ...




class IEvolutions(IModelObjects):  # ievolutions.html
    """
    ## Интерфейс IEvolutions

    Интерфейс коллекции кинематических операций (по траектории).
    """

    def Add(self, EvolutionType) -> IEvolution:  # ievolutions_add.html
        """
        ## Add - Создает новый элемент и добавляет его в коллекцию

        1. Метод позволяет создать новый интерфейс кинематической операции.

        2. Допустимыми значениями EvolutionType являются:

        •o3d_bossEvolution, o3d_cutEvolution для коллекции операций IModelContainer::Evolutions

        •o3d_EvolutionSurface для коллекции поверхностей ISurfaceContainer::EvolutionSurfaces.

        3. После получения нового интерфейса нужно задать параметры операции и вызвать метод IModelObject::Update.

        ### Синтаксис Automation

        ```
        LPDISPATCH Add( ksObj3dTypeEnum EvolutionType );
        ```

        ### Входные параметры

        +----------------------------------------------------------------+
        | EvolutionType | - тип объекта из перечисления ksObj3dTypeEnum. |
        +----------------------------------------------------------------+

        ### Возвращаемое значение

        - указатель на интерфейс IEvolution.
        """
        ...

    def Evolution(self, Index) -> IEvolution:  # ievolutions_evolution.html
        """
        ## Evolution - Возвращает элемент, заданный по индексу или по имени

        Тип данных: Указатель на интерфейс IEvolution.

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        Evolution = Object.Evolution( Index )     # Получить свойство (* )
        Evolution = Object.GetEvolution( Index )  # Получить свойство (**)
        ```

        ### Входные параметры

        +--------------------------------------------+
        | VARIANT Index | - индекс или имя элемента. |
        +--------------------------------------------+
        """
        ...




class IExtensionCurves(IModelObjects):  # iextensioncurves.html
    """
    ## Интерфейс IExtensionCurves

    Интерфейс коллекции продлений кривых

    Интерфейс можно получить используя свойство IAuxiliaryGeomContainer::ExtensionCurves.

    Версия КОМПАС v22
    """

    def Add(self) -> IExtensionCurve:  # iextensioncurves_add.html
        """
        ## Add - Создает новый элемент и добавляет его в коллекцию

        ### Синтаксис Automation

        ```
        LPDISPATCH Add();
        ```

        ### Возвращаемое значение

        Указатель на интерфейс IExtensionCurve
        """
        ...

    def ExtensionCurve(self, Index) -> IExtensionCurve:  # iextensioncurves_extensioncurve.html
        """
        ## ExtensionCurve - Возвращает элемент, заданный по индексу

        Тип данных: Указатель на интерфейс IExtensionCurve.

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        ExtensionCurve = Object.ExtensionCurve( Index )     # Получить свойство (* )
        ExtensionCurve = Object.GetExtensionCurve( Index )  # Получить свойство (**)
        ```
        """
        ...




class IFillets(IModelObjects):  # ifillets.html
    """
    ## Интерфейс IFillets

    Интерфейс коллекции операций скруглений.
    """

    def Add(self) -> IFillet:  # ifillets_add.html
        """
        ## Add - Создает новый элемент и добавляет его в коллекцию

        1. Метод позволяет создать новый интерфейс операции скругления.

        2. После получения нового интерфейса нужно задать параметры операции и вызвать метод IModelObject::Update.

        ### Синтаксис Automation

        ```
        LPDISPATCH Add();
        ```

        ### Возвращаемое значение

        - указатель на интерфейс IFillet.
        """
        ...

    def Fillet(self, Index) -> IFillet:  # ifillets_fillet.html
        """
        ## Fillet - Возвращает элемент, заданный по индексу или по имени

        Тип данных: Указатель на интерфейс IFillet.

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        Fillet = Object.Fillet( Index )     # Получить свойство (* )
        Fillet = Object.GetFillet( Index )  # Получить свойство (**)
        ```

        ### Входные параметры

        +--------------------------------------------+
        | VARIANT Index | - индекс или имя элемента. |
        +--------------------------------------------+
        """
        ...




class IFilletCurves(IModelObjects):  # ifilletcurves.html
    """
    ## Интерфейс IFilletCurves

    Коллекция операций скругления кривых.
    """

    def Add(self) -> IFilletCurve:  # ifilletcurves_add.html
        """
        ## Add - Создает новый элемент и добавляет его в коллекцию

        ### Синтаксис Automation

        ```
        LPDISPATCH Add();
        ```

        ### Возвращаемое значение

        Указатель на интерфейс операции скругления кривых IFilletCurve.
        """
        ...

    def FilletCurve(self, Index) -> IFilletCurve:  # ifilletcurves_filletcurve.html
        """
        ## FilletCurve - Возвращает элемент, заданный по индексу

        Тип данных: указатель на интерфейс IFilletCurve

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        FilletCurve = Object.FilletCurve( Index )     # Получить свойство (* )
        FilletCurve = Object.GetFilletCurve( Index )  # Получить свойство (**)
        ```
        """
        ...




class IHoles3D(IModelObjects):  # iholes3d.html
    """
    ## Интерфейс IHoles3D

    Интерфейс коллекции отверстий.

    Интерфейс можно получить с помощью метода

    IModelContainer::Holes3D
    """

    def Add(self) -> IHole3D:  # iholes3d_add.html
        """
        ## Add - Создает новый элемент и добавляет его в коллекцию

        ### Синтаксис Automation

        ```
        LPDISPATCH Add();
        ```

        ### Возвращаемое значение

        - Указатель на интерфейс IHole3D
        """
        ...

    def Hole3D(self, Index) -> IHole3D:  # iholes3d_hole3d.html
        """
        ## Hole3D - Возвращает элемент, заданный по индексу или по имени

        Тип данных: Указатель на интерфейс IHole3D

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        Hole3D = Object.Hole3D( Index )     # Получить свойство (* )
        Hole3D = Object.GetHole3D( Index )  # Получить свойство (**)
        ```
        """
        ...




class IInclines(IModelObjects):  # iinclines.html
    """
    ## Интерфейс IInclines

    Интерфейс коллекции операций Уклон.
    """

    def Add(self) -> IIncline:  # iinclines_add.html
        """
        ## Add - Создает новый элемент и добавляет его в коллекцию

        1. Метод позволяет создать новый интерфейс операции Уклон.

        2. После получения нового интерфейса нужно задать параметры операции и вызвать метод IModelObject::Update.

        ### Синтаксис Automation

        ```
        LPDISPATCH Add();
        ```

        ### Возвращаемое значение

        - указатель на интерфейс IIncline.
        """
        ...

    def Incline(self, Index) -> IIncline:  # iinclines_incline.html
        """
        ## Incline - Возвращает элемент, заданный по индексу

        Тип данных: Указатель на интерфейс IIncline.

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        Incline = Object.Incline( Index )     # Получить свойство (* )
        Incline = Object.GetIncline( Index )  # Получить свойство (**)
        ```

        ### Входные параметры

        +--------------------------------------------+
        | VARIANT Index | - индекс или имя элемента. |
        +--------------------------------------------+
        """
        ...




class IIsoparametricCurves(IModelObjects):  # iisoparametriccurves.html
    """
    ## Интерфейс IIsoparametricCurves

    Коллекция изопараметрических кривых.

    Интерфейс можно получить у контейнера объектов вспомогательной геометрии с помощью свойства IAuxiliaryGeomContainer::IsoparametricCurves.
    """

    def Add(self) -> IIsoparametricCurve:  # iisoparametriccurves_add.html
        """
        ## Add - Создает новый элемент и добавляет его в коллекцию

        ### Синтаксис Automation

        ```
        LPDISPATCH Add();
        ```

        ### Возвращаемое значение

        Указатель на интерфейс изопараметрической кривой IIsoparametricCurve
        """
        ...

    def IsoparametricCurve(self, Index) -> IIsoparametricCurve:  # iisoparametriccurves_isoparametriccurve.html
        """
        ## IsoparametricCurve - Возвращает элемент, заданный по ссылке или индексу

        Тип данных: Указатель на интерфейс IIsoparametricCurve

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        IsoparametricCurve = Object.IsoparametricCurve( Index )     # Получить свойство (* )
        IsoparametricCurve = Object.GetIsoparametricCurve( Index )  # Получить свойство (**)
        ```
        """
        ...




class IIsoparametricCurvesSets(IModelObjects):  # iisoparametriccurvessets.html
    """
    ## Интерфейс IIsoparametricCurvesSets

    Коллекция групп изопараметрических кривых.

    Интерфейс можно получить у контейнера объектов вспомогательной геометрии с помощью свойства IAuxiliaryGeomContainer::IsoparametricCurvesSets.
    """

    def Add(self) -> IIsoparametricCurvesSet:  # iisoparametriccurvessets_add.html
        """
        ## Add - Создает новый элемент и добавляет его в коллекцию

        ### Синтаксис Automation

        ```
        LPDISPATCH Add();
        ```

        ### Возвращаемое значение

        Указатель на интерфейс группы изопараметрических кривых IIsoparametricCurvesSet
        """
        ...

    def IsoparametricCurvesSet(self, Index) -> IIsoparametricCurvesSet:  # iisoparametriccurvessets_isoparametriccurvesset.html
        """
        ## IsoparametricCurvesSet - Возвращает элемент, заданныйпо ссылке или индексу

        Тип данных: Указатель на интерфейс IIsoparametricCurvesSet

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        IsoparametricCurvesSet = Object.IsoparametricCurvesSet( Index )     # Получить свойство (* )
        IsoparametricCurvesSet = Object.GetIsoparametricCurvesSet( Index )  # Получить свойство (**)
        ```
        """
        ...




class IJointSurfaces(IModelObjects):  # ijointsurfaces.html
    """
    ## Интерфейс IJointSurfaces

    Интерфейс коллекции поверхностей соединения.
    """

    def Add(self) -> IJointSurface:  # ijointsurfaces_add.html
        """
        ## Add - Создает новый элемент и добавляет его в коллекцию

        1. Метод позволяет создать новый интерфейс операции Поверхность соединения.

        2. После получения нового интерфейса нужно задать параметры операции и вызвать метод IModelObject::Update.

        ### Синтаксис Automation

        ```
        LPDISPATCH Add();
        ```

        ### Возвращаемое значение

        - указатель на интерфейс IJointSurface.
        """
        ...

    def JointSurface(self, Index) -> IJointSurface:  # ijointsurfaces_jointsurface.html
        """
        ## JointSurface - Возвращает элемент, заданный по индексу

        Тип данных: Указатель на интерфейс IJointSurface.

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        JointSurface = Object.JointSurface( Index )     # Получить свойство (* )
        JointSurface = Object.GetJointSurface( Index )  # Получить свойство (**)
        ```

        ### Входные параметры

        +--------------------------------------------+
        | VARIANT Index | - индекс или имя элемента. |
        +--------------------------------------------+
        """
        ...




class IJointSurface(IModelObject):  # ijointsurface.html
    """
    ## Интерфейс IJointSurface

    Интерфейс операции Поверхность соединения.
    """

    @property
    def AutoSegmentation(self):
        ...
    @AutoSegmentation.setter
    def AutoSegmentation(self, value): ...

    @property
    def CheckSelfIntersection(self):
        ...
    @CheckSelfIntersection.setter
    def CheckSelfIntersection(self, value): ...

    @property
    def Curves1(self) -> typing.Any:  # ijointsurface_curves1.html
        """
        ## Curves1 - Кривая границы 1

        Тип данных: VARIANT

        ### Синтаксис Automation

        ```
        Curves1 = Object.Curves1       # Получить свойство (* )
        Object.Curves1 = Curves1       # Установить свойство (* )
        Curves1 = Object.GetCurves1()  # Получить свойство (**)
        Object.SetCurves1( Curves1 )   # Установить свойство (**)
        ```
        """
        ...
    @Curves1.setter
    def Curves1(self, value: typing.Any): ...

    @property
    def Curves2(self) -> typing.Any:  # ijointsurface_curves2.html
        """
        ## Curves2 - Кривая границы 2

        Тип данных: VARIANT

        ### Синтаксис Automation

        ```
        Curves2 = Object.Curves2       # Получить свойство (* )
        Object.Curves2 = Curves2       # Установить свойство (* )
        Curves2 = Object.GetCurves2()  # Получить свойство (**)
        Object.SetCurves2( Curves2 )   # Установить свойство (**)
        ```
        """
        ...
    @Curves2.setter
    def Curves2(self, value: typing.Any): ...

    @property
    def EdgesCount(self):
        ...

    @property
    def Face1(self) -> IModelObject:  # ijointsurface_face1.html
        """
        ## Face1 - Грань границы 1

        Тип данных: Указатель на интерфейс IModelObject.

        ### Синтаксис Automation

        ```
        Face1 = Object.Face1       # Получить свойство (* )
        Object.Face1 = Face1       # Установить свойство (* )
        Face1 = Object.GetFace1()  # Получить свойство (**)
        Object.SetFace1( Face1 )   # Установить свойство (**)
        ```
        """
        ...
    @Face1.setter
    def Face1(self, value: IModelObject): ...

    @property
    def Face1ConnectType(self) -> int:  # ijointsurface_face1connecttype.html
        """
        ## Face1ConnectType - Условие сопряжения соединяющей поверхности с поверхностью сопряжения 1

        Тип данных: Значение из перечисления ksConnectTypeEnum.

        ### Синтаксис Automation

        ```
        Face1ConnectType = Object.Face1ConnectType       # Получить свойство (* )
        Object.Face1ConnectType = Face1ConnectType       # Установить свойство (* )
        Face1ConnectType = Object.GetFace1ConnectType()  # Получить свойство (**)
        Object.SetFace1ConnectType( Face1ConnectType )   # Установить свойство (**)
        ```
        """
        ...
    @Face1ConnectType.setter
    def Face1ConnectType(self, value: int): ...

    @property
    def Face2(self) -> IModelObject:  # ijointsurface_face2.html
        """
        ## Face2 - Грань границы 2

        Тип данных: Указатель на интерфейс IModelObject.

        ### Синтаксис Automation

        ```
        Face2 = Object.Face2       # Получить свойство (* )
        Object.Face2 = Face2       # Установить свойство (* )
        Face2 = Object.GetFace2()  # Получить свойство (**)
        Object.SetFace2( Face2 )   # Установить свойство (**)
        ```
        """
        ...
    @Face2.setter
    def Face2(self, value: IModelObject): ...

    @property
    def Face2ConnectType(self) -> int:  # ijointsurface_face2connecttype.html
        """
        ## Face2ConnectType - Условие сопряжения соединяющей поверхности с поверхностью сопряжения 2

        Тип данных: Значение из перечисления ksConnectTypeEnum.

        ### Синтаксис Automation

        ```
        Face2ConnectType = Object.Face2ConnectType       # Получить свойство (* )
        Object.Face2ConnectType = Face2ConnectType       # Установить свойство (* )
        Face2ConnectType = Object.GetFace2ConnectType()  # Получить свойство (**)
        Object.SetFace2ConnectType( Face2ConnectType )   # Установить свойство (**)
        ```
        """
        ...
    @Face2ConnectType.setter
    def Face2ConnectType(self, value: int): ...

    @property
    def GeneralTension(self):
        ...
    @GeneralTension.setter
    def GeneralTension(self, value): ...

    @property
    def Sense1(self) -> bool:  # ijointsurface_sense1.html
        """
        ## Sense1 - Сменить направление сопряжения вдоль границы 1

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        Sense1 = Object.Sense1       # Получить свойство (* )
        Object.Sense1 = Sense1       # Установить свойство (* )
        Sense1 = Object.GetSense1()  # Получить свойство (**)
        Object.SetSense1( Sense1 )   # Установить свойство (**)
        ```
        """
        ...
    @Sense1.setter
    def Sense1(self, value: bool): ...

    @property
    def Sense2(self) -> bool:  # ijointsurface_sense2.html
        """
        ## Sense2 - Сменить направление сопряжения вдоль границы 2

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        Sense2 = Object.Sense2       # Получить свойство (* )
        Object.Sense2 = Sense2       # Установить свойство (* )
        Sense2 = Object.GetSense2()  # Получить свойство (**)
        Object.SetSense2( Sense2 )   # Установить свойство (**)
        ```
        """
        ...
    @Sense2.setter
    def Sense2(self, value: bool): ...

    @property
    def SideEdges1(self):
        ...
    @SideEdges1.setter
    def SideEdges1(self, value): ...

    @property
    def SideEdges2(self):
        ...
    @SideEdges2.setter
    def SideEdges2(self, value): ...

    @property
    def Tension1(self) -> float:  # ijointsurface_tension1.html
        """
        ## Tension1 - Натяжение поверхности вдоль границы 1 %

        Тип данных: double

        ### Синтаксис Automation

        ```
        Tension1 = Object.Tension1       # Получить свойство (* )
        Object.Tension1 = Tension1       # Установить свойство (* )
        Tension1 = Object.GetTension1()  # Получить свойство (**)
        Object.SetTension1( Tension1 )   # Установить свойство (**)
        ```
        """
        ...
    @Tension1.setter
    def Tension1(self, value: float): ...

    @property
    def Tension2(self) -> float:  # ijointsurface_tension2.html
        """
        ## Tension2 - Натяжение поверхности вдоль границы 2 %

        Тип данных: double

        ### Синтаксис Automation

        ```
        Tension2 = Object.Tension2       # Получить свойство (* )
        Object.Tension2 = Tension2       # Установить свойство (* )
        Tension2 = Object.GetTension2()  # Получить свойство (**)
        Object.SetTension2( Tension2 )   # Установить свойство (**)
        ```
        """
        ...
    @Tension2.setter
    def Tension2(self, value: float): ...

    def AddNewEdge(self, IndexAt):
        ...

    def DeleteEdge(self, Index):
        ...

    def GetEdgePointParam(self, EdgeIndex, StartPoint):
        ...

    def GetEdgePointParams(self):
        ...

    def SetEdgePointParam(self, EdgeIndex, StartPoint, X, Y, Z, T, AssociateVertex):
        ...




class ILineSegments3D(IModelObjects):  # ilinesegments3d.html
    """
    ## Интерфейс ILineSegments3D

    Коллекция контуров 3D.
    """

    def Add(self) -> ILineSegment3D:  # ilinesegments3d_add.html
        """
        ## Add - Создает новый элемент и добавляет его в коллекцию

        ### Синтаксис Automation

        ```
        LPDISPATCH Add();
        ```

        ### Возвращаемое значение

        Указатель на интерфейс отрезка ILineSegment3D
        """
        ...

    def LineSegment3D(self, Index) -> ILineSegment3D:  # ilinesegments3d_linesegment3d.html
        """
        ## LineSegment3D - Возвращает элемент, заданный по индексу

        Тип данных: указатель указатель на интерфейс ILineSegment3D

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        LineSegment3D = Object.LineSegment3D( Index )     # Получить свойство (* )
        LineSegment3D = Object.GetLineSegment3D( Index )  # Получить свойство (**)
        ```
        """
        ...




class ILofts(IModelObjects):  # ilofts.html
    """
    ## Интерфейс ILofts

    Интерфейс коллекции операций По сечениям.
    """

    def Add(self, LoftType) -> ILoft:  # ilofts_add.html
        """
        ## Add - Создает новый элемент и добавляет его в коллекцию

        1. Метод позволяет создать новый интерфейс операции по сечениям.

        2. Допустимыми значениями LoftType являются:

        •o3d_bossLoft, o3d_cutLoft для коллекции операций IModelContainer::Lofts ,

        •o3d_LoftSurface для коллекции поверхностей ISurfaceContainer::LoftSurfaces.

        3. После получения нового интерфейса нужно задать параметры операции и вызвать метод IModelObject::Update.

        ### Синтаксис Automation

        ```
        LPDISPATCH Add( ksObj3dTypeEnum LoftType );
        ```

        ### Входные параметры

        +-----------------------------------------------------------+
        | LoftType | - тип объекта из перечисления ksObj3dTypeEnum. |
        +-----------------------------------------------------------+

        ### Возвращаемое значение

        - Указатель на интерфейс ILoft.
        """
        ...

    def Loft(self, Index) -> ILoft:  # ilofts_loft.html
        """
        ## Loft - Возвращает элемент, заданный по индексу

        Тип данных: Указатель на интерфейс ILoft.

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        Loft = Object.Loft( Index );     # Получить свойство (* )
        Loft = Object.GetLoft( Index );  # Получить свойство (**)
        ```

        ### Входные параметры

        +--------------------------------------------+
        | VARIANT Index | - индекс или имя элемента. |
        +--------------------------------------------+
        """
        ...




class INurbsSurfacesByCurvesMeshs(IModelObjects):  # inurbssurfacesbycurvesmeshs.html
    """
    ## Интерфейс INurbsSurfacesByCurvesMeshs

    Интерфейс коллекции сплайновых поверхностей по сетке кривых.
    """

    def Add(self) -> INurbsSurfaceByCurvesMesh:  # inurbssurfacesbycurvesmeshs_add.html
        """
        ## Add - Создает новый элемент и добавляет его в коллекцию

        1. Метод позволяет создать новый интерфейс сплайновой поверхности по сетке кривых.

        2. После получения нового интерфейса нужно задать параметры поверхности и вызвать метод IModelObject::Update.

        ### Синтаксис Automation

        ```
        LPDISPATCH Add();
        ```

        ### Возвращаемое значение

        - указатель на интерфейс INurbsSurfaceByCurvesMesh
        """
        ...

    def NurbsSurfaceByCurvesMesh(self, Index) -> INurbsSurfaceByCurvesMesh:  # inurbssurfacesbycurvesmeshs_nurbssurfacebycurvesmesh.html
        """
        ## NurbsSurfaceByCurvesMesh - Возвращает элемент, заданный по индексу

        Тип данных: Указатель на интерфейс INurbsSurfaceByCurvesMesh.

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        NurbsSurfaceByCurvesMesh = Object.NurbsSurfaceByCurvesMesh( Index )       # Получить свойство (* )
        NurbsSurfaceByCurvesMesh = Object.GetNurbsSurfaceByCurvesMesh( Index ) ;  # Получить свойство (**)
        ```
        """
        ...




class INurbsSurfaceByCurvesMesh(IModelObject):  # inurbssurfacebycurvesmesh.html
    """
    ## Интерфейс INurbsSurfaceByCurvesMesh

    Интерфейс операции Сплайновая поверхность по сетке кривых.
    """

    @property
    def CheckSelfIntersection(self) -> bool:  # inurbssurfacebycurvesmesh_checkselfintersection.html
        """
        ## CheckSelfIntersection - Проверка самопересечения

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        CheckSelfIntersection = Object.CheckSelfIntersection       # Получить свойство (* )
        Object.CheckSelfIntersection = CheckSelfIntersection       # Установить свойство (* )
        CheckSelfIntersection = Object.GetCheckSelfIntersection()  # Получить свойство (**)
        Object.SetCheckSelfIntersection( CheckSelfIntersection )   # Установить свойство (**)
        ```
        """
        ...
    @CheckSelfIntersection.setter
    def CheckSelfIntersection(self, value: bool): ...

    @property
    def UClosed(self) -> bool:  # inurbssurfacebycurvesmesh_uclosed.html
        """
        ## UClosed - Замкнутость по направлении U

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        UClosed = Object.UClosed       # Получить свойство (* )
        Object.UClosed = UClosed       # Установить свойство (* )
        UClosed = Object.GetUClosed()  # Получить свойство (**)
        Object.SetUClosed( UClosed )   # Установить свойство (**)
        ```
        """
        ...
    @UClosed.setter
    def UClosed(self, value: bool): ...

    @property
    def UCurves(self) -> typing.Any:  # inurbssurfacebycurvesmesh_ucurves.html
        """
        ## UCurves - Список кривых в направлении U SAFEARRAY DISPATCH - VT_ARRAY | VT_DISPATCH

        Тип данных: VARIANT

        ### Синтаксис Automation

        ```
        UCurves = Object.UCurves       # Получить свойство (* )
        Object.UCurves = UCurves       # Установить свойство (* )
        UCurves = Object.GetUCurves()  # Получить свойство (**)
        Object.SetUCurves( UCurves )   # Установить свойство (**)
        ```
        """
        ...
    @UCurves.setter
    def UCurves(self, value: typing.Any): ...

    @property
    def VClosed(self) -> bool:  # inurbssurfacebycurvesmesh_vclosed.html
        """
        ## VClosed - Замкнутость по направлении V

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        VClosed = Object.VClosed       # Получить свойство (* )
        Object.VClosed = VClosed       # Установить свойство (* )
        VClosed = Object.GetVClosed()  # Получить свойство (**)
        Object.SetVClosed( VClosed )   # Установить свойство (**)
        ```
        """
        ...
    @VClosed.setter
    def VClosed(self, value: bool): ...

    @property
    def VCurves(self) -> typing.Any:  # inurbssurfacebycurvesmesh_vcurves.html
        """
        ## VCurves - Список кривых в направлении V SAFEARRAY DISPATCH - VT_ARRAY | VT_DISPATCH

        Тип данных: VARIANT

        ### Синтаксис Automation

        ```
        VCurves = Object.VCurves       # Получить свойство (* )
        Object.VCurves = VCurves       # Установить свойство (* )
        VCurves = Object.GetVCurves()  # Получить свойство (**)
        Object.SetVCurves( VCurves )   # Установить свойство (**)
        ```
        """
        ...
    @VCurves.setter
    def VCurves(self, value: typing.Any): ...

    def ConnectSurface(self, Index) -> IModelObject:  # inurbssurfacebycurvesmesh_connectsurface.html
        """
        ## ConnectSurface - Поверхность сопряжения

        Тип данных: Указатель на интерфейс IModelObject.

        ### Синтаксис Automation

        ```
        ConnectSurface = Object.ConnectSurface( Index )     # Получить свойство (* )
        Object.ConnectSurface( Index ) = ConnectSurface     # Установить свойство (* )
        ConnectSurface = Object.GetConnectSurface( Index )  # Получить свойство (**)
        Object.SetConnectSurface( Index, ConnectSurface )   # Установить свойство (**)
        ```

        ### Входные параметры

        +------------------------+
        | long Index | - индекс. |
        +------------------------+
        """
        ...

    def ConnectType(self, Index) -> int:  # inurbssurfacebycurvesmesh_connecttype.html
        """
        ## ConnectType - Условие сопряжения

        Тип данных: из перечисления ksConnectTypeEnum

        ### Синтаксис Automation

        ```
        ConnectType = Object.ConnectType( Index )     # Получить свойство (* )
        Object.ConnectType( Index ) = ConnectType     # Установить свойство (* )
        ConnectType = Object.GetConnectType( Index )  # Получить свойство (**)
        Object.SetConnectType( Index, ConnectType )   # Установить свойство (**)
        ```

        ### Входные параметры

        +------------------------+
        | long Index | - индекс. |
        +------------------------+
        """
        ...

    def SetConnectSurface(self, Index, arg1):
        ...

    def SetConnectType(self, Index, arg1):
        ...




class IPointsArrsOnSurfaces(IModelObjects):  # ipointsarrsonsurfaces.html
    """
    ## Интерфейс IPointsArrsOnSurfaces

    Коллекция групп точек по поверхностям.

    Интерфейс можно получить с помощью свойства IAuxiliaryGeomContainer::PointsArrsOnSurfaces.
    """

    def Add(self) -> IPointsArrOnSurface:  # ipointsarrsonsurfaces_add.html
        """
        ## Add - Создает новый элемент и добавляет его в коллекцию

        ### Синтаксис Automation

        ```
        LPDISPATCH Add();
        ```

        ### Возвращаемое значение

        - указатель на интерфейс IPointsArrOnSurface.
        """
        ...

    def PointsArrOnSurface(self, Index) -> IPointsArrOnSurface:  # ipointsarrsonsurfaces_pointsarronsurface.html
        """
        ## PointsArrOnSurface - Возвращает элемент, заданный по ссылке или индексу

        Тип данных: Указатель на интерфейс IPointsArrOnSurface

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        PointsArrOnSurface = Object.PointsArrOnSurface( Index );     # Получить свойство (* )
        PointsArrOnSurface = Object.GetPointsArrOnSurface( Index );  # Получить свойство (**)
        ```
        """
        ...




class IPointsArrsOnCurves(IModelObjects):  # ipointsarrsoncurves.html
    """
    ## Интерфейс IPointsArrsOnCurves

    Коллекция групп точек по кривым.

    Интерфейс можно получить с помощью свойства IAuxiliaryGeomContainer::PointsArrsOnCurves.
    """

    def Add(self) -> IPointsArrOnCurve:  # ipointsarrsoncurves_add.html
        """
        ## Add - Создает новый элемент и добавляет его в коллекцию

        ### Синтаксис Automation

        ```
        LPDISPATCH Add();
        ```

        ### Возвращаемое значение

        - указатель на интерфейс IPointsArrOnCurve.
        """
        ...

    def PointsArrOnCurve(self, Index) -> IPointsArrOnCurve:  # ipointsarrsoncurves_pointsarroncurve.html
        """
        ## PointsArrOnCurve - Возвращает элемент, заданный по ссылке или индексу

        Тип данных: Указатель на интерфейс IPointsArrOnCurve

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        PointsArrOnCurve = Object.PointsArrOnCurve( Index );     # Получить свойство (* )
        PointsArrOnCurve = Object.GetPointsArrOnCurve( Index );  # Получить свойство (**)
        ```
        """
        ...




class IPointsArrsFromFiles(IModelObjects):  # ipointsarrsfromfiles.html
    """
    ## Интерфейс IPointsArrsFromFiles

    Коллекция групп точек из файла.

    Интерфейс можно получить с помощью свойства IAuxiliaryGeomContainer::PointsArrsFromFiles.
    """

    def Add(self) -> IPointsArrFromFile:  # ipointsarrsfromfiles_add.html
        """
        ## Add - Создает новый элемент и добавляет его в коллекцию

        ### Синтаксис Automation

        ```
        LPDISPATCH Add();
        ```

        ### Возвращаемое значение

        - указатель на интерфейс IPointsArrFromFile.
        """
        ...

    def PointsArrFromFile(self, Index) -> IPointsArrFromFile:  # ipointsarrsfromfiles_pointsarrfromfile.html
        """
        ## PointsArrFromFile - Возвращает элемент, заданный по ссылке или индексу

        Тип данных: Указатель на интерфейс IPointsArrFromFile

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        PointsArrFromFile = Object.PointsArrFromFile( Index );     # Получить свойство (* )
        PointsArrFromFile = Object.GetPointsArrFromFile( Index );  # Получить свойство (**)
        ```
        """
        ...




class IPolyLines(IModelObjects):  # ipolylines.html
    """
    ## Интерфейс IPolyLines

    Интерфейс коллекции элементов "пространственная ломаная".

    Интерфейс позволяет получить коллекции элементов "пространственная ломаная".

    Получить интерфейс коллекции можно, используя свойство контейнера объектов вспомогательной геометрии IAuxiliaryGeomContainer::PolyLines.
    """

    def Add(self) -> IPolyLine:  # ipolylines_add.html
        """
        ## Add - Создать новый элемент и добавить его в коллекцию

        ### Синтаксис Automation

        ```
        LPDISPATCH Add;
        ```

        ### Возвращаемое значение

        - Указатель на интерфейс пространственной ломаной IPolyLine.
        """
        ...

    def PolyLine(self, Index) -> IPolyLine:  # ipolylines_polyline.html
        """
        ## PolyLine - Пространственная ломаная, заданная по индексу

        Тип данных: указатель на интерфейс IPolyLine

        1. В качестве индекса может использоваться индекс в коллекции и reference объекта.

        2. Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        PolyLine = iObject.PolyLine( Index );     # Получить свойство (* )
        PolyLine = iObject.GetPolyLine( Index );  # Получить свойство (**)
        ```

        ### Входные параметры

        +----------------------------------------------------------+
        | Index | - VT_BSTR - имя объекта, VT_I4 - индекс объекта. |
        +----------------------------------------------------------+
        """
        ...




class IProjectionCurves(IModelObjects):  # iprojectioncurves.html
    """
    ## Интерфейс IProjectionCurves

    Интерфейс коллекции проекционных кривых.

    Интерфейс можно получить с помощью свойства IAuxiliaryGeomContainer::ProjectionCurves.
    """

    def Add(self) -> IProjectionCurve:  # iprojectioncurves_add.html
        """
        ## Add - Создает новый элемент и добавляет его в коллекцию

        ### Синтаксис Automation

        ```
        LPDISPATCH Add();
        ```

        ### Возвращаемое значение

        - Указатель на интерфейс IProjectionCurve.
        """
        ...

    def ProjectionCurve(self, Index) -> IProjectionCurve:  # iprojectioncurves_projectioncurve.html
        """
        ## ProjectionCurve - Возвращает элемент, заданный по ссылке или индексу

        Тип данных: указатель на интерфейс IProjectionCurve

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        ProjectionCurve = Object.ProjectionCurve( index )     # Получить свойство (* )
        ProjectionCurve = Object.GetProjectionCurve( index )  # Получить свойство (**)
        ```
        """
        ...




class IRibs(IModelObjects):  # iribs.html
    """
    ## Интерфейс IRibs

    Интерфейс коллекции ребер жесткости.
    """

    def Add(self) -> IRib:  # iribs_add.html
        """
        ## Add - Создает новый элемент и добавляет его в коллекцию

        Метод позволяет создать новый интерфейс операции ребро жесткости.

        После получения нового интерфейса нужно задать параметры операции и вызвать метод IModelObject::Update.

        ### Синтаксис Automation

        ```
        LPDISPATCH Add();
        ```

        ### Возвращаемое значение

        - указатель на интерфейс IRib.
        """
        ...

    def Rib(self, Index) -> IRib:  # iribs_rib.html
        """
        ## Rib - Возвращает элемент, заданный по индексу или имени

        Тип данных: Указатель на интерфейс IRib.

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        Rib = Object.Rib( Index )     # Получить свойство (* )
        Rib = Object.GetRib( Index )  # Получить свойство (**)
        ```

        ### Входные параметры

        +--------------------------------------------+
        | VARIANT Index | - индекс или имя элемента. |
        +--------------------------------------------+
        """
        ...




class IShells(IModelObjects):  # ishells.html
    """
    ## Интерфейс IShells

    Интерфейс коллекции операций Оболочка.
    """

    def Add(self) -> IShell:  # ishells_add.html
        """
        ## Add - Создает новый элемент и добавляет его в коллекцию

        Метод позволяет создать новый интерфейс операции оболочка.

        После получения нового интерфейса нужно задать параметры операции и вызвать метод IModelObject::Update.

        ### Синтаксис Automation

        ```
        LPDISPATCH Add();
        ```

        ### Возвращаемое значение

        - указатель на интерфейс IShell.
        """
        ...

    def Shell(self, Index) -> IShell:  # ishells_shell.html
        """
        ## Shell - Возвращает элемент, заданный по ссылке или индексу

        Тип данных: Указатель на интерфейс IShell.

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        Shell = Object.Shell( Index )     # Получить свойство (* )
        Shell = Object.GetShell( Index )  # Получить свойство (**)
        ```

        ### Входные параметры

        +--------------------------------------------+
        | VARIANT Index | - индекс или имя элемента. |
        +--------------------------------------------+
        """
        ...




class ISplines3D(IModelObjects):  # isplines3d.html
    """
    ## Интерфейс ISplines3D

    Интерфейс коллекции элементов "сплайн".

    Интерфейс позволяет получить элементы "сплайн".

    Получить интерфейс коллекции можно, используя свойство контейнера вспомогательной геометрии IAuxiliaryGeomContainer::Splines3D.
    """

    def Add(self) -> ISpline3D:  # isplines3d_add.html
        """
        ## Add - Создать новый элемент и добавить его в коллекцию

        ### Синтаксис Automation

        ```
        LPDISPATCH Add;
        ```

        ### Возвращаемое значение

        - Указатель на интерфейс сплайна ISpline3D.
        """
        ...

    def Convert(self, Objects) -> list[IDispatch]:  # isplines3d_convert.html
        """
        ## Convert - Создает сплайны по объектам и добавляет их в коллекцию

        ### Синтаксис Automation

        ```
        VARIANT Convert( VARIANT Objects );
        ```

        ### Входные параметры

        +----------------------------------------------------------------------------+
        | Objects | - объект VT_DISPATCH или массив объектов VT_ARRAY | VT_DISPATCH. |
        +----------------------------------------------------------------------------+

        ### Возвращаемое значение

        - объект VT_DISPATCH или массив объектов VT_ARRAY | VT_DISPATCH.
        """
        ...

    def Spline3D(self, Index) -> ISpline3D:  # isplines3d_spline3d.html
        """
        ## Spline3D - Сплайн, заданный по индексу

        Тип данных: указатель на интерфейс ISpline3D

        1. В качестве индекса может использоваться индекс в коллекции и reference объекта.

        2. Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        Spline3D = Object.Spline3D( index )     # Получить свойство (* )
        Spline3D = Object.GetSpline3D( index )  # Получить свойство (**)
        ```

        ### Входные параметры

        +--------------------------------------------------------+
        | Index | VT_BSTR - имя объекта, VT_I4 - индекс объекта. |
        +--------------------------------------------------------+
        """
        ...




class ISplinesOnSurfaces(IModelObjects):  # isplinesonsurfaces.html
    """
    ## Интерфейс ISplinesOnSurfaces

    Коллекция сплайнов на поверхностях.

    Интерфейс можно получить у контейнера объектов вспомогательной геометрии с помощью свойства IAuxiliaryGeomContainer::SplinesOnSurfaces.
    """

    def Add(self) -> ISplineOnSurface:  # isplinesonsurfaces_add.html
        """
        ## Add - Создает новый элемент и добавляет его в коллекцию

        ### Синтаксис Automation

        ```
        LPDISPATCH Add();
        ```

        ### Возвращаемое значение

        Указатель на интерфейс сплайна на поверхности ISplineOnSurface.
        """
        ...

    def SplineOnSurface(self, Index) -> ISplineOnSurface:  # isplinesonsurfaces_splineonsurface.html
        """
        ## SplineOnSurface - Возвращает элемент, заданный по ссылке или индексу

        Тип данных: Указатель на интерфейс ISplineOnSurface

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        SplineOnSurface = Object.SplineOnSurface( Index )     # Получить свойство (* )
        SplineOnSurface = Object.GetSplineOnSurface( Index )  # Получить свойство (**)
        ```
        """
        ...




class ISurfacesIntersectionCurves(IModelObjects):  # isurfacesintersectioncurves.html
    """
    ## Интерфейс ISurfacesIntersectionCurves

    Интерфейс коллекции NURBS-поверхностей.

    Данный интерфейс можно получить с помощью свойства IAuxiliaryGeomContainer::SurfacesIntersectionCurves.
    """

    def Add(self) -> ISurfacesIntersectionCurve|IModelObject:  # isurfacesintersectioncurves_add.html
        """
        ## Add - Создает новый объект «кривая пересечения поверхностей» и добавляет его в коллекцию

        ### Синтаксис Automation

        ```
        LPDISPATCH Add();
        ```

        ### Возвращаемое значение

        - Указатель на интерфейс кривой пересечения поверхностей ISurfacesIntersectionCurve.

        После добавления объекта требуется задать параметры и вызвать метод IModelObject::Update
        """
        ...

    def SurfacesIntersectionCurve(self, Index) -> ISurfacesIntersectionCurve:  # isurfacesintersectioncurves_surfacesintersectioncurve.html
        """
        ## SurfacesIntersectionCurve - Возвращает элемент, заданный по имени или индексу

        Тип данных: указатель на интерфейс ISurfacesIntersectionCurve

        ### Синтаксис Automation

        ```
        SurfacesIntersectionCurve = Object.SurfacesIntersectionCurve( Index )     # Получить свойство (* )
        SurfacesIntersectionCurve = Object.GetSurfacesIntersectionCurve( Index )  # Получить свойство (**)
        ```

        ### Входные параметры

        +-----------------------------------------------+
        | Index | - имя или индекс объекта в коллекции. |
        +-----------------------------------------------+

        Свойство позволяет получить интерфейс кривой пересечения поверхностей по имени или индексу.
        """
        ...




class ISpirals3D(IModelObjects):  # ispirals3d.html
    """
    ## Интерфейс ISpirals3D

    Коллекция спиралей 3D.

    Интерфейс можно получить с помощью свойства IAuxiliaryGeomContainer::Spirals3D.
    """

    def Add(self, Type) -> ISpiral3D:  # ispirals3d_add.html
        """
        ## Add - Создает новый элемент и добавляет его в коллекцию

        ### Синтаксис Automation

        ```
        LPDISPATCH Add( ksObj3dTypeEnum Type );
        ```

        ### Входные параметры

        Type - тип создаваемой спирали (коническая или цилиндрическая)

        ### Возвращаемое значение

        Указатель на интерфейс ISpiral3D
        """
        ...

    def Spiral3D(self, Index) -> ISpiral3D:  # ispirals3d_spiral3d.html
        """
        ## Spiral3D - Возвращает элемент, заданный по индексу

        Тип данных: Указатель на интерфейс ISpiral3D

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        Spiral3D = Object.Spiral3D( Index )     # Получить свойство (* )
        Spiral3D = Object.GetSpiral3D( Index )  # Получить свойство (**)
        ```
        """
        ...




class ITrimmedCurves(IModelObjects):  # itrimmedcurves.html
    """
    ## Интерфейс ITrimmedCurves

    Коллекция операций усечения кривой
    """

    def Add(self) -> ITrimmedCurve:  # itrimmedcurves_add.html
        """
        ## Add - Создает новый элемент и добавляет его в коллекцию

        ### Синтаксис Automation

        ```
        LPDISPATCH Add();
        ```

        ### Возвращаемое значение

        Указатель на интерфейс усеченной кривой ITrimmedCurve.
        """
        ...

    def TrimmedCurve(self, Index) -> ITrimmedCurve:  # itrimmedcurves_trimmedcurve.html
        """
        ## TrimmedCurve - Возвращает элемент, заданный по индексу

        Тип данных: указатель на интерфейс ITrimmedCurve

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        TrimmedCurve = Object.TrimmedCurve( Index )     # Получить свойство (* )
        TrimmedCurve = Object.GetTrimmedCurve( Index )  # Получить свойство (**)
        ```
        """
        ...




class IUnhistoredCurves3D(IModelObjects):  # iunhistoredcurves3d.html
    """
    ## Интерфейс IUnhistoredCurves3D

    Интерфейс коллекции кривых без истории.
    """

    def Add(self, Points) -> IUnhistoredCurve3D:  # iunhistoredcurves3d_add.html
        """
        ## Add - Создает новую кривую по массиву точек

        ### Синтаксис Automation

        ```
        LPDISPATCH Add( VARIANT Points );
        ```

        ### Входные параметры

        +-------------------------------------------+
        | Points | - массив точек VT_ARRAY | VT_R8. |
        +-------------------------------------------+

        ### Возвращаемое значение

        - Указатель на интерфейс IUnhistoredCurve3D
        """
        ...

    def Convert(self, Curve, DeleteSource) -> IUnhistoredCurve3D:  # iunhistoredcurves3d_convert.html
        """
        ## Convert - Создает кривую без истории по кривой

        ### Синтаксис Automation

        ```
        LPDISPATCH Convert( IModelObject * Curve, BOOL DeleteSource );
        ```

        ### Входные параметры

        +------------------------------------------------+
        | Curve        | - указатель на исходную кривую, |
        +------------------------------------------------+
        | DeleteSource | - удалить исходную кривую.      |
        +------------------------------------------------+

        ### Возвращаемое значение

        указатель на интерфейс кривой без истории IUnhistoredCurve3D
        """
        ...

    def Load(self, FileName, SewCurves) -> list[IDispatch]:  # iunhistoredcurves3d_load.html
        """
        ## Load - Прочитать кривые из файла

        ### Синтаксис Automation

        ```
        VARIANT Load( BSTR FileName, BOOL SewCurves );
        ```

        ### Входные параметры

        +-------------------------------+
        | FileName  | - имя файла,      |
        +-------------------------------+
        | SewCurves | - сшивать кривые. |
        +-------------------------------+

        ### Возвращаемое значение

        - массив указателей на кривые VT_ARRAY | VT_DISPATCH.
        """
        ...

    def UnhistoredCurve3D(self, Index) -> IUnhistoredCurve3D:  # iunhistoredcurves3d_unhistoredcurve3d.html
        """
        ## UnhistoredCurve3D - Возвращает элемент, заданный по индексу

        Тип данных: Указатель на интерфейс IUnhistoredCurve3D

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        UnhistoredCurve3D = Object.UnhistoredCurve3D( Index )     # Получить свойство (* )
        UnhistoredCurve3D = Object.GetUnhistoredCurve3D( Index )  # Получить свойство (**)
        ```
        """
        ...




class IUserFolders(IModelObjects):  # iuserfolders.html
    """
    ## Интерфейс IUserFolders

    Интерфейс коллекции пользовательских директорий.
    """

    def Add(self) -> IUserFolder:  # iuserfolders_add.html
        """
        ## Add - Создает новый элемент и добавляет его в коллекцию

        ### Синтаксис Automation

        ```
        LPDISPATCH Add();
        ```

        ### Возвращаемое значение

        - указатель на интерфейс IUserFolder.
        """
        ...

    def UserFolder(self, Index):
        ...




class IUserObjects3D(IModelObjects):  # iuserobjects3d.html
    """
    ## Интерфейс IUserObjects3D

    Интерфейс коллекции пользовательских объектов 3D.
    """

    def Add(self, Type) -> IUserObject3D:  # iuserobjects3d_add.html
        """
        ## Add - Создает новый элемент и добавляет его в коллекцию

        ### Синтаксис Automation

        ```
        LPDISPATCH Add();
        ```

        ### Возвращаемое значение

        Указатель на интерфейс пользовательского объекта IUserObject3D.
        """
        ...

    def UserObject3D(self, Index) -> IUserObject3D:  # iuserobjects3d_userobject3d.html
        """
        ## UserObject3D - Возвращает элемент, заданный по индексу

        Тип данных: Указатель на интерфейс IUserObject3D

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        UserObject3D = Object.UserObject3D( Index )     # Получить свойство (* )
        UserObject3D = Object.GetUserObject3D( Index )  # Получить свойство (**)
        ```
        """
        ...




class IArc3D(IModelObject):  # iarc3d.html
    """
    ## Интерфейс IArc3D

    Интерфейс параметров 3D дуги.

    Интерфейс можно получить у коллекции дуг, используя свойство IArcs3D::Arc3D или метод IArcs3D::Add.

    С помощью метода IUnknown::QueryInterface у интерфейса можно получить интерфейс подчиненного объекта ЛСК ILocalCSObject.
    """

    @property
    def Angle1(self) -> float:  # iarc3d_angle1.html
        """
        ## Angle1 - Начальный угол дуги 3D

        Тип данных: double

        ### Синтаксис Automation

        ```
        Angle1 = Object.Angle1       # Получить свойство (* )
        Object.Angle1 = Angle1       # Установить свойство (* )
        Angle1 = Object.GetAngle1()  # Получить свойство (**)
        Object.SetAngle1( Angle1 )   # Установить свойство (**)
        ```
        """
        ...
    @Angle1.setter
    def Angle1(self, value: float): ...

    @property
    def Angle2(self) -> float:  # iarc3d_angle2.html
        """
        ## Angle2 - Конечный угол дуги 3D

        Тип данных: double

        ### Синтаксис Automation

        ```
        Angle2 = Object.Angle2       # Получить свойство (* )
        Object.Angle2 = Angle2       # Установить свойство (* )
        Angle2 = Object.GetAngle2()  # Получить свойство (**)
        Object.SetAngle2( Angle2 )   # Установить свойство (**)
        ```
        """
        ...
    @Angle2.setter
    def Angle2(self, value: float): ...

    @property
    def BuildingObject(self) -> IModelObject:  # iarc3d_buildingobject.html
        """
        ## BuildingObject - Вспомогательный объект для построения

        Тип данных: указатель на интерфейс IModelObject

        ### Синтаксис Automation

        ```
        BuildingObject = Object.BuildingObject       # Получить свойство (* )
        Object.BuildingObject = BuildingObject       # Установить свойство (* )
        BuildingObject = Object.GetBuildingObject()  # Получить свойство (**)
        Object.SetBuildingObject( BuildingObject )   # Установить свойство (**)
        ```
        """
        ...
    @BuildingObject.setter
    def BuildingObject(self, value: IModelObject): ...

    @property
    def BuildingType(self) -> int:  # iarc3d_buildingtype.html
        """
        ## BuildingType - Способ построения дуги 3D

        Тип данных: из перечисления ksArc3DBuildingTypeEnum

        ### Синтаксис Automation

        ```
        BuildingType = Object.BuildingType       # Получить свойство (* )
        Object.BuildingType = BuildingType       # Установить свойство (* )
        BuildingType = Object.GetBuildingType()  # Получить свойство (**)
        Object.SetBuildingType( BuildingType )   # Установить свойство (**)
        ```
        """
        ...
    @BuildingType.setter
    def BuildingType(self, value: int): ...

    @property
    def BuildingVectorParameters(self) -> IVector3D:  # iarc3d_buildingvectorparameters.html
        """
        ## BuildingVectorParameters - Параметры вектора

        Тип данных: указатель на интерфейс IVector3D

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        BuildingVectorParameters = Object.BuildingVectorParameters       # Получить свойство (* )
        BuildingVectorParameters = Object.GetBuildingVectorParameters()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def Closed(self) -> bool:  # iarc3d_closed.html
        """
        ## Closed - Признак замкнутости дуги

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        Closed = Object.Closed       # Получить свойство (* )
        Object.Closed = Closed       # Установить свойство (* )
        Closed = Object.GetClosed()  # Получить свойство (**)
        Object.SetClosed( Closed )   # Установить свойство (**)
        ```
        """
        ...
    @Closed.setter
    def Closed(self, value: bool): ...

    @property
    def Direction(self) -> bool:  # iarc3d_direction.html
        """
        ## Direction - Направление построения дуги

        Тип данных: BOOL

        Значения свойства:

        +------------------------------+
        | TRUE  | - дуга по умолчанию, |
        +------------------------------+
        | FALSE | - дополняющая дуга.  |
        +------------------------------+

        ### Синтаксис Automation

        ```
        Direction = Object.Direction       # Получить свойство (* )
        Object.Direction = Direction       # Установить свойство (* )
        Direction = Object.GetDirection()  # Получить свойство (**)
        Object.SetDirection( Direction )   # Установить свойство (**)
        ```
        """
        ...
    @Direction.setter
    def Direction(self, value: bool): ...

    @property
    def DirectionPointIndex(self) -> int:  # iarc3d_directionpointindex.html
        """
        ## DirectionPointIndex - Индекс вершины, для которой задано направление

        Тип данных: из перечисления ksArc3DParameterEnum

        ### Синтаксис Automation

        ```
        DirectionPointIndex = Object.DirectionPointIndex       # Получить свойство (* )
        Object.DirectionPointIndex = DirectionPointIndex       # Установить свойство (* )
        DirectionPointIndex = Object.GetDirectionPointIndex()  # Получить свойство (**)
        Object.SetDirectionPointIndex( DirectionPointIndex )   # Установить свойство (**)
        ```
        """
        ...
    @DirectionPointIndex.setter
    def DirectionPointIndex(self, value: int): ...

    @property
    def Radius(self) -> float:  # iarc3d_radius.html
        """
        ## Radius - Радиус дуги 3D

        Тип данных: double

        ### Синтаксис Automation

        ```
        Radius = Object.Radius       # Получить свойство (* )
        Object.Radius = Radius       # Установить свойство (* )
        Radius = Object.GetRadius()  # Получить свойство (**)
        Object.SetRadius( Radius )   # Установить свойство (**)
        ```
        """
        ...
    @Radius.setter
    def Radius(self, value: float): ...

    def AssociationObject(self, Index) -> IModelObject:  # iarc3d_associationobject.html
        """
        ## AssociationObject - Установить опорный объект для вершины

        Тип данных: указатель на интерфейс IModelObject

        ### Синтаксис Automation

        ```
        AssociationObject = Object.AssociationObject( Index )     # Получить свойство (* )
        Object.AssociationObject( Index ) = AssociationObject     # Установить свойство (* )
        AssociationObject = Object.GetAssociationObject( Index )  # Получить свойство (**)
        Object.SetAssociationObject( Index, AssociationObject )   # Установить свойство (**)
        ```

        ### Входные параметры

        Index - индекс вершины из перечисления ksArc3DParameterEnum

        Свойство позволяет связать вершину дуги с уже имеющимся точечным объектом.
        """
        ...

    def GetPoint(self, Index) -> bool:  # iarc3d_getpoint.html
        """
        ## GetPoint - Получить координаты точки дуги 3D

        ### Синтаксис Automation

        ```
        BOOL GetPoint( ksArc3DParameterEnum Index, double * X, double * Y, double * Z );
        ```

        ### Входные параметры

        Index - индекс вершины из перечисления ksArc3DParameterEnum.

        ### Выходные параметры

        X, Y, Z - координаты точки.

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def PointParameters(self, Index):
        ...

    def PointType(self, Index):
        ...

    def SetAssociationObject(self, Index, arg1):
        ...

    def SetPoint(self, Index, X, Y, Z) -> bool:  # iarc3d_setpoint.html
        """
        ## SetPoint - Установить координаты точки дуги 3D

        ### Синтаксис Automation

        ```
        BOOL SetPoint( ksArc3DParameterEnum Index, double X, double Y, double Z );
        ```

        ### Входные параметры

        +------------------------------------------------------------------+
        | Index   | - индекс вершины из перечисления ksArc3DParameterEnum, |
        +------------------------------------------------------------------+
        | X, Y, Z | - координаты точки.                                    |
        +------------------------------------------------------------------+

        Метод позволяет также задавать углы и радиус, которые будут построены путем проецирования точки на дугу.

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def SetPointType(self, Index, arg1):
        ...




class IAxis3D(IModelObject):  # iaxis3d.html
    """
    ## Интерфейс IAxis3D

    Вспомогательная ось 3D.
    """

    @property
    def MathCurve(self) -> IMathCurve3D:  # iaxis3d_mathcurve.html
        """
        ## MathCurve - Получить интерфейс математической кривой

        Тип данных: указатель на интерфейс IMathCurve3D

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        MathCurve = Object.MathCurve       # Получить свойство (* )
        MathCurve = Object.GetMathCurve()  # Получить свойство (**)
        ```
        """
        ...




class IAxisLine3D(IAxis3D):  # iaxisline3d.html
    """
    ## Интерфейс IAxisLine3D

    Осевая линия 3D.
    """

    @property
    def Object1(self) -> IModelObject:  # iaxisline3d_object1.html
        """
        ## Object1 - Вершина 1

        Тип данных: Указатель на интерфейс IModelObject

        ### Синтаксис Automation

        ```
        Object1 = Object.Object1       # Получить свойство (* )
        Object.Object1 = Object1       # Установить свойство (* )
        Object1 = Object.GetObject1()  # Получить свойство (**)
        Object.SetObject1( Object1 )   # Установить свойство (**)
        ```
        """
        ...
    @Object1.setter
    def Object1(self, value: IModelObject): ...

    @property
    def Object2(self) -> IModelObject:  # iaxisline3d_object2.html
        """
        ## Object2 - Вершина 2

        Тип данных: Указатель на интерфейс IModelObject

        ### Синтаксис Automation

        ```
        Object2 = Object.Object2       # Получить свойство (* )
        Object.Object2 = Object2       # Установить свойство (* )
        Object2 = Object.GetObject2()  # Получить свойство (**)
        Object.SetObject2( Object2 )   # Установить свойство (**)
        ```
        """
        ...
    @Object2.setter
    def Object2(self, value: IModelObject): ...




class IAxis3DBy2Points(IAxis3D):  # iaxis3dby2points.html
    """
    ## Интерфейс IAxis3DBy2Points

    Ось через две вершины.
    """

    @property
    def Point1(self) -> IModelObject:  # iaxis3dby2points_point1.html
        """
        ## Point1 - Вершина 1

        Тип данных: Указатель на интерфейс IModelObject

        ### Синтаксис Automation

        ```
        Point1 = Object.Point1       # Получить свойство (* )
        Object.Point1 = Point1       # Установить свойство (* )
        Point1 = Object.GetPoint1()  # Получить свойство (**)
        Object.SetPoint1( Point1 )   # Установить свойство (**)
        ```
        """
        ...
    @Point1.setter
    def Point1(self, value: IModelObject): ...

    @property
    def Point2(self) -> IModelObject:  # iaxis3dby2points_point2.html
        """
        ## Point2 - Вершина 1

        Тип данных: Указатель на интерфейс IModelObject

        ### Синтаксис Automation

        ```
        Point2 = Object.Point2       # Получить свойство (* )
        Object.Point2 = Point2       # Установить свойство (* )
        Point1 = Object.GetPoint2()  # Получить свойство (**)
        Object.SetPoint2( Point2 )   # Установить свойство (**)
        ```
        """
        ...
    @Point2.setter
    def Point2(self, value: IModelObject): ...




class IAxis3DBy2Planes(IAxis3D):  # iaxis3dby2planes.html
    """
    ## Интерфейс IAxis3DBy2Planes

    Ось на пересечении плоскостей.
    """

    @property
    def Plane1(self) -> IModelObject:  # iaxis3dby2planes_plane1.html
        """
        ## Plane1 - Плоскость 1

        Тип данных: Указатель на интерфейс IModelObject

        ### Синтаксис Automation

        ```
        Plane1 = Object.Plane1       # Получить свойство (* )
        Object.Plane1 = Plane1       # Установить свойство (* )
        Plane1 = Object.GetPlane1()  # Получить свойство (**)
        Object.SetPlane1( Plane1 )   # Установить свойство (**)
        ```
        """
        ...
    @Plane1.setter
    def Plane1(self, value: IModelObject): ...

    @property
    def Plane2(self) -> IModelObject:  # iaxis3dby2planes_plane2.html
        """
        ## Plane2 - Плоскость 2

        Тип данных: Указатель на интерфейс IModelObject

        ### Синтаксис Automation

        ```
        Plane2 = Object.Plane2       # Получить свойство (* )
        Object.Plane2 = Plane2       # Установить свойство (* )
        Plane2 = Object.GetPlane2()  # Получить свойство (**)
        Object.SetPlane2( Plane2 )   # Установить свойство (**)
        ```
        """
        ...
    @Plane2.setter
    def Plane2(self, value: IModelObject): ...




class IAxis3DByConeface(IAxis3D):  # iaxis3dbyconeface.html
    """
    ## Интерфейс IAxis3DByConeface

    Ось конической поверхности.
    """

    @property
    def Face(self) -> IModelObject:  # iaxis3dbyconeface_face.html
        """
        ## Face - Поверхность

        Тип данных: Указатель на интерфейс IModelObject

        ### Синтаксис Automation

        ```
        Face = Object.Face       # Получить свойство (* )
        Object.Face = Face       # Установить свойство (* )
        Face = Object.GetFace()  # Получить свойство (**)
        Object.SetFace( Face )   # Установить свойство (**)
        ```
        """
        ...
    @Face.setter
    def Face(self, value: IModelObject): ...




class IAxis3DByEdge(IAxis3D):  # iaxis3dbyedge.html
    """
    ## Интерфейс IAxis3DByEdge

    Ось через ребро.
    """

    @property
    def Edge(self) -> IModelObject:  # iaxis3dbyedge_edge.html
        """
        ## Edge - Ребро

        Тип данных: Указатель на интерфейс IModelObject

        ### Синтаксис Automation

        ```
        Edge = Object.Edge       # Получить свойство (* )
        Object.Edge = Edge       # Установить свойство (* )
        Edge = Object.GetEdge()  # Получить свойство (**)
        Object.SetEdge( Edge )   # Установить свойство (**)
        ```
        """
        ...
    @Edge.setter
    def Edge(self, value: IModelObject): ...




class IAxis3DByPointAndObject(IAxis3D):  # iaxis3dbypointandobject.html
    """
    ## Интерфейс IAxis3DByPointAndObject

    Ось через вершину по объекту.
    """

    @property
    def DirectObject(self) -> IModelObject:  # iaxis3dbypointandobject_directobject.html
        """
        ## DirectObject - Направляющий объект

        Тип данных: Указатель на интерфейс IModelObject

        ### Синтаксис Automation

        ```
        DirectObject = Object.DirectObject       # Получить свойство (* )
        Object.DirectObject = DirectObject       # Установить свойство (* )
        DirectObject = Object.GetDirectObject()  # Получить свойство (**)
        Object.SetDirectObject( DirectObject )   # Установить свойство (**)
        ```
        """
        ...
    @DirectObject.setter
    def DirectObject(self, value: IModelObject): ...

    @property
    def Parallel(self) -> bool:  # iaxis3dbypointandobject_parallel.html
        """
        ## Parallel - Ориентация. TRUE - параллельно направляющему объекту. FALSE - перпендикулярно направляющему объекту

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        Parallel = Object.Parallel       # Получить свойство (* )
        Object.Parallel = Parallel       # Установить свойство (* )
        Parallel = Object.GetParallel()  # Получить свойство (**)
        Object.SetParallel( Parallel )   # Установить свойство (**)
        ```
        """
        ...
    @Parallel.setter
    def Parallel(self, value: bool): ...

    @property
    def Point(self) -> IModelObject:  # iaxis3dbypointandobject_point.html
        """
        ## Point - Точка

        Тип данных: Указатель на интерфейс IModelObject

        ### Синтаксис Automation

        ```
        Point = Object.Point       # Получить свойство (* )
        Object.Point = Point       # Установить свойство (* )
        Point = Object.GetPoint()  # Получить свойство (**)
        Object.SetPoint( Point )   # Установить свойство (**)
        ```
        """
        ...
    @Point.setter
    def Point(self, value: IModelObject): ...

    @property
    def Vector3D(self) -> IVector3D:  # iaxis3dbypointandobject_vector3d.html
        """
        ## Vector3D - Параметры вектора

        Тип данных: Указатель на интерфейс IVector3D

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        Vector3D = Object.Vector3D       # Получить свойство (* )
        Vector3D = Object.GetVector3D()  # Получить свойство (**)
        ```
        """
        ...




class IAxis3DByOperation(IAxis3D):  # iaxis3dbyoperation.html
    """
    ## Интерфейс IAxis3DByOperation

    Ось через вершину по объекту.
    """

    @property
    def Operation(self) -> IModelObject:  # iaxis3dbyoperation_operation.html
        """
        ## Operation - Операция

        Тип данных: Указатель на интерфейс IModelObject

        ### Синтаксис Automation

        ```
        Operation = Object.Operation       # Получить свойство (* )
        Object.Operation = Operation       # Установить свойство (* )
        Operation = Object.GetOperation()  # Получить свойство (**)
        Object.SetOperation( Operation )   # Установить свойство (**)
        ```
        """
        ...
    @Operation.setter
    def Operation(self, value: IModelObject): ...




class IBoolean(IModelObject):  # iboolean.html
    """
    ## Интерфейс IBoolean

    Интерфейс булевой операции.
    """

    @property
    def BaseObject(self) -> IKompasAPIObject:  # iboolean_baseobject.html
        """
        ## BaseObject - Базовый объект

        Тип данных: Указатель на интерфейс IKompasAPIObject

        ### Синтаксис Automation

        ```
        BaseObject = Object.BaseObject       # Получить свойство (* )
        Object.BaseObject = BaseObject       # Установить свойство (* )
        BaseObject = Object.GetBaseObject()  # Получить свойство (**)
        Object.SetBaseObject( BaseObject )   # Установить свойство (**)
        ```
        """
        ...
    @BaseObject.setter
    def BaseObject(self, value: IKompasAPIObject): ...

    @property
    def Bodies(self) -> typing.Any:  # iboolean_bodies.html
        """
        ## Bodies - Массив тел

        Тип данных: VARIANT

        Позволяет получать и устанавливать массив тел, участвующих в операции.

        Массив возвращается в виде массива SAFEARRAY тел LPDISPATCH (VT_ARRAY | VT_DISPATCH).

        ### Синтаксис Automation

        ```
        Bodies = Object.Bodies       # Получить свойство (* )
        Object.Bodies = Bodies       # Установить свойство (* )
        Bodies = Object.GetBodies()  # Получить свойство (**)
        Object.SetBodies( Bodies )   # Установить свойство (**)
        ```
        """
        ...
    @Bodies.setter
    def Bodies(self, value: typing.Any): ...

    @property
    def BooleanType(self) -> int:  # iboolean_booleantype.html
        """
        ## BooleanType - Тип операции над телами

        Тип данных: Значение из перечисления ksBooleanType.

        ### Синтаксис Automation

        ```
        BooleanType = Object.BooleanType       # Получить свойство (* )
        Object.BooleanType = BooleanType       # Установить свойство (* )
        BooleanType = Object.GetBooleanType()  # Получить свойство (**)
        Object.SetBooleanType( BooleanType )   # Установить свойство (**)
        ```
        """
        ...
    @BooleanType.setter
    def BooleanType(self, value: int): ...

    @property
    def ModifyObjects(self) -> typing.Any:  # iboolean_modifyobjects.html
        """
        ## ModifyObjects - Модифицирующие объекты

        Тип данных: VARIANT

        ### Синтаксис Automation

        ```
        ModifyObjects = Object.ModifyObjects       # Получить свойство (* )
        Object.ModifyObjects = ModifyObjects       # Установить свойство (* )
        ModifyObjects = Object.GetModifyObjects()  # Получить свойство (**)
        Object.SetModifyObjects( ModifyObjects )   # Установить свойство (**)
        ```
        """
        ...
    @ModifyObjects.setter
    def ModifyObjects(self, value: typing.Any): ...

    @property
    def SaveCopyBaseObject(self) -> bool:  # iboolean_savecopybaseobject.html
        """
        ## SaveCopyBaseObject - Сохранять копию базового объекта

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        SaveCopyBaseObject = Object.SaveCopyBaseObject       # Получить свойство (* )
        Object.SaveCopyBaseObject = SaveCopyBaseObject       # Установить свойство (* )
        SaveCopyBaseObject = Object.GetSaveCopyBaseObject()  # Получить свойство (**)
        Object.SetSaveCopyBaseObject( SaveCopyBaseObject )   # Установить свойство (**)
        ```
        """
        ...
    @SaveCopyBaseObject.setter
    def SaveCopyBaseObject(self, value: bool): ...

    @property
    def SaveCopyModifyObjects(self) -> bool:  # iboolean_savecopymodifyobjects.html
        """
        ## SaveCopyModifyObjects - Сохранять копии модифицирующих объектов

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        SaveCopyModifyObjects = Object.SaveCopyModifyObjects       # Получить свойство (* )
        Object.SaveCopyModifyObjects = SaveCopyModifyObjects       # Установить свойство (* )
        SaveCopyModifyObjects = Object.GetSaveCopyModifyObjects()  # Получить свойство (**)
        Object.SetSaveCopyModifyObjects( SaveCopyModifyObjects )   # Установить свойство (**)
        ```
        """
        ...
    @SaveCopyModifyObjects.setter
    def SaveCopyModifyObjects(self, value: bool): ...




class ICollectionGeometry(IModelObject):  # icollectiongeometry.html
    """
    ## Интерфейс ICollectionGeometry

    Коллекция геометрии.
    """

    @property
    def Geometry(self) -> typing.Any:  # icollectiongeometry_geometry.html
        """
        ## Geometry - Коллекция геометрии

        Тип данных: VARIANT

        ### Синтаксис Automation

        ```
        Geometry = Object.Geometry       # Получить свойство (* )
        Object.Geometry = Geometry       # Установить свойство (* )
        Geometry = Object.GetGeometry()  # Получить свойство (**)
        Object.SetGeometry( Geometry )   # Установить свойство (**)
        ```
        """
        ...
    @Geometry.setter
    def Geometry(self, value: typing.Any): ...




class IConicSpiral3D(ISpiral3D):  # iconicspiral3d.html
    """
    ## Интерфейс IConicSpiral3D

    Коническая спираль.

    Интерфейс можно получить у коллекции спиралей с помощью свойства ISpirals3D::Spiral3D и метода ISpirals3D::Add
    """

    @property
    def CoordinateSystem(self) -> IModelObject:  # iconicspiral3d_coordinatesystem.html
        """
        ## CoordinateSystem - СК объекта

        Тип данных: указатель на интерфейс IModelObject

        ### Синтаксис Automation

        ```
        CoordinateSystem = Object.CoordinateSystem       # Получить свойство (* )
        Object.CoordinateSystem = CoordinateSystem       # Установить свойство (* )
        CoordinateSystem = Object.GetCoordinateSystem()  # Получить свойство (**)
        Object.SetCoordinateSystem( CoordinateSystem )   # Установить свойство (**)
        ```
        """
        ...
    @CoordinateSystem.setter
    def CoordinateSystem(self, value: IModelObject): ...

    @property
    def Diameter1(self) -> float:  # iconicspiral3d_diameter1.html
        """
        ## Diameter1 - Начальный диаметр

        Тип данных: double

        Свойство позволяет устанавливать и получать начальный диаметр спирали.

        ### Синтаксис Automation

        ```
        Diameter1 = Object.Diameter1       # Получить свойство (* )
        Object.Diameter1 = Diameter1       # Установить свойство (* )
        Diameter1 = Object.GetDiameter1()  # Получить свойство (**)
        Object.SetDiameter1( Diameter1 )   # Установить свойство (**)
        ```
        """
        ...
    @Diameter1.setter
    def Diameter1(self, value: float): ...

    @property
    def Diameter2(self) -> float:  # iconicspiral3d_diameter2.html
        """
        ## Diameter2 - Конечный диаметр

        Тип данных: double

        Свойство позволяет устанавливать и получать конечный диаметр спирали.

        ### Синтаксис Automation

        ```
        Diameter2 = Object.Diameter2       # Получить свойство (* )
        Object.Diameter2 = Diameter2       # Установить свойство (* )
        Diameter2 = Object.GetDiameter2()  # Получить свойство (**)
        Object.SetDiameter2( Diameter2 )   # Установить свойство (**)
        ```
        """
        ...
    @Diameter2.setter
    def Diameter2(self, value: float): ...

    @property
    def DiameterBaseObject1(self) -> IModelObject:  # iconicspiral3d_diameterbaseobject1.html
        """
        ## DiameterBaseObject1 - Объект, задающий начальный диаметр

        Тип данных: указатель на интерфейс IModelObject

        Свойство позволяет устанавливать и получать указатель на объект, задающий начальный диаметр спирали.

        ### Синтаксис Automation

        ```
        DiameterBaseObject1 = Object.DiameterBaseObject1       # Получить свойство (* )
        Object.DiameterBaseObject1 = DiameterBaseObject1       # Установить свойство (* )
        DiameterBaseObject1 = Object.GetDiameterBaseObject1()  # Получить свойство (**)
        Object.SetDiameterBaseObject1( DiameterBaseObject1 )   # Установить свойство (**)
        ```
        """
        ...
    @DiameterBaseObject1.setter
    def DiameterBaseObject1(self, value: IModelObject): ...

    @property
    def DiameterBaseObject2(self) -> IModelObject:  # iconicspiral3d_diameterbaseobject2.html
        """
        ## DiameterBaseObject2 - Объект, задающий конечный диаметр

        Тип данных: указатель на интерфейс IModelObject

        Свойство позволяет устанавливать и получать указатель на объект, задающий конечный диаметр спирали.

        ### Синтаксис Automation

        ```
        DiameterBaseObject2 = Object.DiameterBaseObject2      # Получить свойство (* )
        Object.DiameterBaseObject2 = DiameterBaseObject2      # Установить свойство (* )
        DiameterBaseObject2= Object.GetDiameterBaseObject2()  # Получить свойство (**)
        Object.SetDiameterBaseObject2( DiameterBaseObject2 )  # Установить свойство (**)
        ```
        """
        ...
    @DiameterBaseObject2.setter
    def DiameterBaseObject2(self, value: IModelObject): ...

    @property
    def DiameterType1(self) -> int:  # iconicspiral3d_diametertype1.html
        """
        ## DiameterType1 - Способ задания начального диаметра

        Тип данных: из перечисления ksSpline3DDiameterTypeEnum

        Свойство позволяет устанавливать и получать способ задания диаметра.

        Для начального диаметра способ "По наклону образующей" е доступен.

        ### Синтаксис Automation

        ```
        DiameterType1 = Object.DiameterType1       # Получить свойство (* )
        Object.DiameterType1 = DiameterType1       # Установить свойство (* )
        DiameterType1 = Object.GetDiameterType1()  # Получить свойство (**)
        Object.SetDiameterType1( DiameterType1 )   # Установить свойство (**)
        ```
        """
        ...
    @DiameterType1.setter
    def DiameterType1(self, value: int): ...

    @property
    def DiameterType2(self) -> int:  # iconicspiral3d_diametertype2.html
        """
        ## DiameterType2 - Способ задания конечного диаметра

        Тип данных: из перечисления ksSpline3DDiameterTypeEnum

        Свойство позволяет устанавливать и получать способ задания конечного диаметра спирали.

        ### Синтаксис Automation

        ```
        DiameterType2 = Object.DiameterType2       # Получить свойство (* )
        Object.DiameterType2 = DiameterType2       # Установить свойство (* )
        DiameterType2 = Object.GetDiameterType2()  # Получить свойство (**)
        Object.SetDiameterType2( DiameterType2 )   # Установить свойство (**)
        ```
        """
        ...
    @DiameterType2.setter
    def DiameterType2(self, value: int): ...

    @property
    def GeneratrixTiltAngle(self) -> float:  # iconicspiral3d_generatrixtiltangle.html
        """
        ## GeneratrixTiltAngle - Угол наклона образующей

        Тип данных: double

        Свойство позволяет устанавливать и получать угол наклона образующей.

        ### Синтаксис Automation

        ```
        GeneratrixTiltAngle = Object.GeneratrixTiltAngle       # Получить свойство (* )
        Object.GeneratrixTiltAngle = GeneratrixTiltAngle       # Установить свойство (* )
        GeneratrixTiltAngle = Object.GetGeneratrixTiltAngle()  # Получить свойство (**)
        Object.SetGeneratrixTiltAngle( GeneratrixTiltAngle )   # Установить свойство (**)
        ```
        """
        ...
    @GeneratrixTiltAngle.setter
    def GeneratrixTiltAngle(self, value: float): ...

    @property
    def GeneratrixTiltAngleHow(self) -> bool:  # iconicspiral3d_generatrixtiltanglehow.html
        """
        ## GeneratrixTiltAngleHow - Направление отсчета угла наклона образующей

        Тип данных: BOOL

        Значения свойства:

        +-----------------------------------+
        | TRUE  | - наружу,                 |
        +-----------------------------------+
        | FALSE | - внутрь (к оси спирали). |
        +-----------------------------------+

        Свойство позволяет устанавливать и получать направление отсчета угла наклона образующей.

        ### Синтаксис Automation

        ```
        GeneratrixTiltAngleHow = Object.GeneratrixTiltAngleHow       # Получить свойство (* )
        Object.GeneratrixTiltAngleHow = GeneratrixTiltAngleHow       # Установить свойство (* )
        GeneratrixTiltAngleHow = Object.GetGeneratrixTiltAngleHow()  # Получить свойство (**)
        Object.SetGeneratrixTiltAngleHow( GeneratrixTiltAngleHow )   # Установить свойство (**)
        ```
        """
        ...
    @GeneratrixTiltAngleHow.setter
    def GeneratrixTiltAngleHow(self, value: bool): ...

    @property
    def HeightCorrectionType(self) -> bool:  # iconicspiral3d_heightcorrectiontype.html
        """
        ## HeightCorrectionType - Признак дополнительной высоты. TRUE - продолжить за объект, FALSE - не доходя до объекта

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        HeightCorrectionType = Object.HeightCorrectionType       # Получить свойство (* )
        Object.HeightCorrectionType = HeightCorrectionType       # Установить свойство (* )
        HeightCorrectionType = Object.GetHeightCorrectionType()  # Получить свойство (**)
        Object.SetHeightCorrectionType( HeightCorrectionType )   # Установить свойство (**)
        ```
        """
        ...
    @HeightCorrectionType.setter
    def HeightCorrectionType(self, value: bool): ...




class IConnectCurve(IModelObject):  # iconnectcurve.html
    """
    ## Интерфейс IConnectCurve

    Интерфейс операции соединения кривых.
    """

    @property
    def Curve1(self) -> IModelObject:  # iconnectcurve_curve1.html
        """
        ## Curve1 - Кривая 1 – первая из соединяемых кривых

        Тип данных: указатель на интерфейс IModelObject

        ### Синтаксис Automation

        ```
        Curve1 = Object.Curve1       # Получить свойство (* )
        Object.Curve1 = Curve1       # Установить свойство (* )
        Curve1 = Object.GetCurve1()  # Получить свойство (**)
        Object.SetCurve1( Curve1 )   # Установить свойство (**)
        ```
        """
        ...
    @Curve1.setter
    def Curve1(self, value: IModelObject): ...

    @property
    def Curve1ConnectType(self) -> int:  # iconnectcurve_curve1connecttype.html
        """
        ## Curve1ConnectType - Условие сопряжения соединяющей кривой с первой из соединяемых кривых

        Тип данных: из перечисления ksConnectTypeEnum

        ### Синтаксис Automation

        ```
        Curve1ConnectType = Object.Curve1ConnectType       # Получить свойство (* )
        Object.Curve1ConnectType = Curve1ConnectType       # Установить свойство (* )
        Curve1ConnectType = Object.GetCurve1ConnectType()  # Получить свойство (**)
        Object.SetCurve1ConnectType( Curve1ConnectType )   # Установить свойство (**)
        ```
        """
        ...
    @Curve1ConnectType.setter
    def Curve1ConnectType(self, value: int): ...

    @property
    def Curve1ConnectVertex(self) -> bool:  # iconnectcurve_curve1connectvertex.html
        """
        ## Curve1ConnectVertex - Вершина сопряжения первой из соединяемых кривых

        Тип данных: BOOL

        Значения свойства:

        +----------------------------+
        | TRUE  | - начальная точка, |
        +----------------------------+
        | FALSE | - конечная точка.  |
        +----------------------------+

        ### Синтаксис Automation

        ```
        Curve1ConnectVertex = Object.Curve1ConnectVertex       # Получить свойство (* )
        Object.Curve1ConnectVertex = Curve1ConnectVertex       # Установить свойство (* )
        Curve1ConnectVertex = Object.GetCurve1ConnectVertex()  # Получить свойство (**)
        Object.SetCurve1ConnectVertex( Curve1ConnectVertex )   # Установить свойство (**)
        ```
        """
        ...
    @Curve1ConnectVertex.setter
    def Curve1ConnectVertex(self, value: bool): ...

    @property
    def Curve2(self) -> IModelObject:  # iconnectcurve_curve2.html
        """
        ## Curve2 - Кривая 2 – вторая из соединяемых кривых

        Тип данных: указатель на интерфейс IModelObject

        ### Синтаксис Automation

        ```
        Curve2 = Object.Curve2       # Получить свойство (* )
        Object.Curve2 = Curve2       # Установить свойство (* )
        Curve2 = Object.GetCurve2()  # Получить свойство (**)
        Object.SetCurve2( Curve2 )   # Установить свойство (**)
        ```
        """
        ...
    @Curve2.setter
    def Curve2(self, value: IModelObject): ...

    @property
    def Curve2ConnectType(self) -> int:  # iconnectcurve_curve2connecttype.html
        """
        ## Curve2ConnectType- Условие сопряжения соединяющей кривой со второй из соединяемых кривых

        Тип данных: из перечисления ksConnectTypeEnum.

        ### Синтаксис Automation

        ```
        Curve2ConnectType = Object.Curve2ConnectType       # Получить свойство (* )
        Object.Curve2ConnectType = Curve2ConnectType       # Установить свойство (* )
        Curve2ConnectType = Object.GetCurve2ConnectType()  # Получить свойство (**)
        Object.SetCurve2ConnectType( Curve2ConnectType )   # Установить свойство (**)
        ```
        """
        ...
    @Curve2ConnectType.setter
    def Curve2ConnectType(self, value: int): ...

    @property
    def Curve2ConnectVertex(self) -> bool:  # iconnectcurve_curve2connectvertex.html
        """
        ## Curve2ConnectVertex - Вершина сопряжения второйиз соединяемых кривых

        Тип данных: BOOL

        Значения свойства:

        +----------------------------+
        | TRUE  | - начальная точка, |
        +----------------------------+
        | FALSE | - конечная точка.  |
        +----------------------------+

        ### Синтаксис Automation

        ```
        Curve2ConnectVertex = Object.Curve2ConnectVertex       # Получить свойство (* )
        Object.Curve2ConnectVertex = Curve2ConnectVertex       # Установить свойство (* )
        Curve2ConnectVertex = Object.GetCurve2ConnectVertex()  # Получить свойство (**)
        Object.SetCurve2ConnectVertex( Curve2ConnectVertex )   # Установить свойство (**)
        ```
        """
        ...
    @Curve2ConnectVertex.setter
    def Curve2ConnectVertex(self, value: bool): ...

    @property
    def Tension(self) -> float:  # iconnectcurve_tension.html
        """
        ## Tension - Натяжение соединяющей кривой (%)

        Тип данных: double

        ### Синтаксис Automation

        ```
        Tension = Object.Tension       # Получить свойство (* )
        Object.Tension = Tension       # Установить свойство (* )
        Tension = Object.GetTension()  # Получить свойство (**)
        Object.SetTension( Tension )   # Установить свойство (**)
        ```
        """
        ...
    @Tension.setter
    def Tension(self, value: float): ...




class IContour3D(IModelObject):  # icontour3d.html
    """
    ## Интерфейс IContour3D

    Контур 3D

    Интерфейс можно получить у коллекции контуров 3D с помощью свойства IContours3D::Contour3D и метода IContours3D::Add.
    """

    @property
    def BuildingType(self) -> int:  # icontour3d_buildingtype.html
        """
        ## BuildingType - Тип построения Контура 3D

        Тип данных: Тип построения Контура из перечисления ksContour3DBuildingTypeTypeEnum.

        ### Синтаксис Automation

        ```
        BuildingType = Object.BuildingType       # Получить свойство (* )
        Object.BuildingType = BuildingType       # Установить свойство (* )
        BuildingType = Object.GetBuildingType()  # Получить свойство (**)
        Object.SetBuildingType( BuildingType )   # Установить свойство (**)
        ```
        """
        ...
    @BuildingType.setter
    def BuildingType(self, value: int): ...

    @property
    def Contour3DType(self) -> int:  # icontour3d_contour3dtype.html
        """
        ## Contour3DType - Тип контура

        Тип данных: из перечисления ksContour3DTypeEnum

        ### Синтаксис Automation

        ```
        Contour3DType = Object.Contour3DType       # Получить свойство (* )
        Object.Contour3DType = Contour3DType       # Установить свойство (* )
        Contour3DType = Object.GetContour3DType()  # Получить свойство (**)
        Object.SetContour3DType( Contour3DType )   # Установить свойство (**)
        ```
        """
        ...
    @Contour3DType.setter
    def Contour3DType(self, value: int): ...

    @property
    def CutMode(self) -> int:  # icontour3d_cutmode.html
        """
        ## CutMode - Обход углов

        Тип данных: из перечисления ksEquidistant3DCutModeEnum

        ### Синтаксис Automation

        ```
        CutMode = Object.CutMode       # Получить свойство (* )
        Object.CutMode = CutMode       # Установить свойство (* )
        CutMode = Object.GetCutMode()  # Получить свойство (**)
        Object.SetCutMode( CutMode )   # Установить свойство (**)
        ```
        """
        ...
    @CutMode.setter
    def CutMode(self, value: int): ...

    @property
    def Edges(self) -> typing.Any:  # icontour3d_edges.html
        """
        ## Edges - Список объектов

        Тип данных: VARIANT

        Свойство позволяет устанавливать и получать массив VT_ARRAY | VT_DISPATCH ребер, составляющих контур.

        ### Синтаксис Automation

        ```
        Edges = Object.Edges       # Получить свойство (* )
        Object.Edges = Edges       # Установить свойство (* )
        Edges = Object.GetEdges()  # Получить свойство (**)
        Object.SetEdges( Edges )   # Установить свойство (**)
        ```
        """
        ...
    @Edges.setter
    def Edges(self, value: typing.Any): ...

    @property
    def EdgesCount(self) -> int:  # icontour3d_edgescount.html
        """
        ## EdgesCount - Количество объектов в контуре

        Тип данных: long

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        EdgesCount = Object.EdgesCount       # Получить свойство (* )
        EdgesCount = Object.GetEdgesCount()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def EdgesVisibility(self) -> bool:  # icontour3d_edgesvisibility.html
        """
        ## EdgesVisibility - Видимость исходных объектов в Дереве модели

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        EdgesVisibility = Object.EdgesVisibility       # Получить свойство (* )
        Object.EdgesVisibility = EdgesVisibility       # Установить свойство (* )
        EdgesVisibility = Object.GetEdgesVisibility()  # Получить свойство (**)
        Object.SetEdgesVisibility( EdgesVisibility )   # Установить свойство (**)
        ```
        """
        ...
    @EdgesVisibility.setter
    def EdgesVisibility(self, value: bool): ...

    @property
    def Equidistant(self) -> IEquidistant3D:  # icontour3d_equidistant.html
        """
        ## Equidistant - Получить Эквидистанту

        Тип данных: Указатель на интерфейс IEquidistant3D

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        Equidistant = Object.Equidistant       # Получить свойство (* )
        Equidistant = Object.GetEquidistant()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def PointsCount(self) -> int:  # icontour3d_pointscount.html
        """
        ## PointsCount - Количество точек в контуре

        Тип данных:long

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        PointsCount = Object.PointsCount       # Получить свойство (* )
        PointsCount = Object.GetPointsCount()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def Radius(self) -> float:  # icontour3d_radius.html
        """
        ## Radius - Радиус скругления

        Тип данных: double

        ### Синтаксис Automation

        ```
        Radius = Object.Radius       # Получить свойство (* )
        Object.Radius = Radius       # Установить свойство (* )
        Radius = Object.GetRadius()  # Получить свойство (**)
        Object.SetRadius( Radius )   # Установить свойство (**)
        ```
        """
        ...
    @Radius.setter
    def Radius(self, value: float): ...

    def AddEdge(self, Object):
        ...

    def DelVarRadius(self, PointNum) -> bool:  # icontour3d_delvarradius.html
        """
        ## DelVarRadius - Установить цвета фона текстов с Alpha каналом

        ### Синтаксис Automation

        ```
        BOOL DelVarRadius( long PointNum );
        ```

        ### Входные параметры

        +----------------------------+
        | PointNum | - Индекс точки. |
        +----------------------------+

        ### Возвращаемое значение

        +----------------------------+
        | TRUE  | - в случае удачи,  |
        +----------------------------+
        | FALSE | - в случае ошибки. |
        +----------------------------+
        """
        ...

    def GetIntersectionPoint(self, PointNum):
        ...

    def RemoveEdge(self, Index):
        ...

    def SetVarRadius(self, PointNum, arg1):
        ...

    def VarRadius(self, PointNum) -> float:  # icontour3d_varradius.html
        """
        ## VarRadius - Переменный радиус скругления

        Тип данных: double

        ### Синтаксис Automation

        ```
        VarRadius = Object.VarRadius( PointNum )     # Получить свойство (* )
        Object.VarRadius( PointNum ) = VarRadius     # Установить свойство (* )
        VarRadius = Object.GetVarRadius( PointNum )  # Получить свойство (**)
        Object.SetVarRadius( PointNum, VarRadius )   # Установить свойство (**)
        ```

        ### Входные параметры

        +----------------------------+
        | PointNum | - Индекс точки. |
        +----------------------------+
        """
        ...




class ICopyGeometry(IModelObject):  # icopygeometry.html
    """
    ## Интерфейс ICopyGeometry

    Интерфейс копии геометрии
    """

    @property
    def AutoUpdate(self) -> bool:  # icopygeometry_autoupdate.html
        """
        ## AutoUpdate - Автоматическое обновление

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        AutoUpdate = Object.AutoUpdate       # Получить свойство (* )
        Object.AutoUpdate = AutoUpdate       # Установить свойство (* )
        AutoUpdate = Object.GetAutoUpdate()  # Получить свойство (**)
        Object.SetAutoUpdate( AutoUpdate )   # Установить свойство (**)
        ```
        """
        ...
    @AutoUpdate.setter
    def AutoUpdate(self, value: bool): ...

    @property
    def BuildingType(self) -> int:  # icopygeometry_buildingtype.html
        """
        ## BuildingType - Способ построения операции

        Тип данных: из перечисления ksCopyGeometryBuildingTypeEnum.

        ### Синтаксис Automation

        ```
        BuildingType = Object.BuildingType       # Получить свойство (* )
        Object.BuildingType = BuildingType       # Установить свойство (* )
        BuildingType = Object.GetBuildingType()  # Получить свойство (**)
        Object.SetBuildingType( BuildingType )   # Установить свойство (**)
        ```
        """
        ...
    @BuildingType.setter
    def BuildingType(self, value: int): ...

    @property
    def ByCollectionGeometry(self) -> bool:  # icopygeometry_bycollectiongeometry.html
        """
        ## ByCollectionGeometry - Режим копирования коллекции геометрии

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        ByCollectionGeometry = Object.ByCollectionGeometry       # Получить свойство (* )
        Object.ByCollectionGeometry = ByCollectionGeometry       # Установить свойство (* )
        ByCollectionGeometry = Object.GetByCollectionGeometry()  # Получить свойство (**)
        Object.SetByCollectionGeometry( ByCollectionGeometry )   # Установить свойство (**)
        ```
        """
        ...
    @ByCollectionGeometry.setter
    def ByCollectionGeometry(self, value: bool): ...

    @property
    def CollectionGeometry(self) -> ICollectionGeometry:  # icopygeometry_collectiongeometry.html
        """
        ## CollectionGeometry - Копируемая коллекция геометрии

        Тип данных: Указатель на интерфейс ICollectionGeometry

        ### Синтаксис Automation

        ```
        CollectionGeometry = Object.CollectionGeometry       # Получить свойство (* )
        Object.CollectionGeometry = CollectionGeometry       # Установить свойство (* )
        CollectionGeometry = Object.GetCollectionGeometry()  # Получить свойство (**)
        Object.SetCollectionGeometry( CollectionGeometry )   # Установить свойство (**)
        ```
        """
        ...
    @CollectionGeometry.setter
    def CollectionGeometry(self, value: ICollectionGeometry): ...

    @property
    def ContextObjects(self) -> bool:  # icopygeometry_contextobjects.html
        """
        ## ContextObjects - Работа в контексте

        Тип данных: BOOL

        Значения свойства:

        +----------------------------------+
        | TRUE  | - работа в контексте,    |
        +----------------------------------+
        | FALSE | - по внешнему документу. |
        +----------------------------------+

        ### Синтаксис Automation

        ```
        ContextObjects = Object.ContextObjects       # Получить свойство (* )
        Object.ContextObjects = ContextObjects       # Установить свойство (* )
        ContextObjects = Object.GetContextObjects()  # Получить свойство (**)
        Object.SetContextObjects( ContextObjects )   # Установить свойство (**)
        ```
        """
        ...
    @ContextObjects.setter
    def ContextObjects(self, value: bool): ...

    @property
    def DocumentFileName(self) -> str:  # icopygeometry_documentfilename.html
        """
        ## DocumentFileName - Имя файла внешнего документа

        Тип данных: BSTR

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        DocumentFileName = Object.DocumentFileName       # Получить свойство (* )
        DocumentFileName = Object.GetDocumentFileName()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def ExternalLocalCS(self) -> ILocalCoordinateSystem:  # icopygeometry_externallocalcs.html
        """
        ## ExternalLocalCS - Система координат внешнего документа

        Тип данных: Указатель на интерфейс ILocalCoordinateSystem

        ### Синтаксис Automation

        ```
        ExternalLocalCS = Object.ExternalLocalCS       # Получить свойство (* )
        Object.ExternalLocalCS = ExternalLocalCS       # Установить свойство (* )
        ExternalLocalCS = Object.GetExternalLocalCS()  # Получить свойство (**)
        Object.SetExternalLocalCS( ExternalLocalCS )   # Установить свойство (**)
        ```
        """
        ...
    @ExternalLocalCS.setter
    def ExternalLocalCS(self, value: ILocalCoordinateSystem): ...

    @property
    def InitialObjects(self) -> typing.Any:  # icopygeometry_initialobjects.html
        """
        ## InitialObjects - Исходные объекты массива SafeArray VT_ARRAY | VT_DISPATCH

        Тип данных: VARIANT

        ### Синтаксис Automation

        ```
        InitialObjects = Object.InitialObjects       # Получить свойство (* )
        Object.InitialObjects = InitialObjects       # Установить свойство (* )
        InitialObjects = Object.GetInitialObjects()  # Получить свойство (**)
        Object.SetInitialObjects( InitialObjects )   # Установить свойство (**)
        ```
        """
        ...
    @InitialObjects.setter
    def InitialObjects(self, value: typing.Any): ...

    @property
    def LocalCS(self) -> ILocalCoordinateSystem:  # icopygeometry_localcs.html
        """
        ## LocalCS - Система координат

        Тип данных: Указатель на интерфейс ILocalCoordinateSystem

        ### Синтаксис Automation

        ```
        LocalCS = Object.LocalCS       # Получить свойство (* )
        Object.LocalCS = LocalCS       # Установить свойство (* )
        LocalCS = Object.GetLocalCS()  # Получить свойство (**)
        Object.SetLocalCS( LocalCS )   # Установить свойство (**)
        ```
        """
        ...
    @LocalCS.setter
    def LocalCS(self, value: ILocalCoordinateSystem): ...

    @property
    def MirrorCopy(self) -> bool:  # icopygeometry_mirrorcopy.html
        """
        ## MirrorCopy - Зеркальное копирование

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        MirrorCopy = Object.MirrorCopy       # Получить свойство (* )
        Object.MirrorCopy = MirrorCopy       # Установить свойство (* )
        MirrorCopy = Object.GetMirrorCopy()  # Получить свойство (**)
        Object.SetMirrorCopy( MirrorCopy )   # Установить свойство (**)
        ```
        """
        ...
    @MirrorCopy.setter
    def MirrorCopy(self, value: bool): ...

    @property
    def WatchForSourceChange(self) -> bool:  # icopygeometry_watchforsourcechange.html
        """
        ## WatchForSourceChange - Следить за источником

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        WatchForSourceChange = Object.WatchForSourceChange       # Получить свойство (* )
        Object.WatchForSourceChange = WatchForSourceChange       # Установить свойство (* )
        WatchForSourceChange = Object.GetWatchForSourceChange()  # Получить свойство (**)
        Object.SetWatchForSourceChange( WatchForSourceChange )   # Установить свойство (**)
        ```
        """
        ...
    @WatchForSourceChange.setter
    def WatchForSourceChange(self, value: bool): ...

    def AddInitialObjects(self, Objects) -> bool:  # icopygeometry_addinitialobjects.html
        """
        ## AddInitialObjects - Добавить объекты в массив копируемых объектов

        ### Синтаксис Automation

        ```
        BOOL AddInitialObjects( VARIANT Objects );
        ```

        ### Входные параметры

        +--------------------------------------------------------------------------+
        | Objects | - массив SafeArray VT_ARRAY | VT_DISPATCH копируемых объектов. |
        +--------------------------------------------------------------------------+

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def AddInitialObjectsFromExternalDocument(self, ExternalDocument, ExternalLocalCS, Objects):  # icopygeometry_addinitialobjectsfromexternaldocument.html
        """
        ## AddInitialObjectsFromExternalDocument - Добавить объекты из файла в массив копируемых объектов

        ### Синтаксис Automation

        ```
        BOOL AddInitialObjectsFromExternalDocument( IKompasDocument3D * ExternalDocument, ILocalCoordinateSystem * ExternalLocalCS, VARIANT Objects );
        ```

        ### Входные параметры

        +-----------------------------------------------------------------------------------+
        | ExternalDocument | - указатель на внешний документ,                               |
        +-----------------------------------------------------------------------------------+
        | ExternalLocalCS  | - указатель на систему координат внешнего документа,           |
        +-----------------------------------------------------------------------------------+
        | Objects          | - массив SafeArray VT_ARRAY | VT_DISPATCH копируемых объектов. |
        +-----------------------------------------------------------------------------------+
        """
        ...

    def Clear(self) -> bool:  # icopygeometry_clear.html
        """
        ## Clear - Очистить список исходных объектов массива

        ### Синтаксис Automation

        ```
        BOOL Clear();
        ```

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def Destroy(self) -> bool:  # icopygeometry_destroy.html
        """
        ## Destroy - Разрушить массив

        ### Синтаксис Automation

        ```
        BOOL Destroy();
        ```

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def GetExternalInitialObjects(self, ExternalDocument3D):
        ...

    def GetExternalLocalCooordinateSystem(self, ExternalDocument3D) -> ILocalCoordinateSystem:  # icopygeometry_getexternallocalcooordinatesystem.html
        """
        ## GetExternalLocalCooordinateSystem - Система координат из внешнего документа

        ### Синтаксис Automation

        ```
        LPDISPATCH GetExternalLocalCooordinateSystem( IKompasDocument3D * ExternalDocument3D );
        ```

        ### Входные параметры

        +---------------------------------------------------------------------------------------------------------------------+
        | ExternalDocument3D | - указатель на интерфейс 3D документа, из которого выполнялось копирование, IKompasDocument3D. |
        +---------------------------------------------------------------------------------------------------------------------+

        Версия КОМПАС v22

        ### Возвращаемое значение

        - Указатель на интерфейс ILocalCoordinateSystem.
        """
        ...

    def IsInitialObject(self, Object) -> bool:  # icopygeometry_isinitialobject.html
        """
        ## IsInitialObject - Проверка - является ли объект исходным для массива

        ### Синтаксис Automation

        ```
        BOOL IsInitialObject( IModelObject * Object );
        ```

        ### Входные параметры

        +---------------------------------+
        | Object | - указатель на объект. |
        +---------------------------------+

        ### Возвращаемое значение

        +----------------------------------------+
        | TRUE  | - объект является исходным,    |
        +----------------------------------------+
        | FALSE | - объект не является исходным. |
        +----------------------------------------+
        """
        ...

    def IsSuitableObject(self, Object) -> bool:  # icopygeometry_issuitableobject.html
        """
        ## IsSuitableObject - Проверка пригодности объекта для операции

        ### Синтаксис Automation

        ```
        BOOL IsSuitableObject( IModelObject * Object );
        ```

        ### Входные параметры

        +---------------------------------+
        | Object | - указатель на объект. |
        +---------------------------------+

        ### Возвращаемое значение

        +-------------------------------+
        | TRUE  | - объект пригоден,    |
        +-------------------------------+
        | FALSE | - объект не пригоден. |
        +-------------------------------+
        """
        ...

    def OpenDocument(self, Visible, ReadOnly) -> IKompasDocument3D:  # icopygeometry_opendocument.html
        """
        ## OpenDocument - Открывает документ-источникна редактирование

        ### Синтаксис Automation

        ```
        LPDISPATCH OpenDocument( BOOL Visible, BOOL ReadOnly );
        ```

        ### Входные параметры

        +-----------------------------------------+
        | Visible  | - открыть в видимом режиме,  |
        +-----------------------------------------+
        | ReadOnly | - открыть только для чтения. |
        +-----------------------------------------+

        ### Возвращаемое значение

        - указатель на интерфейс док\\источника IKompasDocument3D.
        """
        ...

    def Synhronise(self) -> bool:  # icopygeometry_synhronise.html
        """
        ## Synhronise - Обновить по источнику

        ### Синтаксис Automation

        ```
        BOOL Synhronise();
        ```

        ### Возвращаемое значение

        +-----------------------------------------+
        | TRUE | - в случае успешного завершения. |
        +-----------------------------------------+
        """
        ...




class IConicCurve3D(IModelObject):  # iconiccurve3d.html
    """
    ## Интерфейс IConicCurve3D

    Интерфейс конической кривой 3D.

    Интерфейс можно получить используя свойство IConicCurves3D::ConicCurve3D и метод IConicCurves3D::Add.

    Версия КОМПАС v22
    """

    @property
    def BasePlane(self) -> IModelObject:  # iconiccurve3d_baseplane.html
        """
        ## BasePlane - Базовая плоскость

        Тип данных: Указатель на интерфейс IModelObject

        ### Синтаксис Automation

        ```
        BasePlane = Object.BasePlane       # Получить свойство (* )
        Object.BasePlane = BasePlane       # Установить свойство (* )
        BasePlane = Object.GetBasePlane()  # Получить свойство (**)
        Object.SetBasePlane( BasePlane )   # Установить свойство (**)
        ```
        """
        ...
    @BasePlane.setter
    def BasePlane(self, value: IModelObject): ...

    @property
    def BuildingType(self) -> int:  # iconiccurve3d_buildingtype.html
        """
        ## BuildingType - Способ построения

        Тип данных: из перечисления ksConicCurve3DBuildingTypeEnum

        ### Синтаксис Automation

        ```
        BuildingType = Object.BuildingType       # Получить свойство (* )
        Object.BuildingType = BuildingType       # Установить свойство (* )
        BuildingType = Object.GetBuildingType()  # Получить свойство (**)
        Object.SetBuildingType( BuildingType )   # Установить свойство (**)
        ```
        """
        ...
    @BuildingType.setter
    def BuildingType(self, value: int): ...

    @property
    def ByHeightCoefficient(self) -> bool:  # iconiccurve3d_byheightcoefficient.html
        """
        ## ByHeightCoefficient - По коэффициенту

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        ByHeightCoefficient = Object.ByHeightCoefficient       # Получить свойство (* )
        Object.ByHeightCoefficient = ByHeightCoefficient       # Установить свойство (* )
        ByHeightCoefficient = Object.GetByHeightCoefficient()  # Получить свойство (**)
        Object.SetByHeightCoefficient( ByHeightCoefficient )   # Установить свойство (**)
        ```
        """
        ...
    @ByHeightCoefficient.setter
    def ByHeightCoefficient(self, value: bool): ...

    @property
    def Height(self) -> float:  # iconiccurve3d_height.html
        """
        ## Height - Высота кривой

        Тип данных: double

        ### Синтаксис Automation

        ```
        Height = Object.Height       # Получить свойство (* )
        Object.Height = Height       # Установить свойство (* )
        Height = Object.GetHeight()  # Получить свойство (**)
        Object.SetHeight( Height )   # Установить свойство (**)
        ```
        """
        ...
    @Height.setter
    def Height(self, value: float): ...

    @property
    def HeightCoefficient(self) -> float:  # iconiccurve3d_heightcoefficient.html
        """
        ## HeightCoefficient - Коэффициент высоты

        Тип данных: double

        ### Синтаксис Automation

        ```
        HeightCoefficient = Object.HeightCoefficient       # Получить свойство (* )
        Object.HeightCoefficient = HeightCoefficient       # Установить свойство (* )
        HeightCoefficient = Object.GetHeightCoefficient()  # Получить свойство (**)
        Object.SetHeightCoefficient( HeightCoefficient )   # Установить свойство (**)
        ```
        """
        ...
    @HeightCoefficient.setter
    def HeightCoefficient(self, value: float): ...

    @property
    def MinimumTension(self) -> bool:  # iconiccurve3d_minimumtension.html
        """
        ## MinimumTension - Минимальное напряжение

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        MinimumTension = Object.MinimumTension       # Получить свойство (* )
        Object.MinimumTension = MinimumTension       # Установить свойство (* )
        MinimumTension = Object.GetMinimumTension()  # Получить свойство (**)
        Object.SetMinimumTension( MinimumTension )   # Установить свойство (**)
        ```
        """
        ...
    @MinimumTension.setter
    def MinimumTension(self, value: bool): ...

    def AssociationObject(self, Index) -> IModelObject:  # iconiccurve3d_associationobject.html
        """
        ## AssociationObject - Установить опорный объект для вершины

        Тип данных: Указатель на интерфейс IModelObject

        ### Синтаксис Automation

        ```
        AssociationObject = Object.AssociationObject       # Получить свойство (* )
        Object.AssociationObject = AssociationObject       # Установить свойство (* )
        AssociationObject = Object.GetAssociationObject()  # Получить свойство (**)
        Object.SetAssociationObject( AssociationObject )   # Установить свойство (**)
        ```
        """
        ...

    def GetPoint(self, Index) -> bool:  # iconiccurve3d_getpoint.html
        """
        ## GetPoint - Получить координату точки

        ### Синтаксис Automation

        ```
        BOOL GetPoint( ksConicCurve3DParameterEnum Index, double * X, double * Y, double * Z );
        ```

        ### Входные параметры

        +----------------------------------------------------------------------+
        | Index | - индекс вершины из перечисления ksConicCurve3DParameterEnum |
        +----------------------------------------------------------------------+

        ### Выходные параметры

        +-------------------------------+
        | X, Y, Z | - координаты точки. |
        +-------------------------------+

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def PointParameters(self, Index) -> IKompasAPIObject:  # iconiccurve3d_pointparameters.html
        """
        ## PointParameters - Получить интерфейс параметров точки

        Тип данных: Указатель на интерфейс IKompasAPIObject

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        PointParameters = Object.PointParameters( Index )     # Получить свойство (* )
        PointParameters = Object.GetPointParameters( Index )  # Получить свойство (**)
        ```

        ### Входные параметры

        Index - индекс вершины из перечисления ksConicCurve3DParameterEnum.
        """
        ...

    def PointType(self, Index) -> int:  # iconiccurve3d_pointtype.html
        """
        ## PointType - Тип параметров построения точки

        Тип данных: из перечисления ksPoint3DTypeEnum

        ### Синтаксис Automation

        ```
        PointType = Object.PointType( Index )     # Получить свойство (* )
        Object.PointType( Index ) = PointType     # Установить свойство (* )
        PointType = Object.GetPointType( Index )  # Получить свойство (**)
        Object.SetPointType( Index, PointType )   # Установить свойство (**)
        ```

        ### Входные параметры

        Index - индекс вершины из перечисления ksConicCurve3DParameterEnum.
        """
        ...

    def SetAssociationObject(self, Index, arg1):
        ...

    def SetPoint(self, Index, X, Y, Z) -> bool:  # iconiccurve3d_setpoint.html
        """
        ## SetPoint - Установить координату точки

        ### Синтаксис Automation

        ```
        BOOL SetPoint( ksConicCurve3DParameterEnum Index, double X, double Y, double Z );
        ```

        ### Входные параметры

        +------------------------------------------------------------------------+
        | Index   | - индекс вершины из перечисления ksConicCurve3DParameterEnum |
        +------------------------------------------------------------------------+
        | X, Y, Z | - координаты точки.                                          |
        +------------------------------------------------------------------------+

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def SetPointType(self, Index, arg1):
        ...

    def SetTangentAngle(self, BeginPoint, arg1):
        ...

    def SetTangentAngleReverse(self, BeginPoint, arg1):
        ...

    def SetTangentObject(self, BeginPoint, arg1):
        ...

    def TangentAngle(self, BeginPoint) -> float:  # iconiccurve3d_tangentangle.html
        """
        ## TangentAngle - Касательный угол в начальной и конечной точке

        Тип данных: double

        ### Синтаксис Automation

        ```
        TangentAngle = Object.TangentAngle( BeginPoint )     # Получить свойство (* )
        Object.TangentAngle( BeginPoint ) = TangentAngle     # Установить свойство (* )
        TangentAngle = Object.GetTangentAngle( BeginPoint )  # Получить свойство (**)
        Object.SetTangentAngle( BeginPoint, TangentAngle )   # Установить свойство (**)
        ```

        ### Входные параметры

        BOOL BeginPoint - TRUE - начальная точка, FALSE - конечная точка.
        """
        ...

    def TangentAngleReverse(self, BeginPoint) -> bool:  # iconiccurve3d_tangentanglereverse.html
        """
        ## TangentAngleReverse - Угол в обратном направлении для начальной и конечной точек

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        TangentAngleReverse = Object.TangentAngleReverse( BeginPoint )     # Получить свойство (* )
        Object.TangentAngleReverse( BeginPoint ) = TangentAngleReverse     # Установить свойство (* )
        TangentAngleReverse = Object.GetTangentAngleReverse( BeginPoint )  # Получить свойство (**)
        Object.SetTangentAngleReverse( BeginPoint, TangentAngleReverse )   # Установить свойство (**)
        ```

        ### Входные параметры

        BOOL BeginPoint - TRUE - начальная точка, FALSE - конечная точка.
        """
        ...

    def TangentObject(self, BeginPoint) -> IModelObject:  # iconiccurve3d_tangentobject.html
        """
        ## TangentObject - Направляющий объект для начальной и конечной точек

        Тип данных: Указатель на интерфейс IModelObject

        ### Синтаксис Automation

        ```
        TangentObject = Object.TangentObject( BeginPoint )     # Получить свойство (* )
        Object.TangentObject( BeginPoint ) = TangentObject     # Установить свойство (* )
        TangentObject = Object.GetTangentObject( BeginPoint )  # Получить свойство (**)
        Object.SetTangentObject( BeginPoint, TangentObject )   # Установить свойство (**)
        ```

        ### Входные параметры

        BOOL BeginPoint - TRUE - начальная точка, FALSE - конечная точка.
        """
        ...

    def TangentVector(self, BeginPoint) -> IVector3D:  # iconiccurve3d_tangentvector.html
        """
        ## TangentVector - Параметры направляющего вектора для начальной и конечной точек

        Тип данных: Указатель на интерфейс IVector3D

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        TangentVector = Object.TangentVector( BeginPoint )     # Получить свойство (* )
        TangentVector = Object.GetTangentVector( BeginPoint )  # Получить свойство (**)
        ```
        """
        ...




class ICurveByLaw(IModelObject):  # icurvebylaw.html
    """
    ## Интерфейс ICurveByLaw

    Интерфейс параметров кривой по закону

    Интерфейс можно получить у коллекции кривых по закону с помощью свойства ICurveByLaws::CurveByLaw и метода ICurveByLaws::Add.
    """

    @property
    def PointsType(self) -> int:  # icurvebylaw_pointstype.html
        """
        ## PointsType - Тип координат

        Тип данных: из перечисления ksPoint3DTypeEnum

        ### Синтаксис Automation

        ```
        PointsType = Object.PointsType       # Получить свойство (* )
        Object.PointsType = PointsType       # Установить свойство (* )
        PointsType = Object.GetPointsType()  # Получить свойство (**)
        Object.SetPointsType( PointsType )   # Установить свойство (**)
        ```
        """
        ...
    @PointsType.setter
    def PointsType(self, value: int): ...

    @property
    def UserVariables(self) -> typing.Any:  # icurvebylaw_uservariables.html
        """
        ## UserVariables - Пользовательские переменные

        Тип данных: VARIANT

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        UserVariables = Object.UserVariables       # Получить свойство (* )
        UserVariables = Object.GetUserVariables()  # Получить свойство (**)
        ```
        """
        ...

    def Expression(self, coord) -> str:  # icurvebylaw_expression.html
        """
        ## Expression - Выражение

        Тип данных: BSTR

        ### Синтаксис Automation

        ```
        Expression = Object.Expression( coord )     # Получить свойство (* )
        Object.Expression( coord ) = Expression     # Установить свойство (* )
        Expression = Object.GetExpression( coord )  # Получить свойство (**)
        Object.SetExpression( coord, Expression )   # Установить свойство (**)
        ```

        ### Входные параметры

        coord - порядок законов из перечисления ksCoordLawEnum.
        """
        ...

    def IntervalExpression(self, coord) -> str:  # icurvebylaw_intervalexpression.html
        """
        ## IntervalExpression - Выражение для интервальной переменной

        Тип данных: BSTR

        ### Синтаксис Automation

        ```
        IntervalExpression = Object.IntervalExpression( coord )     # Получить свойство (* )
        Object.IntervalExpression( coord ) = IntervalExpression     # Установить свойство (* )
        IntervalExpression = Object.GetIntervalExpression( coord )  # Получить свойство (**)
        Object.SetIntervalExpression( coord, IntervalExpression )   # Установить свойство (**)
        ```

        ### Входные параметры

        coord - порядок законов из перечисления ksCoordLawEnum.
        """
        ...

    def LawType(self, coord) -> int:  # icurvebylaw_lawtype.html
        """
        ## LawType - Тип функции

        Тип данных: из перечисления ksLawTypeEnum

        ### Синтаксис Automation

        ```
        LawType = Object.LawType( coord )     # Получить свойство (* )
        Object.LawType( coord ) = LawType     # Установить свойство (* )
        LawType = Object.GetLawType( coord )  # Получить свойство (**)
        Object.SetLawType( coord, LawType )   # Установить свойство (**)
        ```

        ### Входные параметры

        coord - порядок законов из перечисления ksCoordLawEnum.
        """
        ...

    def SetExpression(self, coord, arg1):
        ...

    def SetIntervalExpression(self, coord, arg1):
        ...

    def SetLawType(self, coord, arg1):
        ...

    def SetTMax(self, coord, arg1):
        ...

    def SetTMin(self, coord, arg1):
        ...

    def TMax(self, coord) -> float:  # icurvebylaw_tmax.html
        """
        ## TMax - Максимальный параметр кривой

        Тип данных: double

        ### Синтаксис Automation

        ```
        TMax = Object.TMax( coord )     # Получить свойство (* )
        Object.TMax( coord ) = TMax     # Установить свойство (* )
        TMax = Object.GetTMax( coord )  # Получить свойство (**)
        Object.SetTMax( coord, TMax )   # Установить свойство (**)
        ```

        ### Входные параметры

        coord - порядок законов из перечисления ksCoordLawEnum.
        """
        ...

    def TMin(self, coord) -> float:  # icurvebylaw_tmin.html
        """
        ## TMin - Минимальный параметр кривой

        Тип данных: double

        ### Синтаксис Automation

        ```
        TMin = Object.TMin( coord )     # Получить свойство (* )
        Object.TMin( coord ) = TMin     # Установить свойство (* )
        TMin = Object.GetTMin( coord )  # Получить свойство (**)
        Object.SetTMin( coord, TMin )   # Установить свойство (**)
        ```

        ### Входные параметры

        coord - порядок законов из перечисления ksCoordLawEnum.
        """
        ...




class ICurveBy2Projections(IModelObject):  # icurveby2projections.html
    """
    ## Интерфейс ICurveBy2Projections

    Кривая по двум проекциям.

    Интерфейс можно получить у коллекции кривых по двум проекциям ICurvesBy2Projectionses с помощью свойства ICurvesBy2Projectionses::CurveBy2Projections и метода ICurvesBy2Projectionses::Add.
    """

    @property
    def AutoCheck(self) -> bool:  # icurveby2projections_autocheck.html
        """
        ## AutoCheck - Включение/выключение автоматического выбора всех массивов ребер

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        AutoCheck = Object.AutoCheck       # Получить свойство (* )
        Object.AutoCheck = AutoCheck       # Установить свойство (* )
        AutoCheck = Object.GetAutoCheck()  # Получить свойство (**)
        Object.SetAutoCheck( AutoCheck )   # Установить свойство (**)
        ```
        """
        ...
    @AutoCheck.setter
    def AutoCheck(self, value: bool): ...

    @property
    def EdgesArraysCount(self) -> int:  # icurveby2projections_edgesarrayscount.html
        """
        ## EdgesArraysCount - Количество массивов ребер в кривой пересечения поверхностей

        Тип данных: long

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        EdgesArraysCount = Object.EdgesArraysCount       # Получить свойство (* )
        EdgesArraysCount = Object.GetEdgesArraysCount()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def EdgesChecks(self) -> typing.Any:  # icurveby2projections_edgeschecks.html
        """
        ## EdgesChecks - Задать признаки выбора массивов ребер - массив признаков типа SAFEARRAY VARIANT_BOOL - VT_ARRAY | VT_BOOL

        Тип данных: VARIANT

        ### Синтаксис Automation

        ```
        EdgesChecks = Object.EdgesChecks       # Получить свойство (* )
        Object.EdgesChecks = EdgesChecks       # Установить свойство (* )
        EdgesChecks = Object.GetEdgesChecks()  # Получить свойство (**)
        Object.SetEdgesChecks( EdgesChecks )   # Установить свойство (**)
        ```
        """
        ...
    @EdgesChecks.setter
    def EdgesChecks(self, value: typing.Any): ...

    @property
    def EdgesChecksCount(self) -> int:  # icurveby2projections_edgescheckscount.html
        """
        ## EdgesChecksCount - Количество доступных для создания контуров

        Тип данных: long

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        EdgesChecksCount = Object.EdgesChecksCount       # Получить свойство (* )
        EdgesChecksCount = Object.GetEdgesChecksCount()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def Projection1(self) -> typing.Any:  # icurveby2projections_projection1.html
        """
        ## Projection1 - Проекция 1 в виде SAFEARRAY DISPATCH - VT_ARRAY | VT_DISPATCH

        Тип данных: VARIANT

        Свойство позволяет устанавливать и получать массив ребер, составляющих проекцию 1

        ### Синтаксис Automation

        ```
        Projection1 = Object.Projection1       # Получить свойство (* )
        Object.Projection1 = Projection1       # Установить свойство (* )
        Projection1 = Object.GetProjection1()  # Получить свойство (**)
        Object.SetProjection1( Projection1 )   # Установить свойство (**)
        ```
        """
        ...
    @Projection1.setter
    def Projection1(self, value: typing.Any): ...

    @property
    def Projection2(self) -> typing.Any:  # icurveby2projections_projection2.html
        """
        ## Projection2 - Проекция 2 в виде SAFEARRAY DISPATCH - VT_ARRAY | VT_DISPATCH

        Тип данных: VARIANT

        Свойство позволяет устанавливать и получать массив ребер, составляющих проекцию 2

        ### Синтаксис Automation

        ```
        Projection2 = Object.Projection2       # Получить свойство (* )
        Object.Projection2 = Projection2       # Установить свойство (* )
        Projection2 = Object.GetProjection2()  # Получить свойство (**)
        Object.SetProjection2( Projection2 )   # Установить свойство (**)
        ```
        """
        ...
    @Projection2.setter
    def Projection2(self, value: typing.Any): ...

    def Edges(self, Index) -> typing.Any:  # icurveby2projections_edges.html
        """
        ## Edges - Получить указанный по индексу массив ребер в виде SAFEARRAY DISPATCH - VT_ARRAY | VT_DISPATCH

        Тип данных: VARIANT

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        Edges = Object.Edges( Index )     # Получить свойство (* )
        Edges = Object.GetEdges( Index )  # Получить свойство (**)
        ```
        """
        ...

    def EdgesCheck(self, ContourIndex) -> bool:  # icurveby2projections_edgescheck.html
        """
        ## EdgesCheck - Получить признак выбора указанногопо индексу массива ребер

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        EdgesCheck = Object.EdgesCheck       # Получить свойство (* )
        Object.EdgesCheck = EdgesCheck       # Установить свойство (* )
        EdgesCheck = Object.GetEdgesCheck()  # Получить свойство (**)
        Object.SetEdgesCheck( EdgesCheck)    # Установить свойство (**)
        ```

        ### Входные параметры

        long EdgesArrayIndex - индекс массива ребер.
        """
        ...

    def SetEdgesCheck(self, ContourIndex, arg1):
        ...




class ICylindricSpiral3D(ISpiral3D):  # icylindricspiral3d.html
    """
    ## Интерфейс ICylindricSpiral3D

    Цилиндрическая спираль.

    Интерфейс можно получить у коллекции спиралей с помощью свойства ISpirals3D::Spiral3D и метода ISpirals3D::Add.
    """

    @property
    def CoordinateSystem(self):
        ...
    @CoordinateSystem.setter
    def CoordinateSystem(self, value): ...

    @property
    def Diameter(self) -> float:  # icylindricspiral3d_diameter.html
        """
        ## Diameter - Диаметр

        Тип данных: double

        Свойство позволяет устанавливать и получать диаметр спирали.

        ### Синтаксис Automation

        ```
        Diameter = Object.Diameter       # Получить свойство (* )
        Object.Diameter = Diameter       # Установить свойство (* )
        Diameter = Object.GetDiameter()  # Получить свойство (**)
        Object.SetDiameter( Diameter )   # Установить свойство (**)
        ```
        """
        ...
    @Diameter.setter
    def Diameter(self, value: float): ...

    @property
    def DiameterBaseObject(self) -> IModelObject:  # icylindricspiral3d_diameterbaseobject.html
        """
        ## DiameterBaseObject - Объект, задающий диаметр

        Тип данных: Указатель на интерфейс IModelObject

        Свойство позволяет устанавливать и получать указатель на объект, задающий диаметр спирали.

        ### Синтаксис Automation

        ```
        DiameterBaseObject = Object.DiameterBaseObject       # Получить свойство (* )
        Object.DiameterBaseObject = DiameterBaseObject       # Установить свойство (* )
        DiameterBaseObject = Object.GetDiameterBaseObject()  # Получить свойство (**)
        Object.SetDiameterBaseObject( DiameterBaseObject )   # Установить свойство (**)
        ```
        """
        ...
    @DiameterBaseObject.setter
    def DiameterBaseObject(self, value: IModelObject): ...

    @property
    def DiameterType(self) -> int:  # icylindricspiral3d_diametertype.html
        """
        ## DiameterType - Способ задания диаметра

        Тип данных: из перечисления ksSpline3DDiameterTypeEnum

        Свойство позволяет устанавливать и получать способ задания диаметра.

        ### Синтаксис Automation

        ```
        DiameterType = Object.DiameterType       # Получить свойство (* )
        Object.DiameterType = DiameterType       # Установить свойство (* )
        DiameterType = Object.GetDiameterType()  # Получить свойство (**)
        Object.SetDiameterType( DiameterType )   # Установить свойство (**)
        ```
        """
        ...
    @DiameterType.setter
    def DiameterType(self, value: int): ...

    @property
    def HeightCorrectionType(self) -> bool:  # icylindricspiral3d_heightcorrectiontype.html
        """
        ## HeightCorrectionType - Признак дополнительной высоты. TRUE - продолжить за объект, FALSE - не доходя до объекта

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        HeightCorrectionType = Object.HeightCorrectionType       # Получить свойство (* )
        Object.HeightCorrectionType = HeightCorrectionType       # Установить свойство (* )
        HeightCorrectionType = Object.GetHeightCorrectionType()  # Получить свойство (**)
        Object.SetHeightCorrectionType( HeightCorrectionType )   # Установить свойство (**)
        ```
        """
        ...
    @HeightCorrectionType.setter
    def HeightCorrectionType(self, value: bool): ...




class ICurveOutLine(IModelObject):  # icurveoutline.html
    """
    ## Интерфейс ICurveOutLine

    Интерфейс параметров линии очерка.

    Интерфейс можно получить у коллекции линий очерка с помощью свойства ICurveOutLines::CurveOutLine и метода ICurveOutLines::Add.
    """

    @property
    def AutoCheck(self) -> bool:  # icurveoutline_autocheck.html
        """
        ## AutoCheck - Включение/выключение автоматического выбора всех массивов ребер

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        AutoCheck = Object.AutoCheck       # Получить свойство (* )
        Object.AutoCheck = AutoCheck       # Установить свойство (* )
        AutoCheck = Object.GetAutoCheck()  # Получить свойство (**)
        Object.SetAutoCheck( AutoCheck )   # Установить свойство (**)
        ```
        """
        ...
    @AutoCheck.setter
    def AutoCheck(self, value: bool): ...

    @property
    def EdgesArraysCount(self) -> int:  # icurveoutline_edgesarrayscount.html
        """
        ## EdgesArraysCount - Количество массивов ребер в линии очерка

        Тип данных: long

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        EdgesArraysCount = Object.EdgesArraysCount       # Получить свойство (* )
        EdgesArraysCount = Object.GetEdgesArraysCount()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def EdgesChecks(self) -> typing.Any:  # icurveoutline_edgeschecks.html
        """
        ## EdgesChecks - Задать признаки выбора массивов ребер - массив признаков типа SAFEARRAY VARIANT_BOOL - VT_ARRAY | VT_BOOL

        Тип данных: VARIANT

        ### Синтаксис Automation

        ```
        EdgesChecks = Object.EdgesChecks       # Получить свойство (* )
        Object.EdgesChecks = EdgesChecks       # Установить свойство (* )
        EdgesChecks = Object.GetEdgesChecks()  # Получить свойство (**)
        Object.SetEdgesChecks( EdgesChecks )   # Установить свойство (**)
        ```
        """
        ...
    @EdgesChecks.setter
    def EdgesChecks(self, value: typing.Any): ...

    @property
    def EdgesChecksCount(self) -> int:  # icurveoutline_edgescheckscount.html
        """
        ## EdgesChecksCount - Количество доступных для создания контуров

        Тип данных: long

        ### Синтаксис Automation

        ```
        EdgesChecksCount = Object.EdgesChecksCount       # Получить свойство (* )
        EdgesChecksCount = Object.GetEdgesChecksCount()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def Faces(self) -> typing.Any:  # icurveoutline_faces.html
        """
        ## Faces - Список граней

        Тип данных: VARIANT

        ### Синтаксис Automation

        ```
        Faces = Object.Faces       # Получить свойство (* )
        Object.Faces = Faces       # Установить свойство (* )
        Faces = Object.GetFaces()  # Получить свойство (**)
        Object.SetFaces( Faces )   # Установить свойство (**)
        ```
        """
        ...
    @Faces.setter
    def Faces(self, value: typing.Any): ...

    @property
    def Vector3D(self) -> IVector3D:  # icurveoutline_vector3d.html
        """
        ## Vector3D - Параметры вектора

        Тип данных: Указатель на интерфейс IVector3D

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        Vector3D = Object.Vector3D       # Получить свойство (* )
        Vector3D = Object.GetVector3D()  # Получить свойство (**)
        ```
        """
        ...

    def Edges(self, Index) -> typing.Any:  # icurveoutline_edges.html
        """
        ## Edges - Получить указанный по индексу массив ребер в виде SAFEARRAY DISPATCH - VT_ARRAY | VT_DISPATCH

        Тип данных: VARIANT

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        Edges = Object.Edges( Index )     # Получить свойство (* )
        Edges = Object.GetEdges( Index )  # Получить свойство (**)
        ```

        ### Входные параметры

        Index - индекс массива ребер
        """
        ...

    def EdgesCheck(self, ContourIndex) -> bool:  # icurveoutline_edgescheck.html
        """
        ## EdgesCheck - Получить признак выбора, указанного по индексу массива ребер

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        EdgesCheck = Object.EdgesCheck( EdgesArrayIndex )     # Получить свойство (* )
        Object.EdgesCheck( EdgesArrayIndex ) = EdgesCheck     # Установить свойство (* )
        EdgesCheck = Object.GetEdgesCheck( EdgesArrayIndex )  # Получить свойство (**)
        Object.SetEdgesCheck( EdgesArrayIndex, EdgesCheck )   # Установить свойство (**)
        ```

        ### Входные параметры

        EdgesArrayIndex - индекс массива ребер.
        """
        ...

    def SetEdgesCheck(self, ContourIndex, arg1):
        ...




class IEquidistant3D(IModelObject):  # iequidistant3d.html
    """
    ## Интерфейс IEquidistant3D

    Эквидистанта 3D.

    Позволяет задавать параметры эквидистанты в 3D документе.

    1. Интерфейс можно получить у коллекции 3D-эквидистант, используя свойство IEquidistants3D::Equidistant3D или метод IEquidistants3D::Add.

    2. После задания параметров эквидистанты требуется вызвать метод IModelObject::Update.
    """

    @property
    def Angle(self) -> float:  # iequidistant3d_angle.html
        """
        ## Angle - Угол смещения

        Тип данных: double

        ### Синтаксис Automation

        ```
        Angle = Object.Angle       # Получить свойство (* )
        Object.Angle = Angle       # Установить свойство (* )
        Angle = Object.GetAngle()  # Получить свойство (**)
        Object.SetAngle( Angle )   # Установить свойство (**)
        ```
        """
        ...
    @Angle.setter
    def Angle(self, value: float): ...

    @property
    def BaseObject(self) -> IModelObject:  # iequidistant3d_baseobject.html
        """
        ## BaseObject - Объект, задающий базовое направление смещения или поверхность, которой должны принадлежать базовая кривая

        Тип данных: Указатель на интерфейс IModelObject

        ### Синтаксис Automation

        ```
        BaseObject = Object.BaseObject       # Получить свойство (* )
        Object.BaseObject = BaseObject       # Установить свойство (* )
        BaseObject = Object.GetBaseObject()  # Получить свойство (**)
        Object.SetBaseObject( BaseObject )   # Установить свойство (**)
        ```
        """
        ...
    @BaseObject.setter
    def BaseObject(self, value: IModelObject): ...

    @property
    def CutMode(self) -> int:  # iequidistant3d_cutmode.html
        """
        ## CutMode - Тип построения

        Тип данных: из перечисления ksEquidistant3DCutModeEnum

        ### Синтаксис Automation

        ```
        CutMode = Object.CutMode       # Получить свойство (* )
        Object.CutMode = CutMode       # Установить свойство (* )
        CutMode = Object.GetCutMode()  # Получить свойство (**)
        Object.SetCutMode( CutMode )   # Установить свойство (**)
        ```
        """
        ...
    @CutMode.setter
    def CutMode(self, value: int): ...

    @property
    def DirFromBegin(self) -> bool:  # iequidistant3d_dirfrombegin.html
        """
        ## DirFromBegin - Смещение отсчитывать от начальной вершины пути

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        DirFromBegin = Object.DirFromBegin       # Получить свойство (* )
        Object.DirFromBegin = DirFromBegin       # Установить свойство (* )
        DirFromBegin = Object.GetDirFromBegin()  # Получить свойство (**)
        Object.SetDirFromBegin( DirFromBegin )   # Установить свойство (**)
        ```
        """
        ...
    @DirFromBegin.setter
    def DirFromBegin(self, value: bool): ...

    @property
    def Distance(self) -> float:  # iequidistant3d_distance.html
        """
        ## Distance - Расстояние

        Тип данных: double

        ### Синтаксис Automation

        ```
        Distance = Object.Distance       # Получить свойство (* )
        Object.Distance = Distance       # Установить свойство (* )
        Distance = Object.GetDistance()  # Получить свойство (**)
        Object.SetDistance( Distance )   # Установить свойство (**)
        ```
        """
        ...
    @Distance.setter
    def Distance(self, value: float): ...

    @property
    def Edges(self) -> list[IDispatch]:  # iequidistant3d_edges.html
        """
        ## Edges - Массив ребер

        Тип данных: VARIANT типа VT_ARRAY | VT_DISPATCH

        Если возвращается один объект, то тип VARIANT-а будет VT_DISPATCH, если возвращается несколько объектов, то тип VARIANT-а будет VT_ARRAY | VT_DISPATCH.

        ### Синтаксис Automation

        ```
        Edges = Object.Edges       # Получить свойство (* )
        Object.Edges = Edges       # Установить свойство (* )
        Edges = Object.GetEdges()  # Получить свойство (**)
        Object.SetEdges( Edges )   # Установить свойство (**)
        ```
        """
        ...
    @Edges.setter
    def Edges(self, value: list[IDispatch]): ...

    @property
    def EdgesCount(self) -> int:  # iequidistant3d_edgescount.html
        """
        ## EdgesCount - Количество объектов в эквидистанте

        Тип данных: long

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        EdgesCount = Object.EdgesCount       # Получить свойство (* )
        EdgesCount = Object.GetEdgesCount()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def KeepRadius(self) -> bool:  # iequidistant3d_keepradius.html
        """
        ## KeepRadius - Сохранять радиусы скруглений

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        KeepRadius = Object.KeepRadius       # Получить свойство (* )
        Object.KeepRadius = KeepRadius       # Установить свойство (* )
        KeepRadius = Object.GetKeepRadius()  # Получить свойство (**)
        Object.SetKeepRadius( KeepRadius )   # Установить свойство (**)
        ```
        """
        ...
    @KeepRadius.setter
    def KeepRadius(self, value: bool): ...

    @property
    def OnFace(self) -> bool:  # iequidistant3d_onface.html
        """
        ## OnFace - Способ построения эквидистанта: вдоль поверхности или по направлению от вершины

        Тип данных: BOOL

        Значения свойства:

        +--------------------------------------+
        | TRUE  | - вдоль поверхности,         |
        +--------------------------------------+
        | FALSE | - по направлению от вершины. |
        +--------------------------------------+

        ### Синтаксис Automation

        ```
        OnFace = Object.OnFace       # Получить свойство (* )
        Object.OnFace = OnFace       # Установить свойство (* )
        OnFace = Object.GetOnFace()  # Получить свойство (**)
        Object.SetOnFace( OnFace )   # Установить свойство (**)
        ```
        """
        ...
    @OnFace.setter
    def OnFace(self, value: bool): ...

    @property
    def Vector3D(self) -> IVector3D:  # iequidistant3d_vector3d.html
        """
        ## Vector3D - Получить параметры вектора

        Тип данных: указатель на интерфейс IVector3D

        1. Свойство позволяет получить интерфейс вектора, задающего направление построения эквидистанты.

        2. Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        Vector3D = Object.Vector3D       # Получить свойство (* )
        Vector3D = Object.GetVector3D()  # Получить свойство (**)
        ```
        """
        ...

    def AddEdge(self, Object) -> bool:  # iequidistant3d_addedge.html
        """
        ## AddEdge - Добавить объект в эквидистанту

        ### Синтаксис Automation

        ```
        BOOL AddEdge( IModelObject * Object );
        ```

        ### Входные параметры

        +---------------------------------------------+
        | Object | - указатель на добавляемый объект. |
        +---------------------------------------------+

        ### Возвращаемое значение

        +-----------------------------------------+
        | TRUE  | - в случае удачного завершения, |
        +-----------------------------------------+
        | FALSE | - в случае неудачи.             |
        +-----------------------------------------+
        """
        ...

    def RemoveEdge(self, Index) -> bool:  # iequidistant3d_removeedge.html
        """
        ## RemoveEdge - Удалить объект из списка объектов эквидистанты

        ### Синтаксис Automation

        ```
        BOOL RemoveEdge( long Index );
        ```

        ### Входные параметры

        +--------------------------------------+
        | Index | - индекс удаляемого объекта. |
        +--------------------------------------+

        ### Возвращаемое значение

        +-----------------------------------------+
        | TRUE  | - в случае удачного завершения, |
        +-----------------------------------------+
        | FALSE | - в случае неудачи.             |
        +-----------------------------------------+
        """
        ...




class IExtensionCurve(IModelObject):  # iextensioncurve.html
    """
    ## Интерфейс IExtensionCurve

    Интерфейс Продление кривой

    Интерфейс можно получить, используя свойство IExtensionCurves::ExtensionCurve и метод IExtensionCurves::Add.

    Версия КОМПАС v22
    """

    @property
    def Curves(self) -> typing.Any:  # iextensioncurve_curves.html
        """
        ## Curves - Продлеваемые кривые

        Тип данных: VARIANT

        Кривые передаются в виде SAFEARRAY DISPATCH - VT_ARRAY | VT_DISPATCH.

        ### Синтаксис Automation

        ```
        Curves = Object.Curves       # Получить свойство (* )
        Object.Curves = Curves       # Установить свойство (* )
        Curves = Object.GetCurves()  # Получить свойство (**)
        Object.SetCurves( Curves )   # Установить свойство (**)
        ```
        """
        ...
    @Curves.setter
    def Curves(self, value: typing.Any): ...

    @property
    def Direction(self) -> bool:  # iextensioncurve_direction.html
        """
        ## Direction - Направление

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        Direction = Object.Direction       # Получить свойство (* )
        Object.Direction = Direction       # Установить свойство (* )
        Direction = Object.GetDirection()  # Получить свойство (**)
        Object.SetDirection( Direction )   # Установить свойство (**)
        ```
        """
        ...
    @Direction.setter
    def Direction(self, value: bool): ...

    @property
    def FullLength(self) -> float:  # iextensioncurve_fulllength.html
        """
        ## FullLength - Общая длина

        Тип данных: double

        ### Синтаксис Automation

        ```
        FullLength = Object.FullLength       # Получить свойство (* )
        Object.FullLength = FullLength       # Установить свойство (* )
        FullLength = Object.GetFullLength()  # Получить свойство (**)
        Object.SetFullLength( FullLength )   # Установить свойство (**)
        ```
        """
        ...
    @FullLength.setter
    def FullLength(self, value: float): ...

    @property
    def OnSurface(self) -> bool:  # iextensioncurve_onsurface.html
        """
        ## OnSurface - Вдоль поверхности

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        OnSurface = Object.OnSurface       # Получить свойство (* )
        Object.OnSurface = OnSurface       # Установить свойство (* )
        OnSurface = Object.GetOnSurface()  # Получить свойство (**)
        Object.SetOnSurface( OnSurface )   # Установить свойство (**)
        ```
        """
        ...
    @OnSurface.setter
    def OnSurface(self, value: bool): ...

    @property
    def Symmetric(self) -> bool:  # iextensioncurve_symmetric.html
        """
        ## Symmetric - Симметрично

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        Symmetric = Object.Symmetric       # Получить свойство (* )
        Object.Symmetric = Symmetric       # Установить свойство (* )
        Symmetric = Object.GetSymmetric()  # Получить свойство (**)
        Object.SetSymmetric( Symmetric )   # Установить свойство (**)
        ```
        """
        ...
    @Symmetric.setter
    def Symmetric(self, value: bool): ...

    @property
    def UseFullLength(self) -> bool:  # iextensioncurve_usefulllength.html
        """
        ## UseFullLength - TRUE - Общая длина. FALSE - Длина продления

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        UseFullLength = Object.UseFullLength       # Получить свойство (* )
        Object.UseFullLength = UseFullLength       # Установить свойство (* )
        UseFullLength = Object.GetUseFullLength()  # Получить свойство (**)
        Object.SetUseFullLength( UseFullLength )   # Установить свойство (**)
        ```
        """
        ...
    @UseFullLength.setter
    def UseFullLength(self, value: bool): ...

    @property
    def UseSide2(self) -> bool:  # iextensioncurve_useside2.html
        """
        ## UseSide2 - Второе направление

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        UseSide2 = Object.UseSide2       # Получить свойство (* )
        Object.UseSide2 = UseSide2       # Установить свойство (* )
        UseSide2 = Object.GetUseSide2()  # Получить свойство (**)
        Object.SetUseSide2( UseSide2 )   # Установить свойство (**)
        ```
        """
        ...
    @UseSide2.setter
    def UseSide2(self, value: bool): ...

    def ExtensionType(self, FirstSide) -> int:  # iextensioncurve_extensiontype.html
        """
        ## ExtensionType - Тип продления кривой

        Тип данных: из перечисления ksExtensionCurveTypeEnum

        ### Синтаксис Automation

        ```
        ExtensionType = Object.ExtensionType( FirstSide )     # Получить свойство (* )
        Object.ExtensionType( FirstSide ) = ExtensionType     # Установить свойство (* )
        ExtensionType = Object.GetExtensionType( FirstSide )  # Получить свойство (**)
        Object.SetExtensionType( FirstSide, ExtensionType )   # Установить свойство (**)
        ```

        ### Входные параметры

        BOOL FirstSide - TRUE - первая сторона, FALSE - вторая сторона.
        """
        ...

    def SetExtensionType(self, FirstSide, arg1):
        ...

    def SetSideLength(self, FirstSide, arg1):
        ...

    def SideLength(self, FirstSide) -> float:  # iextensioncurve_sidelength.html
        """
        ## SideLength - Длина

        Тип данных: double

        ### Синтаксис Automation

        ```
        SideLength = Object.SideLength( FirstSide )     # Получить свойство (* )
        Object.SideLength( FirstSide ) = SideLength     # Установить свойство (* )
        SideLength = Object.GetSideLength( FirstSide )  # Получить свойство (**)
        Object.SetSideLength( FirstSide, SideLength )   # Установить свойство (**)
        ```

        ### Входные параметры

        BOOL FirstSide - TRUE - первая сторона, FALSE - вторая сторона.
        """
        ...




class IFilletCurve(IModelObject):  # ifilletcurve.html
    """
    ## Интерфейс IFilletCurve

    Интерфейс операции скругления кривых.
    """

    @property
    def Curve1(self) -> IModelObject:  # ifilletcurve_curve1.html
        """
        ## Curve1 - Кривая 1 (первая из скругляемых кривых)

        Тип данных: указатель на интерфейс IModelObject

        ### Синтаксис Automation

        ```
        Curve1 = Object.Curve1       # Получить свойство (* )
        Object.Curve1 = Curve1       # Установить свойство (* )
        Curve1 = Object.GetCurve1()  # Получить свойство (**)
        Object.SetCurve1( Curve1 )   # Установить свойство (**)
        ```
        """
        ...
    @Curve1.setter
    def Curve1(self, value: IModelObject): ...

    @property
    def Curve2(self) -> IModelObject:  # ifilletcurve_curve2.html
        """
        ## Curve2 - Кривая 2 (вторая из скругляемых кривых)

        Тип данных: указатель на интерфейс IModelObject

        ### Синтаксис Automation

        ```
        Curve2 = Object.Curve2       # Получить свойство (* )
        Object.Curve2 = Curve2       # Установить свойство (* )
        Curve2 = Object.GetCurve2()  # Получить свойство (**)
        Object.SetCurve2( Curve2 )   # Установить свойство (**)
        ```
        """
        ...
    @Curve2.setter
    def Curve2(self, value: IModelObject): ...

    @property
    def Direction(self) -> bool:  # ifilletcurve_direction.html
        """
        ## Direction - Направление дуги скругления

        Тип данных: BOOL

        Значения свойства:

        +---------------------------------+
        | TRUE  | - прямое направление,   |
        +---------------------------------+
        | FALSE | - обратное направление. |
        +---------------------------------+

        Свойство позволяет устанавливать и получать направление дуги скругления.

        ### Синтаксис Automation

        ```
        Direction = Object.Direction       # Получить свойство (* )
        Object.Direction = Direction       # Установить свойство (* )
        Direction = Object.GetDirection()  # Получить свойство (**)
        Object.SetDirection( Direction )   # Установить свойство (**)
        ```
        """
        ...
    @Direction.setter
    def Direction(self, value: bool): ...

    @property
    def OnSurface(self) -> bool:  # ifilletcurve_onsurface.html
        """
        ## OnSurface - По поверхности

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        OnSurface = Object.OnSurface       # Получить свойство (* )
        Object.OnSurface = OnSurface       # Установить свойство (* )
        OnSurface = Object.GetOnSurface()  # Получить свойство (**)
        Object.SetOnSurface( OnSurface )   # Установить свойство (**)
        ```
        """
        ...
    @OnSurface.setter
    def OnSurface(self, value: bool): ...

    @property
    def Radius(self) -> float:  # ifilletcurve_radius.html
        """
        ## Radius - Радиус дуги скругления

        Тип данных: double

        ### Синтаксис Automation

        ```
        Radius = Object.Radius       # Получить свойство (* )
        Object.Radius = Radius       # Установить свойство (* )
        Radius = Object.GetRadius()  # Получить свойство (**)
        Object.SetRadius( Radius )   # Установить свойство (**)
        ```
        """
        ...
    @Radius.setter
    def Radius(self, value: float): ...

    @property
    def TrimCurve1(self) -> bool:  # ifilletcurve_trimcurve1.html
        """
        ## TrimCurve1 - Усекать кривую 1

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        TrimCurve1 = Object.TrimCurve1       # Получить свойство (* )
        Object.TrimCurve1 = TrimCurve1       # Установить свойство (* )
        TrimCurve1 = Object.GetTrimCurve1()  # Получить свойство (**)
        Object.SetTrimCurve1( TrimCurve1 )   # Установить свойство (**)
        ```
        """
        ...
    @TrimCurve1.setter
    def TrimCurve1(self, value: bool): ...

    @property
    def TrimCurve2(self) -> bool:  # ifilletcurve_trimcurve2.html
        """
        ## TrimCurve2 - Усекать кривую 2

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        TrimCurve2 = Object.TrimCurve2       # Получить свойство (* )
        Object.TrimCurve2 = TrimCurve2       # Установить свойство (* )
        TrimCurve2 = Object.GetTrimCurve2()  # Получить свойство (**)
        Object.SetTrimCurve2( TrimCurve2 )   # Установить свойство (**)
        ```
        """
        ...
    @TrimCurve2.setter
    def TrimCurve2(self, value: bool): ...

    def GetCurve1CutPoint(self) -> bool:  # ifilletcurve_getcurve1cutpoint.html
        """
        ## GetCurve1CutPoint - Приближенная точка начала скругления на кривой 1

        ### Синтаксис Automation

        ```
        BOOL GetCurve1CutPoint( double * X, double * Y, double * Z );
        ```

        ### Выходные параметры

        X, Y, Z - координаты точки.

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def GetCurve2CutPoint(self) -> bool:  # ifilletcurve_getcurve2cutpoint.html
        """
        ## GetCurve2CutPoint - Приближенная точка начала скругления на кривой 2

        ### Синтаксис Automation

        ```
        BOOL GetCurve2CutPoint( double * X, double * Y, double * Z );
        ```

        ### Выходные параметры

        X, Y, Z - координаты точки.

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def SetCurve1CutPoint(self, X, Y, Z) -> bool:  # ifilletcurve_setcurve1cutpoint.html
        """
        ## SetCurve1CutPoint - Приближенная точка начала скругления на кривой 1

        ### Синтаксис Automation

        ```
        BOOL SetCurve1CutPoint( double X, double Y, double Z );
        ```

        ### Входные параметры

        X, Y, Z - координаты точки.

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def SetCurve2CutPoint(self, X, Y, Z) -> bool:  # ifilletcurve_setcurve2cutpoint.html
        """
        ## SetCurve2CutPoint - Приближенная точка начала скругления на кривой 2

        ### Синтаксис Automation

        ```
        BOOL SetCurve2CutPoint( double X, double Y, double Z );
        ```

        ### Входные параметры

        X, Y, Z - координаты точки.

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...




class IIsoparametricCurve(IModelObject):  # iisoparametriccurve.html
    """
    ## Интерфейс IIsoparametricCurve

    Изопараметрическая кривая.

    Интерфейс можно получить у коллекции изопараметрических кривых с помощью свойства IIsoparametricCurves::IsoparametricCurve и метода IIsoparametricCurves::Add.
    """

    @property
    def AssociationObject(self) -> IModelObject:  # iisoparametriccurve_associationobject.html
        """
        ## AssociationObject - Установить опорный объект для вершины

        Тип данных: Указатель на интерфейс IModelObject

        ### Синтаксис Automation

        ```
        AssociationObject = Object.AssociationObject       # Получить свойство (* )
        Object.AssociationObject = AssociationObject       # Установить свойство (* )
        AssociationObject = Object.GetAssociationObject()  # Получить свойство (**)
        Object.SetAssociationObject( AssociationObject )   # Установить свойство (**)
        ```
        """
        ...
    @AssociationObject.setter
    def AssociationObject(self, value: IModelObject): ...

    @property
    def SurfaceObject(self) -> IModelObject:  # iisoparametriccurve_surfaceobject.html
        """
        ## SurfaceObject - Установить поверхность для изопараметрической кривой

        Тип данных: Указатель на интерфейс IModelObject

        ### Синтаксис Automation

        ```
        SurfaceObject = Object.SurfaceObject       # Получить свойство (* )
        Object.SurfaceObject = SurfaceObject       # Установить свойство (* )
        SurfaceObject = Object.GetSurfaceObject()  # Получить свойство (**)
        Object.SetSurfaceObject( SurfaceObject )   # Установить свойство (**)
        ```
        """
        ...
    @SurfaceObject.setter
    def SurfaceObject(self, value: IModelObject): ...

    @property
    def U(self) -> float:  # iisoparametriccurve_u.html
        """
        ## U - Значение параметра U в %

        Тип данных: double

        ### Синтаксис Automation

        ```
        U = Object.U       # Получить свойство (* )
        Object.U = U       # Установить свойство (* )
        U = Object.GetU()  # Получить свойство (**)
        Object.SetU( U )   # Установить свойство (**)
        ```
        """
        ...
    @U.setter
    def U(self, value: float): ...

    @property
    def UDirection(self) -> bool:  # iisoparametriccurve_udirection.html
        """
        ## UDirection - Направление

        Тип данных: BOOL

        Значения свойства:

        +-----------------------------+
        | TRUE  | - Направление по U, |
        +-----------------------------+
        | FALSE | - Направление по V. |
        +-----------------------------+

        ### Синтаксис Automation

        ```
        UDirection = Object.UDirection       # Получить свойство (* )
        Object.UDirection = UDirection       # Установить свойство (* )
        UDirection = Object.GetUDirection()  # Получить свойство (**)
        Object.SetUDirection( UDirection )   # Установить свойство (**)
        ```
        """
        ...
    @UDirection.setter
    def UDirection(self, value: bool): ...

    @property
    def V(self) -> float:  # iisoparametriccurve_v.html
        """
        ## V - Значение параметра V в %

        Тип данных: double

        ### Синтаксис Automation

        ```
        V = Object.V       # Получить свойство (* )
        Object.V = V       # Установить свойство (* )
        V = Object.GetV()  # Получить свойство (**)
        Object.SetV( V )   # Установить свойство (**)
        ```
        """
        ...
    @V.setter
    def V(self, value: float): ...

    def GetPoint(self) -> bool:  # iisoparametriccurve_getpoint.html
        """
        ## GetPoint - Получить координату точки на поверхности

        ### Синтаксис Automation

        ```
        BOOL GetPoint( double * X, double * Y, double * Z );
        ```

        ### Выходные параметры

        +------------------------------+
        | X | - координата точки по X, |
        +------------------------------+
        | Y | - координата точки по Y, |
        +------------------------------+
        | Z | - координата точки по Z. |
        +------------------------------+

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def SetPoint(self, X, Y, Z) -> bool:  # iisoparametriccurve_setpoint.html
        """
        ## SetPoint - Установить координату точки на поверхности

        ### Синтаксис Automation

        ```
        BOOL SetPoint( double X, double Y, double Z );
        ```

        ### Входные параметры

        +------------------------------+
        | X | - координата точки по X, |
        +------------------------------+
        | Y | - координата точки по Y, |
        +------------------------------+
        | Z | - координата точки по Z. |
        +------------------------------+

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...




class IIsoparametricCurvesSet(IModelObject):  # iisoparametriccurvesset.html
    """
    ## Интерфейс IIsoparametricCurvesSet

    Группа изопараметрических кривых.

    Интерфейс можно получить у коллекции групп изопараметрических кривых с помощью свойства IIsoparametricCurvesSets::IsoparametricCurvesSet и метода IIsoparametricCurvesSets::Add.
    """

    @property
    def SaveBoundaries(self) -> bool:  # iisoparametriccurvesset_saveboundaries.html
        """
        ## SaveBoundaries - Оставлять границы

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        SaveBoundaries = Object.SaveBoundaries       # Получить свойство (* )
        Object.SaveBoundaries = SaveBoundaries       # Установить свойство (* )
        SaveBoundaries = Object.GetSaveBoundaries()  # Получить свойство (**)
        Object.SetSaveBoundaries( SaveBoundaries )   # Установить свойство (**)
        ```
        """
        ...
    @SaveBoundaries.setter
    def SaveBoundaries(self, value: bool): ...

    @property
    def SurfaceObject(self) -> IModelObject:  # iisoparametriccurvesset_surfaceobject.html
        """
        ## SurfaceObject - Установить поверхность для группы изопараметрических кривых

        Тип данных: Указатель на интерфейс IModelObject

        ### Синтаксис Automation

        ```
        SurfaceObject = Object.SurfaceObject       # Получить свойство (* )
        Object.SurfaceObject = SurfaceObject       # Установить свойство (* )
        SurfaceObject = Object.GetSurfaceObject()  # Получить свойство (**)
        Object.SetSurfaceObject( SurfaceObject )   # Установить свойство (**)
        ```
        """
        ...
    @SurfaceObject.setter
    def SurfaceObject(self, value: IModelObject): ...

    @property
    def UCount(self) -> int:  # iisoparametriccurvesset_ucount.html
        """
        ## UCount - Количество кривых по U

        Тип данных: long

        ### Синтаксис Automation

        ```
        UCount = Object.UCount       # Получить свойство (* )
        Object.UCount = UCount       # Установить свойство (* )
        UCount = Object.GetUCount()  # Получить свойство (**)
        Object.SetUCount( UCount )   # Установить свойство (**)
        ```
        """
        ...
    @UCount.setter
    def UCount(self, value: int): ...

    @property
    def VCount(self) -> int:  # iisoparametriccurvesset_vcount.html
        """
        ## VCount - Количество кривых по V

        Тип данных: long

        ### Синтаксис Automation

        ```
        VCount = Object.VCount       # Получить свойство (* )
        Object.VCount = VCount       # Установить свойство (* )
        VCount = Object.GetVCount()  # Получить свойство (**)
        Object.SetVCount( VCount )   # Установить свойство (**)
        ```
        """
        ...
    @VCount.setter
    def VCount(self, value: int): ...

    def Destroy(self) -> bool:  # iisoparametriccurvesset_destroy.html
        """
        ## Destroy - Рассыпать

        ### Синтаксис Automation

        ```
        BOOL Destroy();
        ```

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...




class ILineSegment3D(IModelObject):  # ilinesegment3d.html
    """
    ## Интерфейс ILineSegment3D

    Отрезок 3D

    Интерфейс можно получить у коллекции отрезков с помощью свойства ILineSegments3D::ILineSegment3D и метода ILineSegments3D::Add.
    """

    @property
    def Angle(self) -> float:  # ilinesegment3d_angle.html
        """
        ## Angle - Угол отрезка

        Тип данных: double

        ### Синтаксис Automation

        ```
        Angle = Object.Angle       # Получить свойство (* )
        Object.Angle = Angle       # Установить свойство (* )
        Angle = Object.GetAngle()  # Получить свойство (**)
        Object.SetAngle( Angle )   # Установить свойство (**)
        ```
        """
        ...
    @Angle.setter
    def Angle(self, value: float): ...

    @property
    def BasePlane(self) -> IModelObject:  # ilinesegment3d_baseplane.html
        """
        ## BasePlane - Базовая плоскость

        Тип данных: Указатель на интерфейс IModelObject

        Свойство позволяет устанавливать и получать плоскость, в которой строится отрезок.

        ### Синтаксис Automation

        ```
        BasePlane = Object.BasePlane       # Получить свойство (* )
        Object.BasePlane = BasePlane       # Установить свойство (* )
        BasePlane = Object.GetBasePlane()  # Получить свойство (**)
        Object.SetBasePlane( BasePlane )   # Установить свойство (**)
        ```
        """
        ...
    @BasePlane.setter
    def BasePlane(self, value: IModelObject): ...

    @property
    def BuildingType(self) -> int:  # ilinesegment3d_buildingtype.html
        """
        ## BuildingType - Тип построения отрезка

        Тип данных: из перечисления ksLineSegment3DTypeEnum

        ### Синтаксис Automation

        ```
        BuildingType = Object.BuildingType       # Получить свойство (* )
        Object.BuildingType = BuildingType       # Установить свойство (* )
        BuildingType = Object.GetBuildingType()  # Получить свойство (**)
        Object.SetBuildingType( BuildingType )   # Установить свойство (**)
        ```
        """
        ...
    @BuildingType.setter
    def BuildingType(self, value: int): ...

    @property
    def Lenght(self) -> float:  # ilinesegment3d_lenght.html
        """
        ## Lenght - Длина отрезка

        Тип данных: double

        ### Синтаксис Automation

        ```
        Lenght = Object.Lenght       # Получить свойство (* )
        Object.Lenght = Length       # Установить свойство (* )
        Lenght = Object.GetLenght()  # Получить свойство (**)
        Object.SetLenght( Lenght )   # Установить свойство (**)
        ```
        """
        ...
    @Lenght.setter
    def Lenght(self, value: float): ...

    def AssociationObject(self, Begin) -> IModelObject:  # ilinesegment3d_associationobject.html
        """
        ## AssociationObject - Установить опорный объект для вершины

        Тип данных: Указатель на интерфейс IModelObject

        ### Синтаксис Automation

        ```
        AssociationObject = Object.AssociationObject( Begin )     # Получить свойство (* )
        Object.AssociationObject( Begin ) = AssociationObject     # Установить свойство (* )
        AssociationObject = Object.GetAssociationObject( Begin )  # Получить свойство (**)
        Object.SetAssociationObject( Begin, AssociationObject )   # Установить свойство (**)
        ```

        ### Входные параметры

        BOOL Begin - начальная точка
        """
        ...

    def GetPoint(self, Begin) -> bool:  # ilinesegment3d_getpoint.html
        """
        ## GetPoint - Получить координату точки

        ### Синтаксис Automation

        ```
        BOOL GetPoint( BOOL Begin, double * X, double * Y, double * Z );
        ```

        ### Входные параметры

        +----------------------------+
        | Begin | - начальная точка. |
        +----------------------------+

        ### Выходные параметры

        +-------------------------+
        | X | - координата точки, |
        +-------------------------+
        | Y | - координата точки, |
        +-------------------------+
        | Z | - координата точки. |
        +-------------------------+

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def PointParameters(self, Begin) -> IKompasAPIObject:  # ilinesegment3d_pointparameters.html
        """
        ## PointParameters - Получить интерфейс параметров точки

        Тип данных: Указатель на интерфейс IKompasAPIObject

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        PointParameters = Object.PointParameters( Begin )     # Получить свойство (* )
        PointParameters = Object.GetPointParameters( Begin )  # Получить свойство (**)
        ```
        """
        ...

    def PointType(self, Begin) -> int:  # ilinesegment3d_pointtype.html
        """
        ## PointType - Тип параметров построения точки

        Тип данных: из перечисления ksPoint3DTypeEnum

        ### Синтаксис Automation

        ```
        PointType = Object.PointType( Begin )     # Получить свойство (* )
        Object.PointType( Begin ) = PointType     # Установить свойство (* )
        PointType = Object.GetPointType( Begin )  # Получить свойство (**)
        Object.SetPointType( Begin, PointType )   # Установить свойство (**)
        ```

        ### Входные параметры

        BOOL Begin - начальная точка
        """
        ...

    def SetAssociationObject(self, Begin, arg1):
        ...

    def SetPoint(self, Begin, X, Y, Z) -> bool:  # ilinesegment3d_setpoint.html
        """
        ## SetPoint - Установить координату точки

        ### Синтаксис Automation

        ```
        BOOL SetPoint( BOOL Begin, double X, double Y, double Z );
        ```

        ### Входные параметры

        +-----------------------------+
        | Begin | - начальная точка,  |
        +-----------------------------+
        | X     | - координата точки, |
        +-----------------------------+
        | Y     | - координата точки, |
        +-----------------------------+
        | Z     | - координата точки. |
        +-----------------------------+

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def SetPointType(self, Begin, arg1):
        ...




class IMeshAroundPointParam(IDispatch):  # imesharoundpointparam.html
    """
    ## Интерфейс IMeshAroundPointParam

    Параметры сетки для группы точек по поверхности вокруг точки.

    Интерфейс можно получить у интерфейса IPointsArrOnSurface с помощью метода IUnknown::QueryInterface.
    """

    @property
    def AssociationObject(self) -> IModelObject:  # imesharoundpointparam_associationobject.html
        """
        ## AssociationObject - Ассоциированный объект

        Тип данных: указатель на интерфейс IModelObject

        Свойство позволяет устанавливать и получать указатель на ассоциированный объект.

        ### Синтаксис Automation

        ```
        AssociationObject = Object.AssociationObject       # Получить свойство (* )
        Object.AssociationObject = AssociationObject       # Установить свойство (* )
        AssociationObject = Object.GetAssociationObject()  # Получить свойство (**)
        Object.SetAssociationObject( AssociationObject )   # Установить свойство (**)
        ```
        """
        ...
    @AssociationObject.setter
    def AssociationObject(self, value: IModelObject): ...

    @property
    def MeshAngle(self) -> float:  # imesharoundpointparam_meshangle.html
        """
        ## MeshAngle - Угол поворота сетки

        Тип данных: double

        Свойство позволяет устанавливать и получать угол поворота сетки.

        ### Синтаксис Automation

        ```
        MeshAngle = Object.MeshAngle       # Получить свойство (* )
        Object.MeshAngle = MeshAngle       # Установить свойство (* )
        MeshAngle = Object.GetMeshAngle()  # Получить свойство (**)
        Object.SetMeshAngle( MeshAngle )   # Установить свойство (**)
        ```
        """
        ...
    @MeshAngle.setter
    def MeshAngle(self, value: float): ...

    @property
    def MeshType(self) -> int:  # imesharoundpointparam_meshtype.html
        """
        ## MeshType - Параметры сетки для группы точекпо поверхности вокруг точки

        Тип данных: из перечисления ksMeshAroundPointTypeEnum

        Свойство позволяет устанавливать и получать способ построения группы точек.

        ### Синтаксис Automation

        ```
        MeshType = Object.MeshType       # Получить свойство (* )
        Object.MeshType = MeshType       # Установить свойство (* )
        MeshType = Object.GetMeshType()  # Получить свойство (**)
        Object.SetMeshType( MeshType )   # Установить свойство (**)
        ```
        """
        ...
    @MeshType.setter
    def MeshType(self, value: int): ...

    @property
    def OffsetU(self) -> float:  # imesharoundpointparam_offsetu.html
        """
        ## OffsetU - Смещение точки по U

        Тип данных: double

        Свойство позволяет устанавливать и получать смещение точки по U.

        ### Синтаксис Automation

        ```
        OffsetU = Object.OffsetU       # Получить свойство (* )
        Object.OffsetU = OffsetU       # Установить свойство (* )
        OffsetU = Object.GetOffsetU()  # Получить свойство (**)
        Object.SetOffsetU( OffsetU )   # Установить свойство (**)
        ```
        """
        ...
    @OffsetU.setter
    def OffsetU(self, value: float): ...

    @property
    def OffsetV(self) -> float:  # imesharoundpointparam_offsetv.html
        """
        ## OffsetV - Смещение точки по V

        Тип данных: double

        Свойство позволяет устанавливать и получать смещение точки по V.

        ### Синтаксис Automation

        ```
        OffsetV = Object.OffsetV       # Получить свойство (* )
        Object.OffsetV = OffsetV       # Установить свойство (* )
        OffsetV = Object.GetOffsetV()  # Получить свойство (**)
        Object.SetOffsetV( OffsetV )   # Установить свойство (**)
        ```
        """
        ...
    @OffsetV.setter
    def OffsetV(self, value: float): ...

    @property
    def RadialStep(self) -> float:  # imesharoundpointparam_radialstep.html
        """
        ## RadialStep - Шаг радиальный

        Тип данных: double

        Свойство позволяет устанавливать и получать радиальный шаг.

        ### Синтаксис Automation

        ```
        RadialStep = Object.RadialStep       # Получить свойство (* )
        Object.RadialStep = RadialStep       # Установить свойство (* )
        RadialStep = Object.GetRadialStep()  # Получить свойство (**)
        Object.SetRadialStep( RadialStep )   # Установить свойство (**)
        ```
        """
        ...
    @RadialStep.setter
    def RadialStep(self, value: float): ...

    @property
    def RayCount(self) -> float:  # imesharoundpointparam_raycount.html
        """
        ## RayCount - Количество радиальных лучей

        Тип данных: double

        Свойство позволяет устанавливать и получать количество радиальных лучей.

        ### Синтаксис Automation

        ```
        RayCount = Object.RayCount       # Получить свойство (* )
        Object.RayCount = RayCount       # Установить свойство (* )
        RayCount = Object.GetRayCount()  # Получить свойство (**)
        Object.SetRayCount( RayCount )   # Установить свойство (**)
        ```
        """
        ...
    @RayCount.setter
    def RayCount(self, value: float): ...

    @property
    def Step(self) -> float:  # imesharoundpointparam_step.html
        """
        ## Step - Шаг между узлами

        Тип данных: double

        Свойство позволяет устанавливать и получать шаг между узлами.

        ### Синтаксис Automation

        ```
        Step = Object.Step       # Получить свойство (* )
        Object.Step = Step       # Установить свойство (* )
        Step = Object.GetStep()  # Получить свойство (**)
        Object.SetStep( Step )   # Установить свойство (**)
        ```
        """
        ...
    @Step.setter
    def Step(self, value: float): ...

    @property
    def Step1(self) -> float:  # imesharoundpointparam_step1.html
        """
        ## Step1 - Шаг по оси 1

        Тип данных: double

        Свойство позволяет устанавливать и получать шаг по оси 1.

        ### Синтаксис Automation

        ```
        Step1 = Object.Step1       # Получить свойство (* )
        Object.Step1 = Step1       # Установить свойство (* )
        Step1 = Object.GetStep1()  # Получить свойство (**)
        Object.SetStep1( Step1 )   # Установить свойство (**)
        ```
        """
        ...
    @Step1.setter
    def Step1(self, value: float): ...

    @property
    def Step2(self) -> float:  # imesharoundpointparam_step2.html
        """
        ## Step2 - Шаг по оси 2

        Тип данных: double

        Свойство позволяет устанавливать и получать шаг по оси 2.

        ### Синтаксис Automation

        ```
        Step2 = Object.Step2       # Получить свойство (* )
        Object.Step2 = Step2       # Установить свойство (* )
        Step2 = Object.GetStep2()  # Получить свойство (**)
        Object.SetStep2( Step2 )   # Установить свойство (**)
        ```
        """
        ...
    @Step2.setter
    def Step2(self, value: float): ...

    @property
    def StepU(self) -> float:  # imesharoundpointparam_stepu.html
        """
        ## StepU - Шаг по U

        Тип данных: double

        Свойство позволяет устанавливать и получать шаг по U.

        ### Синтаксис Automation

        ```
        StepU = Object.StepU       # Получить свойство (* )
        Object.StepU = StepU       # Установить свойство (* )
        StepU = Object.GetStepU()  # Получить свойство (**)
        Object.SetStepU( StepU )   # Установить свойство (**)
        ```
        """
        ...
    @StepU.setter
    def StepU(self, value: float): ...

    @property
    def StepV(self) -> float:  # imesharoundpointparam_stepv.html
        """
        ## StepV - Шаг по V

        Тип данных: double

        Свойство позволяет устанавливать и получать шаг по V.

        ### Синтаксис Automation

        ```
        StepV = Object.StepV       # Получить свойство (* )
        Object.StepV = StepV       # Установить свойство (* )
        StepV = Object.GetStepV()  # Получить свойство (**)
        Object.SetStepV( StepV )   # Установить свойство (**)
        ```
        """
        ...
    @StepV.setter
    def StepV(self, value: float): ...

    def GetOffsetPoint(self) -> bool:  # imesharoundpointparam_getoffsetpoint.html
        """
        ## GetOffsetPoint - Получить точку смещения

        Метод возвращает координаты точки смещения.

        ### Синтаксис Automation

        ```
        BOOL GetOffsetPoint( double * X, double * Y, double * Z );
        ```

        ### Выходные параметры

        +----------------------------------------+
        | X, Y, Z | - координаты точки смещения. |
        +----------------------------------------+

        ### Возвращаемое значение

        +-----------------------------------------+
        | TRUE  | - в случае удачного завершения, |
        +-----------------------------------------+
        | FALSE | - в случае неудачи.             |
        +-----------------------------------------+
        """
        ...

    def SetOffsetPoint(self, X, Y, Z) -> bool:  # imesharoundpointparam_setoffsetpoint.html
        """
        ## SetOffsetPoint - Задать смещение по точке

        Метод задает координаты точки смещения.

        ### Синтаксис Automation

        ```
        BOOL SetOffsetPoint( double X, double Y, double Z );
        ```

        ### Входные параметры

        +----------------------------------------+
        | X, Y, Z | - координаты точки смещения. |
        +----------------------------------------+

        ### Возвращаемое значение

        +-----------------------------------------+
        | TRUE  | - в случае удачного завершения, |
        +-----------------------------------------+
        | FALSE | - в случае неудачи.             |
        +-----------------------------------------+
        """
        ...




class IPointsArrFromFile(IModelObject):  # ipointsarrfromfile.html
    """
    ## Интерфейс IPointsArrFromFile

    Группа точек из файла.

    Интерфейс можно получить у коллекции групп точек из файла с помощью свойства IPointsArrsFromFiles::PointsArrFromFile и метода IPointsArrsFromFiles::Add.
    """

    @property
    def FileName(self) -> str:  # ipointsarrfromfile_filename.html
        """
        ## FileName - Тип точек в файле

        Тип данных: BSTR

        Свойство позволяет устанавливать и получать имя файла.

        ### Синтаксис Automation

        ```
        FileName = Object.FileName       # Получить свойство (* )
        Object.FileName = FileName       # Установить свойство (* )
        FileName = Object.GetFileName()  # Получить свойство (**)
        Object.SetFileName( FileName )   # Установить свойство (**)
        ```
        """
        ...
    @FileName.setter
    def FileName(self, value: str): ...

    @property
    def PointsType(self) -> int:  # ipointsarrfromfile_pointstype.html
        """
        ## PointsType - Тип точек в файле

        Тип данных: из перечисления ksPoint3DTypeEnum

        Свойство позволяет устанавливать и получать тип точек.

        ### Синтаксис Automation

        ```
        PointsType = Object.PointsType       # Получить свойство (* )
        Object.PointsType = PointsType       # Установить свойство (* )
        PointsType = Object.GetPointsType()  # Получить свойство (**)
        Object.SetPointsType( PointsType )   # Установить свойство (**)
        ```
        """
        ...
    @PointsType.setter
    def PointsType(self, value: int): ...

    @property
    def Symbol(self) -> int:  # ipointsarrfromfile_symbol.html
        """
        ## Symbol - Стиль отображения

        Тип данных: из перечисления ksAnnotationSymbolEnum

        Свойство позволяет устанавливать и получать стиль отображения.

        ### Синтаксис Automation

        ```
        Symbol = Object.Symbol       # Получить свойство (* )
        Object.Symbol = Symbol       # Установить свойство (* )
        Symbol = Object.GetSymbol()  # Получить свойство (**)
        Object.SetSymbol( Symbol )   # Установить свойство (**)
        ```
        """
        ...
    @Symbol.setter
    def Symbol(self, value: int): ...

    def Destroy(self) -> bool:  # ipointsarrfromfile_destroy.html
        """
        ## Destroy - Разрушить массив

        Метод позволяет разрушить группу точек.

        ### Синтаксис Automation

        ```
        BOOL Destroy();
        ```

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...




class IPointsArrOnCurve(IModelObject):  # ipointsarroncurve.html
    """
    ## Интерфейс IPointsArrOnCurve

    Группа точек по кривой.

    Интерфейс можно получить у коллекции групп точек по кривой с помощью свойства IPointsArrsOnCurves::PointsArrOnCurve и метода IPointsArrsOnCurves::Add.
    """

    @property
    def BuildingType(self) -> int:  # ipointsarroncurve_buildingtype.html
        """
        ## BuildingType - Способ построения точек группы

        Тип данных: из перечисления ksPointsArrOnCurveTypeEnum

        Свойство позволяет устанавливать и получать способ построения группы точек.

        ### Синтаксис Automation

        ```
        BuildingType = Object.BuildingType       # Получить свойство (* )
        Object.BuildingType = BuildingType       # Установить свойство (* )
        BuildingType = Object.GetBuildingType()  # Получить свойство (**)
        Object.SetBuildingType( BuildingType )   # Установить свойство (**)
        ```
        """
        ...
    @BuildingType.setter
    def BuildingType(self, value: int): ...

    @property
    def ByStep(self) -> bool:  # ipointsarroncurve_bystep.html
        """
        ## ByStep - Способ построения точек

        Тип данных: BOOL

        Значение свойства:

        +------------------------------+
        | TRUE  | - по шагу,           |
        +------------------------------+
        | FALSE | - вдоль всей кривой. |
        +------------------------------+

        Свойство позволяет устанавливать и получать способ построения точек.

        ### Синтаксис Automation

        ```
        ByStep = Object.ByStep       # Получить свойство (* )
        Object.ByStep = ByStep       # Установить свойство (* )
        ByStep = Object.GetByStep()  # Получить свойство (**)
        Object.SetByStep( ByStep )   # Установить свойство (**)
        ```
        """
        ...
    @ByStep.setter
    def ByStep(self, value: bool): ...

    @property
    def Count(self) -> int:  # ipointsarroncurve_count.html
        """
        ## Count - Количество точек

        Тип данных: long

        Свойство позволяет устанавливать и получать количество точек.

        ### Синтаксис Automation

        ```
        Count = Object.Count       # Получить свойство (* )
        Object.Count = Count       # Установить свойство (* )
        Count = Object.GetCount()  # Получить свойство (**)
        Object.SetCount( Count )   # Установить свойство (**)
        ```
        """
        ...
    @Count.setter
    def Count(self, value: int): ...

    @property
    def Curve(self) -> IModelObject:  # ipointsarroncurve_curve.html
        """
        ## Curve - Кривая

        Тип данных: указатель на интерфейс IModelObject

        Свойство позволяет устанавливать и получать указатель на кривую для построения группы точек.

        ### Синтаксис Automation

        ```
        Curve = Object.Curve       # Получить свойство (* )
        Object.Curve = Curve       # Установить свойство (* )
        Curve = Object.GetCurve()  # Получить свойство (**)
        Object.SetCurve( Curve )   # Установить свойство (**)
        ```
        """
        ...
    @Curve.setter
    def Curve(self, value: IModelObject): ...

    @property
    def OnOffsets(self) -> bool:  # ipointsarroncurve_onoffsets.html
        """
        ## OnOffsets - Задать/отменить отступы

        Тип данных: BOOL

        Свойство позволяет задавать и отменять отступы.

        ### Синтаксис Automation

        ```
        OnOffsets = Object.OnOffsets       # Получить свойство (* )
        Object.OnOffsets = OnOffsets       # Установить свойство (* )
        OnOffsets = Object.GetOnOffsets()  # Получить свойство (**)
        Object.SetOnOffsets( OnOffsets )   # Установить свойство (**)
        ```
        """
        ...
    @OnOffsets.setter
    def OnOffsets(self, value: bool): ...

    @property
    def ReverseDirection(self) -> bool:  # ipointsarroncurve_reversedirection.html
        """
        ## ReverseDirection - Направление построения массива

        Тип данных: BOOL

        Значение свойства:

        +---------------------------------+
        | TRUE  | - обратное направление, |
        +---------------------------------+
        | FALSE | - прямое направление.   |
        +---------------------------------+

        Свойство позволяет устанавливать и получать направление построения массива.

        ### Синтаксис Automation

        ```
        ReverseDirection = Object.ReverseDirection       # Получить свойство (* )
        Object.ReverseDirection = ReverseDirection       # Установить свойство (* )
        ReverseDirection = Object.GetReverseDirection()  # Получить свойство (**)
        Object.SetReverseDirection( ReverseDirection )   # Установить свойство (**)
        ```
        """
        ...
    @ReverseDirection.setter
    def ReverseDirection(self, value: bool): ...

    @property
    def Step(self) -> float:  # ipointsarroncurve_step.html
        """
        ## Step - Шаг

        Тип данных: double

        Свойство позволяет устанавливать и получать шаг построения точек.

        ### Синтаксис Automation

        ```
        Step = Object.Step       # Получить свойство (* )
        Object.Step = Step       # Установить свойство (* )
        Step = Object.GetStep()  # Получить свойство (**)
        Object.SetStep( Step )   # Установить свойство (**)
        ```
        """
        ...
    @Step.setter
    def Step(self, value: float): ...

    @property
    def Symbol(self) -> int:  # ipointsarroncurve_symbol.html
        """
        ## Symbol - Стиль отображения

        Тип данных: из перечисления ksAnnotationSymbolEnum

        Свойство позволяет устанавливать и получать направление границы.

        ### Синтаксис Automation

        ```
        Symbol = Object.Symbol       # Получить свойство (* )
        Object.Symbol = Symbol       # Установить свойство (* )
        Symbol = Object.GetSymbol()  # Получить свойство (**)
        Object.SetSymbol( Symbol )   # Установить свойство (**)
        ```

        ### Входные параметры

        +---------------------------------------+
        | BOOL FirstPoint | - для первой точки. |
        +---------------------------------------+
        """
        ...
    @Symbol.setter
    def Symbol(self, value: int): ...

    def Destroy(self) -> bool:  # ipointsarroncurve_destroy.html
        """
        ## Destroy - Разрушить массив

        Метод позволяет разрушить группу точек.

        ### Синтаксис Automation

        ```
        BOOL Destroy();
        ```

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def Offset(self, FirstPoint) -> float:  # ipointsarroncurve_offset.html
        """
        ## Offset - Значение отступа%

        Тип данных: double

        Свойство позволяет устанавливать и получать значение отступа в процентах.

        ### Синтаксис Automation

        ```
        Offset = Object.Offset( FirstPoint )     # Получить свойство (* )
        Object.Offset( FirstPoint ) = Offset     # Установить свойство (* )
        Offset = Object.GetOffset( FirstPoint )  # Получить свойство (**)
        Object.SetOffset( FirstPoint, Offset )   # Установить свойство (**)
        ```

        ### Входные параметры

        +---------------------------------------+
        | BOOL FirstPoint | - для первой точки. |
        +---------------------------------------+
        """
        ...

    def OffsetDirection(self, FirstPoint) -> bool:  # ipointsarroncurve_offsetdirection.html
        """
        ## OffsetDirection - Направление границы

        Тип данных: BOOL

        Свойство позволяет устанавливать и получать направление границы.

        ### Синтаксис Automation

        ```
        OffsetDirection = Object.OffsetDirection( FirstPoint )     # Получить свойство (* )
        Object.OffsetDirection( FirstPoint ) = OffsetDirection     # Установить свойство (* )
        OffsetDirection = Object.GetOffsetDirection( FirstPoint )  # Получить свойство (**)
        Object.SetOffsetDirection( FirstPoint, OffsetDirection )   # Установить свойство (**)
        ```

        ### Входные параметры

        +---------------------------------------+
        | BOOL FirstPoint | - для первой точки. |
        +---------------------------------------+
        """
        ...

    def OffsetType(self, FirstPoint) -> int:  # ipointsarroncurve_offsettype.html
        """
        ## OffsetType - Получить способ задания границ

        Тип данных: из перечисления ksPoint3DCurveParamTypeEnum

        Свойство позволяет устанавливать и получать способ задания границ.

        ### Синтаксис Automation

        ```
        OffsetType = Object.OffsetType( FirstPoint )     # Получить свойство (* )
        Object.OffsetType( FirstPoint ) = OffsetType     # Установить свойство (* )
        OffsetType = Object.GetOffsetType( FirstPoint )  # Получить свойство (**)
        Object.SetOffsetType( FirstPoint, OffsetType )   # Установить свойство (**)
        ```

        ### Входные параметры

        +---------------------------------------+
        | BOOL FirstPoint | - для первой точки. |
        +---------------------------------------+
        """
        ...

    def SetOffset(self, FirstPoint, arg1):
        ...

    def SetOffsetDirection(self, FirstPoint, arg1):
        ...

    def SetOffsetType(self, FirstPoint, arg1):
        ...




class IPointsArrOnSurface(IModelObject):  # ipointsarronsurface.html
    """
    ## Интерфейс IPointsArrOnSurface

    Группа точек по поверхности.

    Интерфейс можно получить у коллекции групп точек по поверхности с помощью свойства IPointsArrsOnSurfaces::PointsArrOnSurface и метода IPointsArrsOnSurfaces::Add.
    """

    @property
    def AllowBoundaries(self) -> bool:  # ipointsarronsurface_allowboundaries.html
        """
        ## AllowBoundaries - Учитывать границы

        Тип данных: BOOL

        Свойство позволяет устанавливать и получать угловое отклонение.

        ### Синтаксис Automation

        ```
        AllowBoundaries = Object.AllowBoundaries       # Получить свойство (* )
        Object.AllowBoundaries = AllowBoundaries       # Установить свойство (* )
        AllowBoundaries = Object.GetAllowBoundaries()  # Получить свойство (**)
        Object.SetAllowBoundaries( AllowBoundaries )   # Установить свойство (**)
        ```
        """
        ...
    @AllowBoundaries.setter
    def AllowBoundaries(self, value: bool): ...

    @property
    def AngularDeflection(self) -> float:  # ipointsarronsurface_angulardeflection.html
        """
        ## AngularDeflection - Угловое отклонение

        Тип данных: double

        Свойство позволяет устанавливать и получать угловое отклонение.

        ### Синтаксис Automation

        ```
        AngularDeflection = Object.AngularDeflection       # Получить свойство (* )
        Object.AngularDeflection = AngularDeflection       # Установить свойство (* )
        AngularDeflection = Object.GetAngularDeflection()  # Получить свойство (**)
        Object.SetAngularDeflection( AngularDeflection )   # Установить свойство (**)
        ```
        """
        ...
    @AngularDeflection.setter
    def AngularDeflection(self, value: float): ...

    @property
    def BuildingType(self) -> int:  # ipointsarronsurface_buildingtype.html
        """
        ## BuildingType - Способ построения точек группы

        Тип данных: из перечисления ksPointsArrOnSurfaceTypeEnum

        Свойство позволяет устанавливать и получать способ построения точек.

        ### Синтаксис Automation

        ```
        BuildingType = Object.BuildingType       # Получить свойство (* )
        Object.BuildingType = BuildingType       # Установить свойство (* )
        BuildingType = Object.GetBuildingType()  # Получить свойство (**)
        Object.SetBuildingType( BuildingType )   # Установить свойство (**)
        ```
        """
        ...
    @BuildingType.setter
    def BuildingType(self, value: int): ...

    @property
    def Face(self) -> IModelObject:  # ipointsarronsurface_face.html
        """
        ## Face - Грань поверхности или тела

        Тип данных: Указатель на интерфейс IModelObject

        Свойство позволяет устанавливать и получать грань для построения группы точек.

        ### Синтаксис Automation

        ```
        Face = Object.Face       # Получить свойство (* )
        Object.Face = Face       # Установить свойство (* )
        Face = Object.GetFace()  # Получить свойство (**)
        Object.SetFace( Face )   # Установить свойство (**)
        ```
        """
        ...
    @Face.setter
    def Face(self, value: IModelObject): ...

    @property
    def LinearDeflection(self) -> float:  # ipointsarronsurface_lineardeflection.html
        """
        ## LinearDeflection - Линейное отклонение

        Тип данных: double

        Свойство позволяет устанавливать и получать линейное отклонение.

        ### Синтаксис Automation

        ```
        LinearDeflection = Object.LinearDeflection       # Получить свойство (* )
        Object.LinearDeflection = LinearDeflection       # Установить свойство (* )
        LinearDeflection = Object.GetLinearDeflection()  # Получить свойство (**)
        Object.SetLinearDeflection( LinearDeflection )   # Установить свойство (**)
        ```
        """
        ...
    @LinearDeflection.setter
    def LinearDeflection(self, value: float): ...

    @property
    def Symbol(self) -> int:  # ipointsarronsurface_symbol.html
        """
        ## Symbol - Стиль отображения

        Тип данных: из перечисления ksAnnotationSymbolEnum

        Свойство позволяет устанавливать и получать стиль отображения.

        ### Синтаксис Automation

        ```
        Symbol = Object.Symbol       # Получить свойство (* )
        Object.Symbol = Symbol       # Установить свойство (* )
        Symbol = Object.GetSymbol()  # Получить свойство (**)
        Object.SetSymbol( Symbol )   # Установить свойство (**)
        ```
        """
        ...
    @Symbol.setter
    def Symbol(self, value: int): ...

    @property
    def UCount(self) -> int:  # ipointsarronsurface_ucount.html
        """
        ## UCount - Количество точек по U

        Тип данных: long

        Свойство позволяет устанавливать и получать количество точек по U.

        ### Синтаксис Automation

        ```
        UCount = Object.UCount       # Получить свойство (* )
        Object.UCount = UCount       # Установить свойство (* )
        UCount = Object.GetUCount()  # Получить свойство (**)
        Object.SetUCount( UCount )   # Установить свойство (**)
        ```
        """
        ...
    @UCount.setter
    def UCount(self, value: int): ...

    @property
    def VCount(self) -> int:  # ipointsarronsurface_vcount.html
        """
        ## VCount - Количество точек по V

        Тип данных: long

        Свойство позволяет устанавливать и получать количество точек по V.

        ### Синтаксис Automation

        ```
        VCount = Object.VCount       # Получить свойство (* )
        Object.VCount = VCount       # Установить свойство (* )
        VCount = Object.GetVCount()  # Получить свойство (**)
        Object.SetVCount( VCount )   # Установить свойство (**)
        ```
        """
        ...
    @VCount.setter
    def VCount(self, value: int): ...

    def Destroy(self) -> bool:  # ipointsarronsurface_destroy.html
        """
        ## Destroy - Разрушить массив

        Метод позволяет разрушить группу точек.

        ### Синтаксис Automation

        ```
        BOOL Destroy();
        ```

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...




class IPolyLine(IModelObject):  # ipolyline.html
    """
    ## Интерфейс IPolyLine

    Интерфейс пространственной ломаной.

    1. Интерфейс можно получить у коллекции пространственных ломаных, используя свойство IPolyLines::PolyLine или метод IPolyLines::Add.

    2. После задания параметров объекта требуется вызвать метод IModelObject::Update.

    3. С помощью метода IUnknown::QueryInterface у интерфейса можно получить интерфейс подчиненного объекта ЛСК ILocalCSObject.
    """

    @property
    def Closed(self) -> bool:  # ipolyline_closed.html
        """
        ## Closed - Замкнутость

        Тип данных: BOOL

        Значения свойства:

        +------------------------------------------------+
        | TRUE  | - пространственная ломаная замкнута,   |
        +------------------------------------------------+
        | FALSE | - пространственная ломаная разомкнута. |
        +------------------------------------------------+

        Свойство позволяет получить/установить разомкнутую или замкнутую ломаную.

        ### Синтаксис Automation

        ```
        Closed = iObject.Closed;       # Получить свойство (* )
        iObject.Closed = Closed;       # Установить свойство (* )
        Closed = iObject.GetClosed();  # Получить свойство (**)
        iObject.SetClosed(Closed);     # Установить свойство (**)
        ```
        """
        ...
    @Closed.setter
    def Closed(self, value: bool): ...

    @property
    def Edges(self) -> typing.Any:  # ipolyline_edges.html
        """
        ## Edges - Массив ребер 3D ломаной

        Тип данных: VARIANT

        Свойство позволяет получать массив ребер 3D ломаной. Массив возвращается в виде массива SAFEARRAY рёбер LPDISPATCH (VT_ARRAY | VT_DISPATCH).

        ### Синтаксис Automation

        ```
        Edges = iObject.Edges;       # Получить свойство (* )
        iObject.Edges = Edges;       # Установить свойство (* )
        Edges = iObject.GetEdges();  # Получить свойство (**)
        iObject.SetEdges( Edges );   # Установить свойство (**)
        ```
        """
        ...

    @property
    def ErrorIndexes(self) -> typing.Any:  # ipolyline_errorindexes.html
        """
        ## ErrorIndexes - Массив не скругленных вершин SAFEARRAY - VT_ARRAY | VT_I4

        Тип данных: VARIANT

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        ErrorIndexes = Object.ErrorIndexes;       # Получить свойство (* )
        ErrorIndexes = Object.GetErrorIndexes();  # Получить свойство (**)
        ```
        """
        ...

    @property
    def VertexCount(self) -> int:  # ipolyline_vertexcount.html
        """
        ## VertexCount - Количество вершин

        Тип данных: long

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        VertexCount = iObject.VertexCount;        # Получить свойство (* )
        VertexCount = iObject.GetVertexCount( );  # Получить свойство (**)
        ```
        """
        ...

    @property
    def VertexParamsArray(self) -> typing.Any:  # ipolyline_vertexparamsarray.html
        """
        ## VertexParamsArray - Массив параметров вершин в виде SAFEARRAY DISPATCH - VT_ARRAY | VT_DISPATCH

        Тип данных: VARIANT

        Значения свойства:

        Массив SafeArray типа VT_ARRAY | VT_DISPATCH.

        1. Свойство доступно только для чтения.

        2. Свойство позволяет получить массив SAFEARRAY параметров всех вершин. Массив VT_DISPATCH, которые можно преобразовать в интерфейсы ICurveVertexParam.

        ### Синтаксис Automation

        ```
        VertexParamsArray = iObject.VertexParamsArray;       # Получить свойство (* )
        VertexParamsArray = iObject.GetVertexParamsArray();  # Получить свойство (**)
        ```
        """
        ...

    @property
    def VertexVisible(self) -> bool:  # ipolyline_vertexvisible.html
        """
        ## VertexVisible - Видимость свободных вершин 3D ломаной

        Тип данных: BOOL

        Значения свойства:

        +------------------------------+
        | TRUE  | - вершины видимые,   |
        +------------------------------+
        | FALSE | - вершины невидимые. |
        +------------------------------+

        Свойство позволяет получить/установить видимость свободных вершин 3D ломаной.

        ### Синтаксис Automation

        ```
        VertexVisible = iObject.VertexVisible;       # Получить свойство (* )
        iObject.VertexVisible = VertexVisible;       # Установить свойство (* )
        VertexVisible = iObject.GetVertexVisible();  # Получить свойство (**)
        iObject.SetVertexVisible(VertexVisible);     # Установить свойство (**)
        ```
        """
        ...
    @VertexVisible.setter
    def VertexVisible(self, value: bool): ...

    def AddVertex(self, Index) -> ICurveVertexParam:  # ipolyline_addvertex.html
        """
        ## AddVertex - Создать новую вершину

        ### Синтаксис Automation

        ```
        LPDISPATCH AddVertex( long Index );
        ```

        ### Возвращаемое значение

        +--------------------------------------------------------------------------+
        | ICurveVertexParam | - указатель на интерфейс параметров вершины ломаной. |
        +--------------------------------------------------------------------------+
        """
        ...

    def DeleteVertex(self, Index) -> bool:  # ipolyline_deletevertex.html
        """
        ## DeleteVertex - Удалить вершину с указанным индексом

        ### Синтаксис Automation

        ```
        BOOL DeleteVertex( long Index );
        ```

        ### Входные параметры

        +---------------------------+
        | Index | - индекс вершины. |
        +---------------------------+

        ### Возвращаемое значение

        +-----------------------------------------+
        | TRUE  | - в случае удачного завершения, |
        +-----------------------------------------+
        | FALSE | - в случае неудачи.             |
        +-----------------------------------------+
        """
        ...

    def Flush(self) -> bool:  # ipolyline_flush.html
        """
        ## Flush - Очистить массив вершин

        ### Синтаксис Automation

        ```
        BOOL Flush();
        ```

        ### Возвращаемое значение

        +-----------------------------------------+
        | TRUE  | - в случае удачного завершения, |
        +-----------------------------------------+
        | FALSE | - в случае неудачи.             |
        +-----------------------------------------+
        """
        ...

    def GetVertexCoordinatesArray(self) -> bool:  # ipolyline_getvertexcoordinatesarray.html
        """
        ## GetVertexCoordinatesArray - Получить массив координати радиусов вершин в виде SAFEARRAY double - VT_ARRAY | VT_R8

        1. Параметры Coordinates и Radiuses не являются обязательными. В функцию достаточно передать один из указателей на VARIANT.

        2. Массивы являются согласованными:

        Координаты точек в массиве Coordinates лежат в следующей последовательности: x0, y0, z0, x1, y1, z1 ...xi, yi, zi,

        Радиусы при вершинах кривой в массиве Radiuses лежат в последовательности: radius0, radius1, ...radiusi.

        ### Синтаксис Automation

        ```
        BOOL GetVertexCoordinatesArray( VARIANT * Coordinates,
        VARIANT * Radiuses );
        ```

        ### Входные параметры

        +--------------------------------------------------------------------------------------+
        | Coordinates | - массив SafeArray типа VT_ARRAY | VT_R8 координат точек всех вершин,  |
        +--------------------------------------------------------------------------------------+
        | Radiuses    | - массив SafeArray типа VT_ARRAY | VT_R8 радиусов при вершинах кривой. |
        +--------------------------------------------------------------------------------------+

        ### Возвращаемое значение

        +-----------------------------------------+
        | TRUE  | - в случае удачного завершения, |
        +-----------------------------------------+
        | FALSE | - в случае неудачи.             |
        +-----------------------------------------+
        """
        ...

    def ReadFromFile(self, FileName) -> bool:  # ipolyline_readfromfile.html
        """
        ## ReadFromFile - Прочитать параметры вершин из текстового файла

        См. также IPolyLine::WriteToFile.

        ### Синтаксис Automation

        ```
        BOOL ReadFromFile( BSTR fileName );
        ```

        ### Входные параметры

        +-------------------------+
        | fileName | - имя файла. |
        +-------------------------+

        ### Возвращаемое значение

        +-----------------------------------------+
        | TRUE  | - в случае удачного завершения, |
        +-----------------------------------------+
        | FALSE | - в случае неудачи.             |
        +-----------------------------------------+
        """
        ...

    def VertexParams(self, Index) -> ICurveVertexParam:  # ipolyline_vertexparams.html
        """
        ## VertexParams - Параметры вершины ломаной

        Тип данных: указатель на интерфейс параметров вершины ломанной ICurveVertexParam

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        VertexParams = iObject.VertexParams( Index );     # Получить свойство (* )
        VertexParams = iObject.GetVertexParams( Index );  # Получить свойство (**)
        ```

        ### Входные параметры

        +---------------------------+
        | Index | - индекс вершины. |
        +---------------------------+
        """
        ...

    def WriteToFile(self, FileName) -> bool:  # ipolyline_writetofile.html
        """
        ## WriteToFile - Записать параметры вершин в текстовый файл

        См. также IPolyLine::ReadFromFile.

        ### Синтаксис Automation

        ```
        BOOL WriteToFile( BSTR fileName );
        ```

        ### Входные параметры

        +-------------------------+
        | fileName | - имя файла. |
        +-------------------------+

        ### Возвращаемое значение

        +-----------------------------------------+
        | TRUE  | - в случае удачного завершения, |
        +-----------------------------------------+
        | FALSE | - в случае неудачи.             |
        +-----------------------------------------+
        """
        ...




class IProjectionCurve(IModelObject):  # iprojectioncurve.html
    """
    ## Интерфейс IProjectionCurve

    Проекционная кривая.

    Интерфейс можно получить у коллекции проекционных кривых IProjectionCurves с помощью свойства IProjectionCurves::ProjectionCurve и метода IProjectionCurves::Add.
    """

    @property
    def AutoCheck(self) -> bool:  # iprojectioncurve_autocheck.html
        """
        ## AutoCheck - Включение/выключение автоматического выбора всех массивов ребер

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        AutoCheck= Object.AutoCheck       # Получить свойство (* )
        Object.AutoCheck= AutoCheck       # Установить свойство (* )
        AutoCheck= Object.GetAutoCheck()  # Получить свойство (**)
        Object.SetAutoCheck( AutoCheck)   # Установить свойство (**)
        ```
        """
        ...
    @AutoCheck.setter
    def AutoCheck(self, value: bool): ...

    @property
    def Curves(self) -> typing.Any:  # iprojectioncurve_curves.html
        """
        ## Curves - Проекция 2 в виде SAFEARRAY DISPATCH - VT_ARRAY | VT_DISPATCH

        Тип данных: VARIANT

        Свойство позволяет устанавливать и получать массив кривых, составляющих базовую кривую.

        ### Синтаксис Automation

        ```
        Curves = Object.Curves       # Получить свойство (* )
        Object.Curves = Curves       # Установить свойство (* )
        Curves = Object.GetCurves()  # Получить свойство (**)
        Object.SetCurves( Curves )   # Установить свойство (**)
        ```
        """
        ...
    @Curves.setter
    def Curves(self, value: typing.Any): ...

    @property
    def EdgesArraysCount(self) -> int:  # iprojectioncurve_edgesarrayscount.html
        """
        ## EdgesArraysCount - Количество массивов ребер в кривой пересечения поверхностей

        Тип данных: long.

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        EdgesArraysCount = Object.EdgesArraysCount;       # Получить свойство (* )
        EdgesArraysCount = Object.GetEdgesArraysCount();  # Получить свойство (**)
        ```
        """
        ...

    @property
    def EdgesChecks(self) -> typing.Any:  # iprojectioncurve_edgeschecks.html
        """
        ## EdgesChecks - Задать признаки выбора массивов ребер - массив признаков типа SAFEARRAY VARIANT_BOOL - VT_ARRAY | VT_BOOL

        Тип данных: VARIANT

        ### Синтаксис Automation

        ```
        EdgesChecks= Object.EdgesChecks       # Получить свойство (* )
        Object.EdgesChecks= EdgesChecks       # Установить свойство (* )
        EdgesChecks= Object.GetEdgesChecks()  # Получить свойство (**)
        Object.SetEdgesChecks( EdgesChecks)   # Установить свойство (**)
        ```
        """
        ...
    @EdgesChecks.setter
    def EdgesChecks(self, value: typing.Any): ...

    @property
    def EdgesChecksCount(self) -> int:  # iprojectioncurve_edgescheckscount.html
        """
        ## EdgesChecksCount - Количество доступных для создания контуров

        Тип данных: long

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        EdgesChecksCount = Object.EdgesChecksCount       # Получить свойство (* )
        EdgesChecksCount = Object.GetEdgesChecksCount()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def Faces(self) -> typing.Any:  # iprojectioncurve_faces.html
        """
        ## Faces - Проекция 1 в виде SAFEARRAY DISPATCH - VT_ARRAY | VT_DISPATCH

        Тип данных: VARIANT

        Свойство позволяет устанавливать и получать массив граней, составляющих поверхность проецирования.

        ### Синтаксис Automation

        ```
        Faces = Object.Faces       # Получить свойство (* )
        Object.Faces = Faces       # Установить свойство (* )
        Faces = Object.GetFaces()  # Получить свойство (**)
        Object.SetFaces( Faces )   # Установить свойство (**)
        ```
        """
        ...
    @Faces.setter
    def Faces(self, value: typing.Any): ...

    @property
    def ProjectionType(self) -> int:  # iprojectioncurve_projectiontype.html
        """
        ## ProjectionType - Тип проекции

        Тип данных: из перечисления ksCurveProjectionTypeEnum

        Свойство позволяет устанавливать и получать массив кривых, составляющих базовую кривую.

        ### Синтаксис Automation

        ```
        ProjectionType= Object.ProjectionType       # Получить свойство (* )
        Object.ProjectionType= ProjectionType       # Установить свойство (* )
        ProjectionType= Object.GetProjectionType()  # Получить свойство (**)
        Object.SetProjectionType( ProjectionType)   # Установить свойство (**)
        ```
        """
        ...
    @ProjectionType.setter
    def ProjectionType(self, value: int): ...

    @property
    def TruncationByBounds(self) -> bool:  # iprojectioncurve_truncationbybounds.html
        """
        ## TruncationByBounds - Усечение по границам

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        TruncationByBounds= Object.TruncationByBounds       # Получить свойство (* )
        Object.TruncationByBounds= TruncationByBounds       # Установить свойство (* )
        TruncationByBounds= Object.GetTruncationByBounds()  # Получить свойство (**)
        Object.SetTruncationByBounds( TruncationByBounds)   # Установить свойство (**)
        ```
        """
        ...
    @TruncationByBounds.setter
    def TruncationByBounds(self, value: bool): ...

    @property
    def Vector3D(self) -> IVector3D:  # iprojectioncurve_vector3d.html
        """
        ## Vector3D - Параметры вектора

        Тип данных: Указатель на интерфейс IVector3D

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        Vector3D = Object.Vector3D;       # Получить свойство (* )
        Vector3D = Object.GetVector3D();  # Получить свойство (**)
        ```
        """
        ...

    def Edges(self, Index) -> typing.Any:  # iprojectioncurve_edges.html
        """
        ## Edges - Получить указанный по индексу массив ребер в виде SAFEARRAY DISPATCH - VT_ARRAY | VT_DISPATCH

        Тип данных: VARIANT

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        Edges = Object.Edges;       # Получить свойство (* )
        Edges = Object.GetEdges();  # Получить свойство (**)
        ```

        ### Входные параметры

        +--------------------------------------+
        | long Index | - индекс массива ребер. |
        +--------------------------------------+
        """
        ...

    def EdgesCheck(self, ContourIndex) -> bool:  # iprojectioncurve_edgescheck.html
        """
        ## EdgesCheck - Получить признак выбора указанногопо индексу массива ребер

        Тип данных: BOOL

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        EdgesChecks= Object.EdgesChecks       # Получить свойство (* )
        Object.EdgesChecks= EdgesChecks       # Установить свойство (* )
        EdgesChecks= Object.GetEdgesChecks()  # Получить свойство (**)
        Object.SetEdgesChecks( EdgesChecks)   # Установить свойство (**)
        ```

        ### Входные параметры

        +------------------------------------------------+
        | long EdgesArrayIndex | - индекс массива ребер. |
        +------------------------------------------------+
        """
        ...

    def SetEdgesCheck(self, ContourIndex, arg1):
        ...




class ISpiral3D(IModelObject):  # ispiral3d.html
    """
    ## Интерфейс ISpiral3D

    Интерфейс можно получить у коллекции спиралей с помощью свойства ISpirals3D::Spiral3D и метода ISpirals3D::Add, а также у интерфейсов IConicSpiral3D и ICylindricSpiral3D с помощью метода IUnknown::QueryInterface.
    """

    @property
    def BasePlane(self) -> IModelObject:  # ispiral3d_baseplane.html
        """
        ## BasePlane - Базовая плоскость

        Тип данных: Указатель на интерфейс IModelObject

        Свойство позволяет устанавливать и получать базовую плоскость или плоскую грань, на которой располагается спираль.

        ### Синтаксис Automation

        ```
        BasePlane = Object.BasePlane       # Получить свойство (* )
        Object.BasePlane = BasePlane       # Установить свойство (* )
        BasePlane = Object.GetBasePlane()  # Получить свойство (**)
        Object.SetBasePlane( BasePlane )   # Установить свойство (**)
        ```
        """
        ...
    @BasePlane.setter
    def BasePlane(self, value: IModelObject): ...

    @property
    def BuildingDirection(self) -> bool:  # ispiral3d_buildingdirection.html
        """
        ## BuildingDirection - Направление построения

        Тип данных: BOOL

        Свойство позволяет устанавливать и получать направление построения спирали.

        ### Синтаксис Automation

        ```
        BuildingDirection = Object.BuildingDirection       # Получить свойство (* )
        Object.BuildingDirection = BuildingDirection       # Установить свойство (* )
        BuildingDirection = Object.GetBuildingDirection()  # Получить свойство (**)
        Object.SetBuildingDirection( BuildingDirection )   # Установить свойство (**)
        ```
        """
        ...
    @BuildingDirection.setter
    def BuildingDirection(self, value: bool): ...

    @property
    def BuildingType(self) -> int:  # ispiral3d_buildingtype.html
        """
        ## BuildingType - Способ построения

        Тип данных: из перечисления ksSpline3DBuildingTypeEnum

        Свойство позволяет устанавливать и получать способ построения спирали.

        ### Синтаксис Automation

        ```
        BuildingType = Object.BuildingType       # Получить свойство (* )
        Object.BuildingType = BuildingType       # Установить свойство (* )
        BuildingType = Object.GetBuildingType()  # Получить свойство (**)
        Object.SetBuildingType( BuildingType )   # Установить свойство (**)
        ```
        """
        ...
    @BuildingType.setter
    def BuildingType(self, value: int): ...

    @property
    def Height(self):
        ...
    @Height.setter
    def Height(self, value): ...

    @property
    def HeightBaseObject(self) -> IModelObject:  # ispiral3d_heightbaseobject.html
        """
        ## HeightBaseObject - Объект, задающий высоту

        Тип данных: Указатель на интерфейс IModelObject

        Свойство позволяет устанавливать и получать указатель на объект, задающий высоту спирали.

        ### Синтаксис Automation

        ```
        HeightBaseObject = Object.HeightBaseObject       # Получить свойство (* )
        Object.HeightBaseObject = HeightBaseObject       # Установить свойство (* )
        HeightBaseObject = Object.GetHeightBaseObject()  # Получить свойство (**)
        Object.SetHeightBaseObject( HeightBaseObject )   # Установить свойство (**)
        ```
        """
        ...
    @HeightBaseObject.setter
    def HeightBaseObject(self, value: IModelObject): ...

    @property
    def HeightCorrection(self) -> float:  # ispiral3d_heightcorrection.html
        """
        ## HeightCorrection - Коррекция высоты, заданной по объекту

        Тип данных: double

        Свойство позволяет устанавливать и получать коррекцию высоты, заданной по объекту.

        ### Синтаксис Automation

        ```
        HeightCorrection = Object.HeightCorrection       # Получить свойство (* )
        Object.HeightCorrection = HeightCorrection       # Установить свойство (* )
        HeightCorrection = Object.GetHeightCorrection()  # Получить свойство (**)
        Object.SetHeightCorrection( HeightCorrection )   # Установить свойство (**)
        ```
        """
        ...
    @HeightCorrection.setter
    def HeightCorrection(self, value: float): ...

    @property
    def HeightType(self) -> int:  # ispiral3d_heighttype.html
        """
        ## HeightType - Способ задания высоты

        Тип данных: из перечисления ksSpiral3DHeightTypeEnum

        Свойство позволяет устанавливать и получать способ задания высоты спирали.

        ### Синтаксис Automation

        ```
        HeightType = Object.HeightType       # Получить свойство (* )
        Object.HeightType = HeightType       # Установить свойство (* )
        HeightType = Object.GetHeightType()  # Получить свойство (**)
        Object.SetHeightType( HeightType )   # Установить свойство (**)
        ```
        """
        ...
    @HeightType.setter
    def HeightType(self, value: int): ...

    @property
    def Sketch(self) -> ISketch:  # ispiral3d_sketch.html
        """
        ## Sketch - Эскиз

        Тип данных: Указатель на интерфейс ISketch

        1. Свойство доступно только для чтения.

        2. Свойство позволяет получить указатель на эскиз спирали.

        ### Синтаксис Automation

        ```
        Sketch = Object.Sketch       # Получить свойство (* )
        Sketch = Object.GetSketch()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def Step(self) -> float:  # ispiral3d_step.html
        """
        ## Step - Шаг

        Тип данных: double

        Свойство позволяет устанавливать и получать шаг витков спирали.

        ### Синтаксис Automation

        ```
        Step = Object.Step       # Получить свойство (* )
        Object.Step = Step       # Установить свойство (* )
        Step = Object.GetStep()  # Получить свойство (**)
        Object.SetStep( Step )   # Установить свойство (**)
        ```
        """
        ...
    @Step.setter
    def Step(self, value: float): ...

    @property
    def TurnDirection(self) -> bool:  # ispiral3d_turndirection.html
        """
        ## TurnDirection - Направление навивки

        Тип данных: BOOL

        Значение свойства:

        +---------------------+
        | TRUE  | - прямое,   |
        +---------------------+
        | FALSE | - обратное. |
        +---------------------+

        Свойство позволяет устанавливать и получать направление навивки.

        ### Синтаксис Automation

        ```
        TurnDirection = Object.TurnDirection       # Получить свойство (* )
        Object.TurnDirection = TurnDirection       # Установить свойство (* )
        TurnDirection = Object.GetTurnDirection()  # Получить свойство (**)
        Object.SetTurnDirection( TurnDirection )   # Установить свойство (**)
        ```
        """
        ...
    @TurnDirection.setter
    def TurnDirection(self, value: bool): ...

    @property
    def TurningAngle(self) -> float:  # ispiral3d_turningangle.html
        """
        ## TurningAngle - Начальный угол (или угол поворота спирали вокруг своей оси)

        Тип данных: double

        1. Свойство позволяет устанавливать и получать угол поворота спирали вокруг перпендикуляра к опорной грани.

        2. Угол измеряется относительно оси абсцисс системы координат опорной грани.

        ### Синтаксис Automation

        ```
        TurningAngle = Object.TurningAngle       # Получить свойство (* )
        Object.TurningAngle = TurningAngle       # Установить свойство (* )
        TurningAngle = Object.GetTurningAngle()  # Получить свойство (**)
        Object.SetTurningAngle( TurningAngle )   # Установить свойство (**)
        ```
        """
        ...
    @TurningAngle.setter
    def TurningAngle(self, value: float): ...

    @property
    def TurnsCount(self) -> float:  # ispiral3d_turnscount.html
        """
        ## TurnsCount - Количество витков

        Тип данных: double

        Свойство позволяет устанавливать и получать количество витков спирали.

        ### Синтаксис Automation

        ```
        TurnsCount = Object.TurnsCount       # Получить свойство (* )
        Object.TurnsCount = TurnsCount       # Установить свойство (* )
        TurnsCount = Object.GetTurnsCount()  # Получить свойство (**)
        Object.SetTurnsCount( TurnsCount )   # Установить свойство (**)
        ```
        """
        ...
    @TurnsCount.setter
    def TurnsCount(self, value: float): ...

    def GetBasePoint(self) -> bool:  # ispiral3d_getbasepoint.html
        """
        ## GetBasePoint - Получить координаты точки привязки спирали на базовой плоскости (точку пересечения оси спирали с базовой плоскостью)

        Метод возвращает координаты точки привязки спирали.

        ### Синтаксис Automation

        ```
        BOOL GetBasePoint( double * X, double * Y );
        ```

        ### Выходные параметры

        X, Y - координаты точки привязки спирали.

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def SetBasePoint(self, X, Y) -> bool:  # ispiral3d_setbasepoint.html
        """
        ## SetBasePoint - Установить координаты точки привязки спирали на базовой плоскости (точку пересечения оси спирали с базовой плоскостью)

        Метод устанавливает координаты точки привязки спирали.

        ### Синтаксис Automation

        ```
        BOOL SetBasePoint( double X, double Y );
        ```

        ### Входные параметры

        X, Y - координаты точки привязки спирали.

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...




class ISpline3D(IModelObject):  # ispline3d.html
    """
    ## Интерфейс ISpline3D

    Интерфейс cплайна.

    1. Интерфейс можно получить у коллекции сплайнов, используя свойство ISplines3D::Spline3D или метод ISplines3D::Add.

    2. После задания параметров сплайна требуется вызвать метод IModelObject::Update.

    3. С помощью метода IUnknown::QueryInterface у интерфейса можно получить интерфейс подчиненного объекта ЛСК ILocalCSObject.
    """

    @property
    def BuildingType(self) -> int:  # ispline3d_buildingtype.html
        """
        ## BuildingType - Способ построения

        Тип данных: из перечисления ksSplineBuildingTypeEnum

        ### Синтаксис Automation

        ```
        BuildingType = Object.BuildingType       # Получить свойство (* )
        Object.BuildingType = BuildingType       # Установить свойство (* )
        BuildingType = Object.GetBuildingType()  # Получить свойство (**)
        Object.SetBuildingType( BuildingType )   # Установить свойство (**)
        ```
        """
        ...
    @BuildingType.setter
    def BuildingType(self, value: int): ...

    @property
    def Closed(self) -> bool:  # ispline3d_closed.html
        """
        ## Closed - Замкнутость

        Тип данных: BOOL

        Значения свойства:

        +-------------------------------+
        | TRUE  | - сплайн замкнутый,   |
        +-------------------------------+
        | FALSE | - сплайн разомкнутый. |
        +-------------------------------+

        Свойство позволяет получить/установить разомкнутый или замкнутый сплайн.

        ### Синтаксис Automation

        ```
        Closed = iObject.Closed;       # Получить свойство (* )
        iObject.Closed = Closed;       # Установить свойство (* )
        Closed = iObject.GetClosed();  # Получить свойство (**)
        iObject.SetClosed(Closed);     # Установить свойство (**)
        ```
        """
        ...
    @Closed.setter
    def Closed(self, value: bool): ...

    @property
    def Knots(self):  # ispline3d_knots.html
        """
        ## Knots - Массив SAFEARRAY узлов кривой NURBS

        ### Синтаксис Automation

        ```
        Knots = Object.Knots       # Получить свойство (* )
        Object.Knots = Knots       # Установить свойство (* )
        Knots = Object.GetKnots()  # Получить свойство (**)
        Object.SetKnots( Knots )   # Установить свойство (**)
        ```
        """
        ...
    @Knots.setter
    def Knots(self, value): ...

    @property
    def SplineOnPoles(self) -> bool:  # ispline3d_splineonpoles.html
        """
        ## SplineOnPoles - Тип построения

        Тип данных: BOOL

        Значения свойства:

        +------------------------------+
        | TRUE  | - сплайн по полюсам, |
        +------------------------------+
        | FALSE | - сплайн по точкам.  |
        +------------------------------+

        Свойство позволяет получить/установить сплайн по точкам/полюсам.

        ### Синтаксис Automation

        ```
        SplineOnPoles = iObject.SplineOnPoles;       # Получить свойство (* )
        iObject.SplineOnPoles = SplineOnPoles;       # Установить свойство (* )
        SplineOnPoles = iObject.GetSplineOnPoles();  # Получить свойство (**)
        iObject.SetSplineOnPoles(SplineOnPoles);     # Установить свойство (**)
        ```
        """
        ...
    @SplineOnPoles.setter
    def SplineOnPoles(self, value: bool): ...

    @property
    def SplineOrder(self) -> int:  # ispline3d_splineorder.html
        """
        ## SplineOrder - Порядок сплайна

        Тип данных: long

        Значения свойства:

        +------------------------------+
        | TRUE  | - сплайн по полюсам, |
        +------------------------------+
        | FALSE | - сплайн по точкам.  |
        +------------------------------+

        Свойство позволяет получить/установить порядок сплайна. Значение порядка кривой должно принадлежать диапазону 3 ... 10.

        ### Синтаксис Automation

        ```
        SplineOrder = iObject.SplineOrder;       # Получить свойство (* )
        iObject.SplineOrder = SplineOrder;       # Установить свойство (* )
        SplineOrder = iObject.GetSplineOrder();  # Получить свойство (**)
        iObject.SetSplineOrder(SplineOrder);     # Установить свойство (**)
        ```
        """
        ...
    @SplineOrder.setter
    def SplineOrder(self, value: int): ...

    @property
    def VertexCount(self) -> int:  # ispline3d_vertexcount.html
        """
        ## VertexCount - Количество вершин

        Тип данных: long

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        VertexCount = iObject.VertexCount;        # Получить свойство (* )
        VertexCount = iObject.GetVertexCount( );  # Получить свойство (**)
        ```
        """
        ...

    @property
    def VertexParamsArray(self) -> typing.Any:  # ispline3d_vertexparamsarray.html
        """
        ## VertexParamsArray - Массив параметров вершин в виде SAFEARRAY DISPATCH - VT_ARRAY | VT_DISPATCH

        Тип данных: VARIANT

        Значения свойства:

        Массив SafeArray типа VT_ARRAY | VT_DISPATCH.

        1. Свойство доступно только для чтения.

        2. Свойство позволяет получить массив SAFEARRAY параметров всех вершин. Это массив VT_DISPATCH, которые можно преобразовать в интерфейсы ICurveVertexParam.

        ### Синтаксис Automation

        ```
        VertexParamsArray = iObject.VertexParamsArray;       # Получить свойство (* )
        VertexParamsArray = iObject.GetVertexParamsArray();  # Получить свойство (**)
        ```
        """
        ...

    def AddVertex(self, Index) -> ICurveVertexParam:  # ispline3d_addvertex.html
        """
        ## AddVertex - Создать новую вершину

        ### Синтаксис Automation

        ```
        LPDISPATCH AddVertex( long Index );
        ```

        ### Возвращаемое значение

        +--------------------------------------------------------------------------+
        | ICurveVertexParam | - указатель на интерфейс параметров вершины ломаной. |
        +--------------------------------------------------------------------------+
        """
        ...

    def CenterPointAssociationObject(self, PointIndex) -> IModelObject:  # ispline3d_centerpointassociationobject.html
        """
        ## CenterPointAssociationObject - Точка привязки центра кривизны

        Тип данных: Указатель на интерфейс IModelObject.

        ### Синтаксис Automation

        ```
        CenterPointAssociationObject = Object.CenterPointAssociationObject( PointIndex );     # Получить свойство (* )
        Object.CenterPointAssociationObject( PointIndex ) = CenterPointAssociationObject;     # Установить свойство (* )
        CenterPointAssociationObject = Object.GetCenterPointAssociationObject( PointIndex );  # Получить свойство (**)
        Object.SetCenterPointAssociationObject( PointIndex, CenterPointAssociationObject )    # Установить свойство (**)
        ```

        ### Входные параметры

        +-----------------------------------+
        | long PointIndex | - индекс точки. |
        +-----------------------------------+
        """
        ...

    def CenterPointParams(self, PointIndex) -> IKompasAPIObject:  # ispline3d_centerpointparams.html
        """
        ## CenterPointParams - Получение параметров точки центра кривизны

        Тип данных: Указатель на интерфейс IKompasAPIObject.

        1. Свойство доступно только для чтения.

        2. В зависимости от типа ISpline3D::CenterPointType, интерфейс параметров должен приводиться к одному из следующих вариантов:

        •ksPDisplace - IPoint3DParamDisplace (интерфейс параметров пространственной точки, заданной по смещению от опорного объекта),

        •ksPIntersect - IPoint3DParamIntersect (интерфейс параметров пространственной точки, заданной на пересечении опорных объектов),

        •ksPCenter - IPoint3DParamCenter (интерфейс параметров пространственной точки, заданной в центре опорного объекта),

        •ksPCurve - IPoint3DParamCurve (интерфейс параметров пространственной точки, заданной на кривой со смещением),

        •ksPSurface - IPoint3DparamSurface (интерфейс параметров пространственной точки, заданной на поверхности),

        •ksPProjection - IPoint3DParamProjection (интерфейс параметров пространственной точки, заданной проецированием).

        ### Синтаксис Automation

        ```
        CenterPointParams = Object.CenterPointParams( PointIndex );     # Получить свойство (* )
        CenterPointParams = Object.GetCenterPointParams( PointIndex );  # Получить свойство (**)
        ```

        ### Входные параметры

        +-----------------------------------+
        | long PointIndex | - индекс точки. |
        +-----------------------------------+
        """
        ...

    def CenterPointType(self, PointIndex) -> int:  # ispline3d_centerpointtype.html
        """
        ## CenterPointType - Установить способ построения точки

        Тип данных: Значение из перечисления ksPoint3DTypeEnum.

        ### Синтаксис Automation

        ```
        CenterPointType = Object.CenterPointType( PointIndex );     # Получить свойство (* )
        Object.CenterPointType( PointIndex ) = CenterPointType;     # Установить свойство (* )
        CenterPointType = Object.GetCenterPointType( PointIndex );  # Получить свойство (**)
        Object.SetCenterPointType( PointIndex, CenterPointType )    # Установить свойство (**)
        ```

        ### Входные параметры

        +-----------------------------------+
        | long PointIndex | - индекс точки. |
        +-----------------------------------+
        """
        ...

    def ClearTangentParameters(self) -> bool:  # ispline3d_cleartangentparameters.html
        """
        ## ClearTangentParameters - Удалить параметры управления во всех точках

        ### Синтаксис Automation

        ```
        BOOL ClearTangentParameters();
        ```

        ### Возвращаемое значение

        +-----------------------------------------+
        | TRUE  | - в случае удачного завершения, |
        +-----------------------------------------+
        | FALSE | - в случае неудачи.             |
        +-----------------------------------------+
        """
        ...

    def ConstraintLimitObject(self, PointIndex) -> IModelObject:  # ispline3d_constraintlimitobject.html
        """
        ## ConstraintLimitObject - Граница

        Тип данных: Указатель на интерфейс IModelObject.

        ### Синтаксис Automation

        ```
        ConstraintLimitObject = Object.ConstraintLimitObject( PointIndex );     # Получить свойство (* )
        Object.ConstraintLimitObject( PointIndex ) = ConstraintLimitObject;     # Установить свойство (* )
        ConstraintLimitObject = Object.GetConstraintLimitObject( PointIndex );  # Получить свойство (**)
        Object.SetConstraintLimitObject( PointIndex, ConstraintLimitObject );   # Установить свойство (**)
        ```

        ### Входные параметры

        +-----------------------------------+
        | long PointIndex | - индекс точки. |
        +-----------------------------------+
        """
        ...

    def ConstraintObject(self, PointIndex) -> IModelObject:  # ispline3d_constraintobject.html
        """
        ## ConstraintObject - Объект сопряжения

        Тип данных: Указатель на интерфейс IModelObject.

        ### Синтаксис Automation

        ```
        ConstraintObject = Object.ConstraintObject( PointIndex );     # Получить свойство (* )
        Object.ConstraintObject( PointIndex ) = ConstraintObject;     # Установить свойство (* )
        ConstraintObject = Object.GetConstraintObject( PointIndex );  # Получить свойство (**)
        Object.SetConstraintObject( PointIndex, ConstraintObject );   # Установить свойство (**)
        ```

        ### Входные параметры

        +-----------------------------------+
        | long PointIndex | - индекс точки. |
        +-----------------------------------+
        """
        ...

    def ConstraintReverse(self, PointIndex) -> bool:  # ispline3d_constraintreverse.html
        """
        ## ConstraintReverse - Противоположное направление

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        ConstraintReverse = Object.ConstraintReverse( PointIndex );     # Получить свойство (* )
        Object.ConstraintReverse( PointIndex ) = ConstraintReverse;     # Установить свойство (* )
        ConstraintReverse = Object.GetConstraintReverse( PointIndex );  # Получить свойство (**)
        Object.SetConstraintReverse( PointIndex, ConstraintReverse );   # Установить свойство (**)
        ```

        ### Входные параметры

        +-----------------------------------+
        | long PointIndex | - индекс точки. |
        +-----------------------------------+
        """
        ...

    def ConstraintType(self, PointIndex) -> int:  # ispline3d_constrainttype.html
        """
        ## ConstraintType - Тип сопряжения в заданной вершине. Управление кривизной

        Тип данных: Значение из перечисления ksNurbsByPointsPointConstraintsEnum.

        ### Синтаксис Automation

        ```
        ConstraintType = Object.ConstraintType( PointIndex );     # Получить свойство (* )
        Object.ConstraintType( PointIndex ) = ConstraintType;     # Установить свойство (* )
        ConstraintType = Object.GetConstraintType( PointIndex );  # Получить свойство (**)
        Object.SetConstraintType( PointIndex, ConstraintType );   # Установить свойство (**)
        ```

        ### Входные параметры

        +-----------------------------------+
        | long PointIndex | - индекс точки. |
        +-----------------------------------+
        """
        ...

    def Curvature(self, PointIndex) -> float:  # ispline3d_curvature.html
        """
        ## Curvature - Величина кривизны в указанной точке

        Тип данных: double

        ### Синтаксис Automation

        ```
        Curvature = Object.Curvature( PointIndex );     # Получить свойство (* )
        Object.Curvature( PointIndex ) = Curvature;     # Установить свойство (* )
        Curvature = Object.GetCurvature( PointIndex );  # Получить свойство (**)
        Object.SetCurvature( PointIndex, Curvature );   # Установить свойство (**)
        ```

        ### Входные параметры

        +-----------------------------------+
        | long PointIndex | - индекс точки. |
        +-----------------------------------+
        """
        ...

    def CurvatureRadius(self, PointIndex) -> float:  # ispline3d_curvatureradius.html
        """
        ## CurvatureRadius - Радиус кривизны

        Тип данных: double

        ### Синтаксис Automation

        ```
        CurvatureRadius = Object.CurvatureRadius( PointIndex );     # Получить свойство (* )
        Object.CurvatureRadius( PointIndex ) = CurvatureRadius;     # Установить свойство (* )
        CurvatureRadius = Object.GetCurvatureRadius( PointIndex );  # Получить свойство (**)
        Object.SetCurvatureRadius( PointIndex, CurvatureRadius );   # Установить свойство (**)
        ```

        ### Входные параметры

        +-----------------------------------+
        | long PointIndex | - индекс точки. |
        +-----------------------------------+
        """
        ...

    def DeleteCenterPointParams(self, PointIndex) -> bool:  # ispline3d_deletecenterpointparams.html
        """
        ## DeleteCenterPointParams - Удалить параметры для заданной точки

        ### Синтаксис Automation

        ```
        BOOL DeleteCenterPointParams( long PointIndex );
        ```

        ### Входные параметры

        +------------------------------+
        | PointIndex | - индекс точки. |
        +------------------------------+

        ### Возвращаемое значение

        +----------------------------------------+
        | TRUE | - в случае удачного завершения. |
        +----------------------------------------+
        """
        ...

    def DeleteVertex(self, Index) -> bool:  # ispline3d_deletevertex.html
        """
        ## DeleteVertex - Удалить вершину с указанным индексом

        ### Синтаксис Automation

        ```
        BOOL DeleteVertex( long Index );
        ```

        ### Входные параметры

        +---------------------------+
        | Index | - индекс вершины. |
        +---------------------------+

        ### Возвращаемое значение

        +-----------------------------------------+
        | TRUE  | - в случае удачного завершения, |
        +-----------------------------------------+
        | FALSE | - в случае неудачи.             |
        +-----------------------------------------+
        """
        ...

    def DirectionVector(self, PointIndex, VectorIndex) -> IVector3D:  # ispline3d_directionvector.html
        """
        ## DirectionVector - Направляющий вектор в точке сопряжения

        Тип данных: Указатель на интерфейс IVector3D.

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        DirectionVector = Object.DirectionVector( PointIndex, VectorIndex, PVal );     # Получить свойство (* )
        DirectionVector = Object.GetDirectionVector( PointIndex, VectorIndex, PVal );  # Получить свойство (**)
        ```

        ### Входные параметры

        +------------------------------------------------------------------------------------------------------------------------+
        | long PointIndex | - индекс точки.                                                                                      |
        +------------------------------------------------------------------------------------------------------------------------+
        | VectorIndex     | - cпособ создания сопряжения в заданной вершине сплайна из перечисления ksTransitionVectorIndexEnum. |
        +------------------------------------------------------------------------------------------------------------------------+
        """
        ...

    def Flush(self) -> bool:  # ispline3d_flush.html
        """
        ## Flush - Очистить массив вершин

        ### Синтаксис Automation

        ```
        BOOL Flush();
        ```

        ### Возвращаемое значение

        +-----------------------------------------+
        | TRUE  | - в случае удачного завершения, |
        +-----------------------------------------+
        | FALSE | - в случае неудачи.             |
        +-----------------------------------------+
        """
        ...

    def GetCenterPoint(self, PointIndex) -> bool:  # ispline3d_getcenterpoint.html
        """
        ## GetCenterPoint - Угол вектора производной в указанной точке

        ### Синтаксис Automation

        ```
        BOOL GetCenterPoint( long PointIndex, double * X, double * Y, double * Z );
        ```

        ### Входные параметры

        +------------------------------+
        | PointIndex | - индекс точки. |
        +------------------------------+

        ### Выходные параметры

        +-------------------------------+
        | X, Y, Z | - координаты точки. |
        +-------------------------------+

        ### Возвращаемое значение

        +----------------------------------------+
        | TRUE | - в случае удачного завершения. |
        +----------------------------------------+
        """
        ...

    def GetVector(self, PointIndex, VectorIndex) -> bool:  # ispline3d_getvector.html
        """
        ## GetVector - Получить направление касательного вектора (в сопаряжении)

        ### Синтаксис Automation

        ```
        BOOL GetVector( long PointIndex, ksTransitionVectorIndexEnum VectorIndex, double * X, double * Y, double * Z );
        ```

        ### Входные параметры

        +--------------------------------------------------------------------------------------------------------------------+
        | PointIndex  | - индекс точки,                                                                                      |
        +--------------------------------------------------------------------------------------------------------------------+
        | VectorIndex | - cпособ создания сопряжения в заданной вершине сплайна из перечисления ksTransitionVectorIndexEnum. |
        +--------------------------------------------------------------------------------------------------------------------+

        ### Выходные параметры

        +---------------------------------------------+
        | X, Y, Z | - координаты направления вектора. |
        +---------------------------------------------+

        ### Возвращаемое значение

        +----------------------------------------+
        | TRUE | - в случае удачного завершения. |
        +----------------------------------------+
        """
        ...

    def GetVertexCoordinatesArray(self) -> bool:  # ispline3d_getvertexcoordinatesarray.html
        """
        ## GetVertexCoordinatesArray - Получить массив координат и весов вершин в виде SAFEARRAY double - VT_ARRAY | VT_R8

        1. Параметры Coordinates и Weights не являются обязательными. В функцию достаточно передать один из указателей на VARIANT.

        2. Массивы являются согласованными:

        Координаты точек в массиве Coordinates лежат в следующей последовательности: x0, y0, z0, x1, y1, z1 ...xi, yi, zi,

        Веса точек в массиве Weights лежат в последовательности: weight0, weight1, ...weighti.

        ### Синтаксис Automation

        ```
        BOOL GetVertexCoordinatesArray( VARIANT * Coordinates,
        VARIANT * Weights );
        ```

        ### Входные параметры

        +-------------------------------------------------------------------------------------+
        | Coordinates | - массив SafeArray типа VT_ARRAY | VT_R8 координат точек всех вершин, |
        +-------------------------------------------------------------------------------------+
        | Weights     | - массив SafeArray типа VT_ARRAY | VT_R8 весов каждой из всех вершин. |
        +-------------------------------------------------------------------------------------+

        ### Возвращаемое значение

        +-----------------------------------------+
        | TRUE  | - в случае удачного завершения, |
        +-----------------------------------------+
        | FALSE | - в случае неудачи.             |
        +-----------------------------------------+
        """
        ...

    def InvertVector(self, PointIndex, VectorIndex) -> bool:  # ispline3d_invertvector.html
        """
        ## InvertVector - Сменить направление касательного вектора на обратное (в сопряжении)

        ### Синтаксис Automation

        ```
        BOOL InvertVector( long PointIndex, ksTransitionVectorIndexEnum VectorIndex );
        ```

        ### Входные параметры

        +--------------------------------------------------------------------------------------------------------------------+
        | PointIndex  | - индекс точки,                                                                                      |
        +--------------------------------------------------------------------------------------------------------------------+
        | VectorIndex | - cпособ создания сопряжения в заданной вершине сплайна из перечисления ksTransitionVectorIndexEnum. |
        +--------------------------------------------------------------------------------------------------------------------+

        ### Возвращаемое значение

        +----------------------------------------+
        | TRUE | - в случае удачного завершения. |
        +----------------------------------------+
        """
        ...

    def ReadFromFile(self, FileName) -> bool:  # ispline3d_readfromfile.html
        """
        ## ReadFromFile - Прочитать параметры вершин из текстового файла

        См. также ISpline3D::WriteToFile.

        ### Синтаксис Automation

        ```
        BOOL ReadFromFile( BSTR fileName );
        ```

        ### Входные параметры

        +-------------------------+
        | fileName | - имя файла. |
        +-------------------------+

        ### Возвращаемое значение

        +-----------------------------------------+
        | TRUE  | - в случае удачного завершения, |
        +-----------------------------------------+
        | FALSE | - в случае неудачи.             |
        +-----------------------------------------+
        """
        ...

    def SetCenterPoint(self, PointIndex, X, Y, Z) -> bool:  # ispline3d_setcenterpoint.html
        """
        ## SetCenterPoint - Угол вектора производной в указанной точке

        ### Синтаксис Automation

        ```
        BOOL SetCenterPoint( long PointIndex, double X, double Y, double Z );
        ```

        ### Входные параметры

        +----------------------------------+
        | PointIndex | - индекс точки,     |
        +----------------------------------+
        | X, Y, Z    | - координаты точки. |
        +----------------------------------+

        ### Возвращаемое значение

        +----------------------------------------+
        | TRUE | - в случае удачного завершения. |
        +----------------------------------------+
        """
        ...

    def SetCenterPointAssociationObject(self, PointIndex, arg1):
        ...

    def SetCenterPointType(self, PointIndex, arg1):
        ...

    def SetConstraintLimitObject(self, PointIndex, arg1):
        ...

    def SetConstraintObject(self, PointIndex, arg1):
        ...

    def SetConstraintReverse(self, PointIndex, arg1):
        ...

    def SetConstraintType(self, PointIndex, arg1):
        ...

    def SetCurvature(self, PointIndex, arg1):
        ...

    def SetCurvatureRadius(self, PointIndex, arg1):
        ...

    def SetSplineTransitionType(self, PointIndex, arg1):
        ...

    def SetTangentVectorLenght(self, PointIndex, arg1):
        ...

    def SetVector(self, PointIndex, VectorIndex, X, Y, Z) -> bool:  # ispline3d_setvector.html
        """
        ## SetVector - Установить направление касательного вектора

        ### Синтаксис Automation

        ```
        BOOL SetVector( long PointIndex, ksTransitionVectorIndexEnum VectorIndex, double X, double Y, double Z );
        ```

        ### Входные параметры

        +--------------------------------------------------------------------------------------------------------------------+
        | PointIndex  | - индекс точки,                                                                                      |
        +--------------------------------------------------------------------------------------------------------------------+
        | VectorIndex | - cпособ создания сопряжения в заданной вершине сплайна из перечисления ksTransitionVectorIndexEnum. |
        +--------------------------------------------------------------------------------------------------------------------+
        | X, Y, Z     | - координаты направления вектора.                                                                    |
        +--------------------------------------------------------------------------------------------------------------------+

        ### Возвращаемое значение

        +----------------------------------------+
        | TRUE | - в случае удачного завершения. |
        +----------------------------------------+
        """
        ...

    def SetVectorDirectionObject(self, PointIndex, VectorIndex, arg2):
        ...

    def SetVectorOrientation(self, PointIndex, VectorIndex, arg2):
        ...

    def SplineTransitionType(self, PointIndex) -> int:  # ispline3d_splinetransitiontype.html
        """
        ## SplineTransitionType - Способ создания сопряжения в заданной вершине

        Тип данных: Значение из перечисления ksSplineTransitionTypeEnum.

        ### Синтаксис Automation

        ```
        SplineTransitionType = Object.SplineTransitionType( PointIndex );     # Получить свойство (* )
        Object.SplineTransitionType( PointIndex ) = SplineTransitionType;     # Установить свойство (* )
        SplineTransitionType = Object.GetSplineTransitionType( PointIndex );  # Получить свойство (**)
        Object.SetSplineTransitionType( PointIndex, SplineTransitionType );   # Установить свойство (**)
        ```

        ### Входные параметры

        +-----------------------------------+
        | long PointIndex | - индекс точки. |
        +-----------------------------------+
        """
        ...

    def TangentVectorLenght(self, PointIndex) -> float:  # ispline3d_tangentvectorlenght.html
        """
        ## TangentVectorLenght - Длина касательного вектора

        Тип данных: double

        ### Синтаксис Automation

        ```
        TangentVectorLenght = Object.TangentVectorLenght( PointIndex );     # Получить свойство (* )
        Object.TangentVectorLenght( PointIndex ) = TangentVectorLenght;     # Установить свойство (* )
        TangentVectorLenght = Object.GetTangentVectorLenght( PointIndex );  # Получить свойство (**)
        Object.SetTangentVectorLenght( PointIndex, TangentVectorLenght );   # Установить свойство (**)
        ```

        ### Входные параметры

        +-----------------------------------+
        | long PointIndex | - индекс точки. |
        +-----------------------------------+
        """
        ...

    def VectorDirectionObject(self, PointIndex, VectorIndex) -> IModelObject:  # ispline3d_vectordirectionobject.html
        """
        ## VectorDirectionObject - Направляющий объект вектора в точке сопряжения

        Тип данных: Указатель на интерфейс IModelObject.

        ### Синтаксис Automation

        ```
        VectorDirectionObject = Object.VectorDirectionObject( PointIndex, VectorIndex );     # Получить свойство (* )
        Object.VectorDirectionObject( PointIndex, VectorIndex ) = VectorDirectionObject;     # Установить свойство (* )
        VectorDirectionObject = Object.GetVectorDirectionObject( PointIndex, VectorIndex );  # Получить свойство (**)
        Object.SetVectorDirectionObject( PointIndex, VectorIndex, VectorDirectionObject );   # Установить свойство (**)
        ```

        ### Входные параметры

        +------------------------------------------------------------------------------------------------------------------------+
        | long PointIndex | - индекс точки.                                                                                      |
        +------------------------------------------------------------------------------------------------------------------------+
        | VectorIndex     | - cпособ создания сопряжения в заданной вершине сплайна из перечисления ksTransitionVectorIndexEnum. |
        +------------------------------------------------------------------------------------------------------------------------+
        """
        ...

    def VectorOrientation(self, PointIndex, VectorIndex) -> bool:  # ispline3d_vectororientation.html
        """
        ## VectorOrientation - Направление вектора в точке сопряжения

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        VectorOrientation = Object.VectorOrientation( PointIndex, VectorIndex );     # Получить свойство (* )
        Object.VectorOrientation( PointIndex, VectorIndex ) = VectorOrientation;     # Установить свойство (* )
        VectorOrientation = Object.GetVectorOrientation( PointIndex, VectorIndex );  # Получить свойство (**)
        Object.SetVectorOrientation( PointIndex, VectorIndex, VectorOrientation );   # Установить свойство (**)
        ```

        ### Входные параметры

        +------------------------------------------------------------------------------------------------------------------------+
        | long PointIndex | - индекс точки.                                                                                      |
        +------------------------------------------------------------------------------------------------------------------------+
        | VectorIndex     | - cпособ создания сопряжения в заданной вершине сплайна из перечисления ksTransitionVectorIndexEnum. |
        +------------------------------------------------------------------------------------------------------------------------+

        В Компас 18.1 удалены свойства:

        •SplineTangent

        •TangentCurve

        •VectorLenght
        """
        ...

    def VertexParams(self, Index) -> ICurveVertexParam:  # ispline3d_vertexparams.html
        """
        ## VertexParams - Получение параметров вершины ломаной

        Тип данных: Указатель на интерфейс параметров вершины ломаной ICurveVertexParam

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        VertexParams = iObject.VertexParams( Index );     # Получить свойство (* )
        VertexParams = iObject.GetVertexParams( Index );  # Получить свойство (**)
        ```

        ### Входные параметры

        +---------------------------+
        | Index | - индекс вершины. |
        +---------------------------+
        """
        ...

    def WriteToFile(self, FileName) -> bool:  # ispline3d_writetofile.html
        """
        ## WriteToFile - Записать параметры вершин в текстовый файл

        См. также ISpline3D::ReadFromFile.

        В Компас 18.1 удалены методы:

        •SetTangentVector

        •GetTangentVector

        •Invert

        ### Синтаксис Automation

        ```
        BOOL WriteToFile( BSTR fileName );
        ```

        ### Входные параметры

        +-------------------------+
        | fileName | - имя файла. |
        +-------------------------+

        ### Возвращаемое значение

        +-----------------------------------------+
        | TRUE  | - в случае удачного завершения, |
        +-----------------------------------------+
        | FALSE | - в случае неудачи.             |
        +-----------------------------------------+
        """
        ...




class ISpline3DSmoothingParams(IDispatch):  # ispline3dsmoothingparams.html
    """
    ## Интерфейс ISpline3DSmoothingParams

    Интерфейс параметров сглаживания сплайна.
    """

    @property
    def MaxDeviation(self) -> float:  # ispline3dsmoothingparams_maxdeviation.html
        """
        ## MaxDeviation - Максимальное отклонение кривой от вершины

        Тип данных: double

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        MaxDeviation = Object.MaxDeviation       # Получить свойство (* )
        MaxDeviation = Object.GetMaxDeviation()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def MaxDeviationIndex(self) -> float:  # ispline3dsmoothingparams_maxdeviationindex.html
        """
        ## MaxDeviationIndex - Индекс вершины, максимально отклоненной от кривой

        Тип данных: double

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        MaxDeviationIndex = Object.MaxDeviationIndex       # Получить свойство (* )
        MaxDeviationIndex = Object.GetMaxDeviationIndex()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def SmoothingAccuracy(self) -> float:  # ispline3dsmoothingparams_smoothingaccuracy.html
        """
        ## SmoothingAccuracy - Точность сглаживания

        Тип данных: double

        ### Синтаксис Automation

        ```
        SmoothingAccuracy = Object.SmoothingAccuracy       # Получить свойство (* )
        Object.SmoothingAccuracy = SmoothingAccuracy       # Установить свойство (* )
        SmoothingAccuracy = Object.GetSmoothingAccuracy()  # Получить свойство (**)
        Object.SetSmoothingAccuracy( SmoothingAccuracy )   # Установить свойство (**)
        ```
        """
        ...
    @SmoothingAccuracy.setter
    def SmoothingAccuracy(self, value: float): ...

    @property
    def SmoothingOn(self) -> bool:  # ispline3dsmoothingparams_smoothingon.html
        """
        ## SmoothingOn - Включить сглаживание

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        SmoothingOn = Object.SmoothingOn       # Получить свойство (* )
        Object.SmoothingOn = SmoothingOn       # Установить свойство (* )
        SmoothingOn = Object.GetSmoothingOn()  # Получить свойство (**)
        Object.SetSmoothingOn( SmoothingOn )   # Установить свойство (**)
        ```
        """
        ...
    @SmoothingOn.setter
    def SmoothingOn(self, value: bool): ...




class ISpline3DMetaSplineParam(IDispatch):  # ispline3dmetasplineparam.html
    """
    ## Интерфейс ISpline3DMetaSplineParam

    Интерфейс параметров построения метасплайна.

    Интерфейс является дополнительным для интерфейса ISpline3D.

    Версия КОМПАС v22
    """

    @property
    def ApproximationType(self) -> int:  # ispline3dmetasplineparam_approximationtype.html
        """
        ## ApproximationType - Способ аппроксимации

        Тип данных: из перечисления ksMetaSplineApproximationTypeEnum

        ### Синтаксис Automation

        ```
        ApproximationType = Object.ApproximationType       # Получить свойство (* )
        Object.ApproximationType = ApproximationType       # Установить свойство (* )
        ApproximationType = Object.GetApproximationType()  # Получить свойство (**)
        Object.SetApproximationType( ApproximationType )   # Установить свойство (**)
        ```
        """
        ...
    @ApproximationType.setter
    def ApproximationType(self, value: int): ...

    @property
    def DensityDegree(self) -> int:  # ispline3dmetasplineparam_densitydegree.html
        """
        ## DensityDegree - Уплотнение

        Тип данных: из перечисления ksMetaSplineDensityDegreeEnum

        ### Синтаксис Automation

        ```
        DensityDegree = Object.DensityDegree       # Получить свойство (* )
        Object.DensityDegree = DensityDegree       # Установить свойство (* )
        DensityDegree = Object.GetDensityDegree()  # Получить свойство (**)
        Object.SetDensityDegree( DensityDegree )   # Установить свойство (**)
        ```
        """
        ...
    @DensityDegree.setter
    def DensityDegree(self, value: int): ...

    @property
    def Elevate(self) -> bool:  # ispline3dmetasplineparam_elevate.html
        """
        ## Elevate - Повышенный порядок кривой Безье

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        Elevate = Object.Elevate       # Получить свойство (* )
        Object.Elevate = Elevate       # Установить свойство (* )
        Elevate = Object.GetElevate()  # Получить свойство (**)
        Object.SetElevate( Elevate )   # Установить свойство (**)
        ```
        """
        ...
    @Elevate.setter
    def Elevate(self, value: bool): ...

    @property
    def FairSmoothing(self) -> int:  # ispline3dmetasplineparam_fairsmoothing.html
        """
        ## FairSmoothing - Сглаживание

        Тип данных: из перечисления ksMetaSplineSmoothingEnum

        ### Синтаксис Automation

        ```
        FairSmoothing = Object.FairSmoothing       # Получить свойство (* )
        Object.FairSmoothing = FairSmoothing       # Установить свойство (* )
        FairSmoothing = Object.GetFairSmoothing()  # Получить свойство (**)
        Object.SetFairSmoothing( FairSmoothing )   # Установить свойство (**)
        ```
        """
        ...
    @FairSmoothing.setter
    def FairSmoothing(self, value: int): ...

    @property
    def Harmony(self) -> bool:  # ispline3dmetasplineparam_harmony.html
        """
        ## Harmony - Гармонизация распределения точек по кривой

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        Harmony = Object.Harmony       # Получить свойство (* )
        Object.Harmony = Harmony       # Установить свойство (* )
        Harmony = Object.GetHarmony()  # Получить свойство (**)
        Object.SetHarmony( Harmony )   # Установить свойство (**)
        ```
        """
        ...
    @Harmony.setter
    def Harmony(self, value: bool): ...

    @property
    def MetaSplineBuildingType(self) -> int:  # ispline3dmetasplineparam_metasplinebuildingtype.html
        """
        ## MetaSplineBuildingType - Способ построения кривой

        Тип данных: из перечисления ksMetaSplineBuildingTypeEnum

        ### Синтаксис Automation

        ```
        MetaSplineBuildingType = Object.MetaSplineBuildingType       # Получить свойство (* )
        Object.MetaSplineBuildingType = MetaSplineBuildingType       # Установить свойство (* )
        MetaSplineBuildingType = Object.GetMetaSplineBuildingType()  # Получить свойство (**)
        Object.SetMetaSplineBuildingType( MetaSplineBuildingType )   # Установить свойство (**)
        ```
        """
        ...
    @MetaSplineBuildingType.setter
    def MetaSplineBuildingType(self, value: int): ...

    @property
    def SmoothingMethod(self) -> int:  # ispline3dmetasplineparam_smoothingmethod.html
        """
        ## SmoothingMethod - Метод сглаживания

        Тип данных: из перечисления ksSmoothingMethodEnum

        ### Синтаксис Automation

        ```
        SmoothingMethod = Object.SmoothingMethod       # Получить свойство (* )
        Object.SmoothingMethod = SmoothingMethod       # Установить свойство (* )
        SmoothingMethod = Object.GetSmoothingMethod()  # Получить свойство (**)
        Object.SetSmoothingMethod( SmoothingMethod )   # Установить свойство (**)
        ```
        """
        ...
    @SmoothingMethod.setter
    def SmoothingMethod(self, value: int): ...




class ISplineOnSurface(IModelObject):  # isplineonsurface.html
    """
    ## Интерфейс ISplineOnSurface

    Сплайн на поверхности.

    Интерфейс можно получить у коллекции сплайнов по поверхностям с помощью свойства ISplinesOnSurfaces::SplineOnSurface и метода ISplinesOnSurfaces::Add.
    """

    @property
    def Closed(self) -> bool:  # isplineonsurface_closed.html
        """
        ## Closed - Замкнутость

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        Closed = Object.Closed       # Получить свойство (* )
        Object.Closed = Closed       # Установить свойство (* )
        Closed = Object.GetClosed()  # Получить свойство (**)
        Object.SetClosed( Closed )   # Установить свойство (**)
        ```
        """
        ...
    @Closed.setter
    def Closed(self, value: bool): ...

    @property
    def SplineOnPoles(self) -> bool:  # isplineonsurface_splineonpoles.html
        """
        ## SplineOnPoles - Тип построения

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        SplineOnPoles = Object.SplineOnPoles       # Получить свойство (* )
        Object.SplineOnPoles = SplineOnPoles       # Установить свойство (* )
        SplineOnPoles = Object.GetSplineOnPoles()  # Получить свойство (**)
        Object.SetSplineOnPoles( SplineOnPoles )   # Установить свойство (**)
        ```
        """
        ...
    @SplineOnPoles.setter
    def SplineOnPoles(self, value: bool): ...

    @property
    def SplineOrder(self) -> int:  # isplineonsurface_splineorder.html
        """
        ## SplineOrder - Порядок сплайна

        Тип данных: long

        ### Синтаксис Automation

        ```
        SplineOrder = Object.SplineOrder       # Получить свойство (* )
        Object.SplineOrder = SplineOrder       # Установить свойство (* )
        SplineOrder = Object.GetSplineOrder()  # Получить свойство (**)
        Object.SetSplineOrder( SplineOrder )   # Установить свойство (**)
        ```
        """
        ...
    @SplineOrder.setter
    def SplineOrder(self, value: int): ...

    @property
    def SurfaceObject(self) -> IModelObject:  # isplineonsurface_surfaceobject.html
        """
        ## SurfaceObject - Установить поверхность

        Тип данных: Указатель на интерфейс IModelObject

        ### Синтаксис Automation

        ```
        SurfaceObject = Object.SurfaceObject       # Получить свойство (* )
        Object.SurfaceObject = SurfaceObject       # Установить свойство (* )
        SurfaceObject = Object.GetSurfaceObject()  # Получить свойство (**)
        Object.SetSurfaceObject( SurfaceObject )   # Установить свойство (**)
        ```
        """
        ...
    @SurfaceObject.setter
    def SurfaceObject(self, value: IModelObject): ...

    @property
    def VertexCount(self) -> int:  # isplineonsurface_vertexcount.html
        """
        ## VertexCount - Получить количество вершин

        Тип данных: long

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        VertexCount = Object.VertexCount       # Получить свойство (* )
        VertexCount = Object.GetVertexCount()  # Получить свойство (**)
        ```
        """
        ...

    def AddPoint(self, IndexAt, X, Y, Z, W, AssociationObject) -> bool:  # isplineonsurface_addpoint.html
        """
        ## AddPoint - Создание новой вершины

        ### Синтаксис Automation

        ```
        BOOL AddPoint( long IndexAt, double X, double Y, double Z, double W, IModelObject * AssociationObject );
        ```

        ### Входные параметры

        +----------------------------------------------+
        | IndexAt           | - индекс вершины,        |
        +----------------------------------------------+
        | X                 | - координата по X,       |
        +----------------------------------------------+
        | Y                 | - координата по Y,       |
        +----------------------------------------------+
        | Z                 | - координата по Z        |
        +----------------------------------------------+
        | W                 | - вес,                   |
        +----------------------------------------------+
        | AssociationObject | - ассоциативная вершина. |
        +----------------------------------------------+

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def AddVertex(self, IndexAt, U, V, W, AssociationObject) -> bool:  # isplineonsurface_addvertex.html
        """
        ## AddVertex - Создание новой вершины

        ### Синтаксис Automation

        ```
        BOOL AddVertex( long IndexAt,
        double U,
        double V,
        double W,
        IModelObject * AssociationObject );
        ```

        ### Входные параметры

        +---------------------------------------------------------------------------+
        | IndexAt           | - индекс вершины, перед которой нужно добавить новую, |
        +---------------------------------------------------------------------------+
        | U                 | - координата по U в %,                                |
        +---------------------------------------------------------------------------+
        | V                 | - координата по V в %,                                |
        +---------------------------------------------------------------------------+
        | W                 | - вес,                                                |
        +---------------------------------------------------------------------------+
        | AssociationObject | - ассоциативная вершина.                              |
        +---------------------------------------------------------------------------+

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def AssociationObject(self, Index) -> IModelObject:  # isplineonsurface_associationobject.html
        """
        ## AssociationObject - Установить опорный объект для вершины

        Тип данных: Указатель на интерфейс IModelObject

        ### Синтаксис Automation

        ```
        AssociationObject = Object.AssociationObject( Index )     # Получить свойство (* )
        Object.AssociationObject( Index ) = AssociationObject     # Установить свойство (* )
        AssociationObject = Object.GetAssociationObject( Index )  # Получить свойство (**)
        Object.SetAssociationObject( Index, AssociationObject )   # Установить свойство (**)
        ```

        ### Входные параметры

        long Index - индекс вершины.
        """
        ...

    def Clear(self) -> bool:  # isplineonsurface_clear.html
        """
        ## Clear - Очистить массив вершин

        ### Синтаксис Automation

        ```
        BOOL Clear();
        ```

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def ClearTangentParameters(self) -> bool:  # isplineonsurface_cleartangentparameters.html
        """
        ## ClearTangentParameters - Удалить параметры управления во всех точках

        ### Синтаксис Automation

        ```
        BOOL ClearTangentParameters();
        ```

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def DeleteVertex(self, Index) -> bool:  # isplineonsurface_deletevertex.html
        """
        ## DeleteVertex - Удалить вершину с указанным индексом

        ### Синтаксис Automation

        ```
        BOOL DeleteVertex( long Index );
        ```

        ### Входные параметры

        +---------------------------+
        | Index | - индекс вершины. |
        +---------------------------+

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def GetPoint(self, Index) -> bool:  # isplineonsurface_getpoint.html
        """
        ## GetPoint - Получить координаты и вес вершины

        ### Синтаксис Automation

        ```
        BOOL GetPoint( long Index, double * X, double * Y, double * Z, double * W );
        ```

        ### Входные параметры

        +---------------------------+
        | Index | - индекс вершины. |
        +---------------------------+

        ### Выходные параметры

        +------------------------+
        | X | - координата по X, |
        +------------------------+
        | Y | - координата по Y, |
        +------------------------+
        | Z | - координата по Z, |
        +------------------------+
        | W | - вес.             |
        +------------------------+

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def GetTangentVector(self, Index) -> bool:  # isplineonsurface_gettangentvector.html
        """
        ## GetTangentVector - Получить направление касательного вектора

        ### Синтаксис Automation

        ```
        BOOL GetTangentVector( long Index, double * X, double * Y, double * Z );
        ```

        ### Входные параметры

        +---------------------------+
        | Index | - индекс вершины. |
        +---------------------------+

        ### Выходные параметры

        +------------------------+
        | X | - координата по X, |
        +------------------------+
        | Y | - координата по Y, |
        +------------------------+
        | Z | - координата по Z. |
        +------------------------+

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def GetVertex(self, Index) -> bool:  # isplineonsurface_getvertex.html
        """
        ## GetVertex - Получить координаты UV и вес вершины

        ### Синтаксис Automation

        ```
        BOOL GetVertex( long Index, double * U, double * V, double * W );
        ```

        ### Входные параметры

        +---------------------------+
        | Index | - индекс вершины. |
        +---------------------------+

        ### Выходные параметры

        +----------------------------+
        | U | - координата по U в %, |
        +----------------------------+
        | V | - координата по V в %, |
        +----------------------------+
        | W | - вес.                 |
        +----------------------------+

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def GetVertexParams(self) -> bool:  # isplineonsurface_getvertexparams.html
        """
        ## GetVertexParams - Массив параметров UV и весов вершин в виде SAFEARRAY double - VT_ARRAY | VT_R8

        ### Синтаксис Automation

        ```
        BOOL GetVertexParams( VARIANT * UV, VARIANT * Points, VARIANT * Weights );
        ```

        ### Выходные параметры

        +-------------------------------------------------------+
        | UV      | - массив VT_ARRAY | VT_R8 параметров UV,    |
        +-------------------------------------------------------+
        | Points  | - массив VT_ARRAY | VT_R8 координат вершин, |
        +-------------------------------------------------------+
        | Weights | - массив VT_ARRAY | VT_R8 весов вершин.     |
        +-------------------------------------------------------+

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def Invert(self, Index) -> bool:  # isplineonsurface_invert.html
        """
        ## Invert - Сменить направление касательного вектора на обратное

        ### Синтаксис Automation

        ```
        BOOL Invert( long Index );
        ```

        ### Входные параметры

        +---------------------------+
        | Index | - индекс вершины. |
        +---------------------------+

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def Reverse(self, Index) -> bool:  # isplineonsurface_reverse.html
        """
        ## Reverse - Противоположное направление

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        Reverse = Object.Reverse( Index )     # Получить свойство (* )
        Object.Reverse( Index ) = Reverse     # Установить свойство (* )
        Reverse = Object.GetReverse( Index )  # Получить свойство (**)
        Object.SetReverse( Index, Reverse )   # Установить свойство (**)
        ```

        ### Входные параметры

        long Index - индекс вершины.

        Свойство позволяет устанавливать и получать признак того, что направление изменено на противоположное.
        """
        ...

    def SetAssociationObject(self, Index, arg1):
        ...

    def SetPoint(self, Index, X, Y, Z, W) -> bool:  # isplineonsurface_setpoint.html
        """
        ## SetPoint - Установить координаты и вес вершины

        ### Синтаксис Automation

        ```
        BOOL SetPoint( long Index, double X, double Y, double Z, double W );
        ```

        ### Входные параметры

        +------------------------+
        | X | - координата по X, |
        +------------------------+
        | Y | - координата по Y, |
        +------------------------+
        | Z | - координата по Z, |
        +------------------------+
        | W | - вес.             |
        +------------------------+

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def SetReverse(self, Index, arg1):
        ...

    def SetSplineTangent(self, Index, arg1):
        ...

    def SetTangentCurve(self, Index, arg1):
        ...

    def SetTangentVector(self, Index, X, Y, Z) -> bool:  # isplineonsurface_settangentvector.html
        """
        ## SetTangentVector - Установить направление касательного вектора

        ### Синтаксис Automation

        ```
        BOOL SetTangentVector( long Index, double X, double Y, double Z );
        ```

        ### Входные параметры

        +----------------------------+
        | Index | - индекс вершины,  |
        +----------------------------+
        | X     | - координата по X, |
        +----------------------------+
        | Y     | - координата по Y, |
        +----------------------------+
        | Z     | - координата по Z. |
        +----------------------------+

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def SetVectorLenght(self, Index, arg1):
        ...

    def SetVertex(self, Index, U, V, W) -> bool:  # isplineonsurface_setvertex.html
        """
        ## SetVertex - Установить координаты UV и вес вершины

        ### Синтаксис Automation

        ```
        BOOL SetVertex( long Index, double U, double V, double W );
        ```

        ### Входные параметры

        +--------------------------------+
        | Index | - индекс вершины,      |
        +--------------------------------+
        | U     | - координата по U в %, |
        +--------------------------------+
        | V     | - координата по V в %, |
        +--------------------------------+
        | W     | - вес.                 |
        +--------------------------------+

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def SplineTangent(self, Index) -> int:  # isplineonsurface_splinetangent.html
        """
        ## SplineTangent - Тип касательного вектора в вершине

        Тип данных: из перечисления ksSplineTangentEnum

        ### Синтаксис Automation

        ```
        SplineTangent = Object.SplineTangent( Index )     # Получить свойство (* )
        Object.SplineTangent( Index ) = SplineTangent     # Установить свойство (* )
        SplineTangent = Object.GetSplineTangent( Index )  # Получить свойство (**)
        Object.SetSplineTangent( Index, SplineTangent )   # Установить свойство (**)
        ```

        ### Входные параметры

        long Index - индекс вершины.
        """
        ...

    def TangentCurve(self, Index) -> IModelObject:  # isplineonsurface_tangentcurve.html
        """
        ## TangentCurve - Касательная кривая

        Тип данных: Указатель на интерфейс IModelObject

        ### Синтаксис Automation

        ```
        TangentCurve = Object.TangentCurve( Index )     # Получить свойство (* )
        Object.TangentCurve( Index ) = TangentCurve     # Установить свойство (* )
        TangentCurve = Object.GetTangentCurve( Index )  # Получить свойство (**)
        Object.SetTangentCurve( Index, TangentCurve )   # Установить свойство (**)
        ```

        ### Входные параметры

        long Index - индекс вершины.
        """
        ...

    def VectorLenght(self, Index) -> float:  # isplineonsurface_vectorlenght.html
        """
        ## VectorLenght - Длина вектора

        Тип данных: double

        ### Синтаксис Automation

        ```
        VectorLenght = Object.VectorLenght( Index )     # Получить свойство (* )
        Object.VectorLenght( Index ) = VectorLenght     # Установить свойство (* )
        VectorLenght = Object.GetVectorLenght( Index )  # Получить свойство (**)
        Object.SetVectorLenght( Index, VectorLenght )   # Установить свойство (**)
        ```

        ### Входные параметры

        long Index - индекс вершины.
        """
        ...




class ISurfacesIntersectionCurve(IModelObject):  # isurfacesintersectioncurve.html
    """
    ## Интерфейс ISurfacesIntersectionCurve

    Интерфейс кривых пересечений поверхностей.

    Интерфейс позволяет создать кривую пересечения двух поверхностей, или двух наборов поверхностей, или поверхности и набора.

    Интерфейс можно получить с помощью свойства ISurfacesIntersectionCurves::SurfacesIntersectionCurve и метода ISurfacesIntersectionCurves::Add.
    """

    @property
    def AutoCheck(self) -> bool:  # isurfacesintersectioncurve_autocheck.html
        """
        ## AutoCheck - Включение/выключение автоматического выбора всех массивов ребер

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        AutoCheck = Object.AutoCheck       # Получить свойство (* )
        Object.AutoCheck = AutoCheck       # Установить свойство (* )
        AutoCheck = Object.GetAutoCheck()  # Получить свойство (**)
        Object.SetAutoCheck( AutoCheck )   # Установить свойство (**)
        ```
        """
        ...
    @AutoCheck.setter
    def AutoCheck(self, value: bool): ...

    @property
    def EdgesArraysCount(self) -> int:  # isurfacesintersectioncurve_edgesarrayscount.html
        """
        ## EdgesArraysCount - Количество массивов ребер в кривой пересечения поверхностей

        Тип данных: long

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        EdgesArraysCount = Object.EdgesArraysCount       # Получить свойство (* )
        EdgesArraysCount = Object.GetEdgesArraysCount()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def EdgesChecks(self) -> list[bool]:  # isurfacesintersectioncurve_edgeschecks.html
        """
        ## EdgesChecks - Признаки выбора массивов ребер

        Тип данных: VARIANT типа VT_ARRAY | VT_BOOL

        ### Синтаксис Automation

        ```
        EdgesChecks = Object.EdgesChecks       # Получить свойство (* )
        Object.EdgesChecks = EdgesChecks       # Установить свойство (* )
        EdgesChecks = Object.GetEdgesChecks()  # Получить свойство (**)
        Object.SetEdgesChecks( EdgesChecks )   # Установить свойство (**)
        ```
        """
        ...
    @EdgesChecks.setter
    def EdgesChecks(self, value: list[bool]): ...

    @property
    def EdgesChecksCount(self) -> int:  # isurfacesintersectioncurve_edgescheckscount.html
        """
        ## EdgesChecksCount - Количество доступных для создания контуров

        Тип данных: long

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        EdgesChecksCount = Object.EdgesChecksCount       # Получить свойство (* )
        EdgesChecksCount = Object.GetEdgesChecksCount()  # Получить свойство (**)
        ```
        """
        ...

    def AddObjects(self, ForArray1, Objects) -> bool:  # isurfacesintersectioncurve_addobjects.html
        """
        ## AddObjects - Добавить элемент(ы) в указанный набор

        Если в набор добавляется одна грань, то ее можно передать как VT_DISPATCH.

        Если в набор добавляется несколько граней, то нужно передать массив VT_ARRAY | VT_DISPATCH.

        ### Синтаксис Automation

        ```
        BOOL AddObjects( BOOL ForArray1, VARIANT * Objects );
        ```

        ### Выходные параметры

        +------------------------------------------------------------+
        | ForArray1 | - TRUE - первый набор, - FALSE - второй набор, |
        +------------------------------------------------------------+
        | Objects   | - набор граней.                                |
        +------------------------------------------------------------+

        ### Возвращаемое значение

        +--------------------------+
        | TRUE | - в случае удачи. |
        +--------------------------+
        """
        ...

    def Clear(self, ForArray1) -> bool:  # isurfacesintersectioncurve_clear.html
        """
        ## Clear - Очистить указанный набор объектов

        ### Синтаксис Automation

        ```
        BOOL Clear( BOOL ForArray1 );
        ```

        ### Выходные параметры

        +------------------------------------------------------------+
        | ForArray1 | - TRUE - первый набор, - FALSE - второй набор. |
        +------------------------------------------------------------+

        ### Возвращаемое значение

        +--------------------------+
        | TRUE | - в случае удачи. |
        +--------------------------+
        """
        ...

    def Edges(self, Index) -> typing.Any:  # isurfacesintersectioncurve_edges.html
        """
        ## Edges - Количество массивов ребер в кривой пересечения поверхностей

        Тип данных: VARIANT

        Свойство доступно только для чтения.

        В зависимости от количества ребер возвращается:

        •нет ребер - VT_EMPTY (если указан неправильный индекс или ребра не созданы),

        •одно ребро - VT_DISPATCH,

        •более одного ребра - VT_ARRAY | VT_DISPATCH.

        Индекс должен быть в диапазоне 0...ISurfacesIntersectionCurve::EdgesArraysCount-1.

        ### Синтаксис Automation

        ```
        Edges = Object.Edges( EdgesArrayIndex )     # Получить свойство (* )
        Edges = Object.GetEdges( EdgesArrayIndex )  # Получить свойство (**)
        ```

        ### Входные параметры

        +------------------------------------------+
        | EdgesArrayIndex | - индекс массива ребер |
        +------------------------------------------+
        """
        ...

    def EdgesCheck(self, ContourIndex) -> bool:  # isurfacesintersectioncurve_edgescheck.html
        """
        ## EdgesCheck - Признак выбора указанного по индексу массива ребер

        Тип данных: BOOL

        Индекс должен быть в диапазоне 0...ISurfacesIntersectionCurve::EdgesArraysCount-1.

        ### Синтаксис Automation

        ```
        EdgesCheck = Object.EdgesCheck( EdgesArrayIndex )     # Получить свойство (* )
        Object.EdgesCheck( EdgesArrayIndex ) = EdgesCheck     # Установить свойство (* )
        EdgesCheck = Object.GetEdgesCheck( EdgesArrayIndex )  # Получить свойство (**)
        Object.SetEdgesCheck( EdgesArrayIndex, EdgesCheck)    # Установить свойство (**)
        ```

        ### Входные параметры

        +-------------------------------------------+
        | EdgesArrayIndex | - индекс массива ребер. |
        +-------------------------------------------+
        """
        ...

    def GetObject(self, ForArray1, Index) -> IModelObject:  # isurfacesintersectioncurve_getobject.html
        """
        ## GetObject - Получить объект из указанного наборапо индексу

        ### Синтаксис Automation

        ```
        LPDISPATCH GetObject( BOOL ForArray1, long Index );
        ```

        ### Выходные параметры

        +------------------------------------------------------------+
        | ForArray1 | - TRUE - первый набор, - FALSE - второй набор, |
        +------------------------------------------------------------+
        | Index     | - индекс объекта в наборе.                     |
        +------------------------------------------------------------+

        ### Возвращаемое значение

        - указатель на интерфейс IModelObject.
        """
        ...

    def GetObjects(self) -> bool:  # isurfacesintersectioncurve_getobjects.html
        """
        ## GetObjects - Получить 1-й и 2-й наборы поверхностей

        Если в наборе одна грань, то она возвращается как VT_DISPATCH.

        Если в наборе несколько граней, то возвращается массив VT_ARRAY | VT_DISPATCH.

        ### Синтаксис Automation

        ```
        BOOL GetObjects( VARIANT * ObjectsArray1, VARIANT * ObjectsArray2 );
        ```

        ### Выходные параметры

        +----------------------------------------+
        | ObjectsArray1 | - первый набор граней, |
        +----------------------------------------+
        | ObjectsArray2 | - первый набор граней. |
        +----------------------------------------+

        ### Возвращаемое значение

        +--------------------------+
        | TRUE | - в случае удачи. |
        +--------------------------+
        """
        ...

    def ObjectsCount(self, ForArray1) -> int:  # isurfacesintersectioncurve_objectscount.html
        """
        ## ObjectsCount - Получить количество объектов в указанном наборе

        Тип данных: long

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        ObjectsCount = Object.ObjectsCount( ForArray1 )     # Получить свойство (* )
        ObjectsCount = Object.GetObjectsCount( ForArray1 )  # Получить свойство (**)
        ```

        ### Входные параметры

        +------------------------------------------------------------+
        | ForArray1 | - TRUE - первый набор, - FALSE - второй набор. |
        +------------------------------------------------------------+

        Свойство позволяет получить количество объектов в указанном наборе.
        """
        ...

    def SetEdgesCheck(self, ContourIndex, arg1):
        ...

    def SetObjects(self, ObjectsArray1, ObjectsArray2) -> bool:  # isurfacesintersectioncurve_setobjects.html
        """
        ## SetObjects - Задать 1-й и 2-й наборы поверхностей

        Если требуется передать одну грань в наборе, ее можно передать как VT_DISPATCH.

        Если требуется передать несколько граней в наборе, то нужно передать его как массив VT_ARRAY | VT_DISPATCH.

        ### Синтаксис Automation

        ```
        BOOL SetObjects( VARIANT ObjectsArray1, VARIANT ObjectsArray2 )
        ```

        ### Входные параметры

        +----------------------------------------+
        | ObjectsArray1 | - первый набор граней, |
        +----------------------------------------+
        | ObjectsArray2 | - первый набор граней. |
        +----------------------------------------+

        ### Возвращаемое значение

        +--------------------------+
        | TRUE | - в случае удачи. |
        +--------------------------+
        """
        ...




class ITrimmedCurve(IModelObject):  # itrimmedcurve.html
    """
    ## Интерфейс ITrimmedCurve

    Интерфейс параметров операции усечения кривой
    """

    @property
    def BeginParameter(self) -> float:  # itrimmedcurve_beginparameter.html
        """
        ## BeginParameter - Начальный параметр кривой

        Тип данных: double

        ### Синтаксис Automation

        ```
        BeginParameter = Object.BeginParameter       # Получить свойство (* )
        Object.BeginParameter = BeginParameter       # Установить свойство (* )
        BeginParameter = Object.GetBeginParameter()  # Получить свойство (**)
        Object.SetBeginParameter( BeginParameter )   # Установить свойство (**)
        ```
        """
        ...
    @BeginParameter.setter
    def BeginParameter(self, value: float): ...

    @property
    def Curve(self) -> IModelObject:  # itrimmedcurve_curve.html
        """
        ## Curve - Усекаемая кривая

        Тип данных: указатель на интерфейс IModelObject

        ### Синтаксис Automation

        ```
        Curve = Object.Curve       # Получить свойство (* )
        Object.Curve = Curve       # Установить свойство (* )
        Curve = Object.GetCurve()  # Получить свойство (**)
        Object.SetCurve( Curve )   # Установить свойство (**)
        ```
        """
        ...
    @Curve.setter
    def Curve(self, value: IModelObject): ...

    @property
    def CutObject1(self) -> IModelObject:  # itrimmedcurve_cutobject1.html
        """
        ## CutObject1 - Первый секущий объект для усечения кривой

        Тип данных: указатель на интерфейс IModelObject

        ### Синтаксис Automation

        ```
        CutObject1 = Object.CutObject1       # Получить свойство (* )
        Object.CutObject1 = CutObject1       # Установить свойство (* )
        CutObject1 = Object.GetCutObject1()  # Получить свойство (**)
        Object.SetCutObject1( CutObject1 )   # Установить свойство (**)
        ```
        """
        ...
    @CutObject1.setter
    def CutObject1(self, value: IModelObject): ...

    @property
    def CutObject2(self) -> IModelObject:  # itrimmedcurve_cutobject2.html
        """
        ## CutObject2 - Второй секущий объект для усечения кривой

        Тип данных: указатель на интерфейс IModelObject

        ### Синтаксис Automation

        ```
        CutObject2 = Object.CutObject2       # Получить свойство (* )
        Object.CutObject2 = CutObject2       # Установить свойство (* )
        CutObject2 = Object.GetCutObject2()  # Получить свойство (**)
        Object.SetCutObject2( CutObject2 )   # Установить свойство (**)
        ```
        """
        ...
    @CutObject2.setter
    def CutObject2(self, value: IModelObject): ...

    @property
    def EndParameter(self) -> float:  # itrimmedcurve_endparameter.html
        """
        ## EndParameter - Конечный параметр кривой

        Тип данных: double

        ### Синтаксис Automation

        ```
        EndParameter = Object.EndParameter       # Получить свойство (* )
        Object.EndParameter = EndParameter       # Установить свойство (* )
        EndParameter = Object.GetEndParameter()  # Получить свойство (**)
        Object.SetEndParameter( EndParameter )   # Установить свойство (**)
        ```
        """
        ...
    @EndParameter.setter
    def EndParameter(self, value: float): ...

    @property
    def Sense(self) -> bool:  # itrimmedcurve_sense.html
        """
        ## Sense - Сменить направление

        Тип данных: BOOL

        Свойство работает только при UseTwoCutObjecs = FALSE.

        ### Синтаксис Automation

        ```
        Sense = Object.Sense       # Получить свойство (* )
        Object.Sense = Sense       # Установить свойство (* )
        Sense = Object.GetSense()  # Получить свойство (**)
        Object.SetSense( Sense )   # Установить свойство (**)
        ```
        """
        ...
    @Sense.setter
    def Sense(self, value: bool): ...

    @property
    def UseTwoCutObjecs(self) -> bool:  # itrimmedcurve_usetwocutobjecs.html
        """
        ## UseTwoCutObjecs - Использовать два секущих объекта

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        UseTwoCutObjecs = Object.UseTwoCutObjecs       # Получить свойство (* )
        Object.UseTwoCutObjecs = UseTwoCutObjecs       # Установить свойство (* )
        UseTwoCutObjecs = Object.GetUseTwoCutObjecs()  # Получить свойство (**)
        Object.SetUseTwoCutObjecs( UseTwoCutObjecs )   # Установить свойство (**)
        ```
        """
        ...
    @UseTwoCutObjecs.setter
    def UseTwoCutObjecs(self, value: bool): ...

    def GetIntersectParameters(self):  # itrimmedcurve_getintersectparameters.html
        """
        ## GetIntersectParameters - Получить массивы точек пересечения кривой с секущими объектами

        ### Синтаксис Automation

        ```
        BOOL GetIntersectParameters( VARIANT * CutPoints1, VARIANT * CutPoints2 );
        ```

        ### Выходные параметры

        CutPoints1 - Массив VT_ARRAY | VT_R8 параметров в точках пересечения с первым секущим объектом.

        CutPoints2 - Массив VT_ARRAY | VT_R8 параметров в точках пересечения со вторым секущим объектом.
        """
        ...




class IUnhistoredCurve3D(IModelObject):  # iunhistoredcurve3d.html
    """
    ## Интерфейс IUnhistoredCurve3D

    Интерфейс кривой без истории.
    """

    def Replace(self, Curve, DeleteSource) -> bool:  # iunhistoredcurve3d_replace.html
        """
        ## Replace - Изменить кривую

        ### Синтаксис Automation

        ```
        BOOL Replace( IModelObject * Curve, BOOL DeleteSource );
        ```

        ### Входные параметры

        +-------------------------------------------+
        | Curve        | - указатель на кривую,     |
        +-------------------------------------------+
        | DeleteSource | - удалить исходную кривую. |
        +-------------------------------------------+

        Метод заменяет математику в кривой без истории на математику с переданной кривой.

        ### Возвращаемое значение

        +-----------------------------------------+
        | TRUE  | - в случае удачного завершения, |
        +-----------------------------------------+
        | FALSE | - в случае неудачи.             |
        +-----------------------------------------+
        """
        ...

    def Unwrap(self, Placement, X, Y, Z, Accuracy) -> bool:  # iunhistoredcurve3d_unwrap.html
        """
        ## Unwrap - Построить развертку

        ### Синтаксис Automation

        ```
        BOOL Unwrap( VARIANT Placement, double X, double Y, double Z, double Accuracy );
        ```

        ### Входные параметры

        +------------------------------------------------------------------------------+
        | Placement | - положение плоскости, на которой требуется построить развертку, |
        +------------------------------------------------------------------------------+
        | X, Y, Z   | - координаты начала построения развертки,                        |
        +------------------------------------------------------------------------------+
        | Accuracy  | - точность.                                                      |
        +------------------------------------------------------------------------------+

        ### Возвращаемое значение

        +-----------------------------------------+
        | TRUE  | - в случае удачного завершения, |
        +-----------------------------------------+
        | FALSE | - в случае неудачи.             |
        +-----------------------------------------+
        """
        ...




class IUserFolder(IModelObject):  # iuserfolder.html
    """
    ## Интерфейс IUserFolder

    Интерфейс пользовательской директории.
    """

    def Add(self, Type) -> IModelObject:  # iuserfolder_add.html
        """
        ## Add - Создает новый элемент и добавляет его в коллекцию

        ### Синтаксис Automation

        ```
        LPDISPATCH Add(ksObj3dTypeEnum Type);
        ```

        ### Входные параметры

        +------------------------------------------------------+
        | Type | - тип объекта из перечисления ksObj3dTypeEnum |
        +------------------------------------------------------+

        ### Возвращаемое значение

        -указатель на интерфейс IModelObject.
        """
        ...

    def GetObjects(self) -> list[IDispatch]:  # iuserfolder_getobjects.html
        """
        ## GetObjects - Получить набор объектов в виде SAFEARRAY DISPATCH - VT_ARRAY | VT_DISPATCH

        ### Синтаксис Automation

        ```
        VARIANT GetObjects();
        ```

        ### Возвращаемое значение

        Коллекция объектов возвращается в виде массива SAFEARRAY объектов LPDISPATCH (VT_ARRAY | VT_DISPATCH).

        Если возвращается один объект, то тип VARIANT-а будет VT_DISPATCH, если возвращается несколько объектов, то тип VARIANT-а будет VT_ARRAY | VT_DISPATCH.
        """
        ...

    def SetObjects(self, Objects) -> bool:  # iuserfolder_setobjects.html
        """
        ## SetObjects - Задать объекты

        •Если требуется передать один объект в наборе, его можно передать как VT_DISPATCH.

        •Если требуется передать несколько объектов в наборе, то нужно передать их как массив VT_ARRAY | VT_DISPATCH.

        ### Синтаксис Automation

        ```
        BOOL SetObjects( VARIANT Objects );
        ```

        ### Входные параметры

        +-----------------------------+
        | Objects | - набор объектов. |
        +-----------------------------+

        ### Возвращаемое значение

        +-----------------------------------------+
        | TRUE | - в случае успешного завершения. |
        +-----------------------------------------+
        """
        ...




class IUserObject3D(IModelObject):  # iuserobject3d.html
    """
    ## Интерфейс IUserObject3D

    Интерфейс пользовательского объекта 3D.
    """

    @property
    def AssociationObjectCount(self) -> int:  # iuserobject3d_associationobjectcount.html
        """
        ## AssociationObjectCount - Количество опорных объектов

        Тип данных: Указатель на интерфейс long

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        AssociationObjectCount = Object.AssociationObjectCount       # Получить свойство (* )
        AssociationObjectCount = Object.GetAssociationObjectCount()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def PropertyObjectEditable(self) -> bool:  # iuserobject3d_propertyobjecteditable.html
        """
        ## PropertyObjectEditable - Поддерживается интерфейс внешних свойств объекта

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        PropertyObjectEditable = Object.PropertyObjectEditable       # Получить свойство (* )
        Object.PropertyObjectEditable = PropertyObjectEditable       # Установить свойство (* )
        PropertyObjectEditable = Object.GetPropertyObjectEditable()  # Получить свойство (**)
        Object.SetPropertyObjectEditable( PropertyObjectEditable )   # Установить свойство (**)
        ```
        """
        ...
    @PropertyObjectEditable.setter
    def PropertyObjectEditable(self, value: bool): ...

    @property
    def UserParameters(self):  # iuserobject3d_userparameters.html
        """
        ## UserParameters - Получить интерфейс параметров

        Тип данных: Указатель на интерфейс IUnknown

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        UserParameters = Object.UserParameters       # Получить свойство (* )
        UserParameters = Object.GetUserParameters()  # Получить свойство (**)
        ```
        """
        ...

    def AssociationObject(self, Index) -> IModelObject:  # iuserobject3d_associationobject.html
        """
        ## AssociationObject - Опорный объект

        Тип данных: Указатель на интерфейс IModelObject

        ### Синтаксис Automation

        ```
        AssociationObject = Object.AssociationObject( Index );     # Получить свойство (* )
        Object.AssociationObject( Index ) = AssociationObject;     # Установить свойство (* )
        AssociationObject = Object.GetAssociationObject( Index );  # Получить свойство (**)
        Object.SetAssociationObject( Index, AssociationObject );   # Установить свойство (**)
        ```

        ### Входные параметры

        +--------------------------------+
        | long Index | - индекс объекта. |
        +--------------------------------+
        """
        ...

    def ClearAssociationObject(self) -> bool:  # iuserobject3d_clearassociationobject.html
        """
        ## ClearAssociationObject - Удалить все опорные объекты

        ### Синтаксис Automation

        ```
        BOOL ClearAssociationObject();
        ```

        ### Возвращаемое значение

        +-----------------------------------------+
        | TRUE  | - в случае удачного завершения, |
        +-----------------------------------------+
        | FALSE | - в случае неудачи.             |
        +-----------------------------------------+
        """
        ...

    def SetAssociationObject(self, Index, arg1):
        ...




class IEdge(IModelObject):  # iedge.html
    """
    ## Интерфейс IEdge

    Интерфейс Ребро 3D.
    """

    @property
    def BaseCurve3DType(self) -> int:  # iedge_basecurve3dtype.html
        """
        ## BaseCurve3DType - Тип базовой кривой

        Тип данных: Значение из перечисления ksMathCurve3DTypeEnum.

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        BaseCurve3DType = Object.BaseCurve3DType       # Получить свойство (* )
        BaseCurve3DType = Object.GetBaseCurve3DType()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def Body(self) -> IBody7:  # iedge_body.html
        """
        ## Body - Тело

        Тип данных: Указатель на интерфейс IBody7

        Свойство доступно только для чтения.

        Версия КОМПАС v22

        ### Синтаксис Automation

        ```
        Body = Object.Body;       # Получить свойство (* )
        Body = Object.GetBody();  # Получить свойство (**)
        ```
        """
        ...

    @property
    def Curve3DType(self) -> int:  # iedge_curve3dtype.html
        """
        ## Curve3DType - Тип кривой

        Тип данных: Значение из перечисления ksMathCurve3DTypeEnum.

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        Curve3DType = Object.Curve3DType       # Получить свойство (* )
        Curve3DType = Object.GetCurve3DType()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def IsArc(self) -> bool:  # iedge_isarc.html
        """
        ## IsArc - Является ли кривая дугой окружности

        Тип данных: BOOL

        Свойство доступно только для чтения.

        Версия КОМПАС v22

        ### Синтаксис Automation

        ```
        IsArc = Object.IsArc       # Получить свойство (* )
        IsArc = Object.GetIsArc()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def IsCircle(self) -> bool:  # iedge_iscircle.html
        """
        ## IsCircle - Является ли кривая окружностью

        Тип данных: BOOL

        Свойство доступно только для чтения.

        Версия КОМПАС v22

        ### Синтаксис Automation

        ```
        IsCircle = Object.IsCircle       # Получить свойство (* )
        IsCircle = Object.GetIsCircle()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def IsEllipse(self) -> bool:  # iedge_isellipse.html
        """
        ## IsEllipse - Является ли кривая эллипсом

        Тип данных: BOOL

        Свойство доступно только для чтения.

        Версия КОМПАС v22

        ### Синтаксис Automation

        ```
        IsEllipse = Object.IsEllipse       # Получить свойство (* )
        IsEllipse = Object.GetIsEllipse()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def IsEllipseArc(self) -> bool:  # iedge_isellipsearc.html
        """
        ## IsEllipseArc - Является ли кривая дугой эллипса

        Тип данных: BOOL

        Свойство доступно только для чтения.

        Версия КОМПАС v22

        ### Синтаксис Automation

        ```
        IsEllipseArc = Object.IsEllipseArc       # Получить свойство (* )
        IsEllipseArc = Object.GetIsEllipseArc()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def IsLineSeg(self) -> IMathCurve3D:  # iedge_islineseg.html
        """
        ## IsLineSeg - Является ли кривая отрезком

        Тип данных: Указатель на интерфейс IMathCurve3D.

        Свойство доступно только для чтения.

        Версия КОМПАС v22

        ### Синтаксис Automation

        ```
        MathCurve = Object.MathCurve       # Получить свойство (* )
        MathCurve = Object.GetMathCurve()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def IsNurbs(self) -> bool:  # iedge_isnurbs.html
        """
        ## IsNurbs - Определить, является ли кривая NURBS

        Тип данных: BOOL

        Свойство доступно только для чтения

        Версия КОМПАС v22.

        ### Синтаксис Automation

        ```
        IsNurbs = Object.IsNurbs       # Получить свойство (* )
        IsNurbs = Object.GetIsNurbs()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def IsPeriodic(self) -> bool:  # iedge_isperiodic.html
        """
        ## IsPeriodic - Является ли ребро периодичным

        Тип данных: BOOL

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        IsPeriodic = Object.IsPeriodic       # Получить свойство (* )
        IsPeriodic = Object.GetIsPeriodic()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def IsPlanar(self) -> bool:  # iedge_isplanar.html
        """
        ## IsPlanar - Является ли кривая плоской

        Тип данных: BOOL.

        Свойство доступно только для чтения.

        Версия КОМПАС v22

        ### Синтаксис Automation

        ```
        IsPlanar = Object.IsPlanar       # Получить свойство (* )
        IsPlanar = Object.GetIsPlanar()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def IsSketchEdge(self) -> bool:  # iedge_issketchedge.html
        """
        ## IsSketchEdge - Является ли ребро кривой эскиза

        Тип данных: BOOL

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        IsSketchEdge = Object.IsSketchEdge       # Получить свойство (* )
        IsSketchEdge = Object.GetIsSketchEdge()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def IsStraight(self) -> bool:  # iedge_isstraight.html
        """
        ## IsStraight - Является ли ребро прямым

        Тип данных: BOOL

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        IsStraight = Object.IsStraight       # Получить свойство (* )
        IsStraight = Object.GetIsStraight()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def MathCurve(self) -> IMathCurve3D:  # iedge_mathcurve.html
        """
        ## MathCurve - Получить указатель на интерфейс математической кривой

        Тип данных: Указатель на интерфейс IMathCurve3D.

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        MathCurve = Object.MathCurve       # Получить свойство (* )
        MathCurve = Object.GetMathCurve()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def OrientedEdges(self) -> typing.Any:  # iedge_orientededges.html
        """
        ## OrientedEdges - Получить указатель на массив ориентированных ребер

        Тип данных: VARIANT

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        OrientedEdges = Object.OrientedEdges       # Получить свойство (* )
        OrientedEdges = Object.GetOrientedEdges()  # Получить свойство (**)
        ```
        """
        ...

    def AdjacentFace(self, FacePlus) -> IFace:  # iedge_adjacentface.html
        """
        ## AdjacentFace - Получить указатель на интерфейс грани, в цикл которой входит ребро

        Тип данных: Указатель на интерфейс IFace.

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        BaseAdjacentFace = Object.BaseAdjacentFace       # Получить свойство (* )
        BaseAdjacentFace = Object.GetBaseAdjacentFace()  # Получить свойство (**)
        ```
        """
        ...

    def AngleType(self, AngleEpsilon) -> int:  # iedge_angletype.html
        """
        ## AngleType - Тип угла

        Тип данных: из перечисления ksAngleTypeEnum.

        Свойство доступно только для чтения.

        Версия КОМПАС v22

        ### Синтаксис Automation

        ```
        AngleType = Object.AngleType       # Получить свойство (* )
        AngleType = Object.GetAngleType()  # Получить свойство (**)
        ```
        """
        ...

    def Edges(self, Start) -> typing.Any:  # iedge_edges.html
        """
        ## Edges - Получить указатель на интерфейс массива ребер, стыкующихся с данным ребром

        Тип данных: VARIANT

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        Edges = Object.Edges( Start )     # Получить свойство (* )
        Edges = Object.GetEdges( Start )  # Получить свойство (**)
        ```
        """
        ...

    def FacesAngle(self, T) -> float:  # iedge_facesangle.html
        """
        ## FacesAngle - Вычислить угол ребра (0 <= T <= 1): для выпуклого ребра угол больше нуля, для вогнутого ребра угол меньше нуля

        Тип данных: double

        Свойство доступно только для чтения.

        Версия КОМПАС v22

        ### Синтаксис Automation

        ```
        FacesAngle = Object.FacesAngle( T )     # Получить свойство (* )
        FacesAngle = Object.GetFacesAngle( T )  # Получить свойство (**)
        ```

        ### Входные параметры

        +------------------------+
        | T | - параметр кривой. |
        +------------------------+
        """
        ...

    def FacesAngleByPoint(self, X, Y, Z) -> float:  # iedge_facesanglebypoint.html
        """
        ## FacesAngleByPoint - Вычислить угол в заданной точке: для выпуклого ребра угол больше нуля, для вогнутого ребра угол меньше нуля

        Тип данных: double

        Свойство доступно только для чтения.

        Версия КОМПАС v22

        ### Синтаксис Automation

        ```
        FacesAngleByPoint = Object.FacesAngleByPoint( X, Y, Z )     # Получить свойство (* )
        FacesAngleByPoint = Object.GetFacesAngleByPoint( X, Y, Z )  # Получить свойство (**)
        ```

        ### Входные параметры

        +----------------------------------------+
        | X, Y, Z | - координаты точки на ребре. |
        +----------------------------------------+
        """
        ...

    def GetLength(self, BitVector):  # iedge_getlength.html
        """
        ## GetLength - Получить длину ребра

        ### Синтаксис Automation

        ```
        double GetLength( ksLengthUnitsEnum Unit );
        ```

        ### Входные параметры

        +---------------------------------------------------------------------+
        | Unit | - единицы измерения длины из перечисления ksLengthUnitsEnum. |
        +---------------------------------------------------------------------+

        ### Возвращаемое значение

        - длина ребра.
        """
        ...

    def GetPoint(self, Start):
        ...

    def Vertex(self, Start) -> IVertex:  # iedge_vertex.html
        """
        ## Vertex - Получить указатель на интерфейс вершин: начальной и конечной

        Тип данных: Указатель на интерфейс IVertex

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        Vertex = Object.Vertex( Start )     # Получить свойство (* )
        Vertex = Object.GetVertex( Start )  # Получить свойство (**)
        ```
        """
        ...




class IFace(IModelObject):  # iface.html
    """
    ## Интерфейс IFace

    Интерфейс грани.
    """

    @property
    def BaseSurface3DType(self) -> int:  # iface_basesurface3dtype.html
        """
        ## BaseSurface3DType - Тип базовой поверхности

        Тип данных: из перечисления ksMathSurface3DTypeEnum.

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        BaseSurface3DType = Object.BaseSurface3DType       # Получить свойство (* )
        BaseSurface3DType = Object.GetBaseSurface3DType()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def Body(self) -> IBody7:  # iface_body.html
        """
        ## Body - Тело

        Тип данных: Указатель на интерфейс IBody7

        Свойство доступно только для чтения.

        Версия КОМПАС v22

        ### Синтаксис Automation

        ```
        Body = Object.Body;       # Получить свойство (* )
        Body = Object.GetBody();  # Получить свойство (**)
        ```
        """
        ...

    @property
    def ConnectedFaces(self) -> typing.Any:  # iface_connectedfaces.html
        """
        ## ConnectedFaces - Массив граней, стыкующихся с данной гранью в виде SAFEARRAY'я DISPATCH - VT_ARRAY | VT_DISPATCH

        Тип данных: VARIANT

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        ConnectedFaces = Object.ConnectedFaces       # Получить свойство (* )
        ConnectedFaces = Object.GetConnectedFaces()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def IsCone(self) -> bool:  # iface_iscone.html
        """
        ## IsCone - Является ли грань конической

        Тип данных: BOOL

        Свойство доступно только для чтения.

        Версия КОМПАС v22

        ### Синтаксис Automation

        ```
        IsCone = Object.IsCone        # Получить свойство (* )
        IsCone = Object.GetIsCone();  # Получить свойство (**)
        ```
        """
        ...

    @property
    def IsCylinder(self) -> bool:  # iface_iscylinder.html
        """
        ## IsCylinder - Является ли грань цилиндрической

        Тип данных: BOOL

        Свойство доступно только для чтения.

        Версия КОМПАС v22

        ### Синтаксис Automation

        ```
        IsCylinder = Object.IsCylinder       # Получить свойство (* )
        IsCylinder = Object.GetIsCylinder()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def IsNurbsSurface(self) -> bool:  # iface_isnurbssurface.html
        """
        ## IsNurbsSurface - Является ли грань NURBS-поверхностью

        Тип данных:BOOL

        Свойство доступно только для чтения.

        Версия КОМПАС v22

        ### Синтаксис Automation

        ```
        IsNurbsSurface = Object.IsNurbsSurface       # Получить свойство (* )
        IsNurbsSurface = Object.GetIsNurbsSurface()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def IsPlanar(self) -> bool:  # iface_isplanar.html
        """
        ## IsPlanar - Является ли грань плоской

        Тип данных:BOOL

        Свойство доступно только для чтения.

        Версия КОМПАС v22

        ### Синтаксис Automation

        ```
        IsPlanar = Object.IsPlanar       # Получить свойство (* )
        IsPlanar = Object.GetIsPlanar()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def IsRevolved(self) -> bool:  # iface_isrevolved.html
        """
        ## IsRevolved - Определяется ли грань поверхностью вращения

        Тип данных:BOOL

        Свойство доступно только для чтения.

        Версия КОМПАС v22

        ### Синтаксис Automation

        ```
        IsRevolved = Object.IsRevolved       # Получить свойство (* )
        IsRevolved = Object.GetIsRevolved()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def IsSalient(self) -> int:  # iface_issalient.html
        """
        ## IsSalient - Является ли грань выпуклой

        Тип данных: из перечисления ksSurfaceSalientTypeEnum

        Свойство доступно только для чтения.

        Версия КОМПАС v22

        ### Синтаксис Automation

        ```
        IsSalient = Object.IsSalient       # Получить свойство (* )
        IsSalient = Object.GetIsSalient()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def IsSphere(self) -> bool:  # iface_issphere.html
        """
        ## IsSphere - Является ли грань сферической

        Тип данных: BOOL

        Свойство доступно только для чтения.

        Версия КОМПАС v22

        ### Синтаксис Automation

        ```
        IsSphere = Object.IsSphere       # Получить свойство (* )
        IsSphere = Object.GetIsSphere()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def IsSwept(self) -> bool:  # iface_isswept.html
        """
        ## IsSwept - Определяется ли грань поверхностью по траектории

        Тип данных:BOOL

        Свойство доступно только для чтения.

        Версия КОМПАС v22

        ### Синтаксис Automation

        ```
        IsSwept = Object.IsSwept       # Получить свойство (* )
        IsSwept = Object.GetIsSwept()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def IsTorus(self) -> bool:  # iface_istorus.html
        """
        ## IsTorus - Является ли грань тором

        Тип данных: BOOL

        Свойство доступно только для чтения.

        Версия КОМПАС v22

        ### Синтаксис Automation

        ```
        IsTorus = Object.IsTorus       # Получить свойство (* )
        IsTorus = Object.GetIsTorus()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def LimitingEdges(self) -> typing.Any:  # iface_limitingedges.html
        """
        ## LimitingEdges - Массив ребер, ограничивающих грань в виде SAFEARRAY'я DISPATCH - VT_ARRAY | VT_DISPATCH

        Тип данных: VARIANT

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        LimitingEdges = Object.LimitingEdges       # Получить свойство (* )
        LimitingEdges = Object.GetLimitingEdges()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def Loops(self) -> typing.Any:  # iface_loops.html
        """
        ## Loops - Массив циклов в виде SAFEARRAY'я DISPATCH - VT_ARRAY | VT_DISPATCH

        Тип данных: VARIANT

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        Loops = Object.Loops       # Получить свойство (* )
        Loops = Object.GetLoops()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def MathSurface(self) -> IMathSurface3D:  # iface_mathsurface.html
        """
        ## MathSurface - Получить интерфейс математической поверхности

        Тип данных: Указатель на интерфейс IMathSurface3D.

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        MathSurface = Object. MathSurface       # Получить свойство (* )
        MathSurface = Object.Get MathSurface()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def NormalOrientation(self) -> bool:  # iface_normalorientation.html
        """
        ## NormalOrientation - Относительная ориентация (ориентация нормали грани относительно ориентации нормали поверхности)

        Тип данных: BOOL

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        NormalOrientation = Object. NormalOrientation       # Получить свойство (* )
        NormalOrientation = Object.Get NormalOrientation()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def Radius(self) -> float:  # iface_radius.html
        """
        ## Radius - Дать максимальный радиус

        Тип данных: double

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        Radius = Object.Radius       # Получить свойство (* )
        Radius = Object.GetRadius()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def Surface3DType(self) -> int:  # iface_surface3dtype.html
        """
        ## Surface3DType - Тип поверхности

        Тип данных: из перечисления ksMathSurface3DTypeEnum.

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        Surface3DType = Object.Surface3DType       # Получить свойство (* )
        Surface3DType = Object.GetSurface3DType()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def Tessellation(self) -> ITessellation7:  # iface_tessellation.html
        """
        ## Tessellation - Параметры триангуляционной сетки

        Тип данных: Указатель на интерфейс ITessellation7.

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        Tessellation = Object.Tessellation       # Получить свойство (* )
        Tessellation = Object.GetTessellation()  # Получить свойство (**)
        ```
        """
        ...

    def GetArea(self, Unit):  # iface_getarea.html
        """
        ## GetArea - Получить площадь грани

        ### Синтаксис Automation

        ```
        double GetArea( ksLengthUnitsEnum Unit );
        ```

        ### Входные параметры

        +---------------------------------------------------------------------+
        | Unit | - единицы измерения длины из перечисления ksLengthUnitsEnum. |
        +---------------------------------------------------------------------+

        ### Возвращаемое значение

        - площадь грани.
        """
        ...

    def GetAreaEx(self, Unit, AngleTolerance):  # iface_getareaex.html
        """
        ## GetAreaEx - Получить площадь грани

        ### Синтаксис Automation

        ```
        double GetAreaEx( ksLengthUnitsEnum Unit, double AngleTolerance );
        ```

        ### Входные параметры

        +--------------------------------------------------------------------------------------------------------+
        | Unit           | - единицы измерения длины из перечисления ksLengthUnitsEnum,                          |
        +--------------------------------------------------------------------------------------------------------+
        | AngleTolerance | - ограничение углового отклонения при аппроксимации оболочки треугольными пластинами. |
        +--------------------------------------------------------------------------------------------------------+

        Компас Версия v21
        """
        ...

    def GetConeParam(self) -> bool:  # iface_getconeparam.html
        """
        ## GetConeParam - Получить параметры конической грани

        ### Синтаксис Automation

        ```
        BOOL GetConeParam( double * Height, double * Angle, double * Radius );
        ```

        ### Выходные параметры

        +--------------------+
        | Height | - высота, |
        +--------------------+
        | Angle  | - угол,   |
        +--------------------+
        | Radius | - радиус. |
        +--------------------+

        ### Возвращаемое значение

        +-----------------------------------------+
        | TRUE | - в случае успешного завершения. |
        +-----------------------------------------+
        """
        ...

    def IsConnectedWith(self, OtherFace):
        ...




class ILoop7(IKompasAPIObject):  # iloop7.html
    """
    ## Интерфейс ILoop7

    Интерфейс цикла (содержит информацию о связях с другими гранями).
    """

    @property
    def Edges(self) -> typing.Any:  # iloop7_edges.html
        """
        ## Edges - Получить массив ребер

        Тип данных: VARIANT

        1. Свойство доступно только для чтения.

        2. Свойство позволяет получить массив SafeArray (VT_APPAY|VT_DISPATCH) ребер грани IEdge.

        ### Синтаксис Automation

        ```
        Edges = Object.Edges       # Получить свойство (* )
        Edges = Object.GetEdges()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def IsOuter(self) -> bool:  # iloop7_isouter.html
        """
        ## IsOuter - TRUE - цикл внешний, FALSE - цикл внутренний

        Тип данных: BOOL

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        IsOuter = Object.IsOuter       # Получить свойство (* )
        IsOuter = Object.GetIsOuter()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def OrientedEdges(self) -> typing.Any:  # iloop7_orientededges.html
        """
        ## OrientedEdges - Получить массив ориентированных ребер

        Тип данных: VARIANT

        1. Свойство доступно только для чтения.

        2. Свойство позволяет получить массив SafeArray (VT_APPAY|VT_DISPATCH) ориентированных ребер IOrientedEdge7.

        ### Синтаксис Automation

        ```
        OrientedEdges = Object.OrientedEdges       # Получить свойство (* )
        OrientedEdges = Object.GetOrientedEdges()  # Получить свойство (**)
        ```
        """
        ...

    def GetLength(self, Unit):  # iloop7_getlength.html
        """
        ## GetLength - Получить общую длину ребер. bitVector = ST_MIX_MM..ST_MIX_M единицы измерения

        ### Синтаксис Automation

        ```
        double GetLength( ksLengthUnitsEnum Unit );
        ```

        ### Входные параметры

        +---------------------------------------------------------------------+
        | Unit | - единицы измерения длины из перечисления ksLengthUnitsEnum. |
        +---------------------------------------------------------------------+

        ### Возвращаемое значение

        - общая длина ребер цикла.
        """
        ...




class IOrientedEdge7(IKompasAPIObject):  # iorientededge7.html
    """
    ## Интерфейс IOrientedEdge7

    Интерфейс ориентированного ребра.
    """

    @property
    def Edge(self) -> IModelObject:  # iorientededge7_edge.html
        """
        ## Edge - Получить интерфейс базового ребра

        Тип данных: Указатель на интерфейс IModelObject.

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        Edge = Object.Edge       # Получить свойство (* )
        Edge = Object.GetEdge()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def IsPole(self):
        ...

    @property
    def IsSeam(self):
        ...

    @property
    def IsStraight(self):
        ...

    @property
    def MathCurve(self):
        ...

    @property
    def Next(self) -> IOrientedEdge7:  # iorientededge7_next.html
        """
        ## Next - Получить следующее ориентированное ребро

        Тип данных: Указатель на интерфейс IOrientedEdge7.

        +--------------------------------------------------+
        | Next = Object.Next      | Получить свойство (* ) |
        +--------------------------------------------------+
        | Next = Object.GetNext() | Получить свойство (**) |
        +--------------------------------------------------+

        Свойство доступно только для чтения.
        """
        ...

    @property
    def Orientation(self) -> bool:  # iorientededge7_orientation.html
        """
        ## Orientation - Получить направление относительно базового ребра

        Тип данных: BOOL

        Свойство доступно только для чтения.

        TRUE - направления совпадают.

        ### Синтаксис Automation

        ```
        Orientation = Object.Orientation       # Получить свойство (* )
        Orientation = Object.GetOrientation()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def SameSense(self) -> bool:  # iorientededge7_samesense.html
        """
        ## SameSense - Получить направление относительно кривой

        Тип данных: BOOL

        Свойство доступно только для чтения.

        TRUE - направления совпадают.

        ### Синтаксис Automation

        ```
        SameSense = Object.SameSense       # Получить свойство (* )
        SameSense = Object.GetSameSense()  # Получить свойство (**)
        ```
        """
        ...

    def AdjacentFace(self, FacePlus) -> IFace:  # iorientededge7_adjacentface.html
        """
        ## AdjacentFace - Получить грань, в которой ребро входит в цикл

        Тип данных: Указатель на интерфейс IFace.

        Свойство доступно только для чтения.

        TRUE - ориентация плюс.

        ### Синтаксис Automation

        ```
        AdjacentFace = Object.AdjacentFace( FacePlus )     # Получить свойство (* )
        AdjacentFace = Object.GetAdjacentFace( FacePlus )  # Получить свойство (**)
        ```
        """
        ...




class ITessellation7(IDispatch):  # itessellation7.html
    """
    ## Интерфейс ITessellation7

    Интерфейс триангуляции грани.
    """

    @property
    def FacetAngle(self) -> float:  # itessellation7_facetangle.html
        """
        ## FacetAngle - Ограничение углового отклонения триангуляционной пластины (Если 0, обычная триангуляция)

        Тип данных: double

        ### Синтаксис Automation

        ```
        FacetAngle = Object.FacetAngle;       # Получить свойство (* )
        Object.FacetAngle = FacetAngle;       # Установить свойство (* )
        FacetAngle = Object.GetFacetAngle();  # Получить свойство (**)
        Object.SetFacetAngle( FacetAngle );   # Установить свойство (**)
        ```
        """
        ...
    @FacetAngle.setter
    def FacetAngle(self, value: float): ...

    @property
    def FacetSag(self) -> float:  # itessellation7_facetsag.html
        """
        ## FacetSag - Ограничение прогиба поверхности (Если 0, обычная триангуляция)

        Тип данных: double

        ### Синтаксис Automation

        ```
        FacetSag = Object.FacetSag;       # Получить свойство (* )
        Object.FacetSag = FacetSag;       # Установить свойство (* )
        FacetSag = Object.GetFacetSag();  # Получить свойство (**)
        Object.SetFacetSag( FacetSag );   # Установить свойство (**)
        ```
        """
        ...
    @FacetSag.setter
    def FacetSag(self, value: float): ...

    @property
    def FacetSize(self) -> float:  # itessellation7_facetsize.html
        """
        ## FacetSize - Ограничение размера ребра (Если 0, обычная триангуляция)

        Тип данных: double

        ### Синтаксис Automation

        ```
        FacetSize = Object.FacetSize;       # Получить свойство (* )
        Object.FacetSize = FacetSize;       # Установить свойство (* )
        FacetSize = Object.GetFacetSize();  # Получить свойство (**)
        Object.SetFacetSize( FacetSize );   # Установить свойство (**)
        ```
        """
        ...
    @FacetSize.setter
    def FacetSize(self, value: float): ...

    @property
    def NeedParams(self) -> bool:  # itessellation7_needparams.html
        """
        ## NeedParams - Необходимость заполнения параметров вершин

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        NeedParams = Object.NeedParams ;       # Получить свойство (* )
        Object.NeedParams = NeedParams ;       # Установить свойство (* )
        NeedParams = Object.GetNeedParams ();  # Получить свойство (**)
        Object.SetNeedParams ( NeedParams );   # Установить свойство (**)
        ```
        """
        ...
    @NeedParams.setter
    def NeedParams(self, value: bool): ...

    def GetFacetParams(self) -> ITessellation7:  # itessellation7_getfacetparams.html
        """
        ## GetFacetParams - Получить параметры вершин триангуляционной сетки

        ### Синтаксис Automation

        ```
        VARIANT GetFacetParams();
        ```

        ### Возвращаемое значение

        +-------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------+
        | SafeArray | - массив вещественных чисел - параметрических координат вершин триангуляционных сетки. Для формирование параметрических координат триангуляционной сетки требуется установить признак ITessellation7::NeedParams. |
        +-------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------+
        """
        ...

    def GetFacetPoints(self) -> bool:  # itessellation7_getfacetpoints.html
        """
        ## GetFacetPoints - Получить параметры вершин треагуляционной сетки

        1. В массиве индексов вершин триангуляционной сетки хранятся индексы точек из массива координат точек points. Для получения параметров вершины триангуляционной сетки нужно получить индекс точки из массива indexes.

        2. Координаты точек в массиве points лежат в последовательности x, y, z первой точки, x, y, z второй точки и т.д.

        3. В массиве нормалей вершин триангуляционной сетки координаты нормалей лежат в последовательности x, y, z - нормаль для первой точки, x, y, z- нормаль второй точки и т.д.

        ### Синтаксис Automation

        ```
        BOOL GetFacetPoints( VARIANT * Points, VARIANT * Indexes, VARIANT * Normals );
        ```

        ### Выходные параметры

        +--------------------------------------------------------------------------------------------+
        | Points  | - массив SafeArray вещественных чисел - координат вершин триангуляционной сетки, |
        +--------------------------------------------------------------------------------------------+
        | Indexes | - массив SafeArray целых чисел - индексов вершин триангуляционной сетки,         |
        +--------------------------------------------------------------------------------------------+
        | Normals | - массив SafeArray вещественных чисел - нормалей вершин триангуляционной сетки.  |
        +--------------------------------------------------------------------------------------------+

        ### Возвращаемое значение

        +-----------------------------------------+
        | TRUE | - в случае успешного завершения. |
        +-----------------------------------------+
        """
        ...

    def RebuildTessellation(self) -> bool:  # itessellation7_rebuildtessellation.html
        """
        ## RebuildTessellation - Перестроить после изменения параметров сетки

        ### Синтаксис Automation

        ```
        BOOL RebuildTessellation();
        ```

        ### Возвращаемое значение

        +-----------------------------------------+
        | TRUE | - в случае успешного завершения. |
        +-----------------------------------------+
        """
        ...




class IVertex(IModelObject):  # ivertex.html
    """
    ## Интерфейс IVertex

    Интерфейс вершины.
    """

    @property
    def Body(self):  # ivertex_body.html
        """
        ## Body - Тело

        Тип данных: Указатель на интерфейсIBody7

        Свойство доступно только для чтения.

        Версия КОМПАС v22

        ### Синтаксис Automation

        ```
        Body = Object.Body;       # Получить свойство (* )
        Body = Object.GetBody();  # Получить свойство (**)
        ```
        """
        ...

    @property
    def IsFreeVertex(self):  # ivertex_isfreevertex.html
        """
        ## IsFreeVertex - Является ли вершина свободной

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        IsFreeVertex = Object.IsFreeVertex;       # Получить свойство (* )
        IsFreeVertex = Object.GetIsFreeVertex();  # Получить свойство (**)
        ```
        """
        ...

    @property
    def IsSketchVertex(self) -> bool:  # ivertex_issketchvertex.html
        """
        ## IsSketchVertex - Является ли вершина точкой эскиза

        Тип данных: BOOL

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        IsSketchVertex = Object.IsSketchVertex;       # Получить свойство (* )
        IsSketchVertex = Object.GetIsSketchVertex();  # Получить свойство (**)
        ```
        """
        ...

    @property
    def IsTopologyVertex(self) -> bool:  # ivertex_istopologyvertex.html
        """
        ## IsTopologyVertex - Является ли вершина топологической

        Тип данных: BOOL

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        IsTopologyVertex = Object.IsTopologyVertex;       # Получить свойство (* )
        IsTopologyVertex = Object.GetIsTopologyVertex();  # Получить свойство (**)
        ```
        """
        ...

    def GetPoint(self) -> bool:  # ivertex_getpoint.html
        """
        ## GetPoint - Получить координаты вершины

        ### Синтаксис Automation

        ```
        BOOL GetPoint( double * X, double * Y, double * Z );
        ```

        ### Выходные параметры

        +-------------------------------+
        | X, Y, Z | - координаты точки. |
        +-------------------------------+

        ### Возвращаемое значение

        +-----------------------------------------+
        | TRUE | - в случае успешного завершения. |
        +-----------------------------------------+
        """
        ...




class ICurveVertexParam(IKompasAPIObject):  # icurvevertexparam.html
    """
    ## Интерфейс ICurveVertexParam

    Интерфейс параметров вершины кривой.
    """

    @property
    def AssociationVertex(self) -> IModelObject:  # icurvevertexparam_associationvertex.html
        """
        ## AssociationVertex - Ассоциативная вершина

        Тип данных: Указатель на интерфейс модельного объекта IModelObject

        ### Синтаксис Automation

        ```
        AssociationVertex = iObject.AssociationVertex;       # Получить свойство (* )
        iObject.AssociationVertex = AssociationVertex;       # Установить свойство (* )
        AssociationVertex = iObject.GetAssociationVertex();  # Получить свойство (**)
        iObject.SetAssociationVertex(AssociationVertex);     # Установить свойство (**)
        ```
        """
        ...
    @AssociationVertex.setter
    def AssociationVertex(self, value: IModelObject): ...

    @property
    def BuildingObject(self) -> IModelObject:  # icurvevertexparam_buildingobject.html
        """
        ## BuildingObject - Объект, относительно которого ведется построение

        Тип данных: Указатель на интерфейс модельного объекта IModelObject

        Задать объект, относительно которого ведется построение, можно для типа построения ksLineBuildingType, равного ksLBTParallel и ksLBTPerpendicular.

        ### Синтаксис Automation

        ```
        BuildingObject = iObject.BuildingObject;       # Получить свойство (* )
        iObject.BuildingObject = BuildingObject;       # Установить свойство (* )
        BuildingObject = iObject.GetBuildingObject();  # Получить свойство (**)
        iObject.SetBuildingObject(BuildingObject);     # Установить свойство (**)
        ```
        """
        ...
    @BuildingObject.setter
    def BuildingObject(self, value: IModelObject): ...

    @property
    def BuildingType(self) -> int:  # icurvevertexparam_buildingtype.html
        """
        ## BuildingType - Способ построения сегмента кривой

        Тип данных: из перечисления ksLineBuildingType

        1. Для сплайна используются только константы ksLBTByPoint и ksLBTByPoint3DParams.

        2. В случае задания ksLBTByPoint3DParams начинают работать свойства PointType и PointParameters.

        ### Синтаксис Automation

        ```
        BuildingType = iObject.BuildingType;       # Получить свойство (* )
        iObject.BuildingType = BuildingType;       # Установить свойство (* )
        BuildingType = iObject.GetBuildingType();  # Получить свойство (**)
        iObject.SetBuildingType(BuildingType);     # Установить свойство (**)
        ```
        """
        ...
    @BuildingType.setter
    def BuildingType(self, value: int): ...

    @property
    def Index(self) -> int:  # icurvevertexparam_index.html
        """
        ## Index - Индекс вершины в массиве вершин кривой

        Тип данных: long

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        Index = iObject.Index;       # Получить свойство (* )
        Index = iObject.GetIndex();  # Получить свойство (**)
        ```
        """
        ...

    @property
    def PointParameters(self) -> IKompasAPIObject:  # icurvevertexparam_pointparameters.html
        """
        ## PointParameters - Интерфейс параметров точки

        Тип данных: Указатель на интерфейс параметров вершины ломанной IKompasAPIObject

        1. Свойство доступно только для чтения.

        2. В зависимости от типа PointType интерфейс параметров должен приводится к одному из следующих вариантов:

        •ksPDisplace - IPoint3DParamDisplace - Интерфейс параметров пространственной точки, заданной по смещению от опорного объекта.

        •ksPIntersect - IPoint3DParamIntersect - Интерфейс параметров пространственной точки, заданной на пересечении опорных объектов.

        •ksPCenter - IPoint3DParamCenter - Интерфейс параметров пространственной точки, заданной в центре опорного объекта,

        •ksPCurve - IPoint3DParamCurve - Интерфейс параметров пространственной точки, заданной на кривой со смещением.

        •ksPSurface - IPoint3DparamSurface - Интерфейс параметров пространственной точки, заданной на поверхности.

        •ksPProjection - IPoint3DParamProjection Интерфейс параметров пространственной точки, заданной проецированием.

        ### Синтаксис Automation

        ```
        PointParameters = iObject.PointParameters( Index );     # Получить свойство (* )
        PointParameters = iObject.GetPointParameters( Index );  # Получить свойство (**)
        ```

        ### Входные параметры

        +---------------------------+
        | Index | - индекс вершины. |
        +---------------------------+
        """
        ...

    @property
    def PointType(self) -> int:  # icurvevertexparam_pointtype.html
        """
        ## PointType - Способ построения точки

        Тип данных: Из перечисления ksPoint3DTypeEnum

        ### Синтаксис Automation

        ```
        PointType = iObject.PointType;       # Получить свойство (* )
        iObject.PointType = PointType;       # Установить свойство (* )
        PointType = iObject.GetPointType();  # Получить свойство (**)
        iObject.SetPointType(PointType);     # Установить свойство (**)
        ```
        """
        ...
    @PointType.setter
    def PointType(self, value: int): ...

    @property
    def Vector3D(self) -> IVector3D:  # icurvevertexparam_vector3d.html
        """
        ## Vector3D - Получить параметры вектора

        Тип данных: указатель на интерфейс IVector3D

        1. Свойство позволяет получить интерфейс вектора, задающего направление построения сегмента ломаной или сплайна.

        2. Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        Vector3D = Object.Vector3D       # Получить свойство (* )
        Vector3D = Object.GetVector3D()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def Vertex(self) -> IModelObject:  # icurvevertexparam_vertex.html
        """
        ## Vertex - Вершина кривой

        Тип данных: указатель на интерфейс модельного объекта IModelObject.

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        Vertex = iObject.Vertex;       # Получить свойство (* )
        Vertex = iObject.GetVertex();  # Получить свойство (**)
        ```
        """
        ...

    def GetParamByDistance(self) -> bool:  # icurvevertexparam_getparambydistance.html
        """
        ## GetParamByDistance - Получить расстояние и радиус (для ломаной) или вес (для сплайна)

        1. Для вершины с индексом 0 расстояние выдается до вершины с индексом 1. Для остальных вершин расстояние выдается до вершины с индексом на 1 меньше, чем у данной вершины.

        2. Получить расстояние можно для любого типа построения.

        3. Установить расстояние можно, если тип построения ksLineBuildingType не равен ksLBTByPoint и не равен ksLBTByPoint3DParams.

        ### Синтаксис Automation

        ```
        BOOL GetParamByDistance( double * Distance,
        double * Value );
        HRESULT GetParamByDistance( [out] double * Distance,
        [out] double * Value,
        [out, retval] VARIANT_BOOL * PVal );
        ```

        ### Выходные параметры

        +----------------------------------------------------------+
        | Distance | - расстояние до предыдущей вершины,           |
        +----------------------------------------------------------+
        | value    | - радиус для точки ломаной и вес для сплайна. |
        +----------------------------------------------------------+

        ### Возвращаемое значение

        +-----------------------------------------+
        | TRUE  | - в случае удачного завершения, |
        +-----------------------------------------+
        | FALSE | - в случае неудачи.             |
        +-----------------------------------------+
        """
        ...

    def GetParamVertex(self) -> bool:  # icurvevertexparam_getparamvertex.html
        """
        ## GetParamVertex - Получить параметры вершины

        ### Синтаксис Automation

        ```
        BOOL GetParamVertex( double * x,
        double * y,
        double * z,
        double * value );
        HRESULT GetParamVertex( [out] double * x,
        [out] double * y,
        [out] double * z,
        [out] double * value,
        [out, retval] VARIANT_BOOL * PVal );
        ```

        ### Выходные параметры

        +---------------------------------------------------------+
        | x, y, z | - пространственные координаты вершины,        |
        +---------------------------------------------------------+
        | value   | - радиус для точки ломаной и вес для сплайна. |
        +---------------------------------------------------------+

        ### Возвращаемое значение

        +-----------------------------------------+
        | TRUE  | - в случае удачного завершения, |
        +-----------------------------------------+
        | FALSE | - в случае неудачи.             |
        +-----------------------------------------+
        """
        ...

    def SetParamByDistance(self, Distance, Value) -> bool:  # icurvevertexparam_setparambydistance.html
        """
        ## SetParamByDistance - Установить расстояние, радиус (ломаная) или вес (сплайн)

        1. Нельзя установить расстояние для вершины с индексом 0.

        2. Установить расстояние можно, если тип построения ksLineBuildingType не равен ksLBTByPoint и не равен ksLBTByPoint3DParams.

        ### Синтаксис Automation

        ```
        double Distance,
        double Value );
        HRESULT SetParamByDistance( [in] double Distance,
        double Value,
        [out, retval] VARIANT_BOOL * PVal );
        Distance  # - расстояние до предыдущей вершины,
        value     # - радиус для точки ломаной и вес для сплайна.
        ```

        ### Возвращаемое значение

        +-----------------------------------------+
        | TRUE  | - в случае удачного завершения, |
        +-----------------------------------------+
        | FALSE | - в случае неудачи.             |
        +-----------------------------------------+
        """
        ...

    def SetParamByVertex(self, Obj, Value) -> bool:  # icurvevertexparam_setparambyvertex.html
        """
        ## SetParamByVertex - Установить параметры вершины по указателю на вершину

        ### Синтаксис Automation

        ```
        BOOL SetParamByVertex( LPDISPATCH Obj,
        double Value );
        HRESULT SetParamByVertex( [in] IModelObject * Obj,
        [in] double Value,
        [out, retval] VARIANT_BOOL * PVal );
        ```

        ### Входные параметры

        +-------------------------------------------------------+
        | obj   | - указатель на вершину,                       |
        +-------------------------------------------------------+
        | value | - радиус для точки ломаной и вес для сплайна. |
        +-------------------------------------------------------+

        ### Возвращаемое значение

        +-----------------------------------------+
        | TRUE  | - в случае удачного завершения. |
        +-----------------------------------------+
        | FALSE | - в случае неудачи.             |
        +-----------------------------------------+
        """
        ...

    def SetParamVertex(self, X, Y, Z, Value) -> bool:  # icurvevertexparam_setparamvertex.html
        """
        ## SetParamVertex - Установить параметры вершины

        ### Синтаксис Automation

        ```
        BOOL SetParamVertex( double x,
        double y,
        double z,
        double value );
        HRESULT SetParamVertex( [in] double x,
        [in] double y,
        [in] double z,
        [in] double value,
        [out, retval] VARIANT_BOOL * PVal );
        ```

        ### Входные параметры

        +---------------------------------------------------------+
        | x, y, z | - пространственные координаты вершины,        |
        +---------------------------------------------------------+
        | value   | - радиус для точки ломаной и вес для сплайна. |
        +---------------------------------------------------------+

        ### Возвращаемое значение

        +-----------------------------------------+
        | TRUE  | - в случае удачного завершения. |
        +-----------------------------------------+
        | FALSE | - в случае неудачи.             |
        +-----------------------------------------+
        """
        ...

    def Update(self) -> bool:  # icurvevertexparam_update.html
        """
        ## Update - Обновить параметры вершин кривой начиная с этой

        ### Синтаксис Automation

        ```
        BOOL Update();
        HRESULT Update( BOOL * Result );
        ```

        ### Возвращаемое значение

        +-----------------------------------------+
        | TRUE  | - в случае удачного завершения, |
        +-----------------------------------------+
        | FALSE | - в случае неудачи.             |
        +-----------------------------------------+
        """
        ...




class IModelCurve3D(IDispatch):  # imodelcurve3d.html
    """
    ## Интерфейс IModelCurve3D

    Объект 3D-кривая.

    Позволяет получить и изменить общие свойства для 3D-кривых.

    Является дополнительным интерфейсом для IModelObject.
    """

    @property
    def EdgesStyle(self) -> int:  # imodelcurve3d_edgesstyle.html
        """
        ## EdgesStyle - Стиль линии

        Тип данных: из перечисления ksCurveStyleEnum

        ### Синтаксис Automation

        ```
        EdgesStyle = Object.EdgesStyle       # Получить свойство (* )
        Object.EdgesStyle = EdgesStyle       # Установить свойство (* )
        EdgesStyle = Object.GetEdgesStyle()  # Получить свойство (**)
        Object.SetEdgesStyle( EdgesStyle )   # Установить свойство (**)
        ```
        """
        ...
    @EdgesStyle.setter
    def EdgesStyle(self, value: int): ...

    @property
    def VertexStyle(self) -> int:  # imodelcurve3d_vertexstyle.html
        """
        ## VertexStyle - Стиль вершин

        Тип данных: из перечисления ksAnnotationSymbolEnum

        ### Синтаксис Automation

        ```
        VertexStyle = Object.VertexStyle       # Получить свойство (* )
        Object.VertexStyle = VertexStyle       # Установить свойство (* )
        VertexStyle = Object.GetVertexStyle()  # Получить свойство (**)
        Object.SetVertexStyle( VertexStyle )   # Установить свойство (**)
        ```
        """
        ...
    @VertexStyle.setter
    def VertexStyle(self, value: int): ...

    @property
    def VertexVisible(self) -> bool:  # imodelcurve3d_vertexvisible.html
        """
        ## VertexVisible - Признак отображения вершин

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        VertexVisible = Object.VertexVisible       # Получить свойство (* )
        Object.VertexVisible = VertexVisible       # Установить свойство (* )
        VertexVisible = Object.GetVertexVisible()  # Получить свойство (**)
        Object.SetVertexVisible( VertexVisible )   # Установить свойство (**)
        ```
        """
        ...
    @VertexVisible.setter
    def VertexVisible(self, value: bool): ...

    def GetVerticesParams(self) -> list[float]:  # imodelcurve3d_getverticesparams.html
        """
        ## GetVerticesParams - Получить параметры вершин кривой

        ### Синтаксис Automation

        ```
        VARIANT GetVerticesParams();
        ```

        ### Возвращаемое значение

        - массив параметров вершин - SafeArray вещественных чисел VT_ARRAY | VT_R8.
        """
        ...




class IMathCurve3D(IKompasAPIObject):  # imathcurve3d.html
    """
    ## Интерфейс IMathCurve3D

    Интерфейс математической кривой в трехмерном пространстве.
    """

    @property
    def BaseCurve2DType(self) -> int:  # imathcurve3d_basecurve2dtype.html
        """
        ## BaseCurve2DType - Тип базовой 2D кривой

        Тип данных: из перечисления ksMathCurve2DTypeEnum

        Свойство доступно только для чтения.

        Версия КОМПАС v22

        ### Синтаксис Automation

        ```
        BaseCurve2DType = Object.BaseCurve2DType;       # Получить свойство (* )
        BaseCurve2DType = Object.GetBaseCurve2DType();  # Получить свойство (**)
        ```
        """
        ...

    @property
    def BaseCurveType(self) -> int:  # imathcurve3d_basecurvetype.html
        """
        ## BaseCurveType - Тип базовой 3D кривой

        Тип данных: из перечисления ksMathCurve3DTypeEnum

        Свойство доступно только для чтения.

        Версия КОМПАС v22

        ### Синтаксис Automation

        ```
        BaseCurveType = Object.BaseCurveType;       # Получить свойство (* )
        BaseCurveType = Object.GetBaseCurveType();  # Получить свойство (**)
        ```
        """
        ...

    @property
    def Closed(self) -> bool:  # imathcurve3d_closed.html
        """
        ## Closed - Замкнутость кривой

        Тип данных: BOOL

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        Closed = Object.Closed;       # Получить свойство (* )
        Closed = Object.GetClosed();  # Получить свойство (**)
        ```
        """
        ...

    @property
    def CurveType(self) -> int:  # imathcurve3d_curvetype.html
        """
        ## CurveType - Получить тип кривой

        Тип данных: из перечисления ksMathCurve3DTypeEnum

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        CurveType = Object.CurveType;       # Получить свойство (* )
        CurveType = Object.GetCurveType();  # Получить свойство (**)
        ```
        """
        ...

    @property
    def Degenerate(self) -> bool:  # imathcurve3d_degenerate.html
        """
        ## Degenerate - Проверка вырожденности кривой

        Тип данных: BOOL

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        Degenerate = Object.Degenerate;       # Получить свойство (* )
        Degenerate = Object.GetDegenerate();  # Получить свойство (**)
        ```
        """
        ...

    @property
    def IsArc(self) -> bool:  # imathcurve3d_isarc.html
        """
        ## IsArc - Является ли кривая дугой окружности

        Тип данных: BOOL

        Свойство доступно только для чтения.

        Версия КОМПАС v22

        ### Синтаксис Automation

        ```
        IsArc = Object.IsArc;       # Получить свойство (* )
        IsArc = Object.GetIsArc();  # Получить свойство (**)
        ```
        """
        ...

    @property
    def IsCircle(self) -> bool:  # imathcurve3d_iscircle.html
        """
        ## IsCircle - Является ли кривая окружностью

        Тип данных: BOOL

        Свойство доступно только для чтения.

        Версия КОМПАС v22

        ### Синтаксис Automation

        ```
        IsCircle = Object.IsCircle;       # Получить свойство (* )
        IsCircle = Object.GetIsCircle();  # Получить свойство (**)
        ```
        """
        ...

    @property
    def IsEllipse(self) -> bool:  # imathcurve3d_isellipse.html
        """
        ## IsEllipse - Является ли кривая эллипсом

        Тип данных: BOOL

        Свойство доступно только для чтения

        Версия КОМПАС v22.

        ### Синтаксис Automation

        ```
        IsEllipse = Object.IsEllipse;       # Получить свойство (* )
        IsEllipse = Object.GetIsEllipse();  # Получить свойство (**)
        ```
        """
        ...

    @property
    def IsEllipseArc(self) -> bool:  # imathcurve3d_isellipsearc.html
        """
        ## IsEllipseArc - Является ли кривая дугой эллипса

        Тип данных: BOOL

        Свойство доступно только для чтения.

        Версия КОМПАС v22

        ### Синтаксис Automation

        ```
        IsEllipseArc = Object.IsEllipseArc;       # Получить свойство (* )
        IsEllipseArc = Object.GetIsEllipseArc();  # Получить свойство (**)
        ```
        """
        ...

    @property
    def IsLineSeg(self) -> bool:  # imathcurve3d_islineseg.html
        """
        ## IsLineSeg - Является ли кривая отрезком

        Тип данных: BOOL

        Свойство доступно только для чтения.

        Версия КОМПАС v22

        ### Синтаксис Automation

        ```
        IsLineSeg = Object.IsLineSeg;       # Получить свойство (* )
        IsLineSeg = Object.GetIsLineSeg();  # Получить свойство (**)
        ```
        """
        ...

    @property
    def IsNurbs(self) -> bool:  # imathcurve3d_isnurbs.html
        """
        ## IsNurbs - Определить, является ли кривая NURBS

        Тип данных: BOOL

        Свойство доступно только для чтения.

        Версия КОМПАС v22

        ### Синтаксис Automation

        ```
        IsNurbs = Object.IsNurbs;       # Получить свойство (* )
        IsNurbs = Object.GetIsNurbs();  # Получить свойство (**)
        ```
        """
        ...

    @property
    def IsPlanar(self) -> bool:  # imathcurve3d_isplanar.html
        """
        ## IsPlanar - Является ли кривая плоской

        Тип данных: BOOL

        Свойство доступно только для чтения.

        Версия КОМПАС v22

        ### Синтаксис Automation

        ```
        IsPlanar = Object.IsPlanar;       # Получить свойство (* )
        IsPlanar = Object.GetIsPlanar();  # Получить свойство (**)
        ```
        """
        ...

    @property
    def IsStraight(self) -> bool:  # imathcurve3d_isstraight.html
        """
        ## IsStraight - Является ли кривая прямолинейной

        Тип данных: BOOL

        Свойство доступно только для чтения.

        Версия КОМПАС v22

        ### Синтаксис Automation

        ```
        IsStraight = Object.IsStraight;       # Получить свойство (* )
        IsStraight = Object.GetIsStraight();  # Получить свойство (**)
        ```
        """
        ...

    @property
    def ParamMax(self) -> float:  # imathcurve3d_parammax.html
        """
        ## ParamMax - Получить значение параметра конечное

        Тип данных: double

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        ParamMax = Object.ParamMax;       # Получить свойство (* )
        ParamMax = Object.GetParamMax();  # Получить свойство (**)
        ```
        """
        ...

    @property
    def ParamMin(self) -> float:  # imathcurve3d_parammin.html
        """
        ## ParamMin - Получить значение параметра начальное

        Тип данных: double

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        ParamMin = Object.ParamMin;       # Получить свойство (* )
        ParamMin = Object.GetParamMin();  # Получить свойство (**)
        ```
        """
        ...

    @property
    def Periodic(self) -> bool:  # imathcurve3d_periodic.html
        """
        ## Periodic - Периодичность замкнутой кривой

        Тип данных:BOOL

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        Periodic = Object.Periodic;       # Получить свойство (* )
        Periodic = Object.GetPeriodic();  # Получить свойство (**)
        ```
        """
        ...

    @property
    def Placement(self) -> IPlacement3D:  # imathcurve3d_placement.html
        """
        ## Placement - Интерфейс локальной системы координат (положение объекта)

        Тип данных: Указатель на интерфейс IPlacement3D

        Свойство доступно только для чтения.

        Версия КОМПАС v22

        ### Синтаксис Automation

        ```
        Placement = Object.Placement;       # Получить свойство (* )
        Placement = Object.GetPlacement();  # Получить свойство (**)
        ```
        """
        ...

    @property
    def Radius(self) -> float:  # imathcurve3d_radius.html
        """
        ## Radius - Радиус кривой

        Тип данных: double

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        Radius = Object.Radius;       # Получить свойство (* )
        Radius = Object.GetRadius();  # Получить свойство (**)
        ```
        """
        ...

    def CalculatePolygon(self, Step) -> list[float]:  # imathcurve3d_calculatepolygon.html
        """
        ## CalculatePolygon - Получить полигон точек на кривой

        1. Если шаг задан равным 0, то применяется шаг, используемый в системе КОМПАС, который рассчитывается с учетом габаритов детали.

        2. Если шаг задан -1, то применяется шаг, используемый в системе КОМПАС при отрисовке кривых. Он рассчитывается с учетом следующих параметров:

        •габариты детали и масштаба отображения модели,

        •тип возвращаемого массива VT_ARRAY | VT_R8,

        •значения координат в массиве лежат в последовательности x1, y1, z1, x2, y2, z2 ... xn, yn, zn.

        ### Синтаксис Automation

        ```
        VARIANT CalculatePolygon( double Step );
        ```

        ### Входные параметры

        +------------------------------------+
        | Step | - шаг для расчета полигона. |
        +------------------------------------+

        ### Возвращаемое значение

        - массив VT_ARRAY | VT_R8 координат точек на кривой.
        """
        ...

    def GetCentre(self) -> bool:  # imathcurve3d_getcentre.html
        """
        ## GetCentre - Получить центр кривой

        ### Синтаксис Automation

        ```
        BOOL GetCentre( double * X, double * Y, double * Z );
        ```

        ### Выходные параметры

        +---------------------------------------+
        | X, Y, Z | - координаты центра кривой. |
        +---------------------------------------+

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def GetDerivativeT(self, ParamT) -> bool:  # imathcurve3d_getderivativet.html
        """
        ## GetDerivativeT - Получить первую производную по T

        ### Синтаксис Automation

        ```
        BOOL GetDerivativeT( double ParamT, double * X, double * Y, double * Z );
        ```

        ### Входные параметры

        +-------------------------------------------------------------------------+
        | ParamT | - значение параметра T в параметрическом представлении кривой. |
        +-------------------------------------------------------------------------+

        ### Выходные параметры

        +--------------------------------------+
        | X, Y, Z | - первая производная по T. |
        +--------------------------------------+

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def GetDerivativeTT(self, ParamT) -> bool:  # imathcurve3d_getderivativett.html
        """
        ## GetDerivativeTT - Получить вторую производную по T

        ### Синтаксис Automation

        ```
        BOOL GetDerivativeTT( double ParamT, double * X, double * Y, double * Z );
        ```

        ### Входные параметры

        +-------------------------------------------------------------------------+
        | ParamT | - значение параметра T в параметрическом представлении кривой. |
        +-------------------------------------------------------------------------+

        ### Выходные параметры

        +--------------------------------------+
        | X, Y, Z | - вторая производная по T. |
        +--------------------------------------+

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def GetDerivativeTTT(self, ParamT) -> bool:  # imathcurve3d_getderivativettt.html
        """
        ## GetDerivativeTTT - Получить третью производную по T

        ### Синтаксис Automation

        ```
        BOOL GetDerivativeTTT( double ParamT, double * X, double * Y, double * Z );
        ```

        ### Входные параметры

        +-------------------------------------------------------------------------+
        | ParamT | - значение параметра T в параметрическом представлении кривой. |
        +-------------------------------------------------------------------------+

        ### Выходные параметры

        +--------------------------------------+
        | X, Y, Z | - третья производная по T. |
        +--------------------------------------+

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def GetEllipseParam(self) -> bool:  # imathcurve3d_getellipseparam.html
        """
        ## GetEllipseParam - Параметры эллипса

        ### Синтаксис Automation

        ```
        BOOL GetEllipseParam( double * MajorRadius, double * MinorRadius, double * Angle ) ;
        ```

        ### Выходные параметры

        +-----------------------------------------+
        | Angle       | - Угол раствора,          |
        +-----------------------------------------+
        | MajorRadius | - Большой радиус эллипса, |
        +-----------------------------------------+
        | MinorRadius | - Малый радиус эллипса.   |
        +-----------------------------------------+

        Версия КОМПАС v22

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def GetGabarit(self) -> bool:  # imathcurve3d_getgabarit.html
        """
        ## GetGabarit - Выдать габарит кривой

        Координаты вершин параллелепипеда возвращаются в системе координат компонента.

        ### Синтаксис Automation

        ```
        BOOL GetGabarit( double * X1, double * Y1, double * Z1, double * X2, double * Y2, double * Z2 ) ;
        ```

        ### Входные параметры

        +---------------------------------------------------------------------------+
        | X1, Y1, Z1, X2, Y2, Z2 | - координаты вершин габаритного параллелепипеда. |
        +---------------------------------------------------------------------------+

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def GetLength(self, BitVector):  # imathcurve3d_getlength.html
        """
        ## GetLength - Получить длину кривой (ST_MIX_MM..ST_MIX_M единицы измерения)

        ### Синтаксис Automation

        ```
        double GetLength( ksLengthUnitEnum BitVector ) ;
        ```

        ### Входные параметры

        +----------------------------------+
        | BitVector | - единица измерения. |
        +----------------------------------+

        ### Возвращаемое значение

        - длина кривой.
        """
        ...

    def GetMetricLength(self, StartParam, EndParam):  # imathcurve3d_getmetriclength.html
        """
        ## GetMetricLength - Метрическая длина кривой

        ### Синтаксис Automation

        ```
        double GetMetricLength( double StartParam, double EndParam );
        ```

        ### Входные параметры

        +---------------------------------------------------------------------------------------+
        | StartParam | - начальное значение параметра T в параметрическом представлении кривой, |
        +---------------------------------------------------------------------------------------+
        | EndParam   | - конечное значение параметра T в параметрическом представлении кривой.  |
        +---------------------------------------------------------------------------------------+

        ### Возвращаемое значение

        - длина кривой.
        """
        ...

    def GetNormal(self, ParamT) -> bool:  # imathcurve3d_getnormal.html
        """
        ## GetNormal - Получить нормаль

        ### Синтаксис Automation

        ```
        BOOL GetNormal( double ParamT, double * X, double * Y, double * Z ;
        ```

        ### Входные параметры

        +-------------------------------------------------------------------------+
        | ParamT | - значение параметра T в параметрическом представлении кривой. |
        +-------------------------------------------------------------------------+

        ### Выходные параметры

        +-----------------------------+
        | X, Y, Z | - вектор нормали. |
        +-----------------------------+

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def GetNurbs3DParams(self, Closed) -> bool:  # imathcurve3d_getnurbs3dparams.html
        """
        ## GetNurbs3DParams - Параметры Nurbs-представления кривой

        ### Синтаксис Automation

        ```
        BOOL GetNurbs3DParams( BOOL Closed, long * Degree, double * ParamMin, double * ParamMax, VARIANT * Points, VARIANT * Weights, VARIANT * Knots ) ;
        ```

        ### Входные параметры

        +------------------------------------------------------------------------------------------------------------------------------+
        | Closed | - TRUE - Вернуть параметры для замкнутого представления - FALSE - Вернуть параметры для разомкнутого представления. |
        +------------------------------------------------------------------------------------------------------------------------------+

        ### Выходные параметры

        +----------------------------------------------------------------+
        | Degree   | - степень,                                          |
        +----------------------------------------------------------------+
        | ParamMin | - минимальный параметр кривой,                      |
        +----------------------------------------------------------------+
        | ParamMax | - максимальный параметр кривой,                     |
        +----------------------------------------------------------------+
        | Points   | - координаты точек в виде массива VT_ARRAY | VT_R8, |
        +----------------------------------------------------------------+
        | Weights  | - веса в точках в виде массива VT_ARRAY | VT_R8,    |
        +----------------------------------------------------------------+
        | Knots    | - массив VT_ARRAY | VT_R8 узлов сплайна.            |
        +----------------------------------------------------------------+

        Версия КОМПАС v22

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def GetPoint(self, ParamT) -> bool:  # imathcurve3d_getpoint.html
        """
        ## GetPoint - Получить точку на кривой

        ### Синтаксис Automation

        ```
        BOOL GetPoint( double ParamT, double * X, double * Y, double * Z );
        ```

        ### Входные параметры

        +-------------------------------------------------------------------------+
        | ParamT | - значение параметра T в параметрическом представлении кривой. |
        +-------------------------------------------------------------------------+

        ### Выходные параметры

        +-----------------------------------------+
        | X, Y, Z | - координаты точки на кривой. |
        +-----------------------------------------+

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def GetTangentVector(self, ParamT) -> bool:  # imathcurve3d_gettangentvector.html
        """
        ## GetTangentVector - Получить тангенциальный вектор (нормализованный)

        ### Синтаксис Automation

        ```
        BOOL GetTangentVector( double ParamT, double * X, double * Y, double * Z );
        ```

        ### Входные параметры

        +-------------------------------------------------------------------------+
        | ParamT | - значение параметра T в параметрическом представлении кривой. |
        +-------------------------------------------------------------------------+

        ### Выходные параметры

        +---------------------------------+
        | X, Y, Z | - касательный вектор. |
        +---------------------------------+

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def GetWeightCentre(self) -> bool:  # imathcurve3d_getweightcentre.html
        """
        ## GetWeightCentre - Получить центр тяжести кривой

        ### Синтаксис Automation

        ```
        BOOL GetWeightCentre( double * X, double * Y, double * Z );
        ```

        ### Выходные параметры

        +-----------------------------------------------+
        | X, Y, Z | - координаты центра тяжести кривой. |
        +-----------------------------------------------+

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def NearPointProjection(self, X, Y, Z, Ext) -> bool:  # imathcurve3d_nearpointprojection.html
        """
        ## NearPointProjection - Получить ближайшую проекцию точки на кривую

        ### Синтаксис Automation

        ```
        BOOL NearPointProjection( double X, double Y, double Z, double * T, BOOL Ext );
        ```

        ### Входные параметры

        +---------------------------------------------------------------------------------------------------------------------------------+
        | X, Y, Z | - координаты исходной пространственной точки,                                                                         |
        +---------------------------------------------------------------------------------------------------------------------------------+
        | Ext     | - если проекция точки вне кривой,                                                                                     |
        +---------------------------------------------------------------------------------------------------------------------------------+
        | TRUE    | - проецировать на продолжение кривой,                                                                                 |
        +---------------------------------------------------------------------------------------------------------------------------------+
        | FALSE   | - найти ближайшую граничную точку кривой. Если проекция точки внутри кривой, значение этого параметра не учитывается. |
        +---------------------------------------------------------------------------------------------------------------------------------+

        ### Выходные параметры

        +---------------------------------------------------+
        | T | - параметрические координаты точки на кривой. |
        +---------------------------------------------------+

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...




class IModelObject(IKompasAPIObject):  # imodelobject.html
    """
    ## Интерфейс IModelObject

    Базовый интерфейс для всех модельных объектов.

    1. Данный интерфейс является базовым для модельных объектов.

    2. Интерфейс можно получить из коллекции модельных объектов IModelObjects с помощью свойства IModelObjects::Item.

    3. Интерфейс может быть получен с помощью метода IUnknown QueryInterface от модельного объекта, например, IPart7, IFeature7.

    4. Интерфейс может быть получен от интерфейса IModelContainer с помощью свойства IModelContainer::Objects из массива SAFEARRAY типа VT_ARRAY|VT_DISPATCH с преобразованием от интерфейса IDispatch к IModelObject c помощью метода IUnknown QueryInterface.

    5. Интерфейс может быть получен от интерфейса IFeature7 с помощью свойства IFeature7::ModelObjects из массива SAFEARRAY типа VT_ARRAY|VT_DISPATCH с преобразованием от интерфейса IDispatch к IModelObject c помощью метода IUnknown QueryInterface.

    6. Имеет дополнительный интерфейс IColorParam7, который можно получить с помощью метода IUnknown::QueryInterface.
    """

    @property
    def Hidden(self) -> bool:  # imodelobject_hidden.html
        """
        ## Hidden - Состояние видимости объекта

        Тип данных: BOOL

        Значения свойства:

        +---------------------------+
        | TRUE  | - объект скрыт,   |
        +---------------------------+
        | FALSE | - объект показан. |
        +---------------------------+

        1. Позволяет получить и установить состояние видимости объекта.

        2. Свойство вступает в силу после вызова метода IModelObject::Update.

        ### Синтаксис Automation

        ```
        Hidden = iObject.Hidden        # Получить свойство(* )
        iObject.Hidden = Hidden        # Установить свойство (* )
        Hidden = iObject.GetHidden( )  # Получить свойство (**)
        iObject.SetHidden ( Hidden )   # Установить свойство (**)
        ```
        """
        ...
    @Hidden.setter
    def Hidden(self, value: bool): ...

    @property
    def ModelObjectType(self):  # imodelobject_modelobjecttype.html
        """
        ## ModelObjectType - Тип модельного объекта

        Тип данных: указатель на интерфейс Obj3dType

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        ModelObjectType = iObject.ModelObjectType        # Получить свойство(* )
        ModelObjectType = iObject.GetModelObjectType( )  # Получить свойство (**)
        ```
        """
        ...

    @property
    def Name(self) -> str:  # imodelobject_name.html
        """
        ## Name - Имя элемента трехмерной модели

        Тип данных: строка

        1. Позволяет получить и установить имя трехмерной модели.

        2. Свойство вступает в силу после вызова метода IModelObject::Update.

        3. Не все объекты имеют имя, например, ребра.

        ### Синтаксис Automation

        ```
        Name = iObject.Name        # Получить свойство(* )
        iObject.Name = Name        # Установить свойство (* )
        Name = iObject.GetName( )  # Получить свойство (**)
        iObject.SetName( Name )    # Установить свойство (**)
        ```
        """
        ...
    @Name.setter
    def Name(self, value: str): ...

    @property
    def Owner(self) -> IFeature7:  # imodelobject_owner.html
        """
        ## Owner - Объект дерева, породивший этот объект

        Тип данных: указатель на интерфейс IFeature7

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        Owner = iObject.Owner        # Получить свойство(* )
        Owner = iObject.GetOwner( )  # Получить свойство (**)
        ```
        """
        ...

    @property
    def Part(self) -> IPart7:  # imodelobject_part.html
        """
        ## Part - Компонент, владеющий элементом

        Тип данных: указатель на интерфейс IPart7.

        1. Возвращает указатель на интерфейс компонента, владеющий объектом.

        2. Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        Part = iObject.Part        # Получить свойство(* )
        Part = iObject.GetPart( )  # Получить свойство (**)
        ```
        """
        ...

    @property
    def Valid(self) -> bool:  # imodelobject_valid.html
        """
        ## Valid - Признак невырожденности объекта

        Тип данных: BOOL

        Значения свойства:

        +---------------------------+
        | TRUE  | - объект скрыт,   |
        +---------------------------+
        | FALSE | - объект показан. |
        +---------------------------+

        1. Позволяет получить признак невырожденности объекта.

        2. Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        Valid = iObject.Valid        # Получить свойство(* )
        Valid = iObject.GetValid( )  # Получить свойство (**)
        ```
        """
        ...

    def Update(self) -> bool:  # imodelobject_update.html
        """
        ## Update - Обновить данные объекта

        1. Метод Update необходимо вызвать для объекта, свойства которого были изменены (после вызова Set- cвойств или методов интерфейса), чтобы эти изменения вступили в силу.

        2. Метод Update вернёт TRUE в случае успеха и FALSE в случае неудачи.

        3. Вызов Update сработает как при изменении одного свойства, так и при изменении группы свойств объекта.

        ### Синтаксис Automation

        ```
        BOOL Update( );
        ```

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...




class IMeshObject3D(IModelObject):  # imeshobject3d.html
    """
    ## Интерфейс IMeshObject3D

    Интерфейс пространственного полигонального объекта.
    """

    def InitByObjects(self, Objects) -> bool:  # imeshobject3d_initbyobjects.html
        """
        ## InitByObjects - Создать по объектам

        ### Синтаксис Automation

        ```
        BOOL InitByObjects( VARIANT Objects );
        ```

        ### Входные параметры

        +-----------------------------------------------------------------------------------------------+
        | Objects | - объект или массив объектов (VARIANT типа VT_DISPATCH или VT_ARRAY | VT_DISPATCH), |
        +-----------------------------------------------------------------------------------------------+

        ### Возвращаемое значение

        +-----------------------------------------+
        | TRUE | - в случае успешного завершения, |
        +-----------------------------------------+
        """
        ...




class IModelObject1(IKompasAPIObject):  # imodelobject1.html
    """
    ## Интерфейс IModelObject1

    Интерфейс 3D объекта.

    Интерфейс можно получить у интерфейса IModelObject с помощью метода IUnknown::QueryInterface.
    """

    @property
    def ConnectedWithInitialEmbodiment(self) -> bool:  # imodelobject1_connectedwithinitialembodiment.html
        """
        ## ConnectedWithInitialEmbodiment - Отменить/восстановить связь объекта зависимого исполнения с соответствующим объектом исходного исполнения

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        ConnectedWithInitialEmbodiment = Object.ConnectedWithInitialEmbodiment       # Получить свойство (* )
        Object.ConnectedWithInitialEmbodiment = ConnectedWithInitialEmbodiment       # Установить свойство (* )
        ConnectedWithInitialEmbodiment = Object.GetConnectedWithInitialEmbodiment()  # Получить свойство (**)
        Object.SetConnectedWithInitialEmbodiment( ConnectedWithInitialEmbodiment )   # Установить свойство (**)
        ```
        """
        ...
    @ConnectedWithInitialEmbodiment.setter
    def ConnectedWithInitialEmbodiment(self, value: bool): ...

    @property
    def Editable(self) -> int:  # imodelobject1_editable.html
        """
        ## Editable - Признак редактирования

        Тип данных: из перечисления ksEditableStateEnum

        ### Синтаксис Automation

        ```
        Editable = Object.Editable       # Получить свойство (* )
        Object.Editable = Editable       # Установить свойство (* )
        Editable = Object.GetEditable()  # Получить свойство (**)
        Object.SetEditable( Editable )   # Установить свойство (**)
        ```
        """
        ...
    @Editable.setter
    def Editable(self, value: int): ...

    @property
    def HiddenEx(self) -> int:  # imodelobject1_hiddenex.html
        """
        ## HiddenEx - Состояние видимости объекта

        Тип данных: из перечисления ksVisibleStateEnum

        ### Синтаксис Automation

        ```
        HiddenEx = Object.HiddenEx       # Получить свойство (* )
        Object.HiddenEx = HiddenEx       # Установить свойство (* )
        HiddenEx = Object.GetHiddenEx()  # Получить свойство (**)
        Object.SetHiddenEx( HiddenEx )   # Установить свойство (**)
        ```
        """
        ...
    @HiddenEx.setter
    def HiddenEx(self, value: int): ...

    @property
    def IsEditableObject(self) -> bool:  # imodelobject1_iseditableobject.html
        """
        ## IsEditableObject - Признак текущей доступности редактирования объекта

        Тип данных: BOOL

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        IsEditableObject = Object.IsEditableObject;       # Получить свойство (* )
        IsEditableObject = Object.GetIsEditableObject();  # Получить свойство (**)
        ```
        """
        ...

    @property
    def IsExternalObject(self) -> bool:  # imodelobject1_isexternalobject.html
        """
        ## IsExternalObject - Объект является внешнимпо отношению к текущему редактируемому контексту

        Тип данных: BOOL

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        IsExternalObject = Object.IsExternalObject       # Получить свойство (* )
        IsExternalObject = Object.GetIsExternalObject()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def LayerNumber(self) -> int:  # imodelobject1_layernumber.html
        """
        ## LayerNumber - Номер слоя

        Тип данных: long

        ### Синтаксис Automation

        ```
        LayerNumber = Object.LayerNumber       # Получить свойство (* )
        Object.LayerNumber = LayerNumber       # Установить свойство (* )
        LayerNumber = Object.GetLayerNumber()  # Получить свойство (**)
        Object.SetLayerNumber( LayerNumber )   # Установить свойство (**)
        ```
        """
        ...
    @LayerNumber.setter
    def LayerNumber(self, value: int): ...

    @property
    def Links(self) -> typing.Any:  # imodelobject1_links.html
        """
        ## Links - Ccылки на опорные примитивы объекта

        Тип данных: VARIANT массив

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        Links = Object.Links;       # Получить свойство (* )
        Links = Object.GetLinks();  # Получить свойство (**)
        ```
        """
        ...

    @property
    def MathObject(self) -> IKompasAPIObject:  # imodelobject1_mathobject.html
        """
        ## MathObject - Математический объект

        Тип данных: Указатель на интерфейс IKompasAPIObject

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        MathObject = Object.MathObject;       # Получить свойство (* )
        MathObject = Object.GetMathObject();  # Получить свойство (**)
        ```
        """
        ...

    @property
    def Projected(self) -> int:  # imodelobject1_projected.html
        """
        ## Projected - Признак проецирования

        Тип данных: из перечисления ksProjectionOptionEnum

        ### Синтаксис Automation

        ```
        Projected = Object.Projected       # Получить свойство (* )
        Object.Projected = Projected       # Установить свойство (* )
        Projected = Object.GetProjected()  # Получить свойство (**)
        Object.SetProjected( Projected )   # Установить свойство (**)
        ```
        """
        ...
    @Projected.setter
    def Projected(self, value: int): ...

    def Childrens(self, RelType) -> typing.Any:  # imodelobject1_childrens.html
        """
        ## Childrens - Потомки объекта

        Тип данных: VARIANT

        1. Свойство доступно только для чтения.

        2. Свойство возвращает массив VT_ARRAY | VT_DISPATCH объектов-потомков данного объекта.

        ### Синтаксис Automation

        ```
        Childrens = Object.Childrens( RelType );     # Получить свойство (* )
        Childrens = Object.GetChildrens( RelType );  # Получить свойство (**)
        ```

        ### Входные параметры

        +-----------------------------------------------------------+
        | ksRelationTypeEnum RelType | - тип родственных отношений. |
        +-----------------------------------------------------------+
        """
        ...

    def IsMyLink(self, RelType, Primitive) -> bool:  # imodelobject1_ismylink.html
        """
        ## IsMyLink - Является ли примитив опорным для объекта

        ### Синтаксис Automation

        ```
        BOOL IsMyLink( ksRelationTypeEnum RelType,
        LPDISPATCH Primitive );
        ```

        ### Входные параметры

        +------------------------------------------+
        | RelType   | - тип родственных отношений, |
        +------------------------------------------+
        | Primitive | - проверяемый примитив.      |
        +------------------------------------------+

        Метод позволяет проверить, является ли примитив опорным для объекта 3D (например, ребро кинематической операции опорным для операции скругления).

        ### Возвращаемое значение

        +-------------------------------------+
        | TRUE | - примитив является опорным. |
        +-------------------------------------+
        """
        ...

    def Parents(self, RelType) -> typing.Any:  # imodelobject1_parents.html
        """
        ## Parents- Родители объекта

        Тип данных: VARIANT

        1. Свойство доступно только для чтения.

        2. Свойство возвращает массив VT_ARRAY | VT_DISPATCH объектов-родителей данного объекта.

        ### Синтаксис Automation

        ```
        Parents = Object.Parents( RelType );     # Получить свойство (* )
        Parents = Object.GetParents( RelType );  # Получить свойство (**)
        ```

        ### Входные параметры

        +--------------------------------------------------------+
        | ksRelationTypeEnum RelType | тип родственных отношений |
        +--------------------------------------------------------+
        """
        ...

    def Reset(self) -> bool:  # imodelobject1_reset.html
        """
        ## Reset - Сбросить заданные параметры

        ### Синтаксис Automation

        ```
        BOOL Reset();
        ```

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...




class IWireFrames3D(IModelObjects):  # iwireframes3d.html
    """
    ## Интерфейс IWireFrames3D

    Интерфейс коллекции трехмерных каркасов.

    Интерфейс можно получить с помощью метода IModelContainer::WireFrames3D.

    Версия: Компас v20
    """

    def Add(self) -> IWireFrame3D:  # iwireframes3d_add.html
        """
        ## Add - Создает новый элемент и добавляет его в коллекцию

        ### Синтаксис Automation

        ```
        LPDISPATCH Add();
        ```

        ### Возвращаемое значение

        Указатель на интерфейс IWireFrame3D.
        """
        ...

    def WireFrame3D(self, Index) -> IWireFrame3D:  # iwireframes3d_wireframe3d.html
        """
        ## WireFrame3D - Возвращает элемент, заданный по индексу или по имени

        Тип данных: Указатель на интерфейс IWireFrame3D.

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        WireFrame3D = Object.WireFrame3D( Index )     # Получить свойство (* )
        WireFrame3D = Object.GetWireFrame3D( Index )  # Получить свойство (**)
        ```

        ### Входные параметры

        +----------------------------------------------------------------------------------------------------------------------------------+
        | VARIANT Index- | - Индекс объекта в коллекции. Поддерживаются следующие типы: - VT_I4 - индекс объекта, - VT_BSTR - имя объекта. |
        +----------------------------------------------------------------------------------------------------------------------------------+
        """
        ...




class IWireFrame3D(IModelObject):  # iwireframe3d.html
    """
    ## Интерфейс IWireFrame3D

    Интерфейс трехмерного каркаса.

    Интерфейс можно получить у коллекции дуг, используя свойство IWireFrames3D::WireFrame3D или метод IWireFrames3D::Add.

    Версия: Компас v20
    """

    @property
    def EdgesCount(self) -> int:  # iwireframe3d_edgescount.html
        """
        ## EdgesCount - Количество ребер

        Тип данных: long

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        EdgesCount = Object.EdgesCount       # Получить свойство (* )
        EdgesCount = Object.GetEdgesCount()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def VertexCount(self) -> int:  # iwireframe3d_vertexcount.html
        """
        ## VertexCount - Количество вершин

        Тип данных: long

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        VertexCount = Object.VertexCount       # Получить свойство (* )
        VertexCount = Object.GetVertexCount()  # Получить свойство (**)
        ```
        """
        ...

    def AddEdge(self, Obj) -> bool:  # iwireframe3d_addedge.html
        """
        ## AddEdge - Добавить ребро

        ### Синтаксис Automation

        ```
        BOOL AddEdge( IKompasAPIObject * Obj );
        ```

        ### Входные параметры

        +--------------------------------------------------+
        | Obj | - Указатель на интерфейс IKompasAPIObject. |
        +--------------------------------------------------+

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def AddVertex(self, Obj) -> bool:  # iwireframe3d_addvertex.html
        """
        ## AddVertex - Добавить вершину

        ### Синтаксис Automation

        ```
        BOOL AddVertex( LPDISPATCH Obj );
        ```

        ### Входные параметры

        +--------------------------------------------------+
        | Obj | - Указатель на интерфейс IKompasAPIObject. |
        +--------------------------------------------------+

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def AddVertexByPoint(self, X, Y, Z) -> bool:  # iwireframe3d_addvertexbypoint.html
        """
        ## AddVertexByPoint - Добавить вершину по координатам

        ### Синтаксис Automation

        ```
        BOOL AddVertexByPoint( double X, double Y, double Z );
        ```

        ### Входные параметры

        +-------------------------------+
        | X, Y, Z | - координаты точки. |
        +-------------------------------+

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def ClearEdges(self) -> bool:  # iwireframe3d_clearedges.html
        """
        ## ClearEdges - Удалить ребра

        ### Синтаксис Automation

        ```
        BOOL ClearEdges();
        ```

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def ClearVertexes(self) -> bool:  # iwireframe3d_clearvertexes.html
        """
        ## ClearVertexes - Удалить вершины

        ### Синтаксис Automation

        ```
        BOOL ClearVertexes();
        ```

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def Edge(self, Index) -> IModelObject:  # iwireframe3d_edge.html
        """
        ## Edge - Получить ребро каркаса по индексу

        Тип данных: Указатель на интерфейс IModelObject.

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        Edge = Object.Edge( Index )     # Получить свойство (* )
        Edge = Object.GetEdge( Index )  # Получить свойство (**)
        ```

        ### Входные параметры

        +--------------------------------+
        | long Index | - индекс объекта. |
        +--------------------------------+
        """
        ...

    def Vertex(self, Index) -> IModelObject:  # iwireframe3d_vertex.html
        """
        ## Vertex - Получить вершину каркаса по индексу

        Тип данных: Указатель на интерфейс IModelObject.

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        Vertex = Object.Vertex( Index )     # Получить свойство (* )
        Vertex = Object.GetVertex( Index )  # Получить свойство (**)
        ```

        ### Входные параметры

        +--------------------------------+
        | long Index | - индекс объекта. |
        +--------------------------------+
        """
        ...




class IFindObject3DParameters(IKompasAPIObject):  # ifindobject3dparameters.html
    """
    ## Интерфейс IFindObject3DParameters

    Интерфейс параметров поиска объектов 3D.

    События:

    см. ksFindObject3DParametersNotify

    Подписка на события ksFindObject3DParametersNotify позволяет осуществить дополнительную фильтрацию объектов в функциях поиска:

    IKompasDocument3D1::PickModelObjects

    IKompasDocument3D1::FindNearestObject

    Версия: Компас v20
    """

    @property
    def ModelObjectType(self) -> int:  # ifindobject3dparameters_modelobjecttype.html
        """
        ## ModelObjectType - Фильтрация объектов по типу объекта

        Тип данных: из перечисления ksObj3dTypeEnum

        ### Синтаксис Automation

        ```
        ModelObjectType = Object.ModelObjectType       # Получить свойство (* )
        Object.ModelObjectType = ModelObjectType       # Установить свойство (* )
        ModelObjectType = Object.GetModelObjectType()  # Получить свойство (**)
        Object.SetModelObjectType( ModelObjectType )   # Установить свойство (**)
        ```
        """
        ...
    @ModelObjectType.setter
    def ModelObjectType(self, value: int): ...




class IUserParameters(IDispatch):  # iuserparameters.html
    """
    ## Интерфейс IUserParameters

    Интерфейс объекта, создаваемого библиотекой.
    """

    @property
    def Command(self) -> int:  # iuserparameters_command.html
        """
        ## Command - Номер команды

        Тип данных: long

        Свойство позволяет устанавливать команду редактирования библиотеки.

        ### Синтаксис Automation

        ```
        Command = Object.Command       # Получить свойство (* )
        Object.Command = Command       # Установить свойство (* )
        Command = Object.GetCommand()  # Получить свойство (**)
        Object.SetCommand( Command )   # Установить свойство (**)
        ```
        """
        ...
    @Command.setter
    def Command(self, value: int): ...

    @property
    def LibraryFileName(self) -> str:  # iuserparameters_libraryfilename.html
        """
        ## LibraryFileName - Имя файла библиотеки

        Тип данных: BSTR

        Свойство позволяет устанавливать имя файла библиотеки.

        ### Синтаксис Automation

        ```
        FileName = Object.FileName       # Получить свойство (* )
        Object.FileName = FileName       # Установить свойство (* )
        FileName = Object.GetFileName()  # Получить свойство (**)
        Object.SetFileName( FileName )   # Установить свойство (**)
        ```
        """
        ...
    @LibraryFileName.setter
    def LibraryFileName(self, value: str): ...

    @property
    def LibraryName(self) -> str:  # iuserparameters_libraryname.html
        """
        ## LibraryName - Имя библиотеки

        Тип данных: BSTR

        Свойство позволяет устанавливать имя библиотеки.

        ### Синтаксис Automation

        ```
        Name = Object.Name       # Получить свойство (* )
        Object.Name = Name       # Установить свойство (* )
        Name = Object.GetName()  # Получить свойство (**)
        Object.SetName( Name )   # Установить свойство (**)
        ```
        """
        ...
    @LibraryName.setter
    def LibraryName(self, value: str): ...

    @property
    def ObjectID(self) -> str:  # iuserparameters_objectid.html
        """
        ## ObjectID - Идентификатор объекта

        Тип данных: BSTR

        ### Синтаксис Automation

        ```
        ObjectID = Object.ObjectID       # Получить свойство (* )
        Object.ObjectID = ObjectID       # Установить свойство (* )
        ObjectID = Object.GetObjectID()  # Получить свойство (**)
        Object.SetObjectID( ObjectID )   # Установить свойство (**)
        ```
        """
        ...
    @ObjectID.setter
    def ObjectID(self, value: str): ...

    @property
    def UserParams(self) -> typing.Any:  # iuserparameters_userparams.html
        """
        ## UserParams - Пользовательские параметры

        Тип данных: VARIANT

        Значение свойства:

        Принимает\\возвращает безопасный массив SAFEARRAY типа VT_ARRAY | VT_UI1.

        Свойство используется для сохранения/получения пользовательских параметров в виде безопасного массива SAFEARRAY байтов.

        ### Синтаксис Automation

        ```
        UserParams = Object.UserParams       # Получить свойство (* )
        Object.UserParams = UserParams       # Установить свойство (* )
        UserParams = Object.GetUserParams()  # Получить свойство (**)
        Object.SetUserParams( UserParams )   # Установить свойство (**)
        ```
        """
        ...
    @UserParams.setter
    def UserParams(self, value: typing.Any): ...

    def ClearUserParameters(self) -> bool:  # iuserparameters_clearuserparameters.html
        """
        ## ClearUserParameters - Очистить пользовательские параметры

        ### Синтаксис Automation

        ```
        BOOL ClearUserParameters();
        ```

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...




class IBodyRepositions(IModelObjects):  # ibodyrepositions.html
    """
    ## Интерфейс IBodyRepositions

    Интерфейс коллекции операций перепозиционирования тела, поверхности.
    """

    def Add(self) -> IBodyReposition:  # ibodyrepositions_add.html
        """
        ## Add - Создает новый элемент и добавляет его в коллекцию

        1. Метод позволяет создать новый интерфейс операции перепозиционирования тела, поверхности.

        2. После получения нового интерфейса нужно задать параметры операции и вызвать метод IModelObject::Update.

        ### Синтаксис Automation

        ```
        LPDISPATCH Add();
        ```

        ### Возвращаемое значение

        - указатель на интерфейс IBodyReposition.
        """
        ...

    def BodyReposition(self, Index) -> IBodyReposition:  # ibodyrepositions_bodyreposition.html
        """
        ## BodyReposition - Возвращает элемент, заданный по индексу

        Тип данных: Указатель на интерфейс IBodyReposition.

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        BodyReposition = Object.BodyReposition( Index );     # Получить свойство (* )
        BodyReposition = Object.GetBodyReposition( Index );  # Получить свойство (**)
        ```

        ### Входные параметры

        +--------------------------------------------+
        | VARIANT Index | - индекс или имя элемента. |
        +--------------------------------------------+
        """
        ...




class IBodyReposition(IModelObject):  # ibodyreposition.html
    """
    ## Интерфейс IBodyReposition

    Интерфейс перепозиционирования тела, поверхности.
    """

    @property
    def CopyBoby(self) -> bool:  # ibodyreposition_copyboby.html
        """
        ## CopyBoby - Копировать тело

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        CopyBoby = Object.CopyBoby       # Получить свойство (* )
        Object.CopyBoby = CopyBoby       # Установить свойство (* )
        CopyBoby = Object.GetCopyBoby()  # Получить свойство (**)
        Object.SetCopyBoby( CopyBoby )   # Установить свойство (**)
        ```
        """
        ...
    @CopyBoby.setter
    def CopyBoby(self, value: bool): ...

    @property
    def Position(self) -> ILocalCoordinateSystem:  # ibodyreposition_position.html
        """
        ## Position - Параметры смещения

        Тип данных: Указатель на интерфейс ILocalCoordinateSystem.

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        Position = Object.Position;       # Получить свойство (* )
        Position = Object.GetPosition();  # Получить свойство (**)
        ```
        """
        ...

    @property
    def RepositionBody(self) -> IKompasAPIObject:  # ibodyreposition_repositionbody.html
        """
        ## RepositionBody - Перемещаемое тело или поверхность

        Тип данных: Указатель на интерфейс IKompasAPIObject.

        ### Синтаксис Automation

        ```
        RepositionBody = Object.RepositionBody       # Получить свойство (* )
        Object.RepositionBody = RepositionBody       # Установить свойство (* )
        RepositionBody = Object.GetRepositionBody()  # Получить свойство (**)
        Object.SetRepositionBody( RepositionBody )   # Установить свойство (**)
        ```
        """
        ...
    @RepositionBody.setter
    def RepositionBody(self, value: IKompasAPIObject): ...

    @property
    def RepositionCentre(self) -> IModelObject:  # ibodyreposition_repositioncentre.html
        """
        ## RepositionCentre - Точка центра смещения

        Тип данных: Указатель на интерфейс IModelObject.

        ### Синтаксис Automation

        ```
        RepositionCentre = Object.RepositionCentre       # Получить свойство (* )
        Object.RepositionCentre = RepositionCentre       # Установить свойство (* )
        RepositionCentre = Object.GetRepositionCentre()  # Получить свойство (**)
        Object.SetRepositionCentre( RepositionCentre )   # Установить свойство (**)
        ```
        """
        ...
    @RepositionCentre.setter
    def RepositionCentre(self, value: IModelObject): ...




class IChooseObjects(IDispatch):  # ichooseobjects.html
    """
    ## Интерфейс IChooseObjects

    Интерфейс области применения для тел и компонентов документа в операции.
    """

    @property
    def ChooseBodies(self) -> typing.Any:  # ichooseobjects_choosebodies.html
        """
        ## ChooseBodies - Заменить массив тел (SAFEARRAY)

        Тип данных: VARIANT

        Свойство позволяет получать и устанавливать тело или массив тел, участвующих в операции.

        Массив возвращается в виде массива SAFEARRAY тел LPDISPATCH (VT_ARRAY | VT_DISPATCH).

        ### Синтаксис Automation

        ```
        ChooseBodies = Object.ChooseBodies       # Получить свойство (* )
        Object.ChooseBodies = ChooseBodies       # Установить свойство (* )
        ChooseBodies = Object.GetChooseBodies()  # Получить свойство (**)
        Object.SetChooseBodies( ChooseBodies )   # Установить свойство (**)
        ```
        """
        ...
    @ChooseBodies.setter
    def ChooseBodies(self, value: typing.Any): ...

    @property
    def ChooseParts(self) -> typing.Any:  # ichooseobjects_chooseparts.html
        """
        ## ChooseParts - Массив вставок (SAFEARRAY)

        Тип данных: VARIANT

        Свойство позволяет получать и устанавливать компонент или массив компонентов, участвующих в операции.

        Массив возвращается в виде массива SAFEARRAY компонентов LPDISPATCH (VT_ARRAY | VT_DISPATCH).

        ### Синтаксис Automation

        ```
        ChooseParts = Object.ChooseParts       # Получить свойство (* )
        Object.ChooseParts = ChooseParts       # Установить свойство (* )
        ChooseParts = Object.GetChooseParts()  # Получить свойство (**)
        Object.SetChooseParts( ChooseParts )   # Установить свойство (**)
        ```
        """
        ...
    @ChooseParts.setter
    def ChooseParts(self, value: typing.Any): ...

    @property
    def ChoosePartsType(self) -> int:  # ichooseobjects_choosepartstype.html
        """
        ## ChoosePartsType - Область применения. Объекты

        Тип данных: Значение из перечисления ksChoosePartsType.

        ### Синтаксис Automation

        ```
        ChoosePartsType = Object.ChoosePartsType       # Получить свойство (* )
        Object.ChoosePartsType = ChoosePartsType       # Установить свойство (* )
        ChoosePartsType = Object.GetChoosePartsType()  # Получить свойство (**)
        Object.SetChoosePartsType( ChoosePartsType )   # Установить свойство (**)
        ```
        """
        ...
    @ChoosePartsType.setter
    def ChoosePartsType(self, value: int): ...

    @property
    def ChooseType(self) -> int:  # ichooseobjects_choosetype.html
        """
        ## ChooseType - Область применения. Группы объектов

        Тип данных: Значение из перечисления ksChooseType.

        ### Синтаксис Automation

        ```
        ChooseType = Object.ChooseType       # Получить свойство (* )
        Object.ChooseType = ChooseType       # Установить свойство (* )
        ChooseType = Object.GetChooseType()  # Получить свойство (**)
        Object.SetChooseType( ChooseType )   # Установить свойство (**)
        ```
        """
        ...
    @ChooseType.setter
    def ChooseType(self, value: int): ...




class IComponentPositioner7(IKompasAPIObject):  # icomponentpositioner7.html
    """
    ## Интерфейс IComponentPositioner7

    Интерфейс управления положением компонентов в сборке.

    Интерфейс можно получить, используя свойство IKompasDocument3D1::ComponentPositioner.

    Версия КОМПАС v22
    """

    def Finish(self) -> bool:  # icomponentpositioner7_finish.html
        """
        ## Finish - Завершить перемещение

        ### Синтаксис Automation

        ```
        BOOL Finish();
        ```

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def InitAxis(self, Axis) -> bool:  # icomponentpositioner7_initaxis.html
        """
        ## InitAxis - Установить ось

        ### Синтаксис Automation

        ```
        BOOL InitAxis( IModelObject * Axis );
        ```

        ### Входные параметры

        +-------------------------------------------------------------------------------------------+
        | Axis | - указатель на интерфейс конструктивной оси или прямолинейного ребра IModelObject. |
        +-------------------------------------------------------------------------------------------+

        ### Возвращаемое значение

        +-----------------------------------------+
        | TRUE  | -в случае успешного завершения, |
        +-----------------------------------------+
        | FALSE | - в случае неудачи.             |
        +-----------------------------------------+
        """
        ...

    def InitAxisByPoints(self, X1, Y1, Z1, X2, Y2, Z2) -> bool:  # icomponentpositioner7_initaxisbypoints.html
        """
        ## InitAxisByPoints - Установить ось по точкам

        ### Синтаксис Automation

        ```
        BOOL InitAxisByPoints( double X1, double Y1, double Z1, double X2, double Y2, double Z2 );
        ```

        ### Входные параметры

        +---------------------------------------------+
        | X1, Y1, Z1 | - координаты первой точки оси, |
        +---------------------------------------------+
        | X2, Y2, Z2 | - координаты второй точки оси. |
        +---------------------------------------------+

        ### Возвращаемое значение

        +-----------------------------------------+
        | TRUE  | -в случае успешного завершения, |
        +-----------------------------------------+
        | FALSE | - в случае неудачи.             |
        +-----------------------------------------+
        """
        ...

    def InitPlane(self, Plane) -> bool:  # icomponentpositioner7_initplane.html
        """
        ## InitPlane - Установить плоскость

        ### Синтаксис Automation

        ```
        BOOL InitPlane( IModelObject * Plane );
        ```

        ### Входные параметры

        +-------------------------------------------------------------------------------------------------+
        | Plane | - указатель на интерфейс конструктивной плоскости или плоской поверхности IModelObject. |
        +-------------------------------------------------------------------------------------------------+

        ### Возвращаемое значение

        +-----------------------------------------+
        | TRUE  | -в случае успешного завершения, |
        +-----------------------------------------+
        | FALSE | - в случае неудачи.             |
        +-----------------------------------------+
        """
        ...

    def InitPlaneByPlacement(self, Placement) -> bool:  # icomponentpositioner7_initplanebyplacement.html
        """
        ## InitPlaneByPlacement - Установить плоскость по СК

        ### Синтаксис Automation

        ```
        BOOL InitPlaneByPlacement( IPlacement3D * Placement );
        HRESULT InitPlaneByPlacement( IPlacement3D * Placement, BOOL * Result );
        ```

        ### Входные параметры

        +-----------------------------------------------------------------------------------------+
        | Placement | - указатель на интерфейсIPlacement3D системы координат, задающей плоскость. |
        +-----------------------------------------------------------------------------------------+

        ### Возвращаемое значение

        +-----------------------------------------+
        | TRUE  | -в случае успешного завершения, |
        +-----------------------------------------+
        | FALSE | - в случае неудачи.             |
        +-----------------------------------------+
        """
        ...

    def InitPlaneByPoints(self, X1, Y1, Z1, X2, Y2, Z2, X3, Y3, Z3) -> bool:  # icomponentpositioner7_initplanebypoints.html
        """
        ## InitPlaneByPoints - Установить плоскость по точкам

        ### Синтаксис Automation

        ```
        BOOL InitPlaneByPoints( double X1, double Y1, double Z1, double X2, double Y2, double Z2, double X3, double Y3, double Z3 );
        ```

        ### Входные параметры

        +------------------------------------------------------+
        | X1, Y1, Z1 | - координаты первой точки на плоскости, |
        +------------------------------------------------------+
        | X2, Y2, Z2 | - координаты второй точки на плоскости, |
        +------------------------------------------------------+
        | X3, Y3, Z3 | - координаты третьей точки на плоскости |
        +------------------------------------------------------+

        ### Возвращаемое значение

        +-----------------------------------------+
        | TRUE  | -в случае успешного завершения, |
        +-----------------------------------------+
        | FALSE | - в случае неудачи.             |
        +-----------------------------------------+
        """
        ...

    def MoveComponent(self, X, Y, Z) -> bool:  # icomponentpositioner7_movecomponent.html
        """
        ## MoveComponent - Перемещение компонента

        ### Синтаксис Automation

        ```
        BOOL MoveComponent( double X, double Y, double Z );
        ```

        ### Входные параметры

        +--------------------------------------+
        | X, Y, Y | - координаты точки сдвига. |
        +--------------------------------------+

        ### Возвращаемое значение

        +-----------------------------------------+
        | TRUE  | -в случае успешного завершения, |
        +-----------------------------------------+
        | FALSE | - в случае неудачи.             |
        +-----------------------------------------+
        """
        ...

    def Prepare(self, Part, PositionerType) -> bool:  # icomponentpositioner7_prepare.html
        """
        ## Prepare - Подготовиться к перемещению компонента

        ### Синтаксис Automation

        ```
        long Prepare( IPart7 * Part, Positioner_Type PositionerType );
        ```

        ### Входные параметры

        +---------------------------------------------------------------------+
        | Part           | - указатель на интерфейс компонента IPart7,        |
        +---------------------------------------------------------------------+
        | PositionerType | - тип перемещения из перечисления Positioner_Type. |
        +---------------------------------------------------------------------+

        ### Возвращаемое значение

        +-----------------------------------------+
        | TRUE  | -в случае успешного завершения, |
        +-----------------------------------------+
        | FALSE | - в случае неудачи.             |
        +-----------------------------------------+
        """
        ...

    def RotateComponent(self, Angle) -> bool:  # icomponentpositioner7_rotatecomponent.html
        """
        ## RotateComponent - Подготовиться к повороту компонента

        ### Синтаксис Automation

        ```
        BOOL RotateComponent( double Angle );
        ```

        ### Входные параметры

        +----------------------------------------------+
        | Angle | - угол поворота компонента, радианы. |
        +----------------------------------------------+

        ### Возвращаемое значение

        +-----------------------------------------+
        | TRUE  | -в случае успешного завершения, |
        +-----------------------------------------+
        | FALSE | - в случае неудачи.             |
        +-----------------------------------------+
        """
        ...

    def SetDragPoint(self, X, Y, Z) -> bool:  # icomponentpositioner7_setdragpoint.html
        """
        ## SetDragPoint - Установить точку захвата ( ручка)

        ### Синтаксис Automation

        ```
        BOOL SetDragPoint( double X, double Y, double Z );
        ```

        ### Входные параметры

        +---------------------------------------+
        | X, Y, Z | - координаты точки захвата. |
        +---------------------------------------+

        ### Возвращаемое значение

        +-----------------------------------------+
        | TRUE  | -в случае успешного завершения, |
        +-----------------------------------------+
        | FALSE | - в случае неудачи.             |
        +-----------------------------------------+
        """
        ...




class IParts7(IModelObjects):  # iparts7.html
    """
    ## Интерфейс IParts7

    Интерфейс коллекции компонентов 3D документа.

    1. Данный интерфейс можно получить от интерфейса IPart7, используя свойство IPart7::Parts.

    2. Данный интерфейс можно получить от интерфейса IModelObjects, используя метод IUnknown QueryInterface.

    3. От интерфейса 3D-документа IKompasDocument3D может быть получен верхний компонент IKompasDocument3D::TopPart, а от полученного верхнего компонента IPart7 может быть получена коллекция компонентов IParts7 с помощью свойства IPart7::Parts. Элементы коллекции-компоненты могут быть получены у коллекции с помощью свойства IParts7::Part. Либо с помощью свойства IModelObjects::Item как объекты типа IModelObject с последующим приведением к типу IPart7 через QueryInterface. От каждого элемента коллекции-компонента может быть получена вложенная коллекция компонентов c помощью свойства IPart7::Parts. И так далее, пока имеются вложенные коллекции компонентов.
    """

    def Add(self, FileName, Plane):  # iparts7_add.html
        """
        ## Add - Создать новый компонент и добавить егов документ и коллекцию

        ### Синтаксис Automation

        ```
        LPDISPATCH Add( LPCTSTR FileName, LPDISPATCH plane );
        ```

        ### Входные параметры

        +----------------------------------------------------------------------------------------+
        | FileName | - имя файла детали, создаваемой в сборке,                                   |
        +----------------------------------------------------------------------------------------+
        | Plane    | - указатель на интерфейс плоского объекта, к которому приклеивается деталь. |
        +----------------------------------------------------------------------------------------+

        ### Возвращаемое значение

        +------------------------------------------------------------------+
        | указатель на интерфейс IPart7 | - в случае успешного завершения, |
        +------------------------------------------------------------------+
        | NULL                          | - в случае неудачи.              |
        +------------------------------------------------------------------+
        """
        ...

    def AddFromFile(self, FileName, ExternalFile, Redraw):  # iparts7_addfromfile.html
        """
        ## AddFromFile - Добавить существующий компонент из файла или из библиотеки моделей в документ и в коллекцию

        При вставке из файла указывается полный путь к файлу. При вставке из библиотеки моделей указывается полный путь к файлу библиотеки и путь внутри библиотеки моделей, например, "C:\\\\standard.l3d|Крепежные элементы|Болты|Болт_1".

        ### Синтаксис Automation

        ```
        LPDISPATCH AddFromFile (LPCTSTR FileName, BOOL ExternalFile, BOOL Redraw);
        ```

        ### Входные параметры

        +-------------------------------------------------------------------------------------------------------------------------------------------------------------------------+
        | FileName     | - имя файла, из которого будет вставлен компонент,                                                                                                       |
        +-------------------------------------------------------------------------------------------------------------------------------------------------------------------------+
        | ExternalFile | - признак сохранения связи с файлом-источником: TRUE - вставка со ссылкой на внешний файл, FALSE - вставка "телом", (без сохранения ссылки на источник), |
        +-------------------------------------------------------------------------------------------------------------------------------------------------------------------------+
        | Redraw       | - признак перестроения документа после вставки компонента: TRUE - перестроить документ, FALSE - не перестраивать.                                        |
        +-------------------------------------------------------------------------------------------------------------------------------------------------------------------------+

        ### Возвращаемое значение

        +------------------------------------------------------------------+
        | указатель на интерфейс IPart7 | - в случае успешного завершения, |
        +------------------------------------------------------------------+
        | NULL                          | - в случае неудачи.              |
        +------------------------------------------------------------------+
        """
        ...

    def CreateDocument(self, Type, Local, Redraw) -> IPart7:  # iparts7_createdocument.html
        """
        ## CreateDocument - Создать локальную деталь и добавить в коллекцию

        ### Синтаксис Automation

        ```
        LPDISPATCH CreateDocument( DocumentTypeEnum Type, BOOL Local, BOOL Redraw );
        ```

        ### Входные параметры

        +------------------------------------------------+
        | Type   | - тип документа вставки,              |
        +------------------------------------------------+
        | Local  | - признак локальной детали,           |
        +------------------------------------------------+
        | Redraw | - TRUE для обновления окна документа. |
        +------------------------------------------------+

        ### Возвращаемое значение

        - указатель на интерфейс вставки IPart7.
        """
        ...

    def CreateDocumentEx(self, StartFileName, Type, Local, Redraw) -> IPart7:  # iparts7_createdocumentex.html
        """
        ## CreateDocumentEx - Создать локальную деталь и добавить в коллекцию

        ### Синтаксис Automation

        ```
        LPDISPATCH CreateDocumentEx( BSTR StartFileName, DocumentTypeEnum Type, BOOL Local, BOOL Redraw );
        ```

        ### Входные параметры

        +-------------------------------------------------------+
        | StartFileName | - имя файла по умолчанию,             |
        +-------------------------------------------------------+
        | Type          | - тип документа вставки,              |
        +-------------------------------------------------------+
        | Local         | - признак локальной детали,           |
        +-------------------------------------------------------+
        | Redraw        | - TRUE для обновления окна документа. |
        +-------------------------------------------------------+

        ### Возвращаемое значение

        - указатель на интерфейс вставки IPart7.
        """
        ...

    def CreateSubAssembly(self, Parts, FileName) -> IPart7:  # iparts7_createsubassembly.html
        """
        ## CreateSubAssembly - Объединить в подсборку

        ### Синтаксис Automation

        ```
        LPDISPATCH CreateSubAssembly( VARIANT Parts, BSTR FileName );
        ```

        ### Входные параметры

        +-----------------------------------------------------------------------------------------------+
        | Parts    | - Список SafeArray VT_ARRAY | VT_DISPATCH перемещаемых в подсборку вставок IPart7, |
        +-----------------------------------------------------------------------------------------------+
        | FileName | - Имя файла.                                                                       |
        +-----------------------------------------------------------------------------------------------+

        Версия: Компас v20

        ### Возвращаемое значение

        - Указатель на интерфейс новой вставки IPart7,
        """
        ...

    def FindPart(self, UniqueMetaObjectKey, Reqursive) -> IPart7:  # iparts7_findpart.html
        """
        ## FindPart - Возвращает компонент по ключу информационного объекта

        ### Синтаксис Automation

        ```
        LPDISPATCH FindPart( BSTR UniqueMetaObjectKey, BOOL Reqursive );
        ```

        ### Входные параметры

        +------------------------------------------------------------------+
        | UniqueMetaObjectKey | - Уникальный ключ информационного объекта, |
        +------------------------------------------------------------------+
        | Reqursive           | - Признак необходимости поиска в глубину.  |
        +------------------------------------------------------------------+

        Версия: Компас v20

        ### Возвращаемое значение

        - Указатель на интерфейс IPart7,
        """
        ...

    def MoveParts(self, Parts) -> bool:  # iparts7_moveparts.html
        """
        ## MoveParts - Переместить вставки

        Функция позволяет переместить вставки по дереву сборки. Например, можно переместить вставки одной подсборки в другую подсборку или в верхнюю сборку.

        Версия: Компас v20

        ### Синтаксис Automation

        ```
        BOOL MoveParts( VARIANT Parts );
        ```

        ### Входные параметры

        +--------------------------------------------------------------------------------+
        | Parts | - Список SafeArray VT_ARRAY | VT_DISPATCH перемещаемых вставок IPart7. |
        +--------------------------------------------------------------------------------+

        ### Возвращаемое значение

        +-------------------------------------+
        | TRUE  | - есть локальный результат, |
        +-------------------------------------+
        | FALSE | - в случае неудачи.         |
        +-------------------------------------+
        """
        ...

    def Part(self, Index) -> IPart7:  # iparts7_part.html
        """
        ## Part - Компонент, заданный по индексу или по имени

        Тип данных: указатель на интерфейс IPart7

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        Part = iObject.Part (Index)     # Получить свойство (* )
        Part = iObject.GetPart (Index)  # Получить свойство (**)
        ```

        ### Входные параметры

        +------------------------------------------------------------------------+
        | Index (Variant) | VT_BSTR - имя компонента, VT_I4 - индекс компонента. |
        +------------------------------------------------------------------------+
        """
        ...

    def SaveAsDetail(self, Objects, Param) -> IPart7:  # iparts7_saveasdetail.html
        """
        ## SaveAsDetail - Преобразовать в деталь

        Функция является аналогом команды Компас Преобразовать в деталь/локальную деталь.

        Преобразование в деталь/локальную деталь возможно только для объектов, построенных непосредственно в текущей модели (для локальной детали — только в текущей сборке) и относящихся к следующим типам:

        •тела,

        •поверхности,

        •пространственные кривые и точки,

        •локальные системы координат,

        •вспомогательные плоскости,

        •вспомогательные оси.

        Объекты могут быть как перемещены, так и скопированы в новый документ.

        Версия: Компас v20

        ### Синтаксис Automation

        ```
        LPDISPATCH SaveAsDetail( VARIANT Objects, LPDISPATCH Param );
        ```

        ### Входные параметры

        +----------------------------------------------------------------------------------------------+
        | Objects | - Список SafeArray VT_ARRAY | VT_DISPATCH объектов, передаваемых в новый документ, |
        +----------------------------------------------------------------------------------------------+
        | Param   | - Параметры создания нового документа.                                             |
        +----------------------------------------------------------------------------------------------+

        ### Возвращаемое значение

        - указатель на интерфейс новой вставки IPart7.
        """
        ...




class IPart7(IModelObject):  # ipart7.html
    """
    ## Интерфейс IPart7

    Интерфейс компонента 3D документа.

    Посредством вызова метода IUnknown::QueryInterface (const GUID far& iid, void** pif) у данного интерфейса можно получить дополнительные интерфейсы компонента:

    •интерфейс объекта Дерева построения IFeature7,

    •интерфейс параметров компонента в источнике ISourcePart7Params.

    •интерфейс контейнера объектов гибки ISheetMetalContainer,

    •интерфейс контейнера трехмерных объектов IModelContainer,

    •интерфейс массово-центровочных характеристик IMassInertiaParam7.

    •интерфейс контейнера условных обозначений в 3D ISymbols3DContainer,

    •интерфейс контейнера объектов вспомогательной геометрии IAuxiliaryGeomContainer,

    •интерфейс контейнера поверхностей ISurfaceContainer,

    •интерфейс результатов редактирования 3D объекта IModelObjectNotifyResult,

    •интерфейс получения/редактирования значения свойств IPropertyKeeper,

    •интерфейс контейнера объектов измерений и диагностики IMeasurementContainer.

    1. Компонент может быть получен от интерфейса коллекции компонентов IParts7.

    2. Компонент может быть получен от интерфейса IModelObject или IFeature7,используя метод IUnknown QueryInterface.

    3. Верхний компонент может быть получен от интерфейса 3D-документа IKompasDocument3D с помощью свойства IKompasDocument3D::TopPart.

    4. От интерфейса 3D-документа IKompasDocument3D может быть получен верхний компонент IKompasDocument3D::TopPart, а от полученного верхнего компонента IPart7 может быть получена коллекция компонентов IParts7 с помощью свойства IPart7::Parts. Элементы коллекции-компоненты могут быть получены у коллекции с помощью свойства IParts7::Part. Либо с помощью свойства IModelObjects::Item как объекты типа IModelObject с последующим приведением к типу IPart7 через QueryInterface. От каждого элемента коллекции-компонента может быть получена вложенная коллекция компонентов c помощью свойства IPart7::Parts. И так далее, пока имеются вложенные коллекции компонентов.
    """

    @property
    def CreateSpcObjects(self) -> bool:  # ipart7_createspcobjects.html
        """
        ## CreateSpcObjects - Создавать объекты спецификации

        Тип данных: BOOL

        Объекты удаляются при перестроении спецификации, если у компонента выключен данный флаг.

        ### Синтаксис Automation

        ```
        CreateSpcObjects = Object.CreateSpcObjects       # Получить свойство (* )
        Object.CreateSpcObjects = CreateSpcObjects       # Установить свойство (* )
        CreateSpcObjects = Object.GetCreateSpcObjects()  # Получить свойство (**)
        Object.SetCreateSpcObjects( CreateSpcObjects )   # Установить свойство (**)
        ```
        """
        ...
    @CreateSpcObjects.setter
    def CreateSpcObjects(self, value: bool): ...

    @property
    def Density(self) -> float:  # ipart7_density.html
        """
        ## Density - Плотность компонента

        Тип данных: double

        Значения свойства:

        Плотность компонента (г/куб.мм).

        1. Свойство доступно только для чтения.

        2. Для установки плотности необходимо использовать метод IPart7::SetMaterial.

        ### Синтаксис Automation

        ```
        Density = iObject.Density       # Получить свойство (* )
        Density = iObject.GetDensity()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def Detail(self) -> bool:  # ipart7_detail.html
        """
        ## Detail - Является ли компонент деталью

        Тип данных: BOOL

        Значения свойства:

        +---------------------------------------+
        | TRUE  | - компонент является деталью, |
        +---------------------------------------+
        | FALSE | - компонент является сборкой. |
        +---------------------------------------+

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        Detail = iObject.Detail       # Получить свойство (* )
        Detail = iObject.GetDetail()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def DummyEmbodimentIndex(self) -> int:  # ipart7_dummyembodimentindex.html
        """
        ## DummyEmbodimentIndex - Индекс текущего исполнения для макета

        Тип данных: long

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        DummyEmbodimentIndex = Object.DummyEmbodimentIndex       # Получить свойство (* )
        DummyEmbodimentIndex = Object.GetDummyEmbodimentIndex()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def DummyFileName(self) -> str:  # ipart7_dummyfilename.html
        """
        ## DummyFileName - Имя файла для макета

        Тип данных: BSTR

        ### Синтаксис Automation

        ```
        DummyFileName = Object.DummyFileName       # Получить свойство(* )
        Object.DummyFileName = DummyFileName       # Установить свойство (* )
        DummyFileName = Object.GetDummyFileName()  # Получить свойство (**)
        Object.SetDummyFileName( DummyFileName )   # Установить свойство (**)
        ```
        """
        ...
    @DummyFileName.setter
    def DummyFileName(self, value: str): ...

    @property
    def FileName(self) -> str:  # ipart7_filename.html
        """
        ## FileName - Имя файла компонента

        Тип данных: строка

        1. Имя файла можно установить только у компонента, вставленного в сборку.

        2. Свойство вступает в силу после вызова метода IModelObject::Update.

        ### Синтаксис Automation

        ```
        FileName = iObject.FileName       # Получить свойство (* )
        iObject.FileName = FileName       # Установить свойство (* )
        FileName = iObject.GetFileName()  # Получить свойство (**)
        iObject.SetFileName ( FileName)   # Установить свойство (**)
        ```
        """
        ...
    @FileName.setter
    def FileName(self, value: str): ...

    @property
    def Fixed(self) -> bool:  # ipart7_fixed.html
        """
        ## Fixed - Состояние фиксации компонента

        Тип данных: BOOL

        Значения свойства:

        +--------------------------------------+
        | TRUE  | - компонент зафиксирован,    |
        +--------------------------------------+
        | FALSE | - компонент не зафиксирован. |
        +--------------------------------------+

        Свойство вступает в силу после вызова метода IModelObject::Update.

        ### Синтаксис Automation

        ```
        Fixed = iObject.Fixed       # Получить свойство (* )
        iObject.Fixed = Fixed       # Установить свойство (* )
        Fixed = iObject.GetFixed()  # Получить свойство (**)
        iObject.SetFixed( Fixed )   # Установить свойство (**)
        ```
        """
        ...
    @Fixed.setter
    def Fixed(self, value: bool): ...

    @property
    def HatchParam(self) -> IHatchParam:  # ipart7_hatchparam.html
        """
        ## HatchParam - Параметры штриховки

        Тип данных: Указатель на интерфейс IHatchParam

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        HatchParam = Object.HatchParam       # Получить свойство (* )
        HatchParam = Object.GetHatchParam()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def InheritExclude(self) -> bool:  # ipart7_inheritexclude.html
        """
        ## InheritExclude - Признак исключения из расчета задан в файле-источнике. TRUE-Задан в источнике, FALSE-Переопределен у вставки

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        InheritExclude = Object.InheritExclude       # Получить свойство (* )
        Object.InheritExclude = InheritExclude       # Установить свойство (* )
        InheritExclude = Object.GetInheritExclude()  # Получить свойство (**)
        Object.SetInheritExclude( InheritExclude )   # Установить свойство (**)
        ```
        """
        ...
    @InheritExclude.setter
    def InheritExclude(self, value: bool): ...

    @property
    def InheritVisible(self) -> bool:  # ipart7_inheritvisible.html
        """
        ## InheritVisible - Признак видимости задан в файле-источнике. TRUE-Задан в источнике, FALSE-Переопределен у вставки

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        InheritVisible = Object.InheritVisible       # Получить свойство (* )
        Object.InheritVisible = InheritVisible       # Установить свойство (* )
        InheritVisible = Object.GetInheritVisible()  # Получить свойство (**)
        Object.SetInheritVisible( InheritVisible )   # Установить свойство (**)
        ```
        """
        ...
    @InheritVisible.setter
    def InheritVisible(self, value: bool): ...

    @property
    def IsBillet(self) -> bool:  # ipart7_isbillet.html
        """
        ## IsBillet - Признак вставки заготовки детали

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        IsBillet = iObject.IsBillet       # Получить свойство (* )
        iObject.IsBillet = IsBillet       # Установить свойство (* )
        IsBillet = iObject.GetIsBillet()  # Получить свойство (**)
        iObject.SetIsBillet( IsBillet )   # Установить свойство (**)
        ```
        """
        ...
    @IsBillet.setter
    def IsBillet(self, value: bool): ...

    @property
    def IsLayoutGeometry(self) -> bool:  # ipart7_islayoutgeometry.html
        """
        ## IsLayoutGeometry - Признак компоновочной геометрии

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        Fixed = iObject.Fixed       # Получить свойство (* )
        iObject.Fixed = Fixed       # Установить свойство (* )
        Fixed = iObject.GetFixed()  # Получить свойство (**)
        iObject.SetFixed( Fixed )   # Установить свойство (**)
        ```
        """
        ...
    @IsLayoutGeometry.setter
    def IsLayoutGeometry(self, value: bool): ...

    @property
    def IsLocal(self) -> bool:  # ipart7_islocal.html
        """
        ## IsLocal - Локальная деталь

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        IsLocal = Object.IsLocal       # Получить свойство (* )
        Object.IsLocal = IsLocal       # Установить свойство (* )
        IsLocal = Object.GetIsLocal()  # Получить свойство (**)
        Object.SetIsLocal( IsLocal )   # Установить свойство (**)
        ```
        """
        ...
    @IsLocal.setter
    def IsLocal(self, value: bool): ...

    @property
    def LeftHandedCS(self) -> bool:  # ipart7_lefthandedcs.html
        """
        ## LeftHandedCS - Признак левосторонней системы координат

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        LeftHandedCS = Object.LeftHandedCS       # Получить свойство (* )
        Object.LeftHandedCS = LeftHandedCS       # Установить свойство (* )
        LeftHandedCS = Object.GetLeftHandedCS()  # Получить свойство (**)
        Object.SetLeftHandedCS( LeftHandedCS )   # Установить свойство (**)
        ```
        """
        ...
    @LeftHandedCS.setter
    def LeftHandedCS(self, value: bool): ...

    @property
    def LoadState(self) -> int:  # ipart7_loadstate.html
        """
        ## LoadState - Тип загрузки компонента

        Тип данных: из перечисления ksLoadStateEnum

        Свойство позволяет получить тип загрузки компонента ksLoadStateEnum.

        ### Синтаксис Automation

        ```
        LoadState = iObject.LoadState       # Получить свойство (* )
        LoadState = iObject.GetLoadState()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def Marking(self) -> str:  # ipart7_marking.html
        """
        ## Marking - Обозначение компонента

        Тип данных: строка

        Свойство вступает в силу после вызова метода IModelObject::Update.

        ### Синтаксис Automation

        ```
        Marking = iObject.Marking       # Получить свойство (* )
        iObject.Marking = Marking       # Установить свойство (* )
        Marking = iObject.GetMarking()  # Получить свойство (**)
        iObject.SetMarking ( Marking)   # Установить свойство (**)
        ```
        """
        ...
    @Marking.setter
    def Marking(self, value: str): ...

    @property
    def Mass(self) -> float:  # ipart7_mass.html
        """
        ## Mass - Масса компонента

        Тип данных: double

        Значения свойства:

        Масса компонента.

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        Mass = iObject.Mass       # Получить свойство (* )
        Mass = iObject.GetMass()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def MateConstraints(self) -> IMateConstraints3D:  # ipart7_mateconstraints.html
        """
        ## MateConstraints - Коллекция сопряжений

        Тип данных: Указатель на интерфейс IMateConstraints3D

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        MateConstraints = Object.MateConstraints       # Получить свойство (* )
        MateConstraints = Object.GetMateConstraints()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def Material(self) -> str:  # ipart7_material.html
        """
        ## Material - Материал компонента

        Тип данных: строка

        Значения свойства:

        Материал компонента.

        1. Свойство доступно только для чтения.

        2. Для установки плотности необходимо использовать метод IPart7::SetMaterial.

        ### Синтаксис Automation

        ```
        Material = iObject.Material       # Получить свойство (* )
        Material = iObject.GetMaterial()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def Parts(self) -> IParts7:  # ipart7_parts.html
        """
        ## Parts - Коллекция компонентов

        Тип данных: указатель на интерфейс IParts7

        Значения свойства:

        - Указатель на интерфейс коллекции компонентов.

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        Parts = iObject.Parts       # Получить свойство (* )
        Parts = iObject.GetParts()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def PartsGroupNumber(self) -> int:  # ipart7_partsgroupnumber.html
        """
        ## PartsGroupNumber - Идентификатор номера группы компонентов

        Тип данных: long

        Вставки, объединенные в группу, в спецификации будут иметь одинаковый номер позиции.

        В группу могут объединяться вставки из разных исполнений документа.

        Версия: Компас v20

        ### Синтаксис Automation

        ```
        PartsGroupNumber = Object.PartsGroupNumber       # Получить свойство (* )
        Object.PartsGroupNumber = PartsGroupNumber       # Установить свойство (* )
        PartsGroupNumber = Object.GetPartsGroupNumber()  # Получить свойство (**)
        Object.SetPartsGroupNumber( PartsGroupNumber )   # Установить свойство (**)
        ```
        """
        ...
    @PartsGroupNumber.setter
    def PartsGroupNumber(self, value: int): ...

    @property
    def Placement(self) -> IPlacement3D:  # ipart7_placement.html
        """
        ## Placement - Получить локальную систему координат компонента

        Тип данных: Указатель на интерфейс IPlacement3D

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        Placement = iObject.Placement       # Получить свойство (* )
        Placement = iObject.GetPlacement()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def ReadOnly(self) -> int:  # ipart7_readonly.html
        """
        ## ReadOnly - Тип доступа к компоненту

        Тип данных: из перечисления ksPartAccessTypeEnum

        Тип ksATDisable установить нельзя. Он возвращается в том случае, если смена доступа защищена паролем.

        ### Синтаксис Automation

        ```
        ReadOnly = Object.ReadOnly       # Получить свойство (* )
        Object.ReadOnly = ReadOnly       # Установить свойство (* )
        ReadOnly = Object.GetReadOnly()  # Получить свойство (**)
        Object.SetReadOnly( ReadOnly )   # Установить свойство (**)
        ```
        """
        ...
    @ReadOnly.setter
    def ReadOnly(self, value: int): ...

    @property
    def RevealComposition(self) -> bool:  # ipart7_revealcomposition.html
        """
        ## RevealComposition - Сборочная единица/Составные части

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        RevealComposition = Object.RevealComposition       # Получить свойство (* )
        Object.RevealComposition = RevealComposition       # Установить свойство (* )
        RevealComposition = Object.GetRevealComposition()  # Получить свойство (**)
        Object.SetRevealComposition( RevealComposition )   # Установить свойство (**)
        ```
        """
        ...
    @RevealComposition.setter
    def RevealComposition(self, value: bool): ...

    @property
    def SpecRough(self) -> ISpecRough3D:  # ipart7_specrough.html
        """
        ## SpecRough - Неуказанная шероховатость 3D

        Тип данных: Указатель на интерфейс ISpecRough3D

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        SpecRough = Object.SpecRough       # Получить свойство (* )
        SpecRough = Object.GetSpecRough()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def StaffVisible(self) -> bool:  # ipart7_staffvisible.html
        """
        ## StaffVisible - Управление видимостью состава

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        StaffVisible = Object.StaffVisible       # Получить свойство (* )
        Object.StaffVisible = StaffVisible       # Установить свойство (* )
        StaffVisible = Object.GetStaffVisible()  # Получить свойство (**)
        Object.SetStaffVisible( StaffVisible )   # Установить свойство (**)
        ```
        """
        ...
    @StaffVisible.setter
    def StaffVisible(self, value: bool): ...

    @property
    def Standard(self) -> bool:  # ipart7_standard.html
        """
        ## Standard - Признак стандартного компонента

        Тип данных: BOOL

        Свойство вступает в силу после вызова метода IModelObject::Update.

        ### Синтаксис Automation

        ```
        Standard = iObject.Standard       # Получить свойство (* )
        iObject.Standard = Standard       # Установить свойство (* )
        Standard = iObject.GetStandard()  # Получить свойство (**)
        iObject.SetStandard ( Standard)   # Установить свойство (**)
        ```
        """
        ...
    @Standard.setter
    def Standard(self, value: bool): ...

    @property
    def ToleranceRecalcType(self) -> int:  # ipart7_tolerancerecalctype.html
        """
        ## ToleranceRecalcType - Способ пересчета

        Тип данных: Значение из перечисления ksToleranceRecalcsEnum.

        ### Синтаксис Automation

        ```
        ToleranceRecalcType = Object.ToleranceRecalcType       # Получить свойство (* )
        Object.ToleranceRecalcType = ToleranceRecalcType       # Установить свойство (* )
        ToleranceRecalcType = Object.GetToleranceRecalcType()  # Получить свойство (**)
        Object.SetToleranceRecalcType( ToleranceRecalcType )   # Установить свойство (**)
        ```
        """
        ...
    @ToleranceRecalcType.setter
    def ToleranceRecalcType(self, value: int): ...

    @property
    def UseDummy(self) -> bool:  # ipart7_usedummy.html
        """
        ## UseDummy - Использовать макет

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        UseDummy = Object. UseDummy       # Получить свойство (* )
        Object. UseDummy = UseDummy       # Установить свойство (* )
        UseDummy = Object.Get UseDummy()  # Получить свойство (**)
        Object.Set UseDummy( UseDummy )   # Установить свойство (**)
        ```
        """
        ...
    @UseDummy.setter
    def UseDummy(self, value: bool): ...

    @property
    def UserFolders(self) -> IUserFolders:  # ipart7_userfolders.html
        """
        ## UserFolders - Коллекция пользовательских директорий

        Тип данных: Указатель на интерфейс IUserFolders.

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        UserFolders = Object.UserFolders       # Получить свойство (* )
        UserFolders = Object.GetUserFolders()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def UserToleranceRecalcId(self) -> int:  # ipart7_usertolerancerecalcid.html
        """
        ## UserToleranceRecalcId - Идентификатор пользовательского пересчета

        Тип данных: long

        ### Синтаксис Automation

        ```
        UserToleranceRecalcId = Object. UserToleranceRecalcId       # Получить свойство (* )
        Object. UserToleranceRecalcId = UserToleranceRecalcId       # Установить свойство (* )
        UserToleranceRecalcId = Object.Get UserToleranceRecalcId()  # Получить свойство (**)
        Object.Set UserToleranceRecalcId( UserToleranceRecalcId )   # Установить свойство (**)
        ```
        """
        ...
    @UserToleranceRecalcId.setter
    def UserToleranceRecalcId(self, value: int): ...

    @property
    def UserToleranceRecalcName(self) -> str:  # ipart7_usertolerancerecalcname.html
        """
        ## UserToleranceRecalcName - Имя для пользовательского способа пересчета

        Тип данных: BSTR

        ### Синтаксис Automation

        ```
        UserToleranceRecalcName = Object. UserToleranceRecalcName       # Получить свойство (* )
        Object. UserToleranceRecalcName = UserToleranceRecalcName       # Установить свойство (* )
        UserToleranceRecalcName = Object.Get UserToleranceRecalcName()  # Получить свойство (**)
        Object.Set UserToleranceRecalcName( UserToleranceRecalcName )   # Установить свойство (**)
        ```
        """
        ...
    @UserToleranceRecalcName.setter
    def UserToleranceRecalcName(self, value: str): ...

    @property
    def VariableTable(self) -> IVariableTable:  # ipart7_variabletable.html
        """
        ## VariableTable - Интерфейс таблицы переменных

        Тип данных: указатель на интерфейс IVariableTable

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        IVariableTable * VariableTable = Part7.VariableTable       # Получить свойство (* )
        IVariableTable * VariableTable = Part7.GetVariableTable()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def ZonesManager(self) -> IZonesManager:  # ipart7_zonesmanager.html
        """
        ## ZonesManager - Менеджер зон

        Тип данных: Указатель на интерфейс IZonesManager.

        Свойство доступно только для чтения.

        Версия Компас v18.1

        ### Синтаксис Automation

        ```
        ZonesManager = Object.ZonesManager       # Получить свойство (* )
        ZonesManager = Object.GetZonesManager()  # Получить свойство (**)
        ```
        """
        ...

    def AddVariable(self, Name, Value, Note) -> IVariable7:  # ipart7_addvariable.html
        """
        ## AddVariable - Создать переменную

        ### Синтаксис Automation

        ```
        LPDISPATCH AddVariable( BSTR Name, double Value, BSTR Note );
        ```

        ### Входные параметры

        +--------------------------------+
        | Name  | - имя переменной,      |
        +--------------------------------+
        | Value | - значение переменной, |
        +--------------------------------+
        | Note  | - примечание.          |
        +--------------------------------+

        ### Возвращаемое значение

        - указатель на интерфейс переменной IVariable7.

        Метод позволяет добавить переменную в массив переменных и документ.
        """
        ...

    def BeginEdit(self, Param) -> IKompasDocument3D:  # ipart7_beginedit.html
        """
        ## BeginEdit - Войти в режим редактирования компонента сборки

        ### Синтаксис Automation

        ```
        LPDISPATCH BeginEdit( IOpenDocumentParam * Param );
        ```

        ### Входные параметры

        Param - указатель на интерфейс параметров открытия документа IOpenDocumentParam.

        ### Возвращаемое значение

        Указатель на документ 3D IKompasDocument3D
        """
        ...

    def ChangeObjectLinks(self, SourceObjs, DestObjs, RebuildAll) -> bool:  # ipart7_changeobjectlinks.html
        """
        ## ChangeObjectLinks - Заменить ссылки на объекты по всем операциям

        ### Синтаксис Automation

        ```
        BOOL ChangeObjectLinks( VARIANT SourceObjs,
        VARIANT DestObjs,
        BOOL RebuildAll );
        ```

        ### Входные параметры

        +--------------------------------------------+
        | SourceObjs | - массив исходных примитивов, |
        +--------------------------------------------+
        | DestObjs   | - массив новых примитивов,    |
        +--------------------------------------------+
        | RebuildAll | - перестроить объекты.        |
        +--------------------------------------------+

        Метод позволяет заменить ссылки в объектах 3D модели с одних опорных примитивов на другие.

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def DefaultObject(self, Type) -> IModelObject:  # ipart7_defaultobject.html
        """
        ## DefaultObject - Получить предопределенные элементы (плоскости, ось и СК)

        Тип данных: указатель на интерфейс IModelObject

        ### Синтаксис Automation

        ```
        DefaultObject = Object.DefaultObject( Type )     # Получить свойство (* )
        DefaultObject = Object.GetDefaultObject( Type )  # Получить свойство (**)
        ```

        ### Входные параметры

        +-------------------------------------------------------------------+
        | Type | - тип объекта - константа из перечисления ksObj3dTypeEnum. |
        +-------------------------------------------------------------------+

        Типы объектов (Type):

        +-----------------------------------------------------+
        | o3d_planeXOY | 1  | плоскость XOY,                  |
        +-----------------------------------------------------+
        | o3d_planeXOZ | 2  | плоскость XOZ,                  |
        +-----------------------------------------------------+
        | o3d_planeYOZ | 3  | плоскость YOZ,                  |
        +-----------------------------------------------------+
        | o3d_pointCS  | 4  | точка начала системы координат, |
        +-----------------------------------------------------+
        | o3d_axisOX   | 71 | ось OX,                         |
        +-----------------------------------------------------+
        | o3d_axisOY   | 72 | ось OY,                         |
        +-----------------------------------------------------+
        | o3d_axisOZ   | 73 | ось OZ.                         |
        +-----------------------------------------------------+
        """
        ...

    def DestroySubassembly(self) -> bool:  # ipart7_destroysubassembly.html
        """
        ## DestroySubassembly - Разрушить подсборку

        ### Синтаксис Automation

        ```
        BOOL DestroySubassembly();
        ```

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def EndEdit(self, Rebuild) -> bool:  # ipart7_endedit.html
        """
        ## EndEdit - Завершить процесс редактирования компонента сборки

        ### Синтаксис Automation

        ```
        BOOL EndEdit();
        ```

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def FindBody(self, UniqueMetaObjectKey) -> bool:  # ipart7_findbody.html
        """
        ## FindBody - Найти тело по идентификатору информационного объекта

        ### Синтаксис Automation

        ```
        LPDISPATCH FindBody( BSTR UniqueMetaObjectKey );
        ```

        ### Входные параметры

        +----------------------------------------------------------------+
        | UniqueMetaObjectKey | - идентификатор информационного объекта. |
        +----------------------------------------------------------------+

        Версия Компас v21

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def FindObject(self, Obj, SourcePart) -> bool:  # ipart7_findobject.html
        """
        ## FindObject - Найти объект в текущем документе по объекту из другого документа

        ### Синтаксис Automation

        ```
        LPDISPATCH FindObject( LPDISPATCH Obj, LPDISPATCH SourcePart );
        ```

        ### Входные параметры

        +-------------------------------------+
        | Obj        | - указатель на объект, |
        +-------------------------------------+
        | SourcePart | - владелец объекта.    |
        +-------------------------------------+

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def FindObjectsByPoint(self, X, Y, Z, FirstLevel) -> list[IDispatch]:  # ipart7_findobjectsbypoint.html
        """
        ## FindObjectsByPoint - Найти объекты по точке

        ### Синтаксис Automation

        ```
        VARIANT FindObjectsByPoint( double X, double Y, double Z, BOOL FirstLevel );
        ```

        ### Входные параметры

        +------------------------------------------------+
        | X ,Y, Z    | - координаты точки,               |
        +------------------------------------------------+
        | FirstLevel | - искать только в текущей модели. |
        +------------------------------------------------+

        ### Возвращаемое значение

        - массив объектов, содержащих заданную точку, в виде SAFEARRAY DISPATCH - VT_ARRAY | VT_DISPATCH.
        """
        ...

    def FindObjectsByPointEx(self, X, Y, Z, FirstLevel, Epsilon):  # ipart7_findobjectsbypointex.html
        """
        ## FindObjectsByPointEx - Найти объекты по точке

        ### Синтаксис Automation

        ```
        VARIANT FindObjectsByPointEx( double X, double Y, double Z, BOOL FirstLevel, double Epsilon );
        ```

        ### Входные параметры

        +--------------------------------------------------------------------------------------------------------+
        | X ,Y, Z    | - координаты точки,                                                                       |
        +--------------------------------------------------------------------------------------------------------+
        | FirstLevel | •TRUE - возвращать объекты только первого уровня, •FALSE - возвращать объекты из вставок, |
        +--------------------------------------------------------------------------------------------------------+
        | Epsilon    | - погрешность.                                                                            |
        +--------------------------------------------------------------------------------------------------------+

        Версия КОМПАС v22
        """
        ...

    def FindObjectsByPointWithParam(self, X, Y, Z, FirstLevel, Epsilon, FilterParam):  # ipart7_findobjectsbypointwithparam.html
        """
        ## FindObjectsByPointWithParam - Найти объекты по точке

        ### Синтаксис Automation

        ```
        VARIANT FindObjectsByPointWithParam( double X, double Y, double Z, BOOL FirstLevel, double Epsilon, IFindObject3DParameters * FilterParam );
        ```

        ### Входные параметры

        +---------------------------------------------------------------------------------------------------------+
        | X ,Y, Z     | - координаты точки,                                                                       |
        +---------------------------------------------------------------------------------------------------------+
        | FirstLevel  | •TRUE - возвращать объекты только первого уровня, •FALSE - возвращать объекты из вставок, |
        +---------------------------------------------------------------------------------------------------------+
        | Epsilon     | - погрешность,                                                                            |
        +---------------------------------------------------------------------------------------------------------+
        | FilterParam | - параметры фильтрации IFindObject3DParameters.                                           |
        +---------------------------------------------------------------------------------------------------------+

        Версия КОМПАС v22
        """
        ...

    def FindSimilarObject(self, Obj) -> IModelObject:  # ipart7_findsimilarobject.html
        """
        ## FindSimilarObject - Найти аналог объекта

        ### Синтаксис Automation

        ```
        LPDISPATCH FindSimilarObject( IModelObject * Obj );
        ```

        ### Входные параметры

        +----------------------------------------------+
        | Obj | - указатель на интерфейс IModelObject. |
        +----------------------------------------------+

        Версия Компас v21

        ### Возвращаемое значение

        Указатель на интерфейс IModelObject.
        """
        ...

    def GetBodyById(self, BodyId) -> IBody7:  # ipart7_getbodybyid.html
        """
        ## GetBodyById - Возвращает тело, заданное по идентификатору

        ### Синтаксис Automation

        ```
        LPDISPATCH GetBodyById( long BodyId );
        ```

        ### Входные параметры

        +--------------------------------+
        | BodyId | - идентификатор тела. |
        +--------------------------------+

        ### Возвращаемое значение

        - указатель на интерфейс IBody7.
        """
        ...

    def GetDummyEmbodimentMarking(self, MarkingType, AddSystemDelimer) -> bool:  # ipart7_getdummyembodimentmarking.html
        """
        ## GetDummyEmbodimentMarking - Обозначение исполнения для макета

        ### Синтаксис Automation

        ```
        BSTR GetDummyEmbodimentMarking( ksVariantMarkingTypeEnum MarkingType, BOOL AddSystemDelimer );
        ```

        ### Входные параметры

        +-----------------------------------------------------------------------------------------+
        | MarkingType      | - индекс части обозначения из перечисления ksVariantMarkingTypeEnum, |
        +-----------------------------------------------------------------------------------------+
        | AddSystemDelimer | - добавлять системные разделители.                                   |
        +-----------------------------------------------------------------------------------------+

        ### Возвращаемое значение

        +-----------------------------------------+
        | TRUE | - в случае успешного завершения, |
        +-----------------------------------------+
        """
        ...

    def GetMaxSag(self):  # ipart7_getmaxsag.html
        """
        ## GetMaxSag - Максимально допустимый прогиб кривой или поверхности в соседних точках на расстоянии шага

        ### Синтаксис Automation

        ```
        double GetMaxSag();
        ```

        ### Возвращаемое значение

        - значение максимально допустимого прогиба кривой.
        """
        ...

    def GetOpenDocumentParam(self) -> IOpenDocumentParam:  # ipart7_getopendocumentparam.html
        """
        ## GetOpenDocumentParam - Получить интерфейс параметров открытия документа для редактирования компонента сборки

        ### Синтаксис Automation

        ```
        LPDISPATCH GetOpenDocumentParam();
        ```

        ### Возвращаемое значение

        Указатель на интерфейс параметров открытия документа IOpenDocumentParam.
        """
        ...

    def GetSimilarInstances(self, Part) -> bool:  # ipart7_getsimilarinstances.html
        """
        ## GetSimilarInstances - Найти одинаковые вставки

        ### Синтаксис Automation

        ```
        VARIANT GetSimilarInstances( IPart7 * Part );
        ```

        ### Входные параметры

        +-------------------------------------------------+
        | Part | - указатель на интерфейс вставки IPart7. |
        +-------------------------------------------------+

        Версия Компас v21

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def GetSummMatrix(self, Part) -> list[float]:  # ipart7_getsummmatrix.html
        """
        ## GetSummMatrix - Получить суммарную матрицу преобразование координат

        1. Элементы матрицы возвращаются в виде одномерного массива из шестнадцати элементов.

        2. Матрица имеет размер 4х4.

        ### Синтаксис Automation

        ```
        VARIANT GetSummMatrix( IPart7 * Part1 );
        ```

        ### Входные параметры

        +---------------------------------------------------------------------------------------------+
        | Part1 | - указатель на интерфейс IPart7 детали из которой нужно сделать пересчет координат. |
        +---------------------------------------------------------------------------------------------+

        ### Возвращаемое значение

        - массив SafeArray типа VT_ARRAY | VT_R8.
        """
        ...

    def InstanceCount(self, Part) -> int:  # ipart7_instancecount.html
        """
        ## InstanceCount - Количество вставок компонента

        Пример в Справке: `partsex_instancecount_example.html`

        Тип данных: long

        1. Свойство доступно только для чтения.

        2. Метод работает для верхнего компонента сборки и для вставок подсборок. Если iPart7 == NULL, то возвращается общее количество компонентов, вставленных в данную подсборку.

        ### Синтаксис Automation

        ```
        Count = iObject.InstanceCount( iPart7 )     # Получить свойство (* )
        Count = iObject.GetInstanceCount( iPart7 )  # Получить свойство (**)
        ```

        ### Входные параметры

        +---------------------------------------------+
        | iPart7 | - указатель на компонент или NULL. |
        +---------------------------------------------+
        """
        ...

    def IsLocalResultExist(self, Reqursive) -> bool:  # ipart7_islocalresultexist.html
        """
        ## IsLocalResultExist - Есть ли локальный результат в модели или во вставках

        ### Синтаксис Automation

        ```
        BOOL IsLocalResultExist( BOOL Reqursive );
        ```

        ### Входные параметры

        +-----------------------------------------------------------------------------+
        | Reqursive | - Проверять наличие локального результата у вставок рекурсивно. |
        +-----------------------------------------------------------------------------+

        Версия: Компас v20

        ### Возвращаемое значение

        +-------------------------------------+
        | TRUE  | - есть локальный результат, |
        +-------------------------------------+
        | FALSE | - в случае неудачи.         |
        +-------------------------------------+
        """
        ...

    def IsVariableNameValid(self, Name) -> bool:  # ipart7_isvariablenamevalid.html
        """
        ## IsVariableNameValid - Проверить допустимость создания новой переменной с данным именем

        ### Синтаксис Automation

        ```
        BOOL IsVariableNameValid( BSTR Name );
        ```

        ### Входные параметры

        +--------------------------+
        | Name | - имя переменной. |
        +--------------------------+

        ### Возвращаемое значение

        +----------------------------+
        | TRUE  | - имя допустимо,   |
        +----------------------------+
        | FALSE | - имя недопустимо. |
        +----------------------------+
        """
        ...

    def Load(self, Full) -> bool:  # ipart7_load.html
        """
        ## Load - Загрузить компонент

        ### Синтаксис Automation

        ```
        BOOL Load( BOOL full );
        ```

        ### Входные параметры

        +------------------+
        | full | - резерв. |
        +------------------+

        ### Возвращаемое значение

        +---------------------------------------+
        | TRUE  | - компонент успешно загружен, |
        +---------------------------------------+
        | FALSE | - компонент не загружен.      |
        +---------------------------------------+
        """
        ...

    def MirroringPlacement(self, Axis) -> bool:  # ipart7_mirroringplacement.html
        """
        ## MirroringPlacement - Изменить систему координат детали на зеркальную

        ### Синтаксис Automation

        ```
        BOOL MirroringPlacement( ksObj3dTypeEnum Axis );
        ```

        ### Входные параметры

        +--------------------------------------------------------+
        | Axis | - ось, относительно которой делается симметрия. |
        +--------------------------------------------------------+

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def OpenSourceDocument(self, Param) -> IKompasDocument3D:  # ipart7_opensourcedocument.html
        """
        ## OpenSourceDocument - Открытие документа-источника на редактирование

        ### Синтаксис Automation

        ```
        LPDISPATCH OpenSourceDocument( IOpenDocumentParam * Param );
        ```

        ### Входные параметры

        +------------------------------------------------------------------------------------+
        | Param | - Указатель на интерфейс параметров открытия документа IOpenDocumentParam. |
        +------------------------------------------------------------------------------------+

        Версия Компас v18.1

        ### Возвращаемое значение

        Указатель на интерфейс документа IKompasDocument3D.
        """
        ...

    def PartsEx(self, PartCollectionType) -> typing.Any:  # ipart7_partsex.html
        """
        ## PartsEx - Массив SAFEARRAY компонентов

        Пример в Справке: `partsex_instancecount_example.html`

        Тип данных: VARIANT

        1. Свойство доступно только для чтения.

        2. Коллекция объектов возвращается в виде массива SAFEARRAY объектов LPDISPATCH ( VT_ARRAY | VT_DISPATCH). Если возвращается один объект, то тип VARIANT-а будет VT_DISPATCH, если возвращается несколько объектов, то тип VARIANT-а будет VT_ARRAY | VT_DISPATCH.

        ### Синтаксис Automation

        ```
        Objects = iObject.PartsEx( ObjType )     # Получить свойство (* )
        Objects = iObject.GetPartsEx( ObjType )  # Получить свойство (**)
        ```

        ### Входные параметры

        +---------------------------------------------------------------------------------------------------------------------------------------+
        | ObjType ( VARIANT) | - Тип коллекции. В настоящее время может принимать значение константы из перечисления ksPart7CollectionTypeEnum. |
        +---------------------------------------------------------------------------------------------------------------------------------------+
        """
        ...

    def RebuildModel(self, Redraw) -> bool:  # ipart7_rebuildmodel.html
        """
        ## RebuildModel - Перестроить модель

        ### Синтаксис Automation

        ```
        BOOL RebuildModel( BOOL Redraw );
        ```

        ### Входные параметры

        +-------------------------------------------------------------------------------------------------------+
        | Redraw | - признак перестроения документа: - TRUE - перестроить документ, - FALSE - не перестраивать. |
        +-------------------------------------------------------------------------------------------------------+

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def SaveAs(self, PathName) -> bool:  # ipart7_saveas.html
        """
        ## SaveAs - Сохранить файл компонента под другим именем

        ### Синтаксис Automation

        ```
        BOOL SaveAs( BSTR PathName );
        ```

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def SelectByPoint(self, Objects, X, Y, Z) -> list[IDispatch]:  # ipart7_selectbypoint.html
        """
        ## SelectByPoint - Выделить объекты, содержащие точку

        Если возвращается один объект, то тип VARIANT-а будет VT_DISPATCH, если возвращается несколько объектов, то тип VARIANT-а будет VT_ARRAY | VT_DISPATCH.

        ### Синтаксис Automation

        ```
        VARIANT SelectByPoint( VARIANT Objects,
        double X,
        double Y,
        double Z );
        ```

        ### Входные параметры

        +--------------------------------------------------------------------+
        | Objects | - массив SafeArray объектов типа VT_ARRAY | VT_DISPATCH, |
        +--------------------------------------------------------------------+
        | x, y, z | - координаты точки.                                      |
        +--------------------------------------------------------------------+

        ### Возвращаемое значение

        Массив SafeArray объектов типа VT_ARRAY | VT_DISPATCH.
        """
        ...

    def SelectByPointEx(self, Objects, X, Y, Z, Epsilon):  # ipart7_selectbypointex.html
        """
        ## SelectByPointEx - Выделить объекты, содержащие точку

        ### Синтаксис Automation

        ```
        VARIANT SelectByPointEx( VARIANT Objects, double X, double Y, double Z, double Epsilon );
        ```

        ### Входные параметры

        +-----------------------------------------------------+
        | Objects | - cписок объектов VT_ARRAY | VT_DISPATCH, |
        +-----------------------------------------------------+
        | X ,Y, Z | - координаты точки,                       |
        +-----------------------------------------------------+
        | Epsilon | - погрешность.                            |
        +-----------------------------------------------------+

        Версия КОМПАС v22
        """
        ...

    def SetDummyEmbodiment(self, Index) -> bool:  # ipart7_setdummyembodiment.html
        """
        ## SetDummyEmbodiment - Установить текущее исполнение для макета по имени или индексу

        ### Синтаксис Automation

        ```
        BOOL SetDummyEmbodiment( VARIANT Index );
        ```

        ### Входные параметры

        +---------------------------+
        | Index | - имя или индекс. |
        +---------------------------+

        ### Возвращаемое значение

        +-----------------------------------------+
        | TRUE | - в случае успешного завершения. |
        +-----------------------------------------+
        """
        ...

    def SetMaterial(self, Name, Density) -> bool:  # ipart7_setmaterial.html
        """
        ## SetMaterial - Установить материал и плотность компонента

        1. Компонент, у которого меняется материал, должен быть деталью.

        2. Компонент не должен быть деталью из библиотеки моделей или стандартным элементом.

        3. Метод используется в детали для верхнего компонента, в сборках - для вставленных деталей, открытых на редактирование.

        4. Изменение материала и плотности вступает в силу после вызова метода IModelObject::Update.

        ### Синтаксис Automation

        ```
        BOOL SetMaterial( LPCTSTR Name, double density);
        ```

        ### Входные параметры

        +---------------------------------------------+
        | Name    | - наименование материала,         |
        +---------------------------------------------+
        | Density | - плотность материала (г/куб.мм). |
        +---------------------------------------------+

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def SetUniqueNum(self, OType, arg1):
        ...

    def TransferObjects(self, Objects, Lcs, HoldPosition) -> bool:  # ipart7_transferobjects.html
        """
        ## TransferObjects - Перенести в СК

        1. Метод позволяет перенести следующие объекты модели IModelObject:

        •3D точка,

        •ломаная,

        •сплайн,

        •эскиз,

        •импортированная поверхность,

        •операция без истории,

        •деталь заготовка,

        •локальная система координат.

        2. Если объекты нужно перенести в систему координат детали, нужно в качестве параметра Lcs передать NULL.

        3. Если нужно перенести один объект, его можно передать в VARIANT-е как указатель на DISPATCH, тип VARIANT-а VT_DISPATCH.

        4. Если нужно перенести несколько объектов, нужно передать в VARIANT-е как указатель на массив SafeArray тип VARIANT-а VT_ARRAY | VT_DISPATCH.

        5. Если параметр holdPosition == TRUE, то положение объекта в документе не изменяется.

        6. Если параметр holdPosition == FALSE, то положение объекта будет задано относительно новой системы координат.

        7. Значение флага holdPosition может не учитываться при наличии ассоциативных связей объектов или особенностей создания объекта.

        ### Синтаксис Automation

        ```
        BOOL TransferObjects( VARIANT objects,
        LPDISPATCH Lcs,
        BOOL holdPosition );
        ```

        ### Входные параметры

        +---------------------------------------------------------------------------------------------------------------+
        | objects      | - объект или список SafeArray объектов которые требуется перенести в другую систему координат, |
        +---------------------------------------------------------------------------------------------------------------+
        | Lcs          | - указатель на интерфейс локальной системы координат ILocalCoordinateSystem,                   |
        +---------------------------------------------------------------------------------------------------------------+
        | holdPosition | - сохранять положение.                                                                         |
        +---------------------------------------------------------------------------------------------------------------+

        ### Возвращаемое значение

        +---------------------------+
        | TRUE | - в случае успеха. |
        +---------------------------+
        """
        ...

    def TransformPoint(self, X, Y, Z, Part) -> bool:  # ipart7_transformpoint.html
        """
        ## TransformPoint - Перевод координат точки присланной детали Part1 в систему координат детали

        ### Синтаксис Automation

        ```
        BOOL TransformPoint( double * X, double * Y, double * Z, IPart7 * Part1 );
        ```

        ### Входные параметры

        +-------------------------------------------------------------------------------------------------------+
        | X, Y, Z | - координаты точки в системе координат детали Part1,                                        |
        +-------------------------------------------------------------------------------------------------------+
        | Part1   | - указатель на интерфейс детали IPart7, из системы координат которой нужно перевести точку. |
        +-------------------------------------------------------------------------------------------------------+

        ### Выходные параметры

        +----------------------------------------------------------+
        | X, Y, Z | - координаты точки в системе координат детали. |
        +----------------------------------------------------------+

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def TransformPoints(self, Points, Part) -> bool:  # ipart7_transformpoints.html
        """
        ## TransformPoints - Перевод координат точек присланной детали Part1 в систему координат детали

        1. Значения координат изменяются непосредственно в присланном массиве SAFEARRAY.

        2. Тип варианта должен быть VT_ARRAY | VT_R8.

        3. Значения в массиве должны лежать в последовательности x1, y1, z1, x2, y2, z2 ... xn, yn, zn.

        ### Синтаксис Automation

        ```
        BOOL TransformPoints( VARIANT * Points, IPart7 * Part1 );
        ```

        ### Входные параметры

        +------------------------------------------------------------------------------------+
        | Points | -массив SAFEARRAY координат точек в системе координат детали Part1,       |
        +------------------------------------------------------------------------------------+
        | Part1  | - указатель на интерфейс детали в системе координат которой заданы точки. |
        +------------------------------------------------------------------------------------+

        ### Выходные параметры

        +-------------------------------------------------------------------------+
        | Points | - массив SAFEARRAY координат точек в системе координат детали. |
        +-------------------------------------------------------------------------+

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def UniqueNum(self, OType) -> int:  # ipart7_uniquenum.html
        """
        ## UniqueNum - Уникальный номер объекта типа

        Тип данных: long

        ### Синтаксис Automation

        ```
        UniqueNum = Object.UniqueNum( OType )     # Получить свойство (* )
        Object.UniqueNum( OType ) = UniqueNum     # Установить свойство (* )
        UniqueNum = Object.GetUniqueNum( OType )  # Получить свойство (**)
        Object.SetUniqueNum( OType, UniqueNum )   # Установить свойство (**)
        ```

        ### Входные параметры

        +------------------------------------------+
        | ksObj3dTypeEnum | - OType - тип объекта. |
        +------------------------------------------+
        """
        ...

    def Unload(self, Full) -> bool:  # ipart7_unload.html
        """
        ## Unload - Выгрузить компонент

        Метод позволяет осуществить выгрузку компонента. Для верхнего компонента не используется.

        ### Синтаксис Automation

        ```
        BOOL Unload( BOOL full )
        ```

        ### Входные параметры

        +----------------------------------------------------------------+
        | full | TRUE - выгрузить полностью, FALSE - выгрузить частично. |
        +----------------------------------------------------------------+

        ### Возвращаемое значение

        +---------------------------------------+
        | TRUE  | - компонент успешно загружен, |
        +---------------------------------------+
        | FALSE | - компонент не загружен.      |
        +---------------------------------------+
        """
        ...

    def UnloadEx(self, Type) -> bool:  # ipart7_unloadex.html
        """
        ## UnloadEx - Выгрузить

        ### Синтаксис Automation

        ```
        BOOL UnloadEx( ksLoadStateEnum Type );
        ```

        ### Входные параметры

        +------------------------------------------------------------------+
        | Type | - тип загрузки документа из перечисления ksLoadStateEnum. |
        +------------------------------------------------------------------+

        ### Возвращаемое значение

        +-----------------------------------------+
        | TRUE | - в случае успешного завершения. |
        +-----------------------------------------+
        """
        ...

    def UpdatePlacement(self, Redraw) -> bool:  # ipart7_updateplacement.html
        """
        ## UpdatePlacement - Установить локальную систему координат детали

        ### Синтаксис Automation

        ```
        BOOL UpdatePlacement( BOOL Redraw );
        ```

        ### Входные параметры

        +-----------------------------------+
        | Redraw | - требуется перерисовка. |
        +-----------------------------------+

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...




class IEmbodiment(IKompasAPIObject):  # iembodiment.html
    """
    ## Интерфейс IEmbodiment

    Интерфейс исполнений.

    Интерфейс можно получить помощью свойств IEmbodimentsManager::Embodiment, IEmbodimentsManager::TopEmbodiment, IEmbodimentsManager::CurrentEmbodiment, IEmbodiment::Embodiment, IEmbodiment::Owner.

    Имеет дополнительные интерфейсы:

    •IPropertyKeeper,

    •IColorParam7.

    •IMassInertiaParam7,

    которые можно получить с помощью метода IUnknown::QueryInterfaceI.
    """

    @property
    def Density(self) -> float:  # iembodiment_density.html
        """
        ## Density - Плотность

        Тип данных: double

        1. Свойство доступно только для чтения.

        2. Задать плотность можно с помощью метода IEmbodiment::SetMaterial.

        ### Синтаксис Automation

        ```
        Density = Object.Density       # Получить свойство (* )
        Density = Object.GetDensity()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def EmbodimentsCount(self) -> int:  # iembodiment_embodimentscount.html
        """
        ## EmbodimentsCount - Количество исполнений

        Тип данных: long

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        EmbodimentsCount = Object.EmbodimentsCount       # Получить свойство (* )
        EmbodimentsCount = Object.GetEmbodimentsCount()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def IsCurrent(self) -> bool:  # iembodiment_iscurrent.html
        """
        ## IsCurrent - Текущее исполнение

        Тип данных: BOOL

        Свойство позволяет узнать, является ли исполнение текущим.

        ### Синтаксис Automation

        ```
        IsCurrent = Object.IsCurrent       # Получить свойство (* )
        Object.IsCurrent = IsCurrent       # Установить свойство (* )
        IsCurrent = Object.GetIsCurrent()  # Получить свойство (**)
        Object.SetIsCurrent( IsCurrent )   # Установить свойство (**)
        ```
        """
        ...
    @IsCurrent.setter
    def IsCurrent(self, value: bool): ...

    @property
    def IsVariant(self) -> bool:  # iembodiment_isvariant.html
        """
        ## IsVariant - Является ли исполнение вариантом геометрии

        Тип данных: BOOL

        Свойство доступно только для чтения.

        Версия КОМПАС v22

        ### Синтаксис Automation

        ```
        IsVariant = Object.IsVariant       # Получить свойство (* )
        IsVariant = Object.GetIsVariant()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def LeftHandedCS(self) -> bool:  # iembodiment_lefthandedcs.html
        """
        ## LeftHandedCS - Признак левосторонней системы координат

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        LeftHandedCS = Object.LeftHandedCS       # Получить свойство (* )
        Object.LeftHandedCS = LeftHandedCS       # Установить свойство (* )
        LeftHandedCS = Object.GetLeftHandedCS()  # Получить свойство (**)
        Object.SetLeftHandedCS( LeftHandedCS )   # Установить свойство (**)
        ```
        """
        ...
    @LeftHandedCS.setter
    def LeftHandedCS(self, value: bool): ...

    @property
    def Mass(self) -> float:  # iembodiment_mass.html
        """
        ## Mass - Масса компонента

        Тип данных: double

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        Mass = Object.Mass       # Получить свойство (* )
        Mass = Object.GetMass()  # Установить свойство (**)
        ```
        """
        ...

    @property
    def Material(self) -> str:  # iembodiment_material.html
        """
        ## Material - Материал

        Тип данных: BSTR

        1. Свойство доступно только для чтения.

        2. Задать материал можно с помощью метода IEmbodiment::SetMaterial.

        ### Синтаксис Automation

        ```
        Material = Object.Material       # Получить свойство (* )
        Material = Object.GetMaterial()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def Name(self) -> str:  # iembodiment_name.html
        """
        ## Name - Имя

        Тип данных: BSTR

        ### Синтаксис Automation

        ```
        Name = Object.Name       # Получить свойство (* )
        Object.Name = Name       # Установить свойство (* )
        Name = Object.GetName()  # Получить свойство (**)
        Object.SetName(Name )    # Установить свойство (**)
        ```
        """
        ...
    @Name.setter
    def Name(self, value: str): ...

    @property
    def Owner(self) -> IEmbodiment:  # iembodiment_owner.html
        """
        ## Owner - Объект, породивший этот объект

        Тип данных: Указатель на интерфейс IEmbodiment

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        Owner = Object.Owner       # Получить свойство (* )
        Owner = Object.GetOwner()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def Part(self) -> IPart7:  # iembodiment_part.html
        """
        ## Part - Компонент исполнения

        Тип данных: Указатель на интерфейс IPart7

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        Part = Object.Part       # Получить свойство (* )
        Part = Object.GetPart()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def SpecRough(self) -> ISpecRough3D:  # iembodiment_specrough.html
        """
        ## SpecRough - Неуказанная шероховатость 3D

        Тип данных: Указатель на интерфейс ISpecRough3D

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        SpecRough = Object.SpecRough       # Получить свойство (* )
        SpecRough = Object.GetSpecRough()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def VariantName(self) -> str:  # iembodiment_variantname.html
        """
        ## VariantName - Наименование варианта геометрии

        Тип данных:BSTR

        ### Синтаксис Automation

        ```
        VariantName = Object.VariantName       # Получить свойство (* )
        Object.VariantName = VariantName       # Установить свойство (* )
        VariantName = Object.GetVariantName()  # Получить свойство (**)
        Object.SetVariantName( VariantName )   # Установить свойство (**)
        ```
        """
        ...
    @VariantName.setter
    def VariantName(self, value: str): ...

    def Delete(self) -> bool:  # iembodiment_delete.html
        """
        ## Delete - Удаление

        ### Синтаксис Automation

        ```
        BOOL Delete();
        ```

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def Embodiment(self, Index) -> IEmbodiment:  # iembodiment_embodiment.html
        """
        ## Embodiment - Зависимое исполнение

        Тип данных: Указатель на интерфейс IEmbodiment

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        Embodiment = Object.Embodiment( Index )     # Получить свойство (* )
        Embodiment = Object.GetEmbodiment( Index )  # Получить свойство (**)
        ```
        """
        ...

    def GetMarking(self, MarkingType, AddSystemDelimer):  # iembodiment_getmarking.html
        """
        ## GetMarking - Обозначение исполнения

        ### Синтаксис Automation

        ```
        BSTR GetMarking( ksVariantMarkingTypeEnum MarkingType, BOOL AddSystemDelimer );
        ```

        ### Входные параметры

        +------------------------------------------------------------+
        | MarkingType      | - какую часть обозначения надо вернуть, |
        +------------------------------------------------------------+
        | AddSystemDelimer | - с разделителями.                      |
        +------------------------------------------------------------+

        Метод возвращает обозначение исполнения.

        ### Возвращаемое значение

        - обозначение исполнения.
        """
        ...

    def MakeAsEmbodiment(self) -> bool:  # iembodiment_makeasembodiment.html
        """
        ## MakeAsEmbodiment - Преобразовать вариант геометрии в исполнение

        ### Синтаксис Automation

        ```
        BOOL MakeAsEmbodiment();
        ```

        ### Возвращаемое значение

        +----------------------------------------------------+
        | TRUE  | - исполнение является вариантом геометрии, |
        +----------------------------------------------------+
        | FALSE | - обычное исполнение.                      |
        +----------------------------------------------------+

        Версия КОМПАС v22
        """
        ...

    def MakeAsVariant(self, VariantName) -> bool:  # iembodiment_makeasvariant.html
        """
        ## MakeAsVariant - Преобразовать исполнение в вариант геометрии

        ### Синтаксис Automation

        ```
        BOOL MakeAsVariant( BSTR VariantName );
        ```

        ### Входные параметры

        +--------------------------------------------------+
        | VariantName | - наименование варианта геометрии. |
        +--------------------------------------------------+

        Версия КОМПАС v22

        ### Возвращаемое значение

        +----------------------------------------------------+
        | TRUE  | - исполнение является вариантом геометрии, |
        +----------------------------------------------------+
        | FALSE | - обычное исполнение.                      |
        +----------------------------------------------------+
        """
        ...

    def SetMarking(self, MarkingType, Marking) -> bool:  # iembodiment_setmarking.html
        """
        ## SetMarking - Обозначение исполнения

        ### Синтаксис Automation

        ```
        BOOL SetMarking( ksVariantMarkingTypeEnum MarkingType, BSTR Marking );
        ```

        ### Входные параметры

        +----------------------------------------------------------+
        | MarkingType | - какую часть обозначения надо установить, |
        +----------------------------------------------------------+
        | Marking     | - обозначение.                             |
        +----------------------------------------------------------+

        Метод устанавливает обозначение исполнения.

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def SetMaterial(self, Name, Density) -> bool:  # iembodiment_setmaterial.html
        """
        ## SetMaterial - Установить материал

        ### Синтаксис Automation

        ```
        BOOL SetMaterial( BSTR Name, double Density );
        ```

        ### Входные параметры

        +-------------------------------------+
        | Name    | - наименование материала, |
        +-------------------------------------+
        | Density | - плотность материала.    |
        +-------------------------------------+

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def Update(self) -> bool:  # iembodiment_update.html
        """
        ## Update - Изменить свойства объекта

        ### Синтаксис Automation

        ```
        BOOL Update();
        ```

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...




class IFeature7(IDispatch):  # ifeature7.html
    """
    ## Интерфейс IFeature7

    Интерфейс объекта Дерева построения.

    1. Данный интерфейс можно получить от интерфейса IModelObject как свойство IModelObject::Owner.

    2. Данный интерфейс можно получить от интерфейса IModelObject или IPart7 с помощью метода IUnknown::QueryInterface (const GUID far& iid, void** pif).
    """

    @property
    def Excluded(self) -> bool:  # ifeature7_excluded.html
        """
        ## Excluded - Исключен из расчета

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        Excluded = iObject.Excluded       # Получить свойство (* )
        iObject.Excluded = Excluded       # Установить свойство (* )
        Excluded = iObject.GetExcluded()  # Получить свойство (**)
        iObject.SetExcluded (Excluded)    # Установить свойство (**)
        ```
        """
        ...
    @Excluded.setter
    def Excluded(self, value: bool): ...

    @property
    def FeatureType(self) -> int:  # ifeature7_featuretype.html
        """
        ## FeatureType - Тип объекта

        Тип данных: из перечисления ksObj3dTypeEnum

        1. Свойство доступно только для чтения.

        2. Тип возвращаемого объекта может принимать значения:

        +------------------------------------+
        | o3d_part           | - деталь,     |
        +------------------------------------+
        | o3d_entity         | - объект,     |
        +------------------------------------+
        | o3d_mateConstraint | - сопряжение. |
        +------------------------------------+

        ### Синтаксис Automation

        ```
        FeatureType = iObject.FeatureType       # Получить свойство (* )
        FeatureType = iObject.GetFeatureType()  # Получить свойство (**)
        ```

        ### Возвращаемое значение

        - тип объектов из перечисления ksObj3dTypeEnum.
        """
        ...

    @property
    def Label(self) -> str:  # ifeature7_label.html
        """
        ## Label - Отображаемое имя объекта в дереве

        Тип данных: BSTR

        Свойство доступно только для чтения.

        Версия: Компас v20

        ### Синтаксис Automation

        ```
        Label = Object.Label       # Получить свойство (* )
        Label = Object.GetLabel()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def Name(self) -> str:  # ifeature7_name.html
        """
        ## Name - Имя объекта

        Тип данных: BSTR (строка)

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        Name = iObject.Name       # Получить свойство (* )
        Name = iObject.GetName()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def ObjectError(self) -> int:  # ifeature7_objecterror.html
        """
        ## ObjectError - Получить номер ошибки

        Тип данных: long

        1. Свойство доступно только для чтения.

        2. Если на объекте взведено несколько ошибок, возвращается номер первой.

        3. Номера ошибок соответствуют перечислению ErrorType3d.

        ### Синтаксис Automation

        ```
        ObjectError = Object.ObjectError       # Получить свойство (* )
        ObjectError = Object.GetObjectError()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def OwnerFeature(self) -> IFeature7:  # ifeature7_ownerfeature.html
        """
        ## OwnerFeature - Объект-владелец

        Тип данных: Указатель на интерфейс IFeature7

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        OwnerFeature = iObject.OwnerFeature       # Получить свойство (* )
        OwnerFeature = iObject.GetOwnerFeature()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def ResultBodies(self) -> typing.Any:  # ifeature7_resultbodies.html
        """
        ## ResultBodies - Массив тел (SAFEARRAY)

        Тип данных: VARIANT

        Значения свойства:

        Массив SafeArray типа VT_ARRAY | VT_DISPATCH.

        1. Свойство доступно только для чтения.

        2. Свойство позволяет получить массив SAFEARRAY всех тел, входящих в дерево. Если возвращается один объект, то тип VARIANT-а будет VT_DISPATCH, если возвращается несколько объектов, то тип VARIANT-а будет VT_ARRAY | VT_DISPATCH.

        ### Синтаксис Automation

        ```
        ResultBodies = iObject.ResultBodies;       # Получить свойство (* )
        ResultBodies = iObject.GetResultBodies();  # Получить свойство (**)
        ```
        """
        ...

    @property
    def State(self) -> int:  # ifeature7_state.html
        """
        ## State - Возвращает комбинацию флагов состояния объекта

        Тип данных: из перечисления ksFeatureStateEnum

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        State = Object.State;                      # Получить свойство (* )
        ResultBodies = State = Object.GetState();  # Получить свойство (**)
        ```
        """
        ...

    @property
    def UpdateStamp(self) -> int:  # ifeature7_updatestamp.html
        """
        ## UpdateStamp - Значение, определяющее время изменения геометрии

        Тип данных: long

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        UpdateStamp = iObject.UpdateStamp       # Получить свойство (* )
        UpdateStamp = iObject.GetUpdateStamp()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def Valid(self) -> bool:  # ifeature7_valid.html
        """
        ## Valid - Признак невырожденности объекта

        Тип данных: BOOL

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        Valid = iObject.Valid       # Получить свойство (* )
        Valid = iObject.GetValid()  # Получить свойство (**)
        ```
        """
        ...

    def Delete(self) -> bool:  # ifeature7_delete.html
        """
        ## Delete - Удалить компонент

        ### Синтаксис Automation

        ```
        BOOL Delete();
        ```

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def ModelObjects(self, ObjType) -> typing.Any:  # ifeature7_modelobjects.html
        """
        ## ModelObjects - Объекты, входящие в состав данного объекта (грани, ребра, вершины)

        Тип данных: VARIANT

        1. Свойство доступно только для чтения.

        2. По типу объекта o3d_unknown возвращается коллекция всех объектов.

        3. Коллекция объектов возвращается в виде массива SAFEARRAY объектов LPDISPATCH (VT_ARRAY | VT_DISPATCH). Если возвращается один объект, то тип VARIANT-а будет VT_DISPATCH, если возвращается несколько объектов, то тип VARIANT-а будет VT_ARRAY | VT_DISPATCH.

        ### Синтаксис Automation

        ```
        ModelObjects = iObject.ModelObjects (ObjType)     # Получить свойство (* )
        ModelObjects = iObject.GetModelObjects (ObjType)  # Получить свойство (**)
        ```

        ### Входные параметры

        +---------------------------------------------------------------------------+
        | VARIANT ObjType | - тип объектов из перечисления KompasAPIObjectTypeEnum. |
        +---------------------------------------------------------------------------+
        """
        ...

    def SubFeatures(self, TreeType, Through, LibObject) -> typing.Any:  # ifeature7_subfeatures.html
        """
        ## SubFeatures - Коллекция элементов дерева заданного типа в виде массива SAFEARRAY DISPATCH - VT_ARRAY | VT_DISPATCH

        Тип данных: VARIANT

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        SubFeatures = Object.SubFeatures( treeType, through, libObject )       # Получить свойство (* )
        SubFeatures = Object. GetSubFeatures ( treeType, through, libObject )  # Получить свойство (**)
        ```

        ### Входные параметры

        +----------------------------------------------------------------------+
        | treeType  | - тип дерева из перечисления ksTreeTypeEnum,             |
        +----------------------------------------------------------------------+
        | through   | - TRUE - выдавать все объекты, даже скрытые,             |
        +----------------------------------------------------------------------+
        | libObject | - TRUE - выдавать содержимое для библиотечного элемента. |
        +----------------------------------------------------------------------+
        """
        ...

    def Variable(self, External, InSource, Index) -> IVariable7:  # ifeature7_variable.html
        """
        ## Variable - Получить параметрическую переменную по имени, индексу

        Тип данных: указатель на интерфейс IVariable7

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        Variable = Object.Variables( External, InSource, Index )     # Получить свойство (* )
        Variable = Object.GetVariables( External, InSource, Index )  # Получить свойство (**)
        ```

        ### Входные параметры

        +--------------------------------------------------+
        | BOOL ExternalOnly | - только внешние,            |
        +--------------------------------------------------+
        | BOOL InSource     | - из источника,              |
        +--------------------------------------------------+
        | VARIANT Index     | - имя или индекс переменной. |
        +--------------------------------------------------+
        """
        ...

    def Variables(self, ExternalOnly, InSource) -> typing.Any:  # ifeature7_variables.html
        """
        ## Variables - Получить массив параметрических переменных в виде SAFEARRAY VT_DISPATCH

        Тип данных: VARIANT

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        Variables = Object.Variables( ExternalOnly, InSource )     # Получить свойство (* )
        Variables = Object.GetVariables( ExternalOnly, InSource )  # Получить свойство (**)
        ```

        ### Входные параметры

        +---------------------------------------+
        | BOOL ExternalOnly | - только внешние, |
        +---------------------------------------+
        | BOOL InSource     | - из источника.   |
        +---------------------------------------+
        """
        ...

    def VariablesCount(self, External, InSource) -> int:  # ifeature7_variablescount.html
        """
        ## VariablesCount - Получить количество параметрических переменных

        Тип данных: long

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        VariablesCount = Object.VariablesCount( External, InSource )     # Получить свойство (* )
        VariablesCount = Object.GetVariablesCount( External, InSource )  # Получить свойство (**)
        ```

        ### Входные параметры

        +---------------------------------------+
        | BOOL ExternalOnly | - только внешние, |
        +---------------------------------------+
        | BOOL InSource     | - из источника.   |
        +---------------------------------------+
        """
        ...




class IMassInertiaParam7(IDispatch):  # imassinertiaparam7.html
    """
    ## Интерфейс IMassInertiaParam7

    Интерфейс массово-центровочных характеристик.

    1. Интерфейс контейнера является дополнительным к интерфейсу IPart7 и IEmbodiment. Данный интерфейс можно получить от IPart7 и от IEmbodiment посредством вызова метода IUnknown::QueryInterface (const GUID far& iid, void** pif);

    2. Интерфейс позволяет получить доступ к массово-центровочным характеристикам детали и сборки. С помощью интерфейса можно получить и отредактировать массово-центровочные характеристики объекта.

    3. Новые характеристики вступают в силу после вызова метода ImodelObject::Update.
    """

    @property
    def Actual(self) -> bool:  # imassinertiaparam7_actual.html
        """
        ## Actual - Актуальность расчета

        Тип данных: BOOL

        Значения свойства:

        +---------------------------------+
        | TRUE  | - информация актуальна, |
        +---------------------------------+
        | FALSE | - не актуальна.         |
        +---------------------------------+

        1. Свойство доступно только для чтения.

        2. Свойство позволяет получить сведения об актуальности информации.

        ### Синтаксис Automation

        ```
        Actual = iObject.Actual        # Получить свойство (* )
        Actual = iObject.GetActual( )  # Получить свойство (**)
        ```
        """
        ...

    @property
    def Area(self) -> float:  # imassinertiaparam7_area.html
        """
        ## Area - Значение площади

        Тип данных: double

        1. Свойство доступно только для чтения.

        2. Свойство позволяет получить значение площади.

        ### Синтаксис Automation

        ```
        Area = iObject.Area        # Получить свойство (* )
        Area = iObject.GetArea( )  # Получить свойство (**)
        ```
        """
        ...

    @property
    def CalculateMass(self) -> float:  # imassinertiaparam7_calculatemass.html
        """
        ## CalculateMass - Расчетное значение массы

        Тип данных: double

        1. Свойство доступно только для чтения.

        2. Свойство позволяет получить единицы измерения массы.

        ### Синтаксис Automation

        ```
        CalculateMass = iObject.CalculateMass( )     # Получить свойство (* )
        CalculateMass = iObject.GetCalculateMass( )  # Получить свойство (**)
        ```
        """
        ...

    @property
    def CopySourceData(self) -> bool:  # imassinertiaparam7_copysourcedata.html
        """
        ## CopySourceData - Данные из источника копирования

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        CopySourceData = Object.CopySourceData       # Получить свойство (* )
        Object.CopySourceData = CopySourceData       # Установить свойство (* )
        CopySourceData = Object.GetCopySourceData()  # Получить свойство (**)
        Object.SetCopySourceData( CopySourceData )   # Установить свойство (**)
        ```
        """
        ...
    @CopySourceData.setter
    def CopySourceData(self, value: bool): ...

    @property
    def Density(self) -> float:  # imassinertiaparam7_density.html
        """
        ## Density - Плотность

        Тип данных: double

        1. Свойство позволяет получить плотность тела (г/куб.мм).

        2. Свойство доступно только для чтения.

        3. Для установки плотности необходимо использовать метод IMassInertiaParam7::SetMaterial.

        ### Синтаксис Automation

        ```
        Density = iObject.Density       # Получить свойство (* )
        Density = iObject.GetDensity()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def DensityMode(self) -> bool:  # imassinertiaparam7_densitymode.html
        """
        ## DensityMode - Режим задания плотности

        Тип данных: BOOL

        Значения свойства:

        +----------------------------------------------+
        | TRUE  | - ручное задание плотности,          |
        +----------------------------------------------+
        | FALSE | - значение плотности из Справочника. |
        +----------------------------------------------+

        Свойство позволяет установить и получить режим задания плотности.

        ### Синтаксис Automation

        ```
        DensityMode = iObject.DensityMode        # Получить свойство (* )
        iObject.DensityMode = DensityMode        # Установить свойство (* )
        DensityMode = iObject.GetDensityMode( )  # Получить свойство (**)
        iObject.SetDensityMode( DensityMode )    # Установить свойство (**)
        ```
        """
        ...
    @DensityMode.setter
    def DensityMode(self, value: bool): ...

    @property
    def HandBookDensity(self) -> float:  # imassinertiaparam7_handbookdensity.html
        """
        ## HandBookDensity - Значение плотности из Справочника

        Тип данных: double

        1. Свойство доступно только для чтения.

        2. Свойство позволяет получить значение плотности из Справочника.

        ### Синтаксис Automation

        ```
        HandBookDensity = iObject.HandBookDensity        # Получить свойство (* )
        HandBookDensity = iObject.GetHandBookDensity( )  # Получить свойство (**)
        ```
        """
        ...

    @property
    def Jx(self) -> float:  # imassinertiaparam7_jx.html
        """
        ## Jx - Осевой момент инерции в центральной системе координат относительно центральной оси координат X

        Тип данных: double

        1. Свойство доступно только для чтения.

        2. Позволяет получить значение осевого момента инерции относительно относительно центральной оси координат X.

        См. также Центральная система координат

        ### Синтаксис Automation

        ```
        Jx = iObject.Jx        # Получить свойство (* )
        Jx = iObject.GetJx( )  # Получить свойство (**)
        ```
        """
        ...

    @property
    def Jx0(self) -> float:  # imassinertiaparam7_jx0.html
        """
        ## Jx0 - Осевой момент инерции в главной центральной системе координат относительно оси X

        Тип данных: double

        Свойство доступно только для чтения.

        См. также Главная центральная система координат

        ### Синтаксис Automation

        ```
        Jx0 = iObject.Jx0        # Получить свойство (* )
        Jx0 = iObject.GetJx0( )  # Получить свойство (**)
        ```
        """
        ...

    @property
    def Jxy(self) -> float:  # imassinertiaparam7_jxy.html
        """
        ## Jxy - Центробежный момент инерции в центральной системе координат относительно центральных осей координат X и Y

        Тип данных: double

        1. Свойство доступно только для чтения.

        2. Позволяет получить значение центробежного момента инерции относительно центральных осей координат X и Y.

        См. также Центральная система координат

        ### Синтаксис Automation

        ```
        Jxy = iObject.Jxy        # Получить свойство (* )
        Jxy = iObject.GetJxy( )  # Получить свойство (**)
        ```
        """
        ...

    @property
    def Jxz(self) -> float:  # imassinertiaparam7_jxz.html
        """
        ## Jxz - Центробежный момент инерции в центральной системе координат относительно центральных осей координат X и Z

        Тип данных: double

        1. Свойство доступно только для чтения.

        2. Позволяет получить значение центробежного момента инерции относительно центральных осей координат X и Z.

        См. также Центральная система координат

        ### Синтаксис Automation

        ```
        Jxz = iObject.Jxz        # Получить свойство (* )
        Jxz = iObject.GetJxz( )  # Получить свойство (**)
        ```
        """
        ...

    @property
    def Jy(self) -> float:  # imassinertiaparam7_jy.html
        """
        ## Jy - Осевой момент инерции в центральной системе координат относительно центральной оси координат Y

        Тип данных: double

        1. Свойство доступно только для чтения.

        2. Позволяет получить значение осевого момента инерции относительно относительно центральной оси координат Y.

        См. также Центральная система координат

        ### Синтаксис Automation

        ```
        Jy = iObject.Jy        # Получить свойство (* )
        Jy = iObject.GetJy( )  # Получить свойство (**)
        ```
        """
        ...

    @property
    def Jy0(self) -> float:  # imassinertiaparam7_jy0.html
        """
        ## Jy0 - Осевой момент инерции в главной центральной системе координат относительно оси Y

        Тип данных: double

        Свойство доступно только для чтения.

        См. также Главная центральная система координат

        ### Синтаксис Automation

        ```
        Jy0 = iObject.Jy0        # Получить свойство (* )
        Jy0 = iObject.GetJy0( )  # Получить свойство (**)
        ```
        """
        ...

    @property
    def Jyz(self) -> float:  # imassinertiaparam7_jyz.html
        """
        ## Jyz - Центробежный момент инерции в центральной системе координат относительно центральных осей координат Y и Z

        Тип данных: double

        1. Свойство доступно только для чтения.

        2. Позволяет получить значение центробежного момента инерции относительно центральных осей координат Y и Z.

        См. также Центральная система координат

        ### Синтаксис Automation

        ```
        Jyz = iObject.Jyz        # Получить свойство (* )
        Jyz = iObject.GetJyz( )  # Получить свойство (**)
        ```
        """
        ...

    @property
    def Jz(self) -> float:  # imassinertiaparam7_jz.html
        """
        ## Jz - Осевой момент инерции в центральной системе координат относительно центральной оси координат Z

        Тип данных: double

        1. Свойство доступно только для чтения.

        2. Позволяет получить значение осевого момента инерции относительно относительно центральной оси координат Z.

        См. также Центральная система координат

        ### Синтаксис Automation

        ```
        Jz = iObject.Jz        # Получить свойство (* )
        Jz = iObject.GetJz( )  # Получить свойство (**)
        ```
        """
        ...

    @property
    def Jz0(self) -> float:  # imassinertiaparam7_jz0.html
        """
        ## Jz0 - Осевой момент инерции в главной центральной системе координат относительно оси Z

        Тип данных: double

        Свойство доступно только для чтения.

        См. также Главная центральная система координат

        ### Синтаксис Automation

        ```
        Jz0 = iObject.Jz0        # Получить свойство (* )
        Jz0 = iObject.GetJz0( )  # Получить свойство (**)
        ```
        """
        ...

    @property
    def LX(self) -> float:  # imassinertiaparam7_lx.html
        """
        ## Lx - Осевой момент инерции относительно глобальной (исходной) оси координат X

        Тип данных: double

        1. Свойство доступно только для чтения.

        2. Позволяет получить значение осевого момента инерции относительно исходной оси координат X.

        ### Синтаксис Automation

        ```
        Lx = iObject.Lx        # Получить свойство (* )
        Lx = iObject.GetLx( )  # Получить свойство (**)
        ```
        """
        ...

    @property
    def LY(self) -> float:  # imassinertiaparam7_ly.html
        """
        ## Ly - Осевой момент инерции относительно глобальной ( исходной ) оси координат Y

        Тип данных: double

        1. Свойство доступно только для чтения.

        2. Позволяет получить значение осевого момента инерции относительно исходной оси координат Y.

        ### Синтаксис Automation

        ```
        Ly = iObject.Ly        # Получить свойство (* )
        Ly = iObject.GetLy( )  # Получить свойство (**)
        ```
        """
        ...

    @property
    def LengthUnits(self) -> int:  # imassinertiaparam7_lengthunits.html
        """
        ## LengthUnits - Единицы измерения длины

        Тип данных: из перечисления ksLengthUnitEnum

        Свойство позволяет установить и получить единицы измерения длины.

        ### Синтаксис Automation

        ```
        LengthUnits = iObject.LengthUnits        # Получить свойство (* )
        iObject.LengthUnits = LengthUnits        # Установить свойство (* )
        LengthUnits = iObject.GetLengthUnits( )  # Получить свойство (**)
        iObject.SetLengthUnits( LengthUnits )    # Установить свойство (**)
        ```
        """
        ...
    @LengthUnits.setter
    def LengthUnits(self, value: int): ...

    @property
    def Lxy(self) -> float:  # imassinertiaparam7_lxy.html
        """
        ## Lxy - Центробежный момент инерции относительно исходных осей координат X и Y

        Тип данных: double

        1. Свойство доступно только для чтения.

        2. Позволяет получить значение осевого момента инерции относительно исходных осей координат X и Y.

        ### Синтаксис Automation

        ```
        Lxy = iObject.Lxy        # Получить свойство (* )
        Lxy = iObject.GetLxy( )  # Получить свойство (**)
        ```
        """
        ...

    @property
    def Lxz(self) -> float:  # imassinertiaparam7_lxz.html
        """
        ## Lxz - Центробежный момент инерции относительно исходных осей координат X и Z

        Тип данных: double

        1. Свойство доступно только для чтения.

        2. Позволяет получить значение осевого момента инерции относительно исходных осей координат X и Z.

        ### Синтаксис Automation

        ```
        Lxz = iObject.Lxz        # Получить свойство (* )
        Lxz = iObject.GetLxz( )  # Получить свойство (**)
        ```
        """
        ...

    @property
    def Lyz(self) -> float:  # imassinertiaparam7_lyz.html
        """
        ## Lyz - Центробежный момент инерции относительно исходных осей координат Y и Z

        Тип данных: double

        1. Свойство доступно только для чтения.

        2. Позволяет получить значение осевого момента инерции относительно исходных осей координат Y и Z.

        ### Синтаксис Automation

        ```
        Lyz = iObject.Lyz        # Получить свойство (* )
        Lyz = iObject.GetLyz( )  # Получить свойство (**)
        ```
        """
        ...

    @property
    def Lz(self) -> float:  # imassinertiaparam7_lz.html
        """
        ## Lz - Осевой момент инерции относительно глобальной ( исходной ) оси координат Z

        Тип данных: double

        1. Свойство доступно только для чтения.

        2. Позволяет получить значение осевого момента инерции относительно исходной оси координат Z.

        ### Синтаксис Automation

        ```
        Lz = iObject.Lz        # Получить свойство (* )
        Lz = iObject.GetLz( )  # Получить свойство (**)
        ```
        """
        ...

    @property
    def ManualMassCentre(self) -> bool:  # imassinertiaparam7_manualmasscentre.html
        """
        ## ManualMassCentre - Признак ручного задания центра масс

        Тип данных: BOOL

        Значения свойства:

        +-------------------------------------------------+
        | TRUE  | - ручное задание координат центра масс, |
        +-------------------------------------------------+
        | FALSE | - рассчитанное значение.                |
        +-------------------------------------------------+

        Свойство позволяет установить и получить признак ручного задания центра масс.

        ### Синтаксис Automation

        ```
        ManualMassCentre = iObject.ManualMassCentre        # Получить свойство (* )
        iObject.ManualMassCentre = ManualMassCentre        # Установить свойство (* )
        ManualMassCentre = iObject.GetManualMassCentre( )  # Получить свойство (**)
        iObject.SetManualMassCentre( ManualMassCentre )    # Установить свойство (**)
        ```
        """
        ...
    @ManualMassCentre.setter
    def ManualMassCentre(self, value: bool): ...

    @property
    def Mass(self) -> float:  # imassinertiaparam7_mass.html
        """
        ## Mass - Масса компонента

        Тип данных: double

        1. Свойство позволяет получить массу тела.

        2. Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        Mass = iObject.Mass       # Получить свойство (* )
        Mass = iObject.GetMass()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def MassSettingMode(self) -> int:  # imassinertiaparam7_masssettingmode.html
        """
        ## MassSettingMode - Режим задания параметров

        Тип данных: из перечисления ksMassSettingModeEnum

        Свойство позволяет установить и получить режим задания параметров.

        ### Синтаксис Automation

        ```
        MassSettingMode = iObject.MassSettingMode        # Получить свойство (* )
        iObject.MassSettingMode = MassSettingMode        # Установить свойство (* )
        MassSettingMode = iObject.GetMassSettingMode( )  # Получить свойство (**)
        iObject.SetMassSettingMode( MassSettingMode )    # Установить свойство (**)
        ```
        """
        ...
    @MassSettingMode.setter
    def MassSettingMode(self, value: int): ...

    @property
    def MassUnits(self) -> int:  # imassinertiaparam7_massunits.html
        """
        ## MassUnits - Единицы измерения массы

        Тип данных: из перечисления ksMassUnitsEnum

        Свойство позволяет установить и получить единицы измерения массы.

        ### Синтаксис Automation

        ```
        MassUnits = iObject.MassUnits        # Получить свойство (* )
        iObject.MassUnits = MassUnits        # Установить свойство (* )
        MassUnits = iObject.GetMassUnits( )  # Получить свойство (**)
        iObject.SetMassUnits( MassUnits )    # Установить свойство (**)
        ```
        """
        ...
    @MassUnits.setter
    def MassUnits(self, value: int): ...

    @property
    def Material(self) -> str:  # imassinertiaparam7_material.html
        """
        ## Material - Материал

        Тип данных: BSTR (строка)

        1. Свойство позволяет получить материал тела.

        2. Свойство доступно только для чтения.

        3. Для установки материала необходимо использовать метод IMassInertiaParam7::SetMaterial.

        ### Синтаксис Automation

        ```
        Material = iObject.Material       # Получить свойство (* )
        Material = iObject.GetMaterial()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def MaterialLocation(self) -> str:  # imassinertiaparam7_materiallocation.html
        """
        ## MaterialLocation - Идентификатор материала

        Тип данных: BSTR

        ### Синтаксис Automation

        ```
        MaterialLocation = Object.MaterialLocation       # Получить свойство (* )
        Object.MaterialLocation = MaterialLocation       # Установить свойство (* )
        MaterialLocation = Object.GetMaterialLocation()  # Получить свойство (**)
        Object.SetMaterialLocation( MaterialLocation )   # Установить свойство (**)
        ```
        """
        ...
    @MaterialLocation.setter
    def MaterialLocation(self, value: str): ...

    @property
    def SourceData(self) -> bool:  # imassinertiaparam7_sourcedata.html
        """
        ## SourceData - Данные из источника

        Тип данных: BOOL

        Значения свойства:

        +------------------------------------+
        | TRUE  | - данные из источника,     |
        +------------------------------------+
        | FALSE | - отличаются от источника. |
        +------------------------------------+

        1. Свойство доступно только для компонента, вставленного в сборку.

        2. Свойство позволяет установить и получить признак того, что данные взяты из источника.

        ### Синтаксис Automation

        ```
        SourceData = iObject.SourceData        # Получить свойство (* )
        iObject.SourceData = SourceData        # Установить свойство (* )
        SourceData = iObject.GetSourceData( )  # Получить свойство (**)
        iObject.SetDensityMode( DensityMode )  # Установить свойство (**)
        ```
        """
        ...
    @SourceData.setter
    def SourceData(self, value: bool): ...

    @property
    def Volume(self) -> float:  # imassinertiaparam7_volume.html
        """
        ## Volume - Значение объема

        Тип данных: double

        1. Свойство доступно только для чтения.

        2. Свойство позволяет получить значение объема.

        ### Синтаксис Automation

        ```
        Volume = iObject.Volume        # Получить свойство (* )
        Volume = iObject.GetVolume( )  # Получить свойство (**)
        ```
        """
        ...

    @property
    def Xc(self) -> float:  # imassinertiaparam7_xc.html
        """
        ## Xc - Координата X центра масс

        Тип данных: double

        Свойство позволяет установить и получить координату X центра масс. Для установки координат центра масс должен быть включен режим ручной установки центра масс (см. свойство IMassInertiaParam7::ManualMassCentre).

        ### Синтаксис Automation

        ```
        Xc = iObject.Xc        # Получить свойство (* )
        iObject.Xc = Xc        # Установить свойство (* )
        Xc = iObject.GetXc( )  # Получить свойство (**)
        iObject.SetXc( Xc )    # Установить свойство (**)
        ```
        """
        ...
    @Xc.setter
    def Xc(self, value: float): ...

    @property
    def Yc(self) -> float:  # imassinertiaparam7_yc.html
        """
        ## Yc - Координата Y центра масс

        Тип данных: double

        Свойство позволяет установить и получить координату Z центра масс. Для установки координат центра масс должен быть включен режим ручной установки центра масс (см. свойство IMassInertiaParam7::ManualMassCentre).

        ### Синтаксис Automation

        ```
        Zc = iObject.Zc        # Получить свойство (* )
        iObject.Zc = Zc        # Установить свойство (* )
        Zc = iObject.GetZc( )  # Получить свойство (**)
        iObject.SetZc( Zc )    # Установить свойство (**)
        ```
        """
        ...
    @Yc.setter
    def Yc(self, value: float): ...

    @property
    def Zc(self) -> float:  # imassinertiaparam7_zc.html
        """
        ## Zc - Координата Z центра масс

        Тип данных: double

        Свойство позволяет установить и получить координату Y центра масс. Для установки координат центра масс должен быть включен режим ручной установки центра масс (см. свойство IMassInertiaParam7::ManualMassCentre).

        ### Синтаксис Automation

        ```
        Yc = iObject.Yc        # Получить свойство (* )
        iObject.Yc = Yc        # Установить свойство (* )
        Yc = iObject.GetYc( )  # Получить свойство (**)
        iObject.SetYc( Yc )    # Установить свойство (**)
        ```
        """
        ...
    @Zc.setter
    def Zc(self, value: float): ...

    @property
    def ManualDensity(self) -> typing.NoReturn:  # imassinertiaparam7_manualdensity.html
        """
        ## ManualDensity - Ручное задание плотности

        Тип данных: double

        1. Свойство доступно только для записи. Позволяет явно установить значение плотности. Введенное значение появится в поле Плотность.

        2. Единицы измерения плотности определяются значениями свойств IMassInertiaParam7::MassUnits и IMassInertiaParam7::LengthUnits.

        3. Для задания плотности необходимо выполнить следующие действия:

        •установить режим Расчет параметров плотности (см. свойство IMassInertiaParam7::MassSettingMode).

        •установить режим Ручной ввод плотности (см. свойство IMassInertiaParam7::DensityMode).

        ### Синтаксис Automation

        ```
        iObject.ManualDensity = ManualDensity     # Установить свойство (* )
        iObject.SetManualDensity( ManualDensity)  # Установить свойство (**)
        ```
        """
        ...
    @ManualDensity.setter
    def ManualDensity(self, value: float): ...

    @property
    def ManualMass(self) -> typing.NoReturn:  # imassinertiaparam7_manualmass.html
        """
        ## ManualMass - Ручное задание массы

        Тип данных: double

        Свойство доступно только для записи. Позволяет вручную установить значение массы. Для задания массы необходимо установить режим ручного задания параметров (см. свойство IMassInertiaParam7::MassSettingMode).

        ### Синтаксис Automation

        ```
        iObject.ManualMass = ManualMass      # Установить свойство (* )
        iObject.SetManualMass( ManualMass )  # Установить свойство (**)
        ```
        """
        ...
    @ManualMass.setter
    def ManualMass(self, value: float): ...

    def Calculate(self) -> bool:  # imassinertiaparam7_calculate.html
        """
        ## Calculate - Рассчитать параметры

        Метод позволяет рассчитать массово-центровочные характеристики объекта. Метод следует вызывать после изменения одного из параметров объекта, чтобы привести остальные параметры в соответствие с измененным.

        ### Синтаксис Automation

        ```
        BOOL Calculate( );
        ```

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def GetAxisX(self) -> bool:  # imassinertiaparam7_getaxisx.html
        """
        ## GetAxisX - Получить вектор направлений главных центральных осей инерции по оси X

        Метод позволяет получать координаты x, y, z точки вектора направления по оси X.

        ### Синтаксис Automation

        ```
        BOOL GetAxisX( double * x, double * y, double * z );
        ```

        ### Выходные параметры

        Координаты x, y, z точки вектора направления по оси X.

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def GetAxisY(self) -> bool:  # imassinertiaparam7_getaxisy.html
        """
        ## GetAxisY - Получить вектор направлений главных центральных осей инерции по оси Y

        Метод позволяет получать координаты x, y, z точки вектора направления по оси Y.

        ### Синтаксис Automation

        ```
        BOOL GetAxisY( double * x, double * y, double * z );
        ```

        ### Выходные параметры

        Координаты x, y, z точки вектора направления по оси Y.

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def GetAxisZ(self) -> bool:  # imassinertiaparam7_getaxisz.html
        """
        ## GetAxisZ - Получить вектор направлений главных центральных осей инерции по оси Z

        Метод позволяет получать координаты x, y, z точки вектора направления по оси Z.

        ### Синтаксис Automation

        ```
        BOOL GetAxisZ( double * x, double * y, double * z );
        ```

        ### Выходные параметры

        - Координаты x, y, z точки вектора направления по оси Z.

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def SetMaterial(self, Name, Density) -> bool:  # imassinertiaparam7_setmaterial.html
        """
        ## SetMaterial - Установить материал и плотность

        Метод позволяет рассчитать массово-центровочные характеристики объекта. Метод следует вызывать после изменения одного из параметров объекта, чтобы привести остальные параметры в соответствие с измененным.

        ### Синтаксис Automation

        ```
        BOOL SetMaterial( BSTR Name,
        double density );
        ```

        ### Входные параметры

        +---------------------------------------------+
        | Name    | - наименование материала,         |
        +---------------------------------------------+
        | Density | - плотность материала (г/куб.мм). |
        +---------------------------------------------+

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...




class ISourcePart7Params(IDispatch):  # isourcepart7params.html
    """
    ## Интерфейс ISourcePart7Params

    Интерфейс параметров компонента в источнике.

    Интерфейс является дополнительным для интерфейса компонента IPart7. Он позволяет получить параметры компонента и параметры документа из источника. Данный интерфейс можно получить от IPart7 посредством вызова метода IUnknown::QueryInterface (const GUID far& iid, void** pif);
    """

    @property
    def DocumentAuthor(self) -> str:  # isourcepart7params_documentauthor.html
        """
        ## DocumentAuthor - Автор документа

        Тип данных: BSTR

        ### Синтаксис Automation

        ```
        DocumentAuthor = iObject.DocumentAuthor;       # Получить свойство (* )
        iObject.DocumentAuthor = DocumentAuthor;       # Установить свойство (* )
        DocumentAuthor = iObject.GetDocumentAuthor();  # Получить свойство (**)
        iObject.DocumentAuthor( DocumentAuthor );      # Установить свойство (**)
        ```
        """
        ...
    @DocumentAuthor.setter
    def DocumentAuthor(self, value: str): ...

    @property
    def DocumentComment(self) -> typing.Any:  # isourcepart7params_documentcomment.html
        """
        ## DocumentComment - Комментарий

        Тип данных: VARIANT

        ### Синтаксис Automation

        ```
        DocumentComment = iObject.DocumentComment;    # Получить свойство (* )
        iObject.DocumentComment= DocumentComment;     # Установить свойство (* )
        DocumentComment = iObject.DocumentComment();  # Получить свойство (**)
        iObject.DocumentComment( DocumentComment );   # Установить свойство (**)
        ```
        """
        ...
    @DocumentComment.setter
    def DocumentComment(self, value: typing.Any): ...

    @property
    def SourceMarking(self) -> str:  # isourcepart7params_sourcemarking.html
        """
        ## SourceMarking - Обозначение в источнике

        Тип данных: BSTR

        ### Синтаксис Automation

        ```
        SourceMarking = iObject.SourceMarking;       # Получить свойство (* )
        iObject.SourceMarking = SourceMarking;       # Установить свойство (* )
        SourceMarking = iObject.GetSourceMarking();  # Получить свойство (**)
        iObject.SourceMarking( SourceMarking );      # Установить свойство (**)
        ```
        """
        ...
    @SourceMarking.setter
    def SourceMarking(self, value: str): ...

    @property
    def SourceName(self) -> str:  # isourcepart7params_sourcename.html
        """
        ## SourceName - Имя компонента в источнике

        Тип данных: BSTR

        ### Синтаксис Automation

        ```
        SourceName = iObject.SourceName;       # Получить свойство (* )
        iObject.SourceName = SourceName;       # Установить свойство (* )
        SourceName = iObject.GetSourceName();  # Получить свойство (**)
        iObject.SetSourceName( SourceName );   # Установить свойство (**)
        ```
        """
        ...
    @SourceName.setter
    def SourceName(self, value: str): ...




class IEmbodimentsManager(IDispatch):  # iembodimentsmanager.html
    """
    ## Интерфейс IEmbodimentsManager

    Интерфейс менеджера исполнений

    Данный интерфейс можно получить у интерфейсов IPart7, IKompasDocument3D, IAssociationView с помощью метода IUnknown::QueryInterface.
    """

    @property
    def CurrentEmbodiment(self) -> IEmbodiment:  # iembodimentsmanager_currentembodiment.html
        """
        ## CurrentEmbodiment - Текущее исполнение

        Тип данных: Указатель на интерфейс IEmbodiment

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        CurrentEmbodiment= Object.CurrentEmbodiment     # Получить свойство (* )
        CurrentEmbodiment= Object.GetCurrentEmbodiment  # Получить свойство (**)
        ```
        """
        ...

    @property
    def CurrentEmbodimentIndex(self) -> int:  # iembodimentsmanager_currentembodimentindex.html
        """
        ## CurrentEmbodimentIndex - Индекс текущего исполнения

        Тип данных: long

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        CurrentEmbodimentMarking = Object.CurrentEmbodiment Index        # Получить свойство (* )
        CurrentEmbodimentMarking = Object.GetCurrentEmbodiment Index ()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def EmbodimentAdditionalNumber(self) -> str:  # iembodimentsmanager_embodimentadditionalnumber.html
        """
        ## EmbodimentAdditionalNumber - Дополнительный номер исполнения модели, использованного для вставки

        Тип данных: BSTR

        ### Синтаксис Automation

        ```
        EmbodimentAdditionalNumber = Object.EmbodimentAdditionalNumber;       # Получить свойство (* )
        Object.EmbodimentAdditionalNumber = EmbodimentAdditionalNumber;       # Установить свойство (* )
        EmbodimentAdditionalNumber = Object.GetEmbodimentAdditionalNumber();  # Получить свойство (**)
        Object.SetEmbodimentAdditionalNumber( EmbodimentAdditionalNumber );   # Установить свойство (**)
        ```
        """
        ...
    @EmbodimentAdditionalNumber.setter
    def EmbodimentAdditionalNumber(self, value: str): ...

    @property
    def EmbodimentCount(self) -> int:  # iembodimentsmanager_embodimentcount.html
        """
        ## EmbodimentCount - Количество исполнений

        Тип данных: long

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        EmbodimentCount = Object.EmbodimentCount       # Получить свойство (* )
        EmbodimentCount = Object.GetEmbodimentCount()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def TopEmbodiment(self) -> IEmbodiment:  # iembodimentsmanager_topembodiment.html
        """
        ## TopEmbodiment - Основное исполнение

        Тип данных: Указатель на интерфейс IEmbodiment.

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        TopEmbodiment = Object.TopEmbodiment     # Получить свойство (* )
        TopEmbodiment = Object.GetTopEmbodiment  # Получить свойство (**)
        ```
        """
        ...

    def AddEmbodiment(self, ParentIndex, Depended, BaseMarking, EmbodimentNumber, AdditionalNumber) -> bool:  # iembodimentsmanager_addembodiment.html
        """
        ## AddEmbodiment - Добавляет новое исполнение и делает его текущим

        ### Синтаксис Automation

        ```
        BOOL AddEmbodiment( VARIANT ParentIndex, BOOL Depended, BSTR BaseMarking, BSTR EmbodimentNumber, BSTR AdditionalNumber );
        ```

        ### Входные параметры

        +--------------------------------------------------+
        | ParentIndex | - индекс родительского исполнения, |
        +--------------------------------------------------+
        | Marking     | - обозначение исполнения.          |
        +--------------------------------------------------+

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def AddMirrorEmbodiment(self, ParentIndex, BaseMarking, EmbodimentNumber, AdditionalNumber) -> bool:  # iembodimentsmanager_addmirrorembodiment.html
        """
        ## AddMirrorEmbodiment - Добавляет новое зеркальное исполнение и делает его текущим

        ### Синтаксис Automation

        ```
        BOOL AddMirrorEmbodiment( VARIANT ParentIndex, BSTR BaseMarking, BSTR EmbodimentNumber, BSTR AdditionalNumber );
        ```

        ### Входные параметры

        +-------------------------------------------------------+
        | ParentIndex      | - индекс родительского исполнения, |
        +-------------------------------------------------------+
        | BaseMarking      | - обозначение исполнения,          |
        +-------------------------------------------------------+
        | EmbodimentNumber | - номер исполнения,                |
        +-------------------------------------------------------+
        | AdditionalNumber | - дополнительный номер.            |
        +-------------------------------------------------------+

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def AddVariant(self, ParentIndex, VariantName):
        ...

    def DeleteEmbodiment(self, Index) -> bool:  # iembodimentsmanager_deleteembodiment.html
        """
        ## DeleteEmbodiment - Удалить исполнение по имени или индексу

        ### Синтаксис Automation

        ```
        BOOL DeleteEmbodiment( VARIANT Index );
        ```

        ### Входные параметры

        +------------------------------------------+
        | Index | - индекс удаляемого обозначения. |
        +------------------------------------------+

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def Embodiment(self, Index) -> IEmbodiment:  # iembodimentsmanager_embodiment.html
        """
        ## Embodiment - Получить исполнение по индексу или обозначению

        Тип данных: Указатель на интерфейс IEmbodiment

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        Embodiment = Object.Embodiment( Index )     # Получить свойство (* )
        Embodiment = Object.GetEmbodiment( Index )  # Получить свойство (**)
        ```
        """
        ...

    def FindEmbodiment(self, UniqueMetaObjectKey):  # iembodimentsmanager_findembodiment.html
        """
        ## FindEmbodiment - Найти исполнение по идентификатору информационного объекта

        ### Синтаксис Automation

        ```
        LPDISPATCH FindEmbodiment( BSTR UniqueMetaObjectKey );
        ```

        ### Входные параметры

        +-------------------------------------------------------+
        | UniqueMetaObjectKey | - ключ информационного объекта. |
        +-------------------------------------------------------+
        """
        ...

    def GetCurrentEmbodimentMarking(self, MarkingType, AddSystemDelimer):  # iembodimentsmanager_getcurrentembodimentmarking.html
        """
        ## GetCurrentEmbodimentMarking - Обозначение текущего исполнения

        ### Синтаксис Automation

        ```
        BSTR GetCurrentEmbodimentMarking( ksVariantMarkingTypeEnum MarkingType,
        BOOL AddSystemDelimer);
        ```

        ### Входные параметры

        +----------------------------------------------------------------------------------+
        | MarkingType      | - часть обозначения из перечисления ksVariantMarkingTypeEnum, |
        +----------------------------------------------------------------------------------+
        | AddSystemDelimer | - добавлять системные разделители.                            |
        +----------------------------------------------------------------------------------+

        Метод позволяет получить обозначение текущего исполнения.

        ### Возвращаемое значение

        - обозначение текущего исполнения.
        """
        ...

    def GetEmbodimentMarking(self, Index, MarkingType, AddSystemDelimer) -> bool:  # iembodimentsmanager_getembodimentmarking.html
        """
        ## GetEmbodimentMarking - Получить обозначение исполнения

        ### Синтаксис Automation

        ```
        BSTR GetEmbodimentMarking( long Index,
        ksVariantMarkingTypeEnum MarkingType,
        BOOL AddSystemDelimer);
        ```

        ### Входные параметры

        +----------------------------------------------------------------------------------+
        | Index            | - индекс обозначения,                                         |
        +----------------------------------------------------------------------------------+
        | MarkingType      | - часть обозначения из перечисления ksVariantMarkingTypeEnum, |
        +----------------------------------------------------------------------------------+
        | AddSystemDelimer | - добавлять системные разделители.                            |
        +----------------------------------------------------------------------------------+

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def GetEmbodimentsTree(self, MarkingType, AddSystemDelimer, AddSpaces) -> str:  # iembodimentsmanager_getembodimentstree.html
        """
        ## GetEmbodimentsTree - Дерево исполнений. Массив узлов дерева исполнений SAFEARRAY BSTR - (VT_ARRAY | VT_BSTR)

        При AddSpaces = TRUE перед обозначением исполнения добавляется соответствующее уровню вложенности количество пробелов.

        ### Синтаксис Automation

        ```
        VARIANT GetEmbodimentsTree( BOOL AddSpaces );
        ```

        ### Входные параметры

        +----------------------------------+
        | AddSpaces | - добавлять пробелы. |
        +----------------------------------+

        Метод возвращает массив обозначений исполнений.

        ### Возвращаемое значение

        Массив SAFEARRAY BSTR - (VT_ARRAY | VT_BSTR);
        """
        ...

    def GetEmbodimentsVariants(self, ParentIndex):  # iembodimentsmanager_getembodimentsvariants.html
        """
        ## GetEmbodimentsVariants - Получить список вариантов геометрии заданного исполнения. SAFEARRAY BSTR - (VT_ARRAY | VT_BSTR)

        ### Синтаксис Automation

        ```
        VARIANT GetEmbodimentsVariants( VARIANT ParentIndex );
        ```

        ### Входные параметры

        +----------------------------------------------------------------------+
        | ParentIndex | - обозначение (VT_BSTR) или индекc (VT_I4) исполнения. |
        +----------------------------------------------------------------------+

        Версия КОМПАС v22

        ### Возвращаемое значение

        Список вариантов геометрии для исполнения;
        """
        ...

    def IsVariant(self, Index) -> bool:  # iembodimentsmanager_isvariant.html
        """
        ## IsVariant - Является ли исполнение вариантом геометрии

        ### Синтаксис Automation

        ```
        BOOL IsVariant( VARIANT Index );
        ```

        ### Входные параметры

        +----------------------------------------------------------------+
        | Index | - обозначение (VT_BSTR) или индекc (VT_I4) исполнения. |
        +----------------------------------------------------------------+

        ### Возвращаемое значение

        HRESULT IsVariant( VARIANT Index, BOOL * Result );

        +----------------------------------------------------+
        | TRUE  | - исполнение является вариантом геометрии, |
        +----------------------------------------------------+
        | FALSE | - обычное исполнение.                      |
        +----------------------------------------------------+

        Версия КОМПАС v22
        """
        ...

    def SetCurrentEmbodiment(self, Index) -> bool:  # iembodimentsmanager_setcurrentembodiment.html
        """
        ## SetCurrentEmbodiment - Установить текущее исполнение по имени или индексу

        ### Синтаксис Automation

        ```
        BOOL SetCurrentEmbodiment( VARIANT Index );
        ```

        ### Входные параметры

        +--------------------------------------+
        | Index | - индекс или имя исполнения. |
        +--------------------------------------+

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def SetEmbodimentMarking(self, Index, MarkingType, Marking) -> bool:  # iembodimentsmanager_setembodimentmarking.html
        """
        ## SetEmbodimentMarking - Установить обозначение исполнения

        ### Синтаксис Automation

        ```
        BOOL SetEmbodimentMarking( long Index,
        ksVariantMarkingTypeEnum MarkingType,
        BSTR Marking);
        ```

        ### Входные параметры

        +-----------------------------------------------------------------------------+
        | Index       | - индекс обозначения,                                         |
        +-----------------------------------------------------------------------------+
        | MarkingType | - часть обозначения из перечисления ksVariantMarkingTypeEnum, |
        +-----------------------------------------------------------------------------+
        | Marking     | - обозначение.                                                |
        +-----------------------------------------------------------------------------+

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...




class IBilletObsolete(ILocalCSObject):  # ibilletobsolete.html
    """
    ## Интерфейс IBilletObsolete

    Интерфейс Детали заготовки и Зеркальной детали
    """

    @property
    def Application(self):
        ...

    @property
    def FileName(self) -> str:  # ibilletobsolete_filename.html
        """
        ## FileName - Имя файла

        Тип данных: BSTR

        ### Синтаксис Automation

        ```
        FileName = Object.FileName       # Получить свойство (* )
        Object.FileName = FileName       # Установить свойство (* )
        FileName = Object.GetFileName()  # Получить свойство (**)
        Object.SetFileName( FileName )   # Установить свойство (**)
        ```
        """
        ...
    @FileName.setter
    def FileName(self, value: str): ...

    @property
    def Hidden(self):
        ...
    @Hidden.setter
    def Hidden(self, value): ...

    @property
    def ModelObjectType(self):
        ...

    @property
    def Name(self):
        ...
    @Name.setter
    def Name(self, value): ...

    @property
    def Owner(self):
        ...

    @property
    def Parent(self):
        ...

    @property
    def Part(self):
        ...

    @property
    def Reference(self):
        ...

    @property
    def Type(self):
        ...

    @property
    def Valid(self):
        ...

    def Update(self):
        ...




class ISaveAsDetailParam(IDispatch):  # isaveasdetailparam.html
    """
    ## Интерфейс ISaveAsDetailParam

    Интерфейс параметров преобразования в модель.

    Данный интерфейс можно получить с помощью метода IKompasDocument1::GetInterface.

    Интерфейс позволяет задать параметры преобразования объектов модели в новую деталь для функции IParts7::SaveAsDetail.

    Версия: Компас v20
    """

    @property
    def FileName(self) -> str:  # isaveasdetailparam_filename.html
        """
        ## FileName - Путь к файлу

        Тип данных: BSTR

        ### Синтаксис Automation

        ```
        FileName = Object.FileName       # Получить свойство (* )
        Object.FileName = FileName       # Установить свойство (* )
        FileName = Object.GetFileName()  # Получить свойство (**)
        Object.SetFileName( FileName )   # Установить свойство (**)
        ```
        """
        ...
    @FileName.setter
    def FileName(self, value: str): ...

    @property
    def LayoutGeometry(self) -> bool:  # isaveasdetailparam_layoutgeometry.html
        """
        ## LayoutGeometry - Компоновочная геометрия

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        LayoutGeometry = Object.LayoutGeometry       # Получить свойство (* )
        Object.LayoutGeometry = LayoutGeometry       # Установить свойство (* )
        LayoutGeometry = Object.GetLayoutGeometry()  # Получить свойство (**)
        Object.SetLayoutGeometry( LayoutGeometry )   # Установить свойство (**)
        ```
        """
        ...
    @LayoutGeometry.setter
    def LayoutGeometry(self, value: bool): ...

    @property
    def Local(self) -> bool:  # isaveasdetailparam_local.html
        """
        ## Local - Локальная деталь

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        Local = Object.Local       # Получить свойство (* )
        Object.Local = Local       # Установить свойство (* )
        Local = Object.GetLocal()  # Получить свойство (**)
        Object.SetLocal( Local )   # Установить свойство (**)
        ```
        """
        ...
    @Local.setter
    def Local(self, value: bool): ...

    @property
    def Marking(self) -> str:  # isaveasdetailparam_marking.html
        """
        ## Marking - Обозначение нового компонента

        Тип данных: BSTR

        ### Синтаксис Automation

        ```
        Marking = Object.Marking       # Получить свойство (* )
        Object.Marking = Marking       # Установить свойство (* )
        Marking = Object.GetMarking()  # Получить свойство (**)
        Object.SetMarking( Marking )   # Установить свойство (**)
        ```
        """
        ...
    @Marking.setter
    def Marking(self, value: str): ...

    @property
    def Mirror(self) -> bool:  # isaveasdetailparam_mirror.html
        """
        ## Mirror - Зеркальная вставка

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        Mirror = Object.Mirror       # Получить свойство (* )
        Object.Mirror = Mirror       # Установить свойство (* )
        Mirror = Object.GetMirror()  # Получить свойство (**)
        Object.SetMirror( Mirror )   # Установить свойство (**)
        ```
        """
        ...
    @Mirror.setter
    def Mirror(self, value: bool): ...

    @property
    def Name(self) -> str:  # isaveasdetailparam_name.html
        """
        ## Name - Имя нового компонента

        Тип данных: BSTR

        ### Синтаксис Automation

        ```
        Name = Object.Name       # Получить свойство (* )
        Object.Name = Name       # Установить свойство (* )
        Name = Object.GetName()  # Получить свойство (**)
        Object.SetName( Name )   # Установить свойство (**)
        ```
        """
        ...
    @Name.setter
    def Name(self, value: str): ...

    @property
    def SaveInitialObjects(self) -> bool:  # isaveasdetailparam_saveinitialobjects.html
        """
        ## SaveInitialObjects - TRUE - Оставлять исходные объекты

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        SaveInitialObjects = Object.SaveInitialObjects       # Получить свойство (* )
        Object.SaveInitialObjects = SaveInitialObjects       # Установить свойство (* )
        SaveInitialObjects = Object.GetSaveInitialObjects()  # Получить свойство (**)
        Object.SetSaveInitialObjects( SaveInitialObjects )   # Установить свойство (**)
        ```
        """
        ...
    @SaveInitialObjects.setter
    def SaveInitialObjects(self, value: bool): ...

    @property
    def Unhistored(self) -> bool:  # isaveasdetailparam_unhistored.html
        """
        ## Unhistored - Без истории

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        Unhistored = Object.Unhistored       # Получить свойство (* )
        Object.Unhistored = Unhistored       # Установить свойство (* )
        Unhistored = Object.GetUnhistored()  # Получить свойство (**)
        Object.SetUnhistored( Unhistored )   # Установить свойство (**)
        ```
        """
        ...
    @Unhistored.setter
    def Unhistored(self, value: bool): ...




class IMateConstraints3D(IKompasCollection):  # imateconstraints3d.html
    """
    ## Интерфейс IMateConstraints3D

    Интерфейс коллекции сопряжений 3D.
    """

    @property
    def MatesMultiCreatedMode(self) -> bool:  # imateconstraints3d_matesmulticreatedmode.html
        """
        ## MatesMultiCreatedMode - Режим группового создания сопряжений

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        MatesMultiCreatedMode = Object.MatesMultiCreatedMode       # Получить свойство (* )
        Object.MatesMultiCreatedMode = MatesMultiCreatedMode       # Установить свойство (* )
        MatesMultiCreatedMode = Object.GetMatesMultiCreatedMode()  # Получить свойство (**)
        Object.SetMatesMultiCreatedMode( MatesMultiCreatedMode )   # Установить свойство (**)
        ```
        """
        ...
    @MatesMultiCreatedMode.setter
    def MatesMultiCreatedMode(self, value: bool): ...

    def Add(self, Type):  # imateconstraints3d_add.html
        """
        ## Add - Создает новый элемент и добавляет его в коллекцию

        ### Синтаксис Automation

        ```
        LPDISPATCH Add( MateConstraintType Type );
        ```

        ### Входные параметры

        +--------------------------+
        | Type | - тип сопряжения. |
        +--------------------------+

        ### Возвращаемое значение

        - новое сопряжение.
        """
        ...

    def AddUserMate(self, Type):  # imateconstraints3d_addusermate.html
        """
        ## AddUserMate - Создает новый пользовательский элемент и добавляет его в коллекцию

        ### Синтаксис Automation

        ```
        LPDISPATCH AddUserMate( MateConstraintType Type );
        ```

        ### Входные параметры

        +--------------------------+
        | Type | - тип сопряжения. |
        +--------------------------+

        ### Возвращаемое значение

        - новое сопряжение.
        """
        ...

    def Item(self, Index):
        ...

    def MateConstraint3D(self, Index) -> IMateConstraint3D:  # imateconstraints3d_mateconstraint3d.html
        """
        ## MateConstraint3D - Возвращает элемент, заданный по индексу или по имени

        Тип данных: Указатель на интерфейс IMateConstraints3D.

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        MateConstraint3D = Object.MateConstraint3D( Index )     # Получить свойство (* )
        MateConstraint3D = Object.GetMateConstraint3D( Index )  # Получить свойство (**)
        ```
        """
        ...

    def ObjectConstraints(self, Object) -> typing.Any:  # imateconstraints3d_objectconstraints.html
        """
        ## ObjectConstraints - Возвращает массив сопряжений, связанных с объектом

        Тип данных: VARIANT

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        ObjectConstraints = Object.ObjectConstraints( Object )     # Получить свойство (* )
        ObjectConstraints = Object.GetObjectConstraints( Object )  # Получить свойство (**)
        ```
        """
        ...




class IMateConstraint3D(IKompasAPIObject):  # imateconstraint3d.html
    """
    ## Интерфейс IMateConstraint3D

    Интерфейс сопряжений 3D.
    """

    @property
    def Alignment(self) -> int:  # imateconstraint3d_alignment.html
        """
        ## Alignment - Варианты выравнивания направлений для сопряжений

        Тип данных: из перечисления ksMateConstraintAlignmentEnum

        ### Синтаксис Automation

        ```
        Alignment = Object.Alignment       # Получить свойство (* )
        Object.Alignment = Alignment       # Установить свойство (* )
        Alignment = Object.GetAlignment()  # Получить свойство (**)
        Object.SetAlignment( Alignment )   # Установить свойство (**)
        ```
        """
        ...
    @Alignment.setter
    def Alignment(self, value: int): ...

    @property
    def BaseObject1(self) -> IModelObject:  # imateconstraint3d_baseobject1.html
        """
        ## BaseObject1 - Базовый объект 1

        Тип данных: Указатель на интерфейс IModelObject

        ### Синтаксис Automation

        ```
        BaseObject1 = Object.BaseObject1       # Получить свойство (* )
        Object.BaseObject1 = BaseObject1       # Установить свойство (* )
        BaseObject1 = Object.GetBaseObject1()  # Получить свойство (**)
        Object.SetBaseObject1( BaseObject1 )   # Установить свойство (**)
        ```
        """
        ...
    @BaseObject1.setter
    def BaseObject1(self, value: IModelObject): ...

    @property
    def BaseObject2(self) -> IModelObject:  # imateconstraint3d_baseobject2.html
        """
        ## BaseObject2 - Базовый объект 2

        Тип данных: Указатель на интерфейс IModelObject

        ### Синтаксис Automation

        ```
        BaseObject2 = Object.BaseObject2       # Получить свойство (* )
        Object.BaseObject2 = BaseObject2       # Установить свойство (* )
        BaseObject2 = Object.GetBaseObject2()  # Получить свойство (**)
        Object.SetBaseObject2( BaseObject2 )   # Установить свойство (**)
        ```
        """
        ...
    @BaseObject2.setter
    def BaseObject2(self, value: IModelObject): ...

    @property
    def ConstraintType(self) -> int:  # imateconstraint3d_constrainttype.html
        """
        ## ConstraintType - Тип сопряжения

        Тип данных: из перечисления MateConstraintType

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        ConstraintType = Object.ConstraintType       # Получить свойство (* )
        ConstraintType = Object.GetConstraintType()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def Fixed(self):  # imateconstraint3d_fixed.html
        """
        ## Fixed - Установить признак фиксации

        Тип данных: из перечисления: ksMateFixedTypeEnum

        ### Синтаксис Automation

        ```
        Fixed = Object.Fixed       # Получить свойство (* )
        Object.Fixed = Fixed       # Установить свойство (* )
        Fixed = Object.GetFixed()  # Получить свойство (**)
        Object.SetFixed( Fixed )   # Установить свойство (**)
        ```
        """
        ...
    @Fixed.setter
    def Fixed(self, value): ...

    @property
    def Hidden(self):
        ...
    @Hidden.setter
    def Hidden(self, value): ...

    @property
    def ModelObjectType(self):
        ...

    @property
    def Name(self):
        ...
    @Name.setter
    def Name(self, value): ...

    @property
    def Owner(self):
        ...

    @property
    def ParamValue(self) -> float:  # imateconstraint3d_paramvalue.html
        """
        ## ParamValue - Параметр для ограничений (расстояние или угол между объектами)

        Тип данных: double

        ### Синтаксис Automation

        ```
        ParamValue = Object.ParamValue       # Получить свойство (* )
        Object.ParamValue = ParamValue       # Установить свойство (* )
        ParamValue = Object.GetParamValue()  # Получить свойство (**)
        Object.SetParamValue( ParamValue )   # Установить свойство (**)
        ```
        """
        ...
    @ParamValue.setter
    def ParamValue(self, value: float): ...

    @property
    def Part(self):
        ...

    @property
    def Valid(self):
        ...

    def GetMateParams(self, Index) -> int:  # imateconstraint3d_getmateparams.html
        """
        ## GetMateParams - Получить параметры математических объектов, участвующих в сопряжении

        1. Параметры возвращаются в системе координат сборки. При необходимости их можно перевести в систему координат нужного компонента с помощью функции IPart::TransformPoint.

        2. Геометрический объект для сопряжения описывается набором из 8-ми вещественных чисел, сгруппированных следующим образом:

        •три координаты точки Pc, Pc = { pointX, pointY, pointZ },

        •три координаты вектора V, V = { vectorI, vectorJ, vectorK },

        •две координаты, соответствующие радиусам, V = { vectorI, vectorJ, vectorK }.

        ### Синтаксис Automation

        ```
        ksMateType GetMateParams( long Index, VARIANT * Params );
        ```

        ### Входные параметры

        +---------------------------+
        | Index | - индекс объекта. |
        +---------------------------+

        ### Выходные параметры

        +-------------------------------------------------------------------+
        | Params | - параметры объекта массив SafeArray вещественных чисел. |
        +-------------------------------------------------------------------+

        ### Возвращаемое значение

        +---------------------------------------------------------------------------------------------------+
        | тип математического объекта, участвующего в сопряжении из ksMateType | - если параметры получены, |
        +---------------------------------------------------------------------------------------------------+
        | ksMateUnknown                                                        | - в случае ошибки.         |
        +---------------------------------------------------------------------------------------------------+
        """
        ...

    def Update(self):
        ...




class IMate3DByAngle(IMateConstraint3D):  # imate3dbyangle.html
    """
    ## Интерфейс IMate3DByAngle

    Интерфейс сопряжения под углом.
    """

    @property
    def Angle3D(self) -> bool:  # imate3dbyangle_angle3d.html
        """
        ## Angle3D - Тип угла. TRUE - пространственный угол, FALSE - плоский угол

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        Angle3D = Object.Angle3D       # Получить свойство (* )
        Object.Angle3D = Angle3D       # Установить свойство (* )
        Angle3D = Object.GetAngle3D()  # Получить свойство (**)
        Object.SetAngle3D( Angle3D )   # Установить свойство (**)
        ```
        """
        ...
    @Angle3D.setter
    def Angle3D(self, value: bool): ...

    @property
    def Axis(self) -> IModelObject:  # imate3dbyangle_axis.html
        """
        ## Axis - Ось плоского угла

        Тип данных: Указатель на интерфейс IModelObject

        ### Синтаксис Automation

        ```
        Axis = Object.Axis       # Получить свойство (* )
        Object.Axis = Axis       # Установить свойство (* )
        Axis = Object.GetAxis()  # Получить свойство (**)
        Object.SetAxis( Axis )   # Установить свойство (**)
        ```
        """
        ...
    @Axis.setter
    def Axis(self, value: IModelObject): ...




class IMate3DDependentPosition(IMateConstraint3D):  # imate3ddependentposition.html
    """
    ## Интерфейс IMate3DDependentPosition

    Интерфейс сопряжения Зависимое положение.
    """

    @property
    def BySample(self) -> bool:  # imate3ddependentposition_bysample.html
        """
        ## BySample - По образцу

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        BySample = Object.BySample       # Получить свойство (* )
        Object.BySample = BySample       # Установить свойство (* )
        BySample = Object.GetBySample()  # Получить свойство (**)
        Object.SetBySample( BySample )   # Установить свойство (**)
        ```
        """
        ...
    @BySample.setter
    def BySample(self, value: bool): ...

    @property
    def SampleObject1(self) -> IPart7:  # imate3ddependentposition_sampleobject1.html
        """
        ## SampleObject1 - Образец - зависимый компонент

        Тип данных: Указатель на интерфейс IPart7

        ### Синтаксис Automation

        ```
        SampleObject1 = Object.SampleObject1       # Получить свойство (* )
        Object.SampleObject1 = SampleObject1       # Установить свойство (* )
        SampleObject1 = Object.GetSampleObject1()  # Получить свойство (**)
        Object.SetSampleObject1( SampleObject1) )  # Установить свойство (**)
        ```
        """
        ...
    @SampleObject1.setter
    def SampleObject1(self, value: IPart7): ...

    @property
    def SampleObject2(self) -> IPart7:  # imate3ddependentposition_sampleobject2.html
        """
        ## SampleObject2 - Образец - базовый компонент

        Тип данных: Указатель на интерфейс IPart7

        ### Синтаксис Automation

        ```
        SampleObject2 = Object.SampleObject2       # Получить свойство (* )
        Object.SampleObject2 = SampleObject2       # Установить свойство (* )
        SampleObject2 = Object.GetSampleObject2()  # Получить свойство (**)
        Object.SetSampleObject2( SampleObject2) )  # Установить свойство (**)
        ```
        """
        ...
    @SampleObject2.setter
    def SampleObject2(self, value: IPart7): ...




class IMate3DSymmetry(IMateConstraint3D):  # imate3dsymmetry.html
    """
    ## Интерфейс IMate3DSymmetry

    Интерфейс сопряжения Симметрия.
    """

    @property
    def Plane(self) -> IModelObject:  # imate3dsymmetry_plane.html
        """
        ## Plane - Плоскость симметрии

        Тип данных: Указатель на интерфейс IModelObject

        ### Синтаксис Automation

        ```
        Plane = Object.Plane       # Получить свойство (* )
        Object.Plane = Plane       # Установить свойство (* )
        Plane = Object.GetPlane()  # Получить свойство (**)
        Object.SetPlane( Plane )   # Установить свойство (**)
        ```
        """
        ...
    @Plane.setter
    def Plane(self, value: IModelObject): ...




class IMate3DByTangent(IMateConstraint3D):  # imate3dbytangent.html
    """
    ## Интерфейс IMate3DByTangent

    Интерфейс сопряжения по касательной.
    """

    @property
    def TangentType(self) -> int:  # imate3dbytangent_tangenttype.html
        """
        ## TangentType - Вид касания

        Тип данных: из перечисления ksMateTangentTypeEnum

        ### Синтаксис Automation

        ```
        TangentType = Object.TangentType       # Получить свойство (* )
        Object.TangentType = TangentType       # Установить свойство (* )
        TangentType = Object.GetTangentType()  # Получить свойство (**)
        Object.SetTangentType( TangentType )   # Установить свойство (**)
        ```
        """
        ...
    @TangentType.setter
    def TangentType(self, value: int): ...




class IMate3DCamGear(IMateConstraint3D):  # imate3dcamgear.html
    """
    ## Интерфейс IMate3DCamGear

    Интерфейс сопряжения двух компонентов кулачкового механизма Кулачек - Толкатель.
    """

    @property
    def CamFaces(self) -> typing.Any:  # imate3dcamgear_camfaces.html
        """
        ## CamFaces - Массив SAFEARRAY граней кулачка

        Тип данных: VARIANT

        ### Синтаксис Automation

        ```
        CamFaces = Object.CamFaces       # Получить свойство (* )
        Object.CamFaces = CamFaces       # Установить свойство (* )
        CamFaces = Object.GetCamFaces()  # Получить свойство (**)
        Object.SetCamFaces( CamFaces )   # Установить свойство (**)
        ```
        """
        ...
    @CamFaces.setter
    def CamFaces(self, value: typing.Any): ...

    @property
    def FollowerFace(self) -> IModelObject:  # imate3dcamgear_followerface.html
        """
        ## FollowerFace - Рабочая грань толкателя

        Тип данных: Указатель на интерфейс IModelObject

        ### Синтаксис Automation

        ```
        FollowerFace = Object.FollowerFace       # Получить свойство (* )
        Object.FollowerFace = FollowerFace       # Установить свойство (* )
        FollowerFace = Object.GetFollowerFace()  # Получить свойство (**)
        Object.SetFollowerFace( FollowerFace )   # Установить свойство (**)
        ```
        """
        ...
    @FollowerFace.setter
    def FollowerFace(self, value: IModelObject): ...

    @property
    def RotationAxis(self) -> IModelObject:  # imate3dcamgear_rotationaxis.html
        """
        ## RotationAxis - Ось вращения кулачка

        Тип данных: Указатель на интерфейс IModelObject

        ### Синтаксис Automation

        ```
        RotationAxis = Object.RotationAxis       # Получить свойство (* )
        Object.RotationAxis = RotationAxis       # Установить свойство (* )
        RotationAxis = Object.GetRotationAxis()  # Получить свойство (**)
        Object.SetRotationAxis( RotationAxis )   # Установить свойство (**)
        ```
        """
        ...
    @RotationAxis.setter
    def RotationAxis(self, value: IModelObject): ...

    @property
    def Trajectory(self) -> IModelObject:  # imate3dcamgear_trajectory.html
        """
        ## Trajectory - Траектория перемещения толкателя

        Тип данных: Указатель на интерфейс IModelObject

        ### Синтаксис Automation

        ```
        Trajectory = Object.Trajectory       # Получить свойство (* )
        Object.Trajectory = Trajectory       # Установить свойство (* )
        Trajectory = Object.GetTrajectory()  # Получить свойство (**)
        Object.SetTrajectory( Trajectory )   # Установить свойство (**)
        ```
        """
        ...
    @Trajectory.setter
    def Trajectory(self, value: IModelObject): ...




class IMate3DTransmission(IMateConstraint3D):  # imate3dtransmission.html
    """
    ## Интерфейс IMate3DTransmission

    Интерфейс сопряжения Механическая передача.
    """

    @property
    def Direction1(self) -> bool:  # imate3dtransmission_direction1.html
        """
        ## Direction1 - Направление движения компонента 1. TRUE - направление 1, FALSE - направление 2

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        Direction1 = Object.Direction1       # Получить свойство (* )
        Object.Direction1 = Direction1       # Установить свойство (* )
        Direction1 = Object.GetDirection1()  # Получить свойство (**)
        Object.SetDirection1( Direction1 )   # Установить свойство (**)
        ```
        """
        ...
    @Direction1.setter
    def Direction1(self, value: bool): ...

    @property
    def Direction2(self) -> bool:  # imate3dtransmission_direction2.html
        """
        ## Direction2 - Направление движения компонента 2. TRUE - направление 1, FALSE - направление 2

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        Direction2 = Object.Direction2       # Получить свойство (* )
        Object.Direction2 = Direction2       # Установить свойство (* )
        Direction2 = Object.GetDirection2()  # Получить свойство (**)
        Object.SetDirection2( Direction2 )   # Установить свойство (**)
        ```
        """
        ...
    @Direction2.setter
    def Direction2(self, value: bool): ...

    @property
    def MotionType1(self) -> int:  # imate3dtransmission_motiontype1.html
        """
        ## MotionType1 - Тип движения компонента 1

        Тип данных: из перечисления ksMateMotionTypeEnum

        ### Синтаксис Automation

        ```
        MotionType1 = Object.MotionType1       # Получить свойство (* )
        Object.MotionType1 = MotionType1       # Установить свойство (* )
        MotionType1 = Object.GetMotionType1()  # Получить свойство (**)
        Object.SetMotionType1( MotionType1 )   # Установить свойство (**)
        ```
        """
        ...
    @MotionType1.setter
    def MotionType1(self, value: int): ...

    @property
    def MotionType2(self) -> int:  # imate3dtransmission_motiontype2.html
        """
        ## MotionType2 - Тип движения компонента 2

        Тип данных: из перечисления ksMateMotionTypeEnum

        ### Синтаксис Automation

        ```
        MotionType2 = Object.MotionType2       # Получить свойство (* )
        Object.MotionType2 = MotionType2       # Установить свойство (* )
        MotionType2 = Object.GetMotionType2()  # Получить свойство (**)
        Object.SetMotionType2( MotionType2 )   # Установить свойство (**)
        ```
        """
        ...
    @MotionType2.setter
    def MotionType2(self, value: int): ...

    @property
    def RotationAxis1(self) -> IModelObject:  # imate3dtransmission_rotationaxis1.html
        """
        ## RotationAxis1 - Ось вращения компонента 1

        Тип данных: Указатель на интерфейс IModelObject

        ### Синтаксис Automation

        ```
        RotationAxis1 = Object.RotationAxis1       # Получить свойство (* )
        Object.RotationAxis1 = RotationAxis1       # Установить свойство (* )
        RotationAxis1 = Object.GetRotationAxis1()  # Получить свойство (**)
        Object.SetRotationAxis1( RotationAxis1 )   # Установить свойство (**)
        ```
        """
        ...
    @RotationAxis1.setter
    def RotationAxis1(self, value: IModelObject): ...

    @property
    def RotationAxis2(self) -> IModelObject:  # imate3dtransmission_rotationaxis2.html
        """
        ## RotationAxis2 - Ось вращения компонента 2

        Тип данных: Указатель на интерфейс IModelObject

        ### Синтаксис Automation

        ```
        RotationAxis2 = Object.RotationAxis2       # Получить свойство (* )
        Object.RotationAxis2 = RotationAxis2       # Установить свойство (* )
        RotationAxis2 = Object.GetRotationAxis2()  # Получить свойство (**)
        Object.SetRotationAxis2( RotationAxis2 )   # Установить свойство (**)
        ```
        """
        ...
    @RotationAxis2.setter
    def RotationAxis2(self, value: IModelObject): ...

    @property
    def Scale1(self) -> float:  # imate3dtransmission_scale1.html
        """
        ## Scale1 - Коэффициент соотношения для 1-го компонента

        Тип данных: double

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        Scale1 = Object.Scale1       # Получить свойство (* )
        Scale1 = Object.GetScale1()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def Scale2(self) -> float:  # imate3dtransmission_scale2.html
        """
        ## Scale2 - Коэффициент соотношения для 2-го компонента

        Тип данных: double

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        Scale2 = Object.Scale2       # Получить свойство (* )
        Scale2 = Object.GetScale2()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def Trajectory1(self) -> IModelObject:  # imate3dtransmission_trajectory1.html
        """
        ## Trajectory1 - Траектория перемещения компонента 1

        Тип данных: Указатель на интерфейс IModelObject

        ### Синтаксис Automation

        ```
        Trajectory1 = Object.Trajectory1       # Получить свойство (* )
        Object.Trajectory1 = Trajectory1       # Установить свойство (* )
        Trajectory1 = Object.GetTrajectory1()  # Получить свойство (**)
        Object.SetTrajectory1( Trajectory1 ))  # Установить свойство (**)
        ```
        """
        ...
    @Trajectory1.setter
    def Trajectory1(self, value: IModelObject): ...

    @property
    def Trajectory2(self) -> IModelObject:  # imate3dtransmission_trajectory2.html
        """
        ## Trajectory2 - Траектория перемещения компонента 2

        Тип данных: Указатель на интерфейс IModelObject

        ### Синтаксис Automation

        ```
        Trajectory2 = Object.Trajectory2       # Получить свойство (* )
        Object.Trajectory2 = Trajectory2       # Установить свойство (* )
        Trajectory2 = Object.GetTrajectory2()  # Получить свойство (**)
        Object.SetTrajectory2( Trajectory2 )   # Установить свойство (**)
        ```
        """
        ...
    @Trajectory2.setter
    def Trajectory2(self, value: IModelObject): ...

    def SetScale(self, Scale1, Scale2) -> bool:  # imate3dtransmission_setscale.html
        """
        ## SetScale - Установить коэффициенты соотношений компонентов

        ### Синтаксис Automation

        ```
        BOOL SetScale( double Scale1, double Scale2 );
        ```

        ### Входные параметры

        +---------------------------+
        | Scale1 | - коэффициент 1, |
        +---------------------------+
        | Scale2 | - коэффициент 2. |
        +---------------------------+

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...




class IMoldCavity(IModelObject):  # imoldcavity.html
    """
    ## Интерфейс IMoldCavity

    Интерфейс операции вычитания компонентов.
    """

    @property
    def Parts(self) -> typing.Any:  # imoldcavity_parts.html
        """
        ## Parts - Объединяемые компоненты

        Тип данных: VARIANT

        Список объектов возвращается в виде массива SAFEARRAY объектов LPDISPATCH ( VT_ARRAY | VT_DISPATCH).

        ### Синтаксис Automation

        ```
        Parts = Object.Parts       # Получить свойство (* )
        Object.Parts = Parts       # Установить свойство (* )
        Parts = Object.GetParts()  # Получить свойство (**)
        Object.SetParts( Parts )   # Установить свойство (**)
        ```
        """
        ...
    @Parts.setter
    def Parts(self, value: typing.Any): ...

    @property
    def Scale(self) -> float:  # imoldcavity_scale.html
        """
        ## Scale - Масштаб

        Тип данных: double

        ### Синтаксис Automation

        ```
        Scale = Object.Scale       # Получить свойство (* )
        Object.Scale = Scale       # Установить свойство (* )
        Scale = Object.GetScale()  # Получить свойство (**)
        Object.SetScale( Scale )   # Установить свойство (**)
        ```
        """
        ...
    @Scale.setter
    def Scale(self, value: float): ...

    @property
    def ScaleCentre(self) -> IModelObject:  # imoldcavity_scalecentre.html
        """
        ## ScaleCentre - Точка центра масштабирования

        Тип данных: Указатель на интерфейс IModelObject.

        ### Синтаксис Automation

        ```
        ScaleCentre = Object.ScaleCentre       # Получить свойство (* )
        Object.ScaleCentre = ScaleCentre       # Установить свойство (* )
        ScaleCentre = Object.GetScaleCentre()  # Получить свойство (**)
        Object.SetScaleCentre( ScaleCentre )   # Установить свойство (**)
        ```
        """
        ...
    @ScaleCentre.setter
    def ScaleCentre(self, value: IModelObject): ...




class IMoldCavities(IModelObjects):  # imoldcavities.html
    """
    ## Интерфейс IMoldCavities

    Интерфейс коллекции операций вычитания компонентов.
    """

    def Add(self) -> IMoldCavity:  # imoldcavities_add.html
        """
        ## Add - Создает новый элемент и добавляет егов коллекцию

        Метод позволяет создать новый интерфейс операции вычитания компонентов.

        После получения нового интерфейса нужно задать параметры операции и вызвать метод IModelObject::Update.

        ### Синтаксис Automation

        ```
        LPDISPATCH Add();
        ```

        ### Возвращаемое значение

        - указатель на интерфейс IMoldCavity.
        """
        ...

    def MoldCavity(self, Index) -> IMoldCavity:  # imoldcavities_moldcavity.html
        """
        ## MoldCavity - Возвращает элемент, заданный по индексу или по имени

        Тип данных: Указатель на интерфейс IMoldCavity.

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        MoldCavity = Object.MoldCavity( Index );     # Получить свойство (* )
        MoldCavity = Object.GetMoldCavity( Index );  # Получить свойство (**)
        ```

        ### Входные параметры

        +--------------------------------------------+
        | VARIANT Index | - индекс или имя элемента. |
        +--------------------------------------------+
        """
        ...




class IUnionComponents(IModelObjects):  # iunioncomponents.html
    """
    ## Интерфейс IUnionComponents

    Интерфейс операции объединения компонентов.
    """

    @property
    def Hidden(self):
        ...
    @Hidden.setter
    def Hidden(self, value): ...

    @property
    def ModelObjectType(self):
        ...

    @property
    def Name(self):
        ...
    @Name.setter
    def Name(self, value): ...

    @property
    def Owner(self):
        ...

    @property
    def Part(self):
        ...

    @property
    def Parts(self) -> typing.Any:  # iunioncomponents_parts.html
        """
        ## Parts - Объединяемые компоненты

        Тип данных: VARIANT

        Список объектов возвращается в виде массива SAFEARRAY объектов LPDISPATCH ( VT_ARRAY | VT_DISPATCH).

        ### Синтаксис Automation

        ```
        Parts = Object.Parts       # Получить свойство (* )
        Object.Parts = Parts       # Установить свойство (* )
        Parts = Object.GetParts()  # Получить свойство (**)
        Object.SetParts( Parts )   # Установить свойство (**)
        ```
        """
        ...
    @Parts.setter
    def Parts(self, value: typing.Any): ...

    @property
    def Valid(self):
        ...

    def Update(self):
        ...




class IUnionsComponents(IModelObjects):  # iunionscomponents.html
    """
    ## Интерфейс IUnionsComponents

    Интерфейс коллекции операций объединение компонентов.
    """

    def Add(self) -> IUnionComponents:  # iunionscomponents_add.html
        """
        ## Add - Создает новый элемент и добавляет егов коллекцию

        Метод позволяет создать новый интерфейс операции объединения компонентов.

        После получения нового интерфейса нужно задать параметры операции и вызвать метод IModelObject::Update.

        ### Синтаксис Automation

        ```
        LPDISPATCH Add();
        ```

        ### Возвращаемое значение

        - указатель на интерфейс IUnionComponents.
        """
        ...

    def UnionComponents(self, Index) -> IUnionComponents:  # iunionscomponents_unioncomponents.html
        """
        ## UnionComponents - Возвращает элемент, заданный по индексу или по имени

        Тип данных: Указатель на интерфейс IUnionComponents.

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        UnionComponents = Object.UnionComponents( Index );     # Получить свойство (* )
        UnionComponents = Object.GetUnionComponents( Index );  # Получить свойство (**)
        ```

        ### Входные параметры

        +--------------------------------------------+
        | VARIANT Index | - индекс или имя элемента. |
        +--------------------------------------------+
        """
        ...




class IMateInterval(IDispatch):  # imateinterval.html
    """
    ## Интерфейс IMateInterval

    Интерфейс интервала значений.

    Интерфейс является дополнительным для интерфейса сопряжений IMateConstraint3D.

    Используется для ограничений:

    •На расстоянии,

    •Под углом.

    Версия КОМПАС v22
    """

    @property
    def MaxValue(self) -> float:  # imateinterval_maxvalue.html
        """
        ## MaxValue - Максимальное значение

        Тип данных: double

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        MinValue = Object.MinValue       # Получить свойство (* )
        MinValue = Object.GetMinValue()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def MinValue(self) -> float:  # imateinterval_minvalue.html
        """
        ## MinValue - Минимальное значение

        Тип данных: double

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        MinValue = Object.MinValue       # Получить свойство (* )
        MinValue = Object.GetMinValue()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def UseInterval(self) -> bool:  # imateinterval_useinterval.html
        """
        ## UseInterval - Использовать диапазон

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        UseInterval = Object.UseInterval       # Получить свойство (* )
        Object.UseInterval = UseInterval       # Установить свойство (* )
        UseInterval = Object.GetUseInterval()  # Получить свойство (**)
        Object.SetUseInterval( UseInterval )   # Установить свойство (**)
        ```
        """
        ...
    @UseInterval.setter
    def UseInterval(self, value: bool): ...

    def SetValueRange(self, MinVal, MaxVal) -> bool:  # imateinterval_setvaluerange.html
        """
        ## SetValueRange - Установить новые ограничения на значение

        ### Синтаксис Automation

        ```
        BOOL SetValueRange( double MinVal, double MaxVal );
        ```

        ### Входные параметры

        +-----------------------------------+
        | MinVal | - минимальное значение,  |
        +-----------------------------------+
        | MaxVal | - максимальное значение. |
        +-----------------------------------+

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...




class IMacroObjects3D(IModelObjects):  # imacroobjects3d.html
    """
    ## Интерфейс IMacroObjects3D

    Интерфейс коллекции макроэлементов 3D.
    """

    def Add(self) -> IMacroObject3D:  # imacroobjects3d_add.html
        """
        ## Add - Создает новый элемент и добавляет его в коллекцию

        Метод позволяет создать новый интерфейс макрообъекта.

        После получения нового интерфейса нужно задать параметры объекта и вызвать метод IModelObject::Update.

        ### Синтаксис Automation

        ```
        LPDISPATCH Add();
        ```

        ### Возвращаемое значение

        - указатель на интерфейс IMacroObject3D.
        """
        ...

    def MacroObject3D(self, Index) -> IMacroObject3D:  # imacroobjects3d_macroobject3d.html
        """
        ## MacroObject3D - Возвращает элемент, заданный по индексу

        Тип данных: Указатель на интерфейс IMacroObject3D.

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        MacroObject3D = Object.MacroObject3D( Index );     # Получить свойство (* )
        MacroObject3D = Object.GetMacroObject3D( Index );  # Получить свойство (**)
        ```

        ### Входные параметры

        +--------------------------------------------+
        | VARIANT Index | - индекс или имя элемента. |
        +--------------------------------------------+
        """
        ...




class IMacroObject3D(IModelObject):  # imacroobject3d.html
    """
    ## Интерфейс IMacroObject3D

    Интерфейс макроэлемента 3D.
    """

    @property
    def AssociationObjectCount(self) -> int:  # imacroobject3d_associationobjectcount.html
        """
        ## AssociationObjectCount - Количество опорных объектов

        Тип данных: long

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        AssociationObjectCount = Object.AssociationObjectCount ;      # Получить свойство (* )
        AssociationObjectCount = Object.GetAssociationObjectCount();  # Получить свойство (**)
        ```
        """
        ...

    @property
    def DoubleClickEditable(self) -> bool:  # imacroobject3d_doubleclickeditable.html
        """
        ## DoubleClickEditable - Редактирование по двойному клику поддерживается

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        DoubleClickEditable = Object.DoubleClickEditable       # Получить свойство (* )
        Object.DoubleClickEditable = DoubleClickEditable       # Установить свойство (* )
        DoubleClickEditable = Object.GetDoubleClickEditable()  # Получить свойство (**)
        Object.SetDoubleClickEditable( DoubleClickEditable )   # Установить свойство (**)
        ```
        """
        ...
    @DoubleClickEditable.setter
    def DoubleClickEditable(self, value: bool): ...

    @property
    def Objects(self) -> typing.Any:  # imacroobject3d_objects.html
        """
        ## Objects - Внутренние объекты макро

        Тип данных: VARIANT

        Список объектов возвращается в виде массива SAFEARRAY объектов LPDISPATCH ( VT_ARRAY | VT_DISPATCH).

        Если объект один, возвращается VARIANT с типом VT_DISPATCH.

        ### Синтаксис Automation

        ```
        Objects = Object.Objects       # Получить свойство (* )
        Object.Objects = Objects       # Установить свойство (* )
        Objects = Object.GetObjects()  # Получить свойство (**)
        Object.SetObjects( Objects )   # Установить свойство (**)
        ```
        """
        ...
    @Objects.setter
    def Objects(self, value: typing.Any): ...

    @property
    def PropertyObjectEditable(self) -> bool:  # imacroobject3d_propertyobjecteditable.html
        """
        ## PropertyObjectEditable - Поддерживается интерфейс внешних свойств объекта

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        PropertyObjectEditable = Object.PropertyObjectEditable       # Получить свойство (* )
        Object.PropertyObjectEditable = PropertyObjectEditable       # Установить свойство (* )
        PropertyObjectEditable = Object.GetPropertyObjectEditable()  # Получить свойство (**)
        Object.SetPropertyObjectEditable( PropertyObjectEditable )   # Установить свойство (**)
        ```
        """
        ...
    @PropertyObjectEditable.setter
    def PropertyObjectEditable(self, value: bool): ...

    @property
    def StaffVisible(self) -> bool:  # imacroobject3d_staffvisible.html
        """
        ## StaffVisible - Управление видимостью состава

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        StaffVisible = Object.StaffVisible       # Получить свойство (* )
        Object.StaffVisible = StaffVisible       # Установить свойство (* )
        StaffVisible = Object.GetStaffVisible()  # Получить свойство (**)
        Object.SetStaffVisible( StaffVisible )   # Установить свойство (**)
        ```
        """
        ...
    @StaffVisible.setter
    def StaffVisible(self, value: bool): ...

    def AssociationObject(self, Index) -> IModelObject:  # imacroobject3d_associationobject.html
        """
        ## AssociationObject - Установить опорный объект

        Тип данных: Указатель на интерфейс IModelObject.

        ### Синтаксис Automation

        ```
        AssociationObject = Object.AssociationObject( Index )     # Получить свойство (* )
        Object.AssociationObject( Index ) = AssociationObject     # Установить свойство (* )
        AssociationObject = Object.GetAssociationObject( Index )  # Получить свойство (**)
        Object.SetAssociationObject( Index, AssociationObject )   # Установить свойство (**)
        ```

        ### Входные параметры

        +--------------------------------+
        | long Index | - индекс объекта. |
        +--------------------------------+
        """
        ...

    def ClearAssociationObject(self) -> bool:  # imacroobject3d_clearassociationobject.html
        """
        ## ClearAssociationObject - Удалить все опорные объекты

        ### Синтаксис Automation

        ```
        BOOL ClearAssociationObject();
        ```

        ### Возвращаемое значение

        +-----------------------------------------+
        | TRUE | - в случае успешного завершения. |
        +-----------------------------------------+
        """
        ...

    def Destroy(self) -> bool:  # imacroobject3d_destroy.html
        """
        ## Destroy - Разрушить макроэлемент

        ### Синтаксис Automation

        ```
        BOOL Destroy();
        ```

        ### Возвращаемое значение

        +-----------------------------------------+
        | TRUE | - в случае успешного завершения. |
        +-----------------------------------------+
        """
        ...

    def SetAssociationObject(self, Index, arg1):
        ...




class ISheetMetalContainer(IDispatch):  # isheetmetalcontainer.html
    """
    ## Интерфейс ISheetMetalContainer

    Интерфейс контейнера объектов гибки.

    ISheetMetalContainer является дополнительным к интерфейсу IPart7 и позволяет работать с коллекциями объектов гибки (листовые тела, сгибы и т.д). Данный интерфейс можно получить от IPart7 посредством вызова метода IUnknown::QueryInterface (const GUID far& iid, void** pif).
    """

    @property
    def ConvertsToSheetMetals(self) -> IConvertsToSheetMetals:  # isheetmetalcontainer_convertstosheetmetals.html
        """
        ## ConvertsToSheetMetals - Коллекция операций преобразования в листовое тело

        Тип данных: Указатель на интерфейс IConvertsToSheetMetals

        Свойство доступно только для чтения.

        Версия КОМПАС v22

        ### Синтаксис Automation

        ```
        IConvertsToSheetMetals = Object.ConvertsToSheetMetals;       # Получить свойство (* )
        IConvertsToSheetMetals = Object.GetConvertsToSheetMetals();  # Получить свойство (**)
        ```
        """
        ...

    @property
    def SheetMetalBendUnfoldParameters(self) -> ISheetMetalBendUnfoldParameters:  # isheetmetalcontainer_sheetmetalbendunfoldparameters.html
        """
        ## SheetMetalBendUnfoldParameters - Параметры развертки

        Тип данных: Указатель на интерфейс ISheetMetalBendUnfoldParameters.

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        SheetMetalBendUnfoldParameters = Object.SheetMetalBendUnfoldParameters;       # Получить свойство (* )
        SheetMetalBendUnfoldParameters = Object.GetSheetMetalBendUnfoldParameters();  # Получить свойство (**)
        ```
        """
        ...

    @property
    def SheetMetalBendedStraightens(self) -> ISheetMetalBendedStraightens:  # isheetmetalcontainer_sheetmetalbendedstraightens.html
        """
        ## SheetMetalBendedStraightens - Коллекция операций Согнуть/Разогнуть

        Тип данных: Указатель на интерфейс ISheetMetalBendedStraightens.

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        SheetMetalBendedStraightens = Object.SheetMetalBendedStraightens;       # Получить свойство (* )
        SheetMetalBendedStraightens = Object.GetSheetMetalBendedStraightens();  # Получить свойство (**)
        ```
        """
        ...

    @property
    def SheetMetalBends(self) -> ISheetMetalBends:  # isheetmetalcontainer_sheetmetalbends.html
        """
        ## SheetMetalBends - Указатель на коллекцию сгибов

        Тип данных: указатель на интерфейс коллекции сгибов ISheetMetalBends

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        ISheetMetalBends = iObject.SheetMetalBends;       # Получить свойство (* )
        ISheetMetalBends = iObject.GetSheetMetalBends();  # Получить свойство (**)
        ```
        """
        ...

    @property
    def SheetMetalBodies(self) -> ISheetMetalBodies:  # isheetmetalcontainer_sheetmetalbodies.html
        """
        ## SheetMetalBodies - Указатель на коллекцию листовых тел

        Тип данных: указатель на интерфейс коллекции листовых тел ISheetMetalBodies

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        ISheetMetalBodies = iObject.SheetMetalBodies;       # Получить свойство (* )
        ISheetMetalBodies = iObject.GetSheetMetalBodies();  # Получить свойство (**)
        ```
        """
        ...

    @property
    def SheetMetalClosedCorners(self) -> ISheetMetalClosedCorners:  # isheetmetalcontainer_sheetmetalclosedcorners.html
        """
        ## SheetMetalClosedCorners - Коллекция замыканий углов

        Тип данных: Указатель на интерфейс ISheetMetalClosedCorners.

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        SheetMetalClosedCorners = Object.SheetMetalClosedCorners;       # Получить свойство (* )
        SheetMetalClosedCorners = Object.GetSheetMetalClosedCorners();  # Получить свойство (**)
        ```
        """
        ...

    @property
    def SheetMetalCuts(self) -> ISheetMetalCuts:  # isheetmetalcontainer_sheetmetalcuts.html
        """
        ## SheetMetalCuts - Указатель на коллекцию элементов листового тела "вырез"

        Тип данных: указатель на интерфейс коллекции элементов "вырез" ISheetMetalCuts

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        ISheetMetalCuts = iObject.SheetMetalCuts;       # Получить свойство (* )
        ISheetMetalCuts = iObject.GetSheetMetalCuts();  # Получить свойство (**)
        ```
        """
        ...

    @property
    def SheetMetalFlangings(self) -> ISheetMetalBends:  # isheetmetalcontainer_sheetmetalflangings.html
        """
        ## SheetMetalFlangings - Коллекция отбортовок

        Тип данных: Указатель на интерфейс ISheetMetalBends.

        Свойство доступно только для чтения.

        Версия: Компас v20

        ### Синтаксис Automation

        ```
        SheetMetalFlangings = Object.SheetMetalFlangings       # Получить свойство (* )
        SheetMetalFlangings = Object.GetSheetMetalFlangings()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def SheetMetalHoles(self) -> ISheetMetalHoles:  # isheetmetalcontainer_sheetmetalholes.html
        """
        ## SheetMetalHoles - Указатель на коллекцию элементов "отверстие"

        Тип данных: указатель на интерфейс коллекции элементов "отверстие" ISheetMetalHoles.

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        ISheetMetalHoles = iObject.SheetMetalHoles;       # Получить свойство (* )
        ISheetMetalHoles = iObject.GetSheetMetalHoles();  # Получить свойство (**)
        ```
        """
        ...

    @property
    def SheetMetalJalousies(self) -> ISheetMetalJalousies:  # isheetmetalcontainer_sheetmetaljalousies.html
        """
        ## SheetMetalJalousies - Коллекция операций Жалюзи

        Тип данных: Указатель на интерфейс ISheetMetalJalousies.

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        SheetMetalJalousies = Object.SheetMetalJalousies;       # Получить свойство (* )
        SheetMetalJalousies = Object.GetSheetMetalJalousies();  # Получить свойство (**)
        ```
        """
        ...

    @property
    def SheetMetalLineBends(self) -> ISheetMetalLineBends:  # isheetmetalcontainer_sheetmetallinebends.html
        """
        ## SheetMetalLineBends - Указатель на коллекцию объектов "сгиб по линии"

        Тип данных: указатель на интерфейс коллекции сгибов по линии ISheetMetalLineBends

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        ISheetMetalLineBends = iObject.SheetMetalLineBends;        # Получить свойство (* )
        ISheetMetalLineBends =\\ iObject.GetSheetMetalLineBends();  # Получить свойство (**)
        ```
        """
        ...

    @property
    def SheetMetalLinearRuledShells(self) -> ISheetMetalBodies:  # isheetmetalcontainer_sheetmetallinearruledshells.html
        """
        ## SheetMetalLinearRuledShells - Вторая коллекция обечаек

        Тип данных: Указатель на интерфейс ISheetMetalBodies.

        Свойство доступно только для чтения.

        Версия Компас v18.1

        ### Синтаксис Automation

        ```
        SheetMetalLinearRuledShells = Object.SheetMetalLinearRuledShells       # Получить свойство (* )
        SheetMetalLinearRuledShells = Object.GetSheetMetalLinearRuledShells()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def SheetMetalPlates(self) -> ISheetMetalPlates:  # isheetmetalcontainer_sheetmetalplates.html
        """
        ## SheetMetalPlates - Коллекция пластин

        Тип данных: Указатель на интерфейс ISheetMetalPlates.

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        SheetMetalPlates = Object.SheetMetalPlates;       # Получить свойство (* )
        SheetMetalPlates = Object.GetSheetMetalPlates();  # Получить свойство (**)
        ```
        """
        ...

    @property
    def SheetMetalPressFormings(self) -> ISheetMetalPressFormings:  # isheetmetalcontainer_sheetmetalpressformings.html
        """
        ## SheetMetalPressFormings - Коллекция операций Открытая/ Закрытая штамповка

        Тип данных: Указатель на интерфейс ISheetMetalPressFormings.

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        SheetMetalPressFormings = Object.SheetMetalPressFormings;       # Получить свойство (* )
        SheetMetalPressFormings = Object.GetSheetMetalPressFormings();  # Получить свойство (**)
        ```
        """
        ...

    @property
    def SheetMetalPunchs(self) -> ISheetMetalPunchs:  # isheetmetalcontainer_sheetmetalpunchs.html
        """
        ## SheetMetalPunchs - Коллекция операций штамповка телом

        Тип данных: Указатель на интерфейс ISheetMetalPunchs

        Свойство доступно только для чтения.

        Версия: Компас v20

        ### Синтаксис Automation

        ```
        SheetMetalPunchs = Object.SheetMetalPunchs;       # Получить свойство (* )
        SheetMetalPunchs = Object.GetSheetMetalPunchs();  # Получить свойство (**)
        ```
        """
        ...

    @property
    def SheetMetalRibs(self) -> ISheetMetalRibs:  # isheetmetalcontainer_sheetmetalribs.html
        """
        ## SheetMetalRibs - Коллекция операций Ребро усиления

        Тип данных: Указатель на интерфейс ISheetMetalRibs.

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        SheetMetalRibs = Object.SheetMetalRibs;       # Получить свойство (* )
        SheetMetalRibs = Object.GetSheetMetalRibs();  # Получить свойство (**)
        ```
        """
        ...

    @property
    def SheetMetalRuledShells(self) -> ISheetMetalBodies:  # isheetmetalcontainer_sheetmetalruledshells.html
        """
        ## SheetMetalRuledShells - Коллекция обечаек

        Тип данных: Указатель на интерфейс ISheetMetalBodies.

        Свойство доступно только для чтения.

        Версия Компас v18.1

        ### Синтаксис Automation

        ```
        SheetMetalRuledShells = Object.SheetMetalRuledShells       # Получить свойство (* )
        SheetMetalRuledShells = Object.GetSheetMetalRuledShells()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def SheetMetalShoulders(self) -> ISheetMetalShoulders:  # isheetmetalcontainer_sheetmetalshoulders.html
        """
        ## SheetMetalShoulders - Коллекция операций Буртик

        Тип данных: Указатель на интерфейс ISheetMetalShoulders.

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        SheetMetalShoulders = Object.SheetMetalShoulders;       # Получить свойство (* )
        SheetMetalShoulders = Object.GetSheetMetalShoulders();  # Получить свойство (**)
        ```
        """
        ...

    @property
    def SheetMetalSketchBends(self) -> ISheetMetalSketchBends:  # isheetmetalcontainer_sheetmetalsketchbends.html
        """
        ## SheetMetalSketchBends - Коллекция сгибов по эскизу

        Тип данных: Указатель на интерфейс ISheetMetalSketchBends.

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        SheetMetalSketchBends = Object.SheetMetalSketchBends;       # Получить свойство (* )
        SheetMetalSketchBends = Object.GetSheetMetalSketchBends();  # Получить свойство (**)
        ```
        """
        ...

    @property
    def SheetMetalUndercuts(self) -> ISheetMetalLineBends:  # isheetmetalcontainer_sheetmetalundercuts.html
        """
        ## SheetMetalUndercuts - Коллекция подсечек

        Тип данных: Указатель на интерфейс ISheetMetalLineBends.

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        SheetMetalUndercuts = Object.SheetMetalUndercuts;       # Получить свойство (* )
        SheetMetalUndercuts = Object.GetSheetMetalUndercuts();  # Получить свойство (**)
        ```
        """
        ...




class IConvertsToSheetMetals(IModelObjects):  # iconvertstosheetmetals.html
    """
    ## Интерфейс IConvertsToSheetMetals

    Интерфейс коллекции операций преобразования в листовое тело.

    Получить интерфейс можно, используя свойство контейнера тел гибки ISheetMetalContainer::ConvertsToSheetMetals.

    Версия КОМПАС v22
    """

    def Add(self) -> IConvertToSheetMetal:  # iconvertstosheetmetals_add.html
        """
        ## Add - Создает новый элемент и добавляет его в коллекцию

        ### Синтаксис Automation

        ```
        LPDISPATH Add();
        ```

        ### Возвращаемое значение

        Указатель на интерфейс операции преобразования в листовое тело IConvertToSheetMetal
        """
        ...

    def ConvertToSheetMetal(self, Index) -> IConvertToSheetMetal:  # iconvertstosheetmetals_converttosheetmetal.html
        """
        ## ConvertToSheetMetal - Возвращает элемент, заданный по индексу

        Тип данных: Указатель на интерфейс IConvertToSheetMetal

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        ConvertToSheetMetal = Object.ConvertToSheetMetal( Index );     # Получить свойство (* )
        ConvertToSheetMetal = Object.GetConvertToSheetMetal( Index );  # Получить свойство (**)
        ```
        """
        ...




class IConvertToSheetMetal(IModelObject):  # iconverttosheetmetal.html
    """
    ## Интерфейс IConvertToSheetMetal

    Интерфейс операций преобразования в листовое тело.

    Интерфейс можно получить, используя свойство IConvertsToSheetMetals::ConvertToSheetMetal и метод IConvertsToSheetMetals::Add.

    Версия КОМПАС v22
    """

    @property
    def BendEdges(self) -> typing.Any:  # iconverttosheetmetal_bendedges.html
        """
        ## BendEdges - Ребра сгибов

        Тип данных:VARIANT

        Набор ребер SAFEARRAY DISPATCH - VT_ARRAY | VT_DISPATCH.

        ### Синтаксис Automation

        ```
        BendEdges = Object.BendEdges      # Получить свойство (* )
        Object.BendEdges = BendEdges      # Установить свойство (* )
        BendEdges = Object.GetBendEdges   # Получить свойство (**)
        Object.SetBendEdges( BendEdges )  # Установить свойство (**)
        ```
        """
        ...
    @BendEdges.setter
    def BendEdges(self, value: typing.Any): ...

    @property
    def BendParametersType(self) -> int:  # iconverttosheetmetal_bendparameterstype.html
        """
        ## BendParametersType - Способ задания параметров сгибов

        Тип данных: из перечисления ksParametersTypeEnum

        ### Синтаксис Automation

        ```
        BendParametersType = Object.BendParametersType      # Получить свойство (* )
        Object.BendParametersType = BendParametersType      # Установить свойство (* )
        BendParametersType = Object.GetBendParametersType   # Получить свойство (**)
        Object.SetBendParametersType( BendParametersType )  # Установить свойство (**)
        ```
        """
        ...
    @BendParametersType.setter
    def BendParametersType(self, value: int): ...

    @property
    def BendTablePath(self) -> str:  # iconverttosheetmetal_bendtablepath.html
        """
        ## BendTablePath - Имя файла таблицы сгибов

        Тип данных: BSTR

        ### Синтаксис Automation

        ```
        BendTablePath = Object.BendTablePath       # Получить свойство (* )
        Object.BendTablePath = BendTablePath       # Установить свойство (* )
        BendTablePath = Object.GetBendTablePath()  # Получить свойство (**)
        Object.SetBendTablePath( BendTablePath )   # Установить свойство (**)
        ```
        """
        ...
    @BendTablePath.setter
    def BendTablePath(self, value: str): ...

    @property
    def BendsCount(self) -> int:  # iconverttosheetmetal_bendscount.html
        """
        ## BendsCount - Количество сгибов

        Тип данных: long

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        BendsCount = Object.BendsCount       # Получить свойство (* )
        BendsCount = Object.GetBendsCount()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def CornerParametersType(self) -> int:  # iconverttosheetmetal_cornerparameterstype.html
        """
        ## CornerParametersType - Способ задания параметров углов

        Тип данных: из перечисления ksParametersTypeEnum

        ### Синтаксис Automation

        ```
        CornerParametersType = Object.CornerParametersType       # Получить свойство (* )
        Object.CornerParametersType = CornerParametersType       # Установить свойство (* )
        CornerParametersType = Object.GetCornerParametersType()  # Получить свойство (**)
        Object.SetCornerParametersType( CornerParametersType )   # Установить свойство (**)
        ```
        """
        ...
    @CornerParametersType.setter
    def CornerParametersType(self, value: int): ...

    @property
    def CornersCount(self) -> int:  # iconverttosheetmetal_cornerscount.html
        """
        ## CornersCount - Количество обрабатываемых углов

        Тип данных: long

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        CornersCount = Object.CornersCount       # Получить свойство (* )
        CornersCount = Object.GetCornersCount()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def DeleteSourceObject(self) -> bool:  # iconverttosheetmetal_deletesourceobject.html
        """
        ## DeleteSourceObject - Удалить исходный объект

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        DeleteSourceObject = Object.DeleteSourceObject       # Получить свойство (* )
        Object.DeleteSourceObject = DeleteSourceObject       # Установить свойство (* )
        DeleteSourceObject = Object.GetDeleteSourceObject()  # Получить свойство (**)
        Object.SetDeleteSourceObject( DeleteSourceObject )   # Установить свойство (**)
        ```
        """
        ...
    @DeleteSourceObject.setter
    def DeleteSourceObject(self, value: bool): ...

    @property
    def Face(self) -> IModelObject:  # iconverttosheetmetal_face.html
        """
        ## Face - Базовая грань

        Тип данных: Указатель на интерфейс IModelObject

        ### Синтаксис Automation

        ```
        Face = Object.Face      # Получить свойство (* )
        Object.Face = Face      # Установить свойство (* )
        Face = Object.GetFace   # Получить свойство (**)
        Object.SetFace( Face )  # Установить свойство (**)
        ```
        """
        ...
    @Face.setter
    def Face(self, value: IModelObject): ...

    @property
    def Thickness(self) -> float:  # iconverttosheetmetal_thickness.html
        """
        ## Thickness - Толщина

        Тип данных: double

        ### Синтаксис Automation

        ```
        Thickness = Object.Thickness      # Получить свойство (* )
        Object.Thickness = Thickness      # Установить свойство (* )
        Thickness = Object.GetThickness   # Получить свойство (**)
        Object.SetThickness( Thickness )  # Установить свойство (**)
        ```
        """
        ...
    @Thickness.setter
    def Thickness(self, value: float): ...

    @property
    def ThicknessDirection(self) -> bool:  # iconverttosheetmetal_thicknessdirection.html
        """
        ## ThicknessDirection - Направление для толщины

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        ThicknessDirection = Object.ThicknessDirection      # Получить свойство (* )
        Object.ThicknessDirection = ThicknessDirection      # Установить свойство (* )
        ThicknessDirection = Object.GetThicknessDirection   # Получить свойство (**)
        Object.SetThicknessDirection( ThicknessDirection )  # Установить свойство (**)
        ```
        """
        ...
    @ThicknessDirection.setter
    def ThicknessDirection(self, value: bool): ...

    def AddEdges(self, PVal) -> bool:  # iconverttosheetmetal_addedges.html
        """
        ## AddEdges - Добавить ребра

        ### Синтаксис Automation

        ```
        BOOL AddEdges( VARIANT Edges );
        ```

        ### Входные параметры

        +-------------------------------------------------------+
        | Edges | - Набор ребер SAFEARRAY DISPATCH - VT_ARRAY . |
        +-------------------------------------------------------+

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def BendCoefficient(self, BendIndex) -> float:  # iconverttosheetmetal_bendcoefficient.html
        """
        ## BendCoefficient - Коэффициент нейтрального слоя

        Тип данных: double

        ### Синтаксис Automation

        ```
        BendCoefficient = Object.BendCoefficient( BendIndex )     # Получить свойство (* )
        Object.BendCoefficient( BendIndex ) = BendCoefficient     # Установить свойство (* )
        BendCoefficient = Object.GetBendCoefficient( BendIndex )  # Получить свойство (**)
        bject.SetBendCoefficient( BendIndex, BendCoefficient )    # Установить свойство (**)
        ```

        ### Выходные параметры

        +----------------------------+
        | BendIndex | - индекс сгиба |
        +----------------------------+
        """
        ...

    def BendRadius(self, BendIndex) -> float:  # iconverttosheetmetal_bendradius.html
        """
        ## BendRadius - Радиус сгиба

        Тип данных: double

        ### Синтаксис Automation

        ```
        BendRadius = Object.BendRadius( BendIndex )     # Получить свойство (* )
        Object.BendRadius( BendIndex ) = BendRadius     # Установить свойство (* )
        BendRadius = Object.GetBendRadius( BendIndex )  # Получить свойство (**)
        Object.SetBendRadius( BendIndex, BendRadius )   # Установить свойство (**)
        ```

        ### Выходные параметры

        +----------------------------+
        | BendIndex | - индекс сгиба |
        +----------------------------+
        """
        ...

    def BendReduction(self, BendIndex) -> float:  # iconverttosheetmetal_bendreduction.html
        """
        ## BendReduction - Уменьшение сгиба

        Тип данных: double

        ### Синтаксис Automation

        ```
        BendReduction = Object.BendReduction( BendIndex )     # Получить свойство (* )
        Object.BendReduction( BendIndex ) = BendReduction     # Установить свойство (* )
        BendReduction = Object.GetBendReduction( BendIndex )  # Получить свойство (**)
        Object.SetBendReduction( BendIndex, BendReduction )   # Установить свойство (**)
        ```

        ### Выходные параметры

        +-----------------------------+
        | BendIndex | - индекс сгиба. |
        +-----------------------------+
        """
        ...

    def BendValue(self, BendIndex) -> float:  # iconverttosheetmetal_bendvalue.html
        """
        ## BendValue - Величина сгиба

        Тип данных: double

        ### Синтаксис Automation

        ```
        BendValue = Object.BendValue( BendIndex )     # Получить свойство (* )
        Object.BendValue( BendIndex ) = BendValue     # Установить свойство (* )
        BendValue = Object.GetBendValue( BendIndex )  # Получить свойство (**)
        Object.SetBendValue( BendIndex, BendValue )   # Установить свойство (**)
        ```

        ### Выходные параметры

        +----------------------------+
        | BendIndex | - индекс сгиба |
        +----------------------------+
        """
        ...

    def ClosingClosedType(self, Index) -> int:  # iconverttosheetmetal_closingclosedtype.html
        """
        ## ClosingClosedType - Способ замыкания углов

        Тип данных: из перечисления ksClosingClosedTypeEnum

        ### Синтаксис Automation

        ```
        ClosingClosedType = Object.ClosingClosedType( Index )     # Получить свойство (* )
        Object.ClosingClosedType( Index ) = ClosingClosedType     # Установить свойство (* )
        ClosingClosedType = Object.GetClosingClosedType( Index )  # Получить свойство (**)
        Object.SetClosingClosedType( Index, ClosingClosedType )   # Установить свойство (**)
        ```

        ### Входные параметры

        +-----------------------------+
        | long Index | - индекс угла. |
        +-----------------------------+
        """
        ...

    def ClosingCorneringType(self, Index) -> int:  # iconverttosheetmetal_closingcorneringtype.html
        """
        ## ClosingCorneringType - Обработка угла при замыкании

        Тип данных: из перечисления ksClosingCorneringEnum

        ### Синтаксис Automation

        ```
        ClosingCorneringType = Object.ClosingCorneringType( Index )     # Получить свойство (* )
        Object.ClosingCorneringType( Index ) = ClosingCorneringType     # Установить свойство (* )
        ClosingCorneringType = Object.GetClosingCorneringType( Index )  # Получить свойство (**)
        Object.SetClosingCorneringType( Index, ClosingCorneringType )   # Установить свойство (**)
        ```

        ### Входные параметры

        +-----------------------------+
        | long Index | - индекс угла. |
        +-----------------------------+
        """
        ...

    def ClosingGapValue(self, Index) -> float:  # iconverttosheetmetal_closinggapvalue.html
        """
        ## ClosingGapValue - Значение зазора при замыкании углов

        Тип данных: double

        ### Синтаксис Automation

        ```
        ClosingGapValue = Object.ClosingGapValue( Index )     # Получить свойство (* )
        Object.ClosingGapValue( Index ) = ClosingGapValue     # Установить свойство (* )
        ClosingGapValue = Object.GetClosingGapValue( Index )  # Получить свойство (**)
        Object.SetClosingGapValue( Index, ClosingGapValue )   # Установить свойство (**)
        ```

        ### Входные параметры

        +-----------------------------+
        | long Index | - индекс угла. |
        +-----------------------------+
        """
        ...

    def ClosingHoleDiameter(self, Index) -> float:  # iconverttosheetmetal_closingholediameter.html
        """
        ## ClosingHoleDiameter - Диаметр отверстия при замыкании угла

        Тип данных: double

        ### Синтаксис Automation

        ```
        ClosingHoleDiameter = Object.ClosingHoleDiameter( Index )     # Получить свойство (* )
        Object.ClosingHoleDiameter( Index ) = ClosingHoleDiameter     # Установить свойство (* )
        ClosingHoleDiameter = Object.GetClosingHoleDiameter( Index )  # Получить свойство (**)
        Object.SetClosingHoleDiameter( Index, ClosingHoleDiameter )   # Установить свойство (**)
        ```

        ### Входные параметры

        +-----------------------------+
        | long Index | - индекс угла. |
        +-----------------------------+
        """
        ...

    def ClosingHoleOffset(self, Index) -> float:  # iconverttosheetmetal_closingholeoffset.html
        """
        ## ClosingHoleOffset - Смещение отверстия при замыкании угла

        Тип данных: double

        ### Синтаксис Automation

        ```
        ClosingHoleOffset = Object.ClosingHoleOffset( Index )     # Получить свойство (* )
        Object.ClosingHoleOffset( Index ) = ClosingHoleOffset     # Установить свойство (* )
        ClosingHoleOffset = Object.GetClosingHoleOffset( Index )  # Получить свойство (**)
        Object.SetClosingHoleOffset( Index, ClosingHoleOffset )   # Установить свойство (**)
        ```

        ### Входные параметры

        +-----------------------------+
        | long Index | - индекс угла. |
        +-----------------------------+
        """
        ...

    def ClosingHolePlacement(self, Index) -> int:  # iconverttosheetmetal_closingholeplacement.html
        """
        ## ClosingHolePlacement - Размещение отверстия при круговой обработке угла

        Тип данных: из перечисления ksClosingHolePlacementEnum

        ### Синтаксис Automation

        ```
        ClosingHolePlacement = Object.ClosingHolePlacement( Index )     # Получить свойство (* )
        Object.ClosingHolePlacement( Index ) = ClosingHolePlacement     # Установить свойство (* )
        ClosingHolePlacement = Object.GetClosingHolePlacement( Index )  # Получить свойство (**)
        Object.SetClosingHolePlacement( Index, ClosingHolePlacement )   # Установить свойство (**)
        ```

        ### Входные параметры

        +-----------------------------+
        | long Index | - индекс угла. |
        +-----------------------------+
        """
        ...

    def DeleteEdges(self, PVal) -> bool:  # iconverttosheetmetal_deleteedges.html
        """
        ## DeleteEdges - Удалить ребра

        ### Синтаксис Automation

        ```
        BOOL DeleteEdges( VARIANT Edges );
        ```

        ### Входные параметры

        +-------------------------------------------------------+
        | Edges | - Набор ребер SAFEARRAY DISPATCH - VT_ARRAY . |
        +-------------------------------------------------------+

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def InternalRadius(self, BendIndex) -> bool:  # iconverttosheetmetal_internalradius.html
        """
        ## InternalRadius - Способ задания радиуса сгиба

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        InternalRadius = Object.InternalRadius ( BendIndex )    # Получить свойство (* )
        Object.InternalRadius( BendIndex ) = InternalRadius     # Установить свойство (* )
        InternalRadius = Object.GetInternalRadius( BendIndex )  # Получить свойство (**)
        Object.SetInternalRadius( BendIndex, InternalRadius )   # Установить свойство (**)
        ```

        ### Входные параметры

        +---------------------------------+
        | long BendIndex | - индекс сгиба |
        +---------------------------------+
        """
        ...

    def SetBendCoefficient(self, BendIndex, arg1):
        ...

    def SetBendRadius(self, BendIndex, arg1):
        ...

    def SetBendReduction(self, BendIndex, arg1):
        ...

    def SetBendValue(self, BendIndex, arg1):
        ...

    def SetClosingClosedType(self, Index, arg1):
        ...

    def SetClosingCorneringType(self, Index, arg1):
        ...

    def SetClosingGapValue(self, Index, arg1):
        ...

    def SetClosingHoleDiameter(self, Index, arg1):
        ...

    def SetClosingHoleOffset(self, Index, arg1):
        ...

    def SetClosingHolePlacement(self, Index, arg1):
        ...

    def SetInternalRadius(self, BendIndex, arg1):
        ...

    def SetUnfoldType(self, BendIndex, arg1):
        ...

    def UnfoldType(self, BendIndex) -> int:  # iconverttosheetmetal_unfoldtype.html
        """
        ## UnfoldType - Способ определения длины развертки

        Тип данных: из перечисления ksUnfoldTypeEnum

        ### Синтаксис Automation

        ```
        UnfoldType = Object.UnfoldType      # Получить свойство (* )
        Object.UnfoldType = UnfoldType      # Установить свойство (* )
        UnfoldType = Object.GetUnfoldType   # Получить свойство (**)
        Object.SetUnfoldType( UnfoldType )  # Установить свойство (**)
        ```

        ### Входные параметры

        +---------------------------------+
        | long BendIndex | - индекс сгиба |
        +---------------------------------+
        """
        ...




class ISheetMetalBends(IModelObjects):  # isheetmetalbends.html
    """
    ## Интерфейс ISheetMetalBends

    Интерфейс коллекции сгибов.

    Получить интерфейс можно, используя свойство контейнера тел гибки ISheetMetalContainer::SheetMetalBends
    """

    def Add(self) -> ISheetMetalBend:  # isheetmetalbends_add.html
        """
        ## Add - Создать сгиб (добавить сгиб в коллекцию)

        1. Метод позволяет создать новый интерфейс сгиба.

        2. После получения нового интерфейса нужно задать параметры листового тела и вызвать метод IModelObject::Update.

        ### Синтаксис Automation

        ```
        LPDISPATH Add();
        ```

        ### Возвращаемое значение

        +--------------------------------------------------------------------------------+
        | Указатель на интерфейс сгиба ISheetMetalBend | - в случае удачного завершения. |
        +--------------------------------------------------------------------------------+
        """
        ...

    def SheetMetalBend(self, Index) -> ISheetMetalBend:  # isheetmetalbends_sheetmetalbend.html
        """
        ## SheetMetalBend - Сгиб, заданный по индексу или ссылке

        Тип данных – указатель на интерфейс ISheetMetalBend.

        В качестве индекса может использоваться:

        •индекс объекта в коллекции,

        •ссылка на объект (reference).

        ### Синтаксис Automation

        ```
        SheetMetalBend = iObject.SheetMetalBend( index )     # Получить свойство (* )
        SheetMetalBend = iObject.GetSheetMetalBend( index )  # Получить свойство (**)
        ```

        ### Входные параметры

        +------------------------------------+
        | VARIANT index | - индекс операции. |
        +------------------------------------+
        """
        ...




class ISheetMetalBodies(IModelObjects):  # isheetmetalbodies.html
    """
    ## Интерфейс ISheetMetalBodies

    Интерфейс коллекции листовых тел.

    Получить интерфейс можно, используя свойство контейнера тел гибки ISheetMetalContainer::SheetMetalBodies.
    """

    def Add(self) -> ISheetMetalBody:  # isheetmetalbodies_add.html
        """
        ## Add - Создать листовое тело

        1. Метод позволяет создать новый интерфейс базового листового тела.

        2. После получения нового интерфейса нужно задать параметры листового тела и вызвать метод IModelObject::Update.

        ### Синтаксис Automation

        ```
        LPDISPATH Add();
        ```

        ### Возвращаемое значение

        +-----------------------------------------------------------------------------------------+
        | Указатель на интерфейс листового тела ISheetMetalBody | - в случае удачного завершения. |
        +-----------------------------------------------------------------------------------------+
        """
        ...

    def SheetMetalBody(self, Index) -> ISheetMetalBody:  # isheetmetalbodies_sheetmetalbody.html
        """
        ## SheetMetalBody - Листовое тело, заданное по индексу или ссылке

        Тип данных: указатель на интерфейс ISheetMetalBody

        В качестве индекса может использоваться:

        •индекс объекта в коллекции,

        •ссылка на объект (reference).

        ### Синтаксис Automation

        ```
        SheetMetalBody = iObject.SheetMetalBody( index )     # Получить свойство (* )
        SheetMetalBody = iObject.GetSheetMetalBody( index )  # Получить свойство (**)
        ```

        ### Входные параметры

        +-------------------------------------------------+
        | VARIANT index | - индекс операции выдавливания. |
        +-------------------------------------------------+
        """
        ...




class ISheetMetalCuts(IModelObjects):  # isheetmetalcuts.html
    """
    ## Интерфейс ISheetMetalCuts

    Интерфейс коллекции элементов листового тела "вырез".

    Получить интерфейс можно, используя свойство контейнера тел гибки ISheetMetalContainer::SheetMetalCuts.
    """

    def Add(self) -> ISheetMetalCut:  # isheetmetalcuts_add.html
        """
        ## Add - Создать объект "вырез"

        1. Метод позволяет создать новый объект "вырез".

        2. После получения нового объекта нужно задать его параметры и вызвать метод IModelObject::Update.

        ### Синтаксис Automation

        ```
        LPDISPATH Add();
        ```

        ### Возвращаемое значение

        +-----------------------------------------------------------------------------------+
        | Указатель на интерфейс отверстия ISheetMetalCut | - в случае удачного завершения. |
        +-----------------------------------------------------------------------------------+
        """
        ...

    def SheetMetalCut(self, Index) -> ISheetMetalCut:  # isheetmetalcuts_sheetmetalcut.html
        """
        ## SheetMetalCut - Объект "вырез", заданный по индексу или ссылке

        Тип данных: указатель на интерфейс ISheetMetalCut

        В качестве индекса может использоваться:

        •индекс объекта в коллекции,

        •ссылка на объект (reference).

        ### Синтаксис Automation

        ```
        ISheetMetalCut = iObject.SheetMetalCut( index )     # Получить свойство (* )
        ISheetMetalCut = iObject.GetSheetMetalCut( index )  # Получить свойство (**)
        ```

        ### Входные параметры

        +-----------------------------------+
        | VARIANT index | - индекс объекта. |
        +-----------------------------------+
        """
        ...




class ISheetMetalHoles(IModelObjects):  # isheetmetalholes.html
    """
    ## Интерфейс ISheetMetalHoles

    Интерфейс коллекции элементов листового тела "отверстие".

    Получить интерфейс можно, используя свойство контейнера тел гибки ISheetMetalContainer::SheetMetalHoles.
    """

    def Add(self) -> ISheetMetalHole:  # isheetmetalholes_add.html
        """
        ## Add - Создать объект "отверстие"

        Синтаксис СОМ:

        HRESULT Add( ISheetMetalHole** Result);

        1. Метод позволяет создать новый объект "отверстие".

        2. После получения нового объекта нужно задать его параметры и вызвать метод IModelObject::Update.

        ### Синтаксис Automation

        ```
        LPDISPATH Add();
        ```

        ### Возвращаемое значение

        +------------------------------------------------------------------------------------+
        | Указатель на интерфейс отверстия ISheetMetalHole | - в случае удачного завершения. |
        +------------------------------------------------------------------------------------+
        """
        ...

    def SheetMetalHole(self, Index) -> ISheetMetalHole:  # isheetmetalholes_sheetmetalhole.html
        """
        ## SheetMetalHole - Объект "отверстие", заданный по индексу или ссылке

        Тип данных: указатель на интерфейс ISheetMetalHole

        В качестве индекса может использоваться:

        •индекс объекта в коллекции,

        •ссылка на объект (reference).

        ### Синтаксис Automation

        ```
        ISheetMetalHole = iObject.SheetMetalHole( index )     # Получить свойство (* )
        ISheetMetalHole = iObject.GetSheetMetalHole( index )  # Получить свойство (**)
        ```

        ### Входные параметры

        +-----------------------------------+
        | VARIANT index | - индекс объекта. |
        +-----------------------------------+
        """
        ...




class ISheetMetalLineBends(IModelObjects):  # isheetmetallinebends.html
    """
    ## Интерфейс ISheetMetalLineBends

    Интерфейс коллекции объектов "сгиб по линии".

    Получить интерфейс можно, используя свойство контейнера тел гибки ISheetMetalContainer::SheetMetalLineBends.
    """

    def Add(self) -> ISheetMetalLineBend:  # isheetmetallinebends_add.html
        """
        ## Add - Создать объект "сгиб по линии" (добавить сгиб в коллекцию)

        1. Метод позволяет создать новый интерфейс сгиба по линии.

        2. После получения нового интерфейса нужно задать параметры листового тела и вызвать метод IModelObject::Update.

        ### Синтаксис Automation

        ```
        LPDISPATH Add();
        ```

        ### Возвращаемое значение

        +--------------------------------------------------------------------------+
        | Указатель на линии ISheetMetalLineBend | - в случае удачного завершения. |
        +--------------------------------------------------------------------------+
        """
        ...

    def SheetMetalLineBend(self, Index) -> ISheetMetalLineBend:  # isheetmetallinebends_sheetmetallinebend.html
        """
        ## SheetMetalLineBend - Операция "сгиб по линии", заданная по индексу или ссылке

        Тип данных: указатель на интерфейс ISheetMetalLineBend

        В качестве индекса может использоваться:

        •индекс объекта в коллекции,

        •ссылка на объект (reference).

        ### Синтаксис Automation

        ```
        ISheetMetalLineBend = iObject.SheetMetalLineBend( index )     # Получить свойство (* )
        ISheetMetalLineBend = iObject.GetSheetMetalLineBend( index )  # Получить свойство (**)
        ```

        ### Входные параметры

        +------------------------------------+
        | VARIANT index | - индекс операции. |
        +------------------------------------+
        """
        ...




class ISheetMetalBody(IModelObject):  # isheetmetalbody.html
    """
    ## Интерфейс ISheetMetalBody

    Интерфейс параметров листового тела.

    Интерфейс можно получить с помощью метода коллекции операций выдавливания ISheetMetalBodies::Add или свойства ISheetMetalBodies::SheetMetalBody.
    """

    @property
    def BendCoefficient(self) -> float:  # isheetmetalbody_bendcoefficient.html
        """
        ## BendCoefficient - Коэффициент нейтрального слоя

        Тип данных: double

        Чтобы свойство было доступно, необходимо установить тип расчета длины развёртки - коэффициент нейтрального слоя. Позволяет считывать и устанавливать коэффициент нейтрального слоя.

        ### Синтаксис Automation

        ```
        BendCoefficient = iObject.BendCoefficient;      # Получить свойство (* )
        iObject.BendCoefficient = BendCoefficient;      # Установить свойство (* )
        BendCoefficient = iObject.GetBendCoefficient;   # Получить свойство (**)
        iObject.SetBendCoefficient( BendCoefficient );  # Установить свойство (**)
        ```
        """
        ...
    @BendCoefficient.setter
    def BendCoefficient(self, value: float): ...

    @property
    def BendReduction(self) -> float:  # isheetmetalbody_bendreduction.html
        """
        ## BendReduction - Уменьшение сгиба

        Тип данных: double

        Чтобы свойство было доступно, необходимо установить тип расчета длины развёртки - уменьшение сгиба. Позволяет считывать и устанавливать уменьшение сгиба.

        ### Синтаксис Automation

        ```
        BendReduction = iObject.BendReduction;      # Получить свойство (* )
        iObject.BendReduction = BendReduction;      # Установить свойство (* )
        BendReduction = iObject.GetBendReduction;   # Получить свойство (**)
        iObject.SetBendReduction( BendReduction );  # Установить свойство (**)
        ```
        """
        ...
    @BendReduction.setter
    def BendReduction(self, value: float): ...

    @property
    def BendTablePath(self) -> str:  # isheetmetalbody_bendtablepath.html
        """
        ## BendTablePath - Путь к таблице сгибов

        Тип данных: BSTR

        Чтобы свойство было доступно, необходимо установить тип расчета длины развёртки -из таблицы. Свойство позволяет считывать и устанавливать путь к таблице сгибов.

        ### Синтаксис Automation

        ```
        BendTablePath = iObject.BendTablePath;      # Получить свойство (* )
        iObject.BendTablePath = BendTablePath;      # Установить свойство (* )
        BendTablePath = iObject.GetBendTablePath;   # Получить свойство (**)
        iObject.SetBendTablePath( BendTablePath );  # Установить свойство (**)
        ```
        """
        ...
    @BendTablePath.setter
    def BendTablePath(self, value: str): ...

    @property
    def BendValue(self) -> float:  # isheetmetalbody_bendvalue.html
        """
        ## BendValue - Величина сгиба

        Тип данных: double

        Чтобы свойство было доступно, необходимо установить тип расчета длины развёртки - величина сгиба. Позволяет считывать и устанавливать величину сгиба.

        ### Синтаксис Automation

        ```
        BendValue = iObject.BendValue;      # Получить свойство (* )
        iObject.BendValue = BendValue;      # Установить свойство (* )
        BendValue = iObject.GetBendValue;   # Получить свойство (**)
        iObject.SetBendValue( BendValue );  # Установить свойство (**)
        ```
        """
        ...
    @BendValue.setter
    def BendValue(self, value: float): ...

    @property
    def Direction(self) -> int:  # isheetmetalbody_direction.html
        """
        ## Direction - Направление выдавливания

        Тип данных: из перечисления ksDirectionTypeEnum

        Позволяет считывать и устанавливать направление выдавливания.

        ### Синтаксис Automation

        ```
        Direction = iObject.Direction;      # Получить свойство (* )
        iObject.Direction = Direction;      # Установить свойство (* )
        Direction = iObject.GetDirection;   # Получить свойство (**)
        iObject.SetDirection( Direction );  # Установить свойство (**)
        ```
        """
        ...
    @Direction.setter
    def Direction(self, value: int): ...

    @property
    def OperationResult(self):
        ...
    @OperationResult.setter
    def OperationResult(self, value): ...

    @property
    def Profile(self) -> IModelObject:  # isheetmetalbody_profile.html
        """
        ## Profile - Сечение

        Тип данных: Указатель на интерфейс IModelObject

        ### Синтаксис Automation

        ```
        Profile = Object.Profile       # Получить свойство (* )
        Object.Profile = Profile       # Установить свойство (* )
        Profile = Object.GetProfile()  # Получить свойство (**)
        Object.SetProfile( Profile )   # Установить свойство (**)
        ```
        """
        ...
    @Profile.setter
    def Profile(self, value: IModelObject): ...

    @property
    def Radius(self) -> float:  # isheetmetalbody_radius.html
        """
        ## Radius - Радиус сгиба

        Тип данных: double

        Позволяет считывать и устанавливать радиус сгиба.

        ### Синтаксис Automation

        ```
        Radius = iObject.Radius;      # Получить свойство (* )
        iObject.Radius = Radius;      # Установить свойство (* )
        Radius = iObject.GetRadius;   # Получить свойство (**)
        iObject.SetRadius( Radius );  # Установить свойство (**)
        ```
        """
        ...
    @Radius.setter
    def Radius(self, value: float): ...

    @property
    def Sketch(self) -> ISketch:  # isheetmetalbody_sketch.html
        """
        ## Sketch - Эскиз

        Тип данных: указатель на интерфейс ISketch

        Позволяет установить новый эскиз для операции и получить указатель на текущий эскиз.

        ### Синтаксис Automation

        ```
        Sketch = iObject.Sketch;      # Получить свойство (* )
        iObject.Sketch = Sketch;      # Установить свойство (* )
        Sketch = iObject.GetSketch;   # Получить свойство (**)
        iObject.SetSketch( Sketch );  # Установить свойство (**)
        ```
        """
        ...
    @Sketch.setter
    def Sketch(self, value: ISketch): ...

    @property
    def Straighten(self) -> bool:  # isheetmetalbody_straighten.html
        """
        ## Straighten - Разогнуть тело

        Тип данных: BOOL

        Позволяет разгибать и сгибать тело.

        ### Синтаксис Automation

        ```
        Straighten = iObject.Straighten;      # Получить свойство (* )
        iObject.Straighten = Straighten;      # Установить свойство (* )
        Straighten = iObject.GetStraighten;   # Получить свойство (**)
        iObject.SetStraighten( Straighten );  # Установить свойство (**)
        ```
        """
        ...
    @Straighten.setter
    def Straighten(self, value: bool): ...

    @property
    def Thickness(self) -> float:  # isheetmetalbody_thickness.html
        """
        ## Thickness - Толщина листового тела

        Тип данных: double

        Позволяет установить и получить толщину листового тела.

        ### Синтаксис Automation

        ```
        Thickness = iObject.Thickness( );   # Получить свойство (* )
        iObject.Thickness( ) = Thickness;   # Установить свойство (* )
        Thickness = iObject.GetThickness;   # Получить свойство (**)
        iObject.SetThickness( Thickness );  # Установить свойство (**)
        ```
        """
        ...
    @Thickness.setter
    def Thickness(self, value: float): ...

    @property
    def ThicknessDirection(self) -> bool:  # isheetmetalbody_thicknessdirection.html
        """
        ## ThicknessDirection - Направление для толщины

        Тип данных: BOOL

        Позволяет установить и получить направление для толщины.

        ### Синтаксис Automation

        ```
        ThicknessDirection = iObject.ThicknessDirection;      # Получить свойство (* )
        iObject.ThicknessDirection = ThicknessDirection;      # Установить свойство (* )
        ThicknessDirection = iObject.GetThicknessDirection;   # Получить свойство (**)
        iObject.SetThicknessDirection( ThicknessDirection );  # Установить свойство (**)
        ```
        """
        ...
    @ThicknessDirection.setter
    def ThicknessDirection(self, value: bool): ...

    @property
    def UnfoldType(self) -> int:  # isheetmetalbody_unfoldtype.html
        """
        ## UnfoldType - Способ определения длины развертки

        Тип данных: из перечисления ksUnfoldTypeEnum

        Позволяет считывать и устанавливать способ определения длины развертки. Если задан способ определения длины развертки - "из таблицы", то будут недоступны свойства установки (Set) для коэффициента нейтрального слоя, величины сгиба и уменьшения сгиба, т.к они будут браться из базы.

        ### Синтаксис Automation

        ```
        UnfoldType = iObject.UnfoldType;      # Получить свойство (* )
        iObject.UnfoldType = UnfoldType;      # Установить свойство (* )
        UnfoldType = iObject.GetUnfoldType;   # Получить свойство (**)
        iObject.SetUnfoldType( UnfoldType );  # Установить свойство (**)
        ```
        """
        ...
    @UnfoldType.setter
    def UnfoldType(self, value: int): ...

    def Depth(self, Normal) -> float:  # isheetmetalbody_depth.html
        """
        ## Depth - Глубина выдавливания

        Тип данных: double

        Позволяет считывать и устанавливать глубину выдавливания.

        ### Синтаксис Automation

        ```
        Depth = iObject.Depth( normal );     # Получить свойство (* )
        iObject.Depth( normal ) = Depth;     # Установить свойство (* )
        Depth = iObject.GetDepth( normal );  # Получить свойство (**)
        iObject.SetDepth( normal, Depth );   # Установить свойство (**)
        ```

        ### Входные параметры

        +--------------------------------------------------------------------------------------------------------------------------+
        | BOOL normal | - TRUE - глубина выдавливания в прямом направлении, - FALSE - глубина выдавливания в обратном направлении. |
        +--------------------------------------------------------------------------------------------------------------------------+
        """
        ...

    def DepthObject(self, Normal) -> IModelObject:  # isheetmetalbody_depthobject.html
        """
        ## DepthObject - Объект, задающий глубину выдавливания

        Тип данных: указатель на объект IModelObject

        Позволяет считывать и устанавливать указатель на объект, задающий глубину выдавливания.

        ### Синтаксис Automation

        ```
        DepthObject = iObject.DepthObject( normal );     # Получить свойство (* )
        iObject.DepthObject( normal ) = DepthObject;     # Установить свойство (* )
        DepthObject = iObject.GetDepthObject( normal );  # Получить свойство (**)
        iObject.SetDepthObject( normal, DepthObject );   # Установить свойство (**)
        ```

        ### Входные параметры

        +------------------------------------------------------------------------------------------------------------------------+
        | BOOL normal | - TRUE - объект выдавливания в прямом направлении, - FALSE - объект выдавливания в обратном направлении. |
        +------------------------------------------------------------------------------------------------------------------------+
        """
        ...

    def ExtrusionType(self, Normal) -> int:  # isheetmetalbody_extrusiontype.html
        """
        ## ExtrusionType - Тип выдавливания

        Тип данных: из перечисления ksEndTypeEnum

        Позволяет считывать и устанавливать тип выдавливания.

        ### Синтаксис Automation

        ```
        ExtrusionType = iObject.ExtrusionType( normal );     # Получить свойство (* )
        iObject.ExtrusionType( normal ) = ExtrusionType;     # Установить свойство (* )
        ExtrusionType = iObject.GetExtrusionType( normal );  # Получить свойство (**)
        iObject.SetExtrusionType( normal, ExtrusionType );   # Установить свойство (**)
        ```

        ### Входные параметры

        +------------------------------------------------------------------------------------------------------------------+
        | BOOL normal | - TRUE - тип выдавливания в прямом направлении, - FALSE - тип выдавливания в обратном направлении. |
        +------------------------------------------------------------------------------------------------------------------+
        """
        ...

    def GetSideParameters(self, Normal) -> bool:  # isheetmetalbody_getsideparameters.html
        """
        ## GetSideParameters - Получить параметры листового тела в одном направлении

        Параметр Depth в позволяет задать на сколько выдавить за объект, задающий глубину выдавливания, или какое расстояние оставить до объекта, задающего глубину выдавливания - в зависимости от типа построения (для типов построения "до поверхности" и "до вершины" см. перечисление ksEndTypeEnum).

        ### Синтаксис Automation

        ```
        BOOL GetSideParameters( BOOL normal,
        End_Type * ExtrusionType,
        double * Depth,
        IModelObject ** DepthObject );
        ```

        ### Входные параметры

        +--------------------------------------------------------------------------+
        | normal | - направление: TRUE - в прямом направлении, FALSE - в обратном. |
        +--------------------------------------------------------------------------+

        ### Выходные параметры

        +-----------------------------------------------------------------------+
        | ExtrusionType | - способ построения листового тела,                   |
        +-----------------------------------------------------------------------+
        | Depth         | - глубина выдавливания,                               |
        +-----------------------------------------------------------------------+
        | DepthObject   | - указатель на объект, задающий глубину выдавливания. |
        +-----------------------------------------------------------------------+

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def SetDepth(self, Normal, arg1):
        ...

    def SetDepthObject(self, Normal, arg1):
        ...

    def SetExtrusionType(self, Normal, arg1):
        ...

    def SetSideParameters(self, Normal, ExtrusionType, Depth, DepthObject) -> bool:  # isheetmetalbody_setsideparameters.html
        """
        ## SetSideParameters - Установить параметры листового тела в одном направлении

        Параметр Depth в позволяет задать на сколько выдавить за объект, задающий глубину выдавливания, или какое расстояние оставить до объекта, задающего глубину выдавливания - в зависимости от типа построения (для типов построения "до поверхности" и "до вершины" см. перечисление ksEndTypeEnum).

        ### Синтаксис Automation

        ```
        BOOL SetSideParameters( BOOL normal,
        End_Type ExtrusionType,
        double Depth,
        IModelObject * DepthObject );
        ```

        ### Входные параметры

        +---------------------------------------------------------------------------------+
        | normal        | - направление: TRUE - в прямом направлении, FALSE - в обратном, |
        +---------------------------------------------------------------------------------+
        | ExtrusionType | - способ построения листового тела,                             |
        +---------------------------------------------------------------------------------+
        | Depth         | - глубина выдавливания,                                         |
        +---------------------------------------------------------------------------------+
        | DepthObject   | - указатель на объект, задающий глубину выдавливания.           |
        +---------------------------------------------------------------------------------+

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...




class ISheetMetalBend(IModelObject):  # isheetmetalbend.html
    """
    ## Интерфейс ISheetMetalBend

    Интерфейс параметров сгиба.

    Интерфейс можно получить с помощью метода коллекции сгибов ISheetMetalBends::Add или свойства ISheetMetalBends::SheetMetalBend.
    """

    @property
    def Angle(self) -> float:  # isheetmetalbend_angle.html
        """
        ## Angle - Угол сгиба

        Тип данных: double

        Позволяет считывать и устанавливать угол сгиба.

        ### Синтаксис Automation

        ```
        Angle = iObject.Angle();     # Получить свойство (* )
        iObject.Angle() = Angle;     # Установить свойство (* )
        Angle = iObject.GetAngle();  # Получить свойство (**)
        iObject.SetAngle( Angle );   # Установить свойство (**)
        ```
        """
        ...
    @Angle.setter
    def Angle(self, value: float): ...

    @property
    def AngleType(self) -> bool:  # isheetmetalbend_angletype.html
        """
        ## AngleType - Способ задания угла - угол сгиба / дополняющий угол

        Тип данных: BOOL

        Значения свойства:

        +-------------------------------------------------------+
        | TRUE  | - угол интерпретируется как угол сгиба,       |
        +-------------------------------------------------------+
        | FALSE | - угол интерпретируется как дополняющий угол. |
        +-------------------------------------------------------+

        Позволяет считывать и устанавливать способ задания угла.

        ### Синтаксис Automation

        ```
        AngleType = iObject.AngleType();     # Получить свойство (* )
        iObject.AngleType() = AngleType;     # Установить свойство (* )
        AngleType = iObject.GetAngleType();  # Получить свойство (**)
        iObject.SetAngleType( AngleType );   # Установить свойство (**)
        ```
        """
        ...
    @AngleType.setter
    def AngleType(self, value: bool): ...

    @property
    def BendCoefficient(self) -> float:  # isheetmetalbend_bendcoefficient.html
        """
        ## BendCoefficient - Коэффициент нейтрального слоя

        Тип данных: double

        Чтобы свойство было доступно, необходимо установить тип расчета длины развёртки - коэффициент нейтрального слоя. Позволяет считывать и устанавливать коэффициент нейтрального слоя. Get - cвойство будет возвращать коэффициент нейтрального слоя базового листового тела пока не будет установлен коэффициент нейтрального слоя для сгиба.

        ### Синтаксис Automation

        ```
        BendCoefficient = iObject.BendCoefficient();     # Получить свойство (* )
        iObject.BendCoefficient() = BendCoefficient;     # Установить свойство (* )
        BendCoefficient = iObject.GetBendCoefficient();  # Получить свойство (**)
        iObject.SetBendCoefficient( BendCoefficient );   # Установить свойство (**)
        ```
        """
        ...
    @BendCoefficient.setter
    def BendCoefficient(self, value: float): ...

    @property
    def BendObject(self) -> IModelObject:  # isheetmetalbend_bendobject.html
        """
        ## BendObject - Ребро

        Тип данных: указатель на интерфейс IModelObject.

        Позволяет получать и устанавливать интерфейс опорного ребра.

        ### Синтаксис Automation

        ```
        BendObject = iObject.BendObject();     # Получить свойство (* )
        iObject.BendObject() = BendObject;     # Установить свойство (* )
        BendObject = iObject.GetBendObject();  # Получить свойство (**)
        iObject.SetBendObject( BendObject );   # Установить свойство (**)
        ```
        """
        ...
    @BendObject.setter
    def BendObject(self, value: IModelObject): ...

    @property
    def BendObjects(self) -> typing.Any:  # isheetmetalbend_bendobjects.html
        """
        ## BendObjects - Ребра

        Тип данных: VARIANT

        ### Синтаксис Automation

        ```
        BendObjects = Object.BendObjects;       # Получить свойство (* )
        Object.BendObjects = BendObjects;       # Установить свойство (* )
        BendObjects = Object.GetBendObjects();  # Получить свойство (**)
        Object.SetBendObjects( BendObjects );   # Установить свойство (**)
        ```
        """
        ...
    @BendObjects.setter
    def BendObjects(self, value: typing.Any): ...

    @property
    def BendReduction(self) -> float:  # isheetmetalbend_bendreduction.html
        """
        ## BendReduction - Уменьшение сгиба

        Тип данных: double

        Чтобы свойство было доступно, необходимо установить тип расчета длины развёртки - уменьшение сгиба. Позволяет считывать и устанавливать уменьшение сгиба. Get - cвойство будет возвращать уменьшение сгиба базового листового тела пока не будет установлено "уменьшение сгиба" для сгиба.

        ### Синтаксис Automation

        ```
        BendReduction = iObject.BendReduction();     # Получить свойство (* )
        iObject.BendReduction() = BendReduction;     # Установить свойство (* )
        BendReduction = iObject.GetBendReduction();  # Получить свойство (**)
        iObject.SetBendReduction( BendReduction );   # Установить свойство (**)
        ```
        """
        ...
    @BendReduction.setter
    def BendReduction(self, value: float): ...

    @property
    def BendRelease(self) -> int:  # isheetmetalbend_bendrelease.html
        """
        ## BendRelease - Тип освобождения cгиба

        Тип данных: из перечисления ksBendReleaseTypeEnum

        Позволяет считывать и устанавливать тип освобождения cгиба. Свойство доступно при включенной опции "использовать освобождение сгиба".

        ### Синтаксис Automation

        ```
        BendRelease = iObject.BendRelease();     # Получить свойство (* )
        iObject.BendRelease() = BendRelease;     # Установить свойство (* )
        BendRelease = iObject.GetBendRelease();  # Получить свойство (**)
        iObject.SetBendRelease( BendRelease );   # Установить свойство (**)
        ```
        """
        ...
    @BendRelease.setter
    def BendRelease(self, value: int): ...

    @property
    def BendTablePath(self) -> str:  # isheetmetalbend_bendtablepath.html
        """
        ## BendTablePath - Путь к таблице сгибов

        Тип данных: BSTR

        Свойство доступно только для чтения. Таблица сгибов одна на документ. Путь к таблице сгибов выбирается у базового листового тела.

        ### Синтаксис Automation

        ```
        BendTablePath = iObject.BendTablePath();     # Получить свойство (* )
        BendTablePath = iObject.GetBendTablePath();  # Получить свойство (**)
        ```
        """
        ...

    @property
    def BendValue(self) -> float:  # isheetmetalbend_bendvalue.html
        """
        ## BendValue - Величина сгиба

        Тип данных: double

        Чтобы свойство было доступно, необходимо установить тип расчета длины развёртки - величина сгиба. Позволяет считывать и устанавливать величину сгиба. Get - cвойство будет возвращать величину сгиба базового листового тела пока не будет установлена "величина сгиба" для сгиба.

        ### Синтаксис Automation

        ```
        BendValue = iObject.BendValue();     # Получить свойство (* )
        iObject.BendValue() = BendValue;     # Установить свойство (* )
        BendValue = iObject.GetBendValue();  # Получить свойство (**)
        iObject.SetBendValue( BendValue );   # Установить свойство (**)
        ```
        """
        ...
    @BendValue.setter
    def BendValue(self, value: float): ...

    @property
    def DeviationLeftSide(self) -> float:  # isheetmetalbend_deviationleftside.html
        """
        ## DeviationLeftSide - Угол на сгибе слева

        Тип данных: double

        Позволяет считывать и устанавливать угол на сгибе слева при способе построения левой стороны - "по уклону и углу слева".

        ### Синтаксис Automation

        ```
        DeviationLeftSide = iObject.DeviationLeftSide();     # Получить свойство (* )
        iObject.DeviationLeftSide() = DeviationLeftSide;     # Установить свойство (* )
        DeviationLeftSide = iObject.GetDeviationLeftSide();  # Получить свойство (**)
        iObject.SetDeviationLeftSide( DeviationLeftSide );   # Установить свойство (**)
        ```
        """
        ...
    @DeviationLeftSide.setter
    def DeviationLeftSide(self, value: float): ...

    @property
    def DeviationRightSide(self) -> float:  # isheetmetalbend_deviationrightside.html
        """
        ## DeviationRightSide - Угол на сгибе справа

        Тип данных: double

        Позволяет считывать и устанавливать угол на сгибе справа при способе построения правой стороны - "по уклону и углу справа".

        ### Синтаксис Automation

        ```
        DeviationRightSide = iObject.DeviationRightSide();     # Получить свойство (* )
        iObject.DeviationRightSide() = DeviationRightSide;     # Установить свойство (* )
        DeviationRightSide = iObject.GetDeviationRightSide();  # Получить свойство (**)
        iObject.SetDeviationRightSide( DeviationRightSide );   # Установить свойство (**)
        ```
        """
        ...
    @DeviationRightSide.setter
    def DeviationRightSide(self, value: float): ...

    @property
    def Direction(self) -> bool:  # isheetmetalbend_direction.html
        """
        ## Direction - Направление построения

        Тип данных: BOOL

        Позволяет установить и получить направление построения сгиба.

        ### Синтаксис Automation

        ```
        Direction = iObject.Direction();     # Получить свойство (* )
        iObject.Direction() = Direction;     # Установить свойство (* )
        Direction = iObject.GetDirection();  # Получить свойство (**)
        iObject.SetDirection( Direction );   # Установить свойство (**)
        ```
        """
        ...
    @Direction.setter
    def Direction(self, value: bool): ...

    @property
    def DismissalAngleType(self) -> int:  # isheetmetalbend_dismissalangletype.html
        """
        ## DismissalAngleType - Способ освобождения угла cгиба

        Тип данных: из перечисления ksBendAngleReleaseTypeEnum

        Позволяет считывать и устанавливать способ освобождения угла cгиба. Свойство доступно при включенной опции "использовать освобождение угла".

        ### Синтаксис Automation

        ```
        DismissalAngleType = iObject.DismissalAngleType();     # Получить свойство (* )
        iObject.DismissalAngleType() = DismissalAngleType;     # Установить свойство (* )
        DismissalAngleType = iObject.GetDismissalAngleType();  # Получить свойство (**)
        iObject.SetDismissalAngleType( DismissalAngleType );   # Установить свойство (**)
        ```
        """
        ...
    @DismissalAngleType.setter
    def DismissalAngleType(self, value: int): ...

    @property
    def DismissalDepth(self) -> float:  # isheetmetalbend_dismissaldepth.html
        """
        ## DismissalDepth - Глубина разгрузки сгиба

        Тип данных: double

        Позволяет считывать и устанавливать глубину разгрузки сгиба. Свойство доступно при включенной опции "использовать освобождение сгиба".

        ### Синтаксис Automation

        ```
        DismissalDepth = iObject.DismissalDepth();     # Получить свойство (* )
        iObject.DismissalDepth() = DismissalDepth;     # Установить свойство (* )
        DismissalDepth = iObject.GetDismissalDepth();  # Получить свойство (**)
        iObject.SetDismissalDepth( DismissalDepth );   # Установить свойство (**)
        ```
        """
        ...
    @DismissalDepth.setter
    def DismissalDepth(self, value: float): ...

    @property
    def DismissalWidth(self) -> float:  # isheetmetalbend_dismissalwidth.html
        """
        ## DismissalWidth - Ширина разгрузки сгиба

        Тип данных: double

        Позволяет считывать и устанавливать ширину разгрузки сгиба. Свойство доступно при включенной опции "использовать освобождение сгиба".

        ### Синтаксис Automation

        ```
        DismissalWidth = iObject.DismissalWidth();     # Получить свойство (* )
        iObject.DismissalWidth() = DismissalWidth;     # Установить свойство (* )
        DismissalWidth = iObject.GetDismissalWidth();  # Получить свойство (**)
        iObject.SetDismissalWidth( DismissalWidth );   # Установить свойство (**)
        ```
        """
        ...
    @DismissalWidth.setter
    def DismissalWidth(self, value: float): ...

    @property
    def DismissalWithWidth(self) -> bool:  # isheetmetalbend_dismissalwithwidth.html
        """
        ## DismissalWithWidth - Учитывать ширину

        Тип данных: BOOL

        Значения свойства:

        +-----------------------------+
        | TRUE  | - учитывать ширину, |
        +-----------------------------+
        | FALSE | - не учитывать.     |
        +-----------------------------+

        Позволяет считывать и устанавливать признак - "учитывать ширину". Свойство доступно при включенной опции "использовать освобождение сгиба".

        ### Синтаксис Automation

        ```
        DismissalWithWidth = iObject.DismissalWithWidth();     # Получить свойство (* )
        iObject.DismissalWithWidth() = DismissalWithWidth;     # Установить свойство (* )
        DismissalWithWidth = iObject.GetDismissalWithWidth();  # Получить свойство (**)
        iObject.SetDismissalWithWidth( DismissalWithWidth );   # Установить свойство (**)
        ```
        """
        ...
    @DismissalWithWidth.setter
    def DismissalWithWidth(self, value: bool): ...

    @property
    def Disposal(self) -> int:  # isheetmetalbend_disposal.html
        """
        ## Disposal - Тип размещения сгиба на ребре

        Тип данных: из перечисления ksBendDisposalEnum

        Позволяет установить и получить тип размещения сгиба на ребре.

        ### Синтаксис Automation

        ```
        Disposal = iObject.Disposal();     # Получить свойство (* )
        iObject.Disposal() = Disposal;     # Установить свойство (* )
        Disposal = iObject.GetDisposal();  # Получить свойство (**)
        iObject.SetDisposal( Disposal );   # Установить свойство (**)
        ```
        """
        ...
    @Disposal.setter
    def Disposal(self, value: int): ...

    @property
    def DistanceLeftSide(self) -> float:  # isheetmetalbend_distanceleftside.html
        """
        ## DistanceLeftSide - Отступ слева

        Тип данных: double

        Позволяет считывать и устанавливать отступ слева.

        ### Синтаксис Automation

        ```
        DistanceLeftSide = iObject.DistanceLeftSide();     # Получить свойство (* )
        iObject.DistanceLeftSide() = DistanceLeftSide;     # Установить свойство (* )
        DistanceLeftSide = iObject.GetDistanceLeftSide();  # Получить свойство (**)
        iObject.SetDistanceLeftSide( DistanceLeftSide );   # Установить свойство (**)
        ```
        """
        ...
    @DistanceLeftSide.setter
    def DistanceLeftSide(self, value: float): ...

    @property
    def DistanceRightSide(self) -> float:  # isheetmetalbend_distancerightside.html
        """
        ## DistanceRightSide - Отступ справа

        Тип данных: double

        Позволяет считывать и устанавливать отступ справа.

        ### Синтаксис Automation

        ```
        DistanceRightSide = iObject.DistanceRightSide();     # Получить свойство (* )
        iObject.DistanceRightSide() = DistanceRightSide;     # Установить свойство (* )
        DistanceRightSide = iObject.GetDistanceRightSide();  # Получить свойство (**)
        iObject.SetDistanceRightSide( DistanceRightSide );   # Установить свойство (**)
        ```
        """
        ...
    @DistanceRightSide.setter
    def DistanceRightSide(self, value: float): ...

    @property
    def InternalLength(self) -> bool:  # isheetmetalbend_internallength.html
        """
        ## InternalLength - Определение длины по внутреннему контуру сгиба / по касанию к сгибу

        Тип данных: BOOL

        Значения свойства:

        +-----------------------------------------+
        | TRUE  | - по внутреннему контуру сгиба, |
        +-----------------------------------------+
        | FALSE | - по касанию к сгибу.           |
        +-----------------------------------------+

        Позволяет считывать и устанавливать тип определения длины.

        ### Синтаксис Automation

        ```
        InternalLength = iObject.InternalLength();     # Получить свойство (* )
        iObject.InternalLength() = InternalLength;     # Установить свойство (* )
        InternalLength = iObject.GetInternalLength();  # Получить свойство (**)
        iObject.SetInternalLength( InternalLength );   # Установить свойство (**)
        ```
        """
        ...
    @InternalLength.setter
    def InternalLength(self, value: bool): ...

    @property
    def InternalRadius(self) -> bool:  # isheetmetalbend_internalradius.html
        """
        ## InternalRadius - Внутренний радиус

        Тип данных: BOOL

        Значения свойства:

        +------------------------------+
        | TRUE  | - внутренний радиус, |
        +------------------------------+
        | FALSE | - наружный радиус.   |
        +------------------------------+

        Позволяет получать и устанавливать признак "внутренний радиус".

        ### Синтаксис Automation

        ```
        InternalRadius = iObject.InternalRadius();     # Получить свойство (* )
        iObject.InternalRadius() = InternalRadius;     # Установить свойство (* )
        InternalRadius = iObject.GetInternalRadius();  # Получить свойство (**)
        iObject.SetInternalRadius( InternalRadius );   # Установить свойство (**)
        ```
        """
        ...
    @InternalRadius.setter
    def InternalRadius(self, value: bool): ...

    @property
    def LeftSideAngle(self) -> float:  # isheetmetalbend_leftsideangle.html
        """
        ## LeftSideAngle - Угол слева (уклон)

        Тип данных: double

        Позволяет считывать и устанавливать уклон слева при способе построения левой стороны - "по уклону и углу слева".

        ### Синтаксис Automation

        ```
        LeftSideAngle = iObject.LeftSideAngle();     # Получить свойство (* )
        iObject.LeftSideAngle() = LeftSideAngle;     # Установить свойство (* )
        LeftSideAngle = iObject.GetLeftSideAngle();  # Получить свойство (**)
        iObject.SetLeftSideAngle( LeftSideAngle );   # Установить свойство (**)
        ```
        """
        ...
    @LeftSideAngle.setter
    def LeftSideAngle(self, value: float): ...

    @property
    def LeftSideType(self) -> int:  # isheetmetalbend_leftsidetype.html
        """
        ## LeftSideType - Способ построения левой боковой стороны

        Тип данных: из перечисления ksBendSideTypeEnum

        Позволяет считывать и устанавливать способ построения левой боковой стороны.

        ### Синтаксис Automation

        ```
        LeftSideType = iObject.LeftSideType();     # Получить свойство (* )
        iObject.LeftSideType() = LeftSideType;     # Установить свойство (* )
        LeftSideType = iObject.GetLeftSideType();  # Получить свойство (**)
        iObject.SetLeftSideType( LeftSideType );   # Установить свойство (**)
        ```
        """
        ...
    @LeftSideType.setter
    def LeftSideType(self, value: int): ...

    @property
    def Length(self) -> float:  # isheetmetalbend_length.html
        """
        ## Length - Длина прямой части сгиба

        Тип данных: double

        Позволяет считывать и устанавливать длину прямой части сгиба.

        ### Синтаксис Automation

        ```
        Length = iObject.Length();     # Получить свойство (* )
        iObject.Length() = Length;     # Установить свойство (* )
        Length = iObject.GetLength();  # Получить свойство (**)
        iObject.SetLength( Length );   # Установить свойство (**)
        ```
        """
        ...
    @Length.setter
    def Length(self, value: float): ...

    @property
    def Length1(self) -> float:  # isheetmetalbend_length1.html
        """
        ## Length1 - Длина сгиба слева

        Тип данных: double

        Свойство используется, если установлен признак LengthBy2Sides.

        ### Синтаксис Automation

        ```
        Length1 = Object.Length1;       # Получить свойство (* )
        Object.Length1 = Length1;       # Установить свойство (* )
        Length1 = Object.GetLength1();  # Получить свойство (**)
        Object.SetLength1( Length1 );   # Установить свойство (**)
        ```
        """
        ...
    @Length1.setter
    def Length1(self, value: float): ...

    @property
    def Length2(self) -> float:  # isheetmetalbend_length2.html
        """
        ## Length2 - Длина сгиба справа

        Тип данных: double

        Свойство используется, если установлен признак LengthBy2Sides.

        ### Синтаксис Automation

        ```
        Length2 = Object.Length2;       # Получить свойство (* )
        Object.Length2 = Length2;       # Установить свойство (* )
        Length2 = Object.GetLength2();  # Получить свойство (**)
        Object.SetLength2( Length2 );   # Установить свойство (**)
        ```
        """
        ...
    @Length2.setter
    def Length2(self, value: float): ...

    @property
    def LengthBuildingType1(self) -> int:  # isheetmetalbend_lengthbuildingtype1.html
        """
        ## LengthBuildingType1 - Способ задания длины продолжения сгиба слева

        Тип данных: из перечисления ksLengthBuildingTypeEnum

        ### Синтаксис Automation

        ```
        LengthBuildingType1 = Object.LengthBuildingType1;       # Получить свойство (* )
        Object.LengthBuildingType1 = LengthBuildingType1;       # Установить свойство (* )
        LengthBuildingType1 = Object.GetLengthBuildingType1();  # Получить свойство (**)
        Object.SetLengthBuildingType1( LengthBuildingType1 );   # Установить свойство (**)
        ```
        """
        ...
    @LengthBuildingType1.setter
    def LengthBuildingType1(self, value: int): ...

    @property
    def LengthBuildingType2(self) -> int:  # isheetmetalbend_lengthbuildingtype2.html
        """
        ## LengthBuildingType2 - Способ задания длины продолжения сгиба справа

        Тип данных: из перечисления ksLengthBuildingTypeEnum

        ### Синтаксис Automation

        ```
        LengthBuildingType2 = Object.LengthBuildingType2;       # Получить свойство (* )
        Object.LengthBuildingType2 = LengthBuildingType2;       # Установить свойство (* )
        LengthBuildingType2 = Object.GetLengthBuildingType2();  # Получить свойство (**)
        Object.SetLengthBuildingType2( LengthBuildingType2 );   # Установить свойство (**)
        ```
        """
        ...
    @LengthBuildingType2.setter
    def LengthBuildingType2(self, value: int): ...

    @property
    def LengthBy2Sides(self) -> bool:  # isheetmetalbend_lengthby2sides.html
        """
        ## LengthBy2Sides - Задание длины по двум сторонам

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        LengthBy2Sides = Object.LengthBy2Sides;       # Получить свойство (* )
        Object.LengthBy2Sides = LengthBy2Sides;       # Установить свойство (* )
        LengthBy2Sides = Object.GetLengthBy2Sides();  # Получить свойство (**)
        Object.SetLengthBy2Sides( LengthBy2Sides );   # Установить свойство (**)
        ```
        """
        ...
    @LengthBy2Sides.setter
    def LengthBy2Sides(self, value: bool): ...

    @property
    def LengthObject1(self) -> IModelObject:  # isheetmetalbend_lengthobject1.html
        """
        ## LengthObject1 - Вершина или плоскость для задания левой длины

        Тип данных: Указатель на интерфейс IModelObject

        ### Синтаксис Automation

        ```
        LengthObject1 = Object.LengthObject1;       # Получить свойство (* )
        Object.LengthObject1 = LengthObject1;       # Установить свойство (* )
        LengthObject1 = Object.GetLengthObject1();  # Получить свойство (**)
        Object.SetLengthObject1( LengthObject1 ;    # Установить свойство (**)
        ```
        """
        ...
    @LengthObject1.setter
    def LengthObject1(self, value: IModelObject): ...

    @property
    def LengthObject2(self) -> IModelObject:  # isheetmetalbend_lengthobject2.html
        """
        ## LengthObject2 - Вершина или плоскость для задания правой длины

        Тип данных: Указатель на интерфейс IModelObject

        ### Синтаксис Automation

        ```
        LengthObject2 = Object.LengthObject2;       # Получить свойство (* )
        Object.LengthObject2 = LengthObject2;       # Установить свойство (* )
        LengthObject2 = Object.GetLengthObject2();  # Получить свойство (**)
        Object.SetLengthObject2( LengthObject2) ;   # Установить свойство (**)
        ```
        """
        ...
    @LengthObject2.setter
    def LengthObject2(self, value: IModelObject): ...

    @property
    def LengthType(self) -> int:  # isheetmetalbend_lengthtype.html
        """
        ## LengthType - Способ задания длины (прямой части) сгиба

        Тип данных: из перечисления ksBendLengthTypeEnum

        Позволяет считывать и устанавливать способ задания длины (прямой части) сгиба.

        ### Синтаксис Automation

        ```
        LengthType = iObject.LengthType();     # Получить свойство (* )
        iObject.LengthType() = LengthType;     # Установить свойство (* )
        LengthType = iObject.GetLengthType();  # Получить свойство (**)
        iObject.SetLengthType( LengthType );   # Установить свойство (**)
        ```
        """
        ...
    @LengthType.setter
    def LengthType(self, value: int): ...

    @property
    def LengthType1(self) -> int:  # isheetmetalbend_lengthtype1.html
        """
        ## LengthType1 - Способ задания длины

        Тип данных: из перечисления ksBendLengthTypeEnum

        ### Синтаксис Automation

        ```
        LengthType1 = Object.LengthType1;       # Получить свойство (* )
        Object.LengthType1 = LengthType1;       # Установить свойство (* )
        LengthType1 = Object.GetLengthType1();  # Получить свойство (**)
        Object.SetLengthType1( LengthType1 );   # Установить свойство (**)
        ```
        """
        ...
    @LengthType1.setter
    def LengthType1(self, value: int): ...

    @property
    def LengthType2(self) -> int:  # isheetmetalbend_lengthtype2.html
        """
        ## LengthType2 - Способ задания длины

        Тип данных: из перечисления ksBendLengthTypeEnum

        ### Синтаксис Automation

        ```
        LengthType2 = Object.LengthType2;       # Получить свойство (* )
        Object.LengthType2 = LengthType2;       # Установить свойство (* )
        LengthType2 = Object.GetLengthType2();  # Получить свойство (**)
        Object.SetLengthType2( LengthType2 );   # Установить свойство (**)
        ```
        """
        ...
    @LengthType2.setter
    def LengthType2(self, value: int): ...

    @property
    def Offset(self) -> float:  # isheetmetalbend_offset.html
        """
        ## Offset - Смещение сгиба

        Тип данных: double

        Позволяет считывать и устанавливать смещение сгиба относительно листового тела.

        ### Синтаксис Automation

        ```
        Offset = iObject.Offset();     # Получить свойство (* )
        iObject.Offset() = Offset;     # Установить свойство (* )
        Offset = iObject.GetOffset();  # Получить свойство (**)
        iObject.SetOffset( Offset );   # Установить свойство (**)
        ```
        """
        ...
    @Offset.setter
    def Offset(self, value: float): ...

    @property
    def OffsetDirection1(self) -> int:  # isheetmetalbend_offsetdirection1.html
        """
        ## OffsetDirection1 - Направление смещения от объекта при задании левой длины по вершине или поверхности

        Тип данных: из перечисления ksDirectionTypeEnum

        ### Синтаксис Automation

        ```
        OffsetDirection1 = Object.OffsetDirection1;       # Получить свойство (* )
        Object.OffsetDirection1 = OffsetDirection1;       # Установить свойство (* )
        OffsetDirection1 = Object.GetOffsetDirection1();  # Получить свойство (**)
        Object.SetOffsetDirection1( OffsetDirection1 )    # Установить свойство (**)
        ```
        """
        ...
    @OffsetDirection1.setter
    def OffsetDirection1(self, value: int): ...

    @property
    def OffsetDirection2(self) -> int:  # isheetmetalbend_offsetdirection2.html
        """
        ## OffsetDirection2 - Направление смещения от объекта при задании правой длины по вершине или поверхности

        Тип данных: из перечисления ksDirectionTypeEnum

        ### Синтаксис Automation

        ```
        OffsetDirection2 = Object.OffsetDirection2;       # Получить свойство (* )
        Object.OffsetDirection2 = OffsetDirection2;       # Установить свойство (* )
        OffsetDirection2 = Object.GetOffsetDirection2();  # Получить свойство (**)
        Object.SetOffsetDirection2( OffsetDirection2 )    # Установить свойство (**)
        ```
        """
        ...
    @OffsetDirection2.setter
    def OffsetDirection2(self, value: int): ...

    @property
    def OffsetFromLengthObject1(self) -> float:  # isheetmetalbend_offsetfromlengthobject1.html
        """
        ## OffsetFromLengthObject1 - Смещение от объекта при задании левой длины по вершине или поверхности

        Тип данных: double

        ### Синтаксис Automation

        ```
        OffsetFromLengthObject1 = Object.OffsetFromLengthObject1;  # Получить свойство (* )
        Object.OffsetFromLengthObject1 = OffsetFromLengthObject1;  # Установить свойство (* )
        OffsetDirection1 = Object.GetOffsetDirection1();           # Получить свойство (**)
        Object.SetOffsetDirection1( OffsetDirection1 );            # Установить свойство (**)
        ```
        """
        ...
    @OffsetFromLengthObject1.setter
    def OffsetFromLengthObject1(self, value: float): ...

    @property
    def OffsetFromLengthObject2(self) -> float:  # isheetmetalbend_offsetfromlengthobject2.html
        """
        ## OffsetFromLengthObject2 - Смещение от объекта при задании правой длины по вершине или поверхности

        Тип данных: double

        ### Синтаксис Automation

        ```
        OffsetFromLengthObject2 = Object.OffsetFromLengthObject2;  # Получить свойство (* )
        Object.OffsetFromLengthObject2 = OffsetFromLengthObject2;  # Установить свойство (* )
        OffsetDirection2 = Object.GetOffsetDirection2();           # Получить свойство (**)
        Object.SetOffsetDirection1( OffsetDirection2 );            # Установить свойство (**)
        ```
        """
        ...
    @OffsetFromLengthObject2.setter
    def OffsetFromLengthObject2(self, value: float): ...

    @property
    def OffsetType(self) -> int:  # isheetmetalbend_offsettype.html
        """
        ## OffsetType - Тип смещения

        Тип данных: из перечисления ksBendOffsetTypeEnum

        Позволяет считывать и устанавливать тип смещения сгиба относительно листового тела.

        ### Синтаксис Automation

        ```
        OffsetType = iObject.OffsetType();     # Получить свойство (* )
        iObject.OffsetType() = OffsetType;     # Установить свойство (* )
        OffsetType = iObject.GetOffsetType();  # Получить свойство (**)
        iObject.SetOffsetType( OffsetType );   # Установить свойство (**)
        ```
        """
        ...
    @OffsetType.setter
    def OffsetType(self, value: int): ...

    @property
    def Radius(self) -> float:  # isheetmetalbend_radius.html
        """
        ## Radius - Внутренний радиус

        Тип данных: double

        Позволяет получать и устанавливать радиус сгиба.

        ### Синтаксис Automation

        ```
        Radius = iObject.Radius();     # Получить свойство (* )
        iObject.Radius() = Radius;     # Установить свойство (* )
        Radius = iObject.GetRadius();  # Получить свойство (**)
        iObject.SetRadius( Radius );   # Установить свойство (**)
        ```
        """
        ...
    @Radius.setter
    def Radius(self, value: float): ...

    @property
    def RightSideAngle(self) -> float:  # isheetmetalbend_rightsideangle.html
        """
        ## RightSideAngle - Угол справа (уклон)

        Тип данных: double

        Позволяет считывать и устанавливать уклон справа при способе построения правой стороны - "по уклону и углу справа".

        ### Синтаксис Automation

        ```
        RightSideAngle = iObject.RightSideAngle();     # Получить свойство (* )
        iObject.RightSideAngle() = RightSideAngle;     # Установить свойство (* )
        RightSideAngle = iObject.GetRightSideAngle();  # Получить свойство (**)
        iObject.SetRightSideAngle( RightSideAngle );   # Установить свойство (**)
        ```
        """
        ...
    @RightSideAngle.setter
    def RightSideAngle(self, value: float): ...

    @property
    def RightSideType(self) -> int:  # isheetmetalbend_rightsidetype.html
        """
        ## RightSideType - Способ построения правой боковой стороны

        Тип данных: из перечисления ksBendSideTypeEnum

        Позволяет считывать и устанавливать способ построения правой боковой стороны.

        ### Синтаксис Automation

        ```
        RightSideType = iObject.RightSideType();     # Получить свойство (* )
        iObject.RightSideType() = RightSideType;     # Установить свойство (* )
        RightSideType = iObject.GetRightSideType();  # Получить свойство (**)
        iObject.SetRightSideType( RightSideType );   # Установить свойство (**)
        ```
        """
        ...
    @RightSideType.setter
    def RightSideType(self, value: int): ...

    @property
    def Straighten(self) -> bool:  # isheetmetalbend_straighten.html
        """
        ## Straighten - Разогнуть сгиб

        Тип данных: BOOL

        Значения свойства:

        +--------------------------+
        | TRUE  | - сгиб разогнут, |
        +--------------------------+
        | FALSE | - сгиб согнут.   |
        +--------------------------+

        Позволяет сгибать и разгибать сгиб.

        ### Синтаксис Automation

        ```
        Straighten = iObject.Straighten();     # Получить свойство (* )
        iObject.Straighten() = Straighten;     # Установить свойство (* )
        Straighten = iObject.GetStraighten();  # Получить свойство (**)
        iObject.SetStraighten( Straighten );   # Установить свойство (**)
        ```
        """
        ...
    @Straighten.setter
    def Straighten(self, value: bool): ...

    @property
    def UnfoldType(self) -> int:  # isheetmetalbend_unfoldtype.html
        """
        ## UnfoldType - Способ определения длины развертки

        Тип данных: из перечисления ksUnfoldTypeEnum

        Позволяет считывать и устанавливать способ определения длины развертки. Если задан способ определения длины развертки - из таблицы, то будут недоступны свойства установки (Set) для коэффициента нейтрального слоя, величины сгиба и уменьшения сгиба, так как они будут браться из базы.

        ### Синтаксис Automation

        ```
        UnfoldType = iObject.UnfoldType();     # Получить свойство (* )
        iObject.UnfoldType() = UnfoldType;     # Установить свойство (* )
        UnfoldType = iObject.GetUnfoldType();  # Получить свойство (**)
        iObject.SetUnfoldType( UnfoldType );   # Установить свойство (**)
        ```
        """
        ...
    @UnfoldType.setter
    def UnfoldType(self, value: int): ...

    @property
    def WideningLeftSide(self) -> float:  # isheetmetalbend_wideningleftside.html
        """
        ## WideningLeftSide - Расширение слева

        Тип данных: double

        Позволяет считывать и устанавливать расширение слева при способе построения левой стороны - "расширение сгиба слева".

        ### Синтаксис Automation

        ```
        WideningLeftSide = iObject.WideningLeftSide();     # Получить свойство (* )
        iObject.WideningLeftSide() = WideningLeftSide;     # Установить свойство (* )
        WideningLeftSide = iObject.GetWideningLeftSide();  # Получить свойство (**)
        iObject.SetWideningLeftSide( WideningLeftSide );   # Установить свойство (**)
        ```
        """
        ...
    @WideningLeftSide.setter
    def WideningLeftSide(self, value: float): ...

    @property
    def WideningRightSide(self) -> float:  # isheetmetalbend_wideningrightside.html
        """
        ## WideningRightSide - Расширение справа

        Тип данных: double

        Позволяет считывать и устанавливать расширение справа при способе построения правой стороны - "расширение сгиба справа".

        ### Синтаксис Automation

        ```
        WideningRightSide = iObject.WideningRightSide();     # Получить свойство (* )
        iObject.WideningRightSide() = WideningRightSide;     # Установить свойство (* )
        WideningRightSide = iObject.GetWideningRightSide();  # Получить свойство (**)
        iObject.SetWideningRightSide( WideningRightSide );   # Установить свойство (**)
        ```
        """
        ...
    @WideningRightSide.setter
    def WideningRightSide(self, value: float): ...

    @property
    def Width(self) -> float:  # isheetmetalbend_width.html
        """
        ## Width - Ширина сгиба

        Тип данных: double

        Позволяет получить и установить ширину сгиба.

        ### Синтаксис Automation

        ```
        Width = iObject.Width();     # Получить свойство (* )
        iObject.Width() = Width;     # Установить свойство (* )
        Width = iObject.GetWidth();  # Получить свойство (**)
        iObject.SetWidth( Width );   # Установить свойство (**)
        ```
        """
        ...
    @Width.setter
    def Width(self, value: float): ...

    @property
    def WithoutAngleRelease(self) -> bool:  # isheetmetalbend_withoutanglerelease.html
        """
        ## WithoutAngleRelease - Без освобождения углов

        Тип данных: BOOL

        Значения свойства:

        +-----------------------------------+
        | TRUE  | - без освобождения углов, |
        +-----------------------------------+
        | FALSE | - с освобождением.        |
        +-----------------------------------+

        Позволяет считывать и устанавливать признак - "без освобождения углов".

        ### Синтаксис Automation

        ```
        WithoutAngleRelease = iObject.WithoutAngleRelease();     # Получить свойство (* )
        iObject.WithoutAngleRelease() = WithoutAngleRelease;     # Установить свойство (* )
        WithoutAngleRelease = iObject.GetWithoutAngleRelease();  # Получить свойство (**)
        iObject.SetWithoutAngleRelease( WithoutAngleRelease );   # Установить свойство (**)
        ```
        """
        ...
    @WithoutAngleRelease.setter
    def WithoutAngleRelease(self, value: bool): ...

    @property
    def WithoutBendRelease(self) -> bool:  # isheetmetalbend_withoutbendrelease.html
        """
        ## WithoutBendRelease - Без освобождения cгиба

        Тип данных: BOOL

        Значения свойства:

        +-----------------------------------+
        | TRUE  | - без освобождения сгиба, |
        +-----------------------------------+
        | FALSE | - с освобождением.        |
        +-----------------------------------+

        Позволяет считывать и устанавливать признак - "без освобождения сгиба".

        ### Синтаксис Automation

        ```
        WithoutBendRelease = iObject.WithoutBendRelease();     # Получить свойство (* )
        iObject.WithoutBendRelease() = WithoutBendRelease;     # Установить свойство (* )
        WithoutBendRelease = iObject.GetWithoutBendRelease();  # Получить свойство (**)
        iObject.SetWithoutBendRelease( WithoutBendRelease );   # Установить свойство (**)
        ```
        """
        ...
    @WithoutBendRelease.setter
    def WithoutBendRelease(self, value: bool): ...




class ISheetMetalBendedStraighten(IModelObject):  # isheetmetalbendedstraighten.html
    """
    ## Интерфейс ISheetMetalBendedStraighten

    Интерфейс операции Согнуть/Разогнуть.
    """

    @property
    def BendObjects(self) -> typing.Any:  # isheetmetalbendedstraighten_bendobjects.html
        """
        ## BendObjects - Сгибы. Массив объектов в виде SAFEARRAY DISPATCH - VT_ARRAY | VT_DISPATCH

        Тип данных: VARIANT

        ### Синтаксис Automation

        ```
        BendObjects = Object.BendObjects;       # Получить свойство (* )
        Object.BendObjects = BendObjects;       # Установить свойство (* )
        BendObjects = Object.GetBendObjects();  # Получить свойство (**)
        Object.SetBendObjects( BendObjects );   # Установить свойство (**)
        ```
        """
        ...
    @BendObjects.setter
    def BendObjects(self, value: typing.Any): ...

    @property
    def FixedFace(self) -> IModelObject:  # isheetmetalbendedstraighten_fixedface.html
        """
        ## FixedFace - Неподвижная грань

        Тип данных: Указатель на интерфейс IModelObject.

        ### Синтаксис Automation

        ```
        FixedFace = Object.FixedFace;       # Получить свойство (* )
        Object.FixedFace = FixedFace;       # Установить свойство (* )
        FixedFace = Object.GetFixedFace();  # Получить свойство (**)
        Object.SetFixedFace( FixedFace );   # Установить свойство (**)
        ```
        """
        ...
    @FixedFace.setter
    def FixedFace(self, value: IModelObject): ...

    @property
    def FoldLinesEnabled(self) -> bool:  # isheetmetalbendedstraighten_foldlinesenabled.html
        """
        ## FoldLinesEnabled - Отображения линий сгиба

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        FoldLinesEnabled = Object.FoldLinesEnabled       # Получить свойство (* )
        Object.FoldLinesEnabled = FoldLinesEnabled       # Установить свойство (* )
        FoldLinesEnabled = Object.GetFoldLinesEnabled()  # Получить свойство (**)
        Object.SetFoldLinesEnabled( FoldLinesEnabled )   # Установить свойство (**)
        ```
        """
        ...
    @FoldLinesEnabled.setter
    def FoldLinesEnabled(self, value: bool): ...

    @property
    def FoldLinesStyle(self) -> int:  # isheetmetalbendedstraighten_foldlinesstyle.html
        """
        ## FoldLinesStyle - Стиль линии

        Тип данных: из перечисления ksCurveStyleEnum.

        ### Синтаксис Automation

        ```
        FoldLinesStyle = Object.FoldLinesStyle       # Получить свойство (* )
        Object.FoldLinesStyle = FoldLinesStyle       # Установить свойство (* )
        FoldLinesStyle = Object.GetFoldLinesStyle()  # Получить свойство (**)
        Object.SetFoldLinesStyle( FoldLinesStyle )   # Установить свойство (**)
        ```
        """
        ...
    @FoldLinesStyle.setter
    def FoldLinesStyle(self, value: int): ...




class ISheetMetalBendedStraightens(IModelObjects):  # isheetmetalbendedstraightens.html
    """
    ## Интерфейс ISheetMetalBendedStraightens

    Интерфейс коллекции элементов “Согнуть/Разогнуть”
    """

    def Add(self, Type) -> ISheetMetalBendedStraighten:  # isheetmetalbendedstraightens_add.html
        """
        ## Add - Создает новый элемент и добавляет его в коллекцию

        1. Метод позволяет создать новый интерфейс операций Согнуть / Разогнуть.

        2. Допустимыми значениями Type являются o3d_sheetMetalBendStraighten (Согнуть), o3d_sheetMetalBendBended (Разогнуть)

        3. После получения нового интерфейса нужно задать параметры операции и вызвать метод IModelObject::Update.

        ### Синтаксис Automation

        ```
        LPDISPATCH Add( ksObj3dTypeEnum Type );
        ```

        ### Входные параметры

        +-------------------------------------------------------+
        | Type | - тип объекта из перечисления ksObj3dTypeEnum. |
        +-------------------------------------------------------+

        ### Возвращаемое значение

        - указатель на интерфейс ISheetMetalBendedStraighten.
        """
        ...

    def SheetMetalBendedStraighten(self, Index) -> ISheetMetalBendedStraighten:  # isheetmetalbendedstraightens_sheetmetalbendedstraighten.html
        """
        ## SheetMetalBendedStraighten - Возвращает элемент, заданный по индексу

        Тип данных: Указатель на интерфейс ISheetMetalBendedStraighten.

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        SheetMetalBendedStraighten = Object.SheetMetalBendedStraighten( Index )     # Получить свойство (* )
        SheetMetalBendedStraighten = Object.GetSheetMetalBendedStraighten( Index )  # Получить свойство (**)
        ```

        ### Входные параметры

        +--------------------------------------------+
        | VARIANT index | - индекс или имя элемента. |
        +--------------------------------------------+
        """
        ...




class ISheetMetalBendUnfoldParameters(IDispatch):  # isheetmetalbendunfoldparameters.html
    """
    ## Интерфейс ISheetMetalBendUnfoldParameters

    Интерфейс параметров развертки листового тела.
    """

    @property
    def ExcludedBendObjects(self) -> typing.Any:  # isheetmetalbendunfoldparameters_excludedbendobjects.html
        """
        ## ExcludedBendObjects - Исключенные сгибы. Массив объектов в виде SAFEARRAY DISPATCH - VT_ARRAY | VT_DISPATCH

        Тип данных: VARIANT

        ### Синтаксис Automation

        ```
        ExcludedBendObjects = Object.ExcludedBendObjects;       # Получить свойство (* )
        Object.ExcludedBendObjects = ExcludedBendObjects;       # Установить свойство (* )
        ExcludedBendObjects = Object.GetExcludedBendObjects();  # Получить свойство (**)
        Object.SetExcludedBendObjects( ExcludedBendObjects );   # Установить свойство (**)
        ```
        """
        ...
    @ExcludedBendObjects.setter
    def ExcludedBendObjects(self, value: typing.Any): ...

    @property
    def FixedFaces(self) -> typing.Any:  # isheetmetalbendunfoldparameters_fixedfaces.html
        """
        ## FixedFaces - Неподвижные грани

        Тип данных: VARIANT

        ### Синтаксис Automation

        ```
        FixedFaces = Object.FixedFaces;       # Получить свойство (* )
        Object.FixedFaces = FixedFaces;       # Установить свойство (* )
        FixedFaces = Object.GetFixedFaces();  # Получить свойство (**)
        Object.SetFixedFaces( FixedFaces );   # Установить свойство (**)
        ```
        """
        ...
    @FixedFaces.setter
    def FixedFaces(self, value: typing.Any): ...

    @property
    def IsCreated(self) -> bool:  # isheetmetalbendunfoldparameters_iscreated.html
        """
        ## IsCreated - Заданы ли параметры развертки

        Тип данных: BOOL

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        IsCreated = Object.IsCreated       # Получить свойство (* )
        IsCreated = Object.GetIsCreated()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def Unfold(self) -> bool:  # isheetmetalbendunfoldparameters_unfold.html
        """
        ## Unfold - Развернуть

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        Unfold = Object.Unfold;       # Получить свойство (* )
        Object.Unfold = Unfold;       # Установить свойство (* )
        Unfold = Object.GetUnfold();  # Получить свойство (**)
        Object.SetUnfold( Unfold );   # Установить свойство (**)
        ```
        """
        ...
    @Unfold.setter
    def Unfold(self, value: bool): ...

    @property
    def UnfoldPlane(self) -> IModelObject:  # isheetmetalbendunfoldparameters_unfoldplane.html
        """
        ## UnfoldPlane - Плоскость развертки

        Тип данных: Указатель на интерфейс IModelObject.

        ### Синтаксис Automation

        ```
        UnfoldPlane = Object.UnfoldPlane;        # Получить свойство (* )
        Object.UnfoldPlane = UnfoldPlane;        # Установить свойство (* )
        UnfoldPlane = Object.GetUnfoldPlane());  # Получить свойство (**)
        Object.SetUnfoldPlane( UnfoldPlane );    # Установить свойство (**)
        ```
        """
        ...
    @UnfoldPlane.setter
    def UnfoldPlane(self, value: IModelObject): ...

    def DeleteParam(self) -> bool:  # isheetmetalbendunfoldparameters_deleteparam.html
        """
        ## DeleteParam - Удалить параметры развертки

        ### Синтаксис Automation

        ```
        BOOL DeleteParam();
        ```

        ### Возвращаемое значение

        +-----------------------------------------+
        | TRUE | - в случае успешного завершения. |
        +-----------------------------------------+
        """
        ...

    def UpdateParam(self) -> bool:  # isheetmetalbendunfoldparameters_updateparam.html
        """
        ## UpdateParam - Установить измененные параметры развертки

        ### Синтаксис Automation

        ```
        BOOL UpdateParam();
        ```

        ### Возвращаемое значение

        +-----------------------------------------+
        | TRUE | - в случае успешного завершения. |
        +-----------------------------------------+
        """
        ...




class ISheetMetalClosedCorners(IModelObjects):  # isheetmetalclosedcorners.html
    """
    ## Интерфейс ISheetMetalClosedCorners

    Интерфейс коллекции элементов Замыкание углов.
    """

    def Add(self) -> ISheetMetalClosedCorner:  # isheetmetalclosedcorners_add.html
        """
        ## Add - Создает новый элемент и добавляет его в коллекцию

        1. Метод позволяет создать новый интерфейс операции Замыкание углов.

        2. После получения нового интерфейса нужно задать параметры операции и вызвать метод IModelObject::Update.

        ### Синтаксис Automation

        ```
        LPDISPATCH Add();
        ```

        ### Возвращаемое значение

        - указатель на интерфейс ISheetMetalClosedCorner.
        """
        ...

    def SheetMetalClosedCorner(self, Index) -> ISheetMetalClosedCorner:  # isheetmetalclosedcorners_sheetmetalclosedcorner.html
        """
        ## SheetMetalClosedCorner - Возвращает элемент, заданный по индексу

        Тип данных: Указатель на интерфейс ISheetMetalClosedCorner.

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        SheetMetalClosedCorner = Object.SheetMetalClosedCorner( Index )     # Получить свойство (* )
        SheetMetalClosedCorner = Object.GetSheetMetalClosedCorner( Index )  # Получить свойство (**)
        ```

        ### Входные параметры

        +--------------------------------------------+
        | VARIANT Index | - индекс или имя элемента. |
        +--------------------------------------------+
        """
        ...




class ISheetMetalClosedCorner(IModelObject):  # isheetmetalclosedcorner.html
    """
    ## Интерфейс ISheetMetalClosedCorner

    Интерфейс операции Замыкание углов.
    """

    @property
    def CornersCount(self) -> int:  # isheetmetalclosedcorner_cornerscount.html
        """
        ## CornersCount - Количество углов

        Тип данных: long

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        CornersCount = Object.CornersCount       # Получить свойство (* )
        CornersCount = Object.GetCornersCount()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def CornersObjects(self) -> typing.Any:  # isheetmetalclosedcorner_cornersobjects.html
        """
        ## CornersObjects - Углы. Массив объектов в виде SAFEARRAY DISPATCH - VT_ARRAY | VT_DISPATCH

        Тип данных: VARIANT

        ### Синтаксис Automation

        ```
        CornersObjects = Object.CornersObjects;       # Получить свойство (* )
        Object.CornersObjects = CornersObjects;       # Установить свойство (* )
        CornersObjects = Object.GetCornersObjects();  # Получить свойство (**)
        Object.SetCornersObjects( CornersObjects );   # Установить свойство (**)
        ```
        """
        ...
    @CornersObjects.setter
    def CornersObjects(self, value: typing.Any): ...

    @property
    def DefailtParametersIndex(self) -> int:  # isheetmetalclosedcorner_defailtparametersindex.html
        """
        ## DefailtParametersIndex - Единые параметры. Индекс угла, задающий общие параметры

        Тип данных: long

        ### Синтаксис Automation

        ```
        DefailtParametersIndex = Object.DefailtParametersIndex;       # Получить свойство (* )
        Object.DefailtParametersIndex = DefailtParametersIndex ;      # Установить свойство (* )
        DefailtParametersIndex = Object.GetDefailtParametersIndex();  # Получить свойство (**)
        Object.SetDefailtParametersIndex( DefailtParametersIndex );   # Установить свойство (**)
        ```
        """
        ...
    @DefailtParametersIndex.setter
    def DefailtParametersIndex(self, value: int): ...

    def AddCornerObject(self, NewVal) -> bool:  # isheetmetalclosedcorner_addcornerobject.html
        """
        ## AddCornerObject - Добавить угол сгиба

        ### Синтаксис Automation

        ```
        BOOL AddCornerObject( IModelObject * NewVal );
        ```

        ### Входные параметры

        +------------------------------------------------------+
        | NewVal | - указатель на объект, задающий угол сгиба. |
        +------------------------------------------------------+

        ### Возвращаемое значение

        +-----------------------------------------+
        | TRUE | - в случае успешного завершения. |
        +-----------------------------------------+
        """
        ...

    def ClearCornerObjects(self) -> bool:  # isheetmetalclosedcorner_clearcornerobjects.html
        """
        ## ClearCornerObjects - Удалить все углы

        ### Синтаксис Automation

        ```
        BOOL ClearCornerObjects();
        ```

        ### Возвращаемое значение

        +-----------------------------------------+
        | TRUE | - в случае успешного завершения. |
        +-----------------------------------------+
        """
        ...

    def ClosingClosedType(self, Index) -> int:  # isheetmetalclosedcorner_closingclosedtype.html
        """
        ## ClosingClosedType - Способ замыкания углов

        Тип данных: Значение из перечисления ksClosingClosedTypeEnum.

        ### Синтаксис Automation

        ```
        ClosingClosedType = Object.ClosingClosedType( Index );     # Получить свойство (* )
        Object.ClosingClosedType( Index ) = ClosingClosedType;     # Установить свойство (* )
        ClosingClosedType = Object.GetClosingClosedType( Index );  # Получить свойство (**)
        Object.SetClosingClosedType( Index, ClosingClosedType );   # Установить свойство (**)
        ```

        ### Входные параметры

        +------------------------------+
        | long Index | - индекс сгиба. |
        +------------------------------+
        """
        ...

    def ClosingContinue(self, Index) -> bool:  # isheetmetalclosedcorner_closingcontinue.html
        """
        ## ClosingContinue - Продолжать замыкание прилегающих парных сгибов

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        ClosingContinue = Object.ClosingContinue( Index );     # Получить свойство (* )
        Object.ClosingContinue( Index ) = ClosingContinue;     # Установить свойство (* )
        ClosingContinue = Object.GetClosingContinue( Index );  # Получить свойство (**)
        Object.SetClosingContinue( Index, ClosingContinue );   # Установить свойство (**)
        ```

        ### Входные параметры

        +------------------------------+
        | long Index | - индекс сгиба. |
        +------------------------------+
        """
        ...

    def ClosingCorneringType(self, Index) -> int:  # isheetmetalclosedcorner_closingcorneringtype.html
        """
        ## ClosingCorneringType - Обработка угла при замыкании

        Тип данных: Значение из перечисления ksClosingCorneringEnum.

        ### Синтаксис Automation

        ```
        ClosingCorneringType = Object.ClosingCorneringType( Index );    # Получить свойство (* )
        Object.ClosingCorneringType( Index ) = ClosingCorneringType;    # Установить свойство (* )
        ClosingCorneringType = Object.GetClosingCorneringType( Index);  # Получить свойство (**)
        Object.SetClosingCorneringType( Index, ClosingCorneringType );  # Установить свойство (**)
        ```

        ### Входные параметры

        +------------------------------+
        | long Index | - индекс сгиба. |
        +------------------------------+
        """
        ...

    def ClosingDirection(self, Index) -> bool:  # isheetmetalclosedcorner_closingdirection.html
        """
        ## ClosingDirection - Направление. Переставить сторону

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        ClosingDirection = Object.ClosingDirection( Index );     # Получить свойство (* )
        Object.ClosingDirection( Index ) = ClosingDirection;     # Установить свойство (* )
        ClosingDirection = Object.GetClosingDirection( Index );  # Получить свойство (**)
        Object.SetClosingDirection( Index, ClosingDirection );   # Установить свойство (**)
        ```

        ### Входные параметры

        +------------------------------+
        | long Index | - индекс сгиба. |
        +------------------------------+
        """
        ...

    def ClosingGapValue(self, Index) -> float:  # isheetmetalclosedcorner_closinggapvalue.html
        """
        ## ClosingGapValue - Значение зазора при замыкании углов

        Тип данных: double

        ### Синтаксис Automation

        ```
        ClosingGapValue = Object.ClosingGapValue( Index );     # Получить свойство (* )
        Object.ClosingGapValue( Index ) = ClosingGapValue;     # Установить свойство (* )
        ClosingGapValue = Object.GetClosingGapValue( Index );  # Получить свойство (**)
        Object.SetClosingGapValue( Index, ClosingGapValue );   # Установить свойство (**)
        ```

        ### Входные параметры

        +------------------------------+
        | long Index | - индекс сгиба. |
        +------------------------------+
        """
        ...

    def ClosingHoleDiameter(self, Index) -> float:  # isheetmetalclosedcorner_closingholediameter.html
        """
        ## ClosingHoleDiameter - Диаметр отверстия при замыкании угла

        Тип данных: double

        ### Синтаксис Automation

        ```
        ClosingHoleDiameter = Object.ClosingHoleDiameter( Index );     # Получить свойство (* )
        Object.ClosingHoleDiameter( Index ) = ClosingHoleDiameter;     # Установить свойство (* )
        ClosingHoleDiameter = Object.GetClosingHoleDiameter( Index );  # Получить свойство (**)
        Object.SetClosingHoleDiameter( Index, ClosingHoleDiameter );   # Установить свойство (**)
        ```

        ### Входные параметры

        +------------------------------+
        | long Index | - индекс сгиба. |
        +------------------------------+
        """
        ...

    def ClosingHoleOffset(self, Index) -> float:  # isheetmetalclosedcorner_closingholeoffset.html
        """
        ## ClosingHoleOffset - Смещение отверстия при замыкании угла

        Тип данных: double

        ### Синтаксис Automation

        ```
        ClosingHoleOffset = Object.ClosingHoleOffset( Index );     # Получить свойство (* )
        Object.ClosingHoleOffset( Index ) = ClosingHoleOffset;     # Установить свойство (* )
        ClosingHoleOffset = Object.GetClosingHoleOffset( Index );  # Получить свойство (**)
        Object.SetClosingHoleOffset( Index, ClosingHoleOffset );   # Установить свойство (**)
        ```

        ### Входные параметры

        +------------------------------+
        | long Index | - индекс сгиба. |
        +------------------------------+
        """
        ...

    def ClosingHolePlacement(self, Index) -> int:  # isheetmetalclosedcorner_closingholeplacement.html
        """
        ## ClosingHolePlacement - Размещение отверстия при круговой обработке угла

        Тип данных: Значение из перечисления ksClosingHolePlacementEnum.

        ### Синтаксис Automation

        ```
        ClosingHolePlacement = Object.ClosingHolePlacement( Index );     # Получить свойство (* )
        Object.ClosingHolePlacement( Index ) = ClosingHolePlacement;     # Установить свойство (* )
        ClosingHolePlacement = Object.GetClosingHolePlacement( Index );  # Получить свойство (**)
        Object.SetClosingHolePlacement( Index, ClosingHolePlacement );   # Установить свойство (**)
        ```

        ### Входные параметры

        +------------------------------+
        | long Index | - индекс сгиба. |
        +------------------------------+
        """
        ...

    def CornerObject(self, Index) -> IModelObject:  # isheetmetalclosedcorner_cornerobject.html
        """
        ## CornerObject - Угол. Объект (ребро или грань), задающий угол листового тела

        Тип данных: Указатель на интерфейс IModelObject.

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        CornerObject = Object.CornerObject( Index )     # Получить свойство (* )
        CornerObject = Object.GetCornerObject( Index )  # Получить свойство (**)
        ```

        ### Входные параметры

        +------------------------------+
        | long Index | - индекс сгиба. |
        +------------------------------+
        """
        ...

    def DeleteCornerObject(self, Index) -> bool:  # isheetmetalclosedcorner_deletecornerobject.html
        """
        ## DeleteCornerObject - Удалить угол сгиба

        ### Синтаксис Automation

        ```
        BOOL DeleteCornerObject( long Index );
        ```

        ### Входные параметры

        +-------------------------+
        | Index | - индекс сгиба. |
        +-------------------------+

        ### Возвращаемое значение

        +-----------------------------------------+
        | TRUE | - в случае успешного завершения. |
        +-----------------------------------------+
        """
        ...

    def SetClosingClosedType(self, Index, arg1):
        ...

    def SetClosingContinue(self, Index, arg1):
        ...

    def SetClosingCorneringType(self, Index, arg1):
        ...

    def SetClosingDirection(self, Index, arg1):
        ...

    def SetClosingGapValue(self, Index, arg1):
        ...

    def SetClosingHoleDiameter(self, Index, arg1):
        ...

    def SetClosingHoleOffset(self, Index, arg1):
        ...

    def SetClosingHolePlacement(self, Index, arg1):
        ...




class ISheetMetalCut(IModelObject):  # isheetmetalcut.html
    """
    ## Интерфейс ISheetMetalCut

    Интерфейс параметров элемента листового тела "Вырез".

    Интерфейс можно получить с помощью метода коллекции элементов "Вырез" ISheetMetalCuts::Add или свойства ISheetMetalCuts::SheetMetalCut.
    """

    @property
    def Body(self) -> IKompasAPIObject:  # isheetmetalcut_body.html
        """
        ## Body - Тело для операции

        Тип данных: указатель на интерфейс IKompasAPIObject

        В качестве тела можно передавать листовое тело ISheetMetalBody, или примитив, принадлежащий листовому телу.

        ### Синтаксис Automation

        ```
        Body = iObject.Body       # Получить свойство (* )
        iObject.Body = Body       # Установить свойство (* )
        Body = iObject.GetBody()  # Получить свойство (**)
        iObject.SetBody( Body )   # Установить свойство (**)
        ```
        """
        ...
    @Body.setter
    def Body(self, value: IKompasAPIObject): ...

    @property
    def Cut(self) -> bool:  # isheetmetalcut_cut.html
        """
        ## Cut - Результат операции

        Тип данных: BOOL

        Значения свойства:

        +----------------------------------+
        | TRUE  | - вычитание элемента,    |
        +----------------------------------+
        | FALSE | - пересечение элементов. |
        +----------------------------------+

        Позволяет считывать и устанавливать результат операции.

        ### Синтаксис Automation

        ```
        Cut = iObject.Cut;       # Получить свойство (* )
        iObject.Cut = Cut;       # Установить свойство (* )
        Cut = iObject.GetCut();  # Получить свойство (**)
        iObject.SetCut( Cut );   # Установить свойство (**)
        ```
        """
        ...
    @Cut.setter
    def Cut(self, value: bool): ...

    @property
    def CutType(self) -> int:  # isheetmetalcut_cuttype.html
        """
        ## CutType - Тип построения выреза

        Тип данных: из перечисления ksHoleCutTypeEnum

        Позволяет считывать и устанавливать тип построения отверстия.

        ### Синтаксис Automation

        ```
        CutType = iObject.CutType;       # Получить свойство (* )
        iObject.CutType = CutType;       # Установить свойство (* )
        CutType = iObject.GetCutType();  # Получить свойство (**)
        iObject.SetCutType( CutType );   # Установить свойство (**)
        ```
        """
        ...
    @CutType.setter
    def CutType(self, value: int): ...

    @property
    def Depth(self) -> float:  # isheetmetalcut_depth.html
        """
        ## Depth - Глубина выреза

        Тип данных: double

        1. Свойство доступно при типе построения отверстия "на глубину", см.ksHoleCutTypeEnum.

        2. Позволяет считывать и устанавливать глубину отверстия.

        ### Синтаксис Automation

        ```
        Depth = iObject.Depth;       # Получить свойство (* )
        iObject.Depth = Depth;       # Установить свойство (* )
        Depth = iObject.GetDepth();  # Получить свойство (**)
        iObject.SetDepth( Depth );   # Установить свойство (**)
        ```
        """
        ...
    @Depth.setter
    def Depth(self, value: float): ...

    @property
    def DepthObject(self) -> IModelObject:  # isheetmetalcut_depthobject.html
        """
        ## DepthObject - Объект, задающий глубину вырезания

        Тип данных: указатель на интерфейс IModelObject

        1. Свойство доступно при типе построения отверстия "до грани", см.ksHoleCutTypeEnum.

        2. Позволяет получать указатель на интерфейс объекта и устанавливать грань, задающую глубину выдавливания.

        ### Синтаксис Automation

        ```
        DepthObject = iObject.DepthObject;       # Получить свойство (* )
        iObject.DepthObject = DepthObject;       # Установить свойство (* )
        DepthObject = iObject.GetDepthObject();  # Получить свойство (**)
        iObject.SetDepthObject( DepthObject );   # Установить свойство (**)
        ```
        """
        ...
    @DepthObject.setter
    def DepthObject(self, value: IModelObject): ...

    @property
    def Profile(self) -> IModelObject:  # isheetmetalcut_profile.html
        """
        ## Profile - Сечение

        Тип данных: Указатель на интерфейс IModelObject

        ### Синтаксис Automation

        ```
        Profile = Object.Profile       # Получить свойство (* )
        Object.Profile = Profile       # Установить свойство (* )
        Profile = Object.GetProfile()  # Получить свойство (**)
        Object.SetProfile( Profile )   # Установить свойство (**)
        ```
        """
        ...
    @Profile.setter
    def Profile(self, value: IModelObject): ...

    @property
    def Sketch(self) -> ISketch:  # isheetmetalcut_sketch.html
        """
        ## Sketch - Эскиз выреза

        Тип данных: указатель на интерфейс ISketch

        Позволяет получать указатель на интерфейс эскиза выреза и устанавливать новый эскиз.

        ### Синтаксис Automation

        ```
        Sketch = iObject.Sketch;       # Получить свойство (* )
        iObject.Sketch = Sketch;       # Установить свойство (* )
        Sketch = iObject.GetSketch();  # Получить свойство (**)
        iObject.SetSketch( Sketch );   # Установить свойство (**)
        ```
        """
        ...
    @Sketch.setter
    def Sketch(self, value: ISketch): ...




class ISheetMetalHole(IModelObject):  # isheetmetalhole.html
    """
    ## Интерфейс ISheetMetalHole

    Интерфейс параметров элемента листового тела "Отверстие".

    Интерфейс можно получить с помощью метода коллекции элементов "отверстие" ISheetMetalHoles::Add или свойства ISheetMetalHoles::SheetMetalHole.
    """

    @property
    def AssociationVertex(self) -> IModelObject:  # isheetmetalhole_associationvertex.html
        """
        ## AssociationVertex - Точечный объект

        Тип данных: Указатель на интерфейс IModelObject

        ### Синтаксис Automation

        ```
        AssociationVertex = Object.AssociationVertex;       # Получить свойство (* )
        Object.AssociationVertex = AssociationVertex;       # Установить свойство (* )
        AssociationVertex = Object.GetAssociationVertex();  # Получить свойство (**)
        Object.SetAssociationVertex( AssociationVertex );   # Установить свойство (**)
        ```
        """
        ...
    @AssociationVertex.setter
    def AssociationVertex(self, value: IModelObject): ...

    @property
    def Axis(self) -> bool:  # isheetmetalhole_axis.html
        """
        ## Axis - Создавать ось

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        Axis = Object. Axis;       # Получить свойство (* )
        Object. Axis = Axis;       # Установить свойство (* )
        Axis = Object.Get Axis();  # Получить свойство (**)
        Object.Set Axis( Axis );   # Установить свойство (**)
        ```
        """
        ...
    @Axis.setter
    def Axis(self, value: bool): ...

    @property
    def BasePlane(self) -> IModelObject:  # isheetmetalhole_baseplane.html
        """
        ## BasePlane - Опорная грань

        Тип данных: указатель на интерфейс IModelObject

        Позволяет устанавливать и получать указатель на интерфейс грани, на которой будет располагаться эскиз отверстия.

        ### Синтаксис Automation

        ```
        BasePlane = iObject.BasePlane;       # Получить свойство (* )
        iObject.BasePlane = BasePlane;       # Установить свойство (* )
        BasePlane = iObject.GetBasePlane();  # Получить свойство (**)
        iObject.SetBasePlane( BasePlane );   # Установить свойство (**)
        ```
        """
        ...
    @BasePlane.setter
    def BasePlane(self, value: IModelObject): ...

    @property
    def Body(self) -> IKompasAPIObject:  # isheetmetalhole_body.html
        """
        ## Body - Тело для операции

        Тип данных: Указатель на интерфейс IKompasAPIObject

        ### Синтаксис Automation

        ```
        Body = Object.Body;       # Получить свойство (* )
        Object.Body = Body;       # Установить свойство (* )
        Body = Object.GetBody();  # Получить свойство (**)
        Object.SetBody( Body );   # Установить свойство (**)
        ```
        """
        ...
    @Body.setter
    def Body(self, value: IKompasAPIObject): ...

    @property
    def CutType(self) -> int:  # isheetmetalhole_cuttype.html
        """
        ## CutType - Тип построения отверстия

        Тип данных: из перечисления ksHoleCutTypeEnum

        Позволяет считывать и устанавливать тип построения отверстия.

        ### Синтаксис Automation

        ```
        CutType = iObject.CutType;       # Получить свойство (* )
        iObject.CutType = CutType;       # Установить свойство (* )
        CutType = iObject.GetCutType();  # Получить свойство (**)
        iObject.SetCutType( CutType );   # Установить свойство (**)
        ```
        """
        ...
    @CutType.setter
    def CutType(self, value: int): ...

    @property
    def Depth(self) -> float:  # isheetmetalhole_depth.html
        """
        ## Depth - Глубина отверстия

        Тип данных: double

        1. Свойство доступно при типе построения отверстия "на глубину", см.ksHoleCutTypeEnum.

        2. Позволяет считывать и устанавливать глубину отверстия.

        ### Синтаксис Automation

        ```
        Depth = iObject.Depth;       # Получить свойство (* )
        iObject.Depth = Depth;       # Установить свойство (* )
        Depth = iObject.GetDepth();  # Получить свойство (**)
        iObject.SetDepth( Depth );   # Установить свойство (**)
        ```
        """
        ...
    @Depth.setter
    def Depth(self, value: float): ...

    @property
    def DepthObject(self) -> IModelObject:  # isheetmetalhole_depthobject.html
        """
        ## DepthObject - Объект, задающий глубину вырезания

        Тип данных: указатель на интерфейс IModelObject

        1. Свойство доступно при типе построения отверстия "до грани", см.ksHoleCutTypeEnum.

        2. Позволяет получать указатель на интерфейс объекта и устанавливать объект, задающий глубину выдавливания.

        ### Синтаксис Automation

        ```
        DepthObject = iObject.DepthObject;       # Получить свойство (* )
        iObject.DepthObject = DepthObject;       # Установить свойство (* )
        DepthObject = iObject.GetDepthObject();  # Получить свойство (**)
        iObject.SetDepthObject( DepthObject );   # Установить свойство (**)
        ```
        """
        ...
    @DepthObject.setter
    def DepthObject(self, value: IModelObject): ...

    @property
    def Diameter(self) -> float:  # isheetmetalhole_diameter.html
        """
        ## Diameter - Диаметр отверстия

        Тип данных: double

        Позволяет считывать и устанавливать диаметр отверстия.

        ### Синтаксис Automation

        ```
        Diameter = iObject.Diameter;       # Получить свойство (* )
        iObject.Diameter = Diameter;       # Установить свойство (* )
        Diameter = iObject.GetDiameter();  # Получить свойство (**)
        iObject.SetDiameter( Diameter );   # Установить свойство (**)
        ```
        """
        ...
    @Diameter.setter
    def Diameter(self, value: float): ...

    @property
    def PerpendicularEdge(self):
        ...
    @PerpendicularEdge.setter
    def PerpendicularEdge(self, value): ...

    @property
    def Point3DParamSurface(self) -> IKompasAPIObject:  # isheetmetalhole_point3dparamsurface.html
        """
        ## Point3DParamSurface - Параметры пространственной точки на поверхности

        Тип данных: Указатель на интерфейс IKompasAPIObject

        Свойство доступно только для чтения.

        Свойство позволяет получить/установить параметры точки привязки отверстия на поверхности, используя интерфейс параметров точки на поверхности IPoint3DParamSurface.

        ### Синтаксис Automation

        ```
        Point3DParamSurface = Object.Point3DParamSurface       # Получить свойство(* )
        Point3DParamSurface = Object.GetPoint3DParamSurface()  # Получить свойство(**)
        ```
        """
        ...

    @property
    def Sketch(self) -> ISketch:  # isheetmetalhole_sketch.html
        """
        ## Sketch - Эскиз

        Тип данных: указатель на интерфейс эскиза ISketch

        1. Свойство позволяет получать эскиз для отверстия.

        2. Свойство доступно только для чтения.

        3. Эскиз создается при создании.

        ### Синтаксис Automation

        ```
        Sketch = iObject.Sketch;       # Получить свойство (* )
        Sketch = iObject.GetSketch();  # Получить свойство (**)
        ```
        """
        ...

    @property
    def X(self) -> float:  # isheetmetalhole_x.html
        """
        ## X - Координата центра отверстия по оси X

        Тип данных: double

        Позволяет считывать и устанавливать координату центра отверстия по X.

        ### Синтаксис Automation

        ```
        X = iObject.X;       # Получить свойство (* )
        iObject.X = X;       # Установить свойство (* )
        X = iObject.GetX();  # Получить свойство (**)
        iObject.SetX( X );   # Установить свойство (**)
        ```
        """
        ...
    @X.setter
    def X(self, value: float): ...

    @property
    def Y(self) -> float:  # isheetmetalhole_y.html
        """
        ## Y - Координата центра отверстия по оси Y

        Тип данных: double

        Позволяет считывать и устанавливать координату центра отверстия по Y.

        ### Синтаксис Automation

        ```
        Y = iObject.Y;       # Получить свойство (* )
        iObject.Y = Y;       # Установить свойство (* )
        Y = iObject.GetY();  # Получить свойство (**)
        iObject.SetY( Y );   # Установить свойство (**)
        ```
        """
        ...
    @Y.setter
    def Y(self, value: float): ...




class ISheetMetalJalousies(IModelObjects):  # isheetmetaljalousies.html
    """
    ## Интерфейс ISheetMetalJalousies

    Интерфейс коллекции элементов Жалюзи.
    """

    def Add(self) -> ISheetMetalJalousie:  # isheetmetaljalousies_add.html
        """
        ## Add - Создает новый элемент и добавляет его в коллекцию

        1. Метод позволяет создать новый интерфейс операции Жалюзи.

        2. После получения нового интерфейса нужно задать параметры операции и вызвать метод IModelObject::Update.

        ### Синтаксис Automation

        ```
        LPDISPATCH Add();
        ```

        ### Возвращаемое значение

        - указатель на интерфейс ISheetMetalJalousie.
        """
        ...

    def SheetMetalJalousie(self, Index) -> ISheetMetalJalousie:  # isheetmetaljalousies_sheetmetaljalousie.html
        """
        ## SheetMetalJalousie - Возвращает элемент, заданный по индексу

        Тип данных: Указатель на интерфейс ISheetMetalJalousie.

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        SheetMetalJalousie = Object.SheetMetalJalousie( Index );     # Получить свойство (* )
        SheetMetalJalousie = Object.GetSheetMetalJalousie( Index );  # Получить свойство (**)
        ```

        ### Входные параметры

        +--------------------------------------------+
        | VARIANT Index | - индекс или имя элемента. |
        +--------------------------------------------+
        """
        ...




class ISheetMetalJalousie(IModelObject):  # isheetmetaljalousie.html
    """
    ## Интерфейс ISheetMetalJalousie

    Интерфейс операции Жалюзи.
    """

    @property
    def BuildingType(self) -> int:  # isheetmetaljalousie_buildingtype.html
        """
        ## BuildingType - Способ построения

        Тип данных: Значение из перечисления ksJalousieBuildingTypeEnum.

        ### Синтаксис Automation

        ```
        BuildingType = Object.BuildingType;       # Получить свойство (* )
        Object.BuildingType = BuildingType;       # Установить свойство (* )
        BuildingType = Object.GetBuildingType();  # Получить свойство (**)
        Object.SetBuildingType( BuildingType );   # Установить свойство (**)
        ```
        """
        ...
    @BuildingType.setter
    def BuildingType(self, value: int): ...

    @property
    def Direction(self) -> bool:  # isheetmetaljalousie_direction.html
        """
        ## Direction - Направление построения: TRUE - Прямое, FALSE - Обратное

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        Direction = Object.Direction        # Получить свойство (* )
        Object.Direction = Direction;       # Установить свойство (* )
        Direction = Object.GetDirection();  # Получить свойство (**)
        Object.SetDirection( Direction );   # Установить свойство (**)
        ```
        """
        ...
    @Direction.setter
    def Direction(self, value: bool): ...

    @property
    def FormEnd(self) -> int:  # isheetmetaljalousie_formend.html
        """
        ## FormEnd - Форма торца

        Тип данных: Значение из перечисления ksJalousieFormEndEnum.

        ### Синтаксис Automation

        ```
        FormEnd = Object.FormEnd;       # Получить свойство (* )
        FormEnd = Object.GetFormEnd();  # Установить свойство (* )
        FormEnd = Object.GetFormEnd();  # Получить свойство (**)
        Object.SetFormEnd( FormEnd );   # Установить свойство (**)
        ```
        """
        ...
    @FormEnd.setter
    def FormEnd(self, value: int): ...

    @property
    def Height(self) -> float:  # isheetmetaljalousie_height.html
        """
        ## Height - Высота

        Тип данных: double

        ### Синтаксис Automation

        ```
        Height = Object.Height;       # Получить свойство (* )
        Object.Height = Height;       # Установить свойство (* )
        Height = Object.GetHeight();  # Получить свойство (**)
        Object.SetHeight( Height );   # Установить свойство (**)
        ```
        """
        ...
    @Height.setter
    def Height(self, value: float): ...

    @property
    def HeightType(self) -> int:  # isheetmetaljalousie_heighttype.html
        """
        ## HeightType - Способ задания высоты

        Тип данных: Значение из перечисления ksJalousieHeightTypeEnum.

        ### Синтаксис Automation

        ```
        HeightType = Object.HeightType;       # Получить свойство (* )
        Object.HeightType = HeightType;       # Установить свойство (* )
        HeightType = Object.GetHeightType();  # Получить свойство (**)
        Object.SetHeightType( HeightType );   # Установить свойство (**)
        ```
        """
        ...
    @HeightType.setter
    def HeightType(self, value: int): ...

    @property
    def Radius(self) -> float:  # isheetmetaljalousie_radius.html
        """
        ## Radius - Радиус скругления основания

        Тип данных: double

        ### Синтаксис Automation

        ```
        Radius = Object.Radius;       # Получить свойство (* )
        Object.Radius = Radius;       # Установить свойство (* )
        Radius = Object.GetRadius();  # Получить свойство (**)
        Object.SetRadius( Radius );   # Установить свойство (**)
        ```
        """
        ...
    @Radius.setter
    def Radius(self, value: float): ...

    @property
    def Round(self) -> bool:  # isheetmetaljalousie_round.html
        """
        ## Round - Скругление основания

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        Round = Object.Round;       # Получить свойство (* )
        Object.Round = Round;       # Установить свойство (* )
        Round = Object.GetRound();  # Получить свойство (**)
        Object.SetRound( Round );   # Установить свойство (**)
        ```
        """
        ...
    @Round.setter
    def Round(self, value: bool): ...

    @property
    def Side(self) -> bool:  # isheetmetaljalousie_side.html
        """
        ## Side - Положение

        Тип данных: BOOL

        Значение свойства:

        +------------------+
        | TRUE  | - справа |
        +------------------+
        | FALSE | - слева. |
        +------------------+

        ### Синтаксис Automation

        ```
        Side = Object.Side;       # Получить свойство (* )
        Object.Side = Side;       # Установить свойство (* )
        Side = Object.GetSide();  # Получить свойство (**)
        Object.SetSide( Side );   # Установить свойство (**)
        ```
        """
        ...
    @Side.setter
    def Side(self, value: bool): ...

    @property
    def Sketch(self) -> IModelObject:  # isheetmetaljalousie_sketch.html
        """
        ## Sketch - Эскиз

        Тип данных: Указатель на интерфейс IModelObject.

        ### Синтаксис Automation

        ```
        Sketch = Object.Sketch;       # Получить свойство (* )
        Object.Sketch = Sketch;       # Установить свойство (* )
        Sketch = Object.GetSketch();  # Получить свойство (**)
        Object.SetSketch( Sketch );   # Установить свойство (**)
        ```
        """
        ...
    @Sketch.setter
    def Sketch(self, value: IModelObject): ...

    @property
    def Width(self) -> float:  # isheetmetaljalousie_width.html
        """
        ## Width - Ширина

        Тип данных: double

        ### Синтаксис Automation

        ```
        Width = Object.Width;       # Получить свойство (* )
        Object.Width = Width;       # Установить свойство (* )
        Width = Object.GetWidth();  # Получить свойство (**)
        Object.SetWidth( Width );   # Установить свойство (**)
        ```
        """
        ...
    @Width.setter
    def Width(self, value: float): ...




class ISheetMetalLineBend(IModelObject):  # isheetmetallinebend.html
    """
    ## Интерфейс ISheetMetalLineBend

    Интерфейс параметров Сгиба по линии.

    Интерфейс можно получить с помощью метода коллекции сгибов ISheetMetalLineBends::Add или свойства ISheetMetalLineBends::SheetMetalLineBend.
    """

    @property
    def Angle(self) -> float:  # isheetmetallinebend_angle.html
        """
        ## Angle - Угол сгиба

        Тип данных: double

        Позволяет считывать и устанавливать угол сгиба.

        ### Синтаксис Automation

        ```
        Angle = iObject.Angle;      # Получить свойство (* )
        iObject.Angle = Angle;      # Установить свойство (* )
        Angle = iObject.GetAngle;   # Получить свойство (**)
        iObject.SetAngle( Angle );  # Установить свойство (**)
        ```
        """
        ...
    @Angle.setter
    def Angle(self, value: float): ...

    @property
    def AngleType(self) -> bool:  # isheetmetallinebend_angletype.html
        """
        ## AngleType - Способ задания угла - угол сгиба / дополняющий угол

        Тип данных: BOOL

        Значения свойства:

        +-------------------------------------------------------+
        | TRUE  | - угол интерпретируется как угол сгиба,       |
        +-------------------------------------------------------+
        | FALSE | - угол интерпретируется как дополняющий угол. |
        +-------------------------------------------------------+

        Позволяет считывать и устанавливать способ задания угла.

        ### Синтаксис Automation

        ```
        AngleType = iObject.AngleType;      # Получить свойство (* )
        iObject.AngleType = AngleType;      # Установить свойство (* )
        AngleType = iObject.GetAngleType;   # Получить свойство (**)
        iObject.SetAngleType( AngleType );  # Установить свойство (**)
        ```
        """
        ...
    @AngleType.setter
    def AngleType(self, value: bool): ...

    @property
    def BendCoefficient(self) -> float:  # isheetmetallinebend_bendcoefficient.html
        """
        ## BendCoefficient - Коэффициент нейтрального слоя

        Тип данных: double

        Чтобы свойство было доступно, необходимо установить тип расчета длины развёртки - коэффициент нейтрального слоя. Позволяет считывать и устанавливать коэффициент нейтрального слоя. Get - cвойство будет возвращать коэффициент нейтрального слоя базового листового тела, пока не будет установлен коэффициент нейтрального слоя для сгиба по линии.

        ### Синтаксис Automation

        ```
        BendCoefficient = iObject.BendCoefficient;      # Получить свойство (* )
        iObject.BendCoefficient = BendCoefficient;      # Установить свойство (* )
        BendCoefficient = iObject.GetBendCoefficient;   # Получить свойство (**)
        iObject.SetBendCoefficient( BendCoefficient );  # Установить свойство (**)
        ```
        """
        ...
    @BendCoefficient.setter
    def BendCoefficient(self, value: float): ...

    @property
    def BendLeftSideFixed(self) -> bool:  # isheetmetallinebend_bendleftsidefixed.html
        """
        ## BendLeftSideFixed - Признак фиксации левой стороны

        Тип данных: BOOL

        Значения свойства:

        +---------------------------------+
        | TRUE  | - прямое направление,   |
        +---------------------------------+
        | FALSE | - обратное направление. |
        +---------------------------------+

        Позволяет установить и получить признак фиксации левой стороны.

        ### Синтаксис Automation

        ```
        BendLeftSideFixed = iObject.BendLeftSideFixed;      # Получить свойство (* )
        iObject.BendLeftSideFixed = BendLeftSideFixed;      # Установить свойство (* )
        BendLeftSideFixed = iObject.GetBendLeftSideFixed;   # Получить свойство (**)
        iObject.SetBendLeftSideFixed( BendLeftSideFixed );  # Установить свойство (**)
        ```
        """
        ...
    @BendLeftSideFixed.setter
    def BendLeftSideFixed(self, value: bool): ...

    @property
    def BendReduction(self) -> float:  # isheetmetallinebend_bendreduction.html
        """
        ## BendReduction - Уменьшение сгиба

        Тип данных: double

        Чтобы свойство было доступно, необходимо установить тип расчета длины развёртки - величина сгиба. Позволяет считывать и устанавливать величину сгиба. Get - cвойство будет возвращать величину сгиба базового листового тела, пока не будет установлена "величина сгиба" для сгиба по линии.

        ### Синтаксис Automation

        ```
        BendReduction = iObject.BendReduction;      # Получить свойство (* )
        iObject.BendReduction = BendReduction;      # Установить свойство (* )
        BendReduction = iObject.GetBendReduction;   # Получить свойство (**)
        iObject.SetBendReduction( BendReduction );  # Установить свойство (**)
        ```
        """
        ...
    @BendReduction.setter
    def BendReduction(self, value: float): ...

    @property
    def BendTablePath(self) -> str:  # isheetmetallinebend_bendtablepath.html
        """
        ## BendTablePath - Уменьшение сгиба

        Тип данных: BSTR

        Свойство доступно только для чтения. Таблица сгибов одна на документ. Путь к таблице сгибов выбирается у базового листового тела.

        ### Синтаксис Automation

        ```
        BendTablePath = iObject.BendTablePath;     # Получить свойство (* )
        BendTablePath = iObject.GetBendTablePath;  # Получить свойство (**)
        ```
        """
        ...

    @property
    def BendType(self) -> int:  # isheetmetallinebend_bendtype.html
        """
        ## BendType - Cпособ формирования сгиба

        Тип данных: из перечисления ksBendTypeEnum

        Позволяет считывать и устанавливать способ формирования сгиба.

        ### Синтаксис Automation

        ```
        BendType = iObject.BendType;      # Получить свойство (* )
        iObject.BendType = BendType;      # Установить свойство (* )
        BendType = iObject.GetBendType;   # Получить свойство (**)
        iObject.SetBendType( BendType );  # Установить свойство (**)
        ```
        """
        ...
    @BendType.setter
    def BendType(self, value: int): ...

    @property
    def BendValue(self) -> float:  # isheetmetallinebend_bendvalue.html
        """
        ## BendValue - Величина сгиба

        Тип данных: double

        Чтобы свойство было доступно, необходимо установить тип расчета длины развёртки - величина сгиба. Позволяет считывать и устанавливать величину сгиба. Get - cвойство будет возвращать величину сгиба базового листового тела, пока не будет установлена "величина сгиба" для сгиба по линии.

        ### Синтаксис Automation

        ```
        BendValue = iObject.BendValue;      # Получить свойство (* )
        iObject.BendValue = BendValue;      # Установить свойство (* )
        BendValue = iObject.GetBendValue;   # Получить свойство (**)
        iObject.SetBendValue( BendValue );  # Установить свойство (**)
        ```
        """
        ...
    @BendValue.setter
    def BendValue(self, value: float): ...

    @property
    def Direction(self) -> bool:  # isheetmetallinebend_direction.html
        """
        ## Direction - Направление построения

        Тип данных: BOOL

        Значения свойства:

        +---------------------------------+
        | TRUE  | - прямое направление,   |
        +---------------------------------+
        | FALSE | - обратное направление. |
        +---------------------------------+

        Позволяет установить и получить направление построения сгиба.

        ### Синтаксис Automation

        ```
        Direction = iObject.Direction;      # Получить свойство (* )
        iObject.Direction = Direction;      # Установить свойство (* )
        Direction = iObject.GetDirection;   # Получить свойство (**)
        iObject.SetDirection( Direction );  # Установить свойство (**)
        ```
        """
        ...
    @Direction.setter
    def Direction(self, value: bool): ...

    @property
    def DismissalAngleType(self) -> int:  # isheetmetallinebend_dismissalangletype.html
        """
        ## DismissalAngleType - Способ освобождения угла cгиба

        Тип данных: из перечисления ksBendAngleReleaseTypeEnum

        Позволяет считывать и устанавливать способ освобождения угла cгиба.

        ### Синтаксис Automation

        ```
        DismissalAngleType = iObject.DismissalAngleType;      # Получить свойство (* )
        iObject.DismissalAngleType = DismissalAngleType;      # Установить свойство (* )
        DismissalAngleType = iObject.GetDismissalAngleType;   # Получить свойство (**)
        iObject.SetDismissalAngleType( DismissalAngleType );  # Установить свойство (**)
        ```
        """
        ...
    @DismissalAngleType.setter
    def DismissalAngleType(self, value: int): ...

    @property
    def Faces(self) -> typing.Any:  # isheetmetallinebend_faces.html
        """
        ## Faces - Массив граней (SAFEARRAY)

        Тип данных: VARIANT

        Позволяет получать и устанавливать грань или массив граней, участвующих в операции Сгиб по линии. Массив граней возвращается в виде массива SAFEARRAY граней LPDISPATCH (VT_ARRAY | VT_DISPATCH).

        ### Синтаксис Automation

        ```
        Faces = iObject.Faces;      # Получить свойство (* )
        iObject.Faces = Faces;      # Установить свойство (* )
        Faces = iObject.GetFaces;   # Получить свойство (**)
        iObject.SetFaces( Faces );  # Установить свойство (**)
        ```
        """
        ...
    @Faces.setter
    def Faces(self, value: typing.Any): ...

    @property
    def InternalRadius(self) -> bool:  # isheetmetallinebend_internalradius.html
        """
        ## InternalRadius - Внутренний радиус

        Тип данных: BOOL

        Значения свойства:

        +------------------------------+
        | TRUE  | - внутренний радиус, |
        +------------------------------+
        | FALSE | - наружный радиус.   |
        +------------------------------+

        Позволяет получать и устанавливать признак "внутренний радиус".

        ### Синтаксис Automation

        ```
        InternalRadius = iObject.InternalRadius;      # Получить свойство (* )
        iObject.InternalRadius = InternalRadius;      # Установить свойство (* )
        InternalRadius = iObject.GetInternalRadius;   # Получить свойство (**)
        iObject.SetInternalRadius( InternalRadius );  # Установить свойство (**)
        ```
        """
        ...
    @InternalRadius.setter
    def InternalRadius(self, value: bool): ...

    @property
    def Line(self) -> IModelObject:  # isheetmetallinebend_line.html
        """
        ## Line - Прямолинейный объект (отрезок эскиза или ломаной, ребро или ось)

        Тип данных: указатель на интерфейс объекта IModelObject

        Позволяет получать и устанавливать прямолинейный объект (отрезок эскиза или ломанной, ребро или ось), относительно которого сгибается листовое тело. Прямолинейный объект и грань (массив граней), участвующие в операции "сгиб по линии", должны располагаться в одной плоскости.

        ### Синтаксис Automation

        ```
        Line = iObject.Line;      # Получить свойство (* )
        iObject.Line = Line;      # Установить свойство (* )
        Line = iObject.GetLine;   # Получить свойство (**)
        iObject.SetLine( Line );  # Установить свойство (**)
        ```
        """
        ...
    @Line.setter
    def Line(self, value: IModelObject): ...

    @property
    def Radius(self) -> float:  # isheetmetallinebend_radius.html
        """
        ## Radius - Радиус сгиба

        Тип данных: double

        Позволяет получать и устанавливать радиус сгиба.

        ### Синтаксис Automation

        ```
        Radius = iObject.Radius;      # Получить свойство (* )
        iObject.Radius = Radius;      # Установить свойство (* )
        Radius = iObject.GetRadius;   # Получить свойство (**)
        iObject.SetRadius( Radius );  # Установить свойство (**)
        ```
        """
        ...
    @Radius.setter
    def Radius(self, value: float): ...

    @property
    def Straighten(self) -> bool:  # isheetmetallinebend_straighten.html
        """
        ## Straighten - Разогнуть сгиб

        Тип данных: BOOL

        Значения свойства:

        +--------------------------+
        | TRUE  | - угол разогнут, |
        +--------------------------+
        | FALSE | - угол согнут.   |
        +--------------------------+

        Позволяет считывать и устанавливать способ формирования сгиба.

        ### Синтаксис Automation

        ```
        Straighten = iObject.Straighten;      # Получить свойство (* )
        iObject.Straighten = Straighten;      # Установить свойство (* )
        Straighten = iObject.GetStraighten;   # Получить свойство (**)
        iObject.SetStraighten( Straighten );  # Установить свойство (**)
        ```
        """
        ...
    @Straighten.setter
    def Straighten(self, value: bool): ...

    @property
    def UnfoldType(self) -> int:  # isheetmetallinebend_unfoldtype.html
        """
        ## UnfoldType - Способ определения длины развертки

        Тип данных: из перечисления ksUnfoldTypeEnum

        Позволяет считывать и устанавливать способ определения длины развертки. Если задан способ определения длины развертки - из таблицы, то будут недоступны свойства установки (Set) для коэффициента нейтрального слоя, величины сгиба и уменьшения сгиба, т.к они будут браться из таблицы.

        ### Синтаксис Automation

        ```
        UnfoldType = iObject.UnfoldType;      # Получить свойство (* )
        iObject.UnfoldType = UnfoldType;      # Установить свойство (* )
        UnfoldType = iObject.GetUnfoldType;   # Получить свойство (**)
        iObject.SetUnfoldType( UnfoldType );  # Установить свойство (**)
        ```
        """
        ...
    @UnfoldType.setter
    def UnfoldType(self, value: int): ...




class ISheetMetalRibs(IModelObjects):  # isheetmetalribs.html
    """
    ## Интерфейс ISheetMetalRibs

    Интерфейс коллекции элементов Ребро усиления.
    """

    def Add(self) -> ISheetMetalRib:  # isheetmetalribs_add.html
        """
        ## Add - Создает новый элемент и добавляет его в коллекцию

        1. Метод позволяет создать новый интерфейс операции Пластина.

        2. После получения нового интерфейса нужно задать параметры операции и вызвать метод IModelObject::Update.

        ### Синтаксис Automation

        ```
        LPDISPATCH Add();
        ```

        ### Возвращаемое значение

        - указатель на интерфейс ISheetMetalRib.
        """
        ...

    def SheetMetalRib(self, Index) -> ISheetMetalRib:  # isheetmetalribs_sheetmetalrib.html
        """
        ## SheetMetalRib - Возвращает элемент, заданный по индексу

        Тип данных: Указатель на интерфейс ISheetMetalRib.

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        SheetMetalRib = Object.SheetMetalRib( Index );     # Получить свойство (* )
        SheetMetalRib = Object.GetSheetMetalRib( Index );  # Получить свойство (**)
        ```

        ### Входные параметры

        +--------------------------------------------+
        | VARIANT Index | - индекс или имя элемента. |
        +--------------------------------------------+
        """
        ...




class ISheetMetalRuledShell(IDispatch):  # isheetmetalruledshell.html
    """
    ## Интерфейс ISheetMetalRuledShell

    Интерфейс операции Обечайка.
    """

    @property
    def GapDraftPosition(self) -> float:  # isheetmetalruledshell_gapdraftposition.html
        """
        ## GapDraftPosition - Угловое смещение зазора вдоль кривой

        Тип данных: double

        ### Синтаксис Automation

        ```
        GapDraftPosition = Object.GapDraftPosition       # Получить свойство (* )
        Object.GapDraftPosition = GapDraftPosition       # Установить свойство (* )
        GapDraftPosition = Object.GetGapDraftPosition()  # Получить свойство (**)
        Object.SetGapDraftPosition( GapDraftPosition )   # Установить свойство (**)
        ```
        """
        ...
    @GapDraftPosition.setter
    def GapDraftPosition(self, value: float): ...

    @property
    def GapOffsetLength(self) -> float:  # isheetmetalruledshell_gapoffsetlength.html
        """
        ## GapOffsetLength - Величина смещения зазора вдоль кривой

        Тип данных: double

        ### Синтаксис Automation

        ```
        GapOffsetLength = Object.GapOffsetLength       # Получить свойство (* )
        Object.GapOffsetLength = GapOffsetLength       # Установить свойство (* )
        GapOffsetLength = Object.GetGapOffsetLength()  # Получить свойство (**)
        Object.SetGapOffsetLength( GapOffsetLength )   # Установить свойство (**)
        ```
        """
        ...
    @GapOffsetLength.setter
    def GapOffsetLength(self, value: float): ...

    @property
    def GapOffsetU(self) -> float:  # isheetmetalruledshell_gapoffsetu.html
        """
        ## GapOffsetU - Смещение зазора в % от длины кривой

        Тип данных: double

        ### Синтаксис Automation

        ```
        GapOffsetU = Object.GapOffsetU       # Получить свойство (* )
        Object.GapOffsetU = GapOffsetU       # Установить свойство (* )
        GapOffsetU = Object.GetGapOffsetU()  # Получить свойство (**)
        Object.SetGapOffsetU( GapOffsetU )   # Установить свойство (**)
        ```
        """
        ...
    @GapOffsetU.setter
    def GapOffsetU(self, value: float): ...

    @property
    def GapValue(self) -> float:  # isheetmetalruledshell_gapvalue.html
        """
        ## GapValue - Зазор

        Тип данных: double

        ### Синтаксис Automation

        ```
        GapValue = Object.GapValue       # Получить свойство (* )
        Object.GapValue = GapValue       # Установить свойство (* )
        GapValue = Object.GetGapValue()  # Получить свойство (**)
        Object.SetGapValue( GapValue )   # Установить свойство (**)
        ```
        """
        ...
    @GapValue.setter
    def GapValue(self, value: float): ...

    @property
    def KeepRadius(self) -> bool:  # isheetmetalruledshell_keepradius.html
        """
        ## KeepRadius - Постоянный радиус

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        KeepRadius = Object.KeepRadius       # Получить свойство (* )
        Object.KeepRadius = KeepRadius       # Установить свойство (* )
        KeepRadius = Object.GetKeepRadius()  # Получить свойство (**)
        Object.SetKeepRadius( KeepRadius )   # Установить свойство (**)
        ```
        """
        ...
    @KeepRadius.setter
    def KeepRadius(self, value: bool): ...

    @property
    def OffsetGapType(self) -> int:  # isheetmetalruledshell_offsetgaptype.html
        """
        ## OffsetGapType - Тип смещения зазора

        Тип данных: Значение из перечисления ksOffsetGapType.

        ### Синтаксис Automation

        ```
        OffsetGapType = Object.OffsetGapType       # Получить свойство (* )
        Object.OffsetGapType = OffsetGapType       # Установить свойство (* )
        OffsetGapType = Object.GetOffsetGapType()  # Получить свойство (**)
        Object.SetOffsetGapType( OffsetGapType )   # Установить свойство (**)
        ```
        """
        ...
    @OffsetGapType.setter
    def OffsetGapType(self, value: int): ...

    @property
    def RuledBorder(self) -> int:  # isheetmetalruledshell_ruledborder.html
        """
        ## RuledBorder - Тип кромки оснований

        Тип данных: из перечисления ksRuledBorderEnum.

        ### Синтаксис Automation

        ```
        RuledBorder = Object.RuledBorder       # Получить свойство (* )
        Object.RuledBorder = RuledBorder       # Установить свойство (* )
        RuledBorder = Object.GetRuledBorder()  # Получить свойство (**)
        Object.SetRuledBorder( RuledBorder )   # Установить свойство (**)
        ```
        """
        ...
    @RuledBorder.setter
    def RuledBorder(self, value: int): ...

    @property
    def RuledJoint(self) -> int:  # isheetmetalruledshell_ruledjoint.html
        """
        ## RuledJoint - Тип кромки стыка

        Тип данных: из перечисления ksRuledJointEnum.

        ### Синтаксис Automation

        ```
        RuledJoint = Object.RuledJoint       # Получить свойство (* )
        Object.RuledJoint = RuledJoint       # Установить свойство (* )
        RuledJoint = Object.GetRuledJoint()  # Получить свойство (**)
        Object.SetRuledJoint( RuledJoint )   # Установить свойство (**)
        ```
        """
        ...
    @RuledJoint.setter
    def RuledJoint(self, value: int): ...

    @property
    def SegmentationMethod(self) -> int:  # isheetmetalruledshell_segmentationmethod.html
        """
        ## SegmentationMethod - Способ сегментации эскиза

        Тип данных: из перечисления ksSegmentationMethodEnum

        ### Синтаксис Automation

        ```
        SegmentationMethod = Object.SegmentationMethod       # Получить свойство (* )
        Object.SegmentationMethod = SegmentationMethod       # Установить свойство (* )
        SegmentationMethod = Object.GetSegmentationMethod()  # Получить свойство (**)
        Object.SetSegmentationMethod( SegmentationMethod )   # Установить свойство (**)
        ```
        """
        ...
    @SegmentationMethod.setter
    def SegmentationMethod(self, value: int): ...

    @property
    def SegmentationSplitValue(self) -> float:  # isheetmetalruledshell_segmentationsplitvalue.html
        """
        ## SegmentationSplitValue - Величина, определяющая разбиения, интерпретация зависит от типа разбиения: это либо количество сегментов, либо длина сегмента

        Тип данных: double

        ### Синтаксис Automation

        ```
        SegmentationSplitValue = Object.SegmentationSplitValue       # Получить свойство (* )
        Object.SegmentationSplitValue = SegmentationSplitValue       # Установить свойство (* )
        SegmentationSplitValue = Object.GetSegmentationSplitValue()  # Получить свойство (**)
        Object.SetSegmentationSplitValue( SegmentationSplitValue )   # Установить свойство (**)
        ```
        """
        ...
    @SegmentationSplitValue.setter
    def SegmentationSplitValue(self, value: float): ...

    @property
    def UseSegmentation(self) -> bool:  # isheetmetalruledshell_usesegmentation.html
        """
        ## UseSegmentation - Сегментация

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        UseSegmentation = Object.UseSegmentation       # Получить свойство (* )
        Object.UseSegmentation = UseSegmentation       # Установить свойство (* )
        UseSegmentation = Object.GetUseSegmentation()  # Получить свойство (**)
        Object.SetUseSegmentation( UseSegmentation )   # Установить свойство (**)
        ```
        """
        ...
    @UseSegmentation.setter
    def UseSegmentation(self, value: bool): ...

    def DraftOutward(self, Normal) -> bool:  # isheetmetalruledshell_draftoutward.html
        """
        ## DraftOutward - Признак уклона наружу

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        DraftOutward = Object.DraftOutward( Normal )     # Получить свойство (* )
        Object.DraftOutward( Normal ) = DraftOutward     # Установить свойство (* )
        DraftOutward = Object.GetDraftOutward( Normal )  # Получить свойство (**)
        Object.SetDraftOutward( Normal, DraftOutward )   # Установить свойство (**)
        ```

        ### Входные параметры

        +------------------------------------------------------+
        | normal TRUE | - угол наклона в прямом направлении.   |
        +------------------------------------------------------+
        | FALSE       | - угол наклона в обратном направлении. |
        +------------------------------------------------------+
        """
        ...

    def DraftValue(self, Normal) -> float:  # isheetmetalruledshell_draftvalue.html
        """
        ## DraftValue - Угол уклона

        Тип данных: double

        ### Синтаксис Automation

        ```
        DraftValue = Object.DraftValue( Normal )     # Получить свойство (* )
        Object.DraftValue( Normal ) = DraftValue     # Установить свойство (* )
        DraftValue = Object.GetDraftValue( Normal )  # Получить свойство (**)
        Object.SetDraftValue( Normal, DraftValue )   # Установить свойство (**)
        ```

        ### Входные параметры

        +------------------------------------------------------+
        | normal TRUE | - угол наклона в прямом направлении.   |
        +------------------------------------------------------+
        | FALSE       | - угол наклона в обратном направлении. |
        +------------------------------------------------------+
        """
        ...

    def SetDraftOutward(self, Normal, arg1):
        ...

    def SetDraftValue(self, Normal, arg1):
        ...




class ISheetMetalLinearRuledShell(IDispatch):  # isheetmetallinearruledshell.html
    """
    ## Интерфейс ISheetMetalLinearRuledShell

    Интерфейс операции Линейчатая обечайка.
    """

    @property
    def AutoSegmentation(self) -> bool:  # isheetmetallinearruledshell_autosegmentation.html
        """
        ## AutoSegmentation - Автоматическое разбиение

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        AutoSegmentation = Object.AutoSegmentation       # Получить свойство (* )
        Object.AutoSegmentation = AutoSegmentation       # Установить свойство (* )
        AutoSegmentation = Object.GetAutoSegmentation()  # Получить свойство (**)
        Object.SetAutoSegmentation( AutoSegmentation )   # Установить свойство (**)
        ```
        """
        ...
    @AutoSegmentation.setter
    def AutoSegmentation(self, value: bool): ...

    @property
    def CurvesCount(self) -> int:  # isheetmetallinearruledshell_curvescount.html
        """
        ## CurvesCount - Количество кривых в сегментации

        Тип данных: long

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        CurvesCount = Object.CurvesCount       # Получить свойство (* )
        CurvesCount = Object.GetCurvesCount()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def EdgesCount(self) -> int:  # isheetmetallinearruledshell_edgescount.html
        """
        ## EdgesCount - Количество ребер

        Тип данных: long

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        EdgesCount = Object.EdgesCount       # Получить свойство (* )
        EdgesCount = Object.GetEdgesCount()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def Sketch2(self) -> ISketch:  # isheetmetallinearruledshell_sketch2.html
        """
        ## Sketch2 - Основание 2

        Тип данных: Указатель на интерфейс ISketch

        ### Синтаксис Automation

        ```
        Sketch2 = Object.Sketch2       # Получить свойство (* )
        Object.Sketch2 = Sketch2       # Установить свойство (* )
        Sketch2 = Object.GetSketch2()  # Получить свойство (**)
        Object.SetSketch2( Sketch2 )   # Установить свойство (**)
        ```
        """
        ...
    @Sketch2.setter
    def Sketch2(self, value: ISketch): ...

    @property
    def UseCommonSegmentationParameters(self) -> bool:  # isheetmetallinearruledshell_usecommonsegmentationparameters.html
        """
        ## UseCommonSegmentationParameters - Единые параметры сегментации

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        UseCommonSegmentationParameters = Object.UseCommonSegmentationParameters       # Получить свойство (* )
        Object.UseCommonSegmentationParameters = UseCommonSegmentationParameters       # Установить свойство (* )
        UseCommonSegmentationParameters = Object.GetUseCommonSegmentationParameters()  # Получить свойство (**)
        Object.SetUseCommonSegmentationParameters( UseCommonSegmentationParameters )   # Установить свойство (**)
        ```
        """
        ...
    @UseCommonSegmentationParameters.setter
    def UseCommonSegmentationParameters(self, value: bool): ...

    def AddNewEdge(self, IndexAt) -> bool:  # isheetmetallinearruledshell_addnewedge.html
        """
        ## AddNewEdge - Добавление ребра после указанного индексом

        ### Синтаксис Automation

        ```
        BOOL AddNewEdge( long IndexAt );
        ```

        ### Входные параметры

        +---------------------+
        | IndexAt | - индекс. |
        +---------------------+

        ### Возвращаемое значение

        +-----------------------------------------+
        | TRUE | - в случае успешного завершения. |
        +-----------------------------------------+
        """
        ...

    def CurveSegmentationMethod(self, CurveIndex) -> int:  # isheetmetallinearruledshell_curvesegmentationmethod.html
        """
        ## CurveSegmentationMethod - Способ сегментации эскиза

        Тип данных: из перечисления ksSegmentationMethodEnum

        ### Синтаксис Automation

        ```
        CurveSegmentationMethod = Object.CurveSegmentationMethod( CurveIndex       # Получить свойство (* )
        Object.CurveSegmentationMethod( CurveIndex ) = CurveSegmentationMethod     # Установить свойство (* )
        CurveSegmentationMethod = Object.GetCurveSegmentationMethod( CurveIndex )  # Получить свойство (**)
        Object.SetCurveSegmentationMethod( CurveIndex, CurveSegmentationMethod )   # Установить свойство (**)
        ```

        ### Входные параметры

        +------------------------------------+
        | long CurveIndex | - индекс кривой. |
        +------------------------------------+
        """
        ...

    def CurveSegmentationSplitValue(self, CurveIndex) -> float:  # isheetmetallinearruledshell_curvesegmentationsplitvalue.html
        """
        ## CurveSegmentationSplitValue - Величина, определяющая разбиения, интерпретация зависит от типа разбиения: количество сегментов либо длина сегмента

        Тип данных: double

        ### Синтаксис Automation

        ```
        CurveSegmentationSplitValue = Object.CurveSegmentationSplitValue( CurveIndex )     # Получить свойство (* )
        Object.CurveSegmentationSplitValue( CurveIndex ) = CurveSegmentationSplitValue     # Установить свойство (* )
        CurveSegmentationSplitValue = Object.GetCurveSegmentationSplitValue( CurveIndex )  # Получить свойство (**)
        Object.SetCurveSegmentationSplitValue( CurveIndex, CurveSegmentationSplitValue )   # Установить свойство (**)
        ```

        ### Входные параметры

        +------------------------------------+
        | long CurveIndex | - индекс кривой. |
        +------------------------------------+
        """
        ...

    def CurveUseSegmentation(self, CurveIndex) -> bool:  # isheetmetallinearruledshell_curveusesegmentation.html
        """
        ## CurveUseSegmentation - Сегментация

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        CurveUseSegmentation = Object.CurveUseSegmentation( CurveIndex )     # Получить свойство (* )
        Object.CurveUseSegmentation( CurveIndex ) = CurveUseSegmentation     # Установить свойство (* )
        CurveUseSegmentation = Object.GetCurveUseSegmentation( CurveIndex )  # Получить свойство (**)
        Object.SetCurveUseSegmentation( CurveIndex, CurveUseSegmentation )   # Установить свойство (**)
        ```

        ### Входные параметры

        +------------------------------------+
        | long CurveIndex | - индекс кривой. |
        +------------------------------------+
        """
        ...

    def DeleteEdge(self, Index) -> bool:  # isheetmetallinearruledshell_deleteedge.html
        """
        ## DeleteEdge - Удаление ребра

        ### Синтаксис Automation

        ```
        BOOL DeleteEdge( long Index );
        ```

        ### Входные параметры

        +-------------------+
        | Index | - индекс. |
        +-------------------+

        ### Возвращаемое значение

        +-----------------------------------------+
        | TRUE | - в случае успешного завершения. |
        +-----------------------------------------+
        """
        ...

    def GetEdgePointParam(self, EdgeIndex, StartPoint) -> bool:  # isheetmetallinearruledshell_getedgepointparam.html
        """
        ## GetEdgePointParam - Получение параметров точки ребра

        ### Синтаксис Automation

        ```
        BOOL GetEdgePointParam( long EdgeIndex, BOOL StartPoint, double * X, double * Y, double * Z, double * T, IModelObject * * AssociateVertex );
        ```

        ### Входные параметры

        +------------------------------------------------------------------+
        | EdgeIndex  | - индекс ребра.                                     |
        +------------------------------------------------------------------+
        | StartPoint | - TRUE - начальная точка, - FALSE - конечная точка. |
        +------------------------------------------------------------------+

        ### Выходные параметры

        +------------------------------------------+
        | X, Y, Z         | - координаты.          |
        +------------------------------------------+
        | T               | - параметр кривой.     |
        +------------------------------------------+
        | AssociateVertex | - ассоциативный объект |
        +------------------------------------------+

        ### Возвращаемое значение

        +-----------------------------------------+
        | TRUE | - в случае успешного завершения. |
        +-----------------------------------------+
        """
        ...

    def GetEdgePointParams(self) -> bool:  # isheetmetallinearruledshell_getedgepointparams.html
        """
        ## GetEdgePointParams - Получение параметров ребер

        ### Синтаксис Automation

        ```
        BOOL GetEdgePointParams( VARIANT * Points1, VARIANT * T1, VARIANT * AssociateVertexes1, VARIANT * Points2, VARIANT * T2, VARIANT * AssociateVertexes2 );
        ```

        ### Выходные параметры

        +------------------------------------------------------------------------------------------------------------------------------+
        | Points1            | - массив SAFEARRAY VT_R8 - VT_ARRAY | VT_R8 начальных координат вершин.                                 |
        +------------------------------------------------------------------------------------------------------------------------------+
        | T1                 | - массив SAFEARRAY VT_R8 - VT_ARRAY | VT_R8 параметров вершин.                                          |
        +------------------------------------------------------------------------------------------------------------------------------+
        | AssociateVertexes1 | - массив SAFEARRAY DISPATCH - VT_ARRAY | VT_DISPATCH вершин первой направляющей, связанных с вершинами. |
        +------------------------------------------------------------------------------------------------------------------------------+
        | Points2            | - массив SAFEARRAY VT_R8 - VT_ARRAY | VT_R8 координат вершин ребер на второй направляющей.              |
        +------------------------------------------------------------------------------------------------------------------------------+
        | T2                 | - массив SAFEARRAY VT_R8 - VT_ARRAY | VT_R8 параметров вершин ребер на первой направляющей.             |
        +------------------------------------------------------------------------------------------------------------------------------+
        | AssociateVertexes2 | массив SAFEARRAY DISPATCH - VT_ARRAY | VT_DISPATCH вершин второй направляющей, связанных с вершинами.   |
        +------------------------------------------------------------------------------------------------------------------------------+

        ### Возвращаемое значение

        +-----------------------------------------+
        | TRUE | - в случае успешного завершения. |
        +-----------------------------------------+
        """
        ...

    def SetCurveSegmentationMethod(self, CurveIndex, arg1):
        ...

    def SetCurveSegmentationSplitValue(self, CurveIndex, arg1):
        ...

    def SetCurveUseSegmentation(self, CurveIndex, arg1):
        ...

    def SetEdgePointParam(self, EdgeIndex, StartPoint, X, Y, Z, T, AssociateVertex) -> bool:  # isheetmetallinearruledshell_setedgepointparam.html
        """
        ## SetEdgePointParam - Изменение положения точки ребра

        ### Синтаксис Automation

        ```
        BOOL SetEdgePointParam( long EdgeIndex, BOOL StartPoint, double X, double Y, double Z, double * T, IModelObject * AssociateVertex );
        ```

        ### Входные параметры

        +-----------------------------------------------------------------------+
        | EdgeIndex       | - индекс ребра.                                     |
        +-----------------------------------------------------------------------+
        | StartPoint      | - TRUE - начальная точка, - FALSE - конечная точка. |
        +-----------------------------------------------------------------------+
        | X, Y, Z         | - координаты.                                       |
        +-----------------------------------------------------------------------+
        | T               | - параметр кривой.                                  |
        +-----------------------------------------------------------------------+
        | AssociateVertex | - ассоциативный объект                              |
        +-----------------------------------------------------------------------+

        ### Возвращаемое значение

        +-----------------------------------------+
        | TRUE | - в случае успешного завершения. |
        +-----------------------------------------+
        """
        ...




class ISheetMetalRib(IModelObject):  # isheetmetalrib.html
    """
    ## Интерфейс ISheetMetalRib

    Интерфейс операции Ребро усиления.
    """

    @property
    def Angle1(self) -> float:  # isheetmetalrib_angle1.html
        """
        ## Angle1 - Угол наклона профиля или угол ребра

        Тип данных: double

        ### Синтаксис Automation

        ```
        Angle1 = Object.Angle1;       # Получить свойство (* )
        Object.Angle1 = Angle1;       # Установить свойство (* )
        Angle1 = Object.GetAngle1();  # Получить свойство (**)
        Object.SetAngle1( Angle1 );   # Установить свойство (**)
        ```
        """
        ...
    @Angle1.setter
    def Angle1(self, value: float): ...

    @property
    def Angle2(self) -> float:  # isheetmetalrib_angle2.html
        """
        ## Angle2 - Угол наклона сечения

        Тип данных: double

        ### Синтаксис Automation

        ```
        Angle2 = Object.Angle2;       # Получить свойство (* )
        Object.Angle2 = Angle2;       # Установить свойство (* )
        Angle2 = Object.GetAngle2();  # Получить свойство (**)
        Object.SetAngle2( Angle2 );   # Установить свойство (**)
        ```
        """
        ...
    @Angle2.setter
    def Angle2(self, value: float): ...

    @property
    def ArchCoefficient(self) -> float:  # isheetmetalrib_archcoefficient.html
        """
        ## ArchCoefficient - Относительная глубина прогиба в%

        Тип данных: double

        ### Синтаксис Automation

        ```
        ArchCoefficient = Object.ArchCoefficient      # Получить свойство (* )
        Object.ArchCoefficient = ArchCoefficient      # Установить свойство (* )
        ArchAngle = Object.GetArchCoefficient()       # Получить свойство (**)
        Object.SetArchCoefficient( ArchCoefficient )  # Установить свойство (**)
        ```
        """
        ...
    @ArchCoefficient.setter
    def ArchCoefficient(self, value: float): ...

    @property
    def ArchCreate(self) -> bool:  # isheetmetalrib_archcreate.html
        """
        ## ArchCreate - Создавать прогиб

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        ArchCreate = Object.ArchCreate       # Получить свойство (* )
        Object.ArchCreate = ArchCreate       # Установить свойство (* )
        ArchCreate = Object.GetArchCreate()  # Получить свойство (**)
        Object.SetArchCreate( ArchCreate )   # Установить свойство (**)
        ```
        """
        ...
    @ArchCreate.setter
    def ArchCreate(self, value: bool): ...

    @property
    def ArchLength(self) -> float:  # isheetmetalrib_archlength.html
        """
        ## ArchLength - Глубина прогиба

        Тип данных: double

        ### Синтаксис Automation

        ```
        ArchLength = Object.ArchLength       # Получить свойство (* )
        Object.ArchLength = ArchLength       # Установить свойство (* )
        ArchLength = Object.GetArchLength()  # Получить свойство (**)
        Object.SetArchLength( ArchLength )   # Установить свойство (**)
        ```
        """
        ...
    @ArchLength.setter
    def ArchLength(self, value: float): ...

    @property
    def ArchMeasure(self) -> int:  # isheetmetalrib_archmeasure.html
        """
        ## ArchMeasure - Способ задания глубины прогиба

        Тип данных: из перечисления ksArchMeasureEnum

        ### Синтаксис Automation

        ```
        ArchMeasure = Object.ArchMeasure       # Получить свойство (* )
        Object.ArchMeasure = ArchMeasure       # Установить свойство (* )
        ArchMeasure = Object.GetArchMeasure()  # Получить свойство (**)
        Object.SetArchMeasure( ArchMeasure )   # Установить свойство (**)
        ```
        """
        ...
    @ArchMeasure.setter
    def ArchMeasure(self, value: int): ...

    @property
    def ArchRadius(self) -> float:  # isheetmetalrib_archradius.html
        """
        ## ArchRadius - Радиус прогиба

        Тип данных: double

        ### Синтаксис Automation

        ```
        ArchRadius = Object.ArchRadius       # Получить свойство (* )
        Object.ArchRadius = ArchRadius       # Установить свойство (* )
        ArchRadius = Object.GetArchRadius()  # Получить свойство (**)
        Object.SetArchRadius( ArchRadius )   # Установить свойство (**)
        ```
        """
        ...
    @ArchRadius.setter
    def ArchRadius(self, value: float): ...

    @property
    def BendEdge(self) -> IModelObject:  # isheetmetalrib_bendedge.html
        """
        ## BendEdge - Ребро сгиба

        Тип данных: Указатель на интерфейс IModelObject.

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        BendEdge = Object.BendEdge( NewVal );     # Получить свойство (* )
        BendEdge = Object.GetBendEdge( NewVal );  # Получить свойство (**)
        ```
        """
        ...

    @property
    def BendFace(self) -> IModelObject:  # isheetmetalrib_bendface.html
        """
        ## BendFace - Грань сгиба

        Тип данных: Указатель на интерфейс IModelObject.

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        BendFace = Object.BendFace( NewVal );     # Получить свойство (* )
        BendFace = Object.GetBendFace( NewVal );  # Получить свойство (**)
        ```
        """
        ...

    @property
    def BendObject(self) -> IModelObject:  # isheetmetalrib_bendobject.html
        """
        ## BendObject - Сгиб

        Тип данных: Указатель на интерфейс IModelObject.

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        BendObject = Object.BendObject;       # Получить свойство (* )
        BendObject = Object.GetBendObject();  # Получить свойство (**)
        ```
        """
        ...

    @property
    def BuildingType(self) -> int:  # isheetmetalrib_buildingtype.html
        """
        ## BuildingType - Способ построения

        Тип данных: Значение из перечисления ksSHRibBuildingTypeEnum.

        ### Синтаксис Automation

        ```
        BuildingType = Object.BuildingType;       # Получить свойство (* )
        Object.BuildingType = BuildingType;       # Установить свойство (* )
        BuildingType = Object.GetBuildingType();  # Получить свойство (**)
        Object.SetBuildingType( BuildingType );   # Установить свойство (**)
        ```
        """
        ...
    @BuildingType.setter
    def BuildingType(self, value: int): ...

    @property
    def CutingType(self) -> int:  # isheetmetalrib_cutingtype.html
        """
        ## CutingType - Форма сечения

        Тип данных: Значение из перечисления ksSHRibCutingTypeEnum.

        ### Синтаксис Automation

        ```
        CutingType = Object.CutingType;       # Получить свойство (* )
        Object.CutingType = CutingType;       # Установить свойство (* )
        CutingType = Object.GetCutingType();  # Получить свойство (**)
        Object.SetCutingType( CutingType );   # Установить свойство (**)
        ```
        """
        ...
    @CutingType.setter
    def CutingType(self, value: int): ...

    @property
    def Direction(self) -> bool:  # isheetmetalrib_direction.html
        """
        ## Direction - Направление смещения вдоль кривой сгиба

        Тип данных: BOOL

        Значение свойства:

        +---------------------+
        | TRUE  | - прямое,   |
        +---------------------+
        | FALSE | - обратное. |
        +---------------------+

        ### Синтаксис Automation

        ```
        Direction = Object.Direction;       # Получить свойство (* )
        Object.Direction = Direction;       # Установить свойство (* )
        Direction = Object.GetDirection();  # Получить свойство (**)
        Object.SetDirection( Direction );   # Установить свойство (**)
        ```
        """
        ...
    @Direction.setter
    def Direction(self, value: bool): ...

    @property
    def Lenght1(self) -> float:  # isheetmetalrib_lenght1.html
        """
        ## Lenght1 - Длина 1 или глубина ребра

        Тип данных: double

        ### Синтаксис Automation

        ```
        Lenght1 = Object.Lenght1;       # Получить свойство (* )
        Object.Lenght1 = Lenght1;       # Установить свойство (* )
        Lenght1 = Object.GetLenght1();  # Получить свойство (**)
        Object.SetLenght1( Lenght1 );   # Установить свойство (**)
        ```
        """
        ...
    @Lenght1.setter
    def Lenght1(self, value: float): ...

    @property
    def Lenght2(self) -> float:  # isheetmetalrib_lenght2.html
        """
        ## Lenght2 - Длина 2

        Тип данных: double

        ### Синтаксис Automation

        ```
        Lenght2 = Object.Lenght2;       # Получить свойство (* )
        Object.Lenght2 = Lenght2;       # Установить свойство (* )
        Lenght2 = Object.GetLenght2();  # Получить свойство (**)
        Object.SetLenght1( Lenght2 );   # Установить свойство (**)
        ```
        """
        ...
    @Lenght2.setter
    def Lenght2(self, value: float): ...

    @property
    def Offset(self) -> float:  # isheetmetalrib_offset.html
        """
        ## Offset - Величина смещения вдоль кривой сгиба

        Тип данных: double

        ### Синтаксис Automation

        ```
        Offset = Object.Offset;       # Получить свойство (* )
        Object.Offset = Offset;       # Установить свойство (* )
        Offset = Object.GetOffset();  # Получить свойство (**)
        Object.SetOffset( Offset);    # Установить свойство (**)
        ```
        """
        ...
    @Offset.setter
    def Offset(self, value: float): ...

    @property
    def OffsetType(self) -> int:  # isheetmetalrib_offsettype.html
        """
        ## OffsetType - Тип задания смещения вдоль кривой сгиба

        Тип данных: Значение из перечисления ksPoint3DCurveParamTypeEnum.

        ### Синтаксис Automation

        ```
        OffsetType = Object.OffsetType;       # Получить свойство (* )
        Object.OffsetType = OffsetType;       # Установить свойство (* )
        OffsetType = Object.GetOffsetType();  # Получить свойство (**)
        Object.SetOffsetType( OffsetType );   # Установить свойство (**)
        ```
        """
        ...
    @OffsetType.setter
    def OffsetType(self, value: int): ...

    @property
    def Radius1(self) -> float:  # isheetmetalrib_radius1.html
        """
        ## Radius1 - Радиус ребра

        Тип данных: double

        ### Синтаксис Automation

        ```
        Radius1 = Object.Radius1;       # Получить свойство (* )
        Object.Radius1 = Radius1;       # Установить свойство (* )
        Radius1 = Object.GetRadius1();  # Получить свойство (**)
        Object.SetRadius1( Radius1 );   # Установить свойство (**)
        ```
        """
        ...
    @Radius1.setter
    def Radius1(self, value: float): ...

    @property
    def Radius2(self) -> float:  # isheetmetalrib_radius2.html
        """
        ## Radius2 - Радиус скругления основания

        Тип данных: double

        ### Синтаксис Automation

        ```
        Radius2 = Object.Radius2;       # Получить свойство (* )
        Object.Radius2 = Radius2;       # Установить свойство (* )
        Radius2 = Object.GetRadius2();  # Получить свойство (**)
        Object.SetRadius2( Radius2 );   # Установить свойство (**)
        ```
        """
        ...
    @Radius2.setter
    def Radius2(self, value: float): ...

    @property
    def Round(self) -> bool:  # isheetmetalrib_round.html
        """
        ## Round - Скругление основания

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        Round = Object.Round;       # Получить свойство (* )
        Object.Round = Round;       # Установить свойство (* )
        Round = Object.GetRound();  # Получить свойство (**)
        Object.SetRound( Round );   # Установить свойство (**)
        ```
        """
        ...
    @Round.setter
    def Round(self, value: bool): ...

    @property
    def Width(self) -> float:  # isheetmetalrib_width.html
        """
        ## Width - Ширина основания

        Тип данных: double

        ### Синтаксис Automation

        ```
        Width = Object.Width;       # Получить свойство (* )
        Object.Width = Width;       # Установить свойство (* )
        Width = Object.GetWidth();  # Получить свойство (**)
        Object.SetWidth( Width );   # Установить свойство (**)
        ```
        """
        ...
    @Width.setter
    def Width(self, value: float): ...

    def AutoInitBendObjects(self, InitObject) -> bool:  # isheetmetalrib_autoinitbendobjects.html
        """
        ## AutoInitBendObjects - Автоматическая инициализация объектов сгиба

        ### Синтаксис Automation

        ```
        BOOL AutoInitBendObjects( IModelObject * InitObject );
        ```

        ### Входные параметры

        +---------------------------------------+
        | InitObject | - грань или ребро сгиба. |
        +---------------------------------------+

        ### Возвращаемое значение

        +-----------------------------------------+
        | TRUE | - в случае успешного завершения. |
        +-----------------------------------------+
        """
        ...

    def CalculateOptimalParams(self) -> bool:  # isheetmetalrib_calculateoptimalparams.html
        """
        ## CalculateOptimalParams - Вычислить оптимальные параметры профиля по сгибу

        Метод пересчитывает глубину, длины 1, 2 и угол наклона профиля в зависимости от выбранного сгиба и типа профиля.

        ### Синтаксис Automation

        ```
        BOOL CalculateOptimalParams();
        ```

        ### Возвращаемое значение

        +-----------------------------------------+
        | TRUE | - в случае успешного завершения. |
        +-----------------------------------------+
        """
        ...

    def InitBendObjects(self, BendObject, BendFace, BendEdge) -> bool:  # isheetmetalrib_initbendobjects.html
        """
        ## InitBendObjects - Инициализация объектов сгиба

        ### Синтаксис Automation

        ```
        BOOL InitBendObjects( IModelObject * BendObject, IModelObject * BendFace, IModelObject * BendEdge );
        ```

        ### Входные параметры

        +--------------------------------------------------------------------------------------+
        | BendObject | - операция листового тела - сгиб (Сгиб, Сгиб по линии, Сгиб по эскизу), |
        +--------------------------------------------------------------------------------------+
        | BendFace   | - грань сгиба,                                                          |
        +--------------------------------------------------------------------------------------+
        | BendEdge   | - ребро сгиба.                                                          |
        +--------------------------------------------------------------------------------------+

        ### Возвращаемое значение

        +-----------------------------------------+
        | TRUE | - в случае успешного завершения. |
        +-----------------------------------------+
        """
        ...




class ISheetMetalSketchBends(IModelObjects):  # isheetmetalsketchbends.html
    """
    ## Интерфейс ISheetMetalSketchBends

    Интерфейс коллекции элементов Сгиб по эскизу.
    """

    def Add(self) -> ISheetMetalSketchBend:  # isheetmetalsketchbends_add.html
        """
        ## Add - Создает новый элемент и добавляет его в коллекцию

        1. Метод позволяет создать новый интерфейс операции cгиб по эскизу.

        2. После получения нового интерфейса нужно задать параметры операции и вызвать метод IModelObject::Update.

        ### Синтаксис Automation

        ```
        LPDISPATCH Add();
        ```

        ### Возвращаемое значение

        - указатель на интерфейс ISheetMetalSketchBend.
        """
        ...

    def SheetMetalSketchBend(self, Index) -> ISheetMetalSketchBend:  # isheetmetalsketchbends_sheetmetalsketchbend.html
        """
        ## SheetMetalSketchBend - Возвращает элемент, заданный по индексу

        Тип данных: Указатель на интерфейс ISheetMetalSketchBend.

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        SheetMetalSketchBend = Object.SheetMetalSketchBend( Index )     # Получить свойство (* )
        SheetMetalSketchBend = Object.GetSheetMetalSketchBend( Index )  # Получить свойство (**)
        ```

        ### Входные параметры

        +--------------------------------------------+
        | VARIANT Index | - индекс или имя элемента. |
        +--------------------------------------------+
        """
        ...




class ISheetMetalSketchBend(IModelObject):  # isheetmetalsketchbend.html
    """
    ## Интерфейс ISheetMetalSketchBend

    Интерфейс параметров операции Сгиб по эскизу.
    """

    @property
    def BendCoefficient(self) -> float:  # isheetmetalsketchbend_bendcoefficient.html
        """
        ## BendCoefficient - Коэффициент нейтрального слоя

        Тип данных: double

        ### Синтаксис Automation

        ```
        BendCoefficient = Object.BendCoefficient;       # Получить свойство (* )
        Object.BendCoefficient = BendCoefficient;       # Установить свойство (* )
        BendCoefficient = Object.GetBendCoefficient();  # Получить свойство (**)
        Object.SetBendCoefficient( BendCoefficient );   # Установить свойство (**)
        ```
        """
        ...
    @BendCoefficient.setter
    def BendCoefficient(self, value: float): ...

    @property
    def BendReduction(self) -> float:  # isheetmetalsketchbend_bendreduction.html
        """
        ## BendReduction - Уменьшение сгиба

        Тип данных: double

        ### Синтаксис Automation

        ```
        BendReduction = Object.BendReduction;       # Получить свойство (* )
        Object.BendReduction = BendReduction;       # Установить свойство (* )
        BendReduction = Object.GetBendReduction();  # Получить свойство (**)
        Object.SetBendReduction( BendReduction );   # Установить свойство (**)
        ```
        """
        ...
    @BendReduction.setter
    def BendReduction(self, value: float): ...

    @property
    def BendRelease(self) -> int:  # isheetmetalsketchbend_bendrelease.html
        """
        ## BendRelease - Тип освобождения cгиба

        Тип данных: Значение из перечисления ksBendReleaseTypeEnum.

        ### Синтаксис Automation

        ```
        BendRelease = Object.BendRelease;       # Получить свойство (* )
        Object.BendRelease = BendRelease;       # Установить свойство (* )
        BendRelease = Object.GetBendRelease();  # Получить свойство (**)
        Object.SetBendRelease( BendRelease );   # Установить свойство (**)
        ```
        """
        ...
    @BendRelease.setter
    def BendRelease(self, value: int): ...

    @property
    def BendTablePath(self) -> str:  # isheetmetalsketchbend_bendtablepath.html
        """
        ## BendTablePath - Имя файла таблицы сгибов

        Тип данных: BSTR

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        BendTablePath = Object.BendTablePath       # Получить свойство (* )
        BendTablePath = Object.GetBendTablePath()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def BendValue(self) -> float:  # isheetmetalsketchbend_bendvalue.html
        """
        ## BendValue - Величина сгиба

        Тип данных: double

        ### Синтаксис Automation

        ```
        BendValue = Object.BendValue;       # Получить свойство (* )
        Object.BendValue = BendValue;       # Установить свойство (* )
        BendValue = Object.GetBendValue();  # Получить свойство (**)
        Object.SetBendValue( BendValue );   # Установить свойство (**)
        ```
        """
        ...
    @BendValue.setter
    def BendValue(self, value: float): ...

    @property
    def BuildingType(self) -> int:  # isheetmetalsketchbend_buildingtype.html
        """
        ## BuildingType - Способ построения сгиба по эскизу

        Тип данных: из перечисления ksSketchBendBuildingTypeEnum

        ### Синтаксис Automation

        ```
        BuildingType = Object.BuildingType;       # Получить свойство (* )
        Object.BuildingType = BuildingType;       # Установить свойство (* )
        BuildingType = Object.GetBuildingType();  # Получить свойство (**)
        Object.SetBuildingType( BuildingType );   # Установить свойство (**)
        ```
        """
        ...
    @BuildingType.setter
    def BuildingType(self, value: int): ...

    @property
    def ClosingHoleDiameter(self) -> float:  # isheetmetalsketchbend_closingholediameter.html
        """
        ## ClosingHoleDiameter - Диаметр отверстия при замыкании угла

        Тип данных: double

        ### Синтаксис Automation

        ```
        ClosingHoleDiameter = Object.ClosingHoleDiameter;       # Получить свойство (* )
        Object.ClosingHoleDiameter = ClosingHoleDiameter;       # Установить свойство (* )
        ClosingHoleDiameter = Object.GetClosingHoleDiameter();  # Получить свойство (**)
        Object.SetClosingHoleDiameter( ClosingHoleDiameter );   # Установить свойство (**)
        ```
        """
        ...
    @ClosingHoleDiameter.setter
    def ClosingHoleDiameter(self, value: float): ...

    @property
    def ClosingHoleOffset(self) -> float:  # isheetmetalsketchbend_closingholeoffset.html
        """
        ## ClosingHoleOffset - Смещение отверстия при замыкании угла

        Тип данных: double

        ### Синтаксис Automation

        ```
        ClosingHoleOffset = Object.ClosingHoleOffset;       # Получить свойство (* )
        Object.ClosingHoleOffset = ClosingHoleOffset;       # Установить свойство (* )
        ClosingHoleOffset = Object.GetClosingHoleOffset();  # Получить свойство (**)
        Object.SetClosingHoleOffset( ClosingHoleOffset );   # Установить свойство (**)
        ```
        """
        ...
    @ClosingHoleOffset.setter
    def ClosingHoleOffset(self, value: float): ...

    @property
    def ClosingHolePlacement(self) -> int:  # isheetmetalsketchbend_closingholeplacement.html
        """
        ## ClosingHolePlacement - Размещение отверстия при круговой обработке угла

        Тип данных: Значение из перечисления ksClosingHolePlacementEnum.

        ### Синтаксис Automation

        ```
        ClosingHolePlacement = Object.ClosingHolePlacement;       # Получить свойство (* )
        Object.ClosingHolePlacement = ClosingHolePlacement;       # Установить свойство (* )
        ClosingHolePlacement = Object.GetClosingHolePlacement();  # Получить свойство (**)
        Object.SetClosingHolePlacement( ClosingHolePlacement );   # Установить свойство (**)
        ```
        """
        ...
    @ClosingHolePlacement.setter
    def ClosingHolePlacement(self, value: int): ...

    @property
    def Direction(self) -> int:  # isheetmetalsketchbend_direction.html
        """
        ## Direction - Направление построения

        Тип данных: Значение из перечисления ksDirectionTypeEnum.

        ### Синтаксис Automation

        ```
        Direction = Object.Direction;       # Получить свойство (* )
        Object.Direction = Direction;       # Установить свойство (* )
        Direction = Object.GetDirection();  # Получить свойство (**)
        Object.SetDirection( Direction );   # Установить свойство (**)
        ```
        """
        ...
    @Direction.setter
    def Direction(self, value: int): ...

    @property
    def DismissalAngleType(self) -> int:  # isheetmetalsketchbend_dismissalangletype.html
        """
        ## DismissalAngleType - Способ освобождения угла cгиба

        Тип данных: Значение из перечисления ksBendAngleReleaseTypeEnum.

        ### Синтаксис Automation

        ```
        DismissalAngleType = Object.DismissalAngleType;       # Получить свойство (* )
        Object.DismissalAngleType = DismissalAngleType;       # Установить свойство (* )
        DismissalAngleType = Object.GetDismissalAngleType();  # Получить свойство (**)
        Object.SetDismissalAngleType( DismissalAngleType );   # Установить свойство (**)
        ```
        """
        ...
    @DismissalAngleType.setter
    def DismissalAngleType(self, value: int): ...

    @property
    def DismissalDepth(self) -> float:  # isheetmetalsketchbend_dismissaldepth.html
        """
        ## DismissalDepth - Глубина разгрузки сгиба

        Тип данных: double

        ### Синтаксис Automation

        ```
        DismissalDepth = Object.DismissalDepth;       # Получить свойство (* )
        Object.DismissalDepth = DismissalDepth;       # Установить свойство (* )
        DismissalDepth = Object.GetDismissalDepth();  # Получить свойство (**)
        Object.SetDismissalDepth( DismissalDepth );   # Установить свойство (**)
        ```
        """
        ...
    @DismissalDepth.setter
    def DismissalDepth(self, value: float): ...

    @property
    def DismissalWidth(self) -> float:  # isheetmetalsketchbend_dismissalwidth.html
        """
        ## DismissalWidth - Ширина разгрузки сгиба

        Тип данных: double

        ### Синтаксис Automation

        ```
        DismissalWidth = Object.DismissalWidth;       # Получить свойство (* )
        Object.DismissalWidth = DismissalWidth;       # Установить свойство (* )
        DismissalWidth = Object.GetDismissalWidth();  # Получить свойство (**)
        Object.SetDismissalWidth( DismissalWidth );   # Установить свойство (**)
        ```
        """
        ...
    @DismissalWidth.setter
    def DismissalWidth(self, value: float): ...

    @property
    def DismissalWithWidth(self) -> bool:  # isheetmetalsketchbend_dismissalwithwidth.html
        """
        ## DismissalWithWidth - Учитывать ширину

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        DismissalWithWidth = Object.DismissalWithWidth;       # Получить свойство (* )
        Object.DismissalWithWidth = DismissalWithWidth;       # Установить свойство (* )
        DismissalWithWidth = Object.GetDismissalWithWidth();  # Получить свойство (**)
        Object.SetDismissalWithWidth( DismissalWithWidth );   # Установить свойство (**)
        ```
        """
        ...
    @DismissalWithWidth.setter
    def DismissalWithWidth(self, value: bool): ...

    @property
    def Edges(self) -> typing.Any:  # isheetmetalsketchbend_edges.html
        """
        ## Edges - Массив ребер в виде SAFEARRAY DISPATCH - VT_ARRAY | VT_DISPATCH

        Тип данных: VARIANT

        Массив ребер, вдоль которых должен располагаться сгиб.

        ### Синтаксис Automation

        ```
        Edges = Object.Edges;       # Получить свойство (* )
        Object.Edges = Edges;       # Установить свойство (* )
        Edges = Object.GetEdges();  # Получить свойство (**)
        Object.SetEdges( Edges );   # Установить свойство (**)
        ```
        """
        ...
    @Edges.setter
    def Edges(self, value: typing.Any): ...

    @property
    def InternalRadius(self) -> bool:  # isheetmetalsketchbend_internalradius.html
        """
        ## InternalRadius - Внутренний радиус

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        InternalRadius = Object.InternalRadius;       # Получить свойство (* )
        Object.InternalRadius = InternalRadius;       # Установить свойство (* )
        InternalRadius = Object.GetInternalRadius();  # Получить свойство (**)
        Object.SetInternalRadius( InternalRadius );   # Установить свойство (**)
        ```
        """
        ...
    @InternalRadius.setter
    def InternalRadius(self, value: bool): ...

    @property
    def Radius(self) -> float:  # isheetmetalsketchbend_radius.html
        """
        ## Radius - Радиус сгиба

        Тип данных: double

        ### Синтаксис Automation

        ```
        Radius = Object.Radius;       # Получить свойство (* )
        Object.Radius = Radius;       # Установить свойство (* )
        Radius = Object.GetRadius();  # Получить свойство (**)
        Object.SetRadius( Radius );   # Установить свойство (**)
        ```
        """
        ...
    @Radius.setter
    def Radius(self, value: float): ...

    @property
    def Sketch(self) -> IModelObject:  # isheetmetalsketchbend_sketch.html
        """
        ## Sketch - Эскиз

        Тип данных: Указатель на интерфейс IModelObject.

        Эскиз определяет профиль сгиба.

        ### Синтаксис Automation

        ```
        Sketch = Object.Sketch;       # Получить свойство (* )
        Object.Sketch = Sketch;       # Установить свойство (* )
        Sketch = Object.GetSketch();  # Получить свойство (**)
        Object.SetSketch( Sketch );   # Установить свойство (**)
        ```
        """
        ...
    @Sketch.setter
    def Sketch(self, value: IModelObject): ...

    @property
    def Straighten(self) -> bool:  # isheetmetalsketchbend_straighten.html
        """
        ## Straighten - Разогнуть

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        Straighten = Object.Straighten;       # Получить свойство (* )
        Object.Straighten = Straighten;       # Установить свойство (* )
        Straighten = Object.GetStraighten();  # Получить свойство (**)
        Object.SetStraighten( Straighten );   # Установить свойство (**)
        ```
        """
        ...
    @Straighten.setter
    def Straighten(self, value: bool): ...

    @property
    def UnfoldType(self) -> int:  # isheetmetalsketchbend_unfoldtype.html
        """
        ## UnfoldType - Способ определения длины развертки

        Тип данных: Значение из перечисления ksUnfoldTypeEnum.

        ### Синтаксис Automation

        ```
        UnfoldType = Object.UnfoldType;       # Получить свойство (* )
        Object.UnfoldType = UnfoldType;       # Установить свойство (* )
        UnfoldType = Object.GetUnfoldType();  # Получить свойство (**)
        Object.SetUnfoldType( UnfoldType );   # Установить свойство (**)
        ```
        """
        ...
    @UnfoldType.setter
    def UnfoldType(self, value: int): ...

    @property
    def Width1(self) -> float:  # isheetmetalsketchbend_width1.html
        """
        ## Width1 - Ширина в прямом направлении

        Тип данных: double

        ### Синтаксис Automation

        ```
        Width1 = Object.Width1;       # Получить свойство (* )
        Object.Width1 = Width1;       # Установить свойство (* )
        Width1 = Object.GetWidth1();  # Получить свойство (**)
        Object.SetWidth1( Width1 );   # Установить свойство (**)
        ```
        """
        ...
    @Width1.setter
    def Width1(self, value: float): ...

    @property
    def Width2(self) -> float:  # isheetmetalsketchbend_width2.html
        """
        ## Width2 - Ширина в обратном направлении

        Тип данных: double

        ### Синтаксис Automation

        ```
        Width2 = Object.Width2;       # Получить свойство (* )
        Object.Width2 = Width2;       # Установить свойство (* )
        Width2 = Object.GetWidth2();  # Получить свойство (**)
        Object.SetWidth2( Width2 );   # Установить свойство (**)
        ```
        """
        ...
    @Width2.setter
    def Width2(self, value: float): ...

    @property
    def WithoutAngleRelease(self) -> bool:  # isheetmetalsketchbend_withoutanglerelease.html
        """
        ## WithoutAngleRelease - Без освобождения углов

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        WithoutAngleRelease = Object.WithoutAngleRelease;       # Получить свойство (* )
        Object.WithoutAngleRelease = WithoutAngleRelease;       # Установить свойство (* )
        WithoutAngleRelease = Object.GetWithoutAngleRelease();  # Получить свойство (**)
        Object.SetWithoutAngleRelease( WithoutAngleRelease );   # Установить свойство (**)
        ```
        """
        ...
    @WithoutAngleRelease.setter
    def WithoutAngleRelease(self, value: bool): ...

    @property
    def WithoutBendRelease(self) -> bool:  # isheetmetalsketchbend_withoutbendrelease.html
        """
        ## WithoutBendRelease - Без освобождения cгиба

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        WithoutBendRelease = Object.WithoutBendRelease;       # Получить свойство (* )
        Object.WithoutBendRelease = WithoutBendRelease;       # Установить свойство (* )
        WithoutBendRelease = Object.GetWithoutBendRelease();  # Получить свойство (**)
        Object.SetWithoutBendRelease( WithoutBendRelease );   # Установить свойство (**)
        ```
        """
        ...
    @WithoutBendRelease.setter
    def WithoutBendRelease(self, value: bool): ...

    def ClosingAngle(self, Type) -> float:  # isheetmetalsketchbend_closingangle.html
        """
        ## ClosingAngle - Угол при замыкании углов

        Тип данных: double

        ### Синтаксис Automation

        ```
        ClosingAngle = Object.ClosingAngle( Type );     # Получить свойство (* )
        Object.ClosingAngle( Type ) = ClosingAngle;     # Установить свойство (* )
        ClosingAngle = Object.GetClosingAngle( Type );  # Получить свойство (**)
        Object.SetClosingAngle( Type, ClosingAngle );   # Установить свойство (**)
        ```

        ### Входные параметры

        +--------------------------------------------------------------------------------------------------+
        | Type | - тип замыкания операции сгиб по эскизу листового тела из перечисления ksClosingTypeEnum. |
        +--------------------------------------------------------------------------------------------------+
        """
        ...

    def ClosingClosedType(self, Type) -> int:  # isheetmetalsketchbend_closingclosedtype.html
        """
        ## ClosingClosedType - Способ замыкания углов

        Тип данных: Значение из перечисления ksClosingClosedTypeEnum.

        ### Синтаксис Automation

        ```
        ClosingClosedType = Object.ClosingClosedType( Type );     # Получить свойство (* )
        Object.ClosingClosedType( Type ) = ClosingClosedType;     # Установить свойство (* )
        ClosingClosedType = Object.GetClosingClosedType( Type );  # Получить свойство (**)
        Object.SetClosingClosedType( Type, ClosingClosedType );   # Установить свойство (**)
        ```

        ### Входные параметры

        +--------------------------------------------------------------------------------------------------+
        | Type | - тип замыкания операции сгиб по эскизу листового тела из перечисления ksClosingTypeEnum. |
        +--------------------------------------------------------------------------------------------------+
        """
        ...

    def ClosingCorneringType(self, Type) -> int:  # isheetmetalsketchbend_closingcorneringtype.html
        """
        ## ClosingCorneringType - Обработка угла при замыкании

        Тип данных: Значение из перечисления ksClosingCorneringEnum.

        ### Синтаксис Automation

        ```
        ClosingCorneringType = Object.ClosingCorneringType( Type );     # Получить свойство (* )
        Object.ClosingCorneringType( Type ) = ClosingCorneringType;     # Установить свойство (* )
        ClosingCorneringType = Object.GetClosingCorneringType( Type );  # Получить свойство (**)
        Object.SetClosingCorneringType( Type, ClosingCorneringType );   # Установить свойство (**)
        ```

        ### Входные параметры

        +--------------------------------------------------------------------------------------------------+
        | Type | - тип замыкания операции сгиб по эскизу листового тела из перечисления ksClosingTypeEnum. |
        +--------------------------------------------------------------------------------------------------+
        """
        ...

    def ClosingEnable(self, Type):  # isheetmetalsketchbend_closingenable.html
        """
        ## ClosingEnable - Включить замыкание углов

        ### Синтаксис Automation

        ```
        ClosingEnable = Object.ClosingEnable( Type );     # Получить свойство (* )
        Object.ClosingEnable( Type ) = ClosingEnable;     # Установить свойство (* )
        ClosingEnable = Object.GetClosingEnable( Type );  # Получить свойство (**)
        Object.SetClosingEnable( Type, ClosingEnable );   # Установить свойство (**)
        ```

        ### Входные параметры

        +--------------------------------------------------------------------------------------------------+
        | Type | - тип замыкания операции сгиб по эскизу листового тела из перечисления ksClosingTypeEnum. |
        +--------------------------------------------------------------------------------------------------+
        """
        ...

    def ClosingGapValue(self, Type) -> float:  # isheetmetalsketchbend_closinggapvalue.html
        """
        ## ClosingGapValue - Значение зазора при замыкании углов

        Тип данных: double

        ### Синтаксис Automation

        ```
        ClosingGapValue = Object.ClosingGapValue( Type );     # Получить свойство (* )
        Object.ClosingGapValue( Type ) = ClosingGapValue;     # Установить свойство (* )
        ClosingGapValue = Object.GetClosingGapValue( Type );  # Получить свойство (**)
        Object.SetClosingGapValue( Type, ClosingGapValue );   # Установить свойство (**)
        ```

        ### Входные параметры

        +--------------------------------------------------------------------------------------------------+
        | Type | - тип замыкания операции сгиб по эскизу листового тела из перечисления ksClosingTypeEnum. |
        +--------------------------------------------------------------------------------------------------+
        """
        ...

    def SetClosingAngle(self, Type, arg1):
        ...

    def SetClosingClosedType(self, Type, arg1):
        ...

    def SetClosingCorneringType(self, Type, arg1):
        ...

    def SetClosingEnable(self, Type, arg1):
        ...

    def SetClosingGapValue(self, Type, arg1):
        ...




class ISheetMetalPlates(IModelObjects):  # isheetmetalplates.html
    """
    ## Интерфейс ISheetMetalPlates

    Интерфейс коллекции элементов Пластина.
    """

    def Add(self) -> ISheetMetalPlate:  # isheetmetalplates_add.html
        """
        ## Add - Создает новый элемент и добавляет его в коллекцию

        1. Метод позволяет создать новый интерфейс операции “пластина”.

        2. После получения нового интерфейса нужно задать параметры операции и вызвать метод IModelObject::Update.

        ### Синтаксис Automation

        ```
        LPDISPATCH Add();
        ```

        ### Возвращаемое значение

        - указатель на интерфейс ISheetMetalPlate.
        """
        ...

    def SheetMetalPlate(self, Index) -> ISheetMetalPlate:  # isheetmetalplates_sheetmetalplate.html
        """
        ## SheetMetalPlate - Возвращает элемент, заданный по индексу

        Тип данных: Указатель на интерфейс ISheetMetalPlate.

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        SheetMetalPlate = Object.SheetMetalPlate( Index )     # Получить свойство (* )
        SheetMetalPlate = Object.GetSheetMetalPlate( Index )  # Получить свойство (**)
        ```

        ### Входные параметры

        +--------------------------------------------+
        | VARIANT Index | - индекс или имя элемента. |
        +--------------------------------------------+
        """
        ...




class ISheetMetalPlate(IModelObject):  # isheetmetalplate.html
    """
    ## Интерфейс ISheetMetalPlate

    Интерфейс операции Пластина.
    """

    @property
    def IsUserThickness(self) -> bool:  # isheetmetalplate_isuserthickness.html
        """
        ## IsUserThickness - Признак толщины, заданной пользователем

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        IsUserThickness = Object.IsUserThickness;       # Получить свойство (* )
        Object.IsUserThickness = IsUserThickness;       # Установить свойство (* )
        IsUserThickness = Object.GetIsUserThickness();  # Получить свойство (**)
        Object.SetIsUserThickness( IsUserThickness );   # Установить свойство (**)
        ```
        """
        ...
    @IsUserThickness.setter
    def IsUserThickness(self, value: bool): ...

    @property
    def Sketch(self) -> IModelObject:  # isheetmetalplate_sketch.html
        """
        ## Sketch - Эскиз

        Тип данных: Указатель на интерфейс IModelObject.

        ### Синтаксис Automation

        ```
        Sketch = Object.Sketch;       # Получить свойство (* )
        Object.Sketch = Sketch;       # Установить свойство (* )
        Sketch = Object.GetSketch();  # Получить свойство (**)
        Object.SetSketch( Sketch );   # Установить свойство (**)
        ```
        """
        ...
    @Sketch.setter
    def Sketch(self, value: IModelObject): ...

    @property
    def Thickness(self) -> float:  # isheetmetalplate_thickness.html
        """
        ## Thickness - Толщина пластины

        Тип данных: double

        ### Синтаксис Automation

        ```
        Thickness = Object.Thickness;       # Получить свойство (* )
        Object.Thickness = Thickness;       # Установить свойство (* )
        Thickness = Object.GetThickness();  # Получить свойство (**)
        Object.SetThickness( Thickness );   # Установить свойство (**)
        ```
        """
        ...
    @Thickness.setter
    def Thickness(self, value: float): ...

    @property
    def ThicknessObject(self) -> IModelObject:  # isheetmetalplate_thicknessobject.html
        """
        ## ThicknessObject - Объект (листовое тело), задающий толщину

        Тип данных: Указатель на интерфейс IModelObject.

        ### Синтаксис Automation

        ```
        ThicknessObject = Object.ThicknessObject;       # Получить свойство (* )
        Object.ThicknessObject = ThicknessObject;       # Установить свойство (* )
        ThicknessObject = Object.GetThicknessObject();  # Получить свойство (**)
        Object.SetThicknessObject( ThicknessObject )    # Установить свойство (**)
        ```
        """
        ...
    @ThicknessObject.setter
    def ThicknessObject(self, value: IModelObject): ...




class ISheetMetalPressFormings(IModelObjects):  # isheetmetalpressformings.html
    """
    ## Интерфейс ISheetMetalPressFormings

    Интерфейс коллекции элементов Открытая или Закрытая штамповка.
    """

    def Add(self, Type) -> ISheetMetalPressForming:  # isheetmetalpressformings_add.html
        """
        ## Add - Создает новый элемент и добавляет его в коллекцию

        1. Метод позволяет создать новый интерфейс операций Открытая или Закрытая штамповка.

        2. Допустимыми значениями Type являются o3d_sheetMetalDrawnCutout (Закрытая штамповка), o3d_sheetMetalDimpleCutout (Открытая штамповка)

        3. После получения нового интерфейса нужно задать параметры операции и вызвать метод IModelObject::Update.

        ### Синтаксис Automation

        ```
        LPDISPATCH Add( ksObj3dTypeEnum Type );
        ```

        ### Входные параметры

        +-------------------------------------------------------+
        | Type | - тип объекта из перечисления ksObj3dTypeEnum. |
        +-------------------------------------------------------+

        ### Возвращаемое значение

        - указатель на интерфейс ISheetMetalPressForming.
        """
        ...

    def SheetMetalPressForming(self, Index) -> ISheetMetalPressForming:  # isheetmetalpressformings_sheetmetalpressforming.html
        """
        ## SheetMetalPressForming - Возвращает элемент, заданный по индексу

        Тип данных: Указатель на интерфейс ISheetMetalPressForming.

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        SheetMetalPressForming = Object.SheetMetalPressForming( Index )     # Получить свойство (* )
        SheetMetalPressForming = Object.GetSheetMetalPressForming( Index )  # Получить свойство (**)
        ```

        ### Входные параметры

        +--------------------------------------------+
        | VARIANT Index | - индекс или имя элемента. |
        +--------------------------------------------+
        """
        ...




class ISheetMetalPressForming(IModelObject):  # isheetmetalpressforming.html
    """
    ## Интерфейс ISheetMetalPressForming

    Интерфейс операции Открытая /Закрытая штамповка.
    """

    @property
    def Angle(self) -> float:  # isheetmetalpressforming_angle.html
        """
        ## Angle - Угол уклона

        Тип данных: double

        ### Синтаксис Automation

        ```
        Angle = Object.Angle;       # Получить свойство (* )
        Object.Angle = Angle;       # Установить свойство (* )
        Angle = Object.GetAngle();  # Получить свойство (**)
        Object.SetAngle( Angle )    # Установить свойство (**)
        ```
        """
        ...
    @Angle.setter
    def Angle(self, value: float): ...

    @property
    def Direction(self) -> bool:  # isheetmetalpressforming_direction.html
        """
        ## Direction - Направление построения

        Тип данных: BOOL

        Значение свойства:

        +---------------------+
        | TRUE  | - прямое,   |
        +---------------------+
        | FALSE | - обратное. |
        +---------------------+

        ### Синтаксис Automation

        ```
        Direction = Object.Direction;       # Получить свойство (* )
        Object.Direction = Direction;       # Установить свойство (* )
        Direction = Object.GetDirection();  # Получить свойство (**)
        Object.SetDirection( Direction )    # Установить свойство (**)
        ```
        """
        ...
    @Direction.setter
    def Direction(self, value: bool): ...

    @property
    def Height(self) -> float:  # isheetmetalpressforming_height.html
        """
        ## Height - Высота

        Тип данных: double

        ### Синтаксис Automation

        ```
        Height = Object.Height;       # Получить свойство (* )
        Object.Height = Height;       # Установить свойство (* )
        Height = Object.GetHeight();  # Получить свойство (**)
        Object.SetHeight( Height )    # Установить свойство (**)
        ```
        """
        ...
    @Height.setter
    def Height(self, value: float): ...

    @property
    def HeightType(self):  # isheetmetalpressforming_heighttype.html
        """
        ## HeightType - Способ задания высоты

        Тип данных:

        ### Синтаксис Automation

        ```
        HeightType = Object.HeightType;       # Получить свойство (* )
        Object.HeightType = HeightType;       # Установить свойство (* )
        HeightType = Object.GetHeightType();  # Получить свойство (**)
        Object.SetHeightType( HeightType )    # Установить свойство (**)
        ```
        """
        ...
    @HeightType.setter
    def HeightType(self, value): ...

    @property
    def Radius1(self) -> float:  # isheetmetalpressforming_radius1.html
        """
        ## Radius1 - Радиус скругления ребер

        Тип данных: double

        ### Синтаксис Automation

        ```
        Radius1 = Object.Radius1;       # Получить свойство (* )
        Object.Radius1 = Radius1;       # Установить свойство (* )
        Radius1 = Object.GetRadius1();  # Получить свойство (**)
        Object.SetRadius1( Radius1 )    # Установить свойство (**)
        ```
        """
        ...
    @Radius1.setter
    def Radius1(self, value: float): ...

    @property
    def Radius2(self) -> float:  # isheetmetalpressforming_radius2.html
        """
        ## Radius2 - Радиус скругления основания

        Тип данных: double

        ### Синтаксис Automation

        ```
        Radius2 = Object.Radius2;       # Получить свойство (* )
        Object.Radius2 = Radius2;       # Установить свойство (* )
        Radius2 = Object.GetRadius2();  # Получить свойство (**)
        Object.SetRadius2( Radius2 )    # Установить свойство (**)
        ```
        """
        ...
    @Radius2.setter
    def Radius2(self, value: float): ...

    @property
    def Radius3(self) -> float:  # isheetmetalpressforming_radius3.html
        """
        ## Radius3 - Радиус скругления дна в закрытой штамповке

        Тип данных: double

        ### Синтаксис Automation

        ```
        Radius3 = Object.Radius3;       # Получить свойство (* )
        Object.Radius3 = Radius3;       # Установить свойство (* )
        Radius3 = Object.GetRadius3();  # Получить свойство (**)
        Object.SetRadius3( Radius3 )    # Установить свойство (**)
        ```
        """
        ...
    @Radius3.setter
    def Radius3(self, value: float): ...

    @property
    def Round(self) -> bool:  # isheetmetalpressforming_round.html
        """
        ## Round - Скругление основания

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        Round = Object.Round;       # Получить свойство (* )
        Object.Round = Round;       # Установить свойство (* )
        Round = Object.GetRound();  # Получить свойство (**)
        Object.SetRound( Round );   # Установить свойство (**)
        ```
        """
        ...
    @Round.setter
    def Round(self, value: bool): ...

    @property
    def RoundBottom(self) -> bool:  # isheetmetalpressforming_roundbottom.html
        """
        ## RoundBottom - Скругление дна в закрытой штамповке

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        RoundBottom = Object.RoundBottom ;      # Получить свойство (* )
        Object.RoundBottom = RoundBottom;       # Установить свойство (* )
        RoundBottom = Object.GetRoundBottom();  # Получить свойство (**)
        Object.SetRoundBottom( RoundBottom );   # Установить свойство (**)
        ```
        """
        ...
    @RoundBottom.setter
    def RoundBottom(self, value: bool): ...

    @property
    def RoundEdges(self) -> bool:  # isheetmetalpressforming_roundedges.html
        """
        ## RoundEdges - Скругление ребер

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        RoundEdges = Object.RoundEdges ;      # Получить свойство (* )
        Object.RoundEdges = RoundEdges;       # Установить свойство (* )
        RoundEdges = Object.GetRoundEdges();  # Получить свойство (**)
        Object.SetRoundEdges( RoundEdges );   # Установить свойство (**)
        ```
        """
        ...
    @RoundEdges.setter
    def RoundEdges(self, value: bool): ...

    @property
    def Side(self) -> bool:  # isheetmetalpressforming_side.html
        """
        ## Side - Неподвижная грань

        Тип данных: BOOL

        Значение свойства:

        +-------------------+
        | TRUE  | - наружу, |
        +-------------------+
        | FALSE | - внутрь. |
        +-------------------+

        ### Синтаксис Automation

        ```
        Side = Object.Side;       # Получить свойство (* )
        Object.Side = Side;       # Установить свойство (* )
        Side = Object.GetSide();  # Получить свойство (**)
        Object.SetSide( Side )    # Установить свойство (**)
        ```
        """
        ...
    @Side.setter
    def Side(self, value: bool): ...

    @property
    def Sketch(self) -> IModelObject:  # isheetmetalpressforming_sketch.html
        """
        ## Sketch - Эскиз

        Тип данных: Указатель на интерфейс IModelObject.

        ### Синтаксис Automation

        ```
        Sketch = Object.Sketch;       # Получить свойство (* )
        Object.Sketch = Sketch;       # Установить свойство (* )
        Sketch = Object.GetSketch();  # Получить свойство (**)
        Object.SetSketch( Sketch )    # Установить свойство (**)
        ```
        """
        ...
    @Sketch.setter
    def Sketch(self, value: IModelObject): ...

    @property
    def ThicknessDirection(self) -> bool:  # isheetmetalpressforming_thicknessdirection.html
        """
        ## ThicknessDirection - Направление добавления толщины стенок штамповки

        Тип данных: BOOL

        Значение свойства:

        +-------------------+
        | TRUE  | - внутрь, |
        +-------------------+
        | FALSE | - наружу. |
        +-------------------+

        ### Синтаксис Automation

        ```
        ThicknessDirection = Object.ThicknessDirection;       # Получить свойство (* )
        Object.ThicknessDirection = ThicknessDirection;       # Установить свойство (* )
        ThicknessDirection = Object.GetThicknessDirection();  # Получить свойство (**)
        Object.SetThicknessDirection( ThicknessDirection )    # Установить свойство (**)
        ```
        """
        ...
    @ThicknessDirection.setter
    def ThicknessDirection(self, value: bool): ...




class ISheetMetalPunchs(IModelObjects):  # isheetmetalpunchs.html
    """
    ## Интерфейс ISheetMetalPunchs

    Интерфейс коллекции штамповок листовых тел другими телами.

    Получить интерфейс можно, используя свойство контейнера листовых операций ISheetMetalContainer::SheetMetalPunchs.

    Версия: Компас v20
    """

    def Add(self) -> ISheetMetalPunch:  # isheetmetalpunchs_add.html
        """
        ## Add - Добавить элемент в коллекцию

        ### Синтаксис Automation

        ```
        LPDISPATCH Add();
        ```

        ### Возвращаемое значение

        Указатель на интерфейс ISheetMetalPunch.
        """
        ...

    def SheetMetalPunch(self, Index) -> ISheetMetalPunch:  # isheetmetalpunchs_sheetmetalpunch.html
        """
        ## SheetMetalPunch - Получить элемент, заданныйпо индексу

        Тип данных: Указатель на интерфейс ISheetMetalPunch

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        SheetMetalPunch = Object.SheetMetalPunch( Index )     # Получить свойство (* )
        SheetMetalPunch = Object.GetSheetMetalPunch( Index )  # Получить свойство (**)
        ```

        ### Входные параметры

        +----------------------------------------------------------------------------------------------------------------------------------+
        | VARIANT Index- | - Индекс объекта в коллекции. Поддерживаются следующие типы: - VT_I4 - индекс объекта, - VT_BSTR - имя объекта. |
        +----------------------------------------------------------------------------------------------------------------------------------+
        """
        ...




class ISheetMetalPunch(IModelObject):  # isheetmetalpunch.html
    """
    ## Интерфейс ISheetMetalPunch

    Интерфейс штамповки телом.

    Интерфейс можно получить у коллекции дуг, используя свойство ISheetMetalPunchs::SheetMetalPunch или метод ISheetMetalPunchs::Add.

    Версия: Компас v20
    """

    @property
    def Body(self) -> IBody7:  # isheetmetalpunch_body.html
        """
        ## Body - Тело инструмент

        Тип данных: Указатель на интерфейс IBody7

        ### Синтаксис Automation

        ```
        Body = Object.Body       # Получить свойство (* )
        Object.Body = Body       # Установить свойство (* )
        Body = Object.GetBody()  # Получить свойство (**)
        Object.SetBody( Body )   # Установить свойство (**)
        ```
        """
        ...
    @Body.setter
    def Body(self, value: IBody7): ...

    @property
    def BodyFaces(self) -> typing.Any:  # isheetmetalpunch_bodyfaces.html
        """
        ## BodyFaces - Грани инструмента для вырубки

        Тип данных: VARIANT

        Возвращается:

        •массив SafeArray VT_ARRAY | VT_DISPATCH граней IFace, если грани для вырубки заданы,

        •VT_EMPTY, если грани не выбраны.

        ### Синтаксис Automation

        ```
        BodyFaces = Object.BodyFaces       # Получить свойство (* )
        Object.BodyFaces = BodyFaces       # Установить свойство (* )
        BodyFaces = Object.GetBodyFaces()  # Получить свойство (**)
        Object.SetBodyFaces( BodyFaces )   # Установить свойство (**)
        ```
        """
        ...
    @BodyFaces.setter
    def BodyFaces(self, value: typing.Any): ...

    @property
    def BuildingType(self) -> int:  # isheetmetalpunch_buildingtype.html
        """
        ## BuildingType - Способ построения. Инструмент

        Тип данных: из перечисления ksSheetMetalPunchBuildingTypeEnum.

        ### Синтаксис Automation

        ```
        BuildingType = Object.BuildingType        # Получить свойство (* )
        Object.BuildingType = BuildingType        # Установить свойство (* )
        BuildingType = Object.GetBuildingType ()  # Получить свойство (**)
        Object.SetBuildingType ( BuildingType )   # Установить свойство (**)
        ```
        """
        ...
    @BuildingType.setter
    def BuildingType(self, value: int): ...

    @property
    def DeleteBody(self) -> bool:  # isheetmetalpunch_deletebody.html
        """
        ## DeleteBody - Удалить тело инструмент

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        DeleteBody = Object. DeleteBody        # Получить свойство (* )
        Object. DeleteBody = DeleteBody        # Установить свойство (* )
        DeleteBody = Object.Get DeleteBody ()  # Получить свойство (**)
        Object.Set DeleteBody ( DeleteBody )   # Установить свойство (**)
        ```
        """
        ...
    @DeleteBody.setter
    def DeleteBody(self, value: bool): ...

    @property
    def Face(self) -> IModelObject:  # isheetmetalpunch_face.html
        """
        ## Face - Грань листового тела

        Тип данных: Указатель на интерфейс IModelObject

        ### Синтаксис Automation

        ```
        Face = Object.Face       # Получить свойство (* )
        Object.Face = Face       # Установить свойство (* )
        Face = Object.GetFace()  # Получить свойство (**)
        Object.SetFace( Face )   # Установить свойство (**)
        ```
        """
        ...
    @Face.setter
    def Face(self, value: IModelObject): ...

    @property
    def PunchEdgeType(self) -> int:  # isheetmetalpunch_punchedgetype.html
        """
        ## PunchEdgeType - Тип кромки вырубки

        Тип данных: из перечисления ksSheetMetalPunchEdgeTypeEnum

        ### Синтаксис Automation

        ```
        PunchEdgeType = Object. PunchEdgeType        # Получить свойство (* )
        Object. PunchEdgeType = PunchEdgeType        # Установить свойство (* )
        PunchEdgeType = Object.Get PunchEdgeType ()  # Получить свойство (**)
        Object.Set PunchEdgeType ( PunchEdgeType )   # Установить свойство (**)
        ```
        """
        ...
    @PunchEdgeType.setter
    def PunchEdgeType(self, value: int): ...

    @property
    def Radius2(self) -> float:  # isheetmetalpunch_radius2.html
        """
        ## Radius2 - Радиус скругления основания

        Тип данных: double

        ### Синтаксис Automation

        ```
        Radius2= Object. Radius2         # Получить свойство (* )
        Object. Radius2 = Radius2        # Установить свойство (* )
        Radius2 = Object.Get Radius2 ()  # Получить свойство (**)
        Object.Set Radius2 ( Radius2 )   # Установить свойство (**)
        ```
        """
        ...
    @Radius2.setter
    def Radius2(self, value: float): ...

    @property
    def Round(self) -> bool:  # isheetmetalpunch_round.html
        """
        ## Round - Скругление основания

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        Round = Object. Round        # Получить свойство (* )
        Object. Round = Round        # Установить свойство (* )
        Round = Object.Get Round ()  # Получить свойство (**)
        Object.Set Round ( Round )   # Установить свойство (**)
        ```
        """
        ...
    @Round.setter
    def Round(self, value: bool): ...

    @property
    def RoundEdgesRadius(self) -> float:  # isheetmetalpunch_roundedgesradius.html
        """
        ## RoundEdgesRadius - Радиус скругления ребер

        Тип данных: double

        ### Синтаксис Automation

        ```
        RoundEdgesRadius = Object. RoundEdgesRadius        # Получить свойство (* )
        Object. RoundEdgesRadius = RoundEdgesRadius        # Установить свойство (* )
        RoundEdgesRadius = Object.Get RoundEdgesRadius ()  # Получить свойство (**)
        Object.Set RoundEdgesRadius ( RoundEdgesRadius )   # Установить свойство (**)
        ```
        """
        ...
    @RoundEdgesRadius.setter
    def RoundEdgesRadius(self, value: float): ...

    @property
    def RoundOffEdges(self) -> bool:  # isheetmetalpunch_roundoffedges.html
        """
        ## RoundOffEdges - Скруглять рёбра

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        RoundOffEdges = Object. RoundOffEdges        # Получить свойство (* )
        Object. RoundOffEdges = RoundOffEdges        # Установить свойство (* )
        RoundOffEdges = Object.Get RoundOffEdges ()  # Получить свойство (**)
        Object.Set RoundOffEdges ( RoundOffEdges )   # Установить свойство (**)
        ```
        """
        ...
    @RoundOffEdges.setter
    def RoundOffEdges(self, value: bool): ...

    @property
    def Thickness(self) -> float:  # isheetmetalpunch_thickness.html
        """
        ## Thickness - Заданная толщина

        Тип данных: double

        ### Синтаксис Automation

        ```
        Round = Object. Thickness           # Получить свойство (* )
        Object. Thickness = Thickness       # Установить свойство (* )
        Round = Object.Get Thickness ()     # Получить свойство (**)
        Object.Set Thickness ( Thickness )  # Установить свойство (**)
        ```
        """
        ...
    @Thickness.setter
    def Thickness(self, value: float): ...

    @property
    def ThicknessType(self) -> int:  # isheetmetalpunch_thicknesstype.html
        """
        ## ThicknessType - Способ определение толщины

        Тип данных: из перечисления ksSheetMetalPunchThicknessTypeEnum.

        ### Синтаксис Automation

        ```
        Round = Object. ThicknessType               # Получить свойство (* )
        Object. ThicknessType = ThicknessType       # Установить свойство (* )
        Round = Object.Get ThicknessType ()         # Получить свойство (**)
        Object.Set ThicknessType ( ThicknessType )  # Установить свойство (**)
        ```
        """
        ...
    @ThicknessType.setter
    def ThicknessType(self, value: int): ...




class ISheetMetalShoulders(IModelObjects):  # isheetmetalshoulders.html
    """
    ## Интерфейс ISheetMetalShoulders

    Интерфейс коллекции элементовБуртик.
    """

    def Add(self) -> ISheetMetalShoulder:  # isheetmetalshoulders_add.html
        """
        ## Add - Создает новый элемент и добавляет его в коллекцию

        1. Метод позволяет создать новый интерфейс операций Буртик.

        2. После получения нового интерфейса нужно задать параметры операции и вызвать метод IModelObject::Update.

        ### Синтаксис Automation

        ```
        LPDISPATCH Add();
        ```

        ### Возвращаемое значение

        - указатель на интерфейс ISheetMetalShoulder.
        """
        ...

    def SheetMetalShoulder(self, Index) -> ISheetMetalShoulder:  # isheetmetalshoulders_sheetmetalshoulder.html
        """
        ## SheetMetalShoulder - Возвращает элемент, заданный по индексу

        Тип данных: Указатель на интерфейс ISheetMetalShoulder.

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        SheetMetalShoulder = Object.SheetMetalShoulder( Index )     # Получить свойство (* )
        SheetMetalShoulder = Object.GetSheetMetalShoulder( Index )  # Получить свойство (**)
        ```

        ### Входные параметры

        +--------------------------------------------+
        | VARIANT Index | - индекс или имя элемента. |
        +--------------------------------------------+
        """
        ...




class ISheetMetalShoulder(IModelObject):  # isheetmetalshoulder.html
    """
    ## Интерфейс ISheetMetalShoulder

    Интерфейс операции Буртик.
    """

    @property
    def BuildingType(self) -> int:  # isheetmetalshoulder_buildingtype.html
        """
        ## BuildingType - Способ построения

        Тип данных: Значение из перечисления ksShoulderBuildingTypeEnum.

        ### Синтаксис Automation

        ```
        BuildingType = Object.BuildingType;       # Получить свойство (* )
        Object.BuildingType = BuildingType;       # Установить свойство (* )
        BuildingType = Object.GetBuildingType();  # Получить свойство (**)
        Object.SetBuildingType( BuildingType )    # Установить свойство (**)
        ```
        """
        ...
    @BuildingType.setter
    def BuildingType(self, value: int): ...

    @property
    def CutingType(self) -> int:  # isheetmetalshoulder_cutingtype.html
        """
        ## CutingType - Форма сечения

        Тип данных: Значение из перечисления ksShoulderCutingTypeEnum.

        ### Синтаксис Automation

        ```
        CutingType = Object.CutingType;       # Получить свойство (* )
        Object.CutingType = CutingType;       # Установить свойство (* )
        CutingType = Object.GetCutingType();  # Получить свойство (**)
        Object.SetCutingType( CutingType )    # Установить свойство (**)
        ```
        """
        ...
    @CutingType.setter
    def CutingType(self, value: int): ...

    @property
    def Direction(self) -> bool:  # isheetmetalshoulder_direction.html
        """
        ## Direction - Направление построения: TRUE - Прямое, FALSE - Обратное

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        Direction = Object.Direction;       # Получить свойство (* )
        Object.Direction = Direction;       # Установить свойство (* )
        Direction = Object.GetDirection();  # Получить свойство (**)
        Object.SetDirection( Direction )    # Установить свойство (**)
        ```
        """
        ...
    @Direction.setter
    def Direction(self, value: bool): ...

    @property
    def GapValue(self) -> float:  # isheetmetalshoulder_gapvalue.html
        """
        ## GapValue - Зазор

        Тип данных: double

        ### Синтаксис Automation

        ```
        GapValue = Object.GapValue;       # Получить свойство (* )
        Object.GapValue = GapValue;       # Установить свойство (* )
        GapValue = Object.GetGapValue();  # Получить свойство (**)
        Object.SetGapValue( GapValue )    # Установить свойство (**)
        ```
        """
        ...
    @GapValue.setter
    def GapValue(self, value: float): ...

    @property
    def Height(self) -> float:  # isheetmetalshoulder_height.html
        """
        ## Height - Высота

        Тип данных: double

        ### Синтаксис Automation

        ```
        Height = Object.Height;       # Получить свойство (* )
        Object.Height = Height;       # Установить свойство (* )
        Height = Object.GetHeight();  # Получить свойство (**)
        Object.SetHeight( Height )    # Установить свойство (**)
        ```
        """
        ...
    @Height.setter
    def Height(self, value: float): ...

    @property
    def Radius1(self) -> float:  # isheetmetalshoulder_radius1.html
        """
        ## Radius1 - Радиус буртика

        Тип данных: double

        ### Синтаксис Automation

        ```
        Radius1 = Object.Radius1;       # Получить свойство (* )
        Object.Radius1 = Radius1;       # Установить свойство (* )
        Radius1 = Object.GetRadius1();  # Получить свойство (**)
        Object.SetRadius1( Radius1 )    # Установить свойство (**)
        ```
        """
        ...
    @Radius1.setter
    def Radius1(self, value: float): ...

    @property
    def Radius2(self) -> float:  # isheetmetalshoulder_radius2.html
        """
        ## Radius2 - Радиус скругления основания

        Тип данных: double

        ### Синтаксис Automation

        ```
        Radius2 = Object.Radius2;       # Получить свойство (* )
        Object.Radius2 = Radius2;       # Установить свойство (* )
        Radius2 = Object.GetRadius2();  # Получить свойство (**)
        Object.SetRadius2( Radius2 )    # Установить свойство (**)
        ```
        """
        ...
    @Radius2.setter
    def Radius2(self, value: float): ...

    @property
    def Round(self) -> bool:  # isheetmetalshoulder_round.html
        """
        ## Round - Скругление основания

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        Round = Object.Round;       # Получить свойство (* )
        Object.Round = Round;       # Установить свойство (* )
        Round = Object.GetRound();  # Получить свойство (**)
        Object.SetRound( Round )    # Установить свойство (**)
        ```
        """
        ...
    @Round.setter
    def Round(self, value: bool): ...

    @property
    def ShoulderType(self) -> int:  # isheetmetalshoulder_shouldertype.html
        """
        ## ShoulderType - Тип. Способ обработки концов буртика

        Тип данных: Значение из перечисления ksShoulderTypeEnum.

        ### Синтаксис Automation

        ```
        ShoulderType = Object.ShoulderType;       # Получить свойство (* )
        Object.ShoulderType = ShoulderType;       # Установить свойство (* )
        ShoulderType = Object.GetShoulderType();  # Получить свойство (**)
        Object.SetShoulderType( ShoulderType )    # Установить свойство (**)
        ```
        """
        ...
    @ShoulderType.setter
    def ShoulderType(self, value: int): ...

    @property
    def Sketch(self) -> IModelObject:  # isheetmetalshoulder_sketch.html
        """
        ## Sketch - Эскиз

        Тип данных: Указатель на интерфейс IModelObject.

        ### Синтаксис Automation

        ```
        Sketch = Object.Sketch;       # Получить свойство (* )
        Object.Sketch = Sketch;       # Установить свойство (* )
        Sketch = Object.GetSketch();  # Получить свойство (**)
        Object.SetSketch( Sketch )    # Установить свойство (**)
        ```
        """
        ...
    @Sketch.setter
    def Sketch(self, value: IModelObject): ...

    @property
    def Width1(self) -> float:  # isheetmetalshoulder_width1.html
        """
        ## Width1 - Ширина основания

        Тип данных: double

        ### Синтаксис Automation

        ```
        Width1 = Object.Width1;       # Получить свойство (* )
        Object.Width1 = Width1;       # Установить свойство (* )
        Width1 = Object.GetWidth1();  # Получить свойство (**)
        Object.SetWidth1( Width1 )    # Установить свойство (**)
        ```
        """
        ...
    @Width1.setter
    def Width1(self, value: float): ...

    @property
    def Width2(self) -> float:  # isheetmetalshoulder_width2.html
        """
        ## Width2 - Ширина дна

        Тип данных: double

        ### Синтаксис Automation

        ```
        Width2 = Object.Width2;       # Получить свойство (* )
        Object.Width2 = Width2;       # Установить свойство (* )
        Width2 = Object.GetWidth2();  # Получить свойство (**)
        Object.SetWidth2( Width2 )    # Установить свойство (**)
        ```
        """
        ...
    @Width2.setter
    def Width2(self, value: float): ...




class ISheetMetalUndercut(IDispatch):  # isheetmetalundercut.html
    """
    ## Интерфейс ISheetMetalUndercut

    Интерфейс операции Подсечка.
    """

    @property
    def Distance(self) -> float:  # isheetmetalundercut_distance.html
        """
        ## Distance - Расстояние

        Тип данных: double

        ### Синтаксис Automation

        ```
        Distance = Object.Distance       # Получить свойство (* )
        Object.Distance = Distance       # Установить свойство (* )
        Distance = Object.GetDistance()  # Получить свойство (**)
        Object.SetDistance (Distance)    # Установить свойство (**)
        ```
        """
        ...
    @Distance.setter
    def Distance(self, value: float): ...

    @property
    def DistanceType(self) -> int:  # isheetmetalundercut_distancetype.html
        """
        ## DistanceType - Задание размера

        Тип данных: Значение из перечисления ksUndercutDistanceTypeEnum.

        ### Синтаксис Automation

        ```
        DistanceType = Object.DistanceType       # Получить свойство (* )
        Object.DistanceType = DistanceType       # Установить свойство (* )
        DistanceType = Object.GetDistanceType()  # Получить свойство (**)
        Object.SetDistanceType (DistanceType)    # Установить свойство (**)
        ```
        """
        ...
    @DistanceType.setter
    def DistanceType(self, value: int): ...

    @property
    def WithAddMaterial(self) -> bool:  # isheetmetalundercut_withaddmaterial.html
        """
        ## WithAddMaterial - С добавлением материала

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        WithAddMaterial = Object.WithAddMaterial       # Получить свойство (* )
        Object.WithAddMaterial = WithAddMaterial       # Установить свойство (* )
        WithAddMaterial = Object.GetWithAddMaterial()  # Получить свойство (**)
        Object.SetWithAddMaterial( WithAddMaterial )   # Установить свойство (**)
        ```
        """
        ...
    @WithAddMaterial.setter
    def WithAddMaterial(self, value: bool): ...




class ISymbols3DContainer(IDispatch):  # isymbols3dcontainer.html
    """
    ## Интерфейс ISymbols3DContainer

    Интерфейс контейнера условных обозначений 3D.

    Позволяет получить коллекции 3D размеров и обозначений.

    Дополнительный интерфейс компонента. Данный интерфейс можно получить у компонента IPart7 посредством вызова метода IUnknown::QueryInterface (const GUID far& iid, void** pif).
    """

    @property
    def AngleDimensions3D(self) -> IAngleDimensions3D:  # isymbols3dcontainer_angledimensions3d.html
        """
        ## AngleDimensions3D - Интерфейс коллекции угловых размеров 3D

        Тип данных: указатель на интерфейс IAngleDimensions3D

        1. Свойство позволяет получать и создавать угловые и угловые с обрывом размеры.

        2. Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        AngleDimensions3D = Object.AngleDimensions3D       # Получить свойство (* )
        AngleDimensions3D = Object.GetAngleDimensions3D()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def ArcDimensions3D(self) -> IArcDimensions3D:  # isymbols3dcontainer_arcdimensions3d.html
        """
        ## ArcDimensions3D - Размеры дуг окружностей 3D

        Тип данных: Указатель на интерфейс IArcDimensions3D.

        Свойство доступно только для чтения.

        Версия: Компас v20

        ### Синтаксис Automation

        ```
        ArcDimensions3D = Object.ArcDimensions3D       # Получить свойство (* )
        ArcDimensions3D = Object.GetArcDimensions3D()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def Bases3D(self) -> IBases3D:  # isymbols3dcontainer_bases3d.html
        """
        ## Bases3D - Интерфейс коллекции обозначений базы 3D

        Тип данных: указатель на интерфейс IBases3D

        1. Свойство позволяет получать и создавать обозначения базы 3D.

        2. Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        Bases3D = Object.Bases3D       # Получить свойство (* )
        Bases3D = Object.GetBases3D()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def DiametralDimensions3D(self) -> IDiametralDimensions3D:  # isymbols3dcontainer_diametraldimensions3d.html
        """
        ## DiametralDimensions3D - Интерфейс коллекции диаметральных размеров 3D

        Тип данных: указатель на интерфейс IDiametralDimensions3D

        1. Свойство позволяет получать и создавать диаметральные размеры.

        2. Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        DiametralDimensions3D = Object.DiametralDimensions3D       # Получить свойство (* )
        DiametralDimensions3D = Object.GetDiametralDimensions3D()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def Leaders3D(self) -> ILeaders3D:  # isymbols3dcontainer_leaders3d.html
        """
        ## Leaders3D - Интерфейс коллекции линий-выносок 3D

        Тип данных: указатель на интерфейс ILeaders3D

        1. Свойство позволяет получать и создавать линии-выноски.

        2. Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        Leaders3D = Object.Leaders3D       # Получить свойство (* )
        Leaders3D = Object.GetLeaders3D()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def LineDimensions3D(self) -> ILineDimensions3D:  # isymbols3dcontainer_linedimensions3d.html
        """
        ## LineDimensions3D - Интерфейс коллекции линейных размеров 3D

        Тип данных: указатель на интерфейс ILineDimensions3D

        1. Свойство позволяет получать и создавать линейные размеры.

        2. Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        LineDimensions3D = Object.LineDimensions3D       # Получить свойство (* )
        LineDimensions3D = Object.GetLineDimensions3D()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def ModelTables(self) -> IModelTables:  # isymbols3dcontainer_modeltables.html
        """
        ## ModelTables - Коллекция таблиц модели

        Тип данных: Указатель на интерфейс IModelTables

        Свойство доступно только для чтения.

        Версия Компас v21

        ### Синтаксис Automation

        ```
        ModelTables = Object.ModelTables       # Получить свойство (* )
        ModelTables = Object.GetModelTables()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def ModelTexts(self) -> IModelTexts:  # isymbols3dcontainer_modeltexts.html
        """
        ## ModelTexts - Коллекция текстов модели

        Тип данных: Указатель на интерфейс IModelTexts

        Свойство доступно только для чтения.

        Версия Компас v21

        ### Синтаксис Automation

        ```
        ModelTexts = Object.ModelTexts       # Получить свойство (* )
        ModelTexts = Object.GetModelTexts()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def RadialDimensions3D(self) -> IRadialDimensions3D:  # isymbols3dcontainer_radialdimensions3d.html
        """
        ## RadialDimensions3D - Интерфейс коллекции радиальных размеров 3D

        Тип данных: указатель на интерфейс IRadialDimensions3D

        1. Свойство позволяет получать и создавать радиальные размеры.

        2. Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        RadialDimensions3D = Object.RadialDimensions3D       # Получить свойство (* )
        RadialDimensions3D = Object.GetRadialDimensions3D()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def Roughs3D(self) -> IRoughs3D:  # isymbols3dcontainer_roughs3d.html
        """
        ## Roughs3D - Интерфейс коллекции обозначений шероховатости 3D

        Тип данных: указатель на интерфейс IRoughs3D

        1. Свойство позволяет получать и создавать обозначения шероховатости 3D.

        2. Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        Roughs3D = Object.Roughs3D       # Получить свойство (* )
        Roughs3D = Object.GetRoughs3D()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def Threads(self) -> IThreads:  # isymbols3dcontainer_threads.html
        """
        ## Threads - Коллекция условных обозначений резьбы

        Тип данных: Указатель на интерфейс IThreads

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        Threads = Object.Threads       # Получить свойство (* )
        Threads = Object.GetThreads()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def Tolerances3D(self) -> ITolerances3D:  # isymbols3dcontainer_tolerances3d.html
        """
        ## Tolerances3D - Интерфейс коллекции обозначений допусков формы 3D

        Тип данных: указатель на интерфейс ITolerances3D

        1. Свойство позволяет получать и создавать обозначения допусков формы 3D.

        2. Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        Tolerances3D = Object.Tolerances3D       # Получить свойство (* )
        Tolerances3D = Object.GetTolerances3D()  # Получить свойство (**)
        ```
        """
        ...

    def CreateGenerativeDimensions(self, Objects, CreateZeroDimensions) -> bool:  # isymbols3dcontainer_creategenerativedimensions.html
        """
        ## CreateGenerativeDimensions - Отобразить размеры эскизов и операций

        ### Синтаксис Automation

        ```
        BOOL CreateGenerativeDimensions( VARIANT Objects, BOOL CreateZeroDimensions );
        ```

        ### Входные параметры

        +--------------------------------------------------------------------------------------------------------------------------------------+
        | Objects              | - объект или массив объектов ( VT_DISPATCH или VT_ARRAY | VT_DISPATCH), для которых нужно отобразить размеры, |
        +--------------------------------------------------------------------------------------------------------------------------------------+
        | CreateZeroDimensions | - создавать размеры с нулевым значением.                                                                      |
        +--------------------------------------------------------------------------------------------------------------------------------------+

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def GetDimensionVariable(self, Dimension) -> IVariable7:  # isymbols3dcontainer_getdimensionvariable.html
        """
        ## GetDimensionVariable - Получить переменную, связанную с размером

        ### Синтаксис Automation

        ```
        LPDISPATCH GetDimensionVariable( LPDISPATCH Dimension );
        ```

        ### Входные параметры

        +----------------------------------------------------------------------------+
        | Dimensions | - указатель на размер, для которого надо получить переменную. |
        +----------------------------------------------------------------------------+

        ### Возвращаемое значение

        указатель на интерфейс переменной IVariable7.
        """
        ...




class IAngleDimensions3D(IKompasCollection):  # iangledimensions3d.html
    """
    ## Интерфейс IAngleDimensions3D

    Интерфейс коллекции угловых размеров 3D.

    Интерфейс позволяет получить и создавать угловые размеры в 3D документе.

    Коллекция содержит угловые и угловые с обрывом размеры.

    Данный интерфейс можно получить у контейнера условных обозначений, используя свойство ISymbols3DContainer::AngleDimensions3D.
    """

    def Add(self) -> IAngleDimension3D:  # iangledimensions3d_add.html
        """
        ## Add - Добавить угловой размер в коллекцию

        ### Синтаксис Automation

        ```
        LPDISPATH Add( );
        ```

        ### Возвращаемое значение

        - указатель на интерфейс углового размера IAngleDimension3D.
        """
        ...

    def AngleDimension3D(self, Index) -> IAngleDimension3D:  # iangledimensions3d_angledimension3d.html
        """
        ## AngleDimension3D -Указатель на угловой размер, заданный по индексу

        Тип данных: указатель на интерфейс углового размера IAngleDimension3D

        1. В качестве индекса может использоваться индекс в коллекции и reference объекта.

        2. Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        AngleDimension = iObject.AngleDimension3D( Index );     # Получить свойство (* )
        AngleDimension = iObject.GetAngleDimension3D( Index );  # Получить свойство (**)
        ```

        ### Входные параметры

        +----------------------------------------------------------------------------------------------------------+
        | Index (Variant) | - Индекс размера в коллекции. Поддерживаются следующие типы: - VT_I4 - индекс размера. |
        +----------------------------------------------------------------------------------------------------------+
        """
        ...

    def Item(self, Index):
        ...




class IArcDimensions3D(IModelObjects):  # iarcdimensions3d.html
    """
    ## Интерфейс IArcDimensions3D

    Интерфейс коллекции размеров дуг окружностей 3D.

    Интерфейс позволяет получить и создавать размеры дуг окружностей в 3D документе.

    Данный интерфейс можно получить у контейнера условных обозначений, используя свойство ISymbols3DContainer::ArcDimensions3D.

    Версия: Компас v20
    """

    def Add(self) -> IArcDimension3D:  # iarcdimensions3d_add.html
        """
        ## Add - Добавить размер дуги в коллекцию

        ### Синтаксис Automation

        ```
        LPDISPATCH Add();
        ```

        ### Возвращаемое значение

        - Указатель на интерфейс IArcDimension3D.
        """
        ...

    def ArcDimension3D(self, Index) -> IArcDimension3D:  # iarcdimensions3d_arcdimension3d.html
        """
        ## ArcDimension3D - Получить размер дуги, заданный по индексу

        Тип данных: Указатель на интерфейс IArcDimension3D.

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        ArcDimension3D = Object.ArcDimension3D( Index )     # Получить свойство (* )
        ArcDimension3D = Object.GetArcDimension3D( Index )  # Получить свойство (**)
        ```

        ### Входные параметры

        +-----------------------------------------------------------------------------------------------------------------------------------------------------------------------+
        | VARIANT Index | - Индекс объекта в коллекции. Поддерживаются следующие типы: VT_I4 - индекс объекта, VT_BSTR - имя объекта, индекс объекта в коллекции (тип VARIANT). |
        +-----------------------------------------------------------------------------------------------------------------------------------------------------------------------+
        """
        ...




class IDimension3D(IDispatch):  # idimension3d.html
    """
    ## Интерфейс IDimension3D

    Интерфейс размера 3D.

    Интерфейс является дополнительным для размеров 3D.

    Версия: Компас v20
    """

    def GetDimensionPoint(self, DimensionPoint, PointIndex) -> bool:  # idimension3d_getdimensionpoint.html
        """
        ## GetDimensionPoint - Получить координаты точек размера

        ### Синтаксис Automation

        ```
        BOOL GetDimensionPoint( ksDimensionPointEnum DimensionPoint, long PointIndex, double * X, double * Y, double * Z );
        ```

        ### Входные параметры

        +--------------------------------------------------------------------+
        | DimensionPoint | - тип точки из перечисления ksDimensionPointEnum, |
        +--------------------------------------------------------------------+
        | PointIndex     | - координаты точки.                               |
        +--------------------------------------------------------------------+

        ### Выходные параметры

        +------------------------------+
        | X,Y, Z | - координаты точки. |
        +------------------------------+

        Версия Компас v21

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def GetTextPosition(self) -> bool:  # idimension3d_gettextposition.html
        """
        ## GetTextPosition - Получить положение размерной линии

        ### Синтаксис Automation

        ```
        BOOL GetTextPosition( double * X, double * Y, double * Z );
        ```

        ### Выходные параметры

        +------------------------------+
        | X,Y, Z | - координаты точки. |
        +------------------------------+

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def SetTextPosition(self, X, Y, Z) -> bool:  # idimension3d_settextposition.html
        """
        ## SetTextPosition - Установить положение размерной линии

        ### Синтаксис Automation

        ```
        BOOL SetTextPosition( double X, double Y, double Z );
        ```

        ### Входные параметры

        +------------------------------+
        | X,Y, Z | - координаты точки. |
        +------------------------------+

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...




class IDiametralDimensions3D(IModelObject):  # idiametraldimensions3d.html
    """
    ## Интерфейс IDiametralDimensions3D

    Интерфейс коллекции диаметральных размеров 3D.

    Интерфейс позволяет получить и создавать диаметральные размеры в 3D документе.

    Данный интерфейс можно получить у контейнера условных обозначений, используя свойство ISymbols3DContainer::DiametralDimensions3D.
    """

    @property
    def Count(self):
        ...

    def Add(self) -> IDiametralDimension3D:  # idiametraldimensions3d_add.html
        """
        ## Add - Добавить диаметральный размер в коллекцию

        ### Синтаксис Automation

        ```
        LPDISPATH Add( );
        ```

        ### Возвращаемое значение

        - Указатель на интерфейс диаметрального размера IDiametralDimension3D.
        """
        ...

    def DiametralDimension3D(self, Index) -> IDiametralDimension3D:  # idiametraldimensions3d_diametraldimension3d.html
        """
        ## DiametralDimension3D - Указатель на диаметральный размер, заданный по индексу

        Тип данных: указатель на интерфейс диаметрального размера IDiametralDimension3D

        1. В качестве индекса может использоваться индекс в коллекции и reference объекта.

        2. Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        DiametralDimension = iObject.DiametralDimension3D( Index );     # Получить свойство (* )
        DiametralDimension = iObject.GetDiametralDimension3D( Index );  # Получить свойство (**)
        ```

        ### Входные параметры

        +--------------------------------------------------------------------------------------------------------+
        | Index (Variant) | Индекс размера в коллекции. Поддерживаются следующие типы: - VT_I4 - индекс размера. |
        +--------------------------------------------------------------------------------------------------------+
        """
        ...

    def Item(self, Index):
        ...




class ILineDimensions3D(IModelObject):  # ilinedimensions3d.html
    """
    ## Интерфейс ILineDimensions3D

    Интерфейс коллекции линейных размеров 3D.

    Интерфейс позволяет получить и создавать линейные размеры (от отрезка до точки и на плоскости) в 3D документе.

    Коллекция содержит линейные размеры (от отрезка до точки и на плоскости).

    Данный интерфейс можно получить у контейнера условных обозначений, используя свойство ISymbols3DContainer::LineDimensions3D.
    """

    @property
    def Count(self):
        ...

    def Add(self, DimType) -> IBaseLineDimension3D:  # ilinedimensions3d_add.html
        """
        ## Add - Добавить линейный размер в коллекцию

        Для создания линейного размера от отрезка до точки нужно задать значение параметра DimType, равное o3d_baselineDimension3D.

        Для создания линейного размера на плоскости нужно задать значение параметра DimType, равное o3d_lineDimension3D.

        ### Синтаксис Automation

        ```
        LPDISPATH Add( );
        ```

        ### Входные параметры

        +----------------------------------------------------------+
        | DimType | - тип объекта из перечисления ksObj3dTypeEnum. |
        +----------------------------------------------------------+

        ### Возвращаемое значение

        - указатель на интерфейс линейного размера IBaseLineDimension3D.
        """
        ...

    def Item(self, Index):
        ...

    def LineDimension3D(self, Index) -> IBaseLineDimension3D:  # ilinedimensions3d_linedimension3d.html
        """
        ## LineDimension3D - Указатель на линейный размер, заданный по индексу

        Тип данных: указатель на интерфейс линейного размера IBaseLineDimension3D

        1. В качестве индекса может использоваться индекс в коллекции и reference объекта.

        2. Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        LineDimension = iObject.LineDimension3D( Index );     # Получить свойство (* )
        LineDimension = iObject.GetLineDimension3D( Index );  # Получить свойство (**)
        ```

        ### Входные параметры

        +----------------------------------------------------------------------------------------------------------+
        | Index (Variant) | - Индекс размера в коллекции. Поддерживаются следующие типы: - VT_I4 - индекс размера. |
        +----------------------------------------------------------------------------------------------------------+
        """
        ...




class IRadialDimensions3D(IModelObject):  # iradialdimensions3d.html
    """
    ## Интерфейс IRadialDimensions3D

    Интерфейс коллекции радиальных размеров 3D.

    Интерфейс позволяет получить и создавать радиальные размеры в 3D документе.

    Данный интерфейс можно получить у контейнера условных обозначений, используя свойство ISymbols3DContainer::RadialDimensions3D.
    """

    @property
    def Count(self):
        ...

    def Add(self) -> IRadialDimension3D:  # iradialdimensions3d_add.html
        """
        ## Add - Добавить радиальный размер в коллекцию

        ### Синтаксис Automation

        ```
        LPDISPATH Add( );
        ```

        ### Возвращаемое значение

        - указатель на интерфейс радиального размера IRadialDimension3D.
        """
        ...

    def Item(self, Index):
        ...

    def RadialDimension3D(self, Index) -> IRadialDimension3D:  # iradialdimensions3d_radialdimension3d.html
        """
        ## RadialDimension3D - Указатель на радиальный размер, заданный по индексу

        Тип данных: указатель на интерфейс радиального размера IRadialDimension3D

        1. В качестве индекса может использоваться индекс в коллекции и reference объекта.

        2. Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        RadialDimension = iObject.RadialDimension3D( Index );     # Получить свойство (* )
        RadialDimension = iObject.GetRadialDimension3D( Index );  # Получить свойство (**)
        ```

        ### Входные параметры

        +----------------------------------------------------------------------------------------------------------+
        | Index (Variant) | - Индекс размера в коллекции. Поддерживаются следующие типы: - VT_I4 - индекс размера. |
        +----------------------------------------------------------------------------------------------------------+
        """
        ...




class IAngleDimension3D(IModelObject):  # iangledimension3d.html
    """
    ## Интерфейс IAngleDimension3D

    Интерфейс параметров углового размера 3D.

    Интерфейс позволяет получить и задать свойства углового размера в 3D документе.

    Интерфейс можно получить у коллекции угловых размеров в 3D документе, используя свойство IAngleDimensions3D::AngleDimension3D или метод IAngleDimensions3D::Add.

    После задания параметров размера требуется вызвать метод IModelObject::Update.

    Интерфейсы IDimensionText и IDimensionParams являются дополнительными. Их можно получить с помощью метода IUnknown::QueryInterface.

    1.При создании углового размера используются прямолинейные и плоские объекты, которые являются сторонами угла.

    Прямолинейные объекты:

    •отрезок в эскизе,

    •сегмент ломаной,

    •вспомогательная ось,

    •ребро тела или поверхности.

    Плоские объекты:

    •координатная плоскость,

    •вспомогательная плоскость,

    •грань тела или поверхности.

    2.Свойство IDimensionText::NominalValue возвращает угол.
    """

    @property
    def DimensionType(self) -> int:  # iangledimension3d_dimensiontype.html
        """
        ## DimensionType - Тип углового размера

        Тип данных: из перечисления ksAngleDimTypeEnum

        ### Синтаксис Automation

        ```
        DimensionType = Object.DimensionType       # Получить свойство (* )
        Object.DimensionType = DimensionType       # Установить свойство (* )
        DimensionType = Object.GetDimensionType()  # Получить свойство (**)
        Object.SetDimensionType( DimensionType )   # Установить свойство (**)
        ```
        """
        ...
    @DimensionType.setter
    def DimensionType(self, value: int): ...

    @property
    def Length(self) -> float:  # iangledimension3d_length.html
        """
        ## Length - Длина выносной линии

        Тип данных: double

        ### Синтаксис Automation

        ```
        Length = Object.Length      # Получить свойство (* )
        Object.Length = Length      # Установить свойство (* )
        Object.Length = Length      # Получить свойство (**)
        Object.SetLength( Length )  # Установить свойство (**)
        ```
        """
        ...
    @Length.setter
    def Length(self, value: float): ...

    @property
    def Object1(self) -> IModelObject:  # iangledimension3d_object1.html
        """
        ## Object1 - Первый объект

        Тип данных: указатель на интерфейс модельного объекта IModelObject

        При создании углового размера используются прямолинейные и плоские объекты, которые являются сторонами угла.

        Прямолинейные объекты:

        •отрезок в эскизе,

        •сегмент ломаной,

        •вспомогательная ось,

        •ребро тела или поверхности.

        Плоские объекты:

        •координатная плоскость,

        •вспомогательная плоскость,

        •грань тела или поверхности.

        ### Синтаксис Automation

        ```
        Object1 = Object.Object1       # Получить свойство (* )
        Object.Object1 = Object1       # Установить свойство (* )
        Object1 = Object.GetObject1()  # Получить свойство (**)
        Object.SetObject1( Object1 )   # Установить свойство (**)
        ```
        """
        ...
    @Object1.setter
    def Object1(self, value: IModelObject): ...

    @property
    def Object2(self) -> IModelObject:  # iangledimension3d_object2.html
        """
        ## Object2 - Второй объект

        Тип данных: указатель на интерфейс модельного объекта IModelObject

        При создании углового размера используются прямолинейные и плоские объекты, которые являются сторонами угла.

        Прямолинейные объекты:

        •отрезок в эскизе,

        •сегмент ломаной,

        •вспомогательная ось,

        •ребро тела или поверхности.

        Плоские объекты:

        •координатная плоскость,

        •вспомогательная плоскость,

        •грань тела или поверхности.

        ### Синтаксис Automation

        ```
        Object2 = Object.Object2       # Получить свойство (* )
        Object.Object2 = Object2       # Установить свойство (* )
        Object2 = Object.GetObject2()  # Получить свойство (**)
        Object.SetObject2( Object2 )   # Установить свойство (**)
        ```
        """
        ...
    @Object2.setter
    def Object2(self, value: IModelObject): ...

    def GetCenterPoint(self) -> bool:  # iangledimension3d_getcenterpoint.html
        """
        ## GetCenterPoint - Получить координаты центра

        ### Синтаксис Automation

        ```
        BOOL GetCenterPoint( double * X,
        double * Y,
        double * Z );
        ```

        ### Выходные параметры

        +--------------------------------------------------------------------------------+
        | X, Y, Z | - координаты точки на оси поверхности или центр окружности или дуги. |
        +--------------------------------------------------------------------------------+

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def Inverse(self) -> bool:  # iangledimension3d_inverse.html
        """
        ## Inverse - Изменить направление размерных линий

        Функция позволяет изменять направление размерных линий на противоположное, сохраняя их длину.

        ### Синтаксис Automation

        ```
        BOOL Inverse( );
        ```

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def SetCenterPoint(self, X, Y, Z) -> bool:  # iangledimension3d_setcenterpoint.html
        """
        ## SetCenterPoint - Установить координаты центра

        Функция позволяет задавать новое положение размера на оси поверхности.

        Изменение положения размера доступно для незафиксированного размера, у которого измеряемым объектом является цилиндрическая, коническая или тороидальная поверхность. Заданная точка будет спроецирована на ось поверхности.

        ### Синтаксис Automation

        ```
        BOOL SetCenterPoint( double X,
        double Y,
        double Z );
        ```

        ### Входные параметры

        +--------------------------------------------------------------------------------+
        | X, Y, Z | - координаты точки на оси поверхности или центр окружности или дуги. |
        +--------------------------------------------------------------------------------+

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...




class IArcDimension3D(IModelObject):  # iarcdimension3d.html
    """
    ## Интерфейс IArcDimension3D

    Интерфейс размера дуги окружности 3D

    Интерфейс позволяет получить и задать свойства размера размера дуги окружности в 3D документе.

    Интерфейс можно получить у коллекции размеров дуг окружностей в 3D документе, используя свойство IArcDimensions3D::ArcDimension3D или метод IArcDimensions3D::Add.

    После задания параметров размера требуется вызвать метод IModelObject::Update.

    Интерфейсы IDimensionText и IDimensionParams являются дополнительными. Их можно получить с помощью метода IUnknown::QueryInterface.

    Версия: Компас v20
    """

    @property
    def Length(self) -> float:  # iarcdimension3d_length.html
        """
        ## Length - Длина выносной линии

        Тип данных: double

        ### Синтаксис Automation

        ```
        Length = Object.Length       # Получить свойство (* )
        Object.Length = Length       # Установить свойство (* )
        Length = Object.GetLength()  # Получить свойство (**)
        Object.SetLength( Length )   # Установить свойство (**)
        ```
        """
        ...
    @Length.setter
    def Length(self, value: float): ...

    @property
    def Object(self) -> IModelObject:  # iarcdimension3d_object.html
        """
        ## Object - Дуга окружности

        Тип данных: Указатель на интерфейс IModelObject.

        ### Синтаксис Automation

        ```
        Object = Object.Object       # Получить свойство (* )
        Object.Object = Object       # Установить свойство (* )
        Object = Object.GetObject()  # Получить свойство (**)
        Object.SetObject( Object )   # Установить свойство (**)
        ```
        """
        ...
    @Object.setter
    def Object(self, value: IModelObject): ...




class IBaseLineDimension3D(IModelObject):  # ibaselinedimension3d.html
    """
    ## Интерфейс IBaseLineDimension3D

    Интерфейс параметров линейного размера от отрезка до точки 3D.

    Интерфейс позволяет получить и задать свойства линейного размера от отрезка до точки в 3D документе.

    Интерфейс можно получить у коллекции линейных размеров в 3D документе, используя свойство ILineDimensions3D::LineDimension3D или метод ILineDimensions3D::Add.

    После задания параметров размера требуется вызвать метод IModelObject::Update.

    Интерфейсы IDimensionText и IDimensionParams являются дополнительными. Их можно получить с помощью метода IUnknown::QueryInterface.

    1. Интерфейс позволяет проставить линейный размер между прямолинейным и точечным объектами. При простановке линейного размера расстояния от ребра до точки в качестве первого объекта можно использовать только прямолинейные объекты, в качестве второго объекта можно использовать только точечные объекты. При простановке линейного размера на плоскости можно использовать и прямолинейные и точечные объекты.

    Прямолинейные объекты:

    •отрезок в эскизе,

    •сегмент ломаной,

    •координатная ось,

    •вспомогательная ось,

    •ребро тела или поверхности.

    Точечные объекты:

    •точка в эскизе,

    •пространственная точка,

    •начало координат,

    •вершина пространственной кривой,

    •вершина тела или поверхности.

    2. Свойство IDimensionText::NominalValue возвращает расстояние.
    """

    @property
    def Length(self) -> float:  # ibaselinedimension3d_length.html
        """
        ## Length - Длина выносной линии

        Тип данных: double

        ### Синтаксис Automation

        ```
        Length = Object.Length       # Получить свойство (* )
        Object.Length = Length       # Установить свойство (* )
        Length = Object.GetLength()  # Получить свойство (**)
        Object.SetLength( Length )   # Установить свойство (**)
        ```
        """
        ...
    @Length.setter
    def Length(self, value: float): ...

    @property
    def Object1(self) -> IModelObject:  # ibaselinedimension3d_object1.html
        """
        ## Object1 - Первый объект

        Тип данных: указатель на интерфейс модельного объекта IModelObject

        При создании линейного размера через по ребру и точке в качестве первого объекта можно использовать только прямолинейные объекты. При создании линейного размера на плоскости можно использовать и прямолинейные и точечные объекты.

        ### Синтаксис Automation

        ```
        Object1 = Object.Object1       # Получить свойство (* )
        Object.Object1 = Object1       # Установить свойство (* )
        Object1 = Object.GetObject1()  # Получить свойство (**)
        Object.SetObject1( Object1 )   # Установить свойство (**)
        ```
        """
        ...
    @Object1.setter
    def Object1(self, value: IModelObject): ...

    @property
    def Object2(self) -> IModelObject:  # ibaselinedimension3d_object2.html
        """
        ## Object2 - Второй объект

        Тип данных: указатель на интерфейс модельного объекта IModelObject

        При создании линейного размера через по ребру и точке в качестве первого объекта можно использовать только прямолинейные объекты. При создании линейного размера на плоскости можно использовать и прямолинейные и точечные объекты.

        ### Синтаксис Automation

        ```
        Object2 = Object.Object2       # Получить свойство (* )
        Object.Object2 = Object2       # Установить свойство (* )
        Object2 = Object.GetObject2()  # Получить свойство (**)
        Object.SetObject2( Object2 )   # Установить свойство (**)
        ```
        """
        ...
    @Object2.setter
    def Object2(self, value: IModelObject): ...




class ILineDimension3D(IModelObject):  # ilinedimension3d.html
    """
    ## Интерфейс ILineDimension3D

    Интерфейс параметров линейного размера на плоскости 3D.

    Интерфейс позволяет получить и задать свойства линейного размера в 3D документе.

    Интерфейс можно получить у коллекции линейных размеров в 3D документе, используя свойство ILineDimensions3D::LineDimension3D или метод ILineDimensions3D::Add.

    После задания парамеров размера требуется вызвать метод IModelObject::Update.

    Интерфейсы IDimensionText и IDimensionParams являются дополнительными. Их можно получить с помощью метода IUnknown::QueryInterface.

    В качестве направляющей плоскости можно использовать:

    •координатную плоскость,

    •вспомогательную плоскость,

    •грань тела или поверхности.
    """

    @property
    def Length(self):
        ...
    @Length.setter
    def Length(self, value): ...

    @property
    def Object1(self):
        ...
    @Object1.setter
    def Object1(self, value): ...

    @property
    def Object2(self):
        ...
    @Object2.setter
    def Object2(self, value): ...

    @property
    def Plane(self) -> IModelObject:  # ilinedimension3d_plane.html
        """
        ## Plane - Базовая плоскость

        Тип данных: указатель на интерфейс модельного объекта IModelObject

        В качестве направляющей плоскости можно использовать:

        •координатную плоскость,

        •вспомогательную плоскость,

        •грань тела или поверхности.

        ### Синтаксис Automation

        ```
        Plane = Object.Plane       # Получить свойство (* )
        Plane = Object.Plane       # Установить свойство (* )
        Plane = Object.GetPlane()  # Получить свойство (**)
        Object.SetPlane( Plane )   # Установить свойство (**)
        ```
        """
        ...
    @Plane.setter
    def Plane(self, value: IModelObject): ...




class IDiametralDimension3D(IModelObject):  # idiametraldimension3d.html
    """
    ## Интерфейс IDiametralDimension3D

    Интерфейс параметров диаметрального размера 3D.

    Интерфейс позволяет получить и задать свойства диаметрального размера в 3D документе.

    Интерфейс можно получить у коллекции диаметральных размеров, используя свойство IDiametralDimensions3D::DiametralDimension3D или метод IDiametralDimensions3D::Add.

    После задания параметров размера требуется вызвать метод IModelObject::Update.

    Интерфейсы IDimensionText и IDimensionParams являются дополнительными. Их можно получить с помощью метода IUnknown::QueryInterface.

    1. При создании радиального и диаметрального размеров используются следующие объекты:

    •окружность (дуга окружности) в эскизе,

    •ребро тела или поверхности, имеющее форму окружности (дуги окружности),

    •грань тела или поверхности, имеющая цилиндрическую, коническую, сферическую или тороидальную форму.

    При выборе в качестве базового объекта окружности в эскизе размер проставляется в плоскости эскиза.

    При выборе в качестве базового объекта ребра тела или поверхности размер проставляется в плоскости, в которой находится выбранное ребро.

    Положение размера можно задавать произвольно или фиксировать.

    Способы задания произвольного положения размера:

    •установите координаты центра с помощью функции SetCenterPoint. Координаты будут спроецированы на ось грани.

    •укажите точку на поверхности или ребре с помощью функции SetSurfacePoint.

    Для фиксации положения размера укажите точечный объект или плоский объект, параллельный плоскости простановки размера. Размер будет проставлен в плоскости, проходящей через объект фиксации. Между размером и объектом фиксации формируется ассоциативная связь. Благодаря этой связи размер следует за объектом фиксации при изменении положения последнего.

    2. Свойство IDimensionText::NominalValue возвращает диаметр.
    """

    @property
    def Angle(self) -> float:  # idiametraldimension3d_angle.html
        """
        ## Angle - Угол наклона размерной линии

        Тип данных: double

        Свойство позволяет задавать угол наклона размерной линии в плоскости размера.

        ### Синтаксис Automation

        ```
        Angle = Object.Angle       # Получить свойство (* )
        Object.Angle = Angle       # Установить свойство (* )
        Angle = Object.GetAngle()  # Получить свойство (**)
        Object.SetAngle( Angle )   # Установить свойство (**)
        ```
        """
        ...
    @Angle.setter
    def Angle(self, value: float): ...

    @property
    def DimensionType(self) -> bool:  # idiametraldimension3d_dimensiontype.html
        """
        ## DimensionType - Тип диаметрального размера (Полная размерная линия\\Размерная линия с обрывом)

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        DimensionType = Object.DimensionType       # Получить свойство (* )
        Object.DimensionType = DimensionType       # Установить свойство (* )
        DimensionType = Object.GetDimensionType()  # Получить свойство (**)
        Object.SetDimensionType( DimensionType )   # Установить свойство (**)
        ```
        """
        ...
    @DimensionType.setter
    def DimensionType(self, value: bool): ...

    @property
    def Object1(self) -> IModelObject:  # idiametraldimension3d_object1.html
        """
        ## Object1 - Измеряемый объект

        Тип данных: указатель на интерфейс модельного объекта IModelObject

        При создании радиального и диаметрального размеров используются следующие объекты:

        •окружность (дуга окружности) в эскизе,

        •ребро тела или поверхности, имеющее форму окружности (дуги окружности),

        •грань тела или поверхности, имеющая цилиндрическую, коническую, сферическую или тороидальную форму.

        При выборе в качестве базового объекта окружности в эскизе размер проставляется в плоскости эскиза.

        При выборе в качестве базового объекта ребра тела или поверхности размер проставляется в плоскости, в которой находится выбранное ребро.

        ### Синтаксис Automation

        ```
        Object1 = Object.Object1       # Получить свойство (* )
        Object.Object1 = Object1       # Установить свойство (* )
        Object1 = Object.GetObject1()  # Получить свойство (**)
        Object.SetObject1( Object1 )   # Установить свойство (**)
        ```
        """
        ...
    @Object1.setter
    def Object1(self, value: IModelObject): ...

    @property
    def PlaneObject(self) -> IModelObject:  # idiametraldimension3d_planeobject.html
        """
        ## PlaneObject - Фиксирующий объект

        Тип данных: указатель на интерфейс модельного объекта IModelObject

        Положение размера можно зафиксировать. Для фиксации положения размера укажите точечный объект или плоский объект, параллельный плоскости простановки размера. Размер будет проставлен в плоскости, проходящей через объект фиксации. Между размером и объектом фиксации формируется ассоциативная связь. Благодаря этой связи размер следует за объектом фиксации при изменении положения последнего.

        Для снятия фиксации установите значение свойства == NULL.

        ### Синтаксис Automation

        ```
        PlaneObject = Object.PlaneObject       # Получить свойство (* )
        Object.PlaneObject = PlaneObject       # Установить свойство (* )
        PlaneObject = Object.GetPlaneObject()  # Получить свойство (**)
        Object.SetPlaneObject( PlaneObject )   # Установить свойство (**)
        ```
        """
        ...
    @PlaneObject.setter
    def PlaneObject(self, value: IModelObject): ...

    def GetCenterPoint(self) -> bool:  # idiametraldimension3d_getcenterpoint.html
        """
        ## GetCenterPoint - Получить координаты центра

        ### Синтаксис Automation

        ```
        BOOL GetCenterPoint( double * X,
        double * Y,
        double * Z );
        ```

        ### Выходные параметры

        +--------------------------------------------------------------------------------+
        | X, Y, Z | - координаты точки на оси поверхности или центр окружности или дуги. |
        +--------------------------------------------------------------------------------+

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def GetSurfacePoint(self) -> bool:  # idiametraldimension3d_getsurfacepoint.html
        """
        ## GetSurfacePoint - Получить точку пересечения размера с поверхностью или ребром

        ### Синтаксис Automation

        ```
        BOOL GetSurfacePoint( double * X,
        double * Y,
        double * Z );
        ```

        ### Выходные параметры

        +-------------------------------+
        | X, Y, Z | - координаты точки. |
        +-------------------------------+

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def SetCenterPoint(self, X, Y, Z) -> bool:  # idiametraldimension3d_setcenterpoint.html
        """
        ## SetCenterPoint - Установить координаты центра

        Функция позволяет задавать новое положение размера на оси поверхности.

        Изменение положения размера доступно для незафиксированного размера, у которого измеряемым объектом является цилиндрическая, коническая или тороидальная поверхность. Заданная точка будет спроецирована на ось поверхности.

        ### Синтаксис Automation

        ```
        BOOL SetCenterPoint( double X,
        double Y,
        double Z );
        ```

        ### Входные параметры

        +--------------------------------------------------------------------------------+
        | X, Y, Z | - координаты точки на оси поверхности или центр окружности или дуги. |
        +--------------------------------------------------------------------------------+

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def SetSurfacePoint(self, X, Y, Z) -> bool:  # idiametraldimension3d_setsurfacepoint.html
        """
        ## SetSurfacePoint - Установить точку пересечения размера с поверхностью или ребром

        Функция позволяет задавать новое положение размера относительно грани или ребра. При указании точки на поверхности точка центра пересчитается.

        ### Синтаксис Automation

        ```
        BOOL SetSurfacePoint( double X,
        double Y,
        double Z );
        ```

        ### Входные параметры

        +-------------------------------+
        | X, Y, Z | - координаты точки. |
        +-------------------------------+

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...




class IRadialDimension3D(IModelObject):  # iradialdimension3d.html
    """
    ## Интерфейс IRadialDimension3D

    Интерфейс параметров радиального размера 3D.

    Интерфейс позволяет получить и задать свойства радиального размера в 3D документе.

    Интерфейс можно получить у коллекции радиальных размеров, используя свойство IRadialDimensions3D::RadialDimension3D или метод IRadialDimensions3D::Add.

    После задания параметров размера требуется вызвать метод IModelObject::Update.

    Интерфейсы IDimensionText и IDimensionParams являются дополнительными. Их можно получить с помощью метода IUnknown::QueryInterface.

    1. При создании радиального и диаметрального размеров используются следующие объекты:

    •окружность (дуга окружности) в эскизе,

    •ребро тела или поверхности, имеющее форму окружности (дуги окружности),

    •грань тела или поверхности, имеющая цилиндрическую, коническую, сферическую или тороидальную форму.

    При выборе в качестве базового объекта окружности в эскизе размер проставляется в плоскости эскиза.

    При выборе в качестве базового объекта ребра тела или поверхности размер проставляется в плоскости, в которой находится выбранное ребро.

    Положение размера можно задавать произвольно или фиксировать.

    Способы задания произвольного положения размера:

    •установите координаты центра с помощью функции SetCenterPoint. Координаты будут спроецированы на ось грани.

    •укажите точку на поверхности или ребре с помощью функции SetSurfacePoint.

    Для фиксации положения размера укажите точечный объект или плоский объект, параллельный плоскости простановки размера. Размер будет проставлен в плоскости, проходящей через объект фиксации. Между размером и объектом фиксации формируется ассоциативная связь. Благодаря этой связи, размер следует за объектом фиксации при изменении положения последнего.

    2. Свойство IDimensionText::NominalValue возвращает радиус.
    """

    @property
    def Angle(self):
        ...
    @Angle.setter
    def Angle(self, value): ...

    @property
    def DimensionType(self):
        ...
    @DimensionType.setter
    def DimensionType(self, value): ...

    @property
    def Object1(self):
        ...
    @Object1.setter
    def Object1(self, value): ...

    @property
    def PlaneObject(self):
        ...
    @PlaneObject.setter
    def PlaneObject(self, value): ...

    def GetCenterPoint(self) -> bool:  # iradialdimension3d_getcenterpoint.html
        """
        ## GetCenterPoint - Получить координаты центра

        ### Синтаксис Automation

        ```
        BOOL GetCenterPoint( double * X,
        double * Y,
        double * Z );
        ```

        ### Выходные параметры

        +--------------------------------------------------------------------------------+
        | X, Y, Z | - координаты точки на оси поверхности или центр окружности или дуги. |
        +--------------------------------------------------------------------------------+

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def GetSurfacePoint(self) -> bool:  # iradialdimension3d_getsurfacepoint.html
        """
        ## GetSurfacePoint - Получить точку пересечения размера с поверхностью или ребром

        ### Синтаксис Automation

        ```
        BOOL GetSurfacePoint( double * X,
        double * Y,
        double * Z );
        ```

        ### Выходные параметры

        +-------------------------------+
        | X, Y, Z | - координаты точки. |
        +-------------------------------+

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def SetCenterPoint(self, X, Y, Z) -> bool:  # iradialdimension3d_setcenterpoint.html
        """
        ## SetCenterPoint - Установить координаты центра

        Функция позволяет задавать новое положение размера на оси поверхности.

        Изменение положения размера доступно для незафиксированного размера, у которого измеряемым объектом является цилиндрическая, коническая или тороидальная поверхность. Заданная точка будет спроецирована на ось поверхности.

        ### Синтаксис Automation

        ```
        BOOL SetCenterPoint( double X,
        double Y,
        double Z );
        ```

        ### Входные параметры

        +--------------------------------------------------------------------------------+
        | X, Y, Z | - координаты точки на оси поверхности или центр окружности или дуги. |
        +--------------------------------------------------------------------------------+

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def SetSurfacePoint(self, X, Y, Z) -> bool:  # iradialdimension3d_setsurfacepoint.html
        """
        ## SetSurfacePoint - Установить точку пересечения размера с поверхностью или ребром

        Функция позволяет задавать новое положение размера относительно грани или ребра. При указании точки на поверхности точка центра пересчитается.

        ### Синтаксис Automation

        ```
        BOOL SetSurfacePoint( double X,
        double Y,
        double Z );
        ```

        ### Входные параметры

        +-------------------------------+
        | X, Y, Z | - координаты точки. |
        +-------------------------------+

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...




class IDesignation3D(IDispatch):  # idesignation3d.html
    """
    ## Интерфейс IDesignation3D

    Интерфейс обозначения 3D.

    Интерфейс является дополнительным для 3D обозначений.

    Версия Компас v21
    """

    def GetDesignationPoint(self, DimensionPoint, PointIndex):  # idesignation3d_getdesignationpoint.html
        """
        ## GetDesignationPoint - Получить координаты точек размера

        ### Синтаксис Automation

        ```
        BOOL GetDesignationPoint( ksDesignationPointEnum DimensionPoint, long PointIndex, double * X, double * Y, double * Z );
        ```

        ### Входные параметры

        +----------------------------------------------------------------------+
        | DimensionPoint | - тип точки из перечисления ksDesignationPointEnum, |
        +----------------------------------------------------------------------+
        | PointIndex     | - индекс точки.                                     |
        +----------------------------------------------------------------------+

        ### Выходные параметры

        +-------------------------------+
        | X, Y, Z | - координаты точки. |
        +-------------------------------+
        """
        ...




class IBases3D(IModelObjects):  # ibases3d.html
    """
    ## Интерфейс IBases3D

    Интерфейс коллекции обозначений базы 3D.

    Интерфейс позволяет получить обозначение базы 3D.

    Получить интерфейс коллекции можно используя свойство контейнера условных обозначений ISymbols3DContainer::Bases3D.
    """

    def Add(self) -> IBase3D:  # ibases3d_add.html
        """
        ## Add - Создать новый элемент и добавить его в коллекцию

        ### Синтаксис Automation

        ```
        LPDISPATCH Add;
        ```

        ### Возвращаемое значение

        - Указатель на интерфейс базы 3D IBase3D.
        """
        ...

    def Base3D(self, Index) -> IBase3D:  # ibases3d_base3d.html
        """
        ## Base3D - Обозначение базы, заданное по индексу

        Тип данных: указатель на интерфейс IBase3D

        1. В качестве индекса может использоваться индекс в коллекции и reference объекта.

        2. Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        Base3D = iObject.Base3D( Index );     # Получить свойство (* )
        Base3D = iObject.GetBase3D( Index );  # Получить свойство (**)
        ```

        ### Входные параметры

        +--------------------------------------------------------+
        | Index | VT_BSTR - имя объекта, VT_I4 - индекс объекта. |
        +--------------------------------------------------------+
        """
        ...




class ILeaders3D(IModelObjects):  # ileaders3d.html
    """
    ## Интерфейс ILeaders3D

    Интерфейс коллекции линий-выносок 3D.

    Получить интерфейс коллекции можно, используя свойство контейнера условных обозначений 3D ISymbols3DContainer::Leaders3D.
    """

    def Add(self, DimType) -> IBaseLeader3D|IConjunctivePoint:  # ileaders3d_add.html
        """
        ## Add - Создать новый элемент и добавить его в коллекцию

        1. Метод позволяет создать новый интерфейс присоединительной точки.

        2. После получения нового интерфейса нужно задать параметры и вызвать метод IModelObject::Update.

        ### Синтаксис Automation

        ```
        LPDISPATCH Add( ksObj3dTypeEnum DimType );
        ```

        ### Входные параметры

        +--------------------------------+
        | DimType | - тип линии-выноски. |
        +--------------------------------+

        Типы линий-выносок:

        +-----------------------------------------------------+
        | o3d_leader3D         | 86 | Линия-выноска 3D,       |
        +-----------------------------------------------------+
        | o3d_markLeader3D     | 87 | Знак маркировки 3D,     |
        +-----------------------------------------------------+
        | o3d_positionLeader3D | 89 | Обозначение позиции 3D, |
        +-----------------------------------------------------+
        | o3d_brandLeader3D    | 90 | Знак клеймения 3D.      |
        +-----------------------------------------------------+

        ### Возвращаемое значение

        - Указатель на интерфейс линии выноски IBaseLeader3D.

        - Указатель на интерфейс присоединительной точки IConjunctivePoint.
        """
        ...

    def Leader3D(self, Index) -> IBaseLeader3D:  # ileaders3d_leader3d.html
        """
        ## Leader3D - Указатель на элемент, заданный по индексу

        Тип данных: указатель на интерфейс IBaseLeader3D

        1. Свойство доступно только для чтения.

        2. В качестве индекса может использоваться индекс в коллекции и reference объекта.

        ### Синтаксис Automation

        ```
        Leader3D = Object.Leader3D( Index )     # Получить свойство (* )
        Leader3D = Object.GetLeader3D( Index )  # Получить свойство (**)
        ```

        ### Входные параметры

        +---------------------------+
        | Index | - индекс объекта. |
        +---------------------------+
        """
        ...




class IRoughs3D(IModelObjects):  # iroughs3d.html
    """
    ## Интерфейс IRoughs3D

    Интерфейс коллекции обозначений шероховатости 3D.

    Позволяет получать и создавать обозначение 3D шероховатости.

    Получить интерфейс коллекции можно, используя свойство контейнера условных обозначений ISymbols3DContainer::Roughs3D.
    """

    def Add(self) -> IRough3D:  # iroughs3d_add.html
        """
        ## Add - Создать новый элемент и добавить его в коллекцию

        ### Синтаксис Automation

        ```
        LPDISPATCH Add;
        ```

        ### Возвращаемое значение

        - Указатель на интерфейс шероховатости 3D IRough3D.
        """
        ...

    def Rough3D(self, Index) -> IRough3D:  # iroughs3d_rough3d.html
        """
        ## Rough3D - Обозначение шероховатости 3D, заданное по индексу

        Тип данных: указатель на интерфейс IRough3D

        1. В качестве индекса может использоваться индекс в коллекции и reference объекта.

        2. Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        Rough3D = iObject.Rough3D( Index );     # Получить свойство (* )
        Rough3D = iObject.GetRough3D( Index );  # Получить свойство (**)
        ```

        ### Входные параметры

        +--------------------------------------------------------+
        | Index | VT_BSTR - имя объекта, VT_I4 - индекс объекта. |
        +--------------------------------------------------------+
        """
        ...




class ITolerances3D(IModelObjects):  # itolerances3d.html
    """
    ## Интерфейс ITolerances3D

    Интерфейс коллекции допусков формы 3D.

    Позволяет получать и создавать обозначения допуска формы 3D.

    Получить интерфейс коллекции можно используя свойство контейнера условных обозначений ISymbols3DContainer::Tolerances3D.
    """

    def Add(self) -> ITolerance3D:  # itolerances3d_add.html
        """
        ## Add - Создать новый элемент и добавить его в коллекцию

        ### Синтаксис Automation

        ```
        LPDISPATCH Add();
        ```

        ### Возвращаемое значение

        - Указатель на интерфейс допуска формы Itolerance3D.
        """
        ...

    def Tolerance3D(self, Index) -> ITolerance3D:  # itolerances3d_tolerance3d.html
        """
        ## Tolerance3D - Указатель на допуск формы 3D, заданный по индексу

        Тип данных: указатель на интерфейс ITolerance

        1. В качестве индекса может использоваться индекс в коллекции и reference объекта.

        2. Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        Tolerance = iObject.Tolerance( Index );     # Получить свойство (* )
        Tolerance = iObject.GetTolerance( Index );  # Получить свойство (**)
        ```

        ### Входные параметры

        +----------------------------------------------------------+
        | Index | - VT_BSTR - имя объекта, VT_I4 - индекс объекта. |
        +----------------------------------------------------------+
        """
        ...




class IBase3D(IModelObject):  # ibase3d.html
    """
    ## Интерфейс IBase3D

    Интерфейс обозначения базы 3D.

    Интерфейс можно получить у коллекции обозначений базы 3D, используя свойство IBases3D::Base3D или метод IBases3D::Add.
    """

    @property
    def AutoSorted(self) -> bool:  # ibase3d_autosorted.html
        """
        ## AutoSorted - Автосортировка

        Тип данных: BOOL

        Значения свойства:

        +----------------------------+
        | TRUE  | - включить опцию,  |
        +----------------------------+
        | FALSE | - отключить опцию. |
        +----------------------------+

        1. По умолчанию значение свойства равно TRUE.

        2. При отключенной опции доступно редактирование текста 3D базы с помощью свойства IBase3D::Text.

        ### Синтаксис Automation

        ```
        AutoSorted = Object.AutoSorted       # Получить свойство (* )
        Object.AutoSorted = AutoSorted       # Установить свойство (* )
        AutoSorted = Object.GetAutoSorted()  # Получить свойство (**)
        Object.SetAutoSorted( AutoSorted )   # Установить свойство (**)
        ```
        """
        ...
    @AutoSorted.setter
    def AutoSorted(self, value: bool): ...

    @property
    def BaseObject(self) -> IModelObject:  # ibase3d_baseobject.html
        """
        ## BaseObject - Опорный объект

        Тип данных: указатель на интерфейс IModelObject

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        BaseObject = Object.BaseObject       # Получить свойство (* )
        BaseObject = Object.GetBaseObject()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def BasePlane(self) -> int:  # ibase3d_baseplane.html
        """
        ## BasePlane - Базовая плоскость

        Тип данных: из перечисления ksObj3dTypeEnum

        Свойство позволяет устанавливать и получать тип плоскости, в которой располагается объект, из перечисления ksObj3dTypeEnum.

        ### Синтаксис Automation

        ```
        BasePlane = Object.BasePlane       # Получить свойство (* )
        Object.BasePlane = BasePlane       # Установить свойство (* )
        BasePlane = Object.GetBasePlane()  # Получить свойство (**)
        Object.SetBasePlane( BasePlane )   # Установить свойство (**)
        ```
        """
        ...
    @BasePlane.setter
    def BasePlane(self, value: int): ...

    @property
    def DrawType(self) -> bool:  # ibase3d_drawtype.html
        """
        ## DrawType - Способ отрисовки обозначения базы

        Тип данных: BOOL

        Значения свойства:

        +--------------------------------------+
        | TRUE  | - перпендикулярно к объекту, |
        +--------------------------------------+
        | FALSE | - произвольное расположение. |
        +--------------------------------------+

        По умолчанию значение свойства равно TRUE.

        ### Синтаксис Automation

        ```
        DrawType = Object.DrawType       # Получить свойство (* )
        Object.DrawType = DrawType       # Установить свойство (* )
        DrawType = Object.GetDrawType()  # Получить свойство (**)
        Object.SetDrawType( DrawType )   # Установить свойство (**)
        ```
        """
        ...
    @DrawType.setter
    def DrawType(self, value: bool): ...

    @property
    def PositionObject(self) -> IModelObject:  # ibase3d_positionobject.html
        """
        ## PositionObject - Положение плоскости обозначения

        Тип данных: указатель на интерфейс IModelObject

        Свойство позволяет устанавливать и получать объект, на котором будет располагаться плоскость.

        ### Синтаксис Automation

        ```
        PositionObject = Object.PositionObject       # Получить свойство (* )
        Object.PositionObject = PositionObject       # Установить свойство (* )
        PositionObject = Object.GetPositionObject()  # Получить свойство (**)
        Object.SetPositionObject( PositionObject )   # Установить свойство (**)
        ```
        """
        ...
    @PositionObject.setter
    def PositionObject(self, value: IModelObject): ...

    @property
    def Text(self):  # ibase3d_text.html
        """
        ## Text - Текст базы

        1. Свойство доступно только для чтения.

        2. Свойство позволяет получить указатель на интерфейс текста IText для редактирования текста 3D базы. Указатель на интерфейс IText вернётся только в случае отключенной опции "Автосортировка". Если автосортировка текста включена, свойство вернёт NULL.

        ### Синтаксис Automation

        ```
        Text = Object.Text       # Получить свойство (* )
        Text = Object.GetText()  # Получить свойство (**)
        ```
        """
        ...

    def GetBranchBeginPoint(self) -> bool:  # ibase3d_getbranchbeginpoint.html
        """
        ## GetBranchBeginPoint - Получить точку привязки обозначения базы на поверхности

        ### Синтаксис Automation

        ```
        BOOL GetBranchBeginPoint( double * x,
        double * x,
        double * z );
        ```

        ### Выходные параметры

        +------------------------------------------------------------------------+
        | x, y, z | - координаты точки привязки обозначения базы на поверхности. |
        +------------------------------------------------------------------------+

        ### Возвращаемое значение

        +-----------------------------------------+
        | TRUE  | - в случае удачного завершения, |
        +-----------------------------------------+
        | FALSE | - в случае неудачи.             |
        +-----------------------------------------+
        """
        ...

    def GetBranchEndPoint(self) -> bool:  # ibase3d_getbranchendpoint.html
        """
        ## GetBranchEndPoint - Получить точку положения надписи обозначения базы

        ### Синтаксис Automation

        ```
        BOOL GetBranchEndPoint( double * x,
        double * x,
        double * z );
        ```

        ### Выходные параметры

        +------------------------------------------------------------------+
        | x, y, z | - координаты точки положения надписи обозначения базы. |
        +------------------------------------------------------------------+

        ### Возвращаемое значение

        +-----------------------------------------+
        | TRUE  | - в случае удачного завершения, |
        +-----------------------------------------+
        | FALSE | - в случае неудачи.             |
        +-----------------------------------------+
        """
        ...

    def GetPosition(self) -> bool:  # ibase3d_getposition.html
        """
        ## GetPosition - Получить точку, задающую положение плоскости объекта

        ### Синтаксис Automation

        ```
        BOOL GetPosition ( double * x,
        double * x,
        double * z );
        ```

        ### Выходные параметры

        +-----------------------------------------------------------+
        | x, y, z | - координаты точки, задающей положения объекта. |
        +-----------------------------------------------------------+

        ### Возвращаемое значение

        +-----------------------------------------+
        | TRUE  | - в случае удачного завершения, |
        +-----------------------------------------+
        | FALSE | - в случае неудачи.             |
        +-----------------------------------------+
        """
        ...

    def SetBranchBeginPoint(self, X, Y, Z, Object) -> bool:  # ibase3d_setbranchbeginpoint.html
        """
        ## SetBranchBeginPoint - Установить точку привязки обозначения базы на поверхности

        ### Синтаксис Automation

        ```
        BOOL SetBranchBeginPoint( double x,
        double x,
        double z );
        ```

        ### Входные параметры

        +------------------------------------------------------------------------+
        | x, y, z | - координаты точки привязки обозначения базы на поверхности. |
        +------------------------------------------------------------------------+

        ### Возвращаемое значение

        +-----------------------------------------+
        | TRUE  | - в случае удачного завершения, |
        +-----------------------------------------+
        | FALSE | - в случае неудачи.             |
        +-----------------------------------------+
        """
        ...

    def SetBranchEndPoint(self, X, Y, Z) -> bool:  # ibase3d_setbranchendpoint.html
        """
        ## SetBranchEndPoint - Установить точку положения надписи обозначения базы

        ### Синтаксис Automation

        ```
        BOOL SetBranchEndPoint( double x,
        double x,
        double z );
        ```

        ### Входные параметры

        +------------------------------------------------------------------+
        | x, y, z | - координаты точки положения надписи обозначения базы. |
        +------------------------------------------------------------------+

        ### Возвращаемое значение

        +-----------------------------------------+
        | TRUE  | - в случае удачного завершения, |
        +-----------------------------------------+
        | FALSE | - в случае неудачи.             |
        +-----------------------------------------+
        """
        ...

    def SetPosition(self, X, Y, Z) -> bool:  # ibase3d_setposition.html
        """
        ## SetPosition - Получить точку, задающую положение плоскости объекта

        ### Синтаксис Automation

        ```
        BOOL SetPosition ( double x,
        double x,
        double z );
        ```

        ### Входные параметры

        +-----------------------------------------------------------+
        | x, y, z | - координаты точки, задающей положения объекта; |
        +-----------------------------------------------------------+

        ### Возвращаемое значение

        +-----------------------------------------+
        | TRUE  | - в случае удачного завершения, |
        +-----------------------------------------+
        | FALSE | - в случае неудачи.             |
        +-----------------------------------------+
        """
        ...




class IBaseLeader3D(IModelObject):  # ibaseleader3d.html
    """
    ## Интерфейс IBaseLeader3D

    Интерфейс обозначения линии-выноски 3D.

    1. Интерфейс можно получить у коллекции линий выносок 3D, используя свойство ILeaders3D::Leader3D или метод ILeaders3D::Add.

    2. Посредством вызова метода IUnknown::QueryInterface (const GUID far& iid, void** pif) интерфейс позволяет получить следующие дополнительные интерфейсы:

    +---------------------------------------------------------------------------------------+
    | Тип линии-выноски                           | ksObj3dTypeEnum      | Интерфейс,       |
    +---------------------------------------------------------------------------------------+
    | Простая линия-выноска                       | o3d_leader3D         | ILeader,         |
    +---------------------------------------------------------------------------------------+
    | Линия-выноска для обозначения позиции       | o3d_positionLeader3D | IPositionLeader, |
    +---------------------------------------------------------------------------------------+
    | Линия-выноска для для обозначения клеймения | o3d_brandLeader3D    | IBrandLeader,    |
    +---------------------------------------------------------------------------------------+
    | Линия-выноска для обозначения маркирования  | o3d_markLeader3D     | IMarkLeader,     |
    +---------------------------------------------------------------------------------------+
    | Знак изменения                              |                      | IChangeLeader,   |
    +---------------------------------------------------------------------------------------+
    | Ответвления                                 |                      | IBranchs3D.      |
    +---------------------------------------------------------------------------------------+

    3. После задания параметров обозначения требуется вызвать метод IModelObject::Update.
    """

    @property
    def ArrowType(self) -> int:  # ibaseleader3d_arrowtype.html
        """
        ## ArrowType - Тип стрелки линии-выноски

        Тип данных: из перечисления ksArrowEnum

        Свойство позволяет устанавливать и получать тип стрелки ответвления.

        ### Синтаксис Automation

        ```
        ArrowType = Object.ArrowType       # Получить свойство (* )
        Object.ArrowType = ArrowType       # Установить свойство (* )
        ArrowType = Object.GetArrowType()  # Получить свойство (**)
        Object.SetArrowType( ArrowType )   # Установить свойство (**)
        ```

        ### Входные параметры

        +----------------------------------------+
        | Index | - индекс ответвления ( long ). |
        +----------------------------------------+
        """
        ...
    @ArrowType.setter
    def ArrowType(self, value: int): ...

    @property
    def BasePlane(self) -> int:  # ibaseleader3d_baseplane.html
        """
        ## BasePlane - Базовая плоскость

        Тип данных: из перечисления ksObj3dTypeEnum

        Значения свойства:

        +----------------------------------+
        | o3d_unknown  | 0 | Неизвестный   |
        +----------------------------------+
        | o3d_planeXOY | 1 | Плоскость XOY |
        +----------------------------------+
        | o3d_planeXOZ | 2 | Плоскость XOZ |
        +----------------------------------+
        | o3d_planeYOZ | 3 | Плоскость YOZ |
        +----------------------------------+

        Свойство позволяет устанавливать и получать тип плоскости из перечисления ksObj3dTypeEnum, в которой располагается объект.

        ### Синтаксис Automation

        ```
        BasePlane = Object.BasePlane       # Получить свойство (* )
        Object.BasePlane = BasePlane       # Установить свойство (* )
        BasePlane = Object.GetBasePlane()  # Получить свойство (**)
        Object.SetBasePlane( BasePlane )   # Установить свойство (**)
        ```
        """
        ...
    @BasePlane.setter
    def BasePlane(self, value: int): ...

    @property
    def PositionObject(self) -> IModelObject:  # ibaseleader3d_positionobject.html
        """
        ## PositionObject - Объект, задающий положение плоскости обозначения

        Тип данных: указатель на интерфейс IModelObject

        Свойство позволяет устанавливать и получать объект, на котором будет располагаться плоскость объекта.

        ### Синтаксис Automation

        ```
        PositionObject = Object.PositionObject       # Получить свойство (* )
        Object.PositionObject = PositionObject       # Установить свойство (* )
        PositionObject = Object.GetPositionObject()  # Получить свойство (**)
        Object.SetPositionObject( PositionObject )   # Установить свойство (**)
        ```
        """
        ...
    @PositionObject.setter
    def PositionObject(self, value: IModelObject): ...

    def GetPosition(self) -> bool:  # ibaseleader3d_getposition.html
        """
        ## GetPosition - Получить точку, задающую положение объекта

        ### Синтаксис Automation

        ```
        BOOL GetPosition ( double * x,
        double * y,
        double * z );
        ```

        ### Выходные параметры

        +-----------------------------------------------------------+
        | x, y, z | - координаты точки, задающей положение объекта. |
        +-----------------------------------------------------------+

        ### Возвращаемое значение

        +-----------------------------------------+
        | TRUE  | - в случае удачного завершения, |
        +-----------------------------------------+
        | FALSE | - в случае неудачи.             |
        +-----------------------------------------+
        """
        ...

    def SetPosition(self, X, Y, Z) -> bool:  # ibaseleader3d_setposition.html
        """
        ## SetPosition - Установить точку, задающую положение объекта

        ### Синтаксис Automation

        ```
        BOOL SetPosition ( double x,
        double y,
        double z );
        ```

        ### Входные параметры

        +-----------------------------------------------------------+
        | x, y, z | - координаты точки, задающей положение объекта. |
        +-----------------------------------------------------------+

        ### Возвращаемое значение

        +-----------------------------------------+
        | TRUE  | - в случае удачного завершения, |
        +-----------------------------------------+
        | FALSE | - в случае неудачи.             |
        +-----------------------------------------+
        """
        ...




class IRough3D(IModelObject):  # irough3d.html
    """
    ## Интерфейс IRough3D

    Интерфейс обозначения шероховатости 3D.

    1. Интерфейс можно получить у коллекции обозначений шероховатости 3D, используя свойство IRoughs3D::Rough3D или метод IRoughs3D::Add.

    2. После задания параметров объекта требуется вызвать метод IModelObject_Update.

    3. Интерфейс IRoughParams является дополнительным, его можно получить с помощью метода IUnknown::QueryInterface.
    """

    @property
    def BaseObject(self) -> IModelObject:  # irough3d_baseobject.html
        """
        ## BaseObject - Опорный объект

        Тип данных: указатель на интерфейс IModelObject

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        BaseObject = Object.BaseObject       # Получить свойство (* )
        BaseObject = Object.GetBaseObject()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def BasePlane(self) -> int:  # irough3d_baseplane.html
        """
        ## BasePlane - Базовая плоскость

        Тип данных: из перечисления ksObj3dTypeEnum

        Свойство позволяет устанавливать и получать тип плоскости, в которой располагается объект, из перечисления ksObj3dTypeEnum.

        ### Синтаксис Automation

        ```
        BasePlane = Object.BasePlane       # Получить свойство (* )
        Object.BasePlane = BasePlane       # Установить свойство (* )
        BasePlane = Object.GetBasePlane()  # Получить свойство (**)
        Object.SetBasePlane( BasePlane )   # Установить свойство (**)
        ```
        """
        ...
    @BasePlane.setter
    def BasePlane(self, value: int): ...

    @property
    def PositionObject(self) -> IModelObject:  # irough3d_positionobject.html
        """
        ## PositionObject - Положение плоскости обозначения

        Тип данных: указатель на интерфейс IModelObject

        Свойство позволяет устанавливать и получать объект, на котором будет располагаться плоскость объекта.

        ### Синтаксис Automation

        ```
        PositionObject = Object.PositionObject       # Получить свойство (* )
        Object.PositionObject = PositionObject       # Установить свойство (* )
        PositionObject = Object.GetPositionObject()  # Получить свойство (**)
        Object.SetPositionObject( PositionObject )   # Установить свойство (**)
        ```
        """
        ...
    @PositionObject.setter
    def PositionObject(self, value: IModelObject): ...

    def GetBasePosition(self) -> bool:  # irough3d_getbaseposition.html
        """
        ## GetBasePosition - Получить точку начала выносной линии или привязки знака

        ### Синтаксис Automation

        ```
        BOOL GetBasePosition( double * x,
        double * x,
        double * z );
        ```

        ### Выходные параметры

        +------------------------------------------------------------------------+
        | x, y, z | - координаты точки начала выносной линии или привязки знака. |
        +------------------------------------------------------------------------+

        ### Возвращаемое значение

        +-----------------------------------------+
        | TRUE  | - в случае удачного завершения, |
        +-----------------------------------------+
        | FALSE | - в случае неудачи.             |
        +-----------------------------------------+
        """
        ...

    def GetPosition(self) -> bool:  # irough3d_getposition.html
        """
        ## GetPosition - Получить точку, задающую положение плоскости объекта

        ### Синтаксис Automation

        ```
        BOOL GetPosition ( double * x,
        double * x,
        double * z );
        ```

        ### Выходные параметры

        +-----------------------------------------------------------+
        | x, y, z | - координаты точки, задающей положения объекта. |
        +-----------------------------------------------------------+

        ### Возвращаемое значение

        +-----------------------------------------+
        | TRUE  | - в случае удачного завершения, |
        +-----------------------------------------+
        | FALSE | - в случае неудачи.             |
        +-----------------------------------------+
        """
        ...

    def GetShelfPosition(self) -> bool:  # irough3d_getshelfposition.html
        """
        ## GetShelfPosition - Получить точку начала полки

        ### Синтаксис Automation

        ```
        BOOL GetShelfPosition( double * x,
        double * x,
        double * z );
        ```

        ### Выходные параметры

        +--------------------------------------------+
        | x, y, z | - координаты точки начала полки. |
        +--------------------------------------------+

        ### Возвращаемое значение

        +-----------------------------------------+
        | TRUE  | - в случае удачного завершения, |
        +-----------------------------------------+
        | FALSE | - в случае неудачи.             |
        +-----------------------------------------+
        """
        ...

    def SetBasePosition(self, X, Y, Z, Object):
        ...

    def SetPosition(self, X, Y, Z) -> bool:  # irough3d_setposition.html
        """
        ## SetPosition - Установить точку, задающую положение плоскости объекта

        ### Синтаксис Automation

        ```
        BOOL SetPosition ( double x,
        double x,
        double z );
        ```

        ### Входные параметры

        +-----------------------------------------------------------+
        | x, y, z | - координаты точки, задающей положения объекта. |
        +-----------------------------------------------------------+

        ### Возвращаемое значение

        +-----------------------------------------+
        | TRUE  | - в случае удачного завершения, |
        +-----------------------------------------+
        | FALSE | - в случае неудачи.             |
        +-----------------------------------------+
        """
        ...

    def SetShelfPosition(self, X, Y, Z) -> bool:  # irough3d_setshelfposition.html
        """
        ## SetShelfPosition - Установить точку начала полки

        ### Синтаксис Automation

        ```
        BOOL SetShelfPosition( double x,
        double x,
        double z );
        ```

        ### Выходные параметры

        +--------------------------------------------+
        | x, y, z | - координаты точки начала полки. |
        +--------------------------------------------+

        ### Возвращаемое значение

        +-----------------------------------------+
        | TRUE  | - в случае удачного завершения, |
        +-----------------------------------------+
        | FALSE | - в случае неудачи.             |
        +-----------------------------------------+
        """
        ...




class ITolerance3D(IModelObject):  # itolerance3d.html
    """
    ## Интерфейс ITolerance3D

    Интерфейс обозначения допуска формы 3D.

    1. Интерфейс можно получить у коллекции обозначений допуска формы 3D, используя свойство ITolerances3D::Tolerance3D или метод ITolerances3D::Add.

    2. Посредством вызова метода IUnknown::QueryInterface (const GUID far& iid, void** pif) интерфейс позволяет получить следующие дополнительные интерфейсы:

    •IToleranceParam - интерфейс параметров допуска формы,

    •IBranchs3D - интерфейс для работы с ответвлениями в 3D,

    •ITable - интерфейс таблицы.

    3. После задания параметров обозначения требуется вызвать метод IModelObject::Update.
    """

    @property
    def BasePlane(self) -> int:  # itolerance3d_baseplane.html
        """
        ## BasePlane - Базовая плоскость

        Тип данных: из перечисления ksObj3dTypeEnum

        Значения свойства:

        +----------------------------------+
        | o3d_unknown  | 0 | Неизвестный   |
        +----------------------------------+
        | o3d_planeXOY | 1 | Плоскость XOY |
        +----------------------------------+
        | o3d_planeXOZ | 2 | Плоскость XOZ |
        +----------------------------------+
        | o3d_planeYOZ | 3 | Плоскость YOZ |
        +----------------------------------+

        Свойство позволяет устанавливать и получать тип плоскости из перечисления ksObj3dTypeEnum, в которой располагается объект.

        ### Синтаксис Automation

        ```
        BasePlane = Object.BasePlane       # Получить свойство (* )
        Object.BasePlane = BasePlane       # Установить свойство (* )
        BasePlane = Object.GetBasePlane()  # Получить свойство (**)
        Object.SetBasePlane( BasePlane )   # Установить свойство (**)
        ```
        """
        ...
    @BasePlane.setter
    def BasePlane(self, value: int): ...

    @property
    def PositionObject(self) -> IModelObject:  # itolerance3d_positionobject.html
        """
        ## PositionObject - Положение плоскости обозначения

        Тип данных: указатель на интерфейс IModelObject

        Свойство позволяет устанавливать и получать объект, на котором будет располагаться плоскость объекта.

        ### Синтаксис Automation

        ```
        PositionObject = Object.PositionObject       # Получить свойство (* )
        Object.PositionObject = PositionObject       # Установить свойство (* )
        PositionObject = Object.GetPositionObject()  # Получить свойство (**)
        Object.SetPositionObject( PositionObject )   # Установить свойство (**)
        ```
        """
        ...
    @PositionObject.setter
    def PositionObject(self, value: IModelObject): ...

    def ArrowType(self, Index) -> bool:  # itolerance3d_arrowtype.html
        """
        ## ArrowType - Тип стрелки ответвления

        Тип данных: BOOL

        Значения свойства:

        +------------------------+
        | TRUE  | - стрелка,     |
        +------------------------+
        | FALSE | - треугольник. |
        +------------------------+

        Свойство позволяет устанавливать и получать тип стрелки ответвления.

        ### Синтаксис Automation

        ```
        ArrowType = Object.ArrowType( Index )     # Получить свойство (* )
        Object.ArrowType( Index ) = ArrowType     # Установить свойство (* )
        ArrowType = Object.GetArrowType( Index )  # Получить свойство (**)
        Object.SetArrowType( Index, ArrowType )   # Установить свойство (**)
        ```

        ### Входные параметры

        +----------------------------------------+
        | Index | - индекс ответвления ( long ). |
        +----------------------------------------+
        """
        ...

    def BranchPos(self, Index) -> int:  # itolerance3d_branchpos.html
        """
        ## BranchPos - Положение ножки

        Тип данных: из перечисления ksTablePointEnum

        ### Синтаксис Automation

        ```
        BranchPos = Object.BranchPos( Index )     # Получить свойство (* )
        Object.BranchPos( Index ) = BranchPos     # Установить свойство (* )
        BranchPos = Object.GetBranchPos( Index )  # Получить свойство (**)
        Object.SetBranchPos( Index, BranchPos )   # Установить свойство (**)
        ```

        ### Входные параметры

        +------------------------------------+
        | long Index | - индекс ответвления. |
        +------------------------------------+
        """
        ...

    def FormatTolerance(self) -> bool:  # itolerance3d_formattolerance.html
        """
        ## FormatTolerance - Форматировать таблицу допуска формы

        Функция выполняет обновление таблицы допуска формы.

        Пустые столбцы и строки в начале и в конце таблицы будут удалены.

        Границы пустых граничных ячеек в непустых столбцах будут удалены.

        Версия: Компас v20

        ### Синтаксис Automation

        ```
        BOOL FormatTolerance();
        ```

        ### Возвращаемое значение

        +-----------------------------------------+
        | TRUE  | - в случае удачного завершения, |
        +-----------------------------------------+
        | FALSE | - в случае неудачи.             |
        +-----------------------------------------+
        """
        ...

    def GetPosition(self) -> bool:  # itolerance3d_getposition.html
        """
        ## GetPosition - Получить точку, задающую положение объекта

        ### Синтаксис Automation

        ```
        BOOL GetPosition ( double * x,
        double * y,
        double * z );
        ```

        ### Выходные параметры

        +-----------------------------------------------------------+
        | x, y, z | - координаты точки, задающей положение объекта. |
        +-----------------------------------------------------------+

        ### Возвращаемое значение

        +-----------------------------------------+
        | TRUE  | - в случае удачного завершения, |
        +-----------------------------------------+
        | FALSE | - в случае неудачи.             |
        +-----------------------------------------+
        """
        ...

    def SetArrowType(self, Index, arg1):
        ...

    def SetBranchPos(self, Index, arg1):
        ...

    def SetPosition(self, X, Y, Z) -> bool:  # itolerance3d_setposition.html
        """
        ## SetPosition - Установить точку, задающую положение объекта

        ### Синтаксис Automation

        ```
        BOOL SetPosition ( double x,
        double y,
        double z );
        ```

        ### Входные параметры

        +-----------------------------------------------------------+
        | x, y, z | - координаты точки, задающей положение объекта. |
        +-----------------------------------------------------------+

        ### Возвращаемое значение

        +-----------------------------------------+
        | TRUE  | - в случае удачного завершения, |
        +-----------------------------------------+
        | FALSE | - в случае неудачи.             |
        +-----------------------------------------+
        """
        ...

    def SetToleranceArrowType(self, Index, arg1):
        ...

    def ToleranceArrowType(self, Index) -> int:  # itolerance3d_tolerancearrowtype.html
        """
        ## ToleranceArrowType - Тип стрелки ответвления

        Тип данных: из перечисления ksToleranceArrowType

        ### Синтаксис Automation

        ```
        ToleranceArrowType = Object.ToleranceArrowType( Index )     # Получить свойство (* )
        Object.ToleranceArrowType( Index ) = ToleranceArrowType     # Установить свойство (* )
        ToleranceArrowType = Object.GetToleranceArrowType( Index )  # Получить свойство (**)
        Object.SetToleranceArrowType( Index, ToleranceArrowType )   # Установить свойство (**)
        ```

        ### Входные параметры

        +-------------------------------+
        | Index | - индекс ответвления. |
        +-------------------------------+
        """
        ...




class IBranchs3D(IDispatch):  # ibranchs3d.html
    """
    ## Интерфейс IBranchs3D

    Интерфейс для работы с ответвлениями.

    Интерфейс можно получить посредством вызова метода IUnknown::QueryInterface у интерфейсов IBaseLeader3D, ITolerance3D, IMarkLeader, IPositionLeader, IChangeLeader, IBrandLeader.
    """

    @property
    def BranchCount(self) -> int:  # ibranchs3d_branchcount.html
        """
        ## BranchCount - Число ответвлений

        Тип данных: long

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        BranchCount = Object.BranchCount       # Получить свойство (* )
        BranchCount = Object.GetBranchCount()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def BranchEndPoints(self) -> list[float]:  # ibranchs3d_branchendpoints.html
        """
        ## BranchEndPoints - Массив SAFEARRAY координат точек целеуказания

        Тип данных: VARIANT типа VT_ARRAY | VT_R8

        1. Свойство доступно только для чтения.

        2. Координаты точек в массиве лежат в следующей последовательности: x0, y0, z0, x1, y1, z1,...xi, yi, zi.

        ### Синтаксис Automation

        ```
        BranchEndPoints = Object.BranchEndPoints       # Получить свойство (* )
        BranchEndPoints = Object.GetBranchEndPoints()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def BranchObjects(self) -> list[IDispatch]:  # ibranchs3d_branchobjects.html
        """
        ## BranchObjects - Массив SAFEARRAY объектов, на которые указывают «ножки»

        Тип данных: VARIANT типа VT_ARRAY | VT_DISPATCH

        1. Если возвращается один объект, то тип VARIANT-а будет VT_DISPATCH, если возвращается несколько объектов, то тип VARIANT-а будет VT_ARRAY | VT_DISPATCH.

        2. Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        BranchObjects = Object.BranchObjects       # Получить свойство (* )
        BranchObjects = Object.GetBranchObjects()  # Получить свойство (**)
        ```
        """
        ...

    def AddBranch(self, Points, Object) -> bool:  # ibranchs3d_addbranch.html
        """
        ## AddBranch - Добавить ответвление

        1. Координаты точек в массиве лежат в следующей последовательности: x0, y0, z0, x1, y1, z1 ...xi, yi, zi.

        2. При задании точек координаты будут спроецированы на плоскость объекта.

        ### Синтаксис Automation

        ```
        BOOL AddBranch( VARIANT Points,
        LPDISPATCH Object );
        ```

        ### Входные параметры

        +--------------------------------------------------------------------+
        | Object | - объект, на который указывает ответвление,               |
        +--------------------------------------------------------------------+
        | Points | - массив SafeArray типа VT_ARRAY | VT_R8 координат точек. |
        +--------------------------------------------------------------------+

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def AddBranchByPoint(self, X, Y, Z, Object) -> bool:  # ibranchs3d_addbranchbypoint.html
        """
        ## AddBranchByPoint - Добавить прямолинейное ответвление

        ### Синтаксис Automation

        ```
        BOOL AddBranchByPoint( double X,
        double Y,
        double Z,
        IModelObject * Object);
        ```

        ### Входные параметры

        +--------------------------------------------+
        | Object  | - объект целеуказания,           |
        +--------------------------------------------+
        | x, y, z | - координаты точки целеуказания. |
        +--------------------------------------------+

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def BranchObject(self, Index) -> IModelObject:  # ibranchs3d_branchobject.html
        """
        ## BranchObject - Объект, на который указывает «ножка»

        Тип данных: указатель на интерфейс IModelObject

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        BranchObject = Object.BranchObject( Index )     # Получить свойство (* )
        BranchObject = Object.GetBranchObject( Index )  # Получить свойство (**)
        ```

        ### Входные параметры

        +-----------------------------------+
        | long Index | - номер ответвления. |
        +-----------------------------------+
        """
        ...

    def BranchPoints(self, Index) -> list[float]:  # ibranchs3d_branchpoints.html
        """
        ## BranchPoints - Массив SAFEARRAY координат точек ответвления

        Тип данных: VARIANT типа VT_ARRAY | VT_R8

        1. Координаты точек в массиве лежат в следующей последовательности: x0, y0, z0, x1, y1, z1,...xi, yi, zi.

        2. При задании точек координаты будут спроецированы на плоскость объекта.

        ### Синтаксис Automation

        ```
        BranchPoints = Object.BranchPoints( Index )     # Получить свойство (* )
        Object.BranchPoints( Index ) = BranchPoints     # Установить свойство (* )
        BranchPoints = Object.GetBranchPoints( Index )  # Получить свойство (**)
        Object.SetBranchPoints( Index, BranchPoints )   # Установить свойство (**)
        ```

        ### Входные параметры

        +-----------------------------------+
        | long Index | - номер ответвления. |
        +-----------------------------------+

        Свойство позволяет получить массив координат точек ответвления, заданного по номеру.
        """
        ...

    def BranchPointsCount(self, Index) -> int:  # ibranchs3d_branchpointscount.html
        """
        ## BranchPointsCount - Число точек в ответвлении

        Тип данных: long

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        BranchPointsCount = Object.BranchPointsCount       # Получить свойство (* )
        BranchPointsCount = Object.GetBranchPointsCount()  # Получить свойство (**)
        ```

        ### Входные параметры

        +-----------------------------------+
        | long Index | - номер ответвления. |
        +-----------------------------------+
        """
        ...

    def DeleteBranch(self, Index) -> bool:  # ibranchs3d_deletebranch.html
        """
        ## DeleteBranch - Удалить ответвление

        ### Синтаксис Automation

        ```
        BOOL DeleteBranch( long Index );
        ```

        ### Входные параметры

        +-------------------------------+
        | Index | - индекс ответвления. |
        +-------------------------------+

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def GetBranchBeginPoint(self) -> bool:  # ibranchs3d_getbranchbeginpoint.html
        """
        ## GetBranchBeginPoint - Получить координаты начала полки или точки привязки

        ### Синтаксис Automation

        ```
        BOOL GetBranchBeginPoint( double * X,
        double * Y,
        double * Z );
        ```

        ### Выходные параметры

        +-------------------------------+
        | x, y, z | - координаты точки. |
        +-------------------------------+

        ### Возвращаемое значение

        +-----------------------------+
        | TRUE  | - в случае успеха,  |
        +-----------------------------+
        | FALSE | - в случае неудачи. |
        +-----------------------------+

        Метод позволяет получить координаты начала полки.
        """
        ...

    def GetBranchEndPoint(self, Index) -> bool:  # ibranchs3d_getbranchendpoint.html
        """
        ## GetBranchEndPoint - Получить координаты конечной точки ответвления

        ### Синтаксис Automation

        ```
        BOOL GetBranchBeginPoint( long Index,
        double * X,
        double * Y,
        double * Z );
        ```

        ### Входные параметры

        +-------------------------------+
        | Index | - индекс ответвления. |
        +-------------------------------+

        ### Выходные параметры

        +-------------------------------+
        | x, y, z | - координаты точки. |
        +-------------------------------+

        ### Возвращаемое значение

        +-----------------------------+
        | TRUE  | - в случае успеха,  |
        +-----------------------------+
        | FALSE | - в случае неудачи. |
        +-----------------------------+

        Метод позволяет получить координаты конечной точки ответвления.
        """
        ...

    def SetBranchBeginPoint(self, X, Y, Z) -> bool:  # ibranchs3d_setbranchbeginpoint.html
        """
        ## SetBranchBeginPoint - Установить координаты начала полки или точки привязки

        ### Синтаксис Automation

        ```
        BOOL SetBranchBeginPoint( double X,
        double Y,
        double Z );
        ```

        ### Выходные параметры

        +-------------------------------+
        | x, y, z | - координаты точки. |
        +-------------------------------+

        ### Возвращаемое значение

        +-----------------------------+
        | TRUE  | - в случае успеха,  |
        +-----------------------------+
        | FALSE | - в случае неудачи. |
        +-----------------------------+

        Метод позволяет установить координаты начала полки.
        """
        ...

    def SetBranchEndPoint(self, Index, X, Y, Z) -> bool:  # ibranchs3d_setbranchendpoint.html
        """
        ## SetBranchEndPoint - Установить координаты конечной точки ответвления

        ### Синтаксис Automation

        ```
        BOOL SetBranchEndPoint( long Index,
        double X,
        double Y,
        double Z );
        ```

        ### Входные параметры

        +---------------------------------+
        | Index   | - индекс ответвления, |
        +---------------------------------+
        | x, y, z | - координаты точки.   |
        +---------------------------------+

        ### Возвращаемое значение

        +-----------------------------+
        | TRUE  | - в случае успеха.  |
        +-----------------------------+
        | FALSE | - в случае неудачи. |
        +-----------------------------+

        Метод позволяет установить координаты конечной точки ответвления.
        """
        ...

    def SetBranchPoints(self, Index, arg1):
        ...




class IUserDesignationCompObj(IModelObject):  # iuserdesignationcompobj.html
    """
    ## Интерфейс IUserDesignationCompObj

    Составной объект для пользовательских объектов обозначений 3D.
    """

    def SetObjects(self, Objects) -> bool:  # iuserdesignationcompobj_setobjects.html
        """
        ## SetObjects - Задать объекты

        ### Синтаксис Automation

        ```
        BOOL SetObjects( VARIANT Objects );
        ```

        ### Входные параметры

        +-----------------------------------------------------------------+
        | Objects | - массив SafeArray (VT_ARRAY | VT_DISPATCH) объектов. |
        +-----------------------------------------------------------------+

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...




class IToleranceRecalcsManager(IDispatch):  # itolerancerecalcsmanager.html
    """
    ## Интерфейс IToleranceRecalcsManager

    Менеджер пересчета модели по допускам.
    """

    @property
    def CurrentRecalc(self) -> IToleranceRecalc:  # itolerancerecalcsmanager_currentrecalc.html
        """
        ## CurrentRecalc - Интерфейс текущего пересчета

        Тип данных: указатель на интерфейс IToleranceRecalc

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        CurrentRecalc= Object.CurrentRecalc       # Получить свойство (* )
        CurrentRecalc= Object.GetCurrentRecalc()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def RecalcsCount(self) -> int:  # itolerancerecalcsmanager_recalcscount.html
        """
        ## RecalcsCount - Количество пересчетов размеров

        Тип данных: long

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        RecalcsCount= Object.RecalcsCount       # Получить свойство (* )
        RecalcsCount= Object.GetRecalcsCount()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def ToleranceMode(self) -> bool:  # itolerancerecalcsmanager_tolerancemode.html
        """
        ## ToleranceMode - Включить\\отключить режим пересчета по допуску

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        ToleranceMode = Object.ToleranceMode;       # Получить свойство (* )
        Object.ToleranceMode = ToleranceMode;       # Установить свойство (* )
        ToleranceMode = Object.GetToleranceMode();  # Получить свойство (**)
        Object.SetToleranceMode( ToleranceMode );   # Установить свойство (* )
        ```
        """
        ...
    @ToleranceMode.setter
    def ToleranceMode(self, value: bool): ...

    def AddRecalc(self) -> IToleranceRecalc:  # itolerancerecalcsmanager_addrecalc.html
        """
        ## AddRecalc - Создать пересчет размеров

        Синтаксис СОМ:

        HRESULT AddRecalc( IToleranceRecalc ** Result );

        ### Синтаксис Automation

        ```
        LPDISPATCH AddRecalc();
        ```

        ### Возвращаемое значение

        - указатель на интерфейс пересчета размеров IToleranceRecalc.
        """
        ...

    def AddRecalcCopy(self, Source) -> IToleranceRecalc|IToleranceRecalc:  # itolerancerecalcsmanager_addrecalccopy.html
        """
        ## AddRecalcCopy - Создать пересчет размеров по образцу

        Синтаксис СОМ:

        HRESULT AddRecalcCopy( IToleranceRecalc * Source, IToleranceRecalc ** Result );

        ### Синтаксис Automation

        ```
        LPDISPATCH AddRecalcCopy( IToleranceRecalc * Source );
        ```

        ### Входные параметры

        +------------------------------------------------------------------------+
        | Source | - указатель на копируемый пересчет размеров IToleranceRecalc. |
        +------------------------------------------------------------------------+

        ### Возвращаемое значение

        - указатель на интерфейс пересчета размеров IToleranceRecalc.
        """
        ...

    def GetRecalc(self, Index) -> typing.Any:  # itolerancerecalcsmanager_getrecalc.html
        """
        ## GetRecalc - Получить пересчет размеров по имени или индексу

        Синтаксис СОМ:

        HRESULT GetRecalc( VARIANT Index, IToleranceRecalc ** Result );

        ### Синтаксис Automation

        ```
        LPDISPATCH GetRecalc( VARIANT Index );
        ```

        ### Входные параметры

        +-------------------------------------+
        | Index | - имя или индекс пересчета. |
        +-------------------------------------+

        ### Возвращаемое значение

        - указатель на интерфейс пересчета размеров IToleranceRecalc.
        """
        ...

    def SaveRecalcModel(self, FileName) -> bool:  # itolerancerecalcsmanager_saverecalcmodel.html
        """
        ## SaveRecalcModel - Сохранить пересчитанную копию модели

        Синтаксис СОМ:

        HRESULT SaveRecalcModel( BSTR FileName, BOOL * Result );

        ### Синтаксис Automation

        ```
        BOOL SaveRecalcModel( BSTR FileName );
        ```

        ### Входные параметры

        +-----------------------------------------------------------+
        | FileName | - имя файла, в который будет сохранена модель. |
        +-----------------------------------------------------------+

        ### Возвращаемое значение

        +-----------------------------------------+
        | TRUE  | - в случае удачного завершения, |
        +-----------------------------------------+
        | FALSE | - в случае неудачи.             |
        +-----------------------------------------+
        """
        ...




class IToleranceRecalc(IKompasAPIObject):  # itolerancerecalc.html
    """
    ## Интерфейс IToleranceRecalc

    Интерфейс пересчета модели.
    """

    @property
    def Current(self) -> bool:  # itolerancerecalc_current.html
        """
        ## Current - Текущий пересчет

        Тип данных: BOOL

        Установить свойство можно только равным TRUE.

        ### Синтаксис Automation

        ```
        Current = Object.Current;       # Получить свойство (* )
        Object.Current = Current;       # Установить свойство (* )
        Current = Object.GetCurrent();  # Получить свойство (**)
        Object.SetCurrent( Current );   # Установить свойство (* )
        ```
        """
        ...
    @Current.setter
    def Current(self, value: bool): ...

    @property
    def Id(self) -> int:  # itolerancerecalc_id.html
        """
        ## Id - Идентификатор

        Тип данных: long

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        Id = Object.Id       # Получить свойство (* )
        Id = Object.GetId()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def ItemsCount(self) -> int:  # itolerancerecalc_itemscount.html
        """
        ## ItemsCount - Количество объектов в пересчете

        Тип данных: long

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        ItemsCount= Object.ItemsCount       # Получить свойство (* )
        ItemsCount= Object.GetItemsCount()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def Name(self) -> str:  # itolerancerecalc_name.html
        """
        ## Name - Имя

        Тип данных: BSTR

        ### Синтаксис Automation

        ```
        Name = Object.Name;       # Получить свойство (* )
        Object.Name = Name;       # Установить свойство (* )
        Name = Object.GetName();  # Получить свойство (**)
        Object.SetName( Name );   # Установить свойство (* )
        ```
        """
        ...
    @Name.setter
    def Name(self, value: str): ...

    def AddAllVariables(self) -> bool:  # itolerancerecalc_addallvariables.html
        """
        ## AddAllVariables - Добавить все переменные

        ### Синтаксис Automation

        ```
        BOOL AddAllVariables();
        ```

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def AddItems(self, Object) -> bool:  # itolerancerecalc_additems.html
        """
        ## AddItems - Добавить переменные, размер или компонент

        ### Синтаксис Automation

        ```
        BOOL AddItems( IKompasAPIObject * Object );
        ```

        ### Входные параметры

        +-----------------------------------------------------------------------+
        | Object | - указатель на интерфейс переменной, размера или компонента. |
        +-----------------------------------------------------------------------+

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def Coefficient(self, Index) -> float:  # itolerancerecalc_coefficient.html
        """
        ## Coefficient - Коэффициент

        Тип данных: double

        ### Синтаксис Automation

        ```
        Coefficient = Object.Coefficient( Index );     # Получить свойство (* )
        Object.Coefficient( Index ) = Coefficient;     # Установить свойство (* )
        Coefficient = Object.GetCoefficient( Index );  # Получить свойство (**)
        Object.SetCoefficient( Index, Coefficient );   # Установить свойство (* )
        ```

        ### Входные параметры

        +--------------------------------------------+
        | VARIANT | Index- имя или индекс пересчета. |
        +--------------------------------------------+
        """
        ...

    def DeleteItem(self, Index) -> bool:  # itolerancerecalc_deleteitem.html
        """
        ## DeleteItem - Удалить переменную или вариант пересчета компонента по имени, индексу или интерфейсу

        ### Синтаксис Automation

        ```
        BOOL DeleteItem( VARIANT Index );
        ```

        ### Входные параметры

        +-------------------------------------+
        | Index | - имя или индекс пересчета. |
        +-------------------------------------+

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def Item(self, Index) -> IKompasAPIObject:  # itolerancerecalc_item.html
        """
        ## Item - Получить переменную или компонент

        Тип данных: Указатель на интерфейс IKompasAPIObject

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        Item = Object.Item( Index )     # Получить свойство (* )
        Item = Object.GetItem( Index )  # Получить свойство (**)
        ```
        """
        ...

    def RecalcType(self, Index) -> int:  # itolerancerecalc_recalctype.html
        """
        ## RecalcType - Способ пересчета

        Тип данных: из перечисления ksToleranceRecalcsEnum

        ### Синтаксис Automation

        ```
        RecalcType = Object.RecalcType( Index );     # Получить свойство (* )
        Object.RecalcType( Index ) = RecalcType;     # Установить свойство (* )
        RecalcType = Object.GetRecalcType( Index );  # Получить свойство (**)
        Object.SetRecalcType( Index, RecalcType );   # Установить свойство (* )
        ```

        ### Входные параметры

        +---------------------------------------------+
        | VARIANT | Index - имя или индекс пересчета. |
        +---------------------------------------------+
        """
        ...

    def RecalcUserType(self, Index) -> str:  # itolerancerecalc_recalcusertype.html
        """
        ## RecalcUserType - Способ пользовательского пересчета для вставки

        Тип данных: BSTR

        ### Синтаксис Automation

        ```
        RecalcUserType = Object.RecalcUserType( Index );     # Получить свойство (* )
        Object.RecalcUserType( Index ) = RecalcUserType;     # Установить свойство (* )
        RecalcUserType = Object.GetRecalcUserType( Index );  # Получить свойство (**)
        Object.SetRecalcUserType( Index, RecalcUserType );   # Установить свойство (* )
        ```

        ### Входные параметры

        +---------------------------------------------+
        | VARIANT | Index - имя или индекс пересчета. |
        +---------------------------------------------+
        """
        ...

    def SetCoefficient(self, Index, arg1):
        ...

    def SetRecalcType(self, Index, arg1):
        ...

    def SetRecalcUserType(self, Index, arg1):
        ...




class ITechnicalDemand3D(IKompasAPIObject):  # itechnicaldemand3d.html
    """
    ## Интерфейс ITechnicalDemand3D

    Технические требования 3D.

    Интерфейс позволяет задавать и получать параметры технических требований в 3D.

    Интерфейс можно получить у интерфейса документа 3D с помощью свойства IKompasDocument3D::TechnicalDemand3D
    """

    @property
    def Editable(self) -> int:  # itechnicaldemand3d_editable.html
        """
        ## Editable - Признак доступности редактирования

        Тип данных: из перечисления ksEditableStateEnum

        ### Синтаксис Automation

        ```
        Editable = Object.Editable       # Получить свойство (* )
        Object.Editable = Editable       # Установить свойство (* )
        Editable = Object.GetEditable()  # Получить свойство (**)
        Object.SetEditable( Editable )   # Установить свойство (**)
        ```
        """
        ...
    @Editable.setter
    def Editable(self, value: int): ...

    @property
    def Hidden(self) -> bool:  # itechnicaldemand3d_hidden.html
        """
        ## Hidden - Состояние видимости технических требований в окне документа

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        Hidden = Object.Hidden       # Получить свойство (* )
        Object.Hidden = Hidden       # Установить свойство (* )
        Hidden = Object.GetHidden()  # Получить свойство (**)
        Object.SetHidden( Hidden )   # Установить свойство (**)
        ```
        """
        ...
    @Hidden.setter
    def Hidden(self, value: bool): ...

    @property
    def IsCreated(self) -> bool:  # itechnicaldemand3d_iscreated.html
        """
        ## IsCreated - Признак наличия технических требований в документе

        Тип данных: BOOL

        1. Свойство позволяет получать признак наличия технических требований в документе.

        2. Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        IsCreated = Object.IsCreated       # Получить свойство (* )
        IsCreated = Object.GetIsCreated()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def Placement(self) -> int:  # itechnicaldemand3d_placement.html
        """
        ## Placement - Размещение

        Тип данных: из перечисления ksTechnicalDemand3DPlacementEnum

        ### Синтаксис Automation

        ```
        Placement = Object.Placement       # Получить свойство (* )
        Object.Placement = Placement       # Установить свойство (* )
        Placement = Object.GetPlacement()  # Получить свойство (**)
        Object.SetPlacement( Placement )   # Установить свойство (**)
        ```
        """
        ...
    @Placement.setter
    def Placement(self, value: int): ...

    @property
    def Text(self) -> IText:  # itechnicaldemand3d_text.html
        """
        ## Text - Текст

        Тип данных: указатель на интерфейс текста IText

        1. Свойство позволяет получать интерфейс текста технических требований.

        2. Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        Text = Object.Text       # Получить свойство (* )
        Text = Object.GetText()  # Получить свойство (**)
        ```
        """
        ...

    def Delete(self) -> bool:  # itechnicaldemand3d_delete.html
        """
        ## Delete - Удалить

        ### Синтаксис Automation

        ```
        BOOL Delete();
        ```

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+

        Версия Компас v21
        """
        ...

    def GetGabarit(self) -> bool:  # itechnicaldemand3d_getgabarit.html
        """
        ## GetGabarit - Габарит

        ### Синтаксис Automation

        ```
        BOOL GetGabarit( long * Left, long * Top, long * Right, long * Bottom );
        ```

        ### Выходные параметры

        +--------------------------------------------------------------------------------------+
        | Left, Top, Right, Bottom | - габариты технических требований в экранных координатах. |
        +--------------------------------------------------------------------------------------+

        Версия Компас v21

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def Update(self) -> bool:  # itechnicaldemand3d_update.html
        """
        ## Update - Обновление данных

        Метод позволяет применить заданные параметры технических требований.

        ### Синтаксис Automation

        ```
        BOOL Update();
        ```

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...




class ISpecRough3D(IKompasAPIObject):  # ispecrough3d.html
    """
    ## Интерфейс ISpecRough3D

    Интерфейс неуказанной шероховатости 3D.
    """

    @property
    def AddSign(self) -> bool:  # ispecrough3d_addsign.html
        """
        ## AddSign - Добавить знак в скобках

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        AddSign = Object.AddSign;       # Получить свойство (* )
        Object.AddSign = AddSign;       # Установить свойство (* )
        AddSign = Object.GetAddSign();  # Получить свойство (**)
        Object.SetAddSign( AddSign );   # Установить свойство (* )
        ```
        """
        ...
    @AddSign.setter
    def AddSign(self, value: bool): ...

    @property
    def Hidden(self):
        ...
    @Hidden.setter
    def Hidden(self, value): ...

    @property
    def IsCreated(self) -> bool:  # ispecrough3d_iscreated.html
        """
        ## IsCreated - Признак отображения в документе

        Тип данных: BOOL

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        IsCreated = Object.IsCreated       # Получить свойство (* )
        IsCreated = Object.GetIsCreated()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def ModelObjectType(self):
        ...

    @property
    def Name(self):
        ...
    @Name.setter
    def Name(self, value): ...

    @property
    def Owner(self):
        ...

    @property
    def Part(self):
        ...

    @property
    def Placement(self) -> int:  # ispecrough3d_placement.html
        """
        ## Placement - Размещение

        Тип данных: из перечисления ksSpecRoughPlacementEnum

        ### Синтаксис Automation

        ```
        Placement = Object. Placement;       # Получить свойство (* )
        Object. Placement = Placement;       # Установить свойство (* )
        Placement = Object.Get Placement();  # Получить свойство (**)
        Object.Set Placement( Placement );   # Установить свойство (* )
        ```
        """
        ...
    @Placement.setter
    def Placement(self, value: int): ...

    @property
    def SignType(self) -> int:  # ispecrough3d_signtype.html
        """
        ## SignType - Тип значка

        Тип данных: из перечисления ksRoughSignEnum

        ### Синтаксис Automation

        ```
        SignType = Object. SignType;       # Получить свойство (* )
        Object. SignType = SignType;       # Установить свойство (* )
        SignType = Object.Get SignType();  # Получить свойство (**)
        Object.Set SignType( SignType );   # Установить свойство (* )
        ```
        """
        ...
    @SignType.setter
    def SignType(self, value: int): ...

    @property
    def Text(self) -> str:  # ispecrough3d_text.html
        """
        ## Text - Текст

        Тип данных: BSTR

        ### Синтаксис Automation

        ```
        Text = Object. Text;       # Получить свойство (* )
        Object. Text = Text;       # Установить свойство (* )
        Text = Object.Get Text();  # Получить свойство (**)
        Object.Set Text( Text );   # Установить свойство (* )
        ```
        """
        ...
    @Text.setter
    def Text(self, value: str): ...

    @property
    def Valid(self):
        ...

    def Delete(self) -> bool:  # ispecrough3d_delete.html
        """
        ## Delete - Удалить объект

        ### Синтаксис Automation

        ```
        BOOL Delete();
        ```

        ### Возвращаемое значение

        +-----------------------------------------+
        | TRUE  | - в случае удачного завершения, |
        +-----------------------------------------+
        | FALSE | - в случае неудачи.             |
        +-----------------------------------------+
        """
        ...

    def Update(self) -> bool:  # ispecrough3d_update.html
        """
        ## Update - Обновление данных

        BOOL Update();

        ### Возвращаемое значение

        +-----------------------------------------+
        | TRUE  | - в случае удачного завершения, |
        +-----------------------------------------+
        | FALSE | - в случае неудачи.             |
        +-----------------------------------------+
        """
        ...




class IModelContainer(IDispatch):  # imodelcontainer.html
    """
    ## Интерфейс IModelContainer

    Интерфейс контейнера трехмерных объектов.

    Интерфейс позволяет работать с коллекциями 3D-объектов, входящих в состав 3D-объекта.

    Интерфейс контейнера является дополнительным к интерфейсу IPart7. Данный интерфейс можно получить от IPart7 посредством вызова метода IUnknown::QueryInterface (const GUID far& iid, void** pif);
    """

    @property
    def BilletsObsoletes(self) -> IBilletsObsoletes:  # imodelcontainer_billetsobsoletes.html
        """
        ## BilletsObsoletes - Коллекция деталей заготовок и зеркальных деталей

        Тип данных: указатель на интерфейс коллекции деталей заготовок и зеркальных деталей IBilletsObsoletes

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        BilletsObsoletes = Object.BilletsObsoletes;       # Получить свойство (* )
        BilletsObsoletes = Object.GetBilletsObsoletes();  # Получить свойство (**)
        ```
        """
        ...

    @property
    def BodyRepositions(self) -> IBodyRepositions:  # imodelcontainer_bodyrepositions.html
        """
        ## BodyRepositions - Коллекция операций перепозиционирования тела, поверхности

        Тип данных: Указатель на интерфейс IBodyRepositions.

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        BodyRepositions = Object.BodyRepositions;       # Получить свойство (* )
        BodyRepositions = Object.GetBodyRepositions();  # Получить свойство (**)
        ```
        """
        ...

    @property
    def Booleans(self) -> IBooleans:  # imodelcontainer_booleans.html
        """
        ## Booleans - Коллекция булевых операций

        Тип данных: Указатель на интерфейс IBooleans.

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        Booleans = Object.Booleans       # Получить свойство (* )
        Booleans = Object.GetBooleans()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def Chamfers(self) -> IChamfers:  # imodelcontainer_chamfers.html
        """
        ## Chamfers - Коллекция операций Фаска

        Тип данных: Указатель на интерфейс IChamfers.

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        Chamfers = Object.Chamfers       # Получить свойство (* )
        Chamfers = Object.GetChamfers()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def CollectionsGeometry(self) -> ICollectionsGeometry:  # imodelcontainer_collectionsgeometry.html
        """
        ## CollectionsGeometry - Коллекции геометрии

        Тип данных: указатель на интерфейс ICollectionsGeometry.

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        CollectionsGeometry = Object.CollectionsGeometry;       # Получить свойство (* )
        CollectionsGeometry = Object.GetCollectionsGeometry();  # Получить свойство (**)
        ```
        """
        ...

    @property
    def CopiesGeometry(self) -> ICopiesGeometry:  # imodelcontainer_copiesgeometry.html
        """
        ## CopiesGeometry - Коллекция операций копирования геометрии

        Тип данных: указатель на интерфейс ICopiesGeometry.

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        CopiesGeometry = Object.CopiesGeometry;       # Получить свойство (* )
        CopiesGeometry = Object.GetCopiesGeometry();  # Получить свойство (**)
        ```
        """
        ...

    @property
    def Cuts(self) -> ICuts:  # imodelcontainer_cuts.html
        """
        ## Cuts - Коллекция операций Cечение

        Тип данных: Указатель на интерфейс ICuts.

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        Cuts = Object.Cuts       # Получить свойство (* )
        Cuts = Object.GetCuts()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def DraftsFromEdges(self) -> IDraftsFromEdges:  # imodelcontainer_draftsfromedges.html
        """
        ## DraftsFromEdges - Коллекция операций "Уклон от базовой линии"

        Тип данных: Указатель на интерфейс IDraftsFromEdges

        Свойство доступно только для чтения.

        Версия КОМПАС v22

        ### Синтаксис Automation

        ```
        DraftsFromEdges = Object.DraftsFromEdges       # Получить свойство (* )
        DraftsFromEdges = Object.GetDraftsFromEdges()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def Evolutions(self) -> IEvolutions:  # imodelcontainer_evolutions.html
        """
        ## Evolutions - Коллекция кинематических операций

        Тип данных: Указатель на интерфейс IEvolutions.

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        Evolutions = Object.Evolutions       # Получить свойство (* )
        Evolutions = Object.GetEvolutions()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def Extrusions(self) -> IExtrusions:  # imodelcontainer_extrusions.html
        """
        ## Extrusions - Указатель на интерфейс коллекции операций выдавливания

        Тип данных: указатель на интерфейс коллекции операций выдавливания IExtrusions.

        1. Свойство доступно только для чтения.

        2. Свойство позволяет получать коллекции операций выдавливания.

        ### Синтаксис Automation

        ```
        Extrusions = iObject.Extrusions;       # Получить свойство (* )
        Extrusions = iObject.GetExtrusions();  # Получить свойство (**)
        ```
        """
        ...

    @property
    def FeaturePatterns(self) -> IFeaturePatterns:  # imodelcontainer_featurepatterns.html
        """
        ## FeaturePatterns - Указатель на интерфейс коллекции операций копирования

        Тип данных: указатель на интерфейс IFeaturePatterns.

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        FeaturePatterns = Object.FeaturePatterns       # Получить свойство (* )
        FeaturePatterns = Object.GetFeaturePatterns()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def Fillets(self) -> IFillets:  # imodelcontainer_fillets.html
        """
        ## Fillets - Коллекция операций Скругление

        Тип данных: Указатель на интерфейс IFillets.

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        Fillets = Object.Fillets       # Получить свойство (* )
        Fillets = Object.GetFillets()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def FullFillets(self) -> IFullFillets:  # imodelcontainer_fullfillets.html
        """
        ## FullFillets - Коллекция операций полного скругления

        Тип данных: Указатель на интерфейс IFullFillets

        Свойство доступно только для чтения.

        Версия Компас v18.1

        ### Синтаксис Automation

        ```
        FullFillets = Object.FullFillets       # Получить свойство (* )
        FullFillets = Object.GetFullFillets()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def Holes3D(self) -> IHoles3D:  # imodelcontainer_holes3d.html
        """
        ## Holes3D - Коллекция отверстий 3D

        Тип данных: Указатель на интерфейс IHoles3D.

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        Holes3D = Object.Holes3D       # Получить свойство (* )
        Holes3D = Object.GetHoles3D()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def Inclines(self) -> IInclines:  # imodelcontainer_inclines.html
        """
        ## Inclines - Коллекция операций Уклон

        Тип данных: Указатель на интерфейс IInclines.

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        Inclines = Object.Inclines       # Получить свойство (* )
        Inclines = Object.GetInclines()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def Lofts(self) -> ILofts:  # imodelcontainer_lofts.html
        """
        ## Lofts - Коллекция операций По сечениям

        Тип данных: Указатель на интерфейс ILofts.

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        Lofts = Object.Lofts       # Получить свойство (* )
        Lofts = Object.GetLofts()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def MacroObjects3D(self) -> IMacroObjects3D:  # imodelcontainer_macroobjects3d.html
        """
        ## MacroObjects3D - Коллекция макроэлементов 3D

        Тип данных: Указатель на интерфейс IMacroObjects3D.

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        MacroObjects3D = Object.MacroObjects3D       # Получить свойство (* )
        MacroObjects3D = Object.GetMacroObjects3D()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def MoldCavities(self) -> IMoldCavities:  # imodelcontainer_moldcavities.html
        """
        ## MoldCavities - Коллекция операций вычитания компонентов

        Тип данных: Указатель на интерфейс IMoldCavities.

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        MoldCavities = Object.MoldCavities       # Получить свойство (* )
        MoldCavities = Object.GetMoldCavities()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def Points3D(self) -> IPoints3D:  # imodelcontainer_points3d.html
        """
        ## Points3D - Указатель на интерфейс коллекции элементов Пространственная точка

        Тип данных: указатель на интерфейс коллекции элементов Пространственная точка IPoints3D.

        1. Свойство доступно только для чтения.

        2. Свойство позволяет получать коллекции элементов Пространственная точка.

        ### Синтаксис Automation

        ```
        Points3D = iObject.Points3D;       # Получить свойство (* )
        Points3D = iObject.GetPoints3D();  # Получить свойство (**)
        ```
        """
        ...

    @property
    def Ribs(self) -> IRibs:  # imodelcontainer_ribs.html
        """
        ## Ribs - Коллекция операций Ребро жесткости

        Тип данных: Указатель на интерфейс IRibs.

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        Ribs = Object.Ribs       # Получить свойство (* )
        Ribs = Object.GetRibs()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def Rotateds(self) -> IRotateds:  # imodelcontainer_rotateds.html
        """
        ## Rotateds - Указатель на интерфейс коллекции операций вращения

        Тип данных: указатель на интерфейс коллекции операций вращения IRotateds.

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        Rotateds = Object.Rotateds       # Получить свойство (* )
        Rotateds = Object.GetRotateds()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def Scalings3D(self) -> IScalings3D:  # imodelcontainer_scalings3d.html
        """
        ## Scalings3D - Коллекция операций масштабирования

        Тип данных: указатель на интерфейс IScalings3D.

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        Scalings3D = Object.Scalings3D       # Получить свойство (* )
        Scalings3D = Object.GetScalings3D()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def Shells(self) -> IShells:  # imodelcontainer_shells.html
        """
        ## Shells - Коллекция операций Оболочка

        Тип данных: Указатель на интерфейс IShells.

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        Shells = Object.Shells       # Получить свойство (* )
        Shells = Object.GetShells()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def Sketchs(self) -> ISketchs:  # imodelcontainer_sketchs.html
        """
        ## Sketchs - Указатель на интерфейс коллекции объектов "Эскиз"

        Тип данных: указатель на интерфейс коллекции элементов «Эскиз» ISketchs.

        1. Свойство доступно только для чтения.

        2. Свойство позволяет получать коллекции элементов "Эскиз".

        ### Синтаксис Automation

        ```
        Sketchs = iObject.Sketchs;       # Получить свойство (* )
        Sketchs = iObject.GetSketchs();  # Получить свойство (**)
        ```
        """
        ...

    @property
    def SplitSolids(self) -> ISplitSolids:  # imodelcontainer_splitsolids.html
        """
        ## SplitSolids - Коллекция операций Разрезать

        Тип данных: Указатель на интерфейс ISplitSolids.

        Свойство доступно только для чтения.

        Версия: Компас v20

        ### Синтаксис Automation

        ```
        SplitSolids = Object.SplitSolids;       # Получить свойство (* )
        SplitSolids = Object.GetSplitSolids();  # Получить свойство (**)
        ```
        """
        ...

    @property
    def SurfaceThickenings(self) -> ISurfaceThickenings:  # imodelcontainer_surfacethickenings.html
        """
        ## SurfaceThickenings - Указатель на интерфейс коллекции операций придания толщины

        Тип данных: указатель на интерфейс коллекции операций придания толщины ISurfaceThickenings.

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        SurfaceThickenings = Object.SurfaceThickenings       # Получить свойство (* )
        SurfaceThickenings = Object.GetSurfaceThickenings()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def UnionsComponents(self) -> IUnionsComponents:  # imodelcontainer_unionscomponents.html
        """
        ## UnionsComponents - Коллекция операций объединения компонентов

        Тип данных: Указатель на интерфейс IUnionsComponents.

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        UnionsComponents = Object.UnionsComponents       # Получить свойство (* )
        UnionsComponents = Object.GetUnionsComponents()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def UserObjects(self) -> IUserObjects3D:  # imodelcontainer_userobjects.html
        """
        ## UserObjects - Коллекции пользовательских объектов 3D

        Тип данных: Указатель на интерфейс IUserObjects3D.

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        UserObjects = Object.UserObjects       # Получить свойство(* )
        UserObjects = Object.GetUserObjects()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def WireFrames3D(self) -> IWireFrames3D:  # imodelcontainer_wireframes3d.html
        """
        ## WireFrames3D - Коллекция трехмерных каркасов

        Тип данных: Указатель на интерфейс IWireFrames3D.

        +------------------------------------------------------------------+
        | WireFrames3D = Object.WireFrames3D      | Получить свойство (* ) |
        +------------------------------------------------------------------+
        | WireFrames3D = Object.GetWireFrames3D() | Получить свойство (**) |
        +------------------------------------------------------------------+

        Свойство доступно только для чтения.

        Версия: Компас v20
        """
        ...

    def AddObject(self, ObjectType) -> IModelObject:  # imodelcontainer_addobject.html
        """
        ## AddObject - Создает новый элемент 3D модели

        ### Синтаксис Automation

        ```
        LPDISPATCH AddObject( ksObj3dTypeEnum ObjectType );
        ```

        ### Входные параметры

        +-------------------------------------------------------------+
        | ObjectType | - тип объекта из перечисления ksObj3dTypeEnum. |
        +-------------------------------------------------------------+

        ### Возвращаемое значение

        - указатель на интерфейс IModelObject.
        """
        ...

    def Objects(self, ObjType) -> typing.Any:  # imodelcontainer_objects.html
        """
        ## Objects - Трехмерные объекты, входящие в состав данного объекта

        Тип данных: VARIANT

        1. Свойство доступно только для чтения.

        2. Коллекция объектов возвращается в виде массива SAFEARRAY объектов LPDISPATCH (VT_ARRAY | VT_DISPATCH). Если возвращается один объект, то тип VARIANT-а будет VT_DISPATCH, если возвращается несколько объектов, то тип VARIANT-а будет VT_ARRAY | VT_DISPATCH.

        ### Синтаксис Automation

        ```
        Objects = iObject.Objects(ObjType)     # Получить свойство (* )
        Objects = iObject.GetObjects(ObjType)  # Получить свойство (**)
        ```

        ### Входные параметры

        +------------------------------------------------------------------------------+
        | ObjType | - тип трехмерных объектов из перечисления KompasAPIObjectTypeEnum. |
        +------------------------------------------------------------------------------+
        """
        ...




class IExtrusions(IModelObjects):  # iextrusions.html
    """
    ## Интерфейс IExtrusions

    Интерфейс коллекции операций выдавливания.

    Получить интерфейс операций выдавливания можно, используя свойство контейнера трехмерных объектов IModelContainer::Extrusions.
    """

    def Add(self, ExtrusionType) -> IExtrusion:  # iextrusions_add.html
        """
        ## Add - Создать операцию выдавливание

        1. Метод позволяет создать новый интерфейс операции выдавливания.

        2. Допустимыми значениями ExtrusionType являются o3d_bossExtrusion, o3d_cutExtrusion.

        3. После получения нового интерфейса нужно задать параметры операции выдавливание и вызвать метод IModelObject::Update.

        ### Синтаксис Automation

        ```
        LPDISPATH Add(ksObj3dTypeEnum ExtrusionType);
        ```

        ### Возвращаемое значение

        +-------------------------------------------------------------------------------------------+
        | Указатель на интерфейс операции выдавливание IExtrusion | - в случае удачного завершения. |
        +-------------------------------------------------------------------------------------------+
        """
        ...

    def Extrusion(self, Index) -> IExtrusion:  # iextrusions_extrusion.html
        """
        ## Extrusion - Операция выдавливания, заданная по индексу или ссылке

        Тип данных: указатель на интерфейс IExtrusion

        В качестве индекса может использоваться:

        •индекс объекта в коллекции,

        •ссылка на объект (reference).

        ### Синтаксис Automation

        ```
        Extrusion = iObject.Extrusion( index )     # Получить свойство (* )
        Extrusion = iObject.GetExtrusion( index )  # Получить свойство (**)
        ```

        ### Входные параметры

        +-------------------------------------------------+
        | VARIANT index | - индекс операции выдавливания. |
        +-------------------------------------------------+
        """
        ...




class IRotateds(IModelObjects):  # irotateds.html
    """
    ## Интерфейс IRotateds

    Коллекция операций вращения.

    Интерфейс можно получить, используя свойство контейнера трехмерных объектов IModelContainer::Rotateds (операции вращения и вырезания вращением) и свойство контейнера поверхностей ISurfaceContainer::RotatedSurfaces (поверхность вращения).
    """

    def Add(self, RotatedType):  # irotateds_add.html
        """
        ## Add - Создает новую операцию вращения и добавляет ее в коллекцию

        Для создания операций вращения и вырезания вращением надо получать коллекцию у контейнера трехмерных объектов с помощью свойства IModelContainer::Rotateds, для создания поверхности вращения - у контейнера поверхностей с помощью свойства ISurfaceContainer::RotatedSurfaces.

        ### Синтаксис Automation

        ```
        LPDISPATCH Add( ksObj3dTypeEnum RotatedType );
        ```

        ### Входные параметры

        RotatedType - тип операции

        ### Возвращаемое значение

        Указатель на интерфейс операции вращения IRotatedIRotated.htm.
        """
        ...

    def Rotated(self, Index) -> IRotated:  # irotateds_rotated.html
        """
        ## Rotated - Возвращает операцию вращения, заданную по индексу

        Тип данных: указатель на интерфейс IRotated

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        Rotated = Object.Rotated( Index )     # Получить свойство (* )
        Rotated = Object.GetRotated( Index )  # Получить свойство (**)
        ```
        """
        ...




class IScalings3D(IModelObjects):  # iscalings3d.html
    """
    ## Интерфейс IScalings3D

    Коллекция операций масштабирования.
    """

    def Add(self) -> IScaling3D:  # iscalings3d_add.html
        """
        ## Add - Создает новый элемент и добавляет его в коллекцию

        ### Синтаксис Automation

        ```
        LPDISPATCH Add();
        ```

        ### Возвращаемое значение

        Указатель на интерфейс IScaling3D

        Создает и добавляет новую операцию масштабирования в коллекцию.
        """
        ...

    def Scaling3D(self, Index) -> IScaling3D:  # iscalings3d_scaling3d.html
        """
        ## Scaling3D - Возвращает элемент, заданный по индексу

        Тип данных: указатель на интерфейс IScaling3D

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        Scaling3D = Object.Scaling3D( Index )     # Получить свойство (* )
        Scaling3D = Object.GetScaling3D( Index )  # Получить свойство (**)
        ```
        """
        ...




class ISurfaceThickenings(IModelObjects):  # isurfacethickenings.html
    """
    ## Интерфейс ISurfaceThickenings

    Коллекция операций придания толщины поверхности.
    """

    def Add(self) -> ISurfaceThickening:  # isurfacethickenings_add.html
        """
        ## Add - Создает новый элемент и добавляет его в коллекцию

        ### Синтаксис Automation

        ```
        LPDISPATCH Add();
        ```

        ### Возвращаемое значение

        Указатель на интерфейс операции придания толщины поверхности ISurfaceThickening.
        """
        ...

    def SurfaceThickening(self, Index) -> ISurfaceThickening:  # isurfacethickenings_surfacethickening.html
        """
        ## SurfaceThickening - Возвращает элемент, заданный по индексу

        Тип данных: указатель на интерфейс ISurfaceThickening

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        SurfaceThickening = Object.SurfaceThickening( Index )     # Получить свойство (* )
        SurfaceThickening = Object.GetSurfaceThickening( Index )  # Получить свойство (**)
        ```
        """
        ...




class IExtrusion(IModelObject):  # iextrusion.html
    """
    ## Интерфейс IExtrusion

    Интерфейс операции выдавливания.

    1. Интерфейс можно получить с помощью метода коллекции операций выдавливания IExtrusions::Add или свойства IExtrusions::Extrusion.

    2. Посредством вызова метода IUnknown::QueryInterface (const GUID far& iid, void** pif) интерфейс позволяет получить следующие дополнительный интерфейсы:

    •IChooseBodies7 - интерфейс для области применения тел в операции,

    •IThinParameters - интерфейс параметров тонкой стенки,

    •ICutExtrusion - интерфейс операции "Вырезать выдавливанием",

    •IExtrusion1 - интерфейс параметров элемента выдавливания.
    """

    @property
    def Direction(self) -> int:  # iextrusion_direction.html
        """
        ## Direction - Тип направления выдавливания

        Тип данных: из перечисления DirectionTypeEnum

        Свойство позволяет получить и установить тип направления выдавливания.

        ### Синтаксис Automation

        ```
        Direction = iObject.Direction;       # Получить свойство (* )
        iObject.Direction = Direction;       # Установить свойство (* )
        Direction = iObject.GetDirection();  # Получить свойство (**)
        iObject.SetDirection( Direction );   # Установить свойство (**)
        ```
        """
        ...
    @Direction.setter
    def Direction(self, value: int): ...

    @property
    def Sketch(self) -> ISketch:  # iextrusion_sketch.html
        """
        ## Sketch - Эскиз

        Тип данных: указатель на интерфейс эскиза ISketch

        Свойство позволяет установить новый эскиз для операции и получить указатель на эскиз.

        ### Синтаксис Automation

        ```
        Sketch = iObject.Sketch;       # Получить свойство (* )
        iObject.Sketch = Sketch;       # Установить свойство (* )
        Sketch = iObject.GetSketch();  # Получить свойство (**)
        iObject.SetSketch( Sketch );   # Установить свойство (**)
        ```
        """
        ...
    @Sketch.setter
    def Sketch(self, value: ISketch): ...

    def Depth(self, Normal) -> float:  # iextrusion_depth.html
        """
        ## Depth - Глубина выдавливания

        Тип данных: double

        Свойство позволяет получить и установить глубину выдавливания.

        ### Синтаксис Automation

        ```
        Depth = iObject.Depth( normal );     # Получить свойство (* )
        iObject.Depth( normal ) = Depth;     # Установить свойство (* )
        Depth = iObject.GetDepth( normal );  # Получить свойство (**)
        iObject.SetDepth( normal, Depth );   # Установить свойство (**)
        ```

        ### Входные параметры

        +-------------------------------------------------------------------------------------------------+
        | normal | TRUE - выдавливание в прямом направлении, FALSE - выдавливание в обратном направлении. |
        +-------------------------------------------------------------------------------------------------+
        """
        ...

    def DepthObject(self, Normal) -> IModelObject:  # iextrusion_depthobject.html
        """
        ## DepthObject - Объект, задающий глубину выдавливания

        Тип данных: указатель на интерфейс объекта IModelObject

        1. Свойство позволяет получать указатель на объект, задающий глубину.

        2. Объект может быть задан, если направление на него совпадает с выбранным направлением (прямым или обратным) операции выдавливания IExtrusion::Direction.

        ### Синтаксис Automation

        ```
        DepthObject = iObject.DepthObject( normal );     # Получить свойство (* )
        iObject.DepthObject( normal ) = DepthObject;     # Установить свойство (* )
        DepthObject = iObject.GetDepthObject( normal );  # Получить свойство (**)
        iObject.SetDepthObject( normal, DepthObject );   # Установить свойство (**)
        ```

        ### Входные параметры

        +-------------------------------------------------------------------------------------------------------------------+
        | normal | TRUE - признак уклона наружу в прямом направлении, FALSE - признак уклона наружу в обратном направлении. |
        +-------------------------------------------------------------------------------------------------------------------+
        """
        ...

    def DraftOutward(self, Normal) -> bool:  # iextrusion_draftoutward.html
        """
        ## DraftOutward - Признак уклона внутрь

        Тип данных: bool

        Свойство позволяет получить и установить признак уклона внутрь.

        ### Синтаксис Automation

        ```
        DraftOutward = iObject.DraftOutward( normal );     # Получить свойство (* )
        iObject.DraftOutward( normal ) = DraftOutward;     # Установить свойство (* )
        DraftOutward = iObject.GetDraftOutward( normal );  # Получить свойство (**)
        iObject.SetDraftOutward( normal, DraftOutward );   # Установить свойство (**)
        ```

        ### Входные параметры

        +-------------------------------------------------------------------------------------------------------------------+
        | normal | TRUE - признак уклона наружу в прямом направлении, FALSE - признак уклона наружу в обратном направлении. |
        +-------------------------------------------------------------------------------------------------------------------+
        """
        ...

    def DraftValue(self, Normal) -> float:  # iextrusion_draftvalue.html
        """
        ## DraftValue - Угол наклона

        Тип данных: double

        Свойство позволяет получить и установить угол наклона.

        ### Синтаксис Automation

        ```
        DraftValue = iObject.DraftValue( normal );     # Получить свойство (* )
        iObject.DraftValue( normal ) = DraftValue;     # Установить свойство (* )
        DraftValue = iObject.GetDraftValue( normal );  # Получить свойство (**)
        iObject.SetDraftValue( normal, DraftValue );   # Установить свойство (**)
        ```

        ### Входные параметры

        +------------------------------------------------------------------------------------------------+
        | normal | TRUE -угол наклона в прямом направлении, FALSE - угол наклона в обратном направлении. |
        +------------------------------------------------------------------------------------------------+
        """
        ...

    def ExtrusionType(self, Normal) -> int:  # iextrusion_extrusiontype.html
        """
        ## ExtrusionType - Тип выдавливания

        Тип данных: из перечисления EndTypeEnum

        Свойство позволяет получить и установить тип выдавливания.

        ### Синтаксис Automation

        ```
        ExtrusionType = iObject.ExtrusionType( normal );     # Получить свойство (* )
        iObject.ExtrusionType( normal ) = ExtrusionType;     # Установить свойство (* )
        ExtrusionType = iObject.GetExtrusionType( normal );  # Получить свойство (**)
        iObject.SetExtrusionType( normal, ExtrusionType );   # Установить свойство (**)
        ```

        ### Входные параметры

        +-------------------------------------------------------------------------------------------------+
        | normal | TRUE - выдавливание в прямом направлении, FALSE - выдавливание в обратном направлении. |
        +-------------------------------------------------------------------------------------------------+
        """
        ...

    def GetSideParameters(self, Normal) -> bool:  # iextrusion_getsideparameters.html
        """
        ## GetSideParameters - Получить параметры выдавливания в одном направлении

        1. Метод позволяет получать параметры операции выдавливание.

        2. Параметр Depth позволяет задать на сколько выдавить за объект, задающий глубину выдавливания, или какое расстояние оставить до объекта, задающего глубину выдавливания - в зависимости от типа построения (для типов построения "до поверхности" и "до вершины" см. перечисление ksEndTypeEnum).

        ### Синтаксис Automation

        ```
        BOOL GetSideParameters( BOOL normal,
        End_Type * ExtrusionType,
        double * Depth,
        double * DraftValue,
        BOOL * DraftOutward,
        IModelObject * DepthObject );
        ```

        ### Входные параметры

        +------------------------------------------------------------------------------------+
        | normal | направление, TRUE - в прямом направлении, FALSE - в обратном направлении. |
        +------------------------------------------------------------------------------------+

        ### Выходные параметры

        +-----------------------------------------------------------------------+
        | ExtrusionType | - тип выдавливания,                                   |
        +-----------------------------------------------------------------------+
        | Depth         | - глубина выдавливания,                               |
        +-----------------------------------------------------------------------+
        | DraftValue    | - угол наклона,                                       |
        +-----------------------------------------------------------------------+
        | DraftOutward  | - признак уклона внутрь,                              |
        +-----------------------------------------------------------------------+
        | DepthObject   | - указатель на объект, задающий глубину выдавливания. |
        +-----------------------------------------------------------------------+

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def SetDepth(self, Normal, arg1):
        ...

    def SetDepthObject(self, Normal, arg1):
        ...

    def SetDraftOutward(self, Normal, arg1):
        ...

    def SetDraftValue(self, Normal, arg1):
        ...

    def SetExtrusionType(self, Normal, arg1):
        ...

    def SetSideParameters(self, Normal, ExtrusionType, Depth, DraftValue, DraftOutward, DepthObject) -> bool:  # iextrusion_setsideparameters.html
        """
        ## SetSideParameters - Установить параметры выдавливания в одном направлении

        1. Метод позволяет получать параметры операции выдавливание.

        2. Параметр Depth в позволяет задать на сколько выдавить за объект, задающий глубину выдавливания, или какое расстояние оставить до объекта, задающего глубину выдавливания - в зависимости от типа построения (для типов построения "до поверхности" и "до вершины" см. перечисление ksEndTypeEnum).

        ### Синтаксис Automation

        ```
        BOOL SetSideParameters( BOOL normal,
        End_Type * ExtrusionType,
        double * Depth,
        double * DraftValue,
        BOOL * DraftOutward,
        IModelObject * DepthObject );
        ```

        ### Входные параметры

        +-------------------------------------------------------------------------------------------+
        | normal        | направление, TRUE - в прямом направлении, FALSE - в обратном направлении, |
        +-------------------------------------------------------------------------------------------+
        | ExtrusionType | - тип выдавливания,                                                       |
        +-------------------------------------------------------------------------------------------+
        | Depth         | - глубина выдавливания,                                                   |
        +-------------------------------------------------------------------------------------------+
        | DraftValue    | - угол наклона,                                                           |
        +-------------------------------------------------------------------------------------------+
        | DraftOutward  | - признак уклона внутрь,                                                  |
        +-------------------------------------------------------------------------------------------+
        | DepthObject   | - указатель на объект, задающий глубину выдавливания.                     |
        +-------------------------------------------------------------------------------------------+

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...




class ICutExtrusion(IExtrusion):  # icutextrusion.html
    """
    ## Интерфейс ICutExtrusion

    Интерфейс операции "Вырезать выдавливанием".

    Интерфейс можно получить с помощью метода коллекции операций выдавливания IExtrusions::Add(Obj3dType ExtrusionType ), передав тип 3D объекта - операция "вырезать выдавливанием" (o3d_cutExtrusion) или свойства IExtrusions::Extrusion.
    """

    @property
    def Cut(self) -> bool:  # icutextrusion_cut.html
        """
        ## Cut - Признак результата операции (результирующего тела)

        Тип данных: bool

        Свойство позволяет установить и получить результат (результирующее тело) операции "Вырезать выдавливанием".

        ### Синтаксис Automation

        ```
        Cut = iObject.Cut;       # Получить свойство(* )
        iObject.Cut = Cut;       # Установить свойство (* )
        Cut = iObject.GetCut();  # Получить свойство (**)
        iObject.SetCut( Cut );   # Установить свойство (**)
        ```
        """
        ...
    @Cut.setter
    def Cut(self, value: bool): ...




class IExtrusionSurface(IExtrusion):  # iextrusionsurface.html
    """
    ## Интерфейс IExtrusionSurface

    Интерфейс параметров поверхности выдавливания

    Интерфейс можно получить с помощью свойства IExtrusions::Extrusion и метода IExtrusions::Add с типом o3d_ExtrusionSurface.
    """

    @property
    def ClosedShell(self) -> bool:  # iextrusionsurface_closedshell.html
        """
        ## ClosedShell - Признак замкнутости поверхности

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        ClosedShell = Object.ClosedShell       # Получить свойство(* )
        Object.ClosedShell = ClosedShell       # Установить свойство (* )
        ClosedShell = Object.GetClosedShell()  # Получить свойство (**)
        Object.SetClosedShell( ClosedShell )   # Установить свойство (**)
        ```
        """
        ...
    @ClosedShell.setter
    def ClosedShell(self, value: bool): ...




class IRotated(IModelObject):  # irotated.html
    """
    ## Интерфейс IRotated

    Интерфейс параметров операции вращения.

    Интерфейс можно получить с помощью свойства IRotateds::Rotated и метода IRotateds::Add с типом o3d_bossRotated.
    """

    @property
    def Axis(self) -> IModelObject:  # irotated_axis.html
        """
        ## Axis - Ось операции вращения

        Тип данных: указатель на интерфейс IModelObject

        Объект, задающий ось, должен быть прямолинейным. Если используется ось из эскиза, то она должна быть изображена отрезком со стилем линии Осевая или объектом Осевая линия.

        ### Синтаксис Automation

        ```
        Axis = Object.Axis      # Получить свойство (* )
        Object.Axis = Axis      # Установить свойство (**)
        Object.Axis = Axis      # Получить свойство (**)
        Object.SetAxis( Axis )  # Установить свойство (**)
        ```
        """
        ...
    @Axis.setter
    def Axis(self, value: IModelObject): ...

    @property
    def Direction(self) -> int:  # irotated_direction.html
        """
        ## Direction - Направление вращения

        Тип данных: из перечисления ksDirectionTypeEnum

        ### Синтаксис Automation

        ```
        Direction = Object.Direction       # Получить свойство (* )
        Object.Direction = Direction       # Установить свойство (**)
        Direction = Object.GetDirection()  # Получить свойство (**)
        Object.SetDirection( Direction )   # Установить свойство (**)
        ```
        """
        ...
    @Direction.setter
    def Direction(self, value: int): ...

    @property
    def Profile(self) -> IModelObject:  # irotated_profile.html
        """
        ## Profile - Сечение для операции вращения

        Тип данных: указатель на интерфейс IModelObject

        Если в эскизе, задающем сечение, присутствует ось, и ось не задана свойством IRotated::Axis, то для построения операции будет использоваться ось из эскиза.

        ### Синтаксис Automation

        ```
        Profile = Object.Profile       # Получить свойство (* )
        Object.Profile = Profile       # Установить свойство (**)
        Profile = Object.GetProfile()  # Получить свойство (**)
        Object.SetProfile( Profile )   # Установить свойство (**)
        ```
        """
        ...
    @Profile.setter
    def Profile(self, value: IModelObject): ...

    @property
    def ToroidShapeType(self) -> bool:  # irotated_toroidshapetype.html
        """
        ## ToroidShapeType - Признак тороида

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        ToroidShapeType = Object.ToroidShapeType       # Получить свойство (* )
        Object.ToroidShapeType = ToroidShapeType       # Установить свойство (**)
        ToroidShapeType = Object.GetToroidShapeType()  # Получить свойство (**)
        Object.SetToroidShapeType( ToroidShapeType )   # Установить свойство (**)
        ```
        """
        ...
    @ToroidShapeType.setter
    def ToroidShapeType(self, value: bool): ...

    def Angle(self, Normal) -> float:  # irotated_angle.html
        """
        ## Angle - Угол вращения

        Тип данных: double

        1. Для вращения в двух направлениях угол требуется задать дважды — для прямого и обратного направления.

        2. Если используется тип направления Средняя плоскость, то угол задается один раз. При этом он воспринимается системой как общий угол (т.е. в каждую сторону откладывается его половина).

        ### Синтаксис Automation

        ```
        Angle = Object.Angle( Normal )     # Получить свойство (* )
        Object.Angle( Normal ) = Angle     # Установить свойство (* )
        Angle = Object.GetAngle( Normal )  # Получить свойство (**)
        Object.SetAngle( Normal, Angle )   # Установить свойство (**)
        ```

        ### Входные параметры

        +------------------------------+
        | BOOL Normal | - направление. |
        +------------------------------+
        """
        ...

    def AngleObject(self, Normal) -> IModelObject:  # irotated_angleobject.html
        """
        ## AngleObject - Объект, задающий угол вращения

        Тип данных: указатель на интерфейс IModelObject

        ### Синтаксис Automation

        ```
        AngleObject = Object.AngleObject( Normal )     # Получить свойство (* )
        Object.AngleObject( Normal ) = AngleObject     # Установить свойство (* )
        AngleObject = Object.GetAngleObject( Normal )  # Получить свойство (**)
        Object.SetAngleObject( Normal, AngleObject )   # Установить свойство (**)
        ```

        ### Входные параметры

        +------------------------------+
        | BOOL Normal | - направление. |
        +------------------------------+
        """
        ...

    def CutOffByPoint(self, Normal) -> bool:  # irotated_cutoffbypoint.html
        """
        ## CutOffByPoint - Флаг «отсекать» для способа определения угла До точки

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        CutOffByPoint( Normal ) = Object.CutOffByPoint     # Получить свойство (* )
        Object.CutOffByPoint( Normal ) = CutOffByPoint     # Установить свойство (* )
        CutOffByPoint = Object.GetCutOffByPoint( Normal )  # Получить свойство (**)
        Object.SetCutOffByPoint( Normal, CutOffByPoint )   # Установить свойство (**)
        ```
        """
        ...

    def RotatedType(self, Normal) -> int:  # irotated_rotatedtype.html
        """
        ## RotatedType - Способ построения угла вращения

        Тип данных: из перечисления ksRotatedTypeEnum

        ### Синтаксис Automation

        ```
        RotatedType = Object.RotatedType( Normal )     # Получить свойство (* )
        Object.RotatedType( Normal ) = RotatedType     # Установить свойство (**)
        RotatedType = Object.GetRotatedType( Normal )  # Получить свойство (**)
        Object.SetRotatedType( Normal, RotatedType )   # Установить свойство (**)
        ```

        ### Входные параметры

        +------------------------------+
        | BOOL Normal | - направление. |
        +------------------------------+
        """
        ...

    def SetAngle(self, Normal, arg1):
        ...

    def SetAngleObject(self, Normal, arg1):
        ...

    def SetCutOffByPoint(self, Normal, arg1):
        ...

    def SetRotatedType(self, Normal, arg1):
        ...




class ICutRotated(IRotated):  # icutrotated.html
    """
    ## Интерфейс ICutRotated

    Интерфейс параметров операции «Вырезать вращением»

    Интерфейс можно получить с помощью свойства IRotateds::Rotated и метода IRotateds::Add с типом o3d_cutRotated.
    """

    @property
    def Cut(self) -> bool:  # icutrotated_cut.html
        """
        ## Cut - Признак результата операции (результирующего тела)

        Тип данных: BOOL

        Значения свойства:

        +----------------------------------+
        | TRUE  | - вычитание элементов,   |
        +----------------------------------+
        | FALSE | - пересечение элементов. |
        +----------------------------------+

        ### Синтаксис Automation

        ```
        Cut = Object.Cut       # Получить свойство (* )
        Object.Cut = Cut       # Установить свойство (* )
        Cut = Object.GetCut()  # Получить свойство (**)
        Object.SetCut( Cut )   # Установить свойство (**)
        ```
        """
        ...
    @Cut.setter
    def Cut(self, value: bool): ...




class IRotatedSurface(IRotated):  # irotatedsurface.html
    """
    ## Интерфейс IRotatedSurface

    Интерфейс параметров поверхности вращения

    Интерфейс можно получить с помощью свойства IRotateds::Rotated и метода IRotateds::Add с типом o3d_RotatedSurface.
    """

    @property
    def ClosedShell(self) -> bool:  # irotatedsurface_closedshell.html
        """
        ## ClosedShell - Признак замкнутости поверхности

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        ClosedShell = Object.ClosedShell       # Получить свойство (* )
        Object.ClosedShell = ClosedShell       # Установить свойство (* )
        ClosedShell = Object.GetClosedShell()  # Получить свойство (**)
        Object.SetClosedShell( ClosedShell )   # Установить свойство (**)
        ```
        """
        ...
    @ClosedShell.setter
    def ClosedShell(self, value: bool): ...




class IRotated1(IDispatch):  # irotated1.html
    """
    ## Интерфейс IRotated1

    Интерфейс операции вращения.
    """

    @property
    def OperationResult(self) -> int:  # irotated1_operationresult.html
        """
        ## OperationResult - Результат операции

        Тип данных: Значение из перечисления ksOperationResultEnum.

        ### Синтаксис Automation

        ```
        OperationResult = Object.OperationResult       # Получить свойство (* )
        Object.OperationResult = OperationResult       # Установить свойство (**)
        OperationResult = Object.GetOperationResult()  # Получить свойство (**)
        Object.SetOperationResult( OperationResult )   # Установить свойство (**)
        ```
        """
        ...
    @OperationResult.setter
    def OperationResult(self, value: int): ...




class IShellSurface(IDispatch):  # ishellsurface.html
    """
    ## Интерфейс IShellSurface

    Интерфейс операции Поверхность (поверхность выдавливания, вращения, по сечениям, кинематическая).
    """

    @property
    def ClosedShell(self) -> bool:  # ishellsurface_closedshell.html
        """
        ## ClosedShell - Признак замкнутости поверхности

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        ClosedShell = Object.ClosedShell       # Получить свойство (* )
        Object.ClosedShell = ClosedShell       # Установить свойство (**)
        ClosedShell = Object.GetClosedShell()  # Получить свойство (**)
        Object.SetClosedShell( ClosedShell )   # Установить свойство (**)
        ```
        """
        ...
    @ClosedShell.setter
    def ClosedShell(self, value: bool): ...




class IScaling3D(IModelObject):  # iscaling3d.html
    """
    ## Интерфейс IScaling3D

    Интерфейс параметров операции масштабирования.
    """

    @property
    def BasePoint(self) -> IModelObject:  # iscaling3d_basepoint.html
        """
        ## BasePoint - Центр масштабирования тела или поверхности

        Тип данных: указатель на интерфейс IModelObject

        ### Синтаксис Automation

        ```
        BasePoint = Object.BasePoint       # Получить свойство (* )
        Object.BasePoint = BasePoint       # Установить свойство (* )
        BasePoint = Object.GetBasePoint()  # Получить свойство (**)
        Object.SetBasePoint( BasePoint )   # Установить свойство (**)
        ```
        """
        ...
    @BasePoint.setter
    def BasePoint(self, value: IModelObject): ...

    @property
    def CentrePointBuildingType(self) -> int:  # iscaling3d_centrepointbuildingtype.html
        """
        ## CentrePointBuildingType - Тип параметров построения точки привязки для центра масштабирования

        Тип данных: из перечисления ksPoint3DTypeEnum

        ### Синтаксис Automation

        ```
        CentrePointBuildingType = Object.CentrePointBuildingType       # Получить свойство (* )
        Object.CentrePointBuildingType = CentrePointBuildingType       # Установить свойство (* )
        CentrePointBuildingType = Object.GetCentrePointBuildingType()  # Получить свойство (**)
        Object.SetCentrePointBuildingType( CentrePointBuildingType )   # Установить свойство (**)
        ```
        """
        ...
    @CentrePointBuildingType.setter
    def CentrePointBuildingType(self, value: int): ...

    @property
    def CentrePointParameters(self) -> IKompasAPIObject:  # iscaling3d_centrepointparameters.html
        """
        ## CentrePointParameters - Получить интерфейс параметров точки

        Тип данных: Указатель на интерфейс IKompasAPIObject

        Свойство доступно только для чтения.

        Версия Компас v21

        ### Синтаксис Automation

        ```
        CentrePointParameters = Object.CentrePointParameters       # Получить свойство (* )
        CentrePointParameters = Object.GetCentrePointParameters()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def CopySolids(self) -> bool:  # iscaling3d_copysolids.html
        """
        ## CopySolids - Сохранить копии тел и поверхностей

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        CopySolids = Object.CopySolids       # Получить свойство (* )
        Object.CopySolids = CopySolids       # Установить свойство (* )
        CopySolids = Object.GetCopySolids()  # Получить свойство (**)
        Object.SetCopySolids( CopySolids )   # Установить свойство (**)
        ```
        """
        ...
    @CopySolids.setter
    def CopySolids(self, value: bool): ...

    @property
    def Scale(self) -> float:  # iscaling3d_scale.html
        """
        ## Scale - Масштаб (коэффициент масштабирования) тела или поверхности

        Тип данных: double

        ### Синтаксис Automation

        ```
        Scale = Object.Scale       # Получить свойство (* )
        Object.Scale = Scale       # Установить свойство (* )
        Scale = Object.GetScale()  # Получить свойство (**)
        Object.SetScale( Scale )   # Установить свойство (**)
        ```
        """
        ...
    @Scale.setter
    def Scale(self, value: float): ...

    @property
    def ScaleX(self) -> float:  # iscaling3d_scalex.html
        """
        ## ScaleX - Масштаб по оси X

        Тип данных: double

        ### Синтаксис Automation

        ```
        ScaleX = Object.ScaleX       # Получить свойство (* )
        Object.ScaleX = ScaleX       # Установить свойство (* )
        ScaleX = Object.GetScaleX()  # Получить свойство (**)
        Object.SetScaleX( ScaleX )   # Установить свойство (**)
        ```
        """
        ...
    @ScaleX.setter
    def ScaleX(self, value: float): ...

    @property
    def ScaleY(self) -> float:  # iscaling3d_scaley.html
        """
        ## ScaleY - Масштаб по оси Y

        Тип данных: double

        ### Синтаксис Automation

        ```
        ScaleY= Object.ScaleY       # Получить свойство (* )
        Object.ScaleY= ScaleY       # Установить свойство (* )
        ScaleY= Object.GetScaleY()  # Получить свойство (**)
        Object.SetScaleY( ScaleY)   # Установить свойство (**)
        ```
        """
        ...
    @ScaleY.setter
    def ScaleY(self, value: float): ...

    @property
    def ScaleZ(self) -> float:  # iscaling3d_scalez.html
        """
        ## ScaleZ - Масштаб по оси Z

        Тип данных: double

        ### Синтаксис Automation

        ```
        ScaleZ= Object.ScaleZ       # Получить свойство (* )
        Object.ScaleZ= ScaleZ       # Установить свойство (* )
        ScaleZ= Object.GetScaleZ()  # Получить свойство (**)
        Object.SetScaleZ( ScaleZ)   # Установить свойство (**)
        ```
        """
        ...
    @ScaleZ.setter
    def ScaleZ(self, value: float): ...

    @property
    def ScalingObjects(self) -> typing.Any:  # iscaling3d_scalingobjects.html
        """
        ## ScalingObjects - Масштабируемые объекты

        Тип данных: VARIANT

        ### Синтаксис Automation

        ```
        ScalingObjects = Object.ScalingObjects       # Получить свойство (* )
        Object.ScalingObjects = ScalingObjects       # Установить свойство (* )
        ScalingObjects = Object.GetScalingObjects()  # Получить свойство (**)
        Object.SetScalingObjects( ScalingObjects )   # Установить свойство (**)
        ```
        """
        ...
    @ScalingObjects.setter
    def ScalingObjects(self, value: typing.Any): ...

    @property
    def ScalingType(self) -> int:  # iscaling3d_scalingtype.html
        """
        ## ScalingType - Способ масштабирования

        Тип данных: из перечисления ksScalingTypeEnum

        ### Синтаксис Automation

        ```
        ScalingType = Object.ScalingType       # Получить свойство (* )
        Object.ScalingType = ScalingType       # Установить свойство (* )
        ScalingType = Object.GetScalingType()  # Получить свойство (**)
        Object.SetScalingType( ScalingType )   # Установить свойство (**)
        ```
        """
        ...
    @ScalingType.setter
    def ScalingType(self, value: int): ...

    @property
    def Shell(self) -> IKompasAPIObject:  # iscaling3d_shell.html
        """
        ## Shell - Тело или поверхность для масштабирования

        Тип данных: указатель на интерфейс IKompasAPIObject

        ### Синтаксис Automation

        ```
        Shell = Object.Shell       # Получить свойство (* )
        Object.Shell = Shell       # Установить свойство (* )
        Shell = Object.GetShell()  # Получить свойство (**)
        Object.SetShell( Shell )   # Установить свойство (**)
        ```
        """
        ...
    @Shell.setter
    def Shell(self, value: IKompasAPIObject): ...

    def GetCentrePoint(self) -> bool:  # iscaling3d_getcentrepoint.html
        """
        ## GetCentrePoint - Получить центр масштабирования

        ### Синтаксис Automation

        ```
        BOOL GetCentrePoint( double * X, double * Y, double * Z );
        ```

        ### Выходные параметры

        +------------------------------------------------+
        | X, Y, Z | - координаты центра масштабирования. |
        +------------------------------------------------+

        Версия Компас v21

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def HideCurves(self) -> bool:  # iscaling3d_hidecurves.html
        """
        ## HideCurves - Скрыть исходные кривые

        ### Синтаксис Automation

        ```
        BOOL HideCurves();
        ```

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+

        Версия Компас v21
        """
        ...

    def SetCentrePoint(self, X, Y, Z) -> bool:  # iscaling3d_setcentrepoint.html
        """
        ## SetCentrePoint - Установить центр масштабирования

        ### Синтаксис Automation

        ```
        BOOL SetCentrePoint( double X, double Y, double Z );
        ```

        ### Входные параметры

        +------------------------------------------------+
        | X, Y, Z | - координаты центра масштабирования. |
        +------------------------------------------------+

        Версия Компас v21

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...




class ISurfaceThickening(IModelObject):  # isurfacethickening.html
    """
    ## Интерфейс ISurfaceThickening

    Интерфейс операции придания толщины поверхности.
    """

    @property
    def Faces(self):
        ...
    @Faces.setter
    def Faces(self, value): ...




class IThread(IKompasCollection):  # ithread.html
    """
    ## Интерфейс IThread

    Интерфейс условного обозначения резьбы.
    """

    @property
    def AutoDiameter(self) -> bool:  # ithread_autodiameter.html
        """
        ## AutoDiameter - Автоопределение диаметра

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        AutoDiameter = Object.AutoDiameter       # Получить свойство (* )
        Object.AutoDiameter = AutoDiameter       # Установить свойство (* )
        AutoDiameter = Object.GetAutoDiameter()  # Получить свойство (**)
        Object.SetAutoDiameter( AutoDiameter )   # Установить свойство (**)
        ```
        """
        ...
    @AutoDiameter.setter
    def AutoDiameter(self, value: bool): ...

    @property
    def AutoLenght(self) -> bool:  # ithread_autolenght.html
        """
        ## AutoLenght - Автоматическое определение длины резьбы (TRUE - длина определяется автоматически - до конечной границы, FALSE - длина задана явно)

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        AutoLenght = Object.AutoLenght       # Получить свойство (* )
        Object.AutoLenght = AutoLenght       # Установить свойство (* )
        AutoLenght = Object.GetAutoLenght()  # Получить свойство (**)
        Object.SetAutoLenght( AutoLenght )   # Установить свойство (**)
        ```
        """
        ...
    @AutoLenght.setter
    def AutoLenght(self, value: bool): ...

    @property
    def BaseObject(self) -> IModelObject:  # ithread_baseobject.html
        """
        ## BaseObject - Ребро или грань, по которой строится резьба

        Тип данных: Указатель на интерфейс IModelObject

        ### Синтаксис Automation

        ```
        BaseObject = Object.BaseObject       # Получить свойство (* )
        Object.BaseObject = BaseObject       # Установить свойство (* )
        BaseObject = Object.GetBaseObject()  # Получить свойство (**)
        Object.SetBaseObject( BaseObject )   # Установить свойство (**)
        ```
        """
        ...
    @BaseObject.setter
    def BaseObject(self, value: IModelObject): ...

    @property
    def BaseObjectAdjustment(self) -> bool:  # ithread_baseobjectadjustment.html
        """
        ## BaseObjectAdjustment - Подгонка диаметра базового объекта под диаметр резьбы

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        BaseObjectAdjustment = Object.BaseObjectAdjustment       # Получить свойство (* )
        Object.BaseObjectAdjustment = BaseObjectAdjustment       # Установить свойство (* )
        BaseObjectAdjustment = Object.GetBaseObjectAdjustment()  # Получить свойство (**)
        Object.SetBaseObjectAdjustment( BaseObjectAdjustment )   # Установить свойство (**)
        ```
        """
        ...
    @BaseObjectAdjustment.setter
    def BaseObjectAdjustment(self, value: bool): ...

    @property
    def BaseObjectAdjustmentOffset1(self) -> bool:  # ithread_baseobjectadjustmentoffset1.html
        """
        ## BaseObjectAdjustmentOffset1 - Дополнительная подгонка для сохранения глубины зенковки/скругления/фаски при подгонке диаметра базового объекта под диаметр резьбы

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        BaseObjectAdjustmentOffset1 = Object.BaseObjectAdjustmentOffset1       # Получить свойство (* )
        Object.BaseObjectAdjustmentOffset1 = BaseObjectAdjustmentOffset1       # Установить свойство (* )
        BaseObjectAdjustmentOffset1 = Object.GetBaseObjectAdjustmentOffset1()  # Получить свойство (**)
        Object.SetBaseObjectAdjustmentOffset1( BaseObjectAdjustmentOffset1 )   # Установить свойство (**)
        ```
        """
        ...
    @BaseObjectAdjustmentOffset1.setter
    def BaseObjectAdjustmentOffset1(self, value: bool): ...

    @property
    def BaseObjectAdjustmentOffset2(self) -> bool:  # ithread_baseobjectadjustmentoffset2.html
        """
        ## BaseObjectAdjustmentOffset2 - Дополнительная подгонка для сохранения глубины неплоского торца отверстия при подгонке диаметра базового объекта под диаметр резьбы

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        BaseObjectAdjustmentOffset2 = Object.BaseObjectAdjustmentOffset2       # Получить свойство (* )
        Object.BaseObjectAdjustmentOffset2 = BaseObjectAdjustmentOffset2       # Установить свойство (* )
        BaseObjectAdjustmentOffset2 = Object.GetBaseObjectAdjustmentOffset2()  # Получить свойство (**)
        Object.SetBaseObjectAdjustmentOffset2( BaseObjectAdjustmentOffset2 )   # Установить свойство (**)
        ```
        """
        ...
    @BaseObjectAdjustmentOffset2.setter
    def BaseObjectAdjustmentOffset2(self, value: bool): ...

    @property
    def Direction(self) -> bool:  # ithread_direction.html
        """
        ## Direction - Направление резьбы (TRUE - прямое, FALSE - обратное)

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        Direction = Object.Direction       # Получить свойство (* )
        Object.Direction = Direction       # Установить свойство (* )
        Direction = Object.GetDirection()  # Получить свойство (**)
        Object.SetDirection( Direction )   # Установить свойство (**)
        ```
        """
        ...
    @Direction.setter
    def Direction(self, value: bool): ...

    @property
    def FinalBorder(self) -> IModelObject:  # ithread_finalborder.html
        """
        ## FinalBorder - Конечная граница

        Тип данных: Указатель на интерфейс IModelObject

        ### Синтаксис Automation

        ```
        FinalBorder = Object.FinalBorder       # Получить свойство (* )
        Object.FinalBorder = FinalBorder       # Установить свойство (* )
        FinalBorder = Object.GetFinalBorder()  # Получить свойство (**)
        Object.SetFinalBorder( FinalBorder )   # Установить свойство (**)
        ```
        """
        ...
    @FinalBorder.setter
    def FinalBorder(self, value: IModelObject): ...

    @property
    def Hidden(self):
        ...
    @Hidden.setter
    def Hidden(self, value): ...

    @property
    def InitialBorder(self) -> IModelObject:  # ithread_initialborder.html
        """
        ## InitialBorder - Начальная граница

        Тип данных: Указатель на интерфейс IModelObject

        ### Синтаксис Automation

        ```
        InitialBorder = Object.InitialBorder       # Получить свойство (* )
        Object.InitialBorder = InitialBorder       # Установить свойство (* )
        InitialBorder = Object.GetInitialBorder()  # Получить свойство (**)
        Object.SetInitialBorder( InitialBorder )   # Установить свойство (**)
        ```
        """
        ...
    @InitialBorder.setter
    def InitialBorder(self, value: IModelObject): ...

    @property
    def LeftThread(self) -> bool:  # ithread_leftthread.html
        """
        ## LeftThread - Левосторонняя резьба

        Тип данных: BOOL

        Включение признака левой резьбы влияет на формирование обозначения резьбы. В обзначение будут добавлены буквы "LH", например, "МК 20*1,5 LH".

        ### Синтаксис Automation

        ```
        LeftThread = Object.LeftThread       # Получить свойство (* )
        Object.LeftThread = LeftThread       # Установить свойство (* )
        LeftThread = Object.GetLeftThread()  # Получить свойство (**)
        Object.SetLeftThread( LeftThread )   # Установить свойство (**)
        ```
        """
        ...
    @LeftThread.setter
    def LeftThread(self, value: bool): ...

    @property
    def Lenght(self) -> float:  # ithread_lenght.html
        """
        ## Lenght - Длина резьбы (для варианта явного задания длины)

        Тип данных: double

        ### Синтаксис Automation

        ```
        Lenght = Object.Lenght       # Получить свойство (* )
        Object.Lenght = Lenght       # Установить свойство (* )
        Lenght = Object.GetLenght()  # Получить свойство (**)
        Object.SetLenght( Lenght )   # Установить свойство (**)
        ```
        """
        ...
    @Lenght.setter
    def Lenght(self, value: float): ...

    @property
    def ModelObjectType(self):
        ...

    @property
    def Name(self):
        ...
    @Name.setter
    def Name(self, value): ...

    @property
    def Owner(self):
        ...

    @property
    def Part(self):
        ...

    @property
    def Valid(self):
        ...

    def Update(self):
        ...




class IThreadPattern(IKompasAPIObject):  # ithreadpattern.html
    """
    ## Интерфейс IThreadPattern

    Интерфейс параметров стандартной резьбы.
    """

    @property
    def ConeAngle(self) -> float:  # ithreadpattern_coneangle.html
        """
        ## ConeAngle - cone - Угол для конической резьбы

        Тип данных: double

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        ConeAngle = Object.ConeAngle       # Получить свойство (* )
        ConeAngle = Object.GetConeAngle()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def HoleKoeff(self) -> float:  # ithreadpattern_holekoeff.html
        """
        ## HoleKoeff - koeff - Коэффициент резьбы

        Тип данных: double

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        HoleKoeff = Object.HoleKoeff      # Получить свойство (* )
        HoleKoeff= Object.GetHoleKoeff()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def PatternsCount(self) -> int:  # ithreadpattern_patternscount.html
        """
        ## PatternsCount - Количество записей в таблице

        Тип данных: long

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        PatternsCount = Object.PatternsCount   # Получить свойство (* )
        PatternsCount= Object.PatternsCount()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def Standart(self) -> str:  # ithreadpattern_standart.html
        """
        ## Standart - Обозначение стандарта

        Тип данных: BSTR

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        Standart = Object.Standart      # Получить свойство (* )
        Standart= Object.GetStandart()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def TableName(self) -> str:  # ithreadpattern_tablename.html
        """
        ## TableName - Имя таблицы данных

        Тип данных: BSTR

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        TableName = Object.TableName   # Получить свойство (* )
        TableName= Object.TableName()  # Получить свойство (**)
        ```
        """
        ...

    def ConeL1(self, Index) -> float:  # ithreadpattern_conel1.html
        """
        ## ConeL1 - L1 Смещение основной плоскости от торца наружной резьбы (мм)

        Тип данных: double

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        ConeL1 = Object.ConeL1( Index )     # Получить свойство (* )
        ConeL1 = Object.GetConeL1( Index )  # Получить свойство (**)
        ```
        """
        ...

    def ConeL2(self, Index) -> float:  # ithreadpattern_conel2.html
        """
        ## ConeL2 - L2 Смещение основной плоскости от торца внутренней резьбы (мм)

        Тип данных: double

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        ConeL2 = Object.ConeL2( Index )     # Получить свойство (* )
        ConeL2 = Object.GetConeL2( Index )  # Получить свойство (**)
        ```
        """
        ...

    def Designation(self, Index) -> str:  # ithreadpattern_designation.html
        """
        ## Designation - Обозначение резьбы

        Тип данных: BSTR

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        Designation = Object.Designation( Index )    # Получить свойство (* )
        Designation= Object.GetDesignation( Index )  # Получить свойство (**)
        ```
        """
        ...

    def GetParameters(self, Index):  # ithreadpattern_getparameters.html
        """
        ## GetParameters - Получить параметры резьбы для заданного диаметра и шага

        ### Синтаксис Automation

        ```
        BSTR GetParameters( long Index, double * NominalDiameter, double * P, double * L, double * L1, double * L2 );
        ```

        ### Входные параметры

        +-----------------------------------------------------+
        | Index | - индекс списка значений в базе стандартов. |
        +-----------------------------------------------------+

        ### Выходные параметры

        +------------------------------------------------------------------------------+
        | NominalDiameter | - номинальный диаметр,                                     |
        +------------------------------------------------------------------------------+
        | P               | - шаг резьбы,                                              |
        +------------------------------------------------------------------------------+
        | L               | - рабочая длина резьбы,                                    |
        +------------------------------------------------------------------------------+
        | L1              | - длина смещения основной плоскости для внешней резьбы,    |
        +------------------------------------------------------------------------------+
        | L2              | - длина смещения основной плоскости для внутренней резьбы. |
        +------------------------------------------------------------------------------+

        ### Возвращаемое значение

        - обозначение резьбы.
        """
        ...

    def Lenght(self, Index) -> float:  # ithreadpattern_lenght.html
        """
        ## Lenght - L - Рабочая длина резьбы (мм)

        Тип данных: double

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        Lenght = Object.Lenght( Index )    # Получить свойство (* )
        Lenght= Object.GetLenght( Index )  # Получить свойство (**)
        ```
        """
        ...

    def NominalDiameter(self, Index) -> float:  # ithreadpattern_nominaldiameter.html
        """
        ## NominalDiameter - D - Номинальный диаметр резьбы (мм)

        Тип данных: double

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        NominalDiameter = Object.NominalDiameter( Index      # Получить свойство (* )
        NominalDiameter= Object.GetNominalDiameter( Index )  # Получить свойство (**)
        ```
        """
        ...

    def P(self, Index) -> float:  # ithreadpattern_p.html
        """
        ## P - P - Шаг резьбы

        Тип данных: double

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        P = Object.P( Index )    # Получить свойство (* )
        P= Object.GetP( Index )  # Получить свойство (**)
        ```
        """
        ...

    def SelectDiameters(self) -> list[float]:  # ithreadpattern_selectdiameters.html
        """
        ## SelectDiameters - Cписок номинальных диаметров резьбы (мм)

        ### Синтаксис Automation

        ```
        VARIANT SelectDiameters();
        ```

        ### Возвращаемое значение

        Массив SafeAttray типа (VT_ARRAY | VT_R8)

        со списком значений диаметров для текущего стандарта.
        """
        ...

    def SelectP(self, NominalDiameter) -> list[float]:  # ithreadpattern_selectp.html
        """
        ## SelectP - Cписок шагов резьбы для данного диаметра

        ### Синтаксис Automation

        ```
        VARIANT SelectP( double NominalDiameter);
        ```

        ### Входные параметры

        +-------------------------------------------------+
        | NominalDiameter | - номинальный диаметр резьбы. |
        +-------------------------------------------------+

        ### Возвращаемое значение

        Массив SafeAttray типа (VT_ARRAY | VT_R8)

        со списком значений шагов для заданного диаметра и текущего стандарта.
        """
        ...

    def SelectParameters(self, NominalDiameter, P):  # ithreadpattern_selectparameters.html
        """
        ## SelectParameters - Установить параметры резьбы для заданного диаметра и шага

        ### Синтаксис Automation

        ```
        BSTR SelectParameters( double NominalDiameter, double P, double * L, double * L1, double * L2 );
        ```

        ### Входные параметры

        +------------------------------------------+
        | NominalDiameter | - номинальный диаметр, |
        +------------------------------------------+
        | P               | - шаг резьбы.          |
        +------------------------------------------+

        ### Выходные параметры

        +-----------------------------------------------------------------+
        | L  | - рабочая длина резьбы,                                    |
        +-----------------------------------------------------------------+
        | L1 | - длина смещения основной плоскости для внешней резьбы,    |
        +-----------------------------------------------------------------+
        | L2 | - длина смещения основной плоскости для внутренней резьбы. |
        +-----------------------------------------------------------------+

        ### Возвращаемое значение

        Обозначение резьбы.
        """
        ...




class IThreads(IModelObjects):  # ithreads.html
    """
    ## Интерфейс IThreads

    Интерфейс коллекции условных обозначений резьбы.
    """

    def Add(self):  # ithreads_add.html
        """
        ## Add - Создает новый элемент и добавляет его в коллекцию

        ### Синтаксис Automation

        ```
        LPDISPATCH Add();
        ```

        ### Возвращаемое значение

        -указатель на интерфейс условного обозначения резьбы.
        """
        ...

    def Thread(self, Index) -> IThread:  # ithreads_thread.html
        """
        ## Thread - Возвращает элемент, заданный по индексу или по имени

        Тип данных: Указатель на интерфейс IThread

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        Thread = Object.Thread( Index )     # Получить свойство (* )
        Thread = Object.GetThread( Index )  # Получить свойство (**)
        ```
        """
        ...




class IThreadsParameters(IDispatch):  # ithreadsparameters.html
    """
    ## Интерфейс IThreadsParameters

    Интерфейс параметров резьбы.
    """

    @property
    def ConicalThreadAngle(self) -> float:  # ithreadsparameters_conicalthreadangle.html
        """
        ## ConicalThreadAngle - Угол уклона конической резьбы

        Тип данных: double

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        ConicalThreadAngle = Object.ConicalThreadAngle       # Получить свойство (* )
        ConicalThreadAngle = Object.GetConicalThreadAngle()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def Diameter(self) -> float:  # ithreadsparameters_diameter.html
        """
        ## Diameter - Номинальный диаметр резьбы

        Тип данных: double

        ### Синтаксис Automation

        ```
        Diameter = Object.Diameter       # Получить свойство (* )
        Object.Diameter = Diameter       # Установить свойство (* )
        Diameter = Object.GetDiameter()  # Получить свойство (**)
        Object.SetDiameter( Diameter )   # Установить свойство (**)
        ```
        """
        ...
    @Diameter.setter
    def Diameter(self, value: float): ...

    @property
    def Internal(self) -> bool:  # ithreadsparameters_internal.html
        """
        ## Internal - TRUE - внутренняя резьба, FALSE - внешняя

        Тип данных: BOOL

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        Internal = Object.Internal       # Получить свойство (* )
        Internal = Object.GetInternal()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def InternalDiameterCoefficient(self) -> float:  # ithreadsparameters_internaldiametercoefficient.html
        """
        ## InternalDiameterCoefficient - Коэффициент для расчета внутреннего диаметра резьбы

        Тип данных: double

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        InternalDiameterCoefficient = Object.InternalDiameterCoefficient       # Получить свойство (* )
        InternalDiameterCoefficient = Object.GetInternalDiameterCoefficient()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def Pitch(self) -> float:  # ithreadsparameters_pitch.html
        """
        ## Pitch - Шаг резьбы

        Тип данных: double

        ### Синтаксис Automation

        ```
        Pitch = Object.Pitch       # Получить свойство (* )
        Object.Pitch = Pitch       # Установить свойство (* )
        Pitch = Object.GetPitch()  # Получить свойство (**)
        Object.SetPitch( Pitch )   # Установить свойство (**)
        ```
        """
        ...
    @Pitch.setter
    def Pitch(self, value: float): ...

    @property
    def ThreadStandardFileName(self) -> str:  # ithreadsparameters_threadstandardfilename.html
        """
        ## ThreadStandardFileName - Имя файла с параметрами стандартной резьбы

        Тип данных: BSTR

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        ThreadStandardFileName = Object.ThreadStandardFileName       # Получить свойство (* )
        ThreadStandardFileName = Object.GetThreadStandardFileName()  # Получить свойство (**)
        ```
        """
        ...

    def Init(self, StandardFileName, Diameter, Pitch) -> bool:  # ithreadsparameters_init.html
        """
        ## Init - Инициализация параметров стандартной резьбы

        ### Синтаксис Automation

        ```
        BOOL Init( BSTR StandardFileName, double Diameter, double Pitch );
        ```

        ### Входные параметры

        +-----------------------------------------------------+
        | StandardFileName | - имя таблицы стандарта резьбы,  |
        +-----------------------------------------------------+
        | Diameter         | - номинальное значение диаметра, |
        +-----------------------------------------------------+
        | Pitch            | - шаг.                           |
        +-----------------------------------------------------+

        ### Возвращаемое значение

        +--------------------------+
        | TRUE | - в случае удачи. |
        +--------------------------+
        """
        ...




class IThreadDialogParam(IKompasAPIObject):  # ithreaddialogparam.html
    """
    ## Интерфейс IThreadDialogParam

    Параметры диалога выбора стандартной резьбы.

    Интерфейс можно получить с помощью метода:

    IApplicationDialogs::GetDialogParam, используя константу ksObjectThreadDialogParam.

    Интерфейс применяется в методе IApplicationDialogs::SelectThread.
    """

    @property
    def IsConic(self) -> bool:  # ithreaddialogparam_isconic.html
        """
        ## IsConic - Фильтрация типа резьбы. Коническая или цилиндрическая

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        IsConic = Object.IsConic ;      # Получить свойство (* )
        Object.IsConic = IsConic;       # Установить свойство (* )
        IsConic = Object.GetIsConic();  # Получить свойство (**)
        Object.SetIsConic( IsConic );   # Установить свойство (**)
        ```
        """
        ...
    @IsConic.setter
    def IsConic(self, value: bool): ...

    @property
    def NominalDiameter(self) -> float:  # ithreaddialogparam_nominaldiameter.html
        """
        ## NominalDiameter - D - номинальный диаметр резьбы (мм)

        Тип данных: double

        ### Синтаксис Automation

        ```
        NominalDiameter = Object.NominalDiameter;       # Получить свойство (* )
        Object.NominalDiameter = NominalDiameter;       # Установить свойство (* )
        NominalDiameter = Object.GetNominalDiameter();  # Получить свойство (**)
        Object.SetNominalDiameter( NominalDiameter );   # Установить свойство (**)
        ```
        """
        ...
    @NominalDiameter.setter
    def NominalDiameter(self, value: float): ...

    @property
    def P(self) -> float:  # ithreaddialogparam_p.html
        """
        ## P - P - шаг резьбы

        Тип данных: double

        ### Синтаксис Automation

        ```
        P = Object.P;       # Получить свойство (* )
        Object.P = P;       # Установить свойство (* )
        P = Object.GetP();  # Получить свойство (**)
        Object.SetP( P );   # Установить свойство (**)
        ```
        """
        ...
    @P.setter
    def P(self, value: float): ...

    @property
    def Standart(self) -> str:  # ithreaddialogparam_standart.html
        """
        ## Standart - Стандарт резьбы

        Тип данных: BSTR

        ### Синтаксис Automation

        ```
        Standart = Object.Standart ;      # Получить свойство (* )
        Object.Standart = Standart;       # Установить свойство (* )
        Standart = Object.GetStandart();  # Получить свойство (**)
        Object.SetStandart( Standart );   # Установить свойство (**)
        ```
        """
        ...
    @Standart.setter
    def Standart(self, value: str): ...




class IChooseBodies7(IDispatch):  # ichoosebodies7.html
    """
    ## Интерфейс IChooseBodies7

    Интерфейс области применения для тел документа в операции.

    Позволяет получить интерфейс области применения для тел документа в операции.

    Данный интерфейс можно получить у операции выдавливание IExtrusion и интерфейса твердого тела IBody7 посредством вызова метода IUnknown::QueryInterface (const GUID far& iid, void** pif);
    """

    @property
    def Bodies(self) -> typing.Any:  # ichoosebodies7_bodies.html
        """
        ## Bodies - Массив тел (SAFEARRAY)

        Тип данных: VARIANT

        Позволяет установить и получить массив тел (SAFEARRAY) в области применения.

        ### Синтаксис Automation

        ```
        Bodies = iObject.Bodies;       # Получить свойство (* )
        iObject.Bodies = Bodies;       # Установить свойство (* )
        Bodies = iObject.GetBodies();  # Получить свойство (**)
        iObject.SetBodies( Bodies );   # Установить свойство (**)
        ```
        """
        ...
    @Bodies.setter
    def Bodies(self, value: typing.Any): ...

    @property
    def ChooseBodiesType(self) -> int:  # ichoosebodies7_choosebodiestype.html
        """
        ## ChooseBodiesType - Тип действия над телами из перечисления ksChooseBodiesType

        Тип данных: из перечисления ksChooseBodiesType

        Позволяет установить и получить тип действия над телами.

        ### Синтаксис Automation

        ```
        ChooseBodiesType = iObject.ChooseBodiesType;       # Получить свойство (* )
        iObject.ChooseBodiesType = ChooseBodiesType;       # Установить свойство (* )
        ChooseBodiesType = iObject.GetChooseBodiesType();  # Получить свойство (**)
        iObject.SetChooseBodiesType( ChooseBodiesType );   # Установить свойство (**)
        ```
        """
        ...
    @ChooseBodiesType.setter
    def ChooseBodiesType(self, value: int): ...




class IExtrusion1(IDispatch):  # iextrusion1.html
    """
    ## Интерфейс IExtrusion1

    Интерфейс параметров элемента выдавливания.
    """

    @property
    def DirectionObject(self) -> IModelObject:  # iextrusion1_directionobject.html
        """
        ## DirectionObject - Направляющий объект для элемента выдавливания

        Тип данных: указатель на интерфейс IModelObject

        ### Синтаксис Automation

        ```
        DirectionObject = Object.DirectionObject       # Получить свойство (* )
        Object.DirectionObject = DirectionObject       # Установить свойство (* )
        DirectionObject = Object.GetDirectionObject()  # Получить свойство (**)
        Object.SetDirectionObject( DirectionObject )   # Установить свойство (**)
        ```
        """
        ...
    @DirectionObject.setter
    def DirectionObject(self, value: IModelObject): ...

    @property
    def OperationResult(self) -> int:  # iextrusion1_operationresult.html
        """
        ## OperationResult - Результат операции

        Тип данных: Значение из перечисления ksOperationResultEnum.

        ### Синтаксис Automation

        ```
        OperationResult = Object.OperationResult       # Получить свойство (* )
        Object.OperationResult = OperationResult       # Установить свойство (* )
        OperationResult = Object.GetOperationResult()  # Получить свойство (**)
        Object.SetOperationResult( OperationResult )   # Установить свойство (**)
        ```
        """
        ...
    @OperationResult.setter
    def OperationResult(self, value: int): ...

    @property
    def Profile(self) -> IModelObject:  # iextrusion1_profile.html
        """
        ## Profile - Сечение элемента выдавливания

        Тип данных: указатель на интерфейс IModelObject

        ### Синтаксис Automation

        ```
        Profile = Object.Profile       # Получить свойство (* )
        Object.Profile = Profile       # Установить свойство (* )
        Profile = Object.GetProfile()  # Получить свойство (**)
        Object.SetProfile( Profile )   # Установить свойство (**)
        ```
        """
        ...
    @Profile.setter
    def Profile(self, value: IModelObject): ...

    @property
    def Profiles(self) -> typing.Any:  # iextrusion1_profiles.html
        """
        ## Profiles - Список сечений

        Тип данных: VARIANT

        ### Синтаксис Automation

        ```
        Profiles = Object.Profiles       # Получить свойство (* )
        Object.Profiles = Profiles       # Установить свойство (* )
        Profiles = Object.GetProfiles()  # Получить свойство (**)
        Object.SetProfiles( Profiles )   # Установить свойство (**)
        ```
        """
        ...
    @Profiles.setter
    def Profiles(self, value: typing.Any): ...

    @property
    def Vector3D(self) -> IVector3D:  # iextrusion1_vector3d.html
        """
        ## Vector3D - Параметры вектора

        Тип данных: указатель на интерфейс IVector3D.

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        Vector3D = Object.Vector3D       # Получить свойство (* )
        Vector3D = Object.GetVector3D()  # Получить свойство (**)
        ```
        """
        ...

    def CutOffByPoint(self, Normal) -> bool:  # iextrusion1_cutoffbypoint.html
        """
        ## CutOffByPoint - Флаг «отсекать» для способа выдавливания До точки

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        CutOffByPoint( Normal ) = Object.CutOffByPoint     # Получить свойство (* )
        Object.CutOffByPoint( Normal ) = CutOffByPoint     # Установить свойство (* )
        CutOffByPoint = Object.GetCutOffByPoint( Normal )  # Получить свойство (**)
        Object.SetCutOffByPoint( Normal, CutOffByPoint )   # Установить свойство (**)
        ```
        """
        ...

    def SetCutOffByPoint(self, Normal, arg1):
        ...




class IThinParameters(IDispatch):  # ithinparameters.html
    """
    ## Интерфейс IThinParameters

    Интерфейс параметров тонкой стенки.

    IThinParameters является дополнительным интерфейсом операции выдавливание. Данный интерфейс можно получить у операции выдавливание IExtrusion посредством вызова метода IUnknown::QueryInterface (const GUID far& iid, void** pif);
    """

    @property
    def Thin(self) -> bool:  # ithinparameters_thin.html
        """
        ## Thin - Признак формирования тонкой стенки

        Тип данных: BOOL

        Позволяет установить и получить признак формирования тонкой стенки.

        ### Синтаксис Automation

        ```
        Thin = iObject.Thin;       # Получить свойство (* )
        iObject.Thin = Thin;       # Установить свойство (* )
        Thin = iObject.GetThin();  # Получить свойство (**)
        iObject.SetThin( Thin );   # Установить свойство (**)
        ```
        """
        ...
    @Thin.setter
    def Thin(self, value: bool): ...

    @property
    def ThinType(self) -> int:  # ithinparameters_thintype.html
        """
        ## ThinType - Направление формирования тонкой стенки

        Тип данных: из перечисления ksDirectionTypeEnum

        Позволяет установить и получить направление формирования тонкой стенки.

        ### Синтаксис Automation

        ```
        ThinType = iObject.ThinType;       # Получить свойство (* )
        iObject.ThinType = ThinType;       # Установить свойство (* )
        ThinType = iObject.GetThinType();  # Получить свойство (**)
        iObject.SetThinType( ThinType );   # Установить свойство (**)
        ```
        """
        ...
    @ThinType.setter
    def ThinType(self, value: int): ...

    def GetThinParameters(self) -> bool:  # ithinparameters_getthinparameters.html
        """
        ## GetThinParameters - Получить параметры тонкой стенки

        Метод позволяет получать параметры тонкой стенки.

        ### Синтаксис Automation

        ```
        BOOL GetThinParameters( BOOL * Thin,
        Direction_Type * ThinType,
        double * ThicknessNormal,
        double * ThicknessReverse
        ```

        ### Выходные параметры

        +--------------------------------------------------------------+
        | Thin             | - признак формирования тонкой стенки,     |
        +--------------------------------------------------------------+
        | ThinType         | - направление формирования тонкой стенки, |
        +--------------------------------------------------------------+
        | ThicknessNormal  | - толщина стенки в направлении наружу,    |
        +--------------------------------------------------------------+
        | ThicknessReverse | - толщина стенки в направлении внутрь.    |
        +--------------------------------------------------------------+

        ### Возвращаемое значение

        +-----------------------------------------+
        | TRUE  | - в случае удачного завершения, |
        +-----------------------------------------+
        | FALSE | - в случае неудачи.             |
        +-----------------------------------------+
        """
        ...

    def SetThickness(self, Normal, arg1):
        ...

    def SetThinParameters(self, Thin, ThinType, ThicknessNormal, ThicknessReverse) -> bool:  # ithinparameters_setthinparameters.html
        """
        ## SetThinParameters - Установить параметры тонкой стенки

        Метод позволяет установить параметры тонкой стенки.

        ### Синтаксис Automation

        ```
        BOOL SetThinParameters( BOOL * Thin,
        Direction_Type * ThinType,
        double * ThicknessNormal,
        double * ThicknessReverse
        ```

        ### Выходные параметры

        +--------------------------------------------------------------+
        | Thin             | - признак формирования тонкой стенки,     |
        +--------------------------------------------------------------+
        | ThinType         | - направление формирования тонкой стенки, |
        +--------------------------------------------------------------+
        | ThicknessNormal  | - толщина стенки в направлении наружу,    |
        +--------------------------------------------------------------+
        | ThicknessReverse | - толщина стенки в направлении внутрь.    |
        +--------------------------------------------------------------+

        ### Возвращаемое значение

        +-----------------------------------------+
        | TRUE  | - в случае удачного завершения, |
        +-----------------------------------------+
        | FALSE | - в случае неудачи.             |
        +-----------------------------------------+
        """
        ...

    def Thickness(self, Normal) -> float:  # ithinparameters_thickness.html
        """
        ## Thickness - Толщина стенки

        Тип данных: double

        Позволяет установить и получить толщину стенки.

        ### Синтаксис Automation

        ```
        Thickness = iObject.Thickness( normal );     # Получить свойство (* )
        iObject.Thickness( normal ) = Thickness;     # Установить свойство (* )
        Thickness = iObject.GetThickness( normal );  # Получить свойство (**)
        iObject.SetThickness( normal, Thickness );   # Установить свойство (**)
        ```

        ### Входные параметры

        +-------------------------------------------------------------------------------------------------------+
        | normal | - TRUE - толщина стенки в направлении наружу, - FALSE - толщина стенки в направлении внутрь. |
        +-------------------------------------------------------------------------------------------------------+
        """
        ...




class IHole3D(IModelObject):  # ihole3d.html
    """
    ## Интерфейс IHole3D

    Интерфейс отверстия.

    Интерфейс можно получить с помощью методов:

    IHoles3D::Hole3D

    IHoles3D::Add

    Интерфейс отверстия имеет также дополнительный интерфейс

    IHoleDisposal- Размещение

    С помощью метода Unknown::QueryInterface у интерфейса можно получить интерфейс подчиненного объекта ЛСК - ILocalCSObject.
    """

    @property
    def Axis(self) -> bool:  # ihole3d_axis.html
        """
        ## Axis - Создавать ось

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        Axis = Object.Axis       # Получить свойство (* )
        Object.Axis = Axis       # Установить свойство (* )
        Axis = Object.GetAxis()  # Получить свойство (**)
        Object.SetAxis( Axis )   # Установить свойство (**)
        ```
        """
        ...
    @Axis.setter
    def Axis(self, value: bool): ...

    @property
    def Depth(self) -> float:  # ihole3d_depth.html
        """
        ## Depth - Глубина отверстия

        Тип данных: double

        ### Синтаксис Automation

        ```
        Depth = Object.Depth       # Получить свойство (* )
        Object.Depth = Depth       # Установить свойство (* )
        Depth = Object.GetDepth()  # Получить свойство (**)
        Object.SetDepth( Depth )   # Установить свойство (**)
        ```
        """
        ...
    @Depth.setter
    def Depth(self, value: float): ...

    @property
    def DepthFace(self) -> IModelObject:  # ihole3d_depthface.html
        """
        ## DepthFace - Поверхность для определения глубины отверстия

        Тип данных: Указатель на интерфейс IModelObject

        ### Синтаксис Automation

        ```
        DepthFace = Object.DepthFace       # Получить свойство (* )
        Object.DepthFace = DepthFace       # Установить свойство (* )
        DepthFace = Object.GetDepthFace()  # Получить свойство (**)
        Object.SetDepthFace( DepthFace )   # Установить свойство (**)
        ```
        """
        ...
    @DepthFace.setter
    def DepthFace(self, value: IModelObject): ...

    @property
    def DepthType(self) -> int:  # ihole3d_depthtype.html
        """
        ## DepthType - Способ определения глубины отверстия

        Тип данных: из перечисления ksDepthTypeEnum

        ### Синтаксис Automation

        ```
        DepthType = Object.DepthType       # Получить свойство (* )
        Object.DepthType = DepthType       # Установить свойство (* )
        DepthType = Object.GetDepthType()  # Получить свойство (**)
        Object.SetDepthType( DepthType )   # Установить свойство (**)
        ```
        """
        ...
    @DepthType.setter
    def DepthType(self, value: int): ...

    @property
    def DepthVertex(self) -> IModelObject:  # ihole3d_depthvertex.html
        """
        ## DepthVertex - Вершина или точка для определения глубины отверстия

        Тип данных: Указатель на интерфейс IModelObject

        ### Синтаксис Automation

        ```
        DepthVertex = Object.DepthVertex       # Получить свойство (* )
        Object.DepthVertex = DepthVertex       # Установить свойство (* )
        DepthVertex = Object.GetDepthVertex()  # Получить свойство (**)
        Object.SetDepthVertex( DepthVertex )   # Установить свойство (**)
        ```
        """
        ...
    @DepthVertex.setter
    def DepthVertex(self, value: IModelObject): ...

    @property
    def Diameter(self) -> float:  # ihole3d_diameter.html
        """
        ## Diameter - Диаметр отверстия

        Тип данных: double

        ### Синтаксис Automation

        ```
        Diameter = Object.Diameter       # Получить свойство (* )
        Object.Diameter = Diameter       # Установить свойство (* )
        Diameter = Object.GetDiameter()  # Получить свойство (**)
        Object.SetDiameter( Diameter )   # Установить свойство (**)
        ```
        """
        ...
    @Diameter.setter
    def Diameter(self, value: float): ...

    @property
    def EndFaceAngle(self) -> float:  # ihole3d_endfaceangle.html
        """
        ## EndFaceAngle - Угол конуса торца

        Тип данных: double

        ### Синтаксис Automation

        ```
        EndFaceAngle = Object.EndFaceAngle       # Получить свойство (* )
        Object.EndFaceAngle = EndFaceAngle       # Установить свойство (* )
        EndFaceAngle = Object.GetEndFaceAngle()  # Получить свойство (**)
        Object.SetEndFaceAngle( EndFaceAngle )   # Установить свойство (**)
        ```
        """
        ...
    @EndFaceAngle.setter
    def EndFaceAngle(self, value: float): ...

    @property
    def EndFaceType(self) -> int:  # ihole3d_endfacetype.html
        """
        ## EndFaceType - Форма торца

        Тип данных: из перечисления ksEndFaceTypeEnum

        ### Синтаксис Automation

        ```
        EndFaceType = Object.EndFaceType       # Получить свойство (* )
        Object.EndFaceType = EndFaceType       # Установить свойство (* )
        EndFaceType = Object.GetEndFaceType()  # Получить свойство (**)
        Object.SetEndFaceType( EndFaceType )   # Установить свойство (**)
        ```
        """
        ...
    @EndFaceType.setter
    def EndFaceType(self, value: int): ...

    @property
    def HoleParameters(self) -> IKompasAPIObject:  # ihole3d_holeparameters.html
        """
        ## HoleParameters - Параметры отверстия

        Тип данных: Указатель на интерфейс IKompasAPIObject

        Свойство доступно только для чтения.

        В зависимости от типа отверстия возвращаются интерфейсы

        •ksHTCounterbore - ISpotfacingHoleParameters

        •ksHTCountersinking - ICountersinkHoleParameters

        •ksHTCounterdrill - ICountersinkSpotfacingHoleParameters

        •ksHTConic - IConicHoleParameters

        •ksHTLfrLibrary - ILibraryHoleParameters

        ### Синтаксис Automation

        ```
        HoleParameters = Object.HoleParameters        # Получить свойство (* )
        THoleParameters = Object.GetHoleParameters()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def HoleType(self) -> int:  # ihole3d_holetype.html
        """
        ## HoleType - Тип отверстия

        Тип данных: из перечисления ksHoleTypeEnum

        ### Синтаксис Automation

        ```
        HoleType = Object.HoleType       # Получить свойство (* )
        Object.HoleType = HoleType       # Установить свойство (* )
        HoleType = Object.GetHoleType()  # Получить свойство (**)
        Object.SetHoleType( HoleType )   # Установить свойство (**)
        ```
        """
        ...
    @HoleType.setter
    def HoleType(self, value: int): ...

    @property
    def ShowThread(self) -> bool:  # ihole3d_showthread.html
        """
        ## ShowThread - Показывать резьбу

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        ShowThread = Object.ShowThread       # Получить свойство (* )
        Object.ShowThread = ShowThread       # Установить свойство (* )
        ShowThread = Object.GetShowThread()  # Получить свойство (**)
        Object.SetShowThread( ShowThread )   # Установить свойство (**)
        ```
        """
        ...
    @ShowThread.setter
    def ShowThread(self, value: bool): ...

    @property
    def Thread(self) -> IThread:  # ihole3d_thread.html
        """
        ## Thread - Условное обозначение резьбы

        Тип данных: Указатель на интерфейс IThread

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        Thread = Object.Thread       # Получить свойство (* )
        Thread = Object.GetThread()  # Получить свойство (**)
        ```
        """
        ...




class ICountersinkHoleParameters(IKompasAPIObject):  # icountersinkholeparameters.html
    """
    ## Интерфейс ICountersinkHoleParameters

    Интерфейс отверстия с зенковкой.
    """

    @property
    def CountersinkAngle(self) -> float:  # icountersinkholeparameters_countersinkangle.html
        """
        ## CountersinkAngle - Угол конуса зенковки

        Тип данных: double

        ### Синтаксис Automation

        ```
        CountersinkAngle = Object.CountersinkAngle       # Получить свойство (* )
        Object.CountersinkAngle = CountersinkAngle       # Установить свойство (* )
        CountersinkAngle = Object.GetCountersinkAngle()  # Получить свойство (**)
        Object.SetCountersinkAngle( CountersinkAngle )   # Установить свойство (**)
        ```
        """
        ...
    @CountersinkAngle.setter
    def CountersinkAngle(self, value: float): ...

    @property
    def CountersinkDepth(self) -> float:  # icountersinkholeparameters_countersinkdepth.html
        """
        ## CountersinkDepth - Глубина зенковки

        Тип данных: double

        ### Синтаксис Automation

        ```
        CountersinkDepth = Object.CountersinkDepth       # Получить свойство (* )
        Object.CountersinkDepth = CountersinkDepth       # Установить свойство (* )
        CountersinkDepth = Object.GetCountersinkDepth()  # Получить свойство (**)
        Object.SetCountersinkDepth( CountersinkDepth )   # Установить свойство (**)
        ```
        """
        ...
    @CountersinkDepth.setter
    def CountersinkDepth(self, value: float): ...

    @property
    def CountersinkDiameter(self) -> float:  # icountersinkholeparameters_countersinkdiameter.html
        """
        ## CountersinkDiameter - Диаметр зенковки

        Тип данных: double

        ### Синтаксис Automation

        ```
        CountersinkDiameter = Object.CountersinkDiameter       # Получить свойство (* )
        Object.CountersinkDiameter = CountersinkDiameter       # Установить свойство (* )
        CountersinkDiameter = Object.GetCountersinkDiameter()  # Получить свойство (**)
        Object.SetCountersinkDiameter( CountersinkDiameter )   # Установить свойство (**)
        ```
        """
        ...
    @CountersinkDiameter.setter
    def CountersinkDiameter(self, value: float): ...

    @property
    def CountersinkType(self) -> int:  # icountersinkholeparameters_countersinktype.html
        """
        ## CountersinkType - Способ определения параметров зенковки

        Тип данных: из перечисления ksCountersinkTypeEnum

        ### Синтаксис Automation

        ```
        CountersinkType = Object.CountersinkType       # Получить свойство (* )
        Object.CountersinkType = CountersinkType       # Установить свойство (* )
        CountersinkType = Object.GetCountersinkType()  # Получить свойство (**)
        Object.SetCountersinkType( CountersinkType )   # Установить свойство (**)
        ```
        """
        ...
    @CountersinkType.setter
    def CountersinkType(self, value: int): ...




class ICountersinkSpotfacingHoleParameters(IKompasAPIObject):  # icountersinkspotfacingholeparameters.html
    """
    ## Интерфейс ICountersinkSpotfacingHoleParameters

    Интерфейс отверстия с зенковкой и цековкой.
    """

    @property
    def CountersinkAngle(self) -> float:  # icountersinkspotfacingholeparameters_countersinkangle.html
        """
        ## CountersinkAngle - Угол конуса зенковки

        Тип данных: double

        ### Синтаксис Automation

        ```
        CountersinkAngle = Object.CountersinkAngle       # Получить свойство (* )
        Object.CountersinkAngle = CountersinkAngle       # Установить свойство (* )
        CountersinkAngle = Object.GetCountersinkAngle()  # Получить свойство (**)
        Object.SetCountersinkAngle( CountersinkAngle )   # Установить свойство (**)
        ```
        """
        ...
    @CountersinkAngle.setter
    def CountersinkAngle(self, value: float): ...

    @property
    def SpotfacingCountersinkDiameter(self) -> float:  # icountersinkspotfacingholeparameters_spotfacingcountersinkdiameter.html
        """
        ## SpotfacingCountersinkDiameter - Диаметр цековки/зенковки

        Тип данных: double

        ### Синтаксис Automation

        ```
        SpotfacingCountersinkDiameter = Object.SpotfacingCountersinkDiameter       # Получить свойство (* )
        Object.SpotfacingCountersinkDiameter = SpotfacingCountersinkDiameter       # Установить свойство (* )
        SpotfacingCountersinkDiameter = Object.GetSpotfacingCountersinkDiameter()  # Получить свойство (**)
        Object.SetSpotfacingCountersinkDiameter( SpotfacingCountersinkDiameter )   # Установить свойство (**)
        ```
        """
        ...
    @SpotfacingCountersinkDiameter.setter
    def SpotfacingCountersinkDiameter(self, value: float): ...

    @property
    def SpotfacingDepth(self) -> float:  # icountersinkspotfacingholeparameters_spotfacingdepth.html
        """
        ## SpotfacingDepth - Глубина цековки

        Тип данных: double

        ### Синтаксис Automation

        ```
        SpotfacingDepth = Object.SpotfacingDepth       # Получить свойство (* )
        Object.SpotfacingDepth = SpotfacingDepth       # Установить свойство (* )
        SpotfacingDepth = Object.GetSpotfacingDepth()  # Получить свойство (**)
        Object.SetSpotfacingDepth( SpotfacingDepth )   # Установить свойство (**)
        ```
        """
        ...
    @SpotfacingDepth.setter
    def SpotfacingDepth(self, value: float): ...




class IConicHoleParameters(IKompasAPIObject):  # iconicholeparameters.html
    """
    ## Интерфейс IConicHoleParameters

    Интерфейс конического отверстия.
    """

    @property
    def ConicAngle(self) -> float:  # iconicholeparameters_conicangle.html
        """
        ## ConicAngle - Угол конуса

        Тип данных: double

        ### Синтаксис Automation

        ```
        ConicAngle = Object.ConicAngle      # Получить свойство (* )
        Object.ConicAngle = ConicAngle      # Установить свойство (* )
        ConicAngle = Object.GetConicAngle(  # Получить свойство (**)
        Object.SetConicAngle( ConicAngle )  # Установить свойство (**)
        ```
        """
        ...
    @ConicAngle.setter
    def ConicAngle(self, value: float): ...

    @property
    def ConicDiameter(self) -> float:  # iconicholeparameters_conicdiameter.html
        """
        ## ConicDiameter - Диаметр основания конуса

        Тип данных: double

        ### Синтаксис Automation

        ```
        ConicDiameter = Object.ConicDiameter       # Получить свойство (* )
        Object.ConicDiameter = ConicDiameter       # Установить свойство (* )
        ConicDiameter = Object.GetConicDiameter()  # Получить свойство (**)
        Object.SetConicDiameter( ConicDiameter )   # Установить свойство (**)
        ```
        """
        ...
    @ConicDiameter.setter
    def ConicDiameter(self, value: float): ...

    @property
    def ConicType(self) -> int:  # iconicholeparameters_conictype.html
        """
        ## ConicType - Способ определения параметров конического отверстия

        Тип данных: из перечисления ksConicTypeEnum

        ### Синтаксис Automation

        ```
        ConicType = Object.ConicType       # Получить свойство (* )
        Object.ConicType = ConicType       # Установить свойство (* )
        ConicType = Object.GetConicType()  # Получить свойство (**)
        Object.SetConicType( ConicType )   # Установить свойство (**)
        ```
        """
        ...
    @ConicType.setter
    def ConicType(self, value: int): ...




class ISpotfacingHoleParameters(IKompasAPIObject):  # ispotfacingholeparameters.html
    """
    ## Интерфейс ISpotfacingHoleParameters

    Интерфейс отверстия с цековкой.
    """

    @property
    def SpotfacingDepth(self) -> float:  # ispotfacingholeparameters_spotfacingdepth.html
        """
        ## SpotfacingDepth - Глубина цековки

        Тип данных: double

        ### Синтаксис Automation

        ```
        SpotfacingDepth = Object.SpotfacingDepth       # Получить свойство (* )
        Object.SpotfacingDepth = SpotfacingDepth       # Установить свойство (* )
        SpotfacingDepth = Object.GetSpotfacingDepth()  # Получить свойство (**)
        Object.SetSpotfacingDepth( SpotfacingDepth )   # Установить свойство (**)
        ```
        """
        ...
    @SpotfacingDepth.setter
    def SpotfacingDepth(self, value: float): ...

    @property
    def SpotfacingDiameter(self) -> float:  # ispotfacingholeparameters_spotfacingdiameter.html
        """
        ## SpotfacingDiameter - Диаметр цековки

        Тип данных: double

        ### Синтаксис Automation

        ```
        SpotfacingDiameter = Object.SpotfacingDiameter       # Получить свойство (* )
        Object.SpotfacingDiameter = SpotfacingDiameter       # Установить свойство (* )
        SpotfacingDiameter = Object.GetSpotfacingDiameter()  # Получить свойство (**)
        Object.SetSpotfacingDiameter( SpotfacingDiameter )   # Установить свойство (**)
        ```
        """
        ...
    @SpotfacingDiameter.setter
    def SpotfacingDiameter(self, value: float): ...




class ILibraryHoleParameters(IKompasAPIObject):  # ilibraryholeparameters.html
    """
    ## Интерфейс ILibraryHoleParameters

    Интерфейс параметров отверстия из библиотеки.
    """

    @property
    def FileName(self) -> str:  # ilibraryholeparameters_filename.html
        """
        ## FileName - Форма. Путь к библиотеке и имя фрагмента в библиотеке документов

        Тип данных: BSTR

        ### Синтаксис Automation

        ```
        FileName = Object.FileName       # Получить свойство (* )
        Object.FileName = FileName       # Установить свойство (* )
        FileName = Object.GetFileName()  # Получить свойство (**)
        Object.SetFileName( FileName )   # Установить свойство (**)
        ```
        """
        ...
    @FileName.setter
    def FileName(self, value: str): ...

    @property
    def Variables(self) -> typing.Any:  # ilibraryholeparameters_variables.html
        """
        ## Variables - Получить массив параметрических переменных в виде SAFEARRAY VT_DISPATCH

        Тип данных: VARIANT

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        Variables = Object.Variables       # Получить свойство (* )
        Variables = Object.GetVariables()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def VariablesCount(self) -> int:  # ilibraryholeparameters_variablescount.html
        """
        ## VariablesCount - Получить количество параметрических переменных

        Тип данных: long

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        VariablesCount = Object.VariablesCount       # Получить свойство (* )
        VariablesCount = Object.GetVariablesCount()  # Получить свойство (**)
        ```
        """
        ...

    def Variable(self, Index) -> IVariable7:  # ilibraryholeparameters_variable.html
        """
        ## Variable - Получить параметрическую переменную по имени или индексу

        Тип данных: Указатель на интерфейс IVariable7.

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        Variable = Object.Variable( Index )     # Получить свойство (* )
        Variable = Object.GetVariable( Index )  # Получить свойство (**)
        ```
        """
        ...




class IHoleDisposal(IDispatch):  # iholedisposal.html
    """
    ## Интерфейс IHoleDisposal

    Интерфейс размещения отверстия.
    """

    @property
    def AssociationVertex(self) -> IModelObject:  # iholedisposal_associationvertex.html
        """
        ## AssociationVertex - Точечный объект

        Тип данных: Указатель на интерфейс IModelObject

        ### Синтаксис Automation

        ```
        AssociationVertex = Object.AssociationVertex       # Получить свойство (* )
        Object.AssociationVertex = AssociationVertex       # Установить свойство (* )
        AssociationVertex = Object.GetAssociationVertex()  # Получить свойство (**)
        Object.SetAssociationVertex( AssociationVertex )   # Установить свойство (**)
        ```
        """
        ...
    @AssociationVertex.setter
    def AssociationVertex(self, value: IModelObject): ...

    @property
    def BaseSurface(self) -> IModelObject:  # iholedisposal_basesurface.html
        """
        ## BaseSurface - Поверхность, на которой требуется построить точку

        Тип данных: Указатель на интерфейс IModelObject

        ### Синтаксис Automation

        ```
        BaseSurface = Object.BaseSurface       # Получить свойство (* )
        Object.BaseSurface = BaseSurface       # Установить свойство (* )
        BaseSurface = Object.GetBaseSurface()  # Получить свойство (**)
        Object.SetBaseSurface( BaseSurface )   # Установить свойство (**)
        ```
        """
        ...
    @BaseSurface.setter
    def BaseSurface(self, value: IModelObject): ...

    @property
    def Direction(self) -> bool:  # iholedisposal_direction.html
        """
        ## Direction - Направление построения

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        Direction = Object.Direction       # Получить свойство (* )
        Object.Direction = Direction       # Установить свойство (* )
        Direction = Object.GetDirection()  # Получить свойство (**)
        Object.SetDirection( Direction )   # Установить свойство (**)
        ```
        """
        ...
    @Direction.setter
    def Direction(self, value: bool): ...

    @property
    def DirectionObject(self) -> IModelObject:  # iholedisposal_directionobject.html
        """
        ## DirectionObject - Направляющий объект

        Тип данных: Указатель на интерфейс IModelObject

        ### Синтаксис Automation

        ```
        DirectionObject = Object.DirectionObject       # Получить свойство (* )
        Object.DirectionObject = DirectionObject       # Установить свойство (* )
        DirectionObject = Object.GetDirectionObject()  # Получить свойство (**)
        Object.SetDirectionObject( DirectionObject )   # Установить свойство (**)
        ```
        """
        ...
    @DirectionObject.setter
    def DirectionObject(self, value: IModelObject): ...

    @property
    def OffsetType(self) -> int:  # iholedisposal_offsettype.html
        """
        ## OffsetType - Тип смещения

        Тип данных: из перечисления ksPoint3DSurfaceParamTypeEnum

        ### Синтаксис Automation

        ```
        OffsetType = Object.OffsetType       # Получить свойство (* )
        Object.OffsetType = OffsetType       # Установить свойство (* )
        OffsetType = Object.GetOffsetType()  # Получить свойство (**)
        OffsetType = Object.GetOffsetType()  # Установить свойство (**)
        ```
        """
        ...
    @OffsetType.setter
    def OffsetType(self, value: int): ...

    @property
    def Perpendicular(self) -> bool:  # iholedisposal_perpendicular.html
        """
        ## Perpendicular - Перпендикулярно поверхности

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        Perpendicular = Object.Perpendicular       # Получить свойство (* )
        Object.Perpendicular = Perpendicular       # Установить свойство (* )
        Perpendicular = Object.GetPerpendicular()  # Получить свойство (**)
        Object.SetPerpendicular( Perpendicular )   # Установить свойство (**)
        ```
        """
        ...
    @Perpendicular.setter
    def Perpendicular(self, value: bool): ...

    @property
    def Point3DParamSurface(self) -> IKompasAPIObject:  # iholedisposal_point3dparamsurface.html
        """
        ## Point3DParamSurface - Параметры пространственной точки на поверхности

        Тип данных: Указатель на интерфейс IKompasAPIObject

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        Point3DParamSurface = Object.Point3DParamSurface       # Получить свойство (* )
        Point3DParamSurface = Object.GetPoint3DParamSurface()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def ProcessCanopy(self) -> bool:  # iholedisposal_processcanopy.html
        """
        ## ProcessCanopy - Обрабатывать навес

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        ProcessCanopy = Object.ProcessCanopy       # Получить свойство (* )
        Object.ProcessCanopy = ProcessCanopy       # Установить свойство (* )
        ProcessCanopy = Object.GetProcessCanopy()  # Получить свойство (**)
        Object.SetProcessCanopy( ProcessCanopy )   # Установить свойство (**)
        ```
        """
        ...
    @ProcessCanopy.setter
    def ProcessCanopy(self, value: bool): ...

    @property
    def Vector(self) -> IVector3D:  # iholedisposal_vector.html
        """
        ## Vector - Вектор

        Тип данных: Указатель на интерфейс IVector3D

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        Vector = Object.Vector       # Получить свойство (* )
        Vector = Object.GetVector()  # Получить свойство (**)
        ```
        """
        ...




class IChamfer(IModelObject):  # ichamfer.html
    """
    ## Интерфейс IChamfer

    Интерфейс операции Фаска.
    """

    @property
    def Angle(self) -> float:  # ichamfer_angle.html
        """
        ## Angle - Угол фаски

        Тип данных: double

        ### Синтаксис Automation

        ```
        Angle = Object.Angle       # Получить свойство (* )
        Object.Angle = Angle       # Установить свойство (* )
        Angle = Object.GetAngle()  # Получить свойство (**)
        Object.SetAngle( Angle )   # Установить свойство (**)
        ```
        """
        ...
    @Angle.setter
    def Angle(self, value: float): ...

    @property
    def BaseObjects(self) -> typing.Any:  # ichamfer_baseobjects.html
        """
        ## BaseObjects - Массив ребер и граней, на которых строится фаска

        Тип данных: VARIANT

        Позволяет получать и устанавливать опорный объект или массив опорных объектов, участвующих в операции фаска.

        Массив объектов возвращается в виде массива SAFEARRAY объектов LPDISPATCH (VT_ARRAY | VT_DISPATCH).

        ### Синтаксис Automation

        ```
        BaseObjects = Object.BaseObjects       # Получить свойство (* )
        Object.BaseObjects = BaseObjects       # Установить свойство (* )
        BaseObjects = Object.GetBaseObjects()  # Получить свойство (**)
        Object.SetBaseObjects( BaseObjects )   # Установить свойство (**)
        ```
        """
        ...
    @BaseObjects.setter
    def BaseObjects(self, value: typing.Any): ...

    @property
    def BuildingType(self) -> int:  # ichamfer_buildingtype.html
        """
        ## BuildingType - Способ построения фаски

        Тип данных: Значение из перечисления ksChamferBuildingTypeEnum

        ### Синтаксис Automation

        ```
        BuildingType = Object.BuildingType        # Получить свойство (* )
        Object.BuildingType = BuildingType        # Установить свойство (* )
        BuildingType = Object.GetBuildingType())  # Получить свойство (**)
        Object.SetBuildingType( BuildingType )    # Установить свойство (**)
        ```
        """
        ...
    @BuildingType.setter
    def BuildingType(self, value: int): ...

    @property
    def CornerForm(self) -> int:  # ichamfer_cornerform.html
        """
        ## CornerForm - Способы обработки углов стыковки

        Тип данных: из перечисления ksCornerFormEnum

        ### Синтаксис Automation

        ```
        CornerForm = Object.CornerForm       # Получить свойство (* )
        Object.CornerForm = CornerForm       # Установить свойство (* )
        CornerForm = Object.GetCornerForm()  # Получить свойство (**)
        Object.SetCornerForm( CornerForm )   # Установить свойство (**)
        ```
        """
        ...
    @CornerForm.setter
    def CornerForm(self, value: int): ...

    @property
    def Direction(self) -> bool:  # ichamfer_direction.html
        """
        ## Direction - Направление фаски

        Тип данных: BOOL

        Значение свойства:

        +-------------------+
        | TRUE  | - первое, |
        +-------------------+
        | FALSE | - второе. |
        +-------------------+

        ### Синтаксис Automation

        ```
        Direction = Object.Direction       # Получить свойство (* )
        Object.Direction = Direction       # Установить свойство (* )
        Direction = Object.GetDirection()  # Получить свойство (**)
        Object.SetDirection( Direction )   # Установить свойство (**)
        ```
        """
        ...
    @Direction.setter
    def Direction(self, value: bool): ...

    @property
    def Distance1(self) -> float:  # ichamfer_distance1.html
        """
        ## Distance1 - Длина первой стороны

        Тип данных: double

        ### Синтаксис Automation

        ```
        Distance1 = Object.Distance1       # Получить свойство (* )
        Object.Distance1 = Distance1       # Установить свойство (* )
        Distance1 = Object.GetDistance1()  # Получить свойство (**)
        Object.SetDistance1( Distance1 )   # Установить свойство (**)
        ```
        """
        ...
    @Distance1.setter
    def Distance1(self, value: float): ...

    @property
    def Distance2(self) -> float:  # ichamfer_distance2.html
        """
        ## Distance2 - Длина второй стороны

        Тип данных: double

        ### Синтаксис Automation

        ```
        Distance2 = Object.Distance2       # Получить свойство (* )
        Object.Distance2 = Distance2       # Установить свойство (* )
        Distance2 = Object.GetDistance2()  # Получить свойство (**)
        Object.SetDistance2( Distance2 )   # Установить свойство (**)
        ```
        """
        ...
    @Distance2.setter
    def Distance2(self, value: float): ...

    @property
    def StopChamferDirection(self):  # ichamfer_stopchamferdirection.html
        """
        ## StopChamferDirection - Направление усечения в точке останова (false - направление от начальной точки до конечной)

        Тип данных: BOO

        ### Синтаксис Automation

        ```
        StopChamferDirection = Object.StopChamferDirection       # Получить свойство (* )
        Object.StopChamferDirection = StopChamferDirection       # Установить свойство (* )
        StopChamferDirection = Object.GetStopChamferDirection()  # Получить свойство (**)
        Object.SetStopChamferDirection( StopChamferDirection )   # Установить свойство (**)
        ```
        """
        ...
    @StopChamferDirection.setter
    def StopChamferDirection(self, value): ...

    @property
    def Tangent(self) -> bool:  # ichamfer_tangent.html
        """
        ## Tangent - По касательным ребрам

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        Tangent = Object.Tangent       # Получить свойство (* )
        Object.Tangent = Tangent       # Установить свойство (* )
        Tangent = Object.GetTangent()  # Получить свойство (**)
        Object.SetTangent( Tangent )   # Установить свойство (**)
        ```
        """
        ...
    @Tangent.setter
    def Tangent(self, value: bool): ...

    def SetStopChamferCutByObject(self, First, arg1):
        ...

    def SetStopChamferCutObject(self, First, arg1):
        ...

    def SetStopChamferCutPointIndex(self, First, arg1):
        ...

    def SetStopChamferOffset(self, First, arg1):
        ...

    def SetStopChamferOffsetMode(self, First, arg1):
        ...

    def SetStopChamferOn(self, First, arg1):
        ...

    def StopChamferCutByObject(self, First) -> bool:  # ichamfer_stopchamfercutbyobject.html
        """
        ## StopChamferCutByObject - Усекать по объекту (При секущей ) для точки останова фаски

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        StopChamferCutByObject = Object.StopChamferCutByObject( First )     # Получить свойство (* )
        Object.StopChamferCutByObject( First ) = StopChamferCutByObject     # Установить свойство (* )
        StopChamferCutByObject = Object.GetStopChamferCutByObject( First )  # Получить свойство (**)
        Object.SetStopChamferCutByObject( First, StopChamferCutByObject )   # Установить свойство (**)
        ```

        ### Входные параметры

        +-----------------------------------------------------------------------+
        | BOOL | First - TRUE - для первой стороны, FALSE - для второй стороны. |
        +-----------------------------------------------------------------------+

        Версия КОМПАС v22
        """
        ...

    def StopChamferCutObject(self, First) -> IModelObject:  # ichamfer_stopchamfercutobject.html
        """
        ## StopChamferCutObject - Секущий объект для точки останова фаски

        Тип данных: Указатель на интерфейс IModelObject

        ### Синтаксис Automation

        ```
        StopChamferCutObject = Object.StopChamferCutObject( First )     # Получить свойство (* )
        Object.StopChamferCutObject( First ) = StopChamferCutObject     # Установить свойство (* )
        StopChamferCutObject = Object.GetStopChamferCutObject( First )  # Получить свойство (**)
        Object.SetStopChamferCutObject( First, StopChamferCutObject )   # Установить свойство (**)
        ```

        ### Входные параметры

        +-----------------------------------------------------------------------+
        | BOOL | First - TRUE - для первой стороны, FALSE - для второй стороны. |
        +-----------------------------------------------------------------------+

        Версия КОМПАС v22
        """
        ...

    def StopChamferCutPointIndex(self, First) -> int:  # ichamfer_stopchamfercutpointindex.html
        """
        ## StopChamferCutPointIndex - Индекс точки пересечения с секущим объектом для точки останова фаски

        Тип данных: long

        ### Синтаксис Automation

        ```
        StopChamferCutPointIndex = Object.StopChamferCutPointIndex( First )     # Получить свойство (* )
        Object.StopChamferCutPointIndex( First ) = StopChamferCutPointIndex     # Установить свойство (* )
        StopChamferCutPointIndex = Object.GetStopChamferCutPointIndex( First )  # Получить свойство (**)
        Object.SetStopChamferCutPointIndex( First, StopChamferCutPointIndex )   # Установить свойство (**)
        ```

        ### Входные параметры

        +-----------------------------------------------------------------------+
        | BOOL | First - TRUE - для первой стороны, FALSE - для второй стороны. |
        +-----------------------------------------------------------------------+

        Версия КОМПАС v22
        """
        ...

    def StopChamferOffset(self, First):  # ichamfer_stopchamferoffset.html
        """
        ## StopChamferOffset - Величина смещения для точки останова фаски

        Тип данных: doublet

        ### Синтаксис Automation

        ```
        StopChamferOffset = Object.StopChamferOffset( First )     # Получить свойство (* )
        Object.StopChamferOffset( First ) = StopChamferOffset     # Установить свойство (* )
        StopChamferOffset = Object.GetStopChamferOffset( First )  # Получить свойство (**)
        Object.SetStopChamferOffset( First, StopChamferOffset )   # Установить свойство (**)
        ```

        ### Входные параметры

        +-----------------------------------------------------------------------+
        | BOOL | First - TRUE - для первой стороны, FALSE - для второй стороны. |
        +-----------------------------------------------------------------------+

        Версия КОМПАС v22
        """
        ...

    def StopChamferOffsetMode(self, First) -> int:  # ichamfer_stopchamferoffsetmode.html
        """
        ## StopChamferOffsetMode - Способ расчета смещения для точки останова фаски

        Тип данных: из перечисления ksFilletOffsetModeEnum

        ### Синтаксис Automation

        ```
        StopChamferOffsetMode = Object.StopChamferOffsetMode( First )     # Получить свойство (* )
        Object.StopChamferOffsetMode( First ) = StopChamferOffsetMode     # Установить свойство (* )
        StopChamferOffsetMode = Object.GetStopChamferOffsetMode( First )  # Получить свойство (**)
        Object.SetStopChamferOffsetMode( First, StopChamferOffsetMode )   # Установить свойство (**)
        ```

        ### Входные параметры

        +-----------------------------------------------------------------------+
        | BOOL | First - TRUE - для первой стороны, FALSE - для второй стороны. |
        +-----------------------------------------------------------------------+

        Версия КОМПАС v22
        """
        ...

    def StopChamferOn(self, First) -> bool:  # ichamfer_stopchamferon.html
        """
        ## StopChamferOn - Использовать точку останова фаски

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        StopChamferOn = Object.StopChamferOn( First )     # Получить свойство (* )
        Object.StopChamferOn( First ) = StopChamferOn     # Установить свойство (* )
        StopChamferOn = Object.GetStopChamferOn( First )  # Получить свойство (**)
        Object.SetStopChamferOn( First, StopChamferOn )   # Установить свойство (**)
        ```

        ### Входные параметры

        +-----------------------------------------------------------------------+
        | BOOL | First - TRUE - для первой стороны, FALSE - для второй стороны. |
        +-----------------------------------------------------------------------+

        Версия КОМПАС v22
        """
        ...




class IFaceMovers(IModelObjects):  # ifacemovers.html
    """
    ## Интерфейс IFaceMovers

    Интерфейс коллекции операций перемещения граней.

    Интерфейс позволяет получать и создавать операции перемещения граней.

    Получить интерфейс коллекции можно, используя свойство контейнера поверхностей ISurfaceContainer::FaceMovers.

    Версия: Компас v20
    """

    def Add(self) -> IFaceMover:  # ifacemovers_add.html
        """
        ## Add - Создает новый элемент и добавляет его в коллекцию

        ### Синтаксис Automation

        ```
        LPDISPATCH Add();
        ```

        ### Возвращаемое значение

        Указатель на интерфейс IFaceMover.
        """
        ...

    def FaceMover(self, Index) -> IFaceMover:  # ifacemovers_facemover.html
        """
        ## FaceMover - Возвращает элемент, заданный по индексу

        Тип данных: Указатель на интерфейс IFaceMover.

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        FaceMover = Object.FaceMover( Index )    # Получить свойство (* )
        FaceMover = Object.GetFaceMover(Index )  # Получить свойство (**)
        ```

        ### Входные параметры

        +---------------------------------------------------------------------------------------------------------------------------------+
        | VARIANT Index | - Индекс объекта в коллекции. Поддерживаются следующие типы: - VT_I4 - индекс объекта, - VT_BSTR - имя объекта. |
        +---------------------------------------------------------------------------------------------------------------------------------+
        """
        ...




class IFaceMover(IModelObject):  # ifacemover.html
    """
    ## Интерфейс IFaceMover

    Интерфейс операции перемещения граней.

    1. Интерфейс можно получить у коллекции операций удаления граней, используя свойство IFaceMovers::FaceMover или метод IFaceMovers::Add.

    2. После задания параметров заплатки требуется вызвать метод IModelObject::Update.

    Версия: Компас v20
    """

    @property
    def Direction(self) -> bool:  # ifacemover_direction.html
        """
        ## Direction - Направление (TRUE - прямое, FALSE - обратное)

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        Direction = Object.Direction       # Получить свойство (* )
        Object.Direction = Direction       # Установить свойство (* )
        Direction = Object.GetDirection()  # Получить свойство (**)
        Object.SetDirection( Direction )   # Установить свойство (**)
        ```
        """
        ...
    @Direction.setter
    def Direction(self, value: bool): ...

    @property
    def Faces(self) -> typing.Any:  # ifacemover_faces.html
        """
        ## Faces - Массив граней в виде SAFEARRAY DISPATCH - VT_ARRAY | VT_DISPATCH

        Тип данных: VARIANT

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        Faces = Object.Faces       # Получить свойство (* )
        Faces = Object.GetFaces()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def Offset(self) -> float:  # ifacemover_offset.html
        """
        ## Offset - Расстояние

        Тип данных: double

        ### Синтаксис Automation

        ```
        Offset = Object.Offset       # Получить свойство (* )
        Object.Offset = Offset       # Установить свойство (* )
        Offset = Object.GetOffset()  # Получить свойство (**)
        Object.SetOffset( Offset )   # Установить свойство (**)
        ```
        """
        ...
    @Offset.setter
    def Offset(self, value: float): ...

    def SetFaces(self, Faces, TangentFaces) -> bool:  # ifacemover_setfaces.html
        """
        ## SetFaces - Установить массив граней в виде SAFEARRAY DISPATCH - VT_ARRAY | VT_DISPATCH

        ### Синтаксис Automation

        ```
        BOOL SetFaces( VARIANT Faces, BOOL TangentFaces );
        ```

        ### Входные параметры

        +-----------------------------------------------+
        | Faces        | - список SafeArray граней,     |
        +-----------------------------------------------+
        | TangentFaces | - добавлять касательные грани. |
        +-----------------------------------------------+

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...




class ICoupling(IModelObject):  # icoupling.html
    """
    ## Интерфейс ICoupling

    Интерфейс цепочки операции Сечение.
    """

    @property
    def Count(self) -> int:  # icoupling_count.html
        """
        ## Count - Количество сечений в цепочке

        Тип данных: long

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        Count = Object.Count       # Получить свойство (* )
        Count = Object.GetCount()  # Получить свойство (**)
        ```
        """
        ...

    def Delete(self) -> bool:  # icoupling_delete.html
        """
        ## Delete - Удалить цепочку

        ### Синтаксис Automation

        ```
        BOOL Delete();
        ```

        ### Возвращаемое значение

        +-----------------------------------------+
        | TRUE | - в случае успешного завершения. |
        +-----------------------------------------+
        """
        ...

    def GetPoint(self, Index) -> bool:  # icoupling_getpoint.html
        """
        ## GetPoint - Получить координаты точки

        ### Синтаксис Automation

        ```
        BOOL GetPoint( long Index, double * X, double * Y, double * Z );
        ```

        ### Входные параметры

        +------------------------------------------+
        | long Index | - индекс сечения в цепочке. |
        +------------------------------------------+

        ### Выходные параметры

        +-------------------------------+
        | X, Y, Z | - координаты точки. |
        +-------------------------------+

        ### Возвращаемое значение

        +-----------------------------------------+
        | TRUE | - в случае успешного завершения. |
        +-----------------------------------------+
        """
        ...

    def Position(self, Index) -> float:  # icoupling_position.html
        """
        ## Position - Величина смещения точки вдоль контура сечения в %

        Тип данных: double

        ### Синтаксис Automation

        ```
        Position = Object.Position( Index )     # Получить свойство (* )
        Object.Position( Index ) = Position     # Установить свойство (* )
        Position = Object.GetPosition( Index )  # Получить свойство (**)
        Object.SetPosition( Index, Position )   # Установить свойство (**)
        ```

        ### Входные параметры

        +------------------------------------------+
        | long Index | - индекс сечения в цепочке. |
        +------------------------------------------+
        """
        ...

    def PositionObject(self, Index) -> IModelObject:  # icoupling_positionobject.html
        """
        ## PositionObject - Фиксирующий объект

        Тип данных: Указатель на интерфейс IModelObject.

        ### Синтаксис Automation

        ```
        PositionObject = Object.PositionObject( Index )     # Получить свойство (* )
        Object.PositionObject( Index ) = PositionObject     # Установить свойство (* )
        PositionObject = Object.GetPositionObject( Index )  # Получить свойство (**)
        Object.SetPositionObject( Index, PositionObject )   # Установить свойство (**)
        ```

        ### Входные параметры

        +------------------------------------------+
        | long Index | - индекс сечения в цепочке. |
        +------------------------------------------+
        """
        ...

    def PositionOffset(self, Index) -> float:  # icoupling_positionoffset.html
        """
        ## PositionOffset - Величина смещения точки вдоль контура сечения в мм

        Тип данных: double

        ### Синтаксис Automation

        ```
        PositionOffset = Object.PositionOffset( Index )     # Получить свойство (* )
        Object.PositionOffset( Index ) = PositionOffset     # Установить свойство (* )
        PositionOffset = Object.GetPositionOffset( Index )  # Получить свойство (**)
        Object.SetPositionOffset( Index, PositionOffset )   # Установить свойство (**)
        ```

        ### Входные параметры

        +------------------------------------------+
        | long Index | - индекс сечения в цепочке. |
        +------------------------------------------+
        """
        ...

    def SetPoint(self, Index, X, Y, Z) -> bool:  # icoupling_setpoint.html
        """
        ## SetPoint - Установить параметры точки

        ### Синтаксис Automation

        ```
        BOOL SetPoint( long Index, double X, double Y, double Z );
        ```

        ### Входные параметры

        +------------------------------------------+
        | long Index | - индекс сечения в цепочке, |
        +------------------------------------------+
        | X, Y, Z    | - координаты точки.         |
        +------------------------------------------+

        ### Возвращаемое значение

        +-----------------------------------------+
        | TRUE | - в случае успешного завершения. |
        +-----------------------------------------+
        """
        ...

    def SetPosition(self, Index, arg1):
        ...

    def SetPositionObject(self, Index, arg1):
        ...

    def SetPositionOffset(self, Index, arg1):
        ...

    def Sketch(self, Index) -> IModelObject:  # icoupling_sketch.html
        """
        ## Sketch - Сечение. Эскиз, контур, пространственная кривая, грань

        Тип данных: Указатель на интерфейс IModelObject.

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        Sketch = Object.Sketch( Index )     # Получить свойство (* )
        Sketch = Object.GetSketch( Index )  # Получить свойство (**)
        ```

        ### Входные параметры

        +------------------------------------------+
        | long Index | - индекс сечения в цепочке. |
        +------------------------------------------+
        """
        ...




class ICut(IModelObject):  # icut.html
    """
    ## Интерфейс ICut

    Интерфейс операции Сечение.
    """

    @property
    def BuildingType(self) -> int:  # icut_buildingtype.html
        """
        ## BuildingType - Способ сечения при сечении по эскизу

        Тип данных: Значение из перечисления ksCutBuildingTypeEnum.

        ### Синтаксис Automation

        ```
        BuildingType = Object.BuildingType       # Получить свойство (* )
        Object.BuildingType = BuildingType       # Установить свойство (* )
        BuildingType = Object.GetBuildingType()  # Получить свойство (**)
        Object.SetBuildingType( BuildingType )   # Установить свойство (**)
        ```
        """
        ...
    @BuildingType.setter
    def BuildingType(self, value: int): ...

    @property
    def ChooseBodies(self) -> typing.Any:  # icut_choosebodies.html
        """
        ## ChooseBodies - Массив тел

        Тип данных: VARIANT

        Позволяет получать и устанавливать тело или массив тел, участвующих в операции.

        Массив возвращается в виде массива SAFEARRAY тел LPDISPATCH (VT_ARRAY | VT_DISPATCH).

        ### Синтаксис Automation

        ```
        ChooseBodies = Object.ChooseBodies       # Получить свойство (* )
        Object.ChooseBodies = ChooseBodies       # Установить свойство (* )
        ChooseBodies = Object.GetChooseBodies()  # Получить свойство (**)
        Object.SetChooseBodies( ChooseBodies )   # Установить свойство (**)
        ```
        """
        ...
    @ChooseBodies.setter
    def ChooseBodies(self, value: typing.Any): ...

    @property
    def ChooseParts(self) -> typing.Any:  # icut_chooseparts.html
        """
        ## ChooseParts - Массив компонентов (SAFEARRAY)

        Тип данных: VARIANT

        Позволяет получать и устанавливать компонент или массив компонентов, участвующих в операции.

        Массив возвращается в виде массива SAFEARRAY компонентов LPDISPATCH (VT_ARRAY | VT_DISPATCH).

        ### Синтаксис Automation

        ```
        ChooseParts = Object.ChooseParts       # Получить свойство (* )
        Object.ChooseParts = ChooseParts       # Установить свойство (* )
        ChooseParts = Object.GetChooseParts()  # Получить свойство (**)
        Object.SetChooseParts( ChooseParts )   # Установить свойство (**)
        ```
        """
        ...
    @ChooseParts.setter
    def ChooseParts(self, value: typing.Any): ...

    @property
    def ChoosePartsType(self) -> int:  # icut_choosepartstype.html
        """
        ## ChoosePartsType - Способ определения области применения для компонентов

        Тип данных: Значение из перечисления ksChoosePartsType.

        ### Синтаксис Automation

        ```
        ChoosePartsType = Object.ChoosePartsType       # Получить свойство (* )
        Object.ChoosePartsType = ChoosePartsType       # Установить свойство (* )
        ChoosePartsType = Object.GetChoosePartsType()  # Получить свойство (**)
        Object.SetChoosePartsType( ChoosePartsType )   # Установить свойство (**)
        ```
        """
        ...
    @ChoosePartsType.setter
    def ChoosePartsType(self, value: int): ...

    @property
    def ChooseType(self) -> int:  # icut_choosetype.html
        """
        ## ChooseType - Область применения

        Тип данных: Значение из перечисления ksChooseType.

        ### Синтаксис Automation

        ```
        ChooseType = Object.ChooseType       # Получить свойство (* )
        Object.ChooseType = ChooseType       # Установить свойство (* )
        ChooseType = Object.GetChooseType()  # Получить свойство (**)
        Object.SetChooseType( ChooseType )   # Установить свойство (**)
        ```
        """
        ...
    @ChooseType.setter
    def ChooseType(self, value: int): ...

    @property
    def CutObject(self) -> IModelObject:  # icut_cutobject.html
        """
        ## CutObject - Секущий объект (эскиз или поверхность)

        Тип данных: Указатель на интерфейс IModelObject

        ### Синтаксис Automation

        ```
        CutObject = Object.CutObject       # Получить свойство (* )
        Object.CutObject = CutObject       # Установить свойство (* )
        CutObject = Object.GetCutObject()  # Получить свойство (**)
        Object.SetCutObject( CutObject )   # Установить свойство (**)
        ```
        """
        ...
    @CutObject.setter
    def CutObject(self, value: IModelObject): ...

    @property
    def Direction(self) -> bool:  # icut_direction.html
        """
        ## Direction - Направление отсечения

        Тип данных: BOOL

        Значения свойства:

        +---------------------------------+
        | TRUE  | - прямое направление,   |
        +---------------------------------+
        | FALSE | - обратное направление. |
        +---------------------------------+

        ### Синтаксис Automation

        ```
        Direction = Object.Direction       # Получить свойство (* )
        Object.Direction = Direction       # Установить свойство (* )
        Direction = Object.GetDirection()  # Получить свойство (**)
        Object.SetDirection( Direction )   # Установить свойство (**)
        ```
        """
        ...
    @Direction.setter
    def Direction(self, value: bool): ...




class IEvolution(IModelObject):  # ievolution.html
    """
    ## Интерфейс IEvolution

    Интерфейс кинематической операции.
    """

    @property
    def BySurfaceNormal(self) -> bool:  # ievolution_bysurfacenormal.html
        """
        ## BySurfaceNormal - Согласованно с нормалью поверхности

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        BySurfaceNormal = Object.BySurfaceNormal       # Получить свойство (* )
        Object.BySurfaceNormal = BySurfaceNormal       # Установить свойство (* )
        BySurfaceNormal = Object.GetBySurfaceNormal()  # Получить свойство (**)
        Object.SetBySurfaceNormal( BySurfaceNormal )   # Установить свойство (**)
        ```
        """
        ...
    @BySurfaceNormal.setter
    def BySurfaceNormal(self, value: bool): ...

    @property
    def Edges(self) -> typing.Any:  # ievolution_edges.html
        """
        ## Edges - Траектория. Массив SAFEARRAY кривых, задающих траекторию движения сечения кинематического элемента

        Тип данных: VARIANT

        Позволяет получать и устанавливать массив кривых, задающих траекторию движения сечения кинематического элемента.

        Массив возвращается в виде массива SAFEARRAY объектов LPDISPATCH (VT_ARRAY | VT_DISPATCH).

        ### Синтаксис Automation

        ```
        Edges = Object.Edges       # Получить свойство (* )
        Object.Edges = Edges       # Установить свойство (* )
        Edges = Object.GetEdges()  # Получить свойство (**)
        Object.SetEdges( Edges )   # Установить свойство (**)
        ```
        """
        ...
    @Edges.setter
    def Edges(self, value: typing.Any): ...

    @property
    def EvolutionVersion(self) -> int:  # ievolution_evolutionversion.html
        """
        ## EvolutionVersion - Версия оптимизации

        Тип данных: из перечисления ksEvolutionVersionEnum

        ### Синтаксис Automation

        ```
        EvolutionVersion = Object.EvolutionVersion       # Получить свойство (* )
        Object.EvolutionVersion = EvolutionVersion       # Установить свойство (* )
        EvolutionVersion = Object.GetEvolutionVersion()  # Получить свойство (**)
        Object.SetEvolutionVersion( EvolutionVersion )   # Установить свойство (**)
        ```
        """
        ...
    @EvolutionVersion.setter
    def EvolutionVersion(self, value: int): ...

    @property
    def OperationResult(self) -> int:  # ievolution_operationresult.html
        """
        ## OperationResult - Результат операции

        Тип данных: Значение из перечисления ksOperationResultEnum.

        ### Синтаксис Automation

        ```
        OperationResult = Object.OperationResult       # Получить свойство (* )
        Object.OperationResult = OperationResult       # Установить свойство (* )
        OperationResult = Object.GetOperationResult()  # Получить свойство (**)
        Object.SetOperationResult( OperationResult )   # Установить свойство (**)
        ```
        """
        ...
    @OperationResult.setter
    def OperationResult(self, value: int): ...

    @property
    def Sketch(self) -> IModelObject:  # ievolution_sketch.html
        """
        ## Sketch - Сечение. Эскиз

        Тип данных: Указатель на интерфейс IModelObject.

        ### Синтаксис Automation

        ```
        Sketch = Object.Sketch       # Получить свойство (* )
        Object.Sketch = Sketch       # Установить свойство (* )
        Sketch = Object.GetSketch()  # Получить свойство (**)
        Object.SetSketch( Sketch )   # Установить свойство (**)
        ```
        """
        ...
    @Sketch.setter
    def Sketch(self, value: IModelObject): ...

    @property
    def SketchShiftType(self) -> int:  # ievolution_sketchshifttype.html
        """
        ## SketchShiftType - Тип движения сечения по траектории

        Тип данных: Значение из перечисления ksEvolutionShiftSketchTypeEnum.

        ### Синтаксис Automation

        ```
        SketchShiftType = Object.SketchShiftType       # Получить свойство (* )
        Object.SketchShiftType = SketchShiftType       # Установить свойство (* )
        SketchShiftType = Object.GetSketchShiftType()  # Получить свойство (**)
        Object.SetSketchShiftType( SketchShiftType )   # Установить свойство (**)
        ```
        """
        ...
    @SketchShiftType.setter
    def SketchShiftType(self, value: int): ...

    def GetPathLength(self, Unit):  # ievolution_getpathlength.html
        """
        ## GetPathLength - Длина траектории

        ### Синтаксис Automation

        ```
        double GetPathLength( ksLengthUnitsEnum Unit );
        ```

        ### Входные параметры

        +---------------------------------------------------------------------+
        | Unit | - единицы измерения длины из перечисления ksLengthUnitsEnum. |
        +---------------------------------------------------------------------+

        ### Возвращаемое значение

        - длина траектории.
        """
        ...




class IFillet(IModelObject):  # ifillet.html
    """
    ## Интерфейс IFillet

    Интерфейс операции Скругление.
    """

    @property
    def AutoSaveEdge(self) -> bool:  # ifillet_autosaveedge.html
        """
        ## AutoSaveEdge - Автоопределение сохранения кромки

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        AutoSaveEdge = Object.AutoSaveEdge       # Получить свойство (* )
        Object.AutoSaveEdge = AutoSaveEdge       # Установить свойство (* )
        AutoSaveEdge = Object.GetAutoSaveEdge()  # Получить свойство (**)
        Object.SetAutoSaveEdge( AutoSaveEdge )   # Установить свойство (**)
        ```
        """
        ...
    @AutoSaveEdge.setter
    def AutoSaveEdge(self, value: bool): ...

    @property
    def BaseObjects(self) -> typing.Any:  # ifillet_baseobjects.html
        """
        ## BaseObjects - Массив скругляемых объектов (граней и ребер)

        Тип данных: VARIANT

        ### Синтаксис Automation

        ```
        BaseObjects = Object.BaseObjects       # Получить свойство (* )
        Object.BaseObjects = BaseObjects       # Установить свойство (* )
        BaseObjects = Object.GetBaseObjects()  # Получить свойство (**)
        Object.SetBaseObjects( BaseObjects )   # Установить свойство (**)
        ```
        """
        ...
    @BaseObjects.setter
    def BaseObjects(self, value: typing.Any): ...

    @property
    def BuildingType(self) -> int:  # ifillet_buildingtype.html
        """
        ## BuildingType - Способ построения скругления

        Тип данных: Значение из перечисления ksFilletBuildingTypeEnum.

        Метод позволяет получать и устанавливать опорный объект или массив опорных объектов, участвующих в операции Скругление.

        Массив объектов возвращается в виде массива SAFEARRAY объектов LPDISPATCH (VT_ARRAY | VT_DISPATCH).

        ### Синтаксис Automation

        ```
        BuildingType = Object.BuildingType       # Получить свойство (* )
        Object.BuildingType = BuildingType       # Установить свойство (* )
        BuildingType = Object.GetBuildingType()  # Получить свойство (**)
        Object.SetBuildingType( BuildingType )   # Установить свойство (**)
        ```
        """
        ...
    @BuildingType.setter
    def BuildingType(self, value: int): ...

    @property
    def Chord(self) -> float:  # ifillet_chord.html
        """
        ## Chord - Хорда

        Тип данных: double

        ### Синтаксис Automation

        ```
        Chord = Object.Chord       # Получить свойство (* )
        Object.Chord = Chord       # Установить свойство (* )
        Chord = Object.GetChord()  # Получить свойство (**)
        Object.SetChord( Chord )   # Установить свойство (**)
        ```
        """
        ...
    @Chord.setter
    def Chord(self, value: float): ...

    @property
    def Coefficient(self) -> float:  # ifillet_coefficient.html
        """
        ## Coefficient - Коэффициент

        Тип данных: double

        ### Синтаксис Automation

        ```
        Coefficient = Object.Coefficient       # Получить свойство (* )
        Object.Coefficient = Coefficient       # Установить свойство (* )
        Coefficient = Object.GetCoefficient()  # Получить свойство (**)
        Object.SetCoefficient( Coefficient )   # Установить свойство (**)
        ```
        """
        ...
    @Coefficient.setter
    def Coefficient(self, value: float): ...

    @property
    def Radius1(self) -> float:  # ifillet_radius1.html
        """
        ## Radius1 - Радиус скругления или полуось 1

        Тип данных: double

        ### Синтаксис Automation

        ```
        Radius1 = Object.Radius1       # Получить свойство (* )
        Object.Radius1 = Radius1       # Установить свойство (* )
        Radius1 = Object.GetRadius1()  # Получить свойство (**)
        Object.SetRadius1( Radius1 )   # Установить свойство (**)
        ```
        """
        ...
    @Radius1.setter
    def Radius1(self, value: float): ...

    @property
    def Radius2(self) -> float:  # ifillet_radius2.html
        """
        ## Radius2 - Полуось 2

        Тип данных: double

        ### Синтаксис Automation

        ```
        Radius2 = Object.Radius2       # Получить свойство (* )
        Object.Radius2 = Radius2       # Установить свойство (* )
        Radius2 = Object.GetRadius2()  # Получить свойство (**)
        Object.SetRadius2( Radius2 )   # Установить свойство (**)
        ```
        """
        ...
    @Radius2.setter
    def Radius2(self, value: float): ...

    @property
    def RoundCorners(self) -> bool:  # ifillet_roundcorners.html
        """
        ## RoundCorners - Обход углов

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        RoundCorners = Object.RoundCorners       # Получить свойство (* )
        Object.RoundCorners = RoundCorners       # Установить свойство (* )
        RoundCorners = Object.GetRoundCorners()  # Получить свойство (**)
        Object.SetRoundCorners( RoundCorners )   # Установить свойство (**)
        ```
        """
        ...
    @RoundCorners.setter
    def RoundCorners(self, value: bool): ...

    @property
    def SaveEdge(self) -> bool:  # ifillet_saveedge.html
        """
        ## SaveEdge - Сохранять кромку

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        SaveEdge = Object.SaveEdge       # Получить свойство (* )
        Object.SaveEdge = SaveEdge       # Установить свойство (* )
        SaveEdge = Object.GetSaveEdge()  # Получить свойство (**)
        Object.SetSaveEdge( SaveEdge )   # Установить свойство (**)
        ```
        """
        ...
    @SaveEdge.setter
    def SaveEdge(self, value: bool): ...

    @property
    def SmoothCorner(self) -> bool:  # ifillet_smoothcorner.html
        """
        ## SmoothCorner - Сглаживать углы

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        SmoothCorner = Object.SmoothCorner       # Получить свойство (* )
        Object.SmoothCorner = SmoothCorner       # Установить свойство (* )
        SmoothCorner = Object.GetSmoothCorner()  # Получить свойство (**)
        Object.SetSmoothCorner( SmoothCorner )   # Установить свойство (**)
        ```
        """
        ...
    @SmoothCorner.setter
    def SmoothCorner(self, value: bool): ...

    @property
    def StopFilletDirection(self) -> bool:  # ifillet_stopfilletdirection.html
        """
        ## StopFilletDirection - Направление усечения в точке останова

        Тип данных: BOOL

        false - направление от начальной точки до конечной.

        ### Синтаксис Automation

        ```
        StopFilletDirection = Object.StopFilletDirection       # Получить свойство (* )
        Object.StopFilletDirection = StopFilletDirection       # Установить свойство (* )
        StopFilletDirection = Object.GetStopFilletDirection()  # Получить свойство (**)
        Object.SetStopFilletDirection( StopFilletDirection )   # Установить свойство (**)
        ```
        """
        ...
    @StopFilletDirection.setter
    def StopFilletDirection(self, value: bool): ...

    @property
    def Tangent(self) -> bool:  # ifillet_tangent.html
        """
        ## Tangent - По касательным ребрам

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        Tangent = Object.Tangent       # Получить свойство (* )
        Object.Tangent = Tangent       # Установить свойство (* )
        Tangent = Object.GetTangent()  # Получить свойство (**)
        Object.SetTangent( Tangent )   # Установить свойство (**)
        ```
        """
        ...
    @Tangent.setter
    def Tangent(self, value: bool): ...

    @property
    def VariableRadiusBuildingType(self) -> int:  # ifillet_variableradiusbuildingtype.html
        """
        ## VariableRadiusBuildingType - Способ построения при скруглении с переменным радиусом

        Тип данных: из перечисления ksVariableRadiusBuildingTypeEnum

        ### Синтаксис Automation

        ```
        VariableRadiusBuildingType = Object.VariableRadiusBuildingType       # Получить свойство (* )
        Object.VariableRadiusBuildingType = VariableRadiusBuildingType       # Установить свойство (* )
        VariableRadiusBuildingType = Object.GetVariableRadiusBuildingType()  # Получить свойство (**)
        Object.SetVariableRadiusBuildingType( VariableRadiusBuildingType )   # Установить свойство (**)
        ```
        """
        ...
    @VariableRadiusBuildingType.setter
    def VariableRadiusBuildingType(self, value: int): ...

    @property
    def VariableRadiusCount(self) -> int:  # ifillet_variableradiuscount.html
        """
        ## VariableRadiusCount - Количество вершин в настройках переменных радиусов скруглений

        Тип данных: long

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        VariableRadiusCount = Object.VariableRadiusCount       # Получить свойство (* )
        VariableRadiusCount = Object.GetVariableRadiusCount()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def VariableRadiusCurve(self) -> typing.Any:  # ifillet_variableradiuscurve.html
        """
        ## VariableRadiusCurve - Граничная кривая

        Тип данных: VARIANT

        ### Синтаксис Automation

        ```
        VariableRadiusCurve = Object.VariableRadiusCurve       # Получить свойство (* )
        Object.VariableRadiusCurve = VariableRadiusCurve       # Установить свойство (* )
        VariableRadiusCurve = Object.GetVariableRadiusCurve()  # Получить свойство (**)
        Object.SetVariableRadiusCurve( VariableRadiusCurve )   # Установить свойство (**)
        ```
        """
        ...
    @VariableRadiusCurve.setter
    def VariableRadiusCurve(self, value: typing.Any): ...

    @property
    def VariableRadiusOn(self) -> bool:  # ifillet_variableradiuson.html
        """
        ## VariableRadiusOn - Включить настройку переменных радиусов скругления

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        VariableRadiusOn = Object.VariableRadiusOn       # Получить свойство (* )
        Object.VariableRadiusOn = VariableRadiusOn       # Установить свойство (* )
        VariableRadiusOn = Object.GetVariableRadiusOn()  # Получить свойство (**)
        Object.SetVariableRadiusOn( VariableRadiusOn )   # Установить свойство (**)
        ```
        """
        ...
    @VariableRadiusOn.setter
    def VariableRadiusOn(self, value: bool): ...

    def AddVariableRadius(self, PointObject, Radius, Position, PositionType) -> bool:  # ifillet_addvariableradius.html
        """
        ## AddVariableRadius - Добавить настройку переменного радиуса для ребра в точке, заданной смещением

        ### Синтаксис Automation

        ```
        BOOL AddVariableRadius( IModelObject * PointObject, double Radius, double Position, BOOL PositionType );
        ```

        ### Входные параметры

        +------------------------------------------------------------------------------------------------------------------------+
        | PointObject  | - фиксирующий объект,                                                                                   |
        +------------------------------------------------------------------------------------------------------------------------+
        | Radius       | - радиус скругления,                                                                                    |
        +------------------------------------------------------------------------------------------------------------------------+
        | Position     | - положение на кривой,                                                                                  |
        +------------------------------------------------------------------------------------------------------------------------+
        | PositionType | - способ определения положения на кривой: TRUE - задано величиной, FALSE - в процентах от длины кривой. |
        +------------------------------------------------------------------------------------------------------------------------+

        ### Возвращаемое значение

        +-----------------------------------------+
        | TRUE | - в случае успешного завершения. |
        +-----------------------------------------+
        """
        ...

    def SetStopFilletCutByObject(self, First, arg1):
        ...

    def SetStopFilletCutObject(self, First, arg1):
        ...

    def SetStopFilletCutPointIndex(self, First, arg1):
        ...

    def SetStopFilletOffset(self, First, arg1):
        ...

    def SetStopFilletOffsetMode(self, First, arg1):
        ...

    def SetStopFilletOn(self, First, arg1):
        ...

    def SetVariableRadius(self, Index, arg1):
        ...

    def SetVariableRadiusPosition(self, Index, arg1):
        ...

    def SetVariableRadiusPositionLenght(self, Index, arg1):
        ...

    def StopFilletCutByObject(self, First) -> bool:  # ifillet_stopfilletcutbyobject.html
        """
        ## StopFilletCutByObject - Усекать по объекту (При секущей) для точки останова скругления

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        StopFilletCutByObject = Object.StopFilletCutByObject( First )     # Получить свойство (* )
        Object.StopFilletCutByObject( First ) = StopFilletCutByObject     # Установить свойство (* )
        StopFilletCutByObject = Object.GetStopFilletCutByObject( First )  # Получить свойство (**)
        Object.SetStopFilletCutByObject( First, StopFilletCutByObject )   # Установить свойство (**)
        ```

        ### Входные параметры

        +-------------------------------------------------+
        | BOOL First - TRUE | - начальная точка останова, |
        +-------------------------------------------------+
        | FALSE             | - конечная точка останова.  |
        +-------------------------------------------------+
        """
        ...

    def StopFilletCutObject(self, First) -> IModelObject:  # ifillet_stopfilletcutobject.html
        """
        ## StopFilletCutObject - Секущий объект для точки останова скругления

        Тип данных: Указатель на интерфейс IModelObject.

        ### Синтаксис Automation

        ```
        StopFilletCutObject = Object.StopFilletCutObject( First )     # Получить свойство (* )
        Object.StopFilletCutObject( First ) = StopFilletCutObject     # Установить свойство (* )
        StopFilletCutObject = Object.GetStopFilletCutObject( First )  # Получить свойство (**)
        Object.SetStopFilletCutObject( First, StopFilletCutObject )   # Установить свойство (**)
        ```

        ### Входные параметры

        +-------------------------------------------------+
        | BOOL First - TRUE | - начальная точка останова, |
        +-------------------------------------------------+
        | FALSE             | - конечная точка останова.  |
        +-------------------------------------------------+
        """
        ...

    def StopFilletCutPointIndex(self, First) -> int:  # ifillet_stopfilletcutpointindex.html
        """
        ## StopFilletCutPointIndex - Индекс точки пересечения с секущим объектом для точки останова скругления

        Тип данных: long

        ### Синтаксис Automation

        ```
        StopFilletCutPointIndex = Object.StopFilletCutPointIndex( First )     # Получить свойство (* )
        Object.StopFilletCutPointIndex( First ) = StopFilletCutPointIndex     # Установить свойство (* )
        StopFilletCutPointIndex = Object.GetStopFilletCutPointIndex( First )  # Получить свойство (**)
        Object.SetStopFilletCutPointIndex( First, StopFilletCutPointIndex )   # Установить свойство (**)
        ```

        ### Входные параметры

        +-------------------------------------------------+
        | BOOL First - TRUE | - начальная точка останова, |
        +-------------------------------------------------+
        | FALSE             | - конечная точка останова.  |
        +-------------------------------------------------+
        """
        ...

    def StopFilletOffset(self, First) -> float:  # ifillet_stopfilletoffset.html
        """
        ## StopFilletOffset - Величина смещения для точки останова скругления

        Тип данных: double

        ### Синтаксис Automation

        ```
        StopFilletOffset = Object.StopFilletOffset( First )     # Получить свойство (* )
        Object.StopFilletOffset( First ) = StopFilletOffset     # Установить свойство (* )
        StopFilletOffset = Object.GetStopFilletOffset( First )  # Получить свойство (**)
        Object.SetStopFilletOffset( First, StopFilletOffset )   # Установить свойство (**)
        ```

        ### Входные параметры

        +-------------------------------------------------+
        | BOOL First - TRUE | - начальная точка останова, |
        +-------------------------------------------------+
        | FALSE             | - конечная точка останова.  |
        +-------------------------------------------------+
        """
        ...

    def StopFilletOffsetMode(self, First) -> int:  # ifillet_stopfilletoffsetmode.html
        """
        ## StopFilletOffsetMode - Способ расчета смещения для точки останова скругления

        Тип данных: Значение из перечисления ksFilletOffsetModeEnum.

        ### Синтаксис Automation

        ```
        StopFilletOffsetMode = Object.StopFilletOffsetMode( First )     # Получить свойство (* )
        Object.StopFilletOffsetMode( First ) = StopFilletOffsetMode     # Установить свойство (* )
        StopFilletOffsetMode = Object.GetStopFilletOffsetMode( First )  # Получить свойство (**)
        Object.SetStopFilletOffsetMode( First, StopFilletOffsetMode )   # Установить свойство (**)
        ```

        ### Входные параметры

        +-------------------------------------------------+
        | BOOL First - TRUE | - начальная точка останова, |
        +-------------------------------------------------+
        | FALSE             | - конечная точка останова.  |
        +-------------------------------------------------+
        """
        ...

    def StopFilletOn(self, First) -> bool:  # ifillet_stopfilleton.html
        """
        ## StopFilletOn - Использовать точку останова скругления

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        StopFilletOn = Object.StopFilletOn( First )     # Получить свойство (* )
        Object.StopFilletOn( First ) = StopFilletOn     # Установить свойство (* )
        StopFilletOn = Object.GetStopFilletOn( First )  # Получить свойство (**)
        Object.SetStopFilletOn( First, StopFilletOn )   # Установить свойство (**)
        ```

        ### Входные параметры

        +-------------------------------------------------+
        | BOOL First - TRUE | - начальная точка останова, |
        +-------------------------------------------------+
        | FALSE             | - конечная точка останова.  |
        +-------------------------------------------------+
        """
        ...

    def VariableRadius(self, Index) -> float:  # ifillet_variableradius.html
        """
        ## VariableRadius - Значение переменного радиуса в точке

        Тип данных: double

        ### Синтаксис Automation

        ```
        VariableRadius = Object.VariableRadius( Index )     # Получить свойство (* )
        Object.VariableRadius( Index ) = VariableRadius     # Установить свойство (* )
        VariableRadius = Object.GetVariableRadius( Index )  # Получить свойство (**)
        Object.SetVariableRadius( Index, VariableRadius )   # Установить свойство (**)
        ```

        ### Входные параметры

        +----------------------------------------------+
        | long Index | - индекс параметров скругления. |
        +----------------------------------------------+
        """
        ...

    def VariableRadiusClear(self) -> bool:  # ifillet_variableradiusclear.html
        """
        ## VariableRadiusClear - Очистить список настроек переменных радиусов скруглений

        ### Синтаксис Automation

        ```
        BOOL VariableRadiusClear();
        ```

        ### Возвращаемое значение

        +-----------------------------------------+
        | TRUE | - в случае успешного завершения. |
        +-----------------------------------------+
        """
        ...

    def VariableRadiusDelete(self, Index) -> bool:  # ifillet_variableradiusdelete.html
        """
        ## VariableRadiusDelete - Удалить настройку переменного радиуса скруглений по индексу

        ### Синтаксис Automation

        ```
        BOOL VariableRadiusDelete( long Index );
        ```

        ### Входные параметры

        +----------------------------+
        | Index | - индекс настроек. |
        +----------------------------+

        ### Возвращаемое значение

        +-----------------------------------------+
        | TRUE | - в случае успешного завершения. |
        +-----------------------------------------+
        """
        ...

    def VariableRadiusEdge(self, Index) -> IModelObject:  # ifillet_variableradiusedge.html
        """
        ## VariableRadiusEdge - Ребро, для которого задана настройка переменного радиуса

        Тип данных: Указатель на интерфейс IModelObject.

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        VariableRadiusEdge = Object.VariableRadiusEdge( Index )     # Получить свойство (* )
        VariableRadiusEdge = Object.GetVariableRadiusEdge( Index )  # Получить свойство (**)
        ```

        ### Входные параметры

        +----------------------------------------------+
        | long Index | - индекс параметров скругления. |
        +----------------------------------------------+
        """
        ...

    def VariableRadiusPosition(self, Index) -> float:  # ifillet_variableradiusposition.html
        """
        ## VariableRadiusPosition - Величина смещения точки настройки переменного радиуса в %

        Тип данных: double

        ### Синтаксис Automation

        ```
        VariableRadiusPosition = Object.VariableRadiusPosition( Index )     # Получить свойство (* )
        Object.VariableRadiusPosition( Index ) = VariableRadiusPosition     # Установить свойство (* )
        VariableRadiusPosition = Object.GetVariableRadiusPosition( Index )  # Получить свойство (**)
        Object.SetVariableRadiusPosition( Index, VariableRadiusPosition )   # Установить свойство (**)
        ```

        ### Входные параметры

        +----------------------------------------------+
        | long Index | - индекс параметров скругления. |
        +----------------------------------------------+
        """
        ...

    def VariableRadiusPositionLenght(self, Index) -> float:  # ifillet_variableradiuspositionlenght.html
        """
        ## VariableRadiusPositionLenght - Величина смещения точки настройки переменного радиуса в мм

        Тип данных: double

        ### Синтаксис Automation

        ```
        VariableRadiusPositionLenght = Object.VariableRadiusPositionLenght( Index )     # Получить свойство (* )
        Object.VariableRadiusPositionLenght( Index ) = VariableRadiusPositionLenght     # Установить свойство (* )
        VariableRadiusPositionLenght = Object.GetVariableRadiusPositionLenght( Index )  # Получить свойство (**)
        Object.SetVariableRadiusPositionLenght( Index, VariableRadiusPositionLenght )   # Установить свойство (**)
        ```

        ### Входные параметры

        +----------------------------------------------+
        | long Index | - индекс параметров скругления. |
        +----------------------------------------------+
        """
        ...




class IFullFillet(IModelObject):  # ifullfillet.html
    """
    ## Интерфейс IFullFillet

    Интерфейс операции Полное скругление.
    """

    @property
    def CenterFaces(self) -> typing.Any:  # ifullfillet_centerfaces.html
        """
        ## CenterFaces - Центральные грани

        Тип данных: VARIANT

        1. Позволяет получать и устанавливать грань или массив центральных граней, участвующих в операции полного скругления.

        2. Массив объектов возвращается в виде массива SAFEARRAY объектов LPDISPATCH (VT_ARRAY | VT_DISPATCH).

        ### Синтаксис Automation

        ```
        CenterFaces = Object.CenterFaces       # Получить свойство (* )
        Object.CenterFaces = CenterFaces       # Установить свойство (* )
        CenterFaces = Object.GetCenterFaces()  # Получить свойство (**)
        Object.SetCenterFaces( CenterFaces )   # Установить свойство (**)
        ```
        """
        ...
    @CenterFaces.setter
    def CenterFaces(self, value: typing.Any): ...

    @property
    def Side1Faces(self) -> typing.Any:  # ifullfillet_side1faces.html
        """
        ## Side1Faces - Боковые грани 1

        Тип данных: VARIANT

        1. Позволяет получать и устанавливать грань или массив граней первой стороны, участвующих в операции полного скругления.

        2. Массив объектов возвращается в виде массива SAFEARRAY объектов LPDISPATCH (VT_ARRAY | VT_DISPATCH).

        ### Синтаксис Automation

        ```
        Side1Faces = Object.Side1Faces       # Получить свойство (* )
        Object.Side1Faces = Side1Faces       # Установить свойство (* )
        Side1Faces = Object.GetSide1Faces()  # Получить свойство (**)
        Object.SetSide1Faces( Side1Faces )   # Установить свойство (**)
        ```
        """
        ...
    @Side1Faces.setter
    def Side1Faces(self, value: typing.Any): ...

    @property
    def Side2Faces(self) -> typing.Any:  # ifullfillet_side2faces.html
        """
        ## Side2Faces - Боковые грани 2

        Тип данных: VARIANT

        1. Позволяет получать и устанавливать грань или массив граней второй стороны, участвующих в операции полного скругления.

        2. Массив объектов возвращается в виде массива SAFEARRAY объектов LPDISPATCH (VT_ARRAY | VT_DISPATCH).

        ### Синтаксис Automation

        ```
        Side2Faces = Object.Side2Faces       # Получить свойство (* )
        Object.Side2Faces = Side2Faces       # Установить свойство (* )
        Side2Faces = Object.GetSide2Faces()  # Получить свойство (**)
        Object.SetSide2Faces( Side2Faces )   # Установить свойство (**)
        ```
        """
        ...
    @Side2Faces.setter
    def Side2Faces(self, value: typing.Any): ...

    @property
    def Tangent(self) -> bool:  # ifullfillet_tangent.html
        """
        ## Tangent - По касательным граням

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        Tangent = Object.Tangent       # Получить свойство (* )
        Object.Tangent = Tangent       # Установить свойство (* )
        Tangent = Object.GetTangent()  # Получить свойство (**)
        Object.SetTangent( Tangent )   # Установить свойство (**)
        ```
        """
        ...
    @Tangent.setter
    def Tangent(self, value: bool): ...




class IIncline(IModelObject):  # iincline.html
    """
    ## Интерфейс IIncline

    Интерфейс операции Уклон.
    """

    @property
    def Angle(self) -> float:  # iincline_angle.html
        """
        ## Angle - Угол уклона

        Тип данных: double

        ### Синтаксис Automation

        ```
        Angle = Object.Angle       # Получить свойство (* )
        Object.Angle = Angle       # Установить свойство (* )
        Angle = Object.GetAngle()  # Получить свойство (**)
        Object.SetAngle( Angle )   # Установить свойство (**)
        ```
        """
        ...
    @Angle.setter
    def Angle(self, value: float): ...

    @property
    def Direction(self) -> bool:  # iincline_direction.html
        """
        ## Direction - Направление уклона

        Тип данных: BOOL

        Значение свойства:

        +-------------------+
        | TRUE  | - наружу, |
        +-------------------+
        | FALSE | - внутрь. |
        +-------------------+

        ### Синтаксис Automation

        ```
        Direction = Object.Direction       # Получить свойство (* )
        Object.Direction = Direction       # Установить свойство (* )
        Direction = Object.GetDirection()  # Получить свойство (**)
        Object.SetDirection( Direction )   # Установить свойство (**)
        ```
        """
        ...
    @Direction.setter
    def Direction(self, value: bool): ...

    @property
    def Faces(self) -> typing.Any:  # iincline_faces.html
        """
        ## Faces - Массив уклоняемых граней

        Тип данных: VARIANT

        Позволяет получать и устанавливать грань или массив граней, участвующих в операции уклон.

        Массив граней возвращается в виде массива SAFEARRAY граней LPDISPATCH (VT_ARRAY | VT_DISPATCH).

        ### Синтаксис Automation

        ```
        Faces = Object.Faces       # Получить свойство (* )
        Object.Faces = Faces       # Установить свойство (* )
        Faces = Object.GetFaces()  # Получить свойство (**)
        Object.SetFaces( Faces )   # Установить свойство (**)
        ```
        """
        ...
    @Faces.setter
    def Faces(self, value: typing.Any): ...

    @property
    def Plane(self) -> IModelObject:  # iincline_plane.html
        """
        ## Plane - Основание уклона

        Тип данных: Указатель на интерфейс IModelObject.

        ### Синтаксис Automation

        ```
        Plane = Object.Plane       # Получить свойство (* )
        Object.Plane = Plane       # Установить свойство (* )
        Plane = Object.GetPlane()  # Получить свойство (**)
        Object.SetPlane( Plane )   # Установить свойство (**)
        ```
        """
        ...
    @Plane.setter
    def Plane(self, value: IModelObject): ...




class IDraftFromEdges(IModelObject):  # idraftfromedges.html
    """
    ## Интерфейс IDraftFromEdges

    Интерфейс операции Уклон от базовой линии.
    """

    @property
    def Angle(self) -> float:  # idraftfromedges_angle.html
        """
        ## Angle - Угол

        Тип данных: double

        ### Синтаксис Automation

        ```
        Angle = Object.Angle       # Получить свойство (* )
        Object.Angle = Angle       # Установить свойство (* )
        Angle = Object.GetAngle()  # Получить свойство (**)
        Object.SetAngle( Angle )   # Установить свойство (**)
        ```
        """
        ...
    @Angle.setter
    def Angle(self, value: float): ...

    @property
    def BaseLine(self) -> typing.Any:  # idraftfromedges_baseline.html
        """
        ## BaseLine - Базовая линия

        Тип данных: VARIANT

        Набор кривых SAFEARRAY DISPATCH - VT_ARRAY | VT_DISPATCH.

        ### Синтаксис Automation

        ```
        BaseLine = Object.BaseLine       # Получить свойство (* )
        Object.BaseLine = BaseLine       # Установить свойство (* )
        BaseLine = Object.GetBaseLine()  # Получить свойство (**)
        Object.SetBaseLine( BaseLine )   # Установить свойство (**)
        ```
        """
        ...
    @BaseLine.setter
    def BaseLine(self, value: typing.Any): ...

    @property
    def DirectionObject(self) -> IModelObject:  # idraftfromedges_directionobject.html
        """
        ## DirectionObject - Базовое направление. Направляющий объект

        Тип данных: Указатель на интерфейс IModelObject

        ### Синтаксис Automation

        ```
        DirectionObject = Object.DirectionObject       # Получить свойство (* )
        Object.DirectionObject = DirectionObject       # Установить свойство (* )
        DirectionObject = Object.GetDirectionObject()  # Получить свойство (**)
        Object.SetDirectionObject( DirectionObject )   # Установить свойство (**)
        ```
        """
        ...
    @DirectionObject.setter
    def DirectionObject(self, value: IModelObject): ...

    @property
    def Reverse(self) -> bool:  # idraftfromedges_reverse.html
        """
        ## Reverse - Сменить направление

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        Reverse = Object.Reverse       # Получить свойство (* )
        Object.Reverse = Reverse       # Установить свойство (* )
        Reverse = Object.GetReverse()  # Получить свойство (**)
        Object.SetReverse( Reverse )   # Установить свойство (**)
        ```
        """
        ...
    @Reverse.setter
    def Reverse(self, value: bool): ...

    @property
    def SideFace(self) -> IModelObject:  # idraftfromedges_sideface.html
        """
        ## SideFace - Сторона. Грань

        Тип данных: Указатель на интерфейс IModelObject

        ### Синтаксис Automation

        ```
        SideFace = Object.SideFace       # Получить свойство (* )
        Object.SideFace = SideFace       # Установить свойство (* )
        SideFace = Object.GetSideFace()  # Получить свойство (**)
        Object.SetSideFace( SideFace )   # Установить свойство (**)
        ```
        """
        ...
    @SideFace.setter
    def SideFace(self, value: IModelObject): ...

    @property
    def Vector3D(self) -> IVector3D:  # vector3d.html
        """
        ## Vector3D - Базовое направление. Параметры вектора

        Тип данных: Указатель на интерфейс IVector3D

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        Vector3D = Object.Vector3D       # Получить свойство (* )
        Vector3D = Object.GetVector3D()  # Получить свойство (**)
        ```
        """
        ...




class ILoft(IModelObject):  # iloft.html
    """
    ## Интерфейс ILoft

    Интерфейс операции По сечениям.
    """

    @property
    def AxisLine(self) -> IModelObject:  # iloft_axisline.html
        """
        ## AxisLine - Осевая линия. Ребро, кривая или зскиз, в котором лежит кривая

        Тип данных: Указатель на интерфейс IModelObject.

        ### Синтаксис Automation

        ```
        AxisLine = Object.AxisLine       # Получить свойство (* )
        Object.AxisLine = AxisLine       # Установить свойство (* )
        AxisLine = Object.GetAxisLine()  # Получить свойство (**)
        Object.SetAxisLine( AxisLine )   # Установить свойство (**)
        ```
        """
        ...
    @AxisLine.setter
    def AxisLine(self, value: IModelObject): ...

    @property
    def Closed(self) -> bool:  # iloft_closed.html
        """
        ## Closed - Замкнуть траекторию

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        Closed = Object.Closed       # Получить свойство (* )
        Object.Closed = Closed       # Установить свойство (* )
        Closed = Object.GetClosed()  # Получить свойство (**)
        Object.SetClosed( Closed )   # Установить свойство (**)
        ```
        """
        ...
    @Closed.setter
    def Closed(self, value: bool): ...

    @property
    def CouplingsCount(self) -> int:  # iloft_couplingscount.html
        """
        ## CouplingsCount - Количество цепочек

        Тип данных: long

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        CouplingsCount = Object.CouplingsCount       # Получить свойство (* )
        CouplingsCount = Object.GetCouplingsCount()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def DirectionalLines(self) -> typing.Any:  # iloft_directionallines.html
        """
        ## DirectionalLines - Направляющие кривые

        Тип данных: VARIANT

        Позволяет получать и устанавливать направляющую кривую или направляющих кривых.

        Массив кривых возвращается в виде массива SAFEARRAY объектов LPDISPATCH (VT_ARRAY | VT_DISPATCH).

        ### Синтаксис Automation

        ```
        DirectionalLines = Object.DirectionalLines        # Получить свойство (* )
        Object.DirectionalLines = DirectionalLines        # Установить свойство (* )
        DirectionalLines = Object.GetDirectionalLines())  # Получить свойство (**)
        Object.SetDirectionalLines( DirectionalLines )    # Установить свойство (**)
        ```
        """
        ...
    @DirectionalLines.setter
    def DirectionalLines(self, value: typing.Any): ...

    @property
    def OperationResult(self) -> int:  # iloft_operationresult.html
        """
        ## OperationResult - Результат операции

        Тип данных: Значение из перечисления ksOperationResultEnum.

        ### Синтаксис Automation

        ```
        OperationResult = Object.OperationResult       # Получить свойство (* )
        Object.OperationResult = OperationResult       # Установить свойство (* )
        OperationResult = Object.GetOperationResult()  # Получить свойство (**)
        Object.SetOperationResult( OperationResult )   # Установить свойство (**)
        ```
        """
        ...
    @OperationResult.setter
    def OperationResult(self, value: int): ...

    @property
    def Sketchs(self) -> typing.Any:  # iloft_sketchs.html
        """
        ## Sketchs - Сечения. Эскизы, контуры, пространственные кривые, грани

        Тип данных: VARIANT

        Свойство позволяет получать и устанавливать массив сечений, участвующих в операции.

        Массив возвращается в виде массива SAFEARRAY объектов LPDISPATCH (VT_ARRAY | VT_DISPATCH).

        ### Синтаксис Automation

        ```
        Sketchs = Object.Sketchs       # Получить свойство (* )
        Object.Sketchs = Sketchs       # Установить свойство (* )
        Sketchs = Object.GetSketchs()  # Получить свойство (**)
        Object.SetSketchs( Sketchs )   # Установить свойство (**)
        ```
        """
        ...
    @Sketchs.setter
    def Sketchs(self, value: typing.Any): ...

    def AddCoupling(self) -> ICoupling:  # iloft_addcoupling.html
        """
        ## AddCoupling - Добавить цепочку

        ### Синтаксис Automation

        ```
        LPDISPATCH AddCoupling();
        ```

        ### Возвращаемое значение

        - указатель на интерфейс ICoupling.
        """
        ...

    def BuildingObject(self, BeginSection) -> IModelObject:  # iloft_buildingobject.html
        """
        ## BuildingObject - Направляющий объект построения элемента у крайних сечений

        Тип данных: Указатель на интерфейс IModelObject.

        ### Синтаксис Automation

        ```
        BuildingObject = Object.BuildingObject( BeginSection )     # Получить свойство (* )
        Object.BuildingObject( BeginSection ) = BuildingObject     # Установить свойство (* )
        BuildingObject = Object.GetBuildingObject( BeginSection )  # Получить свойство (**)
        Object.SetBuildingObject( BeginSection, BuildingObject )   # Установить свойство (**)
        ```

        ### Входные параметры

        +---------------------------------------------------------------------------------+
        | BOOL BeginSection | - TRUE у начального сечения, - FALSE - у конечного сечения. |
        +---------------------------------------------------------------------------------+
        """
        ...

    def BuildingType(self, BeginSection) -> int:  # iloft_buildingtype.html
        """
        ## BuildingType - Способы построения элемента у крайних сечений

        Тип данных: Значение из перечисления ksLoftBuildingType.

        ### Синтаксис Automation

        ```
        BuildingType = Object.BuildingType( BeginSection )     # Получить свойство (* )
        Object.BuildingType( BeginSection ) = BuildingType     # Установить свойство (* )
        BuildingType = Object.GetBuildingType( BeginSection )  # Получить свойство (**)
        Object.SetBuildingType( BeginSection, BuildingType )   # Установить свойство (**)
        ```

        ### Входные параметры

        +---------------------------------------------------------------------------------+
        | BOOL BeginSection | - TRUE у начального сечения, - FALSE - у конечного сечения. |
        +---------------------------------------------------------------------------------+
        """
        ...

    def ClearCouplings(self) -> bool:  # iloft_clearcouplings.html
        """
        ## ClearCouplings - Удалить все цепочки

        ### Синтаксис Automation

        ```
        BOOL ClearCouplings();
        ```

        ### Возвращаемое значение

        +-----------------------------------------+
        | TRUE | - в случае успешного завершения. |
        +-----------------------------------------+
        """
        ...

    def Coupling(self, Index) -> ICoupling:  # iloft_coupling.html
        """
        ## Coupling - Получить параметры цепочки по индексу

        Тип данных: Указатель на интерфейс ICoupling.

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        Coupling = Object.Coupling( Index )     # Получить свойство (* )
        Coupling = Object.GetCoupling( Index )  # Получить свойство (**)
        ```

        ### Входные параметры

        +--------------------------------+
        | long Index | - индекс цепочки. |
        +--------------------------------+
        """
        ...

    def DeleteCoupling(self, Index) -> bool:  # iloft_deletecoupling.html
        """
        ## DeleteCoupling - Удалить цепочку по индексу

        ### Синтаксис Automation

        ```
        BOOL DeleteCoupling( long Index );
        ```

        ### Входные параметры

        +---------------------------+
        | Index | - индекс цепочки. |
        +---------------------------+

        ### Возвращаемое значение

        +-----------------------------------------+
        | TRUE | - в случае успешного завершения. |
        +-----------------------------------------+
        """
        ...

    def SetBuildingObject(self, BeginSection, arg1):
        ...

    def SetBuildingType(self, BeginSection, arg1):
        ...




class IRib(IModelObject):  # irib.html
    """
    ## Интерфейс IRib

    Интерфейс операции Ребро жесткости.
    """

    @property
    def Angle(self) -> float:  # irib_angle.html
        """
        ## Angle - Уклон граней ребра. Угол уклона

        Тип данных: double

        ### Синтаксис Automation

        ```
        Angle = Object.Angle       # Получить свойство (* )
        Object.Angle = Angle       # Установить свойство (* )
        Angle = Object.GetAngle()  # Получить свойство (**)
        Object.SetAngle( Angle )   # Установить свойство (**)
        ```
        """
        ...
    @Angle.setter
    def Angle(self, value: float): ...

    @property
    def Body(self) -> IKompasAPIObject:  # irib_body.html
        """
        ## Body - Тело для операции

        Тип данных: Указатель на интерфейс IKompasAPIObject

        ### Синтаксис Automation

        ```
        Body = Object.Body       # Получить свойство (* )
        Object.Body = Body       # Установить свойство (* )
        Body = Object.GetBody()  # Получить свойство (**)
        Object.SetBody( Body )   # Установить свойство (**)
        ```
        """
        ...
    @Body.setter
    def Body(self, value: IKompasAPIObject): ...

    @property
    def SegmentIndex(self) -> int:  # irib_segmentindex.html
        """
        ## SegmentIndex - Уклон граней ребра. Угол уклона. Индекс направляющего сегмента

        Тип данных: long

        ### Синтаксис Automation

        ```
        SegmentIndex = Object.SegmentIndex       # Получить свойство (* )
        Object.SegmentIndex = SegmentIndex       # Установить свойство (* )
        SegmentIndex = Object.GetSegmentIndex()  # Получить свойство (**)
        Object.SetSegmentIndex( SegmentIndex )   # Установить свойство (**)
        ```
        """
        ...
    @SegmentIndex.setter
    def SegmentIndex(self, value: int): ...

    @property
    def Side(self) -> int:  # irib_side.html
        """
        ## Side - Положение

        Тип данных: Значение из перечисления ksRibSideEnum.

        ### Синтаксис Automation

        ```
        Side = Object.Side       # Получить свойство (* )
        Object.Side = Side       # Установить свойство (* )
        Side = Object.GetSide()  # Получить свойство (**)
        Object.SetSide( Side )   # Установить свойство (**)
        ```
        """
        ...
    @Side.setter
    def Side(self, value: int): ...

    @property
    def Sketch(self) -> ISketch:  # irib_sketch.html
        """
        ## Sketch - Контур ребра жесткости

        Тип данных: Указатель на интерфейс ISketch.

        ### Синтаксис Automation

        ```
        Sketch = Object.Sketch       # Получить свойство (* )
        Object.Sketch = Sketch       # Установить свойство (* )
        Sketch = Object.GetSketch()  # Получить свойство (**)
        Object.SetSketch( Sketch )   # Установить свойство (**)
        ```
        """
        ...
    @Sketch.setter
    def Sketch(self, value: ISketch): ...




class IMultiThicknessGroupsManager(IDispatch):  # imultithicknessgroupsmanager.html
    """
    ## Интерфейс IMultiThicknessGroupsManager

    Менеджер разнотолщинных групп.

    Интерфейс является дополнительным для интерфейса IShell (Оболочка),

    Интерфейс позволяет настроить разную толшину для выбранных граней.
    """

    @property
    def MultiThick(self) -> bool:  # imultithicknessgroupsmanager_multithick.html
        """
        ## MultiThick - Признак используемости разнотолщинных групп при построении операции

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        MultiThick = Object.MultiThick       # Получить свойство (* )
        Object.MultiThick = MultiThick       # Установить свойство (* )
        MultiThick = Object.GetMultiThick()  # Получить свойство (**)
        Object.SetMultiThick( MultiThick )   # Установить свойство (**)
        ```
        """
        ...
    @MultiThick.setter
    def MultiThick(self, value: bool): ...

    def AddMultiThicknessGroup(self, Type, Objects, Thickness) -> bool:  # imultithicknessgroupsmanager_addmultithicknessgroup.html
        """
        ## AddMultiThicknessGroup - Добавить группу объектов с заданной толщиной

        ### Синтаксис Automation

        ```
        BOOL AddMultiThicknessGroup( ksMultiThicknessGroupTypeEnum Type, VARIANT Objects, double Thickness );
        ```

        ### Входные параметры

        +-------------------------------------------------------------------------------------------------------------------------------------+
        | Type      | - Тип группы параметров из перечисления ksMultiThicknessGroupTypeEnum,                                                  |
        +-------------------------------------------------------------------------------------------------------------------------------------+
        | Objects   | - Список граней - массив SafeArray типа VT_ARRAY | VT_DISPATCH для нескольких граней и VT_DISPATCH для одиночной грани, |
        +-------------------------------------------------------------------------------------------------------------------------------------+
        | Thickness | - толщина.                                                                                                              |
        +-------------------------------------------------------------------------------------------------------------------------------------+

        ### Возвращаемое значение

        +-----------------------------------------+
        | TRUE | - в случае успешного завершения, |
        +-----------------------------------------+
        """
        ...

    def ClearMultiThicknessGroups(self) -> bool:  # imultithicknessgroupsmanager_clearmultithicknessgroups.html
        """
        ## ClearMultiThicknessGroups - Удалить все группы толщин

        ### Синтаксис Automation

        ```
        BOOL ClearMultiThicknessGroups();
        ```

        ### Возвращаемое значение

        +-----------------------------------------+
        | TRUE | - в случае успешного завершения, |
        +-----------------------------------------+
        """
        ...

    def DeleteMultiThicknessGroup(self, Type, Index) -> bool:  # imultithicknessgroupsmanager_deletemultithicknessgroup.html
        """
        ## DeleteMultiThicknessGroup - Удалить группу объектов с заданной толщиной

        ### Синтаксис Automation

        ```
        BOOL DeleteMultiThicknessGroup( ksMultiThicknessGroupTypeEnum Type, long Index );
        ```

        ### Входные параметры

        +--------------------------------------------------------------------------------+
        | Type  | - Тип группы параметров из перечисления ksMultiThicknessGroupTypeEnum, |
        +--------------------------------------------------------------------------------+
        | Index | - Индекс параметров в группе.                                          |
        +--------------------------------------------------------------------------------+

        ### Возвращаемое значение

        +-----------------------------------------+
        | TRUE | - в случае успешного завершения, |
        +-----------------------------------------+
        """
        ...

    def DestroyMultiThicknessGroup(self, Type, Index) -> bool:  # imultithicknessgroupsmanager_destroymultithicknessgroup.html
        """
        ## DestroyMultiThicknessGroup - Разрушить группу объектов с заданной толщиной

        ### Синтаксис Automation

        ```
        BOOL DestroyMultiThicknessGroup( ksMultiThicknessGroupTypeEnum Type, long Index );
        ```

        ### Входные параметры

        +--------------------------------------------------------------------------------+
        | Type  | - Тип группы параметров из перечисления ksMultiThicknessGroupTypeEnum, |
        +--------------------------------------------------------------------------------+
        | Index | - Индекс параметров в группе.                                          |
        +--------------------------------------------------------------------------------+

        ### Возвращаемое значение

        +-----------------------------------------+
        | TRUE | - в случае успешного завершения, |
        +-----------------------------------------+
        """
        ...

    def ExcludeMultiThicknessGroupObjects(self, Objects) -> bool:  # imultithicknessgroupsmanager_excludemultithicknessgroupobjects.html
        """
        ## ExcludeMultiThicknessGroupObjects - Исключить объекты из групп

        ### Синтаксис Automation

        ```
        BOOL ExcludeMultiThicknessGroupObjects( VARIANT Objects );
        ```

        ### Входные параметры

        +------------------------------------------------------------------------------------------+
        | Objects | - Грань VT_DISPATCH или список граней массив SafeArray VT_ARRAY | VT_DISPATCH. |
        +------------------------------------------------------------------------------------------+

        ### Возвращаемое значение

        +-----------------------------------------+
        | TRUE | - в случае успешного завершения, |
        +-----------------------------------------+
        """
        ...

    def MultiThicknessGroupsCount(self, Type) -> int:  # imultithicknessgroupsmanager_multithicknessgroupscount.html
        """
        ## MultiThicknessGroupsCount - Количество групп параметров разных толщин

        Тип данных: long

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        MultiThicknessGroupsCount = Object.MultiThicknessGroupsCount( Type )     # Получить свойство (* )
        MultiThicknessGroupsCount = Object.GetMultiThicknessGroupsCount( Type )  # Получить свойство (**)
        ```

        ### Входные параметры

        +-------------------------------------------------------------------------------+
        | Type | - Тип группы параметров из перечисления ksMultiThicknessGroupTypeEnum. |
        +-------------------------------------------------------------------------------+
        """
        ...

    def MultiThicknessGroupsObjects(self, Type, Index) -> typing.Any:  # imultithicknessgroupsmanager_multithicknessgroupsobjects.html
        """
        ## MultiThicknessGroupsObjects - Объекты в группе

        Тип данных: VARIANT

        ### Синтаксис Automation

        ```
        MultiThicknessGroupsObjects = Object.MultiThicknessGroupsObjects( Type, Index )     # Получить свойство (* )
        Object.MultiThicknessGroupsObjects( Type, Index ) = MultiThicknessGroupsObjects     # Установить свойство (* )
        MultiThicknessGroupsObjects = Object.GetMultiThicknessGroupsObjects( Type, Index )  # Получить свойство (**)
        Object.SetMultiThicknessGroupsObjects( Type, Index, MultiThicknessGroupsObjects )   # Установить свойство (**)
        ```

        ### Входные параметры

        +-------------------------------------------------------------------------------------+
        | Type       | - Тип группы параметров из перечисления ksMultiThicknessGroupTypeEnum, |
        +-------------------------------------------------------------------------------------+
        | long Index | - Индекс параметров в группе.                                          |
        +-------------------------------------------------------------------------------------+
        """
        ...

    def MultiThicknessGroupsThickness(self, Type, Index) -> float:  # imultithicknessgroupsmanager_multithicknessgroupsthickness.html
        """
        ## MultiThicknessGroupsThickness - Толщина, заданная для группы

        Тип данных: double

        ### Синтаксис Automation

        ```
        MultiThicknessGroupsThickness = Object.MultiThicknessGroupsThickness( Type, Index )     # Получить свойство (* )
        Object.MultiThicknessGroupsThickness( Type, Index ) = MultiThicknessGroupsThickness     # Установить свойство (* )
        MultiThicknessGroupsThickness = Object.GetMultiThicknessGroupsThickness( Type, Index )  # Получить свойство (**)
        Object.SetMultiThicknessGroupsThickness( Type, Index, MultiThicknessGroupsThickness )   # Установить свойство (**)
        ```

        ### Входные параметры

        +-------------------------------------------------------------------------------------+
        | Type       | - Тип группы параметров из перечисления ksMultiThicknessGroupTypeEnum, |
        +-------------------------------------------------------------------------------------+
        | long Index | - Индекс параметров в группе.                                          |
        +-------------------------------------------------------------------------------------+
        """
        ...

    def SetMultiThicknessGroupsObjects(self, Type, Index, arg2):
        ...

    def SetMultiThicknessGroupsThickness(self, Type, Index, arg2):
        ...




class IShell(IModelObject):  # ishell.html
    """
    ## Интерфейс IShell

    Интерфейс операции Оболочка.
    """

    @property
    def DeletedFaces(self) -> typing.Any:  # ishell_deletedfaces.html
        """
        ## DeletedFaces - Массив удаляемых граней в виде SAFEARRAY DISPATCH - VT_ARRAY | VT_DISPATCH

        Тип данных: VARIANT

        Позволяет получать и устанавливать грань или массив граней, участвующих в операции оболочка.

        Массив граней возвращается в виде массива SAFEARRAY граней LPDISPATCH (VT_ARRAY | VT_DISPATCH).

        ### Синтаксис Automation

        ```
        DeletedFaces = Object.DeletedFaces       # Получить свойство (* )
        Object.DeletedFaces = DeletedFaces       # Установить свойство (* )
        DeletedFaces = Object.GetDeletedFaces()  # Получить свойство (**)
        Object.SetDeletedFaces( DeletedFaces )   # Установить свойство (**)
        ```
        """
        ...
    @DeletedFaces.setter
    def DeletedFaces(self, value: typing.Any): ...

    @property
    def Thickness(self) -> float:  # ishell_thickness.html
        """
        ## Thickness - Толщина стенки

        Тип данных: double

        ### Синтаксис Automation

        ```
        Thickness = Object.Thickness       # Получить свойство (* )
        Object.Thickness = Thickness       # Установить свойство (* )
        Thickness = Object.GetThickness()  # Получить свойство (**)
        Object.SetThickness( Thickness )   # Установить свойство (**)
        ```
        """
        ...
    @Thickness.setter
    def Thickness(self, value: float): ...

    @property
    def ThinType(self) -> int:  # ishell_thintype.html
        """
        ## ThinType - Направление формирования тонкой стенки

        Тип данных: из перечисления ksDirectionTypeEnum.

        ### Синтаксис Automation

        ```
        ThinType = Object.ThinType       # Получить свойство (* )
        Object.ThinType = ThinType       # Установить свойство (* )
        ThinType = Object.GetThinType()  # Получить свойство (**)
        Object.SetThinType( ThinType )   # Установить свойство (**)
        ```
        """
        ...
    @ThinType.setter
    def ThinType(self, value: int): ...

    def SetFaces(self, Faces, TangentFaces) -> bool:  # ishell_setfaces.html
        """
        ## SetFaces - Установить массив граней в виде SAFEARRAY DISPATCH - VT_ARRAY | VT_DISPATCH

        ### Синтаксис Automation

        ```
        BOOL SetFaces( VARIANT Faces, BOOL TangentFaces );
        ```

        ### Входные параметры

        +-----------------------------------------------+
        | Faces        | - Список SafeArray граней,     |
        +-----------------------------------------------+
        | TangentFaces | - Добавлять касательные грани. |
        +-----------------------------------------------+

        Версия: Компас v20

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...




class ISplitSolids(IModelObjects):  # isplitsolids.html
    """
    ## Интерфейс ISplitSolids

    Интерфейс коллекции операций Разрезать.

    Интерфейс позволяет получать и создавать операции Разрезать.

    Получить интерфейс коллекции можно, используя свойство контейнера поверхностей IModelContainer::SplitSolids.

    Версия: Компас v20
    """

    def Add(self) -> ISplitSolid:  # isplitsolids_add.html
        """
        ## Add - Создает новый элемент и добавляет его в коллекцию

        ### Синтаксис Automation

        ```
        LPDISPATCH Add();
        ```

        ### Возвращаемое значение

        - указатель на интерфейс ISplitSolid.
        """
        ...

    def SplitSolid(self, Index) -> ISplitSolid:  # isplitsolids_splitsolid.html
        """
        ## SplitSolid - Возвращает элемент, заданный по индексу или по имени

        Тип данных: Указатель на интерфейс ISplitSolid

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        SplitSolid = Object.SplitSolid( Index )     # Получить свойство(* )
        SplitSolid = Object.GetSplitSolid( Index )  # Получить свойство(**)
        ```
        """
        ...




class ISplitSolid(IModelObject):  # isplitsolid.html
    """
    ## Интерфейс ISplitSolid

    Интерфейс операции Разрезать.
    """

    @property
    def CutObjects(self) -> typing.Any:  # isplitsolid_cutobjects.html
        """
        ## CutObjects - Секущие объекты (эскизы или поверхности)

        Тип данных: VARIANT

        ### Синтаксис Automation

        ```
        CutObjects = Object.CutObjects       # Получить свойство (* )
        Object.CutObjects = CutObjects       # Установить свойство (* )
        CutObjects = Object.GetCutObjects()  # Получить свойство(**)
        Object.SetCutObjects( CutObjects )   # Установить свойство (**)
        ```
        """
        ...
    @CutObjects.setter
    def CutObjects(self, value: typing.Any): ...




class IFeaturePatterns(IModelObjects):  # ifeaturepatterns.html
    """
    ## Интерфейс IFeaturePatterns

    Интерфейс коллекции массивов.
    """

    def Add(self, Type):
        ...

    def FeaturePattern(self, Index):
        ...




class IFeaturePattern(IDispatch):  # ifeaturepattern.html
    """
    ## Интерфейс IFeaturePattern

    Базовый интерфейс работы с массивами.
    """

    @property
    def Application(self):
        ...

    @property
    def BasePoint(self) -> IModelObject:  # ifeaturepattern_basepoint.html
        """
        ## BasePoint - Базовая точка

        Тип данных: Указатель на интерфейс IModelObject

        ### Синтаксис Automation

        ```
        BasePoint = Object.BasePoint       # Получить свойство (* )
        Object.BasePoint = BasePoint       # Установить свойство (* )
        BasePoint = Object.GetBasePoint()  # Получить свойство (**)
        Object.SetBasePoint( BasePoint )   # Установить свойство (**)
        ```
        """
        ...
    @BasePoint.setter
    def BasePoint(self, value: IModelObject): ...

    @property
    def BasePointType(self) -> int:  # ifeaturepattern_basepointtype.html
        """
        ## BasePointType - Способ задания базовой точки

        Тип данных: из перечисления ksPatternBasePointTypeEnum

        ### Синтаксис Automation

        ```
        BasePointType = Object.BasePointType       # Получить свойство (* )
        BasePointType = Object.BasePointType       # Установить свойство (* )
        BasePointType = Object.GetBasePointType()  # Получить свойство (**)
        Object.SetBasePointType( BasePointType )   # Установить свойство (**)
        ```
        """
        ...
    @BasePointType.setter
    def BasePointType(self, value: int): ...

    @property
    def GeometryPattern(self) -> bool:  # ifeaturepattern_geometrypattern.html
        """
        ## GeometryPattern - Признак использования геометрического копирования

        Тип данных: BOOL

        Значения свойства:

        +--------------------------------------------------+
        | TRUE  | - используется геометрический массив,    |
        +--------------------------------------------------+
        | FALSE | - не используется геометрический массив. |
        +--------------------------------------------------+

        ### Синтаксис Automation

        ```
        GeometryPattern = Object.GeometryPattern       # Получить свойство (* )
        Object.GeometryPattern = GeometryPattern       # Установить свойство (* )
        GeometryPattern = Object.GetGeometryPattern()  # Получить свойство (**)
        Object.SetGeometryPattern( GeometryPattern )   # Установить свойство (**)
        ```
        """
        ...
    @GeometryPattern.setter
    def GeometryPattern(self, value: bool): ...

    @property
    def Hidden(self):
        ...
    @Hidden.setter
    def Hidden(self, value): ...

    @property
    def InitialObjects(self) -> typing.Any:  # ifeaturepattern_initialobjects.html
        """
        ## InitialObjects - Исходные объекты массива

        Тип данных: VARIANT

        Если копируется один объект, то возвращается VARIANT с типом VT_DISPATCH.

        Если копируется более одного объекта, то возвращается VARIANT с типом VT_ARRAY | VT_DISPATCH.

        ### Синтаксис Automation

        ```
        InitialObjects = Object.InitialObjects       # Получить свойство (* )
        Object.InitialObjects = InitialObjects       # Установить свойство (* )
        InitialObjects = Object.GetInitialObjects()  # Получить свойство (**)
        Object.SetInitialObjects( InitialObjects )   # Установить свойство (**)
        ```
        """
        ...
    @InitialObjects.setter
    def InitialObjects(self, value: typing.Any): ...

    @property
    def InstanceDeletedIndexes(self) -> typing.Any:  # ifeaturepattern_instancedeletedindexes.html
        """
        ## InstanceDeletedIndexes - Список удаленных экземпляров массива

        Тип данных: VARIANT

        Возвращается массив SafeArray VT_ARRAY | VT_I4.

        Свойства применяется для получения информации об удаленных копиях, а также управления составом удаленных копий.

        Информация об удаленной копии выдается в виде индексов копии в узоре копирования.

        В общем случае это два индекса по ортогональным осям в плоскости узора.

        Для копий по кривой используется один индекс (первый).

        Значения индексов начинаются с 1.

        ### Синтаксис Automation

        ```
        InstanceDeletedIndexes = Object.InstanceDeletedIndexes       # Получить свойство (* )
        Object.InstanceDeletedIndexes = InstanceDeletedIndexes       # Установить свойство (* )
        InstanceDeletedIndexes = Object.GetInstanceDeletedIndexes()  # Получить свойство (**)
        Object.SetInstanceDeletedIndexes( InstanceDeletedIndexes )   # Установить свойство (**)
        ```
        """
        ...
    @InstanceDeletedIndexes.setter
    def InstanceDeletedIndexes(self, value: typing.Any): ...

    @property
    def ModelObjectType(self):
        ...

    @property
    def Name(self):
        ...
    @Name.setter
    def Name(self, value): ...

    @property
    def Owner(self):
        ...

    @property
    def Parent(self):
        ...

    @property
    def Part(self):
        ...

    @property
    def Reference(self):
        ...

    @property
    def Type(self):
        ...

    @property
    def Valid(self):
        ...

    def AddInitialObjects(self, Objects) -> bool:  # ifeaturepattern_addinitialobjects.html
        """
        ## AddInitialObjects - Добавить объекты в массив копируемых объектов

        ### Синтаксис Automation

        ```
        BOOL AddInitialObjects( VARIANT Objects );
        ```

        ### Входные параметры

        +-----------------------------------------------------------------------------------------------------------+
        | Object | - объект - тип VARIANT-а VT_DISPATCH или список объектов - тип VARIANT-а VT_ARRAY | VT_DISPATCH. |
        +-----------------------------------------------------------------------------------------------------------+

        ### Возвращаемое значение

        +--------------------------+
        | TRUE | - в случае удачи. |
        +--------------------------+
        """
        ...

    def Clear(self) -> bool:  # ifeaturepattern_clear.html
        """
        ## Clear - Очистить список исходных объектов массива

        ### Синтаксис Automation

        ```
        BOOL Clear();
        ```

        ### Возвращаемое значение

        +--------------------------+
        | TRUE | - в случае удачи. |
        +--------------------------+
        """
        ...

    def Destroy(self) -> bool:  # ifeaturepattern_destroy.html
        """
        ## Destroy - Разрушить массив

        ### Синтаксис Automation

        ```
        BOOL Destroy();
        ```

        ### Возвращаемое значение

        +--------------------------+
        | TRUE | - в случае удачи. |
        +--------------------------+
        """
        ...

    def Exemplar(self, Index1, Index2) -> IModelObject:  # ifeaturepattern_exemplar.html
        """
        ## Exemplar - Получить экземпляр по индексу (или индексам)

        Тип данных: Указатель на интерфейс IModelObject

        Свойство доступно только для чтения.

        Значения индексов начинаются с 1.

        В массиве по кривой используется один индекс (первый).

        ### Синтаксис Automation

        ```
        Exemplar = Object.Exemplar( index1, index2 )     # Получить свойство (* )
        Exemplar = Object.GetExemplar( index1, index2 )  # Получить свойство (**)
        ```

        ### Входные параметры

        index1, index2 - индекс экземпляра
        """
        ...

    def GetExemplarsCounts(self) -> bool:  # ifeaturepattern_getexemplarscounts.html
        """
        ## GetExemplarsCounts - Получить количество экземпляров

        Два направления копирования имеют массивы по сетке и по концентрической сетке.

        Для массивов остальных типов Count2 равен 1.

        ### Синтаксис Automation

        ```
        BOOL GetExemplarsCounts( long * Count1, long * Count2 );
        ```

        ### Выходные параметры

        +----------------------------------------------------------------------+
        | Count1 | - количество экземпляров в первом направлении копирования,  |
        +----------------------------------------------------------------------+
        | Count2 | - количество экземпляров во втором направлении копирования. |
        +----------------------------------------------------------------------+

        ### Возвращаемое значение

        +--------------------------+
        | TRUE | - в случае удачи. |
        +--------------------------+
        """
        ...

    def IsInitialObject(self, Object) -> bool:  # ifeaturepattern_isinitialobject.html
        """
        ## IsInitialObject - Проверка: является ли объект исходным для массива

        ### Синтаксис Automation

        ```
        BOOL IsInitialObject( IModelObject * Object );
        ```

        ### Входные параметры

        +----------------------------------------------+
        | Object | - указатель на объект IModelObject. |
        +----------------------------------------------+

        ### Возвращаемое значение

        +---------------------------------------------------------+
        | TRUE | - если объект есть в списке копируемых объектов. |
        +---------------------------------------------------------+
        """
        ...

    def IsSuitableObject(self, Object) -> bool:  # ifeaturepattern_issuitableobject.html
        """
        ## IsSuitableObject - Проверка пригодности объекта для операции

        ### Синтаксис Automation

        ```
        BOOL IsSuitableObject( IModelObject * Object );
        ```

        ### Входные параметры

        +----------------------------------------------+
        | Object | - указатель на объект IModelObject. |
        +----------------------------------------------+

        ### Возвращаемое значение

        +------------------------------------------------------------------------+
        | TRUE | - указанный объект можно скопировать с помощью данной операции. |
        +------------------------------------------------------------------------+
        """
        ...

    def Update(self):
        ...




class ILinearPattern(IFeaturePattern):  # ilinearpattern.html
    """
    ## Интерфейс ILinearPattern

    Интерфейс параметров операции копирования по сетке.
    """

    @property
    def Angle1(self) -> float:  # ilinearpattern_angle1.html
        """
        ## Angle1 - Угол наклона первой оси сетки

        Тип данных: double

        ### Синтаксис Automation

        ```
        Angle1 = Object.Angle1       # Получить свойство (* )
        Object.Angle1 = Angle1       # Установить свойство (* )
        Angle1 = Object.GetAngle1()  # Получить свойство (**)
        Object.SetAngle1( Angle1 )   # Установить свойство (**)
        ```
        """
        ...
    @Angle1.setter
    def Angle1(self, value: float): ...

    @property
    def Angle2(self) -> float:  # ilinearpattern_angle2.html
        """
        ## Angle2 - Угол наклона второй оси сетки

        Тип данных: double

        ### Синтаксис Automation

        ```
        Angle2 = Object.Angle2       # Получить свойство (* )
        Object.Angle2 = Angle2       # Установить свойство (* )
        Angle2 = Object.GetAngle2()  # Получить свойство (**)
        Object.SetAngle2( Angle2 )   # Установить свойство (**)
        ```
        """
        ...
    @Angle2.setter
    def Angle2(self, value: float): ...

    @property
    def Axis1(self) -> IModelObject:  # ilinearpattern_axis1.html
        """
        ## Axis1 - Первая ось сетки массива

        Тип данных: указатель на интерфейс IModelObject

        ### Синтаксис Automation

        ```
        Axis1 = Object.Axis1       # Получить свойство (* )
        Object.Axis1 = Axis1       # Установить свойство (* )
        Axis1 = Object.GetAxis1()  # Получить свойство (**)
        Object.SetAxis1( Axis1 )   # Установить свойство (**)
        ```
        """
        ...
    @Axis1.setter
    def Axis1(self, value: IModelObject): ...

    @property
    def Axis2(self) -> IModelObject:  # ilinearpattern_axis2.html
        """
        ## Axis2 - Вторая ось сетки массива

        Тип данных: указатель на интерфейс IModelObject

        ### Синтаксис Automation

        ```
        Axis2 = Object.Axis2       # Получить свойство (* )
        Object.Axis2 = Axis2       # Установить свойство (* )
        Axis2 = Object.GetAxis2()  # Получить свойство (**)
        Object.SetAxis2( Axis2 )   # Установить свойство (**)
        ```
        """
        ...
    @Axis2.setter
    def Axis2(self, value: IModelObject): ...

    @property
    def BoundaryInstancesStepFactor1(self) -> bool:  # ilinearpattern_boundaryinstancesstepfactor1.html
        """
        ## BoundaryInstancesStepFactor1- Интерпретация значения шага копирования вдоль первой оси сетки

        Тип данных: BOOL

        Значения свойства:

        +----------------------------------------------------------+
        | TRUE  | - шаг – расстояние между крайними экземплярами,  |
        +----------------------------------------------------------+
        | FALSE | - шаг – расстояние между соседними экземплярами. |
        +----------------------------------------------------------+

        ### Синтаксис Automation

        ```
        BoundaryInstancesStepFactor1 = Object.BoundaryInstancesStepFactor1       # Получить свойство (* )
        Object.BoundaryInstancesStepFactor1 = BoundaryInstancesStepFactor1       # Установить свойство (* )
        BoundaryInstancesStepFactor1 = Object.GetBoundaryInstancesStepFactor1()  # Получить свойство (**)
        Object.SetBoundaryInstancesStepFactor1( BoundaryInstancesStepFactor1 )   # Установить свойство (**)
        ```
        """
        ...
    @BoundaryInstancesStepFactor1.setter
    def BoundaryInstancesStepFactor1(self, value: bool): ...

    @property
    def BoundaryInstancesStepFactor2(self) -> bool:  # ilinearpattern_boundaryinstancesstepfactor2.html
        """
        ## BoundaryInstancesStepFactor2 - Интерпретация значения шага копирования вдоль второй оси сетки

        Тип данных: BOOL

        Значения свойства:

        +----------------------------------------------------------+
        | TRUE  | - шаг – расстояние между крайними экземплярами,  |
        +----------------------------------------------------------+
        | FALSE | - шаг – расстояние между соседними экземплярами. |
        +----------------------------------------------------------+

        ### Синтаксис Automation

        ```
        BoundaryInstancesStepFactor2 = Object.BoundaryInstancesStepFactor2       # Получить свойство (* )
        Object.BoundaryInstancesStepFactor2 = BoundaryInstancesStepFactor2       # Установить свойство (* )
        BoundaryInstancesStepFactor2 = Object.GetBoundaryInstancesStepFactor2()  # Получить свойство (**)
        Object.SetBoundaryInstancesStepFactor2( BoundaryInstancesStepFactor2 )   # Установить свойство (**)
        ```
        """
        ...
    @BoundaryInstancesStepFactor2.setter
    def BoundaryInstancesStepFactor2(self, value: bool): ...

    @property
    def BuildingType(self) -> int:  # ilinearpattern_buildingtype.html
        """
        ## BuildingType - Способ построения массива

        Тип данных: из перечисления ksLinearPatternBuildingTypeEnum

        ### Синтаксис Automation

        ```
        BuildingType = Object.BuildingType       # Получить свойство (* )
        Object.BuildingType = BuildingType       # Установить свойство (* )
        BuildingType = Object.GetBuildingType()  # Получить свойство (**)
        Object.SetBuildingType( BuildingType )   # Установить свойство (**)
        ```
        """
        ...
    @BuildingType.setter
    def BuildingType(self, value: int): ...

    @property
    def Count1(self) -> int:  # ilinearpattern_count1.html
        """
        ## Count1 - Количество экземпляров по первой оси сетки

        Тип данных: long

        ### Синтаксис Automation

        ```
        Count1 = Object.Count1       # Получить свойство (* )
        Object.Count1 = Count1       # Установить свойство (* )
        Count1 = Object.GetCount1()  # Получить свойство (**)
        Object.SetCount1( Count1 )   # Установить свойство (**)
        ```
        """
        ...
    @Count1.setter
    def Count1(self, value: int): ...

    @property
    def Count2(self) -> int:  # ilinearpattern_count2.html
        """
        ## Count2 - Количество экземпляров по второй оси сетки

        Тип данных: long

        ### Синтаксис Automation

        ```
        Count2 = Object.Count2       # Получить свойство (* )
        Object.Count2 = Count2       # Установить свойство (* )
        Count2 = Object.GetCount2()  # Получить свойство (**)
        Object.SetCount2( Count2 )   # Установить свойство (**)
        ```
        """
        ...
    @Count2.setter
    def Count2(self, value: int): ...

    @property
    def Direction1(self) -> bool:  # ilinearpattern_direction1.html
        """
        ## Direction1 - Направление копирования вдоль первой оси сетки

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        Direction1 = Object.Direction1       # Получить свойство (* )
        Object.Direction1 = Direction1       # Установить свойство (* )
        Direction1 = Object.GetDirection1()  # Получить свойство (**)
        Object.SetDirection1( Direction1 )   # Установить свойство (**)
        ```
        """
        ...
    @Direction1.setter
    def Direction1(self, value: bool): ...

    @property
    def Direction2(self) -> bool:  # ilinearpattern_direction2.html
        """
        ## Direction2 - Направление копирования вдоль второй оси сетки

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        Direction2 = Object.Direction2       # Получить свойство (* )
        Object.Direction2 = Direction2       # Установить свойство (* )
        Direction2 = Object.GetDirection2()  # Получить свойство (**)
        Object.SetDirection2( Direction2 )   # Установить свойство (**)
        ```
        """
        ...
    @Direction2.setter
    def Direction2(self, value: bool): ...

    @property
    def Step1(self) -> float:  # ilinearpattern_step1.html
        """
        ## Step1 - Шаг копирования по первой оси сетки

        Тип данных: double

        ### Синтаксис Automation

        ```
        Step1 = Object.Step1       # Получить свойство (* )
        Object.Step1 = Step1       # Установить свойство (* )
        Step1 = Object.GetStep1()  # Получить свойство (**)
        Object.SetStep1( Step1 )   # Установить свойство (**)
        ```
        """
        ...
    @Step1.setter
    def Step1(self, value: float): ...

    @property
    def Step2(self) -> float:  # ilinearpattern_step2.html
        """
        ## Step2 - Шаг копирования по второй оси сетки

        Тип данных: double

        ### Синтаксис Automation

        ```
        Step2 = Object.Step2       # Получить свойство (* )
        Object.Step2 = Step2       # Установить свойство (* )
        Step2 = Object.GetStep2()  # Получить свойство (**)
        Object.SetStep2( Step2 )   # Установить свойство (**)
        ```
        """
        ...
    @Step2.setter
    def Step2(self, value: float): ...

    @property
    def Vector1(self) -> IVector3D:  # ilinearpattern_vector1.html
        """
        ## Vector1 - Параметры вектора, задающего направление первой оси сетки

        Тип данных: указатель на интерфейс IVector3D

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        Vector1 = Object.Vector1       # Получить свойство (* )
        Vector1 = Object.GetVector1()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def Vector2(self) -> IVector3D:  # ilinearpattern_vector2.html
        """
        ## Vector2 - Параметры вектора, задающего направление второй оси сетки

        Тип данных: указатель на интерфейс IVector3D

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        Vector2 = Object.Vector2       # Получить свойство (* )
        Vector2 = Object.GetVector2()  # Получить свойство (**)
        ```
        """
        ...

    def GetBaseExemplarPlacement(self) -> bool:  # ilinearpattern_getbaseexemplarplacement.html
        """
        ## GetBaseExemplarPlacement - Получить положение базового экземпляра в массиве

        ### Синтаксис Automation

        ```
        BOOL GetBaseExemplarPlacement( long Index1, long Index2 );
        ```

        ### Входные параметры

        +------------------------------------------+
        | index1 | - индекс экземпляра по 1-й оси, |
        +------------------------------------------+
        | index2 | - индекс экземпляра по 2-й оси. |
        +------------------------------------------+

        ### Возвращаемое значение

        +--------------------------+
        | TRUE | - в случае удачи. |
        +--------------------------+
        """
        ...

    def SetBaseExemplarPlacement(self, Index1, Index2) -> bool:  # ilinearpattern_setbaseexemplarplacement.html
        """
        ## SetBaseExemplarPlacement - Задать положение базового экземпляра в массиве

        ### Синтаксис Automation

        ```
        BOOL SetBaseExemplarPlacement( long Index1, long Index2 );
        ```

        ### Входные параметры

        +------------------------------------------+
        | index1 | - индекс экземпляра по 1-й оси, |
        +------------------------------------------+
        | index2 | - индекс экземпляра по 2-й оси. |
        +------------------------------------------+

        ### Возвращаемое значение

        +--------------------------+
        | TRUE | - в случае удачи. |
        +--------------------------+
        """
        ...




class ICircularPattern(IFeaturePattern):  # icircularpattern.html
    """
    ## Интерфейс ICircularPattern

    Интерфейс параметров операции копирования по концентрической сетке.
    """

    @property
    def Axis(self) -> IModelObject:  # icircularpattern_axis.html
        """
        ## Axis - Ось массива

        Тип данных: указатель на интерфейс IModelObject

        ### Синтаксис Automation

        ```
        Axis = Object.Axis       # Получить свойство (* )
        Object.Axis = Axis       # Установить свойство (* )
        Axis = Object.GetAxis()  # Получить свойство (**)
        Object.SetAxis( Axis )   # Установить свойство (**)
        ```
        """
        ...
    @Axis.setter
    def Axis(self, value: IModelObject): ...

    @property
    def BoundaryInstancesStepFactor1(self) -> bool:  # icircularpattern_boundaryinstancesstepfactor1.html
        """
        ## BoundaryInstancesStepFactor1 - Интерпретация значения шага копирования в радиальном направлении

        Тип данных: BOOL

        Значения свойства:

        +----------------------------------------------------------+
        | TRUE  | - шаг – расстояние между крайними экземплярами,  |
        +----------------------------------------------------------+
        | FALSE | - шаг – расстояние между соседними экземплярами. |
        +----------------------------------------------------------+

        ### Синтаксис Automation

        ```
        BoundaryInstancesStepFactor1 = Object.BoundaryInstancesStepFactor1       # Получить свойство (* )
        Object.BoundaryInstancesStepFactor1 = BoundaryInstancesStepFactor1       # Установить свойство (* )
        BoundaryInstancesStepFactor1 = Object.GetBoundaryInstancesStepFactor1()  # Получить свойство (**)
        Object.SetBoundaryInstancesStepFactor1( BoundaryInstancesStepFactor1 )   # Установить свойство (**)
        ```
        """
        ...
    @BoundaryInstancesStepFactor1.setter
    def BoundaryInstancesStepFactor1(self, value: bool): ...

    @property
    def BoundaryInstancesStepFactor2(self) -> bool:  # icircularpattern_boundaryinstancesstepfactor2.html
        """
        ## BoundaryInstancesStepFactor2 - Интерпретация значения шага копирования в кольцевом направлении

        Тип данных: BOOL

        Значения свойства:

        +----------------------------------------------------+
        | TRUE  | - шаг – угол между крайними экземплярами,  |
        +----------------------------------------------------+
        | FALSE | - шаг – угол между соседними экземплярами. |
        +----------------------------------------------------+

        ### Синтаксис Automation

        ```
        BoundaryInstancesStepFactor2 = Object.BoundaryInstancesStepFactor2       # Получить свойство (* )
        Object.BoundaryInstancesStepFactor2 = BoundaryInstancesStepFactor2       # Установить свойство (* )
        BoundaryInstancesStepFactor2 = Object.GetBoundaryInstancesStepFactor2()  # Получить свойство (**)
        Object.SetBoundaryInstancesStepFactor2( BoundaryInstancesStepFactor2 )   # Установить свойство (**)
        ```
        """
        ...
    @BoundaryInstancesStepFactor2.setter
    def BoundaryInstancesStepFactor2(self, value: bool): ...

    @property
    def BuildingType(self) -> int:  # icircularpattern_buildingtype.html
        """
        ## BuildingType - Способ построения массива

        Тип данных: ksCircularPatternBuildingTypeEnum

        ### Синтаксис Automation

        ```
        BuildingType = Object.BuildingType       # Получить свойство (* )
        Object.BuildingType = BuildingType       # Установить свойство (* )
        BuildingType = Object.GetBuildingType()  # Получить свойство (**)
        Object.SetBuildingType( BuildingType )   # Установить свойство (**)
        ```
        """
        ...
    @BuildingType.setter
    def BuildingType(self, value: int): ...

    @property
    def Count1(self) -> int:  # icircularpattern_count1.html
        """
        ## Count1 - Количество экземпляров в радиальном направлении

        Тип данных: long

        ### Синтаксис Automation

        ```
        Count1 = Object.Count1       # Получить свойство (* )
        Object.Count1 = Count1       # Установить свойство (* )
        Count1 = Object.GetCount1()  # Получить свойство (**)
        Object.SetCount1( Count1 )   # Установить свойство (**)
        ```
        """
        ...
    @Count1.setter
    def Count1(self, value: int): ...

    @property
    def Count2(self) -> int:  # icircularpattern_count2.html
        """
        ## Count2 - Количество экземпляров в кольцевом направлении

        Тип данных: long

        ### Синтаксис Automation

        ```
        Count2 = Object.Count2       # Получить свойство (* )
        Object.Count2 = Count2       # Установить свойство (* )
        Count2 = Object.GetCount2()  # Получить свойство (**)
        Object.SetCount2( Count2 )   # Установить свойство (**)
        ```
        """
        ...
    @Count2.setter
    def Count2(self, value: int): ...

    @property
    def ReverseDirection(self) -> bool:  # icircularpattern_reversedirection.html
        """
        ## ReverseDirection - Направление построения массива

        Тип данных: BOOL

        Значения свойства:

        +---------------------------------+
        | TRUE  | - обратное направление, |
        +---------------------------------+
        | FALSE | - прямое направление.   |
        +---------------------------------+

        ### Синтаксис Automation

        ```
        ReverseDirection = Object.ReverseDirection       # Получить свойство (* )
        Object.ReverseDirection = ReverseDirection       # Установить свойство (* )
        ReverseDirection = Object.GetReverseDirection()  # Получить свойство (**)
        Object.SetReverseDirection( ReverseDirection )   # Установить свойство (**)
        ```
        """
        ...
    @ReverseDirection.setter
    def ReverseDirection(self, value: bool): ...

    @property
    def SaveInitialOrientation(self) -> bool:  # icircularpattern_saveinitialorientation.html
        """
        ## SaveInitialOrientation - Ориентация экземпляров массива

        Тип данных: BOOL

        Значения свойства:

        +----------------------------------------------------+
        | TRUE  | - сохранять исходную ориентацию,           |
        +----------------------------------------------------+
        | FALSE | - доворачивать до радиального направления. |
        +----------------------------------------------------+

        ### Синтаксис Automation

        ```
        SaveInitialOrientation = Object.SaveInitialOrientation       # Получить свойство (* )
        Object.SaveInitialOrientation = SaveInitialOrientation       # Установить свойство (* )
        SaveInitialOrientation = Object.GetSaveInitialOrientation()  # Получить свойство (**)
        Object.SetSaveInitialOrientation( SaveInitialOrientation )   # Установить свойство (**)
        ```
        """
        ...
    @SaveInitialOrientation.setter
    def SaveInitialOrientation(self, value: bool): ...

    @property
    def Step1(self) -> float:  # icircularpattern_step1.html
        """
        ## Step1 - Шаг копирования в радиальном направлении

        Тип данных: double

        ### Синтаксис Automation

        ```
        Step1 = Object.Step1       # Получить свойство (* )
        Object.Step1 = Step1       # Установить свойство (* )
        Step1 = Object.GetStep1()  # Получить свойство (**)
        Object.SetStep1( Step1 )   # Установить свойство (**)
        ```
        """
        ...
    @Step1.setter
    def Step1(self, value: float): ...

    @property
    def Step2(self) -> float:  # icircularpattern_step2.html
        """
        ## Step2 - Угловой шаг (градусы) - шаг копирования в кольцевом направлении

        Тип данных: double

        ### Синтаксис Automation

        ```
        Step2 = Object.Step2       # Получить свойство (* )
        Object.Step2 = Step2       # Установить свойство (* )
        Step2 = Object.GetStep2()  # Получить свойство (**)
        Object.SetStep2( Step2 )   # Установить свойство (**)
        ```
        """
        ...
    @Step2.setter
    def Step2(self, value: float): ...

    @property
    def StepByAxis(self) -> float:  # icircularpattern_stepbyaxis.html
        """
        ## StepByAxis - Шаг вдоль оси

        Тип данных: double

        ### Синтаксис Automation

        ```
        StepByAxis = Object.StepByAxis       # Получить свойство (* )
        Object.StepByAxis = StepByAxis       # Установить свойство (* )
        StepByAxis = Object.GetStepByAxis()  # Получить свойство (**)
        Object.SetStepByAxis( StepByAxis )   # Установить свойство (**)
        ```
        """
        ...
    @StepByAxis.setter
    def StepByAxis(self, value: float): ...

    def GetBaseExemplarPlacement(self) -> bool:  # icircularpattern_getbaseexemplarplacement.html
        """
        ## GetBaseExemplarPlacement - Получить положение базового экземпляра в массиве

        ### Синтаксис Automation

        ```
        BOOL GetBaseExemplarPlacement( long Index1, long Index2 );
        ```

        ### Входные параметры

        +----------------------------------------------------------+
        | index1 | - индекс экземпляра по радиальному направлению, |
        +----------------------------------------------------------+
        | index2 | - индекс экземпляра по кольцевому направлению.  |
        +----------------------------------------------------------+

        ### Возвращаемое значение

        +--------------------------+
        | TRUE | - в случае удачи. |
        +--------------------------+
        """
        ...

    def SetBaseExemplarPlacement(self, Index1, Index2) -> bool:  # icircularpattern_setbaseexemplarplacement.html
        """
        ## SetBaseExemplarPlacement - Задать положение базового экземпляра в массиве

        ### Синтаксис Automation

        ```
        BOOL SetBaseExemplarPlacement( long Index1, long Index2 );
        ```

        ### Входные параметры

        +----------------------------------------------------------+
        | index1 | - индекс экземпляра по радиальному направлению, |
        +----------------------------------------------------------+
        | index2 | - индекс экземпляра по кольцевому направлению.  |
        +----------------------------------------------------------+

        ### Возвращаемое значение

        +--------------------------+
        | TRUE | - в случае удачи. |
        +--------------------------+
        """
        ...




class IPathPattern(IFeaturePattern):  # ipathpattern.html
    """
    ## Интерфейс IPathPattern

    Интерфейс параметров операции копирования вдоль кривой.
    """

    @property
    def BoundaryInstancesStepFactor(self) -> bool:  # ipathpattern_boundaryinstancesstepfactor.html
        """
        ## BoundaryInstancesStepFactor - Интерпретация значения шага

        Тип данных: BOOL

        Значения свойства:

        +----------------------------------------------------------+
        | TRUE  | - шаг – расстояние между крайними экземплярами,  |
        +----------------------------------------------------------+
        | FALSE | - шаг – расстояние между соседними экземплярами. |
        +----------------------------------------------------------+

        ### Синтаксис Automation

        ```
        BoundaryInstancesStepFactor = Object.BoundaryInstancesStepFactor       # Получить свойство (* )
        Object.BoundaryInstancesStepFactor = BoundaryInstancesStepFactor       # Установить свойство (* )
        BoundaryInstancesStepFactor = Object.GetBoundaryInstancesStepFactor()  # Получить свойство (**)
        Object.SetBoundaryInstancesStepFactor( BoundaryInstancesStepFactor )   # Установить свойство (**)
        ```
        """
        ...
    @BoundaryInstancesStepFactor.setter
    def BoundaryInstancesStepFactor(self, value: bool): ...

    @property
    def ByStep(self) -> bool:  # ipathpattern_bystep.html
        """
        ## ByStep - Способ построения массива

        Тип данных: BOOL

        Значения свойства:

        +------------------------------------+
        | TRUE  | - по шагу,                 |
        +------------------------------------+
        | FALSE | - вдоль всей направляющей. |
        +------------------------------------+

        ### Синтаксис Automation

        ```
        ByStep = Object.ByStep       # Получить свойство (* )
        Object.ByStep = ByStep       # Установить свойство (* )
        ByStep = Object.GetByStep()  # Получить свойство (**)
        Object.SetByStep( ByStep )   # Установить свойство (**)
        ```
        """
        ...
    @ByStep.setter
    def ByStep(self, value: bool): ...

    @property
    def Count(self) -> int:  # ipathpattern_count.html
        """
        ## Count - Количество экземпляров

        Тип данных: long

        ### Синтаксис Automation

        ```
        Count = Object.Count       # Получить свойство (* )
        Object.Count = Count       # Установить свойство (* )
        Count = Object.GetCount()  # Получить свойство (**)
        Object.SetCount( Count )   # Установить свойство (**)
        ```
        """
        ...
    @Count.setter
    def Count(self, value: int): ...

    @property
    def Curves(self) -> typing.Any:  # ipathpattern_curves.html
        """
        ## Curves - Массив кривых - траектория копирования

        Тип данных: VARIANT

        Если используется одна кривая, то тип VARIANT-а VT_DISPATCH.

        Если используется несколько кривых, то тип VARIANT-а VT_ARRAY | VT_DISPATCH.

        ### Синтаксис Automation

        ```
        Curves = Object.Curves       # Получить свойство (* )
        Object.Curves = Curves       # Установить свойство (* )
        Curves = Object.GetCurves()  # Получить свойство (**)
        Object.SetCurves( Curves )   # Установить свойство (**)
        ```
        """
        ...
    @Curves.setter
    def Curves(self, value: typing.Any): ...

    @property
    def ReverseDirection(self) -> bool:  # ipathpattern_reversedirection.html
        """
        ## ReverseDirection - Направление построения массива

        Тип данных: BOOL

        Значения свойства:

        +---------------------------------+
        | TRUE  | - обратное направление, |
        +---------------------------------+
        | FALSE | - прямое направление.   |
        +---------------------------------+

        ### Синтаксис Automation

        ```
        ReverseDirection = Object.ReverseDirection       # Получить свойство (* )
        Object.ReverseDirection = ReverseDirection       # Установить свойство (* )
        ReverseDirection = Object.GetReverseDirection()  # Получить свойство (**)
        Object.SetReverseDirection( ReverseDirection )   # Установить свойство (**)
        ```
        """
        ...
    @ReverseDirection.setter
    def ReverseDirection(self, value: bool): ...

    @property
    def SaveInitialOrientation(self) -> bool:  # ipathpattern_saveinitialorientation.html
        """
        ## SaveInitialOrientation - Ориентация экземпляров массива

        Тип данных: BOOL

        Значения свойства:

        +-------------------------------------------------+
        | TRUE  | - сохранять исходную ориентацию,        |
        +-------------------------------------------------+
        | FALSE | - доворачивать до нормали к траектории. |
        +-------------------------------------------------+

        ### Синтаксис Automation

        ```
        SaveInitialOrientation = Object.SaveInitialOrientation       # Получить свойство (* )
        Object.SaveInitialOrientation = SaveInitialOrientation       # Установить свойство (* )
        SaveInitialOrientation = Object.GetSaveInitialOrientation()  # Получить свойство (**)
        Object.SetSaveInitialOrientation( SaveInitialOrientation )   # Установить свойство (**)
        ```
        """
        ...
    @SaveInitialOrientation.setter
    def SaveInitialOrientation(self, value: bool): ...

    @property
    def StartingPoint(self) -> IModelObject:  # ipathpattern_startingpoint.html
        """
        ## StartingPoint - Начальная точка для замкнутой траектории

        Тип данных: указатель на интерфейс IModelObject

        ### Синтаксис Automation

        ```
        StartingPoint = Object.StartingPoint       # Получить свойство (* )
        Object.StartingPoint = StartingPoint       # Установить свойство (* )
        StartingPoint = Object.GetStartingPoint()  # Получить свойство (**)
        Object.SetStartingPoint( StartingPoint )   # Установить свойство (**)
        ```
        """
        ...
    @StartingPoint.setter
    def StartingPoint(self, value: IModelObject): ...

    @property
    def Step(self) -> float:  # ipathpattern_step.html
        """
        ## Step - Значение шага (измеряется вдоль траектории)

        Тип данных: double

        ### Синтаксис Automation

        ```
        Step = Object.Step       # Получить свойство (* )
        Object.Step = Step       # Установить свойство (* )
        Step = Object.GetStep()  # Получить свойство (**)
        Object.SetStep( Step )   # Установить свойство (**)
        ```
        """
        ...
    @Step.setter
    def Step(self, value: float): ...




class IDerivedPattern(IFeaturePattern):  # iderivedpattern.html
    """
    ## Интерфейс IDerivedPattern

    Интерфейс параметров массива компонентов по образцу.
    """

    @property
    def AllowDeleted(self) -> bool:  # iderivedpattern_allowdeleted.html
        """
        ## AllowDeleted - Учитывать удаленные

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        AllowDeleted = Object.AllowDeleted       # Получить свойство (* )
        Object.AllowDeleted = AllowDeleted       # Установить свойство (* )
        AllowDeleted = Object.GetAllowDeleted()  # Получить свойство (**)
        Object.SetAllowDeleted( AllowDeleted )   # Установить свойство (**)
        ```
        """
        ...
    @AllowDeleted.setter
    def AllowDeleted(self, value: bool): ...

    @property
    def AllowNesting(self) -> bool:  # iderivedpattern_allownesting.html
        """
        ## AllowNesting - Учитывать вложенность

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        AllowNesting = Object.AllowNesting       # Получить свойство (* )
        Object.AllowNesting = AllowNesting       # Установить свойство (* )
        AllowNesting = Object.GetAllowNesting()  # Получить свойство (**)
        Object.SetAllowNesting( AllowNesting )   # Установить свойство (**)
        ```
        """
        ...
    @AllowNesting.setter
    def AllowNesting(self, value: bool): ...

    @property
    def MasterPattern(self) -> IModelObject:  # iderivedpattern_masterpattern.html
        """
        ## MasterPattern - Массив - образец

        Тип данных: указатель на интерфейс IModelObject

        ### Синтаксис Automation

        ```
        MasterPattern = Object.MasterPattern       # Получить свойство (* )
        Object.MasterPattern = MasterPattern       # Установить свойство (* )
        MasterPattern = Object.GetMasterPattern()  # Получить свойство (**)
        Object.SetMasterPattern( MasterPattern )   # Установить свойство (**)
        ```
        """
        ...
    @MasterPattern.setter
    def MasterPattern(self, value: IModelObject): ...

    @property
    def OrientBySample(self) -> bool:  # iderivedpattern_orientbysample.html
        """
        ## OrientBySample - Ориентировать по образцу

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        OrientBySample = Object.OrientBySample       # Получить свойство (* )
        Object.OrientBySample = OrientBySample       # Установить свойство (* )
        OrientBySample = Object.GetOrientBySample()  # Получить свойство (**)
        Object.SetOrientBySample( OrientBySample )   # Установить свойство (**)
        ```
        """
        ...
    @OrientBySample.setter
    def OrientBySample(self, value: bool): ...

    @property
    def SampleExemplar(self) -> IModelObject:  # iderivedpattern_sampleexemplar.html
        """
        ## SampleExemplar - Экземпляр массива-образца, относительно которого будет копироваться положение

        Тип данных: указатель на интерфейс IModelObject

        ### Синтаксис Automation

        ```
        SampleExemplar = Object.SampleExemplar       # Получить свойство (* )
        Object.SampleExemplar = SampleExemplar       # Установить свойство (* )
        OrientBySample = Object.GetSampleExemplar()  # Получить свойство (**)
        Object.SetSampleExemplar( SampleExemplar )   # Установить свойство (**)
        ```
        """
        ...
    @SampleExemplar.setter
    def SampleExemplar(self, value: IModelObject): ...




class IMirrorPattern(IFeaturePattern):  # imirrorpattern.html
    """
    ## Интерфейс IMirrorPattern

    Интерфейс параметров операции зеркального копирования.
    """

    @property
    def Plane(self) -> IModelObject:  # imirrorpattern_plane.html
        """
        ## Plane - Плоскость симметрии

        Тип данных: указатель на интерфейс IModelObject

        ### Синтаксис Automation

        ```
        Plane = Object.Plane       # Получить свойство (* )
        Object.Plane = Plane       # Установить свойство (* )
        Plane = Object.GetPlane()  # Получить свойство (**)
        Object.SetPlane( Plane )   # Установить свойство (**)
        ```
        """
        ...
    @Plane.setter
    def Plane(self, value: IModelObject): ...

    @property
    def SaveInitialObjects(self) -> bool:  # imirrorpattern_saveinitialobjects.html
        """
        ## SaveInitialObjects - Признак того, что исходные объекты остаются (не удаляются)

        Тип данных: BOOL

        Значение свойства:

        +--------------------------------------+
        | TRUE | - оставлять исходные объекты. |
        +--------------------------------------+

        1. Свойство работает только для o3d_mirrorAllOperation.

        2. У других операций зеркального копирования возможность скрыть экземпляры отсутствует.

        ### Синтаксис Automation

        ```
        SaveInitialObjects = Object.SaveInitialObjects       # Получить свойство (* )
        Object.SaveInitialObjects = SaveInitialObjects       # Установить свойство (* )
        SaveInitialObjects = Object.GetSaveInitialObjects()  # Получить свойство (**)
        Object.SetSaveInitialObjects( SaveInitialObjects )   # Установить свойство (**)
        ```
        """
        ...
    @SaveInitialObjects.setter
    def SaveInitialObjects(self, value: bool): ...




class IPointDrivenPattern(IFeaturePattern):  # ipointdrivenpattern.html
    """
    ## Интерфейс IPointDrivenPattern

    Интерфейс параметров операции копирования по точкам.
    """

    @property
    def DrivenObjects(self) -> typing.Any:  # ipointdrivenpattern_drivenobjects.html
        """
        ## DrivenObjects -Управляющие объекты для копирования

        Тип данных: VARIANT

        ### Синтаксис Automation

        ```
        DrivenObjects = Object.DrivenObjects       # Получить свойство (* )
        Object.DrivenObjects = DrivenObjects       # Установить свойство (* )
        DrivenObjects = Object.GetDrivenObjects()  # Получить свойство (**)
        Object.SetDrivenObjects( DrivenObjects )   # Установить свойство (**)
        ```
        """
        ...
    @DrivenObjects.setter
    def DrivenObjects(self, value: typing.Any): ...

    @property
    def OrientationObject(self) -> IModelObject:  # ipointdrivenpattern_orientationobject.html
        """
        ## OrientationObject - Ориентирующий объект

        Тип данных: Указатель на интерфейс IModelObject

        ### Синтаксис Automation

        ```
        OrientationObject = Object.OrientationObject       # Получить свойство (* )
        Object.OrientationObject = OrientationObject       # Установить свойство (* )
        OrientationObject = Object.GetOrientationObject()  # Получить свойство (**)
        Object.SetOrientationObject( OrientationObject )   # Установить свойство (**)
        ```
        """
        ...
    @OrientationObject.setter
    def OrientationObject(self, value: IModelObject): ...

    @property
    def OrientationType(self) -> int:  # ipointdrivenpattern_orientationtype.html
        """
        ## OrientationType - Способ ориентации экземпляров массива

        Тип данных: из перечисления ksPatternExemplarsOrientationTypeEnum

        ### Синтаксис Automation

        ```
        OrientationType = Object.OrientationType       # Получить свойство (* )
        Object.OrientationType = OrientationType       # Установить свойство (* )
        OrientationType = Object.GetOrientationType()  # Получить свойство (**)
        Object.SetOrientationType( OrientationType )   # Установить свойство (**)
        ```
        """
        ...
    @OrientationType.setter
    def OrientationType(self, value: int): ...

    @property
    def ProjectionPoints(self) -> bool:  # ipointdrivenpattern_projectionpoints.html
        """
        ## ProjectionPoints - Признак проецирования управляющих точек эскиза

        Тип данных: BOOL

        Значения свойства:

        +----------------------------------+
        | TRUE  | - проецировать точки,    |
        +----------------------------------+
        | FALSE | - не проецировать точки. |
        +----------------------------------+

        Позволяет получать признак проецирования управляющих точек эскиза на плоскость, параллельную плоскости эскиза и проходящую через базовую точку.

        ### Синтаксис Automation

        ```
        ProjectionPoints = Object.ProjectionPoints       # Получить свойство (* )
        Object.ProjectionPoints = ProjectionPoints       # Установить свойство (* )
        ProjectionPoints = Object.GetProjectionPoints()  # Получить свойство (**)
        Object.SetProjectionPoints( ProjectionPoints )   # Установить свойство (**)
        ```
        """
        ...
    @ProjectionPoints.setter
    def ProjectionPoints(self, value: bool): ...

    @property
    def SaveInitialOrientation(self) -> bool:  # ipointdrivenpattern_saveinitialorientation.html
        """
        ## SaveInitialOrientation - Ориентация экземпляров массива (TRUE - Сохранять исходную ориентацию, иначе - доворачивать до нормали кривой или поверхности)

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        SaveInitialOrientation = Object.SaveInitialOrientation       # Получить свойство (* )
        Object.SaveInitialOrientation = SaveInitialOrientation       # Установить свойство (* )
        SaveInitialOrientation = Object.GetSaveInitialOrientation()  # Получить свойство (**)
        Object.SetSaveInitialOrientation( SaveInitialOrientation )   # Установить свойство (**)
        ```
        """
        ...
    @SaveInitialOrientation.setter
    def SaveInitialOrientation(self, value: bool): ...

    def ClearDrivenObjects(self) -> bool:  # ipointdrivenpattern_cleardrivenobjects.html
        """
        ## ClearDrivenObjects - Очистить список управляющих объектов

        Метод очищает список управляющих объектов.

        ### Синтаксис Automation

        ```
        BOOL ClearDrivenObjects();
        ```

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def IsSuitableDrivenObject(self, Object) -> bool:  # ipointdrivenpattern_issuitabledrivenobject.html
        """
        ## IsSuitableDrivenObject - Проверка пригодности управляющего объекта для операции

        Метод проверяет объект Object на возможность использования в качестве управляющего объекта операции.

        ### Синтаксис Automation

        ```
        BOOL IsSuitableDrivenObject( IModelObject * Object );
        ```

        ### Входные параметры

        +---------------------------------------------+
        | Object | - управляющий объект для операции. |
        +---------------------------------------------+

        ### Возвращаемое значение

        +-------------------------------------------+
        | TRUE  | - можно добавлять объект Object,  |
        +-------------------------------------------+
        | FALSE | - нельзя добавлять обЪект Object. |
        +-------------------------------------------+
        """
        ...




class ITablePattern(IFeaturePattern):  # itablepattern.html
    """
    ## Интерфейс ITablePattern

    Интерфейс параметров массива по таблице.
    """

    @property
    def FileName(self) -> str:  # itablepattern_filename.html
        """
        ## FileName - Файл-источник координат точек

        Тип данных: BSTR

        ### Синтаксис Automation

        ```
        FileName = Object.FileName       # Получить свойство (* )
        Object.FileName = FileName       # Установить свойство (* )
        FileName = Object.GetFileName()  # Получить свойство (**)
        Object.SetFileName( FileName )   # Установить свойство (**)
        ```
        """
        ...
    @FileName.setter
    def FileName(self, value: str): ...

    @property
    def OrientationObject(self) -> IModelObject:  # itablepattern_orientationobject.html
        """
        ## OrientationObject - Ориентирующий объект

        Тип данных: Указатель на интерфейс IModelObject

        ### Синтаксис Automation

        ```
        OrientationObject = Object.OrientationObject       # Получить свойство (* )
        Object.OrientationObject = OrientationObject       # Установить свойство (* )
        OrientationObject = Object.GetOrientationObject()  # Получить свойство (**)
        Object.SetOrientationObject( OrientationObject )   # Установить свойство (**)
        ```
        """
        ...
    @OrientationObject.setter
    def OrientationObject(self, value: IModelObject): ...

    @property
    def OrientationType(self) -> int:  # itablepattern_orientationtype.html
        """
        ## OrientationType - Способ ориентации экземпляров массива

        Тип данных: из перечисления ksPatternExemplarsOrientationTypeEnum

        ### Синтаксис Automation

        ```
        OrientationType = Object.OrientationType       # Получить свойство (* )
        Object.OrientationType = OrientationType       # Установить свойство (* )
        OrientationType = Object.GetOrientationType()  # Получить свойство (**)
        Object.SetOrientationType( OrientationType )   # Установить свойство (**)
        ```
        """
        ...
    @OrientationType.setter
    def OrientationType(self, value: int): ...

    @property
    def PointsType(self) -> int:  # itablepattern_pointstype.html
        """
        ## PointsType - Тип координат в файле-источнике

        Тип данных: из перечисления ksPoint3DTypeEnum

        ### Синтаксис Automation

        ```
        PointsType = Object.PointsType       # Получить свойство (* )
        Object.PointsType = PointsType       # Установить свойство (* )
        PointsType = Object.GetPointsType()  # Получить свойство (**)
        Object.SetPointsType( PointsType )   # Установить свойство (**)
        ```
        """
        ...
    @PointsType.setter
    def PointsType(self, value: int): ...

    @property
    def SaveInitialOrientation(self) -> bool:  # itablepattern_saveinitialorientation.html
        """
        ## SaveInitialOrientation - Ориентация экземпляров массива

        Тип данных: BOOL

        Значения свойства:

        +------------------------------------------+
        | TRUE  | - cохранять исходную ориентацию, |
        +------------------------------------------+
        | FALSE | - доворачивать до нормали.       |
        +------------------------------------------+

        ### Синтаксис Automation

        ```
        SaveInitialOrientation = Object.SaveInitialOrientation       # Получить свойство (* )
        Object.SaveInitialOrientation = SaveInitialOrientation       # Установить свойство (* )
        SaveInitialOrientation = Object.GetSaveInitialOrientation()  # Получить свойство (**)
        Object.SetSaveInitialOrientation( SaveInitialOrientation )   # Установить свойство (**)
        ```
        """
        ...
    @SaveInitialOrientation.setter
    def SaveInitialOrientation(self, value: bool): ...




class IPoints3D(IModelObjects):  # ipoints3d.html
    """
    ## Интерфейс IPoints3D

    Интерфейс коллекции элементов Пространственная точка.

    Интерфейс позволяет получить коллекции элементов Пространственная точка.

    Получить интерфейс коллекции можно используя свойство контейнера трехмерных объектов IModelContainer::Points3D.
    """

    def Add(self) -> IPoint3D:  # ipoints3d_add.html
        """
        ## Add - Создать новый элемент и добавить его в коллекцию

        Метод позволяет создать новый интерфейс пространственной точки. После получения нового интерфейса нужно задать параметры элемента и вызвать метод IModelObject::Update.

        ### Синтаксис Automation

        ```
        LPDISPATCH Add;
        ```

        ### Возвращаемое значение

        Указатель на интерфейс пространственной точки IPoint3D.
        """
        ...

    def Point3D(self, Index) -> IPoint3D:  # ipoints3d_point3d.html
        """
        ## Point3D - Элемент, заданный по индексу

        Тип данных: указатель на интерфейс IPoint3D

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        Point3D = iObject.Point3D( index )     # Получить свойство (* )
        Point3D = iObject.GetPoint3D( index )  # Получить свойство (**)
        ```

        ### Входные параметры

        +--------------------------------------------------------+
        | Index | VT_BSTR - имя объекта, VT_I4 - индекс объекта. |
        +--------------------------------------------------------+
        """
        ...




class IPoint3D(IModelObject):  # ipoint3d.html
    """
    ## Интерфейс IPoint3D

    Интерфейс пространственной точки.

    Интерфейс можно получить с помощью метода коллекции пространственных точек IPoints3D::Add или свойства IPoints3D::Point3D.

    С помощью метода IUnknown::QueryInterface у интерфейса можно получить интерфейс подчиненного объекта ЛСК ILocalCSObject.
    """

    @property
    def AssociationObject(self) -> IModelObject:  # ipoint3d_associationobject.html
        """
        ## AssociationObject - Опорный объект

        Тип данных: указатель на интерфейс объекта IModelObject

        1. Установить значение свойства можно только при типе параметров "по координатам" IPoint3D::ParameterType.

        2. Для установки объекта необходимо вызвать функцию IPoint3D::SetAssociationObject.

        ### Синтаксис Automation

        ```
        AssociationObject = iObject.AssociationObject;       # Получить свойство (* )
        AssociationObject = iObject.GetAssociationObject();  # Получить свойство (**)
        ```
        """
        ...

    @property
    def ParameterType(self) -> int:  # ipoint3d_parametertype.html
        """
        ## ParameterType - Тип параметров построения точки

        Тип данных: из перечисления ksPoint3DTypeEnum

        ### Синтаксис Automation

        ```
        ParameterType = iObject.ParameterType;       # Получить свойство (* )
        iObject.ParameterType = ParameterType;       # Установить свойство (* )
        ParameterType = iObject.GetParameterType();  # Получить свойство (**)
        iObject.SetParameterType( ParameterType );   # Установить свойство (**)
        ```
        """
        ...
    @ParameterType.setter
    def ParameterType(self, value: int): ...

    @property
    def Parameters(self) -> IKompasAPIObject:  # ipoint3d_parameters.html
        """
        ## Parameters - Интерфейс параметров точки

        Тип данных: указатель на интерфейс объекта IKompasAPIObject

        В зависимости от типа IPoint3D::ParameterType, интерфейс параметров должен приводиться к одному из следующих вариантов:

        •ksPDisplace - IPoint3DParamDisplace - Интерфейс параметров пространственной точки, заданной по смещению от опорного объекта,

        •ksPIntersect - IPoint3DParamIntersect - Интерфейс параметров пространственной точки, заданной на пересечении опорных объектов,

        •ksPCenter - IPoint3DParamCenter - Интерфейс параметров пространственной точки, заданной в центре опорного объекта,

        •ksPCurve - IPoint3DParamCurve - Интерфейс параметров пространственной точки, заданной на кривой со смещением,

        •ksPSurface - IPoint3DparamSurface - Интерфейс параметров пространственной точки, заданной на поверхности,

        •ksPProjection - IPoint3DParamProjection - Интерфейс параметров пространственной точки, заданной проецированием,

        •ksPBetweenPoints - IPoint3DParamBetweenPoints - Интерфейс параметров пространственной точки между точками.

        ### Синтаксис Automation

        ```
        Parameters = iObject.Parameters;       # Получить свойство (* )
        Parameters = iObject.GetParameters();  # Получить свойство (**)
        ```
        """
        ...

    @property
    def Symbol(self) -> int:  # ipoint3d_symbol.html
        """
        ## Symbol - Стиль отображения

        Тип данных: из перечисления ksAnnotationSymbolEnum

        ### Синтаксис Automation

        ```
        Symbol = iObject.Symbol;       # Получить свойство (* )
        iObject.Symbol = Symbol;       # Установить свойство (* )
        Symbol = iObject.GetSymbol();  # Получить свойство (**)
        iObject.SetSymbol( Symbol );   # Установить свойство (**)
        ```
        """
        ...
    @Symbol.setter
    def Symbol(self, value: int): ...

    @property
    def X(self) -> float:  # ipoint3d_x.html
        """
        ## X - Координата X

        Тип данных: double

        Установить значение свойства можно только при типе параметров "по координатам" IPoint3D::ParameterType.

        ### Синтаксис Automation

        ```
        X = iObject.X;       # Получить свойство (* )
        iObject.X = X;       # Установить свойство (* )
        X = iObject.GetX();  # Получить свойство (**)
        iObject.SetX( X );   # Установить свойство (**)
        ```
        """
        ...
    @X.setter
    def X(self, value: float): ...

    @property
    def Y(self) -> float:  # ipoint3d_y.html
        """
        ## Y - Координата Y

        Тип данных: double

        Установить значение свойства можно только при типе параметров "по координатам" IPoint3D::ParameterType.

        ### Синтаксис Automation

        ```
        Y = iObject.Y;       # Получить свойство (* )
        iObject.Y = Y;       # Установить свойство (* )
        Y = iObject.GetY();  # Получить свойство (**)
        iObject.SetY( Y );   # Установить свойство (**)
        ```
        """
        ...
    @Y.setter
    def Y(self, value: float): ...

    @property
    def Z(self) -> float:  # ipoint3d_z.html
        """
        ## Z - Координата Z

        Тип данных: double

        Установить значение свойства можно только при типе параметров "по координатам" IPoint3D::ParameterType.

        ### Синтаксис Automation

        ```
        Z = iObject.Z;       # Получить свойство (* )
        iObject.Z = Z;       # Установить свойство (* )
        Z = iObject.GetZ();  # Получить свойство (**)
        iObject.SetZ( Z );   # Установить свойство (**)
        ```
        """
        ...
    @Z.setter
    def Z(self, value: float): ...

    def SetAssociationObject(self, NewVal) -> bool:  # ipoint3d_setassociationobject.html
        """
        ## SetAssociationObject - Установить опорный объект

        Метод позволяет установить опорный объект только для типа точки по координатам (IPoint3D::ParameterType).

        ### Синтаксис Automation

        ```
        BOOL SetAssociationObject( LPDISPATCH obj );
        ```

        ### Входные параметры

        +--------------------------------------+
        | obj | - указатель на опорный объект. |
        +--------------------------------------+

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...




class IConjunctivePoint(IPoint3D):  # iconjunctivepoint.html
    """
    ## Интерфейс IConjunctivePoint

    Интерфейс присоединительной точки.

    1. Интерфейс можно получить, используя свойство IConjunctivePoints::ConjunctivePoint или IModelObjects::Item или метод IConjunctivePoints::Add.

    2. Интерфейс наследует у интерфейса IPoint3D пространственной точки все методы и свойства для позиционирования, за исключением следующих:

    Symbol() - невозможно установить значения свойства, поскольку стиль отображения точки постоянный; получаемое значение свойства во всех случаях будет одинаковым, равным ksCirclePoint из перечисления ksAnnotationSymbolEnum.

    3. С помощью метода IUnknown::QueryInterface у интерфейса можно получить интерфейс подчиненного объекта ЛСК ILocalCSObject.
    """

    @property
    def BuildingType(self) -> int:  # iconjunctivepoint_buildingtype.html
        """
        ## BuildingType - Способ построения присоединительной точки

        Тип данных: из перечисления ksConjunctivePointTypeEnum

        ### Синтаксис Automation

        ```
        BuildingType = Object.BuildingType       # Получить свойство (* )
        Object.BuildingType = BuildingType       # Установить свойство (* )
        BuildingType = Object.GetBuildingType()  # Получить свойство (**)
        Object.SetBuildingType( BuildingType )   # Установить свойство (**)
        ```
        """
        ...
    @BuildingType.setter
    def BuildingType(self, value: int): ...

    @property
    def Direction1(self) -> bool:  # iconjunctivepoint_direction1.html
        """
        ## Direction1 - Направление объекта 1

        Тип данных: BOOL

        Значения свойства:

        +---------------------+
        | TRUE  | - прямое,   |
        +---------------------+
        | FALSE | - обратное. |
        +---------------------+

        Свойство позволяет устанавливать и получать направление объекта 1.

        ### Синтаксис Automation

        ```
        Direction1 = Object.Direction1        # Получить свойство (* )
        Object.Direction1 = Direction1        # Установить свойство (* )
        Direction1 = Object.GetDirection1( )  # Получить свойство (**)
        Object.SetDirection1( Direction1 )    # Установить свойство (**)
        ```
        """
        ...
    @Direction1.setter
    def Direction1(self, value: bool): ...

    @property
    def Direction2(self) -> bool:  # iconjunctivepoint_direction2.html
        """
        ## Direction2 - Направление объекта 2

        Тип данных: BOOL

        Значения свойства:

        +---------------------+
        | TRUE  | - прямое,   |
        +---------------------+
        | FALSE | - обратное. |
        +---------------------+

        Свойство позволяет устанавливать и получать направление объекта 2.

        ### Синтаксис Automation

        ```
        Direction2 = Object.Direction2        # Получить свойство (* )
        Object.Direction2 = Direction2        # Установить свойство (* )
        Direction2 = Object.GetDirection2( )  # Получить свойство (**)
        Object.SetDirection2( Direction2 )    # Установить свойство (**)
        ```
        """
        ...
    @Direction2.setter
    def Direction2(self, value: bool): ...

    @property
    def DirectionObject1(self) -> IModelObject:  # iconjunctivepoint_directionobject1.html
        """
        ## DirectionObject1 - Получить направляющий объект 1

        Тип данных: указатель на интерфейс IModelObject

        1. Свойство позволяет получить указатель на интерфейс направляющего объекта 1.

        2. Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        DirectionObject1 = Object.DirectionObject1       # Получить свойство (* )
        DirectionObject1 = Object.GetDirectionObject1()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def DirectionObject2(self) -> IModelObject:  # iconjunctivepoint_directionobject2.html
        """
        ## DirectionObject2 - Получить направляющий объект 2

        Тип данных: указатель на интерфейс IModelObject

        1. Свойство позволяет получить указатель на интерфейс направляющего объекта 2.

        2. Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        DirectionObject2 = Object.DirectionObject2       # Получить свойство (* )
        DirectionObject2 = Object.GetDirectionObject2()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def ShowName(self):  # iconjunctivepoint_showname.html
        """
        ## ShowName - Показывать надпись

        ### Синтаксис Automation

        ```
        ShowName = Object.ShowName       # Получить свойство (* )
        Object.ShowName = ShowName       # Установить свойство (* )
        ShowName = Object.GetShowName()  # Получить свойство (**)
        Object.SetShowName( ShowName )   # Установить свойство (**)
        ```
        """
        ...
    @ShowName.setter
    def ShowName(self, value): ...

    @property
    def UseDirection2(self) -> bool:  # iconjunctivepoint_usedirection2.html
        """
        ## UseDirection2 - Использовать второе направление

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        UseDirection2 = Object.UseDirection2        # Получить свойство (* )
        Object.UseDirection2 = UseDirection2        # Установить свойство (* )
        UseDirection2 = Object.GetUseDirection2( )  # Получить свойство (**)
        Object.SetUseDirection2( UseDirection2 )    # Установить свойство (**)
        ```
        """
        ...
    @UseDirection2.setter
    def UseDirection2(self, value: bool): ...

    def Axis(self, Val) -> IModelObject:  # iconjunctivepoint_axis.html
        """
        ## Axis - Получить ось присоединительной точки

        Тип данных: указатель на интерфейс IModelObject

        1. Свойство позволяет получить указатель на интерфейс оси присоединительной точки в соответствии с переданным типом.

        2. Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        Axis = Object.Axis(type)     # Получить свойство (* )
        Axis = Object.GetAxis(type)  # Получить свойство (**)
        ```

        ### Входные параметры

        +----------------------------------------------------------------------------------------------------------------------------------------------------------------------------+
        | type | - тип запрашиваемой оси из перечисления ksObj3dTypeEnum, допустимыми значениями являются следующие: - o3d_axisOX - ось со стрелкой, - o3d_axisOY - ось без стрелки. |
        +----------------------------------------------------------------------------------------------------------------------------------------------------------------------------+

        ### Возвращаемое значение

        +----------------------------------------+
        | указатель на ось | - в случае успеха,  |
        +----------------------------------------+
        | NULL             | - в случае неудачи. |
        +----------------------------------------+
        """
        ...

    def GetVector(self, Axis) -> bool:  # iconjunctivepoint_getvector.html
        """
        ## GetVector - Получить вектора направлений главных осей

        ### Синтаксис Automation

        ```
        BOOL GetVector( ksObj3dTypeEnum Axis, double * X, double * Y, double * Z );
        ```

        ### Входные параметры

        +---------------+
        | Axis | - ось. |
        +---------------+

        ### Выходные параметры

        +------------------------+
        | X | - координата по X, |
        +------------------------+
        | Y | - координата по Y, |
        +------------------------+
        | Z | - координата по Z. |
        +------------------------+

        ### Возвращаемое значение

        +-----------------------------------------+
        | TRUE  | - в случае удачного завершения, |
        +-----------------------------------------+
        | FALSE | - в случае неудачи.             |
        +-----------------------------------------+
        """
        ...

    def InitByMatrix3D(self, mtr) -> bool:  # iconjunctivepoint_initbymatrix3d.html
        """
        ## InitByMatrix3D - Установить систему координат по матрице SAFEARRAY double (VT_ARRAY | VT_R8)

        ### Синтаксис Automation

        ```
        BOOL InitByMatrix3D( VARIANT mtr );
        ```

        ### Входные параметры

        +---------------------------------------------------------------+
        | mtr | - массив координат SAFEARRAY double (VT_ARRAY | VT_R8). |
        +---------------------------------------------------------------+

        ### Возвращаемое значение

        +-----------------------------------------+
        | TRUE  | - в случае удачного завершения, |
        +-----------------------------------------+
        | FALSE | - в случае неудачи.             |
        +-----------------------------------------+
        """
        ...

    def SetDirectionObject1(self, NewVal) -> bool:  # iconjunctivepoint_setdirectionobject1.html
        """
        ## SetDirectionObject1 - Установить направляющий объект 1

        ### Синтаксис Automation

        ```
        BOOL SetDirectionObject1( LPDISPATCH NewVal );
        ```

        ### Возвращаемое значение

        +-----------------------------------------+
        | TRUE  | - в случае удачного завершения, |
        +-----------------------------------------+
        | FALSE | - в случае неудачи.             |
        +-----------------------------------------+
        """
        ...

    def SetDirectionObject2(self, NewVal) -> bool:  # iconjunctivepoint_setdirectionobject2.html
        """
        ## SetDirectionObject2 - Установить направляющий объект 2

        ### Синтаксис Automation

        ```
        BOOL SetDirectionObject2( LPDISPATCH NewVal );
        ```

        ### Возвращаемое значение

        +-----------------------------------------+
        | TRUE  | - в случае удачного завершения, |
        +-----------------------------------------+
        | FALSE | - в случае неудачи.             |
        +-----------------------------------------+
        """
        ...

    def Vector3D(self, First) -> IVector3D:  # iconjunctivepoint_vector3d.html
        """
        ## Vector3D - Получить параметры вектора

        Тип данных: указатель на интерфейс IVector3D

        1. Свойство позволяет получить интерфейс вектора, задающего направление присоединительной точки.

        2. Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        Vector3D = Object.Vector3D( First )     # Получить свойство (* )
        Vector3D = Object.GetVector3D( First )  # Получить свойство (**)
        ```

        ### Входные параметры

        +------------------------------------------------------------------------------------------------+
        | First | TRUE - вектор, задающий направление оси X, FALSE - вектор, задающий направление оси Y. |
        +------------------------------------------------------------------------------------------------+
        """
        ...




class IControlPoint(IPoint3D):  # icontrolpoint.html
    """
    ## Интерфейс IControlPoint

    Интерфейс контрольной точки.

    1. Интерфейс можно получить, используя свойство IControlPoints::ControlPoint или IModelObjects::Item или метод IControlPoints::Add.

    2. Интерфейс наследует у интерфейса IPoint3D пространственной точки все методы и свойства для позиционирования, за исключением следующих:

    Symbol() - невозможно установить значения свойства, поскольку стиль отображения точки постоянный; получаемое значение свойства во всех случаях будет одинаковым, равным ksCirclePoint из перечисления ksAnnotationSymbolEnum.

    3. С помощью метода IUnknown::QueryInterface у интерфейса можно получить интерфейс подчиненного объекта ЛСК ILocalCSObject.
    """

    @property
    def ShowName(self) -> bool:  # icontrolpoint_showname.html
        """
        ## ShowName - Показывать надпись

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        ShowName = Object.ShowName       # Получить свойство (* )
        Object.ShowName = ShowName       # Установить свойство (* )
        ShowName = Object.GetShowName()  # Получить свойство (**)
        Object.SetShowName( ShowName )   # Установить свойство (**)
        ```
        """
        ...
    @ShowName.setter
    def ShowName(self, value: bool): ...




class ILocalCoordinateSystem(IPoint3D):  # ilocalcoordinatesystem.html
    """
    ## Интерфейс ILocalCoordinateSystem

    Интерфейс локальной системы координат.

    1. Интерфейс можно получить, используя свойство интерфейса коллекции ЛСК ILocalCoordinateSystems::LocalCoordinateSystem или метод ILocalCoordinateSystems::Add.

    2. Интерфейс наследует от интерфейса 3D точки IPoint3D все методы и свойства для позиционирования ЛСК.

    3. Интерфейс позволяет задавать смещение вдоль собственных осей ЛСК с помощью метода ILocalCoordinateSystem::SetDisplacementByAxis.

    4. С помощью метода IUnknown::QueryInterface у интерфейса можно получить интерфейс подчиненного объекта ЛСК ILocalCSObject.
    """

    @property
    def Current(self) -> bool:  # ilocalcoordinatesystem_current.html
        """
        ## Current - Состояние ЛСК - текущая/нетекущая

        Тип данных: BOOL

        По умолчанию ЛСК создаётся текущей. Чтобы ЛСК создалась нетекущей, нужно установить свойство iObject.Current = FALSE.

        См. также ILocalCoordinateSystems::SetCurrent.

        ### Синтаксис Automation

        ```
        Current = iObject.Current;       # Получить свойство (* )
        iObject.Current = Current;       # Установить свойство (* )
        Current = iObject.GetCurrent();  # Получить свойство (**)
        iObject.SetCurrent( Current );   # Установить свойство (**)
        ```
        """
        ...
    @Current.setter
    def Current(self, value: bool): ...

    @property
    def LocalCSParameters(self) -> IKompasAPIObject:  # ilocalcoordinatesystem_localcsparameters.html
        """
        ## LocalCSParameters - Интерфейс параметров ЛСК

        Тип данных: указатель на интерфейс объекта IKompasAPIObject

        В зависимости от типа ILocalCoordinateSystem::OrientationType интерфейс параметров должен приводиться к следующим вариантам:

        •ksAxisOrientation - ILocalCSAxesDirectionParam – Интерфейс параметров ЛСК для типа ориентации "Направление осей",

        •ksEulerCorners - ILocalCSEulerParam – Интерфейс параметров ЛСК для типа ориентации "Система углов Эйлера",

        •ksOrientByObject - ILocalCSOrientByObjectParam – Интерфейс параметров ЛСК для типа ориентации "По объекту".

        ### Синтаксис Automation

        ```
        LocalCSParameters = iObject.LocalCSParameters;       # Получить свойство (* )
        LocalCSParameters = iObject.GetLocalCSParameters();  # Получить свойство (**)
        ```
        """
        ...

    @property
    def OrientationType(self) -> int:  # ilocalcoordinatesystem_orientationtype.html
        """
        ## OrientationType - Тип ориентирования ЛСК

        Тип данных: из перечисления ksOrientationTypeEnum

        ### Синтаксис Automation

        ```
        OrientationType = iObject.OrientationType;       # Получить свойство (* )
        iObject.OrientationType = OrientationType;       # Установить свойство (* )
        OrientationType = iObject.GetOrientationType();  # Получить свойство (**)
        iObject.SetOrientationType( OrientationType );   # Установить свойство (**)
        ```
        """
        ...
    @OrientationType.setter
    def OrientationType(self, value: int): ...

    @property
    def ShowName(self) -> bool:  # ilocalcoordinatesystem_showname.html
        """
        ## ShowName - Показывать надпись

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        ShowName = Object.ShowName;       # Получить свойство (* )
        Object.ShowName = ShowName;       # Установить свойство (* )
        ShowName = Object.GetShowName();  # Получить свойство (**)
        Object.SetShowName( ShowName );   # Установить свойство (**)
        ```
        """
        ...
    @ShowName.setter
    def ShowName(self, value: bool): ...

    def DefaultObject(self, Val) -> IModelObject:  # ilocalcoordinatesystem_defaultobject.html
        """
        ## DefaultObject - Получить примитив ЛСК (ось или плоскость)

        Тип данных: указатель на интерфейс объекта IModelObject

        1. Свойство доступно только для чтения.

        2. Позволяет получить указатель на примитив ЛСК (ось или плоскость) в виде указателя на интерфейс IModelObject.

        ### Синтаксис Automation

        ```
        DefaultObject = iObject.DefaultObject( type );     # Получить свойство (* )
        DefaultObject = iObject.GetDefaultObject( type );  # Получить свойство (**)
        ```

        ### Входные параметры

        +-----------------------------------------------------------------------------------------------------------------------------------------------------------------+
        | type | - тип объекта из перечисления ksObj3dTypeEnum; допустимые типы: - o3d_axisOX, - o3d_axisOY - o3d_axisOZ, - o3d_planeXOY, - o3d_planeXOZ, - o3d_planeYOZ. |
        +-----------------------------------------------------------------------------------------------------------------------------------------------------------------+

        ### Возвращаемое значение

        +---------------------------------------------------------------------+
        | Указатель на примитив ЛСК (ось или плоскость). | - в случае успеха; |
        +---------------------------------------------------------------------+
        """
        ...

    def GetVector(self, Axis) -> bool:  # ilocalcoordinatesystem_getvector.html
        """
        ## GetVector - Получить вектора направлений главных осей

        ### Синтаксис Automation

        ```
        BOOL GetVector( ksObj3dTypeEnum axis,
        double * x,
        double * y,
        double * z );
        ```

        ### Входные параметры

        +-------------------------------------------------------------------------------------------------------------+
        | axis | - тип оси из перечисления ksObj3dTypeEnum; допустимые типы: - o3d_axisOX, - o3d_axisOY - o3d_axisOZ. |
        +-------------------------------------------------------------------------------------------------------------+

        ### Выходные параметры

        +---------------------------------------------+
        | x | - координата X вектора направления оси, |
        +---------------------------------------------+
        | y | - координата Y вектора направления оси, |
        +---------------------------------------------+
        | z | - координата Z вектора направления оси. |
        +---------------------------------------------+

        ### Возвращаемое значение

        +-----------------------------+
        | TRUE  | - в случае успеха;  |
        +-----------------------------+
        | FALSE | - в случае неудачи. |
        +-----------------------------+
        """
        ...

    def InitByMatrix3D(self, mtr):  # ilocalcoordinatesystem_initbymatrix3d.html
        """
        ## InitByMatrix3D - Установить систему координат по матрице

        1. Элементы матрицы возвращаются в виде одномерного массива из 16 элементов.

        2. Матрица имеет размер 4х4.

        ### Синтаксис Automation

        ```
        BOOL InitByMatrix3D( VARIANT mtr );
        ```

        ### Входные параметры

        +----------------------------------------------------+
        | mtr | - массив SafeAttray типа (VT_ARRAY | VT_R8). |
        +----------------------------------------------------+
        """
        ...

    def ReadFromFile(self, FileName) -> bool:  # ilocalcoordinatesystem_readfromfile.html
        """
        ## ReadFromFile - Прочитать файл с данными

        Позволяет установить направление осей ЛСК в соответствии со значениями в текстовом файле. Файл должен быть записан с помощью метода ILocalCoordinateSystem::WriteToFile.

        ### Синтаксис Automation

        ```
        BOOL ReadFromFile( BSTR fileName );
        ```

        ### Входные параметры

        +--------------------------------+
        | fileName | - полное имя файла. |
        +--------------------------------+

        ### Возвращаемое значение

        +-----------------------------+
        | TRUE  | - в случае успеха;  |
        +-----------------------------+
        | FALSE | - в случае неудачи. |
        +-----------------------------+
        """
        ...

    def SetDisplacementByAxis(self, Axis, NewVal) -> bool:  # ilocalcoordinatesystem_setdisplacementbyaxis.html
        """
        ## SetDisplacementByAxis - Установить смещение по оси

        1. Позволяет установить смещение точки начала координат ЛСК вдоль оси ЛСК.

        2. Чтобы изменения вступили в силу, нужно вызвать метод IModelObject::Update.

        ### Синтаксис Automation

        ```
        BOOL SetDisplacementByAxis( ksObj3dTypeEnum axis,
        double NewVal );
        ```

        ### Входные параметры

        +--------------------------------------------------------------------------------------------------------------+
        | axis   | - тип оси из перечисления ksObj3dTypeEnum допустимые типы: - o3d_axisOX, - o3d_axisOY - o3d_axisOZ. |
        +--------------------------------------------------------------------------------------------------------------+
        | NewVal | - величина смещения.                                                                                |
        +--------------------------------------------------------------------------------------------------------------+

        ### Возвращаемое значение

        +-----------------------------+
        | TRUE  | - в случае успеха;  |
        +-----------------------------+
        | FALSE | - в случае неудачи. |
        +-----------------------------+
        """
        ...

    def SetStartingOrientation(self) -> bool:  # ilocalcoordinatesystem_setstartingorientation.html
        """
        ## SetStartingOrientation - Установить исходное положение ЛСК

        Позволяет установить ЛСК в положение, соответствующее матрице векторов:

        +-------------------+
        | Ось   | X | Y | Z |
        +-------------------+
        | Ось X | 1 | 0 | 0 |
        +-------------------+
        | Ось Y | 0 | 1 | 0 |
        +-------------------+
        | Ось Z | 0 | 0 | 1 |
        +-------------------+

        ### Синтаксис Automation

        ```
        BOOL SetStartingOrientation();
        ```

        ### Возвращаемое значение

        +-----------------------------+
        | TRUE  | - в случае успеха;  |
        +-----------------------------+
        | FALSE | - в случае неудачи. |
        +-----------------------------+
        """
        ...

    def Vector3D(self, Axis) -> IVector3D:  # ilocalcoordinatesystem_vector3d.html
        """
        ## Vector3D - Вектор, задающий направление оси

        Тип данных: указатель на интерфейс IVector3D

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        Vector3D = Object.Vector3D( Axis )     # Получить свойство (* )
        Vector3D = Object.GetVector3D( Axis )  # Получить свойство (**)
        ```

        ### Входные параметры

        ksObj3dTypeEnum Axis – ось.
        """
        ...

    def WriteToFile(self, FileName) -> bool:  # ilocalcoordinatesystem_writetofile.html
        """
        ## WriteToFile - Записать файл с данными

        Позволяет записать значения векторов направлений осей ЛСК в текстовый файл.

        ### Синтаксис Automation

        ```
        BOOL WriteToFile( BSTR fileName );
        ```

        ### Входные параметры

        +--------------------------------+
        | fileName | - полное имя файла. |
        +--------------------------------+

        ### Возвращаемое значение

        +-----------------------------+
        | TRUE  | - в случае успеха;  |
        +-----------------------------+
        | FALSE | - в случае неудачи. |
        +-----------------------------+
        """
        ...




class IPoint3DParamBetweenPoints(IKompasAPIObject):  # ipoint3dparambetweenpoints.html
    """
    ## Интерфейс IPoint3DParamBetweenPoints

    Интерфейс параметров пространственной точки между точками.

    Интерфейс можно получить с помощью свойства интерфейса пространственной точки IPoint3D::Parameters.

    Версия КОМПАС v22
    """

    @property
    def Curve(self) -> IModelObject:  # ipoint3dparambetweenpoints_curve.html
        """
        ## Curve - Кривая

        Тип данных: Указатель на интерфейс IModelObject

        ### Синтаксис Automation

        ```
        Curve = Object.Curve       # Получить свойство (* )
        Object.Curve = Curve       # Установить свойство (* )
        Curve = Object.GetCurve()  # Получить свойство (**)
        Object.SetCurve( Curve )   # Установить свойство (**)
        ```
        """
        ...
    @Curve.setter
    def Curve(self, value: IModelObject): ...

    @property
    def Direction(self) -> bool:  # ipoint3dparambetweenpoints_direction.html
        """
        ## Direction - Направление

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        Direction = Object.Direction       # Получить свойство (* )
        Object.Direction = Direction       # Установить свойство (* )
        Direction = Object.GetDirection()  # Получить свойство (**)
        Object.SetDirection( Direction )   # Установить свойство (**)
        ```
        """
        ...
    @Direction.setter
    def Direction(self, value: bool): ...

    @property
    def Offset(self) -> float:  # ipoint3dparambetweenpoints_offset.html
        """
        ## Offset - Величина смещения

        Тип данных: double

        ### Синтаксис Automation

        ```
        Offset = Object.Offset       # Получить свойство (* )
        Object.Offset = Offset       # Установить свойство (* )
        Offset = Object.GetOffset()  # Получить свойство (**)
        Object.SetOffset( Offset )   # Установить свойство (**)
        ```
        """
        ...
    @Offset.setter
    def Offset(self, value: float): ...

    @property
    def OffsetType(self) -> int:  # ipoint3dparambetweenpoints_offsettype.html
        """
        ## OffsetType - Тип задания смещения

        Тип данных: из перечисления ksPoint3DCurveParamTypeEnum

        ### Синтаксис Automation

        ```
        OffsetType = Object.OffsetType       # Получить свойство (* )
        Object.OffsetType = OffsetType       # Установить свойство (* )
        OffsetType = Object.GetOffsetType()  # Получить свойство (**)
        Object.SetOffsetType( OffsetType )   # Установить свойство (**)
        ```
        """
        ...
    @OffsetType.setter
    def OffsetType(self, value: int): ...

    @property
    def Point1(self) -> IModelObject:  # ipoint3dparambetweenpoints_point1.html
        """
        ## Point1 - Точка 1

        Тип данных: Указатель на интерфейс IModelObject

        ### Синтаксис Automation

        ```
        Point1 = Object.Point1       # Получить свойство (* )
        Object.Point1 = Point1       # Установить свойство (* )
        Point1 = Object.GetPoint1()  # Получить свойство (**)
        Object.SetPoint1( Point1 )   # Установить свойство (**)
        ```
        """
        ...
    @Point1.setter
    def Point1(self, value: IModelObject): ...

    @property
    def Point2(self) -> IModelObject:  # ipoint3dparambetweenpoints_point2.html
        """
        ## Point2 - Точка 2

        Тип данных: Указатель на интерфейс IModelObject

        ### Синтаксис Automation

        ```
        Point2 = Object.Point2       # Получить свойство (* )
        Object.Point2 = Point2       # Установить свойство (* )
        Point2 = Object.GetPoint2()  # Получить свойство (**)
        Object.SetPoint2( Point2 )   # Установить свойство (**)
        ```
        """
        ...
    @Point2.setter
    def Point2(self, value: IModelObject): ...




class IPoint3DParamCenter(IKompasAPIObject):  # ipoint3dparamcenter.html
    """
    ## Интерфейс IPoint3DParamCenter

    Интерфейс параметров пространственной точки заданной в центре опорного объекта.

    Интерфейс можно получить с помощью свойства пространственной точки IPoint3D::Parameters.
    """

    @property
    def Object(self) -> IModelObject:  # ipoint3dparamcenter_object.html
        """
        ## Object - Получить объект

        Тип данных: указатель на интерфейс объекта IModelObject

        ### Синтаксис Automation

        ```
        Object = iObject.Object;       # Получить свойство (* )
        Object = iObject.GetObject();  # Получить свойство (**)
        ```
        """
        ...

    def SetObject(self, NewVal) -> bool:  # ipoint3dparamcenter_setobject.html
        """
        ## SetObject - Установить объект

        Объект должен иметь центр.

        ### Синтаксис Automation

        ```
        LPDISPATCH obj );
        ```

        ### Входные параметры

        +--------------------------------+
        | obj | - объект, имеющий центр. |
        +--------------------------------+

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...




class IPoint3DParamCurve(IKompasAPIObject):  # ipoint3dparamcurve.html
    """
    ## Интерфейс IPoint3DParamCurve

    Интерфейс параметров пространственной точки, заданной на кривой со смещением.

    Интерфейс можно получить с помощью свойства пространственной точки IPoint3D::Parameters.
    """

    @property
    def CurveObject(self) -> IModelObject:  # ipoint3dparamcurve_curveobject.html
        """
        ## CurveObject - Получить кривую

        Тип данных: указатель на интерфейс объекта IModelObject

        ### Синтаксис Automation

        ```
        CurveObject = iObject.CurveObject;       # Получить свойство (* )
        CurveObject = iObject.GetCurveObject();  # Получить свойство (**)
        ```
        """
        ...

    @property
    def Direction(self) -> bool:  # ipoint3dparamcurve_direction.html
        """
        ## Direction - Направление смещения

        Тип данных: BOOL

        Значения свойства:

        +---------------------------------+
        | TRUE  | - прямое направление,   |
        +---------------------------------+
        | FALSE | - обратное направление. |
        +---------------------------------+

        ### Синтаксис Automation

        ```
        Direction = iObject.Direction;       # Получить свойство (* )
        iObject.Direction = Direction;       # Установить свойство (* )
        Direction = iObject.GetDirection();  # Получить свойство (**)
        iObject.SetDirection( Direction );   # Установить свойство (**)
        ```
        """
        ...
    @Direction.setter
    def Direction(self, value: bool): ...

    @property
    def Offset(self) -> float:  # ipoint3dparamcurve_offset.html
        """
        ## Offset - Величина смещения

        Тип данных: double

        1. Для типа смещения по параметру U (ksOffsetByU) - свойство 0..100 (величина U в %).

        2. Для типа смещения по длине дуги (ksOffsetByLen) - свойство определяет длину дуги.

        3. Для типа смещения по центральному углу дуги (ksOffsetByAngle) - свойство определяет угол.

        ### Синтаксис Automation

        ```
        Offset = iObject.Offset;       # Получить свойство (* )
        iObject.Offset = Offset;       # Установить свойство (* )
        Offset = iObject.GetOffset();  # Получить свойство (**)
        iObject.SetOffset( Offset );   # Установить свойство (**)
        ```
        """
        ...
    @Offset.setter
    def Offset(self, value: float): ...

    @property
    def OffsetType(self) -> int:  # ipoint3dparamcurve_offsettype.html
        """
        ## OffsetType - Тип задания смещения

        Тип данных: из перечисления ksPoint3DCurveParamTypeEnum

        ### Синтаксис Automation

        ```
        OffsetType = iObject.OffsetType;       # Получить свойство (* )
        iObject.OffsetType = OffsetType;       # Установить свойство (* )
        OffsetType = iObject.GetOffsetType();  # Получить свойство (**)
        iObject.SetOffsetType( OffsetType );   # Установить свойство (**)
        ```
        """
        ...
    @OffsetType.setter
    def OffsetType(self, value: int): ...

    def SetCurveObject(self, NewVal) -> bool:  # ipoint3dparamcurve_setcurveobject.html
        """
        ## SetCurveObject - Установить объект

        ### Синтаксис Automation

        ```
        BOOL SetCurveObject( LPDISPATCH obj );
        ```

        ### Входные параметры

        +-----------------+
        | obj | - кривая. |
        +-----------------+

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...




class IPoint3DParamDisplace(IKompasAPIObject):  # ipoint3dparamdisplace.html
    """
    ## Интерфейс IPoint3DParamDisplace

    Интерфейс параметров пространственной точки, заданной по смещению от опорного объекта.

    Интерфейс можно получить с помощью свойства пространственной точки IPoint3D::Parameters.
    """

    @property
    def AssociationVertex(self) -> IModelObject:  # ipoint3dparamdisplace_associationvertex.html
        """
        ## AssociationVertex - Интерфейс опорной точки

        Тип данных: указатель на интерфейс объекта IModelObject

        ### Синтаксис Automation

        ```
        AssociationVertex = iObject.AssociationVertex;       # Получить свойство (* )
        AssociationVertex = iObject.GetAssociationVertex();  # Получить свойство (**)
        ```
        """
        ...

    @property
    def DX(self) -> float:  # ipoint3dparamdisplace_dx.html
        """
        ## DX - Смещение по X

        Тип данных: double

        ### Синтаксис Automation

        ```
        DX = iObject.DX;       # Получить свойство (* )
        iObject.DX = DX;       # Установить свойство (* )
        DX = iObject.GetDX();  # Получить свойство (**)
        iObject.SetDX( DX );   # Установить свойство (**)
        ```
        """
        ...
    @DX.setter
    def DX(self, value: float): ...

    @property
    def DY(self) -> float:  # ipoint3dparamdisplace_dy.html
        """
        ## DY - Смещение по Y

        Тип данных: double

        ### Синтаксис Automation

        ```
        DY = iObject.DY;       # Получить свойство (* )
        iObject.DY = DY;       # Установить свойство (* )
        DY = iObject.GetDY();  # Получить свойство (**)
        iObject.SetDY( DY );   # Установить свойство (**)
        ```
        """
        ...
    @DY.setter
    def DY(self, value: float): ...

    @property
    def DZ(self) -> float:  # ipoint3dparamdisplace_dz.html
        """
        ## DZ - Смещение по Z

        Тип данных: double

        ### Синтаксис Automation

        ```
        DZ = iObject.DZ;       # Получить свойство (* )
        iObject.DZ = DZ;       # Установить свойство (* )
        DZ = iObject.GetDZ();  # Получить свойство (**)
        iObject.SetDZ( DZ );   # Установить свойство (**)
        ```
        """
        ...
    @DZ.setter
    def DZ(self, value: float): ...

    @property
    def Distance(self) -> float:  # ipoint3dparamdisplace_distance.html
        """
        ## Distance - Расстояние от опорного объекта

        Тип данных: double

        +--------------------------------------------------------------+
        | Distance = iObject.Distance;      | Получить свойство (* )   |
        +--------------------------------------------------------------+
        | iObject.Distance = Distance;      | Установить свойство (* ) |
        +--------------------------------------------------------------+
        | Distance = iObject.GetDistance(); | Получить свойство (**)   |
        +--------------------------------------------------------------+
        | iObject.SetDistance( Distance );  | Установить свойство (**) |
        +--------------------------------------------------------------+
        """
        ...
    @Distance.setter
    def Distance(self, value: float): ...

    @property
    def GuidingObject(self) -> IModelObject:  # ipoint3dparamdisplace_guidingobject.html
        """
        ## GuidingObject - Направляющий объект

        Тип данных: указатель на интерфейс объекта IModelObject

        ### Синтаксис Automation

        ```
        GuidingObject = iObject.GuidingObject;       # Получить свойство (* )
        GuidingObject = iObject.GetGuidingObject();  # Получить свойство (**)
        ```
        """
        ...

    @property
    def PositionObject(self) -> IModelObject:  # ipoint3dparamdisplace_positionobject.html
        """
        ## PositionObject - Точка привязки

        Тип данных: Указатель на интерфейс IModelObject

        +------------------------------------------------------------------------+
        | PositionObject = Object.PositionObject      | Получить свойство (* )   |
        +------------------------------------------------------------------------+
        | Object.PositionObject = PositionObject      | Установить свойство (* ) |
        +------------------------------------------------------------------------+
        | PositionObject = Object.GetPositionObject() | Получить свойство (**)   |
        +------------------------------------------------------------------------+
        | Object.SetPositionObject( PositionObject )  | Установить свойство (**) |
        +------------------------------------------------------------------------+
        """
        ...
    @PositionObject.setter
    def PositionObject(self, value: IModelObject): ...

    @property
    def Vector3D(self) -> IVector3D:  # ipoint3dparamdisplace_vector3d.html
        """
        ## Vector3D - Получить параметры вектора

        Тип данных: указатель на интерфейс IVector3D

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        Vector3D = Object.Vector3D       # Получить свойство (* )
        Vector3D = Object.GetVector3D()  # Получить свойство (**)
        ```
        """
        ...

    def SetAssociationVertex(self, NewVal) -> bool:  # ipoint3dparamdisplace_setassociationvertex.html
        """
        ## SetAssociationVertex - Установить опорную точку

        ### Синтаксис Automation

        ```
        BOOL SetAssociationVertex( LPDISPATCH obj );
        ```

        ### Входные параметры

        +------------------------+
        | obj | - опорная точка. |
        +------------------------+

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def SetGuidingObject(self, NewVal) -> bool:  # ipoint3dparamdisplace_setguidingobject.html
        """
        ## SetGuidingObject - Установить направляющий объект

        ### Синтаксис Automation

        ```
        BOOL SetGuidingObject( LPDISPATCH obj );
        ```

        ### Входные параметры

        +------------------------+
        | obj | - опорная точка. |
        +------------------------+

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...




class IPoint3DParamIntersect(IKompasAPIObject):  # ipoint3dparamintersect.html
    """
    ## Интерфейс IPoint3DParamIntersect

    Интерфейс параметров пространственной точки, заданной на пересечении опорных объектов.

    Интерфейс можно получить с помощью свойства пространственной точки IPoint3D::Parameters.
    """

    @property
    def CartPointArray(self) -> typing.Any:  # ipoint3dparamintersect_cartpointarray.html
        """
        ## CartPointArray - Массив координат точек пересечения кривой и поверхности в виде SAFEARRAY VT_R8 - VT_ARRAY | VT_R8

        Тип данных: VARIANT

        Массив SafeArray типа VT_ARRAY | VT_R8 координат точек для пересечения.

        ### Синтаксис Automation

        ```
        CartPointArray = iObject.CartPointArray;       # Получить свойство (* )
        CartPointArray = iObject.GetCartPointArray();  # Получить свойство (**)
        ```
        """
        ...

    @property
    def CurveObject(self) -> IModelObject:  # ipoint3dparamintersect_curveobject.html
        """
        ## CurveObject - Получить кривую

        Тип данных: указатель на интерфейс объекта IModelObject

        ### Синтаксис Automation

        ```
        CurveObject = iObject.CurveObject;       # Получить свойство (* )
        CurveObject = iObject.GetCurveObject();  # Получить свойство (**)
        ```
        """
        ...

    @property
    def Index(self) -> float:  # ipoint3dparamintersect_index.html
        """
        ## Index - Индекс точки пересечения

        Тип данных: double

        Индекс нужной точки можно вычислить из IPoint3DParamIntersect::CartPointArray.

        ### Синтаксис Automation

        ```
        Index = iObject.Index;       # Получить свойство (* )
        iObject.Index = Index;       # Установить свойство (* )
        Index = iObject.GetIndex();  # Получить свойство (**)
        iObject.SetIndex( Index );   # Установить свойство (**)
        ```
        """
        ...
    @Index.setter
    def Index(self, value: float): ...

    @property
    def SurfaceObject(self) -> IModelObject:  # ipoint3dparamintersect_surfaceobject.html
        """
        ## SurfaceObject - Получить поверхность

        Тип данных: указатель на интерфейс объекта IModelObject

        ### Синтаксис Automation

        ```
        SurfaceObject = iObject.SurfaceObject;       # Получить свойство (* )
        SurfaceObject = iObject.GetSurfaceObject();  # Получить свойство (**)
        ```
        """
        ...

    def SetCurveObject(self, NewVal) -> bool:  # ipoint3dparamintersect_setcurveobject.html
        """
        ## SetCurveObject - Установить направляющий объект

        ### Синтаксис Automation

        ```
        BOOL SetCurveObject( LPDISPATCH obj );
        ```

        ### Входные параметры

        +-----------------+
        | obj | - кривая. |
        +-----------------+

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def SetSurfaceObject(self, NewVal) -> bool:  # ipoint3dparamintersect_setsurfaceobject.html
        """
        ## SetSurfaceObject - Установить поверхность

        ### Синтаксис Automation

        ```
        BOOL SetSurfaceObject( LPDISPATCH obj );
        ```

        ### Входные параметры

        +----------------------+
        | obj | - поверхность. |
        +----------------------+

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...




class IPoint3DParamProjection(IKompasAPIObject):  # ipoint3dparamprojection.html
    """
    ## Интерфейс IPoint3DParamProjection

    Интерфейс параметров пространственной точки, заданной проецированием.

    Интерфейс можно получить с помощью свойства пространственной точки IPoint3D::Parameters.
    """

    @property
    def AssociationVertex(self) -> IModelObject:  # ipoint3dparamprojection_associationvertex.html
        """
        ## AssociationVertex - Получить опорную точку

        Тип данных: указатель на интерфейс объекта IModelObject

        ### Синтаксис Automation

        ```
        AssociationVertex = iObject.AssociationVertex       # Получить свойство (* )
        AssociationVertex = iObject.GetAssociationVertex()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def GuidingObject(self) -> IModelObject:  # ipoint3dparamprojection_guidingobject.html
        """
        ## GuidingObject - Получить направляющий объект

        Тип данных: указатель на интерфейс объекта IModelObject

        ### Синтаксис Automation

        ```
        GuidingObject = iObject.GuidingObject;       # Получить свойство (* )
        GuidingObject = iObject.GetGuidingObject();  # Получить свойство (**)
        ```
        """
        ...

    @property
    def SurfaceObject(self) -> IModelObject:  # ipoint3dparamprojection_surfaceobject.html
        """
        ## SurfaceObject - Получить поверхность

        Тип данных: указатель на интерфейс объекта IModelObject

        ### Синтаксис Automation

        ```
        SurfaceObject = iObject.SurfaceObject;       # Получить свойство (* )
        SurfaceObject = iObject.GetSurfaceObject();  # Получить свойство (**)
        ```
        """
        ...

    @property
    def Vector3D(self) -> IVector3D:  # ipoint3dparamprojection_vector3d.html
        """
        ## Vector3D - Получить параметры вектора

        Тип данных: указатель на интерфейс IVector3D

        1. Свойство позволяет получить интерфейс вектора, задающего направление проецирования точки.

        2. Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        Vector3D = Object.Vector3D       # Получить свойство (* )
        Vector3D = Object.GetVector3D()  # Получить свойство (**)
        ```
        """
        ...

    def SetAssociationVertex(self, NewVal) -> bool:  # ipoint3dparamprojection_setassociationvertex.html
        """
        ## SetAssociationVertex - Установить опорную точку

        ### Синтаксис Automation

        ```
        BOOL SetAssociationVertex( LPDISPATCH obj );
        ```

        ### Входные параметры

        +------------------------+
        | obj | - опорная точка. |
        +------------------------+

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def SetGuidingObject(self, NewVal) -> bool:  # ipoint3dparamprojection_setguidingobject.html
        """
        ## SetGuidingObject - Установить направляющий объект

        ### Синтаксис Automation

        ```
        BOOL SetGuidingObject( LPDISPATCH obj );
        ```

        ### Входные параметры

        +------------------------+
        | obj | - опорная точка. |
        +------------------------+

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def SetSurfaceObject(self, NewVal) -> bool:  # ipoint3dparamprojection_setsurfaceobject.html
        """
        ## SetSurfaceObject - Установить объект1

        ### Синтаксис Automation

        ```
        BOOL SetSurfaceObject( LPDISPATCH obj );
        ```

        ### Входные параметры

        +----------------------+
        | obj | - поверхность. |
        +----------------------+

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...




class IPoint3DParamSurface(IKompasAPIObject):  # ipoint3dparamsurface.html
    """
    ## Интерфейс IPoint3DParamSurface

    Интерфейс параметров пространственной точки, заданной на поверхности.

    Интерфейс можно получить с помощью свойства пространственной точки IPoint3D::Parameters.
    """

    @property
    def AllowBoundaries(self) -> bool:  # ipoint3dparamsurface_allowboundaries.html
        """
        ## AllowBoundaries - TRUE - Учитывать границы

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        AllowBoundaries = Object.AllowBoundaries       # Получить свойство (* )
        Object.AllowBoundaries = AllowBoundaries       # Установить свойство (* )
        AllowBoundaries = Object.GetAllowBoundaries()  # Получить свойство (**)
        Object.SetAllowBoundaries( AllowBoundaries )   # Установить свойство (**)
        ```
        """
        ...
    @AllowBoundaries.setter
    def AllowBoundaries(self, value: bool): ...

    @property
    def Object1(self) -> IModelObject:  # ipoint3dparamsurface_object1.html
        """
        ## Object1- Получить объект1

        Тип данных: указатель на интерфейс объекта IModelObject

        ### Синтаксис Automation

        ```
        Object1 = iObject.Object1;       # Получить свойство (* )
        Object1 = iObject.GetObject1();  # Получить свойство (**)
        ```
        """
        ...

    @property
    def Object2(self) -> IModelObject:  # ipoint3dparamsurface_object2.html
        """
        ## Object2 - Получить объект2

        Тип данных: указатель на интерфейс объекта IModelObject

        ### Синтаксис Automation

        ```
        Object2 = iObject.Object2;       # Получить свойство (* )
        Object2 = iObject.GetObject2();  # Получить свойство (**)
        ```
        """
        ...

    @property
    def Offset1(self) -> float:  # ipoint3dparamsurface_offset1.html
        """
        ## Offset1 - Величина смещения1

        Тип данных: double

        1. Для типа смещения по параметру U и V (ksOffsetByUV) - свойство 0..100 (величина U в %).

        2. Для типа смещения по расстояниям от плоских объектов (ksOffsetByLenFromObj) - свойство определяет расстояние от плоского объекта 1.

        ### Синтаксис Automation

        ```
        Offset1 = iObject.Offset1;       # Получить свойство (* )
        iObject.Offset1 = Offset1;       # Установить свойство (* )
        Offset1 = iObject.GetOffset1();  # Получить свойство (**)
        iObject.SetOffset1( Offset1 );   # Установить свойство (**)
        ```
        """
        ...
    @Offset1.setter
    def Offset1(self, value: float): ...

    @property
    def Offset2(self) -> float:  # ipoint3dparamsurface_offset2.html
        """
        ## Offset2 - Величина смещения2

        Тип данных: double

        1. Для типа смещения по параметру U и V (ksOffsetByUV) - свойство 0..100 (величина V в %).

        2. Для типа смещения по расстояниям от плоских объектов (ksOffsetByLenFromObj) - свойство определяет расстояние от плоского объекта 2.

        ### Синтаксис Automation

        ```
        Offset2 = iObject.Offset2;       # Получить свойство (* )
        iObject.Offset2 = Offset2;       # Установить свойство (* )
        Offset2 = iObject.GetOffset2();  # Получить свойство (**)
        iObject.SetOffset2( Offset2 );   # Установить свойство (**)
        ```
        """
        ...
    @Offset2.setter
    def Offset2(self, value: float): ...

    @property
    def OffsetType(self) -> int:  # ipoint3dparamsurface_offsettype.html
        """
        ## OffsetType - Тип задания смещения

        Тип данных: из перечисления ksPoint3DCurveParamTypeEnum

        ### Синтаксис Automation

        ```
        OffsetType = iObject.OffsetType;       # Получить свойство (* )
        iObject.OffsetType = OffsetType;       # Установить свойство (* )
        OffsetType = iObject.GetOffsetType();  # Получить свойство (**)
        iObject.SetOffsetType( OffsetType );   # Установить свойство (**)
        ```
        """
        ...
    @OffsetType.setter
    def OffsetType(self, value: int): ...

    @property
    def SurfaceObject(self) -> IModelObject:  # ipoint3dparamsurface_surfaceobject.html
        """
        ## SurfaceObject - Получить поверхность

        Тип данных: указатель на интерфейс объекта IModelObject

        ### Синтаксис Automation

        ```
        SurfaceObject = iObject.SurfaceObject;       # Получить свойство (* )
        SurfaceObject = iObject.GetSurfaceObject();  # Получить свойство (**)
        ```
        """
        ...

    def SetObject1(self, NewVal) -> bool:  # ipoint3dparamsurface_setobject1.html
        """
        ## SetObject1 - Установить объект1

        ### Синтаксис Automation

        ```
        BOOL SetObject1( LPDISPATCH obj );
        ```

        ### Входные параметры

        +-------------------------+
        | obj | - плоский объект. |
        +-------------------------+

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def SetObject2(self, NewVal) -> bool:  # ipoint3dparamsurface_setobject2.html
        """
        ## SetObject2 - Установить объект2

        ### Синтаксис Automation

        ```
        BOOL SetObject2( LPDISPATCH obj );
        ```

        ### Входные параметры

        +-------------------------+
        | obj | - плоский объект. |
        +-------------------------+

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def SetSurfaceObject(self, NewVal) -> bool:  # ipoint3dparamsurface_setsurfaceobject.html
        """
        ## SetSurfaceObject - Установить поверхность

        ### Синтаксис Automation

        ```
        BOOL SetSurfaceObject( LPDISPATCH obj );
        ```

        ### Входные параметры

        +----------------------+
        | obj | - поверхность. |
        +----------------------+

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...




class IPoint3DParamByCylinder(IKompasAPIObject):  # ipoint3dparambycylinder.html
    """
    ## Интерфейс IPoint3DParamByCylinder

    Интерфейс параметров пространственной точки, заданной по цилиндрическим координатам
    """

    @property
    def A(self) -> float:  # ipoint3dparambycylinder_a.html
        """
        ## A - Координата A (угол поворота точки)

        Тип данных: double

        ### Синтаксис Automation

        ```
        A = Object.A       # Получить свойство (* )
        Object.A = A       # Установить свойство (* )
        A = Object.GetA()  # Получить свойство (**)
        Object.SetA( A )   # Установить свойство (**)
        ```
        """
        ...
    @A.setter
    def A(self, value: float): ...

    @property
    def AssociationObject(self) -> IModelObject:  # ipoint3dparambycylinder_associationobject.html
        """
        ## AssociationObject - Опорный объект

        Тип данных: указатель на интерфейс объекта IModelObject

        Если точка связана с точечным объектом, то координаты недоступны для изменения.

        ### Синтаксис Automation

        ```
        AssociationObject = Object.AssociationObject       # Получить свойство (* )
        Object.AssociationObject = AssociationObject       # Установить свойство (* )
        AssociationObject = Object.GetAssociationObject()  # Получить свойство (**)
        Object.SetAssociationObject( AssociationObject )   # Установить свойство (**)
        ```
        """
        ...
    @AssociationObject.setter
    def AssociationObject(self, value: IModelObject): ...

    @property
    def R(self) -> float:  # ipoint3dparambycylinder_r.html
        """
        ## R - Координата R (радиус цилиндра)

        Тип данных: double

        ### Синтаксис Automation

        ```
        R = Object.R       # Получить свойство (* )
        Object.R = R       # Установить свойство (* )
        R = Object.GetR()  # Получить свойство (**)
        Object.SetR( R )   # Установить свойство (**)
        ```
        """
        ...
    @R.setter
    def R(self, value: float): ...

    @property
    def RadiusObject(self) -> IModelObject:  # ipoint3dparambycylinder_radiusobject.html
        """
        ## RadiusObject - Объект радиуса

        Тип данных: указатель на интерфейс объекта IModelObject

        Если задан объект, определяющий радиус, координата R недоступна для изменения.

        ### Синтаксис Automation

        ```
        RadiusObject = Object.RadiusObject       # Получить свойство (* )
        Object.RadiusObject = RadiusObject       # Установить свойство (* )
        RadiusObject = Object.GetRadiusObject()  # Получить свойство (**)
        Object.SetRadiusObject( RadiusObject )   # Установить свойство (**)
        ```
        """
        ...
    @RadiusObject.setter
    def RadiusObject(self, value: IModelObject): ...

    @property
    def Z(self) -> float:  # ipoint3dparambycylinder_z.html
        """
        ## Z - Координата по оси Z

        Тип данных: double

        ### Синтаксис Automation

        ```
        Z = Object.Z      # Получить свойство (* )
        Object.Z = Z      # Установить свойство (* )
        Object.Z = Z      # Получить свойство (**)
        Object.SetZ( Z )  # Установить свойство (**)
        ```
        """
        ...
    @Z.setter
    def Z(self, value: float): ...




class IPoint3DParamBySphere(IKompasAPIObject):  # ipoint3dparambysphere.html
    """
    ## Интерфейс IPoint3DParamBySphere

    Интерфейс параметров пространственной точки, заданной по сферическим координатам.
    """

    @property
    def A(self) -> float:  # ipoint3dparambysphere_a.html
        """
        ## A - Координата A (азимутальный угол)

        Тип данных: double

        ### Синтаксис Automation

        ```
        A = Object.A       # Получить свойство (* )
        Object.A = A       # Установить свойство (* )
        A = Object.GetA()  # Получить свойство (**)
        Object.SetA( A )   # Установить свойство (**)
        ```
        """
        ...
    @A.setter
    def A(self, value: float): ...

    @property
    def AssociationObject(self) -> IModelObject:  # ipoint3dparambysphere_associationobject.html
        """
        ## AssociationObject - Опорный объект

        Тип данных: указатель на интерфейс объекта IModelObject

        Если точка связана с точечным объектом, то координаты недоступны для изменения.

        ### Синтаксис Automation

        ```
        AssociationObject = Object.AssociationObject       # Получить свойство (* )
        Object.AssociationObject = AssociationObject       # Установить свойство (* )
        AssociationObject = Object.GetAssociationObject()  # Получить свойство (**)
        Object.SetAssociationObject( AssociationObject )   # Установить свойство (**)
        ```
        """
        ...
    @AssociationObject.setter
    def AssociationObject(self, value: IModelObject): ...

    @property
    def B(self) -> float:  # ipoint3dparambysphere_b.html
        """
        ## B - Координата B (зенитный угол)

        Тип данных: double

        ### Синтаксис Automation

        ```
        B = Object.B       # Получить свойство (* )
        Object.B = B       # Установить свойство (* )
        B = Object.GetB()  # Получить свойство (**)
        Object.SetB( B )   # Установить свойство (**)
        ```
        """
        ...
    @B.setter
    def B(self, value: float): ...

    @property
    def R(self) -> float:  # ipoint3dparambysphere_r.html
        """
        ## R - Координата R (радиус сферы)

        Тип данных: double

        ### Синтаксис Automation

        ```
        R = Object.R       # Получить свойство (* )
        Object.R = R       # Установить свойство (* )
        R = Object.GetR()  # Получить свойство (**)
        Object.SetR( R )   # Установить свойство (**)
        ```
        """
        ...
    @R.setter
    def R(self, value: float): ...

    @property
    def RadiusObject(self) -> IModelObject:  # ipoint3dparambysphere_radiusobject.html
        """
        ## RadiusObject - Объект радиуса

        Тип данных: указатель на интерфейс объекта IModelObject

        Если задан объект, определяющий радиус, координата R недоступна для изменения.

        ### Синтаксис Automation

        ```
        RadiusObject = Object.RadiusObject       # Получить свойство (* )
        Object.RadiusObject = RadiusObject       # Установить свойство (* )
        RadiusObject = Object.GetRadiusObject()  # Получить свойство (**)
        Object.SetRadiusObject( RadiusObject )   # Установить свойство (**)
        ```
        """
        ...
    @RadiusObject.setter
    def RadiusObject(self, value: IModelObject): ...




class ISketchs(IModelObjects):  # isketchs.html
    """
    ## Интерфейс ISketchs

    Интерфейс коллекции элементов "Эскиз".

    Получить интерфейс можно, используя свойство контейнера объектов 3D IModelContainer::Sketchs.
    """

    def Add(self) -> ISketch:  # isketchs_add.html
        """
        ## Add - Создать объект "Эскиз"

        1. Метод позволяет создать новый объект "Эскиз".

        2. После получения нового интерфейса нужно задать параметры листового тела и вызвать метод IModelObject::Update.

        ### Синтаксис Automation

        ```
        LPDISPATH Add();
        ```

        ### Возвращаемое значение

        +-------------------------------------------------------------------------+
        | Указатель на интерфейс эскиза ISketch | - в случае удачного завершения. |
        +-------------------------------------------------------------------------+
        """
        ...

    def Sketch(self, Index) -> ISketch:  # isketchs_sketch.html
        """
        ## Sketch - Элемент "Эскиз", заданный по индексу или ссылке

        Тип данных: указатель на интерфейс ISketch.

        В качестве индекса может использоваться:

        •индекс объекта в коллекции,

        •ссылка на объект (reference).

        ### Синтаксис Automation

        ```
        ISketch = iObject.Isketch( index )  # Получить свойство (* )
        ISketch = iObject.Isketch( index )  # Получить свойство (**)
        ```

        ### Входные параметры

        +------------------------------------+
        | VARIANT index | - индекс операции. |
        +------------------------------------+
        """
        ...




class ISketch(IModelObject):  # isketch.html
    """
    ## Интерфейс ISketch

    Интерфейс элемента Эскиз.

    Интерфейс можно получить с помощью метода коллекции эскизов ISketchs::Add или свойства ISketchs::Sketch.

    С помощью метода IUnknown::QueryInterface у интерфейса можно получить интерфейс подчиненного объекта ЛСК ILocalCSObject.
    """

    @property
    def Angle(self) -> float:  # isketch_angle.html
        """
        ## Angle - Угол поворота эскиза относительно проекции системы координат модели на плоскость эскиза (в градусах)

        Тип данных: double

        ### Синтаксис Automation

        ```
        Angle = iObject.Angle;       # Получить свойство (* )
        iObject.Angle = Angle;       # Установить свойство (* )
        Angle = iObject.GetAngle();  # Получить свойство (**)
        iObject.SetAngle( Angle );   # Установить свойство (**)
        ```
        """
        ...
    @Angle.setter
    def Angle(self, value: float): ...

    @property
    def AssociationObject(self) -> IModelObject:  # isketch_associationobject.html
        """
        ## AssociationObject - Точка привязки

        Тип данных: Указатель на интерфейс IModelObject

        ### Синтаксис Automation

        ```
        AssociationObject = Object.AssociationObject;        # Получить свойство (* )
        Object.AssociationObject = AssociationObject;        # Установить свойство (* )
        AssociationObject = Object.GetAssociationObject());  # Получить свойство (**)
        Object.SetAssociationObject( AssociationObject );    # Установить свойство (**)
        ```
        """
        ...
    @AssociationObject.setter
    def AssociationObject(self, value: IModelObject): ...

    @property
    def ConstraintsState(self) -> int:  # isketch_constraintsstate.html
        """
        ## ConstraintsState - Параметрическое состояние эскиза

        Тип данных: Значение из перечисления ksConstraintsStateEnum

        Свойство доступно только для чтения.

        Версия КОМПАС v22

        ### Синтаксис Automation

        ```
        ConstraintsState = Object.ConstraintsState       # Получить свойство(* )
        ConstraintsState = Object.GetConstraintsState()  # Получить свойство(**)
        ```
        """
        ...

    @property
    def CoordinateSystem(self) -> IModelObject:  # isketch_coordinatesystem.html
        """
        ## CoordinateSystem - система координат эскиза

        Тип данных: Указатель на интерфейс IModelObject

        ### Синтаксис Automation

        ```
        CoordinateSystem = Object.CoordinateSystem;       # Получить свойство (* )
        Object.CoordinateSystem = CoordinateSystem;       # Установить свойство (* )
        CoordinateSystem = Object.GetCoordinateSystem();  # Получить свойство (**)
        Object.SetCoordinateSystem( CoordinateSystem );   # Установить свойство (**)
        ```
        """
        ...
    @CoordinateSystem.setter
    def CoordinateSystem(self, value: IModelObject): ...

    @property
    def Fixed(self) -> bool:  # isketch_fixed.html
        """
        ## Fixed - Фиксация

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        Fixed = Object.Fixed;       # Получить свойство (* )
        Object.Fixed = Fixed;       # Установить свойство (* )
        Fixed = Object.GetFixed();  # Получить свойство (**)
        Object.SetFixed( Fixed );   # Установить свойство (**)
        ```
        """
        ...
    @Fixed.setter
    def Fixed(self, value: bool): ...

    @property
    def LeftHandedCS(self) -> bool:  # isketch_lefthandedcs.html
        """
        ## LeftHandedCS - Признак левосторонней системы координат

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        LeftHandedCS = Object.LeftHandedCS;       # Получить свойство (* )
        Object.LeftHandedCS = LeftHandedCS;       # Установить свойство (* )
        LeftHandedCS = Object.GetLeftHandedCS();  # Получить свойство (**)
        Object.SetLeftHandedCS( LeftHandedCS );   # Установить свойство (**)
        ```
        """
        ...
    @LeftHandedCS.setter
    def LeftHandedCS(self, value: bool): ...

    @property
    def Plane(self) -> IModelObject:  # isketch_plane.html
        """
        ## Plane - Опорная плоскость

        Тип данных: указатель на интерфейс IModelObject

        ### Синтаксис Automation

        ```
        Plane = iObject.Plane;       # Получить свойство (* )
        iObject.Plane = Plane;       # Установить свойство (* )
        Plane = iObject.GetPlane();  # Получить свойство (**)
        iObject.SetPlane( Plane );   # Установить свойство (**)
        ```
        """
        ...
    @Plane.setter
    def Plane(self, value: IModelObject): ...

    def AddProjectionOf(self, Object) -> list[IDispatch]:  # isketch_addprojectionof.html
        """
        ## AddProjectionOf - Добавить в эскиз проекцию объекта

        ### Синтаксис Automation

        ```
        VARIANT AddProjectionOf( IModelObject * Object );
        ```

        ### Входные параметры

        +-------------------------------------------------+
        | Object | - указатель на интерфейс IModelObject. |
        +-------------------------------------------------+

        Версия КОМПАС v22

        ### Возвращаемое значение

        - Массив созданных проекционных объектов в виде в виде SAFEARRAY'я DISPATCH - VT_ARRAY | VT_DISPATCH.
        """
        ...

    def AddSketch(self, Sketch, Plane) -> bool:  # isketch_addsketch.html
        """
        ## AddSketch - Создать эскиз из уже имеющегося в заданной плоскости

        ### Синтаксис Automation

        ```
        BOOL AddSketch( IModelObject * Sketch, IModelObject * Plane );
        ```

        ### Входные параметры

        +------------------------------------+
        | Sketch | - указатель на эскиз,     |
        +------------------------------------+
        | Plane  | - указатель на плоскость. |
        +------------------------------------+

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def BeginEdit(self) -> IFragmentDocument|IFragmentDocument:  # isketch_beginedit.html
        """
        ## BeginEdit - Войти в режим редактирования эскиза

        После редактирования необходимо вызвать функцию ISketch::EndEdit.

        ### Синтаксис Automation

        ```
        BeginEdit();
        ```

        ### Возвращаемое значение

        +---------------------------------------------------------+
        | SketchDoc | - указатель на интерфейс IFragmentDocument. |
        +---------------------------------------------------------+

        +---------------------------------------------------------+
        | SketchDoc | - указатель на интерфейс IFragmentDocument. |
        +---------------------------------------------------------+
        """
        ...

    def BeginEditEx(self, ReadOnly) -> bool:  # isketch_begineditex.html
        """
        ## BeginEditEx - Войти в режим редактирования эскиза

        ### Синтаксис Automation

        ```
        LPDISPATCH BeginEditEx( BOOL ReadOnly );
        ```

        ### Входные параметры

        +-----------------------------------+
        | ReadOnly | - = TRUE - для чтения. |
        +-----------------------------------+

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def DeleteWrongProjection(self) -> bool:  # isketch_deletewrongprojection.html
        """
        ## DeleteWrongProjection - Удалить ошибочные проекции

        ### Синтаксис Automation

        ```
        BOOL DeleteWrongProjection();
        ```

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def DirectingObject(self, Axis) -> IModelObject:  # isketch_directingobject.html
        """
        ## DirectingObject - Направляющий объект для оси

        Тип данных: Указатель на интерфейс IModelObject

        1. При указании базовой плоскости можно задать направляющий объект для оси X или оси Y.

        2. Направление второй оси будет определяться как векторное произведение оси Z, направленной по нормали к плоскости, и заданным направлением.

        3. Направление осей можно задать также параметрами вектора.

        ### Синтаксис Automation

        ```
        DirectingObject = Object.DirectingObject( Axis );     # Получить свойство (* )
        Object.DirectingObject( Axis ) = DirectingObject;     # Установить свойство (* )
        DirectingObject = Object.GetDirectingObject( Axis );  # Получить свойство (**)
        Object.SetDirectingObject( Axis, DirectingObject );   # Установить свойство (**)
        ```

        ### Входные параметры

        +----------------------------------------------------------+
        | Axis | - тип оси: o3d_axisOX - ось X, o3d_axisOY - ось Y |
        +----------------------------------------------------------+
        """
        ...

    def Edges(self, EdgesType) -> typing.Any:  # isketch_edges.html
        """
        ## Edges - Массив рёбер(SAFEARRAY)

        Тип данных: VARIANT

        Свойство доступно только для чтения.

        Версия КОМПАС v22

        ### Синтаксис Automation

        ```
        Edges = Object.Edges( EdgesType )     # Получить свойство(* )
        Edges = Object.GetEdges( EdgesType )  # Получить свойство(**)
        ```
        """
        ...

    def EndEdit(self) -> bool:  # isketch_endedit.html
        """
        ## EndEdit - Выйти из режима редактирования эскиза

        ### Синтаксис Automation

        ```
        BOOL EndEdit();
        ```

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def GetLocation(self) -> bool:  # isketch_getlocation.html
        """
        ## GetLocation - Получить смещение системы координат эскиза относительно проекции системы координат модели на плоскость эскиза

        ### Синтаксис Automation

        ```
        BOOL GetLocation( double X, double Y);
        ```

        ### Выходные параметры

        +---------------------------------------------------+
        | X | - смещение системы координат эскиза по оси X, |
        +---------------------------------------------------+
        | Y | - смещение системы координат эскиза по оси Y. |
        +---------------------------------------------------+

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def GetLoftPoint(self) -> bool:  # isketch_getloftpoint.html
        """
        ## GetLoftPoint - Получить координаты точки в плоскости эскиза

        ### Синтаксис Automation

        ```
        BOOL GetLoftPoint( double X, double Y);
        ```

        ### Выходные параметры

        +-------------------------------------------+
        | X | - значение координаты точки по оси X, |
        +-------------------------------------------+
        | Y | - значение координаты точки по оси Y. |
        +-------------------------------------------+

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def GetPoint3D(self, XIn, YIn) -> bool:  # isketch_getpoint3d.html
        """
        ## GetPoint3D - Получить пространственную точку по точке на плоскости XY

        ### Синтаксис Automation

        ```
        BOOL GetPoint3D( double XIn, double YIn, double * XOut, double * YOut, double * ZOut );
        ```

        ### Входные параметры

        +----------------------------------+
        | XIn, YIn | -- координаты эскиза. |
        +----------------------------------+

        ### Выходные параметры

        +----------------------------------------+
        | XIn, YIn, ZIn | - координаты 3D точки. |
        +----------------------------------------+

        Версия КОМПАС v22

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def GetPointProjectionToXY(self, XIn, YIn, ZIn) -> bool:  # isketch_getpointprojectiontoxy.html
        """
        ## GetPointProjectionToXY - Проекция точки на плоскость XY

        ### Синтаксис Automation

        ```
        BOOL GetPointProjectionToXY( double XIn, double YIn, double ZIn, double * XOut, double * YOut );
        ```

        ### Входные параметры

        +-----------------------------------------+
        | XIn, YIn, ZIn | -- координаты 3D точки. |
        +-----------------------------------------+

        ### Выходные параметры

        +-----------------------------------------------+
        | XOut, YOut | - координаты проекционной точки. |
        +-----------------------------------------------+

        Версия КОМПАС v22

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def RotateAxis(self, Axis) -> bool:  # isketch_rotateaxis.html
        """
        ## RotateAxis - Сменить направление оси на противоположное

        1. Метод позволяет менять направления осей на противоположные.

        2. Для осей можно выбирать направляющие объекты (см. метод ISketch::DirectingObject ,ISketch::Vector3D).

        3. Можно выбрать направление для оси X или оси Y, третья ось ориентируется, исходя из положения оси Z и выбранной.

        4. Менять направление на противоположное можно только у выбранных осей.

        ### Синтаксис Automation

        ```
        BOOL RotateAxis( ksObj3dTypeEnum axis );
        ```

        ### Входные параметры

        +-------------------------------------------------------------------------------------------------------+
        | axis | - тип оси из перечисления ksObj3dTypeEnum (должны передаваться типы : o3d_axisOX o3d_axisOY ). |
        +-------------------------------------------------------------------------------------------------------+

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def SetDirectingObject(self, Axis, arg1):
        ...

    def SetLocation(self, X, Y) -> bool:  # isketch_setlocation.html
        """
        ## SetLocation - Установить смещение системы координат эскиза относительно проекции системы координат модели на плоскость эскиза

        ### Синтаксис Automation

        ```
        BOOL SetLocation( double X, double Y);
        ```

        ### Входные параметры

        +---------------------------------------------------+
        | X | - смещение системы координат эскиза по оси X, |
        +---------------------------------------------------+
        | Y | - смещение системы координат эскиза по оси Y. |
        +---------------------------------------------------+

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def SetLoftPoint(self, X, Y) -> bool:  # isketch_setloftpoint.html
        """
        ## SetLoftPoint - Установить координаты точки в плоскости эскиза

        ### Синтаксис Automation

        ```
        BOOL SetLoftPoint( double X, double Y);
        ```

        ### Входные параметры

        +-------------------------------------------+
        | X | - значение координаты точки по оси X, |
        +-------------------------------------------+
        | Y | - значение координаты точки по оси Y. |
        +-------------------------------------------+

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def UserSetPlacement(self, Prompt) -> bool:  # isketch_usersetplacement.html
        """
        ## UserSetPlacement - Разместить эскиз

        ### Синтаксис Automation

        ```
        BOOL UserSetPlacement( BSTR Prompt );
        HRESULT UserSetPlacement( BSTR Prompt, BOOL * Result );
        ```

        ### Входные параметры

        +--------------------------------+
        | Prompt | - cтрока приглашение. |
        +--------------------------------+

        Версия КОМПАС v22

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def Vector3D(self, Axis) -> IVector3D:  # isketch_vector3d.html
        """
        ## Vector3D - Вектор, задающий направление оси

        Тип данных: Указатель на интерфейс IVector3D

        1. При указании базовой плоскости можно задать направляющий объект для оси X или оси Y.

        2. Направление второй оси будет определяться как векторное произведение оси Z, направленной по нормали к плоскости, и заданным направлением.

        3. Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        Vector3D = Object.Vector3D( Axis )     # Получить свойство(* )
        Vector3D = Object.GetVector3D( Axis )  # Получить свойство(**)
        ```

        ### Входные параметры

        +----------------------------------------------------------+
        | Axis | - тип оси: o3d_axisOX - ось X, o3d_axisOY - ось Y |
        +----------------------------------------------------------+
        """
        ...

    def WriteToFragment(self, FileName) -> bool:  # isketch_writetofragment.html
        """
        ## WriteToFragment - Сохранить эскиз во фрагмент

        ### Синтаксис Automation

        ```
        BOOL AddSketch( IModelObject * Sketch, IModelObject * Plane );
        ```

        ### Входные параметры

        +-----------------------------------+
        | FileName | - имя файла фрагмента. |
        +-----------------------------------+

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...




class IMathSurface3D(IKompasAPIObject):  # imathsurface3d.html
    """
    ## Интерфейс IMathSurface3D

    Интерфейс математической поверхности в трехмерном пространстве.
    """

    @property
    def BaseSurface3DType(self) -> int:  # imathsurface3d_basesurface3dtype.html
        """
        ## BaseSurface3DType - Тип базовой поверхности

        Тип данных: из перечисления ksMathSurface3DTypeEnum

        Свойство доступно только для чтения.

        Версия КОМПАС v22

        ### Синтаксис Automation

        ```
        BaseSurface3DType = Object.BaseSurface3DType       # Получить свойство (* )
        BaseSurface3DType = Object.GetBaseSurface3DType()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def BoundaryCount(self) -> int:  # imathsurface3d_boundarycount.html
        """
        ## BoundaryCount - Получить количество границ

        Тип данных: long

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        BoundaryCount = Object.BoundaryCount       # Получить свойство (* )
        BoundaryCount = Object.GetBoundaryCount()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def ClosedU(self) -> bool:  # imathsurface3d_closedu.html
        """
        ## ClosedU - Получить замкнутость кривой по U

        Тип данных: BOOL

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        ClosedU = Object.ClosedU       # Получить свойство (* )
        ClosedU = Object.GetClosedU()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def ClosedV(self) -> bool:  # imathsurface3d_closedv.html
        """
        ## ClosedV - Получить замкнутость кривой по V

        Тип данных: BOOL

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        ClosedV= Object.ClosedV        # Получить свойство (* )
        ClosedV = Object.GetClosedV()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def IsCone(self) -> bool:  # mathsurface3d_iscone.html
        """
        ## IsCone - Является ли поверхность конической

        Тип данных: BOOL

        Свойство доступно только для чтения

        Версия КОМПАС v22.

        ### Синтаксис Automation

        ```
        IsCone = Object.IsCone       # Получить свойство (* )
        IsCone = Object.GetIsCone()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def IsCylinder(self) -> bool:  # imathsurface3d_iscylinder.html
        """
        ## IsCylinder - Является ли поверхность цилиндрической

        Тип данных: BOOL

        Свойство доступно только для чтения.

        Версия КОМПАС v22

        ### Синтаксис Automation

        ```
        IsCylinder = Object.IsCylinder       # Получить свойство (* )
        IsCylinder = Object.GetIsCylinder()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def IsNurbsSurface(self) -> bool:  # imathsurface3d_isnurbssurface.html
        """
        ## IsNurbsSurface - Является ли поверхность NURBS-поверхностью

        Тип данных:BOOL

        Свойство доступно только для чтения.

        Версия КОМПАС v22

        ### Синтаксис Automation

        ```
        IsNurbsSurface = Object.IsNurbsSurface       # Получить свойство (* )
        IsNurbsSurface = Object.GetIsNurbsSurface()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def IsPlanar(self) -> bool:  # imathsurface3d_isplanar.html
        """
        ## IsPlanar - Является ли поверхность плоской

        Тип данных:BOOL

        Свойство доступно только для чтения.

        Версия КОМПАС v22

        ### Синтаксис Automation

        ```
        IsPlanar = Object.IsPlanar       # Получить свойство (* )
        IsPlanar = Object.GetIsPlanar()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def IsRevolved(self) -> bool:  # imathsurface3d_isrevolved.html
        """
        ## IsRevolved - Определяется ли поверхность поверхностью вращения

        Тип данных:BOOL

        Свойство доступно только для чтения.

        Версия КОМПАС v22

        ### Синтаксис Automation

        ```
        IsRevolved = Object.IsRevolved       # Получить свойство (* )
        IsRevolved = Object.GetIsRevolved()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def IsSalient(self) -> int:  # imathsurface3d_issalient.html
        """
        ## IsSalient - Является ли поверхность выпуклой

        Тип данных: из перечисления ksSurfaceSalientTypeEnum

        Свойство доступно только для чтения.

        Версия КОМПАС v22

        ### Синтаксис Automation

        ```
        IsSalient = Object.IsSalient       # Получить свойство (* )
        IsSalient = Object.GetIsSalient()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def IsSphere(self) -> bool:  # imathsurface3d_issphere.html
        """
        ## IsSphere - Является ли поверхность сферической

        Тип данных: BOOL

        Свойство доступно только для чтения.

        Версия КОМПАС v22

        ### Синтаксис Automation

        ```
        IsSphere = Object.IsSphere       # Получить свойство (* )
        IsSphere = Object.GetIsSphere()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def IsSwept(self) -> bool:  # imathsurface3d_isswept.html
        """
        ## IsSwept - Определяется ли поверхность поверхностью по траектории

        Тип данных:BOOL

        Свойство доступно только для чтения.

        Версия КОМПАС v22

        ### Синтаксис Automation

        ```
        IsSwept = Object.IsSwept       # Получить свойство (* )
        IsSwept = Object.GetIsSwept()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def IsTorus(self) -> bool:  # imathsurface3d_istorus.html
        """
        ## IsTorus - Является ли поверхность тором

        Тип данных: BOOL

        Свойство доступно только для чтения.

        Версия КОМПАС v22

        ### Синтаксис Automation

        ```
        IsTorus = Object.IsTorus       # Получить свойство (* )
        IsTorus = Object.GetIsTorus()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def ParamUMax(self) -> float:  # imathsurface3d_paramumax.html
        """
        ## ParamUMax - Получить значение параметра U конечное

        Тип данных: double

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        ParamUMax = Object.ParamUMax       # Получить свойство (* )
        ParamUMax = Object.GetParamUMax()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def ParamUMin(self) -> float:  # imathsurface3d_paramumin.html
        """
        ## ParamUMin - Получить значение параметра U начальное

        Тип данных: double

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        ParamUMin = Object.ParamUMin       # Получить свойство (* )
        ParamUMin = Object.GetParamUMin()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def ParamVMax(self) -> float:  # imathsurface3d_paramvmax.html
        """
        ## ParamVMax - Получить значение параметра V конечное

        Тип данных: double

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        ParamVMax = Object.ParamVMax       # Получить свойство (* )
        ParamVMax = Object.GetParamVMax()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def ParamVMin(self) -> float:  # imathsurface3d_paramvmin.html
        """
        ## ParamVMin - Получить значение параметра V начальное

        Тип данных: double

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        ParamVMin = Object.ParamVMin       # Получить свойство (* )
        ParamVMin = Object.GetParamVMin()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def Placement(self) -> IPlacement3D:  # imathsurface3d_placement.html
        """
        ## Placement - Интерфейс локальной системы координат (положение объекта)

        Тип данных: Указатель на интерфейс IPlacement3D

        Свойство доступно только для чтения.

        Версия КОМПАС v22

        ### Синтаксис Automation

        ```
        Placement = Object.Placement       # Получить свойство (* )
        Placement = Object.GetPlacement()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def Radius(self) -> float:  # imathsurface3d_radius.html
        """
        ## Radius - Радиус поверхности

        Тип данных: double

        Свойство доступно только для чтения.

        Версия КОМПАС v22

        ### Синтаксис Automation

        ```
        Radius = Object.Radius       # Получить свойство (* )
        Radius = Object.GetRadius()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def Surface3DType(self) -> int:  # imathsurface3d_surface3dtype.html
        """
        ## Surface3DType - Тип поверхности

        Тип данных: из перечисления ksMathSurface3DTypeEnum

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        Surface3DType = Object.Surface3DType       # Получить свойство (* )
        Surface3DType = Object.GetSurface3DType()  # Получить свойство (**)
        ```
        """
        ...

    def GetArea(self, BitVector):  # imathsurface3d_getarea.html
        """
        ## GetArea - Получить площадь грани (ST_MIX_MM..ST_MIX_M единицы измерения)

        ### Синтаксис Automation

        ```
        double GetArea( ksLengthUnitsEnum BitVector );
        ```

        ### Входные параметры

        +--------------------------------------------------------------------+
        | BitVector | - единицы измерения в интервале [ST_MIX_MM..ST_MIX_M]. |
        +--------------------------------------------------------------------+

        ### Возвращаемое значение

        - площадь поверхности.
        """
        ...

    def GetAreaEx(self, Unit, AngleTolerance):  # imathsurface3d_getareaex.html
        """
        ## GetAreaEx - Получить площадь грани

        ### Синтаксис Automation

        ```
        double GetAreaEx( ksLengthUnitsEnum Unit, double AngleTolerance );
        ```

        ### Входные параметры

        +--------------------------------------------------------------------------------------------------------+
        | Unit           | - единицы измерения длины из перечисления ksLengthUnitsEnum,                          |
        +--------------------------------------------------------------------------------------------------------+
        | AngleTolerance | - ограничение углового отклонения при аппроксимации оболочки треугольными пластинами. |
        +--------------------------------------------------------------------------------------------------------+

        Версия Компас v21
        """
        ...

    def GetBoundaryUVNurbs(self, UV, Closed, LoopIndex, EdgeIndex) -> bool:  # imathsurface3d_getboundaryuvnurbs.html
        """
        ## GetBoundaryUVNurbs - Получить параметры границы поверхности в UV NURBS-представлении

        1. Если признак UV равен TRUE, то в массиве points возвращаются параметры UV для Nurbs-представления границы. Параметры в массиве лежат в следующей последовательности: u0, v0, u1, v1, ... ui, vi.

        2. Если признак UV равен FALSE, то в массиве points возвращаются координаты вершин Nurbs-представления границы. Координаты точек в полученном массиве лежат в следующей последовательности: x0, y0, z0, x1, y1, z1, ...xi, yi, zi.

        3. Если индекс ребра равен -1, формируется Nurbs-представление контура границы.

        4. Если индекс >= 0 то формируется Nurbs-представление ребра, входящего в цикл, параметр unclamped при этом игнорируется.

        ### Синтаксис Automation

        ```
        BOOL GetBoundaryUVNurbs( BOOL UV, BOOL Closed, long LoopIndex, long EdgeIndex,
        long * Degree, VARIANT * Points, VARIANT * Weights, VARIANT * Knots, double *
        TMin, double * TMax );
        ```

        ### Входные параметры

        +------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------+
        | UV        | - TRUE - получить параметры границы поверхности в UV- параметрическом представлении исходной поверхности, - FALSE - получить параметры границы поверхности в 3D координатах, |
        +------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------+
        | Closed    | - TRUE - сомкнуть, если граница разомкнутая, - FALSE - разомкнуть, если граница замкнутая,                                                                                   |
        +------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------+
        | LoopIndex | - индекс цикла,                                                                                                                                                              |
        +------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------+
        | EdgeIndex | - индекс ребра в цикле (совпадает с индексом ребра в коллекции ориентированных ребер).                                                                                       |
        +------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------+

        ### Выходные параметры

        +------------------------------------------------------------------------------------------------------------------+
        | Degree     | - порядок NURBS (степень полинома + 1), от 3 до 10,                                                 |
        +------------------------------------------------------------------------------------------------------------------+
        | Points     | - массив параметров UV или координат вершин - массив SafeArray вещественных чисел VT_ARRAY | VT_R8, |
        +------------------------------------------------------------------------------------------------------------------+
        | Weights    | - веса точек - массив SafeArray вещественных чисел VT_ARRAY | VT_R8,                                |
        +------------------------------------------------------------------------------------------------------------------+
        | Knots      | - узлы точек - массив SafeArray вещественных чисел VT_ARRAY | VT_R8,                                |
        +------------------------------------------------------------------------------------------------------------------+
        | TMin, TMax | - минимальный и максимальный параметры.                                                             |
        +------------------------------------------------------------------------------------------------------------------+

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def GetConeParam(self) -> bool:  # imathsurface3d_getconeparam.html
        """
        ## GetConeParam - Получить параметры конической поверхности

        ### Синтаксис Automation

        ```
        BOOL GetConeParam( double * Height, double * Angle, double * Radius );
        ```

        ### Выходные параметры

        +--------------------+
        | Height | - Высота, |
        +--------------------+
        | Angle  | - Угол,   |
        +--------------------+
        | Radius | - Радиус. |
        +--------------------+

        Версия КОМПАС v22

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def GetDerivativeU(self, ParamU, ParamV) -> bool:  # imathsurface3d_getderivativeu.html
        """
        ## GetDerivativeU - Получить первую производную по U

        ### Синтаксис Automation

        ```
        BOOL GetDerivativeU( double ParamU, double ParamV, double * X, double * Y, double * Z );
        ```

        ### Входные параметры

        +-------------------------------------------------------------------------------------------+
        | ParamU, ParamV | - значения параметров U и V в параметрическом представлении поверхности. |
        +-------------------------------------------------------------------------------------------+

        ### Выходные параметры

        +--------------------------------------+
        | X, Y, Z | - первая производная по U. |
        +--------------------------------------+

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def GetDerivativeUU(self, ParamU, ParamV) -> bool:  # imathsurface3d_getderivativeuu.html
        """
        ## GetDerivativeUU - Получить вторую производную по UU

        ### Синтаксис Automation

        ```
        BOOL GetDerivativeUU( double ParamU, double ParamV, double * X, double * Y, double * Z );
        ```

        ### Входные параметры

        +-------------------------------------------------------------------------------------------+
        | ParamU, ParamV | - значения параметров U и V в параметрическом представлении поверхности. |
        +-------------------------------------------------------------------------------------------+

        ### Выходные параметры

        +---------------------------------------+
        | X, Y, Z | - вторая производная по UU. |
        +---------------------------------------+

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def GetDerivativeUUU(self, ParamU, ParamV) -> bool:  # imathsurface3d_getderivativeuuu.html
        """
        ## GetDerivativeUUU - Получить третью производную по UUU

        ### Синтаксис Automation

        ```
        BOOL GetDerivativeUUU( double ParamU, double ParamV, double * X, double * Y, double * Z );
        ```

        ### Входные параметры

        +-------------------------------------------------------------------------------------------+
        | ParamU, ParamV | - значения параметров U и V в параметрическом представлении поверхности. |
        +-------------------------------------------------------------------------------------------+

        ### Выходные параметры

        +----------------------------------------+
        | X, Y, Z | - третья производная по UUU. |
        +----------------------------------------+

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def GetDerivativeUUV(self, ParamU, ParamV) -> bool:  # imathsurface3d_getderivativeuuv.html
        """
        ## GetDerivativeUUV - Получить третью производную по UUV

        ### Синтаксис Automation

        ```
        BOOL GetDerivativeUUV( double ParamU, double ParamV, double * X, double * Y, double * Z );
        ```

        ### Входные параметры

        +-------------------------------------------------------------------------------------------+
        | ParamU, ParamV | - значения параметров U и V в параметрическом представлении поверхности. |
        +-------------------------------------------------------------------------------------------+

        ### Выходные параметры

        +----------------------------------------+
        | X, Y, Z | - третья производная по UUV. |
        +----------------------------------------+

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def GetDerivativeUV(self, ParamU, ParamV) -> bool:  # imathsurface3d_getderivativeuv.html
        """
        ## GetDerivativeUV - Получить вторую производную по UV

        ### Синтаксис Automation

        ```
        BOOL GetDerivativeUV( double ParamU, double ParamV, double * X, double * Y, double * Z );
        ```

        ### Входные параметры

        +-------------------------------------------------------------------------------------------+
        | ParamU, ParamV | - значения параметров U и V в параметрическом представлении поверхности. |
        +-------------------------------------------------------------------------------------------+

        ### Выходные параметры

        +---------------------------------------+
        | X, Y, Z | - вторая производная по UV. |
        +---------------------------------------+

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def GetDerivativeUVV(self, ParamU, ParamV) -> bool:  # imathsurface3d_getderivativeuvv.html
        """
        ## GetDerivativeUVV - Получить третью производнуюпо UVV

        ### Синтаксис Automation

        ```
        BOOL GetDerivativeUVV( double ParamU, double ParamV, double * X, double * Y, double * Z );
        ```

        ### Входные параметры

        +-------------------------------------------------------------------------------------------+
        | ParamU, ParamV | - значения параметров U и V в параметрическом представлении поверхности. |
        +-------------------------------------------------------------------------------------------+

        ### Выходные параметры

        +----------------------------------------+
        | X, Y, Z | - третья производная по UVV. |
        +----------------------------------------+

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def GetDerivativeV(self, ParamU, ParamV) -> bool:  # imathsurface3d_getderivativev.html
        """
        ## GetDerivativeV - Получить вторую производную по V

        ### Синтаксис Automation

        ```
        BOOL GetDerivativeV( double ParamU, double ParamV, double * X, double * Y, double * Z );
        ```

        ### Входные параметры

        +-------------------------------------------------------------------------------------------+
        | ParamU, ParamV | - значения параметров U и V в параметрическом представлении поверхности. |
        +-------------------------------------------------------------------------------------------+

        ### Выходные параметры

        +--------------------------------------+
        | X, Y, Z | - первая производная по V. |
        +--------------------------------------+

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def GetDerivativeVV(self, ParamU, ParamV) -> bool:  # imathsurface3d_getderivativevv.html
        """
        ## GetDerivativeVV - Получить вторую производную по VV

        ### Синтаксис Automation

        ```
        BOOL GetDerivativeVV( double ParamU, double ParamV, double * X, double * Y, double * Z );
        ```

        ### Входные параметры

        +-------------------------------------------------------------------------------------------+
        | ParamU, ParamV | - значения параметров U и V в параметрическом представлении поверхности. |
        +-------------------------------------------------------------------------------------------+

        ### Выходные параметры

        +---------------------------------------+
        | X, Y, Z | - вторая производная по VV. |
        +---------------------------------------+

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def GetDerivativeVVV(self, ParamU, ParamV) -> bool:  # imathsurface3d_getderivativevvv.html
        """
        ## GetDerivativeVVV - Получить третью производнуюпо VVV

        ### Синтаксис Automation

        ```
        BOOL GetDerivativeVVV( double ParamU, double ParamV, double * X, double * Y, double * Z );
        ```

        ### Входные параметры

        +-------------------------------------------------------------------------------------------+
        | ParamU, ParamV | - значения параметров U и V в параметрическом представлении поверхности. |
        +-------------------------------------------------------------------------------------------+

        ### Выходные параметры

        +----------------------------------------+
        | X, Y, Z | - третья производная по VVV. |
        +----------------------------------------+

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def GetEdgesCount(self, LoopIndex):  # imathsurface3d_getedgescount.html
        """
        ## GetEdgesCount - Получить количество ребер в границе

        ### Синтаксис Automation

        ```
        long GetEdgesCount( long LoopIndex );
        ```

        ### Выходные параметры

        +-------------------------------+
        | LoopIndex | - индекс границы. |
        +-------------------------------+

        ### Возвращаемое значение

        - количество ребер в границе.
        """
        ...

    def GetGabarit(self) -> bool:  # imathsurface3d_getgabarit.html
        """
        ## GetGabarit - Получить габарит

        Координаты вершин параллелепипеда возвращаются в системе координат компонента.

        ### Синтаксис Automation

        ```
        BOOL GetGabarit( double * X1, double * Y1, double * Z1, double * X2, double * Y2, double * Z2 );
        ```

        ### Выходные параметры

        +---------------------------------------------------------------------------+
        | X1, Y1, Z1, X2, Y2, Z2 | - координаты вершин габаритного параллелепипеда. |
        +---------------------------------------------------------------------------+

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def GetNormal(self, ParamU, ParamV) -> bool:  # imathsurface3d_getnormal.html
        """
        ## GetNormal - Получить нормаль

        ### Синтаксис Automation

        ```
        BOOL GetNormal( double ParamU, double ParamV, double * X, double * Y, double * Z );
        ```

        ### Входные параметры

        +-------------------------------------------------------------------------------------------+
        | ParamU, ParamV | - значения параметров U и V в параметрическом представлении поверхности. |
        +-------------------------------------------------------------------------------------------+

        ### Выходные параметры

        +-----------------------------+
        | X, Y, Z | - вектор нормали. |
        +-----------------------------+

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def GetNurbsParams(self, ClosedV, ClosedU) -> bool:  # imathsurface3d_getnurbsparams.html
        """
        ## GetNurbsParams - Получить параметры Nurbs-поверхности

        ### Синтаксис Automation

        ```
        BOOL GetNurbsParams( BOOL ClosedV, BOOL ClosedU, long * DegreeV, long * DegreeU, long * NPV, long * NPU, VARIANT * Points, VARIANT * Weights, VARIANT * KnotsV, VARIANT * KnotsU );
        ```

        ### Входные параметры

        +----------------------------------------------------------------------------------------------------------------------------------------------+
        | ClosedV | - TRUE - получение параметров для замкнутого по V представления, FALSE - получение параметров для разомкнутого по V представления, |
        +----------------------------------------------------------------------------------------------------------------------------------------------+
        | ClosedU | TRUE - получение параметров для замкнутого по V представления, FALSE - получение параметров для разомкнутого по V представления.   |
        +----------------------------------------------------------------------------------------------------------------------------------------------+

        ### Выходные параметры

        +--------------------------------------------------------------------+
        | DegreeV | - порядок NURBS по V (степень полинома + 1), от 3 до 10, |
        +--------------------------------------------------------------------+
        | DegreeU | - порядок NURBS по U (степень полинома + 1), от 3 до 10  |
        +--------------------------------------------------------------------+
        | NPV     | - количество точек по V,                                 |
        +--------------------------------------------------------------------+
        | NPU     | - количество точек по U,                                 |
        +--------------------------------------------------------------------+
        | Points  | - массив SafeArray точек VT_ARRAY | VT_R8,               |
        +--------------------------------------------------------------------+
        | Weights | - веса,                                                  |
        +--------------------------------------------------------------------+
        | KnotsV  | - массив SafeArray узлов точек по V VT_ARRAY | VT_R8,    |
        +--------------------------------------------------------------------+
        | KnotsU  | - массив SafeArray узлов точек по U VT_ARRAY | VT_R8.    |
        +--------------------------------------------------------------------+

        Версия КОМПАС v22

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def GetPoint(self, ParamU, ParamV) -> bool:  # imathsurface3d_getpoint.html
        """
        ## GetPoint - Получить точку на поверхности

        ### Синтаксис Automation

        ```
        BOOL GetPoint( double ParamU, double ParamV, double * X, double * Y, double * Z );
        ```

        ### Входные параметры

        +-------------------------------------------------------------------------------------------+
        | ParamU, ParamV | - значения параметров U и V в параметрическом представлении поверхности. |
        +-------------------------------------------------------------------------------------------+

        ### Выходные параметры

        +----------------------------------------------+
        | X, Y, Z | - координаты точки на поверхности. |
        +----------------------------------------------+

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def GetTangentVectorU(self, ParamU, ParamV) -> bool:  # imathsurface3d_gettangentvectoru.html
        """
        ## GetTangentVectorU - Получить касательный вектор по U

        ### Синтаксис Automation

        ```
        BOOL GetTangentVectorU( double ParamU, double ParamV, double * X, double * Y, double * Z );
        ```

        ### Входные параметры

        +-------------------------------------------------------------------------------------------+
        | ParamU, ParamV | - значения параметров U и V в параметрическом представлении поверхности. |
        +-------------------------------------------------------------------------------------------+

        ### Выходные параметры

        +--------------------------------------+
        | X, Y, Z | - касательный вектор по U. |
        +--------------------------------------+

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def GetTangentVectorV(self, ParamU, ParamV) -> bool:  # imathsurface3d_gettangentvectorv.html
        """
        ## GetTangentVectorV - Получить касательный вектор по V

        ### Синтаксис Automation

        ```
        BOOL GetTangentVectorV( double ParamU, double ParamV, double * X, double * Y, double * Z );
        ```

        ### Входные параметры

        +-------------------------------------------------------------------------------------------+
        | ParamU, ParamV | - значения параметров U и V в параметрическом представлении поверхности. |
        +-------------------------------------------------------------------------------------------+

        ### Выходные параметры

        +--------------------------------------+
        | X, Y, Z | - касательный вектор по V. |
        +--------------------------------------+

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def GetTorusParam(self) -> bool:  # imathsurface3d_gettorusparam.html
        """
        ## GetTorusParam - Получить параметры тора

        ### Синтаксис Automation

        ```
        BOOL GetTorusParam( double * Radius, double * GeneratrixRadius );
        ```

        ### Выходные параметры

        +-----------------------------------------+
        | Radius           | - Радиус,            |
        +-----------------------------------------+
        | GeneratrixRadius | - Радиус образующей. |
        +-----------------------------------------+

        Версия КОМПАС v22

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def NearDirectPointProjection(self, X, Y, Z, VX, VY, VZ, Extended) -> bool:  # imathsurface3d_neardirectpointprojection.html
        """
        ## NearDirectPointProjection - Проекция точки на кривую

        ### Синтаксис Automation

        ```
        BOOL NearDirectPointProjection( double X, double Y, double Z, double VX, double VY, double VZ, BOOL Extended, double * U, double * V );
        ```

        ### Входные параметры

        +----------------------------------------------+
        | X, Y, Z    | - координаты точки,             |
        +----------------------------------------------+
        | VX, VY, VZ | - направление вектора,          |
        +----------------------------------------------+
        | Extended   | - TRUE - учитывать продолжение. |
        +----------------------------------------------+

        ### Выходные параметры

        +-----------------------------------------------------------+
        | U, V | - параметрические координаты точки на поверхности. |
        +-----------------------------------------------------------+

        Версия: Компас v20

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def NearPointProjection(self, X, Y, Z, Ext) -> bool:  # imathsurface3d_nearpointprojection.html
        """
        ## NearPointProjection - Получить ближайшую проекцию точки на поверхность

        ### Синтаксис Automation

        ```
        BOOL NearPointProjection( double X, double Y, double Z, double * ParamU, double * ParamV, BOOL Ext );
        ```

        ### Входные параметры

        +-------------------------------------------------------------------------------------------------------------------------------------------------------+
        | X, Y, Z | - координаты исходной пространственной точки,                                                                                               |
        +-------------------------------------------------------------------------------------------------------------------------------------------------------+
        | Ext     | - если проекция точки вне поверхности; - TRUE проецировать на продолжение поверхности, - FALSE найти ближайшую граничную точку поверхности. |
        +-------------------------------------------------------------------------------------------------------------------------------------------------------+

        ### Выходные параметры

        +---------------------------------------------------------------------+
        | ParamU, ParamV | - параметрические координаты точки на поверхности. |
        +---------------------------------------------------------------------+

        ### Возвращаемое значение

        +-----------------------------------------------------------------------------------+
        | TRUE  | - если проекция точки попала на саму поверхность или если ext равен TRUЕ, |
        +-----------------------------------------------------------------------------------+
        | FALSE | - если проекция точки не попала на саму поверхность.                      |
        +-----------------------------------------------------------------------------------+
        """
        ...

    def PointProjection(self, X, Y, Z, VX, VY, VZ, Extended) -> list[float]:  # imathsurface3d_pointprojection.html
        """
        ## PointProjection - Проекция точки на поверхность

        ### Синтаксис Automation

        ```
        VARIANT PointProjection( double X, double Y, double Z, double VX, double VY, double VZ, BOOL Extended );
        ```

        ### Входные параметры

        +----------------------------------------------+
        | X, Y, Z    | - координаты точки,             |
        +----------------------------------------------+
        | VX, VY, VZ | - направление вектора,          |
        +----------------------------------------------+
        | Extended   | - TRUE - учитывать продолжение. |
        +----------------------------------------------+

        Версия: Компас v20

        ### Возвращаемое значение

        Массив Safearray VT_ARRAY | VT_R8 параметрических координат точек поверхности в последовательности U1, V1, U2, V2....
        """
        ...




class ISurfaceContainer(IModelObject):  # isurfacecontainer.html
    """
    ## Интерфейс ISurfaceContainer

    Интерфейс контейнера поверхностей.

    Позволяет устанавливать и получать коллекции операций с поверхностями.

    Дополнительный интерфейс компонента. Данный интерфейс можно получить у компонента IPart7 посредством вызова метода IUnknown::QueryInterface (const GUID far& iid, void** pif).
    """

    @property
    def CloudPointsSurfaces(self) -> ICloudPointsSurfaces:  # isurfacecontainer_cloudpointssurfaces.html
        """
        ## CloudPointsSurfaces - Все поверхности по пласту (облаку) точек, входящие в состав данного объекта

        Тип данных: указатель на интерфейс коллекции поверхностей по пласту (облаку) точек ICloudPointsSurfaces

        1. Свойство позволяет получить интерфейс коллекции поверхностей по пласту (облаку) точек, входящих в состав объекта.

        2. Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        CloudPointsSurfaces = Object.CloudPointsSurfaces       # Получить свойство (* )
        CloudPointsSurfaces = Object.GetCloudPointsSurfaces()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def ConicSurfaces(self) -> IConicSurfaces:  # isurfacecontainer_conicsurfaces.html
        """
        ## ConicSurfaces - Коллекция поверхностей конического сечения

        Тип данных: указатель на интерфейс IConicSurfaces

        Свойство доступно только для чтения.

        Версия КОМПАС v22

        ### Синтаксис Automation

        ```
        ConicSurfaces = Object.ConicSurfaces       # Получить свойство (* )
        ConicSurfaces = Object.GetConicSurfaces()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def EquidistantSurfaces(self) -> IEquidistantSurfaces:  # isurfacecontainer_equidistantsurfaces.html
        """
        ## EquidistantSurfaces - Все эквидистантные поверхности, входящие в состав данного объекта

        Тип данных: указатель на интерфейс коллекции эквидистантных поверхностей IEquidistantSurfaces

        1. Свойство позволяет получить интерфейс коллекции эквидистантных поверхностей, входящих в состав объекта.

        2. Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        EquidistantSurfaces = Object.EquidistantSurfaces       # Получить свойство (* )
        EquidistantSurfaces = Object.GetEquidistantSurfaces()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def EvolutionSurfaces(self) -> IEvolutions:  # isurfacecontainer_evolutionsurfaces.html
        """
        ## EvolutionSurfaces - Коллекция кинематических поверхностей (поверхностей по траектории)

        Тип данных: Указатель на интерфейс IEvolutions.

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        EvolutionSurfaces = Object.EvolutionSurfaces       # Получить свойство (* )
        EvolutionSurfaces = Object.GetEvolutionSurfaces()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def ExtensionSurfaces(self) -> IExtensionSurfaces:  # isurfacecontainer_extensionsurfaces.html
        """
        ## ExtensionSurfaces - Все операции продления поверхности, входящие в состав данного объекта

        Тип данных: указатель на интерфейс коллекции операций продления поверхности IExtensionSurfaces

        1. Свойство позволяет получить интерфейс коллекции операций продления поверхности, входящих в состав объекта.

        2. Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        ExtensionSurfaces = Object.ExtensionSurfaces       # Получить свойство (* )
        ExtensionSurfaces = Object.GetExtensionSurfaces()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def ExtrusionSurfaces(self) -> IExtrusions:  # isurfacecontainer_extrusionsurfaces.html
        """
        ## ExtrusionSurfaces - Все поверхности выдавливания, входящие в состав данного объекта

        Тип данных: указатель на интерфейс коллекции поверхностей выдавливания IExtrusions

        1. Свойство позволяет получить интерфейс коллекции поверхностей выдавливания, входящих в состав объекта.

        2. Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        ExtrusionSurfaces = Object.ExtrusionSurfaces       # Получить свойство (* )
        ExtrusionSurfaces = Object.GetExtrusionSurfaces()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def FaceMovers(self) -> IFaceMovers:  # isurfacecontainer_facemovers.html
        """
        ## FaceMovers - Коллекция операций перемещения граней

        Тип данных: Указатель на интерфейс IFaceMovers.

        1. Свойство позволяет получить интерфейс коллекции операций перемещения граней, входящих в состав объекта.

        2. Свойство доступно только для чтения.

        Версия: Компас v20

        ### Синтаксис Automation

        ```
        FaceMovers = Object.FaceMovers       # Получить свойство (* )
        FaceMovers = Object.GetFaceMovers()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def FaceRemovers(self) -> IFaceRemovers:  # isurfacecontainer_faceremovers.html
        """
        ## FaceRemovers - Все операции удаления граней, входящие в состав данного объекта

        Тип данных: указатель на интерфейс коллекции операций удаления граней IFaceRemovers.

        1. Свойство позволяет получить интерфейс коллекции операций удаления граней, входящих в состав объекта.

        2. Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        FaceRemovers = iObject.FaceRemovers();     # Получить свойство (* )
        FaceRemovers = iObject.GetFaceRemovers();  # Получить свойство (**)
        ```
        """
        ...

    @property
    def ImportedSurfaces(self) -> IImportedSurfaces:  # isurfacecontainer_importedsurfaces.html
        """
        ## ImportedSurfaces - Все импортированные поверхности, входящие в состав данного объекта

        Тип данных: указатель на интерфейс коллекции импортированных поверхностей IImportedSurfaces

        1. Свойство позволяет получить интерфейс коллекции импортированных поверхностей, входящих в состав объекта.

        2. Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        ImportedSurfaces = Object.ImportedSurfaces       # Получить свойство (* )
        ImportedSurfaces = Object.GetImportedSurfaces()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def JointSurfaces(self) -> IJointSurfaces:  # isurfacecontainer_jointsurfaces.html
        """
        ## JointSurfaces - Коллекция поверхностей соединения

        Тип данных: Указатель на интерфейс IJointSurfaces

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        JointSurfaces = Object.JointSurfaces       # Получить свойство (* )
        JointSurfaces = Object.GetJointSurfaces()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def LoftSurfaces(self) -> ILofts:  # isurfacecontainer_loftsurfaces.html
        """
        ## LoftSurfaces - Коллекция поверхностей по сечениям

        Тип данных: Указатель на интерфейс ILofts.

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        LoftSurfaces = Object.LoftSurfaces       # Получить свойство (* )
        LoftSurfaces = Object.GetLoftSurfaces()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def MeshPointsSurfaces(self) -> IMeshPointsSurfaces:  # isurfacecontainer_meshpointssurfaces.html
        """
        ## MeshPointsSurfaces - Все поверхности по сети точек, входящие в состав данного объекта

        Тип данных: указатель на интерфейс коллекции поверхностей по сети точек IMeshPointsSurfaces

        1. Свойство позволяет получить интерфейс коллекции поверхностей по сети точек, входящих в состав объекта.

        2. Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        MeshPointsSurfaces = Object.MeshPointsSurfaces       # Получить свойство (* )
        MeshPointsSurfaces = Object.GetMeshPointsSurfaces()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def NurbsSurfaces(self) -> INurbsSurfaces:  # isurfacecontainer_nurbssurfaces.html
        """
        ## NurbsSurfaces - Все NURBS-поверхности, входящие в состав данного объекта

        Тип данных: указатель на интерфейс коллекции NURBS-поверхностей INurbsSurfaces.

        1. Свойство позволяет получить интерфейс коллекции NURBS-поверхностей, входящих в состав объекта.

        2. Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        NurbsSurfaces = iObject.NurbsSurfaces();     # Получить свойство (* )
        NurbsSurfaces = iObject.GetNurbsSurfaces();  # Получить свойство (**)
        ```
        """
        ...

    @property
    def NurbsSurfacesByCurvesMeshs(self) -> INurbsSurfacesByCurvesMeshs:  # isurfacecontainer_nurbssurfacesbycurvesmeshs.html
        """
        ## NurbsSurfacesByCurvesMeshs - Коллекция поверхностей по сети кривых

        Тип данных: Указатель на интерфейс INurbsSurfacesByCurvesMeshs

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        NurbsSurfacesByCurvesMeshs = Object.NurbsSurfacesByCurvesMeshs       # Получить свойство (* )
        NurbsSurfacesByCurvesMeshs = Object.GetNurbsSurfacesByCurvesMeshs()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def RestoredSurfaces(self) -> IRestoredSurfaces:  # isurfacecontainer_restoredsurfaces.html
        """
        ## RestoredSurfaces - Коллекция операций восстановленная поверхность

        Тип данных: Указатель на интерфейс IRestoredSurfaces

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        RestoredSurfaces = Object.RestoredSurfaces       # Получить свойство (* )
        RestoredSurfaces = Object.GetRestoredSurfaces()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def RotatedSurfaces(self) -> IRotateds:  # isurfacecontainer_rotatedsurfaces.html
        """
        ## RotatedSurfaces - Все поверхности вращения, входящие в состав данного объекта

        Тип данных: указатель на интерфейс коллекции поверхностей вращения IRotateds

        1. Свойство позволяет получить интерфейс коллекции поверхностей вращения, входящих в состав объекта.

        2. Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        RotatedSurfaces = Object.RotatedSurfaces       # Получить свойство (* )
        RotatedSurfaces = Object.GetRotatedSurfaces()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def RuledSurfaces(self) -> IRuledSurfaces:  # isurfacecontainer_ruledsurfaces.html
        """
        ## RuledSurfaces - Коллекция линейчатых поверхностей

        Тип данных: указатель на интерфейс коллекции операций создания линейчатой поверхности IRuledSurfaces

        1. Свойство позволяет получить коллекцию всех линейчатых поверхностей.

        2. Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        RuledSurfaces = Object.RuledSurfaces       # Получить свойство (* )
        RuledSurfaces = Object.GetRuledSurfaces()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def SurfacePatches(self) -> ISurfacePatches:  # isurfacecontainer_surfacepatches.html
        """
        ## SurfacePatches - Все заплатки, входящие в состав данного объекта

        Тип данных: указатель на интерфейс коллекции заплаток ISurfacePatches

        1. Свойство позволяет получить интерфейс коллекции заплаток, входящих в состав объекта.

        2. Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        SurfacePatches = iObject.SurfacePatches();     # Получить свойство (* )
        SurfacePatches = iObject.GetSurfacePatches();  # Получить свойство (**)
        ```
        """
        ...

    @property
    def SurfaceSewers(self) -> ISurfaceSewers:  # isurfacecontainer_surfacesewers.html
        """
        ## SurfaceSewers - Все операции сшивки поверхностей, входящие в состав данного объекта

        Тип данных: указатель на интерфейс коллекции операций сшивки поверхностей ISurfaceSewers

        1. Свойство позволяет получить интерфейс коллекции операций сшивки поверхностей, входящих в состав объекта.

        2. Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        SurfaceSewers = iObject.SurfaceSewers();     # Получить свойство (* )
        SurfaceSewers = iObject.GetSurfaceSewers();  # Получить свойство (**)
        ```
        """
        ...

    @property
    def TrimmedSurfaces(self) -> ITrimmedSurfaces:  # isurfacecontainer_trimmedsurfaces.html
        """
        ## TrimmedSurfaces - Все операции усечения поверхности, входящие в состав данного объекта

        Тип данных: указатель на интерфейс коллекции операций усечения поверхности ITrimmedSurfaces

        1. Свойство позволяет получить интерфейс коллекции операций усечения поверхности, входящих в состав объекта.

        2. Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        TrimmedSurfaces = Object.TrimmedSurfaces       # Получить свойство (* )
        TrimmedSurfaces = Object.GetTrimmedSurfaces()  # Получить свойство (**)
        ```
        """
        ...




class ICloudPointsSurfaces(IModelObjects):  # icloudpointssurfaces.html
    """
    ## Интерфейс ICloudPointsSurfaces

    Коллекция поверхностей по пласту (облаку) точек.
    """

    def Add(self) -> ICloudPointsSurface:  # icloudpointssurfaces_add.html
        """
        ## Add - Добавить новый элемент

        ### Синтаксис Automation

        ```
        LPDISPATCH Add();
        ```

        ### Возвращаемое значение

        Указатель на интерфейс поверхности по облаку точек ICloudPointsSurface
        """
        ...

    def CloudPointsSurface(self, Index) -> ICloudPointsSurface:  # icloudpointssurfaces_cloudpointssurface.html
        """
        ## CloudPointsSurface - Возвращает элемент, заданный по индексу

        Тип данных: указатель на интерфейс ICloudPointsSurface

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        CloudPointsSurface = Object.CloudPointsSurface( Index )     # Получить свойство (* )
        CloudPointsSurface = Object.GetCloudPointsSurface( Index )  # Получить свойство (**)
        ```
        """
        ...

    def Load(self, FileName) -> ICloudPointsSurface:  # icloudpointssurfaces_load.html
        """
        ## Load - Прочитать точки поверхности из файла

        ### Синтаксис Automation

        ```
        LPDISPATCH Load( BSTR FileName );
        ```

        ### Входные параметры

        FileName - имя файла

        Метод позволяет создать поверхность по координатам точек, загруженным из файла.

        ### Возвращаемое значение

        Указатель на интерфейс поверхности по облаку точек ICloudPointsSurface
        """
        ...




class IConicSurfaces(IModelObjects):  # iconicsurfaces.html
    """
    ## Интерфейс IConicSurfaces

    Интерфейс коллекции поверхностей конического сечения.

    Интерфейс можно получить, используя свойство ISurfaceContainer::ConicSurfaces.

    Версия КОМПАС v22
    """

    def Add(self) -> IConicSurface:  # iconicsurfaces_add.html
        """
        ## Add - Создает новый элемент и добавляет его в коллекцию

        ### Синтаксис Automation

        ```
        LPDISPATCH Add();
        ```

        ### Возвращаемое значение

        Указатель на интерфейс IConicSurface.
        """
        ...

    def ConicSurface(self, Index) -> IConicSurface:  # iconicsurfaces_conicsurface.html
        """
        ## ConicSurface - Возвращает элемент, заданный по индексу

        Тип данных: указатель на интерфейс IConicSurface

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        ConicSurface = Object.ConicSurface( Index )     # Получить свойство (* )
        ConicSurface = Object.GetConicSurface( Index )  # Получить свойство (**)
        ```
        """
        ...




class IEquidistantSurfaces(IModelObjects):  # iequidistantsurfaces.html
    """
    ## Интерфейс IEquidistantSurfaces

    Коллекция эквидистант поверхности

    Данный интерфейс можно получить у интерфейса ISurfaceContainer c помощью свойства ISurfaceContainer::EquidistantSurfaces .
    """

    def Add(self) -> IEquidistantSurface:  # iequidistantsurfaces_add.html
        """
        ## Add - Создать эквидистанту поверхности (добавить в коллекцию)

        ### Синтаксис Automation

        ```
        LPDISPATCH Add();
        ```

        ### Возвращаемое значение

        Указатель на интерфейс эквидистанты поверхности IEquidistantSurface.
        """
        ...

    def EquidistantSurface(self, Index) -> IEquidistantSurface:  # iequidistantsurfaces_equidistantsurface.html
        """
        ## EquidistantSurface - Возвращает эквидистанту поверхности, заданную по индексу

        Тип данных: указатель на интерфейс IEquidistantSurface

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        EquidistantSurface = Object.EquidistantSurface( Index )     # Получить свойство (* )
        EquidistantSurface = Object.GetEquidistantSurface( Index )  # Получить свойство (**)
        ```
        """
        ...




class IExtensionSurfaces(IModelObjects):  # iextensionsurfaces.html
    """
    ## Интерфейс IExtensionSurfaces

    Коллекция операций продления поверхности.
    """

    def Add(self) -> IExtensionSurface:  # iextensionsurfaces_add.html
        """
        ## Add - Создает новый элемент и добавляет его в коллекцию

        ### Синтаксис Automation

        ```
        LPDISPATCH Add();
        ```

        ### Возвращаемое значение

        Указатель на интерфейс операции продления поверхности IExtensionSurface.
        """
        ...

    def ExtensionSurface(self, Index) -> IExtensionSurface:  # iextensionsurfaces_extensionsurface.html
        """
        ## ExtensionSurface - Возвращает элемент, заданный по индексу

        Тип данных: указатель на интерфейс IExtensionSurface

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        ExtensionSurface = Object.ExtensionSurface( Index )     # Получить свойство (* )
        ExtensionSurface = Object.GetExtensionSurface( Index )  # Получить свойство (**)
        ```
        """
        ...




class IFaceRemovers(IModelObjects):  # ifaceremovers.html
    """
    ## Интерфейс IFaceRemovers

    Интерфейс коллекции операций удаления граней.

    Интерфейс позволяет получать и создавать операции удаления граней.

    Получить интерфейс коллекции можно, используя свойство контейнера поверхностей ISurfaceContainer::SurfacePatches.
    """

    def Add(self) -> IFaceRemover:  # ifaceremovers_add.html
        """
        ## Add - Создать новый элемент и добавить его в коллекцию

        ### Синтаксис Automation

        ```
        LPDISPATCH Add;
        ```

        ### Возвращаемое значение

        - Указатель на интерфейс операции удаления грани IFaceRemover.
        """
        ...

    def FaceRemover(self, Index) -> IFaceRemover:  # ifaceremovers_faceremover.html
        """
        ## FaceRemover - Операция удаления грани, заданная по индексу

        Тип данных: указатель на интерфейс IFaceRemover

        1. В качестве индекса может использоваться индекс в коллекции и reference объекта.

        2. Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        FaceRemover = iObject.FaceRemover( Index );     # Получить свойство (* )
        FaceRemover = iObject.GetFaceRemover( Index );  # Получить свойство (**)
        ```
        """
        ...




class IImportedSurfaces(IModelObjects):  # iimportedsurfaces.html
    """
    ## Интерфейс IImportedSurfaces

    Коллекция импортированных поверхностей.
    """

    def Add(self) -> IImportedSurface:  # iimportedsurfaces_add.html
        """
        ## Add - Добавить новый элемент

        ### Синтаксис Automation

        ```
        LPDISPATCH Add();
        ```

        ### Возвращаемое значение

        Указатель на интерфейс IImportedSurface
        """
        ...

    def ImportedSurface(self, Index) -> IImportedSurface:  # iimportedsurfaces_importedsurface.html
        """
        ## ImportedSurface - Возвращает элемент, заданный по индексу

        Тип данных: указатель на интерфейс IImportedSurface

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        ImportedSurface = Object.ImportedSurface( Index )     # Получить свойство (* )
        ImportedSurface = Object.GetImportedSurface( Index )  # Получить свойство (**)
        ```
        """
        ...

    def Load(self, FileName, SewSurfaces) -> list[IDispatch]:  # iimportedsurfaces_load.html
        """
        ## Load - Прочитать поверхность из файла

        ### Синтаксис Automation

        ```
        VARIANT Load( BSTR FileName,
        BOOL SewSurfaces );
        ```

        ### Входные параметры

        +--------------------------------------+
        | FileName    | - имя файла,           |
        +--------------------------------------+
        | SewSurfaces | - сшивать поверхности. |
        +--------------------------------------+

        Метод позволяет загрузить поверхности из файла .igs или .sat.

        ### Возвращаемое значение

        Массив VT_ARRAY | VT_DISPATCH поверхностей;
        """
        ...




class IMeshPointsSurfaces(IModelObjects):  # imeshpointssurfaces.html
    """
    ## Интерфейс IMeshPointsSurfaces

    Коллекция поверхностей по сети точек
    """

    def Add(self) -> IMeshPointsSurface:  # imeshpointssurfaces_add.html
        """
        ## Add - Добавить новый элемент

        ### Синтаксис Automation

        ```
        LPDISPATCH Add();
        ```

        ### Возвращаемое значение

        Указатель на интерфейс поверхности по сети точек IMeshPointsSurface
        """
        ...

    def Load(self, FileName) -> IMeshPointsSurface:  # imeshpointssurfaces_load.html
        """
        ## Load - Прочитать поверхность из файла

        ### Синтаксис Automation

        ```
        LPDISPATCH Load( BSTR FileName );
        ```

        ### Входные параметры

        FileName - имя файла.

        Метод позволяет создать поверхность по координатам точек, загруженным из файла.

        ### Возвращаемое значение

        Указатель на интерфейс поверхности по сети точек IMeshPointsSurface.
        """
        ...

    def MeshPointsSurface(self, Index) -> IMeshPointsSurface:  # imeshpointssurfaces_meshpointssurface.html
        """
        ## MeshPointsSurface - Возвращает элемент, заданный по индексу

        Тип данных: указатель на интерфейс IMeshPointsSurface

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        MeshPointsSurface = Object.MeshPointsSurface( Index )     # Получить свойство (* )
        MeshPointsSurface = Object.GetMeshPointsSurface( Index )  # Получить свойство (**)
        ```
        """
        ...




class INurbsSurfaces(IModelObjects):  # inurbssurfaces.html
    """
    ## Интерфейс INurbsSurfaces

    Интерфейс коллекции NURBS-поверхностей.

    Позволяет создавать и получать NURBS-поверхности.

    Данный интерфейс можно получить у контейнера поверхностей ISurfaceContainer::NurbsSurfaces.
    """

    def Add(self) -> INurbs|INurbsSurface:  # inurbssurfaces_add.html
        """
        ## Add - Создать новый элемент и добавить его в коллекцию

        ### Синтаксис Automation

        ```
        LPDISPATCH Add()
        ```

        ### Возвращаемое значение

        - указатель на интерфейс Nurbs-поверхности INurbsSurface
        """
        ...

    def NurbsSurface(self, Index) -> INurbs|INurbsSurface:  # inurbssurfaces_nurbssurface.html
        """
        ## NurbsSurface - Nurbs-поверхность, заданная по имени или индексу

        Тип данных: указатель на интерфейс Nurbs-поверхности INurbsSurface.

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        NurbsSurface = Object.NurbsSurface( Index )     # Получить свойство (* )
        NurbsSurface = Object.GetNurbsSurface( Index )  # Получить свойство (**)
        ```
        """
        ...




class IPlanes3D(IModelObjects):  # iplanes3d.html
    """
    ## Интерфейс IPlanes3D

    Коллекция плоскостей.
    """

    def Add(self, PlaneType) -> IPlane3D:  # iplanes3d_add.html
        """
        ## Add - Создает новый элемент и добавляет его в коллекцию

        ### Синтаксис Automation

        ```
        LPDISPATCH Add( ksObj3dTypeEnum PlaneType );
        ```

        ### Входные параметры

        +------------------------------+
        | PlaneType | - тип плоскости. |
        +------------------------------+

        ### Возвращаемое значение

        - указатель на интерфейс плоскости IPlane3D.
        """
        ...

    def Plane3D(self, Index) -> IPlane3D:  # iplanes3d_plane3d.html
        """
        ## Plane3D - Возвращает элемент, заданный по индексу или по имени

        Тип данных: указатель на интерфейс IPlane3D

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        Plane3D = Object.Plane3D( Index )     # Получить свойство (* )
        Plane3D = Object.GetPlane3D( Index )  # Получить свойство (**)
        ```
        """
        ...




class IRuledSurfaces(IModelObjects):  # iruledsurfaces.html
    """
    ## Интерфейс IRuledSurfaces

    Коллекция операций создания линейчатой поверхности.

    Данный интерфейс можно получить у интерфейса ISurfaceContainer с помощью свойства ISurfaceContainer::RuledSurfaces.
    """

    def Add(self) -> IRuledSurface:  # iruledsurfaces_add.html
        """
        ## Add - Создает новый элемент и добавляет его в коллекцию

        ### Синтаксис Automation

        ```
        LPDISPATCH Add();
        ```

        ### Возвращаемое значение

        Указатель на интерфейс создания линейчатой поверхности IRuledSurface.
        """
        ...

    def RuledSurface(self, Index) -> IRuledSurface:  # iruledsurfaces_ruledsurface.html
        """
        ## RuledSurface - Возвращает элемент, заданный по индексу

        Тип данных: указатель на интерфейс IRuledSurface

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        RuledSurface = Object.RuledSurface( Index )     # Получить свойство (* )
        RuledSurface = Object.GetRuledSurface( Index )  # Получить свойство (**)
        ```
        """
        ...




class ISplitLines(IModelObjects):  # isplitlines.html
    """
    ## Интерфейс ISplitLines

    Интерфейс коллекции элементов "линия разъема".

    Интерфейс позволяет получить элементы "линия разъема".

    Получить интерфейс коллекции можно, используя свойство контейнера вспомогательной геометрии IAuxiliaryGeomContainer::SplitLines.
    """

    def Add(self) -> ISplitLine:  # isplitlines_add.html
        """
        ## Add - Создать новый элемент и добавить его в коллекцию

        ### Синтаксис Automation

        ```
        LPDISPATCH Add;
        ```

        ### Возвращаемое значение

        - Указатель на интерфейс линии разъема ISplitLine.
        """
        ...

    def SplitLine(self, Index) -> ISplitLine:  # isplitlines_splitline.html
        """
        ## SplitLine - Линия разъема, заданная по индексу

        Тип данных: указатель на интерфейс ISplitLine

        1. В качестве индекса может использоваться индекс в коллекции и reference объекта.

        2. Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        SplitLine = iObject.SplitLine( Index );     # Получить свойство (* )
        SplitLine = iObject.GetSplitLine( Index );  # Получить свойство (**)
        ```
        """
        ...




class ISurfacePatches(IModelObjects):  # isurfacepatches.html
    """
    ## Интерфейс ISurfacePatches

    Интерфейс коллекции элементов "заплатка".

    Интерфейс позволяет получить элементы "заплатка".

    Получить интерфейс коллекции можно, используя свойство контейнера поверхностей ISurfaceContainer::SurfacePatches.
    """

    def Add(self) -> ISurfacePatch:  # isurfacepatches_add.html
        """
        ## Add - Создать новый элемент и добавить его в коллекцию

        ### Синтаксис Automation

        ```
        LPDISPATCH Add;
        ```

        ### Возвращаемое значение

        - Указатель на интерфейс заплатки ISurfacePatch.
        """
        ...

    def SurfacePatch(self, Index) -> ISurfacePatch:  # isurfacepatches_surfacepatch.html
        """
        ## SurfacePatch - Заплатка, заданная по индексу

        Тип данных: указатель на интерфейс ISurfacePatch

        1. В качестве индекса может использоваться индекс в коллекции и reference объекта.

        2. Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        SurfacePatch = iObject.SurfacePatch( Index );     # Получить свойство (* )
        SurfacePatch = iObject.GetSurfacePatch( Index );  # Получить свойство (**)
        ```
        """
        ...




class ISurfaceSewers(IModelObjects):  # isurfacesewers.html
    """
    ## Интерфейс ISurfaceSewers

    Интерфейс коллекции операций сшивки поверхностей.

    Позволяет получать и создавать объект операции сшивки поверхностей.

    Получить интерфейс коллекции можно, используя свойство контейнера поверхностей ISurfaceContainer::SurfaceSewers.
    """

    def Add(self) -> IBaseLeader3D:  # isurfacesewers_add.html
        """
        ## Add - Создать новый элемент и добавить его в коллекцию

        1. Метод позволяет создать новый интерфейс присоединительной точки.

        2. После получения нового интерфейса нужно задать параметры и вызвать метод IModelObject::Update.

        ### Синтаксис Automation

        ```
        LPDISPATCH Add();
        ```

        ### Входные параметры

        +--------------------------------+
        | DimType | - тип линии-выноски. |
        +--------------------------------+

        Типы линий-выносок:

        +----------------------------------------------------+
        | o3d_leader3D         | 86 | Линия-выноска 3D       |
        +----------------------------------------------------+
        | o3d_markLeader3D     | 87 | Знак маркировки 3D     |
        +----------------------------------------------------+
        | o3d_positionLeader3D | 89 | Обозначение позиции 3D |
        +----------------------------------------------------+
        | o3d_brandLeader3D    | 90 | Знак клеймения 3D      |
        +----------------------------------------------------+

        ### Возвращаемое значение

        - Указатель на интерфейс линии выноски IBaseLeader3D.

        - Указатель на интерфейс присоединительной точки IConjunctivePointI.
        """
        ...

    def SurfaceSewer(self, Index) -> ISurfaceSewer:  # isurfacesewers_surfacesewer.html
        """
        ## SurfaceSewer - Указатель на элемент, заданный по индексу

        Тип данных: указатель на интерфейс ISurfaceSewer

        1. Свойство доступно только для чтения.

        2. В качестве индекса может использоваться индекс в коллекции и reference объекта.

        ### Синтаксис Automation

        ```
        SurfaceSewer = iObject.SurfaceSewer( Index );     # Получить свойство (* )
        SurfaceSewer = iObject.GetSurfaceSewer( Index );  # Получить свойство (**)
        ```

        ### Входные параметры

        +---------------------------+
        | Index | - индекс объекта. |
        +---------------------------+
        """
        ...




class ITrimmedSurfaces(IModelObjects):  # itrimmedsurfaces.html
    """
    ## Интерфейс ITrimmedSurfaces

    Коллекция операций усечения поверхности
    """

    def Add(self) -> ITrimmedSurface:  # itrimmedsurfaces_add.html
        """
        ## Add - Создает новый элемент и добавляет его в коллекцию

        ### Синтаксис Automation

        ```
        LPDISPATCH Add();
        ```

        ### Возвращаемое значение

        Указатель на интерфейс операции усечения поверхности ITrimmedSurface
        """
        ...

    def TrimmedSurface(self, Index) -> ITrimmedSurface:  # itrimmedsurfaces_trimmedsurface.html
        """
        ## TrimmedSurface - Возвращает элемент, заданный по индексу

        Тип данных: указатель на интерфейс ITrimmedSurface

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        TrimmedSurface = Object.TrimmedSurface( Index )     # Получить свойство (* )
        TrimmedSurface = Object.GetTrimmedSurface( Index )  # Получить свойство (**)
        ```
        """
        ...




class IRestoredSurfaces(IModelObjects):  # irestoredsurfaces.html
    """
    ## Интерфейс IRestoredSurfaces

    Коллекция операций Восстановленная поверхность.

    Получить интерфейс коллекции операций восстановленная поверхность можно, используя свойство контейнера трехмерных объектов ISurfaceContainer::RestoredSurfaces.
    """

    def Add(self) -> IRestoredSurface:  # irestoredsurfaces_add.html
        """
        ## Add - Создает новый элемент и добавляет его в коллекцию

        1. Метод позволяет создать новый интерфейс операции восстановления поверхности.

        2. После получения нового интерфейса нужно задать параметры операции и вызвать метод IModelObject::Update.

        ### Синтаксис Automation

        ```
        LPDISPATCH Add();
        ```

        ### Возвращаемое значение

        - Указатель на интерфейс IRestoredSurface.
        """
        ...

    def RestoredSurface(self, Index) -> IRestoredSurface:  # irestoredsurfaces_restoredsurface.html
        """
        ## RestoredSurface - Возвращает элемент, заданный по индексу

        Тип данных: Указатель на интерфейс IRestoredSurface

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        RestoredSurface = Object.RestoredSurface( Index )     # Получить свойство (* )
        RestoredSurface = Object.GetRestoredSurface( Index )  # Получить свойство (**)
        ```

        ### Входные параметры

        +--------------------------------------------+
        | VARIANT Index | - индекс или имя элемента. |
        +--------------------------------------------+
        """
        ...




class ICloudPointsSurface(IModelObject):  # icloudpointssurface.html
    """
    ## Интерфейс ICloudPointsSurface

    Интерфейс параметров поверхности по пласту (облаку) точек
    """

    @property
    def BuildingType(self) -> int:  # icloudpointssurface_buildingtype.html
        """
        ## BuildingType - Тип поверхности по пласту (облаку) точек

        Тип данных: из перечисления ksCloudPointsSurfaceBuildingTypeEnum

        ### Синтаксис Automation

        ```
        BuildingType = Object.BuildingType       # Получить свойство (* )
        Object.BuildingType = BuildingType       # Установить свойство (* )
        BuildingType = Object.GetBuildingType()  # Получить свойство (**)
        Object.SetBuildingType( BuildingType )   # Установить свойство (**)
        ```
        """
        ...
    @BuildingType.setter
    def BuildingType(self, value: int): ...

    @property
    def CheckSelfIntersection(self) -> bool:  # icloudpointssurface_checkselfintersection.html
        """
        ## CheckSelfIntersection - Проверка самопересечений

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        CheckSelfIntersection = Object.CheckSelfIntersection       # Получить свойство (* )
        Object.CheckSelfIntersection = CheckSelfIntersection       # Установить свойство (* )
        CheckSelfIntersection = Object.GetCheckSelfIntersection()  # Получить свойство (**)
        Object.SetCheckSelfIntersection( CheckSelfIntersection )   # Установить свойство (**)
        ```
        """
        ...
    @CheckSelfIntersection.setter
    def CheckSelfIntersection(self, value: bool): ...

    @property
    def CloudLCS(self) -> ILocalCoordinateSystem:  # icloudpointssurface_cloudlcs.html
        """
        ## CloudLCS - СК объекта

        Тип данных: указатель на интерфейс ILocalCoordinateSystem

        ### Синтаксис Automation

        ```
        CloudLCS = Object.CloudLCS       # Получить свойство (* )
        Object.CloudLCS = CloudLCS       # Установить свойство (* )
        CloudLCS = Object.GetCloudLCS()  # Получить свойство (**)
        Object.SetCloudLCS( CloudLCS )   # Установить свойство (**)
        ```
        """
        ...
    @CloudLCS.setter
    def CloudLCS(self, value: ILocalCoordinateSystem): ...

    @property
    def CloudType(self) -> int:  # icloudpointssurface_cloudtype.html
        """
        ## CloudType - Способ распознавания сети точек

        Тип данных: из перечисления ksCloudTypeEnum

        ### Синтаксис Automation

        ```
        CloudType = Object.CloudType       # Получить свойство (* )
        Object.CloudType = CloudType       # Установить свойство (* )
        CloudType = Object.GetCloudType()  # Получить свойство (**)
        Object.SetCloudType( CloudType )   # Установить свойство (**)
        ```
        """
        ...
    @CloudType.setter
    def CloudType(self, value: int): ...

    @property
    def Degree(self) -> int:  # icloudpointssurface_degree.html
        """
        ## Degree - Степень сплайна

        Тип данных: long

        ### Синтаксис Automation

        ```
        Degree = Object.Degree       # Получить свойство (* )
        Object.Degree = Degree       # Установить свойство (* )
        Degree = Object.GetDegree()  # Получить свойство (**)
        Object.SetDegree( Degree )   # Установить свойство (**)
        ```
        """
        ...
    @Degree.setter
    def Degree(self, value: int): ...

    @property
    def FixedPosition(self) -> bool:  # icloudpointssurface_fixedposition.html
        """
        ## FixedPosition - Фиксировать положение

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        FixedPosition = Object.FixedPosition       # Получить свойство (* )
        Object.FixedPosition = FixedPosition       # Установить свойство (* )
        FixedPosition = Object.GetFixedPosition()  # Получить свойство (**)
        Object.SetFixedPosition( FixedPosition )   # Установить свойство (**)
        ```
        """
        ...
    @FixedPosition.setter
    def FixedPosition(self, value: bool): ...

    @property
    def Points(self) -> list[float]:  # icloudpointssurface_points.html
        """
        ## Points - Массив точек

        Тип данных: VARIANT типа VT_ARRAY | VT_R8

        ### Синтаксис Automation

        ```
        Points = Object.Points       # Получить свойство (* )
        Object.Points = Points       # Установить свойство (* )
        Points = Object.GetPoints()  # Получить свойство (**)
        Object.SetPoints( Points )   # Установить свойство (**)
        ```
        """
        ...
    @Points.setter
    def Points(self, value: list[float]): ...

    @property
    def PointsCount(self) -> int:  # icloudpointssurface_pointscount.html
        """
        ## PointsCount - Количество точек

        Тип данных: long

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        PointsCount = Object.PointsCount       # Получить свойство (* )
        PointsCount = Object.GetPointsCount()  # Получить свойство (**)
        ```
        """
        ...

    def AddPoint(self, X, Y, Z, PVal, AssociationObject):  # icloudpointssurface_addpoint.html
        """
        ## AddPoint - Добавить точку

        ### Синтаксис Automation

        ```
        LPDISPATCH AddPoint( double X,
        double Y,
        double Z,
        ksPoint3DTypeEnum PVal,
        IModelObject * AssociationObject );
        ```

        ### Выходные параметры

        +-------------------------------------------------+
        | X, Y, Z           | - координаты точки,         |
        +-------------------------------------------------+
        | PVal              | - тип параметров точки,     |
        +-------------------------------------------------+
        | AssociationObject | - опорный объект для точки. |
        +-------------------------------------------------+

        ### Возвращаемое значение

        Указатель на интерфейс параметров точки
        """
        ...

    def AddPoints(self, Points) -> bool:  # icloudpointssurface_addpoints.html
        """
        ## AddPoints - Добавить точки

        ### Синтаксис Automation

        ```
        BOOL AddPoints( VARIANT Points );
        ```

        ### Входные параметры

        Points: массив VT_ARRAY | VT_R8 координат точек

        ### Возвращаемое значение

        +-----------------------------------------+
        | TRUE  | - в случае удачного завершения, |
        +-----------------------------------------+
        | FALSE | - в случае неудачи.             |
        +-----------------------------------------+
        """
        ...

    def AssociationObject(self, Index) -> IModelObject:  # icloudpointssurface_associationobject.html
        """
        ## AssociationObject - Установить опорный объект для вершины

        Тип данных: указатель на интерфейс IModelObject

        ### Синтаксис Automation

        ```
        AssociationObject = Object.AssociationObject( Index )     # Получить свойство (* )
        Object.AssociationObject( Index ) = AssociationObject     # Установить свойство (* )
        AssociationObject = Object.GetAssociationObject( Index )  # Получить свойство (**)
        Object.SetAssociationObject( Index, AssociationObject )   # Установить свойство (**)
        ```

        ### Входные параметры

        long Index - индекс точки, для которой нужно установить опорный объект
        """
        ...

    def ClearPoints(self) -> bool:  # icloudpointssurface_clearpoints.html
        """
        ## ClearPoints - Очистить список рядов точек

        ### Синтаксис Automation

        ```
        BOOL ClearPoints();
        ```

        ### Возвращаемое значение

        +-----------------------------------------+
        | TRUE  | - в случае удачного завершения, |
        +-----------------------------------------+
        | FALSE | - в случае неудачи.             |
        +-----------------------------------------+

        Метод позволяет удалить все точки поверхности.
        """
        ...

    def DeletePoint(self, Index) -> bool:  # icloudpointssurface_deletepoint.html
        """
        ## DeletePoint - Удалить точку

        ### Синтаксис Automation

        ```
        BOOL DeletePoint( long Index );
        ```

        ### Входные параметры

        Index - индекс удаляемой точки.

        ### Возвращаемое значение

        +-----------------------------------------+
        | TRUE  | - в случае удачного завершения, |
        +-----------------------------------------+
        | FALSE | - в случае неудачи.             |
        +-----------------------------------------+
        """
        ...

    def GetPoint(self, Index) -> bool:  # icloudpointssurface_getpoint.html
        """
        ## GetPoint - Получить параметры точки

        ### Синтаксис Automation

        ```
        BOOL GetPoint( long Index,
        double * X,
        double * Y,
        double * Z );
        ```

        ### Входные параметры

        Index - индекс точки

        ### Выходные параметры

        X, Y, Z - координаты точки

        Метод позволяет получить координаты точки.

        ### Возвращаемое значение

        +-----------------------------------------+
        | TRUE  | - в случае удачного завершения, |
        +-----------------------------------------+
        | FALSE | - в случае неудачи.             |
        +-----------------------------------------+
        """
        ...

    def PointParameters(self, Index) -> IKompasAPIObject:  # icloudpointssurface_pointparameters.html
        """
        ## PointParameters - Интерфейс параметров точки поверхности

        Тип данных: указатель на интерфейс IKompasAPIObject

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        PointParameters = Object.PointParameters( Index )     # Получить свойство (* )
        PointParameters = Object.GetPointParameters( Index )  # Получить свойство (**)
        ```
        """
        ...

    def PointType(self, Index) -> int:  # icloudpointssurface_pointtype.html
        """
        ## PointType - Тип параметров построения точки поверхности

        Тип данных: из перечисления ksPoint3DTypeEnum

        ### Синтаксис Automation

        ```
        PointType = Object.PointType( Index )     # Получить свойство (* )
        Object.PointType( Index ) = PointType     # Установить свойство (* )
        PointType = Object.GetPointType( Index )  # Получить свойство (**)
        Object.SetPointType( Index, PointType )   # Установить свойство (**)
        ```

        ### Входные параметры

        long Index - индекс точки.
        """
        ...

    def SetAssociationObject(self, Index, arg1):
        ...

    def SetPoint(self, Index, X, Y, Z) -> bool:  # icloudpointssurface_setpoint.html
        """
        ## SetPoint - Установить параметры точки

        ### Синтаксис Automation

        ```
        BOOL SetPoint( long Index, double X, double Y, double Z );
        ```

        ### Входные параметры

        +-------------------------------+
        | Index   | - индекс точки,     |
        +-------------------------------+
        | X, Y, Z | - координаты точки. |
        +-------------------------------+

        Метод позволяет установить параметры точки.

        ### Возвращаемое значение

        +-----------------------------------------+
        | TRUE  | - в случае удачного завершения, |
        +-----------------------------------------+
        | FALSE | - в случае неудачи.             |
        +-----------------------------------------+
        """
        ...

    def SetPointType(self, Index, arg1):
        ...




class IConicSurface(IModelObject):  # iconicsurface.html
    """
    ## Интерфейс IConicSurface

    Интерфейс поверхности конического сечения.

    Интерфейс можно получить, используя свойство IConicSurfaces::ConicSurface и метод IConicSurfaces::Add.

    Версия КОМПАС v22
    """

    @property
    def ApexCurve(self):
        ...
    @ApexCurve.setter
    def ApexCurve(self, value): ...

    @property
    def BuildingType(self) -> int:  # iconicsurface_buildingtype.html
        """
        ## BuildingType - Способ построения треугольника

        Тип данных: из перечисления ksConicSurfaceBuildingTypeEnum

        ### Синтаксис Automation

        ```
        BuildingType = Object.BuildingType       # Получить свойство (* )
        Object.BuildingType = BuildingType       # Установить свойство (* )
        BuildingType = Object.GetBuildingType()  # Получить свойство (**)
        Object.SetBuildingType( BuildingType )   # Установить свойство (**)
        ```
        """
        ...
    @BuildingType.setter
    def BuildingType(self, value: int): ...

    @property
    def DirectionObjects1(self) -> typing.Any:  # iconicsurface_directionobjects1.html
        """
        ## DirectionObjects1 - Первый список направляющих

        Тип данных: VARIANT

        Направляющие передаются в виде SAFEARRAY DISPATCH - VT_ARRAY | VT_DISPATCH.

        ### Синтаксис Automation

        ```
        DirectionObjects1 = Object.DirectionObjects1       # Получить свойство (* )
        Object.DirectionObjects1 = DirectionObjects1       # Установить свойство (* )
        DirectionObjects1 = Object.GetDirectionObjects1()  # Получить свойство (**)
        Object.SetDirectionObjects1( DirectionObjects1 )   # Установить свойство (**)
        ```
        """
        ...
    @DirectionObjects1.setter
    def DirectionObjects1(self, value: typing.Any): ...

    @property
    def DirectionObjects2(self) -> typing.Any:  # iconicsurface_directionobjects2.html
        """
        ## DirectionObjects2 - Второй список направляющих

        Тип данных: VARIANT

        Направляющие передаются в виде SAFEARRAY DISPATCH - VT_ARRAY | VT_DISPATCH.

        ### Синтаксис Automation

        ```
        DirectionObjects2 = Object.DirectionObjects2       # Получить свойство (* )
        Object.DirectionObjects2 = DirectionObjects2       # Установить свойство (* )
        DirectionObjects2 = Object.GetDirectionObjects2()  # Получить свойство (**)
        Object.SetDirectionObjects2( DirectionObjects2 )   # Установить свойство (**)
        ```
        """
        ...
    @DirectionObjects2.setter
    def DirectionObjects2(self, value: typing.Any): ...

    @property
    def Inverse(self) -> bool:  # iconicsurface_inverse.html
        """
        ## Inverse - Сменить направление построения поверхности

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        Inverse = Object.Inverse       # Получить свойство (* )
        Object.Inverse = Inverse       # Установить свойство (* )
        Inverse = Object.GetInverse()  # Получить свойство (**)
        Object.SetInverse( Inverse )   # Установить свойство (**)
        ```
        """
        ...
    @Inverse.setter
    def Inverse(self, value: bool): ...

    @property
    def IsSelfIntersect(self) -> bool:  # iconicsurface_isselfintersect.html
        """
        ## IsSelfIntersect - Проверять самопересечение

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        IsSelfIntersect = Object.IsSelfIntersect       # Получить свойство (* )
        Object.IsSelfIntersect = IsSelfIntersect       # Установить свойство (* )
        IsSelfIntersect = Object.GetIsSelfIntersect()  # Получить свойство (**)
        Object.SetIsSelfIntersect( IsSelfIntersect )   # Установить свойство (**)
        ```
        """
        ...
    @IsSelfIntersect.setter
    def IsSelfIntersect(self, value: bool): ...

    @property
    def MovementAxisLine(self) -> typing.Any:  # iconicsurface_movementaxisline.html
        """
        ## MovementAxisLine - Осевая линия движения сечения

        Тип данных: VARIANT

        Набор кривых для осевой линии передаются в виде SAFEARRAY DISPATCH - VT_ARRAY | VT_DISPATCH.

        ### Синтаксис Automation

        ```
        MovementAxisLine = Object.MovementAxisLine       # Получить свойство (* )
        Object.MovementAxisLine = MovementAxisLine       # Установить свойство (* )
        MovementAxisLine = Object.GetMovementAxisLine()  # Получить свойство (**)
        Object.SetMovementAxisLine( MovementAxisLine )   # Установить свойство (**)
        ```
        """
        ...
    @MovementAxisLine.setter
    def MovementAxisLine(self, value: typing.Any): ...

    @property
    def MovementDirectionObject(self) -> IModelObject:  # iconicsurface_movementdirectionobject.html
        """
        ## MovementDirectionObject - Направляющий объект для движения сечения

        Тип данных: Указатель на интерфейс IModelObject

        ### Синтаксис Automation

        ```
        MovementDirectionObject = Object.MovementDirectionObject       # Получить свойство (* )
        Object.MovementDirectionObject = MovementDirectionObject       # Установить свойство (* )
        MovementDirectionObject = Object.GetMovementDirectionObject()  # Получить свойство (**)
        Object.SetMovementDirectionObject( MovementDirectionObject )   # Установить свойство (**)
        ```
        """
        ...
    @MovementDirectionObject.setter
    def MovementDirectionObject(self, value: IModelObject): ...

    @property
    def MovementType(self) -> int:  # iconicsurface_movementtype.html
        """
        ## MovementType - Движение сечения

        Тип данных: из перечисления ksConicSurfaceMovementTypeEnum

        ### Синтаксис Automation

        ```
        MovementType = Object.MovementType       # Получить свойство (* )
        Object.MovementType = MovementType       # Установить свойство (* )
        MovementType = Object.GetMovementType()  # Получить свойство (**)
        Object.SetMovementType( MovementType )   # Установить свойство (**)
        ```
        """
        ...
    @MovementType.setter
    def MovementType(self, value: int): ...

    @property
    def MovementVector(self) -> IVector3D:  # iconicsurface_movementvector.html
        """
        ## MovementVector - Направляющий вектор для движения сечения

        Тип данных:Указатель на интерфейс IVector3D

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        MovementVector = Object.MovementVector       # Получить свойство (* )
        MovementVector = Object.GetMovementVector()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def SectionCurveFormType(self) -> int:  # iconicsurface_sectioncurveformtype.html
        """
        ## SectionCurveFormType - Способ задания формы кривой конического сечения

        Тип данных: из перечисления ksConicSurfaceSectionCurveFormEnum

        ### Синтаксис Automation

        ```
        SectionCurveFormType = Object.SectionCurveFormType       # Получить свойство (* )
        Object.SectionCurveFormType = SectionCurveFormType       # Установить свойство (* )
        SectionCurveFormType = Object.GetSectionCurveFormType()  # Получить свойство (**)
        Object.SetSectionCurveFormType( SectionCurveFormType )   # Установить свойство (**)
        ```
        """
        ...
    @SectionCurveFormType.setter
    def SectionCurveFormType(self, value: int): ...

    @property
    def SectionCurveLawObject(self) -> IModelObject:  # iconicsurface_sectioncurvelawobject.html
        """
        ## SectionCurveLawObject - Закон. Эскиз с кривой определяющей функцию y=f(x) кривой сечения

        Тип данных: Указатель на интерфейс IModelObject

        ### Синтаксис Automation

        ```
        SectionCurveLawObject = Object.SectionCurveLawObject       # Получить свойство (* )
        Object.SectionCurveLawObject = SectionCurveLawObject       # Установить свойство (* )
        SectionCurveLawObject = Object.GetSectionCurveLawObject()  # Получить свойство (**)
        Object.SetSectionCurveLawObject( SectionCurveLawObject )   # Установить свойство (**)
        ```
        """
        ...
    @SectionCurveLawObject.setter
    def SectionCurveLawObject(self, value: IModelObject): ...

    @property
    def SectionCurveLawType(self) -> int:  # iconicsurface_sectioncurvelawtype.html
        """
        ## SectionCurveLawType - Тип функции для построения кривой конического сечения

        Тип данных: из перечисления ksLawTypeEnum

        ### Синтаксис Automation

        ```
        SectionCurveLawType = Object.SectionCurveLawType       # Получить свойство (* )
        Object.SectionCurveLawType = SectionCurveLawType       # Установить свойство (* )
        SectionCurveLawType = Object.GetSectionCurveLawType()  # Получить свойство (**)
        Object.SetSectionCurveLawType( SectionCurveLawType )   # Установить свойство (**)
        ```
        """
        ...
    @SectionCurveLawType.setter
    def SectionCurveLawType(self, value: int): ...

    @property
    def SectionCurveOnSurface(self) -> typing.Any:  # iconicsurface_sectioncurveonsurface.html
        """
        ## SectionCurveOnSurface - Кривая на поверхности для задания формы кривой сечения

        Тип данных: VARIANT

        Кривые передаются в виде SAFEARRAY DISPATCH - VT_ARRAY | VT_DISPATCH.

        ### Синтаксис Automation

        ```
        SectionCurveOnSurface = Object.SectionCurveOnSurface       # Получить свойство (* )
        Object.SectionCurveOnSurface = SectionCurveOnSurface       # Установить свойство (* )
        SectionCurveOnSurface = Object.GetSectionCurveOnSurface()  # Получить свойство (**)
        Object.SetSectionCurveOnSurface( SectionCurveOnSurface )   # Установить свойство (**)
        ```
        """
        ...
    @SectionCurveOnSurface.setter
    def SectionCurveOnSurface(self, value: typing.Any): ...

    @property
    def SectionCurveTangentSurface(self) -> IModelObject:  # iconicsurface_sectioncurvetangentsurface.html
        """
        ## SectionCurveTangentSurface - Кривая на поверхности для задания формы кривой сечения

        Тип данных: Указатель на интерфейс IModelObject

        ### Синтаксис Automation

        ```
        SectionCurveTangentSurface = Object.SectionCurveTangentSurface       # Получить свойство (* )
        Object.SectionCurveTangentSurface = SectionCurveTangentSurface       # Установить свойство (* )
        SectionCurveTangentSurface = Object.GetSectionCurveTangentSurface()  # Получить свойство (**)
        Object.SetSectionCurveTangentSurface( SectionCurveTangentSurface )   # Установить свойство (**)
        ```
        """
        ...
    @SectionCurveTangentSurface.setter
    def SectionCurveTangentSurface(self, value: IModelObject): ...

    def GetTrianglePoint(self, BeginSide, PointIndex):  # iconicsurface_gettrianglepoint.html
        """
        ## GetTrianglePoint - Получить точки треугольников, в которые вписаны начало и конец поверхности конического сечения

        ### Синтаксис Automation

        ```
        BOOL GetTrianglePoint( BOOL BeginSide, ksConicSectionTrianglePointEnum PointIndex, double * X, double * Y, double * Z );
        ```

        ### Входные параметры

        +------------------------------------------------------------------------------+
        | BeginSide  | - TRUE - начальная сторона, - FALSE - конечная сторона,         |
        +------------------------------------------------------------------------------+
        | PointIndex | - индекс точки из перечисления ksConicSectionTrianglePointEnum. |
        +------------------------------------------------------------------------------+

        ### Выходные параметры

        +-------------------------------+
        | X, Y, Z | - координаты точки, |
        +-------------------------------+
        """
        ...

    def SectionCurveCoefficient(self, ValueType) -> float:  # iconicsurface_sectioncurvecoefficient.html
        """
        ## SectionCurveCoefficient - Значение угла для построения угла треугольника

        Тип данных: double

        ### Синтаксис Automation

        ```
        SectionCurveCoefficient = Object.SectionCurveCoefficient       # Получить свойство (* )
        Object.SectionCurveCoefficient = SectionCurveCoefficient       # Установить свойство (* )
        SectionCurveCoefficient = Object.GetSectionCurveCoefficient()  # Получить свойство (**)
        Object.SetSectionCurveCoefficient( SectionCurveCoefficient )   # Установить свойство (**)
        ```

        ### Входные параметры

        +-------------------------------------------------------------------------+
        | ValueType | - Тип движения сечения из перечисления ksSideAngleTypeEnum. |
        +-------------------------------------------------------------------------+
        """
        ...

    def SetSectionCurveCoefficient(self, ValueType, arg1):
        ...

    def SetSideAngle(self, FirstSide, ValueType, arg2):
        ...

    def SetSideAngleBuildingType(self, FirstSide, arg1):
        ...

    def SetSideAngleDirection(self, FirstSide, arg1):
        ...

    def SetSideAngleLawObject(self, FirstSide, arg1):
        ...

    def SetSideAngleLawType(self, FirstSide, arg1):
        ...

    def SetSideBuildingType(self, FirstSide, arg1):
        ...

    def SetSideFaces(self, FirstSide, arg1):
        ...

    def SideAngle(self, FirstSide, ValueType) -> float:  # iconicsurface_sideangle.html
        """
        ## SideAngle - Значение угла для построения угла треугольника

        Тип данных: double

        ### Синтаксис Automation

        ```
        SideAngle = Object.SideAngle( FirstSide, ValueType )     # Получить свойство (* )
        Object.SideAngle( FirstSide, ValueType ) = SideAngle     # Установить свойство (* )
        SideAngle = Object.GetSideAngle( FirstSide, ValueType )  # Получить свойство (**)
        Object.SetSideAngle( FirstSide, ValueType, SideAngle )   # Установить свойство (**)
        ```

        ### Входные параметры

        +------------------------------------------------------------------------------+
        | BOOL FirstSide | - TRUE - первая сторона, FALSE - вторая сторона,            |
        +------------------------------------------------------------------------------+
        | ValueType      | - тип движения сечения из перечисления ksSideAngleTypeEnum. |
        +------------------------------------------------------------------------------+
        """
        ...

    def SideAngleBuildingType(self, FirstSide) -> int:  # iconicsurface_sideanglebuildingtype.html
        """
        ## SideAngleBuildingType - Условие построения

        Тип данных: из перечисления ksConicSurfaceSideAngleBuildingTypeEnum

        ### Синтаксис Automation

        ```
        SideAngleBuildingType = Object.SideAngleBuildingType( FirstSide )     # Получить свойство (* )
        Object.SideAngleBuildingType( FirstSide ) = SideAngleBuildingType     # Установить свойство (* )
        SideAngleBuildingType = Object.GetSideAngleBuildingType( FirstSide )  # Получить свойство (**)
        Object.SetSideAngleBuildingType( FirstSide, SideAngleBuildingType )   # Установить свойство (**)
        ```

        ### Входные параметры

        +-------------------------------------------------------------------+
        | BOOL FirstSide | - TRUE - первая сторона, FALSE - вторая сторона. |
        +-------------------------------------------------------------------+
        """
        ...

    def SideAngleDirection(self, FirstSide) -> bool:  # iconicsurface_sideangledirection.html
        """
        ## SideAngleDirection - Направление угла для построения угла треугольника

        Тип данных:BOOL

        ### Синтаксис Automation

        ```
        SideAngleDirection = Object.SideAngleDirection( FirstSide )     # Получить свойство (* )
        Object.SideAngleDirection( FirstSide ) = SideAngleDirection     # Установить свойство (* )
        SideAngleDirection = Object.GetSideAngleDirection( FirstSide )  # Получить свойство (**)
        Object.SetSideAngleDirection( FirstSide, SideAngleDirection )   # Установить свойство (**)
        ```

        ### Входные параметры

        +-------------------------------------------------------------------+
        | BOOL FirstSide | - TRUE - первая сторона, FALSE - вторая сторона. |
        +-------------------------------------------------------------------+
        """
        ...

    def SideAngleLawObject(self, FirstSide) -> IModelObject:  # iconicsurface_sideanglelawobject.html
        """
        ## SideAngleLawObject - Закон. Эскиз с кривой определяющей функцию y=f(x) угла наклона боковой стороны треугольника сечения

        Тип данных: Указатель на интерфейс IModelObject

        ### Синтаксис Automation

        ```
        SideAngleLawObject = Object.SideAngleLawObject( FirstSide )     # Получить свойство (* )
        Object.SideAngleLawObject( FirstSide ) = SideAngleLawObject     # Установить свойство (* )
        SideAngleLawObject = Object.GetSideAngleLawObject( FirstSide )  # Получить свойство (**)
        Object.SetSideAngleLawObject( FirstSide, SideAngleLawObject )   # Установить свойство (**)
        ```

        ### Входные параметры

        +-------------------------------------------------------------------+
        | BOOL FirstSide | - TRUE - первая сторона, FALSE - вторая сторона. |
        +-------------------------------------------------------------------+
        """
        ...

    def SideAngleLawType(self, FirstSide) -> int:  # iconicsurface_sideanglelawtype.html
        """
        ## SideAngleLawType - Тип функции для построения угла треугольника

        Тип данных: из перечисления ksLawTypeEnum

        ### Синтаксис Automation

        ```
        SideAngleLawType = Object.SideAngleLawType( FirstSide )     # Получить свойство (* )
        Object.SideAngleLawType( FirstSide ) = SideAngleLawType     # Установить свойство (* )
        SideAngleLawType = Object.GetSideAngleLawType( FirstSide )  # Получить свойство (**)
        Object.SetSideAngleLawType( FirstSide, SideAngleLawType )   # Установить свойство (**)
        ```

        ### Входные параметры

        +-------------------------------------------------------------------+
        | BOOL FirstSide | - TRUE - первая сторона, FALSE - вторая сторона. |
        +-------------------------------------------------------------------+
        """
        ...

    def SideBuildingType(self, FirstSide) -> int:  # iconicsurface_sidebuildingtype.html
        """
        ## SideBuildingType - Способ построения стороны в треугольнике

        Тип данных: из перечисления ksConicSurfaceSideBuildingTypeEnum

        ### Синтаксис Automation

        ```
        SideBuildingType = Object.SideBuildingType( FirstSide )     # Получить свойство (* )
        Object.SideBuildingType( FirstSide ) = SideBuildingType     # Установить свойство (* )
        SideBuildingType = Object.GetSideBuildingType( FirstSide )  # Получить свойство (**)
        Object.SetSideBuildingType( FirstSide, SideBuildingType )   # Установить свойство (**)
        ```

        ### Входные параметры

        +-------------------------------------------------------------------+
        | BOOL FirstSide | - TRUE - первая сторона, FALSE - вторая сторона. |
        +-------------------------------------------------------------------+
        """
        ...

    def SideFaces(self, FirstSide) -> typing.Any:  # iconicsurface_sidefaces.html
        """
        ## SideFaces - Список направляющих поверхностей стороны треугольника

        Тип данных: VARIANT

        Поверхности передаются в виде SAFEARRAY DISPATCH - VT_ARRAY | VT_DISPATCH.

        ### Синтаксис Automation

        ```
        SideFaces = Object.SideFaces( FirstSide )     # Получить свойство (* )
        Object.SideFaces( FirstSide ) = SideFaces     # Установить свойство (* )
        SideFaces = Object.GetSideFaces( FirstSide )  # Получить свойство (**)
        Object.SetSideFaces( FirstSide, SideFaces )   # Установить свойство (**)
        ```

        ### Входные параметры

        +-------------------------------------------------------------------+
        | BOOL FirstSide | - TRUE - первая сторона, FALSE - вторая сторона. |
        +-------------------------------------------------------------------+
        """
        ...




class IEquidistantSurface(IModelObject):  # iequidistantsurface.html
    """
    ## Интерфейс IEquidistantSurface

    Интерфейс операции построения эквидистанты поверхности
    """

    @property
    def BaseSurface(self) -> typing.Any:  # iequidistantsurface_basesurface.html
        """
        ## BaseSurface - Базовая поверхность для эквидистантной поверхности (грань или совокупность граней)

        Тип данных: VARIANT

        В качестве базовой поверхности могут использоваться следующие объекты:

        •грань тела или поверхности;

        •связная совокупность граней одного тела или поверхности.

        Связная совокупность граней — множество граней, каждая из которых имеет общее ребро хотя бы еще с одной гранью этого множества, причем одно ребро одновременно принадлежит не более чем двум граням.

        ### Синтаксис Automation

        ```
        BaseSurface = Object.BaseSurface       # Получить свойство (* )
        Object.BaseSurface = BaseSurface       # Установить свойство (* )
        BaseSurface = Object.GetBaseSurface()  # Получить свойство (**)
        Object.SetBaseSurface( BaseSurface )   # Установить свойство (**)
        ```
        """
        ...
    @BaseSurface.setter
    def BaseSurface(self, value: typing.Any): ...

    @property
    def Direction(self) -> bool:  # iequidistantsurface_direction.html
        """
        ## Direction - Направление построения эквидистантной поверхности относительно базовой

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        Direction = Object.Direction       # Получить свойство (* )
        Object.Direction = Direction       # Установить свойство (* )
        Direction = Object.GetDirection()  # Получить свойство (**)
        Object.SetDirection( Direction )   # Установить свойство (**)
        ```
        """
        ...
    @Direction.setter
    def Direction(self, value: bool): ...

    @property
    def Distance(self) -> float:  # iequidistantsurface_distance.html
        """
        ## Distance - Расстояние от базовой поверхности до эквидистантной поверхности

        Тип данных: double

        ### Синтаксис Automation

        ```
        Distance = Object.Distance       # Получить свойство (* )
        Object.Distance = Distance       # Установить свойство (* )
        Distance = Object.GetDistance()  # Получить свойство (**)
        Object.SetDistance( Distance )   # Установить свойство (**)
        ```
        """
        ...
    @Distance.setter
    def Distance(self, value: float): ...




class IExtensionSurface(IModelObject):  # iextensionsurface.html
    """
    ## Интерфейс IExtensionSurface

    Интерфейс операции продления поверхности
    """

    @property
    def BuildingVectorParameters(self) -> IVector3D:  # iextensionsurface_buildingvectorparameters.html
        """
        ## BuildingVectorParameters - Параметры вектора

        Тип данных: Указатель на интерфейс IVector3D

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        BuildingVectorParameters = Object.BuildingVectorParameters       # Получить свойство (* )
        BuildingVectorParameters = Object.GetBuildingVectorParameters()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def DirObject(self) -> IModelObject:  # iextensionsurface_dirobject.html
        """
        ## DirObject - Объект, определяющий направление для типа построения По направлению

        Тип данных: указатель на интерфейс IModelObject

        ### Синтаксис Automation

        ```
        DirObject= Object.DirObject        # Получить свойство (* )
        Object.DirObject= DirObject        # Установить свойство (* )
        DirObject = Object.GetDirObject()  # Получить свойство (**)
        Object.SetDirObject( DirObject )   # Установить свойство (**)
        ```
        """
        ...
    @DirObject.setter
    def DirObject(self, value: IModelObject): ...

    @property
    def Edges(self) -> list[IDispatch]:  # iextensionsurface_edges.html
        """
        ## Edges - Массив ребер в виде SAFEARRAY DISPATCH - VT_ARRAY | VT_DISPATCH

        Тип данных: VARIANT типа VT_ARRAY | VT_DISPATCH

        ### Синтаксис Automation

        ```
        Edges = Object.Edges       # Получить свойство (* )
        Object.Edges = Edges       # Установить свойство (* )
        Edges = Object.GetEdges()  # Получить свойство (**)
        Object.SetEdges( Edges )   # Установить свойство (**)
        ```
        """
        ...
    @Edges.setter
    def Edges(self, value: list[IDispatch]): ...

    @property
    def ExtensionLimitType(self) -> int:  # iextensionsurface_extensionlimittype.html
        """
        ## ExtensionLimitType - Способ ограничения

        Тип данных: из перечисления ksExtensionLimitTypeEnum

        ### Синтаксис Automation

        ```
        ExtensionLimitType = Object.ExtensionLimitType       # Получить свойство (* )
        Object.ExtensionLimitType = ExtensionLimitType       # Установить свойство (* )
        ExtensionLimitType = Object.GetExtensionLimitType()  # Получить свойство (**)
        Object.SetExtensionLimitType( ExtensionLimitType )   # Установить свойство (**)
        ```
        """
        ...
    @ExtensionLimitType.setter
    def ExtensionLimitType(self, value: int): ...

    @property
    def ExtensionType(self):  # iextensionsurface_extensiontype.html
        """
        ## ExtensionType - Тип продления поверхности

        Тип данных: из перечисления ksExtensionTypeEnum

        ### Синтаксис Automation

        ```
        ExtensionType = Object.ExtensionType       # Получить свойство (* )
        Object.ExtensionType = ExtensionType       # Установить свойство (* )
        ExtensionType = Object.GetExtensionType()  # Получить свойство (**)
        Object.SetExtensionType( ExtensionType )   # Установить свойство (**)
        ```
        """
        ...
    @ExtensionType.setter
    def ExtensionType(self, value): ...

    @property
    def Length(self) -> float:  # iextensionsurface_length.html
        """
        ## Length - Длина (расстояние удлинения поверхности)

        Тип данных: double

        ### Синтаксис Automation

        ```
        Length = Object.Length       # Получить свойство (* )
        Object.Length = Length       # Установить свойство (* )
        Length = Object.GetLength()  # Получить свойство (**)
        Object.SetLength( Length )   # Установить свойство (**)
        ```
        """
        ...
    @Length.setter
    def Length(self, value: float): ...

    @property
    def Sense(self) -> bool:  # iextensionsurface_sense.html
        """
        ## Sense - Сменить направление

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        Sense = Object.Sense       # Получить свойство (* )
        Object.Sense = Sense       # Установить свойство (* )
        Sense = Object.GetSense()  # Получить свойство (**)
        Object.SetSense( Sense )   # Установить свойство (**)
        ```
        """
        ...
    @Sense.setter
    def Sense(self, value: bool): ...

    @property
    def SideEdges(self) -> bool:  # iextensionsurface_sideedges.html
        """
        ## SideEdges - Боковые ребра

        Тип данных: BOOL

        Значения свойства:

        +-------------------------------------------------+
        | TRUE  | - как продление исходных боковых ребер, |
        +-------------------------------------------------+
        | FALSE | - по нормали к указанным кромкам.       |
        +-------------------------------------------------+

        ### Синтаксис Automation

        ```
        SideEdges = Object.SideEdges       # Получить свойство (* )
        Object.SideEdges = SideEdges       # Установить свойство (* )
        SideEdges = Object.GetSideEdges()  # Получить свойство (**)
        Object.SetSideEdges( SideEdges )   # Установить свойство (**)
        ```
        """
        ...
    @SideEdges.setter
    def SideEdges(self, value: bool): ...

    @property
    def TargetObject(self) -> IModelObject:  # iextensionsurface_targetobject.html
        """
        ## TargetObject - Объект, до которого производится удлинение при способе построения До вершины

        Тип данных: указатель на интерфейс IModelObject

        ### Синтаксис Automation

        ```
        TargetObject = Object.TargetObject       # Получить свойство (* )
        Object.TargetObject = TargetObject       # Установить свойство (**)
        TargetObject = Object.GetTargetObject()  # Получить свойство (**)
        Object.SetTargetObject( TargetObject )   # Установить свойство (**)
        ```
        """
        ...
    @TargetObject.setter
    def TargetObject(self, value: IModelObject): ...




class IFaceRemover(IModelObject):  # ifaceremover.html
    """
    ## Интерфейс IFaceRemover

    Интерфейс операции удаления граней.

    1. Интерфейс можно получить у коллекции операций удаления граней, используя свойство IFaceRemovers::FaceRemover или метод IFaceRemovers::Add.

    2. После задания параметров заплатки требуется вызвать метод IModelObject::Update.
    """

    @property
    def Faces(self) -> typing.Any:  # ifaceremover_faces.html
        """
        ## Faces - Массив граней в виде SAFEARRAY DISPATCH - VT_ARRAY | VT_DISPATCH

        Тип данных: VARIANT

        Свойство позволяет устанавливать и получать грани операции. Если возвращается один объект, то тип VARIANT-а будет VT_DISPATCH, если возвращается несколько объектов, то тип VARIANT-а будет VT_ARRAY | VT_DISPATCH.

        ### Синтаксис Automation

        ```
        Faces = Object.Faces       # Получить свойство (* )
        Object.Faces = Faces       # Установить свойство (* )
        Faces = Object.GetFaces()  # Получить свойство (**)
        Object.SetFaces( Faces )   # Установить свойство (**)
        ```
        """
        ...
    @Faces.setter
    def Faces(self, value: typing.Any): ...

    @property
    def SaveBody(self) -> bool:  # ifaceremover_savebody.html
        """
        ## SaveBody - Сохранять целостность тела

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        SaveBody = Object.SaveBody       # Получить свойство (* )
        Object.SaveBody = SaveBody       # Установить свойство (* )
        SaveBody = Object.GetSaveBody()  # Получить свойство (**)
        Object.SetSaveBody( SaveBody )   # Установить свойство (**)
        ```
        """
        ...
    @SaveBody.setter
    def SaveBody(self, value: bool): ...




class IImportedSurface(IModelObject):  # iimportedsurface.html
    """
    ## Интерфейс IImportedSurface

    Интерфейс параметров импортированной поверхности
    """

    @property
    def PointsUCount(self) -> int:  # iimportedsurface_pointsucount.html
        """
        ## PointsUCount - Количество точек по U

        Тип данных: long

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        PointsUCount = Object.PointsUCount       # Получить свойство (* )
        PointsUCount = Object.GetPointsUCount()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def PointsVCount(self) -> int:  # iimportedsurface_pointsvcount.html
        """
        ## PointsVCount - Количество точек по V

        Тип данных: long

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        PointsVCount = Object.PointsVCount       # Получить свойство (* )
        PointsVCount = Object.GetPointsVCount()  # Получить свойство (**)
        ```
        """
        ...

    def AddPoint(self, X, Y, Z) -> bool:  # iimportedsurface_addpoint.html
        """
        ## AddPoint - Добавить точку в создаваемый ряд

        Метод должен вызываться между вызовами методов IImportedSurface::BeginPointsSeries и IImportedSurface::EndPointsSeries.

        ### Синтаксис Automation

        ```
        BOOL AddPoint( double X,
        double Y,
        double Z );
        ```

        ### Входные параметры

        +-------------------------------+
        | X, Y, Z | - координаты точки. |
        +-------------------------------+

        ### Возвращаемое значение

        +-----------------------------------------+
        | TRUE  | - в случае удачного завершения, |
        +-----------------------------------------+
        | FALSE | - в случае неудачи.             |
        +-----------------------------------------+
        """
        ...

    def AddPointsSeries(self, UV, IndexAt, Arr) -> bool:  # iimportedsurface_addpointsseries.html
        """
        ## AddPointsSeries - Добавить ряд точек

        ### Синтаксис Automation

        ```
        BOOL AddPointsSeries( BOOL UV,
        long IndexAt,
        VARIANT arr );
        ```

        ### Возвращаемое значение

        +-----------------------------------------+
        | TRUE  | - в случае удачного завершения, |
        +-----------------------------------------+
        | FALSE | - в случае неудачи.             |
        +-----------------------------------------+

        +------------------------------------------------------+
        | UV      | - TRUE - добавлять по U, FALSE - по V,     |
        +------------------------------------------------------+
        | IndexAt | - индекс, перед которым добавлять,         |
        +------------------------------------------------------+
        | arr     | - массив VT_ARRAY | VT_R8 координат точек. |
        +------------------------------------------------------+
        """
        ...

    def BeginPointsSeries(self, UV, IndexAt) -> bool:  # iimportedsurface_beginpointsseries.html
        """
        ## BeginPointsSeries - Начать добавление нового ряда точек

        ### Синтаксис Automation

        ```
        BOOL BeginPointsSeries( BOOL UV,
        long IndexAt );
        ```

        ### Входные параметры

        +-------------------------------------------------------------+
        | UV      | - TRUE - добавлять по U, - FALSE - по V,          |
        +-------------------------------------------------------------+
        | IndexAt | - индекс ряда, перед которым добавлять новый ряд. |
        +-------------------------------------------------------------+

        ### Возвращаемое значение

        +-----------------------------------------+
        | TRUE  | - в случае удачного завершения, |
        +-----------------------------------------+
        | FALSE | - в случае неудачи.             |
        +-----------------------------------------+
        """
        ...

    def ClearPointsSeries(self) -> bool:  # iimportedsurface_clearpointsseries.html
        """
        ## ClearPointsSeries - Очистить список рядов точек

        ### Синтаксис Automation

        ```
        BOOL ClearPointsSeries();
        ```

        ### Возвращаемое значение

        +-----------------------------------------+
        | TRUE  | - в случае удачного завершения, |
        +-----------------------------------------+
        | FALSE | - в случае неудачи.             |
        +-----------------------------------------+

        Метод позволяет удалить все точки поверхности.
        """
        ...

    def DeletePointsSeries(self, UV, Index) -> bool:  # iimportedsurface_deletepointsseries.html
        """
        ## DeletePointsSeries - Удалить ряд точек

        ### Синтаксис Automation

        ```
        BOOL DeletePointsSeries( BOOL UV,
        long Index );
        ```

        ### Входные параметры

        +------------------------------------------------+
        | UV    | - TRUE - удалять по U, - FALSE - по V, |
        +------------------------------------------------+
        | Index | - индекс удаляемого ряда.              |
        +------------------------------------------------+

        ### Возвращаемое значение

        +-----------------------------------------+
        | TRUE  | - в случае удачного завершения, |
        +-----------------------------------------+
        | FALSE | - в случае неудачи.             |
        +-----------------------------------------+
        """
        ...

    def EndPointsSeries(self) -> bool:  # iimportedsurface_endpointsseries.html
        """
        ## EndPointsSeries - Завершить создание нового ряда точек

        ### Синтаксис Automation

        ```
        BOOL EndPointsSeries();
        ```

        ### Возвращаемое значение

        +-----------------------------------------+
        | TRUE  | - в случае удачного завершения, |
        +-----------------------------------------+
        | FALSE | - в случае неудачи.             |
        +-----------------------------------------+
        """
        ...

    def GetPoint(self, UIndex, VIndex) -> bool:  # iimportedsurface_getpoint.html
        """
        ## GetPoint - Получить параметры точки

        ### Синтаксис Automation

        ```
        BOOL GetPoint( long UIndex,
        long VIndex,
        double * X,
        double * Y, double * Z );
        ```

        ### Входные параметры

        +-------------------------------+
        | UIndex | - индекс точки по U, |
        +-------------------------------+
        | VIndex | - индекс точки по V. |
        +-------------------------------+

        ### Выходные параметры

        X, Y,Z - координаты точки.

        Метод позволяет получить параметры точки.

        ### Возвращаемое значение

        +-----------------------------------------+
        | TRUE  | - в случае удачного завершения, |
        +-----------------------------------------+
        | FALSE | - в случае неудачи.             |
        +-----------------------------------------+
        """
        ...

    def GetPoints(self) -> list[float]:  # iimportedsurface_getpoints.html
        """
        ## GetPoints - Получить массив всех точек

        ### Синтаксис Automation

        ```
        VARIANT GetPoints( long * UPCount, long * VPCount );
        ```

        ### Выходные параметры

        +------------------------------------+
        | UPCount | - количество точек по U, |
        +------------------------------------+
        | VPCount | - количество точек по V. |
        +------------------------------------+

        ### Возвращаемое значение

        Массив VT_ARRAY | VT_R8 точек поверхности
        """
        ...

    def SetPoint(self, UIndex, VIndex, X, Y, Z) -> bool:  # iimportedsurface_setpoint.html
        """
        ## SetPoint - Установить параметры точки

        ### Синтаксис Automation

        ```
        BOOL SetPoint( long UIndex,
        long VIndex,
        double X,
        double Y,
        double Z );
        ```

        ### Входные параметры

        +--------------------------------+
        | UIndex  | - индекс точки по U, |
        +--------------------------------+
        | VIndex  | - индекс точки по V, |
        +--------------------------------+
        | X, Y, Z | - координаты точки.  |
        +--------------------------------+

        Метод позволяет установить параметры точки.

        ### Возвращаемое значение

        +-----------------------------------------+
        | TRUE  | - в случае удачного завершения, |
        +-----------------------------------------+
        | FALSE | - в случае неудачи.             |
        +-----------------------------------------+
        """
        ...

    def SetPoints(self, UPCount, VPCount, Points) -> bool:  # iimportedsurface_setpoints.html
        """
        ## SetPoints - Установить массив всех точек

        ### Синтаксис Automation

        ```
        BOOL SetPoints( long UPCount,
        long VPCount, VARIANT Points );
        ```

        ### Входные параметры

        +------------------------------------------------------+
        | UPCount | - количество точек по U,                   |
        +------------------------------------------------------+
        | VPCount | - количество точек по V,                   |
        +------------------------------------------------------+
        | Points  | - массив VT_ARRAY | VT_R8 координат точек. |
        +------------------------------------------------------+

        Метод позволяет установить массив точек поверхности.

        ### Возвращаемое значение

        +-----------------------------------------+
        | TRUE  | - в случае удачного завершения, |
        +-----------------------------------------+
        | FALSE | - в случае неудачи.             |
        +-----------------------------------------+
        """
        ...




class IMeshPointsSurface(IModelObject):  # imeshpointssurface.html
    """
    ## Интерфейс IMeshPointsSurface

    Интерфейс параметров поверхности по сети точек.
    """

    @property
    def BuildingType(self) -> int:  # imeshpointssurface_buildingtype.html
        """
        ## BuildingType - Тип поверхности по сети точек

        Тип данных: из перечисления ksMeshPointsSurfaceBuildingTypeEnum

        ### Синтаксис Automation

        ```
        BuildingType = Object.BuildingType       # Получить свойство (* )
        Object.BuildingType = BuildingType       # Установить свойство (* )
        BuildingType = Object.GetBuildingType()  # Получить свойство (**)
        Object.SetBuildingType( BuildingType )   # Установить свойство (**)
        ```
        """
        ...
    @BuildingType.setter
    def BuildingType(self, value: int): ...

    @property
    def CheckSelfIntersection(self) -> bool:  # imeshpointssurface_checkselfintersection.html
        """
        ## CheckSelfIntersection - Проверка самопересечений

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        CheckSelfIntersection = Object.CheckSelfIntersection       # Получить свойство (* )
        Object.CheckSelfIntersection = CheckSelfIntersection       # Установить свойство (* )
        CheckSelfIntersection = Object.GetCheckSelfIntersection()  # Получить свойство (**)
        Object.SetCheckSelfIntersection( CheckSelfIntersection )   # Установить свойство (**)
        ```
        """
        ...
    @CheckSelfIntersection.setter
    def CheckSelfIntersection(self, value: bool): ...

    @property
    def ClosedU(self) -> bool:  # imeshpointssurface_closedu.html
        """
        ## ClosedU - Признак замкнутости поверхности по направлению U

        Тип данных: BOOL

        Замыкание по тому или иному направлению возможно, если количество точек в этом направлении - три или больше.

        ### Синтаксис Automation

        ```
        ClosedU = Object.ClosedU       # Получить свойство (* )
        Object.ClosedU = ClosedU       # Установить свойство (* )
        ClosedU = Object.GetClosedU()  # Получить свойство (**)
        Object.SetClosedU( ClosedU )   # Установить свойство (**)
        ```
        """
        ...
    @ClosedU.setter
    def ClosedU(self, value: bool): ...

    @property
    def ClosedV(self) -> bool:  # imeshpointssurface_closedv.html
        """
        ## ClosedV - Признак замкнутости поверхности по направлению V

        Тип данных: BOOL

        Замыкание по тому или иному направлению возможно, если количество точек в этом направлении - три или больше.

        ### Синтаксис Automation

        ```
        ClosedV = Object.ClosedV       # Получить свойство (* )
        Object.ClosedV = ClosedV       # Установить свойство (* )
        ClosedV = Object.GetClosedV()  # Получить свойство (**)
        Object.SetClosedV( ClosedV )   # Установить свойство (**)
        ```
        """
        ...
    @ClosedV.setter
    def ClosedV(self, value: bool): ...

    @property
    def ColumnsCount(self) -> int:  # imeshpointssurface_columnscount.html
        """
        ## ColumnsCount - Количество точек в одном ряду по направлению U

        Тип данных: long

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        ColumnsCount = Object.ColumnsCount       # Получить свойство (* )
        ColumnsCount = Object.GetColumnsCount()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def DegreeU(self) -> int:  # imeshpointssurface_degreeu.html
        """
        ## DegreeU - Порядок сплайна по направлению U

        Тип данных: long

        ### Синтаксис Automation

        ```
        DegreeU = Object.DegreeU       # Получить свойство (* )
        Object.DegreeU = DegreeU       # Установить свойство (* )
        DegreeU = Object.GetDegreeU()  # Получить свойство (**)
        Object.SetDegreeU( DegreeU )   # Установить свойство (**)
        ```
        """
        ...
    @DegreeU.setter
    def DegreeU(self, value: int): ...

    @property
    def DegreeV(self) -> int:  # imeshpointssurface_degreev.html
        """
        ## DegreeV - Порядок сплайна по направлению V

        Тип данных: long

        ### Синтаксис Automation

        ```
        DegreeV = Object.DegreeV       # Получить свойство (* )
        Object.DegreeV = DegreeV       # Установить свойство (* )
        DegreeV = Object.GetDegreeV()  # Получить свойство (**)
        Object.SetDegreeV( DegreeV )   # Установить свойство (**)
        ```
        """
        ...
    @DegreeV.setter
    def DegreeV(self, value: int): ...

    @property
    def RowsCount(self) -> int:  # imeshpointssurface_rowscount.html
        """
        ## RowsCount - Количество рядов по направлению V

        Тип данных: long

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        RowsCount = Object.RowsCount       # Получить свойство (* )
        RowsCount = Object.GetRowsCount()  # Получить свойство (**)
        ```
        """
        ...

    def AddPoint(self, X, Y, Z, Weight, PointType, AssociationObject):  # imeshpointssurface_addpoint.html
        """
        ## AddPoint - Добавить точку в создаваемый ряд точек

        Метод должен вызываться между вызовами методов IMeshPointsSurface::BeginPointsSeries и IMeshPointsSurface::EndPointsSeries.

        ### Синтаксис Automation

        ```
        LPDISPATCH AddPoint( double X,
        double Y,
        double Z,
        double Weight,
        ksPoint3DTypeEnum PointType,
        IModelObject * AssociationObject );
        ```

        ### Входные параметры

        +---------------------------------------------+
        | X, Y, Z           | - координаты точки,     |
        +---------------------------------------------+
        | Weight            | - вес точки,            |
        +---------------------------------------------+
        | PointType         | - тип параметров точки, |
        +---------------------------------------------+
        | AssociationObject | - опорный объект.       |
        +---------------------------------------------+

        ### Возвращаемое значение

        Указатель на интерфейс параметров точки
        """
        ...

    def AddPointsSeries(self, AddNewRow, IndexAt, Points, Weights) -> bool:  # imeshpointssurface_addpointsseries.html
        """
        ## AddPointsSeries - Добавить ряд точек

        ### Синтаксис Automation

        ```
        BOOL AddPointsSeries( BOOL AddNewRow,
        long IndexAt,
        VARIANT Points,
        VARIANT Weights );
        ```

        ### Входные параметры

        +----------------------------------------------------------------+
        | AddNewRow | - TRUE - добавить ряд, - FALSE - добавить колонку, |
        +----------------------------------------------------------------+
        | IndexAt   | - индекс, перед которым нужно добавить ряд,        |
        +----------------------------------------------------------------+
        | Points    | - массив VT_ARRAY | VT_R8 координат точек,         |
        +----------------------------------------------------------------+
        | Weights   | - массив VT_ARRAY | VT_R8 весов точек.             |
        +----------------------------------------------------------------+

        ### Возвращаемое значение

        +-----------------------------------------+
        | TRUE  | - в случае удачного завершения, |
        +-----------------------------------------+
        | FALSE | - в случае неудачи.             |
        +-----------------------------------------+
        """
        ...

    def AssociationObject(self, IRow, ICol) -> IModelObject:  # imeshpointssurface_associationobject.html
        """
        ## AssociationObject - Установить опорный объект для вершины

        Тип данных: Указатель на интерфейс IModelObject

        ### Синтаксис Automation

        ```
        AssociationObject = Object.AssociationObject( UIndex, VIndex )     # Получить свойство (* )
        Object.AssociationObject( UIndex, VIndex ) = AssociationObject     # Установить свойство (* )
        AssociationObject = Object.GetAssociationObject( UIndex, VIndex )  # Получить свойство (**)
        Object.SetAssociationObject( UIndex, VIndex, AssociationObject )   # Установить свойство (**)
        ```

        ### Входные параметры

        +------------------------------------+
        | long UIndex | - индекс точки по U, |
        +------------------------------------+
        | long VIndex | - индекс точки по V. |
        +------------------------------------+
        """
        ...

    def BeginPointsSeries(self, AddNewRow, IndexAt):
        ...

    def ClearPointsSeries(self) -> bool:  # imeshpointssurface_clearpointsseries.html
        """
        ## ClearPointsSeries - Очистить список рядов точек

        ### Синтаксис Automation

        ```
        BOOL ClearPointsSeries();
        ```

        ### Возвращаемое значение

        +-----------------------------------------+
        | TRUE  | - в случае удачного завершения, |
        +-----------------------------------------+
        | FALSE | - в случае неудачи.             |
        +-----------------------------------------+

        Метод позволяет удалить все точки поверхности.
        """
        ...

    def DeletePointsSeries(self, DeleteRow, Index) -> bool:  # imeshpointssurface_deletepointsseries.html
        """
        ## DeletePointsSeries - Удалить ряд точек

        ### Синтаксис Automation

        ```
        BOOL DeletePointsSeries( BOOL DeleteRow,
        long Index );
        ```

        ### Входные параметры

        +------------------------------------------------------------------+
        | DeleteRow | - TRUE - удаляется ряд, - FALSE - удаляется колонка, |
        +------------------------------------------------------------------+
        | Index     | - индекс удаляемого ряда.                            |
        +------------------------------------------------------------------+

        ### Возвращаемое значение

        +-----------------------------------------+
        | TRUE  | - в случае удачного завершения, |
        +-----------------------------------------+
        | FALSE | - в случае неудачи.             |
        +-----------------------------------------+
        """
        ...

    def EndPointsSeries(self) -> bool:  # imeshpointssurface_endpointsseries.html
        """
        ## EndPointsSeries - Завершить создание ряда точек

        ### Синтаксис Automation

        ```
        BOOL EndPointsSeries();
        ```

        ### Возвращаемое значение

        +-----------------------------------------+
        | TRUE  | - в случае удачного завершения, |
        +-----------------------------------------+
        | FALSE | - в случае неудачи.             |
        +-----------------------------------------+
        """
        ...

    def GetParams(self) -> bool:  # imeshpointssurface_getparams.html
        """
        ## GetParams - Получить параметры поверхности

        ### Синтаксис Automation

        ```
        BOOL GetParams( BOOL * ClosedV,
        BOOL * ClosedU,
        long * DegreeV,
        long * DegreeU,
        long * PointsVCount,
        long * PointsUCount,
        VARIANT * Points,
        VARIANT * Weights );
        ```

        ### Выходные параметры

        +---------------------------------------------------------+
        | ClosedV      | признак замкнутости поверхности по V,    |
        +---------------------------------------------------------+
        | ClosedU      | признак замкнутости поверхности по U,    |
        +---------------------------------------------------------+
        | DegreeV      | порядок сплайна по V,                    |
        +---------------------------------------------------------+
        | DegreeU      | порядок сплайна по U,                    |
        +---------------------------------------------------------+
        | PointsVCount | количество точек по V,                   |
        +---------------------------------------------------------+
        | PointsUCount | количество точек по U,                   |
        +---------------------------------------------------------+
        | Points       | массив VT_ARRAY | VT_R8 координат точек, |
        +---------------------------------------------------------+
        | Weights      | массив VT_ARRAY | VT_R8 весов точек.     |
        +---------------------------------------------------------+

        ### Возвращаемое значение

        +-----------------------------------------+
        | TRUE  | - в случае удачного завершения, |
        +-----------------------------------------+
        | FALSE | - в случае неудачи.             |
        +-----------------------------------------+
        """
        ...

    def GetPoint(self, IRow, ICol) -> bool:  # imeshpointssurface_getpoint.html
        """
        ## GetPoint - Параметры точки

        ### Синтаксис Automation

        ```
        BOOL GetPoint( long UIndex,
        long VIndex,
        double * X,
        double * Y,
        double * Z,
        double * Weight );
        ```

        ### Входные параметры

        +-------------------------------+
        | UIndex | - индекс точки по U, |
        +-------------------------------+
        | VIndex | - индекс точки по V. |
        +-------------------------------+

        ### Выходные параметры

        +-------------------------------+
        | X, Y, Z | - координаты точки, |
        +-------------------------------+
        | Weight  | - вес точки.        |
        +-------------------------------+

        Метод позволяет получить параметры точки.

        ### Возвращаемое значение

        +-----------------------------------------+
        | TRUE  | - в случае удачного завершения, |
        +-----------------------------------------+
        | FALSE | - в случае неудачи.             |
        +-----------------------------------------+
        """
        ...

    def InitParamByFace(self, Face) -> bool:  # imeshpointssurface_initparambyface.html
        """
        ## InitParamByFace - Создать по объекту

        ### Синтаксис Automation

        ```
        BOOL InitParamByFace( IModelObject * Face );
        ```

        ### Входные параметры

        Face - исходная поверхность.

        Метод позволяет создать поверхность по сети точек с помощью сплайновой аппроксимации поверхности.

        ### Возвращаемое значение

        +-----------------------------------------+
        | TRUE  | - в случае удачного завершения, |
        +-----------------------------------------+
        | FALSE | - в случае неудачи.             |
        +-----------------------------------------+
        """
        ...

    def PointParameters(self, IRow, ICol) -> IKompasAPIObject:  # imeshpointssurface_pointparameters.html
        """
        ## PointParameters - Интерфейс параметров точки поверхности

        Тип данных: указатель на интерфейс IKompasAPIObject

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        PointParameters = Object.PointParameters( UIndex, VIndex )     # Получить свойство (* )
        PointParameters = Object.GetPointParameters( UIndex, VIndex )  # Получить свойство (**)
        ```

        ### Входные параметры

        +------------------------------------+
        | long UIndex | - индекс точки по U, |
        +------------------------------------+
        | long VIndex | - индекс точки по V. |
        +------------------------------------+
        """
        ...

    def PointType(self, IRow, ICol) -> int:  # imeshpointssurface_pointtype.html
        """
        ## PointType - Тип параметров построения точки поверхности

        Тип данных: из перечисления ksPoint3DTypeEnum

        ### Синтаксис Automation

        ```
        PointType = Object.PointType( UIndex, VIndex )     # Получить свойство (* )
        Object.PointType( UIndex, VIndex ) = PointType     # Установить свойство (* )
        PointType = Object.GetPointType( UIndex, VIndex )  # Получить свойство (**)
        Object.SetPointType( UIndex, VIndex, PointType )   # Установить свойство (**)
        ```

        ### Входные параметры

        +------------------------------------+
        | long UIndex | - индекс точки по U, |
        +------------------------------------+
        | long VIndex | - индекс точки по V. |
        +------------------------------------+
        """
        ...

    def SetAssociationObject(self, IRow, ICol, arg2):
        ...

    def SetParams(self, ClosedV, ClosedU, DegreeV, DegreeU, RowsCount, ColumsCount, Points, Weights) -> bool:  # imeshpointssurface_setparams.html
        """
        ## SetParams - Установить параметры поверхности

        ### Синтаксис Automation

        ```
        BOOL SetParams( BOOL ClosedV,
        BOOL ClosedU,
        long DegreeV,
        long DegreeU,
        long PointsVCount,
        long PointsUCount,
        VARIANT Points,
        VARIANT Weights );
        ```

        ### Входные параметры

        +-----------------------------------------------------------+
        | ClosedV      | - признак замкнутости поверхности по V,    |
        +-----------------------------------------------------------+
        | ClosedU      | - признак замкнутости поверхности по U,    |
        +-----------------------------------------------------------+
        | DegreeV      | - порядок сплайна по V,                    |
        +-----------------------------------------------------------+
        | DegreeU      | - порядок сплайна по U,                    |
        +-----------------------------------------------------------+
        | PointsVCount | - количество точек по V,                   |
        +-----------------------------------------------------------+
        | PointsUCount | - количество точек по U,                   |
        +-----------------------------------------------------------+
        | Points       | - массив VT_ARRAY | VT_R8 координат точек, |
        +-----------------------------------------------------------+
        | Weights      | - массив VT_ARRAY | VT_R8 весов точек.     |
        +-----------------------------------------------------------+

        ### Возвращаемое значение

        +-----------------------------------------+
        | TRUE  | - в случае удачного завершения, |
        +-----------------------------------------+
        | FALSE | - в случае неудачи.             |
        +-----------------------------------------+
        """
        ...

    def SetPoint(self, IRow, ICol, X, Y, Z, Weight) -> bool:  # imeshpointssurface_setpoint.html
        """
        ## SetPoint - Параметры точки

        ### Синтаксис Automation

        ```
        BOOL SetPoint( long UIndex,
        long VIndex,
        double X,
        double Y,
        double Z,
        double Weight );
        ```

        ### Входные параметры

        +--------------------------------+
        | UIndex  | - индекс точки по U, |
        +--------------------------------+
        | VIndex  | - индекс точки по V, |
        +--------------------------------+
        | X, Y, Z | - координаты точки,  |
        +--------------------------------+
        | Weight  | - вес точки.         |
        +--------------------------------+

        Метод позволяет установить параметры точки.

        ### Возвращаемое значение

        +-----------------------------------------+
        | TRUE  | - в случае удачного завершения, |
        +-----------------------------------------+
        | FALSE | - в случае неудачи.             |
        +-----------------------------------------+
        """
        ...

    def SetPointType(self, IRow, ICol, arg2):
        ...




class INurbsSurface(IModelObject):  # inurbssurface.html
    """
    ## Интерфейс INurbsSurface

    Интерфейс Nurbs-поверхности.

    Интерфейс позволяет создать NURBS-поверхность по заданной поверхности или по параметрам NURBS-поверхности. Есть возможность ограничить поверхность набором контуров.

    Интерфейс можно получить с помощью свойства INurbsSurfaces::NurbsSurface и метода INurbsSurfaces::Add.
    """

    @property
    def BoundaryCount(self) -> int:  # inurbssurface_boundarycount.html
        """
        ## BoundaryCount - Количество циклов

        Тип данных:long

        ### Синтаксис Automation

        ```
        BoundaryCount = Object.BoundaryCount       # Получить свойство (* )
        BoundaryCount = Object.GetBoundaryCount()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def ClosedU(self) -> bool:  # inurbssurface_closedu.html
        """
        ## ClosedU - Признак замкнутости поверхности по U

        Тип данных: BOOL

        Свойство позволяет получить признак замкнутости поверхности по U.

        Свойство позволяет также преобразовать представление поверхности из замкнутого по U представления в разомкнутое по U и наоборот, из разомкнутого представления в замкнутое. См также INurbsSurface::ClosedV.

        ### Синтаксис Automation

        ```
        ClosedU = Object.ClosedU       # Получить свойство (* )
        Object.ClosedU = ClosedU       # Установить свойство (* )
        ClosedU = Object.GetClosedU()  # Получить свойство (**)
        Object.SetClosedU( ClosedU )   # Установить свойство (**)
        ```
        """
        ...
    @ClosedU.setter
    def ClosedU(self, value: bool): ...

    @property
    def ClosedV(self) -> bool:  # inurbssurface_closedv.html
        """
        ## ClosedV - Признак замкнутости поверхности по V

        Тип данных: BOOL

        Свойство позволяет получить признак замкнутости поверхности по V.

        Свойство позволяет также преобразовать представление поверхности из замкнутого по V представления в разомкнутое по V и наоборот, из разомкнутого представления в замкнутое. См. также INurbsSurface::ClosedU.

        ### Синтаксис Automation

        ```
        ClosedV = Object.ClosedV       # Получить свойство (* )
        Object.ClosedV = ClosedV       # Установить свойство (* )
        ClosedV = Object.GetClosedV()  # Получить свойство (**)
        Object.SetClosedV( ClosedV )   # Установить свойство (**)
        ```
        """
        ...
    @ClosedV.setter
    def ClosedV(self, value: bool): ...

    def AddBoundary(self, UV, Closed, Degree, Points, Weights, Knots, TMin, TMax) -> bool:  # inurbssurface_addboundary.html
        """
        ## AddBoundary - Добавить границу для NURBS-поверхности

        Позволяет добавить границу для поверхности.

        Граница не должна пересекаться с другими границами данной поверхности.

        ### Синтаксис Automation

        ```
        BOOL AddBoundary( BOOL UV, BOOL Closed, long Degree, VARIANT Points, VARIANT Weights, VARIANT Knots, double TMin, double TMax );
        ```

        ### Выходные параметры

        +--------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------+
        | UV         | - TRUE параметры кривой заданы в UV координатах поверхности (2D-Nurbs), - FALSE параметры заданы 3D координатами (3D-Nurbs). При создании границы выполняется проецирование 3D кривой на поверхность, |
        +--------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------+
        | Closed     | - TRUE замкнутое представление - FALSE - разомкнутое представление,                                                                                                                                   |
        +--------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------+
        | Degree     | - порядок NURBS (степень полинома + 1), от 3 до 10,                                                                                                                                                   |
        +--------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------+
        | Points     | - массив SafeArray точек VT_ARRAY | VT_R8,                                                                                                                                                            |
        +--------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------+
        | Weights    | - веса,                                                                                                                                                                                               |
        +--------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------+
        | Knots      | - массив SafeArray узлов точек VT_ARRAY | VT_R8,                                                                                                                                                      |
        +--------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------+
        | TMin, TMax | - минимальный и максимальный параметры кривой.                                                                                                                                                        |
        +--------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------+

        ### Возвращаемое значение

        +--------------------------+
        | TRUE | - в случае удачи. |
        +--------------------------+
        """
        ...

    def DeleteBoundary(self, BoundaryIndex) -> bool:  # inurbssurface_deleteboundary.html
        """
        ## DeleteBoundary - Удалить границу

        ### Синтаксис Automation

        ```
        BOOL DeleteBoundary( long BoundaryIndex )
        ```

        ### Выходные параметры

        +--------------------------------------------------+
        | BoundaryIndex | - индекс цикла (индекс контура). |
        +--------------------------------------------------+

        ### Возвращаемое значение

        +--------------------------+
        | TRUE | - в случае удачи. |
        +--------------------------+
        """
        ...

    def GetBoundary(self, UV, Closed, BoundaryIndex) -> bool:  # inurbssurface_getboundary.html
        """
        ## GetBoundary - Получить параметры NURBS-представления границы

        ### Синтаксис Automation

        ```
        BOOL GetBoundary( BOOL UV, BOOL Closed, long BoundaryIndex, long * Degree,
        VARIANT * Points, VARIANT * Weights, VARIANT * Knots,
        double * TMin, double * TMax );
        ```

        ### Входные параметры

        +------------------------------------------------------------------+
        | Degree     | - порядок NURBS (степень полинома + 1), от 3 до 10, |
        +------------------------------------------------------------------+
        | Points     | - массив SafeArray точек VT_ARRAY | VT_R8,          |
        +------------------------------------------------------------------+
        | Weights    | - веса,                                             |
        +------------------------------------------------------------------+
        | Knots      | - массив SafeArray узлов точек VT_ARRAY | VT_R8,    |
        +------------------------------------------------------------------+
        | TMin, TMax | - минимальный и максимальный параметры кривой.      |
        +------------------------------------------------------------------+

        ### Выходные параметры

        +-----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------+
        | UV            | - TRUE параметры кривой заданы в UV координатах поверхности (2D-Nurbs), - FALSE параметры заданы 3D координатами (3D-Nurbs). При создании границы выполняется проецирование 3D кривой на поверхность. |
        +-----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------+
        | Closed        | - TRUE замкнутое представление, - FALSE - разомкнутое представление,                                                                                                                                  |
        +-----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------+
        | BoundaryIndex | - индекс цикла (индекс контура).                                                                                                                                                                      |
        +-----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------+

        ### Возвращаемое значение

        +--------------------------+
        | TRUE | - в случае удачи. |
        +--------------------------+
        """
        ...

    def GetNurbsParams(self, ClosedV, ClosedU) -> bool:  # inurbssurface_getnurbsparams.html
        """
        ## GetNurbsParams - Получить параметры NURBS-поверхности

        ### Синтаксис Automation

        ```
        BOOL GetNurbsParams( BOOL ClosedV, BOOL ClosedU,
        long * DegreeV, long * DegreeU, long * NPV, long * NPU,
        VARIANT * Points, VARIANT * Weights, VARIANT * KnotsV, VARIANT * KnotsU );
        ```

        ### Входные параметры

        +--------------------------------------------------------------------+
        | DegreeV | - порядок NURBS по V (степень полинома + 1), от 3 до 10, |
        +--------------------------------------------------------------------+
        | DegreeU | - порядок NURBS по U (степень полинома + 1), от 3 до 10, |
        +--------------------------------------------------------------------+
        | NPV     | - количество точек по V,                                 |
        +--------------------------------------------------------------------+
        | NPU     | - количество точек по U,                                 |
        +--------------------------------------------------------------------+
        | Points  | - массив SafeArray точек VT_ARRAY | VT_R8,               |
        +--------------------------------------------------------------------+
        | Weights | - веса,                                                  |
        +--------------------------------------------------------------------+
        | KnotsV  | - массив SafeArray узлов точек по V VT_ARRAY | VT_R8,    |
        +--------------------------------------------------------------------+
        | KnotsU  | - массив SafeArray узлов точек по U VT_ARRAY | VT_R8.    |
        +--------------------------------------------------------------------+

        ### Выходные параметры

        +------------------------------------------------------------------------------------------------------------------------------------------------+
        | ClosedV | - TRUE - получение параметров для замкнутого по V представления, - FALSE - получение параметров для разомкнутого по V представления, |
        +------------------------------------------------------------------------------------------------------------------------------------------------+
        | ClosedU | - TRUE- получение параметров для замкнутого по U представления, - FALSE - получение параметров для разомкнутого по U представления.  |
        +------------------------------------------------------------------------------------------------------------------------------------------------+

        ### Возвращаемое значение

        +--------------------------+
        | TRUE | - в случае удачи. |
        +--------------------------+
        """
        ...

    def InitParamByFace(self, Face) -> bool:  # inurbssurface_initparambyface.html
        """
        ## InitParamByFace - Создать по объекту

        ### Синтаксис Automation

        ```
        BOOL InitParamByFace( LPDISPATCH Face );
        ```

        ### Выходные параметры

        +-------------------------------------------+
        | Face | - указатель на грань IModelObject. |
        +-------------------------------------------+

        ### Возвращаемое значение

        +--------------------------+
        | TRUE | - в случае удачи. |
        +--------------------------+
        """
        ...

    def SetNurbsParams(self, ClosedV, ClosedU, DegreeV, DegreeU, NPV, NPU, Points, Weights, KnotsV, KnotsU) -> bool:  # inurbssurface_setnurbsparams.html
        """
        ## SetNurbsParams - Установить параметры NURBS-поверхности

        ### Синтаксис Automation

        ```
        BOOL SetNurbsParams( BOOL ClosedV, BOOL ClosedU,
        long DegreeV, long DegreeU, long NPV, long NPU,
        VARIANT Points, VARIANT Weights, VARIANT KnotsV, VARIANT KnotsU );
        ```

        ### Выходные параметры

        +-----------------------------------------------------------------------------------------------------------------------------------------------+
        | ClosedV | - TRUE- получение параметров для замкнутого по V представления, - FALSE - получение параметров для разомкнутого по V представления, |
        +-----------------------------------------------------------------------------------------------------------------------------------------------+
        | ClosedU | - TRUE- получение параметров для замкнутого по U представления, - FALSE - получение параметров для разомкнутого по U представления, |
        +-----------------------------------------------------------------------------------------------------------------------------------------------+
        | DegreeV | - порядок NURBS по V (степень полинома + 1), от 3 до 10,                                                                            |
        +-----------------------------------------------------------------------------------------------------------------------------------------------+
        | DegreeU | - порядок NURBS по U (степень полинома + 1), от 3 до 10,                                                                            |
        +-----------------------------------------------------------------------------------------------------------------------------------------------+
        | NPV     | - количество точек по V,                                                                                                            |
        +-----------------------------------------------------------------------------------------------------------------------------------------------+
        | NPU     | - Количество точек по U,                                                                                                            |
        +-----------------------------------------------------------------------------------------------------------------------------------------------+
        | Points  | - массив SafeArray точек VT_ARRAY | VT_R8,                                                                                          |
        +-----------------------------------------------------------------------------------------------------------------------------------------------+
        | Weights | - веса,                                                                                                                             |
        +-----------------------------------------------------------------------------------------------------------------------------------------------+
        | KnotsV  | - массив SafeArray узлов точек по V VT_ARRAY | VT_R8,                                                                               |
        +-----------------------------------------------------------------------------------------------------------------------------------------------+
        | KnotsU  | - массив SafeArray узлов точек по U VT_ARRAY | VT_R8.                                                                               |
        +-----------------------------------------------------------------------------------------------------------------------------------------------+

        ### Возвращаемое значение

        +--------------------------+
        | TRUE | - в случае удачи. |
        +--------------------------+
        """
        ...




class IPlane3D(IModelObject):  # iplane3d.html
    """
    ## Интерфейс IPlane3D

    Интерфейс плоскости 3D.
    """

    @property
    def Surface(self) -> IMathSurface3D:  # iplane3d_surface.html
        """
        ## Surface - Получить интерфейс математической поверхности

        Тип данных: указатель на интерфейс IMathSurface3D

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        Surface = Object.Surface       # Получить свойство (* )
        Surface = Object.GetSurface()  # Получить свойство (**)
        ```
        """
        ...




class IPlane3DByPlaneCurve(IPlane3D):  # iplane3dbyplanecurve.html
    """
    ## Интерфейс IPlane3DByPlaneCurve

    Плоскость через плоскую кривую.
    """

    @property
    def Curve(self) -> IModelObject:  # iplane3dbyplanecurve_curve.html
        """
        ## Curve - Интерфейс плоской кривой

        Тип данных: Указатель на интерфейс IModelObject

        ### Синтаксис Automation

        ```
        Curve = Object.Curve       # Получить свойство (* )
        Object.Curve = Curve       # Установить свойство (* )
        Curve = Object.GetCurve()  # Получить свойство (**)
        Object.SetCurve( Curve )   # Установить свойство (**)
        ```
        """
        ...
    @Curve.setter
    def Curve(self, value: IModelObject): ...




class IPlane3DTangentToFaceInPoint(IPlane3D):  # iplane3dtangenttofaceinpoint.html
    """
    ## Интерфейс IPlane3DTangentToFaceInPoint

    Плоскость касательная к грани в точке 3D.
    """

    @property
    def Face(self) -> IModelObject:  # iplane3dtangenttofaceinpoint_face.html
        """
        ## Face - Грань для построения плоскости

        Тип данных: Указатель на интерфейс IModelObject.

        ### Синтаксис Automation

        ```
        Face = Object.Face       # Получить свойство (* )
        Object.Face = Face       # Установить свойство (* )
        Face = Object.GetFace()  # Получить свойство (**)
        Object.SetFace( Face )   # Установить свойство (**)
        ```
        """
        ...
    @Face.setter
    def Face(self, value: IModelObject): ...

    @property
    def ParamU(self) -> float:  # iplane3dtangenttofaceinpoint_paramu.html
        """
        ## ParamU - Параметр U

        Тип данных: double

        ### Синтаксис Automation

        ```
        ParamU = Object.ParamU       # Получить свойство (* )
        Object.ParamU = ParamU       # Установить свойство (* )
        ParamU = Object.GetParamU()  # Получить свойство (**)
        Object.SetParamU( ParamU )   # Установить свойство (**)
        ```
        """
        ...
    @ParamU.setter
    def ParamU(self, value: float): ...

    @property
    def ParamV(self) -> float:  # iplane3dtangenttofaceinpoint_paramv.html
        """
        ## ParamV - Параметр V

        Тип данных: double

        ### Синтаксис Automation

        ```
        ParamV = Object.ParamV       # Получить свойство (* )
        Object.ParamV = ParamV       # Установить свойство (* )
        ParamV = Object.GetParamV()  # Получить свойство (**)
        Object.SetParamV( ParamV )   # Установить свойство (**)
        ```
        """
        ...
    @ParamV.setter
    def ParamV(self, value: float): ...

    @property
    def Point(self) -> IModelObject:  # iplane3dtangenttofaceinpoint_point.html
        """
        ## Point - Точка для построения плоскости, касательной к грани

        Тип данных: Указатель на интерфейс IModelObject.

        ### Синтаксис Automation

        ```
        Point = Object.Point       # Получить свойство (* )
        Object.Point = Point       # Установить свойство (* )
        Point = Object.GetPoint()  # Получить свойство (**)
        Object.SetPoint( Point )   # Установить свойство (**)
        ```
        """
        ...
    @Point.setter
    def Point(self, value: IModelObject): ...




class IPlane3DByOffset(IPlane3D):  # iplane3dbyoffset.html
    """
    ## Интерфейс IPlane3DByOffset

    Смещенная плоскость.
    """

    @property
    def BasePlane(self) -> IModelObject:  # iplane3dbyoffset_baseplane.html
        """
        ## BasePlane - Интерфейс базовой плоскости или плоской грани

        Тип данных: Указатель на интерфейс IModelObject

        ### Синтаксис Automation

        ```
        BasePlane = Object.BasePlane       # Получить свойство (* )
        Object.BasePlane = BasePlane       # Установить свойство (* )
        BasePlane = Object.GetBasePlane()  # Получить свойство (**)
        Object.SetBasePlane( BasePlane )   # Установить свойство (**)
        ```
        """
        ...
    @BasePlane.setter
    def BasePlane(self, value: IModelObject): ...

    @property
    def Direction(self) -> bool:  # iplane3dbyoffset_direction.html
        """
        ## Direction - Направление смещения от базовой плоскости. TRUE - прямое направление, FALSE - обратное направление

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        Direction = Object.Direction       # Получить свойство (* )
        Object.Direction = Direction       # Установить свойство (* )
        Direction = Object.GetDirection()  # Получить свойство (**)
        Object.SetDirection( Direction )   # Установить свойство (**)
        ```
        """
        ...
    @Direction.setter
    def Direction(self, value: bool): ...

    @property
    def Offset(self) -> float:  # iplane3dbyoffset_offset.html
        """
        ## Offset - Смещение (расстояние) от базовой плоскости

        Тип данных: double

        ### Синтаксис Automation

        ```
        Offset = Object.Offset       # Получить свойство (* )
        Object.Offset = Offset       # Установить свойство (* )
        Offset = Object.GetOffset()  # Получить свойство (**)
        Object.SetOffset( Offset )   # Установить свойство (**)
        ```
        """
        ...
    @Offset.setter
    def Offset(self, value: float): ...




class IPlane3DBy3Points(IPlane3D):  # iplane3dby3points.html
    """
    ## Интерфейс IPlane3DBy3Points

    Плоскость, проходящая через три вершины.
    """

    @property
    def Point1(self) -> IModelObject:  # iplane3dby3points_point1.html
        """
        ## Point1 - Интерфейс первой точки

        Тип данных: Указатель на интерфейс IModelObject

        ### Синтаксис Automation

        ```
        Point1 = Object.Point1       # Получить свойство (* )
        Object.Point1 = Point1       # Установить свойство (* )
        Point1 = Object.GetPoint1()  # Получить свойство (**)
        Object.SetPoint1( Point1 )   # Установить свойство (**)
        ```
        """
        ...
    @Point1.setter
    def Point1(self, value: IModelObject): ...

    @property
    def Point2(self) -> IModelObject:  # iplane3dby3points_point2.html
        """
        ## Point2 - Интерфейс второй точки

        Тип данных: Указатель на интерфейс IModelObject

        ### Синтаксис Automation

        ```
        Point2 = Object.Point2       # Получить свойство (* )
        Object.Point2 = Point2       # Установить свойство (* )
        Point2 = Object.GetPoint2()  # Получить свойство (**)
        Object.SetPoint2( Point2 )   # Установить свойство (**)
        ```
        """
        ...
    @Point2.setter
    def Point2(self, value: IModelObject): ...

    @property
    def Point3(self) -> IModelObject:  # iplane3dby3points_point3.html
        """
        ## Point3 - Интерфейс третьей точки

        Тип данных: Указатель на интерфейс IModelObject

        ### Синтаксис Automation

        ```
        Point3 = Object.Point3       # Получить свойство (* )
        Object.Point3 = Point3       # Установить свойство (* )
        Point3 = Object.GetPoint3()  # Получить свойство (**)
        Object.SetPoint3( Point3 )   # Установить свойство (**)
        ```
        """
        ...
    @Point3.setter
    def Point3(self, value: IModelObject): ...




class IPlane3DByAngle(IPlane3D):  # iplane3dbyangle.html
    """
    ## Интерфейс IPlane3DByAngle

    Плоскость под углом к другой плоскости.
    """

    @property
    def Angle(self) -> float:  # iplane3dbyangle_angle.html
        """
        ## Angle - Угол наклона плоскости относительно базовой плоскости

        Тип данных: double

        ### Синтаксис Automation

        ```
        Angle = Object.Angle       # Получить свойство (* )
        Object.Angle = Angle       # Установить свойство (* )
        Angle = Object.GetAngle()  # Получить свойство (**)
        Object.SetAngle( Angle )   # Установить свойство (**)
        ```
        """
        ...
    @Angle.setter
    def Angle(self, value: float): ...

    @property
    def BaseLine(self) -> IModelObject:  # iplane3dbyangle_baseline.html
        """
        ## BaseLine - Интерфейс базовой прямой (оси или ребра)

        Тип данных: Указатель на интерфейс IModelObject

        ### Синтаксис Automation

        ```
        BaseLine = Object.BaseLine       # Получить свойство (* )
        Object.BaseLine = BaseLine       # Установить свойство (* )
        BaseLine = Object.GetBaseLine()  # Получить свойство (**)
        Object.SetBaseLine( BaseLine )   # Установить свойство (**)
        ```
        """
        ...
    @BaseLine.setter
    def BaseLine(self, value: IModelObject): ...

    @property
    def BasePlane(self) -> IModelObject:  # iplane3dbyangle_baseplane.html
        """
        ## BasePlane - Интерфейс базовой плоскости

        Тип данных: Указатель на интерфейс IModelObject

        ### Синтаксис Automation

        ```
        BasePlane = Object.BasePlane       # Получить свойство (* )
        Object.BasePlane = BasePlane       # Установить свойство (* )
        BasePlane = Object.GetBasePlane()  # Получить свойство (**)
        Object.SetBasePlane( BasePlane )   # Установить свойство (**)
        ```
        """
        ...
    @BasePlane.setter
    def BasePlane(self, value: IModelObject): ...

    @property
    def Direction(self) -> bool:  # iplane3dbyangle_direction.html
        """
        ## Direction - Направление смещения от базовой плоскости. TRUE - прямое направление, FALSE - обратное направление

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        Direction = Object.Direction       # Получить свойство (* )
        Object.Direction = Direction       # Установить свойство (* )
        Direction = Object.GetDirection()  # Получить свойство (**)
        Object.SetDirection( Direction )   # Установить свойство (**)
        ```
        """
        ...
    @Direction.setter
    def Direction(self, value: bool): ...




class IPlane3DByEdgeAndPoint(IPlane3D):  # iplane3dbyedgeandpoint.html
    """
    ## Интерфейс IPlane3DByEdgeAndPoint

    Плоскость через ребро и вершину.
    """

    @property
    def Edge(self) -> IModelObject:  # iplane3dbyedgeandpoint_edge.html
        """
        ## Edge - Интерфейс ребра, через которое требуется построить плоскость

        Тип данных: Указатель на интерфейс IModelObject

        ### Синтаксис Automation

        ```
        Edge = Object.Edge       # Получить свойство (* )
        Object.Edge = Edge       # Установить свойство (* )
        Edge = Object.GetEdge()  # Получить свойство (**)
        Object.SetEdge( Edge )   # Установить свойство (**)
        ```
        """
        ...
    @Edge.setter
    def Edge(self, value: IModelObject): ...

    @property
    def Point(self) -> IModelObject:  # iplane3dbyedgeandpoint_point.html
        """
        ## Point - Интерфейс вершины, через которую требуется построить плоскость

        Тип данных: Указатель на интерфейс IModelObject

        ### Синтаксис Automation

        ```
        Point = Object.Point       # Получить свойство (* )
        Object.Point = Point       # Установить свойство (* )
        Point = Object.GetPoint()  # Получить свойство (**)
        Object.SetPoint( Point )   # Установить свойство (**)
        ```
        """
        ...
    @Point.setter
    def Point(self, value: IModelObject): ...




class IPlane3DBy2Edge(IPlane3D):  # iplane3dby2edge.html
    """
    ## Интерфейс IPlane3DBy2Edge

    Плоскость через ребро параллельно /перпендикулярно другому ребру 3D.
    """

    @property
    def Edge1(self) -> IModelObject:  # iplane3dby2edge_edge1.html
        """
        ## Edge1 - Первое ребро, через которое будет проходить новая плоскость

        Тип данных: Указатель на интерфейс IModelObject.

        ### Синтаксис Automation

        ```
        Edge1 = Object.Edge1       # Получить свойство (* )
        Object.Edge1 = Edge1       # Установить свойство (* )
        Edge1 = Object.GetEdge1()  # Получить свойство (**)
        Object.SetEdge1( Edge1 )   # Установить свойство (**)
        ```
        """
        ...
    @Edge1.setter
    def Edge1(self, value: IModelObject): ...

    @property
    def Edge2(self) -> IModelObject:  # iplane3dby2edge_edge2.html
        """
        ## Edge2 - Второе ребро, через которое будет проходить новая плоскость

        Тип данных: Указатель на интерфейс IModelObject

        ### Синтаксис Automation

        ```
        Edge2 = Object.Edge2       # Получить свойство (* )
        Object.Edge2 = Edge2       # Установить свойство (* )
        Edge2 = Object.GetEdge2()  # Получить свойство (**)
        Object.SetEdge2( Edge2 )   # Установить свойство (**)
        ```
        """
        ...
    @Edge2.setter
    def Edge2(self, value: IModelObject): ...

    @property
    def Parallel(self) -> bool:  # iplane3dby2edge_parallel.html
        """
        ## Parallel - Признак положения плоскости. TRUE - Параллельно ребру, FALSE - Перпендикулярно ребру

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        Parallel = Object.Parallel       # Получить свойство (* )
        Object.Parallel = Parallel       # Установить свойство (* )
        Parallel = Object.GetParallel()  # Получить свойство (**)
        Object.SetParallel( Parallel )   # Установить свойство (**)
        ```
        """
        ...
    @Parallel.setter
    def Parallel(self, value: bool): ...




class IPlane3DParallelByPoint(IPlane3D):  # iplane3dparallelbypoint.html
    """
    ## Интерфейс IPlane3DParallelByPoint

    Плоскость через вершину параллельно другой плоскости 3D.
    """

    @property
    def Plane(self) -> IModelObject:  # iplane3dparallelbypoint_plane.html
        """
        ## Plane - Интерфейс плоскости или плоской грани, параллельно которой требуется построить плоскость

        Тип данных: Указатель на интерфейс IModelObject

        ### Синтаксис Automation

        ```
        Plane = Object.Plane       # Получить свойство (* )
        Object.Plane = Plane       # Установить свойство (* )
        Plane = Object.GetPlane()  # Получить свойство (**)
        Object.SetPlane( Plane )   # Установить свойство (**)
        ```
        """
        ...
    @Plane.setter
    def Plane(self, value: IModelObject): ...

    @property
    def Point(self) -> IModelObject:  # iplane3dparallelbypoint_point.html
        """
        ## Point - Интерфейс вершины, через которую требуется построить плоскость

        Тип данных: Указатель на интерфейс IModelObject

        ### Синтаксис Automation

        ```
        Point = Object.Point       # Получить свойство (* )
        Object.Point = Point       # Установить свойство (* )
        Point = Object.GetPoint()  # Получить свойство (**)
        Object.SetPoint( Point )   # Установить свойство (**)
        ```
        """
        ...
    @Point.setter
    def Point(self, value: IModelObject): ...




class IPlane3DPerpendicularByEdge(IPlane3D):  # iplane3dperpendicularbyedge.html
    """
    ## Интерфейс IPlane3DPerpendicularByEdge

    Плоскость, проходящая через вершину перпендикулярно ребру.
    """

    @property
    def Edge(self) -> IModelObject:  # iplane3dperpendicularbyedge_edge.html
        """
        ## Edge - Интерфейс ребра, перпендикулярно которому будет проходить новая плоскость

        Тип данных: Указатель на интерфейс IModelObject.

        ### Синтаксис Automation

        ```
        Edge = Object.Edge       # Получить свойство (* )
        Object.Edge = Edge       # Установить свойство (* )
        Edge = Object.GetEdge()  # Получить свойство (**)
        Object.SetEdge( Edge )   # Установить свойство (**)
        ```
        """
        ...
    @Edge.setter
    def Edge(self, value: IModelObject): ...

    @property
    def Point(self) -> IModelObject:  # iplane3dperpendicularbyedge_point.html
        """
        ## Point - Интерфейс вершины, через которую требуется построить плоскость

        Тип данных: Указатель на интерфейс IModelObject

        ### Синтаксис Automation

        ```
        Point = Object.Point       # Получить свойство (* )
        Object.Point = Point       # Установить свойство (* )
        Point = Object.GetPoint()  # Получить свойство (**)
        Object.SetPoint( Point )   # Установить свойство (**)
        ```
        """
        ...
    @Point.setter
    def Point(self, value: IModelObject): ...

    @property
    def Vector3D(self) -> IVector3D:  # iplane3dperpendicularbyedge_vector3d.html
        """
        ## Vector3D - Получить параметры вектора

        Тип данных: Указатель на интерфейс IVector3D

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        Vector3D = Object.Vector3D       # Получить свойство (* )
        Vector3D = Object.GetVector3D()  # Получить свойство (**)
        ```
        """
        ...




class IPlane3DNormalToSurface(IPlane3D):  # iplane3dnormaltosurface.html
    """
    ## Интерфейс IPlane3DNormalToSurface

    Нормальная плоскость.
    """

    @property
    def Angle(self) -> float:  # iplane3dnormaltosurface_angle.html
        """
        ## Angle - Угол наклона плоскости относительно плоского объекта

        Тип данных: double

        ### Синтаксис Automation

        ```
        Angle = Object.Angle       # Получить свойство (* )
        Object.Angle = Angle       # Установить свойство (* )
        Angle = Object.GetAngle()  # Получить свойство (**)
        Object.SetAngle( Angle )   # Установить свойство (**)
        ```
        """
        ...
    @Angle.setter
    def Angle(self, value: float): ...

    @property
    def AngleDirection(self) -> bool:  # iplane3dnormaltosurface_angledirection.html
        """
        ## AngleDirection - Направление угла.TRUE - прямое направление, FALSE - обратное направление

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        AngleDirection = Object.AngleDirection       # Получить свойство (* )
        Object.AngleDirection = AngleDirection       # Установить свойство (* )
        AngleDirection = Object.GetAngleDirection()  # Получить свойство (**)
        Object.SetAngleDirection( AngleDirection )   # Установить свойство (**)
        ```
        """
        ...
    @AngleDirection.setter
    def AngleDirection(self, value: bool): ...

    @property
    def Face(self) -> IModelObject:  # iplane3dnormaltosurface_face.html
        """
        ## Face - Интерфейс цилиндрической или конической грани, к которой требуется построить нормальную плоскость

        Тип данных: Указатель на интерфейс IModelObject

        ### Синтаксис Automation

        ```
        Face = Object.Face       # Получить свойство (* )
        Object.Face = Face       # Установить свойство (* )
        Face = Object.GetFace()  # Получить свойство (**)
        Object.SetFace( Face )   # Установить свойство (**)
        ```
        """
        ...
    @Face.setter
    def Face(self, value: IModelObject): ...

    @property
    def Plane(self) -> IModelObject:  # iplane3dnormaltosurface_plane.html
        """
        ## Plane - Интерфейс координатной плоскости или плоской грани, параллельно которой требуется построить плоскость

        Тип данных: Указатель на интерфейс IModelObject

        ### Синтаксис Automation

        ```
        Plane = Object.Plane       # Получить свойство (* )
        Object.Plane = Plane       # Установить свойство (* )
        Plane = Object.GetPlane()  # Получить свойство (**)
        Object.SetPlane( Plane )   # Установить свойство (**)
        ```
        """
        ...
    @Plane.setter
    def Plane(self, value: IModelObject): ...




class IPlane3DMiddle(IPlane3D):  # iplane3dmiddle.html
    """
    ## Интерфейс IPlane3DMiddle

    Средняя плоскость 3D.
    """

    @property
    def Object1(self) -> IModelObject:  # iplane3dmiddle_object1.html
        """
        ## Object1 - Прямолинейное ребро, плоская грань или конструктивная плоскость 1

        Тип данных: Указатель на интерфейс IModelObject

        ### Синтаксис Automation

        ```
        Object1 = Object.Object1       # Получить свойство (* )
        Object.Object1 = Object1       # Установить свойство (* )
        Object1 = Object.GetObject1()  # Получить свойство (**)
        Object.SetObject1( Object1 )   # Установить свойство (**)
        ```
        """
        ...
    @Object1.setter
    def Object1(self, value: IModelObject): ...

    @property
    def Object2(self) -> IModelObject:  # iplane3dmiddle_object2.html
        """
        ## Object2 - Прямолинейное ребро, плоская грань или конструктивная плоскость 2

        Тип данных: Указатель на интерфейс IModelObject

        ### Синтаксис Automation

        ```
        Object2 = Object.Object2       # Получить свойство (* )
        Object.Object2 = Object2       # Установить свойство (* )
        Object2 = Object.GetObject2()  # Получить свойство (**)
        Object.SetObject2( Object2 )   # Установить свойство (**)
        ```
        """
        ...
    @Object2.setter
    def Object2(self, value: IModelObject): ...

    @property
    def Oriention(self) -> bool:  # iplane3dmiddle_oriention.html
        """
        ## Oriention - Положение плоскости. TRUE - Положение 1, FALSE - Положение 2

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        Oriention = Object.Oriention       # Получить свойство (* )
        Object.Oriention = Oriention       # Установить свойство (* )
        Oriention = Object.GetOriention()  # Получить свойство (**)
        Object.SetOriention( Oriention )   # Установить свойство (**)
        ```
        """
        ...
    @Oriention.setter
    def Oriention(self, value: bool): ...




class IPlane3DByEdgeAndPlane(IPlane3D):  # iplane3dbyedgeandplane.html
    """
    ## Интерфейс IPlane3DByEdgeAndPlane

    Плоскость через ребро параллельно / перпендикулярно грани 3D.
    """

    @property
    def Edge(self) -> IModelObject:  # iplane3dbyedgeandplane_edge.html
        """
        ## Edge - Ребро, через которое будет проходить новая плоскость

        Тип данных: Указатель на интерфейс IModelObject

        ### Синтаксис Automation

        ```
        Edge = Object.Edge       # Получить свойство (* )
        Object.Edge = Edge       # Установить свойство (* )
        Edge = Object.GetEdge()  # Получить свойство (**)
        Object.SetEdge( Edge )   # Установить свойство (**)
        ```
        """
        ...
    @Edge.setter
    def Edge(self, value: IModelObject): ...

    @property
    def Parallel(self) -> bool:  # iplane3dbyedgeandplane_parallel.html
        """
        ## Parallel - Признак положения плоскости. TRUE - Параллельно грани, FALSE - Перпендикулярно грани

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        Object.Parallel = Object.Object.Parallel       # Получить свойство (* )
        Object.Object.Parallel = Object.Parallel       # Установить свойство (* )
        Object.Parallel = Object.GetObject.Parallel()  # Получить свойство (**)
        Object.SetObject.Parallel( Object.Parallel )   # Установить свойство (**)
        ```
        """
        ...
    @Parallel.setter
    def Parallel(self, value: bool): ...

    @property
    def Plane(self) -> IModelObject:  # iplane3dbyedgeandplane_plane.html
        """
        ## Plane - Объект, параллельно или перпендикулярно которому будет проходить новая плоскость

        Тип данных: Указатель на интерфейс IModelObject

        ### Синтаксис Automation

        ```
        Plane = Object.Plane       # Получить свойство (* )
        Object.Plane = Plane       # Установить свойство (* )
        Plane = Object.GetPlane()  # Получить свойство (**)
        Object.SetPlane( Plane )   # Установить свойство (**)
        ```
        """
        ...
    @Plane.setter
    def Plane(self, value: IModelObject): ...




class IPlane3DTangentToFace(IPlane3D):  # iplane3dtangenttoface.html
    """
    ## Интерфейс IPlane3DTangentToFace

    Плоскость, касательная к грани 3D.
    """

    @property
    def Angle(self) -> float:  # iplane3dtangenttoface_angle.html
        """
        ## Angle - Угол

        Тип данных: double

        ### Синтаксис Automation

        ```
        Angle = Object.Angle       # Получить свойство (* )
        Object.Angle = Angle       # Установить свойство (* )
        Angle = Object.GetAngle()  # Получить свойство (**)
        Object.SetAngle( Angle )   # Установить свойство (**)
        ```
        """
        ...
    @Angle.setter
    def Angle(self, value: float): ...

    @property
    def Face(self) -> IModelObject:  # iplane3dtangenttoface_face.html
        """
        ## Face - Цилиндрическая или коническая поверхность для построения касательной плоскости

        Тип данных: Указатель на интерфейс IModelObject

        ### Синтаксис Automation

        ```
        Face = Object.Face       # Получить свойство (* )
        Object.Face = Face       # Установить свойство (* )
        Face = Object.GetFace()  # Получить свойство (**)
        Object.SetFace( Face )   # Установить свойство (**)
        ```
        """
        ...
    @Face.setter
    def Face(self, value: IModelObject): ...

    @property
    def Orientation(self) -> bool:  # iplane3dtangenttoface_orientation.html
        """
        ## Orientation - Положение плоскости. TRUE - Положение 1, FALSE - Положение 2

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        Orientation = Object.Orientation       # Получить свойство (* )
        Object.Orientation = Orientation       # Установить свойство (* )
        Orientation = Object.GetOrientation()  # Получить свойство (**)
        Object.SetOrientation( Orientation )   # Установить свойство (**)
        ```
        """
        ...
    @Orientation.setter
    def Orientation(self, value: bool): ...

    @property
    def Plane(self) -> IModelObject:  # iplane3dtangenttoface_plane.html
        """
        ## Plane - Плоскость, проходящая через ось грани

        Тип данных: Указатель на интерфейс IModelObject

        ### Синтаксис Automation

        ```
        Plane = Object.Plane       # Получить свойство (* )
        Object.Plane = Plane       # Установить свойство (* )
        Plane = Object.GetPlane()  # Получить свойство (**)
        Object.SetPlane( Plane )   # Установить свойство (**)
        ```
        """
        ...
    @Plane.setter
    def Plane(self, value: IModelObject): ...




class IPlane3DUnhistoried(IPlane3D):  # iplane3dunhistoried.html
    """
    ## Интерфейс IPlane3DUnhistoried

    Интерфейс плоскости без истории.

    Интерфейс можно получить, используя свойство IPlanes3D::Plane3D для импортированных плоскостей.

    Версия КОМПАС v22
    """

    def GetPlacement(self) -> bool:  # iplane3dunhistoried_getplacement.html
        """
        ## GetPlacement - Получить положение плоскости

        Элементы матрицы передаются в виде одномерного массива из 16 элементов SafeAttray типа (VT_ARRAY | VT_R8).

        Матрица имеет размер 4х4.

        ### Синтаксис Automation

        ```
        BOOL GetPlacement( VARIANT * place );
        ```

        ### Выходные параметры

        Place - Матрица, задающая положение плоскости.

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def SetPlacement(self, Place):  # iplane3dunhistoried_setplacement.html
        """
        ## SetPlacement - Установить положение плоскости

        Элементы матрицы передаются в виде одномерного массива из 16 элементов SafeAttray типа (VT_ARRAY | VT_R8).

        Матрица имеет размер 4х4.

        ### Синтаксис Automation

        ```
        BOOL SetPlacement( VARIANT Place );
        ```

        ### Входные параметры

        Place - Матрица, задающая положение плоскости.
        """
        ...




class IRestoredSurface(IModelObject):  # irestoredsurface.html
    """
    ## Интерфейс IRestoredSurface

    Интерфейс Восстановленная поверхность.

    Интерфейс можно получить с помощью метода коллекции операций восстановленная поверхность IRestoredSurfaces::Add или свойства IRestoredSurfaces::RestoredSurface.

    Версия: KOMPAS v19
    """

    @property
    def Face(self) -> IModelObject:  # irestoredsurface_face.html
        """
        ## Face - Грань поверхности

        Тип данных: Указатель на интерфейс IModelObject

        ### Синтаксис Automation

        ```
        Face = Object.Face       # Получить свойство (* )
        Object.Face = Face       # Установить свойство (* )
        Face = Object.GetFace()  # Получить свойство (**)
        Object.SetFace( Face )   # Установить свойство (**)
        ```
        """
        ...
    @Face.setter
    def Face(self, value: IModelObject): ...




class IRuledSurface(IModelObject):  # iruledsurface.html
    """
    ## Интерфейс IRuledSurface

    Интерфейс операции создания линейчатой поверхности.
    """

    @property
    def AutoSegmentation(self):
        ...
    @AutoSegmentation.setter
    def AutoSegmentation(self, value): ...

    @property
    def BuildingType(self):
        ...
    @BuildingType.setter
    def BuildingType(self, value): ...

    @property
    def CheckSelfIntersection(self):
        ...
    @CheckSelfIntersection.setter
    def CheckSelfIntersection(self, value): ...

    @property
    def ConsiderComplianceVertices(self):
        ...
    @ConsiderComplianceVertices.setter
    def ConsiderComplianceVertices(self, value): ...

    @property
    def Curves1(self):
        ...
    @Curves1.setter
    def Curves1(self, value): ...

    @property
    def Curves2(self):
        ...
    @Curves2.setter
    def Curves2(self, value): ...

    @property
    def EdgesCount(self):
        ...

    @property
    def Parameters(self):
        ...

    def AddNewEdge(self, IndexAt) -> bool:  # iruledsurface_addnewedge.html
        """
        ## AddNewEdge - Добавление ребра линейчатой поверхности после указанного индексом

        ### Синтаксис Automation

        ```
        BOOL AddNewEdge( long IndexAt );
        ```

        ### Входные параметры

        IndexAt - индекс ребра, после которого нужно вставить новое ребро.

        Метод позволяет вставить новое ребро перед заданным. Новое ребро будет иметь следующий по порядку номер.

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def DeleteEdge(self, Index) -> bool:  # iruledsurface_deleteedge.html
        """
        ## DeleteEdge - Удаление ребра линейчатой поверхности

        1. Оставшиеся ребра нумеруются заново.

        2. Удаление ребер невозможно, если в списке остались два ребра.

        ### Синтаксис Automation

        ```
        BOOL DeleteEdge( long Index )
        ```

        ### Входные параметры

        Index - индекс удаляемого ребра.

        Метод позволяет удалить ребро, заданное по индексу.

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def GetEdgePointParam(self, EdgeIndex, StartPoint) -> bool:  # iruledsurface_getedgepointparam.html
        """
        ## GetEdgePointParam - Получение параметров точки ребра линейчатой поверхности

        ### Синтаксис Automation

        ```
        BOOL GetEdgePointParam( long EdgeIndex,
        BOOL StartPoint,
        double * X,
        double * Y,
        double * Z,
        double * T,
        IModelObject ** Segment,
        IModelObject ** AssociateVertex );
        ```

        ### Входные параметры

        +------------------------------------------------------+
        | EdgeIndex  | - индекс ребра,                         |
        +------------------------------------------------------+
        | StartPoint | - начальная или конечная вершина ребра. |
        +------------------------------------------------------+

        ### Выходные параметры

        +----------------------------------------------------------------------------+
        | X, Y, Z         | - координаты точки,                                      |
        +----------------------------------------------------------------------------+
        | T               | - параметр точки на кривой,                              |
        +----------------------------------------------------------------------------+
        | Segment         | - сегмент направляющей, на котором находится точка,      |
        +----------------------------------------------------------------------------+
        | AssociateVertex | - вершина направляющей, с которой связана вершина ребра. |
        +----------------------------------------------------------------------------+

        Метод позволяет получить параметры вершины ребра линейчатой поверхности.

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def GetEdgePointParams(self) -> bool:  # iruledsurface_getedgepointparams.html
        """
        ## GetEdgePointParams - Получение параметров ребер линейчатой поверхности

        ### Синтаксис Automation

        ```
        BOOL GetEdgePointParams( VARIANT * Points1,
        VARIANT * T1,
        VARIANT * Segments1,
        VARIANT * AssociateVertexes1,
        VARIANT * Points2,
        VARIANT * T2, V
        ARIANT * Segments2,
        VARIANT * AssociateVertexes2 );
        ```

        ### Входные параметры

        +------------------------------------------------------------------------------------------------------------------------------+
        | Points1            | - массив SAFEARRAY VT_R8 - VT_ARRAY | VT_R8 координат вершин ребер на первой направляющей,              |
        +------------------------------------------------------------------------------------------------------------------------------+
        | T1                 | - массив SAFEARRAY VT_R8 - VT_ARRAY | VT_R8 параметров вершин ребер на первой направляющей,             |
        +------------------------------------------------------------------------------------------------------------------------------+
        | Segments1          | - массив SAFEARRAY DISPATCH - VT_ARRAY | VT_DISPATCH сегментов первой направляющей,                     |
        +------------------------------------------------------------------------------------------------------------------------------+
        | AssociateVertexes1 | - массив SAFEARRAY DISPATCH - VT_ARRAY | VT_DISPATCH вершин первой направляющей, связанных с вершинами, |
        +------------------------------------------------------------------------------------------------------------------------------+
        | Points2            | - массив SAFEARRAY VT_R8 - VT_ARRAY | VT_R8 координат вершин ребер на второй направляющей,              |
        +------------------------------------------------------------------------------------------------------------------------------+
        | T2                 | - массив SAFEARRAY VT_R8 - VT_ARRAY | VT_R8 параметров вершин ребер на второй направляющей,             |
        +------------------------------------------------------------------------------------------------------------------------------+
        | Segments2          | - массив SAFEARRAY DISPATCH - VT_ARRAY | VT_DISPATCH сегментов второй направляющей,                     |
        +------------------------------------------------------------------------------------------------------------------------------+
        | AssociateVertexes2 | - массив SAFEARRAY DISPATCH - VT_ARRAY | VT_DISPATCH вершин второй направляющей, связанных с вершинами. |
        +------------------------------------------------------------------------------------------------------------------------------+

        Метод позволяет получать параметры ребер линейчатой поверхности.

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def SetEdgePointParam(self, EdgeIndex, StartPoint, X, Y, Z, T, Segment, AssociateVertex) -> bool:  # iruledsurface_setedgepointparam.html
        """
        ## SetEdgePointParam - Изменение положения точки ребра линейчатой поверхности

        ### Синтаксис Automation

        ```
        BOOL SetEdgePointParam( long EdgeIndex,
        BOOL StartPoint,
        double X,
        double Y,
        double Z,
        double * T,
        IModelObject * Segment,
        IModelObject * AssociateVertex );
        ```

        ### Входные параметры

        +----------------------------------------------------------------------------+
        | EdgeIndex       | - индекс ребра,                                          |
        +----------------------------------------------------------------------------+
        | StartPoint      | - начальная или конечная вершина ребра,                  |
        +----------------------------------------------------------------------------+
        | X, Y, Z         | - координаты точки,                                      |
        +----------------------------------------------------------------------------+
        | T               | - параметр точки на кривой,                              |
        +----------------------------------------------------------------------------+
        | Segment         | - сегмент направляющей, на котором находится точка,      |
        +----------------------------------------------------------------------------+
        | AssociateVertex | - вершина направляющей, с которой связана вершина ребра. |
        +----------------------------------------------------------------------------+

        Метод позволяет задать положение вершины ребра линейчатой поверхности на направляющей.

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...




class IRuledSurfaceMovement(IDispatch):  # iruledsurfacemovement.html
    """
    ## Интерфейс IRuledSurfaceMovement

    Интерфейс данных о движении сечения линейчатой поверхности

    Интерфейс является дополнительным для интерфейса линейчатой поверхности IRuledSurface.

    Версия КОМПАС v22
    """

    @property
    def MovementAxisLine(self) -> typing.Any:  # iruledsurfacemovement_movementaxisline.html
        """
        ## MovementAxisLine - Осевая линия движения сечения

        Тип данных: VARIANT

        Объекты, задающие осевую линию для движения сечения в виде SAFEARRAY'я DISPATCH - VT_ARRAY | VT_DISPATCH.

        ### Синтаксис Automation

        ```
        MovementAxisLine = Object.MovementAxisLine       # Получить свойство (* )
        Object.MovementAxisLine = MovementAxisLine       # Установить свойство (* )
        MovementAxisLine = Object.GetMovementAxisLine()  # Получить свойство (**)
        Object.SetMovementAxisLine( MovementAxisLine )   # Установить свойство (**)
        ```
        """
        ...
    @MovementAxisLine.setter
    def MovementAxisLine(self, value: typing.Any): ...

    @property
    def MovementDirectionObject(self) -> IModelObject:  # iruledsurfacemovement_movementdirectionobject.html
        """
        ## MovementDirectionObject - Направляющий объект для движения сечения

        Тип данных: Указатель на интерфейс IModelObject

        ### Синтаксис Automation

        ```
        MovementDirectionObject = Object.MovementDirectionObject       # Получить свойство (* )
        Object.MovementDirectionObject = MovementDirectionObject       # Установить свойство (* )
        MovementDirectionObject = Object.GetMovementDirectionObject()  # Получить свойство (**)
        Object.SetMovementDirectionObject( MovementDirectionObject )   # Установить свойство (**)
        ```
        """
        ...
    @MovementDirectionObject.setter
    def MovementDirectionObject(self, value: IModelObject): ...

    @property
    def MovementType(self) -> int:  # iruledsurfacemovement_movementtype.html
        """
        ## MovementType - Движение сечения

        Тип данных: из перечисления ksRuledSurfaceMovementTypeEnum

        ### Синтаксис Automation

        ```
        MovementType = Object.MovementType       # Получить свойство (* )
        Object.MovementType = MovementType       # Установить свойство (* )
        MovementType = Object.GetMovementType()  # Получить свойство (**)
        Object.SetMovementType( MovementType )   # Установить свойство (**)
        ```
        """
        ...
    @MovementType.setter
    def MovementType(self, value: int): ...

    @property
    def MovementVector(self) -> IVector3D:  # iruledsurfacemovement_movementvector.html
        """
        ## MovementVector - Направляющий вектор для движения сечения

        Тип данных: Указатель на интерфейс IVector3D

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        MovementVector = Object.MovementVector       # Получить свойство (* )
        MovementVector = Object.GetMovementVector()  # Получить свойство (**)
        ```
        """
        ...




class IRuledSurfaceParametersByCurves(IDispatch):  # iruledsurfaceparametersbycurves.html
    """
    ## Интерфейс IRuledSurfaceParametersByCurves

    Интерфейс параметров линейчатой поверхности "По двум кривым".

    Интерфейс можно получить с помощью способа IRuledSurface::Parameters для типа построения - Линейчатая поверхность по двум кривым.

    Версия КОМПАС v22
    """

    @property
    def AutoSegmentation(self) -> bool:  # iruledsurfaceparametersbycurves_autosegmentation.html
        """
        ## AutoSegmentation - Автоматическое разбиение

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        AutoSegmentation = Object.AutoSegmentation       # Получить свойство (* )
        Object.AutoSegmentation = AutoSegmentation       # Установить свойство (* )
        AutoSegmentation = Object.GetAutoSegmentation()  # Получить свойство (**)
        Object.SetAutoSegmentation( AutoSegmentation )   # Установить свойство (**)
        ```
        """
        ...
    @AutoSegmentation.setter
    def AutoSegmentation(self, value: bool): ...

    @property
    def Curves1(self) -> typing.Any:  # iruledsurfaceparametersbycurves_curves1.html
        """
        ## Curves1 - Первая цепочка кривых

        Тип данных: VARIANT

        Объекты, задающие первую цепочку в виде SAFEARRAY'я DISPATCH - VT_ARRAY | VT_DISPATCH.

        ### Синтаксис Automation

        ```
        Curves1 = Object.Curves1       # Получить свойство (* )
        Object.Curves1 = Curves1       # Установить свойство (* )
        Curves1 = Object.GetCurves1()  # Получить свойство (**)
        Object.SetCurves1( Curves1 )   # Установить свойство (**)
        ```
        """
        ...
    @Curves1.setter
    def Curves1(self, value: typing.Any): ...

    @property
    def Curves2(self) -> typing.Any:  # iruledsurfaceparametersbycurves_curves2.html
        """
        ## Curves2 - Вторая цепочка кривых

        Тип данных: VARIANT

        Объекты, задающие вторую цепочку в виде SAFEARRAY'я DISPATCH - VT_ARRAY | VT_DISPATCH.

        ### Синтаксис Automation

        ```
        Curves2 = Object.Curves2       # Получить свойство (* )
        Object.Curves2 = Curves2       # Установить свойство (* )
        Curves2 = Object.GetCurves2()  # Получить свойство (**)
        Object.SetCurves2( Curves2 )   # Установить свойство (**)
        ```
        """
        ...
    @Curves2.setter
    def Curves2(self, value: typing.Any): ...

    @property
    def EdgesCount(self) -> int:  # iruledsurfaceparametersbycurves_edgescount.html
        """
        ## EdgesCount - Количество ребер

        Тип данных: long

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        EdgesCount = Object.EdgesCount       # Получить свойство (* )
        EdgesCount = Object.GetEdgesCount()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def SectionAlignmentType(self) -> int:  # iruledsurfaceparametersbycurves_sectionalignmenttype.html
        """
        ## SectionAlignmentType - Способ выравнивания сечения

        Тип данных:из перечисления ksRuledSurfaceSectionAlignmentTypeEnum

        ### Синтаксис Automation

        ```
        SectionAlignmentType = Object.SectionAlignmentType       # Получить свойство (* )
        Object.SectionAlignmentType = SectionAlignmentType       # Установить свойство (* )
        SectionAlignmentType = Object.GetSectionAlignmentType()  # Получить свойство (**)
        Object.SetSectionAlignmentType( SectionAlignmentType )   # Установить свойство (**)
        ```
        """
        ...
    @SectionAlignmentType.setter
    def SectionAlignmentType(self, value: int): ...

    @property
    def SplitEdges(self) -> bool:  # iruledsurfaceparametersbycurves_splitedges.html
        """
        ## SplitEdges - Разбивать на грани

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        SplitEdges = Object.SplitEdges       # Получить свойство (* )
        Object.SplitEdges = SplitEdges       # Установить свойство (* )
        SplitEdges = Object.GetSplitEdges()  # Получить свойство (**)
        Object.SetSplitEdges( SplitEdges )   # Установить свойство (**)
        ```
        """
        ...
    @SplitEdges.setter
    def SplitEdges(self, value: bool): ...

    def AddNewEdge(self, IndexAt) -> bool:  # iruledsurfaceparametersbycurves_addnewedge.html
        """
        ## AddNewEdge - Добавление ребра после указанного индексом

        ### Синтаксис Automation

        ```
        BOOL AddNewEdge( long IndexAt );
        ```

        ### Входные параметры

        IndexAt - индекс

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def DeleteEdge(self, Index) -> bool:  # iruledsurfaceparametersbycurves_deleteedge.html
        """
        ## DeleteEdge - Удаление ребра

        ### Синтаксис Automation

        ```
        BOOL DeleteEdge( long Index );
        ```

        ### Входные параметры

        Index - индекс

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def GetEdgePointParam(self, EdgeIndex, StartPoint) -> bool:  # iruledsurfaceparametersbycurves_getedgepointparam.html
        """
        ## GetEdgePointParam - Получение параметров точки ребра

        ### Синтаксис Automation

        ```
        BOOL GetEdgePointParam( long EdgeIndex, BOOL StartPoint, double * X, double * Y, double * Z, double * T, IModelObject * * Segment, IModelObject * * AssociateVertex );
        ```

        ### Входные параметры

        +--------------------------------------+
        | EdgeIndex  | - индекс ребра,         |
        +--------------------------------------+
        | StartPoint | - признак начала ребра. |
        +--------------------------------------+

        ### Выходные параметры

        +---------------------------------------------------------+
        | StartPoint      | - признак начала ребра,               |
        +---------------------------------------------------------+
        | X, Y, Z         | - новые координаты точки,             |
        +---------------------------------------------------------+
        | T               | - параметр кривой,                    |
        +---------------------------------------------------------+
        | Segment         | - сегмент кривой IModelObject,        |
        +---------------------------------------------------------+
        | AssociateVertex | - ассоциативная вершина IModelObject. |
        +---------------------------------------------------------+

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def GetEdgePointParams(self) -> bool:  # iruledsurfaceparametersbycurves_getedgepointparams.html
        """
        ## GetEdgePointParams - Получение параметров ребер

        ### Синтаксис Automation

        ```
        BOOL GetEdgePointParams( VARIANT * Points1, VARIANT * T1, VARIANT * Segments1, VARIANT * AssociateVertexes1, VARIANT * Points2, VARIANT * T2, VARIANT * Segments2, VARIANT * AssociateVertexes2 );
        ```

        ### Выходные параметры

        +--------------------------------------------------------------+
        | Points1            | - точки на первой цепочке кривых,       |
        +--------------------------------------------------------------+
        | T1                 | - параметры кривых первой цепочки,      |
        +--------------------------------------------------------------+
        | Segments1          | - сегменты первой цепочки,              |
        +--------------------------------------------------------------+
        | AssociateVertexes1 | - ассоциативные вершины первой цепочки, |
        +--------------------------------------------------------------+
        | Points2            | - точки на второй цепочке кривых,       |
        +--------------------------------------------------------------+
        | T2                 | - параметры кривых второй цепочки,      |
        +--------------------------------------------------------------+
        | Segments2          | - сегменты второй цепочки,              |
        +--------------------------------------------------------------+
        | AssociateVertexes2 | - ассоциативные вершины второй цепочки. |
        +--------------------------------------------------------------+

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def SetEdgePointParam(self, EdgeIndex, StartPoint, X, Y, Z, T, Segment, AssociateVertex) -> bool:  # iruledsurfaceparametersbycurves_setedgepointparam.html
        """
        ## SetEdgePointParam - Изменение положения точки ребра

        ### Синтаксис Automation

        ```
        BOOL SetEdgePointParam( long EdgeIndex, BOOL StartPoint, double X, double Y, double Z, double * T, IModelObject * Segment, IModelObject * AssociateVertex );
        ```

        ### Входные параметры

        +---------------------------------------------------------+
        | EdgeIndex       | - индекс ребра,                       |
        +---------------------------------------------------------+
        | StartPoint      | - признак начала ребра,               |
        +---------------------------------------------------------+
        | X, Y, Z         | - новые координаты точки,             |
        +---------------------------------------------------------+
        | T               | - параметр кривой,                    |
        +---------------------------------------------------------+
        | Segment         | - сегмент кривой IModelObject,        |
        +---------------------------------------------------------+
        | AssociateVertex | - ассоциативная вершина IModelObject. |
        +---------------------------------------------------------+

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...




class IRuledSurfaceParametersByFaces(IKompasAPIObject):  # iruledsurfaceparametersbyfaces.html
    """
    ## Интерфейс IRuledSurfaceParametersByFaces

    Интерфейс параметров линейчатой поверхности "По двум поверхностям".

    Интерфейс можно получить с помощью способа IRuledSurface::Parameters для типа построения - Линейчатая поверхность по двум поверхностям.

    Версия КОМПАС v22
    """

    @property
    def Surface1(self) -> typing.Any:  # iruledsurfaceparametersbyfaces_surface1.html
        """
        ## Surface1 - Поверхность 1

        Тип данных: VARIANT

        Объекты, задающие первый набор поверхностей SAFEARRAY'я DISPATCH - VT_ARRAY | VT_DISPATCH.

        ### Синтаксис Automation

        ```
        Surface1 = Object.Surface1       # Получить свойство (* )
        Object.Surface1 = Surface1       # Установить свойство (* )
        Surface1 = Object.GetSurface1()  # Получить свойство (**)
        Object.SetSurface1( Surface1 )   # Установить свойство (**)
        ```
        """
        ...
    @Surface1.setter
    def Surface1(self, value: typing.Any): ...

    @property
    def Surface2(self) -> typing.Any:  # iruledsurfaceparametersbyfaces_surface2.html
        """
        ## Surface2 - Поверхность 2

        Тип данных: VARIANT

        Объекты, задающие второй набор поверхностей SAFEARRAY'я DISPATCH - VT_ARRAY | VT_DISPATCH.

        ### Синтаксис Automation

        ```
        Surface2 = Object.Surface2       # Получить свойство (* )
        Object.Surface2 = Surface2       # Установить свойство (* )
        Surface2 = Object.GetSurface2()  # Получить свойство (**)
        Object.SetSurface2( Surface2 )   # Установить свойство (**)
        ```
        """
        ...
    @Surface2.setter
    def Surface2(self, value: typing.Any): ...

    @property
    def TouchOption(self) -> int:  # iruledsurfaceparametersbyfaces_touchoption.html
        """
        ## TouchOption - Вариант касания

        Тип данных:long

        ### Синтаксис Automation

        ```
        TouchOption = Object.TouchOption       # Получить свойство (* )
        Object.TouchOption = TouchOption       # Установить свойство (* )
        TouchOption = Object.GetTouchOption()  # Получить свойство (**)
        Object.SetTouchOption( TouchOption )   # Установить свойство (**)
        ```
        """
        ...
    @TouchOption.setter
    def TouchOption(self, value: int): ...

    def SetNextTouchOption(self, Forward) -> bool:  # iruledsurfaceparametersbyfaces_setnexttouchoption.html
        """
        ## SetNextTouchOption - Установить следующий вариант касания

        ### Синтаксис Automation

        ```
        BOOL SetNextTouchOption( BOOL Forward );
        ```

        ### Входные параметры

        +-------------------------------------------------------------------------------------------+
        | Forward | - TRUE - установить следующий вариант, - FALSE - установить предыдущий вариант. |
        +-------------------------------------------------------------------------------------------+

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...




class IRuledSurfaceParametersByCurveAndDir(IKompasAPIObject):  # iruledsurfaceparametersbycurveanddir.html
    """
    ## Интерфейс IRuledSurfaceParametersByCurveAndDir

    Интерфейс параметров линейчатой поверхности "По кривой и направлению".

    Интерфейс можно получить с помощью метода IRuledSurface::Parameters для типа построения - Линейчатая поверхность по кривой и направлению.

    Версия КОМПАС v22
    """

    @property
    def Curve(self) -> typing.Any:  # iruledsurfaceparametersbycurveanddir_curve.html
        """
        ## Curve - Кривая

        Тип данных: VARIANT

        Набор кривых SAFEARRAY DISPATCH - VT_ARRAY | VT_DISPATCH.

        ### Синтаксис Automation

        ```
        Curve = Object.Curve       # Получить свойство (* )
        Object.Curve = Curve       # Установить свойство (* )
        Curve = Object.GetCurve()  # Получить свойство (**)
        Object.SetCurve( Curve )   # Установить свойство (**)
        ```
        """
        ...
    @Curve.setter
    def Curve(self, value: typing.Any): ...

    @property
    def CurveDirection(self) -> bool:  # iruledsurfaceparametersbycurveanddir_curvedirection.html
        """
        ## CurveDirection - Направление кривой-сечения

        Тип данных:BOOL

        ### Синтаксис Automation

        ```
        CurveDirection = Object.CurveDirection       # Получить свойство (* )
        Object.CurveDirection = CurveDirection       # Установить свойство (* )
        CurveDirection = Object.GetCurveDirection()  # Получить свойство (**)
        Object.SetCurveDirection( CurveDirection )   # Установить свойство (**)
        ```
        """
        ...
    @CurveDirection.setter
    def CurveDirection(self, value: bool): ...

    @property
    def DeviationLawSketch(self) -> IModelObject:  # iruledsurfaceparametersbycurveanddir_deviationlawsketch.html
        """
        ## DeviationLawSketch - Закон. Эскиз с кривой. Значение отклонения изменяется по закону y=f(x), заданному кривой в эскизе

        Тип данных: Указатель на интерфейс IModelObject

        ### Синтаксис Automation

        ```
        DeviationLawSketch = Object.DeviationLawSketch       # Получить свойство (* )
        Object.DeviationLawSketch = DeviationLawSketch       # Установить свойство (* )
        DeviationLawSketch = Object.GetDeviationLawSketch()  # Получить свойство (**)
        Object.SetDeviationLawSketch( DeviationLawSketch )   # Установить свойство (**)
        ```
        """
        ...
    @DeviationLawSketch.setter
    def DeviationLawSketch(self, value: IModelObject): ...

    @property
    def DeviationLawType(self) -> int:  # iruledsurfaceparametersbycurveanddir_deviationlawtype.html
        """
        ## DeviationLawType - Тип функции для отклонения

        Тип данных: из перечисления ksLawTypeEnum

        ### Синтаксис Automation

        ```
        DeviationLawType = Object.DeviationLawType       # Получить свойство (* )
        Object.DeviationLawType = DeviationLawType       # Установить свойство (* )
        DeviationLawType = Object.GetDeviationLawType()  # Получить свойство (**)
        Object.SetDeviationLawType( DeviationLawType )   # Установить свойство (**)
        ```
        """
        ...
    @DeviationLawType.setter
    def DeviationLawType(self, value: int): ...

    @property
    def DeviationMaxAngle(self) -> float:  # iruledsurfaceparametersbycurveanddir_deviationmaxangle.html
        """
        ## DeviationMaxAngle - Конечный угол

        Тип данных: double

        ### Синтаксис Automation

        ```
        DeviationMaxAngle = Object.DeviationMaxAngle       # Получить свойство (* )
        Object.DeviationMaxAngle = DeviationMaxAngle       # Установить свойство (* )
        DeviationMaxAngle = Object.GetDeviationMaxAngle()  # Получить свойство (**)
        Object.SetDeviationMaxAngle( DeviationMaxAngle )   # Установить свойство (**)
        ```
        """
        ...
    @DeviationMaxAngle.setter
    def DeviationMaxAngle(self, value: float): ...

    @property
    def DeviationMinAngle(self) -> float:  # iruledsurfaceparametersbycurveanddir_deviationminangle.html
        """
        ## DeviationMinAngle - Начальный угол

        Тип данных: double

        ### Синтаксис Automation

        ```
        DeviationMinAngle = Object.DeviationMinAngle       # Получить свойство (* )
        Object.DeviationMinAngle = DeviationMinAngle       # Установить свойство (* )
        DeviationMinAngle = Object.GetDeviationMinAngle()  # Получить свойство (**)
        Object.SetDeviationMinAngle( DeviationMinAngle )   # Установить свойство (**)
        ```
        """
        ...
    @DeviationMinAngle.setter
    def DeviationMinAngle(self, value: float): ...

    @property
    def DirectionObject(self):  # iruledsurfaceparametersbycurveanddir_directionobject.html
        """
        ## DirectionObject - Направляющий объект

        Тип данных: Указатель на интерфейсIModelObject

        ### Синтаксис Automation

        ```
        DirectionObject = Object.DirectionObject       # Получить свойство (* )
        Object.DirectionObject = DirectionObject       # Установить свойство (* )
        DirectionObject = Object.GetDirectionObject()  # Получить свойство (**)
        Object.SetDirectionObject( DirectionObject )   # Установить свойство (**)
        ```
        """
        ...
    @DirectionObject.setter
    def DirectionObject(self, value): ...

    @property
    def DirectionVector(self) -> IVector3D:  # iruledsurfaceparametersbycurveanddir_directionvector.html
        """
        ## DirectionVector - Направляющий вектор

        Тип данных: Указатель на интерфейс IVector3D

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        DirectionVector = Object.DirectionVector       # Получить свойство (* )
        DirectionVector = Object.GetDirectionVector()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def OffsetLawSketch(self) -> IModelObject:  # iruledsurfaceparametersbycurveanddir_offsetlawsketch.html
        """
        ## OffsetLawSketch - Закон. Эскиз с кривой. Значение смещения изменяется по закону y=f(x), заданному кривой в эскизе

        Тип данных: Указатель на интерфейс IModelObject

        ### Синтаксис Automation

        ```
        OffsetLawSketch = Object.OffsetLawSketch       # Получить свойство (* )
        Object.OffsetLawSketch = OffsetLawSketch       # Установить свойство (* )
        OffsetLawSketch = Object.GetOffsetLawSketch()  # Получить свойство (**)
        Object.SetOffsetLawSketch( OffsetLawSketch )   # Установить свойство (**)
        ```
        """
        ...
    @OffsetLawSketch.setter
    def OffsetLawSketch(self, value: IModelObject): ...

    @property
    def OffsetLawType(self) -> int:  # iruledsurfaceparametersbycurveanddir_offsetlawtype.html
        """
        ## OffsetLawType - Тип функции для смещения

        Тип данных: из перечисления ksLawTypeEnum

        ### Синтаксис Automation

        ```
        OffsetLawType = Object.OffsetLawType       # Получить свойство (* )
        Object.OffsetLawType = OffsetLawType       # Установить свойство (* )
        OffsetLawType = Object.GetOffsetLawType()  # Получить свойство (**)
        Object.SetOffsetLawType( OffsetLawType )   # Установить свойство (**)
        ```
        """
        ...
    @OffsetLawType.setter
    def OffsetLawType(self, value: int): ...

    @property
    def OffsetMaxValue(self) -> float:  # iruledsurfaceparametersbycurveanddir_offsetmaxvalue.html
        """
        ## OffsetMaxValue - Конечное расстояние

        Тип данных: double

        ### Синтаксис Automation

        ```
        OffsetMaxValue = Object.OffsetMaxValue       # Получить свойство (* )
        Object.OffsetMaxValue = OffsetMaxValue       # Установить свойство (* )
        OffsetMaxValue = Object.GetOffsetMaxValue()  # Получить свойство (**)
        Object.SetOffsetMaxValue( OffsetMaxValue )   # Установить свойство (**)
        ```
        """
        ...
    @OffsetMaxValue.setter
    def OffsetMaxValue(self, value: float): ...

    @property
    def OffsetMinValue(self) -> float:  # iruledsurfaceparametersbycurveanddir_offsetminvalue.html
        """
        ## OffsetMinValue - Начальное расстояние

        Тип данных: double

        ### Синтаксис Automation

        ```
        OffsetMinValue = Object.OffsetMinValue       # Получить свойство (* )
        Object.OffsetMinValue = OffsetMinValue       # Установить свойство (* )
        OffsetMinValue = Object.GetOffsetMinValue()  # Получить свойство (**)
        Object.SetOffsetMinValue( OffsetMinValue )   # Установить свойство (**)
        ```
        """
        ...
    @OffsetMinValue.setter
    def OffsetMinValue(self, value: float): ...




class IRuledSurfaceParametersByCurveAndFace(IKompasAPIObject):  # iruledsurfaceparametersbycurveandface.html
    """
    ## Интерфейс IRuledSurfaceParametersByCurveAndFace

    Интерфейс параметров линейчатой поверхности "По кривой и поверхности".

    Интерфейс можно получить с помощью метода IRuledSurface::Parameters для типа построения - Линейчатая поверхность по кривой и поверхности.

    Версия КОМПАС v22
    """

    @property
    def Curve(self) -> typing.Any:  # iruledsurfaceparametersbycurveandface_curve.html
        """
        ## Curve - Кривая

        Тип данных: VARIANT

        Набор кривых SAFEARRAY DISPATCH - VT_ARRAY | VT_DISPATCH.

        ### Синтаксис Automation

        ```
        Curve = Object.Curve       # Получить свойство (* )
        Object.Curve = Curve       # Установить свойство (* )
        Curve = Object.GetCurve()  # Получить свойство (**)
        Object.SetCurve( Curve )   # Установить свойство (**)
        ```
        """
        ...
    @Curve.setter
    def Curve(self, value: typing.Any): ...

    @property
    def CurveDirection(self) -> bool:  # iruledsurfaceparametersbycurveandface_curvedirection.html
        """
        ## CurveDirection - Направление кривой-сечения

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        CurveDirection = Object.CurveDirection       # Получить свойство (* )
        Object.CurveDirection = CurveDirection       # Установить свойство (* )
        CurveDirection = Object.GetCurveDirection()  # Получить свойство (**)
        Object.SetCurveDirection( CurveDirection )   # Установить свойство (**)
        ```
        """
        ...
    @CurveDirection.setter
    def CurveDirection(self, value: bool): ...

    @property
    def DeviationLawSketch(self) -> IModelObject:  # iruledsurfaceparametersbycurveandface_eviationlawsketch.html
        """
        ## DeviationLawSketch - Закон. Эскиз с кривой. Значение отклонения изменяется по закону y=f(x), заданному кривой в эскизе

        Тип данных: Указатель на интерфейс IModelObject

        ### Синтаксис Automation

        ```
        DeviationLawSketch = Object.DeviationLawSketch       # Получить свойство (* )
        Object.DeviationLawSketch = DeviationLawSketch       # Установить свойство (* )
        DeviationLawSketch = Object.GetDeviationLawSketch()  # Получить свойство (**)
        Object.SetDeviationLawSketch( DeviationLawSketch )   # Установить свойство (**)
        ```
        """
        ...
    @DeviationLawSketch.setter
    def DeviationLawSketch(self, value: IModelObject): ...

    @property
    def DeviationLawType(self) -> int:  # iruledsurfaceparametersbycurveandface_deviationlawtype.html
        """
        ## DeviationLawType - Тип функции для отклонения

        Тип данных: из перечисления ksLawTypeEnum

        ### Синтаксис Automation

        ```
        DeviationLawType = Object.DeviationLawType       # Получить свойство (* )
        Object.DeviationLawType = DeviationLawType       # Установить свойство (* )
        DeviationLawType = Object.GetDeviationLawType()  # Получить свойство (**)
        Object.SetDeviationLawType( DeviationLawType )   # Установить свойство (**)
        ```
        """
        ...
    @DeviationLawType.setter
    def DeviationLawType(self, value: int): ...

    @property
    def DeviationMaxAngle(self) -> float:  # iruledsurfaceparametersbycurveandface_deviationmaxangle.html
        """
        ## DeviationMaxAngle - Конечный угол

        Тип данных: double

        ### Синтаксис Automation

        ```
        DeviationMaxAngle = Object.DeviationMaxAngle       # Получить свойство (* )
        Object.DeviationMaxAngle = DeviationMaxAngle       # Установить свойство (* )
        DeviationMaxAngle = Object.GetDeviationMaxAngle()  # Получить свойство (**)
        Object.SetDeviationMaxAngle( DeviationMaxAngle )   # Установить свойство (**)
        ```
        """
        ...
    @DeviationMaxAngle.setter
    def DeviationMaxAngle(self, value: float): ...

    @property
    def DeviationMinAngle(self) -> float:  # iruledsurfaceparametersbycurveandface_deviationminangle.html
        """
        ## DeviationMinAngle - Начальный угол

        Тип данных: double

        ### Синтаксис Automation

        ```
        DeviationMinAngle = Object.DeviationMinAngle       # Получить свойство (* )
        Object.DeviationMinAngle = DeviationMinAngle       # Установить свойство (* )
        DeviationMinAngle = Object.GetDeviationMinAngle()  # Получить свойство (**)
        Object.SetDeviationMinAngle( DeviationMinAngle )   # Установить свойство (**)
        ```
        """
        ...
    @DeviationMinAngle.setter
    def DeviationMinAngle(self, value: float): ...

    @property
    def OffsetLawSketch(self) -> IModelObject:  # iruledsurfaceparametersbycurveandface_offsetlawsketch.html
        """
        ## OffsetLawSketch - Закон. Эскиз с кривой. Значение смещения изменяется по закону y=f(x), заданному кривой в эскизе

        Тип данных: Указатель на интерфейс IModelObject

        ### Синтаксис Automation

        ```
        OffsetLawSketch = Object.OffsetLawSketch       # Получить свойство (* )
        Object.OffsetLawSketch = OffsetLawSketch       # Установить свойство (* )
        OffsetLawSketch = Object.GetOffsetLawSketch()  # Получить свойство (**)
        Object.SetOffsetLawSketch( OffsetLawSketch )   # Установить свойство (**)
        ```
        """
        ...
    @OffsetLawSketch.setter
    def OffsetLawSketch(self, value: IModelObject): ...

    @property
    def OffsetLawType(self) -> int:  # iruledsurfaceparametersbycurveandface_offsetlawtype.html
        """
        ## OffsetLawType - Тип функции для смещения

        Тип данных: из перечисления ksLawTypeEnum

        ### Синтаксис Automation

        ```
        OffsetLawType = Object.OffsetLawType       # Получить свойство (* )
        Object.OffsetLawType = OffsetLawType       # Установить свойство (* )
        OffsetLawType = Object.GetOffsetLawType()  # Получить свойство (**)
        Object.SetOffsetLawType( OffsetLawType )   # Установить свойство (**)
        ```
        """
        ...
    @OffsetLawType.setter
    def OffsetLawType(self, value: int): ...

    @property
    def OffsetMaxValue(self) -> float:  # iruledsurfaceparametersbycurveandface_offsetmaxvalue.html
        """
        ## OffsetMaxValue - Конечное расстояние

        Тип данных: double

        ### Синтаксис Automation

        ```
        OffsetMaxValue = Object.OffsetMaxValue       # Получить свойство (* )
        Object.OffsetMaxValue = OffsetMaxValue       # Установить свойство (* )
        OffsetMaxValue = Object.GetOffsetMaxValue()  # Получить свойство (**)
        Object.SetOffsetMaxValue( OffsetMaxValue )   # Установить свойство (**)
        ```
        """
        ...
    @OffsetMaxValue.setter
    def OffsetMaxValue(self, value: float): ...

    @property
    def OffsetMinValue(self) -> float:  # iruledsurfaceparametersbycurveandface_offsetminvalue.html
        """
        ## OffsetMinValue - Начальное расстояние

        Тип данных: double

        ### Синтаксис Automation

        ```
        OffsetMinValue = Object.OffsetMinValue       # Получить свойство (* )
        Object.OffsetMinValue = OffsetMinValue       # Установить свойство (* )
        OffsetMinValue = Object.GetOffsetMinValue()  # Получить свойство (**)
        Object.SetOffsetMinValue( OffsetMinValue )   # Установить свойство (**)
        ```
        """
        ...
    @OffsetMinValue.setter
    def OffsetMinValue(self, value: float): ...

    @property
    def RuledSurfaceDirection(self) -> int:  # iruledsurfaceparametersbycurveandface_ruledsurfacedirection.html
        """
        ## RuledSurfaceDirection - Направление построения

        Тип данных: из перечисления ksRuledSurfaceDirectionTypeEnum

        ### Синтаксис Automation

        ```
        RuledSurfaceDirection = Object.RuledSurfaceDirection       # Получить свойство (* )
        Object.RuledSurfaceDirection = RuledSurfaceDirection       # Установить свойство (* )
        RuledSurfaceDirection = Object.GetRuledSurfaceDirection()  # Получить свойство (**)
        Object.SetRuledSurfaceDirection( RuledSurfaceDirection )   # Установить свойство (**)
        ```
        """
        ...
    @RuledSurfaceDirection.setter
    def RuledSurfaceDirection(self, value: int): ...

    @property
    def Surface(self) -> typing.Any:  # iruledsurfaceparametersbycurveandface_surface.html
        """
        ## Surface - Поверхность

        Тип данных: VARIANT

        Набор поверхностей SAFEARRAY'я DISPATCH - VT_ARRAY | VT_DISPATCH.

        ### Синтаксис Automation

        ```
        Surface = Object.Surface       # Получить свойство (* )
        Object.Surface = Surface       # Установить свойство (* )
        Surface = Object.GetSurface()  # Получить свойство (**)
        Object.SetSurface( Surface )   # Установить свойство (**)
        ```
        """
        ...
    @Surface.setter
    def Surface(self, value: typing.Any): ...

    @property
    def SurfaceDirection(self) -> bool:  # iruledsurfaceparametersbycurveandface_surfacedirection.html
        """
        ## SurfaceDirection - Направление относительно поверхности, задающей положение кривой-сечения

        Тип данных:BOOL

        ### Синтаксис Automation

        ```
        SurfaceDirection = Object.SurfaceDirection       # Получить свойство (* )
        Object.SurfaceDirection = SurfaceDirection       # Установить свойство (* )
        SurfaceDirection = Object.GetSurfaceDirection()  # Получить свойство (**)
        Object.SetSurfaceDirection( SurfaceDirection )   # Установить свойство (**)
        ```
        """
        ...
    @SurfaceDirection.setter
    def SurfaceDirection(self, value: bool): ...




class IRuledSurfaceTangentParametersByCurveAndFace(IKompasAPIObject):  # iruledsurfacetangentparametersbycurveandface.html
    """
    ## Интерфейс IRuledSurfaceTangentParametersByCurveAndFace

    Интерфейс параметров линейчатой поверхности "По кривой с касанием к поверхности".

    Интерфейс можно получить с помощью метода IRuledSurface::Parameters для типа построения - Линейчатая поверхность по кривой с касанием к поверхности.

    Версия КОМПАС v22
    """

    @property
    def Curve(self) -> typing.Any:  # iruledsurfacetangentparametersbycurveandface_curve.html
        """
        ## Curve - Кривая

        Тип данных: VARIANT

        Набор кривых SAFEARRAY'я DISPATCH - VT_ARRAY | VT_DISPATCH.

        ### Синтаксис Automation

        ```
        Curve = Object.Curve       # Получить свойство (* )
        Object.Curve = Curve       # Установить свойство (* )
        Curve = Object.GetCurve()  # Получить свойство (**)
        Object.SetCurve( Curve )   # Установить свойство (**)
        ```
        """
        ...
    @Curve.setter
    def Curve(self, value: typing.Any): ...

    @property
    def Surface(self) -> typing.Any:  # iruledsurfacetangentparametersbycurveandface_surface.html
        """
        ## Surface - Поверхность

        Тип данных: VARIANT

        Набор поверхностей SAFEARRAY'я DISPATCH - VT_ARRAY | VT_DISPATCH.

        ### Синтаксис Automation

        ```
        Surface = Object.Surface       # Получить свойство (* )
        Object.Surface = Surface       # Установить свойство (* )
        Surface = Object.GetSurface()  # Получить свойство (**)
        Object.SetSurface( Surface )   # Установить свойство (**)
        ```
        """
        ...
    @Surface.setter
    def Surface(self, value: typing.Any): ...

    @property
    def TouchOption(self) -> int:  # iruledsurfacetangentparametersbycurveandface_touchoption.html
        """
        ## TouchOption - Вариант касания

        Тип данных: long

        ### Синтаксис Automation

        ```
        TouchOption = Object.TouchOption       # Получить свойство (* )
        Object.TouchOption = TouchOption       # Установить свойство (* )
        TouchOption = Object.GetTouchOption()  # Получить свойство (**)
        Object.SetTouchOption( TouchOption )   # Установить свойство (**)
        ```
        """
        ...
    @TouchOption.setter
    def TouchOption(self, value: int): ...

    def SetNextTouchOption(self, Forward) -> bool:  # iruledsurfacetangentparametersbycurveandface_setnexttouchoption2.html
        """
        ## SetNextTouchOption - Установить следующий вариант касания

        ### Синтаксис Automation

        ```
        BOOL SetNextTouchOption( BOOL Forward );
        ```

        ### Входные параметры

        +-------------------------------------------------------------------------------------------+
        | Forward | - TRUE - установить следующий вариант, - FALSE - установить предыдущий вариант. |
        +-------------------------------------------------------------------------------------------+

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...




class ISplitLine(IModelObject):  # isplitline.html
    """
    ## Интерфейс ISplitLine

    Интерфейс линии разъема.

    1. Интерфейс можно получить у коллекции линий разъема, используя свойство ISplitLines::SplitLine или метод ISplitLines::Add.

    2. После задания параметров линии разъема требуется вызвать метод IModelObject::Update.
    """

    @property
    def CutObjects(self) -> typing.Any:  # isplitline_cutobjects.html
        """
        ## CutObjects - Секущие объекты

        Тип данных: VARIANT

        ### Синтаксис Automation

        ```
        CutObjects = Object.CutObjects      # Получить свойство (* )
        Object.CutObjects = CutObjects      # Установить свойство (* )
        CutObjects = Object.GetCutObjects   # Получить свойство (**)
        Object.SetCutObjects( CutObjects )  # Установить свойство (**)
        ```
        """
        ...
    @CutObjects.setter
    def CutObjects(self, value: typing.Any): ...

    @property
    def Direction(self) -> int:  # isplitline_direction.html
        """
        ## Direction - Направление формирования линии разъема

        Тип данных: из перечисления ksDirectionTypeEnum

        Свойство позволяет устанавливать и получать направление формирования линии разъема.

        ### Синтаксис Automation

        ```
        Direction = Object.Direction       # Получить свойство (* )
        Object.Direction = Direction       # Установить свойство (* )
        Direction = Object.GetDirection()  # Получить свойство (**)
        Object.SetDirection( Direction )   # Установить свойство (**)
        ```
        """
        ...
    @Direction.setter
    def Direction(self, value: int): ...

    @property
    def Faces(self) -> typing.Any:  # isplitline_faces.html
        """
        ## Faces - Массив граней в виде SAFEARRAY DISPATCH - VT_ARRAY | VT_DISPATCH

        Тип данных: VARIANT

        Свойство позволяет устанавливать и получать грани операции. Если возвращается один объект, то тип VARIANT-а будет VT_DISPATCH, если возвращается несколько объектов, то тип VARIANT-а будет VT_ARRAY | VT_DISPATCH.

        ### Синтаксис Automation

        ```
        Faces = Object.Faces       # Получить свойство (* )
        Object.Faces = Faces       # Установить свойство (* )
        Faces = Object.GetFaces()  # Получить свойство (**)
        Object.SetFaces( Faces )   # Установить свойство (**)
        ```
        """
        ...
    @Faces.setter
    def Faces(self, value: typing.Any): ...

    @property
    def Sketch(self) -> ISketch:  # isplitline_sketch.html
        """
        ## Sketch - Эскиз

        Тип данных: указатель на интерфейс эскиза ISketch

        Свойство позволяет устанавливать и получать эскиз линии разъема.

        ### Синтаксис Automation

        ```
        Sketch = iObject.Sketch();     # Получить свойство (* )
        iObject.Sketch() = Sketch;     # Установить свойство (* )
        Sketch = iObject.GetSketch();  # Получить свойство (**)
        iObject.SetSketch( );          # Установить свойство (**)
        ```
        """
        ...
    @Sketch.setter
    def Sketch(self, value: ISketch): ...




class ISurfacePatch(IModelObject):  # isurfacepatch.html
    """
    ## Интерфейс ISurfacePatch

    Интерфейс заплатки.

    1. Интерфейс можно получить у коллекции заплаток, используя свойство ISurfacePatches::SurfacePatch или метод ISurfacePatches::Add.

    2. После задания параметров заплатки требуется вызвать метод IModelObject::Update.
    """

    @property
    def Edges(self) -> typing.Any:  # isurfacepatch_edges.html
        """
        ## Edges - Массив ребер в виде SAFEARRAY DISPATCH - VT_ARRAY | VT_DISPATCH

        Тип данных: VARIANT

        Свойство позволяет устанавливать и получать ребра операции.

        ### Синтаксис Automation

        ```
        Edges = Object.Edges       # Получить свойство (* )
        Object.Edges = Edges       # Установить свойство (* )
        Edges = Object.GetEdges()  # Получить свойство (**)
        Object.SetEdges( Edges )   # Установить свойство (**)
        ```
        """
        ...
    @Edges.setter
    def Edges(self, value: typing.Any): ...




class ISurfaceSewer(IModelObject):  # isurfacesewer.html
    """
    ## Интерфейс ISurfaceSewer

    Интерфейс операции сшивки поверхностей.

    Интерфейс позволяет создавать и редактировать операцию сшивки поверхностей.

    1. Интерфейс можно получить у коллекции заплаток, используя свойство ISurfaceSewers::SurfaceSewer или метод ISurfaceSewers::Add.

    2. После задания параметров заплатки требуется вызвать метод IModelObject::Update.
    """

    @property
    def CreateBody(self) -> bool:  # isurfacesewer_createbody.html
        """
        ## CreateBody - Создавать тело

        Тип данных: BOOL

        Свойство позволяет устанавливать и получать флаг создания тела.

        ### Синтаксис Automation

        ```
        CreateBody = Object.CreateBody       # Получить свойство (* )
        Object.CreateBody = CreateBody       # Установить свойство (* )
        CreateBody = Object.GetCreateBody()  # Получить свойство (**)
        Object.SetCreateBody( CreateBody )   # Установить свойство (**)
        ```
        """
        ...
    @CreateBody.setter
    def CreateBody(self, value: bool): ...

    @property
    def Precision(self) -> float:  # isurfacesewer_precision.html
        """
        ## Precision - Точность сшивки

        Тип данных: double

        Свойство позволяет устанавливать и получать точность сшивки (максимальное расстояние между сшиваемыми ребрами).

        ### Синтаксис Automation

        ```
        Precision = Object.Precision       # Получить свойство (* )
        Object.Precision = Precision       # Установить свойство (* )
        Precision = Object.GetPrecision()  # Получить свойство (**)
        Object.SetPrecision( Precision )   # Установить свойство (**)
        ```
        """
        ...
    @Precision.setter
    def Precision(self, value: float): ...

    @property
    def Shells(self) -> typing.Any:  # isurfacesewer_shells.html
        """
        ## Shells - Массив оболочек в виде SAFEARRAY DISPATCH - VT_ARRAY | VT_DISPATCH

        Тип данных: VARIANT

        1. Свойство позволяет устанавливать и получать оболочки для выполнения операции.

        2. Свойство возвращает оболочки в виде интерфейсов IBody7.

        ### Синтаксис Automation

        ```
        Shells = Object.Shells       # Получить свойство (* )
        Object.Shells = Shells       # Установить свойство (* )
        Shells = Object.GetShells()  # Получить свойство (**)
        Object.SetShells( Shells )   # Установить свойство (**)
        ```
        """
        ...
    @Shells.setter
    def Shells(self, value: typing.Any): ...




class ITrimmedSurface(IModelObject):  # itrimmedsurface.html
    """
    ## Интерфейс ITrimmedSurface

    Интерфейс параметров операции усечения поверхности.
    """

    @property
    def CutObject(self) -> typing.Any:  # itrimmedsurface_cutobject.html
        """
        ## CutObject - Секущий объект для усечения поверхности

        Тип данных: VARIANT

        ### Синтаксис Automation

        ```
        CutObject = Object.CutObject       # Получить свойство (* )
        Object.CutObject = CutObject       # Установить свойство (**)
        CutObject = Object.GetCutObject()  # Получить свойство (**)
        CutObject = Object.GetCutObject()  # Установить свойство (**)
        ```
        """
        ...
    @CutObject.setter
    def CutObject(self, value: typing.Any): ...

    @property
    def OperationResult(self) -> int:  # itrimmedsurface_operationresult.html
        """
        ## OperationResult - Результат операции

        Тип данных: из перечисления ksOperationResultEnum

        ### Синтаксис Automation

        ```
        OperationResult = Object.OperationResult       # Получить свойство (* )
        Object.OperationResult = OperationResult       # Установить свойство (**)
        OperationResult = Object.GetOperationResult()  # Получить свойство (**)
        OperationResult = Object.GetOperationResult()  # Установить свойство (**)
        ```
        """
        ...
    @OperationResult.setter
    def OperationResult(self, value: int): ...

    @property
    def Sense(self) -> bool:  # itrimmedsurface_sense.html
        """
        ## Sense - Сменить направление усечения поверхности

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        Sense = Object.Sense       # Получить свойство (* )
        Object.Sense = Sense       # Установить свойство (**)
        Sense = Object.GetSense()  # Получить свойство (**)
        Object.SetSense( Sense )   # Установить свойство (**)
        ```
        """
        ...
    @Sense.setter
    def Sense(self, value: bool): ...

    @property
    def Surface(self) -> typing.Any:  # itrimmedsurface_surface.html
        """
        ## Surface - Усекаемая поверхность

        Тип данных: VARIANT

        ### Синтаксис Automation

        ```
        Surface = Object.Surface       # Получить свойство (* )
        Object.Surface = Surface       # Установить свойство (* )
        Surface = Object.GetSurface()  # Получить свойство (**)
        Object.SetSurface( Surface )   # Установить свойство (**)
        ```
        """
        ...
    @Surface.setter
    def Surface(self, value: typing.Any): ...




class IDismantleParameters(IDispatch):  # idismantleparameters.html
    """
    ## Интерфейс IDismantleParameters

    Интерфейс параметров разнесения сборки.

    Интерфейс может быть получен в качестве дополнительного для интерфейса IAssemblyDocument посредством вызова метода IUnknown::QueryInterface (const GUID far& IID, void** pif).

    Версия: Компас v20
    """

    @property
    def DismantleStepsCount(self) -> int:  # idismantleparameters_dismantlestepscount.html
        """
        ## DismantleStepsCount - Количество шагов разнесения

        Тип данных: long

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        DismantleStepsCount = Object.DismantleStepsCount       # Получить свойство (* )
        DismantleStepsCount = Object.GetDismantleStepsCount()  # Получить свойство (**)
        ```
        """
        ...

    def AddDismantleStep(self) -> IDismantleStep:  # idismantleparameters_adddismantlestep.html
        """
        ## AddDismantleStep - Добавить шаг разнесения

        ### Синтаксис Automation

        ```
        LPDISPATCH AddDismantleStep();
        ```

        ### Возвращаемое значение

        - Указатель на интерфейс IDismantleStep.
        """
        ...

    def ClearDismantleParameters(self) -> bool:  # idismantleparameters_cleardismantleparameters.html
        """
        ## ClearDismantleParameters - Удалить параметры разнесения

        ### Синтаксис Automation

        ```
        BOOL ClearDismantleParameters();
        ```

        ### Возвращаемое значение

        +-----------------------------+
        | TRUE  | - в случае успеха,  |
        +-----------------------------+
        | FALSE | - в случае неудачи. |
        +-----------------------------+
        """
        ...

    def DeleteDismantleStep(self, Index) -> bool:  # idismantleparameters_deletedismantlestep.html
        """
        ## DeleteDismantleStep - Удалить шаг разнесения

        ### Синтаксис Automation

        ```
        BOOL DeleteDismantleStep( long Index );
        ```

        ### Входные параметры

        +-----------------------------------+
        | Index | - Индекс шага разнесения. |
        +-----------------------------------+

        ### Возвращаемое значение

        +-----------------------------+
        | TRUE  | - в случае успеха,  |
        +-----------------------------+
        | FALSE | - в случае неудачи. |
        +-----------------------------+
        """
        ...

    def DismantleStep(self, Index) -> IDismantleStep:  # idismantleparameters_dismantlestep.html
        """
        ## DismantleStep - Параметры шага разнесения

        Тип данных: Указатель на интерфейс IDismantleStep.

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        DismantleStep = Object.DismantleStep( Index )     # Получить свойство (* )
        DismantleStep = Object.GetDismantleStep( Index )  # Получить свойство (**)
        ```

        ### Входные параметры

        +----------------------------------------+
        | long index | - Индекс шага в коллекции |
        +----------------------------------------+
        """
        ...




class IDismantleStep(IKompasAPIObject):  # idismantlestep.html
    """
    ## Интерфейс IDismantleStep

    Интерфейс Шаг разнесения сборки.
    """

    @property
    def DirectionObject(self) -> IModelObject:  # idismantlestep_directionobject.html
        """
        ## DirectionObject - Направляющий объект

        Тип данных: Указатель на интерфейс IModelObject.

        ### Синтаксис Automation

        ```
        DirectionObject = Object.DirectionObject       # Получить свойство(* )
        Object.DirectionObject = DirectionObject       # Установить свойство (* )
        DirectionObject = Object.GetDirectionObject()  # Получить свойство (**)
        Object.SetDirectionObject( DirectionObject )   # Установить свойство (**)
        ```
        """
        ...
    @DirectionObject.setter
    def DirectionObject(self, value: IModelObject): ...

    @property
    def Distance(self) -> float:  # idismantlestep_distance.html
        """
        ## Distance - Расстояние

        Тип данных: double

        ### Синтаксис Automation

        ```
        Distance = Object.Distance       # Получить свойство(* )
        Object.Distance = Distance       # Установить свойство (* )
        Distance = Object.GetDistance()  # Получить свойство (**)
        Object.SetDistance( Distance )   # Установить свойство (**)
        ```
        """
        ...
    @Distance.setter
    def Distance(self, value: float): ...

    @property
    def Parts(self) -> typing.Any:  # idismantlestep_parts.html
        """
        ## Parts - Список сдвигаемых вставок

        Тип данных: VARIANT

        Возвращается массив SafeArray VT_ARRAY | VT_DISPATCH граней IPart7.

        ### Синтаксис Automation

        ```
        Parts = Object.Parts       # Получить свойство(* )
        Object.Parts = Parts       # Установить свойство (* )
        Parts = Object.GetParts()  # Получить свойство (**)
        Object.SetParts( Parts )   # Установить свойство (**)
        ```
        """
        ...
    @Parts.setter
    def Parts(self, value: typing.Any): ...

    @property
    def Vector(self) -> IVector3D:  # idismantlestep_vector.html
        """
        ## Vector - Направляющий вектор

        Тип данных: Указатель на интерфейс IVector3D.

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        Vector = Object.Vector       # Получить свойство(* )
        Vector = Object.GetVector()  # Получить свойство (**)
        ```
        """
        ...

    def Update(self):
        ...




class ILoadCombination(IDispatch):  # iloadcombination.html
    """
    ## Интерфейс ILoadCombination

    Интерфейс для работы с типами загрузки.

    1. Интерфейс может быть получен в качестве дополнительного для интерфейса IAssemblyDocument.

    2. Интерфейс предназначен для работы с типами загрузки. Позволяет получить массив типов загрузки документа, текущий индекс типа загрузки, создать, удалить, применить тип загрузки.

    У сборки 3D есть три умолчательных типа загрузки, индексы которых соответствуют перечислению ksLoadStateEnum:

    +-------------------------------------------------------+
    | Полная     | 0 | - полная загрузка,                   |
    +-------------------------------------------------------+
    | Пустая     | 1 | - все компоненты не загружены,       |
    +-------------------------------------------------------+
    | Упрощенная | 2 | - все компоненты загружены частично. |
    +-------------------------------------------------------+

    Кроме этого могут быть еще и пользовательские типы загрузки.
    """

    @property
    def CompletelyLoaded(self) -> bool:  # iloadcombination_completelyloaded.html
        """
        ## CompletelyLoaded - Документ загружен полностью

        Тип данных: BOOL

        Значения свойства:

        +-------------------------------------------+
        | TRUE  | - документ загружен полностью,    |
        +-------------------------------------------+
        | FALSE | - документ загружен не полностью, |
        +-------------------------------------------+

        1. Свойство позволяет узнать загружен ли документ полностью.

        2. Если документ загружен полностью, можно получить массово-центровочные характеристики документа. В противном случае вернутся обнуленные данные.

        ### Синтаксис Automation

        ```
        CompletelyLoaded = iObject.CompletelyLoaded       # Получить свойство (* )
        CompletelyLoaded = iObject.GetCompletelyLoaded()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def CurrentIndex(self) -> int:  # iloadcombination_currentindex.html
        """
        ## CurrentIndex - Текущий индекс набора

        Тип данных: long

        1. Свойство позволяет получить текущий индекс набора, по которому загружается документ по умолчанию.

        2. Набор становится текущим после выполнения команды Применить.

        ### Синтаксис Automation

        ```
        CurrentIndex = iObject.CurrentIndex       # Получить свойство (* )
        CurrentIndex = iObject.GetCurrentIndex()  # Получить свойство (**)
        ```
        """
        ...
    @CurrentIndex.setter
    def CurrentIndex(self, value: int): ...

    @property
    def LoadCombinations(self) -> str:  # iloadcombination_loadcombinations.html
        """
        ## LoadCombinations - Массив наборов

        Тип данных: VARIANT - массив наборов в виде массива SAFEARRAY BSTR - (VT_ARRAY | VT_BSTR)

        1. Свойство позволяет получить массив наборов документа.

        2. У сборки 3D есть три умолчательных набора, индексы которых соответствуют перечислению ksLoadStateEnum.:

        +-------------------------------------------------------+
        | Полная     | 0 | - полная загрузка,                   |
        +-------------------------------------------------------+
        | Пустая     | 1 | - все компоненты не загружены,       |
        +-------------------------------------------------------+
        | Упрощенная | 2 | - все компоненты загружены частично. |
        +-------------------------------------------------------+

        Кроме этого могут быть еще и пользовательские наборы.

        ### Синтаксис Automation

        ```
        LoadCombinations = iObject.LoadCombinations       # Получить свойство (* )
        LoadCombinations = iObject.GetLoadCombinations()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def ProtectedFlags(self) -> typing.Any:  # iloadcombination_protectedflags.html
        """
        ## ProtectedFlags - Получить массив признаков защищенности типов загрузок в виде SAFEARRAY BOOL - (VT_ARRAY | VT_BOOL)

        Тип данных: VARIANT

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        ProtectedFlags = Object.ProtectedFlags       # Получить свойство (* )
        ProtectedFlags = Object.GetProtectedFlags()  # Получить свойство (**)
        ```
        """
        ...

    def Apply(self, LoadCombinationIndex) -> bool:  # iloadcombination_apply.html
        """
        ## Apply - Применить набор

        1. Метод позволяет применить набор с заданным именем или индексом.

        2. Набор становится текущим, что позволит открывать документ с таким набором по умолчанию.

        ### Синтаксис Automation

        ```
        BOOL Apply( VARIANT loadCombinationIndex )
        ```

        ### Входные параметры

        +----------------------------------------------------------------------+
        | loadCombinationIndex | - тип VARIANT - индекс набора или имя набора. |
        +----------------------------------------------------------------------+

        ### Возвращаемое значение

        +-----------------------------------+
        | TRUE  | - набор успешно применен, |
        +-----------------------------------+
        | FALSE | - не применен.            |
        +-----------------------------------+
        """
        ...

    def ApplyEx(self, LoadCombinationIndex, Password) -> bool:  # iloadcombination_applyex.html
        """
        ## ApplyEx - Применить тип загрузки

        ### Синтаксис Automation

        ```
        BOOL ApplyEx( VARIANT LoadCombinationIndex, BSTR Password );
        ```

        ### Входные параметры

        +------------------------------------------------+
        | LoadCombinationIndex | - индекс типа загрузки, |
        +------------------------------------------------+
        | Password             | - пароль.               |
        +------------------------------------------------+

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def Create(self, CombinationName):  # iloadcombination_create.html
        """
        ## Create - Создать набор

        1. Метод позволяет создать пользовательский набор с заданным именем.

        2. В набор сохраняется текущее состояние дерева построения. То есть предварительно нужно отдельно пройтись по вставкам компонентов и выполнить команды Выгрузить или Загрузить компонент.

        3. Набор не становится автоматически текущим.

        ### Синтаксис Automation

        ```
        long Create( LPCTSTR combinationName )
        ```

        ### Входные параметры

        +---------------------------------+
        | combinationName | - имя набора. |
        +---------------------------------+

        ### Возвращаемое значение

        +-------------------------------------------------------+
        | индекс набора в массиве наборов | - в случае успеха,  |
        +-------------------------------------------------------+
        | -1                              | - в случае неудачи. |
        +-------------------------------------------------------+
        """
        ...

    def Delete(self, LoadCombinationIndex) -> bool:  # iloadcombination_delete.html
        """
        ## Delete - Удалить набор

        Метод позволяет удалить пользовательский набор с заданным именем или индексом.

        ### Синтаксис Automation

        ```
        BOOL Delete( VARIANT loadCombinationIndex )
        ```

        ### Входные параметры

        +----------------------------------------------------------------------+
        | loadCombinationIndex | - тип VARIANT - индекс набора или имя набора. |
        +----------------------------------------------------------------------+

        ### Возвращаемое значение

        +----------------------+
        | TRUE  | - удален,    |
        +----------------------+
        | FALSE | - не удален. |
        +----------------------+
        """
        ...

    def DeleteEx(self, LoadCombinationIndex, Password, DeleteDependant) -> bool:  # iloadcombination_deleteex.html
        """
        ## DeleteEx - Удалить тип загрузки

        ### Синтаксис Automation

        ```
        BOOL DeleteEx( VARIANT LoadCombinationIndex,
        BSTR Password,
        BOOL DeleteDependant );
        ```

        ### Входные параметры

        +-----------------------------------------------------------------+
        | LoadCombinationIndex | - индекс типа загрузки,                  |
        +-----------------------------------------------------------------+
        | Password             | - пароль,                                |
        +-----------------------------------------------------------------+
        | DeleteDependant      | - TRUE - с подчиненными типами загрузки, |
        +-----------------------------------------------------------------+
        |                      | - FALSE - без подчиненных.               |
        +-----------------------------------------------------------------+

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def GetLoadCombinationComment(self, LoadCombinationIndex) -> str:  # iloadcombination_getloadcombinationcomment.html
        """
        ## GetLoadCombinationComment - Получить комментарий

        ### Синтаксис Automation

        ```
        BSTR GetLoadCombinationComment( VARIANT LoadCombinationIndex );
        ```

        ### Входные параметры

        +------------------------------------------------+
        | LoadCombinationIndex | - индекс типа загрузки. |
        +------------------------------------------------+

        ### Возвращаемое значение

        строка с комментарием
        """
        ...

    def GetLoadCombinationName(self, LoadCombinationIndex) -> str:  # iloadcombination_getloadcombinationname.html
        """
        ## GetLoadCombinationName - Получить имя

        ### Синтаксис Automation

        ```
        BSTR GetLoadCombinationName( VARIANT LoadCombinationIndex );
        ```

        ### Входные параметры

        +------------------------------------------------+
        | LoadCombinationIndex | - индекс типа загрузки. |
        +------------------------------------------------+

        ### Возвращаемое значение

        строка с именем
        """
        ...

    def SetLoadCombinationComment(self, LoadCombinationIndex, NewVal, Password) -> bool:  # iloadcombination_setloadcombinationcomment.html
        """
        ## SetLoadCombinationComment - Установить комментарий

        ### Синтаксис Automation

        ```
        BOOL SetLoadCombinationComment( VARIANT LoadCombinationIndex,
        BSTR NewVal,
        BSTR Password );
        ```

        ### Входные параметры

        +-------------------------------------------------+
        | LoadCombinationIndex | - индекс типа загрузки,  |
        +-------------------------------------------------+
        | NewVal               | - строка с комментарием, |
        +-------------------------------------------------+
        | Password             | - пароль.                |
        +-------------------------------------------------+

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def SetLoadCombinationName(self, LoadCombinationIndex, NewVal, Password) -> bool:  # iloadcombination_setloadcombinationname.html
        """
        ## SetLoadCombinationName - Установить имя

        ### Синтаксис Automation

        ```
        BOOL SetLoadCombinationName( VARIANT LoadCombinationIndex,
        BSTR NewVal,
        BSTR Password );
        ```

        ### Входные параметры

        +------------------------------------------------+
        | LoadCombinationIndex | - индекс типа загрузки, |
        +------------------------------------------------+
        | NewVal               | - строка с именем,      |
        +------------------------------------------------+
        | Password             | - пароль.               |
        +------------------------------------------------+

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def SetPassword(self, LoadCombinationIndex, OldPassword, Password, UnprotectUsers) -> bool:  # iloadcombination_setpassword.html
        """
        ## SetPassword - Установить пароль

        ### Синтаксис Automation

        ```
        BOOL SetPassword( VARIANT LoadCombinationIndex,
        BSTR OldPassword,
        BSTR Password,
        BOOL UnprotectUsers );
        ```

        ### Входные параметры

        +------------------------------------------------------------------------------------------------------------+
        | LoadCombinationIndex | - индекс типа загрузки,                                                             |
        +------------------------------------------------------------------------------------------------------------+
        | OldPassword          | - старый пароль,                                                                    |
        +------------------------------------------------------------------------------------------------------------+
        | Password             | - новый пароль,                                                                     |
        +------------------------------------------------------------------------------------------------------------+
        | UnprotectUsers       | - TRUE - при удалении пароля системного типа удалять пароли пользовательских типов. |
        +------------------------------------------------------------------------------------------------------------+

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def UpdateByModel(self, LoadCombinationIndex, Password) -> bool:  # iloadcombination_updatebymodel.html
        """
        ## UpdateByModel - Обновить по текущему состоянию модели

        ### Синтаксис Automation

        ```
        BOOL UpdateByModel( VARIANT LoadCombinationIndex, BSTR Password );
        ```

        ### Входные параметры

        +------------------------------------------------+
        | LoadCombinationIndex | - индекс типа загрузки, |
        +------------------------------------------------+
        | Password             | - пароль.               |
        +------------------------------------------------+

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...




class IOpenDocumentParam(IKompasAPIObject):  # iopendocumentparam.html
    """
    ## Интерфейс IOpenDocumentParam

    Интерфейс параметров открытия документа.

    Интерфейс можно получить с помощью метода IDocuments::GetOpenDocumentParam или IPart7::GetOpenDocumentParam.
    """

    @property
    def ApplyingIndex(self) -> int:  # iopendocumentparam_applyingindex.html
        """
        ## ApplyingIndex - Индекс примененного типа загрузки

        Тип данных: long

        ### Синтаксис Automation

        ```
        ApplyingIndex = Object.ApplyingIndex       # Получить свойство (* )
        Object.ApplyingIndex = ApplyingIndex       # Установить свойство (* )
        ApplyingIndex = Object.GetApplyingIndex()  # Получить свойство (**)
        Object.SetApplyingIndex( ApplyingIndex )   # Установить свойство (**)
        ```
        """
        ...
    @ApplyingIndex.setter
    def ApplyingIndex(self, value: int): ...

    @property
    def ReadOnly(self) -> bool:  # iopendocumentparam_readonly.html
        """
        ## ReadOnly - Открытие документа с присвоением признака «Для чтения»

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        ReadOnly = Object.ReadOnly       # Получить свойство (* )
        Object.ReadOnly = ReadOnly       # Установить свойство (* )
        ReadOnly = Object.GetReadOnly()  # Получить свойство (**)
        Object.SetReadOnly( ReadOnly )   # Установить свойство (**)
        ```
        """
        ...
    @ReadOnly.setter
    def ReadOnly(self, value: bool): ...

    @property
    def Visible(self) -> bool:  # iopendocumentparam_visible.html
        """
        ## Visible - Видимость документа

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        Visible = Object.Visible       # Получить свойство (* )
        Object.Visible = Visible       # Установить свойство (* )
        Visible = Object.GetVisible()  # Получить свойство (**)
        Object.SetVisible( Visible )   # Установить свойство (**)
        ```
        """
        ...
    @Visible.setter
    def Visible(self, value: bool): ...

    @property
    def Password(self) -> typing.NoReturn:  # iopendocumentparam_password.html
        """
        ## Password - Пароль типа загрузки

        Тип данных: BSTR

        ### Синтаксис Automation

        ```
        Object.Password = Password      # Получить свойство (* )
        Object.SetPassword( Password )  # Получить свойство (**)
        ```
        """
        ...
    @Password.setter
    def Password(self, value: str): ...




class ILoadCombinationsParam(IKompasAPIObject):  # iloadcombinationsparam.html
    """
    ## Интерфейс ILoadCombinationsParam

    Интерфейс параметров типа загрузки документа.

    Интерфейс можно получить с помощью метода IDocuments::GetLoadCombinationsParam.
    """

    @property
    def ApplyingIndex(self) -> int:  # iloadcombinationsparam_applyingindex.html
        """
        ## ApplyingIndex - Получить индекс примененного типа загрузки

        Тип данных: long

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        ApplyingIndex = Object.ApplyingIndex       # Получить свойство (* )
        ApplyingIndex = Object.GetApplyingIndex()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def LoadCombinations(self) -> typing.Any:  # iloadcombinationsparam_loadcombinations.html
        """
        ## LoadCombinations - Получить массив типов загрузок в виде SAFEARRAY BSTR(VT_ARRAY | VT_BSTR)

        Тип данных: VARIANT

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        LoadCombinations = Object.LoadCombinations       # Получить свойство (* )
        LoadCombinations = Object.GetLoadCombinations()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def ProtectedFlags(self) -> typing.Any:  # iloadcombinationsparam_protectedflags.html
        """
        ## ProtectedFlags - Получить массив признаков защищенности типов загрузок в виде SAFEARRAY BOOL(VT_ARRAY | VT_BOOL)

        Тип данных: VARIANT

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        ProtectedFlags = Object.ProtectedFlags       # Получить свойство (* )
        ProtectedFlags = Object.GetProtectedFlags()  # Получить свойство (**)
        ```
        """
        ...




class IVector3D(IKompasAPIObject):  # ivector3d.html
    """
    ## Интерфейс IVector3D

    Интерфейс параметров вектора 3D.
    """

    @property
    def Parameters(self) -> IKompasAPIObject:  # ivector3d_parameters.html
        """
        ## Parameters - Получить интерфейс параметров построения вектора

        Тип данных: указатель на интерфейс IKompasAPIObject

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        Parameters = Object.Parameters       # Получить свойство (* )
        Parameters = Object.GetParameters()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def ParametersType(self) -> int:  # ivector3d_parameterstype.html
        """
        ## ParametersType - Тип параметров вектора 3D

        Тип данных: из перечисления ksVector3DParametersTypeEnum

        ### Синтаксис Automation

        ```
        ParametersType = Object.ParametersType       # Получить свойство (* )
        Object.ParametersType = ParametersType       # Установить свойство (* )
        ParametersType = Object.GetParametersType()  # Получить свойство (**)
        Object.SetParametersType( ParametersType )   # Установить свойство (**)
        ```
        """
        ...
    @ParametersType.setter
    def ParametersType(self, value: int): ...




class IVector3DAlongSurfaceNormalParameters(IKompasAPIObject):  # ivector3dalongsurfacenormalparameters.html
    """
    ## Интерфейс IVector3DAlongSurfaceNormalParameters

    Интерфейс параметров вектора, перпендикулярного грани в указанной точке.
    """

    @property
    def BaseObject(self) -> IModelObject:  # ivector3dalongsurfacenormalparameters_baseobject.html
        """
        ## BaseObject - Объект для построения вектора

        Тип данных: указатель на интерфейс IModelObject

        ### Синтаксис Automation

        ```
        BaseObject = Object.BaseObject       # Получить свойство (* )
        Object.BaseObject = BaseObject       # Установить свойство (* )
        BaseObject = Object.GetBaseObject()  # Получить свойство (**)
        Object.SetBaseObject( BaseObject )   # Установить свойство (**)
        ```
        """
        ...
    @BaseObject.setter
    def BaseObject(self, value: IModelObject): ...

    @property
    def Direction(self) -> bool:  # ivector3dalongsurfacenormalparameters_direction.html
        """
        ## Direction - Направление вектора

        Тип данных: BOOL

        Значения свойства:

        +---------------------------------+
        | TRUE  | - прямое направление,   |
        +---------------------------------+
        | FALSE | - обратное направление. |
        +---------------------------------+

        Свойство позволяет устанавливать и получать направление вектора.

        ### Синтаксис Automation

        ```
        Direction = Object.Direction       # Получить свойство (* )
        Object.Direction = Direction       # Установить свойство (* )
        Direction = Object.GetDirection()  # Получить свойство (**)
        Object.SetDirection( Direction )   # Установить свойство (**)
        ```
        """
        ...
    @Direction.setter
    def Direction(self, value: bool): ...

    @property
    def U(self) -> float:  # ivector3dalongsurfacenormalparameters_u.html
        """
        ## U - Параметр U

        Тип данных: double

        ### Синтаксис Automation

        ```
        U = Object.U       # Получить свойство (* )
        Object.U = U       # Установить свойство (* )
        U = Object.GetU()  # Получить свойство (**)
        Object.SetU( U )   # Установить свойство (**)
        ```
        """
        ...
    @U.setter
    def U(self, value: float): ...

    @property
    def V(self) -> float:  # ivector3dalongsurfacenormalparameters_v.html
        """
        ## V - Параметр V

        Тип данных: double

        ### Синтаксис Automation

        ```
        V = Object.V       # Получить свойство (* )
        Object.V = V       # Установить свойство (* )
        V = Object.GetV()  # Получить свойство (**)
        Object.SetV( V )   # Установить свойство (**)
        ```
        """
        ...
    @V.setter
    def V(self, value: float): ...




class IVector3DBy2AnglesParameters(IKompasAPIObject):  # ivector3dby2anglesparameters.html
    """
    ## Интерфейс IVector3DBy2AnglesParameters

    Интерфейс параметров вектора по двум углам.
    """

    @property
    def AngleA(self) -> float:  # ivector3dby2anglesparameters_anglea.html
        """
        ## AngleA - Азимутальный угол

        Тип данных: double

        ### Синтаксис Automation

        ```
        AngleA = Object.AngleA       # Получить свойство (* )
        Object.AngleA = AngleA       # Установить свойство (* )
        AngleA = Object.GetAngleA()  # Получить свойство (**)
        Object.SetAngleA( AngleA )   # Установить свойство (**)
        ```
        """
        ...
    @AngleA.setter
    def AngleA(self, value: float): ...

    @property
    def AngleB(self) -> float:  # ivector3dby2anglesparameters_angleb.html
        """
        ## AngleB - Зенитный угол

        Тип данных: double

        ### Синтаксис Automation

        ```
        AngleB = Object.AngleB       # Получить свойство (* )
        Object.AngleB = AngleB       # Установить свойство (* )
        AngleB = Object.GetAngleB()  # Получить свойство (**)
        Object.SetAngleB( AngleB )   # Установить свойство (**)
        ```
        """
        ...
    @AngleB.setter
    def AngleB(self, value: float): ...

    @property
    def LocalCS(self) -> ILocalCoordinateSystem:  # ivector3dby2anglesparameters_localcs.html
        """
        ## LocalCS - Локальная система координат

        Тип данных: указатель на интерфейс ILocalCoordinateSystem

        Чтобы установить глобальную СК, нужно передать NULL.

        ### Синтаксис Automation

        ```
        LocalCS = Object.LocalCS       # Получить свойство (* )
        Object.LocalCS = LocalCS       # Установить свойство (* )
        LocalCS = Object.GetLocalCS()  # Получить свойство (**)
        Object.SetLocalCS( LocalCS )   # Установить свойство (**)
        ```
        """
        ...
    @LocalCS.setter
    def LocalCS(self, value: ILocalCoordinateSystem): ...




class IVector3DBy2VertexesParameters(IKompasAPIObject):  # ivector3dby2vertexesparameters.html
    """
    ## Интерфейс IVector3DBy2VertexesParameters

    Интерфейс параметров вектора 3D по двум вершинам.
    """

    @property
    def Direction(self) -> bool:  # ivector3dby2vertexesparameters_direction.html
        """
        ## Direction - Направление вектора 3D

        Тип данных: BOOL

        Значения свойства:

        +---------------------------------+
        | TRUE  | - прямое направление,   |
        +---------------------------------+
        | FALSE | - обратное направление. |
        +---------------------------------+

        Свойство позволяет устанавливать и получать направление вектора.

        ### Синтаксис Automation

        ```
        Direction = Object.Direction       # Получить свойство (* )
        Object.Direction = Direction       # Установить свойство (* )
        Direction = Object.GetDirection()  # Получить свойство (**)
        Object.SetDirection( Direction )   # Установить свойство (**)
        ```
        """
        ...
    @Direction.setter
    def Direction(self, value: bool): ...

    @property
    def Vertex1(self) -> IModelObject:  # ivector3dby2vertexesparameters_vertex1.html
        """
        ## Vertex1 - Первая вершина вектора 3D

        Тип данных: указатель на интерфейс IModelObject

        ### Синтаксис Automation

        ```
        Vertex1 = Object.Vertex1       # Получить свойство (* )
        Object.Vertex1 = Vertex1       # Установить свойство (* )
        Vertex1 = Object.GetVertex1()  # Получить свойство (**)
        Object.SetVertex1( Vertex1 )   # Установить свойство (**)
        ```
        """
        ...
    @Vertex1.setter
    def Vertex1(self, value: IModelObject): ...

    @property
    def Vertex2(self) -> IModelObject:  # ivector3dby2vertexesparameters_vertex2.html
        """
        ## Vertex2 - Вторая вершина вектора 3D

        Тип данных: указатель на интерфейс IModelObject

        ### Синтаксис Automation

        ```
        Vertex2 = Object.Vertex2       # Получить свойство (* )
        Object.Vertex2 = Vertex2       # Установить свойство (* )
        Vertex2 = Object.GetVertex2()  # Получить свойство (**)
        Object.SetVertex2( Vertex2 )   # Установить свойство (**)
        ```
        """
        ...
    @Vertex2.setter
    def Vertex2(self, value: IModelObject): ...




class IVector3DByCoefficientsParameters(IKompasAPIObject):  # ivector3dbycoefficientsparameters.html
    """
    ## Интерфейс IVector3DByCoefficientsParameters

    Интерфейс параметров вектора по коэффициентам.
    """

    @property
    def CoefficientByX(self) -> float:  # ivector3dbycoefficientsparameters_coefficientbyx.html
        """
        ## CoefficientByX - Коэффициент разложения по оси X

        Тип данных: double

        ### Синтаксис Automation

        ```
        CoefficientByX = Object.CoefficientByX       # Получить свойство (* )
        Object.CoefficientByX = CoefficientByX       # Установить свойство (* )
        CoefficientByX = Object.GetCoefficientByX()  # Получить свойство (**)
        Object.SetCoefficientByX( CoefficientByX )   # Установить свойство (**)
        ```
        """
        ...
    @CoefficientByX.setter
    def CoefficientByX(self, value: float): ...

    @property
    def CoefficientByY(self) -> float:  # ivector3dbycoefficientsparameters_coefficientbyy.html
        """
        ## CoefficientByY - Коэффициент разложения по оси Y

        Тип данных: double

        ### Синтаксис Automation

        ```
        CoefficientByY = Object.CoefficientByY       # Получить свойство (* )
        Object.CoefficientByY = CoefficientByY       # Установить свойство (* )
        CoefficientByY = Object.GetCoefficientByY()  # Получить свойство (**)
        Object.SetCoefficientByY( CoefficientByY )   # Установить свойство (**)
        ```
        """
        ...
    @CoefficientByY.setter
    def CoefficientByY(self, value: float): ...

    @property
    def CoefficientByZ(self) -> float:  # ivector3dbycoefficientsparameters_coefficientbyz.html
        """
        ## CoefficientByZ - Коэффициент разложения по оси Z

        Тип данных: double

        ### Синтаксис Automation

        ```
        CoefficientByZ = Object.CoefficientByZ       # Получить свойство (* )
        Object.CoefficientByZ = CoefficientByZ       # Установить свойство (* )
        CoefficientByZ = Object.GetCoefficientByZ()  # Получить свойство (**)
        Object.SetCoefficientByZ( CoefficientByZ )   # Установить свойство (**)
        ```
        """
        ...
    @CoefficientByZ.setter
    def CoefficientByZ(self, value: float): ...

    @property
    def LocalCS(self) -> ILocalCoordinateSystem:  # ivector3dbycoefficientsparameters_localcs.html
        """
        ## LocalCS - Локальная система координат

        Тип данных: указатель на интерфейс ILocalCoordinateSystem

        Чтобы установить глобальную СК, нужно передать NULL.

        ### Синтаксис Automation

        ```
        LocalCS = Object.LocalCS       # Получить свойство (* )
        Object.LocalCS = LocalCS       # Установить свойство (* )
        LocalCS = Object.GetLocalCS()  # Получить свойство (**)
        Object.SetLocalCS( LocalCS )   # Установить свойство (**)
        ```
        """
        ...
    @LocalCS.setter
    def LocalCS(self, value: ILocalCoordinateSystem): ...




class IVector3DByCurveParameters(IKompasAPIObject):  # ivector3dbycurveparameters.html
    """
    ## Интерфейс IVector3DByCurveParameters

    Интерфейс параметров вектора по базисному вектору в точке кривой.
    """

    @property
    def Curve(self) -> IModelObject:  # ivector3dbycurveparameters_curve.html
        """
        ## Curve - Кривая

        Тип данных: указатель на интерфейс IModelObject

        ### Синтаксис Automation

        ```
        Curve = Object.Curve       # Получить свойство (* )
        Object.Curve = Curve       # Установить свойство (* )
        Curve = Object.GetCurve()  # Получить свойство (**)
        Object.SetCurve( Curve )   # Установить свойство (**)
        ```
        """
        ...
    @Curve.setter
    def Curve(self, value: IModelObject): ...

    @property
    def Direction(self) -> bool:  # ivector3dbycurveparameters_direction.html
        """
        ## Direction - Направление вектора

        Тип данных: BOOL

        Значения свойства:

        +--------------------------------+
        | TRUE  | - прямое направление   |
        +--------------------------------+
        | FALSE | - обратное направление |
        +--------------------------------+

        Свойство позволяет устанавливать и получать направление вектора.

        ### Синтаксис Automation

        ```
        Direction = Object.Direction       # Получить свойство (* )
        Object.Direction = Direction       # Установить свойство (* )
        Direction = Object.GetDirection()  # Получить свойство (**)
        Object.SetDirection( Direction )   # Установить свойство (**)
        ```
        """
        ...
    @Direction.setter
    def Direction(self, value: bool): ...

    @property
    def Offset(self) -> float:  # ivector3dbycurveparameters_offset.html
        """
        ## Offset - Смещение

        Тип данных: double

        ### Синтаксис Automation

        ```
        Offset = Object.Offset       # Получить свойство (* )
        Object.Offset = Offset       # Установить свойство (* )
        Offset = Object.GetOffset()  # Получить свойство (**)
        Object.SetOffset( Offset )   # Установить свойство (**)
        ```
        """
        ...
    @Offset.setter
    def Offset(self, value: float): ...

    @property
    def VectorType(self) -> int:  # ivector3dbycurveparameters_vectortype.html
        """
        ## VectorType - Тип базисного вектора

        Тип данных: из перечисления ksBasisVectorTypeEnum

        ### Синтаксис Automation

        ```
        VectorType = Object.VectorType       # Получить свойство (* )
        Object.VectorType = VectorType       # Установить свойство (* )
        VectorType = Object.GetVectorType()  # Получить свойство (**)
        Object.SetVectorType( VectorType )   # Установить свойство (**)
        ```
        """
        ...
    @VectorType.setter
    def VectorType(self, value: int): ...




class IVector3DByLocalCSParameters(IKompasAPIObject):  # ivector3dbylocalcsparameters.html
    """
    ## Интерфейс IVector3DByLocalCSParameters

    Интерфейс параметров вектора по углу в плоскости СК и по оси СК.
    """

    @property
    def Angle(self) -> float:  # ivector3dbylocalcsparameters_angle.html
        """
        ## Angle - Угол наклона вектора

        Тип данных: double

        Свойство доступно для типа параметров вектора ksVector3DAngle.

        ### Синтаксис Automation

        ```
        Angle = Object.Angle       # Получить свойство (* )
        Object.Angle = Angle       # Установить свойство (* )
        Angle = Object.GetAngle()  # Получить свойство (**)
        Object.SetAngle( Angle )   # Установить свойство (**)
        ```
        """
        ...
    @Angle.setter
    def Angle(self, value: float): ...

    @property
    def AxisType(self) -> int:  # ivector3dbylocalcsparameters_axistype.html
        """
        ## AxisType - Тип оси

        Тип данных: из перечисления ksObj3dTypeEnum

        Тип оси также определяет плоскость размещения вектора.

        ### Синтаксис Automation

        ```
        AxisType = Object.AxisType       # Получить свойство (* )
        Object.AxisType = AxisType       # Установить свойство (* )
        AxisType = Object.GetAxisType()  # Получить свойство (**)
        Object.SetAxisType( AxisType )   # Установить свойство (**)
        ```
        """
        ...
    @AxisType.setter
    def AxisType(self, value: int): ...

    @property
    def Direction(self) -> bool:  # ivector3dbylocalcsparameters_direction.html
        """
        ## Direction - Направление вектора

        Тип данных: BOOL

        Значения свойства:

        +---------------------------------+
        | TRUE  | - прямое направление,   |
        +---------------------------------+
        | FALSE | - обратное направление. |
        +---------------------------------+

        Свойство позволяет устанавливать и получать направление вектора.

        Свойство доступно для типа параметров вектора ksVector3DAxis.

        ### Синтаксис Automation

        ```
        Direction = Object.Direction       # Получить свойство (* )
        Object.Direction = Direction       # Установить свойство (* )
        Direction = Object.GetDirection()  # Получить свойство (**)
        Object.SetDirection( Direction )   # Установить свойство (**)
        ```
        """
        ...
    @Direction.setter
    def Direction(self, value: bool): ...

    @property
    def LocalCS(self) -> ILocalCoordinateSystem:  # ivector3dbylocalcsparameters_localcs.html
        """
        ## LocalCS - Локальная система координат

        Тип данных: указатель на интерфейс ILocalCoordinateSystem

        Чтобы установить глобальную СК, нужно передать NULL.

        ### Синтаксис Automation

        ```
        LocalCS = Object.LocalCS       # Получить свойство (* )
        Object.LocalCS = LocalCS       # Установить свойство (* )
        LocalCS = Object.GetLocalCS()  # Получить свойство (**)
        Object.SetLocalCS( LocalCS )   # Установить свойство (**)
        ```
        """
        ...
    @LocalCS.setter
    def LocalCS(self, value: ILocalCoordinateSystem): ...




class IVector3DByObjectParameters(IKompasAPIObject):  # ivector3dbyobjectparameters.html
    """
    ## Интерфейс IVector3DByObjectParameters

    Интерфейс параметров вектора по ребру или плоскости.
    """

    @property
    def BaseObject(self) -> IModelObject:  # ivector3dbyobjectparameters_baseobject.html
        """
        ## BaseObject - Объект для построения вектора

        Тип данных: указатель на интерфейс IModelObject

        ### Синтаксис Automation

        ```
        BaseObject = Object.BaseObject       # Получить свойство (* )
        Object.BaseObject = BaseObject       # Установить свойство (* )
        BaseObject = Object.GetBaseObject()  # Получить свойство (**)
        Object.SetBaseObject( BaseObject )   # Установить свойство (**)
        ```
        """
        ...
    @BaseObject.setter
    def BaseObject(self, value: IModelObject): ...

    @property
    def Direction(self) -> bool:  # ivector3dbyobjectparameters_direction.html
        """
        ## Direction - Направление вектора

        Тип данных: BOOL

        Значения свойства:

        +---------------------------------+
        | TRUE  | - прямое направление,   |
        +---------------------------------+
        | FALSE | - обратное направление. |
        +---------------------------------+

        Свойство позволяет устанавливать и получать направление вектора.

        ### Синтаксис Automation

        ```
        Direction = Object.Direction       # Получить свойство (* )
        Object.Direction = Direction       # Установить свойство (* )
        Direction = Object.GetDirection()  # Получить свойство (**)
        Object.SetDirection( Direction )   # Установить свойство (**)
        ```
        """
        ...
    @Direction.setter
    def Direction(self, value: bool): ...




class IVector3DByScreenNormalParameters(IKompasAPIObject):  # ivector3dbyscreennormalparameters.html
    """
    ## Интерфейс IVector3DByScreenNormalParameters

    Интерфейс параметров вектора, перпендикулярного плоскости экрана.
    """

    @property
    def Direction(self) -> bool:  # ivector3dbyscreennormalparameters_direction.html
        """
        ## Direction - Направление вектора

        Тип данных: BOOL

        Значения свойства:

        +-------------------------------+
        | TRUE  | Прямое направление,   |
        +-------------------------------+
        | FALSE | Обратное направление. |
        +-------------------------------+

        Свойство позволяет устанавливать и получать направление вектора.

        ### Синтаксис Automation

        ```
        Direction = Object.Direction       # Получить свойство (* )
        Object.Direction = Direction       # Установить свойство (* )
        Direction = Object.GetDirection()  # Получить свойство (**)
        Object.SetDirection( Direction )   # Установить свойство (**)
        ```
        """
        ...
    @Direction.setter
    def Direction(self, value: bool): ...

    @property
    def Fix(self) -> bool:  # ivector3dbyscreennormalparameters_fix.html
        """
        ## Fix - Фиксация вектора

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        Fix = Object.Fix       # Получить свойство (* )
        Object.Fix = Fix       # Установить свойство (* )
        Fix = Object.GetFix()  # Получить свойство (**)
        Object.SetFix( Fix )   # Установить свойство (**)
        ```
        """
        ...
    @Fix.setter
    def Fix(self, value: bool): ...




class IBody7(IKompasAPIObject):  # ibody7.html
    """
    ## Интерфейс IBody7

    Интерфейс твердого тела.

    Позволяет получить интерфейс твердого тела. Тело является результатом выполнения операций (например, операции выдавливания). Представляет собой область, ограниченную замкнутой поверхностью.

    1. Интерфейс можно получить у интерфейса дерева с помощью метода IFeature7::ResultBodies.

    2. Интерфейс можно получить у интерфейса области применения для тел документа в операции с помощью метода IChooseBodies7::Bodies.

    3. Имеет дополнительные интерфейсы IMassInertiaParam7, IColorParam7, IFeature7, IPropertyKeeper, которые можно получить с помощью метода IUnknown::QueryInterface.
    """

    @property
    def BeginBodyId(self) -> int:  # ibody7_beginbodyid.html
        """
        ## BeginBodyId - Идентификатор начального тела

        Тип данных: long

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        BeginBodyId = Object. BeginBodyId      # Получить свойство (* )
        BeginBodyId = Object.GetBeginBodyId()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def BodyId(self) -> int:  # ibody7_bodyid.html
        """
        ## BodyId - Идентификатор тела

        Тип данных: long

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        BodyId = Object. BodyId      # Получить свойство (* )
        BodyId = Object.GetBodyId()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def CreateSpcObjects(self) -> bool:  # ibody7_createspcobjects.html
        """
        ## CreateSpcObjects - Создавать объекты спецификации

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        CreateSpcObjects = Object.CreateSpcObjects       # Получить свойство (* )
        Object.CreateSpcObjects = CreateSpcObjects       # Установить свойство (* )
        CreateSpcObjects = Object.GetCreateSpcObjects()  # Получить свойство (**)
        Object.SetCreateSpcObjects( CreateSpcObjects )   # Установить свойство (**)
        ```
        """
        ...
    @CreateSpcObjects.setter
    def CreateSpcObjects(self, value: bool): ...

    @property
    def Editable(self) -> int:  # ibody7_editable.html
        """
        ## Editable - Признак редактирования

        Тип данных: из перечисления ksEditableStateEnum

        ### Синтаксис Automation

        ```
        Editable = Object.Editable       # Получить свойство (* )
        Object.Editable = Editable       # Установить свойство (* )
        Editable = Object.GetEditable()  # Получить свойство (**)
        Object.SetEditable( Editable )   # Установить свойство (**)
        ```
        """
        ...
    @Editable.setter
    def Editable(self, value: int): ...

    @property
    def FinalBodyId(self) -> int:  # ibody7_finalbodyid.html
        """
        ## FinalBodyId - Идентификатор конечного тела

        Тип данных: long

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        FinalBodyId = Object. FinalBodyId      # Получить свойство (* )
        FinalBodyId = Object.GetFinalBodyId()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def HatchParam(self) -> IHatchParam:  # ibody7_hatchparam.html
        """
        ## HatchParam - Параметры штриховки

        Тип данных: Указатель на интерфейс IHatchParam

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        HatchParam = Object.HatchParam       # Получить свойство (* )
        HatchParam = Object.GetHatchParam()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def Hidden(self) -> bool:  # ibody7_hidden.html
        """
        ## Hidden - Состояние видимости объекта

        Тип данных: BOOL

        Свойство позволяет устанавливать и получать обозначение компонента.

        ### Синтаксис Automation

        ```
        Hidden = Object.Hidden       # Получить свойство (* )
        Object.Hidden = Hidden       # Установить свойство (* )
        Hidden = Object.GetHidden()  # Получить свойство (**)
        Object.SetHidden( Hidden )   # Установить свойство (**)
        ```
        """
        ...
    @Hidden.setter
    def Hidden(self, value: bool): ...

    @property
    def HiddenEx(self) -> int:  # ibody7_hiddenex.html
        """
        ## HiddenEx - Состояние видимости объекта

        Тип данных: из перечисления ksVisibleStateEnum

        ### Синтаксис Automation

        ```
        HiddenEx = Object.HiddenEx       # Получить свойство (* )
        Object.HiddenEx = HiddenEx       # Установить свойство (* )
        HiddenEx = Object.GetHiddenEx()  # Получить свойство (**)
        Object.SetHiddenEx( HiddenEx )   # Установить свойство (**)
        ```
        """
        ...
    @HiddenEx.setter
    def HiddenEx(self, value: int): ...

    @property
    def LayerNumber(self) -> int:  # ibody7_layernumber.html
        """
        ## LayerNumber - Номер слоя

        Тип данных: long

        ### Синтаксис Automation

        ```
        LayerNumber = Object.LayerNumber       # Получить свойство (* )
        Object.LayerNumber = LayerNumber       # Установить свойство (* )
        LayerNumber = Object.GetLayerNumber()  # Получить свойство (**)
        Object.SetLayerNumber( LayerNumber )   # Установить свойство (**)
        ```
        """
        ...
    @LayerNumber.setter
    def LayerNumber(self, value: int): ...

    @property
    def Marking(self) -> str:  # ibody7_marking.html
        """
        ## Marking - Обозначение компонента

        Тип данных: строка

        Свойство позволяет устанавливать и получать обозначение компонента.

        ### Синтаксис Automation

        ```
        Marking = iObject.Marking       # Получить свойство (* )
        iObject.Marking = Marking       # Установить свойство (* )
        Marking = iObject.GetMarking()  # Получить свойство (**)
        iObject.SetMarking ( Marking)   # Установить свойство (**)
        ```
        """
        ...
    @Marking.setter
    def Marking(self, value: str): ...

    @property
    def Name(self) -> str:  # ibody7_name.html
        """
        ## Name - Имя тела

        Тип данных: BSTR (строка)

        Свойство позволяет устанавливать и получать имя трехмерной модели.

        ### Синтаксис Automation

        ```
        Name = iObject.Name       # Получить свойство (* )
        iObject.Name = Name       # Установить свойство (* )
        Name = iObject.GetName()  # Получить свойство (**)
        iObject.SetName (Name)    # Установить свойство (**)
        ```
        """
        ...
    @Name.setter
    def Name(self, value: str): ...

    @property
    def OwnerBodyId(self) -> int:  # ibody7_ownerbodyid.html
        """
        ## OwnerBodyId - Идентификатор тела, которое поглотило это тело

        Тип данных: long

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        OwnerBodyId = Object. OwnerBodyId      # Получить свойство (* )
        OwnerBodyId = Object.GetOwnerBodyId()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def Projected(self) -> int:  # ibody7_projected.html
        """
        ## Projected - Признак проецирования

        Тип данных: из перечисления ksProjectionOptionEnum

        ### Синтаксис Automation

        ```
        Projected = Object.Projected       # Получить свойство (* )
        Object.Projected = Projected       # Установить свойство (* )
        Projected = Object.GetProjected()  # Получить свойство (**)
        Object.SetProjected( Projected )   # Установить свойство (**)
        ```
        """
        ...
    @Projected.setter
    def Projected(self, value: int): ...

    def GetGabarit(self) -> bool:  # ibody7_getgabarit.html
        """
        ## GetGabarit - Получить габарит

        Метод позволяет установить габаритный прямоугольник Панели свойств. Точки задаются в относительных координатах окна КОМПАС.

        ### Синтаксис Automation

        ```
        BOOL GetGabarit( double * X1,
        double * Y1,
        double * Z1,
        double * X2,
        double * Y2,
        double * Z2 );
        ```

        ### Входные параметры

        +-------------------------------------------------------------------+
        | X1, Y1, Z1, X2, Y2, Z2 | - координаты габаритного прямоугольника. |
        +-------------------------------------------------------------------+

        ### Возвращаемое значение

        +--------------------------------+
        | TRUE  | - успешное завершение, |
        +--------------------------------+
        | FALSE | - в случае неудачи.    |
        +--------------------------------+
        """
        ...

    def Update(self) -> bool:  # ibody7_update.html
        """
        ## Update - Изменить свойства объекта

        После вызова метода вступают в силу измененные свойства тела: имя, обозначение, свойства массово-центровочных характеристик.

        ### Синтаксис Automation

        ```
        BOOL Update( );
        ```

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def UserParameters(self, Val):  # ibody7_userparameters.html
        """
        ## UserParameters - Получить интерфейс параметров

        Тип данных: Указатель на интерфейс IUnknown

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        UserParameters = Object.UserParameters( Val )     # Получить свойство(* )
        UserParameters = Object.GetUserParameters( Val )  # Получить свойство (**)
        ```
        """
        ...




class IColorParam7(IDispatch):  # icolorparam7.html
    """
    ## Интерфейс IColorParam7

    Свойства цвета объекта.

    1. Интерфейс является дополнительным для IModelObject, IBody7IEmbodiment.

    2. Оптические свойства используются для верхнего компонента, вставки, тела, операций, поверхностей, грани. Для остальных объектов используется только цвет и свойство UseColor.

    3. Оптические свойства задаются в относительных единицах 0...1.
    """

    @property
    def Ambient(self) -> float:  # icolorparam7_ambient.html
        """
        ## Ambient - Общий свет

        Тип данных: double

        Значения свойства:

        - Общий свет (от 0 до 1).

        ### Синтаксис Automation

        ```
        Ambient = Object.Ambient       # Получить свойство (* )
        Object.Ambient = Ambient       # Установить свойство (* )
        Ambient = Object.GetAmbient()  # Получить свойство (**)
        Object.SetAmbient (Ambient)    # Установить свойство (**)
        ```
        """
        ...
    @Ambient.setter
    def Ambient(self, value: float): ...

    @property
    def Color(self) -> int:  # icolorparam7_color.html
        """
        ## Color - Цвет

        Тип данных: long

        ### Синтаксис Automation

        ```
        Color = Object.Color       # Получить свойство (* )
        Object.Color = Color       # Установить свойство (* )
        Color = Object.GetColor()  # Получить свойство (**)
        Object.SetColor( Color )   # Установить свойство (**)
        ```
        """
        ...
    @Color.setter
    def Color(self, value: int): ...

    @property
    def Diffuse(self) -> float:  # icolorparam7_diffuse.html
        """
        ## Diffuse - Диффузия

        Тип данных: double

        Значения свойства:

        - Диффузия (от 0 до 1).

        ### Синтаксис Automation

        ```
        Diffuse = Object.diffuse       # Получить свойство (* )
        Object.diffuse = diffuse       # Установить свойство (* )
        Diffuse = Object.GetDiffuse()  # Получить свойство (**)
        Object.SetDiffuse (diffuse)    # Установить свойство (**)
        ```
        """
        ...
    @Diffuse.setter
    def Diffuse(self, value: float): ...

    @property
    def Emission(self) -> float:  # icolorparam7_emission.html
        """
        ## Emission - Излучение

        Тип данных: double

        Значения свойства:

        - Излучение (от 0 до 1).

        ### Синтаксис Automation

        ```
        Emission = Object.Emission       # Получить свойство (* )
        Object.Emission = Emission       # Установить свойство (* )
        Emission = Object.GetEmission()  # Получить свойство (**)
        Object.SetEmission(Emission)     # Установить свойство (**)
        ```
        """
        ...
    @Emission.setter
    def Emission(self, value: float): ...

    @property
    def Shininess(self) -> float:  # icolorparam7_shininess.html
        """
        ## Shininess - Блеск

        Тип данных: double

        Значения свойства:

        - Блеск (от 0 до 1).

        ### Синтаксис Automation

        ```
        Shininess = Object.Shininess       # Получить свойство (* )
        Object.Shininess = Shininess       # Установить свойство (* )
        Shininess = Object.GetShininess()  # Получить свойство (**)
        Object.SetShininess (Shininess)    # Установить свойство (**)
        ```
        """
        ...
    @Shininess.setter
    def Shininess(self, value: float): ...

    @property
    def Specularity(self) -> float:  # icolorparam7_specularity.html
        """
        ## Specularity - Зеркальность

        Тип данных: double

        Значения свойства:

        - Зеркальность (от 0 до 1).

        ### Синтаксис Automation

        ```
        specularity = Object.specularity       # Получить свойство (* )
        Object.specularity = specularity       # Установить свойство (* )
        specularity = Object.GetSpecularity()  # Получить свойство (**)
        Object.SetSpecularity (specularity)    # Установить свойство (**)
        ```
        """
        ...
    @Specularity.setter
    def Specularity(self, value: float): ...

    @property
    def Transparency(self) -> float:  # icolorparam7_transparency.html
        """
        ## Transparency - Прозрачность

        Тип данных: double

        Значения свойства:

        - Прозрачность (от 0 до 1).

        Этот параметр характеризует способность поверхности пропускать падающий на нее свет. Если значение этого параметра равно 1 (или 100%), то поверхность совершенно непрозрачная. При моделировании деталей из стекла, органического стекла, слюды, бесцветного полиэтилена и подобных материалов значение прозрачности выбирают из диапазона между 0 и 1.

        Если значение этого параметра равно 0,5 (или 50%), то поверхность полупрозрачная.

        Если значение этого параметра равно 0, то поверхность полностью прозрачная. Такая поверхность невидима.

        ### Синтаксис Automation

        ```
        Transparency = Object.Transparency       # Получить свойство (* )
        Object.Transparency = Transparency       # Установить свойство (* )
        Transparency = Object.GetTransparency()  # Получить свойство (**)
        Object.SetTransparency (Transparency)    # Установить свойство (**)
        ```
        """
        ...
    @Transparency.setter
    def Transparency(self, value: float): ...

    @property
    def UseColor(self) -> int:  # icolorparam7_usecolor.html
        """
        ## UseColor - Используемый цвет

        Тип данных: из перечисления UseColor

        В случае ошибки возвращается useColorUnknown.

        ### Синтаксис Automation

        ```
        UseColor = Object.UseColor       # Получить свойство (* )
        Object.UseColor = UseColor       # Установить свойство (* )
        UseColor = Object.GetUseColor()  # Получить свойство (**)
        Object.SetUseColor( UseColor )   # Установить свойство (**)
        ```
        """
        ...
    @UseColor.setter
    def UseColor(self, value: int): ...

    def GetAdvancedColor(self) -> bool:  # icolorparam7_getadvancedcolor.html
        """
        ## GetAdvancedColor - Получить параметры цвета объекта

        Оптические свойства задаются в относительных единицах от 0..1.

        ### Синтаксис Automation

        ```
        BOOL GetAdvancedColor( long* Color,
        double* Ambient,
        double* Diffuse,
        double* Specularity,
        double* Shininess,
        double* Transparency,
        double* Emission );
        ```

        ### Выходные параметры

        +------------------------------+
        | color        | цвет,         |
        +------------------------------+
        | ambient      | Общий свет,   |
        +------------------------------+
        | diffuse      | Диффузия,     |
        +------------------------------+
        | specularity  | Зеркальность, |
        +------------------------------+
        | shininess    | Блеск,        |
        +------------------------------+
        | transparency | Прозрачность, |
        +------------------------------+
        | emission     | Излучение.    |
        +------------------------------+

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def SetAdvancedColor(self, Color, Ambient, Diffuse, Specularity, Shininess, Transparency, Emission) -> bool:  # icolorparam7_setadvancedcolor.html
        """
        ## SetAdvancedColor - Установить параметры цвета объекта

        Оптические свойства задаются в относительных единицах от 0..1.

        ### Синтаксис Automation

        ```
        BOOL SetAdvancedColor( long* Color,
        double* Ambient,
        double* Diffuse,
        double* Specularity,
        double* Shininess,
        double* Transparency,
        double* Emission );
        ```

        ### Выходные параметры

        +------------------------------+
        | color        | цвет,         |
        +------------------------------+
        | ambient      | Общий свет,   |
        +------------------------------+
        | diffuse      | Диффузия,     |
        +------------------------------+
        | specularity  | Зеркальность, |
        +------------------------------+
        | shininess    | Блеск,        |
        +------------------------------+
        | transparency | Прозрачность, |
        +------------------------------+
        | emission     | Излучение.    |
        +------------------------------+

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...




class ITexturesParam(IDispatch):  # itexturesparam.html
    """
    ## Интерфейс ITexturesParam

    Интерфейс параметров наложения текстур.
    """

    def SetTextureAngle(self, Type, arg1):
        ...

    def SetTextureDx(self, Type, arg1):
        ...

    def SetTextureDy(self, Type, arg1):
        ...

    def SetTextureFileName(self, Type, arg1):
        ...

    def SetTextureHeight(self, Type, arg1):
        ...

    def SetTextureWidth(self, Type, arg1):
        ...

    def TextureAngle(self, Type) -> float:  # itexturesparam_textureangle.html
        """
        ## TextureAngle - Поворот текстуры

        Тип данных: double

        ### Синтаксис Automation

        ```
        TextureAngle = Object.TextureAngle ( Type )     # Получить свойство (* )
        Object.TextureAngle ( Type ) = TextureAngle     # Установить свойство (* )
        TextureAngle = Object.GetTextureAngle ( Type )  # Получить свойство (**)
        Object.SetTextureAngle ( Type, TextureAngle )   # Установить свойство (**)
        ```

        ### Входные параметры

        +--------------------------------------------+
        | ksTextureTypeEnum | - Type - тип текстуры. |
        +--------------------------------------------+
        """
        ...

    def TextureDx(self, Type) -> float:  # itexturesparam_texturedx.html
        """
        ## TextureDx - Смещение по горизонтали

        Тип данных: double

        ### Синтаксис Automation

        ```
        TextureDx = Object.TextureDx ( Type )     # Получить свойство (* )
        Object.TextureDx ( Type ) = TextureDx     # Установить свойство (* )
        TextureDx = Object.GetTextureDx ( Type )  # Получить свойство (**)
        Object.SetTextureDx ( Type, TextureDx )   # Установить свойство (**)
        ```

        ### Входные параметры

        +--------------------------------------------+
        | ksTextureTypeEnum | - Type - тип текстуры. |
        +--------------------------------------------+
        """
        ...

    def TextureDy(self, Type) -> float:  # itexturesparam_texturedy.html
        """
        ## TextureDy - Смещение по вертикали

        Тип данных: double

        ### Синтаксис Automation

        ```
        TextureDy = Object.TextureDy ( Type )     # Получить свойство (* )
        Object.TextureDy ( Type ) = TextureDy     # Установить свойство (* )
        TextureDy = Object.GetTextureDy ( Type )  # Получить свойство (**)
        Object.SetTextureDy ( Type, TextureDy )   # Установить свойство (**)o
        ```

        ### Входные параметры

        +--------------------------------------------+
        | ksTextureTypeEnum | - Type - тип текстуры. |
        +--------------------------------------------+
        """
        ...

    def TextureFileName(self, Type) -> str:  # itexturesparam_texturefilename.html
        """
        ## TextureFileName - Имя файла текстуры

        Тип данных: BSTR

        ### Синтаксис Automation

        ```
        TextureFileName = Object.TextureFileName( Type )     # Получить свойство (* )
        Object.TextureFileName( Type ) = TextureFileName     # Установить свойство (* )
        TextureFileName = Object.GetTextureFileName( Type )  # Получить свойство (**)
        Object.SetTextureFileName( Type, TextureFileName )   # Установить свойство (**)
        ```

        ### Входные параметры

        +--------------------------------------------+
        | ksTextureTypeEnum | - Type - тип текстуры. |
        +--------------------------------------------+
        """
        ...

    def TextureHeight(self, Type) -> float:  # itexturesparam_textureheight.html
        """
        ## TextureHeight - Высота текстуры

        Тип данных: double

        ### Синтаксис Automation

        ```
        TextureHeight = Object.TextureHeight ( Type )     # Получить свойство (* )
        Object.TextureHeight ( Type ) = TextureHeight     # Установить свойство (* )
        TextureHeight = Object.GetTextureHeight ( Type )  # Получить свойство (**)
        Object.SetTextureHeight ( Type, TextureHeight )   # Установить свойство (**)
        ```

        ### Входные параметры

        +--------------------------------------------+
        | ksTextureTypeEnum | - Type - тип текстуры. |
        +--------------------------------------------+
        """
        ...

    def TextureWidth(self, Type) -> float:  # itexturesparam_texturewidth.html
        """
        ## TextureWidth - Ширина текстуры

        Тип данных: double

        ### Синтаксис Automation

        ```
        TextureWidth = Object.TextureWidth ( Type )     # Получить свойство (* )
        Object.TextureWidth ( Type ) = TextureWidth     # Установить свойство (* )
        TextureWidth = Object.GetTextureWidth ( Type )  # Получить свойство (**)
        Object.SetTextureWidth ( Type, TextureWidth )   # Установить свойство (**)
        ```

        ### Входные параметры

        +--------------------------------------------+
        | ksTextureTypeEnum | - Type - тип текстуры. |
        +--------------------------------------------+
        """
        ...

    def Update(self) -> bool:  # itexturesparam_update.html
        """
        ## Update - Обновление данных

        ### Синтаксис Automation

        ```
        BOOL Update();
        ```

        ### Возвращаемое значение

        +-----------------------------------------+
        | TRUE | - в случае успешного завершения, |
        +-----------------------------------------+
        """
        ...




class IModelText(IModelObjects):  # imodeltext.html
    """
    ## Интерфейс IModelText

    Интерфейс текста в модели.

    Интерфейс можно получить, используя свойство IModelTexts::ModelText или метод IModelTexts::Add.

    Версия Компас v21
    """

    @property
    def Allocation(self) -> int:  # imodeltext_allocation.html
        """
        ## Allocation - Размещение точки привязки текста

        Тип данных: из перечисления ksAllocationEnum

        ### Синтаксис Automation

        ```
        Allocation = Object.Allocation       # Получить свойство (* )
        Object.Allocation = Allocation       # Установить свойство (* )
        Allocation = Object.GetAllocation()  # Получить свойство (**)
        Object.SetAllocation( Allocation )   # Установить свойство (**)
        ```
        """
        ...
    @Allocation.setter
    def Allocation(self, value: int): ...

    @property
    def Angle(self) -> float:  # imodeltext_angle.html
        """
        ## Angle - Угол наклона текста

        Тип данных: double

        ### Синтаксис Automation

        ```
        Angle = Object.Angle       # Получить свойство (* )
        Object.Angle = Angle       # Установить свойство (* )
        Angle = Object.GetAngle()  # Получить свойство (**)
        Object.SetAngle( Angle )   # Установить свойство (**)
        ```
        """
        ...
    @Angle.setter
    def Angle(self, value: float): ...

    @property
    def AssociationObject(self) -> IModelObject:  # imodeltext_associationobject.html
        """
        ## AssociationObject - Установить опорный объект для точки привязки

        Тип данных: Указатель на интерфейс IModelObject

        ### Синтаксис Automation

        ```
        AssociationObject = Object.AssociationObject       # Получить свойство (* )
        Object.AssociationObject = AssociationObject       # Установить свойство (* )
        AssociationObject = Object.GetAssociationObject()  # Получить свойство (**)
        Object.SetAssociationObject( AssociationObject )   # Установить свойство (**)
        ```
        """
        ...
    @AssociationObject.setter
    def AssociationObject(self, value: IModelObject): ...

    @property
    def HFormat(self) -> int:  # imodeltext_hformat.html
        """
        ## HFormat - Признак горизонтального форматирования

        Тип данных: из перечисления ksTextHorizontalFormatEnum

        ### Синтаксис Automation

        ```
        HFormat = Object.HFormat       # Получить свойство (* )
        Object.HFormat = HFormat       # Установить свойство (* )
        HFormat = Object.GetHFormat()  # Получить свойство (**)
        Object.SetHFormat( HFormat )   # Установить свойство (**)
        ```
        """
        ...
    @HFormat.setter
    def HFormat(self, value: int): ...

    @property
    def Height(self) -> float:  # imodeltext_height.html
        """
        ## Height - Высота блока форматирования

        Тип данных: double

        ### Синтаксис Automation

        ```
        Height = Object.Height       # Получить свойство (* )
        Object.Height = Height       # Установить свойство (* )
        Height = Object.GetHeight()  # Получить свойство (**)
        Object.SetHeight( Height )   # Установить свойство (**)
        ```
        """
        ...
    @Height.setter
    def Height(self, value: float): ...

    @property
    def Hidden(self):
        ...
    @Hidden.setter
    def Hidden(self, value): ...

    @property
    def ModelObjectType(self):
        ...

    @property
    def Name(self):
        ...
    @Name.setter
    def Name(self, value): ...

    @property
    def Owner(self):
        ...

    @property
    def Part(self):
        ...

    @property
    def Plane(self) -> IModelObject:  # imodeltext_plane.html
        """
        ## Plane - Базовая плоскость

        Тип данных: Указатель на интерфейс IModelObject

        ### Синтаксис Automation

        ```
        Plane = Object.Plane       # Получить свойство (* )
        Object.Plane = Plane       # Установить свойство (* )
        Plane = Object.GetPlane()  # Получить свойство (**)
        Object.SetPlane( Plane )   # Установить свойство (**)
        ```
        """
        ...
    @Plane.setter
    def Plane(self, value: IModelObject): ...

    @property
    def PointParameterType(self) -> int:  # imodeltext_pointparametertype.html
        """
        ## PointParameterType - Тип параметров построения точки привязки

        Тип данных: из перечисления ksPoint3DTypeEnum

        ### Синтаксис Automation

        ```
        PointParameterType = Object.PointParameterType       # Получить свойство (* )
        Object.PointParameterType = PointParameterType       # Установить свойство (* )
        PointParameterType = Object.GetPointParameterType()  # Получить свойство (**)
        Object.SetPointParameterType( PointParameterType )   # Установить свойство (**)
        ```
        """
        ...
    @PointParameterType.setter
    def PointParameterType(self, value: int): ...

    @property
    def PointParameters(self) -> IKompasAPIObject:  # imodeltext_pointparameters.html
        """
        ## PointParameters - Получить интерфейс параметров точки

        Тип данных: Указатель на интерфейс IKompasAPIObject

        1. Свойство доступно только для чтения.

        2. В зависимости от типа PointType интерфейс параметров должен приводится к одному из следующих вариантов:

        •ksPDisplace - IPoint3DParamDisplace - интерфейс параметров пространственной точки, заданной по смещению от опорного объекта.

        •ksPIntersect - IPoint3DParamIntersect - интерфейс параметров пространственной точки, заданной на пересечении опорных объектов.

        •ksPCenter - IPoint3DParamCenter - интерфейс параметров пространственной точки, заданной в центре опорного объекта.

        •ksPCurve - IPoint3DParamCurve - интерфейс параметров пространственной точки, заданной на кривой со смещением.

        •ksPSurface - IPoint3DparamSurface - интерфейс параметров пространственной точки, заданной на поверхности.

        •ksPProjection - IPoint3DParamProjection - интерфейс параметров пространственной точки, заданной проецированием.

        ### Синтаксис Automation

        ```
        PointParameters = Object.PointParameters       # Получить свойство (* )
        PointParameters = Object.GetPointParameters()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def Text(self) -> IText:  # imodeltext_text.html
        """
        ## Text - Текст

        Тип данных: Указатель на интерфейс IText

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        Text = Object.Text       # Получить свойство (* )
        Text = Object.GetText()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def TextObjects(self) -> typing.Any:  # imodeltext_textobjects.html
        """
        ## TextObjects - Объекты применения в виде SAFEARRAY'я DISPATCH - VT_ARRAY | VT_DISPATCH

        Тип данных: VARIANT

        ### Синтаксис Automation

        ```
        TextObjects = Object.TextObjects       # Получить свойство (* )
        Object.TextObjects = TextObjects       # Установить свойство (* )
        TextObjects = Object.GetTextObjects()  # Получить свойство (**)
        Object.SetTextObjects( TextObjects )   # Установить свойство (**)
        ```
        """
        ...
    @TextObjects.setter
    def TextObjects(self, value: typing.Any): ...

    @property
    def VFormat(self) -> bool:  # imodeltext_vformat.html
        """
        ## VFormat - Признак вертикального форматирования (FALSE- нет форматирования, TRUE-изменение шага строк)

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        VFormat = Object.VFormat       # Получить свойство (* )
        Object.VFormat = VFormat       # Установить свойство (* )
        VFormat = Object.GetVFormat()  # Получить свойство (**)
        Object.SetVFormat( VFormat )   # Установить свойство (**)
        ```
        """
        ...
    @VFormat.setter
    def VFormat(self, value: bool): ...

    @property
    def Valid(self):
        ...

    @property
    def Width(self) -> float:  # imodeltext_width.html
        """
        ## Width - Ширина блока форматирования

        Тип данных: double

        ### Синтаксис Automation

        ```
        Width = Object.Width       # Получить свойство (* )
        Object.Width = Width       # Установить свойство (* )
        Width = Object.GetWidth()  # Получить свойство (**)
        Object.SetWidth( Width )   # Установить свойство (**)
        ```
        """
        ...
    @Width.setter
    def Width(self, value: float): ...

    def GetPoint(self) -> bool:  # imodeltext_getpoint.html
        """
        ## GetPoint - Получить координату точки привязки

        ### Синтаксис Automation

        ```
        BOOL GetPoint( double * X, double * Y, double * Z );
        ```

        ### Входные параметры

        +-------------------------------+
        | X, Y, Z | - координаты точки. |
        +-------------------------------+

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def SetPoint(self, X, Y, Z) -> bool:  # imodeltext_setpoint.html
        """
        ## SetPoint - Установить координату точки привязки

        ### Синтаксис Automation

        ```
        BOOL SetPoint( double X, double Y, double Z );
        ```

        ### Входные параметры

        +-------------------------------+
        | X, Y, Z | - координаты точки. |
        +-------------------------------+

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def Update(self):
        ...




class IModelTexts(IModelObjects):  # imodeltexts.html
    """
    ## Интерфейс IModelTexts

    Интерфейс коллекции текстов в модели.

    Интерфейс можно получить, используя свойство ISymbols3DContainer::ModelTexts.

    Версия Компас v21
    """

    def Add(self) -> IModelText:  # imodeltexts_add.html
        """
        ## Add - Добавить текст в коллекцию

        ### Синтаксис Automation

        ```
        LPDISPATCH Add();
        ```

        ### Возвращаемое значение

        Указатель на интерфейс IModelText
        """
        ...

    def ModelText(self, Index) -> IModelText:  # imodeltexts_modeltext.html
        """
        ## ModelText - Получить элемент, заданный по индексу

        Тип данных: Указатель на интерфейс IModelText

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        ModelText = Object.ModelText( Index )     # Получить свойство (* )
        ModelText = Object.GetModelText( Index )  # Получить свойство (**)
        ```
        """
        ...




class IModelTables(IModelObjects):  # imodeltables.html
    """
    ## Интерфейс IModelTables

    Интерфейс коллекции таблиц в модели.

    Интерфейс можно получить, используя свойство ISymbols3DContainer::ModelTables

    Версия Компас v21
    """

    def Add(self, RowsCount, ColumnsCount, RowHeigh, ColumnsWidth, TitlePos) -> IModelTable:  # imodeltables_add.html
        """
        ## Add - Добавить таблицу в коллекцию

        ### Синтаксис Automation

        ```
        LPDISPATCH Add( long RowsCount, long ColumnsCount, double RowHeigh, double ColumnsWidth, ksTableTileLayoutEnum TitlePos );
        ```

        ### Входные параметры

        +-------------------------------------------------------------------------------------+
        | RowsCount    | - количество строк,                                                  |
        +-------------------------------------------------------------------------------------+
        | ColumnsCount | - количество колонок,                                                |
        +-------------------------------------------------------------------------------------+
        | RowHeigh     | - высота ячеек,                                                      |
        +-------------------------------------------------------------------------------------+
        | ColumnsWidth | - ширина ячеек,                                                      |
        +-------------------------------------------------------------------------------------+
        | TitlePos     | - положение заголовка таблицы из перечисления ksTableTileLayoutEnum. |
        +-------------------------------------------------------------------------------------+

        ### Возвращаемое значение

        Указатель на интерфейс IModelTable.
        """
        ...

    def Load(self, FileName) -> IModelTable:  # imodeltables_load.html
        """
        ## Load - Загрузить таблицу из файла

        ### Синтаксис Automation

        ```
        LPDISPATCH Load( BSTR FileName );
        ```

        ### Входные параметры

        +---------------------------------+
        | FileName | - имя файла таблицы. |
        +---------------------------------+

        ### Возвращаемое значение

        Указатель на интерфейс IModelTable.
        """
        ...

    def ModelTable(self, Index) -> IModelTable:  # imodeltables_modeltable.html
        """
        ## ModelTable - Получить таблицу по индексу

        Тип данных: Указатель на интерфейс IModelTable

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        ModelTable = Object.ModelTable( Index )     # Получить свойство (* )
        ModelTable = Object.GetModelTable( Index )  # Получить свойство (**)
        ```

        ### Входные параметры

        +-------------------------------------------+
        | VARIANT Index | - индекс или имя объекта. |
        +-------------------------------------------+
        """
        ...




class IModelTable(IModelObjects):  # imodeltable.html
    """
    ## Интерфейс IModelTable

    Интерфейс таблицы в модели.

    Интерфейс можно получить, используя свойство IModelTables::ModelTable или метод IModelTables::Add.

    Версия Компас v21
    """

    @property
    def Angle(self) -> float:  # imodeltable_angle.html
        """
        ## Angle - Угол наклона текста

        Тип данных: double

        ### Синтаксис Automation

        ```
        Angle = Object.Angle       # Получить свойство (* )
        Object.Angle = Angle       # Установить свойство (* )
        Angle = Object.GetAngle()  # Получить свойство (**)
        Object.SetAngle( Angle )   # Установить свойство (**)
        ```
        """
        ...
    @Angle.setter
    def Angle(self, value: float): ...

    @property
    def AssociationObject(self) -> IModelObject:  # imodeltable_associationobject.html
        """
        ## AssociationObject - Установить опорный объект для точки привязки

        Тип данных: Указатель на интерфейс IModelObject

        ### Синтаксис Automation

        ```
        AssociationObject = Object.AssociationObject       # Получить свойство (* )
        Object.AssociationObject = AssociationObject       # Установить свойство (* )
        AssociationObject = Object.GetAssociationObject()  # Получить свойство (**)
        Object.SetAssociationObject( AssociationObject )   # Установить свойство (**)
        ```
        """
        ...
    @AssociationObject.setter
    def AssociationObject(self, value: IModelObject): ...

    @property
    def FixedCellsSize(self) -> bool:  # imodeltable_fixedcellssize.html
        """
        ## FixedCellsSize - Зафиксировать габариты ячеек

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        FixedCellsSize = Object.FixedCellsSize       # Получить свойство (* )
        Object.FixedCellsSize = FixedCellsSize       # Установить свойство (* )
        FixedCellsSize = Object.GetFixedCellsSize()  # Получить свойство (**)
        Object.SetFixedCellsSize( FixedCellsSize )   # Установить свойство (**)
        ```
        """
        ...
    @FixedCellsSize.setter
    def FixedCellsSize(self, value: bool): ...

    @property
    def FixedColumnCount(self) -> bool:  # imodeltable_fixedcolumncount.html
        """
        ## FixedColumnCount - Запретить изменять число столбцов

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        FixedColumnCount = Object.FixedColumnCount       # Получить свойство (* )
        Object.FixedColumnCount = FixedColumnCount       # Установить свойство (* )
        FixedColumnCount = Object.GetFixedColumnCount()  # Получить свойство (**)
        Object.SetFixedColumnCount( FixedColumnCount )   # Установить свойство (**)
        ```
        """
        ...
    @FixedColumnCount.setter
    def FixedColumnCount(self, value: bool): ...

    @property
    def FixedRowCount(self) -> bool:  # imodeltable_fixedrowcount.html
        """
        ## FixedRowCount - Запретить изменять число строк

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        FixedRowCount = Object.FixedRowCount       # Получить свойство (* )
        Object.FixedRowCount = FixedRowCount       # Установить свойство (* )
        FixedRowCount = Object.GetFixedRowCount()  # Получить свойство (**)
        Object.SetFixedRowCount( FixedRowCount )   # Установить свойство (**)
        ```
        """
        ...
    @FixedRowCount.setter
    def FixedRowCount(self, value: bool): ...

    @property
    def Hidden(self):
        ...
    @Hidden.setter
    def Hidden(self, value): ...

    @property
    def ModelObjectType(self):
        ...

    @property
    def Name(self):
        ...
    @Name.setter
    def Name(self, value): ...

    @property
    def Owner(self):
        ...

    @property
    def Part(self):
        ...

    @property
    def Plane(self) -> IModelObject:  # imodeltable_plane.html
        """
        ## Plane - Базовая плоскость

        Тип данных: Указатель на интерфейс IModelObject

        ### Синтаксис Automation

        ```
        Plane = Object.Plane       # Получить свойство (* )
        Object.Plane = Plane       # Установить свойство (* )
        Plane = Object.GetPlane()  # Получить свойство (**)
        Object.SetPlane( Plane )   # Установить свойство (**)
        ```
        """
        ...
    @Plane.setter
    def Plane(self, value: IModelObject): ...

    @property
    def PointParameterType(self) -> int:  # imodeltable_pointparametertype.html
        """
        ## PointParameterType - Тип параметров построения точки привязки

        Тип данных: из перечисления ksPoint3DTypeEnum

        ### Синтаксис Automation

        ```
        PointParameterType = Object.PointParameterType       # Получить свойство (* )
        Object.PointParameterType = PointParameterType       # Установить свойство (* )
        PointParameterType = Object.GetPointParameterType()  # Получить свойство (**)
        Object.SetPointParameterType( PointParameterType )   # Установить свойство (**)
        ```
        """
        ...
    @PointParameterType.setter
    def PointParameterType(self, value: int): ...

    @property
    def PointParameters(self) -> IKompasAPIObject:  # imodeltable_pointparameters.html
        """
        ## PointParameters - Получить интерфейс параметров точки

        Тип данных: Указатель на интерфейс IKompasAPIObject

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        PointParameters = Object.PointParameters       # Получить свойство (* )
        PointParameters = Object.GetPointParameters()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def Table(self) -> ITable:  # imodeltable_table.html
        """
        ## Table - Получить интерфейс таблицы

        Тип данных: Указатель на интерфейс ITable

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        Table = Object.Table       # Получить свойство (* )
        Table = Object.GetTable()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def Valid(self):
        ...

    def GetPoint(self) -> bool:  # imodeltable_getpoint.html
        """
        ## GetPoint - Получить координату точки привязки

        ### Синтаксис Automation

        ```
        BOOL GetPoint( double * X, double * Y, double * Z );
        ```

        ### Выходные параметры

        +-------------------------------+
        | X, Y, Z | - координаты точки. |
        +-------------------------------+

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def Save(self, FileName) -> bool:  # imodeltable_save.html
        """
        ## Save - Сохранить в файл

        ### Синтаксис Automation

        ```
        BOOL Save( BSTR FileName );
        ```

        ### Входные параметры

        +---------------------------------+
        | FileName | - имя файла таблицы. |
        +---------------------------------+

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def SetPoint(self, X, Y, Z) -> bool:  # imodeltable_setpoint.html
        """
        ## SetPoint - Установить координату точки привязки

        ### Синтаксис Automation

        ```
        BOOL SetPoint( double X, double Y, double Z );
        ```

        ### Входные параметры

        +-------------------------------+
        | X, Y, Z | - координаты точки. |
        +-------------------------------+

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def Update(self):
        ...




class ITextDocumentSectionsManager(IDispatch):  # itextdocumentsectionsmanager.html
    """
    ## Интерфейс ITextDocumentSectionsManager

    Интерфейс менеджера разделов текстового документа.

    Интерфейс является дополнительным к интерфейсу документа ITextDocument. Интерфейс можно получить посредством вызова метода IUnknown::QueryInterface (const GUID far& iid, void** pif).

    Версия KOMPAS v19
    """

    @property
    def SectionsCount(self) -> int:  # itextdocumentsectionsmanager_sectionscount.html
        """
        ## SectionsCount - Количество разделов

        Тип данных: long

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        SectionsCount = Object.SectionsCount       # Получить свойство(* )
        SectionsCount = Object.GetSectionsCount()  # Получить свойство (**)
        ```
        """
        ...

    def AddSection(self) -> ITextDocumentSection:  # itextdocumentsectionsmanager_addsection.html
        """
        ## AddSection - Добавить раздел

        1. Метод позволяет создать новый интерфейс параметров раздела текстового документа.

        2. Метод добавляет новый раздел в конец документа.

        ### Синтаксис Automation

        ```
        LPDISPATCH AddSection();
        ```

        ### Возвращаемое значение

        - Указатель на интерфейс ITextDocumentSection
        """
        ...

    def AddSectionAt(self, Index) -> ITextDocumentSection:  # itextdocumentsectionsmanager_addsectionat.html
        """
        ## AddSectionAt - Добавить раздел

        1. Метод позволяет создать новый интерфейс параметров раздела текстового документа.

        2. Метод добавляет новый раздел в определенное место текстового документа.

        ### Синтаксис Automation

        ```
        LPDISPATCH AddSectionAt( long Index );
        ```

        ### Входные параметры

        +---------------------------+
        | Index | - индекс раздела. |
        +---------------------------+

        ### Возвращаемое значение

        - Указатель на интерфейс ITextDocumentSection
        """
        ...

    def GetSectionLineIndexes(self, SectionIndex):  # itextdocumentsectionsmanager_getsectionlineindexes.html
        """
        ## GetSectionLineIndexes - Получить индексы первой и последней строки раздела и число строк раздела

        ### Синтаксис Automation

        ```
        long GetSectionLineIndexes( long SectionIndex, long * FirstLineIndex, long * LastLineIndex );
        ```

        ### Входные параметры

        +-----------------------------------------+
        | SectionIndex | - Идентификатор раздела. |
        +-----------------------------------------+

        ### Выходные параметры

        +----------------------------------------------------------------+
        | FirstLineIndex | - индекс строки, в которой начинается раздел, |
        +----------------------------------------------------------------+
        | LastLineIndex  | - индекс последней строки раздела.            |
        +----------------------------------------------------------------+

        ### Возвращаемое значение

        - Число строк в разделе.
        """
        ...

    def MoveLinesToSection(self, Section, FirstLineIndex, LastLineIndex) -> bool:  # itextdocumentsectionsmanager_movelinestosection.html
        """
        ## MoveLinesToSection - Перенести строки в другой раздел

        ### Синтаксис Automation

        ```
        BOOL MoveLinesToSection( ITextDocumentSection * Section, long FirstLineIndex, long LastLineIndex );
        ```

        ### Входные параметры

        +----------------------------------------------------------------+
        | Section        | - индекс раздела,                             |
        +----------------------------------------------------------------+
        | FirstLineIndex | - индекс строки, в которой начинается раздел, |
        +----------------------------------------------------------------+
        | LastLineIndex  | - индекс последней строки раздела.            |
        +----------------------------------------------------------------+

        ### Возвращаемое значение

        +-----------------------------------------+
        | TRUE | - в случае успешного завершения, |
        +-----------------------------------------+
        """
        ...

    def MoveSection(self, SectionIndex, NewSectionIndex) -> bool:  # itextdocumentsectionsmanager_movesection.html
        """
        ## MoveSection - Переместить раздел

        Метод перемещает выбранный раздел в определенное место текстового документа.

        ### Синтаксис Automation

        ```
        BOOL MoveSection( long SectionIndex, long NewSectionIndex );
        ```

        ### Входные параметры

        +------------------------------------------------------------------------------------------+
        | SectionIndex    | - индекс перемещаемого раздела,                                        |
        +------------------------------------------------------------------------------------------+
        | NewSectionIndex | - индекс раздела, перед которым будет размещаться перемещаемый раздел. |
        +------------------------------------------------------------------------------------------+

        ### Возвращаемое значение

        +-----------------------------------------+
        | TRUE | - в случае успешного завершения, |
        +-----------------------------------------+
        """
        ...

    def Section(self, Index) -> ITextDocumentSection:  # itextdocumentsectionsmanager_section.html
        """
        ## Section - Получить параметры раздела по индексу

        Тип данных: Указатель на интерфейс ITextDocumentSection.

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        Section = Object.Section      # Получить свойство(* )
        Section= Object.GetSection()  # Получить свойство (**)
        ```

        ### Входные параметры

        +--------------------------------+
        | long Index | - индекс раздела. |
        +--------------------------------+
        """
        ...

    def SectionByTextLine(self, LineIndex) -> ITextDocumentSection:  # itextdocumentsectionsmanager_sectionbytextline.html
        """
        ## SectionByTextLine - Получить параметры раздела по индексу строки

        Тип данных: Указатель на интерфейс ITextDocumentSection

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        SectionByTextLine = Object.SectionByTextLine      # Получить свойство(* )
        SectionByTextLine= Object.GetSectionByTextLine()  # Получить свойство (**)
        ```

        ### Входные параметры

        +--------------------------------+
        | long Index | - индекс раздела. |
        +--------------------------------+
        """
        ...




class ITextDocumentSection(IKompasAPIObject):  # itextdocumentsection.html
    """
    ## Интерфейс ITextDocumentSection

    Интерфейс параметров раздела текстового документа.

    Интерфейс можно получить с помощью методов менеджера разделов ITextDocumentSectionsManager.
    """

    @property
    def EvenLayoutLibraryFileName(self) -> str:  # itextdocumentsection_evenlayoutlibraryfilename.html
        """
        ## EvenLayoutLibraryFileName - Имя файла библиотеки стилей оформления для четных листов

        Тип данных: BSTR

        ### Синтаксис Automation

        ```
        EvenLayoutLibraryFileName = Object.EvenLayoutLibraryFileName       # Получить свойство (* )
        Object.EvenLayoutLibraryFileName = EvenLayoutLibraryFileName       # Установить свойство (* )
        EvenLayoutLibraryFileName = Object.GetEvenLayoutLibraryFileName()  # Получить свойство (**)
        Object.SetEvenLayoutLibraryFileName( EvenLayoutLibraryFileName )   # Установить свойство (**)
        ```
        """
        ...
    @EvenLayoutLibraryFileName.setter
    def EvenLayoutLibraryFileName(self, value: str): ...

    @property
    def EvenLayoutStyleNumber(self) -> float:  # itextdocumentsection_evenlayoutstylenumber.html
        """
        ## EvenLayoutStyleNumber - Номер стиля оформления для четных листов

        Тип данных: double

        ### Синтаксис Automation

        ```
        EvenLayoutStyleNumber = Object.EvenLayoutStyleNumber       # Получить свойство (* )
        Object.EvenLayoutStyleNumber = EvenLayoutStyleNumber       # Установить свойство (* )
        EvenLayoutStyleNumber = Object.GetEvenLayoutStyleNumber()  # Получить свойство (**)
        Object.SetEvenLayoutStyleNumber( EvenLayoutStyleNumber )   # Установить свойство (**)
        ```
        """
        ...
    @EvenLayoutStyleNumber.setter
    def EvenLayoutStyleNumber(self, value: float): ...

    @property
    def Format(self) -> ISheetFormat:  # itextdocumentsection_format.html
        """
        ## Format - Формат листа

        Тип данных: Указатель на интерфейс ISheetFormat

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        Format = Object.Format       # Получить свойство(* )
        Format = Object.GetFormat()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def LayoutLibraryFileName(self) -> str:  # itextdocumentsection_layoutlibraryfilename.html
        """
        ## LayoutLibraryFileName - Имя файла библиотеки стилей оформления для первого листа

        Тип данных: BSTR

        ### Синтаксис Automation

        ```
        LayoutLibraryFileName = Object.LayoutLibraryFileName       # Получить свойство (* )
        Object.LayoutLibraryFileName = LayoutLibraryFileName       # Установить свойство (* )
        LayoutLibraryFileName = Object.GetLayoutLibraryFileName()  # Получить свойство (**)
        Object.SetLayoutLibraryFileName( LayoutLibraryFileName )   # Установить свойство (**)
        ```
        """
        ...
    @LayoutLibraryFileName.setter
    def LayoutLibraryFileName(self, value: str): ...

    @property
    def LayoutStyleNumber(self) -> float:  # itextdocumentsection_layoutstylenumber.html
        """
        ## LayoutStyleNumber - Номер стиля оформления для первого листа

        Тип данных: double

        ### Синтаксис Automation

        ```
        LayoutStyleNumber = Object.LayoutStyleNumber       # Получить свойство (* )
        Object.LayoutStyleNumber = LayoutStyleNumber       # Установить свойство (* )
        LayoutStyleNumber = Object.GetLayoutStyleNumber()  # Получить свойство (**)
        Object.SetLayoutStyleNumber( LayoutStyleNumber )   # Установить свойство (**)
        ```
        """
        ...
    @LayoutStyleNumber.setter
    def LayoutStyleNumber(self, value: float): ...

    @property
    def OddLayoutLibraryFileName(self) -> str:  # itextdocumentsection_oddlayoutlibraryfilename.html
        """
        ## OddLayoutLibraryFileName - Имя файла библиотеки стилей оформления для нечетных листов

        Тип данных: BSTR

        ### Синтаксис Automation

        ```
        OddLayoutLibraryFileName = Object.OddLayoutLibraryFileName       # Получить свойство (* )
        Object.OddLayoutLibraryFileName = OddLayoutLibraryFileName       # Установить свойство (* )
        OddLayoutLibraryFileName = Object.GetOddLayoutLibraryFileName()  # Получить свойство (**)
        Object.SetOddLayoutLibraryFileName( OddLayoutLibraryFileName )   # Установить свойство (**)
        ```
        """
        ...
    @OddLayoutLibraryFileName.setter
    def OddLayoutLibraryFileName(self, value: str): ...

    @property
    def OddLayoutStyleNumber(self) -> float:  # itextdocumentsection_oddlayoutstylenumber.html
        """
        ## OddLayoutStyleNumber - Номер стиля оформления для первого листа

        Тип данных: double

        ### Синтаксис Automation

        ```
        OddLayoutStyleNumber = Object.OddLayoutStyleNumber       # Получить свойство (* )
        Object.OddLayoutStyleNumber = OddLayoutStyleNumber       # Установить свойство (* )
        OddLayoutStyleNumber = Object.GetOddLayoutStyleNumber()  # Получить свойство (**)
        Object.SetOddLayoutStyleNumber( OddLayoutStyleNumber )   # Установить свойство (**)
        ```
        """
        ...
    @OddLayoutStyleNumber.setter
    def OddLayoutStyleNumber(self, value: float): ...

    def Delete(self) -> bool:  # itextdocumentsection_delete.html
        """
        ## Delete - Удалить раздел

        ### Синтаксис Automation

        ```
        BOOL Delete();
        ```

        ### Возвращаемое значение

        +-----------------------------------------+
        | TRUE | - в случае успешного завершения. |
        +-----------------------------------------+
        """
        ...

    def Update(self) -> bool:  # itextdocumentsection_update.html
        """
        ## Update - Обновление данных

        ### Синтаксис Automation

        ```
        BOOL Update();
        ```

        ### Возвращаемое значение

        +-----------------------------------------+
        | TRUE | - в случае успешного завершения. |
        +-----------------------------------------+
        """
        ...




class ILibraryManager(IKompasAPIObject):  # ilibrarymanager.html
    """
    ## Интерфейс ILibraryManager

    Интерфейс Менеджера библиотек.

    1. Данный интерфейс объединяет работу с библиотеками процедур (файлы *.rtw и *.dll ) и библиотеками документов: фрагментов (файлы *.lfr ) и моделей (файлы *.l3d). Интерфейс позволяет выполнять следующие действия:

    •получить коллекции соответствующих библиотек, активный узел в менеджере библиотек, его название и комментарий;

    •получить и управлять положением и видимостью Менеджера библиотек.

    2. Данный интерфейс можно получить у интерфейса приложения IApplication с помощью свойства IApplication::LibraryManager.
    """

    @property
    def ActiveFolder(self) -> str:  # ilibrarymanager_activefolder.html
        """
        ## ActiveFolder - Активная папка в окне Менеджера библиотек

        Функция не поддерживается

        Тип данных: BSTR (строка)

        Позволяет получить и установить имя текущей папки в Менеджере библиотек.

        ### Синтаксис Automation

        ```
        ActiveFolder = iObject.ActiveFolder       # Получить свойство (* )
        iObject.ActiveFolder = ActiveFolder       # Установить свойство (* )
        ActiveFolder = iObject.GetActiveFolder()  # Получить свойство (**)
        iObject.SetActiveFolder (ActiveFolder)    # Установить свойство (**)
        ```
        """
        ...
    @ActiveFolder.setter
    def ActiveFolder(self, value: str): ...

    @property
    def ActiveFolderComment(self) -> typing.Any:  # ilibrarymanager_activefoldercomment.html
        """
        ## ActiveFolderComment - Комментарий активной папки

        Функция не поддерживается

        Тип данных: VARIANT

        Комментарий текущей папки может быть многострочным. Поэтому свойство возвращает или принимает переменную типа VARIANT на безопасный массив строк SAFEARRAY (VT_ARRAY | VT_BSTR).

        ### Синтаксис Automation

        ```
        ActiveFolderComment = iObject.ActiveFolderComment       # Получить свойство (* )
        iObject.ActiveFolderComment = ActiveFolderComment       # Установить свойство (* )
        ActiveFolderComment = iObject.GetActiveFolderComment()  # Получить свойство (**)
        iObject.SetActiveFolderComment (ActiveFolderComment)    # Установить свойство (**)
        ```
        """
        ...
    @ActiveFolderComment.setter
    def ActiveFolderComment(self, value: typing.Any): ...

    @property
    def CurrentLibrary(self) -> IProceduresLibrary:  # ilibrarymanager_currentlibrary.html
        """
        ## CurrentLibrary - Текущая прикладная библиотека

        Тип данных: указатель на интерфейс IProceduresLibrary

        1. Подключенными одновременно могут быть несколько библиотек, но текущей библиотекой может быть одна.

        2. Текущей является библиотека, у которой непосредственно выполняется команда или вызван обработчик события.

        ### Синтаксис Automation

        ```
        CurrentLibrary = iObject.CurrentLibrary       # Получить свойство (* )
        CurrentLibrary = iObject.GetCurrentLibrary()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def DocumentsLibraries(self) -> IInsertsLibraries:  # ilibrarymanager_documentslibraries.html
        """
        ## DocumentsLibraries - Коллекция библиотек документов

        Тип данных: Указатель на интерфейс IInsertsLibraries

        Свойство доступно только для чтения

        ### Синтаксис Automation

        ```
        DocumentsLibraries = Object.DocumentsLibraries       # Получить свойство (* )
        DocumentsLibraries = Object.GetDocumentsLibraries()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def FragmentsLibraries(self) -> IInsertsLibraries:  # ilibrarymanager_fragmentslibraries.html
        """
        ## FragmentsLibraries - Коллекция библиотек фрагментов

        Тип данных: Указатель на коллекцию библиотек документов IInsertsLibraries

        1. Свойство позволяет получить коллекцию библиотек фрагментов, которые зарегистрированы в Менеджере библиотек.

        2. Под библиотекой фрагментов понимается библиотека документов КОМПАС, состоящая из фрагментов.

        3. Следует различать следующие состояния библиотек фрагментов:

        •зарегистрирована в Менеджере библиотек; имеется описание библиотеки в Менеджере,

        •подключена; эта библиотека отображается в меню "Библиотеки", рядом с ее названием в списке показан знак «галочка»,

        4. Если библиотека защищена лицензией, то при подключении библиотеки она захватывает лицензию.

        5. Фрагменты из подключенной библиотеки могут быть вставлены в графические документ системы КОМПАС.

        6. Если библиотека не защищена, ее можно редактировать.

        ### Синтаксис Automation

        ```
        FragmentsLibraries = iObject.FragmentsLibraries  # Получить свойство (* )
        LayerGroups = iObject.GetLayerGroups()           # Получить свойство (**)
        ```
        """
        ...

    @property
    def Layout(self) -> int:  # ilibrarymanager_layout.html
        """
        ## Layout - Положение Менеджера библиотек (вверху, внизу, слева, справа, плавающий)

        Тип данных: PropertyManagerLayout

        Позволяет получить положение Менеджера библиотек.

        ### Синтаксис Automation

        ```
        Layout = iObject.Layout       # Получить свойство (* )
        Layout = iObject.GetLayout()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def ModelsLibraries(self) -> IInsertsLibraries:  # ilibrarymanager_modelslibraries.html
        """
        ## ModelsLibraries - Коллекция библиотек моделей

        Тип данных: Указатель на коллекцию библиотек документов IInsertsLibraries

        1. Свойство позволяет получить коллекцию библиотек моделей, которые зарегистрированы в Менеджере библиотек.

        2. Под библиотекой фрагментов понимается библиотека документов КОМПАС, состоящая из деталей и сборок.

        3. Следует различать следующие состояния библиотек фрагментов:

        •зарегистрирована в Менеджере библиотек; имеется описание библиотеки в Менеджере,

        •подключена; эта библиотека отображается в меню "Библиотеки", рядом с ее названием в списке показан знак «галочка»,

        4. Если библиотека защищена лицензией, то при подключении библиотеки она захватывает лицензию.

        5. Модели из подключенной библиотеки могут быть вставлены в документ-модель системы КОМПАС.

        6. Если библиотека не защищена, ее можно редактировать.

        ### Синтаксис Automation

        ```
        ModelsLibraries = iObject.ModelsLibraries    # Получить свойство (* )
        ModelsLibraries = iObject.ModelsLibraries()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def ProceduresLibraries(self) -> IProceduresLibraries:  # ilibrarymanager_procedureslibraries.html
        """
        ## ProceduresLibraries - Коллекция прикладных библиотек

        Тип данных: IProceduresLibraries

        1. Свойство позволяет получить коллекцию процедурных библиотек, которые зарегистрированы в Менеджере библиотек.

        2. Под процедурной библиотекой понимается библиотека, разработанная в какой-либо среде программирования с использованием Компас API.

        3. Следует различать следующие состояния процедурных библиотек:

        •зарегистрирована в Менеджере библиотек; имеется описание библиотеки в Менеджере,

        •подключена; библиотека подключена к задаче, эта библиотека отображается в меню "Библиотеки", рядом с ее названием в списке показан знак «галочка»,

        •библиотека выполняется (является текущей); вызвана команда (процедура) библиотеки или библиотека подписалась на события системы КОМПАС. В случае выполнения библиотечной команды библиотеку нельзя отключить и удалить описание библиотеки из Менеджера библиотек.

        4. Если библиотека защищена лицензией, то при подключении библиотеки она захватывает лицензию.

        ### Синтаксис Automation

        ```
        ProceduresLibraries = iObject.ProceduresLibraries       # Получить свойство (* )
        ProceduresLibraries = iObject.GetProceduresLibraries()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def SystemControlStartLibrary(self) -> IProceduresLibrary:  # ilibrarymanager_systemcontrolstartlibrary.html
        """
        ## SystemControlStartLibrary - Получить прикладную библиотеку, которая запустила SystemControlStart

        Тип данных: IProceduresLibrary

        1. Свойство позволяет вернуть интерфейс библиотеки.

        2. Библиотека во время выполнения может вызвать функцию SystemControlStart для передачи управления системе КОМПАС. После вызова этой функции выполнение библиотеки приостанавливается.

        3. Управление возвращается библиотеке при вызове функции SystemControlStop из этой же библиотеки или при выполнении соответствующей команды меню.

        ### Синтаксис Automation

        ```
        SystemControlStartLibrary = iObject.SystemControlStartLibrary       # Получить свойство (* )
        SystemControlStartLibrary = iObject.GetSystemControlStartLibrary()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def SystemControlStartResult(self) -> int:  # ilibrarymanager_systemcontrolstartresult.html
        """
        ## SystemControlStartResult - Получить результат работы SystemControlStart

        Тип данных: ksSystemControlStartEnum.

        ### Синтаксис Automation

        ```
        SystemControlStartResult = iObject.SystemControlStartResult       # Получить свойство (* )
        SystemControlStartResult = iObject.GetSystemControlStartResult()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def Visible(self) -> bool:  # ilibrarymanager_visible.html
        """
        ## Visible - Свойство видимости Менеджера библиотек

        Тип данных: BOOL

        Позволяет получить и установить свойство видимости Менеджера библиотек.

        ### Синтаксис Automation

        ```
        Visible = iObject.Visible       # Получить свойство (* )
        iObject.Visible = Visible       # Установить свойство (* )
        Visible = iObject.GetVisible()  # Получить свойство (**)
        iObject.SetVisible (Visible)    # Установить свойство (**)
        ```
        """
        ...
    @Visible.setter
    def Visible(self, value: bool): ...

    def AddFolder(self, PathFolder):  # ilibrarymanager_addfolder.html
        """
        ## AddFolder - Добавить новую папку в Менеджер библиотек

        Функция не поддерживается

        ### Синтаксис Automation

        ```
        void AddFolder (BSTR PathFolder);
        ```

        ### Входные параметры

        +----------------------------------------------------------------------------------------------------------------------------------------------------------------------+
        | PathFolder | - путь в Менеджере библиотек к папке. Если папка является вложенной, т. е. добавляется в другую папку, имена папок внутри пути отделяются символом "|". |
        +----------------------------------------------------------------------------------------------------------------------------------------------------------------------+
        """
        ...

    def RemoveFolder(self, PathFolder):  # ilibrarymanager_removefolder.html
        """
        ## RemoveFolder - Удалить папку из Менеджера библиотек

        Функция не поддерживается

        ### Синтаксис Automation

        ```
        void RemoveFolder (BSTR PathFolder);
        ```

        ### Входные параметры

        +----------------------------------------------------------------------------------------------------------------------------------------------------------------------+
        | PathFolder | - путь в Менеджере библиотек к папке. Если папка является вложенной, т. е. добавляется в другую папку, имена папок внутри пути отделяются символом "|". |
        +----------------------------------------------------------------------------------------------------------------------------------------------------------------------+
        """
        ...

    def SetCurrentLibrary(self, PVal) -> bool:  # ilibrarymanager_setcurrentlibrary.html
        """
        ## SetCurrentLibrary - Установить текущую прикладную библиотеку

        Данный метод следует использовать осторожно, иначе можно нарушить последовательность выполнения библиотек.

        Метод может быть применен к подключенной библиотеке.

        Метод позволяет остановить режим SystemControlStart, запущенный не из выполняемой (текущей) библиотеки. Для этого необходимо выполнить следующие действия:

        1. При помощи свойства SystemControlStartLibrary получить библиотеку, вызвавшую SystemControlStart.

        2. Сделать ее текущей методом SetCurrentLibrary.

        3. При помощи метода ksSystemControlStop вернуть управление библиотеке, которая запустила SystemControlStart.

        4. Восстановить в качестве текущей выполняемую ранее библиотеку.

        ### Синтаксис Automation

        ```
        BOOL SetCurrentLibrary (LPDISPATCH pVal);
        ```

        ### Входные параметры

        +----------------------------------------------------------------------------------+
        | pVal | - интерфейс библиотеки IProceduresLibrary, которую нужно сделать текущей. |
        +----------------------------------------------------------------------------------+

        ### Возвращаемое значение

        +-------------------------------------+
        | TRUE  | - библиотека стала текущей, |
        +-------------------------------------+
        | FALSE | - в случае ошибки.          |
        +-------------------------------------+
        """
        ...




class ILibrary(IKompasAPIObject):  # ilibrary.html
    """
    ## Интерфейс ILibrary

    Базовый интерфейс всех библиотек системы КОМПАС.

    Базовый интерфейс для прикладных библиотек, библиотек фрагментов и библиотек моделей. Включает в себя функционал, общий для этих библиотек. С помощью этого интерфейса можно узнать тип библиотеки, имя библиотеки, имя файла, признак лицензионной защиты, управлять подключением библиотеки.
    """

    @property
    def Attach(self) -> bool:  # ilibrary_attach.html
        """
        ## Attach - Подключение библиотеки

        Тип данных: BOOL

        Значение свойства:

        +-------------------------------------+
        | TRUE  | - библиотека подключена,    |
        +-------------------------------------+
        | FALSE | - библиотека не подключена. |
        +-------------------------------------+

        Свойство позволяет получить и управлять подключением библиотеки. При подключении библиотеки в системе КОМПАС создается объект, отвечающий за работу данной библиотеки. В Менеджере библиотек напротив имени библиотеки появляется значок «галочка». “Захватывается лицензия на библиотеку. Если это прикладная библиотека, то подключенную библиотеку можно посылать на выполнение, вызвав метод Execute. Для библиотек документов можно производить вставку документа в документ КОМПАС.

        ### Синтаксис Automation

        ```
        Attach = iObject.Attach       # Получить свойство (* )
        iObject.Attach = Attach       # Установить свойство (* )
        Attach = iObject.GetAttach()  # Получить свойство (**)
        iObject.SetAttach (Attach)    # Установить свойство (**)
        ```
        """
        ...
    @Attach.setter
    def Attach(self, value: bool): ...

    @property
    def Enable(self) -> bool:  # ilibrary_enable.html
        """
        ## Enable - Признак лицензионной защиты библиотеки

        Тип данных: BOOL

        Значения свойства:

        +-------------------------------------------------------------+
        | TRUE  | - библиотека доступна для выполнения или вставки,   |
        +-------------------------------------------------------------+
        | FALSE | - библиотека недоступна для выполнения или вставки. |
        +-------------------------------------------------------------+

        Свойство возвращает признак лицензионной защиты библиотеки. Библиотека может быть не подключена.

        ### Синтаксис Automation

        ```
        Enable = iObject.Enable       # Получить свойство (* )
        Enable = iObject.GetEnable()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def LibraryManagerFolder(self) -> str:  # ilibrary_librarymanagerfolder.html
        """
        ## LibraryManagerFolder - Папка библиотеки в Менеджере библиотек

        Тип данных: BSTR (строка)

        1. Свойство доступно только для чтения.

        2. Если библиотека зарегистрирована в Менеджере библиотек, то свойство позволяет получить в виде строки папку или папки, где библиотека располагается. Если папка является вложенной в другую папку, имена папок отделяются символом "|".

        ### Синтаксис Automation

        ```
        LibraryManagerFolder = iObject.LibraryManagerFolder         # Получить свойство (* )
        iObject->get_LibraryManagerFolder( &LibraryManagerFolder )  # Получить свойство (**)
        ```
        """
        ...

    @property
    def LibraryType(self) -> int:  # ilibrary_librarytype.html
        """
        ## LibraryType - Тип библиотеки

        Тип данных: ksLibraryTypeEnum

        Свойство возвращает тип библиотеки (прикладная, фрагментов, моделей).

        ### Синтаксис Automation

        ```
        LibraryType = iObject.LibraryType       # Получить свойство (* )
        LibraryType = iObject.GetLibraryType()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def Name(self) -> str:  # ilibrary_name.html
        """
        ## Name - Имя библиотеки

        Тип данных: BSTR (строка)

        Свойство возвращает отображаемое имя библиотеки, задаваемое при помощи функции DisplayLibraryName.

        ### Синтаксис Automation

        ```
        Name = iObject.Name       # Получить свойство (* )
        Name = iObject.GetName()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def PathName(self) -> str:  # ilibrary_pathname.html
        """
        ## PathName - Имя файла библиотеки

        Тип данных: BSTR (строка)

        Свойство возвращает имя файла библиотеки.

        ### Синтаксис Automation

        ```
        PathName = iObject.PathName       # Получить свойство (* )
        PathName = iObject.GetPathName()  # Получить свойство (**)
        ```
        """
        ...

    def Execute(self, Command, External, Post) -> bool:  # ilibrary_execute.html
        """
        ## Execute - Выполнить команду

        Метод позволяет выполнить команду прикладной библиотеки. При этом проверяется лицензионная защита, и может быть передан пользовательский интерфейс для обмена данными между библиотеками. Выполнение команды может быть отложенное или немедленное.

        Получить интерфейс для обмена данными между библиотекой в функции LibraryEntry можно с помощью свойства IProceduresLibrary::ExternalInterface

        ### Синтаксис Automation

        ```
        BOOL Execute (long command, LPDISPATCH external, VARIANT_BOOL post);
        ```

        ### Входные параметры

        +---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------+
        | command  | - номер команды,                                                                                                                                                                 |
        +---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------+
        | external | - интерфейс для обмена данными между библиотекой, вызвавшей Execute, и библиотекой, в которой реализована команда. Интерфейс определен разработчиком библиотек. Может быть NULL, |
        +---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------+
        | post     | - отложенное выполнение, если TRUE, если FALSE - немедленное.                                                                                                                    |
        +---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------+

        ### Возвращаемое значение

        +---------------------------------+
        | TRUE  | - команда выполнена.    |
        +---------------------------------+
        | FALSE | - команда не выполнена. |
        +---------------------------------+
        """
        ...




class IInsertsLibrary(ILibrary):  # iinsertslibrary.html
    """
    ## Интерфейс IInsertsLibrary

    Интерфейс библиотеки элементов (документов) Компас.

    Позволяет получить доступ к библиотеке элементов (документов) системы КОМПАС (фрагментов или моделей), редактировать структуру библиотеки, получить доступ к коллекции элементов (документов) библиотеки.

    Интерфейс библиотеки фрагментов или моделей системы КОМПАС можно получить у интерфейса IInsertsLibraries с помощью свойства IInsertsLibraries::Item.
    """

    @property
    def ActiveFolder(self) -> str:  # iinsertslibrary_activefolder.html
        """
        ## ActiveFolder - Активная папка в окне библиотеки элементов

        Функция не поддерживается

        Тип данных: BSTR (строка)

        Свойство позволяет получить активную папку в окне библиотеки.

        ### Синтаксис Automation

        ```
        ActiveFolder = iObject.ActiveFolder     # Получить свойство (* )
        ActiveFolder = iObject.GetActiveFolder  # Получить свойство (**)
        ```
        """
        ...

    @property
    def Inserts(self) -> IInserts:  # iinsertslibrary_inserts.html
        """
        ## Inserts - Коллекция элементов (документов) библиотеки

        Тип данных: указатель на интерфейс IInserts

        Свойство позволяет получить коллекцию элементов (документов) библиотеки.

        ### Синтаксис Automation

        ```
        Inserts = iObject.Inserts     # Получить свойство (* )
        Inserts = iObject.GetInserts  # Получить свойство (**)
        ```
        """
        ...

    def AddFolder(self, PathFolder) -> bool:  # iinsertslibrary_addfolder.html
        """
        ## AddFolder - Добавить папку в библиотеку элементов

        Функция не поддерживается

        Добавляет новую папку в библиотеку элементов.

        ### Синтаксис Automation

        ```
        BOOL AddFolder (BSTR PathFolder);
        ```

        ### Входные параметры

        +----------------------------------------------+
        | PathFolder | - путь к папке,                 |
        +----------------------------------------------+
        | post       | - TRUE - отложенное выполнение. |
        +----------------------------------------------+

        ### Возвращаемое значение

        +-----------------------------+
        | TRUE  | - в случае успеха,  |
        +-----------------------------+
        | FALSE | - в случае неудачи. |
        +-----------------------------+
        """
        ...

    def Delete(self) -> bool:  # iinsertslibrary_delete.html
        """
        ## Delete - Удалить описание библиотеки

        ### Синтаксис Automation

        ```
        BOOL Delete();
        ```

        ### Возвращаемое значение

        +-----------------------------+
        | TRUE  | - в случае успеха,  |
        +-----------------------------+
        | FALSE | - в случае неудачи. |
        +-----------------------------+
        """
        ...

    def RemoveFolder(self, PathFolder) -> bool:  # iinsertslibrary_removefolder.html
        """
        ## RemoveFolder - Удалить папку из библиотеки элементов

        Функция не поддерживается

        Удаляет папку из библиотеки элементов.

        ### Синтаксис Automation

        ```
        BOOL RemoveFolder (BSTR PathFolder);
        ```

        ### Входные параметры

        +----------------------------------------------+
        | PathFolder | - путь к папке,                 |
        +----------------------------------------------+
        | post       | - TRUE - отложенное выполнение. |
        +----------------------------------------------+

        ### Возвращаемое значение

        +-----------------------------+
        | TRUE  | - в случае успеха,  |
        +-----------------------------+
        | FALSE | - в случае неудачи. |
        +-----------------------------+
        """
        ...

    def SetActiveFolder(self, PathFolder, LastIsFile, HaveLibName):  # iinsertslibrary_setactivefolder.html
        """
        ## SetActiveFolder - Установить активную папку в окне библиотеки элементов (документов)

        Функция не поддерживается

        Пример. Библиотека с именем 111, содержит папку с именем ddd, в которой находится файл с именем fragment. Путь к этому файлу должен быть задан следующим образом: 111|ddd|fragment.

        Метод позволяет установить активную папку в окне библиотеки документов. Путь к папке может содержать или не содержать имя библиотеки и имя документа. Имя библиотеки, имя папки и имя документа разделяются символом "|". Если папка является вложенной, т. е. находится внутри другой папки, названия папок в пути отделяются символом "|".

        ### Синтаксис Automation

        ```
        BOOL SetActiveFolder (BSTR PathFolder, BOOL LastIsFile, BOOL HaveLibName);
        ```

        ### Входные параметры

        +----------------------------------------------------------------+
        | PathFolder        | - путь к папке,                            |
        +----------------------------------------------------------------+
        | LastIsFile        | TRUE - в пути имеется имя файла документа, |
        +----------------------------------------------------------------+
        | HaveLibName(BOOL) | - TRUE - в пути имеется имя библиотеки.    |
        +----------------------------------------------------------------+

        ### Возвращаемое значение

        +-----------------------------+
        | TRUE  | - в случае успеха,  |
        +-----------------------------+
        | FALSE | - в случае неудачи. |
        +-----------------------------+
        """
        ...




class IProceduresLibrary(ILibrary):  # iprocedureslibrary.html
    """
    ## Интерфейс IProceduresLibrary

    Интерфейс прикладной библиотеки КОМПАС.

    1. Интерфейс позволяет работать с прикладной библиотекой (*.rtw или *.dll), зарегистрированной в Менеджере библиотек. Можно получить стиль отображения библиотеки (панель, окно, диалог, меню) и массив процедур, реализованных в библиотеке.

    2. Данный интерфейс можно получить следующими способами:

    •у интерфейса коллекции прикладных библиотек IProceduresLibraries c помощью свойства IProceduresLibraries::Item,

    •у интерфейса ILibraryManager с помощью свойства ILibraryManager::CurrentLibrary,

    ILibraryManager::SystemControlStartLibrary.
    """

    @property
    def AddIns(self) -> bool:  # iprocedureslibrary_addins.html
        """
        ## AddIns - Признак AddIn-библиотеки

        Тип данных: BOOL

        Если библиотека является расширением, свойство вернет TRUE.

        ### Синтаксис Automation

        ```
        AddIns = iObject.AddIns     # Получить свойство (* )
        AddIns = iObject.GetAddIns  # Получить свойство (**)
        ```
        """
        ...

    @property
    def CurrentCommand(self) -> int:  # iprocedureslibrary_currentcommand.html
        """
        ## CurrentCommand - Текущая команда библиотеки

        Тип данных: long

        Свойство позволяет получить текущую команду библиотеки. Текущая команда - это команда, которая выполняется в данный момент времени.

        ### Синтаксис Automation

        ```
        CurrentCommand = iObject.CurrentCommand     # Получить свойство (* )
        CurrentCommand = iObject.GetCurrentCommand  # Получить свойство (**)
        ```
        """
        ...

    @property
    def Executable(self) -> bool:  # iprocedureslibrary_executable.html
        """
        ## Executable - Признак выполнения библиотеки

        Тип данных: BOOL

        Если выполняется команда библиотеки или отрабатывается событие, на которое подписалась библиотека, то свойство вернет TRUE.

        ### Синтаксис Automation

        ```
        Executable = iObject.Executable       # Получить свойство (* )
        Executable = iObject.GetExecutable()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def ExternalInterface(self) -> IDispatch:  # iprocedureslibrary_externalinterface.html
        """
        ## ExternalInterface - Получить указатель внешнего интерфейса

        Тип данных: Указатель на интерфейс IDispatch

        1. Свойство доступно только для чтения.

        2. Addref на выдаваемый интерфейс не выполняется.

        3. Внешний интерфейс можно использовать для обмена данными между двумя приложениями. Первое приложение передает интерфейс при вызове метода ILibrary::Execute.

        4. После выполнения ILibrary::Execute свойство ExternalInterface возвращает NULL.

        ### Синтаксис Automation

        ```
        ExternalInterface = Object.ExternalInterface       # Получить свойство (* )
        ExternalInterface = Object.GetExternalInterface()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def IconsFont(self) -> str:  # iprocedureslibrary_iconsfont.html
        """
        ## IconsFont - Имя файла шрифта для панели команд библиотеки

        Тип данных: BSTR

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        IconsFont = Object.IconsFont       # Получить свойство (* )
        IconsFont = Object.GetIconsFont()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def LibraryName(self) -> str:  # iprocedureslibrary_libraryname.html
        """
        ## LibraryName - Имя библиотеки, возвращаемое функцией LIBRARYNAME или LIBRARYID

        Тип данных: BSTR

        Свойство возвращает имя библиотеки, возвращаемое функцией LIBRARYNAME или LIBRARYID.

        Отображаемое имя библиотеки может отличаться, если в библиотеке реализована функция DisplayLibraryName.

        Для получения отображаемого имени библиотеки используется свойство ILibrary::Name.

        При получении библиотеки по имени из коллекции можно использовать как отображаемое, так и внутреннее имя.

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        LibraryName = Object.LibraryName       # Получить свойство (* )
        LibraryName = Object.GetLibraryName()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def Procedures(self) -> IProcedures:  # iprocedureslibrary_procedures.html
        """
        ## Procedures - Коллекция процедур библиотеки

        Тип данных: указатель на интерфейс IProcedures

        Свойство позволяет получить коллекцию процедур библиотеки.

        ### Синтаксис Automation

        ```
        Procedures = iObject.Procedures     # Получить свойство (* )
        Procedures = iObject.GetProcedures  # Получить свойство (**)
        ```
        """
        ...

    @property
    def Style(self) -> int:  # iprocedureslibrary_style.html
        """
        ## Style - Стиль отображения библиотеки

        Тип данных: ksLibraryStyleEnum

        Позволяет получить и установить стиль отображения библиотеки.

        ### Синтаксис Automation

        ```
        Style = iObject.Style       # Получить свойство (* )
        iObject.Style = Style       # Установить свойство (* )
        Style = iObject.GetStyle()  # Получить свойство (**)
        iObject.SetStyle (Style)    # Установить свойство (**)
        ```
        """
        ...
    @Style.setter
    def Style(self, value: int): ...

    @property
    def UniqueId(self) -> int:  # iprocedureslibrary_uniqueid.html
        """
        ## UniqueId - Идентификатор библиотеки

        Тип данных: long

        Свойство позволяет получить идентификатор библиотеки. Идентификатор присваивается библиотеке, когда она подключена к системе. В этот момент библиотека отображается в меню Библиотеки и в Менеджере библиотек рядом с именем библиотеки взводится «галочка». У подключенной библиотеки можно выполнять команды (процедуры).

        ### Синтаксис Automation

        ```
        UniqueId = iObject.UniqueId     # Получить свойство (* )
        UniqueId = iObject.GetUniqueId  # Получить свойство (**)
        ```
        """
        ...

    def Delete(self) -> bool:  # iprocedureslibrary_delete.html
        """
        ## Delete - Удалить описание библиотеки

        ### Синтаксис Automation

        ```
        BOOL Delete();
        ```

        ### Возвращаемое значение

        +-----------------------------+
        | TRUE  | - в случае успеха,  |
        +-----------------------------+
        | FALSE | - в случае неудачи. |
        +-----------------------------+
        """
        ...

    def OpenHelp(self, HelpId) -> bool:  # iprocedureslibrary_openhelp.html
        """
        ## OpenHelp - Открыть справку приложения

        ### Синтаксис Automation

        ```
        BOOL OpenHelp( long HelpId );
        ```

        ### Входные параметры

        +--------------------------------------------+
        | HelpId | - идентификатор страницы справки. |
        +--------------------------------------------+

        Версия КОМПАС v22

        ### Возвращаемое значение

        +-----------------------------+
        | TRUE  | - в случае успеха,  |
        +-----------------------------+
        | FALSE | - в случае неудачи. |
        +-----------------------------+
        """
        ...

    def SystemControlStart(self, menuCommand) -> int:  # iprocedureslibrary_systemcontrolstart.html
        """
        ## SystemControlStart - Передать управление системе из под библиотеки

        Библиотечная функция отдает управление КОМПАС-ГРАФИК для интерактивной доработки документа. Возврат в библиотечную функцию будет осуществлен после нажатия команды в меню Библиотеки, обозначенной строкой menuCommand.

        Если строка не задана, то ей автоматически будет присвоено значение "Вернуться в библиотеку" (функция вернет ksSCStoppedByMenuCommand). Или при вызове из этой же библиотеки SystemControlStop (функция вернет ksSCStopItself). Библиотека, находясь в этом режиме, как бы замирает на вызове SystemControlStart. Если этот режим вызван в другой библиотеке, то в данной библиотеке режим выполняться не будет (функция вернет ksSCStartedByAnotherLibrary). Только одна библиотека может находиться в режиме SystemControlStart.

        ### Синтаксис Automation

        ```
        ksSystemControlStartEnum SystemControlStart( BSTR menuCommand );
        ```

        ### Входные параметры

        +----------------------------------------------+
        | PathFolder | - путь к папке,                 |
        +----------------------------------------------+
        | post       | - TRUE - отложенное выполнение. |
        +----------------------------------------------+

        ### Возвращаемое значение

        ksSystemControlStartEnum
        """
        ...

    def SystemControlStop(self) -> bool:  # iprocedureslibrary_systemcontrolstop.html
        """
        ## SystemControlStop - Вернуть управление в библиотеку

        Метод позволяет вернуть управление в библиотеку, если она находится в режиме SystemControlStart.

        ### Синтаксис Automation

        ```
        BOOL SystemControlStop();
        ```

        ### Возвращаемое значение

        +-----------------------------+
        | TRUE  | - в случае успеха,  |
        +-----------------------------+
        | FALSE | - в случае неудачи. |
        +-----------------------------+
        """
        ...




class IProcedure(IKompasAPIObject):  # iprocedure.html
    """
    ## Интерфейс IProcedure

    Интерфейс команды прикладной библиотеки КОМПАС.

    1. Интерфейс позволяет получить имя, идентификатор команды, ее папку в библиотеке, выполнить команду.

    2. Данный интерфейс может быть получен у интерфейса коллекции команд IProcedures с помощью свойств IProcedures::Item, IProcedures::ItemByID.
    """

    @property
    def Id(self) -> int:  # iprocedure_id.html
        """
        ## ID - Идентификатор команды в прикладной библиотеке

        Тип данных: long

        Позволяет получить идентификатор команды в прикладной библиотеке.

        ### Синтаксис Automation

        ```
        ID = iObject.ID       # Получить свойство (* )
        ID = iObject.GetID()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def LibraryFolder(self) -> str:  # iprocedure_libraryfolder.html
        """
        ## LibraryFolder - Папка в прикладной библиотеке

        Тип данных: строка

        Позволяет получить папку в прикладной библиотеке.

        ### Синтаксис Automation

        ```
        LibraryFolder = iObject.LibraryFolder       # Получить свойство (* )
        LibraryFolder = iObject.GetLibraryFolder()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def Name(self) -> str:  # iprocedure_name.html
        """
        ## Name - Имя команды

        Тип данных: строка

        Позволяет получить имя команды.

        ### Синтаксис Automation

        ```
        Name = iObject.Name       # Получить свойство (* )
        Name = iObject.GetName()  # Получить свойство (**)
        ```
        """
        ...

    def Execute(self, External, Post) -> bool:  # iprocedure_execute.html
        """
        ## Execute - Выполнить команду

        Метод позволяет выполнить команду прикладной библиотеки. При этом проверяется лицензионная защита и может быть передан пользовательский интерфейс для обмена данными между библиотеками. Выполнение команды может быть отложенное или немедленное.

        ### Синтаксис Automation

        ```
        BOOL Execute (LPDISPATCH external, BOOL post);
        ```

        ### Входные параметры

        +---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------+
        | external | - интерфейс для обмена данными между библиотекой, вызвавшей Execute, и библиотекой, в которой реализована команда. Интерфейс определен разработчиком библиотек. Может быть NULL, |
        +---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------+
        | post     | - TRUE - отложенное выполнение.                                                                                                                                                  |
        +---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------+

        ### Возвращаемое значение

        +---------------------------------+
        | TRUE  | - команда выполнена,    |
        +---------------------------------+
        | FALSE | - команда не выполнена. |
        +---------------------------------+
        """
        ...




class IInsert(IKompasAPIObject):  # iinsert.html
    """
    ## Интерфейс IInsert

    Интерфейс элемента (фрагмента или модели) библиотеки элементов.

    Позволяет получить доступ к документу-вставке в библиотеку элементов (фрагментов или моделей).

    Данный интерфейс можно получить у интерфейса IInserts с помощью свойства IInserts::Item или метода IInserts::Add.
    """

    @property
    def Comment(self) -> typing.Any:  # iinsert_comment.html
        """
        ## Comment - Комментарий

        Тип данных: VARIANT

        Позволяет получить и установить комментарий для вставки. Комментарий может быть многострочным. Поэтому свойство возвращает или принимает переменную типа VARIANT на безопасный массив строк SAFEARRAY (VT_ARRAY | VT_BSTR).

        ### Синтаксис Automation

        ```
        Comment = iObject.Comment       # Получить свойство (* )
        iObject.Comment = Comment       # Установить свойство (* )
        Comment = iObject.GetComment()  # Получить свойство (**)
        iObject.SetComment (Comment)    # Установить свойство (**)
        ```
        """
        ...
    @Comment.setter
    def Comment(self, value: typing.Any): ...

    @property
    def InsertType(self) -> int:  # iinsert_inserttype.html
        """
        ## InsertType - Тип элемента

        Тип данных: из перечисления ksDocumentsLibraryInsertionTypeEnum

        Свойство доступно только для чтения

        ### Синтаксис Automation

        ```
        InsertType = Object.InsertType       # Получить свойство (* )
        InsertType = Object.GetInsertType()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def LibraryFolder(self) -> str:  # iinsert_libraryfolder.html
        """
        ## LibraryFolder - Папка в библиотеке элементов

        Тип данных: строка

        Свойство позволяет получить папку вставки внутри библиотеки. Папки отделяются символом "|".

        Пример "детали|литье|фланец".

        ### Синтаксис Automation

        ```
        LibraryFolder = iObject.LibraryFolder       # Получить свойство (* )
        LibraryFolder = iObject.GetLibraryFolder()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def Name(self) -> str:  # iinsert_name.html
        """
        ## Name - Имя элемента

        Тип данных: строка

        Позволяет получить и установить имя вставки.

        ### Синтаксис Automation

        ```
        Name = iObject.Name       # Получить свойство (* )
        iObject.Name = Name       # Установить свойство (* )
        Name = iObject.GetName()  # Получить свойство (**)
        iObject.SetName (Name)    # Установить свойство (**)
        ```
        """
        ...
    @Name.setter
    def Name(self, value: str): ...

    @property
    def PathName(self) -> str:  # iinsert_pathname.html
        """
        ## PathName - Полное имя элемента

        Тип данных: строка

        Свойство позволяет получить полное имя вставки вместе с папками внутри библиотеки. Папки отделяются символом "|".

        Пример "детали|литье|фланец".

        ### Синтаксис Automation

        ```
        PathName = iObject.PathName()     # Получить свойство (* )
        PathName = iObject.GetPathName()  # Получить свойство (**)
        ```
        """
        ...

    def Delete(self) -> bool:  # iinsert_delete.html
        """
        ## Delete - Удалить элемент из библиотеки

        Функция не поддерживается

        Метод позволяет удалить вставку из библиотеки документов.

        ### Синтаксис Automation

        ```
        BOOL Delete();
        ```

        ### Возвращаемое значение

        +-----------------------------+
        | TRUE  | - в случае успеха,  |
        +-----------------------------+
        | FALSE | - в случае неудачи. |
        +-----------------------------+
        """
        ...

    def Edit(self) -> IKompasDocument:  # iinsert_edit.html
        """
        ## Edit - Редактировать элемент

        Функция не поддерживается

        Метод позволяет создать и добавить в библиотеку документ-вставку определенного типа. В библиотеки фрагментов можно вставлять фрагменты, в библиотеки моделей, как 3D модели, так и сборки. Имя вставки и папки в имени вставки отделяются символом "|".

        Пример "детали|литье|фланец".

        ### Синтаксис Automation

        ```
        IKompasDocument* Edit();
        ```

        ### Возвращаемое значение

        Указатель на интерфейс IKompasDocument созданной вставки.
        """
        ...




class ICheckSum(IKompasAPIObject):  # ichecksum.html
    """
    ## Интерфейс ICheckSum

    Интерфейс контрольной суммы.

    Интерфейс позволяет формировать контрольную сумму с последовательности параметров, получаемых в прикладной библиотеке, и контролировать её работоспособность. Запись в файл полученной контрольной суммы после выполнения заданной последовательности команд библиотеки с последующим сравнением значений контрольной суммы, полученных в разное время, позволяет контролировать повторяемость результатов - получения одних и тех же параметров, а в случае внесения изменений в текст библиотеки - обнаружить скрытые ошибки (ошибки, приведшие к изменению получаемых параметров и, соответственно, изменению контрольной суммы). Если в библиотеке выполняется анализ документа, рекомендуется выполнять тестовые запуски библиотеки на одних и тех же документах.

    Данный интерфейс можно получить от интерфейса IApplicationI, используя свойство IApplication::CheckSum.
    """

    @property
    def Result(self) -> typing.Any:  # ichecksum_result.html
        """
        ## Result - Получить контрольную сумму в виде массива SAFEARRAY значений типа BYTE (VT_ARRAY | VT_UI1)

        Тип данных: VARIANT

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        Result = iObject.Result       # Получить свойство (* )
        Result = iObject.GetResult()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def StrResult(self) -> str:  # ichecksum_strresult.html
        """
        ## StrResult- Получить контрольную сумму в виде строки

        Тип данных: BSTR (строка)

        1. Свойство доступно только для чтения.

        2. Строка контрольной суммы представляет собой последовательность из 32 буквенных латинских символов. Строка инициализированной (очищенной) контрольной суммы представляет собой последовательность aaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa.

        ### Синтаксис Automation

        ```
        StrResult = iObject.StrResult       # Получить свойство (* )
        StrResult = iObject.GetStrResult()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def Version(self) -> int:  # ichecksum_version.html
        """
        ## Version - Версия контрольной суммы

        Тип данных: long

        Версия контрольной суммы это число, равное версии потока КОМПАС. ICheckSum используется для автоматизированного тестирования функционала.

        Смысл тестирования заключается в получении значения контрольной суммы на тесте и сравнения его с эталонным. Контрольные суммы должны совпадать.

        Причины различий могут быть следующими:

        •ошибка в функционале,

        •развитие функционала.

        Введение свойства Version позволяет установить причину несовпадения контрольных сумм.

        До введения свойства развитие функционала воспринималось как его ошибка.

        Отслеживание версии контрольной суммы начинается с версии потока 0x0A001023L, что соответствует версии КОМПАС V10 SP2.

        Получаем версию контрольной суммы всегда как версию потока КОМПАС.

        Задать версию контрольной суммы можно следующими способами:

        •непосредственно числом (версия потока),

        •из перечисления ksCheckSumVersionEnum.

        При получении интерфейса ICheckSum версия контрольной суммы инициируется версией потока текущей задачи КОМПАС.

        ### Синтаксис Automation

        ```
        Version = iObject.Version       # Получить свойство (* )
        iObject.Version = Version       # Установить свойство (* )
        Version = iObject.GetVersion()  # Получить свойство (**)
        iObject.SetVersion(Version)     # Установить свойство (**)
        ```
        """
        ...
    @Version.setter
    def Version(self, value: int): ...

    def Add(self, Value, Com):  # ichecksum_add.html
        """
        ## Add - Добавить параметр к контрольной сумме

        Параметр value может принимать значения следующих типов:

        •VT_INT,

        •VT_I2,

        •VT_I4,

        •VT_R4,

        •VT_R8,

        •VT_UI1,

        •VT_UINT,

        •VT_BOOL,

        •VT_BSTR,

        •VT_ARRAY | VT_UI1.

        ### Синтаксис Automation

        ```
        void Add (VARIANT value, BOOL order);
        ```

        ### Входные параметры

        +---------------------------------------------------------------------------------------------------------------------------------------------------------+
        | value | - добавляемое значение,                                                                                                                         |
        +---------------------------------------------------------------------------------------------------------------------------------------------------------+
        | order | - формировать суммарную контрольную сумму значений: TRUE - с учетом порядка следования значений, FALSE - без учета порядка следования значений. |
        +---------------------------------------------------------------------------------------------------------------------------------------------------------+
        """
        ...

    def AddInterface(self, Object, Com) -> bool:  # ichecksum_addinterface.html
        """
        ## AddInterface - Добавить к контрольной сумме параметр, получаемый из объекта по его интерфейсу

        1. Метод получает значение контрольной суммы с присланного объекта по его интерфейсу и добавляет полученное значение в суммарную контрольную сумму.

        2. Метод рекомендуется только для контроля интерфейсов API. Значение добавляемой контрольной суммы зависит от реализации интерфейса, и при развитии интерфейса (увеличении количества свойств и методов) контрольная сумма изменяется.

        3. Метод реализован не для всех интерфейсов API.

        ### Синтаксис Automation

        ```
        BOOL AddInterface (LPUNKNOWN object, BOOL order);
        ```

        ### Входные параметры

        +----------------------------------------------------------------------------------------------------------------------------------------------------------+
        | object | - указатель на интерфейс,                                                                                                                       |
        +----------------------------------------------------------------------------------------------------------------------------------------------------------+
        | order  | - формировать суммарную контрольную сумму значений: TRUE - с учетом порядка следования значений, FALSE - без учета порядка следования значений. |
        +----------------------------------------------------------------------------------------------------------------------------------------------------------+

        ### Возвращаемое значение

        +-----------------------------------------------------------------------------------------------------------------------------------------------+
        | TRUE  | - в случае успешного получения контрольной суммы присланного объекта и добавления полученного значения в суммарную контрольную сумму, |
        +-----------------------------------------------------------------------------------------------------------------------------------------------+
        | FALSE | - в случае неудачи.                                                                                                                   |
        +-----------------------------------------------------------------------------------------------------------------------------------------------+
        """
        ...

    def AddReference(self, Object, Doc, Com) -> bool:  # ichecksum_addreference.html
        """
        ## AddReference - Добавить к контрольной сумме параметр, получаемый из объекта по его указателю reference

        1. Метод получает значение контрольной суммы с присланного объекта по значению его указателя reference и добавляет полученное значение в суммарную контрольную сумму.

        2. Метод рекомендуется только для контроля API. Значение добавляемой контрольной суммы зависит от реализации API, со временем добавляемая контрольная сумма изменяется.

        ### Синтаксис Automation

        ```
        BOOL AddReference (long object, long doc, BOOL order);
        ```

        ### Входные параметры

        +----------------------------------------------------------------------------------------------------------------------------------------------------------+
        | object | - указатель reference объекта,                                                                                                                  |
        +----------------------------------------------------------------------------------------------------------------------------------------------------------+
        | doc    | - указатель reference документа,                                                                                                                |
        +----------------------------------------------------------------------------------------------------------------------------------------------------------+
        | order  | - формировать суммарную контрольную сумму значений: TRUE - с учетом порядка следования значений, FALSE - без учета порядка следования значений. |
        +----------------------------------------------------------------------------------------------------------------------------------------------------------+

        ### Возвращаемое значение

        +-----------------------------------------------------------------------------------------------------------------------------------------------+
        | TRUE  | - в случае успешного получения контрольной суммы присланного объекта и добавления полученного значения в суммарную контрольную сумму, |
        +-----------------------------------------------------------------------------------------------------------------------------------------------+
        | FALSE | - в случае неудачи.                                                                                                                   |
        +-----------------------------------------------------------------------------------------------------------------------------------------------+
        """
        ...

    def Clear(self):  # ichecksum_clear.html
        """
        ## Clear- Очистить контрольную сумму

        Строка инициализированной (очищенной) контрольной суммы представляет собой последовательность aaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa.

        ### Синтаксис Automation

        ```
        void Clear();
        ```
        """
        ...




class IInserts(IKompasCollection):  # iinserts.html
    """
    ## Интерфейс IInserts

    Интерфейс коллекции элементов(документов) библиотеки.

    Интерфейс позволяет получить документы, входящие в библиотеку. Интерфейс можно получить при помощи свойства IInsertsLibrary::Inserts от интерфейса библиотеки элементов IInsertsLibrary.
    """

    def Add(self, Name, DocType) -> IInsert:  # iinserts_add.html
        """
        ## Add - Создать и добавить элемент в коллекцию

        Функция не поддерживается

        Метод позволяет создать и добавить в библиотеку документ-вставку определенного типа. В библиотеки фрагментов можно вставлять фрагменты, в библиотеки моделей, как 3D модели, так и сборки. Имя вставки и названия папок в имени вставки отделяются символом "|".

        Пример "детали|литье|фланец".

        ### Синтаксис Automation

        ```
        IInsert* Add( BSTR Name, DocumentTypeEnum docType );
        ```

        ### Входные параметры

        +-------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------+
        | Name                       | - имя вставки в библиотеке.                                                                                                                                                                      |
        +-------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------+
        | docType (DocumentTypeEnum) | - тип документа. Актуально для библиотек моделей (*.l3d). В эти библиотеки можно добавлять как сборки, так и модели. Если docType==ksDocumentPart, то это признак, что нужно добавить 3D модель. |
        +-------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------+

        ### Возвращаемое значение

        - интерфейс IInsert созданной вставки.
        """
        ...

    def Item(self, Index) -> IInsert:  # iinserts_item.html
        """
        ## Item - Элемент, заданный по имени или по индексу

        Тип данных: указатель на интерфейс элемента (документа) в библиотеке элементов IInsert

        Свойство позволяет получить интерфейс вставки библиотеки документов по имени документа или по индексу документа в коллекции вставок.

        ### Синтаксис Automation

        ```
        Item = iObject.Item (v)     # Получить свойство (* )
        Item = iObject.GetItem (v)  # Получить свойство (**)
        ```

        ### Входные параметры

        +------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------+
        | Index (VARIANT) | Index ( VARIANT ) - если передано целочисленное значение, то оно интерпретируется, как индекс документа в коллекции, - если передано строковое значение, то оно интерпретируется, как имя документа. |
        +------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------+
        """
        ...




class IInsertsLibraries(IKompasCollection):  # iinsertslibraries.html
    """
    ## Интерфейс IInsertsLibraries

    Интерфейс коллекции библиотек элементов (документов) системы КОМПАС.

    Позволяет получить коллекцию библиотек элементов (документов) системы КОМПАС (фрагментов или моделей) и получить интерфейс каждой из библиотек коллекции.

    1. Интерфейс коллекции библиотек фрагментов системы КОМПАС можно получить у интерфейса ILibraryManager с помощью свойства ILibraryManager::FragmentsLibraries.

    2. Интерфейс на коллекцию библиотек моделей системы КОМПАС можно получить у интерфейса ILibraryManager с помощью свойства ILibraryManager::ModelsLibraries.
    """

    def Add(self, PathName, LibManagerFolder) -> IInsertsLibrary:  # iinsertslibraries_add.html
        """
        ## Add - Добавить элемент в коллекцию

        ### Синтаксис Automation

        ```
        LPDISPATCH Add( LPCTSTR PathName, LPCTSTR LibManagerFolder );
        ```

        ### Входные параметры

        +----------------------------------------------------+
        | PathName         | - путь к библиотеке документов, |
        +----------------------------------------------------+
        | LibManagerFolder | - раздел в менеджере библиотек. |
        +----------------------------------------------------+

        ### Возвращаемое значение

        - указатель на интерфейс библиотеки документов IInsertsLibrary, библиотека регистрируется в менеджере библиотек (В V17 Конфигуратор).
        """
        ...

    def Item(self, Index) -> IInsertsLibrary:  # iinsertslibraries_item.html
        """
        ## Item - Элемент, заданный по имени или по индексу

        Тип данных: указатель на интерфейс IInsertsLibrary

        Свойство позволяет получить интерфейс библиотеки документов по имени библиотеки или по индексу библиотеки в коллекции библиотек.

        ### Синтаксис Automation

        ```
        Item = iObject.Item (v)     # Получить свойство (* )
        Item = iObject.GetItem (v)  # Получить свойство (**)
        ```

        ### Входные параметры

        +------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------+
        | Index (VARIANT) | - если передано целочисленное значение, то оно интерпретируется как индекс библиотеки в коллекции, - если передано строковое значение, то оно интерпретируется как имя библиотеки. |
        +------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------+
        """
        ...




class IProcedures(IKompasCollection):  # iprocedures.html
    """
    ## Интерфейс IProcedures

    Интерфейс коллекции процедур прикладной библиотеки КОМПАС.

    Интерфейс позволяет получить команды, входящие в библиотеку процедур.

    Интерфейс можно получить при помощи свойства IProceduresLibrary::Procedures у интерфейса прикладной библиотеки IProceduresLibrary.
    """

    def Item(self, Index) -> IProcedure:  # iprocedures_item.html
        """
        ## Item - Команда, заданная по имени или по индексу

        Тип данных: указатель на интерфейс IProcedure

        Свойство позволяет получить интерфейс библиотечной команды по имени или по индексу в коллекции команд.

        ### Синтаксис Automation

        ```
        Item = iObject.Item (v)     # Получить свойство (* )
        Item = iObject.GetItem (v)  # Получить свойство (**)
        ```

        ### Входные параметры

        +-----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------+
        | Index (VARIANT) | Index ( VARIANT ), - если передано целочисленное значение, то оно интерпретируется как индекс процедуры в коллекции, - если передано строковое значение, то оно интерпретируется как имя процедуры. |
        +-----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------+
        """
        ...

    def ItemByID(self, Id) -> IProcedure:  # iprocedures_itembyid.html
        """
        ## ItemByID - Команда, заданная по идентификатору команды

        Тип данных: указатель на интерфейс IProcedure

        Свойство позволяет получить интерфейс библиотечной команды по идентификатору команды в коллекции команд.

        ### Синтаксис Automation

        ```
        Item = iObject.ItemByID (id)     # Получить свойство (* )
        Item = iObject.GetItemByID (id)  # Получить свойство (**)
        ```

        ### Входные параметры

        +------------------------------------+
        | ID (ID) | - идентификатор команды. |
        +------------------------------------+
        """
        ...




class IProceduresLibraries(IKompasCollection):  # iprocedureslibraries.html
    """
    ## Интерфейс IProceduresLibraries

    Интерфейс коллекции прикладных библиотек КОМПАС.

    1. Интерфейс позволяет получить прикладные библиотеки, зарегистрированные в Менеджере библиотек.

    2. Данный интерфейс можно получить у интерфейса менеджера библиотек ILibraryManager с помощью свойства ILibraryManager::ProceduresLibraries.
    """

    def Add(self, PathName, LibManagerFolder) -> IProceduresLibrary:  # iprocedureslibraries_add.html
        """
        ## Add - Добавить элемент в коллекцию

        ### Синтаксис Automation

        ```
        LPDISPATCH Add( LPCTSTR PathName, LPCTSTR LibManagerFolder );
        ```

        ### Входные параметры

        +----------------------------------------------------+
        | PathName         | - путь к библиотеке документов, |
        +----------------------------------------------------+
        | LibManagerFolder | - раздел в менеджере библиотек. |
        +----------------------------------------------------+

        ### Возвращаемое значение

        - указатель на интерфейс библиотеки документов IProceduresLibrary, библиотека регистрируется в менеджере библиотек (В V17 Конфигуратор).
        """
        ...

    def Item(self, Index) -> IProceduresLibrary:  # iprocedureslibraries_item.html
        """
        ## Item - Прикладная библиотека, заданная по имени или по индексу

        Тип данных: указатель на интерфейс IProceduresLibrary

        Свойство позволяет получить интерфейс прикладной библиотеки по имени библиотеки или по индексу библиотеки в коллекции библиотек.

        ### Синтаксис Automation

        ```
        Item = iObject.Item (v)     # Получить свойство (* )
        Item = iObject.GetItem (v)  # Получить свойство (**)
        ```

        ### Входные параметры

        +--------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------+
        | v (VARIANT) | - если передано целочисленное значение, то оно интерпретируется как индекс библиотеки в коллекции, - если передано строковое значение, то оно интерпретируется как имя библиотеки. |
        +--------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------+
        """
        ...




class IDrawingDocumentSettings(IDispatch):  # idrawingdocumentsettings.html
    """
    ## Интерфейс IDrawingDocumentSettings

    Настройки чертежа.

    Настройки могут быть для текущего документа (можно получить у документа) и для новых документов (можно получить у приложения).

    Данный интерфейс можно получить от интерфейса документа IKompasDocument с помощью свойства IKompasDocument::DocumentSettings с последующим использованием метода IUnknown QueryInterface для преобразования от интерфейса IDocumentSettings к данному интерфейсу.
    """

    @property
    def EmbodimentsTableAddNumberInStamp(self) -> bool:  # idrawingdocumentsettings_embodimentstableaddnumberinstamp.html
        """
        ## EmbodimentsTableAddNumberInStamp - Добавлять номер таблицы исполнений при создании ссылки в штампе

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        EmbodimentsTableAddNumberInStamp = Object.EmbodimentsTableAddNumberInStamp       # Получить свойство (* )
        Object.EmbodimentsTableAddNumberInStamp = EmbodimentsTableAddNumberInStamp       # Установить свойство (* )
        EmbodimentsTableAddNumberInStamp = Object.GetEmbodimentsTableAddNumberInStamp()  # Получить свойство (**)
        Object.SetEmbodimentsTableAddNumberInStamp( EmbodimentsTableAddNumberInStamp )   # Установить свойство (**)
        ```
        """
        ...
    @EmbodimentsTableAddNumberInStamp.setter
    def EmbodimentsTableAddNumberInStamp(self, value: bool): ...

    @property
    def EmbodimentsTableAutoFontParamInStamp(self) -> bool:  # idrawingdocumentsettings_embodimentstableautofontparaminstamp.html
        """
        ## EmbodimentsTableAutoFontParamInStamp - Подбирать параметры шрифта при создании ссылки на ассоциативные таблицы исполнений в штампе

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        EmbodimentsTableAutoFontParamInStamp = Object.EmbodimentsTableAutoFontParamInStamp       # Получить свойство (* )
        Object.EmbodimentsTableAutoFontParamInStamp = EmbodimentsTableAutoFontParamInStamp       # Установить свойство (* )
        EmbodimentsTableAutoFontParamInStamp = Object.GetEmbodimentsTableAutoFontParamInStamp()  # Получить свойство (**)
        Object.SetEmbodimentsTableAutoFontParamInStamp( EmbodimentsTableAutoFontParamInStamp )   # Установить свойство (**)
        ```
        """
        ...
    @EmbodimentsTableAutoFontParamInStamp.setter
    def EmbodimentsTableAutoFontParamInStamp(self, value: bool): ...

    @property
    def EmbodimentsTableCreateLinkInStamp(self) -> bool:  # idrawingdocumentsettings_embodimentstablecreatelinkinstamp.html
        """
        ## EmbodimentsTableCreateLinkInStamp - Добавлять ссылки на таблицы исполнений в штамп

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        EmbodimentsTableCreateLinkInStamp = Object.EmbodimentsTableCreateLinkInStamp       # Получить свойство (* )
        Object.EmbodimentsTableCreateLinkInStamp = EmbodimentsTableCreateLinkInStamp       # Установить свойство (* )
        EmbodimentsTableCreateLinkInStamp = Object.GetEmbodimentsTableCreateLinkInStamp()  # Получить свойство (**)
        Object.SetEmbodimentsTableCreateLinkInStamp( EmbodimentsTableCreateLinkInStamp )   # Установить свойство (**)
        ```
        """
        ...
    @EmbodimentsTableCreateLinkInStamp.setter
    def EmbodimentsTableCreateLinkInStamp(self, value: bool): ...

    @property
    def EmbodimentsTableTextInStamp(self) -> str:  # idrawingdocumentsettings_embodimentstabletextinstamp.html
        """
        ## EmbodimentsTableTextInStamp - Текст, добавляемый в штамп, при создании ссылки на таблицу исполнений

        Тип данных: BSTR

        ### Синтаксис Automation

        ```
        EmbodimentsTableTextInStamp = Object.EmbodimentsTableTextInStamp       # Получить свойство (* )
        Object.EmbodimentsTableTextInStamp = EmbodimentsTableTextInStamp       # Установить свойство (* )
        EmbodimentsTableTextInStamp = Object.GetEmbodimentsTableTextInStamp()  # Получить свойство (**)
        Object.SetEmbodimentsTableTextInStamp( EmbodimentsTableTextInStamp )   # Установить свойство (**)
        ```
        """
        ...
    @EmbodimentsTableTextInStamp.setter
    def EmbodimentsTableTextInStamp(self, value: str): ...

    @property
    def SheetAutoCount(self) -> bool:  # idrawingdocumentsettings_sheetautocount.html
        """
        ## SheetAutoCount - Автоматическое определение количества листов

        Тип данных: BOOL

        Свойство, позволяющее автоматически подсчитать фактическое количество листов текущего документа и занести полученное число в соответствующую графу основной надписи.

        ### Синтаксис Automation

        ```
        SheetAutoCount = Object.SheetAutoCount       # Получить свойство (* )
        Object.SheetAutoCount = SheetAutoCount       # Установить свойство (* )
        SheetAutoCount = Object.GetSheetAutoCount()  # Получить свойство (**)
        Object.SetSheetAutoCount( SheetAutoCount )   # Установить свойство (**)
        ```
        """
        ...
    @SheetAutoCount.setter
    def SheetAutoCount(self, value: bool): ...

    @property
    def SheetAutoNumber(self) -> bool:  # idrawingdocumentsettings_sheetautonumber.html
        """
        ## SheetAutoNumber - Автоматическая нумерация листов

        Тип данных: BOOL

        Опция, управляющая автонумерацией листов.

        •Если опция включена, то всем листам документа автоматически присваиваются порядковые номера.

        •Если опция выключена, то графа Номер листа в основной надписи текущего документа не заполняется.

        Вы можете ввести произвольный номер для каждого листа документа.

        ### Синтаксис Automation

        ```
        SheetAutoNumber = Object.SheetAutoNumber       # Получить свойство (* )
        Object.SheetAutoNumber = SheetAutoNumber       # Установить свойство (* )
        SheetAutoNumber = Object.GetSheetAutoNumber()  # Получить свойство (**)
        Object.SetSheetAutoNumber( SheetAutoNumber )   # Установить свойство (**)
        ```
        """
        ...
    @SheetAutoNumber.setter
    def SheetAutoNumber(self, value: bool): ...

    @property
    def SheetFirstNumber(self) -> int:  # idrawingdocumentsettings_sheetfirstnumber.html
        """
        ## SheetFirstNumber - Номер первого листа

        Тип данных: long

        Свойство задает номер, с которого начнется автоматическая нумерация.

        По умолчанию он равен единице. Вы можете задать нужное значение.

        Свойство используется при включенной опции Автоматическая нумерация листов (IDrawingDocumentSettings::SheetAutoNumber).

        ### Синтаксис Automation

        ```
        SheetFirstNumber = Object.SheetFirstNumber       # Получить свойство (* )
        Object.SheetFirstNumber = SheetFirstNumber       # Установить свойство (* )
        SheetFirstNumber = Object.GetSheetFirstNumber()  # Получить свойство (**)
        Object.SetSheetFirstNumber( SheetFirstNumber )   # Установить свойство (**)
        ```
        """
        ...
    @SheetFirstNumber.setter
    def SheetFirstNumber(self, value: int): ...

    @property
    def SheetStampMassDigitsCount(self) -> int:  # idrawingdocumentsettings_sheetstampmassdigitscount.html
        """
        ## SheetStampMassDigitsCount - Количество знаков после запятой для массы в штампе

        Тип данных: long

        ### Синтаксис Automation

        ```
        SheetStampMassDigitsCount = Object.SheetStampMassDigitsCount        # Получить свойство (* )
        Object.SheetStampMassDigitsCount = SheetStampMassDigitsCount        # Установить свойство (* )
        SSheetStampMassDigitsCount = Object.GetSheetStampMassDigitsCount()  # Получить свойство (**)
        Object.SetSheetStampMassDigitsCount( SheetStampMassDigitsCount )    # Установить свойство (**)
        ```
        """
        ...
    @SheetStampMassDigitsCount.setter
    def SheetStampMassDigitsCount(self, value: int): ...

    @property
    def SheetStampMassUnit(self) -> int:  # idrawingdocumentsettings_sheetstampmassunit.html
        """
        ## SheetStampMassUnit - Единицы измерения массы в штампе

        Тип данных: из перечисления ksMassUnitsEnum

        ### Синтаксис Automation

        ```
        SheetStampMassUnit = Object.SheetStampMassUnit       # Получить свойство (* )
        Object.SheetStampMassUnit = SheetStampMassUnit       # Установить свойство (* )
        SheetStampMassUnit = Object.GetSheetStampMassUnit()  # Получить свойство (**)
        Object.SetSheetStampMassUnit( SheetStampMassUnit )   # Установить свойство (**)
        ```
        """
        ...
    @SheetStampMassUnit.setter
    def SheetStampMassUnit(self, value: int): ...

    @property
    def SheetsCount(self) -> int:  # idrawingdocumentsettings_sheetscount.html
        """
        ## SheetsCount - Количество листов

        Тип данных: long

        Тип данных: BOOL

        Свойство, позволяющее задать произвольное число, которое будет занесено в графу Количество листов основной надписи каждого листа текущего документа.

        Таким образом можно сформировать документ, являющийся частью другого документа.

        Свойство используется если отключена автоматическая нумерация листов (IDrawingDocumentSettings::SheetAutoCount).

        ### Синтаксис Automation

        ```
        SheetsCount = Object.SheetsCount       # Получить свойство (* )
        Object.SheetsCount = SheetsCount       # Установить свойство (* )
        SheetsCount = Object.GetSheetsCount()  # Получить свойство (**)
        Object.SetSheetsCount( SheetsCount )   # Установить свойство (**)
        ```
        """
        ...
    @SheetsCount.setter
    def SheetsCount(self, value: int): ...

    @property
    def TechnicalDemandSynchronize(self) -> bool:  # idrawingdocumentsettings_technicaldemandsynchronize.html
        """
        ## TechnicalDemandSynchronize - Синхронизировать технические требования

        Тип данных: BOOL

        Свойство позволяет при создании ассоциативного вида синхронизировать технические требования чертежа с техническими требованиями документа 3D, с которого создается вид.

        ### Синтаксис Automation

        ```
        TechnicalDemandSynchronize = Object. TechnicalDemandSynchronize        # Получить свойство (* )
        Object. TechnicalDemandSynchronize = TechnicalDemandSynchronize        # Установить свойство (* )
        TechnicalDemandSynchronize = Object.Get TechnicalDemandSynchronize ()  # Получить свойство (**)
        Object.Set TechnicalDemandSynchronize (TechnicalDemandSynchronize )    # Установить свойство (**)
        ```
        """
        ...
    @TechnicalDemandSynchronize.setter
    def TechnicalDemandSynchronize(self, value: bool): ...

    def SetSheetStampMassDesignation(self, MassUnit, arg1):
        ...

    def SheetStampMassDesignation(self, MassUnit) -> str:  # idrawingdocumentsettings_sheetstampmassdesignation.html
        """
        ## SheetStampMassDesignation - Добавочная строка к массе в штампе

        Тип данных: BSTR

        ### Синтаксис Automation

        ```
        SheetStampMassDesignation = Object.SheetStampMassDesignation( MassUnit )     # Получить свойство (* )
        Object.SheetStampMassDesignation( MassUnit ) = SheetStampMassDesignation     # Установить свойство (* )
        SheetStampMassDesignation = Object.GetSheetStampMassDesignation( MassUnit )  # Получить свойство (**)
        Object.SetSheetStampMassDesignation( MassUnit, SheetStampMassDesignation )   # Установить свойство (**)
        ```

        ### Входные параметры

        +--------------------------------------------------------+
        | ksMassUnitsEnum | - MassUnit- единицы измерения массы. |
        +--------------------------------------------------------+
        """
        ...




class IFragmentDocumentSettings(IDocument2DSettings):  # ifragmentdocumentsettings.html
    """
    ## Интерфейс IFragmentDocumentSettings

    Настройки фрагмента.

    Настройки могут быть для текущего документа (можно получить у документа) и для новых документов (можно получить у приложения).

    Данный интерфейс можно получить от интерфейса документа IKompasDocument с помощью свойства IKompasDocument::DocumentSettings с последующим использованием метода IUnknown QueryInterface для преобразования от интерфейса IDocumentSettings к данному интерфейсу.
    """

    ...


class INewDocument3DSettings(IDispatch):  # inewdocument3dsettings.html
    """
    ## Интерфейс INewDocument3DSettings

    Настройки новых 3D документов.

    Интерфейс можно получить у интерфейса INewPartDocumentSettings с помощью метода IUnknown::QueryInterface.
    """

    @property
    def ColorParam(self) -> IColorParam7:  # inewdocument3dsettings_colorparam.html
        """
        ## ColorParam - Параметры цвета

        Тип данных: указатель на интерфейс IColorParam7

        1. Свойство позволяет получить интерфейс параметров цвета объекта.

        2. Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        ColorParam = Object. ColorParam      # Получить свойство (* )
        ColorParam = Object.GetColorParam()  # Получить свойство (**)
        ```
        """
        ...




class INewPartDocumentSettings(IDispatch):  # inewpartdocumentsettings.html
    """
    ## Интерфейс INewPartDocumentSettings

    Настройки новых документов – деталей.

    Интерфейс можно получить с помощью свойства ISystemSettings::NewDocumentSettings.
    """

    @property
    def Density(self) -> float:  # inewpartdocumentsettings_density.html
        """
        ## Density - Модель: Деталь: Cвойства: Плотность (базовые ед.)

        Тип данных: double

        ### Синтаксис Automation

        ```
        Density = Object.Density       # Получить свойство (* )
        Object.Density = Density       # Установить свойство (* )
        Density = Object.GetDensity()  # Получить свойство (**)
        Object.SetDensity( Density )   # Установить свойство (**)
        ```
        """
        ...
    @Density.setter
    def Density(self, value: float): ...

    @property
    def HatchParam(self) -> IHatchParam:  # inewpartdocumentsettings_hatchparam.html
        """
        ## HatchParam - Параметры штриховки

        Тип данных: указатель на интерфейс IHatchParam

        +--------------------------------------------------------------+
        | HatchParam = Object.HatchParam      | Получить свойство (* ) |
        +--------------------------------------------------------------+
        | HatchParam = Object.GetHatchParam() | Получить свойство (**) |
        +--------------------------------------------------------------+

        Свойство доступно только для чтения.
        """
        ...

    @property
    def Material(self) -> str:  # inewpartdocumentsettings_material.html
        """
        ## Material - Новые документы: Модель: Деталь: Cвойства: Материал

        Тип данных: BSTR

        ### Синтаксис Automation

        ```
        Material = Object.Material       # Получить свойство (* )
        Object.Material = Material       # Установить свойство (* )
        Material = Object.GetMaterial()  # Получить свойство (**)
        Object.SetMaterial( Material )   # Установить свойство (**)
        ```
        """
        ...
    @Material.setter
    def Material(self, value: str): ...

    @property
    def MaterialLocation(self) -> str:  # inewpartdocumentsettings_materiallocation.html
        """
        ## MaterialLocation - Новые документы: Модель: Деталь: Cвойства: Идентификатор материала

        Тип данных: BSTR

        ### Синтаксис Automation

        ```
        MaterialLocation = Object.MaterialLocation       # Получить свойство (* )
        Object.MaterialLocation = MaterialLocation       # Установить свойство (* )
        MaterialLocation = Object.GetMaterialLocation()  # Получить свойство (**)
        Object.SetMaterialLocation( MaterialLocation )   # Установить свойство (**)
        ```
        """
        ...
    @MaterialLocation.setter
    def MaterialLocation(self, value: str): ...




class ILibItemSettings(IDispatch):  # ilibitemsettings.html
    """
    ## Интерфейс ILibItemSettings

    Интерфейс для выбора состояний элементов библиотек в настройках

    Позволяет управлять состоянием элементов библиотек свойств.

    Дополнительный интерфейс. Данный интерфейс можно получить у ILibArraySettings посредством вызова метода IUnknown::QueryInterface (const GUID far& iid, void** pif).
    """

    @property
    def ItemCount(self) -> int:  # ilibitemsettings_itemcount.html
        """
        ## ItemCount - Количество подключенных элементов

        Тип данных: long

        +------------------------------------------------------------+
        | ItemCount = Object.ItemCount      | Получить свойство (* ) |
        +------------------------------------------------------------+
        | ItemCount = Object.GetItemCount() | Получить свойство (**) |
        +------------------------------------------------------------+

        Свойство только для чтения.
        """
        ...

    def GetItem(self, Index):  # ilibitemsettings_getitem.html
        """
        ## GetItem - Получить элемент библиотеки и признак использования по индексу

        ### Синтаксис Automation

        ```
        double GetItem( VARIANT index, BOOL * use );
        ```

        ### Входные параметры

        +-------------------------------------------------------------------------+
        | Index | - индекс в массиве VT_I4, либо уникальный номер элемента VT_R8, |
        +-------------------------------------------------------------------------+
        | use   | - признак использования.                                        |
        +-------------------------------------------------------------------------+

        ### Возвращаемое значение

        +--------------------------------------------------------------+
        | Уникальный номер элемента | - в случае успешного завершения, |
        +--------------------------------------------------------------+
        | -1                        | - в случае неудачи.              |
        +--------------------------------------------------------------+
        """
        ...

    def GetItems(self) -> bool:  # ilibitemsettings_getitems.html
        """
        ## GetItems - Получить массив всех элементов библиотек и признаков использования

        ### Синтаксис Automation

        ```
        BOOL GetItems( VARIANT * uniqIds, VARIANT * uses );
        ```

        ### Входные параметры

        +-----------------------------------------------------------------+
        | UniqIds | - массив уникальных номеров библиотек VT_ARRAY|VT_R8, |
        +-----------------------------------------------------------------+
        | uses    | - массив признаков использования VT_ARRAY|VT_BOOL.    |
        +-----------------------------------------------------------------+

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def GetItemsEx(self) -> bool:  # ilibitemsettings_getitemsex.html
        """
        ## GetItemsEx - Получить массив всех элементов библиотек и признаков использования

        ### Синтаксис Automation

        ```
        BOOL GetItemsEx( VARIANT * UniqIds, VARIANT * Uses, VARIANT * ItemNames, VARIANT * FileNames );
        ```

        ### Выходные параметры

        +-------------------------------------------------------------------+
        | UniqIds   | - массив уникальных номеров библиотек VT_ARRAY|VT_R8, |
        +-------------------------------------------------------------------+
        | Uses      | - массив признаков использования VT_ARRAY|VT_BOOL,    |
        +-------------------------------------------------------------------+
        | ItemNames | - массив имен элементов VT_ARRAY|VT_BSTR,             |
        +-------------------------------------------------------------------+
        | FileNames | - массив имен файлов библиотек VT_ARRAY|VT_BSTR.      |
        +-------------------------------------------------------------------+

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def SetItem(self, Index, Use) -> bool:  # ilibitemsettings_setitem.html
        """
        ## SetItem - Установить признак использования

        ### Синтаксис Automation

        ```
        BOOL SetItem( VARIANT index, BOOL use );
        ```

        ### Входные параметры

        +-------------------------------------------------------------------------+
        | Index | - индекс в массиве VT_I4, либо уникальный номер элемента VT_R8, |
        +-------------------------------------------------------------------------+
        | use   | - признак использования.                                        |
        +-------------------------------------------------------------------------+

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def SetItems(self, UniqIds, Uses) -> bool:  # ilibitemsettings_setitems.html
        """
        ## SetItems - Установить массив всех элементов библиотек и признаков использования

        ### Синтаксис Automation

        ```
        BOOL SetItem( VARIANT index, BOOL use );
        ```

        ### Входные параметры

        +-------------------------------------------------------------------------------+
        | UniqIds | - список идентификаторов в виде массива VT_ARRAY | VT_R8,           |
        +-------------------------------------------------------------------------------+
        | Uses    | - признаки использования свойств в виде массива VT_ARRAY | VT_BOOL. |
        +-------------------------------------------------------------------------------+

        Версия КОМПАС v22

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...




class IDocumentSettings(IKompasAPIObject):  # idocumentsettings.html
    """
    ## Интерфейс IDocumentSettings

    Настройки документа.

    Настройки могут быть для текущего документа (можно получить у документа) и для новых документов (можно получить у приложения).

    Данный интерфейс можно получить от интерфейса документа IKompasDocument с помощью свойства IKompasDocument::DocumentSettings.
    """

    ...


class IDocument2DSettings(IDocumentSettings):  # idocument2dsettings.html
    """
    ## Интерфейс IDocument2DSettings

    Настройки графического документа.

    Настройки могут быть для текущего документа (можно получить у документа) и для новых документов (можно получить у приложения).

    1. Данный интерфейс можно получить от интерфейса документа IKompasDocument с помощью свойства IKompasDocument::DocumentSettings с последующим использованием метода IUnknown QueryInterface для преобразования от интерфейса IDocumentSettings к данному интерфейсу.

    2. Данный интерфейс можно получить от интерфейса настроек чертежа IDrawingDocumentSettings и интерфейса настроек фрагмента IFragmentDocumentSettings с помощью метода IUnknown QueryInterface.
    """

    @property
    def AutoSortObjectsParameters(self) -> IAutoSortObjectsParameters:  # idocument2dsettings_autosortobjectsparameters.html
        """
        ## AutoSortObjectsParameters - Параметры автосортировки буквенных обозначений

        Тип данных: Указатель на интерфейс IAutoSortObjectsParameters

        Свойство доступно только для чтения.

        Версия: Компас v20

        ### Синтаксис Automation

        ```
        AutoSortObjectsParameters = Object.AutoSortObjectsParameters       # Получить свойство (* )
        AutoSortObjectsParameters = Object.GetAutoSortObjectsParameters()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def CompositionInherit(self) -> bool:  # idocument2dsettings_compositioninherit.html
        """
        ## CompositionInherit - Наследовать состав родительского объекта

        Тип данных: BOOL

        1. Данное свойство используется, если свойству LayersGroupWay установлено значение wgLayersCharacteristics.

        2. Если установлено значение TRUE, то при создании группы свойств слоев будет выполнено следующее:

        •если родительским элементом является группа свойств, то в новую группу будут скопированы все слои родительской группы и назначаемые им в данной группе свойства,

        •если родительским элементом является вид, то новая группа будет содержать все слои вида и их свойства,

        •если родительским элементом является корневой элемент Документ, то новая группа будет содержать все слои всех видов.

        3. Изменение способа группировки в документе приводит к необратимым изменениям созданных групп.

        ### Синтаксис Automation

        ```
        CompositionInherit = iObject.CompositionInherit       # Получить свойство (* )
        iObject.CompositionInherit = CompositionInherit       # Установить свойство (* )
        CompositionInherit = iObject.GetCompositionInherit()  # Получить свойство (**)
        iObject.SetCompositionInherit (CompositionInherit)    # Установить свойство (**)
        ```
        """
        ...
    @CompositionInherit.setter
    def CompositionInherit(self, value: bool): ...

    @property
    def DecimalDelimiterType(self) -> int:  # idocument2dsettings_decimaldelimitertype.html
        """
        ## DecimalDelimiterType - Разделитель десятичной части размеров

        Тип данных: из перечисления ksDecimalDelimiterTypeEnum

        ### Синтаксис Automation

        ```
        DecimalDelimiterType = Object.DecimalDelimiterType       # Получить свойство (* )
        Object.DecimalDelimiterType = DecimalDelimiterType       # Установить свойство (* )
        DecimalDelimiterType = Object.GetDecimalDelimiterType()  # Получить свойство (**)
        Object.SetDecimalDelimiterType( DecimalDelimiterType )   # Установить свойство (**)
        ```
        """
        ...
    @DecimalDelimiterType.setter
    def DecimalDelimiterType(self, value: int): ...

    @property
    def LayersGroupWay(self) -> int:  # idocument2dsettings_layersgroupway.html
        """
        ## LayersGroupWay - Способ группировки слоев

        Тип данных: из перечисления LayersGroupWayEnum

        1. Если данному свойству установлено значение wgLayers, то в документе будут создаваться и использоваться статические и динамические (фильтры) группы слоев.

        2. Если свойству установлено значение wgLayersCharacteristics, то в документе будут создаваться и использоваться группы свойств слоев.

        3. Изменение способа группировки в документе приводит к необратимым изменениям созданных групп и фильтров.

        ### Синтаксис Automation

        ```
        LayersGroupWay = iObject.LayersGroupWay       # Получить свойство (* )
        iObject.LayersGroupWay = LayersGroupWay       # Установить свойство (* )
        LayersGroupWay = iObject.GetLayersGroupWay()  # Получить свойство (**)
        iObject.SetLayersGroupWay (LayersGroupWay)    # Установить свойство (**)
        ```
        """
        ...
    @LayersGroupWay.setter
    def LayersGroupWay(self, value: int): ...

    @property
    def RoughModification(self) -> int:  # idocument2dsettings_roughmodification.html
        """
        ## RoughModification - Вариант создания шероховатости

        Тип данных: из перечисления ksRoughModificationEnum

        ### Синтаксис Automation

        ```
        RoughModification = Object.RoughModification       # Получить свойство (* )
        Object.RoughModification = RoughModification       # Установить свойство (* )
        RoughModification = Object.GetRoughModification()  # Получить свойство (**)
        Object.SetRoughModification( RoughModification)    # Установить свойство (**)
        ```
        """
        ...
    @RoughModification.setter
    def RoughModification(self, value: int): ...




class IDocument3DSettings(IDocumentSettings):  # idocument3dsettings.html
    """
    ## Интерфейс IDocument3DSettings

    Интерфейс настроек 3D документа.
    """

    @property
    def AccuracyMassProperties(self) -> float:  # idocument3dsettings_accuracymassproperties.html
        """
        ## AccuracyMassProperties - Точность расчета МЦХ

        Тип данных: double

        Свойство позволяет увеличить или уменьшить погрешность вычисления массово-центровочных характеристик модели и площадей граней.

        ### Синтаксис Automation

        ```
        AccuracyMassProperties = Object.AccuracyMassProperties       # Получить свойство (* )
        Object.AccuracyMassProperties = AccuracyMassProperties       # Установить свойство (* )
        AccuracyMassProperties = Object.GetAccuracyMassProperties()  # Получить свойство (**)
        Object.SetAccuracyMassProperties (AccuracyMassProperties)    # Установить свойство (**)
        ```
        """
        ...
    @AccuracyMassProperties.setter
    def AccuracyMassProperties(self, value: float): ...

    @property
    def AccuracyModelDisplaying(self) -> float:  # idocument3dsettings_accuracymodeldisplaying.html
        """
        ## AccuracyModelDisplaying - Точность отрисовки

        Тип данных: double

        Свойство позволяет увеличить или уменьшить точность аппроксимации криволинейных ребер и линий очерка модели отрезками прямых линий, а криволинейных поверхностей — треугольниками. Чем выше точность, тем более «гладким» выглядит изображение и тем больше времени требуется на расчеты.

        ### Синтаксис Automation

        ```
        AccuracyModelDisplaying = Object.AccuracyModelDisplaying       # Получить свойство (* )
        Object.AccuracyModelDisplaying = AccuracyModelDisplaying       # Установить свойство (* )
        AccuracyModelDisplaying = Object.GetAccuracyModelDisplaying()  # Получить свойство (**)
        Object.SetAccuracyModelDisplaying (AccuracyModelDisplaying)    # Установить свойство (**)
        ```
        """
        ...
    @AccuracyModelDisplaying.setter
    def AccuracyModelDisplaying(self, value: float): ...

    @property
    def DecimalDelimiterType(self) -> int:  # idocument3dsettings_decimaldelimitertype.html
        """
        ## DecimalDelimiterType - Разделитель десятичной части размеров

        Тип данных: из перечисления ksDecimalDelimiterTypeEnum

        ### Синтаксис Automation

        ```
        DecimalDelimiterType = Object.DecimalDelimiterType       # Получить свойство (* )
        Object.DecimalDelimiterType = DecimalDelimiterType       # Установить свойство (* )
        DecimalDelimiterType = Object.GetDecimalDelimiterType()  # Получить свойство (**)
        Object.SetDecimalDelimiterType (DecimalDelimiterType)    # Установить свойство (**)
        ```
        """
        ...
    @DecimalDelimiterType.setter
    def DecimalDelimiterType(self, value: int): ...

    @property
    def EmbodimentsReportSettings(self):
        ...

    @property
    def RoughModification(self) -> int:  # idocument3dsettings_roughmodification.html
        """
        ## RoughModification - Вариант создания шероховатости

        Тип данных: из перечисления ksRoughModificationEnum

        ### Синтаксис Automation

        ```
        RoughModification = Object.RoughModification       # Получить свойство (* )
        Object.RoughModification = RoughModification       # Установить свойство (* )
        RoughModification = Object.GetRoughModification()  # Получить свойство (**)
        Object.SetRoughModification( RoughModification )   # Установить свойство (**)
        ```
        """
        ...
    @RoughModification.setter
    def RoughModification(self, value: int): ...

    @property
    def UsePartsColor(self):
        ...
    @UsePartsColor.setter
    def UsePartsColor(self, value): ...




class IDocumentSettings1(IDispatch):  # idocumentsettings1.html
    """
    ## Интерфейс IDocumentSettings1

    Интерфейс настройки документа.

    Дополнительный интерфейс для настроек документа. Данный интерфейс можно получить у интерфейса графического объекта IDocumentSettings посредством вызова метода IUnknown::QueryInterface (const GUID far& IID, void** pif).

    Версия: Компас v20
    """

    @property
    def FormatLabelRules(self) -> IFormatLabelRules:  # idocumentsettings1_formatlabelrules.html
        """
        ## FormatLabelRules - Правила формирования отображаемых имен объектов в дереве документа

        Тип данных: Указатель на интерфейс IFormatLabelRules

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        FormatLabelRules = Object.FormatLabelRules       # Получить свойство(* )
        FormatLabelRules = Object.GetFormatLabelRules()  # Получить свойство(**)
        ```
        """
        ...

    @property
    def ReportSettings(self):
        ...

    @property
    def TextStability(self):
        ...
    @TextStability.setter
    def TextStability(self, value): ...




class ILibArraySettings(IKompasAPIObject):  # ilibarraysettings.html
    """
    ## Интерфейс ILibArraySettings

    Интерфейс для выбора состояний библиотек в настройках

    Интерфейс позволяет задавать параметры свойства.

    1. Настройка списка подключенных библиотек стилей отчета - ISystemSettings::ReportStyleListSettings.

    2. Посредством вызова метода IUnknown::QueryInterface (const GUID far& iid, void** pif) у данного интерфейса можно получить дополнительный интерфейс ILibItemSettings.
    """

    @property
    def LibraryCount(self) -> int:  # ilibarraysettings_librarycount.html
        """
        ## LibraryCount - Количество подключенных библиотек

        Тип данных: long

        Свойство только для чтения.

        ### Синтаксис Automation

        ```
        LibraryCount = Object.LibraryCount       # Получить свойство (* )
        LibraryCount = Object.GetLibraryCount()  # Получить свойство (**)
        ```
        """
        ...

    def AddLibrary(self, FileName, Use) -> bool:  # ilibarraysettings_addlibrary.html
        """
        ## AddLibrary - Добавить библиотеку и признак использования

        ### Синтаксис Automation

        ```
        BOOL AddLibrary( LPCTSTR fileName, BOOL use );
        ```

        ### Входные параметры

        +---------------------------------------------------------+
        | fileName | - полный путь к библиотеке на диске VT_BSTR, |
        +---------------------------------------------------------+
        | use      | - признак использования.                     |
        +---------------------------------------------------------+

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def GetLibrary(self, Index):  # ilibarraysettings_getlibrary.html
        """
        ## GetLibrary - Получить имя библиотеки и признак использования по индексу

        ### Синтаксис Automation

        ```
        BSTR GetLibrary( long index, BOOL * use );
        ```

        ### Входные параметры

        +----------------------------------+
        | index | - индекс в массиве,      |
        +----------------------------------+
        | use   | - признак использования. |
        +----------------------------------+

        ### Возвращаемое значение

        - Полный путь к библиотеке на диске.
        """
        ...

    def GetLibrarys(self) -> bool:  # ilibarraysettings_getlibrarys.html
        """
        ## GetLibrarys - Получить массив всех библиотек и флагов использования

        ### Синтаксис Automation

        ```
        BOOL GetLibrarys( VARIANT * fileNames, VARIANT * uses );
        ```

        ### Входные параметры

        +----------------------------------------------------------------+
        | fileNames | - массив имён файлов на диске VT_ARRAY|VT_BSTR,    |
        +----------------------------------------------------------------+
        | uses      | - массив признаков использования VT_ARRAY|VT_BOOL. |
        +----------------------------------------------------------------+

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def RemoveLibrary(self, Index) -> bool:  # ilibarraysettings_removelibrary.html
        """
        ## RemoveLibrary - Удалить библиотеку и признак использования

        ### Синтаксис Automation

        ```
        BOOL RemoveLibrary( VARIANT index );
        ```

        ### Входные параметры

        +-----------------------------------------------------------------------------------+
        | Index | - полный путь к библиотеке на диске VT_BSTR, либо индекс в массиве VT_I4. |
        +-----------------------------------------------------------------------------------+

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def SetLibraryUse(self, Index, Use) -> bool:  # ilibarraysettings_setlibraryuse.html
        """
        ## SetLibraryUse - Установить флаг использования по индексу

        ### Синтаксис Automation

        ```
        BOOL SetLibraryUse( VARIANT index, BOOL use );
        ```

        ### Входные параметры

        +-----------------------------------------------------------------------------------+
        | Index | - Полный путь к библиотеке на диске VT_BSTR, либо индекс в массиве VT_I4, |
        +-----------------------------------------------------------------------------------+
        | use   | - признак использования.                                                  |
        +-----------------------------------------------------------------------------------+

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...




class ISystemSettings(IKompasAPIObject):  # isystemsettings.html
    """
    ## Интерфейс ISystemSettings

    Параметры системы.

    Данный интерфейс можно получить от интерфейса IApplication с помощью метода IApplication::SystemSettings.
    """

    @property
    def AssociationViewAutoSaveBeforeRebuild(self) -> bool:  # isystemsettings_associationviewautosavebeforerebuild.html
        """
        ## AssociationViewAutoSaveBeforeRebuild - Общее: Управление системой: Выполнять автосохранение перед обработкой

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        AssociationViewAutoSaveBeforeRebuild = Object.AssociationViewAutoSaveBeforeRebuild       # Получить свойство (* )
        Object.AssociationViewAutoSaveBeforeRebuild = AssociationViewAutoSaveBeforeRebuild       # Установить свойство (* )
        AssociationViewAutoSaveBeforeRebuild = Object.GetAssociationViewAutoSaveBeforeRebuild()  # Получить свойство (**)
        Object.SetAssociationViewAutoSaveBeforeRebuild( AssociationViewAutoSaveBeforeRebuild )   # Установить свойство (**)
        ```
        """
        ...
    @AssociationViewAutoSaveBeforeRebuild.setter
    def AssociationViewAutoSaveBeforeRebuild(self, value: bool): ...

    @property
    def AssociationViewRebuildParallel(self) -> bool:  # isystemsettings_associationviewrebuildparallel.html
        """
        ## AssociationViewRebuildParallel - Общее: Управление системой: Разрешить параллельную обработку

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        AssociationViewRebuildParallel = Object.AssociationViewRebuildParallel       # Получить свойство (* )
        Object.AssociationViewRebuildParallel = AssociationViewRebuildParallel       # Установить свойство (* )
        AssociationViewRebuildParallel = Object.GetAssociationViewRebuildParallel()  # Получить свойство (**)
        Object.SetAssociationViewRebuildParallel( AssociationViewRebuildParallel )   # Установить свойство (**)
        ```
        """
        ...
    @AssociationViewRebuildParallel.setter
    def AssociationViewRebuildParallel(self, value: bool): ...

    @property
    def AssociationViewRebuildParallelLowPriority(self) -> bool:  # isystemsettings_associationviewrebuildparallellowpriority.html
        """
        ## AssociationViewRebuildParallelLowPriority - Общее: Управление системой: Разрешить параллельную обработку. С пониженным приоритетом

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        AssociationViewRebuildParallelLowPriority = Object.AssociationViewRebuildParallelLowPriority       # Получить свойство (* )
        Object.AssociationViewRebuildParallelLowPriority = AssociationViewRebuildParallelLowPriority       # Установить свойство (* )
        AssociationViewRebuildParallelLowPriority = Object.GetAssociationViewRebuildParallelLowPriority()  # Получить свойство (**)
        Object.SetAssociationViewRebuildParallelLowPriority( AssociationViewRebuildParallelLowPriority )   # Установить свойство (**)
        ```
        """
        ...
    @AssociationViewRebuildParallelLowPriority.setter
    def AssociationViewRebuildParallelLowPriority(self, value: bool): ...

    @property
    def EnableAddFilesToRecentList(self) -> bool:  # isystemsettings_enableaddfilestorecentlist.html
        """
        ## EnableAddFilesToRecentList - Файлы: Добавлять открываемые файлы в список недавних документов

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        EnableAddFilesToRecentList = Object.EnableAddFilesToRecentList       # Получить свойство (* )
        Object.EnableAddFilesToRecentList = EnableAddFilesToRecentList       # Установить свойство (* )
        EnableAddFilesToRecentList = Object.GetEnableAddFilesToRecentList()  # Получить свойство (**)
        Object.SetEnableAddFilesToRecentList( EnableAddFilesToRecentList )   # Установить свойство (**)
        ```
        """
        ...
    @EnableAddFilesToRecentList.setter
    def EnableAddFilesToRecentList(self, value: bool): ...

    @property
    def EnablesAddSystemDelimersInMarking(self) -> bool:  # isystemsettings_enablesaddsystemdelimersinmarking.html
        """
        ## EnablesAddSystemDelimersInMarking - Добавлять системные разделители в обозначение в функциях API

        Тип данных: BOOL

        Если в обозначении есть системная часть, то обозначение может возвращаться без системных разделителей.

        ### Синтаксис Automation

        ```
        EnablesAddSystemDelimersInMarking = Object.EnablesAddSystemDelimersInMarking       # Получить свойство(* )
        Object.EnablesAddSystemDelimersInMarking = EnablesAddSystemDelimersInMarking       # Установить свойство (* )
        EnablesAddSystemDelimersInMarking = Object.GetEnablesAddSystemDelimersInMarking()  # Получить свойство (**)
        Object.SetEnablesAddSystemDelimersInMarking( EnablesAddSystemDelimersInMarking )   # Установить свойство (**)
        ```
        """
        ...
    @EnablesAddSystemDelimersInMarking.setter
    def EnablesAddSystemDelimersInMarking(self, value: bool): ...

    @property
    def FilesAutoSaveSwitchOn(self) -> bool:  # isystemsettings_filesautosaveswitchon.html
        """
        ## FilesAutoSaveSwitchOn - Автоматическое сохранение файлов документов

        Тип данных: BOOL

        Значение свойства:

        +-------------------------------------+
        | TRUE  | - автосохранение включено,  |
        +-------------------------------------+
        | FALSE | - автосохранение выключено. |
        +-------------------------------------+

        ### Синтаксис Automation

        ```
        FilesAutoSaveSwitchOn = iObject.FilesAutoSaveSwitchOn       # Получить свойство (* )
        iObject.FilesAutoSaveSwitchOn = FilesAutoSaveSwitchOn       # Установить свойство (* )
        FilesAutoSaveSwitchOn = iObject.GetFilesAutoSaveSwitchOn()  # Получить свойство (**)
        iObject.SetFilesAutoSaveSwitchOn (FilesAutoSaveSwitchOn)    # Установить свойство (**)
        ```
        """
        ...
    @FilesAutoSaveSwitchOn.setter
    def FilesAutoSaveSwitchOn(self, value: bool): ...

    @property
    def FilesBackupPrevCopySwitchOn(self) -> bool:  # isystemsettings_filesbackupprevcopyswitchon.html
        """
        ## FilesBackupPrevCopySwitchOn - Автоматическое сохранение предыдущей копии файла документа

        Тип данных: BOOL

        Значение свойства:

        +-------------------------------------+
        | TRUE  | - автосохранение включено,  |
        +-------------------------------------+
        | FALSE | - автосохранение выключено. |
        +-------------------------------------+

        ### Синтаксис Automation

        ```
        FilesBackupPrevCopySwitchOn = iObject.FilesBackupPrevCopySwitchOn       # Получить свойство (* )
        iObject.FilesBackupPrevCopySwitchOn = FilesBackupPrevCopySwitchOn       # Установить свойство (* )
        FilesBackupPrevCopySwitchOn = iObject.GetFilesBackupPrevCopySwitchOn()  # Получить свойство (**)
        iObject.SetFilesBackupPrevCopySwitchOn (FilesBackupPrevCopySwitchOn)    # Установить\\ свойство (**)
        ```
        """
        ...
    @FilesBackupPrevCopySwitchOn.setter
    def FilesBackupPrevCopySwitchOn(self, value: bool): ...

    @property
    def FilesNewFileNameMakeMode(self) -> int:  # isystemsettings_filesnewfilenamemakemode.html
        """
        ## FilesNewFileNameMakeMode - Файлы: Имя файла при первом сохранении

        Тип данных: из перечисления ksFileNameMakeModeEnum

        ### Синтаксис Automation

        ```
        FilesNewFileNameMakeMode = iObject.FilesNewFileNameMakeMode       # Получить свойство (* )
        iObject.FilesNewFileNameMakeMode = FilesNewFileNameMakeMode       # Установить свойство (* )
        FilesNewFileNameMakeMode = iObject.GetFilesNewFileNameMakeMode()  # Получить свойство (**)
        iObject.SetFilesNewFileNameMakeMode ( FilesNewFileNameMakeMode )  # Установить\\ свойство (**)
        ```
        """
        ...
    @FilesNewFileNameMakeMode.setter
    def FilesNewFileNameMakeMode(self, value: int): ...

    @property
    def FilesNewFileNameSeparator1(self) -> str:  # isystemsettings_filesnewfilenameseparator1.html
        """
        ## FilesNewFileNameSeparator1 - Файлы: Разделитель в имени файла для Обозначение+Наименование

        Тип данных: BSTR

        ### Синтаксис Automation

        ```
        FilesNewFileNameSeparator1 = iObject.FilesNewFileNameSeparator1       # Получить свойство (* )
        iObject.FilesNewFileNameSeparator1 = FilesNewFileNameSeparator1       # Установить свойство (* )
        FilesNewFileNameSeparator1 = iObject.GetFilesNewFileNameSeparator1()  # Получить свойство (**)
        iObject.SetFilesNewFileNameSeparator1 ( FilesNewFileNameSeparator1 )  # Установить\\ свойство (**)
        ```
        """
        ...
    @FilesNewFileNameSeparator1.setter
    def FilesNewFileNameSeparator1(self, value: str): ...

    @property
    def FilesNewFileNameSeparator2(self) -> str:  # isystemsettings_filesnewfilenameseparator2.html
        """
        ## FilesNewFileNameSeparator2 - Файлы: Разделитель в имени файла для Наименование+Обозначение

        Тип данных: BSTR

        ### Синтаксис Automation

        ```
        FilesNewFileNameSeparator2 = iObject.FilesNewFileNameSeparator2       # Получить свойство (* )
        iObject.FilesNewFileNameSeparator2 = FilesNewFileNameSeparator2       # Установить свойство (* )
        FilesNewFileNameSeparator2 = iObject.GetFilesNewFileNameSeparator2()  # Получить свойство (**)
        iObject.SetFilesNewFileNameSeparator2 ( FilesNewFileNameSeparator2 )  # Установить\\ свойство (**)
        ```
        """
        ...
    @FilesNewFileNameSeparator2.setter
    def FilesNewFileNameSeparator2(self, value: str): ...

    @property
    def JtAutoSaveSwitchOn(self) -> bool:  # isystemsettings_jtautosaveswitchon.html
        """
        ## JtAutoSaveSwitchOn - Файлы: Автоматический экспорт: Автоматический экспорт в Jt

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        JtAutoSaveSwitchOn = Object.JtAutoSaveSwitchOn       # Получить свойство(* )
        Object.JtAutoSaveSwitchOn = JtAutoSaveSwitchOn       # Установить свойство (* )
        JtAutoSaveSwitchOn = Object.GetJtAutoSaveSwitchOn()  # Получить свойство (**)
        Object.SetJtAutoSaveSwitchOn( JtAutoSaveSwitchOn )   # Установить свойство (**)
        ```
        """
        ...
    @JtAutoSaveSwitchOn.setter
    def JtAutoSaveSwitchOn(self, value: bool): ...

    @property
    def Language(self) -> str:  # isystemsettings_language.html
        """
        ## Language - Марка. Текст префикса

        Тип данных: BSTR

        Свойство доступно только для чтения.

        Список возможных значений свойства см. https://msdn.microsoft.com/ru-ru/library/ee825488(v=cs.20).aspx

        В зависимости от текущего языка выбирается словарь (текстовый UNICOD-ный документ с расширением .dic). Имя файла для приложений формируется как имя файла приложения - подчеркивание - имя текущего языка.

        Пример:

        komlib_ru_RU.dic - словарь для русского языка

        komlib_ru_EN.dic - словарь для английского языка.

        ### Синтаксис Automation

        ```
        Language = Object.Language       # Получить свойство (* )
        Language = Object.GetLanguage()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def ModelFillChooseFace(self) -> bool:  # isystemsettings_modelfillchooseface.html
        """
        ## ModelFillChooseFace - Редактор моделей: Закрашивать грани при подсвечивании

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        ModelFillChooseFace = Object.ModelFillChooseFace       # Получить свойство (* )
        Object.ModelFillChooseFace = ModelFillChooseFace       # Установить свойство (* )
        ModelFillChooseFace = Object.GetModelFillChooseFace()  # Получить свойство (**)
        Object.SetModelFillChooseFace( ModelFillChooseFace )   # Установить свойство (**)
        ```
        """
        ...
    @ModelFillChooseFace.setter
    def ModelFillChooseFace(self, value: bool): ...

    @property
    def ModelInverseInDynamicSelect(self) -> bool:  # isystemsettings_modelinverseindynamicselect.html
        """
        ## ModelInverseInDynamicSelect - Редактор моделей: Инверсия при динамическом подсвечивании

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        ModelInverseInDynamicSelect = Object.ModelInverseInDynamicSelect       # Получить свойство (* )
        Object.ModelInverseInDynamicSelect = ModelInverseInDynamicSelect       # Установить свойство (* )
        ModelInverseInDynamicSelect = Object.GetModelInverseInDynamicSelect()  # Получить свойство (**)
        Object.SetModelInverseInDynamicSelect( ModelInverseInDynamicSelect )   # Установить свойство (**)
        ```
        """
        ...
    @ModelInverseInDynamicSelect.setter
    def ModelInverseInDynamicSelect(self, value: bool): ...

    @property
    def ModelLocalCSCreateInAbsoluteCS(self) -> bool:  # isystemsettings_modellocalcscreateinabsolutecs.html
        """
        ## ModelLocalCSCreateInAbsoluteCS - Создавать ЛСК только в абсолютной СК

        Тип данных: BOOL

        Значение свойства:

        +----------------------------------------------------------------------------+
        | TRUE  | - новая локальная система координат будет создана в абсолютной СК, |
        +----------------------------------------------------------------------------+
        | FALSE | - новая локальная система координат будет создана в текущей СК.    |
        +----------------------------------------------------------------------------+

        ### Синтаксис Automation

        ```
        ModelLocalCSCreateInAbsoluteCS = iObject.ModelLocalCSCreateInAbsoluteCS       # Получить свойство (* )
        iObject.ModelLocalCSCreateInAbsoluteCS = ModelLocalCSCreateInAbsoluteCS       # Установить свойство (* )
        ModelLocalCSCreateInAbsoluteCS = iObject.GetModelLocalCSCreateInAbsoluteCS()  # Получить свойство (**)
        iObject.SetModelLocalCSCreateInAbsoluteCS (ModelLocalCSCreateInAbsoluteCS)    # Установить свойство (**)
        ```
        """
        ...
    @ModelLocalCSCreateInAbsoluteCS.setter
    def ModelLocalCSCreateInAbsoluteCS(self, value: bool): ...

    @property
    def ModelLocalCSSetActive(self) -> bool:  # isystemsettings_modellocalcssetactive.html
        """
        ## ModelLocalCSSetActive - При создании ЛСК назначать ее текущей СК

        Тип данных: BOOL

        Значение свойства:

        +-------------------------------------------------------------+
        | TRUE  | - новая локальная система координат станет текущей, |
        +-------------------------------------------------------------+
        | FALSE | - текущей останется старая СК.                      |
        +-------------------------------------------------------------+

        ### Синтаксис Automation

        ```
        ModelLocalCSSetActive = iObject.ModelLocalCSSetActive       # Получить свойство (* )
        iObject.ModelLocalCSSetActive = ModelLocalCSSetActive       # Установить свойство (* )
        ModelLocalCSSetActive = iObject.GetModelLocalCSSetActive()  # Получить свойство (**)
        iObject.SetModelLocalCSSetActive(ModelLocalCSSetActive)     # Установить свойство (**)
        ```
        """
        ...
    @ModelLocalCSSetActive.setter
    def ModelLocalCSSetActive(self, value: bool): ...

    @property
    def ModelPerformanceLevel(self) -> int:  # isystemsettings_modelperformancelevel.html
        """
        ## ModelPerformanceLevel - Редактор моделей: Качество сглаживания

        Тип данных: из перечисления ksModelPerformanceLevelEnum

        ### Синтаксис Automation

        ```
        ModelPerformanceLevel = Object.ModelPerformanceLevel       # Получить свойство (* )
        Object.ModelPerformanceLevel = ModelPerformanceLevel       # Установить свойство (* )
        ModelPerformanceLevel = Object.GetModelPerformanceLevel()  # Получить свойство (**)
        Object.SetModelPerformanceLevel( ModelPerformanceLevel )   # Установить свойство (**)
        ```
        """
        ...
    @ModelPerformanceLevel.setter
    def ModelPerformanceLevel(self, value: int): ...

    @property
    def ModelRenderType(self) -> int:  # isystemsettings_modelrendertype.html
        """
        ## ModelRenderType - Общее: Управление системой: Вариант отрисовки

        Тип данных: из перечисления ksModelRenderTypeEnum.

        ### Синтаксис Automation

        ```
        ModelRenderType = Object.ModelRenderType       # Получить свойство (* )
        Object.ModelRenderType = ModelRenderType       # Установить свойство (* )
        ModelRenderType = Object.GetModelRenderType()  # Получить свойство (**)
        Object.SetModelRenderType( ModelRenderType )   # Установить свойство (**)
        ```
        """
        ...
    @ModelRenderType.setter
    def ModelRenderType(self, value: int): ...

    @property
    def ModelScaleFactor(self) -> float:  # isystemsettings_modelscalefactor.html
        """
        ## ModelScaleFactor - Редактор моделей: Коэффициент изменения масштаба

        Тип данных: double

        ### Синтаксис Automation

        ```
        ModelScaleFactor = Object.ModelScaleFactor       # Получить свойство (* )
        Object.ModelScaleFactor = ModelScaleFactor       # Установить свойство (* )
        ModelScaleFactor = Object.GetModelScaleFactor()  # Получить свойство (**)
        Object.SetModelScaleFactor( ModelScaleFactor )   # Установить свойство (**)
        ```
        """
        ...
    @ModelScaleFactor.setter
    def ModelScaleFactor(self, value: float): ...

    @property
    def ModelSmoothMotion(self) -> bool:  # isystemsettings_modelsmoothmotion.html
        """
        ## ModelSmoothMotion - Редактор моделей: Изменение ориентации: Плавность

        Тип данных: BOOL

        Свойство позволяет включить или отключить плавность поворота модели при редактировании.

        ### Синтаксис Automation

        ```
        ModelSmoothMotion = Object.ModelSmoothMotion       # Получить свойство (* )
        Object.ModelSmoothMotion = ModelSmoothMotion       # Установить свойство (* )
        ModelSmoothMotion = Object.GetModelSmoothMotion()  # Получить свойство (**)
        Object.SetModelSmoothMotion( ModelSmoothMotion )   # Установить свойство (**)
        ```
        """
        ...
    @ModelSmoothMotion.setter
    def ModelSmoothMotion(self, value: bool): ...

    @property
    def ModelStepMoveDetail(self) -> float:  # isystemsettings_modelstepmovedetail.html
        """
        ## ModelStepMoveDetail - Редактор моделей: Шаг перемещения изображения модели (% окна)

        Тип данных: double

        ### Синтаксис Automation

        ```
        ModelStepMoveDetail = Object.ModelStepMoveDetail       # Получить свойство (* )
        Object.ModelStepMoveDetail = ModelStepMoveDetail       # Установить свойство (* )
        ModelStepMoveDetail = Object.GetModelStepMoveDetail()  # Получить свойство (**)
        Object.SetModelStepMoveDetail( ModelStepMoveDetail )   # Установить свойство (**)
        ```
        """
        ...
    @ModelStepMoveDetail.setter
    def ModelStepMoveDetail(self, value: float): ...

    @property
    def ModelStepRotateDetail(self) -> float:  # isystemsettings_modelsteprotatedetail.html
        """
        ## ModelStepRotateDetail - Редактор моделей: Шаг угла поворота модели (гр.)

        Тип данных: double

        ### Синтаксис Automation

        ```
        ModelStepRotateDetail = Object.ModelStepRotateDetail       # Получить свойство (* )
        Object.ModelStepRotateDetail = ModelStepRotateDetail       # Установить свойство (* )
        ModelStepRotateDetail = Object.GetModelStepRotateDetail()  # Получить свойство (**)
        Object.SetModelStepRotateDetail( ModelStepRotateDetail )   # Установить свойство (**)
        ```
        """
        ...
    @ModelStepRotateDetail.setter
    def ModelStepRotateDetail(self, value: float): ...

    @property
    def ModelTransparencyType(self) -> int:  # isystemsettings_modeltransparencytype.html
        """
        ## ModelTransparencyType - Редактор моделей: Прозрачность

        Тип данных: из перечисления ksModelTransparencyTypeEnum

        ### Синтаксис Automation

        ```
        ModelTransparencyType = Object.ModelTransparencyType       # Получить свойство (* )
        Object.ModelTransparencyType = ModelTransparencyType       # Установить свойство (* )
        ModelTransparencyType = Object.GetModelTransparencyType()  # Получить свойство (**)
        Object.SetModelTransparencyType( ModelTransparencyType )   # Установить свойство (**)
        ```
        """
        ...
    @ModelTransparencyType.setter
    def ModelTransparencyType(self, value: int): ...

    @property
    def ModelUseOpenGLSearch(self) -> bool:  # isystemsettings_modeluseopenglsearch.html
        """
        ## ModelUseOpenGLSearch - Редактор моделей: При указании использовать OpenGL-поиск

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        ModelUseOpenGLSearch = Object.ModelUseOpenGLSearch       # Получить свойство (* )
        Object.ModelUseOpenGLSearch = ModelUseOpenGLSearch       # Установить свойство (* )
        ModelUseOpenGLSearch = Object.GetModelUseOpenGLSearch()  # Получить свойство (**)
        Object.SetModelUseOpenGLSearch( ModelUseOpenGLSearch )   # Установить свойство (**)
        ```
        """
        ...
    @ModelUseOpenGLSearch.setter
    def ModelUseOpenGLSearch(self, value: bool): ...

    @property
    def ModelUsePartColorForEdit(self) -> bool:  # isystemsettings_modelusepartcolorforedit.html
        """
        ## ModelUsePartColorForEdit - Редактор моделей: Собственные цвета компонентов при контекстном редактировании

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        ModelUsePartColorForEdit = Object.ModelUsePartColorForEdit       # Получить свойство (* )
        Object.ModelUsePartColorForEdit = ModelUsePartColorForEdit       # Установить свойство (* )
        ModelUsePartColorForEdit = Object.GetModelUsePartColorForEdit()  # Получить свойство (**)
        Object.SetModelUsePartColorForEdit( ModelUsePartColorForEdit )   # Установить свойство (**)
        ```
        """
        ...
    @ModelUsePartColorForEdit.setter
    def ModelUsePartColorForEdit(self, value: bool): ...

    @property
    def ReportStyleListSettings(self) -> ILibArraySettings:  # isystemsettings_reportstylelistsettings.html
        """
        ## ReportStyleListSettings - Настройка списка подключенных библиотек стилей отчета

        Тип данных: указатель на интерфейс ILibArraySettings

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        ReportStyleListSettings = Object.ReportStyleListSettings       # Получить свойство (* )
        ReportStyleListSettings = Object.GetReportStyleListSettings()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def StandardsThreadsListSettings(self) -> ILibArraySettings:  # isystemsettings_standardsthreadslistsettings.html
        """
        ## StandardsThreadsListSettings - Настройка списка подключаемых стандартов резьб

        Тип данных: указатель на интерфейс ILibArraySettings

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        StandardsThreadsListSettings = Object.StandardsThreadsListSettings( SettingsType )     # Получить свойство(* )
        StandardsThreadsListSettings = Object.GetStandardsThreadsListSettings( SettingsType )  # Получить свойство (**)
        ```
        """
        ...

    @property
    def Theme(self) -> int:  # isystemsettings_theme.html
        """
        ## Theme - Общее: Управление системой: Выполнять автосохранение перед обработкой

        Тип данных: значение из перечисления ksThemeEnum

        Свойство доступно только для чтения.

        Версия: КОМПАС v18

        ### Синтаксис Automation

        ```
        Theme = Object.Theme                       # Получить свойство(* )
        ThreadPattern = Theme = Object.GetTheme()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def UseHardwareAcceleration(self) -> bool:  # isystemsettings_usehardwareacceleration.html
        """
        ## UseHardwareAcceleration - Общее: Управление системой: Использовать аппаратное ускорение

        Тип данных: BOOL

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        UseHardwareAcceleration = Object.UseHardwareAcceleration       # Получить свойство (* )
        UseHardwareAcceleration = Object.GetUseHardwareAcceleration()  # Получить свойство (**)
        ```
        """
        ...

    def ModelEditColor(self, ColorType) -> int:  # isystemsettings_modeleditcolor.html
        """
        ## ModelEditColor - Редактор моделей: Цвета редактирования объектов

        Тип данных: Long

        ### Синтаксис Automation

        ```
        ModelEditColor = Object.ModelEditColor( ColorType )     # Получить свойство (* )
        Object.ModelEditColor( ColorType ) = ModelEditColor     # Установить свойство (* )
        ModelEditColor = Object.GetModelEditColor( ColorType )  # Получить свойство (**)
        Object.SetModelEditColor( ColorType, ModelEditColor )   # Установить свойство (**)
        ```

        ### Входные параметры

        +---------------------------------------------------------------+
        | ksEditColorTypeEnum | - ColorType - тип цвета редактирования. |
        +---------------------------------------------------------------+
        """
        ...

    def NewDocumentSettings(self, SettingsType) -> IKompasAPIObject:  # isystemsettings_newdocumentsettings.html
        """
        ## NewDocumentSettings - Настройки новых документов

        Тип данных: указатель на интерфейс IKompasAPIObject

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        NewDocumentSettings = Object.NewDocumentSettings( SettingsType )     # Получить свойство(* )
        NewDocumentSettings = Object.GetNewDocumentSettings( SettingsType )  # Получить свойство (**)
        ```

        ### Входные параметры

        +--------------------------------------------------------------+
        | ksNewDocumentSettingsTypeEnum SettingsType | - тип настроек. |
        +--------------------------------------------------------------+
        """
        ...

    def ObjectsFilter3D(self, Type) -> bool:  # isystemsettings_objectsfilter3d.html
        """
        ## ObjectsFilter3D - Способ фильтрации 3D объектов

        Тип данных: BOOL.

        ### Синтаксис Automation

        ```
        ObjectsFilter3D( Type ) = Object.ObjectsFilter3D     # Получить свойство (* )
        Object.ObjectsFilter3D( Type ) = ObjectsFilter3D     # Установить свойство (* )
        ObjectsFilter3D = Object.GetObjectsFilter3D( Type )  # Получить свойство (**)
        Object.SetObjectsFilter3D( Type, ObjectsFilter3D )   # Установить свойство (**)
        ```

        ### Входные параметры

        Type - Способ фильтрации 3D объектов ksObjectsFilter3DEnum

        ### Возвращаемое значение

        Признак фильтрации для данного способа.
        """
        ...

    def SetModelEditColor(self, ColorType, arg1):
        ...

    def SetObjectsFilter3D(self, Type, arg1):
        ...

    def SystemPath(self, PathType) -> str:  # isystemsettings_systempath.html
        """
        ## SystemPath - Получить путь к системному каталогу по типу

        Тип данных: BSTR

        Свойство доступно только для чтения

        Версия КОМПАС v22.

        ### Синтаксис Automation

        ```
        SystemPath = Object.SystemPath( PathType )     # Получить свойство(* )
        SystemPath = Object.GetSystemPath( PathType )  # Получить свойство (**)
        ```

        ### Входные параметры

        +----------------------------------------------------------------------------+
        | PathType | - Тип системных каталогов из перечисления ksSystemPathTypeEnum. |
        +----------------------------------------------------------------------------+
        """
        ...

    def ThreadPattern(self, ThreadTableName) -> IThreadPattern:  # isystemsettings_threadpattern.html
        """
        ## ThreadPattern - Параметры стандартной резьбы

        Тип данных: Указатель на интерфейс IThreadPattern

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        ThreadPattern = Object.ThreadPattern( ThreadTableName )     # Получить свойство(* )
        ThreadPattern = Object.GetThreadPattern( ThreadTableName )  # Получить свойство (**)
        ```
        """
        ...




class ISystemTemplatesSettings(IDispatch):  # isystemtemplatessettings.html
    """
    ## Интерфейс ISystemTemplatesSettings

    Интерфейс шаблонов документов
    """

    @property
    def AddGroupSpecificationTemplate(self) -> bool:  # isystemtemplatessettings_addgroupspecificationtemplate.html
        """
        ## AddGroupSpecificationTemplate - Добавить шаблон групповой спецификации

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        AddGroupSpecificationTemplate = Object.AddGroupSpecificationTemplate       # Получить свойство (* )
        Object.AddGroupSpecificationTemplate = AddGroupSpecificationTemplate       # Установить свойство (* )
        AddGroupSpecificationTemplate = Object.GetAddGroupSpecificationTemplate()  # Получить свойство (**)
        Object.SetAddGroupSpecificationTemplate( AddGroupSpecificationTemplate )   # Установить свойство (**)
        ```
        """
        ...
    @AddGroupSpecificationTemplate.setter
    def AddGroupSpecificationTemplate(self, value: bool): ...

    @property
    def GroupSpecificationEmbodimentCount(self) -> int:  # isystemtemplatessettings_groupspecificationembodimentcount.html
        """
        ## GroupSpecificationEmbodimentCount - Максимальное количество исполнений в сборке для создания групповой A спецификации

        Тип данных: long

        ### Синтаксис Automation

        ```
        GroupSpecificationEmbodimentCount = Object.GroupSpecificationEmbodimentCount       # Получить свойство (* )
        Object.GroupSpecificationEmbodimentCount = GroupSpecificationEmbodimentCount       # Установить свойство (* )
        GroupSpecificationEmbodimentCount = Object.GetGroupSpecificationEmbodimentCount()  # Получить свойство (**)
        Object.SetGroupSpecificationEmbodimentCount( GroupSpecificationEmbodimentCount )   # Установить свойство (**)
        ```
        """
        ...
    @GroupSpecificationEmbodimentCount.setter
    def GroupSpecificationEmbodimentCount(self, value: int): ...

    def SetSpecificationTemplatePath(self, SpcType, arg1):
        ...

    def SetTemplatePath(self, DocumentType, DocumentTypeId, arg2):
        ...

    def SpecificationTemplatePath(self, SpcType) -> bool:  # isystemtemplatessettings_specificationtemplatepath.html
        """
        ## SpecificationTemplatePath - Путь к файлу шаблона

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        SpecificationTemplatePath = Object.SpecificationTemplatePath( SpcType )     # Получить свойство (* )
        Object.SpecificationTemplatePath( SpcType ) = SpecificationTemplatePath     # Установить свойство (* )
        SpecificationTemplatePath = Object.GetSpecificationTemplatePath( SpcType )  # Получить свойство (**)
        Object.SetSpecificationTemplatePath( SpcType, SpecificationTemplatePath )   # Установить свойство (**)
        ```

        ### Входные параметры

        +--------------------------------------------------------------------------+
        | SpcType | - Тип спецификации из перечисления ksSpecificationVariantEnum. |
        +--------------------------------------------------------------------------+
        """
        ...

    def TemplatePath(self, DocumentType, DocumentTypeId) -> bool:  # isystemtemplatessettings_templatepath.html
        """
        ## TemplatePath - Путь к файлу шаблона

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        TemplatePath = Object.TemplatePath( DocumentType, DocumentTypeId )     # Получить свойство (* )
        Object.TemplatePath( DocumentType, DocumentTypeId ) = TemplatePath     # Установить свойство (* )
        TemplatePath = Object.GetTemplatePath( DocumentType, DocumentTypeId )  # Получить свойство (**)
        Object.SetTemplatePath( DocumentType, DocumentTypeId, TemplatePath )   # Установить свойство (**)
        ```

        ### Входные параметры

        +--------------------------------------------------------------------+
        | DocumentType   | - тип документа из перечисления DocumentTypeEnum, |
        +--------------------------------------------------------------------+
        | DocumentTypeId | - идентификатор специализации документа.          |
        +--------------------------------------------------------------------+
        """
        ...




class IViewProjectionManager(IKompasAPIObject):  # iviewprojectionmanager.html
    """
    ## Интерфейс IViewProjectionManager

    Менеджер проекций отображения модели в окне.

    Интерфейс можно получить используя свойство IKompasDocument3D1::ViewProjectionManager.

    Версия КОМПАС v22
    """

    @property
    def BaseUserOrientation(self) -> typing.Any:  # iviewprojectionmanager_baseuserorientation.html
        """
        ## BaseUserOrientation - Матрица базовой ориентации модели

        Тип данных: VARIANT

        1.Элементы матрицы возвращаются в виде одномерного массива из 16 элементов SafeAttray типа (VT_ARRAY | VT_R8).. Матрица имеет размер 4х4.

        2.Метод позволяет получить матрицу, которая может использоваться для отображения вида спереди.

        3.По умолчанию используется единичная матрица без смещения.

        ### Синтаксис Automation

        ```
        BaseUserOrientation = Object.BaseUserOrientation       # Получить свойство (* )
        Object.BaseUserOrientation = BaseUserOrientation       # Установить свойство (* )
        BaseUserOrientation = Object.GetBaseUserOrientation()  # Получить свойство (**)
        Object.SetBaseUserOrientation( BaseUserOrientation )   # Установить свойство (**)
        ```
        """
        ...
    @BaseUserOrientation.setter
    def BaseUserOrientation(self, value: typing.Any): ...

    @property
    def Count(self) -> int:  # iviewprojectionmanager_count.html
        """
        ## Count - Количество проекций

        Тип данных: long

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        Count = Object.Count       # Получить свойство (* )
        Count = Object.GetCount()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def Matrix3D(self) -> typing.Any:  # iviewprojectionmanager_matrix3d.html
        """
        ## Matrix3D - Текущая матрица отображения в окне документа

        Тип данных:VARIANT

        1.Свойство доступно только для чтения.

        2.Для изменения текущей ориентации используется метод IViewProjection7_SetMatrix3D.

        3.Элементы матрицы возвращаются в виде одномерного массива из 16 элементов SafeAttray типа (VT_ARRAY | VT_R8). Матрица имеет размер 4х4.

        ### Синтаксис Automation

        ```
        Matrix3D = Object.Matrix3D       # Получить свойство (* )
        Matrix3D = Object.GetMatrix3D()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def Scale(self) -> float:  # iviewprojectionmanager_scale.html
        """
        ## Scale - Масштаб, установленный в текущем окне документа

        Тип данных: double

        ### Синтаксис Automation

        ```
        Scale = Object.Scale       # Получить свойство (* )
        Object.Scale = Scale       # Установить свойство (* )
        Scale = Object.GetScale()  # Получить свойство (**)
        Object.SetScale( Scale )   # Установить свойство (**)
        ```
        """
        ...
    @Scale.setter
    def Scale(self, value: float): ...

    @property
    def ShowDynamicSection(self) -> bool:  # iviewprojectionmanager_showdynamicsection.html
        """
        ## ShowDynamicSection - Отображать сохраненное динамическое сечение

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        ShowDynamicSection = Object.ShowDynamicSection       # Получить свойство (* )
        Object.ShowDynamicSection = ShowDynamicSection       # Установить свойство (* )
        ShowDynamicSection = Object.GetShowDynamicSection()  # Получить свойство (**)
        Object.SetShowDynamicSection( ShowDynamicSection )   # Установить свойство (**)
        ```
        """
        ...
    @ShowDynamicSection.setter
    def ShowDynamicSection(self, value: bool): ...

    @property
    def ShowLayersStates(self) -> bool:  # iviewprojectionmanager_showlayersstates.html
        """
        ## ShowLayersStates - Отображать сохраненные состояния слоев

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        ShowLayersStates = Object.ShowLayersStates       # Получить свойство (* )
        Object.ShowLayersStates = ShowLayersStates       # Установить свойство (* )
        ShowLayersStates = Object.GetShowLayersStates()  # Получить свойство (**)
        Object.SetShowLayersStates( ShowLayersStates )   # Установить свойство (**)
        ```
        """
        ...
    @ShowLayersStates.setter
    def ShowLayersStates(self, value: bool): ...

    @property
    def ViewProjectionScheme(self) -> int:  # iviewprojectionmanager_viewprojectionscheme.html
        """
        ## ViewProjectionScheme - Текущая схема ориентаций модели

        Тип данных: ksViewProjectionScheme

        ### Синтаксис Automation

        ```
        ViewProjectionScheme = Object.ViewProjectionScheme       # Получить свойство (* )
        Object.ViewProjectionScheme = ViewProjectionScheme       # Установить свойство (* )
        ViewProjectionScheme = Object.GetViewProjectionScheme()  # Получить свойство (**)
        Object.SetViewProjectionScheme( ViewProjectionScheme )   # Установить свойство (**)
        ```
        """
        ...
    @ViewProjectionScheme.setter
    def ViewProjectionScheme(self, value: int): ...

    def Add(self):  # iviewprojectionmanager_add.html
        """
        ## Add - Добавить новую пользовательскую проекцию

        Метод позволяет создать новый интерфейс стиля.

        ### Синтаксис Automation

        ```
        LPDISPATCH Add();
        ```

        ### Возвращаемое значение

        - Указатель на интерфейс новой пользовательской проекцииIViewProjection7
        """
        ...

    def AddUnfoldProjection(self, Place) -> IViewProjection7:  # iviewprojectionmanager_addunfoldprojection.html
        """
        ## AddUnfoldProjection - Добавить проекцию отображения - развертка

        В массиве должны лежать 16 элементов, которые представляют собой матрицу размера 4х4.

        ### Синтаксис Automation

        ```
        LPDISPATCH AddUnfoldProjection( VARIANT place );
        ```

        ### Входные параметры

        +--------------------------------------------------------------------+
        | Place | - динамический массив SAFEARRAY double (VT_ARRAY | VT_R8), |
        +--------------------------------------------------------------------+

        ### Возвращаемое значение

        - Указатель на интерфейс проекции /*IViewProjection7*/
        """
        ...

    def SetMatrix3D(self, Matrix3D, Scale) -> bool:  # iviewprojectionmanager_setmatrix3d.html
        """
        ## SetMatrix3D - Установить систему координат отображения модели в окне по матрице и масштабу

        В массиве должны лежать 16 элементов, которые представляют собой матрицу размера 4х4.

        ### Синтаксис Automation

        ```
        BOOL SetMatrix3D( VARIANT Matrix3D, double Scale );
        ```

        ### Входные параметры

        +-----------------------------------------------------------------------+
        | Matrix3D | - динамический массив SAFEARRAY double (VT_ARRAY | VT_R8), |
        +-----------------------------------------------------------------------+
        | Scale    | - масштаб.                                                 |
        +-----------------------------------------------------------------------+

        ### Возвращаемое значение

        +-----------------------------------------+
        | TRUE  | - в случае удачного завершения, |
        +-----------------------------------------+
        | FALSE | - в случае неудачи.             |
        +-----------------------------------------+
        """
        ...

    def ViewProjection(self, Index) -> IViewProjection7:  # iviewprojectionmanager_viewprojection.html
        """
        ## ViewProjection - Получить интерфейс проекции по индексу или имени

        Тип данных: Указатель на интерфейс IViewProjection7

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        ViewProjection = Object.ViewProjection( Index )     # Получить свойство (* )
        ViewProjection = Object.GetViewProjection( Index )  # Получить свойство (**)
        ```
        """
        ...




class IViewProjection7(IKompasAPIObject):  # iviewprojection7.html
    """
    ## Интерфейс IViewProjection7

    Интерфейс проекции отображения модели в окне.

    Интерфейс можно получить, используя свойства и методы интерфейса IViewProjectionManager.

    Версия КОМПАС v22
    """

    @property
    def ApplyDynamicCrossSection(self) -> int:  # iviewprojection7_applydynamiccrosssection.html
        """
        ## ApplyDynamicCrossSection - Включить динамическое сечение

        Тип данных: long

        ### Синтаксис Automation

        ```
        ApplyDynamicCrossSection = Object.ApplyDynamicCrossSection       # Получить свойство (* )
        Object.ApplyDynamicCrossSection = ApplyDynamicCrossSection       # Установить свойство (* )
        ApplyDynamicCrossSection = Object.GetApplyDynamicCrossSection()  # Получить свойство (**)
        Object.SetApplyDynamicCrossSection( ApplyDynamicCrossSection )   # Установить свойство (**)
        ```
        """
        ...
    @ApplyDynamicCrossSection.setter
    def ApplyDynamicCrossSection(self, value: int): ...

    @property
    def Current(self) -> bool:  # iviewprojection7_current.html
        """
        ## Current - Признак является ли данная проекция текущей

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        Current = Object.Current       # Получить свойство (* )
        Object.Current = Current       # Установить свойство (* )
        Current = Object.GetCurrent()  # Получить свойство (**)
        Object.SetCurrent( Current )   # Установить свойство (**)
        ```
        """
        ...
    @Current.setter
    def Current(self, value: bool): ...

    @property
    def DynamicCrossSection(self) -> IDynamicCrossSection:  # iviewprojection7_dynamiccrosssection.html
        """
        ## DynamicCrossSection - Динамическое сечение

        Тип данных: Указатель на интерфейс IDynamicCrossSection

        ### Синтаксис Automation

        ```
        DynamicCrossSection = Object.DynamicCrossSection       # Получить свойство (* )
        Object.DynamicCrossSection = DynamicCrossSection       # Установить свойство (* )
        DynamicCrossSection = Object.GetDynamicCrossSection()  # Получить свойство (**)
        Object.SetDynamicCrossSection( DynamicCrossSection )   # Установить свойство (**)
        ```
        """
        ...
    @DynamicCrossSection.setter
    def DynamicCrossSection(self, value: IDynamicCrossSection): ...

    @property
    def Name(self) -> str:  # iviewprojection7_name.html
        """
        ## Name - Имя проекции

        Тип данных: BSTR

        ### Синтаксис Automation

        ```
        Name = Object.Name       # Получить свойство (* )
        Object.Name = Name       # Установить свойство (* )
        Name = Object.GetName()  # Получить свойство (**)
        Object.SetName( Name )   # Установить свойство (**)
        ```
        """
        ...
    @Name.setter
    def Name(self, value: str): ...

    @property
    def Placement(self) -> IPlacement3D:  # iviewprojection7_placement.html
        """
        ## Placement - Система координат отображения модели в окне

        Тип данных: Указатель на интерфейс IPlacement3D

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        Placement = Object.Placement       # Получить свойство (* )
        Placement = Object.GetPlacement()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def Scale(self) -> float:  # iviewprojection7_scale.html
        """
        ## Scale - Масштаб, установленный в текущем окне документа

        Тип данных: double

        ### Синтаксис Automation

        ```
        Scale = Object.Scale       # Получить свойство (* )
        Object.Scale = Scale       # Установить свойство (* )
        Scale = Object.GetScale()  # Получить свойство (**)
        Object.SetScale( Scale )   # Установить свойство (**)
        ```
        """
        ...
    @Scale.setter
    def Scale(self, value: float): ...

    @property
    def UserProjectionIndex(self) -> int:  # iviewprojection7_userprojectionindex.html
        """
        ## UserProjectionIndex - Индекс пользовательской проекции

        Тип данных: long

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        UserProjectionIndex = Object.UserProjectionIndex       # Получить свойство (* )
        UserProjectionIndex = Object.GetUserProjectionIndex()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def ViewProjectonType(self) -> int:  # iviewprojection7_viewprojectontype.html
        """
        ## ViewProjectonType - Тип проекции

        Тип данных: ksViewProjectionType

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        ViewProjectonType = Object.ViewProjectonType       # Получить свойство (* )
        ViewProjectonType = Object.GetViewProjectonType()  # Получить свойство (**)
        ```
        """
        ...

    def Delete(self) -> bool:  # iviewprojection7_delete.html
        """
        ## Delete - Удалить пользовательскую проекцию

        ### Синтаксис Automation

        ```
        BOOL Delete();
        ```

        ### Возвращаемое значение

        +-----------------------------------------+
        | TRUE  | - в случае удачного завершения, |
        +-----------------------------------------+
        | FALSE | - в случае неудачи.             |
        +-----------------------------------------+
        """
        ...

    def SetMatrix3D(self, Matrix3D) -> bool:  # iviewprojection7_setmatrix3d.html
        """
        ## SetMatrix3D - Установить систему координат отображения модели в окне по матрице

        Элементы матрицы возвращаются в виде одномерного массива из 16 элементов SafeAttray типа (VT_ARRAY | VT_R8).

        Матрица имеет размер 4х4.

        ### Синтаксис Automation

        ```
        BOOL SetMatrix3D( VARIANT Matrix3D );
        ```

        ### Входные параметры

        +--------------------------------------------------------------------------------+
        | Matrix3D | - Матрица. Динамический массив SAFEARRAY double (VT_ARRAY | VT_R8), |
        +--------------------------------------------------------------------------------+

        ### Возвращаемое значение

        +-----------------------------------------+
        | TRUE  | - в случае удачного завершения, |
        +-----------------------------------------+
        | FALSE | - в случае неудачи.             |
        +-----------------------------------------+
        """
        ...

    def Update(self) -> bool:  # iviewprojection7_update.html
        """
        ## Update - Создать пользовательскую проекцию

        ### Синтаксис Automation

        ```
        BOOL Update();
        ```

        ### Возвращаемое значение

        +-----------------------------------------+
        | TRUE  | - в случае удачного завершения, |
        +-----------------------------------------+
        | FALSE | - в случае неудачи.             |
        +-----------------------------------------+
        """
        ...




class IStylesManager(IDispatch):  # istylesmanager.html
    """
    ## Интерфейс IStylesManager

    Менеджер стилей.

    Интерфейс является дополнительным для интерфейсов:

    •IApplication - позволяет получать системные стили.

    •IKompasDocument - позволяет получать\\создавать пользовательские стили.

    Интерфейс можно получить посредством вызова метода IUnknown::QueryInterface (const GUID far& iid, void** pif).

    Версия KOMPAS v19
    """

    @property
    def CurvesStyles(self) -> IStyles:  # istylesmanager_curvesstyles.html
        """
        ## CurvesStyles - Стили линий

        Тип данных: Указатель на интерфейс IStyles.

        Свойство доступно только для чтения.

        Свойство не работает в текстовом документе и спецификации.

        ### Синтаксис Automation

        ```
        CurvesStyles = Object.CurvesStyles       # Получить свойство(* )
        CurvesStyles = Object.GetCurvesStyles()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def HatchsStyles(self) -> IStyles:  # istylesmanager_hatchsstyles.html
        """
        ## HatchsStyles - Стили штриховок

        Тип данных: Указатель на интерфейс IStyles.

        Свойство доступно только для чтения.

        Свойство не работает в текстовом документе и спецификации.

        ### Синтаксис Automation

        ```
        HatchsStyles = Object.HatchsStyles       # Получить свойство(* )
        HatchsStyles = Object.GetHatchsStyles()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def TextsStyles(self) -> IStyles:  # istylesmanager_textsstyles.html
        """
        ## TextsStyles - Стили текстов

        Тип данных: Указатель на интерфейс IStyles.

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        TextsStyles = Object.TextsStyles       # Получить свойство(* )
        TextsStyles = Object.GetTextsStyles()  # Получить свойство (**)
        ```
        """
        ...




class IStyles(IKompasCollection):  # istyles.html
    """
    ## Интерфейс IStyles

    Интерфейс коллекции стилей.

    Интерфейс можно получить с помощью метода IApplication::LibraryStyles или с помощью методов менеджера стилей IStylesManager.

    Версия KOMPAS v19
    """

    def Add(self) -> IStyle:  # istyles_add.html
        """
        ## Add - Создает новый элемент и добавляет его в коллекцию

        Метод позволяет создать новый интерфейс стиля.

        ### Синтаксис Automation

        ```
        LPDISPATCH Add();
        ```

        ### Возвращаемое значение

        - Указатель на интерфейс IStyle
        """
        ...

    def AddStyleFromLibrary(self, LibraryPath, Id, External) -> IStyle:  # istyles_addstylefromlibrary.html
        """
        ## AddStyleFromLibrary - Добавить стиль из библиотеки

        ### Синтаксис Automation

        ```
        LPDISPATCH AddStyleFromLibrary (BSTR LibraryPath, long Id, BOOL External :
        ```

        ### Входные параметры

        +----------------------------------------------+
        | LibraryPath | - имя файла библиотеки стилей, |
        +----------------------------------------------+
        | Id          | - номер стиля в библиотеке,    |
        +----------------------------------------------+
        | External    | - признак внешнего стиля.      |
        +----------------------------------------------+

        ### Возвращаемое значение

        - Указатель на интерфейс IStyle
        """
        ...

    def Copy(self, Style) -> IStyle:  # istyles_copy.html
        """
        ## Copy - Копировать стиль

        ### Синтаксис Automation

        ```
        LPDISPATCH Copy (IStyle * Style );
        ```

        ### Входные параметры

        +------------------+
        | Style | - стиль. |
        +------------------+

        ### Возвращаемое значение

        - Указатель на интерфейс IStyle
        """
        ...

    def FindStyleFromLibrary(self, LibraryPath, LibraryStyleId) -> IStyle:  # istyles_findstylefromlibrary.html
        """
        ## FindStyleFromLibrary - Возвращает внешний стиль, если он был добавлен из библиотеки

        ### Синтаксис Automation

        ```
        LPDISPATCH FindStyleFromLibrary( BSTR LibraryPath, long LibraryStyleId );
        ```

        ### Входные параметры

        +-------------------------------------------------+
        | LibraryPath    | - имя файла библиотеки стилей, |
        +-------------------------------------------------+
        | LibraryStyleId | - уникальный номер стиля.      |
        +-------------------------------------------------+

        ### Возвращаемое значение

        - Указатель на интерфейс IStyle
        """
        ...

    def Item(self, Index) -> IStyle:  # istyles_item.html
        """
        ## Item - Возвращает элемент, заданный по индексу или по имени

        Тип данных: Указатель на интерфейс IStyle

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        Item = Object.Item(Index)     # Получить свойство (* )
        Item = Object.GetItem(Index)  # Получить свойство (**)
        ```

        ### Входные параметры

        +--------------------------------------------+
        | VARIANT Index | - индекс или имя элемента. |
        +--------------------------------------------+
        """
        ...

    def StyleByApiId(self, Id) -> IStyle:  # istyles_stylebyapiid.html
        """
        ## StyleByApiId - Возвращает элемент, заданный по идентификатору стиля API

        Тип данных: Указатель на интерфейс IStyle

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        StyleByApiId = Object.StyleByApiId(Id)     # Получить свойство (* )
        StyleByApiId = Object.GetStyleByApiId(Id)  # Получить свойство (**)
        ```

        ### Входные параметры

        +----------------------------------------+
        | long Id | - идентификатор стиля в API. |
        +----------------------------------------+
        """
        ...




class IStyle(IKompasAPIObject):  # istyle.html
    """
    ## Интерфейс IStyle

    Интерфейс стиля.
    """

    @property
    def ApiStyleId(self) -> int:  # istyle_apistyleid.html
        """
        ## ApiStyleId - Идентификатор стиля в API

        Тип данных: long

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        ApiStyleId = Object.ApiStyleId       # Получить свойство (* )
        ApiStyleId = Object.GetApiStyleId()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def DisplayStyleId(self) -> int:  # istyle_displaystyleid.html
        """
        ## DisplayStyleId - Отображаемый идентификатор стиля

        Тип данных: long

        ### Синтаксис Automation

        ```
        DisplayStyleId = Object.DisplayStyleId       # Получить свойство (* )
        Object.DisplayStyleId = DisplayStyleId       # Установить свойство (* )
        DisplayStyleId = Object.GetDisplayStyleId()  # Получить свойство (**)
        Object.SetDisplayStyleId( DisplayStyleId )   # Установить свойство (**)
        ```
        """
        ...
    @DisplayStyleId.setter
    def DisplayStyleId(self, value: int): ...

    @property
    def IsExternalStyle(self) -> bool:  # istyle_isexternalstyle.html
        """
        ## IsExternalStyle - Внешний стиль, загружаемый из библиотеки стилей

        Тип данных: BOOL

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        IsExternalStyle = Object.IsExternalStyle     # Получить свойство (* )
        IsExternalStyle = Object.GetIsExternalStyle  # Получить свойство (**)
        ```
        """
        ...

    @property
    def LibraryPath(self) -> str:  # istyle_librarypath.html
        """
        ## LibraryPath - Путь к файлу библиотеки стилей

        Тип данных: BSTR

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        LibraryPath = Object.LibraryPath       # Получить свойство (* )
        LibraryPath = Object.GetLibraryPath()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def LibraryStyleId(self) -> int:  # istyle_librarystyleid.html
        """
        ## LibraryStyleId - Идентификатор стиля в библиотеке стилей

        Тип данных: long

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        LibraryStyleId = Object.LibraryStyleId       # Получить свойство (* )
        LibraryStyleId = Object.GetLibraryStyleId()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def Name(self) -> str:  # istyle_name.html
        """
        ## Name - Имя стиля

        Тип данных: BSTR

        ### Синтаксис Automation

        ```
        Name = Object.Name       # Получить свойство (* )
        Object.Name = Name       # Установить свойство (* )
        Name = Object.GetName()  # Получить свойство (**)
        Object.SetName( Name )   # Установить свойство (**)
        ```
        """
        ...
    @Name.setter
    def Name(self, value: str): ...

    def Delete(self) -> bool:  # istyle_delete.html
        """
        ## Delete - Удалить стиль

        ### Синтаксис Automation

        ```
        BOOL Delete();
        ```

        ### Возвращаемое значение

        +-----------------------------------------+
        | TRUE | - в случае успешного завершения. |
        +-----------------------------------------+
        """
        ...

    def Update(self) -> bool:  # istyle_update.html
        """
        ## Update - Обновление данных

        ### Синтаксис Automation

        ```
        BOOL Update();
        ```

        ### Возвращаемое значение

        +-----------------------------------------+
        | TRUE | - в случае успешного завершения. |
        +-----------------------------------------+
        """
        ...




class ICurveStyle(IStyle):  # icurvestyle.html
    """
    ## Интерфейс ICurveStyle

    Интерфейс стиля линии.

    Интерфейс можно получить с помощью методов коллекции стилей линий, полученной с помощью

    •метода IStylesManager::CurvesStyles,

    •коллекции стилей библиотеки стилей линий (*.lcs) IApplication::LibraryStyles.

    Версия: KOMPAS v19
    """

    @property
    def Color(self) -> int:  # icurvestyle_color.html
        """
        ## Color - Цвет

        Тип данных: long

        ### Синтаксис Automation

        ```
        Color = Object.Color       # Получить свойство (* )
        Object.Color = Color       # Установить свойство (**)
        Color = Object.GetColor()  # Получить свойство (**)
        Object.SetColor( Color )   # Установить свойство (**)
        ```
        """
        ...
    @Color.setter
    def Color(self, value: int): ...

    @property
    def CurvePenType(self) -> int:  # icurvestyle_curvepentype.html
        """
        ## CurvePenType - Способ задания параметров пера

        Тип данных: из перечисления ksCurvePenTypeEnum.

        ### Синтаксис Automation

        ```
        CurvePenType = Object.CurvePenType       # Получить свойство (* )
        Object.CurvePenType = CurvePenType       # Установить свойство (**)
        CurvePenType = Object.GetCurvePenType()  # Получить свойство (**)
        Object.SetCurvePenType( CurvePenType )   # Установить свойство (**)
        ```
        """
        ...
    @CurvePenType.setter
    def CurvePenType(self, value: int): ...

    @property
    def CurveStyleType(self) -> int:  # icurvestyle_curvestyletype.html
        """
        ## CurveStyleType - Тип стиля кривой

        Тип данных: из перечисления ksCurveStyleTypeEnum

        ### Синтаксис Automation

        ```
        CurveStyleType = Object.CurveStyleType       # Получить свойство (* )
        Object.CurveStyleType = CurveStyleType       # Установить свойство (**)
        CurveStyleType = Object.GetCurveStyleType()  # Получить свойство (**)
        Object.SetCurveStyleType( CurveStyleType )   # Установить свойство (**)
        ```
        """
        ...
    @CurveStyleType.setter
    def CurveStyleType(self, value: int): ...

    @property
    def ForHatch(self) -> bool:  # icurvestyle_forhatch.html
        """
        ## ForHatch - Является границей для штриховки

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        ForHatch = Object.ForHatch       # Получить свойство (* )
        Object.ForHatch = ForHatch       # Установить свойство (**)
        ForHatch = Object.GetForHatch()  # Получить свойство (**)
        Object.SetForHatch( ForHatch )   # Установить свойство (**)
        ```
        """
        ...
    @ForHatch.setter
    def ForHatch(self, value: bool): ...

    @property
    def IgnoreFragmentStyle(self) -> bool:  # icurvestyle_ignorefragmentstyle.html
        """
        ## IgnoreFragmentStyle - Игнорировать стиль линий фрагментов

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        IgnoreFragmentStyle = Object.IgnoreFragmentStyle       # Получить свойство (* )
        Object.IgnoreFragmentStyle = IgnoreFragmentStyle       # Установить свойство (**)
        IgnoreFragmentStyle = Object.GetIgnoreFragmentStyle()  # Получить свойство (**)
        Object.SetIgnoreFragmentStyle( IgnoreFragmentStyle )   # Установить свойство (**)
        ```
        """
        ...
    @IgnoreFragmentStyle.setter
    def IgnoreFragmentStyle(self, value: bool): ...

    @property
    def PaperWidth(self) -> float:  # icurvestyle_paperwidth.html
        """
        ## PaperWidth - Толщина на бумаге (мм)

        Тип данных: double

        ### Синтаксис Automation

        ```
        PaperWidth = Object.PaperWidth       # Получить свойство (* )
        Object.PaperWidth = PaperWidth       # Установить свойство (**)
        PaperWidth = Object.GetPaperWidth()  # Получить свойство (**)
        Object.SetPaperWidth( PaperWidth )   # Установить свойство (**)
        ```
        """
        ...
    @PaperWidth.setter
    def PaperWidth(self, value: float): ...

    @property
    def PatternsCount(self) -> int:  # icurvestyle_patternscount.html
        """
        ## PatternsCount - Количество параметров пары штрих-промежуток

        Тип данных: long

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        PatternsCount = Object.PatternsCount       # Получить свойство(* )
        PatternsCount = Object.GetPatternsCount()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def ScreenWidth(self) -> int:  # icurvestyle_screenwidth.html
        """
        ## ScreenWidth - Толщина на экране (пикс)

        Тип данных: long

        ### Синтаксис Automation

        ```
        ScreenWidth = Object.ScreenWidth       # Получить свойство (* )
        Object.ScreenWidth = ScreenWidth       # Установить свойство (**)
        ScreenWidth = Object.GetScreenWidth()  # Получить свойство (**)
        Object.SetScreenWidth( ScreenWidth )   # Установить свойство (**)
        ```
        """
        ...
    @ScreenWidth.setter
    def ScreenWidth(self, value: int): ...

    @property
    def SmartParts(self) -> bool:  # icurvestyle_smartparts.html
        """
        ## SmartParts - Кривая всегда заканчивается штрихом

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        SmartParts = Object.SmartParts       # Получить свойство (* )
        Object.SmartParts = SmartParts       # Установить свойство (**)
        SmartParts = Object.GetSmartParts()  # Получить свойство (**)
        Object.SetSmartParts( SmartParts )   # Установить свойство (**)
        ```
        """
        ...
    @SmartParts.setter
    def SmartParts(self, value: bool): ...

    def AddPattern(self, VisibleSegmentLenght, invisibleSegmentLenght) -> bool:  # icurvestyle_addpattern.html
        """
        ## AddPattern - Добавить параметры пары штрих-промежуток

        ### Синтаксис Automation

        ```
        BOOL AddPattern( double VisibleSegmentLenght, double invisibleSegmentLenght );
        ```

        ### Входные параметры

        +------------------------------------------------------+
        | VisibleSegmentLenght   | - длина видимого участка,   |
        +------------------------------------------------------+
        | invisibleSegmentLenght | - длина невидимого участка. |
        +------------------------------------------------------+

        ### Возвращаемое значение

        +-----------------------------------------+
        | TRUE | - в случае успешного завершения. |
        +-----------------------------------------+
        """
        ...

    def AddPatternFragmentFilletsPoligon(self, PatternIndex, Points) -> bool:  # icurvestyle_addpatternfragmentfilletspoligon.html
        """
        ## AddPatternFragmentFilletsPoligon - Добавить заливку, ограниченную ломаной, заданной полигоном точек

        ### Синтаксис Automation

        ```
        BOOL AddPatternFragmentFilletsPoligon( long PatternIndex, VARIANT Points );
        ```

        ### Входные параметры

        +-----------------------------------------------------------------------------------------------------------+
        | PatternIndex | - индекс параметров,                                                                       |
        +-----------------------------------------------------------------------------------------------------------+
        | Points       | - безопасный массив 2D точек SAFEARRAY VT_ARRAY | VT_R8, задающих полигон границы заливки. |
        +-----------------------------------------------------------------------------------------------------------+

        ### Возвращаемое значение

        +-----------------------------------------+
        | TRUE | - в случае успешного завершения. |
        +-----------------------------------------+
        """
        ...

    def AddPatternFragmentPoligon(self, PatternIndex, Points) -> bool:  # icurvestyle_addpatternfragmentpoligon.html
        """
        ## AddPatternFragmentPoligon - Добавить полигон точек

        ### Синтаксис Automation

        ```
        BOOL AddPatternFragmentPoligon( long PatternIndex, VARIANT Points );
        ```

        ### Входные параметры

        +-------------------------------------------------------------------------+
        | PatternIndex | - индекс параметров,                                     |
        +-------------------------------------------------------------------------+
        | Points       | - безопасный массив 2D точек SAFEARRAY VT_ARRAY | VT_R8. |
        +-------------------------------------------------------------------------+

        ### Возвращаемое значение

        +-----------------------------------------+
        | TRUE | - в случае успешного завершения. |
        +-----------------------------------------+
        """
        ...

    def ClearPatternFragmentFilletsPoligons(self, PatternIndex) -> bool:  # icurvestyle_clearpatternfragmentfilletspoligons.html
        """
        ## ClearPatternFragmentFilletsPoligons - Удалить все заливки

        ### Синтаксис Automation

        ```
        BOOL ClearPatternFragmentFilletsPoligons( long PatternIndex );
        ```

        ### Входные параметры

        +----------------------------------+
        | PatternIndex | - индекс образца. |
        +----------------------------------+

        ### Возвращаемое значение

        +-----------------------------------------+
        | TRUE | - в случае успешного завершения. |
        +-----------------------------------------+
        """
        ...

    def ClearPatternFragmentPoligons(self, PatternIndex) -> bool:  # icurvestyle_clearpatternfragmentpoligons.html
        """
        ## ClearPatternFragmentPoligons - Очистить список полигонов

        ### Синтаксис Automation

        ```
        BOOL ClearPatternFragmentPoligons( long PatternIndex );
        ```

        ### Входные параметры

        +-------------------------------------+
        | PatternIndex | - индекс параметров. |
        +-------------------------------------+

        ### Возвращаемое значение

        +-----------------------------------------+
        | TRUE | - в случае успешного завершения. |
        +-----------------------------------------+
        """
        ...

    def ClearPatterns(self) -> bool:  # icurvestyle_clearpatterns.html
        """
        ## ClearPatterns - Очистить параметры участков штрих-промежуток

        ### Синтаксис Automation

        ```
        BOOL ClearPatterns();
        ```

        ### Возвращаемое значение

        +-----------------------------------------+
        | TRUE | - в случае успешного завершения. |
        +-----------------------------------------+
        """
        ...

    def DeletePattern(self, PatternIndex) -> bool:  # icurvestyle_deletepattern.html
        """
        ## DeletePattern - Удалить параметры пары штрих-промежуток

        ### Синтаксис Automation

        ```
        BOOL DeletePattern( long PatternIndex );
        ```

        ### Входные параметры

        +-------------------------------------+
        | PatternIndex | - индекс параметров. |
        +-------------------------------------+

        ### Возвращаемое значение

        +-----------------------------------------+
        | TRUE | - в случае успешного завершения. |
        +-----------------------------------------+
        """
        ...

    def DeletePatternFragmentFilletsPoligon(self, PatternIndex, PoligonIndex) -> bool:  # icurvestyle_deletepatternfragmentfilletspoligon.html
        """
        ## DeletePatternFragmentFilletsPoligon - Удалить заливку

        ### Синтаксис Automation

        ```
        BOOL DeletePatternFragmentFilletsPoligon( long PatternIndex, long PoligonIndex );
        ```

        ### Входные параметры

        +----------------------------------------+
        | PatternIndex      | - индекс образца,  |
        +----------------------------------------+
        | long PoligonIndex | - индекс полигона. |
        +----------------------------------------+

        ### Возвращаемое значение

        +-----------------------------------------+
        | TRUE | - в случае успешного завершения. |
        +-----------------------------------------+
        """
        ...

    def DeletePatternFragmentPoligon(self, PatternIndex, PoligonIndex) -> bool:  # icurvestyle_deletepatternfragmentpoligon.html
        """
        ## DeletePatternFragmentPoligon - Удалить полигон

        ### Синтаксис Automation

        ```
        BOOL DeletePatternFragmentPoligon( long PatternIndex, long PoligonIndex );
        ```

        ### Входные параметры

        +------------------------------------------+
        | PatternIndex      | - индекс параметров, |
        +------------------------------------------+
        | long PoligonIndex | - индекс полигона.   |
        +------------------------------------------+

        ### Возвращаемое значение

        +-----------------------------------------+
        | TRUE | - в случае успешного завершения. |
        +-----------------------------------------+
        """
        ...

    def LoadPatternFragment(self, PatternIndex, FileName) -> bool:  # icurvestyle_loadpatternfragment.html
        """
        ## LoadPatternFragment - Загрузить фрагмент из файла

        ### Синтаксис Automation

        ```
        BOOL LoadPatternFragment( long PatternIndex, BSTR FileName );
        ```

        ### Входные параметры

        +----------------------------------+
        | PatternIndex | - индекс образца, |
        +----------------------------------+
        | FileNamex    | - имя фрагмента.  |
        +----------------------------------+

        ### Возвращаемое значение

        +-----------------------------------------+
        | TRUE | - в случае успешного завершения. |
        +-----------------------------------------+
        """
        ...

    def PatternFragmentDx(self, PatternIndex) -> float:  # icurvestyle_patternfragmentdx.html
        """
        ## PatternFragmentDx - Величина смещения фрагмента по Х

        Тип данных: double

        ### Синтаксис Automation

        ```
        PatternFragmentDx = Object.PatternFragmentDx( PatternIndex )      # Получить свойство (* )
        Object.PatternFragmentDx( PatternIndex ) = PatternFragmentDx      # Установить свойство (**)
        PatternFragmentDx = Object.GetPatternFragmentDx( PatternIndex ))  # Получить свойство (**)
        Object.SetPatternFragmentDx( PatternIndex, PatternFragmentDx )    # Установить свойство (**)
        ```

        ### Входные параметры

        +------------------------------------------+
        | long PatternIndex | - индекс параметров. |
        +------------------------------------------+
        """
        ...

    def PatternFragmentDy(self, PatternIndex) -> float:  # icurvestyle_patternfragmentdy.html
        """
        ## PatternFragmentDy - Величина смещения фрагмента по Y

        Тип данных: double

        ### Синтаксис Automation

        ```
        PatternFragmentDy = Object.PatternFragmentDy( PatternIndex )      # Получить свойство (* )
        Object.PatternFragmentDy( PatternIndex ) = PatternFragmentDy      # Установить свойство (**)
        PatternFragmentDy = Object.GetPatternFragmentDy( PatternIndex ))  # Получить свойство (**)
        Object.SetPatternFragmentDy( PatternIndex, PatternFragmentDy )    # Установить свойство (**)
        ```

        ### Входные параметры

        +------------------------------------------+
        | long PatternIndex | - индекс параметров. |
        +------------------------------------------+
        """
        ...

    def PatternFragmentFilletsPoligon(self, PatternIndex, PoligonIndex) -> typing.Any:  # icurvestyle_patternfragmentfilletspoligon.html
        """
        ## PatternFragmentFilletsPoligon - Массив точек полигона границ заливок

        Тип данных: VARIANT

        ### Синтаксис Automation

        ```
        PatternFragmentFilletsPoligon = Object.PatternFragmentFilletsPoligon( PatternIndex, PoligonIndex )     # Получить свойство (* )
        Object.PatternFragmentFilletsPoligon( PatternIndex, PoligonIndex ) = PatternFragmentFilletsPoligon     # Установить свойство (**)
        PatternFragmentFilletsPoligon = Object.GetPatternFragmentFilletsPoligon( PatternIndex, PoligonIndex )  # Получить свойство (**)
        Object.SetPatternFragmentFilletsPoligon( PatternIndex, PoligonIndex, PatternFragmentFilletsPoligon )   # Установить свойство (**)
        ```

        ### Входные параметры

        +------------------------------------------+
        | long PatternIndex | - индекс параметров, |
        +------------------------------------------+
        | long PoligonIndex | - индекс полигона.   |
        +------------------------------------------+
        """
        ...

    def PatternFragmentFilletsPoligonsCount(self, PatternIndex):
        ...

    def PatternFragmentPoligon(self, PatternIndex, PoligonIndex) -> typing.Any:  # icurvestyle_patternfragmentpoligon.html
        """
        ## PatternFragmentPoligon - Массив точек полигона

        Тип данных: VARIANT

        ### Синтаксис Automation

        ```
        PatternFragmentPoligon = Object.PatternFragmentPoligon( PatternIndex, PoligonIndex )     # Получить свойство (* )
        Object.PatternFragmentPoligon( PatternIndex, PoligonIndex ) = PatternFragmentPoligon     # Установить свойство (**)
        PatternFragmentPoligon = Object.GetPatternFragmentPoligon( PatternIndex, PoligonIndex )  # Получить свойство (**)
        Object.SetPatternFragmentPoligon( PatternIndex, PoligonIndex, PatternFragmentPoligon )   # Установить свойство (**)
        ```

        ### Входные параметры

        +------------------------------------------+
        | long PatternIndex | - индекс параметров, |
        +------------------------------------------+
        | long PoligonIndex | - индекс полигона.   |
        +------------------------------------------+
        """
        ...

    def PatternFragmentPoligonsCount(self, PatternIndex) -> int:  # icurvestyle_patternfragmentpoligonscount.html
        """
        ## PatternFragmentPoligonsCount - Количество ломаных во фрагменте

        Тип данных: long

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        PatternFragmentPoligonsCount = Object.PatternFragmentPoligonsCount( PatternIndex )     # Получить свойство(* )
        PatternFragmentPoligonsCount = Object.GetPatternFragmentPoligonsCount( PatternIndex )  # Получить свойство (**)
        ```

        ### Входные параметры

        +------------------------------------------+
        | long PatternIndex | - индекс параметров. |
        +------------------------------------------+
        """
        ...

    def PatternInvisibleSegmentLenght(self, PatternIndex) -> float:  # icurvestyle_patterninvisiblesegmentlenght.html
        """
        ## PatternInvisibleSegmentLenght - Длина невидимого участка

        Тип данных: double

        ### Синтаксис Automation

        ```
        PatternInvisibleSegmentLenght = Object.PatternInvisibleSegmentLenght( PatternIndex )     # Получить свойство (* )
        Object.PatternInvisibleSegmentLenght( PatternIndex ) = PatternInvisibleSegmentLenght     # Установить свойство (**)
        PatternInvisibleSegmentLenght = Object.GetPatternInvisibleSegmentLenght( PatternIndex )  # Получить свойство (**)
        Object.SetPatternInvisibleSegmentLenght( PatternIndex, PatternInvisibleSegmentLenght )   # Установить свойство (**)
        ```

        ### Входные параметры

        +------------------------------------------+
        | long PatternIndex | - индекс параметров. |
        +------------------------------------------+
        """
        ...

    def PatternVisibleSegmentLenght(self, PatternIndex) -> float:  # icurvestyle_patternvisiblesegmentlenght.html
        """
        ## PatternVisibleSegmentLenght - Длина видимого участка

        Тип данных: double

        ### Синтаксис Automation

        ```
        PatternVisibleSegmentLenght = Object.PatternVisibleSegmentLenght( PatternIndex )     # Получить свойство (* )
        Object.PatternVisibleSegmentLenght( PatternIndex ) = PatternVisibleSegmentLenght     # Установить свойство (**)
        PatternVisibleSegmentLenght = Object.GetPatternVisibleSegmentLenght( PatternIndex )  # Получить свойство (**)
        Object.SetPatternVisibleSegmentLenght( PatternIndex, PatternVisibleSegmentLenght )   # Установить свойство (**)
        ```

        ### Входные параметры

        +------------------------------------------+
        | long PatternIndex | - индекс параметров. |
        +------------------------------------------+
        """
        ...

    def SetPatternFragment(self, PatternIndex, Geom) -> bool:  # icurvestyle_setpatternfragment.html
        """
        ## SetPatternFragment - Установить фрагмент по геометрии

        ### Синтаксис Automation

        ```
        BOOL SetPatternFragment( long PatternIndex, VARIANT Geom );
        ```

        ### Входные параметры

        +----------------------------------------------------------------------------------+
        | PatternIndex | - индекс образца,                                                 |
        +----------------------------------------------------------------------------------+
        | Geom         | - массив объектов SAFEARRAY VT_DISPATCH - VT_ARRAY | VT_DISPATCH. |
        +----------------------------------------------------------------------------------+

        ### Возвращаемое значение

        +-----------------------------------------+
        | TRUE | - в случае успешного завершения. |
        +-----------------------------------------+
        """
        ...

    def SetPatternFragmentDx(self, PatternIndex, arg1):
        ...

    def SetPatternFragmentDy(self, PatternIndex, arg1):
        ...

    def SetPatternFragmentFilletsPoligon(self, PatternIndex, PoligonIndex, arg2):
        ...

    def SetPatternFragmentPoligon(self, PatternIndex, PoligonIndex, arg2):
        ...

    def SetPatternInvisibleSegmentLenght(self, PatternIndex, arg1):
        ...

    def SetPatternVisibleSegmentLenght(self, PatternIndex, arg1):
        ...




class IHatchStyle(IStyle):  # ihatchstyle.html
    """
    ## Интерфейс IHatchStyle

    Интерфейс стиля штриховки.

    Интерфейс можно получить с помощью методов коллекции стилей штриховок, полученной с помощью:

    •метода IStylesManager::HatchsStyles,

    •коллекции стилей библиотеки стилей штриховок (*.lhs) IApplication::LibraryStyles.

    KOMPAS v19
    """

    @property
    def Angle(self) -> float:  # ihatchstyle_angle.html
        """
        ## Angle - Угол наклона

        Тип данных: double

        ### Синтаксис Automation

        ```
        Angle = Object.Angle       # Получить свойство (* )
        Object.Angle = Angle       # Установить свойство (* )
        Angle = Object.GetAngle()  # Получить свойство (**)
        Object.SetAngle( Angle )   # Установить свойство (**)
        ```
        """
        ...
    @Angle.setter
    def Angle(self, value: float): ...

    @property
    def CurvesStyles(self) -> IStyles:  # ihatchstyle_curvesstyles.html
        """
        ## CurvesStyles - Стили линий

        Тип данных: Указатель на интерфейс IStyles

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        CurvesStyles = Object.CurvesStyles       # Получить свойство (* )
        CurvesStyles = Object.GetCurvesStyles()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def HatchType(self) -> bool:  # ihatchstyle_hatchtype.html
        """
        ## HatchType - Тип штриховки. TRUE - Область FALSE - полоса

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        HatchType = Object.HatchType       # Получить свойство (* )
        Object.HatchType = HatchType       # Установить свойство (* )
        HatchType = Object.GetHatchType()  # Получить свойство (**)
        Object.SetHatchType(HatchType)     # Установить свойство (**)
        ```
        """
        ...
    @HatchType.setter
    def HatchType(self, value: bool): ...

    @property
    def KeepAngle(self) -> bool:  # ihatchstyle_keepangle.html
        """
        ## KeepAngle - Не изменять угол наклона

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        KeepAngle = Object.KeepAngle       # Получить свойство (* )
        Object.KeepAngle = KeepAngle       # Установить свойство (* )
        KeepAngle = Object.GetKeepAngle()  # Получить свойство (**)
        Object.SetKeepAngle(Keep Angle )   # Установить свойство (**)
        ```
        """
        ...
    @KeepAngle.setter
    def KeepAngle(self, value: bool): ...

    @property
    def KeepScale(self) -> bool:  # ihatchstyle_keepscale.html
        """
        ## KeepScale - Не изменять масштаб

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        KeepScale = Object.KeepScale       # Получить свойство (* )
        Object.KeepScale = KeepScale       # Установить свойство (* )
        KeepScale = Object.GetKeepScale()  # Получить свойство (**)
        Object.SetKeepScale(KeepScale)     # Установить свойство (**)
        ```
        """
        ...
    @KeepScale.setter
    def KeepScale(self, value: bool): ...

    @property
    def LinesCount(self) -> int:  # ihatchstyle_linescount.html
        """
        ## LinesCount - Количество линий

        Тип данных: long

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        LinesCount = Object.LinesCount       # Получить свойство (* )
        LinesCount = Object.GetLinesCount()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def Scale(self) -> float:  # ihatchstyle_scale.html
        """
        ## Scale - Масштаб

        Тип данных: double

        ### Синтаксис Automation

        ```
        Scale = Object.Scale       # Получить свойство (* )
        Object.Scale = Scale       # Установить свойство (* )
        Scale = Object.GetScale()  # Получить свойство (**)
        Object.SetScale(Scale )    # Установить свойство (**)
        ```
        """
        ...
    @Scale.setter
    def Scale(self, value: float): ...

    @property
    def Width(self) -> float:  # ihatchstyle_width.html
        """
        ## Width - Ширина полосы

        Тип данных: double

        ### Синтаксис Automation

        ```
        Width = Object.Width       # Получить свойство (* )
        Object.Width = Width       # Установить свойство (* )
        Width = Object.GetWidth()  # Получить свойство (**)
        Object.SetWidth(Width)     # Установить свойство (**)
        ```
        """
        ...
    @Width.setter
    def Width(self, value: float): ...

    def AddLine(self, Style, X, Y, Angle, DX, DY) -> bool:  # ihatchstyle_addline.html
        """
        ## AddLine - Добавить линию

        ### Синтаксис Automation

        ```
        BOOL AddLine( ICurveStyle * Style, double X, double Y, double Angle, double DX, double DY );
        ```

        ### Входные параметры

        +-----------------------------------------------------------------+
        | Style | - пользовательский стиль кривой ICurveStyle,            |
        +-----------------------------------------------------------------+
        | X     | - начальное положение по x относительно базовой точки,  |
        +-----------------------------------------------------------------+
        | Y     | - начальное положение по y относительно базовой точки,  |
        +-----------------------------------------------------------------+
        | Angle | - добавочный угол поворота относительно угла штриховки, |
        +-----------------------------------------------------------------+
        | DX    | - смещение по направлению линии,                        |
        +-----------------------------------------------------------------+
        | DY    | - смещение по нормали к линии.                          |
        +-----------------------------------------------------------------+

        ### Возвращаемое значение

        +-----------------------------------------+
        | TRUE | - в случае успешного завершения, |
        +-----------------------------------------+
        """
        ...

    def ClearLines(self) -> bool:  # ihatchstyle_clearlines.html
        """
        ## ClearLines - Удалить все параметры линий

        ### Синтаксис Automation

        ```
        BOOL ClearLines();
        ```

        ### Возвращаемое значение

        +-----------------------------------------+
        | TRUE | - в случае успешного завершения, |
        +-----------------------------------------+
        """
        ...

    def DeleteLine(self, Index) -> bool:  # ihatchstyle_deleteline.html
        """
        ## DeleteLine - Удалить параметры линии по индексу

        ### Синтаксис Automation

        ```
        BOOL DeleteLine( long Index );
        ```

        ### Входные параметры

        +-------------------------+
        | Index | - индекс линии. |
        +-------------------------+

        ### Возвращаемое значение

        +-----------------------------------------+
        | TRUE | - в случае успешного завершения, |
        +-----------------------------------------+
        """
        ...

    def LineAngle(self, Index) -> float:  # ihatchstyle_lineangle.html
        """
        ## LineAngle - Добавочный угол поворота относительно угла штриховки

        Тип данных: double

        ### Синтаксис Automation

        ```
        LineAngle = Object.LineAngle( Index )     # Получить свойство (* )
        Object.LineAngle( Index ) = LineAngle     # Установить свойство (* )
        LineAngle = Object.GetLineAngle( Index )  # Получить свойство (**)
        Object.SetLineAngle( Index, LineAngle )   # Установить свойство (**)
        ```

        ### Входные параметры

        +------------------------------+
        | long Index | - индекс линии. |
        +------------------------------+
        """
        ...

    def LineBeginX(self, Index) -> float:  # ihatchstyle_linebeginx.html
        """
        ## LineBeginX - Начальное положение относительно базовой точки штриховки по X

        Тип данных: double

        ### Синтаксис Automation

        ```
        LineBeginX = Object.LineBeginX( Index )     # Получить свойство (* )
        Object.LineBeginX( Index ) = LineBeginX     # Установить свойство (* )
        LineBeginX = Object.GetLineBeginX( Index )  # Получить свойство (**)
        Object.SetLineBeginX( Index, LineBeginX )   # Установить свойство (**)
        ```

        ### Входные параметры

        +------------------------------+
        | long Index | - индекс линии. |
        +------------------------------+
        """
        ...

    def LineBeginY(self, Index) -> float:  # ihatchstyle_linebeginy.html
        """
        ## LineBeginY - Начальное положение относительно базовой точки штриховки по Y

        Тип данных: double

        ### Синтаксис Automation

        ```
        LineBeginY = Object.LineBeginY( Index )     # Получить свойство (* )
        Object.LineBeginY( Index ) = LineBeginY     # Установить свойство (* )
        LineBeginY = Object.GetLineBeginY( Index )  # Получить свойство (**)
        Object.SetLineBeginY( Index, LineBeginY )   # Установить свойство (**)
        ```

        ### Входные параметры

        +------------------------------+
        | long Index | - индекс линии. |
        +------------------------------+
        """
        ...

    def LineDx(self, Index) -> float:  # ihatchstyle_linedx.html
        """
        ## LineDx - Смещение по направлению линии

        Тип данных: double

        ### Синтаксис Automation

        ```
        LineDx = Object.LineDx( Index )     # Получить свойство (* )
        Object.LineDx( Index ) = LineDx     # Установить свойство (* )
        LineDx = Object.GetLineDx( Index )  # Получить свойство (**)
        Object.SetLineDx( Index, LineDx )   # Установить свойство (**)
        ```

        ### Входные параметры

        +------------------------------+
        | long Index | - индекс линии. |
        +------------------------------+
        """
        ...

    def LineDy(self, Index) -> float:  # ihatchstyle_linedy.html
        """
        ## LineDy - Смещение по нормали к линии

        Тип данных: double

        ### Синтаксис Automation

        ```
        LineDy = Object.LineDy( Index )     # Получить свойство (* )
        Object.LineDy( Index ) = LineDy     # Установить свойство (* )
        LineDy = Object.GetLineDy( Index )  # Получить свойство (**)
        Object.SetLineDy( Index, LineDy )   # Установить свойство (**)
        ```

        ### Входные параметры

        +------------------------------+
        | long Index | - индекс линии. |
        +------------------------------+
        """
        ...

    def LineStyle(self, Index) -> ICurveStyle:  # ihatchstyle_linestyle.html
        """
        ## LineStyle - Стиль линии штриховки

        Тип данных: Указатель на интерфейс ICurveStyle.

        ### Синтаксис Automation

        ```
        LineStyle = Object.LineStyle( Index )     # Получить свойство (* )
        Object.LineStyle( Index ) = LineStyle     # Установить свойство (* )
        LineStyle = Object.GetLineStyle( Index )  # Получить свойство (**)
        Object.SetLineStyle( Index, LineStyle )   # Установить свойство (**)
        ```

        ### Входные параметры

        +------------------------------+
        | long Index | - индекс линии. |
        +------------------------------+
        """
        ...

    def SetLineAngle(self, Index, arg1):
        ...

    def SetLineBeginX(self, Index, arg1):
        ...

    def SetLineBeginY(self, Index, arg1):
        ...

    def SetLineDx(self, Index, arg1):
        ...

    def SetLineDy(self, Index, arg1):
        ...

    def SetLineStyle(self, Index, arg1):
        ...




class IAdditionalBlockSectionTuning(IKompasAPIObject):  # iadditionalblocksectiontuning.html
    """
    ## Интерфейс IAdditionalBlockSectionTuning

    Интерфейс настройки раздела блока дополнительных (вложенных) разделов.

    Данный интерфейс позволяет получить доступ к настройкам раздела блока дополнительных (вложенных) разделов спецификации. Чтобы использовать в блоке этот раздел, установите свойству Use значение TRUE.

    Если настройки взяты у стиля спецификации, то они являются умолчательными для данного стиля, и изменять их нельзя. В этом случае данный интерфейс отражает содержимое настроек стиля в библиотеке стилей на момент получения и в дальнейшем не отслеживает изменений в библиотеке стилей.

    Если настройки взяты у описания спецификации, то они отображают и отслеживают текущее состояние описания спецификации. Эти настройки можно изменять и они могут отличаться от умолчательных настроек стиля спецификации, по которому создано данное описание спецификации.

    После изменения настроек (для того чтобы эти изменения реально отобразились в документе) необходимо вызвать метод ISpecificationTuning::Update.

    Данный интерфейс можно получить у коллекции IAdditionalBlockSectionTunings.
    """

    @property
    def Number(self) -> int:  # iadditionalblocksectiontuning_number.html
        """
        ## Number - Номер раздела

        Тип данных: long

        1. Данное свойство определяет уникальный номер раздела блока дополнительных (вложенных) разделов в данном стиле спецификации.

        2. Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        Number = iObject.Number       # Получить свойство (* )
        Number = iObject.GetNumber()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def Use(self) -> bool:  # iadditionalblocksectiontuning_use.html
        """
        ## Use - Использовать этот раздел

        Тип данных: BOOL

        Значения свойства:

        +-------------------------------------------+
        | TRUE  | - использовать данный раздел,     |
        +-------------------------------------------+
        | FALSE | - раздел использоваться не будет. |
        +-------------------------------------------+

        Позволяет получить\\изменить доступность данного раздела при заполнении спецификации.

        ### Синтаксис Automation

        ```
        Use = iObject.Use       # Получить свойство (* )
        iObject.Use = Use       # Установить свойство (* )
        Use = iObject.GetUse()  # Получить свойство (**)
        iObject.SetUse (Use)    # Установить свойство (**)
        ```
        """
        ...
    @Use.setter
    def Use(self, value: bool): ...




class IAdditionalBlockStyle(IKompasAPIObject):  # iadditionalblockstyle.html
    """
    ## Интерфейс IAdditionalBlockStyle

    Интерфейс стиля блока дополнительных (вложенных) разделов спецификации.

    Вложенные разделы располагаются внутри раздела, после всех объектов этого раздела.

    Дополнительные разделы располагаются в конце спецификации, после всех ее разделов.

    Блок состоит из тех же разделов, что и сама спецификация.

    Данный интерфейс можно получить от интерфейса коллекции стилей дополнительных (вложенных) разделов спецификации IAdditionalBlockStyles, IAdditionalBlockStyles::Item.
    """

    @property
    def Name(self) -> str:  # iadditionalblockstyle_name.html
        """
        ## Name - Имя блока дополнительных (вложенных) разделов

        Тип данных: BSTR (строка)

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        Name = iObject.Name       # Получить свойство (* )
        Name = iObject.GetName()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def Number(self) -> int:  # iadditionalblockstyle_number.html
        """
        ## Number - Номер блока дополнительных (вложенных) разделов

        Тип данных: long

        1. Блоки разделов в спецификации располагаются в порядке возрастания или убывания их номеров Number, в зависимости от значения свойства ISpecificationStyle::SortSectionDown.

        2. Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        Number = iObject.Number       # Получить свойство (* )
        Number = iObject.GetNumber()  # Получить свойство (**)
        ```
        """
        ...




class IAdditionalBlockTuning(IKompasAPIObject):  # iadditionalblocktuning.html
    """
    ## Интерфейс IAdditionalBlockTuning

    Интерфейс настройки блока дополнительных (вложенных) разделов..

    Данный интерфейс позволяет получить доступ к настройкам блока дополнительных (вложенных) разделов спецификации. Если настройки взяты у стиля спецификации, то они являются умолчательными для данного стиля, и изменять их нельзя. В этом случае данный интерфейс отражает содержимое настроек стиля в библиотеке стилей на момент получения и в дальнейшем не отслеживает изменений в библиотеке стилей.

    Если настройки взяты у описания спецификации, то они отображают и отслеживают текущее состояние описания спецификации. Эти настройки можно изменять и они могут отличаться от умолчательных настроек стиля спецификации, по которому создано данное описание спецификации.

    После изменения настроек (для того чтобы эти изменения реально отобразились в документе) необходимо вызвать метод ISpecificationTuning::Update.

    Данный интерфейс можно получить у коллекции IAdditionalBlockTunings.
    """

    @property
    def DocumentName(self) -> str:  # iadditionalblocktuning_documentname.html
        """
        ## DocumentName - Имя файла документа, по которому устанавливается имя блока дополнительных (вложенных) разделов

        Тип данных: BSTR (строка)

        Позволяет получить\\изменить полное имя файла КОМПАС-документа для связи с заголовком блока. Обозначение из этого файла может включаться в заголовок блока. Чтобы это включение было возможно, имя блока (заданное при настройке стиля спецификации) должно содержать синтаксическую конструкцию вида #XXX# (Количество и тип символов между «решетками» не имеет значения). Вместо этой конструкции в заголовок блока в спецификации будет подставлено обозначение, полученное из документа, путь к которому указан в этом свойстве. Таким образом, указание документа имеет смысл лишь при настройке блоков, заголовки которых содержат конструкцию вида #XXX#. Обычно для связи с заголовком блока выбирается электромонтажный чертеж или таблица соединений.

        ### Синтаксис Automation

        ```
        DocumentName = iObject.DocumentName       # Получить свойство (* )
        iObject.DocumentName = DocumentName       # Установить свойство (* )
        DocumentName = iObject.GetDocumentName()  # Получить свойство (**)
        iObject.SetDocumentName (DocumentName)    # Установить свойство (**)
        ```
        """
        ...
    @DocumentName.setter
    def DocumentName(self, value: str): ...

    @property
    def FirstOnSheet(self) -> bool:  # iadditionalblocktuning_firstonsheet.html
        """
        ## FirstOnSheet - Блок размещать с новой страницы

        Тип данных: BOOL

        Значения свойства:

        +-------------------------------------------------+
        | TRUE  | - располагать блок на новой странице,   |
        +-------------------------------------------------+
        | FALSE | - располагать блок сразу за предыдущим. |
        +-------------------------------------------------+

        Позволяет включать и отключать размещение блока на новом листе спецификации.

        ### Синтаксис Automation

        ```
        FirstOnSheet = iObject.FirstOnSheet       # Получить свойство (* )
        iObject.FirstOnSheet = FirstOnSheet       # Установить свойство (* )
        FirstOnSheet = iObject.GetFirstOnSheet()  # Получить свойство (**)
        iObject.SetFirstOnSheet (FirstOnSheet)    # Установить свойство (**)
        ```
        """
        ...
    @FirstOnSheet.setter
    def FirstOnSheet(self, value: bool): ...

    @property
    def IndependentPosition(self) -> bool:  # iadditionalblocktuning_independentposition.html
        """
        ## IndependentPosition - Независимая нумерация позиций

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        IndependentPosition = Object.IndependentPosition       # Получить свойство (* )
        Object.IndependentPosition = IndependentPosition       # Установить свойство (* )
        IndependentPosition = Object.GetIndependentPosition()  # Получить свойство (**)
        Object.SetIndependentPosition( IndependentPosition )   # Установить свойство (**)
        ```
        """
        ...
    @IndependentPosition.setter
    def IndependentPosition(self, value: bool): ...

    @property
    def Mark(self) -> str:  # iadditionalblocktuning_mark.html
        """
        ## Mark - Марка. Текст префикса

        Тип данных: BSTR

        ### Синтаксис Automation

        ```
        Mark = Object.Mark       # Получить свойство (* )
        Object.Mark = Mark       # Установить свойство (* )
        Mark = Object.GetMark()  # Получить свойство (**)
        Object.SetMark( Mark )   # Установить свойство (**)
        ```
        """
        ...
    @Mark.setter
    def Mark(self, value: str): ...

    @property
    def MarkOn(self) -> bool:  # iadditionalblocktuning_markon.html
        """
        ## MarkOn - Марка. Включена простановка позиций c префиксом

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        MarkOn = Object.MarkOn       # Получить свойство (* )
        Object.MarkOn = MarkOn       # Установить свойство (* )
        MarkOn = Object.GetMarkOn()  # Получить свойство (**)
        Object.SetMarkOn( MarkOn )   # Установить свойство (**)
        ```
        """
        ...
    @MarkOn.setter
    def MarkOn(self, value: bool): ...

    @property
    def Number(self) -> int:  # iadditionalblocktuning_number.html
        """
        ## Number - Номер блока дополнительных (вложенных) разделов

        Тип данных: long

        1. Данное свойство определяет уникальный номер блока дополнительных (вложенных) разделов в данном стиле спецификации.

        2. Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        Number = iObject.Number       # Получить свойство (* )
        Number = iObject.GetNumber()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def Sections(self) -> IAdditionalBlockSectionTunings:  # iadditionalblocktuning_sections.html
        """
        ## Sections - Разделы блока дополнительных (вложенных) разделов

        Тип данных: указатель на интерфейс IAdditionalBlockSectionTunings коллекции настроек разделов блока дополнительных (вложенных) разделов

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        Sections = iObject.Sections       # Получить свойство (* )
        Sections = iObject.GetSections()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def Use(self) -> bool:  # iadditionalblocktuning_use.html
        """
        ## Use - Использовать этот блок

        Тип данных: BOOL

        Значения свойства:

        +-----------------------------------------+
        | TRUE  | - использовать данный блок,     |
        +-----------------------------------------+
        | FALSE | - блок использоваться не будет. |
        +-----------------------------------------+

        Позволяет получить\\изменить доступность данного блока при заполнении спецификации.

        ### Синтаксис Automation

        ```
        Use = iObject.Use       # Получить свойство (* )
        iObject.Use = Use       # Установить свойство (* )
        Use = iObject.GetUse()  # Получить свойство (**)
        iObject.SetUse (Use)    # Установить свойство (**)
        ```
        """
        ...
    @Use.setter
    def Use(self, value: bool): ...




class IAttachedDocument(IKompasAPIObject):  # iattacheddocument.html
    """
    ## Интерфейс IAttachedDocument

    Интерфейс параметров присоединенного документа к объекту спецификации.

    Объект спецификации можно связать с документом КОМПАС-3D. Эта связь является двусторонней и ассоциативной и позволяет передавать данные об объекте спецификации в подключенный документ, или наоборот, данные из документа в соответствующий ему объект спецификации.

    В результате подключения документа в соответствующие колонки объекта спецификации автоматически будут переданы наименование и обозначение из модели или основной надписи чертежа, а также обозначение формата, на котором выполнен подключенный чертеж.

    Информация из подключенного документа может передаваться не только в колонки бланка спецификации, но и в дополнительные колонки. Например, в дополнительную колонку объекта может быть автоматически передана масса детали из модели или из соответствующей графы основной надписи.

    Данный интерфейс можно получить у коллекции присоединенных документов к объекту спецификации.IAttachedDocuments с помощью свойства IAttachedDocuments::Item и метода IAttachedDocuments::Add.
    """

    @property
    def Comment(self):
        ...
    @Comment.setter
    def Comment(self, value): ...

    @property
    def Format(self):
        ...

    @property
    def FormatTransmit(self):
        ...
    @FormatTransmit.setter
    def FormatTransmit(self, value): ...

    @property
    def Marking(self):
        ...

    @property
    def Name(self):
        ...

    @property
    def Transmit(self):
        ...
    @Transmit.setter
    def Transmit(self, value): ...

    def Delete(self):
        ...




class IBilletsObjectsManager(IDispatch):  # ibilletsobjectsmanager.html
    """
    ## Интерфейс IBilletsObjectsManager

    Интерфейс менеджера объектов заготовок.

    Интерфейс является дополнительным для описания спецификации ISpecificationDescription.

    Версия KOMPAS v22
    """

    def AddBilletObject(self, ParentObject) -> ISpecificationBaseObject:  # ibilletsobjectsmanager_addbilletobject.html
        """
        ## AddBilletObject - Получить владельца для заготовки

        ### Синтаксис Automation

        ```
        LPDISPATCH AddBilletObject( ISpecificationBaseObject * ParentObject );
        ```

        ### Входные параметры

        +------------------------------------------------------------------------+
        | ParentObject | - базовый объект спецификации ISpecificationBaseObject. |
        +------------------------------------------------------------------------+

        ### Возвращаемое значение

        - объект спецификации - заготовка ISpecificationBaseObject.
        """
        ...

    def FindBilletObjects(self, ParentObject) -> list[IDispatch]:  # ibilletsobjectsmanager_findbilletobjects.html
        """
        ## FindBilletObjects - Получить список заготовок

        ### Синтаксис Automation

        ```
        VARIANT FindBilletObjects( ISpecificationBaseObject * ParentObject );
        ```

        ### Входные параметры

        +-----------------------------------------------------------------------+
        | ParentObject | - базовый объект спецификации ISpecificationBaseObject |
        +-----------------------------------------------------------------------+

        ### Возвращаемое значение

        - Объекты заготовки в виде массива SAFEARRAY'я DISPATCH - VT_ARRAY | VT_DISPATCH.
        """
        ...

    def FindParentForBilletObject(self, BilletObject) -> ISpecificationBaseObject:  # ibilletsobjectsmanager_findparentforbilletobject.html
        """
        ## FindParentForBilletObject - Получить владельца для заготовки

        ### Синтаксис Automation

        ```
        LPDISPATCH FindParentForBilletObject( ISpecificationBaseObject * BilletObject );
        ```

        ### Входные параметры

        +-------------------------------------------------------------+
        | BilletObject | - объект- заготовка ISpecificationBaseObject |
        +-------------------------------------------------------------+

        ### Возвращаемое значение

        - Базовый объект спецификации, для которого создана заготовка ISpecificationBaseObject.
        """
        ...




class ICommonNameObject(ISpecificationObject):  # icommonnameobject.html
    """
    ## Интерфейс ICommonNameObject

    Интерфейс Объект спецификации - Общее наименование.

    Интерфейс можно получить с помощью методов интерфейса ICommonNamesManager.

    Версия KOMPAS v22
    """

    @property
    def Caption(self) -> str:  # icommonnameobject_caption.html
        """
        ## Caption - Заголовок общего наименования

        Тип данных: BSTR

        ### Синтаксис Automation

        ```
        Caption = Object.Caption       # Получить свойство (* )
        Object.Caption = Caption       # Установить свойство (* )
        Caption = Object.GetCaption()  # Получить свойство (**)
        Object.SetCaption( Caption )   # Установить свойство (**)
        ```
        """
        ...
    @Caption.setter
    def Caption(self, value: str): ...

    @property
    def NameItemsCount(self) -> int:  # icommonnameobject_nameitemscount.html
        """
        ## NameItemsCount - Количество элементов в общем наименовании

        Тип данных: long

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        NameItemsCount = Object.NameItemsCount       # Получить свойство (* )
        NameItemsCount = Object.GetNameItemsCount()  # Получить свойство (**)
        ```
        """
        ...

    def ItemName(self, ItemIndex) -> str:  # icommonnameobject_itemname.html
        """
        ## ItemName - Заголовок элемента в общем наименовании

        Тип данных: BSTR

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        ItemName = Object.ItemName( ItemIndex )     # Получить свойство (* )
        ItemName = Object.GetItemName( ItemIndex )  # Получить свойство (**)
        ```

        ### Входные параметры

        +-------------------------------------+
        | long ItemIndex | - индекс элемента. |
        +-------------------------------------+
        """
        ...

    def ItemReadOnly(self, ItemIndex) -> str:  # icommonnameobject_itemreadonly.html
        """
        ## ItemReadOnly - Доступность изменения признака использования в общем наименовании

        Тип данных: BSTR

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        ItemReadOnly = Object.ItemReadOnly( ItemIndex )     # Получить свойство (* )
        ItemReadOnly = Object.GetItemReadOnly( ItemIndex )  # Получить свойство (**)
        ```
        """
        ...

    def ItemUses(self, ItemIndex) -> bool:  # icommonnameobject_itemuses.html
        """
        ## ItemUses - Элемент используется в общем наименовании

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        ItemUses = Object.ItemUses( ItemIndex )     # Получить свойство (* )
        Object.ItemUses( ItemIndex ) = ItemUses     # Установить свойство (* )
        ItemUses = Object.GetItemUses( ItemIndex )  # Получить свойство (**)
        Object.SetItemUses( ItemIndex, ItemUses )   # Установить свойство (**)
        ```

        ### Входные параметры

        +-------------------------------------+
        | long ItemIndex | - индекс элемента. |
        +-------------------------------------+
        """
        ...

    def ItemValue(self, ItemIndex) -> str:  # icommonnameobject_itemvalue.html
        """
        ## ItemValue - Значение элемента

        Тип данных: BSTR

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        ItemValue = Object.ItemValue( ItemIndex )     # Получить свойство (* )
        ItemValue = Object.GetItemValue( ItemIndex )  # Получить свойство (**)
        ```

        ### Входные параметры

        +-------------------------------------+
        | long ItemIndex | - индекс элемента. |
        +-------------------------------------+
        """
        ...

    def SetItemUses(self, ItemIndex, arg1):
        ...




class ICommonNamesManager(IDispatch):  # icommonnamesmanager.html
    """
    ## Интерфейс ICommonNamesManager

    Интерфейс менеджера объектов заготовок

    Интерфейс является дополнительным для описания спецификации ISpecificationDescription.

    Версия KOMPAS v22
    """

    def AddCommonName(self, ParentObject) -> ICommonNameObject:  # icommonnamesmanager_addcommonname.html
        """
        ## AddCommonName - Добавить объект общее наименование

        ### Синтаксис Automation

        ```
        LPDISPATCH AddCommonName( ISpecificationBaseObject * ParentObject );
        ```

        ### Входные параметры

        +------------------------------------------------------------------------+
        | ParentObject | - базовый объект спецификации ISpecificationBaseObject. |
        +------------------------------------------------------------------------+

        ### Возвращаемое значение

        - Объект спецификации - Общее наименование ICommonNameObject.
        """
        ...

    def FindCommonName(self, ParentObject) -> ICommonNameObject:  # icommonnamesmanager_findcommonname.html
        """
        ## FindCommonName - Найти объект общее наименование для заданного объекта

        ### Синтаксис Automation

        ```
        LPDISPATCH FindCommonName( ISpecificationBaseObject * ParentObject );
        ```

        ### Входные параметры

        +------------------------------------------------------------------------+
        | ParentObject | - базовый объект спецификации ISpecificationBaseObject. |
        +------------------------------------------------------------------------+

        ### Возвращаемое значение

        - Объект спецификации - Общее наименование ICommonNameObject.
        """
        ...

    def IsEnableCreateCommonName(self, BaseObject) -> int:  # icommonnamesmanager_isenablecreatecommonname.html
        """
        ## IsEnableCreateCommonName - Проверить возможность создания объекта общее наименование для заданного объекта

        ### Синтаксис Automation

        ```
        ksCreateCommonNameState IsEnableCreateCommonName( ISpecificationBaseObject * BaseObject );
        ```

        ### Входные параметры

        +----------------------------------------------------------------------+
        | BaseObject | - базовый объект спецификации ISpecificationBaseObject. |
        +----------------------------------------------------------------------+

        ### Возвращаемое значение

        - Признак доступности создания объекта общее наименование из перечисления ksCreateCommonNameState.
        """
        ...




class IGroupObjectsManager(IDispatch):  # igroupobjectsmanager.html
    """
    ## Интерфейс IGroupObjectsManager

    Интерфейс менеджера групповых объектов.

    Интерфейс является дополнительным для интерфейса описания спецификаци ISpecificationDescription.

    Версия КОМПАС v22
    """

    def FindGroupObject(self, BaseObject) -> ISpecificationBaseObject:  # igroupobjectsmanager_findgroupobject.html
        """
        ## FindGroupObject - Получить групповой объект спецификации по указателю на экземпляр

        ### Синтаксис Automation

        ```
        LPDISPATCH FindGroupObject( ISpecificationBaseObject * BaseObject );
        ```

        ### Входные параметры

        +----------------------------------------------------------------------+
        | BaseObject | - базовый объект спецификации ISpecificationBaseObject. |
        +----------------------------------------------------------------------+

        ### Возвращаемое значение

        - групповой объект спецификации ISpecificationBaseObject.
        """
        ...

    def FindGroupObjects(self, ParentGroupObject) -> list[IDispatch]:  # igroupobjectsmanager_findgroupobjects.html
        """
        ## FindGroupObjects - Получить список сгруппированных объектов

        ### Синтаксис Automation

        ```
        VARIANT FindGroupObjects( ISpecificationBaseObject * ParentGroupObject );
        ```

        ### Входные параметры

        +-------------------------------------------------------------------------------+
        | ParentGroupObject | - групповой объект спецификации ISpecificationBaseObject. |
        +-------------------------------------------------------------------------------+

        ### Возвращаемое значение

        - Базовые объекты в виде массива SAFEARRAY'я DISPATCH - VT_ARRAY | VT_DISPATCH.
        """
        ...

    def GetGroupObjectsGeometry(self, BaseObject) -> list[IDispatch]:  # igroupobjectsmanager_getgroupobjectsgeometry.html
        """
        ## GetGroupObjectsGeometry - Получить общий список подключенной геометрии к объектам в группе

        У базового объекта СП может быть установлена геометрия, которая ассоциируется с данным объектом.

        Для 2D документа – это массив IDrawingObject.

        Для 3D документа – это массив объектов:

        •IPart7 (может быть один компонент),

        •IBody7 (тела верхнего уровня сборки),

        •IPositionLeader (обозначения позиций верхнего уровня сборки).

        Получить можно всегда массив объектов. Если возвращается один объект, то тип VARIANT-а будет VT_DISPATCH, если возвращается несколько объектов, то тип VARIANT-а будет VT_ARRAY | VT_DISPATCH.

        Установить можно как массив, так и одиночный объект. При установке геометрии старая геометрия очищается.

        ### Синтаксис Automation

        ```
        VARIANT GetGroupObjectsGeometry( ISpecificationBaseObject * GroupObjec );
        ```

        ### Входные параметры

        +------------------------------------------------------------------------+
        | BaseObject | - групповой объект спецификации ISpecificationBaseObject. |
        +------------------------------------------------------------------------+

        ### Возвращаемое значение

        - Массив SafeArray типа VT_ARRAY | VT_DISPATCH.
        """
        ...




class ISpecificationBaseObject(ISpecificationObject):  # ispecificationbaseobject.html
    """
    ## Интерфейс ISpecificationBaseObject

    Интерфейс базового объекта спецификации.

    Интерфейс позволяет получить и изменить параметры для базовых объектов спецификации. Для базовых объектов предусмотрена возможность автоматического заполнения колонок, сортировки, подключения графических объектов из сборочного чертежа, подключения деталей из сборки и т.д.

    1. После изменения параметров объекта спецификации требуется вызвать метод ISpecificationObject::Update.

    2. Данный интерфейс можно получить у коллекции базовых объектов спецификации ISpecificationBaseObjects.
    """

    @property
    def AttributeNumber(self) -> float:  # ispecificationbaseobject_attributenumber.html
        """
        ## AttributeNumber - Номер типа атрибута

        Тип данных: double

        Позволяет получить используемый тип атрибута для колонки с типом значения ksValueTypeRecord ISpecificationColumn::ValueType, и установить новый. Если требуется просто удалить тип атрибута, то в качестве значения этого свойства надо присвоить 0. В этом случае значение колонки с типом ksValueTypeRecord будет аналогично типу ksValueTypeString. Изменение этого свойства вступит в силу после вызова метода ISpecificationObject::Update.

        ### Синтаксис Automation

        ```
        AttributeNumber = iObject.AttributeNumber       # Получить свойство (* )
        iObject.AttributeNumber = AttributeNumber       # Установить свойство (* )
        AttributeNumber = iObject.GetAttributeNumber()  # Получить свойство (**)
        iObject.SetAttributeNumber( AttributeNumber )   # Установить свойство (**)
        ```
        """
        ...
    @AttributeNumber.setter
    def AttributeNumber(self, value: float): ...

    @property
    def CommentObjects(self) -> ISpecificationCommentObjects:  # ispecificationbaseobject_commentobjects.html
        """
        ## CommentObjects - Вспомогательные объекты, прикрепленные к объекту

        Тип данных: Указатель на интерфейс ISpecificationCommentObjects коллекции вспомогательных объектов спецификации

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        CommentObjects = iObject.CommentObjects       # Получить свойство (* )
        CommentObjects = iObject.GetCommentObjects()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def Documents(self) -> typing.Any:  # ispecificationbaseobject_documents.html
        """
        ## Documents - Документы, связанные с объектом спецификации

        Тип данных: VARIANT

        1. Связанными документами являются документы, в которых находится геометрия, подключенная к данному объекту, поэтому список связанных с объектом спецификации документов можно получить только для объектов в документе спецификации, в остальных документах геометрию можно подключить только из этого же документа.

        2. Свойство возвращает переменную типа VARIANT на безопасный массив имен файлов, связанных с объектом спецификации SAFEARRAY (VT_ARRAY | VT_BSTR).

        3. Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        Documents = iObject.Documents       # Получить свойство (* )
        Documents = iObject.GetDocuments()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def Draw(self) -> bool:  # ispecificationbaseobject_draw.html
        """
        ## Draw - Показывать объект

        Тип данных: BOOL

        Значения свойства:

        +-------------------------------------------------------------------------------------------------------------------------------------------------+
        | TRUE  | - объект виден в таблице спецификации в любом режиме работы с ней,                                                                      |
        +-------------------------------------------------------------------------------------------------------------------------------------------------+
        | FALSE | - показывать объект в таблице спецификации не нужно; при этом объект не удаляется (он по-прежнему участвует в расчете номеров позиций). |
        +-------------------------------------------------------------------------------------------------------------------------------------------------+

        Свойство позволяет скрыть объект спецификации.

        ### Синтаксис Automation

        ```
        Draw = iObject.Draw       # Получить свойство (* )
        iObject.Draw = Draw       # Установить свойство (* )
        Draw = iObject.GetDraw()  # Получить свойство (**)
        iObject.SetDraw( Draw )   # Установить свойство (**)
        ```
        """
        ...
    @Draw.setter
    def Draw(self, value: bool): ...

    @property
    def DrawPosition(self) -> bool:  # ispecificationbaseobject_drawposition.html
        """
        ## DrawPosition - Показывать позицию

        Тип данных: BOOL

        Значения свойства:

        +-------------------------------------------------------------------+
        | TRUE  | - в колонке "Позиция" объекта отображается ее содержимое, |
        +-------------------------------------------------------------------+
        | FALSE | - колонка "Позиция" будет показываться пустой.            |
        +-------------------------------------------------------------------+

        Свойство позволяет скрыть номер позиции объекта в спецификации.

        ### Синтаксис Automation

        ```
        DrawPosition = iObject.DrawPosition       # Получить свойство (* )
        iObject.DrawPosition = DrawPosition       # Установить свойство (* )
        DrawPosition = iObject.GetDrawPosition()  # Получить свойство (**)
        iObject.SetDrawPosition( DrawPosition )   # Установить свойство (**)
        ```
        """
        ...
    @DrawPosition.setter
    def DrawPosition(self, value: bool): ...

    @property
    def EditSourceObject(self) -> bool:  # ispecificationbaseobject_editsourceobject.html
        """
        ## EditSourceObject - Передавать изменения в документ - владелец объекта

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        EditSourceObject = Object.EditSourceObject       # Получить свойство (* )
        Object.EditSourceObject = EditSourceObject       # Установить свойство (* )
        EditSourceObject = Object.GetEditSourceObject()  # Получить свойство (**)
        Object.SetEditSourceObject( EditSourceObject )   # Установить свойство (**)
        ```
        """
        ...
    @EditSourceObject.setter
    def EditSourceObject(self, value: bool): ...

    @property
    def FillNameByMaterial(self) -> bool:  # ispecificationbaseobject_fillnamebymaterial.html
        """
        ## FillNameByMaterial - Синхронизировать наименование с материалом

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        FillNameByMaterial = Object.FillNameByMaterial       # Получить свойство (* )
        Object.FillNameByMaterial = FillNameByMaterial       # Установить свойство (* )
        FillNameByMaterial = Object.GetFillNameByMaterial()  # Получить свойство(**)
        Object.SetFillNameByMaterial( FillNameByMaterial )   # Установить свойство (**)
        ```
        """
        ...
    @FillNameByMaterial.setter
    def FillNameByMaterial(self, value: bool): ...

    @property
    def Geometry(self) -> typing.Any:  # ispecificationbaseobject_geometry.html
        """
        ## Geometry - Геометрия объекта спецификации

        Тип данных: VARIANT

        Значения свойства:

        Массив SafeArray типа VT_ARRAY | VT_DISPATCH

        У базового объекта СП может быть установлена геометрия, которая ассоциируется с данным объектом.

        Для 2D документа это массив IDrawingObject.

        Для 3D документа это массив объектов:

        •IPart7(может быть один компонент),

        •IBody7 (тела верхнего уровня сборки),

        •IPositionLeader ( обозначения позиций верхнего уровня сборки).

        •Получить можно всегда массив объектов. Если возвращается один объект, то тип VARIANT-а будет VT_DISPATCH, если возвращается несколько объектов, то тип VARIANT-а будет VT_ARRAY | VT_DISPATCH.

        •Установить можно как массив, так и одиночный объект. При установке геометрии старая геометрия очищается.

        При установке геометрии используется текущее значение флага ISpecificationBaseObject::SyncronizeWithProperties.

        Версия: Компас v20

        ### Синтаксис Automation

        ```
        Geometry = iObject.Geometry       # Получить свойство (* )
        iObject.Geometry = Geometry       # Установить свойство (* )
        Geometry = iObject.GetGeometry()  # Получить свойство (**)
        iObject.SetGeometry(Geometry)     # Установить свойство (**)
        ```
        """
        ...
    @Geometry.setter
    def Geometry(self, value: typing.Any): ...

    @property
    def GroupObject(self) -> bool:  # groupobject.html
        """
        ## GroupObject - Группировать объект

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        GroupObject = Object.GroupObject       # Получить свойство (* )
        Object.GroupObject = GroupObject       # Установить свойство (* )
        GroupObject = Object.GetGroupObject()  # Получить свойство (**)
        Object.SetGroupObject( GroupObject )   # Установить свойство (**)
        ```
        """
        ...
    @GroupObject.setter
    def GroupObject(self, value: bool): ...

    @property
    def IsFirstObject(self) -> bool:  # ispecificationbaseobject_isfirstobject.html
        """
        ## IsFirstObject - TRUE - первый одинаковый объект, FALSE - последующие одинаковые объекты

        Тип данных: BOOL

        Свойство доступно только для чтения.

        Версия: Компас v20

        ### Синтаксис Automation

        ```
        IsFirstObject = Object.IsFirstObject       # Получить свойство (* )
        IsFirstObject = Object.GetIsFirstObject()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def IsGroupObject(self) -> bool:  # ispecificationbaseobject_isgroupobject.html
        """
        ## IsGroupObject - Признак группового объекта спецификации

        Тип данных: BOOL

        Свойство доступно только для чтения.

        Версия КОМПАС v22

        ### Синтаксис Automation

        ```
        IsGroupObject = Object.IsGroupObject       # Получить свойство (* )
        IsGroupObject = Object.GetIsGroupObject()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def Performance(self) -> bool:  # ispecificationbaseobject_performance.html
        """
        ## Performance - Объект является исполнением

        Тип данных: BOOL

        Значения свойства:

        +-------------------------------------------+
        | TRUE  | - Объект является исполнением,    |
        +-------------------------------------------+
        | FALSE | - Объект не является исполнением. |
        +-------------------------------------------+

        Объекты спецификации, являющиеся исполнением, отличаются от простого объекта наличием суффикса исполнения. Их обозначение должно заканчиваться суффиксом с номером исполнения. Например: -02, -03, -10.

        Отображение объектов, являющимися исполнениями зависит от настроек стиля спецификации.

        ### Синтаксис Automation

        ```
        Performance = iObject.Performance       # Получить свойство (* )
        iObject.Performance = Performance       # Установить свойство (* )
        Performance = iObject.GetPerformance()  # Получить свойство (**)
        iObject.SetPerformance( Performance )   # Установить свойство (**)
        ```
        """
        ...
    @Performance.setter
    def Performance(self, value: bool): ...

    @property
    def SyncronizeWithProperties(self) -> bool:  # ispecificationbaseobject_syncronizewithproperties.html
        """
        ## SyncronizeWithProperties - Брать данные из свойств и передавать данные в свойства объекта

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        SyncronizeWithProperties = Object.SyncronizeWithProperties       # Получить свойство (* )
        Object.SyncronizeWithProperties = SyncronizeWithProperties       # Установить свойство (* )
        SyncronizeWithProperties = Object.GetSyncronizeWithProperties()  # Получить свойство (**)
        Object.SetSyncronizeWithProperties( SyncronizeWithProperties )   # Установить свойство (**)
        ```
        """
        ...
    @SyncronizeWithProperties.setter
    def SyncronizeWithProperties(self, value: bool): ...

    @property
    def UniqueMetaObjectKey(self) -> bool:  # ispecificationbaseobject_uniquemetaobjectkey.html
        """
        ## UniqueMetaObjectKey - Уникальный идентификатор объекта

        Тип данных: BOOL

        Свойство доступно только для чтения

        ### Синтаксис Automation

        ```
        UniqueMetaObjectKey = Object.UniqueMetaObjectKey       # Получить свойство (* )
        UniqueMetaObjectKey = Object.GetUniqueMetaObjectKey()  # Получить свойство (**)
        ```
        """
        ...

    def ClearGeometry(self, ClearGeometry, ClearLeaders) -> bool:  # ispecificationbaseobject_cleargeometry.html
        """
        ## ClearGeometry - Очистить геометрию объекта спецификации

        ### Синтаксис Automation

        ```
        BOOL ClearGeometry( BOOL ClearGeometry, BOOL ClearLeaders );
        ```

        ### Входные параметры

        +-------------------------------------------+
        | ClearGeometry | - очистить геометрию,     |
        +-------------------------------------------+
        | ClearLeaders  | - очистить линии выноски. |
        +-------------------------------------------+

        ### Возвращаемое значение

        +----------------------------------------+
        | TRUE | - в случае удачного завершения. |
        +----------------------------------------+
        """
        ...

    def IncludeGeometry(self, PVal, FillText) -> bool:  # ispecificationbaseobject_includegeometry.html
        """
        ## IncludeGeometry - Добавить геометрию к объекту спецификации

        Если при подключении компонента присылается флаг fillTexts = true, в объекте спецификации взводится флаг ISpecificationBaseObject::SyncronizeWithProperties.

        ### Синтаксис Automation

        ```
        BOOL IncludeGeometry( VARIANT PVal, BOOL FillText );
        ```

        ### Входные параметры

        +---------------------------------------------------------------------------------------------------------------------+
        | PVal     | - Объект VT_DISPATCH или список объектов VT_ARRAY | VT_DISPATCH,                                         |
        +---------------------------------------------------------------------------------------------------------------------+
        | FillText | - признак автоматического заполнения колонок: наименование, обозначение, масса взяты из свойств объекта. |
        +---------------------------------------------------------------------------------------------------------------------+

        ### Возвращаемое значение

        +----------------------------------------+
        | TRUE | - в случае удачного завершения. |
        +----------------------------------------+
        """
        ...

    def SetMaterial(self, Material, Density) -> bool:  # ispecificationbaseobject_setmaterial.html
        """
        ## SetMaterial - Установить материал в объект спецификации и связанный с ним документ

        ### Синтаксис Automation

        ```
        BOOL SetMaterial( BSTR Material, double Density );
        ```

        ### Входные параметры

        +-------------------------+
        | Material | - материал,  |
        +-------------------------+
        | Density  | - плотность. |
        +-------------------------+

        ### Возвращаемое значение

        +-----------------------------------------+
        | TRUE  | - в случае удачного завершения, |
        +-----------------------------------------+
        | FALSE | - в случае неудачи.             |
        +-----------------------------------------+
        """
        ...

    def SetSection(self, Val) -> bool:  # ispecificationbaseobject_setsection.html
        """
        ## SetSection - Установить номер раздела

        Задаваемый номер раздела должен присутствовать в данной спецификации.

        Узнавать текущий номер раздела объекта надо у базового интерфейса ISpecificationObject.

        ### Синтаксис Automation

        ```
        BOOL SetSection( long Val );
        ```

        ### Входные параметры

        +------------------------+
        | Val | - номер раздела. |
        +------------------------+

        ### Возвращаемое значение

        +-----------------------------------------+
        | TRUE  | - в случае удачного завершения, |
        +-----------------------------------------+
        | FALSE | - в случае неудачи.             |
        +-----------------------------------------+
        """
        ...

    def SetSpcUsed(self, SpcUsed, arg1):
        ...

    def SpcUsed(self, SpcUsed) -> bool:  # ispecificationbaseobject_spcused.html
        """
        ## SpcUsed - Признаки использования в спецификации

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        SpcUsed = Object.SpcUsed(SpcUsedType)      # Получить свойство (* )
        Object.SpcUsed(SpcUsedType) = SpcUsed      # Установить свойство (* )
        SpcUsed = Object.GetSpcUsed(SpcUsedType)   # Получить свойство (**)
        Object.SetSpcUsed( SpcUsedType, SpcUsed )  # Установить свойство (**)
        ```

        ### Входные параметры

        +------------------------------------------------------------------------------------------+
        | SpcUsedType | - Признаки использования в спецификации из перечисления ksSpcUsedTypeEnum. |
        +------------------------------------------------------------------------------------------+
        """
        ...

    def SummaryCount(self, ColumnTypeNumber, BlockNumber) -> float:  # ispecificationbaseobject_summarycount.html
        """
        ## SummaryCount - Суммарное количество для исполнения

        Тип данных: double

        1. Свойство доступно только для чтения.

        2. Данное свойство возвращает суммарное значение в колонке "Количество" указанного номера колонки для одинаковых объектов указанного блока исполнений.

        ### Синтаксис Automation

        ```
        SummaryCount = iObject.SummaryCount( ColumnTypeNumber, BlockNumber )     # Получить свойство (* )
        SummaryCount = iObject.GetSummaryCount( ColumnTypeNumber, BlockNumber )  # Получить свойство (**)
        ```

        ### Входные параметры

        +--------------------------------------------------+
        | ColumnTypeNumber | - номер колонки данного типа, |
        +--------------------------------------------------+
        | BlockNumber      | - номер блока исполнений.     |
        +--------------------------------------------------+
        """
        ...

    def SummaryCountText(self, ColumnTypeNumber, BlockNumber) -> IText:  # ispecificationbaseobject_summarycounttext.html
        """
        ## SummaryCountText - Суммарное количество для исполнения

        Тип данных: Указатель на интерфейс IText

        •В разных разделах количество может иметь различный тип.

        •В разделе Детали, стандартные изделия, например, имеют целочисленное значение. При объединении нескольких объектов спецификации в этом случае количество суммируется.

        •В разделе Материал количество строковое, и текст возвращается с объекта, у которого задано не пустое значение.

        •В разделе Документация также формируется объединенное значение.

        В отличие от ISpecificationBaseObject::SummaryCount метод может вернуть не пустое значение для строковых колонок.

        ### Синтаксис Automation

        ```
        SummaryCountText = Object.TemplatePath( ColumnTypeNumber, BlockNumber )         # Получить свойство (* )
        SummaryCountText = Object.GetSummaryCountText( ColumnTypeNumber, BlockNumber )  # Получить свойство (**)
        ```

        ### Входные параметры

        +------------------------------------------------+
        | ColumnTypeNumber | - номер колонки количество, |
        +------------------------------------------------+
        | BlockNumber      | - номер блока исполнений.   |
        +------------------------------------------------+
        """
        ...




class ISpecificationColumn(IKompasAPIObject):  # ispecificationcolumn.html
    """
    ## Интерфейс ISpecificationColumn

    Интерфейс колонки объекта спецификации.

    Колонка спецификации представляет ячейку строки объекта спецификации в таблице спецификации. Колонка содержит информацию определенного типа, который определяется типом колонки ColumnType.

    Если в спецификации необходимо иметь несколько колонок с одинаковым типом, то они отличаются друг от друга номером колонки данного типа ColumnTypeNumber.

    Различаются основные и дополнительные колонки объектов спецификации. Основные колонки отображаются в таблице спецификации и выводятся на печать. Дополнительные колонки содержат сведения, дополняющие информацию, включаемую в стандартный бланк. В таблице спецификации такие колонки не видны и на печать не выводятся. Примером информации в дополнительных колонках могут служить масса и стоимость объекта. В дополнительные колонки вводят и любую другую информацию об объекте (код ОКП, материал, текстовый комментарий и т.д.). Их количество и состав определяются потребностями пользователя и хранятся в стиле спецификации.

    Данный интерфейс можно получить у коллекции колонок объекта спецификации ISpecificationColumns.
    """

    @property
    def AttributeNumber(self) -> float:  # ispecificationcolumn_attributenumber.html
        """
        ## AttributeNumber - Номер типа атрибута

        Тип данных: double

        1. Используется только для колонки с типом значения ksValueTypeRecord ISpecificationColumn::ValueType.

        Если требуется просто удалить тип атрибута, то в качестве значения этого свойства надо присвоить 0. В этом случае значение колонки с типом ksValueTypeRecord будет аналогично типу ksValueTypeString.

        2. Изменение этого свойства вступит в силу после вызова метода ISpecificationObject::Update.

        ### Синтаксис Automation

        ```
        AttributeNumber = iObject.AttributeNumber       # Получить свойство (* )
        iObject.AttributeNumber = AttributeNumber       # Установить свойство (* )
        AttributeNumber = iObject.GetAttributeNumber()  # Получить свойство (**)
        iObject.SetAttributeNumber( AttributeNumber )   # Установить свойство (**)
        ```
        """
        ...
    @AttributeNumber.setter
    def AttributeNumber(self, value: float): ...

    @property
    def BlockNumber(self) -> int:  # ispecificationcolumn_blocknumber.html
        """
        ## BlockNumber - Номер блока

        Тип данных: long

        1. Определяет какому блоку исполнений соответствует данная колонка. Используется для базовых объектов групповых спецификаций.

        2. Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        BlockNumber = iObject.BlockNumber       # Получить свойство (* )
        BlockNumber = iObject.GetBlockNumber()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def ColumnItems(self) -> ISpecificationColumnItems:  # ispecificationcolumn_columnitems.html
        """
        ## ColumnItems - Элементы колонки объекта спецификации

        Тип данных: Указатель на интерфейс ISpecificationColumnItems коллекции элементов колонки объекта спецификации

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        ColumnItems = iObject.ColumnItems       # Получить свойство (* )
        ColumnItems = iObject.GetColumnItems()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def ColumnName(self) -> str:  # ispecificationcolumn_columnname.html
        """
        ## ColumnName - Наименование колонки

        Тип данных: BSTR

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        ColumnName = Object.ColumnName       # Получить свойство (* )
        ColumnName = Object.GetColumnName()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def ColumnType(self) -> int:  # ispecificationcolumn_columntype.html
        """
        ## ColumnType - Тип колонки

        Тип данных: из перечисления ksSpecificationColumnTypeEnum

        1. Тип колонки определяет, какая именно информация будет храниться в этой колонке (независимо от ее названия). Тип колонки используется при автоматическом выполнении различных операций. Например:

        •если тип колонки - ПОЗИЦИЯ (ksSColumnPosition), то именно в эту колонку будут заноситься номера позиций при их автоматическом перерасчете;

        •если тип колонки - ЗОНА (ksSColumnZone), то именно в эту колонку будут попадать номера зон;

        •если тип колонки - ФОРМАТ (ksSColumnFormat), то в нее будут переноситься обозначения форматов из штампов чертежей деталей.

        При перерождении спецификации в спецификацию другого стиля (например, простой спецификации в групповую) колонки превращаются в колонки такого же типа (если он есть в стиле той спецификации, в которую перерождается исходная).

        2. Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        ColumnType = iObject.ColumnType       # Получить свойство (* )
        ColumnType = iObject.GetColumnType()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def ColumnTypeNumber(self) -> int:  # ispecificationcolumn_columntypenumber.html
        """
        ## ColumnTypeNumber - Номер колонки данного типа

        Тип данных: long

        1. Иногда однородная (но не одинаковая) информация должна храниться в нескольких колонках и по этой причине они имеют одинаковый тип. Например, несколько колонок групповой спецификации содержат информацию о количестве, но числа в них разные, т.к. они относятся к разным исполнениям. Чтобы отличать типы таких колонок, вместе с типом задается еще и номер, отличающий колонку этого типа от других колонок такого же типа. Если разные колонки имеют одинаковый тип, то номера колонок этого типа должны быть разными.

        Обратите внимание, что во вкладке Колонки диалога настройки стиля спецификации в скобках рядом с типом колонки показан этот номер (например, КОЛИЧЕСТВО[2]). Если несколько колонок имеют одинаковый тип, то при автоматическом выполнении операций будет использоваться та из них, тип которой имеет номер 1. Например, в спецификации есть колонки с типом ЗОНА[1] и ЗОНА[2]; при автоматическом расчете номера зон будут заноситься в колонку с типом ЗОНА[1].

        2. Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        ColumnTypeNumber = iObject.ColumnTypeNumber       # Получить свойство (* )
        ColumnTypeNumber = iObject.GetColumnTypeNumber()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def CountUniteCells(self) -> int:  # ispecificationcolumn_countunitecells.html
        """
        ## CountUniteCells - Количество объединяемых с текущей ячеек

        Тип данных: long

        ### Синтаксис Automation

        ```
        CountUniteCells = Object.CountUniteCells       # Получить свойство (* )
        Object.CountUniteCells = CountUniteCells       # Установить свойство (* )
        CountUniteCells = Object.GetCountUniteCells()  # Получить свойство (**)
        Object.SetCountUniteCells( CountUniteCells )   # Установить свойство (**)
        ```
        """
        ...
    @CountUniteCells.setter
    def CountUniteCells(self, value: int): ...

    @property
    def Number(self) -> int:  # ispecificationcolumn_number.html
        """
        ## Number - Номер колонки, начиная с единицы

        Тип данных: long

        Свойство доступно только для чтения

        ### Синтаксис Automation

        ```
        Number = Object.Number       # Получить свойство (* )
        Number = Object.GetNumber()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def Text(self) -> IText:  # ispecificationcolumn_text.html
        """
        ## Text - Текст колонки объекта спецификации

        Тип данных: Указатель на интерфейс IText

        Свойство позволяет получить и изменить текст колонки объекта спецификации.

        Свойство доступно только для чтения

        ### Синтаксис Automation

        ```
        Text = Object.Text       # Получить свойство (* )
        Text = Object.GetText()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def ValueType(self) -> int:  # ispecificationcolumn_valuetype.html
        """
        ## ValueType - Тип значения колонки

        Тип данных: из перечисления ksValueTypeEnum

        Значение свойства:

        +-------------------------------------------------------------------------------------------------------------+
        | ksValueTypeInteger | - Колонка содержит целое со знаком и состоит из одного элемента,                       |
        +-------------------------------------------------------------------------------------------------------------+
        | ksValueTypeFloat   | - Колонка содержит вещественное число и состоит из одного элемента,                    |
        +-------------------------------------------------------------------------------------------------------------+
        | ksValueTypeString  | - Колонка содержит строку длиной 255 символов и состоит из одного элемента,            |
        +-------------------------------------------------------------------------------------------------------------+
        | ksValueTypeRecord  | - Колонка содержит запись, количество элементов в колонке определяется типом атрибута. |
        +-------------------------------------------------------------------------------------------------------------+

        1. Тип ksValueTypeRecord доступен только для базовых объектов спецификации, и может быть только одна колонка данного типа. Тип атрибута для такой колонки определяется при создании базового объекта (см. AttributeNumber, ISpecificationBaseObject::AttributeNumber и ISpecificationBaseObjects::Add). Если тип атрибута 0 или объект не является базовым объектом спецификации, то данная колонка используется аналогично колонкам с типом значения ksValueTypeString.

        2. Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        ValueType = iObject.ValueType       # Получить свойство (* )
        ValueType = iObject.GetValueType()  # Получить свойство (**)
        ```
        """
        ...




class ISpecificationColumnItem(IKompasAPIObject):  # ispecificationcolumnitem.html
    """
    ## Интерфейс ISpecificationColumnItem

    Интерфейс элемента колонки объекта спецификации.

    Элемент колонки позволяет работать с данными колонки определенного типа. Этот тип соответствует типу колонки объекта спецификации или типу колонки атрибута для колонок типа Запись (ksValueTypeRecord). Если у колонки типа Запись тип атрибута не задан, то она состоит из одного элемента с типом Строка (ksValueTypeString).

    Спецификация на текущем этапе позволяет иметь тип Запись только в одной колонке.

    Данный интерфейс можно получить у коллекции элементов колонки объекта спецификации ISpecificationColumnItems.
    """

    @property
    def Key(self) -> int:  # ispecificationcolumnitem_key.html
        """
        ## Key - Ключ

        Тип данных: short

        Свойство задает приоритет для сортировки. Если ключ равен 0, сортировки нет. Используется для колонки типа Запись, если задан тип атрибута и позволяет определить приоритет каждого из элементов колонки для сортировки. Обратите внимание, что во вкладке Колонки диалога настройки стиля спецификации в скобках рядом с типом колонки показан этот номер (например, КОЛИЧЕСТВО[2]). Если несколько колонок имеют одинаковый тип, то при автоматическом выполнении операций будет использоваться та из них, тип которой имеет номер 1. Например, в спецификации есть колонки с типом ЗОНА[1] и ЗОНА[2]; при автоматическом расчете номера зон будут заноситься в колонку с типом ЗОНА[1].

        ### Синтаксис Automation

        ```
        Key = iObject.Key       # Получить свойство (* )
        iObject.Key = Key       # Установить свойство (* )
        Key = iObject.GetKey()  # Получить свойство (**)
        iObject.SetKey( Key )   # Установить свойство (**)
        ```
        """
        ...
    @Key.setter
    def Key(self, value: int): ...

    @property
    def Value(self) -> typing.Any:  # ispecificationcolumnitem_value.html
        """
        ## Value - Значение элемента

        Тип данных: VARIANT

        Позволяет получить и установить значение элемента колонки объекта спецификации.

        В зависимости от типа значения ValueType возвращаемое значение имеет тип:

        •ksValueTypeInteger - VT_I4 (long),

        •ksValueTypeFloat - VT_R8 (double),

        •ksValueTypeString - VT_BSTR (строка).

        ### Синтаксис Automation

        ```
        Value = iObject.Value       # Получить свойство (* )
        iObject.Value = Value       # Установить свойство (* )
        Value = iObject.GetValue()  # Получить свойство (**)
        iObject.SetValue( Value )   # Установить свойство (**)
        ```
        """
        ...
    @Value.setter
    def Value(self, value: typing.Any): ...

    @property
    def ValueType(self) -> int:  # ispecificationcolumnitem_valuetype.html
        """
        ## ValueType - Тип значения элемента колонки

        Тип данных: из перечисления ksValueTypeEnum

        Значение свойства:

        +---------------------------------------------------------------------------------------------------+
        | ksValueTypeInteger | - Элемент колонки содержит целое со знаком и состоит из одного элемента,     |
        +---------------------------------------------------------------------------------------------------+
        | ksValueTypeFloat   | - Элемент колонки – вещественное число и состоит из одного элемента,         |
        +---------------------------------------------------------------------------------------------------+
        | ksValueTypeString  | - Элемент колонки – строка длиной 255 символов и состоит из одного элемента, |
        +---------------------------------------------------------------------------------------------------+
        | ksValueTypeRecord  | - не используется.                                                           |
        +---------------------------------------------------------------------------------------------------+

        1. См также ISpecificationColumnItem::Value.

        2. Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        ValueType = iObject.ValueType       # Получить свойство (* )
        ValueType = iObject.GetValueType()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def Visible(self) -> bool:  # ispecificationcolumnitem_visible.html
        """
        ## Visible - Видимость элемента

        Тип данных: BOOL

        Значение свойства:

        +----------------------------------------------------------------------------+
        | TRUE  | - содержимое элемента выводится в колонке объекта спецификации,    |
        +----------------------------------------------------------------------------+
        | FALSE | - содержимое элемента не выводится в колонке объекта спецификации. |
        +----------------------------------------------------------------------------+

        ### Синтаксис Automation

        ```
        Visible = iObject.Visible       # Получить свойство (* )
        iObject.Visible = Visible       # Установить свойство (* )
        Visible = iObject.GetVisible()  # Получить свойство (**)
        iObject.SetVisible( Visible )   # Установить свойство (**)
        ```
        """
        ...
    @Visible.setter
    def Visible(self, value: bool): ...




class ISpecificationColumnStyle(IKompasAPIObject):  # ispecificationcolumnstyle.html
    """
    ## Интерфейс ISpecificationColumnStyle

    Интерфейс стиля колонки спецификации.

    Данный интерфейс можно получить от интерфейса стиля колонки спецификации ISpecificationColumnStyles, используя свойство ISpecificationColumnStyles::Item.
    """

    @property
    def AttributeKey1(self) -> int:  # ispecificationcolumnstyle_attributekey1.html
        """
        ## AttributeKey1 - Первый ключ атрибута

        Тип данных: long

        1. Используется только для колонок со значением типа Запись (см. ValueType).

        2. Для данной колонки можно будет использовать только те типы атрибутов, первый ключ которых совпадает с указанным.

        3. Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        AttributeKey1 = iObject.AttributeKey1       # Получить свойство (* )
        AttributeKey1 = iObject.GetAttributeKey1()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def AttributeKey2(self) -> int:  # ispecificationcolumnstyle_attributekey2.html
        """
        ## AttributeKey2 - Второй ключ атрибута

        Тип данных: long

        1. Используется только для колонок со значением типа Запись (см. ValueType).

        2. Для данной колонки можно будет использовать только те типы атрибутов, второй ключ которых совпадает с указанным.

        3. Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        AttributeKey2 = iObject.AttributeKey2       # Получить свойство (* )
        AttributeKey2 = iObject.GetAttributeKey2()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def AttributeKey3(self) -> int:  # ispecificationcolumnstyle_attributekey3.html
        """
        ## AttributeKey3 - Третий ключ атрибута

        Тип данных: long

        1. Используется только для колонок со значением типа Запись (см. ValueType).

        2. Для данной колонки можно будет использовать только те типы атрибутов, третий ключ которых совпадает с указанным.

        3. Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        AttributeKey3 = iObject.AttributeKey3       # Получить свойство (* )
        AttributeKey3 = iObject.GetAttributeKey3()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def AttributeKey4(self) -> int:  # ispecificationcolumnstyle_attributekey4.html
        """
        ## AttributeKey4 - Четвертый ключ атрибута

        Тип данных: long

        1. Используется только для колонок со значением типа Вещественный или Целый (см. ValueType).

        2. Определяет минимальное значение числа в колонке.

        3. Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        AttributeKey4 = iObject.AttributeKey4       # Получить свойство (* )
        AttributeKey4 = iObject.GetAttributeKey4()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def AttributeLibraryName(self) -> str:  # ispecificationcolumnstyle_attributelibraryname.html
        """
        ## AttributeLibraryName - Имя файла библиотеки типов атрибутов

        Тип данных: BSTR (строка)

        1. Используется только для колонок со значением типа Запись (см. ValueType).

        2. Структура записи в колонке определяется типом атрибута из библиотеки типов атрибутов.

        3. Для данной колонки можно будет использовать типы атрибутов только из указанной библиотеки

        4. Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        AttributeLibraryName = iObject.AttributeLibraryName       # Получить свойство (* )
        AttributeLibraryName = iObject.GetAttributeLibraryName()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def CalculateSum(self) -> bool:  # ispecificationcolumnstyle_calculatesum.html
        """
        ## CalculateSum - Рассчитывать сумму значений для колонки

        Тип данных: BOOL

        Значения свойства:

        +-------------------------------------------------------------------------------------------+
        | TRUE  | - складывать значения в таблице при вызове команды "Сложить значения в колонках", |
        +-------------------------------------------------------------------------------------------+
        | FALSE | - складывать значения в таблице запрещено.                                        |
        +-------------------------------------------------------------------------------------------+

        1. Используется только для колонок со значением типа Вещественный или Целый (см. ValueType).

        2. Используется только для колонок, полученных у стиля спецификации ISpecificationStyle.

        3. Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        CalculateSum = iObject.CalculateSum       # Получить свойство (* )
        CalculateSum = iObject.GetCalculateSum()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def ColumnType(self) -> int:  # ispecificationcolumnstyle_columntype.html
        """
        ## ColumnType - Тип колонки

        Тип данных: из перечисления ksSpecificationColumnTypeEnum

        1. Используется только для колонок, полученных у стиля спецификации ISpecificationStyle.

        2. Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        ColumnType = iObject.ColumnType       # Получить свойство (* )
        ColumnType = iObject.GetColumnType()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def Edit(self) -> bool:  # ispecificationcolumnstyle_edit.html
        """
        ## Edit - Редактируемая колонка в данном разделе

        Тип данных: BOOL

        Значения свойства:

        +-----------------------------------------------------------------+
        | TRUE  | - в колонку можно вводить данные,                       |
        +-----------------------------------------------------------------+
        | FALSE | - заполнение колонки в настраиваемом разделе запрещено. |
        +-----------------------------------------------------------------+

        1. Используется только для колонок, полученных у стиля раздела спецификации ISpecificationSectionStyle..

        2. Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        Edit = iObject.Edit       # Получить свойство (* )
        Edit = iObject.GetEdit()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def MaxValue(self) -> int:  # ispecificationcolumnstyle_maxvalue.html
        """
        ## MaxValue - Максимальное значение

        Тип данных: long

        1. Используется только для колонок со значением типа Вещественный или Целый (см. ValueType).

        2. Определяет минимальное значение числа в колонке.

        3. Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        MaxValue = iObject.MaxValue       # Получить свойство (* )
        MaxValue = iObject.GetMaxValue()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def MinValue(self) -> int:  # ispecificationcolumnstyle_minvalue.html
        """
        ## MinValue - Минимальное значение

        Тип данных: long

        1. Используется только для колонок со значением типа Вещественный или Целый (см. ValueType).

        2. Определяет минимальное значение числа в колонке.

        3. Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        MinValue = iObject.MinValue       # Получить свойство (* )
        MinValue = iObject.GetMinValue()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def MultiplyToCount(self) -> bool:  # ispecificationcolumnstyle_multiplytocount.html
        """
        ## MultiplyToCount - При расчете суммы домножать на количество

        Тип данных: BOOL

        Значения свойства:

        +----------------------------------------------------------------------------------------------------+
        | TRUE  | - перед сложением значение в колонке должно быть умножено на число в колонке "Количество", |
        +----------------------------------------------------------------------------------------------------+
        | FALSE | - складывать без изменений.                                                                |
        +----------------------------------------------------------------------------------------------------+

        1. Используется только для колонок со значением типа Вещественный или Целый (см. ValueType), если ISpecificationColumnStyle::CalculateSum равно TRUE.

        2. Используется только для колонок, полученных у стиля спецификации ISpecificationStyle.

        3. Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        MultiplyToCount = iObject.MultiplyToCount       # Получить свойство (* )
        MultiplyToCount = iObject.GetMultiplyToCount()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def Name(self) -> str:  # ispecificationcolumnstyle_name.html
        """
        ## Name - Имя колонки

        Тип данных: BSTR (строка)

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        Name = iObject.Name       # Получить свойство (* )
        Name = iObject.GetName()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def Number(self) -> int:  # ispecificationcolumnstyle_number.html
        """
        ## Number - Номер колонки данного типа

        Тип данных: long

        1. Данное свойство определяет уникальный номер колонки для колонок с таким же типом. Иногда однородная (но не одинаковая) информация должна храниться в нескольких колонках, и по этой причине они имеют одинаковый тип ColumnType Например, несколько колонок групповой спецификации содержат информацию о количестве, но числа в них разные, т.к. они относятся к разным исполнениям. Чтобы отличать такие колонки, вместе с типом задается еще и номер, отличающий колонку этого типа от других колонок такого же типа. Если разные колонки имеют одинаковый тип, то номера колонок этого типа должны быть разными.

        2. Используется только для колонок, полученных у стиля спецификации ISpecificationStyle.

        3. Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        Number = iObject.Number       # Получить свойство (* )
        Number = iObject.GetNumber()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def StampLinkID(self) -> int:  # ispecificationcolumnstyle_stamplinkid.html
        """
        ## StampLinkID - Номер ячейки штампа для связи

        Тип данных: long

        Значения свойства:

        Номер ячейки штампа.

        1. Данное свойство определяет ячейку штампа чертежа детали, данные из которой могут автоматически передаваться в данную колонку спецификации. Например, в колонку Формат можно передавать текст из ячейки Формат штампа чертежа детали.

        2. Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        StampLinkID = iObject.StampLinkID       # Получить свойство (* )
        StampLinkID = iObject.GetStampLinkID()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def TextDown(self) -> bool:  # ispecificationcolumnstyle_textdown.html
        """
        ## TextDown - Текст расположен в нижней части колонки

        Тип данных: BOOL

        Значения свойства:

        +-------------------------------------------------------------------------------------------------------------------------+
        | TRUE  | - в колонке однострочные тексты будут располагаться в той строке, в которой кончается текст предыдущей колонки, |
        +-------------------------------------------------------------------------------------------------------------------------+
        | FALSE | - тексты будут располагаться в верхней части колонки.                                                           |
        +-------------------------------------------------------------------------------------------------------------------------+

        1. Чаще всего эту опцию включают при настройке колонки Количество, когда требуется, чтобы количество было записано в той строке, где заканчивается наименование.

        2. Используется только для основных колонок спецификации.

        3. Используется только для колонок, полученных у стиля спецификации ISpecificationStyle.

        4. Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        TextDown = iObject.TextDown       # Получить свойство (* )
        TextDown = iObject.GetTextDown()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def UseForSectionTitle(self) -> bool:  # ispecificationcolumnstyle_useforsectiontitle.html
        """
        ## UseForSectionTitle - Использовать колонку для вывода имени раздела

        Тип данных: BOOL

        Значения свойства:

        +--------------------------------------------------------------------------+
        | TRUE  | - в колонке будут располагаться заголовки разделов спецификации, |
        +--------------------------------------------------------------------------+
        | FALSE | - колонка будет пустой для объектов спецификации "Имя раздела".  |
        +--------------------------------------------------------------------------+

        1. В стандартных спецификациях заголовки разделов обычно располагаются в колонке Наименование. Значение этого свойства TRUE - может быть только для одной колонки спецификации. Если у всех колонок значение этого свойства FALSE, заголовки разделов показываться не будут (даже при включении в настройке разделов опции Показывать заголовки разделов).

        2. Используется только для основных колонок спецификации.

        3. Используется только для колонок, полученных у стиля спецификации ISpecificationStyle.

        4. Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        UseForSectionTitle = iObject.UseForSectionTitle       # Получить свойство (* )
        UseForSectionTitle = iObject.GetUseForSectionTitle()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def UseIn3D(self) -> bool:  # ispecificationcolumnstyle_usein3d.html
        """
        ## UseIn3D - Используется в модели

        Тип данных: BOOL

        Значения свойства:

        +----------------------------------------------------------------------------------------------------------+
        | TRUE  | - в колонку можно было вводить данные при формировании объектов спецификации в документе-модели, |
        +----------------------------------------------------------------------------------------------------------+
        | FALSE | - заполнение колонки в объектах спецификации модели невозможно.                                  |
        +----------------------------------------------------------------------------------------------------------+

        1. Свойство позволяет настроить стиль спецификации так, чтобы в документах-моделях формировались частично заполненные объекты спецификации. Это позволяет избежать конфликтов взаимодействия документа-модели со спецификацией и чертежом.

        2. Используется только для колонок, полученных у стиля спецификации ISpecificationStyle.

        3. Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        UseIn3D = iObject.UseIn3D       # Получить свойство (* )
        UseIn3D = iObject.GetUseIn3D()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def ValueType(self) -> int:  # ispecificationcolumnstyle_valuetype.html
        """
        ## ValueType - Тип значения колонки

        Тип данных: из перечисления ksValueTypeEnum

        1. Тип "Запись" доступен только для основных колонок.

        2. Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        ValueType = iObject.ValueType       # Получить свойство (* )
        ValueType = iObject.GetValueType()  # Получить свойство (**)
        ```
        """
        ...




class ISpecificationCommentObject(ISpecificationObject):  # ispecificationcommentobject.html
    """
    ## Интерфейс ISpecificationCommentObject

    Интерфейс вспомогательного объекта спецификации.

    Интерфейс позволяет получить и изменить параметры вспомогательного объекта спецификации. В отличие от базового для вспомогательного объекта не предусмотрены сервисные функции, выполнение которых обеспечивает спецификация. Вспомогательные объекты не сортируются автоматически и т.д. Вспомогательные объекты рекомендуется использовать для выполнения таких приемов оформления спецификации, которые не могут быть обеспечены вводом базовых объектов. Например, при помощи вспомогательного объекта спецификации можно ввести произвольный текст (комментарий) в таблицу спецификации или создать пустую строку в середине раздела.

    1. После изменения параметров объекта спецификации требуется вызвать метод ISpecificationObject::Update.

    2. Данный интерфейс можно получить у коллекции базовых объектов спецификации ISpecificationCommentObjects.
    """

    @property
    def BaseObject(self) -> ISpecificationBaseObject:  # ispecificationcommentobject_baseobject.html
        """
        ## BaseObject - Базовый объект спецификации, к которому прикреплен данный объект

        Тип данных: указатель на интерфейс базового объекта спецификации ISpecificationBaseObject

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        BaseObject = iObject.BaseObject       # Получить свойство (* )
        BaseObject = iObject.GetBaseObject()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def BlockNumber(self) -> int:  # ispecificationcommentobject_blocknumber.html
        """
        ## BlockNumber - Номер блока

        Тип данных: long

        Установить номер блока можно только для нового вспомогательного объекта спецификации.

        ### Синтаксис Automation

        ```
        BlockNumber = iObject.BlockNumber       # Получить свойство (* )
        iObject.BlockNumber = BlockNumber       # Установить свойство (* )
        BlockNumber = iObject.GetBlockNumber()  # Получить свойство (**)
        iObject.SetBlockNumber( BlockNumber )   # Установить свойство (**)
        ```
        """
        ...
    @BlockNumber.setter
    def BlockNumber(self, value: int): ...

    @property
    def EditSourceObject(self) -> bool:  # ispecificationcommentobject_editsourceobject.html
        """
        ## EditSourceObject - Передавать изменения в документ - владелец объекта

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        EditSourceObject = Object.EditSourceObject       # Получить свойство (* )
        Object.EditSourceObject = EditSourceObject       # Установить свойство (* )
        EditSourceObject = Object.GetEditSourceObject()  # Получить свойство (**)
        Object.SetEditSourceObject( EditSourceObject )   # Установить свойство (**)
        ```
        """
        ...
    @EditSourceObject.setter
    def EditSourceObject(self, value: bool): ...

    def SetSection(self, Val) -> bool:  # ispecificationcommentobject_setsection.html
        """
        ## SetSection - Установить номер раздела

        Задаваемый номер раздела должен присутствовать в данной спецификации.

        Узнавать текущий номер раздела объекта надо у базового интерфейса ISpecificationObject.

        ### Синтаксис Automation

        ```
        BOOL SetSection( long Val );
        ```

        ### Входные параметры

        +------------------------+
        | Val | - номер раздела. |
        +------------------------+

        ### Возвращаемое значение

        +-----------------------------------------+
        | TRUE  | - в случае удачного завершения, |
        +-----------------------------------------+
        | FALSE | - в случае неудачи.             |
        +-----------------------------------------+
        """
        ...




class ISpecificationDescription(IKompasAPIObject):  # ispecificationdescription.html
    """
    ## Интерфейс ISpecificationDescription

    Интерфейс описания спецификации у документа.

    Описание спецификации включает в себя имя файла спецификации, подключенной к графическому документу и стиль этой спецификации.

    Для фрагмента и детали описание спецификации содержит только стиль спецификации. Это связано с тем, что фрагмент и деталь непосредственно к спецификации не подключаются, но в них можно создавать объекты спецификации для передачи в чертежи и сборки.

    Установка свойств доступна только для графических документов и документов-моделей. Для документа спецификации свойства интерфейса работают в режиме "только для чтения". После изменения параметров оформления нужно вызвать метод Update, чтобы новые данные вступили в силу.

    Данный интерфейс можно получить у коллекции описаний спецификации ISpecificationDescriptions.
    """

    @property
    def Active(self) -> bool:  # ispecificationdescription_active.html
        """
        ## Active - Текущее описание

        Тип данных: BOOL

        Значения свойства:

        +-------------------------------+
        | TRUE  | - описание активно,   |
        +-------------------------------+
        | FALSE | - описание неактивно. |
        +-------------------------------+

        1. Свойство возвращает признак активности описания и позволяет сделать описание активным. Tо есть установить свойство можно только в TRUE.

        2. После изменения параметров оформления метод Update вызывать не требуется.

        ### Синтаксис Automation

        ```
        Active = iObject.Active       # Получить свойство (* )
        iObject.Active = Active       # Установить свойство (* )
        Active = iObject.GetActive()  # Получить свойство (**)
        iObject.SetActive (Active)    # Установить свойство (**)
        ```
        """
        ...
    @Active.setter
    def Active(self, value: bool): ...

    @property
    def BaseObjects(self) -> ISpecificationBaseObjects:  # ispecificationdescription_baseobjects.html
        """
        ## BaseObjects - Базовые объекты спецификации

        Тип данных: указатель на интерфейс ISpecificationBaseObjects коллекции базовых объектов спецификации

        1. Свойство позволяет получить коллекцию базовых объектов спецификации.

        2. Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        Objects = iObject.BaseObjects       # Получить свойство (* )
        Objects = iObject.GetBaseObjects()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def CommentObjects(self) -> ISpecificationCommentObjects:  # ispecificationdescription_commentobjects.html
        """
        ## CommentObjects - Объекты спецификации - комментарии

        Тип данных: указатель на интерфейс ISpecificationCommentObjects коллекции вспомогательных объектов спецификации

        1. Свойство позволяет получить коллекцию вспомогательных объектов спецификации.

        2. Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        Objects = iObject.CommentObjects       # Получить свойство (* )
        Objects = iObject.GetCommentObjects()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def CurrentObject(self) -> ISpecificationObject:  # ispecificationdescription_currentobject.html
        """
        ## CurrentObject - Текущий объект спецификации (выделенный или редактируемый в таблице спецификации)

        Тип данных: указатель на интерфейс ISpecificationObject объекта спецификации.

        1. С помощью данного свойства можно получить и установить текущий объект спецификации.

        2. В графическом документе или документе-модели метод работает, если открыто окно спецификации в подчиненном режиме.

        ### Синтаксис Automation

        ```
        CurrentObject = iObject.CurrentObject       # Получить свойство (* )
        iObject.CurrentObject = CurrentObject       # Установить свойство (* )
        CurrentObject = iObject.GetCurrentObject()  # Получить свойство (**)
        iObject.SetCurrentObject (CurrentObject)    # Установить свойство (**)
        ```
        """
        ...
    @CurrentObject.setter
    def CurrentObject(self, value: ISpecificationObject): ...

    @property
    def DelegateMode(self) -> bool:  # ispecificationdescription_delegatemode.html
        """
        ## DelegateMode - Режим редактирования внешних объектов для описания спецификации сборки

        Тип данных: BOOL

        Позволяет получать и создавать внешние объекты спецификации в документе типа "сборка".

        ### Синтаксис Automation

        ```
        DelegateMode = Object.DelegateMode       # Получить свойство (* )
        Object.DelegateMode = DelegateMode       # Установить свойство (* )
        DelegateMode = Object.GetDelegateMode()  # Получить свойство (**)
        Object.SetDelegateMode( DelegateMode )   # Установить свойство (**)
        ```
        """
        ...
    @DelegateMode.setter
    def DelegateMode(self, value: bool): ...

    @property
    def LayoutName(self) -> str:  # ispecificationdescription_layoutname.html
        """
        ## LayoutName - Имя файла библиотеки стилей

        Тип данных: BSTR

        1. Свойство позволяет получить и установить имя библиотеки стилей, из которой будет взят стиль спецификации для данного описания.

        2. После изменения параметров оформления нужно вызвать метод Update, чтобы новые данные вступили в силу.

        ### Синтаксис Automation

        ```
        LayoutName = iObject.LayoutName       # Получить свойство (* )
        iObject.LayoutName = LayoutName       # Установить свойство (* )
        LayoutName = iObject.GetLayoutName()  # Получить свойство (**)
        iObject.SetLayoutName (LayoutName)    # Установить свойство (**)
        ```
        """
        ...
    @LayoutName.setter
    def LayoutName(self, value: str): ...

    @property
    def NeedRebuild(self) -> bool:  # ispecificationdescription_needrebuild.html
        """
        ## NeedRebuild - Необходимость перестроения спецификации

        Тип данных: BOOL

        1. Свойство позволяет временно отключить обновление окна документа спецификации из функций API при создании и изменении объектов спецификации. А также ускорить:

        •создание и изменение большого количества объектов,

        •работу функции изменения имен исполнений в групповых спецификациях.

        2. При изменении объектов спецификации можно воспользоваться свойством ISpecificationDescription::PerformanceName или методом ISpecificationObject::Update.

        ### Синтаксис Automation

        ```
        NeedRebuild = Object.NeedRebuild       # Получить свойство (* )
        Object.NeedRebuild = NeedRebuild       # Установить свойство (* )
        NeedRebuild = Object.GetNeedRebuild()  # Получить свойство (**)
        Object.SetNeedRebuild (NeedRebuild)    # Установить свойство (**)
        ```
        """
        ...
    @NeedRebuild.setter
    def NeedRebuild(self, value: bool): ...

    @property
    def Objects(self) -> typing.Any:  # ispecificationdescription_objects.html
        """
        ## Objects - Объекты спецификации

        Тип данных: VARIANT

        1. Свойство позволяет получить полный список объектов спецификации. Порядок и состав списка соответствует визуальному отображению данного описания спецификации.

        2. Свойство возвращает переменную типа VARIANT на безопасный массив интерфейсов SAFEARRAY (VT_ARRAY | VT_DISPATCH). Если возвращается один объект, то тип VARIANT-а будет VT_DISPATCH, если возвращается несколько объектов, то тип VARIANT-а будет VT_ARRAY | VT_DISPATCH.

        3. Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        Objects = iObject.Objects       # Получить свойство (* )
        Objects = iObject.GetObjects()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def PerformanceCount(self) -> int:  # ispecificationdescription_performancecount.html
        """
        ## PerformanceCount - Количество исполнений

        Тип данных: long

        ### Синтаксис Automation

        ```
        PerformanceCount = Object. PerformanceCount       # Получить свойство (* )
        Object. PerformanceCount = PerformanceCount       # Установить свойство (* )
        PerformanceCount = Object.Get PerformanceCount()  # Получить свойство (**)
        Object.Set PerformanceCount ( PerformanceCount)   # Установить свойство (**)
        ```
        """
        ...
    @PerformanceCount.setter
    def PerformanceCount(self, value: int): ...

    @property
    def PerformanceCountInBlock(self) -> int:  # ispecificationdescription_performancecountinblock.html
        """
        ## PerformanceCountInBlock - Количество исполнений в блоке

        Тип данных: long

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        PerformanceCountInBlock = Object.PerformanceCountInBlock       # Получить свойство (* )
        PerformanceCountInBlock = Object.GetPerformanceCountInBlock()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def ShowAllObjects(self) -> bool:  # ispecificationdescription_showallobjects.html
        """
        ## ShowAllObjects - Показывать все объекты

        Тип данных: BOOL

        Значения свойства:

        +-----------------------------------------------------------------------------------------------+
        | TRUE  | - отображаются все объекты в описании,                                                |
        +-----------------------------------------------------------------------------------------------+
        | FALSE | - одинаковые объекты отображаются одной строкой, а в колонке количество отображается. |
        +-----------------------------------------------------------------------------------------------+

        Свойство позволяет получить или установить состояние режима работы, в котором на экране отображаются все объекты спецификации, в том числе базовые объекты спецификации, имеющие одинаковую текстовую часть, а также базовые объекты спецификации, в настройках дополнительных параметров которых выключена опция показа объекта в таблице.

        Одинаковые объекты могут иметь одинаковые или разные номера позиций, могут находиться в соседних строках или в разных местах таблицы (в пределах одного раздела спецификации). Если объекты с одинаковой текстовой частью находятся в разных разделах спецификации, то они считаются разными.

        ### Синтаксис Automation

        ```
        ShowAllObjects = iObject.ShowAllObjects       # Получить свойство (* )
        iObject.ShowAllObjects = ShowAllObjects       # Установить свойство (* )
        ShowAllObjects = iObject.GetShowAllObjects()  # Получить свойство (**)
        iObject.SetShowAllObjects (ShowAllObjects)    # Установить свойство (**)
        ```
        """
        ...
    @ShowAllObjects.setter
    def ShowAllObjects(self, value: bool): ...

    @property
    def ShowExcludedObjects(self) -> bool:  # ispecificationdescription_showexcludedobjects.html
        """
        ## ShowExcludedObjects - Показывать только исключенные объекты

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        ShowExcludedObjects = Object.ShowExcludedObjects       # Получить свойство (* )
        Object.ShowExcludedObjects = ShowExcludedObjects       # Установить свойство (* )
        Object.SetShowExcludedObjects( ShowExcludedObjects ))  # Получить свойство (**)
        iObject.SetShowOnSheet (ShowOnSheet)                   # Установить свойство (**)
        ```
        """
        ...
    @ShowExcludedObjects.setter
    def ShowExcludedObjects(self, value: bool): ...

    @property
    def ShowOnSheet(self) -> bool:  # ispecificationdescription_showonsheet.html
        """
        ## ShowOnSheet - Показывать на листе

        Тип данных: BOOL

        Значения свойства:

        +------------------------------------------------+
        | TRUE  | - описание отображается на чертеже,    |
        +------------------------------------------------+
        | FALSE | - описание не отображается на чертеже. |
        +------------------------------------------------+

        1. Свойство позволяет отобразить или скрыть спецификацию в чертеже.

        2. Свойство работает только в чертеже.

        ### Синтаксис Automation

        ```
        ShowOnSheet = iObject.ShowOnSheet       # Получить свойство (* )
        iObject.ShowOnSheet = ShowOnSheet       # Установить свойство (* )
        ShowOnSheet = iObject.GetShowOnSheet()  # Получить свойство (**)
        iObject.SetShowOnSheet (ShowOnSheet)    # Установить свойство (**)
        ```
        """
        ...
    @ShowOnSheet.setter
    def ShowOnSheet(self, value: bool): ...

    @property
    def SpecificationDocumentName(self) -> str:  # ispecificationdescription_specificationdocumentname.html
        """
        ## SpecificationDocumentName - Имя подключенного файла документа спецификации

        Тип данных: BSTR

        1. Свойство позволяет получить и установить полное имя файла документа спецификации подключенной к данному чертежу или сборке. Доступно только для чертежа или сборки.

        2. После изменения параметров оформления нужно вызвать метод Update, чтобы новые данные вступили в силу.

        ### Синтаксис Automation

        ```
        DocumentName = iObject.SpecificationDocumentName       # Получить свойство (* )
        iObject.SpecificationDocumentName = DocumentName       # Установить свойство (* )
        DocumentName = iObject.GetSpecificationDocumentName()  # Получить свойство (**)
        iObject.SetSpecificationDocumentName (DocumentName)    # Установить свойство (**)
        ```
        """
        ...
    @SpecificationDocumentName.setter
    def SpecificationDocumentName(self, value: str): ...

    @property
    def SpecificationStyle(self) -> ISpecificationStyle:  # ispecificationdescription_specificationstyle.html
        """
        ## SpecificationStyle - Стиль спецификации

        Тип данных: указатель на интерфейс ISpecificationStyle cтиля спецификации

        1. Полученный интерфейс отражает содержимое стиля в библиотеке стилей для данного описания на момент получения и в дальнейшем не отслеживает изменений в библиотеке стилей.

        2. Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        Style = iObject.SpecificationStyle       # Получить свойство (* )
        Style = iObject.GetSpecificationStyle()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def SpecificationTuning(self) -> ISpecificationTuning:  # ispecificationdescription_specificationtuning.html
        """
        ## SpecificationTuning - Настройки спецификации

        Тип данных: указатель на интерфейс ISpecificationTuning настроек спецификации

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        SpecificationTuning = iObject.SpecificationTuning       # Получить свойство (* )
        SpecificationTuning = iObject.GetSpecificationTuning()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def StyleID(self) -> int:  # ispecificationdescription_styleid.html
        """
        ## StyleID - Номер стиля в библиотеке стилей

        Тип данных: long

        1. Свойство позволяет получить и установить номер стиля в библиотеке стилей, из которой будет взят стиль спецификации для данного описания (см. LayoutName).

        2. После изменения параметров оформления нужно вызвать метод Update, чтобы новые данные вступили в силу.

        ### Синтаксис Automation

        ```
        StyleID = iObject.StyleID       # Получить свойство (* )
        iObject.StyleID = StyleID       # Установить свойство (* )
        StyleID = iObject.GetStyleID()  # Получить свойство (**)
        iObject.SetStyleID (StyleID)    # Установить свойство (**)
        ```
        """
        ...
    @StyleID.setter
    def StyleID(self, value: int): ...

    def CompareStyleWithLibStyle(self):  # ispecificationdescription_comparestylewithlibstyle.html
        """
        ## CompareStyleWithLibStyle - Отличие стиля СП от библиотечного: 0 - не отличается, 1 -отличается, -1 - стиль не найден

        ### Синтаксис Automation

        ```
        ksSpecificationStyleDifferenceTypeEnum CompareStyleWithLibStyle();
        ```

        ### Возвращаемое значение

        +-------------------------+
        | 0  | - не отличается,   |
        +-------------------------+
        | 1  | - отличается,      |
        +-------------------------+
        | -1 | - стиль не найден. |
        +-------------------------+
        """
        ...

    def Delete(self):  # ispecificationdescription_delete.html
        """
        ## Delete - Удалить описание

        Метод удаляет описание спецификации.

        ### Синтаксис Automation

        ```
        BOOL Delete();
        ```
        """
        ...

    def GetPerformanceParam(self, DisplayPerformanceIndex):  # ispecificationdescription_getperformanceparam.html
        """
        ## GetPerformanceParam - Получить параметры и имя колонки исполнения по индексу

        1. Индекс исполнения начинается с единицы.

        2. В групповой спецификации по варианту Б, созданной по сборке, у которой есть исполнения с непоследовательными номерами исполнений, часть колонок исполнений может быть отключена и отображаться будут только те исполнения, которые пришли из сборки. В такой спецификации в одной строке объекта спецификации могут отображаться исполнения и количество из нескольких блоков.

        3. В групповой спецификации по варианту A также могут быть непоследовательные номера исполнений, если спецификация создана по сборке.

        4. Метод рекомендуется использовать вместо свойства ISpecificationDescription::PerformanceName.PerformanceIndex и BlockIndex позволяют сопоставить имя исполнения и значение количества в конкретной колонке количество объектов спецификации.

        ### Синтаксис Automation

        ```
        LPCTSTR GetPerformanceParam( long DisplayPerformanceIndex, long * PerformanceIndex, long * BlockIndex );
        ```

        ### Входные параметры

        +-------------------------------------------------------------------------------------+
        | DisplayPerformanceIndex | - индекс отображаемого исполнения (начинается с единицы). |
        +-------------------------------------------------------------------------------------+

        ### Выходные параметры

        +-------------------------------------------------------------------------------------+
        | PerformanceIndex | - номер колонки исполнения,                                      |
        +-------------------------------------------------------------------------------------+
        | BlockIndex       | - указатель на структуру параметров библиотечной панели свойств. |
        +-------------------------------------------------------------------------------------+

        ### Возвращаемое значение

        - Имя исполнения.

        Метод позволяет получить имя исполнения, номер исполнения и номер блока по индексу исполнения.
        """
        ...

    def PerformanceName(self, PerformanceIndex, BlockIndex) -> str:  # ispecificationdescription_performancename.html
        """
        ## PerformanceName - Отображаемое имя исполнения

        Тип данных: BSTR

        1. В текст обозначения исполнения, которое редактируется, нужно включать разделитель.

        2. Для получения имен исполнений рекомендуется использовать метод ISpecificationDescription::GetPerformanceParam.

        3. Для ускорения работы функции при массовом изменении спецификации рекомендуется воспользоваться методом ISpecificationDescription::NeedRebuild.

        ### Синтаксис Automation

        ```
        PerformanceName = iObject.PerformanceName( PerformanceIndex, BlockIndex )     # Получить свойство (* )
        iObject.PerformanceName( PerformanceIndex, BlockIndex ) = PerformanceName     # Установить свойство (* )
        PerformanceName = iObject.GetPerformanceName( PerformanceIndex, BlockIndex )  # Получить свойство (**)
        iObject.SetPerformanceName( PerformanceIndex, BlockIndex, PerformanceName )   # Установить свойство (**)
        ```

        ### Входные параметры

        +----------------------------------------------------------------------------------------------------+
        | PerformanceIndex | - номер колонки типа "количество", начиная с 1(актуально для СП по варианту Б), |
        +----------------------------------------------------------------------------------------------------+
        | BlockIndex       | - номер блока, начиная с 0 (актуально для СП по варианту A).                    |
        +----------------------------------------------------------------------------------------------------+
        """
        ...

    def SetPerformanceName(self, PerformanceIndex, BlockIndex, arg2):
        ...

    def Update(self):  # ispecificationdescription_update.html
        """
        ## Update - Изменить описание

        Метод устанавливает новые параметры для описания спецификации.

        ### Синтаксис Automation

        ```
        BOOL Update();
        ```
        """
        ...




class ISpecificationObject(IKompasAPIObject):  # ispecificationobject.html
    """
    ## Интерфейс ISpecificationObject

    Интерфейс объекта спецификации.

    Различаются несколько типов объектов спецификации (см. ksSpecificationObjectTypeEnum). Данный интерфейс позволяет получить общие параметры для объектов спецификации и изменить общие параметры для базовых и вспомогательных объектов спецификации.

    1. После изменения параметров объекта спецификации требуется вызвать метод ISpecificationObject::Update.

    2. Данный интерфейс можно получить у базового ISpecificationBaseObject и вспомогательного ISpecificationCommentObject объектов спецификации и через безопасный массив ISpecificationDescription::Objects для остальных типов объектов спецификации.
    """

    @property
    def AdditionalBlock(self) -> int:  # ispecificationobject_additionalblock.html
        """
        ## AdditionalBlock - Номер блока дополнительных разделов

        Тип данных: long

        1. Свойство доступно только для базового и вспомогательного объекта спецификации.

        2. Изменение этого свойства вступит в силу после вызова метода ISpecificationObject::Update.

        ### Синтаксис Automation

        ```
        AdditionalBlock = iObject.AdditionalBlock       # Получить свойство (* )
        iObject.AdditionalBlock = AdditionalBlock       # Установить свойство (* )
        AdditionalBlock = iObject.GetAdditionalBlock()  # Получить свойство (**)
        iObject.SetAdditionalBlock (AdditionalBlock)    # Установить свойство (**)
        ```
        """
        ...
    @AdditionalBlock.setter
    def AdditionalBlock(self, value: int): ...

    @property
    def AdditionalColumns(self) -> ISpecificationColumns:  # ispecificationobject_additionalcolumns.html
        """
        ## AdditionalColumns - Дополнительные колонки

        Тип данных: указатель на интерфейс ISpecificationColumns коллекции колонок объекта спецификации

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        AdditionalColumns = iObject.AdditionalColumns       # Получить свойство (* )
        AdditionalColumns = iObject.GetAdditionalColumns()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def AdditionalSection(self) -> int:  # ispecificationobject_additionalsection.html
        """
        ## AdditionalSection - Номер дополнительного раздела

        Тип данных: long

        1. Свойство доступно только для базового и вспомогательного объекта спецификации.

        2. Изменение этого свойства вступит в силу после вызова метода ISpecificationObject::Update.

        ### Синтаксис Automation

        ```
        AdditionalSection = iObject.AdditionalSection       # Получить свойство (* )
        iObject.AdditionalSection = AdditionalSection       # Установить свойство (* )
        AdditionalSection = iObject.GetAdditionalSection()  # Получить свойство (**)
        iObject.SetAdditionalSection (AdditionalSection)    # Установить свойство (**)
        ```
        """
        ...
    @AdditionalSection.setter
    def AdditionalSection(self, value: int): ...

    @property
    def AttachedDocuments(self) -> IAttachedDocuments:  # ispecificationobject_attacheddocuments.html
        """
        ## AttachedDocuments - Присоединенные документы

        Тип данных: указатель на интерфейс IAttachedDocuments коллекции присоединенных документов

        1. Получить коллекцию можно только для базовых и вспомогательных объектов спецификации, если в стиле для заданного раздела установлен признак ручное заполнение или чтение из основной надписи.

        2. Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        AttachedDocuments = iObject.AttachedDocuments       # Получить свойство (* )
        AttachedDocuments = iObject.GetAttachedDocuments()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def Columns(self) -> ISpecificationColumns:  # ispecificationobject_columns.html
        """
        ## Columns - Колонки объекта спецификации

        Тип данных: указатель на интерфейс ISpecificationColumns коллекции колонок объекта спецификации

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        Columns = iObject.Columns       # Получить свойство (* )
        Columns = iObject.GetColumns()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def FirstOnSheet(self) -> bool:  # ispecificationobject_firstonsheet.html
        """
        ## FirstOnSheet - Размещать объект в начале нового листа спецификации

        Тип данных: BOOL

        Значения свойства:

        +--------------------------------------------------------------------------------+
        | TRUE  | - объект спецификации разместить в начале новой страницы спецификации, |
        +--------------------------------------------------------------------------------+
        | FALSE | - объект спецификации разместить после предыдущего.                    |
        +--------------------------------------------------------------------------------+

        1. Свойство доступно только для базового и вспомогательного объекта спецификации.

        2. Изменение этого свойства вступит в силу после вызова метода ISpecificationObject::Update.

        ### Синтаксис Automation

        ```
        FirstOnSheet = iObject.FirstOnSheet       # Получить свойство (* )
        iObject.FirstOnSheet = FirstOnSheet       # Установить свойство (* )
        FirstOnSheet = iObject.GetFirstOnSheet()  # Получить свойство (**)
        iObject.SetFirstOnSheet (FirstOnSheet)    # Установить свойство (**)
        ```
        """
        ...
    @FirstOnSheet.setter
    def FirstOnSheet(self, value: bool): ...

    @property
    def IncrementPosition(self) -> bool:  # ispecificationobject_incrementposition.html
        """
        ## IncrementPosition - Позиция объекта возрастает

        Тип данных: BOOL

        Значения свойства:

        +-----------------------------------------------------------------------------------------------------------+
        | TRUE  | - номер позиции объекта должен быть на единицу больше номера позиции предшествующего ему объекта, |
        +-----------------------------------------------------------------------------------------------------------+
        | FALSE | - номер позиции объекта совпадает с номером позиции предыдущего объекта.                          |
        +-----------------------------------------------------------------------------------------------------------+

        1. Свойство доступно только для базового и вспомогательного объекта спецификации.

        2. Изменение этого свойства вступит в силу после вызова метода ISpecificationObject::Update.

        ### Синтаксис Automation

        ```
        IncrementPosition = iObject.IncrementPosition       # Получить свойство (* )
        iObject.IncrementPosition = IncrementPosition       # Установить свойство (* )
        IncrementPosition = iObject.GetIncrementPosition()  # Получить свойство (**)
        iObject.SetIncrementPosition (IncrementPosition)    # Установить свойство (**)
        ```
        """
        ...
    @IncrementPosition.setter
    def IncrementPosition(self, value: bool): ...

    @property
    def NestedBlock(self) -> int:  # ispecificationobject_nestedblock.html
        """
        ## NestedBlock - Номер блока вложенных разделов

        Тип данных: long

        1. Свойство доступно только для базового и вспомогательного объекта спецификации.

        2. Изменение этого свойства вступит в силу после вызова метода ISpecificationObject::Update.

        ### Синтаксис Automation

        ```
        NestedBlock = iObject.NestedBlock       # Получить свойство (* )
        iObject.NestedBlock = NestedBlock       # Установить свойство (* )
        NestedBlock = iObject.GetNestedBlock()  # Получить свойство (**)
        iObject.SetNestedBlock (NestedBlock)    # Установить свойство (**)
        ```
        """
        ...
    @NestedBlock.setter
    def NestedBlock(self, value: int): ...

    @property
    def NestedSection(self) -> int:  # ispecificationobject_nestedsection.html
        """
        ## NestedSection - Номер вложенного раздела

        Тип данных: long

        1. Свойство доступно только для базового и вспомогательного объекта спецификации.

        2. Изменение этого свойства вступит в силу после вызова метода ISpecificationObject::Update.

        ### Синтаксис Automation

        ```
        NestedSection = iObject.NestedSection     # Получить свойство (* )
        iObject.NestedSection = NestedSection     # Установить свойство (* )
        iObject.GetNestedSection()                # Получить свойство (**)
        iObject.SetNestedSection (NestedSection)  # Установить свойство (**)
        ```
        """
        ...
    @NestedSection.setter
    def NestedSection(self, value: int): ...

    @property
    def ObjectType(self) -> int:  # ispecificationobject_objecttype.html
        """
        ## ObjectType - Тип объекта

        Тип данных: из перечисления ksSpecificationObjectTypeEnum

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        ObjectType = iObject.ObjectType       # Получить свойство (* )
        ObjectType = iObject.GetObjectType()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def Section(self) -> int:  # ispecificationobject_section.html
        """
        ## Section - Номер раздела

        Тип данных: long

        1. При создании базового или вспомогательного объекта спецификации номер раздела передается в метод Add. Переместить уже созданные объекты из одного раздела в другой нельзя.

        2. Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        Section = iObject.Section       # Получить свойство (* )
        Section = iObject.GetSection()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def State(self) -> int:  # ispecificationobject_state.html
        """
        ## State - Состояние объекта

        Тип данных: из перечисления ksSpecificationObjectStateEnum

        Значения свойства:

        +-------------------------------------------------------------------------------------------------+
        | ssIndependent | - объект создан в данном документе,                                             |
        +-------------------------------------------------------------------------------------------------+
        | ssFromInsert  | - объект пришел из вставки другого документа (фрагмента, детали или подсборки), |
        +-------------------------------------------------------------------------------------------------+
        | ssEdit        | - объект из вставки другого документа, но редактировался в данном документе.    |
        +-------------------------------------------------------------------------------------------------+

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        State = iObject.State       # Получить свойство (* )
        State = iObject.GetState()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def Subsection(self) -> int:  # ispecificationobject_subsection.html
        """
        ## Subsection - Номер подраздела

        Тип данных: long

        1. Свойство доступно только для базового и вспомогательного объекта спецификации.

        2. Изменение этого свойства вступит в силу после вызова метода ISpecificationObject::Update.

        ### Синтаксис Automation

        ```
        Subsection = iObject.Subsection       # Получить свойство (* )
        iObject.Subsection = Subsection       # Установить свойство (* )
        Subsection = iObject.GetSubsection()  # Получить свойство (**)
        iObject.SetSubsection (Subsection)    # Установить свойство (**)
        ```
        """
        ...
    @Subsection.setter
    def Subsection(self, value: int): ...

    @property
    def UniqueNumber(self) -> float:  # ispecificationobject_uniquenumber.html
        """
        ## UniqueNumber - Уникальный номер, присваивается при создании

        Тип данных: double

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        UniqueNumber = iObject.UniqueNumber  # Получить свойство (* )
        Objects = iObject.GetBaseObjects()   # Получить свойство (**)
        ```
        """
        ...

    def BlockNumberByIndex(self, Val) -> int:  # ispecificationobject_blocknumberbyindex.html
        """
        ## BlockNumberByIndex - Номер блока

        Тип данных: long

        1. Свойство позволяет получить номер блока объекта спецификации.

        2. Один и тот же базовый объект спецификации может быть вставлен в спецификации несколько раз в нескольких блоках.

        3. Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        BlockNumber = iObject.BlockNumberByIndex (index)     # Получить свойство (* )
        BlockNumber = iObject.GetBlockNumberByIndex (index)  # Получить свойство (**)
        ```

        ### Входные параметры

        +-----------------------------------------------------------------+
        | index | - индекс объекта в общем массиве объектов спецификации. |
        +-----------------------------------------------------------------+
        """
        ...

    def Delete(self):  # ispecificationobject_delete.html
        """
        ## Delete - Удалить объект

        Метод позволяет удалить базовый или вспомогательный объект спецификации из документа и из коллекции.

        ### Синтаксис Automation

        ```
        BOOL Delete();
        ```
        """
        ...

    def Edit(self):  # ispecificationobject_edit.html
        """
        ## Edit - Редактировать объект

        Метод позволяет запустить процесс визуального редактирования для базового или вспомогательного объекта спецификации.

        ### Синтаксис Automation

        ```
        BOOL Edit();
        ```
        """
        ...

    def Update(self):  # ispecificationobject_update.html
        """
        ## Update - Обновить данные

        После изменения параметров объекта спецификации требуется вызвать данный метод, чтобы новые данные попали в модельный объект спецификации. Если объект спецификации еще не создан, метод Update создает объект спецификации в документе.

        Для ускорения работы функции при массовом изменении спецификации рекомендуется воспользоваться методом ISpecificationDescription::NeedRebuild.

        ### Синтаксис Automation

        ```
        BOOL Update();
        ```
        """
        ...




class ISpecificationObjectCreateParam(IKompasAPIObject):  # ispecificationobjectcreateparam.html
    """
    ## Интерфейс ISpecificationObjectCreateParam

    Интерфейс расширенных параметров создания объекта спецификации.

    Данный интерфейс можно получить с помощью метода IKompasDocument1::GetInterface.

    Интерфейс позволяет задать расширенные параметры создания объектов спецификации для функций:

    •ISpecificationBaseObjects::AddWithParam

    •ISpecificationCommentObjects::AddWithParam

    Версия: Компас v20
    """

    @property
    def AdditionalBlock(self) -> int:  # ispecificationobjectcreateparam_additionalblock.html
        """
        ## AdditionalBlock - Номер блока дополнительных разделов

        Тип данных: long

        ### Синтаксис Automation

        ```
        AdditionalBlock = Object.AdditionalBlock       # Получить свойство (* )
        Object.AdditionalBlock = AdditionalBlock       # Установить свойство (* )
        AdditionalBlock = Object.GetAdditionalBlock()  # Получить свойство (**)
        Object.SetAdditionalBlock( AdditionalBlock )   # Установить свойство (**)
        ```
        """
        ...
    @AdditionalBlock.setter
    def AdditionalBlock(self, value: int): ...

    @property
    def AdditionalSection(self) -> int:  # ispecificationobjectcreateparam_additionalsection.html
        """
        ## AdditionalSection - Номер дополнительного раздела

        Тип данных: long

        ### Синтаксис Automation

        ```
        AdditionalSection = Object.AdditionalSection       # Получить свойство (* )
        Object.AdditionalSection = AdditionalSection       # Установить свойство (* )
        AdditionalSection = Object.GetAdditionalSection()  # Получить свойство (**)
        Object.SetAdditionalSection( AdditionalSection )   # Установить свойство (**)
        ```
        """
        ...
    @AdditionalSection.setter
    def AdditionalSection(self, value: int): ...

    @property
    def AttributeNumber(self) -> float:  # ispecificationobjectcreateparam_attributenumber.html
        """
        ## AttributeNumber - Номер атрибута

        Тип данных: double

        ### Синтаксис Automation

        ```
        AttributeNumber = Object.AttributeNumber       # Получить свойство (* )
        Object.AttributeNumber = AttributeNumber       # Установить свойство (* )
        AttributeNumber = Object.GetAttributeNumber()  # Получить свойство (**)
        Object.SetAttributeNumber( AttributeNumber )   # Установить свойство (**)
        ```
        """
        ...
    @AttributeNumber.setter
    def AttributeNumber(self, value: float): ...

    @property
    def FillNameByMaterial(self) -> bool:  # ispecificationobjectcreateparam_fillnamebymaterial.html
        """
        ## FillNameByMaterial - Синхронизировать наименование с материалом

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        FillNameByMaterial = Object.FillNameByMaterial       # Получить свойство (* )
        Object.FillNameByMaterial = FillNameByMaterial       # Установить свойство (* )
        FillNameByMaterial = Object.GetFillNameByMaterial()  # Получить свойство (**)
        Object.SetFillNameByMaterial( FillNameByMaterial )   # Установить свойство (**)
        ```
        """
        ...
    @FillNameByMaterial.setter
    def FillNameByMaterial(self, value: bool): ...

    @property
    def NestedBlock(self) -> int:  # ispecificationobjectcreateparam_nestedblock.html
        """
        ## NestedBlock - Номер блока вложенных разделов

        Тип данных: long

        ### Синтаксис Automation

        ```
        NestedBlock = Object.NestedBlock       # Получить свойство (* )
        Object.NestedBlock = NestedBlock       # Установить свойство (* )
        NestedBlock = Object.GetNestedBlock()  # Получить свойство (**)
        Object.SetNestedBlock( NestedBlock )   # Установить свойство (**)
        ```
        """
        ...
    @NestedBlock.setter
    def NestedBlock(self, value: int): ...

    @property
    def NestedSection(self) -> int:  # ispecificationobjectcreateparam_nestedsection.html
        """
        ## NestedSection - Номер вложенного раздела

        Тип данных: long

        ### Синтаксис Automation

        ```
        NestedSection = Object.NestedSection       # Получить свойство (* )
        Object.NestedSection = NestedSection       # Установить свойство (* )
        NestedSection = Object.GetNestedSection()  # Получить свойство (**)
        Object.SetNestedSection( NestedSection )   # Установить свойство (**)
        ```
        """
        ...
    @NestedSection.setter
    def NestedSection(self, value: int): ...

    @property
    def Section(self) -> int:  # ispecificationobjectcreateparam_section.html
        """
        ## Section - Номер раздела

        Тип данных: long

        ### Синтаксис Automation

        ```
        Section = Object.Section       # Получить свойство (* )
        Object.Section = Section       # Установить свойство (* )
        Section = Object.GetSection()  # Получить свойство (**)
        Object.SetSection( Section )   # Установить свойство (**)
        ```
        """
        ...
    @Section.setter
    def Section(self, value: int): ...

    @property
    def Subsection(self) -> int:  # ispecificationobjectcreateparam_subsection.html
        """
        ## Subsection - Номер подраздела

        Тип данных: long

        ### Синтаксис Automation

        ```
        Subsection = Object.Subsection       # Получить свойство (* )
        Object.Subsection = Subsection       # Установить свойство (* )
        Subsection = Object.GetSubsection()  # Получить свойство (**)
        Object.SetSubsection( Subsection )   # Установить свойство (**)
        ```
        """
        ...
    @Subsection.setter
    def Subsection(self, value: int): ...




class ISpecificationSectionStyle(IKompasAPIObject):  # ispecificationsectionstyle.html
    """
    ## Интерфейс ISpecificationSectionStyle

    Интерфейс стиля раздела спецификации.

    В спецификации все объекты располагаются в своем разделе, если включено разбиение на разделы ISpecificationStyle::SectionOn. Разделы располагаются в порядке возрастания или убывания их номеров ISpecificationSectionStyle::Number, в зависимости от значения свойства ISpecificationStyle::SortSectionDown. Раздел автоматически добавляется в спецификацию, если в нем создается базовый объект спецификации или комментарий, и автоматически удаляется из спецификации при удалении последнего объекта спецификации.

    Данный интерфейс можно получить от интерфейса стиля раздела спецификации ISpecificationSectionStyles, ISpecificationSectionStyles::Item.
    """

    @property
    def AdditionalBlocks(self) -> IAdditionalBlockStyles:  # ispecificationsectionstyle_additionalblocks.html
        """
        ## AdditionalBlocks - Блоки вложенных разделов

        Тип данных: указатель на интерфейс IAdditionalBlockStyles коллекции стилей блоков дополнительных разделов.

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        AdditionalBlocks = iObject.AdditionalBlocks       # Получить свойство (* )
        AdditionalBlocks = iObject.GetAdditionalBlocks()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def AdditionalColumns(self) -> ISpecificationColumnStyles:  # ispecificationsectionstyle_additionalcolumns.html
        """
        ## AdditionalColumns - Дополнительные колонки спецификации

        Тип данных: указатель на интерфейс ISpecificationColumnStyles коллекции стилей колонок спецификации

        1. Настройки стиля колонок для данного раздела (могут отличаться от умолчательных).

        2. Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        AdditionalColumns = iObject.AdditionalColumns       # Получить свойство (* )
        AdditionalColumns = iObject.GetAdditionalColumns()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def Columns(self) -> ISpecificationColumnStyles:  # ispecificationsectionstyle_columns.html
        """
        ## Columns - Колонки спецификации

        Тип данных: указатель на интерфейс ISpecificationColumnStyles коллекции стилей колонок спецификации.

        1. Настройки стиля колонок для данного раздела (могут отличаться от умолчательных).

        2. Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        Columns = iObject.Columns       # Получить свойство (* )
        Columns = iObject.GetColumns()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def FillDataFromStamp(self) -> bool:  # ispecificationsectionstyle_filldatafromstamp.html
        """
        ## FillDataFromStamp - Чтение данных из основной надписи

        Тип данных: BOOL

        Значения свойства:

        +-------------------------------------------------------------------------------------------------------------------------------------------------------------+
        | TRUE  | - заполнение колонок смешанным способом - вручную с возможностью чтения информации из основной надписи связанных с объектами спецификации чертежей, |
        +-------------------------------------------------------------------------------------------------------------------------------------------------------------+
        | FALSE | - заполнение колонок - вручную.                                                                                                                     |
        +-------------------------------------------------------------------------------------------------------------------------------------------------------------+

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        FillDataFromStamp = iObject.FillDataFromStamp       # Получить свойство (* )
        FillDataFromStamp = iObject.GetFillDataFromStamp()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def Name(self) -> str:  # ispecificationsectionstyle_name.html
        """
        ## Name - Имя раздела

        Тип данных: BSTR (строка)

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        Name = iObject.Name       # Получить свойство (* )
        Name = iObject.GetName()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def Number(self) -> int:  # ispecificationsectionstyle_number.html
        """
        ## Number - Номер раздела

        Тип данных: long

        1. Данное свойство определяет уникальный номер раздела в данном стиле спецификации.

        2. Разделы в спецификации располагаются в порядке возрастания или убывания их номеров Number, в зависимости от значения свойства ISpecificationStyle::SortSectionDown.

        3. Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        Number = iObject.Number       # Получить свойство (* )
        Number = iObject.GetNumber()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def SortColumnNumber(self) -> int:  # ispecificationsectionstyle_sortcolumnnumber.html
        """
        ## SortColumnNumber - Номер колонки, в которой необходимо проводить сортировку

        Тип данных: long

        1. Данное свойство определяет уникальный номер колонки для колонок с типом SortColumnType, по которой будет производится сортировка объектов в разделе. Иногда однородная (но не одинаковая) информация должна храниться в нескольких колонках и по этой причине они имеют одинаковый тип ColumnType.

        Чтобы отличать такие колонки, вместе с типом задается еще и номер, отличающий колонку этого типа от других колонок такого же типа.

        2. Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        SortColumnNumber = iObject.SortColumnNumber       # Получить свойство (* )
        SortColumnNumber = iObject.GetSortColumnNumber()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def SortColumnType(self) -> int:  # ispecificationsectionstyle_sortcolumntype.html
        """
        ## SortColumnType - Общий тип колонки, в которой необходимо проводить сортировку

        Тип данных: из перечисления ksSpecificationColumnTypeEnum

        1. Используется при включенной опции ISpecificationTuningSection::SortObjects.

        2. Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        SortColumnType = iObject.SortColumnType       # Получить свойство (* )
        SortColumnType = iObject.GetSortColumnType()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def SortLevelsCount(self) -> int:  # ispecificationsectionstyle_sortlevelscount.html
        """
        ## SortLevelsCount - Количество уровней сортировки

        Тип данных:long

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        SortLevelsCount = Object.SortLevelsCount       # Получить свойство (* )
        SortLevelsCount = Object.GetSortLevelsCount()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def SortType(self) -> int:  # ispecificationsectionstyle_sorttype.html
        """
        ## SortType - Тип сортировки объектов спецификации в разделе

        Тип данных: из перечисления ksSortTypeEnum

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        SortType = iObject.SortType       # Получить свойство (* )
        SortType = iObject.GetSortType()  # Получить свойство (**)
        ```
        """
        ...

    def SortColumnNumberEx(self, Index) -> int:  # ispecificationsectionstyle_sortcolumnnumberex.html
        """
        ## SortColumnNumberEx - Номер колонки, в которой проводить сортировку

        Тип данных: long

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        SortColumnNumberEx = Object.SortColumnNumberEx( Index )     # Получить свойство (* )
        SortColumnNumberEx = Object.GetSortColumnNumberEx( Index )  # Получить свойство (**)
        ```

        ### Входные параметры

        +---------------------------+
        | Index | - индекс колонки. |
        +---------------------------+
        """
        ...

    def SortColumnTypeEx(self, Index) -> int:  # ispecificationsectionstyle_sortcolumntypeex.html
        """
        ## SortColumnTypeEx - Общий тип колонки, в которой необходимо проводить сортировку

        Тип данных: из перечисления ksSpecificationColumnTypeEnum

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        SortColumnTypeEx = Object.SortColumnTypeEx( Index )     # Получить свойство (* )
        SortColumnTypeEx = Object.GetSortColumnTypeEx( Index )  # Получить свойство (**)
        ```

        ### Входные параметры

        +---------------------------+
        | Index | - индекс колонки. |
        +---------------------------+
        """
        ...

    def SortTypeEx(self, Index) -> int:  # ispecificationsectionstyle_sorttypeex.html
        """
        ## SortTypeEx - Тип сортировки

        Тип данных: из перечисления ksSortTypeEnum

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        SortTypeEx = Object.SortTypeEx( Index )     # Получить свойство (* )
        SortTypeEx = Object.GetSortTypeEx( Index )  # Получить свойство (**)
        ```

        ### Входные параметры

        +---------------------------+
        | Index | - индекс колонки. |
        +---------------------------+
        """
        ...




class ISpecificationStyle(IKompasAPIObject):  # ispecificationstyle.html
    """
    ## Интерфейс ISpecificationStyle

    Интерфейс стиля спецификации.

    Описывает стиль спецификации. Стили спецификаций описаны в библиотеках стилей спецификаций (файлы библиотек стилей спецификаций имеют расширение lyt).

    Данный интерфейс отражает содержимое стиля в библиотеке стилей на момент получения и в дальнейшем не отслеживает изменений в библиотеке стилей.

    Данный интерфейс может быть получен от интерфейса ISpecificationDescription с помощью свойства ISpecificationDescription::SpecificationStyle.
    """

    @property
    def AdditionalBlocks(self) -> IAdditionalBlockStyles:  # ispecificationstyle_additionalblocks.html
        """
        ## AdditionalBlocks - Блоки дополнительных разделов

        Тип данных: указатель на интерфейс IAdditionalBlockStyles коллекции стилей блоков дополнительных разделов

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        AdditionalBlocks = iObject.AdditionalBlocks       # Получить свойство (* )
        AdditionalBlocks = iObject.GetAdditionalBlocks()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def AdditionalColumns(self) -> ISpecificationColumnStyles:  # ispecificationstyle_additionalcolumns.html
        """
        ## AdditionalColumns - дополнительные колонки спецификации - умолчательные значения

        Тип данных: указатель на интерфейс ISpecificationColumnStyles коллекции стилей колонок спецификации

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        AdditionalColumns = iObject.AdditionalColumns       # Получить свойство (* )
        AdditionalColumns = iObject.GetAdditionalColumns()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def Columns(self) -> ISpecificationColumnStyles:  # ispecificationstyle_columns.html
        """
        ## Columns - Колонки спецификации - умолчательные значения

        Тип данных: указатель на интерфейс ISpecificationColumnStyles коллекции стилей колонок спецификации

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        Columns = iObject.Columns       # Получить свойство (* )
        Columns = iObject.GetColumns()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def Format(self) -> ISheetFormat:  # ispecificationstyle_format.html
        """
        ## Format - Формат листа

        Тип данных: указатель на интерфейс ISheetFormat формата листа спецификации

        1. Данное свойство определяет формат листа бумаги, на котором будет размещаться спецификация.

        2. Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        Format = iObject.Format       # Получить свойство (* )
        Format = iObject.GetFormat()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def LayoutName1(self) -> str:  # ispecificationstyle_layoutname1.html
        """
        ## LayoutName1 - Имя файла библиотеки оформлений для первого листа

        Тип данных: BSTR (строка)

        1. Позволяет получить имя файла библиотеки оформлений для первого листа.

        2. Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        LayoutName1 = iObject.LayoutName1       # Получить свойство (* )
        LayoutName1 = iObject.GetLayoutName1()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def LayoutName2(self) -> str:  # ispecificationstyle_layoutname2.html
        """
        ## LayoutName2 - Имя файла библиотеки оформлений для последующих листов

        Тип данных: BSTR (строка)

        1. Позволяет получить имя файла библиотеки оформлений для последующих листов.

        2. Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        LayoutName2 = iObject.LayoutName2       # Получить свойство (* )
        LayoutName2 = iObject.GetLayoutName2()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def PerformanceCountInBlock(self) -> int:  # ispecificationstyle_performancecountinblock.html
        """
        ## PerformanceCountInBlock - Количество исполнений в блоке

        Тип данных: long

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        PerformanceCountInBlock = Object.PerformanceCountInBlock( PVal )     # Получить свойство (* )
        PerformanceCountInBlock = Object.GetPerformanceCountInBlock( PVal )  # Получить свойство (**)
        ```
        """
        ...

    @property
    def SectionOn(self) -> bool:  # ispecificationstyle_sectionon.html
        """
        ## SectionOn - Деление на разделы

        Тип данных: BOOL

        Значения свойства:

        +-------------------------------------------------------------------------------------------+
        | TRUE  | - разбиение на разделы включено, все объекты будут располагаться в своем разделе, |
        +-------------------------------------------------------------------------------------------+
        | FALSE | - разбиение на разделы выключено.                                                 |
        +-------------------------------------------------------------------------------------------+

        1. Позволяет получить признак разбиения спецификации на разделы.

        2. Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        SectionOn = iObject.SectionOn       # Получить свойство (* )
        SectionOn = iObject.GetSectionOn()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def Sections(self) -> ISpecificationSectionStyles:  # ispecificationstyle_sections.html
        """
        ## Sections - Разделы

        Тип данных: указатель на интерфейс ISpecificationSectionStyles коллекции стилей разделов спецификации

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        Sections = iObject.Sections       # Получить свойство (* )
        Sections = iObject.GetSections()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def SortSectionDown(self) -> bool:  # ispecificationstyle_sortsectiondown.html
        """
        ## SortSectionDown - Сортировка разделов по убыванию

        Тип данных: BOOL

        Значения свойства:

        +-----------------------------------------------------------------------------------------------------------------------------------------------+
        | TRUE  | - сортировка разделов по убыванию включена, разделы будут располагаться внутри спецификации в порядке убывания их порядковых номеров, |
        +-----------------------------------------------------------------------------------------------------------------------------------------------+
        | FALSE | - сортировка разделов по убыванию выключена.                                                                                          |
        +-----------------------------------------------------------------------------------------------------------------------------------------------+

        1. Позволяет получить признак сортировки разделов по убыванию.

        2. Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        SortSectionDown = iObject.SortSectionDown       # Получить свойство (* )
        SortSectionDown = iObject.GetSortSectionDown()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def SpecificationTuning(self) -> ISpecificationTuning:  # ispecificationstyle_specificationtuning.html
        """
        ## SpecificationTuning - Настройки спецификации

        Тип данных: указатель на интерфейс ISpecificationTuning настроек спецификации

        Свойство доступно только для чтения. Полученные настройки будут соответствовать настройкам, описанным для данного стиля в библиотеке стилей. Для получения настроек описания спецификации в документе надо брать настройки непосредственно у описания ISpecificationDescription::SpecificationTuning, т.к. они могут отличатся от настроек стиля.

        ### Синтаксис Automation

        ```
        SpecificationTuning = iObject.SpecificationTuning       # Получить свойство (* )
        SpecificationTuning = iObject.GetSpecificationTuning()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def StyleID1(self) -> int:  # ispecificationstyle_styleid1.html
        """
        ## StyleID1 - Номер стиля в библиотеке оформлений для первого листа

        Тип данных: long

        1. Позволяет получить номер стиля в библиотеке оформлений для первого листа.

        2. Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        StyleID1 = iObject.StyleID1       # Получить свойство (* )
        StyleID1 = iObject.GetStyleID1()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def StyleID2(self) -> int:  # ispecificationstyle_styleid2.html
        """
        ## StyleID2 - Номер стиля в библиотеке оформлений для последующих листов

        Тип данных: long

        1. Позволяет получить номер стиля в библиотеке оформлений для последующих листов.

        2. Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        StyleID2 = iObject.StyleID2       # Получить свойство (* )
        StyleID2 = iObject.GetStyleID2()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def Variant(self) -> int:  # ispecificationstyle_variant.html
        """
        ## Variant - Вариант оформления спецификации

        Тип данных: из перечисления ksSpecificationVariantEnum

        1. Позволяет получить способ представления информации об исполнениях изделия.

        2. Доступны только варианты "Простая", "Вариант А" или "Вариант Б."

        3. Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        Variant = iObject.Variant       # Получить свойство (* )
        Variant = iObject.GetVariant()  # Получить свойство (**)
        ```
        """
        ...




class ISpecificationSubsection(IKompasAPIObject):  # ispecificationsubsection.html
    """
    ## Интерфейс ISpecificationSubsection

    Интерфейс подраздела спецификации.

    Данный интерфейс позволяет получить доступ к подразделу спецификации. Группы объектов внутри разделов называются подразделами. Подразделы, как и разделы, являются компонентом стиля спецификации. Их количество, названия и порядок внутри каждого раздела формируются при настройке стиля. Названия подразделов не отображаются в бланке спецификации. Они служат лишь для удобства выбора подраздела. Подразделы располагаются в разделе в порядке возрастания номеров.

    Данный интерфейс можно получить у коллекции ISpecificationSubsections.
    """

    @property
    def Name(self) -> str:  # ispecificationsubsection_name.html
        """
        ## Name - Имя подраздела

        Тип данных: BSTR (строка)

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        Name = iObject.Name       # Получить свойство (* )
        Name = iObject.GetName()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def Number(self) -> int:  # ispecificationsubsection_number.html
        """
        ## Number - Номер подраздела

        Тип данных: long.

        1. Данное свойство определяет уникальный номер подраздела в данном разделе спецификации.

        2. Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        Number = iObject.Number       # Получить свойство (* )
        Number = iObject.GetNumber()  # Получить свойство (**)
        ```
        """
        ...

    def Change(self, Name, Number) -> bool:  # ispecificationsubsection_change.html
        """
        ## Change - Изменить параметры подраздела

        ### Синтаксис Automation

        ```
        BOOL Change (BSTR Name, short Number);
        ```

        ### Входные параметры

        +------------------------------------+
        | Name   | - новое имя подраздела,   |
        +------------------------------------+
        | Number | - новый номер подраздела. |
        +------------------------------------+

        ### Возвращаемое значение

        +-----------------------------+
        | TRUE  | - в случае успеха,  |
        +-----------------------------+
        | FALSE | - в случае неудачи. |
        +-----------------------------+
        """
        ...

    def Delete(self) -> bool:  # ispecificationsubsection_delete.html
        """
        ## Delete - Удалить подраздел

        ### Синтаксис Automation

        ```
        BOOL Delete();
        ```

        ### Возвращаемое значение

        +-----------------------------+
        | TRUE  | - в случае успеха,  |
        +-----------------------------+
        | FALSE | - в случае неудачи. |
        +-----------------------------+
        """
        ...




class ISpecificationTuning(IKompasAPIObject):  # ispecificationtuning.html
    """
    ## Интерфейс ISpecificationTuning

    Интерфейс настроек спецификации.

    Данный интерфейс позволяет получить доступ к настройкам стиля спецификации ISpecificationStyle или настройкам описания спецификации ISpecificationDescription.

    Если настройки взяты у стиля спецификации, то они являются умолчательными для данного стиля и изменять их нельзя. В этом случае данный интерфейс отражает содержимое настроек стиля в библиотеке стилей на момент получения и в дальнейшем не отслеживает изменений в библиотеке стилей.

    Если настройки взяты у описания спецификации, то они отображают и отслеживают текущее состояние описания спецификации. Эти настройки можно изменять и они могут отличаться от умолчательных настроек стиля спецификации, по которому создано данное описание спецификации.

    После изменения настроек (для того чтобы эти изменения реально отобразились в документе) необходимо вызвать метод ISpecificationTuning::Update.

    Данный интерфейс можно получить от интерфейса описания спецификации документа ISpecificationDescription, используя свойство ISpecificationDescription::SpecificationTuning или от интерфейса стиля спецификации ISpecificationStyle с помощью свойства ISpecificationStyle::SpecificationTuning.
    """

    @property
    def AdditionalBlockTextStyleFirst(self) -> ITextStyle:  # ispecificationtuning_additionalblocktextstylefirst.html
        """
        ## AdditionalBlockTextStyleFirst - Стиль текста заголовка дополнительных блоков - первая строка

        Тип данных: указатель на интерфейс ITextStyle параметров стиля текста.

        1. Позволяет получить параметры шрифта для отображения первой строки в названиях дополнительных блоков разделов спецификации.

        2. Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        AdditionalBlockTextStyleFirst = iObject.AdditionalBlockTextStyleFirst       # Получить свойство (* )
        AdditionalBlockTextStyleFirst = iObject.GetAdditionalBlockTextStyleFirst()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def AdditionalBlockTextStyleNext(self) -> ITextStyle:  # ispecificationtuning_additionalblocktextstylenext.html
        """
        ## AdditionalBlockTextStyleNext - Стиль текста заголовка дополнительных блоков - последующие строки

        Тип данных: указатель на интерфейс ITextStyle параметров стиля текста.

        1. Позволяет получить параметры шрифта для отображения второй и последующих строк в многострочных названиях дополнительных блоков разделов спецификации.

        2. Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        AdditionalBlockTextStyleNext = iObject.AdditionalBlockTextStyleNext       # Получить свойство (* )
        AdditionalBlockTextStyleNext = iObject.GetAdditionalBlockTextStyleNext()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def AdditionalBlocks(self) -> IAdditionalBlockTunings:  # ispecificationtuning_additionalblocks.html
        """
        ## AdditionalBlocks - Блоки дополнительных разделов

        Тип данных: указатель на интерфейс IAdditionalBlockTunings коллекции настроек блоков дополнительных разделов спецификации

        1. Позволяет получить параметры настроек блоков дополнительных разделов спецификации.

        2. Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        AdditionalBlocks = iObject.AdditionalBlocks       # Получить свойство (* )
        AdditionalBlocks = iObject.GetAdditionalBlocks()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def BlockCount(self) -> int:  # ispecificationtuning_blockcount.html
        """
        ## BlockCount - Количество блоков

        Тип данных: long

        1. Позволяет получить количество блоков исполнений в спецификации. Имеет смысл только при настройке групповых спецификаций.

        2. Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        BlockCount = iObject.BlockCount       # Получить свойство (* )
        BlockCount = iObject.GetBlockCount()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def BlockOnNewPage(self) -> bool:  # ispecificationtuning_blockonnewpage.html
        """
        ## BlockOnNewPage - Располагать блок на новой странице

        Тип данных: BOOL

        Значения свойства:

        +-------------------------------------------------+
        | TRUE  | - располагать блок на новой странице,   |
        +-------------------------------------------------+
        | FALSE | - располагать блок сразу за предыдущим. |
        +-------------------------------------------------+

        Позволяет указать, требуется ли располагать начала всех блоков на новых листах спецификации.

        ### Синтаксис Automation

        ```
        BlockOnNewPage = iObject.BlockOnNewPage       # Получить свойство (* )
        iObject.BlockOnNewPage = BlockOnNewPage       # Установить свойство (* )
        BlockOnNewPage = iObject.GetBlockOnNewPage()  # Получить свойство (**)
        iObject.SetBlockOnNewPage (BlockOnNewPage)    # Установить свойство (**)
        ```
        """
        ...
    @BlockOnNewPage.setter
    def BlockOnNewPage(self, value: bool): ...

    @property
    def CalculatePosition(self) -> bool:  # ispecificationtuning_calculateposition.html
        """
        ## CalculatePosition - Режим расчета позиций

        Тип данных: BOOL

        Значения свойства:

        +---------------------------------------------------------------------------------+
        | TRUE  | - расчет позиций автоматически (после каждой сортировки) не происходит, |
        +---------------------------------------------------------------------------------+
        | FALSE | - расчет позиций происходит автоматически.                              |
        +---------------------------------------------------------------------------------+

        Позволяет включить или отключить расчет позиций (то есть присвоение объектам новых номеров позиций после того, как нумерация нарушилась в результате автоматической сортировки или удаления объектов).

        ### Синтаксис Automation

        ```
        CalculatePosition = iObject.CalculatePosition       # Получить свойство (* )
        iObject.CalculatePosition = CalculatePosition       # Установить свойство (* )
        CalculatePosition = iObject.GetCalculatePosition()  # Получить свойство (**)
        iObject.SetCalculatePosition (CalculatePosition)    # Установить свойство (**)
        ```
        """
        ...
    @CalculatePosition.setter
    def CalculatePosition(self, value: bool): ...

    @property
    def CalculateZone(self) -> bool:  # ispecificationtuning_calculatezone.html
        """
        ## CalculateZone - Режим расчета зон

        Тип данных: BOOL

        Значения свойства:

        +-----------------------------------------------------------------------------------------------------+
        | TRUE  | - при автоматической простановке номеров позиций происходит обновление номеров зон чертежа, |
        +-----------------------------------------------------------------------------------------------------+
        | FALSE | - зоны не обновляются.                                                                      |
        +-----------------------------------------------------------------------------------------------------+

        Позволяет включить или отключить передачу в спецификацию обозначений зон сборочного чертежа, в которых находятся позиционные линии-выноски объектов. Если передача разрешена, она происходит при синхронизации чертежа со спецификацией.

        ### Синтаксис Automation

        ```
        CalculateZone = iObject.CalculateZone       # Получить свойство (* )
        iObject.CalculateZone = CalculateZone       # Установить свойство (* )
        CalculateZone = iObject.GetCalculateZone()  # Получить свойство (**)
        iObject.SetCalculateZone (CalculateZone)    # Установить свойство (**)
        ```
        """
        ...
    @CalculateZone.setter
    def CalculateZone(self, value: bool): ...

    @property
    def CopySpcObjectOnCopyGeometry(self) -> bool:  # ispecificationtuning_copyspcobjectoncopygeometry.html
        """
        ## CopySpcObjectOnCopyGeometry - Копировать объекты спецификации при копировании геометрии

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        CopySpcObjectOnCopyGeometry = Object.CopySpcObjectOnCopyGeometry       # Получить свойство (* )
        Object.CopySpcObjectOnCopyGeometry = CopySpcObjectOnCopyGeometry       # Установить свойство (* )
        CopySpcObjectOnCopyGeometry = Object.GetCopySpcObjectOnCopyGeometry()  # Получить свойство (**)
        Object.SetCopySpcObjectOnCopyGeometry( CopySpcObjectOnCopyGeometry )   # Установить свойство (**)
        ```
        """
        ...
    @CopySpcObjectOnCopyGeometry.setter
    def CopySpcObjectOnCopyGeometry(self, value: bool): ...

    @property
    def DeleteGeometry(self) -> bool:  # ispecificationtuning_deletegeometry.html
        """
        ## DeleteGeometry - Режим удаления геометрии при удалении объекта спецификации

        Тип данных: BOOL

        Значения свойства:

        +-----------------------------------------------------------------------------------------------------------------------------------------+
        | TRUE  | - в результате удаления объекта спецификации из чертежа должны удаляться входящие в состав этого объекта графические объекты,   |
        +-----------------------------------------------------------------------------------------------------------------------------------------+
        | FALSE | - в результате удаления объекта спецификации из чертежа входящие в состав этого объекта графические объекты удаляться не будут. |
        +-----------------------------------------------------------------------------------------------------------------------------------------+

        Управляет удалением графических объектов, входящих в состав удаляемых объектов спецификации.

        ### Синтаксис Automation

        ```
        DeleteGeometry = iObject.DeleteGeometry       # Получить свойство (* )
        iObject.DeleteGeometry = DeleteGeometry       # Установить свойство (* )
        DeleteGeometry = iObject.GetDeleteGeometry()  # Получить свойство (**)
        iObject.SetDeleteGeometry (DeleteGeometry)    # Установить свойство (**)
        ```
        """
        ...
    @DeleteGeometry.setter
    def DeleteGeometry(self, value: bool): ...

    @property
    def DeleteSpcObjectOnDeleteGeometry(self) -> bool:  # ispecificationtuning_deletespcobjectondeletegeometry.html
        """
        ## DeleteSpcObjectOnDeleteGeometry - Режим удаления спецификации при удалении геометрии

        Тип данных: BOOL

        Значения свойства:

        +--------------------------------------------------------------------------------------------------------------------------+
        | TRUE  | - в результате удаления графического объекта из чертежа должны удаляться соответствующие объекты спецификации,   |
        +--------------------------------------------------------------------------------------------------------------------------+
        | FALSE | - в результате удаления графического объекта из чертежа соответствующие объекты спецификации удаляться не будут. |
        +--------------------------------------------------------------------------------------------------------------------------+

        Управляет удалением объектов спецификации при удалении графических объектов.

        ### Синтаксис Automation

        ```
        DeleteSpcObjectOnDeleteGeometry = iObject.DeleteSpcObjectOnDeleteGeometry       # Получить свойство (* )
        iObject.DeleteSpcObjectOnDeleteGeometry = DeleteSpcObjectOnDeleteGeometry       # Установить свойство (* )
        DeleteSpcObjectOnDeleteGeometry = iObject.GetDeleteSpcObjectOnDeleteGeometry()  # Получить свойство (**)
        iObject.SetDeleteSpcObjectOnDeleteGeometry (DeleteSpcObjectOnDeleteGeometry)    # Установить свойство (**)
        ```
        """
        ...
    @DeleteSpcObjectOnDeleteGeometry.setter
    def DeleteSpcObjectOnDeleteGeometry(self, value: bool): ...

    @property
    def DisableAdditionalBlockEmptyStrings(self) -> bool:  # ispecificationtuning_disableadditionalblockemptystrings.html
        """
        ## DisableAdditionalBlockEmptyStrings - Запретить вывод пустых строк вокруг начала дополнительного блока

        Тип данных: BOOL

        Значения свойства:

        +--------------------------------------------------------------------------------------------------------+
        | TRUE  | - не показывать пустые строки, обрамляющие название начала дополнительных блоков спецификации, |
        +--------------------------------------------------------------------------------------------------------+
        | FALSE | - показывать пустые строки, обрамляющие название начала дополнительных блоков спецификации.    |
        +--------------------------------------------------------------------------------------------------------+

        Позволяет включать и отключать показ пустых строк, обрамляющих название начала дополнительных блоков спецификации.

        ### Синтаксис Automation

        ```
        DisableAdditionalBlockEmptyStrings = iObject.DisableAdditionalBlockEmptyStrings       # Получить свойство (* )
        iObject.DisableAdditionalBlockEmptyStrings = DisableAdditionalBlockEmptyStrings       # Установить свойство (* )
        DisableAdditionalBlockEmptyStrings = iObject.GetDisableAdditionalBlockEmptyStrings()  # Получить свойство (**)
        iObject.SetDisableAdditionalBlockEmptyStrings (DisableAdditionalBlockEmptyStrings)    # Установить свойство (**)
        ```
        """
        ...
    @DisableAdditionalBlockEmptyStrings.setter
    def DisableAdditionalBlockEmptyStrings(self, value: bool): ...

    @property
    def DisableEmptyBlockString(self) -> bool:  # ispecificationtuning_disableemptyblockstring.html
        """
        ## DisableEmptyBlockString - Режим вывода пустых строк вокруг начала блока

        Тип данных: BOOL

        Значения свойства:

        +-----------------------------------------------------------------------------------------+
        | TRUE  | - не показывать пустые строки, обрамляющие название начала блоков спецификации, |
        +-----------------------------------------------------------------------------------------+
        | FALSE | - показывать пустые строки, обрамляющие название начала блоков спецификации.    |
        +-----------------------------------------------------------------------------------------+

        Позволяет включать и отключать показ пустых строк, обрамляющих название начала блоков спецификации.

        ### Синтаксис Automation

        ```
        DisableEmptyBlockString = iObject.DisableEmptyBlockString       # Получить свойство (* )
        iObject.DisableEmptyBlockString = DisableEmptyBlockString       # Установить свойство (* )
        DisableEmptyBlockString = iObject.GetDisableEmptyBlockString()  # Получить свойство (**)
        iObject.SetDisableEmptyBlockString (DisableEmptyBlockString)    # Установить свойство (**)
        ```
        """
        ...
    @DisableEmptyBlockString.setter
    def DisableEmptyBlockString(self, value: bool): ...

    @property
    def DisableEmptyString(self) -> bool:  # ispecificationtuning_disableemptystring.html
        """
        ## DisableEmptyString - Режим вывода пустых строк вокруг заголовка раздела

        Тип данных: BOOL

        Значения свойства:

        +------------------------------------------------------------------------------------+
        | TRUE  | - не показывать пустые строки, обрамляющие название разделов спецификации, |
        +------------------------------------------------------------------------------------+
        | FALSE | - показывать пустые строки, обрамляющие название разделов спецификации.    |
        +------------------------------------------------------------------------------------+

        Позволяет включать и отключать показ пустых строк, обрамляющих название разделов спецификации.

        ### Синтаксис Automation

        ```
        DisableEmptyString = iObject.DisableEmptyString       # Получить свойство (* )
        iObject.DisableEmptyString = DisableEmptyString       # Установить свойство (* )
        DisableEmptyString = iObject.GetDisableEmptyString()  # Получить свойство (**)
        iObject.SetDisableEmptyString (DisableEmptyString)    # Установить свойство (**)
        ```
        """
        ...
    @DisableEmptyString.setter
    def DisableEmptyString(self, value: bool): ...

    @property
    def DisableNestingBlockEmptyStrings(self) -> bool:  # ispecificationtuning_disablenestingblockemptystrings.html
        """
        ## DisableNestingBlockEmptyStrings - Запретить вывод пустых строк вокруг начала вложенного блока

        Тип данных: BOOL

        Значения свойства:

        +---------------------------------------------------------------------------------------------------+
        | TRUE  | - не показывать пустые строки, обрамляющие название начала вложенных блоков спецификации, |
        +---------------------------------------------------------------------------------------------------+
        | FALSE | - показывать пустые строки, обрамляющие название начала вложенных блоков спецификации.    |
        +---------------------------------------------------------------------------------------------------+

        Позволяет включать и отключать показ пустых строк, обрамляющих название начала вложенных блоков спецификации.

        ### Синтаксис Automation

        ```
        DisableNestingBlockEmptyStrings = iObject.DisableNestingBlockEmptyStrings       # Получить свойство (* )
        iObject.DisableNestingBlockEmptyStrings = DisableNestingBlockEmptyStrings       # Установить свойство (* )
        DisableNestingBlockEmptyStrings = iObject.GetDisableNestingBlockEmptyStrings()  # Получить свойство (**)
        iObject.SetDisableNestingBlockEmptyStrings (DisableNestingBlockEmptyStrings)    # Установить свойство (**)
        ```
        """
        ...
    @DisableNestingBlockEmptyStrings.setter
    def DisableNestingBlockEmptyStrings(self, value: bool): ...

    @property
    def DrawBottomUp(self) -> bool:  # ispecificationtuning_drawbottomup.html
        """
        ## DrawBottomUp - Строить снизу вверх

        Тип данных: BOOL

        Значения свойства:

        +--------------------------------+
        | TRUE  | - строить снизу вверх, |
        +--------------------------------+
        | FALSE | - строить сверху вниз. |
        +--------------------------------+

        Позволяет управлять порядком следования разделов и объектов в них. Если значение свойства - TRUE, разделы располагаются в порядке, обратном указанному в стиле спецификации, и порядок сортировки объектов также становится «обратным». Такой порядок расположения предписывается некоторыми СТП при заполнении спецификации, размещаемой на чертеже.

        ### Синтаксис Automation

        ```
        DrawBottomUp = iObject.DrawBottomUp       # Получить свойство (* )
        iObject.DrawBottomUp = DrawBottomUp       # Установить свойство (* )
        DrawBottomUp = iObject.GetDrawBottomUp()  # Получить свойство (**)
        iObject.SetDrawBottomUp (DrawBottomUp)    # Установить свойство (**)
        ```
        """
        ...
    @DrawBottomUp.setter
    def DrawBottomUp(self, value: bool): ...

    @property
    def InitialPosition(self) -> int:  # ispecificationtuning_initialposition.html
        """
        ## InitialPosition - Начальная позиция

        Тип данных: long

        Позволяет получить или указать номер позиции, который должен иметь первый объект спецификации.

        ### Синтаксис Automation

        ```
        InitialPosition = iObject.InitialPosition       # Получить свойство (* )
        iObject.InitialPosition = InitialPosition       # Установить свойство (* )
        InitialPosition = iObject.GetInitialPosition()  # Получить свойство (**)
        iObject.SetInitialPosition (InitialPosition)    # Установить свойство (**)
        ```
        """
        ...
    @InitialPosition.setter
    def InitialPosition(self, value: int): ...

    @property
    def InsertDash(self) -> bool:  # ispecificationtuning_insertdash.html
        """
        ## InsertDash - Настройка начала блока: вставлять тире

        Тип данных: BOOL

        Значения свойства:

        +-------------------------------------------+
        | TRUE  | - вставлять тире перед числом,    |
        +-------------------------------------------+
        | FALSE | - не вставлять тире перед числом. |
        +-------------------------------------------+

        Позволяет настроить формат номеров исполнений специфицируемого изделия (номеров исполнений, располагающихся в «шапке» спецификации).

        Настройка номеров исполнений имеет смысл только для групповых спецификаций.

        ### Синтаксис Automation

        ```
        InsertDash = iObject.InsertDash       # Получить свойство (* )
        iObject.InsertDash = InsertDash       # Установить свойство (* )
        InsertDash = iObject.GetInsertDash()  # Получить свойство (**)
        iObject.SetInsertDash( InsertDash )   # Установить свойство (**)
        ```
        """
        ...
    @InsertDash.setter
    def InsertDash(self, value: bool): ...

    @property
    def InsertNull(self) -> bool:  # ispecificationtuning_insertnull.html
        """
        ## InsertNull - Настройка начала блока: вставлять нули перед числом

        Тип данных: BOOL

        Значения свойства:

        +-------------------------------------------+
        | TRUE  | - вставлять нули перед числом,    |
        +-------------------------------------------+
        | FALSE | - не вставлять нули перед числом. |
        +-------------------------------------------+

        Позволяет настроить формат номеров исполнений специфицируемого изделия (номеров исполнений, располагающихся в «шапке» спецификации).

        Настройка номеров исполнений имеет смысл только для групповых спецификаций.

        ### Синтаксис Automation

        ```
        InsertNull = iObject.InsertNull       # Получить свойство (* )
        iObject.InsertNull = InsertNull       # Установить свойство (* )
        InsertNull = iObject.GetInsertNull()  # Получить свойство (**)
        iObject.SetInsertNull (InsertNull)    # Установить свойство (**)
        ```
        """
        ...
    @InsertNull.setter
    def InsertNull(self, value: bool): ...

    @property
    def LinkType(self):  # ispecificationtuning_linktype.html
        """
        ## LinkType - Режим связки сборки или чертежа со спецификацией

        Тип данных: из перечисления ksSpecificationLinkTypeEnum

        Значения свойства:

        +--------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------+
        | slNone                  | - информация из подключенного сборочного чертежа не будет передаваться в спецификацию, однако чертеж будет по-прежнему подключен, и связь можно будет включить в любой момент,                                               |
        +--------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------+
        | slOnlyObjects           | - при сохранении подключенного чертежа, в который внесены изменения, касающиеся спецификации (созданы или отредактированы объекты спецификации), эти изменения будут переданы в спецификацию.                                |
        +--------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------+
        | slWithPositionCalculate | - после передачи изменений из чертежа в спецификацию в ней будет автоматически произведен расчет номеров позиций (в соответствии с последовательностью сортировки объектов), и новые номера позиций будут переданы в чертеж. |
        +--------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------+

        Позволяет включать и блокировать связь, подключенных друг к другу документов - спецификации и модели (сборочного чертежа), и устанавливать тип этой связи.

        ### Синтаксис Automation

        ```
        LinkType = iObject.LinkType       # Получить свойство (* )
        iObject.LinkType = LinkType       # Установить свойство (* )
        LinkType = iObject.GetLinkType()  # Получить свойство (**)
        iObject.SetLinkType (LinkType)    # Установить свойство (**)
        ```
        """
        ...
    @LinkType.setter
    def LinkType(self, value): ...

    @property
    def NestingBlockTextStyleFirst(self) -> ITextStyle:  # ispecificationtuning_nestingblocktextstylefirst.html
        """
        ## NestingBlockTextStyleFirst - Стиль текста заголовка вложенных блоков - первая строка

        Тип данных: указатель на интерфейс ITextStyle параметров стиля текста

        1. Позволяет получить параметры шрифта для отображения первой строки в названиях вложенных блоков разделов спецификации.

        2. Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        NestingBlockTextStyleFirst = iObject.NestingBlockTextStyleFirst       # Получить свойство (* )
        NestingBlockTextStyleFirst = iObject.GetNestingBlockTextStyleFirst()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def NestingBlockTextStyleNext(self) -> ITextStyle:  # ispecificationtuning_nestingblocktextstylenext.html
        """
        ## NestingBlockTextStyleNext - Стиль текста заголовка вложенных блоков - последующие строки

        Тип данных: указатель на интерфейс ITextStyle параметров стиля текста

        1. Позволяет получить параметры шрифта для отображения второй и последующих строк в многострочных названиях вложенных блоков разделов спецификации.

        2. Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        NestingBlockTextStyleNext = iObject.NestingBlockTextStyleNext       # Получить свойство (* )
        NestingBlockTextStyleNext = iObject.GetNestingBlockTextStyleNext()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def ObjectTextStyle(self) -> ITextStyle:  # ispecificationtuning_objecttextstyle.html
        """
        ## ObjectTextStyle - Стиль текста объекта спецификации

        Тип данных: указатель на интерфейс ITextStyle параметров стиля текста

        1. Позволяет получить параметры шрифта текстовой части объектов спецификации, отличающиеся от параметров шрифта в оформлении. Имеет смысл, если свойство UserTextStyle имеет значение TRUE.

        2. Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        ObjectTextStyle = iObject.ObjectTextStyle       # Получить свойство (* )
        ObjectTextStyle = iObject.GetObjectTextStyle()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def PerformanceBlockTextStyleFirst(self) -> ITextStyle:  # ispecificationtuning_performanceblocktextstylefirst.html
        """
        ## PerformanceBlockTextStyleFirst - Стиль текста заголовка блока исполнений - первая строка

        Тип данных: указатель на интерфейс ITextStyle параметров стиля текста

        1. Позволяет получить параметры шрифта для отображения первой строки в названиях блоков исполнений спецификации.

        2. Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        PerformanceBlockTextStyleFirst = iObject.PerformanceBlockTextStyleFirst       # Получить свойство (* )
        PerformanceBlockTextStyleFirst = iObject.GetPerformanceBlockTextStyleFirst()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def PerformanceBlockTextStyleNext(self) -> ITextStyle:  # ispecificationtuning_performanceblocktextstylenext.html
        """
        ## PerformanceBlockTextStyleNext - Стиль текста заголовка блока исполнений - последующие строки

        Тип данных: указатель на интерфейс ITextStyle параметров стиля текста

        1. Позволяет получить параметры шрифта для отображения второй и последующих строк в многострочных названиях блоков исполнений спецификации.

        2. Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        PerformanceBlockTextStyleNext = iObject.PerformanceBlockTextStyleNext       # Получить свойство (* )
        PerformanceBlockTextStyleNext = iObject.GetPerformanceBlockTextStyleNext()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def PerformanceCount(self) -> int:  # ispecificationtuning_performancecount.html
        """
        ## PerformanceCount - Количество исполнений

        Тип данных: long

        Позволяет указать количество исполнений специфицируемого изделия. Введенное число будет влиять на доступность ячеек "Количество", на исполнение и количество самих этих ячеек.Имеет смысл только при настройке групповых спецификаций.

        ### Синтаксис Automation

        ```
        PerformanceCount = iObject.PerformanceCount       # Получить свойство (* )
        iObject.PerformanceCount = PerformanceCount       # Установить свойство (* )
        PerformanceCount = iObject.GetPerformanceCount()  # Получить свойство (**)
        iObject.SetPerformanceCount (PerformanceCount)    # Установить свойство (**)
        ```
        """
        ...
    @PerformanceCount.setter
    def PerformanceCount(self, value: int): ...

    @property
    def PerformanceCountInBlock(self) -> int:  # ispecificationtuning_performancecountinblock.html
        """
        ## PerformanceCountInBlock - Количество исполнений в блоке

        Тип данных: long

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        PerformanceCountInBlock = Object.PerformanceCountInBlock( PVal )     # Получить свойство (* )
        PerformanceCountInBlock = Object.GetPerformanceCountInBlock( PVal )  # Получить свойство (**)
        ```
        """
        ...

    @property
    def PositionUp(self) -> bool:  # ispecificationtuning_positionup.html
        """
        ## PositionUp - Настройка исполнений объектов: позиции возрастают

        Тип данных: BOOL

        Значения свойства:

        +------------------------------------------------------+
        | TRUE  | - позиции объектов-исполнений возрастают,    |
        +------------------------------------------------------+
        | FALSE | - позиции объектов-исполнений не возрастают. |
        +------------------------------------------------------+

        Позволяет указать, требуется ли в спецификации отображать возрастающие номера объектов, являющихся исполнениями.

        ### Синтаксис Automation

        ```
        PositionUp = iObject.PositionUp       # Получить свойство (* )
        iObject.PositionUp = PositionUp       # Установить свойство (* )
        PositionUp = iObject.GetPositionUp()  # Получить свойство (**)
        iObject.SetPositionUp (PositionUp)    # Установить свойство (**)
        ```
        """
        ...
    @PositionUp.setter
    def PositionUp(self, value: bool): ...

    @property
    def PredefinedTextFileName(self) -> str:  # ispecificationtuning_predefinedtextfilename.html
        """
        ## PredefinedTextFileName - Имя файла для вставки текстовых шаблонов

        Тип данных: BSTR (строка)

        Позволяет получить\\изменить полное имя файла текстовых шаблонов (*.tdp), тексты из которого требуется вставлять при вводе и редактировании текстовой части объектов спецификации.

        ### Синтаксис Automation

        ```
        PredefinedTextFileName = iObject.PredefinedTextFileName       # Получить свойство (* )
        iObject.PredefinedTextFileName = PredefinedTextFileName       # Установить свойство (* )
        PredefinedTextFileName = iObject.GetPredefinedTextFileName()  # Получить свойство (**)
        iObject.SetPredefinedTextFileName (PredefinedTextFileName)    # Установить свойство (**)
        ```
        """
        ...
    @PredefinedTextFileName.setter
    def PredefinedTextFileName(self, value: str): ...

    @property
    def SectionTextStyleFirst(self) -> ITextStyle:  # ispecificationtuning_sectiontextstylefirst.html
        """
        ## SectionTextStyleFirst - Стиль текста заголовка раздела - первая строка

        Тип данных: указатель на интерфейс ITextStyle параметров стиля текста

        1. Позволяет получить параметры шрифта для отображения первой строки в названиях разделов спецификации.

        2. Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        SectionTextStyleFirst = iObject.SectionTextStyleFirst       # Получить свойство (* )
        SectionTextStyleFirst = iObject.GetSectionTextStyleFirst()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def SectionTextStyleNext(self) -> ITextStyle:  # ispecificationtuning_sectiontextstylenext.html
        """
        ## SectionTextStyleNext - Стиль текста заголовка раздела - последующие строки

        Тип данных: указатель на интерфейс ITextStyle параметров стиля текста

        1. Позволяет получить параметры шрифта для отображения второй и последующих строк в многострочных названиях разделов спецификации.

        2. Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        SectionTextStyleNext = iObject.SectionTextStyleNext       # Получить свойство (* )
        SectionTextStyleNext = iObject.GetSectionTextStyleNext()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def Sections(self) -> ISpecificationTuningSections:  # ispecificationtuning_sections.html
        """
        ## Sections - Настройки разделов

        Тип данных: указатель на интерфейс ISpecificationTuningSections коллекции настроек разделов спецификации

        1. Позволяет получить параметры настроек разделов спецификации.

        2. Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        Sections = iObject.Sections       # Получить свойство (* )
        Sections = iObject.GetSections()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def ShowAdditionalBlockName(self) -> bool:  # ispecificationtuning_showadditionalblockname.html
        """
        ## ShowAdditionalBlockName - Показывать имя дополнительного блока

        Тип данных: BOOL

        Значения свойства:

        +-------------------------------------------------------------------------------+
        | TRUE  | - имена дополнительных блоков показываются в таблице спецификации,    |
        +-------------------------------------------------------------------------------+
        | FALSE | - имена дополнительных блоков не показываются в таблице спецификации. |
        +-------------------------------------------------------------------------------+

        Позволяет включать и отключать показ в таблице спецификации заголовков блоков дополнительных разделов и пустых строк вокруг них.

        ### Синтаксис Automation

        ```
        ShowAdditionalBlockName = iObject.ShowAdditionalBlockName       # Получить свойство (* )
        iObject.ShowAdditionalBlockName = ShowAdditionalBlockName       # Установить свойство (* )
        ShowAdditionalBlockName = iObject.GetShowAdditionalBlockName()  # Получить свойство (**)
        iObject.SetShowAdditionalBlockName (ShowAdditionalBlockName)    # Установить свойство (**)
        ```
        """
        ...
    @ShowAdditionalBlockName.setter
    def ShowAdditionalBlockName(self, value: bool): ...

    @property
    def ShowEmbodimentWithoutVariables(self) -> bool:  # ispecificationtuning_showembodimentwithoutvariables.html
        """
        ## ShowEmbodimentWithoutVariables - Показывать исполнения, не содержащие переменных данных

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        ShowEmbodimentWithoutVariables = Object.ShowEmbodimentWithoutVariables       # Получить свойство (* )
        Object.ShowEmbodimentWithoutVariables = ShowEmbodimentWithoutVariables       # Установить свойство (* )
        ShowEmbodimentWithoutVariables = Object.GetShowEmbodimentWithoutVariables()  # Получить свойство (**)
        Object.SetShowEmbodimentWithoutVariables( ShowEmbodimentWithoutVariables )   # Установить свойство (**)
        ```
        """
        ...
    @ShowEmbodimentWithoutVariables.setter
    def ShowEmbodimentWithoutVariables(self, value: bool): ...

    @property
    def ShowInfoByObjects(self) -> bool:  # ispecificationtuning_showinfobyobjects.html
        """
        ## ShowInfoByObjects - Выдавать информацию по объектам (для исполнений более 10)

        Тип данных: BOOL

        Значения свойства:

        +--------------------------------------------+
        | TRUE  | - выдавать информацию по объектам, |
        +--------------------------------------------+
        | FALSE | - выдавать информацию блоками.     |
        +--------------------------------------------+

        Позволяет выбрать способ отображения информации.

        Свойство имеет смысл, если число исполнений, введенное в общих настройках спецификации, больше, чем количество колонок бланка спецификации, предназначенное для ввода количества на исполнение, становятся доступны выбора способа отображения информации.

        ### Синтаксис Automation

        ```
        ShowInfoByObjects = iObject.ShowInfoByObjects       # Получить свойство (* )
        iObject.ShowInfoByObjects = ShowInfoByObjects       # Установить свойство (* )
        ShowInfoByObjects = iObject.GetShowInfoByObjects()  # Получить свойство (**)
        iObject.SetShowInfoByObjects (ShowInfoByObjects)    # Установить свойство (**)
        ```
        """
        ...
    @ShowInfoByObjects.setter
    def ShowInfoByObjects(self, value: bool): ...

    @property
    def ShowListCountIsSameFormat(self) -> bool:  # ispecificationtuning_showlistcountissameformat.html
        """
        ## ShowListCountIsSameFormat - Отображать количество листов одинакового формата

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        ShowListCountIsSameFormat = Object.ShowListCountIsSameFormat       # Получить свойство (* )
        Object.ShowListCountIsSameFormat = ShowListCountIsSameFormat       # Установить свойство (* )
        ShowListCountIsSameFormat = Object.GetShowListCountIsSameFormat()  # Получить свойство (**)
        Object.SetShowListCountIsSameFormat( ShowListCountIsSameFormat )   # Установить свойство (**)
        ```
        """
        ...
    @ShowListCountIsSameFormat.setter
    def ShowListCountIsSameFormat(self, value: bool): ...

    @property
    def ShowNestingBlockName(self) -> bool:  # ispecificationtuning_shownestingblockname.html
        """
        ## ShowNestingBlockName - Показывать имя вложенного блока

        Тип данных: BOOL

        Значения свойства:

        +--------------------------------------------------------------------------+
        | TRUE  | - имена вложенных блоков показываются в таблице спецификации,    |
        +--------------------------------------------------------------------------+
        | FALSE | - имена вложенных блоков не показываются в таблице спецификации. |
        +--------------------------------------------------------------------------+

        Позволяет включать и отключать показ в таблице спецификации заголовков блоков вложенных разделов и пустых строк вокруг них.

        ### Синтаксис Automation

        ```
        ShowNestingBlockName = iObject.ShowNestingBlockName       # Получить свойство (* )
        iObject.ShowNestingBlockName = ShowNestingBlockName       # Установить свойство (* )
        ShowNestingBlockName = iObject.GetShowNestingBlockName()  # Получить свойство (**)
        iObject.SetShowNestingBlockName (ShowNestingBlockName)    # Установить свойство (**)
        ```
        """
        ...
    @ShowNestingBlockName.setter
    def ShowNestingBlockName(self, value: bool): ...

    @property
    def ShowPerformanceBlockName(self) -> bool:  # ispecificationtuning_showperformanceblockname.html
        """
        ## ShowPerformanceBlockName - Показывать имя блока исполнений

        Тип данных: BOOL

        Значения свойства:

        +---------------------------------------------------------------------------+
        | TRUE  | - имена блоков исполнений показываются в таблице спецификации,    |
        +---------------------------------------------------------------------------+
        | FALSE | - имена блоков исполнений не показываются в таблице спецификации. |
        +---------------------------------------------------------------------------+

        Позволяет включать и отключать показ в таблице спецификации заголовков блоков исполнений и пустых строк вокруг них.

        ### Синтаксис Automation

        ```
        ShowPerformanceBlockName = iObject.ShowPerformanceBlockName       # Получить свойство (* )
        iObject.ShowPerformanceBlockName = ShowPerformanceBlockName       # Установить свойство (* )
        ShowPerformanceBlockName = iObject.GetShowPerformanceBlockName()  # Получить свойство (**)
        iObject.SetShowPerformanceBlockName (ShowPerformanceBlockName)    # Установить свойство (**)
        ```
        """
        ...
    @ShowPerformanceBlockName.setter
    def ShowPerformanceBlockName(self, value: bool): ...

    @property
    def ShowPerformanceFull(self) -> bool:  # ispecificationtuning_showperformancefull.html
        """
        ## ShowPerformanceFull - Обозначение исполнения показывать полностью

        Тип данных: BOOL

        Значения свойства:

        +--------------------------------------------------------+
        | TRUE  | - обозначение исполнения показывать полностью, |
        +--------------------------------------------------------+
        | FALSE | - показывать только номер.                     |
        +--------------------------------------------------------+

        Позволяет определить вариант отображения обозначений объектов-исполнений.

        ### Синтаксис Automation

        ```
        ShowPerformanceFull = iObject.ShowPerformanceFull       # Получить свойство (* )
        iObject.ShowPerformanceFull = ShowPerformanceFull       # Установить свойство (* )
        ShowPerformanceFull = iObject.GetShowPerformanceFull()  # Получить свойство (**)
        iObject.SetShowPerformanceFull (ShowPerformanceFull)    # Установить свойство (**)
        ```
        """
        ...
    @ShowPerformanceFull.setter
    def ShowPerformanceFull(self, value: bool): ...

    @property
    def ShowSectionName(self) -> bool:  # ispecificationtuning_showsectionname.html
        """
        ## ShowSectionName - Режим отображения заголовков разделов

        Тип данных: BOOL

        Значения свойства:

        +----------------------------------------------------------------------+
        | TRUE  | - заголовки разделов показываются в таблице спецификации,    |
        +----------------------------------------------------------------------+
        | FALSE | - заголовки разделов не показываются в таблице спецификации. |
        +----------------------------------------------------------------------+

        Позволяет включить или отключить показ в таблице спецификации заголовков разделов и пустых строк. Если значение свойства - FALSE, объекты показываются в таблице спецификации непрерывным списком. При этом порядок их сортировки сохраняется (в том числе учитывается принадлежность объектов к разным разделам). На отображение резервных строк значение этого свойства не влияет.

        ### Синтаксис Automation

        ```
        ShowSectionName = iObject.ShowSectionName       # Получить свойство (* )
        iObject.ShowSectionName = ShowSectionName       # Установить свойство (* )
        ShowSectionName = iObject.GetShowSectionName()  # Получить свойство (**)
        iObject.SetShowSectionName (ShowSectionName)    # Установить свойство (**)
        ```
        """
        ...
    @ShowSectionName.setter
    def ShowSectionName(self, value: bool): ...

    @property
    def SupportPerformance(self) -> bool:  # ispecificationtuning_supportperformance.html
        """
        ## SupportPerformance - Поддержка исполнений в спецификации

        Тип данных: BOOL

        Значения свойства:

        +--------------------------------------------------------+
        | TRUE  | - исполнения поддерживаются в спецификации,    |
        +--------------------------------------------------------+
        | FALSE | - исполнения не поддерживаются в спецификации. |
        +--------------------------------------------------------+

        Позволяет определить требуется ли в спецификации создавать объекты-исполнения.

        ### Синтаксис Automation

        ```
        SupportPerformance = iObject.SupportPerformance       # Получить свойство (* )
        iObject.SupportPerformance = SupportPerformance       # Установить свойство (* )
        SupportPerformance = iObject.GetSupportPerformance()  # Получить свойство (**)
        iObject.SetSupportPerformance (SupportPerformance)    # Установить свойство (**)
        ```
        """
        ...
    @SupportPerformance.setter
    def SupportPerformance(self, value: bool): ...

    @property
    def UseAdditionalBlocks(self) -> bool:  # ispecificationtuning_useadditionalblocks.html
        """
        ## UseAdditionalBlocks - Использовать блоки дополнительных разделов

        Тип данных: BOOL

        Значения свойства:

        +------------------------------------------------------------------+
        | TRUE  | - использовать блоки дополнительных разделов,            |
        +------------------------------------------------------------------+
        | FALSE | - блоки дополнительных разделов использоваться не будут. |
        +------------------------------------------------------------------+

        Позволяет получить\\изменить возможность использования блоков дополнительных разделов. Если значение свойства - TRUE, при создании объектов в спецификации доступны блоки дополнительных разделов.

        ### Синтаксис Automation

        ```
        UseAdditionalBlocks = iObject.UseAdditionalBlocks       # Получить свойство (* )
        iObject.UseAdditionalBlocks = UseAdditionalBlocks       # Установить свойство (* )
        UseAdditionalBlocks = iObject.GetUseAdditionalBlocks()  # Получить свойство (**)
        iObject.SetUseAdditionalBlocks (UseAdditionalBlocks)    # Установить свойство (**)
        ```
        """
        ...
    @UseAdditionalBlocks.setter
    def UseAdditionalBlocks(self, value: bool): ...

    @property
    def UserTextStyle(self) -> bool:  # ispecificationtuning_usertextstyle.html
        """
        ## UserTextStyle - Стиль текста объектов спецификации пользовательский

        Тип данных: BOOL

        Значения свойства:

        +---------------------------------------------------------------------------------------------------------------------------------+
        | TRUE  | - шрифт текстовой части объектов спецификации задается свойством ObjectTextStyle,                                       |
        +---------------------------------------------------------------------------------------------------------------------------------+
        | FALSE | - шрифт текстовой части объектов спецификации будет таким, каким он был установлен по умолчанию в таблице спецификации. |
        +---------------------------------------------------------------------------------------------------------------------------------+

        Позволяет определить, какой шрифт будет использоваться для отображения текстовой части объектов спецификации.

        ### Синтаксис Automation

        ```
        UserTextStyle = iObject.UserTextStyle       # Получить свойство (* )
        iObject.UserTextStyle = UserTextStyle       # Установить свойство (* )
        UserTextStyle = iObject.GetUserTextStyle()  # Получить свойство (**)
        iObject.SetUserTextStyle (UserTextStyle)    # Установить свойство (**)
        ```
        """
        ...
    @UserTextStyle.setter
    def UserTextStyle(self, value: bool): ...

    def Update(self) -> bool:  # ispecificationtuning_update.html
        """
        ## Update - Обновить описание спецификации в соответствии с внесенными в настройки изменениями

        Все изменения, сделанные в интерфейсах настроек спецификации, вступят в силу только после вызова этого метода.

        ### Синтаксис Automation

        ```
        BOOL Update();
        ```

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - обновление прошло успешно,     |
        +------------------------------------------+
        | FALSE | - обновить настройки не удалось. |
        +------------------------------------------+
        """
        ...




class ISpecificationTuningSection(IKompasAPIObject):  # ispecificationtuningsection.html
    """
    ## Интерфейс ISpecificationTuningSection

    Настройка раздела спецификации.

    Данный интерфейс позволяет получить доступ к настройкам раздела спецификации. Если настройки взяты у стиля спецификации, то они являются умолчательными для данного стиля и изменять их нельзя. В этом случае данный интерфейс отражает содержимое настроек стиля в библиотеке стилей на момент получения и в дальнейшем не отслеживает изменений в библиотеке стилей.

    Если настройки взяты у описания спецификации, то они отображают и отслеживают текущее состояние описания спецификации. Эти настройки можно изменять и они могут отличаться от умолчательных настроек стиля спецификации, по которому создано данное описание спецификации.

    После изменения настроек для того, чтобы эти изменения реально отобразились в документе, необходимо вызвать метод ISpecificationTuning::Update.

    Данный интерфейс можно получить у коллекции ISpecificationTuningSections.
    """

    @property
    def AdditionalBlocks(self) -> IAdditionalBlockTunings:  # ispecificationtuningsection_additionalblocks.html
        """
        ## AdditionalBlocks - Блоки вложенных разделов

        Тип данных: указатель на интерфейс IAdditionalBlockTunings коллекции настроек блоков вложенных разделов спецификации

        1. Позволяет получить параметры настроек блоков вложенных разделов спецификации.

        2. Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        AdditionalBlocks = iObject.AdditionalBlocks       # Получить свойство (* )
        AdditionalBlocks = iObject.GetAdditionalBlocks()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def AttachGeometry(self) -> bool:  # ispecificationtuningsection_attachgeometry.html
        """
        ## AttachGeometry - Подключать геометрию

        Тип данных: BOOL

        Значения свойства:

        +-----------------------------------------------------------------------------------------------------------+
        | TRUE  | - объектам раздела можно поставить в соответствие геометрические объекты графического документа,  |
        +-----------------------------------------------------------------------------------------------------------+
        | FALSE | - объектам раздела нельзя поставить в соответствие геометрические объекты графического документа. |
        +-----------------------------------------------------------------------------------------------------------+

        Позволяет включать и отключать подключение геометрии к объектам раздела.

        ### Синтаксис Automation

        ```
        AttachGeometry = iObject.AttachGeometry       # Получить свойство (* )
        iObject.AttachGeometry = AttachGeometry       # Установить свойство (* )
        AttachGeometry = iObject.GetAttachGeometry()  # Получить свойство (**)
        iObject.SetAttachGeometry (AttachGeometry)    # Установить свойство (**)
        ```
        """
        ...
    @AttachGeometry.setter
    def AttachGeometry(self, value: bool): ...

    @property
    def FirstOnSheet(self) -> bool:  # ispecificationtuningsection_firstonsheet.html
        """
        ## FirstOnSheet - Размещать на новом листе

        Тип данных: BOOL

        Значения свойства:

        +---------------------------------------------------+
        | TRUE  | - располагать раздел на новой странице,   |
        +---------------------------------------------------+
        | FALSE | - располагать раздел сразу за предыдущим. |
        +---------------------------------------------------+

        Позволяет включать и отключать размещение раздела на новом листе спецификации.

        ### Синтаксис Automation

        ```
        FirstOnSheet = iObject.FirstOnSheet       # Получить свойство (* )
        FirstOnSheet = iObject.FirstOnSheet       # Установить свойство (* )
        FirstOnSheet = iObject.GetFirstOnSheet()  # Получить свойство (**)
        iObject.SetFirstOnSheet (FirstOnSheet)    # Установить свойство (**)
        ```
        """
        ...
    @FirstOnSheet.setter
    def FirstOnSheet(self, value: bool): ...

    @property
    def IndependentPosition(self) -> bool:  # ispecificationtuningsection_independentposition.html
        """
        ## IndependentPosition - Независимая нумерация позиций

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        IndependentPosition = Object.IndependentPosition       # Получить свойство (* )
        Object.IndependentPosition = IndependentPosition       # Установить свойство (* )
        IndependentPosition = Object.GetIndependentPosition()  # Получить свойство (**)
        Object.SetIndependentPosition( IndependentPosition )   # Установить свойство (**)
        ```
        """
        ...
    @IndependentPosition.setter
    def IndependentPosition(self, value: bool): ...

    @property
    def Mark(self) -> str:  # ispecificationtuningsection_mark.html
        """
        ## Mark - Марка. Текст префикса

        Тип данных: BSTR

        ### Синтаксис Automation

        ```
        Mark = Object.Mark       # Получить свойство (* )
        Object.Mark = Mark       # Установить свойство (* )
        Mark = Object.GetMark()  # Получить свойство (**)
        Object.SetMark( Mark )   # Установить свойство (**)
        ```
        """
        ...
    @Mark.setter
    def Mark(self, value: str): ...

    @property
    def MarkOn(self) -> bool:  # ispecificationtuningsection_markon.html
        """
        ## MarkOn - Марка. Включена простановка позиций c префиксом

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        MarkOn = Object.MarkOn       # Получить свойство (* )
        Object.MarkOn = MarkOn       # Установить свойство (* )
        MarkOn = Object.GetMarkOn()  # Получить свойство (**)
        Object.SetMarkOn( MarkOn )   # Установить свойство (**)
        ```
        """
        ...
    @MarkOn.setter
    def MarkOn(self, value: bool): ...

    @property
    def Number(self) -> int:  # ispecificationtuningsection_number.html
        """
        ## Number - Номер раздела

        Тип данных: long

        1. Данное свойство определяет уникальный номер раздела в данном стиле спецификации.

        2. Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        Number = iObject.Number       # Получить свойство (* )
        Number = iObject.GetNumber()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def PutPosition(self) -> bool:  # ispecificationtuningsection_putposition.html
        """
        ## PutPosition - Проставлять позиции

        Тип данных: BOOL

        Значения свойства:

        +-----------------------------------------------------------------------+
        | TRUE  | - в разделе возможен автоматический расчет номеров позиций,   |
        +-----------------------------------------------------------------------+
        | FALSE | - в разделе невозможен автоматический расчет номеров позиций. |
        +-----------------------------------------------------------------------+

        Позволяет включать и отключать простановку позиций в разделе. Следует понимать, что отключение простановки позиций отличается от запрета ввода данных в колонку "Позиция". Если в разделе включена простановка позиций, но запрещен ввод данных в колонку "Позиция", то объектам этого раздела при автоматической простановке позиций будут присвоены номера позиций. И хотя эти номера не будут видны в таблице (из-за запрета на ввод данных в колонку "Позиция"), нумерация объектов в следующем разделе будет производиться с их учетом.

        Если в разделе отключена простановка позиций и разрешен ввод данных в колонку "Позиция", то после автоматической простановки позиций номера позиций в этом разделе не изменятся, а номера позиций в следующем разделе не будут учитывать введенные вручную номера позиций в данном разделе. Обычно запрет ввода данных в колонку "Позиция" какого-либо раздела сочетают с отключением простановки позиций в этом разделе (пример такого раздела — Документация). И наоборот, если данные в колонку "Позиция" раздела вводить можно, то в этом разделе включают автоматическую простановку позиций.

        ### Синтаксис Automation

        ```
        PutPosition = iObject.PutPosition       # Получить свойство (* )
        iObject.PutPosition = PutPosition       # Установить свойство (* )
        PutPosition = iObject.GetPutPosition()  # Получить свойство (**)
        iObject.SetPutPosition (PutPosition)    # Установить свойство (**)
        ```
        """
        ...
    @PutPosition.setter
    def PutPosition(self, value: bool): ...

    @property
    def ReserveStringCount(self) -> int:  # ispecificationtuningsection_reservestringcount.html
        """
        ## ReserveStringCount - Число резервных строк и позиций

        Тип данных: long

        Позволяет получить или указать количество резервных строк в разделе.

        ### Синтаксис Automation

        ```
        ReserveStringCount = iObject.ReserveStringCount       # Получить свойство (* )
        iObject.ReserveStringCount = ReserveStringCount       # Установить свойство (* )
        ReserveStringCount = iObject.GetReserveStringCount()  # Получить свойство (**)
        iObject.SetReserveStringCount (ReserveStringCount)    # Установить свойство (**)
        ```
        """
        ...
    @ReserveStringCount.setter
    def ReserveStringCount(self, value: int): ...

    @property
    def ShowDocumentCode(self) -> bool:  # ispecificationtuningsection_showdocumentcode.html
        """
        ## ShowDocumentCode - Показывать код документа

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        ShowDocumentCode = Object.ShowDocumentCode           # Получить свойство (* )
        Object.ShowDocumentCode = ShowDocumentCode           # Установить свойство (* )
        ShowShowDocumentCode = Object.GetShowDocumentCode()  # Получить свойство (**)
        Object.SetShowDocumentCode( ShowDocumentCode )       # Установить свойство (**)
        ```
        """
        ...
    @ShowDocumentCode.setter
    def ShowDocumentCode(self, value: bool): ...

    @property
    def SortObjects(self) -> bool:  # ispecificationtuningsection_sortobjects.html
        """
        ## SortObjects - Сортировать объекты

        Тип данных: BOOL

        Значения свойства:

        +---------------------------------------------------------------------------------------------------+
        | TRUE  | - сортировка объектов внутри раздела производится по предусмотренным стандартом правилам, |
        +---------------------------------------------------------------------------------------------------+
        | FALSE | - в разделе сортировка объектов не производится.                                          |
        +---------------------------------------------------------------------------------------------------+

        Позволяет включать и отключать автоматическую сортировку объектов в разделе. Если сортировка выключена, объекты будут добавляться в конец раздела и их можно будет перемещать вверх или вниз.

        ### Синтаксис Automation

        ```
        SortObjects = iObject.SortObjects       # Получить свойство (* )
        iObject.SortObjects = SortObjects       # Установить свойство (* )
        SortObjects = iObject.GetSortObjects()  # Получить свойство (**)
        iObject.SetSortObjects (SortObjects)    # Установить свойство (**)
        ```
        """
        ...
    @SortObjects.setter
    def SortObjects(self, value: bool): ...

    @property
    def SubsectionOn(self) -> bool:  # ispecificationtuningsection_subsectionon.html
        """
        ## SubsectionOn - Деление на подразделы включено

        Тип данных: BOOL

        Значения свойства:

        +--------------------------------------------+
        | TRUE  | - деление на подразделы включено,  |
        +--------------------------------------------+
        | FALSE | - деление на подразделы выключено. |
        +--------------------------------------------+

        Позволяет включать и отключать деление раздела на подразделы. Если деление на подразделы включено, объекты внутри раздела будут располагаться с учетом порядка подраздела. Подразделы располагаются в разделе в порядке возрастания номеров.

        ### Синтаксис Automation

        ```
        SubsectionOn = iObject.SubsectionOn       # Получить свойство (* )
        iObject.SubsectionOn = SubsectionOn       # Установить свойство (* )
        SubsectionOn = iObject.GetSubsectionOn()  # Получить свойство (**)
        iObject.SetSubsectionOn (SubsectionOn)    # Установить свойство (**)
        ```
        """
        ...
    @SubsectionOn.setter
    def SubsectionOn(self, value: bool): ...

    @property
    def Subsections(self) -> ISpecificationSubsections:  # ispecificationtuningsection_subsections.html
        """
        ## Subsections - Подразделы

        Тип данных: указатель на интерфейс ISpecificationSubsections подразделов спецификации

        1. Позволяет получить коллекцию подразделов данного раздела спецификации.

        2. Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        Subsections = iObject.Subsections       # Получить свойство (* )
        Subsections = iObject.GetSubsections()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def UseAdditionalBlocks(self) -> bool:  # ispecificationtuningsection_useadditionalblocks.html
        """
        ## UseAdditionalBlocks - Использовать блоки вложенных разделов

        Тип данных: BOOL

        Значения свойства:

        +-------------------------------------------------------------+
        | TRUE  | - использовать блоки вложенных разделов,            |
        +-------------------------------------------------------------+
        | FALSE | - блоки вложенных разделов использоваться не будут. |
        +-------------------------------------------------------------+

        Позволяет получить или указать количество резервных строк в разделе.

        ### Синтаксис Automation

        ```
        UseAdditionalBlocks = iObject.UseAdditionalBlocks       # Получить свойство (* )
        iObject.UseAdditionalBlocks = UseAdditionalBlocks       # Установить свойство (* )
        UseAdditionalBlocks = iObject.GetUseAdditionalBlocks()  # Получить свойство (**)
        iObject.SetUseAdditionalBlocks (UseAdditionalBlocks)    # Установить свойство (**)
        ```
        """
        ...
    @UseAdditionalBlocks.setter
    def UseAdditionalBlocks(self, value: bool): ...




class IAdditionalBlockSectionTunings(IKompasCollection):  # iadditionalblocksectiontunings.html
    """
    ## Интерфейс IAdditionalBlockSectionTunings

    Интерфейс коллекции настроек разделов блока дополнительных (вложенных) разделов.

    Управляет массивом интерфейсов IAdditionalBlockSectionTuning. В коллекции разделов доступны все разделы, определенные в стиле спецификации. Настройки разделов, использующихся в блоках, полностью совпадают с настройками соответствующих обычных разделов.

    Данный интерфейс может быть получен от интерфейса настройки блока дополнительных (вложенных) разделов IAdditionalBlockTuning с помощью свойства IAdditionalBlockTuning::Sections
    """

    def Item(self, Index) -> IAdditionalBlockSectionTuning:  # iadditionalblocksectiontunings_item.html
        """
        ## Item - Настройки раздела блока дополнительных (вложенных) разделов, заданные по индексу

        Тип данных: указатель на интерфейс IAdditionalBlockSectionTuning настроек раздела блока дополнительных (вложенных) разделов

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        BlockTuning = iObject.Item (Index)     # Получить свойство (* )
        BlockTuning = iObject.GetItem (Index)  # Получить свойство (**)
        ```
        """
        ...




class IAdditionalBlockStyles(IKompasCollection):  # iadditionalblockstyles.html
    """
    ## Интерфейс IAdditionalBlockStyles

    Интерфейс коллекции стилей блоков дополнительных (вложенных) разделов.

    Управляет массивом интерфейсов IAdditionalBlockStyle.

    Блоки разделов бывают вложенными и дополнительными. Вложенные блоки разделов располагаются внутри раздела, после всех объектов этого раздела.

    Дополнительные блоки разделов располагаются в конце спецификации, после всех ее разделов.

    Данный интерфейс может быть получен от интерфейса стиля спецификации ISpecificationStyle с помощью свойства ISpecificationStyle::AdditionalBlocks.
    """

    def Item(self, Index) -> IAdditionalBlockStyle:  # iadditionalblockstyles_item.html
        """
        ## Item - Стиль блока дополнительных (вложенных) разделов спецификации, заданный по индексу

        Тип данных: указатель на интерфейс IAdditionalBlockStyle стиля блока дополнительных (вложенных) разделов спецификации

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        Block = iObject.Item (Index)     # Получить свойство (* )
        Block = iObject.GetItem (Index)  # Получить свойство (**)
        ```
        """
        ...




class IAdditionalBlockTunings(IKompasCollection):  # iadditionalblocktunings.html
    """
    ## Интерфейс IAdditionalBlockTunings

    Интерфейс коллекции настроек блоков дополнительных (вложенных) разделов.

    Управляет массивом интерфейсов IAdditionalBlockTuning.

    1. Интерфейс настроек блоков дополнительных разделов можно получить от интерфейса настроек спецификации ISpecificationTuning с помощью свойства ISpecificationTuning::AdditionalBlocks.

    2. Интерфейс коллекции настроек блоков вложенных разделов можно получить от интерфейса настроек раздела спецификации ISpecificationTuningSection с помощью свойства ISpecificationTuningSection::AdditionalBlocks.
    """

    def Item(self, Index) -> IAdditionalBlockTuning:  # iadditionalblocktunings_item.html
        """
        ## Item - Настройки блока дополнительных (вложенных) разделов, заданные по индексу

        Тип данных: указатель на интерфейс IAdditionalBlockTuning настроек блока дополнительных (вложенных) разделов

        Свойство доступно только для чтения

        ### Синтаксис Automation

        ```
        BlockTuning = iObject.Item (Index)      # Получить свойство (* )
        BlockTuning = iObject.GetItem( Index )  # Получить свойство (**)
        ```
        """
        ...




class IAttachedDocuments(IKompasCollection):  # iattacheddocuments.html
    """
    ## Интерфейс IAttachedDocuments

    Интерфейс коллекции документов, присоединенных к объекту спецификации.

    Объект спецификации можно связать с документами КОМПАС-3D. Эта связь является двусторонней и ассоциативной и позволяет передавать данные об объекте спецификации в подключенный документ или наоборот, данные из документа в соответствующий ему объект спецификации. Если к объекту спецификации подключено несколько документов, то в объект передаются данные из первого подключенного документа. Если отредактировать подключенную к объекту модель или данные в основной надписи подключенного документа, а также изменить его формат, то при сохранении этого документа будет выдан запрос на подтверждение передачи данных в спецификацию. После изменения наименования или обозначения объекта в документе-спецификации новые данные могут быть переданы в основную надпись подключенного документа.

    1. Управляет массивом интерфейсов IAttachedDocument.

    2. Данный интерфейс можно получить у интерфейса объекта спецификации ISpecificationObject с помощью свойства ISpecificationObject::AttachedDocuments.
    """

    def Add(self, Name, Transmit) -> IAttachedDocument:  # iattacheddocuments_add.html
        """
        ## Add - Добавить новый документ к объекту спецификации и в коллекцию

        1. После вызова метода Add документ будет добавлен в коллекцию.

        2. Для удаления документа из коллекции используется метод IAttachedDocument::Delete

        3. После добавления нужных документов вызвать метод ISpecificationObject::Update.

        4. В результате подключения документа в соответствующие колонки объекта спецификации автоматически будут переданы наименование и обозначение из модели или основной надписи чертежа, а также обозначение формата, на котором выполнен подключенный чертеж.

        5. Информация из подключенного документа может передаваться не только в колонки бланка спецификации, но и в дополнительные колонки. Например, в дополнительную колонку объекта может быть автоматически передана масса детали из модели или из соответствующей графы основной надписи.

        ### Синтаксис Automation

        ```
        LPDISPATCH Add( BSTR Name, BOOL transmit );
        ```

        ### Входные параметры

        +----------------------------------------------------------+
        | Name     | - полное имя файла присоединяемого документа, |
        +----------------------------------------------------------+
        | transmit | - передавать изменения в документ.            |
        +----------------------------------------------------------+

        ### Возвращаемое значение

        - указатель на интерфейс IAttachedDocument присоединенного документа.
        """
        ...

    def AddDocument(self, Name, Transmit, FillText, EmbodimentMarking) -> IAttachedDocument:  # iattacheddocuments_adddocument.html
        """
        ## AddDocument - Добавляет новый документ к объекту спецификации и в коллекцию

        ### Синтаксис Automation

        ```
        LPDISPATCH AddDocument( BSTR Name, BOOL Transmit, BOOL FillText, BSTR
        ```

        ### Входные параметры

        +------------------------------------------------------------------------+
        | Name              | - полное имя присоединяемого документа,            |
        +------------------------------------------------------------------------+
        | Transmit          | - передавать изменения в документ,                 |
        +------------------------------------------------------------------------+
        | FillText          | - взять данные из документа в объект спецификации, |
        +------------------------------------------------------------------------+
        | EmbodimentMarking | - имя подключаемого исполнения.                    |
        +------------------------------------------------------------------------+

        ### Возвращаемое значение

        - указатель на интерфейс IAttachedDocument присоединенного документа.
        """
        ...

    def Item(self, Index) -> IAttachedDocument:  # iattacheddocuments_item.html
        """
        ## Item - Присоединенный документ, заданный по индексу

        Тип данных: указатель на интерфейс IAttachedDocument присоединенного документа

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        ColumnItem = iObject.Item( Index )     # Получить свойство (* )
        ColumnItem = iObject.GetItem( Index )  # Получить свойство (**)
        ```
        """
        ...




class ISpecificationBaseObjects(IKompasCollection):  # ispecificationbaseobjects.html
    """
    ## Интерфейс ISpecificationBaseObjects

    Интерфейс коллекции базовых объектов спецификации.

    Позволяет получить список базовых объектов спецификации.

    1. Управляет массивом интерфейсов ISpecificationBaseObject.

    2. Коллекция позволяет создать новые базовые объекты спецификации. Для создания нового базового объекта спецификации используется метод Add. После вызова метода Add объект будет добавлен в коллекцию, но в документе не появится.

    3. Для отображения объекта в документе нужно задать его параметры и вызвать метод ISpecificationObject::Update.

    4. Данный интерфейс можно получить от интерфейса описания спецификации ISpecificationDescription с помощью свойства ISpecificationDescription::BaseObjects.
    """

    def Add(self, SectionNumb, AttrNumb) -> ISpecificationBaseObject:  # ispecificationbaseobjects_add.html
        """
        ## Add - Добавить новый базовый объект спецификации в коллекцию

        1. После вызова метода Add объект будет добавлен в коллекцию, но в документе не появится. Для отображения объекта в документе нужно задать его параметры и вызвать метод ISpecificationObject::Update.

        2. Объект c номером раздела 0.0. добавить нельзя. Нужно добавлять объект спецификации в существующий раздел. Затем с помощью ISpecificationObject::AdditionalSection переносить в дополнительный раздел.

        ### Синтаксис Automation

        ```
        LPDISPATCH Add (long Section, double AttrNumb);
        ```

        ### Входные параметры

        +------------------------------+
        | Section  | - номер раздела,  |
        +------------------------------+
        | AttrNumb | - номер атрибута. |
        +------------------------------+

        ### Возвращаемое значение

        - Указатель на интерфейс ISpecificationBaseObject базового объекта спецификации.
        """
        ...

    def AddWithParam(self, Param, PropertyObject, FillText, Geometry) -> ISpecificationBaseObject:  # ispecificationbaseobjects_addwithparam.html
        """
        ## AddWithParam - Добавить новый объект спецификации в коллекцию

        1. При передаче в функцию указателя на объект PropertyObject объект спецификации и объект будут иметь общий ключ информационного объекта. Данные будут сохраняться в общем блоке свойств в метаданных.

        2. При редактировании свойств объекта изменения будут передаваться в объект спецификации. Изменение текста объекта спецификации будет передаваться в свойства объекта.

        Если объект не передается, то объект спецификации может иметь произвольную геометрию для подсветки, но тексты не будут связаны со свойствами какого-то из объектов.

        ### Синтаксис Automation

        ```
        LPDISPATCH AddWithParam( ISpecificationObjectCreateParam * Param, IPropertyKeeper * PropertyObject, BOOL FillText, VARIANT Geometry );
        ```

        ### Входные параметры

        +---------------------------------------------------------------------------------------------------------------------------------+
        | Param          | - Указатель на интерфейс расширенных параметров создания объекта спецификации ISpecificationObjectCreateParam, |
        +---------------------------------------------------------------------------------------------------------------------------------+
        | PropertyObject | - Указатель на интерфейс объекта, связываемого с объектом спецификации IPropertyKeeper,                        |
        +---------------------------------------------------------------------------------------------------------------------------------+
        | Geometry       | - Геометрия, используемая для подсветки в документе.                                                           |
        +---------------------------------------------------------------------------------------------------------------------------------+

        ### Выходные параметры

        +-----------------------------------------------------------------------------------------+
        | FillText | - TRUE - заполнить тексты колонок объекта спецификации по свойствам объекта. |
        +-----------------------------------------------------------------------------------------+

        ### Возвращаемое значение

        - указатель на интерфейс ISpecificationBaseObject.
        """
        ...

    def CopySpecificationObject(self, SpcObj, Geometry):  # ispecificationbaseobjects_copyspecificationobject.html
        """
        ## CopySpecificationObject - Копирование объекта спецификации

        ### Синтаксис Automation

        ```
        LPDISPATCH CopySpecificationObject( ISpecificationBaseObject * SpcObj, VARIANT Geometry );
        ```

        ### Входные параметры

        +--------------------------------------------------------------------------------------------------------------+
        | SpcObj   | - копируемый объект,                                                                              |
        +--------------------------------------------------------------------------------------------------------------+
        | Geometry | - геометрия объекта спецификации (Объект VT_DISPATCH или список объектов VT_ARRAY | VT_DISPATCH). |
        +--------------------------------------------------------------------------------------------------------------+

        ### Возвращаемое значение

        - Указатель на интерфейс скопированного объекта.
        """
        ...

    def GetSpecificationObjectsForGeom(self, PGeom, Equal, SectionNumb, AttrNumb) -> list[IDispatch]:  # ispecificationbaseobjects_getspecificationobjectsforgeom.html
        """
        ## GetSpecificationObjectsForGeom - По геометрии получить массив объектов спецификации в виде SAFEARRAY'я DISPATCH - VT_ARRAY | VT_DISPATCH

        1. По присланной геометрии получить массив базовых объектов спецификации, содержащих эту геометрию. У базового объекта СП может быть установлена геометрия, которая ассоциируется с данным объектом.

        Для 2D документа это массив IDrawingObject.

        Для 3D документа это массив объектов:

        •IPart7 (может быть один компонент),

        •IBody7 (тела верхнего уровня сборки),

        •IPositionLeader (обозначения позиций верхнего уровня сборки).

        Получить массив объектов можно всегда.

        2. Если геометрия должна совпадать полностью, нужно передать параметр Equal=TRUE.

        3. Поиск можно ограничить объектами определенного раздела, если указать номер раздела объектов.

        4. Поиск можно ограничить объектами определенного типа атрибута.

        ### Синтаксис Automation

        ```
        VARIANT GetSpecificationObjectsForGeom( VARIANT PGeom,
        VARIANT_BOOL Equal,
        long SectionNumb,
        double AttrNumb );
        ```

        ### Входные параметры

        +------------------------------------------------------------------------------------------------------------+
        | PGeom       | - Массив SafeArray типа VT_ARRAY | VT_DISPATCH объектов геометрии,                           |
        +------------------------------------------------------------------------------------------------------------+
        | Equal       | - флаг типа BOOL TRUE - полное совпадение геометрии, FALSE - частичное совпадение геометрии, |
        +------------------------------------------------------------------------------------------------------------+
        | SectionNumb | - номер раздела объектов СП,                                                                 |
        +------------------------------------------------------------------------------------------------------------+
        | AttrNumb    | - номер типа атрибута объектов СП или 0.                                                     |
        +------------------------------------------------------------------------------------------------------------+

        ### Возвращаемое значение

        - Массив SafeArray типа VT_ARRAY | VT_DISPATCH объектов спецификации.
        """
        ...

    def Item(self, Index) -> ISpecificationBaseObject:  # ispecificationbaseobjects_item.html
        """
        ## Item - Базовый объект спецификации, заданный по индексу, по уникальному идентификатору или по уникальному номеру

        Тип данных: Указатель на интерфейс ISpecificationBaseObject объекта спецификации

        1. С помощью данного свойства можно получить базовый объект спецификации, заданный по индексу, по уникальному идентификатору IKompasAPIObject::Reference или уникальному номеру ISpecificationObject::UniqueNumber.

        2. Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        spcObj = iObject.Item (Index)     # Получить свойство (* )
        spcObj = iObject.GetItem (Index)  # Получить свойство (**)
        ```
        """
        ...




class ISpecificationColumnItems(IKompasCollection):  # ispecificationcolumnitems.html
    """
    ## Интерфейс ISpecificationColumnItems

    Интерфейс коллекции элементов колонки объекта спецификации.

    Количество элементов колонки зависит от типа колонки.

    Колонки типа Целое, Вещественное или Строка имеют только один элемент соответствующего типа.

    Колонка типа запись (ksValueTypeRecord см. ISpecificationColumn::ValueType) может иметь более одного элемента.

    Если базовый объект спецификации создан на основе атрибута, то количество элементов в колонке будет равно количеству колонок атрибута. Если объекту не указан тип атрибута, то в колонке типа запись будет один элемент с типом значения - строка.

    1. Управляет массивом интерфейсов ISpecificationColumnItem.

    2. Данный интерфейс можно получить от интерфейса колонки объекта спецификации ISpecificationColumn с помощью свойства ISpecificationColumn::ColumnItems.
    """

    def Item(self, Index) -> ISpecificationColumnItem:  # ispecificationcolumnitems_item.html
        """
        ## Item - Элемент колонки объекта спецификации, заданный по индексу

        Тип данных: указатель на интерфейс ISpecificationColumnItem элемента колонки объекта спецификации

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        ColumnItem = iObject.Item( Index )     # Получить свойство (* )
        ColumnItem = iObject.GetItem( Index )  # Получить свойство (**)
        ```
        """
        ...




class ISpecificationColumns(IKompasCollection):  # ispecificationcolumns.html
    """
    ## Интерфейс ISpecificationColumns

    Интерфейс коллекции колонок объекта спецификации.

    Различаются основные и дополнительные колонки объектов спецификации. Основные колонки отображаются в таблице спецификации и выводятся на печать. Дополнительные колонки содержат сведения, дополняющие информацию, включаемую в стандартный бланк. В таблице спецификации такие колонки не видны и на печать не выводятся. Количество основных колонок может не соответствовать количеству колонок в таблице спецификации. Это вызвано наличием исполнений для групповых спецификаций.

    1. Управляет массивом интерфейсов ISpecificationColumn.

    2. Интерфейс коллекции основных колонок объекта спецификации может быть получен от интерфейса ISpecificationObject с помощью свойства ISpecificationObject::Columns.

    3. Интерфейс коллекции дополнительных колонок может быть получен от интерфейса ISpecificationObject с помощью свойства ISpecificationObject::AdditionalColumns.
    """

    def Column(self, ColumnType, ColumnNumber, BlockNumber) -> ISpecificationColumn:  # ispecificationcolumns_column.html
        """
        ## Column - Колонка объекта спецификации, заданная по типу, номеру и блоку

        Тип данных: указатель на интерфейс ISpecificationColumn колонки объекта спецификации

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        Column = iObject.Column( type, ColumnNumber, BlockNumber )     # Получить свойство (* )
        Column = iObject.GetColumn( type, ColumnNumber, BlockNumber )  # Получить свойство (**)
        ```

        ### Входные параметры

        +-------------------------------------------------------------------------------------+
        | type         | - тип колонки объекта спецификации из ksSpecificationColumnTypeEnum, |
        +-------------------------------------------------------------------------------------+
        | ColumnNumber | - номер колонки данного типа,                                        |
        +-------------------------------------------------------------------------------------+
        | BlockNumber  | - номер блока.                                                       |
        +-------------------------------------------------------------------------------------+
        """
        ...

    def Item(self, Index) -> ISpecificationColumn:  # ispecificationcolumns_item.html
        """
        ## Item - Колонка объекта спецификации, заданная по индексу

        Тип данных: Указатель на интерфейс ISpecificationColumn колонки объекта спецификации

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        Column = iObject.Item( Index )     # Получить свойство (* )
        Column = iObject.GetItem( Index )  # Получить свойство (**)
        ```
        """
        ...




class ISpecificationColumnStyles(IKompasCollection):  # ispecificationcolumnstyles.html
    """
    ## Интерфейс ISpecificationColumnStyles

    Интерфейс коллекции стилей колонок спецификации.

    Управляет массивом интерфейсов ISpecificationColumnStyle. Различаются основные и дополнительные колонки объектов спецификации.

    1. Чтобы получить коллекцию стилей основных колонок спецификации, используется свойство интерфейса стиля спецификации ISpecificationStyleISpecificationStyle::Columns или свойство интерфейса стиля раздела спецификации ISpecificationSectionStyle. ISpecificationSectionStyle::Columns.

    2. Чтобы получить коллекцию стилей дополнительных колонок спецификации, используется свойство интерфейса стиля спецификации ISpecificationStyleISpecificationStyle::AdditionalColumns или свойство интерфейса стиля раздела спецификации ISpecificationSectionStyle. ISpecificationSectionStyle::AdditionalColumns.
    """

    def Item(self, Index) -> ISpecificationColumnStyle:  # ispecificationcolumnstyles_item.html
        """
        ## Item - Стиль колонки спецификации, заданный по индексу

        Тип данных: указатель на интерфейс ISpecificationColumnStyle стиля колонки спецификации

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        Column = iObject.Item (Index)     # Получить свойство (* )
        Column = iObject.GetItem (Index)  # Получить свойство (**)
        ```
        """
        ...




class ISpecificationCommentObjects(IKompasCollection):  # ispecificationcommentobjects.html
    """
    ## Интерфейс ISpecificationCommentObjects

    Интерфейс коллекции вспомогательных объектов спецификации.

    Позволяет получить список вспомогательных объектов спецификации.

    1. Управляет массивом интерфейсов ISpecificationCommentObject.

    2. Коллекция позволяет создать новые вспомогательные объекты спецификации. Для создания нового вспомогательного объекта спецификации используется метод Add. После вызова метода Add объект будет добавлен в коллекцию, но в документе не появится.

    3. Для отображения объекта в документе нужно задать его параметры и вызвать метод ISpecificationObject::Update.

    4. Методы Attach и Detach работают только для коллекции, взятой у базового объекта СП.

    5. Данный интерфейс можно получить от интерфейса описания спецификации ISpecificationDescription с помощью свойства ISpecificationDescription::CommentObjects и от интерфейса базового объекта спецификации ISpecificationBaseObject с помощью свойства ISpecificationBaseObject_CommentObjects.
    """

    def Add(self, SectionNamb) -> ISpecificationCommentObject:  # ispecificationcommentobjects_add.html
        """
        ## Add - Добавить новый вспомогательный объект спецификации в коллекцию

        После вызова метода Add объект будет добавлен в коллекцию, но в документе не появится. Для отображения объекта в документе нужно задать его параметры и вызвать метод ISpecificationObject::Update.

        ### Синтаксис Automation

        ```
        LPDISPATCH Add (long Section);
        ```

        ### Входные параметры

        +----------------------------+
        | Section | - номер раздела. |
        +----------------------------+

        ### Возвращаемое значение

        - Указатель на интерфейс ISpecificationCommentObject вспомогательного объекта спецификации.
        """
        ...

    def AddWithParam(self, Param, ParentObject) -> ISpecificationCommentObject:  # ispecificationcommentobjects_addwithparam.html
        """
        ## AddWithParam - Добавить новый объект спецификации в коллекцию

        ### Синтаксис Automation

        ```
        LPDISPATCH AddWithParam( ISpecificationObjectCreateParam * Param, ISpecificationBaseObject * ParentObject );
        ```

        ### Входные параметры

        +-------------------------------------------------------------------------------------------------------------------------------+
        | Param        | - Указатель на интерфейс расширенных параметров создания объекта спецификации ISpecificationObjectCreateParam. |
        +-------------------------------------------------------------------------------------------------------------------------------+
        | ParentObject | - Указатель на базовый объект спецификации ISpecificationBaseObject, для которого добавляется комментарий.     |
        +-------------------------------------------------------------------------------------------------------------------------------+

        Версия: Компас v20

        ### Возвращаемое значение

        - Указатель на интерфейс ISpecificationCommentObject вспомогательного объекта спецификации.
        """
        ...

    def Attach(self, PVal) -> bool:  # ispecificationcommentobjects_attach.html
        """
        ## Attach - Добавить вспомогательный объект спецификации в коллекцию прикрепленных объектов (Для базового объекта)

        Метод работает только для коллекции взятой у базового объекта СП.

        ### Синтаксис Automation

        ```
        BOOL Attach( LPDISPATCH spcObj );
        ```

        ### Входные параметры

        +-----------------------------------------------------------------------------------------+
        | spcObj | - указатель на интерфейс прикрепляемого вспомогательного объекта спецификации. |
        +-----------------------------------------------------------------------------------------+

        ### Возвращаемое значение

        +-----------------------------+
        | TRUE  | - в случае успеха,  |
        +-----------------------------+
        | FALSE | - в случае неудачи. |
        +-----------------------------+
        """
        ...

    def CopySpecificationObject(self, SpcObj):  # ispecificationcommentobjects_copyspecificationobject.html
        """
        ## CopySpecificationObject - Копирование объекта спецификации

        ### Синтаксис Automation

        ```
        LPDISPATCH CopySpecificationObject( ISpecificationCommentObject * SpcObj );
        ```

        ### Входные параметры

        +-------------------------------+
        | spcObj | - копируемый объект. |
        +-------------------------------+

        ### Возвращаемое значение

        - Указатель на интерфейс скопированного объекта.
        """
        ...

    def Detach(self, PVal) -> bool:  # ispecificationcommentobjects_detach.html
        """
        ## Detach - Удалить вспомогательный объект спецификации из коллекции прикрепленных объектов (Для базового объекта)

        Метод работает только для коллекции взятой у базового объекта СП.

        ### Синтаксис Automation

        ```
        BOOL Detach (LPDISPATCH spcObj);
        ```

        ### Входные параметры

        +-----------------------------------------------------------------------------------------+
        | spcObj | - указатель на интерфейс прикрепляемого вспомогательного объекта спецификации. |
        +-----------------------------------------------------------------------------------------+

        ### Возвращаемое значение

        +-----------------------------+
        | TRUE  | - в случае успеха,  |
        +-----------------------------+
        | FALSE | - в случае неудачи. |
        +-----------------------------+
        """
        ...

    def Item(self, Index) -> ISpecificationCommentObject:  # ispecificationcommentobjects_item.html
        """
        ## Item - Вспомогательный объект спецификации, заданный по индексу, по уникальному идентификатору или по уникальному номеру

        Тип данных: Указатель на интерфейс ISpecificationCommentObject объекта спецификации.

        1. С помощью данного свойства можно получить вспомогательный объект спецификации, заданный по индексу, по уникальному идентификатору IKompasAPIObject::Reference или уникальному номеру ISpecificationObject::UniqueNumber.

        2. Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        spcObj = iObject.Item (Index)     # Получить свойство (* )
        spcObj = iObject.GetItem (Index)  # Получить свойство (**)
        ```
        """
        ...




class ISpecificationDescriptions(IKompasCollection):  # ispecificationdescriptions.html
    """
    ## Интерфейс ISpecificationDescriptions

    Интерфейс коллекции описаний спецификации у документа.

    1. Для документа-спецификации возможно только одно описание, и оно есть всегда (т.е. добавлять и удалять нельзя).

    2. В текстовом документе описаний нет.

    3. Для остальных документов (чертеж, фрагмент, деталь и сборка) описаний может быть сколько угодно, в том числе и ни одного.

    4. При добавлении нового описания оно становится текущим.

    5. Данный интерфейс может быть получен следующими способами:

    •от интерфейса документа IKompasDocument с помощью свойства IKompasDocument::SpecificationDescriptions,

    •от интерфейса чертежа IDrawingDocument с помощью свойства IDrawingDocument::ChangeListDescriptions.
    """

    @property
    def Active(self) -> ISpecificationDescription:  # ispecificationdescriptions_active.html
        """
        ## Active - Текущее описание

        Тип данных: Указатель на интерфейс ISpecificationDescription описания спецификации

        1. Если в документе нет описаний спецификаций, возвращается NULL.

        2. Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        Description = iObject.Active       # Получить свойство (* )
        Description = iObject.GetActive()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def ActiveFromLibStyle(self) -> ISpecificationDescription:  # ispecificationdescriptions_activefromlibstyle.html
        """
        ## ActiveFromLibStyle - Текущее описание, соответствующее библиотечному стилю

        Тип данных: Указатель на интерфейс ISpecificationDescription

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        ActiveFromLibStyle = Object.ActiveFromLibStyle       # Получить свойство (* )
        ActiveFromLibStyle = Object.GetActiveFromLibStyle()  # Получить свойство (**)
        ```

        ### Возвращаемое значение

        +---------------------------------------------------------------------------------------------------------------------------------------------------------------------------+
        | \\ | - если текущее описание соответствует своему описанию-источнику в библиотеке, то просто возвращается оно же,                                                          |
        +---------------------------------------------------------------------------------------------------------------------------------------------------------------------------+
        |   | - если текущее описание не соответствует своему описанию-источнику в библиотеке, то возвращается новое описание, созданное по описанию-источнику и сделанное текущим, |
        +---------------------------------------------------------------------------------------------------------------------------------------------------------------------------+
        |   | - если текущего описания нет или для текущего описания не найдено описание-источник (не найдена библиотека-источник), то возвращается NULL.                           |
        +---------------------------------------------------------------------------------------------------------------------------------------------------------------------------+
        """
        ...

    def Add(self, LayoutName, StyleID, SpcName) -> ISpecificationDescription:  # ispecificationdescriptions_add.html
        """
        ## Add - Добавить новое описание спецификации в документ и в коллекцию

        К одному документу могут быть добавлены описания спецификации только разных стилей. Если LayoutName и StyleID не заданы, то добавляется описание спецификации по умолчанию, если оно не было создано ранее.

        ### Синтаксис Automation

        ```
        LPDISPATCH Add (BSTR LayoutName,
        long StyleID,
        BSTR SpcName,
        ISpecificationDescription);
        ```

        ### Входные параметры

        +---------------------------------------------------------------------------------------------+
        | LayoutName | - имя библиотеки стилей,                                                       |
        +---------------------------------------------------------------------------------------------+
        | StyleID    | - номер стиля в библиотеке стилей,                                             |
        +---------------------------------------------------------------------------------------------+
        | SpcName    | - имя документа спецификации, связанного с описанием спецификации в документе. |
        +---------------------------------------------------------------------------------------------+

        ### Возвращаемое значение

        - Указатель на интерфейс ISpecificationDescription описания спецификации.
        """
        ...

    def Description(self, LayoutName, StyleID) -> ISpecificationDescription:  # ispecificationdescriptions_description.html
        """
        ## Description - Описание спецификации по имени файла библиотеки оформлений и номеру стиля в библиотеке

        Тип данных: Указатель на интерфейс ISpecificationDescription описания спецификации

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        Description = iObject.Description (LayoutName, StyleID)     # Получить свойство (* )
        Description = iObject.GetDescription (LayoutName, StyleID)  # Получить свойство (**)
        ```

        ### Входные параметры

        +-----------------------------------------------------+
        | LayoutName | - имя файла библиотеки оформлений,     |
        +-----------------------------------------------------+
        | StyleID    | - номер стиля в библиотеке оформлений. |
        +-----------------------------------------------------+
        """
        ...

    def Item(self, Index) -> ISpecificationDescription:  # ispecificationdescriptions_item.html
        """
        ## Item - Описание спецификации, заданное по индексу

        Тип данных: Указатель на интерфейс ISpecificationDescription описания спецификации

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        Description = iObject.Item (Index)     # Получить свойство (* )
        Description = iObject.GetItem (Index)  # Получить свойство (**)
        ```
        """
        ...




class ISpecificationSectionStyles(IKompasCollection):  # ispecificationsectionstyles.html
    """
    ## Интерфейс ISpecificationSectionStyles

    Интерфейс коллекции стилей разделов спецификации.

    Управляет массивом интерфейсов ISpecificationSectionStyle.

    Данный интерфейс может быть получен от интерфейса ISpecificationStyle с помощью свойства ISpecificationStyle::Sections.
    """

    def Item(self, Index) -> ISpecificationSectionStyle:  # ispecificationsectionstyles_item.html
        """
        ## Item - Стиль раздела спецификации, заданный по индексу

        Тип данных: указатель на интерфейс ISpecificationSectionStyle стиля раздела спецификации

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        Section = iObject.Item (Index)     # Получить свойство (* )
        Section = iObject.GetItem (Index)  # Получить свойство (**)
        ```
        """
        ...




class ISpecificationSubsections(IKompasCollection):  # ispecificationsubsections.html
    """
    ## Интерфейс ISpecificationSubsections

    Интерфейс коллекции подразделов спецификации.

    Управляет массивом интерфейсов ISpecificationSubsection. Группы объектов внутри разделов называются подразделами. Подразделы, как и разделы, являются компонентом стиля спецификации. Их количество, названия и порядок внутри каждого раздела формируются при настройке стиля. Если деление на подразделы не запрещено в стиле текущей спецификации, то можно изменить список подразделов в каждом ее разделе. Подразделы располагаются в разделе в порядке возрастания номеров.

    Данный интерфейс можно получить от интерфейса настройки раздела спецификации ISpecificationTuningSection с помощью свойства ISpecificationTuningSection::Subsections.
    """

    def Add(self, Name, Number) -> ISpecificationSubsection:  # ispecificationsubsections_add.html
        """
        ## Add - Добавить новый подраздел

        ### Синтаксис Automation

        ```
        LPDISPATCH Add (BSTR Name, short Number);
        ```

        ### Входные параметры

        +------------------------------+
        | Name   | - имя подраздела,   |
        +------------------------------+
        | Number | - номер подраздела. |
        +------------------------------+

        ### Возвращаемое значение

        - Указатель на интерфейс подраздела спецификации ISpecificationSubsection.
        """
        ...

    def Item(self, Index) -> ISpecificationSubsection:  # ispecificationsubsections_item.html
        """
        ## Item - Настройки раздела спецификации, заданные по индексу

        Тип данных: указатель на интерфейс ISpecificationSubsection подраздела спецификации

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        Subsection = iObject.Item (Index)     # Получить свойство (* )
        Subsection = iObject.GetItem (Index)  # Получить свойство (**)
        ```
        """
        ...




class ISpecificationTuningSections(IKompasCollection):  # ispecificationtuningsections.html
    """
    ## Интерфейс ISpecificationTuningSections

    Интерфейс коллекции настроек разделов спецификации.

    Управляет массивом интерфейсов ISpecificationTuningSection .

    Данный интерфейс может быть получен от интерфейса настроек спецификации ISpecificationTuning с помощью свойства ISpecificationTuning::Sections
    """

    def Item(self, Index) -> ISpecificationTuningSection:  # ispecificationtuningsections_item.html
        """
        ## Item - Настройки раздела спецификации, заданные по индексу

        Тип данных: указатель на интерфейс ISpecificationTuningSection настроек раздела спецификации

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        Section = iObject.Item (Index)     # Получить свойство (* )
        Section = iObject.GetItem (Index)  # Получить свойство (**)
        ```
        """
        ...




class ksFindObject3DParametersNotify(IDispatch):  # ksfindobject3dparametersnotify.html
    """
    ## Интерфейс ksFindObject3DParametersNotify

    Интерфейс фильтрации объектов 3D в функциях поиска объектов.
    """

    def close(self):
        ...




class IModelObjectNotifyResult(IDispatch):  # imodelobjectnotifyresult.html
    """
    ## Интерфейс IModelObjectNotifyResult

    Интерфейс результатов редактирования 3d объекта.

    Позволяет контролировать редактирование объектов документа-модели. Источником событий для подписки является указатель на интерфейс IModelObject.

    Получить интерфейс можно с помощью метода IUnknown::QueryInterface у интерфейса компонента IPart7.
    """

    @property
    def IsRedoMode(self) -> bool:  # imodelobjectnotifyresult_isredomode.html
        """
        ## IsRedoMode - Признак работы команды Redo

        Тип данных: BOOL

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        IsRedoMode = Object.IsRedoMode;       # Получить свойство (* )
        IsRedoMode = Object.GetIsRedoMode();  # Получить свойство (**)
        ```
        """
        ...

    @property
    def IsUndoMode(self) -> bool:  # imodelobjectnotifyresult_isundomode.html
        """
        ## IsUndoMode - Признак работы команды Undo

        Тип данных: BOOL

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        IsUndoMode = Object.IsUndoMode;       # Получить свойство (* )
        IsUndoMode = Object.GetIsUndoMode();  # Получить свойство (**)
        ```
        """
        ...

    @property
    def NotifyObjects(self) -> typing.Any:  # imodelobjectnotifyresult_notifyobjects.html
        """
        ## NotifyObjects - Массив SafeArray удаляемых объектов

        Тип данных: VARIANT

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        NotifyObjects = Object.NotifyObjects;       # Получить свойство (* )
        NotifyObjects = Object.GetNotifyObjects();  # Получить свойство (**)
        ```
        """
        ...

    @property
    def NotifyType(self) -> int:  # imodelobjectnotifyresult_notifytype.html
        """
        ## NotifyType - Тип события

        Тип данных: из перечисления KsObject3DNotifyEnum

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        NotifyType = Object.NotifyType;       # Получить свойство (* )
        NotifyType = Object.GetNotifyType();  # Получить свойство (**)
        ```
        """
        ...

    @property
    def ProcessType(self) -> int:  # imodelobjectnotifyresult_processtype.html
        """
        ## ProcessType - Тип процесса

        Тип данных: из перечисления ProcessTypeEnum

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        ProcessType = Object.ProcessType;       # Получить свойство (* )
        ProcessType = Object.GetProcessType();  # Получить свойство (**)
        ```
        """
        ...




class IKompasDocument3DNotifyResult(IDispatch):  # ikompasdocument3dnotifyresult.html
    """
    ## Интерфейс IKompasDocument3DNotifyResult

    Интерфейс дополнительных параметров для событий документа-модели.

    1. Интерфейс позволяет получить указатель на объект, для которого посылается данное событие. Например, выбор материала и обозначения доступны для верхнего компонента, вставки и тела.

    2. Интерфейс является дополнительным для IKompasDocument3D.

    3. Интерфейс можно получить с помощью метода IUnknown::QueryInterface у IKompasDocument3D.
    """

    @property
    def NotifyObject(self) -> IKompasAPIObject:  # ikompasdocument3dnotifyresult_notifyobject.html
        """
        ## NotifyObject - Тип объекта

        Тип данных: указатель на интерфейс IKompasAPIObject

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        NotifyObject = Object.NotifyObject       # Получить свойство (* )
        NotifyObject = Object.GetNotifyObject()  # Получить свойство (**)
        ```

        ### Возвращаемое значение

        В зависимости от типа объекта, возвращаемого методом NotifyObjectType возвращается:

        +-------------------------+
        | тип объекта | Интерфейс |
        +-------------------------+
        | o3d_part    | - IPart7  |
        +-------------------------+
        | o3d_body    | - IBody7  |
        +-------------------------+
        | o3d_uknown  | - NULL    |
        +-------------------------+
        """
        ...

    @property
    def NotifyObjectType(self) -> int:  # ikompasdocument3dnotifyresult_notifyobjecttype.html
        """
        ## NotifyObjectType - Тип объекта

        Тип данных: из перечисления ksObj3dTypeEnum

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        NotifyObjectType = Object.NotifyObjectType       # Получить свойство (* )
        NotifyObjectType = Object.GetNotifyObjectType()  # Получить свойство (**)
        ```

        ### Возвращаемое значение

        - Тип объекта из перечисления ksObj3dTypeEnum. Допустимыми значениями являются o3d_part, o3d_body или o3d_uknown.
        """
        ...

    @property
    def NotifyType(self) -> int:  # ikompasdocument3dnotifyresult_notifytype.html
        """
        ## NotifyType - Тип события

        Тип данных: из перечисления ksDocument3DNotifyEnum

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        NotifyType = Object.NotifyType       # Получить свойство (* )
        NotifyType = Object.GetNotifyType()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def RequestFilesType(self) -> int:  # ikompasdocument3dnotifyresult_requestfilestype.html
        """
        ## RequestFilesType - Тип процесса, запрашивающего файл

        Тип данных: из перечисления ksRequestFilesTypeEnum

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        RequestFilesType = Object.RequestFilesType       # Получить свойство (* )
        RequestFilesType = Object.GetRequestFilesType()  # Получить свойство (**)
        ```

        ### Возвращаемое значение

        Возвращает идентификатор процесса, запрашивающего файл.
        """
        ...




class ksDocument3DNotify7():  # ksdocument3dnotify7.html
    """
    ## Интерфейс ksDocument3DNotify7

    Интерфейс событий документа-модели в API7 (disp-интерфейс).

    Позволяет контролировать редактирование документа-модели.

    Источником для подписки является документ-модель.
    """

    def close(self):
        ...




class ksDocumentFrameNotify():  # ksdocumentframenotify.html
    """
    ## Интерфейс ksDocumentFrameNotify/IDocumentFrameNotify

    +-------------------------------------------------+
    | ksDocumentFrameNotify | - интерфейс Automation, |
    +-------------------------------------------------+
    | IdocumentFrameNotify  | - интерфейс COM         |
    +-------------------------------------------------+

    Интерфейс событий для окна документа (клавиатура, мышь, события по отрисовке документа)

    Позволяют контролировать состояние окна документа.

    Источником событий для подписки на данный интерфейс является объект IDocumentFrame - интерфейс окна документа.

    В COM источником для подписки является указатель на документ. Дополнительный параметр при подписке задает интерфейс окна IDocumentFrame, для которого предназначен данный интерфейс событий.
    """

    def close(self):
        ...




class ksDrawingObjectNotify():  # ksdrawingobjectnotify.html
    """
    ## Интерфейс ksDrawingObjectNotify/IDrawingObjectNotify

    +------------------------------------------------+
    | ksDrawingObjectNotify | - интерфейс Automation |
    +------------------------------------------------+

    Интерфейс событий для графического объекта.

    Позволяет контролировать состояние объектов графического документа.

    В Automation источником событий для подписки на данный интерфейс являются объекты IDrawingObject - базового интерфейса для графических объектов (события генерируются только для этого объекта) или коллекции объектов 2D IDrawingObjects (события генерируются для всех объектов данной коллекции).

    Для получения указателей на документ и объект можно использовать свойство IKompasAPIObject::Reference.

    В API5 аналогом является интерфейс событий объектов графических документов ksObject2DNotify, поэтому перечисления событий совпадают с перечислением событий для объектов графических документов KsObject2DNotifyEnum.

    В процессе обработки события можно получить дополнительную информацию о редактировании объекта при помощи интерфейса ksObject2DNotifyResult, который можно получить при помощи метода IKompasDocument2D::GetDrawingObjectNotifyResult.
    """

    def close(self):
        ...




class ksKompasObjectNotify():  # kskompasobjectnotify.html, kskompasobjectnotify5.html
    """
    ## Интерфейс ksKompasObjectNotify/IKompasObjectNotify

    +-----------------------------------------------+
    | ksKompasObjectNotify | - интерфейс Automation |
    +-----------------------------------------------+
    | IKompasObjectNotify  | - интерфейс COM        |
    +-----------------------------------------------+

    Интерфейс событий приложения.

    Позволяют контролировать состояние приложения.

    В Automation источником событий для подписки являются:

    •для API интерфейсов версии 5 - объект KompasObject.

    •для API интерфейсов версии 7 - объект IApplication.

    В СOM источника нет.

    ----------

    ## Интерфейс событий приложения ksKompasObjectNotify/IKompasObjectNotify

    +-----------------------------------------------+
    | ksKompasObjectNotify | - интерфейс Automation |
    +-----------------------------------------------+
    | IKompasObjectNotify  | - интерфейс COM        |
    +-----------------------------------------------+

    Позволяют контролировать состояние приложения.

    В Automation источником событий для подписки являются:

    •для API интерфейсов версии 5 - объект KompasObject.

    •для API интерфейсов версии 7 - объект IApplication.

    В СOM источника нет.
    """

    def close(self):
        ...




class ksLayoutSheetsNotify():  # kslayoutsheetsnotify.html
    """
    ## Интерфейс ksLayoutSheetsNotify

    Интерфейс событий для листов оформления.

    Позволяют контролировать состояние листов оформления.

    Источником событий для подписки на данный интерфейс являются объекты ILayoutSheets - Интерфейс коллекции листов оформления.
    """

    def close(self):
        ...




class ksLibraryManagerNotify():  # kslibrarymanagernotify.html
    """
    ## Интерфейс ksLibraryManagerNotify

    Интерфейс событий Менеджера библиотек.

    Позволяют следить и при необходимости управлять основными событиями менеджера библиотек.

    Источником событий для подписки на данный интерфейс является объект ILibraryManager.
    """

    def close(self):
        ...




class ksModelObjectNotify():  # ksmodelobjectnotify.html
    """
    ## Интерфейс ksModelObjectNotify

    Интерфейс событий для объектов документа-модели.

    Позволяют контролировать состояние объектов документа - модели.

    В Automation источником событий для подписки на данный интерфейс являются объекты IModelObject (события генерируются только для этого объекта) или коллекции объектов 3D IModelObjects (события генерируются для всех объектов данной коллекции).

    В процессе обработки события можно получить дополнительную информацию о редактировании объекта при помощи интерфейса IModelObjectNotifyResult.

    В API 5 аналогом является Интерфейс событий объектов документа-модели ksObject3DNotify, поэтому перечисления событий совпадают с перечислением событий для объектов документа-модели KsObject3DNotifyEnum.
    """

    def close(self):
        ...




class ksProcess3DManipulatorsNotify(IDispatch):  # ksprocess3dmanipulatorsnotify.html
    """
    ## Интерфейс IProcess3DManipulatorsNotify\\ksProcess3DManipulatorsNotify

    Интерфейс событий манипуляторов.

    Источником событий манипуляторов является коллекция манипуляторов IManipulators.

    Версия: KOMPAS v19
    """

    def BeginDragManipulator(self, ManipulatorId, PrimitiveType) -> bool:  # ksprocess3dmanipulatorsnotify_begindragmanipulator.html
        """
        ## BeginDragManipulator - Начало перемещения примитива манипулятором

        ### Синтаксис Automation

        ```
        BOOL BeginDragManipulator ( long ManipulatorId, ksManipulatorPrimitiveEnum PrimitiveType );
        ```

        ### Входные параметры

        +-------------------------------------------------------------------------------------+
        | ManipulatorId | - идентификатор манипулятора,                                       |
        +-------------------------------------------------------------------------------------+
        | PrimitiveType | - примитив манипулятора из перечисления ksManipulatorPrimitiveEnum. |
        +-------------------------------------------------------------------------------------+

        ### Возвращаемое значение

        +---------------------------------------------------------------------------------------------------------------+
        | TRUE  | - если библиотека обработала событие,                                                                 |
        +---------------------------------------------------------------------------------------------------------------+
        | FALSE | - библиотека не обработала событие, требуется использовать встроенную обработку событий манипулятора. |
        +---------------------------------------------------------------------------------------------------------------+
        """
        ...

    def ChangeManipulatorValue(self, ManipulatorId, PrimitiveType, newValue) -> bool:  # ksprocess3dmanipulatorsnotify_changemanipulatorvalue.html
        """
        ## ChangeManipulatorValue - Завершение редактирования значения в редакторе манипулятора

        ### Синтаксис Automation

        ```
        BOOL ChangeManipulatorValue (long ManipulatorId, ksManipulatorPrimitiveEnum PrimitiveType, double newValue );
        ```

        ### Входные параметры

        +-------------------------------------------------------------------------------------+
        | ManipulatorId | - идентификатор манипулятора,                                       |
        +-------------------------------------------------------------------------------------+
        | PrimitiveType | - примитив манипулятора из перечисления ksManipulatorPrimitiveEnum, |
        +-------------------------------------------------------------------------------------+
        | newValue      | - новое значение.                                                   |
        +-------------------------------------------------------------------------------------+

        ### Возвращаемое значение

        +---------------------------------------------------------------------------------------------------------------+
        | TRUE  | - если библиотека обработала событие,                                                                 |
        +---------------------------------------------------------------------------------------------------------------+
        | FALSE | - библиотека не обработала событие, требуется использовать встроенную обработку событий манипулятора. |
        +---------------------------------------------------------------------------------------------------------------+
        """
        ...

    def ClickManipulatorPrimitive(self, ManipulatorId, PrimitiveType, DoubleClick) -> bool:  # ksprocess3dmanipulatorsnotify_clickmanipulatorprimitive.html
        """
        ## ClickManipulatorPrimitive - Клик или двойной клик по примитиву манипулятора

        ### Синтаксис Automation

        ```
        BOOL ClickManipulatorPrimitive( long ManipulatorId, ksManipulatorPrimitiveEnum PrimitiveType, BOOL DoubleClick );
        ```

        ### Входные параметры

        +-------------------------------------------------------------------------------------+
        | ManipulatorId | - идентификатор манипулятора,                                       |
        +-------------------------------------------------------------------------------------+
        | PrimitiveType | - примитив манипулятора из перечисления ksManipulatorPrimitiveEnum, |
        +-------------------------------------------------------------------------------------+
        | DoubleClick   | - двойной клик.                                                     |
        +-------------------------------------------------------------------------------------+

        ### Возвращаемое значение

        +---------------------------------------------------------------------------------------------------------------+
        | TRUE  | - если библиотека обработала событие,                                                                 |
        +---------------------------------------------------------------------------------------------------------------+
        | FALSE | - библиотека не обработала событие, требуется использовать встроенную обработку событий манипулятора. |
        +---------------------------------------------------------------------------------------------------------------+
        """
        ...

    def CreateManipulatorEdit(self, ManipulatorId, PrimitiveType) -> bool:  # ksprocess3dmanipulatorsnotify_createmanipulatoredit.html
        """
        ## CreateManipulatorEdit - Создание редактора для ввода значения, управляющего положением манипулятора

        ### Синтаксис Automation

        ```
        BOOL CreateManipulatorEdit ( long ManipulatorId, ksManipulatorPrimitiveEnum PrimitiveType );
        ```

        ### Входные параметры

        +-------------------------------------------------------------------------------------+
        | ManipulatorId | - идентификатор манипулятора,                                       |
        +-------------------------------------------------------------------------------------+
        | PrimitiveType | - примитив манипулятора из перечисления ksManipulatorPrimitiveEnum, |
        +-------------------------------------------------------------------------------------+

        ### Возвращаемое значение

        +---------------------------------------------------------------------------------------------------------------+
        | TRUE  | - если библиотека обработала событие,                                                                 |
        +---------------------------------------------------------------------------------------------------------------+
        | FALSE | - библиотека не обработала событие, требуется использовать встроенную обработку событий манипулятора. |
        +---------------------------------------------------------------------------------------------------------------+
        """
        ...

    def DestroyManipulatorEdit(self, ManipulatorId) -> bool:  # ksprocess3dmanipulatorsnotify_destroymanipulatoredit.html
        """
        ## DestroyManipulatorEdit - Удаление редактора для ввода значения, управляющего положением манипулятора

        ### Синтаксис Automation

        ```
        BOOL DestroyManipulatorEdit ( long ManipulatorId );
        ```

        ### Входные параметры

        +-----------------------------------------------+
        | ManipulatorId | - идентификатор манипулятора. |
        +-----------------------------------------------+

        ### Возвращаемое значение

        +---------------------------------------------------------------------------------------------------------------+
        | TRUE  | - если библиотека обработала событие,                                                                 |
        +---------------------------------------------------------------------------------------------------------------+
        | FALSE | - библиотека не обработала событие, требуется использовать встроенную обработку событий манипулятора. |
        +---------------------------------------------------------------------------------------------------------------+
        """
        ...

    def EndDragManipulator(self, ManipulatorId, PrimitiveType) -> bool:  # ksprocess3dmanipulatorsnotify_enddragmanipulator.html
        """
        ## EndDragManipulator - Завершение перемещения примитива манипулятором

        ### Синтаксис Automation

        ```
        BOOL EndDragManipulator ( long ManipulatorId, ksManipulatorPrimitiveEnum PrimitiveType );
        ```

        ### Входные параметры

        +-------------------------------------------------------------------------------------+
        | ManipulatorId | - идентификатор манипулятора,                                       |
        +-------------------------------------------------------------------------------------+
        | PrimitiveType | - примитив манипулятора из перечисления ksManipulatorPrimitiveEnum, |
        +-------------------------------------------------------------------------------------+

        ### Возвращаемое значение

        +---------------------------------------------------------------------------------------------------------------+
        | TRUE  | - если библиотека обработала событие,                                                                 |
        +---------------------------------------------------------------------------------------------------------------+
        | FALSE | - библиотека не обработала событие, требуется использовать встроенную обработку событий манипулятора. |
        +---------------------------------------------------------------------------------------------------------------+
        """
        ...

    def MoveManipulator(self, ManipulatorId, VX, VY, VZ, Delta, FromEdit) -> bool:  # ksprocess3dmanipulatorsnotify_movemanipulator.html
        """
        ## MoveManipulator - Перемещение манипулятора

        ### Синтаксис Automation

        ```
        BOOL MoveManipulator( long ManipulatorId, double VX, double VY, double VZ, double Delta, BOOL FromEdit );
        ```

        ### Входные параметры

        +---------------------------------------------------------+
        | ManipulatorId | - идентификатор манипулятора,           |
        +---------------------------------------------------------+
        | VX            | - первая компонента вектора движения,   |
        +---------------------------------------------------------+
        | VY            | - вторая компонента вектора движения,   |
        +---------------------------------------------------------+
        | VZ            | - третья компонента вектора движения,   |
        +---------------------------------------------------------+
        | Delta         | - значение сдвига по вектору движения,  |
        +---------------------------------------------------------+
        | FromEdit      | - признак поворота с помощью редактора. |
        +---------------------------------------------------------+

        ### Возвращаемое значение

        +------------------------------------------------------------------------------------------------------------------------------------------------------------------+
        | TRUE  | - если библиотека обработала событие,                                                                                                                    |
        +------------------------------------------------------------------------------------------------------------------------------------------------------------------+
        | FALSE | - библиотека не обработала событие, требуется использовать встроенную обработку событий манипулятора (при этом положение фантома процесса не изменится). |
        +------------------------------------------------------------------------------------------------------------------------------------------------------------------+
        """
        ...

    def RotateManipulator(self, ManipulatorId, X0, Y0, Z0, AxisZX, AxisZXY, AxisZZ, Angle, FromEdit) -> bool:  # ksprocess3dmanipulatorsnotify_rotatemanipulator.html
        """
        ## RotateManipulator - Вращение манипулятора относительно оси на угол

        ### Синтаксис Automation

        ```
        BOOL RotateManipulator( long ManipulatorId, double X0, double Y0, double Z0, double AxisZX, double AxisZXY, double AxisZZ, double Angle, BOOL FromEdit );
        ```

        ### Входные параметры

        +---------------------------------------------------------+
        | ManipulatorId | - идентификатор манипулятора,           |
        +---------------------------------------------------------+
        | X0            | - первая координата точки,              |
        +---------------------------------------------------------+
        | Y0            | - вторая координата точки,              |
        +---------------------------------------------------------+
        | Z0            | - третья координата точки,              |
        +---------------------------------------------------------+
        | AxisZX        | - ось X,                                |
        +---------------------------------------------------------+
        | AxisZXY       | - ось Y,                                |
        +---------------------------------------------------------+
        | AxisZZ        | - ось Z,                                |
        +---------------------------------------------------------+
        | Angle         | - угол поворота.                        |
        +---------------------------------------------------------+
        | FromEdit      | - признак поворота с помощью редактора. |
        +---------------------------------------------------------+

        ### Возвращаемое значение

        +------------------------------------------------------------------------------------------------------------------------------------------------------------------+
        | TRUE  | - если библиотека обработала событие,                                                                                                                    |
        +------------------------------------------------------------------------------------------------------------------------------------------------------------------+
        | FALSE | - библиотека не обработала событие, требуется использовать встроенную обработку событий манипулятора (при этом положение фантома процесса не изменится). |
        +------------------------------------------------------------------------------------------------------------------------------------------------------------------+
        """
        ...




class ksPLMObjectNotify(IDispatch):  # ksplmobjectnotify.html
    """
    ## Интерфейс ksPLMObjectNotify

    Интерфейс событий объектов версионирования.

    Источником событий объектов версионирования является 3D документ IKompasDocument3D.

    Версия: KOMPAS v19
    """

    def PLMChangeChanged(self, Object, newValue) -> bool:  # ksplmobjectnotify_plmchangechanged.html
        """
        ## PLMChangeChanged - Изменение признака отличия

        ### Синтаксис Automation

        ```
        BOOL PLMChangeChanged( IDispatch* Object, long newValue );
        ```

        ### Входные параметры

        +--------------------------------------------------------------------------------+
        | Object   | - объект версионирования,                                           |
        +--------------------------------------------------------------------------------+
        | newValue | - новое значение признака отличия из перечисления ksPLMChangesEnum. |
        +--------------------------------------------------------------------------------+

        ### Возвращаемое значение

        +-----------------------------------------+
        | TRUE | - в случае успешного завершения. |
        +-----------------------------------------+
        """
        ...

    def PLMStatusChanged(self, Object, newValue) -> bool:  # ksplmobjectnotify_plmstatuschanged.html
        """
        ## PLMStatusChanged - Изменение статуса в системе версионирования

        ### Синтаксис Automation

        ```
        BOOL PLMStatusChanged( IDispatch* Object, long newValue );
        ```

        ### Входные параметры

        +----------------------------------------------------------------------+
        | Object   | - объект версионирования,                                 |
        +----------------------------------------------------------------------+
        | newValue | - новое значение статуса из перечисления ksPLMStatusEnum. |
        +----------------------------------------------------------------------+

        ### Возвращаемое значение

        +---------------------------+
        | TRUE | - не используется. |
        +---------------------------+
        """
        ...




class ksProcess2DNotify(IDispatch):  # ksprocess2dnotify.html
    """
    ## Интерфейс ksProcess2DNotify\\IProcess2DNotify

    Интерфейс событий для процесса 2D.
    """

    def close(self):
        ...




class ksProcess3DNotify(IDispatch):  # ksprocess3dnotify.html
    """
    ## Интерфейс ksProcess3DNotify\\IProcess3DNotify

    Интерфейс событий для процесса 3D.
    """

    def close(self):
        ...




class ksPropertyManagerNotify():  # kspropertymanagernotify.html
    """
    ## Интерфейс ksPropertyManagerNotify/IPropertyManagerNotify

    +--------------------------------------------------+
    | ksPropertyManagerNotify | - интерфейс Automation |
    +--------------------------------------------------+
    | IPropertyManagerNotify  | - интерфейс COM        |
    +--------------------------------------------------+

    Интерфейс событий Панели свойств или процессных параметров.

    Позволяют контролировать состояние Панели свойств.

    Источником событий для подписки на данный интерфейс являются объекты:

    •IPropertyManager - интерфейс панели свойств.

    •IProcessParam - интерфейс параметров процесса.
    """

    def close(self):
        ...




class ksPropertyUserControlNotify():  # kspropertyusercontrolnotify.html
    """
    ## Интерфейс ksPropertyUserControlNotify/IPropertyUserControlNotify

    +------------------------------------------------------+
    | ksPropertyUserControlNotify | - интерфейс Automation |
    +------------------------------------------------------+
    |                             | - аналога в COM нет    |
    +------------------------------------------------------+

    Интерфейс событий пользовательского элемента управления.

    Позволяют контролировать состояние элемента управления OCX.

    Источником событий для подписки на данный интерфейс является IPropertyUserControl - пользовательский элемент управления Панели свойств.
    """

    def close(self):
        ...




class ksContentDialogNotify(IDispatch):  # kscontentdialognotify.html
    """
    ## Интерфейс ksContentDialogNotify

    Интерфейс событий диалога с произвольным контентом IContentDialogParam.
    """

    def close(self):
        ...




class ksSpecificationDescriptionNotify():  # ksspecificationdescriptionnotify.html
    """
    ## Интерфейс ksSpecificationDescriptionNotify

    Интерфейс событий описания спецификации.

    Позволяют контролировать редактирование описания спецификации.

    Источником событий для подписки на данный интерфейс являются объекты:

    •ISpecificationDescriptions - Интерфейс коллекции описаний спецификации,

    •ISpecificationDescription - Интерфейс описания спецификации.
    """

    def close(self):
        ...




class ksSpecificationObjectNotify():  # ksspecificationobjectnotify.html
    """
    ## Интерфейс ksSpecificationObjectNotify

    Интерфейс событий объектов спецификации.

    Предназначен для того, чтобы следить и при необходимости управлять редактированием объектов спецификации.

    Источником событий для подписки на данный интерфейс являются объекты:

    •ISpecificationObject - Интерфейс объекта спецификации (базового или вспомогательного),

    •ISpecificationBaseObjects - Интерфейс коллекции базовых объектов спецификации,

    •ISpecificationCommentObjects - Интерфейс коллекции вспомогательных объектов спецификации.
    """

    def close(self):
        ...




class ksViewsAndLayersManagerNotify():  # ksviewsandlayersmanagernotify.html
    """
    ## Интерфейс ksViewsAndLayersManagerNotify/IViewsAndLayersManagerNotify

    Интерфейс событий менеджера видов и слоев

    +--------------------------------------------------------+
    | ksViewsAndLayersManagerNotify | - интерфейс Automation |
    +--------------------------------------------------------+
    | IViewsAndLayersManagerNotify  | - интерфейс COM        |
    +--------------------------------------------------------+

    Позволяют контролировать начало и завершение работы с диалогом менеджера видов и слоев.

    Источником событий для подписки на данный интерфейс являются объекты:

    IViewsAndLayersManager - Менеджер слоев и видов графического документа.
    """

    def close(self):
        ...




class ksFindObjectParametersNotify(IDispatch):  # ksfindobjectparametersnotify.html
    """
    ## Интерфейс ksFindObjectParametersNotify

    Интерфейс фильтрации объектов в функциях поиска объектов под курсором.
    """

    def FilterObject(self, Object) -> bool:  # ksfindobjectparametersnotify_filterobject.html
        """
        ## FilterObject - Фильтрация объектов

        ### Синтаксис Automation

        ```
        BOOL FilterObject( IDispatch* Object );
        ```

        ### Входные параметры

        +----------------------------------------------+
        | Object | - указатель на объект под курсором. |
        +----------------------------------------------+

        ### Возвращаемое значение

        +--------------------------------+
        | TRUE | - если объект подходит. |
        +--------------------------------+
        """
        ...




class IConverter(IKompasAPIObject):  # iconverter.html
    """
    ## Интерфейс IСonverter

    Конвертер файлов КОМПАС.

    1. Интерфейс позволяет управлять конвертером системы КОМПАС, задавать параметры конвертирования, получать фильтр и выполнять конвертацию документа системы КОМПАС.

    2. Данный интерфейс может быть получен от интерфейса приложения IApplication с помощью свойства IApplication::Converter.
    """

    def Convert(self, InputFile, Outfile, Command, ShowParam):  # iconverter_convert.html
        """
        ## Convert - Запустить процесс конвертации

        Если имя исходного файла документа не задано, то берется текущий документ. Если новое имя файла документа не задано, то конвертация будет происходить в новый документ системы КОМПАС.

        ### Синтаксис Automation

        ```
        long * Convert (BSTR inputFile, BSTR outfile, long command, BOOL showParam);
        ```

        ### Входные параметры

        +-----------------------------------------------------------------------------------------------------------------+
        | inputFile | - имя исходного файла документа,                                                                    |
        +-----------------------------------------------------------------------------------------------------------------+
        | outfile   | - новое имя файла документа,                                                                        |
        +-----------------------------------------------------------------------------------------------------------------+
        | command   | - номер команды,                                                                                    |
        +-----------------------------------------------------------------------------------------------------------------+
        | showParam | - TRUE - выдавать диалог параметров конвертации, FALSE - не выдавать диалог параметров конвертации. |
        +-----------------------------------------------------------------------------------------------------------------+

        ### Возвращаемое значение

        +--------------------------------------+
        | 1 | - в случае успешного завершения, |
        +--------------------------------------+
        | 0 | - в случае неудачи.              |
        +--------------------------------------+
        """
        ...

    def ConverterParameters(self, Command):  # iconverter_converterparameters.html
        """
        ## ConverterParameters - Получить интерфейс параметров конвертирования

        Данный метод позволяет получить интерфейс параметров конвертирования, который объявлен, реализован и поддерживается в самом конвертере.

        ### Синтаксис Automation

        ```
        LPDISPATCH ConverterParameters (long command);
        ```

        ### Входные параметры

        +----------------------------------------------------------------------------------+
        | command | - номер команды, для которой необходимо получить интерфейс параметров. |
        +----------------------------------------------------------------------------------+

        ### Возвращаемое значение

        +--------------------------------------------------------------------+
        | указатель на интерфейс IUnknown | - в случае успешного завершения, |
        +--------------------------------------------------------------------+
        | 0                               | - в случае неудачи.              |
        +--------------------------------------------------------------------+
        """
        ...

    def GetFilter(self, DocType, SaveAs) -> str:  # iconverter_getfilter.html
        """
        ## GetFilter - Получить фильтр и номер команды по типу документа

        ### Синтаксис Automation

        ```
        BSTR * GetFilter (long docType, BOOL saveAs, long * command);
        ```

        ### Входные параметры

        +--------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------+
        | docType | - при импорте в КОМПАС-документ - тип документа из перечисления DocumentTypeEnum, - при экспорте КОМПАС-документа - тип документа, в который производится экспорт, перечисление типов документов, должно быть задано в конверторе. Например, для конвертора в DXF и DWG используются типы: #define FORMAT_DXF 1 // ID команды для работы с DXF #define FORMAT_DWG 2 // ID команды для работы с DWG |
        +--------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------+
        | saveAs  | - TRUE - экспорт документа КОМПАС, - FALSE - импорт в документ КОМПАС.                                                                                                                                                                                                                                                                                                                             |
        +--------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------+

        ### Выходные параметры

        +----------------------------+
        | command | - номер команды. |
        +----------------------------+

        ### Возвращаемое значение

        +------------------------------------------------------------------------------------------------------------------------------------------------------------+
        | фильтр | - строка со справочным значением (например 'КОМПАС-Фрагменты (*.myfrw)|*.myfrw|'), которое используется в диалогах Открыть файл и Сохранить файл. |
        +------------------------------------------------------------------------------------------------------------------------------------------------------------+
        """
        ...

    def VisualEditConvertParam(self, ParentHwnd, Command) -> bool:  # iconverter_visualeditconvertparam.html
        """
        ## VisualEditConvertParam - Запустить визуальное редактирование параметров конвертации

        ### Синтаксис Automation

        ```
        BOOL * VisualEditConvertParam( OLE_HANDLE parentHwnd, long command );
        ```

        ### Входные параметры

        +------------------------------------------+
        | parentHwnd | - дескриптор окна родителя, |
        +------------------------------------------+
        | command    | - номер команды.            |
        +------------------------------------------+

        ### Возвращаемое значение

        +----------------------------------------------+
        | TRUE  | - выход из диалога по кнопке OK,     |
        +----------------------------------------------+
        | FALSE | - выход из диалога по кнопке Отмена. |
        +----------------------------------------------+
        """
        ...




class IAdditionConvertParameters(IKompasAPIObject):  # iadditionconvertparameters.html
    """
    ## Интерфейс IAdditionConvertParameters

    Интерфейс параметров для конвертации в дополнительные форматы.

    Интерфейс можно получить, используя свойство IConverter::ConverterParameters или метод IKompasDocument1::GetInterface.

    Версия Компас v21
    """

    @property
    def Angle(self) -> float:  # iadditionconvertparameters_angle.html
        """
        ## Angle - Максимально допустимое угловое отклонение касательных кривой или нормалей поверхности в соседних точках на расстоянии шага

        Тип данных: double

        ### Синтаксис Automation

        ```
        Angle = Object.Angle       # Получить свойство (* )
        Object.Angle = Angle       # Установить свойство (* )
        Angle = Object.GetAngle()  # Получить свойство (**)
        Object.SetAngle( Angle )   # Установить свойство (**)
        ```
        """
        ...
    @Angle.setter
    def Angle(self, value: float): ...

    @property
    def Author(self) -> str:  # iadditionconvertparameters_author.html
        """
        ## Author - Автор

        Тип данных: BSTR

        ### Синтаксис Automation

        ```
        Author = Object.Author       # Получить свойство (* )
        Object.Author = Author       # Установить свойство (* )
        Author = Object.GetAuthor()  # Получить свойство (**)
        Object.SetAuthor( Author )   # Установить свойство (**)
        ```
        """
        ...
    @Author.setter
    def Author(self, value: str): ...

    @property
    def Comment(self) -> str:  # iadditionconvertparameters_comment.html
        """
        ## Comment - Комментарий

        Тип данных: BSTR

        ### Синтаксис Automation

        ```
        Comment = Object.Comment       # Получить свойство (* )
        Object.Comment = Comment       # Установить свойство (* )
        Comment = Object.GetComment()  # Получить свойство (**)
        Object.SetComment( Comment )   # Установить свойство (**)
        ```
        """
        ...
    @Comment.setter
    def Comment(self, value: str): ...

    @property
    def Configuration(self) -> typing.Any:  # iadditionconvertparameters_configuration.html
        """
        ## Configuration - Выбранная конфигурация

        Тип данных: VARIANT

        ### Синтаксис Automation

        ```
        Configuration = Object.Configuration       # Получить свойство (* )
        Object.Configuration = Configuration       # Установить свойство (* )
        Configuration = Object.GetConfiguration()  # Получить свойство (**)
        Object.SetConfiguration( Configuration )   # Установить свойство (**)
        ```
        """
        ...
    @Configuration.setter
    def Configuration(self, value: typing.Any): ...

    @property
    def ConfigurationFileName(self) -> str:  # iadditionconvertparameters_configurationfilename.html
        """
        ## ConfigurationFileName - Путь к текущему файлу конфигурации

        Тип данных: BSTR

        ### Синтаксис Automation

        ```
        ConfigurationFileName = Object.ConfigurationFileName       # Получить свойство (* )
        Object.ConfigurationFileName = ConfigurationFileName       # Установить свойство (* )
        ConfigurationFileName = Object.GetConfigurationFileName()  # Получить свойство (**)
        Object.SetConfigurationFileName( ConfigurationFileName )   # Установить свойство (**)
        ```
        """
        ...
    @ConfigurationFileName.setter
    def ConfigurationFileName(self, value: str): ...

    @property
    def Format(self) -> int:  # iadditionconvertparameters_format.html
        """
        ## Format - Формат конвертации

        Тип данных: из перечисления ksKOMPASConverterEnum

        ### Синтаксис Automation

        ```
        Format = Object.Format       # Получить свойство (* )
        Object.Format = Format       # Установить свойство (* )
        Format = Object.GetFormat()  # Получить свойство (**)
        Object.SetFormat( Format )   # Установить свойство (**)
        ```
        """
        ...
    @Format.setter
    def Format(self, value: int): ...

    @property
    def FormatBinary(self) -> bool:  # iadditionconvertparameters_formatbinary.html
        """
        ## FormatBinary - Формат файла (двоичный или текстовый). Используется для x_t, x_b, stl

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        FormatBinary = Object.FormatBinary       # Получить свойство (* )
        Object.FormatBinary = FormatBinary       # Установить свойство (* )
        FormatBinary = Object.GetFormatBinary()  # Получить свойство (**)
        Object.SetFormatBinary( FormatBinary )   # Установить свойство (**)
        ```
        """
        ...
    @FormatBinary.setter
    def FormatBinary(self, value: bool): ...

    @property
    def Length(self) -> float:  # iadditionconvertparameters_length.html
        """
        ## Length - Максимально допустимое расстояние между соседними точками на расстоянии шага

        Тип данных: double

        ### Синтаксис Automation

        ```
        Length = Object.Length       # Получить свойство (* )
        Object.Length = Length       # Установить свойство (* )
        Length = Object.GetLength()  # Получить свойство (**)
        Object.SetLength( Length )   # Установить свойство (**)
        ```
        """
        ...
    @Length.setter
    def Length(self, value: float): ...

    @property
    def LengthUnits(self) -> int:  # iadditionconvertparameters_lengthunits.html
        """
        ## LengthUnits - Единицы измерения длины

        Тип данных: из перечисления ksLengthUnitsEnum

        ### Синтаксис Automation

        ```
        LengthUnits = Object.LengthUnits       # Получить свойство (* )
        Object.LengthUnits = LengthUnits       # Установить свойство (* )
        LengthUnits = Object.GetLengthUnits()  # Получить свойство (**)
        Object.SetLengthUnits( LengthUnits )   # Установить свойство (**)
        ```
        """
        ...
    @LengthUnits.setter
    def LengthUnits(self, value: int): ...

    @property
    def MaxTeselationCellCount(self) -> int:  # iadditionconvertparameters_maxteselationcellcount.html
        """
        ## MaxTeselationCellCount - Максимальное количество ячеек в строке и ряду триангуляционной сетки (если 0, то не задано)

        Тип данных: long

        ### Синтаксис Automation

        ```
        MaxTeselationCellCount = Object.MaxTeselationCellCount       # Получить свойство (* )
        Object.MaxTeselationCellCount = MaxTeselationCellCount       # Установить свойство (* )
        MaxTeselationCellCount = Object.GetMaxTeselationCellCount()  # Получить свойство (**)
        Object.SetMaxTeselationCellCount( MaxTeselationCellCount )   # Установить свойство (**)
        ```
        """
        ...
    @MaxTeselationCellCount.setter
    def MaxTeselationCellCount(self, value: int): ...

    @property
    def NeedCreateComponentsFiles(self) -> bool:  # iadditionconvertparameters_needcreatecomponentsfiles.html
        """
        ## NeedCreateComponentsFiles - Создавать файлы компонентов

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        NeedCreateComponentsFiles = Object.NeedCreateComponentsFiles       # Получить свойство (* )
        Object.NeedCreateComponentsFiles = NeedCreateComponentsFiles       # Установить свойство (* )
        NeedCreateComponentsFiles = Object.GetNeedCreateComponentsFiles()  # Получить свойство (**)
        Object.SetNeedCreateComponentsFiles( NeedCreateComponentsFiles )   # Установить свойство (**)
        ```
        """
        ...
    @NeedCreateComponentsFiles.setter
    def NeedCreateComponentsFiles(self, value: bool): ...

    @property
    def Organization(self) -> str:  # iadditionconvertparameters_organization.html
        """
        ## Organization - Организация

        Тип данных: BSTR

        ### Синтаксис Automation

        ```
        Organization = Object.Organization       # Получить свойство (* )
        Object.Organization = Organization       # Установить свойство (* )
        Organization = Object.GetOrganization()  # Получить свойство (**)
        Object.SetOrganization( Organization )   # Установить свойство (**)
        ```
        """
        ...
    @Organization.setter
    def Organization(self, value: str): ...

    @property
    def Password(self) -> str:  # iadditionconvertparameters_password.html
        """
        ## Password - Пароль

        Тип данных: BSTR

        ### Синтаксис Automation

        ```
        Password = Object.Password       # Получить свойство (* )
        Object.Password = Password       # Установить свойство (* )
        Password = Object.GetPassword()  # Получить свойство (**)
        Object.SetPassword( Password )   # Установить свойство (**)
        ```
        """
        ...
    @Password.setter
    def Password(self, value: str): ...

    @property
    def Placement(self) -> IPlacement3D:  # iadditionconvertparameters_placement.html
        """
        ## Placement - ЛСК, относительно которой позиционирована модель

        Тип данных: Указатель на интерфейс IPlacement3D

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        Placement = Object.Placement       # Получить свойство (* )
        Placement = Object.GetPlacement()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def SaveResultDocument(self) -> bool:  # iadditionconvertparameters_saveresultdocument.html
        """
        ## SaveResultDocument - Сохранить полученный документ

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        SaveResultDocument = Object.SaveResultDocument       # Получить свойство (* )
        Object.SaveResultDocument = SaveResultDocument       # Установить свойство (* )
        SaveResultDocument = Object.GetSaveResultDocument()  # Получить свойство (**)
        Object.SetSaveResultDocument( SaveResultDocument )   # Установить свойство (**)
        ```
        """
        ...
    @SaveResultDocument.setter
    def SaveResultDocument(self, value: bool): ...

    @property
    def Step(self) -> float:  # iadditionconvertparameters_step.html
        """
        ## Step - Максимально допустимый прогиб кривой или поверхности в соседних точках на расстоянии шага

        Тип данных: double

        ### Синтаксис Automation

        ```
        Step = Object.Step       # Получить свойство (* )
        Object.Step = Step       # Установить свойство (* )
        Step = Object.GetStep()  # Получить свойство (**)
        Object.SetStep( Step )   # Установить свойство (**)
        ```
        """
        ...
    @Step.setter
    def Step(self, value: float): ...

    @property
    def StepType(self) -> int:  # iadditionconvertparameters_steptype.html
        """
        ## StepType - Способ вычисления приращения параметра при движении по объекту

        Тип данных: из перечисления ksStepTypeEnum

        ### Синтаксис Automation

        ```
        StepType = Object.StepType       # Получить свойство (* )
        Object.StepType = StepType       # Установить свойство (* )
        StepType = Object.GetStepType()  # Получить свойство (**)
        Object.SetStepType( StepType )   # Установить свойство (**)
        ```
        """
        ...
    @StepType.setter
    def StepType(self, value: int): ...

    @property
    def StitchPrecision(self) -> float:  # iadditionconvertparameters_stitchprecision.html
        """
        ## StitchPrecision - Точность сшивки поверхностей

        Тип данных: double

        ### Синтаксис Automation

        ```
        StitchPrecision = Object.StitchPrecision       # Получить свойство (* )
        Object.StitchPrecision = StitchPrecision       # Установить свойство (* )
        StitchPrecision = Object.GetStitchPrecision()  # Получить свойство (**)
        Object.SetStitchPrecision( StitchPrecision )   # Установить свойство (**)
        ```
        """
        ...
    @StitchPrecision.setter
    def StitchPrecision(self, value: float): ...

    @property
    def StitchSurfaces(self) -> bool:  # iadditionconvertparameters_stitchsurfaces.html
        """
        ## StitchSurfaces - Сшивать поверхности

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        StitchSurfaces = Object.StitchSurfaces       # Получить свойство (* )
        Object.StitchSurfaces = StitchSurfaces       # Установить свойство (* )
        StitchSurfaces = Object.GetStitchSurfaces()  # Получить свойство (**)
        Object.SetStitchSurfaces( StitchSurfaces )   # Установить свойство (**)
        ```
        """
        ...
    @StitchSurfaces.setter
    def StitchSurfaces(self, value: bool): ...

    @property
    def TextExportForm(self) -> int:  # iadditionconvertparameters_textexportform.html
        """
        ## TextExportForm - Способ экспорта текстов размеров и обозначений

        Тип данных: из перечисления ksTextExportFormEnum

        ### Синтаксис Automation

        ```
        TextExportForm = Object.TextExportForm       # Получить свойство (* )
        Object.TextExportForm = TextExportForm       # Установить свойство (* )
        TextExportForm = Object.GetTextExportForm()  # Получить свойство (**)
        Object.SetTextExportForm( TextExportForm )   # Установить свойство (**)
        ```
        """
        ...
    @TextExportForm.setter
    def TextExportForm(self, value: int): ...

    @property
    def TopolgyIncluded(self) -> bool:  # iadditionconvertparameters_topolgyincluded.html
        """
        ## TopolgyIncluded - Включить топологию. Используется для igs

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        TopolgyIncluded = Object.TopolgyIncluded       # Получить свойство (* )
        Object.TopolgyIncluded = TopolgyIncluded       # Установить свойство (* )
        TopolgyIncluded = Object.GetTopolgyIncluded()  # Получить свойство (**)
        Object.SetTopolgyIncluded( TopolgyIncluded )   # Установить свойство (**)
        ```
        """
        ...
    @TopolgyIncluded.setter
    def TopolgyIncluded(self, value: bool): ...

    def Clear(self) -> bool:  # iadditionconvertparameters_clear.html
        """
        ## Clear - Сбросить параметры

        ### Синтаксис Automation

        ```
        BOOL Clear();
        ```

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def LoadConfigurationFile(self, FileName) -> bool:  # iadditionconvertparameters_loadconfigurationfile.html
        """
        ## LoadConfigurationFile - Загрузить конфигурацию из файла

        ### Синтаксис Automation

        ```
        BOOL LoadConfigurationFile( BSTR FileName );
        ```

        ### Входные параметры

        +--------------------------------------+
        | FileName | - имя файла конфигурации. |
        +--------------------------------------+

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def ObjectsOptions(self, Option) -> bool:  # iadditionconvertparameters_objectsoptions.html
        """
        ## ObjectsOptions - Признаки чтения/записи объектов модели

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        ObjectsOptions = Object.ObjectsOptions( Option )     # Получить свойство (* )
        Object.ObjectsOptions( Option ) = ObjectsOptions     # Установить свойство (* )
        ObjectsOptions = Object.GetObjectsOptions( Option )  # Получить свойство (**)
        Object.SetObjectsOptions( Option, ObjectsOptions )   # Установить свойство (**)
        ```

        ### Входные параметры

        +-------------------------------------------------------------------+
        | Option | - тип объектов из перечисления ksD3ConverterOptionsEnum. |
        +-------------------------------------------------------------------+
        """
        ...

    def SaveConfigurationFile(self, FileName) -> bool:  # iadditionconvertparameters_saveconfigurationfile.html
        """
        ## SaveConfigurationFile - Сохранить конфигурацию в файл

        ### Синтаксис Automation

        ```
        BOOL SaveConfigurationFile( BSTR FileName );
        ```

        ### Входные параметры

        +--------------------------------------+
        | FileName | - имя файла конфигурации. |
        +--------------------------------------+

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def SetObjectsOptions(self, Option, arg1):
        ...




class IRasterConvertParameters(IKompasAPIObject):  # irasterconvertparameters.html
    """
    ## Интерфейс IRasterConvertParameters

    Интерфейс параметров для конвертации в растровые форматы.

    Интерфейс можно получить, используя свойство IConverter::ConverterParameters или метод IKompasDocument1::GetInterface.

    Версия Компас v21
    """

    @property
    def ColorBPP(self) -> int:  # irasterconvertparameters_colorbpp.html
        """
        ## ColorBPP - Глубина цвета (бит на пиксел)

        Тип данных: из перечисления ksColorBPPEnum

        ### Синтаксис Automation

        ```
        ColorBPP = Object.ColorBPP       # Получить свойство (* )
        Object.ColorBPP = ColorBPP       # Установить свойство (* )
        ColorBPP = Object.GetColorBPP()  # Получить свойство (**)
        Object.SetColorBPP( ColorBPP )   # Установить свойство (**)
        ```
        """
        ...
    @ColorBPP.setter
    def ColorBPP(self, value: int): ...

    @property
    def ColorType(self) -> int:  # irasterconvertparameters_colortype.html
        """
        ## ColorType - Тип цвета объектов

        Тип данных: из перечисления ksObjectColorTypeEnum

        ### Синтаксис Automation

        ```
        ColorType = Object.ColorType       # Получить свойство (* )
        Object.ColorType = ColorType       # Установить свойство (* )
        ColorType = Object.GetColorType()  # Получить свойство (**)
        Object.SetColorType( ColorType )   # Установить свойство (**)
        ```
        """
        ...
    @ColorType.setter
    def ColorType(self, value: int): ...

    @property
    def GreyScale(self) -> bool:  # irasterconvertparameters_greyscale.html
        """
        ## GreyScale - Оттенки серого

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        GreyScale = Object.GreyScale       # Получить свойство (* )
        Object.GreyScale = GreyScale       # Установить свойство (* )
        GreyScale = Object.GetGreyScale()  # Получить свойство (**)
        Object.SetGreyScale( GreyScale )   # Установить свойство (**)
        ```
        """
        ...
    @GreyScale.setter
    def GreyScale(self, value: bool): ...

    @property
    def MultiPageOutput(self) -> int:  # irasterconvertparameters_multipageoutput.html
        """
        ## MultiPageOutput - Cохранять все листы в одном файле

        Тип данных: из перечисления ksMultiPageOutputEnum

        ### Синтаксис Automation

        ```
        MultiPageOutput = Object.MultiPageOutput       # Получить свойство (* )
        Object.MultiPageOutput = MultiPageOutput       # Установить свойство (* )
        MultiPageOutput = Object.GetMultiPageOutput()  # Получить свойство (**)
        Object.SetMultiPageOutput( MultiPageOutput )   # Установить свойство (**)
        ```
        """
        ...
    @MultiPageOutput.setter
    def MultiPageOutput(self, value: int): ...

    @property
    def OnlyThinLine(self) -> bool:  # irasterconvertparameters_onlythinline.html
        """
        ## OnlyThinLine - Только тонкими линиями

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        OnlyThinLine = Object.OnlyThinLine       # Получить свойство (* )
        Object.OnlyThinLine = OnlyThinLine       # Установить свойство (* )
        OnlyThinLine = Object.GetOnlyThinLine()  # Получить свойство (**)
        Object.SetOnlyThinLine( OnlyThinLine )   # Установить свойство (**)
        ```
        """
        ...
    @OnlyThinLine.setter
    def OnlyThinLine(self, value: bool): ...

    @property
    def RasterFormat(self) -> int:  # irasterconvertparameters_rasterformat.html
        """
        ## RasterFormat - Формат растра

        Тип данных: из перечисления ksRasterFormatEnum

        ### Синтаксис Automation

        ```
        RasterFormat = Object.RasterFormat       # Получить свойство (* )
        Object.RasterFormat = RasterFormat       # Установить свойство (* )
        RasterFormat = Object.GetRasterFormat()  # Получить свойство (**)
        Object.SetRasterFormat( RasterFormat)    # Установить свойство (**)
        ```
        """
        ...
    @RasterFormat.setter
    def RasterFormat(self, value: int): ...

    @property
    def Resolution(self) -> int:  # irasterconvertparameters_resolution.html
        """
        ## Resolution - Разрешение экрана

        Тип данных: long

        ### Синтаксис Automation

        ```
        Resolution = Object.Resolution       # Получить свойство (* )
        Object.Resolution = Resolution       # Установить свойство (* )
        Resolution = Object.GetResolution()  # Получить свойство (**)
        Object.SetResolution( Resolution )   # Установить свойство (**)
        ```
        """
        ...
    @Resolution.setter
    def Resolution(self, value: int): ...

    @property
    def ResultArrayBytes(self) -> typing.Any:  # irasterconvertparameters_resultarraybytes.html
        """
        ## ResultArrayBytes - Результат сохранения в растр в виде массива байт

        Тип данных: VARIANT

        Для одностраничного растра возвращается массив байт VT_ARRAY | VT_UI1.

        Для многостраничного растра возвращается массив VT_ARRAY | VT_VARIANT - каждая страница добавляется отдельным элементом в массив в виде массива байт VT_ARRAY | VT_UI1.

        ### Синтаксис Automation

        ```
        ResultArrayBytes = Object.ResultArrayBytes       # Получить свойство (* )
        Object.ResultArrayBytes = ResultArrayBytes       # Установить свойство (* )
        ResultArrayBytes = Object.GetResultArrayBytes()  # Получить свойство (**)
        Object.SetResultArrayBytes( ResultArrayBytes )   # Установить свойство (**)
        ```
        """
        ...
    @ResultArrayBytes.setter
    def ResultArrayBytes(self, value: typing.Any): ...

    @property
    def ReturnResultAsArrayBytes(self) -> bool:  # irasterconvertparameters_returnresultasarraybytes.html
        """
        ## ReturnResultAsArrayBytes - Вернуть результат массивом байт

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        ReturnResultAsArrayBytes = Object.ReturnResultAsArrayBytes       # Получить свойство (* )
        Object.ReturnResultAsArrayBytes = ReturnResultAsArrayBytes       # Установить свойство (* )
        ReturnResultAsArrayBytes = Object.GetReturnResultAsArrayBytes()  # Получить свойство (**)
        Object.SetReturnResultAsArrayBytes( ReturnResultAsArrayBytes )   # Установить свойство (**)
        ```
        """
        ...
    @ReturnResultAsArrayBytes.setter
    def ReturnResultAsArrayBytes(self, value: bool): ...

    @property
    def SaveWorkArea(self) -> bool:  # irasterconvertparameters_saveworkarea.html
        """
        ## SaveWorkArea - Cохранить отображение рабочей области

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        SaveWorkArea = Object.SaveWorkArea       # Получить свойство (* )
        Object.SaveWorkArea = SaveWorkArea       # Установить свойство (* )
        SaveWorkArea = Object.GetSaveWorkArea()  # Получить свойство (**)
        Object.SetSaveWorkArea( SaveWorkArea )   # Установить свойство (**)
        ```
        """
        ...
    @SaveWorkArea.setter
    def SaveWorkArea(self, value: bool): ...

    @property
    def Scale(self) -> float:  # irasterconvertparameters_scale.html
        """
        ## Scale - Масштаб

        Тип данных: double

        ### Синтаксис Automation

        ```
        Scale = Object.Scale       # Получить свойство (* )
        Object.Scale = Scale       # Установить свойство (* )
        Scale = Object.GetScale()  # Получить свойство (**)
        Object.SetScale( Scale )   # Установить свойство (**)
        ```
        """
        ...
    @Scale.setter
    def Scale(self, value: float): ...

    @property
    def Sheets(self) -> typing.Any:  # irasterconvertparameters_sheets.html
        """
        ## Sheets - Cписок диапазонов выводимых листов

        Тип данных: VARIANT

        ### Синтаксис Automation

        ```
        Sheets = Object.Sheets       # Получить свойство (* )
        Object.Sheets = Sheets       # Установить свойство (* )
        Sheets = Object.GetSheets()  # Получить свойство (**)
        Object.SetSheets( Sheets )   # Установить свойство (**)
        ```
        """
        ...
    @Sheets.setter
    def Sheets(self, value: typing.Any): ...

    @property
    def SheetsRange(self) -> int:  # irasterconvertparameters_sheetsrange.html
        """
        ## SheetsRange - Диапазон листов документа

        Тип данных: из перечисления ksSheetsRangeEnum

        ### Синтаксис Automation

        ```
        SheetsRange = Object.SheetsRange       # Получить свойство (* )
        Object.SheetsRange = SheetsRange       # Установить свойство (* )
        SheetsRange = Object.GetSheetsRange()  # Получить свойство (**)
        Object.SetSheetsRange( SheetsRange )   # Установить свойство (**)
        ```
        """
        ...
    @SheetsRange.setter
    def SheetsRange(self, value: int): ...

    @property
    def Uncompressed(self) -> bool:  # irasterconvertparameters_uncompressed.html
        """
        ## Uncompressed - Не сжимать изображение при сохранении в растр

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        Uncompressed = Object.Uncompressed       # Получить свойство (* )
        Object.Uncompressed = Uncompressed       # Установить свойство (* )
        Uncompressed = Object.GetUncompressed()  # Получить свойство (**)
        Object.SetUncompressed( Uncompressed )   # Установить свойство (**)
        ```
        """
        ...
    @Uncompressed.setter
    def Uncompressed(self, value: bool): ...

    def Clear(self) -> bool:  # irasterconvertparameters_clear.html
        """
        ## Clear - Очистить параметры

        ### Синтаксис Automation

        ```
        BOOL Clear();
        ```

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...




class IPrintJob(IKompasAPIObject):  # iprintjob.html
    """
    ## Интерфейс IPrintJob

    Интерфейс задания на печать.

    Интерфейс позволяет устанавливать и получать параметры задания на печать.

    Термины:

    Лист - лист документа.

    Страница устройства печати - область печати при заданном размере листов бумаги печатающего устройства.

    1. Интерфейс можно получить у интерфейса приложения КОМПАС с помощью свойства IApplication::PrintJob.

    2. Посредством вызова метода IUnknown::QueryInterface (const GUID far& iid, void** pif) у данного интерфейса можно получить дополнительный интерфейс IPrintJob_OutputParameters.
    """

    @property
    def PagesCount(self) -> int:  # iprintjob_pagescount.html
        """
        ## PagesCount - Количество страниц печати

        Тип данных: long

        1. Свойство позволяет получать текущее количество страниц устройства печати c учетом выключенных.

        2. Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        PagesCount = Object.PagesCount       # Получить свойство (* )
        PagesCount = Object.GetPagesCount()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def SheetsCount(self) -> int:  # iprintjob_sheetscount.html
        """
        ## SheetsCount - Количество листов

        Тип данных: long

        1. Свойство позволяет получать количество загруженных листов документов в задании на печать.

        2. Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        SheetsCount = Object.SheetsCount       # Получить свойство (* )
        SheetsCount = Object.GetSheetsCount()  # Получить свойство (**)
        ```
        """
        ...

    def AddSheets(self, FileName, Sheets, Range) -> bool:  # iprintjob_addsheets.html
        """
        ## AddSheets - Добавить листы указанного документа

        1. Метод позволяет добавить в задание на печать листы указанного документа.

        2. Если вместо имени файла передается пустая строка, то добавляются листы активного документа.

        ### Синтаксис Automation

        ```
        BOOL AddSheets( BSTR FileName,
        VARIANT Sheets,
        long Range );
        ```

        ### Входные параметры

        +-----------------------------------------------------------------------+
        | FileName | - имя файла, из которого надо добавить листы,              |
        +-----------------------------------------------------------------------+
        | Sheets   | - массив VT_ARRAY | VT_DISPATCH листов документа,          |
        +-----------------------------------------------------------------------+
        | Range    | - тип диапазона страниц из перечисления ksSheetsRangeEnum. |
        +-----------------------------------------------------------------------+

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def Clear(self) -> bool:  # iprintjob_clear.html
        """
        ## Clear - Очистить здание на печать

        Метод позволяет очистить задание на печать и привести все настройки в умолчательное состояние.

        ### Синтаксис Automation

        ```
        BOOL Clear();
        ```

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def Execute(self, OutputFileName) -> bool:  # iprintjob_execute.html
        """
        ## Execute - Отправить задание на печать на устройство печати

        1. Метод позволяет отправить задание на печать на устройство печати.

        2. Чтобы отправить задание на печать на текущий принтер, следует в качестве параметра OutputFileName прислать пустую строку.

        ### Синтаксис Automation

        ```
        BOOL Execute( BSTR OutputFileName );
        ```

        ### Входные параметры

        +-------------------------------------------+
        | OutputFileName | - имя устройства печати. |
        +-------------------------------------------+

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def GetPageGabarites(self) -> bool:  # iprintjob_getpagegabarites.html
        """
        ## GetPageGabarites - Получить габариты страницы устройства печати

        Метод позволяет получить габариты страницы устройства печати.

        ### Синтаксис Automation

        ```
        BOOL GetPageGabarites( double * Width
        double * Height );
        ```

        ### Выходные параметры

        +-----------------------------+
        | Width  | - ширина страницы, |
        +-----------------------------+
        | Height | - высота страницы. |
        +-----------------------------+

        ### Возвращаемое значение

        +------------------------------------------------------------------------+
        | TRUE  | - в случае успешного завершения,                               |
        +------------------------------------------------------------------------+
        | FALSE | - в случае неудачи (если листа с таким номером не существует). |
        +------------------------------------------------------------------------+
        """
        ...

    def GetPagesMapGabarites(self) -> bool:  # iprintjob_getpagesmapgabarites.html
        """
        ## GetPagesMapGabarites - Получить размеры карты страниц

        Метод позволяет получить размеры карты страниц - количество страниц по вертикали и горизонтали.

        ### Синтаксис Automation

        ```
        BOOL GetPagesMapGabarites( long * WPageCount,
        long * HPageCount );
        ```

        ### Выходные параметры

        +---------------------------------------------------+
        | WPageCount | - количество страниц по горизонтали, |
        +---------------------------------------------------+
        | HPageCount | - количество страниц по вертикали.   |
        +---------------------------------------------------+

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def Load(self, FileName) -> bool:  # iprintjob_load.html
        """
        ## Load - Загрузить задание на печать из файла

        1. Метод позволяет загрузить задание на печать из файла *.pjd.

        2. FALSE возвращается в следующих случаях:

        •файл не найден,

        •файл поврежден,

        •файл имеет неправильный формат,

        •загрузка файла была прервана из-за отсутствия данных,

        •ни одного листа ни одного документа загрузить в задание не удалось.

        3. Если задание загружено, возвращается TRUE, даже если при загрузке возникли некритические ошибки.

        ### Синтаксис Automation

        ```
        BOOL Load( BSTR FileName,
        VARIANT * Errors );
        ```

        ### Входные параметры

        +-------------------------------------------+
        | FileName | - имя файла задания на печать. |
        +-------------------------------------------+

        ### Выходные параметры

        +------------------------------------------------------------+
        | Errors | - массив VT_ARRAY | VT_BSTR сообщений об ошибках. |
        +------------------------------------------------------------+

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def PagePrintableFlag(self, WPage, HPage) -> bool:  # iprintjob_pageprintableflag.html
        """
        ## PagePrintableFlag - Признак печатаемости страницы по ее индексам

        Тип данных: BOOL

        1. Свойство позволяет включить/отключить печать страницы по ее индексам (по горизонтали и вертикали). Если индексы заданы вне диапазона, возвращается FALSE.

        2. Состояние страницы сбросится, если в результате перемещения листов какой-либо из ее индексов станет не действительным.

        ### Синтаксис Automation

        ```
        PagePrintableFlag = Object.PagePrintableFlag( WPage, HPage )     # Получить свойство (* )
        Object.PagePrintableFlag( WPage, HPage ) = PagePrintableFlag     # Установить свойство (* )
        PagePrintableFlag = Object.GetPagePrintableFlag( WPage, HPage )  # Получить свойство (**)
        Object.SetPagePrintableFlag( WPage, HPage, DimensionLineScale )  # Установить свойство (**)
        ```

        ### Входные параметры

        +------------------------------------------------+
        | long WPage | - индекс страницы по горизонтали, |
        +------------------------------------------------+
        | long HPage | - индекс страницы по вертикали.   |
        +------------------------------------------------+
        """
        ...

    def RemoveSheets(self, Index) -> bool:  # iprintjob_removesheets.html
        """
        ## RemoveSheets - Удалить лист из задания на печать

        Метод позволяет удалить лист из задания на печать.

        ### Синтаксис Automation

        ```
        BOOL RemoveSheets( VARIANT Index );
        ```

        ### Входные параметры

        +-------------------------+
        | Index | - индекс листа. |
        +-------------------------+

        ### Возвращаемое значение

        +------------------------------------------------------------------------+
        | TRUE  | - в случае успешного завершения,                               |
        +------------------------------------------------------------------------+
        | FALSE | - в случае неудачи (если листа с таким номером не существует). |
        +------------------------------------------------------------------------+
        """
        ...

    def Save(self, FileName) -> bool:  # iprintjob_save.html
        """
        ## Save - Сохранить задание на печать в файл

        Метод позволяет сохранить задание на печать в файл *.pjd.

        ### Синтаксис Automation

        ```
        BOOL Save( BSTR FileName,
        VARIANT * Errors );
        ```

        ### Входные параметры

        +-------------------------------------------+
        | FileName | - имя файла задания на печать. |
        +-------------------------------------------+

        ### Выходные параметры

        +---------------------------------------------------------+
        | Errors | - массив VT_ARRAY | VT_BSTR, если есть ошибки. |
        +---------------------------------------------------------+

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def SetPagePrintableFlag(self, WPage, HPage, arg2):
        ...

    def Sheet(self, Index) -> IPrintJob_Sheet:  # iprintjob_sheet_1.html
        """
        ## Sheet - Интерфейс листа документа

        Тип данных: указатель на интерфейс листа документа IPrintJob_Sheet

        1. Свойство позволяет получать интерфейс листа документа.

        2. Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        Sheet = Object.Sheet ( Index )    # Получить свойство (* )
        Sheet = Object.GetSheet( Index )  # Получить свойство (**)
        ```

        ### Входные параметры

        +---------------------------------------------------+
        | long Index | - индекс листа, нумерация с единицы. |
        +---------------------------------------------------+
        """
        ...

    def ShowPreviewWindow(self) -> bool:  # iprintjob_showpreviewwindow.html
        """
        ## ShowPreviewWindow - Показать окно предварительного просмотра перед печатью

        1. Метод позволяет показать окно предварительного просмотра перед печатью с текущим состоянием задания на печать.

        2. После закрытия окна предварительного просмотра задание на печать очищается, аналогично вызову метода IPrintJob::Clear.

        ### Синтаксис Automation

        ```
        BOOL ShowPreviewWindow();
        ```

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def SpecialExecute(self, OutputFileName) -> bool:  # iprintjob_specialexecute.html
        """
        ## SpecialExecute - Отправить задание на специальную печать

        Чтобы отправить задание на печать на текущий принтер, следует в качестве параметра OutputFileName прислать пустую строку.

        ### Синтаксис Automation

        ```
        BOOL SpecialExecute( BSTR OutputFileName );
        ```

        ### Входные параметры

        +-------------------------------------------+
        | OutputFileName | - имя устройства печати. |
        +-------------------------------------------+

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+

        Метод позволяет отправить задание на полистную печать на устройство печати.
        """
        ...




class IPrintJob_Sheet(IKompasAPIObject):  # iprintjob_sheet_.html
    """
    ## Интерфейс IPrintJob_Sheet

    Задание на печать: Интерфейс листа документа.

    Интерфейс позволяет устанавливать и получать параметры листа документа в задании на печать.

    Интерфейс можно получить у интерфейса задания на печать с помощью свойства IPrintrJob_Sheet.
    """

    @property
    def ClipFlag(self) -> bool:  # iprintjob_sheet_clipflag.html
        """
        ## ClipFlag - Получить признак вывода части листа документа

        Тип данных: BOOL

        Свойство позволяет устанавливать и получать признак вывода части листа документа.

        ### Синтаксис Automation

        ```
        ClipFlag = Object.ClipFlag       # Получить свойство (* )
        Object.ClipFlag = ClipFlag       # Установить свойство (* )
        ClipFlag = Object.GetClipFlag()  # Получить свойство (**)
        Object.SetClipFlag( ClipFlag )   # Установить свойство (**)
        ```
        """
        ...
    @ClipFlag.setter
    def ClipFlag(self, value: bool): ...

    @property
    def DocumentName(self) -> str:  # iprintjob_sheet_documentname.html
        """
        ## DocumentName - Имя документа-владельца листа

        Тип данных: BSTR

        1. Свойство позволяет получать имя документа-владельца листа.

        2. Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        DocumentName = Object.DocumentName       # Получить свойство (* )
        DocumentName = Object.GetDocumentName()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def Number(self) -> int:  # iprintjob_sheet_number.html
        """
        ## Number - Номер листа документа

        Тип данных: long

        1. Свойство позволяет получать номер листа документа.

        2. Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        Number = Object.Number       # Получить свойство (* )
        Number = Object.GetNumber()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def Orientation(self) -> int:  # iprintjob_sheet_orientation.html
        """
        ## Orientation - Получить текущую ориентацию листа

        Тип данных: из перечисления ksAngleEnum

        Свойство позволяет устанавливать и получать текущую ориентацию листа.

        ### Синтаксис Automation

        ```
        Orientation = Object.Orientation       # Получить свойство (* )
        Object.Orientation = Orientation       # Установить свойство (* )
        Orientation = Object.GetOrientation()  # Получить свойство (**)
        Object.SetOrientation( Orientation )   # Установить свойство (**)
        ```
        """
        ...
    @Orientation.setter
    def Orientation(self, value: int): ...

    @property
    def Scale(self) -> float:  # iprintjob_sheet_scale.html
        """
        ## Scale - Задать масштаб листа

        Тип данных: double

        Свойство позволяет устанавливать и получать масштаб листа.

        ### Синтаксис Automation

        ```
        Scale = Object.Scale       # Получить свойство (* )
        Object.Scale = Scale       # Установить свойство (* )
        Scale = Object.GetScale()  # Получить свойство (**)
        Object.SetScale( Scale )   # Установить свойство (**)
        ```
        """
        ...
    @Scale.setter
    def Scale(self, value: float): ...

    @property
    def X(self) -> float:  # iprintjob_sheet_x.html
        """
        ## X - Координата Х левого нижнего угла листа

        Тип данных: double

        Свойство позволяет устанавливать и получать координату Х левого нижнего угла листа.

        ### Синтаксис Automation

        ```
        X = Object.X       # Получить свойство (* )
        Object.X = X       # Установить свойство (* )
        X = Object.GetX()  # Получить свойство (**)
        Object.SetX( X )   # Установить свойство (**)
        ```
        """
        ...
    @X.setter
    def X(self, value: float): ...

    @property
    def Y(self) -> float:  # iprintjob_sheet_y.html
        """
        ## Y - Координата Y левого нижнего угла листа

        Тип данных: double

        Свойство позволяет устанавливать и получать координату Y левого нижнего угла листа.

        ### Синтаксис Automation

        ```
        Y = Object.Y       # Получить свойство (* )
        Object.Y = Y       # Установить свойство (* )
        Y = Object.GetY()  # Получить свойство (**)
        Object.SetY( Y )   # Установить свойство (**)
        ```
        """
        ...
    @Y.setter
    def Y(self, value: float): ...

    def GetClipFrameGabarites(self) -> bool:  # iprintjob_sheet_getclipframegabarites.html
        """
        ## GetClipFrameGabarites - Получить выводимые на печать габариты листа

        Метод позволяет получить выводимые на печать габариты листа.

        ### Синтаксис Automation

        ```
        BOOL GetClipFrameGabarites( double * Left,
        double * Bottom,
        double * Right,
        double * Top );
        ```

        ### Выходные параметры

        +----------------------------------------------+
        | Left, Bottom, Right, Top | - габариты рамки. |
        +----------------------------------------------+

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def GetGabarites(self) -> bool:  # iprintjob_sheet_getgabarites.html
        """
        ## GetGabarites - Получить габариты листа

        Метод позволяет получить габариты листа.

        ### Синтаксис Automation

        ```
        BOOL GetGabarites( double * Width,
        double * Height );
        ```

        ### Выходные параметры

        +--------------------------+
        | Width  | - ширина листа, |
        +--------------------------+
        | Height | - высота листа. |
        +--------------------------+

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def SetClipFrameGabarites(self, Left, Bottom, Right, Top) -> bool:  # iprintjob_sheet_setclipframegabarites.html
        """
        ## SetClipFrameGabarites - Установить выводимые на печать габариты листа

        1. Метод позволяет установить выводимые на печать габариты листа.

        2. Никакой из габаритов рамки не может быть больше соответствующего ему габарита листа.

        ### Синтаксис Automation

        ```
        BOOL SetClipFrameGabarites( double Left,
        double Bottom,
        double Right,
        double Top );
        ```

        ### Входные параметры

        +----------------------------------------------+
        | Left, Bottom, Right, Top | - габариты рамки. |
        +----------------------------------------------+

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...




class IPrintJob_OutputParameters(IKompasAPIObject):  # iprintjob_outputparameters.html
    """
    ## Интерфейс IPrintJob_OutputParameters

    Задание на печать:Интерфейс параметров вывода.

    Интерфейс позволяет устанавливать и получать параметры вывода задания на печать.

    Интерфейс является дополнительным к интерфейсу IPrintJob.
    """

    @property
    def AccuracyModelOutput(self) -> int:  # iprintjob_outputparameters_accuracymodeloutput.html
        """
        ## AccuracyModelOutput - Точность вывода моделей

        Тип данных: long

        1. Свойство позволяет устанавливать и получать точность вывода моделей.

        2. Если задаваемое значение находится вне диапазона, то его значение корректируется к ближайшему (минимальному или максимальному) значению.

        ### Синтаксис Automation

        ```
        AccuracyModelOutput = Object.AccuracyModelOutput                # Получить свойство (* )
        Object.AccuracyModelOutput = AccuracyModelOutput                # Установить свойство (* )
        AccuracyModelOutput = Object.GetAccuracyModelOutput()           # Получить свойство (**)
        Object.SetAlternativeFillingOutput( AlternativeFillingOutput )  # Установить свойство (**)
        ```
        """
        ...
    @AccuracyModelOutput.setter
    def AccuracyModelOutput(self, value: int): ...

    @property
    def AlternativeFillingOutput(self) -> bool:  # iprintjob_outputparameters_alternativefillingoutput.html
        """
        ## AlternativeFillingOutput - Альтернативный способ вывода заливок

        Тип данных: BOOL

        Свойство позволяет устанавливать и получать признак альтернативного способа вывода заливок.

        ### Синтаксис Automation

        ```
        AlternativeFillingOutput = Object.AlternativeFillingOutput       # Получить свойство (* )
        Object.AlternativeFillingOutput = AlternativeFillingOutput       # Установить свойство (* )
        AlternativeFillingOutput = Object.GetAlternativeFillingOutput()  # Получить свойство (**)
        Object.SetAlternativeFillingOutput( AlternativeFillingOutput )   # Установить свойство (**)
        ```
        """
        ...
    @AlternativeFillingOutput.setter
    def AlternativeFillingOutput(self, value: bool): ...

    @property
    def AutoScale(self) -> bool:  # iprintjob_outputparameters_autoscale.html
        """
        ## AutoScale - Автоподгонка масштаба при добавлении листов

        Тип данных: BOOL

        Свойство позволяет устанавливать и получать признак автоподгонки масштаба при добавлении листов.

        ### Синтаксис Automation

        ```
        AutoScale = Object.AutoScale       # Получить свойство (* )
        Object.AutoScale = AutoScale       # Установить свойство (* )
        AutoScale = Object.GetAutoScale()  # Получить свойство (**)
        Object.SetAutoScale( AutoScale )   # Установить свойство (**)
        ```
        """
        ...
    @AutoScale.setter
    def AutoScale(self, value: bool): ...

    @property
    def CatchSpacing(self) -> int:  # iprintjob_outputparameters_catchspacing.html
        """
        ## CatchSpacing - Зазор между листами

        Тип данных: long

        Свойство позволяет устанавливать и получать зазор между листами при привязке к углам листов, мм.

        ### Синтаксис Automation

        ```
        CatchSpacing = Object.CatchSpacing       # Получить свойство (* )
        Object.CatchSpacing = CatchSpacing       # Установить свойство (* )
        CatchSpacing = Object.GetCatchSpacing()  # Получить свойство (**)
        Object.SetCatchSpacing( CatchSpacing )   # Установить свойство (**)
        ```
        """
        ...
    @CatchSpacing.setter
    def CatchSpacing(self, value: int): ...

    @property
    def CollateCopies(self) -> bool:  # iprintjob_outputparameters_collatecopies.html
        """
        ## CollateCopies - Копии в подбор

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        CollateCopies = Object.CollateCopies       # Получить свойство (* )
        Object.CollateCopies = CollateCopies       # Установить свойство (* )
        CollateCopies = Object.GetCollateCopies()  # Получить свойство (**)
        Object.SetCollateCopies( CollateCopies )   # Установить свойство (**)
        ```
        """
        ...
    @CollateCopies.setter
    def CollateCopies(self, value: bool): ...

    @property
    def Color(self) -> int:  # iprintjob_outputparameters_color.html
        """
        ## Color - Цвет вывода

        Тип данных: из перечисления ksOutputColorTypeEnum

        Свойство позволяет устанавливать и получать цвет вывода.

        ### Синтаксис Automation

        ```
        Color = Object.Color       # Получить свойство (* )
        Object.Color = Color       # Установить свойство (* )
        Color = Object.GetColor()  # Получить свойство (**)
        Object.SetColor( Color )   # Установить свойство (**)
        ```
        """
        ...
    @Color.setter
    def Color(self, value: int): ...

    @property
    def DefaultScale(self) -> float:  # iprintjob_outputparameters_defaultscale.html
        """
        ## DefaultScale - Масштаб листов по умолчанию

        Тип данных: double

        1. Свойство позволяет устанавливать и получать масштаб листов по умолчанию.

        2. Если задаваемое значение находится вне диапазона (1e-006 : 1e+006), то его значение корректируется к ближайшему (минимальному или максимальному) значению.

        ### Синтаксис Automation

        ```
        DefaultScale = Object.DefaultScale       # Получить свойство (* )
        Object.DefaultScale = DefaultScale       # Установить свойство (* )
        DefaultScale = Object.GetDefaultScale()  # Получить свойство (**)
        Object.SetDefaultScale( DefaultScale )   # Установить свойство (**)
        ```
        """
        ...
    @DefaultScale.setter
    def DefaultScale(self, value: float): ...

    @property
    def Hooking(self) -> int:  # iprintjob_outputparameters_hooking.html
        """
        ## Hooking - Диапазон привязки к узлам страниц

        Тип данных: long

        1. Свойство позволяет устанавливать и получать диапазон привязки к узлам страниц.

        2. Если задаваемое значение находится вне диапазона (1: 50), то его значение корректируется к ближайшему (минимальному или максимальному) значению.

        ### Синтаксис Automation

        ```
        Hooking = Object.Hooking       # Получить свойство (* )
        Object.Hooking = Hooking       # Установить свойство (* )
        Hooking = Object.GetHooking()  # Получить свойство (**)
        Object.SetHooking( Hooking )   # Установить свойство (**)
        ```
        """
        ...
    @Hooking.setter
    def Hooking(self, value: int): ...

    @property
    def NumberOfCopies(self) -> int:  # iprintjob_outputparameters_numberofcopies.html
        """
        ## NumberOfCopies - Количество копий

        Тип данных: long

        Свойство позволяет устанавливать и получать количество копий.

        ### Синтаксис Automation

        ```
        NumberOfCopies = Object.NumberOfCopies       # Получить свойство (* )
        Object.NumberOfCopies = NumberOfCopies       # Установить свойство (* )
        NumberOfCopies = Object.GetNumberOfCopies()  # Получить свойство (**)
        Object.SetNumberOfCopies( NumberOfCopies )   # Установить свойство (**)
        ```
        """
        ...
    @NumberOfCopies.setter
    def NumberOfCopies(self, value: int): ...

    @property
    def OnlyThinLines(self) -> bool:  # iprintjob_outputparameters_onlythinlines.html
        """
        ## OnlyThinLines - Вывод тонкими линиями

        Тип данных: BOOL

        Свойство позволяет устанавливать и получать признак вывода тонкими линиями.

        ### Синтаксис Automation

        ```
        OnlyThinLines = Object.OnlyThinLines       # Получить свойство (* )
        Object.OnlyThinLines = OnlyThinLines       # Установить свойство (* )
        OnlyThinLines = Object.GetOnlyThinLines()  # Получить свойство (**)
        Object.SetOnlyThinLines( OnlyThinLines )   # Установить свойство (**)
        ```
        """
        ...
    @OnlyThinLines.setter
    def OnlyThinLines(self, value: bool): ...

    @property
    def PageOutputOrder(self) -> int:  # iprintjob_outputparameters_pageoutputorder.html
        """
        ## PageOutputOrder - Порядок вывода страниц на печать

        Тип данных: long

        1. Свойство позволяет устанавливать и получать порядок вывода страниц на печать.

        2. Если задаваемое значение вне диапазона (1:8), то его значение корректируется к ближайшему (минимальному или максимальному) значению.

        ### Синтаксис Automation

        ```
        PageOutputOrder = Object.PageOutputOrder       # Получить свойство (* )
        Object.PageOutputOrder = PageOutputOrder       # Установить свойство (* )
        PageOutputOrder = Object.GetPageOutputOrder()  # Получить свойство (**)
        Object.SetPageOutputOrder( PageOutputOrder )   # Установить свойство (**)
        ```
        """
        ...
    @PageOutputOrder.setter
    def PageOutputOrder(self, value: int): ...

    @property
    def PlotToFile(self) -> bool:  # iprintjob_outputparameters_plottofile.html
        """
        ## PlotToFile - Вывести в файл

        Тип данных: BOOL

        Свойство позволяет устанавливать и получать признак вывода в файл.

        ### Синтаксис Automation

        ```
        PlotToFile = Object.PlotToFile       # Получить свойство (* )
        Object.PlotToFile = PlotToFile       # Установить свойство (* )
        PlotToFile = Object.GetPlotToFile()  # Получить свойство (**)
        Object.SetPlotToFile( PlotToFile )   # Установить свойство (**)
        ```
        """
        ...
    @PlotToFile.setter
    def PlotToFile(self, value: bool): ...

    @property
    def UseCatchSpacing(self) -> bool:  # iprintjob_outputparameters_usecatchspacing.html
        """
        ## UseCatchSpacing - Привязка к углам листов

        Тип данных: BOOL

        Свойство позволяет устанавливать и получать признак привязки к углам листов.

        ### Синтаксис Automation

        ```
        UseCatchSpacing = Object.UseCatchSpacing       # Получить свойство (* )
        Object.UseCatchSpacing = UseCatchSpacing       # Установить свойство (* )
        UseCatchSpacing = Object.GetUseCatchSpacing()  # Получить свойство (**)
        Object.SetUseCatchSpacing( UseCatchSpacing )   # Установить свойство (**)
        ```
        """
        ...
    @UseCatchSpacing.setter
    def UseCatchSpacing(self, value: bool): ...

    @property
    def UseHooking(self) -> bool:  # iprintjob_outputparameters_usehooking.html
        """
        ## UseHooking - Привязка к узлам страниц

        Тип данных: BOOL

        Свойство позволяет устанавливать и получать признак привязки к узлам страниц.

        ### Синтаксис Automation

        ```
        UseHooking = Object.UseHooking       # Получить свойство (* )
        Object.UseHooking = UseHooking       # Установить свойство (* )
        UseHooking = Object.GetUseHooking()  # Получить свойство (**)
        Object.SetUseHooking( UseHooking )   # Установить свойство (**)
        ```
        """
        ...
    @UseHooking.setter
    def UseHooking(self, value: bool): ...




class IPrintJob_PrinterSettings(IKompasAPIObject):  # iprintjob_printersettings.html
    """
    ## Интерфейс IPrintJob_PrinterSettings

    Задание на печать: Интерфейс настроек принтера

    Позволяет настроить параметры принтера или плоттера.

    Интерфейс можно получить у интерфейса IPrintJob с помощью метода IUnknown::QueryInterface (const GUID far& IID, void** pif).
    """

    @property
    def DeviceName(self) -> str:  # iprintjob_printersettings_devicename.html
        """
        ## DeviceName - Имя устройства вывода

        Тип данных: BSTR

        1. Свойство доступно только для чтения.

        2. Установить другой принтер можно с помощью метода InitPrinterSettings

        ### Синтаксис Automation

        ```
        DeviceName = Object.DeviceName       # Получить свойство (* )
        DeviceName = Object.GetDeviceName()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def IsPortraitPage(self) -> bool:  # iprintjob_printersettings_isportraitpage.html
        """
        ## IsPortraitPage - Установить ориентацию бумаги

        Значения свойства:

        +---------------------------------------+
        | TRUE  | - книжная (вертикальная),     |
        +---------------------------------------+
        | FALSE | - альбомная (горизонтальная). |
        +---------------------------------------+

        ### Синтаксис Automation

        ```
        IsPortraitPage = Object.IsPortraitPage       # Получить свойство (* )
        Object.IsPortraitPage = IsPortraitPage       # Установить свойство (* )
        IsPortraitPage = Object.GetIsPortraitPage()  # Получить свойство (**)
        Object.SetIsPortraitPage( IsPortraitPage )   # Установить свойство (**)
        ```
        """
        ...
    @IsPortraitPage.setter
    def IsPortraitPage(self, value: bool): ...

    @property
    def PaperLength(self) -> int:  # iprintjob_printersettings_paperlength.html
        """
        ## PaperLength - Длина бумаги

        Тип данных: long

        Используется, если не задан размер бумаги.

        ### Синтаксис Automation

        ```
        PaperLength = Object.PaperLength       # Получить свойство (* )
        Object.PaperLength = PaperLength       # Установить свойство (* )
        PaperLength = Object.GetPaperLength()  # Получить свойство (**)
        Object.SetPaperLength( PaperLength )   # Установить свойство (**)
        ```
        """
        ...
    @PaperLength.setter
    def PaperLength(self, value: int): ...

    @property
    def PaperSize(self) -> int:  # iprintjob_printersettings_papersize.html
        """
        ## PaperSize - Размер бумаги

        Тип данных: long

        1. Значение свойства задается из списка предопределенных констант (подробнее см. описание переменной dmPaperSize из структуры DEVMODE в MSDN).

        2. Используется, если не заданы длина и ширина.

        ### Синтаксис Automation

        ```
        PaperSize = Object.PaperSize       # Получить свойство (* )
        Object.PaperSize = PaperSize       # Установить свойство (* )
        PaperSize = Object.GetPaperSize()  # Получить свойство (**)
        Object.SetPaperSize( PaperSize )   # Установить свойство (**)
        ```
        """
        ...
    @PaperSize.setter
    def PaperSize(self, value: int): ...

    @property
    def PaperSource(self) -> int:  # iprintjob_printersettings_papersource.html
        """
        ## PaperSource - Подача бумаги

        Тип данных: long

        Значение свойства задается из списка предопределенных констант (подробнее см. описание переменной dmDefaultSource из структуры DEVMODE в MSDN).

        ### Синтаксис Automation

        ```
        PaperSource = Object.PaperSource       # Получить свойство (* )
        Object.PaperSource = PaperSource       # Установить свойство (* )
        PaperSource = Object.GetPaperSource()  # Получить свойство (**)
        Object.SetPaperSource( PaperSource )   # Установить свойство (**)
        ```
        """
        ...
    @PaperSource.setter
    def PaperSource(self, value: int): ...

    @property
    def PaperWidth(self) -> int:  # iprintjob_printersettings_paperwidth.html
        """
        ## PaperWidth - Ширина бумаги

        Тип данных: long

        Используется, если не задан размер бумаги.

        ### Синтаксис Automation

        ```
        PaperWidth = Object.PaperWidth       # Получить свойство (* )
        Object.PaperWidth = PaperWidth       # Установить свойство (* )
        PaperWidth = Object.GetPaperWidth()  # Получить свойство (**)
        Object.SetPaperWidth( PaperWidth )   # Установить свойство (**)
        ```
        """
        ...
    @PaperWidth.setter
    def PaperWidth(self, value: int): ...

    @property
    def Port(self) -> str:  # iprintjob_printersettings_port.html
        """
        ## Port- Порт вывода

        Тип данных: BSTR

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        Port = Object.Port       # Получить свойство (* )
        Port = Object.GetPort()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def PrinterType(self) -> int:  # iprintjob_printersettings_printertype.html
        """
        ## PrinterType - Тип настраиваемого принтера

        Тип данных: из перечисления ksPrinterTypeEnum

        ### Синтаксис Automation

        ```
        PrinterType = Object.PrinterType       # Получить свойство (* )
        Object.PrinterType = PrinterType       # Установить свойство (* )
        PrinterType = Object.GetPrinterType()  # Получить свойство (**)
        Object.SetPrinterType( PrinterType )   # Установить свойство (**)
        ```
        """
        ...
    @PrinterType.setter
    def PrinterType(self, value: int): ...

    def InitPrinterSettings(self, DeviceName, IsPortraitPage, PaperSize, PaperLength, PaperWidth, PaperSource) -> bool:  # iprintjob_printersettings_initprintersettings.html
        """
        ## InitPrinterSettings - Установить настройки принтера

        Размер бумаги можно задать двумя способами:

        1. Константой, переданной параметром PaperSize. В этом случае параметры PaperLength и PaperWidth должны равняться -1.

        2. Задать произвольный размер с помощью параметров PaperLength и PaperWidth. В этом случае параметр PaperSize должен быть равен 0.

        ### Синтаксис Automation

        ```
        BOOL InitPrinterSettings( BSTR DeviceName,
        BOOL IsPortraitPage,
        long PaperSize,
        long PaperLength,
        long PaperWidth,
        long PaperSource );
        ```

        ### Входные параметры

        +---------------------------------------+
        | DeviceName     | - имя принтера,      |
        +---------------------------------------+
        | IsPortraitPage | - ориентация бумаги, |
        +---------------------------------------+
        | PaperSize      | - размер бумаги,     |
        +---------------------------------------+
        | PaperLength    | - длина бумаги,      |
        +---------------------------------------+
        | PaperWidth     | - ширина бумаги,     |
        +---------------------------------------+
        | PaperSource    | - источник бумаги.   |
        +---------------------------------------+

        Метод позволяет настроить параметры принтера или плоттера, на котором будут печататься документы.

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def LoadPrinterConfig(self, FileName) -> bool:  # iprintjob_printersettings_loadprinterconfig.html
        """
        ## LoadPrinterConfig - Загрузить конфигурацию принтера

        Если в качестве параметра передать пустую строку, то будет загружена конфигурация из настроек в зависимости от типа принтера

        ### Синтаксис Automation

        ```
        BOOL LoadPrinterConfig( BSTR FileName );
        ```

        ### Входные параметры

        +--------------------------------------+
        | FileName | - имя файла конфигурации. |
        +--------------------------------------+

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+

        Метод позволяет загрузить настройки печати из файла.
        """
        ...

    def SavePrinterConfig(self, FileName) -> bool:  # iprintjob_printersettings_saveprinterconfig.html
        """
        ## SavePrinterConfig - Сохранить конфигурацию принтера

        Сохранить конфигурацию принтера

        ### Синтаксис Automation

        ```
        BOOL SavePrinterConfig( BSTR FileName );
        ```

        ### Входные параметры

        +--------------------------------------+
        | FileName | - имя файла конфигурации. |
        +--------------------------------------+

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+

        Метод позволяет сохранить настройки печати в файл.
        """
        ...




class IMath2D(IKompasAPIObject):  # imath2d.html
    """
    ## Интерфейс IMath2D

    Интерфейс 2D математики.

    Интерфейс позволяет создавать математические кривые и осуществлять работу с точками.

    Интерфейс можно получить у интерфейса приложения КОМПАС с помощью свойства IApplication::Math2D.
    """

    def Arc(self, Xc, Yc, Radius, Angle1, Angle2, Direction) -> ICurve2D:  # imath2d_arc.html
        """
        ## Arc - Временная математическая дуга

        Метод позволяет создать временную математическую дугу.

        ### Синтаксис Automation

        ```
        LPDISPATCH Arc( double Xc,
        double Yc,
        double Radius,
        double Angle1,
        double Angle2, );
        ```

        ### Входные параметры

        +------------------------------------------------------------+
        | X, Y  | - координаты точки, через которую проходит прямая, |
        +------------------------------------------------------------+
        | Angle | - угол между прямой и осью ОХ.                     |
        +------------------------------------------------------------+

        ### Возвращаемое значение

        - указатель на интерфейс математической кривой ICurve2D.
        """
        ...

    def Bezier(self, Closed, AllPoints, Points) -> ICurve2D:  # imath2d_bezier.html
        """
        ## Bezier - Временная математическая кривая Безье

        Метод позволяет создать временную математическую кривую Безье.

        ### Синтаксис Automation

        ```
        LPDISPATCH Bezier( BOOL Closed,
        BOOL AllPoints,
        VARIANT Points );
        ```

        ### Входные параметры

        +--------------------------------------------------------------------------------------------------------------------+
        | Closed    | - признак замкнутости кривой,                                                                          |
        +--------------------------------------------------------------------------------------------------------------------+
        | AllPoints | - TRUE - в массиве присутствуют все точки узла, - FALSE - в массиве присутствуют только опорные точки, |
        +--------------------------------------------------------------------------------------------------------------------+
        | Points    | - массив SAFEARRAY | VT_R8 координат точек кривой.                                                     |
        +--------------------------------------------------------------------------------------------------------------------+

        ### Возвращаемое значение

        - указатель на интерфейс математической кривой ICurve2D.
        """
        ...

    def Circle(self, Xc, Yc, Radius) -> ICurve2D:  # imath2d_circle.html
        """
        ## Circle - Временная математическая окружность

        Метод позволяет создать временную математическую окружность.

        ### Синтаксис Automation

        ```
        LPDISPATCH Circle( double Xc,
        double Yc,
        double Radius );
        ```

        ### Входные параметры

        +------------------------------------------+
        | Xc, Yc | - координаты центра окружности, |
        +------------------------------------------+
        | Radius | - радиус окружности.            |
        +------------------------------------------+

        ### Возвращаемое значение

        - указатель на интерфейс математической кривой ICurve2D.
        """
        ...

    def Ellipse(self, Xc, Yc, A, B, Angle) -> ICurve2D:  # imath2d_ellipse.html
        """
        ## Ellipse - Временный математический эллипс

        Метод позволяет создать временный математический эллипс.

        ### Синтаксис Automation

        ```
        LPDISPATCH Ellipse( double Xc,
        double Yc,
        double A,
        double B,
        double Angle );
        ```

        ### Входные параметры

        +-------------------------------------------+
        | Xc, Yc | - координаты центра эллипса,     |
        +-------------------------------------------+
        | А, В   | - длина полуосей эллипса,        |
        +-------------------------------------------+
        | Angle  | - угол наклона эллипса к оси ОХ. |
        +-------------------------------------------+

        ### Возвращаемое значение

        - указатель на интерфейс математической кривой ICurve2D.
        """
        ...

    def EllipseArc(self, Xc, Yc, A, B, Angle, Angle1, Angle2, Direction) -> ICurve2D:  # imath2d_ellipsearc.html
        """
        ## EllipseArc - Временная математическая дуга эллипса

        Метод позволяет создать временную математическую дугу эллипса.

        ### Синтаксис Automation

        ```
        LPDISPATCH EllipseArc( double Xc,
        double Yc,
        double A,
        double B,
        double Angle,
        double Angle1,
        double Angle2 );
        ```

        ### Входные параметры

        +------------------------------------------------+
        | Xc, Yc | - координаты центра дуги эллипса,     |
        +------------------------------------------------+
        | А, В   | - длина полуосей дуги эллипса,        |
        +------------------------------------------------+
        | Angle  | - угол наклона дуги эллипса к оси ОХ. |
        +------------------------------------------------+
        | Angle1 | - начальный угол дуги к оси А,        |
        +------------------------------------------------+
        | Angle2 | - конечный угол дуги к оси А.         |
        +------------------------------------------------+

        ### Возвращаемое значение

        - указатель на интерфейс математической кривой ICurve2D.
        """
        ...

    def Line(self, X, Y, Angle) -> ICurve2D:  # imath2d_line.html
        """
        ## Line - Временная математическая прямая

        Метод позволяет создать временную математическую прямую.

        ### Синтаксис Automation

        ```
        LPDISPATCH Line( double X,
        double Y,
        double Angle );
        ```

        ### Входные параметры

        +------------------------------------------------------------+
        | X, Y  | - координаты точки, через которую проходит прямая, |
        +------------------------------------------------------------+
        | Angle | - угол наклона прямой к оси ОХ.                    |
        +------------------------------------------------------------+

        ### Возвращаемое значение

        - указатель на интерфейс математической кривой ICurve2D.
        """
        ...

    def LineSeg(self, X1, Y1, X2, Y2) -> ICurve2D:  # imath2d_lineseg.html
        """
        ## LineSeg - Временный математический отрезок

        Метод позволяет создать временный математический отрезок.

        ### Синтаксис Automation

        ```
        LPDISPATCH LineSeg( double X1,
        double Y1,
        double X2,
        double Y2 );
        ```

        ### Входные параметры

        +---------------------------------------------+
        | X1, Y1 | - координаты первой точки отрезка, |
        +---------------------------------------------+
        | X2, Y2 | - координаты второй точки отрезка. |
        +---------------------------------------------+

        ### Возвращаемое значение

        - указатель на интерфейс математической кривой ICurve2D.
        """
        ...

    def MovePoint(self, X, Y, Angle, Len) -> bool:  # imath2d_movepoint.html
        """
        ## MovePoint - Сместить точку

        ### Синтаксис Automation

        ```
        BOOL MovePoint( double * X,
        double * Y,
        Angle,
        double Len );
        ```

        ### Входные параметры

        +--------------------------------+
        | X, Y  | - координаты точки,    |
        +--------------------------------+
        | Angle | - угол смещения,       |
        +--------------------------------+
        | Len   | - расстояние смещения. |
        +--------------------------------+

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def Nurbs(self, Closed, Degree, Points, Weights, Knots) -> ICurve2D:  # imath2d_nurbs.html
        """
        ## Nurbs - Временная математическая Nurbs-кривая

        Метод позволяет создать временную математическую Nurbs-кривую.

        ### Синтаксис Automation

        ```
        LPDISPATCH Nurbs( BOOL Closed,
        long Degree,
        VARIANT Points,
        VARIANT Weights,
        VARIANT Knots );
        ```

        ### Входные параметры

        +-------------------------------------------------------------+
        | Closed | - признак замкнутости кривой,                      |
        +-------------------------------------------------------------+
        | Degree | - порядок кривой,                                  |
        +-------------------------------------------------------------+
        | Points | - массив SAFEARRAY | VT_R8 координат точек кривой, |
        +-------------------------------------------------------------+
        | Weight | - массив SAFEARRAY | VT_R8 весов точек,            |
        +-------------------------------------------------------------+
        | Knots  | - массив SAFEARRAY | VT_R8 узлов сплайна.          |
        +-------------------------------------------------------------+

        ### Возвращаемое значение

        - указатель на интерфейс математической кривой ICurve2D.
        """
        ...

    def PolyLine(self, Points, Closed) -> ICurve2D:  # imath2d_polyline.html
        """
        ## PolyLine - Временная математическая полилиния

        Метод позволяет создать временную математическую полилинию.

        ### Синтаксис Automation

        ```
        LPDISPATCH PolyLine( VARIANT Points,
        BOOL Closed );
        ```

        ### Входные параметры

        +----------------------------------------------------------------+
        | Points | - массив SAFEARRAY | VT_R8 координат точек полилинии, |
        +----------------------------------------------------------------+
        | Closed | - признак замкнутости кривой.                         |
        +----------------------------------------------------------------+

        ### Возвращаемое значение

        - указатель на интерфейс математической кривой ICurve2D.
        """
        ...

    def Rotate(self, X, Y, Xc, Yc, Angle) -> bool:  # imath2d_rotate.html
        """
        ## Rotate - Повернуть точку на угол

        Метод позволяет повернуть точку на угол.

        ### Синтаксис Automation

        ```
        BOOL Rotate( double * X,
        double * Y,
        double Xc,
        double Yc,
        double Angle );
        ```

        ### Входные параметры

        +-------------------------------------------------------+
        | X, Y   | - координаты точки, которую нужно повернуть, |
        +-------------------------------------------------------+
        | Xс, Yс | - координаты центра поворота,                |
        +-------------------------------------------------------+
        | Angle  | - угол поворота.                             |
        +-------------------------------------------------------+

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def Symmetry(self, X, Y, Curve) -> bool:  # imath2d_symmetry.html
        """
        ## Symmetry - Симметрия точки относительно оси

        Метод позволяет получить координаты точки, симметричной относительно заданной оси.

        ### Синтаксис Automation

        ```
        BOOL Symmetry( double * X,
        double * Y,
        LPDISPATCH Curve );
        ```

        ### Входные параметры

        +----------------------------------------------------+
        | X, Y  | - координаты точки,                        |
        +----------------------------------------------------+
        | Curve | - ось (математическая прямая или отрезок). |
        +----------------------------------------------------+

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...




class IAreaMeasurements3D(IModelObjects):  # iareameasurements3d.html
    """
    ## Интерфейс IAreaMeasurements3D

    Интерфейс коллекции измерений площади.
    """

    def Add(self) -> IAreaMeasurement3D:  # iareameasurements3d_add.html
        """
        ## Add - Создает новый элемент и добавляет его в коллекцию

        1. Метод позволяет создать новый интерфейс измерения площади.

        2. После получения нового интерфейса нужно задать параметры операции и вызвать метод IModelObject::Update.

        ### Синтаксис Automation

        ```
        LPDISPATCH Add();
        ```

        ### Возвращаемое значение

        - указатель на интерфейс IAreaMeasurement3D
        """
        ...

    def AreaMeasurement3D(self, Index) -> IAreaMeasurement3D:  # iareameasurements3d_areameasurement3d.html
        """
        ## AreaMeasurement3D - Возвращает элемент, заданный по индексу или по имени

        Тип данных: Указатель на интерфейс IAreaMeasurement3D.

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        AreaMeasurement3D = Object.AreaMeasurement3D( Index )     # Получить свойство (* )
        AreaMeasurement3D = Object.GetAreaMeasurement3D( Index )  # Получить свойство (**)
        ```

        ### Входные параметры

        +--------------------------------------------+
        | VARIANT Index | - индекс или имя элемента. |
        +--------------------------------------------+
        """
        ...




class IAreaMeasurement3D(IModelObject):  # iareameasurement3d.html
    """
    ## Интерфейс IAreaMeasurement3D

    Интерфейс измерения площади.
    """

    @property
    def Areas(self) -> typing.Any:  # iareameasurement3d_areas.html
        """
        ## Areas - Массив SAFEARRAY площадей граней

        Тип данных: VARIANT

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        Areas = Object.Areas       # Получить свойство (* )
        Areas = Object.GetAreas()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def Faces(self) -> typing.Any:  # iareameasurement3d_faces.html
        """
        ## Faces - Массив SAFEARRAY граней

        Тип данных: VARIANT

        ### Синтаксис Automation

        ```
        Faces = Object.Faces       # Получить свойство (* )
        Object.Faces = Faces       # Установить свойство (* )
        Faces = Object.GetFaces()  # Получить свойство (**)
        Object.SetFaces (Faces)    # Установить свойство (**)
        ```
        """
        ...
    @Faces.setter
    def Faces(self, value: typing.Any): ...

    @property
    def Sum(self) -> float:  # iareameasurement3d_sum.html
        """
        ## Sum - Сумма площадей граней

        Тип данных: double

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        Sum = Object.Sum       # Получить свойство (* )
        Sum = Object.GetSum()  # Получить свойство (**)
        ```
        """
        ...




class IDistanceAngleMeasurements3D(IModelObjects):  # idistanceanglemeasurements3d.html
    """
    ## Интерфейс IDistanceAngleMeasurements3D

    Интерфейс коллекции измерений расстояния и угла.
    """

    def Add(self) -> IDistanceAngleMeasurement3D:  # idistanceanglemeasurements3d_add.html
        """
        ## Add - Создает новый элемент и добавляет его в коллекцию

        1. Метод позволяет создать новый интерфейс измерения расстояния и угла.

        2. После получения нового интерфейса нужно задать параметры операции и вызвать метод IModelObject::Update.

        ### Синтаксис Automation

        ```
        LPDISPATCH Add();
        ```

        ### Возвращаемое значение

        - указатель на интерфейс IDistanceAngleMeasurement3D.
        """
        ...

    def DistanceAngleMeasurement3D(self, Index) -> IDistanceAngleMeasurement3D:  # idistanceanglemeasurements3d_distanceanglemeasurement3d.html
        """
        ## DistanceAngleMeasurement3D - Возвращает элемент, заданный по индексу или по имени

        Тип данных: Указатель на интерфейс IDistanceAngleMeasurement3D.

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        DistanceAngleMeasurement3D = Object.DistanceAngleMeasurement3D( Index )     # Получить свойство (* )
        DistanceAngleMeasurement3D = Object.GetDistanceAngleMeasurement3D( Index )  # Получить свойство (**)
        ```

        ### Входные параметры

        +--------------------------------------------+
        | VARIANT Index | - индекс или имя элемента. |
        +--------------------------------------------+
        """
        ...




class IDistanceAngleMeasurement3D(IModelObject):  # idistanceanglemeasurement3d.html
    """
    ## Интерфейс IDistanceAngleMeasurement3D

    Интерфейс измерений расстояния и угла.
    """

    @property
    def Angle(self) -> float:  # idistanceanglemeasurement3d_angle.html
        """
        ## Angle - Угол между объектами

        Тип данных: double

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        Angle = Object.Angle       # Получить свойство (* )
        Angle = Object.GetAngle()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def Briefly(self) -> bool:  # idistanceanglemeasurement3d_briefly.html
        """
        ## Briefly - Кратко

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        Briefly = Object.Briefly       # Получить свойство (* )
        Object.Briefly = Briefly       # Установить свойство (* )
        Briefly = Object.GetBriefly()  # Получить свойство (**)
        Object.SetBriefly (Briefly)    # Установить свойство (**)
        ```
        """
        ...
    @Briefly.setter
    def Briefly(self, value: bool): ...

    @property
    def IsAngleValid(self) -> bool:  # idistanceanglemeasurement3d_isanglevalid.html
        """
        ## IsAngleValid - Применимость расчета угла. TRUE - если для данных объектов угол имеет смысл

        Тип данных: BOOL

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        IsAngleValid = Object.IsAngleValid       # Получить свойство (* )
        IsAngleValid = Object.GetIsAngleValid()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def LNormal(self) -> float:  # idistanceanglemeasurement3d_lnormal.html
        """
        ## LNormal - Расcтояние по нормали

        Тип данных: double

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        LNormal = Object.LNormalt       # Получитьo свойство (* )
        LNormal = Object.GetLNormalt()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def Lmax(self) -> float:  # idistanceanglemeasurement3d_lmax.html
        """
        ## Lmax - Максимальное расcтояние

        Тип данных: double

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        Lmax = Object.Lmax       # Получить свойство (* )
        Lmax = Object.GetLmax()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def Lmin(self) -> float:  # idistanceanglemeasurement3d_lmin.html
        """
        ## Lmin - Минимальное расcтояние

        Тип данных: double

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        Lmin = Object.Lmin       # Получить свойство (* )
        Lmin = Object.GetLmin()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def MeasureResult(self) -> int:  # idistanceanglemeasurement3d_measureresult.html
        """
        ## MeasureResult - Результат измерения

        Тип данных: Значение из перечисления ksMeasureResultEnum.

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        MeasureResult = Object.MeasureResult       # Получить свойство (* )
        MeasureResult = Object.GetMeasureResult()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def Object1(self) -> IModelObject:  # idistanceanglemeasurement3d_object1.html
        """
        ## Object1 - Первый объект

        Тип данных: Указатель на интерфейс IModelObject.

        ### Синтаксис Automation

        ```
        Object1 = Object.Object1       # Получить свойство (* )
        Object.Object1 = Object1       # Установить свойство (* )
        Object1 = Object.GetObject1()  # Получить свойство (**)
        Object.SetObject1 (Object1)    # Установить свойство (**)
        ```
        """
        ...
    @Object1.setter
    def Object1(self, value: IModelObject): ...

    @property
    def Object2(self) -> IModelObject:  # idistanceanglemeasurement3d_object2.html
        """
        ## Object2 - Второй объект

        Тип данных: Указатель на интерфейс IModelObject.

        ### Синтаксис Automation

        ```
        Object2 = Object.Object2       # Получить свойство (* )
        Object.Object2 = Object2       # Установить свойство (* )
        Object2 = Object.GetObject2()  # Получить свойство (**)
        Object.SetObject2 (Object2)    # Установить свойство (**)
        ```
        """
        ...
    @Object2.setter
    def Object2(self, value: IModelObject): ...

    def GetMaxPoint1(self) -> bool:  # idistanceanglemeasurement3d_getmaxpoint1.html
        """
        ## GetMaxPoint1 - Получить первую точку отрезка максимального расстояния

        ### Синтаксис Automation

        ```
        BOOL GetMaxPoint1( double * X, double * Y, double * Z );
        ```

        ### Выходные параметры

        +-------------------------------+
        | X, Y, Z | - координаты точки. |
        +-------------------------------+

        ### Возвращаемое значение

        +-----------------------------------------+
        | TRUE | - в случае успешного завершения. |
        +-----------------------------------------+
        """
        ...

    def GetMaxPoint2(self) -> bool:  # idistanceanglemeasurement3d_getmaxpoint2.html
        """
        ## GetMaxPoint2 - Получить вторую точку отрезка максимального расстояния

        ### Синтаксис Automation

        ```
        BOOL GetMaxPoint2( double * X, double * Y, double * Z );
        ```

        ### Выходные параметры

        +-------------------------------+
        | X, Y, Z | - координаты точки. |
        +-------------------------------+

        ### Возвращаемое значение

        +-----------------------------------------+
        | TRUE | - в случае успешного завершения. |
        +-----------------------------------------+
        """
        ...

    def GetMinPoint1(self) -> bool:  # idistanceanglemeasurement3d_getminpoint1.html
        """
        ## GetMinPoint1 - Получить первую точку отрезка минимального расстояния

        ### Синтаксис Automation

        ```
        BOOL GetMinPoint1( double * X, double * Y, double * Z );
        ```

        ### Выходные параметры

        +-------------------------------+
        | X, Y, Z | - координаты точки. |
        +-------------------------------+

        ### Возвращаемое значение

        +-----------------------------------------+
        | TRUE | - в случае успешного завершения. |
        +-----------------------------------------+
        """
        ...

    def GetMinPoint2(self) -> bool:  # idistanceanglemeasurement3d_getminpoint2.html
        """
        ## GetMinPoint2 - Получить вторую точку отрезка минимального расстояния

        ### Синтаксис Automation

        ```
        BOOL GetMinPoint2( double * X, double * Y, double * Z );
        ```

        ### Выходные параметры

        +-------------------------------+
        | X, Y, Z | - координаты точки. |
        +-------------------------------+

        ### Возвращаемое значение

        +-----------------------------------------+
        | TRUE | - в случае успешного завершения. |
        +-----------------------------------------+
        """
        ...

    def GetNormalPoint1(self) -> bool:  # idistanceanglemeasurement3d_getnormalpoint1.html
        """
        ## GetNormalPoint1 - Получить первую точку отрезка расстояния по нормали

        ### Синтаксис Automation

        ```
        BOOL GetNormalPoint1( double * X, double * Y, double * Z );
        ```

        ### Выходные параметры

        +-------------------------------+
        | X, Y, Z | - координаты точки. |
        +-------------------------------+

        ### Возвращаемое значение

        +-----------------------------------------+
        | TRUE | - в случае успешного завершения. |
        +-----------------------------------------+
        """
        ...

    def GetNormalPoint2(self) -> bool:  # idistanceanglemeasurement3d_getnormalpoint2.html
        """
        ## GetNormalPoint2 - Получить вторую точку отрезка расстояния по нормали

        ### Синтаксис Automation

        ```
        BOOL GetNormalPoint2( double * X, double * Y, double * Z );
        ```

        ### Выходные параметры

        +-------------------------------+
        | X, Y, Z | - координаты точки. |
        +-------------------------------+

        ### Возвращаемое значение

        +-----------------------------------------+
        | TRUE | - в случае успешного завершения. |
        +-----------------------------------------+
        """
        ...




class IEdgeLengthMeasurements3D(IModelObjects):  # iedgelengthmeasurements3d.html
    """
    ## Интерфейс IEdgeLengthMeasurements3D

    Интерфейс коллекции измерений длины ребра.
    """

    def Add(self) -> IEdgeLengthMeasurement3D:  # iedgelengthmeasurements3d_add.html
        """
        ## Add - Создает новый элемент и добавляет его в коллекцию

        1. Метод позволяет создать новый интерфейс объекта измерение длины ребра.

        2. После получения нового интерфейса нужно задать параметры измерения и вызвать метод IModelObject::Update.

        ### Синтаксис Automation

        ```
        LPDISPATCH Add();
        ```

        ### Возвращаемое значение

        - указатель на интерфейс IEdgeLengthMeasurement3D.
        """
        ...

    def EdgeLengthMeasurement3D(self, Index) -> IEdgeLengthMeasurement3D:  # iedgelengthmeasurements3d_edgelengthmeasurement3d.html
        """
        ## EdgeLengthMeasurement3D - Возвращает элемент, заданный по индексу или по имени

        Тип данных: Указатель на интерфейс IEdgeLengthMeasurement3D.

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        EdgeLengthMeasurement3D = Object.EdgeLengthMeasurement3D( Index )     # Получить свойство (* )
        EdgeLengthMeasurement3D = Object.GetEdgeLengthMeasurement3D( Index )  # Получить свойство (**)
        ```

        ### Входные параметры

        +--------------------------------------------+
        | VARIANT Index | - индекс или имя элемента. |
        +--------------------------------------------+
        """
        ...




class IEdgeLengthMeasurement3D(IModelObject):  # iedgelengthmeasurement3d.html
    """
    ## Интерфейс IEdgeLengthMeasurement3D

    Интерфейс измерений длины ребра.
    """

    @property
    def Edges(self) -> typing.Any:  # iedgelengthmeasurement3d_edges.html
        """
        ## Edges - Maссив SAFEARRAY ребер

        Тип данных: VARIANT

        ### Синтаксис Automation

        ```
        Edges = Object.Edges       # Получить свойство (* )
        Object.Edges = Edges       # Установить свойство (* )
        Edges = Object.GetEdges()  # Получить свойство (**)
        Object.SetEdges (Edges)    # Установить свойство (**)
        ```
        """
        ...
    @Edges.setter
    def Edges(self, value: typing.Any): ...

    @property
    def Lengths(self) -> typing.Any:  # iedgelengthmeasurement3d_lengths.html
        """
        ## Lengths - Массив SAFEARRAY длин ребер

        Тип данных: VARIANT

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        Lengths = Object.Lengths       # Получить свойство (* )
        Lengths = Object.GetLengths()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def Sum(self) -> float:  # iedgelengthmeasurement3d_sum.html
        """
        ## Sum - Сумма длин ребер

        Тип данных: double

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        Sum = Object.Sum       # Получить свойство (* )
        Sum = Object.GetSum()  # Получить свойство (**)
        ```
        """
        ...




class IMeasurementContainer(IDispatch):  # imeasurementcontainer.html
    """
    ## Интерфейс IMeasurementContainer

    Интерфейс контейнера объектов измерений и диагностики.
    """

    @property
    def AreaMeasurements3D(self) -> IAreaMeasurements3D:  # imeasurementcontainer_areameasurements3d.html
        """
        ## AreaMeasurements3D - Коллекция измерений площади

        Тип данных: Указатель на интерфейс IAreaMeasurements3D.

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        AreaMeasurements3D = iObject.AreaMeasurements3D       # Получить свойство (* )
        AreaMeasurements3D = iObject.GetAreaMeasurements3D()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def DistanceAngleMeasurements3D(self) -> IDistanceAngleMeasurements3D:  # imeasurementcontainer_distanceanglemeasurements3d.html
        """
        ## DistanceAngleMeasurements3D - Коллекция измерений расстояния и угла

        Тип данных: Указатель на интерфейс IDistanceAngleMeasurements3D.

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        DistanceAngleMeasurements3D = iObject.DistanceAngleMeasurements3D       # Получить свойство (* )
        DistanceAngleMeasurements3D = iObject.GetDistanceAngleMeasurements3D()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def EdgeLengthMeasurements3D(self) -> IEdgeLengthMeasurements3D:  # imeasurementcontainer_edgelengthmeasurements3d.html
        """
        ## EdgeLengthMeasurements3D - Коллекция измерений длины ребра

        Тип данных: Указатель на интерфейс IEdgeLengthMeasurements3D.

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        EdgeLengthMeasurements3D = iObject.EdgeLengthMeasurements3D       # Получить свойство (* )
        EdgeLengthMeasurements3D = iObject.GetEdgeLengthMeasurements3D()  # Получить свойство (**)
        ```
        """
        ...




class IDocument3DManager(IKompasAPIObject):  # idocument3dmanager.html
    """
    ## Интерфейс IDocument3DManager

    Менеджер 3D документа.
    """

    @property
    def Layers3D(self) -> ILayers3D:  # idocument3dmanager_layers3d.html
        """
        ## Layers3D - Коллекция слоев в 3D

        Тип данных: Указатель на интерфейс ILayers3D

        Свойство только для чтения.

        ### Синтаксис Automation

        ```
        Layers3D = Object.Layers3D      # Получить свойство (* )
        Layers3D = Object.GetLayers3D)  # Получить свойство (**)
        ```
        """
        ...

    @property
    def LayersDynamicGroups3D(self) -> ILayerGroups3D:  # idocument3dmanager_layersdynamicgroups3d.html
        """
        ## LayersDynamicGroups3D - Коллекция динамических групп слоев в 3D

        Тип данных: Указатель на интерфейс ILayerGroups3D

        Свойство только для чтения.

        ### Синтаксис Automation

        ```
        LayersDynamicGroups3D = Object.LayersDynamicGroups3D     # Получить свойство (* )
        LayersDynamicGroups3D = Object.GetLayersDynamicGroups3D  # Получить свойство (**)
        ```
        """
        ...

    @property
    def LayersGroups3D(self) -> ILayerGroups3D:  # idocument3dmanager_layersgroups3d.html
        """
        ## LayersGroups3D - Коллекция групп слоев в 3D

        Тип данных: Указатель на интерфейс ILayerGroups3D

        Свойство только для чтения.

        ### Синтаксис Automation

        ```
        LayersGroups3D = Object.LayersGroups3D     # Получить свойство (* )
        LayersGroups3D = Object.GetLayersGroups3D  # Получить свойство (**)
        ```
        """
        ...

    @property
    def PartsGroups(self) -> ILayers3D:  # idocument3dmanager_partsgroups.html
        """
        ## PartsGroups - Группы компонентов

        Тип данных: Указатель на интерфейс ILayers3D

        ### Синтаксис Automation

        ```
        PartsGroups = Object.PartsGroups       # Получить свойство (* )
        PartsGroups = Object.GetPartsGroups()  # Получить свойство (**)
        ```
        """
        ...

    def Update(self) -> bool:  # idocument3dmanager_update.html
        """
        ## Update - Обновление данных

        ### Синтаксис Automation

        ```
        BOOL Update();
        ```

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...




class ILayers3D(IKompasCollection):  # ilayers3d.html
    """
    ## Интерфейс ILayers3D

    Интерфейс коллекции слоев в 3D.
    """

    def Add(self) -> ILayer3D:  # ilayers3d_add.html
        """
        ## Add - Создать слой (добавляет слой в коллекцию)

        ### Синтаксис Automation

        ```
        LPDISPATCH Add();
        ```

        ### Возвращаемое значение

        - указатель на интерфейс слоя ILayer3D.
        """
        ...

    def Attach(self, PVal) -> bool:  # ilayers3d_attach.html
        """
        ## Attach - Добавить существующий слой

        ### Синтаксис Automation

        ```
        BOOL Attach( ILayer3D * PVal );
        ```

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def Detach(self, PVal) -> bool:  # ilayers3d_detach.html
        """
        ## Detach - Отсоединить слой

        1. Интерфейс слоя отсоединяется от коллекции, слой при этом из документа не удаляется.

        2. Метод работает только для коллекции слоев группы ILayerGroup3D, если она не является динамической.

        ### Синтаксис Automation

        ```
        BOOL Detach( ILayer3D * PVal );
        ```

        ### Входные параметры

        +------------------------------------------------+
        | PVal | - указатель на интерфейс слоя ILayer3D. |
        +------------------------------------------------+

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+

        - Полный путь к библиотеке на диске.
        """
        ...

    def Layer3D(self, Index) -> ILayer3D:  # ilayers3d_layer3d.html
        """
        ## Layer3D - Возвращает слой, заданный по индексу, имени или ссылке

        Тип данных: Указатель на интерфейс ILayer3D

        1. Свойство только для чтения.

        2. В качестве индекса может использоваться индекс элемента в массиве, ссылка на слой, имя слоя.

        ### Синтаксис Automation

        ```
        Layer3D = Object.Layer3D( Index )     # Получить свойство (* )
        Layer3D = Object.GetLayer3D( Index )  # Получить свойство (**)
        ```
        """
        ...

    def Layer3DByNumber(self, Number) -> ILayer3D:  # ilayers3d_layer3dbynumber.html
        """
        ## Layer3DByNumber - Возвращает слой, заданный по номеру

        Тип данных: Указатель на интерфейс ILayer3D

        Свойство только для чтения.

        ### Синтаксис Automation

        ```
        Layer3DByNumber = Object.Layer3DByNumber( Number )    # Получить свойство (* )
        Layer3DByNumber = Object.GetLayer3DByNumber( Number)  # Получить свойство (**)
        ```

        ### Входные параметры

        +----------------------------------+
        | number | - номер слоя; тип long. |
        +----------------------------------+
        """
        ...




class ILayerGroups3D(IKompasCollection):  # ilayergroups3d.html
    """
    ## Интерфейс ILayerGroups3D

    Интерфейс коллекции групп cлоев в 3D.
    """

    def Add(self) -> ILayerGroup3D:  # ilayergroups3d_add.html
        """
        ## Add - Создать группу (добавляет группу в коллекцию)

        ### Синтаксис Automation

        ```
        LPDISPATCH Add();
        ```

        ### Возвращаемое значение

        - указатель на интерфейс группы слоев ILayerGroup3D.
        """
        ...

    def LayerGroup3D(self, Index) -> ILayerGroup3D:  # ilayergroups3d_layergroup3d.html
        """
        ## LayerGroup3D - Возвращает группу по индексу, уникальному идентификатору или имени

        Тип данных: Указатель на интерфейс ILayerGroup3D

        Свойство только для чтения.

        ### Синтаксис Automation

        ```
        LayerGroup3D = Object.LayerGroup3D( Index )     # Получить свойство (* )
        LayerGroup3D = Object.GetLayerGroup3D( Index )  # Получить свойство (**)
        ```
        """
        ...




class ILayer3D(IKompasAPIObject):  # ilayer3d.html
    """
    ## Интерфейс ILayer3D

    Интерфейс cлоя в 3D.

    Чтобы новые параметры интерфейса IColorParam7 слоя ILayer3D обновились у объектов модели, нужно вызвать метод IDocument3DManager::Update.
    """

    @property
    def Color(self) -> int:  # ilayer3d_color.html
        """
        ## Color - Цвет слоя

        Тип данных: long

        ### Синтаксис Automation

        ```
        Color = Object.Color       # Получить свойство (* )
        Object.Color = Color       # Установить свойство (* )
        Color = Object.GetColor()  # Получить свойство (**)
        Object.SetColor (Color)    # Установить свойство (**)
        ```
        """
        ...
    @Color.setter
    def Color(self, value: int): ...

    @property
    def Comment(self) -> str:  # ilayer3d_comment.html
        """
        ## Comment - Комментарий

        Тип данных: BSTR

        ### Синтаксис Automation

        ```
        Comment = Object.Comment       # Получить свойство (* )
        Object.Comment = Comment       # Установить свойство (* )
        Comment = Object.GetComment()  # Получить свойство (**)
        Object.SetComment (Comment)    # Установить свойство (**)
        ```
        """
        ...
    @Comment.setter
    def Comment(self, value: str): ...

    @property
    def Current(self) -> bool:  # ilayer3d_current.html
        """
        ## Current - Состояние слоя - текущий

        Тип данных: BOOL

        Установить свойство можно только равным TRUE. Новый слой всегда создается текущим.

        ### Синтаксис Automation

        ```
        Current = Object.Current       # Получить свойство (* )
        Object.Current = Current       # Установить свойство (* )
        Current = Object.GetCurrent()  # Получить свойство (**)
        Object.SetCurrent (Current)    # Установить свойство (**)
        ```
        """
        ...
    @Current.setter
    def Current(self, value: bool): ...

    @property
    def Editable(self) -> bool:  # ilayer3d_editable.html
        """
        ## Editable - Признак редактирования

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        Editable = Object.Editable       # Получить свойство (* )
        Object.Editable = Editable       # Установить свойство (* )
        Editable = Object.GetEditable()  # Получить свойство (**)
        Object.SetEditable (Editable)    # Установить свойство (**)
        ```
        """
        ...
    @Editable.setter
    def Editable(self, value: bool): ...

    @property
    def Name(self) -> str:  # ilayer3d_name.html
        """
        ## Name - Имя слоя

        Тип данных: BSTR

        ### Синтаксис Automation

        ```
        Name = Object.Name       # Получить свойство (* )
        Object.Name = Name       # Установить свойство (* )
        Name = Object.GetName()  # Получить свойство (**)
        Object.SetName (Name)    # Установить свойство (**)
        ```
        """
        ...
    @Name.setter
    def Name(self, value: str): ...

    @property
    def Number(self) -> int:  # ilayer3d_number.html
        """
        ## Number - Номер слоя

        Тип данных: long

        ### Синтаксис Automation

        ```
        Number = Object.Number       # Получить свойство (* )
        Object.Number = Number       # Установить свойство (* )
        Number = Object.GetNumber()  # Получить свойство (**)
        Object.SetNumber (Number)    # Установить свойство (**)
        ```
        """
        ...
    @Number.setter
    def Number(self, value: int): ...

    @property
    def Projected(self) -> bool:  # ilayer3d_projected.html
        """
        ## Projected - Признак проецирования

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        Projected = Object.Projected       # Получить свойство (* )
        Object.Projected = Projected       # Установить свойство (* )
        Projected = Object.GetProjected()  # Получить свойство (**)
        Object.SetProjected (Projected)    # Установить свойство (**)
        ```
        """
        ...
    @Projected.setter
    def Projected(self, value: bool): ...

    @property
    def Visible(self) -> bool:  # ilayer3d_visible.html
        """
        ## Visible - Состояние слоя - видимый или погашенный

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        Visible = Object.Visible       # Получить свойство (* )
        Object.Visible = Visible       # Установить свойство (* )
        Visible = Object.GetVisible()  # Получить свойство (**)
        Object.SetVisible (Visible)    # Установить свойство (**)
        ```
        """
        ...
    @Visible.setter
    def Visible(self, value: bool): ...

    def Delete(self) -> bool:  # ilayer3d_delete.html
        """
        ## Delete - Удалить слой

        ### Синтаксис Automation

        ```
        BOOL Delete();
        ```

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...

    def Update(self) -> bool:  # ilayer3d_update.html
        """
        ## Update - Обновление данных

        ### Синтаксис Automation

        ```
        BOOL Update();
        ```

        ### Возвращаемое значение

        +-----------------------------------------+
        | TRUE | - в случае успешного завершения. |
        +-----------------------------------------+
        """
        ...




class ILayerGroup3D(IKompasCollection):  # ilayergroup3d.html
    """
    ## Интерфейс ILayerGroup3D

    Интерфейс группы cлоев в 3D.
    """

    @property
    def LayerFilterConditions(self) -> ILayerFilterConditions:  # ilayergroup3d_layerfilterconditions.html
        """
        ## LayerFilterConditions - Коллекция условий фильтрации

        Тип данных: Указатель на интерфейс ILayerFilterConditions

        Свойство только для чтения.

        ### Синтаксис Automation

        ```
        LayerFilterConditions = Object.LayerFilterConditions       # Получить свойство (* )
        LayerFilterConditions = Object.GetLayerFilterConditions()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def LayerGroups(self) -> ILayerGroup3D:  # ilayergroup3d_layergroups.html
        """
        ## LayerGroups - Коллекция групп слоев

        Тип данных: Указатель на интерфейс ILayerGroup3D

        Свойство только для чтения.

        ### Синтаксис Automation

        ```
        LayerGroups = Object.LayerGroups       # Получить свойство (* )
        LayerGroups = Object.GetLayerGroups()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def Layers(self) -> ILayers3D:  # ilayergroup3d_layers.html
        """
        ## Layers - Коллекция слоев

        Тип данных: Указатель на интерфейс ILayers3D

        Свойство только для чтения.

        ### Синтаксис Automation

        ```
        Layers = Object.Layers       # Получить свойство (* )
        Layers = Object.GetLayers()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def Name(self) -> str:  # ilayergroup3d_name.html
        """
        ## Name - Имя группы

        Тип данных: BSTR

        ### Синтаксис Automation

        ```
        Name = Object.Name       # Получить свойство (* )
        Object.Name = Name       # Установить свойство (* )
        Name = Object.GetName()  # Получить свойство (**)
        Object.SetName (Name)    # Установить свойство (**)
        ```
        """
        ...
    @Name.setter
    def Name(self, value: str): ...

    @property
    def OwnerGroup(self) -> ILayerGroup3D:  # ilayergroup3d_ownergroup.html
        """
        ## OwnerGroup - Владелец группы - другая группа

        Тип данных: Указатель на интерфейс ILayerGroup3D

        Свойство только для чтения.

        ### Синтаксис Automation

        ```
        OwnerGroup = Object.OwnerGroup       # Получить свойство (* )
        OwnerGroup = Object.GetOwnerGroup()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def UniqueId(self) -> float:  # ilayergroup3d_uniqueid.html
        """
        ## UniqueId - Уникальный идентификатор группы слоев

        Тип данных: double

        1. Свойство только для чтения.

        2. После создания группы свойство не изменяется и хранится в чертеже.

        ### Синтаксис Automation

        ```
        UniqueId = Object.UniqueId       # Получить свойство (* )
        UniqueId = Object.GetUniqueId()  # Получить свойство (**)
        ```
        """
        ...

    def Delete(self) -> bool:  # ilayergroup3d_delete.html
        """
        ## Delete - Удалить группу

        При удалении группы слоев слои, входящие в нее, из документа не удаляются.

        ### Синтаксис Automation

        ```
        BOOL Delete();
        ```

        ### Возвращаемое значение

        +------------------------------------------+
        | TRUE  | - в случае успешного завершения, |
        +------------------------------------------+
        | FALSE | - в случае неудачи.              |
        +------------------------------------------+
        """
        ...




class IZone(IModelObject):  # izone.html
    """
    ## Интерфейс IZone

    Интерфейс параметров зоны
    """

    @property
    def Parameters(self) -> IKompasAPIObject:  # izone_parameters.html
        """
        ## Parameters - Параметры зоны

        Тип данных: Указатель на интерфейс IKompasAPIObject

        Свойство доступно только для чтения.

        В зависимости от способа создания зоны IZone::ZoneType возвращается интерфейс параметров зоны.

        •ksZoneFree - Нет параметров;

        •ksZoneByPoints - Параметры зоны заданной габаритным параллелепипедом - IZoneParametersByBorderPoints;

        •ksZoneByObjects - Параметры зоны по суммарному габариту объектов - IZoneParametersByObjects.

        ### Синтаксис Automation

        ```
        Parameters = Object.Parameters       # Получить свойство (* )
        Parameters = Object.GetParameters()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def ZoneType(self) -> int:  # izone_zonetype.html
        """
        ## ZoneType - Способ создания зоны

        Тип данных: из перечисления ksZoneTypeEnum

        ### Синтаксис Automation

        ```
        ZoneType = Object.ZoneType       # Получить свойство (* )
        Object.ZoneType = ZoneType       # Установить свойство (* )
        ZoneType = Object.GetZoneType()  # Получить свойство (**)
        Object.SetZoneType( ZoneType )   # Установить свойство (**)
        ```
        """
        ...
    @ZoneType.setter
    def ZoneType(self, value: int): ...

    def GetGabarit(self) -> bool:  # izone_getgabarit.html
        """
        ## GetGabarit - Выдать габарит

        ### Синтаксис Automation

        ```
        BOOL GetGabarit( double * X1, double * Y1, double * Z1, double * X2, double * Y2, double * Z2 );
        ```

        ### Выходные параметры

        +--------------------------------------------------------------------+
        | X1, Y1, Z1, X2, Y2, Z2 | - координаты габаритного параллелепипеда. |
        +--------------------------------------------------------------------+

        ### Возвращаемое значение

        +-----------------------------------------+
        | TRUE | - в случае успешного завершения. |
        +-----------------------------------------+
        """
        ...

    def SelectObjects(self, SelectType) -> typing.Any:  # izone_selectobjects.html
        """
        ## SelectObjects - Вернуть объекты, попадающие в зону или находящиеся вне зоны

        Тип данных: VARIANT

        Свойство доступно только для чтения.

        Массив объектов возвращается в виде массива SAFEARRAY объектов LPDISPATCH (VT_ARRAY | VT_DISPATCH).

        ### Синтаксис Automation

        ```
        SelectObjects = Object.SelectObjects( SelectType )     # Получить свойство (* )
        SelectObjects = Object.GetSelectObjects( SelectType )  # Получить свойство (**)
        ```
        """
        ...

    def SelectParts(self, SelectType) -> typing.Any:  # izone_selectparts.html
        """
        ## SelectParts - Вернуть вставки моделей с редактируемого уровня, попадающие в зону или находящиеся вне зоны

        Тип данных: VARIANT

        Свойство доступно только для чтения.

        Массив объектов возвращается в виде массива SAFEARRAY объектов LPDISPATCH (VT_ARRAY | VT_DISPATCH).

        ### Синтаксис Automation

        ```
        SelectParts = Object.SelectParts( SelectType )     # Получить свойство (* )
        SelectParts = Object.GetSelectParts( SelectType )  # Получить свойство (**)
        ```
        """
        ...




class IZonesManager(IKompasAPIObject):  # izonesmanager.html
    """
    ## Интерфейс IZonesManager

    Интерфейс Менеджера зон
    """

    @property
    def CreateZonesInGlobalCS(self) -> bool:  # izonesmanager_createzonesinglobalcs.html
        """
        ## CreateZonesInGlobalCS - Создавать зоны в абсолютной СК

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        CreateZonesInGlobalCS = Object.CreateZonesInGlobalCS       # Получить свойство (* )
        Object.CreateZonesInGlobalCS = CreateZonesInGlobalCS       # Установить свойство (* )
        CreateZonesInGlobalCS = Object.GetCreateZonesInGlobalCS()  # Получить свойство (**)
        Object.SetCreateZonesInGlobalCS( CreateZonesInGlobalCS )   # Установить свойство (**)
        ```
        """
        ...
    @CreateZonesInGlobalCS.setter
    def CreateZonesInGlobalCS(self, value: bool): ...

    @property
    def CurrentZone(self) -> IZone:  # izonesmanager_currentzone.html
        """
        ## CurrentZone - Текущая зона

        Тип данных: Указатель на интерфейс IZone

        ### Синтаксис Automation

        ```
        CurrentZone = Object.CurrentZone       # Получить свойство (* )
        Object.CurrentZone = CurrentZone       # Установить свойство (* )
        CurrentZone = Object.GetCurrentZone()  # Получить свойство (**)
        Object.SetCurrentZone( CurrentZone )   # Установить свойство (**)
        ```
        """
        ...
    @CurrentZone.setter
    def CurrentZone(self, value: IZone): ...

    @property
    def ZonesCount(self) -> int:  # izonesmanager_zonescount.html
        """
        ## ZonesCount - Количество зон

        Тип данных: long

        ### Синтаксис Automation

        ```
        ZonesCount = Object.ZonesCount       # Получить свойство (* )
        ZonesCount = Object.GetZonesCount()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def ZonesDivisionCount(self) -> int:  # izonesmanager_zonesdivisioncount.html
        """
        ## ZonesDivisionCount - Количество объектов Разбиение зоны

        Тип данных: long

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        ZonesDivisionCount = Object.ZonesDivisionCount       # Получить свойство (* )
        ZonesDivisionCount = Object.GetZonesDivisionCount()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def ZonesTree(self) -> IFeature7:  # izonesmanager_zonestree.html
        """
        ## ZonesTree - Дерево зон

        Тип данных: Указатель на интерфейс IFeature7

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        ZonesTree = Object.ZonesTree       # Получить свойство (* )
        ZonesTree = Object.GetZonesTree()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def ZonesVisible(self) -> bool:  # izonesmanager_zonesvisible.html
        """
        ## ZonesVisible - Отображать зоны

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        ZonesVisible = Object.ZonesVisible       # Получить свойство (* )
        Object.ZonesVisible = ZonesVisible       # Установить свойство (* )
        ZonesVisible = Object.GetZonesVisible()  # Получить свойство (**)
        Object.SetZonesVisible( ZonesVisible )   # Установить свойство (**)
        ```
        """
        ...
    @ZonesVisible.setter
    def ZonesVisible(self, value: bool): ...

    def AddZone(self, ZoneType) -> IZone:  # izonesmanager_addzone.html
        """
        ## AddZone - Добавить зону

        ### Синтаксис Automation

        ```
        LPDISPATCH AddZone( ksZoneTypeEnum ZoneType );
        ```

        ### Входные параметры

        +------------------------------------------------------------------+
        | ZoneType | - cпособ создания зоны из перечисления ksZoneTypeEnum |
        +------------------------------------------------------------------+

        ### Возвращаемое значение

        Указатель на интерфейс IZone
        """
        ...

    def AddZoneDivision(self, ZoneType) -> IZoneDivision:  # izonesmanager_addzonedivision.html
        """
        ## AddZoneDivision - Добавить Разбиение зоны

        ### Синтаксис Automation

        ```
        LPDISPATCH AddZoneDivision( ksZoneDivisionTypeEnum ZoneType );
        ```

        ### Входные параметры

        +---------------------------------------------------------------------------+
        | ZoneType | - cпособ разбиения зоны из перечисления ksZoneDivisionTypeEnum |
        +---------------------------------------------------------------------------+

        ### Возвращаемое значение

        - указатель на интерфейс IZoneDivision
        """
        ...

    def Zone(self, Index) -> IZone:  # izonesmanager_zone.html
        """
        ## Zone - Получить зону по имени или индексу

        Тип данных: Указатель на интерфейс IZone

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        Zone = Object.Zone( Index )     # Получить свойство (* )
        Zone = Object.GetZone( Index )  # Получить свойство (**)
        ```

        ### Входные параметры

        +--------------------------------------------+
        | VARIANT Index | - индекс или имя элемента. |
        +--------------------------------------------+
        """
        ...

    def ZoneDivision(self, Index) -> IZoneDivision:  # izonesmanager_zonedivision.html
        """
        ## ZoneDivision - Получить объект Разбиение зоны по имени или индексу

        Тип данных: Указатель на интерфейс IZoneDivision

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        ZoneDivision = Object.ZoneDivision( Index )     # Получить свойство (* )
        ZoneDivision = Object.GetZoneDivision( Index )  # Получить свойство (**)
        ```

        ### Входные параметры

        +--------------------------------------------+
        | VARIANT Index | - индекс или имя элемента. |
        +--------------------------------------------+
        """
        ...




class IZoneDivision(IKompasAPIObject):  # izonedivision.html
    """
    ## Интерфей IZoneDivision

    Интерфейс параметров разбиения зоны
    """

    @property
    def Hidden(self):
        ...
    @Hidden.setter
    def Hidden(self, value): ...

    @property
    def ModelObjectType(self):
        ...

    @property
    def Name(self):
        ...
    @Name.setter
    def Name(self, value): ...

    @property
    def Owner(self):
        ...

    @property
    def Parameters(self) -> IKompasAPIObject:  # izonedivision_parameters.html
        """
        ## Parameters - Параметры зоны

        Тип данных: Указатель на интерфейс IKompasAPIObject

        Свойство доступно только для чтения.

        В зависимости от способа создания зоны IZoneDivision::ZoneDivisionType возвращается интерфейс параметров зоны.

        •ksZoneDivisionRegular - Параметры разбиения зоны для способа Равномерно по осям - IZoneDivisionParametersRegular;

        •ksZoneDivisionByPlanes - Параметры разбиения зоны для способа По набору плоскостей - IZoneDivisionParametersByPlanes.

        ### Синтаксис Automation

        ```
        Parameters = Object.Parameters       # Получить свойство (* )
        Parameters = Object.GetParameters()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def Part(self):
        ...

    @property
    def Valid(self):
        ...

    @property
    def Zone(self) -> IZone:  # izonedivision_zone.html
        """
        ## Zone - Зона

        Тип данных: Указатель на интерфейс IZone

        ### Синтаксис Automation

        ```
        Zone = Object.Zone       # Получить свойство (* )
        Object.Zone = Zone       # Установить свойство (* )
        Zone = Object.GetZone()  # Получить свойство (**)
        Object.SetZone( Zone )   # Установить свойство (**)
        ```
        """
        ...
    @Zone.setter
    def Zone(self, value: IZone): ...

    @property
    def ZoneDivisionType(self) -> int:  # izonedivision_zonedivisiontype.html
        """
        ## ZoneDivisionType - Способ разбиения зоны

        Тип данных: из перечисления ksZoneDivisionTypeEnum

        ### Синтаксис Automation

        ```
        ZoneDivisionType = Object.ZoneDivisionType       # Получить свойство (* )
        Object.ZoneDivisionType = ZoneDivisionType       # Установить свойство (* )
        ZoneDivisionType = Object.GetZoneDivisionType()  # Получить свойство (**)
        Object.SetZoneDivisionType( ZoneDivisionType )   # Установить свойство (**)
        ```
        """
        ...
    @ZoneDivisionType.setter
    def ZoneDivisionType(self, value: int): ...

    def Update(self):
        ...




class IZoneParametersByBorderPoints(IKompasAPIObject):  # izoneparametersbyborderpoints.html
    """
    ## Интерфейс IZoneParametersByBorderPoints

    Интерфейс параметров зоны, заданной габаритным параллелепипедом.
    """

    @property
    def BuildingType(self) -> int:  # izoneparametersbyborderpoints_buildingtype.html
        """
        ## BuildingType - Способ задания габаритов

        Тип данных: из перечисления ksGabaritBuildingTypeEnum.

        ### Синтаксис Automation

        ```
        BuildingType = Object.BuildingType       # Получить свойство (* )
        Object.BuildingType = BuildingType       # Установить свойство (* )
        BuildingType = Object.GetBuildingType()  # Получить свойство (**)
        Object.SetBuildingType( BuildingType )   # Установить свойство (**)
        ```
        """
        ...
    @BuildingType.setter
    def BuildingType(self, value: int): ...

    def AssociationObject(self, First) -> IModelObject:  # izoneparametersbyborderpoints_associationobject.html
        """
        ## AssociationObject - Установить опорный объект для вершины

        Тип данных: Указатель на интерфейс IModelObject

        ### Синтаксис Automation

        ```
        AssociationObject = Object.AssociationObject( First )     # Получить свойство (* )
        Object.AssociationObject( First ) = AssociationObject     # Установить свойство (* )
        AssociationObject = Object.GetAssociationObject( First )  # Получить свойство (**)
        Object.SetAssociationObject( First, AssociationObject )   # Установить свойство (**)
        ```

        ### Входные параметры

        +-----------------------------------------------------------------+
        | BOOL | - First - TRUE - первая вершина, FALSE - вторая вершина. |
        +-----------------------------------------------------------------+
        """
        ...

    def GetPoint(self, First) -> bool:  # izoneparametersbyborderpoints_getpoint.html
        """
        ## GetPoint - Получить координаты точки

        ### Синтаксис Automation

        ```
        BOOL GetPoint( BOOL First, double * X, double * Y, double * Z );
        ```

        ### Входные параметры

        +------------------------------------------------------------+
        | First | - TRUE - первая вершина, - FALSE - вторая вершина. |
        +------------------------------------------------------------+

        ### Выходные параметры

        +---------------------------------+
        | X, Y, Z | - Координаты вершины. |
        +---------------------------------+

        ### Возвращаемое значение

        +-----------------------------------------+
        | TRUE | - в случае успешного завершения. |
        +-----------------------------------------+
        """
        ...

    def PointParameters(self, First) -> IKompasAPIObject:  # izoneparametersbyborderpoints_pointparameters.html
        """
        ## PointParameters - Получить интерфейс параметров точки

        Тип данных: Указатель на интерфейс IKompasAPIObject

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        PointParameters = Object.PointParameters( First )     # Получить свойство (* )
        PointParameters = Object.GetPointParameters( First )  # Получить свойство (**)
        ```
        """
        ...

    def PointType(self, First) -> int:  # izoneparametersbyborderpoints_pointtype.html
        """
        ## PointType - Тип параметров построения точки

        Тип данных: из перечисления ksPoint3DTypeEnum

        ### Синтаксис Automation

        ```
        PointType = Object.PointType( First )     # Получить свойство (* )
        Object.PointType( First ) = PointType     # Установить свойство (* )
        PointType = Object.GetPointType( First )  # Получить свойство (**)
        Object.SetPointType( First, PointType )   # Установить свойство (**)
        ```

        ### Входные параметры

        +-----------------------------------------------------------------+
        | BOOL | - First - TRUE - первая вершина, FALSE - вторая вершина. |
        +-----------------------------------------------------------------+
        """
        ...

    def SetAssociationObject(self, First, arg1):
        ...

    def SetPoint(self, First, X, Y, Z) -> bool:  # izoneparametersbyborderpoints_setpoint.html
        """
        ## SetPoint - Установить координаты точки

        ### Синтаксис Automation

        ```
        BOOL SetPoint( BOOL First, double X, double Y, double Z );
        ```

        ### Входные параметры

        +------------------------------------------------------------+
        | First | - TRUE - первая вершина, - FALSE - вторая вершина. |
        +------------------------------------------------------------+

        ### Возвращаемое значение

        +-----------------------------------------+
        | TRUE | - в случае успешного завершения. |
        +-----------------------------------------+
        """
        ...

    def SetPointType(self, First, arg1):
        ...




class IZoneParametersByObjects(IKompasAPIObject):  # izoneparametersbyobjects.html
    """
    ## Интерфейс IZoneParametersByObjects

    Интерфейс параметров зоны по суммарному габариту объектов.
    """

    @property
    def BaseObjects(self) -> typing.Any:  # izoneparametersbyobjects_baseobjects.html
        """
        ## BaseObjects - Объекты

        Тип данных: VARIANT

        Позволяет получать и устанавливать опорный объект или массив опорных объектов по которым определяется суммарный габарит зоны.

        Массив объектов возвращается в виде массива SAFEARRAY объектов LPDISPATCH (VT_ARRAY | VT_DISPATCH).

        ### Синтаксис Automation

        ```
        BaseObjects = Object.BaseObjects       # Получить свойство (* )
        Object.BaseObjects = BaseObjects       # Установить свойство (* )
        BaseObjects = Object.GetBaseObjects()  # Получить свойство (**)
        Object.SetBaseObjects( BaseObjects )   # Установить свойство (**)
        ```
        """
        ...
    @BaseObjects.setter
    def BaseObjects(self, value: typing.Any): ...




class IZoneDivisionParametersRegular(IKompasAPIObject):  # izonedivisionparametersregular.html
    """
    ## Интерфейс IZoneDivisionParametersRegular

    Интерфейс параметров разбиения зоны для способа Равномерно по осям.
    """

    @property
    def XCount(self) -> int:  # izonedivisionparametersregular_xcount.html
        """
        ## XCount - Количество по X

        Тип данных: long

        ### Синтаксис Automation

        ```
        XCount = Object.XCount       # Получить свойство (* )
        Object.XCount = XCount       # Установить свойство (* )
        XCount = Object.GetXCount()  # Получить свойство (**)
        Object.SetXCount( XCount )   # Установить свойство (**)
        ```
        """
        ...
    @XCount.setter
    def XCount(self, value: int): ...

    @property
    def YCount(self) -> int:  # izonedivisionparametersregular_ycount.html
        """
        ## YCount - Количество по Y

        Тип данных: long

        ### Синтаксис Automation

        ```
        YCount = Object.YCount       # Получить свойство (* )
        Object.YCount = YCount       # Установить свойство (* )
        YCount = Object.GetYCount()  # Получить свойство (**)
        Object.SetYCount( YCount )   # Установить свойство (**)
        ```
        """
        ...
    @YCount.setter
    def YCount(self, value: int): ...

    @property
    def ZCount(self) -> int:  # izonedivisionparametersregular_zcount.html
        """
        ## ZCount - Количество по Z

        Тип данных: long

        ### Синтаксис Automation

        ```
        ZCount = Object.ZCount       # Получить свойство (* )
        Object.ZCount = ZCount       # Установить свойство (* )
        ZCount = Object.GetZCount()  # Получить свойство (**)
        Object.SetZCount( ZCount )   # Установить свойство (**)
        ```
        """
        ...
    @ZCount.setter
    def ZCount(self, value: int): ...




class IZoneDivisionParametersByPlanes(IKompasAPIObject):  # izonedivisionparametersbyplanes.html
    """
    ## Интерфейс IZoneDivisionParametersByPlanes

    Интерфейс параметров разбиения зоны для способа по набору плоскостей.
    """

    @property
    def Planes(self) -> typing.Any:  # izonedivisionparametersbyplanes_planes.html
        """
        ## Planes - Секущие плоскости

        Тип данных: VARIANT

        Позволяет получать и устанавливать секущую плоскость или массив секущих плоскостей для разбиения зоны.

        Массив объектов возвращается в виде массива SAFEARRAY объектов LPDISPATCH (VT_ARRAY | VT_DISPATC.

        ### Синтаксис Automation

        ```
        Planes = Object.Planes       # Получить свойство (* )
        Object.Planes = Planes       # Установить свойство (* )
        Planes = Object.GetPlanes()  # Получить свойство (**)
        Object.SetPlanes( Planes )   # Установить свойство (**)
        ```
        """
        ...
    @Planes.setter
    def Planes(self, value: typing.Any): ...




class IDynamicCrossSection(IModelObject):  # idynamiccrosssection.html
    """
    ## Интерфейс IDynamicCrossSection

    Интерфейс параметров динамического сечения.
    """

    @property
    def FillCutPlanes(self) -> bool:  # idynamiccrosssection_fillcutplanes.html
        """
        ## FillCutPlanes - Закрашивать сечение

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        FillCutPlanes = Object.FillCutPlanes       # Получить свойство (* )
        Object.FillCutPlanes = FillCutPlanes       # Установить свойство (* )
        FillCutPlanes = Object.GetFillCutPlanes()  # Получить свойство (**)
        Object.SetFillCutPlanes( FillCutPlanes )   # Установить свойство (**)
        ```
        """
        ...
    @FillCutPlanes.setter
    def FillCutPlanes(self, value: bool): ...

    @property
    def StepsCount(self) -> int:  # idynamiccrosssection_stepscount.html
        """
        ## StepsCount - Количество шагов сечения

        Тип данных: long

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        StepsCount = Object.StepsCount       # Получить свойство (* )
        StepsCount = Object.GetStepsCount()  # Получить свойство (**)
        ```
        """
        ...

    def AddStep(self, BuildingType) -> IDynamicCrossSectionStep:  # idynamiccrosssection_addstep.html
        """
        ## AddStep - Добавить шаг

        ### Синтаксис Automation

        ```
        LPDISPATCH AddStep( ksDynamicCrossSectionStepBuildingTypeEnum BuildingType );
        ```

        ### Входные параметры

        +--------------------------------------------------------------------------------------------------------------------------+
        | BuildingType IPropert | - Способ создания шага сечения модели из перечисления ksDynamicCrossSectionStepBuildingTypeEnum. |
        +--------------------------------------------------------------------------------------------------------------------------+

        ### Возвращаемое значение

        Указатель на интерфейс IDynamicCrossSectionStep.
        """
        ...

    def Delete(self) -> bool:  # idynamiccrosssection_delete.html
        """
        ## Delete - Удаление сечения

        ### Синтаксис Automation

        ```
        BOOL Delete();
        ```

        ### Возвращаемое значение

        +-----------------------------------------+
        | TRUE | - в случае успешного завершения. |
        +-----------------------------------------+
        """
        ...

    def Step(self, Index) -> IDynamicCrossSectionStep:  # idynamiccrosssection_step.html
        """
        ## Step - Получить шаг сечения по индексу

        Тип данных: Указатель на интерфейс IDynamicCrossSectionStep.

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        Step = Object.Step( Index )     # Получить свойство (* )
        Step = Object.GetStep( Index )  # Получить свойство (**)
        ```

        ### Входные параметры

        +-----------------------------+
        | long Index | - индекс шага. |
        +-----------------------------+
        """
        ...




class IDynamicCrossSectionStep(IKompasAPIObject):  # idynamiccrosssectionstep.html
    """
    ## Интерфейс IDynamicCrossSectionStep

    Интерфейс Шаг динамического сечения.
    """

    @property
    def BuildingType(self) -> int:  # idynamiccrosssectionstep_buildingtype.html
        """
        ## BuildingType - Тип шага динамического сечения

        Тип данных: из перечисления ksDynamicCrossSectionStepBuildingTypeEnum.

        ### Синтаксис Automation

        ```
        BuildingType = Object.BuildingType       # Получить свойство (* )
        Object.BuildingType = BuildingType       # Установить свойство (* )
        BuildingType = Object.GetBuildingType()  # Получить свойство (**)
        Object.SetBuildingType( BuildingType )   # Установить свойство (**)
        ```
        """
        ...
    @BuildingType.setter
    def BuildingType(self, value: int): ...

    @property
    def Parameters(self) -> IKompasAPIObject:  # idynamiccrosssectionstep_parameters.html
        """
        ## Parameters - Параметры шага

        Тип данных: Указатель на интерфейс IKompasAPIObject

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        Parameters = Object.Parameters       # Получить свойство (* )
        Parameters = Object.GetParameters()  # Получить свойство (**)
        ```
        """
        ...

    def Delete(self) -> bool:  # idynamiccrosssectionstep_delete.html
        """
        ## Delete - Удаление шага

        ### Синтаксис Automation

        ```
        BOOL Delete();
        ```

        ### Возвращаемое значение

        +-----------------------------------------+
        | TRUE | - в случае успешного завершения. |
        +-----------------------------------------+
        """
        ...




class IDynamicCrossSectionsManager(IDispatch):  # idynamiccrosssectionsmanager.html
    """
    ## Интерфейс IDynamicCrossSectionsManager

    Интерфейс Менеджера динамических сечений.
    """

    @property
    def CurrentDynamicCrossSection(self) -> IDynamicCrossSection:  # idynamiccrosssectionsmanager_currentdynamiccrosssection.html
        """
        ## CurrentDynamicCrossSection - Текущее динамическое сечение

        Тип данных: Указатель на интерфейс IDynamicCrossSection.

        ### Синтаксис Automation

        ```
        CurrentDynamicCrossSection = Object.CurrentDynamicCrossSection       # Получить свойство (* )
        Object.CurrentDynamicCrossSection = CurrentDynamicCrossSection       # Установить свойство (* )
        CurrentDynamicCrossSection = Object.GetCurrentDynamicCrossSection()  # Получить свойство (**)
        Object.SetCurrentDynamicCrossSection( CurrentDynamicCrossSection )   # Установить свойство (**)
        ```
        """
        ...
    @CurrentDynamicCrossSection.setter
    def CurrentDynamicCrossSection(self, value: IDynamicCrossSection): ...

    @property
    def DynamicCrossSectionModeOn(self) -> bool:  # idynamiccrosssectionsmanager_dynamiccrosssectionmodeon.html
        """
        ## DynamicCrossSectionModeOn - Включить/отключить режим отображения сечения модели

        Тип данных: BOOL

        ### Синтаксис Automation

        ```
        DynamicCrossSectionModeOn = Object.DynamicCrossSectionModeOn       # Получить свойство (* )
        Object.DynamicCrossSectionModeOn = DynamicCrossSectionModeOn       # Установить свойство (* )
        DynamicCrossSectionModeOn = Object.GetDynamicCrossSectionModeOn()  # Получить свойство (**)
        Object.SetDynamicCrossSectionModeOn( DynamicCrossSectionModeOn )   # Установить свойство (**)
        ```
        """
        ...
    @DynamicCrossSectionModeOn.setter
    def DynamicCrossSectionModeOn(self, value: bool): ...

    @property
    def DynamicCrossSectionsCount(self) -> int:  # idynamiccrosssectionsmanager_dynamiccrosssectionscount.html
        """
        ## DynamicCrossSectionsCount - Количество динамических сечений

        Тип данных: long

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        DynamicCrossSectionsCount = Object.DynamicCrossSectionsCount       # Получить свойство (* )
        DynamicCrossSectionsCount = Object.GetDynamicCrossSectionsCount()  # Получить свойство (**)
        ```
        """
        ...

    def AddDynamicCrossSection(self) -> IDynamicCrossSection:  # idynamiccrosssectionsmanager_adddynamiccrosssection.html
        """
        ## AddDynamicCrossSection - Добавить динамическое сечение

        ### Синтаксис Automation

        ```
        LPDISPATCH AddDynamicCrossSection();
        ```

        ### Возвращаемое значение

        - Указатель на интерфейс IDynamicCrossSection.
        """
        ...

    def DynamicCrossSection(self, Index) -> IDynamicCrossSection:  # idynamiccrosssectionsmanager_dynamiccrosssection.html
        """
        ## DynamicCrossSection - Получить объект по имени, индексу или указателю на размер

        Тип данных: Указатель на интерфейс IDynamicCrossSection.

        Свойство доступно только для чтения

        ### Синтаксис Automation

        ```
        DynamicCrossSection = Object.DynamicCrossSection( Index )     # Получить свойство (* )
        DynamicCrossSection = Object.GetDynamicCrossSection( Index )  # Получить свойство (**)
        ```
        """
        ...




class IDynamicCrossSectionStepParametersByFreePlane(IKompasAPIObject):  # idynamiccrosssectionstepparametersbyfreeplane.html
    """
    ## Интерфейс IDynamicCrossSectionStepParametersByFreePlane

    Интерфейс параметров шага динамического сечения по произвольной плоскости.
    """

    @property
    def LocalCoordinateSystem(self) -> ILocalCoordinateSystem:  # idynamiccrosssectionstepparametersbyfreeplane_localcoordinatesystem.html
        """
        ## LocalCoordinateSystem - Локальная система координат

        Тип данных: Указатель на интерфейс ILocalCoordinateSystem.

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        LocalCoordinateSystem = Object.LocalCoordinateSystem       # Получить свойство (* )
        LocalCoordinateSystem = Object.GetLocalCoordinateSystem()  # Получить свойство (**)
        ```
        """
        ...

    def ReverseDirection(self) -> bool:  # idynamiccrosssectionstepparametersbyfreeplane_reversedirection.html
        """
        ## ReverseDirection - Изменить направление отсечения

        ### Синтаксис Automation

        ```
        BOOL ReverseDirection();
        ```

        ### Возвращаемое значение

        +-----------------------------------------+
        | TRUE | - в случае успешного завершения. |
        +-----------------------------------------+
        """
        ...




class IDynamicCrossSectionStepParametersByOffsetPlane(IKompasAPIObject):  # idynamiccrosssectionstepparametersbyoffsetplane.html
    """
    ## Интерфейс IDynamicCrossSectionStepParametersByOffsetPlane

    Интерфейс параметров шага динамического сечения по смещенной плоскости.
    """

    @property
    def BaseStep(self) -> IDynamicCrossSectionStep:  # idynamiccrosssectionstepparametersbyoffsetplane_basestep.html
        """
        ## BaseStep - Базовый шаг

        Тип данных: Указатель на интерфейс IDynamicCrossSectionStep

        ### Синтаксис Automation

        ```
        BaseStep = Object.BaseStep       # Получить свойство (* )
        Object.BaseStep = BaseStep       # Установить свойство (* )
        BaseStep = Object.GetBaseStep()  # Получить свойство (**)
        Object.SetBaseStep( BaseStep )   # Установить свойство (**)
        ```
        """
        ...
    @BaseStep.setter
    def BaseStep(self, value: IDynamicCrossSectionStep): ...

    @property
    def OffsetPlane(self) -> IPlane3DByOffset:  # idynamiccrosssectionstepparametersbyoffsetplane_offsetplane.html
        """
        ## OffsetPlane - Параметры смещенной плоскости

        Тип данных: Указатель на интерфейс IPlane3DByOffset.

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        OffsetPlane = Object.OffsetPlane       # Получить свойство (* )
        OffsetPlane = Object.GetOffsetPlane()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def PlaneBuildingType(self) -> int:  # idynamiccrosssectionstepparametersbyoffsetplane_planebuildingtype.html
        """
        ## PlaneBuildingType - Тип шага динамического сечения

        Тип данных: из перечисления ksCrossSectionPlaneBuildingTypeEnum.

        ### Синтаксис Automation

        ```
        PlaneBuildingType = Object.PlaneBuildingType       # Получить свойство (* )
        Object.PlaneBuildingType = PlaneBuildingType       # Установить свойство (* )
        PlaneBuildingType = Object.GetPlaneBuildingType()  # Получить свойство (**)
        Object.SetPlaneBuildingType( PlaneBuildingType )   # Установить свойство (**)
        ```
        """
        ...
    @PlaneBuildingType.setter
    def PlaneBuildingType(self, value: int): ...

    def ReverseDirection(self) -> bool:  # idynamiccrosssectionstepparametersbyoffsetplane_reversedirection.html
        """
        ## ReverseDirection - Изменить направление отсечения

        ### Синтаксис Automation

        ```
        BOOL ReverseDirection();
        ```

        ### Возвращаемое значение

        +-----------------------------------------+
        | TRUE | - в случае успешного завершения. |
        +-----------------------------------------+
        """
        ...




class IDynamicCrossSectionStepParametersByRotatedPlane(IKompasAPIObject):  # idynamiccrosssectionstepparametersbyrotatedplane.html
    """
    ## Интерфейс IDynamicCrossSectionStepParametersByRotatedPlane

    Интерфейс параметров шага динамического сечения по плоскости под углом.
    """

    @property
    def BaseStep(self) -> IDynamicCrossSectionStep:  # idynamiccrosssectionstepparametersbyrotatedplane_basestep.html
        """
        ## BaseStep - Базовый шаг

        Тип данных: Указатель на интерфейс IDynamicCrossSectionStep.

        ### Синтаксис Automation

        ```
        BaseStep = Object.BaseStep       # Получить свойство (* )
        Object.BaseStep = BaseStep       # Установить свойство (* )
        BaseStep = Object.GetBaseStep()  # Получить свойство (**)
        Object.SetBaseStep( BaseStep )   # Установить свойство (**)
        ```
        """
        ...
    @BaseStep.setter
    def BaseStep(self, value: IDynamicCrossSectionStep): ...

    @property
    def Plane(self) -> IPlane3DByAngle:  # idynamiccrosssectionstepparametersbyrotatedplane_plane.html
        """
        ## Plane - Плоскость под углом к другой плоскости

        Тип данных: Указатель на интерфейс IPlane3DByAngle.

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        Plane = Object.Plane       # Получить свойство (* )
        Plane = Object.GetPlane()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def PlaneBuildingType(self) -> int:  # idynamiccrosssectionstepparametersbyrotatedplane_planebuildingtype.html
        """
        ## PlaneBuildingType - Тип шага динамического сечения

        Тип данных: из перечисления ksCrossSectionPlaneBuildingTypeEnum.

        ### Синтаксис Automation

        ```
        PlaneBuildingType = Object.PlaneBuildingType       # Получить свойство (* )
        Object.PlaneBuildingType = PlaneBuildingType       # Установить свойство (* )
        PlaneBuildingType = Object.GetPlaneBuildingType()  # Получить свойство (**)
        Object.SetPlaneBuildingType( PlaneBuildingType )   # Установить свойство (**)
        ```
        """
        ...
    @PlaneBuildingType.setter
    def PlaneBuildingType(self, value: int): ...

    def ReverseDirection(self):
        ...




class IDynamicCrossSectionStepParametersByZone(IKompasAPIObject):  # idynamiccrosssectionstepparametersbyzone.html
    """
    ## Интерфейс IDynamicCrossSectionStepParametersByZone

    Интерфейс параметров шага динамического сечения по зоне.
    """

    @property
    def Zone(self) -> IZone:  # idynamiccrosssectionstepparametersbyzone_zone.html
        """
        ## Zone - Зона

        Тип данных: Указатель на интерфейс IZone.

        ### Синтаксис Automation

        ```
        Zone = Object.Zone       # Получить свойство (* )
        Object.Zone = Zone       # Установить свойство (* )
        Zone = Object.GetZone()  # Получить свойство (**)
        Object.SetZone( Zone )   # Установить свойство (**)
        ```
        """
        ...
    @Zone.setter
    def Zone(self, value: IZone): ...




class IDynamicCrossSectionStepParametersByBorderPoints(IKompasAPIObject):  # idynamiccrosssectionstepparametersbyborderpoints.html
    """
    ## Интерфейс IDynamicCrossSectionStepParametersByBorderPoints

    Интерфейс параметров шага динамического сечения по габаритному параллелепипеду, заданному точками.
    """

    @property
    def BuildingType(self) -> int:  # idynamiccrosssectionstepparametersbyborderpoints_buildingtype.html
        """
        ## BuildingType - Вариант построения габарита

        Тип данных: из перечисления ksGabaritBuildingTypeEnum.

        ### Синтаксис Automation

        ```
        BuildingType = Object.BuildingType       # Получить свойство (* )
        Object.BuildingType = BuildingType       # Установить свойство (* )
        BuildingType = Object.GetBuildingType()  # Получить свойство (**)
        Object.SetBuildingType( BuildingType )   # Установить свойство (**)
        ```
        """
        ...
    @BuildingType.setter
    def BuildingType(self, value: int): ...

    def AssociationObject(self, First) -> IModelObject:  # idynamiccrosssectionstepparametersbyborderpoints_associationobject.html
        """
        ## AssociationObject - Установить опорный объект для вершины

        Тип данных: Указатель на интерфейс IModelObject.

        ### Синтаксис Automation

        ```
        AssociationObject = Object.AssociationObject( First )     # Получить свойство (* )
        Object.AssociationObject( First ) = AssociationObject     # Установить свойство (* )
        AssociationObject = Object.GetAssociationObject( First )  # Получить свойство (**)
        Object.SetAssociationObject( First, AssociationObject )   # Установить свойство (**)
        ```

        ### Входные параметры

        +-----------------------------------------------------------------+
        | BOOL | - First - TRUE - первая вершина, FALSE - вторая вершина. |
        +-----------------------------------------------------------------+
        """
        ...

    def GetPoint(self, First) -> bool:  # idynamiccrosssectionstepparametersbyborderpoints_getpoint.html
        """
        ## GetPoint - Получить координату точки

        ### Синтаксис Automation

        ```
        BOOL GetPoint( BOOL First, double * X, double * Y, double * Z );
        ```

        ### Входные параметры

        +----------------------------------------------------------+
        | First | - TRUE - первая вершина, FALSE - вторая вершина. |
        +----------------------------------------------------------+

        ### Выходные параметры

        +--------------------------------+
        | X,Y, Z | - координаты вершины. |
        +--------------------------------+

        ### Возвращаемое значение

        +-----------------------------------------+
        | TRUE | - в случае успешного завершения. |
        +-----------------------------------------+
        """
        ...

    def PointParameters(self, First) -> IKompasAPIObject:  # idynamiccrosssectionstepparametersbyborderpoints_pointparameters.html
        """
        ## PointParameters - Получить интерфейс параметров точки

        Тип данных: Указатель на интерфейс IKompasAPIObject.

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        PointParameters = Object.PointParameters( First )     # Получить свойство (* )
        PointParameters = Object.GetPointParameters( First )  # Получить свойство (**)
        ```
        """
        ...

    def PointType(self, First) -> int:  # idynamiccrosssectionstepparametersbyborderpoints_pointtype.html
        """
        ## PointType - Тип параметров построения точки

        Тип данных: из перечисления ksPoint3DTypeEnum.

        ### Синтаксис Automation

        ```
        PointType = Object.PointType( First )     # Получить свойство (* )
        Object.PointType( First ) = PointType     # Установить свойство (* )
        PointType = Object.GetPointType( First )  # Получить свойство (**)
        Object.SetPointType( First, PointType )   # Установить свойство (**)
        ```

        ### Входные параметры

        +-----------------------------------------------------------------+
        | BOOL | - First - TRUE - первая вершина, FALSE - вторая вершина. |
        +-----------------------------------------------------------------+
        """
        ...

    def SetAssociationObject(self, First, arg1):
        ...

    def SetPoint(self, First, X, Y, Z) -> bool:  # idynamiccrosssectionstepparametersbyborderpoints_setpoint.html
        """
        ## SetPoint - Установить координату точки

        ### Синтаксис Automation

        ```
        BOOL SetPoint( BOOL First, double X, double Y, double Z );
        ```

        ### Входные параметры

        +-----------------------------------------------------------+
        | First  | - TRUE - первая вершина, FALSE - вторая вершина. |
        +-----------------------------------------------------------+
        | X,Y, Z | - координаты вершины.                            |
        +-----------------------------------------------------------+

        ### Возвращаемое значение

        +-----------------------------------------+
        | TRUE | - в случае успешного завершения. |
        +-----------------------------------------+
        """
        ...

    def SetPointType(self, First, arg1):
        ...




class IInsulateManager(IDispatch):  # iinsulatemanager.html
    """
    ## Интерфейс IInsulateManager

    Менеджер изоляции объектов 3D.

    Интерфейс является дополнительным для интерфейса документа сборки IAssemblyDocument.

    Версия КОМПАС v22
    """

    @property
    def InsulateObjects(self) -> bool:  # iinsulatemanager_insulateobjects.html
        """
        ## InsulateObjects - Получить список изолированных объектов

        Тип данных: BOOL

        1.Свойство доступно только для чтения.

        2.Метод возвращает список изолируемых объектов в виде массива SAFEARRAY'я DISPATCH - VT_ARRAY | VT_DISPATCH.

        ### Синтаксис Automation

        ```
        InsulateObjects = Object.InsulateObjects       # Получить свойство (* )
        InsulateObjects = Object.GetInsulateObjects()  # Получить свойство (**)
        ```
        """
        ...

    @property
    def IsInsulateMode(self) -> bool:  # iinsulatemanager_isinsulatemode.html
        """
        ## IsInsulateMode - Признак включенного режима изоляции

        Тип данных: BOOL

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        IsInsulateMode = Object.IsInsulateMode       # Получить свойство (* )
        IsInsulateMode = Object.GetIsInsulateMode()  # Получить свойство (**)
        ```
        """
        ...

    def AddInsulate(self, Objects) -> bool:  # iinsulatemanager_addinsulate.html
        """
        ## AddInsulate - Добавить объект в список изолированных

        ### Синтаксис Automation

        ```
        BOOL AddInsulate( VARIANT Objects ) ;
        ```

        ### Входные параметры

        +-------------------------------------------------------------------------------------------------------+
        | Objects | - Список изолируемых объектов в виде массива SAFEARRAY'я DISPATCH - VT_ARRAY | VT_DISPATCH. |
        +-------------------------------------------------------------------------------------------------------+

        ### Возвращаемое значение

        +--------------------------------+
        | TRUE  | - успешное завершение, |
        +--------------------------------+
        | FALSE | - в случае неудачи.    |
        +--------------------------------+
        """
        ...

    def DeleteInsulate(self, Objects) -> bool:  # iinsulatemanager_deleteinsulate.html
        """
        ## DeleteInsulate - Удалить объект из списка изолированных

        ### Синтаксис Automation

        ```
        BOOL DeleteInsulate( VARIANT Objects ) ;
        ```

        ### Входные параметры

        +-------------------------------------------------------------------------------------------------------------------+
        | Objects | - Список исключаемых из изоляции объектов в виде массива SAFEARRAY'я DISPATCH - VT_ARRAY | VT_DISPATCH. |
        +-------------------------------------------------------------------------------------------------------------------+

        ### Возвращаемое значение

        +--------------------------------+
        | TRUE  | - успешное завершение, |
        +--------------------------------+
        | FALSE | - в случае неудачи.    |
        +--------------------------------+
        """
        ...

    def IsInsulate(self, Object) -> bool:  # iinsulatemanager_isinsulate.html
        """
        ## IsInsulate - Признак изолированности объекта

        Тип данных: BOOL

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        IsInsulate = Object.IsInsulate       # Получить свойство (* )
        IsInsulate = Object.GetIsInsulate()  # Получить свойство (**)
        ```
        """
        ...

    def StartInsulateMode(self, Objects) -> bool:  # iinsulatemanager_startinsulatemode.html
        """
        ## StartInsulateMode - Включить режим изоляции

        ### Синтаксис Automation

        ```
        BOOL StartInsulateMode( VARIANT Objects) ;
        ```

        ### Входные параметры

        +-------------------------------------------------------------------------------------------------------+
        | Objects | - Список изолируемых объектов в виде массива SAFEARRAY'я DISPATCH - VT_ARRAY | VT_DISPATCH. |
        +-------------------------------------------------------------------------------------------------------+

        ### Возвращаемое значение

        +--------------------------------+
        | TRUE  | - успешное завершение, |
        +--------------------------------+
        | FALSE | - в случае неудачи.    |
        +--------------------------------+
        """
        ...

    def StopInsulateMode(self) -> bool:  # iinsulatemanager_stopinsulatemode.html
        """
        ## StopInsulateMode - Выключить режим изоляции

        ### Синтаксис Automation

        ```
        BOOL StopInsulateMode() ;
        ```

        ### Возвращаемое значение

        +--------------------------------+
        | TRUE  | - успешное завершение, |
        +--------------------------------+
        | FALSE | - в случае неудачи.    |
        +--------------------------------+
        """
        ...




class IProductDataManager(IDispatch):  # iproductdatamanager.html
    """
    ## Интерфейс IProductDataManager

    Менеджер Дерева СЧИ.
    """

    @property
    def MetaProductInfo(self) -> str:  # iproductdatamanager_metaproductinfo.html
        """
        ## MetaProductInfo - Метаданные дерева СЧИ

        Тип данных: BSTR

        ### Синтаксис Automation

        ```
        MetaProductInfo = Object.MetaProductInfo       # Получить свойство (* )
        Object.MetaProductInfo = MetaProductInfo       # Установить свойство (* )
        MetaProductInfo = Object.GetMetaProductInfo()  # Получить свойство (**)
        Object.SetMetaProductInfo( MetaProductInfo )   # Установить свойство (**)
        ```
        """
        ...
    @MetaProductInfo.setter
    def MetaProductInfo(self, value: str): ...

    @property
    def ReferenceData(self) -> str:  # iproductdatamanager_referencedata.html
        """
        ## ReferenceData - Справочные данные свойств

        Тип данных: BSTR

        ### Синтаксис Automation

        ```
        ReferenceData = Object.ReferenceData       # Получить свойство (* )
        Object.ReferenceData = ReferenceData       # Установить свойство (* )
        ReferenceData = Object.GetReferenceData()  # Получить свойство (**)
        Object.SetReferenceData( ReferenceData )   # Установить свойство (**)
        ```
        """
        ...
    @ReferenceData.setter
    def ReferenceData(self, value: str): ...

    def AddProductObject(self, Parent, Name, ObjectType) -> bool:  # iproductdatamanager_addproductobject.html
        """
        ## AddProductObject - Добавить объект дерева СЧИ по идентификатору

        В зависимости от типа информационного объекта выбирается владелец объекта. Например, для вставок моделей и тел владелец - исполнение.

        ### Синтаксис Automation

        ```
        LPDISPATCH AddProductObject( IPropertyKeeper * Parent, BSTR Name, ksProductObjectTypeEnum ObjectType );
        ```

        ### Входные параметры

        +--------------------------------------------------+
        | Parent     | - владелец информационного объекта, |
        +--------------------------------------------------+
        | Name       | - имя объекта,                      |
        +--------------------------------------------------+
        | ObjectType | - тип объекта.                      |
        +--------------------------------------------------+

        ### Возвращаемое значение

        +-----------------------------------------+
        | TRUE | - в случае успешного завершения. |
        +-----------------------------------------+
        """
        ...

    def AddReferenceData(self, ReferenceDataType, ReferenceDataInfo):  # iproductdatamanager_addreferencedata.html
        """
        ## AddReferenceData - Добавить справочные данные

        ### Синтаксис Automation

        ```
        BSTR AddReferenceData( BSTR ReferenceDataType, BSTR ReferenceDataInfo );
        ```

        ### Входные параметры

        +----------------------------------------------------+
        | ReferenceDataType | - тип справочных данных,       |
        +----------------------------------------------------+
        | ReferenceDataInfo | - заголовок справочных данных. |
        +----------------------------------------------------+

        ### Возвращаемое значение

        - идентификатор справочных данных.
        """
        ...

    def DeleteProductObject(self, UniqueMetaObjectKey) -> bool:  # iproductdatamanager_deleteproductobject.html
        """
        ## DeleteProductObject - Удалить объект из дерева СЧИ по идентификатору

        ### Синтаксис Automation

        ```
        BOOL DeleteProductObject( BSTR UniqueMetaObjectKey );
        ```

        ### Входные параметры

        +----------------------------------------------------+
        | UniqueMetaObjectKey | - идентификатор объекта СЧИ. |
        +----------------------------------------------------+

        ### Возвращаемое значение

        +-----------------------------------------+
        | TRUE | - в случае успешного завершения. |
        +-----------------------------------------+
        """
        ...

    def DeleteReferenceData(self, ReferenceDataType, ReferenceDataId) -> bool:  # iproductdatamanager_deletereferencedata.html
        """
        ## DeleteReferenceData - Удалить справочные данные

        ### Синтаксис Automation

        ```
        BOOL DeleteReferenceData( BSTR ReferenceDataType, BSTR ReferenceDataId );
        ```

        ### Входные параметры

        +--------------------------------------------------------+
        | ReferenceDataType | - тип справочных данных,           |
        +--------------------------------------------------------+
        | ReferenceDataId   | - идентификатор справочных данных. |
        +--------------------------------------------------------+

        ### Возвращаемое значение

        +-----------------------------------------+
        | TRUE | - в случае успешного завершения. |
        +-----------------------------------------+
        """
        ...

    def FindProductObjectByGeometry(self, Geometry) -> IPropertyKeeper:  # iproductdatamanager_findproductobjectbygeometry.html
        """
        ## FindProductObjectByGeometry - Найти объект дерева СЧИ по геометрии

        ### Синтаксис Automation

        ```
        LPDISPATCH FindProductObjectByGeometry( IKompasAPIObject * Geometry );
        ```

        ### Входные параметры

        +-----------------------------------+
        | Geometry | - указатель на объект. |
        +-----------------------------------+

        Версия КОМПАС v22

        ### Возвращаемое значение

        - Указатель на интерфейс IPropertyKeeper.
        """
        ...

    def Geometry(self, PropObject) -> typing.Any:  # iproductdatamanager_geometry.html
        """
        ## Geometry - Список подключенных документов

        Тип данных: VARIANT

        Свойство возвращает переменную типа VARIANT на безопасный массив SAFEARRAY (VT_ARRAY | VT_DISPATCH).

        ### Синтаксис Automation

        ```
        Geometry = Object.Geometry( PropObject )     # Получить свойство (* )
        Object.Geometry( PropObject ) = Geometry     # Установить свойство (* )
        Geometry = Object.GetGeometry( PropObject )  # Получить свойство (**)
        Object.SetGeometry( PropObject, Geometry )   # Установить свойство (**)
        ```

        ### Входные параметры

        +-----------------------------------------------------------+
        | IPropertyKeeper * PropObject | - указатель на объект СЧИ. |
        +-----------------------------------------------------------+
        """
        ...

    def GetObjectLinkPropertyValue(self, PropObject, EmbodimentObject, PropertyPath):
        ...

    def GetObjectPropertyValue(self, PropObject, PropertyPath):
        ...

    def ObjectAttachedDocuments(self, PropObject) -> typing.Any:  # iproductdatamanager_objectattacheddocuments.html
        """
        ## ObjectAttachedDocuments - Список подключенных к объекту документов

        Тип данных: VARIANT

        Свойство возвращает переменную типа VARIANT на безопасный массив имен файлов, связанных с объектом СЧИ SAFEARRAY (VT_ARRAY | VT_BSTR).

        ### Синтаксис Automation

        ```
        ObjectAttachedDocuments = Object.ObjectAttachedDocuments( PropObject )     # Получить свойство (* )
        Object.ObjectAttachedDocuments( PropObject ) = ObjectAttachedDocuments     # Установить свойство (* )
        ObjectAttachedDocuments = Object.GetObjectAttachedDocuments( PropObject )  # Получить свойство (**)
        Object.SetObjectAttachedDocuments( PropObject, ObjectAttachedDocuments )   # Установить свойство (**)
        ```

        ### Входные параметры

        +-----------------------------------------------------------+
        | IPropertyKeeper * PropObject | - указатель на объект СЧИ. |
        +-----------------------------------------------------------+
        """
        ...

    def ObjectAttribute(self, PropObject, PropertyPath, Attribute):
        ...

    def ObjectEmbodiments(self, PropObject):
        ...

    def ObjectLinkAttribute(self, PropObject, EmbodimentObject, PropertyPath, Attribute):
        ...

    def ObjectMetaProductInfo(self, PropObject) -> str:  # iproductdatamanager_objectmetaproductinfo.html
        """
        ## ObjectMetaProductInfo - Метаданные объекта дерева СЧИ

        Тип данных: BSTR

        ### Синтаксис Automation

        ```
        ObjectMetaProductInfo = Object.ObjectMetaProductInfo( PropObject )     # Получить свойство (* )
        Object.ObjectMetaProductInfo( PropObject ) = ObjectMetaProductInfo     # Установить свойство (* )
        ObjectMetaProductInfo = Object.GetObjectMetaProductInfo( PropObject )  # Получить свойство (**)
        Object.SetObjectMetaProductInfo( PropObject, ObjectMetaProductInfo )   # Установить свойство (**)
        ```

        ### Входные параметры

        +-----------------------------------------------------------+
        | IPropertyKeeper * PropObject | - указатель на объект СЧИ. |
        +-----------------------------------------------------------+
        """
        ...

    def ObjectSpcUsed(self, PropObject, SpcUsed):
        ...

    def ProductObject(self, UniqueMetaObjectKey) -> IPropertyKeeper:  # iproductdatamanager_productobject.html
        """
        ## ProductObject - Получить объект дерева СЧИ по идентификатору

        Тип данных: Указатель на интерфейс IPropertyKeeper

        Свойство доступно только для чтения.

        ### Синтаксис Automation

        ```
        ProductObject = Object.ProductObject( UniqueMetaObjectKey )     # Получить свойство (* )
        ProductObject = Object.GetProductObject( UniqueMetaObjectKey )  # Получить свойство (**)
        ```

        ### Входные параметры

        +----------------------------------------------------------------+
        | BSTR UniqueMetaObjectKey | - уникальный идентификатор объекта. |
        +----------------------------------------------------------------+
        """
        ...

    def ProductObjects(self, Filter) -> typing.Any:  # iproductdatamanager_productobjects.html
        """
        ## ProductObjects - Получить массив объектов дерева СЧИ

        Тип данных: VARIANT

        Свойство доступно только для чтения.

        Фильтр формируется из комбинации нужных типов объектов из перечисления ksProductObjectTypeEnum.

        ### Синтаксис Automation

        ```
        ProductObjects = Object.ProductObjects( Filter )     # Получить свойство (* )
        ProductObjects = Object.GetProductObjects( Filter )  # Получить свойство (**)
        ```

        ### Входные параметры

        +-------------------------------------------+
        | long Filter | - фильтр по типам объектов. |
        +-------------------------------------------+
        """
        ...

    def ReferenceDataIds(self, ReferenceDataType) -> typing.Any:  # iproductdatamanager_referencedataids.html
        """
        ## ReferenceDataIds - Идентификаторы справочных данных

        Тип данных: VARIANT

        Свойство доступно только для чтения.

        Свойство возвращает переменную типа VARIANT на безопасный массив идентификаторов справочных данных SAFEARRAY (VT_ARRAY | VT_BSTR).

        ### Синтаксис Automation

        ```
        ReferenceDataIds = Object.ReferenceDataIds( ReferenceDataType )     # Получить свойство (* )
        ReferenceDataIds = Object.GetReferenceDataIds( ReferenceDataType )  # Получить свойство (**)
        ```
        """
        ...

    def ReferenceDataInfo(self, ReferenceDataType, ReferenceDataId):
        ...

    def SetGeometry(self, PropObject, arg1):
        ...

    def SetObjectAttachedDocuments(self, PropObject, arg1):
        ...

    def SetObjectLinkPropertyValue(self, PropObject, EmbodimentObject, PropertyPath, Value):
        ...

    def SetObjectMetaProductInfo(self, PropObject, arg1):
        ...

    def SetObjectPropertyValue(self, PropObject, PropertyPath, Value):
        ...

    def SetObjectSpcUsed(self, PropObject, SpcUsed, arg2):
        ...

    def SetReferenceDataInfo(self, ReferenceDataType, ReferenceDataId, arg2):
        ...




class ksDocumentFileNotify():  # ksdocumentfilenotify.html
    """
    ## Интерфейс событий документа; работа с файлом ksDocumentFileNotify/IDocumentFileNotify

    +-----------------------------------------------+
    | ksDocumentFileNotify | - интерфейс Automation |
    +-----------------------------------------------+
    | IDocumentFileNotify  | - интерфейс COM        |
    +-----------------------------------------------+

    События интерфейса позволяют контролировать состояние документа.

    Источники событий для подписки:

    • API 7,

    • API 5.
    """

    def close(self):
        ...




class ksStampNotify():  # ksstampnotify.html
    """
    ## Интерфейс событий основной надписи графического документа ksStampNotify/IStampNotify

    +----------------------------------------+
    | ksStampNotify | - интерфейс Automation |
    +----------------------------------------+
    | IStampNotify  | - интерфейс COM        |
    +----------------------------------------+

    Позволяют контролировать состояние основной надписи.

    В Automation источником событий для подписки является указатель на интерфейс ksStamp.

    В СOM контейнером для подписки является указатель на документ.
    """

    def close(self):
        ...




class ksSelectionMngNotify():  # ksselectionmngnotify.html
    """
    ## Интерфейс событий менеджера выделенных объектов ksSelectionMngNotify/ISelectionMngNotify

    +-----------------------------------------------+
    | ksSelectionMngNotify | - интерфейс Automation |
    +-----------------------------------------------+
    | ISelectionMngNotify  | - интерфейс COM        |
    +-----------------------------------------------+

    Позволяют контролировать выделение объектов документа.

    В Automation источник событий для графического документа SelectionMngNotify можно получить при помощи метода ksDocument2D::GetSelectionMngNotify.

    Для документа-модели источником является Менеджер выделенных объектов ksSelectionMng.

    В СOM контейнером для подписки на данный интерфейс является указатель на графический документ или документ-модель.
    """

    def close(self):
        ...




MajorVersion: int = 1


MinorVersion: int = 0


LibraryFlags: int = 8


LCID: int = 0


class IApplicationServises(IDispatch):
    def CheckDocumentMatchingNames(self, Document):
        ...

    def CreateModelSnapshot(self, Path, Options):
        ...

    def GetCurrentUniqueName(self, Document):
        ...

    def ReloadChangedObserverFiles(self):
        ...




RecordMap: dict = ...


CLSIDToClassMap: dict = ...


CLSIDToPackageMap: dict = ...


VTablesToPackageMap: dict = ...


VTablesToClassMap: dict = ...


NamesToIIDMap: dict = ...


_in_gencache_: int = 1
